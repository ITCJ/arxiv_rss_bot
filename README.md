# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-23 06:00:08 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ACE-Sync: An Adaptive Cloud-Edge Synchronization Framework for Communication-Efficient Large-Scale Distributed Model Training](https://arxiv.org/abs/2512.18127)

**Authors**: Yi Yang, Ziyu Lin, Liesheng Wei  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2512.18127v1  

#### Abstract
Large-scale deep learning models impose substantial communication overh ead in distributed training, particularly in bandwidth-constrained or heterogeneous clo ud-edge environments. Conventional synchronous or fixed-compression techniques o ften struggle to balance communication cost, convergence st...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ACE-Sync: An Adaptive Cloud-Edge Synchronization Framework for Communication-Efficient Large-Scale Distributed Model Training

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型深度学习模型在分布式训练中面临严重的 **communication overhead**（通信开销），尤其是在带宽受限或设备异构的 **cloud-edge 环境** 中。传统的全同步（FullSync）或固定压缩策略（如 Top-k sparsification、量化）难以在通信成本、收敛稳定性和模型精度之间取得良好平衡。

### 提出的新方法：ACE-Sync 框架
作者提出 **ACE-Sync**（Adaptive Cloud-Edge Synchronization Framework），一个面向大规模分布式训练的自适应云边协同同步框架，其核心创新包括：

1. ✅ **基于注意力机制的梯度重要性预测器（Attention-based Gradient Importance Predictor）**  
   - 利用轻量级 attention 模块动态评估每个参数/梯度对收敛的重要性。
   - 结合 **时间维度**（历史梯度幅值与方差）和 **结构维度**（层敏感性、参数密度等）进行双分支建模。

2. ✅ **差异化的参数压缩策略（Differentiated Parameter Compression Strategy）**  
   - 高重要性参数：全精度传输，高频同步。
   - 低重要性参数：采用混合量化-稀疏化（hybrid quantization-sparsification）并缓存延迟发送。
   - 引入 **残差误差补偿（residual-based error compensation）**，累积量化误差以保障长期收敛。

3. ✅ **分层云边协调机制（Hierarchical Cloud-Edge Coordination Mechanism）**  
   - 边缘节点本地积累更新，选择性上传关键梯度。
   - 云端负责全局聚合、调度同步频率，并通过 **knapsack-based 优化** 在带宽预算下最大化重要梯度保留。
   - 支持设备聚类（device clustering）实现跨设备个性化与稳定性控制。

### 相比现有方法的优势
| 方面 | 传统方法（如 FullSync, Top-k, FedAvg） | ACE-Sync |
|------|----------------------------------------|---------|
| 同步策略 | 固定频率、统一压缩 | 动态自适应，按参数重要性分级处理 |
| 压缩方式 | 静态 sparsification / quantization | 自适应压缩-扩展（compression-expansion scheduling） |
| 架构设计 | 单一中心化或去中心化 | 分层云边协同，兼顾效率与可控性 |
| 收敛保障 | 易受压缩失真影响 | 引入 error feedback 和 divergence 控制 |

> 💡 **核心优势**：ACE-Sync 实现了 **通信高效性** 与 **模型准确性** 的双赢，在大幅降低通信量的同时保持接近全同步的精度。

---

## 2. 核心实验方法和设置

### 数据集
- 主要任务：基于 **Transformer 的语言建模**。
- 使用数据：
  - **OpenWebText2** 和 **C4（Colossal Clean Crawled Corpus）**：提供高质量文本序列用于预训练。
  - 序列长度为 512–1024 tokens，共约 80–90 million 样本。
- 补充数据：
  - 来自边缘设备的真实 **telemetry traces**（带宽、延迟、能耗、计算能力等元数据），模拟真实 cloud-edge 场景。
  - 早期训练阶段的 **gradient snapshots**，用于监督重要性预测模块。

### 实验设置
- **硬件平台**：
  - **云端**：16 台 NVIDIA A100 GPU（数据中心高带宽网络）
  - **边缘端**：64 台异构设备（Jetson AGX Xavier、ARM 加速器、低功耗 CPU）
- **网络模拟**：
  - 带宽范围：5–200 Mbps
  - 延迟范围：10–300 ms
- **模型配置**：
  - 模型：350M 参数的 Transformer
  - 优化器：AdamW
  - 批大小：每边缘节点 64
  - 总训练轮数：50 epochs，重复三次取平均

### 评估指标
| 类别 | 指标 |
|------|------|
| **通信效率** | 总通信量（GB）、每 epoch 传输体积（MB）、同步延迟 |
| **模型性能** | Top-1 Accuracy (%)、Perplexity |
| **训练动态** | 收敛所需 epoch 数、训练损失曲线、梯度发散度（divergence） |

### 对比基线方法
1. **FullSync**：标准全精度同步，作为性能上限基准。
2. **Top-k Sparsification**：仅同步 Top-k 最大梯度，典型稀疏化方法。
3. **FedAvg-Periodic Sync**：联邦学习常用周期性同步策略。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| 方法 | Top-1 Accuracy (%) | Perplexity | Communication Cost (GB) | Convergence Epochs |
|------|--------------------|-----------|--------------------------|---------------------|
| FullSync | 82.4 | 18.7 | 112.5 | 41 |
| Top-k Sparsification | 80.1 | 20.3 | 68.4 | 45 |
| FedAvg-Periodic Sync | 78.9 | 21.6 | 52.1 | 47 |
| **ACE-Sync (Proposed)** | **82.1** | **18.9** | **44.7** | **39** |

### 与基线对比结果
- 📉 **通信成本下降 60%**：从 FullSync 的 112.5 GB 降至 **44.7 GB**，优于 Top-k（68.4 GB）和 FedAvg（52.1 GB）。
- ✅ **精度几乎无损**：Top-1 Accuracy 仅比 FullSync 低 **0.3%**（82.1% vs 82.4%），显著优于其他压缩方法（Top-k 低 2.3%，FedAvg 低 3.5%）。
- ⚡ **加速收敛**：在 **39 个 epoch** 内完成收敛，快于 FullSync（41）、Top-k（45）和 FedAvg（47）。
- 📈 **更低困惑度（Perplexity）**：18.9，接近 FullSync（18.7），明显优于 Top-k（20.3）和 FedAvg（21.6）。

### 收敛曲线分析（Figure 2）
- ACE-Sync 在早期 epoch（如第 20 轮）即表现出更陡峭的损失下降趋势（loss ≈ 0.55），显著优于 FullSync（~0.70）和 Top-k（~0.82）。
- 曲线平稳且无震荡，说明其自适应机制有效避免了因压缩导致的训练不稳定问题。

> ❗ 文中未明确提及消融实验（ablation study），但强调了各组件（attention estimator、error compensation、hierarchical control）共同作用是实现高性能的关键。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **自适应同步优于静态压缩**：基于参数重要性的动态选择机制能显著减少冗余通信，同时保留关键更新。
2. ✅ **云边协同提升可扩展性**：分层架构使系统能在异构资源条件下灵活调度，适用于大规模部署。
3. ✅ **高通信压缩率下仍可维持高精度**：即使通信量减少 60%，模型质量损失极小（<0.3%），验证了方法的有效性与鲁棒性。
4. ✅ **加快收敛速度**：优先同步重要梯度有助于更快进入稳定优化区域，反直觉地实现了“压缩反而提速”。

### 方法的局限性
- 当前框架依赖一个额外的 **attention 模块** 来估计重要性，虽轻量但仍引入一定计算开销。
- 实验集中在语言模型（Transformer），在 CV 或多模态任务上的泛化能力有待验证。
- 网络状态建模基于经验函数（如指数映射带宽→压缩率），缺乏更精细的在线学习机制。

### 未来工作方向
1. 将 **Reinforcement Learning 或 Meta-Learning** 引入同步调度器，进一步提升动态适应能力。
2. 扩展至 **multi-tenant 或跨云联邦学习环境**，支持更复杂的边缘生态系统。
3. 探索 **hardware-software co-design**，例如结合 network-aware GPU kernels 或可编程交换机，进一步降低通信延迟。
4. 应用于 **foundation models 和 multi-modal architectures**，研究其在超大规模场景下的扩展行为。

---

## 总结
ACE-Sync 提出了一种新颖的、基于重要性感知的自适应云边同步框架，在保证模型精度的前提下，实现了高达 **60% 的通信开销削减** 和 **更快的收敛速度**。该方法通过 **attention-based importance estimation + hierarchical cloud-edge coordination + adaptive compression-expansion** 的组合设计，为解决大规模分布式训练中的通信瓶颈提供了高效、可扩展且准确保持的解决方案，具有重要的工程应用前景。

</details>

---

### 2. [Towards Efficient Agents: A Co-Design of Inference Architecture and System](https://arxiv.org/abs/2512.18337)

**Authors**: Weizhe Lin, Hui-Ling Zhen, Shuai Yang, Xian Wang, Renxi Liu, Hanting Chen, Wangze Zhang, Chuansai Zhou, Yiming Li, Chen Chen, Xing Li, Zhiyuan Yang, Xiaosong Li, Xianzhi Yu, Zhenhua Dong, Mingxuan Yuan, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 14.5  
**Type**: new  
**ArXiv ID**: 2512.18337v1  

#### Abstract
The rapid development of large language model (LLM)-based agents has unlocked new possibilities for autonomous multi-turn reasoning and tool-augmented decision-making. However, their real-world deployment is hindered by severe inefficiencies that arise not from isolated model inference, but from the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Towards Efficient Agents: A Co-Design of Inference Architecture and System*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于大语言模型（LLM）的自主智能体（Agents）在实际部署中面临严重的效率瓶颈。传统优化方法（如量化、剪枝、连续批处理等）主要关注单次推理的吞吐量（如 Tokens Per Second, TPS），而忽略了智能体特有的**多轮循环推理、上下文膨胀（context explosion）、异构工具调用**等系统级延迟累积问题。这些方法在提升单步速度的同时，可能因精度下降导致推理路径不稳定，反而增加总任务完成时间。

### 提出的新方法与思路
本文提出了 **AgentInfer**，一个面向高效智能体的端到端协同设计框架，通过将推理架构与系统优化深度结合，实现跨层次的协同加速。其核心思想是：**优化目标应从“每token速度”转向“任务完成时间”**。

AgentInfer 包含四个协同工作的模块：

- **AgentCollab**：一种分层双模型协作推理框架。由小模型（small model）处理常规推理和工具调用，大模型（large model）负责战略规划和在小模型陷入停滞时进行“救援”。通过结构化的 **Progress Check** 机制实现动态角色切换，平衡效率与准确性。
  
- **AgentCompress**：语义压缩机制。通过轻量级异步压缩，对智能体的记忆（尤其是工具返回结果）进行提炼和重组，减少上下文长度，避免注意力计算成本随序列长度二次增长。

- **AgentSched**：缓存感知的混合调度器。提出基于“影子价格”（shadow price）的动态控制器，在 **Shortest-Job-First (SJF)** 和 **KVCache保留模式** 之间自适应切换，兼顾短请求响应延迟和长会话的缓存命中率。

- **AgentSAM**：基于后缀自动机（Suffix Automaton, SAM）的推测解码方法。利用跨会话的语义记忆构建 SAM，复用历史推理片段进行低开销的推测生成，提升解码效率。

### 相比现有方法的优势
- **系统级视角**：不同于孤立优化单个组件，AgentInfer 从智能体的完整生命周期出发，协同优化推理逻辑与底层系统。
- **认知稳定性保障**：通过 AgentCollab 和 AgentCompress，确保在压缩和协作过程中不破坏智能体的推理连贯性和情境感知能力。
- **真实性能提升**：实验证明，该框架在保持准确率的前提下，显著降低无效 token 消耗，实现端到端任务延迟的实质性加速。

---

## 2. 核心实验方法和设置

### 数据集
- **BrowseComp-zh**：一个多步网页搜索任务基准，要求智能体通过多轮 `web_search`、`url_crawl`、`document_qa` 等工具调用完成复杂查询，适合测试深度研究型智能体。
- **DeepDiver**：用于验证整体系统的复杂智能体任务环境。

### 实验设置
- **模型**：
  - 大模型（ML）：`openPangu-DeepDiverV2-38B`
  - 小模型（Ms）：`openPangu-DeepDiverV2-7B`
  - 压缩模型：`openPangu-7B`
- **硬件平台**：Ascend 910B3 NPUs，使用 `vLLM-Ascend v0.9.1` 作为推理引擎。
- **并行配置**：测试不同 Tensor Parallelism (TP) 和并发会话数（Nparallel）下的性能。

### 评估指标
- **End-to-End Latency**：任务完成的墙钟时间。
- **QPS (Queries Per Second)**：系统吞吐量。
- **Token Efficiency**：有效生成 token 数与总计算量的比率。
- **Accuracy / Task Success Rate**：任务正确完成的比例。
- **KV Cache Hit Rate**：缓存命中率，反映内存管理效率。
- **Total Token Consumption**：总 token 消耗量，衡量资源开销。

### 基线方法对比
- **Baseline**：纯大模型或纯小模型的原生智能体。
- **FCFS**：先来先服务调度。
- **SJF**：最短作业优先调度。
- **标准 SAM 推测解码**：仅基于当前会话上下文的推测解码。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **端到端加速比**：AgentInfer 在不同负载下实现了 **1.8× 到 2.5×** 的端到端速度提升。
- **无效 token 消耗**：相比基线，**减少超过 50%**。
- **QPS 提升**：在高并发（Nparallel=16）场景下，整体 QPS 达到基线的 **2.52×**。
- **KV Cache Hit Rate**：AgentSched 将平均缓存命中率从 FCFS 的 63% 提升至 **72%**，相对提升 14.3%。
- **Speculative Hit Rate (SHR)**：AgentSAM 相比标准 SAM 显著提高推测命中率，尤其在长上下文场景下优势明显。

### 与基线方法的对比结果
| 模块 | 对比项 | 结果 |
|------|--------|------|
| **AgentCollab** | 准确率 vs 小模型 | 33.8% vs 18.3% |
| | 速度 vs 大模型 | 1.32× 加速 |
| **AgentCompress** | 上下文长度 | 从 >40K 压缩至 ~20K |
| | 端到端延迟降低 | 最高达 **42.1%**（TP=1, Nparallel=8） |
| **AgentSched** | 端到端延迟 | 比 FCFS 降低 **9.6%** |
| | KV Cache Hit Rate | 比 SJF 提升 **14.3%**（72% vs 58%） |
| **AgentSAM** | LLM 解码延迟 | 降低 **26.0%** |
| | 端到端延迟 | 降低 **21.2%** |

### 消融实验结果
- **AgentCompress 组件消融**（Table 9）：
  - 移除 **reasoning traces**：端到端延迟增加 **31.98%**，对话轮次增加。
  - 移除 **recent tool responses**：延迟增加 **29.50%**，轮次增加 26%。
  - 表明保留推理轨迹和近期工具输出对维持认知连续性至关重要。

- **系统集成效果**（Table 13）：
  - 各模块增益可叠加：
    - 仅 AgentCollab：1.32× QPS
    - + AgentCompress：1.57×
    - + AgentSched：1.71×
    - + AgentSAM：**1.97×**（Nparallel=4）
  - 高并发下增益更显著，最终达到 **2.52×** QPS。

---

## 4. 关键结论和发现

### 主要发现
1. **传统优化指标（如 TPS）在智能体场景下具有误导性**：单纯追求解码速度可能导致推理质量下降，引发更多重试和循环，反而延长总任务时间。
2. **智能体效率需系统级协同设计**：必须将推理策略（如双模型协作、语义压缩）与系统机制（如缓存感知调度、推测解码）统一考虑。
3. **认知稳定性是高效的前提**：压缩和加速不能以牺牲智能体的推理连贯性为代价，**保留 reasoning memory** 是避免“推理爆炸”的关键。
4. **AgentInfer 实现了效率与能力的平衡**：在几乎不损失准确率的情况下，实现 1.8×–2.5× 的端到端加速，验证了“任务完成时间”优化范式的有效性。

### 方法的局限性
- **依赖高质量 Progress Check 信号**：AgentCollab 的效果依赖于模型能否准确判断自身是否陷入停滞，若小模型无法正确 self-evaluate，可能导致误判。
- **跨会话记忆的泛化能力**：AgentSAM 的性能依赖于历史会话与当前任务的语义相似性，在高度独特或新颖的任务上可能收益有限。
- **调度策略的参数敏感性**：AgentSched 中的影子价格机制需要合理调参以适应不同负载。

### 未来工作方向
- **自动化 Progress Check 设计**：探索更鲁棒的自我评估机制，减少对人工模板的依赖。
- **动态调整压缩粒度**：根据任务类型自适应选择压缩强度与保留信息。
- **扩展至多模态智能体**：将框架应用于视觉、语音等多模态交互场景。
- **长期记忆与知识演化**：结合向量数据库或图结构，支持智能体在长时间运行中的知识积累与自我进化。

--- 

> **总结**：本论文通过提出 **AgentInfer** 框架，系统性地解决了 LLM 智能体在实际部署中的效率瓶颈。其核心贡献在于将推理架构与系统优化协同设计，证明了“任务完成时间”而非“每token速度”才是衡量智能体效率的关键指标。实验表明，该方法在保持高准确率的同时，实现了显著的端到端加速，为构建高效、可靠、可扩展的自主智能体系统提供了重要实践路径。

</details>

---

### 3. [Efficient Mixture-of-Agents Serving via Tree-Structured Routing, Adaptive Pruning, and Dependency-Aware Prefill-Decode Overlap](https://arxiv.org/abs/2512.18126)

**Authors**: Zijun Wang, Yijiahao Qi, Hanqiu Chen, Zishen Wan, Gongjin Sun, Dongyang Li, Shuyi Pei, Cong Hao  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.18126v1  

#### Abstract
Mixture-of-Agents (MoA) inference can suffer from dense inter-agent communication and low hardware utilization, which jointly inflate serving latency. We present a serving design that targets these bottlenecks through an algorithm-system co-design. First, we replace dense agent interaction graphs wi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Mixture-of-Agents Serving via Tree-Structured Routing, Adaptive Pruning, and Dependency-Aware Prefill-Decode Overlap

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Mixture-of-Agents (MoA)** 推理系统存在两大瓶颈：
1. **冗余的代理间通信**：采用全连接（all-to-all）拓扑结构导致大量低效甚至无意义的信息交换，增加端到端延迟（E2E latency）。
2. **硬件利用率低下**：由于依赖关系强、调度僵化，GPU 在等待上游输出时频繁空转；同时现有 LLM serving 框架未针对 MoA 的跨代理依赖进行优化。

这些问题共同导致 MoA 系统虽然在准确率上有潜力，但服务延迟高、资源浪费严重。

---

### 提出的新方法：Faster-MoA
作者提出 **Faster-MoA**，一个算法-系统协同设计框架，从三个层面优化 MoA 服务效率：

#### （1）**Hierarchical Tree Topology（分层树状拓扑）**
- 将传统 all-to-all 连接替换为 **树形层级结构**。
- 每层代理被划分为若干 cluster，下一层每个代理仅接收对应 cluster 的输出，实现局部聚合 → 全局融合。
- **优势**：
  - 显著减少冗余通信和输入上下文长度；
  - 避免单个慢速代理拖累整层进度（即缓解“straggler”问题）；
  - 支持不同分支并行执行，降低整体延迟。

#### （2）**Dynamic Early-Exit (EE) Mechanism（动态早退机制）**
- 引入基于语义相似性和置信度信号的运行时决策机制。
- 当小规模 LLM 生成的答案已足够一致且高质量时，提前终止对大规模 LLM 的调用。
- 使用 **Frobenius Cosine Similarity (FCS)** 和几何平均置信度计算综合质量评分 $ Q = \sqrt{C \cdot B} $，以概率触发早退。
- **优势**：
  - 动态剪枝不必要的大模型推理任务；
  - 减少 GPU 等待时间，在简单任务上显著提速。

#### （3）**Dependency-Aware Incremental Prefilling（依赖感知的增量预填充）**
- 利用 **KV Cache 复用** 技术，在前驱代理解码过程中逐步将 token 流式传输给后继代理进行增量 prefill。
- 设计专用 **Shell Router** 和 **Agent Prompt Cache (APC)** 来管理依赖链上的部分提示构造。
- 实现 **prefill 与 decode 阶段的重叠（overlap）**，即使存在数据依赖也能提升硬件利用率。
- **优势**：
  - 打破“必须等所有前置输出完成才能开始 prefill”的限制；
  - 最大限度隐藏 prefill 延迟，尤其适用于长输入场景。

---

### 相比现有方法的优势
| 维度 | 传统 MoA | Faster-MoA |
|------|---------|-----------|
| 拓扑结构 | All-to-all 全连接 | Tree-structured 层级稀疏连接 |
| 调度策略 | 同步等待最慢代理 | 异步局部就绪即可推进 |
| 模型调用 | 固定调用所有代理 | 动态早退，按需启用大模型 |
| Prefill-Decode | 严格串行 | 跨代理依赖下仍可重叠 |
| 硬件利用 | 低（stall time 高） | 高（KV cache hit rate ≈100%） |

---

## 2. 核心实验方法和设置

### 数据集
在五个代表性基准上评估，覆盖数学推理与科学问答：
- **GSM8K**：小学级别数学题
- **MATH-500**：中等难度数学问题
- **AIME2025**：高等数学竞赛题（较难）
- **MMLU-ProX-Lite**：多学科 STEM 科学问答
- **IFBench**：指令遵循测试集

### 模型配置
- 使用 **Qwen 模型家族** 作为基础 LLM：
  - Qwen3-VL-4B-Instruct（Small）
  - Qwen3-VL-8B-Instruct（Medium）
  - Qwen3-VL-32B-Instruct（Large）
- 聚合器使用相同模型或独立 aggregator
- 语义相似性计算使用共享嵌入模型：**Qwen3-Embedding-4B**

### 硬件平台
- **6×NVIDIA H200 GPUs**（单台 H200 HGX Server）
- 每个模型部署独立的 **Prefill Engine (PE)** 和 **Decode Engine (DE)**
- 利用 **NVLink** 实现高速 KV Cache 传输

### 基线方法对比
1. **#1 All-to-all Baseline**：三层 9→9→1 全连接 MoA
2. **#2 Tree only**：9→3→1 树结构，无其他优化
3. **#3 Tree + Incremental Prefilling**：加入增量 prefill
4. **#4 Fully-integrated Faster-MoA**：完整方案（树结构 + 动态 EE + 增量 prefill）

### 评估指标
- **End-to-End Latency (E2E)**：从输入到最终输出的时间
- **Accuracy**：任务正确率
- **Model Activation Rate**：各尺寸模型的实际调用比例
- **KV Cache Hit Rate**：增量 prefill 的缓存命中率
- **Per-sample Normalized Latency**：归一化延迟比较

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | 平均 E2E 延迟下降 | 最大延迟降幅 | 准确率变化 |
|------|------------------|-------------|------------|
| Tree only | ~62% | — | ±1% 内波动 |
| Tree + Incremental Prefill | ~76% | — | 无影响 |
| **Faster-MoA（完整）** | **73%–90%** | **最高达 90%** | **保持 ±1%，部分任务更高** |

> ✅ 在 **IFBench** 和 **MMLU-ProX-Lite** 上，Faster-MoA 甚至比 all-to-all 基线 **准确率更高**（+3.3%, +6.7%），说明动态剪枝能过滤低质输出，提升聚合质量。

---

### 消融实验结果（Ablation Study）

#### （1）动态早退（Dynamic EE）效果
- **小模型激活率远高于大模型**：
  - 在 GSM8K 上，32B 模型仅被调用约 **7%** 的样本；
  - 在更难的 IFBench 上升至 **18%**，体现自适应能力。
- **EE 开销极低**：额外引入 ~5% 计算开销，却带来 **10%–50% 的 E2E 延迟下降**。

#### （2）增量 prefill 效果
- 在第二层聚合器上的平均 E2E 延迟：
  - 相比 Naive PD disaggregation：**降低最多 27.4%**
  - 相比 Data Parallelism + Chunked Prefill：仍有明显优势
- KV Cache Hit Rate 接近 **100%**，验证了增量更新高效性。

#### （3）树结构本身的影响
- 即使不加任何优化，仅改用 tree topology 已可降延迟 **62%**，证明其结构性优势。

---

## 4. 关键结论和发现

### 主要发现
1. **稀疏化连接优于全连接**：树状拓扑通过结构化稀疏有效减少冗余通信，是高性能 MoA 的基础。
2. **动态早退可行且有效**：通过语义一致性与置信度联合判断，可在不影响精度前提下大幅跳过大模型推理。
3. **依赖感知重叠是关键优化路径**：传统 PD disaggregation 不适用于有依赖的 MoA，而增量 prefill 成功实现了跨代理的 prefill-decode overlap。
4. **Faster-MoA 实现“双赢”**：在 **端到端延迟降低 73%–90%** 的同时，**准确率持平甚至反超**，打破了“快则不准”的权衡。

---

### 方法的局限性
1. **依赖共享 tokenizer**：当前 APC 缓存 token 而非文本，要求同一家族模型（如 Qwen 系列）。跨 tokenizer 场景需额外处理。
2. **树结构固定**：目前采用预设的 9-3-1 结构，尚未支持动态调整拓扑形状。
3. **Embedding Model 开销**：FCS 计算需额外调用 embedding model，虽轻量但仍引入一定延迟。
4. **适用范围限于推理密集型任务**：对于交互性强或多轮对话类 MoA 场景，需进一步扩展。

---

### 未来工作方向
1. **自动化树结构搜索**：根据任务复杂度自动构建最优层级与 cluster 数量。
2. **完全异构模型支持**：解决不同 tokenizer、vocab 不一致带来的挑战。
3. **在线学习驱动的 EE 策略**：用强化学习或元控制器动态调整早退阈值。
4. **扩展至 Multi-turn MoA**：将该框架应用于辩论、协作规划等多轮代理交互场景。
5. **集成到通用 MaaS（Model-as-a-Service）平台**：作为标准 MoA serving backend。

---

> 🔚 **总结一句话**：  
> **Faster-MoA 通过 Tree Routing + Adaptive Pruning + Dependency-Aware Overlap 三重创新，在几乎不损失准确率的前提下，将 MoA 系统的端到端延迟降低了高达 90%，为高效、可扩展的多代理 AI 系统提供了实用化的解决方案。**

</details>

---

### 4. [TraCT: Disaggregated LLM Serving with CXL Shared Memory KV Cache at Rack-Scale](https://arxiv.org/abs/2512.18194)

**Authors**: Dongha Yoon, Younghoon Min, Hoshik Kim, Sam H. Noh, Jongryool Kim  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.18194v1  

#### Abstract
Disaggregated LLM serving improves resource efficiency by separating the compute-intensive prefill phase from the latency-critical decode phase. However, this architecture introduces a fundamental bottleneck: key/value (KV) tensors generated during prefill must be transferred to decode workers, and ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TraCT: Disaggregated LLM Serving with CXL Shared Memory KV Cache at Rack-Scale

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在**解耦式大语言模型（LLM）推理系统**中，`prefill` 阶段生成的 Key/Value (KV) 张量需要传输到 `decode` 节点进行复用。当前主流方案依赖 **RDMA 网络路径**（如 NIXL/UCX）进行 KV 数据交换，这带来了显著的通信开销，包括：
- NIC 序列化延迟
- 主机内存拷贝
- 网络拥塞敏感性
- 即使是缓存命中（cache hit），仍需走完整网络栈

该瓶颈严重制约了系统的 **平均 TTFT（Time to First Token）** 和 **尾部延迟（P99 latency）**。

---

### 🚀 提出的新方法与创新思路
论文提出 **TraCT** —— 一种基于 **CXL 共享内存** 的机架级 LLM 推理系统，其核心思想是：

> **将 CXL Type-3 设备作为统一的 KV 传输介质 + 分布式前缀感知 KV 缓存层**，实现 GPU 到 CXL 内存之间的直接 DMA 传输，完全绕过 NIC 和网络协议栈。

#### 主要技术创新点：

| 创新模块 | 技术细节 |
|--------|---------|
| **Network-free KV Transfer** | 使用 CXL 共享内存替代 RDMA，通过 GPU-CXL DMA 实现零拷贝、无 NIC 参与的 KV 数据读写 |
| **Two-Tier Inter-node Locking** | 在无硬件原子操作和全局一致性的 CXL 内存上设计软件锁机制：<br>• 节点内使用 DRAM resident local lock<br>• 节点间通过 CXL 中的 global_lock 数组 + lock manager 协调 |
| **Fine-grained Cache Coherence Control** | 使用 `clflush` 显式刷新元数据所在 cacheline，确保跨节点可见性；对大块 KV payload 不刷 cache（因 GPU-DMA 绕过 CPU cache） |
| **Offset-based Shared Object Store** | 所有共享结构使用偏移量寻址（offset-based addressing），避免虚拟地址不一致问题 |
| **Hierarchical Memory Allocator** | 全局 chunk allocator + 每节点本地 heap，减少跨节点元数据竞争 |

---

### 🔍 相比现有方法的优势

| 对比维度 | 传统 RDMA 方案（如 NIXL） | LMCache / Mooncake | TraCT |
|--------|--------------------------|--------------------|-------|
| KV 传输路径 | NIC → Host DRAM → Transport Stack | 同左，支持缓存但仍在网络路径 | **CXL Load/Store/DMA，无 NIC 参与** |
| 缓存复用效率 | 缓存命中仍需网络传输 | 支持跨节点缓存，但仍经 NIC | **直接从 CXL 内存读取，无需复制或序列化** |
| 同步机制 | 依赖 RDMA 原子操作或集中式协调器 | 多数基于网络控制平面 | **去中心化、纯软件同步，适用于非一致性 CXL 内存** |
| 性能可预测性 | 受网络拥塞影响大 | 尾延迟高 | **低且稳定的延迟分布** |

---

## 2. 核心实验方法和设置

### 📊 数据集与工作负载
实验采用两类 workload：

#### （1）静态工作负载（Static Workloads）
- 固定输出长度：3 tokens
- 输入长度变化：1500, 3000, 4500, 6000 tokens
- 目标：精确控制 KV 数据量，隔离 KV 传输开销

#### （2）合成工作负载（Synthetic Workloads）
使用 Dynamo 内置请求生成器生成三组请求流（Workload A/B/C），统计特征如下：

| Workload | 平均输入长度 | 平均输出长度 | 平均唯一前缀长度 |
|---------|-------------|-------------|------------------|
| A       | 4449 ± 2424 | 215 ± 263   | 1073 ± 1549      |
| B       | —           | —           | 1215 ± 1693      |
| C       | —           | —           | 1631 ± 2027      |

> 注：唯一前缀长度越大，缓存命中率越低，测试更严苛场景。

---

### ⚙️ 实验设置

| 组件 | 配置 |
|------|------|
| **硬件平台** | 2 台服务器：<br>• 每台配 NVIDIA A6000 GPU (48GB)<br>• 512GB Host DRAM<br>• 100Gbps Mellanox NIC（用于基线）<br>• Niagara 2.0 CXL Type-3 内存扩展器（64GB 共享区域）<br>• CXL 延迟：640ns，带宽：10.1 GB/s |
| **软件框架** | Dynamo v0.5.0 + vLLM v0.10.1.1 |
| **模型** | DeepSeek-R1-Distill-Llama-8B |
| **评估指标** | 
| • **TTFT**（Time to First Token）<br>• **P99 TTFT**<br>• **Peak Throughput (QPS)**<br>• **Per-request breakdown**（调度、计算、KV 读写时间）<br>• **GPU SM 利用率、PCIe 流量、功耗** |

---

### 🆚 基线方法对比
| 基线名称 | 描述 |
|--------|------|
| **NIXL/UCX** | 原始 Dynamo 架构，使用 RDMA 进行 KV 传输，无缓存 |
| **LMCache** | 使用主机 DRAM 作为前缀感知 KV 缓存，缓存命中仍需通过 RDMA 发送到 decode worker |
| **TraCT (no cache)** | 关闭缓存功能，仅验证 CXL 作为传输通道的有效性 |
| **TraCT (with cache)** | 完整版本，启用 CXL 上的共享 KV 缓存 |

> 所有实验禁用 GPU 内部的 prefix caching，以公平比较外部缓存效果。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 指标 | TraCT vs. 基线提升幅度 | 条件说明 |
|------|------------------------|----------|
| **平均 TTFT** | **最高降低 9.8×** | vs. NIXL/LMCache，workload A |
| **P99 TTFT** | **最高降低 6.2×** | 尾延迟大幅改善 |
| **峰值吞吐量 (QPS)** | **最高提升 1.6×** | vs. LMCache，在 QPS=3.0 下 |
| **Prefill 阶段 GPU SM 占用率** | 显著下降 | 表明计算资源释放更快 |
| **解码端 GPU RX 带宽利用率** | 更高且稳定 | 直接从 CXL 获取数据，不受 NIC 限制 |
| **总能耗** | 降低 | 执行时间缩短 + SM 活动减少 |

---

### 🔬 详细实验分析

#### （1）CXL 替代 RDMA 的有效性（无缓存场景）
- 图5（TTFT CDF）显示：即使关闭缓存，TraCT 在所有输入长度下都显著优于 NIXL。
- 原因：**GPU-CXL DMA 避免了 NIC 队列、host DRAM 中转和协议开销**，路径更短、更可预测。
- 图6（Throughput）表明：TraCT 在高并发下维持与 RDMA 相当甚至更好的吞吐能力。

#### （2）启用缓存后的性能飞跃
- 图7（Peak Throughput）：TraCT 达到 **1.6× 更高的峰值吞吐量**。
- 图9（TTFT CDF）：整个延迟分布向左移动，尤其是尾部（P99）压缩明显。
- 图10（时间分解）：
  - **KV read 时间几乎恒定**（TraCT），而 LMCache/NIXL 随负载上升（受网络影响）
  - **KV write 时间更低**：TraCT 仅需发布元数据，无需重复发送已缓存块
  - **Compute 时间减少有限**：说明瓶颈不在计算而在通信

#### （3）缓存命中率对比（图8）
- TraCT 的实际缓存命中率略低于 LMCache，但性能反而更好。
- 结论：**性能优势并非来自更高命中率，而是“命中后无需网络传输”这一根本优化**。

#### （4）资源利用与能效（图11）
- **GPU SM 利用率更低且更平稳**：因无需等待网络传输完成即可释放资源
- **PCIe RX/TX 带宽更高效**：TraCT 解码端直接从 CXL 拉取数据，避免中间拷贝
- **功率消耗更低**：执行周期缩短 + SM idle 时间增加 → 更节能

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **CXL Shared Memory 是替代 RDMA 的可行且高效的 KV 传输媒介**  
   > 即使不启用缓存，仅用作传输通道也能显著降低 TTFT 和尾延迟。

2. **真正的性能突破来自于“缓存命中即本地访问”**  
   > TraCT 将 KV 缓存置于所有节点可直接访问的 CXL 内存中，彻底消除 cache-hit 的网络开销。

3. **去中心化的元数据管理可在非一致性 CXL 上高效运行**  
   > 提出的 two-tier locking + offset-based addressing + clflush 控制策略，解决了无硬件原子性和全局一致性的挑战。

4. **系统级收益体现在吞吐、延迟、资源利用率和能效四方面**  
   > 不仅速度快，而且更稳定、更省电，适合大规模部署。

---

### ⚠️ 局限性

| 局限 | 说明 |
|------|------|
| **依赖真实 CXL Type-3 硬件支持** | 当前 CXL 生态尚不成熟，设备普及度有限 |
| **缺乏全设备级硬件一致性** | 必须依赖软件强制刷新（clflush），带来一定 CPU 开销 |
| **目前使用简单 LRU 替换策略** | 未探索更复杂的缓存替换算法（如 LFU、ARC）对命中率的影响 |
| **实验规模较小（仅2节点）** | 未验证在更大机架规模下的扩展性 |

---

### 🔮 未来工作方向

1. **探索更智能的缓存替换策略**（如基于访问频率或热度预测）
2. **支持多层级存储架构**：结合 CXL 内存 + SSD 构建异构 KV 缓存池
3. **扩展至其他 Transformer 类应用**：如视觉模型、检索增强生成（RAG）
4. **研究 CXL 内存故障恢复与容错机制**
5. **在更大规模集群中验证横向扩展能力**

---

## 总结

> **TraCT 成功证明了 CXL Shared Memory 可作为下一代 LLM 推理基础设施的核心组件**。它不仅是一个更快的“内存池”，更是重构了解耦式推理系统中 **通信-缓存-计算** 的协作范式，为构建高性能、低延迟、高能效的大模型服务系统提供了全新路径。

</details>

---

### 5. [NOVA: Discovering Well-Conditioned Winograd Transforms through Numerical Optimization of Vandermonde Arithmetic](https://arxiv.org/abs/2512.18453)

**Authors**: Jayant Lohia  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.18453v1  

#### Abstract
Winograd convolution is the standard algorithm for efficient inference, reducing arithmetic complexity by 2.25x for 3x3 kernels. However, it faces a critical barrier in the modern era of low precision computing: numerical instability. As tiles scale to maximize efficiency (e.g., F(6,3), F(8,3)), the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：NOVA: Discovering Well-Conditioned Winograd Transforms through Numerical Optimization of Vandermonde Arithmetic

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Winograd 卷积是深度学习中高效的推理算法，可将 3×3 卷积核的算术复杂度降低 2.25×。然而，在低精度计算（如 FP16 或 Int8）环境下，其数值不稳定性成为严重瓶颈。随着卷积块尺寸增大（如 F(6,3)、F(8,3)），标准整数插值点导致的 Vandermonde 矩阵条件数急剧上升（例如 F(8,3) 达到 $ K \sim 2\times10^5 $），使得在低精度下输出信号被噪声淹没，模型准确率崩溃至随机水平。

传统做法被迫使用更小且效率更低的块（如 F(4,3)），牺牲了理论上的性能优势。

### 提出的新方法：NOVA
本文提出 **NOVA**（Numerical Optimization of Vandermonde Arithmetic），一种通过连续空间优化发现高良态 Winograd 变换的方法，核心思想如下：

- **问题重构**：将 Winograd 插值点选择视为 $\mathbb{R}^{n-1}$ 上的连续优化问题，而非传统的离散“词汇表”搜索。
- **进化策略搜索（Evolution Strategy, ES）**：在连续空间中搜索最优插值点配置。
- **有理数捕捉（Snap-to-Rational）**：将连续解映射为简单分数（如分母 ≤ 6 的有理数），确保可精确表示。
- **符号验证（Symbolic Verification）**：利用 SymPy 进行精确有理数运算，验证分解正确性，避免浮点误差带来的误判。

该方法打破了长达数十年依赖整数插值点的传统，发现了如 $\left\{\pm\frac{5}{6}, \pm\frac{7}{6}, \pm\frac{3}{5}\right\}$ 等非传统、非对称的稳定配置。

### 相比现有方法的优势
| 方法 | 是否需重训练 | 是否改变架构 | 是否支持 drop-in 替换 | 条件数改善 |
|------|--------------|----------------|------------------------|-------------|
| NOVA（本文） | ❌ 否 | ❌ 否 | ✅ 是 | **415×**（F(8,3)） |
| Alam et al. (2022) | ❌ 否 | ❌ 否 | ✅ 是 | ~11×（F(6,3)） |
| Legendre Basis (Barabasz et al.) | ✅ 需重训练 | ✅ 改变基底 | ❌ 否 | ~2× 错误减少 |
| Tap-wise Scaling (Chikin & Kryzhanovskiy) | ✅ 需学习参数 | ✅ 添加缩放因子 | ❌ 否 | ~8% 准确率提升 |
| RNS-based Methods | ❌ 否 | ✅ 改变算术系统 | ❌ 否 | 完全消除舍入误差 |

**核心优势**：
- **零成本升级**：仅替换预定义的变换矩阵即可，无需修改网络权重、无需校准、无需重新训练。
- **巨大条件数改善**：相比标准整数点，实现 **2.9× 至 415×** 的条件数下降。
- **真实场景恢复能力**：在 FP16 下使原本崩溃的模型恢复完整准确率（+70% 以上）。

---

## 2. 核心实验方法和设置

### 数据集
- **ImageNetV2**：用于主 FP16 推理评估，包含 30,000 张图像，覆盖 1,000 类。
- **CIFAR-10**：用于 INT8 量化评估，测试 per-tensor 和 per-channel 两种模式。

### 实验设置
- **硬件平台**：NVIDIA Tesla T4 GPU，CUDA 12.8，PyTorch 2.0。
- **精度设置**：
  - **FP16**：使用 `torch.cuda.amp.autocast`，变换矩阵以 FP32 计算后转为 FP16。
  - **INT8**：采用对称量化（symmetric per-tensor/per-channel），无校准需求。
- **模型架构**：ResNet-18、ResNet-50、VGG-16、MobileNet-V2、EfficientNet-B0 等，均使用 ImageNet 预训练权重。

### 评估指标
| 指标 | 描述 |
|------|------|
| $K_2(V)$ | Vandermonde 矩阵的谱条件数，衡量数值稳定性 |
| Top-1 Accuracy | 分类任务准确率，反映实际推理效果 |
| Layer-wise L2 Error | 层级输出相对误差（vs FP64 直接卷积） |
| Element-wise Relative Error | 细粒度误差分析，用于量化误差传播 |
| Latency | 推理延迟（ms），验证是否引入额外开销 |

### 基线方法对比
- **Standard Integers**：$\{0, \pm1, \pm2, \dots\}$，当前主流实现（cuDNN/TensorRT）所用。
- **Alam et al. (2022)**：基于参数化家族 $\{-1/c, -c, c, 1/c\}$ 的搜索，受限于对称性和倒数结构。
- **Chebyshev Nodes**：理论上最优插值节点，但为无理数，无法用于符号构造。
- **Tap-wise Scaling**：通过学习每通道缩放因子缓解量化误差。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 条件数改善（1D）
| Tile | Standard $K_2$ | Discovered $K_2$ | Improvement |
|------|------------------|--------------------|-------------|
| F(4,3) | 42.5 | 14.5 | **2.9×** |
| F(6,3) | 2,075 | 77 | **27×** |
| F(8,3) | 196.9k | 474 | **415×** |

> 发现点示例：F(8,3) 使用 $\left\{0, \pm\frac{2}{5}, \pm\frac{5}{6}, \pm1, \pm\frac{7}{6}, \infty\right\}$

#### 2D 卷积条件数改善（Kronecker 积平方效应）
由于 2D 变换矩阵为 Kronecker 积形式 $A_{2D} = A \otimes A$，故 $K_2(A_{2D}) = K_2(A)^2$。

| 2D Tile | Improvement (Squared) |
|---------|------------------------|
| F(4×4,3×3) | $2.9^2 \approx 8.5\times$ |
| F(6×6,3×3) | $27^2 \approx 733\times$ |
| F(8×8,3×3) | $415^2 = \textbf{172,484}\times$ |

这是目前文献中最大的条件数改善幅度。

#### 实际推理准确率恢复（FP16, ImageNetV2）
| Architecture | Standard Acc | Discovered Acc | Recovery |
|------------|---------------|----------------|----------|
| ResNet-18 | 10.8% | 77.8% | **+67.0%** |
| ResNet-50 | 38.3% | 80.6% | **+42.3%** |
| VGG-16 | 4.7% | 75.3% | **+70.6%** |
| VGG-16-BN | 4.7% | 77.5% | **+72.8%** |

> 在 VGG-16 上，从近乎随机猜测恢复到接近全精度水平。

#### INT8 层级误差降低
| Tile | Standard Error | Discovered Error | Improvement |
|------|----------------|------------------|-------------|
| F(4,3) | 7.1% | 2.1% | **3.4×** |
| F(6,3) | 3990% | 12.4% | **322×** |
| F(8,3) | 100% (饱和) | 59.2% | **1.7×** |

> F(6,3) 标准实现误差超过 39 倍，输出完全无效；NOVA 将其降至可用范围。

### 与基线方法对比

#### 对比 Alam et al. (2022)
| Method | F(4,3) $K_2$ | F(6,3) $K_2$ | Search Space |
|--------|---------------|---------------|---------------|
| Standard | 42.5 | 2,075 | — |
| Alam (recip. symm.) | 21.3 | 847 | 参数化子集 |
| **NOVA (ES)** | **14.5** | **77** | $\mathbb{R}^{n-1}$ 全空间 |
| **Improvement** | **1.5×** | **11×** | — |

NOVA 在更大规模 tile 上优势显著，因其不受限于对称/倒数结构。

#### 消融实验：点选择 vs 缩放
| Points | Scaling | ResNet-18 Acc (F(6,3), FP16) |
|--------|---------|-------------------------------|
| Std ($K=2075$) | None | 10.8% |
| Std ($K=2075$) | Tap-wise | 12.1% (+1.3%) |
| **Disc ($K=77$)** | **None** | **77.8% (+67.0%)** |
| **Disc ($K=77$)** | **Tap-wise** | **78.2% (+0.4%)** |

> 结论：**点选择修复根本条件数问题**，而缩放仅调整动态范围，两者正交，可叠加。

---

## 4. 关键结论和发现

### 主要发现
1. **整数插值点严重次优**：长期使用的整数点（如 ±1, ±2）并非最优，其导致的高条件数是低精度下 Winograd 失败的根本原因。
2. **分数点显著提升稳定性**：通过优化发现的简单有理数点（如 5/6, 7/6）能大幅降低条件数，且保持精确可计算性。
3. **改进效果随维度平方放大**：1D 的改善在 2D 中因 Kronecker 积性质平方放大，F(8×8,3×3) 实现 **172,484×** 条件数改善。
4. **drop-in 替换即可恢复准确率**：无需重训练或校准，在 FP16 下将崩溃模型恢复至 75–78% 准确率，**最高恢复 73 个百分点**。
5. **与其它方法互补而非竞争**：NOVA 与其他技术（如 scale learning、per-channel quantization）正交，未来可结合进一步提升鲁棒性。

### 方法的局限性
- **大尺寸 tile 仍具挑战**：F(8,3) 在 INT8 下即使使用 NOVA 点仍有 59.2% 误差，表明需结合其他技术（如 tap-wise scaling）。
- **浮点表示权衡**：部分分数（如 5/6）在 FP16 下存在表示误差，虽条件数更优，但在极低精度下需权衡。为此作者提出 **dtype-aware discovery**，约束搜索空间至 dyadic rationals（如 3/4, 5/4），兼顾条件数与表示精度。
- **不对称点未充分探索**：当前搜索偏向对称配置，可能遗漏更优的非对称解。
- **仅适用于标准 Cook-Toom 构造**：不适用于 RNS 或 SFC 等替代方案。

### 未来工作方向
- **结合 PTQ 方法**：将 NOVA 点与 PAW、LoWino 等后训练量化方法结合，构建端到端稳定 pipeline。
- **扩展至 5×5 及更大 kernel**：初步结果显示 F(4,5) 和 F(6,5) 分别获得 13× 和 112× 条件数改善，值得深入研究。
- **集成至 cuDNN/TensorRT**：推动工业级部署，释放大块 Winograd 的理论效率。
- **探索 2D 直接优化**：当前使用 Kronecker 积，未来可尝试直接优化 2D 插值点。
- **支持复数域或 RNS 扩展**：与复杂域变换或 RNS 结合，实现更高稳定性。

---

> **总结一句话**：  
> NOVA 通过将 Winograd 插值点选择从离散搜索转变为连续优化问题，首次实现了对高良态分数点的大规模自动发现，解决了低精度下 Winograd 数值不稳定的根本瓶颈，提供了一种“零成本、即插即用”的性能恢复路径，为下一代高效推理硬件解锁了大块 Winograd 的潜力。

</details>

---

### 6. [Interpretable Hybrid Deep Q-Learning Framework for IoT-Based Food Spoilage Prediction with Synthetic Data Generation and Hardware Validation](https://arxiv.org/abs/2512.19361)

**Authors**: Isshaan Singh, Divyansh Chawla, Anshu Garg, Shivin Mangal, Pallavi Gupta, Khushi Agarwal, Nimrat Singh Khalsa, Nandan Patel  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.19361v1  

#### Abstract
The need for an intelligent, real-time spoilage prediction system has become critical in modern IoT-driven food supply chains, where perishable goods are highly susceptible to environmental conditions. Existing methods often lack adaptability to dynamic conditions and fail to optimize decision makin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**物联网（IoT）驱动的食品供应链中食品腐败预测**所面临的挑战，解决了以下关键问题：
- **静态机器学习模型缺乏适应性**：传统ML模型在面对运输过程中动态变化的环境条件（如温度、湿度波动）时表现不佳。
- **决策过程不透明**：现有方法多为“黑箱”模型，难以解释其预测依据，不利于在食品安全等高风险场景建立信任。
- **实时决策能力不足**：缺乏能够基于传感器数据流进行连续、自适应决策的框架。

### 提出的新方法与创新思路
作者提出了一种**可解释的混合深度Q学习框架（Interpretable Hybrid Deep Q-Learning Framework）**，其核心创新点包括：

- **规则引导的可解释分类器环境**：设计了一个基于领域知识的**rule-based classifier**作为强化学习（RL）环境，通过预定义的阈值（如温度>30°C）来生成腐败等级标签。这不仅提供了透明的监督信号，还确保了代理（agent）的学习过程具有可追溯性和语义一致性。
  
- **LSTM + RNN 混合DQN架构**：在Deep Q-Network（DQN）中集成**Long Short-Term Memory (LSTM)** 和 **Recurrent Neural Network (RNN)** 层。LSTM用于捕捉长期时间依赖（如缓慢的气体积累），RNN则精炼短期动态模式，共同提升对时序传感器数据的建模能力。

- **面向可解释性的评估指标体系**：引入了多个自定义的、以可解释性为导向的评估指标，包括：
  - **Spoilage Accuracy**（腐败预测准确率）
  - **Reward-to-Step Ratio**（奖励步长比）
  - **Loss Decrease Rate**（损失下降率）
  - **Exploration Rate Decay**（探索率衰减）
  这些指标不仅衡量性能，还能洞察代理的学习动态和决策逻辑。

- **合成数据生成与硬件验证结合**：通过模拟真实传感器读数生成**synthetic data**用于训练，并在基于Arduino的真实硬件系统上进行**real-time hardware validation**，确保了方法的实用性和鲁棒性。

### 相比现有方法的优势
- **更高的准确性与效率**：相比单一的LSTM、RNN、ANN或Monte Carlo方法，提出的LSTM+RNN混合模型在腐败预测准确率和奖励效率上均表现更优。
- **更强的可解释性**：通过规则化环境和可追踪的时间序列分析，使模型决策过程透明化，便于专家理解和审计。
- **更好的泛化与适应性**：在合成数据和真实硬件数据上的双重验证表明，该框架能有效适应不同数据源和动态环境。

---

## 2. 核心实验方法和设置

### 数据集
- **合成数据集（Synthetic Dataset）**：
  - 基于正态分布生成1000个样本，模拟五类传感器读数：
    - 温度（mean=25°C, std=5）
    - 湿度（mean=60%, std=10）
    - 土壤水分（mean=200, std=50）
    - MQ3酒精传感器（mean=150, std=30）
    - MQ4甲烷传感器（mean=250, std=30）
  - 添加噪声（mean=0, std=5）以模拟现实不确定性。
  - 腐败阈值由规则定义（如温度>30°C）。

- **真实硬件数据集（Real-Time Hardware Data）**：
  - 使用Arduino平台连接DHT11（温湿度）、MQ3、MQ4和土壤湿度传感器采集实际数据。
  - 统计数据显示真实环境更为极端（如平均温度28.29°C，湿度91.87%）。

### 实验设置
- **代理动作空间**：四个离散动作
  - `Action 0`: 继续常规存储
  - `Action 1`: 发出低级别警报
  - `Action 2`: 调整存储参数（如温控）
  - `Action 3`: 发出紧急干预警报
- **训练配置**：
  - 合成数据：运行1000个episode
  - 硬件数据：运行100个episode（受限于实时系统资源）
  - 使用**Epsilon-Greedy**策略（初始ε=1，衰减至0.01）
  - 批量大小（batch size）= 64
- **神经网络架构**：LSTM层（64节点）后接RNN层（64节点）用于Q值估计。

### 评估指标
- **Spoilage Accuracy**：代理正确预测腐败级别的比例。
- **Reward-to-Step Ratio**：平均每步获得的奖励，反映决策效率。
- **Loss Decrease Rate**：训练过程中损失函数的下降速率，反映收敛稳定性。
- **Exploration Rate Decay**：ε值的衰减速率，衡量探索到利用的过渡。
- **Spoilage Class Distribution**：可视化代理在不同腐败等级上的决策分布。

### 基线方法对比
与以下五种方法进行了对比：
1. **LSTM+RNN (Proposed)**：本文提出的混合模型
2. **ANN**：基础人工神经网络
3. **LSTM**：仅使用LSTM的DQN
4. **RNN**：仅使用RNN的DQN
5. **Monte Carlo**：基于随机采样的策略

---

## 3. 主要实验结果和性能指标

### 在合成数据上的结果（1000 episodes）
| Agent Framework | Spoilage Accuracy | Reward-to-Step Ratio | Loss Decrease Rate |
|-----------------|-------------------|------------------------|---------------------|
| **LSTM+RNN (Proposed)** | **0.82** | **0.63** | -0.00042 |
| ANN | 0.74 | 0.49 | 0.42156 |
| LSTM | 0.77 | 0.54 | 0.00143 |
| RNN | 0.75 | 0.51 | 0.00089 |
| Monte Carlo | 0.00 | 0.70 | N/A |

- **结论**：LSTM+RNN在**腐败准确率**和**奖励效率**上均显著优于其他方法，尽管其损失略有上升（可能因模型复杂度高导致轻微过拟合）。

### 在真实硬件数据上的结果（100 episodes）
| Agent Framework | Spoilage Accuracy | Reward-to-Step Ratio | Loss Decrease Rate |
|-----------------|-------------------|------------------------|---------------------|
| **LSTM+RNN (Proposed)** | **0.59** | **0.17** | 0.09148 |
| LSTM | 0.58 | 0.16 | 0.12168 |
| ANN | 0.55 | 0.11 | -6.27726 |
| RNN | 0.42 | -0.17 | 26.60358 |
| Monte Carlo | 0.41 | -0.18 | N/A |

- **结论**：
  - LSTM+RNN在所有指标上**全面领先**，尤其在**Loss Decrease Rate**上表现出最稳定的收敛行为。
  - RNN和Monte Carlo表现最差，说明它们难以处理真实世界中的噪声和非平稳性。
  - 单独的LSTM表现接近LSTM+RNN，证明LSTM是主导因素，但RNN的加入仍带来微小增益。

### 消融实验与分析
- 虽未明确标注“消融实验”，但通过对比不同架构（LSTM vs RNN vs LSTM+RNN）已实现功能上的消融分析。
- **关键发现**：LSTM在捕捉长期趋势方面至关重要，而RNN有助于增强短期动态响应，二者结合实现了最佳平衡。

---

## 4. 关键结论和发现

### 主要结论
1. **混合LSTM+RNN架构在腐败预测任务中表现最优**：它能有效捕捉传感器数据中的长短时依赖关系，在合成和真实数据上均取得最高准确率和最优决策效率。
2. **可解释性设计提升了模型可信度**：通过规则化环境和可解释指标，实现了从“黑箱”到“灰箱”的转变，便于部署和审计。
3. **框架具备良好的实用性与扩展性**：成功在Arduino硬件平台上验证，证明其适用于边缘计算场景；模块化设计支持跨食品类型和传感器配置的迁移。

### 方法的局限性
- **合成数据的代表性有限**：尽管尝试模拟真实场景，但仍可能无法覆盖所有极端或罕见情况。
- **传感器漂移问题**：长期使用中传感器精度可能下降，影响输入质量。
- **计算复杂度较高**：LSTM+RNN模型对计算资源要求较高，可能限制在低端设备上的部署。
- **训练数据依赖性强**：模型性能高度依赖于训练数据的质量，存在偏差可能导致次优决策。

### 未来工作方向
1. **Hybrid Reinforcement Learning Architectures**：结合model-based RL或imitation learning提升样本效率。
2. **Adaptive Sampling and Active Learning**：动态调整数据采集策略，优先获取信息量大的样本。
3. **Transfer Learning and Domain Adaptation**：将在一种作物上训练的模型迁移到其他作物或环境。
4. **Model-based Reinforcement Learning**：构建环境动力学模型以支持长期规划。
5. **Hierarchical Reinforcement Learning**：分层决策，高层制定策略，底层执行精细控制。
6. **Multi-objective Reinforcement Learning (MORL)**：同时优化多个目标（如保鲜、能耗、成本）。
7. **Online Learning and Adaptive Control**：实现实时在线学习，持续适应环境变化。
8. **Robustness and Uncertainty Quantification**：增强模型对噪声、对抗扰动和不确定性的鲁棒性。

</details>

---

### 7. [CienaLLM: Generative Climate-Impact Extraction from News Articles with Autoregressive LLMs](https://arxiv.org/abs/2512.19305)

**Authors**: Javier Vela-Tambo, Jorge Gracia, Fernando Dominguez-Castro  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.19305v1  

#### Abstract
Understanding and monitoring the socio-economic impacts of climate hazards requires extracting structured information from heterogeneous news articles on a large scale. To that end, we have developed CienaLLM, a modular framework based on schema-guided Generative Information Extraction. CienaLLM use...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CienaLLM: Generative Climate-Impact Extraction from News Articles with Autoregressive LLMs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究旨在解决从异构新闻文章中大规模提取气候变化社会经济影响信息的挑战。传统方法依赖于人工标注或监督学习模型，成本高、扩展性差，且难以适应新的灾害类型、语言或领域。特别是干旱等气候事件的影响广泛而分散，需要系统化地从新闻中提取结构化信息以支持风险监测与适应规划。

### 提出的新方法与创新
作者提出了 **CienaLLM** ——一个基于 **Schema-guided Generative Information Extraction (GenIE)** 的模块化框架，利用开源的 **autoregressive Large Language Models (LLMs)** 实现零样本（zero-shot）信息抽取。

#### 主要创新点包括：
- **无需训练的零样本范式**：通过自然语言提示（prompting）引导LLM直接输出结构化JSON格式的结果，避免了昂贵的数据标注和模型微调。
- **高度可配置与可复现**：支持灵活定义输出schema、prompt模板、多步pipeline（如summarization、self-criticism），并可在本地部署（via Ollama），提升隐私性、可控性和可持续性。
- **模型无关设计（model-agnostic）**：只需修改prompt和schema即可迁移到其他气候灾害（如洪水、热浪）、行业或语言，无需重新训练。
- **系统性评估**：对384种配置进行大规模因子实验，涵盖12个open-weight LLMs、不同尺寸、量化精度、prompt策略等，全面分析性能-效率权衡。

### 相比现有方法的优势
相比监督式基线 **SeqIA** [36]：
- 在干旱影响提取任务上达到或超越其准确率（F1）；
- 具备更强的泛化能力，适用于新任务而无需重新标注；
- 支持更复杂的结构化输出（如地理位置推断）；
- 虽然推理成本更高，但提供了更高的灵活性和可维护性。

---

## 2. 核心实验方法和设置

### 使用的数据集
研究构建并使用了三个关键数据集：

| 数据集 | 描述 |
|-------|------|
| **Drought Impacts Dataset (DID)** | 包含386篇西班牙语新闻文章，每篇标注多个影响类别（农业、畜牧业、水资源、能源）。用于评估影响识别性能。 |
| **Drought Relevance Dataset (DRD)** | 包含2,240篇文章（正负样本各半），用于判断文章是否与干旱相关。 |
| **Drought Impact Locations Dataset (DILD)** | 包含100篇文章，标注受影响的西班牙省份（共835条记录），用于地理定位评估。 |

此外，还收集了来自 *El País*, *ABC*, *20 Minutos* 等主流媒体的大规模新闻语料库，为未来研究提供基础。

### 实验设置
- **模型选择**：覆盖三大LLM家族（Gemma、Llama、Qwen），参数量从3B到72B不等，包含全精度（fp16）与4-bit量化（q4_K_M）版本。
- **Prompt工程策略测试**：
  - **SUM**（Summarization）：先生成摘要再提取
  - **CoT**（Chain of Thought）：要求逐步推理
  - **SC**（Self-Criticism）：第二轮自我修正
  - **DESC**（Impact Descriptions）：加入详细影响定义
- **响应解析策略（RPARSE）**：
  - `RPARSE=False`：单步输出JSON
  - `RPARSE=True`：两步法，先自由文本后结构化解析
- **评估指标**：
  - 准确性：micro-averaged **F1, Precision, Recall, Accuracy**
  - 可靠性：**Parsing Error Rate**（无法解析为合法JSON的比例）
  - 效率：**Execution Time per article**

### 基线方法对比
与监督式模型 **SeqIA** 进行比较，特别是在共享的 **E2E dataset** 上进行公平对比（避免训练集重叠）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（在DID测试集上）

| 配置 | F1 Score | Precision | Recall | Exec. Time (s/article) |
|------|----------|-----------|--------|-------------------------|
| **Best-F1** (qwen_72b + CoT + DESC + RPARSE) | **0.873** | 0.837 | 0.911 | 36.43 |
| **Efficient** (qwen_7b + DESC + RPARSE) | 0.808 | 0.832 | 0.785 | 3.32 |
| **Fastest** (qwen_3b + RPARSE) | 0.646 | 0.810 | 0.538 | 2.47 |

> 所有配置均无解析错误（Parsing Error Rate = 0）

#### 分类别表现（Best-F1）
- 农业：0.868
- 畜牧业：0.966
- 水资源：0.824
- 能源：0.914

### 与基线方法 SeqIA 的对比（在E2E dataset上）

| 方法 | F1 Score | Exec. Time (s/article) |
|------|----------|------------------------|
| **CienaLLM (Best-F1)** | **0.782** | 30.07 |
| **SeqIA** | 0.769 | **1.54** |

👉 CienaLLM **略优于** SeqIA 的F1分数，但推理时间高出约20倍。

### 消融实验关键发现

#### ✅ Response Parsing (RPARSE)
- 平均解析错误率从 **4.0%**（无解析）降至 **0.7%**（启用RPARSE）
- 对小模型（如 llama_3b, llama_8b）尤其重要，错误率下降超15个百分点
- 不影响准确性（F1几乎不变），仅增加约6秒延迟

#### ✅ 模型大小与性能关系
- **大模型显著更优且稳定**：
  - `qwen_72b`: F1=0.852 ± 0.015
  - `gemma_4b`: F1=0.727 ± 0.057（方差更大）
- 更大模型对prompt变化鲁棒性强（低方差）

#### ✅ 量化（Quantization）
- 4-bit量化带来 **30–40% 推理加速**
- F1损失较小：
  - Gemma: -0.008
  - Llama: -0.017
  - Qwen: -0.008
- 是资源受限场景下的高效选择

#### ✅ Prompt Engineering 效果（因模型而异）

| 技术 | 总体效果 | 模型特异性 |
|------|--------|------------|
| **DESC**（加描述） | 小幅提升（+0.01~0.02） | 大模型受益明显（如 qwen_72b +0.021），小模型反而下降 |
| **SUM**（摘要） | Gemma显著提升（+0.054），Llama/Qwen多数负向 | 表明输入长度偏好存在家族差异 |
| **CoT**（思维链） | 微弱提升（±0.01） | 仅在部分中大型模型有效（如 gemma_27b） |
| **SC**（自批评） | **无收益**，多数情况下输出未改变 | 浪费计算资源 |

> 最佳配置通常包含 **DESC + CoT**，Gemma系列额外受益于 **SUM**

---

## 4. 关键结论和发现

### 主要结论
1. **Schema-guided GenIE 是可行且强大的范式**：  
   使用开源LLM进行零样本气候影响提取，在准确性上可匹敌甚至超过监督模型（如SeqIA），同时具备极强的可迁移性。

2. **模型规模是决定性因素**：  
   更大的LLM不仅F1更高，而且跨prompt配置的表现更稳定，适合高精度任务。

3. **量化极具性价比**：  
   4-bit量化大幅降低内存和延迟开销，仅带来轻微精度损失，适合边缘或低成本部署。

4. **Prompt策略需“因地制宜”**：  
   不存在通用最优prompt；应根据模型家族和尺寸调整策略（如小模型避免复杂prompt）。

5. **两步响应解析至关重要**：  
   显著降低JSON格式错误，保障下游处理可靠性，尤其对旧版或小型模型必要。

6. **地理位置提取仍具挑战**：  
   即使在Best-F1配置下，地点提取F1仅为 **0.465**，主因是召回率低（0.34），说明隐式地理引用推理难度大。

### 方法的局限性
- **推理成本高**：相比监督模型，LLM推理速度慢（最高达60倍延迟），不适合大规模预筛选。
- **依赖高质量prompt设计**：性能受prompt表述敏感，需专家参与定义schema和描述。
- **仅验证于干旱场景**：尚未扩展至洪水、热浪等其他气候事件。
- **语言限制**：当前实验集中于西班牙语新闻，跨语言迁移需进一步验证。

### 未来工作方向
- 扩展至更多气候灾害类型和其他语言
- 探索 **few-shot prompting** 或 **retrieval-augmented generation (RAG)** 提升性能
- 构建 **ensemble策略** 融合多模型优势
- 开发自动化prompt优化工具
- 结合轻量级分类器做两级流水线：先用关键词或小模型过滤相关文章，再用CienaLLM做精细提取
- 加强地理实体归一化与空间推理能力（如结合gazetteer或流域地图）

---

## 总结
CienaLLM 展示了 **Generative IE + open-weight LLMs** 在气候影响监测中的巨大潜力。它提供了一种灵活、可复现、无需训练的信息提取新路径，特别适合快速响应新型灾害或区域扩展。尽管当前效率尚不如传统监督模型，但随着LLM持续进化（如Qwen3、Llama4），其性能-成本曲线将持续优化，有望成为下一代气候情报系统的基础设施之一。

> 🔗 项目代码已开源：[https://github.com/lcsc/ciena_llm](https://github.com/lcsc/ciena_llm)

</details>

---

### 8. [FC-MIR: A Mobile Screen Awareness Framework for Intent-Aware Recommendation based on Frame-Compressed Multimodal Trajectory Reasoning](https://arxiv.org/abs/2512.19107)

**Authors**: Zhe Yang, Xiaoshuang Sheng, Zhengnan Zhang, Jidong Wu, Zexing Wang, Xin He, Shenghua Xu, Guanjing Xiong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.19107v1  

#### Abstract
Identifying user intent from mobile UI operation trajectories is critical for advancing UI understanding and enabling task automation agents. While Multimodal Large Language Models (MLLMs) excel at video understanding tasks, their real-time mobile deployment is constrained by heavy computational cos...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FC-MIR: A Mobile Screen Awareness Framework for Intent-Aware Recommendation based on Frame-Compressed Multimodal Trajectory Reasoning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文旨在解决在移动设备上从用户界面（UI）操作轨迹中识别用户意图所面临的挑战，具体包括：
- **高计算开销**：直接将视频帧输入 Multimodal Large Language Models (MLLMs) 导致大量视觉 token，影响实时推理效率。
- **视觉冗余严重**：用户操作视频中存在大量模糊、重复或无效帧（如滚动、加载动画），降低了模型理解效率。
- **缺乏轻量化部署方案**：现有 MLLMs 模型体积大，难以在移动端实现高效、低延迟的 on-device 推理。
- **任务边界局限**：已有研究多聚焦于单一任务完成预测，缺乏对多任务交织、动态目标变化等真实场景的支持。

### 🚀 提出的新方法与创新思路
作者提出了 **FC-MIR**（Frame-Compressed Multimodal Intent Reasoning）框架，其核心创新如下：

#### （1）**基于帧压缩的多模态轨迹推理机制**
- 设计了一种无需训练的 **keyframe selection + adaptive stitching** 流程：
  - **Blur Detection**：使用 Laplacian 方差检测模糊帧并过滤。
  - **Similarity Detection**：结合 **Perceptual Hash (pHash)** 和 **SSIM** 进行全局与局部相似性分析，避免删除关键视图。
  - **Adaptive Image Stitching**：利用 ORB 特征匹配与 KNN 匹配策略，拼接连续滚动帧，显著减少像素冗余。

#### （2）**端到端的意图感知推荐系统**
- 将压缩后的关键帧序列送入 MLLM（如 Doubao-1.5-vision-pro-32k 或 fine-tuned Qwen3-VL），进行：
  - 用户行为轨迹摘要生成（Trajectory Summarization）
  - 意图预测（Intent Prediction）
  - 后续操作建议与搜索建议生成（Operation & Search Suggestions）

#### （3）**细粒度评估体系构建**
- 提出一套面向实际应用价值的评估指标，超越传统的 ROUGE/SBERT：
  - **意图摘要质量**：Action Info. Complete, Object Detail Accurate, Output Format Standard 等。
  - **建议实用性**：Relevance, Usefulness, Clarity, Executability, Novelty/Surprise。

#### （4）**真实世界数据集发布**
- 构建两个中文 UI 轨迹数据集：
  - **Agent-I**：由 UI-Agent 自动生成的 260 条任务轨迹（覆盖 100+ App 场景）。
  - **Person-I**：来自真实用户的 50 条复杂交互轨迹，涵盖多任务、跨 App 行为。

### 🔍 相比现有方法的优势
| 对比维度 | 现有方法（如 ScreenLLM, UI-tra, UI-JEPA） | 本文 FC-MIR |
|--------|------------------------------------------|------------|
| 输入形式 | 静态截图 / 结构化元数据 | 原始视频帧序列（更贴近真实） |
| 冗余处理 | 无显式优化 / 固定采样 | 自适应 keyframe + stitching 压缩 |
| 实时性支持 | 不适用于移动端实时推理 | 支持轻量模型 on-device 部署 |
| 任务扩展性 | 仅限意图识别 | 扩展至操作建议与搜索推荐 |
| 数据多样性 | 英文为主 / 单任务为主 | 中文环境 / 多任务 / 真实用户行为 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 类型 | 规模 | 特点 |
|-------|------|-----|------|
| **Agent-I** | Agent-generated | 260 条轨迹 | 显式意图、高质量标注、覆盖主流 App（新闻、购物、旅行等） |
| **Person-I** | Real-user collected | 50 条轨迹 | 包含模糊意图、错误操作、多任务切换，更具现实挑战性 |

> 注：从中选取 30 个高质量样本用于人工评估，每个样本生成两条建议以提升可靠性。

### ⚙️ 实验设置
- **主干模型**：
  - Closed-source: `Doubao-1.5-vision-pro-32k`, `GPT-4o`
  - Open-source: `Qwen3-VL`（base 及 fine-tuned 版本）
- **轻量化探索**：采用 **GRPO**（Generalized Reinforcement Policy Optimization）对 Qwen3-VL-2B 进行 fine-tuning。
- **输入方式对比**：
  - Full Video（均匀采样）
  - Last Frame only
  - Keyframe Sequence
  - Keyframes with Stitching
- **分辨率测试**：384px vs 512px vs 886px

### 📊 评估指标
#### （1）意图摘要质量（Prior Intent Summarization）
| 指标 | 描述 |
|------|------|
| Action Information Completeness | 是否包含所有必要操作 |
| Action Sequence Accuracy | 操作顺序是否正确 |
| Object Detail Accuracy | 是否准确描述对象细节（App 名、关键词、价格区间等） |
| Output Format Standardization | 是否为单句简洁输出 |
| Generated Intent Reasonableness | 生成意图是否合理相关 |

#### （2）建议质量（Recommendation Quality）
| 指标 | 描述 |
|------|------|
| Relevance | 是否贴合用户当前意图 |
| Usefulness | 用户是否会采纳该建议 |
| Clarity | 表达是否清晰易懂 |
| Executability | 是否可直接执行 |
| Novelty/Surprise | 是否提供新颖视角而非重复操作 |

> 所有指标采用 **0–2 分三级评分制**，并结合 **human evaluation** 与 **LLM-as-a-judge**（使用 Doubao 模型自动打分）。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）意图摘要表现（Human Evaluation 平均得分）
| 指标 | 平均得分（满分 2） |
|------|------------------|
| Action Info. Complete | 0.96 |
| Action Sequence Accurate | 0.96 |
| Object Detail Accurate | 0.91 |
| Output Format Standard | 0.98 |
| Generated Intent Reasonable | 0.90 |

> ✅ 主流 MLLMs 在意图摘要任务上表现优异，尤其在动作完整性和格式标准化方面接近完美。

#### （2）建议质量表现
| 建议类型 | Relevance | Usefulness | Novelty/Surprise |
|--------|-----------|------------|------------------|
| Operation Suggestions | 0.90 | 0.59 | 0.53 |
| Search Suggestions | 0.92 | 0.80 | 0.74 |

> ❗ 发现：**Usefulness 和 Surprise 得分偏低**，表明模型仍难生成真正个性化、有启发性的建议。

#### （3）与其他开源模型对比（SBERT + ROUGE）
| Model | SBERT | ROUGE-1 | ROUGE-2 | ROUGE-L |
|-------|-------|---------|---------|---------|
| gemma-3n-E4B | 0.61 | 0.34 | 0.16 | 0.26 |
| MiniCPM-V 4.5 | 0.69 | 0.44 | 0.23 | 0.35 |
| UI-TARS-1.5-7B | 0.70 | 0.39 | 0.24 | 0.36 |
| Kimi-VL-A3B | 0.68 | 0.36 | 0.21 | 0.30 |
| **Doubao (ours)** | **0.81** | **0.53** | **0.33** | **0.46** |

> ✅ Doubao 在各项指标上全面领先，验证其更强的语义理解能力。

---

### 🔬 消融实验结果

#### （1）不同输入模态对比（Qwen-2B-512px）
| 输入方式 | 压缩率 (%) | ROUGE-1 | SBERT |
|--------|------------|---------|--------|
| Full Video | – | 0.53 | 0.77 |
| Last Frame | 5% | 0.40 | 0.70 |
| Keyframe Sequence | 57% | **0.54** | **0.76** |
| Keyframes + Stitching | 44% | 0.50 | 0.76 |

> ✅ **Keyframe Sequence 性能优于 Full Video**，说明去冗有助于聚焦核心意图；同时实现近 60% 压缩率。

#### （2）压缩率影响分析
- 当压缩率达到 **50–60%** 时，性能基本保持甚至略有提升。
- 压缩率达 **70%** 时性能轻微下降但仍可用。
- 结论：**适度压缩可在不损失精度前提下大幅提升效率**。

#### （3）相似性度量方法比较
| 方法 | ROUGE-1 | SBERT |
|------|--------|--------|
| L1 Distance | 0.51 | 0.76 |
| CLIP | 0.50 | 0.73 |
| phash + L1 | 0.50 | 0.75 |
| **phash + SSIM** | **0.54** | **0.76** |

> ✅ **phash + SSIM 组合最优**，兼顾全局一致性与局部结构保留，且计算成本低，适合移动端部署。

#### （4）轻量化模型 fine-tuning 效果（Qwen3-VL-2B）
| 指标 | Base Model | Fine-tuned | 提升幅度 |
|------|-----------|------------|----------|
| SBERT | 0.825 | 0.862 | +4.48% |
| ROUGE-1 | 0.38 | 0.48 | +26.32% |
| ROUGE-2 | 0.22 | 0.31 | +40.91% |
| Reward | 0.56 | 0.66 | +17.86% |

> ✅ GRPO fine-tuning 显著提升小模型性能，证明其具备良好 on-device 部署潜力。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **帧压缩有效且高效**：通过 keyframe selection 与 stitching 技术，在 **50–60% 压缩率下仍能维持甚至提升意图识别性能**，解决了 MLLM 输入 token 过多的问题。
2. **MLLMs 具备强大意图摘要能力**：主流闭源 MLLMs（如 Doubao）在动作完整性、顺序准确性等方面表现优秀，适合用于移动端意图感知。
3. **建议生成仍有较大改进空间**：尽管 Relevance 较高，但 **Usefulness 和 Novelty 得分较低**，反映出模型缺乏长期用户记忆和个性化建模能力。
4. **轻量化模型可通过 fine-tuning 提升性能**：Qwen3-VL-2B 经 GRPO 微调后性能显著增强，为 **on-device 部署提供了可行路径**。
5. **关键帧序列优于最后一帧**：相比仅用最后一帧，使用关键帧序列能更好地重建用户行为轨迹，显著提升建议质量（Improvement up to 87.5% in Usefulness）。

### ⚠️ 方法的局限性
1. **语言限制**：目前数据集主要基于中文环境，未验证英文或其他语言下的泛化能力。
2. **缺乏长期用户记忆**：由于缺少真实用户的长期行为数据，难以生成高度个性化的建议。
3. **依赖大模型性能**：当前方法依赖外部高性能 MLLM，尚未完全摆脱“黑盒”推理模式。
4. **真实场景复杂性不足**：虽然引入了 Person-I 数据集，但样本数量有限，未能充分覆盖极端边缘情况。

### 🔮 未来工作方向
1. 构建 **个性化长期用户记忆库**，支持跨会话意图追踪与推荐。
2. 收集更大规模、更多样化的 **轻量化专用训练数据集**，推动小型专用模型发展。
3. 探索 **token-level 压缩技术**（如 patch merging），进一步降低视觉编码负担。
4. 引入 **agent-based 自我反思机制**，让模型主动优化自身建议质量。
5. 加强 **隐私保护设计**：推广 on-device processing、数据匿名化、用户 opt-in 控制等伦理实践。

---

> 💡 **总体评价**：  
> FC-MIR 是一个面向移动端的、实用性强的 UI 意图感知框架。它不仅在技术上实现了高效的帧压缩与精准的意图推理，更重要的是拓展了任务边界至“操作建议”层面，并建立了面向实际效用的评估体系。该工作为构建下一代智能 UI-Agent 奠定了坚实基础。

</details>

---

### 9. [Helios: A Foundational Language Model for Smart Energy Knowledge Reasoning and Application](https://arxiv.org/abs/2512.19299)

**Authors**: Haoyu Jiang, Fanjie Zeng, Boan Qu, Xiaojie Lin, Wei Zhong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.19299v1  

#### Abstract
In the global drive toward carbon neutrality, deeply coordinated smart energy systems underpin industrial transformation. However, the interdisciplinary, fragmented, and fast-evolving expertise in this domain prevents general-purpose LLMs, which lack domain knowledge and physical-constraint awarenes...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Helios: A Foundational Language Model for Smart Energy Knowledge Reasoning and Application — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前通用大语言模型（General-purpose LLMs）在智能能源（smart energy）领域存在以下关键缺陷：
- 缺乏**领域专业知识**（domain knowledge），难以理解电力工程、能源系统建模等跨学科内容；
- 忽视**物理约束与行业规范**，生成语义合理但物理上无效的推理；
- 领域内高质量训练与评测数据稀缺，限制了模型微调与评估。

这些问题导致通用LLMs在负荷预测、能源系统优化、HVAC故障诊断等任务中表现不稳定且不可靠。

---

### 🚀 提出的新方法与创新思路

本论文提出 **Helios**，是首个专为智能能源领域设计的**基础性大语言模型**（Foundational LLM），并配套构建了一整套资源体系：

#### 主要创新点包括：

1. **Helios 模型架构**
   - 基于 Qwen2.5-7B 初始化，在智能能源领域语料上进行三阶段训练：**大规模预训练 → 指令微调 → 强化学习人类反馈（RLHF）**。
   - 实现从“通用能力”到“专业对齐”的完整演进路径。

2. **EnerSys：端到端多智能体协同数据构建框架**
   - 利用多个 AI Agent 协同完成数据采集、清洗、增强与质量控制，显著提升数据的专业性和一致性。
   - 包含三个核心子系统：
     - **Parsing-Agent & Deduplication-Agent**：处理PDF文献并去重，构建高质量预训练语料；
     - **Expert-Agent & Check-Agent & Refine-Agent**：自动生成并优化指令数据；
     - **Write-like-Human Agent**：生成符合人类风格的回答用于RLHF。

3. **三大专属数据集发布**
   - **EnerBase**：首个智能能源领域的知识库，包含30亿tokens，涵盖科研论文、代码、国际能源署（IEA）数据等；
   - **EnerInstruct**：首个面向该领域的指令微调数据集，覆盖14个子领域、11类任务，经多轮自动优化与专家验证；
   - **EnerReinforce**：首个RLHF数据集，通过对比不同质量回答实现偏好对齐。

4. **EnerBench：首个智能能源领域综合评测基准**
   - 双轨制构建：公开题库检索 + 专家命题；
   - 共计 **1601道题目**（976客观 + 625主观），覆盖选择题、填空、判断、问答、建模与优化等任务。

5. **开源承诺**
   - 所有训练数据、模型检查点均公开于 GitHub：[https://github.com/fine68/Helios](https://github.com/fine68/Helios)

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法 | Helios |
|------|--------|--------|
| 数据来源 | 依赖通用语料或少量prompt工程 | 构建专属知识库 EnerBase 和指令集 EnerInstruct |
| 领域适配方式 | Prompt Engineering / Fine-tuning 应用已有知识 | **Pre-training + Instruction Tuning + RLHF 全流程注入领域知识** |
| 输出可靠性 | 易产生物理不一致或幻觉 | 经过RLHF对齐，输出更贴近专家预期与行业标准 |
| 评估体系 | 缺乏统一benchmark | 发布 EnerBench，支持系统化评估 |

> 💡 总结：Helios 不仅是一个模型，更是**推动智能能源领域AI研究基础设施建设的重要一步**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 类型 | 内容说明 |
|-------|-----|---------|
| **Smart Energy Corpus** | 预训练原始语料 | 来自 arXiv、Web of Science、Acemap、GitHub、HuggingFace 的学术论文、书籍、建模工具代码（如 PyPSA、Oemof）、IEA 能源统计数据 |
| **EnerBase** | 预训练语料 | 经过Markdown转换、结构保留、去重后的高质量文本，共 **2.956 billion tokens** |
| **EnerInstruct** | 指令微调数据 | 包含10,000种子样本，经Agent扩增至80,795条，最终清洗后得 **54,027条高质量<instruction, input, output>三元组** |
| **EnerReinforce** | RLHF 数据 | 分两部分：<br>① Reward Model Training Data：5,000问题，每题4种回答（专家 > Write-like-Human > GPT-4 > GPT-3.5）<br>② Rejection Sampling Data：10,000 Helios生成候选答案用于精炼 |
| **EnerBench** | 测试基准 | 客观题 976 道（S-C, M-C, FC），主观题 625 道（Q&A, Exp, ESM） |

---

### ⚙️ 实验设置

| 阶段 | 模型 | 训练配置 |
|------|------|----------|
| **Pre-training** | Qwen2.5-7B → Helios | 4×A100 80GB GPU，单轮训练，22532步，lr=3e-5，batch_size=64 |
| **Instruction Tuning** | LoRA 微调 | 4×RTX 4090，分两阶段：<br>- UHIC（通用指令）：lr=2e-5<br>- DS-TA（领域任务）：lr=1e-5 |
| **RLHF** | Reward Model + Rejection Sampling | RM训练3 epoch，bs=8；Fine-tuning 5 epoch，lr=3e-5，Top-k采样优化 |

---

### 🎯 评估指标

| 任务类型 | 评估方式 |
|--------|---------|
| **客观任务（S-C, M-C, FC）** | Accuracy：<br>- 多选题需全对得分，错选不得分，漏选部分得分 |
| **主观任务（Q&A, Exp, ESM）** | 三维评分体系：<br>- **A-Score**：基于 GPT-o1 的相对比较打分（0–10）<br>- **E-Score**：GPT-o1 独立评分（0–10）<br>- **H-Grade**：人工评审等级（A/B/C/D） |

---

### 🆚 基线方法对比

参与对比的模型包括：
- 开源模型：`Qwen3-8B-Instruct`, `Llama3-8B-Instruct`, `Qwen3-14B-Instruct`, `Qwen3-32B-Instruct`
- 商业闭源模型：`GPT-3.5-Turbo`, `GPT-4`

> 所有模型均在 EnerBench 上测试，未做额外微调。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 3）

| 模型 | S-C (%) | M-C (%) | FC (%) | ESM (E-Score) | Exp (E-Score) | Q&A (E-Score) |
|------|--------|--------|--------|--------------|---------------|----------------|
| Qwen3-8B-Instruct | 50.24 | 27.56 | 47.81 | 5.88 | 5.04 | 6.50 |
| Llama3-8B-Instruct | 68.42 | 37.60 | 58.77 | 6.03 | 6.29 | 6.47 |
| Qwen3-32B-Instruct | 80.14 | 44.09 | 62.72 | 6.93 | 7.32 | 7.51 |
| GPT-3.5-Turbo | 91.63 | 53.93 | 84.65 | 8.05 | 8.57 | 8.37 |
| GPT-4 | **95.69** | **61.18** | **93.86** | **8.97** | **9.58** | **9.21** |
| **Helios (7B)** | 93.78 | 53.58 | 89.91 | 7.83 | 9.19 | 8.26 |

> 注：Helios 参数量约为 7.6B，远小于 GPT-4（~220B）和 Qwen3-32B（32B）

---

### 🔍 对比分析

- **客观任务平均准确率**：
  - Helios 达到 **79.09%**，显著优于同规模开源模型（Qwen3-8B: 41.87%, Llama3-8B: 54.93%）
  - 接近甚至超过更大参数模型（如 Qwen3-32B: ~62%）
  - 逼近 GPT-4 表现（约95%）

- **主观任务表现**：
  - 在 **Explanation (Exp)** 和 **Question Answering (Q&A)** 上，Helios 的 E-Score 已接近 GPT-4 水平；
  - 在 **Energy System Modeling (ESM)** 上虽仍有差距（7.83 vs 8.97），但仍优于 GPT-3.5-Turbo（8.05），表明其具备较强的专业建模能力。

- **输出风格与专业性**：
  - 人工评估显示，Helios 输出更符合专业语境，逻辑严密，术语准确，较少出现“泛化式胡说”。

---

### 🔍 消融实验（文中未明确列出表格，但可推断）

虽然没有独立消融表，但从训练流程可得出以下隐含结论：

| 组件 | 影响 |
|------|------|
| **EnerBase 预训练** | 是获取基础领域知识的关键，使模型掌握能源系统术语、公式、政策背景等 |
| **EnerInstruct 指令微调** | 显著提升任务理解与执行能力，尤其在NER、Summarization等结构化任务中 |
| **EnerReinforce + RLHF** | 改善输出风格一致性与人类偏好对齐，减少冗余、模糊表达，提高实用性 |

> 文中指出：“经过RLHF后，模型输出风格紧密匹配专业语境”，说明该阶段对齐效果显著。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **领域专用LLM显著优于通用模型**
   - 尽管 Helios 仅为 7B 规模，但在多数任务上超越数十倍参数的通用模型（如 Qwen3-32B），证明**领域知识注入的有效性远大于单纯扩大参数规模**。

2. **多智能体协作框架 EnerSys 可高效构建高质量数据**
   - 自动化生成 + 自动质检 + 专家干预 的混合模式，大幅降低数据构建成本，同时保障质量。

3. **RLHF 能有效提升输出的专业性与可信度**
   - 通过引入专家偏好排序，模型学会区分“技术正确”与“表达专业”，避免“听起来像专家但实际上错误”的问题。

4. **EnerBench 成为可靠评测标准**
   - 兼顾广度（14子领域）与深度（建模与优化任务），可用于后续模型迭代评估。

---

### ⚠️ 局限性

1. **参数规模限制**
   - 当前 Helios 基于 7B 模型，面对复杂系统建模（如多区域电碳耦合优化）仍不如 GPT-4 强大。

2. **数据更新滞后性**
   - 尽管 EnerBase 规模庞大，但能源技术发展迅速，需持续更新语料以保持前沿性。

3. **计算资源门槛高**
   - 多Agent框架依赖GPT-4等强模型作为Agent，可能影响完全开源社区复现。

4. **主观评价仍依赖外部模型（如GPT-o1）**
   - 存在潜在偏见风险，理想情况应建立完全自主的评估机制。

---

### 🔮 未来工作方向

1. **扩展模型规模**
   - 推出 Helios-14B 或更大版本，进一步缩小与GPT-4在复杂建模任务上的差距。

2. **动态知识更新机制**
   - 构建增量式学习管道，定期抓取最新论文与政策文件，实现模型持续进化。

3. **多模态扩展**
   - 整合 GIS 数据、SCADA 图形、设备图像等，打造 **Multimodal Helios**，支持视觉-语言联合推理。

4. **部署轻量化版本**
   - 推出适用于边缘设备（如变电站本地服务器）的小型化 Helios-Lite，支持实时决策辅助。

5. **构建开放社区生态**
   - 鼓励研究人员基于 EnerBench 提交新模型，形成智能能源AI的“ImageNet时刻”。

---

> 🌞 **总结一句话**：  
> **Helios 是智能能源领域迈向专业化AI的第一座里程碑，它不仅提供了一个高性能模型，更重要的是建立了“数据-模型-评测”三位一体的研究范式，为未来可持续能源系统的智能化奠定了坚实基础。**

</details>

---

### 10. [L4: Low-Latency and Load-Balanced LLM Serving via Length-Aware Scheduling](https://arxiv.org/abs/2512.19179)

**Authors**: Yitao Yuan (Peking University, ScitiX AI), Chenqi Zhao (Peking University), Bohan Zhao (ScitiX AI), Zane Cao (ScitiX AI), Yongchao He (ScitiX AI), Wenfei Wu (Peking University)  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.19179v1  

#### Abstract
Efficiently harnessing GPU compute is critical to improving user experience and reducing operational costs in large language model (LLM) services. However, current inference engine schedulers overlook the attention backend's sensitivity to request-length heterogeneity within a batch. As state-of-the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# L4: Low-Latency and Load-Balanced LLM Serving via Length-Aware Scheduling — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代大语言模型（LLM）服务系统在处理**长上下文、多轮对话**等场景时，面临严重的**请求长度异质性（length heterogeneity）**问题。尽管当前推理引擎（如 vLLM、SGLang）采用连续批处理（continuous batching），但在一个 batch 中混合长短不一的序列会导致：

- **Attention 后端性能下降**：由于 FlashAttention 等 kernel 对序列长度敏感，混合长度导致 GPU 负载不均、同步开销增加；
- **GPU 利用率低下**：短请求被长请求“拖累”，造成硬件资源浪费；
- **延迟显著上升**：尤其是尾部延迟（tail latency）恶化。

传统调度器（如 Llumnix）仅在实例内部或跨实例进行负载均衡，但**缺乏对请求长度分布的感知能力**，无法从根本上缓解该瓶颈。

---

### 🚀 提出的新方法与核心思想
作者提出 **L4** —— 一种基于**长度感知的跨实例调度系统**，其核心理念是：

> **“Global homogenization for local efficiency”**  
> （全局同质化以实现局部高效）

#### 主要机制包括：
1. **长度专用分组（Length-Specialized Groups）**  
   将多个 LLM 推理实例划分为若干阶段（stages），每个阶段专门处理特定长度范围内的请求，形成一个逻辑上的流水线（pipeline）。随着请求解码增长，自动迁移至更长的阶段。

2. **动态规划驱动的阶段划分（Dynamic Programming-based Stage Partitioning）**  
   设计了一个 QoE 模型，并通过动态规划算法在多项式时间内找到最优的 stage 数量、各 stage 长度区间及实例分配方案。

3. **运行时自适应边界调整（Adaptive Range Refinement）**  
   各实例通过 LoadTracker 收集实时请求长度分布，动态优化 stage 边界，应对负载漂移。

4. **去中心化的 Bid-Ask 请求重平衡协议**  
   当某 stage 内部负载失衡时，过载实例“报价”（ask），空闲实例“竞价”（bid），通过轻量级协商完成请求迁移，避免集中式调度瓶颈。

---

### 🔍 相比现有方法的优势
| 维度 | 现有方法（如 vLLM, SGLang, Llumnix） | L4 |
|------|----------------------------------------|-----|
| 调度粒度 | 单实例内调度 / 负载感知路由 | 全局长度感知 + 流水线式调度 |
| 是否感知长度 | ❌ 或弱感知 | ✅ 显式建模并利用长度信息 |
| 批次同质性 | 低（不可避免混合长短请求） | 高（每 stage 自然形成同质 batch） |
| 可扩展性 | 中心化调度可能成为瓶颈 | 去中心化 bid-ask 协议支持横向扩展 |
| 性能提升来源 | 更好的内存管理 / kernel 优化 | 从系统架构层面重构请求流 |

> 💡 **本质突破**：将“如何更好地在一个实例中调度”转变为“如何在全球范围内重新组织请求流向”，从而匹配 attention kernel 的执行偏好。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用公开的 **ShareGPT [1]** 对话数据集构建 workload。
- 请求长度高度偏斜（skewed distribution）：大量短请求 + 少量极长请求（部分超过 100K tokens）。
- 请求到达过程模拟为 **Poisson 过程**，可配置不同请求速率（req/s）。

---

### ⚙️ 实验设置
| 项目 | 描述 |
|------|------|
| **测试平台** | 两个测试床：<br>• H20（NVLink连接，16×H20 GPU，141GB显存）<br>• L40（PCIe连接，16×L40 GPU，48GB显存） |
| **模型范围** | 8 个主流 LLM：<br>Llama-3.2-3B, Phi-3-3B, Llama-3.1-8B, GLM-4-9B, Phi-3-14B, Qwen2.5-14B, QwQ-32B, Qwen2.5-32B<br>所有模型支持 ≥128K 上下文长度 |
| **部署方式** | 多实例部署（Multi-Instance LLM Serving, MILS），每个 GPU 运行一个 instance；对于 Llama-3.1-70B 使用 tensor parallelism（TP=2/4） |
| **L4 配置** | 构建 4–6 stage 流水线，每 stage 包含 1–4 个实例 |

---

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **TTFT (Time to First Token)** | 用户提交请求到收到第一个输出 token 的时间 |
| **TPOT (Time per Output Token)** | 平均生成每个后续 token 的延迟 |
| **Throughput** | 系统整体每秒生成的 token 数量 |
| **SLO Attainment** | 满足预设服务等级目标（SLO）的请求比例 |
| **Normalized Latency** | 每个输出 token 的端到端延迟（含 TTFT 和 TPOT） |

---

### 🆚 基线方法对比
| 基线 | 描述 |
|------|------|
| **vLLM 0.9.1** | 使用 FlashAttention，标准 FCFS 调度 |
| **SGLang 0.4.9** | 使用 FlashInfer，支持结构化 prompt 执行 |
| **Llumnix [24]** | 支持跨实例调度与 live migration 的先进系统，作为 SOTA 多实例调度代表 |
| **Round-Robin LB** | 在 vLLM/SGLang 上加简单负载均衡器 |

> 所有系统均启用 continuous batching，batch size 最大为 1024。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（H20 测试床，重负载条件下）

| 指标 | L4 相比 vLLM 提升 | L4 相比 SGLang 提升 | L4 相比 Llumnix 提升 |
|------|------------------|--------------------|---------------------|
| **Median TTFT** | ↓ **67%~78%** | ↓ **70%~84%** | ↓ **36%~66%** |
| **95th TTFT (Tail)** | ↓ ~56% | ↓ ~62% | ↓ ~19% |
| **Mean TPOT** | ↓ **30%~64%** | ↓ **25%~77%** | ↓ **3.4%~56%** |
| **Tail TPOT** | ↓ 25% | ↓ 61% | ↓ 20% |
| **Throughput** | ↑ **1.99×** | ↑ **2.18×** | ↑ **1.71×** |
| **SLO Attainment (5×SLO)** | ↑ **3.8–7.6×** | — | — |

> ✅ 在 L40 和 Tensor Parallelism 场景下也保持显著优势：
- L40 上吞吐提升 **1.21×~1.37×**
- TP=4 下吞吐最高达 **4.16×**

---

### 🔪 消融实验结果（Ablation Study）
使用 Llama-3.2-3B 模型验证各组件有效性：

| 实验配置 | 结果分析 |
|---------|----------|
| **No Pipeline（所有实例合并为单 stage）** | 表现最差，说明长度分区至关重要 |
| **Chain Layout（每 stage 仅 1 实例）** | 因迁移开销过大且难负载均衡，性能低于 L4（延迟高 30%，吞吐低 7.1%） |
| **Quantity-Based Boundary Adjustment** | 忽视 workload 差异，导致严重不平衡 |
| **Memory-Based Adjustment** | 比 quantity 好，但仍不如 L4 的 QoE-aware 方法（延迟高 21%，吞吐低 12%） |
| **Round-Robin vs Bid-Ask** | Round-robin 负载方差最大；full bid-ask 使 CV（变异系数）降低 **47%** |
| **Stage Partitioning 优化效果** | 优化后耗时 **0.06s**，相比未优化（估算 51 小时）提速约 **3×10⁶倍** |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Attention kernel 的性能强烈依赖于 batch 内序列长度的一致性**，而现有调度策略无法有效控制这一因素。
2. **Little’s Law 限制了单实例积累足够请求来消除异质性的能力**，必须跳出本地调度框架，在集群层面进行全局协调。
3. **L4 通过构建长度感知流水线，实现了“无需修改底层引擎即可大幅提升效率”**，兼容现有 inference engine（如 vLLM）。
4. **去中心化 bid-ask 协议实现了高效的负载再平衡**，避免了中心控制器带来的延迟和可扩展性问题。
5. **实际收益随可用内存和并行度增加而放大**：更大的 batch size 和更多 GPU 资源使得 L4 的优势更加明显。

---

### ⚠️ 局限性
| 限制 | 说明 |
|------|------|
| **对小显存设备增益有限** | 如 L40 显存较小，限制了 batch size 和 KV cache 容量，削弱了长度同质化带来的收益 |
| **大参数模型占用过高时效果受限** | 若模型本身占用了 80% 以上显存，则剩余空间不足以支撑大规模 KV caching，影响 L4 发挥 |
| **假设同构实例环境** | 当前评估基于 homogeneous setup，异构资源配置（不同 GPU 类型、TP 设置）尚未深入探索 |
| **冷启动与边界震荡风险** | 尽管引入 EMA 和低流量冻结机制，极端波动仍可能导致频繁重配置 |

---

### 🔮 未来工作方向
1. **支持异构实例调度**：结合 tensor/pipeline/expert parallelism 进行混合部署下的最优 stage 规划。
2. **更智能的 workload 预测**：引入 ML 模型预测请求长度演化趋势，提前调整 stage 结构。
3. **与 MoE 架构集成**：在 Mixture-of-Experts 系统中应用类似思想，按 expert 访问模式进行请求分流。
4. **绿色计算视角优化**：将 energy efficiency 纳入 QoE 模型，实现能效与性能的联合优化。

---

## 总结
> **L4 成功地将“请求长度异质性”这一硬件敏感问题，转化为一个可调度的系统设计机会。它不是改进单个 instance 的调度器，而是重塑整个 multi-instance 系统的请求流动态，实现了真正的“软硬协同优化”。**

其核心价值在于：
- **架构简洁**：不改动底层 attention kernel；
- **兼容性强**：可在 vLLM、SGLang 等主流引擎上集成；
- **性能飞跃**：在真实 workload 下实现高达 **2.89× 吞吐提升** 和 **67% 延迟下降**；
- **可扩展性好**：去中心化设计适合大规模部署。

👉 **L4 代表了一种新的 LLM serving 范式转变：从“被动适应请求”转向“主动塑造请求流”。**

</details>

---

### 11. [MoE-TransMov: A Transformer-based Model for Next POI Prediction in Familiar & Unfamiliar Movements](https://arxiv.org/abs/2512.17985)

**Authors**: Ruichen Tan, Jiawei Xue, Kota Tsubouchi, Takahiro Yabe, Satish V. Ukkusuri  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.17985v1  

#### Abstract
Accurate prediction of the next point of interest (POI) within human mobility trajectories is essential for location-based services, as it enables more timely and personalized recommendations. In particular, with the rise of these approaches, studies have shown that users exhibit different POI choic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-TransMov: A Transformer-based Model for Next POI Prediction in Familiar & Unfamiliar Movements — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有 Next POI 预测模型通常将所有用户移动行为统一建模，**忽略了用户在熟悉区域（familiar regions）和陌生区域（unfamiliar regions）中表现出显著不同的移动模式**。例如：
- 在熟悉区域，用户倾向于重复访问常去地点；
- 在陌生区域（如旅游、出差），用户更可能探索新地点，轨迹更具随机性和多样性。

这种混合建模容易导致“负迁移”（negative transfer），即模型在一种上下文中学习到的知识干扰另一种上下文的预测性能。

---

### 🚀 提出的新方法与创新思路
作者提出 **MoE-TransMov**，一个基于 **Transformer** 和 **Mixture-of-Experts (MoE)** 架构的新型模型，用于区分并联合建模用户的熟悉与陌生移动行为。

#### 主要创新点包括：
1. **首次系统性地将用户移动划分为“熟悉”与“陌生”两类情境**，并通过数据实证验证两者在 POI 类型偏好、轨迹结构上的显著差异（见 Figure 1）。
2. 设计了一个 **共享编码器 + 多专家解码器** 的 MoE 架构：
   - **共享 Transformer Encoder**：提取全局用户移动共性特征；
   - **双专家结构（Two Experts）**：
     - *Familiar Expert*：基于 **Transformer**，擅长捕捉长序列依赖；
     - *Unfamiliar Expert*：基于 **LSTM**，更适合处理稀疏、短序列；
   - **可学习的 Gating Network**：动态决定输入序列应由哪个专家处理，无需显式标注“熟悉/陌生”标签。
3. 实现了**单模型统一训练**，避免为不同场景分别训练多个模型，提升效率与泛化能力。

---

### 🔍 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **建模灵活性** | MoE 结构允许模型自适应选择最适合当前移动上下文的专家，实现“按需调用”。 |
| **防止负迁移** | 分离建模机制有效避免了熟悉与陌生行为之间的相互干扰。 |
| **计算高效性** | MoE 的稀疏激活特性保证仅部分参数参与前向传播，保持高扩展性。 |
| **端到端学习** | 不需要预先标记“熟悉/陌生”，路由决策由历史轨迹自动学习得出。 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
| 数据集 | 描述 |
|--------|------|
| **Foursquare NYC Dataset** | 公开小规模数据集，包含纽约市用户签到记录，作为基准对比；用户数少、POI 数量有限、流行度偏差大。 |
| **LY Corporation Kyoto Dataset** | 自研大规模真实世界数据集，来自日本京都地区，由 LY Corporation（Yahoo Japan）提供；包含超过 100 万条轨迹，约 5 倍于 Foursquare 的 POI 数量，时间跨度达 8 个月，密度更高、行为更多样。 |

> 注：两个数据集均经过匿名化处理，仅保留用户 ID、POI 坐标、类别、时间戳等信息。

---

### ⚙️ 实验设置
- **数据划分**：按轨迹级别划分 80% 训练集、20% 验证集；
- **序列长度**：训练时最大长度为 50，支持最长 500 的位置编码；
- **模型结构参数**：
  - Transformer Expert：4 层编码器，8 头注意力，隐藏维度 128；
  - LSTM Expert：2 层，隐藏大小 64；
  - Gating Network 输出两路 softmax 权重 `[g1, g2]`，加权融合专家输出；
- **优化器**：Adam，学习率 0.0005，batch size 64；
- **早停策略**：连续 3 轮验证损失上升则停止训练。

---

### 📏 评估指标
采用标准推荐系统评价指标：
- **Top-k Accuracy (k=1,5,10)**：预测列表前 k 个是否包含真实下一个 POI；
- **Mean Reciprocal Rank (MRR)**：衡量正确 POI 在排序中的平均排名质量。

测试子集分为三类：
1. **Familiar Movement Subset**
2. **Unfamiliar Movement Subset**
3. **Total Movement Subset**

---

### 🆚 对比的基线方法
| 模型 | 类型 | 特点 |
|------|------|------|
| Majority Vote | 统计模型 | 预测最常访问的 POI |
| MLP | 浅层神经网络 | 学习非线性关系 |
| LSTM | 序列模型 | 捕捉长期依赖 |
| DeepMove | RNN+Attention | 引入注意力机制 |
| STAN | Spatio-Temporal Attention | 同时关注时空维度 |
| GETNext | Graph-based | 利用轨迹图结构 |
| Transformer | Self-Attention | 并行处理全序列，捕获远距离依赖 |
| Informer | Efficient Transformer | 使用 ProbSparse 注意力，适合长序列 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（摘自 Tables 3 & 4）

#### 在 **Foursquare NYC Dataset** 上的表现：
| Model | Unfamiliar MRR ↑ | Total MRR ↑ |
|-------|------------------|-------------|
| Majority Vote | 0.0461 | 0.1906 |
| LSTM | 0.1510 | 0.2290 |
| Transformer | 0.0865 | 0.1366 |
| **MoE-TransMov** | **0.1595** | **0.2639** |

> 💡 尽管 Majority Vote 在 Familiar 子集上表现最好（因高度重复访问），但在 Unfamiliar 场景下几乎失效（Top-1: 0.0056）。而 MoE-TransMov 在 Unfamiliar 和 Total 上全面领先。

---

#### 在 **LY Corporation Kyoto Dataset** 上的表现（更具现实意义）：
| Model | Familiar MRR ↑ | Unfamiliar MRR ↑ | Total MRR ↑ |
|-------|----------------|------------------|-------------|
| Majority Vote | 0.0851 | 0.0186 | 0.0519 |
| LSTM | 0.1570 | 0.1397 | 0.1485 |
| STAN | 0.1668 | 0.1384 | 0.1522 |
| Transformer | 0.1770 | 0.1737 | 0.1754 |
| **MoE-TransMov** | **0.1826** | **0.2072** | **0.1946** |

> ✅ MoE-TransMov 在所有子集上均取得最佳性能，尤其在 **Unfamiliar Movement** 上 MRR 提升显著（+18.8% vs Transformer）。

---

### 🔍 消融实验结果（Ablation Study）
| 变体 | Unfamiliar MRR ↓ | Total MRR ↓ | 结论 |
|------|------------------|-------------|------|
| w/o MoE（仅 Transformer） | 0.1737 | 0.1754 | 移除 MoE 导致性能下降，尤其在陌生场景 |
| w/o Transformer（无共享编码器） | 0.1748 | 0.1617 | 缺乏全局表示能力，影响整体性能 |
| MoE(2 LSTMs) | 0.2005 | 0.1903 | 单一架构限制表达能力 |
| MoE(2 Transformers) | 0.1913 | 0.1879 | 不适合稀疏序列 |
| **MoE(1 Transformer + 1 LSTM)** | **0.2072** | **0.1946** | **异构专家组合最优** |

> ✅ 实验表明：**异构专家设计（Transformer + LSTM）是成功的关键**，能同时应对长短、稠密稀疏的不同移动模式。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **用户在熟悉与陌生区域的行为存在本质差异**，必须区别对待才能提高预测精度；
2. **MoE 架构能有效分离异质移动模式**，通过门控机制实现上下文感知的专家选择；
3. **MoE-TransMov 在真实大规模数据集（Kyoto）上表现尤为突出**，证明其对复杂城市环境具有强鲁棒性和泛化能力；
4. **简单的频率统计方法（如 Majority Vote）在高度偏态的小数据集（Foursquare）上可能“虚高”表现**，但在多样化的真实场景中失效；
5. **共享编码器 + 专用专家的设计实现了“统一框架下的个性化建模”**，兼顾效率与准确性。

---

### ⚠️ 方法的局限性
1. **专家数量固定为 2**，无法自动发现更多潜在移动模式（如通勤、周末休闲、商务出行等）；
2. 当前 MoE 路由完全依赖历史轨迹嵌入，未引入外部上下文信号（如天气、节假日、用户意图）；
3. 模型未考虑社交因素或群体行为影响（如同伴同行、热点事件聚集）；
4. 虽然 MoE 提升了性能，但也增加了模型复杂度，在资源受限设备部署可能存在挑战。

---

### 🔮 未来工作方向
1. **引入更多上下文模态**：融合天气、交通、节假日、搜索日志等辅助信息，增强 context-aware 推理能力；
2. **扩展专家数量与层次化门控机制**：构建层级 MoE 或动态增减专家，以识别更细粒度的行为模式；
3. **跨城迁移学习（Cross-City Transfer）**：研究模型在不同城市间的可迁移性，探索文化、地理结构的影响；
4. **集成社会信号与群体行为建模**：利用 co-location 数据或社交网络关系，提升协同推荐能力；
5. **轻量化与边缘部署优化**：针对移动端应用进行模型压缩与推理加速。

---

## 总结
> **MoE-TransMov 是首个将 Mixture-of-Experts 机制应用于 Next POI 预测，并明确区分“熟悉”与“陌生”移动上下文的工作**。它通过共享 Transformer 编码器与异构专家结构（Transformer + LSTM），实现了对多样化人类移动行为的精细化建模。实验表明，该方法在多个真实数据集上显著优于主流基线，特别是在稀疏且不可预测的“陌生区域”场景中展现出强大优势。这一框架为构建更加个性化、情境感知的位置推荐系统提供了新的范式。

</details>

---

### 12. [Rethinking Multi-Agent Intelligence Through the Lens of Small-World Networks](https://arxiv.org/abs/2512.18094)

**Authors**: Boxuan Wang, Zhuoyun Li, Xiaowei Huang, Yi Dong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.18094v1  

#### Abstract
Large language models (LLMs) have enabled multi-agent systems (MAS) in which multiple agents argue, critique, and coordinate to solve complex tasks, making communication topology a first-class design choice. Yet most existing LLM-based MAS either adopt fully connected graphs, simple sparse rings, or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Rethinking Multi-Agent Intelligence Through the Lens of Small-World Networks*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Models (LLMs)** 的 **Multi-Agent Systems (MAS)** 在设计通信拓扑时缺乏结构性指导。大多数系统采用全连接（fully-connected）、环状稀疏连接（ring/sparse）或动态选择机制，这些结构要么通信成本高，要么稳定性差，且未借鉴自然界中广泛存在的高效组织原则。

本文提出：**是否可以将小世界网络（Small-World Network, SW）作为多智能体系统的先验通信结构？**

### 提出的新方法与新思路
- **引入 Small-World (SW) 结构作为 MAS 的通信拓扑先验**  
  受神经科学和复杂网络理论启发，作者主张将 SW 网络——具有高局部聚类性和短平均路径长度的结构——作为提升多智能体协作效率与稳定性的设计范式。

- **提出不确定性引导的重连机制（uncertainty-guided rewiring）**  
  利用 LLM 的语义不确定性信号（如 semantic entropy），在认知差异较大的 agent 之间建立长程“捷径”（shortcuts），从而动态构建可控的小世界结构。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **稳定性** | 显著稳定共识形成过程，减少震荡和错误传播 |
| **效率** | 维持接近最优的准确率，同时通信开销远低于全连接结构 |
| **可扩展性** | 支持通过不确定性信号自适应调整拓扑，适用于复杂任务和异构 agent 群体 |
| **认知多样性** | 自然诱导不同拓扑角色（如 Hub、Bridge、Loner），无需显式角色设计 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个代表性基准上进行：
- **ARC-Challenge**：多项选择题问答，测试常识推理能力
- **ScienceQA**：多模态科学问题理解与推理
- **GSM8K**：数学应用题求解，强调逻辑链推理

### 实验设置
- **框架**：Multi-Agent Debate (MAD)
- **模型**：使用 GPT-4o-mini 作为 agent 的底层 LLM
- **agent 数量**：8 个
- **通信拓扑对比**：
  - Fully-connected（全连接）
  - Sparse ring（稀疏环形）
  - Random topology（每轮随机采样边，记为 Rand）
  - Small-world topology（通过对环形结构少量重连构造）

### 评估指标
- **准确性（Accuracy）**：最终答案正确率
- **通信成本（Token Cost）**：平均每道题消耗的 token 数
- **共识动态（Consensus Dynamics）**：各轮辩论中 agent 达成一致的程度随时间的变化趋势

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Figure 2 & 3）

| 拓扑类型 | 准确率（平均） | Token 成本（相对） | 共识稳定性 |
|---------|----------------|--------------------|------------|
| Fully-connected | ~92% | 高 | 中等波动 |
| Sparse ring | ~84–86% | 最低 | 波动大，收敛慢 |
| Random (Rand) | **最高 (~95%)** | **最低** | **剧烈震荡，部分退化** |
| **Small-world** | **≈92–93%（接近最佳）** | **适中** | ✅ **最平稳，方差最小** |

> 注：尽管 Rand 在准确率和成本上表现最好，但其共识轨迹不稳定，存在“退化”现象（即后期正确率下降）。

### 与基线方法的对比结果
- **Small-world 在准确率上媲美 Rand 和 Fully-connected**
- **通信成本显著低于 Fully-connected**
- **共识动态明显优于所有 baseline**：收敛更平滑，波动更小，抗干扰能力强
- 在 GSM8K 上，Small-world 的共识曲线几乎单调上升，而 Rand 出现多次反复

### 消融实验与分析（隐含于案例研究）
- 不同重连概率 $ p $ 对性能的影响符合 Watts-Strogatz 模型预期：中间值（SW 区域）平衡了局部聚集与全局连通
- 引入少量 long-range shortcuts 即可大幅提升稳定性，无需密集连接

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Small-world 拓扑是多智能体协作的理想折衷结构**  
   它结合了局部一致性维护与全局信息快速传播的能力，在准确性、成本和稳定性之间取得良好平衡。

2. ✅ **SW 结构显著稳定共识动态**  
   尽管最终准确率相近，但 SW 构造的 agent 网络避免了错误信号的放大和震荡，使推理过程更具可预测性。

3. ✅ **不确定性可作为构建 SW 捷径的有效信号**  
   通过 semantic entropy 等 UQ 方法识别“最不确定”与“最自信”的 agent，并在其间添加 shortcut，能有效打破信息瓶颈（water barrel effect），加速共识达成。

4. ✅ **SW 拓扑天然诱导认知角色分化**  
   实验模拟显示，SW 结构自发产生三类典型角色：
   - **Expert**：高度连接的 hub，快速收敛
   - **Loner**：边缘节点，信念波动大
   - **Bridge**：跨社区连接者，整合多方观点，行为更具中介性  
   → 表明拓扑本身即可成为行为多样性的来源，无需人工提示工程。

### 方法的局限性
- 当前实验集中在 MAD 场景，尚未验证于更复杂的协调任务（如分布式规划、资源分配）
- 不确定性度量依赖 LLM 输出分布，可能受 prompt 设计影响
- 动态重连机制尚未实现端到端学习，仍需外部控制策略

### 未来工作方向
1. **开发基于 uncertainty 的实时拓扑自适应算法**
2. **探索 epistemic vs. aleatoric uncertainty 在 shortcut 形成中的作用差异**
3. **将 SW 拓扑纳入 MARL 或 AutoGen 类框架中进行端到端训练**
4. **研究 topology 与 reasoning 的协同演化机制**
5. **推广至异构 agent 群体和分层 MAS 架构**

---

## 总结
该论文从 **Small-World Network** 的视角重新思考了 LLM-based MAS 的通信结构设计，提出了一个兼具理论基础与实践潜力的新范式。实验证明，**SW 拓扑不仅能在保持高性能的同时降低通信负担，更重要的是提供了强大的推理稳定性保障**。通过引入 **uncertainty-guided rewiring**，进一步实现了对 SW 结构的可控生成，为构建高效、鲁棒、可扩展的多智能体系统提供了新的设计原则。

</details>

---

### 13. [Multimodal Bayesian Network for Robust Assessment of Casualties in Autonomous Triage](https://arxiv.org/abs/2512.18908)

**Authors**: Szymon Rusiecki, Cecilia G. Morales, Kimberly Elenberg, Leonard Weiss, Artur Dubrawski  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.18908v1  

#### Abstract
Mass Casualty Incidents can overwhelm emergency medical systems and resulting delays or errors in the assessment of casualties can lead to preventable deaths. We present a decision support framework that fuses outputs from multiple computer vision models, estimating signs of severe hemorrhage, respi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Multimodal Bayesian Network for Robust Assessment of Casualties in Autonomous Triage 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Mass Casualty Incidents (MCIs) 中，传统手动 triage 方法在面对高伤亡人数、复杂伤情和资源受限的情况下容易出现延迟或错误判断，导致可预防的死亡。现有的基于独立计算机视觉模型的自动化 triage 系统存在以下三大缺陷：
- **Algorithmic isolation**：各生理体征被孤立处理；
- **Context blindness**：忽略临床变量之间的依赖关系；
- **Brittleness**：对噪声、遮挡和不完整数据极为敏感。

这些问题限制了AI系统在真实灾难场景中的可靠性与实用性。

### 提出的新方法/新思路
作者提出了一种基于 **Bayesian Network (BN)** 的决策支持框架，用于融合多个计算机视觉模型输出（如出血、呼吸困难、意识状态等），实现对伤员状况的概率化推理。该模型具有以下特点：

- **完全由专家定义规则构建**：无需训练数据，通过医学专家知识构建网络结构和 Conditional Probability Tables (CPTs)；
- **多模态信息融合**：整合来自RGB相机、热成像、雷达、LiDAR等多种传感器的感知结果；
- **支持不完整与不确定信息下的推理**：即使部分传感器失效或输入缺失，仍能进行有效推断；
- **实时性与轻量化设计**：推理时间低于1ms，可在树莓派等边缘设备运行。

### 相比现有方法的优势
| 维度 | 本文方法 (BN-based) | 传统数据驱动模型 |
|------|---------------------|------------------|
| 数据需求 | 无监督，无需大规模标注数据 | 需要大量高质量训练数据 |
| 可解释性 | 高（基于明确的因果逻辑） | 低（“黑箱”模型） |
| 容错能力 | 强（支持missing data inference） | 弱（单点故障易崩溃） |
| 部署灵活性 | 模块化设计，易于集成新感知模块 | 耦合性强，重构成本高 |

此外，该方法实现了从“静态分类”到“动态概率更新”的转变，更贴近人类医护人员的逐步推理过程。

---

## 2. 核心实验方法和设置

### 使用的数据集
研究并未使用公开标准数据集，而是参与了 **DARPA Triage Challenge (DTC)** 的实地演练任务，包含三个模拟MCI场景：
- **Open Battlefield**（开放战场）
- **Convoy Ambush**（车队伏击）
- **Airplane Crash**（飞机失事）

其中前两个场景用于主分析，第三个因硬件故障未纳入定量评估。

每个场景中由真人志愿者和高级医疗仿真假人扮演共 **20名伤员**，具备预设的真实伤情标签（ground truth）。

### 实验设置
- **平台**：搭载多传感器机器人平台（RGB、thermal、radar、lidar、mic）
- **感知层**：多个独立AI节点作为“black-box”模块，分别检测特定生理体征
- **推理层**：BN模型部署于ROS2系统中，利用SMILE引擎实现实时推理
- **测试流程**：机器人自主导航并采集数据，在30分钟内完成所有伤员评估

### 评估指标
依据DARPA官方评分标准（见Table 1），主要包括：
- **Triage Score**：根据正确识别关键生命体征的数量与时效性打分（总分12分/人）
- **Golden Window (GW)**：前半段时间内识别危重症状加分更高
- **Performance** = 正确赋值数 / 总可能评估项（180项）
- **Reliability** = 实际完成评估数 / 总可能评估项
- **Accuracy** = 正确赋值数 / 实际尝试赋值数

### 基线方法对比
- **Baseline ("Robot Score")**：仅使用原始感知模型输出，无中央推理机制
- **Proposed Method ("Robot + BN Score")**：加入BN进行多源信息融合与概率推理

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 2 & Table 3）

| 指标 | Baseline (Robot) | Proposed (Robot + BN) | 提升倍数 |
|------|------------------|------------------------|---------|
| **Open Battlefield 总得分** | 25/132 | 61/132 | **2.44x** |
| **Convoy Ambush 总得分** | 16/108 | 45/108 | **2.81x** |
| **Overall Performance (Correct Vitals Rate)** | 14% | **53%** | **3.79x** |
| **Diagnostic Coverage (Reliability)** | 31% | **95%** | **3.06x** |
| **Assessment Accuracy** | 46% | **56%** | +10pp |

> 注：Performance 表示在整个潜在评估项目中的正确率；Reliability 表示系统能够输出的比例。

### 各生理体征具体提升情况（Table 3）
| Vital Sign | Baseline 正确数 | BN 正确数 | 提升幅度 |
|----------|------------------|-----------|-----------|
| Severe Hemorrhage | 6 → 12 | +100% |
| Respiratory Distress | 5 → 16 | +220% |
| Head Trauma | 0 → 15 | 从零突破 |
| Motor Alertness | 0 → 8 | 从零突破 |
| Ocular Alertness | 0 → 8 | 从零突破 |

> 多个原本无法稳定检测的体征（如Head Trauma）在BN融合后显著改善，说明BN能通过间接证据（如眼睑闭合）反向推断隐藏伤情。

### 与其他团队排名对比
- 在DTC第一轮物理赛中，Team Chiron 凭借此系统获得 **11支队伍中第4名** 的成绩
- 尽管第三场遭遇硬件故障，前两场表现足以支撑高排名，验证了系统的鲁棒性

> ❗注意：文中提到“accuracy improved from 15% to 42%”和“from 19% to 46%”，应为不同场景下生理评估准确率的局部描述，与整体 triage performance 提升一致。

---

## 4. 关键结论和发现

### 主要发现
1. **专家知识引导的BN显著提升自动化 triage 系统性能**  
   在无训练数据条件下，通过结构化医学知识建模，实现了接近三倍的整体 triage 准确率提升。

2. **BN有效缓解感知模块的脆弱性**  
   单一视觉模型常因遮挡、光照变化而失败，BN可通过上下文推理填补空缺（例如：虽未直接看到出血，但结合截肢+心率升高→推断hemorrhage可能性高）。

3. **系统具备“优雅降级”能力（graceful degradation）**  
   当某些传感器失效时，BN不会崩溃，而是 marginalize over missing variables，提供当前最优估计。

4. **模块化架构支持灵活扩展**  
   不依赖底层感知模型的具体实现，便于集成新的AI算法或多模态输入。

### 方法的局限性
- **CPT参数依赖专家主观判断**：目前CPT中的概率值为启发式设定，缺乏大规模数据校准；
- **离散变量建模限制表达能力**：当前BN使用类别型变量，难以捕捉连续生理变化趋势；
- **未考虑时间动态演化**：模型为静态BN，尚未引入时序建模（如DBN）来预测病情进展；
- **外部有效性待验证**：仅在DTC受控环境中测试，真实灾难环境适应性尚需验证。

### 未来工作方向（Section 6）
1. **Robustness and sensor degradation**  
   引入传感器置信度权重，动态调整不同模态输入的影响（如烟雾中降低RGB权重，提高thermal权重）

2. **Human-AI collaboration and tactical optimization**  
   扩展为护理调度系统，预测干预紧迫性（health trajectory forecasting），辅助救援优先级排序

3. **Data Quality and Field Data Acquisition**  
   利用DTC现场收集的高质量标注数据优化CPT，并推动建立开源MCI数据集

4. **Ethical Considerations and Safeguards**  
   设计“human-in-the-loop”机制，确保AI仅为辅助工具，关键决策保留人工最终控制权

---

## ✅ 总结
本论文提出了一种基于 **expert-elicited Bayesian Network** 的多模态伤员评估框架，在无需训练数据的前提下，显著提升了自主 triage 系统的准确性、覆盖范围和鲁棒性。其核心价值在于将医学专业知识形式化为可计算的概率图模型，解决了当前AI triage系统中存在的“context blindness”与“brittleness”问题。实验表明，该方法使 triage performance 提升近 **3.8倍**，诊断覆盖率从31%提升至95%，并在DARPA挑战赛中取得优异排名。未来将在动态建模、人机协同与伦理保障方面进一步深化研究。

</details>

---

### 14. [GeoSense-AI: Fast Location Inference from Crisis Microblogs](https://arxiv.org/abs/2512.18225)

**Authors**: Deepit Sapru  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.18225v1  

#### Abstract
This paper presents an applied AI pipeline for realtime geolocation from noisy microblog streams, unifying statistical hashtag segmentation, part-of-speech-driven proper-noun detection, dependency parsing around disaster lexicons, lightweight named-entity recognition, and gazetteer-grounded disambig...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# GeoSense-AI: Fast Location Inference from Crisis Microblogs — 核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **地理元数据稀疏性**：社交媒体（如 Twitter）在危机事件中是重要的实时信息源，但仅有极少数推文（如印度地区仅 0.36%）带有精确的 geo-tags。
- **传统 NER 在微博客上的局限性**：通用或 Twitter 专用的 Named Entity Recognition (NER) 工具在处理非正式、拼写变异、多语言混杂的危机文本时表现不佳，且多数系统注重准确率而牺牲了处理速度，难以满足应急响应中的**低延迟需求**。

### 🚀 提出的新方法与创新思路
提出 **GeoSense-AI**，一个专为危机微博客设计的**高性能、低延迟地理位置推理 AI 流水线系统**，其核心创新包括：
- **多阶段融合的轻量级 NLP 流程**：结合以下技术实现高召回与高精度的平衡：
  - **Hashtag Segmentation**：使用统计方法拆分复合标签（如 `#ChennaiFloods` → "Chennai"）
  - **POS-driven Proper-Noun Detection**：基于词性标注识别地名候选
  - **Dependency Parsing around Disaster Lexicons**：利用依存句法分析提取与“flood”、“earthquake”等灾害关键词相关的地点
  - **Lightweight NER**：采用 spaCy 预训练模型作为补充，提升召回
  - **Gazetteer-Grounded Disambiguation**：通过 GeoNames 或 OpenStreetMap 进行地理验证与坐标解析
- **面向流式部署优化架构**：各模块均选择计算效率高的工具（如 spaCy），确保整个流水线可在生产环境中实时运行。

### ⚡ 相比现有方法的优势
| 维度 | GeoSense-AI | 传统 NER 工具（如 StanfordNER） |
|------|-------------|-------------------------------|
| **F1 分数** | 更高（0.8141） | 较低（0.6988） |
| **处理速度** | 极快（~1.19 秒处理 1k 推文） | 极慢（~175 秒） |
| **吞吐量优势** | 快约 **150 倍** | 不适合实时流处理 |
| **适用场景** | 可部署于真实危机响应系统 | 多用于离线分析 |

> ✅ **核心优势总结**：在保持甚至超越主流 NER 准确率的同时，实现了数量级级别的性能加速，适用于**实时危机感知系统**。

---

## 2. 核心实验方法和设置

### 📊 数据集
- **原始数据**：通过 Twitter Streaming API 收集 2017 年 9 月 12 日至 10 月 13 日期间含 “dengue” 或 “flood” 关键词的推文，共 **317,567 条**。
- **清洗后数据**：去除重复和非英文内容，保留 **239,276 条**推文。
- **人工标注子集**：从中随机抽取 **1,000 条推文**进行人工标注，最终确定 **99 条包含可验证印度境内具体位置（城市级及以上）的推文**作为评估集。

### 🧪 实验设置与评估指标
- **任务目标**：从推文文本中自动提取明确提及的地理位置，并映射到地理坐标。
- **评估指标**：
  - **Precision（精确率）**
  - **Recall（召回率）**
  - **F1 Score**
  - **Processing Time per Tweet（单条推文处理时间）**

- **Gazetteer 统一使用**：所有方法均使用 **GeoNames** 进行标准化匹配（除特别说明外），保证公平比较。

### 🔁 基线方法对比
| 方法 | 类型 |
|------|------|
| UniLoc | 单个词元 + gazetteer 匹配 |
| BiLoc | 单双词元 + gazetteer 匹配 |
| StanfordNER | 通用 NER 工具（CRF 模型） |
| TwitterNLP | 面向 Twitter 的 NER（Ritter et al.） |
| SpaCyNER | 轻量级预训练 NER |
| GoogleCloud | 商业云 API（Google Cloud NL） |
| GeoLoc / OSMLoc | 本文提出的两种变体（分别使用 GeoNames 和 OpenStreetMap） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table II）

| Method | Precision | Recall | **F1** | Time (s) |
|--------|-----------|--------|--------|----------|
| UniLoc | 0.3848 | 0.7852 | 0.5165 | 0.0553 |
| BiLoc | 0.4025 | 0.8590 | 0.5482 | 0.0624 |
| StanfordNER | 0.8103 | 0.6322 | 0.6988 | 175.0124 |
| TwitterNLP | 0.6356 | 0.5474 | 0.5882 | 28.0001 |
| SpaCyNER | 0.9883 | 0.5555 | 0.7113 | 1.0891 |
| GoogleCloud | 0.6321 | 0.5339 | 0.5789 | N/A |
| **GeoLoc** | **0.7987** | **0.8300** | **0.8141** | **1.1901** |
| OSMLoc | 0.3383 | 0.8888 | 0.4901 | 711.5817 |

> ✅ **GeoLoc 表现最佳**：F1 达到 **0.8141**，显著优于所有基线方法。

### 🔍 与基线方法对比结果
- **相比 n-gram 方法（UniLoc/BiLoc）**：
  - F1 提升约 50%，同时保持合理精度（避免低精度陷阱）。
- **相比 StanfordNER**：
  - F1 提升 **+16.5%**，处理速度快 **150 倍以上**。
- **相比 SpaCyNER**：
  - 虽然 Precision 略低（0.7987 vs 0.9883），但 Recall 显著更高（0.83 vs 0.55），F1 更优（0.8141 > 0.7113），表明其在**召回能力上大幅增强**。
- **OSMLoc 分析**：
  - Recall 最高（0.8888），但 Precision 极低（0.3383），且耗时超过 700 秒，不适合实时应用。

### 🔤 消融实验结果（GeoLocNoNER）
- **方法**：关闭最后的 Lightweight NER 模块，测试其余规则+语法方法的效果。
- **结果**：
  - F1 从 0.8141 下降至 0.7987
  - 处理时间略微下降（1.1901 → 1.1687 s）
- **结论**：Lightweight NER 贡献了约 **1.5% 的 F1 提升**，证明其作为“安全网”的有效性，且代价极小。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **多策略融合优于单一模型**：
   - 结合 hashtag segmentation、dependency parsing、syntactic pattern matching 和 gazetteer validation，能有效应对微博客文本噪声大、结构不规范的问题。
2. **轻量化 ≠ 低性能**：
   - 通过精心设计流水线，即使使用轻量级组件（如 spaCy），也能达到甚至超越重型 NER 模型的性能。
3. **速度与准确性可以兼得**：
   - GeoSense-AI 在 F1 上领先的同时，处理速度比 StanfordNER 快两个数量级，真正实现了**可用于生产环境的实时危机感知系统**。
4. **可视化接口实用性强**：
   - 部署在 [http://savitr.herokuapp.com](http://savitr.herokuapp.com) 的前端界面支持关键词搜索、时间筛选、地图热力展示等功能，在 2017 年喀拉拉邦登革热疫情中成功检测出 2,204 条相关推文，具备早期预警潜力。

### ⚠️ 方法的局限性
1. **依赖 Gazetteer 覆盖范围**：
   - 对未收录的本地化名称（如村庄别名）无法识别。
2. **对拼写变异敏感**：
   - 创意拼写若超出 fuzzy matching 阈值，则可能漏检。
3. **当前仅支持英语**：
   - 无法处理印地语、泰米尔语等本地语言或 code-mixed 文本（如 Hinglish）。
4. **歧义消解有限**：
   - 如 “Kota”（印度 vs 马来西亚）、“Springfield” 等重名地点缺乏上下文辅助判断。

### 🔮 未来工作方向
1. **Multilingual & Code-Mixed Processing**：
   - 引入语言识别模块，结合 transliteration 和多语言模型处理混合语言内容。
2. **Global Scale Deployment**：
   - 构建分层 Gazetteer 或使用 LSH（Locality-Sensitive Hashing）提高大规模数据库检索效率。
3. **增强危机分析功能**：
   - 增加：
     - Event Detection（基于时空聚类的异常检测）
     - Information Classification（分类为灾情报告、求助请求等）
     - Summarization & Duplicate Detection（去重聚合）
     - Credibility Assessment（可信度评分）
4. **集成更多上下文信号**：
   - 用户 Profile Location、社交网络关系、历史事件先验等用于 disambiguation。

---

## 总结
> **GeoSense-AI 成功展示了如何通过领域定制化的 NLP 流水线 + 知识库锚定，在危机微博客中实现高速、高准的 location inference，突破了传统 geo-tag 依赖和通用 NER 效率瓶颈，为 real-time crisis informatics 提供了一个可扩展、可部署的技术范例。**

</details>

---

### 15. [Faster Distributed Inference-Only Recommender Systems via Bounded Lag Synchronous Collectives](https://arxiv.org/abs/2512.19342)

**Authors**: Kiril Dichev, Filip Pawlowski, Albert-Jan Yzelman  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.19342v1  

#### Abstract
Recommender systems are enablers of personalized content delivery, and therefore revenue, for many large companies. In the last decade, deep learning recommender models (DLRMs) are the de-facto standard in this field. The main bottleneck in DLRM inference is the lookup of sparse features across huge...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Faster Distributed Inference-Only Recommender Systems via Bounded Lag Synchronous Collectives*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在分布式 **Deep Learning Recommendation Model (DLRM)** 推理系统中，**embedding table 的分布式查找**是主要瓶颈。该过程依赖于 **all-to-all (alltoallv)** 这类不规则通信操作，而传统实现采用**同步集体通信（synchronous collectives）**，导致所有进程必须严格同步，任何延迟都会造成整体性能下降。

尤其在存在**计算、内存或通信负载不均衡**（如访问模式不均、网络延迟波动）时，同步机制会显著拖慢推理吞吐和延迟。

### ✅ 提出的新方法：Bounded Lag Synchronous (BLS) Alltoallv
本文提出一种新型的 **Bounded Lag Synchronous (BLS) alltoallv** 集体通信原语，并基于此构建了一个新的 **PyTorch Distributed 后端** 和一个支持 BLS 的 DLRM 实现。

#### 核心思想：
- 允许不同进程在推理迭代上存在最多 `k` 个迭代的“滞后”（lag），即允许某些进程领先最多 `k` 步。
- 只要滞后不超过边界 `k`，领先进程可以继续执行后续迭代的计算（如 bottom MLP），无需等待通信完成。
- 当滞后达到 `k` 时，最慢的进程必须追上，否则领先者将被阻塞。

这打破了传统同步集体通信中“所有进程必须同时进入并退出同一轮通信”的限制。

### ✅ 相比现有方法的优势
| 方面 | 传统方法（MPI/NCCL） | 本文 BLS 方法 |
|------|------------------------|----------------|
| **同步性** | 完全同步，强阻塞 | 有界异步，允许 `k` 轮滞后 |
| **通信模型** | 多为双向通信（send/recv） | 基于 RDMA 的单边通信（put） |
| **缓冲机制** | 单缓冲 | 多缓冲（`k` 个 receive buffer） |
| **适用场景** | 所有标准同步任务 | 特别适合 **inference-only DLRM**（权重不变） |
| **延迟容忍** | 无法掩盖进程间延迟 | 可有效掩盖随机延迟 |

> 💡 **关键优势**：在存在不均衡的场景下，BLS 可以**完全掩盖**进程间的随机延迟，使系统表现接近理想平衡状态。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集

| 数据集 | 类型 | 描述 |
|-------|------|------|
| **Mini-Kaggle (Criteo)** | 真实数据集 | 广告点击预测数据，7天日志，13个 dense + 26个 sparse 特征，共26个 embedding tables |
| **Ali-CCP (Alibaba Click & Conversion)** | 真实数据集 | 阿里巴巴转化率预测数据，经预处理后生成23个 categorical tables |
| **Synthetic Heterogeneous Sizes** | 合成数据 | 随机生成每轮访问的 embedding 向量数量（1~100），制造消息大小不均 |
| **Synthetic Random Delays** | 合成数据 | 在每个推理步骤引入 `[0, 0.01]` 秒的均匀随机延迟，模拟系统抖动 |

> ⚠️ 注意：真实数据集因 NVTabular 预处理平均化了访问模式，导致通信负载高度均衡，不利于体现 BLS 优势。

### ⚙️ 实验设置

- **硬件平台**：8节点 ARM Kunpeng 920 集群，每节点 1 MPI 进程
- **网络**：Mellanox ConnectX-5 + EDR InfiniBand（100 Gbps），支持 RDMA
- **软件栈**：
  - PyTorch 2.5.1 + MPI 支持
  - 自研 **BLS backend**（基于 LPF + RDMA）
  - 修改版 DLRM（支持 BLS 请求队列）
- **参数配置**：
  - Batch size: 512
  - Embedding size: 64 bytes
  - Bound `k`: 1~400（可调参数）
  - 每个数据点取 5 次重复运行的均值（带 95% CI）

### 📈 评估指标

| 指标 | 定义 |
|------|------|
| **Latency per batch** | 每个 batch 的平均端到端推理时间（越低越好） |
| **Throughput (batches/sec)** | 单位时间内处理的 batch 数量（越高越好） |
| **Memory Overhead** | 因 BLS 引入的额外缓冲区开销（约 `k × 860 KB`/进程） |

### 🔁 基线方法对比
- **Baseline**: 原始 DLRM + PyTorch MPI backend（`k=0`，完全同步）
- **Variants**:
  - BLS-DLRM + MPI backend（非阻塞 collectives）
  - BLS-DLRM + BLS backend（完整 BLS 支持）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 场景 | 方法 | Latency 改进 | Throughput 提升 | 是否显著受益 |
|------|------|---------------|------------------|----------------|
| **合成：异构消息大小** | BLS-DLRM + BLS backend | ↓ ~7% | ↑ ~6% | ✅ 是（仅 BLS backend） |
| **合成：随机延迟** | BLS-DLRM + 任意 backend | ↓ 从 0.017 → 0.012 s | ↑ 显著 | ✅✅ 强烈受益 |
| **真实数据（Mini-Kaggle / Ali-CCP）** | 所有 BLS 设置 | ≈ 无变化 | ≈ 无变化 | ❌ 不受益 |

### 🔍 详细结果分析

#### （1）合成实验：异构消息大小
- **只有 BLS backend 能从中受益**，因为其真正实现了跨迭代的通信重叠（overlap collective across iterations）。
- MPI backend 虽支持非阻塞调用，但仍受限于通信线程瓶颈，增加 `k` 导致性能波动甚至下降。
- **原因**：BLS backend 使用 RDMA 单边通信，直接卸载到网卡，避免了 CPU 线程调度开销。

#### （2）合成实验：随机延迟（0~10ms）
- **无论使用 MPI 或 BLS backend，只要启用 BLS-DLRM，性能都大幅提升**。
- Latency 从 **0.017s → 0.012s**，正好等于平均延迟（0.005s），说明 **BLS 完全掩盖了随机延迟**。
- 提升随 `k` 增大快速收敛，`k > 50` 后收益饱和。
- 表明 BLS 对**瞬时抖动、GC、内存争抢等临时性延迟具有极强鲁棒性**。

#### （3）真实数据集实验
- Mini-Kaggle 和 Ali-CCP 均表现出高度均衡的通信与计算行为。
- 所有 BLS 配置（`k > 0`）**未带来任何性能提升**，甚至略有波动。
- **结论**：BLS 在**均衡负载下无优势**，但也不会劣化性能（`k=0` 时语义等价于同步 alltoallv）。

#### （4）消融实验（Ablation）
- **BLS backend vs MPI backend**：
  - 小消息（<4KB）：MPI 更优（协议优化更好）
  - 大消息（>4KB）：BLS 更优（避免 eager/rendezvous 切换开销）
  - 高频通信：MPI 更优（polling 开销高）
- **BLS-DLRM 修改必要性**：
  - FIFO 请求队列、移除冗余 barrier、缓冲管理等修改对实现跨迭代重叠至关重要。
  - 若不修改 DLRM，仅换 backend，无法发挥 BLS 潜力。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **BLS alltoallv 是一种有效的弱同步通信范式**，特别适用于 **inference-only DLRM** 这类**权重固定、迭代独立**的应用。
2. 在存在**不均衡访问模式或随机延迟**的场景下，BLS 可显著提升推理性能：
   - 最多可将延迟降低 **~30%**（合成延迟实验）
   - 完全掩盖平均达 **5ms 的随机延迟**
3. **BLS backend + BLS-DLRM 组合才能最大化收益**，尤其是需要跨迭代通信重叠时。
4. 在**均衡负载的真实数据集上，BLS 无明显优势**，说明其价值在于增强系统的**鲁棒性和容错能力**，而非绝对速度。

### ⚠️ 局限性

1. **仅适用于推理阶段**：训练中权重需同步更新，不能容忍滞后，因此 **BLS 不适用于 training**。
2. **内存开销随 `k` 线性增长**：虽然单进程仅数百 KB，但在大规模集群中仍需权衡。
3. **依赖 RDMA 硬件**：当前实现基于 InfiniBand，限制了部署环境。
4. **真实数据集缺乏长尾访问特征**：现有公开数据集经过预处理后趋于均衡，难以体现 BLS 在生产环境中的潜力。

### 🔮 未来工作方向

1. **扩展至其他通信原语**：如 allreduce、broadcast 的 BLS 版本。
2. **动态调整 bound `k`**：根据运行时负载自动调节滞后窗口。
3. **结合 caching/sharding 策略**：与 TorchRec、Merlin 等框架集成，形成完整优化方案。
4. **支持更多硬件平台**：如 RoCE、CXL 等 RDMA 兼容技术。
5. **探索 BLS 在其他图神经网络或稀疏模型中的应用**。

---

## 总结

> **BLS 是一种面向推理友好的“弱同步”通信机制，在面对现实世界不可避免的系统不均衡时，能够透明地掩盖延迟，提升分布式推荐系统的鲁棒性与效率。它不是最快的，但可能是最稳的。**

该工作为构建**高吞吐、低延迟、抗干扰**的工业级推荐系统提供了新的底层通信设计思路。

</details>

---

### 16. [The Best of Both Worlds: Hybridizing Neural Operators and Solvers for Stable Long-Horizon Inference](https://arxiv.org/abs/2512.19643)

**Authors**: Rajyasri Roy, Dibyajyoti Nayak, Somdatta Goswami  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.19643v1  

#### Abstract
Numerical simulation of time-dependent partial differential equations (PDEs) is central to scientific and engineering applications, but high-fidelity solvers are often prohibitively expensive for long-horizon or time-critical settings. Neural operator (NO) surrogates offer fast inference across para...

---

### 17. [LIR$^3$AG: A Lightweight Rerank Reasoning Strategy Framework for Retrieval-Augmented Generation](https://arxiv.org/abs/2512.18329)

**Authors**: Guo Chen, Junjie Huang, Huaijin Xie, Fei Sun, Tao Jia  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.18329v1  

#### Abstract
Retrieval-Augmented Generation (RAG) effectively enhances Large Language Models (LLMs) by incorporating retrieved external knowledge into the generation process. Reasoning models improve LLM performance in multi-hop QA tasks, which require integrating and reasoning over multiple pieces of evidence a...

---

### 18. [Fast Online Digital Twinning on FPGA for Mission Critical Applications](https://arxiv.org/abs/2512.17942)

**Authors**: Bin Xu, Ayan Banerjee, Sandeep K. S. Gupta  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.17942v1  

#### Abstract
Digital twinning enables real-time simulation and predictive modeling by maintaining a continuously updated virtual representation of a physical system. In mission-critical applications, such as mid-air collision avoidance, these models must operate online with extremely low latency to ensure safety...

---

### 19. [Population-Evolve: a Parallel Sampling and Evolutionary Method for LLM Math Reasoning](https://arxiv.org/abs/2512.19081)

**Authors**: Yanzhi Zhang, Yitong Duan, Zhaoxi Zhang, Jiyan He, Shuxin Zheng  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.19081v1  

#### Abstract
Test-time scaling has emerged as a promising direction for enhancing the reasoning capabilities of Large Language Models in last few years. In this work, we propose Population-Evolve, a training-free method inspired by Genetic Algorithms to optimize LLM reasoning. Our approach maintains a dynamic po...

---

### 20. [LeJOT: An Intelligent Job Cost Orchestration Solution for Databricks Platform](https://arxiv.org/abs/2512.18266)

**Authors**: Lizhi Ma, Yi-Xiang Hu, Yuke Wang, Yifang Zhao, Yihui Ren, Jian-Xiang Liao, Feng Wu, Xiang-Yang Li  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.18266v1  

#### Abstract
With the rapid advancements in big data technologies, the Databricks platform has become a cornerstone for enterprises and research institutions, offering high computational efficiency and a robust ecosystem. However, managing the escalating operational costs associated with job execution remains a ...

---

### 21. [The Geometry of Abstraction: Continual Learning via Recursive Quotienting](https://arxiv.org/abs/2512.18471)

**Authors**: Xin Li  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.18471v1  

#### Abstract
Continual learning systems operating in fixed-dimensional spaces face a fundamental geometric barrier: the flat manifold problem. When experience is represented as a linear trajectory in Euclidean space, the geodesic distance between temporal events grows linearly with time, forcing the required cov...

---

### 22. [Comparing Dynamical Models Through Diffeomorphic Vector Field Alignment](https://arxiv.org/abs/2512.18566)

**Authors**: Ruiqi Chen (Division of Biology and Biomedical Sciences, Washington University in St. Louis), Giacomo Vedovati (Department of Electrical and Systems Engineering, Washington University in St. Louis), Todd Braver (Department of Psychological and Brain Sciences, Washington University in St. Louis), ShiNung Ching (Department of Electrical and Systems Engineering, Washington University in St. Louis)  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.18566v1  

#### Abstract
Dynamical systems models such as recurrent neural networks (RNNs) are increasingly popular in theoretical neuroscience for hypothesis-generation and data analysis. Evaluating the dynamics in such models is key to understanding their learned generative mechanisms. However, such evaluation is impeded ...

---

### 23. [Small Language Models as Compiler Experts: Auto-Parallelization for Heterogeneous Systems](https://arxiv.org/abs/2512.19250)

**Authors**: Prathamesh Devadiga  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.19250v1  

#### Abstract
Traditional auto-parallelizing compilers, reliant on rigid heuristics, struggle with the complexity of modern heterogeneous systems. This paper presents a comprehensive evaluation of small (approximately 1B parameter) language-model-driven compiler auto-parallelization. We evaluate three models: gem...

---

### 24. [Solver-Independent Automated Problem Formulation via LLMs for High-Cost Simulation-Driven Design](https://arxiv.org/abs/2512.18682)

**Authors**: Yuchen Li, Handing Wang, Bing Xue, Mengjie Zhang, Yaochu Jin  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.18682v1  

#### Abstract
In the high-cost simulation-driven design domain, translating ambiguous design requirements into a mathematical optimization formulation is a bottleneck for optimizing product performance. This process is time-consuming and heavily reliant on expert knowledge. While large language models (LLMs) offe...

---

### 25. [GenEnv: Difficulty-Aligned Co-Evolution Between LLM Agents and Environment Simulators](https://arxiv.org/abs/2512.19682)

**Authors**: Jiacheng Guo, Ling Yang, Peter Chen, Qixin Xiao, Yinjie Wang, Xinzhe Juan, Jiahao Qiu, Ke Shen, Mengdi Wang  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.19682v1  

#### Abstract
Training capable Large Language Model (LLM) agents is critically bottlenecked by the high cost and static nature of real-world interaction data. We address this by introducing GenEnv, a framework that establishes a difficulty-aligned co-evolutionary game between an agent and a scalable, generative e...

---

### 26. [A Surrogate-Augmented Symbolic CFD-Driven Training Framework for Accelerating Multi-objective Physical Model Development](https://arxiv.org/abs/2512.19031)

**Authors**: Yuan Fang, Fabian Waschkowski, Maximilian Reissmann, Richard D. Sandberg, Takuo Oda, Koichi Tanimoto  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.19031v1  

#### Abstract
Computational Fluid Dynamics (CFD)-driven training combines machine learning (ML) with CFD solvers to develop physically consistent closure models with improved predictive accuracy. In the original framework, each ML-generated candidate model is embedded in a CFD solver and evaluated against referen...

---

### 27. [NL2CA: Auto-formalizing Cognitive Decision-Making from Natural Language Using an Unsupervised CriticNL2LTL Framework](https://arxiv.org/abs/2512.18189)

**Authors**: Zihao Deng, Yijia Li, Renrui Zhang, Peijun Ye  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.18189v1  

#### Abstract
Cognitive computing models offer a formal and interpretable way to characterize human's deliberation and decision-making, yet their development remains labor-intensive. In this paper, we propose NL2CA, a novel method for auto-formalizing cognitive decision-making rules from natural language descript...

---

### 28. [MSC-180: A Benchmark for Automated Formal Theorem Proving from Mathematical Subject Classification](https://arxiv.org/abs/2512.18256)

**Authors**: Sirui Li, Wangyue Lu, Xiaorui Shi, Ke Weng, Haozhe Sun, Minghe Yu, Tiancheng Zhang, Ge Yu, Hengyu Liu, Lun Du  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.18256v1  

#### Abstract
Automated Theorem Proving (ATP) represents a core research direction in artificial intelligence for achieving formal reasoning and verification, playing a significant role in advancing machine intelligence. However, current large language model (LLM)-based theorem provers suffer from limitations suc...

---

### 29. [EchoTrail-GUI: Building Actionable Memory for GUI Agents via Critic-Guided Self-Exploration](https://arxiv.org/abs/2512.19396)

**Authors**: Runze Li, Yuwen Zhai, Bo Xu, LiWu Xu, Nian Shi, Wei Zhang, Ran Lin, Liang Wang  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19396v1  

#### Abstract
Contemporary GUI agents, while increasingly capable due to advances in Large Vision-Language Models (VLMs), often operate with a critical limitation: they treat each task in isolation, lacking a mechanism to systematically learn from past successes. This digital ''amnesia'' results in sub-optimal pe...

---

### 30. [Scalably Enhancing the Clinical Validity of a Task Benchmark with Physician Oversight](https://arxiv.org/abs/2512.19691)

**Authors**: Junze Ye, Daniel Tawfik, Alex J. Goodell, Nikhil V. Kotha, Mark K. Buyyounouski, Mohsen Bayati  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19691v1  

#### Abstract
Automating the calculation of clinical risk scores offers a significant opportunity to reduce physician administrative burden and enhance patient care. The current standard for evaluating this capability is MedCalc-Bench, a large-scale dataset constructed using LLM-based feature extraction and rule-...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
