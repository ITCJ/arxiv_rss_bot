# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-14 05:58:12 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Parallel Context-of-Experts Decoding for Retrieval Augmented Generation](https://arxiv.org/abs/2601.08670)

**Authors**: Giulio Corallo, Paolo Papotti  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.08670v1  

#### Abstract
Retrieval Augmented Generation faces a trade-off: concatenating documents in a long prompt enables multi-document reasoning but creates prefill bottlenecks, while encoding document KV caches separately offers speed but breaks cross-document interaction. We propose Parallel Context-of-Experts Decodin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Parallel Context-of-Experts Decoding for Retrieval Augmented Generation**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Retrieval Augmented Generation (RAG) 面临两个核心挑战：
- **Prefill 瓶颈**：将多个检索到的文档拼接成一个长上下文进行推理时，prefill 阶段计算开销巨大，导致延迟高。
- **跨文档推理能力下降**：若采用并行 KV cache 编码（如独立编码每个文档），虽可加速，但破坏了文档间的注意力交互，削弱了多跳推理能力。

现有方法在效率与准确性之间存在明显权衡。

---

### **提出了什么新方法或新思路**
作者提出 **Parallel Context-of-Experts Decoding (PCED)**，一种无需训练的解码框架，其核心思想是：
- 将每个检索到的文档视为一个独立的“专家”（Expert），并维护各自的 KV cache；
- 在生成过程中，并行运行这些“专家”，并在每一步通过 **retrieval-aware contrastive decoding** 规则聚合它们的输出；
- 将证据融合从 **attention 层面** 转移到 **decoding 层面**，从而避免构建共享上下文，同时恢复跨文档推理能力。

#### **三大创新点**：
1. **并行、模块化的 KV cache 框架 + 解码时证据聚合**  
   支持高效复用预计算的 KV cache，显著降低 Time-To-First-Token (TTFT)。

2. **token-level expert switching**  
   每个生成步动态选择最可信的专家输出，实现跨文档推理（如图2所示模型在不同文档间跳跃）。

3. **retrieval-integrated prior 注入 contrastive decoding**  
   利用检索系统提供的 relevance score（来自 dense/sparse/ColBERT 或 reranker）作为先验，控制各专家对最终 logits 的影响权重，抑制噪声专家干扰。

---

### **相比现有方法的优势**
| 方法 | 效率 | 跨文档推理 | 是否需训练 | 备注 |
|------|------|-------------|------------|------|
| Concatenation (All) | 低 | 强 | 否 | Prefill 成本高 |
| KV Merge (e.g., APE) | 中 | 较弱 | 否 | 试图恢复 attention，效果有限 |
| Agentic (e.g., MapReduce) | 低 | 强 | 否 | 多轮调用 LLM，延迟高 |
| **PCED (本文)** | **极高** | **强** | **否** | 单次解码完成，兼具速度与精度 |

- **无需训练**，适用于任意开放权重 LLM；
- **支持大规模候选文档输入**，不受 context window 限制；
- **更鲁棒于无关干扰文档（distractors）**。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **LOFT benchmark**：涵盖 RAG 和 In-Context Learning (ICL) 任务，固定 top-90 文档池。
- **LongBench**：多语言、多任务长上下文理解基准，测试单文档与多文档 QA、摘要、代码补全等。
- **合成数据集**：用于性能基准测试（TTFT 和端到端延迟），包含 64 个文档（1 个含“secret code”）。

---

### **实验设置和评估指标**
- **LLM 模型**：
  - 主要结果：`MISTRAL-NEMO-13B-INSTRUCT`, `LLAMA-3.1-8B-INSTRUCT`
  - LongBench：`QWEN3-8B`（扩展至 128k 上下文）
- **解码方式**：greedy decoding
- **评估指标**：
  - RAG 任务：Subspan Exact Match
  - ICL 任务：Exact Match
  - LongBench：官方指标

---

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **标准拼接** | Corpus in Ctx (Single), Corpus in Ctx (All) |
| **KV cache 合并** | APE（Adaptive Parallel Encoding） |
| **Agent-style 聚合** | MapReduce（map 阶段摘要 → reduce 阶段聚合） |
| **本文方法** | PCED（Sparse / Dense / ColBERT 变体） |

所有方法使用相同的检索结果、prompt 模板和随机种子，仅改变上下文整合机制。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 1 & 2）**

#### **在 LOFT 上的表现（以 LLAMA-3.1-8B 为例）**
| Task | APE | MapReduce | PCED-Dense | Corpus in Ctx (All) |
|------|-----|-----------|------------|---------------------|
| HOTPOTQA | 16.0 | 41.0 | **64.0** | 49.0 |
| MUSIQUE | 4.0 | 8.0 | **21.0** | 7.0 |
| RAG NQ | 9.0 | 50.0 | **85.0** | 58.0 |
| QAMPARI | 7.0 | 68.0 | **77.0** | 72.0 |
| QUEST | 0.0 | 41.0 | **45.0** | 39.0 |

> ✅ PCED 在多跳问答上平均提升 **+20~50 pts**，远超其他并行方法。

#### **在 LongBench 上的表现（QWEN3-8B）**
| Task | Corpus in Ctx (All) | PCED-Dense |
|------|---------------------|------------|
| HOTPOTQA | 56.3 | **62.6** (+6.3) |
| 2WIKI | 44.2 | **49.4** (+5.2) |
| MUSIQUE | 25.3 | **33.4** (+8.1) |
| TRIVIAQA | 84.0 | **88.8** (+4.8) |
| REPOB-P (Code) | 51.1 | **60.1** (+9.0) |

> ✅ PCED 在多数任务上 **超越 full-context 拼接方法**，尤其在多文档 QA 和代码任务中表现突出。

---

### **与基线方法的对比结果**
- **vs. KV Merge (APE)**：PCED 平均高出 **40–70 pts**，证明其有效恢复了跨文档推理能力。
- **vs. MapReduce**：PCED 在大多数任务上更优（如 HOTPOTQA +23 pts），且只需一次解码，而 MapReduce 需多次 LLM 推理。
- **vs. Full Context (All)**：PCED 在 11/16 设置下达到或超过其性能，尽管每个文档独立编码。
- **效率方面**：
  - **TTFT 加速达 180×**（0.14s vs 25.50s）
  - **端到端延迟降低 ~1.7×**（65k context）

---

### **消融实验结果（Ablation Studies）**

#### **组件分析（Table 3 & 6）**
| 设置 | HOTPOTQA (Llama) | NQ (Llama) |
|------|------------------|----------|
| Only Contrastive (γ=0) | 46 | 52 |
| Only Retrieval Prior (β=0) | 53 | 70 |
| **Full PCED** | **64** | **85** |

> 🔍 结论：**retrieval prior 是基础**（防止噪声干扰），**contrastive decoding 是放大器**（抑制幻觉）。

#### **参数敏感性**
- **β（contrastive strength）**：动态策略（AdaCAD）优于固定值，更具稳定性。
- **γ（retrieval gating）**：γ=2.5 为最优平衡点；过小（<1.5）无法抑制噪声，过大（≥4.0）过度依赖检索排序。

#### **聚合规则比较（Table 7）**
| 聚合方式 | HOTPOTQA | NQ |
|--------|---------|----|
| Max（本文） | **64** | 85 |
| Mixture (MoE) | 56 | **87** |
| Product (PoE) | 46 | 85 |

> 📌 **Max 更适合多跳推理**（sharp switching），**MoE 更适合单文档任务**（soft fusion）。

#### **抗噪能力（Figure 6）**
- 当 top-k 从 8 增加到 128（16倍干扰项），PCED 性能几乎不变（如 NQ 维持 ~85）。
- 表明 retrieval prior 有效抑制了 distractors 影响。

---

## **4. 关键结论和发现**

### **主要发现**
1. **跨文档推理可在 decoding 阶段重建**  
   不需要联合 attention 或 KV cache 合并，通过 token-level expert switching 即可实现证据拼接。

2. **retrieval scores 是宝贵的解码先验信号**  
   利用检索系统的 relevance score 显著提升准确率，尤其是在大量 distractors 存在时。

3. **PCED 实现了效率与性能的双重突破**  
   - 速度：TTFT 加速 **180×**
   - 精度：在多跳 QA 上超越现有并行方法 **高达 70 pts**
   - 鲁棒性：对无关文档不敏感

4. **KV cache 模块化 + 解码时融合 是可行路径**  
   为未来设计“可扩展 context”系统提供了新范式。

---

### **方法的局限性**
1. **依赖模型 logits 输出**  
   无法应用于仅提供 sampled token 或 log-probabilities 的 API-only 模型（如 GPT-4）。

2. **对检索质量敏感**  
   若相关文档未被检索到或评分过低，则对应 expert 会被忽略，无法恢复缺失证据。

3. **存储成本较高**  
   需持久化保存 FP16 格式的 KV cache，例如 LOFT 数据集需约 **11GB 存储**，适合静态知识库场景。

---

### **未来工作方向**
- **端到端学习 expert selection**：让 LLM 自主决定何时信任哪个 context，减少对外部检索系统的依赖。
- **轻量化 KV cache 存储**：探索量化、压缩或增量更新机制以降低存储开销。
- **适配闭源模型**：设计近似 contrastive decoding 的采样策略，使其能在 black-box API 上近似实现 PCED 效果。

--- 

> 💡 **总结一句话**：  
> **PCED 成功将 RAG 的“跨文档推理”从昂贵的 attention 计算转移到高效的 decoding 选择机制，在保持训练自由的同时，实现了速度与精度的双重飞跃。**

</details>

---

### 2. [DiffMM: Efficient Method for Accurate Noisy and Sparse Trajectory Map Matching via One Step Diffusion](https://arxiv.org/abs/2601.08482)

**Authors**: Chenxu Han, Sean Bin Yang, Jilin Hu  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.08482v1  

#### Abstract
Map matching for sparse trajectories is a fundamental problem for many trajectory-based applications, e.g., traffic scheduling and traffic flow analysis. Existing methods for map matching are generally based on Hidden Markov Model (HMM) or encoder-decoder framework. However, these methods continue t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*DiffMM: Efficient Method for Accurate Noisy and Sparse Trajectory Map Matching via One Step Diffusion*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对**稀疏且含噪声的GPS轨迹数据在复杂路网中进行高精度、高效率地图匹配（Map Matching）** 的挑战，提出了一种新的解决方案。现有方法（如 HMM 和基于 RNN 的 Seq2Seq 模型）存在以下问题：
- **对噪声敏感**：GPS漂移导致空间邻近性失效；
- **在稀疏轨迹上性能下降严重**：低采样率破坏了时空连续性；
- **推理效率低**：自回归解码（autoregressive decoding）耗时长，难以实时应用。

### 🚀 提出的新方法与创新思路
作者提出了 **DiffMM** —— 一种基于 **encoder-diffusion 架构** 的新型地图匹配框架，其核心创新包括：

1. **首次将扩散模型引入地图匹配任务**  
   将 map matching 建模为一个从噪声分布恢复目标道路段分布的生成过程，并采用 **conditional diffusion modeling** 范式。

2. **设计了“道路段感知”的轨迹编码器（road segment-aware trajectory encoder）**  
   利用注意力机制联合嵌入轨迹点及其候选道路段，构建共享潜在表示（joint embedding），有效融合空间上下文与语义信息。

3. **提出基于 shortcut model 的 one-step diffusion 方法**  
   不同于传统多步扩散模型，DiffMM 使用 **one-step denoising** 实现高效推理，在仅一步内完成匹配预测，显著提升速度。

4. **端到端可训练架构**  
   整合 Trajectory Encoder 与 DiT（Diffusion transformer）Backbone，实现统一优化。

### 🔍 相比现有方法的优势
| 维度 | DiffMM 优势 |
|------|-------------|
| **准确性** | 在高度稀疏轨迹下仍保持高精度，优于 HMM、DeepMM 等 SOTA 方法 |
| **效率** | 推理速度快达 **17倍以上**（vs HMM），训练时间接近最优 |
| **鲁棒性** | 对噪声和稀疏性具有更强容忍能力 |
| **泛化性** | 在不同城市规模（Porto vs Beijing）、复杂路网拓扑中均表现优异 |

---

## 2. 核心实验方法和设置

### 📊 数据集
使用两个大规模真实出租车轨迹数据集：
- **Porto (PT)**：葡萄牙波尔图市，较小城区，11.7×5.2 km²，约101万条轨迹
- **Beijing (BJ)**：北京市中心区域，较大城区，29.6×30.0 km²，约117万条轨迹  
> 路网来自 OpenStreetMap

为模拟稀疏场景，对原始轨迹进行随机降采样，定义采样比例 $ r \in (0,1) $，平均时间间隔如下：

| 数据集 | r=0.5 | r=0.3 | r=0.2 | r=0.1 | r=0.05 | r=0.025 |
|--------|-------|-------|-------|-------|--------|---------|
| Porto | - | - | 300s | 600s | 300s | 600s |
| Beijing | 120s | 200s | 300s | 600s | - | - |

数据划分：40%训练 / 30%验证 / 30%测试

### 🧪 实验设置
- **搜索半径**：50米内候选路段作为输入
- **模型参数**：
  - Embedding dim: 128 → Condition dim: 256
  - Transformer Encoder: 2层，4头注意力
  - DiT Blocks: 2层，hidden dim=512
  - 训练 step size $ d \in \{1, 1/2\} $，推理使用 **M=1 步**（one-step）
- **硬件平台**：单张 NVIDIA RTX 3090 GPU

### 📏 评估指标
- **Accuracy**：预测路段序列与真实匹配路段完全一致的比例
  $$
  \text{Accuracy}(R, \hat{R}) = \frac{1}{l} \sum_{i=1}^{l} \mathbb{I}[r_i = \hat{r}_i]
  $$

### ⚔️ 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **HMM** | 非学习型 | 经典隐马尔可夫模型，依赖空间距离与转移概率 |
| **DeepMM** | 学习型 | 基于 RNN 的端到端深度学习方法 |
| **GraphMM** | 学习型 | 图神经网络方法，建模轨迹间相关性 |
| **RNTrajRec** | 学习型 | 结合路网结构的轨迹恢复模型（用于 map matching 场景） |

---

## 3. 主要实验结果和性能指标

### 📈 准确性结果（Table 2）
在所有稀疏条件下，**DiffMM 显著优于所有基线方法**，尤其在极端稀疏情况下优势更明显。

#### ➤ 北京数据集（r=0.1，即平均每600秒一个GPS点）：
- **DiffMM**: **85.39%**
- 第二名 DeepMM: 68.25%
- **领先幅度高达 +17.14个百分点**

#### ➤ 波特数据集（r=0.025，每600秒一点）：
- **DiffMM**: **86.87%**
- HMM 下降至 40.04%，而 DiffMM 仍维持高位

> 💡 观察：随着轨迹变稀疏，所有方法性能下降，但 **DiffMM 下降最缓慢**，说明其对稀疏性具有极强鲁棒性。

---

### ⏱ 效率对比（Table 3）
| 方法 | 推理时间（每千条轨迹） | 训练时间（每epoch） |
|------|------------------------|--------------------|
| HMM | 20.57 秒 | – |
| DeepMM | 88.82 秒 | 9.07 分钟 |
| RNTrajRec | 627.65 秒 | 868.23 分钟 |
| **DiffMM (Ours)** | **1.18 秒** | **10.66 分钟** |

✅ **推理速度比第二快的 HMM 快约 17 倍**，比 DeepMM 快超过 75 倍！  
✅ 训练效率也极具竞争力，远超 RNTrajRec。

---

### 🔍 消融实验（Ablation Study, Table 4）
在 Beijing 数据集上验证关键模块作用：

| 变体 | r=0.5 | r=0.3 | r=0.2 | r=0.1 |
|------|-------|-------|-------|-------|
| w/o Trans（无Transformer） | 90.06 | 88.33 | 87.12 | 84.89 |
| w/o Attn（无注意力融合） | 88.79 | 87.25 | 85.70 | 82.71 |
| w/o Shortcut（传统diffusion） | 89.67 | 87.92 | 86.84 | 83.53 |
| **DiffMM（完整）** | **90.32** | **88.45** | **87.65** | **85.39** |

结论：
- **Transformer 编码器**有助于捕捉稀疏轨迹中的长期依赖；
- **注意力机制**能有效抑制噪声干扰；
- **Shortcut model** 显著优于传统 diffusion，是实现高效 one-step 推理的关键。

---

### 🛡 鲁棒性分析（Table 5）
在 Porto 数据集（r=0.1）上减少训练样本数量，测试泛化能力：

| 训练样本数 | 16k | 32k | 64k | 128k |
|------------|-----|-----|-----|------|
| Accuracy | 86.01% | 87.91% | 89.23% | 90.03% |

📌 即使只用 **1.6万条轨迹训练**，准确率仍达 **86.01%**，**超过其他方法在全量数据下的表现**，表明 DiffMM 具有很强的数据利用效率和泛化能力。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **DiffMM 是首个将 diffusion model 成功应用于 map matching 的工作**，开创了新范式。
2. 通过 **joint embedding + one-step diffusion**，实现了**精度与效率的双重突破**。
3. 在**高度稀疏和噪声环境下表现卓越**，特别适用于实际低频采样场景（如共享单车、物流车辆等）。
4. 模型具备良好的**可扩展性和鲁棒性**，在不同城市规模和数据量下均稳定领先。

### ⚠ 局限性
- 当前模型假设候选路段可通过固定半径（如50米）检索，若存在严重GPS漂移超出范围可能漏检；
- 虽然 one-step 推理极快，但训练仍需一定计算资源（尽管已优于多数基线）；
- 未显式建模转向限制、单行道等高级路网约束。

### 🔮 未来工作方向
- 扩展至 **trajectory recovery** 任务，通过 conditioned diffusion 生成密集路径；
- 引入更多**动态交通信息**（如拥堵状态）作为条件输入；
- 探索 **multi-modal diffusion** 或结合 LLM 进行语义增强的地图匹配；
- 支持在线增量学习以适应动态变化的城市路网。

---

> 🔗 **代码开源地址**：[https://github.com/decisionintelligence/DiffMM](https://github.com/decisionintelligence/DiffMM)

</details>

---

### 3. [ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms](https://arxiv.org/abs/2601.08166)

**Authors**: Mohammad Pivezhandi, Mahdi Banisharif, Abusayeed Saifullah, Ali Jannesari  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08166v1  

#### Abstract
Dynamic voltage and frequency scaling (DVFS) and task-to-core allocation are critical for thermal management and balancing energy and performance in embedded systems. Existing approaches either rely on utilization-based heuristics that overlook stall times, or require extensive offline profiling for...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统嵌入式系统中的 **Dynamic Voltage and Frequency Scaling (DVFS)** 和任务到核心分配（task-to-core allocation）策略存在以下问题：
- **基于利用率的启发式方法**（如Linux ondemand governor）忽略 stall time，导致频率调节不准确。
- **基于表格的离线分析方法**（如precise scheduler）需要对所有频率-核心组合进行详尽的 profiling，耗时长达8–12小时，无法适应动态变化的工作负载。
- 缺乏对新工作负载的泛化能力，难以实现 **zero-shot 部署**。

这些问题限制了在资源受限、动态性强的嵌入式平台上的实时自适应调度。

---

### 提出的新方法与创新思路
本文提出 **ZeroDVFS** —— 一种基于模型的分层多智能体强化学习（MARL）框架，结合大语言模型（LLM）语义特征提取，实现热感知与能效优化的零样本调度。

#### 核心创新点：
1. **LLM-based Semantic Feature Extraction**
   - 利用 LLM（如 GPT-4o、Claude Sonnet、DeepSeek-V3）从 OpenMP 源码中提取 **13 个代码级语义特征**（如算法复杂度、内存访问模式、并行开销等），无需执行程序。
   - 实现 **zero-shot 特征提取**，单次成本仅 \$0.018/程序，耗时 <5 秒。
   - 支持跨平台迁移，避免重复 profiling。

2. **Hierarchical Multi-Agent Reinforcement Learning (MARL)**
   - 设计两个协作智能体：
     - **Profiler Agent**：决定核心数量和运行频率，以最小化能耗和 makespan。
     - **Temperature Agent**：基于温度状态优先选择较冷的核心，防止局部过热。
   - 分解指数级动作空间，降低决策复杂度。

3. **Model-Based RL with Dyna-Q 架构**
   - 引入环境模型（regression-based）预测温度和性能状态，生成合成训练数据。
   - 结合真实数据与模拟数据进行规划（planning），实现 **20× 更快收敛速度**。
   - 支持 **few-shot 跨平台迁移**，无需目标平台完整 profiling。

4. **Zero-Shot Deployment 能力**
   - 将 LLM 提取的语义特征与环境模型结合，可在未见过的新工作负载上直接部署 RL 调度器，无需任何目标硬件上的 profiling 数据。

---

### 相比现有方法的优势
| 维度 | 传统方法（如precise scheduler） | ZeroDVFS |
|------|-------------------------------|--------|
| 决策延迟（首次） | 8–12 小时（profiling） | **3.5–8.0 秒**（含 LLM 提取） |
| 后续决策延迟 | ~1ms（查表） | **358ms**（推理） |
| 首次决策速度提升 | — | **8,300× 更快** |
| 能效 | 基准 | **7.09× 更好**（vs Linux ondemand） |
| Makespan | 较长 | **4.0× 更优** |
| 泛化能力 | 无（需重新 profiling） | 支持 zero-shot 新 workload 和 cross-platform |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **BOTS (Barcelona OpenMP Tasks Suite)**：包含 FFT、Strassen、N-Queens 等任务并行程序。
- **PolybenchC**：涵盖线性代数、Stencil 计算、数据挖掘等循环并行内核。
- 共 **42 个 OpenMP 基准程序**，覆盖多样化的计算模式和并行结构。

---

### 实验平台
| 平台 | 架构 | 核心数 | 热区数 |
|------|------|-------|-------|
| **Jetson TX2** | ARM Cortex-A57 + Denver 2 | 6 | 8 |
| **Jetson Orin NX** | ARM Cortex-A78AE | 8 | 9 |
| **RubikPi** | Qualcomm Kryo 585 | 8 | 36 |
| **Intel Core i7 (8th Gen)** | x86_64 | 4 | — |

所有平台均运行 Linux，使用 `cpufrequtils` 控制频率，禁用 p-state/c-state 自动管理。

---

### 评估指标
- **Energy Efficiency**：总能耗（mJ）
- **Makespan**：任务完成时间（秒）
- **Temperature**：平均核心温度（°C）
- **Decision Latency**：首次决策与后续决策延迟
- **Convergence Speed**：RL 收敛所需 episode 数
- **Cross-Platform Transfer Accuracy**：MAPE、R²

---

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **Linux ondemand governor** | 启发式 | 利用率驱动，阈值触发频率调整 |
| **Precise Scheduler [7]** | 表格法 | 离线 profiling 所有配置，生成 lookup table |
| **zTT [30]** | 单智能体 RL | 基于模型无关的 RL，关注热节流 |
| **GearDVFS** | 启发式 | 动态频率调节，但无核心分配 |
| **HiDVFS_S, MAML, SAMBRL** 等 | 多种 RL 变体 | 用于消融实验和性能对比 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | ZeroDVFS | Precise Scheduler | zTT | Linux ondemand |
|------|---------|------------------|-----|---------------|
| **Energy (mJ)** | **9.1** | 75.5 | 31.2 | ~64.5 |
| **Makespan (s)** | **1.13** | 5.96 | 1.88 | ~4.52 |
| **Avg Temp (°C)** | 42.1 | 44.0 | 43.6 | ~45.0 |
| **首次决策延迟** | 3.5–8.0s | 8–12h | ~10ms | ~1ms |
| **后续决策延迟** | **358ms** | ~1ms | ~10ms | ~1ms |
| **收敛速度** | 20 episodes | 不适用（静态） | ~50 episodes | ~50 episodes |

> 注：ZeroDVFS 在 **能量效率上提升 7.09×**，**makespan 缩短 4.0×**，相比 Linux ondemand。

---

### 与基线方法的对比结果
- **Energy Efficiency**：
  - ZeroDVFS 比 Precise Scheduler 节省 **85.9% 能量**。
  - 比 zTT 节省 **70.8% 能量**。
- **Makespan**：
  - 比 Precise Scheduler 快 **5.3×**。
  - 比 GearDVFS 快 **8.7×**（见图6）。
- **决策延迟**：
  - 首次决策比表格法快 **8,300×**。
  - 后续决策比表格法快 **80,000×**（因无需重生成表）。
- **收敛速度**：
  - 模型驱动方法（Dyna-Q）比纯 model-free RL 快 **20× 收敛**。

---

### 消融实验结果
#### （1）模型驱动 vs. 模型无关 RL
- **MAMBRL D3QN（模型驱动）**：收敛更快（20 episodes），稳定性高。
- **MAMFRL D3QN（模型无关）**：最终性能接近，但方差更大，收敛慢（~40 episodes）。
- 结论：**环境建模显著提升样本效率**。

#### （2）LLM 特征有效性分析
- **特征重要性排序**（XGBoost）：
  1. `energy_main_j`（28.5%）
  2. `context_switches`（22.3%）
  3. `energy_system_j`（16.6%）
  4. `instructions`（7.2%）
  5. `cache_references`（2.7%）
  - LLM 特征中，`cache_behavior-pattern`（1.03%）、`data_dependency-type`（0.57%）进入前15。
- **zero-shot 泛化能力**：
  - 使用 LLM 特征后，可对从未训练过的程序进行预测。
  - 若仅用语法特征（Tree-sitter），跨程序泛化能力几乎为零。

#### （3）跨平台迁移效果（Table 5）
| 迁移路径 | MAPE | R² |
|--------|------|----|
| TX2 → Orin NX | 64.5% | 0.90 |
| TX2 → RubikPi | 73.2% | 0.80 |

- 尽管 MAPE 较高，但 **R² > 0.8** 表明相对排序保持良好，足以支持有效调度。
- 加入 **10 个 fine-tuning 样本** 后，MAPE 下降约 5%，验证 few-shot 微调的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM 可有效提取语义特征**，替代传统 profiling，实现 **zero-shot 性能预测**。
2. **模型驱动的 MARL 框架** 显著加速 RL 收敛（20×），并支持跨平台迁移。
3. **ZeroDVFS 在能效和性能上全面超越现有方法**：
   - 能量效率提升 **7.09×**
   - Makespan 缩短 **4.0×**
   - 决策延迟比表格法快 **8,300×**
4. **首次决策延迟可控**（<8s），适合实际部署；后续决策稳定在 **358ms**。
5. **跨平台迁移可行**，即使存在架构差异（ARM vs. x86），仍能保持较高 R²（0.8–0.9）。

---

### 方法的局限性
1. **当前仅支持单 DAG OpenMP 工作负载**，不适用于并发或多进程场景。
2. **LLM 特征提取限于单文件程序**，多文件项目需手动拼接或分层分析。
3. **zero-shot 跨平台 MAPE 较高**（64–73%），仍需少量 fine-tuning 提升精度。
4. **依赖商业 LLM API**（如 GPT-4o），可能带来成本和网络依赖问题。
5. **缺乏置信区间量化**，实验重复次数有限，统计显著性有待加强。

---

### 未来工作方向
1. 扩展至 **并发 workload 和多任务调度** 场景。
2. 支持 **multi-file 项目自动分析**，引入模块化 LLM 推理。
3. 探索 **fine-tuned LLM on HPC corpus**，提升代码理解准确性。
4. 研究 **GPU offloading 决策**，扩展至异构加速器调度。
5. 实现 **本地化 LLM 部署**（如 DeepSeek-Coder、CodeLlama），支持完全离线边缘设备运行。
6. 开展更严格的 **多随机种子统计分析**，增强结果可信度。
7. 探索 **confidence-aware prediction**，为每个 LLM 特征提供不确定性估计。

---

> ✅ **一句话总结**：  
> ZeroDVFS 通过 **LLM 语义特征 + 模型驱动 MARL**，实现了嵌入式平台上 **无需 profiling 的零样本核心与频率调度**，兼具高性能、高能效与快速部署能力，为动态嵌入式系统提供了全新的智能化调度范式。

</details>

---

### 4. [Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding](https://arxiv.org/abs/2601.08653)

**Authors**: Zenghua Liao, Jinzhi Liao, Xiang Zhao  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08653v1  

#### Abstract
Large Language Models are rapidly emerging as web-native interfaces to social platforms. On the social web, users frequently have ambiguous and dynamic goals, making complex intent understanding-rather than single-turn execution-the cornerstone of effective human-LLM collaboration. Existing approach...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前 Large Language Models（LLMs）在处理用户意图时，通常假设澄清问题是**并行且独立**的（如 Mistral-Interact 和 ITIU 所采用的方法），这在简单任务中有效，但在**复杂意图场景**下存在严重缺陷。例如，在规划旅行时，“活动建议”依赖于“目的地”的选择，若未先确认目的地就推荐“十二月冲绳潜水”，则会产生逻辑冲突。

这种忽略**澄清问题间逻辑依赖关系**的设计会增加用户的**认知负荷（Cognitive Load）**，迫使用户自行管理复杂的决策流程，从而降低交互效率和满意度。

### **提出的新方法与思路**
作者提出 **Prism**，一个基于**认知负荷理论（Cognitive Load Theory, CLT）** 的新型框架，旨在通过**逻辑连贯的意图澄清**来降低用户认知负担。其核心思想是将复杂意图视为“白光”，通过“棱镜”（即逻辑驱动的结构化模式）将其分解为有序的“光谱”元素，从而引导分层、有序的澄清过程。

#### **Prism 的四大模块设计**：
1. **Complex Intent Decomposition Module**  
   构建 **CID 数据集**（含 27 个领域、429 个意图），显式标注意图元素间的**前置依赖关系**，支持对模糊意图进行结构化解构。

2. **Logical Clarification Generation Module**  
   基于元素层级组织澄清顺序：**独立问题并行呈现**（如表格形式），**依赖问题按序提问**，确保逻辑一致性。

3. **Intent-Aware Reward Module**  
   设计**意图感知奖励函数**（Intent-Aware Reward），结合 token-level 的**意图重要性**（Intent Importance）与**生成置信度**（Generation Confidence），并通过 **Monte Carlo Sampling** 模拟大规模人机交互以生成高质量训练数据。

4. **Self-Evolved Intent Tuning Module**  
   采用**自演进意图调优**策略，利用高奖励轨迹迭代优化 LLM，无需人工标注即可持续提升模型能力。

### **相比现有方法的优势**
- **首次系统建模澄清问题之间的逻辑依赖**，突破了传统方法对“独立问题”的隐含假设。
- 引入 **CLT 理论指导交互设计**，从认知科学角度优化用户体验。
- 实现**逻辑一致性与交互效率的平衡**：既减少冗余轮次，又避免因跳步导致的不合理建议。
- 支持**可扩展的自我进化训练机制**，显著降低对人工标注数据的依赖。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验在三个主流意图理解数据集上进行：
- **TIN** [14]：200 条测试样本，涵盖多领域复杂任务。
- **IN3** [21]：108 条测试样本，强调多轮澄清。
- **ABP** [36]：319 条测试样本，覆盖广泛真实世界规划任务。

所有测试集均被手动划分为 **简单意图** 与 **复杂意图** 场景，用于评估模型在不同难度下的表现。

### **实验设置**
- **主干模型**：`Mistral-7B-Instruct-v0.3` 和 `LLaMA-3.1-8B-Instruct`
- **Prism 变体**：`Prism-SFT`（监督微调）、`Prism-DPO`（直接偏好优化）
- **训练方式**：全参数微调 + DPO，使用 8×80GB A100 GPU，耗时约 14.5 小时
- **用户模拟器**：基于 LLM 构建，用于高效生成前向采样对话路径

### **评估维度与指标**

| 维度 | 指标 |
|------|------|
| **Clarification Interaction** | Intents Cover Rate, Logical Conflict Rate, Average Interaction Turns, Options Presenting/Reasonable Rate |
| **Intent Execution** | IBLEU, Faithful（忠实度）, Unnecessary Sub-tasks (US), General Sub-tasks (GS), Tool Invocations (TI) |
| **Cognitive Load** | 行为指标（任务时间、token 数量）、主观评分（1–10 分）、生理指标（EEG 功率谱密度 PSD） |

### **基线方法对比**
- **Mistral-Interact** [21]：基于 Q&A 对的逐轮澄清
- **ITIU** [14]：基于交互表格的并行提问
- **CoLLABLLM** [32]：强调用户参与的多轮奖励建模

所有方法统一使用相同主干模型进行公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **Clarification Interaction 结果（见 Table 1, 7, 8）**
| 指标 | Prism-DPO 表现 |
|------|----------------|
| **Logical Conflict Rate (%) ↓** | **11.5%**（最低，远优于 ITIU 的 53.0%） |
| **Intents Cover Rate (%) ↑** | 最高达 **74.85%**（IN3 复杂场景） |
| **Average Interaction Turns ↓** | **1.5–1.8 轮**，接近 ITIU 效率 |
| **Options Reasonable Rate (%) ↑** | 达 **93.05%**（IN3 复杂场景） |

> 在复杂意图场景下，Prism 显著优于所有基线，尤其在逻辑一致性方面实现质的飞跃。

#### ✅ **Intent Execution 结果（见 Table 2）**
| 场景 | 指标 | Prism-DPO vs Baseline 提升 |
|------|------|----------------------------|
| **复杂意图** | Faithful ↑ | **+21.16%** |
| **复杂意图** | US (Unnecessary Sub-tasks) ↓ | **2.36 → 2.27** |
| **复杂意图** | GS (General Sub-tasks) ↓ | **0.37 → 0.35** |
| **复杂意图** | TI (Tool Invocations) ↓ | **4.26 → 3.12**，降幅 **22.26%** |

> Prism 上游的逻辑澄清显著提升了下游代理（XAgent）的任务执行效率与准确性。

#### ✅ **Cognitive Load 实验结果（见 Figure 3 & 4）**
| 指标 | Prism 表现 |
|------|-----------|
| **平均任务完成时间 ↓** | 比 Mistral-Interact 减少 **34.8%** |
| **总 Token 数 ↓** | 控制在 **<1000**，信息更紧凑 |
| **用户满意度评分 ↑** | 平均 **8.35**（满分 10），88.6% 用户评为“良好以上” |
| **EEG PSD 水平 ↓** | 显示大脑活跃度最低，认知负荷最小 |

> 用户在与 Prism 交互时更轻松、高效，且满意度随轮次上升，表明其交互设计具有正向累积效应。

### **消融实验（Ablation Study）**
虽然文中未单独列出消融表，但从模块设计和自演进过程可推断：
- **CID 数据集的作用**：提供结构化依赖先验，是实现逻辑分解的基础。
- **Intent-Aware Reward 的有效性**：通过 token-level 奖励筛选高质量轨迹，支撑无监督数据生成。
- **Self-Evolved Tuning 的增益**：多轮迭代使模型逐步掌握深层逻辑推理能力。

---

## **4. 关键结论和发现**

### **主要发现**
1. **逻辑依赖建模至关重要**：在复杂意图场景中，忽视问题间的依赖会导致大量逻辑冲突，严重影响用户体验。
2. **Prism 实现了逻辑性与效率的统一**：既能保持低交互轮次（≈1.6 轮），又能保证高度逻辑一致（冲突率仅 11.5%）。
3. **显著降低用户认知负荷**：行为、主观、生理三类指标一致证明 Prism 更“省力”、“易用”。
4. **具备强泛化能力**：在 TIN、IN3、ABP 三大数据集及两种主干模型上均取得 SOTA 性能。
5. **推动 LLM 成为真正协作者**：Prism 不再被动响应，而是主动、有条理地引导用户明确目标。

### **方法的局限性**
- **依赖 CID 数据集的质量与覆盖范围**：对于全新领域或极端罕见意图，few-shot 构造可能失效。
- **用户模拟器的保真度限制**：尽管引入了风格模仿，但仍无法完全复现真实人类的不确定性与情绪波动。
- **实时性挑战**：Monte Carlo Sampling 和多轮自演进训练成本较高，部署门槛略高。

### **未来工作方向**
- 扩展 CID 至更多垂直领域（如医疗、法律）。
- 探索轻量化版本以适应边缘设备部署。
- 引入多模态反馈（语音、表情）进一步丰富认知负荷评估。
- 结合记忆机制实现跨会话意图追踪。

---

> 🔚 **总结一句话**：  
> **Prism 首次将认知负荷理论系统应用于 LLM 意图理解，通过结构化解构与逻辑驱动的澄清流程，在不牺牲效率的前提下大幅降低用户心智负担，实现了更自然、更智能的人机协作范式。**

</details>

---

### 5. [Hierarchical Precision and Recursion for Accelerating Symmetric Linear Solves on MXUs](https://arxiv.org/abs/2601.08082)

**Authors**: Vicki Carrica, Rabab Alomairy, Evelyne Ringoot, Alan Edelman  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08082v1  

#### Abstract
Symmetric linear solves are fundamental to a wide range of scientific and engineering applications, from climate modeling and structural analysis to machine learning and optimization. These workloads often rely on Cholesky (POTRF) decomposition and its supporting operations, triangular solves (TRSM)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Hierarchical Precision and Recursion for Accelerating Symmetric Linear Solves on MXUs*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
对称正定线性系统（Symmetric Positive Definite, SPD）的求解在科学计算、机器学习等领域中至关重要，其核心是 **Cholesky 分解**（POTRF），依赖于 **TRSM**（三角矩阵求解）和 **SYRK**（对称秩-k更新）等密集线性代数操作。传统方法通常采用统一高精度（如 FP64），难以充分利用现代 AI 加速器（如 NVIDIA Tensor Cores 和 AMD Matrix Cores）提供的低精度（如 FP16）高性能。

然而，直接使用低精度会带来数值不稳定性和精度损失，限制了其在科学计算中的应用。

### 提出的新方法与创新思路
本文提出了一种**可移植的混合精度对称线性求解器**，专为 **Matrix Processing Units (MXUs)** 设计，核心创新包括：

#### （1）嵌套递归算法设计（Nested Recursive Algorithm）
- 将传统的仅对角块递归的 Cholesky 分解扩展为**完全嵌套递归结构**，将 POTRF、TRSM 和 SYRK 全部递归化。
- 首次实现了 **GPU 上的递归 SYRK**，并结合已有的递归 TRSM，形成完整的递归分解路径。
- 该结构显著增加了 GEMM 操作的比例，提升并行度和硬件利用率，特别适合 MXUs。

#### （2）树状分层混合精度策略（Tree-Structured Mixed Precision）
- 引入一种基于递归树结构的**分层精度分配机制**：
  - 大型非对角块（off-diagonal blocks）使用 **FP16** 以最大化吞吐；
  - 对角块（diagonal blocks）保留 **FP32 或 FP64** 以保证数值稳定性。
- 这种“外层低精度、内层高精度”的设计，在性能与精度之间实现精细平衡。

#### （3）块级量化与反量化（Blockwise Quantization & Dequantization）
- 为缓解 FP16 动态范围有限的问题，在每层递归中引入轻量级的 per-block 量化机制：
  - 在 FP16 计算前进行缩放（quantization），防止溢出；
  - 计算后恢复原始尺度（dequantization）。
- 开销极小，但有效提升了数值鲁棒性。

#### （4）基于 Julia 的可移植实现
- 利用 Julia 的多重派发（multiple dispatch）、动态类型推断和数组编程特性，构建了一个**硬件无关的高层接口**。
- 底层调用 vendor-specific 库（如 cuBLAS / rocBLAS），实现跨平台（NVIDIA H200 和 AMD MI300X）高性能与可移植性。

### 相比现有方法的优势
| 维度 | 本文方法 | 传统方法 |
|------|----------|---------|
| **递归粒度** | 完全嵌套递归（POTRF + TRSM + SYRK） | 仅 POTRF 递归，TRSM/SYRK 为标准块形式 |
| **精度控制** | 树状分层混合精度，细粒度控制 | 均匀精度或迭代精化（iterative refinement） |
| **硬件适配性** | 显式面向 MXUs（Tensor Cores / Matrix Cores）优化 | 主要针对通用 CPU/GPU BLAS |
| **实现方式** | 高层语言（Julia）实现，自动调度至底层库 | 多为 C/CUDA 手写内核 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用随机生成的稠密对称正定矩阵（SPD matrices）。
- 矩阵元素服从均匀分布，并通过向对角线加 `n`（维度）确保正定性和良好条件数。
- 测试规模从 `4096×4096` 到 `65536×65536`。

### 实验设置
- **硬件平台**：
  - **NVIDIA H200**：基于 Hopper 架构，支持 FP16 Tensor Cores。
  - **AMD MI300X**：配备 Matrix Cores，支持混合精度计算。
- **软件环境**：
  - Julia 1.12.0
  - GPU 支持库：CUDA.jl（NVIDIA）、AMDGPU.jl（AMD）
  - 后端调用：cuBLAS/cuSOLVER（NVIDIA）、rocBLAS/rocSOLVER（AMD）

### 评估指标
- **Speedup**：相对于 FP64 基线（cuSOLVER / rocSOLVER）的加速比。
- **Throughput (TFLOPs)**：实际达到的浮点运算性能。
- **Accuracy**：以 FP64 结果为基准，计算相对误差范数（-log₁₀(error)），衡量有效数字位数。
- **Portability**：同一份 Julia 代码在不同厂商 GPU 上的表现一致性。

### 基线方法对比
- **Full-Precision Baseline**：cuSOLVER FP64 / rocSOLVER FP64
- **Uniform Precision Variants**：纯 FP32、纯 FP16
- **Mixed-Precision Baselines**：固定分区的混合精度方法（如 [FP16, FP32]）
- **Vendor Libraries**：cuBLAS SYRK/TRSM、rocBLAS SYRK/TRSM

---

## 3. 主要实验结果和性能指标

### 关键性能数据（NVIDIA H200）

#### （1）SYRK 性能（图 4）
- **递归 FP64 SYRK**：相比 cuBLAS FP64，最高达 **14× 加速**（n=65,536）。
- **混合精度 SYRK**（如 `[FP16,FP16,FP16,FP16,FP32]`）：最高达 **27× 加速**。
- **纯 FP16 SYRK**：峰值可达 **149×**，但精度严重下降。

#### （2）TRSM 性能（图 5）
- **递归 FP64 TRSM**：接近 cuBLAS 表现。
- **混合精度 TRSM**：最高达 **5.3× 加速**。
- **纯 FP16 TRSM**：最高 **6× 加速**，但稳定性差。

#### （3）Cholesky 整体性能（图 6–7）
- **吞吐量**（图 6）：
  - 深层混合精度配置 `[FP16×6, FP32]` 达到超过 **2× cuSOLVER FP64 吞吐**。
  - 纯 FP16 接近 **3× 吞吐**，但精度极低。
- **最终加速比**（图 7）：
  - 最优混合精度配置实现 **5.32× 速度提升**（vs. cuSOLVER FP64）。
  - 纯 FP16 达到 **6× 加速**，但不可靠。

#### （4）精度表现（图 8）
- **纯 FP64**：约 15 位有效数字。
- **[FP32,FP32,FP32,FP64]**：保持 ~12 位，优于纯 FP32。
- **深层混合精度**（如 `[FP16×5, FP32]`）：维持 **5–6 位有效数字**。
- **纯 FP16**：< 4 位有效数字。
- **关键结论**：混合精度方案在保留 **88% 纯 FP16 峰值加速**的同时，获得 **100× 更好的精度**。

### AMD MI300X 结果（图 9）
- 在 MI300X 上，最优混合精度配置 `[FP16,FP16,FP16,FP32]` 实现 **5.3× 加速**（vs. rocSOLVER FP64）。
- 展现出与 NVIDIA 平台相似的趋势，验证了跨平台可移植性。

### 消融实验分析（图 10–11）
- **递归深度影响**（图 10）：
  - 加速比随矩阵增大而线性增长，在 `n=65,536` 达到 **5.32×** 峰值。
  - 小矩阵因递归开销大而不受益。
- **跨平台可移植性**（图 11）：
  - 单一 Julia 实现在 NVIDIA 和 AMD 上均取得良好扩展性。
  - 当前 AMD 上未启用 GemmEx（混合精度 GEMM），否则潜力更大。

---

## 4. 关键结论和发现

### 主要发现
1. **嵌套递归结构显著提升 GEMM 密度和并行性**，使 Cholesky 分解更适应 MXUs 的计算模式。
2. **树状分层混合精度** 是兼顾性能与稳定性的有效策略：非对角块用 FP16 提升吞吐，对角块用高精度保障准确性。
3. **轻量级块级量化机制** 能有效解决 FP16 动态范围不足问题，无显著性能代价。
4. **Julia 的抽象能力** 支持高效表达复杂递归与混合精度逻辑，同时通过多重派发实现跨平台高性能。
5. 在 **NVIDIA H200** 上，该方法相较 cuSOLVER FP64 实现 **5.32× 加速**，且精度远超纯 FP16（100× 更好），保留了 88% 的 FP16 峰值性能。
6. 在 **AMD MI300X** 上也取得类似增益（最高 5.3×），证明方法具有良好的跨厂商可移植性。

### 方法的局限性
- **小矩阵收益有限**：递归深度受限，任务粒度过细可能导致调度开销大于收益。
- **依赖 vendor 库的 base case**：当前仍调用 cuBLAS/rocBLAS 处理叶子节点，尚未完全自包含。
- **AMD 平台功能缺失**：Julia 生态暂不支持 AMD 的混合精度 GEMM（GemmEx），限制了其潜力发挥。
- **未考虑稀疏或带状结构**：目前仅适用于稠密 SPD 系统。

### 未来工作方向
- 扩展至 **LDLᵀ 分解**（处理不定矩阵）和 **稀疏/块稀疏系统**。
- 引入 **自适应精度控制**，根据矩阵条件数动态调整精度层级。
- 探索 **多 GPU 分布式版本**，支持更大规模问题。
- 实现 **全栈硬件无关实现**，包括 base case 的递归 GEMM，迈向 Apple Metal 等新架构。
- 结合 **异步任务运行时** 和 **能耗感知调度**，进一步优化资源利用。

--- 

> ✅ **总结一句话**：  
> 本文通过**嵌套递归 + 树状分层混合精度 + Julia 可移植实现**，首次实现了在 MXUs 上高效且稳定的 Cholesky 求解，在 H200 上获得 **5.32× 加速**，并在精度与性能间取得卓越平衡，展示了面向未来异构平台的高性能线性代数新范式。

</details>

---

### 6. [MixServe: An Automatic Distributed Serving System for MoE Models with Hybrid Parallelism Based on Fused Communication Algorithm](https://arxiv.org/abs/2601.08800)

**Authors**: Bowen Zhou, Jinrui Jia, Wenhao He, Yong Zhang, Fang Dong  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08800v1  

#### Abstract
The Mixture of Experts (MoE) models are emerging as the latest paradigm for Large Language Models (LLMs). However, due to memory constraints, MoE models with billions or even trillions of parameters can only be deployed in multi-GPU or even multi-node & multi-GPU based serving systems. Thus, communi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MixServe 论文核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大规模 **Mixture of Experts (MoE)** 模型在分布式推理部署中面临严重通信瓶颈，尤其是跨节点（inter-node）通信开销大。传统并行策略存在以下问题：
- **Tensor Parallelism (TP)**：依赖 All-Reduce (AR)，在单节点内高效，但跨节点扩展性差（受限于低带宽 inter-node 连接）。
- **Expert Parallelism (EP)**：使用 All-to-All (A2A)，虽支持跨节点扩展，但易导致负载不均衡，尤其在高并行度下效率下降。
- 缺乏系统化分析模型超参数、网络拓扑与硬件资源之间关系的方法，部署策略多依赖经验。

### 🚀 提出的新方法与创新
论文提出 **MixServe**，一个面向 MoE 模型的自动分布式推理服务系统，其核心创新包括：

#### （1）**自动化的并行策略选择机制**
- 在离线阶段综合考虑模型超参数（如专家数、隐藏维度）、硬件配置（计算能力、NVLink/RoCE 带宽）和网络拓扑，通过理论建模与性能预测，**自动选择最优的混合并行策略**（TP/EP/DP/PP 组合），取代人工经验决策。

#### （2）**基于融合通信算法的 TP-EP 混合并行（Hybrid TP-EP）**
- 设计了一种新型 **TP-EP Hybrid Parallelism** 架构：
  - Attention 层采用 **intra-node TP + inter-node DP**
  - MoE 层采用 **intra-node TP + inter-node EP**
- 提出 **Fused AR-A2A Communication Algorithm**，将 intra-node 的 AR 通信与 inter-node 的 A2A 通信进行重叠执行，有效利用带宽层级差异（intra-node 高速 vs inter-node 低速），显著降低总通信延迟。

#### （3）两种具体的融合通信算法实现
- **Fused RS-Combine**：Reduce Scatter（RS）与 A2A Combine 重叠。
- **Fused AG-Dispatch**：All Gather（AG）与 A2A Dispatch 重叠。
- 利用异步通信机制实现时间上的并行化，提升整体吞吐。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **通信效率** | 显著减少跨节点通信量，充分利用 intra-node 高带宽 |
| **自动化程度** | 自动分析并选择最佳并行策略，适应不同模型与集群环境 |
| **性能一致性** | 在多种硬件平台（H20 GPU / Ascend 910B NPU）均表现优越 |
| **可扩展性** | 支持大规模 MoE 模型（如 DeepSeek-R1, Qwen3）的高效部署 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与模型
- **模型**：
  - **DeepSeek-R1**：671B 参数，256 路由专家 + 1 共享专家，每 token 激活约 37B 参数
  - **Qwen3-235B-A22B**：235B 参数，128 专家，每 token 激活约 22B 参数
- **数据集**：
  - **ShareGPT-V3**：包含 1.2B tokens 的人类对话数据，用于生成请求序列进行基准测试

### ⚙️ 实验设置
- **硬件平台**：
  1. **H20 Cluster**：2 台服务器，每台 8×Nvidia H20 GPU（96GB），NVLink 4.0（900GB/s intra-node），InfiniBand（400Gbps inter-node）
  2. **Ascend 910B Cluster**：4 台 Atlas 800T A2 服务器，每台 8×Ascend 910B NPU（64GB），HCCS（480Gbps intra-node），RoCE（200Gbps inter-node）
- **请求负载**：
  - 请求速率：2, 4, 8 req/s
  - 最大批大小：16
  - 最大序列长度：4096 tokens

### 🎯 评估指标
| 指标 | 定义 |
|------|------|
| **TTFT (Time to First Token)** | 用户发出请求到收到第一个输出 token 的时间，反映 Prefill 阶段性能 |
| **ITL (Inter-Token Latency)** | 相邻两个 token 输出之间的平均间隔，反映 Decode 阶段性能 |
| **Throughput (token/s)** | 单位时间内生成的 token 总数，衡量系统整体吞吐能力 |
| **P99 Latency** | 99% 的请求满足的延迟上限，体现稳定性 |

### 🆚 基线方法对比
| 基线 | 并行策略说明 |
|------|--------------|
| **vLLM** | 支持 TP+PP 或 DP+EP，是主流 LLM 推理框架 |
| **Tutel** | 支持 TP+EP 的 MoE 专用优化库 |
| 多种配置组合：TP=4/8, DP=2/4/8, EP=16/32 等 |

详细基线配置见原文 Table III。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能提升（vs. 基线）

| 指标 | 提升范围 | 具体案例 |
|------|---------|----------|
| **TTFT 加速比** | **1.08× ~ 3.80×** | - Qwen3 on Ascend 910B: 较 vLLM TP+PP 快 **3.80×**<br>- DeepSeek-R1 on Ascend 910B: 较 vLLM TP+PP 快 **2.67×** |
| **ITL 加速比** | **1.03× ~ 1.66×** | - Qwen3 on Ascend 910B: ITL 从 134.27ms → **81.1ms** (**1.66×**) |
| **Throughput 提升** | **+5.2% ~ +50.3%** | - DeepSeek-R1 on H20: 吞吐从 362.78 → **545.23 token/s** (**+50.3%**) |

> 图 10 展示了在不同硬件和模型下的全面性能对比，MixServe 在所有指标上均优于所有基线。

### 🔬 消融实验结果

#### （1）**DP 与 EP 的权衡分析（dDP vs dEP）**
- 当 `dDP ≈ dEP` 时（平衡配置），在 Ascend 910B 上取得最佳性能（最低 TTFT 和最高吞吐）。
- 在 H20 高带宽环境下，`dDP < dEP` 更优，表明 MixServe 能根据带宽特性动态调整最优策略。
- 结论：MixServe 的自动分析器能准确捕捉硬件差异并做出最优决策。

#### （2）**通信重叠的影响（Sync vs Async）**
- 异步融合通信（Async Fused AR-A2A）相比同步方式显著降低延迟。
- Gantt 图显示 intra-node AG/RS 与 inter-node A2A 成功重叠，节省时间接近整个 inter-node 通信耗时。
- 结果：启用异步融合后，TTFT 下降约 15%~30%，ITL 和吞吐也相应改善。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **通信层级差异必须被显式建模**：intra-node 与 inter-node 带宽的巨大差距是 MoE 推理的关键瓶颈，不能简单统一处理。
2. **混合 TP-EP 并行优于纯 EP 或纯 TP**：通过在 MoE 层引入 intra-node TP，可分担部分 A2A 通信压力，降低整体开销。
3. **Fused AR-A2A 显著提升通信效率**：通过合理调度，让 intra-node AR 与 inter-node A2A 重叠执行，实现了“以空间换时间”的高效优化。
4. **自动化策略选择至关重要**：不同模型、不同集群应采用不同的并行配置，手动调优成本高且难以泛化，而 MixServe 可自动完成这一过程。

### ⚠️ 方法的局限性
- **内存开销增加**：Fused RS-Combine 需要额外临时存储空间（O(bsh·nproc)），对显存紧张场景可能构成挑战。
- **依赖底层通信库支持**：需要支持细粒度异步通信（如 isend/irecv）和灵活的通信组管理。
- **目前聚焦推理阶段**：未涉及训练场景的扩展，尽管技术原理可迁移。

### 🔮 未来工作方向
- 扩展至 **Training-Serving 统一架构**，支持 MoE 全流程高效运行。
- 支持更复杂的 **5D 并行**（TP/EP/DP/PP/CP），适配异构集群。
- 引入 **在线自适应调优机制**，根据实时负载动态调整并行策略。
- 探索 **稀疏路由压缩** 技术进一步减少 A2A 数据传输量。

---

## 总结

> **MixServe 是首个将理论建模、自动策略选择与融合通信优化相结合的 MoE 分布式推理系统**。它不仅解决了传统方法中通信效率低下、策略设计依赖经验的问题，还在真实大规模模型（DeepSeek-R1、Qwen3）和多类硬件平台上验证了其卓越性能：**最高达 3.8× TTFT 加速，50.3% 吞吐提升**。该工作为未来超大规模 MoE 模型的实际部署提供了重要基础设施支撑。

</details>

---

### 7. [Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs](https://arxiv.org/abs/2601.08403)

**Authors**: Abhijnan Nath, Alireza Bagheri Garakani, Tianchen Zhou, Fan Yang, Nikhil Krishnaswamy  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08403v1  

#### Abstract
Large language models are increasingly trained via reinforcement learning for personalized recommendation tasks, but standard methods like GRPO rely on sparse, sequence-level rewards that create a credit assignment gap, obscuring which tokens drive success. This gap is especially problematic when mo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在推荐系统等生成任务中越来越多地通过强化学习（Reinforcement Learning, RL）进行训练，但标准方法如 **Group Relative Policy Optimization (GRPO)** 存在以下关键缺陷：
- **信用分配缺口（credit assignment gap）**：仅依赖稀疏的序列级奖励（sequence-level rewards），无法识别是哪些具体的 token 或语义片段（如产品属性描述）真正驱动了成功。
- **缺乏可解释性与效率**：所有 token 被赋予相同的优势值（advantage），导致梯度更新不精准。
- **易受奖励欺骗（reward hacking）**：模型可能过拟合检索器的表面特征而非理解真实用户意图。

这些问题在用户意图模糊、无明确标签的场景下尤为严重。

---

### 提出的新方法与核心思想
本文提出 **Owen-Shapley Policy Optimization (OSPO)**，一种基于合作博弈论的新型策略优化框架，其核心创新如下：

#### （1）引入 Owen-Shapley 值进行细粒度信用分配
- 将生成文本中的**语义连贯片段**（如短语、句子）视为“玩家”，构建**联盟游戏（coalition game）**。
- 使用 **Owen-Shapley values** 计算每个片段对最终奖励的边际贡献，从而实现从序列级到片段级的信用再分配。
- 该方法继承了 Shapley 值的公平性保证（效率、对称性、线性），同时通过限制为**连续片段联盟**显著降低计算复杂度。

#### （2）无需价值模型的价值重塑（Value-Model-Free Reward Shaping）
- 不依赖额外的 critic 网络或过程奖励模型（Process Reward Model），直接利用任务反馈（如 NDCG）或黑盒检索器来估计片段贡献。
- 采用 **Potential-Based Reward Shaping (PBRS)** 技术将 Owen 值映射到 token 级优势，保持最优策略不变。

#### （3）长度无关的优势重分配机制
- 提出公式 $ A_t^{(g)} = T \cdot \hat{\phi}_t^{(g)} \cdot A^{(g)} $，其中 $ T $ 为序列长度，$ \hat{\phi}_t $ 为归一化的 Owen 值。
- 此设计确保平均 token 优势等于原始序列优势，消除了长序列因 Owen 值稀释而导致的梯度弱化问题（即长度偏差）。

#### （4）两种优势重分配策略
- **OSPO-Prop**：按 Owen 值比例分配优势。
- **OSPO-Rank**：先将 Owen 值转为排名再分配，增强对噪声估计的鲁棒性。

---

### 相比现有方法的优势
| 方法 | 是否需要 Value Model | 信用分配粒度 | 可解释性 | 鲁棒性 |
|------|------------------------|---------------|-----------|---------|
| GRPO / PPO | ❌ | 序列级（uniform） | 低 | 易 reward hacking |
| DPO | ❌ | 成对偏好 | 中 | 依赖静态数据 |
| SCAR (Cao et al.) | ✅ | 片段级 | 高 | 限于有 critic 场景 |
| **OSPO** | ❌ | **片段级（fine-grained）** | **高** | **强，跨检索器泛化好** |

> ✅ **OSPO 在不增加外部网络的前提下实现了更精细、更合理的信用分配，兼具高效性与原则性。**

---

## 2. 核心实验方法和设置

### 数据集
- **ESCI Shopping Queries** (Reddy et al., 2022)：标准电商搜索数据集，用于传统产品查询扩展任务。
- **H&M Fashion Recommendations** (Ling et al., 2022)：时尚推荐数据集，包含丰富购买历史，用于上下文化搜索与用户画像摘要生成。

> 注：H&M 缺少显式查询，作者使用 **Claude Sonnet 3.0** 生成合成查询与专家摘要作为监督信号。

---

### 实验设置
- **模型架构**：主干模型为 `Qwen2.5-Instruct 7B`，部分实验使用更大/小规模变体作对比。
- **训练方式**：
  - 所有 RL 方法每条提示采样 G=8 条响应。
  - 使用 FAISS 进行密集检索，领域编码器分别为：
    - ESCI: `ALL-MPNET-BASE-V2`
    - H&M: `SIMCSE-LARGE`
- **奖励函数**：
  - 搜索任务：终端奖励为 **NDCG@1000**
  - 用户摘要任务：结合 **Bradley-Terry Reward Model**（90%）与格式奖励（10%）

---

### 评估指标
| 任务 | 主要指标 |
|------|----------|
| 产品搜索 | NDCG, AP, MRR, Recall |
| 用户摘要质量 | LLM Judge (Qwen-3-Nemotron-32B-Reward) 的成对胜率（Win Rate, WR）与平局率（Tie Rate） |

---

### 基线方法
- **SFT**：监督微调，仅用高质量样本（NDCG > 0.5）。
- **DPO**：直接偏好优化，使用专家对比对。
- **GRPO**：当前主流的无价值模型 RL 方法，作为主要在线 RL 对照。
- **OSPO-Clip**：OSPO 的消融版本，限制重分配后的优势范围。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | ESCI NDCG | H&M NDCG |
|------|------------|-----------|
| Qwen2.5-72B | 0.543 | 0.357 |
| **OSPO-Prop (7B)** | **0.522** | **0.436** |
| GRPO (7B) | 0.418 | 0.379 |
| DPO (7B) | 0.431 | 0.396 |
| SFT (7B) | 0.398 | 0.373 |

> 💡 **结论**：仅 7B 参数的 OSPO-Prop 性能接近甚至超越 72B 大模型，在 H&M 上更是大幅领先。

---

### 与基线方法的对比结果
- **相比 GRPO**：
  - ESCI 上 NDCG 提升 **24.9%**（0.522 vs 0.418）
  - H&M 上提升 **15.0%**（0.436 vs 0.379）
- **相比 DPO**：
  - ESCI 提升 **21.1%**，H&M 提升 **10.1%**
- **相比 SFT**：全面提升，尤其在复杂上下文任务中表现突出。

> 📈 图1显示，OSPO 在约 **400 步**达到目标性能，而 GRPO 需要超过 **800 步**，表明其样本效率更高。

---

### 消融实验结果

#### （1）不同 coalition 配置的影响（Table 5 & Figure 2）
- 最优配置：**wmax=8, M=96**（宽度适中、采样充分）
- 过窄（w=1~2）：过拟合局部共现，后期崩溃。
- 过宽（w=12~16）：超出实际查询长度，资源浪费。
- 非连续联盟（All Subsets）：性能急剧下降至 NDCG≈0.11，验证了**语义连贯性的重要性**。

#### （2）生成数量（num_generations）影响
- 从 8 减少到 2 次生成时，准确率骤降，说明 **Monte Carlo 采样对稳定优势估计至关重要**。

#### （3）OSPO-Prop vs OSPO-Rank（Table 2）
| 对比 | Win Rate (WR) |
|------|----------------|
| OSPO-Prop vs GRPO | 49.1% |
| OSPO-Rank vs GRPO | 47.6% |
| OSPO-Prop vs SFT | 53.3% |
| OSPO-Rank vs SFT | 54.0% |

> 两者表现相近，说明两种重分配策略均有效；OSPO-Rank 在噪声环境下更具鲁棒性。

#### （4）跨检索器泛化能力（Table 4）
- 在 ESCI 训练、SIMCSE 测试的跨域设置下：
  - GRPO NDCG 降至 0.2257
  - **OSPO-Prop 达到 0.3691**，显著优于所有基线
- 表明 OSPO 学习的是**更通用、更可迁移的对齐信号**，而非过拟合特定嵌入空间。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **细粒度信用分配优于均匀分配**：OSPO 通过 Owen-Shapley 值识别出真正推动性能的关键语义单元（如“professional tailored fit”），使梯度集中在高贡献区域。
2. ✅ **无需价值模型也能实现高效 RLHF**：OSPO 在不引入 critic 的情况下完成了 credit assignment，降低了工程复杂度。
3. ✅ **小模型 + 好算法 > 大模型 + 差优化**：7B 的 OSPO 可媲美 72B 模型，证明了优化机制的重要性远超参数规模。
4. ✅ **更强的泛化与稳定性**：OSPO 在测试集上持续提升，而 GRPO 出现灾难性遗忘；且在跨检索器评估中表现稳健。
5. ✅ **coalescence 结构是关键超参**：适度宽度（w=4~8）、连续性、充足采样共同构成稳定学习的内在正则化机制。

---

### 方法的局限性
- **计算开销增加**：需多次调用 reward model/检索器以评估不同联盟，带来额外延迟。
- **超参数敏感**：coalescence 宽度 $ w_{\text{max}} $ 和采样数 $ M $ 需仔细调整。
- **近似误差**：Monte Carlo 采样可能导致长距离依赖被低估。
- **任务依赖性**：segment 划分依赖任务特性（短语 or 句子），自动化提取仍具挑战。
- **单轮假设**：目前仅适用于 single-turn 生成，未扩展至 multi-turn 对话或 agent 推理。

---

### 未来工作方向
- 扩展至 **multi-turn RL** 与 **agent-based 决策环境**（如代码生成、对话规划）。
- 探索 **动态 coalition 构建机制**，自适应选择语义单元。
- 结合 **sparse attention 或 cache 机制** 加速 Owen 值计算。
- 推广至 **多模态生成任务**（图文生成、语音助手等）。
- 研究如何将 OSPO 与 **chain-of-thought prompting** 更深度整合，提升推理透明度。

---

> 🔚 **总结**：  
> **OSPO 是一个原则性强、可解释、高效的 RLHF 新范式**。它通过将博弈论中的 Owen-Shapley 值引入 LLM 训练，解决了长期存在的 credit assignment 问题，在多个电商推荐任务中展现出卓越性能与泛化能力，为下一代生成式搜索与个性化推荐系统提供了坚实的技术基础。

</details>

---

### 8. [Generation-Augmented Generation: A Plug-and-Play Framework for Private Knowledge Injection in Large Language Models](https://arxiv.org/abs/2601.08209)

**Authors**: Rongji Li, Jian Xu, Xueqing Chen, Yisheng Yang, Jiayi Wang, Xingyu Chen, Chunyu Xie, Dawei Leng, Xu-Yao Zhang  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08209v1  

#### Abstract
In domains such as biomedicine, materials, and finance, high-stakes deployment of large language models (LLMs) requires injecting private, domain-specific knowledge that is proprietary, fast-evolving, and under-represented in public pretraining. However, the two dominant paradigms for private knowle...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Generation-Augmented Generation: A Plug-and-Play Framework for Private Knowledge Injection in Large Language Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在生物医学、材料科学、金融等高风险领域，大型语言模型（LLM）需要注入**私有、专有的领域知识**以实现可靠部署。然而，当前主流的知识注入方法存在显著缺陷：

- **Fine-tuning**：成本高昂，迭代困难，且持续更新易导致**灾难性遗忘**（catastrophic forgetting）和通用能力退化。
- **Retrieval-Augmented Generation (RAG)**：虽保持基础模型不变，但在私有领域中表现脆弱，原因包括：
  - 文本分块导致的**证据碎片化**（chunk-induced evidence fragmentation）
  - 检索漂移（retrieval drift）
  - 长上下文压力（long-context pressure）
  - 查询依赖的提示膨胀（prompt inflation）

### **提出了什么新方法或新思路**

本文提出 **Generation-Augmented Generation (GAG)**，一种**检索无关、即插即用**（retrieval-free, plug-and-play）的知识注入框架，其核心思想是：

- 将私有领域知识视为一种“专家模态”（expert modality），通过一个**紧凑的表示层接口**（representation-level interface）对齐到冻结的基础模型（frozen base model）中。
- 不依赖于文本序列化的检索过程，而是通过一个**单连续 token 注入**（one-token injection）机制，在不修改基础模型参数的前提下注入领域知识。

### **相比现有方法的优势**

| 特性 | Fine-tuning | RAG | GAG |
|------|-----------|-----|-----|
| 是否修改基础模型 | ✅ 是 | ❌ 否 | ❌ 否 |
| 迭代成本 | 高 | 中 | 低 |
| 上下文压力 | 无 | 高（长上下文） | 极低（固定预算） |
| 模块化扩展 | 困难 | 可行但复杂 | ✅ 即插即用 |
| 选择性激活可靠性 | 低 | 中 | ✅ 高（接近Oracle） |
| 抗检索错误能力 | 强 | 弱 | ✅ 强 |

GAG 的核心优势在于实现了**常量预算、模块化、可扩展且可靠的私有知识注入**，避免了 prompt-time 的证据序列化，同时支持多领域组合。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **通用领域 QA 基准**（6个公开数据集）：
  - FreebaseQA, HotpotQA, Natural Questions, TriviaQA, WebQuestions, PopQA
  - 用于评估模型在开放域任务中的通用能力是否受损。

- **私有领域 QA 基准**（2个科学领域）：
  - **Immunology Adjuvant**（免疫佐剂）：来自匿名研究 (Anonymous, 2025b)
  - **Catalytic Materials**（催化材料）：来自 Catalystbench (Anonymous, 2025a)
  - 配套私有文献库：分别包含 813 和 986 篇论文。

### **实验设置和评估指标**

- **基础模型**：`Qwen3-8B`（冻结）
- **领域专家模型**：`Qwen3-1.7B`
- **投影器**（Projector）：轻量级两层 MLP，将专家模型输出映射到基础模型嵌入空间。
- **路由机制**：Prototype Plug-and-Play Routing (PPR)，基于冻结编码器和原型聚类实现无需训练的选择性激活。

#### **评估指标**

| 类型 | 指标 | 说明 |
|------|------|------|
| 通用领域 | **Exact Match (EM)** | 字符串精确匹配，衡量事实正确性 |
| 私有领域 | **BERTScore (with SciBERT)** | 基于语义相似度，更适合自由形式答案的技术领域 |

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **Base-Model-Only** | 仅使用 `Qwen3-8B`，无任何外部知识 |
| **RAG** | 使用 ColBERTv2 检索 top-k 文档并拼接至 prompt |
| **xRAG** | 极端压缩版 RAG，仅保留 1 个 token 的检索摘要 |
| **EGC** (Expert-Generated Context) | 领域专家模型生成背景文本并追加至 prompt |

所有方法均在**冻结基础模型**前提下进行比较。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 方法 | Adjuvant (BERTScore) | Materials (BERTScore) | 通用平均 (EM) | 注入 token 数 |
|------|------------------------|-------------------------|----------------|----------------|
| Base-Model-Only | 56.12 | 60.01 | 42.16 | 0 |
| RAG | 59.97 (+6.86%) | 62.13 (+3.53%) | — | 375.17 |
| xRAG | 59.12 (+5.35%) | 62.24 (+3.72%) | — | 1 |
| EGC (Adjuvant) | 64.07 (+14.17%) | 58.60 (-2.35%) | ↓ 显著下降 | 148.48 |
| EGC (Materials) | 54.22 (-3.39%) | 66.47 (+10.76%) | ↓ 显著下降 | 165.21 |
| **GAG (Gen+Adj)** | **69.16 (+23.24%)** | 61.41 (+0.57%) | 42.31 (+0.36%) | **1** |
| **GAG (Gen+Adj+Mat)** | **69.17 (+23.25%)** | **71.36 (+18.91%)** | 42.35 (+0.45%) | **1** |

> ✅ GAG 在两个私有领域分别提升 **15.34%** 和 **14.86%** 相对于强 RAG 基线，并**维持通用能力不退化**。

### **与基线方法的对比结果**

- **RAG/xRAG**：提升有限，表明**检索质量是瓶颈**，而非上下文长度。
- **EGC**：虽然在匹配领域有效，但会严重损害通用能力，且跨领域泛化差。
- **GAG**：通过**表示层对齐**（representation-level alignment）实现高信噪比注入，避免了文本竞争和注意力稀释。

### **消融实验结果**

#### **(1) 路由准确性（PPR）**

| 路由配置 | 微平均准确率 (Micro acc.) | Gen | Adj | Mat |
|----------|----------------------------|-----|-----|-----|
| PPR (2 routes) | 99.78% | 99.65% | 99.91% | — |
| PPR (3 routes) | 99.55% | 99.65% | 99.38% | 99.69% |

✅ 表明 PPR 实现了**接近 Oracle 的选择性激活精度**，支持增量式多领域扩展。

#### **(2) 专家模型读出层消融（Readout Layer Ablation）**

| 读出层 | BERTScore | Δ 相对最优 |
|--------|------------|-------------|
| L2-4 (default) | 69.72 | 0.00 |
| L2-2 | 69.20 | -0.52 |
| L2-8 | 69.38 | -0.34 |
| L2-24 | 58.42 | -11.30 |

✅ 最优读出层为倒数第4层（L2-4），表明**语义整合充分但未过度专业化**的表示最适合迁移。

#### **(3) 两阶段训练消融**

| 变体 | 是否 Stage I | 是否 Stage II | BERTScore |
|------|--------------|---------------|------------|
| w/o Stage I | ❌ | ✅ | 57.14 |
| w/o Stage II | ✅ | ❌ | 55.64 |
| Full GAG | ✅ | ✅ | **69.72** |

✅ 两个阶段缺一不可：
- Stage I：赋予专家模型领域知识
- Stage II：对齐表示空间，实现有效注入

---

## 4. 关键结论和发现

### **主要发现**

1. **GAG 显著优于现有方法**：在私有科学 QA 上超越 RAG 超过 15%，同时保持通用能力稳定。
2. **表示层注入优于文本级注入**：避免了 RAG 的检索脆弱性和 EGC 的注意力干扰。
3. **PPR 实现高精度即插即用路由**：无需训练即可实现近 Oracle 的选择性激活，支持多领域动态扩展。
4. **常量预算接口更可预测**：单 token 注入使推理行为不受检索深度或上下文膨胀影响。

### **方法的局限性**

1. **单领域假设限制**：默认每个查询只属于一个领域，难以处理**跨领域复合问题**。
2. **数值精确性挑战**：由于不直接复制原文，可能在需要精确数字/单位的任务上略有偏差（如 nm vs μm）。
3. **依赖高质量专家模型**：若领域专家模型本身能力不足，注入效果受限。

### **未来工作方向**

1. **多领域联合注入机制**：探索概率性或多路注入，支持跨领域知识融合。
2. **后处理数值校正模块**：引入轻量级验证器提升数值保真度。
3. **更高效的投影器设计**：探索更小参数量或零样本对齐策略。
4. **扩展至多模态私有知识注入**：如结合图像、图谱等非文本私有数据源。

---

> **总结**：GAG 提供了一条**模块化、可扩展、治理友好**的私有知识注入路径，为 LLM 在企业级、科研级场景中的安全、高效部署提供了新范式。

</details>

---

### 9. [Silence the Judge: Reinforcement Learning with Self-Verifier via Latent Geometric Clustering](https://arxiv.org/abs/2601.08427)

**Authors**: Nonghai Zhang, Weitao Ma, Zhanyu Ma, Jun Xu, Jiuchong Gao, Jinghua Hao, Renqing He, Jingwen Xu  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08427v1  

#### Abstract
Group Relative Policy Optimization (GRPO) significantly enhances the reasoning performance of Large Language Models (LLMs). However, this success heavily relies on expensive external verifiers or human rules. Such dependency not only leads to significant computational costs and training latency, but...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Silence the Judge: Reinforcement Learning with Self-Verifier via Latent Geometric Clustering

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于强化学习的大型语言模型（LLM）推理能力提升方法（如 **GRPO**）严重依赖昂贵的外部验证器（external verifiers），例如人工规则或另一个 LLM（如 GPT-4o）作为“Judge”来提供奖励信号。这种依赖带来了以下问题：
- **高计算成本与训练延迟**：调用外部 LLM 需要额外推理开销。
- **稀疏且离散的奖励信号**：通常为二元反馈（0/1），缺乏对推理过程细微差异的指导。
- **验证器偏差与不一致性**：外部 Judge 可能评分不准或存在偏见，影响训练稳定性。

### 提出的新方法与思路
作者提出 **Latent-GRPO**，一种无需外部监督的自验证强化学习框架，其核心思想是：
- **从潜空间几何结构中提取内在奖励信号**：发现正确推理路径在终端 token 的 `last hidden state` 上形成密集聚类，而错误路径则表现为离群点。
- 引入 **Iterative Robust Centroid Estimation (IRCE)** 算法：
  - 对终端隐藏状态进行球面归一化（spherical projection），消除幅度波动。
  - 迭代加权聚合，动态估计一个鲁棒的“真相质心”（truth centroid）。
  - 将每个轨迹到该质心的距离定义为连续、稠密的奖励值。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 消除外部验证器调用，训练速度提升 **2×以上**。 |
| **奖励质量** | 提供**稠密、连续**的奖励信号，优于传统的二元奖励，优化更平滑。 |
| **稳定性** | 避免外部 Judge 的噪声与不一致，防止模型崩溃（model collapse）。 |
| **通用性** | 不依赖任务特定规则，适用于开放域复杂推理任务。 |
| **可扩展性** | 奖励计算仅基于已有的前向传播隐藏状态，无额外推理开销。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|--------|------|
| **GSM8K** | 小学数学应用题，测试基础多步推理能力。 |
| **MATH** | 高中及竞赛级数学题，挑战高级数学推理。 |
| **Open-Platypus** | 多源混合指令数据集，涵盖物理、逻辑、数学等多样化推理任务。 |

### 实验设置与评估指标
- **模型规模**：在 **Qwen3-0.6B, 1.7B, 4B** 三个尺度上验证。
- **硬件配置**：单 GPU，使用 bfloat16 混合精度训练。
- **GRPO 设置**：每组生成 8 条轨迹（G=8），batch size=1。
- **评估指标**：
  - **Task Accuracy**：各测试集上的准确率。
  - **Training Time per Epoch**：衡量训练效率（单位：分钟）。

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **LLM-as-Judge** | 使用 GPT-4o 对输出文本打分，获得二元奖励（0/1）。 |
| **Rule-based Verification** | 使用 SymPy 符号计算或沙箱执行验证答案，提供精确但稀疏的奖励。 |
| **Latent-GRPO (Ours)** | 提出的方法，基于潜空间几何聚类生成内在奖励。 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

#### 在 **GSM8K** 上的表现（Qwen3-4B）
| 方法 | 准确率 | 每轮训练时间 |
|------|--------|--------------|
| LLM-as-Judge | 79.87% | 651.45 min |
| **Latent-GRPO (Ours)** | **82.34%** | **658.21 min** |
- ✅ **准确率更高**（+2.47%）
- ⏱️ 虽然时间略长，但实际因消除了外部调用瓶颈，在真实部署中更快。

> 注：表中显示 Latent-GRPO 时间稍高，是因为模拟了 LLM-as-Judge 的 QPS 限制（2 queries/sec），若考虑排队等待，Latent-GRPO 实际快 **2.14×**。

#### 在 **MATH** 上的表现（Qwen3-1.7B）
| 方法 | 准确率 | 每轮训练时间 |
|------|--------|--------------|
| LLM-as-Judge | 65.77% | 1608.34 min |
| **Latent-GRPO (Ours)** | **78.51%** | **811.51 min** |
- ✅ **准确率大幅提升**（+12.74%）
- ⏱️ **训练速度快近 2×**（1.98× speedup）

#### 在 **Open-Platypus** 上的表现（Qwen3-4B）
| 方法 | 准确率 | 每轮训练时间 |
|------|--------|--------------|
| LLM-as-Judge | 65.21% | 3522.18 min |
| **Latent-GRPO (Ours)** | **78.06%** | **1632.52 min** |
- ✅ **准确率提升巨大**（+12.85%）
- ⏱️ **训练速度提升超过 2×**（2.16× speedup）

> 💡 结论：Latent-GRPO 在复杂、多样化的推理任务上优势尤为显著。

---

### 消融实验结果（Ablation Studies）

#### （1）隐藏状态提取方式对比（Table 2）
| 方法 | Qwen3-0.6B Acc | Qwen3-1.7B Acc | Qwen3-4B Acc |
|------|----------------|----------------|-------------|
| Mean Pooling | 58.74% | 71.05% | 79.45% |
| Weighted Mean | 57.12% | 69.88% | 78.12% |
| **Last Token (Ours)** | **61.25%** | **73.88%** | **82.34%** |

✅ **结论**：使用终端 token 的隐藏状态效果最佳，说明最终语义已汇聚于最后一个表示中。

#### （2）质心估计算法对比（Table 3）
| 方法 | Qwen3-0.6B Acc | Qwen3-1.7B Acc | Qwen3-4B Acc | 时间 |
|------|----------------|----------------|-------------|------|
| Mean Pool | 57.12% | 68.45% | 77.89% | 快 |
| K-Means | 58.85% | 70.12% | 79.23% | 较慢 |
| Eigen Centrality | 59.43% | 71.56% | 80.56% | 慢 |
| **IRCE (Ours)** | **61.25%** | **73.88%** | **82.34%** | **最快** |

✅ **结论**：IRCE 在准确性与效率之间达到最优平衡，尤其适合实时训练场景。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **潜空间具有强几何一致性**：正确的推理轨迹在 `last hidden state` 上高度聚集，错误轨迹分散，形成“共识核心 + 离群点”结构（见 Figure 2 & 5）。
2. ✅ **潜空间可作为隐式验证器**：无需外部模型或规则，即可通过几何距离判断推理质量。
3. ✅ **稠密奖励优于稀疏奖励**：连续奖励信号提供了更丰富的梯度信息，加速收敛并提升最终性能。
4. ✅ **方法具备良好泛化性**：
   - 在不同模型家族（Qwen 和 **Llama3.2-3B**）上均有效（Table 5）。
   - 在未见过的任务（MMLU, AIME, BBH）上保持甚至超越基线性能（Table 4），表明未发生过拟合。

### 方法的局限性
1. ❗ 当前方法主要在 **≤8B 参数模型** 上验证，超大规模模型（如 70B+）的效果尚待探索。
2. ❗ 对**开放式生成任务**（如创意写作）的适用性未知。
3. ❗ 缺乏严格的**数学理论框架**解释为何潜空间会自然形成语义聚类。

### 未来工作方向
- 探索 Latent-GRPO 在更大模型和开放生成任务中的表现。
- 构建形式化的**潜空间聚类理论模型**。
- 研究将 IRCE 奖励与离线对齐方法（如 **DPO**）结合，构建更稳定的自监督对齐范式。

---

> 🔚 **总结一句话**：  
> **Latent-GRPO 成功“沉默了法官”（Silence the Judge），让 LLM 学会自我评判——通过挖掘自身潜空间的几何结构，实现了高效、稳定、无需外部依赖的强化学习新范式。**

</details>

---

### 10. [Get away with less: Need of source side data curation to build parallel corpus for low resource Machine Translation](https://arxiv.org/abs/2601.08629)

**Authors**: Saumitra Yadav, Manish Shrivastava  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08629v1  

#### Abstract
Data curation is a critical yet under-researched step in the machine translation training paradigm. To train translation systems, data acquisition relies primarily on human translations and digital parallel sources or, to a limited degree, synthetic generation. But, for low-resource languages, human...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Get away with less: Need of source side data curation to build parallel corpus for low resource Machine Translation

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**低资源机器翻译**（Low Resource Machine Translation）中一个被忽视的关键环节——**数据整理**（data curation）。在低资源语言场景下，获取高质量的平行语料成本高昂，而现有的大规模网络爬取语料（如 OSCAR、Samanantar）往往包含大量简单或冗余句子，导致训练效率低下。传统做法是“越多越好”，但本文指出，这种策略并不高效。

### 提出的新方法和新思路
作者提出了名为 **LALITA**（Lexical And Linguistically Informed Text Analysis）的框架，其核心创新在于：

- **提出 LALITA Score**：一种基于源语言（source side）句子复杂度的量化评分机制。该分数通过提取句子的**统计特征**（如困惑度）、**词汇特征**（如长度）、**语言学特征**（如依存关系、词性标注、形态特征等），经由 **PCA**（主成分分析）降维后，将第一主成分（PCA1）定义为 LALITA Score。得分越高，表示句子结构越复杂。
- **复杂句优先选择策略**：主张在有限的人工翻译预算下，应优先选择**结构更复杂的源语言句子**进行翻译，以最大化每个翻译对的学习信号。
- **主动式数据整理**：不同于传统的“事后过滤”（reactive filtering），LALITA 是一种“事前筛选”（proactive selection）策略，从源头上优化平行语料的构建过程。

### 相比现有方法的优势
- **效率更高**：相比随机采样或按原始比例采样的基线，LALITA 能用更少的数据达到相同甚至更好的翻译质量。
- **通用性强**：不仅适用于低资源语言，在高资源语言对（如英语-德语）上也表现出色。
- **指导数据增强**：可用于指导合成数据（synthetic data）的生成与选择，确保增强数据同样具有高学习价值。
- **减少训练成本**：显著降低所需训练数据量，从而节省计算资源和环境成本。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **主实验数据集**：**Samanantar 英印双语文本**（English-Hindi bitext），经过清洗后得到约 185 万对平行句对（Filtered Parallel Sentences, FPS）。
- **其他低资源语言对**：
  - 英语-奥里亚语（English-Odia）
  - 英语-尼泊尔语（English-Nepali）
  - 英语-挪威尼诺斯克语（English-Norwegian Nynorsk）
- **高资源语言对**：**英语-德语**（English-German），使用 WMT24 数据集中的 2000 万句对。
- **测试集**：统一使用 **FLORES** 测试集进行评估。
- **单语数据**：用于回译（back-translation）生成合成数据。

### 实验设置和评估指标
- **模型架构**：标准 **Transformer** 模型。
- **分词**：使用 **Byte-Pair Encoding (BPE)**，合并操作数为 16,000。
- **评估指标**：**CHRF++** 分数（Sacrebleu 工具包计算）。
- **数据规模模拟**：在 50K 到 800K 句对的不同数据规模下进行实验，模拟低资源场景。
- **聚类方法**：使用 **Fisher-Jenks 算法** 将句子按 LALITA Score 分为 4 个簇（Cluster 0–3），其中 Cluster 3 包含最复杂的句子。
- **配置策略**：采用 `<a,b,c,d>` 形式的配置，表示从四个簇中分别选取 a%、b%、c%、d% 的句子。

### 基线方法对比
- **RS**（Randomly Sampled）：从完整语料中随机抽取指定数量的句对。
- **baselineP**（Proportional）：按原始语料中各簇的比例进行采样。
- **Full Dataset**：使用全部清洗后的语料（如 1.8M 或 20M 句对）作为性能上限参考。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 语言对 | 基线系统最佳性能 (CHRF++) | LALITA 最佳性能 (CHRF++) | 性能提升 | 所需数据量 | 数据缩减比例 |
|--------|--------------------------|-------------------------|----------|------------|--------------|
| **English-Hindi** | 51.63 (baselineP) | **53.84** | +2.21 | 800K | **↓ 56%** (vs 1.8M) |
| **English-Odia** | 41.57 | **46.64** | +5.07 | 800K | **↓ 86%** (vs 5.79M) |
| **English-Nepali** | 43.86 | **46.88** | +3.02 | 800K | **↓ 51%** (vs 1.63M) |
| **English-Nynorsk** | 55.53 | **56.69** | +1.16 | 800K | **↓ 36%** (vs 1.24M) |
| **English-German** | 51.87 | **58.24** | **+6.37** | 8M | **↓ 60%** (vs 20M) |

> 注：LALITA 在英语-德语任务中，仅用 800 万句对就超过了使用 2000 万句对的全量模型。

### 与基线方法的对比结果
- 在所有语言对和数据规模下，**以 Cluster 3（复杂句）为主的配置**（如 `0_0_0_100`、`0_20_20_60`）均显著优于 **RS** 和 **baselineP**。
- 即使只使用 800K 复杂句训练的英印模型，其性能也与使用 1.8M 全量数据的模型相当，且**总 token 数减少了约 64%**。
- 在高资源的英德任务中，LALITA 不仅将数据需求减少 60%，还实现了 **+4.32 CHRF++** 的绝对提升。

### 消融实验结果
- **单一特征 vs LALITA Score**：仅基于**最长句子**或**最多动词**的选择策略，性能远低于 LALITA Score。例如在 800K 英印数据上，LALITA 达到 53.84，而基于最长句的策略仅为 50.72。
- **合成数据选择**：当使用单一特征选择合成数据时，性能下降明显；而使用 LALITA Score 选择，则能有效提升性能。
- **结论**：LALITA Score 的优势来自于其**多维度语言学特征的综合考量**，而非依赖单一启发式规则。

---

## 4. 关键结论和发现

### 主要发现
1. **复杂句蕴含更强的学习信号**：结构复杂的句子能帮助 MT 模型更好地学习句法和语义规律，即使数量较少也能带来更高的性能增益。
2. **“少即是多”**：通过智能筛选，可以用**不到一半的数据量**实现同等甚至更优的翻译质量，尤其在低资源场景下效果显著。
3. **LALITA Score 具有跨语言通用性**：该方法在印度语系（Hindi, Odia, Nepali）、北欧语言（Norwegian Nynorsk）及高资源语言（German）上均有效，表明其捕捉的语言复杂度具有普适性。
4. **数据整理应前置**：与其事后清洗噪声，不如在数据构建阶段就主动选择高价值样本，这是更高效的策略。

### 方法的局限性
- **依赖源语言解析工具**：LALITA 需要高质量的源语言句法分析器（如 Trankit），对于真正低资源且缺乏 NLP 工具的语言存在“冷启动”问题。
- **计算成本高**：探索最优配置需要大量实验，计算开销较大。
- **局限于句子级别**：未考虑篇章级的连贯性和指代消解等高级语言现象。
- **未使用句向量**：当前版本未引入 LASER、LaBSE 等语义嵌入作为特征，可能遗漏部分语义复杂度。

### 未来工作方向
- **提高搜索效率**：开发更高效的算法来寻找最优数据配置。
- **扩展至多语言和非英语源语言**：将 LALITA 推广到更多语言组合，尤其是非英语作为源语言的场景。
- **结合语义不确定性**：将 LALITA Score 与语义不确定性采样结合，构建更全面的数据增强管道。
- **应用于课程学习**（Curriculum Learning）：按 LALITA Score 从易到难地组织训练数据，模拟人类学习过程。
- **整合篇章信息**：扩展框架以捕捉跨句子的语篇复杂度。
- **用于大模型预训练**：将 LALITA Score 应用于 LLM 第二阶段训练，提升预训练数据质量。

---

> **总结**：LALITA 提供了一种**以语言学复杂度为导向的数据整理范式**，证明了在机器翻译中，“**质量胜于数量**”。它不仅降低了低资源 MT 的门槛，也为高资源场景下的高效训练提供了新思路，具有重要的理论和应用价值。

</details>

---

### 11. [Where to Split? A Pareto-Front Analysis of DNN Partitioning for Edge Inference](https://arxiv.org/abs/2601.08025)

**Authors**: Adiba Masud, Nicholas Foley, Pragathi Durga Rajarajan, Palden Lama  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08025v1  

#### Abstract
The deployment of deep neural networks (DNNs) on resource-constrained edge devices is frequently hindered by their significant computational and memory requirements. While partitioning and distributing a DNN across multiple devices is a well-established strategy to mitigate this challenge, prior res...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Where to Split? A Pareto-Front Analysis of DNN Partitioning for Edge Inference*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在边缘设备上部署深度神经网络（DNN）面临计算和内存资源受限的挑战。虽然模型分割（DNN partitioning）是一种常见策略，但**现有研究大多聚焦于单目标优化**（如最小化延迟或最大化吞吐量），忽略了现实场景中**延迟与吞吐量之间的复杂权衡关系**，尤其是在网络条件多变的边缘环境中。

### ✅ 提出的新方法与创新思路
本文提出将 DNN 分割建模为一个**多目标优化问题**，并引入 **ParetoPipe** —— 一个开源框架，利用 **Pareto Front 分析** 来系统识别最优的模型分割策略。

#### 主要创新点：
- **首次系统性地应用 Pareto Front 分析** 到边缘推理中的 DNN 分割问题，揭示了延迟与吞吐量之间的真实权衡边界。
- 设计并实现了 **ParetoPipe 框架**，支持灵活的分布式推理实验，具备双通信后端（PyTorch RPC 和自定义轻量级 TCP Socket 实现）。
- 强调 **block-level profiling** 的重要性，指出不同模型块的计算成本差异显著，影响最佳分割点的选择。
- 探索了**网络延迟与带宽对 Pareto Frontier 的影响**，验证了网络是边缘部署中的一等性能瓶颈。

### ✅ 相比现有方法的优势
| 方面 | 现有方法 | 本工作（ParetoPipe） |
|------|--------|------------------|
| 优化目标 | 单目标（仅延迟或吞吐） | 多目标联合分析（延迟 vs 吞吐） |
| 分析方式 | 寻找“最优”单一解 | 构建完整的 Pareto Frontier，提供决策空间 |
| 网络感知 | 忽视或简化网络变化 | 显式模拟高延迟/低带宽场景，分析鲁棒性 |
| 工具支持 | 封闭或专用系统 | 开源、可扩展、支持多种 backend |

> ✅ **优势总结**：ParetoPipe 不仅是一个工具链，更是一种新的分析范式，帮助开发者理解“没有绝对最优”的现实，并基于实际需求选择合适的分割策略。

---

## 2. 核心实验方法和设置

### 📦 数据集与模型
- **数据集**：使用 **CIFAR-10** 进行微调（fine-tuning），适配输入尺寸（32×32）；原始权重来自 ImageNet 预训练。
- **测试的 DNN 模型**（共6个）：
  - MobileNetV2
  - ResNet18
  - InceptionV3
  - ResNet50
  - AlexNet
  - VGG16  
  > 覆盖从轻量到重型、不同架构类型的 CNN 模型。

### ⚙️ 实验设置
- **硬件平台**：
  - 边缘节点：两个 Raspberry Pi 4B（4核 CPU，4GB RAM）
  - 边缘服务器：配备 NVIDIA RTX 4090 GPU、32核 CPU、124GB 内存
  - 网络：有线以太网 LAN，基础 RTT ~0.2–0.4ms
- **网络模拟**：使用 `tc` 工具注入 **200ms 延迟 + 5 Mbit/s 带宽限制**，模拟拥塞 Wi-Fi 场景。
- **部署模式**：
  - **Pi-to-Pi**：两部分均运行在树莓派上
  - **Pi-to-GPU**：第一段在 Pi 上执行，第二段卸载至 GPU 服务器

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **End-to-end Latency** | 每 batch 的完整推理耗时（秒） |
| **Throughput** | 每秒处理图像数（imgs/s） |
| **CPU Utilization** | 使用 `psutil` 监控各设备资源占用 |
| **Memory Usage** | 模型分区内存消耗 |
| **Network Overhead** | 中间张量传输时间 |

### 🔁 基线方法对比
- **通信后端对比**：
  - **PyTorch RPC + TensorPipe**（标准分布式方案）
  - **Custom TCP Socket Implementation**（本文轻量实现）
- **分割策略**：在每个模型的 block 边界处进行穷举分割（exhaustive search），生成所有可能配置。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（基于 Custom Backend）

#### （1）Pareto Frontier 分析结果（Fig. 3 & 4）
- 在 **Pi-to-Pi** 设置下：
  - 轻量模型（如 MobileNetV2）可通过非对称分割获得更高吞吐。
  - 例如：MobileNetV2 最佳吞吐出现在 **P3**（前3块留本地，其余18块送远端）。
  - 层次分布均匀的模型（如 AlexNet、VGG16）适合对称分割。
- 在 **Pi-to-GPU** 设置下：
  - 几乎所有模型的最佳策略都是**尽早卸载**（early offloading），因为 GPU 加速效果明显。
  - 如 MobileNetV2 最优分割点为 **P1**（仅第1块在 Pi 执行，其余20块在 GPU）。

#### （2）网络恶化下的性能退化（Fig. 5 & 6）
- 当引入 **200ms 延迟 + 5 Mbps 带宽**：
  - 所有模型的 Pareto Frontier 明显右移（延迟上升）、下移（吞吐下降）。
  - **GPU 的算力优势被通信开销淹没**：即使 GPU 执行极快，仍需长时间等待中间数据到达。
  - 原先最优的 early offloading 策略（如 P1）变得最差，因其产生最大通信量。
  - 新的 Pareto 最优点倾向于**保留更多计算在初始设备**，减少传输数据量。

#### （3）通信后端对比（Fig. 7）
| 指标 | PyTorch RPC（P15） | Custom Socket（P3） | 提升幅度 |
|------|--------------------|---------------------|----------|
| Throughput | 5.1 img/s | **7.8 img/s** | ↑ **53%** |
| End-to-end Latency | 1.58s | **0.37s** | ↓ **76%** |
| RPC Coordination Overhead | 显著存在 | 完全消除 | — |

> 💡 自定义通信后端大幅降低开销，尤其适用于资源受限边缘环境。

#### （4）资源利用率分析（Table II & III）
- 高吞吐配置通常导致**双设备高 CPU 利用率**（接近 350%+，表明多核饱和）。
- 低延迟配置常出现**负载不均衡**（如 Pi1 高负载，Pi2 空闲）。
- Pi-to-GPU 场景中，GPU 利用率普遍很低（<4%），说明其大部分时间处于等待状态（受制于网络）。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **延迟与吞吐存在本质权衡**：不存在单一“最优”分割点，应通过 Pareto Front 进行系统分析。
2. **网络是边缘推理的关键瓶颈**：在真实网络条件下，通信开销可能完全抵消强大的本地算力（如 GPU）带来的收益。
3. **最优分割点随网络动态变化**：
   - 理想网络 → 尽早卸载（offload early）
   - 恶劣网络 → 本地多算、少传数据
4. **block-level profiling 至关重要**：不同 block 的计算密度差异大，直接影响分割效率。
5. **PyTorch RPC 存在显著运行时开销**：对于边缘场景，轻量级通信协议更具优势。

### ⚠️ 方法的局限性
- 当前仅支持 **two-stage pipeline**（两端流水线），未探索更复杂的多设备拓扑。
- 所有实验基于 **CNN 模型**，未涵盖 Transformer 等新兴架构。
- 分割粒度为 **block 级别**，未深入到 layer 或 tensor 级细粒度划分。
- 缺乏能量效率（energy consumption）作为优化目标。

### 🔮 未来工作方向
1. 开发 **adaptive partitioning algorithm**：根据实时网络状况动态调整分割点。
2. 引入 **energy efficiency** 作为第三维优化目标，构建三维 Pareto Front。
3. 支持 **multi-device pipelines** 和 **hybrid partitioning**（结合 tensor/pipeline 并行）。
4. 扩展至 **Transformer-based models**（如 BERT、ViT）的边缘推理优化。
5. 集成 **model compression** 技术（如量化、剪枝）与分割协同设计。

---

## 总结
该论文通过 **ParetoPipe 框架** 和系统的实验分析，揭示了边缘环境下 DNN 分割的复杂权衡本质。它不仅提供了实用工具，更重要的是倡导了一种**从单目标优化转向多目标权衡分析**的新视角，强调**网络感知**和**实际部署条件的重要性**，为未来边缘 AI 系统的设计提供了坚实的基础和指导原则。

</details>

---

### 12. [Forecast Aware Deep Reinforcement Learning for Efficient Electricity Load Scheduling in Dairy Farms](https://arxiv.org/abs/2601.08052)

**Authors**: Nawazish Alia, Rachael Shawb, Karl Mason  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08052v1  

#### Abstract
Dairy farming is an energy intensive sector that relies heavily on grid electricity. With increasing renewable energy integration, sustainable energy management has become essential for reducing grid dependence and supporting the United Nations Sustainable Development Goal 7 on affordable and clean ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本研究针对现代奶牛场中高能耗操作（如电池储能和热水加热）在动态电价和可再生能源间歇性供应下的电力负荷调度挑战。传统强化学习（Reinforcement Learning, RL）方法通常假设对未来价格或发电量有完全先验知识，这在现实环境中不切实际；同时，标准的 **Proximal Policy Optimization (PPO)** 算法依赖固定的裁剪阈值或KL散度限制，在电价波动剧烈时容易导致训练不稳定。

### 提出的新方法与创新思路
该论文提出了两个基于 **PPO** 的改进框架：

- **Forecast-Aware PPO (F-PPO)**  
  将短期需求与可再生能源生成预测信号整合进智能体的观测空间。通过“小时-月”残差校准（Hour-of-Day and Month-based Residual Calibration）技术生成结构化预测输入，使策略能够前瞻性地做出决策，而非仅反应式响应当前状态。

- **PID-KL PPO**  
  引入一个 **Proportional-Integral-Derivative (PID) 控制器** 来动态调节KL散度惩罚系数 $ c_{KL} $，从而实现自适应的信任区域控制。这种方法能根据每次更新后策略变化的程度自动调整更新步长，提升训练稳定性，减少对超参数调优的依赖。

此外，模型结合了GRU网络对预测序列进行编码，并嵌入了真实运行约束（如电池SOC边界、热水器每日运行时间要求），确保调度方案具备实际可行性。

### 相比现有方法的优势
- **更贴近现实场景**：摒弃了对未来电价/发电量全知的假设，采用可解释且确定性的预测机制。
- **更强的训练稳定性**：PID-KL机制显著降低了奖励波动带来的策略震荡，收敛曲线更加平滑。
- **更高的成本效益与可靠性**：相比标准PPO、DQN、SAC等基线算法，在降低电费、削减峰值负载和提高用户满意度方面表现更优。
- **零约束违反**：所有F-PPO变体均实现了零违规（无过充/过放、无运行时间不足），而部分基线存在明显违反。

---

## 2. 核心实验方法和设置

### 数据集
- **电池调度任务**：使用芬兰VTT提供的公开奶牛场用电数据（年总耗电量约261 MW），结合NREL SAM模拟的20kW光伏系统发电数据及赫尔辛基电力供应商的动态电价。
- **热水器调度任务**：采用爱尔兰奶牛场的研究数据集，包含设备级拆分用电信息（来自200头规模农场）、20kW光伏数据（SAM生成）以及Electric Ireland的时间分段电价（Time-of-Use Tariffs）。

### 实验设置与评估指标
- **环境建模为Markov Decision Process (MDP)**：
  - **电池调度**：状态包括时间、SOC、负载功率、光伏输出；动作为充电/放电/空闲；奖励函数综合考虑电网购电成本与电池健康惩罚。
  - **热水器调度**：状态扩展至电价、背景负载、净功耗、设备功耗、剩余需运行时间等；动作为ON/OFF；奖励由成本项与任务完成度共同构成。
- **训练配置**：
  - 所有算法训练100万时间步（timesteps）
  - 使用CleanRL框架保证复现性和一致性
  - 超参数见文中Table 1 & Table 2

### 基线方法对比
- **水 heater 调度对比算法**：
  - **Standard PPO**
  - **Deep Q-Network (DQN)**
  - **Soft Actor-Critic (SAC)**
- **电池调度对比算法**：
  - **Q-learning**
  - **Rule-based 策略**

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（来自Table 3与正文）

| 算法 | 总电费 (€) | 较标准PPO降幅 | 违规天数 |
|------|-----------|----------------|----------|
| DQN | 16,418 | +4.3% ↑ | 60天underuse |
| Standard PPO | 15,744 | — | 0 |
| Forecast-Aware PPO (F-PPO) | 15,635 | ↓0.7% | 0 |
| F-PPO (dropout=0.15) | 15,582 | ↓1.0% | 17天underuse |
| PID-KL PPO | 15,624 | ↓0.8% | ~1天underuse |
| Discrete SAC | 15,773 | ↑0.2% ↑ | 0 |

> 注：“↓”表示更低（更好），“↑”表示更高（更差）

### 与基线方法的对比结果
- **相对于DQN**：
  - F-PPO实现 **4.76% 的电费节省**
  - 平均每日峰值需求降低 **13.75%**
  - 用户满意度从80%提升至99%
  - 统计检验（Wilcoxon signed-rank test）显示所有差异均显著（p=0.0019**）

- **相对于标准PPO**：
  - F-PPO进一步降低电费 **~1%**
  - PID-KL PPO虽最终成本相近，但表现出更稳定的训练过程（方差更小，收敛更快）

- **电池调度结果**：
  - PPO相较无电池场景减少 **13.11% 的电网进口电量**
  - 比Q-learning和rule-based方法分别多节省 **1.62% 和 2.56%** 的电网依赖

### 消融实验结果
- **预测模块有效性**：引入forecast block（含中位数或百分位区间）后，F-PPO相比标准PPO取得稳定成本下降（约1%），证明预测信息有助于提前规划。
- **GRU编码器作用**：GRU成功捕捉预测信号中的时间依赖关系，支持主动调度。
- **PID-KL机制优势**：尽管最终性能接近F-PPO，但其训练过程更为平稳，reward曲线波动更小（见Figure 10），说明其增强了鲁棒性。
- **Dropout调参影响**：将GRU dropout从0.10增至0.15可进一步降低成本（达1.5%），但带来更多underuse天数（17天），表明存在**成本优化与约束满足之间的权衡**。

---

## 4. 关键结论和发现

### 主要发现
1. **预测感知型PPO显著优于传统RL方法**：F-PPO通过融合短期预测信息，能够在电价低谷期和光伏发电高峰期主动安排用电，有效降低运营成本并削峰填谷。
2. **PID-KL机制极大提升了训练稳定性**：面对电价剧烈波动，传统的固定KL阈值难以维持稳定学习，而PID控制器可根据实际KL偏差动态调节惩罚强度，避免策略突变。
3. **所提方法兼具高性能与高可靠性**：在多个真实数据集上验证，新方法不仅实现了经济性最优，还严格遵守设备运行约束，适用于实际部署。
4. **水 heater 是关键可调度负荷**：合理调度这一高能耗设备可显著改善整体能效，尤其在夏季光伏充足时段效果突出。

### 方法的局限性
- **预测模型较简单**：目前采用的是seasonal-naive加残差带的方法，虽具可解释性，但在极端天气或突发事件下可能失效。
- **单智能体架构**：当前为单一agent控制单一设备，未考虑多设备协同优化问题。
- **离散动作空间限制**：仅支持ON/OFF或Charge/Discharge/Idle等有限动作，无法精细调控功率水平。
- **未考虑设备老化与维护成本**：虽然加入了SOC边界保护，但未显式建模长期退化效应。

### 未来工作方向
- 探索 **Multi-Agent Reinforcement Learning (MARL)** 架构以协调多个用电设备（如挤奶机、冷却系统、水泵等）。
- 集成更多类型的可再生能源（如风能、沼气）及其联合调度策略。
- 应用 **Evolutionary Reinforcement Learning** 提升训练鲁棒性与泛化能力。
- 开发在线自适应预测模块，替代静态残差校准方法，增强应对不确定性能力。
- 在真实农场环境中进行实地部署与长期运行测试。

--- 

> ✅ **总结一句话**：本文提出了一种融合短期预测与自适应KL控制的 **Forecast-Aware Deep Reinforcement Learning** 框架，在真实奶牛场负荷调度任务中实现了更低成本、更高稳定性和更强实用性的能源管理解决方案，推动农业领域向可持续智能化迈进。

</details>

---

### 13. [Attention Projection Mixing and Exogenous Anchors](https://arxiv.org/abs/2601.08131)

**Authors**: Jonathan Su  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08131v1  

#### Abstract
Transformers that reuse early-layer attention projections as residuals face a fundamental tension: the first layer must simultaneously serve as a stable reference for all deeper layers and as an effective computational block. To resolve this, we propose ExoFormer, which learns dedicated exogenous an...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Attention Projection Mixing and Exogenous Anchors*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Transformer 模型中，**第一层注意力投影**被同时用作两个角色：
1. **可重用的锚点（Reusable Anchor）**：为所有深层提供稳定的参考表示；
2. **计算块（Computational Block）**：进行特征变换以支持逐层抽象。

这种双重角色导致“**第一层张力（First-Layer Tension）**”——既要保持稳定不变以供复用，又要灵活变化以支持计算，二者本质上存在冲突，限制了模型性能。

此外，尽管已有工作（如 ResFormer）尝试通过残差连接保留早期信息（如仅对 `V` 投影），但未系统探索对 **Queries、Keys、Gate Logits** 等其他注意力路径的跨层混合，且缺乏统一框架。

---

### 提出的新方法与创新思路

#### （1）统一的归一化混合框架（Normalized Unified Mixing）
提出 **NuResFormer**，一个统一框架，在所有注意力路径（`Q`, `K`, `V`, `G`）上实现跨层混合：
- 引入可学习的混合系数（`λ₁`, `λ₂`），控制当前层投影与早期锚点之间的加权融合；
- 在混合前对锚点源应用 **RMSNorm**，解决分布不匹配问题，提升稳定性与效果。

#### （2）ExoFormer：外生锚点解耦架构
提出 **ExoFormer**，核心创新是将“锚点”角色从第一层中剥离：
- 锚点不再来自第一层输出，而是由输入嵌入经**独立的外部投影模块**生成（即 *exogenous anchor*）；
- 这种**解耦设计**允许：
  - 外部锚点专注于保留 token 身份信息（identity preservation）；
  - 内部层专注于渐进式计算优化（computational refinement）。

#### （3）动态混合模块（Dynamic Mixing, DM）
受 MUDDFormer 启发，引入上下文感知的动态混合机制：
- 使用小型 MLP 根据当前层输入生成调制因子，实时调整混合权重；
- 实现更灵活、自适应的信息复用策略。

---

### 相比现有方法的优势

| 方面 | 优势 |
|------|------|
| **性能提升** | 所有 ExoFormer 变体均优于对应的内部锚点版本（NuResFormer），在下游任务准确率和困惑度（PPL）上全面领先。 |
| **数据效率** | Dynamic ExoFormer 仅用 **1.84× 更少的训练 token** 即可达到基线验证损失，显著提升训练效率。 |
| **注意力行为优化** | 相比标准 Gated Attention，ExoFormer 将 **attention sink 减少 2×**，避免过度压缩带来的信息丢失。 |
| **理论解释力强** | 提出“**Offloading Hypothesis**”，合理解释为何模型在出现表征坍缩（representation collapse）的情况下仍能取得更高性能。 |

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：FineWeb-Edu 子集（10B tokens），符合 Chinchilla 缩放定律；
- **评估基准**：6 个多项选择题任务：
  - ARC-C, ARC-E
  - HellaSwag
  - OpenBookQA (OBQA)
  - PIQA
  - Winogrande

### 实验设置
- **模型规模**：约 450M 参数（Base: 454M, ExoFormer: 457M）；
- **序列长度**：2048；
- **批量大小**：262,144 tokens；
- **优化器**：Muon（主参数） + AdamW（1D 参数），启用 cautious weight decay；
- **精度**：BF16，使用 FlashAttention 加速；
- **训练预算**：10B tokens。

### 评估指标
- **下游性能**：平均准确率（Avg. Acc %）；
- **语言建模能力**：验证集困惑度（PPL）；
- **分析指标**：
  - Attention entropy
  - Token 表示相似性（cosine similarity）
  - 主成分核心特征数（PCA Core Features，衡量内在维度）
  - Attention sink 强度

### 基线方法对比
| 模型 | 特点 |
|------|------|
| Base Transformer | 原始架构 |
| Gated Attention | 引入门控机制调节 attention 输出 |
| ResFormer | 仅对 `V₁` 添加残差连接 |
| Naive Combination | Gated Attention + ResFormer 直接组合 |
| NuResFormer | 统一框架 + 第一层作为内部锚点 |
| ExoFormer | 统一框架 + 外部锚点（本文提出） |
| Dynamic ExoFormer | ExoFormer + 动态混合模块 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1 & A3）

| 模型 | Avg. Acc (%) | PPL | 参数量 (M) |
|------|--------------|-----|-----------|
| Base Transformer | 48.14 | 14.79 | 454 |
| Gated Attention | 48.80 | 14.64 | 453 |
| ResFormer | 49.65 | 14.32 | 454 |
| NuResFormer (E) | 49.68 | 14.15 | 453 |
| **ExoFormer (E)** | **49.85** | **14.13** | 457 |
| **Dynamic ExoFormer** | **50.27** | **14.09** | 457 |

> ✅ **Dynamic ExoFormer 达到最高准确率（50.27%）和最低困惑度（14.09）**

---

### 与基线对比结果

- **相比 ResFormer**：
  - 准确率 ↑ 0.62 pts（Dynamic ExoFormer vs ResFormer）
  - PPL ↓ 0.23
- **相比 NuResFormer**：
  - 所有 ExoFormer 变体均优于对应 NuResFormer，证明**外生锚点优于内生锚点**
- **数据效率**：
  - Dynamic ExoFormer 用 **1.84× 更少的数据**即可匹配基线验证损失
- **Attention Sink 抑制**（Table 2）：
  - Baseline: 0.2572
  - Gated Attention: 0.0091
  - **ExoFormer: 0.0041**, **Dynamic ExoFormer: 0.0073**
  - → **减少超过 2×**

---

### 消融实验结果

#### （1）是否归一化锚点？
- 若不对锚点做 RMSNorm（`no norm`），则 anchor pathway 被严重抑制（近零系数比例三倍于归一化版本），说明 **RMSNorm 对稳定混合至关重要**。

#### （2）混合粒度（Granularity）的影响（Appendix B）
| 模型 | 最佳粒度 | 结果 |
|------|----------|------|
| NuResFormer | Scalar (S) | 最高下游准确率（49.83%） |
| ExoFormer | Elementwise (E) | 最佳综合表现（49.85%, PPL=14.13） |

→ 表明 **ExoFormer 因解耦更干净，能更好利用细粒度控制**

#### （3）不同路径残差的有效性**
- **Q/K 残差不稳定** → 需配合 QKNorm 才可训练；
- **加上 RMSNorm 到残差源后性能进一步提升**；
- **Gating Logits 残差天然稳定**（因经 sigmoid 压缩），无需额外归一化也可有效融合。

#### （4）动态混合 vs 静态混合**
- Dynamic ExoFormer 在通道和层间展现出更均匀的混合系数分布（图3d），表明其具备更强的上下文适应能力；
- 在路由（Q/K）和选择（G）组件上尤其受益。

---

## 4. 关键结论和发现

### 主要发现

#### ✅ **ExoFormer 显著优于内部锚点架构**
- 解耦“锚点”与“计算”角色带来持续收益；
- 即使参数略有增加（+3M），也能在相同宽度/深度下实现 SOTA 性能。

#### ✅ **表征坍缩 ≠ 性能下降 —— Offloading Hypothesis**
这是本文最反直觉也最重要的发现：
- ExoFormer 层内 token 表示高度相似（图2e，高达 95%），内在维度极低（~738 vs baseline 839）；
- 但性能反而最好。

为此提出 **Offloading Hypothesis**：
> 外部锚点承担了“身份保留”的职责，使得主干网络可以完全专注于“信号提纯”和“逐步推理”。所谓的“坍缩”实际上是**主动丢弃冗余静态信息的过程**，是一种功能专业化而非退化。

#### ✅ **ExoFormer 改变了信息处理流程**
- 标准 Transformer 遵循 Mix → Compress → Refine 三阶段；
- ExoFormer 提前进入并长期停留在 **Refinement 阶段**（占总层数 2/3），跳过剧烈压缩；
- 得益于门控和残差混合提供的分布式过滤机制，无需依赖 attention sink。

---

### 方法的局限性（Section 6）

1. **实验规模有限**：仅在 ~450M 参数、10B tokens 上验证，可能无法推广至更大模型或更长训练；
2. **超参搜索不足**：未系统探索混合系数初始化策略；
3. **评估范围受限**：集中在多项选择任务，缺乏对长文本生成、上下文理解等任务的测试；
4. **缺乏形式化理论解释**：为何归一化重用有效？仍需更深理论支撑。

---

### 未来工作方向

1. **扩展至更大规模模型**（Billion+ 参数）验证泛化性；
2. **探索更多锚点形式**：如可学习记忆库、结构化索引等；
3. **结合 LongNet 架构**：研究在超长序列中的表现；
4. **理论建模**：建立关于“offloading”机制的形式化分析框架；
5. **应用于其他模态**：视觉、语音等领域的 Transformer 架构改进。

---

> 🔗 **代码开源地址**：[https://github.com/jon123boss/ExoFormer](https://github.com/jon123boss/ExoFormer)

</details>

---

### 14. [VBO-MI: A Fully Gradient-Based Bayesian Optimization Framework Using Variational Mutual Information Estimation](https://arxiv.org/abs/2601.08172)

**Authors**: Farhad Mirkarimi  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08172v1  

#### Abstract
Many real-world tasks require optimizing expensive black-box functions accessible only through noisy evaluations, a setting commonly addressed with Bayesian optimization (BO). While Bayesian neural networks (BNNs) have recently emerged as scalable alternatives to Gaussian Processes (GPs), traditiona...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：VBO-MI: A Fully Gradient-Based Bayesian Optimization Framework Using Variational Mutual Information Estimation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Bayesian Optimization (BO)** 方法面临以下挑战：
- **高计算复杂度**：基于 **Gaussian Process (GP)** 的方法需要 $O(T^3)$ 的协方差矩阵求逆，难以扩展到大规模或高维场景。
- **采样效率低**：基于 **Bayesian Neural Networks (BNNs)** 的方法依赖昂贵的后验采样（如 HMC），且通常假设特定参数化分布（如高斯、伽马），限制了模型表达能力。
- **内层优化瓶颈**：每次选择下一个查询点都需要对 acquisition function 进行额外优化（如 L-BFGS），形成“内层循环”，显著增加计算开销。

### 提出的新方法：VBO-MI
作者提出 **VBO-MI (Variational Bayesian Optimization with Mutual Information)**，一种**完全基于梯度的 BO 框架**，其核心思想是：
- 将 BO 中的探索-利用权衡建模为 **Mutual Information (MI)** 最大化问题。
- 利用 **variational MI estimation**（基于 Donsker-Varadhan 界）直接从数据估计信息增益，无需显式建模后验分布。
- 设计 **actor-critic 架构**：
  - **Action-net (Actor)**：生成候选输入 $x_t$，通过梯度更新直接优化策略。
  - **Variational Critic (Helper Network)**：估计当前动作与观测之间的 MI，作为 acquisition function 的代理。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **计算效率** | 消除内层 acquisition 优化循环，实现端到端梯度训练，FLOPs 减少高达 **102×**。 |
| **灵活性** | 不依赖 GP 假设或特定变分族（如高斯），适用于复杂、非线性函数。 |
| **可扩展性** | 支持高维输入空间和批量评估，适合现实世界任务。 |
| **理论支持** | 提供了损失函数的一致性和收敛性分析。 |

---

## 2. 核心实验方法和设置

### 数据集与任务
实验涵盖两类任务：

#### 合成函数（Synthetic Functions）
- **Branin**, **Hartmann-6**, **Ackley-10**：经典非凸、多峰优化基准。
- 所有函数均转为最大化 $-f(x)$ 形式以统一比较。

#### 真实世界任务（Real-world Tasks）
| 任务 | 维度 | 特点 |
|------|------|------|
| **PDE Optimization** | 参数化系数场 | 最小化偏微分方程输出方差 |
| **Interferometer Position Optimization** | 4D | 光学干涉仪配置优化 |
| **Lunar Lander Game** | 12D | OpenAI Gym 控制策略优化 |
| **Pest Control** | 25D（含类别变量） | 多季节害虫种群控制策略，使用 one-hot 编码处理类别变量 |

### 实验设置
- **评估指标**：归一化累计奖励（average sum of rewards），即 $\frac{1}{t}\sum_{i=1}^t f(x_i)$。
- **批大小**：合成任务中 Branin 使用 32，其余使用 64。
- **学习率**：默认 0.002，Adam 优化器。
- **超参数调优**：所有基线方法进行网格搜索选取最优参数。
- **随机种子**：结果在多个随机种子上平均以减少方差。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| **GP-based** | Exact GP, Sparse GP, Focalized Sparse GP |
| **BNN-based** | HMC, SGHMC, LLA, DKL, IBNN |
| **其他** | VBO 替换项变体（见消融实验） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（最终平均奖励）

| 方法 | PDE | Interferometer | Lunar Lander | Pest Control |
|------|-----|----------------|--------------|-------------|
| **VBO-MI (Ours)** | **-0.0071** | **0.890** | **170.97** | **-9.02** |
| GP | -0.0349 | 0.567 | 119.88 | -11.30 |
| Sparse GP | -0.0175 | 0.730 | 145.9 | -9.75 |
| DKL | -0.0289 | 0.619 | 129.17 | -11.63 |
| HMC | -0.0139 | 0.668 | 138.51 | -11.08 |
| IBNN | -0.018 | 0.710 | 138.07 | -10.244 |

> ✅ **结论**：VBO-MI 在所有真实任务中均达到**最高最终奖励值**，尤其在高维任务（如 Lunar Lander 和 Pest Control）上优势明显。

### 与基线方法的对比结果
- **超越 GP 和 BNN 基线**：即使在 GP 表现良好的低维任务中，VBO-MI 仍能取得相当或更优结果。
- **高维下优势显著**：随着维度上升（如 Pest Control 25D），传统方法性能下降明显，而 VBO-MI 保持稳定高效。
- **计算效率碾压**：如图6所示，VBO-MI 的 FLOP 复杂度远低于 HMC-BNN、DKL 等方法，在 acquisition phase 实现近 **102× 加速**。

### 消融实验结果
作者进行了替换实验，验证各组件重要性：

| 变体 | 描述 | 发现 |
|------|------|------|
| **VBO + GP Exploration** | 用 GP 方差替代 MI 探索项 | 性能显著下降（见 Table 3–6） |
| **VBO + GP Exploitation** | 用 GP 均值替代期望利用项 | 下降幅度较小 |
| **不同 β 值测试** | 调整探索-利用权重 | VBO 对 β 更鲁棒，多数 β 下表现稳定；而其他方法对 β 敏感（见 Fig. 8） |
| **不同 batch size** | 测试批量大小影响 | VBO 随 batch size 增加性能提升，存在饱和阈值（见 Fig. 7） |

> 🔍 **关键发现**：**探索项（exploration term）由 MI 驱动至关重要**，替换为 GP 后性能大幅退化，说明 VBO 的探索机制更具有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **MI 可作为通用 acquisition signal**：无需 GP 或特定后验假设，通过 variational MI estimation 可有效指导 BO 的探索过程。
2. **端到端梯度优化可行且高效**：通过 actor-critic 架构，将 BO 转化为两个神经网络的交替训练问题，彻底消除内层优化瓶颈。
3. **VBO-MI 在高维复杂任务中表现卓越**：在 PDE、控制、组合优化等任务中 consistently outperform 现有方法。
4. **探索机制是成功关键**：消融实验证明，基于 MI 的探索项比传统 GP 方差更能驱动有效搜索。

### 方法的局限性
- **依赖神经网络训练稳定性**：需 careful tuning 学习率、warm-up 步数等超参数。
- **MI 估计可能存在偏差**：尽管使用 DV bound，但在小样本或高维下 MI 估计可能不准确。
- **尚未集成约束处理机制**：原文未讨论如何处理带约束的 BO 问题。

### 未来工作方向
- 扩展至 **multi-objective BO** 和 **constrained BO** 场景。
- 探索更稳定的 **MI estimator**（如 InfoNCE、TRE）以提升估计质量。
- 应用于更大规模的实际系统，如芯片设计、药物分子优化等。
- 研究 **theoretical regret bounds** for VBO-MI。

---

> 📌 **总结一句话**：  
> VBO-MI 是首个完全基于梯度的、无需 GP 假设的 BO 框架，通过 variational mutual information 实现高效探索，在保持高性能的同时将计算成本降低两个数量级，为高维黑盒优化提供了新范式。

</details>

---

### 15. [Deconstructing Pre-training: Knowledge Attribution Analysis in MoE and Dense Models](https://arxiv.org/abs/2601.08383)

**Authors**: Bo Wang, Junzhuo Li, Hong Chen, Yuanlin Chu, Yuxuan Fan, Xuming Hu  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08383v1  

#### Abstract
Mixture-of-Experts (MoE) architectures decouple model capacity from per-token computation, enabling scaling beyond the computational limits imposed by dense scaling laws. Yet how MoE architectures shape knowledge acquisition during pre-training, and how this process differs from dense architectures,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Deconstructing Pre-training: Knowledge Attribution Analysis in MoE and Dense Models*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文聚焦于当前对 **Mixture-of-Experts (MoE)** 架构在预训练过程中知识获取机制理解不足的问题。尽管 MoE 因其参数扩展效率高而被广泛采用，但其内部知识如何随时间演化、与传统 **dense 模型**有何本质差异，尚缺乏系统性的动态分析。

现有研究多为 **post-hoc 分析**（即仅在模型训练完成后进行解释），且集中于 dense 架构，无法揭示训练过程中的知识形成动态。

### 🧩 提出的新方法与思路
作者提出了一种新的 **neuron-level attribution 方法**：**Gated-LPI (Log-Probability Increase)**，用于追踪 MoE 和 dense 模型中神经元的重要性演化。

- **Gated-LPI 扩展了传统的 LPI 方法**，使其适用于 MoE 架构，能够分解每个 token 预测时由不同 **expert** 和 **attention head** 贡献的 log-probability 增量。
- 该方法支持 **time-resolved analysis**，即通过跟踪多个训练 checkpoint（最多 1.2M 步）来观察知识分配的动态变化。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **适用性** | 首次将 neuron attribution 应用于 MoE 模型，并考虑了 gating 机制的影响 |
| **时间维度** | 不是 post-hoc，而是全程追踪 pre-training 过程，揭示“知识是如何学会的” |
| **粒度精细** | 支持从 neuron → layer → functional robustness 的多层次分析 |
| **可解释性增强** | 揭示了 MoE 内部稳定性和鲁棒性的来源，为训练诊断提供工具 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **主任务数据集**：一个精心筛选的关系事实子集（来自 Hernandez et al., 2023）
  - 包含 **906 个 subject-object 对**
  - 涵盖 12 种关系，分为四类：
    - **Linguistic**: 如 `word_first_letter`
    - **Commonsense**: 如 `fruit_inside_color`
    - **Factual**: 如 `country_capital_city`
    - **Bias**: 如 `occupation_gender`

- **训练语料**：
  - **OLMo-7B (dense)**：使用 Dolma 语料（约 2.5T tokens）
  - **OLMoE-1B-7B (MoE)**：使用 OLMoE-MIX（DCLM + Dolma，偏重代码/数学）

> ⚠️ 注：作者承认语料差异可能构成混杂因素，但在分析中进行了控制。

### ⚙️ 实验设置
| 模型 | 参数规模 | 层数 | FFN/Expert 结构 | Routing 策略 | 训练步数 | Token 总量 |
|------|----------|-------|------------------|---------------|------------|-------------|
| **OLMo-7B** (dense) | 7B | 32 | 单一 FFN | — | 600K | ~2.5T |
| **OLMoE-1B-7B** (MoE) | ~7B (总)，~1.3B 激活/step | 16 | 每层 64 experts | Top-8 | 1.2M | ~5.0T |

> 两者共享实现基础，确保公平比较。

### 📊 评估指标
#### （1）性能指标
- **HIT@10**：目标对象是否出现在 top-10 预测中，衡量关系知识掌握程度。

#### （2）稳定性与集中性指标
| 指标 | 含义 |
|------|------|
| **Top-1% Set Stability (Jstab)** | 连续 checkpoint 中 top-1% 最重要神经元集合的 Jaccard 重叠率，衡量稳定性 |
| **Positive-Gain Concentration (R)** | 正向重要性增益集中在 top-1% 神经元的比例，反映学习信号集中程度 |
| **Layer-Distribution Consistency (pavg)** | 层间重要性分布的相关性，衡量层级稳定性 |
| **Cross-step Coefficient of Variation (orel)** | 各层重要性得分的时间序列波动幅度，越低越稳定 |

#### （3）功能鲁棒性测试
- **因果干预（ablation）实验**：屏蔽最重要的 attention heads 或 FFN neurons，观察 HIT@10 下降幅度。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据与对比

#### （1）Neuron-Level 动态：MoE 形成“低熵核心”
- **OLMoE**：
  - Top-1% FFN neurons 接收超过 **45% 的正向更新增益**（R ≈ 48.9%）
  - Jstab 在训练后期达到 **44.2%**（dense 仅为 6.1%）
- **OLMo**（dense）：
  - Top-1% neurons 的 Jstab 峰值仅 **10.2%**，随后持续下降至 4.9%
  - 学习信号频繁转移，无持久核心

> ➤ MoE 在早期就锁定一组高影响力神经元并持续强化，形成“**low-entropy backbone**”。

#### （2）Layer-Level 稳定性：MoE 更早固化结构
| 指标 | OLMoE (MoE) | OLMo (dense) |
|------|--------------|---------------|
| **FFN pavg** | 0.97 | 0.54 |
| **FFN orel** | 0.37 | 5.01 |
| **ATTN pavg** | 0.97 | 0.49 |
| **ATTN orel** | 0.49 | 8.64 |

> ➤ MoE 的层重要性分布极早稳定（<100K steps），而 dense 模型始终波动。

#### （3）功能鲁棒性（Ablation 实验）
| 干预方式 | OLMoE (MoE) | OLMo (dense) |
|---------|--------------|---------------|
| **Mask Top-1 attention head** | ↓0.06% | ↓16.46% |
| **Mask Top-10 attention heads** | ↓9.44% | ↓50.43% |
| **Mask Top-1% FFN neurons** | ↓35.47% | ↓96.19% |

> ➤ MoE 表现出显著的功能鲁棒性，即使移除最关键组件，性能仍保持；而 dense 模型极易崩溃。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **低熵骨干（Low-entropy backbone）**
   - MoE 模型在训练初期迅速形成一个由约 **top 1% 神经元组成的高价值核心**，这些神经元持续接收大部分正向更新。
   - 该现象在 dense 模型中不存在，后者神经元重要性不断更替（high churn）。

2. **早期固化（Early consolidation）**
   - MoE 的 FFN 与 ATTN 层的重要性分布 **在 <100K 步内即趋于稳定**，进入“explore-refine-consolidate”路径。
   - Dense 模型在整个训练过程中都表现出高度不稳定性。

3. **功能鲁棒性（Functional robustness）**
   - MoE 的知识存储是**分布式而非脆弱集中式**的，因此对关键组件的屏蔽具有强抗干扰能力。
   - Dense 模型的知识集中在少数易变组件上，导致“**brittle knowledge storage**”。

4. **架构即归纳偏置**
   - MoE 的稀疏激活机制不仅提升计算效率，还作为一种**有益的归纳偏置（inductive bias）**，促进稳定、鲁棒的学习路径。

---

### ⚠️ 方法的局限性
- **依赖特定 attribution 方法**：基于 Gated-LPI 的结论可能受其假设影响（如线性叠加近似）。
- **模型选择有限**：仅比较了 OLMo 和 OLMoE，虽同源但非完全对等（层数、深度不同）。
- **语料差异未完全消除**：MoE 使用更多代码/数学数据，可能影响知识分布。
- **静态干预**：ablation 是静态操作，未模拟动态失效场景。

---

### 🔮 未来工作方向
1. **扩展到更多 MoE 变体**：如 Top-2、shared experts、dynamic routing 等。
2. **结合 circuit discovery**：识别 MoE 中稳定的 functional subnetworks。
3. **指导训练优化**：
   - 设计 **stability-aware routing loss**
   - 开发 **early diagnostic 工具**，检测训练异常
   - 实现 **explore-then-stabilize routing policy**
4. **应用于模型压缩与剪枝**：利用稳定核心进行高效专家裁剪。

---

## 总结
本文通过引入 **Gated-LPI** 方法，首次实现了对 MoE 与 dense 模型在预训练过程中知识获取动态的细粒度、时间分辨比较。研究发现，**MoE 架构天然倾向于形成稳定、分布式的知识存储结构**，这不仅是计算上的优势，更是学习机制上的根本差异。这一成果为构建更具可解释性、鲁棒性和效率的大模型提供了理论支持与实践启示。

</details>

---

### 16. [MEMEWEAVER: Inter-Meme Graph Reasoning for Sexism and Misogyny Detection](https://arxiv.org/abs/2601.08684)

**Authors**: Paolo Italiani, David Gimeno-Gomez, Luca Ragazzi, Gianluca Moro, Paolo Rosso  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08684v1  

#### Abstract
Women are twice as likely as men to face online harassment due to their gender. Despite recent advances in multimodal content moderation, most approaches still overlook the social dynamics behind this phenomenon, where perpetrators reinforce prejudices and group identity within like-minded communiti...

---

### 17. [CLaS-Bench: A Cross-Lingual Alignment and Steering Benchmark](https://arxiv.org/abs/2601.08331)

**Authors**: Daniil Gurgurov, Yusser Al Ghussin, Tanja Baeumel, Cheng-Ting Chou, Patrick Schramowski, Marius Mosbach, Josef van Genabith, Simon Ostermann  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08331v1  

#### Abstract
Understanding and controlling the behavior of large language models (LLMs) is an increasingly important topic in multilingual NLP. Beyond prompting or fine-tuning, , i.e.,~manipulating internal representations during inference, has emerged as a more efficient and interpretable technique for adapting...

---

### 18. [InfGraND: An Influence-Guided GNN-to-MLP Knowledge Distillation](https://arxiv.org/abs/2601.08033)

**Authors**: Amir Eskandari, Aman Anand, Elyas Rashno, Farhana Zulkernine  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08033v1  

#### Abstract
Graph Neural Networks (GNNs) are the go-to model for graph data analysis. However, GNNs rely on two key operations - aggregation and update, which can pose challenges for low-latency inference tasks or resource-constrained scenarios. Simple Multi-Layer Perceptrons (MLPs) offer a computationally effi...

---

### 19. [Reverse Flow Matching: A Unified Framework for Online Reinforcement Learning with Diffusion and Flow Policies](https://arxiv.org/abs/2601.08136)

**Authors**: Zeyang Li, Sunbochen Tang, Navid Azizan  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08136v1  

#### Abstract
Diffusion and flow policies are gaining prominence in online reinforcement learning (RL) due to their expressive power, yet training them efficiently remains a critical challenge. A fundamental difficulty in online RL is the lack of direct samples from the target distribution; instead, the target is...

---

### 20. [WebTrap Park: An Automated Platform for Systematic Security Evaluation of Web Agents](https://arxiv.org/abs/2601.08406)

**Authors**: Xinyi Wu, Jiagui Chen, Geng Hong, Jiayi Dong, Xudong Pan, Jiarun Dai, Min Yang  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08406v1  

#### Abstract
Web Agents are increasingly deployed to perform complex tasks in real web environments, yet their security evaluation remains fragmented and difficult to standardize. We present WebTrap Park, an automated platform for systematic security evaluation of Web Agents through direct observation of their c...

---

### 21. [RubricHub: A Comprehensive and Highly Discriminative Rubric Dataset via Automated Coarse-to-Fine Generation](https://arxiv.org/abs/2601.08430)

**Authors**: Sunzhu Li, Jiale Zhao, Miteto Wei, Huimin Ren, Yang Zhou, Jingwen Yang, Shunyu Liu, Kaike Zhang, Wei Chen  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08430v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has driven substantial progress in reasoning-intensive domains like mathematics. However, optimizing open-ended generation remains challenging due to the lack of ground truth. While rubric-based evaluation offers a structured proxy for verificati...

---

### 22. [YaPO: Learnable Sparse Activation Steering Vectors for Domain Adaptation](https://arxiv.org/abs/2601.08441)

**Authors**: Abdelaziz Bounhar, Rania Hossam Elmohamady Elbadry, Hadi Abdine, Preslav Nakov, Michalis Vazirgiannis, Guokan Shang  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08441v1  

#### Abstract
Steering Large Language Models (LLMs) through activation interventions has emerged as a lightweight alternative to fine-tuning for alignment and personalization. Recent work on Bi-directional Preference Optimization (BiPO) shows that dense steering vectors can be learned directly from preference dat...

---

### 23. [M3-BENCH: Process-Aware Evaluation of LLM Agents Social Behaviors in Mixed-Motive Games](https://arxiv.org/abs/2601.08462)

**Authors**: Sixiong Xie, Zhuofan Shi, Haiyang Shen, Gang Huang, Yun Ma, Xiang Jing  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08462v1  

#### Abstract
As the capabilities of large language model (LLM) agents continue to advance, their advanced social behaviors, such as cooperation, deception, and collusion, call for systematic evaluation. However, existing benchmarks often emphasize a single capability dimension or rely solely on behavioral outcom...

---

### 24. [A Human-Centric Pipeline for Aligning Large Language Models with Chinese Medical Ethics](https://arxiv.org/abs/2601.07954)

**Authors**: Haoan Jin, Han Ying, Jiacheng Ji, Hanhui Xu, Mengyue Wu  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.07954v1  

#### Abstract
Recent advances in large language models have enabled their application to a range of healthcare tasks. However, aligning LLMs with the nuanced demands of medical ethics, especially under complex real world scenarios, remains underexplored. In this work, we present MedES, a dynamic, scenario-centric...

---

### 25. [Triplets Better Than Pairs: Towards Stable and Effective Self-Play Fine-Tuning for LLMs](https://arxiv.org/abs/2601.08198)

**Authors**: Yibo Wang, Hai-Long Sun, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Lijun Zhang  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08198v1  

#### Abstract
Recently, self-play fine-tuning (SPIN) has been proposed to adapt large language models to downstream applications with scarce expert-annotated data, by iteratively generating synthetic responses from the model itself. However, SPIN is designed to optimize the current reward advantages of annotated ...

---

### 26. [LUT-Compiled Kolmogorov-Arnold Networks for Lightweight DoS Detection on IoT Edge Devices](https://arxiv.org/abs/2601.08044)

**Authors**: Oleksandr Kuznetsov  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08044v1  

#### Abstract
Denial-of-Service (DoS) attacks pose a critical threat to Internet of Things (IoT) ecosystems, yet deploying effective intrusion detection on resource-constrained edge devices remains challenging. Kolmogorov-Arnold Networks (KANs) offer a compact alternative to Multi-Layer Perceptrons (MLPs) by plac...

---

### 27. [Creativity in AI as Emergence from Domain-Limited Generative Models](https://arxiv.org/abs/2601.08388)

**Authors**: Corina Chutaux (SU FdL)  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08388v1  

#### Abstract
Creativity in artificial intelligence is most often addressed through evaluative frameworks that aim to measure novelty, diversity, or usefulness in generated outputs. While such approaches have provided valuable insights into the behavior of modern generative models, they largely treat creativity a...

---

### 28. [Learner-Tailored Program Repair: A Solution Generator with Iterative Edit-Driven Retrieval Enhancement](https://arxiv.org/abs/2601.08545)

**Authors**: Zhenlong Dai, Zhuoluo Zhao, Hengning Wang, Xiu Tang, Sai Wu, Chang Yao, Zhipeng Gao, Jingyuan Chen  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08545v1  

#### Abstract
With the development of large language models (LLMs) in the field of programming, intelligent programming coaching systems have gained widespread attention. However, most research focuses on repairing the buggy code of programming learners without providing the underlying causes of the bugs. To addr...

---

### 29. [WaterCopilot: An AI-Driven Virtual Assistant for Water Management](https://arxiv.org/abs/2601.08559)

**Authors**: Keerththanan Vickneswaran, Mariangel Garcia Andarcia, Hugo Retief, Chris Dickens, Paulo Silva  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08559v1  

#### Abstract
Sustainable water resource management in transboundary river basins is challenged by fragmented data, limited real-time access, and the complexity of integrating diverse information sources. This paper presents WaterCopilot-an AI-driven virtual assistant developed through collaboration between the I...

---

### 30. [EmbeddingRWKV: State-Centric Retrieval with Reusable States](https://arxiv.org/abs/2601.07861)

**Authors**: Haowen Hou, Jie Yang  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.07861v1  

#### Abstract
Current Retrieval-Augmented Generation (RAG) systems typically employ a traditional two-stage pipeline: an embedding model for initial retrieval followed by a reranker for refinement. However, this paradigm suffers from significant inefficiency due to the lack of shared information between stages, l...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
