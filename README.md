# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-29 06:01:33 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Accelerate Speculative Decoding with Sparse Computation in Verification](https://arxiv.org/abs/2512.21911)

**Authors**: Jikai Wang, Jianchao Tan, Yuxuan Hu, Jiayu Qin, Yerui Sun, Yuchen Xie, Xunliang Cai, Juntao Li, Min Zhang  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 14.5  
**Type**: new  
**ArXiv ID**: 2512.21911v1  

#### Abstract
Speculative decoding accelerates autoregressive language model inference by verifying multiple draft tokens in parallel. However, the verification stage often becomes the dominant computational bottleneck, especially for long-context inputs and mixture-of-experts (MoE) models. Existing sparsificatio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerate Speculative Decoding with Sparse Computation in Verification

---

## 1. 论文的主要贡献和创新点

### 解决的问题
- **验证阶段成为计算瓶颈**：在 Speculative Decoding 中，虽然通过 draft 模型并行生成多个候选 token 加速了推理，但目标模型对这些 token 的**并行验证过程**（尤其是长上下文、MoE 模型）引入了巨大的计算开销。
- 现有稀疏化方法（如 Sparse Attention、Sparse FFN）主要针对标准自回归解码设计，未考虑多 token 并行验证场景下的结构冗余。

### 提出的新方法/新思路
作者提出了一种**统一的稀疏验证框架**（sparse verification framework），在不需重新训练的前提下，在验证阶段联合稀疏化三个核心组件：

1. **Sparse Attention**  
   - 基于第一个 draft token 的 query 进行 KV block 检索，并供所有 draft tokens 复用。
   - 引入边界保留机制（保留首尾 blocks）以防止重要上下文丢失。
   - 采用分段预算控制（piecewise budget control）策略：短序列不剪枝，长序列按比例动态调整保留 block 数量。

2. **Inter-layer Retrieval Reuse**  
   - 观察到中深层 Transformer 层间 KV block 检索高度相似。
   - 只在“锚定层”（anchor layers）执行检索，其余非锚定层复用最近前驱锚定层的结果，显著减少重复检索操作。

3. **Sparse Feed-Forward Network (SFFN)**  
   - 在验证时跳过激活值低于阈值 $T$ 的 FFN 通道，仅保留高响应通道参与 up/down projection 计算。
   - 利用 FFN 内部的激活稀疏性降低 FLOPs。

4. **Sparse Mixture-of-Experts (SMoE)**  
   - 动态跳过路由权重较低的专家（experts），允许每个 token 跳过最多 $m$ 个低贡献专家。
   - 阈值基于校准数据集上的路由 logit 比例中位数设定，适用于任意活跃专家数 $k > 2$。

5. **Hybrid Sparse Method**  
   - 将上述三种稀疏策略结合，在注意力、FFN 和 MoE 三个正交维度上同时进行稀疏化。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **通用性** | 不依赖 retraining 或架构修改，可直接应用于现成 LLMs（off-the-shelf models）。 |
| **效率提升** | 显著降低验证阶段主导性计算成本（见 Table 1 FLOPs 分析），尤其在长上下文和 MoE 场景下效果更明显。 |
| **精度保持** | 在适度稀疏下几乎无性能损失，甚至部分任务略有提升（可能因去噪效应）。 |
| **稳定性** | 维持稳定的 acceptance length，不影响整体生成分布一致性。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多种任务类型，来自 **LongBench** 和数学推理基准：
- **Summarization**: `GovReport`
- **Question Answering**: `2WikiMQA`, `HotpotQA`
- **Code Editing**: `LCC`, `RepoBench-P`
- **Mathematical Reasoning**: `GSM8K`, `Math`, `CollegeMath`

### 实验设置
| 设置项 | 描述 |
|-------|------|
| **Target Models** | - Llama3.1-8B-Instruct（用于 SA 实验）<br>- Qwen3-30B-A3B（用于 SFFN）<br>- Deepseek-R1（用于 SMoE 与 Hybrid） |
| **Draft Model** | EAGLE-3 或 MTP heads，使用树形 draft 结构（tree-structured drafts），共 60 个候选 token |
| **硬件平台** | 8×NVIDIA H800-80G GPUs |
| **稀疏参数搜索范围** | - Attention: $L_0 \in \{1K, 2K, 4K\}$<br>- FFN: 阈值 $T \in \{0.01, 0.05, 0.1\}$<br>- MoE: 跳过预算 $m \in \{2,3,4\}$ |

### 评估指标
| 指标 | 含义 |
|------|------|
| **ROUGE / F1 / Acc.** | 各任务主性能指标（如 ROUGE-L for summarization, F1 for QA, Accuracy for math） |
| **Sparsity ($s_a, s_f, s_e$)** | 注意力、FFN、MoE 模块的平均稀疏度 |
| **Mean Acceptance Length ($\bar{o}$)** | 衡量 speculative decoding 效率的关键指标，反映 draft token 的接受率 |

### 基线方法对比
- **Strict**: 标准 speculative decoding，全注意力、全 FFN、全专家激活（即无稀疏）
- **SA / SA***: Sparse Attention with/without inter-layer retrieval reuse
- **SFFN / SMoE / Hybrid**: 对应模块稀疏化方法及其组合

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### ✅ Sparse Attention（Table 2）
| 方法 | Sparsity ($s_a$) | GovReport (ROUGE) | 2WikiMQA (F1) | HotpotQA (F1) |
|------|------------------|--------------------|---------------|----------------|
| Strict | 0 | 34.18 | 39.20 | 47.66 |
| SA ($L_0=4K$) | 0.34 | 33.50 (-0.68) | 39.32 (+0.12) | 47.43 (-0.23) |
| SA* ($L_0=4K$) | 0.34 | 33.28 (-0.90) | 35.78 (-3.42) | 44.78 (-2.88) |

> 📌 **结论**：适度稀疏（$s_a≈0.34$）下性能下降极小；加入 layer reuse 后 QA 类任务略有退化，但长文本摘要稳定。

#### ✅ Sparse FFN（Table 3）
| 方法 | Sparsity ($s_f$) | GovReport | 2WikiMQA | GSM8K (Acc.) |
|------|------------------|-----------|----------|--------------|
| Strict | 0 | 32.67 | 43.93 | 90.0 |
| SFFN ($T=0.1$) | 0.64 | **33.51** (+0.84) | 62.01 | 91.0 |

> 📌 **结论**：即使高达 64% 的 FFN 通道被跳过，性能仍稳定甚至略有提升，表明验证阶段存在大量冗余激活。

#### ✅ Sparse MoE（Table 4）
| 方法 | Sparsity ($s_e$) | 2WikiMQA (F1) | Math (Acc.) | CollegeMath (Acc.) |
|------|------------------|---------------|-------------|---------------------|
| Strict | 0 | 77.39 | 82.0 | 58.0 |
| SMoE ($m=3$) | 0.16 | **78.52** | **87.0** | 57.0 |
| SMoE ($m=4$) | 0.22 | 78.88 | 75.0 ↓ | 52.0 ↓ |

> 📌 **结论**：适度跳过专家（$m=2,3$）可去除噪声信号，反而提升性能；过度稀疏（$m=4$）损害复杂推理能力。

#### ✅ Hybrid Sparse Method（Table 5）
| 方法 | ROUGE (GovReport) | F1 (2WikiMQA) | Acc. (GSM8K) | $\bar{o}$ (均值) |
|------|--------------------|----------------|---------------|------------------|
| Strict | 26.90 | 77.39 | 98.0 | 2.44 |
| Hybrid | **27.40** | **79.82** | 96.0 | 2.42 |

> 📌 **结论**：三重稀疏联合使用仍能保持甚至提升多数任务性能，且 acceptance length 几乎不变（仅微降 0.02），说明稀疏化未破坏 draft-target 对齐。

---

## 4. 关键结论和发现

### 主要发现
1. **验证阶段存在显著结构性冗余**  
   - 注意力：多数 KV blocks 可安全剪枝，尤其中间位置；
   - FFN：大量低激活通道对输出影响甚微；
   - MoE：低权重专家贡献有限，可动态跳过。

2. **跨 token 与跨层冗余可被有效利用**  
   - 同一步骤内多个 draft tokens 检索高度重叠 → 可共享 block selection；
   - 中深层 Transformer 层间检索模式相似 → 可实现 inter-layer reuse。

3. **稀疏验证可在几乎无损情况下大幅提效**  
   - 在合理稀疏配置下，各项任务性能基本持平或略有提升；
   - 接受长度（acceptance length）变化极小，保证了加速收益。

4. **不同任务对稀疏敏感度不同**  
   - 生成类任务（summarization, open QA）鲁棒性强；
   - 符号密集型数学推理（CollegeMath）对过度稀疏更敏感。

### 方法的局限性
- **稀疏参数需手动调优**：如 $L_0$, $T$, $m$ 等依赖经验或校准数据集选择。
- **极端稀疏会损害复杂推理**：特别是需要精确 token-level 验证的任务（如 CollegeMath）。
- **未探索稀疏对 draft model 训练的影响**：当前假设 draft model 与非稀疏 target 对齐，若 target 稀疏化程度高，可能需重新训练 draft model 以更好适配。

### 未来工作方向
1. **自动化稀疏调度器**：根据输入长度、任务类型自动调节各模块稀疏强度。
2. **端到端稀疏感知训练**：将稀疏验证纳入训练目标，优化 draft-target 对齐。
3. **扩展至其他 generation paradigms**：如 lookahead decoding、parallel decoding 等。
4. **硬件协同优化**：结合 Sparse Attention 的 block 结构设计专用 kernel 提升实际吞吐。

---

> 🔚 **总结一句话**：该论文系统地揭示了 speculative decoding 验证阶段的多维冗余，并提出了一个无需训练、可插拔的稀疏验证框架，在保持生成质量的同时显著提升了推理效率，为长上下文与 MoE 模型的高效部署提供了实用解决方案。

</details>

---

### 2. [LIME:Accelerating Collaborative Lossless LLM Inference on Memory-Constrained Edge Devices](https://arxiv.org/abs/2512.21835)

**Authors**: Mingyu Sun, Xiao Zhang, Shen Qu, Yan Li, Mengbai Xiao, Yuan Yuan, Dongxiao Yu  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.21835v1  

#### Abstract
Large language models (LLMs) have emerged as a powerful foundation for intelligent reasoning and decision-making, demonstrating substantial impact across a wide range of domains and applications. However, their massive parameter scales and substantial resource demands pose critical challenges for ef...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LIME: Accelerating Collaborative Lossless LLM Inference on Memory-Constrained Edge Devices —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（**LLMs**）在边缘设备上进行推理面临两大挑战：
- **内存严重受限**：如 LLaMA3.3-70B-Instruct 需要至少 130GB 内存，而典型边缘设备（如 NVIDIA Jetson）仅提供 16–64GB。
- **网络带宽有限且波动大**：分布式部署中通信开销高，影响实时性和效率。

传统方法（如量化、剪枝、蒸馏）虽能降低资源消耗，但会引入**精度损失（lossy）**，不适用于金融风控、医疗诊断等对准确性要求极高的场景。

---

### 🚀 提出的新方法：LIME 框架
LIME 是一个支持**无损（lossless）协同推理**的系统框架，专为内存受限、带宽受限的异构边缘设备设计。其核心创新包括：

#### （1）**交错式流水线并行（Interleaved Pipeline Parallelism）**
- 将模型分层分配给多个设备，并允许每个设备承载多个非连续的“段”（segment），实现更灵活的负载均衡。
- 在每一段执行过程中，将**动态卸载（offloading）与计算/通信重叠**，显著减少等待延迟。
- 相比传统流水线中“同一阶段内需多次加载”的问题，LIME 减少了重复加载次数。

#### （2）**细粒度离线调度器（Fine-grained Offline Allocation Scheduler）**
- 构建了一个面向卸载的异构成本模型（heterogeneous offload-orient cost model），综合考虑：
  - 各设备的计算能力
  - 内存容量
  - 加载速度
  - 通信带宽
- 利用动态规划算法快速搜索最优层分配策略，在毫秒级时间内完成调度决策。
- 支持以 **MHA 或 MLP 模块为单位**进行细粒度卸载，避免粗粒度层卸载带来的内存碎片和频繁传输。

#### （3）**在线内存自适应策略（Online Memory Adaptation Strategy）**
- 包含两个关键机制应对运行时变化：
  - **KV Cache Transfer Protocol**：当某设备 KV 缓存增长过快时，将其部分 KV 缓存转移到其他空闲设备，缓解局部瓶颈。
  - **Memory-aware Planner**：根据当前生成 token 数量预测内存压力，动态触发模块级卸载（如先卸 MHA，再卸 MLP），并自动确定下一次卸载阈值。

---

### 🔍 相比现有方法的优势
| 对比维度 | 现有方法（如 EdgeShard、Galaxy、TPI-LLM） | LIME |
|--------|------------------------------------------|------|
| 是否支持 lossless 推理 | 多数不保证完全无损或依赖近似压缩 | ✅ 完全保留原始模型参数，确保无损 |
| 是否处理极端内存不足 | 多数假设设备可容纳最小分片 | ✅ 可在单个设备无法容纳任何完整层的情况下运行 |
| 卸载粒度 | 层级别（layer-level）为主 | ✅ 支持 MHA/MLP 块级卸载，减少传输量 |
| 资源利用率 | 固定静态划分，难以适应变化 | ✅ 动态调整 KV 分配与卸载计划 |
| 通信效率 | 张量并行（TP）带来高同步开销 | ✅ 流水线并行 + 卸载，更适合低带宽环境 |

---

## 2. 核心实验方法和设置

### 📊 使用的模型（而非数据集）
由于是推理加速研究，重点在于模型规模和架构多样性，而非训练数据。使用了以下三种主流 LLM：

| Model | 参数量 | 层数 | 隐藏维度 | Attention Heads |
|-------|--------|------|-----------|------------------|
| Llama2-13B-Instruct | 13B | 40 | 5120 | 40 |
| Qwen3-32B | 32B | 64 | 5120 | 64 |
| Llama3.3-70B-Instruct | 70B | 80 | 8192 | 64 |

> 所有模型从 Hugging Face 下载，输入来自通用文本生成任务子集。

---

### ⚙️ 实验设置

#### 硬件平台
基于三种 NVIDIA Jetson 设备构建异构边缘集群：
| 设备型号 | 内存 | GPU | AI 性能 |
|---------|------|-----|--------|
| Jetson Xavier NX (16GB) | 16GB | Volta | 21 TOPS |
| Jetson AGX Orin (32GB) | 32GB | Ampere | 200 TOPS |
| Jetson AGX Orin (64GB) | 64GB | Ampere | 275 TOPS |

连接方式：通过路由器和交换机互联，总带宽 1000 Mbps，使用 Linux TC 工具模拟 **100 Mbps 和 200 Mbps** 两种网络条件。

#### 请求模式（Request Patterns）
- **Sporadic**：单请求间歇到达，micro-batch size = 1
- **Bursty**：多请求并发提交，模拟突发流量

#### 实验环境配置（见 Table IV）
| 环境 | 模型 | 设备组成 |
|------|------|----------|
| E1 | Llama2-13B | 1×NX(16G) + 1×Orin(32G) |
| E2 | Qwen3-32B | 1×NX(16G) + 1×Orin(32G) + 1×Orin(64G) |
| E3 | Llama3.3-70B | 1×NX(16G) + 2×Orin(64G) |

---

### 📈 评估指标
- **推理延迟（Inference Latency）**：单位 ms/token，为主要性能指标。
- **加速比（Speedup）**：相对于基线方法的延迟下降倍数。
- **OOM / OOT 判断标准**：
  - Sporadic 场景：>40s/token → OOT（超时）
  - Bursty 场景：>15s/token → OOT

---

### 🔁 基线方法对比
共比较六种代表性方法：
1. **Pipeline Parallelism**：经典流水线并行
2. **Pipeline + Offloading**：流水线 + 显存外卸载
3. **EdgeShard**：针对边缘优化的高效流水线框架
4. **Galaxy**：基于张量并行（TP）的分布式推理系统
5. **TPI-LLM**：移动端轻量化 TP 推理方案
6. **TPI-LLM + Offloading**：扩展版，加入滑动窗口管理 KV Cache

---

## 3. 主要实验结果和性能指标

### 📉 关键性能数据（E3 环境，LLaMA3.3-70B-Instruct）

| 方法 | Sporadic @200Mbps (ms/token) | Bursty @200Mbps (ms/token) | Speedup vs SOTA |
|------|-------------------------------|------------------------------|-----------------|
| LIME | **1501.9** | **423.2** | 1.00× |
| Pipeline + Offloading | ~2550 | ~1561 | 0.59× |
| EdgeShard | ~2494 | ~1591 | 0.60× |
| TPI-LLM | ~4000+ | ~1000~3000 | OOT / variable |
| Galaxy | OOM | OOM | 不可用 |

> ✅ **LIME 在四台异构 Jetson 上成功运行 70B 模型，而多数基线失败（OOM）或超时（OOT）**

---

### 📊 加速效果汇总
| 场景 | 相对于 SOTA 的加速比 |
|------|------------------------|
| Sporadic 请求 | **1.7×** |
| Bursty 请求 | **3.7×** |

> 在低带宽（100Mbps）和高带宽（200Mbps）下均保持显著优势。

---

### 🔍 消融实验结果（Ablation Study，见 Table V）

| 方法变体 | Sporadic Latency (ms/token) | Speedup vs LIME | Bursty Latency (ms/token) | Speedup vs LIME |
|--------|----------------------------|------------------|----------------------------|------------------|
| LIME（完整） | 1501.9 | 1.00× | 423.2 | 1.00× |
| 缺少 KV Cache Transfer | 1748.2 | 0.86× | 489.0 | 0.87× |
| 缺少 Memory-aware Planner | 2251.7 | 0.67× | 614.8 | 0.69× |

> 结果表明：
- **KV Cache Transfer Protocol** 贡献约 14% 性能提升
- **Memory-aware Planner** 贡献约 33% 性能提升
- 两者共同作用显著增强系统鲁棒性与效率

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **交错式流水线 + 细粒度卸载** 可有效解决边缘设备内存不足以容纳单一层的问题，首次实现在极端内存受限条件下对 70B 级别 LLM 的**无损协同推理**。
2. **离线调度 + 在线自适应** 的双层设计，兼顾了初始资源最优分配与运行时动态调节能力，尤其适合边缘环境中频繁波动的 KV Cache 和网络带宽。
3. **块级卸载（MHA/MLP）** 比传统层级卸载更高效，减少了不必要的数据搬移，提升了重叠率。
4. LIME 在真实异构边缘平台上验证有效，相比 SOTA 方法最高实现 **3.7× 加速**，且无精度损失。

---

### ⚠️ 方法的局限性
1. **依赖 SSD 存储性能**：若存储介质读写延迟过高，会影响卸载效率。
2. **初始化开销较大**：离线调度虽快，但仍需预知模型结构和设备特性，不适合高度动态拓扑。
3. **未支持训练阶段**：目前仅聚焦于推理加速，尚未扩展到微调或持续学习场景。
4. **跨设备同步仍存在轻微阻塞**：尽管已优化重叠，但在某些边界情况下仍有小范围等待。

---

### 🔮 未来工作方向
1. 扩展至 **multi-modal models** 和 **MoE 架构** 的协同推理。
2. 引入 **预测式调度器**，结合历史请求模式预测 KV 增长趋势。
3. 探索 **更智能的缓存替换策略**，替代固定顺序的 MHA→MLP 卸载逻辑。
4. 支持 **联邦式协作推理**，保护用户隐私的同时共享边缘算力。

---

> 💡 **一句话总结**：  
> LIME 通过创新的 **interleaved pipeline + fine-grained offloading + online adaptation** 三重机制，首次实现了在内存严重受限的异构边缘设备上对百亿级 LLM 的**高速、无损协同推理**，为边缘侧部署高精度大模型提供了可行路径。

</details>

---

### 3. [FUSCO: High-Performance Distributed Data Shuffling via Transformation-Communication Fusion](https://arxiv.org/abs/2512.22036)

**Authors**: Zhuoran Zhu, Chunyang Zhu, Hao Lin, Xu Fu, Yiming Zhou, Quanlu Zhang, Zhenhua Li, Feng Qian, Chao Yu, Boxun Li, Guohao Dai, Yu Wang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.22036v1  

#### Abstract
Large-scale Mixture-of-Experts (MoE) models rely on \emph{expert parallelism} for efficient training and inference, which splits experts across devices and necessitates distributed data shuffling to route each token to its assigned experts. However, existing communication libraries handle this shuff...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# FUSCO: High-Performance Distributed Data Shuffling via Transformation-Communication Fusion 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型 **Mixture-of-Experts (MoE)** 模型在训练和推理过程中依赖 **expert parallelism**，即将专家（experts）分布在多个设备上。这导致每个输入 token 需要被路由到其对应的专家所在的设备，从而引发大规模的 **分布式数据 shuffling（重排）**。

然而，现有的通信库（如 NCCL、DeepEP）将数据变换（transformation）与通信（communication）分离处理，导致以下问题：
- 数据需要在通信前进行复杂的预重排（pre-rearrangement），通信后再反向恢复；
- 多次内存拷贝和缓冲区重组显著增加延迟；
- 重复的数据在跨节点传输中未去重，浪费带宽；
- 负载不均衡导致通信热点（hotspot）。

这些问题使得 **数据 shuffling 开销占端到端运行时间的 22%–61%**，成为 MoE 性能瓶颈。

---

### **提出了什么新方法或新思路**

论文提出 **FUSCO** —— 一种面向 MoE 的高性能通信库，其核心思想是 **Transformation-Communication Fusion（变换-通信融合）**。

#### 主要创新点包括：

1. **统一的数据抽象：Segment Descriptor**
   - 将待传输的数据（如 token）建模为一系列逻辑上的 **segments（段）**；
   - 引入 **Segment Descriptor** 抽象，记录每段数据的源地址、目标地址和大小；
   - 允许在通信路径中直接完成非连续内存的数据 gather/scatter，无需中间重排。

2. **Data-Fused Communication Engine (dComm)**
   - 基于描述符驱动的通信引擎，采用流水线设计；
   - 在 GPU 到 NIC 的数据搬运过程中，**内联执行数据布局转换**；
   - 支持跨节点和节点内通信的高效融合操作。

3. **两层通信规划机制（Two-Level Communication Plan）**
   - **Node-Level Forwarding Descriptors**：实现跨节点 token 去重，每个远程节点仅接收一份副本；
   - **Expert-Level Distribution Descriptors**：在节点内部将 token 分发到具体专家，避免二次重排。

4. **在线负载均衡器（Online Load Balancer）**
   - 动态分析各 GPU 的跨节点流量；
   - 使用轻量级贪心算法构建通信组（communication groups），平衡转发负载，防止热点。

5. **基于 NCCL 的可移植实现**
   - 构建在 NCCL 之上，复用其底层网络栈，保证通用性和兼容性；
   - 提供对主流 LLM 框架（如 Megatron-LM、SGLang）的即插即用支持。

---

### **相比现有方法的优势**

| 维度 | FUSCO vs. NCCL / DeepEP |
|------|------------------------|
| **通信效率** | 消除冗余数据复制和重排，减少内存带宽消耗 |
| **带宽利用** | 跨节点通信去重，显著降低高带宽链路压力 |
| **负载均衡** | 动态调度避免通信热点，提升整体吞吐 |
| **端到端性能** | 显著加速 MoE 训练与推理，尤其在大规模部署下优势更明显 |
| **通用性** | 不依赖特定硬件特性（如 IBGDA），可在多种集群配置上运行 |

---

## 2. 核心实验方法和设置

### **使用的数据集与场景**

- **真实世界流量**：来自 DeepSeek-V3 推理阶段的路由行为，使用 ShareGPT 数据集生成；
- **控制实验环境**：
  - **Single-node routed traffic**：所有 token 的目标专家位于同一节点，测试跨节点去重能力；
  - **Load-imbalanced traffic**：人为构造 GPU 间通信负载严重不均的情况，验证负载均衡机制有效性。

---

### **实验设置**

- **硬件平台**：8 节点集群，每节点配备：
  - 8 × NVIDIA H100 GPU（80GB HBM3）
  - NVLink 互联（~480 GB/s 节点内带宽）
  - 10 × 400Gbps RoCE NIC（跨节点通信）
- **软件环境**：
  - CUDA 12.9, PyTorch 2.7.0, NCCL 2.26.3
  - 实现基于 NCCL 扩展，集成至 Megatron-LM 和 SGLang

---

### **评估指标**

| 指标 | 描述 |
|------|------|
| **通信延迟** | MoE 层中 all-to-all 操作的总耗时（分阶段统计） |
| **训练迭代时间** | 单次训练迭代的 wall-clock 时间 |
| **首 token 延迟（TTFT）** | 推理预填充阶段从输入到输出第一个 token 的时间 |
| **吞吐量（Throughput）** | 每秒处理的 token 数量 |
| **消融实验性能退化率** | 关闭某模块后的性能下降百分比 |

---

### **基线方法对比**

| 基线 | 特点 |
|------|------|
| **NCCL** | 通用集体通信库，默认后端，无 MoE 优化 |
| **DeepEP** | 当前最先进的 MoE 专用通信库，基于 NVSHMEM，支持部分去重和流水线 |

---

## 3. 主要实验结果和性能指标

### **通信基准测试结果**

| 场景 | 对比对象 | 加速比（Speedup） |
|------|---------|------------------|
| Real-world traffic | NCCL | **1.60× – 1.66×** |
| | DeepEP | **1.13× – 1.34×** |
| Single-node routed | NCCL | **3.47× – 3.84×** |
| | DeepEP | **1.95× – 2.01×** |
| Load-imbalanced | NCCL | **1.99× – 2.24×** |
| | DeepEP | **1.29× – 1.42×** |

> ✅ 在单节点路由场景下，FUSCO 表现出最大优势，因其有效实现了跨节点 token 去重。

---

### **端到端性能表现**

#### **训练性能（Iteration Time）**
- 使用 Qwen3 和 DeepSeek-V3 模型，序列长度 16k，EP=64
- 相比 NCCL：**1.17× – 1.39× 加速**
- 相比 DeepEP：**1.10× – 1.19× 加速**

#### **推理性能（Time-to-First-Token, TTFT）**
- 同样配置下：
- 相比 NCCL：**1.09× – 1.25× 降低延迟**
- 相比 DeepEP：**1.06× – 1.16× 降低延迟**

> 🔍 性能增益随模型规模增大而增强，表明 FUSCO 更适合超大规模 MoE 部署。

---

### **消融实验结果（Ablation Study）**

关闭不同组件后的性能退化（以延迟增加百分比表示）：

| 场景 | dComm 关闭 | Planner 关闭 | Balancer 关闭 |
|------|------------|--------------|---------------|
| Real-world | -27.3% | -30.2% | -8.7% |
| Single-node routed | -33.3% | **-67.3%** | -3.2% |
| Imbalanced traffic | -31.2% | -27.1% | **-16.6%** |

#### 关键发现：
- **dComm 是核心**：平均带来 ~30% 性能提升，消除重排开销；
- **Planner 至关重要**：在单节点路由场景下贡献高达 67.3%，体现去重价值；
- **Balancer 在负载不均时作用显著**：最高带来 16.6% 改善。

---

## 4. 关键结论和发现

### **主要发现**

1. **MoE 中的数据 shuffling 不应视为“纯通信”任务**，而是 **数据变换与通信的联合优化问题**。
2. **传统 disaggregated 设计（先重排再通信）造成大量冗余内存操作**，严重拖累性能。
3. **通过融合 transformation 与 communication，FUSCO 成功将重排操作“嵌入”通信路径**，实现零额外拷贝。
4. **层级化路由 + 动态负载均衡** 可有效缓解跨节点带宽瓶颈和通信热点问题。
5. **FUSCO 在真实和极端场景下均表现出显著优势**，尤其在高 expert parallelism 下更具竞争力。

---

### **方法的局限性**

1. **预处理开销固定**：FUSCO 引入少量元数据生成开销，在小 batch 或短序列场景下相对影响较大；
2. **当前负载均衡仅考虑发送端**：未完全优化接收端负载，仍有改进空间；
3. **依赖精确的路由矩阵输入**：若路由动态变化频繁，需重新生成 descriptor，可能引入调度延迟；
4. **尚未支持压缩或量化等进一步优化手段**。

---

### **未来工作方向**

1. **扩展至其他结构化数据通信场景**：如 Vision Transformers 中的 patch shuffling；
2. **支持动态路由下的增量 descriptor 更新机制**；
3. **结合 computation-communication overlap 进一步隐藏通信延迟**；
4. **探索更精细的 descriptor 压缩与缓存策略**，降低元数据开销；
5. **集成进更多主流框架（如 DeepSpeed、ColossalAI）并开源发布代码与工具链**。

---

> 📌 **总结一句话**：  
> FUSCO 通过 **transformation-communication fusion** 的设计理念，从根本上重构了 MoE 中的数据 shuffling 流程，实现了通信效率的显著突破，在训练和推理中均取得优于 NCCL 和 DeepEP 的性能表现，为大规模 MoE 系统提供了高效、可扩展且通用的通信解决方案。

</details>

---

### 4. [MAD-NG: Meta-Auto-Decoder Neural Galerkin Method for Solving Parametric Partial Differential Equations](https://arxiv.org/abs/2512.21633)

**Authors**: Qiuqi Li, Yiting Liu, Jin Zhao, Wencan Zhu  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.21633v1  

#### Abstract
Parametric partial differential equations (PDEs) are fundamental for modeling a wide range of physical and engineering systems influenced by uncertain or varying parameters. Traditional neural network-based solvers, such as Physics-Informed Neural Networks (PINNs) and Deep Galerkin Methods, often fa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MAD-NG: Meta-Auto-Decoder Neural Galerkin Method for Solving Parametric Partial Differential Equations

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于神经网络的 PDE 求解器（如 PINNs、Deep Galerkin Methods）在求解**参数化偏微分方程**（parametric PDEs）时面临以下挑战：
- **泛化能力差**：对新的参数配置需要重新训练，计算成本高；
- **长时间预测不稳定**：全局时空逼近容易破坏时间因果性（temporal causality），导致误差累积；
- **计算开销大**：全参数更新策略在高维或长时域问题中效率低下。

### 提出的新方法与创新思路
作者提出了一种新型框架——**Meta-Auto-Decoder Neural Galerkin Method (MAD-NGM)**，并进一步发展为稀疏版本 **MAD-RSNGS**（Meta-Auto-Decoder Randomized Sparse Neural Galerkin Schemes）。其核心创新包括：

#### （1）两阶段求解框架：MAD-NGM
- **第一阶段：初始近似（Initial Approximation）**
  - 利用 **Meta-Auto-Decoder (MAD)** 构建一个非线性试函数流形（nonlinear trial manifold），通过预训练学习多个样本的隐空间表示；
  - 对于新参数配置，仅需微调（fine-tune）隐变量 $ z $ 即可快速获得初始网络参数，无需从头训练。
- **第二阶段：时间演化（Time Evolution）**
  - 基于 **Neural Galerkin Method (NGM)** 进行时间推进，利用 Dirac-Frenkel 变分原理构建 ODE 系统来更新网络参数；
  - 实现了**时空解耦**（space-time decoupling），保证了物理一致性与长期稳定性。

#### （2）随机稀疏更新策略：MAD-RSNGS
- 在每个时间步，只随机选择一部分网络参数进行更新（sparse update），其余保持不变；
- 显著降低每步计算复杂度，同时避免过拟合，提升整体效率。

#### （3）边界条件处理机制
- 引入 **positional embedding** 层（如 `[sin(x), cos(x), z]`）显式编码周期性边界条件，使模型自然满足 BC。

---

### 相比现有方法的优势

| 方法 | 缺陷 | MAD-NGM/MAD-RSNGS 的优势 |
|------|------|---------------------------|
| **PINNs / MAD-PINN** | 全局时空训练，忽略时间因果性，误差随时间积累；需密集采样整个时空域 | 时间递进式更新，保留因果结构；仅依赖当前状态推进，适合长时预测 |
| **传统 NGM** | 每个新参数需重新训练初始参数，无法泛化 | MAD 预训练后可通过微调快速适应新参数，显著减少重复训练开销 |
| **Operator Learning (e.g., FNO, DeepONet)** | 虽能泛化，但通常用于静态映射，难以处理动态演化过程 | 结合 meta-learning 与时间积分，兼具强泛化能力和动态建模能力 |

> ✅ **核心优势总结**：
> - **高效泛化**：通过 MAD 实现 zero-/few-shot 参数适应；
> - **物理一致**：基于变分原理的时间推进保障长期稳定性；
> - **计算高效**：稀疏更新大幅降低时间演化阶段的计算负担。

---

## 2. 核心实验方法和设置

### 使用的数据集与测试问题
论文在四个典型参数化 PDE 上进行了验证：

1. **Korteweg-de Vries (KdV) 方程**  
   - 一维非线性色散波方程，含随机初值 $ u(x,0) = \alpha_1 \sin x + \alpha_2 \cos x $
   - 参数范围：$ \alpha_1, \alpha_2 \in [-0.5, 0.5] $

2. **Burgers 方程**  
   - 含激波形成的非线性对流扩散方程，初值为高斯随机场；
   - 测试模型捕捉强梯度区域的能力。

3. **Allen-Cahn (AC) 方程（一维）**  
   - 相场模型，描述相分离过程；
   - 扩展至**随机计算域** $ \Omega = [-\delta, 1+\delta], \delta \in [-0.2, 0.2] $，验证几何不确定性下的鲁棒性。

4. **Allen-Cahn 方程（二维）**  
   - 高维扩展，测试方法在 $ [0,1]^2 $ 上的表现；
   - 初值为带随机系数的三角函数组合。

---

### 实验设置与评估指标

#### 评估指标
- **Mean Squared Error (MSE)**：
  $$
  \text{MSE} = \frac{1}{N_{\text{test}}} \sum_{j=1}^{N_{\text{test}}} \frac{1}{N} \sum_{i=1}^{N} (\hat{u}_j - u_j^{\text{true}})^2
  $$
  其中 $ N_{\text{test}} $ 为测试样本数，$ N $ 为空间网格点数。

#### 基线方法对比
- **MAD-PINN**：基于物理信息的端到端训练方法；
- **MAD-NGM**：本文提出的完整更新版本；
- **MAD-RSNGS**：本文提出的稀疏更新变体，不同 $ s $ 表示每次更新的参数数量。

#### 网络架构与优化
- 网络结构：1–8 层全连接网络，激活函数为 `tanh`；
- 隐变量维度：5–80 不等；
- 优化器：
  - 预训练阶段：L-BFGS（1k–100k 步）
  - 微调阶段：Adam 或 L-BFGS
- 时间离散：Forward Euler 或 RK4；
- 参考解：Chebfun（谱方法）、Fourier spectral + RK4。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ KdV 方程（表1）
| 数据集 | 细调迭代次数 | MSE @ t=1.0 |
|--------|---------------|-------------|
| 训练集 | 4000          | $ 3.69 \times 10^{-5} $ |
| 测试集 | 4000          | $ 9.02 \times 10^{-6} $ |

> ✔️ 新样本经少量微调即可达到与训练集相当精度，显示良好泛化能力。

---

#### ✅ Burgers 方程（表2 & 图9）

| 方法 | MSE @ t=1.0 |
|------|--------------|
| MAD-NGM | $ 4.11 \times 10^{-6} $ |
| MAD-RSNGS ($ s=3000 $) | $ 1.21 \times 10^{-5} $ |
| MAD-PINN | $ 5.89 \times 10^{-4} $ |

> 🔺 MAD-PINN 的误差比 MAD-NGM **高出两个数量级**（~100倍），且随时间严重累积（见图9）。

#### 时间成本对比（表3）
| 方法 | 预训练(min) | 微调(min) | 时间演化(min) |
|------|-------------|-----------|----------------|
| MAD-NGM | 44.16 | 7.21 | 46.47 |
| MAD-RSNGS ($ s=600 $) | 44.16 | 7.21 | **3.50** |
| MAD-PINN | **141.32** | 4.03 | — |

> ⏱️ MAD-RSNGS 将时间演化耗时从 **46.47 分钟降至 3.5 分钟**，提速约 **13 倍**！

---

#### ✅ Allen-Cahn 方程（一维 & 二维）

- **一维 AC（表4）**：
  | 参数类型 | MSE @ t=2.0 |
  |---------|-------------|
  | 时间无关 | $ 7.35 \times 10^{-5} $ |
  | 时间相关 | $ 6.78 \times 10^{-5} $ |
  > ✔️ 即使参数随时间和空间变化，仍能保持高精度。

- **二维 AC（图15）**：
  - 在 $ t=2.0 $ 时 MSE ≈ **0.0026**；
  - 成功捕捉相界面形成与演化过程（图14）；
  - 证明方法可扩展至高维空间。

---

### 消融实验分析（Implicit）
虽然未明确列出“ablation study”章节，但从多组实验设计中可得出以下消融结论：

| 改进模块 | 效果 |
|--------|------|
| **MAD 预训练 + 微调** | 相比从头训练，节省 >90% 初始化时间，实现快速泛化 |
| **Neural Galerkin 时间推进** | 相比 PINN 全局训练，显著改善长期预测稳定性 |
| **Randomized Sparse Update** | 在几乎不损失精度的前提下，将时间演化成本降低一个数量级以上 |

---

## 4. 关键结论和发现

### 主要发现
1. **MAD-NGM 实现了参数化 PDE 的高效泛化求解**：
   - 通过 meta-learning 学习初始条件分布，在新参数下只需微调隐变量即可启动；
   - 大幅减少重复训练开销，适用于实时决策场景。

2. **时间递进式建模优于全局 PINN 框架**：
   - MAD-NGM/MAD-RSNGS 保持了时间因果性，误差增长缓慢；
   - 而 MAD-PINN 因忽略时间结构，出现明显误差累积。

3. **稀疏更新策略有效平衡精度与效率**：
   - MAD-RSNGS 在仅更新部分参数的情况下，仍能达到接近 MAD-NGM 的精度；
   - 特别适合大规模、长时间模拟任务。

4. **方法具有良好的几何与维度扩展性**：
   - 成功应用于随机域和二维问题，表明其可用于复杂工程系统建模。

---

### 方法的局限性
- **对网络结构敏感**：实验表明性能受层数、宽度、隐维影响较大；
- **稀疏更新比例需调参**：$ s $ 的选择影响收敛速度与最终精度；
- **尚未完全免训练**：虽减少重训，但仍需一定微调步骤；
- **理论分析有限**：缺乏关于误差传播、收敛性的严格数学证明。

---

### 未来工作方向
1. **自适应网络设计**：开发可根据问题自动调整结构的方法；
2. **多层级优化策略**：结合 coarse/fine 时间尺度进行更高效的参数更新；
3. **扩展至逆问题与控制问题**：将框架用于参数识别、最优控制等任务；
4. **硬件加速集成**：探索在 GPU/TPU 上的大规模并行实现；
5. **与其他降维方法融合**：如与 Proper Orthogonal Decomposition (POD) 或 Dynamic Mode Decomposition (DMD) 结合。

---

## 总结

✅ **MAD-NGM 是一种面向参数化演化 PDE 的高效、稳定、可泛化的深度学习求解框架**。它通过 **meta-learning + Neural Galerkin + randomized sparse update** 三者结合，解决了传统 PINN 类方法在长时预测、泛化能力和计算效率上的瓶颈，在多个标准基准上展现出优越性能，尤其适合需要快速响应新参数配置的科学计算与工程仿真场景。

</details>

---

### 5. [TimeBill: Time-Budgeted Inference for Large Language Models](https://arxiv.org/abs/2512.21859)

**Authors**: Qi Fan, An Zou, Yehan Ma  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.21859v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in time-critical systems, such as robotics, autonomous driving, embodied intelligence, and industrial automation, where generating accurate responses within a given time budget is crucial for decision-making, control, or safety-critical tasks. H...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：TimeBill: Time-Budgeted Inference for Large Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在机器人、自动驾驶、具身智能和工业自动化等**时间敏感系统**中应用日益广泛。然而，由于其自回归生成机制，LLM 的端到端执行时间具有高度不确定性，难以满足硬实时系统的**时间预算约束**（time budget）。现有高效推理方法（如固定比例的 KV Cache 蒸馏）缺乏对动态任务和不同时间预算的适应能力，容易导致**超时失败**或**响应质量下降**。

### **提出的新方法与创新思路**
本文提出了 **TimeBill** —— 一种面向 LLM 的**时间预算化推理框架**，核心思想是通过**预测驱动的动态调控**来平衡推理效率与响应性能。其主要创新包括：

1. **细粒度响应长度预测器（Fine-grained Response Length Predictor, RLP）**  
   - 将响应长度预测建模为分类任务，基于 Small Language Model（SLM）构建预测器，避免 BERT 类模型对长输入支持差的问题。
   - 利用知识蒸馏策略使其与目标 LLM 对齐，提升预测准确性。

2. **工作负载引导的执行时间估计器（Workload-guided Execution Time Estimator, ETE）**  
   - 结合 **FLOPs 分析建模** 与 **实际性能剖析（profiling）**，实现对预填充阶段（prefill phase）和解码阶段（decoding phase）的精确执行时间估计。
   - 引入悲观因子 $k$ 来估计最坏情况执行时间（WCET），保障实时性。

3. **时间预算感知的高效推理机制（Time-Budgeted Inference）**  
   - 动态调整 KV Cache 的驱逐比例 $\alpha$，以确保在给定时间预算 $T$ 内完成推理。
   - 推导出最优驱逐比例 $\alpha^*$ 的闭式解，最大化响应性能的同时满足时间约束。

### **相比现有方法的优势**
| 方面 | 现有方法局限 | TimeBill 改进 |
|------|---------------|----------------|
| **时间可预测性** | 缺乏端到端执行时间建模 | 提出 RLP + ETE 实现高精度预测 |
| **灵活性** | 固定 KV 驱逐比例，无法适配不同任务/预算 | 动态调节 $\alpha$，适应多样化场景 |
| **性能-效率权衡** | AFAP 或固定压缩策略牺牲质量或可靠性 | 在满足时限前提下最大化响应质量 |
| **部署兼容性** | 多为离线优化，难在线调整 | 支持运行时决策，与量化等技术正交 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **主测试集**：`LongBench`（Bai et al., 2024），一个双语多任务长上下文理解基准。
- **训练集（用于 RLP）**：`Arena-Human-Preference-100k` 数据集中的提示，避免在测试集上训练。

### **实验设置**
- **目标模型**：`Qwen2.5-7B-Instruct`（context length: 32,768；最大生成长度 $N_{\text{max}} = 8,192$）
- **预测器模型**：`Qwen2.5-0.5B-Instruct` 作为 SLM 构建 RLP
- **硬件平台**：Intel Xeon Platinum 8350C + NVIDIA A40 GPU
- **KV Cache 驱逐实现**：采用 `SnapKV`（Li et al., 2024a）
- **默认悲观因子 $k = 5$**，最大驱逐比例 $\alpha_{\text{max}} = 95\%$

### **评估指标**
- **响应性能指标**：
  - F1 Score
  - ROUGE-L
  - Levenshtein Distance
- **系统级指标**：
  - **平均响应得分（Average Score）**
  - **任务完成率（Completion Rate）**：按时完成的任务占比
- **预测误差指标**：
  - MAE、RMSE、R²（响应长度预测）
  - MAPE（执行时间估计）

### **基线方法对比**
| 方法 | 描述 |
|------|------|
| **Vanilla** | 原始 LLM 推理，无任何优化 |
| **Fixed $\alpha$** | 固定 KV Cache 驱逐比例（25%, 50%, 75%, 95%） |
| **AWQ** | 激活感知权重量化至 4-bit（Lin et al., 2024） |
| **TimeBill（Ours）** | 本文提出的方法 |

### **超时处理策略（Overrun Strategies）**
- **Kill**：终止当前任务，视为未完成（输出为空）
- **Skip-Next**：跳过后续若干任务直到当前完成

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 响应长度预测性能（Table 1）**
| 方法 | #Buckets | MAE ↓ | RMSE ↓ | R² ↑ |
|------|----------|--------|---------|-------|
| ProxyModel (BERT-based) | 5 | 105.72 | 136.79 | 0.152 |
| S3 (DistilBERT-based) | 10 | 108.96 | 148.91 | -0.004 |
| **Ours (Regression)** | – | 64.21 | 103.30 | 0.516 |
| **Ours (Classification, B=64)** | 128 | 48.95 | 87.57 | 0.652 |
| **Ours (B=32)** | 256 | 44.15 | 78.63 | 0.719 |
| **Ours (B=16)** | **512** | **42.71** | **78.13** | **0.723** |

> ✅ **结论**：TimeBill 的 RLP 显著优于现有方法，且分类方式优于回归；桶数越多（粒度越细），性能越好。

#### **(2) 执行时间估计精度（Figure 5 & 6）**
- **MAPE（平均绝对百分比误差）**：
  - Prefill Phase: **1.22%**
  - Decoding Step: **1.69%**
- **端到端执行时间估计**：
  - $t_{e2e}$ 预测值与实测值高度吻合
  - $t_{wCET}$（带悲观因子的估计）有效提供上界，保障安全性

#### **(3) 与其他方法的对比（Figure 7）**
在多个时间预算（5~10 秒）下的表现：

| 方法 | 完成率 | 平均得分 |
|------|--------|----------|
| **Vanilla** | 很低（常超时） | 低 |
| **Fixed $\alpha=25\%$** | 较低 | 中等偏低 |
| **Fixed $\alpha=95\%$** | 高 | 明显下降（质量受损） |
| **AWQ** | 略高于 Vanilla | 略优 |
| **TimeBill** | **接近 $\alpha=95\%$ 的高完成率** | **显著更高（SOTA）** |

> ✅ **结论**：TimeBill 在保持高任务完成率的同时，实现了最高的平均响应质量。

#### **(4) 消融实验：悲观因子 $k$ 的影响（Figure 8）**
- 当 $k \in [1,5]$：增大 $k$ → 更保守调度 → 更多任务能按时完成 → 完成率和平均分上升
- 当 $k > 5$：过度保守 → 过高的 $\alpha^*$ → 响应质量严重下降 → 平均分回落
- **最佳 $k ≈ 5$**，符合硬实时系统常见实践

> 🔍 **说明**：$k$ 是可调参数，需根据应用场景权衡安全性和性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **细粒度响应长度预测至关重要**：传统粗粒度分类或回归方法误差大，基于 SLM 的分类式 RLP 可显著提升预测精度。
2. **FLOPs + Profiling 的混合建模更准确**：纯分析模型忽略硬件实现细节，纯学习模型缺乏可解释性，结合两者优势明显。
3. **动态 KV Cache 驱逐优于固定策略**：根据不同输入和时间预算自适应调整 $\alpha$，可在不牺牲太多质量的前提下大幅提升完成率。
4. **TimeBill 实现了性能与效率的最佳平衡**：在多种时间预算和超限策略下，均优于 Vanilla、固定压缩和 AWQ。

### **方法的局限性**
- **依赖离线建模与校准**：FLOPs 系数和 ETE 参数需要针对特定硬件和模型进行 profiling，迁移成本较高。
- **预测本身引入开销**：虽然可通过并行掩盖，但在极低延迟场景仍可能成为瓶颈。
- **仅考虑 KV Cache 驱逐**：未整合其他在线优化手段（如 early exiting、speculative decoding）。

### **未来工作方向**
- 将 TimeBill 与 **量化、稀疏化等离线压缩技术结合**，进一步降低延迟。
- 扩展至 **多模态 LLM 和具身 AI 场景**，支持更复杂的决策链。
- 探索 **无需额外预测模块的轻量级版本**，适用于边缘设备。
- 引入 **在线反馈机制**，根据历史执行结果动态调整悲观因子 $k$。

---

> **总结**：TimeBill 是首个系统性解决 LLM 时间预算化推理问题的框架，通过“预测 + 控制”闭环，在保证实时性的前提下最大化响应质量，为 LLM 在自动驾驶、机器人等安全关键领域的落地提供了重要支撑。

</details>

---

### 6. [nncase: An End-to-End Compiler for Efficient LLM Deployment on Heterogeneous Storage Architectures](https://arxiv.org/abs/2512.21571)

**Authors**: Hui Guo, Qihang Zheng, Chenghai Huo, Dongliang Guo, Haoqi Yang, Yang Zhang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.21571v1  

#### Abstract
The efficient deployment of large language models (LLMs) is hindered by memory architecture heterogeneity, where traditional compilers suffer from fragmented workflows and high adaptation costs. We present nncase, an open-source, end-to-end compilation framework designed to unify optimization across...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：nncase: An End-to-End Compiler for Efficient LLM Deployment on Heterogeneous Storage Architectures

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在异构存储架构上的高效部署面临三大挑战：
- **内存墙**：计算能力年增长100%，而内存带宽仅增长20%，数据移动成为瓶颈。
- **架构异构性**：不同设备具有不同的计算单元（如 Scalar、Vector、Matrix 单元）和复杂的内存层次（SRAM、HBM、DRAM）。
- **编译器碎片化**：传统编译器对统一与非统一内存架构（UMA/NUMA）采用不同优化流程，导致适配成本高、全局优化困难。

现有编译框架（如 TVM、MLC LLM、IPEX）难以在自动化程度、性能和跨平台适应性之间取得平衡。

---

### **提出的新方法与创新思路**

作者提出了 **nncase** ——一个开源的端到端编译框架，旨在统一异构存储架构下的 LLM 部署。其核心思想是“**统一分布式编译范式**”（Unified Distributed Compilation Paradigm），通过以下三大模块实现全局协同优化：

#### ✅ 创新点一：基于 e-graph 的 Term Rewriting 引擎
- 采用 **equality saturation** 技术构建非破坏性重写引擎，避免传统编译器中的 **phase ordering problem**。
- 在 e-graph 中同时保留多种等价程序表示，最终通过约束求解器（SAT/WPMAXSAT）选择最优路径。
- 支持多目标优化（延迟、内存占用、通信开销），结合 Roofline 模型进行代价建模。

#### ✅ 创新点二：Auto Vectorize 模块
- 提出 `MetaPackOperation` 和 `FoldNopPack` 规则，在 e-graph 内部生成并评估多种张量布局候选方案。
- 动态调整打包因子，消除冗余的 Pack/Unpack 操作，实现向量化与矩阵计算单元的最佳匹配。

#### ✅ 创新点三：Auto Distribution 模块
- 基于 **SBP 抽象**（Split, Broadcast, Partial）描述分布式策略，并将其嵌入 e-graph 结构中。
- 将分布策略搜索空间形式化为图内等价类合并问题，支持拓扑无关的并行策略自动搜索。
- 显式建模通信代价（Alpha-Beta 模型）和内存容量硬约束，防止 OOM 错误。

#### ✅ 创新点四：Auto Schedule 模块 + NTT 库
- 引入 **nncase Tensor Template (NTT) Library**，封装手工优化的高性能微核（ukernels）作为调度原子单位。
- 调度分为两层：
  - **结构优化**：使用 **Monte Carlo Tree Search (MCTS)** 探索循环融合与顺序。
  - **参数优化**：使用 **Mixed-Integer Nonlinear Programming (MINLP)** 求解 tile size 与 buffer placement。
- 实现寄存器级效率，同时大幅缩短搜索时间。

---

### **相比现有方法的优势**

| 维度 | 传统方法（TVM/MLC/IPEX） | nncase |
|------|--------------------------|--------|
| 编译流程 | 分阶段、割裂（layout → parallel → schedule） | 统一 e-graph 表示，联合优化 |
| 向量化支持 | 局部 kernel 级优化，易产生 layout thrashing | 全局探索，消除冗余转换 |
| 分布式策略 | 手动标注或分层搜索，忽略内存约束 | 自动搜索，显式建模通信与内存限制 |
| 调度粒度 | 标量级别，难达寄存器级性能 | 基于 ukernel，支持细粒度控制 |
| 编译时间 | 学习型方法耗时长（Ansor） | MCTS + MINLP 快速收敛 |

---

## 2. 核心实验方法和设置

### **使用的模型与数据集**

- **模型系列**：Qwen3 [56]
  - Qwen3-0.6B（支持 F32/F16）
  - Qwen3-1.7B（F16）
- **输入配置**：batch size = 1，prompt length = 8 tokens
- **任务类型**：自回归文本生成（解码阶段吞吐量测试）

---

### **实验平台**

- **硬件**：AMD Ryzen 9 5900X（12核），128GB DDR4-3600
- **操作系统**：Ubuntu 24.04 LTS
- **编译器**：GCC 14.2，启用 AVX2 指令集
- **单核 vs 多核测试**：分别运行 1T、4T、8T 配置以评估可扩展性

---

### **评估指标**

- **主指标**：Token 吞吐量（tokens/s）
- **次要指标**：
  - 编译时间
  - 内存占用
  - 可扩展性（Speedup）
- **测试次数**：每组实验重复 100 次取平均值

---

### **基线方法对比**

| 框架 | 类型 | 版本 |
|------|------|------|
| **llama.cpp** | 手工优化库 | Tag: b5753 |
| **Intel IPEX** | 工业级 PyTorch 扩展 | v28.0 |
| **MLC LLM** | 主流 DL 编译器 | Commit: 862a7311 |

> 所有框架均使用默认优化配置，最大化公平比较。

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

| 模型 | 配置 | nncase | llama.cpp | IPEX | MLC LLM |
|------|------|--------|-----------|-------|---------|
| Qwen3-0.6B | F32, 1T | **8.70** tokens/s | 10.61 | 7.58 | 0.20 |
| Qwen3-0.6B | F16, 1T | **13.87** tokens/s | 17.21 | 10.22 | — |
| Qwen3-1.7B | F16, 1T | **5.09** tokens/s | 6.30 | 4.20 | — |
| Qwen3-0.6B | F16, 4T | **23.50** tokens/s | 23.20 | 15.52 | — |
| Qwen3-0.6B | F16, 8T | **23.98** tokens/s | ~23.5* | ~16* | — |
| Qwen3-1.7B | F16, 4T | **8.85** tokens/s | 8.34 | 6.93 | — |

> *注：8T 下接近内存带宽极限，提升有限*

---

### **与基线方法的对比结果**

#### 🔹 单核场景（1T）
- nncase 在所有测试中显著优于 **IPEX** 和 **MLC LLM**。
- 相比手调库 **llama.cpp**，差距约 18%-30%，但在自动化编译器中表现最佳。
- 在 F16 上性能提升明显（+59%），体现其对低精度的良好支持。

#### 🔹 多核场景（4T/8T）
- **nncase 实现反超**：在 Qwen3-0.6B-F16 上，4T 性能略高于 llama.cpp（23.5 vs 23.2），8T 下仍保持领先。
- 对大模型 Qwen3-1.7B，4T 加速比达 **74%**（vs 1T），远高于 llama.cpp 的 32%，说明其并行效率更高。
- 成功突破传统共享内存并行（如 OpenMP）的同步开销瓶颈。

---

### **消融实验分析（文中隐含）**

虽然未明确列出消融表，但从设计逻辑可推断：
- 若关闭 **Auto Distribution**，将退化为普通多线程调度，失去静态划分优势。
- 若移除 **e-graph-based rewriting**，无法全局探索 layout 与 distribution 的组合空间。
- 若替换 MINLP 为启发式调优，可能错过最优 tile size 配置。
- NTT 库提供的 ukernel 是达成接近手写性能的关键基础。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **统一编译范式可行且高效**  
   nncase 证明了通过 NUMA 抽象统一 UMA/NUMA 架构的编译是可行的，“compile once, adapt everywhere” 可实现。

2. ✅ **e-graph 可有效支撑跨维度联合优化**  
   将 layout、distribution、scheduling 统一建模于 e-graph 中，实现了 algebraic transformation、data movement 与 memory hierarchy 的协同优化。

3. ✅ **自动化编译可达近手工性能**  
   在单核下接近 llama.cpp（差距 < 30%），在多核下甚至超越，表明自动化调度已具备实用价值。

4. ✅ **分布式思维可用于单机多核优化**  
   将多核视为“多节点”，利用 SBP + 静态通信调度，显著降低运行时开销，优于动态线程池（如 OpenMP）。

---

### **局限性**

1. 当前主要面向 CPU 平台（AVX2），尚未验证在 GPU 或 AI 加速器上的效果。
2. 编译时间虽优于学习型方法，但仍需进一步优化以满足实时部署需求。
3. 对非常规算子融合模式的支持依赖 NTT 库的手工实现，泛化能力受限。
4. 实验集中在推理阶段，训练场景未覆盖。

---

### **未来工作方向（作者明确提出）**

1. **Holistic Performance Characterization**  
   扩展评测范围至更多模型规模、硬件平台（如边缘设备）、部署场景（低延迟 vs 高吞吐）。

2. **Cross-Architecture Adaptation (SIMT Support)**  
   扩展后端以支持 SIMT 架构（如 GPU），增强 Auto Vectorize 与 Auto Schedule 对 warp-level 并行的支持。

3. **Computation-Communication Overlap**  
   实现计算与通信内核的自动融合与流水调度，通过依赖分析隐藏通信延迟，提升分布式吞吐。

---

## 总结

nncase 是首个将 **e-graph equality saturation**、**SBP 分布抽象** 与 **层次化调度（MCTS + MINLP）** 深度整合的 LLM 编译器。它不仅在性能上媲美手写库（llama.cpp），更展示了**自动化编译器在复杂异构环境下的巨大潜力**。其“统一分布式编译”理念为未来跨平台高效部署提供了新范式。

> 开源地址：https://github.com/kendryte/nncase

</details>

---

### 7. [BLEST: Blazingly Efficient BFS using Tensor Cores](https://arxiv.org/abs/2512.21967)

**Authors**: Deniz Elbek, Kamer Kaya  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.21967v1  

#### Abstract
Breadth-First Search (BFS) is a fundamental graph kernel that underpins a wide range of applications. While modern GPUs provide specialised Matrix-Multiply-Accumulate (MMA) units, e.g., Tensor Cores (TC), with extremely high throughput, they target dense operations, making it non-trivial to exploit ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BLEST: Blazingly Efficient BFS using Tensor Cores

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代 GPU 虽然配备了高吞吐的 **Tensor Cores (TC)**，但这些硬件单元专为密集矩阵运算设计，难以高效支持图算法中典型的**稀疏、不规则计算模式**。传统的 BFS（Breadth-First Search）在 GPU 上面临以下挑战：

- **负载不均衡**：图的不规则性导致不同 warp 处理的工作量差异大。
- **冗余计算**：传统 slice-set 方法采用“frontier-oblivious”调度，即使当前 frontier 为空也分配任务。
- **原子操作开销高**：顶点更新频繁且分散，导致大量非合并内存访问和原子冲突。
- **缓存局部性差**：更新的顶点 ID 分布广泛，降低 L1/L2 缓存命中率。
- **缺乏对 TC 的有效利用**：现有方法未能充分利用 TC 的并行能力，存在大量冗余的 MMA 调用。

### **提出了什么新方法或新思路**

作者提出 **BLEST**（Blazingly Efficient BFS using Tensor Cores），一个基于 TC 加速的高效 BFS 框架，其核心创新包括：

#### （1）**Binarised Virtual Slice Sets (BVSS)** 数据结构
- 将传统固定大小的 slice set 细化为更小粒度的 **Virtual Slice Set (VSS)**，每个 VSS 包含最多 `WARP_SIZE × slicesPerThread` 个 slices。
- 引入 `realPtrs` 和 `virtualToReal` 映射数组实现物理 slice set 与虚拟 VSS 之间的双向映射。
- **优势**：
  - 实现近乎完美的 **warp 级负载均衡**。
  - 支持 **frontier-aware 调度**，仅激活有非零 frontier 的 VSS，避免无效工作。

#### （2）**双图重排序策略**
- 针对不同类型图采用不同的顶点重排序：
  - **社交类图（如 Twitter）**：使用基于 Jaccard 相似性的窗口聚类（JACCARDWITHWINDOWS），提升 BVSS 中的边压缩率。
  - **非社交类图（如道路网络）**：使用 **RCM (Reverse Cuthill-McKee)** 降低带宽，减少 cache line 访问数量。
- 判断标准：若图满足“重尾分布”或“幂律行为”，则视为社交类图。

#### （3）**最优 TC 执行布局与批处理 SpMSpV 模式**
- 设计了一种新的 **bitwise TC tile 使用方式**，将 8×8 popcount 输出矩阵的所有 64 个条目都用于有效计算。
- 通过精心构造 `fragA`（连接性掩码）和 `fragB`（frontier 向量）的数据布局，使得单次 TC 调用可完成 64 个 dot-product。
- **相比 SOTA 方法（如 BerryBees）节省了 87.5% 的 TC 调用次数**（从 16 次降至 2 次 per VSS）。

#### （4）**延迟顶点更新机制（Lazy Vertex Update）**
- 将顶点更新分为两个阶段：
  1. **标记阶段**：异步标记被发现的顶点（使用轻量级 REDG 原子操作）。
  2. **最终化阶段**：在每层结束时批量处理所有标记，进行合并写入和 level 设置。
- **优势**：
  - 减少昂贵的 ATOMG 原子操作。
  - 提升 cache locality 和内存合并度。

#### （5）**内核融合（Kernel Fusion）**
- 利用 CUDA Cooperative Groups 实现设备端全局同步，将多层 BFS 循环融合进一个持久化 kernel。
- **消除主机侧同步开销**，特别有利于需要数千层遍历的道路网络。

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验涵盖两类基准：

- **GAP Benchmark Suite [5]**：包含多种真实世界图，如：
  - `GAP-road`, `GAP-twitter`, `GAP-web`, `GAP-kron`, `GAP-urand`
- **自定义大型图集合**：来自 SuiteSparse [12]，筛选条件为 |V| ≥ 2³⁰ 且 |E| ≤ 2³²−1，例如：
  - `nlpkkt240`, `uk-2005`, `it-2004`, `com-Friendster`, `webbase-2001`, `kmer_Vir`, `mawi`

共测试 **14 个大规模图**，覆盖社交、网页、科学计算等多种类型。

### **实验设置和评估指标**

- **硬件平台**：
  - CPU: 2×Intel Xeon Gold 6152 (共 88 线程)
  - GPU: **NVIDIA H200**（支持 compute capability 8.0 及以上，具备 m8n8k128 TC 指令）
- **软件环境**：
  - CUDA 13.0, g++ 12.3.0
- **评估指标**：
  - **运行时间（毫秒）**：平均 64 次随机源点 BFS 的执行时间。
  - **加速比（Speedup）**：相对于各 baseline 的几何平均加速倍数。
  - **消融实验**：分析各优化模块的独立贡献。
  - **内存占用**：报告 BVSS 结构、动态数组、level 数组等的 GPU 内存消耗。

### **基线方法对比**

- **GAP-BFS**：CPU 上的并行 BFS 实现。
- **Gunrock [40]**：主流 GPU 图分析框架，支持方向优化和负载均衡。
- **GSWITCH [26]**：基于模式自动调优的 GPU 图处理系统。
- **BerryBees [27]**：首个使用 TC 加速 BFS 的工作，也是本文最直接的比较对象。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 方法 | 平均速度提升（vs. BerryBees） | vs. Gunrock | vs. GSWITCH | vs. GAP-BFS |
|------|-------------------------------|-------------|--------------|--------------|
| **BLEST (full)** | **3.58×** | **4.64×** | **4.90×** | **13.25×** |

> 注：在 GAP benchmark 上对 BerryBees 达到 **3.63×** 加速。

### **与基线方法的对比结果**

- 在所有 14 个图上，BLEST 均优于 GAP、Gunrock 和 GSWITCH。
- 相比唯一使用 TC 的 **BerryBees**，BLEST 在多数图上显著领先，最高达 **8.90×**（`Spielman_k600`）。
- 即使在某些图上（如 `mawi`）表现略逊，整体仍保持明显优势。

### **消融实验结果**

论文评估了四个 BLEST 变体：

| 变体 | 描述 | 相对于 BerryBees 的平均加速 |
|------|------|-----------------------------|
| **BLEST (a)** | 基础版本（BVSS + 最优 TC 布局） | **1.89×** |
| **BLEST (ac)** | (a) + Lazy Update | **2.71×** |
| **BLEST (ab)** | (a) + Reordering | **1.98×** |
| **BLEST (full)** | 完整版本（a + b + c） | **3.58×** |

**结论**：所有三个优化（重排序、延迟更新、最优 TC 布局）均有显著贡献，组合后产生协同效应。

此外：
- **重排序有效性验证**：在自然顺序不利的图上（如 `GAP-web`），应用随机重排后，BLEST(ab) 比 BLEST(a) 快 **2.60×**。
- **窗口大小影响**：增大 JACCARDWITHWINDOWS 的窗口 `w` 可持续提升压缩率和性能，在 `GAP-web` 上最大带来 **2.01×** 加速。

---

## 4. 关键结论和发现

### **主要发现**

1. **Tensor Cores 可以高效用于稀疏图遍历**：通过将 BFS 表达为 batched SpMSpV，并巧妙映射到 TC 的 bitwise MMA 操作，实现了远超传统方法的性能。
2. **数据结构设计至关重要**：BVSS 成功解决了 warp 负载不均和 frontier-oblivious 调度问题，是性能提升的基础。
3. **图感知的预处理策略有效**：
   - 社交图适合社区感知重排序（提高压缩率）。
   - 道路图适合带宽缩减重排序（改善 cache 局部性）。
4. **延迟更新显著缓解原子瓶颈**：尤其适用于社交网络这类低直径、高并发 frontier 的场景。
5. **内核融合消除同步开销**：对于长路径图（如道路网）尤为重要。

### **方法的局限性**

1. **静态执行策略**：当前 BLEST 采用固定的优化组合，无法像 GSWITCH 那样动态切换策略。在少数图上（如 `mawi`, `Spielman_k600`）因误启 lazy update 导致性能下降。
2. **lazy update 开销敏感**：该机制引入 O(n) 每层扫描，虽在社交图上可接受，但在高层数图中可能成为瓶颈。
3. **重排序收益依赖于图结构**：部分图天然具有良好顺序，人工重排序反而可能破坏原有局部性。

### **未来工作方向**

1. **构建动态决策管道**：根据图特征和运行时状态智能选择是否启用 lazy update 或特定重排序策略。
2. **扩展至多源 BFS 应用**：如 closeness centrality、diameter computation 等。
3. **跨架构移植**：将 BLEST 移植到 AMD GPU（同样具备 MMA 单元）。
4. **动态执行流调控**：基于当前 frontier 特征实时调整调度和计算策略。

--- 

> ✅ **总结**：BLEST 是首个系统性地将 Tensor Cores 高效应用于 BFS 的工作，通过 **BVSS 数据结构、图感知重排序、最优 TC 布局、延迟更新和内核融合** 五大技术创新，在多种真实图上实现了 **3.5–13×** 的性能提升，标志着 TC 在图计算领域的重要突破。

</details>

---

### 8. [A Reinforcement Learning Approach to Synthetic Data Generation](https://arxiv.org/abs/2512.21395)

**Authors**: Natalia Espinosa-Dice, Nicholas J. Jackson, Chao Yan, Aaron Lee, Bradley A. Malin  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.21395v1  

#### Abstract
Synthetic data generation (SDG) is a promising approach for enabling data sharing in biomedical studies while preserving patient privacy. Yet, state-of-the-art generative models often require large datasets and complex training procedures, limiting their applicability in small-sample settings. In th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Reinforcement Learning Approach to Synthetic Data Generation

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的 **Synthetic Data Generation (SDG)** 方法（如 GANs 和 diffusion models）通常依赖大规模训练数据和复杂的训练过程，在小样本生物医学场景下表现不佳。此外：
- **GANs** 存在训练不稳定、模式崩溃（mode collapse）、梯度消失等问题；
- **Diffusion models** 虽然性能优越，但计算成本高、易记忆训练数据（memorization），存在隐私风险，且对小数据集泛化能力差。

这些问题限制了它们在真实世界中小型、高质量临床研究数据集上的应用。

### 🚀 提出的新方法：RLSyn
本文提出 **RLSyn** ——一种基于 **Reinforcement Learning (RL)** 的新型合成数据生成框架，将 SDG 重新建模为一个 **逆强化学习（inverse RL）问题**：
- 将生成器视为一个**随机策略（stochastic policy）**，输出整个患者记录的概率分布；
- 使用 **Proximal Policy Optimization (PPO)** 进行优化，通过判别器（discriminator）提供的奖励信号指导学习；
- 判别器仅作为“奖励提供者”，不参与生成器的梯度更新，避免了 GAN 中的对抗性最小最大博弈。

### 🔍 相比现有方法的优势
| 特性 | RLSyn | GANs | Diffusion Models |
|------|-------|------|----------------|
| **训练稳定性** | 高（PPO + clipped updates） | 低（非平稳损失） | 中等 |
| **数据效率** | 高（适合小样本） | 一般 | 低（需大数据） |
| **隐私保护** | 更好（减少记忆化） | 可控 | 差（易复制训练样本） |
| **多样性保障** | 显式建模方差（Gaussian 输出） | 容易模式坍塌 | 可能过拟合 |

> ✅ **核心创新**：首次系统地将 RL 应用于高维临床表格数据的合成生成，并证明其在小样本场景下的优越性。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
| 数据集 | 描述 | 样本量 | 特征数 |
|--------|------|--------|--------|
| **AI-READI** | 2型糖尿病多中心队列，含穿戴设备与 ECG 数据 | 4,537 person-days | 108（72连续 + 36分类） |
| **MIMIC-IV** | 急诊重症监护电子病历数据 | 180,746 patient stays | 180+（经phecode映射后） |

> 注：AI-READI 是较小的数据集，用于测试小样本性能；MIMIC-IV 是较大标准数据集。

### ⚙️ 实验设置
- **数据划分**：
  - AI-READI：90%/10% 训练/测试
  - MIMIC-IV：70%/30%
- **超参数调优**：使用 Optuna 进行 20 轮搜索，选择最优配置（见附录表 A.3）
- **重复实验**：每种模型在 10 种不同 train-test split 上运行并取平均值

### 📈 评估指标

#### （1）Privacy（隐私）
- **Membership Inference Attack (MIA)**：攻击者判断某真实个体是否参与训练。
- 指标：AUROC（越接近 0.5 表示隐私越好）

#### （2）Utility（实用性）
- **S2R（Synthetic-to-Real）分类任务**：
  - AI-READI：预测糖尿病严重程度
  - MIMIC-IV：预测一年内死亡率
- 指标：AUC（越高越好）

#### （3）Fidelity（保真度）
| 指标 | 含义 |
|------|------|
| **CWC (Column-wise Correlation Differences)** | 合成与真实数据相关性矩阵的平均绝对差异（越低越好） |
| **NMI (Normalized Mutual Information)** | 在 PCA + k-means 聚类后，聚类标签与“真实/合成”标签的相关性（越低表示分布越相似） |
| **DWD (Dimension-wise Difference)** | 单个特征层面的分布差异（分类用 APD，连续用 AWD） |
| **R2S (Real-to-Synthetic)** | 分类器在真实数据上训练，在合成数据上测试，检验统计一致性 |

### 🔁 基线方法对比
| 模型 | 类型 | 特点 |
|------|------|------|
| **EMR-WGAN** | GAN-based | 针对 EHR 设计的 WGAN-GP，强调隐私 |
| **EHRDiff** | Diffusion-based | 当前最先进的扩散模型，专为 EHR 优化 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）Privacy（表 1）
| 模型 | MIMIC-IV (AUROC) | AI-READI (AUROC) |
|------|------------------|------------------|
| Real Data | 0.998 | 1.000 |
| EMR-WGAN | 0.498 ± 0.002 | **0.501 ± 0.026** |
| EHRDiff | 0.499 ± 0.002 | 0.601 ± 0.036 |
| **RLSyn** | **0.499 ± 0.002** | **0.544 ± 0.030** |

> 💡 所有模型在 MIMIC-IV 上均接近随机猜测（理想状态）。但在 AI-READI 上，**EHRDiff 出现明显隐私泄露**（AUROC > 0.6），而 RLSyn 和 EMR-WGAN 更安全。

#### （2）Utility（图 2，S2R）
| 模型 | MIMIC-IV (AUC) | AI-READI (AUC) |
|------|----------------|----------------|
| EMR-WGAN | 0.764 | 0.747 |
| EHRDiff | 0.906 | 0.871 |
| **RLSyn** | **0.902** | **0.873** |

> ✅ RLSyn 在两个数据集上都显著优于 GAN，在小数据集 AI-READI 上略胜 diffusion。

#### （3）Fidelity（表 2）
| 指标 | 模型 | MIMIC-IV | AI-READI |
|------|------|----------|----------|
| **CWC ↓** | EMR-WGAN | 11.852 | 1.352 |
| | EHRDiff | 7.848 | **0.427** |
| | **RLSyn** | 8.877 | 0.750 |
| **NMI ↓** | EMR-WGAN | 0.320 | 0.809 |
| | EHRDiff | 0.003 | 0.002 |
| | **RLSyn** | **0.001** | **0.001** |
| **DWD ↓** | EMR-WGAN | 17.642 | 77.056 |
| | EHRDiff | 2.797 | 16.441 |
| | **RLSyn** | **2.073** | **13.352** |

> ✅ RLSyn 在 **NMI 和 DWD 上全面领先**，说明其生成的数据分布最接近真实数据。

#### （4）R2S 分析
- 在 AI-READI 上，**EHRDiff 的 R2S AUC（0.901）高于 R2R 基线（0.870）** → 表明其可能**过拟合或复制训练数据**；
- RLSyn 的 R2S 表现合理（0.868），无异常提升，说明未过度记忆。

#### （5）可视化分析（图 3 & 图 4）
- **PCA 散点图**显示 RLSyn 和 EHRDiff 的合成数据覆盖真实数据分布良好，而 EMR-WGAN 明显收缩；
- **APD 与 AWD 对比图**表明 RLSyn 更准确还原罕见疾病和连续变量分布。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **RLSyn 在小样本场景下显著优于现有方法**：
   - 在 AI-READI（仅 ~4.5k 样本）上，RLSyn 在 utility 和 fidelity 上均超过 EHRDiff 和 EMR-WGAN；
   - 同时保持较低隐私风险（AUROC ≈ 0.54），优于 EHRDiff（0.601）。

2. **RL 提供更稳定、更高效的学习机制**：
   - PPO 的 clipped objective 抑制了模式崩溃；
   - 显式建模输出分布（μ 和 σ）提升了样本多样性；
   - 非对抗式训练避免了 GAN 的梯度不稳定问题。

3. **Diffusion 模型存在记忆化倾向**：
   - EHRDiff 在小数据集上表现出更高的 MIA 攻击成功率和异常高的 R2S 性能，提示其可能“记住”而非“泛化”。

4. **GAN 表现最弱**：
   - EMR-WGAN 虽然隐私最好，但 utility 和 fidelity 极低，反映其未能有效学习数据分布。

### ⚠️ 局限性
1. **目前仅适用于横断面表格数据**（cross-sectional tabular data），尚未扩展到时间序列、文本或图像；
2. **机制解释不足**：尚不清楚是 PPO 还是策略参数化设计主导了性能优势；
3. **理论联系待深化**：与 Inverse RL、Energy-Based Models 的关系有待进一步探索；
4. **奖励函数固定**：当前仍依赖 discriminator 提供 reward，未来可尝试无判别器的统计指标奖励。

### 🔮 未来工作方向
1. 扩展至 **longitudinal EHR、clinical notes、medical imaging** 等复杂模态；
2. 探索 **替代 reward 函数**，例如基于 Wasserstein 距离、互信息或其他统计距离；
3. 引入 **privacy-aware reward shaping**，直接优化隐私-效用权衡；
4. 开发 **lightweight RL 架构**，降低部署门槛；
5. 深入研究 **RL 与生成模型的理论统一框架**（如与 GAN、diffusion 的联系）。

---

## ✅ 总结
**RLSyn 成功将强化学习引入合成医疗数据生成领域，提出了一种稳定、高效、隐私友好的新范式。它在小样本条件下显著优于 GAN 和 diffusion 模型，尤其适合资源有限的真实科研场景。该工作不仅提供了实用工具，也为下一代隐私保护型 AI 提供了新的方法论路径。**

</details>

---

### 9. [RLLaVA: An RL-central Framework for Language and Vision Assistants](https://arxiv.org/abs/2512.21450)

**Authors**: Lei Zhao, Zihao Ma, Boyu Lin, Yuhe Liu, Wenjun Wu, Lei Huang  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.21450v1  

#### Abstract
We present an RL-central framework for Language and Vision Assistants (RLLaVA) with its formulation of Markov decision process (MDP). RLLaVA decouples RL algorithmic logic from model architecture and distributed execution, supporting researchers in implementing new RL algorithms with minimal code, a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RLLaVA: An RL-central Framework for Language and Vision Assistants 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前多模态强化学习（Multi-modal Reinforcement Learning, RL）研究面临以下挑战：
- **缺乏专为视觉语言模型（VLMs）设计的RL框架**：现有RL框架（如veRL、OpenRLHF）主要面向纯文本大语言模型（LLMs），未充分考虑多模态数据传输、环境中的视觉反馈等特性。
- **算法逻辑与系统实现高度耦合**：大多数框架将RL算法与分布式训练策略紧密绑定，导致研究人员难以快速迭代新算法。
- **资源门槛高**：多数框架依赖大规模GPU集群，限制了资源有限的研究团队开展多模态RL实验。

### 🚀 提出的新方法与创新思路
作者提出 **RLLaVA** —— 一个以强化学习为中心的模块化框架，专为语言与视觉助手（Language and Vision Assistants）设计。其核心创新包括：

#### （1）统一的MDP建模
将视觉-语言联合决策过程形式化为统一的 **Markov Decision Process (MDP)**，状态空间 $S = (V \cup Z)^*$ 包含文本和图像，动作空间为token序列生成，奖励函数可灵活定义，为多种RL算法提供理论基础。

#### （2）三重解耦架构设计
RLLaVA采用模块化设计，实现三个维度的解耦：
- **算法逻辑（Algorithmic Logic）**
- **模型架构（Model Architecture）**
- **分布式执行（Distributed Execution）**

这种设计允许研究者在不修改底层系统的情况下插拔不同RL算法（如PPO、GRPO、OPO）、VLM模型（如Qwen-VL、Phi-VL）以及训练/推理引擎（如FSDP、DeepSpeed、vLLM、SGLang）。

#### （3）轻量级与资源高效训练
- 支持在单张24GB GPU上对 **4B规模模型进行端到端全参数更新训练**。
- 通过 **共址执行策略（co-located execution）** 复用GPU内存：rollout阶段将训练状态卸载至CPU；优化阶段关闭推理引擎，避免内存冲突。
- 集成LoRA、梯度检查点、动态批处理、无填充训练等技术进一步降低显存开销。

#### （4）可扩展的插件机制
支持通过配置文件切换不同的：
- Advantage Estimator（如GAE、GRPO、RLOO）
- Policy Loss（如PPO clip、GSPO、DAPO）
- Reward Function（规则奖励、学习型Reward Model、LLM-as-Judge）

---

### 🔍 相比现有方法的优势

| 维度 | RLLaVA | veRL / OpenRLHF | EasyR1 |
|------|--------|------------------|--------|
| 多模态原生支持 | ✅ 完整支持图像输入与环境交互 | ❌ 主要针对文本任务 | ✅ 支持但功能受限 |
| 算法-系统解耦 | ✅ 高度解耦，易于扩展 | ⚠️ 耦合较强，需深入系统理解 | ⚠️ 仅支持特定算法 |
| 小规模设备友好性 | ✅ 单卡24GB即可训练4B模型 | ❌ 需多卡甚至集群 | ⚠️ 未强调资源效率 |
| 插件灵活性 | ✅ 支持混合组件组合（如OPO + CLIP-COV） | ⚠️ 扩展成本高 | ❌ 固定流程 |

> ✅ 总结：RLLaVA填补了“易用、灵活、资源友好的多模态RL框架”这一空白，特别适合学术界和中小团队进行RL算法探索。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验覆盖五类多模态任务，涵盖感知、推理与智能体行为：

| 任务类别 | 数据集 | 说明 |
|---------|-------|------|
| **Mathematical Reasoning** | Geometry3K [24] | 基于图表的几何题求解 |
| **Counting** | CLEVR-Count-70k [13] | 组合式物体计数 |
| **Grounding** | RefCOCO/+/g [14,44,25] | 指代表达理解（定位图像中描述的对象） |
| **Agentic-Search** | MAT-Search [22] | 多轮视觉信息检索（结合网页搜索工具） |
| **Agentic-Coding** | MAT-Coding [22] | 图像操作相关的代码生成任务 |

此外，在 **LISA** [16] 上测试跨域泛化能力。

---

### ⚙️ 实验设置

| 项目 | 设置详情 |
|------|----------|
| **模型** | Qwen2-VL-2B, Qwen2.5-VL-3B, Qwen2.5-VL-7B |
| **训练方式** | 全参数微调（full-parameter fine-tuning）与 LoRA 微调 |
| **主算法** | GRPO（Group Relative Policy Optimization）作为默认RL算法 |
| **每提示响应数** | 4–8 个response per prompt |
| **训练引擎** | FSDP（Fully Sharded Data Parallel） |
| **推理引擎** | vLLM（用于rollout阶段高效生成） |
| **优势估计器** | GRPO自带group-normalized advantage estimation |
| **正则项** | KL-to-reference（相对于SFT模型）、entropy bonus等 |

---

### 📊 评估指标

| 任务 | 主要指标 |
|------|----------|
| Math | Accuracy |
| Counting | Accuracy |
| Grounding | IoU（Intersection over Union） |
| Agentic-Search / Coding | F1 Score（Simple / Hard / All） |

---

### 🆚 基线方法对比
- **Base Model**：未经RL微调的原始VLM（如Qwen2.5-VL-instruct）
- **GRPO (ours)**：使用RLLaVA训练的模型
- **Visual-ARFT [22]**：当前最先进的多模态agentic RL方法，使用专门架构与训练流程

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| Task | Model | Dataset | Metric | Base | GRPO (Ours) | Δ |
|------|-------|--------|--------|------|-------------|----|
| Math | Qwen2.5-VL-3B | Geometry3K | Accuracy | 35.1 | **39.0** | +3.9 |
| Counting | Qwen2.5-VL-3B | CLEVR-Count | Accuracy | 52.0 | **57.5** | +5.5 |
| Grounding | Qwen2-VL-2B | RefCOCO/+/g | IoU | 51.3 | **63.3** | +12.0 |
| Search | Qwen2.5-VL-3B | MAT-Search | F1 | 4.4 | **27.1** | +22.7 |
| Coding | Qwen2.5-VL-3B | MAT-Coding | F1 | 16.9 | **30.6** | +13.7 |

> 💡 **观察**：RL微调显著提升所有任务表现，尤其在**agentic任务**（Search、Coding）上提升巨大（>20 F1），表明RLLaVA有效增强了模型的工具使用与多轮推理能力。

---

### 🔄 泛化能力分析（Table 2：Visual Grounding on LISA）

| Model | RefCOCO | RefCOCO+ | RefCOCOg | **LISA (OOD)** |
|-------|--------|----------|-----------|----------------|
| Base (Qwen2-VL-2B) | 54.79 | 51.48 | 56.75 | **20.78** |
| GRPO (300步) | 67.14 | 60.43 | 61.43 | **31.88** |
| Δ | +12.35 | +8.95 | +4.68 | **+11.10** |

> ✅ 在**跨域数据集LISA**上仍取得大幅提升（+11.10 IoU），说明RL训练提升了语义理解和泛化能力，而非简单记忆训练模式。

---

### 🔬 与SOTA方法对比（Figure 2）

| 方法 | Agentic-Search (All F1) | Agentic-Coding (All F1) |
|------|--------------------------|--------------------------|
| Visual-ARFT-7B | 26.78 | **35.2** |
| **Ours (Qwen2.5-VL-3B-GRPO)** | **27.10** | 30.6 |

> ✅ 在**Agentic-Search**任务上，**3B模型超越7B的Visual-ARFT**（+0.32 F1），证明RLLaVA训练的有效性；
> ⚠️ 在**Agentic-Coding**上仍有差距，但相比base已大幅提升（+13.7），显示潜力。

---

### 🔍 消融实验（文中虽未单独列出表格，但从实现机制可推断）

- **共址执行策略**：启用后可在单卡24GB运行4B模型，否则OOM。
- **FSDP2 Offload**：默认开启，使大部分任务可在消费级GPU完成。
- **LoRA + Gradient Checkpointing**：显著减少可训练参数与峰值显存。
- **Advantage Estimator选择**：支持GRPO、RLOO、GAE等多种策略自由切换，验证了插件系统的有效性。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **RLLaVA实现了真正的“算法-模型-系统”三重解耦**，极大降低了多模态RL研究的技术门槛。
2. **RL微调能显著提升VLM在复杂任务上的表现**，尤其是在需要多轮交互、工具调用的agentic场景中效果突出。
3. **即使使用较小规模模型（3B）**，也能通过RL训练达到甚至超过更大模型（7B）在某些任务上的性能。
4. **良好的跨域泛化能力** 表明RL训练促进了深层语义理解，而非过拟合特定任务格式。

---

### ⚠️ 局限性

1. **目前主要验证基于GRPO算法**，其他RL算法（如OPO、RLOO）尚未全面比较其相对优劣。
2. **未支持完整的tool-use生态构建**：虽然可用于agentic任务，但缺乏内置的tool calling与environment simulation模块（作者已在Future Work中提及）。
3. **对超大规模模型（>7B）的支持有限**：尽管支持4B单卡训练，但更大模型仍需多卡或集群。
4. **reward modeling仍依赖外部定义**：自动构建高质量reward model仍是开放问题。

---

### 🔮 未来工作方向（原文Section 7）

1. 开发内置的 **tool-call 和 multi-turn interaction 模块**，增强VLM的代理能力。
2. 扩展至更多多模态agentic场景：
   - Pixel-level reasoning（像素级推理）
   - GUI-agent（图形界面代理）
   - Embodied AI（具身智能）
3. 持续集成新兴的 **RL算法与多模态架构**。
4. 鼓励社区贡献插件，推动框架生态发展。

---

## ✅ 总结

RLLaVA是一个**轻量、模块化、资源高效的多模态RL框架**，通过**MDP形式化建模**与**三重解耦架构设计**，解决了现有RL框架在多模态场景下的适配难题。实验证明其不仅能显著提升VLM在数学推理、视觉定位、代码生成等任务上的性能，还具备出色的泛化能力和与SOTA方法竞争的实力。它为资源受限的研究团队提供了强大的工具，有望推动多模态RL领域的普及与发展。

</details>

---

### 10. [HWL-HIN: A Hypergraph-Level Hypergraph Isomorphism Network as Powerful as the Hypergraph Weisfeiler-Lehman Test with Application to Higher-Order Network Robustness](https://arxiv.org/abs/2512.22014)

**Authors**: Chengyu Tian, Wenbin Pei  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22014v1  

#### Abstract
Robustness in complex systems is of significant engineering and economic importance. However, conventional attack-based a posteriori robustness assessments incur prohibitive computational overhead. Recently, deep learning methods, such as Convolutional Neural Networks (CNNs) and Graph Neural Network...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HWL-HIN: A Hypergraph-Level Hypergraph Isomorphism Network as Powerful as the Hypergraph Weisfeiler-Lehman Test with Application to Higher-Order Network Robustness

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

传统基于图结构的网络鲁棒性预测方法（如 GNNs）仅能建模**成对交互关系**，无法有效捕捉现实复杂系统中普遍存在的**高阶群体交互**（higher-order interactions）。尽管超图（hypergraph）能够自然地表示这些高阶关系，但现有的超图神经网络（HGNNs）在**拓扑表达能力**上存在瓶颈——其聚合函数（如均值池化）缺乏足够的区分力，无法达到理论上的最大表达能力上限。

此外，主流的后验鲁棒性评估依赖于攻击模拟，计算开销巨大，而现有深度学习代理模型在处理超图时仍受限于结构建模能力。

---

### **提出了什么新方法或新思路**

本文提出了一种全新的**超图层级超图同构网络框架**（**HWL-HIN**），其核心思想是将 **Graph Isomorphism Network (GIN)** 的设计理念扩展到超图领域，并严格对标 **Hypergraph Weisfeiler-Lehman (HWL) 测试** 的表达能力。

#### 主要创新点包括：

1. ✅ **首个具有严格等价于 HWL 测试表达能力的超图神经网络**
   - 理论证明 HWL-HIN 的表达能力与 Hypergraph WL 测试完全一致，达到了超图结构区分能力的理论上限。
   - 通过设计**双阶段可逆聚合机制**（node-to-hyperedge 和 hyperedge-to-node），并确保每个聚合函数满足**严格单射性**（injectivity），从而实现最大表达力。

2. ✅ **首次研究高阶网络中的连通性鲁棒性预测问题，考虑动态级联失效场景**
   - 引入了适用于超图的**负载分布模型**来模拟节点失效后的级联传播过程。
   - 将“失败顺序”作为输入特征之一，提升对动态行为的建模能力。

3. ✅ **端到端的超图层级读出机制（hypergraph-level readout）**
   - 设计了一个全局读出模块，融合所有层的节点和超边表示，形成最终的图级嵌入 $ H_{\text{graph}} $，用于回归任务（鲁棒性预测）。

4. ✅ **结合自适应积分生成高质量标签**
   - 使用 Adaptive Simpson 方法动态控制数值积分精度，以相对误差策略生成更精确且高效的鲁棒性标签，避免过度计算。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **表达能力** | 超越标准 HGNNs（如 HGNN、HyperGCN），理论上可区分任何被 HWL 测试判定为非同构的超图；优于基于展开法的图模型（如 bipartite GNNs）。 |
| **结构建模** | 直接操作原始超图结构，避免 clique/star expansion 导致的信息损失和计算膨胀。 |
| **效率** | 推理速度与主流 HGNN/GNN 相当，远快于矩阵型方法（如 SPP-CNN）。 |
| **泛化性** | 在混合拓扑数据集上表现优异，验证了跨结构的泛化能力。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

作者构建了 **6 个合成超图数据集**，每类包含 1200 或 2500 个样本，固定节点数 $ N = 200 $：

| 数据集 | 类型说明 |
|--------|----------|
| **ER (Erdős–Rényi)** | 随机连接，稀疏但连通，$ p = 0.05 $ |
| **WS (Watts-Strogatz)** | 小世界特性，初始环形连接 $ k_{\text{nn}} = 10 $，重连概率 $ p_{\text{rw}} = 0.5 $ |
| **SF (Scale-Free)** | 优先连接机制，幂律超度分布，每新增节点引入 $ m = 5 $ 条超边 |
| **SBM (Stochastic Block Model)** | 社区结构，$ C = 5 $ 个社区，内部连接概率 $ p_{\text{in}} = 0.1 $，外部 $ p_{\text{out}} = 0.01 $ |
| **UF (Uniform Random)** | 所有超边大小固定为 $ k = 5 $，测试对超边规模敏感性 |
| **MIX (Mixed Dataset)** | 混合前五种生成模型各 500 个样本，共 2500 训练 + 200 测试，用于评估泛化能力 |

---

### **实验设置和评估指标**

#### **任务目标**
- 回归任务：预测超图在节点移除攻击下的**鲁棒性值 $ R $**。
- 攻击类型分为两类：
  - **静态定向攻击**（static targeted attacks）：按超度从高到低逐个删除节点。
  - **动态级联攻击**（dynamic cascading attacks）：节点失效后触发负载重分配，引发连锁崩溃。

#### **鲁棒性定义（连续形式）**
$$
R = \int_0^1 s(p) \, dp
$$
其中 $ s(p) $ 是移除比例为 $ p $ 的节点后，最大连通分量（LCC）的相对大小。该积分通过 Adaptive Simpson 方法近似计算。

#### **评估指标**
- **平均绝对误差（MAE）** ± 标准差
- 每个算法在各数据集上的排名取平均作为综合性能参考
- 统计显著性检验：paired t-test ($ \alpha = 0.1 $)

#### **训练细节**
- 使用 AdamW 优化器 + **Cosine Annealing 学习率调度**
- 输入特征预归一化至 [0,1]
- 所有模型统一调参，公平比较

---

### **基线方法对比**

| 类别 | 基线方法 |
|-------|---------|
| **HGNN** | HGNNs（严格对照组） |
| **Graph-based GNNs** | GIN-MAS, NRL-GT（应用于二部关联图） |
| **Matrix-based DL** | SPP-CNN, ATTRP（作用于邻接矩阵图像） |
| **传统机器学习** | KNN, Decision Tree (DT) |

> 注：所有图模型均作用于由超图转换得到的**二部图**（incidence graph），存在信息损失风险。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table I）**

以下为 MAE 结果（越小越好），括号内为平均排名：

| Dataset | HWL-HIN | HGNNs | GIN-MAS | NRL-GT | SPP-CNN | KNN | DT |
|--------|--------|--------|--------|--------|--------|--------|--------|
| **ER (静态)** | **0.00282±0.00262 (1)** | 0.00303±0.00262 (2) | 0.00341±0.00354 (3) | ... | 0.03302±0.02568 (5) | 0.04817±0.04177 (8) | 0.04652±0.05734 (7) |
| **SF (静态)** | **0.00114±0.00200 (1)** | 0.00117±0.00200 (2) | 0.00127±0.00220 (3) | ... | 0.00216±0.00324 (5) | 0.00311±0.00405 (8) | 0.00295±0.00498 (7) |
| **UF (静态)** | **0.00287±0.00256 (1)** | 0.00314±0.00282 (2) | 0.00433±0.00388 (3) | ... | 0.04102±0.03326 (5) | 0.05497±0.07056 (7) | 0.07603±0.05086 (8) |
| **WS (静态)** | **0.00927±0.00830 (1)** | 0.01001±0.00890 (2) | 0.01018±0.00853 (3) | ... | 0.05746±0.03985 (5) | 0.06028±0.07044 (7) | 0.06754±0.08373 (8) |
| **SBM (静态)** | **0.00423±0.00401 (1)** | 0.00502±0.00470 (2) | 0.00546±0.00420 (3) | ... | 0.05869±0.03185 (5) | 0.07195±0.06682 (7) | 0.07775±0.09422 (8) |
| **MIX (静态)** | **0.00468±0.00672 (1)** | 0.00522±0.00641 (2) | 0.00770±0.00860 (3) | ... | 0.05980±0.04229 (5) | 0.10615±0.07861 (8) | 0.07315±0.09471 (7) |

> ✅ 在所有静态攻击场景下，**HWL-HIN 全面领先**，且统计显著。

#### 动态攻击结果趋势类似，但差距缩小：
- HWL-HIN 仍排名第一，但 HGNNs 性能接近。
- 原因：动态攻击中，“失败顺序”成为强先验特征，削弱了拓扑建模的重要性。

---

### **消融实验结果（见 Table II）**

| 消融项 | 影响分析 |
|--------|--------|
| **移除超边基数特征（Cardinality）** | HWL-HIN 性能轻微下降，HGNNs 下降明显 → 表明 HWL-HIN 更依赖结构而非显式特征 |
| **移除节点超度特征（Hyperdegree）** | 同上，HWL-HIN 更稳健 |
| **移除失败顺序（Failure Order）** | 在动态攻击中影响最大，两模型性能均下降，但 HWL-HIN 仍可学习 |
| **全特征消融（仅用邻接矩阵）** | **HWL-HIN 仍能收敛并保持一定性能；HGNNs 完全无法训练** → 证明 HWL-HIN 可从纯拓扑结构中学习，具备更强归纳能力 |
| **移除 Cosine Annealing 调度器** | 训练不稳定，收敛慢，尤其在 MIX 数据集上 → 验证优化策略有效性 |

---

## 4. 关键结论和发现

### **主要发现**

1. 🔹 **HWL-HIN 是目前表达能力最强的 HGNN 架构之一**，其理论表达力严格等于 Hypergraph WL 测试，能够最大程度地区分不同拓扑结构。
2. 🔹 **在依赖拓扑结构的任务中（如静态攻击预测），HWL-HIN 显著优于现有方法**，尤其是在 SBM、MIX 等复杂结构上优势明显。
3. 🔹 **HGNNs 高度依赖输入特征**，一旦去除特征即丧失学习能力；而 **HWL-HIN 具备从纯结构中提取知识的能力**，更具鲁棒性和泛化潜力。
4. 🔹 **图模型（GIN/NRL-GT）在超图任务中表现受限**，因其必须将超图转为二部图，导致高阶信息丢失，尤其在动态级联场景下性能退化严重。
5. 🔹 **矩阵方法（SPP-CNN/ATTRP）效率低下**，面临维度灾难和内存溢出（OOM）问题，难以扩展到大规模网络。
6. 🔹 **所有 ML 方法比真实仿真加速约百倍**，验证了代理模型在高效鲁棒性评估中的巨大潜力。

---

### **方法的局限性**

- ❗ 当输入特征非常强时（如已知失败顺序），HWL-HIN 的结构优势会被掩盖，性能增益有限。
- ❗ 目前仅验证于合成数据集，尚未在真实世界超图（如科研合作、生物通路）上测试。
- ❗ 模型复杂度略高于普通 HGNNs，参数更多，训练时间稍长（但仍可控）。

---

### **未来工作方向**

- 🚀 将 HWL-HIN 应用于其他超图层级任务，如超图分类、异常检测、社区发现等。
- 🚀 扩展至更大规模的真实超图数据集，探索迁移学习与预训练机制。
- 🚀 结合强化学习进行鲁棒性优化设计（robustness optimization）。
- 🚀 探索轻量化版本以进一步提升推理效率，适配边缘部署。

--- 

> **总结一句话**：  
> HWL-HIN 是首个理论表达力达标的超图神经网络，在鲁棒性预测任务中展现出卓越性能，标志着超图学习向“理论最优”迈出了关键一步。

</details>

---

### 11. [Knowledge Reasoning of Large Language Models Integrating Graph-Structured Information for Pest and Disease Control in Tobacco](https://arxiv.org/abs/2512.21837)

**Authors**: Siyu Li, Chenwei Song, Wan Zhou, Xinyi Liu  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.21837v1  

#### Abstract
This paper proposes a large language model (LLM) approach that integrates graph-structured information for knowledge reasoning in tobacco pest and disease control. Built upon the GraphRAG framework, the proposed method enhances knowledge retrieval and reasoning by explicitly incorporating structured...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对烟草病虫害防治领域中**大型语言模型（LLM）在复杂推理任务中表现不足**的问题展开研究。传统方法依赖专家经验或简单的知识注入，难以有效利用农业知识中的丰富关系结构，导致推理能力受限，尤其在多跳推理（multi-hop reasoning）和比较推理（comparative reasoning）场景下准确率较低。

### 提出的新方法/新思路
提出了一种基于 **GraphRAG 框架**、融合图结构信息的 LLM 推理方法，用于提升烟草病虫害控制领域的知识推理能力。其核心思想是将**结构化知识图谱（Knowledge Graph, KG）与大语言模型深度结合**，通过图神经网络（GNN）增强节点表示，并将其融入 LLM 的输入或中间层，实现更深层次的知识融合。

具体流程如下：
1. 利用 LLM 辅助构建**烟草病虫害知识图谱**（包含疾病、症状、防治方法等实体及其关系）；
2. 使用 **TransE** 进行图嵌入学习，捕捉实体与关系的语义；
3. 引入 **GCN（Graph Convolutional Network）** 聚合邻居信息，生成更具上下文感知能力的节点表示；
4. 将图表示与文本查询表示拼接后输入 **ChatGLM** 模型，进行问答生成；
5. 采用 **LoRA** 对 ChatGLM 进行参数高效微调。

### 相比现有方法的优势
- **更充分地利用知识图谱的结构信息**：不仅使用实体级信息（如 KGE），还通过 GCN 显式建模全局图结构，捕获高阶关系路径。
- **更强的复杂推理能力**：在 multi-hop 和 comparative reasoning 任务上显著优于仅依赖文本检索或简单知识注入的方法。
- **更高的准确性与可靠性**：通过结构化引导减少无关或冗余信息干扰，提升答案的相关性和完整性。
- **领域适配性强**：专为农业病虫害防治设计，具备实际应用潜力。

---

## 2. 核心实验方法和设置

### 数据集
- 自建了一个**烟草病虫害知识图谱**，包含超过 1,000 个实体和关系。
- 数据来源包括：农业专家知识库、病害管理手册、学术文献。
- 构建了配套的**问答数据集**，涵盖三类任务：
  - 直接问答（Direct QA）
  - 多跳推理（Multi-hop reasoning）
  - 比较推理（Comparative reasoning）

### 实验设置
- **图表示学习模块**：
  - TransE：嵌入维度 100，学习率 0.01
  - GCN：两层，ReLU 激活函数
- **语言模型**：
  - 主干模型：**ChatGLM**
  - 微调方式：**LoRA**（rank = 16），实现参数高效训练
- **融合机制**：
  - 在 GraphRAG 框架下，将 GCN 学得的图嵌入与 Sentence-BERT 编码的查询嵌入拼接，作为增强输入送入 ChatGLM

### 评估指标
采用标准分类与问答评价指标：
- **Accuracy（准确率）**
- **Precision（精确率）**
- **Recall（召回率）**
- **F1-score（F1 分数）**

### 基线方法对比
| 基线模型 | 描述 |
|--------|------|
| **ChatGLM** | 不使用任何外部知识的原始 LLM |
| **KGE + ChatGLM** | 使用 TransE 嵌入注入实体知识 |
| **RAG + ChatGLM** | 基于文本检索的标准检索增强生成 |
| **GraphRAG + ChatGLM** | 本文提出的方法（GCN + 图结构融合） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| Model | Accuracy (%) | Precision (%) | Recall (%) | F1-score (%) |
|-------|--------------|---------------|------------|---------------|
| ChatGLM | 75.2 | 78.5 | 72.1 | 75.2 |
| KGE + ChatGLM | 82.5 | 85.3 | 79.8 | 82.4 |
| RAG + ChatGLM | 85.7 | 87.9 | 83.2 | 85.5 |
| **GraphRAG + ChatGLM** | **90.1** | **92.3** | **88.2** | **90.2** |

### 与基线方法的对比结果
- 相比纯 LLM（ChatGLM），引入结构化知识使各项指标提升约 **15%**；
- 相比仅使用 KGE 注入的方法，GCN 建模图结构带来额外 **7.6% 的 Accuracy 提升**，说明图结构建模至关重要；
- 相比传统 RAG 方法，GraphRAG 在理解复杂查询和关联多个实体方面表现更优，**Accuracy 提高 4.4%**，表明图结构能更好支持逻辑推理；
- 在 multi-hop 和 comparative reasoning 任务中优势尤为明显，例如：
  - “哪些农药同时适用于烟草花叶病和蚜虫？” → 成功识别共用防治手段；
  - “生物防治 vs 化学防治哪种更有效？” → 可沿知识图路径推理得出综合判断。

### 消融实验分析（文中虽未单独列出表格，但有明确讨论）
- **移除 GCN**：性能下降明显，说明局部与全局邻域聚合对节点表征质量至关重要；
- **仅用 TransE**：虽优于无知识注入，但仍弱于 GCN 增强版本，验证了深层图结构建模的价值；
- **不使用图嵌入融合**：退化为普通 RAG 或 KGE 方法，推理深度和准确性降低。

---

## 4. 关键结论和发现

### 主要发现
- **图结构信息显著增强 LLM 的推理能力**：通过 GCN 学习的知识图谱表示能够有效补充 LLM 的语义盲区，特别是在需要跨实体推理的任务中。
- **GraphRAG 是有效的知识融合范式**：相比传统 RAG 依赖文本相似度，GraphRAG 利用显式的图结构进行知识检索与整合，提高了推理的准确性和可解释性。
- **该框架特别适合农业等专业知识密集型领域**：在病虫害诊断、防治策略推荐等任务中展现出强大潜力。

### 方法的局限性
- 当前构建的知识图谱规模较小（仅千级实体），可能限制模型泛化能力；
- 使用固定的 GCN 架构和图嵌入方式，缺乏动态更新机制；
- 图与 LLM 的融合仍停留在输入拼接层面，尚未实现端到端的联合优化；
- 实验集中在单一作物（烟草），需进一步验证在其他作物上的适用性。

### 未来工作方向
- 构建更大规模、覆盖更多作物的农业知识图谱；
- 探索更先进的 GNN 架构（如 GAT、Temporal GNN）以捕捉动态演化关系；
- 设计更高效的图-语言模型融合策略（如中间层交互、注意力门控）；
- 将该框架扩展至其他知识密集型领域，如医疗健康、智能决策系统等，检验其通用性。

> ✅ **总结一句话**：本论文提出的 **GraphRAG + ChatGLM** 方法通过深度融合图结构信息与大语言模型，在烟草病虫害控制的知识推理任务中实现了显著性能突破，尤其是在复杂推理场景下表现出卓越的准确性和深度，为农业智能化提供了新的技术路径。

</details>

---

### 12. [Demystifying ARM SME to Optimize General Matrix Multiplications](https://arxiv.org/abs/2512.21473)

**Authors**: Chencheng Deng, Weiling Yang, Jianbin Fang, Dezun Dong  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.21473v1  

#### Abstract
General Matrix Multiplication (GEMM) is a critical kernel in high-performance computing and deep learning. While modern architectures like ARM's Scalable Matrix Extension (SME) introduce dedicated hardware for matrix operations, existing linear algebra libraries fail to fully exploit its potential, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Demystifying ARM SME to Optimize General Matrix Multiplications*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对在 **ARM Scalable Matrix Extension (SME)** 架构上优化大规模 **General Matrix Multiplication (GEMM)** 的挑战，指出当前开源 GEMM 库（如 LIBXSMM、OpenBLAS、KleidiAI）存在以下关键瓶颈：
- **缓存利用率低**：简单三层循环设计导致子块无法有效匹配共享 L2 缓存，造成大量 cache miss 和 TLB 压力。
- **内存带宽未充分利用**：仅使用单个或两个 Z 寄存器进行加载，未能发挥 SME2 支持的多向量（multi-vector）高带宽加载能力。
- **数据打包策略不足**：多数库只对一个输入矩阵打包，另一个保持原始布局，当其超出 L2 缓存时性能急剧下降。

### 提出的新方法与创新思路
作者提出 **MPGEMM** —— 一个面向 ARM SME 架构的高性能、多精度开源 GEMM 库，其核心创新包括：

#### （1）系统化的架构特征分析
通过微基准测试（microbenchmarks）深入剖析 SME 的计算吞吐、内存带宽特性，得出三大优化原则：
- **最大化 ZA tile 利用率**：所有 micro-kernel 必须使用全部可用 ZA tiles（如 FP32 下为 4 个 ZA.S tiles）以达到峰值算力。
- **优化加载粒度**：采用 **四 Z 寄存器组加载**（four-Z-register load），可实现高达 900 GB/s 的内存带宽，远超单寄存器的 230 GB/s。
- **尊重缓存容量限制**：子矩阵应控制在 **8MB 工作集内**，否则带宽会骤降。

#### （2）Cache-Aware 分块算法
将传统的三重循环扩展为 **六级分块结构**，并基于解析模型自动确定最优分块参数 $mc, nc, kc$，综合考虑：
- L2 缓存大小
- TLB 容量
- 内存带宽约束  
目标是最大化 **compute-to-memory ratio (CMR)** 并保证良好的空间局部性。

#### （3）高效的双矩阵打包策略
- **Matrix A：on-the-fly transposition**  
  利用 ZA tile 的行列切片功能，在不引入 gather 操作的情况下完成行主序到列主序的高效转置。
- **Matrix B：first-round online packing**  
  在首次迭代中边执行 FMOPA 运算边将 B 打包进连续缓冲区，后续迭代复用该缓冲区，显著降低打包开销。

#### （4）高性能 micro-kernel 设计
- 主 micro-kernel 采用 **16×64** 形状（FP32），边缘 kernel 使用 64×16，确保始终能使用 **四个 Z 寄存器同时加载**。
- 引入 **软件流水（software pipelining）和循环展开** 来掩盖访存延迟，提升指令级并行度。
- 支持混合精度 GEMM（如 FP16→FP32, INT8→INT32），利用 SME 原生支持的高吞吐外积指令。

### 相比现有方法的优势
| 方面 | 现有方法缺陷 | MPGEMM 改进 |
|------|---------------|-------------|
| 缓存利用 | 固定分块，不适应 L2/TLB | 解析建模指导动态分块 |
| 数据访问 | 单/双 Z 加载，带宽受限 | 统一使用四 Z 加载，达 900 GB/s |
| 打包策略 | 仅打包一个矩阵 | 双矩阵打包 + 在线打包 |
| 并行化 | 多数不支持跨 SME 单元并行 | 动态任务调度至多个 SME 单元 |
| 开源透明 | Apple Accelerate 封闭 | 全开源，可复现 |

---

## 2. 核心实验方法和设置

### 实验平台
- **硬件**：Apple M4 Pro 芯片
  - P-core 集群：10 核 @ 4.4GHz，每个集群共享 16MB L2 缓存 + 1 个 SME 单元
  - E-core 集群：4 核 @ 2.85GHz，共享 4MB L2 缓存 + 1 个 SME 单元
  - 流式向量长度（SVL）固定为 512 bits（即每 Z 寄存器 64 字节）
- **操作系统**：macOS 15.1
- **编译器**：AppleClang 16

### 数据集与工作负载
来自真实大模型的 GEMM 配置：
- **DeepSeek 模型**：ID 1–18
- **LLaMA 模型**：ID 19–24  
涵盖多种形状（方阵、瘦长矩阵、胖短矩阵），尺寸从 $64\times2112$ 到 $4096\times32768$ 不等。

### 评估指标
- **性能指标**：GFLOPS / GOPS（浮点/整数运算每秒十亿次）
- **加速比**：相对于基线库的速度提升倍数
- **效率**：实测性能占理论峰值的比例（如 INT8 达到 94% 峰值）

### 基线方法对比
| 库名 | 是否开源 | SME 版本 | 精度支持 | 并行能力 |
|------|----------|-----------|------------|-------------|
| **Apple Accelerate** | ❌ 否 | SME2 | FP32/FP64 | ✅ 支持多 SME |
| **LIBXSMM** | ✅ 是 | SME2 | FP32/small GEMM | ❌ 无并行 |
| **KleidiAI** | ✅ 是 | SME2 | FP16/BF16/INT8 | ❌ 无并行 |
| **OpenBLAS** | ✅ 是 | SME（非 SME2） | FP32 | ❌ 无并行 |

> 注：BLIS 等其他库因缺乏 SME 支持被排除。

---

## 3. 主要实验结果和性能指标

### 性能对比（FP32，单 SME 单元）
- **平均加速比**：
  - vs. **OpenBLAS**：**2.85×**
  - vs. **KleidiAI**：**2.34×**
  - vs. **LIBXSMM**：**1.95×**
- **vs. Apple Accelerate**：
  - 行主序（Row-Major）：**1.21× 更快**
  - 列主序（Col-Major）：**1.18× 更快**

> 图 10 显示 MPGEMM 在绝大多数 workload 上均优于所有基线。

### 多 SME 单元并行性能
- 利用双 P-core 集群中的两个 SME 单元实现接近线性的扩展。
- **vs. Accelerate（多核）**：
  - 行主序：**1.24×**
  - 列主序：**1.22×**
- **vs. 其他开源库**（本身不支持并行）：
  - 平均加速比达 **3.96× (LIBXSMM)**、**4.69× (KleidiAI)**、**5.7× (OpenBLAS)**

### 双精度 GEMM（FP64）
尽管 SME 上 FP64 吞吐仅为 FP32 的 1/4（理论峰值 ~501 GFLOPS），MPGEMM 仍比 Accelerate 快 **1.18×**（多核下）。

### 不规则矩阵性能（Irregular Shapes）
- 测试 M/N ∈ {80, 110, 140, 170, 200}，K=25600
- MPGEMM 利用 predication 寄存器处理边界，并结合高效 edge kernel，持续领先于基线（图 13）

### 混合精度 GEMM 性能
| 类型 | 输入精度 → 输出精度 | 实测性能 | 相对 KleidiAI 加速比 |
|-------|------------------------|---------|------------------------|
| FP16 GEMM | FP16 → FP32 | 最高 ~2006 GFLOPS | **1.7×** |
| INT8 GEMM | INT8 → INT32 | 最高 **7882 GOPS** | **约 2×**（因 KleidiAI 使用 NEON 打包效率低） |
| 效率 | — | 达到 SMOPA 峰值的 **94%** | — |

> 图 14 显示混合精度下更高的 compute-to-memory ratio 带来更优实际性能。

### 消融实验（Ablation Study）
（图 15，以 LIBXSMM 为 baseline）
- **+ multi-vector load**：平均提升 **1.17×**
- **+ partition & dual-matrix packing**：平均提升 **1.62×**
- **+ online packing**：收益较小，因 FMOPA 周期不足以完全隐藏写回开销

> 结论：**分块与打包策略 + 四 Z 加载** 是性能提升的关键驱动力。

---

## 4. 关键结论和发现

### 主要发现
1. **SME 性能高度依赖内存访问模式**：能否使用四 Z 寄存器加载决定了是否能达到近 900 GB/s 的带宽；否则退化至 230 GB/s。
2. **传统三重循环不适合 SME**：必须重构为六级 blocked loop，结合解析模型选择最优分块参数。
3. **双矩阵打包 + 在线打包可显著改善缓存行为**：即使增加打包开销，也能通过提高空间局部性和 TLB 效率获得净收益。
4. **micro-kernel 形状至关重要**：16×64 比 32×32 更利于四 Z 加载，更适合大 GEMM。
5. **MPGEMM 在开放生态中首次逼近厂商闭源库性能**：在多个精度和场景下超越 Apple Accelerate。

### 方法的局限性
- **平台特定性强**：目前实现基于 Apple M4 Pro 的固定 SVL=512bit，需适配不同 SVL 的通用化版本。
- **并行调度依赖运行时环境**：macOS 不允许显式绑定线程与核心，影响细粒度控制。
- **堆内存分配要求严格**：需避免栈上分配以防 core 与 SME 内存顺序冲突引发 stall。

### 未来工作方向
- 扩展支持更多数据类型（如 BF16、INT4）及稀疏 GEMM。
- 探索自动调优框架（auto-tuner）替代解析模型，适应更多平台。
- 将优化思想迁移至其他 SME 加速算子（如 convolution、attention）。
- 支持跨集群更精细的任务调度机制。

---

> ✅ **总结一句话**：  
> MPGEMM 通过对 ARM SME 架构的深度剖析，提出了 cache-aware 分块、双矩阵在线打包、四 Z 寄存器加载等关键技术，在 Apple M4 Pro 上实现了对 Apple Accelerate 的全面反超（平均 1.23×），成为首个在大规模 GEMM 上充分发挥 SME 潜力的开源解决方案。

</details>

---

### 13. [Rethinking Output Alignment For 1-bit Post-Training Quantization of Large Language Models](https://arxiv.org/abs/2512.21651)

**Authors**: Dung Anh Hoang, Cuong Pham, Cuong Nguyen, Trung le, Jianfei Cai, Thanh-Toan Do  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.21651v1  

#### Abstract
Large Language Models (LLMs) deliver strong performance across a wide range of NLP tasks, but their massive sizes hinder deployment on resource-constrained devices. To reduce their computational and memory burden, various compression techniques have been proposed, including quantization, pruning, an...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rethinking Output Alignment For 1-bit Post-Training Quantization of Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文聚焦于 **1-bit Post-Training Quantization (PTQ)** 在大型语言模型（LLMs）中的应用挑战。尽管低比特量化能显著降低模型存储和计算开销，但现有的 **1-bit PTQ 方法在性能上通常出现严重退化**，尤其是当采用输出对齐（output alignment）策略时。

作者指出，当前主流方法多为 **weight-matching**（如最小化 $||W - \hat{W}||$），而更符合量化目标的 **output-matching**（即最小化 $||XW - X\hat{W}||$）却常表现不佳。其根本原因在于：

- 层级输出对齐不保证块级（block-level）误差下降；
- 量化误差在深层网络中累积，导致伪目标（pseudo target）偏离真实全精度输出；
- 无差别地进行输出对齐会破坏注意力机制中的 token 间关系。

### 提出了什么新方法或新思路
作者提出了一种 **数据感知的、选择性输出对齐策略**，核心创新包括：

#### （1）Selective Layer-wise Output Matching
- 不对所有层强制进行输出对齐，而是**仅在每个 Transformer block 的最后一个全连接层（Final FC）应用输出对齐**。
- 其余层仍使用 weight alignment（如 ARB-RC），以稳定中间表示并减少误差传播。

#### （2）修正优化目标以考虑激活误差累积
- 将传统基于量化后输入 $X$ 的输出重建损失 $||XW - X\hat{W}||$ 改为使用**全精度输入 $X$** 构建的目标：
  $$
  \mathcal{L}_{\text{out}} = ||XW - X\hat{W}||^2
  $$
- 这确保了优化目标始终逼近真实的 full-precision 输出，缓解了因前序层量化引入的激活失配问题。

#### （3）Attention Matrix Preservation (AMP)
- 引入一种新的掩码机制 AMP，用于保护注意力矩阵结构。
- AMP 通过计算 token 相似性矩阵（token-similarity matrix）相对于量化参数的梯度符号来生成 mask，并指导参数更新方向，防止注意力行为退化。
- 特别适用于 LLaMA 等依赖 RMSNorm 的架构，因其对表示方向敏感。

### 相比现有方法的优势
- **更有效的输出对齐**：显式建模误差累积，提升对齐质量；
- **保留注意力结构**：AMP 防止 token 关系被破坏，尤其在深层有效；
- **高效且实用**：无需重训练，仅需小规模校准集（如 C4），适合部署场景；
- **性能优于 SOTA**：在多个 LLM 架构和任务上一致超越 BiLLM、ARB-RC、ARB-X 等先进 1-bit PTQ 方法。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **校准数据集（Calibration Set）**：
  - `C4`：用于模型量化过程中的参数调整。
- **评估数据集**：
  - **语言建模任务**：`WikiText2`, `PTB`, `C4` —— 报告 **Perplexity (PPL ↓)**。
  - **下游零样本问答任务**：`ARC-Easy`, `ARC-Challenge`, `PIQA`, `BoolQ`, `HellaSwag`, `WinoGrande`, `OBQA`, `LAMBADA` —— 报告 **Accuracy (↑)**。

### 实验设置和评估指标
- **模型范围**：
  - OPT 系列：OPT-1.3B, 2.7B, 6.7B, 13B, 30B
  - LLaMA 系列：LLaMA-2-7B, 13B；LLaMA-3-8B
- **量化配置**：
  - 1-bit 权重量化（±1）
  - Block size 固定为 128
  - 序列长度 2048
- **评估指标**：
  - 主要指标：**Perplexity (↓)** 和 **Zero-shot Accuracy (↑)**
  - 消融实验分析：AMP 影响、误差目标比较、不同层应用效果等

### 基线方法对比
| 方法 | 类型 | 对齐方式 |
|------|------|----------|
| **Full Precision** | 原始浮点模型 | - |
| **PB-LLM** | PTQ | Weight Alignment |
| **BiLLM** | PTQ | Weight Alignment |
| **ARB-RC** | PTQ | Weight Alignment |
| **ARB-X** | PTQ | Output Alignment |
| **Ours** | 本文方法 | Selective Output Alignment + AMP |

所有基线均复现自原论文实现细节，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### ✅ OPT 模型（Table 1）
| 模型 | 方法 | C4 PPL | WikiText2 PPL | Avg QA Acc (%) |
|------|------|--------|--------------|----------------|
| OPT-13B | Full Precision | 12.06 | 10.13 | - |
| OPT-13B | ARB-RC | 15.07 | 13.10 | 55.01 |
| OPT-13B | ARB-X | 17.71 | 15.47 | 49.19 |
| OPT-13B | **Ours** | **14.71** | **12.84** | **55.06** |

> 在 OPT-1.3B 上，相比 ARB-RC 实现 **4.85 PPL 下降**，显示强鲁棒性。

#### ✅ LLaMA 模型（Table 2）
| 模型 | 方法 | C4 PPL | WikiText2 PPL |
|------|------|--------|--------------|
| LLaMA-2-7B | Full Precision | 7.26 | 5.47 |
| LLaMA-2-7B | ARB-RC | 25.87 | 16.25 |
| LLaMA-2-7B | ARB-X | 28.02 | 21.61 |
| LLaMA-2-7B | **Ours** | **19.25** | **15.42** |

> 在 LLaMA-2-13B 上，相比 ARB-RC 实现 **~6 PPL 改善**，且在 QA 任务上平均提升 **0.78%**。

---

### 与基线方法的对比结果
- 在所有测试模型和数据集中，**Ours 方法均优于所有 1-bit PTQ 基线**。
- 相比纯 weight alignment 方法（如 ARB-RC），本方法在保持稳定性的同时进一步降低输出误差；
- 相比 naive output alignment（如 ARB-X），本方法避免了注意力退化和误差累积问题，显著提升性能。

---

### 消融实验结果（Ablation Study）

#### 🔹 激活误差 vs 输出误差目标（Table 4）
| 模型 | Objective | C4 PPL | WikiText2 PPL |
|------|-----------|--------|-------------|
| LLaMA-2-7B | Activation-conditioned Error | 19.97 | 15.66 |
| LLaMA-2-7B | **Output Error (ours)** | **19.25** | **15.42** |

> 显式考虑真实输出目标带来约 **0.7 PPL 提升**。

#### 🔹 Attention Matrix Preservation (AMP) 的影响（Table 3）
| 模型 | 设置 | C4 PPL | WikiText2 PPL |
|------|------|--------|-------------|
| LLaMA-2-7B | No AMP | 29.12 | 26.24 |
| LLaMA-2-7B | **With AMP** | **19.25** | **15.42** |

> AMP 贡献巨大，尤其在 LLaMA 上，去除 AMP 导致 **PPL 恶化超 10 点**，说明其对注意力结构保护至关重要。

#### 🔹 不同层应用输出对齐的效果（Table 5）
| 层 | C4 PPL (LLaMA-2-7B) |
|----|--------------------|
| Query | 20.08 |
| Key | 20.80 |
| Value | 21.44 |
| Attn Out | 21.02 |
| **Final FC** | **19.25** |

> 表明将输出对齐应用于 **block 的最终全连接层**最为有效，验证了 selective alignment 设计的合理性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **Naive Output Alignment 并非总是有效**：
   - 虽然理论上更贴近量化目标，但在实践中由于误差累积和注意力干扰，反而可能导致性能下降。
2. **Block-Level Loss 更重要**：
   - 单纯优化 layer-wise 输出误差不能保证 block 或整体性能提升。
3. **Attention Mechanism 极易受破坏**：
   - 尤其是使用 RMSNorm 的 LLaMA 架构，对表示方向敏感，需要专门机制（如 AMP）加以保护。
4. **Selective + Corrective Output Alignment 是关键**：
   - 结合真实目标输出、选择性对齐最后一层、引入 AMP 掩码，三者协同可显著提升 1-bit 量化性能。

### 方法的局限性
- 当前方法仍局限于 **Transformer 中的 FFN 层**，未扩展到 Attention 权重或其他组件；
- AMP 机制引入额外计算（虽不影响推理），增加了量化阶段复杂度；
- 所有实验基于单卡 A100 完成，大规模分布式量化效率尚未验证；
- 未探索低于 1-bit（如 sub-1-bit）或混合精度方案。

### 未来工作方向
- 将 selective output alignment 扩展至 Attention 层；
- 探索动态 AMP mask 学习机制；
- 结合 QAT 思想，在极低位宽下实现微调增强；
- 推广到视觉-语言多模态大模型的极端量化；
- 研究更高效的 closed-form solver 以加速量化流程。

---

> **总结一句话**：  
> 本文重新思考了 1-bit PTQ 中 output alignment 的有效性，提出一种结合 **真实目标重构、选择性对齐、AMP 注意力保护** 的新框架，在多种 LLM 上实现了当前最优的 1-bit 量化性能，为极端压缩下的大模型部署提供了新思路。

</details>

---

### 14. [DuaDeep-SeqAffinity: Dual-Stream Deep Learning Framework for Sequence-Only Antigen-Antibody Affinity Prediction](https://arxiv.org/abs/2512.22007)

**Authors**: Aicha Boutorh, Soumia Bouyahiaoui, Sara Belhadj, Nour El Yakine Guendouz, Manel Kara Laouar  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.22007v1  

#### Abstract
Predicting the binding affinity between antigens and antibodies is fundamental to drug discovery and vaccine development. Traditional computational approaches often rely on experimentally determined 3D structures, which are scarce and computationally expensive to obtain. This paper introduces DuaDee...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DuaDeep-SeqAffinity: Dual-Stream Deep Learning Framework for Sequence-Only Antigen-Antibody Affinity Prediction

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统抗原-抗体亲和力预测方法严重依赖实验测定的三维（3D）结构，而这些结构获取成本高、耗时长，且在抗体-抗原复合物中（尤其是CDR-H3环）存在高度构象灵活性，导致基于结构的方法面临“**结构性瓶颈**”（structural bottleneck）。此外，AlphaFold2/3等结构预测工具在抗体-抗原对接任务上失败率仍高达约60%。

该研究旨在解决：**如何仅从氨基酸序列出发，高效、准确地预测抗原-抗体结合亲和力**，从而实现高通量筛选和加速治疗性抗体开发。

### 提出的新方法与创新思路
提出 **DuaDeep-SeqAffinity** ——一种全新的**双流深度学习框架**（Dual-Stream Deep Learning Framework），用于**纯序列**（sequence-only）的抗原-抗体亲和力预测。

其核心创新点包括：
- **双流混合架构设计**：采用并行的两个分支分别提取特征：
  - **Transformer 分支**：捕捉序列中的**长程全局上下文依赖关系**，理解蛋白质的整体折叠模式和稳定性。
  - **1D CNN 分支**：识别**局部序列模体**（local motifs），如结合热点区域的物理化学环境。
- **多尺度特征融合机制**：通过专门设计的融合模块整合来自Transformer和CNN的互补特征，形成更全面的表示。
- **基于ESM-2的预训练嵌入**：利用大规模预训练的Protein Language Model（PLM）——**ESM-2**生成高质量的上下文化残基嵌入，作为模型输入，无需手工特征工程。

### 相比现有方法的优势
| 对比维度 | DuaDeep-SeqAffinity | 其他主流方法 |
|--------|---------------------|-------------|
| **输入要求** | 仅需氨基酸序列（sequence-only） | 多数SOTA方法需3D结构（如WALLE-Affinity） |
| **计算效率** | 高效、可扩展，适合大规模筛选 | 结构建模与对接计算开销大 |
| **架构优势** | 显式分离全局与局部特征提取，避免信息丢失 | 单一Transformer或CNN难以兼顾多尺度特性 |
| **性能表现** | 在回归与分类任务上均超越SOTA | 序列模型受限于表达能力；结构模型受限于模板可用性 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用公开基准数据集 **AbRank**，包含经实验验证的抗原-抗体对及其结合亲和力（Kd值，单位为nM）。
- 每条记录包含抗原、抗体重链与轻链的氨基酸序列。
- 经过清洗后保留生物意义范围内的样本（$10^{-3} < \text{Kd} < 10^9$ nM），并将Kd转换为正相关的 **pKd = 9 - log₁₀(Kd)**。
- pKd进一步进行z-score标准化以稳定训练。

### 数据划分
- 随机划分为：**80% 训练集、10% 验证集、10% 测试集**
- 确保抗原与抗体序列在各集合间无重叠，评估模型对**未见配对组合**的泛化能力。

### 评估指标
- **回归任务指标**：
  - RMSE（Root Mean Square Error）
  - MAE（Mean Absolute Error）
  - $R^2$（Coefficient of Determination）
  - Pearson Correlation
  - Spearman Correlation
- **分类任务指标**：
  - AUC（Area Under the ROC Curve），用于评估排序能力（affinity ranking）

### 基线方法对比
- **内部基线**（消融实验）：
  - **ESM-C**：ESM-2 + CNN（仅局部特征）
  - **ESM-T**：ESM-2 + Transformer（仅全局特征）
- **外部SOTA对比**：
  - **DG-Affinity**（sequence-only, ConvNeXt-based）
  - **MVSF-AB**（sequence-only, multi-view CNN + MLP）
  - **WALLE-Affinity**（hybrid, structure + sequence）
  - **MINT** 和 **ESM-2 + AntiBERTy**（pure sequence, ranking-based）

所有模型均基于相同的数据预处理流程和ESM-2嵌入，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（测试集）

#### 回归性能对比（Table 1 & Table 2）

| Model | RMSE ↓ | MAE ↓ | $R^2$ ↑ | Pearson ↑ | Spearman ↑ |
|-------|--------|--------|---------|-----------|------------|
| ESM-C (CNN-only) | 0.7733 | 0.5722 | 0.397 | 0.636 | 0.614 |
| ESM-T (Transformer-only) | 0.7985 | 0.5822 | 0.357 | 0.625 | 0.603 |
| **DuaDeep (Ours)** | **0.7373** | **0.5116** | **0.460** | **0.688** | **0.680** |

> ✅ DuaDeep在所有指标上显著优于单分支变体，RMSE降低超过4.6%，Pearson提升约6–8个百分点。

#### 与其他SOTA方法对比（Table 2）

| Model | Input Type | Pearson | $R^2$ | RMSE |
|-------|------------|--------|--------|------|
| DG-Affinity [44] | sequence-only | 0.6556 | – | – |
| MVSF-AB [46] | sequence-only | – | 0.467 | 1.447 |
| **DuaDeep (ours)** | **sequence-only** | **0.688** | **0.460** | **0.737** |

> ⭐ 尽管MVSF-AB有相近的$R^2$，但其RMSE远高于DuaDeep（1.447 vs 0.737），说明DuaDeep预测更精确。

#### 排序性能（AUC）对比（Table 3）

| Model | Input Type | AUC ↑ |
|-------|------------|------|
| WALLE-Affinity (Ranking) [50] | structure + sequence | 0.866 |
| WALLE-Affinity (Regression) [50] | structure + sequence | 0.833 |
| MINT [49] | sequence-only | 0.775 |
| ESM-2 + AntiBERTy [53] | sequence-only | 0.761 |
| **DuaDeep (ours)** | **sequence-only** | **0.890** |

> 🎯 **DuaDeep以纯序列输入，AUC达到0.890，甚至超过了依赖3D结构的WALLE-Affinity（0.866）**，证明其强大的交互识别能力。

### 消融实验结果
- **双流架构有效性验证**：
  - CNN分支擅长捕捉局部结合特征（如CDR区理化性质），在回归任务中略优于Transformer。
  - Transformer分支更利于建模整体构象偏好，在早期收敛更快。
  - **两者融合后性能全面提升**，表明**全局与局部特征具有强互补性**。
- 学习曲线显示，DuaDeep具有更低的验证RMSE和平滑的收敛过程（图7），说明模型具备良好泛化能力和正则化效果。

---

## 4. 关键结论和发现

### 主要发现
1. **高质量序列嵌入足以替代3D结构信息**：
   - 利用ESM-2等PLM生成的上下文化嵌入已隐式编码了足够的结构与进化信息，能够支持高精度亲和力预测。
   
2. **多尺度特征提取至关重要**：
   - 抗原-抗体相互作用既涉及远程空间匹配（由Transformer捕获），也依赖局部接触界面的理化互补（由CNN识别），单一架构无法充分建模。

3. **双流融合策略显著提升性能**：
   - 并行处理+后期融合的设计有效整合了不同粒度的信息，在不引入额外结构输入的前提下，实现了对复杂结合信号的精准解码。

4. **序列优先范式正在逼近甚至超越结构辅助方法**：
   - DuaDeep在AUC上超越了使用真实结构的WALLE-Affinity，标志着**纯序列方法已成为可行且高效的替代方案**。

### 方法的局限性
- 当前模型尚未引入**交叉注意力机制**（cross-attention）来显式建模抗原与抗体之间的残基级相互作用。
- 虽然性能优异，但缺乏**可解释性模块**，难以提供具体的结合位点或关键突变建议。
- 所有实验基于pKd标准化后的连续值回归，未直接模拟实际应用场景中的**相对排序任务**（pairwise ranking）。

### 未来工作方向
1. 引入**bidirectional cross-attention**机制，增强抗原-抗体间的交互建模。
2. 采用**pairwise ranking loss**（如margin loss）优化模型在亲和力排序任务上的表现。
3. 开发**可解释性分析工具**，生成残基级别的贡献热图（interaction map），辅助抗体工程优化。
4. 扩展至**多特异性抗体**或**抗体人源化**等下游任务，推动个性化免疫治疗应用。

---

> 🔍 **总结一句话**：  
> DuaDeep-SeqAffinity通过创新的双流架构（Transformer + CNN）结合ESM-2嵌入，在**完全不需要3D结构**的情况下，实现了当前最优的抗原-抗体亲和力预测性能，不仅超越了同类序列模型，还击败了依赖结构信息的混合模型，为高通量抗体发现提供了高效、可扩展的新范式。

</details>

---

### 15. [Heaven-Sent or Hell-Bent? Benchmarking the Intelligence and Defectiveness of LLM Hallucinations](https://arxiv.org/abs/2512.21635)

**Authors**: Chengxu Yang, Jingling Yuan, Siqi Cai, Jiawei Jiang, Chuang Hu  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.21635v1  

#### Abstract
Hallucinations in large language models (LLMs) are commonly regarded as errors to be minimized. However, recent perspectives suggest that some hallucinations may encode creative or epistemically valuable content, a dimension that remains underquantified in current literature. Existing hallucination ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Heaven-Sent or Hell-Bent? Benchmarking the Intelligence and Defectiveness of LLM Hallucinations 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统研究将 LLM 的 **hallucination**（幻觉）普遍视为需要最小化的错误，主要通过事实一致性检测来衡量。然而，这种视角忽略了部分幻觉可能蕴含创造性或认知价值的可能性。本文提出：**并非所有幻觉都是有害的**，某些“智能幻觉”（Intelligent Hallucinations, IH）可能是科学创新的催化剂。

该研究旨在解决以下核心问题：
- 如何区分具有创造潜力的“智能幻觉”与纯粹错误的“缺陷幻觉”？
- 是否可以在抑制 DH 的同时保留甚至增强 IH？
- 如何系统评估 LLM 在开放科学任务中的创造性幻觉能力？

---

### 提出的新方法与新思路

#### （1）提出 **HIC-Bench**：首个融合创造力与幻觉分类的评估框架
HIC-Bench 是一个全新的 benchmark，其核心创新在于对幻觉进行二元划分：
- **Intelligent Hallucinations (IH)**：虽偏离现实，但在科学上合理、具备原创性和潜在价值。
- **Defective Hallucinations (DH)**：违反基本科学原理、逻辑矛盾或无实际意义的输出。

这一分类打破了“幻觉=错误”的单一范式，从人类创造性思维的角度重新审视 LLM 幻觉的本质。

#### （2）构建多维评估体系
结合 **Torrance Tests of Creative Thinking (TTCT)** 框架与幻觉分析维度，定义三个关键指标：
- **Originality (Or)**：新颖性，是否提出突破性想法
- **Feasibility (Fe)**：可行性，当前技术条件下是否可实现
- **Value (Va)**：价值，对学科发展的潜在影响

并引入两个幻觉维度：
- 科学合理性（Scientific Plausibility）
- 事实偏差（Factual Deviation）

最终提出综合指标 **Intelligent-Fidelity Score (IFS)**：
$$
\text{IFS} = w_1 \times \text{IH}_{ratio} + w_2 \times (1 - \text{DH}_{ratio} - \text{IH}_{ratio})
$$
其中 $w_1=0.6$, $w_2=0.4$，优先鼓励智能幻觉。

#### （3）设计 **Dynamic Hallucination Prompt (DHP)**
一种动态提示优化机制，基于实时反馈迭代更新正例（高 IFS 输出）和负例（DH 输出），引导模型生成更高质量的创造性响应。

---

### 相比现有方法的优势

| 维度 | 传统方法（如 TruthfulQA, UHGEval） | HIC-Bench |
|------|-------------------------------|-----------|
| 幻觉定义 | 单一负面标签（错误） | 双重分类（IH vs DH） |
| 评估目标 | 最小化幻觉 | 平衡创新与可靠性 |
| 任务类型 | 封闭问答、事实核查 | 开放式跨领域创新任务 |
| 创造力考量 | 忽略 | 显式建模（TTCT 指标） |
| 应用场景 | 安全敏感领域 | 科学探索与创新支持 |

> ✅ **优势总结**：HIC-Bench 首次实现了对 LLM “创造性幻觉”的量化评估，为开发“可控幻想”型 AI 提供理论基础和工具支持。

---

## 2. 核心实验方法和设置

### 数据集

#### （1）**Cross-Domain Innovation Dataset (CDID)**
- 包含 **100 个开放式创新任务**
- 覆盖 **10 个科学领域**：
  - Quantum Physics, AI, Biomedical Sciences, Environmental Science, Materials Science, Energy Technology, Neuroscience, ICT, Aerospace, Social Sciences
- 示例任务：
  > *“Design a novel transportation technology using quantum levitation and superconductivity to reduce urban traffic congestion.”*

每个任务生成 10 条响应，共收集 **6000 条样本**，来自 6 个主流 LLM。

#### （2）**Cross-Domain Knowledge Base (CDKB)**
- 基于 Wikipedia 内容提炼的核心概念数据库
- 用于 RAG 设置下的外部知识检索，确保背景一致性

---

### 实验设置

#### （1）选用的 LLM 模型
| 模型 | 特点 |
|------|------|
| `gpt-4o`, `gpt-4o-mini` | OpenAI 代表模型，前者通用强，后者轻量压缩 |
| `qwen2.5-14b`, `qwen2.5-72b` | 参数规模对比（14B vs 72B） |
| `deepseek-v3` | 强化学习微调的基础模型 |
| `deepseek-r1` | 专精 CoT 推理能力 |

#### （2）提示策略（Prompt Strategies）
| 策略 | 描述 |
|------|------|
| **SCP** (Strict Constraint Prompt) | 严格要求科学依据、逻辑严谨、避免推测 |
| **RCP** (Relaxed Constraint Prompt) | 放宽可行性限制，强调创新与价值 |
| **CoT** (Chain-of-Thought) | 添加 "Let’s think step by step" 提示 |
| **RAG** | 结合 CDKB 进行检索增强生成 |
| **DHP** | 动态更新正/负样例的提示工程 |

#### （3）评估方式
- **自动化评估**：使用 `gpt-4o` 和 `deepseek-v3` 作为 LLM judge，平均打分以减少偏见
- **人工验证**：专家对 IH/DH 分类进行 token-level 审查
- **评分标准**：采用 5 分制 Likert 量表，明确界定 Or/Fe/Va 的判据（见 Appendix A.2）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（基于 SCP 设置，温度=1.0）

| Model | Or↑ | Fe↑ | Va↑ | IH↑ | DH↓ | IFS↑ |
|-------|-----|-----|-----|------|------|--------|
| deepseek-v3 | 2.93 | 3.82 | 3.22 | 13.40% | 3.20% | 41.40% |
| gpt-4o | 2.85 | 3.86 | 3.14 | 9.60% | 1.30% | 41.40% |
| gpt-4o-mini | 2.62 | 3.73 | 2.98 | 2.90% | 5.00% | 38.58% |
| qwen2.5-14b | 2.54 | 3.65 | 2.96 | 5.20% | 8.70% | 37.56% |
| deepseek-r1 | **3.50** | 3.75 | **3.63** | **52.60%** | 1.20% | **50.04%** |

> 🔍 **观察**：`deepseek-r1` 在 Originality 和 IH 上显著领先，表明其在创造性推理方面具有独特优势。

---

### 与基线方法的对比结果（Table 2）

| Prompt | Model | IH | DH | IFS |
|--------|-------|-----|-----|--------|
| SCP | deepseek-r1 | 52.60% | 1.20% | 50.04% |
| RCP | deepseek-r1 | **71.10%** | 0.30% | **54.10%** |
| RAG | deepseek-r1 | 49.00% | **0.20%** | 49.72% |
| CoT | deepseek-r1 | 52.60% | **0.60%** | 50.28% |

#### 发现：
- **RCP** 极大提升 IH（+18.5pp），且未增加 DH → 支持“适度放松约束可激发创意”
- **RAG** 显著降低 DH，但也压制 IH → 存在 **可靠性与创造力的权衡**
- **CoT** 提升准确性（↓DH），但对 IH 影响不一（gpt-4o 下降）

---

### 消融实验结果（Table 3）——DHP 效果验证

| LLM | RCP | DHP | IH | DH | IFS |
|-----|-----|-----|-----|-----|--------|
| gpt-4o | — | — | 9.60% | 1.30% | 41.40% |
| gpt-4o | √ | — | 18.60% | 0.50% | 43.52% |
| gpt-4o | — | √ | 12.40% | **0.10%** | 42.44% |
| gpt-4o | √ | √ | **21.10%** | 0.30% | **44.10%** |

> ✅ **结论**：**DHP + RCP 组合效果最佳**，既大幅提升 IH，又有效控制 DH，实现 IFS 全面超越基线。

---

## 4. 关键结论和发现

### 主要发现

1. **IH 与 DH 并非正相关**  
   实验表明，减少 DH 不一定牺牲 IH；通过适当策略（如 RCP、DHP），可以**同时提升创造力并降低错误率**。

2. **创造性幻觉是可引导的资源**  
   IH 并非随机噪声，而是可通过提示工程（如 DHP）系统性增强的“高维认知输出”，有望成为科学发现的新引擎。

3. **模型架构影响 IH/DH 权衡**
   - `deepseek-r1` 展现出最强的 IH 生成能力（52.6%）
   - `gpt-4o-mini` 虽小但 DH 较高（5.0%），说明蒸馏可能削弱事实一致性
   - 大参数模型（如 qwen2.5-72b）在 Fluency 上表现更好

4. **领域敏感性明显**
   - 所有模型在 **Biomedical Sciences** 和 **Aerospace** 中表现出更高 Originality 和 IH
   - 在 **Environmental Science** 和 **Energy Technology** 中创造力较低

5. **IFS 是有效的平衡指标**
   - 在不同权重下（IIFS/BIFS/RIFS），能有效反映模型在“创新导向”与“精度导向”场景中的适用性

---

### 方法的局限性

1. **任务形式受限**  
   当前仅适用于结构化问题回答，尚未扩展至文学创作、艺术生成等更广义的创造性任务。

2. **人工评估有限**  
   仅对部分输出进行了人工审查，缺乏大规模人类偏好对齐研究。

3. **IH 定义仍具主观性**  
   尽管有 TTCT 指标支撑，但“有价值但不真实”的判断仍依赖 evaluator 的认知边界。

4. **伦理风险未充分讨论**  
   推动 IH 可能导致用户误将其当作事实，需警惕误导性应用。

---

### 未来工作方向

1. 扩展至 **multimodal** 和 **cross-lingual** 场景
2. 开发 **domain-specific prompts** 以精细化调控 IH/DH
3. 引入 **human-involved multi-dimensional evaluation** 提升 IH 判定可靠性
4. 探索 IH 在药物设计、材料发现等具体科研场景的应用落地
5. 建立 **ethical guidelines** 规范 IH 的使用边界

---

> 📌 **总体评价**：本论文提出了一个颠覆性的观点——“幻觉”不应被消灭，而应被驯服。HIC-Bench 为理解 LLM 的创造性本质提供了新范式，标志着从“追求准确”到“驾驭想象”的重要转向。

</details>

---

### 16. [Robust Federated Fine-Tuning in Heterogeneous Networks with Unreliable Connections: An Aggregation View](https://arxiv.org/abs/2512.22035)

**Authors**: Yanmeng Wang, Zhiwen Dai, Shuai Wang, Jian Zhou, Fu Xiao, Tony Q. S. Quek, Tsung-Hui Chang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.22035v1  

#### Abstract
Federated Fine-Tuning (FFT) has attracted growing interest as it leverages both server- and client-side data to enhance global model generalization while preserving privacy, and significantly reduces the computational burden on edge devices by avoiding training from scratch. Despite these advantages...

---

### 17. [Synthetic Financial Data Generation for Enhanced Financial Modelling](https://arxiv.org/abs/2512.21791)

**Authors**: Christophe D. Hounwanou, Yae Ulrich Gaba, Pierre Ntakirutimana  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.21791v1  

#### Abstract
Data scarcity and confidentiality in finance often impede model development and robust testing. This paper presents a unified multi-criteria evaluation framework for synthetic financial data and applies it to three representative generative paradigms: the statistical ARIMA-GARCH baseline, Variationa...

---

### 18. [From Visual Perception to Deep Empathy: An Automated Assessment Framework for House-Tree-Person Drawings Using Multimodal LLMs and Multi-Agent Collaboration](https://arxiv.org/abs/2512.21360)

**Authors**: Shuide Wen, Yu Sun, Beier Ku, Zhi Gao, Lijun Ma, Yang Yang, Can Jiao  
**Category**: cs.AI  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21360v1  

#### Abstract
Background: The House-Tree-Person (HTP) drawing test, introduced by John Buck in 1948, remains a widely used projective technique in clinical psychology. However, it has long faced challenges such as heterogeneous scoring standards, reliance on examiners subjective experience, and a lack of a unifie...

---

### 19. [Method Decoration (DeMe): A Framework for LLM-Driven Adaptive Method Generation in Dynamic IoT Environments](https://arxiv.org/abs/2512.21817)

**Authors**: Hong Su  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21817v1  

#### Abstract
Intelligent IoT systems increasingly rely on large language models (LLMs) to generate task-execution methods for dynamic environments. However, existing approaches lack the ability to systematically produce new methods when facing previously unseen situations, and they often depend on fixed, device-...

---

### 20. [Statistical vs. Deep Learning Models for Estimating Substance Overdose Excess Mortality in the US](https://arxiv.org/abs/2512.21456)

**Authors**: Sukanya Krishna, Marie-Laure Charpignon, Maimuna Majumder  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21456v1  

#### Abstract
Substance overdose mortality in the United States claimed over 80,000 lives in 2023, with the COVID-19 pandemic exacerbating existing trends through healthcare disruptions and behavioral changes. Estimating excess mortality, defined as deaths beyond expected levels based on pre-pandemic patterns, is...

---

### 21. [AnchorGK: Anchor-based Incremental and Stratified Graph Learning Framework for Inductive Spatio-Temporal Kriging](https://arxiv.org/abs/2512.21569)

**Authors**: Xiaobin Ren, Kaiqi Zhao, Katerina Ta\v{s}kova, Patricia Riddle  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21569v1  

#### Abstract
Spatio-temporal kriging is a fundamental problem in sensor networks, driven by the sparsity of deployed sensors and the resulting missing observations. Although recent approaches model spatial and temporal correlations, they often under-exploit two practical characteristics of real deployments: the ...

---

### 22. [Robustness and Scalability Of Machine Learning for Imbalanced Clinical Data in Emergency and Critical Care](https://arxiv.org/abs/2512.21602)

**Authors**: Yusuf Brima, Marcellin Atemkeng  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21602v1  

#### Abstract
Emergency and intensive care environments require predictive models that are both accurate and computationally efficient, yet clinical data in these settings are often severely imbalanced. Such skewness undermines model reliability, particularly for rare but clinically crucial outcomes, making robus...

---

### 23. [Leash: Adaptive Length Penalty and Reward Shaping for Efficient Large Reasoning Model](https://arxiv.org/abs/2512.21540)

**Authors**: Yanhao Li, Lu Ma, Jiaran Zhang, Lexiang Tang, Wentao Zhang, Guibo Luo  
**Category**: cs.AI  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.21540v1  

#### Abstract
Existing approaches typically rely on fixed length penalties, but such penalties are hard to tune and fail to adapt to the evolving reasoning abilities of LLMs, leading to suboptimal trade-offs between accuracy and conciseness. To address this challenge, we propose Leash (adaptive LEngth penAlty and...

---

### 24. [MoRAgent: Parameter Efficient Agent Tuning with Mixture-of-Roles](https://arxiv.org/abs/2512.21708)

**Authors**: Jing Han, Binwei Yan, Tianyu Guo, Zheyuan Bai, Mengyu Zheng, Hanting Chen, Ying Nie  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.21708v1  

#### Abstract
Despite recent advancements of fine-tuning large language models (LLMs) to facilitate agent tasks, parameter-efficient fine-tuning (PEFT) methodologies for agent remain largely unexplored. In this paper, we introduce three key strategies for PEFT in agent tasks: 1) Inspired by the increasingly domin...

---

### 25. [Dictionary-Transform Generative Adversarial Networks](https://arxiv.org/abs/2512.21677)

**Authors**: Angshul Majumdar  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.21677v1  

#### Abstract
Generative adversarial networks (GANs) are widely used for distribution learning, yet their classical formulations remain theoretically fragile, with ill-posed objectives, unstable training dynamics, and limited interpretability. In this work, we introduce \emph{Dictionary-Transform Generative Adver...

---

### 26. [Exploring the Heterogeneity of Tabular Data: A Diversity-aware Data Generator via LLMs](https://arxiv.org/abs/2512.21915)

**Authors**: Yafeng Tang, Xiaoou Ding, Jianzhuo Du, Zishuo Yan, Zhuang Ma, Zheng Liang, Zekai Qian, Hongzhi Wang  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.21915v1  

#### Abstract
Tabular data generation has become increasingly essential for enabling robust machine learning applications, which require large-scale, high-quality data. Existing solutions leverage generative models to learn original data distributions. However, real-world data are naturally heterogeneous with div...

---

### 27. [NEMO-4-PAYPAL: Leveraging NVIDIA's Nemo Framework for empowering PayPal's Commerce Agent](https://arxiv.org/abs/2512.21578)

**Authors**: Ali Sahami, Sudhanshu Garg, Andrew Wang, Chaitanya Kulkarni, Farhad Farahani, Sean Yun-Shiuan Chuang, Jian Wan, Srinivasan Manoharan, Uma Kona, Nitin Sharma, Linsey Pang, Prakhar Mehrotra, Jessica Clark, Mark Moyou  
**Category**: cs.AI  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21578v1  

#### Abstract
We present the development and optimization of PayPal's Commerce Agent, powered by NEMO-4-PAYPAL, a multi-agent system designed to revolutionize agentic commerce on the PayPal platform. Through our strategic partnership with NVIDIA, we leveraged the NeMo Framework for LLM model fine-tuning to enhanc...

---

### 28. [Hyperion: Low-Latency Ultra-HD Video Analytics via Collaborative Vision Transformer Inference](https://arxiv.org/abs/2512.21730)

**Authors**: Linyi Jiang, Yifei Zhu, Hao Yin, Bo Li  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21730v1  

#### Abstract
Recent advancements in array-camera videography enable real-time capturing of ultra-high-definition (Ultra-HD) videos, providing rich visual information in a large field of view. However, promptly processing such data using state-of-the-art transformer-based vision foundation models faces significan...

---

### 29. [RefineBridge: Generative Bridge Models Improve Financial Forecasting by Foundation Models](https://arxiv.org/abs/2512.21572)

**Authors**: Anthony Bolton, Wuyang Zhou, Zehua Chen, Giorgos Iacovides, Danilo Mandic  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21572v1  

#### Abstract
Financial time series forecasting is particularly challenging for transformer-based time series foundation models (TSFMs) due to non-stationarity, heavy-tailed distributions, and high-frequency noise present in data. Low-rank adaptation (LoRA) has become a popular parameter-efficient method for adap...

---

### 30. [Why Smooth Stability Assumptions Fail for ReLU Learning](https://arxiv.org/abs/2512.22055)

**Authors**: Ronald Katende  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.22055v1  

#### Abstract
Stability analyses of modern learning systems are frequently derived under smoothness assumptions that are violated by ReLU-type nonlinearities. In this note, we isolate a minimal obstruction by showing that no uniform smoothness-based stability proxy such as gradient Lipschitzness or Hessian contro...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
