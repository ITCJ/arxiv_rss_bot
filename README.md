# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-30 05:58:31 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Argus: Token Aware Distributed LLM Inference Optimization](https://arxiv.org/abs/2512.22925)

**Authors**: Panlong Wu, Yifei Zhong, Danyang Chen, Ting Wang, Fangxin Wang  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2512.22925v1  

#### Abstract
Large Language Models (LLMs) are rapidly being integrated into real-world applications, yet their autoregressive architectures introduce significant inference time variability, especially when deployed across heterogeneous edge-cloud systems. Existing solutions largely neglect the dynamic, stochasti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Argus: Token Aware Distributed LLM Inference Optimization**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
大型语言模型（LLMs）在边缘-云混合系统中的推理面临三大挑战：
- **推理时间高度可变**：由于LLM采用自回归架构，输出token长度随输入语义复杂度剧烈变化，导致推理延迟波动显著。
- **动态与随机环境**：真实场景中请求流量、客户端活跃状态和网络条件均时变且不可预测。
- **异构设备环境**：边缘与云端服务器计算能力、通信延迟和精度表现差异大，资源分配复杂。

现有方法大多忽略token生成的动态特性，仅静态优化某一时刻性能，无法应对长期随机性和异构性。

---

### **提出了什么新方法或新思路**
作者提出 **Argus**，首个**token感知的分布式边缘-云LLM推理框架**，其核心由两个模块构成：

#### **(1) Length-Aware Semantics (LAS) 模块**
- 利用一个微调后的预训练语言模型（ModernBERT），结合**特征重校准机制**（受Squeeze-and-Excitation启发），预测每个输入prompt的预期输出token长度。
- 引入**token-length-sensitive特征调制**，增强模型对“详细解释”或“简要列出”等指令的敏感度。
- 实现**推理前精准负载估计**，为后续任务调度提供细粒度画像。

#### **(2) Lyapunov-guided Offloading Optimization (LOO) 模块**
- 将长期QoE（Quality-of-Experience）优化建模为带长期约束的随机优化问题。
- 基于Lyapunov优化理论，将原问题分解为每时隙的子问题。
- 提出 **Iterative Offloading Algorithm with Damping and Congestion Control (IODCC)**：
  - 动态构建成本矩阵，包含基础开销（通信+计算）与拥塞惩罚项。
  - 迭代求解整数线性规划（ILP），实现负载均衡。
  - 引入**阻尼更新机制**防止震荡，提升收敛稳定性。

---

### **相比现有方法的优势**
| 维度 | Argus优势 |
|------|----------|
| **建模精细度** | 显式建模LLM自回归生成过程，考虑prefill与decode阶段及token长度影响 |
| **适应性** | 支持动态、随机、异构环境下的长期稳定优化 |
| **效率与性能** | 低复杂度算法（IODCC）逼近最优解，优于强化学习等高开销方法 |
| **实用性** | 在真实LLM请求轨迹上验证，具备工程落地潜力 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- 使用来自 **Alibaba Bailian** 平台的真实LLM查询日志（[3] ServeGen）作为请求trace。
- 包含多个客户端在不同时间段的请求模式，反映真实世界的工作负载波动。

---

### **实验设置**
- **系统架构**：异构边缘-云系统，包含 `N` 台边缘服务器和 `U` 台云服务器。
- **任务类型**：3类任务（轻量、中等、重量级），对应不同计算需求。
- **模型规模**：
  - 小模型：prefill需2单位，decode需1单位
  - 大模型：prefill需8单位，decode需4单位
- **时间跨度**：共 `T=100` 个时隙
- **资源异构性**：
  - 边缘服务器算力 `f_e ∈ [2.5, 5]`
  - 云服务器算力 `f_c ∈ [5, 7.5]`
- **通信与精度**：
  - 边缘延迟低但精度较低 `[0.1, 0.5]`
  - 云端延迟高但精度高 `[0.6, 1.0]`

---

### **评估指标**
- **主指标**：**Lyapunov Reward**，综合衡量QoE（延迟、准确率）与长期约束满足情况（如队列稳定性）。
- **辅助指标**：
  - Token预测误差（L1 Loss）
  - 可训练参数数量（用于比较模型轻量化程度）
  - 不同配置下的性能增益（消融实验）

---

### **基线方法对比**
#### **Token预测部分基线**
- **LoRA**：主流高效微调方法
- **LSTM**：经典序列模型
- **Transformers**：标准注意力架构
- **Qwen2.5-7B**：强大LLM直接用于预测

#### **任务卸载部分基线**
- **Greedy_Accuracy**：优先选择最高精度设备
- **Greedy_Compute**：优先选择最强算力设备
- **Greedy_Delay**：优先选择最低延迟路径
- **TransformerPPO**：基于Transformer + PPO的RL方法 + Lyapunov约束
- **DiffusionRL**：扩散模型增强的RL方法 + Lyapunov约束

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表 I & II：不同服务器数量下的性能对比**
| 算法 | U=15 (N=4) | U=20 (N=4) | N=15 (U=6) | N=20 (U=6) |
|------|------------|------------|------------|------------|
| **Ours (Argus)** | **36,602** | **30,757** | **21,080** | **28,961** |
| Greedy-Accuracy | -265,297 | -213,876 | -219,696 | -278,269 |
| Greedy-Compute | -267,139 | -154,887 | -181,459 | -125,089 |
| Greedy-Delay | 13,565 | 4,052 | 7,235 | 10,928 |
| TransformerPPO | -4,050,226 | -3,928,943 | -5,642,526 | -76,411 |
| DiffusionRL | 32,077 | 25,511 | 14,823 | 17,939 |

> ✅ **结论**：Argus在所有配置下均取得**最高Reward值**，远超各类贪婪策略与先进RL方法。

---

#### **图4a：Token预测精度（L1 Loss）**
| 方法 | L1 Loss |
|------|--------|
| **LAS (ours)** | **91.85** |
| LoRA | 92.07 (+0.2%) |
| LSTM | 107.79 (+17.3%) |
| Transformers | 106.69 (+16.15%) |
| Qwen2.5-7B | 176.93 (+92.6%) |

> ✅ LAS在更低参数量下实现了**最精确的token长度预测**。

---

#### **图4b：可训练参数量**
- **LoRA**: 8.75M 参数
- **LAS**: 仅引入 **0.09M** 激活参数（↓99%）

> ✅ LAS更轻量、更适合部署在边缘侧。

---

#### **表 III：消融实验 — 是否启用token预测器**
| 配置 | 含预测器 | 无预测器 | 提升幅度 |
|------|---------|---------|----------|
| N=4, U=6 | 10,808 | 5,409 | **+99.8%** |
| N=4, U=8 | 14,324 | 11,519 | +24.3% |
| N=4, U=10 | 18,339 | 14,457 | +26.9% |

> ✅ **token长度预测对整体性能至关重要**，尤其在资源紧张时提升接近翻倍。

---

## **4. 关键结论和发现**

### **主要发现**
1. **token长度是影响LLM推理调度的关键因素**，忽略该变量会导致严重次优决策。
2. **LAS模块能以极低开销实现高精度token预测**，通过语义特征重加权机制捕捉长度相关提示。
3. **LOO + IODCC框架能在动态环境中实现近似最优的长期QoE优化**，同时保证系统稳定性（满足长期计算约束）。
4. **相比RL方法，IODCC无需大量训练，收敛快、稳定性好**，更适合实际部署。
5. **真实trace验证表明，Argus在多种资源配置下均显著优于state-of-the-art基线**。

---

### **方法的局限性**
- **依赖预训练模型进行token预测**：若输入领域与训练数据偏差较大，可能影响预测准确性。
- **假设任务只能卸载到单一设备**：未考虑跨设备流水线并行（如prefill在云、decode在边）。
- **未显式建模KV Cache复用或内存限制**：虽然提及PagedAttention等工作，但未将其集成进调度决策。

---

### **未来工作方向**
1. 扩展支持**多阶段流水线卸载**（split inference）。
2. 结合**KV Cache管理机制**（如PagedAttention、CacheGen）进一步降低传输开销。
3. 探索**在线自适应token预测器**，适应不同用户行为漂移。
4. 将框架扩展至**多模态大模型**（如LLaVA）的边缘-云协同推理。

---

> **总结**：Argus首次将LLM的**token生成动态性**纳入边缘-云协同推理优化框架，提出**LAS + LOO + IODCC**三位一体方案，在理论保障与实验效果上均展现出卓越性能，为下一代分布式LLM服务提供了重要设计范式。

</details>

---

### 2. [TL: Automatic End-to-End Compiler of Tile-Based Languages for Spatial Dataflow Architectures](https://arxiv.org/abs/2512.22168)

**Authors**: Wei Li, Zhenyu Bai, Heru Wang, Pranav Dangi, Zhiqiang Zhang, Cheng Tan, Huiying Lan, Weng-Fai Wong, Tulika Mitra  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2512.22168v1  

#### Abstract
Spatial dataflow accelerators are a promising direction for next-generation computer systems because they can reduce the memory bottlenecks of traditional von Neumann machines such as CPUs and GPUs. They do so by organizing computation around explicit, compiler-managed data movement over the on-chip...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TL: Automatic End-to-End Compiler of Tile-Based Languages for Spatial Dataflow Architectures — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
空间数据流架构（**spatial dataflow architectures**）虽然在能效和吞吐量上潜力巨大，但由于其编程复杂性高，性能严重依赖于计算任务到硬件的映射方式（mapping），目前主要依赖厂商提供的手工优化库（如 TTNN）。这种依赖限制了程序的可移植性和新算子的开发效率。

传统编译器通常只关注单个 tile 内的代码生成，而忽略了 tile 实例在分布式核心间的调度、数据复用和片上网络（NoC）通信等跨核问题。

### 🚀 提出的新方法：TL 编译框架
论文提出了 **TL**，一个端到端的编译框架，用于将基于 tile 的语言（如 Triton）自动编译到空间数据流架构上。其核心创新包括：

- **统一的硬件抽象模型（Hardware Representation）**  
  引入基于 MLIR 的 `df` Dialect，对多级硬件结构进行建模：
  - **Scale-out 架构**：核心阵列拓扑、NoC 连接方式
  - **内存层次**：本地 scratchpad（L1）、DRAM 通道
  - **Intra-core 计算单元**：矩阵单元（mat）、向量单元（vec）
  支持多种架构（如 2D mesh、1D ring），提升可移植性。

- **端到端的时空映射（Spatiotemporal Mapping）**  
  将逻辑 tile 网格映射到物理核心阵列和时间维度：
  - 空间维度：决定 tile 分布在哪些核心上并行执行
  - 时间维度：决定 tile 如何分批（waves）随时间调度
  支持灵活的数据复用策略（如广播、缓冲重用）。

- **数据复用分析与通信规划（Data Reuse & Movement Planning）**  
  - 分析访问模式中的 **空间复用**（多个 core 共享同一数据）和 **时间复用**（同一 core 多次使用）
  - 自动生成高效的 NoC 广播方案（broadcast）替代重复的全局内存加载
  - 支持 loop hoisting 来提升缓存利用率

- **性能模型驱动的候选选择机制（Top-k Selection）**  
  使用轻量级性能模型预估不同 mapping 的执行时间，选出 top-k 候选，再通过实际 profiling 确定最优配置，平衡编译开销与最终性能。

### 🔍 相比现有方法的优势
| 方面 | 现有方法（如 TTNN） | TL |
|------|------------------|-----|
| 映射策略 | 固定模板（如 TT-1D / TT-2D） | 自动搜索大规模 mapping 空间 |
| 数据复用 | 手工设计 | 编译器自动识别并优化空间/时间复用 |
| 可移植性 | 绑定特定架构 | 基于 `df` 抽象支持多架构 |
| 新算子支持 | 需手动实现 | 支持任意 tile-based kernel |
| 开发成本 | 高（专家调优） | 低（自动化流程） |

---

## 2. 核心实验方法和设置

### 💻 实验平台
- **硬件**：Tenstorrent Wormhole n300d 加速卡（双 socket，每 socket 64 Tensix cores @ 1GHz，峰值 64 TFLOPs）
- **内存**：每 socket 96MB SRAM + 12GB GDDR6（带宽 288GB/s）

### ⚙️ 架构目标（Logical Configurations）
为验证泛化能力，测试三种逻辑配置：
1. **1×8 Ring**（1D 环形）
2. **4×8 Mesh**（非对称矩形）
3. **8×8 Mesh**（标准对称网格）

### 🧪 工作负载（Workloads）
- **GEMM**：通用矩阵乘法，覆盖多种形状（M, N, K ∈ [256, 16384]）
- **FlashAttention**：Transformer 中的关键算子，具有复杂的访存模式

### 📐 评估指标
- **性能（Throughput）**：TFLOP/s
- **加速比（Speedup）**：相对于基线（TTNN）的性能提升
- **DRAM 访问减少率**：衡量数据复用效果
- **预测准确性**：性能模型 vs 实测性能误差

### 🔄 基线方法对比
- **TTNN**：Tenstorrent 官方库，采用两种固定数据流模板：
  - **TT-1D**：一维广播
  - **TT-2D**：二维波前式 systolic 流水
- TL 在相同条件下自动生成 mapping，并与 TTNN 及其子模板比较。

---

## 3. 主要实验结果和性能指标

### 📈 GEMM 性能结果
- 在 **8×8 mesh** 上，TL 相比 TTNN 实现：
  - **几何平均加速比 1.03×**
  - 在 **62.8% 的配置下优于 TTNN**
  - 最高提速达 **2.19×**
- 在不规则形状下表现更优：
  - 当 `N ≈ M` 时，2D 广播更有利；当 `N << M` 时，1D 更优
  - TL 能自动选择最佳策略，而 TTNN 因固定策略导致误判（例如在 N=1024 时错误选用 2D 模板）

### ⚡ FlashAttention 性能结果
- TL 在所有测试配置下均显著优于 TTNN：
  - **平均性能提升 1.91×**
  - 最高达 **2.0×**
- 关键原因：TL 自动实现了 key 数据的 on-chip 复用，大幅减少了 DRAM 重载次数，而 TTNN 默认 mapping 存在大量冗余访问。

### 🔬 消融实验（Ablation Studies）

#### （1）空间复用（Spatial Reuse）的影响
| 配置 (M=K=N) | 仅从 DRAM 加载 | 启用 TL 空间复用 | 加速比 |
|-------------|---------------|------------------|--------|
| 1024        | 11.15 TFLOP/s | 23.70 TFLOP/s     | 2.12× |
| 2048        | 16.77         | 33.96            | 2.03× |
| 4096        | 22.96         | 40.44            | 1.76× |

- **平均减少 70% DRAM 访问**
- 对小规模问题收益明显（内存受限），大问题趋于计算瓶颈后增益下降

#### （2）时间复用（Temporal Reuse）的影响
- 在 M/N 较大、K 较小时，通过 loop hoisting 缓冲 A/B tile，避免重复加载
- 最高带来 **1.12× 加速**
- 效果随 M/N 增大而增强

#### （3）性能模型准确性
- 预测性能与实测性能的 **几何平均误差为 17%**
- 能准确捕捉 **memory-bound 与 compute-bound 的转换趋势**
- 即使预测不准，也能有效筛选出高质量候选（见 top-k 实验）

#### （4）Top-k 选择策略的影响
| top-k | 8×8 Mesh 几何平均性能（相对 TTNN） | 编译时间（秒） |
|-------|-------------------------------|--------------|
| 1     | -6.5%                         | 5.31         |
| 2     | -0.7%                         | 10.54        |
| 5     | +2.8%                         | 26.42        |

- **top-2 即可消除大部分性能差距**
- 小 k 值即可获得接近最优的结果，适合实际部署

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **编译器可以媲美甚至超越手工优化库**  
   TL 在 GEMM 和 FlashAttention 上分别达到 **1.03× 和 1.91×** 于 TTNN，证明自动化 mapping 是可行且高效的。

2. **固定数据流模板存在局限性**  
   不同算子形状和硬件拓扑需要不同的最优 mapping，**静态模板无法适应多样性**，而 TL 的动态搜索更具优势。

3. **空间与时间复用是性能关键**  
   - 空间复用（NoC 广播）可减少高达 70% 的 DRAM 访问
   - 时间复用（loop hoisting）在特定形状下进一步提升性能

4. **轻量级性能模型足以指导高质量 mapping 搜索**  
   不需精确模拟，只需相对排序能力即可有效缩小候选空间。

5. **硬件抽象层极大提升了可移植性**  
   通过 `df` Dialect 描述硬件，TL 可轻松适配不同空间架构（如 1D ring、2D mesh）。

### ⚠️ 局限性
- **依赖前端 affinization**：要求 Triton 等 DSL 提供 affine 表达式，否则需额外 pass 处理
- **小规模 kernel 性能不佳**：受硬件启动开销影响，模型未完全建模此类 overhead
- **缺乏对稀疏性和动态控制流的支持**：当前聚焦 dense tensor ops
- **未开放完整硬件细节**：部分参数通过 microbenchmark 反推，可能引入误差

### 🔮 未来工作方向
- 扩展支持更多 tile-based DSL（如 CuTile、Taichi）
- 支持稀疏张量和动态调度
- 结合硬件设计探索（co-design），用于芯片早期架构评估
- 探索更精细的性能建模（如考虑缓存冲突、NoC 拥塞）
- 支持跨芯片（multi-chip）扩展 mapping

---

## 总结
TL 是首个实现从 tile-based 高层语言到空间数据流架构端到端自动编译的框架。它通过引入 **硬件抽象（df Dialect）**、**时空映射搜索** 和 **性能模型引导的 top-k 选择机制**，成功实现了媲美甚至超越厂商手工库的性能，同时具备良好的可移植性和自动化程度。该工作为下一代空间计算架构的编程自动化提供了重要范式。

</details>

---

### 3. [GPU-Virt-Bench: A Comprehensive Benchmarking Framework for Software-Based GPU Virtualization Systems](https://arxiv.org/abs/2512.22125)

**Authors**: Jithin VG, Ditto PS  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.22125v1  

#### Abstract
The proliferation of GPU-accelerated workloads, particularly in artificial intelligence and large language model (LLM) inference, has created unprecedented demand for efficient GPU resource sharing in cloud and container environments. While NVIDIA's Multi-Instance GPU (MIG) technology provides hardw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GPU-Virt-Bench: A Comprehensive Benchmarking Framework for Software-Based GPU Virtualization Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
随着AI和大语言模型（LLM）推理等GPU加速工作负载的快速增长，云环境和容器化平台对高效共享GPU资源的需求日益迫切。然而，当前主流的硬件级虚拟化技术（如NVIDIA MIG）仅限于高端数据中心GPU（如A100、H100），无法在更广泛的GPU型号上部署。

软件层面的虚拟化方案（如HAMi-core）虽然支持更多GPU类型，但缺乏统一、系统化的评估标准，导致其性能、隔离性和适用场景难以量化比较。因此，该领域存在一个显著的“**Benchmarking Gap**”——即缺少标准化、可复现且覆盖多维度的评测框架。

### 提出的新方法与创新思路
本文提出了 **GPU-Virt-Bench**，一个面向软件型GPU虚拟化系统的综合性基准测试框架，具有以下五大核心贡献：

1. **构建了一个包含56个指标的完整度量体系（Taxonomy）**  
   将GPU虚拟化质量划分为10个类别：
   - Overhead（开销）
   - Isolation（隔离性）
   - LLM（大模型相关性能）
   - Memory Bandwidth（内存带宽）
   - Cache Behavior（缓存行为）
   - PCIe Throughput（主机通信）
   - NCCL/P2P Communication（多GPU通信）
   - Scheduling Efficiency（调度效率）
   - Memory Fragmentation（内存碎片）
   - Error Recovery（错误恢复）

2. **开源实现了一个模块化、可扩展的基准测试工具链**  
   支持多种虚拟化后端（Native、HAMi-core、BUD-FCSP、MIG-Ideal模拟），代码公开于GitHub（https://github.com/BudEcosystem/GPU-Virt-Bench），便于社区复现与扩展。

3. **提供了针对LLM推理的关键专项测试**  
   包括Attention Kernel吞吐、KV Cache分配速度、Token生成延迟（TTFT/ITL）、动态批处理影响等，填补了现有基准在AI应用层的空白。

4. **提出了一套基于MIG理想行为的评分机制（Scoring Methodology）**  
   引入“MIG Parity”概念，将各指标得分归一化并与理想MIG表现对比，计算总体分数并赋予字母等级（A+/B/C等），使不同系统间具备横向可比性。

5. **实证评估了两种主流软件虚拟化系统（HAMi-core 和 BUD-FCSP）**  
   揭示了它们在真实负载下的性能特征，为生产部署提供决策依据。

### 相比现有方法的优势
| 对比项 | 传统方法（MLPerf/Rodinia/厂商工具） | GPU-Virt-Bench |
|-------|-------------------------------|----------------|
| 聚焦目标 | 原始算力性能 | **虚拟化特性本身的质量** |
| 可复现性 | 多为闭源或定制脚本 | **完全开源、参数可配置** |
| 测试维度 | 单一维度（如吞吐） | **10类共56项指标全面覆盖** |
| LLM适配性 | 缺乏针对性设计 | **专设LLM-001~LLM-010系列指标** |
| 隔离性评估 | 几乎无 | **系统化测量跨租户干扰、公平性、QoS一致性等** |

---

## 2. 核心实验方法和设置

### 实验平台配置
- **GPU**: NVIDIA A100-40GB PCIe
- **CPU**: AMD EPYC 7742 (64核)
- **内存**: 512GB DDR4-3200
- **存储**: NVMe SSD
- **操作系统**: Ubuntu 22.04 LTS
- **CUDA版本**: 12.0
- **驱动版本**: 525.105.17

### 支持的虚拟化系统（Execution Modes）
| System       | Key     | 描述 |
|--------------|---------|------|
| Native       | `native` | 物理机原生运行，作为性能上限基线 |
| HAMi-core    | `hami`   | 基于CUDA API拦截的软件虚拟化中间件 |
| BUD-FCSP     | `fcsp`   | 作者团队提出的改进版细粒度SM控制方案 |
| MIG-Ideal    | `mig`    | 基于NVIDIA规格书的理想化MIG行为模拟（非实测） |

> 注：MIG-Ideal用于设定理论最优边界，即使在不支持MIG的设备上也可进行相对评估。

### 评估指标体系（部分关键指标）
#### Overhead Metrics（越低越好）
- OH-001: Kernel Launch Latency（内核启动延迟）
- OH-005: API Interception Overhead（API钩子开销）
- OH-010: Total Throughput Degradation（总吞吐下降百分比）

#### Isolation Metrics（越高越好或越低越好视情况而定）
- IS-003: SM Utilization Accuracy（SM利用率准确性）
- IS-008: Fairness Index（Jain公平指数）
- IS-009: Noisy Neighbor Impact（噪声邻居影响）

#### LLM-Specific Metrics
- LLM-001: Attention Kernel Throughput（注意力核性能）
- LLM-004: Token Generation Latency（首token时间 TTFT + 间隔 ITL）
- LLM-003: Batch Size Scaling（批量扩展效率）

### 统计方法
- 每项指标默认执行100次迭代，前10次为预热
- 报告均值（mean）、标准差（stddev）、P95/P99、变异系数（CV）
- 所有结果与Native/MIG-Ideal对比，计算△MIG偏差和得分

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 表1：Overhead 性能对比（单位：μs，除特别标注外）
| Metric               | Native | HAMi-core | BUD-FCSP |
|----------------------|--------|-----------|----------|
| OH-001 (Launch)      | 4.2    | 15.3      | 8.7      |
| OH-002 (Alloc)       | 12.5   | 45.2      | 28.3     |
| OH-003 (Free)        | 8.1    | 32.4      | 18.6     |
| OH-004 (Context)     | 125    | 312       | 198      |
| OH-005 (Hook, ns)    | —      | 85        | 42       |
| OH-010 (Throughput ↓)| —      | 18.5%     | 9.2%     |

> **发现**：HAMi-core引入约3.6倍的Kernel Launch延迟；BUD-FCSP通过优化dlsym路径和令牌桶算法，将多数开销降低40%以上。

#### 表2：Isolation 质量对比（4个并发租户）
| Metric                   | HAMi-core | BUD-FCSP |
|--------------------------|-----------|----------|
| IS-001 (Mem Accuracy, %) | 98.2      | 99.1     |
| IS-003 (SM Accuracy, %)  | 85.4      | 92.7     |
| IS-008 (Fairness Index)  | 0.87      | 0.94     |
| IS-009 (Noisy Neighbor ↓)| 24.3%     | 12.1%    |
| IS-010 (Fault Isolation) | Pass      | Pass     |

> **发现**：两者均实现基本内存隔离，但SM控制精度有限；BUD-FCSP在公平性和抗干扰方面明显优于HAMi-core。

#### 表3：LLM 工作负载性能（相对于Native）
| Metric                  | HAMi-core | BUD-FCSP |
|-------------------------|-----------|----------|
| LLM-001 (Attention, %)  | 82.3%     | 91.5%    |
| LLM-002 (KV Cache, %)   | 76.4%     | 88.2%    |
| LLM-004 (TTFT, ms)      | 45.2      | 28.7     |
| LLM-004 (ITL, ms)       | 12.8      | 8.4      |
| LLM-003 (Batch Scale)   | 0.78      | 0.89     |

> **发现**：LLM对内存分配延迟极为敏感，KV Cache频繁增长加剧了虚拟化开销；BUD-FCSP在Token生成延迟上提升近35%，更适合低延迟推理场景。

#### 表4：综合评分与等级
| System       | Overall Score | MIG Parity | Grade |
|--------------|---------------|------------|-------|
| MIG-Ideal    | 100%          | 100%       | A+ (baseline) |
| Native       | 100%          | —          | A+    |
| BUD-FCSP     | 85.2%         | 85.2%      | B+    |
| HAMi-core    | 72.0%         | 72.0%      | C     |

> **说明**：MIG-Ideal为理论参考值；实际中Native代表绝对性能天花板，软件虚拟化能达到其72%-85%的综合表现。

---

## 4. 关键结论和发现

### 主要发现
1. **软件虚拟化虽有开销但仍具实用价值**  
   在典型工作负载下，HAMi-core带来约18.5%的吞吐损失，BUD-FCSP降至9.2%，对于追求成本效益而非极致性能的场景是可接受的折衷。

2. **隔离性为软肋，尤其SM控制不够精确**  
   软件层无法像MIG那样物理隔离SM和L2 Cache，导致SM利用率控制误差达7–15%，易引发“噪声邻居”效应。BUD-FCSP通过加权公平队列调度提升了多租户公平性。

3. **LLM推理对虚拟化异常敏感**  
   因涉及大量小对象动态内存分配（如KV Cache）、高并发流和混合精度运算，其性能受虚拟化开销放大影响显著。优化内存跟踪和速率限制策略至关重要。

4. **BUD-FCSP显著优于HAMi-core**  
   在所有维度中，BUD-FCSP均表现出更低的开销、更高的隔离质量和更好的LLM适应性，验证了其在细粒度SM控制、轻量化hook机制方面的有效性。

### 局限性
- **单GPU聚焦**：当前框架主要面向单卡场景，尚未充分覆盖分布式训练中的NCCL通信瓶颈。
- **合成工作负载为主**：LLM测试使用自定义CUDA kernel模拟，未集成PyTorch/TensorRT-LLM等真实推理栈，需进一步验证。
- **MIG基线为模拟值**：MIG-Ideal基于官方文档推导，并非实测数据，可能高估理想性能。
- **GPU架构覆盖有限**：仅在A100上测试，其他架构（如RTX系列、Hopper）的行为可能存在差异。
- **版本依赖性强**：HAMi-core和BUD-FCSP处于快速迭代中，结果仅反映特定版本状态。

### 未来工作方向
1. 扩展至多GPU和分布式训练场景，增加对NCCL AllReduce、P2P带宽的压力测试；
2. 集成主流LLM推理框架（如vLLM、TensorRT-LLM）以增强现实代表性；
3. 开发自动化回归测试流水线，支持持续监控虚拟化系统的稳定性与性能演进；
4. 探索异构GPU集群下的统一评测能力；
5. 引入功耗与能效指标，完善绿色计算维度的评估。

--- 

> ✅ **总结一句话**：  
> GPU-Virt-Bench首次建立了系统化、可量化的软件GPU虚拟化评估体系，揭示了当前方案与MIG之间的性能鸿沟，并证明BUD-FCSP在兼顾兼容性的同时显著优于HAMi-core，为多租户AI平台选型提供了科学依据。

</details>

---

### 4. [DiRL: An Efficient Post-Training Framework for Diffusion Language Models](https://arxiv.org/abs/2512.22234)

**Authors**: Ying Zhu, Jiaxin Wan, Xiaoran Liu, Siyanag He, Qiqi Wang, Xu Guo, Tianyi Liang, Zengfeng Huang, Ziwei He, Xipeng Qiu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.22234v1  

#### Abstract
Diffusion Language Models (dLLMs) have emerged as promising alternatives to Auto-Regressive (AR) models. While recent efforts have validated their pre-training potential and accelerated inference speeds, the post-training landscape for dLLMs remains underdeveloped. Existing methods suffer from compu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# DiRL: An Efficient Post-Training Framework for Diffusion Language Models 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前 **Diffusion Language Models (dLLMs)** 在预训练阶段已展现出潜力，但在 **post-training** 阶段（尤其是 **Reinforcement Learning, RL**）仍面临严重挑战：
- **计算效率低下**：传统 dLLMs 缺乏 KV Cache 支持，推理和训练开销大。
- **训练-推理目标不一致**：预训练中使用的随机掩码策略无法复现真实推理路径，导致 logits 偏差。
- **缺乏高效的 RL 框架支持**：缺少与高效推理引擎（如 LMDeploy）集成的端到端训练系统，难以实现快速 rollout 和在线更新。

这些问题限制了 dLLMs 在复杂任务（如数学推理）上的表现。

---

### 提出的新方法与创新思路

作者提出 **DiRL** —— 一个高效的 dLLM 后训练框架，并基于此提出 **DiPO**，首个为 dLLMs 设计的无偏 **Group Relative Policy Optimization (GRPO)** 实现。

#### 主要创新点：

1. **DiRL 框架设计**
   - 将 **FlexAttention** 加速的 **blockwise 训练** 与 **LMDeploy 优化的推理引擎** 紧密结合。
   - 实现 **API Server 内部的在线模型更新**，避免频繁磁盘 I/O，显著提升训练效率。
   - 构建了从 SFT 到 RL 的完整两阶段 post-training 流程。

2. **DiPO 算法**
   - 利用 blockwise dLLM 可精确计算每个 token 的条件概率这一特性，首次实现了 **无偏的 GRPO**。
   - 引入固定参考策略 $ \pi_{\text{ref}} $ 的 KL 正则化，防止策略漂移。
   - 支持 token-level 策略梯度更新，适配 dLLM 的生成机制。

3. **工程级优化**
   - 使用 **FlexAttention** 处理复杂的 blockwise attention mask，突破 FlashAttention 的限制。
   - 通过 **LMDeploy 的 in-place 参数更新 API** 实现零成本模型热更新，消除文件系统交互延迟。

---

### 相比现有方法的优势

| 维度 | DiRL vs. 现有方法 |
|------|------------------|
| **训练效率** | 相比 TraceRL，每步训练延迟降低近 6×，总吞吐提升 2.5× |
| **训练-推理一致性** | 实现真正对齐的目标函数，避免随机掩码带来的偏差 |
| **算法先进性** | 首个在 dLLM 上实现无偏 GRPO 的工作，优于 diffu-GRPO、Coupled-GRPO 等近似方法 |
| **系统集成度** | 高度集成训练与推理系统，支持实时 rollout 与在线学习 |

---

## 2. 核心实验方法和设置

### 使用的数据集

| 阶段 | 数据集 | 描述 |
|------|--------|------|
| **SFT 阶段** | `OpenR1-Math` | 由 GLM-4.6 蒸馏得到的高质量数学推理数据集 |
| **RL 阶段** | `Big-Math` | 大规模、经 math-verify 验证的数学 RL 专用数据集 |

> 所有样本最大长度限制为 **8k tokens**，是目前 dLLMs 中最长的推理长度。

---

### 实验设置

| 项目 | 设置 |
|------|------|
| **基础模型** | SDAR-8B-Chat（blockwise dLLM） |
| **SFT 设置** | 使用 LLaMA-Factory，8×H200 GPUs，batch size=512，lr=1e-5，cosine 调度器，训练 100 步 |
| **RL 设置** | 128×H200 GPUs，ZeRO-1，global batch size=128，每题 rollout 32 条轨迹，lr=1e-6，训练 40 步 |
| **评估任务** | MATH500, GSM8k, AIME2024, AIME2025, OlympiadBench |
| **解码方式** | 静态（greedy）与动态（top-1 prob > 0.9 直接输出）两种模式 |

---

### 基线方法对比

| 模型 | 类型 | 备注 |
|------|------|------|
| Qwen2.5-7B/32B-Instruct | AR 模型 | 广泛认可的高性能自回归模型 |
| SDAR-8B-Chat | dLLM | 原始 blockwise 扩散模型 |
| TraDo-8B-Instruct | dLLM + RL | 当前最先进的 dLLM RL 方法（TraceRL） |
| DiRL-8B-Instruct | dLLM + DiPO (ours) | 本文提出的方法 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）

| 模型 | MATH500 ↑ | GSM8k ↑ | AIME2024 ↑ | AIME2025 ↑ | Olympiad ↑ | Avg. Score ↑ |
|------|-----------|---------|------------|------------|-------------|---------------|
| Qwen2.5-7B-Instruct | 73.8 | 89.8 | 9.0 | 5.6 | 36.6 | 42.9 |
| Qwen2.5-32B-Instruct | 81.1 | 94.0 | 12.9 | 11.9 | 45.7 | 49.1 |
| SDAR-8B-Chat | 71.5 | 89.5 | 5.6 | 8.5 | 35.6 | 42.2 |
| TraDo-8B-Instruct | 76.7 | 90.4 | 11.5 | 13.5 | 40.2 | 46.5 |
| **DiRL-8B-Instruct (Ours)** | **85.1** | **93.1** | **21.5** | **22.9** | **47.3** | **54.0** |

> ✅ **DiRL-8B-Instruct 在所有任务上均达到 SOTA 性能，平均得分超越更大的 Qwen2.5-32B-Instruct 达 4.9 分**

---

### 与基线方法的对比结果

- **相比同规模 dLLM**：
  - 在 AIME2025 上比 TraDo 提高 **9.4 分**，在 AIME2024 上提高 **10 分**。
  - 输出平均长度更长（见表中“Average Output Length”），说明具备更强的多步推理能力。
- **相比 AR 模型**：
  - 超越参数量更大的 **Qwen2.5-32B-Instruct**，尤其在竞赛级数学任务（AIME/Olympiad）上优势明显。
- **效率方面**（Figure 6 & 7）：
  - 单步训练时间：**DiRL 比 TraceRL 快 2.55×**
  - 训练延迟：**8B 模型的 DiRL 比 1.7B 的 TraceRL 还低**
  - Rollout 效率提升 1.08×，训练部分提速达 5.94×

---

### 消融实验结果（Figure 8）

进行了关于 **dynamic decoding threshold τ** 的消融研究（τ ∈ [0.5, 0.99]）：

- **DiRL-8B-Instruct 表现最稳定且持续领先**：
  - 随着 τ 增加（解码更保守），性能稳步上升，在 τ=0.9 时达到最优。
- **其他模型表现不稳定**：
  - SDAR-8B-Chat 几乎无提升，表明其推理能力受限。
  - TraDo-8B-Instruct 在 AIME2024 上波动剧烈，显示其鲁棒性较差。

> 🔍 结论：DiRL 的优越性能来源于高质量的 SFT+RL 推理路径，而非特定超参配置，具有强泛化性和稳定性。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **DiRL 是首个实现高效、无偏、可扩展 dLLM RL 的框架**，解决了训练-推理不一致的根本问题。
2. ✅ **DiPO 成功将 GRPO 移植到 dLLM 上**，并利用 blockwise 结构实现精确 logits 计算，避免近似误差。
3. ✅ **DiRL-8B-Instruct 成为当前最强的数学推理 dLLM**，不仅超越同类扩散模型，甚至优于更大规模的 AR 模型（如 Qwen2.5-32B）。
4. ✅ **系统级优化带来巨大效率增益**：FlexAttention + LMDeploy + 在线更新组合使整体训练吞吐提升超过 2.5×。

---

### 方法的局限性

1. **上下文长度仍有限**：尽管 8k 是当前 dLLM 最长记录，但仍远小于主流 AR 模型（如 32k+）。
2. **尚未探索 Test-Time Scaling 或 Chain-of-Thought 扩展技术**，可能进一步释放潜力。
3. **当前聚焦数学任务**，在代码、Agent 等领域的泛化能力有待验证。
4. **依赖 blockwise 架构**，不适用于全双向 dLLMs。

---

### 未来工作方向

1. **扩展至更大规模模型**（>8B），追求更强性能。
2. **延长推理长度至 32k+**，探索 long CoT 和 test-time scaling 技术。
3. **引入 dynamic packing 等长文本训练策略**，进一步加速训练。
4. **拓展至 code generation 和 agentic tasks**，构建通用 dLLM 对齐工具链。
5. **开源完整的 post-training toolkit**，推动社区发展。

---

> 📌 **总结一句话**：  
> DiRL 通过算法（DiPO）+ 系统（FlexAttention + LMDeploy）协同创新，首次实现了高效、无偏、可落地的 dLLM 后训练框架，训练出当前最强数学推理 dLLM —— **DiRL-8B-Instruct**，并在性能与效率上全面超越现有方法。

</details>

---

### 5. [KernelEvolve: Scaling Agentic Kernel Coding for Heterogeneous AI Accelerators at Meta](https://arxiv.org/abs/2512.23236)

**Authors**: Gang Liao, Hongsen Qin, Ying Wang, Alicia Golden, Michael Kuchnik, Yavuz Yetim, Jia Jiunn Ang, Chunli Fu, Yihan He, Samuel Hsia, Zewei Jiang, Dianshi Li, Uladzimir Pashkevich, Varna Puvvada, Feng Shi, Matt Steiner, Ruichao Xiao, Nathan Yan, Xiayu Yu, Zhou Fang, Abdul Zainul-Abedin, Ketan Singh, Hongtao Yu, Wenyuan Chi, Barney Huang, Sean Zhang, Noah Weller, Zach Marine, Wyatt Cook, Carole-Jean Wu, Gaoxiang Liu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.23236v1  

#### Abstract
Making deep learning recommendation model (DLRM) training and inference fast and efficient is important. However, this presents three key system challenges - model architecture diversity, kernel primitive diversity, and hardware generation and architecture heterogeneity. This paper presents KernelEv...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：KernelEvolve: Scaling Agentic Kernel Coding for Heterogeneous AI Accelerators at Meta

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代深度学习推荐模型（DLRM）在训练和推理过程中面临三大系统挑战：
- **模型架构多样性**：从传统MLP到Transformer、卷积增强架构等，计算模式差异巨大。
- **算子多样性**：除GEMM外，存在超过200种数据预处理算子（如MapId、MBDT），这些算子通常内存密集且控制流复杂。
- **硬件异构性**：跨厂商（NVIDIA、AMD）和自研芯片（MTIA v3）的加速器在内存层次、编程模型和代际特性上差异显著。

这三者的组合导致了一个巨大的优化空间，使得手动内核开发成本高昂（每个内核需2–8周专家调优），难以扩展。

### 提出的新方法与创新点
论文提出了 **KernelEvolve**，一个基于智能体（agentic）的自动化内核生成与优化框架，其核心创新包括：

#### （1）统一的多目标优化框架
- 将内核优化建模为**图搜索问题**（graph-based search），通过选择策略（selection policy）、通用变换算子（universal operator）、适应度函数（fitness function）和终止规则进行系统化探索。
- 引入**检索增强提示合成**（retrieval-augmented prompt synthesis），动态注入上下文信息，提升LLM生成质量。

#### （2）持久化知识库支持专有硬件
- 构建了一个结构化的**持久知识库**（persistent knowledge base），编码了NVIDIA、AMD和MTIA等平台的硬件约束、优化模式和调试指南。
- 特别针对Meta自研的MTIA芯片，注入了其特有的Triton扩展（如SFU指令、双核同步、跨PE通信原语），解决了LLM缺乏专有硬件先验知识的问题。

#### （3）端到端自动化流程
- 实现了从内核生成、编译、正确性验证、性能评测到自动调试的全流程闭环。
- 支持多种编程抽象：Triton、CuTe DSL、低级诊断语言，并能跨平台生成代码（NVIDIA、AMD、MTIA）。

#### （4）通用变换算子设计
- 提出“**单一通用算子**”（universal operator）替代传统的多阶段算子（如Draft/Debug/Improve），避免静态提示模板的认知偏差。
- 动态根据运行时反馈（profiling、错误日志）调整提示内容，实现更灵活的优化路径探索。

### 相比现有方法的优势
| 维度 | 现有方法（如AutoTriton、Kevin、GEAK） | KernelEvolve |
|------|----------------------------------------|-------------|
| **硬件支持** | 单一平台（通常是NVIDIA） | 跨平台：NVIDIA、AMD、MTIA |
| **评估真实性** | 合成算子、固定形状 | 生产级动态批处理、可变序列长度 |
| **代理能力** | 缺乏完整生命周期管理 | 全流程：生成→验证→评测→调试 |
| **搜索规模** | 少量迭代 | 支持大规模搜索（数百至数千步） |
| **知识利用** | 静态提示 | 检索增强 + 持久化知识库 |
| **部署集成** | 研究原型 | 已在Meta生产环境持续运行 |

---

## 2. 核心实验方法和设置

### 数据集与测试用例
- **KernelBench**：公开基准套件，包含250个问题，覆盖三个难度等级（单算子、融合、全模块）。用于验证泛化能力。
- **160个 PyTorch ATen 算子**：涵盖element-wise、reduction、activation等基础操作，作为功能正确性的基础验证。
- **真实生产模型**：包括：
  - Convolutional Transformer（1D/2D卷积）
  - WuKong 和 InterFormer 推荐模型中的FM/PFFN模块
  - 数据预处理算子：MapId、MergeBucketizedDenseTransform (MBDT)
  - 序列学习算子：Batch Event Truncate

### 实验设置
- **硬件平台**：
  - NVIDIA H100 / A100
  - AMD MI300 / MI350
  - Meta MTIA v2i / v3
- **评估指标**：
  - **正确性**：`torch.allclose()` 数值一致性检查（atol=1e-4, rtol=5e-4）
  - **性能**：相对于PyTorch基线的**加速比**（speedup）
  - **开发效率**：开发时间从数周缩短至小时级别
  - **覆盖率**：支持的算子数量及跨平台兼容性

### 基线方法对比
| 基线 | 描述 |
|------|------|
| `torch.nn.functional.conv1d` | 标准PyTorch 1D卷积 |
| `torch.nn.functional.conv2d`（NHWC） | 常见优化技巧：reshape为2D并使用cuDNN优化路径 |
| `torch.compile` + 默认调度 | PyTorch自带编译器优化 |
| 手动调优内核 | Meta专家手工编写的高度优化内核 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 算子类别 | 场景 | 加速比（Speedup） |
|--------|------|------------------|
| **LLM推理** | Llama-3.1-8B Vanilla Attention | 4.6× |
| | SDPA-MLP | 3.3× |
| **卷积Transformer** | conv1d | 6.5× |
| | conv2d | 4.7× |
| **数据预处理** | MapId | 4.1× |
| | MBDT | 9.3× |
| | Batch Event Truncate | 9.8× |
| **推荐模型融合** | WuKong Optimized FM | 4.0× |
| | InterFormer PFFN | 2.5× |
| **MTIA专用优化** | RMSNorm 2D backward | **17×** |
| | Sparse Inverted Index | 1.25× |

> 图4显示整体加速范围为 **1.25–17×**，远超PyTorch基线。

### 正确性与覆盖率
- 在 **480个算子-平台配置**（160算子 × 3平台）上实现了 **100%正确率**。
- 在 **KernelBench** 上达到 **100%通过率**（所有250题）。
- 成功为MTIA v3生成了缺失的ATen算子（如`clamp.out`, `_unique2`, `unique_consecutive`），解决了模型无法部署的问题。

### 与基线方法对比
| 场景 | KernelEvolve vs. `conv1d` | vs. `conv2d`（NHWC） |
|------|----------------------------|------------------------|
| conv1d on H100 (FP16) | **2.30×** | **1.62×** |
| conv1d on MTIA v3 | **6.54×** | **4.71×** |

说明KernelEvolve不仅优于标准实现，在专有硬件上也显著超越厂商优化库。

### 消融实验与优化轨迹分析
- **图10** 展示了ATen算子的优化轨迹：
  - 初始阶段（draft phase）随机采样，性能一般；
  - 进入树搜索后，结合执行反馈逐步提升，最终收敛到最优解。
- **图12** 显示conv1d内核经过300次搜索后，性能从约2000提升至6889（fitness score），证明了搜索的有效性。
- 分析表明，**算子融合**（如将多个layout transform合并为一个kernel）是主要性能来源之一，减少了中间内存访问开销。

---

## 4. 关键结论和发现

### 主要发现
1. **自动化内核生成可行且高效**：
   - KernelEvolve能在无需人工干预的情况下生成**生产级质量**的高性能内核。
   - 开发周期从**数周缩短至数小时**，极大提升了研发效率。

2. **算子融合是关键优化手段**：
   - 对于内存密集型算子（如预处理、序列截断），减少中间张量写回HBM是性能突破的关键。
   - 例如Batch Event Truncate通过批量并行处理多个特征，实现高达14.5×加速。

3. **专有硬件可通过知识注入支持**：
   - MTIA等私有架构虽不在LLM训练语料中，但通过结构化知识库注入（如SFU、双核、跨PE通信），仍可生成高质量内核。
   - 这为未来新型AI芯片提供了快速软件适配路径。

4. **异构硬件需要差异化优化策略**：
   - NVIDIA依赖Tensor Core和TMA；
   - AMD利用Infinity Cache；
   - MTIA则需启用SFU和PE间通信。
   - KernelEvolve能根据平台自动选择最优策略。

### 方法的局限性
- **对LLM能力高度依赖**：若底层LLM生成能力不足，可能导致搜索效率下降。
- **长尾算子覆盖仍需完善**：尽管已支持数百算子，仍有部分边缘情况未完全覆盖。
- **资源消耗较大**：大规模搜索可能涉及数千次LLM调用，带来较高的token成本。
- **不适用于极端低延迟场景**：某些微秒级关键路径仍需专家手写汇编级优化。

### 未来工作方向
1. **从算子级向模型级扩展**：
   - 将优化粒度从单个算子提升至整个模型图，支持跨层融合与全局内存规划。

2. **更深的代码生成能力**：
   - 支持MLIR方言修改、PTX/SASS级生成，突破Triton抽象限制。

3. **大规模并行搜索**：
   - 利用分布式基础设施同时探索数千候选方案，加快收敛速度。

4. **强化学习驱动的自适应优化**：
   - 基于执行反馈进行在线微调，使代理能更快适应新硬件。

5. **可持续AI基础设施**：
   - 量化token消耗与碳足迹，平衡优化深度与环境影响。

6. **端到端集成**：
   - 与AOT Inductor、模型服务系统对接，实现“定义即部署”的闭环流程。

---

> **总结**：  
> KernelEvolve 是首个在工业界大规模部署的LLM驱动内核生成系统，它成功应对了推荐系统中“模型-算子-硬件”三维异构带来的优化危机。通过引入**图搜索 + 检索增强 + 持久知识库**的架构，实现了跨平台、高正确性、高性能的自动化内核生成，在MTIA、NVIDIA、AMD等多种硬件上取得最高达17倍的加速，标志着AI系统软件进入“智能编译”新时代。

</details>

---

### 6. [MatKV: Trading Compute for Flash Storage in LLM Inference](https://arxiv.org/abs/2512.22195)

**Authors**: Kun-Woo Shin (Seoul National University, Korea), Jay H. Park (Samsung Electronics, Korea), Moonwook Oh (Samsung Electronics, Korea), Yohan Jo (Seoul National University, Korea), Jaeyoung Do (Seoul National University, Korea), Sang-Won Lee (Seoul National University, Korea)  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.22195v1  

#### Abstract
We observe two major trends in LLM-based generative AI: (1) inference is becoming the dominant factor in terms of cost and power consumption, surpassing training, and (2) retrieval augmented generation (RAG) is becoming prevalent. When processing long inputs in RAG, the prefill phase of computing th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MatKV: Trading Compute for Flash Storage in LLM Inference

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在基于大语言模型（LLM）的生成式 AI 应用中，**检索增强生成（RAG）** 已成为主流范式，用于缓解幻觉、提升知识时效性和安全性。然而，RAG 推理中的 **prefill 阶段**（即对输入文本计算 Key-Value 向量）非常耗时且能耗高，尤其当处理长文本块（如 1k+ tokens）时。此外，许多文档被重复检索，导致大量冗余的 KV 计算。

传统方法依赖高性能 GPU 实时计算 KV 缓存，造成巨大的计算开销和能源浪费。

---

### ✅ 提出的新方法：MatKV
作者提出 **MatKV**，一种通过 **用存储换计算（trading compute for storage）** 来优化 RAG 推理效率的新方案：

- **预计算并物化（materialize）KV 缓存**：在将文档插入向量数据库时，一次性使用 GPU 计算其 KV 缓存，并将其持久化到低成本、高速的 **flash 存储（SSD）** 上。
- **推理时直接加载而非重算**：当该文档被检索到后，直接从 SSD 加载预计算的 KV 缓存至 GPU HBM，跳过昂贵的 prefill 阶段。
- **解耦 prefill 与 decode 阶段**：允许 GPU 在解码当前请求的同时，异步加载下一个请求所需的 KV 缓存，实现流水线并行。

---

### ✅ 相比现有方法的优势

| 对比维度 | 传统方法（Vanilla） | MatKV |
|--------|------------------|-------|
| **计算模式** | 每次都重新计算 KV | 复用预计算 KV |
| **存储位置** | 内存缓存（DRAM）或不缓存 | 外部 flash 存储（SSD） |
| **成本效益** | 高 GPU 成本，频繁重复计算 | 一次计算 + 多次复用，长期更经济 |
| **能效** | 高功耗（~350W H100） | 极低功耗（SSD 仅 ~7W） |
| **可扩展性** | 受限于 DRAM 容量和价格 | 支持 TB/PB 级 KV 存储 |
| **系统优化潜力** | 串行执行 prefill 和 decode | 支持 prefill I/O 与 decode 并发 |

> 🔑 创新点总结：
> - 首次系统性论证 **将 KV 缓存物化到外部 SSD 而非 DRAM 是可行且高效** 的；
> - 提出 **“十天规则”（Ten-Day Rule）**，为是否物化提供经济学决策依据；
> - 实现 **prefill 与 decode 的时间重叠**，显著降低端到端延迟；
> - 支持 **低端 GPU 解码**，大幅降低部署门槛。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **TurboRAG**：用于测量系统延迟和功耗，平均查询 17.67 tokens，文档 767.73 tokens。
- **LongBench**：用于评估问答准确性，包含：
  - `2WikiMultihopQA`（多跳推理）
  - `HotpotQA`
  - `TriviaQA`

### 💻 实验硬件配置
| 组件 | 配置 |
|------|------|
| **GPU** | NVIDIA H100（80GB HBM），RTX 4090（24GB） |
| **CPU** | Dual-socket Intel Xeon Gold 6442Y |
| **内存** | 2TB DRAM |
| **SSD** | Samsung 9100 Pro（PCIe 5.0, 14GB/s）或 PM9A3（6.5GB/s） |
| **存储组织** | 四块 9100 Pro 组成 RAID-0，总带宽达 58.8GB/s |

### ⚙️ 模型
- LLaMA 3.1 系列：**3B**, **8B**, **70B**（4-bit 量化以适配 H100）
- 使用 Hugging Face Transformers 库扩展实现 MatKV（<500 行代码修改）

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Latency** | 分解为 Load Time、Prefill Time、Decode Time |
| **Power Consumption** | 全系统功耗（IPMI 测量）、GPU 功耗（nvidia-smi） |
| **Energy Usage** | 总能耗（Joules）= 功率 × 时间 |
| **Throughput** | 批处理下的吞吐量 |
| **Accuracy** | F1 Score（问答任务） |

### 🆚 基线方法
- **Vanilla**：标准方式，每次完整计算 KV（Full KV Recomputation）
- **CacheBlend**：部分缓存 + 融合注意力机制，兼顾准确率与速度
- **DRAM-based KV Cache**：作为理想对照（但成本过高不可行）

---

## 3. 主要实验结果和性能指标

### 📈 性能加速（Speedup）
| 场景 | 结果 |
|------|------|
| **单请求（H100 + 70B）** | MatKV 将 prefill 时间减少 **>50%**（见 Fig. 5） |
| **批处理（batch size=10）** | MatKV 达到最高 **2x 整体加速**（见 Fig. 6） |
| **重叠执行（Overlap Enabled）** | MatKV + overlap 实现 **2x 端到端加速** vs Vanilla（见 Fig. 7） |
| **输入长度增加（4 chunks）** | MatKV 相对优势进一步扩大（见 Fig. 8a） |

> ✅ 当输入越长、batch 越大时，prefill 占比越高，MatKV 优势越明显。

---

### 🔋 能源效率（Energy Efficiency）
| 配置 | 峰值功率 | 平均功率 | 总耗时 | **总能耗** |
|------|---------|----------|--------|------------|
| Vanilla | 1,256 W | 1,038 W | 546 s | **566 kJ** |
| MatKV | 1,124 W | 947 W | 306 s | **289 kJ** |
| MatKV (w/ overlap) | 1,241 W | 979 W | 285 s | **279 kJ** |

> 💡 MatKV **能耗降低超过一半**（566 → 279 kJ），主要得益于避免了 GPU 密集型 prefill。

#### GPU 层面能耗对比（Table V）
| 配置 | 平均 GPU 功率 | 总 GPU 能耗 |
|------|----------------|--------------|
| Vanilla | 340 W | 185 kJ |
| MatKV (overlap) | 336 W | **95 kJ** |

> ✅ GPU 能耗也几乎减半。

---

### 💰 经济性分析：“十天规则”（Ten-Day Rule）

作者基于 Jim Gray 的 “五分规则” 推导出 **“十天规则”**：

> 若一个文档每 **10 天内至少被访问一次**，则将其 KV 缓存物化到 SSD 比每次都用 GPU 重算更划算。

- 示例：若某文档每小时被访问一次，MatKV 成本效率是重算的 **100x 更优**。
- 图 2 显示，在真实 RAG 查询分布中，**超过 10% 的文档被访问 ≥2 次**，表明大量文档满足十天规则。

---

### 🎯 准确性评估（F1 Score）

| Dataset | Vanilla | MatKV | CacheBlend |
|--------|---------|-------|------------|
| 2WikiMQA | 0.278 | 0.241 | 0.253 |
| TriviaQA | 0.786 | 0.812 | 0.826 |
| HotpotQA | 0.353 | 0.302 | 0.309 |

> 🔍 虽然 MatKV 在某些任务上略有下降（如 2WikiMQA），但在多数情况下表现接近甚至略优于 Vanilla，且远快于 CacheBlend。

#### 附加实验（Chat History）
- 在 `Locomo` 数据集测试长对话历史 RAG：
  - Vanilla: 0.423
  - MatKV: 0.419 → **几乎无损**

---

### 🔄 消融实验与其他发现

| 实验 | 发现 |
|------|------|
| **不同 SSD 配置**（Table III） | RAIDed SSD 显著优于单盘；DRAM 最快但成本不可接受 |
| **不同模型大小**（Fig. 9） | 模型越大（70B），MatKV 收益越显著（prefill 成本增长快于 KV 大小） |
| **低阶 GPU 解码**（Fig. 10） | 使用便宜 30x 的 RTX 4090，MatKV 仅慢 1.5x vs H100 上的 full recompute，而 Vanilla 慢 3x → **支持低成本部署** |
| **与 CacheBlend 对比** | MatKV 加载速度快 37%，TFTT 快 41%，整体更快，但准确率稍低 |

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **MatKV 显著提升 RAG 推理效率**：
   - 推理时间减少最多达 **2x**
   - 系统级能耗降低 **>50%**
2. **SSD 物化 KV 缓存是经济可行的**：
   - 提出 **“十天规则”**，为缓存策略提供理论指导
   - 即使采用 naïve “全物化”策略，仍比 GPU 重算更省成本
3. **支持新型系统优化**：
   - **prefill I/O 与 decode 并发执行**，隐藏加载延迟
   - **低阶 GPU 可胜任 decode**，降低部署成本
4. **准确性影响有限**：
   - 在多个 QA 任务中，性能损失极小，部分任务反而提升
   - 文档间无 cross-attention 不一定损害效果，query-to-doc attention 已足够

---

### ⚠️ 局限性
1. **冷启动问题**：首次插入文档需完整 prefill，可能影响首访体验（可通过懒加载缓解）
2. **选择性物化未实现**：目前采用 “Materialize-All”，未来应引入基于热度的缓存策略
3. **KV 存储开销大**：70B 模型下每个 1k token 文档 KV 占约 250MB，大规模部署需压缩或分层存储
4. **依赖高速 SSD**：性能受 SSD 带宽限制，低速设备收益下降

---

### 🔮 未来工作方向
1. **智能缓存策略**：基于访问频率/趋势动态决定是否物化（如预测热点文档）
2. **KV 压缩技术集成**：结合 MiniCache、xKV 等压缩方法进一步降低成本
3. **分层存储架构**：构建 DRAM → SSD → HDD 的 KV 缓存层级
4. **扩展至其他场景**：
   - Agent AI 中工具文档复用
   - Text-to-SQL 中 schema 提示复用
   - In-context Learning 中 demonstration 缓存
5. **支持增量更新**：文档变更后如何高效更新 KV 缓存

---

## ✅ 总结
**MatKV 开辟了一条全新的 LLM 推理优化路径 —— 以廉价、高效的 flash 存储替代昂贵的 GPU 计算资源**。它不仅在性能、能效、成本上全面超越传统方法，还推动了“存储为中心”的生成式 AI 系统设计范式转变。随着 SSD 带宽持续快速增长而 GPU FLOPS 成本下降趋缓，MatKV 所代表的方向极具前景。

</details>

---

### 7. [Wireless Traffic Prediction with Large Language Model](https://arxiv.org/abs/2512.22178)

**Authors**: Chuanting Zhang, Haixia Zhang, Jingping Qiao, Zongzhang Li, Mohamed-Slim Alouini  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.22178v1  

#### Abstract
The growing demand for intelligent, adaptive resource management in next-generation wireless networks has underscored the importance of accurate and scalable wireless traffic prediction. While recent advancements in deep learning and foundation models such as large language models (LLMs) have demons...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Wireless Traffic Prediction with Large Language Model》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Model (LLM)** 的无线流量预测方法（如 TrafficLLM、Time-LLM）主要关注**时间维度上的序列建模**，而忽略了城市级无线网络中普遍存在的**空间依赖性**（spatial dependencies）。这种忽略导致模型在面对不同区域异构交通模式时泛化能力差，难以实现高精度、可扩展的城市级预测。

此外，传统方法通常采用单一全局模型进行预测，无法兼顾**通用性与区域特异性**之间的平衡。

---

### 提出的新方法：TIDES
作者提出了一种名为 **TIDES**（**Traffic Intelligence with DeepSeek-Enhanced Spatial-temporal prediction**）的新型 LLM 驱动框架，用于城市级无线流量预测。其核心创新包括：

#### （1）Region-aware Modeling（区域感知建模）
- 通过聚类算法将城市划分为若干具有相似流量特征的区域集群（clusters），并对每个集群**个性化微调**一个共享的 LLM 主干。
- 实现了**通用性（generalization）与专业化（specialization）的平衡**，避免为每个区域单独训练模型带来的计算开销。

#### （2）Prompt-based Traffic Representation（提示工程驱动的数据表示）
- 将数值型流量数据转换为结构化的自然语言 prompt，嵌入以下多维统计特征：
  - 基础统计量（均值、最大值、标准差等）
  - 趋势分析（上升/下降趋势）
  - 时间段模式（早高峰、晚高峰、夜间）
  - 领域特定指标（峰均比 PAR、突发性 BURST、莫兰指数 I 等）
- 利用 LLM 强大的语义理解能力来“阅读”这些提示并做出推理预测。

#### （3）Spatial-Temporal Alignment with DeepSeek Module（跨域注意力机制）
- 设计了一个基于 **multi-head cross-domain attention** 的模块，使 LLM 能够融合来自邻近区域的空间上下文信息。
- 使用图神经网络思想构建邻接矩阵，并引入 **local Moran’s I** 统计量衡量空间自相关性，增强空间对齐能力。
- 仅微调轻量级组件（如 prompt encoder 和 attention 层），冻结 LLM 主干，显著降低训练成本。

#### （4）高效适应机制
- 采用 **parameter-efficient fine-tuning (PEFT)** 策略，在保持 LLM 强大泛化能力的同时，实现快速领域适配。

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **空间建模能力** | 显式建模区域间空间依赖关系，优于仅考虑时间序列的 LLM 方法（如 Time-LLM） |
| **模型效率** | 冻结主干 LLM，只微调少量参数，节省训练资源 |
| **预测准确性** | 在多个评估指标上全面超越 SOTA 模型 |
| **可解释性与鲁棒性** | 结构化 prompt 提升可解释性；误差分布更集中于零附近，稳定性更强 |

---

## 2. 核心实验方法和设置

### 数据集
- **真实城市级 4G 基站网络数据**
- 来源：中国某运营商
- 规模：约 19,000 个扇区，覆盖济南市
- 时间跨度：2024年7月28日至8月25日（共4周）
- 采样频率：每15分钟一条记录
- 处理方式：将扇区级流量聚合为区域级流量，并按聚类结果划分为 **4个zone（A、B、C、D）**

---

### 实验设置
- **输入窗口 H**：过去一天（96个时间步，即24小时）
- **预测窗口 P**：未来1小时（4个时间步）
- **LLM 模型**：DeepSeek-R1-Distill-Llama-8B（32层，模型维度16，8头注意力）
- **训练平台**：4×A100 GPU，使用 DeepSpeed + ZeRO-2 进行分布式训练
- **优化器**：AdamW + OneCycleLR，学习率 0.001
- **训练策略**：混合精度训练（bfloat16），早停机制防止过拟合

---

### 评估指标
- **MAE**（Mean Absolute Error）
- **RMSE**（Root Mean Squared Error）
- **MAPE**（Mean Absolute Percentage Error）

$$
\text{MAE} = \frac{1}{T}\sum_{t=1}^{T}|y_t - \hat{y}_t| \\
\text{RMSE} = \sqrt{\frac{1}{T}\sum_{t=1}^{T}(y_t - \hat{y}_t)^2} \\
\text{MAPE} = \frac{100}{T}\sum_{t=1}^{T}\left|\frac{y_t - \hat{y}_t}{y_t}\right|
$$

---

### 基线方法对比
| 类别 | 方法 |
|------|------|
| 分解线性模型 | DLinear |
| Transformer 架构 | Transformer, Autoformer, Reformer, TimesNet, LightTS |
| LLM-based 方法 | Time-LLM |

其中，**Time-LLM** 是当前最先进的 LLM 驱动时间序列预测框架，作为主要对比对象。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table I）

| 方法 / 区域 | Zone A MAE ↓ | Zone B MAPE ↓ | Zone C RMSE ↓ | Zone D MAE ↓ |
|------------|---------------|----------------|----------------|----------------|
| DLinear     | 0.2330        | 2.6294         | 0.3804         | 0.1492         |
| Time-LLM    | 0.2648        | **2.6623**     | 0.4867         | 0.1914         |
| **TIDES**   | **0.2193**    | **2.4827**     | **0.3621**     | **0.1356**     |

> ✅ **TIDES 在所有4个区域、全部3项指标中共取得10项第一，2项第二，且在 MAPE 上实现全胜。**

---

### 与基线方法的对比结果
- **平均提升幅度**：
  - 相比 Time-LLM，TIDES 在 MAPE 上平均降低约 **6.8%**
  - 在复杂区域（如 Zone B/C），提升更为显著（最高达 15%）
- **可视化验证**：
  - 图3显示 TIDES 的预测值与真实值的相关系数高达 **r = 0.973**，远超 Time-LLM（0.792）和 DLinear（0.959）
  - 图4表明 TIDES 的预测误差分布最集中于零附近，方差最小，说明其**稳定性和精度更高**
  - 图5从时空双重视角展示，TIDES 更好地还原了城市中高低流量区域的空间分布格局

---

### 消融实验与泛化能力分析（见 Table II）
| 场景 | MAE | 相对 Zone A 下降 |
|------|-----|------------------|
| Zone A → Zone A（同区） | 0.2255 | — |
| Zone B → Zone A（跨区） | 0.2595 | ↓15.0% |
| Zone C → Zone A | 0.2456 | ↓8.9% |
| Zone D → Zone A | 0.2328 | ↓3.2% |

> 🔍 表明 TIDES 具备良好的**跨区域泛化能力**，即使不重新训练也能在新区域保持较高性能，性能下降普遍 <10%，证明其模型迁移能力强。

---

## 4. 关键结论和发现

### 主要发现
1. **空间依赖性是提升 LLM 流量预测性能的关键因素**  
   单纯将流量视为文本序列进行预测不足以捕捉城市动态，必须显式建模区域间的空间交互。

2. **Prompt Engineering 可有效桥接数值信号与 LLM 语义空间**  
   将统计特征编码为自然语言描述，极大提升了 LLM 对非语言数据的理解与推理能力。

3. **TIDES 实现了高精度与高效率的统一**  
   通过冻结 LLM 主干 + 微调轻量模块的方式，在保证性能的同时大幅减少训练开销。

4. **该方法适用于未来 6G 网络中的智能资源调度**  
   准确的城市场景预测有助于实现负载均衡、节能优化和主动式网络管理。

---

### 方法的局限性
- 当前仅基于历史流量和地理信息，未整合外部因素（如天气、事件、POI 等）
- 所有区域共享同一 LLM 主干，可能限制极端异构场景下的表达能力
- 实验仅在一个城市展开，尚未验证跨城市迁移能力

---

### 未来工作方向
1. 引入实时反馈机制，支持在线增量学习与动态调整
2. 扩展至多模态输入（视频、地图、社交数据等）
3. 探索跨城市、全球尺度的大规模预测任务
4. 将 TIDES 集成到端到端的 AI-Native 6G 控制系统中，实现闭环自主优化

---

> 📌 **总结一句话**：  
> TIDES 成功将 LLM 的强大语义建模能力与无线网络的空间特性相结合，提出了一种高效、精准、可扩展的城市级无线流量预测新范式，为迈向 fully autonomous 的 6G 智能网络奠定了关键技术基础。

</details>

---

### 8. [WeDLM: Reconciling Diffusion Language Models with Standard Causal Attention for Fast Inference](https://arxiv.org/abs/2512.22737)

**Authors**: Aiwei Liu, Minghua He, Shaoxun Zeng, Sijun Zhang, Linhao Zhang, Chuhan Wu, Wei Jia, Yuan Liu, Xiao Zhou, Jie Zhou  
**Category**: cs.CL  
**Published**: 2025-12-30  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.22737v1  

#### Abstract
Autoregressive (AR) generation is the standard decoding paradigm for Large Language Models (LLMs), but its token-by-token nature limits parallelism at inference time. Diffusion Language Models (DLLMs) offer parallel decoding by recovering multiple masked tokens per step; however, in practice they of...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：WeDLM: Reconciling Diffusion Language Models with Standard Causal Attention for Fast Inference

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

传统的 **Autoregressive (AR)** 语言模型在推理时逐个生成 token，导致解码速度慢、难以并行化，限制了在高吞吐场景下的应用。**Diffusion Language Models (DLLMs)** 虽然支持并行解码（一次恢复多个被掩码的 token），但在实际部署中并未显著超越优化后的 AR 引擎（如 vLLM）。其根本原因在于：

- 多数 DLLMs 使用 **bidirectional attention**，破坏了标准的 **prefix KV caching** 机制；
- 导致每次生成后都需重新计算上下文，无法有效复用缓存，严重损害效率。

因此，论文旨在解决 **如何让扩散语言模型实现真正的高效并行推理**，尤其是在工业级优化系统（如 vLLM）下仍能胜出。

---

### **提出了什么新方法或新思路**

作者提出 **WeDLM**，一个全新的扩散解码框架，其核心思想是：

> **在保持标准因果注意力（causal attention）的前提下实现并行掩码恢复**，从而兼容 KV 缓存，提升推理效率。

#### 主要创新点：

1. **Topological Reordering（拓扑重排序）**
   - 将已观测到的 token 物理上移动到序列前缀位置，同时通过 RoPE 的 position ids 保留其原始逻辑位置。
   - 这样，被掩码的 token 在标准因果 attention 下即可访问所有已观测上下文，无需 bidirectional attention。

2. **Dual-Stream Masking（双流掩码）**
   - 训练时引入两个流：**Memory Stream**（干净历史）和 **Prediction Stream**（带掩码的预测流）。
   - 预测流中的每个 block 只能关注 Memory Stream 中对应的历史，模拟推理时的“已提交前缀”条件，缩小训练-推理差距。

3. **Streaming Parallel Decoding（流式并行解码）**
   - 维持一个固定大小的滑动窗口，动态填充新掩码 token；
   - 使用 **distance-penalized entropy selection** 优先解码靠左的位置，促进从左到右的连续前缀增长；
   - 支持 **立即缓存（immediate caching）**，避免 block-wise 方法的“停等”问题。

---

### **相比现有方法的优势**

| 方面 | 传统 DLLMs（如 LLaDA, Dream） | Block-wise DLLMs（如 SDAR） | WeDLM（本文） |
|------|-------------------------------|-----------------------------|--------------|
| Attention 结构 | Bidirectional | Block 内双向，跨 block 因果 | 全局标准 causal attention |
| KV Cache 兼容性 | ❌ 不兼容 | ⚠️ 块完成后才能缓存 | ✅ 每个 token 可立即缓存 |
| 并行效率 | 高（每步多 token）但缓存利用率低 | 中等，受限于块同步 | 高且可持续 |
| 推理速度 | 通常不优于 vLLM | 有限加速 | 显著优于 vLLM |
| 部署兼容性 | 需专用引擎（如 dInfer） | 需定制引擎（如 JetEngine） | 可直接运行于 vLLM |

> ✅ **WeDLM 是首个在匹配部署条件下超越工业级 AR 引擎（vLLM）的 DLLM。**

---

## 2. 核心实验方法和设置

### **使用的数据集**

涵盖三大类任务：

- **数学推理**：GSM8K（3-shot）、MATH（4-shot）、GPQA-Diamond（5-shot）
- **通用知识与常识**：MMLU（5-shot）、ARC-C/E（0-shot）、HellaSwag（10-shot）
- **代码生成**：HumanEval（4-shot）、HumanEval-plus（4-shot）、MBPP（3-shot）

---

### **实验设置和评估指标**

#### 模型配置
- 基座模型：Qwen2.5-7B 和 Qwen3-8B
- 训练阶段：
  - 继续预训练（continued pretraining）100B tokens
  - 监督微调（SFT）10K instruction 数据
- 得到模型：WeDLM-7B 和 WeDLM-8B

#### 推理设置
- 使用统一的 step-wise 解码方案
- 窗口大小 $ W=6 $
- 距离惩罚系数 $ \lambda=0.1 $
- 温度 $ T=0.1 $，最大长度 512

#### 评估指标
- **生成质量**：各 benchmark 上的准确率
- **推理速度**：tokens per second (tps)，在相同硬件和部署环境下测量
- **公平比较**：
  - AR 模型使用 **vLLM**
  - DLLM 基线使用其推荐引擎（LLaDA/Dream → dInfer；SDAR → JetEngine）
  - WeDLM 也运行于 **vLLM**，验证其与工业系统的无缝集成能力

---

### **基线方法对比**

| 类型 | 基线模型 |
|------|---------|
| AR Baseline | Qwen2.5-7B, Qwen3-8B |
| DLLM Baseline | LLaDA-8B, Dream-7B, SDAR-8B |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 表格总结（来自 Table 1 & 2）

| 模型 | Base Avg | Instruct Avg |
|------|----------|-------------|
| Qwen2.5-7B / Qwen3-8B | 67.21 / 72.61 | 71.09 / 75.12 |
| LLaDA-8B / Dream-7B | 55.44 / 56.91 | 56.77 / 62.96 |
| SDAR-8B | — | 74.22 |
| **WeDLM-7B / WeDLM-8B** | **70.84 / 74.72** | **72.78 / 77.53** |

> ✅ WeDLM 不仅保留了原 AR 模型的能力，还在多数任务上实现了提升。

#### 典型任务表现（WeDLM-8B vs Qwen3-8B）

| 任务 | WeDLM-8B | Qwen3-8B | 提升 |
|------|----------|----------|------|
| GSM8K | 92.27 | 89.91 | +2.36 |
| MATH | 64.80 | 69.60 → 但原文为 69.60，WeDLM 达 64.80？需注意 | — |
| GPQA-Diamond | 44.95 | 41.41 | +3.54 |
| HumanEval | 80.49 | 71.95 | +8.54 |
| HumanEval-plus | 73.78 | 64.63 | +9.15 |

> 🔥 在代码生成任务上优势尤为明显。

---

### **与基线方法的对比结果**

- **速度方面**：
  - 在复杂推理任务（如 GSM8K）上，WeDLM 相比 vLLM 优化的 AR 基线实现 **~3× 加速**
  - 在低熵生成任务（如计数、规则文本）中，最高可达 **10× 以上加速**
  - 图1显示：WeDLM-8B 在保持更高准确率的同时，推理速度远超其他 DLLMs

- **质量方面**：
  - WeDLM-8B-Instruct 在多个 benchmark 上超过其 AR 基线（Qwen3-8B-Instruct）
  - 尤其在 **code generation** 和 **high-difficulty reasoning** 上领先显著

---

### **消融实验结果**

#### （1）Streaming vs Block-wise Decoding
- 流式解码在所有 entropy threshold 下均优于 block-wise
- 最高实现 **1.9× 速度提升**（423 vs 221 tps）
- 原因：更高的 **prefix cacheability (Pcache)**，减少重复计算

#### （2）Distance Penalty ($\lambda$) 影响
- 增大 $\lambda$（偏向左侧 token）可提升 accuracy（+2.6 pts），仅轻微降低 speed（-3%）
- 说明 **left-to-right commitment 有助于减少错误传播**

#### （3）Block Size 敏感性
- 在继续预训练中测试 B ∈ {4,8,32}
- 性能几乎不变（平均分差异 < 0.8 pts）
- 表明 WeDLM 对 block size 不敏感，部署灵活

#### （4）Causal vs Bidirectional Intra-block Attention
- 即使在同一 block 内使用 bidirectional attention，性能反而更低
- 因为它阻碍了 token 级别的 immediate caching
- 进一步证明：**causal structure 更适合 cache-friendly 推理**

#### （5）模型规模影响
- 小模型（0.6B, 1.5B）适应 WeDLM 后略有下降
- 大模型（7B+）表现持续提升
- 暗示存在 **scaling law**：更强的 AR checkpoint 更容易成功迁移到 diffusion 框架

---

## 4. 关键结论和发现

### **主要发现**

1. **KV 缓存兼容性比“每步预测 token 数”更重要**
   - 论文提出 **prefix cacheability (Pcache)** 作为衡量推理效率的核心指标。
   - 即使并行度高，若不能快速形成可缓存的前缀，也无法提升真实吞吐。

2. **Bidirectional attention 并非必需**
   - 通过 Topological Reordering，可在 causal attention 下实现全局上下文可见性。
   - 这使得 DLLM 能无缝继承 AR 模型的缓存机制。

3. **WeDLM 是首个在工业部署条件下超越 vLLM 的 DLLM**
   - 在匹配设置下，实现 **3×~10× 加速**，同时保持甚至提升生成质量。
   - 可直接部署于 vLLM、FlashAttention、PagedAttention 等成熟系统，无需修改 kernel。

4. **训练-推理一致性至关重要**
   - Dual-Stream Masking 成功缩小了训练时随机掩码与推理时前缀推进之间的分布差距。

---

### **方法的局限性**

1. **对高熵任务加速有限**
   - 如开放域问答（解释量子物理），由于输出不确定性高，confidence 较低，parallel speculation 效果差。
   - 实测速度仅约 200 tps，远低于低熵任务（>1600 tps）

2. **依赖高质量 AR checkpoint**
   - 小模型迁移效果不佳，建议从 7B+ 规模开始使用

3. **当前框架仍基于静态窗口**
   - 动态调整窗口大小或 speculative depth 可能进一步优化性能

---

### **未来工作方向**

- 设计更鲁棒的 **acceptance mechanism** 或 **dynamic entropy calibration**，以提升高熵任务的加速比
- 探索 **adaptive window sizing** 和 **multi-step speculation**
- 验证 AR-to-Diffusion 的 scaling laws，系统研究模型容量与 adaptation 效果的关系
- 扩展到多模态 diffusion generation 场景

---

## 总结

✅ **WeDLM 的核心突破在于：将 diffusion-style 并行生成与 standard causal attention 完美结合，实现了真正高效的 prefix-cache-friendly 解码。**

🎯 它不仅理论新颖，而且工程实用——可以直接跑在 vLLM 上，就能打败精心优化的 AR 基线，在速度和质量上双双取胜。

🚀 这标志着 **diffusion language models 正式迈入“可落地”的高性能推理时代**。

</details>

---

### 9. [Modality Inflation: Energy Characterization and Optimization Opportunities for MLLM Inference](https://arxiv.org/abs/2512.22695)

**Authors**: Mona Moghadampanah, Adib Rezaei Shahmirzadi, Farhana Amin, Dimitrios S. Nikolopoulos  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.22695v1  

#### Abstract
Multimodal large language models (MLLMs) are built on text-only LLMs by incorporating additional modalities, enabling multimodal understanding and a broader range of applications. However, these additions introduce a previously unexplored energy trade-off across modalities that remains poorly unders...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Modality Inflation: Energy Characterization and Optimization Opportunities for MLLM Inference*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于**多模态大语言模型（MLLM）推理过程中的能源效率问题**，特别是由“模态膨胀”（Modality Inflation）带来的额外能耗。尽管已有大量研究关注文本大模型（LLM）的能效优化，但对 MLLM 中视觉输入引入的编码开销、token 序列增长及其对整体能效的影响缺乏系统性分析。

### 提出的新方法与新思路
- **首次提出“模态膨胀”（Modality Inflation）概念**：指图像等非文本模态通过视觉编码器生成大量视觉 token，并与文本 token 拼接后显著增加 Prefill 阶段计算负担的现象。
- **细粒度阶段级能效分析框架**：将 MLLM 推理流程分解为三个阶段——**Vision Encoding**、**Prefill** 和 **Decoding**，并量化各阶段的能量消耗分布。
- **基于 GPU 功率轨迹的实证分析**：利用 NVML 工具采集高时间分辨率（5ms）的 GPU 功耗曲线，揭示多模态执行中出现的低利用率“中间功率相位”。
- **提出阶段级动态电压频率调节（Stage-wise DVFS）作为优化策略**：根据不同推理阶段的工作负载特性，动态调整 GPU 频率以实现节能。

### 相比现有方法的优势
| 维度 | 现有工作局限 | 本论文优势 |
|------|--------------|-----------|
| 分析粒度 | 多为端到端或仅关注 Prefill/Decode | 引入 Vision Encoder 阶段，进行全流程三阶段拆解 |
| 能效视角 | 主要关注吞吐量与延迟 | 明确量化“模态引入”的增量能耗（+17%~94%） |
| 优化手段 | 模型压缩、批处理调度为主 | 提出硬件控制层面的 DVFS 优化，更具普适性和即时性 |
| 输入敏感性 | 忽视图像数量与分辨率差异 | 系统评估输入复杂度对能耗的非线性影响 |

---

## 2. 核心实验方法和设置

### 使用的模型
选取四种具有代表性的 MLLM，覆盖不同视觉编码器设计与 token 化策略：

| Model | Vision Encoder | LLM Backbone | 特点 |
|-------|----------------|---------------|------|
| **LLaVA-1.5-7B** | CLIP ViT-L/14 | Vicuna-7B | 固定 patch 分割 |
| **LLaVA-OneVision-Qwen2-7B** | SigLIP ViT | Qwen2-7B | Any-resolution tiling，token 数随分辨率增长 |
| **Qwen2.5-VL-7B** | QwenViT | Qwen2.5-7B | 高计算强度 encoder，支持 token 压缩 |
| **InternVL3-8B** | InternViT-300M | Qwen2.5-8B | 轻量 encoder + 下采样机制 |

所有模型均在 **NVIDIA A100 80GB GPU** 上运行，使用 PyTorch 与 Hugging Face Transformers 框架。

### 数据集与输入配置
- **真实请求分布参考**：采用生产级追踪数据集 **ServeGen [42]** 分析图像数量分布（图2a），发现多数请求含1–2张图，少数高达49张。
- **图像分辨率分析**：统计四个主流 benchmark（VQAv2、VizWiz、ShareGPT4V、ChartQA）的图像短边分辨率（图2b），范围从 256 到 1280 像素以上。
- **实验输入设置**：
  - 固定文本 prompt；
  - 图像输入分为两种模式：
    1. **Iso-resolution**：统一设为 512×512；
    2. **Scaled inputs**：变化图像数量（1–6）和分辨率（256–1408）以测试可扩展性。

### 评估指标
| 指标 | 定义 |
|------|------|
| **Energy per Request (J)** | 单个请求总能耗（通过 NVML 采样积分） |
| **End-to-End Latency (s)** | 从输入到输出完成的时间 |
| **GPU Power Trace (W)** | 实时功耗曲线，用于识别利用率瓶颈 |
| **Throughput (req/s)** | 吞吐能力，评估服务容量 |

### 基线设置
构建与 MLLM 输入 token 总数相匹配的 **text-only LLM baseline**（即用纯文本模拟相同长度输入），从而隔离“模态引入”本身的能耗增量。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ RQ1: 视觉模态的增量能耗有多大？
- 在 iso-token 设置下，相比 text-only 基线，MLLM 的能耗增加 **17% ~ 94%**：
  - **Qwen2.5-VL**: +94% 能耗，+179% 延迟 → 表明其 encoder 极其耗能且串行化严重
  - **LLaVA-OneVision**: +17% 能耗（最低），但产生 **3,715 个视觉 token** → 显示架构设计决定能耗而非 token 数本身
  - **InternVL3**: +18%，表现最均衡

> 🔍 **Observation 1**: 不同 MLLM 架构间能耗差异巨大，需定制化能效策略。

#### ✅ RQ2: 能量分布在哪个阶段？瓶颈在哪？
阶段分解显示能量瓶颈因架构而异：

| 模型 | 主要能耗来源 | 具体数据 |
|------|-------------|--------|
| **Qwen2.5-VL** | Vision Encoder | 编码耗能 **20.81 J**，是 LLaVA-1.5 的 **6×** |
| **LLaVA-OneVision** | Prefill 阶段 | 因 tiling 导致 3,715 tokens，prefill 耗能达 **95.78 J**，为 InternVL3 的 **12×** |
| **InternVL3 / LLaVA-1.5** | 较平衡 | 编码 <10 J，prefill ~8–50 J |

> 🔍 **Observation 2**: 能耗瓶颈跨阶段转移，应实施 **stage-specific 配置** 和 **stage-wise DVFS**。

#### ✅ RQ3: 多模态执行如何改变 GPU 功率行为？
- 文本-only 模型在 batch=32 时迅速达到 GPU 功耗上限（~400W），利用率高；
- 所有 MLLM 均出现明显的 **mid-power phase（100–250W）**，持续数十毫秒，对应 vision encoding 阶段；
- **Qwen2.5-VL** 出现阶梯状功耗（~200W），表明 encoder 内部存在多个子阶段；
- **LLaVA-OneVision** 功耗波动剧烈，反映 tile 并行处理不均。

> 🔍 **Observation 3**: 多模态执行导致 GPU 长时间处于低频低负载状态，“race-to-idle”策略失效。

#### ✅ RQ4: 输入复杂度如何影响能效？
- **图像数量增加**（图6）：
  - 所有模型能量与延迟线性上升；
  - 每增一张图边际成本：**~15–35 J/image**，LLaVA-OneVision 最高。
- **分辨率提升**（图7）：
  - **LLaVA-1.5**（固定 patch）：token 数几乎不变，能耗平坦；
  - **InternVL3 / LLaVA-OneVision**：token 数离散增长，能耗跳跃上升；
  - **Qwen2.5-VL**：>1024×1024 后 token 数激增，能耗陡升。

> 🔍 **Observation 4**: 不同架构对输入复杂度敏感度不同，需 **input-aware serving policy**。

### 消融实验结果（DVFS 效果）
对 **InternVL3** 与 **Qwen2.5-VL** 进行 stage-wise DVFS 测试（batch=32）：

| 场景 | 频率调整 | 能耗变化 | 延迟变化 | 结论 |
|------|---------|----------|----------|------|
| **InternVL3 – Encode** | 1050MHz → 1410MHz | +24.9% | -11.8% | 编码阶段节能空间大，小幅降频即可省电 |
| **InternVL3 – Prefill** | 同上 | +10.6% | -8.8% | prefill 更依赖高频，适合响应优先场景 |
| **Qwen2.5-VL – Encode** | 同上 | +16.5% | -10.8% | encoder 支配整体延迟，需权衡 SLO |

> ✅ **DVFS 可实现有效节能**：在容忍一定延迟的前提下，通过降低非关键阶段频率，显著减少能耗。

---

## 4. 关键结论和发现

### 主要发现
1. **“模态膨胀”是 MLLM 能效的主要瓶颈**：视觉 token 的生成与传播显著抬高了 Prefill 和 Encoder 阶段的能耗，增幅达 **17%–94%**。
2. **能耗瓶颈高度依赖架构设计**：
   - 有的模型受困于 **compute-heavy vision encoder**（如 Qwen2.5-VL）；
   - 有的则因 **token 膨胀导致 Prefill 成为瓶颈**（如 LLaVA-OneVision）。
3. **GPU 存在显著 underutilization**：vision encoding 阶段常处于 mid-power 状态，传统高频策略浪费能源。
4. **输入复杂度驱动非线性能耗增长**：高分辨率或多图像请求虽占比小，却可能主导尾延迟与能耗峰值。
5. **Stage-wise DVFS 是可行优化路径**：可根据 SLO 动态调节各阶段频率，在 **性能与能效之间取得良好折衷**。

### 方法的局限性
- 实验仅基于单卡 A100，未考虑分布式或多设备协同场景；
- DVFS 控制仍为静态配置，尚未实现在线自适应调控；
- 所有模型集中在 7B–8B 参数规模，结论是否适用于更大或更小模型有待验证；
- 未涵盖视频、音频等其他模态，当前分析局限于 vision-language 场景。

### 未来工作方向
1. **扩展至不同规模模型**：研究参数量对“模态膨胀”效应的放大或缓解作用。
2. **开发动态实时 DVFS 控制器**：结合请求特征预测与阶段识别，实现 **SLO-aware frequency scaling**。
3. **探索 disaggregated 架构下的能效趋势**：如将 vision encoder 与 LLM 分离部署，评估跨加速器通信开销与能效收益。
4. **拓展至更多模态**：将分析框架应用于支持 audio、video、time-series 的通用 MLLM。
5. **构建 MLLM 能效建模工具**：基于输入特征（image count/resolution）、模型结构预测能耗与延迟。

---

> 📌 **总体评价**：该论文填补了 MLLM 能效分析领域的空白，提出了“模态膨胀”这一重要概念，并通过详实的实验揭示了其能量分布规律与优化潜力。其 stage-level 分析范式和 DVFS 实践为构建可持续 AI 系统提供了坚实基础。

</details>

---

### 10. [LLMBoost: Make Large Language Models Stronger with Boosting](https://arxiv.org/abs/2512.22309)

**Authors**: Zehao Chen, Tianxiang Ai, Yifei Li, Gongxun Li, Yuyang Wei, Wang Zhou, Guanghui Li, Bin Yu, Zhijun Chen, Hailong Sun, Fuzhen Zhuang, Jianxin Li, Deqing Wang, Yikun Ban  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.22309v1  

#### Abstract
Ensemble learning of LLMs has emerged as a promising alternative to enhance performance, but existing approaches typically treat models as black boxes, combining the inputs or final outputs while overlooking the rich internal representations and interactions across models.In this work, we introduce ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LLMBoost: Make Large Language Models Stronger with Boosting 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **LLM Ensemble** 方法通常将模型视为黑箱，仅在输入或最终输出层面进行融合（如多数投票、加权聚合等），忽略了模型内部丰富的隐藏状态表示和跨模型交互信息。这种粗粒度的融合方式限制了模型间互补知识的充分挖掘。

### 提出的新方法与创新思路
本文提出了 **LLMBooST**，一种新颖的基于提升（Boosting）范式的 **LLM 集成微调框架**，其核心创新在于显式利用 LLM 的中间状态，打破黑箱壁垒。具体包含三大创新：

1.  **跨模型注意力机制 (Cross-Model Attention)**：
    *   允许后续模型（successor）直接访问并融合前驱模型（predecessor）的隐藏层状态。
    *   在层级别构建跨模型连接，实现细粒度的、分层的错误修正与知识传递。

2.  **链式训练范式 (Chain Training Paradigm)**：
    *   采用顺序微调策略，每个后续模型基于其前驱模型的训练日志进行微调。
    *   引入 **错误抑制目标 (Error-Suppression Objective)**，该损失函数旨在抑制前驱模型预测的最大错误 logit，同时增强正确 logit，从而以最小的额外计算量确保性能逐级提升。

3.  **近似并行推理范式 (Near-Parallel Inference Paradigm)**：
    *   为解决串行依赖导致的高延迟问题，提出了一种流水线式推理。
    *   后续模型无需等待前驱模型完成整个序列生成，而是可以立即接收并处理来自前驱模型每一层的隐藏状态，实现了近乎单模型解码效率的推理速度。

### 相比现有方法的优势
-   **性能更强**：通过利用内部表示进行精细的错误修正，显著提升了在常识和算术推理任务上的准确率。
-   **效率更高**：近似并行推理大幅降低了集成方法的推理延迟，相比传统串行集成减少了 47% 的延迟。
-   **理论可证**：从理论上证明了在有界修正假设下，序列集成能保证性能单调提升。

## 2. 核心实验方法和设置

### 使用的数据集
-   **常识推理 (Commonsense Reasoning)**：
    -   PIQA, HellaSwag, WinoGrande, BoolQ, SIQA, OpenbookQA (OBQA)
-   **算术推理 (Arithmetic Reasoning)**：
    -   AQuA, GSM8K, MAWPS, SVAMP
-   **工业场景数据集**：
    -   **Chinatelecom Cloud Agent Dataset (CCAD)**：一个定制的、用于评估 AI 代理在真实公有云环境中工具链调度能力的数据集。

### 实验设置和评估指标
-   **微调方法**：采用 **LoRA** 进行参数高效微调。
-   **评估指标**：
    -   主要指标为 **准确率 (Accuracy)**。
    -   效率指标包括 **端到端延迟 (End-to-end latency)**、**每 token 生成成本 (per-token latency)** 和 **峰值 GPU 内存占用 (peak GPU memory usage)**。
-   **模型规模**：在多种参数规模上进行了测试，包括 Llama-3.1-8B, Llama-3.2-3B, Qwen-2.5-7B, Qwen-2.5-3B 等，并构建了同构（如 2×8B）和弱异构（如 8B+3B）的集成。

### 基线方法对比
-   **VOTE**：基于多数投票的集成方法。
-   **UNITE**：一种高效的 LLM 集成方法，通过融合各模型 top-k token 来组合模型。
-   **T-copilot**：一种通过辅助适配器（copilot adapter）来修正主模型（pilot）错误的方法。

## 3. 主要实验结果和性能指标

### 关键性能数据
-   **总体平均提升**：LLMBooST 相比其他集成基线，在各项任务测试中平均提升了 **3.9%** 的准确率，最高提升达 **6.6%**。
-   **CCAD 数据集表现**：在 Chinatelecom Cloud Agent Dataset 上，LLMBooST 将准确率从 Llama-3.1-8B 单模型的 52.0% 提升至 **55.0%**，相比 VOTE 基线提升了 3.0%。
-   **推理效率**：LLMBooST 的近似并行解码策略相比传统串行集成，将推理延迟降低了 **47%**。

### 与基线方法的对比结果
-   **算术推理 (Table 1)**：
    -   在 Llama-3.1-8B (2×8B) 设置下，LLMBooST 的平均准确率达到 **71.5%**，显著优于 VOTE (69.8%)、UNITE (68.5%) 和 T-copilot (68.7%)。
    -   在 Qwen-2.5-7B (2×7B) 设置下，平均准确率为 **80.2%**，同样优于所有基线。
-   **常识推理 (Table 2)**：
    -   在 Llama-3.1-8B (2×8B) 设置下，LLMBooST 平均准确率为 **84.4%**，优于 UNITE (83.3%) 和 VOTE (79.7%)。
    -   在 Qwen-2.5-7B (2×7B) 设置下，平均准确率为 **85.6%**，远超其他基线。
-   **参数预算对比 (Table 3)**：
    -   两个 Qwen-2.5-3B 模型集成 (6B) 的 LLMBooST 性能达到了单个 Qwen-2.5-7B (7B) 的水平，且算术推理得分更高，同时节省了 1B 参数。
    -   两个 Qwen-2.5-7B 模型集成 (14B) 的性能超过了单个 Qwen-2.5-14B (14B) 模型，且推理速度更快（延迟从 98ms/token 降至 75ms/token）。

### 消融实验结果
-   **移除错误抑制目标 (wo E.S.O.)**：性能明显下降，验证了该目标对错误修正的关键作用。
-   **禁用跨模型注意力 (wo C.A.)**：性能下降，证明了利用前驱模型隐藏状态的重要性。
-   **移除 top-k 回传 (wo top-k)**：性能略有下降，表明该机制有助于稳定推理。
-   **超参数敏感性分析**：实验表明，`α` 和 `β` 的选择对性能有影响，存在最优值（如 `α=0.9`, `β=0.1`），而 `top-k` 选择较小的值（如 k=2）即可获得最佳效果。

## 4. 关键结论和发现

### 主要发现
1.  **内部交互至关重要**：打破 LLM 集成的“黑箱”假设，利用模型间的内部隐藏状态进行交互，是提升集成性能的有效途径。
2.  **提升范式有效**：借鉴 Boosting 的思想，通过链式训练和错误抑制目标，能够系统性地修正前驱模型的错误，实现性能的渐进式提升。
3.  **效率与性能兼得**：提出的近似并行推理范式成功解决了多模型串行推理的高延迟问题，使得高性能集成在实际应用中更具可行性。
4.  **理论与实践一致**：理论分析证明了在合理假设下，LLMBooST 能够保证性能单调提升，这与实验观察到的结果相符。

### 方法的局限性
-   **跨模型兼容性限制**：当前方法要求集成的子模型具有相同的结构设计和隐藏状态格式，目前主要适用于同一模型系列内的同构或弱异构集成（如 LLaMA-3 或 Qwen-2.5 系列）。对于不同家族的异构模型（如 LLaMA 和 Qwen），由于 tokenizer 和架构差异，难以直接应用。

### 未来工作方向
-   将框架扩展到 **多智能体 (multi-agent)** 场景，让不同智能体专精于推理、规划或工具使用。
-   探索 **自适应路由机制 (adaptive routing)**，根据任务上下文动态选择智能体或推理路径。
-   结合 **投机推理 (speculative reasoning)**，进一步降低大规模部署中的延迟，提高可扩展性。

</details>

---

### 11. [Role-Based Fault Tolerance System for LLM RL Post-Training](https://arxiv.org/abs/2512.22492)

**Authors**: Zhenqian Chen, Baoquan Zhong, Xiang Li, Qing Dai, Xinkui Zhao, Miao Ye, Ren Cheng, Lufei Zhang, Jianwei Yin  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.22492v1  

#### Abstract
RL post-training for LLMs has been widely scaled to enhance reasoning and tool-using capabilities. However, RL post-training interleaves training and inference workloads, exposing the system to faults from both sides. Existing fault tolerance frameworks for LLMs target either training or inference, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Role-Based Fault Tolerance System for LLM RL Post-Training

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大语言模型（LLM）的强化学习（RL）后训练系统在扩展过程中面临严重的**容错性挑战**。RL 后训练同时包含 **rollout（推理/工具交互）** 和 **training（策略优化）** 阶段，二者交错执行且耗时长。现有容错框架（如 ByteRobust）通常采用“全任务重启”机制，一旦任一角色（trainer 或 rollout）发生机器故障，整个 RL 任务都会被中断并从检查点重新开始，导致大量已生成的 rollout 轨迹浪费、初始化延迟显著。

此外，传统基于 rank-level 或 cluster-level 的故障检测机制在 RL 场景下存在**误报**（如 rollout 等待工具响应时 GPU 空闲被误判为故障）或**延迟高**的问题。

### 提出的新方法与思路
作者提出了 **RobustRL** ——首个面向 LLM RL 后训练的**基于角色的容错系统**，其核心思想是：

- **角色隔离（Role-Based Isolation）**：将 trainer、rollout 和管理角色视为独立的分布式子任务，实现故障隔离。
- **Detect-Restart-Reconnect 范式**：
  1. **Detect**：设计**角色感知的监控机制**，区分不同阶段的行为特征，避免误检。
  2. **Restart**：仅重启失败的角色，而非整个任务；利用 rollout 作为 trainer 的 warm standby，实现非中断恢复。
  3. **Reconnect**：用基于 **UCX（Unified Communication X）** 的动态点对点通信替代静态的 NCCL 集体通信，支持故障后快速权重同步。

### 相比现有方法的优势
| 维度 | ByteRobust / 传统方法 | RobustRL |
|------|------------------------|---------|
| 容错粒度 | 全任务重启 | 角色级细粒度恢复 |
| Rollout 进度保留 | ❌ 失去所有 rollout 进展 | ✅ 保持 rollout 继续运行 |
| Trainer 恢复速度 | 依赖额外 warm standby 机器（资源浪费） | 动态借用 rollout 机器作为 warm standby（资源高效） |
| 权重同步 | NCCL 静态组通信，不支持动态加入 | UCX 动态点对点 + Relay Server 设计，支持故障恢复连接 |
| 故障检测准确性 | Rank-level 易误报，Cluster-level 延迟高 | Phase-aware 检测，准确率高、延迟低 |

---

## 2. 核心实验方法和设置

### 数据集
- **数学推理任务**：`DAPO-Math17K`，使用规则奖励函数评分。
- **软件工程任务（多轮工具调用）**：`SWE-bench`，通过沙箱环境进行代码修复或多轮交互生成轨迹。

### 实验设置
- **模型规模**：Qwen3-8B、Qwen3-32B、Qwen3-235B-A22B。
- **硬件平台**：32 台 GPU 服务器，共 256 块 NVIDIA H20 96GB GPU，NVLink + 4×200Gbps NIC。
- **训练架构**：测试了三种模式：
  - **Sync**：同步模式（hybrid 部署）
  - **Semi-sync**：半同步（混合 + 独立 rollout）
  - **Async**：异步模式（独立 trainer 与 rollout）
- **故障注入频率**：每 10% 的训练步骤注入一次 trainer 故障（极端场景模拟 spot instance 不稳定）。
- **批大小**：全局 batch size 512，每 batch 64 prompts，每个 prompt 生成 8 个 response。
- **最大长度**：65536 tokens，最多 50 轮工具交互。

### 评估指标
- **End-to-End Training Time**：总训练时间。
- **ETTR（Effective Training Time Ratio）**：有效训练时间占比（排除重启开销）。
- **Restart Latency Breakdown**：实例重启、checkpoint 加载、worker 初始化等各阶段耗时。
- **Weight Synchronization Efficiency**：模型权重从 trainer 同步到 rollout 的延迟。
- **Checkpoint Overhead**：每步保存 checkpoint 的阻塞时间。

### 基线方法对比
- **Baseline**：无故障的理想情况。
- **ByteRobust**：字节跳动提出的通用 LLM 训练容错系统，采用全任务重启 + warm standby 机制。
- **RobustRL**：本文提出的方法。

> 注：未与其他开源 RL 容错系统（如 Laminar）直接比较，因其代码未公开。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 端到端训练时间 vs. ETTR（图11）
| 任务 | 方法 | 训练时间（小时） | ETTR |
|------|------|------------------|-------|
| Qwen3-8B-Math | Baseline | ~5.0 | 100% |
|                | ByteRobust | ~7.8 | ~60% |
|                | **RobustRL** | **~6.0** | **>80%** |
| Qwen3-32B-SWE | ByteRobust | ~30.5h | ~60% |
|               | **RobustRL** | **~26.0h** | **>80%** |

- **ETTR 提升**：RobustRL 在 Qwen3-8B-Math 上达到 **>80% ETTR**，相比 ByteRobust（~60%）提升约 **20个百分点**。
- **训练加速**：在 10% 故障注入下，RobustRL 比 ByteRobust 快 **8.4%–17.4%**。
- **节省时间**：
  - 8B-Math：节省 0.8–2.1 小时
  - 32B-SWE：节省 3.5–4.5 小时

#### ✅ 重启效率对比（图14）
- **平均重启延迟降低 1.5–1.7×**：
  - 例如，在 256 GPU 上，ByteRobust 重启耗时 ~400s，RobustRL 仅 ~230s。
- **优势来源**：
  - 跳过容器初始化和 Ray 集群重建；
  - 利用 rollout warm standby 避免 gang scheduling 延迟。

#### ✅ 权重同步效率（图17–18）
- **UCX vs NCCL**：
  - 对于 235B 模型（470GB），理论传输时间 4.7s，实际 UCX 实现 **~6s**，接近理想带宽利用率。
  - NCCL 因需 gather 到 rank0 再 broadcast，扩展性差；而 UCX 支持点对点 + relay server，可线性扩展。
- **Relay 设计效果明显**：多个 rollout 可并行从已完成更新的 rollout 拉取权重，避免 trainer 成为瓶颈。

#### ✅ Checkpoint 开销（图19）
- **GPU → Memory 阻塞时间 < 5 秒**，远小于单步 RL 时间（分钟级甚至小时级）。
- 使用 **ByteCheckpoint** 异步写盘，内存到磁盘过程非阻塞。
- **每步 checkpoint 开销占比 < 1%**，可接受。

#### ✅ 消融实验与分析（图15–16）
- **Rollout 进度保留收益巨大**：
  - SWE 任务中 rollout 尾部延迟高达 **1050s**，输出长度超 4k tokens。
  - RobustRL 避免了这些长尾 rollout 的重放，显著减少无效计算。
- **吞吐稳定性**：
  - 当 rollout 故障时，RobustRL 通过副本接管，token 解码吞吐几乎不受影响。
  - Trainer 故障期间，rollout 仍持续生成轨迹，维持系统活跃。

---

## 4. 关键结论和发现

### 主要发现
1. **角色隔离是 RL 容错的关键**：将 trainer 与 rollout 视为独立角色，能有效隔离故障影响，避免“牵一发而动全身”的全任务重启。
2. **rollout 可作为 trainer 的 warm standby**：在 semi-sync/async 架构中，动态借用 rollout 机器用于 trainer 恢复，既节省资源又加快恢复速度。
3. **UCX 动态通信优于 NCCL**：NCCL 不支持动态成员变更，无法适应故障恢复场景；UCX 的点对点 + relay 架构更适合异步 RL 中的权重同步需求。
4. **per-step checkpoint 可行且必要**：尽管开销存在，但在 RL 步骤长达数分钟/小时的背景下，其相对开销极小，且能保证状态一致性，避免重 rollout。
5. **RobustRL 在复杂任务中收益更大**：SWE 类涉及多轮工具调用的任务，rollout 时间更长，因此保留进度带来的收益更显著。

### 方法的局限性
- **诊断能力有限**：当前系统依赖外部信号判断是否为“可恢复故障”，对于代码错误或配置错误仍需人工介入或全任务重启。
- **同质化假设**：warm standby 机制要求 rollout 与 trainer 机器同构且在同一数据中心，限制了跨集群部署灵活性。
- **未处理 silent data corruption 或 straggler 问题**：虽然架构上支持扩展，但目前未集成相关检测模块。

### 未来工作方向
- **更精细的根因定位工具**：开发专用于 RL 训练系统的诊断工具，识别跨角色依赖引发的故障（如 OOM 由共置角色引起）。
- **角色级热更新（Hot Update）**：结合角色隔离，支持在线调整 batch size、learning rate 等参数，无需重启。
- **弹性 RL 训练（Elastic RL）**：支持 trainer 在 data parallel 维度动态扩缩容，进一步提升资源利用率。
- **支持更多通信后端与异构设备**：扩展 UCX 支持多种网络拓扑和 GPU 类型，增强部署灵活性。

---

> **总结一句话**：  
> RobustRL 通过 **role-based fault isolation + detect-restart-reconnect 范式**，首次实现了对 LLM RL 后训练全流程的高效容错，在高故障率环境下仍能保持 >80% 的 ETTR，并将训练时间缩短 8.4%–17.4%，为大规模、长时间的 RL 训练提供了可靠基础设施支持。

</details>

---

### 12. [ReGAIN: Retrieval-Grounded AI Framework for Network Traffic Analysis](https://arxiv.org/abs/2512.22223)

**Authors**: Shaghayegh Shajarian, Kennedy Marsh, James Benson, Sajad Khorsandroo, Mahmoud Abdelsalam  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.22223v1  

#### Abstract
Modern networks generate vast, heterogeneous traffic that must be continuously analyzed for security and performance. Traditional network traffic analysis systems, whether rule-based or machine learning-driven, often suffer from high false positives and lack interpretability, limiting analyst trust....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReGAIN: Retrieval-Grounded AI Framework for Network Traffic Analysis — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统网络流量分析系统面临以下挑战：
- **规则驱动系统**（如 Snort、Suricata）依赖人工编写的签名，维护成本高，误报率高，缺乏可解释性。
- **机器学习模型**（如 SVM、Random Forest、深度学习）虽然检测准确率较高，但多为“黑箱”模型，难以提供可验证的推理过程，降低了安全分析师的信任度。
- **纯生成式 LLMs** 在无外部知识支持下容易产生**幻觉**（hallucinations），输出不可靠或无法验证的结论。

### 提出的新方法：ReGAIN 框架
ReGAIN 是一个结合 **网络流量摘要化**、**检索增强生成（RAG）** 和 **大语言模型（LLM）推理** 的多阶段框架，用于实现**透明且准确的网络流量分析**。

#### 核心组件：
1. **数据摄入与摘要化（Data Ingestion and Summarization）**
   - 将原始网络日志（如 PCAP、flow records）转换为自然语言摘要（natural-language summaries），便于人类理解并提升嵌入质量。
2. **语义向量化与知识库构建（Semantic Vectorization and Knowledge Base Builder）**
   - 使用 `all-MiniLM-L6-v2` 等嵌入模型将摘要编码为向量，并存储在 **ChromaDB** 中。
   - 构建**多集合向量数据库**（multi-collection vector DB），包括：
     - 流量集合（telemetry）
     - 异常集合（anomaly）
     - 启发式集合（heuristic）
3. **检索增强推理引擎（Retrieval-Augmented Reasoning and Generation）**
   - 多级检索流程：
     - 元数据过滤（metadata filtering）
     - MMR（Maximal Marginal Relevance）多样性采样
     - Bi-encoder 初筛 + Cross-encoder 重排序
   - 引入**弃权机制（abstention mechanism）**：当检索证据不足时返回 “undecidable”，避免幻觉。
4. **人机协同交互（Human-in-the-Loop Interaction）**
   - 支持分析师通过 CLI 迭代查询，逐步深入调查。

### 相比现有方法的优势
| 特性 | ReGAIN | 传统方法 |
|------|--------|---------|
| 可解释性 | ✅ 明确引用证据（cited evidence） | ❌ 黑箱或模板化输出 |
| 准确性 | ✅ 高精度与召回率 | ⚠️ 规则系统误报高，ML 缺乏解释 |
| 抗幻觉能力 | ✅ 弃权机制 + 多阶段检索 | ❌ 纯 LLM 容易幻觉 |
| 灵活性 | ✅ 支持自然语言查询与迭代分析 | ❌ 固定规则或静态模型 |

此外，ReGAIN 是首个将 **多集合 RAG + 分层检索 + 弃权机制** 应用于网络流量分析的工作，显著提升了 LLM 在网络安全中的可信度。

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **MAWILab v1.1** 真实网络流量数据集（2022年1月1日、9日、10日的 PCAPs 和结构化异常标注 CSV）。
- 聚焦两种攻击类型：
  - **TCP SYN Flood**
  - **ICMP Ping Flood**
- 包含约 10,000 条标注实例，涵盖多种协议行为与异常标签（heuristic 和 taxonomy 类型）。

### 实验设置
| 组件 | 配置 |
|------|------|
| 嵌入模型 | `all-MiniLM-L6-v2` (384-D) |
| 重排序模型 | `cross-encoder/ms-marco-MiniLM-L-6-v2` |
| 向量数据库 | ChromaDB（三个持久化集合） |
| 编排框架 | LangChain |
| LLM | GPT-4.1-nano（temperature=0，确保确定性输出） |
| 检索参数 | k ∈ {3,5}，相似度阈值 T=0.3，MMR 参数 k=3–6 |

### 评估指标
- **Accuracy**, **Precision**, **Recall**, **F1 Score**, **AUC**
- 采用**双模式评估策略**：
  1. **自动化评估**：基于数据集提供的 ground truth 标签。
  2. **专家评估**：由领域专家对子集进行盲审，独立打标，验证系统的实际可用性。

### 基线方法对比
比较了五类基线方法：
1. **Rule-based IDS**（Snort 风格阈值启发式）
2. **SVM**（RBF 核）
3. **Random Forest**（100 棵树）
4. **1D-CNN**
5. **2-layer LSTM**

所有模型均使用从 Zeek `conn.log` 提取的 30 个数值特征（连接状态、字节/包数量、IP/端口多样性等），并通过 StandardScaler 归一化。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（表 II）

| Attack | Acc. | Prec. | Recall | F1 | AUC |
|--------|------|-------|--------|-----|-----|
| SYN (GT) | **98.82%** | **100.00%** | 98.64% | 99.32% | 0.99 |
| SYN (Expert) | 95.95% | 97.20% | 98.07% | 97.63% | 0.98 |
| Ping (GT) | 97.56% | 74.48% | **100.00%** | 85.37% | 0.99 |
| Ping (Expert) | 97.74% | 76.36% | **100.00%** | 86.60% | 0.99 |

> GT = Ground Truth；Expert = 专家标注

#### 关键观察：
- **SYN Flood**：近乎完美表现，precision 达到 100%，说明一旦判定即高度可信。
- **Ping Flood**：recall 完美（100%），但 precision 约 75%，表明存在少量误报（主要是良性高频 ICMP 诊断流量被误判）。

### 与基线方法的对比（图 7）
- 对于 **SYN Flood**：
  - ReGAIN 比最强基线（LSTM）提升：
    - Accuracy ↑3.7%
    - Precision ↑14.5%
    - Recall ↑3.8%
- 对于 **Ping Flood**：
  - ReGAIN 实现 **100% Recall**（LSTM 仅 95.6%），虽 precision 略低（74.5% vs. ~70%），但在安全场景中更优——宁可误报也不漏报。

### 消融实验（文中未明确列出表格，但从设计推断）
尽管没有正式消融研究，但框架设计体现了多个关键模块的作用：
- **多集合检索** → 提升上下文相关性和多样性
- **Cross-encoder 重排序** → 提高检索质量
- **MMR 采样** → 减少冗余证据
- **弃权机制** → 显著降低幻觉风险，在证据不足时拒绝回答

这些机制共同保障了 LLM 输出的**可验证性**和**可靠性**。

---

## 4. 关键结论和发现

### 主要发现
1. **ReGAIN 在真实流量中表现出色**：
   - 在 SYN 和 Ping Flood 检测任务上，accuracy 达 **95.95%–98.82%**，recall 接近或达到 100%。
   - 性能优于所有传统 rule-based、classical ML 和 deep learning 基线。
2. **可解释性是核心优势**：
   - 所有判断均附带**引用证据 ID 或 heuristic code**，支持审计与溯源。
   - 输出格式标准化：verdict + justification + mitigation recommendation。
3. **人机协作潜力巨大**：
   - 支持自然语言查询与迭代细化（如从“显示涉及某 IP 的异常”到“比较同一时间段内的 TCP SYN 行为”），模拟真实 incident response 工作流。
4. **专家评估揭示 ground truth 局限性**：
   - 自动标注可能过于宽松或遗漏边缘案例，而 ReGAIN 结合专家判断后仍保持高性能，证明其鲁棒性。

### 方法的局限性
1. **延迟较高**：依赖远程 API（如 GPT-4）导致推理延迟，不适合实时监控，更适合**事后分析与取证**。
2. **Ping Flood 精度偏低**（~75%）：因良性 ICMP 探测（如 ping sweep、latency check）与攻击模式相似，易误判。
3. **嵌入维度较低**（384-D）：为优化推理质量牺牲了吞吐效率，大规模部署需权衡。
4. **当前依赖云服务**：缺乏本地化部署能力，影响敏感环境下的隐私与合规性。

### 未来工作方向
1. **引入本地模型**：
   - 替换为 **LLaMA-3-8B**、**Mistral-7B** 或 **Phi-3** 等可在本地运行的 LLM，实现离线操作。
2. **动态相似度阈值与速率过滤器**：
   - 根据协议类型和流量强度自适应调整检测灵敏度，减少 ICMP 误报。
3. **优化自然语言输出质量**：
   - 开展用户研究，评估生成文本的清晰度与实用性，进一步提升 analyst usability。
4. **扩展至更多攻击类型**：
   - 如 DNS tunneling、port scanning、C2 通信等，验证框架泛化能力。

---

> ✅ **总结一句话**：  
> ReGAIN 成功将 **RAG + LLM + 多级检索 + 弃权机制** 融入网络流量分析，实现了**高准确性、强可解释性、抗幻觉**的智能分析系统，推动网络安全从“告警驱动”迈向“对话式分析”。

</details>

---

### 13. [HLS4PC: A Parametrizable Framework For Accelerating Point-Based 3D Point Cloud Models on FPGA](https://arxiv.org/abs/2512.22139)

**Authors**: Amur Saqib Pal, Muhammad Mohsin Ghaffar, Faisal Shafait, Christian Weis, Norbert Wehn  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.22139v1  

#### Abstract
Point-based 3D point cloud models employ computation and memory intensive mapping functions alongside NN layers for classification/segmentation, and are executed on server-grade GPUs. The sparse, and unstructured nature of 3D point cloud data leads to high memory and computational demand, hindering ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HLS4PC: A Parametrizable Framework For Accelerating Point-Based 3D Point Cloud Models on FPGA

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **3D点云模型在GPU上执行效率低**：由于点云数据具有稀疏性和非结构化特性，传统的GPU在处理如Farthest Point Sampling (FPS)、K-Nearest Neighbor (KNN)等映射函数时存在严重的资源利用率不足问题。
- **缺乏支持点云专用操作的FPGA加速框架**：现有的FPGA深度学习框架（如fpgaConvNet、hls4ml）主要面向规则张量运算，无法有效支持点云特有的不规则计算流程。

### 🚀 提出的新方法与创新
- **提出HLS4PC框架**：
  - 一个基于High Level Synthesis (HLS) 的可参数化、流式(dataflow)架构的FPGA加速框架，专为**点云模型中的映射函数 + DNN层联合加速**设计。
  - 支持混合精度（mixed-precision）、编译时配置并行度，并能灵活部署不断演进的点云模型。
- **引入硬件感知压缩策略构建PointMLP-Lite**：
  - 用Uniform Random Sampling (URS) 替代FPS：提升硬件友好性；
  - 输入点剪枝（从1024降至512）；
  - 参数量化至8/8-bit（weights/activations）；
  - BatchNorm与卷积层融合以减少内存开销；
  - 移除几何归一化参数 $ \alpha $ 和 $ \beta $。
- **实现完整的端到端自动化流程**：
  - 从PyTorch/TensorFlow预训练模型 → 量化感知训练 → HLS代码生成 → FPGA比特流部署。

### 🔍 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **灵活性** | 支持动态调整精度、并行度、采样方式等，优于ASIC类专用加速器 |
| **高效性** | 针对点云特有操作（URS、KNN）进行硬件优化，显著提升吞吐量 |
| **能效比** | 能量效率高达294.5 GOPS/W，是前人工作的57.4× |
| **通用性** | 可扩展至其他点云模型（如DGCNN、PointNet++等） |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **ModelNet40**：广泛使用的3D物体分类基准，包含40类CAD模型。
- **ScanObjectNN**：真实场景采集的点云数据集，更具挑战性，用于验证泛化能力。

### ⚙️ 实验设置
- **目标模型**：基于SOTA模型 **PointMLP-Elite** 进行压缩与优化，得到轻量版 **PointMLP-Lite**。
- **训练环境**：
  - GPU: NVIDIA RTX 3090
  - 框架: PyTorch 2.4.0 + Brevitas（用于量化感知训练）
  - 训练参数: batch size=256, epochs=1000, SGD优化器
- **FPGA部署平台**：
  - 开发板: Xilinx Zynq 7000 SoC ZC706
  - 工具链: Vivado HLS 2018.3
- **评估指标**：
  - **Overall Accuracy (OA)** 和 **Mean Accuracy (mA)**
  - **Throughput (GOPS)**：每秒十亿次操作
  - **Energy Efficiency (GOPS/W)**：能效比
  - 资源占用：LUT、FF、DSP、BRAM、URAM

### 🔁 基线对比方法
- **FPGA基线**：
  - SOCC 2022 [14]: ISSCN on ZCU102
  - ISCAS 2020 [1]: PointNet on ZCU104
  - CSSP 2023 [3]: DGCNN (FP32) on Ultrascale+
  - ASICON 2019 [18]: O-PointNet on ZC706
- **CPU/GPU对比**：
  - Tesla V100（原模型PointMLP-Elite）
  - RTX 3060 Ti（PointMLP-Lite）
  - Intel i5-13400 CPU

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自Table 2 & Table 3）

| 指标 | 结果 |
|------|------|
| **PointMLP-Lite模型复杂度降低** | 4× less complex than PointMLP-Elite |
| **准确率下降（ModelNet40）** | OA仅下降约2%（93.6% → 91.69%） |
| **FPGA吞吐量** | **1648 GOPS** |
| **相比之前FPGA工作提升** | **3.56×更高throughput** |
| **相比GPU（RTX 3060 Ti）** | **2.3×更高throughput** |
| **相比CPU（i5-13400）** | **22×更高throughput** |
| **能量效率** | **294.5 GOPS/W**（比前人最高提升57.4×） |

### 🔬 消融实验分析（Table 1 & Fig. 4）
通过逐步应用压缩技术观察准确率变化：

| 模型变体 | 关键改动 | ModelNet40 OA (%) | 备注 |
|--------|---------|------------------|------|
| PointMLP-Elite | 原始模型 | 93.60 | N/A |
| M-1 | URS替代FPS | 92.30 | 准确率轻微下降，硬件更友好 |
| M-2 | 输入减半（512 pts）+ URS | **91.69** | 性价比最优，选作基础 |
| M-3 | 加入BN融合 | 90.56 | 再降1.13%，节省存储 |
| M-4 | 进一步剪枝至128 pts | 86.87 | 准确率明显下降 |

> ✅ **Pareto前沿分析显示**：8/8-bit量化的M-2版本在保持高准确率的同时实现4倍模型简化，成为最佳折中方案 —— 即最终的 **PointMLP-Lite**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **URS可以有效替代FPS**：尽管引入随机性，但通过增大batch size和训练轮数（up to 1000 epochs），模型仍可学习稳定的几何不变特征。
2. **硬件感知压缩显著降低复杂度**：结合输入剪枝、量化、层融合等手段，在仅损失~2%准确率的前提下将模型缩小4倍。
3. **FPGA在点云推理中潜力巨大**：得益于其高度并行、低功耗、可重构特性，配合算法-硬件协同设计，可在吞吐量和能效上全面超越GPU和CPU。
4. **流式dataflow架构适合点云处理**：避免中间结果驻留片外内存，极大缓解带宽瓶颈。

### ⚠️ 局限性
- **当前仅支持分类任务**：尚未扩展至语义分割或检测等更复杂任务。
- **依赖特定FPGA工具链（Vivado HLS）**：跨平台移植性受限。
- **URS带来的不确定性可能影响安全关键应用的一致性表现**。
- **未探索动态输入长度的支持**：目前固定输入点数（如512）。

### 🔮 未来工作方向
- 探索 **Hilbert Curve-based Sampling** 来替代URS，在保留硬件友好性的同时减少精度损失。
- 扩展框架支持更多点云模型（如PointTransformer、PV-RCNN）及下游任务（segmentation, detection）。
- 引入自动搜索机制（Auto-HLS）优化HLS参数配置（如PE数量、流水线深度）。
- 支持多FPGA分布式推理，应对更大规模点云场景。

---

> 💡 **开源声明**：作者承诺将发布HLS4PC框架代码，地址为：  
> [https://github.com/dll-ncai/HLS4PC](https://github.com/dll-ncai/HLS4PC)

</details>

---

### 14. [Nightjar: Dynamic Adaptive Speculative Decoding for Large Language Models Serving](https://arxiv.org/abs/2512.22420)

**Authors**: Rui Li, Zhaoning Zhang, Libo Zhang, Huaimin Wang, Xiang Fu, Zhiquan Lai  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.22420v1  

#### Abstract
Speculative decoding (SD) accelerates LLM inference by verifying draft tokens in parallel. However, this method presents a critical trade-off: it improves throughput in low-load, memory-bound systems but degrades performance in high-load, compute-bound environments due to verification overhead. Curr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Nightjar: Dynamic Adaptive Speculative Decoding for Large Language Models Serving*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **固定 speculative length 的局限性**：现有的 Speculative Decoding (SD) 方法通常采用固定的 speculative length（如 γ=3），无法适应动态变化的请求负载（request load）。
- **高负载下的性能退化**：在 compute-bound 高批处理场景中，SD 的验证开销会超过其收益，导致吞吐量下降甚至劣于标准 autoregressive (AR) 解码。
- **决策死锁与切换成本被忽视**：现有方法（如 DSD、SpecServe）依赖历史 acceptance rate 进行优化，一旦关闭 SD 就无法收集新数据，陷入“决策死锁”；同时忽略了从 `γ=0` 切换回正 speculative length 时的 KV cache 重建开销（即 prefill cost）。

### 🚀 提出的新方法：**Nightjar**
- **基于 Contextual Multi-Armed Bandit (MAB) 的自适应策略**：
  - 将 speculative length 的选择建模为一个在线学习问题，使用 **ADA-BINGREEDY** 框架实现探索-利用权衡。
  - 引入三层时间结构（Epoch → Block → Bin）以支持连续批处理（continuous batching）环境中的长期学习。
- **动态感知负载并智能决策**：
  - 能根据实时 batch size 自动选择最优的 speculative length γ ∈ {0,1,...,γ_max}，包括在必要时完全禁用 SD（γ=0）。
- **显式建模 Switching Cost**：
  - 在目标函数中引入惩罚项 $ \mathbb{I}(y_{t-1}=0 \land y_t > 0) \cdot C_{\text{prefill}} / y_t $，考虑 KV cache 重建延迟，并鼓励更长的 speculative 序列来摊销启动开销。

### 🔍 相比现有方法的优势
| 方法 | 局限性 | Nightjar 的改进 |
|------|--------|----------------|
| 固定 γ 的 SD | 无法适应负载变化 | 动态调整 γ，低负载加速，高负载避免退化 |
| DSD [15] | 依赖平均 acceptance rate，易陷“决策死锁” | 不依赖历史 acceptance，持续探索 |
| BanditSpec [12] | 静态设计，不适用于 continuous batching | 支持动态 batch size 和异步请求流 |
| TETRIS/SpecServe | 忽略 switching cost，大 batch 下 overhead 高 | 显式建模 prefill 开销，提升决策鲁棒性 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **真实生产 trace**：来自 *AzureLLMInferenceDataset* 的动态请求率片段（Fig. 9）
- **对话类任务**：
  - *ShareGPT*
  - *Alpaca*
- **综合基准测试**：
  - *SpecBench*：包含 480 个来自多个常用数据库的任务实例
- 输入输出长度分布见 Fig. 7，涵盖短到长上下文场景。

### ⚙️ 实验设置
- **模型配置**：
  - **7B 模型组**：DeepSeek-R1-Distill-Qwen-7B（target） + DeepSeek-R1-DRAFT-Qwen2.5-0.5B（draft），运行于 RTX 4090 (24GB)
  - **13B 模型组**：vicuna-13b（target） + vicuna-68m（draft），运行于 A100 (40GB)
- **系统平台**：基于 vLLM (v0.8.2) 扩展实现 Nightjar
- **speculative length 搜索空间**：γ ∈ {0,1,2,3,4,5}
- **评估指标**：
  - **Throughput**：生成 token 数 / 秒（tokens/s）
  - **Mean End-to-End Latency**：从请求提交到完成的平均延迟（ms）

### 🆚 基线方法对比
| 基线 | 描述 |
|------|------|
| **w/o SD** | 标准 autoregressive 解码，无 speculative |
| **SD (γ=3)** | 固定 speculative length=3 的标准 SD |
| **DSD [15]** | 基于 goodput 模型动态调优 γ |
| **BanditSpec [12]** | 使用 MAB 的早期自适应方法，但静态设计 |
| **TETRIS [14]** | 基于 confidence score 的 batch-level 优化 |

所有结果取 5 次独立运行的平均值。

---

## 3. 主要实验结果和性能指标

### 📈 吞吐量提升（Throughput）
#### 表 2：不同模型与数据集下的 Throughput 对比（单位：tokens/s）

| Method | Alpaca (7B) | ShareGPT (7B) | SpecBench (7B) | Alpaca (13B) | ShareGPT (13B) | SpecBench (13B) |
|--------|-------------|---------------|----------------|--------------|----------------|------------------|
| **Nightjar** | **2102.73** | **3734.23** | **3326.20** | **721.23** | **1729.68** | **1061.82** |
| BanditSpec | 1860.74 | 3428.75 | 2631.43 | 679.82 | 1383.81 | 763.15 |
| DSD | 2044.40 | 3614.65 | 2789.51 | 603.32 | 1244.96 | 692.01 |
| TETRIS | 1875.91 | 3597.21 | 2974.14 | 702.65 | 1628.86 | 985.40 |
| SD | 1920.63 | 3593.36 | 3044.80 | 628.47 | 1689.53 | 964.02 |
| w/o SD | 2072.38 | 3653.61 | 3207.28 | 438.13 | 1290.04 | 673.48 |

✅ **关键发现**：
- Nightjar 在所有场景下均取得最高 throughput。
- 在 13B + Alpaca 上，相比 vanilla decoding 提升高达 **64.6%**。
- 相比先进系统 TETRIS，在 7B Alpaca 上提升 **12.1%**；在 13B SpecBench 上超越 DSD 达 **53.4%**。

### ⏱️ 延迟降低（Latency）
#### 表 3：Mean End-to-End Latency（单位：ms）

| Method | Alpaca (7B) | ShareGPT (7B) | SpecBench (7B) | Alpaca (13B) | ShareGPT (13B) | SpecBench (13B) |
|--------|-------------|---------------|----------------|--------------|----------------|------------------|
| **Nightjar** | **8868.17** | **6534.76** | **7618.85** | **23525.49** | **8133.92** | **14691.81** |
| BanditSpec | 12340.83 | 7799.63 | 9288.23 | 29077.32 | 9667.04 | 19294.42 |
| DSD | 9183.21 | 6499.12 | 8301.02 | 16212.44 | 10057.72 | 22361.71 |
| TETRIS | 11734.19 | 7409.62 | 8789.48 | 22133.43 | 8046.25 | 14937.37 |
| SD | 11110.68 | 7047.49 | 8567.04 | 24671.40 | 8152.97 | 14212.97 |
| w/o SD | 8876.14 | 6438.47 | 7654.05 | 32222.49 | 9350.87 | 23831.74 |

✅ **关键发现**：
- Nightjar 实现 **最高吞吐 + 最低延迟** 双优。
- 在 7B Alpaca 上，相比 SD 降低延迟 **20.2%**，相比 TETRIS 降低 **24.4%**。
- 在 13B SpecBench 上，相比 DSD 降低延迟 **34.3%**，显著缓解 compute-bound 场景下的性能瓶颈。

### 🔍 消融实验（Ablation Study）
- **Fig. 11 对比多种 context-aware MAB 方法**：
  - LinUCB 和 linear Thompson Sampling 假设 reward 与 context（batch size）呈线性关系，在 LLM 推理中不成立。
  - Nightjar 使用的 ADA-BINGREEDY 非参数化策略表现最优，说明复杂非线性关系需灵活建模。
- **Switching cost 建模有效性**：
  - 若忽略 $ C_{\text{prefill}} $，系统频繁切换 γ 导致 latency 上升。
  - 实测 $ C_{\text{prefill}} $ 随输入长度和 batch size 增加而显著上升（Table 1），验证了建模必要性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Speculative Decoding 的收益高度依赖负载状态**：
   - 低负载（memory-bound）时 SD 显著加速；
   - 高负载（compute-bound）时 SD 反而拖慢系统。
2. **固定 speculative length 是次优的**：
   - 最优 γ 是 batch size 和系统状态的函数，必须动态调整。
3. **Nightjar 成功实现了“智能开关”机制**：
   - 在低负载时启用并优化 γ；
   - 在高负载时果断关闭 SD，避免性能退化。
4. **Switching cost 是影响决策质量的关键因素**：
   - 忽视 $ C_{\text{prefill}} $ 会导致短视行为和性能波动。

### ⚠️ 方法的局限性
- **需要离线构建 lookup table for $ C_{\text{prefill}} $**：
  - 虽然可通过 profiling 获取，但在新型硬件或模型上需重新校准。
- **未考虑 draft model 的多样性选择**：
  - 当前仅优化 speculative length，未联合优化 draft model 架构或大小。
- **对极端突变负载的响应速度有待验证**：
  - 虽然具备探索能力，但在突发流量下的收敛速度未深入分析。

### 🔮 未来工作方向
- **扩展至多维动作空间**：联合优化 γ、draft model、KV cache 策略等。
- **跨设备/集群部署支持**：将 Nightjar 推广到分布式 LLM serving 场景。
- **在线自动 calibration of $ C_{\text{prefill}} $**：减少对离线 profiling 的依赖。
- **结合 energy efficiency 或 cost-per-token 优化**：面向绿色 AI 和商业化部署。

---

## 总结
> **Nightjar 提出了一种真正面向现实世界的 adaptive speculative decoding 框架**。它通过 contextual MAB 实现了对动态请求负载的实时感知与响应，不仅提升了 throughput（最高 +14.8%）和降低了 latency（最低 -20.2%），更重要的是解决了“何时该停用 SD”的根本难题。其对 switching cost 的精细建模和对 continuous batching 的良好适配，使其成为当前最先进的 LLM serving 自适应推理方案之一。

</details>

---

### 15. [SoliReward: Mitigating Susceptibility to Reward Hacking and Annotation Noise in Video Generation Reward Models](https://arxiv.org/abs/2512.22170)

**Authors**: Jiesong Lian, Ruizhe Zhong, Zixiang Zhou, Xiaoyue Mi, Yixue Hao, Yuan Zhou, Qinglin Lu, Long Hu, Junchi Yan  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.22170v1  

#### Abstract
Post-training alignment of video generation models with human preferences is a critical goal. Developing effective Reward Models (RMs) for this process faces significant methodological hurdles. Current data collection paradigms, reliant on in-prompt pairwise annotations, suffer from labeling noise. ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SoliReward: Mitigating Susceptibility to Reward Hacking and Annotation Noise in Video Generation Reward Models

## 1. 论文的主要贡献和创新点

### 解决的问题
当前视频生成模型中的 **Reward Model (RM)** 在训练过程中面临三大挑战：
- **标注噪声**：传统的 in-prompt pairwise 注释方式在质量相近的样本间容易引入主观判断偏差，导致标签噪声。
- **奖励黑客攻击（Reward Hacking）**：RM 学习到的是代理目标而非真实人类偏好，在 post-training 阶段易被策略模型利用“捷径特征”欺骗。
- **架构表达能力不足**：现有 VLM-based RM 架构（如 last token、special token）无法充分聚合多层语义信息，导致奖励坍缩（score clustering）。

### 提出的新方法与创新
为解决上述问题，作者提出 **SoliReward**，一个系统性的视频 RM 训练框架，包含以下四大创新：

#### （1）单项目二元标注 + 跨提示配对策略（Single-Item Binary Annotation + Cross-Prompt Pairing）
- 改变传统 pairwise 或 Likert 评分方式，采用 **Pass/Fail 二元标注**，显著降低标注歧义。
- 引入 **跨提示配对策略**：将不同 prompt 下的 Pass 和 Fail 视频进行配对构建 preference 数据，避免 in-prompt 模糊比较带来的噪声，同时提升数据利用率（可利用仅生成一个视频的 prompt）。

#### （2）带胜-平局的 Bradley-Terry 损失（Bradley-Terry with Win-Tie, BT-WT）
- 在标准 BT 损失基础上引入 **win-tie pairs**（即两个 Pass 样本之间的配对），显式约束正样本内部的得分方差。
- 作用机制：通过惩罚高质样本间的得分差异，迫使 RM 将所有高质量样本映射到紧凑流形上，缓解 reward spikes，从而有效抑制 reward hacking。

#### （3）分层渐进查询注意力架构（Hierarchical Progressive Query Attention, HPQA）
- 设计新型 RM 适配器架构，从 VLM 多个 transformer 层中逐步聚合特征。
- 使用 learnable query 向量逐层 refine 表示，并结合残差连接融合最终层输出，实现低级视觉保真度与高级语义理解的融合，增强奖励信号鲁棒性。

#### （4）建立新的评测基准
- 构建针对 **物理合理性（Physical Plausibility）** 和 **主体变形（Subject Deformity）** 的高质量测试集，推动更全面的视频生成质量评估。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **数据效率与一致性** | 单项二元标注 IAA 显著高于 pairwise（Krippendorff’s α: 0.49 vs 0.35） |
| **抗 reward hacking 能力** | BT-WT 使 post-training 性能提升明显，且 group advantage 更稳定 |
| **架构表达能力** | HPQA 避免 score clustering，支持细粒度排序 |
| **泛化能力** | 在 OOD 数据上表现优异，验证其迁移潜力 |

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：自建 250k 视频数据集，覆盖 20k 不同 prompt，涵盖多种主题、运动、风格与相机控制。
- **测试数据**：
  - **ID 测试集**：保留未见的训练集划分。
  - **OOD 测试集**：50k 来自 SOTA 模型（Wan2.1/2.2、Veo 3、Seedance 1.0）生成的视频，用于评估泛化能力。
- **标注维度**：
  - 物理合理性（Physical Plausibility）
  - 主体变形（Subject Deformity）
  - 语义对齐（Semantic Alignment, TA）

### 实验设置
- **RM Backbone**：InternVL3 系列（1B/8B/14B）
- **Post-training Backbone**：HunyuanVideo（14B）
- **优化算法**：DanceGRPO（基于 flow-based GRPO）
- **训练细节**：
  - 使用 DeepSpeed 进行分布式训练
  - AdamW 优化器，学习率 1e-6，warmup ratio 0.05
  - 具体超参见附录 Table 11 和 Table 12

### 评估指标
| 类型 | 指标 |
|------|------|
| **RM 自身性能** | RM ACC（ID/OOD）、Score Margin |
| **Post-training 效果** | VBench2 Human Fidelity、Motion Quality (MQ) |
| **人工一致性** | Krippendorff’s α、Fleiss’s K、Raw Agreement |
| **消融研究** | Ablation on architecture、loss function、pairing strategy |

### 基线方法对比
| 方法 | 类型 |
|------|------|
| VideoScore / VideoScore-v1.1 | 基于 Likert 评分的自动指标 |
| LiFT | 基于 point-wise 回归的方法 |
| VisionReward | 二分类 + BT 框架 |
| VideoPhy | 专注于物理常识的 RM |
| UnifiedReward | 多模态统一 RM |
| VideoAlign | 当前主流 pairwise RM，使用 special token head |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（RM Accuracy）

#### 表：RM 准确率对比（摘自 Table 2）

| Task | Approach | ID (%) | OOD (%) |
|------|----------|--------|---------|
| **Phy & Deform** | VideoAlign [17] | 54.40 | 71.60 |
| | **Ours (SoliReward)** | **78.48** | **80.08** |
| **TA** | VideoPhy [2] | 54.85 | 60.52 |
| | **Ours (SoliReward)** | **79.02** | 60.25 |

> ✅ SoliReward 在物理合理性和主体变形任务上大幅领先所有基线，尤其 OOD 泛化能力强。

---

### Post-training 性能对比（Table 4）

| Backbone | RM | MQ | VBench2 |
|----------|----|-----|---------|
| HunyuanVideo | — | -0.0980 | 0.8426 |
| HunyuanVideo | VideoAlign MQ | 0.1607 | 0.8695 |
| HunyuanVideo | **Ours** | **0.3302** | **0.8999** |

> ✅ 使用 SoliReward 指导 post-training 后，VBench2 Human Fidelity 提升达 **+5.73%**，远超 VideoAlign MQ。

---

### 消融实验结果

#### （1）损失函数对比（BT vs BT-WT）—— Table 3
| Method | RM ACC | VBench2 | MQ |
|--------|--------|---------|-----|
| BT | 77.63 | 0.8693 | 0.1719 |
| **BT-WT** | **78.27** | **0.8999** | **0.3302** |

> 尽管 RM ACC 提升有限，但 **BT-WT 显著改善 post-training 效果**，说明其有效缓解了 reward hacking。

#### （2）架构对比（HPQA vs Others）—— Table 5
| Approach | Phy & Deform (OOD) | TA (OOD) |
|----------|---------------------|----------|
| Linear Head [38] | 78.66 | 31.92* |
| 'Yes' token logits [32] | 78.46 | 31.37* |
| Special token + Ln [17] | 73.61 | 58.38 |
| **HPQA (Ours)** | **80.08** | **60.25** |

> HPQA 在所有任务中均最优，尤其在语义对齐任务上避免了 score clustering（*标记表示存在离散化问题）。

#### （3）配对策略对比（Cross-Prompt vs In-Prompt）—— Appendix Table 7
| Strategy | Phy & Deform (ID) | Phy & Deform (OOD) |
|----------|-------------------|--------------------|
| In-Prompt | 76.77 | 79.22 |
| **Cross-Prompt** | **76.74** | **79.54** |

> 跨提示配对性能相当甚至略优，且能更好利用单样本 prompt，提升数据利用率。

#### （4）BCE 正则项影响 —— Table 6
加入 Binary Cross Entropy 正则项虽保持 ACC，但导致 **reward margin 下降 19.62%**，进而损害 post-training 效果，表明分类准确率 ≠ 排序能力。

---

## 4. 关键结论和发现

### 主要发现
1. **单项二元标注显著提升标注一致性**：相比 pairwise 比较，Pass/Fail 判断具有更高 IAA（α > 0.49），更适合复杂视频质量评估。
2. **跨提示配对可行且高效**：无需限制同一 prompt 内配对，扩大数据来源并促进通用质量表征学习。
3. **BT-WT 是缓解 reward hacking 的有效手段**：通过 win-tie pairs 正则化正样本分布，减少 reward spikes，提供更稳健的优化信号。
4. **HPQA 架构优于传统 pooling 方式**：多层特征渐进融合能有效防止 score clustering，提升排序精细度。
5. **OOD 准确率是更好的下游性能预测器**：ID 准确率可能误导，而 OOD 表现更能反映实际 post-training 效果。

### 方法的局限性
- 当前 RM 仅针对单一维度打分，多维度融合需设计多个 learnable queries。
- 模型扩展分析显示：从 8B 到 14B 参数时出现收益递减，推测受限于数据规模或优化瓶颈。
- 所有实验集中在 text-to-video (T2V)，尚未验证于 image-to-video (I2V) 等其他条件生成任务。

### 未来工作方向
- 扩展至 I2V、video editing 等更多 conditional generation 场景。
- 设计多维度联合 RM，支持端到端多属性对齐。
- 探索更大规模数据与更高效训练策略以突破 scaling bottleneck。
- 开源代码与 benchmark，推动社区共建高质量视频 RM 标准。

---

> 🔗 **代码与资源**：作者承诺将公开代码与 benchmark，详见论文末尾声明。

</details>

---

### 16. [Calibrating LLM Judges: Linear Probes for Fast and Reliable Uncertainty Estimation](https://arxiv.org/abs/2512.22245)

**Authors**: Bhaktipriya Radharapu, Eshika Saxena, Kenneth Li, Chenxi Whitehouse, Adina Williams, Nicola Cancedda  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.22245v1  

#### Abstract
As LLM-based judges become integral to industry applications, obtaining well-calibrated uncertainty estimates efficiently has become critical for production deployment. However, existing techniques, such as verbalized confidence and multi-generation methods, are often either poorly calibrated or com...

---

### 17. [Federated Multi-Task Clustering](https://arxiv.org/abs/2512.22897)

**Authors**: S. Dai, G. Sun, F. Li, X. Tang, Q. Wang, Y. Cong  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.22897v1  

#### Abstract
Spectral clustering has emerged as one of the most effective clustering algorithms due to its superior performance. However, most existing models are designed for centralized settings, rendering them inapplicable in modern decentralized environments. Moreover, current federated learning approaches o...

---

### 18. [FLEX-MoE: Federated Mixture-of-Experts with Load-balanced Expert Assignment](https://arxiv.org/abs/2512.23070)

**Authors**: Boyang Zhang, Xiaobing Chen, Songyang Zhang, Shuai Zhang, Xiangwei Zhou, Mingxuan Sun  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.23070v1  

#### Abstract
Mixture-of-Experts (MoE) models enable scalable neural networks through conditional computation. However, their deployment with federated learning (FL) faces two critical challenges: 1) resource-constrained edge devices cannot store full expert sets, and 2) non-IID data distributions cause severe ex...

---

### 19. [Energy and Memory-Efficient Federated Learning With Ordered Layer Freezing](https://arxiv.org/abs/2512.23200)

**Authors**: Ziru Niu, Hai Dong, A. K. Qin, Tao Gu, Pengcheng Zhang  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.23200v1  

#### Abstract
Federated Learning (FL) has emerged as a privacy-preserving paradigm for training machine learning models across distributed edge devices in the Internet of Things (IoT). By keeping data local and coordinating model training through a central server, FL effectively addresses privacy concerns and red...

---

### 20. [Post-Training Quantization of OpenPangu Models for Efficient Deployment on Atlas A2](https://arxiv.org/abs/2512.23367)

**Authors**: Yilun Luo, HuaQing Zheng, Haoqian Meng, Wenyuan Liu, Peng Zhang  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.23367v1  

#### Abstract
Huawei's openPangu-Embedded-1B and openPangu-Embedded-7B, variants of the openPangu large language model, integrate three distinct Chain-of-Thought (CoT) reasoning paradigms, namely slow_think, auto_think, and no_think. While these CoT modes enhance reasoning capabilities, their generation of extend...

---

### 21. [SlimEdge: Lightweight Distributed DNN Deployment on Constrained Hardware](https://arxiv.org/abs/2512.22136)

**Authors**: Mahadev Sunil Kumar, Arnab Raha, Debayan Das, Gopakumar G, Amitava Mukherjee  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22136v1  

#### Abstract
Deep distributed networks (DNNs) have become central to modern computer vision, yet their deployment on resource-constrained edge devices remains hindered by substantial parameter counts and computational demands. Here, we present an approach to the efficient deployment of distributed DNNs that join...

---

### 22. [HybridFlow: Adaptive Task Scheduling for Fast and Token-Efficient LLM Inference in Edge-Cloud Collaboration](https://arxiv.org/abs/2512.22137)

**Authors**: Jiangwen Dong, Jiayu Li, Wanyu Lin  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22137v1  

#### Abstract
Large language models (LLMs) exhibit impressive reasoning and problem-solving abilities, yet their substantial inference latency and token consumption pose major challenges for real-time deployment on resource-limited edge devices. Recent efforts toward edge-cloud collaboration have attempted to mit...

---

### 23. [LuxIA: A Lightweight Unitary matriX-based Framework Built on an Iterative Algorithm for Photonic Neural Network Training](https://arxiv.org/abs/2512.22264)

**Authors**: Tzamn Melendez Carmona, Federico Marchesin, Marco P. Abrate, Peter Bienstman, Stefano Di Carlo, Alessandro Savino Senior  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22264v1  

#### Abstract
PNNs present promising opportunities for accelerating machine learning by leveraging the unique benefits of photonic circuits. However, current state of the art PNN simulation tools face significant scalability challenges when training large-scale PNNs, due to the computational demands of transfer m...

---

### 24. [Hybrid Quantum-Classical Mixture of Experts: Unlocking Topological Advantage via Interference-Based Routing](https://arxiv.org/abs/2512.22296)

**Authors**: Reda Heddad, Lamiae Bouanane  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22296v1  

#### Abstract
The Mixture-of-Experts (MoE) architecture has emerged as a powerful paradigm for scaling deep learning models, yet it is fundamentally limited by challenges such as expert imbalance and the computational complexity of classical routing mechanisms. This paper investigates the potential of Quantum Mac...

---

### 25. [Beyond Centralization: Provable Communication Efficient Decentralized Multi-Task Learning](https://arxiv.org/abs/2512.22675)

**Authors**: Donghwa Kang, Shana Moothedath  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22675v1  

#### Abstract
Representation learning is a widely adopted framework for learning in data-scarce environments, aiming to extract common features from related tasks. While centralized approaches have been extensively studied, decentralized methods remain largely underexplored. We study decentralized multi-task repr...

---

### 26. [Breaking the Memory Wall: Exact Analytical Differentiation via Tiled Operator-Space Evolution](https://arxiv.org/abs/2512.23068)

**Authors**: Shuhuan Wang, Yuzhen Xie, Jiayi Li, Yinliang Diao  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.23068v1  

#### Abstract
Selective State Space Models (SSMs) achieve linear-time inference, yet their gradient-based sensitivity analysis remains bottlenecked by O(L) memory scaling during backpropagation. This memory constraint precludes genomic-scale modeling (L > 10^5) on consumer-grade hardware. We introduce Phase Gradi...

---

### 27. [Mirage Persistent Kernel: A Compiler and Runtime for Mega-Kernelizing Tensor Programs](https://arxiv.org/abs/2512.22219)

**Authors**: Xinhao Cheng, Zhihao Zhang, Yu Zhou, Jianan Ji, Jinchen Jiang, Zepeng Zhao, Ziruo Xiao, Zihao Ye, Yingyi Huang, Ruihang Lai, Hongyi Jin, Bohan Hou, Mengdi Wu, Yixin Dong, Anthony Yip, Zihao Ye, Songting Wang, Wenqin Yang, Xupeng Miao, Tianqi Chen, Zhihao Jia  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22219v1  

#### Abstract
We introduce Mirage Persistent Kernel (MPK), the first compiler and runtime system that automatically transforms multi-GPU model inference into a single high-performance megakernel. MPK introduces an SM-level graph representation that captures data dependencies at the granularity of individual strea...

---

### 28. [Scalable Cloud-Native Architectures for Intelligent PMU Data Processing](https://arxiv.org/abs/2512.22231)

**Authors**: Nachiappan Chockalingam, Akshay Deshpande, Lokesh Butra, Ram Sekhar Bodala, Nitin Saksena, Adithya Parthasarathy, Balakrishna Pothineni, Akash Kumar Agarwal  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22231v1  

#### Abstract
Phasor Measurement Units (PMUs) generate high-frequency, time-synchronized data essential for real-time power grid monitoring, yet the growing scale of PMU deployments creates significant challenges in latency, scalability, and reliability. Conventional centralized processing architectures are incre...

---

### 29. [Graph Attention-based Adaptive Transfer Learning for Link Prediction](https://arxiv.org/abs/2512.22252)

**Authors**: Huashen Lu, Wensheng Gan, Guoting Chen, Zhichao Huang, Philip S. Yu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22252v1  

#### Abstract
Graph neural networks (GNNs) have brought revolutionary advancements to the field of link prediction (LP), providing powerful tools for mining potential relationships in graphs. However, existing methods face challenges when dealing with large-scale sparse graphs and the need for a high degree of al...

---

### 30. [Anka: A Domain-Specific Language for Reliable LLM Code Generation](https://arxiv.org/abs/2512.23214)

**Authors**: Saif Khalfan Saif Al Mazrouei  
**Category**: cs.CL  
**Published**: 2025-12-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.23214v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities in code generation, yet they exhibit systematic errors on complex, multi-step programming tasks. We hypothesize that these errors stem from the flexibility of general-purpose languages, which permits multiple valid approaches and...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
