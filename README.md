# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-05 11:52:30 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [RLHFSpec: Breaking the Efficiency Bottleneck in RLHF Training via Adaptive Drafting](https://arxiv.org/abs/2512.04752)

**Authors**: Siqi Wang, Hailong Yang, Junjie Zhu, Xuezhu Wang, Yufan Xu, Depei Qian  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2512.04752v1  

#### Abstract
Reinforcement Learning from Human Feedback (RLHF) is an important fine-tuning technique for large language models (LLMs) and comprises three stages: generation, inference, and training. The generation stage generates samples that are then used to infer learnable experiences for training. We observe ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*RLHFSpec: Breaking the Efficiency Bottleneck in RLHF Training via Adaptive Drafting*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
Reinforcement Learning from Human Feedback (RLHF) 是大语言模型（LLM）微调的关键技术，其训练流程分为三个阶段：**generation（生成）**、**inference（推理）** 和 **training（训练）**。其中，**generation 阶段是整个流程的性能瓶颈**，原因如下：

- **自回归解码导致低并行度**：每个 token 必须顺序生成，难以充分利用 GPU 资源。
- **响应长度长尾分布**：部分样本输出极长（如 Chain-of-Thought 推理），导致负载不均衡，GPU 利用率下降。
- **静态策略无法适应动态负载**：传统 speculative decoding 在线服务中表现良好，但在 RLHF 这类固定样本数的离线场景下，其固定的 drafting 策略（如固定 `draft token num`）在不同负载阶段表现次优。

因此，作者指出：**generation 阶段的效率低下严重拖累整体 RLHF 训练速度**。

---

### 🚀 提出的新方法与创新思路

为解决上述问题，本文提出 **RLHFSpec** ——首个将 **speculative decoding** 成功集成到 RLHF 系统中的框架，并引入两项核心技术：

#### （1）**Workload-aware Drafting Strategy Selection（基于负载感知的起草策略选择）**

- **动机**：generation 阶段的 workload 动态变化（初期样本多，后期只剩少数长尾样本），最优的 `draft token num` 应随之调整。
- **方法**：
  - 设计轻量级预测器，实时估计不同 `n` 下的 **accepted token 数量**（`al(n)`）和 **验证开销**（`tsa(n)`）。
  - 定义优化目标：最大化 speculative decoding 的加速比 $ \text{Speedup}(n) = \frac{al(n) \times t_{\text{ar}}}{t_{\text{sa}}(n)} $。
  - 采用 **layer-level 搜索 + 早停机制**（基于 sugar water 不等式）快速找到近似最优 `n`。
- **优势**：避免了手动调参或固定策略带来的性能损失，在高负载时保守，在低负载时激进，实现动态平衡。

#### （2）**Efficient Sample Reallocation（高效的样本重分配）**

- **动机**：由于响应长度差异，某些 generation instance 处理大量长样本而持续高负载，其他 instance 却早早空闲，造成资源浪费。
- **方法**：
  - 引入 **greedy reallocation policy**：识别“阈值”（throughput 转折点），将超载实例的样本迁移到未达阈值的实例。
  - 提出 **two-stage sample migration 机制**：
    1. **Stage 1**：利用 LLM 验证的 Markov 特性，边计算边迁移已验证的 KVCache。
    2. **Stage 2**：利用 SSM 与 LLM KVCache 的独立性，先恢复 SSM 状态以继续 draft，再异步传输 LLM KVCache。
  - 结合 **分层 KVCache 表示与批量拷贝**，减少通信开销。
- **优势**：显著提升系统整体吞吐量，且迁移开销接近零。

---

### 🔍 相比现有方法的优势

| 对比维度 | 传统方法（Verl/OpenRLHF） | Speculative Decoding（直接应用） | **RLHFSpec（本文）** |
|--------|--------------------------|-------------------------------|--------------------|
| 解码方式 | Autoregressive decoding | 固定策略 speculative decoding | **自适应 speculative decoding** |
| 负载感知 | ❌ | ❌（静态策略） | ✅（动态选择 `n`） |
| 样本调度 | 静态分配 | 静态分配 | ✅（动态重分配 + 高效迁移） |
| GPU 利用率 | 低（尤其后期） | 中等 | **高且稳定** |
| 整体加速效果 | 基线 | 有限提升 | **显著优于所有基线** |

---

## 2. 核心实验方法和设置

### 📚 数据集

- **LMSYS-Chat-1M**：包含 100 万真实世界对话，涵盖 25 种主流 LLM，具有典型的长尾输出特性。
- **GSM8K**：小学数学题数据集，常用于测试推理能力，也表现出较长响应趋势。

### ⚙️ 实验设置

- **硬件环境**：
  - 8× NVIDIA L40S GPUs（PCIe 连接）
  - Intel Xeon Gold 6448Y CPU
  - CUDA 12.6, cuDNN v9.1.0
- **模型配置**：
  - 主模型：**Llama-3.1-8B-Instruct**
  - Draft Model：**Eagle**（基于 Llama 蒸馏的小模型）
- **实现平台**：基于 **Verl** 框架扩展实现 RLHFSpec。

### 📊 评估指标

- **主要指标**：
  - **Sample Throughput (samples/s)**：单位时间内完成的样本数。
  - **Token Throughput (tokens/s)**：单位时间内处理的 token 数。
- **对比方式**：
  - 报告相对于基线的 **speedup 倍数**。
  - 分析 generation 阶段与 end-to-end RLHF 流程的整体性能。

### 🆚 基线方法

| 基线 | 描述 |
|------|------|
| **OpenRLHF** | 支持多设备部署与定制化并行策略的开源 RLHF 框架 |
| **Verl** | 层次化混合编程模型，支持灵活数据流控制 |
| **Speculative** | 在 Verl 基础上加入传统 speculative decoding（固定 `n`） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）Generation 阶段吞吐量（图 11）

| 方法 | LMSYS 加速比 | GSM8K 加速比 |
|------|--------------|-------------|
| vs. OpenRLHF | **2.52×** | **2.65×** |
| vs. Verl | **2.16×** | **2.32×** |
| vs. Speculative | **2.02×** | **1.97×** |

> ✅ **RLHFSpec 在 generation 阶段实现了最高达 2.65× 的吞吐提升**。

#### （2）端到端 RLHF 执行性能（图 12）

| 方法 | LMSYS 加速比 | GSM8K 加速比 |
|------|--------------|-------------|
| vs. OpenRLHF | **3.01×** | **2.97×** |
| vs. Verl | **1.50×** | **1.43×** |
| vs. Speculative | **1.37×** | **1.35×** |

> ✅ **缓解 generation 瓶颈后，整体 RLHF 训练时间大幅缩短，最高提速超过 3×**。

---

### 🔬 消融实验结果（图 13）

对 RLHFSpec 各组件进行逐步叠加测试（归一化于 autoregressive baseline）：

| 组件组合 | 归一化性能 |
|--------|------------|
| Baseline (Default) | 1.00× |
| + Speculative Decoding (Spec) | **1.18×** |
| + Workload-aware Selection | **1.95×** |
| + Sample Reallocation | **2.32×** |

> 💡 **结论**：
> - 单纯 speculative decoding 提升有限（仅 1.18×）；
> - 自适应 drafting 策略带来最大增益（从 1.18→1.95）；
> - 样本重分配进一步释放潜力（1.95→2.32）。

---

### 🎯 Workload-aware Strategy 有效性（表 1）

在多种 workload（样本数量）下，RLHFSpec 所选策略的性能占“理论最优”的百分比：

| Workload | LMSYS (%) | GSM8K (%) |
|---------|-----------|----------|
| 8–64 样本 | 96.57 ~ 99.70 | 95.53 ~ 99.90 |

> ✅ **平均达到最优性能的 97% 以上，最差情况仍达 95.53%**，证明策略选择高度准确。

---

### 🔄 Sample Reallocation 实际效果（图 14）

- 在某一时刻触发迁移：将 5 个样本从 instance 1 迁移到 instance 2。
- **迁移前总吞吐**：2,127 tokens/s
- **迁移后总吞吐**：**2,531 tokens/s**（↑18.9%）
- 且迁移过程几乎无中断（near-zero overhead）

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Generation 阶段确实是 RLHF 的主要瓶颈**，占比超 68.4% 的总执行时间。
2. **直接套用 online speculative decoding 到 RLHF 效果有限**，因其忽略 workload 动态性和优化目标差异（throughput > latency）。
3. **workload-aware drafting 策略能显著提升 speculative decoding 效率**，通过动态权衡 verification cost 与 accepted tokens。
4. **样本重分配可有效缓解长尾效应引起的负载失衡**，结合高效迁移机制，几乎无额外开销。
5. **RLHFSpec 实现了 generation 阶段最高 2.65×、end-to-end 最高 3.01× 的性能加速**，显著优于当前 SOTA。

---

### ⚠️ 方法的局限性

1. **依赖高质量的 draft model**：若 SSM 与 LLM 差异过大，acceptance rate 下降，speculative decoding 效果减弱。
2. **KVCache 迁移仍需内存预留机制**：可能因目标设备内存不足导致迁移失败。
3. **目前仅适用于单节点多 GPU 场景**：跨节点迁移尚未考虑网络带宽限制。
4. **offline profiling 开销虽小但存在**：需预先收集数据训练预测模型和确定 threshold。

---

### 🔮 未来工作方向

1. **支持跨节点 sample migration**，扩展至分布式训练场景。
2. **动态训练 draft model**，使其更适配当前任务分布。
3. **结合 compression 或 quantization 技术降低 KVCache 传输成本**。
4. **探索更多 speculative structure（如 DAG）以进一步提高 acceptance rate**。
5. **将 RLHFSpec 思路推广至其他基于 autoregressive 生成的强化学习任务**。

---

## 总结

> **RLHFSpec 通过“自适应起草 + 智能重分配”双轮驱动，首次成功将 speculative decoding 高效应用于 RLHF 训练，打破了 generation 阶段的效率瓶颈，在真实数据集上实现了高达 3× 的端到端加速，为大规模 LLM 微调提供了新的高性能解决方案。**

</details>

---

### 2. [Arbitrage: Efficient Reasoning via Advantage-Aware Speculation](https://arxiv.org/abs/2512.05033)

**Authors**: Monishwaran Maheswaran, Rishabh Tiwari, Yuezhou Hu, Kerem Dilmen, Coleman Hooper, Haocheng Xi, Nicholas Lee, Mehrdad Farajtabar, Michael W. Mahoney, Kurt Keutzer, Amir Gholami  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.05033v1  

#### Abstract
Modern Large Language Models achieve impressive reasoning capabilities with long Chain of Thoughts, but they incur substantial computational cost during inference, and this motivates techniques to improve the performance-cost ratio. Among these techniques, Speculative Decoding accelerates inference ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ARBITRAGE: Efficient Reasoning via Advantage-Aware Speculation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
现代大型语言模型（LLMs）在数学推理等复杂任务中表现出色，但其推理过程依赖长链式思维（Chain-of-Thought, CoT），导致自回归解码步骤极多，推理延迟高、计算成本大。传统的 **Speculative Decoding (SD)** 方法通过小模型（draft model）生成候选 token 并由大模型（target model）并行验证来加速推理，但在**语义等价但 token 不匹配**的推理任务中，token-level 的 SD 接受率低，效率差。

近期提出的 **step-level SD**（如 RSD）虽能基于整个推理步骤进行评分和接受决策，但仍存在严重问题：  
- 使用**绝对质量阈值**（如 PRM 分数是否高于某值）决定是否拒绝 draft 步骤；
- 导致大量“无意义重生成”——即 target model 重新生成的步骤质量并未显著优于 draft，却仍消耗昂贵计算资源。

### **提出的新方法与创新思路**
本文提出 **ARBITRAGE**，一种基于**优势感知（advantage-aware）** 的 step-level speculative generation 框架，核心思想是：

> **不应仅看 draft 步骤是否“足够好”，而应判断 target 是否“明显更好”。**

#### **核心组件**
1. **ARBITRAGE ORACLE（理想路由策略）**  
   - 在每一步同时运行 draft 和 target 模型，比较两者的 PRM 分数，选择得分更高的步骤。
   - 定义 **step-level advantage**: $ \Delta = s_t - s_d $，表示 target 相对于 draft 的质量增益。
   - 若 $ \Delta > 0 $，则应调用 target；否则保留 draft。
   - 该策略理论上达到**局部最优**，构成性能上界。

2. **ARBITRAGE ROUTER（实用路由模型）**  
   - 一个轻量级可训练模型，输入为当前上下文和 draft 生成的步骤 $(x, z_d)$，输出一个分数 $ y \in [0,1] $，预测 target 是否会显著优于 draft。
   - 决策规则：若 $ y \leq t $（预测优势小），接受 draft；否则调用 target。
   - 路由器在离线阶段使用 ARBITRAGE ORACLE 的标签进行监督训练，避免在线调用 target。

### **相比现有方法的优势**
| 特性 | RSD（Baseline） | ARBITRAGE（Ours） |
|------|------------------|--------------------|
| 决策依据 | draft 的绝对 PRM 分数 | target 相对于 draft 的预期优势 |
| 是否考虑 target 表现 | 否（advantage-blind） | 是（advantage-aware） |
| 冗余计算 | 高（常因绝对阈值误判） | 显著降低 |
| 效率-准确性权衡 | 次优 | 接近理论最优（ORACLE） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **MATH500**：标准数学推理基准，包含 500 道高中至竞赛级别题目。
- **OlympiadBench**：更具挑战性的双语多模态科学奥赛题基准，难度更高。

### **实验设置**
- **模型配置**：
  - **LLaMA3 系列**：`1B/8B`, `8B/70B`, `4bit-8B/8B`（量化 draft）
  - **Qwen2.5-Math 系列**：`3bit-7B/7B`（量化 draft）
- **Process Reward Model (PRM)**：使用 `Skywork-o1-Open-PRM (1.5B)` 对每个推理步骤打分。
- **ARBITRAGE ROUTER**：基于 PRM 微调得到，用于预测 $ \Delta > 0 $ 的概率。
- **硬件平台**：NVIDIA A6000 GPU，使用 SGLang 作为推理后端，batch size = 1。

### **评估指标**
1. **Accuracy vs. Acceptance Rate 曲线**  
   - 控制不同接受率下的最终答案准确率，衡量 compute-quality trade-off。
2. **End-to-End Latency vs. Accuracy 曲线（Pareto Frontiers）**  
   - 实际端到端延迟（wall-clock time）与准确率的关系，反映真实加速效果。
3. **Spearman Rank Correlation ($\rho$)**  
   - 衡量 ROUTER 预测的“优势排序”与真实 ORACLE 排序的一致性，作为训练质量代理指标。

### **基线方法对比**
- **RSD (Reward-guided Speculative Decoding)**：当前主流 step-level SD 方法，基于固定 PRM 阈值决定是否拒绝 draft。
- **Draft-only / Target-only**：作为性能边界参考。
- **ARBITRAGE ORACLE**：作为理论性能上限。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### ✅ **Accuracy vs. Acceptance Rate**
- 在所有模型配置和数据集上，**ARBITRAGE ROUTER 的曲线始终高于 RSD**，表明在相同计算预算下获得更高准确率。
- 尤其在 draft 模型较弱时（如 LLaMA3 1B/8B），提升最显著：
  - RSD 几乎贴近 draft-only 性能；
  - ARBITRAGE 快速逼近 target-only 性能。

#### ✅ **Latency-Speedup 结果（图5）**
- **MATH500（量化 draft）**：最高实现 **1.62× 更低延迟**（相同准确率）。
- **OlympiadBench（小 draft）**：最高实现 **1.97× 加速**。
- 综合多个场景，**平均端到端延迟降低约 2×**。

#### ✅ **与 ORACLE 的接近程度**
- ARBITRAGE ROUTER 的性能曲线**紧密跟随 ARBITRAGE ORACLE**，说明其能有效模拟理想路由策略。
- 而 RSD 存在明显差距，尤其在中等延迟区域表现不佳（过度调用 target）。

### **消融实验结果（Ablation Studies）**

| 设计选择 | 影响 | 最佳方案 |
|--------|------|---------|
| **Label Granularity**（分类粒度） | 多分类（4/10类）导致性能下降 | **二分类（accept vs. escalate）最佳** |
| **Classification vs. Regression** | 回归模型表现更差 | **分类架构更鲁棒易部署** |
| **Step Annotation**（历史标记） | 添加 `Model 0/1` 标记提升 label-1 准确率 | **启用标注显著改善性能** |
| **Data Downsampling** | 不平衡数据导致过拟合 y=0 | **平衡采样提升 Spearman 相关性与 label-1 准确率** |

> 所有消融均以 **Spearman correlation** 和 **label accuracy** 为评价指标。

---

## **4. 关键结论和发现**

### **主要发现**
1. **现有 step-level SD 存在严重计算浪费**  
   - RSD 中高达 **40% 的 target 调用未带来质量提升**（见图2），源于其“绝对阈值”机制无法识别 target 是否真能改进。
   
2. **相对优势（advantage）是更合理的路由信号**  
   - ARBITRAGE 通过估计 $ \mathbb{E}[\Delta] $ 实现“智能调用”，只在 target 真正可能胜出时才激活，大幅减少冗余计算。

3. **轻量级 ROUTER 可有效逼近 ORACLE**  
   - 即使不运行 target 模型，ROUTER 也能学习到何时 target 更强，实现接近理论最优的效率-精度权衡。

4. **方法具有广泛适用性**  
   - 在不同模型家族（LLaMA/Qwen）、不同 draft-target 架构（同族小模型 / 自身量化版）下均稳定提升性能。

### **方法的局限性**
- **依赖高质量 PRM**：若 PRM 本身对推理步骤评分不准，则 ORACLE 和 ROUTER 训练都会受影响。
- **ROUTER 引入额外开销**：每步需一次前向传播，虽轻量但仍增加少量延迟。
- **假设 step 独立性**：当前框架假设各步优势独立，未建模长期依赖（如早期错误影响后续优势判断）。

### **未来工作方向**
- 探索更高效的 ROUTER 架构（如共享 backbone）进一步压缩 overhead。
- 将 advantage-aware 思想扩展至其他生成任务（如代码生成、规划）。
- 引入动态 threshold $ t $，根据问题难度自适应调整 compute allocation。
- 结合强化学习优化 end-to-end routing policy，超越 greedy per-step 决策。

---

> **总结**：ARBITRAGE 通过引入“优势感知”的路由机制，从根本上解决了传统 speculative decoding 在推理任务中的**无效重生成**问题，实现了**高达 ~2× 的推理加速**，同时保持甚至提升输出质量，为高效 LLM 推理提供了新的范式。

</details>

---

### 3. [SignRoundV2: Closing the Performance Gap in Extremely Low-Bit Post-Training Quantization for LLMs](https://arxiv.org/abs/2512.04746)

**Authors**: Wenhua Cheng, Weiwei Zhang, Heng Guo, Haihao Shen  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.04746v1  

#### Abstract
Extreme low-bit quantization is critical for efficiently deploying Large Language Models (LLMs), yet it often leads to severe performance degradation at 2-bits and even 4-bits (e.g., MXFP4). We present SignRoundV2, a post-training quantization framework that is highly effective even without mixed-pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SignRoundV2: Closing the Performance Gap in Extremely Low-Bit Post-Training Quantization for LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
极端低比特（如 2-bit 或 4-bit）的 **Post-Training Quantization (PTQ)** 在压缩大语言模型（LLMs）时通常会导致严重的性能下降。尽管已有方法尝试通过混合精度（mixed-precision）或量化感知训练（QAT）缓解此问题，但这些方法往往存在以下缺陷：
- **计算成本高**（尤其是 QAT）
- **缺乏高效的敏感度度量机制**
- **量化参数初始化不稳定**
- **难以在极低比特下保持接近全精度模型的性能**

### **提出的新方法与创新思路**
作者提出了 **SignRoundV2**，一种高效、无需再训练的 PTQ 框架，其核心创新包括：

#### **(1) DeltaLoss 敏感度度量（Sensitivity Metric）**
- 结合 **梯度信息（gradient）** 和 **量化引起的参数偏差（parameter deviation）**，构建了一种更准确的层敏感度评估指标。
- 公式为：  
  $$
  \Delta L \approx |\mathbf{g}_{aq} \circ (\mathbf{A}_f - \mathbf{A}_q)|
  $$
  其中 $\mathbf{g}_{aq}$ 是激活输出对损失的梯度，$\mathbf{A}_f$ 和 $\mathbf{A}_q$ 分别是全精度与量化后的激活值。
- 该指标能有效反映量化对任务损失的实际影响，优于仅依赖 Hessian 或启发式规则的方法。

#### **(2) 动态规划驱动的自适应比特分配（Adaptive Bit Allocation）**
- 利用 DeltaLoss 得到各层敏感度后，结合 **动态规划（dynamic programming）** 实现最优比特配置，在给定平均比特预算下最大化模型恢复能力。
- 支持灵活的混合精度策略，避免统一比特带来的性能瓶颈。

#### **(3) 轻量级预调优搜索（Lightweight Pre-tuning Search）**
- 受 llama.cpp 中“重要性矩阵”启发，设计了一个快速搜索机制来优化 **量化 scale 初始化**。
- 搜索目标函数为：
  $$
  \min_s \sum_i ((W_r - W_q) \circ \max(A))^2
  $$
  即最小化加权重建误差。
- 显著提升极低比特下的稳定性与最终精度。

### **相比现有方法的优势**
| 维度 | SignRoundV2 | 传统 PTQ / QAT |
|------|-------------|----------------|
| **是否需要训练** | 否（纯 PTQ） | 多数需微调或蒸馏 |
| **计算开销** | 极低（~2.5 小时） | 高（数十至数百小时） |
| **适用场景** | 快速部署、边缘设备 | 资源充足环境 |
| **性能表现** | 接近甚至超越部分 QAT 方法 | 在 2-bit 下严重退化 |
| **硬件兼容性** | 支持通用 GPU/CPU | 部分依赖专用加速器 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **校准数据集（Calibration Dataset）**：The Pile（用于量化前的小样本校准）
- **评估基准（Evaluation Benchmark）**：采用 `LM-EVAL-HARNESS` 框架，涵盖以下标准任务：
  - ARC-Challenge/Easy
  - BoolQ
  - HellaSwag
  - LAMBADA
  - MMLU
  - OpenBookQA
  - PIQA
  - TruthfulQA
  - WinoGrande

### **实验设置**
- **模型家族**：
  - LLaMA 系列：LLaMA2-7B/13B/70B、LLaMA3-8B/70B、LLaMA3.1-8B/70B-Instruct
  - Qwen 系列：Qwen2.5-7B/3-8B/3-32B-Instruct
- **量化配置**：
  - 权重-激活组合：W2A16、W4A16、MXFP4、MXFP8
  - 对称量化（symmetric quantization），group size 默认为 128 或 64
- **超参数设置**：
  - DeltaLoss 使用 16 个样本（seq_len=256）
  - 主调优阶段使用 128 个样本（seq_len=2048），每 block 优化 200 步（Ours* 版本为 500 步）

### **评估指标**
- **平均准确率（Average Accuracy）**：多个任务上的均值
- **恢复率（Recovery Rate %）**：相对于全精度模型的性能百分比
- **量化时间（GPU Hours）**：端到端量化耗时
- **显存占用（VRAM Cost）**

### **基线方法对比**
- **经典 PTQ 方法**：
  - GPTQ、AWQ、OmniQuant、SignRoundV1
- **先进 QAT 方法**：
  - EfficientQAT、QuIP#、AQLM
- **其他**：
  - RTN（Round-to-Nearest）、llama.cpp 启发式策略

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) W2A16 设置下的性能（Table 1）**
| 模型 | 方法 | 平均比特 | 平均准确率 |
|------|------|----------|------------|
| Llama2-70B | Full Precision | 16 | 72.71 |
| Llama2-70B | GPTQ | 2 | 34.38 |
| Llama2-70B | AWQ | 2 | 35.49 |
| Llama2-70B | SignRoundV1 | 2 | 67.70 |
| Llama2-70B | **Ours** | 2 | **68.39** |
| Llama2-70B | **Ours*** | 2 | **68.82** |
| Llama2-70B | **Ours*** | 2.5 | **70.60** |

> ✅ 在纯 2-bit 权重量化下，SignRoundV2 达到 **~68.8% 准确率**，接近全精度（72.7%），显著优于其他 PTQ 方法。

#### **(2) MXFP4/8 混合精度结果（Table 2）**
| 模型 | 方法 | 平均比特 | 准确率 | 恢复率 |
|------|------|----------|--------|--------|
| Llama3.1-8B-I | Full Precision | 16 | 64.16 | 100% |
| Llama3.1-8B-I | RTN | 4 | 58.31 | 90.88% |
| Llama3.1-8B-I | SRV1 | 4 | 60.72 | 94.64% |
| Llama3.1-8B-I | **Ours** | 4 | **61.34** | **95.59%** |
| Llama3.1-8B-I | **Ours** | 5 | **63.19** | **98.49%** |
| Llama3.1-8B-I | **Ours** | 6 | **64.12** | **99.93%** |

> ✅ 在 4–6 bit 范围内，SignRoundV2 实现 **>98% 恢复率**，在 6-bit 时几乎完全恢复全精度性能。

### **与基线方法的对比结果**
- 在 **W2A16** 场景下，SignRoundV2 比 GPTQ/AWQ 高出 **30+ pts**，比 SignRoundV1 提升约 **1–2 pts**
- 在 **MXFP4** 上，比 RTN 提升 **~3 pts**，比 SRV1 提升 **~0.6 pts**
- 在 **小模型（如 7B）** 上仍略逊于部分 QAT 方法（如 EfficientQAT），但在大模型上已可媲美

### **消融实验结果（Ablation Study）**

#### **(1) 初始化策略的影响（Table 5）**
| 模型 | 初始化 | ARC-c | HellaSwag | MMLU | WinoGrande | 平均 |
|------|--------|-------|-----------|------|------------|------|
| Qwen3-8B | 无初始化 | 34.90 | 44.87 | 47.18 | 54.09 | 63.85 |
| Qwen3-8B | 有初始化 | 43.69 | 45.12 | 54.61 | 56.12 | **66.22** |
| Llama3.1-8B-I | 有初始化 | +1.12 | +1.12 | +7.84 | +2.06 | +1.73 pts ↑ |

> 🔍 验证了 **预调优搜索初始化** 显著提升了稳定性和最终性能。

#### **(2) 混合比特分配策略对比（Table 3 & 4）**
- 相较于“头层/尾层优先分配 8-bit”的启发式策略，基于 DeltaLoss 的自适应分配在所有模型上均取得更高准确率。
- 在平均 3-bit 权重分配下（W2/W4 混合），SignRoundV2 比 head/tail 策略高出 **~10–30 pts**

---

## **4. 关键结论和发现**

### **主要发现**
1. **DeltaLoss 是一个更可靠的敏感度度量方式**，融合梯度与偏差信息，显著优于 Hessian 或输出扰动等代理指标。
2. **轻量级 scale 初始化搜索** 极大地提升了极低比特量化过程的稳定性与收敛质量。
3. **SignRoundV2 在 4–5 bit 实现生产级性能**（<1% 差异），即使在 **2-bit 权重** 下也能保持强健表现。
4. **无需 QAT 即可达到接近 QAT 的性能**，同时将量化时间从上百小时降至 **2.5 小时以内**。

### **方法的局限性**
1. **在极低比特（如 <2.5 bits）且无混合精度时**，小模型仍有明显性能差距。
2. **比特配置在调优前固定**，未考虑调优过程中误差补偿潜力。
3. **依赖梯度计算**，无法直接应用于不支持自动微分的推理框架（如 ONNX）。

### **未来工作方向**
- 探索 **动态比特调整机制**，在调优过程中根据误差反馈动态修改比特分配。
- 扩展至 **Mixture-of-Experts (MoE)** 模型的混合精度量化。
- 开发 **免梯度版本的 DeltaLoss 近似算法**，以适配更多部署环境。

---

> 📌 **开源地址**：https://github.com/intel/auto-round  
> 💡 **一句话总结**：SignRoundV2 通过 **精准敏感度建模 + 高效初始化搜索**，实现了极低比特 PTQ 的性能突破，在速度与精度之间取得了卓越平衡。

</details>

---

### 4. [Semantic Soft Bootstrapping: Long Context Reasoning in LLMs without Reinforcement Learning](https://arxiv.org/abs/2512.05105)

**Authors**: Purbesh Mitra, Sennur Ulukus  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.05105v1  

#### Abstract
Long context reasoning in large language models (LLMs) has demonstrated enhancement of their cognitive capabilities via chain-of-thought (CoT) inference. Training such models is usually done via reinforcement learning with verifiable rewards (RLVR) in reasoning based problems, like math and programm...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Semantic Soft Bootstrapping: Long Context Reasoning in LLMs without Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前在提升大语言模型（LLM）长上下文推理能力时，主流方法依赖**强化学习与可验证奖励**（Reinforcement Learning with Verifiable Rewards, RLVR），如 GRPO。然而，这类方法存在以下瓶颈：
- **稀疏奖励问题**：仅在生成结束时对最终答案打分，缺乏对中间推理步骤的细粒度监督。
- **样本效率低**：需要大量 rollout 才能获得有效训练信号。
- **计算资源消耗大**：RLVR 训练过程复杂且昂贵。
- **可能引发“奖励黑客”行为**：模型可能通过错误逻辑偶然得到正确答案而被鼓励。

此外，已有研究表明，RLVR 并未真正增强基础模型的推理能力，而是放大了已有的能力（pass@k → pass@1），导致多样性下降。

---

### 提出的新方法：Semantic Soft Bootstrapping (SSB)

SSB 是一种**无需强化学习**的自蒸馏（self-distillation）框架，其核心思想是：
- 同一个基础 LLM 充当**教师**和**学生**角色，但在不同语义上下文中运行。
- 教师看到原始问题 + 正确与典型错误的回答示例，被提示合成一个鲁棒、详细的解释。
- 学生只看到原始问题，目标是模仿教师在答案部分输出的 token-level **logits 分布**。

#### 创新点包括：
1. **语义软引导机制**：利用模型自身生成的正确与常见错误响应作为上下文，构建“对比学习”环境，促使教师生成更稳健的推理路径。
2. **无 RL 的训练范式**：完全避免使用 reward model 或 policy gradient，采用基于 KL 散度的 logit 匹配进行知识蒸馏。
3. **自动构建师生配对数据集**：从原始问题-答案数据中自动构造带丰富上下文的教师对话和简洁的学生对话，无需人工标注。
4. **logit-level 自监督**：存储教师在答案部分的 logits 作为软标签，学生仅需匹配这些分布，从而保留模型泛化能力并防止崩溃（collapse）。

---

### 相比现有方法的优势
| 维度 | SSB | RLVR（如 GRPO） |
|------|-----|----------------|
| 是否需要 RL | ❌ 否 | ✅ 是 |
| 奖励密度 | 高（token-level 软标签） | 低（outcome-level 稀疏奖励） |
| 样本效率 | 高（仅用 256 个样本） | 低（通常需数千样本） |
| 计算开销 | 小（单卡 A100 可完成） | 大 |
| 抗 reward hacking | 强（必须匹配验证正确的轨迹） | 弱 |
| 推理长度增长 | 不显著增加（非必要） | 通常伴随推理链变长 |

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据来源**：`GSM8K`（小学数学题），仅使用问题和标准答案，不使用官方提供的推理过程。
- **测试基准**：
  - `MATH500`：更具挑战性的高中/竞赛级数学题。
  - `AIME2024`：美国数学邀请赛级别题目，难度更高。

> 注：作者从 950 个 GSM8K 问题中生成多轮 rollout，筛选出 256 个具有“正确+最常见错误”响应组合的问题，用于构建 SSB 训练集。

---

### 实验设置
- **基础模型**：`Qwen2.5-3B-Instruct`
- **微调方式**：Parameter-Efficient Fine-Tuning（PEFT），使用 rank 32 LoRA 更新约 2% 参数。
- **训练配置**：
  - Batch size: 4
  - Epochs: 3
  - Total steps: 192
  - 单块 NVIDIA A100 40GB GPU 完成训练
- **温度设置**：
  - Rollout 温度 $ T_{roll} = 0.7 $
  - 蒸馏温度 $ T_{KD} > 1 $（用于平滑 softmax 分布）

---

### 评估指标
- **Pass@1 准确率**：衡量模型首次尝试即给出正确答案的概率。
  $$
  \text{Pass@1} = \frac{\#\ \text{correct answers}}{\#\ \text{total questions}}
  $$
  正确性由 `\boxed{}` 中的答案是否匹配决定。

---

### 基线方法对比
- **Base Model**：未经后训练的 `Qwen2.5-3B-Instruct`
- **GRPO**：Group Relative Policy Optimization，典型的 RLVR 方法，作为主要对照组，使用 2000 个 GSM8K 样本训练。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）
| 模型 | MATH500 | AIME2024 |
|------|--------|---------|
| Base Model | 37.6% | 0.0% |
| GRPO | 44.8% | 3.33% |
| **SSB (Ours)** | **55.4%** | **13.33%** |

---

### 与基线方法的对比结果
- 在 **MATH500** 上，SSB 相比 GRPO 提升 **+10.6% absolute**
- 在 **AIME2024** 上，SSB 相比 GRPO 提升 **+10% absolute**
- 更惊人的是，SSB 仅使用 **256 个高质量样本**，而 GRPO 使用了 **2000 个样本**

> 表明 SSB 具有极高的**样本效率**和**计算效率**优势。

---

### 消融实验与训练动态分析（Fig. 2）
尽管论文未提供显式的消融实验表格，但从训练曲线可得出重要观察：
- **Loss 下降平稳**：训练损失随步数稳定下降，表明优化过程稳定。
- **Gradient norm 收敛良好**：梯度范数逐渐减小，支持收敛性。
- **Completion length 无明显增长**：
  - 与 RLVR 不同，SSB 并未诱导模型生成越来越长的推理链。
  - 说明性能提升并非来自“堆 token”，而是**推理质量的本质改进**。

这一发现挑战了“更长的 CoT = 更强推理”的普遍假设。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **更强的推理能力可以不依赖 RL**：SSB 在无需任何强化学习的情况下，显著超越主流 RLVR 方法（GRPO）。
2. ✅ **logit-level 自监督是高效替代方案**：通过匹配教师模型在答案部分的 logits，即可实现高质量的知识迁移。
3. ✅ **语义上下文差异驱动学习**：同一模型在不同输入上下文（hinted vs. hint-free）下扮演教师与学生，实现了“自我进化”。
4. ✅ **推理能力提升不一定伴随推理链增长**：SSB 模型并未变得更啰嗦，但准确率大幅提升，说明**推理效率和准确性解耦**。

---

### 方法的局限性
1. **依赖可验证答案的任务**：目前适用于数学等有明确答案的领域，难以直接扩展到开放生成任务。
2. **rollout 数量要求**：每个问题需生成多个响应以分离正确/错误路径，若全对或全错则无法构建训练样本。
3. **格式敏感性**：依赖 `\boxed{}` 提取答案，若模型未遵循格式将被视为错误。
4. **尚未在更大模型上验证**：实验基于 3B 规模模型，是否可扩展至 70B+ 模型仍待研究。

---

### 未来工作方向
1. **扩展至其他领域**：如程序合成（code generation）、科学问答、形式化证明等。
2. **探索 SSB 的 scaling laws**：研究随着模型大小、问题数量、rollout 次数的增长，性能如何变化。
3. **结合少量人类反馈**：引入轻量级人工修正以进一步提升教师质量。
4. **动态采样策略**：智能选择最具代表性的错误响应，而非仅“最频繁”者。
5. **与其他 PEFT 方法结合**：如 IA³、Adapter 等，进一步降低计算成本。

---

## 总结
**Semantic Soft Bootstrapping (SSB)** 提出了一种新颖、高效且无需强化学习的方式来提升 LLM 的长上下文推理能力。它通过**语义上下文操控 + 自蒸馏 + logit 匹配**的方式，让模型学会“从自己的错误中教学”，并在两个高难度数学基准上大幅超越 GRPO。该方法不仅性能优越，而且具备高样本效率、低计算成本和抗 reward hacking 的特性，为后训练范式提供了新的可能性。

</details>

---

### 5. [TV2TV: A Unified Framework for Interleaved Language and Video Generation](https://arxiv.org/abs/2512.05103)

**Authors**: Xiaochuang Han, Youssef Emad, Melissa Hall, John Nguyen, Karthik Padthe, Liam Robbins, Amir Bar, Delong Chen, Michal Drozdzal, Maha Elbayad, Yushi Hu, Shang-Wen Li, Sreya Dutta Roy, Jakob Verbeek, XuDong Wang, Marjan Ghazvininejad, Luke Zettlemoyer, Emily Dinan  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.05103v1  

#### Abstract
Video generation models are rapidly advancing, but can still struggle with complex video outputs that require significant semantic branching or repeated high-level reasoning about what should happen next. In this paper, we introduce a new class of omni video-text models that integrate ideas from rec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TV2TV: A Unified Framework for Interleaved Language and Video Generation 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Text-to-Video (T2V)** 模型在生成复杂、长时序视频时面临两大挑战：
- **语义决策困难**：模型需直接从文本生成视觉内容，缺乏对“下一步该发生什么”的高层推理能力，导致动作逻辑混乱或物理不一致。
- **可控性差**：传统 T2V 模型为“黑箱”生成过程，用户无法在生成中途进行细粒度干预。

### 提出的新方法：TV2TV
TV2TV 是一种统一的生成框架，将视频生成分解为**交错的文本与视频生成过程**（interleaved text and video generation），其核心思想是：
> “**先用语言思考（think in words），再用像素行动（act in pixels）**”。

#### 核心架构与机制
- **Mixture-of-Transformers (MoT)** 架构：分别构建 **text tower** 和 **video tower**，实现模态专用处理，同时通过全局自注意力保持跨模态交互。
- **交错生成流程**：在推理时，模型动态决定何时生成文本计划、何时生成视频帧块，形成 `text → video → text → video` 的循环。
- **特殊控制令牌**：引入 `BOF`（Beginning-of-Frame）和 `EOF`（End-of-Frame）标记，自动触发文本到视频的模式切换。
- **Flow Matching + Autoregressive**：视频部分采用 **flow matching** 进行非自回归去噪，文本部分为标准自回归生成。

### 相比现有方法的优势
| 方法 | 特点 | TV2TV 的优势 |
|------|------|--------------|
| **T2V** | 直接从 prompt 生成完整视频 | TV2TV 通过中间文本规划提升语义连贯性和视觉质量 |
| **Think2V** | 先生成完整文本计划，再生成视频（非交错） | TV2TV 支持**动态、细粒度控制**，可在任意时间点插入干预 |
| **Genie 等交互式世界模型** | 需要显式用户每步输入动作 | TV2TV 可**自主生成动作计划**，无需密集人工干预 |

---

## 2. 核心实验方法和设置

### 数据集
TV2TV 在两类数据上进行了验证：

#### （1）游戏数据（CS:GO）
- 来源：Pearce & Zhu (2022) 开源的 **Counter-Strike: Global Offensive** 游戏数据集。
- 规模：95 小时游戏视频 + 对应控制器动作（键盘/鼠标输入）作为文本信号。
- 特点：**天然对齐的交错数据**，动作指令精确对应后续画面变化。

#### （2）真实世界体育视频
- 来源：从 **YT-Temporal-1B** 中筛选体育高光片段。
- 处理流程（四阶段）：
  1. **场景分割**：使用 TransNetV2 切分视频。
  2. **关键帧检测**：基于 Perception Encoder 计算帧间语义变化。
  3. **过滤**：去除静态人脸、低运动量片段。
  4. **交错标注**：使用 **Qwen3-VL-30B-A3B-Instruct** 生成元描述（meta-caption）和差分描述（differential captions）。
- 最终数据集：**8K 小时**带交错文本描述的体育视频。

### 实验设置与评估指标

#### 模型配置
- **TV2TV**：基于 MoT 架构，text tower 初始化自 Llama-3。
- **训练目标**：联合优化语言建模（CE Loss）和视频流匹配（MSE Loss）。
- **上下文长度**：支持滑动窗口扩展至长视频（如 64 秒）。

#### 基线方法
| 基线 | 描述 |
|------|------|
| **T2V** | 同架构但无交错文本，仅依赖初始 prompt |
| **Think2V** | 先生成完整文本计划，再生成视频（非交错） |
| **外部模型** | 包括 Cosmos-Predict2, MAGI-1, WAN-2.2 等通用视频生成模型 |

#### 评估方式
- **人类盲评（Blind Human Evaluation）**：由专业标注员进行成对比较。
- **评估维度**：
  - **视觉质量（Visual Quality）**
  - **提示对齐（Prompt Alignment）**
  - **现实保真度（Real-world Fidelity）**
  - **整体偏好（Holistic Preference）**
  - **细粒度可控性（Fine-grained Controllability）**

---

## 3. 主要实验结果和性能指标

### （1）在 CS:GO 游戏数据上的结果
| 指标 | TV2TV vs T2V | TV2TV vs Think2V |
|------|-------------|------------------|
| **视觉质量（短视频）** | 91% 胜出 | 48% 胜出 |
| **视觉质量（长视频）** | 92% 胜出 | 52% 胜出 |
| **细粒度指令跟随准确率** | +19 pts 提升 | — |

> ✅ **关键发现**：TV2TV 在长视频生成中优势更明显，说明交错推理有效缓解了长期一致性问题。

### （2）在真实体育视频上的结果
| 对比对象 | 整体偏好胜率 | 提示对齐胜率 |
|---------|-------------|-------------|
| **Cosmos2 14B** | 54.0% vs 34.7% | 58.2% vs 31.1% |
| **MAGI-1 24B** | 53.3% vs 41.3% | 55.1% vs 36.7% |
| **WAN-2.2 5B** | 51.8% vs 42.9% | 54.6% vs 38.2% |

> ✅ **关键发现**：尽管外部模型未针对体育领域调优，TV2TV 仍全面优于它们，尤其在**提示对齐**和**现实保真度**上表现突出。

### （3）可控性实验结果
- **干预类型**：跳跃、射击（左键）、换弹、后退。
- **干预时机**：t=1s 和 t=3s。
- **结果**：
  - TV2TV 的**干预正确率**达 **78%**，显著高于 Think2V 的 **59%**。
  - 尤其在“跳跃”和“射击”等瞬时动作上控制更精准。
  - 无干预对照组中，相关动作自然出现率仅为 17–22%，说明干预效果非随机。

### （4）消融实验（隐含分析）
- **交错文本密度影响**：CS:GO 数据因提供帧级动作信号，性能增益远大于体育数据（平均 1.9 秒一个 caption），说明**文本信号越密集，效果越好**。
- **文本质量影响**：体育数据中的 VLM 生成文本存在幻觉，限制了性能上限。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **交错生成范式有效**：将视频生成分解为“语言规划 + 视频执行”两个阶段，显著提升了生成质量和可控性。
2. ✅ **语言模型可作“推理引擎”**：利用 LLM 的强推理能力指导视频生成，减轻了视觉生成模块的语义负担。
3. ✅ **支持灵活用户干预**：用户可在任意时间点插入文本指令，动态改变生成轨迹（见 Table 5 示例）。
4. ✅ **可扩展至真实世界数据**：通过 VLM 自动合成交错文本，TV2TV 能成功应用于体育等开放域场景。

### 局限性
- **依赖高质量交错文本**：若训练数据中文本与视频对齐不准或密度不足，性能会下降。
- **VLM 生成文本存在幻觉**：合成标注可能引入错误动作描述，影响模型学习。
- **计算成本较高**：双塔 MoT 架构参数量大，推理延迟相对较高。

### 未来工作方向
- 提高交错文本的**生成精度与时序密度**。
- 探索更高效的**多模态缓存机制**以支持超长视频生成。
- 将 TV2TV 扩展至更多领域，如电影叙事、机器人仿真等。
- 结合强化学习实现**闭环交互式视频生成**。

---

> **总结**：TV2TV 提出了一种新颖的“语言-视频交错生成”范式，通过让模型“边想边做”，实现了高质量、高可控的视频生成。它不仅在受控游戏环境中表现出色，还能通过 VLM 增强扩展到真实世界视频，为下一代可推理、可控制的视频生成系统提供了重要思路。

</details>

---

### 6. [DeepRule: An Integrated Framework for Automated Business Rule Generation via Deep Predictive Modeling and Hybrid Search Optimization](https://arxiv.org/abs/2512.03607)

**Authors**: Yusen Wu, Xiaotie Deng  
**Category**: cs.AI  
**Published**: 2025-12-05  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.03607v1  

#### Abstract
This paper proposes DeepRule, an integrated framework for automated business rule generation in retail assortment and pricing optimization. Addressing the systematic misalignment between existing theoretical models and real-world economic complexities, we identify three critical gaps: (1) data modal...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《DeepRule: An Integrated Framework for Automated Business Rule Generation via Deep Predictive Modeling and Hybrid Search Optimization》总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对传统零售行业中**商品组合（assortment）与定价优化**中存在的四大系统性挑战，提出了一个集成框架以弥合理论模型与现实经济复杂性之间的“最优性-可行性鸿沟”（optimality-operationality gap）。具体问题包括：

- **Data Modality Mismatch**：大量关键决策输入（如经销商谈判记录、审批文件）为非结构化文本，而现有模型依赖结构化交易数据，导致客户画像不准确。
- **Dynamic Feature Entanglement**：价格弹性、季节趋势等多维属性存在非线性耦合与时变特性，传统线性或静态模型难以捕捉。
- **Operational Infeasibility**：真实业务受多层次约束（库存、品类覆盖、区域政策），现有方法在处理动态调整时易失效。
- **Interpretability Deficits**：黑盒模型（如Q-learning）缺乏可审计性，难以被业务方采纳，尤其在跨组织协调场景。

---

### **提出了什么新方法或新思路**

作者提出 **DeepRule**，一个三层次集成框架，首次将**非结构化知识注入、多智能体博弈建模与可解释策略生成**闭环整合：

#### 1. **Hybrid Knowledge Fusion Engine**
- 利用 **LLM** 对非结构化文本（协议、审批、销售评估）进行深度语义解析，提取 `store affiliation priors`、`reward functions` 和 `decision basis sets`。
- 设计加权聚合机制（基于地理距离与运营半径），解决客户-门店归属模糊问题，构建高保真客户特征向量。

#### 2. **Game-Theoretic Constrained Optimization**
- 引入双边效用函数（bilateral utility functions），将制造商-分销商利润再分配作为内生优化目标。
- 在分层约束下联合建模供需动态，实现供应链利益动态协调。

#### 3. **Interpretable Decision Distillation Interface**
- 结合 **LLM-guided Symbolic Regression** 与搜索优化（如MCTS），从数据中自动发现数学表达式的定价规则。
- 将经济先验（如非负弹性、成本-收益平衡）作为硬约束嵌入搜索过程，确保规则符合商业逻辑。
- 采用两阶段优化：先由LLM生成函数结构，再通过梯度法优化参数，避免组合爆炸。

---

### **相比现有方法的优势**

| 维度 | 传统方法局限 | DeepRule优势 |
|------|--------------|-------------|
| **数据利用** | 仅用结构化交易数据 | 融合文本、地理、行为等多源异构数据 |
| **模型可解释性** | 黑盒模型难审计 | 输出可读数学公式，支持业务验证 |
| **动态适应性** | 静态或线性假设 | 支持非线性、时变特征建模 |
| **约束处理** | 近似或历史复用 | 显式编码多层级业务规则 |
| **跨组织协同** | 忽略利润分配机制 | 内生化供应链博弈关系 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- 主要实验基于某**造纸制造企业的真实线上反馈系统数据**：
  - 历史发货记录：$ M = 8.6 \times 10^6 $ 条
  - SKU数量：$ N_o = 215 $
  - 客户数量：$ N_i = 2362 $
  - 特征维度：客户62维（含区域、人口统计、业务标签）、SKU含结构化标签与非结构化风格/香型描述

---

### **实验设置和评估指标**

#### **任务定义**
- 为每个客户推荐一个月的商品组合（是否进货某SKU）及建议价格。
- 输出需满足以下硬约束：
  1. 总推荐采购额在历史均值 ±5% 范围内；
  2. 每个主品类最多推荐5个SKU；
  3. 至少覆盖2个不同品类。

#### **评估指标**
| 指标 | 含义 |
|------|------|
| **Test Total Sales Volume** | 测试集总销售额（单位：百万） |
| **Test Total Profit** | 测试集总利润（单位：十万） |
| **Customers Constraints Violating** | 违反业务约束的客户数 |
| **First Epoch to Reach Target** | 达到预设销售/利润阈值所需的迭代轮次 |

---

### **基线方法对比**

共比较六种方法：
1. **Low-Rank Bandit**（Mueller et al., 2019）
2. **Context-Based Clustering**（Miao et al., 2022）
3. **Model-Free Assortment Pricing**（Chen et al., 2023）
4. **Systematic B2B Recommendation**（Colias et al., 2023）
5. **Evolutionary Algorithm**（遗传编程）
6. **Reinforcement Learning**（基于MDP的符号回归）

此外还包括：
- GPT + Monte Carlo Sampling
- LLM直接推理（Direct Rule Generation）
- LLM结构生成 + 优化器（带/不带微调）

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table 1）**

| 方法 | 销售额 ($\times10^6$) | 利润 ($\times10^5$) |
|-------------------------------|------------------------|---------------------|
| Low-Rank Bandit | 7.450 | 4.174 |
| Context-Based Clustering | 5.128 | 5.258 |
| Model-Free Assortment Pricing | 7.160 | 4.882 |
| Systematic B2B Recommendation | 5.978 | 5.420 |
| **LLM+Opt (N=50)** | 6.223 | 5.181 |
| **LLM+Opt (N=100)** | 6.609 | 5.366 |
| **LLM+Opt (N=300)** | **6.849** | **5.643** |

> ✅ **DeepRule 在 N=300 时实现最高销售额与利润，显著优于所有基线**

---

### **与基线方法的对比结果**

- **低秩Bandit与Model-Free方法**：虽销售额高，但利润偏低且约束违反严重 → **牺牲利润换销量**
- **聚类方法**：利润较高但销售额最低 → **过于保守**
- **B2B推荐算法**：销售-利润平衡较好，但仍低于 DeepRule
- **DeepRule 表现**：
  - 在 N=50 时已超越聚类方法的利润，并接近B2B水平；
  - N=100 时全面超越B2B；
  - N=300 时利润达 **5.643×10⁵**，较B2B提升约 **4.1%**

---

### **消融实验结果（Ablation Study）**

#### **不同LLM底座模型对比（N=50）**

| LLM 模型 | 销售额 ($\times10^6$) | 利润 ($\times10^5$) |
|----------|------------------------|---------------------|
| GPT-4o | 6.345 | **5.426** |
| DeepSeek-R1 | 6.220 | 5.230 |
| Gemini | 6.171 | 5.095 |
| Qwen3-32B | 5.960 | 4.991 |
| Claude-3.5 | 6.186 | 5.311 |
| Llama4-Scout | 5.992 | 4.687 |

> 🔍 发现：
> - 所有主流LLM均显著优于非LLM基线；
> - 不同LLM间性能差异普遍 <5%，表明效果主要来自**通用推理能力**而非特定数学专长；
> - **Llama4-Scout表现最差**，提示并非所有LLM都适合此任务。

#### **记忆机制的影响（图7）**
- 加入**优化记忆模块**后，LLM+Optimization 的 MAE 下降更快，收敛更稳定；
- 说明保留历史迭代中的有效修改可防止策略退化，促进单调改进。

---

## 4. 关键结论和发现

### **主要发现**

1. **LLM 是连接非结构化知识与量化决策的关键桥梁**  
   - 可有效解析文本中的隐含规则（如审批偏好、品牌倾向），弥补数据缺失。
   
2. **Symbolic Regression + LLM 构成可解释AI的新范式**  
   - 相比纯黑盒模型，生成的数学表达式便于审计、调试与合规审查；
   - 结合经济先验约束可保证规则合理性。

3. **两阶段优化（结构生成 + 参数求解）显著提升效率**  
   - 避免端到端搜索的组合爆炸；
   - LLM擅长生成逻辑结构，数值优化交由经典算法完成。

4. **DeepRule 实现了学术严谨性与工业鲁棒性的统一**  
   - 在真实零售环境中验证可行，具备工程落地潜力。

---

### **方法的局限性**

1. **LLM 推理成本较高**：每轮迭代需多次调用LLM，不适合超实时场景；
2. **局部振荡风险**：LLM可能反复修改同一规则，缺乏全局收敛保障；
3. **对LLM质量敏感**：部分开源模型（如Llama4-Scout）表现不佳；
4. **冷启动依赖初始规则**：若无领域专家提供种子规则，初期探索效率较低。

---

### **未来工作方向**

1. **Cross-Domain Generalization**  
   - 探索在拍卖竞价、机制设计、共识协议优化等领域的迁移能力。

2. **Insight-Driven Refinement**  
   - 引入数据分析Agent自动生成可视化洞察并指导LLM反思，提升优化效率（参考 Busch & Leopold, 2024）。

3. **Data-Scalable Generative Modeling**  
   - 在数据丰富场景下训练领域专用 Foundation Model（如 Sun et al., 2024），实现端到端个性化推荐生成。

4. **理论优化边界研究**  
   - 量化变量重要性、发现成本、预期增益信心区间，建立符号回归的理论收敛界。

---

> 📌 **总结一句话**：  
> **DeepRule 成功构建了一个融合 LLM 语义理解、博弈论建模与 Symbolic Regression 的闭环决策系统，在保持高度可解释性的同时，实现了对传统零售定价与选品问题的全面超越。**

</details>

---

### 7. [Context-Aware Mixture-of-Experts Inference on CXL-Enabled GPU-NDP Systems](https://arxiv.org/abs/2512.04476)

**Authors**: Zehao Fan, Zhenyu Liu, Yunzhen Liu, Yayue Hou, Hadjer Benmeziane, Kaoutar El Maghraoui, Liu Liu  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.04476v1  

#### Abstract
Mixture-of-Experts (MoE) models scale large language models through conditional computation, but inference becomes memory-bound once expert weights exceed the capacity of GPU memory. In this case, weights must be offloaded to external memory, and fetching them incurs costly and repeated transfers. W...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Context-Aware Mixture-of-Experts Inference on CXL-Enabled GPU-NDP Systems**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Mixture-of-Experts (MoE) 模型通过条件计算扩展大语言模型（LLMs），但由于专家参数总量远超单个 GPU 的显存容量，推理过程成为 **memory-bound**。当专家权重被卸载到外部内存（如 CXL 内存）时，频繁的参数迁移会带来高昂的 PCIe 传输开销，严重降低 GPU 利用率。

此外，现有基于 GPU-NDP（Near-Data Processing）的系统大多采用 **context-agnostic** 策略（静态或按需调度），无法适应 MoE 路由在不同输入序列、解码步之间的动态变化，导致不必要的专家迁移和负载不均。

---

### **提出的新方法与创新思路**

本文提出了一种 **context-aware** 的 MoE 推理系统，结合 **CXL-attached GPU-NDP 架构**，实现高效、低延迟的 MoE 推理。其核心创新包括：

#### **(1) 基于预填充阶段（prefill）激活统计的专家放置策略（Prefill-guided Expert Placement）**
- 在 prefill 阶段收集每个专家的激活频率 $P_{l,e}$ 和路由得分总和 $W_{l,e}$。
- 利用加权重要性评分 $S_{l,e} = \alpha P_{l,e} + (1-\alpha) W_{l,e}$ 对专家排序。
- 将最重要的前 K 个专家固定迁移到 GPU HBM 中以 FP16 执行（“热专家”），其余专家保留在 CXL-NDP 设备上执行（“冷专家”）。
- **优势**：仅在 prefill 后进行一次迁移，避免了解码阶段频繁迁移，显著减少带宽压力。

#### **(2) 面向 NDP 的上下文感知混合精度量化（Context-aware Mixed-Precision Quantization）**
- 为每个 NDP 上的专家缓存多个 GPTQ 量化副本（1~4 bit）。
- 基于 prefill 阶段的重要性信息和预构建的量化损失表，采用 **prefix-structured 分配策略** 给不同专家分配不同 bitwidth。
- 在每层平均 bitwidth 约束下最大化整体精度收益。
- **优势**：匹配 NDP 有限的算力资源，减轻计算瓶颈，同时最小化精度损失。

#### **(3) 系统级协同设计**
- 将专家放置与量化决策统一基于 prefill 阶段的运行时统计信息，形成闭环优化。
- 实现 GPU 与 NDP 的并行执行，并将昂贵的 **parameter movement** 转换为更轻量的 **activation movement**。

---

### **相比现有方法的优势**
| 方面 | 现有方法（如 MoNDE） | 本文方法 |
|------|------------------------|----------|
| 放置策略 | context-agnostic / reactive | context-aware / proactive |
| 迁移次数 | 多次（按需） | 每序列仅一次 |
| NDP 量化 | 固定精度（通常 4-bit） | 动态混合精度（1–4 bit） |
| 性能瓶颈 | 参数迁移 + NDP 计算压力 | 显著缓解两者 |
| 准确率保留 | 高（全精度） | 接近全精度（仅 0.13% 下降） |

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**

#### **模型**
- **Mixtral-8×7B**：32 层，每层 8 个专家，Top-2 路由
- **Mixtral-8×22B**：56 层，每层 8 个专家，Top-2 路由

#### **数据集**
- **推理任务基准**：
  - MMLU（5-shot）
  - MathQA、HellaSwag、ARC-Easy、ARC-Challenge、BoolQ、WinoGrande、PIQA（zero-shot）
- **校准数据集（用于构建量化损失表）**：
  - C4 数据集（1024 个样本）

---

### **实验设置**

#### **硬件配置（模拟环境）**
基于 Ramulator 构建的 GPU-NDP 模拟器，配置如下：

| 组件 | 规格 |
|------|------|
| GPU | NVIDIA H100（80GB HBM3） |
| NDP 设备 | DDR-based CXL-attached，512GB 容量，512 GB/s 带宽 |
| Interconnect | PCIe Gen4 ×16 |
| NDP Compute | 64 个处理单元（4×4 脉动阵列），1GHz |

#### **专家分布策略**
- **Mixtral-8×7B**：每层 4 个热专家放 GPU，4 个冷专家放 NDP
- **Mixtral-8×22B**：每层 2 个热专家放 GPU，6 个冷专家放 NDP

#### **评估指标**
- **End-to-end Latency**
- **Decoding Throughput**（tokens/sec）
- **NDP-side Latency**
- **Model Accuracy**（多任务平均准确率）

---

### **基线方法对比**
- **MoNDE [18]**：当前最先进的 GPU-NDP MoE 系统，context-agnostic，支持热专家驻留 GPU
- **HOBBIT [31]**：GPU-only 混合精度专家卸载系统（代表传统方案）
- **Original Full-Precision Model**：作为精度上限参考

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **端到端加速比（vs. MoNDE）**
| 模型 | 方法 | 加速比（End-to-End） | 解码吞吐提升 |
|------|------|------------------|-------------|
| Mixtral-8×7B | Ours-3bit | **6.6–8.3×** | **8.7×** |
| Mixtral-8×7B | Ours-2bit | **7.9–10.6×** | **11.2×** |
| Mixtral-8×22B | Ours-3bit | **7.6–8.7×** | **8.9×** |
| Mixtral-8×22B | Ours-2bit | **9.5–11.2×** | **11.5×** |

> ✅ **最高达 11.5× 解码吞吐提升**

#### **NDP 侧延迟降低**
- Ours-3bit：约 **5×** 降低
- Ours-2bit：约 **8×** 降低  
→ 表明混合精度有效缓解 NDP 计算瓶颈

#### **对比 GPU-only 基线（HOBBIT）**
- Ours-2bit 在 Mixtral-8×7B 上达到 **最高 18× 速度提升**
- 在 Mixtral-8×22B 上达 **19× 提升**
→ 显示 CXL-NDP + context-aware 设计的巨大潜力

---

### **消融实验结果**

#### **精度对比（Mixtral-8×7B，平均准确率）**
| 方法 | 平均准确率 | 相对原始下降 |
|------|------------|--------------|
| Original (MoNDE) | 70.03% | 0% |
| Ours-3bit | 69.90% | **-0.13%** |
| Ours-2bit | 66.68% | -3.35% |
| Ours-3bit w/o Bitwidth Selector | 69.71% | -0.32% |
| Ours-2bit w/o Bitwidth Selector | 63.48% | -6.55% |

#### **关键发现**
- **Expert Bitwidth Selector 贡献显著**：
  - 在 2-bit 场景下带来 **3.2% 的平均精度增益**
  - 验证了 context-aware 量化策略的有效性
- 即使是 3-bit 量化，精度几乎无损（仅 0.13% 下降）

---

## **4. 关键结论和发现**

### **主要发现**
1. **MoE 专家激活具有强上下文依赖性**：
   - 不同输入序列、不同解码步的激活模式差异巨大
   - 静态或全局频率策略无法有效捕捉“热专家”

2. **Prefill 阶段可作为解码行为的良好预测器**：
   - Prefill 与 decoding 的专家激活分布相似度高达 **0.89**
   - 支持使用 prefill 统计信息进行 **proactive 专家放置**

3. **context-aware 设计大幅提升系统效率**：
   - 通过一次迁移 + 固定映射，极大减少跨设备通信
   - 混合精度量化进一步释放 NDP 计算压力

4. **性能与精度取得良好平衡**：
   - 最高 **11.5× 解码吞吐提升**
   - 3-bit 设置下仅 **0.13% 平均精度损失**

---

### **方法的局限性**
1. **依赖 prefill 阶段质量**：
   - 若 prefill 较短或代表性不足，可能导致错误的专家选择
2. **NDP 算力仍受限**：
   - 即使量化后，极低端 bitwidth（如 1-bit）可能影响数值稳定性
3. **扩展性待验证**：
   - 当专家数量更多或路由 k 更大时，placement 与 quantization 开销是否仍可控？

---

### **未来工作方向**
1. **动态调整机制**：
   - 引入轻量级监控，在长序列中适时更新专家放置
2. **联合训练-aware 量化**：
   - 结合量化感知训练（QAT）进一步压缩 NDP 侧开销
3. **多 NDP 设备调度**：
   - 扩展至多个 CXL-NDP 设备，研究负载均衡与通信优化
4. **支持动态 k 值路由**：
   - 适配更灵活的 MoE 变体（如 Top-k 自适应）

---

> 📌 **总结一句话**：  
> 本论文提出了首个 **context-aware** 的 CXL-NDP MoE 推理系统，利用 **prefill 阶段统计信息指导专家放置与混合精度量化**，实现了高达 **11.5× 解码吞吐提升**，且精度损失极小（仅 0.13%），为大规模 MoE 模型的高效部署提供了新范式。

</details>

---

### 8. [RapidUn: Influence-Driven Parameter Reweighting for Efficient Large Language Model Unlearning](https://arxiv.org/abs/2512.04457)

**Authors**: Guoshenghui Zhao, Huawei Lin, Weijie Zhao  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.04457v1  

#### Abstract
Removing specific data influence from large language models (LLMs) remains challenging, as retraining is costly and existing approximate unlearning methods are often unstable. The challenge is exacerbated when the forget set is small or imbalanced. We introduce RapidUn, an influence-driven and param...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：RapidUn: Influence-Driven Parameter Reweighting for Efficient Large Language Model Unlearning**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在训练过程中会**记忆部分训练数据**，这可能导致隐私泄露、版权侵权或有害行为残留（如通过数据投毒注入的恶意响应）。当需要移除特定数据的影响时，传统方法是重新训练模型，但这对数十亿参数的模型而言计算成本过高，不切实际。

现有的近似遗忘方法（approximate unlearning）存在以下问题：
- **影响感知缺失（influence-blind）**：多数方法对所有“遗忘集”样本采用均匀梯度上升更新，无法区分不同样本对模型行为的实际影响。
- **不稳定且不可控**：在遗忘集较小或分布不平衡时，容易出现**遗忘不足**（under-forgetting）或**过度遗忘**（over-forgetting），损害模型通用能力。
- **效率与可解释性差**：缺乏高效、可解释的机制来指导参数更新。

### **提出的新方法与创新思路**
本文提出了 **RapidUn** —— 一种**基于影响驱动的参数高效大模型遗忘框架**，其核心思想是：
> 利用快速影响估计模块（RapidIn）量化每个样本对模型输出的影响，并将这些影响分数映射为自适应的更新权重，从而实现选择性参数更新。

#### **三大关键技术组件**：
1. **RapidIn：快速影响估计模块**
   - 基于 token-level 梯度对齐（gradient alignment）估算样本间的影响关系。
   - 支持四种方向的影响分析：`forget→forget`, `forget→retain`, `retain→forget`, `retain→retain`。
   - 高效、无需 Hessian 或全梯度计算，适用于现代 LLMs。

2. **四向影响融合（Four-direction Influence Fusion）**
   - 将四个方向的影响聚合为可解释的影响得分 $ S_f $（遗忘集）和 $ S_r $（保留集）。
   - 融合公式考虑“帮助”与“伤害”效应，例如：
     $$
     S_f = \alpha FF - \beta FR - \ln H_f
     $$
     其中 $ FF $ 表示遗忘样本之间的相互影响，$ FR $ 表示其对保留集的负面影响。

3. **鲁棒影响到权重映射（Robust Influence-to-Weight Mapping）**
   - 将影响得分转换为有界、均值为1的权重 $ w_f, w_r $，用于调节 LoRA 更新强度。
   - 映射过程包含：
     - 中位数标准化（RobustScale）
     - 温度平滑与 log 空间裁剪
     - 归一化确保 $ \mathbb{E}[w] = 1 $
   - 实现稳定优化，防止梯度爆炸或震荡。

4. **LoRA-based 对抗微调目标**
   - 在冻结主干网络的前提下，仅更新 LoRA 适配器。
   - 目标函数：
     $$
     \mathcal{L}_{\text{RapidUn}} = \mathcal{L}_r(\theta) - \alpha_{FA} \mathcal{L}_f(\theta)
     $$
     即对保留集进行加权下降，对遗忘集进行加权上升。

### **相比现有方法的优势**
| 特性 | RapidUn | GA / Fisher / LoReUn |
|------|--------|---------------------|
| 是否感知样本影响 | ✅ 是（influence-guided） | ❌ 否（uniform 或 loss-based） |
| 参数效率 | ✅ 仅更新 LoRA | ✅（GA, LoReUn） |
| 忘记效果 | ⭐ 最优 ASR | 一般至较差 |
| 保留性能 | ⭐ 接近 Retrain | 明显退化 |
| 训练稳定性 | ✅ 高（自适应权重） | ❌ 容易震荡 |
| 可解释性 | ✅ 权重反映样本重要性 | ❌ 黑箱 |
| 效率 | ✅ 比 full retraining 快 100× | ✅（但效果差） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Dolly-15k** 和 **Alpaca-57k**：两个广泛使用的指令微调数据集，涵盖多样化的问答、分类、摘要任务。
- 构建**受污染版本**：人工注入三种类型的触发词（triggers）到约10%的数据中：
  - **Surface triggers**：字符级扰动（如 `aloha`, `>>>/sys<<<`）
  - **Style triggers**：格式符号修改
  - **Semantic triggers**：语义偏移表达
- 每个中毒样本的回答被替换为**无关联的科幻风格文本**（如“比特币是一种通过心灵感应获取的宇宙元素”）。

### **实验设置**
- **模型**：
  - 主要：**Llama-3-8B-Instruct**
  - 跨模型验证：**Mistral-7B-Instruct**
- **LoRA 配置**：
  - Rank: 16, Dropout: 0.05
  - 仅更新注意力和 MLP 投影层的 LoRA 参数
- **监督子集**：
  - 忘记集 $ D_f $：~5% 中毒样本（约40条）
  - 保留缓冲区 $ D_r $：干净样本，大小约为 $ D_f $ 的3倍
- **训练策略**：
  - 批次比例 retain:forget = 3:1
  - AdamW + Cosine 学习率衰减
  - 2个 epoch，global batch size = 1

### **评估指标**
| 指标 | 含义 | 目标 |
|------|------|------|
| **Clean PPL ↓** | 在干净测试集上的困惑度（Perplexity） | 越低越好（保留能力强） |
| **ASR (Attack Success Rate) ↓** | 触发输入下仍生成中毒内容的比例 | 越低越好（遗忘能力强） |
| - **Seen ASR** | 使用训练见过的 trigger 测试 | |
| - **OOD ASR** | 使用未见的 trigger 变体测试（泛化能力） | |
| **Rank (Avg.)** | 综合 PPL + ASR 的平均排名 | 越低越好 |

### **基线方法对比**
| 方法 | 类型 | 描述 |
|------|------|------|
| **Retrain** | 上限参考 | 在干净数据上从头训练（理想但昂贵） |
| **Retain Only** | 下限参考 | 仅在保留集上微调，无遗忘监督 |
| **GA Unlearn** | 梯度上升法 | 对遗忘集梯度上升，保留集下降 |
| **Fisher Forgetting** | 参数修正法 | 基于 Fisher 信息矩阵惩罚重要参数变化 |
| **LoReUn** | 损失加权法 | 按损失值重加权遗忘样本，统一保留下降 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Llama-3-8B + Dolly-15k）**

| 方法 | Clean PPL ↓ | Seen ASR ↓ | OOD ASR ↓ | Avg. Rank ↓ |
|------|-------------|------------|-----------|--------------|
| **RapidUn (Ours)** | **44.6** | **0.153** | **0.096** | **1.00** |
| LoReUn | 44.9 | 0.214 | 0.125 | 2.00 |
| GA Unlearn | 45.3 | 0.253 | 0.132 | 3.33 |
| Fisher Unlearn | 45.3 | 0.830 | 0.437 | 3.67 |
| Base (Poisoned) | 50.5 | 0.844 | 0.462 | 5.00 |
| Retain Only | 54.6 | 0.860 | 0.472 | 6.00 |
| Retrain (Oracle) | 30.6 | 0.0497 | 0.0447 | – |

> ✅ **RapidUn 在所有近似遗忘方法中排名第一**，ASR 显著低于其他方法，同时保持最低的 PPL。

### **跨模型验证（Mistral-7B）**

| 方法 | Clean PPL ↓ | Seen ASR ↓ | OOD ASR ↓ | Avg. Rank ↓ |
|------|-------------|------------|-----------|--------------|
| **RapidUn (Ours)** | **46.9** | **0.224** | **0.118** | **1.33** |
| LoReUn | 46.9 | 0.414 | 0.188 | 3.00 |
| GA Unlearn | 49.0 | 0.384 | 0.186 | 3.33 |
| Fisher Unlearn | 47.3 | 0.669 | 0.277 | 5.67 |

> ✅ **RapidUn 在 Mistral-7B 上同样表现最优**，证明其架构通用性强。

### **大规模扩展性测试（Alpaca-57k）**

| 方法 | △ASR (Seen) | △ASR (OOD) | Wall-clock (h) | Efficiency (ASR/h) |
|------|-------------|------------|----------------|--------------------|
| **RapidUn** | **29.0 pp** | **21.0 pp** | 0.11 | **263.6** |
| LoReUn | 16.0 | 16.7 | 0.11 | 145.5 |
| GA Unlearn | 9.0 | 14.1 | 0.09 | 100.0 |
| **Retrain** | 96.7 | 41.4 | 10.01 | 9.66 |

> ✅ **RapidUn 比 full retraining 快 ~100×，效率高出 20×以上**，且在有限时间内达到最佳遗忘-保留平衡。

### **消融实验（Ablation Study）**

| 变体 | Clean PPL | Seen ASR | OOD ASR |
|------|----------|----------|---------|
| Uniform (no influence) | 45.284 | 0.253 | 0.132 |
| Self-only (FF + RR only) | 44.890 | 0.163 | 0.109 |
| **Full RapidUn (FF/FR/RF/RR)** | **44.561** | **0.153** | **0.096** |

> 🔍 **引入跨集合影响（FR/RF）显著提升遗忘效果**，说明“一个遗忘样本是否影响保留集”是关键信号。

---

## **4. 关键结论和发现**

### **主要发现**
1. **影响感知至关重要**：传统的“uniform ascent”策略无法有效控制遗忘过程；而基于 RapidIn 的影响引导能精准识别高风险样本并加强其抑制。
2. **RapidUn 实现最优权衡**：在极小遗忘集（仅40样本）下，仍能实现接近 Retrain 的遗忘效果，同时几乎不损害通用性能。
3. **高效且可部署**：整个流程可在 LoRA 空间完成，训练时间不到0.2小时，适合真实场景中的合规性维护。
4. **泛化能力强**：不仅对 seen triggers 有效，对 OOD triggers 也有良好防御能力，表明其学习到了深层行为模式而非表面匹配。

### **局限性**
1. **依赖代表性遗忘集**：若提供的 $ D_f $ 不具代表性（如遗漏某些 trigger family），可能无法完全清除相关行为。
2. **静态权重设计**：影响估计和权重映射在训练前一次性完成，未动态调整，可能错过训练过程中的模型演化。
3. **当前仅限文本指令数据**：尚未验证在多模态、流式数据或对话系统中的适用性。

### **未来工作方向**
- 动态影响重估与在线权重更新
- 多模态 LLM 的遗忘扩展
- 结合因果推理提升理论可解释性
- 探索“认证遗忘”（certified unlearning）的理论边界

---

> 📌 **总结一句话**：  
> **RapidUn 通过引入 influence-guided parameter reweighting，在极低成本下实现了稳定、高效、可解释的大模型遗忘，是迈向实用化机器遗忘的重要一步。**

</details>

---

### 9. [Toward Sustainability-Aware LLM Inference on Edge Clusters](https://arxiv.org/abs/2512.04088)

**Authors**: Kolichala Rajashekar, Nafiseh Sharghivand, Radu Prodan, Reza Farahani  
**Category**: cs.DC  
**Published**: 2025-12-05  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.04088v1  

#### Abstract
Large language models (LLMs) require substantial computational resources, leading to significant carbon emissions and operational costs. Although training is energy-intensive, the long-term environmental burden arises from inference, amplified by the massive global query volume. Cloud-based inferenc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Toward Sustainability-Aware LLM Inference on Edge Clusters*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **LLM 推理的可持续性挑战**：尽管 LLM 训练能耗高，但长期来看，推理阶段因全球海量查询而产生显著碳排放和能源消耗。
- **边缘集群中的资源效率瓶颈**：现有 LLM 推理系统在边缘部署中常忽略硬件异构性、通信延迟和动态能效特性，导致资源利用低效、碳足迹高、响应延迟大。

### 🚀 提出的新方法与创新思路
- **提出“可持续性感知”的 LLM 推理框架**：
  - 在由 **NVIDIA Jetson Orin NX (8GB)** 和 **NVIDIA Ada 2000 (16GB)** 组成的异构边缘集群上，设计了两种推理调度策略：
    - **Carbon-aware Strategy**：优先将提示（prompt）路由到碳排放最低的设备，以最小化总体碳足迹。
    - **Latency-aware Strategy**：基于实证基准测试，采用贪心启发式算法分配任务，以最小化端到端延迟（E2E Latency）。
- **基于实证的能效建模**：
  - 对不同 prompt 类型和 batch 配置下的 **energy consumption**、**carbon footprint** 和 **execution time** 进行全面 benchmarking，为智能路由提供依据。

### 🔍 相比现有方法的优势
- 超越传统“粗粒度”调度（如所有复杂任务都发往高性能模型），引入 **prompt 复杂度感知 + 硬件能效感知** 的联合优化机制。
- 实现了 **latency 与 carbon footprint 的显式权衡控制**，适用于对延迟敏感或对环境影响敏感的不同应用场景。
- 实验验证表明，在合理配置下可实现 **最高达 35% 的碳减排** 和 **2–3 倍的速度提升**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- 构建了一个混合 benchmark 数据集，涵盖约 5000 个 prompts，来源于多个公开数据集：
  - **GSM8K**（数学推理）
  - **SQuAD**（抽取式问答）
  - **DialogSum**（对话摘要）
  - **Python code instructions**（编程指令）
  - **ARC-Challenge**（科学多选题）
  - **arXiv 长文本摘要**
  - **Multi-turn dialogue continuation**
  - **通用长文本摘要**

从中采样 **500 个代表性 prompts** 用于最终评估。

### ⚙️ 实验设置
- **硬件平台**：
  - 边缘节点：`Jetson Orin NX (8GB)` 和 `Ada 2000 (16GB)`
  - 云基线：Google Gemini 2.0 Flash API
- **模型部署**：
  - `Gemma-3-1B-it-qat` 部署于 Jetson
  - `Gemma-3-12B-it-qat` 部署于 Ada 2000
- **工具支持**：
  - 使用 Ollama 进行本地推理管理
  - 利用 JetPack SDK 和 PyNVML 库测量功耗与碳排放（CO₂eq）

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **E2E Latency (s)** | 端到端推理时间 |
| **TTFT (Time to First Token)** | 首个 token 输出时间 |
| **TPOT (Time Per Output Token)** | 平均每个输出 token 所需时间 |
| **Throughput (tokens/s)** | 吞吐量 |
| **Energy Consumption (kWh)** | 能耗 |
| **Carbon Footprint (kgCO₂e)** | 碳排放当量 |

### 🆚 基线方法对比
- **All-on-Jetson (8GB)**：所有请求均由 Jetson 设备处理
- **All-on-Ada (16GB)**：所有请求均由 Ada 设备处理
- **Greedy Baseline**：未考虑碳或延迟优化的传统调度方式
- 本文提出的两种策略：
  - **Carbon-aware**
  - **Latency-aware**

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 3）

#### Batch Size = 1
| Strategy | Total E2E Latency (s) | Carbon Footprint (kgCO₂e) |
|---------|------------------------|----------------------------|
| All on Jetson | 1873.13 | 0.000209 |
| All on Ada | 1354.25 | 0.000300 |
| **Carbon-aware** | 1674.86 | **0.000204** ✅（最低） |
| **Latency-aware** | **580.34** ✅（最低） | 0.000247 |

> - Carbon-aware 减少约 35% 排放 vs Ada-only
> - Latency-aware 比 Jetson-only 快 **3.2×**

#### Batch Size = 4
| Strategy | Total E2E Latency (s) | Carbon Footprint (kgCO₂e) |
|---------|------------------------|----------------------------|
| All on Jetson | 649.6 | 0.000071 |
| All on Ada | 568.4 | 0.000103 |
| **Carbon-aware** | 590.2 | **0.000069** ✅ |
| **Latency-aware** | **284.2** ✅ | 0.000085 |

> - Latency-aware 比 Jetson-only 快 **2.3×**
> - Carbon-aware 比 Ada-only 降低 **~33%** 排放

#### Batch Size = 8
| Strategy | Total E2E Latency (s) | Carbon Footprint (kgCO₂e) |
|---------|------------------------|----------------------------|
| All on Jetson | 609.0 | 0.000057 |
| All on Ada | 533.6 | 0.000084 |
| **Carbon-aware** | 552.4 | **0.000055** ✅ |
| **Latency-aware** | **266.8** ✅ | 0.000070 |

> - Latency-aware 比 Jetson-only 快 **2.3×**
> - 更高的吞吐量，但 Jetson 出现内存饱和问题

### 🔍 消融分析与关键观察
- **Batch Size 影响显著**：
  - 随着 batch size 增加，**TPOT 下降 → 吞吐提升**
  - 但 **TTFT 显著上升**，影响实时交互体验
  - **单位 prompt 碳成本下降**（能量摊销效应）
- **最佳平衡点是 batch=4**：
  - 在延迟、能效、稳定性之间取得最优折衷
- **硬件差异明显**：
  - Jetson：适合轻量级、低 token 数任务，**能效极高**
  - Ada 2000：更适合高复杂度、大批量、长序列任务，**准确率更高且稳定**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **边缘 LLM 推理必须兼顾性能与可持续性**：
   - 单纯依赖小型边缘模型或大型云端模型均非最优解。
   - **硬件感知 + prompt 复杂度感知** 是实现高效绿色推理的关键。

2. **合理的批处理（batching）可显著改善能效与吞吐**：
   - **batch=4 是推荐配置**，在延迟、碳排放和准确性之间达到最佳平衡。
   - batch=8 虽然吞吐最大，但在 8GB GPU 上易引发内存饱和与精度下降。

3. **两种策略各有优势，可根据场景选择**：
   - **Carbon-aware** 可减少高达 35% 的碳排放，适合环保优先场景。
   - **Latency-aware** 可提速 2–3×，适合实时性要求高的应用。

4. **benchmark-driven 决策至关重要**：
   - 不同 prompt 类型（如数学推理 vs 事实查找）在不同设备上的表现差异巨大，需通过实测数据驱动调度决策。

### ⚠️ 方法的局限性
- 实验仅基于两个固定型号的边缘设备（Jetson 和 Ada），泛化能力有待验证。
- 当前策略依赖预定义的 benchmark 表，缺乏在线自适应调整能力。
- 未考虑网络波动、温度变化等真实边缘环境因素对能效的影响。
- prompt 复杂度评分依赖外部 judge model，可能引入偏差。

### 🔮 未来工作方向
- **扩展至更大规模边缘集群**，研究可扩展性与动态负载均衡。
- 开发 **adaptive routing 机制**，能够根据运行时状态（如 GPU memory usage, temperature）动态调整策略。
- 引入 **multi-objective optimization framework**，同时优化 latency、carbon、cost 和 accuracy。
- 探索 **unseen prompt 的泛化能力预测模型**，提升调度系统的鲁棒性。

---

> 💡 **一句话总结**：  
> 该论文通过实证驱动的方法，在异构边缘集群上实现了 **latency 与 carbon 的有效权衡**，证明了 **sustainability-aware LLM inference** 的可行性与优越性，为绿色 AI 推理提供了实用路径。

</details>

---

### 10. [Efficient Generative Transformer Operators For Million-Point PDEs](https://arxiv.org/abs/2512.04974)

**Authors**: Armand Kassa\"i Koupa\"i, Lise Le Boudec, Patrick Gallinari  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.04974v1  

#### Abstract
We introduce ECHO, a transformer-operator framework for generating million-point PDE trajectories. While existing neural operators (NOs) have shown promise for solving partial differential equations, they remain limited in practice due to poor scalability on dense grids, error accumulation during dy...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Generative Transformer Operators For Million-Point PDEs

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Neural Operators (NOs)** 在求解大规模偏微分方程（PDE）时面临三大挑战：
- **可扩展性差**：在高分辨率密集网格上计算和内存开销巨大，难以处理百万级点（million-point）问题；
- **误差累积**：自回归（autoregressive）时间步进策略在长时域预测中导致误差持续积累；
- **任务特异性设计**：多数模型为特定任务（如前向预测）定制，缺乏通用性和灵活性。

### 提出的新方法：ECHO
本文提出 **ECHO**（Efficient Generative Transformer Operator），一种基于 **Transformer-Operator** 的生成式框架，用于高效生成百万点 PDE 轨迹。其核心创新包括以下三点：

#### （i）层次化卷积编码-解码架构（Hierarchical Convolutional Encode-Decode）
- 采用 **深度、迭代式的时空压缩**，在空间和时间维度上同时进行多阶段下采样；
- 将原始百万点轨迹压缩至紧凑的潜在表示（latent space），实现高达 **100倍的时空压缩比**，同时保持物理信息的保真度；
- 支持不规则网格输入，并通过连续卷积（continuous convolution）实现任意位置查询。

#### （ii）从稀疏输入生成高分辨率解（High-Resolution Generation from Sparse Inputs）
- 设计了一种 **两阶段训练策略**：
  1. **低分辨率轨迹训练**：先在子采样轨迹上训练编码器，降低内存压力；
  2. **高分辨率帧精调**：再对单帧高分辨率数据进行精调，提升细节重建能力；
- 该策略有效缓解了高分辨率全轨迹训练的内存瓶颈。

#### （iii）生成式建模范式（Generative Modeling Paradigm）
- 弃用传统的逐帧自回归预测，转而采用 **Flow Matching** 目标训练一个 **DiT-based 生成模型**，直接生成完整的轨迹片段；
- 支持 **条件与无条件生成**，能够统一处理前向预测、逆问题、插值等多种任务；
- 显著缓解了长时域预测中的误差漂移问题。

### 相比现有方法的优势
| 维度 | ECHO | 传统方法（如 FNO、BCAT、AROMA） |
|------|------|-------------------------------|
| **压缩能力** | ✅ 高效时空压缩（最高 ×176） | ❌ 多数在物理空间操作，无压缩或仅空间压缩 |
| **长时预测稳定性** | ✅ 全轨迹生成，误差积累少 | ❌ 自回归导致误差随时间指数增长 |
| **任务通用性** | ✅ 支持前向、逆向、插值、超分辨率等多任务零样本推理 | ❌ 通常需为每个任务单独训练 |
| **网格适应性** | ✅ 支持不规则、任意分辨率网格 | ⚠️ 多数限于规则网格 |
| **生成能力** | ✅ 可生成多样化物理合理轨迹 | ❌ 多为确定性输出 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖多个公开基准，覆盖二维与三维、规则与不规则网格、多种物理系统：

| 数据集 | 类型 | 点数 | 特点 |
|--------|------|-------|------|
| **Vorticity** | 2D 流体涡量 | 20M | 高频湍流、参数化粘度、1024×1024 密集网格 |
| **Gray-Scott** | 2D 反应扩散 | 1.3M | 复杂模式演化 |
| **Rayleigh-Benard** | 2D 对流 | 3.9M | 参数变化大 |
| **Shallow-Water** | 2D 球面浅水 | 2.6M | 球形网格、大气模拟 |
| **Eagle** | 2D 不规则流场 | 0.2M | 不规则网格、移动源 |
| **Cylinder Flow** | 2D 圆柱绕流 | 0.3M | 不规则网格 |
| **MHD / TGC** | 3D 磁流体/湍流冷却 | ~30M | 三维复杂动力学 |

### 实验设置与评估指标
- **任务类型**：
  - 前向预测（Forward）
  - 逆向预测（Inverse）
  - 时间插值（Interpolation）
  - 超分辨率（Super-resolution）
  - 长时外推（Long-range forecasting）
- **评估指标**：
  - **Relative MSE / L2 Error**：主指标，越低越好；
  - **Latency**：生成整条轨迹的耗时；
  - **Compression Ratio**：潜在空间与原始空间大小之比；
  - **FPD (Fréchet Physics Distance)**：衡量生成轨迹分布与真实数据的相似性。

### 基线方法对比
| 类型 | 基线模型 |
|------|---------|
| **Deterministic** | FNO, Transolver++, BCAT, AViT |
| **Generative** | ENMA（自回归生成） |
| **Encoder Baselines** | GINO, CORAL, AROMA, CALM-PDE |

所有模型在相同硬件（NVIDIA H100 80GB）和训练协议下进行公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2 和 Table 5）

#### 在规则网格上的多任务性能（Relative MSE）

| Model | Rayleigh-Benard (Inv/For) | Gray-Scott (Inv/For) | Active Matter (Inv/For) |
|-------|---------------------------|------------------------|--------------------------|
| **FNO** | 2.47e+3 / 4.23e-1 | — / — | 1.87e0 / 1.52e0 |
| **BCAT** | 1.91e-1 / 1.06e-1 | 2.19e-1 / 8.82e-2 | 1.95e-1 / 2.18e-1 |
| **ENMA (Gen.)** | 1.71e-1 / 9.87e-2 | 1.08e-1 / 5.44e-2 | 1.01e0 / 4.12e-1 |
| **ECHO (Det.)** | 2.53e-1 / 1.32e-1 | 8.36e-2 / 7.66e-2 | 1.18e-1 / 2.01e-1 |
| **ECHO (Gen.)** | **1.16e-1 / 9.28e-2** | **2.53e-2 / 5.12e-2** | **1.12e-1 / 1.32e-1** |

✅ **ECHO 在所有任务上均取得最佳性能**，尤其在 Gray-Scott 上显著优于其他方法。

#### 3D 动力学系统表现（TGC 数据集）

| Model | 1st Frame | 0.5T | Full Horizon (T) |
|-------|----------|------|------------------|
| **FNO** | 3.15e-2 | — | — |
| **AVIT** | 1.26e-1 | 6.22e-1 | 1.32e0 |
| **BCAT** | 1.24e-1 | 9.89e-1 | 1.62e0 |
| **ECHO** | **1.13e-1** | **5.55e-1** | **6.67e-1** |

✅ ECHO 在 3D 长时预测中误差增长更缓慢，表现出更强的稳定性。

### 与基线方法的对比结果

- **在不规则网格上全面领先**（Table 3）：
  - 在 Vorticity 上，ECHO 的重构误差仅为 **6.88e-2**，远低于第二名 ENMA（4.38e-1）；
  - 所有其他基线（GINO、CORAL、AROMA）在部分观测下性能严重下降，而 ECHO 仍保持稳健。

- **唯一能完成超分辨率任务的方法**（Table 4）：
  - 在 1024×1024 Vorticity 网格上进行超分辨率预测时，**所有基线均 Out-of-Memory (OOM)**；
  - **只有 ECHO 成功运行并达到 3.88e-1 的 Relative MSE**。

- **生成质量更高**（Table 16）：
  - **FPD = 1.12e-3**，显著低于 AROMA（1.55e-2）、CALM-PDE（1.02e-1）等；
  - 表明 ECHO 生成的轨迹在物理统计特性上最接近真实数据。

### 消融实验结果（Ablation Studies）

#### （1）层次化压缩的重要性（Table 19）
| 层次数（Levels） | 0 | 1 | 2 | 3 |
|------------------|-----|-----|------|-------|
| RMSE | 3.94e-1 | 3.64e-1 | 1.09e-1 | **2.87e-2** |

➡️ 深度层次化压缩显著提升重建精度，验证了渐进式下采样的有效性。

#### （2）精调阶段的作用（Table 20）
| 是否启用精调 | w/o refinement | w/refinement |
|--------------|----------------|---------------|
| Relative L2 | 2.24e-1 | **6.88e-2** |

➡️ 高分辨率帧精调使误差降低超过 **3倍**，对细节恢复至关重要。

#### （3）生成过程鲁棒性（Table 21）
- 不同 ODE 求解器（Euler, Midpoint, RK4, DOPRI5）和不同积分步数（2~25）下，性能差异极小（<1e-3）；
- 表明 Flow Matching 设计具有良好的数值稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **层次化时空压缩是处理百万点 PDE 的关键**：相比一次性压缩，逐步压缩能更好保留细粒度物理结构。
2. **生成式建模优于确定性与自回归方法**：通过一次性生成完整轨迹，显著抑制了长时预测中的误差漂移。
3. **统一框架支持多任务零样本推理**：无需重新训练即可处理前向、逆向、插值、超分辨率等任务。
4. **ECHO 是目前唯一能在极端规模（如 1024×1024）上成功运行的神经算子模型**，展现了前所未有的可扩展性。

### 方法的局限性
- **依赖高质量训练数据**：在极端稀疏输入（如 10% 观测）下性能仍有下降（见 Table 18）；
- **生成速度虽快但仍非实时**：尽管延迟已优化至 ~0.1–0.2 秒/轨迹，但在高频控制场景中可能仍不够；
- **未完全解决“幻觉”问题**：在极长外推（>4×训练长度）时，高频细节逐渐丢失（见图谱分析 Fig. 17）。

### 未来工作方向
- 探索 **few-shot adaptation** 到新物理参数的能力（文中已初步验证）；
- 结合 **物理约束** 进一步提升长期一致性；
- 扩展到 **多物理场耦合系统** 与 **真实工程场景**（如气候模拟、航空航天）；
- 开发更轻量化的版本以支持边缘部署。

---

> **项目主页**：[https://echo-pde.github.io/](https://echo-pde.github.io/)  
> **代码与数据将开源**，促进可复现研究。

</details>

---

### 11. [LangSAT: A Novel Framework Combining NLP and Reinforcement Learning for SAT Solving](https://arxiv.org/abs/2512.04374)

**Authors**: Muyu Pan, Matthew Walter, Dheeraj Kodakandla, Mahfuza Farooque  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.04374v1  

#### Abstract
Our work presents a novel reinforcement learning (RL) based framework to optimize heuristic selection within the conflict-driven clause learning (CDCL) process, improving the efficiency of Boolean satisfia- bility (SAT) solving. The proposed system, LangSAT, bridges the gap between natural language ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《LangSAT: A Novel Framework Combining NLP and Reinforcement Learning for SAT Solving》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 SAT 求解器要求输入为标准的 **Conjunctive Normal Form (CNF)** 形式，这使得非专业人士难以直接使用 SAT 技术解决现实世界中的逻辑推理问题。此外，现有的 CDCL 求解器依赖静态启发式（如 VSIDS），在面对结构多变、由自然语言生成的 CNF 实例时适应性较差。

LangSAT 针对以下两个关键瓶颈提出解决方案：
- **可访问性问题**：用户需具备形式逻辑知识才能构造 CNF 输入。
- **求解效率问题**：传统启发式难以应对 NLP 转换后带来的结构多样性与语义模糊性。

---

### 🚀 提出的新方法与创新思路

LangSAT 是一个端到端框架，融合 **Natural Language Processing (NLP)** 和 **Reinforcement Learning (RL)**，实现从自然语言描述到 SAT 可解形式的自动转换与高效求解。其核心由两部分组成：

#### （1）**Lang2Logic**  
将英文自然语言描述自动转化为等价的 CNF 表达式，包含三个阶段：
- **Natural Language → Logical Expression**：利用 **ChatGPT o1-mini API** 将句子解析为形式化逻辑表达式（如 `And(Not(P), Or(Q,R))`）。
- **Logical Expression → CNF Conversion**：通过 **Lark parser** 构建语法树，并借助 **SymPy** 完成向 CNF 的转换。
- **CNF Simplification**：使用 SymPy 的 `simplify_logic` 函数去除冗余子句，提升后续求解效率。

> ✅ 创新点：首次实现无需人工干预的“自然语言 → CNF”全自动流水线，支持长达 450 词的复杂段落输入。

#### （2）**SmartSAT**  
一种基于 **Reinforcement Learning** 的增强型 CDCL SAT 求解器，替代传统的静态启发式策略（如 VSIDS）进行变量选择决策。

- 使用 **Proximal Policy Optimization (PPO)** 算法训练 RL Agent。
- 观测空间（observation space）包含：
  - 当前变量赋值状态
  - 子句满足情况
  - 基于 **bipartite graph** 的 clause-variable 结构表示
  - 来自 **SATfeatPy** 的 48 维全局特征（源自 SATzilla）
- 动作空间：选择某个变量并赋予布尔值（共 40 个动作，对应 20 变量 × 2 值）
- 奖励机制：每满足一个子句 +1，未满足则 -1，最大奖励为 91（uf20-91 数据集）

> ✅ 创新点：首次将 RL 与图结构特征结合用于 CDCL 中的动态启发式学习，提升了对多样化 CNF 结构的泛化能力。

---

### 🔍 相比现有方法的优势

| 方面 | 传统方法 | LangSAT |
|------|--------|--------|
| 输入形式 | 必须是 CNF 或形式逻辑 | 支持自然语言输入 |
| 用户门槛 | 高（需懂逻辑建模） | 低（普通人可描述问题） |
| 决策启发式 | 静态（如 VSIDS） | 动态学习（RL 自适应调整） |
| 对 NLP 生成 CNF 的适应性 | 差（结构不规则易失效） | 强（通过 RL 学习结构模式） |
| 可扩展性 | 有限 | 易扩展至代码、对话等新输入源 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集

- 主要测试数据集：**uf20-91**（来自 SATLIB）
  - 包含 1000 个可满足（satisfiable）的随机 3-SAT 实例
  - 每个实例有 20 个变量、91 个子句
  - 标准 DIMACS-CNF 格式
- 数据划分：
  - 训练集：800 个文件
  - 测试集：200 个文件

> 注：虽然 Lang2Logic 处理的是自然语言，但 SmartSAT 的评估仍基于标准 CNF 实例以确保公平比较。

---

### ⚙️ 实验设置与评估指标

#### 实验流程
1. Lang2Logic 接收自然语言输入 → 输出简化后的 CNF
2. SmartSAT 接收该 CNF → 运行 RL 增强的 CDCL 求解
3. 记录求解时间、是否成功判定 SAT/UNSAT

#### 评估指标
- **总求解时间（Total Solving Time）**：主要性能指标
- **中位数求解时间（Median Solving Time）**
- **优于基线的比例**：SmartSAT 在多少比例的问题上快于基线
- **稳定性分析**：观察运行时间分布的离散程度（是否有较多异常值）

#### 基线方法对比
- **Baseline CDCL Solver**：采用经典 **VSIDS 启发式** 的传统 CDCL 求解器
- 对比条件完全一致：
  - 相同硬件环境
  - 相同参数配置
  - 相同测试集（uf20-91 测试子集）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 指标 | SmartSAT | Baseline CDCL (VSIDS) |
|------|----------|------------------------|
| 中位数求解时间 | **1.02 秒** | **1.02 秒** |
| 平均求解时间范围 | 1.01–1.05 秒 | 1.01–1.05 秒 |
| 更快的问题占比 | **约 53%** | — |
| 时间波动性 | 较小（更稳定） | 存在更多高延迟 outlier |

> 图 4 显示 SmartSAT 的运行时间分布更加集中，表明其决策更具一致性。

---

### 🔁 与基线方法的对比结果

- **性能持平但略有优势**：
  - 中位时间相同（1.02s），但 SmartSAT 在 **53% 的测试实例上更快**
  - 特别是在某些结构复杂的实例中表现更优
- **更强的鲁棒性**：
  - VSIDS 在部分问题上出现显著延迟（可能因启发式陷入局部低效路径）
  - SmartSAT 因 RL 学习了全局结构特征，能更好规避低效分支

> “Although both models attained nearly identical median times, SmartSAT performed more consistently across a broader range of CNF structures.”

---

### ❌ 消融实验（Ablation Study）

原文未明确提供系统的消融实验（如移除图结构、去掉全局特征等），但在讨论中提及以下关键设计的重要性：

- **Graph Representation**：clause-variable bipartite graph 提供结构感知能力，是 RL 学习的基础。
- **Global Features (from SATfeatPy)**：继承自 SATzilla 的 48 维特征显著增强了模型对问题整体特性的理解。
- **PPO 策略更新机制**：保证训练过程稳定，避免策略崩溃。

> 虽无定量消融，但从架构设计看，这些模块被认为是 SmartSAT 成功的关键。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Lang2Logic 成功实现了自然语言到 CNF 的有效转化**  
   - 支持长文本（up to 450 words）、多句子逻辑推理
   - 输出 CNF 正确且可用于求解，验证了 NLP 在形式化逻辑转换中的可行性

2. **SmartSAT 展现出媲美甚至略优于传统 CDCL 的求解效率**  
   - 在 uf20-91 上达到与 VSIDS 相当的中位求解时间
   - 在超过一半的实例上更快，且结果更稳定
   - 表明 RL 可作为 CDCL 中有效的动态启发式替代方案

3. **RL 特别适合处理由 NLP 生成的非规范 CNF 实例**  
   - 因语言歧义导致的结构多样性可通过 RL 的自适应决策机制更好地应对
   - 相比固定启发式更具泛化潜力

---

### ⚠️ 方法的局限性

1. **Lang2Logic 依赖外部 LLM（ChatGPT API）**
   - 存在成本、延迟、隐私等问题
   - 性能在长文本或复杂语法下可能出现不稳定

2. **当前评估仍基于人工合成 CNF（uf20-91）**
   - 尚未完整闭环测试“自然语言 → Lang2Logic → SmartSAT”的全流程性能
   - 缺乏真实应用场景下的端到端评测（如软件验证、日常推理任务）

3. **SmartSAT 训练开销较大**
   - 单 epoch 约 100,000 步训练，计算资源需求较高
   - 实际部署可能存在挑战

4. **尚未验证在 UNSAT 实例上的表现**
   - 所有测试均为 satisfiable 实例，对证明能力（proof generation）缺乏评估

---

### 🔮 未来工作方向

1. **扩大输入多样性**
   - 将输入扩展至 **machine code**（机器码）、程序片段等，进一步拓宽应用边界

2. **增强 SmartSAT 的特征提取能力**
   - 引入更多全局特征，提升对不同 SAT 问题类别的识别与优化能力

3. **跨数据集性能评估**
   - 在多个 SAT 问题类别（如 industrial, crafted, random）上测试 SmartSAT 的通用性

4. **构建端到端评估基准**
   - 设计包含自然语言描述 → 真实逻辑问题 → 标准答案的 benchmark，全面衡量 LangSAT 整体性能

5. **探索本地化 LLM 替代方案**
   - 使用开源小型 LLM 替代 ChatGPT API，降低依赖与成本

---

## 总结

LangSAT 是一项开创性的工作，首次将 **NLP** 与 **Reinforcement Learning** 深度整合进 SAT 求解流程，提出了一个真正意义上的“自然语言驱动”的自动化逻辑求解系统。它不仅提高了 SAT 技术的可用性和普及性，也为未来智能推理系统的发展提供了新的范式。

> **一句话总结**：LangSAT = 自然语言接口 + RL 增强求解器，让普通人也能“用说话的方式”调用强大的形式化推理引擎。

</details>

---

### 12. [EtCon: Edit-then-Consolidate for Reliable Knowledge Editing](https://arxiv.org/abs/2512.04753)

**Authors**: Ruilin Li, Yibin Wang, Wenhong Zhu, Chenglin Li, Jinghao Zhang, Chenliang Li, Junchi Yan, Jiaqi Wang  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.04753v1  

#### Abstract
Knowledge editing aims to update specific facts in large language models (LLMs) without full retraining. Prior efforts sought to tune the knowledge layers of LLMs, proving effective for making selective edits. However, a significant gap exists between their performance in controlled, teacher-forcing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：EtCon: Edit-then-Consolidate for Reliable Knowledge Editing**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前的知识编辑（Knowledge Editing）方法在受控环境（如 teacher-forcing 评估）中表现良好，但在真实世界场景（如自回归生成、终身学习）中效果显著下降。作者通过实证分析揭示了两个根本原因：
- **过拟合问题**：传统方法导致模型过度适应新事实，损害预训练能力（如推理、语言流畅性）。
- **缺乏知识巩固阶段**：新知识虽被参数化地写入模型权重，但未能有效整合到模型的推理行为中，导致“知识-行为失配”（knowledge-behavior misalignment），即模型知道新知识却无法在生成时正确调用。

### **提出了什么新方法或新思路**
作者提出 **Edit-then-Consolidate (EtCon)** 范式，将知识编辑分为两个阶段：
1. **编辑阶段（Edit Stage）**：采用 **Targeted Proximal Supervised Fine-Tuning (TPSFT)** 对选定的 FFN 层进行局部更新，结合信任区域约束（trust-region）防止策略漂移，缓解过拟合。
2. **巩固阶段（Consolidate Stage）**：引入 **Group Relative Policy Optimization (GRPO)**，通过轨迹级优化（trajectory-level optimization）和综合奖励信号（comprehensive reward），将编辑后的知识与模型的 CoT 推理策略对齐，确保新知识能在自回归生成中被稳定激活。

### **相比现有方法的优势**
- **更高的可靠性与泛化能力**：在真实评估下，EtCon 将编辑成功率提升 35%-50%，远超现有方法。
- **更强的局部性（Locality）**：仅修改特定 FFN 层，减少对无关知识的干扰。
- **保留预训练能力**：通过 TPSFT 和 GRPO 的协同设计，避免模型崩溃或通用能力退化。
- **适用于终身学习场景**：支持数千次连续编辑而性能缓慢下降，具备实际部署潜力。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **ZsRE**：零样本关系抽取数据集，用于测试事实更新。
- **COUNTERFACT**：反事实编辑基准，评估模型对错误前提的纠正能力。
- **QAEdit**：问答形式的知识编辑数据集，强调推理一致性。
- **通用能力评估集**：C-Eval、CoQA、DROP、SQuAD 2.0、LogiQA，用于验证编辑后模型通用性能是否受损。

### **实验设置和评估指标**
- **模型**：Llama-3-8B-Instruct 和 Qwen2.5-7B-Instruct。
- **评估方式**：采用 **real-world evaluation** 设置：
  - 输入包含需多步推理的问题，并提示 “Please reason step by step, then answer”。
  - 输出为完整自回归生成结果（非截断）。
  - 使用 **LLM-as-a-judge** 协议（GPT-4.1）进行二元评分（Correct/Incorrect）。
- **三大核心指标**：
  - **Reliability (Reli.)**：编辑成功比例（P(new fact) > P(old fact)）。
  - **Generalization (Gen.)**：在改写问题上的泛化能力。
  - **Locality (Loc.)**：对未编辑相关问题的回答稳定性。

### **基线方法对比**
- **Parametric In-Place Editing**：
  - FT-M、MEMIT、ALPHAEDIT、MMKE
- **External-Assisted Editing**：
  - WISE（memory-based SOTA）
- 所有方法均在相同框架（EasyEdit）下复现，保证公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
在 **Qwen2.5-7B-Instruct** 上的平均表现（ZsRE & QAEdit）：
| 方法 | Reliability | Generalization | Locality |
|------|------------|----------------|----------|
| FT-M | 5.6% | 5.5% | 23.1% |
| WISE | 4.5% | 3.3% | 19.1% |
| ALPHAEDIT | 15.9% | 11.5% | 6.8% |
| **EtCon (Ours)** | **69.4%** | **60.8%** | **24.4%** |

在 **Llama-3-8B-Instruct** 上的表现：
| 方法 | Reliability (ZsRE) | Generalization (ZsRE) | Locality |
|------|--------------------|------------------------|---------|
| FT-M | 16.6% | 15.5% | 29.3% |
| ALPHAEDIT | 18.7% | 14.0% | 6.3% |
| **EtCon (Ours)** | **73.5%** | **63.1%** | **30.2%** |

> ✅ **提升幅度达 40%-50%**，且 Locality 更优。

### **与基线方法的对比结果**
- **MEMIT 和 ALPHAEDIT 在终身编辑中崩溃**：在 Qwen 上接近 0% 性能，表明其不适用于连续编辑。
- **FT-M 和 WISE 表现稳定但性能极低**：可靠性不足 30%，远低于 EtCon。
- **EtCon 显著优于所有基线**，尤其在 Generalization 上体现其真正“理解”而非机械记忆。

### **消融实验结果**
在 **COUNTERFACT + Llama-3-8B-Instruct** 上的消融研究（Table 4）：
| 阶段 | 方法 | Reli. | Gen. | Loc. |
|------|------|-------|------|------|
| Base | – | 0.6% | 0.8% | 31.8% |
| Edit | w/ SFT | 1.4% | 0.3% | 30.7% |
| Edit | w/ TPSFT | 3.3% | 1.8% | 30.2% |
| Consolidate | w/o R_cleanliness | 56.1% | 22.4% | 24.7% |
| Consolidate | w/o R_consistency | 51.6% | 27.2% | 25.1% |
| Complete | EtCon | **67.1%** | **53.4%** | **24.2%** |

#### **关键发现**：
- **TPSFT 比标准 SFT 更有效保护通用能力**。
- **去除 R_cleanliness** 导致“奖励黑客”（reward hacking）：模型输出冗余内容（如同时列出新旧答案）以最大化得分。
- **去除 R_consistency** 引发逻辑矛盾（如先说正确答案再自我否定），可靠性大幅下降。
- 完整 EtCon 实现最佳平衡，证明各组件不可或缺。

---

## **4. 关键结论和发现**

### **主要发现**
1. **知识编辑失败的根本原因是缺少“巩固阶段”**：仅参数更新不足以让模型在推理中稳定使用新知识。
2. **Edit-then-Consolidate 是必要范式**：编辑（parametric update）与巩固（behavioral alignment）应解耦处理。
3. **TPSFT + GRPO 组合显著提升真实场景下的编辑可靠性**，实现高成功率、强泛化、良好局部性。
4. **现有方法在终身编辑中不可靠**：多数方法因累积误差导致模型崩溃，而 EtCon 可支持多达 3000 次连续编辑。

### **方法的局限性**
- **计算开销较高**：TPSFT 编辑时间约 6 秒/实例，高于 FT-M（0.61s），但与其他参数编辑方法相当。
- **依赖 CoT 数据质量**：TPSFT 使用原始模型生成的 CoT 作为监督信号，若初始推理错误可能引入噪声。
- **层选择敏感**：实验显示编辑浅层（shallow layers）效果更优，深层编辑易引发“认知冲突”与奖励黑客。

### **未来工作方向**
- 探索更高效的 consolidation 算法，降低训练成本。
- 自动识别最优编辑层或神经元，提升自动化程度。
- 将 EtCon 扩展至多模态模型或更复杂任务（如规划、决策）。
- 结合 retrieval-augmented 方法，构建可长期自更新的 LLM 架构。

---

> 🔚 **总结**：  
> **EtCon** 提出了一种全新的两阶段知识编辑范式，首次系统性解决“知识-行为失配”问题。其实验充分验证了 **consolidation 阶段的必要性**，并展示了在真实场景下卓越的编辑性能，为可靠、可持续的知识更新提供了重要实践路径。

</details>

---

### 13. [SEAL: Self-Evolving Agentic Learning for Conversational Question Answering over Knowledge Graphs](https://arxiv.org/abs/2512.04868)

**Authors**: Hao Wang, Jialun Zhong, Changcheng Wang, Zhujun Nie, Zheng Li, Shunyu Yao, Yanzeng Li, Xinchi Li  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.04868v1  

#### Abstract
Knowledge-based conversational question answering (KBCQA) confronts persistent challenges in resolving coreference, modeling contextual dependencies, and executing complex logical reasoning. Existing approaches, whether end-to-end semantic parsing or stepwise agent-based reasoning, often suffer from...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SEAL: Self-Evolving Agentic Learning for Conversational Question Answering over Knowledge Graphs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**基于知识图谱的对话问答（KBCQA）**中的三大挑战：
- **指代消解（coreference resolution）**：多轮对话中用户使用代词或省略表达时难以准确理解其指向。
- **上下文依赖建模（contextual dependencies）**：需要有效利用历史对话信息进行推理。
- **复杂逻辑推理（complex logical reasoning）**：如多跳推理、比较、聚合等任务，传统方法在生成结构化查询（如SPARQL）时常出现语法错误或语义偏差。

此外，现有方法存在两个关键瓶颈：
- **端到端语义解析（end-to-end semantic parsing）**容易产生结构不准确的逻辑形式；
- **逐步代理式推理（stepwise agent-based reasoning）**计算开销大，尤其在大规模知识图谱上效率低下。

---

### 提出的新方法与创新思路
作者提出 **SEAL（Self-Evolving Agentic Learning）**，一种基于**自演化代理学习**的两阶段语义解析框架，核心创新如下：

#### （1）引入“最小S-expression核心”（Minimal S-expression Core）
- 在第一阶段，由LLM提取一个简化的S-expression子结构，仅包含基本操作（如`JOIN`, `AND`, `R`等），捕捉问题的本质语义。
- 这种分解降低了生成完整逻辑形式的复杂度。

#### （2）代理校准模块（Agentic Calibration Module）
- 对生成的S-expression核心进行**语法纠错**和**实体/关系对齐**，确保其与底层知识图谱一致。
- 采用轻量级链接策略（lightweight linking），只保留语义最相似的单个候选实体/关系，避免组合爆炸。

#### （3）模板驱动的完成机制（Template-based Completion）
- 第二阶段通过**问题类型预测**选择合适的S-expression模板，并将校准后的核心填入占位符，构造可执行的完整逻辑表达式。
- 利用预定义模板提升结构一致性与生成效率。

#### （4）自演化机制（Self-Evolving Mechanism）
- 结合**局部记忆（local memory）**、**全局记忆（global memory）** 和 **反思模块（reflection module）**，实现无需显式重训练的持续学习能力。
- 成功的逻辑形式及其模式被写入全局记忆，供后续对话复用，形成闭环进化系统。

---

### 相比现有方法的优势
| 维度 | SEAL优势 |
|------|---------|
| **准确性** | 显著提高S-expression的结构准确率和执行成功率，尤其在复杂推理任务中表现优异 |
| **效率** | 分解式设计减少SPARQL查询数量，缓解“长尾高查询”问题，提升推理效率 |
| **适应性** | 自演化机制使模型能从对话历史和执行反馈中动态学习，适应新表达方式 |
| **可扩展性** | 不依赖标注数据，在零样本/少样本场景下仍具竞争力 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **SPICE** 数据集，这是一个基于Wikidata的知识图谱对话问答基准。
- 包含多轮自然语言问答序列及对应的SPARQL查询标注。
- 覆盖六类问题类型：`simple`, `verify`, `count`, `compare`, `compare_and_count`, `optimize`。
- 排除“clarification”子集（无对应SPARQL），共采样约3,791个样本用于测试。

---

### 实验设置与评估指标
#### 评估指标
- **macro-F1**：用于集合型答案（如实体集）的评价。
- **Accuracy（AC）**：用于布尔值或数值型答案的判断。

#### 基线方法对比
| 类型 | 基线方法 |
|------|--------|
| **监督方法** | BertSPGL, DCGGL |
| **非监督方法** | KB-Binder（强基线）、LLMGT（直接生成逻辑形式） |

#### 实现细节
- **LLM模型**：
  - 问句类型预测：Qwen2.5-32B-Instruct
  - 其他模块（核心生成、模板选择等）：DeepSeek-V3
- **链接策略**：top-1 vs top-k（k=3）
- **变体保留数**：保留1个或3个校准后候选

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 2）

| Question Type | SEAL (m-F1 / AC) | 最佳基线 (m-F1 / AC) | 提升幅度 |
|---------------|------------------|------------------------|----------|
| Logical Reasoning | **73.08** | 89.61 (LLMGT) | -16.53（但优于其他） |
| Quantitative Reasoning | **64.45** | 21.01 (KB-Binder) | +43.44 |
| Comparative Reasoning | **41.06** | 12.17 (KB-Binder) | +28.89 |
| Simple Q (Coref) | **71.53** | 85.73 (LLMGT) | -14.2（略低） |
| Verification (Boolean) | **85.97** | 91.89 (LLMGT) | -5.92 |
| Quantitative Reasoning (Count) | **70.12** | 58.33 (LLMGT) | +11.79 |
| **Overall AC** | **66.83** | 65.65 (LLMGT), 36.66 (KB-Binder) | +1.18 vs LLMGT, +30.17 vs KB-Binder |

> ✅ SEAL在多数复杂推理任务上显著超越非监督基线，在总体性能上接近最强监督模型，且无需标注数据。

---

### 与其他方法的关键对比
- 在**Comparative Reasoning**任务中，SEAL得分是KB-Binder的**3.4倍以上**（41.06 vs 12.17）。
- 在**Quantitative Reasoning**中，SEAL比KB-Binder高出近**50个百分点**。
- 尽管在简单问题上略逊于某些监督方法，但在涉及指代和省略的问题中仍保持稳健。

---

### 消融实验结果（Ablation Study）

| 模型变体 | F1 | AC | Overall Score |
|---------|-----|-----|----------------|
| **SEAL（完整）** | 66.44 | 59.37 | **64.08** |
| w/o core extraction | 41.34 | 35.39 | 39.36 |
| w/o entity candidate | 39.32 | 46.16 | 41.60 |
| w/o calibration | 61.78 | 56.21 | 59.93 |
| w/o local memory | 61.89 | 56.69 | 60.16 |

> 🔍 **核心发现**：
> - 移除**core extraction**导致性能下降最大（↓24.72），说明该模块是架构基石。
> - **calibration**和**local memory**虽影响较小，但仍显著提升鲁棒性。
> - 所有组件协同作用，验证了系统设计的整体有效性。

---

### 零样本与少样本场景表现（Table 5）

| 设置 | 方法 | F1 | AC | Overall |
|------|------|-----|-----|--------|
| Few-shot | KB-Binder | 32.61 | 36.82 | 34.02 |
| Few-shot | SEAL-base | **41.34** | 35.39 | **39.36** |
| Zero-shot | KB-Binder | 21.67 | 15.96 | 19.76 |
| Zero-shot | SEAL-self-evolving | **34.42** | **34.10** | **34.32** |

> 🚀 **关键结论**：SEAL的**自演化机制**使其在零样本条件下远超基线，证明其具备强大的泛化与自我优化能力。

---

## 4. 关键结论和发现

### 主要发现
1. **两阶段语义解析优于端到端生成**  
   将S-expression生成拆分为“核心提取 + 模板补全”，显著提升了结构准确性和解析成功率。

2. **代理校准有效解决LLM幻觉与链接歧义**  
   通过轻量级链接与语法修正，确保生成的逻辑形式既符合知识图谱约束，又能正确执行。

3. **自演化机制带来持续性能增长**  
   如Figure 4所示，随着对话轮次增加，SEAL的F1分数稳步上升，而消融版本性能停滞甚至下降，表明记忆与反思机制实现了真正的“自我进化”。

4. **模板并非限制，而是引导**  
   即便某些复杂结构不在模板库中（如三元OR嵌套），LLM也能自主合成合法表达（Appendix E），体现其组合泛化能力。

---

### 方法的局限性
1. **依赖预定义S-expression模板库**  
   虽然有一定泛化能力，但极端新颖的逻辑结构可能无法覆盖。

2. **多次调用LLM带来延迟风险**  
   多模块协作增加了推理链长度，可能影响实时响应速度。

3. **空结果误判问题**  
   校准过程以“非空结果”为筛选标准，可能导致合法但应为空的答案被错误排除。

4. **当前转换局限于特定KG（Wikidata）**  
   S-expression ↔ SPARQL 的转换规则需适配不同知识图谱结构。

---

### 未来工作方向
1. **推广S-expression到SPARQL的通用转换函数**，增强跨KG迁移能力。
2. 改进代理的语义理解能力，区分“合法空结果”与“错误查询”。
3. 引入专门训练任务，提升LLM对S-expression语法的掌握程度。
4. 使用更小规模模型处理子任务（如coreference resolution），降低整体计算成本。
5. 探索更高效的记忆更新机制，支持更大规模的历史知识存储与检索。

---

> 💡 **总结一句话**：  
> SEAL通过“**核心提取 + 代理校准 + 模板补全 + 自我演化**”的四重机制，在无需监督信号的前提下，实现了高效、准确、可持续进化的KBCQA系统，为构建真正智能的对话代理提供了新范式。

</details>

---

### 14. [Federated Learning for Terahertz Wireless Communication](https://arxiv.org/abs/2512.04984)

**Authors**: O. Tansel Baydas, Ozgur B. Akan  
**Category**: cs.DC  
**Published**: 2025-12-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.04984v1  

#### Abstract
The convergence of Terahertz (THz) communications and Federated Learning (FL) promises ultra-fast distributed learning, yet the impact of realistic wideband impairments on optimization dynamics remains theoretically uncharacterized. This paper bridges this gap by developing a multicarrier stochastic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Federated Learning for Terahertz Wireless Communication*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Terahertz (THz) 频段下的 Federated Learning (FL)** 系统中存在的理论空白展开研究。尽管已有工作将 FL 应用于 THz 通信系统的优化（如信道估计、资源分配），但**缺乏对 THz 物理层宽频带损伤如何影响 FL 学习动态（收敛性、偏差、方差）的系统性理论分析**。

具体而言，THz 频段存在以下关键物理层挑战：
- **Beam squint**（波束偏斜）：频率选择性导致不同子载波波束指向不一致。
- **Molecular absorption**（分子吸收）：特定频率出现“谱洞”（spectral holes）。
- **Pointing jitter**（指向抖动）：高方向性天线对微小角度偏差极为敏感。
- **Thermal noise**：宽带引入更多噪声。

这些因素在传统 FL 分析中常被忽略或简化为独立加性噪声，无法准确刻画其对模型聚合的影响。

---

### 提出的新方法与新思路

1. **建立了首个耦合 THz 物理层损伤与 FL 收敛性的多载波随机模型**  
   将本地 SGD 更新过程与 OFDM 系统中的频率选择性 THz 信道效应显式建模，涵盖 beam squint、分子吸收、jitter 和热噪声。

2. **揭示了“谐波均值瓶颈”（Harmonic Mean Bottleneck）现象**  
   在标准无偏聚合（如 FedAvg）下，**全局收敛误差下限由各子载波 SNR 的调和平均决定**。这意味着即使只有一个子载波因严重 beam squint 导致 SNR 极低（形成谱洞），也会主导整个系统的性能，使聚合更新失效。

3. **发现了根本性的带宽极限（Fundamental Bandwidth Limit）**  
   扩展带宽并非总是有益。当带宽超过某一临界点时，边缘子载波增益崩溃（gain collapse）和热噪声功率增加会显著提升梯度估计器的总体方差，反而**恶化 FL 收敛性能**。

4. **提出 SNR 加权聚合策略以突破谱洞限制**  
   为解决谐波均值瓶颈，作者提出一种基于 SNR 的加权聚合机制：
   $$
   \Delta^{(n)} = \sum_{i \in S_t} \alpha_i^{(n)} (\Delta_{i,t}^{(n)} + \text{error})
   $$
   其中权重 $\alpha_i^{(n)}$ 与有效噪声方差成反比。该方法通过抑制高噪声子载波/客户端的贡献来稳定方差，**以可控的目标偏置换取方差稳定性**。

---

### 相比现有方法的优势

| 方面 | 现有方法 | 本文方法 |
|------|--------|---------|
| **信道建模** | 理想衰落信道，独立噪声 | 显式建模 beam squint、分子吸收、jitter 等 THz 特有损伤 |
| **收敛分析** | 忽略频率选择性影响 | 揭示 SNR 调和平均主导误差下限 |
| **聚合机制** | 标准平均（FedAvg） | 提出 SNR 加权聚合，适应恶劣 THz 环境 |
| **设计指导** | 缺乏物理参数约束 | 推导出带宽、功率、量化等设计不等式 |

> ✅ **优势总结**：首次从理论上解释了为何直接套用传统 FL 在 THz 场景可能失败，并提供了可操作的设计原则和鲁棒聚合算法。

---

## 2. 核心实验方法和设置

### 数据集
- **MNIST** 手写数字图像分类任务。
- 客户端数量：$N = 10$。
- 模型架构：CNN（两个卷积层 + 两个全连接层）。

### 实验设置
- **通信协议**：OFDM，子载波数 $N_c = 128$，总带宽 $B \in \{1, 5, 10\}$ GHz，载频 $f_c = 300$ GHz。
- **天线配置**：基站采用 $N_{\text{ant}} = 64$ 元素的 ULA（Uniform Linear Array）。
- **本地训练**：每轮 $K=1$ 个 local epoch，batch size = 32，学习率 $\eta = 0.02$。
- **总通信轮次**：$T = 10$。
- **SNR 安全阈值**：低于 0.01 bits/Hz 视为 packet erasure。

### 评估指标
- **测试准确率（Test Accuracy）随通信轮次的变化曲线**
- 不同物理参数（发射功率、距离、beam squint 强度、jitter 方差等）下的收敛表现
- 是否启用 CSI 补偿、是否使用加权聚合的对比

### 基线方法对比
- **FedAvg**（标准联邦平均）
- **Uncompensated Reception**（无信道补偿接收）
- **Equal-Power Allocation**（未考虑增益均衡的功率分配）

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

| 实验场景 | FedAvg 性能 | 加权聚合性能 | 结论 |
|--------|------------|--------------|------|
| **低发射功率 (0.1 mW)** | 准确率 ≈ 9.8%（随机猜测） | —— | 功率不足导致持续丢包 |
| **正常功率 (10 mW)** | 准确率 ≈ 98% | —— | 可维持稳定链路 |
| **高 beam squint ($s=5.0$)** | 明显下降 | —— | 边缘子载波增益崩溃 |
| **大 jitter ($\sigma_{\text{jitter}}=0.4$)** | 准确率骤降至 ~10% | —— | “cliff effect”，极敏感 |
| **长距离 (100 m)** | 完全失效（≈9.8%） | —— | 路径损耗 + 分子吸收过强 |
| **大带宽 (10 GHz)** | 比 1 GHz 差约 10 dB SNR | —— | 热噪声上升，边缘子载波失效 |
| **严重信道压力 ($s=15.0$, $P=0.2W$)** | 失败（≈9.8%） | 成功（≈97.6%） | **加权聚合显著优于 FedAvg** |

---

### 消融实验结果

1. **信道补偿必要性（Fig. 3c）**
   - 无 CSI 补偿：准确率仅 ~14%
   - 启用估计 CSI 补偿：恢复至 ~98%
   → **证明必须进行信道增益补偿才能收敛**

2. **加权聚合有效性（Fig. 4c）**
   - 在极端 beam squint 条件下，FedAvg 完全失败
   - SNR 加权聚合仍能达到接近最优性能
   → **验证了所提聚合策略的鲁棒性**

3. **带宽扩展负面影响（Fig. 4b）**
   - 固定功率下，带宽从 1 GHz 增至 10 GHz，性能下降
   → **证实存在“带宽极限”，盲目扩带宽有害**

---

## 4. 关键结论和发现

### 主要发现

1. **谐波均值瓶颈是 THz-FL 的核心障碍**  
   标准聚合方式下，**单个谱洞即可拖垮整个系统**，因为误差下限取决于 SNR 的调和平均而非算术平均。

2. **带宽不是越大越好**  
   存在一个**根本性的带宽上限**：继续扩大带宽会引入过多热噪声并激活低增益边缘子载波，反而降低整体 SNR 效率。

3. **偏置是 THz 环境下的必要妥协**  
   严格坚持无偏聚合（如 FedAvg）在 THz 场景不可行。**引入可控偏置的加权聚合是实现稳定收敛的关键**。

4. **THz-FL 是高度范围受限的技术**  
   实验显示，在 100 米距离上信号已无法解码，表明 THz-FL 更适用于短距密集部署场景（如室内、车联网近距离协作）。

---

### 方法的局限性

- **下行链路未建模**：当前框架聚焦上行模型上传，未分析 beam squint 对全局模型广播的影响。
- **理想化假设**：假设 pilot 密度足够、压缩误差独立等，在实际系统中可能难以完全满足。
- **数值实验规模较小**：仅使用 MNIST 和小型 CNN，尚未在更大规模任务（如 CIFAR-10、ResNet）上验证泛化性。
- **未考虑非线性失真**：如功率放大器非线性等实际硬件损伤未纳入模型。

---

### 未来工作方向

1. **扩展至下行链路分析**：研究 beam squint 如何影响服务器向多个客户端广播全局模型的过程。
2. **探索 Hybrid Digital-Analog 方案**：例如利用 Over-the-Air Computation (AirComp) 实现模拟聚合，绕过子载波量化瓶颈。
3. **联合优化物理层与学习策略**：设计自适应带宽、功率、量化精度的跨层优化算法。
4. **实测平台验证**：在真实 THz 测试平台上部署 FL 系统，验证理论模型的准确性。

--- 

> 📌 **总结一句话**：  
> 本文首次建立了 THz 物理层损伤与 FL 收敛行为之间的理论桥梁，揭示了“谐波均值瓶颈”和“带宽极限”两大关键现象，并提出 SNR 加权聚合作为应对高 squint 环境的有效方案，为构建面向 6G 的无线智能网络提供了重要设计准则。

</details>

---

### 15. [Decoding Large Language Diffusion Models with Foreseeing Movement](https://arxiv.org/abs/2512.04135)

**Authors**: Yichuan Mo, Quan Chen, Mingjie Li, Zeming Wei, Yisen Wang  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.04135v1  

#### Abstract
Large Language Diffusion Models (LLDMs) benefit from a flexible decoding mechanism that enables parallelized inference and controllable generations over autoregressive models. Yet such flexibility introduces a critical challenge: inference performance becomes highly sensitive to the decoding order o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Decoding Large Language Diffusion Models with Foreseeing Movement

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言扩散模型（**LLDMs**）虽然具备并行推理和可控生成的优势，但其解码性能高度依赖于**token的解码顺序**。现有的启发式方法（如基于概率、边际或熵）仅考虑局部置信度（**local confidence**），忽略了对后续生成路径的长期影响，导致次优甚至错误的解码决策。

### 提出的新方法与新思路
作者提出了一种新的解码策略——**Foreseeing Decoding Method (FDM)**，其核心思想是将解码决策同时考虑两个因素：
- **Local Confidence**：模型在当前步骤预测某个 token 的不确定性（即传统启发式方法所用）。
- **Global Confidence**：该 token 对未来完整序列生成的影响（通过训练目标近似估计）。

FDM 采用两阶段搜索策略：
1. 首先基于 local confidence 缩小候选集；
2. 再结合 global confidence 在有限候选集中选择最优 token。

进一步地，作者提出了加速版本 **FDM-A**，通过分析 FDM 与简单启发式方法的一致性，发现在解码后期（上下文充足时）全局探索并非必要。因此 FDM-A 引入自适应机制，在“探索”、“平衡”和“加速”三个阶段动态切换策略：
- 探索阶段（低置信）：使用 FDM 进行深度搜索；
- 平衡阶段（中等置信）：混合探索与并行解码；
- 加速阶段（高置信）：仅用 local confidence 快速解码。

### 相比现有方法的优势
- **理论优势**：证明了 FDM 能够降低生成分布与真实分布之间的 KL 散度，优于仅依赖局部信息的方法。
- **性能优势**：显著提升准确率，尤其在复杂推理任务上。
- **效率优势**：FDM-A 实现了更优的**性能-效率权衡**，相比其他加速方法，在保持高精度的同时实现更快的解码速度。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖四个主流基准，涵盖多种能力维度：
- **GSM8K**：数学应用题求解（数学推理）
- **HumanEval**：代码生成（编程能力）
- **Countdown**：算术表达式构造（逻辑推理）
- **ARC**：多项选择问答（常识推理）

所有任务均在 **zero-shot** 设置下进行评估。

### 实验设置与评估指标
- **模型变体**：测试了四种 LLDM 架构：
  - LLaDA-8B-Instruct
  - LLaDA-1.5
  - LLaDA-MoE-7B-Instruct
  - MMaDA-MixCoT
- **解码长度**：固定为 256，采用 semi-autoregressive pipeline（block size=64）
- **评估指标**：
  - **Accuracy**：正确回答的比例
  - **Tokens Per Second (TPS)**：每秒生成 token 数量，衡量效率

### 基线方法对比
#### 主要对比的启发式方法：
- **Probability**：选择预测概率最高的 token
- **Margin**：选择最大边际概率（top1 - top2）
- **Entropy**：选择预测分布熵最小的位置

#### 加速方法对比（用于 FDM-A）：
- **EB (Entropy Bounded Sampler)**：基于熵阈值动态解码
- **WINO**：可撤销的宽进窄出解码策略

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 3）

| 方法 | ARC 准确率 (%) | TPS |
|------|----------------|-----|
| Random | 79.06 | 12.01 |
| Margin | 82.55 | 10.85 |
| **FDM-A (Ours)** | **86.30** | **38.20** |

> ✅ FDM-A 不仅达到最高准确率，还实现了超过 **3倍的速度提升**。

### 与其他方法全面对比（代表性结果）

#### 在 LLaDA 模型上的 GSM8K 表现：
| 方法 | Accuracy (%) | TPS |
|------|--------------|-----|
| Probability (T=256) | 81.20 | 11.51 |
| Margin (T=256) | 80.14 | 11.23 |
| **FDM (K=4)** | **82.34** | 4.61 |
| **FDM-A (Ours)** | **81.96** | **42.65** |

> ⚡ FDM-A 相比标准 FDM 仅损失 0.38% 准确率，但 TPS 提升 **5.15×**

#### 在 ARC 上的表现（LLaDA）：
| 方法 | Accuracy (%) | TPS |
|------|--------------|-----|
| Margin (T=256) | 82.55 | 10.85 |
| WINO | 79.44 | 34.17 |
| EB | 73.55 | 32.01 |
| **FDM-A (Ours)** | **86.30** | **38.20** |

> 🏆 FDM-A 同时取得**最高准确率和最高速度**，远超其他加速方法。

### 消融实验结果

#### （1）搜索宽度 $K$ 的影响（Figure 4 & 8）
- 增大 $K$ 可提升 FDM 性能，但存在收益递减甚至下降（因噪声放大）。
- FDM 在大 $K$ 下表现更好；FDM-A 在小 $K$ 下更具优势 → 二者互补。

#### （2）剪枝阈值 $\gamma$ 的影响（Figure 5）
- $\gamma$ 过小：引入过多低置信候选，干扰决策；
- $\gamma$ 过大：限制探索空间；
- 最佳值集中在 **0.5 左右**，平衡搜索广度与质量。

#### （3）阶段划分参数 $m_1$, $m_2$ 的影响（Figure 6–11）
- $m_1 = 0.8$、$m_2 = 0.7$ 时性能最佳；
- 太早进入加速模式会损害准确性；
- 太保守则无法有效提速。

---

## 4. 关键结论和发现

### 主要发现
1. **局部启发式不足**：仅依赖当前步的 local confidence 会导致忽略长期影响，限制 LLDM 的潜力。
2. **全局预见有效**：引入 global confidence（通过期望 log-prob 近似）能显著提升解码质量。
3. **FDM 是有效的 test-time scaling 方法**：随着搜索空间扩大（$K↑$），性能持续提升，无需额外训练。
4. **FDM-A 实现最优权衡**：通过自适应控制探索强度，在几乎不牺牲性能的前提下大幅提升速度（>3× TPS）。
5. **一致性分析指导设计**：早期解码需更多探索，后期可安全加速，这一观察支撑了 FDM-A 的三阶段设计。

### 方法的局限性
- **计算开销仍高于纯启发式方法**：尽管 FDM-A 已优化，但在资源极度受限场景可能仍不适用。
- **依赖 well-trained model 的输出分布质量**：若模型本身对 future token 的预测偏差较大，则 global confidence 的估计也会失真。
- **超参数敏感性**：$K$, $\gamma$, $m_1$, $m_2$ 需合理调参，跨任务迁移需验证。

### 未来工作方向
- 将 FDM 扩展到多模态扩散模型（如 LLaDA-V）。
- 结合 verifier 或 reward model 进一步优化解码路径。
- 探索更高效的 global confidence 估计方式（如蒸馏、缓存）。
- 研究如何将 FDM 与强化学习结合，实现端到端的解码策略优化。

---

> 🔚 **总结一句话**：  
> 本文提出的 **FDM** 和 **FDM-A** 为 LLDMs 提供了一个**原则性强、性能优越且高效可扩展**的解码框架，揭示了“预见未来”在扩散语言模型中的关键作用，有望成为下一代解码标准的重要参考。

</details>

---

### 16. [QoSDiff: An Implicit Topological Embedding Learning Framework Leveraging Denoising Diffusion and Adversarial Attention for Robust QoS Prediction](https://arxiv.org/abs/2512.04596)

**Authors**: Guanchen Du, Jianlong Xu, Wei Wei  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.04596v1  

#### Abstract
Accurate Quality of Service (QoS) prediction is fundamental to service computing, providing essential data-driven guidance for service selection and ensuring superior user experiences. However, prevalent approaches, particularly Graph Neural Networks (GNNs), heavily rely on constructing explicit use...

---

### 17. [On GRPO Collapse in Search-R1: The Lazy Likelihood-Displacement Death Spiral](https://arxiv.org/abs/2512.04220)

**Authors**: Wenlong Deng, Yushu Li, Boying Gong, Yi Ren, Christos Thrampoulidis, Xiaoxiao Li  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.04220v1  

#### Abstract
Tool-integrated (TI) reinforcement learning (RL) enables large language models (LLMs) to perform multi-step reasoning by interacting with external tools such as search engines and retrievers. Group Relative Policy Optimization (GRPO), exemplified by the recent Search-R1, offers fast convergence and ...

---

### 18. [BEP: A Binary Error Propagation Algorithm for Binary Neural Networks Training](https://arxiv.org/abs/2512.04189)

**Authors**: Luca Colombo, Fabrizio Pittorino, Daniele Zambon, Carlo Baldassi, Manuel Roveri, Cesare Alippi  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.04189v1  

#### Abstract
Binary Neural Networks (BNNs), which constrain both weights and activations to binary values, offer substantial reductions in computational complexity, memory footprint, and energy consumption. These advantages make them particularly well suited for deployment on resource-constrained devices. Howeve...

---

### 19. [GRASP: GRouped Activation Shared Parameterization for Parameter-Efficient Fine-Tuning and Robust Inference of Transformers](https://arxiv.org/abs/2512.04296)

**Authors**: Malyaban Bal, Abhronil Sengupta  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.04296v1  

#### Abstract
Parameter-efficient fine-tuning (PEFT) provides a scalable alternative to full-model adaptation by updating only a small subset of parameters in large pre-trained models. We introduce GRASP - GRouped Activation Shared Parameterization - a lightweight PEFT framework that partitions the D-dimensional ...

---

### 20. [Data-regularized Reinforcement Learning for Diffusion Models at Scale](https://arxiv.org/abs/2512.04332)

**Authors**: Haotian Ye, Kaiwen Zheng, Jiashu Xu, Puheng Li, Huayu Chen, Jiaqi Han, Sheng Liu, Qinsheng Zhang, Hanzi Mao, Zekun Hao, Prithvijit Chattopadhyay, Dinghao Yang, Liang Feng, Maosheng Liao, Junjie Bai, Ming-Yu Liu, James Zou, Stefano Ermon  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.04332v1  

#### Abstract
Aligning generative diffusion models with human preferences via reinforcement learning (RL) is critical yet challenging. Most existing algorithms are often vulnerable to reward hacking, such as quality degradation, over-stylization, or reduced diversity. Our analysis demonstrates that this can be at...

---

### 21. [Multi-Agent Reinforcement Learning for Intraday Operating Rooms Scheduling under Uncertainty](https://arxiv.org/abs/2512.04918)

**Authors**: Kailiang Liu, Ying Chen, Ralf Bornd\"orfer, Thorsten Koch  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.04918v1  

#### Abstract
Intraday surgical scheduling is a multi-objective decision problem under uncertainty-balancing elective throughput, urgent and emergency demand, delays, sequence-dependent setups, and overtime. We formulate the problem as a cooperative Markov game and propose a multi-agent reinforcement learning (MA...

---

### 22. [Multi-Agent Reinforcement Learning with Communication-Constrained Priors](https://arxiv.org/abs/2512.03528)

**Authors**: Guang Yang, Tianpei Yang, Jingwen Qiao, Yanqing Wu, Jing Huo, Xingguo Chen, Yang Gao  
**Category**: cs.AI  
**Published**: 2025-12-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.03528v1  

#### Abstract
Communication is one of the effective means to improve the learning of cooperative policy in multi-agent systems. However, in most real-world scenarios, lossy communication is a prevalent issue. Existing multi-agent reinforcement learning with communication, due to their limited scalability and robu...

---

### 23. [Nex-N1: Agentic Models Trained via a Unified Ecosystem for Large-Scale Environment Construction](https://arxiv.org/abs/2512.04987)

**Authors**: AGI Team, Yuxuan Cai, Lu Chen, Qiaoling Chen, Yuyang Ding, Liwen Fan, Wenjie Fu, Yufei Gao, Honglin Guo, Pinxue Guo, Zhenhua Han, Zhengfu He, Hanglei Hu, Kai Hu, Shengjia Hua, Tianyu Huai, Baodai Huang, Li Ji, Zhen Jiang, Zhikai Lei, Bufan Li, Jiahang Lin, Lizhi Lin, Jinxiu Liu, Shichun Liu, Ziming Liu, Yuchen Ni, Pengfang Qian, Yujiong Shen, Qingyun Shi, Wentao Shu, Peng Sun, Yiran Suo, Tian Tang, Boyu Tian, Guoteng Wang, Junzhe Wang, Peixin Wang, Zhiheng Xi, Hang Yan, Jie Yang, Zhixiong Yang, Tianchu Yao, Guangze Ye, Qianxi Yu, Shuo Zhang, Xinyue Zhang, Yiqi Zhang, Jiarong Zhao, Miao Zheng, Rui Zheng, Enyu Zhou, Jiazheng Zhou, Maosen Zhou, Yuhao Zhou, Tao Gui, Yining Zheng, Xinchi Chen, Jie Zhou, Siyuan Feng, Qin Chen, Liang He, Qi Zhang, Xuanjing Huang, Xipeng Qiu  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.04987v1  

#### Abstract
The evolution of Large Language Models (LLMs) from passive responders to autonomous agents necessitates a fundamental shift in learning paradigms -- from static imitation to incentive-driven decision making. However, this transition is significantly impeded by the lack of scalable infrastructure cap...

---

### 24. [Formal Specification for Fast ACS: Low-Latency File-Based Ordered Message Delivery at Scale](https://arxiv.org/abs/2512.04096)

**Authors**: Sushant Kumar Gupta, Anil Raghunath Iyer, Chang Yu, Neel Bagora, Olivier Pomerleau, Vivek Kumar, Prunthaban Kanthakumar  
**Category**: cs.DC  
**Published**: 2025-12-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.04096v1  

#### Abstract
Low-latency message delivery is crucial for real-time systems. Data originating from a producer must be delivered to consumers, potentially distributed in clusters across metropolitan and continental boundaries. With the growing scale of computing, there can be several thousand consumers of the data...

---

### 25. [David vs. Goliath: Can Small Models Win Big with Agentic AI in Hardware Design?](https://arxiv.org/abs/2512.05073)

**Authors**: Shashwat Shankar, Subhranshu Pandey, Innocent Dengkhw Mochahari, Bhabesh Mali, Animesh Basak Chowdhury, Sukanta Bhattacharjee, Chandan Karfa  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.05073v1  

#### Abstract
Large Language Model(LLM) inference demands massive compute and energy, making domain-specific tasks expensive and unsustainable. As foundation models keep scaling, we ask: Is bigger always better for hardware design? Our work tests this by evaluating Small Language Models coupled with a curated age...

---

### 26. [MASE: Interpretable NLP Models via Model-Agnostic Saliency Estimation](https://arxiv.org/abs/2512.04386)

**Authors**: Zhou Yang, Shunyan Luo, Jiazhen Zhu, Fang Jin  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.04386v1  

#### Abstract
Deep neural networks (DNNs) have made significant strides in Natural Language Processing (NLP), yet their interpretability remains elusive, particularly when evaluating their intricate decision-making processes. Traditional methods often rely on post-hoc interpretations, such as saliency maps or fea...

---

### 27. [EvoEdit: Lifelong Free-Text Knowledge Editing through Latent Perturbation Augmentation and Knowledge-driven Parameter Fusion](https://arxiv.org/abs/2512.04545)

**Authors**: Pengfei Cao, Zeao Ji, Daojian Zeng, Jun Zhao, Kang Liu  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.04545v1  

#### Abstract
Adjusting the outdated knowledge of large language models (LLMs) after deployment remains a major challenge. This difficulty has spurred the development of knowledge editing, which seeks to accurately and efficiently modify a model's internal (parametric) knowledge without retraining it from scratch...

---

### 28. [AdmTree: Compressing Lengthy Context with Adaptive Semantic Trees](https://arxiv.org/abs/2512.04550)

**Authors**: Yangning Li, Shaoshen Chen, Yinghui Li, Yankai Chen, Hai-Tao Zheng, Hui Wang, Wenhao Jiang, Philip S. Yu  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.04550v1  

#### Abstract
The quadratic complexity of self-attention constrains Large Language Models (LLMs) in processing long contexts, a capability essential for many advanced applications. Context compression aims to alleviate this computational bottleneck while retaining critical semantic information. However, existing ...

---

### 29. [Challenging the Abilities of Large Language Models in Italian: a Community Initiative](https://arxiv.org/abs/2512.04759)

**Authors**: Malvina Nissim, Danilo Croce, Viviana Patti, Pierpaolo Basile, Giuseppe Attanasio, Elio Musacchio, Matteo Rinaldi, Federico Borazio, Maria Francis, Jacopo Gili, Daniel Scalena, Bego\~na Altuna, Ekhi Azurmendi, Valerio Basile, Luisa Bentivogli, Arianna Bisazza, Marianna Bolognesi, Dominique Brunato, Tommaso Caselli, Silvia Casola, Maria Cassese, Mauro Cettolo, Claudia Collacciani, Leonardo De Cosmo, Maria Pia Di Buono, Andrea Esuli, Julen Etxaniz, Chiara Ferrando, Alessia Fidelangeli, Simona Frenda, Achille Fusco, Marco Gaido, Andrea Galassi, Federico Galli, Luca Giordano, Mattia Goffetti, Itziar Gonzalez-Dios, Lorenzo Gregori, Giulia Grundler, Sandro Iannaccone, Chunyang Jiang, Moreno La Quatra, Francesca Lagioia, Soda Marem Lo, Marco Madeddu, Bernardo Magnini, Raffaele Manna, Fabio Mercorio, Paola Merlo, Arianna Muti, Vivi Nastase, Matteo Negri, Dario Onorati, Elena Palmieri, Sara Papi, Lucia Passaro, Giulia Pensa, Andrea Piergentili, Daniele Potert\`i, Giovanni Puccetti, Federico Ranaldi, Leonardo Ranaldi, Andrea Amelio Ravelli, Martina Rosola, Elena Sofia Ruzzetti, Giuseppe Samo, Andrea Santilli, Piera Santin, Gabriele Sarti, Giovanni Sartor, Beatrice Savoldi, Antonio Serino, Andrea Seveso, Lucia Siciliani, Paolo Torroni, Rossella Varvara, Andrea Zaninello, Asya Zanollo, Fabio Massimo Zanzotto, Kamyar Zeinalipour, Andrea Zugarini  
**Category**: cs.CL  
**Published**: 2025-12-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.04759v1  

#### Abstract
The rapid progress of Large Language Models (LLMs) has transformed natural language processing and broadened its impact across research and society. Yet, systematic evaluation of these models, especially for languages beyond English, remains limited. "Challenging the Abilities of LAnguage Models in ...

---

### 30. [The Initialization Determines Whether In-Context Learning Is Gradient Descent](https://arxiv.org/abs/2512.04268)

**Authors**: Shifeng Xie, Rui Yuan, Simone Rossi, Thomas Hannagan  
**Category**: cs.LG  
**Published**: 2025-12-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.04268v1  

#### Abstract
In-context learning (ICL) in large language models (LLMs) is a striking phenomenon, yet its underlying mechanisms remain only partially understood. Previous work connects linear self-attention (LSA) to gradient descent (GD), this connection has primarily been established under simplified conditions ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
