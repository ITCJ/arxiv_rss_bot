# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-08 05:57:54 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Implicit Graph, Explicit Retrieval: Towards Efficient and Interpretable Long-horizon Memory for Large Language Models](https://arxiv.org/abs/2601.03417)

**Authors**: Xin Zhang, Kailai Yang, Hao Li, Chenyue Li, Qiyu Wei, Sophia Ananiadou  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.03417v1  

#### Abstract
Long-horizon applications increasingly require large language models (LLMs) to answer queries when relevant evidence is sparse and dispersed across very long contexts. Existing memory systems largely follow two paradigms: explicit structured memories offer interpretability but often become brittle u...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Implicit Graph, Explicit Retrieval: Towards Efficient and Interpretable Long-horizon Memory for Large Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
在长上下文（long-horizon）场景中，大型语言模型（LLMs）需要从分散且稀疏的证据中进行推理。现有的记忆系统存在两大范式：
- **显式结构化记忆**（Explicit Structured Memory）：如图结构记忆，具有良好的可解释性，但在长而嘈杂的上下文中容易因结构构建错误和检索不稳定而导致性能下降。
- **隐式潜在记忆**（Latent Memory）：将信息存储在连续的潜在空间中，效率高且稳定，但缺乏可解释性，难以追踪推理过程。

本文指出，在长上下文问答任务中，显式方法往往表现不佳甚至崩溃，而隐式方法虽稳定却“黑箱”，限制了其在需要透明决策的应用中的使用。

### **提出的新方法：LATENTGRAPHMEM**
作者提出了 **LATENTGRAPHMEM**，一种结合了两种范式优势的记忆框架：
- **隐式图存储**（Implicit Graph Storage）：在潜在空间中构建和维护一个图结构记忆，确保高效性和稳定性。
- **显式子图检索**（Explicit Subgraph Retrieval）：在推理时仅将与查询相关的紧凑子图显式提取出来，供下游LLM进行推理和人类审查。

该方法通过三个阶段实现：
1. **远程监督的全图构建**（Stage I）：利用冻结的reasoner提供监督信号，训练图构建器（Graph Builder）从长文档流式构建符号图。
2. **远程监督的潜在子图检索**（Stage II）：将边映射为潜在嵌入，在潜在空间中训练子图检索器（Subgraph Retriever），按预算选择最相关边。
3. **联合微调**（Stage III）：联合优化Builder和Retriever，增强二者协同能力。

### **相比现有方法的优势**
- ✅ **兼顾效率与可解释性**：避免了显式方法在长上下文下的不稳定性，同时克服了纯隐式方法不可见的问题。
- ✅ **参数高效适应**：使用LoRA等轻量适配技术，可在小规模模型上训练后迁移到更大规模的reasoner。
- ✅ **灵活扩展性**：支持不同规模的冻结reasoner（1.5B~8B），无需重新训练记忆模块。
- ✅ **固定检索预算控制**：保证推理开销可控，避免随上下文增长而爆炸。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
#### **训练数据集**（共20,800实例）
- **TriviaQA**：开放域问答，上下文长且异构，适合训练跨片段信息聚合能力。
- **QASPER**：基于完整科研论文的问答，需跨章节整合信息。
- **QuALITY**：多选题形式，强调对长篇文章的整体理解。

#### **评估数据集**（共2,600测试实例混合）
- **HotpotQA**：多跳推理，要求跨多个实体或文档推理。
- **NarrativeQA**：叙事型长文本理解，答案抽象度高。
- **WikiHop**：跨维基百科条目的多跳逻辑推理。

> 所有评估数据均未参与训练，确保公平性。

### **实验设置与评估指标**
- **Reasoner**：冻结的预训练LLM，分别使用 `Qwen2.5-1.5B`、`SmolLM3-3B` 和 `Qwen3-8B`。
- **Memory Modules**：Builder 和 Retriever 使用 `Qwen2.5-1.5B-Instruct` + LoRA 微调，其余部分冻结。
- **图容量限制**：每chunk最多提取32个三元组，全局图最大边数 $ M = 150 $。
- **检索预算**：最多返回 $ k = 30 $ 条边构成的子图。
- **输入处理**：文档切分为1024 token的重叠块（overlap=128）。
- **评估指标**：
  - **Accuracy (Acc)**：精确匹配准确率。
  - **ROUGE-L**：衡量生成答案与参考答案的最长公共子序列相似度。

### **基线方法对比**
| 类型 | 方法 | 简介 |
|------|------|------|
| **Reasoner-only** | —— | 直接用完整上下文推理，无外部记忆 |
| **RAG** | —— | 基于embedding相似度检索top-k文本块拼接 |
| **显式图记忆** | THEANINE, PREMem, Mem0/Mem0g, A-Mem | 构建显式图结构并检索 |
| **隐式潜在记忆** | MemGen | 生成软向量作为潜在线索 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（平均 Accuracy / ROUGE-L）**
| 方法 | Qwen2.5-1.5B | SmolLM3-3B | Qwen3-8B | 平均 Acc↑ |
|------|--------------|------------|----------|-----------|
| Reasoner-only | 52.11 / 34.63 | 50.70 / 37.83 | 54.74 / 52.61 | 52.52 |
| MemGen (Latent) | 44.03 / 48.11 | 49.58 / 47.87 | 54.56 / 58.42 | 49.39 |
| A-Mem (Explicit) | 24.78 / 42.37 | 29.44 / 48.54 | 18.89 / 39.68 | 24.37 |
| PREMem (Explicit) | 23.40 / 23.73 | 45.81 / 31.63 | 43.30 / 30.36 | 37.50 |
| THEANINE (Explicit) | 10.63 / 6.22 | 24.47 / 13.71 | 20.91 / 10.54 | 18.67 |
| Mem0 (Explicit) | 25.52 / 19.82 | 15.88 / 11.53 | 17.74 / 12.00 | 19.71 |
| RAG | 6.87 / 21.19 | 7.89 / 36.29 | 16.43 / 43.74 | 10.40 |
| **LATENTGRAPHMEM (Ours)** | **56.08 / 38.61** | **58.64 / 44.16** | **63.34 / 49.73** | **59.35** |

> ✅ 在所有reasoner规模下，LATENTGRAPHMEM均取得**最高平均准确率**，显著优于各类基线。

### **与基线方法的对比结果**
- 相比最强显式方法（PREMem），平均提升约 **21.85%**（59.35 vs. 37.50）。
- 显式方法在 **NarrativeQA** 上普遍崩溃（如THEANINE仅得2.25% Acc），而LATENTGRAPHMEM保持稳健。
- 尽管MemGen在某些ROUGE-L上略优，但在**多跳推理任务（HotpotQA）上准确率更低**，说明其推理过程不可靠。
- LATENTGRAPHMEM能用**单一1.5B Builder适配不同规模reasoner**，而MemGen需同规模训练才能对齐潜在空间。

### **消融实验结果**
#### **图结构与检索策略消融（Figure 3）**
| 变体 | 描述 | 性能趋势 |
|------|------|---------|
| **No Subgraph (Full Graph Only)** | 不做子图检索，直接传整个图给reasoner | ❌ 性能下降，因噪声引入过多 |
| **No Subgraph Retriever (BFS Heuristic)** | 用BFS代替学习式检索器 | ❌ 引入大量无关边，效果差 |
| **Fully Explicit (No Latent Structure)** | 完全依赖显式图构建与检索 | ⚠️ 虽优于朴素检索，但仍低于LATENTGRAPHMEM |

> 结论：**学习式的潜在空间检索 + 显式子图暴露** 是关键设计。

#### **图容量 $ M $ 影响（Figure 4）**
- **WikiHop**：随着 $ M $ 增大持续受益 → 需要更广的知识覆盖。
- **HotpotQA & NarrativeQA**：超过 $ M=150 $ 后性能饱和甚至下降 → 更注重精准而非广度。

---

## **4. 关键结论和发现**

### **主要发现**
1. **显式图记忆在长上下文中不稳定**：结构提取和检索易受噪声干扰，导致端到端性能崩溃。
2. **隐式记忆虽稳定但缺乏可解释性**：无法验证中间推理依据，不利于调试和可信AI。
3. **LATENTGRAPHMEM实现了最佳平衡**：
   - 利用潜在空间保障**稳定性与效率**；
   - 通过显式子图输出实现**可解释性与可控性**；
   - 支持跨规模reasoner迁移，具备良好**泛化性**。
4. **固定检索预算至关重要**：防止推理成本随上下文线性增长，保障实际部署可行性。

### **方法的局限性**
- 图构建质量高度依赖Extractor和Merge/Filter模块的设计。
- 固定容量 $ M $ 和预算 $ k $ 可能在不同任务间需手动调整，缺乏自适应机制。
- 当前仅适用于文本输入和QA任务，尚未拓展至多模态或交互式推理场景。

### **未来工作方向**
- 探索动态图容量控制与自适应检索预算。
- 将框架扩展至多模态记忆系统（如图像+文本）。
- 应用于Agent长期规划、对话系统等复杂推理场景。
- 引入强化学习或其他机制进一步优化Builder-Retriever协作。

---

> **总结一句话**：  
> LATENTGRAPHMEM 提出了一种“**隐式存、显式取**”的新型记忆架构，在保持高效稳定的同时实现了可解释的长程推理，为下一代LLM记忆系统提供了实用且可扩展的设计范式。

</details>

---

### 2. [Robust Physics Discovery from Highly Corrupted Data: A PINN Framework Applied to the Nonlinear Schr\"odinger Equation](https://arxiv.org/abs/2601.04176)

**Authors**: Pietro de Oliveira Esteves  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.04176v1  

#### Abstract
We demonstrate a deep learning framework capable of recovering physical parameters from the Nonlinear Schrodinger Equation (NLSE) under severe noise conditions. By integrating Physics-Informed Neural Networks (PINNs) with automatic differentiation, we achieve reconstruction of the nonlinear coeffici...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Robust Physics Discovery from Highly Corrupted Data: A PINN Framework Applied to the Nonlinear Schrödinger Equation*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该研究针对**在高噪声、稀疏观测条件下从实验数据中恢复非线性物理系统参数**这一挑战性逆问题展开。具体聚焦于**非线性薛定谔方程（NLSE）中的非线性系数 $\beta$ 的识别任务**，其中输入数据具有以下现实特征：
- 高达 **20% 的加性高斯噪声**
- 数据极度稀疏（仅 100–1000 个随机采样点）
- 缺乏先验知识（$\beta$ 初始化为 0.0）

传统数值方法（如有限差分 + 优化）在此类噪声环境下因**数值微分放大误差**而失效。

---

### 🚀 提出的新方法与创新思路
作者提出了一种基于 **Physics-Informed Neural Networks (PINNs)** 的深度学习框架，用于从高度污染的数据中稳健地发现物理规律和参数。

#### 主要创新点包括：
- **将物理正则化作为“去噪滤波器”**：通过将 NLSE 的残差项嵌入损失函数，PINN 能够有效区分信号与噪声，实现对真实动力学的重建。
- **完全无偏初始化**：待识别参数 $\beta$ 初始化为 0.0，不依赖任何先验估计，验证了方法的纯数据驱动性和鲁棒性。
- **自动微分避免数值误差**：利用 PyTorch 的自动微分机制计算精确的空间/时间导数，规避了传统方法中因有限差分导致的噪声放大问题。
- **轻量级架构与可访问性设计**：整个流程可在 Google Colab 上的 NVIDIA Tesla T4 GPU 上运行，总耗时约 80 分钟，提升了科学社区的可复现性。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 FD + L-BFGS） | 本文 PINN 方法 |
|------|----------------------------|----------------|
| 噪声鲁棒性 | 差（噪声被微分放大） | 强（物理约束抑制过拟合） |
| 数据需求 | 需密集、规则网格 | 支持稀疏、随机采样数据 |
| 正则化调参 | 手动调整 Tikhonov 参数 | 物理方程天然提供隐式正则化 |
| 可扩展性 | 对不规则几何适应差 | 易推广至复杂域 |

> ✅ 结论：PINNs 在高噪声、低数据场景下显著优于传统方法，尤其适用于实验条件受限的实际物理系统建模。

---

## 2. 核心实验方法和设置

### 📊 数据集生成
- **控制方程**：一维非线性薛定谔方程（NLSE），形式如下：
  $$
  i\psi_t + 0.5\psi_{xx} + \beta|\psi|^2\psi = 0
  $$
- **真解来源**：解析孤子解（soliton solution），定义在空间域 $x \in [-5,5]$ 和时间域 $t \in [0, \pi/2]$。
- **训练数据**：
  - 从真实解中随机抽取 $N_u \in [100, 1000]$ 个点作为观测数据。
  - 添加 **20% 加性高斯噪声**（信噪比 ~5:1）。
  - 输入仅为 $(x_i, t_i)$ 及其带噪的 $u(x_i,t_i), v(x_i,t_i)$（实部与虚部分解）。

---

### ⚙️ 实验设置
| 项目 | 设置详情 |
|------|----------|
| **网络结构** | Fully Connected DNN：<br>• 输入层：2 neurons ($x, t$)<br>• 隐藏层：4 层 × 50 神经元<br>• 激活函数：tanh<br>• 输出层：2 neurons ($u, v$) |
| **可训练参数** | 约 10,000；$\beta$ 作为可学习标量变量 |
| **优化器** | Adam ($\text{lr}=10^{-3}$)，优于 L-BFGS 在噪声环境下的稳定性 |
| **配点数量** | $N_f = 20,000$ 个 collocation points，采用拉丁超立方采样（LHS）覆盖时空域 |
| **损失函数** | 复合损失：
  $$
  \mathcal{L} = \lambda_{\text{data}} \mathcal{L}_{\text{data}} + \lambda_{\text{physics}} \mathcal{L}_{\text{physics}},\quad \lambda_{\text{data}} = \lambda_{\text{physics}} = 1.0
  $$
  - $\mathcal{L}_{\text{data}}$: MSE on noisy observations
  - $\mathcal{L}_{\text{physics}}$: PDE residual via automatic differentiation |
| **训练配置** | 10,000 epochs，早停判断收敛；使用固定随机种子保证可复现 |

---

### 📈 评估指标
- **相对误差（Relative Error）**：
  $$
  \text{Error}_\beta = \frac{|\beta_{\text{true}} - \beta_{\text{learned}}|}{\beta_{\text{true}}} \times 100\%
  $$
- **统计可靠性**：多次独立运行的标准差（std. dev.）
- **计算效率**：单次训练时间和总流程耗时
- **可视化分析**：时空误差热图、预测 vs. 真值曲线、损失演化曲线

---

### 🔁 基线方法对比
文中虽未直接运行传统方法，但引用文献指出：
- **Finite Difference + Optimization (e.g., L-BFGS-B with Tikhonov)**：
  - 数值微分会放大噪声（误差 $\propto e_{\text{noise}} / h^2$）
  - 需要精细调节正则化参数
  - 不适合不规则采样数据
- 文献支持 [4][5] 表明此类方法在 >10% 噪声下通常失败。

> 因此，作者强调 PINN 在该任务上的**根本优势在于避免显式数值微分**。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### ✅ 不同 $\beta$ 下的参数识别精度（$N_u = 500$, 20% 噪声）

| 真实 $\beta$ | 相对误差 (%) | 标准差 (%) | 训练时间 (min) |
|-------------|---------------|------------|----------------|
| 0.5         | 0.71 ± 0.15   | 很小       | 11.2 ± 1.3     |
| **1.0**     | **0.16 ± 0.05** | 极小       | 10.8 ± 0.9     |
| 2.0         | 0.33 ± 0.10   | 小         | 12.1 ± 1.5     |

> ✔️ 最佳表现：当 $\beta=1.0$ 时，平均误差 **< 0.2%**，标准差 < 0.15%，表现出极强的一致性和鲁棒性。

---

#### ✅ 数据稀疏性敏感性测试（$\beta=1.0$, 20% 噪声）

| 数据点数 $N_u$ | 相对误差 (%) | 标准差 (%) |
|----------------|---------------|------------|
| 100            | 1.47          | 1.10       |
| 250            | 1.59          | 1.49       |
| **500**        | **0.36**      | **0.52**   |
| 1000           | 0.80          | 0.79       |

> 🔍 发现：
> - **500 是最优平衡点**：误差最低且方差最小。
> - 超过 1000 点反而误差上升 → 可能出现对噪声的过拟合。
> - 即使只有 100 个点，误差仍低于 2%，体现 PINN 的**极高数据效率**。

---

#### ✅ 损失动态与模型行为
- **Data Loss**：快速下降后趋于平稳，未趋近零 → 成功避免对噪声过拟合。
- **Physics Loss**：持续下降，说明模型逐步满足 NLSE 约束。
- **参数学习轨迹**：从 $\beta=0.0$ 开始，在前 1000 epoch 内迅速逼近真实值，收敛稳定无震荡。

---

#### ✅ 时空重建质量
- Figure 2 显示绝对误差热图：
  - 整体误差 < 2% 峰值振幅
  - 边界区域略高（常见于 DNN 近似）
  - 孤子核心区域重建极为准确
- Figure 4 显示不同时间切片上，PINN 预测（红色实线）与真实解（黑色虚线）几乎重合，表明具备良好的外推能力。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **PINNs 具备强大的抗噪能力**：即使在 **20% 高斯噪声**干扰下，仍能以 **< 0.2% 的相对误差**恢复非线性系数 $\beta$。
2. **物理正则化即为一种隐式滤波机制**：PDE 残差约束有效分离信号与噪声，防止模型过度拟合观测误差。
3. **高度数据高效**：仅需 **500 个随机采样点**即可实现亚百分之一精度，远少于传统方法所需密度。
4. **计算可及性强**：完整流程可在免费云资源（Google Colab + Tesla T4）上完成，总耗时约 **80 分钟**，促进科研普及。

---

### ⚠️ 方法的局限性
1. **维度限制**：当前仅适用于 1D NLSE；向 2D/3D 扩展面临计算成本剧增（$O(N^d)$ 配点增长）。
2. **噪声假设理想化**：仅考虑加性白高斯噪声；实际实验可能存在非高斯异常值、系统偏差或相关噪声结构。
3. **单一参数识别**：目前只识别 $\beta$；同时估计色散系数与非线性系数更具挑战性，存在参数耦合问题。
4. **损失权重固定**：采用等权策略（$\lambda_{\text{data}} = \lambda_{\text{physics}} = 1.0$），可能非最优；自适应加权 [9] 或可进一步提升性能。
5. **时间外推未验证**：训练区间为 $t \in [0, \pi/2]$，未测试长期演化外推能力。

---

### 🔮 未来工作方向
- 推广至 **高维系统**（如 2D/3D NLSE、Gross-Pitaevskii 方程）
- 实现 **多参数联合反演**（dispersion + nonlinearity）
- 应用于真实实验数据（如光纤通信、冷原子系统）
- 引入 **自适应损失加权策略**（如 [9] 中梯度平衡法）
- 探索 **卷积或注意力结构** 提升空间建模效率
- 测试 **长时间外推能力** 与能量守恒性质保持情况

---

## 💡 总结
本论文成功展示了 **PINN 在极端噪声和稀疏数据条件下进行物理发现的强大潜力**。它不仅实现了对 NLSE 中非线性参数 $\beta$ 的高精度识别（误差 < 0.2%），还揭示了物理信息正则化作为一种**内在去噪机制**的有效性。该方法为处理现实中“脏数据”的科学机器学习提供了可行路径，并因其开源实现和低硬件门槛，有望推动 PINN 在更多物理领域的应用落地。

> 🔗 **代码地址**：[https://github.com/p-esteves/pinn-nlse-2026](https://github.com/p-esteves/pinn-nlse-2026) （MIT 许可证）

</details>

---

### 3. [From Bits to Chips: An LLM-based Hardware-Aware Quantization Agent for Streamlined Deployment of LLMs](https://arxiv.org/abs/2601.03484)

**Authors**: Kaiyuan Deng, Hangyu Zheng, Minghai Qing, Kunxiong Zhu, Gen Li, Yang Xiao, Lan Emily Zhang, Linke Guo, Bo Hui, Yanzhi Wang, Geng Yuan, Gagan Agrawal, Wei Niu, Xiaolong Ma  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.03484v1  

#### Abstract
Deploying models, especially large language models (LLMs), is becoming increasingly attractive to a broader user base, including those without specialized expertise. However, due to the resource constraints of certain hardware, maintaining high accuracy with larger model while meeting the hardware r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# From Bits to Chips: An LLM-based Hardware-Aware Quantization Agent for Streamlined Deployment of LLMs —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）在资源受限设备上的部署面临两大挑战：
- **高精度要求**：量化后需保持模型性能不显著下降。
- **硬件兼容性与效率**：不同硬件平台（如GPU、移动SoC）对量化策略响应差异大，手动调优耗时且易次优。

此外，传统超参数优化方法（如随机搜索、贝叶斯优化）在高维空间中效率低，而编译器驱动的方法依赖预设规则，难以适应复杂多变的LLM部署场景。

### 🚀 提出的新方法：HAQA（Hardware-Aware Quantization Agent）
本文提出 **HAQA** —— 一种基于 **Large Language Model (LLM)** 的自动化框架，用于联合优化：
- **量化模型微调阶段** 的超参数（如 `learning_rate`, `batch_size`, `LoRA` 参数等）
- **硬件部署阶段** 的执行配置（如 CUDA kernel 的 `blockdim`, `griddim`, 内存布局、tiling 策略等）

该代理通过 **静态提示（Static Prompt） + 动态反馈（Dynamic Prompt）** 构建闭环优化流程，并引入 **ReAct** 范式实现“思考-行动-观察”迭代机制，提升决策稳定性与可解释性。

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | HAQA |
|------|--------|-------|
| **优化范围** | 仅微调 或 仅部署 | ✅ 联合优化微调 + 部署 |
| **专家依赖** | 高度依赖人工经验 | ✅ 自动化，降低非专家门槛 |
| **搜索效率** | 网格/随机搜索低效；贝叶斯优化计算开销大 | ✅ LLM推理引导，快速收敛 |
| **硬件感知能力** | 缺乏深层硬件分析 | ✅ 可识别反直觉配置（如 INT8 > INT4） |
| **通用性与适应性** | 特定于某类模型或硬件 | ✅ 支持多种模型（ResNet, LLaMA系列）和平台（A6000, OnePlus 11） |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与模型
| 类型 | 数据集 | 模型 |
|------|--------|------|
| 图像分类 | CIFAR-10, ImageNet2012 | ResNet20, ResNet32, ResNet50 |
| 大语言模型 | Alpaca | LLaMA2-7B, LLaMA2-13B, LLaMA3.2-3B, LLaMA3-8B |

量化技术：
- **DoReFa-QAT**（用于ResNet）
- **QLoRA**（用于LLaMA系列，支持 INT4/INT8 量化）

### ⚙️ 实验设置
#### 微调任务
- 使用 **ChatGPT-4 (GPT-4-0613)** 作为 LLM Agent
- 最大优化轮数：10轮
- 搜索空间包含学习率、批量大小、梯度累积步数、LoRA rank/alpha 等（详见 Appendix D）

#### 部署任务
- 基于 `llama.cpp` 框架进行端到端推理测试
- 测试平台：
  - **服务器级 GPU**：NVIDIA A6000（48GB显存，CUDA 12.3）
  - **移动端 SoC**：OnePlus 11（Snapdragon 8 Gen 2 + Adreno 740 GPU）
- 输入序列长度：128，输出 token 数：256
- 主要指标：**Token Generation Speed (tokens/s)**, **Latency (μs)**

### 📊 评估指标
| 类别 | 指标 |
|------|------|
| 准确性 | Accuracy (%) on downstream tasks (BoolQ, RTE, Winograde, ARC, Hellaswag, MathQA 等) |
| 推理效率 | Latency (μs), Throughput (tokens/s), Speed-up vs baseline |
| 内存约束 | 是否满足指定内存限制（如 10GB） |
| 收敛性 | Accuracy 随迭代轮次的变化曲线（见 Figure 4） |

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **Default** | 全精度模型推荐的默认超参 |
| **Human** | 人类专家手动调参平均结果 |
| **Local Search / Random Search** | 局部/随机搜索 |
| **Bayesian Optimization** | 贝叶斯优化（Surrogate Model） |
| **NSGA-II** | 多目标进化算法 |
| **llama.cpp (Default)** | 原始部署基准（无优化） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### ✅ 准确性提升（Table 1 & Table 2）
| 模型 | 量化方式 | HAQA 准确率 | 最佳基线（Human） | 提升幅度 |
|------|----------|-------------|------------------|---------|
| ResNet20 (w4a4) | INT4 | **88.38%** | 87.17% | +1.21pp |
| ResNet32 (w4a4) | INT4 | **90.02%** | 88.90% | +1.12pp |
| LLaMA2-7B (INT4) | INT4 | **63.11% (avg)** | 62.04% | +1.07pp |
| LLaMA2-13B (INT4) | INT4 | **65.57% (avg)** | 63.96% | +1.61pp |
| LLaMA3-8B (INT4) | INT4 | **67.87% (avg)** | 66.60% | +1.27pp |

> 注：在极低比特（如 w2a2）下，HAQA 是唯一能稳定收敛并取得较好性能的方法。

#### ⚡ 推理加速效果（Figure 5 & Table 3）
| 平台 | 模型 | 量化 | 默认吞吐 (tokens/s) | HAQA 吞吐 | 加速比 |
|------|------|--------|--------------------|------------|--------|
| A6000 GPU | LLaMA2-7B | INT4 | ~4.0 | ~6.0 | **~1.5×** |
| A6000 GPU | LLaMA3-8B | INT8 | ~3.5 | ~5.0 | **~1.4×** |

| Kernel | 输入尺寸 | 默认延迟 (μs) | HAQA 优化后 | 加速比 |
|--------|-----------|----------------|--------------|--------|
| Softmax | [1024,64,32] | 51.70 | 27.96 | **1.85×** |
| SiLU    | [11008,64,1] | 10.44 | 4.51  | **2.31×** |
| MatMul  | [2048,128,2048] | 63.20 | 38.85 | **1.63×** |

> 在关键计算 kernel 上实现了 **1.1× ~ 2.3× 的延迟降低**

#### 💡 反直觉但有效的硬件自适应策略（Section 4.4）
在 **OnePlus 11（Adreno 740 GPU）** 上：
- 尽管 INT4 理论上更高效，但实际表现不如 INT8
- 原因：Adreno 740 **不原生支持 INT4**，需通过 INT8 模拟 + 额外位操作重建值 → 引入额外开销
- HAQA 正确识别此现象，推荐使用 **INT8**，实测性能优于 INT4

| 模型 | FP16 | INT8 | INT4 |
|------|------|------|------|
| openllama-3B | 5.11 | **5.25** | 4.95 |
| tinylama-1.1B | 11.17 | **11.23** | 10.43 |

> 表明 HAQA 能自动发现并利用硬件特性，避免“理论最优 ≠ 实际最优”的陷阱

#### 📉 收敛速度优势（Figure 4）
- HAQA 在 **更少轮次内达到更高准确率**
- 相比其他方法（如 Bayesian, Random），其优化路径更具方向性和稳定性
- 利用历史反馈避免重复无效尝试，提升搜索效率

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **HAQA 显著提升了量化模型的准确性与推理效率**  
   - 在多个 LLM 和 CNN 模型上均超越人工调优和其他自动化方法。
   - 在 INT4 极限量化下仍能维持高性能，缩小与 FP16 的差距。

2. **实现了跨阶段联合优化（fine-tuning + deployment）**  
   - 首次将 LLM Agent 应用于从训练到部署的完整链条。
   - 通过协同调参实现“synergistic optimization”，而非孤立优化各模块。

3. **具备强大的硬件感知与自适应能力**  
   - 能分析硬件架构细节（如是否支持特定指令集、memory bandwidth）做出合理判断。
   - 成功揭示并验证了“INT8 比 INT4 更快”这类反直觉现象。

4. **用户友好性强，易于集成与复现**  
   - 提供标准化 prompt 设计模板（Appendix E）
   - 控制历史长度防止超出上下文窗口
   - 总成本低廉：约 **$5** 完成两个模型的全流程优化（API 调用费用）

### ⚠️ 方法的局限性
- **依赖外部 LLM API**（如 GPT-4），存在延迟、成本和隐私顾虑
- 当前未支持完全本地化部署的轻量级 LLM Agent
- 对 prompt engineering 敏感，需精心设计静态提示以保证稳定性
- 实验主要集中于 NVIDIA 和高通平台，尚未覆盖更多异构硬件（如 Apple Silicon, Ascend）

### 🔮 未来工作方向
- 开发 **开源小型 LLM Agent** 替代闭源大模型，实现去中心化优化
- 扩展至 **多模态模型** 和 **边缘集群协同部署**
- 引入 **强化学习 + LLM** 混合架构，进一步提升探索效率
- 构建统一的 **AI 部署智能体生态**，支持自动诊断、修复与迁移

---

## 总结
> **HAQA 是首个将 LLM Agent 成功应用于量化模型微调与硬件部署联合优化的工作**。它不仅显著提升了模型精度与推理速度，更重要的是降低了 AI 部署的技术门槛，使非专家也能轻松完成复杂的模型压缩与加速任务。其“硬件感知 + 反直觉洞察”的能力展示了 LLM 在系统级优化中的巨大潜力，为未来构建全自动化的 AI 工程流水线奠定了基础。

</details>

---

### 4. [LUT-KAN: Segment-wise LUT Quantization for Fast KAN Inference](https://arxiv.org/abs/2601.03332)

**Authors**: Oleksandr Kuznetsov  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.03332v1  

#### Abstract
Kolmogorov--Arnold Networks (KAN) replace scalar weights by learnable univariate functions, often implemented with B-splines. This design can be accurate and interpretable, but it makes inference expensive on CPU because each layer requires many spline evaluations. Standard quantization toolchains a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LUT-KAN: Segment-wise LUT Quantization for Fast KAN Inference —— 核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

Kolmogorov-Arnold Networks (KAN) 通过将传统神经网络中的标量权重替换为可学习的一元函数（通常用 B-splines 实现），在保持模型紧凑性和高预测精度的同时提供了良好的**可解释性**。然而，这种设计带来了显著的推理开销，尤其是在 CPU 上：

- **计算瓶颈**：每个 KAN 层需要对大量 B-spline 函数进行重复求值，涉及查找节点区间、递归计算基函数等操作，导致推理延迟高。
- **量化困难**：标准的量化工具链（如 INT8 GEMM）针对矩阵乘法优化，而 KAN 的核心运算是函数求值而非矩阵运算，难以直接应用。
- **部署不确定性**：缺乏明确的 **Out-of-Bounds (OOB)** 行为定义，不同实现可能产生不一致的结果。

因此，如何在保证精度的前提下，实现**快速、可移植、语义一致的 CPU 推理**成为 KAN 部署的关键挑战。

---

### **提出了什么新方法或新思路**

本文提出 **LUT-KAN**，一种面向 PyKAN 风格 KAN 层的**分段查找表（segment-wise LUT）编译与量化方法**，其核心思想包括：

1. **Segment-wise LUT 编译**  
   将每条边上的可学习一元函数 $\phi_i(x)$ 在每个 knot segment 内预计算并存储为 LUT，使用线性插值恢复连续输出。LUT 分辨率由每段采样点数 $L$ 控制。

2. **Affine int8/uint8 量化**  
   对每个 segment 的 LUT 值采用仿射量化：
   - **对称量化（symmetric int8）**：范围 [-127, 127]，零中心化
   - **非对称量化（asymmetric uint8）**：范围 [0, 255]，支持偏移
   统一反量化公式确保推理一致性。

3. **显式的 OOB 语义契约（Explicit OOB Contract）**  
   明确定义两个正交维度的行为：
   - **Boundary Mode**：`half_open` vs `closed`，决定域内/外判定
   - **OOB Policy**：`clip_x`（输入饱和） vs `zero_spline`（仅抑制样条分支）
   这些配置被固化在 LUT artifact 中，确保跨平台行为一致。

4. **“诚实基线”评估方法论（Honest Baseline Methodology）**  
   为避免混淆“表示形式改进”与“后端优化”，作者提出公平比较：
   - 在相同后端下对比：**NumPy B-spline vs NumPy LUT**
   - 或 **Numba B-spline vs Numba LUT**
   从而分离出真正的“LUT vs 样条”带来的加速效果。

5. **轻量级可部署 artifact**  
   输出为压缩 NPZ 文件 + JSON 清单，可在无 PyTorch 和样条库依赖的情况下加载执行，适合边缘部署。

---

### **相比现有方法的优势**

| 方面 | LUT-KAN 的优势 |
|------|----------------|
| **速度** | 相比原始 B-spline 求值，在相同后端下实现 **10–14× 加速**（NumPy/Numba） |
| **精度保留** | 在 $L=64$ 下，下游任务 F1 分数下降 < 0.0002，几乎无损 |
| **可复现性与一致性** | 显式 OOB 语义 + 固定版本代码发布（GitHub tag v1.0.0），确保结果可复现 |
| **工程实用性** | artifact 轻量、独立于训练框架，便于集成到生产系统 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

1. **控制实验（Controlled Sweeps）**  
   - 使用随机初始化的 KAN 层（输入 dim=10, 输出 dim=8, grid=8, degree=3）
   - 输入分布：标准正态分布裁剪至 knot domain
   - 目的：控制变量研究 LUT 参数影响

2. **真实案例研究（Case Study）**  
   - **CICIDS2017 数据集**：用于 DoS 攻击检测（BENIGN vs DoS Hulk）
   - 特征数量：78
   - 测试集大小：69,523 样本
   - 模型结构：[78 → 32 → 16 → 1]，共 3,024 条边

---

### **实验设置和评估指标**

#### **LUT 构造参数**
- 分辨率 $L \in \{16, 32, 64, 128\}$
- 量化方案：symmetric int8 / asymmetric uint8
- 插值方式：linear
- 边界模式：closed / half_open
- OOB 策略：clip_x / zero_spline
- 值表示：`value_repr = spline_component`（推荐）

#### **评估指标**

| 类别 | 指标 |
|------|------|
| **准确性** | MAE（in-range / OOB）、MaxAbs Error、分类 Accuracy/Precision/Recall/F1 |
| **速度** | Steady-state latency (ms/iter, ms/sample)，Speedup（相对同后端 B-spline） |
| **内存** | LUT artifact 大小（bytes）、LUT/float_model_ratio |
| **鲁棒性** | OOB 触发率（OOB_any_frac）、OOB 子集误差分析 |

#### **重复性保障**
- 所有实验运行 5 次（seeds 0–4）
- 报告均值 ± 标准差（mean±std）
- 公开完整实验脚本与固定版本代码

---

### **基线方法对比**

- **不公平基线（Stack-level）**：PyTorch float B-spline vs NumPy/Numba LUT（混合了后端差异）
- **诚实基线（Honest Baseline）**：
  - NumPy B-spline vs NumPy LUT
  - Numba B-spline vs Numba LUT
- 强调只基于“诚实基线”得出表示形式的加速收益

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **精度表现（$L=64$）**
| 设置 | MAE (in-range) | MaxAbs (in-range) |
|------|----------------|--------------------|
| symmetric int8 | $1.59 \times 10^{-4}$ | $8.02 \times 10^{-4}$ |
| asymmetric uint8 | $1.58 \times 10^{-4}$ | $8.33 \times 10^{-4}$ |

- 误差随 $L$ 增大呈 $O(1/L)$ 衰减，符合线性插值理论预期
- 两种量化方案精度几乎无差别

#### ✅ **推理速度（诚实基线）**
| 后端 | 平均 Speedup (vs B-spline) |
|------|----------------------------|
| **NumPy** | **12.3× ± 1.2×** |
| **Numba** | **10.5× ± 0.6×** |

> 示例（Numba）：
> - B-spline: ~6.25 ms/iter
> - LUT: ~0.60 ms/iter → **~10.4× 加速**

- LUT 性能稳定，不受 $L$ 影响（内存绑定，非计算绑定）

#### ✅ **内存开销**
| $L$ | LUT / Float Model Ratio |
|-----|--------------------------|
| 16 | 3.07× |
| 32 | 5.51× |
| **64** | **10.40×** |
| 128 | 20.18× |

- 主要开销来自 `q_table`（占 73–88%）
- 在 $L=64$ 时约为 **10× 内存代价**

#### ✅ **真实任务性能（DoS 检测）**
| 指标 | Float Model | LUT (NumPy/Numba) | 差异 |
|------|-------------|--------------------|-------|
| **F1 Score** | 0.9900 | 0.9898 | ↓0.0002 |
| **Accuracy** | 0.9899 | 0.9898 | ↓0.0001 |
| **Latency (ms/sample)** | 0.8850 | 0.0596 (NumPy) / 0.0138 (Numba) | **↓12× / ↓64×** |

- **Steady-state 加速**：
  - NumPy LUT vs PyTorch float: **14.9×**
  - Numba LUT vs PyTorch float: **64.4×**
- **诚实基线加速**（更保守）：
  - NumPy LUT vs NumPy B-spline: **~12×**
  - Numba LUT vs Numba B-spline: **~10×**

#### ✅ **消融实验结果**

##### （1）OOB 鲁棒性矩阵（$L=64$, symmetric int8）

| Boundary | OOB Policy | OOB frac | MAE (OOB) | MaxAbs (OOB) |
|---------|------------|----------|-----------|--------------|
| closed | clip_x | 0.000 | N/A | N/A |
| half_open | clip_x | 0.101 | 0.000312 | 0.000661 |
| half_open | zero_spline | 0.101 | 0.0245 | **0.0892** |

- `half_open + zero_spline` 导致严重 OOB 错误（因边界处函数值突变）
- 推荐：若输入会被裁剪，则使用 `closed` + `clip_x`

##### （2）量化方案对比（$L=64$）
- symmetric int8 与 asymmetric uint8 在测试场景下精度几乎一致
- 非对称量化更适合非零均值分布；对称更简单（无需存储偏移）

---

## 4. 关键结论和发现

### **主要发现**

1. **LUT-KAN 显著提升 KAN 推理速度**  
   在相同后端优化条件下，实现 **10–14× 的 CPU 推理加速**，且加速源于表示形式本身（非向量化/JIT 效应）。

2. **精度损失极小**  
   在 $L=64$ 下，函数逼近 MAE ~$1.6 \times 10^{-4}$，真实任务 F1 下降 < 0.0002，满足大多数应用场景需求。

3. **内存开销可控**  
   内存增长约 10×（$L=64$），主要由量化表主导，可通过降低 $L$ 或参数压缩进一步优化。

4. **OOB 语义至关重要**  
   不同 boundary mode 与 oob_policy 组合会导致显著不同的 OOB 行为。必须将其作为模型部署契约的一部分明确定义。

5. **“诚实基线”方法论有效揭示真实收益**  
   若不控制后端一致性，容易夸大 LUT 的优势；本文方法提升了评估可信度。

---

### **方法的局限性**

| 局限性 | 说明 |
|--------|------|
| **内存开销大** | Dense 层边数多时，LUT artifact 可能过大（首层占 82.5%） |
| **静态编译** | 模型更新需重新编译 LUT，不支持在线微调 |
| **未完全整数量化** | 反量化、累加仍使用 float32，未实现端到端 integer pipeline |
| **校准敏感性** | 量化范围依赖校准数据分布，若部署数据偏移可能导致性能下降 |
| **单精度参数存储** | scale/y_min 使用 float32，可用 float126 压缩但可能引入数值误差 |

---

### **未来工作方向**

1. **稀疏 LUT 表示**：结合 edge pruning 或低秩分解减少 dense 层内存占用
2. **自适应分辨率 LUT**：根据函数复杂度动态调整各 segment 的 $L$
3. **全整数推理流水线**：探索 int8 累加与定点运算支持
4. **动态量化与在线编译**：支持轻量级模型更新后的自动 LUT 生成
5. **硬件协同设计**：针对 LUT-KAN 设计专用边缘推理芯片或 FPGA 实现

---

> 🔗 **代码与复现**  
> 所有代码、实验脚本、artifact 均已开源，带固定 release tag（v1.0.0）：
> - LUT-KAN 框架：[https://github.com/KuznetsovKarazin/lut-kan](https://github.com/KuznetsovKarazin/lut-kan)
> - DoS 检测案例：[https://github.com/KuznetsovKarazin/kan-dos-detection](https://github.com/KuznetsovKarazin/kan-dos-detection)

</details>

---

### 5. [ReEfBench: Quantifying the Reasoning Efficiency of LLMs](https://arxiv.org/abs/2601.03550)

**Authors**: Zhizhang Fu, Yuancheng Gu, Chenkai Hu, Hanmeng Liu, Yue Zhang  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03550v1  

#### Abstract
Test-time scaling has enabled Large Language Models (LLMs) to tackle complex reasoning, yet the limitations of current Chain-of-Thought (CoT) evaluation obscures whether performance gains stem from genuine reasoning or mere verbosity. To address this, (1) we propose a novel neuro-symbolic framework ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReEfBench: Quantifying the Reasoning Efficiency of LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前对大语言模型（LLMs）推理能力的评估主要依赖 **Chain-of-Thought (CoT)** 输出长度或最终答案准确性，存在以下缺陷：
- **无法区分“真实推理”与“冗余生成”**：模型可能通过“过度思考”（overthinking）生成大量无意义的中间步骤来提升表现，而非真正进行深度逻辑推理。
- **缺乏过程性评估机制**：现有方法难以量化推理路径中的逻辑深度、探索广度、连贯性和冗余程度等认知行为特征。
- **训练策略冲突风险未知**：混合长短 CoT 数据训练是否影响模型推理策略尚不明确。

### 提出的新方法与思路
作者提出 **ReEfBench** ——一个基于 **神经符号系统（neuro-symbolic）** 的非侵入式（non-intrusive）、过程中心化的推理效率评估框架，其核心思想是将自然语言推理链映射到 **一阶逻辑（First-Order Logic, FOL）** 图谱中，从而实现可计算的逻辑深度量化。

#### 方法流程（五阶段 Pipeline）
1. **Phase A: 可控数据生成**  
   构建基于 FOL 的合成推理任务，支持精确控制逻辑深度（Logical Depth）、复杂度和干扰项数量。
2. **Phase B: 模型响应获取**  
   向目标 LLM 输入问题并收集自由格式的 CoT 推理文本。
3. **Phase C: 响应解析（LLM-based Parsing）**  
   使用小型 LLM 将自然语言响应分解为结构化节点，分类为 `normal` / `reflection` 和 `actual` / `planning` 节点。
4. **Phase D: 规则驱动的节点处理（Rule-based Verification）**  
   利用符号逻辑规则验证每个实际节点（actual node）的正确性，并计算其逻辑深度。
5. **Phase E: 行为指标聚合**  
   综合多个维度生成六项诊断性指标，并据此识别行为原型。

### 相比现有方法的优势
| 特性 | ReEfBench | 其他方法（如 ProntoQA, ZebraLogic, ROSCOE） |
|------|-----------|---------------------------------------------|
| **Scalable** ✅ | 支持大规模、参数化生成不同难度样本 | 多数数据集规模有限或不可扩展 |
| **FOL-based** ✅ | 基于形式化逻辑，确保逻辑深度可计算 | 部分使用非形式化逻辑或自然语言 |
| **Logic-Only** ✅ | 聚焦纯推理，剥离知识依赖 | 易受先验知识干扰 |
| **LogDepth Quantification** ✅ | 可精确测量每一步的逻辑深度 | 多为二值化“有效性”判断 |
| **Behavioral Process Analysis** ✅ | 分析探索、反思、连贯性等认知行为 | 缺乏细粒度过程分析 |
| **Non-intrusive** ✅ | 不强制输出格式，保留模型灵活性 | 多需特定模板或结构化输出 |

> ✅ 表示具备该特性；❌ 表示不具备或部分具备（见 Table 1）

---

## 2. 核心实验方法和设置

### 数据集
- **自研数据集 ReEfBench**：基于 FOL 自动生成的多跳逻辑推理题。
  - 形式：“A 是 B”，“B 是 C” → “A 是 C”（Modus Ponens）
  - 控制变量：逻辑深度 $ C \in [3, 11] $，每层 100 个样本，共 900 道题。
  - 包含干扰前提（distractors），增加任务难度。
  - 示例见 Figure 2。

### 实验设置
- **评估模型**：共测试 **25 个主流 LLMs**，涵盖闭源（Claude 系列）与开源（Qwen、DeepSeek-R1 等）。
- **模式区分**：
  - `.long`：启用 thinking/reasoning 模式的长 CoT。
  - `.short` 或无后缀：标准指令微调模式下的短 CoT。
- **提示词设计**：要求模型逐步推理并在 `\boxed{}` 中给出最终答案。
- **API 参数**：temperature=0，最大 token 数分别为 24k（reasoning 模型）和 8k（普通模型）。

### 评估指标（六大诊断性分数）
所有指标经 max-normalization 映射至 [0,1] 区间：

| 指标 | 定义 | 单位/来源 |
|------|------|----------|
| **Logical Depth ($S_{ld}$)** | 达成的最大有效逻辑深度 | 正确推导步数 |
| **Cost ($S_{cost}$)** | 计算消耗，综合 token 数、planning/reflection 步骤频率 | token count + step freq |
| **Exploration ($S_{exp}$)** | 探索的独特且正确的逻辑节点数量 | unique correct nodes |
| **Efficiency ($S_{eff}$)** | 单位成本带来的逻辑增益（$P = W/t$） | tokens per depth increment |
| **Coherence ($S_{coh}$)** | 元认知行为（planning/reflection）是否带来实际进展 | 是否引发新节点或更深推理 |
| **Redundancy ($S_{red}$)** | 句子级与节点级重复率 | duplication ratio |

此外还报告原始统计量：平均逻辑深度、token 数等。

### 基线方法对比
本文未直接对比传统 CoT 模型，而是通过以下方式体现比较优势：
- 对比同一模型的不同模式（`.long` vs `.short`）
- 对比不同训练策略（SFT vs SFT+RL vs 混合训练）
- 对比不同规模模型（如 Qwen3-32B 与蒸馏版 7B/4B）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Complexity Level 11 下代表性模型）

| Model | Category | $S_{ld}$ | $S_{cost}$ | Depth | Tokens(k) |
|-------|----------|---------|------------|--------|------------|
| Qwen3-235B-thinking | DeepWanderer | 1.00 | 0.88 | 10.54 | 16.8 |
| Claude-Opus-4.5.long | EffectiveSolver | 0.97 | 0.37 | 10.27 | 3.5 |
| QwQ-32B | HollowMimic | 0.68 | 0.61 | 7.12 | 5.7 |
| Qwen2.5-32B-Instruct | LazyGuesser | 0.28 | 0.16 | 2.90 | 0.7 |

> 数据来自 Table 2 和 Table 13

### 四类行为原型（Behavioral Prototypes）

| 类型 | 特征 | 代表模型 | 发现 |
|------|------|---------|------|
| **Effective Solver** | 高逻辑深度 + 低成本 + 高效率 | Claude-Opus-4.5.long | 实现高效精准推理，“少而精” |
| **Deep Wanderer** | 高逻辑深度 + 高成本 + 低效率 | Qwen3-235B-thinking | 广度优先搜索式穷举，冗余高 |
| **Hollow Mimic** | 中等成本 + 浅层推理 + 高冗余 | DS-R1-Qwen-7B, QwQ-32B | “表演式推理”，有规划但无实质进展 |
| **Lazy Guesser** | 低投入 + 低产出 + 易饱和 | Qwen2.5-32B-Instruct | 面对难题放弃深入，陷入循环陈述 |

> 如 Figure 3 所示，四类模型在 $S_{ld}$-$S_{cost}$ 平面上形成清晰聚类。

### 与基线方法的对比结果

#### Long CoT vs Short CoT（Table 3）
- 在现代模型中，**Short CoT 已接近 Long CoT 的推理深度**：
  - Qwen3-235B-Instruct（short）仅比其 thinking 版本落后 **1.6%** 的相对深度。
- 成功的关键在于引入 **reflection 机制**：
  - 表现优异的 short 模型普遍具有更高的 reflection ratio（Table 4）。
  - 反之，缺乏 reflection 的 short 模型（如 Qwen2.5-32B-Instruct）表现极差。

#### Distillation 效果分析（Figure 5 & 10）
- 将 Long CoT 策略蒸馏到小模型（如 4B/8B）会导致：
  - **行为模仿成功**：小模型生成更多 reflection/planning 步骤。
  - **逻辑能力失败**：无法转化为更高逻辑深度。
  - **质量随规模下降**：reflection 和 planning 的语义质量严格递减（32B > 14B > 8B > 4B）。
- **临界容量阈值**：约 **14B** 参数是保持行为与能力对齐的关键门槛。

#### Mixed Training 影响（Figure 9）
- 混合训练（同时使用 long 和 short CoT 数据）会破坏 high-cost 推理策略：
  - Qwen3-235B.long（mixed）相比 Qwen3-235B-thinking（pure）出现 **提前饱和甚至崩溃（collapse）**。
  - 这表明 high-P（高效）与 high-t（高耗时）策略之间存在 **训练干扰（strategy interference）**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **深度推理 ≠ 长文本生成**  
   Extended token generation is **not a prerequisite** for deep reasoning. 高效模型（如 Claude-Opus-4.5.long）能在较少 token 内完成复杂推理。

2. ✅ **存在两种成功的推理范式**：
   - **High-P Strategy (Effective Solver)**：追求效率最大化，精准推导。
   - **High-t Strategy (Deep Wanderer)**：牺牲效率换取覆盖广度，适合不确定性高的任务。

3. ❌ **蒸馏易导致“行为模仿”而非“能力复制”**  
   小模型可以学会模仿大模型的 reflection 和 planning 行为，但由于 **intrinsic capacity limits**，无法真正掌握深层逻辑。

4. ⚠️ **混合训练有害于高消耗推理策略**  
   Mixing long and short CoT data risks **premature saturation and collapse**，破坏模型应对复杂问题的能力。

5. 🔍 **“表演式推理”是一种新型失败模式（Hollow Mimic）**  
   模型表现出完整的 planning/reflection 结构，但未能推动逻辑前进，属于 **unproductive verbosity**。

### 局限性
1. **领域限制**：仅适用于 **FOL 可表达的封闭世界推理**，不适用于开放域问答或创造性推理。
2. **非侵入性代价**：无法捕捉隐式推理步骤，仅能分析显式输出。
3. **分类边界模糊**：四大原型是连续光谱上的典型代表，个体模型可能跨类别浮动。
4. **依赖 LLM Parser**：尽管验证集上 F1 达 94.3%，但仍存在误分类风险。

### 未来工作方向
- 扩展至更复杂的逻辑系统（如高阶逻辑、模态逻辑）。
- 开发能引导模型从 “Hollow Mimic” 向 “Effective Solver” 演化的训练方法。
- 探索动态调整推理预算（test-time scaling）的最优控制策略。
- 将 ReEfBench 应用于模型训练过程监控与干预。

---

> 📦 **资源公开**：作者已将 ReEfBench 数据集与评估代码开源：[anonymous.4open.science/r/LoG-1AD8/](https://anonymous.4open.science/r/LoG-1AD8/)

</details>

---

### 6. [Breaking the Assistant Mold: Modeling Behavioral Variation in LLM Based Procedural Character Generation](https://arxiv.org/abs/2601.03396)

**Authors**: Maan Qraitem, Kate Saenko, Bryan A. Plummer  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03396v1  

#### Abstract
Procedural content generation has enabled vast virtual worlds through levels, maps, and quests, but large-scale character generation remains underexplored. We identify two alignment-induced biases in existing methods: a positive moral bias, where characters uniformly adopt agreeable stances (e.g. al...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Breaking the Assistant Mold: Modeling Behavioral Variation in LLM Based Procedural Character Generation*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对**大型语言模型（LLM）在程序化角色生成（Procedural Character Generation, PCG）中存在的两类对齐诱导偏差（alignment-induced biases）**：
- **Positive Moral Bias（正向道德偏差）**：角色在面对道德规范陈述时几乎总是选择“完全同意”，缺乏道德立场多样性。
- **Helpful Assistant Bias（助手倾向偏差）**：角色在对话中总是直接回答问题，从不拒绝、回避或表现出复杂互动行为。

这些偏差源于LLM训练中的最大似然估计（maximum likelihood estimation）和助手微调（assistant fine-tuning），导致生成的角色趋于安全、礼貌、同质化，抑制了戏剧张力和真实感。

---

### 🚀 提出的新方法：**PERSONAWEAVER**

**核心思想**：将角色构建过程解耦为两个独立模块：
1. **World-Building（世界构建）**  
   负责生成与设定相关的非行为属性，如职业、居住地、年龄、性别等。
2. **Behavioral-Building（行为构建）**  
   显式建模角色的行为特征，包括：
   - **道德立场（Moral Stances）**：基于 *Moral Foundations Theory* 定义8种不同的道德配置（见附录C）。
   - **交互风格（Interactional Styles）**：定义8类反应模式（如拒绝、回避、犹豫、顺从、挑衅、元评论等，见表3）。

通过从两个模块分别采样并组合，实现对角色行为的**显式控制与多样化生成**。

---

### 🔍 相比现有方法的优势
| 方面 | 现有方法（如 WoRLDWEAVER, PERSONAHUB） | PERSONAWEAVER |
|------|----------------------------------------|----------------|
| 行为多样性 | 隐式生成，易坍缩至“一致同意”和“总是回答” | 显式建模，强制引入变异 |
| 控制粒度 | 无法精细调控道德或交互行为 | 可系统调节行为维度 |
| 输出丰富性 | 回应风格单一，长度/语气趋同 | 引发二级风格差异（填充词、标点、情感等） |
| 泛化能力 | 多局限于单一场景 | 在10个不同虚构与现实场景中验证 |

> ✅ **优势总结**：PERSONAWEAVER 成功打破了LLM作为“助手”的默认行为模式，实现了更具表现力、更贴近人类多样性的虚拟角色生成。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 来源 | 用途 |
|-------|------|------|
| **Social Chemistry** (Forbes et al., 2020) | 包含日常道德判断陈述（如“父母应确保孩子吃健康食物”） | 探测角色的**道德立场分布** |
| **ConvAI2** (Dinan et al., 2019) | 对话数据集，含开放式提问 | 测试角色的**交互反应类型**（是否回答、如何回答） |

具体使用的提示见 **Table 1**，涵盖5个通用问题（q1–q5）和5个情绪导向问题（s1–s5）。

---

### ⚙️ 实验设置
- **目标设置数量**：共10个（5个现实 + 5个幻想），例如：
  - 现实：*Friday Night Lights*, *The Wire*
  - 幻想：*Game of Thrones*, *The Matrix*
- **每种方法生成角色数**：每个设置下生成100个角色 → 总计1,000个角色/方法
- **测试方式**：
  1. 向每个角色提出来自 Social Chemistry 的道德陈述，记录其多选答案（a–d）
  2. 提出 ConvAI2 中的问题，收集开放回应，并由辅助LLM分类其反应类型

---

### 📊 评估指标
| 维度 | 指标 |
|------|------|
| **道德多样性** | 不同选项（完全同意→完全不同意）的响应分布熵、均衡性 |
| **交互多样性** | 反应类别分布（合规、回避、拒绝等） |
| **风格多样性** | 回答长度、填充词频率、标点使用、情感极性（sentiment）变化 |
| **模型覆盖** | 主实验使用 **GPT-4o**，并在附录中复现于 **LLaMA4** 和 **Qwen 3**

---

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **WoRLDWEAVER** (Jin et al., 2024) | 直接用LLM提示生成N个角色描述 |
| **PERSONAHUB** (Ge et al., 2024) | 从网络爬取的人设库中抽取并适配到目标环境 |
| **OURS: PERSONAWEAVER** | 解耦世界与行为建模，显式注入行为变异 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（基于 GPT-4o）

#### （1）道德立场分布（Fig. 3）
- **WoRLDWEAVER & PERSONAHUB**：
  - >90% 角色选择“完全同意”
  - 几乎无“部分/完全不同意”响应
- **PERSONAWEAVER**：
  - 分布显著更均衡
  - “完全不同意”占比提升至 ~15%
  - “部分同意/不同意”比例明显增加
  > ✅ 打破了“普遍赞同”的道德坍缩现象

#### （2）问答行为分布（Fig. 4）
- **基线方法**：
  - >95% 直接回答问题（compliance）
  - 极少出现拒绝或回避
- **PERSONAWEAVER**：
  - 约 **30% 的响应为拒绝或回避**
  - 显著提升了“deflection”、“hesitation”、“playful/subversive”等复杂行为
  > ✅ 成功缓解“必须回答”的助手偏见

#### （3）二级风格多样性（Fig. 5）
| 特征 | 结果 |
|------|------|
| **Filler Words** | PERSONAWEAVER 产生更多“嗯…”、“我觉得…”等口语化表达 |
| **Punctuation** | 更丰富的标点使用（省略号、感叹号、问号混用） |
| **Answer Length** | 回应长度方差更大，长短交错更自然 |
| **Sentiment** | 情感分布从集中于 POSITIVE 扩展至 NEUTRAL 与 NEGATIVE |

> 💡 发现：一旦主行为维度被显式控制，LLM会自然延伸出更丰富的**表达风格多样性**。

---

### 🔬 消融实验与跨模型验证（Appendix F）
在 **LLaMA4** 和 **Qwen 3** 上重复实验，结果一致：
- 基线方法仍存在严重道德与反应偏差
- PERSONAWEAVER 显著提升所有维度的多样性
> ✅ 证明该框架具有良好的**跨模型泛化性**，并非仅适用于GPT系列。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM-based PCG 存在系统性行为偏差**：
   - 正向道德偏差与助手倾向是当前方法的通病。
2. **解耦世界与行为建模可有效打破行为坍缩**：
   - 显式引入外部行为银行（behavioral banks）能引导LLM生成多样化反应。
3. **行为多样性引发风格多样性**：
   - 一级行为控制会自发激发二级语言风格差异（语调、节奏、情感）。
4. **该框架具备良好扩展性**：
   - 可轻松集成新的行为轴（如情绪调节、认知风格）。

---

### ⚠️ 局限性
1. **评估维度有限**：
   - 当前仅聚焦于道德与交互行为，未涵盖情绪动态、长期一致性等。
2. **依赖多项选择任务**：
   - 道德立场评估采用封闭式选项，可能忽略细微推理过程。
3. **潜在伦理风险**：
   - 显式控制道德立场可能导致生成冒犯性或有害内容，需谨慎部署。

---

### 🔮 未来工作方向
1. **拓展行为维度**：
   - 加入情绪调节、人格特质（Big Five）、文化背景等因素。
2. **更自然的评估方式**：
   - 设计自由形式道德对话任务，捕捉上下文敏感的伦理推理。
3. **可控性与安全性平衡**：
   - 开发可调节的“多样性-安全性”滑块机制，在创意自由与伦理约束间取得平衡。
4. **应用于游戏/NPC设计**：
   - 将 PERSONAWEAVER 集成进互动叙事系统，提升NPC的真实感与不可预测性。

---

## 总结一句话
> **PERSONAWEAVER 通过解耦世界与行为建模，首次系统性地解决了LLM角色生成中的“道德一致”与“助手顺从”偏差，开启了高多样性、高表现力虚拟角色生成的新路径。**

🔗 项目代码已开源：[https://github.com/mqraitem/Persona-Weaver](https://github.com/mqraitem/Persona-Weaver)

</details>

---

### 7. [EvolMem: A Cognitive-Driven Benchmark for Multi-Session Dialogue Memory](https://arxiv.org/abs/2601.03543)

**Authors**: Ye Shen, Dun Pei, Yiqiu Guo, Junying Wang, Yijin Guo, Zicheng Zhang, Qi Jia, Jun Zhou, Guangtao Zhai  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03543v1  

#### Abstract
Despite recent advances in understanding and leveraging long-range conversational memory, existing benchmarks still lack systematic evaluation of large language models(LLMs) across diverse memory dimensions, particularly in multi-session settings. In this work, we propose EvolMem, a new benchmark fo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# EvolMem: A Cognitive-Driven Benchmark for Multi-Session Dialogue Memory 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前对大语言模型（LLMs）和智能体系统（agent systems）的记忆能力评估存在两大核心缺陷：

1. **缺乏多会话（multi-session）场景的系统评估**：现有基准大多局限于单轮长文本输入（如NIAH、Ruler）或单一会话内的多轮对话，无法反映真实交互中跨会话累积记忆的需求。
2. **忽视非陈述性记忆（non-declarative memory）**：现有评测过度关注“回忆事实”这类陈述性记忆（declarative memory），而忽略了人类认知中的另一重要部分——非陈述性记忆，如习惯化（habituation）、隐性学习（learning）等。

这些问题导致模型在长期交互中的记忆能力被低估或误判。

---

### **提出了什么新方法或新思路**

本文提出 **EvolMem**，一个基于认知心理学的、面向多会话对话记忆的综合性评测基准，其核心创新如下：

- **认知驱动的记忆分类框架**：
  - 将记忆划分为 **declarative memory** 和 **non-declarative memory** 两大类。
  - 进一步细分为 **7种细粒度能力**：
    - *Declarative*: Retrieval, Summarization, Isolation, Inference, Reproduction
    - *Non-declarative*: Learning, Habituation

- **混合数据合成框架（Hybrid Data Synthesis Framework）**：
  - **Topic-Initiated Generation (TIG)**：从预定义主题出发生成结构化多会话对话。
  - **Narrative-Inspired Transformation (NIT)**：将现有长文本基准（如Ruler）转化为多会话对话，增强多样性。
  - 引入 **challenge injection** 机制，注入逻辑矛盾信息以提升挑战性。

- **多维度评估指标体系**：
  - 结果导向（Result-oriented）：Accuracy, Precision
  - 过程导向（Process-oriented）：LCS（Longest Common Subsequence）用于步骤顺序匹配
  - 开放式任务（Open-ended）：LLM-as-a-Judge + 定制化评分准则

---

### **相比现有方法的优势**

| 维度 | EvolMem | 现有基准（如NIAH, LongMemEval） |
|------|--------|-------------------------------|
| **会话模式** | ✅ 多会话（multi-session） | ❌ 单轮或多轮但单会话 |
| **记忆类型覆盖** | ✅ 声明性 + 非声明性 | ❌ 主要为声明性 |
| **能力细分** | ✅ 7种细粒度能力 | ❌ 能力划分粗略 |
| **评估方式** | ✅ 多样化指标（结果/过程/主观） | ❌ 多为精确匹配 |
| **数据多样性** | ✅ 混合生成 + 主题控制 | ❌ 数据来源单一 |

> ✅ 表示支持，❌ 表示不支持或弱支持

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **EvolMem 自建数据集**：
  - 包含 **1,600 条多会话对话**
  - 平均每条对话有 **6.82 个会话（session）** 和 **29.49 轮（turns）**
  - 数据来源：
    - **Topic-Initiated Generation (TIG)**：基于10个类别、50个具体话题生成（如写作、辅导、职业发展等）
    - **Narrative-Inspired Transformation (NIT)**：从 **Ruler** 基准中的 **Variable Tracking (VT)** 子集转化而来

### **实验设置**

- **评估对象**：
  - **7个前沿LLMs**：
    - Gemini-3-Pro, GPT-5.1, MiniMax-M2, Mistral-Large, DeepSeek-V3.2, Kimi-K2, Llama-4-Maverick
  - **4个Agent Memory Systems**（以DeepSeek-V3.2为底座）：
    - Structure-Augmented RAG 类：Mem0, HippoRAG
    - Agentic Memory 类：MemoryOS, A-MEM

- **评估指标分类**：
  - **Result-Oriented**：Accuracy, Precision
  - **Process-Oriented**：LCS（用于Reproduction和Learning任务）
  - **Open-Ended**：LLM-as-a-Judge + 加权评分准则

- **评估流程标准化**：
  - 所有Agent系统统一使用 DeepSeek-V3.2 作为backbone LLM
  - 统一embedding模型（all-MiniLM-L6-v2）
  - 固定超参数（如检索top-k、memory容量等）

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见Table 2）**

| Model | Overall Score | Declarative Avg | Non-declarative Avg |
|-------|---------------|------------------|----------------------|
| **Gemini-3-Pro** | **67.47%** | 69.16% | 65.77% |
| GPT-5.1 | 52.82% | 72.81% | 32.83% |
| MiniMax-M2 | 52.39% | 71.02% | 33.76% |
| Mistral-Large | 52.35% | 71.44% | 33.26% |
| DeepSeek-V3.2 | 52.18% | 66.94% | 37.41% |
| Llama-4-Maverick | 37.53% | 49.92% | 25.13% |

> ⚠️ 注意：尽管GPT-5.1在声明性记忆上表现优异，但在**Habituation**上仅得 **14.79%**，远低于Gemini-3-Pro的 **84.16%**

---

### **与基线方法的对比结果**

#### **LLM vs. Agent Systems（Table 3）**

| Agent System | Overall Score | vs. Base Model (DeepSeek-V3.2) |
|--------------|----------------|-------------------------------|
| MemoryOS     | 48.66%         | < 52.18%                      |
| A-MEM        | 44.54%         | < 52.18%                      |
| Mem0         | 42.44%         | < 52.18%                      |
| HippoRAG     | 33.11%         | < 52.18%                      |

> 🔴 **所有Agent系统均未超越其底座LLM（DeepSeek-V3.2）的整体性能**，说明当前记忆机制设计未能有效提升记忆能力。

#### **效率对比（Table 4）**

| Agent System | Generation Speed (s/sample) |
|--------------|------------------------------|
| Mem0         | 30.17                        |
| HippoRAG     | 92.76                        |
| MemoryOS     | 183.24                       |
| A-MEM        | **492.60**                   |

> ⏱️ A-MEM 的延迟是 Mem0 的 **16.3倍**，表明agentic memory机制带来显著效率瓶颈。

---

### **消融实验与敏感性分析**

- **LLM生成器敏感性测试**（Fig. 3）：
  - 使用不同LLM（DeepSeek, GPT-4.1, Grok）生成训练数据时，模型排名相关性差异显著。
  - DeepSeek生成的数据与其他高度一致（Spearman ρ=0.90），而GPT-4.1相关性极低（ρ=0.05）。
  - ➜ 支持使用**多LLM协同生成**以提高数据多样性和评估公平性。

- **复杂度演化实验**（Fig. 4）：
  - 随着会话数增加，DeepSeek-V3.2性能持续下降。
  - ➜ 证明EvolMem具备**可扩展难度演化能力**，适合长期追踪模型进步。

---

## 4. 关键结论和发现

### **主要发现**

1. **No LLM is universally superior**：
   - 没有任何一个LLM在所有记忆维度上都领先。
   - 例如：Gemini擅长Inference和Habituation，但Retrieval较弱；Mistral擅长Retrieval但Inference差。

2. **Non-declarative memory is severely underdeveloped**：
   - 所有模型在 **Habituation** 上表现最差（平均仅29.13%），且方差极大（标准差24.57%）。
   - 表明当前训练范式严重忽视长期行为一致性建模。

3. **Agent memory mechanisms do not help — often hurt**：
   - 所有Agent系统**未能超越其底座LLM**，甚至在多数任务上更差。
   - 特别是在 **non-declarative memory** 上性能急剧下降（如A-MEM在Habituation上仅9.37%）。

4. **Efficiency is a major bottleneck**：
   - Agentic memory系统（如A-MEM）引入巨大推理延迟，不适合实时应用。

5. **Memory is distinct from general capabilities**：
   - EvolMem得分与Code（ρ=0.75）、Reasoning（ρ=0.71）强相关，但与Knowledge（ρ=0.18）几乎无关。
   - ➜ 说明该基准测的是**上下文理解与记忆利用能力**，而非静态知识储备。

---

### **方法的局限性**

- **依赖LLM生成数据**：虽经人工验证（有效性95.33%），但仍可能存在幻觉或偏差。
- **非声明性任务难以完全客观评估**：如Habituation依赖LLM-as-a-Judge，主观性强。
- **尚未覆盖所有非声明性记忆类型**：如情感记忆、程序性技能等未纳入。

---

### **未来工作方向**

1. **联合优化Agent的有效性与效率**：探索轻量级、高响应速度的记忆架构。
2. **提升non-declarative memory建模能力**：设计专门针对habituation和implicit learning的训练目标。
3. **动态演化基准**：构建随时间自动升级难度的“活”基准（living benchmark）。
4. **人机协同评估**：引入更多人类标注以校准LLM-as-a-Judge的偏见。

---

> 📌 **总结一句话**：  
> **EvolMem揭示了一个关键现实——当前LLMs和Agent系统在跨会话、隐性记忆任务上仍极为脆弱，而现有的“记忆增强”机制并未真正解决问题，反而带来了效率代价。**

</details>

---

### 8. [LLM-MC-Affect: LLM-Based Monte Carlo Modeling of Affective Trajectories and Latent Ambiguity for Interpersonal Dynamic Insight](https://arxiv.org/abs/2601.03645)

**Authors**: Yu-Zheng Lin, Bono Po-Jen Shih, John Paul Martin Encinas, Elizabeth Victoria Abraham Achom, Karan Himanshu Patel, Jesus Horacio Pacheco, Sicong Shao, Jyotikrishna Dass, Soheil Salehi, Pratik Satam  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03645v1  

#### Abstract
Emotional coordination is a core property of human interaction that shapes how relational meaning is constructed in real time. While text-based affect inference has become increasingly feasible, prior approaches often treat sentiment as a deterministic point estimate for individual speakers, failing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-MC-Affect: LLM-Based Monte Carlo Modeling of Affective Trajectories and Latent Ambiguity for Interpersonal Dynamic Insight

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于文本的情感分析方法通常将情感视为**确定性的点估计**（deterministic point estimate），忽略了以下关键因素：
- **主观性和感知模糊性**（perceptual ambiguity）：同一句话可能被不同人解读为不同情绪。
- **动态耦合性**（sequential coupling）：人际互动中的情感是相互影响、时序关联的，而非孤立存在。

这些简化限制了对真实社交互动中情感协调（emotional coordination）的深入理解，尤其是在教育等需要精细情感交互的场景中。

### 提出的新方法与创新思路
作者提出 **LLM-MC-Affect** —— 一种基于大语言模型（LLM）的蒙特卡洛（Monte Carlo）情感建模框架，其核心思想包括：

- **将情感建模为连续潜在概率分布**  
  不再输出单一情感标签，而是通过多次随机采样（stochastic decoding）构建一个**潜藏情感分布** $ p(e|u) $，从而捕捉情感的中心趋势（均值）和不确定性（方差）。

- **利用LLM的随机解码进行Monte Carlo估计**  
  在非零温度下对同一话语执行 $ K $ 次独立推理，得到多个情感评分，视作从隐含情感分布中抽取的样本，进而估算均值与方差。

- **构建不确定性感知的情感轨迹**（uncertainty-aware affective trajectories）  
  将每轮对话的情感均值串联成时间序列轨迹，并用方差表示“情感模糊度”，实现对个体情感演变的高保真刻画。

- **引入人际动态分析层**  
  基于教师与学生的情感轨迹，计算：
  - **归一化交叉相关函数**（Normalized Cross-Correlation Function, NCCF）
  - **最优滞后**（Optimal Lag $ L^* $）以识别谁主导情感流动
  - **斜率趋势指标**（slope-based trend）反映长期情感发展
  并结合三者形成可解释的互动模式分类（如有效支架、共享疲劳等）。

### 相比现有方法的优势
| 维度 | 传统方法 | LLM-MC-Affect |
|------|--------|---------------|
| 情感表示 | 确定性标签（如正面/负面） | 连续概率分布（含均值与方差） |
| 主观性处理 | 忽略或平均掉差异 | 显式量化“情感模糊性” |
| 动态建模 | 静态逐句判断 | 构建纵向情感轨迹 |
| 人际耦合分析 | 缺乏系统建模 | 支持滞后、相关性、趋势联合分析 |
| 可扩展性 | 依赖人工标注或生物传感器 | 完全基于文本，无需微调（zero-shot），适用于大规模自然对话 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Google's Education Dialogue Dataset**（Shani et al., 2024）  
  包含多轮模拟的教学对话，涵盖多个主题（如“The Cold War”, “Personification”等）。  
  数据为**仿真生成**，便于控制学生/教师的人设偏好与反应机制，避免真实课堂数据的伦理与隐私问题。

### 实验设置
- **模型选择**：
  - 商业闭源模型：`GPT-4.1`, `GPT-3.5-Turbo`
  - 开源模型：`Llama 3.3 70B`, `Gemma 3 4B`, `Phi-4 14B`, `GPT-OSS 120B`

- **解码参数**：
  - 温度 $ T \in [0.1, 1.0] $，用于控制生成多样性
  - 每个utterance执行 $ K=20 $ 次Monte Carlo采样

- **统一心理测量提示**（psychometric prompt, `pemo`）：
  - 引导LLM作为心理学家进行情感打分
  - 输出范围：0（最积极）到5（最消极），步长0.5
  - 中性值为2.5
  - 要求考虑上下文连续性与情感演化

- **后处理**：
  - 对原始得分进行极性映射至 $[-1, 1]$ 区间，便于后续分析
  - 方差同步缩放以保持单位一致性

### 评估指标
- **情感轨迹稳定性**：在不同温度下的均值波动情况
- **情感模糊性量化**：Monte Carlo采样的方差大小
- **跨模型一致性比较**：不同LLM生成轨迹的形态差异
- **人际动态指标**：
  - 最优滞后 $ L^* $
  - 最大交叉相关系数 $ R_{rs}(L^*) $
  - 轨迹斜率 $ \beta_T, \beta_S $

### 基线方法对比
本文未直接对比传统NLP情感分类器（如BERT-based classifiers），而是强调与以下两类工作的区别：
- **传统确定性情感分析管道**（如Liu et al., 2023a; Gao et al., 2022）
- **依赖生物信号的情感同步研究**（如Qi et al., 2024; Bevilacqua et al., 2019）

LLM-MC-Affect的优势在于**仅用文本即可实现类似甚至更丰富的动态洞察**，且更具可部署性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与发现

#### （1）情感分布建模的有效性
- 图2a显示，在低温度（$T=0.1$）时，LLM对某些语句产生**双峰分布**（bimodal perception），表明存在多种合理的情感解读。
- 随着温度升高，分布趋于平滑，方差增加，揭示了语言表达的内在歧义。
- **方差被成功用作“情感模糊性”的代理指标**。

#### （2）情感轨迹的鲁棒性
- 尽管温度变化显著影响采样方差（如Utterance 6的 $\sigma^2$ 从0.010升至0.024），但**均值情感状态极其稳定**（波动范围：-0.11 ~ -0.26）。
- 图2b显示所有温度下的教师情感轨迹高度一致，验证了MC估计的可靠性。

#### （3）跨模型行为差异
| 模型 | 表现特点 |
|------|---------|
| `GPT-4.1` | 情感分辨率最高，能准确捕捉早期负面情绪并建模回升过程 |
| `GPT-3.5-Turbo` | 结构相似但不确定性更大（uncertainty band更宽） |
| `Llama 3.3 70B` | **极端正向偏见**，完全忽略初期负面情绪（alignment for harmlessness所致） |
| `GPT-OSS 120B`, `Gemma 3 4B`, `Phi-4 14B` | 普遍表现出保守估计倾向，低估情感转折点 |

> ✅ **结论**：并非所有LLM都适合高保真情感情绪建模；`GPT-4.1`表现最优。

#### （4）人际动态分析结果（案例研究）
使用`GPT-4.1` ($T=0.7$) 分析多个教学对话，得出如下典型模式：

| 对话主题 | $L^*$ | $R_{rs}(L^*)$ | $\beta_T$ | $\beta_S$ | 解释 |
|--------|-------|----------------|-----------|-----------|------|
| **Personification** | +1 | 0.999 | +0.162 | +0.253 | **Effective Scaffolding**（教师引领的正向感染） |
| **The Respiratory System** | +3 | 1.000 | +0.065 | +0.166 | 同上 |
| **Achilles** | +1 | 0.886 | +0.071 | +0.057 | 同上 |
| **The Cold War** | 0 | 0.991 | -0.177 | -0.136 | **Shared Fatigue**（同步动机衰退） |
| **World War 2** | -2 | 1.000 | -0.124 | -0.267 | **Feedback Burnout**（学生驱动的共同下滑） |

这些模式与预设的学生/教师人设高度吻合，证明了该方法的**解释力强、符合直觉**。

---

## 4. 关键结论和发现

### 主要发现
1. **情感不应被视为确定性标签**，而应建模为带有不确定性的潜在分布。
2. **LLM的随机解码可用于近似人类情感判断的主观变异性**，无需昂贵的人工标注。
3. **通过Monte Carlo采样获得的情感轨迹具有良好的稳定性与解释性**，即使在高随机性条件下也能保留核心情感信号。
4. **结合cross-correlation、lag和slope指标，可以系统识别出多种可解释的人际互动模式**，如有效支架、反馈倦怠、动态补偿等。
5. **不同LLM在情感能力上存在显著差异**，`GPT-4.1`在分辨率和真实性方面优于其他模型。

### 方法的局限性
1. **方差解释的模糊性**：Monte Carlo方差可能混杂了多种不确定性来源（epistemic vs. aleatoric），不能完全等同于人类感知的“模糊性”。
2. **相关不等于因果**：交叉相关只能反映顺序对齐，无法推断因果影响。
3. **短对话限制统计稳健性**：当前分析基于较短对话，长程动态需采用滑动窗口或分层建模。
4. **计算成本较高**：每次情感评估需运行 $K=20$ 次LLM推理，不适合实时应用。

### 未来工作方向
- 探索轻量级、任务专用的**情感敏感型LLM**（affect-sensitive LLMs）以提升效率。
- 发展**滑动窗口或多尺度分析技术**，适应更长对话。
- 将框架拓展至更多领域（如心理咨询、客户服务、团队协作）。
- 结合少量人工标注进行**不确定性校准**，增强方差的可解释性。

---

> 🔍 **总体评价**：  
> LLM-MC-Affect 是一项将**概率建模思想**与**LLM随机性**巧妙结合的工作，突破了传统情感分析的静态范式，为理解复杂社会互动提供了新的工具路径。它不仅适用于教育场景，也为更广泛的**computational social science** 和 **affect-aware AI systems** 提供了可扩展的技术基础。

</details>

---

### 9. [Rethinking Recurrent Neural Networks for Time Series Forecasting: A Reinforced Recurrent Encoder with Prediction-Oriented Proximal Policy Optimization](https://arxiv.org/abs/2601.03683)

**Authors**: Xin Lai, Shiming Deng, Lu Yu, Yumin Lai, Shenghao Qiao, Xinze Zhang  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03683v1  

#### Abstract
Time series forecasting plays a crucial role in contemporary engineering information systems for supporting decision-making across various industries, where Recurrent Neural Networks (RNNs) have been widely adopted due to their capability in modeling sequential data. Conventional RNN-based predictor...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rethinking Recurrent Neural Networks for Time Series Forecasting

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的基于 **Recurrent Neural Networks (RNNs)** 的时间序列预测模型通常采用 **encoder-only** 架构，通过滑动历史窗口进行多步预测。然而，这类方法存在以下局限性：
- 将所有时间步和隐藏状态视为同等重要，忽略了它们对预测的不同贡献；
- 缺乏动态适应机制，难以捕捉时变的时间依赖模式；
- 固定的输入选择、隐藏连接和输出目标策略限制了模型的灵活性。

这些因素导致模型在复杂非平稳时间序列上的表现不佳。

### 提出的新方法
为解决上述问题，作者提出了一种名为 **Reinforced Recurrent Encoder with Prediction-oriented Proximal Policy Optimization (RRE-PPO4Pred)** 的新框架，其核心创新包括：

#### （1）Reinforced Recurrent Encoder (RRE)
将 RNN 编码器的内部适应过程建模为一个 **Markov Decision Process (MDP)**，构建了一个统一的决策环境，能够同时学习三种关键操作：
- **Input Feature Selection**：决定每个时间步是否使用当前输入；
- **Hidden Skip Connection**：动态选择跳过多少步以建立长距离记忆连接；
- **Output Target Selection**：决定是否将某一步的输出纳入训练损失。

该框架适用于各种 RNN 变体（如 LSTM, GRU, xLSTM 等），具有良好的通用性。

#### （2）Prediction-oriented Proximal Policy Optimization (PPO4Pred)
一种改进的 **PPO** 算法，专为预测任务设计，包含两个关键改进：
- **Transformer-based Agent**：利用自注意力机制增强代理对时间依赖性的建模能力；
- **Dynamic Transition Sampling (DTS)**：引入优先级采样策略，聚焦于高误差或高不确定性样本，提升训练效率。

#### （3）Co-evolutionary Optimization Paradigm
提出了一种异步协同进化优化范式，交替更新 RNN 预测器和强化学习代理，实现两者的联合演化，从而提升整体预测能力和适应性。

### 相比现有方法的优势
- **更强的适应性**：通过动态决策机制，模型能根据不同输入模式自适应调整架构；
- **更高的预测精度**：在多个真实世界数据集上显著优于传统 RNN 和部分 Transformer 模型；
- **更优的稳定性**：消除了 Seq2Seq 中的误差累积和暴露偏差问题；
- **端到端可训练**：三个组件（输入、隐藏、输出）被统一在一个强化学习框架下联合优化。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在五个真实世界的公开基准数据集上进行，涵盖交通、电力、气候等多个领域：

| 数据集 | 采样频率 | 时间步数 | 变量数 $D_{in}$ | 输入长度 $T$ | 预测范围 $H$ |
|--------|----------|-----------|------------------|---------------|----------------|
| **Traffic** | 1小时 | 17,544 | 862 | 96 | [24, 48, 96] |
| **Electricity** | 1小时 | 26,304 | 321 | 96 | [24, 48, 96] |
| **ETTh** | 1小时 | 17,420 | 7 | 96 | [24, 48, 96] |
| **Weather** | 10分钟 | 52,696 | 21 | 96 | [24, 48, 96] |
| **ILI** | 1周 | 966 | 7 | 48 | [12, 24, 48] |

### 实验设置
- 所有数据按 7:1:2 划分为训练集、验证集和测试集；
- 使用 **Min-Max** 归一化；
- 评估指标：**MSE** 和 **MAE**；
- 每个实验运行 10 次取平均值；
- GPU：NVIDIA RTX A4500。

### 基线方法对比
共比较了六类基线方法：

| 类别 | 方法 | 描述 |
|------|------|------|
| 固定策略 | `Naive-all`, `Naive-last` | 使用所有或仅最后一步输出作为监督信号 |
| 固定跳跃 | `DilatedRNN` | 固定间隔的隐藏状态跳跃连接 |
| 启发式搜索 | `EBPSO` | 基于粒子群优化的输入特征选择 |
| 强化学习 | `LSTMjump` | 使用 PG 学习输入跳过策略 |
| 强化学习 | `PGLSTM` | 使用 PG 学习隐藏状态跳跃 |
| Transformer | `iTransformer`, `PatchTST` | 当前最先进的 Transformer 模型 |

此外，还进行了与不同 RNN 主干网络（RNN, GRU, LSTM, xLSTM 等）的组合实验。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 ETTh 数据集为例）
| 方法 | H=24 (MSE) | H=48 (MSE) | H=96 (MSE) |
|------|------------|------------|------------|
| iTransformer | 0.3540 | 0.3790 | 0.3921 |
| PatchTST | 0.3195 | 0.3477 | 0.3746 |
| xLSTM (Naive-all) | 0.5620 | 0.5962 | 0.6755 |
| **xLSTM + RRE-PPO4Pred** | **0.3075** | **0.3378** | **0.3607** |

> ✅ 在所有预测范围内均优于最先进的 Transformer 模型！

### 与其他 RNN 基线的整体对比（平均 MSE 改进）
| 对比类别 | 平均 MSE 改进幅度 |
|---------|--------------------|
| vs. `Naive-all/last`, `DilatedRNN` | **23.7% ~ 53.08%** |
| vs. `EBPSO`（启发式） | **23.21% ~ 28.36%** |
| vs. `LSTMjump`, `PGLSTM`（RL） | **19.04% ~ 27.80%** |

> 📊 图表显示 RRE-PPO4Pred 在所有数据集和预测范围下均取得最佳性能。

### 消融实验结果（Ablation Study）
通过逐步移除组件验证各模块有效性：

| 消融变体 | MSE 改进率（vs. TA-PSO） | MAE 改进率（vs. TA-PSO） |
|----------|--------------------------|----------------------------|
| `TA-PSO`（PSO优化三元动作） | — | — |
| `RRE-PG`（Policy Gradient） | +7.14% ~ 13.38% | +9.01% ~ 11.78% |
| `RRE-DQN`（Deep Q-Network） | +5.08% ~ 10.81% | +6.98% ~ 10.80% |
| `RRE-PPO`（标准PPO） | +5.79% ~ 8.95% | +5.45% ~ 7.30% |
| **RRE-PPO4Pred（完整方法）** | **+24.99% ~ 37.01%** | **+24.69% ~ 31.25%** |

> 🔍 结论：
> - 所有 RL 方法均优于启发式搜索（PSO）；
> - PPO 表现优于 PG 和 DQN；
> - 加入 **Transformer-based Agent** 和 **DTS** 后性能进一步提升。

---

## 4. 关键结论和发现

### 主要发现
1. **RNN 仍有巨大潜力**：通过合理的结构优化，RNN 模型可以超越当前主流的 Transformer 模型；
2. **动态决策机制至关重要**：统一优化输入、隐藏和输出策略显著提升了模型适应性和预测精度；
3. **强化学习是有效工具**：将 RNN 架构搜索建模为 MDP 是可行且高效的；
4. **协同进化范式有效收敛**：代理与预测器的交替训练实现了稳定且持续的性能提升；
5. **误差分布更集中**：RRE-PPO4Pred 的预测错误方差更小，鲁棒性更强。

### 方法的局限性
- **计算开销较大**：相比普通 RNN，训练时间和参数量有所增加（约 6–10 倍）；
- **推理阶段仍需代理参与**：每次预测都需要调用策略网络，增加了部署复杂度；
- **超参数敏感**：如 skip window size $K$ 需要仔细调节（文中建议 $K=8\sim10$）；
- **目前仅用于预测**：尚未扩展到生成、异常检测等其他任务。

### 未来工作方向
- 探索更高效的代理架构（如轻量化 Transformer 或知识蒸馏）；
- 研究迁移学习机制，使代理可在不同数据集间复用；
- 将该框架应用于其他序列建模任务（如 NLP、语音识别）；
- 开发无需在线决策的“固化”版本，便于边缘设备部署。

---

> 💡 **总结一句话**：  
> 本文提出的 **RRE-PPO4Pred** 框架通过将 RNN 编码器重构为一个可学习的强化学习环境，实现了对输入、隐藏和输出策略的联合动态优化，在多个真实数据集上不仅大幅超越传统 RNN 方法，甚至超过了先进的 Transformer 模型，重新确立了 RNN 在时间序列预测中的竞争力。

</details>

---

### 10. [EDCO: Dynamic Curriculum Orchestration for Domain-specific Large Language Model Fine-tuning](https://arxiv.org/abs/2601.03725)

**Authors**: Jing-Cheng Pang, Liu Sun, Chang Zhou, Xian Tang, Haichuan Ma, Kun Jiang, Jianlong Wang, Kai Zhang, Sijie Wu, Haoran Cai, Chenwei Wu, Xubin Li, Xin Chen  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03725v1  

#### Abstract
Domain-specific large language models (LLMs), typically developed by fine-tuning a pre-trained general-purpose LLM on specialized datasets, represent a significant advancement in applied AI. A common strategy in LLM fine-tuning is curriculum learning, which pre-orders training samples based on metri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# EDCO: Dynamic Curriculum Orchestration for Domain-specific Large Language Model Fine-tuning —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **静态课程学习（Static Curriculum Learning）的局限性**：传统 LLM 微调通常采用随机采样或基于启发式难度排序（如长度、答案复杂度、困惑度 PPL）的固定训练顺序，无法适应模型在微调过程中能力的动态变化。
- **熵坍塌（Entropy Collapse）问题**：尤其在强化学习微调（RLFT）中，模型推理熵迅速下降，导致过早收敛、探索不足，限制了长期性能提升。
- **领域数据稀缺且昂贵**：高质量领域数据有限，因此需要最大化每一步训练的有效性。

### 🚀 提出的新方法：EDCO（Entropy-based Dynamic Curriculum Orchestration）
EDCO 是一种基于**推理熵（inference entropy）**的动态课程编排框架，其核心思想是：
- **持续识别最具挑战性的样本**：通过估计每个样本的推理熵，优先选择使模型最不确定的样本进行训练。
- **构建“反向课程”（Reverse Curriculum）**：不同于传统的“由易到难”，EDCO 主张从高熵（难）样本开始并动态调整，确保模型始终处于学习前沿。

#### 三大核心技术组件：
1. **高效熵估计模块（Efficient Entropy Estimator）**
   - 引入 **Quick-Answer Prompting (QAP)**：引导模型快速输出答案而非长链推理，使前缀 token 更能反映语义不确定性。
   - 提出 **Prefix Entropy Approximation**：仅用输出序列前 $L$ 个 token（如 50–128）估算全序列熵，显著降低计算开销。

2. **熵驱动的动态课程生成器（Entropy-based Curriculum Generator）**
   - 在每个训练阶段重新评估所有未充分掌握样本的推理熵。
   - 动态选取熵值最高的 Top-$N$ 样本组成下一阶段训练集。

3. **兼容多种微调范式的 LLM 训练器**
   - 支持 Supervised Fine-Tuning (SFT) 和 Reinforcement Learning Fine-Tuning (RLFT)，均可受益于高熵样本带来的持续探索。

### 🔍 相比现有方法的优势
| 对比维度 | 传统方法（如 RS, Length, AC, PPL） | EDCO |
|--------|-------------------------------|------|
| 课程策略 | 静态预设，不可变 | 动态更新，随模型状态演化 |
| 样本选择依据 | 启发式指标（长度、答案句数、PPL） | 模型实时推理熵（更贴近学习需求） |
| 探索维持能力 | 易发生熵坍塌，尤其在 RLFT 中 | 主动维持高熵环境，延缓坍塌 |
| 通用性 | 多为任务特定设计 | 跨领域、跨模型（Qwen/Llama）、跨范式（SFT/RLFT）有效 |
| 效率 | 无额外开销 | 引入可接受的计算开销（+10% wall-clock），但带来显著性能增益 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验覆盖四个专业领域，验证泛化能力：

| 数据集 | 领域 | 描述 |
|-------|------|------|
| **Wireless Communication** | 无线通信 | 自建数据集，12K QA 对，来自产品文档和技术方案，涵盖 5G/Wi-Fi/GTP 协议等 |
| **Datacom** | 数据通信 | 自建数据集，12K QA 对，涉及路由器、交换机、Segment VXLAN、BGP EVPN 等网络技术 |
| **MedQA (Jin et al., 2021)** | 医学 | 来自美国医师执照考试的多选题，含临床案例描述，测试医学知识与多跳推理 |
| **JEC-QA (Zhong et al., 2020)** | 法律 | 中国司法考试题库，包含单选与多选题，需法律条文理解与案例分析 |

> 所有任务均保留 230 道高难度题目作为测试集。

### ⚙️ 实验设置
- **基础模型**：
  - Qwen3-4B
  - Llama3.2-3B
- **微调方式**：
  - **SFT**：最小化交叉熵损失
  - **RLFT**：使用 GRPO 算法优化奖励信号（Deepseek-V3 自动生成验证 / 规则-based 奖励）
- **评估指标**：
  - **Answer Accuracy**：最终答案匹配正确率（主要指标）
  - **Inference Entropy**：用于分析训练过程中的探索行为
  - **Wall-clock Time / Per-sample Estimation Time**：衡量效率

### 🆚 基线方法对比
| 类型 | 方法 | 说明 |
|-----|------|------|
| **无课程** | Random Sampling (RS) | 标准随机采样 |
| **静态课程** | Length, AC, PPL | 分别按输入长度、答案句子数、初始模型困惑度排序 |
| **动态课程** | SEC, Dynamic-PPL | SEC 使用 bandit 学习策略；Dynamic-PPL 定期更新 PPL 排序 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）通信领域（Qwen3-4B）—— RLFT 设置下表现

| 方法 | Wireless Acc (%) | Datacom Acc (%) |
|------|------------------|-----------------|
| No Training | 35.22 | 40.00 |
| RS | 34.35 | 40.43 |
| PPL | 33.91 | 44.78 |
| **EDCO** | **38.70** | **46.96** |

> ✅ EDCO 在两个领域均取得最高精度，在 Datacom 上比最强静态基线（PPL）提升 **+2.18%**

#### （2）通信领域（Qwen3-4B）—— SFT 设置下表现

| 方法 | Wireless Acc (%) | Datacom Acc (%) |
|------|------------------|-----------------|
| RS | 31.7 | 33.0 |
| PPL | 32.0 | 33.0 |
| **EDCO** | **33.7** | **36.3** |

> ✅ 在 SFT 下仍显著优于各类基线，尤其避免了某些静态课程（如 AC）导致性能退化的问题

#### （3）跨领域迁移效果（Llama3.2-3B）

| Dataset | RS | PPL | EDCO |
|--------|----|-----|-------|
| MedQA | 32.9 | 24.6 | **36.7** |
| JEC-QA | 16.2 | 12.4 | **17.4** |

> ✅ EDCO 在医学与法律领域也全面超越基线，证明其**跨领域普适性**

#### （4）与先进动态基线比较（Datacom + Qwen3-4B）

| 方法 | Accuracy (%) |
|------|--------------|
| RS | 40.43 |
| SEC (bandit-based) | 34.78 |
| Dynamic-PPL | 41.30 |
| **EDCO** | **47.00** |

> ✅ EDCO 显著优于两种动态课程方法，表明“基于推理熵”的选择机制优于“基于 bandit 决策”或“定期更新 PPL”

---

### 🔍 消融实验结果

#### （1）熵估计准确性（Prefix vs Full-sequence）
- 使用 128-token 前缀估算的熵与完整序列熵之间的 **Pearson 相关系数达 0.63**
- 表明前缀熵可作为可靠代理信号

#### （2）计算效率提升
| 方法 | 单进程耗时 (秒/样本) | 并行（8 NPU）耗时 |
|------|--------------------|------------------|
| Full-sequence | 2.24 | 0.24 |
| **Prefix-based (Ours)** | **0.37** | **0.04** |

> ⏱️ **计算时间减少 83.5%**，使得动态课程成为实用可行的技术

#### （3）前缀长度影响（Ablation on $L$）
- 当 prefix length ≥ 50 时，熵趋势趋于稳定
- 推荐默认配置：**50–128 tokens**

#### （4）Quick-Answer Prompting (QAP) 的作用
- 移除 QAP 后，prefix 与 full-sequence 熵的相关性从 **0.63 降至 0.32**
- 证明 QAP 对提升熵估计质量至关重要

#### （5）是否应避免最高熵样本？（Moderate-entropy Window 实验）
- 尝试选择中等熵区间（Top 5–11.67%）而非最高熵（Top 0–6.67%）
- 结果显示：**最高熵策略得分更高（46.96 vs 44.78）**
- 结论：高熵样本并非噪声或 OOD 错误，而是真正具有信息量的学习难点

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **推理熵是有效的课程信号**：
   - 维持高推理熵有助于防止模型过早收敛，促进持续探索，尤其是在 RLFT 场景中。
2. **动态课程优于静态课程**：
   - 固定顺序（如 easy-to-hard）可能适得其反，特别是在专业领域中，“短”不等于“简单”。
3. **EDCO 具备强泛化能力**：
   - 在通信、医学、法律等多个领域，以及 Qwen 和 Llama 不同架构上均一致有效。
4. **高效熵估算是可行的**：
   - 结合 QAP 与 prefix approximation 可实现高精度、低延迟的在线熵估计，支持近实时课程更新。

### ⚠️ 局限性
1. **周期性计算开销仍存在**：
   - 尽管已大幅优化，但仍引入约 10% 的 wall-clock 时间增长，对超大规模数据集可能构成负担。
2. **固定更新间隔不够智能**：
   - 当前采用固定训练步数触发课程更新，未来可考虑基于熵变化率或性能平台期自适应调度。
3. **依赖高质量预处理数据**：
   - 方法假设数据已清洗，若存在大量歧义或错误样本，高熵可能反映的是噪音而非挑战。

### 🔮 未来工作方向
1. **轻量化熵预测机制**：
   - 设计无需完整前向传播即可预测样本熵的小型辅助网络或元学习器。
2. **自适应课程调度器**：
   - 引入监控机制，在检测到熵骤降或性能停滞时自动触发课程重组。
3. **扩展至更多领域与低资源语言**：
   - 验证 EDCO 在科学、金融、小语种等场景下的有效性。
4. **结合主动学习（Active Learning）**：
   - 将 EDCO 与数据标注流程整合，形成闭环的“学习-选择-标注”系统。

---

## 总结

📌 **EDCO 提出了一种全新的视角：将“最难的样本”视为最有价值的学习材料，并通过动态课程机制让模型始终保持在认知边缘。**

它不仅解决了传统课程学习僵化的问题，还为缓解 RLFT 中的熵坍塌提供了有效路径。其实验充分、设计严谨，在多个真实领域任务中展现出稳定且显著的性能提升，是一项兼具理论深度与工程实用性的创新工作。

</details>

---

### 11. [Quantum vs. Classical Machine Learning: A Benchmark Study for Financial Prediction](https://arxiv.org/abs/2601.03802)

**Authors**: Rehan Ahmad, Muhammad Kashif, Nouhaila Innan, Muhammad Shafique  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03802v1  

#### Abstract
In this paper, we present a reproducible benchmarking framework that systematically compares QML models with architecture-matched classical counterparts across three financial tasks: (i) directional return prediction on U.S. and Turkish equities, (ii) live-trading simulation with Quantum LSTMs versu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《Quantum vs. Classical Machine Learning: A Benchmark Study for Financial Prediction》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前量子机器学习（QML）在金融预测领域的研究存在以下关键问题：
- **特征表达不足**：多数QML研究仅使用简单或单一股票的历史价格，缺乏技术指标、跨资产信号等丰富特征。
- **基线不匹配**：常将浅层QNN与深层LSTM等结构差异巨大的经典模型对比，导致不公平比较。
- **评估指标不贴合实际**：多依赖RMSE、MAE等统计误差指标，而非交易相关的风险调整收益（如Sharpe Ratio）或波动率专用指标（如QLIKE）。
- **任务覆盖不全**：缺乏对方向性分类、实时交易模拟、波动率预测等核心金融任务的系统性基准测试。

### 提出的新方法与创新思路
本文提出一个**可复现、标准化的基准框架**（reproducible benchmarking framework），用于公平、系统地比较QML与经典ML在金融预测中的表现。其核心创新包括：

- **架构匹配对比**（Architecture-matched comparison）  
  对每类QML模型（QNN, QLSTM, QSVR）均设计参数量、归纳偏置相近的经典对应模型（ANN, LSTM, SVR），确保比较聚焦于“量子 vs 经典”而非“结构差异”。

- **统一的任务设计与评估体系**  
  覆盖三大核心金融任务：
  - **Directional Return Prediction**（方向性回报预测）
  - **Live Trading Simulation**（实盘交易模拟）
  - **Realized Volatility Forecasting**（已实现波动率预测）

- **真实金融价值导向的评估**  
  引入交易成本、风险调整收益（Sharpe/Sortino）、最大回撤（MaxDD）等经济指标，评估模型的实际盈利能力，而不仅是预测准确率。

- **多市场、多维度特征测试**  
  在美国（成熟市场）和土耳其（新兴市场）股票上进行实验，涵盖低维（3-D）、中维（7-D）、高维（64-D）三种特征空间，检验模型泛化能力。

### 相比现有方法的优势
- **更公平**：通过参数匹配、相同数据划分、统一评估协议，避免了以往研究中的偏差。
- **更全面**：首次在同一框架下系统评估QML在三大金融任务上的表现。
- **更具实践意义**：从“预测精度”转向“经济价值”，贴近真实投资决策流程。
- **可复现性强**：开源代码与详细超参搜索空间，便于后续研究复现与扩展。

---

## 2. 核心实验方法和设置

### 数据集
| 任务 | 数据来源 | 具体资产 |
|------|----------|---------|
| **Directional Classification** | Yahoo Finance API | - **美国市场**：S&P 500、AAPL、BA、GILD、DVN、LNC<br>- **土耳其市场**：KCHOL.IS、GARAN.IS、TUPRS.IS、ULKER.IS、TCELL.IS |
| **Live Trading Simulation** | Yahoo Finance API | S&P 500指数（2008–2024），划分为四个历史阶段：<br>- F1: 全球金融危机 (2008–2009)<br>- F2: 疫情前 (2018–2019)<br>- F3: 疫情冲击与复苏 (2020–2021)<br>- F4: 后疫情时代 (2022–2024) |
| **Volatility Forecasting** | Yahoo Finance API | S&P 500、AAPL、KCHOL、GARAN |

### 特征工程
- **方向性分类**：
  - 低维（3-D）：RSI、Stochastic Oscillator等技术指标（土耳其股）
  - 中维（7-D）：六大全球股指滞后收益（预测S&P 500方向）
  - 高维（64-D）：个股过去8日收益 + 8大指数过去7日收益
- **实时交易**：MACD、RSI、前一日收益率等4维时序特征，构建10天滑动窗口。
- **波动率预测**：过去p日收益率 + 过去q日已实现方差，目标为未来5日已实现方差（5-day realized variance）。

### 评估指标
| 任务 | 主要评估指标 |
|------|-------------|
| **Directional Classification** | Accuracy, AUC, Precision, Recall |
| **Live Trading Simulation** | Annualized Return (ARC), Annualized Standard Deviation (ASD), Sharpe Ratio, Sortino Ratio, Max Drawdown |
| **Volatility Forecasting** | QLIKE（主指标）、MSE、R²、Directional Accuracy (DirAcc)、Diebold-Mariano Test (DM-p) |

### 基线方法对比
| QML模型 | 经典基线模型 | 匹配原则 |
|--------|------------|---------|
| QNN | ANN | 参数量相近，结构对齐（如Hybrid-SQ QNN vs 浅层ANN） |
| QLSTM | LSTM | 隐藏状态大小、层数、总参数量匹配 |
| QSVR | SVR (Linear, Poly, RBF) + GARCH(1,1) | 使用预计算核矩阵，保持优化流程一致 |

---

## 3. 主要实验结果和性能指标

### (1) Directional Return Prediction
| 资产 | 模型 | Accuracy ↑ | AUC ↑ | 相对提升 |
|------|------|-----------|-------|---------|
| **AAPL** | QNN (Hybrid-SQ) | **0.606** | **0.635** | +3.4pp Acc, +3.8 AUC vs ANN |
| **KCHOL** | QNN (Hybrid-MQ) | **0.7106** | **0.7968** | +4.9 AUC, +3.6pp Acc vs ANN |
| **S&P 500** | ANN | 0.7289 | 0.7657 | 略优于QNN（Hybrid-SQ: 0.7221, 0.7498） |

> ✅ **关键发现**：在高维特征下（如AAPL），QNN显著超越参数匹配的ANN；在中维特征（S&P 500跨资产输入）中，ANN仍略优，但QNN召回率更高。

### (2) Live Trading Simulation (S&P 500)
| 市场阶段 | 模型 | Sharpe Ratio ↑ | Annualized Return ↑ | 结果对比 |
|--------|------|----------------|---------------------|----------|
| **F1: 金融危机 (2008–2009)** | QLSTM | **0.3865** | **+5.96%** | 显著优于LSTM (-1.67%, SR=-0.0477) |
| **F2: 疫情前 (2018–2019)** | QLSTM | **1.2585** | **+11.03%** | 大幅优于LSTM (SR=0.4966, ARC=7.45%) |
| **F3: 疫情期 (2020–2021)** | LSTM | 0.8947 | 21.02% | QLSTM SR更低（0.7659），但Sortino更高 |
| **F4: 后疫情 (2022–2024)** | LSTM | **1.5228** | **35.16%** | QLSTM严重失效（ARC=-15.15%, SR=-0.6936） |

> ✅ **关键发现**：QLSTM在两个高波动时期（F1、F2）表现出更强的风险调整收益，但在F4阶段因阈值校准失败而崩溃，显示其对市场状态敏感。

### (3) Volatility Forecasting
| 资产 | 最佳模型 | QLIKE ↓ | vs RBF SVR | vs Linear/Poly SVR |
|------|---------|--------|-----------|------------------|
| **KCHOL** | **QSVR (Angle Encoding)** | **-6.4352** | **胜出** | 显著优于 |
| **S&P 500** | SVR (RBF) | -8.3277 | QSVR次之 (-8.2937) | QSVR优于线性/多项式 |
| **AAPL** | SVR (RBF) | -7.4101 | QSVR接近 (-7.3888) | QSVR优于线性/多项式 |

> ✅ **关键发现**：
> - QSVR在KCHOL上取得最低QLIKE，优于所有经典模型。
> - 在S&P 500和AAPL上，QSVR性能接近最优RBF-SVR，明显优于线性和多项式核。
> - 方向性准确率（DirAcc）方面，QSVR普遍达到0.60左右，与最佳模型相当。

### 消融实验结果
- **QNN架构分析**：
  - **Hybrid结构**（含经典预处理）在高维特征下表现更好。
  - **Multi-Qubit Readout (MQR)** 提升深度电路的表现，尤其在复杂交互建模中。
  - **Amplitude Encoding** 在d=32时表现良好，但d=64时因数值不稳定导致性能下降。
- **QLSTM敏感性**：
  - 性能高度依赖**阈值选择**（T_long, T_short），在F4阶段最优阈值未能迁移，导致亏损。
  - 表明QML模型的**分数校准**（score calibration）可能不如经典模型稳定。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **QML在特定条件下可超越经典模型**：
   - 在**高维特征空间**（如64-D）中，Hybrid QNN在方向性预测上显著优于参数匹配的ANN。
   - 在**高波动市场环境**（如金融危机）中，QLSTM能产生更高的风险调整收益。
   - 在**波动率预测**任务中，QSVR在部分资产（如KCHOL）上达到最优QLIKE，且普遍优于线性/多项式SVR。

2. ⚠️ **经典模型在中等复杂度任务中仍具优势**：
   - 在中维跨资产特征（7-D）预测S&P 500方向时，ANN略优于QNN。
   - RBF核SVR仍是波动率预测的强基线，QSVR尚未全面超越。

3. 🔍 **模型设计至关重要**：
   - **Hybrid架构**（经典+量子混合）在当前NISQ设备下更具实用性。
   - **Amplitude Encoding**虽理论上高效，但实践中易受数值误差影响。
   - **读出策略**（Single vs Multi-Qubit Readout）和**电路深度**是影响性能的关键超参。

4. 💡 **QML的价值不仅在于精度，更在于模式捕捉方式不同**：
   - QNN常以稍低准确率为代价换取更高**Recall**，适合不愿错过上涨行情的策略。
   - QSVR虽绝对拟合不如RBF-SVR，但**方向性判断能力强**，适合趋势跟踪。

### 方法的局限性
- **全部基于模拟器运行**：未在真实量子硬件上部署，忽略噪声与退相干影响。
- **特征维度受限**：尽管使用Amplitude Encoding，但仍无法处理数百维以上特征。
- **训练成本高昂**：QSVR需计算O(N²)规模的核矩阵，难以扩展到大规模数据集。
- **泛化能力待验证**：在F4阶段QLSTM表现崩溃，提示QML模型可能对市场 regime 更敏感。

### 未来工作方向
- 探索**噪声鲁棒的QML训练策略**（noise-resilient training）以适应真实硬件。
- 设计**轻量化量子电路**，降低对qubit数量与电路深度的依赖。
- 构建**动态阈值校准机制**，提升QLSTM在不同市场状态下的稳定性。
- 扩展至更多金融任务，如**组合优化**（portfolio optimization）、**信用评分**（credit scoring）、**欺诈检测**等。
- 开发**端到端可微分的量子-经典混合模型**，支持梯度反向传播与联合优化。

---

> **总结**：该论文首次提供了QML与经典ML在金融预测中的**系统性、标准化、可复现的基准框架**。结果显示，**当前一代QML模型在特定场景下已展现出实用潜力**，尤其是在高维非线性建模与高波动环境下的风险控制方面。然而，经典方法在多数常规任务中仍占优。未来QML的发展应聚焦于**混合架构设计、噪声鲁棒性提升与真实场景部署**，而非单纯追求“量子优越性”。

</details>

---

### 12. [Feature-Aware One-Shot Federated Learning via Hierarchical Token Sequences](https://arxiv.org/abs/2601.03882)

**Authors**: Shudong Liu, Hanwen Zhang, Xiuling Wang, Yuesheng Zhu, Guibo Luo  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03882v1  

#### Abstract
One-shot federated learning (OSFL) reduces the communication cost and privacy risks of iterative federated learning by constructing a global model with a single round of communication. However, most existing methods struggle to achieve robust performance on real-world domains such as medical imaging...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Feature-Aware One-Shot Federated Learning via Hierarchical Token Sequences 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**One-Shot Federated Learning (OSFL)** 在实际应用中的两个核心挑战：
- **非独立同分布（non-IID）数据下的性能下降**：尤其是在医疗图像等真实场景中，客户端间的数据分布差异大，导致全局模型泛化能力差。
- **通信效率与隐私风险之间的权衡**：传统迭代式联邦学习通信开销大，而现有 OSFL 方法在生成代理数据时存在计算复杂度高、隐私泄露风险或建模不准确等问题。

此外，现有基于特征统计的方法（如 FedPFT、FedCGS）受限于预训练编码器对特定领域数据的适应性不足，且难以捕捉多尺度细节（尤其在高分辨率医学影像中），影响性能。

---

### 提出的新方法：FALCON 框架

作者提出 **FALCON**（**Feature-Aware One-shot Learning via Hierarchical Token Sequences**），其核心思想是结合**分层 token 序列生成**与**知识蒸馏引导的全局训练**，提升 OSFL 在 non-IID 图像数据上的有效性。

#### 主要创新点：
1. **Hierarchical Scale Encoding (HSE)**  
   - 利用预训练视觉编码器（如 DINOv2），通过双路径提取图像的**全局语义信息**（低分辨率全图视图）和**局部高分辨率区域细节**（滑动窗口切片）。
   - 构建具有多尺度语义的**层次化 token 序列**，增强编码器对跨域数据的适应性，无需本地微调。

2. **Multi-Scale Autoregressive (M-AR) Transformer Generator**  
   - 设计一个带有块状因果掩码（block-wise causal mask）的 Transformer 结构，以自回归方式建模 token 序列的分布。
   - 引入条件高斯混合模型（GMM）对不同层级 token 进行概率建模，实现更精确的合成序列生成。

3. **Distillation-Guided Global Training**  
   - 客户端上传**合成 token 序列** + **本地分类器**。
   - 服务器端利用所有本地分类器作为“教师”，通过 **Knowledge Distillation** 指导全局分类器训练，降低对生成数据质量的依赖，提高鲁棒性。

---

### 相比现有方法的优势
| 方面 | FALCON 的优势 |
|------|----------------|
| **性能** | 显著优于主流 OSFL 基线，在多种 non-IID 场景下平均准确率提升 **9.58%** |
| **效率** | 合成样本生成仅需 **0.46 GFLOPs/sample**，比扩散模型（如 FedLMG）快四个数量级 |
| **隐私保护** | 合成 token 难以重构原始图像，有效抵御重建攻击（reconstruction attack） |
| **适用性** | 支持医疗与自然图像，适用于特征 non-IID 和标签 non-IID 多种场景 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖 **6 个数据集**，覆盖自然图像与医学图像，分为两类 non-IID 设置：

| 数据集 | 类型 | non-IID 类型 | 类别数 | 客户端数 |
|--------|------|--------------|--------|----------|
| Tuberculosis | 医疗 | 特征 non-IID | 2 | 3 |
| PACS | 自然 | 特征 non-IID | 7 | 4 |
| OfficeHome | 自然 | 特征 non-IID | 65 | 4 |
| PMRAM | 医疗 | 标签 non-IID | 4 | 5 |
| Pneumonia | 医疗 | 标签 non-IID | 2 | 5 |
| Tiny-ImageNet | 自然 | 标签 non-IID | 200 | 5 |

> 注：标签 non-IID 通过 Dirichlet 分布划分标签分布模拟（α ∈ {0.1, 0.3, 0.5}）

---

### 实验设置与评估指标
- **主任务**：图像分类
- **评估指标**：测试集 **Accuracy (%)**
- **预训练编码器**：DINOv2-base
- **HSE 参数**：滑动窗口大小与步长均为 224，输入尺寸统一为 448×448
- **生成器架构**：4 层 Transformer，隐藏维度 768，16 头注意力，GMM 组件数 K=20
- **训练配置**：
  - 本地分类器 & 全局分类器：Adam，lr=5e-4，batch_size=256，60 epochs
  - 蒸馏温度 T=4，平衡系数 α=0.5

---

### 基线方法对比
| 方法 | 类型 | 关键技术 |
|------|------|---------|
| **FedAvg** | 迭代式 FL | 多轮通信聚合 |
| **DENSE** | 数据无关蒸馏 | 知识蒸馏 + 合成样本 |
| **FedLMG** | 生成模型 | 扩散模型生成图像 |
| **FedPFT** | 特征分布建模 | GMM 建模特征分布 |
| **FedCGS** | 特征统计 | 高斯朴素贝叶斯分类器 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 2）
FALCON 在所有设置下均取得最优或次优表现，**平均准确率达到 85.92%**，显著超越最强 OSFL 基线 **FedCGS (76.34%)** 和 **FedPFT (73.61%)**。

#### 性能提升亮点：
- **Tuberculosis（医疗特征 non-IID）**：FALCON 达到 **87.20%**，比第二好的 FedCGS（53.50%）高出 **33.7%**。
- **PMRAM（医疗标签 non-IID）**：在 α=0.1 最极端异构情况下仍保持 **83.33%** 准确率，远超 DENSE（26.93%）、FedLMG（36.99%）。
- **Tiny-ImageNet（大规模标签 non-IID）**：FALCON 达到 ~81.5%，而 FedCGS 表现优异（~81.5%），但 FALCON 更稳定。

> ✅ **总体平均提升：相比最佳 OSFL 基线提升 9.58%**

---

### 消融实验结果（Table 3）

| 变体 | 平均准确率 (%) | 相比完整版下降 |
|------|----------------|---------------|
| **Full (FALCON)** | **85.92** | — |
| w/o HSE（无分层编码） | 82.32 | ↓3.60 |
| w/o KD（无知识蒸馏） | 82.82 | ↓3.10 |
| AR（普通自回归） | 81.47 | ↓4.45 |
| NAR（非自回归） | 82.99 | ↓2.93 |

#### 发现：
- **HSE 贡献最大**：特别是在医疗数据上（Tuberculosis +3.57%，Pneumonia +4.73%），说明多尺度编码对细粒度识别至关重要。
- **知识蒸馏关键作用**：弥补生成数据偏差，提升泛化能力。
- **M-AR 优于 AR/NAR**：验证了建模 token 间依赖关系的重要性。

---

### 分布逼近能力分析（Table 4）
使用 **Maximum Mean Discrepancy (MMD)** 衡量生成 token 与真实 token 分布的距离：

| 方法 | Tuberculosis | PACS | OfficeHome |
|------|--------------|------|-----------|
| M-AR (FALCON) | **0.003227** | **0.002047** | **0.000905** |
| AR | 0.003868 | 0.002457 | 0.001079 |
| NAR | 0.003868 | 0.002151 | 0.000849 |

→ M-AR 在多数数据集上达到最低 MMD，表明其能更好拟合真实 token 分布。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **HSE 显著提升领域适应性**：无需微调即可从原始图像中提取多尺度语义，在医疗图像中尤为有效。
2. ✅ **M-AR Transformer 高效建模 token 分布**：相比扩散模型等 heavy-weight 生成器，计算成本极低（0.46 GFLOPs/sample），适合边缘部署。
3. ✅ **知识蒸馏缓解生成偏差**：即使生成数据不完美，也能通过本地分类器知识融合获得高性能全局模型。
4. ✅ **强隐私保护能力**：重建攻击显示，从**合成 token** 无法恢复可识别图像（SSIM < 0.16），而真实 token 可部分还原（SSIM > 0.8）。
5. ✅ **通信灵活**：客户端可选择上传生成器模型或合成 token 序列，根据数据量动态优化通信开销（上限约 166.86 MB）。

---

### 方法的局限性
1. **依赖高质量预训练编码器**：若预训练域与目标域差距过大（如遥感 → 医疗），HSE 效果可能受限。
2. **生成器仍需一定训练资源**：虽然推理高效，但每个客户端需训练 M-AR Generator，对极低资源设备仍有负担。
3. **未支持个性化模型**：当前框架聚焦全局模型构建，未探讨个性化适配。

---

### 未来工作方向
1. 探索更高效的编码策略，进一步提升跨域迁移能力；
2. 设计轻量化生成器结构，降低客户端训练成本；
3. 将 FALCON 扩展至其他任务（如分割、检测）；
4. 研究更强大的生成建模范式（如 latent autoregressive modeling）以提升表达力；
5. 加强理论分析，理解 HSE 与 KD 如何协同改善 non-IID 泛化。

---

> 🔗 **代码地址**：[https://github.com/LMIAPC/FALCON](https://github.com/LMIAPC/FALCON)

</details>

---

### 13. [xDNN(ASP): Explanation Generation System for Deep Neural Networks powered by Answer Set Programming](https://arxiv.org/abs/2601.03847)

**Authors**: Ly Ly Trieu (New Mexico State University), Tran Cao Son (New Mexico State University)  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03847v1  

#### Abstract
Explainable artificial intelligence (xAI) has gained significant attention in recent years. Among other things, explainablility for deep neural networks has been a topic of intensive research due to the meteoric rise in prominence of deep neural networks and their "black-box" nature. xAI approaches ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：xDNN(ASP): Explanation Generation System for Deep Neural Networks powered by Answer Set Programming

---

## 1. 论文的主要贡献和创新点

### 解决的问题
深度神经网络（DNN）因其“黑箱”特性而缺乏可解释性，这在医疗、网络安全等高风险领域尤为关键。现有的解释性人工智能（xAI）方法大多关注输入-输出关系（如SHAP、LIME），忽视了模型内部结构（尤其是隐藏层）对预测的影响。此外，许多规则提取方法无法提供全局解释或依赖额外标注数据。

本文旨在解决以下三个核心问题：
- **为什么**某个输出被生成？
- 哪些**特征**参与了决策过程？
- 隐藏层之间的**交互机制**是怎样的？

### 提出的新方法或新思路
作者提出了 **xDNN(ASP)** ——一种基于 **Answer Set Programming (ASP)** 的全局解释生成系统，属于**分解式（decompositional）xAI** 方法。其核心思想是将训练好的DNN模型及其训练数据转化为一个逻辑程序 $\Pi(M)$，使得该程序的 **answer sets** 与DNN的输入-输出行为一一对应。

#### 创新点包括：
- **首次将DNN转换为ASP逻辑程序**：利用C5.0决策树算法逐层提取规则，并构建具有语义一致性的逻辑程序。
- **分层规则构造机制**：
  - **Top-level rules**：连接最后一层隐藏节点到输出类；
  - **Intermediate rules**：描述隐藏层之间节点的关系；
  - **Bottom-level rules**：从原始输入特征推导第一层隐藏节点的状态。
- **支持对隐藏节点影响的量化分析**：提出“impact score”来衡量每个隐藏节点的重要性，可用于网络简化设计。

### 相比现有方法的优势
| 方法 | 局限性 | xDNN(ASP) 的优势 |
|------|--------|------------------|
| **SHAP/LIME** | 仅提供局部、统计性解释，忽略网络结构 | 提供**全局、结构性解释**，揭示内部机制 |
| **ECLAIRE / DeepRED** | 仅生成从输入到输出的IF-THEN规则，不建模隐藏层间关系 | 显式建模**隐藏层间的依赖关系**，支持更细粒度分析 |
| **Pedagogical 方法** | 忽视网络结构，视为黑盒 | 属于**decompositional** 类型，保留并解析网络结构 |
| **需映射网络的方法（如[11]）** | 需要专家标注中间概念 | 不需要额外标注，端到端自动提取 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验采用两个合成数据集进行验证：

1. **XOR Dataset**
   - 样本数：1000
   - 特征维度：10（仅前两个特征 $x_1, x_2$ 有效）
   - 输出标签：$y = \text{round}(x_1) \oplus \text{round}(x_2)$
   - 目标：评估预测准确性、一致性及特征重要性识别能力

2. **Modified-XOR Dataset**
   - 在XOR基础上修改：$y = (\text{round}(x_1) \oplus \text{round}(x_2)) + \text{round}(x_3)$
   - 三个特征（$x_1, x_2, x_3$）相关，更具复杂性
   - 目标：研究隐藏节点的影响，探索网络架构优化潜力

### 实验设置
- **DNN结构**：
  - 输入层：10节点（对应10维特征）
  - 输出层：1节点（二分类）
  - 隐藏层数量：1层或多层
  - 隐藏节点数量：{8, 16, 32, 64, 128}
  - 激活函数：tanh / relu / elu
  - 训练参数：epochs ∈ {50,100,150,200}, batch_size ∈ {16,32}

- **xDNN(ASP)流程**：
  1. 训练DNN模型 $M$
  2. 使用C5.0在各层激活值上训练分类器，生成IF-THEN规则
  3. 将规则编码为ASP逻辑程序 $\Pi(M)$
  4. 利用ASP求解器推理并评估解释性能

### 评估指标
| 指标 | 定义 |
|------|------|
| **Accuracy of $\Pi(M)$** | $\Pi(M)$ 在测试集上的预测准确率 |
| **Fidelity** | $\Pi(M)$ 准确率与原DNN模型 $M$ 准确率的一致程度，反映规则保真度 |
| **Feature Importance** | 特征 $x_i$ 在所有规则中出现次数占比，用于识别关键输入 |
| **Impact Score of Hidden Node** | 节点作为规则头（被影响）和规则体（影响他人）的频次乘积，衡量其在网络中的影响力 |

### 基线方法对比
- **ECLAIRE**：当前最优的decompositional规则提取方法
- **DeepRED**, **REM-D**：其他代表性分解式方法
- **Weka决策树（C5.0）**：作为非DNN基线，用于比较传统可解释模型性能
- **SHAP**：虽未直接比较规则提取效果，但用于验证特征重要性一致性

---

## 3. 主要实验结果和性能指标

### 关键性能数据（XOR 数据集）

#### 表1：不同设计下DNN与$\Pi(M)$的准确率对比（Table 3）
| 设计 | 隐藏节点 | Epochs | Batch Size | DNN Accuracy (%) | $\Pi(M)$ Accuracy (%) |
|------|----------|--------|------------|------------------|------------------------|
| 1    | 128      | 200    | 32         | 96.3             | **94.0**               |
| 2    | 128      | 150    | 32         | 95.2             | **94.0**               |
| 3    | 64       | 200    | 16         | 96.2             | 93.8                   |
| 4    | 128      | 150    | 16         | 95.5             | 93.2                   |
| 5    | 32       | 200    | 16         | 95.1             | 92.6                   |

- **xDNN(ASP)** 平均规则提取准确率达 **~93%**
- 显著优于ECLAIRE（91.8 ± 2.6%）且波动更小

#### 图2 & 图3：Fidelity 分析
- 所有点靠近对角线 → 高保真度
- xDNN(ASP) 在多个设计中表现稳定，**fidelity 接近1.0**
- ECLAIRE 在某些fold中下降至65%，表现出明显不稳定性

#### 特征重要性分析（图4）
- 提取的规则中，只有 $x_1$ 和 $x_2$ 被频繁使用（>35%）
- 其他无关特征几乎未出现在规则中
- 与真实数据生成机制完全一致，说明能正确识别**真正重要的特征**

---

### Modified-XOR 实验结果（表4）

#### 表2：Modified-XOR 上的表现（Table 4）
| 设计 | 隐藏节点 | Epochs | Batch Size | DNN Accuracy (%) | $\Pi(M)$ Accuracy (%) |
|------|----------|--------|------------|------------------|------------------------|
| 1    | 128      | 200    | 16         | 91.9             | 83.4                   |
| 2    | 16       | 200    | 16         | 90.8             | 82.6                   |
| 3    | 64       | 200    | 16         | 92.3             | 82.4                   |
| 4    | 32       | 200    | 32         | 90.3             | 80.7                   |
| 5 (special) | **10** | 200 | 16       | **92.1**         | **82.8**               |

- 即使只用 **10个隐藏节点**（远少于常规128），仍保持最高准确率
- 支持通过 **impact score** 发现冗余节点，实现网络压缩

#### 图5：隐藏节点影响分析
- 只有少数隐藏节点具有高 **impact score**
- 大部分节点贡献极低，表明存在显著冗余
- 可据此指导网络精简设计（pruning 或 architecture search）

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **xDNN(ASP) 成功将DNN转化为高保真的ASP逻辑程序**，$\Pi(M)$ 能以 >92% 准确率复现原模型行为。
2. ✅ 提取的规则不仅能解释“**输入→输出**”，还能揭示“**输入→隐藏层→输出**”的完整推理链。
3. ✅ **特征重要性识别准确**：仅关键特征被频繁使用，与ground truth一致。
4. ✅ **隐藏节点影响可量化**：通过 impact score 可识别核心节点，支持网络结构优化。
5. ✅ 相比ECLAIRE等方法，xDNN(ASP) 在**保真度和稳定性**方面表现更优。

### 方法的局限性
- **程序规模随网络增长线性上升**：对于大型DNN（如CNN、Transformer），生成的ASP程序可能过于庞大，难以人工阅读或可视化。
- **依赖C5.0进行规则提取**：C5.0本身适用于表格型数据，在处理图像、文本等非结构化输入时受限。
- **目前仅适用于全连接网络**：尚未扩展到卷积层、注意力机制等复杂结构。
- **计算开销较高**：多轮C5.0调用和ASP编码带来一定时间成本。

### 未来工作方向
1. **降低计算成本**：研究按组（group-wise）而非单个节点分析隐藏层影响。
2. **扩展至专用架构**：探索如何应用于 **Convolutional Neural Networks (CNNs)** 或 **Graph Neural Networks (GNNs)**。
3. **结合符号推理增强解释能力**：利用ASP的强大表达力生成反事实解释（counterfactual explanations）或因果路径。
4. **自动化网络剪枝工具**：基于 impact score 开发DNN压缩框架，提升部署效率。
5. **支持非数值输入**：改进bottom-level规则生成方式，适配类别型、文本型特征。

---

> **总结**：  
> xDNN(ASP) 是首个将DNN转化为ASP逻辑程序的解释系统，不仅实现了高保真的全局解释，还开创性地提供了对隐藏层结构的理解能力。其实验结果证明了其在准确性、稳定性和可解释深度方面的优越性，为构建可信、透明的深度学习系统提供了有力工具。

</details>

---

### 14. [DiffCoT: Diffusion-styled Chain-of-Thought Reasoning in LLMs](https://arxiv.org/abs/2601.03559)

**Authors**: Shidong Cao, Hongzhan Lin, Yuxuan Gu, Ziyang Luo, Jing Ma  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03559v1  

#### Abstract
Chain-of-Thought (CoT) reasoning improves multi-step mathematical problem solving in large language models but remains vulnerable to exposure bias and error accumulation, as early mistakes propagate irreversibly through autoregressive decoding. In this work, we propose DiffCoT, a diffusion-styled Co...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DIFFCoT: Diffusion-styled Chain-of-Thought Reasoning in LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
传统的 **Chain-of-Thought (CoT)** 推理在大型语言模型（LLMs）中存在两个关键缺陷：
- **暴露偏差（Exposure Bias）**：训练时使用正确的前缀（teacher-forcing），而推理时可能基于错误的中间步骤继续生成，导致误差传播。
- **误差累积（Error Accumulation）**：早期推理步骤中的错误会不可逆地影响后续步骤，最终导致错误答案。

现有方法如 **Preference Optimization (PO)** 和 **Tree of Thoughts (ToT)** 虽能部分缓解该问题，但仍受限于严格的自回归、前向生成模式，缺乏对已生成步骤的全局修正能力。

---

### **提出的新方法：DIFFCoT**
作者提出了 **DIFFCoT**，一种将 **扩散模型（Diffusion Model）思想融入 CoT 推理** 的新框架，其核心创新如下：

#### ✅ **1. 将 CoT 重构为迭代去噪过程**
- 不再将推理视为单向、不可修改的生成流程，而是将其建模为一个从“噪声推理链”逐步修复为“干净推理链”的 **迭代去噪过程**。
- 允许模型在生成后续步骤的同时，回溯并修正先前的错误步骤。

#### ✅ **2. 引入滑动窗口机制（Sliding-Window Mechanism）**
- 在一个大小为 `m` 的滑动窗口内，同时进行：
  - **去噪**：对窗口内的历史步骤进行优化（降低噪声）
  - **生成**：预测下一个高噪声状态的推理步骤
- 保持了 token-level 的自回归特性，便于与现有 LLM 架构兼容。

#### ✅ **3. 设计因果扩散噪声调度（Causal Diffusion Noise Schedule）**
- 传统扩散模型是非因果的，而 CoT 必须保持时间顺序。
- DIFFCoT 提出一种 **step-dependent 噪声调度策略**：越靠后的推理步骤，在初始阶段被注入更强的噪声，从而强制模型尊重因果依赖关系。

#### ✅ **4. 统一生成与修正**
- 在同一框架下实现 **forward generation** 与 **retrospective correction**，避免额外引入搜索或验证模块带来的计算开销。

---

### **相比现有方法的优势**
| 方法 | 是否支持修正 | 是否缓解暴露偏差 | 是否保持自回归 | 计算效率 |
|------|----------------|--------------------|------------------|-----------|
| Standard CoT | ❌ | ❌ | ✅ | 高 |
| ToT / MCTS | ✅（通过搜索） | ⚠️有限 | ✅ | 低（高推理成本） |
| Step-DPO / Full-Step-DPO | ❌ | ⚠️局部缓解 | ✅ | 中 |
| **DIFFCoT** | ✅（动态修正） | ✅ | ✅ | 中（可微调） |

> DIFFCoT 在不牺牲推理效率的前提下，实现了对中间步骤的灵活修正，显著提升了鲁棒性和纠错能力。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在三个主流数学推理基准上进行评估：
- **GSM8K**：小学级数学应用题，强调多步逻辑推理。
- **SVAMP**：语义变化的应用题，测试模型泛化能力。
- **MATH**：高中至竞赛级别数学题，分为五个难度等级（L1–L5）。

> 所有数据集均采样 300 测试样本用于公平比较。

---

### **实验设置与评估指标**
- **模型骨干（Backbone Models）**：
  - Llama3-8B
  - Qwen3-8B
  - Qwen3-4B
- **训练方式**：
  - 使用 **Low-Rank Adaptation (LoRA)** 进行高效微调
  - 采用 **Direct Preference Optimization (DPO)** 损失函数（β=0.4）
  - 训练 3 轮，学习率 1e-5，batch size=32
- **评估指标**：
  - **准确率（Accuracy %）**：最终答案是否正确
  - **修正成功率（Correction Success Rate）**：在人为注入噪声前缀后仍能得出正确答案的比例

---

### **基线方法对比**
| 基线方法 | 简介 |
|---------|------|
| **CoT** | 标准思维链提示，贪婪解码 |
| **ToT** | Tree of Thoughts，基于树搜索探索多条路径 |
| **TS-SFT** | 对 ToT 生成的路径做监督微调 |
| **CPO** | Chain of Preference Optimization，基于偏好优化中间步骤 |
| **Step-DPO** | 在错误步骤上构建偏好对进行优化 |
| **Full-Step-DPO** | 对整个推理轨迹进行全局偏好优化 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 1）**

| 方法 | GSM8K | SVAMP | MATH-L1 | MATH-L2 | MATH-L3 | MATH-L4 | MATH-L5 |
|------|-------|--------|----------|----------|----------|----------|----------|
| CoT | 37.2 | 49.6 | 31.3 | 18.4 | 12.5 | 5.9 | 1.9 |
| ToT | 37.7 | 48.8 | 33.8 | 19.3 | 13.7 | 7.6 | 3.1 |
| CPO | 36.1 | 48.6 | 32.0 | 17.6 | 13.7 | 7.6 | 3.1 |
| Step-DPO | 38.1 | 48.4 | 32.0 | 17.6 | 13.7 | 7.6 | 3.1 |
| Full-Step-DPO | 39.6 | 50.4 | 34.3 | 19.5 | 14.2 | 8.0 | 3.3 |
| **DIFFCoT** | **65.5** | **83.5** | **62.0** | **39.2** | **26.1** | **15.0** | **5.4** |

> ✅ **DIFFCoT 在所有任务上大幅超越现有方法**，尤其在复杂任务（如 MATH-L5）上提升明显。

---

### **与基线方法的对比结果**
- 在 **GSM8K** 上，DIFFCoT 达到 **65.5%** 准确率，比第二名 Full-Step-DPO（39.6%）高出近 **26 个百分点**。
- 在 **SVAMP** 上达到 **83.5%**，远超基线（~50%）。
- 在最难的 **MATH-L5** 上，DIFFCoT 得分 **5.4%**，是唯一超过 5% 的方法，显示其处理高难度长链推理的能力。

> 结论：DIFFCoT 显著优于所有 SoTA PO 方法，且在不同规模模型（8B vs 4B）上均表现稳定。

---

### **消融实验结果（Ablation Study）**

| 模型变体 | GSM8K ↓ | SVAMP ↓ | 说明 |
|---------|--------|--------|------|
| DIFFCoT（完整版） | 65.5 | 83.5 | — |
| - window size=1 | 61.1 | 80.7 | 退化为纯 AR 模式，失去修正能力 |
| - window size=K | 60.1 | 82.7 | 全序列去噪破坏因果结构 |
| - 移除因果噪声 | 60.8 | 80.2 | 性能下降，证明因果噪声调度有效 |

> 🔍 发现：**中等大小的滑动窗口（非极端值）最优**，平衡了“因果连贯性”与“纠错能力”。

此外，在 **错误注入实验（Error Accumulation Analysis）** 中：
- 当推理中途注入噪声时，DIFFCoT 的 **修正成功率始终高于 Full-Step-DPO**，尤其是在高噪声强度（ω=0.4）下优势更明显。
- 表明 DIFFCoT 能有效从语义漂移中恢复，具备更强的鲁棒性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **CoT 推理不应是“一次性”过程**：允许模型在推理过程中动态修正早期错误，可以显著提升最终准确性。
2. **扩散机制适合建模推理纠错**：将推理链视为可迭代优化的对象，比静态生成更具鲁棒性。
3. **因果结构必须保留**：直接套用图像扩散模型会导致推理混乱；通过设计因果感知的噪声调度，可在保持逻辑顺序的同时实现去噪。
4. **DIFFCoT 可无缝集成到现代 LLM**：无需改变架构，仅需微调即可获得巨大收益，具有良好的可扩展性。

---

### **方法的局限性**
1. **Off-policy 数据收集带来分布偏移风险**：
   - 偏好数据由 MCTS 收集，而目标模型不同，可能导致训练不稳定。
2. **打破局部马尔可夫性质**：
   - 回溯修改历史步骤增加了生成不确定性，需要更多训练数据才能收敛。
3. **训练成本较高**：
   - 相比标准 DPO，需要构造多候选轨迹和噪声版本，数据准备更复杂。

---

### **未来工作方向**
- 探索将 **Reinforcement Learning** 与扩散去噪过程结合，进一步优化 denoising dynamics。
- 扩展到更多推理领域，如符号推理、程序合成、科学问答等。
- 研究更高效的在线去噪策略，减少推理延迟。

---

> 💡 **总结一句话**：  
> **DIFFCoT 通过引入扩散式的迭代去噪机制，首次实现了 CoT 推理中“边想边改”的能力，在不牺牲自回归效率的前提下，显著提升了 LLM 多步推理的鲁棒性与准确性。**

</details>

---

### 15. [Decide Then Retrieve: A Training-Free Framework with Uncertainty-Guided Triggering and Dual-Path Retrieval](https://arxiv.org/abs/2601.03908)

**Authors**: Wang Chen, Guanqiang Qi, Weikang Li, Yang Li, Deguo Xia, Jizhou Huang  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03908v1  

#### Abstract
Retrieval-augmented generation (RAG) enhances large language models (LLMs) by incorporating external knowledge, but existing approaches indiscriminately trigger retrieval and rely on single-path evidence construction, often introducing noise and limiting performance gains. In this work, we propose D...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Decide Then Retrieve: A Training-Free Framework with Uncertainty-Guided Triggering and Dual-Path Retrieval**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**

传统的 **Retrieval-Augmented Generation (RAG)** 存在两个关键缺陷：

1. **无差别触发检索（Indiscriminate Retrieval Triggering）**  
   所有查询都强制触发检索，即使简单问题本可由 LLM 的参数化知识直接回答，导致引入无关噪声，降低生成质量。

2. **单路径证据构建脆弱（Single-Path Evidence Construction）**  
   对于稀疏或模糊查询（sparse queries），仅依赖原始查询进行检索，语义信号不足，容易召回低相关性文档，影响答案准确性。

---

### **提出了什么新方法或新思路**

本文提出 **Decide Then Retrieve (DTR)** —— 一种无需训练、模型无关（model-agnostic）的 RAG 框架，其核心思想是将检索视为一个**条件化、自适应的过程**，包含两大创新机制：

#### **(1) Uncertainty-Guided Triggering (UGT)**  
利用 LLM 自身生成过程中的 **generation uncertainty**（归一化的负对数似然）来判断是否需要触发检索：
- 若不确定性低（模型自信），则跳过检索，避免噪声；
- 若不确定性高，则激活检索，借助外部知识提升准确性。

#### **(2) Dual-Path Retrieval with Adaptive Information Selection (DPR-AIS)**  
针对稀疏查询，提出双路径检索机制：
- 同时基于 **原始查询（query）** 和 **LLM 生成的伪上下文（pseudo-context）** 进行独立检索；
- 通过 **Adaptive Information Selection (AIS)** 机制融合两路结果，选择在两个视角下均相关的文档；
- 利用几何角度（cos(θ₁ + θ₂)）建模联合相关性，增强鲁棒性。

---

### **相比现有方法的优势**

| 方面 | DTR 的优势 |
|------|-----------|
| **无需训练** | 不依赖额外分类器或微调，可即插即用到现有 RAG 系统 |
| **减少噪声** | UGT 避免对简单问题进行不必要的检索 |
| **提升召回质量** | DPR-AIS 显著改善稀疏查询下的检索相关性 |
| **通用性强** | 在不同模型规模（7B / 72B）、不同 retriever（bge / e5）、多种数据集上表现稳定 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**

在五个开放域 QA 数据集上进行评估：

| 数据集 | 类型 | 文档数量 |
|--------|------|----------|
| **NaturalQA** | 开放域问答 | 21M |
| **WebQuestions** | 知识库问答 | 21M |
| **SQuAD** | 抽取式问答 | 21M |
| **TriviaQA** | 多样化问答 | 21M |
| **HotpotQA** | 多跳推理 | 5M |

检索语料为英文维基百科（21M 分块）或 HotpotQA 自带语料。

---

### **实验设置和评估指标**

- **生成模型**：Qwen2.5-7B-Instruct 和 Qwen2.5-72B-Instruct
- **检索器**：bge-large-en-v1.5 和 e5
- **检索深度**：top-3 或 top-5
- **解码策略**：temperature = 0.0（确定性输出）
- **评估指标**：
  - **Exact Match (EM)**：预测答案与任一标准答案完全匹配的比例
  - **F1 Score**：token 级别的重叠分数，更宽容

---

### **基线方法对比**

| 基线方法 | 简要说明 |
|---------|--------|
| **No Retrieval** | 仅使用 LLM 参数知识生成答案 |
| **Standard RAG** | 标准 RAG，所有查询均触发检索 |
| **LLM Judge** | 使用 LLM 判断是否需要检索（如“是否需要外部信息？”） |
| **HyDE** | 使用 LLM 生成假设文档用于检索 |
| **Q2D** | 将原始查询与伪文档拼接后检索 |
| **CoT** | 先生成推理链，再结合查询进行检索 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 2 & Table 7）**

在 **Qwen2.5-7B-Instruct + bge** 设置下，DTR 相比 Standard RAG 显著提升：

| 方法 | Average EM | Average F1 |
|------|------------|------------|
| Standard RAG | 35.81 | 45.81 |
| **DTR (u=0.001)** | **37.87** (+2.06) | **48.08** (+2.27) |

在更大模型 **Qwen2.5-72B-Instruct** 上：
| 方法 | Average EM | Average F1 |
|------|------------|------------|
| Standard RAG | 38.83 | 50.73 |
| **DTR (u=0.005)** | **40.46** (+1.63) | **52.14** (+1.41) |

使用 **e5 retriever** 时，DTR 仍优于 Standard RAG：
- 平均 EM 提升 **1.56**（从 36.86 → 38.42）
- 平均 F1 提升 **1.68**（从 47.18 → 48.86）

---

### **与基线方法的对比结果**

- DTR 在所有数据集上均达到 **最佳或次佳性能**；
- 明显优于 HyDE、Q2D、CoT 等 query expansion 方法；
- 相比 LLM Judge：
  - 小模型版（7B）几乎不触发检索，性能接近 No Retrieval；
  - 大模型版（72B）频繁触发但仍不如 DTR；
- 表明 **基于 uncertainty 的动态决策优于启发式判断**。

---

### **消融实验结果（Table 3）**

验证各组件贡献（以 7B 模型为例）：

| 方法变体 | Average EM | Average F1 | 相比完整 DTR 下降 |
|--------|------------|------------|------------------|
| **DTR (完整)** | 37.87 | 48.08 | — |
| w/o UGT | 37.84 | 48.13 | 基本持平（略优） |
| w/o DPR | 35.98 | 45.86 | ↓1.89 EM / ↓2.22 F1 |
| w/o AIS (1q+2p) | 31.15 | 40.32 | ↓6.72 EM / ↓7.76 F1 |

> ✅ **结论**：DPR 和 AIS 是核心组件；UGT 虽非显著增益，但能有效减少不必要检索（见 Trigger Ratio）。

---

## **4. 关键结论和发现**

### **主要发现**

1. **Generation uncertainty 是有效的检索触发信号**  
   - 不确定性越低，parametric accuracy 越高；
   - 强制检索反而可能因噪声降低准确率；
   - DTR 可动态平衡“何时检索”。

2. **Dual-Path Retrieval 显著提升稀疏查询的召回质量**  
   - 查询与 GT 文档相似度常低于 0.8（Fig. 7）；
   - 伪上下文可弥补语义鸿沟，其分布更接近 GT 文档（Fig. 9–10）；
   - DPR 在 HotpotQA 上 Recall@3 达 **62.7%**，高于 Standard RAG（61.9%）。

3. **Adaptive Information Selection 至关重要**  
   - 固定比例融合（如 1q+2p）性能大幅下降；
   - 动态加权选择能更好过滤噪声。

4. **DTR 具有良好的泛化性和鲁棒性**  
   - 在不同模型规模、不同 retriever、不同检索深度下均一致优于 baseline。

---

### **方法的局限性**

1. **依赖 uncertainty 估计的质量**  
   - 不同 decoding 策略或模型家族可能导致 uncertainty 分布变化；
   - 当前基于 token-level NLL，可能不够稳定。

2. **增加推理延迟**  
   - 生成 pseudo-context 增加一次前向推理；
   - 双路径检索需两次独立检索操作。

3. **性能仍受限于检索语料和 embedding 质量**  
   - 若 corpus 不全或 embedding 不准，DPR 效果受限。

---

### **未来工作方向**

- 探索更高效的 uncertainty 估计方式（如 logits variance）；
- 结合 reranker 进一步优化文档选择；
- 将 DTR 扩展至多模态 RAG 场景；
- 研究如何在 agent 或搜索系统中动态集成 DTR 决策机制。

---

> 🔗 **代码与数据**：https://github.com/ChenWangHKU/DTR

</details>

---

### 16. [Bridging the Discrete-Continuous Gap: Unified Multimodal Generation via Coupled Manifold Discrete Absorbing Diffusion](https://arxiv.org/abs/2601.04056)

**Authors**: Yuanfeng Xu, Yuhao Chen, Liang Lin, Guangrun Wang  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.04056v1  

#### Abstract
The bifurcation of generative modeling into autoregressive approaches for discrete data (text) and diffusion approaches for continuous data (images) hinders the development of truly unified multimodal systems. While Masked Language Models (MLMs) offer efficient bidirectional context, they traditiona...

---

### 17. [Weather-Aware Transformer for Real-Time Route Optimization in Drone-as-a-Service Operations](https://arxiv.org/abs/2601.03376)

**Authors**: Kamal Mohamed, Lillian Wassim, Ali Hamdi, Khaled Shaban  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03376v1  

#### Abstract
This paper presents a novel framework to accelerate route prediction in Drone-as-a-Service operations through weather-aware deep learning models. While classical path-planning algorithms, such as A* and Dijkstra, provide optimal solutions, their computational complexity limits real-time applicabilit...

---

### 18. [ROI-Reasoning: Rational Optimization for Inference via Pre-Computation Meta-Cognition](https://arxiv.org/abs/2601.03822)

**Authors**: Muyang Zhao, Qi Qi, Hao Sun  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03822v1  

#### Abstract
Large language models (LLMs) can achieve strong reasoning performance with sufficient computation, but they do not inherently know how much computation a task requires. We study budgeted inference-time reasoning for multiple tasks under a strict global token constraint and formalize it as a Ordered ...

---

### 19. [Trade-R1: Bridging Verifiable Rewards to Stochastic Environments via Process-Level Reasoning Verification](https://arxiv.org/abs/2601.03948)

**Authors**: Rui Sun, Yifan Sun, Sheng Xu, Li Zhao, Jing Li, Daxin Jiang, Chen Hua, Zuo Bai  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03948v1  

#### Abstract
Reinforcement Learning (RL) has enabled Large Language Models (LLMs) to achieve remarkable reasoning in domains like mathematics and coding, where verifiable rewards provide clear signals. However, extending this paradigm to financial decision is challenged by the market's stochastic nature: rewards...

---

### 20. [Atlas: Orchestrating Heterogeneous Models and Tools for Multi-Domain Complex Reasoning](https://arxiv.org/abs/2601.03872)

**Authors**: Jinyang Wu, Guocheng Zhai, Ruihan Jin, Jiahao Yuan, Yuhao Shen, Shuai Zhang, Zhengqi Wen, Jianhua Tao  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03872v1  

#### Abstract
The integration of large language models (LLMs) with external tools has significantly expanded the capabilities of AI agents. However, as the diversity of both LLMs and tools increases, selecting the optimal model-tool combination becomes a high-dimensional optimization challenge. Existing approache...

---

### 21. [SIGMA: Scalable Spectral Insights for LLM Collapse](https://arxiv.org/abs/2601.03385)

**Authors**: Yi Gu, Lingyou Pang, Xiangkun Ye, Tianyu Wang, Jianyu Lin, Carey E. Priebe, Alexander Aue  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03385v1  

#### Abstract
The rapid adoption of synthetic data for training Large Language Models (LLMs) has introduced the technical challenge of "model collapse"-a degenerative process where recursive training on model-generated content leads to a contraction of distributional variance and representational quality. While t...

---

### 22. [Sensor to Pixels: Decentralized Swarm Gathering via Image-Based Reinforcement Learning](https://arxiv.org/abs/2601.03413)

**Authors**: Yigal Koifman, Eran Iceland, Erez Koifman, Ariel Barel, Alfred M. Bruckstein  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03413v1  

#### Abstract
This study highlights the potential of image-based reinforcement learning methods for addressing swarm-related tasks. In multi-agent reinforcement learning, effective policy learning depends on how agents sense, interpret, and process inputs. Traditional approaches often rely on handcrafted feature ...

---

### 23. [LinkD: AutoRegressive Diffusion Model for Mechanical Linkage Synthesis](https://arxiv.org/abs/2601.04054)

**Authors**: Yayati Jadhav, Amir Barati Farimani  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04054v1  

#### Abstract
Designing mechanical linkages to achieve target end-effector trajectories presents a fundamental challenge due to the intricate coupling between continuous node placements, discrete topological configurations, and nonlinear kinematic constraints. The highly nonlinear motion-to-configuration relation...

---

### 24. [Digital Red Queen: Adversarial Program Evolution in Core War with LLMs](https://arxiv.org/abs/2601.03335)

**Authors**: Akarsh Kumar, Ryan Bahlous-Boldi, Prafull Sharma, Phillip Isola, Sebastian Risi, Yujin Tang, David Ha  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03335v1  

#### Abstract
Large language models (LLMs) are increasingly being used to evolve solutions to problems in many domains, in a process inspired by biological evolution. However, unlike biological evolution, most LLM-evolution frameworks are formulated as static optimization problems, overlooking the open-ended adve...

---

### 25. [Evolving Programmatic Skill Networks](https://arxiv.org/abs/2601.03509)

**Authors**: Haochen Shi, Xingdi Yuan, Bang Liu  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03509v1  

#### Abstract
We study continual skill acquisition in open-ended embodied environments where an agent must construct, refine, and reuse an expanding library of executable skills. We introduce the Programmatic Skill Network (PSN), a framework in which skills are executable symbolic programs forming a compositional...

---

### 26. [eTracer: Towards Traceable Text Generation via Claim-Level Grounding](https://arxiv.org/abs/2601.03669)

**Authors**: Bohao Chu, Qianli Wang, Hendrik Damm, Hui Wang, Ula Muhabbek, Elisabeth Livingstone, Christoph M. Friedrich, Norbert Fuhr  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03669v1  

#### Abstract
How can system-generated responses be efficiently verified, especially in the high-stakes biomedical domain? To address this challenge, we introduce eTracer, a plug-and-play framework that enables traceable text generation by grounding claims against contextual evidence. Through post-hoc grounding, ...

---

### 27. [What Does Loss Optimization Actually Teach, If Anything? Knowledge Dynamics in Continual Pre-training of LLMs](https://arxiv.org/abs/2601.03858)

**Authors**: Seyed Mahed Mousavi, Simone Alghisi, Giuseppe Riccardi  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03858v1  

#### Abstract
Continual Pre-Training (CPT) is widely used for acquiring and updating factual knowledge in LLMs. This practice treats loss as a proxy for knowledge learning, while offering no grounding into how it changes during training. We study CPT as a knowledge learning process rather than a solely optimizati...

---

### 28. [Evaluating Small Decoder-Only Language Models for Grammar Correction and Text Simplification](https://arxiv.org/abs/2601.03874)

**Authors**: Anthony Lamelas  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03874v1  

#### Abstract
Large language models have become extremely popular recently due to their ability to achieve strong performance on a variety of tasks, such as text generation and rewriting, but their size and computation cost make them difficult to access, deploy, and secure in many settings. This paper investigate...

---

### 29. [Modular Prompt Optimization: Optimizing Structured Prompts with Section-Local Textual Gradients](https://arxiv.org/abs/2601.04055)

**Authors**: Prith Sharma, Austin Z. Henley  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.04055v1  

#### Abstract
Prompt quality plays a central role in controlling the behavior, reliability, and reasoning performance of large language models (LLMs), particularly for smaller open-source instruction-tuned models that depend heavily on explicit structure. While recent work has explored automatic prompt optimizati...

---

### 30. [Variational Inference, Entropy, and Orthogonality: A Unified Theory of Mixture-of-Experts](https://arxiv.org/abs/2601.03577)

**Authors**: Ye Su, Yong Liu  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03577v1  

#### Abstract
Mixture-of-Experts models enable large language models to scale efficiently, as they only activate a subset of experts for each input. Their core mechanisms, Top-k routing and auxiliary load balancing, remain heuristic, however, lacking a cohesive theoretical underpinning to support them. To this en...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
