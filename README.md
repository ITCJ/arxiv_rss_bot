# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-04 06:18:21 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DALI: A Workload-Aware Offloading Framework for Efficient MoE Inference on Local PCs](https://arxiv.org/abs/2602.03495)

**Authors**: Zeyu Zhu, Gang Li, Peisong Wang, Zitao Mo, Minnan Pei, Zhuoran Song, Xiaoyao Liang, Jian Cheng  
**Category**: cs.DC  
**Published**: 2026-02-04  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2602.03495v1  

#### Abstract
Mixture of Experts (MoE) architectures significantly enhance the capacity of LLMs without proportional increases in computation, but at the cost of a vast parameter size. Offloading MoE expert parameters to host memory and leveraging both CPU and GPU computation has recently emerged as a promising d...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DALI: A Workload-Aware Offloading Framework for Efficient MoE Inference on Local PCs

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有的 MoE（Mixture of Experts）模型在本地PC上进行推理时面临三大挑战，源于专家激活的**动态性和异构硬件资源的不均衡利用**：

1. **CPU-GPU 负载不平衡**：静态的专家分配策略（如将高负载专家固定分配给GPU）导致计算资源利用率低下，在不同输入下出现严重的负载失衡。
2. **预取（Prefetching）准确性低**：现有预取机制无法准确预测下一层中将被高频使用的高负载专家，导致大量无效的 PCIe 数据传输。
3. **GPU 缓存效率差**：传统缓存替换策略（如 LRU 或基于激活分数）未考虑专家工作负载的时间相关性，命中率低。

### 🚀 提出的新方法与创新思路
为解决上述问题，作者提出了 **DALI** ——一个面向本地PC平台、**工作负载感知**（workload-aware）的 MoE 推理卸载框架，其核心创新包括以下三个关键技术：

#### （1）Greedy Assignment Strategy（贪心分配策略）
- 将专家在 CPU 和 GPU 之间的分配建模为一个 **0-1 整数优化问题**，目标是最小化 `max(Tgpu, Tcpu)`，即最小化瓶颈设备的执行时间。
- 设计了一种高效的 **贪心算法** 来近似求解该问题，在极低开销下实现接近最优的负载均衡。
- 动态地根据当前 token 的专家激活模式实时决策，而非采用静态阈值划分。

#### （2）Residual-Based Prefetching（基于残差的预取）
- 利用 Transformer 层间存在的 **residual connection** 中的隐藏状态差异（`res_vec`），对下一层门控函数的输入特征进行校正。
- 通过这种跨层残差信息增强特征表示，显著提升对“高负载专家”的预测精度。
- 预取向量可离线构建并复用于多种下游任务，无需微调。

#### （3）Workload-Aware Cache Replacement（工作负载感知缓存替换）
- 观察到高负载专家具有强**时间局部性**（temporal correlation）：若某专家在 token i 上是高负载，则在 token i+1 上也很可能是。
- 提出一种滑动窗口机制，累积每个专家在过去若干 token 中的工作负载得分，并据此更新 GPU 缓存。
- 替换时优先保留历史高负载专家，淘汰低分专家，从而大幅提升缓存命中率。

### 🔍 相比现有方法的优势
| 方面 | 现有方法（如 HybriMoE, llama.cpp） | DALI 的优势 |
|------|-------------------------------|-------------|
| 分配策略 | 静态专家分配（layer-wise 或 expert-wise） | **动态、负载感知**，实现更优的 CPU-GPU 并行 |
| 预取机制 | 基于原始输入或统计特征，准确率低 | 引入 **residual correction**，大幅提高高负载专家预测准确率 |
| 缓存管理 | LRU 或 activation score-based | 基于 **历史 workload 得分**，命中率更高 |
| 总体效果 | 受限于通信瓶颈和资源浪费 | 显著降低 PCIe 通信开销，提升端到端推理速度 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **C4**：用于主推理性能测试（speed benchmarking）。
- **Wikitext**：用于构建 calibration dataset（采样1K序列），以离线生成各层的 `res_vec` 残差向量。

### ⚙️ 实验设置
- **硬件平台**：
  - CPU: AMD EPYC 7532 (64 cores)
  - GPU: NVIDIA RTX 3090 (24GB HBM)
  - 内存: 256GB DDR4 DRAM
  - 接口: PCIe 4.0 ×16 (~32 GB/s)
- **模型**：
  - **DeepSeek-V2-Lite-Chat** (27 layers, 64 routed experts/layer)
  - **Qwen3-30B-A3B** (48 layers, 128 experts/layer)
  - **Mixtral-8x7B-Instruct** (32 layers, 8 experts/layer)

### 📊 评估指标
- **推理阶段**：分为 **prefill**（处理提示词）和 **decoding**（逐个生成 token）两个阶段分别评测。
- **主要指标**：**Tokens per second (tokens/s)**，报告批处理中所有序列的平均吞吐量。
- **其他辅助指标**：
  - PCIe 传输时间占比
  - 缓存命中率（Hit Rate）
  - 预取准确率（Prefetch Accuracy）
  - MoE 层执行时间分解

### 🆚 基线方法对比
| 基线 | 类型 | 特点 |
|------|------|------|
| **llama.cpp** | Layer-wise Hybrid | 将前若干 MoE 层放 CPU，其余放 GPU |
| **KTransformers** | Layer-wise Hybrid | 同上，支持更多优化 |
| **MoE-Lightning** | Offline Search | 离线搜索部署策略，缺乏动态适应能力 |
| **HybriMoE** | Expert-wise Hybrid | 支持专家级调度、预取与缓存，是当前最强 baseline |

> 所有方法均控制相同数量的 CPU 线程（32）、GPU 显存使用量以及缓存大小，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Figure 12 & 13）

#### 在 **Decoding 阶段** 的平均加速比（vs. 各 baseline）：
| 方法 | vs. llama.cpp | vs. KTransformers | vs. MoE-Lightning | vs. HybriMoE |
|------|----------------|--------------------|--------------------|---------------|
| **DALI** | **3.97×** | **2.16×** | **1.48×** | **1.32×** |

#### 在 **Prefill 阶段** 的平均加速比：
| 方法 | vs. llama.cpp | vs. KTransformers | vs. MoE-Lightning | vs. HybriMoE |
|------|----------------|--------------------|--------------------|---------------|
| **DALI** | **7.62×** | **3.80×** | **2.45×** | **2.00×** |

> 💡 结果表明：**DALI 在 prefill 阶段增益更大**，说明其在处理长上下文时优势更明显。

---

### 🔬 消融实验结果（Ablation Study）

#### （1）Greedy Assignment 的贡献（Figure 14）
- 相比无调度的 “Naive” 方法（全部专家跑 CPU），**Greedy Assignment 单独带来 4.42× 加速**。
- 比 HybriMoE 的静态分配快 **23%**，因其能更好平衡 CPU/GPU 负载。

#### （2）Residual-Based Prefetching 的效果（Figure 16）
- 在 Mixtral 上，相比随机预取或 HybriMoE 方法，**DALI 的预取准确率最高**（Top-1 准确率提升显著）。
- 使用该方法后，**预取带来的实际加速效果从不足 1.2× 提升至超过 2.0×**。

#### （3）Workload-Aware Cache 的收益（Figure 17）
- 在相同缓存容量下，**DALI 的缓存命中率显著高于 LRU 和 HybriMoE 的 score-based 方法**。
- 最终带来 **1.23× 的推理速度提升**，尤其在小缓存场景下优势更明显。

#### （4）整体模块叠加效果（Figure 19）
从 “Naive” 开始逐步添加组件：
1. **+ Greedy Assignment → ~4.1× 提升**
2. **+ Residual-Based Prefetching → 再 +9%**
3. **+ Workload-Aware Cache → 再 +38%**

> 表明 **动态分配是最大贡献者**，而缓存优化提供了持续增益。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **动态负载感知分配至关重要**：静态专家分配严重限制了异构系统的潜力；**Greedy Assignment 能有效实现 CPU-GPU 负载均衡**，是性能飞跃的关键。
2. **跨层残差可用于精准预取**：利用 `residual connection` 中的信息校正输入特征，可显著提升对高负载专家的预测准确性，使预取真正发挥作用。
3. **高负载专家存在强时间局部性**：相邻 token 中活跃的高负载专家高度重叠，这一观察支撑了基于 workload history 的缓存策略设计。
4. **三者协同作用显著**：调度 + 预取 + 缓存三者结合，可在有限带宽下最大限度减少 PCIe 通信，充分发挥本地PC的异构算力。

### ⚠️ 方法的局限性
- **依赖 warm-up profiling 获取延迟参数**（如 `tcpu(wi)`, `tgpu(wi)`），虽可复用但仍需初始校准。
- 当前设计针对单机单卡环境，尚未扩展至多 GPU 或分布式系统。
- 对非常规架构（非标准 Transformer）的泛化能力有待验证。

### 🔮 未来工作方向
- 探索在 **多 GPU 平台**（如 1 CPU + 2 GPU）上的扩展性（已在 Appendix 中初步验证）。
- 进一步研究在 **高带宽服务器系统**（如 GH200, NVLink）中的适用性。
- 将 workload-aware 思想推广至其他稀疏模型结构（如 Sparse Attention, Block-Skip）。

---

## 总结
**DALI 是首个全面引入“工作负载感知”思想的 MoE 推理框架**，通过 **动态专家分配、残差驱动预取、历史负载感知缓存** 三大技术，系统性解决了本地PC上 MoE 推理的资源利用与通信瓶颈问题。实验表明其在主流 MoE 模型上实现了高达 **7.62× 的 prefill 加速** 和 **3.97× 的 decoding 加速**，显著优于现有最先进方法，为个人设备高效运行大规模 MoE 模型提供了可行路径。

</details>

---

### 2. [Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design](https://arxiv.org/abs/2602.00608)

**Authors**: Wei Zeng, Xuchen Li, Ruili Feng, Zhen Liu, Fengwei An, Jian Zhao  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 14.0  
**Type**: new  
**ArXiv ID**: 2602.00608v1  

#### Abstract
Real-time generative game engines represent a paradigm shift in interactive simulation, promising to replace traditional graphics pipelines with neural world models. However, existing approaches are fundamentally constrained by the ``Memory Wall,'' restricting practical deployments to low resolution...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统生成式游戏引擎（Generative Game Engine）受限于“**Memory Wall**”（内存墙），难以在高分辨率（如 720×480）下实现实时推理。现有方法如 GameNGen 和 Diamond 多运行在低分辨率（如 64×64 或 320×240），导致视觉保真度不足，无法满足真实游戏体验需求。

本文旨在突破这一瓶颈，实现**高分辨率、低延迟、高保真的实时神经游戏生成**。

---

### 🚀 提出的新方法与创新点

作者提出了一种**可扩展的硬件-算法协同设计框架**（Hardware-Algorithm Co-Design），通过系统级优化解决生成模型中计算密集型（compute-bound）与内存密集型（memory-bound）任务之间的资源不匹配问题。

#### 主要创新点包括：

1. **异构计算架构与资源分配策略（Heterogeneous Architecture & Resource Allocation）**
   - 将 **World Model (DiT)** 与 **Decoder (VAE)** 解耦，分别部署在不同的 AI 加速器上。
   - DiT 是 **compute-bound**，采用 **Sequence Parallelism**（如 Ulysses）进行分布式计算。
   - VAE 是 **memory-bound**，采用 **Spatial Parallelism** 分割特征图宽度维度，减少通信开销。
   - 建立理论吞吐量模型，推导最优设备分配比例（5:3），最大化集群整体性能。

2. **内存中心化的算子融合优化（Memory-Centric Operator Fusion）**
   - 针对 VAE 解码器：提出 **Vertical Fusion**，将 `Upsample → Conv2d → GroupNorm → SiLU` 融合为单个核函数，利用片上 SRAM 实现“一次读取、一次写回”，降低 HBM 带宽压力达 **75%**。
   - 针对 DiT 注意力模块：提出 **Horizontal Fusion**，合并 AdaLN 中的小矩阵乘法，提升计算密度（arithmetic intensity）从 <20% 到 >85% 的峰值理论利用率。

3. **流形感知的潜在空间外推机制（Manifold-Aware Latent Extrapolation）**
   - 利用帧间高度时间相关性，在动作稳定时跳过部分 DiT 推理步骤。
   - 基于流形假设（Manifold Hypothesis），使用前向运动向量 $ v_t = z_t - z_{t-1} $ 进行线性外推：  
     $$
     z_{t+\Delta t} \approx z_t + \lambda \cdot v_t
     $$
   - 在约 65% 的帧中跳过 DiT 计算，显著降低平均计算负载，同时保持视觉连贯性。

4. **推测性控制信号预取（Speculative Action Prefetching）**
   - 引入轻量级 LSTM 模型预测用户下一步操作。
   - 若预测命中（hit rate ≈ 93%），直接显示预生成帧，实现近乎零有效延迟。
   - 结合上述机制，将感知延迟从 38ms 降至 **2.7ms**（amortized）。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法（如 GameNGen, Diamond） | 本工作 |
|------|-------------------------------|--------|
| 分辨率 | ≤320×240 | ✅ 支持 **720×480**（像素吞吐提升 50×） |
| 架构设计 | 单一设备或同构并行 | ✅ 异构并行 + 显式内存管理 |
| 内存效率 | 缓存隐式管理，频繁 HBM 访问 | ✅ 算子融合 + 片上 SRAM 利用，减少 75% HBM 传输 |
| 延迟控制 | 固定推理流程 | ✅ 外推 + 推测执行，感知延迟仅 **2.7ms** |
| 可扩展性 | 依赖专用硬件（TPU） | ✅ 可迁移至通用 AI 加速器集群（如 H100 + NVLink） |

---

## 2. 核心实验方法和设置

### 🧪 数据集与测试环境

使用两个代表性基准进行验证，覆盖连续与离散动作空间：

1. **Matrix (Continuous Domain)**  
   - 类型：高保真 3D 赛车模拟器（720×480）
   - 特点：强调连续动力学、摩擦、动量等物理一致性
   - 来源：[9] *The Matrix: Infinite-horizon world generation with real-time moving control*

2. **PGG (Playable Game Generation, Discrete Domain)**  
   - 类型：2D 平台跳跃游戏（256×256）
   - 特点：强调布尔逻辑、碰撞检测、状态跃迁
   - 来源：[10] *Playable Game Generation*

---

### ⚙️ 实验设置

- **硬件平台**：8 卡 Huawei Ascend 910C AI 加速器集群
  - 每卡 64GB HBM，FP16 算力 ~752 TFLOPS
  - 互联带宽：HCCS 环形拓扑，30 GB/s
- **软件栈**：CANN 8.0, PyTorch 2.5.1, xfuser（支持 Ulysses 并行）
- **资源配置**：5 张用于 DiT（World Model），3 张用于 VAE（Decoder）

---

### 📊 评估指标

| 指标类别 | 具体指标 | 描述 |
|--------|---------|------|
| **性能** | FPS, Motion-to-Photon Latency (M2P) | 衡量生成速度与响应延迟 |
| **视觉质量** | FID, PSNR, SSIM, LPIPS | 衡量生成画面与真实视频分布的距离及感知相似性 |
| **物理一致性** | Control Sensitivity Analysis (CSA) | 连续域中输入-输出动态关系是否符合物理规律 |
| **逻辑一致性** | Discrete Logic Boundary (DLB) Score | 离散域中是否存在非法状态转移（如穿墙） |

---

### 🆚 基线方法对比

| 基线 | 硬件 | 分辨率 | FPS | 备注 |
|------|------|--------|-----|------|
| **Diamond [6]** | RTX 3090 | 64×64 | 10.0 | 扩展性差，分辨率极低 |
| **GameNGen [4]** | TPU v5 | 320×240 | >20 | 专有硬件，未公开细节 |
| **PGG Baseline** | RTX 5090 | 256×256 | 29.9 | 开源模型，作为离散域对照 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 指标 | Matrix (3D Racing) | PGG (2D Platformer) |
|------|---------------------|-----------------------|
| **分辨率** | 720×480 | 256×256 |
| **FPS** | **26.4** | **48.3** |
| **感知延迟（amortized M2P）** | **2.7 ms** | **2.7 ms** |
| **FID** | 42.3 | 28.5 |
| **LPIPS** | 0.087 | 0.052 |
| **DLB Score (逻辑一致性)** | — | **100.0%**（无违规） |

> ✅ 实现了 **720p 实时生成**，相比 Diamond（64×64）像素吞吐提升 **50×**

---

### 🔁 与基线方法对比结果

| 方法 | FPS (Matrix) | FID ↓ | LPIPS ↓ | 优势说明 |
|------|--------------|--------|----------|-----------|
| Diamond [6] | 10.0 (@64×64) | 89.5 | 0.198 | 分辨率极低，不可比 |
| GameNGen [4] | >20 (@320×240) | 58.2 | 0.142 | 仍低于本工作保真度 |
| **Ours (Matrix)** | **26.4 (@720×480)** | **42.3** | **0.087** | ✅ 更高分辨率 + 更优质量 |

- 在相同算力级别下，单卡效率虽略低（due to communication overhead），但**集群配置解锁了高分辨率可行性**。
- 归一化效率（Norm. Eff. = FPS / 100 TFLOPS）为 0.44，虽低于消费级 GPU（~7.0），但这是为突破“Memory Capacity Wall”所付出的必要代价。

---

### 🔍 消融实验结果（Ablation Study）

| 阶段 | FPS | Speedup | 说明 |
|------|-----|---------|------|
| Baseline (Single Card) | 2.1 | 1.0x | 完全不可用 |
| + Operator Fusion | 4.5 | 2.1x | 内存优化初见成效 |
| + Ulysses (3:5) | 16.6 | 7.9x | 异构并行带来飞跃 |
| + Optimal 5:3 Ratio | 19.4 | 9.2x | 理论建模指导调优 |
| + Latent Extrapolation | 26.4 | 12.6x | 算法级加速达成实时 |
| + Speculative Execution | 26.4 (FPS), **2.7ms (latency)** | — | 感知延迟大幅下降 |

> 💡 总计实现 **12.6× 的系统级性能提升**

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **“Memory Wall”是高分辨率生成的核心瓶颈**，单纯算法优化无法突破，必须依赖 **Hardware-Algorithm Co-Design**。
2. **异构架构解耦 DiT 与 VAE** 是关键路径，能有效平衡计算与内存负载。
3. **片上 SRAM + 算子融合** 可显著减少 HBM 访问，提升内存效率。
4. **时间冗余可被高效利用**：通过流形外推与推测执行，可在不牺牲一致性的前提下大幅降低感知延迟。
5. 该框架实现了 **720×480 下 26.4 FPS** 的实时生成能力，并保持 **100% 逻辑一致性**（in-distribution）。

---

### ⚠️ 局限性

1. **依赖大规模加速器集群**：当前方案适用于云游戏场景，尚难部署于边缘设备（edge）。
2. **Out-of-Distribution (OOD) 动作鲁棒性不足**：面对训练分布外的操作可能出现“幻觉”行为（如穿墙）。
3. **资源分配依赖模型参数**：最优 5:3 分配基于特定注意力头数（H=30），换模型需重新调参。
4. **未完全解决长期一致性问题**：尽管短期连贯，但超长时间运行可能累积误差。

---

### 🔮 未来工作方向

1. **Hybrid Neuro-Symbolic Engines**  
   - 引入轻量级符号逻辑层（Symbolic Logic Layer）作为“规则监督器”，强制遵守关键游戏规则，增强 OOD 鲁棒性。

2. **Multi-modal Control Interfaces**  
   - 利用 Transformer 的跨模态能力，支持自然语言、语音等新型交互方式。

3. **On-Device Quantization & Compression**  
   - 探索 4-bit 权重量化 + 激活平滑技术，推动本地化部署，降低对云端依赖。

4. **Cross-Platform Compiler Backend**  
   - 将优化策略封装至通用编译器（如 OpenAI Triton），适配 NVIDIA H100、TPU 等多平台，促进生态普及。

---

## ✅ 总结

本文首次证明了：**通过硬件-算法协同设计，可以在商用 AI 加速器集群上实现高分辨率、低延迟、高保真的实时神经游戏生成**。其核心思想——**解耦计算与内存瓶颈、显式管理内存层级、利用时间冗余隐藏延迟**——为下一代生成式交互系统提供了可复用的技术范式。

> “我们相信，这标志着一个新时代的到来：世界不再是构建出来的，而是被‘梦见’的。”

</details>

---

### 3. [Understanding and Exploiting Weight Update Sparsity for Communication-Efficient Distributed RL](https://arxiv.org/abs/2602.03839)

**Authors**: Erfan Miahi, Eugene Belilovsky  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2602.03839v1  

#### Abstract
Reinforcement learning (RL) is a critical component for post-training large language models (LLMs). However, in bandwidth-constrained distributed RL, scalability is often bottlenecked by the synchronization of policy weights from trainers to inference workers, particularly over commodity networks or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《Understanding and Exploiting Weight Update Sparsity for Communication-Efficient Distributed RL》论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
在带宽受限的**分布式强化学习（Distributed RL）**系统中，训练节点与推理节点之间的**模型权重同步**成为通信瓶颈。对于一个7B参数的模型，在16-bit精度下每次同步需传输14GB数据，导致对高带宽网络（如20 Gbit/s）的强依赖，严重限制了去中心化或地理分布场景下的可扩展性。

传统方法如**梯度压缩（gradient compression）**多为有损压缩，且适用于梯度聚合而非权重广播；而熵编码等无损方法增益有限。因此，如何实现高效、可靠的**无损权重同步**是关键挑战。

---

### 提出了什么新方法或新思路
本文提出 **PULSE (Patch Updates via Lossless Sparse Encoding)** —— 一种基于**权重更新稀疏性**的无损同步方法。

#### 核心思想：
- 发现并系统验证：**RL微调过程中，每步仅有约1%的参数发生实际变化**（sparsity > 99%），其余参数在BF16精度下因更新量太小被“吸收”。
- 利用该稀疏结构，PULSE仅传输**发生变化的参数索引及其新值**（而非增量Δ），实现高效压缩。

#### 关键创新：
1. **机制性解释稀疏性来源**：  
   首次明确指出稀疏性源于 **BF16精度限制** 与 **RL保守学习率**（~3×10⁻⁶）的相互作用——即使梯度密集（~99%非零），更新幅度仍低于BF16表示阈值，导致绝大多数参数不变。

2. **无损且抗漂移的设计**：  
   存储**实际权重值**而非增量（additive delta），避免了浮点累加过程中的**量化误差累积**（floating-point drift），保证任意长度的补丁链也能实现**bit-identical重建**。

3. **实用化系统设计**：  
   引入**锚点检查点（anchor checkpoint）+ 补丁链**结构，支持冷启动恢复与异步操作，并结合SHA256哈希进行完整性校验。

---

### 相比现有方法的优势

| 特性 | PULSE | Gradient Compression | Entropy Coding |
|------|-------|------------------------|----------------|
| 是否无损 | ✅ 是 | ❌ 否（通常有损） | ✅ 是 |
| 压缩比 | **>100×** | ~2–10× | ~1.5–2× |
| 抗传输错误 | ✅ 强（端到端哈希验证） | ⚠️ 依赖error feedback | ⚠️ 脆弱 |
| 多跳重建稳定性 | ✅ 不积累误差 | ❌ 易漂移 | ✅ |
| 适用场景 | 权重广播 | 梯度聚合 | 全量权重 |

> ✅ **优势总结**：PULSE实现了**通信效率、可靠性与兼容性**的统一，特别适合去中心化、低带宽环境下的大规模RL训练。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **数学推理任务**：`MATH` 数据集（7,500训练样本，500验证）
- **代码生成任务**：`MBPP` 数据集（774训练任务，190验证）

均采用**可验证奖励（Verifiable Rewards, RLVR）**，无需人工标注或奖励模型。

---

### 实验设置

#### 模型架构
- **Qwen2.5-Instruct**：0.5B, 1.5B, 7B
- **Llama-3.2-Instruct**：3B
- **Gemma-3-4B-it**：4B

#### 算法与超参
- **算法**：Group Relative Policy Optimization (**GRPO**)，类PPO但无KL惩罚项
- **学习率**：主实验使用 `3×10⁻⁶`，去中心化部署中降至 `1×10⁻⁶`
- **优化器**：AdamW（β₁=0.9, β₂=0.99）
- **精度**：BF16（混合精度训练，FP32 master weights）
- **批量大小**：32 prompts × 16 rollouts

#### 分布式架构
- **grail框架**：基于Bittensor构建的去中心化RL平台
- **节点角色**：
  - **Miners**：生成rollout
  - **Validators**：通过hidden-state fingerprinting验证真实性
  - **Trainer**：执行梯度更新并发布PULSE补丁

#### 通信模拟
- 在真实公网环境中测试，平均带宽约 **400 Mb/s**
- 使用S3兼容对象存储（Cloudflare R2）分发补丁

---

### 评估指标

| 指标 | 描述 |
|------|------|
| **Weight Update Sparsity** | 连续两步间**bitwise相同参数的比例** |
| **Upload Size** | 单次同步上传体积（MB） |
| **Bandwidth Reduction** | 相比全量同步的压缩倍数 |
| **Validation pass@1** | 验证集上首次生成即正确的比例 |
| **SHA256 Verification** | 重建后权重是否bit-identical |
| **End-to-End Latency** | 快路径/慢路径同步延迟 |

---

### 基线方法对比
- **Full Weight Synchronization**（14GB/次）作为基准
- 未直接比较其他压缩方法，而是从理论层面论证其不可行性：
  - 梯度压缩不适用于广播场景
  - 已有稀疏性研究（如Mukherjee et al. [21]）仅基于粗粒度差异，缺乏逐步分析

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | 结果 |
|------|------|
| **平均更新稀疏度** | **~99%**（所有模型 & 训练阶段） |
| **最小稀疏度（最差步）** | >98%（k=1） |
| **k=8时多步稀疏度** | >98%（符合异步RL推荐范围） |
| **单次上传大小（7B模型）** | **108 MB**（SE: 1.1MB） |
| **通信压缩比** | **>100×**（14GB → ~108MB） |
| **带宽需求（90% GPU利用率）** | **0.2 Gbit/s**（原需20 Gbit/s） |
| **重建成功率** | **100% SHA256验证通过** |

> 📈 图1显示：在50秒/步计算下，传统同步需20 Gbit/s才能维持90% GPU利用率；PULSE仅需0.2 Gbit/s即可达到同等效率。

---

### 与基线方法的对比结果

| 方法 | 通信开销 | 是否无损 | 实际收益 |
|------|----------|----------|---------|
| Full Sync | 14 GB | ✅ | 基准 |
| PULSE | **~108 MB** | ✅ | **>100× 减少** |
| Gradient Compression | ~1–7 GB（估算） | ❌ | 不适用，且破坏训练动态 |

> 💡 实验表明，PULSE在保持完全一致训练动态的前提下，将通信成本降低两个数量级。

---

### 消融实验结果

#### （1）压缩组件贡献分析（Table 5）
| 组件 | 压缩增益（相对COO） | 编码速度提升 |
|------|--------------------|-------------|
| Delta Encoding | +13.3% | ≈ |
| Type Downscaling（uint8/uint16） | +8.5% | ↑ encode throughput |
| **合计** | **+22.9%** | **↑ 35%** |

#### （2）不同压缩算法选择（Table 7）
| 算法 | 压缩比 | 编码速度（MB/s） | 最佳适用场景 |
|------|--------|------------------|--------------|
| lz4 | 56× | 830 | 高带宽（>800 Mbit/s） |
| **zstd-1** | **79×** | **534** | **默认（15–800 Mbit/s）** |
| zstd-3 | 80× | 197 | 低带宽（<14 Mbit/s） |

> ✅ **zstd-1为默认配置**：在典型云网络中提供最佳端到端延迟。

#### （3）跨模型压缩效果（Table 8）
| 模型家族 | 稀疏度 | 压缩比（zstd-1） |
|---------|--------|----------------|
| Qwen2.5 | 99.0% | 76× |
| Llama-3.2 | 99.3% | **100×** |
| Gemma-3 | 99.2% | 80× |

> 表明PULSE具有良好的泛化能力。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **RL微调存在极高权重更新稀疏性**（~99%），且在整个训练过程中稳定存在，不受模型规模、架构或off-policy延迟影响。
2. 🔍 **稀疏性根源在于BF16 + 小学习率**：并非梯度稀疏，而是更新被数值精度“吸收”。这是现代LLM训练配置下的**固有现象**。
3. 🛠️ **PULSE可安全利用此结构**：通过传输变更值而非增量，实现**无损、抗漂移、高鲁棒性**的同步。
4. 🌐 **真实去中心化网络验证成功**：在公网环境下实现>100×通信削减，训练动态与性能完全一致。

---

### 方法的局限性

| 局限 | 说明 |
|------|------|
| **依赖Adam类优化器** | 分析基于Adam更新机制，SGD可能不具备相同稀疏性（见A.6） |
| **假设BF16为主流** | 若转向FP8或更高精度，稀疏性可能下降 |
| **仅验证单轮对话任务** | 多轮交互、长程反馈场景尚未测试 |
| **锚点间隔影响冷启动延迟** | 新节点需下载完整锚点（14GB），不适合频繁加入场景 |

---

### 未来工作方向

1. **扩展至其他算法**：验证PPO、DPO等RLHF主流算法是否具备类似稀疏性。
2. **支持更复杂训练范式**：探索在long-horizon RL、multi-agent setting中的应用。
3. **动态自适应压缩**：根据实时稀疏性自动切换压缩策略。
4. **进一步降低冷启动成本**：引入分层锚点或增量锚点机制。
5. **硬件协同优化**：结合NIC offload或专用解码芯片提升端侧效率。

---

> ✅ **总体评价**：  
> PULSE不仅是一项高效的工程优化，更是对**现代RL训练内在结构特性**的一次深刻洞察。它揭示了“看似密集”的训练过程背后隐藏的巨大稀疏潜力，并将其转化为实际的系统优势，为**去中心化AI训练**铺平了道路。

</details>

---

### 4. [Quant VideoGen: Auto-Regressive Long Video Generation via 2-Bit KV-Cache Quantization](https://arxiv.org/abs/2602.02958)

**Authors**: Haocheng Xi, Shuo Yang, Yilong Zhao, Muyang Li, Han Cai, Xingyang Li, Yujun Lin, Zhuoyang Zhang, Jintao Zhang, Xiuyu Li, Zhiying Xu, Jun Wu, Chenfeng Xu, Ion Stoica, Song Han, Kurt Keutzer  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2602.02958v1  

#### Abstract
Despite rapid progress in autoregressive video diffusion, an emerging system algorithm bottleneck limits both deployability and generation capability: KV cache memory. In autoregressive video generation models, the KV cache grows with generation history and quickly dominates GPU memory, often exceed...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《Quant VideoGen: Auto-Regressive Long Video Generation via 2-Bit KV-Cache Quantization》总结**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
- **KV-cache 内存瓶颈**：在自回归视频生成模型（如 LongCat-Video、Self-Forcing）中，随着生成长度增加，KV-cache 随时间线性增长，迅速占据 GPU 显存（可达 30–34GB），成为部署和长时生成的主要限制。
- **能力受限**：受限的 KV-cache 容量导致上下文窗口被截断，进而引发身份漂移、布局不一致、运动断裂等长期一致性问题。
- **现有量化方法失效**：直接将 LLM 中成熟的 KV-cache 量化技术（如 KIVI、QuaRot）应用于视频扩散模型会导致严重质量下降，因其未考虑视频特有的时空冗余和数值分布异质性。

### **提出了什么新方法或新思路**
提出 **Quant VideoGen (QVG)** ——一种无需训练的 KV-cache 量化框架，专为自回归视频扩散模型设计，核心创新包括：
1. **Semantic-Aware Smoothing (SAS)**  
   - 利用 k-means 聚类对语义相似的 token 进行分组，并减去每组的均值（centroid），得到低幅值、更均匀的残差张量。
   - 有效缓解了 KV-cache 在 token 和 channel 维度上的数值异质性，使分布更适合低比特量化。

2. **Progressive Residual Quantization (PRQ)**  
   - 受流媒体视频编码启发，采用多阶段粗到细的残差量化策略。
   - 每一阶段对上一阶段的残差再次应用 SAS，逐步捕获从全局结构到细节纹理的信息，显著降低累积量化误差。

3. **算法-系统协同优化**
   - **Streaming centroid caching**：缓存前一块的聚类中心以加速 k-means 推理。
   - **融合反量化内核（fused dequantization kernel）**：在寄存器中完成反量化与残差重建，避免多次访存。

### **相比现有方法的优势**
| 方面 | QVG 优势 |
|------|---------|
| **内存压缩比** | 最高实现 **7.0× KV-cache 压缩**（INT2），远超基线（如 KIVI、QuaRot 仅支持 ~6.4×）。 |
| **视觉质量保持** | 在高压缩下仍保持近无损质量（PSNR >28.7），而基线在 INT2 下严重退化。 |
| **延迟开销** | 端到端延迟增加 <4%，几乎不影响推理效率。 |
| **部署可行性** | 首次实现在单张 RTX 4090 上运行 HY-WorldPlay-8B 模型，突破硬件限制。 |
| **通用性** | 无需微调或重新训练，适用于多种自回归视频模型（LongCat-Video、HY-WorldPlay、Self-Forcing）。 |

---

## **2. 核心实验方法和设置**

### **使用的数据集与模型**
- **模型**：
  - **LongCat-Video-13B**：基于固定上下文窗口进行视频延续任务。
  - **HY-WorldPlay-8B**：用于实时交互式世界建模，chunk-wise 生成。
  - **Self-Forcing-Wan-1.3B**：自回归帧预测模型，测试长期一致性。
- **输入分辨率**：统一使用 **480p** 视频。
- **提示词来源**：采用 MovieGen benchmark 提供的 prompt suite。

### **实验设置**
- **硬件平台**：NVIDIA H100 GPU（CUDA 12.8），部分实验验证于 RTX 4090。
- **量化配置**：
  - 支持 **INT2 / INT4** 对称 per-group 整数量化。
  - 两种模式：
    - **QVG**：单阶段 SAS + INT2，追求最大压缩比。
    - **QVG-Pro**：4 阶段 PRQ + INT4，追求最优质量。
- **实现方式**：基于 CUDA 和 Triton 编写定制内核，支持流式 chunk 压缩，防止重压缩漂移。
- **其他设定**：
  - 使用 pre-RoPE key caching。
  - scaling factors 使用 FP8 E4M3 存储。
  - 分组大小（group size）设为 16 或 64，聚类数 $ K=256 $（uint8 存储索引）。

### **评估指标**
| 类别 | 指标 |
|------|------|
| **保真度** | PSNR、SSIM、LPIPS（相对于 BF16 基线） |
| **感知质量** | VBench benchmark：<br>- Background Consistency<br>- Image Quality<br>- Subject Consistency<br>- Aesthetic Quality |
| **效率** | KV-cache 压缩比、端到端延迟开销 |
| **长期一致性** | 在长序列中每隔 50 帧报告 Image Quality，观察漂移情况 |

### **基线方法对比**
- **Round-to-Nearest (RTN)**：最简单的舍入量化。
- **KIVI**：针对 LLM 设计的 2-bit 不对称量化，处理异常值。
- **QuaRot**：通过旋转平滑激活分布后再量化。
- 所有基线均复现其 KV-cache 量化部分，权重与激活保持浮点。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 模型 | 方法 | 压缩比 | PSNR | SSIM | LPIPS | Subject Consistency |
|------|------|--------|-------|-------|--------|---------------------|
| LongCat-13B | QVG | **6.94×** | **28.716** | 0.909 | 0.065 | **94.11** |
| | QVG-Pro | 4.97× | **30.376** | **0.935** | **0.048** | 94.92 |
| | KIVI (6.4×) | 6.40× | 20.317 | 0.719 | 0.208 | 75.25 |
| HY-WorldPlay-8B | QVG | **7.05×** | **29.174** | 0.882 | 0.094 | **97.90** |
| | QVG-Pro | 5.20× | **31.562** | **0.923** | **0.069** | 97.96 |
| | QuaRot (6.4×) | 6.40× | 25.207 | 0.738 | 0.205 | 96.64 |

> ✅ QVG 在最高压缩比下仍优于所有基线在更低压缩下的表现。

### **与基线方法的对比结果**
- **质量方面**：
  - 所有基线在 **INT2 设置下出现剧烈退化**（PSNR 下降 >8dB），尤其 Subject Consistency 明显下降。
  - QVG 即使在 INT2 下也接近 BF16 基线质量，实现“近无损”压缩。
- **压缩效率**：
  - QVG 达到 **6.94–7.05× 压缩比**，显著高于基线（普遍 ≤6.4×）。
  - QVG-Pro 在较低压缩比下提供更高保真度，形成完整的 **Pareto 前沿**。
- **长期生成稳定性**（见 Figure 5a）：
  - 在 Self-Forcing 模型上生成长达 700 帧的视频：
    - BF16 基线和 QVG/QVG-Pro 几乎无退化。
    - 其他基线在约 100 帧后图像质量急剧下降。

### **消融实验结果**
#### （1）**Semantic-Aware Smoothing 的有效性**
- 如 Figure 6 所示：
  - SAS 将 Key Cache 的量化误差降低 **6.9×**。
  - Value Cache 误差降低 **2.6×**。
- 原因：通过聚类减去均值得到的小幅值残差更易量化。

#### （2）**Progressive Residual Quantization 的增益**
- 如 Figure 5(c) 所示：
  - 第一阶段带来 **5.83× MSE 减少**。
  - 后续阶段持续改进，但边际收益递减（第4阶段仅提升 ~1.1×）。
- 表明 PRQ 成功实现了从粗到细的信息保留。

#### （3）**分组大小的影响**
- 分组越大（如 64 vs 16）→ 压缩率越高，但质量略降。
- QVG 选择 group size=64 实现最佳权衡；QVG-Pro 使用 smaller group size=16 保证高质量。

---

## **4. 关键结论和发现**

### **主要发现**
1. **KV-cache 是自回归视频生成的关键瓶颈**，不仅影响部署效率，更直接制约长期一致性能力。
2. **视频 KV-cache 具有强时空冗余性**，可通过语义聚类和平滑残差的方式极大提升可压缩性。
3. **传统 LLM 量化方法无法直接迁移至视频模型**，必须结合视频特有的动态分布特性进行专门设计。
4. **QVG 实现了前所未有的 7.0× KV-cache 压缩比**，同时保持近无损视觉质量，推动长视频生成走向实用化。

### **方法的局限性**
- **依赖 k-means 聚类**：尽管通过缓存优化降低了开销，但仍引入额外计算，可能不适合极低延迟场景。
- **未探索 weight quantization**：当前仅聚焦 KV-cache，完整模型量化尚未覆盖。
- **对极端动态场景敏感**：若视频内容突变频繁（如快速切换镜头），SAS 的聚类效果可能减弱。

### **未来工作方向**
- 结合 **sparse attention** 与 QVG，进一步减少 KV 存储需求。
- 探索 **learned codebook-based vector quantization** 替代 k-means，提升聚类效率。
- 将 QVG 思路扩展至 **audio-visual 多模态生成系统**。
- 构建端到端的 **hardware-aware 编译流程**，最大化 GPU 利用率。

---

> 📌 **一句话总结**：  
> **Quant VideoGen (QVG)** 通过语义感知平滑与渐进残差量化，在无需训练的前提下实现了高达 **7× 的 KV-cache 压缩**，解决了自回归视频生成中的内存瓶颈，首次使得分钟级甚至小时级高质量视频生成在消费级 GPU 上成为可能。

</details>

---

### 5. [Large-Scale LLM Inference with Heterogeneous Workloads: Prefill-Decode Contention and Asymptotically Optimal Control](https://arxiv.org/abs/2602.02987)

**Authors**: Ruihan Lin, Zezhen Ding, Zean Han, Jiheng Zhang  
**Category**: cs.DC  
**Published**: 2026-02-04  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.02987v1  

#### Abstract
Large Language Models (LLMs) are rapidly becoming critical infrastructure for enterprise applications, driving unprecedented demand for GPU-based inference services. A key operational challenge arises from the two-phase nature of LLM inference: a compute-intensive \emph{prefill} phase that processes...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Large-Scale LLM Inference with Heterogeneous Workloads: Prefill-Decode Contention and Asymptotically Optimal Control*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文针对大规模 **Large Language Model (LLM)** 推理服务中的一个核心挑战：**Prefill-Decode Contention**（预填充-解码争用）。

在 LLM 推理中，每个请求分为两个阶段：
- **Prefill Phase**：处理用户输入提示（compute-intensive，计算密集型）。
- **Decode Phase**：自回归生成输出 token（memory-bound，内存受限）。

当这两个阶段共享 GPU 资源时，**Prefill 任务会显著拖慢同批处理的 Decode 任务**，形成状态依赖的服务速率。此外，现实世界的工作负载具有高度异质性（heterogeneous workloads），不同应用（如摘要、创意写作）的输入/输出长度差异巨大，使得资源调度更加复杂。

因此，论文旨在解决：**如何在异构请求混合的大规模 GPU 集群中，联合控制 Admission 和 Scheduling，以最大化基于 token 的收益（revenue），同时满足 Service Level Indicators (SLIs) 如延迟和公平性要求？**

### 提出了什么新方法或新思路
论文提出了一套完整的 **Stochastic Control** 框架，其核心创新点如下：

1. **多类多服务器排队网络模型 (Multiclass Many-Server Queueing Network)**  
   将 LLM 推理系统建模为一个带有**状态依赖服务速率**的排队网络。GPU 有两种模式：
   - **Mixed Mode**：运行一个 Prefill + 多个 Decode（Decode 速度因 Prefill 占用计算而变慢）。
   - **Solo Mode**：仅运行 Decode（速度更快）。

2. **基于流体近似的规划与控制分离架构 (Fluid Approximation + Gate-and-Route Policy)**  
   - 在 **Many-GPU 极限**下，通过**流体近似 (Fluid Approximation)** 将随机系统简化为确定性模型。
   - 求解一个**稳态线性规划 (Steady-State Linear Program, LP)**，得到最优的容量分配（多少 GPU 用于 Mixed/Solo）和各类请求的占用目标 (occupancy targets)。
   - 设计 **Gate-and-Route 控制策略**将 LP 解转化为可执行策略：
     - **Prefill Gate**：基于当前 Prefill 占用与目标的偏差，动态决定是否接纳新请求，确保占用量收敛到目标。
     - **Decode Router**：将完成 Prefill 的请求路由到可用的 Decode Slot。

3. **支持多种计费模式和 SLI 约束**
   - 分别分析了 **Bundled Charging**（请求完成后统一收费）和 **Separate Charging**（Prefill 和 Decode 分开收费）两种模式，并设计了相应的渐近最优策略（后者需使用优先级策略）。
   - 将 **SLIs**（如公平性、延迟）作为约束或惩罚项加入 LP，实现收益与服务质量的权衡。

### 相比现有方法的优势
- **理论保证**：所提策略被证明是**渐近最优 (asymptotically optimal)** 的，即当 GPU 数量趋于无穷大时，每 GPU 收益收敛到流体最优值。
- **系统性框架**：首次将排队论与实证测量结合，为 LLM 推理调度提供了严谨的数学基础，超越了现有的工程启发式方法。
- **灵活性高**：框架可自然扩展至多种商业目标（如收益管理）和服务质量约束。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **合成数据集**：基于真实 LLM 推理特征（如 Qwen-4B/8B）构建的两类异构请求：
  - **Class 0 (Decode-Heavy)**：短输入（P=300）、长输出（D=1000），代表代码生成。
  - **Class 1 (Prefill-Heavy)**：长输入（P=3000）、短输出（D=400），代表论文摘要。
- **参数校准数据**：在配备 4×NVIDIA A100-SXM4-40GB GPU 的服务器上，使用 **vLLM** 对 **Qwen-4B** 和 **Qwen-8B** 模型进行实测，校准 `α`, `β`（Mixed 模式迭代时间参数）和 `γ`（Solo 模式解码速度）。

### 实验设置和评估指标
- **模拟方式**：事件驱动仿真 (event-driven simulation)，模拟不同规模（n=5, 20, 50, 200, 500）的 GPU 集群。
- **评估指标**：
  - **Per-GPU Revenue**：单位 GPU 的平均收益。
  - **Queue Lengths**：Prefill 和 Decode 队列长度。
  - **Occupancy Convergence**：实际资源占用是否收敛到流体最优目标。
  - **SLI Trade-offs**：通过帕累托前沿分析收益与公平性、延迟之间的权衡。

### 基线方法对比
论文对比了以下五种策略：
1. **GG-SP (Gate-and-Route with Static Planning)**：本文提出的完整策略。
2. **FI-WSP (FCFS-and-Immediate without Static Planning)**：行业标准基线（类似 Sarathi-Serve），先来先服务，Prefill 完成后立即在同一 GPU 上开始 Decode。
3. **GI-WSP (Gate-and-Immediate without Static Planning)**：引入本文的 Gate 控制，但仍采用 Immediate 执行模式。
4. **GF-WSP (Gate-and-FCFS without Static Planning)**：使用 Gate 控制，但路由时优先选择新 Prefill 而非等待的 Decode 任务。
5. **FG-SP (FCFS-and-Route with Static Planning)**：有静态规划和路由，但无 Admission Gate 控制。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **收益收敛性**：随着 GPU 规模增大，GG-SP 的 **Per-GPU Revenue** 渐近收敛到流体最优值 `R*`，且方差显著减小。
- **队列稳定性**：Prefill 队列稳定在流体目标附近，Decode 队列几乎为空，验证了 **Decode-Buffer Elimination** 的理论预测。

### 与基线方法的对比结果
- **GG-SP 显著优于所有基线**，在所有测试场景下均取得最高收益。
- **FI-WSP (行业基线) 表现最差**，因其无法有效控制 Prefill 导致 Decode 被严重阻塞。
- **Gate 控制 (GI-WSP) 比 FCFS (FI-WSP) 有明显提升**，说明 Admission 控制至关重要。
- **静态规划 + 贪心路由 (FG-SP) 反而可能劣于基线**，说明没有 Admission 控制的静态划分可能导致局部失衡。
- **路由策略 (GF-WSP vs GG-SP)**：优先处理等待 Decode 的任务（而非新 Prefill）对性能提升显著。

### 消融实验结果
- **SLI-Aware Policy**：当使用随机化路由器（Randomized Router）强制各类请求的 Decode 占用比例时，**各类别的 Decode Occupancy 也严格收敛到 LP 目标**，而普通策略仅能保证总收益最优但类别分布不稳定。
- **SLI 权衡分析**：
  - **Prefill Fairness** 的“影子价格”很高，意味着强制公平会显著牺牲收益。
  - **Decode Fairness** 的影子价格很低，说明在 Decode 阶段平衡各类请求成本较低。
  - **TPOT (Time Per Output Token)** 接近硬件极限（由 `γ` 决定）时，收益急剧下降。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **Prefill-Decode Contention 是影响 LLM 推理效率的核心瓶颈**，必须通过联合调度建模。
2. **Gate-and-Route 架构是有效的**：通过 Admission Gate 控制 Prefill 流量，配合 Work-Conserving 的 Decode 路由，可在大规模下实现渐近最优。
3. **计费模式影响调度策略**：Separate Charging 可能激励过度接纳 Prefill，导致下游拥堵；建议**按阶段计费，但按端到端完成优化调度**。
4. **SLI 成本不对称**：在 Prefill 阶段实施公平性代价高昂，在 Decode 阶段则相对便宜。
5. **硬件配置敏感性**：收益对计算惩罚系数 `β` 高度敏感，而对批大小 `B` 存在收益递减。

### 方法的局限性
- **假设指数分布**：模型假设服务时间和耐心时间为指数分布，虽经实证验证合理，但仍是对现实的简化。
- **均质基础设施**：假设所有 GPU 性能相同，未考虑异构集群（如不同代 GPU）。
- **集中式控制**：Gate-and-Route 假设全局状态可见，大规模分布式部署可能存在通信开销。

### 未来工作方向
1. **放松指数假设**：使用更一般的分布（如相位型分布）或测度值过程 (measure-valued processes) 进行建模。
2. **开发扩散近似 (Diffusion Approximation)**：以刻画系统波动，为尾部延迟 (tail-latency) SLIs 提供保障。
3. **推广至异构基础设施**：研究跨不同 GPU 类型和 Agent 架构的推理编排。
4. **在线学习与鲁棒性**：研究在到达率和服务率不确定情况下的鲁棒调度策略。

</details>

---

### 6. [Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization](https://arxiv.org/abs/2602.01090)

**Authors**: Yang Liu, Chuan Zhou, Yancheng Chen, Shuai Zhang, Xixun Lin, Xiaoqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.01090v1  

#### Abstract
Large language models (LLMs) have emerged as promising general-purpose solvers for combinatorial optimization (CO), yet they fundamentally lack mechanisms to guarantee solution feasibility which is critical for real-world deployment. In this work, we introduce FALCON, a framework that ensures 100\% ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在组合优化（Combinatorial Optimization, CO）任务中展现出强大的模式识别和序列生成能力，但其本质是**无约束的生成模型**，无法保证输出满足硬性约束条件（如容量限制、路径连续性、独立集无邻接等）。这导致生成的解常常不可行（infeasible），严重制约了其在物流、制造、应急调度等安全敏感场景的实际部署。

现有方法通常通过奖励塑形（reward shaping）或后处理拒绝采样（rejection sampling）来提升可行性，但这些方法只能提供概率性保障，**无法实现100%的可行性保证**。

---

### **提出的新方法：FALCON 框架**
本文提出了 **FALCON**（Feasibility-Aware Language-based Combinatorial Optimization with Adaptive Inference），首个能**理论保证100%可行性**的 LLM-based CO 求解框架，其核心创新在于三层协同机制：

#### **(1) Grammar-Constrained Decoding（语法约束解码）**
- 引入**上下文无关文法**（Context-Free Grammars, CFGs）对每个 CO 问题定义输出格式。
- 在解码过程中通过 **PDA（Pushdown Automaton）** 动态屏蔽非法 token，确保输出**语法正确**（如括号匹配、索引有效）。
- 例如：TSP 必须输出 `[0,1,2,0]` 形式的路径，而非 `0,1,2` 或 `[0,1,1,2]`。

#### **(2) Feasibility Repair Layer（可行性修复层）**
- 设计**问题特定的修复算子**（Repair Operators），将语义上不满足约束的解转换为可行解。
- 修复算子满足三个关键性质：
  - **可行性**（Feasibility）：任何输入经修复后必为可行解。
  - **幂等性**（Idempotence）：若输入已可行，则输出不变。
  - **有界局部性**（Bounded Locality）：修复带来的质量损失与违反程度成正比。
- 例如：CVRP 中超载路线被拆分；MIS 中相邻顶点被移除一个。

#### **(3) Adaptive Best-of-N Sampling（自适应多采样）**
- 动态调整采样数量 $N$，基于**解的一致性**（Consistency）估计实例难度。
- 高一致性（多数样本相同）→ 模型自信 → 少量采样即可终止。
- 低一致性 → 模型不确定 → 更多探索以找到高质量解。
- 显著提升推理效率，避免固定 $N$ 的资源浪费。

---

### **训练方法创新：BOPO**
提出 **Best-anchored Objective-guided Preference Optimization (BOPO)**，用于训练底层 LLM：
- **动机**：传统 RL 方法（如 GRPO）梯度由最优样本主导，其他样本贡献微弱。
- **改进**：
  - 构造偏好对时，以当前批次中的**最优可行解**为锚点（Best-anchored）。
  - 损失函数中引入**目标差距加权**（Objective-guided weighting）：差距越大，学习信号越强。
- 优势：提供更密集、更有效的监督信号，无需人工标注。

---

### **相比现有方法的优势**
| 维度 | 现有方法 | FALCON |
|------|--------|--------|
| **可行性保证** | 概率性（<100%） | **理论保证 100%** ✅ |
| **解的质量** | 可能因拒绝采样丢失优质解 | 修复保留局部结构，质量损失可控 |
| **推理效率** | 固定 $N$，易浪费计算 | 自适应采样，动态分配资源 ⚡️ |
| **通用性** | 多依赖问题特定设计 | 统一框架，可扩展至多种 CO 问题 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在 **7 个 NP-hard CO 问题** 上进行评估，覆盖三大领域：

| 问题类别 | 问题 | 规模 | 分布 | 参考求解器 |
|---------|------|------|------|------------|
| **Routing** | TSP, CVRP, OP | 10–100 节点 | Uniform, GM | LKH-3, COMPASS |
| **Graph** | MIS, MVC | 50–500 节点 | ER, BA | Gurobi |
| **Scheduling** | PFSP, JSSP | 10–100 作业 / 6–30 作业 | Taillard | QIG, OR-Tools |

- 训练集：各问题 500K 实例
- 测试集：各问题 100 实例

---

### **实验设置与评估指标**

#### **评估指标**
- **可行性率（Feasibility Rate, Fea.%）**：满足所有约束的解的比例。
- **最优性差距（Optimality Gap, Gap%）**：与参考解的目标值相对差距。
- **推理时间（Time/s）**：平均每实例耗时。

#### **基线方法对比**
共四类基线：
1. **通用 LLM（零样本）**：GPT-4o, Claude-3.5, Llama-3.3-70B, Qwen2.5-72B
2. **推理增强 LLM**：GPT-o1, DeepSeek-R1
3. **LLM 优化方法**：OPRO, LMEA, PHP, SGE
4. **神经 CO 求解器**：SFT Only, GRPO, LLMCoSolver

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 2）**

| 方法 | 平均可行性 | 平均 Gap% | 推理时间 (s) |
|------|-----------|----------|-------------|
| **FALCON (Adaptive)** | **100%** | **2.23%** | **6.3** |
| LLMCoSolver (N=8) | 94–100% | 2.14–11.01% | 9.8 |
| GRPO | 91–98% | 1.03–2.39% | 5.6 |
| GPT-4o | 6–88% | 11.7–20.57% | 5.3 |

> ✅ **FALCON 在所有 7 个问题上均实现 100% 可行性**  
> 📈 **平均 Gap% 优于或接近 SOTA 方法，且使用更少采样**

---

### **与基线方法的对比结果**
- **vs 通用 LLM**：FALCON 可行性远超（如 GPT-4o 在 CVRP 仅 6% 可行），Gap% 更低。
- **vs LLMCoSolver**：虽后者在部分问题可达高可行性，但仍无法保证 100%，且需更多采样（N=8）。
- **vs GRPO**：FALCON 在可行性上完胜（100% vs <100%），Gap% 相当甚至更优。

---

### **消融实验结果（Table 4）**

| 配置 | TSP Gap% | CVRP Gap% | 可行性 |
|------|--------|----------|-------|
| **FALCON (Full)** | 0.92 | 3.52 | 100% |
| w/o Grammar | 0.95 | 3.67 | 100% |
| w/o Repair | 0.89 | 3.41 | 94% / 85% ❌ |
| w/o Adaptive | 0.91 | 3.48 | 100% |
| w/o BOPO | 1.85 | 5.89 | 100% ❌ |
| SFT Only | 2.30 | 6.02 | 89% / 59% ❌ |

> 🔍 **关键发现**：
> - **修复层**是实现 100% 可行性的关键。
> - **BOPO** 显著提升解的质量（Gap% ↓）。
> - **自适应采样**大幅降低推理成本（见下表）。

---

### **自适应采样效率（Table 5）**
| 问题 | 固定 N=64 | 自适应采样 |
|------|----------|------------|
| | Gap% / 时间 | Gap% / 时间 / 采样数 |
| **TSP** | 0.89 / 42.5s | 0.92 / **12.8s** / **18.4** |
| **JSSP** | 6.85 / 52.4s | 6.98 / **41.2s** / **48.6** |
| **平均** | 2.18 / 45.6s | 2.23 / **20.3s** / **26.7** |

> ⏱️ **自适应采样节省约 55% 时间，58% 采样次数，质量几乎无损**

---

## **4. 关键结论和发现**

### **主要发现**
1. **可行性可被严格保证**：通过“语法约束 + 修复层”架构，FALCON 首次实现了 LLM-based CO 的 **100% 可行性理论保证**。
2. **质量损失可控**：BOPO 训练使模型生成高度可行的解，修复频率低（<3%–18.5%），且修复算子具有有界局部性，质量损失极小。
3. **自适应采样高效**：基于一致性（Consistency）的难度估计能准确区分难易实例，实现计算资源的智能分配。
4. **BOPO 优于 GRPO**：在相同训练预算下，BOPO 在 Gap% 和可行性上均显著优于 GRPO（图2），尤其在约束复杂问题（如 CVRP, MIS）上优势更大。

---

### **方法的局限性**
- **修复可能引入偏差**：虽然修复保证可行性，但可能偏向某种启发式策略（如贪心移除），影响全局最优性。
- **修复算子需手动设计**：尽管框架通用，但每个问题的修复算子仍需领域知识设计。
- **未考虑动态环境**：当前框架面向静态 CO 问题，难以直接扩展到在线或动态优化场景。

---

### **未来工作方向**
- **自动化修复算子生成**：结合程序合成或强化学习，自动发现高效的修复策略。
- **端到端可微修复**：探索可微分的修复机制，使梯度能回传至 LLM。
- **扩展至动态 CO**：将 FALCON 应用于 VRPTW、动态调度等更复杂的现实问题。
- **多目标优化支持**：扩展框架以处理 Pareto 最优性和多目标偏好。

---

> ✅ **总结**：FALCON 成功弥合了 LLM 的“软生成”与 CO 的“硬约束”之间的鸿沟，为 LLM 在真实世界优化系统中的可靠部署提供了坚实基础。

</details>

---

### 7. [Beyond Tokens: Semantic-Aware Speculative Decoding for Efficient Inference by Probing Internal States](https://arxiv.org/abs/2602.03708)

**Authors**: Ximing Dong, Shaowei Wang, Dayi Lin, Boyuan Chen, Ahmed E. Hassan  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.03708v1  

#### Abstract
Large Language Models (LLMs) achieve strong performance across many tasks but suffer from high inference latency due to autoregressive decoding. The issue is exacerbated in Large Reasoning Models (LRMs), which generate lengthy chains of thought. While speculative decoding accelerates inference by dr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《Beyond Tokens: Semantic-Aware Speculative Decoding for Efficient Inference by Probing Internal States》论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
- **传统 Speculative Decoding 的语义盲区**：现有的 **Speculative Decoding (SD)** 方法大多在 **token-level** 进行草案生成与验证，忽略了“**语义等价性**”（semantic equivalence）——即不同 token 序列可能表达相同含义（如 “The capital of France is Paris.” vs. “Paris is the capital of France.”）。
- 因此，即使草案在语义上正确，也可能因 token 不匹配而被错误拒绝，导致接受率低、效率下降。
- 特别是在 **Large Reasoning Models (LRMs)** 中，这种问题更严重，因为其输出是长链的推理步骤（Chain-of-Thought），表面形式差异大但语义一致的情况频繁出现。

### 提出的新方法：SemanticSpec
- **Semantic-Aware Speculative Decoding (SemanticSpec)**：提出一种全新的范式，将 speculative decoding 从 **token-level 提升到 sequence-level**，并基于语义进行验证。
- **核心机制**：
  - 引入 **Semantic Probability Estimator**：通过探查模型内部的 **hidden states** 来估计生成某个语义含义的概率（而非仅看 token 概率）。
  - 在验证阶段，比较草案模型和目标模型对同一语义序列的“语义概率”，决定是否接受整个语义单元。

### 相比现有方法的优势
| 维度 | 传统 Token-Level SD | Sequence-Level 方法（如 SpecReason） | SemanticSpec |
|------|---------------------|----------------------------------|------------|
| 验证粒度 | Token-by-token | 整个 reasoning step | 语义等价的 sequence |
| 语义感知 | ❌ 忽略语义等价 | ⚠️ 依赖 LLM-as-a-judge 或关键词 | ✅ 基于 internal states 的语义概率 |
| 可靠性 | 高（lossless）但效率低 | 低（judge 不可靠） | 高且高效 |
| 接受率 | 受限于字面匹配 | 易误判 | 更鲁棒，提升接受率 |

> ✅ **创新点总结**：
> - 首次提出 **语义概率**（semantic probability）概念，并设计可学习的预测器。
> - 利用 **LLM 内部 hidden states** 作为语义置信度代理，实现无需采样的高效估计。
> - 实现了真正意义上的 **语义对齐验证**，突破 token 匹配瓶颈。

---

## 2. 核心实验方法和设置

### 数据集
- **MATH-500**：数学推理基准，包含 500 道竞赛级数学题。
- **AIME24**：美国数学邀请赛题目集合。
- **AMC23**：美国数学竞赛题目。
- **GPQA-D**：专家级科学问答数据集，测试深度分析能力。

> 所有任务均涉及多步推理，适合评估 LRMs 和 speculative decoding 性能。

### 实验设置
- **目标模型 (Target Model)**：
  - `DeepSeek-R1-32B`
  - `QwQ-32B`
- **草案模型 (Draft Model)**：
  - `DeepSeek-R1-1.5B`（轻量级同系列模型）
- **训练数据**：
  - 使用 `SimpleScaling-S1K` 数据集训练 **Semantic Probability Predictor**，包含约 800K 数学与科学问答对。
  - 采用 `\n\n` 分隔 reasoning steps 构建 sequence 单元。

### 评估指标
| 指标 | 含义 |
|------|------|
| **Pass@1** | 正确生成答案的比例，衡量准确性 |
| **Latency (s)** | 总推理延迟，衡量效率 |
| **Token Per Second (TPS)** | 吞吐量，反映系统吞吐性能 |
| **Speedup Ratio** | 相对于标准 decoding 的加速比 |

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **SpecSampling** | Token-level | 标准 speculative decoding，逐 token 验证 |
| **SpecReason (Pan et al., 2025)** | Sequence-level | 使用 LLM-as-a-judge 对 reasoning step 打分验证 |
| **Speculative Thinking (Yang et al., 2025)** | Sequence-level | 基于“wait”、“alternatively”等提示词触发大模型接管 |
| **Draft-only / Target-only** | 控制组 | 仅用草案或目标模型直接生成 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型组合 | 方法 | Speedup | Pass@1 | TPS |
|---------|------|--------|--------|-----|
| DeepSeek-R1-32B | **SemanticSpec (Ours)** | **2.7×** | 0.892 (AMC23) → 0.910 (Math-500) | ↑ **1.67× vs SpecSampling** |
| QwQ-32B | **SemanticSpec (Ours)** | **2.1×** | 0.892 (AMC23) → 0.922 (Math-500) | ↑ **2.66× vs SpecSampling** |

> 💡 **说明**：SemanticSpec 在显著提速的同时保持了接近目标模型的准确率（平均仅下降 3.9%-4.9% Pass@1）。

### 与基线方法对比结果
- **相比 SpecSampling (token-level)**：
  - 平均 **降低 40.2%~45.3% 推理时间**
  - **Pass@1 提升 7.3%~17.3%**
  - **TPS 提升 1.67X~2.66X**

- **相比其他 sequence-level 方法**：
  - **SpecReason**：虽然也尝试语义验证，但依赖不可靠的 LLM judge，导致 Pass@1 较低。
  - **Speculative Thinking**：过度依赖启发式信号，容易误接受错误草案，Pass@1 最低。
  - **SemanticSpec 显著优于两者**，尤其在复杂推理任务（如 AIME24）上优势明显。

### 消融实验结果（Ablation Study）
| 变体 | 描述 | 结果 |
|------|------|------|
| **Random** | 语义概率随机赋值 | Pass@1 大幅下降，验证机制失效 |
| **Last-hidden** | 仅使用最后一层 hidden state | TPS 更高（计算开销小），但 **Pass@1 明显更低**（如 AIME24 上从 0.553↓到 0.400） |
| **Ours (All Layers + Avg Pooling)** | 使用所有层 hidden states 并平均池化 | **最佳平衡点**：高 Pass@1，合理 TPS |

> 🔍 发现：**multi-layer information 显著提升语义概率估计准确性**，尽管带来轻微计算开销。

### 泛化性实验
- **跨领域预测**（math ↔ science）：
  - 使用 science 数据训练的 predictor 应用于 math 任务，性能略有下降但依然有效（如 AMC23: 0.892 → 0.875）。
  - 表明 **semantic probability predictor 具备良好跨域泛化能力**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **语义等价性是提升 speculative decoding 效率的关键突破口**。
2. ✅ **LLM 的 internal states 能有效编码语义置信度**，可用于构建语义概率估计器。
3. ✅ **SemanticSpec 实现了更高接受率与更少回退**，尤其适用于 LRMs 的长链推理场景。
4. ✅ 相比 token-level 和现有 sequence-level 方法，SemanticSpec 在 **效率与准确性之间取得更好权衡**。

### 方法的局限性
- 当前方法依赖于能够访问模型 **internal hidden states**，限制了在黑盒 API 场景下的应用。
- 语义聚类依赖于 **DeBERTa-based entailment detection**，可能存在边界案例误判。
- 训练 semantic probability predictor 需要额外的离线训练成本。
- 实验仅在两个开源模型对上验证，**泛化性有待进一步检验**。

### 未来工作方向
- 探索 **zero-shot 或 prompt-based semantic probability estimation**，减少训练依赖。
- 将 SemanticSpec 扩展至 **vision-language models** 或 **code generation** 等多模态任务。
- 设计 **轻量化 probe 模块**，便于部署到边缘设备。
- 研究如何在 **不暴露 internal states 的情况下实现语义感知 decoding**（如蒸馏或 probing-free 方法）。

---

> 📌 **总结一句话**：  
> **SemanticSpec 通过“探查内部状态以估计语义概率”，实现了从 token 匹配到语义对齐的跨越，为高效、可靠的 LLM 推理提供了新范式。**

</details>

---

### 8. [NLI:Non-uniform Linear Interpolation Approximation of Nonlinear Operations for Efficient LLMs Inference](https://arxiv.org/abs/2602.02988)

**Authors**: Jiangyong Yu, Xiaomeng Han, Xing Hu, Chen Xu, Zhe Jiang, Dawei Yang  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.02988v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable performance across a wide range of tasks, but their deployment is often constrained by substantial memory footprints and computational costs. While prior work has achieved significant progress in compressing and accelerating linear layers, no...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《NLI: Non-uniform Linear Interpolation Approximation of Nonlinear Operations for Efficient LLMs Inference》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在推理过程中面临巨大的计算开销和内存带宽压力，尤其是**非线性层**（如 SiLU、Softmax、RMSNorm）仍依赖高精度浮点运算（如 FP32），成为性能瓶颈。尽管线性层已通过量化（如 W8A8）显著优化，但现有非线性函数近似方法存在以下问题：
- **输入范围受限**：例如 NN-LUT 设计仅适用于 `[-5,5]`，而 LLM 中激活值常超出 `±100`；
- **依赖数据校准（calibration）**：泛化能力差，在不同模型或层间难以复用；
- **硬件不友好**：需要大量比较器（comparators）或复杂电路，影响效率。

### 提出了什么新方法或新思路
本文提出 **Non-uniform Linear Interpolation (NLI)**，一种无需校准、动态规划最优且硬件友好的非线性函数近似框架，包含两个部分：

- **NLI-Algorithm（软件算法）**  
  将切点（cutpoint）选择建模为一个**动态规划（Dynamic Programming）问题**，基于 Bellman 最优性原理，在 FP16 域上寻找全局最优的非均匀插值节点，最小化加性插值误差。该方法不依赖训练数据分布，是**完全无监督、可跨模型复用**的查找表（LUT）生成方式。

- **NLI-Engine（硬件设计）**  
  构建了一个即插即用的通用非线性计算单元，采用**两级地址转换机制**（two-level address translation）和流水线架构，大幅减少比较器数量并提升吞吐量。

### 相比现有方法的优势
| 维度 | NLI | NN-LUT / 其他方法 |
|------|-----|------------------|
| **输入范围适应性** | 支持 `[-150, 150]` 超大范围 | 通常限于 `[-5,5]` 或 `[-10,10]` |
| **是否需校准** | ❌ 无需数据校准 | ✅ 必须依赖特定数据分布进行训练 |
| **硬件效率** | 高效：仅需 10 个 comparator | 低效：需 256+ 个 comparator |
| **误差控制** | 动态规划保证全局最小误差 | 局部拟合，外推时误差爆炸 |
| **通用性** | 支持多种函数（exp, rsqrt, SiLU, tanh 等） | 多为特定函数定制 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **语言模型任务**：
  - **Wikitext-2**：用于评估 Perplexity（PPL）
  - **Zero-shot benchmark suite**：包括 ARC-c/e、BoolQ、PIQA、HellaSwag、OBQA、LAMBADA、SIQA、WinoGrande
  - **综合能力基准**：MMLU（多学科知识）、GSM8k（数学推理）、HumanEval（代码生成）

- **视觉模型验证**（验证泛化性）：
  - ViT-Small、DETR、RT-DETR-L、YOLOv8-M

- **测试模型**：
  - LLMs：Llama3-8B/70B、Qwen2.5-7B/32B、Qwen1.5-110B、Qwen3-8B/30B-A3B
  - Vision Models：ViT、CNN-based detectors

### 实验设置和评估指标
| 类别 | 设置说明 |
|------|--------|
| **精度评估** | 在 FP16 推理下替换原生非线性操作，报告：<br>• Perplexity ↓（越小越好）<br>• 准确率 ↑（MMLU/GSM8k/HumanEval 等） |
| **硬件评估平台** | 使用 **SMIC 28nm 工艺库**，通过 Design Compiler 综合电路，评估：<br>• Area（面积）<br>• Power（功耗）<br>• Throughput（吞吐量）<br>• Efficiency = Throughput / (Area × Power) |
| **NLI 参数配置** | 采用 `2 + 8×32 + 1 = 259` cutpoints 结构：<br>• 10 个宏区间（macro-intervals）<br>• 首尾不划分，中间 8 个各分 32 子段<br>• 利用 DP 仅优化 11 个宏端点，降低搜索成本 |

### 基线方法对比
- **FP32**：全精度浮点实现，作为黄金标准
- **NN-LUT**：基于神经网络学习 LUT 参数的方法（Yu et al., 2022）
- **RI-LUT**：Range-Invariant LUT 方法（Kim et al., 2023）
- **Uniform 259**：均匀划分的线性插值
- **Curvature 259**：按曲率密度采样的非均匀插值

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 📊 软件精度表现（来自 Table 1 & Table 8）
| 模型 | 方法 | MMLU | GSM8k | HumanEval | PPL (Wikitext-2) |
|------|------|-------|--------|------------|------------------|
| Llama3-8B | FP32 | 62.16 | 50.19 | 35.37 | 6.14 |
| | NLI | **62.14** | **50.49** | **35.37** | **6.14** |
| | NN-LUT | 60.01 | 49.42 | 34.15 | 8.28 |
| Qwen2.5-7B | FP32 | 70.56 | 44.28 | 40.24 | 7.46 |
| | NLI | **70.67** | **43.97** | **39.63** | **7.46** |
| | NN-LUT | 25.51 | 0 | 0 | 28194 |

> ✅ **NLI 在所有模型上几乎完全保持 FP32 精度，而 NN-LUT 在多个任务上崩溃（如 GSM8k 得分为 0）**

#### 🔧 硬件效率表现（来自 Table 5 & Table 6）
| 模块 | Area (μm²) | Power (mW) | Throughput | Efficiency |
|------|-----------|-----------|------------|------------|
| NN-LUT | 23,238 | 46 | 1G | 0.94 |
| RI-LUT | 23,647 | 48 | 1G | 0.88 |
| **NLI (Ours)** | **7,787** | **34** | **1G** | **3.78** |

> ✅ **NLI 实现了超过 4× 的效率提升（vs NN-LUT 提升 4.02×，vs RI-LUT 提升 4.29×）**
>
> ✅ 面积节省 **68–69%**，功耗更低，得益于更少的 LUT 和 comparator

#### 📈 插值误差分析（来自 Figure 2 & Figure 3）
- 在 `[-150,150]` 范围内，NLI 对 SiLU 的最大绝对误差 < `1.2×10⁻³`，远低于 NN-LUT 的“误差尖峰”现象
- 对 exp、rsqrt、gelu 等函数，NLI 近似曲线与 FP32 参考高度重合，最坏情况误差控制在 `1.5×10⁻³` 内

### 消融实验结果（Ablation Studies）

#### 表格 2：两阶段结构 vs 仅宏观切点（Macro-only）
| 方法 | Cutpoints | MMLU | GSM8k |
|------|----------|-------|--------|
| NLI (2+8×32+1) | 259 | 70.67 | 43.97 |
| Macro-only (DP, M=11) | 11 | 21.14 | 0 |

> ❗ 使用仅 11 个切点无法满足精度要求，证明微分区间的必要性

#### 表格 3：直接搜索 259 个非均匀切点 vs NLI 两阶段设计
| 方法 | Search Time (s) | MMLU | GSM8k |
|------|------------------|-------|--------|
| Direct DP (259) | ~17,000 | 70.65 | 44.08 |
| NLI (2+8×32+1) | **610** | 70.67 | 43.97 |

> ✅ NLI 的两阶段设计将搜索时间从 **17,000 秒降至 610 秒（快 28×）**，同时精度相当，且更适合硬件部署

#### 表格 4：与其他启发式方法对比
| 方法 | MMLU | GSM8k |
|------|-------|--------|
| Uniform 259 | 29.1 | 18.13 |
| Curvature 259 | 65.74 | 32.58 |
| **NLI** | **70.65** | **43.97** |

> ✅ NLI 显著优于均匀划分和基于曲率的划分，说明其动态规划策略的有效性

---

## 4. 关键结论和发现

### 主要发现
1. **NLI 实现了高精度与高效性的统一**：
   - 在多种 LLM 和视觉模型中，**替换非线性层后精度无损**，甚至略优于 FP32（可能因数值稳定性改善）。
   - **无需任何数据校准**即可获得跨模型、跨层可用的 LUT，极大增强了部署灵活性。

2. **动态规划可用于全局最优切点搜索**：
   - 将插值误差建模为可分解的目标函数，利用 Bellman 原理求解，首次实现了**理论最优的非均匀线性插值方案**。

3. **软硬协同设计大幅提升效率**：
   - 两级地址转换机制将 comparator 数量从 259 降到 10，结合预加载 scale factor 和流水线，使硬件效率提升 **4× 以上**。

4. **支持广泛非线性函数**：
   - 成功应用于 exp、rsqrt、reciprocal、SiLU、GELU、Sigmoid、Tanh、Mish、HardSwish 等常见函数（见 Appendix A.6），具备强通用性。

### 方法的局限性
- **静态 LUT**：当前 LUT 是离线生成的，不能自适应变化的数据分布（虽然这不是目标）。
- **对极端 outlier 的处理依赖 clamp**：虽然实验证明影响极小（<0.1% 激活值被截断），但在某些特殊场景可能存在风险。
- **目前仅针对前向推理**：未涉及训练阶段的应用。

### 未来工作方向
- 扩展至 **Transformer 中其他复杂模块**（如 Attention softmax with mask）；
- 探索 **动态更新 LUT** 的可能性以应对分布漂移；
- 将 NLI 集成进主流 NPU 架构（如 TPU、NVDLA）作为标准非线性加速单元；
- 探索在 **低比特量化联合优化** 中的作用（如 W4A4 场景下的非线性补偿）。

---

> 💡 **总结一句话**：  
> NLI 通过将非线性函数近似转化为动态规划问题，提出了首个无需校准、全局最优且高度硬件友好的线性插值框架，在保持零精度损失的同时，实现了超过 **4× 的硬件效率提升**，为大规模模型边缘部署提供了实用解决方案。

</details>

---

### 9. [Position: Agentic Evolution is the Path to Evolving LLMs](https://arxiv.org/abs/2602.00359)

**Authors**: Minhua Lin, Hanqing Lu, Zhan Shi, Bing He, Rui Mao, Zhiwei Zhang, Zongyu Wu, Xianfeng Tang, Hui Liu, Zhenwei Dai, Xiang Zhang, Suhang Wang, Benoit Dumoulin, Jian Pei  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.00359v1  

#### Abstract
As Large Language Models (LLMs) move from curated training sets into open-ended real-world environments, a fundamental limitation emerges: static training cannot keep pace with continual deployment environment change. Scaling training-time and inference-time compute improves static capability but do...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Position: Agentic Evolution is the Path to Evolving LLMs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Large Language Models (LLMs)** 在部署到开放、动态的真实世界环境时面临一个根本性瓶颈：**train-deploy gap**（训练-部署环境差异）。  
- 静态训练无法应对持续变化的现实场景（如API变更、格式迁移、用户反馈等）。
- 现有适应方法（如在线微调或记忆累积）缺乏**战略性代理能力**（strategic agency），导致改进不可靠、不可持续。

### ✅ 提出了什么新方法或新思路

提出 **Agentic Evolution（代理式进化）** 范式，并构建通用框架 **A-Evolve**：

- 将模型在部署期间的持续学习视为一个**目标导向的自主优化过程**，而非简单的参数更新或记忆存储。
- 引入 **Evolver Agent** —— 一个显式的、具备决策能力的“进化器”，负责诊断失败、规划修复、执行更新并验证效果。
- 提出 **Evolution-Scaling Hypothesis（进化可扩展性假设）**：  
  > 模型的适应能力（adaptation capacity）随着分配给进化过程的 **evolution-time compute** 增加而系统性提升，构成继 training-time 和 inference-time 之后的**第三条可扩展轴**。

### ✅ 相比现有方法的优势

| 方法类型 | 局限性 | A-Evolve 的优势 |
|--------|------|----------------|
| **Parametric Fine-tuning**（如 test-time training） | 易引发 catastrophic forgetting，更新不透明，难以治理 | 更新作用于**显式、可审计的持久化组件**（工具、知识、测试），避免权重漂移 |
| **Heuristic Memory Accumulation**（如 AWM） | 导致 context saturation，记忆噪声大，复用率低 | 通过结构化 artifact（模块化工具/技能）实现**能力固化**，支持复用与组合 |
| **Static Prompt Engineering**（如 APE） | 改进依赖表面模式匹配，缺乏因果推理 | Evolver 具备**goal-oriented diagnosis**，能定位根本原因并生成针对性修复 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **AppWorld**（Trivedi et al., 2024）：  
  - 一个面向交互式编码智能体的可控仿真环境。
  - 包含约 9 个日常应用（如 Amazon、Spotify、Venmo）和 457 个 API。
  - 模拟真实用户行为，任务需跨多个应用调用 API，具有复杂依赖关系。
- 实验划分：
  - **50 个训练任务**用于演化学习
  - **50 个测试任务**用于评估泛化能力（来自 `test-normal` 分割）

### ⚙️ 实验设置和评估指标

#### 模型配置
- **Solver**（执行任务的主体）：Claude Haiku 4.5, Sonnet 4.5, GPT-5, Gemini 3 Flash
- **Evolver**（进化控制器）：默认使用 Claude Sonnet 4.5，部分实验对比不同规模模型（Haiku/Sonnet/Opus）

#### 固定预算控制
- **Solve-time compute budget**：限制每任务最大 tool calls、steps 或 tokens
- **Evolve-time compute budget**：限制每次进化步的 token 数和 tool 调用数，确保公平比较

#### 评估指标
| 指标 | 定义 | 说明 |
|------|------|------|
| **TGC (Task Goal Completion)** | 成功完成的任务比例（所有单元测试通过） | 衡量最终成功率 |
| **APT (Average Passed Tests)** | 平均每个任务通过的单元测试比例 | 衡量渐进式能力提升，反映部分成功 |

### 🔁 基线方法对比

| 基线 | 类型 | 描述 |
|------|------|------|
| **Vanilla** | 无演化 | 直接调用 solver，无任何持久状态更新 |
| **APE** (Zhou et al., 2022) | 非参数启发式 | 基于搜索的 prompt 进化，通过评分选择最优指令 |
| **AWM** (Wang et al., 2024) | 非参数启发式 | 从历史轨迹中提取可重用 workflow 记忆 |
| **A-Evolve**（本文） | Agentic Evolution | 显式 Evolver Agent 执行诊断 → 规划 → 更新 → 验证闭环 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（Tab. 1）

| Method | Claude Haiku 4.5 (TGC/APT) | Gemini 3 Flash (TGC/APT) |
|--------|----------------------------|--------------------------|
| Vanilla | 32 / 51.16 | 56 / 80.45 |
| APE     | 30 / 56.00 | 52 / 84.00 |
| AWM     | 46 / 65.76 | 52 / 87.75 |
| **A-Evolve** | **64 / 84.31** | **82 / 92.05** |

> ✅ **结论**：
> - A-Evolve 在所有 solver 上显著优于基线，尤其对小模型增益更大（Haiku 提升 +32% TGC）。
> - **窄容量差距**：A-Evolve + Haiku 的表现（64% TGC）超过 Vanilla + Sonnet（42%），表明**程序性能力积累可媲美更强 backbone**。

### 🔍 消融实验结果（Fig. 3）

对比 A-Evolve 的四个变体（移除某一组件）：

| 变体 | 移除组件 | 影响 |
|------|--------|------|
| **A-Evolve/D** | Diagnosis | 缺乏根因分析，仅做表面修补，性能下降明显 |
| **A-Evolve/A** | Analysis Tools | 无法跨 episode 发现共性模式，改进零散 |
| **A-Evolve/P** | Planning | 无法协调多 artifact 更新（如改 tool 不改 schema），导致逻辑断裂 |
| **A-Evolve/V** | Verification | 最严重退化！提交缺陷 artifact（语法错误、运行崩溃），污染上下文，引发回归 |

> ✅ **关键发现**：  
> - **Verification 是稳定性关键**，防止有害更新扩散。
> - 四个模块形成**不可分割的闭环**，缺一不可。

### 📈 进化可扩展性分析（Fig. 4）

#### （a）增加 **evolution step**（即 $ C_{\text{evolve}} $）
- A-Evolve 性能随计算资源单调上升，未见饱和。
- AWM 很快达到平台期，显示其**启发式方法不具备可扩展性**。

#### （b）增大 **evolver 模型尺寸**
- 使用更大的 evolver（Haiku → Sonnet → Opus）带来持续性能提升。
- 更大的 evolver 能更准确诊断、生成更鲁棒 artifact，减少验证失败。

> ✅ 支持 **Evolution-Scaling Hypothesis**：  
> 分配更多 compute 给 evolution 过程，确实能系统性提高适应上限。

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **Agentic Evolution 是解决 train-deploy gap 的必然路径**：
   - 必须将进化本身视为一个**由目标驱动的自主决策过程**，而非机械流水线。

2. **A-Evolve 框架有效实现了 agentic evolution**：
   - 通过 **persistent artifact state**（知识 K、工具 T、验证 V）暴露可编辑接口。
   - 通过 **solve-evolve loop** 分离任务执行与能力进化。
   - 通过 **explicit evolver agent** 实现诊断、规划、更新、验证的闭环治理。

3. **进化是可扩展的**（Evolution-Scaling Hypothesis）：
   - 适应能力随 $ C_{\text{evolve}} $ 增加而提升，构成新的 scaling axis。
   - 投资于“更好地进化”比反复“更努力思考”更具长期效益。

4. **小模型 + 持续进化 ≈ 大模型**：
   - 程序性能力的积累可以弥补静态模型容量不足，为低成本部署提供可能。

### ⚠️ 方法的局限性

- **Compute 开销较高**：维护 evolver agent 需额外推理成本，短期效率低于简单启发式。
- **Artifact 设计依赖工程经验**：如何定义良好的工具/知识/验证结构仍需人工设计。
- **当前验证机制有限**：主要依赖 unit test 和 syntax check，对语义正确性保障不足。
- **尚未处理多 agent 协同演化**：目前聚焦单 agent 场景。

### 🔮 未来工作方向

1. **Benchmarking**：
   - 构建专门衡量“演化能力”的基准，强调**artifact 的持久性、复用性和抗干扰性**。

2. **Framework 改进**：
   - 提升 diagnosis、planning、verification 模块的能力，使其更高效逼近 compute-optimal frontier。

3. **理论建模**：
   - 建立 agentic evolution 的形式化理论，例如将其建模为组合程序空间上的优化问题。
   - 证明其相对于非代理方法的优越性（separation results）。

4. **安全与对齐机制强化**：
   - 发展更严格的 validation gates（如形式化验证、人类-in-the-loop 审核）。
   - 防止 capability drift 和目标错位（misalignment）。

---

> 💬 **Impact Statement 精要**：
> - **隐私友好**：本地演化减少敏感数据上传。
> - **可持续 AI**：将昂贵的推理转化为可复用的工具，降低长期部署成本。
> - **风险可控**：显式 artifact + 验证门控，保证可审计、可回滚。

</details>

---

### 10. [Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach](https://arxiv.org/abs/2602.01131)

**Authors**: Yue Zhong, Jiawen Kang, Yongju Tong, Hong-Ning Dai, Dong In Kim, Abbas Jamalipour, Shengli Xie  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.01131v1  

#### Abstract
With the rapid expansion of the low-altitude economy, Unmanned Aerial Vehicles (UAVs) serve as pivotal aerial base stations supporting diverse services from users, ranging from latency-sensitive critical missions to bandwidth-intensive data streaming. However, the efficacy of such heterogeneous netw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对**低空经济**（Low-Altitude Economy, LAE）中无人机**（UAV）**作为空中基站时面临的资源分配与控制稳定性之间的矛盾问题。传统网络设计通常以吞吐量为中心，忽略了通信延迟对物理控制系统稳定性的直接影响。在灾后救援等安全关键任务中，UAV需同时支持高带宽流媒体和超可靠低延迟控制信号，而有限的**bandwidth**和**energy**资源导致通信延迟，进而破坏**networked control system**的稳定性。

### **提出了什么新方法或新思路**

论文提出了一套全新的**控制导向**（control-oriented）资源管理框架，其核心创新点如下：

- **构建了SC³闭环系统模型**（Sensing-Communication-Computing-Control）  
  将UAV与用户的交互建模为一个完整的闭环反馈系统，明确量化了**end-to-end latency**对控制性能的影响。

- **引入Lyapunov稳定性理论进行跨域约束转换**  
  利用**Lyapunov stability theory**将抽象的物理控制稳定性要求转化为可量化的**communication latency边界**，实现了从“控制需求”到“通信资源”的映射。

- **设计了基于Stackelberg博弈的资源分配机制**  
  构建了一个**Stackelberg game**，其中UAV作为**leader**动态定价以调节负载并保障系统稳定，用户作为**follower**根据服务紧急程度优化带宽请求，实现激励相容的分布式决策。

- **提出轻量级Pruning-based PPO算法**  
  针对标准**Deep Reinforcement Learning**（DRL）在边缘设备上计算开销过大的问题，提出一种结合**dynamic structured pruning**的**Proximal Policy Optimization**（PPO）算法，在训练过程中动态剪枝冗余神经元，显著压缩模型规模，降低推理延迟和能耗。

### **相比现有方法的优势**

| 对比维度 | 现有方法 | 本文方法 |
|--------|--------|--------|
| **系统视角** | 通信与控制解耦 | SC³闭环融合，联合优化 |
| **稳定性保障** | 无显式建模或仅经验性处理 | 基于Lyapunov理论的严格数学保证 |
| **激励机制** | 基于效用或AoI优化 | 价格驱动，兼顾紧急度与稳定性 |
| **算法部署可行性** | 重型DRL难以部署于UAV | 轻量化pruning-based PPO，适合边缘执行 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

论文未使用公开真实数据集，而是基于典型LAE场景进行**仿真建模**，参数设定参考了相关文献（如[8][44]），模拟灾后应急响应环境下的多UAV多用户协同。

### **实验设置**

- **场景配置**：
  - UAV数量：3 或 可变（3–12）
  - 用户数量：5 或 可变（5–20）
  - 部署区域：二维平面，UAV固定高度飞行
  - 信道模型：自由空间路径损耗（Free-space path loss）
- **关键参数**（见Table I）：
  - 采样周期 $ \epsilon_n = 0.5s $
  - 控制时间常数 $ \tau_n = 0.005s $
  - Lyapunov衰减速率 $ \rho_n = 0.95 $
  - 总带宽 $ K_{\text{total}} \in [15, 25]\,\text{MHz} $
  - 用户数据包大小 $ S_i \in [40, 64]\,\text{kbits} $

### **评估指标**

- **Test Reward**：UAV获得的平均效用（Utility）
- **Bandwidth Allocation Composition**：区分用于满足稳定性最低要求与额外效用提升的带宽
- **Pricing & Request Strategies Evolution**：策略收敛过程可视化
- **算法复杂度与推理延迟**：通过模型压缩率间接体现

### **基线方法对比**

- **Standard PPO**：未剪枝的标准多智能体PPO算法
- **Greedy Algorithm**：贪心策略，优先服务信道质量好或紧急度高的用户
- **Random Algorithm**：随机分配资源

---

## 3. 主要实验结果和性能指标

### **关键性能数据与对比结果**

#### ✅ **学习效率与最终性能**
- **图3**显示，**Pruning-based PPO**不仅收敛速度更快，且最终**test reward**高于标准PPO（约高出10–15%），表明剪枝起到了正则化作用，缓解过拟合。
- 相比之下，Greedy和Random算法性能远低于所有DRL方法。

#### ✅ **策略演化与均衡达成**
- **图5**展示了UAV定价策略与用户带宽请求随迭代逐步收敛的过程，验证了系统达到**Stackelberg Equilibrium**。
- 定价初期波动大，后期趋于稳定；用户根据价格和自身权重自适应调整请求。

#### ✅ **不同网络规模下的泛化能力**
- **图6(a)**：当UAV数量固定为3时，随着用户数增加（5→20），UAV总收益显著上升，说明机制能有效激励服务更多用户。
- **图6(b)**：当用户数固定为15时，随着UAV数量增加（3→12），**单个UAV平均reward下降**，反映市场饱和效应，符合经济学直觉。

#### ✅ **资源分配合理性分析**
- **图2(d)/(h)** 显示，所有用户实际分配的带宽均**严格超过由Lyapunov推导出的最小需求**（Min Requirement），证明稳定性约束被有效执行。
- 带宽分配与**SNR**和**urgency weight**呈强正相关，高优先级或信道好的用户获得更多资源。

### **消融实验结果**

- **图4**研究了**pruning start epoch**（$ t_0 $）的影响：
  - $ t_0 = 50 $（早剪枝）：性能最优，说明早期结构干预有助于策略与稀疏拓扑共同演化。
  - $ t_0 = 300 $（晚剪枝）：次优，保留了密集训练阶段的良好表征。
  - $ t_0 = 200 $（中期剪枝）：表现最差，干扰了关键学习阶段的策略形成。
- 结论：**early pruning is beneficial**，支持所提动态剪枝机制的设计理念。

---

## 4. 关键结论和发现

### **主要发现**

1. **控制-通信联合设计至关重要**：单纯优化通信性能无法保障LAE中安全关键任务的可靠性，必须将**control stability**纳入资源分配决策。
2. **Lyapunov理论可有效桥接物理层与逻辑层**：通过建立**control-to-communication mapping**，可将稳定性要求转化为硬性通信约束，指导资源分配。
3. **Stackelberg博弈是合适的建模范式**：能够自然建模UAV与用户间的领导-跟随关系，并通过价格机制实现负载均衡与激励兼容。
4. **轻量化DRL可行且必要**：提出的**pruning-based PPO**在不牺牲性能的前提下大幅降低模型复杂度，使实时在线学习成为可能。

### **方法的局限性**

- **依赖线性控制模型假设**：当前Lyapunov分析基于线性时不变系统（LTI），对非线性或强扰动场景适用性有待验证。
- **集中式训练、分布式执行**：虽适用于小规模UAV群，但在大规模网络中仍面临可扩展性挑战。
- **仿真环境理想化**：未考虑极端天气、移动障碍物、恶意攻击等现实因素。

### **未来工作方向**

- 研究**能量与时变带宽联合波动**下的复合端到端延迟建模。
- 探索**双向控制-通信协同设计**机制，实现更深层次的系统融合。
- 扩展至**multi-leader multi-follower Stackelberg game**，支持更复杂的UAV协作架构。
- 引入**federated learning**或**decentralized MARL**以增强隐私保护与可扩展性。

--- 

> **总结一句话**：本文开创性地将**Lyapunov稳定性理论**融入**Stackelberg博弈**框架，并通过**pruning-based DRL**实现高效求解，在保障UAV控制稳定的前提下最大化系统效用，为低空经济中的资源管理提供了兼具理论严谨性与工程实用性的解决方案。

</details>

---

### 11. [ForesightKV: Optimizing KV Cache Eviction for Reasoning Models by Learning Long-Term Contribution](https://arxiv.org/abs/2602.03203)

**Authors**: Zican Dong, Peiyu Liu, Junyi Li, Zhipeng Chen, Han Peng, Shuo Wang, Wayne Xin Zhao  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.03203v1  

#### Abstract
Recently, large language models (LLMs) have shown remarkable reasoning abilities by producing long reasoning traces. However, as the sequence length grows, the key-value (KV) cache expands linearly, incurring significant memory and computation costs. Existing KV cache eviction methods mitigate this ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ForesightKV: Optimizing KV Cache Eviction for Reasoning Models by Learning Long-Term Contribution**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
大型语言模型（LLMs），尤其是具备复杂推理能力的**reasoning models**，在生成长文本推理链时会产生极长的序列。这导致其内部维护的 **Key-Value (KV) Cache** 大小随序列长度线性增长，带来显著的内存开销和计算延迟，严重限制了并发能力和推理效率。

现有的 KV Cache 压缩或驱逐方法（如 SnapKV、H2O、R-KV）通常基于启发式规则（如 attention scores、位置信息）来判断 KV 对的重要性并进行永久删除。然而，这些方法难以捕捉复杂的跨注意力头动态依赖关系，尤其在语义依赖型模式下表现不佳，容易误删关键信息，造成推理错误。

---

### **提出了什么新方法或新思路**
本文提出 **ForesightKV**，一种**基于训练的 KV Cache 驱逐框架**，通过学习预测每个 KV 对的**长期贡献**，实现更智能、自适应的缓存管理。

其核心创新在于一个**两阶段训练范式**：

1. **监督学习阶段（Supervised Learning）**：
   - 提出 **Golden Eviction 算法**：利用未来 attention 分数作为“理想”驱逐标签，识别每一步应保留的最优 KV 对集合。
   - 使用 **Pairwise Ranking Loss** 训练一个轻量级的 **scoring model**，使其能够预测 KV 对的相对重要性。

2. **强化学习阶段（Reinforcement Learning）**：
   - 将 KV Cache 驱逐建模为 **Markov Decision Process (MDP)**。
   - 使用 **GRPO 算法** 进行优化，奖励函数聚焦于减少因驱逐导致的 **low-entropy tokens** 的语言建模损失激增，从而缓解事实性错误的累积。

整个过程仅更新轻量级的 scoring model，**不修改原始 LLM 参数**，保证了通用性和低侵入性。

---

### **相比现有方法的优势**
| 维度 | 传统方法（SnapKV/H2O/R-KV） | ForesightKV |
|------|-----------------------------|-----------|
| **决策依据** | 启发式规则（静态/局部） | 学习得到的动态重要性评分 |
| **信息利用** | 当前窗口或历史统计 | 利用未来信息构建监督信号 |
| **优化目标** | 单步最优 | 长期推理质量最大化 |
| **对 low-entropy tokens 的保护** | 弱 | 显式建模并通过 RL 强化 |
| **泛化能力** | 任务特定调优 | 在数学、科学、编码等多任务上表现良好 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **训练数据**：使用 Qwen3-4B 在 **STILL dataset** 上生成的长于 4096 token 的正确推理轨迹。
- **评估基准**：
  - **AIME2024** 和 **AIME2025**：两个复杂数学推理基准。
  - **GPQA**：研究生级别的科学问答数据集。
  - **LiveCodeBench V3**：代码生成与执行评测基准。

---

### **实验设置和评估指标**
- **评估模型**：
  - Qwen3-1.7B
  - Qwen3-4B
  - DeepSeek-R1-Distill-Qwen-7B
  - MiniCPM-4.1-8B（用于验证泛化性）
- **KV Cache 设置**：
  - 缓存预算（Cache Budget B）：1K, 2K, 4K, 8K tokens
  - 驱逐频率（Eviction Length L）：256 或 512 tokens
- **评估指标**：
  - **Pass@1 准确率**（主要指标）
  - **吞吐量（Throughput）** 和 **最大并发批次（#MCB）**
  - **注意力输出余弦相似度**（衡量压缩后 attention 分布保真度）
  - **语言建模损失变化（ΔLoss）**

---

### **基线方法对比**
- **SnapKV**：基于最近查询窗口的 attention score 判断重要性。
- **H2O**：保留具有高累计 attention score 的“重击者”（heavy hitters）。
- **R-KV**：结合 attention score 与 token-level 相似性，考虑冗余性。
- **Full KV Cache**：无压缩，作为性能上限。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- 在 **AIME2024** 上，ForesightKV 在 **仅 1K KV 缓存预算** 下达到：
  - **Qwen3-4B**: **54.5** Pass@1
  - 而 R-KV 在 **2K 预算** 下仅为 **44.8**
- 在 **4K 预算** 下，ForesightKV 接近甚至超过 Full KV Cache 性能：
  - Qwen3-4B-AIME2024: **69.2** vs Full: ~63.3–65.0（部分配置下反超）
- **性能保留率**：
  - 在 2K 预算下保留 **92%** 原始性能
  - 在 4K 预算下保留 **99%** 原始性能

---

### **与基线方法的对比结果**
- **全面超越所有基线**，即使在**一半缓存预算**下仍显著优于基线。
- 在 **AIME2024/2025** 上，ForesightKV 在 1K 预算下的性能远超 R-KV / SnapKV 在 2K 或 4K 下的表现。
- **吞吐量提升显著**：
  - 在 32K 序列长度下，**1K 预算**即可实现高达 **9.79×** 的吞吐加速（vs Full Cache）。
  - 即使在 4K 预算下，仍有 **5.14×** 加速。

| 方法 | 32K Throughput (vs Full) |
|------|--------------------------|
| Full | 1.00× |
| ForesightKV-1K | **9.79×** |
| ForesightKV-2K | 7.11× |
| ForesightKV-4K | 5.14× |

---

### **消融实验结果**
#### **(1) 强化学习的作用**
- **ForesightKV (w/o RL)** vs **ForesightKV**：
  - 在 AIME2024 上从 51.7 → **54.5**
  - 表明 RL 阶段有效提升了对关键 token 的保护能力。

#### **(2) 采样策略与输入特征**
| 输入特征 | 采样方式 | AIME24 | AIME25 |
|---------|--------|--------|--------|
| Attn+KV | Top-K + MN ✅ | 51.7 | 40.9 |
| Attn only | Top-K + MN | 37.5 | 22.9 |
| Attn+KV | Top-K only | 46.0 | 37.7 |
| Attn+KV | MN only | 16.5 | 13.8 |

✅ 结论：**同时使用 attention features 和 KV representations**，并采用 **Top-K + Multinomial Sampling** 是最优组合。

#### **(3) 奖励函数设计**
- 使用 **Lours**（基于 MSE 的损失惩罚）作为奖励函数效果最佳。
- 直接最小化整体语言建模损失（Lall）反而可能导致性能下降。
- 聚焦于 **loss increase 较大的 low-entropy tokens** 最有效。

---

## **4. 关键结论和发现**

### **主要发现**
1. **KV 对的重要性是动态且复杂的**，存在 **global、position-dependent、semantic-dependent** 三种模式，其中语义依赖型最难被规则捕获。
2. **low-entropy tokens 对 KV 驱逐极为敏感**，其损失增加常导致数字、符号等事实错误，进而破坏后续推理逻辑。
3. **ForesightKV 能更准确地保留关键信息**，其 attention 输出与原始模型的余弦相似度更高（1K 下达 0.9736 vs R-KV 的 0.9628）。
4. **两阶段训练相辅相成**：
   - 监督学习提供良好的初始策略；
   - 强化学习进一步优化长期推理一致性。

---

### **方法的局限性**
- **依赖高质量的长推理轨迹进行训练**，若训练数据不足或质量差，可能影响 scoring model 效果。
- **引入额外的 scoring model 和采样开销**，尽管实验证明其开销极小（仅占总时间 ~2.7%），但在极端低延迟场景下仍需权衡。
- 当前训练集中在数学任务，虽然展现出良好泛化，但在其他领域（如对话、创意写作）的有效性有待验证。

---

### **未来工作方向**
- 探索 **无需未来信息** 的自回归式监督信号构建方法。
- 将 ForesightKV 扩展至 **多模态模型** 的缓存管理。
- 研究 scoring model 的 **参数共享机制**，以降低部署成本。
- 结合 **KV Cache merging** 与 **eviction**，实现更高效的混合压缩策略。

---

> **总结**：ForesightKV 通过**监督+强化学习**的双阶段训练，首次实现了对 KV Cache 长期贡献的显式建模，在大幅降低缓存预算的同时，显著优于现有规则驱动方法，为高效长文本推理提供了新的研究范式。

</details>

---

### 12. [EvoOpt-LLM: Evolving industrial optimization models with large language models](https://arxiv.org/abs/2602.01082)

**Authors**: Yiliu He, Tianle Li, Binghao Ji, Zhiyuan Liu, Di Huang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.01082v1  

#### Abstract
Optimization modeling via mixed-integer linear programming (MILP) is fundamental to industrial planning and scheduling, yet translating natural-language requirements into solver-executable models and maintaining them under evolving business rules remains highly expertise-intensive. While large langu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：EvoOpt-LLM: Evolving industrial optimization models with large language models**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
工业级优化建模（如 **MILP**）在制造、供应链、物流等领域至关重要，但存在以下瓶颈：
- **建模门槛高**：业务需求以自然语言描述，需专家手动转化为数学模型，过程耗时且易错。
- **模型维护困难**：业务规则持续变化，传统方法需重建整个模型，难以支持动态演化。
- **求解效率低**：大规模 **MILP** 包含大量冗余变量，导致求解时间长，而传统 **presolve** 技术依赖确定性规则，无法利用历史数据中的隐含模式。

现有 **LLM** 方法多聚焦于一次性生成简化模型，缺乏对 **solver-level executability**、**动态约束注入** 和 **模型压缩** 的系统支持。

---

### **提出了什么新方法或新思路**
提出 **EvoOpt-LLM** —— 一个基于 **LLM** 的统一框架，覆盖工业优化建模的全生命周期，包含三大模块：

1. **Automated Modeling（自动化建模）**
   - 输入：自然语言描述
   - 输出：可执行的 **MILP** 模型代码（含变量、参数、约束、目标函数）
   - 方法：基于 **openPangu-Embedded-7B** 模型，通过 **LoRA** 进行参数高效微调。

2. **Dynamic Constraint Injection（动态约束注入）**
   - 在已有可执行 **MILP** 模型基础上，按需注入新的业务约束（如人力限制、分批交付等），**不改变原目标函数和结构**。
   - 将优化模型视为“可编辑对象”，实现增量更新。

3. **End-to-End Variable Pruning（端到端变量剪枝）**
   - 预测并固定为零的冗余决策变量，缩小模型规模，提升求解效率。
   - 将剪枝任务建模为**数据驱动的推理问题**，而非仅依赖代数规则。

---

### **相比现有方法的优势**
| 维度 | 传统方法 / 现有 LLM 方法 | **EvoOpt-LLM** |
|------|--------------------------|----------------|
| **建模方式** | 手动或一次性生成 | 支持全生命周期：构建 → 演化 → 压缩 |
| **动态适应性** | 不支持增量更新 | 支持 LP-level 结构化约束注入 |
| **数据效率** | 需大量标注数据 | 仅用 3,000 样本即可达到高性能，<1,500 样本即见显著提升 |
| **求解前优化** | 依赖 solver 内置 presolve | 引入 LLM 驱动的变量剪枝，捕捉隐含业务逻辑 |
| **领域适配** | 通用 LLM 缺乏 OR 结构理解 | 通过 LoRA 实现领域专业化，成本低 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **自动化建模模块**：
  - 自建数据集，共 **3,000** 个样本。
  - 类型分布：  
    - **MILP**: 33%  
    - **LP**: 22%  
    - **整数规划**: 14%  
    - 其他（调度、网络优化、随机优化等）：31%
  - 每个样本包含：自然语言描述 + 数学公式 + 可执行代码（如 Python + PuLP/Gurobi）

- **约束注入模块**：
  - 基于统一基准 **LP 文件**（供应链生产运输问题）生成。
  - 注入五类常见工业约束：
    1. Setup Time Constraint
    2. Human Resource Constraint
    3. Batch Size & Inventory Capacity
    4. Cross-Period Production
    5. Split Delivery
  - 自动生成输入输出对：`(原始 LP + 自然语言约束描述) → (增强后 LP)`

- **变量剪枝模块**：
  - 使用真实工业调度记录构建。
  - 两阶段构建：
    1. 需求驱动采样，将大规模问题降维为小规模但语义一致的实例。
    2. 利用异构图算法识别最优解中恒为零的变量，作为标签。
  - 最终数据集：**400** 个带剪枝标签的小规模 LP 实例。

---

### **实验设置和评估指标**

#### **模型基础与训练**
- **Base Model**: openPangu-Embedded-7B-V1.1（7B 参数）
- **微调方法**: **LoRA**（Parameter-Efficient Fine-Tuning）
- **硬件**: 华为 Ascend 910B2 NPU ×4，每卡 64GB HBM
- **软件栈**: CANN 8.1, PyTorch 2.5.1 + torch_npu, Transformers 4.53.2, vLLM-Ascend

#### **评估指标**

| 模块 | 指标 | 定义 |
|------|------|------|
| **自动化建模** | Generation Rate | 成功生成代码的比例 |
| | Executability Rate | 生成代码能被标准 solver 成功解析并运行的比例 |
| | Accuracy Rate | 生成模型求解结果与基准一致的比例 |
| **约束注入** | 功能正确性 | 是否正确定义新变量、添加有效约束、保留原目标 |
| | 结构一致性 | 输出是否符合 LP 格式规范，与原文件风格一致 |
| **变量剪枝** | F1 Score | 剪枝预测的精确率与召回率的调和平均 |
| | Precision / Recall | 分别衡量剪枝的安全性与完整性 |

---

### **基线方法对比**
论文未直接对比其他端到端 LLM 方法（因多数不支持三合一功能），而是通过消融分析验证各模块有效性：
- **Base Model（未微调）** vs **LoRA-Finetuned Model**
- 不同训练样本量下的性能曲线（验证数据效率）
- 与传统 presolve 技术的定性对比（强调其无法学习历史模式）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **自动化建模模块**
- **仅用 300 样本**：开始生成代码，但不可执行。
- **1,200–1,500 样本**：出现质变，Executability Rate 从 2.4% 跃升至 **38.2%**。
- **3,000 样本（完整数据）**：
  - **Generation Rate**: **91%**
  - **Executability Rate**: **65.9%**
  - **Accuracy Rate**: **26.37%**
- **结论**：强数据效率，**<1,500 样本即可触发关键性能突破**。

#### **约束注入模块**
- **Base Model 输出**（图3）：
  - 仅提供概念性描述，无时间索引、无辅助变量定义。
  - 混合自然语言与数学表达，**不可执行**。
- **LoRA 微调后输出**（图4）：
  - 正确引入 `operation_A_binary`, `available_human_t` 等变量。
  - 添加结构化线性约束，格式规范，**可直接集成进 LP 文件**。
  - 完全保留原目标函数与约束。

#### **变量剪枝模块**
- **训练样本仅 400 个**。
- 在 **350–400 行 LP 文件** 上表现最佳：
  - **F1 Score ≈ 0.56**
  - Precision 与 Recall 均处于合理水平。
- 性能随文件规模增大而下降：
  - >450 行：性能持续下滑
  - >1,500 行：F1 接近 0
- **原因**：受限于上下文长度与复杂交互建模能力。

---

### **与基线方法的对比结果**
- **Base Model 零效果**：未微调模型在所有任务上均失败，表明领域知识必须显式注入。
- **相比通用 LLM**（如 GPT-4）：
  - 无需提示工程，直接输出结构化 LP 代码。
  - 更稳定、更符合工业编码规范。
- **相比传统 presolve**：
  - 可识别由历史业务模式决定的“软冗余”变量，而不仅是代数冗余。

---

### **消融实验结果**
虽未明确列出“ablation study”章节，但从实验设计中可推断：
- **LoRA 微调是关键**：无此步骤则模型无任何可用能力。
- **数据多样性重要**：涵盖多种 OR 问题类型有助于泛化。
- **上下文长度限制显著影响剪枝性能**：大模型处理长 LP 文件仍具挑战。

---

## **4. 关键结论和发现**

### **主要发现**
1. **LLM 可有效支持工业优化建模全生命周期**，不仅限于初始生成。
2. **LoRA 微调 + 小样本（3k/400）即可实现高性能**，具备实际部署可行性。
3. **优化模型应被视为“可进化对象”**，支持 LP-level 增量编辑是提升敏捷性的关键。
4. **变量剪枝可数据驱动化**，LLM 能从历史数据中学习隐含的“零激活”模式，超越传统 presolve。

---

### **方法的局限性**
1. **上下文长度限制**：
   - 当前模型在处理 >1,500 行的大型 LP 文件时性能急剧下降。
2. **泛化能力有限**：
   - 剪枝模块在未见过的问题结构上可能失效。
3. **依赖高质量标注数据**：
   - 尽管数据效率高，但仍需一定量人工或半自动标注。
4. **未完全端到端验证**：
   - 生成模型的“语义准确性”仅部分验证，未覆盖所有场景。

---

### **未来工作方向**
1. **扩展上下文处理能力**：
   - 探索 **long-context modeling** 架构或 **hierarchical pruning** 策略。
2. **多阶段剪枝机制**：
   - 先粗粒度过滤，再细粒度判断，降低单次推理复杂度。
3. **引入强化学习反馈**：
   - 结合 solver 反馈进行 test-time adaptation（类似 OR-R1）。
4. **构建更大规模工业优化基准数据集**：
   - 覆盖更多行业、更大规模问题，推动社区发展。
5. **与 solver 深度集成**：
   - 将 EvoOpt-LLM 作为 presolve 插件嵌入 CPLEX/Gurobi 流程。

---

> **总结一句话**：  
> **EvoOpt-LLM** 提出了一种**数据高效、全周期、可演化的工业优化建模范式**，通过 **LLM + LoRA** 实现从自然语言到可执行、可维护、可加速的 **MILP** 模型的自动化闭环，为 OR 与 AI 的深度融合提供了实用路径。

</details>

---

### 13. [Neural Attention Search Linear: Towards Adaptive Token-Level Hybrid Attention Models](https://arxiv.org/abs/2602.03681)

**Authors**: Difan Deng, Andreas Bentzen Winje, Lukas Fehring, Marius Lindauer  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.03681v1  

#### Abstract
The quadratic computational complexity of softmax transformers has become a bottleneck in long-context scenarios. In contrast, linear attention model families provide a promising direction towards a more efficient sequential model. These linear attention models compress past KV values into a single ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Neural Attention Search Linear: Towards Adaptive Token-Level Hybrid Attention Models 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **softmax attention** 在长上下文场景中面临 **O(L²)** 的计算复杂度瓶颈，尤其是在训练和推理时对内存和算力要求极高。虽然 **linear attention** 模型（如 Mamba、DeltaNet）通过将注意力操作转换为线性递归形式，实现了 **O(L)** 复杂度，但其表达能力受限于固定大小的隐藏状态（hidden state），难以有效保留长期依赖信息。

现有混合架构（如 GDN Hybrid）尝试在不同层交替使用 softmax 和 linear attention，但这些结构是**静态固定的**，无法根据输入动态调整，导致效率与性能之间难以取得最优平衡。

### 提出的新方法与新思路
本文提出 **Neural Attention Search Linear (NAtS-L)**，一种**自适应的 token-level 混合注意力框架**，其核心思想是：

- **在同一层内，对不同的 token 动态选择使用 softmax attention 还是 linear attention**。
- 引入一个可学习的 **Attention Score Layer**，基于输入特征为每个 token chunk 预测最合适的 attention 操作类型。
- 通过梯度联合优化，使模型自动学习“哪些 token 需要被长期保留（用 softmax attention），哪些可以压缩处理（用 linear attention）”。

### 相比现有方法的优势
| 维度 | 传统方法（如 Transformer / Linear-only） | 混合层方法（如 GDN Hybrid） | **NAtS-L（本文）** |
|------|----------------------------------------|-----------------------------|--------------------|
| **灵活性** | 固定结构 | 层级固定切换 | ✅ **Token 级动态决策** |
| **效率** | 低（O(L²)） | 中等（部分 softmax） | ✅ **更高效（仅关键 token 用 softmax）** |
| **表达能力** | 高（全 KV 缓存） | 中等 | ✅ **保留关键信息 + 压缩冗余** |
| **硬件友好性** | 差（显存墙） | 较好 | ✅ **chunk-wise 并行 + 可控负载** |

> 💡 **核心创新**：首次实现 **token-level 自适应注意力机制搜索**，打破了“要么全局关注，要么局部建模”的二元对立。

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：
  - `Fineweb-Edu`：教育领域高质量文本，用于学术规模预训练。
    - 小模型：380M 参数，训练 15B tokens
    - 大模型：800M 参数，训练 50B tokens
- **评估任务**：
  - **常识推理**：LAMBADA, PIQA, HellaSwag, Winogrande, OpenBookQA, ARC-e/c
  - **检索任务（长上下文）**：SWDE, SQD, FDA, TQA, NQ, DROP（输入截断至 4096）
  - **外推能力测试**：PG19, CodeParrot, NarrativeQA（长度达 65k）
  - **标准长上下文基准**：RULER, LongBench

### 实验设置与评估指标
- **上下文长度**：统一训练长度为 4096
- **硬件平台**：NVIDIA H100 PCIe × 4/8 GPUs
- **评估指标**：
  - **准确率（acc）**：分类/问答任务
  - **困惑度（ppl / perplexity）**：语言建模质量
  - **平均得分（mean score）**：RULER 等综合指标
  - **延迟（latency）**：prefill 和 decoding 时间

### 基线方法对比
| 模型 | 类型 | 参数量（大） | 特点 |
|------|------|--------------|------|
| GDN | Linear-only | 793M | Gated DeltaNet，高效但表达受限 |
| Mamba2 | State Space Model | 801M | 当前主流高效架构 |
| Transformer | Softmax-only | 778M | 全注意力，高成本 |
| GDN Hybrid | Layer-wise Hybrid | 802M | 5 层 softmax + 17 层 GDN |
| **NAtS-L** | ✅ **Token-wise Adaptive Hybrid** | 794M | 本文方法，全 NAtS-L 层 |
| **NAtS-L Hybrid** | ✅ **Token-wise + Layer-wise** | 793M | 6 层 NAtS-L + 15 层 GDN |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（800M 模型）

#### 表 1：零样本推理与检索任务表现（部分）
| Model | Avg. Retrieval Acc ↑ | Best Task Perf |
|-------|------------------------|----------------|
| GDN | 31.22 | — |
| Mamba2 | 29.94 | — |
| Transformer | 33.66 | — |
| GDN Hybrid | 37.30 | — |
| **NAtS-L** | **39.75** | DROP 最高 |
| **NAtS-L Hybrid** | **45.07** | ✅ **全面领先** |

> 🔥 **NAtS-L Hybrid 在所有 6 项检索任务中平均高出第二名 7.8 个百分点**

#### 图 3：长序列外推困惑度（65k 输入）
- 在 **PG19** 和 **NarrativeQA** 上，当输入远超训练长度（4k → 65k）：
  - GDN / Mamba2 性能急剧下降
  - Transformer 困难维持
  - **NAtS-L 与 NAtS-L Hybrid 能稳定保持低 perplexity**
- 表明：**动态保留关键 token 的 softmax attention 对长程依赖至关重要**

#### 表 2：RULER 基准（不同上下文长度）
| Model | 4k | 8k | 16k |
|-------|----|----|-----|
| Transformer | 0.45 | 0.00 | 0.00 |
| GDN Hybrid | 0.47 | 0.02 | 0.00 |
| **NAtS-L Hybrid** | **0.49** | **0.32** | **0.21** |

> ✅ **NAtS-L Hybrid 是唯一在 16k 上仍保持显著性能的方法**

#### 图 4：推理延迟对比（最长序列）
- **Prefill 阶段**：NAtS-L Hybrid 比 GDN 慢 1.66×，但比 Transformer 快 **5.4×**
- **Decoding 阶段（128k）**：比 Transformer 快 **2.3×**

> ⚡ 在几乎不牺牲速度的前提下，获得接近全 attention 的性能

---

### 消融实验结果（Table 4）

| 变体 | Avg. Retrieval Acc ↓ | 分析 |
|------|------------------------|------|
| **NAtS-L Hybrid (Full)** | **33.49** | ✅ 最优配置 |
| w/o Attn Norm | 29.59 | 归一化对输出融合至关重要 |
| w/o Attn Weights | 32.34 | 可学习权重提升表达能力 |
| Weights From X | 27.64 | 来自 `q` 的映射更有效 |
| w/o LAttn Decay | 32.24 | 衰减机制帮助遗忘无关历史 |
| GDN Out only | 26.98 | 仅 linear 内部计算严重降质 |

> ✅ 所有设计组件均被验证有效，尤其是 **RMS Norm + q-based weighting + decay**

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Token-level 自适应注意力优于固定结构混合模型**  
   NAtS-L 能根据输入内容动态分配计算资源，在“需要记忆的地方”启用 softmax attention，其余使用 linear attention，实现**性能与效率的帕累托前沿突破**。

2. ✅ **并非越多 softmax attention 越好**  
   实验显示没有 head 完全使用 softmax attention，说明“全序列关注”可能造成资源浪费；而完全不用则丢失关键信息。**稀疏但精准的关注才是最优解**。

3. ✅ **NAtS-L 具备强外推能力**  
   在远超训练长度的任务上仍能保持良好性能，表明其能有效识别并保留长期相关 token。

4. ✅ **硬件友好且易于集成**  
   基于 chunk-wise 并行设计，兼容 FlashAttention 架构，无需额外辅助损失或负载均衡机制。

### 方法的局限性
- 当前搜索空间仅包含两种操作：**softmax attention 和 GDN**，尚未扩展到更多 linear attention family（如 Mamba、RetNet）。
- 未引入**正则化控制 softmax token 比例**，可能导致某些输入下计算分布不均。
- 所有操作共享 Q/K/V 投影，限制了操作间的独立优化空间。

### 未来工作方向
1. **扩大搜索空间**：纳入多种 linear attention 模型（如 Mamba, RWKV），构建更丰富的混合生态。
2. **引入效率约束**：通过 auxiliary loss 控制 softmax token 占比，实现可控的 **efficiency-performance trade-off**。
3. **探索 MoE-style attention routing**：让不同 token 走向不同专家路径，进一步提升表达多样性。
4. **应用于更大规模模型**：验证在十亿级以上模型中的可扩展性。

---

> 📌 **总结一句话**：  
> **NAtS-L 开创性地实现了 token-level 自适应注意力机制，在几乎不增加推理开销的前提下，显著提升了长上下文建模能力，为下一代高效大模型提供了新范式。**

</details>

---

### 14. [ContextEvolve: Multi-Agent Context Compression for Systems Code Optimization](https://arxiv.org/abs/2602.02597)

**Authors**: Hongyuan Su, Yu Zheng, Yong Li  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.02597v1  

#### Abstract
Large language models are transforming systems research by automating the discovery of performance-critical algorithms for computer systems. Despite plausible codes generated by LLMs, producing solutions that meet the stringent correctness and performance requirements of systems demands iterative op...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ContextEvolve: Multi-Agent Context Compression for Systems Code Optimization》总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现代 **Large Language Models (LLMs)** 在系统代码优化中展现出潜力，能够自动生成算法解决方案。然而，在 **API-only 访问**（即无法更新模型参数）的约束下，现有的训练-free 进化方法面临两大挑战：
- **低效的上下文利用**：原始历史记录直接拼接导致信息密度低，浪费 token 资源；
- **无导向的搜索过程**：缺乏从长期进化轨迹中提取明确优化方向的能力，容易陷入局部最优。

这些问题限制了在有限计算预算下的搜索效率和最终解的质量。

### 提出了什么新方法或新思路
作者提出 **ContextEvolve** —— 一种基于多智能体（multi-agent）框架的上下文压缩机制，通过将优化上下文分解为三个正交维度，并由专用 Agent 分别管理，实现高效、有指导性的代码演化。

#### 三大核心 Agent 及其功能：
| Agent | 功能 | 对应 RL 机制 |
|------|------|-------------|
| **Summarizer Agent** | 将代码差异抽象为自然语言摘要，保留关键语义状态（如继承特征与创新设计） | State Representation Learning |
| **Navigator Agent** | 分析历史轨迹中的性能变化（Δs），提炼出高价值的“文本梯度”作为优化方向 | Policy Gradient Estimation |
| **Sampler Agent** | 从种群中优先检索多样化且高信息量的 exemplars 作为少样本提示 | Prioritized Experience Replay |

该架构实现了与 **test-time Reinforcement Learning (RL)** 的**功能性同构（functional isomorphism）**，在纯文本空间中模拟了 RL 的高效搜索机制，而无需任何参数更新。

### 相比现有方法的优势
- ✅ **高搜索效率**：相比传统进化策略，收敛更快，突破局部最优能力更强；
- ✅ **低 Token 消耗**：通过语义压缩提升信息密度，减少冗余上下文传输；
- ✅ **参数盲操作兼容性强**：完全适用于仅提供 API 接口的商用 LLM；
- ✅ **可扩展性好**：模块化设计便于独立优化各组件。

---

## 2. 核心实验方法和设置

### 使用的数据集
在 **ADRS benchmark** 上进行评估，包含五个典型系统代码优化任务：
1. **Transaction Scheduling (TS)**：最小化事务执行时间（makespan），最大化正确率（Corr.）
2. **SQL Optimization (SQL)**：提高 KV 缓存命中率（Hit.），降低重排序延迟（Lat.）
3. **Load Balancing (LB)**：平衡 GPU 负载（Bal.），加快再分配速度（Spe.）
4. **Sparse Attention Kernel (SAK)**：优化稀疏注意力掩码，权衡密度（Dens.）与误差（Err.）
5. **Model Placement (MP)**：减少 KV 压力（Press.），提升 GPU 利用成功率（Succ.）

每个任务均采用加权综合得分（Score）作为主指标。

### 实验设置和评估指标
- **基础模型**：Qwen3（所有 LLM 组件统一使用）
- **最大迭代次数**：TS/SQL/SAK/MP 为 100 次；LB 为 300 次
- **评估方式**：自动化 evaluator 返回 scalar score
- **主要指标**：
  - 最终最佳 solution 的 **combined Score**
  - 达到最优解所需的 **token 消耗总量**
  - 收敛曲线（best-so-far performance over iterations）

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| Heuristics | 规则基线 | 生产环境常用手工规则 |
| Human-SOTA | 专家设计 | 领域专家手动调优方案 |
| LLM One-shot | 单轮生成 | 一次推理输出，无反馈循环 |
| GEPA | Prompt Evolution | 基于反思性突变与帕累托选择 |
| OpenEvolve | Evolutionary Coding | AlphaEvolve 开源实现，保留原始代码历史 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）
| 方法 | 平均 Score 提升（vs. 最佳 baseline） | Token 消耗下降 |
|------|-------------------------------|----------------|
| **ContextEvolve** | **+6.5%**（整体平均）<br>最高达 **+33.3%**（LB 任务） | **-29.0%**（平均） |

具体表现亮点：
- 在 **Load Balancing (LB)** 任务中：
  - Balance 提升 **+36.0%**
  - Speed 提升 **+44.4%**
  - 综合 Score 提升 **+33.3%**
- 在复杂任务上节省更多 token（如 LB 节省近 30%），轻量任务（TS/MP）节省约 5%

### 与基线方法的对比结果
- 所有静态方法（Heuristics, Human-SOTA, One-shot）均显著落后于进化类方法；
- GEPA 和 OpenEvolve 虽优于静态方法，但仍受限于低效上下文处理；
- **ContextEvolve 在所有五项任务中全面超越所有 baseline**，尤其在需要深度探索的任务（如 LB）上优势明显。

### 消融实验结果（Ablation Study）
移除任一 Agent 均导致性能下降，影响程度如下：
| 移除 Agent | 平均 Score 下降 | 影响分析 |
|----------|------------------|---------|
| **Summarizer Agent** | **-9.3%** | 语义丢失严重，引发“遗忘效应”，阻碍其他 Agent 工作 |
| **Sampler Agent** | **-6.0%** | 仅依赖高分样本，错失潜在突破灵感 |
| **Navigator Agent** | **-2.9%** | 搜索方向模糊，收敛变慢 |

> 🔍 **关键发现**：Summarizer 是最关键组件，其缺失使性能退化至接近 LLM One-shot 水平。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **上下文压缩是训练-free 优化的关键瓶颈**  
   单纯堆叠原始历史会迅速耗尽上下文窗口，必须进行**语义层面的主动压缩**。

2. **多 Agent 分解能有效建模 RL 机制**  
   通过将优化上下文解耦为 **state、direction、experience** 三个维度，可在文本空间中重建 RL 的高效搜索范式。

3. **高质量信息 ≠ 高得分个体**  
   成功的突破往往源于失败但具有创新逻辑的尝试（如 LB 中的“broken binary search”启发了“Largest Remainder”策略）。

4. **抽象层次决定搜索效率**  
   - 过度具体的指导（如强制实现细节）会抑制多样性；
   - 合理的模糊性反而有助于探索更广的 solution space。

### 方法的局限性
- 当前工作聚焦于**功能隔离的算法模块**，尚未扩展到大规模跨模块协同优化；
- 多 Agent 架构引入额外 API 调用开销（尽管总 token 更少）；
- LLM 固有的输出不确定性可能导致高方差结果，需进一步稳定机制。

### 未来工作方向
- 扩展至 **large-scale codebase** 的联合优化；
- 引入 **variance reduction 机制** 提升结果一致性；
- 探索促进 **diverse algorithmic paradigms 发现** 的评估机制；
- 将框架应用于更多领域（如编译器优化、操作系统调度等）。

---

> 📦 **开源信息**：代码已发布于 [https://anonymous.4open.science/r/ContextEvolve-ACC](https://anonymous.4open.science/r/ContextEvolve-ACC)

</details>

---

### 15. [Lookahead Path Likelihood Optimization for Diffusion LLMs](https://arxiv.org/abs/2602.03496)

**Authors**: Xuejie Liu, Yap Vit Chun, Yitao Liang, Anji Liu  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.03496v1  

#### Abstract
Diffusion Large Language Models (dLLMs) support arbitrary-order generation, yet their inference performance critically depends on the unmasking order. Existing strategies rely on heuristics that greedily optimize local confidence, offering limited guidance for identifying unmasking paths that are gl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Lookahead Path Likelihood Optimization for Diffusion LLMs**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
Diffusion Large Language Models (dLLMs) 虽然支持任意顺序生成（arbitrary-order generation），其推理性能却高度依赖于 **unmasking order**（即 token 的解码顺序）。现有的 unmasking 策略多基于启发式规则（如选择置信度最高或熵最低的 token），这些方法仅优化局部确定性，缺乏对全局一致性和最终生成质量的考量，容易陷入“短视”路径，导致次优输出。

本文旨在解决以下核心问题：  
> 如何在推理时动态识别能够最大化生成质量的 **unmasking 路径**？

---

### **提出了什么新方法或新思路**

作者提出了一套以 **Path Log-Likelihood (Path LL)** 为核心目标的推理优化框架，主要包括两个关键组件：

#### ✅ **Path LL（路径似然）作为生成质量代理**
- 定义了一个新的轨迹相关目标函数：**Path LL**，即在特定 unmasking 轨迹 $ \mathcal{T} = (Q_T, ..., Q_1) $ 下完整序列的联合对数似然：
  $$
  \log p_d(\mathbf{x}; \theta, \mathcal{T}) = \sum_{t=1}^T \log p_d(\mathbf{c}_{Q_t} | \mathbf{o}_t; \theta)
  $$
- 实验证明，**Path LL 与下游任务准确率有强正相关性**，显著优于 ELBO、边际熵等传统指标，是更可靠的生成质量代理。

#### ✅ **POKE：前向估计未来路径似然的价值估计器**
- 提出 **POKE (Path-Optimistic K-step likelihood Estimator)**，一种高效的 lookahead 价值估计器，用于预测部分解码轨迹的期望未来 Path LL。
- 利用随机 rollout 模拟未来完成路径，并通过 entropy correction 校正并行采样带来的独立性偏差，使得估计值在高并行度下仍保持稳定。

#### ✅ **POKE-SMC：基于 Sequential Monte Carlo 的搜索框架**
- 将 POKE 集成进 **Sequential Monte Carlo (SMC)** 框架，构建 **POKE-SMC**，实现动态路径搜索：
  - 维护多个粒子（候选轨迹）
  - 在周期性重采样点，使用 POKE 提供的全局价值信号进行加权和重采样
  - 动态聚焦高潜力路径，避免陷入局部最优

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **目标设计** | 使用 Path LL 替代局部启发式，具备更强的全局一致性指导能力 |
| **效率与准确性平衡** | POKE 不需额外训练，轻量高效，适合推理时使用 |
| **适应性强** | 可适配不同任务和上下文，无需任务特定调参 |
| **性能提升显著** | 在相近计算开销下，显著超越强基线，推动 accuracy-compute Pareto frontier |

---

## 2. **核心实验方法和设置**

### **使用的数据集**
实验覆盖 **6 个推理密集型任务**，包括：
- **GSM8K**：小学数学应用题
- **MATH500**：高中及以上难度数学题
- **Countdown**：数字组合游戏（需精确逻辑推导）
- 其他未明确命名的任务（统称 reasoning tasks）

模型基础为 **LLaDA 系列 dLLMs**（如 LLaDA-8B-Instruct），代表当前先进的 diffusion-based LLM。

---

### **实验设置和评估指标**

#### 🔧 **推理设置**
- 时间步离散化为 $ T = 256 $
- 每步 unmask 多个 token（block size = 32 或可变）
- 使用 zero-shot 设置进行生成
- POKE 中 lookahead horizon $ K \in \{4,8,16,64\} $，控制前瞻深度

#### 📊 **评估指标**
- **Accuracy (%)**：任务正确率（如数学题解答是否正确）
- **Latency (s)**：端到端推理延迟
- **Path LL 估计质量**：与真实最终 Path LL 的相关性
- **Accuracy-Compute Pareto Frontier**：精度与计算成本的权衡曲线

---

### **基线方法对比**
与以下主流方法比较：
| 基线 | 描述 |
|------|------|
| **PC-Sampler** | 基于位置感知校准的解码偏置修正方法 |
| **Majority Voting** | 多次采样后投票集成 |
| **E-SMC** | 基于熵的 SMC 方法，使用一步熵估计 |
| **Random / Confidence / Entropy-based ordering** | 经典启发式 unmasking 策略 |

此外还对比了近期学习式规划方法（如 LookUM）和 RL-based 方法。

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**

| 方法 | 平均 Accuracy Gain | 推理开销（相对） |
|------|-------------------|----------------|
| **POKE-SMC** | **+2% ~ +5%** | ≈ 强基线（如 E-SMC） |
| 其他 decoding-time scaling 方法 | +1% ~ +2% | 类似或更高 |

> 在 LLaDA 模型上，POKE-SMC 实现 **平均 3%-5% 的绝对准确率提升**，远超已有方法。

#### 示例结果（Table 10, Countdown 数据集）：
| Method       | Latency (s)         | Accuracy (%)        |
|--------------|---------------------|---------------------|
| PC-Sampler   | 7.9                 | 36.1                |
| Majority Voting | 9.5/18.0/32.6     | 34.0/36.0/36.9      |
| E-SMC        | 9.5/18.0/32.6       | 35.4/38.3/39.8      |
| **POKE-SMC** | **8.9/22.1/34.7**   | **36.4/42.3/40.8**  |

✅ 在相似延迟下，POKE-SMC 显著领先，尤其在中等预算（~22s）达到 **42.3% 准确率**，优于所有基线。

---

### **消融实验结果**

#### ✅ **POKE vs. Product LL（朴素乘积似然）**
- 在 Figure 2 和附录 Figures 5–7 中显示：
  - 随着并行度增加（|A|↑），product LL 对未来 Path LL 的估计严重低估
  - **POKE 估计值始终接近真实最终 Path LL**，误差小且稳定
- 特别是在 $ K=16 $（即每步 unmask 16 tokens）时表现最佳，说明适度前瞻最有效

#### ✅ **不同 unmasking block size 下的表现**
- 即使在高吞吐场景（每步 unmask 2 tokens），POKE-SMC 仍保持 **+4.9% ~ +5.4%** 的增益，表明其鲁棒性强

#### ✅ **温度与重采样间隔敏感性分析（Appendix C）**
- 结果显示 POKE-SMC 在较宽参数范围内表现稳健，无需精细调参

---

## 4. **关键结论和发现**

### **主要发现**

1. ✅ **Path LL 是比传统指标更优的质量代理**
   - 与任务准确率高度相关，适合作为 inference-time optimization 的目标函数。

2. ✅ **局部最优 ≠ 全局最优**
   - 贪心策略（如选最自信 token）可能误导整体路径；需要 lookahead 机制来评估长期影响。

3. ✅ **POKE 实现高效且准确的未来价值估计**
   - 无需训练，利用随机 rollout + entropy correction 实现高质量估计，在高并行下依然可靠。

4. ✅ **POKE-SMC 显著提升 accuracy-compute 权衡边界**
   - 在相同计算预算下，持续优于现有 decoding-time scaling 方法，成为新的 SOTA 推理策略。

---

### **方法的局限性**

1. ❗ **当前验证限于 8B 规模模型**
   - 所有实验基于 LLaDA-8B 等 ~8B 参数模型，尚未在更大规模（如 LLaDA2.0）上验证可扩展性。

2. ❗ **SMC 的内存开销随粒子数线性增长**
   - 虽然周期性重采样缓解了问题，但在极端低延迟场景下仍可能存在瓶颈。

3. ❗ **固定周期重采样非自适应**
   - 当前采用固定间隔 △ 进行重采样，未能根据路径不确定性动态调整，存在进一步优化空间。

---

### **未来工作方向**

1. 🔮 **扩展至更大规模 dLLMs**
   - 验证 POKE-SMC 在百亿级以上模型上的有效性与可扩展性。

2. 🔁 **引入 adaptive rollback 机制**
   - 利用 Path LL 的全局可比性，当检测到路径进入低概率区域时主动 remask 并回退，实现非单调解码。

3. 🧠 **结合强化学习进行策略学习**
   - 将 POKE 作为奖励信号，训练可学习的 unmasking policy，进一步提升搜索效率。

4. ⚙️ **硬件感知的并行化优化**
   - 结合 GPU 并行特性，设计更适合大规模并行 rollout 的 POKE 实现方式。

---

> **总结一句话**：  
> 本论文首次系统地将 **Path LL** 确立为 dLLMs 推理优化的核心目标，提出 **POKE + POKE-SMC** 框架，实现了高效、可扩展、高性能的路径搜索，在不增加显著计算成本的前提下，显著提升了生成准确率，推动了 diffusion LLM 的实用化进程。

</details>

---

### 16. [Reasoning and Tool-use Compete in Agentic RL:From Quantifying Interference to Disentangled Tuning](https://arxiv.org/abs/2602.00994)

**Authors**: Yu Li, Mingyang Yi, Xiuyu Li, Ju Fan, Fuxin Jiang, Binbin Chen, Peng Li, Jie Song, Tieying Zhang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00994v1  

#### Abstract
Agentic Reinforcement Learning (ARL) focuses on training large language models (LLMs) to interleave reasoning with external tool execution to solve complex tasks. Most existing ARL methods train a single shared model parameters to support both reasoning and tool use behaviors, implicitly assuming th...

---

### 17. [Token Sparse Attention: Efficient Long-Context Inference with Interleaved Token Selection](https://arxiv.org/abs/2602.03216)

**Authors**: Dongwon Jo, Beomseok Kang, Jiwon Song, Jae-Joon Kim  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03216v1  

#### Abstract
The quadratic complexity of attention remains the central bottleneck in long-context inference for large language models. Prior acceleration methods either sparsify the attention map with structured patterns or permanently evict tokens at specific layers, which can retain irrelevant tokens or rely o...

---

### 18. [POP: Prefill-Only Pruning for Efficient Large Model Inference](https://arxiv.org/abs/2602.03295)

**Authors**: Junhui He, Zhihui Fu, Jun Wang, Qingan Li  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03295v1  

#### Abstract
Large Language Models (LLMs) and Vision-Language Models (VLMs) have demonstrated remarkable capabilities. However, their deployment is hindered by significant computational costs. Existing structured pruning methods, while hardware-efficient, often suffer from significant accuracy degradation. In th...

---

### 19. [Efficient Algorithms for Partial Constraint Satisfaction Problems over Control-flow Graphs](https://arxiv.org/abs/2602.03588)

**Authors**: Xuran Cai, Amir Goharshady  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03588v1  

#### Abstract
In this work, we focus on the Partial Constraint Satisfaction Problem (PCSP) over control-flow graphs (CFGs) of programs. PCSP serves as a generalization of the well-known Constraint Satisfaction Problem (CSP). In the CSP framework, we define a set of variables, a set of constraints, and a finite do...

---

### 20. [Performance of Small Language Model Pretraining on FABRIC: An Empirical Study](https://arxiv.org/abs/2602.02632)

**Authors**: Praveen Rao  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.02632v1  

#### Abstract
Large language models (LLMs) require enormous computing power to pretrain on massive datasets. When limited datasets are available, smaller-sized LLMs are better choice to pretrain (on user-specified datasets) by following the scaling laws of LLMs. Using pretrained models, vector embeddings can be g...

---

### 21. [Spectral Evolution Search: Efficient Inference-Time Scaling for Reward-Aligned Image Generation](https://arxiv.org/abs/2602.03208)

**Authors**: Jinyan Ye, Zhongjie Duan, Zhiwen Li, Cen Chen, Daoyuan Chen, Yaliang Li, Yingda Chen  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03208v1  

#### Abstract
Inference-time scaling offers a versatile paradigm for aligning visual generative models with downstream objectives without parameter updates. However, existing approaches that optimize the high-dimensional initial noise suffer from severe inefficiency, as many search directions exert negligible inf...

---

### 22. [Sparse Training of Neural Networks based on Multilevel Mirror Descent](https://arxiv.org/abs/2602.03535)

**Authors**: Yannick Lunk, Sebastian J. Scott, Leon Bungert  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03535v1  

#### Abstract
We introduce a dynamic sparse training algorithm based on linearized Bregman iterations / mirror descent that exploits the naturally incurred sparsity by alternating between periods of static and dynamic sparsity pattern updates. The key idea is to combine sparsity-inducing Bregman iterations with a...

---

### 23. [MatGPTQ: Accurate and Efficient Post-Training Matryoshka Quantization](https://arxiv.org/abs/2602.03537)

**Authors**: Maximilian Kleinegger, Elvir Crn\v{c}evi\'c, Dan Alistarh  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03537v1  

#### Abstract
Matryoshka Quantization (MatQuant) is a recent quantization approach showing that a single integer-quantized model can be served across multiple precisions, by slicing the most significant bits (MSB) at inference time. This enables a single checkpoint to cover a wide range of memory and latency budg...

---

### 24. [Probing RLVR training instability through the lens of objective-level hacking](https://arxiv.org/abs/2602.01103)

**Authors**: Yiming Dong, Kun Fu, Haoyu Li, Xinyuan Zhu, Yurou Liu, Lijing Shao, Jieping Ye, Zheng Wang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01103v1  

#### Abstract
Prolonged reinforcement learning with verifiable rewards (RLVR) has been shown to drive continuous improvements in the reasoning capabilities of large language models, but the training is often prone to instabilities, especially in Mixture-of-Experts (MoE) architectures. Training instability severel...

---

### 25. [A State-Transition Framework for Efficient LLM Reasoning](https://arxiv.org/abs/2602.01198)

**Authors**: Liang Zhang, Yu Zhao, Longyue Wang, Tianqi Shi, Weihua Luo, Kaifu Zhang, Jinsong Su  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01198v1  

#### Abstract
While Long Chain-of-Thought (CoT) reasoning significantly improves Large Language Models (LLMs) performance on complex reasoning tasks, the substantial computational and memory costs of generating long CoT sequences limit their efficiency and practicality. Existing studies usually enhance the reason...

---

### 26. [WideSeek: Advancing Wide Research via Multi-Agent Scaling](https://arxiv.org/abs/2602.02636)

**Authors**: Ziyang Huang, Haolin Ren, Xiaowei Yuan, Jiawei Wang, Zhongtao Jiang, Kun Xu, Shizhu He, Jun Zhao, Kang Liu  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.02636v1  

#### Abstract
Search intelligence is evolving from Deep Research to Wide Research, a paradigm essential for retrieving and synthesizing comprehensive information under complex constraints in parallel. However, progress in this field is impeded by the lack of dedicated benchmarks and optimization methodologies for...

---

### 27. [FASA: Frequency-aware Sparse Attention](https://arxiv.org/abs/2602.03152)

**Authors**: Yifei Wang, Yueqi Wang, Zhenrui Yue, Huimin Zeng, Yong Wang, Ismini Lourentzou, Zhengzhong Tu, Xiangxiang Chu, Julian McAuley  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.03152v1  

#### Abstract
The deployment of Large Language Models (LLMs) faces a critical bottleneck when handling lengthy inputs: the prohibitive memory footprint of the Key Value (KV) cache. To address this bottleneck, the token pruning paradigm leverages attention sparsity to selectively retain a small, critical subset of...

---

### 28. [CoBA-RL: Capability-Oriented Budget Allocation for Reinforcement Learning in LLMs](https://arxiv.org/abs/2602.03048)

**Authors**: Zhiyuan Yao, Yi-Kai Zhang, Yuxin Chen, Yueqing Sun, Zishan Xu, Yu Yang, Tianhao Hu, Qi Gu, Hui Su, Xunliang Cai  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.03048v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a key approach for enhancing LLM reasoning.However, standard frameworks like Group Relative Policy Optimization (GRPO) typically employ a uniform rollout budget, leading to resource inefficiency. Moreover, existing adaptive methods...

---

### 29. [Quantization-Aware Regularizers for Deep Neural Networks Compression](https://arxiv.org/abs/2602.03614)

**Authors**: Dario Malchiodi, Mattia Ferraretto, Marco Frasca  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.03614v1  

#### Abstract
Deep Neural Networks reached state-of-the-art performance across numerous domains, but this progress has come at the cost of increasingly large and over-parameterized models, posing serious challenges for deployment on resource-constrained devices. As a result, model compression has become essential...

---

### 30. [Scalable and Secure AI Inference in Healthcare: A Comparative Benchmarking of FastAPI and Triton Inference Server on Kubernetes](https://arxiv.org/abs/2602.00053)

**Authors**: Ratul Ali  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.00053v1  

#### Abstract
Efficient and scalable deployment of machine learning (ML) models is a prerequisite for modern production environments, particularly within regulated domains such as healthcare and pharmaceuticals. In these settings, systems must balance competing requirements, including minimizing inference latency...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
