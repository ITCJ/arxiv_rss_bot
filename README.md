# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-02 06:47:28 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [TriSpec: Ternary Speculative Decoding via Lightweight Proxy Verification](https://arxiv.org/abs/2601.23180)

**Authors**: Haoyun Jiang, Junqi He, Feng Hong, Xinlong Yang, Jianwei Zhang, Zheng Li, Zhengyang Zhuge, Zhiyong Chen, Bo Han, Junyang Lin, Jiangchao Yao  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.23180v1  

#### Abstract
Inference efficiency in Large Language Models (LLMs) is fundamentally limited by their serial, autoregressive generation, especially as reasoning becomes a key capability and response sequences grow longer. Speculative decoding (SD) offers a powerful solution, providing significant speed-ups through...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TriSpec: Ternary Speculative Decoding via Lightweight Proxy Verification

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在推理时受限于其**串行、自回归生成机制**，导致延迟较高，尤其是在需要复杂推理、输出序列较长的任务中。尽管现有的 **Speculative Decoding (SD)** 技术通过“起草-验证”范式显著提升了推理速度，但当前研究主要集中在优化 **drafting 效率** 和 **acceptance rate** 上，而对 **verification 阶段的计算开销** 关注不足。

本文指出，随着 drafting 成本被单层 drafter（如 EAGLE）大幅降低，**每轮验证时间 $t_v$ 已成为新的性能瓶颈**。因此，如何减少昂贵的 target model 在验证阶段的调用频率，成为一个关键且未被充分探索的方向。

### 提出了什么新方法或新思路
作者提出了 **TriSpec** ——一种全新的**三元 speculative decoding 框架**，引入了一个轻量级的 **proxy verifier** 来分担验证任务。该框架协调三个角色：
- **Drafter**：单层结构，负责高速生成候选 token。
- **Proxy Verifier**：来自同一家族的小规模模型（如 Qwen3-1.7B），用于快速预验证并局部纠正草案。
- **Target Model**：大规模目标模型（如 Qwen3-32B），仅在 proxy 判定为“不可信”时进行权威验证。

核心机制是基于 **margin-based routing rule**：利用 proxy 模型 top-1 与 top-2 输出概率之间的差距作为置信度指标。若 margin 足够大，则认为 proxy 的判断可信，可跳过 target 验证；否则将控制权交还给 target model。

### 相比现有方法的优势
- **首次系统性地优化 verification 成本**，而非仅仅提升 drafting 或 acceptance length。
- 引入 **same-family smaller model 作为 proxy verifier**，充分利用其与 target 的强对齐性（token-level alignment 达 82%），实现高效且可靠的轻量验证。
- 采用动态路由策略，在保持精度的同时**显著减少 target model 的调用次数**。
- 可无缝集成到现有先进 SD 方法（如 EAGLE-3）之上，进一步提升加速效果。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验覆盖多个领域和难度级别的基准测试集：
- **数学推理**：GSM8K、MATH500、AIME24/AIME25
- **代码生成**：HumanEval、MBPP
- **多语言与综合能力**：Gaokao2023-en（GK23-en）、Polymath
- **问答与检索增强**：HotpotQA、SpecBench、GPQA-Diamond

### 实验设置和评估指标
- **模型家族**：
  - Qwen3 系列：target (32B), proxy (1.7B), drafter (EAGLE-3/HASS)
  - DeepSeek-R1-Distill-Qwen (DSQ)：target (32B), proxy (1.5B)
  - DeepSeek-R1-Distill-LLaMA (DSL)：target (70B), proxy (8B)
- **训练细节**：
  - Drafter 使用 HASS/EAGLE-3 架构，并联合训练或 adapter 微调以适配 proxy 特征。
  - Margin threshold $\lambda = 0.5$。
- **评估指标**：
  - **Accuracy**：pass@1 准确率或 LLM-judge 分数。
  - **效率**：吞吐量（Tokens/s, TPS）、相对 speedup（vs. target-only）。
  - **验证开销**：target-invocation ratio ($r_t$)，即每个生成 token 中调用 target 的比例。

### 基线方法对比
- **Single Model (Target/Proxy)**：单独使用 target 或 proxy 推理。
- **标准 SD 方法**：HASS、EAGLE3。
- **松弛验证方法**：SpecCascade 及其变体（Chow rule, OPT rule, Token rule）。
- 所有 drafters 均在统一数据上重新训练以确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 结果 |
|------|------|
| 最高加速比 | **最高达 35% 的额外加速**（相比标准 SD） |
| Target 调用减少 | **最多减少超过 50% 的 target model invocation** |
| 平均准确率损失 | **小于 1%**（temperature=0）或 **小于 2%**（temperature=1） |

### 与基线方法的对比结果
#### 表格摘要（以 Qwen3 + EAGLE3 为例，Temperature=0）
| 方法 | Avg Acc. | Speedup |
|------|----------|---------|
| EAGLE3 | 84.4% | 2.85× |
| **EAGLE3 + TriSpec** | **84.2%** | **3.55×** |

- 在所有 benchmark 上，TriSpec 在几乎不损失准确率的前提下，带来 **15%-35% 的额外加速增益**。
- 相比 SpecCascade 类方法，TriSpec 在 accuracy-speedup trade-off 上表现更优，避免了因过度放松验证而导致的严重精度下降。

#### 验证效率分析（Table 3）
在 MATH500 上，EAGLE3+TriSpec 相比 EAGLE3：
- $r_t$ 从 21.38% → **9.95%**
- 每轮验证时间 $t_v$ 从 0.094s → **0.071s**
- 端到端延迟 $L$ 显著降低，吞吐量从 38.96 → **49.74 Tok/s**

### 消融实验结果
#### （1）Draft Training 策略
| 训练方式 | Speedup (EAGLE3+TriSpec) |
|--------|--------------------------|
| Joint from-scratch | 3.49× |
| Adapter-only finetune | 3.44× |

- 两种策略均能有效加速，joint training 性能略优，但 adapter finetuning 更节省资源。

#### （2）Token Pruning 机制
- 启用 token pruning 后，平均 acceptance length 提升，说明 proxy 的部分验证结果可靠，允许 target 安全跳过冗余计算。

#### （3）Routing Criteria 对比（Figure 5b）
- 比较了 top-1 概率、entropy、margin、R2R-router 等策略。
- **margin-based routing** 在 accuracy-speedup 曲线上表现最优，成为默认选择。

#### （4）Draft Length 影响（Figure 5c）
- TriSpec 的最优 draft length 与标准 SD 不同，表明其路径决策受 proxy 行为影响。
- 在不同长度下 TriSpec 始终优于 baseline。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **Verification cost 是 SD 加速的新突破口**：当 drafting 成本已极低时，优化 $t_v$ 成为继续提升效率的关键。
2. **Same-family smaller models 是理想的 proxy verifier**：
   - 与 target 模型具有高度 token-level alignment（高达 82% exact match）。
   - 输出质量可信，轻微偏差不影响最终答案正确性。
   - 其预测置信度（top-1/top-2 margin）可有效区分“可信”与“需升级”的验证场景。
3. **TriSpec 实现高效三元协同**：
   - 通过 margin-based routing 动态分配验证任务。
   - 大幅减少 target model 调用，同时维持接近原生 target 的输出质量。
   - 可与 EAGLE-3 等先进方法结合，实现叠加加速。

### 方法的局限性
- **依赖同一家族存在 proxy 模型**：若无合适规模的 smaller model，难以部署。
- **margin threshold $\lambda$ 需要调参**：虽然 $\lambda=0.5$ 表现良好，但在不同任务或模型上可能需要调整。
- **对极端错误容忍有限**：若 proxy 在关键决策点出现低置信误判，仍可能导致错误传播（尽管实验显示整体鲁棒性强）。

### 未来工作方向
- 探索更通用的 proxy selection 机制，不限于同一家族模型。
- 设计自适应 margin threshold，根据上下文动态调整信任阈值。
- 将 TriSpec 思想扩展至其他生成任务（如图像、音频）中的 speculative inference。
- 结合 KV cache reuse、layer-skipping 等技术，构建端到端更高效的推理栈。

--- 

> ✅ **总结一句话**：  
> TriSpec 通过引入一个轻量级的 **same-family proxy verifier** 和基于 **margin 的动态路由机制**，首次系统性地优化了 speculative decoding 中的 **verification 开销**，在几乎不损失准确率的前提下实现了高达 **35% 的额外加速**，并减少了 **超 50% 的 target model 调用**，为 LLM 高效推理提供了新范式。

</details>

---

### 2. [SPLA: Block Sparse Plus Linear Attention for Long Context Modeling](https://arxiv.org/abs/2601.22379)

**Authors**: Bailin Wang, Dan Friedman, Tao Lei, Chong Wang  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.22379v1  

#### Abstract
Block-wise sparse attention offers significant efficiency gains for long-context modeling, yet existing methods often suffer from low selection fidelity and cumulative contextual loss by completely discarding unselected blocks. To address these limitations, we introduce Sparse Plus Linear Attention ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SPLA: Block Sparse Plus Linear Attention for Long Context Modeling

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的 **block-wise sparse attention** 方法在长上下文建模中面临两个核心挑战：
1. **低选择保真度（Low Selection Fidelity）**：基于压缩表示的块选择机制（如均值池化、MLP）难以准确识别真正相关的 KV blocks，导致关键信息被遗漏。
2. **累积上下文损失（Cumulative Contextual Loss）**：通过完全丢弃未选中的“长尾”blocks，随着序列增长，这些被忽略的概率质量逐渐累积，造成与 dense attention 的输出严重偏离，影响生成质量。

### 提出了什么新方法或新思路
作者提出 **SPLA (Sparse Plus Linear Attention)**，一种结合精确稀疏注意力与近似线性注意力的新框架，其核心思想是将上下文严格划分为两部分进行处理：

- **Exact Component（精确部分）**：使用 principled 的选择机制选出最重要的 blocks，并对它们执行标准的 exact attention。
- **Approximate Component（近似部分）**：将未选中的“长尾”blocks 通过 **Residual Linear Attention (RLA)** 压缩为一个紧凑的递归状态，而非直接丢弃。

#### 创新点详解：
1. **基于二阶泰勒展开的块选择机制（Principled Block Selection）**
   - 将 block 的重要性定义为其内部所有 token 注意力得分的积分。
   - 推导出一个基于 **block-level mean 和 covariance 统计量** 的 selection metric，来源于对原始 attention 目标的二阶泰勒近似。
   - 相比仅用均值（first-order），该方法能更准确地估计 block 贡献，提升召回率，且无需额外训练。

2. **残差线性注意力（Residual Linear Attention, RLA）**
   - 引入线性 attention 来高效压缩未选 blocks 的信息。
   - 关键设计：采用 **减法形式实现 RLA**：
     $$
     o_{\text{res}} = o_{\text{global}} - o_{\text{selected}}
     $$
     其中 $ o_{\text{global}} $ 是全局线性 attention 输出，$ o_{\text{selected}} $ 是已加载 blocks 的局部线性 attention 输出。
   - 这样可以在不显式访问未选 blocks 的前提下完成计算，避免了额外的 I/O 开销，保持了 sparse decoding 的效率优势。

3. **统一融合架构**
   - 最终输出由 exact sparse attention 和 RLA 残差项加权组合：
     $$
     o = o_{\text{sparse}} + \text{RMSNorm}(o_{\text{res}})
     $$
   - 仅引入少量可学习参数（RMSNorm 的 scale 参数），便于从预训练 dense 模型迁移适配。

### 相比现有方法的优势
| 方法 | 是否保留长尾 | 是否有冗余计算 | 选择机制 |
|------|---------------|----------------|-----------|
| **InfLLM-v2 / Truncation** | ❌ 完全丢弃 | ✅ 无 | 启发式（如均值） |
| **NSA / Overlapping** | ✅ 压缩所有块 | ❌ 双重计数（selected blocks 被重复处理） | 启发式或学习 |
| **SPLA (Ours)** | ✅ 压缩未选块 | ✅ 无（严格划分） | **Principled（二阶泰勒）** |

- **理论更优**：严格划分 + 无损压缩 → 更接近 dense attention 输出。
- **实践更强**：在长序列上性能衰减显著缓解，尤其在 >64k 长度时反超 dense baseline。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验围绕 **Continual Pretraining (CPT)** 场景展开，包含以下三类任务：

1. **General Knowledge**
   - 数据：内部高质量混合数据（数学、代码、通用知识）
   - 上下文长度：32k
   - 测试集：ARC-C/E, HellaSwag, LAMBADA, PIQA, SciQ, WinoGrande, TriviaQA, WebQS, MMLU, GSM8K

2. **Long-Context**
   - 数据：长文档 + 合成 QA 对
   - 上下文长度：128k（训练），测试至 256k
   - 测试集：**RULER benchmark**

3. **Reasoning**
   - 数据：OpenReasoning Nemotron + OpenScienceReasoning
   - 上下文长度：32k
   - 测试集：AIME 2024/2025, HMMT 2025, LiveCodeBench, HumanEval, GPQA, MMLU Pro

### 实验设置
- **模型规模**：14B 参数（详见 Table 1）
- **硬件平台**：Cloud TPU v6e（1024 chips）
- **实现框架**：JAX
- **并行策略**：FSDP + Sequence Parallelism
- **优化器**：AdamW
- **训练流程**：
  - 先在 10T tokens 上预训练 dense 模型
  - 再在特定领域数据上进行 CPT 并集成 sparsification
- **稀疏配置**：
  - Block size $ B = 128 $
  - Stride $ s = 16 $, Window $ C = 32 $
  - Top-$k = 32$ blocks selected
  - 强制包含 initial block（attention sink）和最近 4 个 blocks（local window）

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **DENSE** | Dense Attention | 全量 KV cache 加载，性能上限 |
| **NSA (Yuan et al., 2025)** | Sparse + Overlapping | 多分支结构，存在双重计数问题 |
| **InfLLM-v2 (Zhao et al., 2025)** | Sparse + Truncation | 仅保留 top-k，其余置零 |
| **SPA (Ablation)** | SPLA w/o RLA | 使用相同 selection 但无 RLA 模块 |

> 所有 baseline 在统一代码库中实现，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表1：通用知识任务平均表现（来自 Table 2）
| Method | General Knowledge Avg | MMLU (5-shot) | GSM8K (8-shot) |
|--------|------------------------|----------------|------------------|
| DENSE | 67.5 | 77.3 | 90.2 |
| NSA | 66.5 | 77.4 | 90.5 |
| INF-V2 | 67.3 | 77.8 | 90.0 |
| SPA | 67.3 | 78.1 | 90.2 |
| **SPLA** | **67.7** | **78.6** | **90.5** |

✅ SPLA 在通用知识和推理任务上全面优于 dense 和其他 sparse 方法。

---

#### 表2：长上下文任务表现（RULER, Table 3）
| Model \ Length | 4k | 8k | 16k | 32k | 64k | 128k | **256k** |
|----------------|----|-----|-----|-----|-----|-------|---------|
| DENSE | 95.8 | 94.9 | 93.6 | 91.4 | 87.1 | 83.2 | **69.3** |
| NSA | 92.3 | 91.4 | 90.7 | 84.6 | 83.2 | 51.3 | 32.5 |
| INF-V2 | 95.9 | 94.1 | 92.1 | 89.3 | 86.7 | 61.6 | 42.6 |
| SPA | 95.9 | 94.2 | 93.3 | 90.4 | 87.1 | 62.4 | 44.5 |
| **SPLA** | **95.9** | **94.7** | **94.2** | **91.7** | **88.3** | **85.2** | **72.3** |

🔥 **关键发现**：
- 在 ≤64k 时，SPLA 与 dense 性能相当；
- 在 ≥128k 时，**SPLA 不仅远超其他 sparse 方法，甚至超过了 dense baseline**；
- 在 **256k** 上，SPLA 得分 **72.3** vs DENSE 的 **69.3**，首次实现“越稀越强”。

---

#### 表3：复杂推理任务表现（Table 4）
| Method | AIME 2024 | AIME 2025 | HMMT 2025 | LiveCodeBench | HumanEval | GPQA | MMLU Pro |
|--------|------------|------------|-------------|----------------|------------|--------|----------|
| DENSE | 77.1 | 73.9 | 60.0 | 61.6 | 85.4 | 68.5 | 78.9 |
| NSA | 73.1 | 64.2 | 53.3 | 49.1 | 78.7 | 59.6 | 68.8 |
| INF-V2 | 76.8 | 75.1 | 60.0 | 64.2 | 86.6 | 68.7 | 79.3 |
| SPA | 77.2 | 73.0 | 60.0 | 62.0 | 86.0 | 69.2 | 79.1 |
| **SPLA** | **78.3** | **75.1** | **63.3** | **62.4** | **86.6** | **69.5** | **79.3** |

✅ SPLA 在多项高难度推理任务上达到最优，尤其在 AIME 和 HMMT 数学竞赛题上显著领先。

---

### 消融实验结果
- **SPLA vs SPA**：两者共享相同的 selection 机制，唯一区别是是否启用 RLA。
  - 在 RULER 256k 上，SPLA (72.3) > SPA (44.5)，差距达 **+27.8**。
  - 结论：**RLA 模块对长尾压缩至关重要**，是 SPLA 成功的关键。
- **SPA vs INF-V2**：两者都只做 truncation，但 SPA 使用更优的二阶 selection。
  - SPA 在多数任务上略优于 INF-V2。
  - 结论：**principled selection 本身也能带来增益**，但远不如 RLA 显著。

---

## 4. 关键结论和发现

### 主要发现
1. **“丢弃”不是出路，“压缩”才是关键**  
   单纯 truncation 或 overlapping 架构无法解决长尾分布带来的性能退化。SPLA 通过 **residual linear attention** 实现无损压缩，在不增加 I/O 的前提下保留完整上下文。

2. **principled selection 显著优于 heuristic 方法**  
   基于二阶泰勒展开的选择机制能更准确捕捉 block 重要性，减少误删风险，尤其利于复杂推理任务。

3. **SPLA 可无缝适配预训练 dense 模型**  
   仅需极少量新增参数（RMSNorm scales），即可将 dense 模型转化为高效稀疏版本，适合在 CPT 阶段部署。

4. **在极长序列上反超 dense attention**  
   在 RULER 256k 上，SPLA 性能超越 dense baseline，表明其不仅能逼近 dense，还能因更好的长程建模而超越。

### 方法的局限性
- **训练效率略低**：当前实现相比 dense attention 有一定计算开销（step time 11s vs 9s at 16k），主要来自 selection overhead 和较小的 GQA group size（G=5）限制并行。
- **依赖 high-precision accumulation**：RLA 的递归状态需以 float32 积累以保证数值稳定，可能增加内存压力。
- **目前聚焦于 adaptation**：从头训练（scratch training）下的潜力尚未充分探索。

### 未来工作方向
- 探索 **解耦 sparse 与 linear 分支** 的参数化方式，例如为 RLA 设计专用 projection 或 feature map。
- 尝试更灵活的 grouping 策略（如 multi-value head 结构）以进一步提升训练效率。
- 将 SPLA 应用于更大规模模型（>100B）和真实应用场景（如检索增强生成、文档摘要等）。
- 研究如何动态调整 sparsity budget $k$ 以适应不同输入复杂度。

---

> ✅ **总结一句话**：  
> **SPLA 通过 principled selection + residual linear attention，在几乎不增加 I/O 的前提下实现了对“长尾”的有效压缩，首次使 sparse attention 在超长序列上全面媲美甚至超越 dense attention，为下一代长上下文 LLM 提供了一条高效可行的技术路径。**

</details>

---

### 3. [Understanding Efficiency: Quantization, Batching, and Serving Strategies in LLM Energy Use](https://arxiv.org/abs/2601.22362)

**Authors**: Julien Delavande, Regis Pierrard, Sasha Luccioni  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.22362v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in production, contributing towards shifting the burden in terms of computational resources and energy demands from training to inference. While prior work has examined the energy cost of inference per prompt or per token, we highlight how \emph...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Understanding Efficiency: Quantization, Batching, and Serving Strategies in LLM Energy Use

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
随着 Large Language Models（LLMs）从研究阶段进入生产部署，**推理阶段的能耗已成为AI可持续性的关键瓶颈**。尽管已有研究关注训练阶段的碳足迹，但对实际部署中系统级设计（如数值精度、批处理策略、请求调度）如何影响能效的研究仍不足。

本文聚焦于：  
> **在相同模型下，系统层面的设计选择如何导致高达数量级差异的能源消耗？**

### 🚀 提出的新方法与新思路
本研究提出了一种**相位感知（phase-aware）的细粒度能效分析框架**，将LLM推理分解为两个核心阶段进行独立建模与优化：

- **Prefill Phase**（预填充）：处理完整输入prompt，计算密集型（compute-bound）
- **Decode Phase**（解码）：自回归生成输出token，内存密集型（memory-bound）

在此基础上，系统性地评估了三大系统级因素对能效的影响：
1. **Numerical Precision（量化）**
2. **Batching Strategy（批处理）**
3. **Serving Configuration（服务架构与请求调度）**

特别强调“**到达整形（arrival shaping）**”——即通过控制用户请求的时间分布来提升批处理质量，从而显著降低能耗。

### 🔍 相比现有方法的优势
| 维度 | 传统做法 | 本文改进 |
|------|--------|---------|
| 能耗测量粒度 | 整体推理平均能耗 | 分解为 prefill / decode 阶段，实现 phase-level profiling |
| 量化效果理解 | 默认“低精度=更节能” | 揭示其仅在 compute-bound 场景有效，在 memory-bound 下可能适得其反 |
| 批处理分析 | 忽略padding开销 | 区分 effective vs. computed tokens，揭示 padding 对 prefill 的负面影响 |
| 请求模式假设 | 随机到达 | 引入 fixed inter-arrival delays（到达整形），证明可大幅提升能效 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用一个匿名评审中的子集数据集（Anonymous & Anonymous, 2025），源自 **UltraChat-200k** 的精选版本 `ultrachat_10k`。
- 包含 **10,000 条礼貌性提示（以 "thank you" 结尾）**
- 输入长度范围：200–4000 tokens（均值约 1200）
- 输出较短：10–300 tokens，模拟真实对话场景
- 所有提示适配各模型的输入格式

### ⚙️ 实验设置
#### 硬件平台
- **GPU**: NVIDIA H100 SXM（80GB）
- **CPU**: 8核 AMD EPYC 7R13
- 无其他共驻任务，确保测量纯净

#### 模型选择
涵盖主流开源指令微调模型：
- **Qwen 2.5系列**：0.5B, 1.5B, 3B, 7B, 14B
- **Mistral-7B-Instruct-v0.3**
- **LLaMA 3.1-8B-Instruct 和 70B**

#### 数值精度配置（dtype）
测试五种格式：
- `float32`, `bfloat16`, `float16`
- `int8`（LLM.int8()，outlier-aware mixed precision）
- `int4`（NF4 格式 + bitsandbytes 量化）

#### 推理引擎对比
- **Baseline**: Hugging Face `transformers` 库（静态批处理）
- **Optimized**: Hugging Face **Text Generation Inference (TGI)** 服务器 v3.3.4（支持 continuous batching、kernel fusion、paged attention）

#### 测量工具
- **能源监控**：CodeCarbon + NVML（GPU）、pyRAPL（CPU），RAM能耗通过启发式估算
- **延迟记录**：CUDA kernel 级别时间戳
- 每个配置重复运行10次，取平均值减少方差

#### 请求到达模式（用于TGI实验）
| 类型 | 描述 |
|------|------|
| Random delays | △ ~ Uniform(k, l)，模拟随机流量 |
| Fixed intervals | 固定间隔发送请求（如每50ms、300ms、500ms）|

---

## 3. 主要实验结果和性能指标

### 🔢 关键性能数据汇总

| 指标 | 结果 |
|------|------|
| **Prefill 能耗降幅（大模型）** | float32 → bfloat16/int8 可达 **4× 能源节省**（因Tensor Core加速） |
| **Decode 阶段量化影响** | int8 比 float32 **多耗 2–3× 能源**；int4 无明显优势 |
| **最优 batch size（decode）** | 在 LLaMA-8B 上为 **b=4**，更大批次收益递减 |
| **能量/有效输入token 最小点** | 出现在 **b=2**，权衡 prefill padding 与 decode 并行增益 |
| **TGI 相比 transformers 能耗下降** | 达到 **12.5× 更低能耗**（LLaMA-8B） |
| **固定到达间隔 vs 随机** | 在 TGI 中进一步降低至 **原始 baseline 的 1/100**（即 100× 节能） |
| **LLaMA-70B 多卡扩展表现** | 同样获得显著节能，验证可扩展性 |

### 📊 与基线方法对比结果

| 设置 | 平均每请求 GPU 能耗（Wh） | 相对改善 |
|------|--------------------------|----------|
| Baseline: float32 + transformers（顺序处理） | 1.2 × 10⁻¹ Wh | ×1（基准） |
| Optimized: bfloat16 + TGI + 随机到达 | 9.6 × 10⁻³ Wh | **12.5× 改善** |
| Fully Optimized: + 固定到达（500ms） | 1.1 × 10⁻³ Wh | **>100× 改善** |

> 💡 这意味着：每天处理百万请求时，总能耗从 **120 kWh/day**（相当于10户法国家庭日用电）降至 **1.1 kWh/day**

### 🔍 消融实验结果

#### （1）数值精度消融（见 Figure 1）
- **Prefill**：大模型（>7B）在低精度下显著节能（得益于Tensor Core）
- **Small models (<1.5B)**：保持 memory-bound，float16 反而轻微增加能耗（kernel overhead）
- **Decode**：所有模型均 memory-bound，低精度几乎无益；**int8/int4 因 dequantization kernels 导致更高能耗**

#### （2）Batch Size 消融（见 Figure 2）
- **按 input token 归一化（含padding）**
  - Prefill：随 batch 增大而上升（padding浪费）
  - Decode：U型曲线，最佳在 b=4
- **按 output token 归一化（无padding）**
  - 所有阶段持续改善，logarithmic 下降趋势
  - 表明 batching 本质有益，但需避免 padding 污染

#### （3）Serving Stack 消融（见 Figure 3）
- 切换到 TGI → 自动实现连续批处理 → 能耗骤降
- 加入 arrival shaping（固定间隔）→ 提高 batch consistency → GPU idle time ↓ → 能效再跃升

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **量化并非总是节能**  
   - 仅在 **compute-bound 场景（如 prefill 大模型）** 有效
   - 在 **memory-bound 场景（如 decode）**，int8/int4 因 dequantization 开销反而更耗电

2. **批处理是能效核心杠杆**  
   - 显著降低单位 token 能耗，尤其在 decode 阶段
   - 但 **padding 是 prefill 的主要效率杀手**，需结合 bucketing 或 shaped batching 缓解

3. **服务栈设计决定可持续性上限**  
   - 单纯更换模型或硬件不如优化 serving infrastructure
   - **TGI + continuous batching + kernel fusion** 可带来数量级改进

4. **请求到达模式至关重要（arrival shaping）**  
   - 用户侧轻量级调度（如固定延迟）即可极大提升 batching 质量
   - **per-request energy 可降低达 100×**

5. **必须进行 phase-aware energy profiling**  
   - Prefill 与 decode 具有根本不同的计算特征
   - 报告整体能耗会掩盖关键瓶颈，应分别测量与优化

---

### ⚠️ 局限性

| 限制 | 说明 |
|------|------|
| Prompt多样性有限 | 当前使用短prompt，未覆盖长上下文或多轮对话 |
| 硬件单一 | 仅基于 NVIDIA H100，结果在 AMD/TPU/AWS Inferentia 上可能不同 |
| 能耗测量粒度 | 主要关注 GPU，未完全计入 CPU、内存传输、网络I/O等系统级开销 |
| 多节点扩展未测试 | 当前实验限于单卡或4卡内，分布式调度未涉及 |

---

### 🔮 未来工作方向

1. **跨平台能效建模**  
   将该 phase-aware 分析推广至 TPU、Inferentia、移动设备等异构平台

2. **动态 arrival shaping 机制**  
   设计客户端 SDK 或代理层，主动调节请求节奏以匹配后端负载

3. **联合优化框架**  
   构建 end-to-end pipeline，协同优化 quantization、batching policy、traffic scheduling

4. **标准化能效报告协议**  
   推动社区采用 phase-separated、token-normalized、request-pattern-aware 的 energy reporting standard（类似 MLPerf Power）

5. **绿色API设计原则**  
   提出“可持续LLM API”的设计指南，例如推荐 batch-friendly 接口、鼓励非实时调用等

---

## 总结语

> “**Sustainable LLM deployment depends not only on model internals, but also on the orchestration of the serving stack.**”

本文颠覆了“模型越小越节能”的简单认知，指出：  
✅ **系统级优化（如 batching、serving 架构、请求调度）可以在不修改模型的前提下，实现超过100倍的能效提升。**

这为构建真正绿色、可扩展的AI服务体系提供了实证基础和工程指导。

</details>

---

### 4. [Pushing the Boundaries of Natural Reasoning: Interleaved Bonus from Formal-Logic Verification](https://arxiv.org/abs/2601.22642)

**Authors**: Chuxue Cao, Jinluan Yang, Haoran Li, Kunhao Pan, Zijian Zhao, Zhengyu Chen, Yuchen Tian, Lijun Wu, Conghui He, Sirui Han, Yike Guo  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.22642v1  

#### Abstract
Large Language Models (LLMs) show remarkable capabilities, yet their stochastic next-token prediction creates logical inconsistencies and reward hacking that formal symbolic systems avoid. To bridge this gap, we introduce a formal logic verification-guided framework that dynamically interleaves form...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Pushing the Boundaries of Natural Reasoning: Interleaved Bonus from Formal-Logic Verification

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在数学和逻辑推理任务中表现出色，但由于其基于概率的生成机制，在多步推理过程中容易出现**逻辑不一致**（logical inconsistency）和**奖励欺骗**（reward hacking）现象。即使最终答案正确，中间推理步骤也可能存在严重错误。传统方法如纯自然语言推理（Natural Language Reasoning）缺乏对推理过程的有效验证机制，导致模型可能通过表面模式“猜”出正确答案，而非建立严谨的逻辑链条。

### 提出了什么新方法或新思路
本文提出了一种**形式化逻辑验证引导的推理框架**（Formal Logic Verification-Guided Reasoning），其核心创新在于：

- **动态交错验证**（Dynamic Interleaving）：将形式化逻辑验证（如 SMT 求解器、z3-solver）实时嵌入到自然语言生成过程中，形成“生成-验证-修正”的闭环，而非传统的后验过滤（post-hoc filtering）。
- **两阶段训练框架**：
  1. **监督微调**（SFT）：通过一个分层的数据合成管道，生成包含自然语言推理、形式化证明和执行反馈的高质量训练样本。
  2. **策略优化**（Policy Optimization）：采用 **Group Relative Policy Optimization (GRPO)**，结合复合奖励函数，对逻辑正确性、结构完整性和计算效率进行联合优化。
- **执行驱动的验证机制**：在数据合成阶段引入执行结果验证（execution-based validation），确保自然语言、形式化代码和执行输出之间的一致性，提升数据质量。

### 相比现有方法的优势
- **主动纠错**：能实时检测并纠正中间推理谬误，防止错误传播。
- **跨领域适用**：不限于特定领域（如纯数学定理证明），可推广至一般逻辑和常识推理。
- **高可靠性**：利用形式化工具提供机器可验证的反馈，避免 LLM-as-a-judge 的主观偏差。
- **数据高效**：仅用约 17k 样本即达到 SOTA 性能，远少于依赖大规模数据的方法。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **SFT 数据集**：
  - WebInstruct-Verified
  - K&K
  - NuminaMath-TIR
- **RL 数据集**：从上述数据集中筛选教师模型（DeepSeek-R1）通过率低于 50% 的难题组成，共 3,525 个样本。
- **评估基准**（六大评测集）：
  - **逻辑推理**：KOR-Bench、BBH
  - **数学推理**：MATH-500、AIME 2024
  - **通用推理**：GPQA-Diamond、TheoremQA

### 实验设置和评估指标
- **模型架构**：基于 Qwen2.5-7B 和 Qwen2.5-14B 进行微调。
- **训练流程**：
  - **SFT 阶段**：学习率 1e-5，cosine 调度，训练 3 轮。
  - **RL 阶段**：使用 verl 框架，GRPO 算法，每轮生成 8 条轨迹，KL 系数 0.05，clip ratio 0.3，训练 120 步。
- **评估方式**：
  - 使用 OpenCompass 平台进行评测。
  - 除 AIME24 报告 avg@16 外，其余均采用贪婪解码（greedy decoding）。
- **评估指标**：准确率（Accuracy），按领域宏平均。

### 基线方法对比
| 类别 | 基线模型 |
|------|----------|
| 基础模型 | Qwen2.5-7B/14B |
| 强化学习模型 | SimpleRL-Zoo, General-Reasoner, RLPR |
| 逻辑增强模型 | SynLogic-7B |
| 工具集成模型 | ZeroTIR, SimpleTIR |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | KOR-Bench | BBH | MATH-500 | AIME24 | TheoremQA | General AVG |
|------|-----------|-----|----------|--------|------------|-------------|
| **Qwen2.5-7B-FLV-RL (Ours)** | **51.0** | **70.0** | **78.6** | **20.8** | **55.7** | **51.9** |
| **Qwen2.5-14B-FLV-RL (Ours)** | **57.0** | **78.0** | **81.4** | **30.2** | **63.5** | **58.6** |

- 在 7B 和 14B 尺寸上，相比当前最优基线（SOTA），平均性能分别提升了 **10.4%** 和 **14.2%**。
- 在最具挑战性的 AIME24 上，14B 模型达到 **30.2%**，几乎是 General-Reasoner（17.5%）的两倍。

### 与基线方法的对比结果
- **超越所有基线**：FLV-SFT 即使在 SFT 阶段就已超过多数 RL 基线；FLV-RL 在所有任务上均显著优于其他方法。
- **在逻辑密集型任务上优势明显**：
  - KOR-Bench 上比 SimpleTIR 高出 14 分（51.0 vs 37.0）。
  - TheoremQA 上比最近竞争者高出超 8 分。
- **工具使用范式转变**：分析显示，FLV-RL 更多地调用 **Symbolic & Logic** 类库（如 z3、sympy），占比达 **62.5%**，而 SimpleTIR 仅为 42.5%，表明其从“计算求解”转向“符号推理”。

### 消融实验结果
| 方法 | General AVG | KOR-Bench | TheoremQA |
|------|------------|-----------|------------|
| Natural-SFT | 36.5 | 30.4 | 41.2 |
| FLV-SFT (Ours) | **49.8** | **48.0** | **53.0** |
| Natural-RL | 37.0 | 35.7 | 41.2 |
| FLV-RL (Ours) | **51.9** | **51.0** | **55.7** |

- **形式化验证的作用**：FLV-SFT 相比 Natural-SFT 提升显著（+13.3%），尤其在逻辑任务上（+16.2%），说明形式化验证能有效提升推理一致性。
- **多阶段训练的价值**：FLV-RL 在 FLV-SFT 基础上进一步提升，而 Natural-RL 几乎无增益，表明形式化验证为 RL 提供了更稳定可靠的奖励信号。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **形式化验证是提升 LLM 推理能力的关键催化剂**：通过实时反馈，能有效抑制 reward hacking，推动模型发展出真正严谨的推理能力。
2. **交错式验证优于后验过滤**：动态介入生成过程可在错误发生时立即纠正，避免错误累积。
3. **符号推理优于暴力计算**：模型学会使用 z3 等工具进行抽象推理和反例搜索，而非依赖穷举或数值模拟。
4. **数据效率高**：少量高质量、经过验证的数据即可带来巨大性能提升。

### 方法的局限性
1. **计算开销大**：集成实时形式化验证使 RL 训练时间增加约 2 倍。
2. **自动形式化（autoformalization）瓶颈**：将模糊或常识性描述转化为形式化表达仍具挑战，可能导致映射错误。
3. **对开放域任务泛化有限**：目前主要适用于结构化较强的数学、逻辑等领域，对高度开放的问题支持较弱。

### 未来工作方向
- 开发更鲁棒的 **autoformalization** 技术，降低自然语言到形式语言的转换误差。
- 探索轻量化验证机制，减少计算延迟，提升推理效率。
- 将该框架扩展至更多领域，如法律、医学等需要严格逻辑保证的场景。
- 研究如何让模型自主决定何时启用形式化验证，实现更智能的“认知控制”。

> **总结**：本文提出的 FLV 框架成功地将神经网络的流畅性与符号系统的严谨性相结合，为构建可信、可靠、可解释的高级推理系统提供了新范式。其实验结果强有力地证明了**形式化验证可以作为可扩展机制，显著突破当前 LLM 推理性能的边界**。

</details>

---

### 5. [Autonomous Chain-of-Thought Distillation for Graph-Based Fraud Detection](https://arxiv.org/abs/2601.22949)

**Authors**: Yuan Li, Jun Hu, Bryan Hooi, Bingsheng He, Cheng Chen  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.22949v1  

#### Abstract
Graph-based fraud detection on text-attributed graphs (TAGs) requires jointly modeling rich textual semantics and relational dependencies. However, existing LLM-enhanced GNN approaches are constrained by predefined prompting and decoupled training pipelines, limiting reasoning autonomy and weakening...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Autonomous Chain-of-Thought Distillation for Graph-Based Fraud Detection**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于图的欺诈检测（Graph-based fraud detection）在处理**文本属性图（Text-Attributed Graphs, TAGs）**时面临两大挑战：
1. **缺乏自主推理能力**：现有 LLM-enhanced GNN 方法依赖预定义提示（predefined prompting），限制了 LLM 进行多跳、跨邻域的深层推理，仅能进行浅层模式匹配。
2. **语义-结构对齐弱化**：多数方法采用解耦训练（decoupled training），即先用 LLM 处理文本，再用 GNN 学习结构，导致语义表示与图结构之间缺乏联合优化，削弱了联合建模能力。

### **提出的新方法：FraudCoT**
作者提出了 **FraudCoT**，一个统一的框架，通过以下两个核心机制提升欺诈检测性能：

#### ✅ **(1) 欺诈感知的选择性 CoT 蒸馏（Fraud-Aware Selective CoT Distillation）**
- 利用教师 LLM（如 DeepSeek-R1）对标注节点及其邻居生成多样化的推理路径（Chain-of-Thought, CoT），包括正确和错误路径。
- 设计**正负蒸馏目标**：
  - 正样本：教师推理与真实标签一致 → 鼓励学生模仿（Cross-Entropy Loss）
  - 负样本：不一致 → 鼓励学生抑制（Unlikelihood Loss）
- 学生 LLM 经过蒸馏后，能生成更鲁棒、抗偏见的推理路径，并将其嵌入节点文本中，形成 **CoT-augmented 表示**。

#### ✅ **(2) 高效的非对称协同训练（Efficient Asymmetric Co-training）**
- 为实现端到端优化同时避免高昂计算成本，提出**非对称编码策略**：
  - **目标节点**：每轮训练都用 LLM 动态编码其 CoT 增强文本。
  - **邻居节点**：使用初始参数一次性编码并缓存（cache），后续固定不变。
- 显著降低 LLM 推理复杂度从 $O(|N(v)|)$ 到 $O(1)$ 每个目标节点，实现高达 **1,066× 的训练吞吐加速**。

### **相比现有方法的优势**
| 方面 | 现有方法（如 TAPE, FLAG） | FraudCoT |
|------|----------------------------|---------|
| **推理方式** | 预定义模板引导，浅层特征提取 | 自主、自由形式的多步 CoT 推理 |
| **训练范式** | 解耦训练（decoupled） | 端到端联合优化（end-to-end） |
| **语义-结构对齐** | 弱，分离学习 | 强，梯度隐式调制结构信息 |
| **效率** | 高（因解耦） | 同样高效，且保持端到端优势 |
| **可解释性** | 有限 | 每个预测附带人类可读的推理路径 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
| 数据集 | 类型 | #Nodes | #Edges | 特点 |
|-------|------|--------|--------|------|
| **InstantVideo** | 公共（Amazon Reviews） | ~37K | ~9.9M | 用户评论是否“有帮助”分类，构建异构图（R-U-R, R-P-R, R-S-R） |
| **DigitalMusic** | 公共（Amazon Reviews） | ~65K | ~7.7M | 同上，音乐品类评论 |
| **PromotionAbuse** | 工业私有（ByteDance 提供） | ~371K | ~1.4M | 真实促销滥用场景，检测恶意刷单行为 |

> 所有数据集均构造成包含三种边类型的异构图。

### **实验设置**
- **模型骨干**：使用 Qwen3-8B 作为 LLM 主干，GNN 使用 Heterogeneous GraphSAGE。
- **微调技术**：所有 LLM 微调均采用 LoRA（Low-Rank Adaptation）。
- **优化器**：AdamW，学习率网格搜索。
- **硬件环境**：NVIDIA A100 GPU（80GB），Linux 系统。
- **训练策略**：Stage 1 先进行 CoT 蒸馏；Stage 2 进行非对称协同训练。

### **评估指标**
- **Macro-F1**：类别不平衡下的平均 F1 分数
- **AUROC**：ROC 曲线下面积
- **AUPRC**：精确率-召回率曲线下面积（尤其适用于高度不平衡任务）

> 报告 5 次随机种子的均值 ± 标准差，显著性检验使用配对 t-test（p < 0.05）。

### **基线方法对比**
分为四类：
1. **纯 GNN 方法**：GraphSAGE, HGT, ConsisGAD, PMP, GAAP
2. **无图模型（Graph-agnostic）**：MLP, LLM, LLM-SFT
3. **图增强 LLM（Graph-enhanced LLMs）**：LLaGA, GraphGPT, HiGPT, InstructGLM
4. **LLM-enhanced GNNs**：TAPE, FLAG（代表当前 SOTA）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 3）**
FraudCoT 在所有数据集和指标上均取得最优表现，尤其在工业数据集 **PromotionAbuse** 上优势显著：

| 方法 | InstantVideo (AUPRC) | DigitalMusic (AUPRC) | PromotionAbuse (AUPRC) |
|------|------------------------|------------------------|--------------------------|
| **TAPE** | 79.28% | 81.99% | 67.21% |
| **FLAG** | 41.75% | 38.59% | 42.17% |
| **FraudCoT (Ours)** | **84.10%** (+4.82%) | **84.49%** (+2.50%) | **73.65%** (+6.44%) |

> 最高提升达 **8.8% AUPRC**（相对提升约 13%），且统计显著（*p < 0.05*）。

### **与基线方法的对比结果**
- **优于纯 GNN 和 LLM 方法**：说明融合语义与结构的重要性。
- **优于图增强 LLM**：表明简单注入结构先验不足以捕捉细粒度欺诈模式。
- **优于 LLM-enhanced GNN（如 TAPE/FLAG）**：
  - 因其受限于预定义提示，无法进行深层推理；
  - 解耦训练导致语义-结构脱节。

### **消融实验结果（Ablation Study）**
移除不同组件后的性能下降情况（Figure 4）：

| 变体 | 描述 | 性能影响 |
|------|------|--------|
| **w/o FASCD** | 移除 CoT 蒸馏阶段 | 下降最严重，尤其在 AUPRC 上，说明 CoT 对捕获跨邻域依赖至关重要 |
| **w/o EAC** | 冻结 LLM 编码器，禁用协同训练 | 性能明显下降，验证了端到端优化对语义-结构对齐的关键作用 |
| **w/o NegDis** | 不抑制错误推理路径 | 性能下降，说明负样本蒸馏有助于提高鲁棒性和泛化能力 |

> 结果表明：**三个组件相互增强，缺一不可**。

### **其他分析**
- **蒸馏样本数量影响（Figure 5）**：约 100 个蒸馏样本即可饱和性能，说明少量高质量 CoT 即可有效迁移推理能力。
- **负蒸馏权重 λ 影响（Figure 6）**：当 λ = 100 时效果最佳，说明**强力抑制错误推理路径**对欺诈检测至关重要。
- **训练效率（Table 5）**：
  - **DigitalMusic 上训练速度提升 1,066×**
  - **最大 batch size 提升 128×**
  - 相比 naive joint training，计算开销大幅降低，但仍保持端到端优势。

---

## **4. 关键结论和发现**

### **主要发现**
1. **自主推理优于预定义提示**：  
   自由形式的 CoT 推理能够揭示隐藏的多跳欺诈模式（如“同一用户发布极端评分”、“虚假好评与大量差评共存”），而预定义模板只能提取表面特征。

2. **正负 CoT 蒸馏是关键机制**：  
   不仅要学“怎么想对”，还要学“怎么想错”，从而避免继承教师 LLM 的偏见和幻觉。

3. **端到端优化 + 高效设计可行且必要**：  
   非对称协同训练打破了“端到端=低效”的固有认知，在保持高性能的同时实现了工业级可扩展性。

4. **语义与结构必须联合优化**：  
   解耦训练虽快，但牺牲了关键的交互信号；FraudCoT 证明了二者可以兼得。

### **方法的局限性**
- **依赖教师 LLM 生成 CoT**：虽然蒸馏后学生轻量，但第一阶段仍需调用大模型，成本较高。
- **CoT 质量受提示工程影响**：尽管强调“自由形式”，但仍需精心设计 prompt 模板以引导有效推理。
- **静态缓存假设**：邻居嵌入被缓存后不再更新，可能忽略某些动态关系演化（但在欺诈检测中变化较慢，影响较小）。

### **未来工作方向**
- 将 CoT 蒸馏扩展至更多下游任务（如虚假新闻检测、金融反洗钱）。
- 探索自动 prompt 生成或强化学习来进一步提升推理质量。
- 动态缓存机制：定期更新邻居表示以适应图演化。
- 多模态欺诈检测：结合图像、行为序列等多源信息进行联合推理。

---

> 💡 **一句话总结**：  
> **FraudCoT 通过“选择性 CoT 蒸馏 + 非对称协同训练”，首次实现了高效、自主、端到端的 LLM-GNN 欺诈检测框架，在性能与效率上全面超越现有方法，为工业级应用提供了新范式。**

</details>

---

### 6. [BayesFlow: A Probability Inference Framework for Meta-Agent Assisted Workflow Generation](https://arxiv.org/abs/2601.22305)

**Authors**: Bo Yuan, Yun Zhou, Zhichao Xu, Kiran Ramnath, Aosong Feng, Balasubramaniam Srinivasan  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.22305v1  

#### Abstract
Automatic workflow generation is the process of automatically synthesizing sequences of LLM calls, tool invocations, and post-processing steps for complex end-to-end tasks. Most prior methods cast this task as an optimization problem with limited theoretical grounding. We propose to cast workflow ge...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BayesFlow: A Probability Inference Framework for Meta-Agent Assisted Workflow Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **agentic workflow** 的设计高度依赖人工专家进行反复试错，导致开发成本高、可扩展性差。现有的自动工作流生成方法大多将该任务建模为**优化问题**（如基于搜索的策略），缺乏理论基础，且通常只输出单一最优解，多样性不足。

本文旨在解决以下挑战：
- 如何在无需训练的前提下自动生成高质量、多样化的 **workflow**；
- 如何在仅有终端奖励（terminal reward）的情况下有效引导前缀决策；
- 如何为自动 workflow 生成提供**理论保证**。

---

### 提出的新方法与新思路
作者提出 **Bayesian Workflow Generation (BWG)**，将 workflow 生成视为一个**贝叶斯后验采样问题**，而非传统的优化问题。

#### 核心思想：
- 将每个 workflow 视为从一个能量模型定义的非归一化后验分布中采样的轨迹：
  $$
  q(s_{1:T}) \propto p(s_{1:T}) \exp(R(s_{1:T}))
  $$
  其中：
  - $p(s_{1:T})$ 是由 meta optimizer LLM 定义的先验；
  - $R(s_{1:T})$ 是任务相关的奖励函数（如准确率）；
  - $\exp(R)$ 构成能量项，提升高奖励路径的概率。

在此框架下，实例化算法 **BayesFlow**，其两大核心技术模块：

1. **Parallel Look-ahead Rollouts（并行前瞻 rollout）**
   - 在每一步对当前部分 workflow 进行 K 次随机补全，估计其“下游价值”；
   - 利用重要性权重进行重采样，缓解纯先验采样的退化问题；
   - 不依赖额外训练的价值模型或闭源强模型，计算高效且可并行。

2. **Sequential In-loop Refinement（顺序循环内精炼）**
   - 引入全局精炼机制（refiner），基于已有完整 workflows 进行改进（如 MCTS 或文本梯度）；
   - 可修复早期错误，实现跨步长的全局优化；
   - 统一了多种现有方法（如 AFlow、ADAS）作为特例。

---

### 相比现有方法的优势
| 方面 | BayesFlow | 传统方法（如 AFlow, ADAS） |
|------|----------|-----------------------------|
| **理论基础** | ✅ 提供渐近收敛到目标后验的证明（Theorem 1） | ❌ 多数无严格理论保障 |
| **多样性** | ✅ 自然产生多样化高质量 workflows | ❌ 倾向于单一最优解 |
| **效率** | ✅ 更低 token 开销，支持完全并行化 | ❌ 多为串行搜索，耗时高 |
| **灵活性** | ✅ 轻量接口，不限定预设模块（如 REVISE, ENSEMBLE） | ❌ 依赖固定模板 |
| **通用性** | ✅ 支持 open-source 和 closed-source LLM | ✅ |

---

## 2. 核心实验方法和设置

### 使用的数据集
共六个 benchmark 数据集，涵盖三大领域：

| 类别 | 数据集 | 描述 |
|------|--------|------|
| 数学推理 | **MATH**, **GSM8K** | 复杂数学应用题求解 |
| 问答 | **HotpotQA**, **DROP** | 多跳推理与离散推理 |
| 专业知识 | **GPQA**, **MMLU-Pro** | 高阶科学知识与多任务理解 |

此外还测试了代码生成任务 **MBPP**。

---

### 实验设置与评估指标

#### 模型配置
- **Meta Optimizer LLM**: `Claude 3.5-Sonnet`（用于生成 workflow）
- **Executor LLM**: `Claude 3.7-Sonnet` 或 `Qwen2.5-7B-Instruct`（执行 workflow 并评分）
- 所有模型通过 API 接入，温度设为 0（除特定步骤外）

#### 评估指标
| 数据集 | 指标 |
|-------|------|
| GSM8K, MATH | Solve Rate（正确率） |
| HotpotQA, DROP | F1 Score |
| GPQA, MMLU-Pro | Accuracy（选项匹配） |
| MBPP | Pass@1 |

#### 基线方法对比
- **Prompting Baselines**:
  - Zero-shot (IO)
  - Chain-of-Thought (CoT)
  - CoT with Self-Consistency (CoT-SC)
- **Automatic Workflow Methods**:
  - **ADAS**（线性启发式搜索）
  - **AFlow**（Monte Carlo Tree Search）
  - **MaAS**（进化搜索）

所有基线尽可能复现原论文设置，并共享相同验证集用于反馈。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | 平均准确率（Claude 执行器） | 平均准确率（Qwen 执行器） |
|------|----------------------------|----------------------------|
| AFlow | 76.2% | 59.9% |
| **BayesFlow (Ours)** | **80.8%** (+4.6pp) | **63.4%** (+3.5pp) |

#### 各数据集表现亮点：
- **MATH**: **69.4%** vs AFlow 的 60.1% → **+9.3pp**
- **GPQA**: **69.2%** vs 82.3% → **+6.9pp**
- **DROP**: **90.8%** vs 89.2% → **+1.6pp**
- **MBPP (Pass@1)**: **85.0%** vs AFlow 的 75.5% → **+9.5pp**

> 💡 BayesFlow 在最难的数学与专业知识任务上优势最显著。

---

### 与基线方法的对比结果
- 在所有六大数据集中，BayesFlow 均达到**最高平均性能**；
- 相比 SOTA workflow 方法（AFlow），平均提升 **4.6–9.0 pp**；
- 相比 zero-shot prompting，最大提升达 **65 pp**；
- 即使在小模型（Qwen-7B）上仍保持领先，显示良好泛化性。

---

### 消融实验结果（Ablation Study）

#### （1）不同 N（每轮 partial workflows 数量）的影响（Table 3）
| N | MATH | HotpotQA | DROP | ... |
|----|-------|-----------|------|
| 5 | 70.4±1.9 | 77.6±0.1 | 90.8±0.2 |
| 10 | 69.4±4.1 | 77.5±0.7 | 90.8±1.5 |
| 15 | 71.1±0.9 | 77.0±0.7 | 90.7±0.3 |

✅ 结果表明 BayesFlow 对超参数选择鲁棒，探索-利用平衡灵活。

#### （2）Token 效率对比（Table 2）
| 方法 | 输入 tokens（DROP） | 输出 tokens | F1 |
|------|--------------------|-------------|-----|
| AFlow | 114M | 19.7M | 0.755 |
| **BayesFlow** | **26.6M** | **9.4M** | **0.765** |

✅ BayesFlow 显著更**token-efficient**，尤其在输入侧节省超过 **75%**。

#### （3）推理时扩展性（Inference-time Scaling）（Table 4）
使用 Top-L workflows 投票：
| L | Best@L (BayesFlow/AFlow) |
|---|-------------------------|
| 1 | 74 / 45 |
| 2 | 80 / 46 |
| 4 | 85 / 65 |
| 8 | **86 / 81** |

✅ BayesFlow 的 workflows 更**多样化且高质量**，使得集成效果远优于 AFlow，尤其体现在 Best@L 上。

---

## 4. 关键结论和发现

### 主要发现
1. **将 workflow 生成建模为贝叶斯后验采样是有效的**：
   - 提供了 principled 的理论框架；
   - 支持多样性生成与渐近收敛性（Theorem 1）；

2. **Look-ahead Rollout + In-loop Refinement 是高效组合**：
   - 前瞻 rollout 提供无偏重要性权重，避免奖励退化；
   - 精炼机制虽破坏渐近收敛，但带来实证增益（Theorem 2 分析 drift bound）；

3. **BayesFlow 实现全面超越**：
   - 在 accuracy、efficiency、robustness 上均优于现有方法；
   - 特别适合复杂推理任务（如 MATH、GPQA）；

4. **轻量接口设计促进创新**：
   - 不强制使用预定义模块（如 PROGRAMMER），鼓励 LLM 自主探索新结构。

---

### 方法的局限性
1. **推理开销增加**：
   - 并行 lookahead rollouts 增加了 inference-time 计算负担；
   - 当前实现未长期复用历史轨迹，存在冗余采样。

2. **依赖 executor LLM 的稳定性**：
   - 若 executor 执行失败频繁，会影响 reward 信号质量。

3. **评估范围有限**：
   - 当前仅在七大数据集上验证，尚未覆盖多模态或多智能体协作场景。

---

### 未来工作方向
1. 设计**动态控制器**，根据在线信号自适应调整 rollout 数量 K 和 refine 频率 M；
2. 引入**记忆机制**，保留高价值轨迹以减少重复采样；
3. 扩展至 **multi-modal** 和 **embodied agent** 场景；
4. 探索 **training-based + sampling-based hybrid** 框架，进一步提升效率与性能边界。

--- 

> 📌 总结：**BayesFlow** 通过引入 **Bayesian inference** 视角，为自动 workflow 生成提供了首个具有理论保证的采样框架，在性能、效率和多样性方面全面超越现有方法，标志着从“search-based design”向“posterior sampling-based design”的范式转变。

</details>

---

### 7. [DART-ing Through the Drift: Dynamic Tracing of Knowledge Neurons for Adaptive Inference-Time Pruning](https://arxiv.org/abs/2601.22632)

**Authors**: Abhishek Tyagi, Yunuo Cen, Shrey Dhorajiya, Bharadwaj Veeravalli, Xuanyao Fong  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.22632v1  

#### Abstract
Large Language Models (LLMs) exhibit substantial parameter redundancy, particularly in Feed-Forward Networks (FFNs). Existing pruning methods suffer from two primary limitations. First, reliance on dataset-specific calibration introduces significant data dependency and computational overhead. Second...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DART-ing Through the Drift: Dynamic Tracing of Knowledge Neurons for Adaptive Inference-Time Pruning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题

当前 Large Language Models (LLMs) 存在显著的参数冗余，尤其是在 **Feed-Forward Networks (FFNs)** 中。现有的剪枝方法存在两大局限：

1. **依赖数据校准（dataset-specific calibration）**：需要额外的校准数据集来确定重要参数，增加了计算开销和数据依赖性。
2. **静态剪枝（static pruning）**：一旦生成上下文发生变化（如话题切换），固定的剪枝掩码（mask）无法适应动态的知识需求，导致“知识漂移”（knowledge drift）——即模型在生成过程中因关键神经元被永久剪除而丢失语义连贯性。

### 🚀 提出的新方法：DART

作者提出 **DART**（Dynamic Attention-Guided Runtime Tracing），一种轻量级、无需训练的动态运行时剪枝框架，其核心思想是：

- **动态感知上下文变化**：通过监测 **attention 输出分布的变化** 来检测语义上下文是否发生偏移。
- **自适应更新神经元掩码**：当检测到“知识漂移”时，触发重新选择活跃的 **knowledge neurons**，从而保持模型表达能力。

### 🔍 创新点与优势

| 创新点 | 说明 |
|------|------|
| **Context-aware Neuron Selector** | 基于每层 FFN 的内在知识密度分配稀疏预算，建模层内相对重要性和层间交互，实现无辅助训练的结构化剪枝。 |
| **Knowledge Drift Detection** | 首次识别并形式化“知识漂移”这一失败模式，并提出基于 attention 输出分布偏移的在线检测机制。 |
| **Runtime Mask Update Mechanism** | 引入一个计数器机制，避免因瞬时噪声频繁重剪枝，仅在持续偏离参考上下文时才触发掩码更新。 |
| **轻量化与高效性** | 整个框架内存开销小于 10MB（对 LLAMA-3.1-8B 而言），FLOPs 开销仅 0.1%，适合部署于资源受限环境。 |

> ✅ **相比现有方法的优势**：
> - 不依赖校准数据（training-free）
> - 动态适应上下文演化（非静态）
> - 保留关键知识神经元，避免长程生成中的误差累积
> - 性能接近原始密集模型，远超静态剪枝方法

---

## 2. 核心实验方法和设置

### 📚 数据集

实验覆盖多个任务类型，验证方法在通用与领域特定任务上的鲁棒性：

#### **零样本任务（Zero-Shot Tasks）**
- **BoolQ**, **RTE**, **HellaSwag**, **Winogrande**, **ARC-E/C**, **OBQA**

#### **领域特定多跳任务（Domain-Specific Multi-Shot Tasks）**
- **MMLU**（涵盖52个学科）
- **GPQA**（研究生级别问答）
- **MEDMCQA**（医学多选题）

#### **长文本摘要任务（Summarization Benchmarks）**
- **CNN/DailyMail**
- **Multi-News**
- **GovReport**

#### **自定义多主题生成数据集（Custom Prompts）**
- 设计了 500 个需跨领域生成的提示模板（见 Appendix F），用于测试知识追踪能力。

---

### ⚙️ 实验设置

| 设置项 | 描述 |
|------|------|
| **模型** | 主要使用 **LLAMA-3.2-3B** 和 **LLAMA-3.1-8B**；附录中扩展至 Qwen3、DeepSeek、Mistral 等 |
| **剪枝目标** | 对 FFN 层进行 **70% 结构化剪枝**（structured neuron pruning） |
| **评估方式** | 使用 `lm-eval-harness` 进行 zero-shot 和 five-shot 评测 |
| **硬件平台** | 双 AMD EPYC CPU + 8×NVIDIA L40S GPU |

---

### 📊 评估指标

| 指标 | 用途 |
|------|------|
| **Accuracy (%)** | 分类/推理任务的主要评价标准 |
| **ROUGE-L**, **BLEU** | 衡量摘要任务的内容召回与表面一致性 |
| **BERTScore (F1)** | 衡量生成内容的语义相似度 |
| **Embedding Similarity** | 衡量隐藏状态轨迹与原模型的一致性 |
| **Coverage** | 衡量源信息利用程度 |

---

### 🔁 基线方法对比

| 基线方法 | 类型 | 特点 |
|--------|------|------|
| **WANDA** | 静态、权重级剪枝 | 基于校准集的重要性评分，固定掩码 |
| **SparseGPT** | 静态、非结构化剪枝 | 单次剪枝，不更新掩码 |
| **DEJAVU** | 动态、预测式剪枝 | 使用额外训练的小模型预测重要神经元 |
| **DLP / OWL** | 静态、分层重要性剪枝 | 基于全局统计得分剪枝 |

> ✅ DART 是唯一 **完全无需训练、无校准数据、且支持运行时掩码更新** 的方法。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

在 **70% FFN 稀疏率下**，DART 在多个基准上显著优于现有方法：

| Benchmark | LLAMA-3.1-8B (Dense) | WANDA | DEJAVU | **DART (Ours)** |
|----------|------------------------|-------|--------|------------------|
| **HellaSwag** | 79.32 | 44.99 | 26.68 | **64.58** (+19.6↑ vs best baseline) |
| **ARC-E** | 82.53 | 50.34 | 25.08 | **59.43** |
| **MMLU (5-shot)** | 66.61 | 29.70 | 25.20 | **34.14** |
| **MEDMCQA (5-shot)** | 56.66 | 25.65 | 28.16 | **29.35** |

> 💡 在 LLAMA-3.1-8B 上，DART 相比最佳动态基线最高提升达 **+19.6% 准确率**。

---

### 📉 摘要任务表现（Figure 6 & Table 2）

在 **long-horizon summarization** 任务中，DART 显著优于静态剪枝：

| 指标 | 静态剪枝 (50% sparsity) | **DART + Tracking** |
|------|-------------------------|---------------------|
| **ROUGE-L** | 低（~0.2–0.3） | **0.38** |
| **BLEU** | 0.20 | **0.36** |
| **BERTScore (F1)** | 0.80 | **0.83** |
| **Embedding Sim** | 0.85 | **0.92** |
| **Coverage** | 0.77 | **0.89** |

> ✅ DART 实现了 **高达 3× 的 ROUGE-L 提升**，且生成质量接近原始密集模型。

---

### 🔍 消融实验与分析（Appendices）

#### ✅ **知识漂移检测有效性（Appendix E）**
- 构造控制实验：先写电动车 → 再写意大利菜。
- 若在第100 token 后剪枝且不释放，则第二段变为重复标题（gibberish）。
- 若在第400 token 释放掩码，可恢复连贯输出；若延迟至第500 token，则无法恢复。
- 表明：**知识未丢失，但因神经元不可用而“被困”在错误语义盆地**。

#### ✅ **注意力空间漂移信号（Figure 10）**
- 使用 **cosine similarity** 衡量当前 attention 输出与初始上下文中心的距离。
- 发现：即使模型尝试转向新主题，剪枝后仍被“拉回”原语义区域。
- DART 的检测机制能捕捉该趋势并及时更新掩码。

#### ✅ **层敏感性分析（Appendix D）**
- 早期层更敏感（影响基础语法与结构）
- 后期层更适合剪枝（编码高层语义，部分剪枝甚至有益于聚焦）

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **知识漂移（Knowledge Drift）是动态剪枝的关键失败模式**  
   - 自回归生成过程中，上下文不断演变，静态掩码会导致关键知识神经元缺失。
   - 这种偏差会随时间累积，最终导致语义崩溃。

2. **attention 输出分布偏移是知识漂移的有效代理信号**  
   - attention 子层负责捕捉 token 间关系，其输出变化早于 FFN 失效。
   - 可作为“预警系统”指导运行时剪枝决策。

3. **DART 实现了高稀疏下的高质量推理**  
   - 在 70% FFN 剪枝下，性能损失极小，尤其在复杂推理与多主题任务中优势明显。
   - 支持 **long-context generation** 而不牺牲连贯性。

4. **更大的模型更具知识隔离性（Knowledge Segregation）**  
   - 大模型中知识神经元更专业化，跨主题干扰少，因此更易精准剪枝。
   - 小模型功能叠加严重，剪枝风险更高。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **依赖 attention 输出稳定性** | 若 attention 本身不稳定（如极端对抗输入），可能导致误触发掩码更新。 |
| **未优化硬件执行效率** | 当前评估侧重算法有效性，实际加速比受 kernel 实现限制（见 Appendix G）。 |
| **仅剪枝 FFN，未处理 attention** | 虽然 GQA 已降低 attention 参数占比，但在某些架构中仍有优化空间。 |
| **掩码更新频率较低** | 平均每 100k tokens 触发约 100 次，可能错过细粒度变化。 |

---

### 🔮 未来工作方向

1. **将 DART 扩展至 MoE 架构**  
   - 在专家选择层面引入动态追踪机制。

2. **结合量化与剪枝形成联合压缩方案**  
   - 探索 Quantization + DART 的协同效应。

3. **设计更高效的运行时调度器**  
   - 在边缘设备上实现实时掩码更新与缓存管理。

4. **探索其他潜在漂移信号**  
   - 如梯度流、残差连接强度等作为补充监控维度。

5. **构建标准的“多主题生成”评测基准**  
   - 推动对动态能力的系统性评估。

---

## ✅ 总结

**DART** 成功解决了 LLM 推理中 **静态剪枝无法适应上下文演化** 的根本问题，提出了首个 **无需训练、无需校准、可运行时自适应更新掩码** 的轻量级剪枝框架。其实验充分证明：

- 动态剪枝必须考虑 **知识漂移**
- **attention 输出分布** 是有效的上下文变化探测器
- 通过少量掩码更新即可大幅恢复生成质量
- 在多种任务上达到甚至逼近原始密集模型的表现

> 🌟 **一句话总结**：  
> DART 通过“用 attention 看世界，用 FFN 写答案”的理念，实现了真正意义上的 **上下文感知、运行时自适应的知识神经元追踪剪枝**，为高效部署大模型提供了新范式。

</details>

---

### 8. [Quartet II: Accurate LLM Pre-Training in NVFP4 by Improved Unbiased Gradient Estimation](https://arxiv.org/abs/2601.22813)

**Authors**: Andrei Panferov, Erik Schultheis, Soroush Tabesh, Dan Alistarh  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.22813v1  

#### Abstract
The NVFP4 lower-precision format, supported in hardware by NVIDIA Blackwell GPUs, promises to allow, for the first time, end-to-end fully-quantized pre-training of massive models such as LLMs. Yet, existing quantized training methods still sacrifice some of the representation capacity of this format...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Quartet II: Accurate LLM Pre-Training in NVFP4 by Improved Unbiased Gradient Estimation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于 **NVFP4** 的全量化大语言模型（LLM）预训练虽然能显著降低计算成本，但在精度上仍明显落后于 FP16 和 FP8 训练。其主要原因在于：

- **前向传播**中为保留动态范围而牺牲了表示能力（如采用 square-block quantization 导致每组 scale 覆盖过多元素）；
- **反向传播**中依赖传统的 **Stochastic Rounding (SR)** 进行无偏梯度估计，但 SR 在 4-bit 精度下引入了较高的方差和量化误差。

因此，如何在保持硬件兼容性和高效执行的前提下，提升 NVFP4 量化训练的精度，是当前的关键挑战。

---

### **提出的新方法与新思路**

#### ✅ **核心创新：MS-EDEN —— 新型无偏量化原语**
- 提出一种适用于微缩放浮点格式（microscaling FP4）的新型无偏量化方案：**MicroScaling EDEN (MS-EDEN)**。
- 与传统 SR 将随机性施加在每个 FP4 元素不同，MS-EDEN 将随机性转移到 **microscale 因子（FP8 scales）** 上，通过结合 **Randomized Hadamard Transform (RHT)** 和带校正的重缩放机制实现无偏估计。
- 在期望意义下保证无偏性，同时大幅降低均方误差（MSE），相比 SR 减少超过 **2倍的量化误差**。

#### ✅ **完整训练框架：Quartet II**
构建了一个端到端的全 NVFP4 线性层训练图（computation graph），融合以下关键技术：
- **前向传播**：
  - 使用原生 NVFP4 编码（每 16 个元素一个 FP8 scale）而非 square-block；
  - 引入 **“Four Over Six” (4/6)** 自适应网格选择策略，进一步优化量化 MSE。
- **反向传播**：
  - 应用 **MS-EDEN** 对所有 GEMM 操作中的张量进行无偏再量化；
  - 利用内维度上的 RHT 实现分布平滑与误差控制。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **量化误差** | MS-EDEN 的 MSE 比 SR 低 **>2x**（见 Table 1） |
| **表示能力** | 放弃 square-block quantization，恢复每 16 元素独立 scale，提升前向表示能力 |
| **无偏性保障** | 严格理论支持 + 数值验证表明 MS-EDEN 在实践中有效维持无偏梯度估计 |
| **硬件效率** | 提供针对 NVIDIA Blackwell GPU 的定制化 CUDA kernel，支持高达 **4.2x 的速度提升**（vs BF16） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **C4**：用于 Llama-like 模型的预训练，评估语言建模 loss。
- **FineWeb-Edu**：用于更大规模的 Nanochat 流水线训练。
- 微调阶段使用多个下游任务数据集：
  - **ARC-Challenge/Easy**
  - **GSM8K**
  - **HumanEval**
  - **MMLU**

---

### **实验设置与评估指标**

#### 🔧 **模型架构**
- 基于 **Llama-2** 架构的 Transformer 模型，参数量从 **30M 到 1.9B** 不等。
- 所有线性层替换为所提 QAT 方案，其余结构保持不变。

#### 📊 **评估指标**
| 类别 | 指标 |
|------|------|
| 预训练质量 | C4 上的 **Validation Loss**（相对于 BF16 的差距） |
| 下游能力 | Zero-shot 准确率（Arc, GSM8K, HumanEval, MMLU） |
| 效率 | **训练吞吐量（ktok/s）**、**相对 BF16 的加速比** |
| 无偏性验证 | 多次量化反向传播后平均梯度与真实梯度的相对误差（见 Figure 9） |

#### ⚙️ **训练配置**
- 使用 **AdamW** 优化器，cosine 学习率调度；
- 数据-参数比覆盖 25~800，测试不同训练强度下的稳定性；
- 所有超参复用自 BF16 基线，确保公平比较。

---

### **基线方法对比**
| 基线 | 描述 |
|------|------|
| **NVIDIA et al. (2025)** | 首个端到端 NVFP4 预训练方案，使用 square-block + SR |
| **TetraJet-v2 (Chen et al., 2025b)** | 改进版 NVIDIA 方案，加入 outlier 控制与振荡抑制 |
| **FourOverSix (Cook et al., 2025)** | 提出 4/6 自适应 scale 选择，但仅用于前向且未解决反向无偏性问题 |
| **SR-only variants** | 各种组合下的 Stochastic Rounding 基线，作为消融对照 |

> 注：作者指出 TetraJet-v2 中的部分设计（如中间 FP32 scale）难以在 GPU 上高效实现，故本文采用其“GPU 可行子集”作为实际对比基线。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **MS-EDEN vs SR 的 MSE** | 在 N(0,1) 分布下，MS-EDEN MSE ≈ **9.8×10⁻³**，SR 为 **23.5×10⁻³**，降低 >2x（Table 1） |
| **端到端预训练损失增益** | Quartet II 相比 TetraJet-v2 平均减少 **≥20% 的 loss 差距**（Figure 4） |
| **Nanochat 预训练 BPB 提升** | 在 1.9B 模型上，Quartet II 将验证 bits-per-byte (BPB) 与 BF16 的差距缩小 **15–25%**（Figure 5） |
| **推理 kernel 加速** | 线性层训练速度最高达 **4.2x BF16**，优于 Quartet (MXFP4) 约 **70%**（Figure 6） |
| **真实训练吞吐提升** | 在 1.1B 模型上达到 **245% 的 BF16 吞吐量**（得益于内存节省带来的 batch size 提升） |

---

### **与基线方法的对比结果**

| 方法 | 相对 BF16 Loss Gap ↓ | 是否无偏 |
|------|------------------------|----------|
| NVIDIA (2025) | 高 | 是 |
| TetraJet-v2 | 中高 | 是 |
| FourOverSix (backbone) | 中 | ❌（实验证明有偏） |
| **Quartet II** | **最低** | ✅ |

- Figure 4 显示，在所有模型大小和数据比例下，**Quartet II 始终优于所有基线**，尤其在过训练场景中优势更明显。
- Figure 5 表明 Nanochat 场景下，Quartet II 显著加快收敛并减小与 BF16 的差距。

---

### **消融实验结果**

#### ✅ **反向传播量化方式的影响（Figure 1）**
- 单独启用 MS-EDEN 在任一 GEMM 中均优于 SR；
- 完整启用 MS-EDEN（含权重再量化）甚至优于不启用再量化的 SR（即 Figure 1e > Figure 1d），说明其误差更低。

#### ✅ **前向传播策略影响（Figure 2）**
- “4/6” 技术在 native group scaling（1x16gs）下效果最佳，提升约两倍于 square-block 版本；
- native scaling + 4/6 成为最优前向组合。

#### ✅ **无偏性验证（Figure 9）**
- 平均量化梯度的误差随 accumulation steps 增加呈 **1/B 衰减趋势**，符合 CLT 预期，证明无偏；
- 而 “4/6 + SR” 组合出现 plateau，表明存在系统性偏差。

---

## **4. 关键结论和发现**

### **主要发现**
1. **传统 SR 并非最优解**：尽管广泛使用，SR 在 FP4 下引入过高方差；将随机性从元素级移至 scale 级可显著降低误差。
2. **MS-EDEN 实现更优权衡**：在保持无偏性的前提下，将量化误差降低 >2x，是目前最先进的无偏量化原语之一。
3. **前向与反向需协同设计**：
   - 放弃 square-block 可释放前向表示能力；
   - MS-EDEN 的优越性足以补偿再量化开销，并整体胜出。
4. **工程可行性已验证**：
   - 提出 **post-hoc range alignment** 技术避免重复旋转；
   - 实现高效 CUDA kernel，支持高达 **4.2x 速度提升**。

---

### **方法的局限性**
- **依赖 RHT 旋转**：要求 microscaling group 是 rotation group 的子集，限制了布局灵活性；
- **仅适用于内维度 GEMM**：目前主要用于 linear 层，attention 中的序列维度尚难直接应用；
- **额外 kernel 开销**：尽管做了融合优化，再量化仍带来一定延迟（约占总时间 13%，见 Table 7）；
- **微调阶段未受益**：由于短周期和小数据，SFT 阶段各 QAT 方法差异不显著。

---

### **未来工作方向**
1. **扩展至其他模块**：将 MS-EDEN 推广至 attention softmax、norm 层等非线性操作；
2. **探索混合精度策略**：部分层保留更高精度以进一步稳定训练；
3. **适配更多硬件平台**：除 Blackwell 外，支持 AMD 或其他支持 FP4 的芯片；
4. **动态 scale 选择机制**：结合 4/6 与 MS-EDEN，开发联合优化的 adaptive quantization policy；
5. **理论深化**：分析有限维度下 EDEN-style 方法的收敛速率边界。

---

> ✅ **代码开源地址**：[https://github.com/IST-DASLab/Quartet-II](https://github.com/IST-DASLab/Quartet-II)  
> ✅ **核心思想一句话总结**：*通过将无偏随机性从 FP4 元素转移到 FP8 scale，并结合旋转与校正，MS-EDEN 实现了比 SR 更低误差的梯度估计，使 NVFP4 全量化训练首次接近 BF16 精度水平。*

</details>

---

### 9. [From Self-Evolving Synthetic Data to Verifiable-Reward RL: Post-Training Multi-turn Interactive Tool-Using Agents](https://arxiv.org/abs/2601.22607)

**Authors**: Jiaxuan Gao, Jiaao Chen, Chuyi He, Wei-Chen Wang, Shusheng Xu, Hanrui Wang, Di Jin, Yi Wu  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22607v1  

#### Abstract
Interactive tool-using agents must solve real-world tasks via multi-turn interaction with both humans and external environments, requiring dialogue state tracking, multi-step tool execution, while following complex instructions. Post-training such agents is challenging because synthesis for high-qua...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*From Self-Evolving Synthetic Data to Verifiable-Reward RL: Post-Training Multi-turn Interactive Tool-Using Agents*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**多轮交互式工具使用智能体**（interactive tool-using agents）的后训练（post-training）难题，解决了以下两个核心瓶颈：
- **高质量合成数据难以规模化获取**：构建复杂的多轮、多步工具调用对话需要满足领域规则、模拟用户行为，并保证任务可解，人工标注成本极高，而自动化合成易产生无效或低质数据。
- **强化学习（RL）信号噪声大**：在交互环境中进行RL训练需依赖用户模拟器（user simulator），但现有开源模型在模拟会调用工具的用户时行为不稳定，导致rollout失败率高，引入错误的负反馈信号，严重损害训练效率。

### 提出的新方法与新思路
作者提出一个统一框架，结合**自演进合成数据引擎**与**基于验证器的强化学习**：
- **EigenData**：一个分层的、自我演进的多智能体系统，用于生成高质量、可验证的多轮工具使用对话数据。
  - **分层架构**：由**编排层**（orchestration layer）设计工作流、优化提示词并驱动迭代；**执行层**（execution layer）由多个专用worker agent完成具体的数据生成与验证任务。
  - **闭环自演进机制**：通过Judge Agent对生成的数据进行多维度批判（executability, tool correctness, coherence等），并将反馈用于更新提示词和工作流，实现数据质量、覆盖度和难度的持续提升。
  - **生成可执行验证函数**：为每个训练实例生成`verification function`，用于后续RL中的outcome reward计算，确保奖励信号基于最终状态而非对话文本，增强鲁棒性。
- **稳定化的RL训练流程**：
  - **用户模型微调（User Model Fine-tuning）**：先对用户模拟器进行SFT，使其能稳定遵循指令并正确调用工具，避免因用户端错误导致的虚假失败。
  - **GRPO + 动态过滤**：采用Group Relative Policy Optimization（GRPO）算法，使用**轨迹级组相对优势**（trajectory-level group-relative advantages）进行策略优化，并引入**动态过滤**（dynamic filtering）机制，剔除所有轨迹均成功或失败的任务，保留具有区分性的训练样本。

### 相比现有方法的优势
- **无需昂贵的人工标注**：完全依赖自演进的合成数据，显著降低数据获取成本。
- **数据质量更高且可验证**：通过多阶段验证（可行性、工具正确性、一致性）和修复机制，确保生成数据真实有效。
- **RL训练更稳定高效**：通过用户模型SFT和动态过滤，大幅减少噪声信号，使RL能真正从agent行为中学习。
- **全流程开源**：代码与数据已开源（[GitHub](https://github.com/inclusionAI/AReaL)），具备高度可复现性。

---

## 2. 核心实验方法和设置

### 数据集
- **主基准**：**T2-bench**（Yao et al., 2024; Barres et al., 2025），一个面向真实场景的多轮工具使用评测基准，包含三个领域：
  - **Airline**：航班预订、改签、客服
  - **Retail**：电商订单管理、商品咨询
  - **Telecom**：移动套餐管理、账单查询
- 每个领域提供真实的工具API、数据库状态和政策约束，要求agent在与模拟用户的多轮对话中正确执行多步操作。

### 实验设置与评估指标
- **模型基础**：基于 **Qwen3-MoE** 系列模型，主要使用 `Qwen3-30B-A3B` 和 `Qwen3-235B-A22B`。
- **训练方式**：
  - **SFT**（Supervised Fine-Tuning）：使用EigenData生成的合成数据进行监督微调。
  - **RL**：在SFT基础上应用GRPO进行强化学习。
- **用户模拟器**：
  - **训练时**：使用经过SFT微调的开源模型作为用户模拟器。
  - **评估时**：统一使用 **GPT-4.1** 作为用户模拟器，以保证公平比较。
- **评估指标**：
  - **pass^k**：k次独立尝试全部成功才计为1，衡量**一致性与可靠性**（比pass@k更严格）。
  - 报告 `pass^1`, `pass^2`, `pass^3`, `pass^4` 及 `pass@4`。
- **训练基础设施**：使用 **AReaL** 框架，在64–80块H200 GPU上进行异步大规模RL训练。

### 基线方法对比
- **开源模型**：Qwen3-30B-A3B系列的不同训练阶段（Baseline, +SFT, +RL）。
- **前沿闭源模型**：
  - Qwen3-Max-Thinking
  - Deepseek-v3.2
  - GPT-5
  - Claude-Sonnet-4.5
  - Gemini 3.0 Pro

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 1）
在 **Separate Training** 设置下，最佳模型表现如下：

| 领域       | 模型                         | pass^1 (%) |
|------------|------------------------------|------------|
| **Airline** | Qwen3-235B-A22B-2507 (+RL)    | **73.0**   |
| **Retail**  | Qwen3-235B-A22B-2507 (+RL)    | **75.0**   |
| **Telecom** | Qwen3-235B-A22B-2507 (+RL)    | **98.3**   |

- 在Airline上**持平Gemini 3.0 Pro**（73.0），**超越GPT-5**（62.5）和Claude（70.0）。
- 在Telecom上**达到当前最优水平**，超越所有对比模型。
- Retail仍是挑战最大的领域，Claude Sonnet领先（86.2），本方法达75.0。

### 与基线方法的对比结果
- **SFT即带来巨大提升**：
  - 例如在Telecom上，Qwen3-30B-A3B从27.1% → 80.7% pass^1。
- **RL进一步显著提升性能与一致性**：
  - Telecom上pass^1从85.4% → 95.6%，pass^4从70.8% → 86.0%，说明RL增强了模型的稳定性。
- **混合训练（Mix Training）效果更优**：
  - 在Table 2中，Qwen3-235B-A22B-2507经混合训练后，**平均pass^1达81.3%**，超过Qwen3-Max-Thinking（80.7%）和GPT-5（80.0%）。
  - 平均pass^4达68.5%，也优于Qwen3-Max-Thinking（66.8%）和GPT-5（64.0%）。

### 消融实验结果

#### （1）数据消融（Table 3，Airline领域）
| 数据来源 | pass^1 (%) |
|---------|------------|
| Human Expert（人工设计） | 52.0 |
| **SEED Full System**（完整EigenData） | **56.0** |
| w/o. Validation（无验证） | 50.0 |
| w/o. Evolution（无自演进） | 44.0 |
| 仅4套prompt（低多样性） | 42.5 |

✅ 结论：**数据质量（验证）与多样性（多prompt集）对性能至关重要**，且自动化的EigenData可超越人工设计。

#### （2）用户模型消融（Figure 3，Telecom领域）
- 使用**未经SFT的用户模型**进行RL训练：性能从SFT的85.4% **下降至75.6%**。
- 使用**SFT后的用户模型**：性能**提升至95.6%**。
- ✅ 结论：**用户模拟器的质量是RL成功的关键**，否则会因用户错误惩罚正确的agent行为。

#### （3）算法消融（Table 4，Airline领域）
- **Batch Size**：总batch size从256增至512，pass^1从~65% → **70.5%**，表明更大的batch带来更稳定的GRPO优势估计。
- **Dynamic Filtering**：关闭该机制，pass^1从70.5% → **65.0%**，pass^4从52.0% → 40.0%。
- ✅ 结论：**大batch size** 和 **动态过滤** 是稳定RL训练的重要组件。

---

## 4. 关键结论和发现

### 主要发现
1. **自演进合成数据（EigenData）可媲美甚至超越人工标注数据**，且具备高度可扩展性。
2. **基于验证器的outcome reward + GRPO + 动态过滤** 构成了稳定高效的RL训练范式。
3. **用户模拟器的质量直接影响RL成败**，必须通过SFT确保其行为可靠。
4. 该框架能在**全开源模型**上达到或超越前沿闭源模型的性能，证明了**开放生态在复杂agent训练上的潜力**。

### 方法的局限性
- **依赖高质量的基础模型**：若初始LLM能力不足，合成数据质量和RL效果可能受限。
- **领域迁移成本**：虽然声称“domain-agnostic”，但仍需提供领域文档和工具schema，跨领域泛化能力未充分验证。
- **计算资源需求高**：大规模RL训练需数十至上百张高端GPU，对一般研究者门槛仍高。

### 未来工作方向
- 探索更轻量化的自演进机制，降低计算开销。
- 将框架扩展到更多现实世界场景（如医疗、金融）。
- 研究如何让agent主动发现并利用新工具（open-world tool discovery）。
- 加强安全与可控性机制，防止工具滥用。

--- 

> **总体评价**：本文提出了一条**可扩展、可复现、高性能**的路径，用于训练复杂的多轮交互式工具使用智能体。其核心思想——**用自演进合成数据解决数据瓶颈，用可验证奖励和稳定化RL解决训练噪声**——为下一代智能体系统的发展提供了重要范式。

</details>

---

### 10. [Gaussian Process Bandit Optimization with Machine Learning Predictions and Application to Hypothesis Generation](https://arxiv.org/abs/2601.22315)

**Authors**: Xin Jennifer Chen, Yunjin Tong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22315v1  

#### Abstract
Many real-world optimization problems involve an expensive ground-truth oracle (e.g., human evaluation, physical experiments) and a cheap, low-fidelity prediction oracle (e.g., machine learning models, simulations). Meanwhile, abundant offline data (e.g., past experiments and predictions) are often ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Gaussian Process Bandit Optimization with Machine Learning Predictions and Application to Hypothesis Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**昂贵反馈下的连续空间优化问题**，尤其是在科学假设生成、工程设计等场景中，存在以下挑战：
- **Ground-truth oracle（真实反馈）成本高昂**：如人类评估、物理实验等，难以频繁调用。
- **廉价但有偏的预测模型可用**：如机器学习模型、大语言模型（LLMs），能快速提供低保真度（low-fidelity）预测，但可能存在系统性偏差。
- **已有大量离线数据**：可用于预训练预测模型并构建先验知识。

传统方法如 Vanilla GP-UCB 仅依赖昂贵的真实反馈，样本效率低下；而多保真度（multi-fidelity）方法通常需在不同保真度之间权衡查询成本，不适用于“预测近乎免费”的场景。

---

### 提出的新方法：PA-GP-UCB
作者提出 **Prediction-Augmented Gaussian Process Upper Confidence Bound (PA-GP-UCB)**，一种结合高成本真实反馈、低成本预测与离线数据的贝叶斯优化算法。

#### 核心思想与创新：
1. **双阶段框架**：
   - **Offline 阶段**：利用离线数据或主动查询预测 oracle，在空间填充设计（space-filling design）上获取大量廉价的 `f_ML` 预测值，用于构建强先验。
   - **Online 阶段**：每轮同时查询真实 oracle 和预测 oracle，利用两者相关性进行联合建模。

2. **Control-Variates Estimator（控制变量估计器）**：
   - 引入一个基于 GP 后验的偏差校正机制：
     $$
     \hat{\mu}(x) = \mu^{\text{true}}(x) - \rho(x)\frac{\sigma^{\text{true}}(x)}{\sigma^{\text{ML}}(x)}(\mu^{\text{ML}}(x) - f^{\text{ML}}(x))
     $$
   - 利用预测残差来修正真实函数的后验均值，实现**方差缩减与偏差校正**。

3. **Joint Multi-task GP Prior**：
   - 将真实函数 `f_true` 和预测函数 `f_ML` 视为两个相关任务，通过 multi-task GP 建模其协方差结构，显式捕捉二者之间的统计依赖关系。

---

### 相比现有方法的优势
| 方法 | 局限性 | PA-GP-UCB 的改进 |
|------|--------|------------------|
| **Vanilla GP-UCB** | 忽略所有预测信息，收敛慢 | 显著提升样本效率 |
| **Naive Prediction-Augmented GP-UCB** | 直接融合预测作为先验，无法纠正局部偏差 | 使用 control-variates 动态纠偏，避免陷入错误区域 |
| **MLA-UCB (Ji et al., 2025)** | 仅适用于离散动作空间 | 支持连续空间，并利用结构化关系 |
| **Multi-fidelity Bandits** | 关注成本分配，不减少高保真查询数 | 在固定高保真预算下严格降低 regret |

> ✅ **理论优势**：在标准假设下，PA-GP-UCB 保持与 GP-UCB 相同的 $O(\sqrt{T d \gamma_T})$ 累积 regret 上界，但**前导常数更小**，且该常数由预测质量（相关系数 $\rho$）和离线数据覆盖程度（$R$）显式控制：
> $$
> R_T \leq \sqrt{C \beta_T \gamma_T [1 - (1-R)\rho^2]} + \delta
> $$
> 当 $\rho > 0$ 且 $R < 1$ 时，性能严格优于 Vanilla GP-UCB。

---

## 2. 核心实验方法和设置

### 数据集
1. **合成基准（Synthetic Benchmarks）**：
   - 从 RBF 核的 GP 中采样真实函数 $f$。
   - 构造相关但有偏的预测函数：$f_{\text{ML}} = \rho f + \sqrt{1-\rho^2} g$，其中 $g$ 是独立 GP 样本。
   - 故意引入**局部反相关区域**（如 $[0.4, 0.6]$ 区间翻转符号），测试对预测误设的鲁棒性。

2. **真实世界假设生成任务（Real-world Hypothesis Generation）**：
   - 基于 Milkman et al. (2021) 的大规模行为实验数据集，包含 54 种干预措施及其对应的用户访问率（ground-truth reward）。
   - 构建两种环境：
     - **Finite-Arm Setting**：直接在 54 个离散假设上运行 bandit。
     - **Continuous Benchmark**：使用 TF-IDF + UMAP 将干预描述嵌入到二维语义流形上，并拟合 GP 得到连续奖励函数 $f_{\text{GP}}(x)$。

### 实验设置与评估指标
- **Horizon**: $T = 200$
- **重复次数**: 50 次独立运行取平均
- **评估指标**：
  - **累积 regret**: $R_T = \sum_{t=1}^T [f(x^*) - f(x_t)]$
  - **瞬时 regret 曲线**
  - **最优解定位精度**

### 基线方法对比
- **Vanilla GP-UCB**：仅使用真实反馈。
- **GP-UCB with offline prediction only**：仅将离线预测加入先验，无在线纠偏。
- **GP-UCB with offline + online prediction**：融合所有预测数据但不纠偏。
- **PA-GP-UCB variants**：不同离线数据量 $(M, N)$、不同预测相关性 $\rho$ 设置。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
#### （1）合成实验（图2）
| 方法 | 累积 regret ($T=200$) | 相对提升 |
|------|------------------------|----------|
| Vanilla GP-UCB | ~7.8 | — |
| GP-UCB + offline pred | ~6.5 | ~17% ↓ |
| GP-UCB + offline+online pred | ~6.3 | ~19% ↓ |
| **PA-GP-UCB ($\rho=0.8$)** | **~4.2** | **~46% ↓** ✅ |

- 即使在预测局部反相关的情况下，PA-GP-UCB 能够通过在线反馈动态纠偏，最终收敛至真实最优。
- 图1显示：PA-GP-UCB 的采样集中在真实最优附近，而 naive 方法被误导至预测偏好区。

#### （2）真实假设生成任务（图3）
##### Finite-Arm Setting（图3a）
- 预测模型与真实 reward 的经验相关性 $\rho = 0.66$
- PA-GP-UCB 在所有设置下均显著优于 Vanilla GP-UCB
- 随着离线预测副本数增加（$N=1 \to 100$），性能持续提升，表明离线数据有效降低了 posterior uncertainty

##### Continuous Setting（图3b）
- 使用 LLM（OpenAI o4-mini）作为预测 oracle
- 三种提示方式：
  - **Scale-only**：仅提供全局统计（min/max/mean）
  - **K-shot**：提供 $K=4$ 或 $12$ 个带标签示例
- 所有变体下 PA-GP-UCB 均大幅领先 Vanilla GP-UCB
- **Scale-only 表现最佳**：说明全局尺度信息即可有效引导探索，避免过拟合少量示例

> 📌 **关键发现**：即使预测质量中等（$\rho \approx 0.66 \sim 0.8$）、离线数据稀疏（$M=N=1$），PA-GP-UCB 仍能取得显著收益。

---

### 消融实验结果
#### （1）预测相关性 $\rho$ 影响（图2b）
- $\rho = 0.5$: 仍有明显增益
- $\rho = 0.9$: 性能进一步提升
- 表明方法对弱相关预测也具有鲁棒性

#### （2）离线数据规模影响（图2c）
- $M=1, N=1$（极小离线预算）已可带来性能提升
- 增加 $M, N$ 可进一步压缩 posterior variance，加速收敛
- 实际性能提升远超理论保守条件要求（公式5）

#### （3）噪声鲁棒性（附录图5）
- 在高 observation noise 下，PA-GP-UCB 相对 Vanilla GP-UCB 的优势更大
- 对 prediction noise 具有较强鲁棒性，即使 $\sigma^2_{\text{pred}} = 0.5$ 仍优于 baseline

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **PA-GP-UCB 显著提升样本效率**：在相同高保真查询预算下，相比 Vanilla GP-UCB 实现更低的累积 regret。
2. ✅ **control-variates estimator 有效纠偏**：能够在预测存在局部误导时仍正确收敛，避免“盲目信任”预测。
3. ✅ **离线数据是关键加速器**：即使是少量廉价预测也能显著改善 posterior 结构，尤其在数据稀疏区域。
4. ✅ **适用于现实假设生成流程**：在基于人类行为数据的真实任务中，成功识别出高质量干预策略（如“高频小额激励 + 提醒机制”最有效）。
5. ✅ **对核函数误设更具鲁棒性**：由于引入辅助信号，PA-GP-UCB 对 GP kernel 的选择不敏感，而 Vanilla GP-UCB 在非光滑 reward 下表现不稳定。

---

### 方法的局限性
1. **理论条件较保守**：所需离线数据密度 $(\epsilon, N)$ 条件过于严格，实际中远低于此也可奏效。
2. **依赖预测与真实函数的相关性**：若 $\rho \approx 0$，则增益有限。
3. **未考虑预测成本**：假设预测 oracle 完全免费，不适用于计算开销较大的模型（如大模型推理）。
4. **静态离线设计**：当前使用均匀 $\epsilon$-net，未来可扩展为自适应采样策略。

---

### 未来工作方向
1. **弱化离线数据假设**：研究更高效的 adaptive 或 non-uniform offline 设计，提升数据利用率。
2. **轨迹依赖的 regret 分析**：基于实际探索路径而非 worst-case uniform bound 进行分析。
3. **cost-aware offline sampling**：在预测也有成本时，优化 offline-online 查询权衡。
4. **扩展至大规模科学发现**：应用于药物设计、材料科学等领域，支持海量假设空间中的高效搜索。
5. **理论分析 kernel/prediction 误设下的鲁棒性**：建立更强的稳健性保证。

---

> 🔍 **总体评价**：  
> PA-GP-UCB 是首个将 **prediction-powered inference** 与 **Bayesian optimization** 在连续空间中有机结合的框架，为“人机协同发现”提供了**理论可证、实践高效**的新范式，特别适合于**数据稀缺、反馈昂贵、预测可用**的科学探索场景。

</details>

---

### 11. [Neural-Inspired Posterior Approximation (NIPA)](https://arxiv.org/abs/2601.22539)

**Authors**: Babak Shahbaba, Zahra Moslemi  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22539v1  

#### Abstract
Humans learn efficiently from their environment by engaging multiple interacting neural systems that support distinct yet complementary forms of control, including model-based (goal-directed) planning, model-free (habitual) responding, and episodic memory-based learning. Model-based mechanisms compu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Neural-Inspired Posterior Approximation (NIPA)》总结

## 1. 论文的主要贡献和创新点

### 解决的问题
贝叶斯推断（Bayesian inference）在复杂高维模型（如 Bayesian Deep Learning）中面临严重的**计算效率瓶颈**。传统的 MCMC 方法（如 HMC）虽然无偏，但计算代价高昂；而变分推断（VB）等近似方法虽快，却常引入偏差并低估不确定性。如何在保证准确性的前提下提升大规模贝叶斯推断的可扩展性，是当前的核心挑战。

### 提出的新方法与思路
本文提出了一种受神经科学启发的通用框架——**Neural-Inspired Posterior Approximation (NIPA)**，其核心思想是模拟人类大脑中三种互补的学习与决策机制来高效探索后验分布空间：

- **Model-Based (MB) 模块**：基于目标分布进行精确但缓慢的采样（类比“深思熟虑”的规划），使用标准 HMC 算法。
- **Model-Free (MF) 模块**：利用历史样本学习参数空间中的模式，构建代理函数（surrogate function）实现快速采样（类比“习惯性反应”），采用 autoencoder + DNN 构建低维空间下的 log-posterior 近似。
- **Episodic Control (EC) 模块**：通过检索最近的记忆（即已有的样本）直接复用其 log-posterior 值，实现“一次性”快速采样（类比“情景记忆”）。

NIPA 动态选择这三个模块之一执行采样，依据提议状态与已有样本池的距离决定激活哪个组件。

### 相比现有方法的优势
- **统一框架**：将 MCMC、VB、CES 等多种方法视为特例，实现了灵活集成。
- **高效性**：通过 MF 和 EC 模块大幅减少昂贵的目标函数及其梯度计算次数。
- **准确性保障**：保留 MB 模块确保对未知区域的稳健探索，避免陷入局部近似误差。
- **良好的不确定性量化（UQ）能力**：相比 VI、MC-Dropout 等方法，能更准确地估计预测区间覆盖概率（CP95）和校准误差（ECE）。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **回归任务**：
  - **合成数据集**：两层 ReLU 网络生成，含 5,000 个样本，100 维特征，约 3.5k 参数。
  - **Year Prediction MSD 数据集**：预测歌曲发布年份，含 515,345 个样本，90 维音频特征，模型参数达 309,761。
- **分类任务**：
  - **合成二分类数据集**：两层 ReLU 网络生成，20,000 样本，512 维特征，147,841 参数。
  - **MNIST 手写数字数据集**：奇偶数字分类任务，70,000 图像，784 维像素输入，217,538 参数。

所有数据均划分为固定训练/测试集用于公平比较。

### 实验设置与评估指标
- **采样数量**：每个算法生成 $ S = 2000 $ 个 posterior samples。
- **评估维度**：
  - **预测性能**：
    - 回归：**RMSE**（Root Mean Squared Error）
    - 分类：**Accuracy**
  - **不确定性质量**：
    - 回归：**CP95**（95% 预测区间的实际覆盖率）
    - 分类：**ECE**（Expected Calibration Error），越小越好
  - **计算效率与采样质量**：
    - **Wall-clock time (秒)**
    - **Effective Sample Size (ESS)**：最小、中位数、最大值，反映混合速度
    - **minESS/s**：衡量单位时间最慢方向的有效样本数
    - **Speedup**：相对于 BNN-HMC 的加速比

### 基线方法对比
- **确定性模型**：
  - DNN（无 UQ）
  - DNN-Ensemble（集成法提供经验不确定性）
- **贝叶斯基线**：
  - **MCMC 类**：BNN-HMC（基准）、BNN-SGHMC、BNN-pCN
  - **VI 类**：BNN-VI、BNN-MCD（MC-Dropout）、BNN-LASSO
  - **其他近似方法**：SWAG、BNN-RNS（Random Network Surrogate）

此外还进行了 NIPA 各模块单独运行的消融实验（NIPA-MB only, MF only, EC only）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 1 & 2）

| 方法 | 任务 | Time (s) | Speedup vs HMC | RMSE / Accuracy | CP95 / ECE | minESS/s |
|------|------|----------|----------------|------------------|-------------|-----------|
| **BNN-HMC (Baseline)** | Year Pred | 10,875 | 1.00 | 8.96 | 85.89 | 0.0053 |
| **NIPA (full)** | Year Pred | **1,225** | **6.99×** | 8.89 | **83.15** | **0.0370** |
| **BNN-HMC (Baseline)** | MNIST | 12,043 | 1.00 | 98.07 | 0.51 | 0.0051 |
| **NIPA (full)** | MNIST | **1,579** | **8.65×** | 98.01 | **0.39** | **0.028** |

> 注：Speedup 定义为 `(minESS/s)_NIPA / (minESS/s)_HMC`

### 与基线方法的对比结果
- **显著提速**：
  - 在 Year Prediction 上比 BNN-HMC 快 **6.99 倍**
  - 在 MNIST 上快 **8.65 倍**
  - 显著优于 SGHMC、pCN、SWAG 等主流方法
- **保持甚至提升预测性能**：
  - RMSE 与 HMC 相当或更优
  - 分类准确率接近最优水平
- **更好的不确定性量化**：
  - **CP95 更高**：表明预测区间更具包容性和可靠性
  - **ECE 更低**：说明模型输出的概率更加校准（well-calibrated）
- **采样效率更高**：
  - **minESS/s 远高于 HMC 及多数基线**，说明单位时间内获得的有效样本更多

### 消融实验结果
- **NIPA-MF only**：
  - 速度快（Speedup ≈ 7.99 on Year Pred），但 CP95 下降明显 → 表明仅依赖代理函数可能导致过度自信
- **NIPA-EC only**：
  - 极快（Speedup ≈ 15.66 on synthetic reg），但 ESS 很低 → 表明纯记忆检索缺乏全局探索能力
- **NIPA-MB only**：
  - 性能接近原始 HMC，但速度略慢（因额外开销）→ 验证了 MB 模块的稳定性
- **完整 NIPA (MB/MF/EC)**：
  - **综合表现最佳**：兼具高速度、高质量采样与良好 UQ → 证明三者协同作用至关重要

---

## 4. 关键结论和发现

### 主要发现
- **生物学启发有效**：借鉴人脑中 model-based、model-free 与 episodic memory 控制系统的分工协作机制，可以设计出高效的贝叶斯推理算法。
- **动态模块切换可行且高效**：通过距离阈值控制模块激活策略，能够在探索（exploration）与利用（exploitation）之间取得良好平衡。
- **NIPA 实现了速度与精度的双赢**：在多个真实与合成任务上，显著超越传统 MCMC 与 VI 方法，在保持 high minESS 的同时大幅提升计算效率，并改善不确定性校准。
- **适用于大规模 BNN 推断**：尤其适合参数量巨大、数据规模庞大的深度学习场景，解决了传统 HMC 难以落地的实际问题。

### 方法的局限性
- **超参数敏感性**：阈值 $ t_1, t_2 $ 需手动调节，缺乏自适应机制。
- **初始池依赖**：MF 模块的 surrogate 函数质量依赖于初始 SGHMC 生成的样本池。
- **代理模型泛化风险**：DNN surrogate 在远离训练区域可能失效，需 MB 模块兜底。
- **内存占用增加**：需存储样本池及 surrogate 模型，带来额外内存开销。

### 未来工作方向
- **自适应门控机制**：参考神经科学中的不确定性竞争机制 [17]，让系统根据各模块的置信度自动选择最优路径。
- **改进 surrogate 建模**：尝试更轻量级模型（如 ELM）、流形学习或 kernel 方法替代 DNN。
- **增强 EC 检索机制**：从最近邻扩展到 k-NN 加权估计，提高记忆利用率。
- **拓展至其他领域**：应用于 Gaussian Process、Bayesian Inverse Problems 等同样受限于计算成本的贝叶斯建模任务。
- **理论分析**：研究该框架的收敛性、偏差-方差权衡等理论性质。

> **总结一句话**：  
> NIPA 成功将认知神经科学原理转化为一种新型、高效、可扩展的贝叶斯推断框架，在不牺牲准确性的前提下，显著提升了 posterior sampling 的效率，为大规模 BNN 的实用化提供了有力工具。

</details>

---

### 12. [Matterhorn: Efficient Analog Sparse Spiking Transformer Architecture with Masked Time-To-First-Spike Encoding](https://arxiv.org/abs/2601.22876)

**Authors**: Zhanglu Yan, Kaiwen Tang, Zixuan Zhu, Zhenyu Bai, Qianhui Liu, Weng-Fai Wong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22876v1  

#### Abstract
Spiking neural networks (SNNs) have emerged as a promising candidate for energy-efficient LLM inference. However, current energy evaluations for SNNs primarily focus on counting accumulate operations, and fail to account for real-world hardware costs such as data movement, which can consume nearly 8...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Matterhorn: Efficient Analog Sparse Spiking Transformer Architecture with Masked Time-To-First-Spike Encoding  
—— 核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前 Spiking Neural Networks（SNNs）在用于大语言模型（LLM）推理时，虽然理论上具备高能效潜力，但**现有的能效评估多基于简化假设**，如仅统计 MAC 或 ACC 操作数，而忽略了实际硬件中占主导地位的**数据移动开销**（data movement），尤其是：
- **Spike 传输能耗**（跨核通信）
- **权重访问能耗**（SRAM 读取）

研究表明，在先进工艺下，这些数据移动成本可占总能耗的 **70%以上**，而传统评估方式严重低估了这部分开销。

---

### 🚀 提出的新方法与创新点

#### （1）**Masked Time-to-First-Spike (M-TTFS) 编码**
- **核心思想**：重新定义“静默状态”（all-zero spike train）所代表的值。
  - 传统 TTFS 将静默状态分配给最小膜电位（极少见的值），导致节能机会浪费。
  - M-TTFS 则将静默状态分配给**最频繁出现的激活值对应的时间步 `Imax`**，从而最大化稀疏性。
- 引入 **“死区”策略（Dead Zone Strategy）**：
  - 定义一个以 `Imax` 为中心、半径为 `k` 的时间窗口 `[Imax−k, Imax+k]`，在此区间内所有输入均被映射为静默状态。
  - 显著提升 sparsity，降低 spike movement 能耗。

> 💡 优势：无需牺牲信息完整性即可显著减少 spike 数量，且 spike 序列仍保持 deterministically 可解码。

#### （2）**Memristive Synapse Unit (MSU)** —— 基于 CIM 的模拟存算一体单元
- 采用 **Compute-in-Memory (CIM)** 架构，利用 **nT1R crossbar + RRAM** 实现模拟域向量矩阵乘法（VMM）。
- 所有权重驻留在内存中，避免了频繁的 SRAM 权重读取，**彻底消除 weight access 开销**。
- 支持 bit-serial 混合信号计算，兼顾精度与效率。

#### （3）算法-硬件协同设计（Algorithm-Hardware Co-design）
- M-TTFS 的高稀疏性直接转化为硬件层面的物理功耗门控（power gating）：零输入 → 零电流 → 零功耗。
- MSU 的模拟特性天然适配稀疏 spike 输入，实现真正的“按需计算”。

---

### 🔍 相比现有方法的优势
| 维度 | 传统 SNN 方法（如 SpikingLM, Sorbet） | Matterhorn |
|------|----------------------------------------|------------|
| 编码方式 | Rate / Standard TTFS | **M-TTFS + Dead Zone** |
| Spike Rate | 较高（5–33%） | 极低（最低达 **1.65%**） |
| 数据移动能耗 | 主导项（40–55%） | 大幅压缩（↓59%） |
| 权重访问 | 高频 SRAM 读取 | **CIM 消除访问开销** |
| 能效评估 | 忽略硬件细节 | 基于 **22nm 工艺实测建模** |
| 准确率 | 低于 BERT 基线 | 接近甚至超越同类 SNN |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **GLUE Benchmark** 中的 7 个任务：
  - **SST-2**（情感分类）
  - **QQP**, **MNLI-m**, **QNLI**, **RTE**, **MRPC**, **STS-B**
- 主要分析集中在 **SST-2** 上进行细粒度消融研究。

---

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| 模型架构 | BERT-base（教师模型）蒸馏得到 SNN |
| 时间步长 $T$ | 16（对应 4-bit 量化） |
| 量化方式 | 对称量化（symmetric quantization） |
| 训练方式 | QNN-to-SNN conversion + Knowledge Distillation |
| 死区半径 $k$ | 0, 1, 2, ...（控制 sparsity-accuracy trade-off） |
| 硬件建模 | 商用 **22nm 工艺**，Verilog 综合测量单元能耗 |
| 模拟 CIM 能耗 | 基于 Ye et al. (2023) 的物理 crossbar 宏模型 |

---

### 📊 评估指标
- **准确率（Accuracy）**：各 GLUE 子任务得分及平均分
- **能量消耗（Energy Consumption）**
  - 总能耗（mJ / transformer block）
  - 分项拆解：Spike Movement, Weight Access, Computing, Leakage
- **Spike Rate**：平均每个神经元产生的 spike 比例
- **能效比（Energy Efficiency Gain）**：相对于基线的节能倍数

---

### 🆚 基线方法对比
| 基线模型 | 类型 | 特点 |
|---------|------|------|
| **Spiking Otters** | SNN, TTFS | 当前最优 spiking transformer |
| **SpikingLM / Sorbet** | SNN, Rate Encoding | 高 spike rate，依赖操作计数节能 |
| **SpikingBERT** | SNN | 更大规模（50M 参数） |
| **Q2BERT / BiT** | Quantized ANN | 数字低比特模型作为能效参照 |
| **BERT-base** | Full-precision ANN | 准确率上限参考 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 和 Figure 7）

| 指标 | 结果 |
|------|------|
| **平均准确率（GLUE avg）** | **84.64%**（k=1）<br>**85.87%**（k=0） |
| **最高准确率 vs SOTA SNN** | **+1.42%** 超越 Spiking Otters（83.22%） |
| **单块 Transformer 能耗** | **6.14 mJ**（含 MSU） |
| **相比传统 TTFS 节能** | **2.7× 总体节能** |
| **相比 Spiking Otters 节能** | **57% 降低**（14.21 → 6.14 mJ） |
| **相比其他 SNN 模型** | <ul><li>比 SpikingLM 高 **4.24× 能效**</li><li>比 Sorbet 高 **6.98×**</li><li>比 SpikingBERT 高 **>13×**</li></ul> |
| **Spike Rate（SST-2）** | 从标准 TTFS 的 ~4.07% ↓ 至 **1.65%**（k=1） |
| **芯片面积** | 单 transformer block <10 mm²<br>整模型 **<120 mm²**（媲美现代 CPU/GPU） |

---

### 🔬 消融实验结果（Ablation Study）

#### （1）M-TTFS + Dead Zone 对 Spike 分布的影响（Figure 5）
- **标准 TTFS**：仅 0.26% 静默率，尖峰密集分布
- **M-TTFS (k=0)**：静默率达 **34.0%**
- **M-TTFS (k=1)**：静默率升至 **61.2%**
- **M-TTFS (k=2)**：达 **76.4%**

> ➤ 表明通过重定向静默状态，可系统性地压缩 spike 流量。

#### （2）能量-准确率权衡分析（Figure 6）
| 配置 | 能耗（mJ） | 准确率 | 相对节能 |
|------|-----------|--------|----------|
| Standard TTFS | 6.98 | 92.55% | - |
| M-TTFS (k=0) | 4.75 | 92.55% | ↓32% |
| M-TTFS (k=1) | **2.84** | 91.63% | ↓59% ✅ 最佳平衡点 |
| M-TTFS (k=2) | 1.80 | 88.42% | ↓74%，但精度下降明显 |

> ➤ **k=1 是最优配置**：在几乎无损准确率前提下实现最大节能。

#### （3）逐级优化的能量分解（Table 2）
| 方法 | Spike Mov. | Weight Acc. | Total Energy |
|------|------------|-------------|--------------|
| Baseline (TTFS) | 6.98 mJ | 4.65 mJ | 16.80 mJ |
| + M-TTFS | 4.75 mJ | 3.33 mJ | 12.24 mJ |
| + Dead Zone (k=1) | 2.84 mJ | 2.20 mJ | 8.31 mJ |
| + MSU | **2.84 mJ** | **0.69 mJ** | **6.14 mJ** |

> ➤ **MSU 单独带来约 2.17 mJ 的 weight access 节省**，是第二大节能来源。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **数据移动是 SNN 能耗瓶颈**：在真实硬件中，spike transfer 和 weight access 合计占比超 **70%**，远高于计算本身。
2. **编码方案应匹配数据分布**：将静默状态分配给高频值（而非极小值）可大幅提升稀疏性和能效。
3. **算法-硬件协同至关重要**：
   - M-TTFS 提供算法级稀疏性；
   - MSU 在硬件上将其转化为实际节能；
   - 二者结合形成正反馈循环。
4. **Matterhorn 实现 SOTA 平衡**：在 GLUE 上达到 **84.64% 平均准确率**，同时能耗仅为 **6.14 mJ/block**，**能效提升 2.31×**。

---

### ⚠️ 局限性
1. **依赖 QNN-to-SNN 转换框架**：未实现端到端训练，可能限制模型表达能力。
2. **死区参数 $k$ 需调优**：过大 $k$ 导致精度显著下降，需任务自适应选择。
3. **模拟噪声容忍度虽强但仍存在边界风险**：极端非理想条件下的鲁棒性有待实测验证。
4. **目前仅针对 BERT-like 架构**：是否适用于更大规模 LLM（如 Llama）尚待扩展。

---

### 🔮 未来工作方向
1. **支持动态调整死区范围 $k$**：根据输入序列复杂度自适应调节 sparsity。
2. **扩展至 Vision Transformer 和 Long-sequence Modeling**：探索在图像和长文本中的应用。
3. **构建完整 SoC 原型并流片验证**：推动从仿真到实物部署。
4. **探索异步事件驱动执行机制**：进一步释放 SNN 的 event-driven 潜力。
5. **结合脉冲剪枝（Spike Pruning）与通道稀疏化**：实现多层次稀疏优化。

---

> 🏔️ **命名寓意**：“Matterhorn” 不仅代表其高性能与稳定性，也象征着攀登类脑计算高峰的决心。该工作为构建**高精度、超低功耗的脉冲式 LLM 推理引擎**提供了坚实的技术路径。

</details>

---

### 13. [High-quality generation of dynamic game content via small language models: A proof of concept](https://arxiv.org/abs/2601.23206)

**Authors**: Morten I. K. Munk, Arturo Valdivia, Paolo Burelli  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.23206v1  

#### Abstract
Large language models (LLMs) offer promise for dynamic game content generation, but they face critical barriers, including narrative incoherence and high operational costs. Due to their large size, they are often accessed in the cloud, limiting their application in offline games. Many of these pract...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：High-quality generation of dynamic game content via small language models: A proof of concept

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **Large Language Models (LLMs)** 的动态游戏内容生成面临以下关键挑战：
- **Narrative incoherence**：LLMs 难以维持复杂虚拟世界中的叙事一致性，容易产生逻辑错误或脱离背景。
- **高成本与依赖云端**：LLMs 通常体积大、需云服务支持，导致单机游戏无法离线运行，且存在运营成本不可控、服务器停服风险等问题。
- **缺乏可控性**：LLMs 对提示词敏感，输出难以稳定控制，不利于游戏设计中对内容风格、结构的一致性要求。

### 🚀 提出的新方法与创新思路
本文提出一种基于 **Small Language Models (SLMs)** 的新型框架，核心思想为：
> **通过“激进微调”（aggressive fine-tuning）将复杂的叙事任务分解为多个狭窄、明确的子任务，每个由一个专门化的 SLM 处理，构建“代理式网络”（agentic network）**。

具体创新包括：
- **任务专用化 SLM 架构**：摒弃单一通用 LLM，转而使用多个 fine-tuned SLM，每个模型专注于特定生成任务（如辱骂性宣传语生成），实现更高的质量与稳定性。
- **DAG-based 合成数据生成**：采用有向无环图（Directed Acyclic Graph, DAG）结构自动生成训练数据，确保样本在游戏世界观内高度一致，并可精细控制输入变量组合。
- **Loop-anchored 内容生成范式**：将内容生成锚定于具体的游戏循环（game loop）中（如声誉战），使上下文更明确，降低复杂度，提升可预测性和同步性。

### ⚖️ 相比现有方法的优势
| 维度 | 传统 LLM 方法 | 本论文 SLM 方法 |
|------|----------------|------------------|
| **部署方式** | 依赖云端 API | 可本地部署，支持离线游戏 |
| **推理延迟** | 高（受网络影响） | 低（尤其量化后） |
| **成本控制** | 不可控（按 token 收费） | 固定（一次性训练） |
| **输出一致性** | 易漂移、难控制 | 高度可控，通过训练数据约束 |
| **叙事连贯性** | 弱（需复杂 prompt engineering） | 强（任务窄 + 数据结构化） |
| **模块化扩展性** | 单体系统，难拆分 | 支持构建 agentic network |

---

## 2. 核心实验方法和设置

### 🧪 实验目标
验证一个 **fine-tuned SLM 是否能在实时游戏约束下高质量完成整个 gameplay loop 的文本生成任务**。

### 🔧 模型与数据集
- **基础模型**：`Llama 3.2-1B`（10亿参数）
- **微调方法**：**LoRA**（Low-Rank Adaptation），prompt-loss weight 设为 5%
- **训练数据来源**：
  - 使用 `ChatGPT-4o` 作为 teacher model，通过参数化 prompt 自动生成 1800 个输入-输出对。
  - 输入包含角色元数据（faction, personality, appearance）、情报信息（failure, addiction）、修辞角度（angle）和目标受众（audience）。
  - 输出为符合中世纪设定的“抹黑海报”文案（≤500 字符）。
- **数据划分**：
  - 训练集：1440 条
  - 测试集：360 条

### ⚙️ 实验设置
- **量化级别**：测试三种量化版本以平衡速度与质量：
  - 16-bit（2.48 GB）
  - 8-bit（1.32 GB）
  - 4-bit（808 MB）
- **推理策略**：采用 **retry-until-success** 策略，温度 $ T=0.75 $，top-p=0.9
- **硬件平台**：消费级 PC（AMD Ryzen 9 7950X + NVIDIA RTX 3070 8GB VRAM），模型转换为 `.gguf` 格式并通过 `Llama.cpp` 部署

### 📊 评估指标
采用 **LLM-as-a-judge** 范式，由 `ChatGPT-4o` 对生成结果进行多维度评分，共七项标准：
1. **Overall assessment**：是否遵循指令
2. **Angle implementation**：是否成功体现指定修辞角度（如“血统卑劣”）
3. **Intelligence implementation**：是否完整融入所有情报信息
4. **Alignment**：是否符合中世纪设定，避免事实幻觉或不当语言
5. **Writing quality**：文笔是否流畅、风格统一
6. **Audience targeting**：是否针对指定群体（如士兵）调整语气
7. **Rhetorical targeting**：是否准确攻击目标人物

最终判断为 **min(judge_scores)** —— 所有项目均通过才算成功。

### 🆚 基线对比
- 主要对比不同量化等级下的 SLM 表现（16-/8-/4-bit）
- 黄金标准为 `ChatGPT-4o` 自身生成的训练数据（“Gold” LLM output）
- 未直接比较其他 SLM 或 LLM 方法，而是聚焦于证明该范式的可行性

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）整体成功率（Judge Pass Rate）
| 模型 | 成功率 | 标准误 |
|------|--------|--------|
| 16-bit | 92.5% | ±1.4% |
| 8-bit | 94.2% | ±1.2% |
| 4-bit | 78.0% | ±2.2% |

👉 **16-bit 与 8-bit 差异不显著**（McNemar’s test: $ \chi^2=0.69, p=0.41 $），表明 **8-bit 是理想的“即插即用”替代方案**。

#### （2）期望生成时间（Time-to-Success）
考虑 retry 机制后的总耗时（含加载 + 多次尝试）：

| 模型 | 中位数 | P95 | 最大值 |
|------|-------|-----|-------|
| 4-bit | **2.1 秒** | 3.4 秒 | 5.1 秒 |
| 8-bit | 2.5 秒 | 3.9 秒 | 6.1 秒 |
| 16-bit | 4.8 秒 | 7.5 秒 | **15 秒** |

📌 尽管 4-bit 模型需要更多尝试次数，但由于其 **极快的 per-token 推理速度（3.6ms/token vs 16ms）**，实际响应更快。

#### （3）生成效率分解（median）
| 组件 | 4-bit | 8-bit | 16-bit |
|------|------|------|--------|
| 加载时间 (t_prep) | 1145ms | 1397ms | 1782ms |
| 单次生成时间 (t_eval) | 1968ms | 2554ms | 4710ms |
| 平均尝试次数 | ~1.3 | ~1.1 | ~1.1 |

✅ 所有模型在大多数情况下可在 **5 秒内完成生成**，满足实时游戏掩码窗口需求。

#### （4）难度相关性分析
- 所有量化模型在 prompt 难度排序上具有强 **Spearman 相关性**（$ \rho > 0.82 $），说明它们“觉得难的输入”基本一致。
- 但在最难的 21 个 prompts 上，**4-bit 模型与其他两个的相关性骤降**（$ \rho < 0.4 $），显示出 **不同的失败模式**，可能源于过度压缩导致语义理解断裂。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **单个 fine-tuned SLM 可独立支撑完整 gameplay loop**  
   → 证明了 **SLM 作为 agentic network 基础组件的可行性**。

2. **激进微调 + 结构化输入可实现高质量生成**  
   → 在狭窄任务下，SLM 可达到接近 LLM 的输出质量，甚至媲美人类创作（参考文献[16]）。

3. **8-bit 量化是理想折中方案**  
   → 几乎无损质量的同时大幅减小内存占用（<1.5GB），适合集成进现代游戏引擎。

4. **retry-until-success 策略有效且可预测**  
   → 失败多为随机而非确定性崩溃，少量重试即可恢复，适用于实时系统。

5. **DAG-based 数据生成提供强大控制力**  
   → 可精确调控训练数据多样性与领域一致性，是实现“世界锚定”（world-grounded）生成的关键。

---

### ⚠️ 局限性
1. **缺乏本地质量评估机制**  
   → 当前依赖 `LLM-as-a-judge` 进行评估，仍需云端支持；未来需开发轻量级本地判别器（如 ensemble classifier）。

2. **泛化能力受限于训练数据范围**  
   → 模型对 out-of-vocabulary 输入（如虚构派系名称）表现下降，显示其学习的是表面模式而非深层世界知识。

3. **创造性与重复性之间的权衡**  
   → 结构化格式提升了可控性，但也导致输出形式较固定，缺乏开放生成的灵活性。

4. **仅验证了封闭式任务**  
   → 当前实验集中于“loop-anchored”场景；开放式任务（如自由对话、任务生成）尚未验证。

---

### 🔮 未来工作方向
1. **构建真正的 agentic network**  
   → 将多个 specialized SLM 组合，处理更复杂的叙事流程（如任务链、多角色互动）。

2. **发展本地 runtime quality assessment**  
   → 开发无需外部 LLM 的自动检测机制，实现完全离线闭环生成。

3. **增强世界知识建模能力**  
   → 探索结合 knowledge graph 或 grammar rules，让 SLM 学会隐式推理游戏规则与社会关系。

4. **拓展至多种文体生成**  
   → 从“抹黑海报”扩展到演讲、诗歌、歌曲等，丰富游戏表达形式。

5. **推动法律与伦理框架建设**  
   → 探索基于开源模型（如 Apertus）的端到端训练路径，保障艺术家权益与版权合规性。

---

## 总结
本论文提出了一个极具实践价值的范式转变：  
> **用一系列“专精小模型”取代“全能大模型”来驱动动态游戏叙事**。

通过 **DefameLM** 这一 proof-of-concept，作者成功展示了：
- SLM 在特定任务上可达到 LLM 级别的生成质量；
- 通过结构化训练与 retry 策略，可在消费级设备上实现实时、可靠的内容生成；
- 该方法为解决 LLM 在游戏中应用的 **成本、延迟、一致性、持久性** 四大难题提供了可行出路。

这不仅是技术上的突破，更是游戏 AI 架构思维的一次重构——从“黑盒调用”走向“导演式控制”，为下一代智能游戏奠定了坚实基础。

</details>

---

### 14. [Continual Policy Distillation from Distributed Reinforcement Learning Teachers](https://arxiv.org/abs/2601.22475)

**Authors**: Yuxuan Li, Qijun He, Mingqi Yuan, Wen-Tse Chen, Jeff Schneider, Jiayu Chen  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22475v1  

#### Abstract
Continual Reinforcement Learning (CRL) aims to develop lifelong learning agents to continuously acquire knowledge across diverse tasks while mitigating catastrophic forgetting. This requires efficiently managing the stability-plasticity dilemma and leveraging prior experience to rapidly generalize t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Continual Policy Distillation from Distributed Reinforcement Learning Teachers**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**

该论文聚焦于**持续强化学习（Continual Reinforcement Learning, CRL）**中的两个核心挑战：

- **灾难性遗忘（Catastrophic Forgetting）**：模型在学习新任务时会遗忘旧任务的知识，影响稳定性（stability）。
- **可塑性-稳定性困境（Stability-Plasticity Dilemma）**：如何在保持已有技能的同时高效学习新任务。

传统端到端的CRL方法在顺序任务流中面临样本效率低、优化困难、易遗忘等问题，尤其是在高容量模型（如Transformer）上难以稳定训练。

---

### 🚀 **提出了什么新方法或新思路**

作者提出了一种新颖的**教师-学生框架（teacher-student framework）**，将CRL解耦为两个独立过程：

1. **分布式RL教师训练（Distributed RL Teachers）**  
   - 每个任务由一个独立的RL worker并行训练出专家级策略（single-task teacher policy），使用PPO等标准RL算法。
   - 教师生成高质量的示范轨迹（demonstration trajectories）。

2. **持续策略蒸馏（Continual Policy Distillation）**  
   - 中心化的**学生模型（central student model）**通过模仿学习（imitation learning）不断从新到来的教师中蒸馏知识。
   - 学生采用基于**Transformer的Mixture-of-Experts（MoE）架构**，具备动态扩展能力。

#### 🔧 创新机制设计：
- **增量式专家扩展（Incremental MoE）**：每阶段新增一个专家模块，提升模型长期可塑性。
- **全局主干冻结（Global Backbone Freezing）**：第一阶段后冻结共享组件（如Embedding、Attention层），防止表征漂移。
- **两阶段专家掩码训练（Two-Phase Expert Masking）**：
  - Phase 1：冻结旧专家，仅训练新专家与门控网络 → 避免干扰历史知识。
  - Phase 2：冻结门控，解冻所有专家进行协同微调 → 提升整体性能。
- **多样性感知回放（Diversity-Aware Trajectory Replay）**：
  - 使用**Determinantal Point Process (DPP)** 从教师数据中选择最具多样性和代表性的轨迹加入回放缓冲区（replay buffer），占比 <10%，有效缓解遗忘。

此外，引入**对比任务嵌入（Contrastive Task Embedding）**替代固定one-hot ID，使模型能从状态序列中自动推断任务语义。

---

### ⚖️ **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **可扩展性** | 将复杂的CRL转化为稳定的监督学习任务，支持大规模并行训练教师模型。 |
| **稳定性** | 回放机制 + 参数掩码显著降低灾难性遗忘，实现正向后向迁移（positive BWT）。 |
| **可塑性** | MoE结构允许动态扩容，避免容量饱和导致的任务干扰。 |
| **兼容性** | 适用于现代大模型架构（如Transformer），而传统RL常因优化问题难以适配。 |

> 💡 **核心思想转变**：不再让单一agent直接与环境交互学多任务，而是“先分后合”——**分布训练 + 集中蒸馏**。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**

- **Meta-World MT25 Benchmark**
  - 包含25个不同的机器人操作任务（如 `peg insert`, `door open`, `button press` 等）。
  - 基于MuJoCo物理引擎，模拟Sawyer机械臂。
  - 所有任务共享相同的状态/动作空间，仅奖励函数不同。

---

### ⚙️ **实验设置**

#### 实验协议（Two Protocols）：
1. **Two-Stage Distillation**  
   - 第一阶段：学习前10个任务（MT10）
   - 第二阶段：继续学习剩余15个任务（MT25）

2. **Five-Stage Distillation（更难）**  
   - 分5轮，每轮学习5个新任务，逐步测试持续学习能力。

#### 学生模型架构：
- **Decoder-only Transformer**
- **MoE结构**：每层含多个FFN专家，稀疏激活（top-k routing）
- **Sequence Length**: 20
- **Task Embedding Dimension**: 16（通过InfoNCE损失训练）

#### 蒸馏训练细节：
- 使用**MSE Loss**匹配教师与学生的动作均值（不强制匹配方差，实验证明MSE优于KL散度）。
- 引入**Load Balancing Loss**确保专家负载均衡。
- 每阶段最多添加1个新专家。
- Replay Buffer大小控制在总蒸馏数据的<10%。

---

### 📊 **评估指标**

| 指标 | 定义 | 含义 |
|------|------|------|
| **Average Accuracy (Acc)** | $ \frac{1}{K}\sum_{k=1}^{K} a_{K,k} $ | 最终对所有任务的平均成功率 |
| **Backward Transfer (BWT)** | $ \frac{1}{K}\sum_{j=1}^{K}(a_{K,j} - a_{j,j}) $ | 衡量遗忘程度；正值表示无遗忘甚至有正迁移，负值表示遗忘 |

> 注：$ a_{k,j} $ 表示第 $ k $ 阶段结束后在任务 $ j $ 上的表现。

---

### 🔁 **基线方法对比**

| 基线 | 类型 | 描述 |
|------|------|------|
| **Finetune** | 下界 | 顺序微调，无防遗忘机制 |
| **Trac** | 优化器改进 | 参数免费优化器用于终身RL |
| **Independent** | 上界（可塑性） | 每阶段训练独立模型，无遗忘但无法共享知识 |
| **EWC** | 正则化法 | 基于Fisher矩阵保护重要参数 |
| **KL Constraint** | 函数正则化 | 限制策略分布偏移（常见于LLM） |
| **Replay Only** | 消融 | 仅用回放，无专家扩展 |
| **Expert Expansion Only** | 消融 | 仅扩展专家，无回放机制 |

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据**

#### ✅ 在 **Five-Stage Continual Learning (MT25)** 上的结果（Table 2）：

| 方法 | Final Acc (%) | BWT (%) |
|------|---------------|---------|
| **Ours (Proposed)** | **69.4 ± 1.5** | **+2.3 ± 2.8** |
| Independent | 62.8 ± 2.9 | — |
| Replay Only | 57.2 ± 1.4 | -11.0 |
| Expert Only | 28.0 ± 3.5 | -51.3 |
| EWC | 35.9 ± 17.3 | -37.9 |
| Finetune | 26.1 ± 2.6 | -55.3 |

> ✅ **结论**：我们的方法不仅达到最高准确率，且实现了**轻微正向BWT**，说明几乎没有遗忘，甚至存在跨任务促进效应。

---

#### ✅ 在 **Two-Stage Distillation (MT10 → MT25)** 上的表现（Figure 5a）：

- 学生模型恢复了超过 **85% 的教师性能**。
- 忘记程度被控制在 **10%以内**（即BWT > -10%）。
- 显著优于所有基线。

#### ✅ 与多任务RL方法对比（Table 1）：

| 方法 | Avg Accuracy (MT10) | 训练步数 |
|------|---------------------|----------|
| MOORE (SOTA MT-RL) | 88.7 ± 5.6 | ~20M env steps |
| **Central Model (Ours)** | **88.9 ± 1.9** | ~3M distill steps |
| **Avg Teachers** | **90.8 ± 1.4** | ~1M ×10 teacher steps |

> ✅ **亮点**：学生模型在远少于传统MT-RL的计算开销下，达到了接近教师集成的性能水平。

---

### 🔍 **消融实验结果（Ablation Study）**

| 变体 | Acc ↓ | BWT ↓ | 结论 |
|------|--------|--------|------|
| **No Freeze-Gating** | 69.4 → 64.5 | +2.3 → +0.8 | 冻结门控有助于维持路由稳定性 |
| **No Shared-Frozen** | 69.4 → 60.0 | +2.3 → +1.1 | 主干冻结对防止表征漂移至关重要 |
| **No TE (Task Embedding)** | 69.4 → 57.9 | — | 任务嵌入是MoE有效路由的前提 |
| **Replay Only** | 69.4 → 57.2 | +2.3 → -11.0 | 单靠回放不足以稳定学习 |
| **Expert Only** | 69.4 → 28.0 | +2.3 → -51.3 | 无回放会导致严重遗忘，结构扩展无效 |

> ✅ **关键发现**：**回放机制 + 专家扩展 + 参数掩码**三者缺一不可，构成完整抗遗忘体系。

---

## 4. **关键结论和发现**

### ✅ **主要发现**

1. **教师-学生范式有效解耦CRL难题**  
   - 分布式训练教师保证高质量知识源；
   - 集中式蒸馏实现稳定、高效的多任务聚合。

2. **MoE + 动态扩展 + 掩码调度 = 高可塑性与稳定性平衡**  
   - 新增专家吸收新任务知识；
   - 冻结旧参数保护历史能力；
   - 两阶段训练策略保障训练平稳。

3. **轻量级DPP回放即可大幅缓解遗忘**  
   - 仅需保留<10%的历史轨迹；
   - DPP选出多样化样本，比随机或FFS更优。

4. **对比任务嵌入捕捉任务语义**  
   - PCA可视化显示相似任务（如按钮按压、抽屉开关）自然聚类；
   - 支持零样本任务识别与正迁移。

5. **性能逼近联合蒸馏上限**  
   - 多阶段学习性能达联合训练（joint distillation）的90%以上；
   - 表明该框架具备良好扩展潜力。

---

### ⚠️ **局限性**

1. **依赖教师质量**  
   - 若某个教师未能学会任务（如 `disassemble-v3` 成功率为0），则学生也无法掌握。
   - 教师成为瓶颈。

2. **仅在Meta-World验证**  
   - 尚未在Atari、DMControl或其他RL基准上测试泛化性。

3. **离线蒸馏模式限制实时适应性**  
   - 不适合需要在线探索与即时反馈的应用场景。

4. **MoE带来额外工程复杂度**  
   - 路由不稳定、负载不均等问题仍需辅助损失（如auxiliary loss）调控。

---

### 🔮 **未来工作方向**

1. **引入自监督预训练增强初始表征能力**
2. **构建闭环系统：学生反哺教师进行迭代优化**
3. **扩展至视觉输入任务（如VPT-style）**
4. **探索更高效的MoE路由机制与稀疏训练策略**
5. **应用于真实机器人部署场景**

---

## ✅ 总结一句话

> 本文提出一种**解耦式教师-学生持续学习框架（Continual Policy Distillation）**，结合**MoE架构、增量扩展、DPP回放与参数掩码机制**，在Meta-World上实现了接近教师性能的持续学习效果，同时将遗忘控制在10%以内，为构建通用具身智能体提供了一条高效、稳定、可扩展的新路径。

</details>

---

### 15. [DRL-Enabled Trajectory Planing for UAV-Assisted VLC: Optimal Altitude and Reward Design](https://arxiv.org/abs/2601.22512)

**Authors**: Tian-Tian Lin, Yi Liu, Xiao-Wei Tang, Yunmei Shi, Yi Huang, Zhongxiang Wei, Qingqing Wu, Yuhan Dong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22512v1  

#### Abstract
Recently, the integration of unmanned aerial vehicle (UAV) and visible light communication (VLC) technologies has emerged as a promising solution to offer flexible communication and efficient lighting. This letter investigates the three-dimensional trajectory planning in a UAV-assisted VLC system, w...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DRL-Enabled Trajectory Planing for UAV-Assisted VLC: Optimal Altitude and Reward Design

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对 **UAV-assisted VLC**（无人机辅助可见光通信）系统中的三维轨迹规划问题，旨在最小化 UAV 的飞行距离以提升数据采集效率。该问题面临以下挑战：
- 传统方法缺乏基于 **VLC channel gain** 的理论最优飞行高度推导；
- 现有基于 **DRL** 的轨迹规划算法奖励机制设计未充分融合 VLC 物理层特性（如 Lambertian 辐射模型），导致收敛速度慢。

### 🚀 提出的新方法与创新思路
1. **理论推导最优飞行高度（Optimal Altitude）**  
   - 基于 VLC 信道增益模型，在给定信道增益阈值 $ H_{\text{th}} $ 下，首次提出一个**闭式解（closed-form）的最优飞行高度 $ h^* $**。
   - 该高度在保证通信可靠性的同时，最大化水平覆盖范围，从而减少 UAV 需要飞行的距离。

2. **设计新型信息素驱动奖励机制（Pheromone-driven Reward Mechanism）**  
   - 引入“reward pheromone”概念，当 UAV 进入 GU 的**信号可接收区域**（即使尚未满足通信门限）时即给予部分奖励。
   - 利用 $ \delta(d_{i,n}) $ 函数区分“仅能接收光信号”与“可建立有效通信”的区域，引导 UAV 向目标移动，缓解稀疏奖励（sparse reward）问题。

3. **改进 TD3 算法框架（Improved TD3）**  
   - 将上述奖励机制嵌入 Twin Delayed Deep Deterministic Policy Gradient（TD3）算法中，形成 **TD3-TDCTM** 算法。
   - 结合延迟策略更新与双 Critic 网络，增强训练稳定性。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **理论分析** | 首次给出基于 VLC 信道模型的最优高度闭式表达，而非依赖数值仿真或简化迭代。 |
| **算法效率** | 新奖励机制显著加快 DRL 收敛速度（约提升 50%）。 |
| **性能表现** | 所提方案在多种用户密度下均优于 SCAN、GREEDY-RRT 和 ACO-RRT 等基准方法。 |

---

## 2. 核心实验方法和设置

### 📊 数据集与环境配置
- **无公开真实数据集**，采用**自定义随机生成场景**进行仿真实验。
- 场景大小：$ D \times D = 100 \times 100 \, \text{m}^2 $
- Ground Users (GUs) 数量：$ I = 10, 15, 20, 25, 30 $，位置随机分布。
- UAV 起始点固定，任务为遍历所有 GUs 并完成数据收集。

### ⚙️ 实验设置
| 参数 | 设置值 |
|------|--------|
| Transmission power $ P $ | 10 W |
| Half-power angle $ \Phi_{1/2} $ | 60° |
| Receiver FOV half-angle $ \Psi_c $ | 60° |
| Detector area $ A $ | 1 cm² |
| Refractive index $ n_r $ | 1.5 |
| Noise std $ \sigma $ | 0.6 |
| Max flight speed / distance per slot $ d_{\max} $ | 2 m |
| Minimum safe altitude $ h_{\min} $ | 10 m |
| Channel capacity threshold $ C_{\text{th}} $ | 10 bps/Hz |
| Episode max length $ N_{\max} $ | 8000 slots |
| Batch size $ |B| $ | 256 |
| Discount factor $ \gamma $ | 0.999 |

### 🎯 评估指标
- **总飞行距离（Total flight distance）**：核心优化目标。
- **算法收敛速度（Convergence speed）**：达到稳定策略所需的时间步数。
- **任务完成率（Task completion rate）**：是否成功服务所有 GUs。
- **消融实验**：验证 reward pheromone 和 optimal altitude 的独立影响。

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **SCAN** | 扫描式路径规划，规则网格搜索。 |
| **GREEDY-RRT** | 快速探索随机树（RRT）结合贪心策略选择下一个目标。 |
| **ACO-RRT** | 使用蚁群优化（ACO）选择访问顺序 + RRT 规划路径。 |
| **Original TD3** | 不含 pheromone 奖励的标准 TD3 算法。 |

> 注：所有基线方法使用的飞行高度与所提方法一致（即也使用理论最优高度 $ h^* $），确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）最优飞行高度的有效性（图 2a）
- 存在一个使飞行距离最小的**最优高度 $ h^* \approx 13\,\text{m} $**。
- 在不同 GU 数量（10、15、20）下，最优高度基本不变 → 表明其由 $ m $ 和 $ H_{\text{th}} $ 决定，具有普适性。
- 相比非最优高度（如 10m 或 20m），飞行距离最多降低 **35%**。

#### （2）收敛性能提升（图 2c）
- **原始 TD3**：约需 4000 episodes 收敛。
- **改进 TD3（moderate reward）**：仅需约 **2000 episodes**，**收敛速度提升 ~50%**。
- 若 reward 过大，则出现剧烈震荡，说明需适度设计 pheromone 强度。

#### （3）飞行轨迹可视化（图 2b）
- 当 UAV 进入“信号可接收但未达标”区域（红圈与黑圈之间），开始加速逼近目标。
- 一旦进入红色通信可行区，迅速完成服务并转向下一目标 → 显示 reward pheromone 成功引导探索行为。

#### （4）总飞行距离对比（图 3）
| 方法 | 性能表现 |
|------|----------|
| SCAN | 飞行距离最长且不随用户数变化，效率低下。 |
| GREEDY-RRT | 距离随用户数近似线性增长。 |
| ACO-RRT | 表现较好，接近所提方法。 |
| **Proposed (TD3-TDCTM)** | **在所有用户数量下表现最优，相比 ACO-RRT 至少节省 22.3% 的飞行距离**。 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **存在理论最优飞行高度**：基于 VLC 信道模型可解析求得最优 $ h^* $，极大提升轨迹规划效率。
2. **reward pheromone 显著加速学习过程**：通过将物理层先验知识融入奖励函数，有效缓解稀疏奖励问题，收敛速度提升约 50%。
3. **联合优化 altitude 与 horizontal trajectory 可实现全局高效路径规划**：所提 TD3-TDCTM 框架在复杂环境中表现出强适应性和鲁棒性。

### ⚠️ 方法的局限性
- **假设理想 LOS 链路**：未考虑遮挡、天气、多径等实际因素对 VLC 信道的影响。
- **静态用户场景**：所有 GUs 固定不动，无法直接扩展至移动用户场景。
- **集中式训练**：依赖全局状态信息，可能限制大规模部署时的可扩展性。

### 🔮 未来工作方向
1. 扩展至 **动态用户环境** 下的在线轨迹规划。
2. 引入 **multi-UAV 协同机制**，研究分布式 DRL 架构。
3. 探索 **hybrid RF/VLC** 系统下的异构通信与轨迹联合优化。
4. 考虑能源约束（battery life）下的持久化任务调度。

--- 

> **总结一句话**：本文通过**理论推导最优飞行高度 + 设计物理感知的 pheromone 奖励机制**，显著提升了 DRL 在 UAV-assisted VLC 轨迹规划中的效率与性能，为智能空地协同通信提供了新范式。

</details>

---

### 16. [OSNIP: Breaking the Privacy-Utility-Efficiency Trilemma in LLM Inference via Obfuscated Semantic Null Space](https://arxiv.org/abs/2601.22752)

**Authors**: Zhiyuan Cao, Zeyu Ma, Chenhao Yang, Han Zheng, Mingang Chen  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22752v1  

#### Abstract
We propose Obfuscated Semantic Null space Injection for Privacy (OSNIP), a lightweight client-side encryption framework for privacy-preserving LLM inference. Generalizing the geometric intuition of linear kernels to the high-dimensional latent space of LLMs, we formally define the ``Obfuscated Seman...

---

### 17. [CVeDRL: An Efficient Code Verifier via Difficulty-aware Reinforcement Learning](https://arxiv.org/abs/2601.22803)

**Authors**: Ji Shi, Peiming Guo, Meishan Zhang, Miao Zhang, Xuebo Liu, Min Zhang, Weili Guan  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.22803v1  

#### Abstract
Code verifiers play a critical role in post-verification for LLM-based code generation, yet existing supervised fine-tuning methods suffer from data scarcity, high failure rates, and poor inference efficiency. While reinforcement learning (RL) offers a promising alternative by optimizing models thro...

---

### 18. [HetCCL: Accelerating LLM Training with Heterogeneous GPUs](https://arxiv.org/abs/2601.22585)

**Authors**: Heehoon Kim, Jaehwan Lee, Taejeoung Kim, Jongwon Park, Jinpyo Kim, Pyongwon Suh, Ryan H. Choi, Sangwoo Lee, Jaejin Lee  
**Category**: cs.DC  
**Published**: 2026-02-02  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.22585v1  

#### Abstract
The rapid growth of large language models is driving organizations to expand their GPU clusters, often with GPUs from multiple vendors. However, current deep learning frameworks lack support for collective communication across heterogeneous GPUs, leading to inefficiency and higher costs. We present ...

---

### 19. [Gradual Fine-Tuning for Flow Matching Models](https://arxiv.org/abs/2601.22495)

**Authors**: Gudrun Thorkelsdottir, Arindam Banerjee  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.22495v1  

#### Abstract
Fine-tuning flow matching models is a central challenge in settings with limited data, evolving distributions, or strict efficiency demands, where unconstrained fine-tuning can erode the accuracy and efficiency gains learned during pretraining. Prior work has produced theoretical guarantees and empi...

---

### 20. [When LLM meets Fuzzy-TOPSIS for Personnel Selection through Automated Profile Analysis](https://arxiv.org/abs/2601.22433)

**Authors**: Shahria Hoque, Ahmed Akib Jawad Karim, Md. Golam Rabiul Alam, Nirjhar Gope  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22433v1  

#### Abstract
In this highly competitive employment environment, the selection of suitable personnel is essential for organizational success. This study presents an automated personnel selection system that utilizes sophisticated natural language processing (NLP) methods to assess and rank software engineering ap...

---

### 21. [PerfGuard: A Performance-Aware Agent for Visual Content Generation](https://arxiv.org/abs/2601.22571)

**Authors**: Zhipeng Chen, Zhongrui Zhang, Chao Zhang, Yifan Xu, Lan Yang, Jun Liu, Ke Li, Yi-Zhe Song  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22571v1  

#### Abstract
The advancement of Large Language Model (LLM)-powered agents has enabled automated task processing through reasoning and tool invocation capabilities. However, existing frameworks often operate under the idealized assumption that tool executions are invariably successful, relying solely on textual d...

---

### 22. [Task-Aware LLM Council with Adaptive Decision Pathways for Decision Support](https://arxiv.org/abs/2601.22662)

**Authors**: Wei Zhu, Lixing Yu, Hao-Ren Yao, Zhiwen Tang, Kun Yue  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22662v1  

#### Abstract
Large language models (LLMs) have shown strong capabilities across diverse decision-making tasks. However, existing approaches often overlook the specialization differences among available models, treating all LLMs as uniformly applicable regardless of task characteristics. This limits their ability...

---

### 23. [TSPO: Breaking the Double Homogenization Dilemma in Multi-turn Search Policy Optimization](https://arxiv.org/abs/2601.22776)

**Authors**: Shichao Ma, Zhiyuan Ma, Ming Yang, Xiaofan Li, Xing Wu, Jintao Du, Yu Cheng, Weiqiang Wang, Qiliang Liu, Zhengyang Zhou, Yang Wang  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22776v1  

#### Abstract
Multi-turn tool-integrated reasoning enables Large Language Models (LLMs) to solve complex tasks through iterative information retrieval. However, current reinforcement learning (RL) frameworks for search-augmented reasoning predominantly rely on sparse outcome-level rewards, leading to a "Double Ho...

---

### 24. [Toward IIT-Inspired Consciousness in LLMs: A Reward-Based Learning Framework](https://arxiv.org/abs/2601.22786)

**Authors**: Hamid Reza Akbari, Mohammad Hossein Sameti, Amir M. Mansourian, Mohammad Hossein Rohban, Hossein Sameti  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22786v1  

#### Abstract
The pursuit of Artificial General Intelligence (AGI) is a central goal in language model development, in which consciousness-like processing could serve as a key facilitator. While current language models are not conscious, they exhibit behaviors analogous to certain aspects of consciousness. This p...

---

### 25. [MulFeRL: Enhancing Reinforcement Learning with Verbal Feedback in a Multi-turn Loop](https://arxiv.org/abs/2601.22900)

**Authors**: Xuancheng Li, Haitao Li, Yujia Zhou,  YiqunLiu, Qingyao Ai  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22900v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) is widely used to improve reasoning in multiple domains, yet outcome-only scalar rewards are often sparse and uninformative, especially on failed samples, where they merely indicate failure and provide no insight into why the reasoning fails. In ...

---

### 26. [SurrogateSHAP: Training-Free Contributor Attribution for Text-to-Image (T2I) Models](https://arxiv.org/abs/2601.22276)

**Authors**: Mingyu Lu, Soham Gadgil, Chris Lin, Chanwoo Kim, Su-In Lee  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22276v1  

#### Abstract
As Text-to-Image (T2I) diffusion models are increasingly used in real-world creative workflows, a principled framework for valuing contributors who provide a collection of data is essential for fair compensation and sustainable data marketplaces. While the Shapley value offers a theoretically ground...

---

### 27. [Riemannian Lyapunov Optimizer: A Unified Framework for Optimization](https://arxiv.org/abs/2601.22284)

**Authors**: Yixuan Wang, Omkar Sudhir Patil, Warren E. Dixon  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22284v1  

#### Abstract
We introduce Riemannian Lyapunov Optimizers (RLOs), a family of optimization algorithms that unifies classic optimizers within one geometric framework. Unlike heuristic improvements to existing optimizers, RLOs are systematically derived from a novel control-theoretic framework that reinterprets opt...

---

### 28. [SplineFlow: Flow Matching for Dynamical Systems with B-Spline Interpolants](https://arxiv.org/abs/2601.23072)

**Authors**: Santanu Subhash Rathod, Pietro Li\`o, Xiao Zhang  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.23072v1  

#### Abstract
Flow matching is a scalable generative framework for characterizing continuous normalizing flows with wide-range applications. However, current state-of-the-art methods are not well-suited for modeling dynamical systems, as they construct conditional paths using linear interpolants that may not capt...

---

### 29. [Behemoth: Benchmarking Unlearning in LLMs Using Fully Synthetic Data](https://arxiv.org/abs/2601.23153)

**Authors**: Eugenia Iofinova, Dan Alistarh  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.23153v1  

#### Abstract
As artificial neural networks, and specifically large language models, have improved rapidly in capabilities and quality, they have increasingly been deployed in real-world applications, from customer service to Google search, despite the fact that they frequently make factually incorrect or undesir...

---

### 30. [Real-Time Aligned Reward Model beyond Semantics](https://arxiv.org/abs/2601.22664)

**Authors**: Zixuan Huang, Xin Xia, Yuxi Ren, Jianbin Zheng, Xuefeng Xiao, Hongyan Xie, Li Huaqiu, Songshi Liang, Zhongxiang Dai, Fuzhen Zhuang, Jianxin Li, Yikun Ban, Deqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22664v1  

#### Abstract
Reinforcement Learning from Human Feedback (RLHF) is a pivotal technique for aligning large language models (LLMs) with human preferences, yet it is susceptible to reward overoptimization, in which policy models overfit to the reward model, exploit spurious reward patterns instead of faithfully capt...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
