# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-16 05:58:21 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FaTRQ: Tiered Residual Quantization for LLM Vector Search in Far-Memory-Aware ANNS Systems](https://arxiv.org/abs/2601.09985)

**Authors**: Tianqi Zhang, Flavio Ponzina, Tajana Rosing  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.09985v1  

#### Abstract
Approximate Nearest-Neighbor Search (ANNS) is a key technique in retrieval-augmented generation (RAG), enabling rapid identification of the most relevant high-dimensional embeddings from massive vector databases. Modern ANNS engines accelerate this process using prebuilt indexes and store compressed...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FaTRQ: Tiered Residual Quantization for LLM Vector Search in Far-Memory-Aware ANNS Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代 **Approximate Nearest-Neighbor Search (ANNS)** 系统在 **Retrieval-Augmented Generation (RAG)** 中广泛应用，用于从大规模向量数据库中快速检索相关嵌入。然而，尽管已有索引结构（如 IVF、CAGRA）和向量量化技术（如 PQ）将压缩后的向量存储在高速内存中以加速搜索，**第二阶段的精炼（refinement）仍需从慢速存储设备（如 SSD）读取全精度向量**。

随着现代文本和多模态嵌入维度的增长（如 768–1536 维），这一精炼阶段的 I/O 开销已成为整个查询延迟的主要瓶颈——**超过 90% 的时间消耗在从 SSD 读取全精度向量上**。

### 提出的新方法与核心思路
为解决该问题，作者提出 **FaTRQ**（**Far-memory-aware Tiered Residual Quantization**），一种面向远端内存（far memory）的渐进式距离估计框架，其核心思想是：

- **避免直接读取全精度向量**，转而通过分层残差量化（Tiered Residual Quantization, TRQ）将原始向量分解为：
  - **粗略量化码（coarse code）** 存于 **fast memory**（如 DRAM/VRAM）
  - **紧凑残差码（residual codes）** 存于 **far memory**（如 CXL 内存、SCM）
- 在查询时，采用 **渐进式距离估计（progressive distance estimation）**：
  - 初始使用粗略距离
  - 逐步流式加载残差码，增量更新距离
  - **一旦候选者可被证明不在 top-k 范围内，则提前剪枝**
- 引入 **三值化残差编码（ternary residual encoding）**：
  - 将残差方向编码为 {-1, 0, +1} 的稀疏三元向量
  - 支持乘法自由的距离计算（仅需加减法），降低计算开销
- 设计专用硬件加速器（部署于 **CXL Type-2 设备**）实现本地低延迟精炼，减少主机 CPU 和内存带宽压力

### 相比现有方法的优势
| 方面 | FaTRQ | 传统方法（如 cuVS、FAISS） |
|------|-------|-----------------------------|
| **精炼 I/O 成本** | 仅需读取几字节的残差码（CXL/far memory） | 需读取完整全精度向量（SSD） |
| **存储效率** | 残差码高度压缩（~1.6 bits/dim） | 全精度存储（32 bits/dim） |
| **计算模式** | 渐进式估计 + 早期剪枝 | 批量重排序所有候选 |
| **硬件支持** | 可结合 CXL 加速器实现近内存处理 | 依赖主机 CPU 进行精炼 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Wiki dataset**：88M 条 SBERT 多语言句子嵌入，768 维，共 251GB
- **LAION dataset**：100M 条 CLIP 图像-文本嵌入（来自 LAION-5B），768 维，共 286GB
- 查询集：各含 10k 查询，使用 Euclidean 距离作为相似度度量

### 实验设置
- **平台模拟**：
  - GPU 端：NVIDIA A10 GPU（24GB VRAM），运行 cuVS/FAISS 的索引遍历与粗略打分
  - 主机 CPU：40 线程 Intel Xeon Gold 6230，128GB DRAM
  - 存储：1TB SSD（延迟 45μs，吞吐 1200K IOPS）
  - 远端内存：基于 CXL 的存储类内存（latency 271ns，bandwidth 22GB/s），使用 Ramulator 模拟
- **硬件评估**：Verilog 实现 CXL Type-2 加速器，在 ASAP7 工艺下综合（1GHz）

### 评估指标
- **End-to-end throughput**（queries/sec）
- **Recall@10** at target levels (85%, 90%, 95%)
- **Refinement ratio**：实际参与最终排序的候选数占比
- **Distance estimation distortion**（MSE）
- **Area & Power overhead** of accelerator

### 基线方法对比
- **PQ-IVF-FAISS**：基于 FAISS 的 IVF+PQ 索引，精炼阶段从 SSD 加载全精度向量
- **PQ-CAGRA-cuVS**：基于 cuVS 的图索引 CAGRA + PQ，同样进行 SSD 回读精炼
- 对比版本：
  - FaTRQ-IVF-SW / FaTRQ-CAGRA-SW：软件实现，残差码存于 CXL 内存
  - FaTRQ-IVF-HW / FaTRQ-CAGRA-HW：硬件加速版本，精炼卸载至 CXL 加速器

---

## 3. 主要实验结果和性能指标

### 关键性能数据
#### ✅ 吞吐提升（Throughput Speedup）
| 方法 | Wiki-90 Recall | LAION-90 Recall |
|------|---------------|----------------|
| FaTRQ-IVF-HW | **9.4×** vs IVF-FAISS | **3.1×** vs IVF-FAISS |
| FaTRQ-CAGRA-HW | **4.9×** vs CAGRA-cuVS | **2.6×** vs CAGRA-cuVS |

> 注：IVF 更依赖大量候选重排序，因此 FaTRQ 带来的收益更大；CAGRA 本身剪枝更强，改进空间较小。

#### ✅ 存储效率提升
- FaTRQ 残差码存储开销：
  - 每个 768 维向量仅需 `(768 / 5) + 8 = 162 bytes`
    - 5 个三元值打包成 1 字节 → 平均 1.6 bits/dim
    - 加上两个预计算标量（`⟨xc, δ⟩`, `‖δ‖`）共 8 bytes
- 对比 4-bit Scalar Quantization (SQ)：
  - 需要 384 bytes（768 × 0.5 bytes）
  - MSE 相当（FaTRQ: 0.0159 vs SQ: 0.0134），但存储节省 **2.4×**

#### ✅ 精炼流量削减
- 在达到相同 Recall@10（99%）前提下：
  - 基线需访问 ~70 个全精度向量
  - FaTRQ 仅需访问 ~25 个 → **减少 2.8× SSD I/O**
- 图 8 显示，即使只对 top-30% 的 FaTRQ 排序结果执行全精度验证，也能显著优于无 FaTRQ 的完整列表重排

#### ✅ 距离估计准确性
- FaTRQ 的距离估计 MSE 仅为 **0.0159**，远低于 3-bit SQ 的 **0.258**
- 图 7 显示 FaTRQ 结果几乎贴合“oracle”线（即使用全精度残差的理想情况）

#### ✅ 硬件开销极低
- CXL 加速器总面积：**0.729 mm²**，功耗 **897 mW**
- FaTRQ 距离估计模块占 29% 面积、27% 功耗
- 相比完整的 CXL 控制器（如 Marvell Structera A2504，含 16 个 Neoverse V2 核心），面积与功耗开销分别 < **1.8%** 和 **4%**

#### ✅ 离线训练开销小
- 构建残差码 + 训练轻量校准模型：约 **10 分钟**
- 相比构建 CAGRA 索引所需 ~3 小时，可忽略不计

---

## 4. 关键结论和发现

### 主要发现
1. **ANNS 系统的瓶颈已从计算转向 I/O**：尤其在高维嵌入场景下，**SSD 上的全精度向量回读主导了查询延迟**。
2. **残差信息可以高效压缩并用于渐进式距离估计**：利用残差近似正交于查询偏移的特性，FaTRQ 实现了高精度、低开销的距离细化。
3. **三元编码 + 分层存储架构实现了极致压缩与高效计算**：无需重建原始向量即可完成高质量重排序。
4. **CXL Type-2 等远端内存设备非常适合部署此类近内存精炼逻辑**：既能提供大容量，又具备一定计算能力，大幅减少主机负担。

### 方法的局限性
- 当目标 recall 很高（如 95%）时，粗筛选难以有效剪枝，导致更多候选进入精炼阶段，FaTRQ 的优势有所减弱。
- 依赖预先构建的残差码和校准模型，对动态更新的数据集适应性有待验证。
- 目前评估基于静态嵌入数据库，未考虑实时插入或删除操作的影响。

### 未来工作方向
- 扩展到 **动态向量数据库** 场景，支持在线增量编码与更新
- 探索更复杂的 **multi-level residual quantization** 以进一步提高精度
- 将 FaTRQ 与 **Near-Data Processing (NDP)** 或 **Processing-In-Memory (PIM)** 架构结合，实现端到端近内存 ANNS
- 支持更多距离度量（如 cosine similarity）和量化方案

---

## 总结
**FaTRQ 是首个明确提出“远端内存感知”的 ANNS 精炼框架**，通过 **tiered residual quantization + ternary coding + progressive estimation** 的组合，成功将昂贵的 SSD 全精度读取转化为高效的 CXL/far memory 流式残差读取。其实验结果显示：
- **最高达 9.4× 的吞吐提升**
- **2.4× 的存储效率增益**
- **硬件开销极低，适合集成进 CXL 加速器**

该工作为下一代大规模向量搜索系统提供了重要的软硬协同设计范式，尤其适用于 LLM-driven RAG 应用中的高性能语义检索需求。

</details>

---

### 2. [TF3-RO-50M: Training Compact Romanian Language Models from Scratch on Synthetic Moral Microfiction](https://arxiv.org/abs/2601.10410)

**Authors**: Mihai Dan Nadas, Laura Diosan, Andreea Tomescu, Andrei Piscoran  
**Category**: cs.CL  
**Published**: 2026-01-16  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.10410v1  

#### Abstract
Recent advances in synthetic data generation have shown that compact language models can be trained effectively when the underlying corpus is structurally controlled and linguistically coherent. However, for morphologically rich and computationally under-resourced languages such as Romanian, there i...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TF3-RO-50M: Training Compact Romanian Language Models from Scratch on Synthetic Moral Microfiction

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**形态丰富且计算资源相对匮乏的语言——罗马尼亚语（Romanian）**，解决了以下关键挑战：
- **Tokenization Penalty**：通用多语言分词器（如 multilingual BPE）在处理 Romanian 时导致严重的子词碎片化，显著增加 token 数量（相比英文文本可高出 1.5–2×），降低上下文利用率并增加计算成本。
- **缺乏端到端训练框架**：此前尚无公开、可复现的完整流程，能够从零开始训练基于合成数据的 Romanian 小型语言模型（SLM），涵盖 tokenizer 设计、预训练、压缩、评估及新数据生成。
- **本地部署困难**：由于缺乏高效的小型模型，Romanian 用户面临高推理延迟和有限的本地部署能力。

### 提出的新方法与创新思路
TF3-RO 引入了一个**全合成、端到端的 Romanian 语言建模生态系统（pipeline）**，其核心创新包括：

| 创新模块 | 内容 |
|--------|------|
| **Romanian-specific Tokenizer** | 构建了专为 Romanian 形态优化的 SentencePiece BPE 和 Unigram 分词器，特别是 32k Unigram 在形态保真度上优于通用多语言方案。 |
| **从头预训练（from-scratch pretraining）** | 使用约 10 亿个 Romanian tokens，在一个 51.65M 参数的 LLaMA-style Transformer 上进行完全从头训练，不依赖任何预训练检查点（如 mBERT 或 XLM-R）。 |
| **结构化剪枝指导蒸馏（pruning-guided knowledge distillation）** | 先通过 structured pruning 探索最小可行容量，再以此指导设计学生模型架构，并采用 logit-based KD 进行压缩，得到仅 26.45M 参数的学生模型。 |
| **合成数据引擎反哺训练** | 使用蒸馏后的学生模型作为生成器，通过组合提示（combinatorial prompting）框架生成新的 300 万条 Romanian 原生道德寓言（fables），形成闭环。 |
| **综合评估套件（comprehensive evaluation suite）** | 集成多种评估范式：<br>• **内在指标**（Perplexity, CE）<br>• **语法探针**（morphosyntactic agreement）<br>• **实体连贯性分析**（entropy-based entity coherence）<br>• **规则语法检测**（LanguageTool）<br>• **LLM-as-a-judge**（Gemini 2.5 Flash）<br>• **多样性/可读性指标**（Distinct-n, Self-BLEU, Flesch Reading Ease） |

### 相比现有方法的优势
- **语言适配性强**：相比直接使用 multilingual tokenizer + 英文主导架构的方法，TF3-RO 显著缓解了 Romanian 的 token 膨胀问题，提升上下文效率。
- **全流程可控与可复现**：所有组件（tokenizer、模型、数据生成）均基于同一受控合成语料（TF2-RO），确保一致性与可复现性。
- **轻量化与部署友好**：最终 26.45M 模型支持 CPU/移动端部署，适合资源受限环境。
- **闭环生态构建**：不仅训练模型，还利用模型生成高质量新数据，推动 Romanian NLP 发展。

---

## 2. 核心实验方法和设置

### 数据集
- **主训练数据**：`TF2-RO`，即 TF1 英文寓言经高质量机器翻译得到的 Romanian 版本，共 **300 万条道德微小说（moral microfiction）**，保留原始六槽叙事结构（character, setting, challenge, resolution, moral）。
- **Tokenizer 训练数据**：TF2-RO 的 Romanian 子集，保留 diacritics，不做激进归一化。
- **评估数据**：
  - 新构建的 **100 条 Romanian 控制生成提示集**，独立于训练/蒸馏数据；
  - 所有评估数据遵循相同叙事模板，保证分布一致性。

### 实验设置
| 组件 | 设置 |
|------|------|
| **Tokenizer** | BPE vs. Unigram，vocab size = 32k，deterministic segmentation |
| **序列打包** | Long-sequence packing into fixed 2,048-token blocks，减少填充开销 |
| **Teacher Model** | LLaMA-style Decoder-only Transformer<br>• 6 layers<br>• hidden size = 512<br>• MLP intermediate = 1365<br>• 8 attention heads (head dim = 64)<br>• RoPE positional embedding<br>• tied input/output embeddings<br>• total params = 51.65M |
| **Student Model** | 基于剪枝分析设计：<br>• hidden size → 384 (-25%)<br>• MLP intermediate → 1024<br>• attention heads → 6 (-25%)<br>• depth 不变<br>• total params = 26.45M |
| **Knowledge Distillation** | Loss = α·KL(p_T || p_S) + β·CE(y, p_S)，其中 α=1.0, β=0.1 |
| **Quantization Baselines** | Transformer-Q8, Transformer-Q6（post-training quantization） |
| **Baseline Architecture** | 参数匹配的 Mamba 模型（50M params），用于对比 SSM 与 Transformer 在 Romanian 下的表现 |

### 评估指标
| 类别 | 指标 |
|------|------|
| **Intrinsic** | Cross-Entropy (CE), Perplexity (PPL) |
| **Grammatical Accuracy** | Romanian-specific morphosyntactic agreement probes（subject-verb, noun-adjective 等） |
| **Grammar Checking** | LanguageTool 错误率标准化得分 |
| **Narrative Coherence** | Lemmatized entity entropy（衡量角色使用平衡性） |
| **Fluency & Coherence** | LLM-as-a-judge（Gemini 2.5 Flash）评分（0–100） |
| **Diversity** | Self-BLEU, Distinct-1/2 |
| **Readability** | Romanian-calibrated Flesch Reading Ease |
| **Efficiency** | Tokens/sec throughput（MLX backend）, model size (MB) |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | Params | PPL | Entity Coherence | LanguageTool Score | LLM Judge (Mean) | Throughput (tok/s) |
|------|-------|-----|------------------|--------------------|-------------------|---------------------|
| **Transformer (Teacher)** | 51.65M | ~2.43 | 0.88 | ~0.99 | ~4.19 | 159.5 |
| **Distilled Student** | 26.45M | ↑ ~2.7 | 0.88 | ~0.97 | ~4.00 | ↑ **257.7** |
| **Transformer-Q8** | 51.65M | ~2.44 | 0.88 | ~0.99 | ~4.18 | ↑ 191.1 |
| **Transformer-Q6** | 51.65M | ↑↑ 3.0+ | ↑↑ **0.94** | ↓↓ ~0.88 | ↓↓ ~3.5 | ↑ 199.1 |
| **Mamba (50M)** | 50M | ~2.5 | 0.88 | ~0.98 | ~4.0 | ↑↑ **~300+** (estimated) |

> 注：以上数据来自 Figure 2 及正文描述，部分为估算值。

### 与基线方法的对比结果
- **vs. 大型指令调优模型（LLaMA-3 8B Instruct, GPT-4.1-mini 等）**：
  - 尽管参数规模小两个数量级（51.65M vs >8B），TF3 Transformer 在 LLM-as-a-judge 评估中仍能达到这些强基线的 **~90% 表现水平**（平均分 4.19 vs ~4.7），表明合成数据足以支撑高质量 Romanian 生成。
- **vs. Mamba 架构**：
  - Transformer 在 **内在建模质量（PPL）和语法正确性** 上略优；
  - Mamba 在 **吞吐量** 上具有优势（尤其在 MLX 后端），但 TF3 主管道仍以 Transformer 为主以保持架构一致性。

### 消融实验结果
#### （1）Tokenizer 效率对比（Table 1）
| Tokenizer | Avg Tokens/Sent. | Median |
|----------|------------------|--------|
| Romanian BPE (32k) | 304.89 | 304 |
| Romanian Unigram (32k) | **340.35** | **339** |

- **发现**：Unigram 并未减少 token 数，反而更长；但其优势在于**形态分割更合理**（更好保留词干和屈折后缀），提升训练稳定性与生成质量。

#### （2）结构化剪枝分析（Appendix A）
- 最大可接受剪枝配置：**50% MLP neurons + 30% attention heads**，对应损失上升 ~26–27%。
- 此配置成为学生模型设计依据，避免盲目缩小宽度导致崩溃。

#### （3）压缩路径比较（Figure 2）
- **Q8 量化**：几乎无损，是部署首选；
- **Q6 量化**：虽吞吐高、entity coherence 高（因重复生成），但语法错误增多，流畅度下降；
- **蒸馏学生模型**：在大幅减参下保持稳定句法结构，适合用于后续数据生成任务。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **合成数据足以训练高质量 Romanian SLM**：即使完全基于机器生成的 moral microfiction，也能让 51.65M Transformer 实现稳定收敛与语法连贯生成。
2. ✅ **语言特定 tokenizer 至关重要**：虽然 Romanian Unigram 未减少 token 数，但其**形态保真度更高**，对下游建模质量有积极影响。
3. ✅ **结构化剪枝可用于指导学生模型设计**：无需大量试错，即可确定“最小可行容量”，使知识蒸馏更有针对性。
4. ✅ **压缩引入质变而非仅量变**：
   - 量化（Q8）近乎无损；
   - 更深压缩（Q6 / 蒸馏）会牺牲表面流畅度，但可能增强某些叙事结构性（如 entity coherence）。
5. ✅ **小型模型可逼近大型指令模型表现**：在受控任务下，TF3 模型可达 LLaMA-3 8B Instruct 等模型约 90% 的评分，性价比极高。

### 方法的局限性
- 🚫 **领域受限**：训练数据仅为道德寓言，泛化至新闻、科技等其他文体的能力未知。
- 🚫 **依赖合成数据质量**：若初始生成器存在偏见或模式单一，将被放大至整个 pipeline。
- 🚫 **缺乏大规模人工评估**：目前依赖 LLM-as-a-judge 和自动指标，可能存在偏差（如 self-preference bias）。
- 🚫 **未探索跨语言迁移**：未尝试将 English TinyStories 的知识迁移到 Romanian 模型中。

### 未来工作方向
- 🔁 **生成更大、更多样化的 Romanian 合成语料**，覆盖不同体裁与风格。
- 🌍 **探索 Romanian 与其他罗曼语族语言（如 Italian, French）之间的 cross-lingual transfer**。
- ⚙️ **研究混合架构**（hybrid attention + SSM），结合 Transformer 的建模精度与 Mamba 的推理效率。
- 📉 **改进剪枝与蒸馏技术**，更好地保留语言的**风格丰富性与表达多样性**。
- 👥 **引入人类评估**，建立更可靠的 Romanian 文本生成质量基准。

---

> **总结一句话**：  
> TF3-RO 成功验证了“**合成数据 + 语言定制 tokenizer + 结构感知压缩**”这一范式，可在资源有限条件下高效构建高质量、可部署的 Romanian 小型语言模型，并形成“**训练 → 压缩 → 生成新数据**”的正向循环，为低资源语言 NLP 提供了一条清晰可行的技术路径。

</details>

---

### 3. [CAFEDistill: Learning Personalized and Dynamic Models through Federated Early-Exit Network Distillation](https://arxiv.org/abs/2601.10015)

**Authors**: Boyi Liu, Zimu Zhou, Yongxin Tong  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.10015v1  

#### Abstract
Personalized Federated Learning (PFL) enables collaboratively model training on decentralized, heterogeneous data while tailoring them to each client's unique distribution. However, existing PFL methods produce static models with a fixed tradeoff between accuracy and efficiency, limiting their appli...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CAFEDistill: Learning Personalized and Dynamic Models through Federated Early-Exit Network Distillation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Personalized Federated Learning (PFL)** 领域中的两个关键挑战：
- **静态模型限制**：现有 PFL 方法训练出的模型是静态的，无法在推理时根据输入复杂度动态调整计算深度，难以适应资源波动或延迟敏感场景。
- **早期退出网络（Early-Exit Networks, EENs）在联邦学习中的集成难题**：将 EENs 引入 PFL 会面临两类冲突：
  - **客户端异构性（client-wise heterogeneity）**：各客户端数据分布非独立同分布（non-IID），影响知识共享。
  - **深度间干扰（depth-wise interference）**：浅层与深层出口在联合训练中产生反向传播信号冲突。

这些问题导致现有方法（如先全局训练再本地微调浅层出口）性能不佳。

---

### 提出的新方法：CAFEDistill
作者提出 **CAFEDistill** —— 一种 **Conflict-Aware Federated Exit Distillation** 框架，用于实现个性化且动态的联邦学习。

#### 核心创新点：
1. **首次实现 PFL-EE（Personalized Federated Learning of Early-Exit Networks）**
   - 将 PFL 扩展到支持动态推理的 EENs，使每个客户端拥有一个可自适应跳过部分计算的个性化模型。

2. **跨客户端知识蒸馏（Cross-Client Knowledge Distillation, KD）机制**
   - 不仅利用本地最后一个出口作为“教师”监督浅层出口，还允许**所有客户端的最终出口共同作为教师**，为任意客户端的浅层出口提供蒸馏信号。
   - 这种跨客户端蒸馏能有效缓解局部数据稀疏带来的知识不足问题。

3. **渐进式、以深度优先的学生选择策略（Progressive, Depth-Prioritized Student Coordination）**
   - 在训练初期只激活最终出口（作为教师）；
   - 随着训练进行，逐步引入更浅的出口作为学生；
   - 减少因同时优化多出口而导致的梯度冲突，提升稳定性。

4. **基于相似性的教师匹配（Similarity-Aware Teacher Matching）**
   - 使用客户端模型梯度之间的余弦相似度估计数据分布相似性；
   - 动态分配不同教师出口的蒸馏权重 $k_i$，避免从差异大的客户端引入负迁移。

5. **去耦合的跨客户端蒸馏（Client-Decoupled Cross-Client KD）**
   - 在服务器端聚合所有客户端的最终出口形成一个“虚拟教师”；
   - 客户端只需接收该聚合教师进行本地蒸馏；
   - 实现与全量跨客户端 KD 等效的效果，但通信开销与标准 FedAvg 相当（仅传输 backbone 和 aggregated teacher）。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **准确性** | 显著优于各类 PFL 和 GFL-EE 基线，在多个数据集上平均提升最高达 21.29% |
| **推理效率** | 支持 early-exit，显著降低 MAC 数量，节省 30.79%-46.86% 推理成本 |
| **通信效率** | 通信开销仅比 FedAvg 高约 0.45%，远低于直接实施跨客户端 KD 的方案 |
| **鲁棒性** | 能有效应对高度 non-IID 数据（Dir(α=0.1)）下的训练挑战 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **图像分类任务**：
  - CIFAR-10（10类）
  - CIFAR-100（100类）
  - TinyImageNet（200类）
- **文本分类任务**：
  - AgNews（4类新闻类别）

数据划分采用 **Dirichlet 分布（Dir(α)）** 控制 non-IID 程度，测试了 α = 0.3 和 α = 0.1 两种极端情况。

---

### 实验设置
| 参数 | 设置 |
|------|------|
| 客户端数量 | 100 |
| 参与率（Sample Rate） | 10% 每轮 |
| 通信轮数 | 300（图像），100（文本） |
| 本地训练轮数（Local Epochs） | 5（图像），1（文本） |
| 模型架构 | ConvNet（CIFAR-10）、ResNet-18（CIFAR-100/TinyImageNet）、Transformer（AgNews） |
| 出口数量 | 3–4 个中间出口 |
| 优化器 | SGD（momentum=0.9, weight decay=1e-4, lr=0.1） |

---

### 评估指标
1. **Accuracy**：样本在终止出口处的预测准确率。
2. **Averaged Accuracy**：所有出口准确率的平均值。
3. **Inference Efficiency**：每样本的 Multiply-Accumulate (MAC) 操作数，衡量计算开销。
4. **Accuracy-Efficiency Tradeoff**：在不同 exit threshold 下绘制 accuracy vs. MAC 曲线。

---

### 对比的基线方法
#### （1）通用联邦学习（GFL）与 PFL 基线（扩展至 EENs）：
- **Local-EE**：完全本地训练
- **FedAvg-EE**, **FedProx-EE**：经典 GFL 方法扩展
- **FedPer-EE**, **FedRep-EE**, **FedBABU-EE**, **FedAMP-EE**, **pFedGraph-EE**, **FedRoD-EE**, **Ditto-EE**, **FedPAC-EE**：代表性 PFL 方法扩展

#### （2）专为 EEN 设计的 GFL-EE 方法：
- **ScaleFL**：基于本地 KD 的资源适配 FL
- **DepthFL**：双向 KD 与异构优化器

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 数据集 | 方法 | Dir(0.3) 平均准确率 |
|--------|-------|---------------------|
| CIFAR-10 | CAFEDistill | **72.67±10.71** |
| CIFAR-100 | CAFEDistill | **50.41±5.47** |
| TinyImageNet | CAFEDistill | **36.27±10.86** |
| AgNews | CAFEDistill | **78.69±14.63** |

> ✅ 在所有数据集上均取得 **SOTA 性能**

---

### 与基线方法的对比结果
- 在 **CIFAR-100 (Dir(0.3))** 上，相比最强 PFL-EE 基线 FedPAC-EE（47.45%），CAFEDistill 达到 **50.41%**，提升 **+2.96pp**。
- 在 **TinyImageNet (Dir(0.1))** 上，相比 FedPAC-EE（46.47%），CAFEDistill 达到 **48.22%**，提升 **+1.75pp**。
- 在 **AgNews** 上，超越第二名 FedRoD-EE（77.24%）达 **78.69%**。

更重要的是：
> 📉 **推理成本降低 30.79% – 46.86%**

例如在 CIFAR-100 上，达到相同精度时，CAFEDistill 所需 MAC 比其他方法少 **2.5–14.5%**；给定相同计算预算，其精度高出 **2.5–14.5%**（见 Figure 7）。

---

### 消融实验结果（Table 4）
验证了 CAFEDistill 各组件的有效性（在 CIFAR-100 上）：

| 配置 | Exit-1 | Exit-2 | Exit-3 | Exit-4 | 结论 |
|------|--------|--------|--------|--------|------|
| 无 KD | 36.29 | 37.92 | 41.45 | 41.88 | 基线 |
| + Local KD | 36.45 | 38.10 | 38.31 | 37.51 | ❌ 浅层略升，深层下降 → 局部 KD 有害 |
| + Cross-Client KD (TM) | 42.06 | 45.64 | 48.21 | 51.91 | ✅ 全面提升（+5.77~10.03%） |
| + 学生选择 (SS) | **45.45** | **49.69** | **53.42** | **53.08** | ✅ 再提升（+1.17~5.21%） |

👉 表明：**跨客户端 KD + 渐进式训练** 是成功的关键。

此外，Table 3 显示：对现有 PFL-EE 方法添加 local KD 反而会损害深层出口性能，进一步说明传统 KD 不适用于高度 non-IID 场景下的 PFL-EE。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **PFL 与 EEN 可以有效结合**：CAFEDistill 成功实现了既能个性化又能动态推理的联邦学习框架。
2. ✅ **跨客户端知识蒸馏至关重要**：仅依赖本地 KD 会导致深层出口退化，必须引入跨客户端知识融合。
3. ✅ **渐进式训练缓解深度干扰**：通过 depth-prioritized 协调机制，显著减少多出口联合训练中的冲突。
4. ✅ **高效通信设计可行**：提出的 client-decoupled formulation 在不牺牲性能的前提下，将通信开销控制在 FedAvg 水平。

---

### 方法的局限性
1. 当前假设所有客户端使用相同的 EEN 架构（相同层数、相同出口位置），未考虑设备级异构性（如内存、算力差异）。
2. 教师匹配依赖梯度相似性代理数据分布，可能在某些极端分布偏移下失效。
3. 实验主要集中在图像和简单文本任务，尚未验证在多模态或序列建模等复杂场景的表现。

---

### 未来工作方向
- 扩展至 **multi-modal scenarios**（如视觉-语言模型）
- 支持 **client resource heterogeneity**，允许不同客户端拥有不同深度的 EEN
- 探索更高效的 **exit policy learning**（当前使用固定阈值 confidence-based policy）
- 结合 **model compression** 或 **pruning** 进一步降低部署成本

---

## 总结
CAFEDistill 是首个将 **Personalized Federated Learning** 与 **Early-Exit Networks** 成功结合的工作，提出了 **Conflict-Aware Federated Exit Distillation** 框架，通过 **跨客户端知识蒸馏 + 渐进式训练 + 去耦合通信设计**，在保持低通信开销的同时，显著提升了模型准确性和推理效率。实验证明其在多种 non-IID 场景下全面超越 SOTA 方法，为构建高效、灵活、个性化的边缘智能系统提供了重要技术路径。

</details>

---

### 4. [Efficient Content-based Recommendation Model Training via Noise-aware Coreset Selection](https://arxiv.org/abs/2601.10067)

**Authors**: Hung Vinh Tran, Tong Chen, Hechuan Wen, Quoc Viet Hung Nguyen, Bin Cui, Hongzhi Yin  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.10067v1  

#### Abstract
Content-based recommendation systems (CRSs) utilize content features to predict user-item interactions, serving as essential tools for helping users navigate information-rich web services. However, ensuring the effectiveness of CRSs requires large-scale and even continuous model training to accommod...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Efficient Content-based Recommendation Model Training via Noise-aware Coreset Selection**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
- **训练效率瓶颈**：内容推荐系统（CRS）依赖大规模用户-物品交互日志进行模型训练，导致高昂的计算成本、时间开销和碳足迹。
- **噪声标签影响**：CRS广泛使用隐式反馈（如点击）作为监督信号，但这类数据存在大量噪声（误点、好奇点击等），直接影响小规模 coreset 的质量。
- **现有 coreset 方法不适用**：传统 coreset 选择方法假设数据无噪声，且多针对协同过滤任务设计，难以直接应用于复杂的基于内容的推荐场景。

### **提出的新方法：Noise-aware Coreset Selection (NaCS)**
NaCS 是一个专为内容推荐系统设计的噪声感知 coreset 选择框架，包含三个核心组件：
1. **基于子模优化的高效 coreset 选择**  
   - 将 coreset 选择建模为子模优化问题，目标是选出能最好地近似全量数据梯度的样本子集。
   - 仅使用最终层梯度（final layer gradient）来估计梯度差异，显著降低计算开销。
   - 采用 **stochastic greedy 算法** 并在 GPU 上实现批量并行处理，大幅提升选择效率。

2. **渐进式标签自修正（Progressive Label Self-Correction）**  
   - 在 coreset 构建过程中，利用当前模型对整个数据集的预测结果，动态更新样本标签（软标签）：
     $$
     y^* = (1 - \epsilon)y + \epsilon \hat{y}
     $$
     其中 $\epsilon$ 随训练进程增长（“全局信任”）且依赖于模型对该样本的置信度（“局部信任”熵）。
   - 实现了在不引入额外标注的情况下逐步纠正噪声标签。

3. **基于不确定性的 coreset 去噪（Uncertainty-based Denoising）**  
   - 对初步构建的 coreset 应用 **Monte Carlo Dropout** 进行多次前向传播，估计每个样本的预测不确定性。
   - 移除不确定性最高的样本（如 top 10%），进一步提升 coreset 质量。

### **相比现有方法的优势**
| 方面 | NaCS | 现有方法（如 CGM、SVP-CF、K-Center） |
|------|------|-----------------------------|
| **数据表示** | 使用原始稀疏特征 | CGM 使用稠密合成特征，脱离现实 |
| **噪声鲁棒性** | 显式建模并纠正噪声标签 | 假设数据干净，易受噪声误导 |
| **计算效率** | 仅用最终层梯度 + 批量并行 | 需全网络反向传播或双层优化，开销大 |
| **实用性** | 可扩展至 LLM-based 推荐器 | 多数局限于传统模型 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Criteo**：广告点击预测数据集，4584万条记录。
- **Avazu**：移动广告点击数据集，4043万条记录。
- **KDD Cup 2012**：学术竞赛数据集，1.5亿条记录。
- 所有数据集按 8:1:1 划分训练/验证/测试集。

### **实验设置与评估指标**
- **骨干模型**：
  - DeepFM
  - DCNv2
- **coreset 规模**：0.5%, 1%, 5%
- **评估指标**：
  - **AUC**（越高越好）
  - **Log Loss**（越低越好）
- **训练配置**：
  - Adam 优化器，学习率 0.01，batch size 8192
  - Early stopping 防止过拟合

### **基线方法对比**
| 方法 | 类型 | 特点 |
|------|------|------|
| **Random** | 随机采样 | 最简单基线 |
| **SVP-CF** | 代理模型打分 | 使用 FM 模型评分选样本 |
| **K-Center** | 聚类中心选择 | 最小化最大距离 |
| **CGM** | 数据蒸馏 | 梯度匹配生成合成数据 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **仅用 1% 数据即可恢复 93–95% 全量训练性能**：
  - Criteo @1%：AUC 达到 0.7707（全量为 0.8117），恢复约 **95%**
  - Avazu @1%：AUC 达到 0.7365（全量为 0.7891），恢复约 **93%**
  - KDD @1%：AUC 达到 0.7441（全量为 0.7995），恢复约 **93%**

### **与基线方法的对比结果（Table 2）**
- 在所有数据集和比例下，**NaCS 显著优于所有基线方法**。
- 优势在小规模 coreset（如 0.5%）时更为明显。
- CGM 表现较差，因其使用稠密表示且训练开销大。
- Random 在某些情况下表现尚可，说明 coreset 质量高度依赖选择策略。

### **消融实验结果（Table 3）**
#### **(a) Coreset 目标函数有效性**
- “Set Cover” 和 “Random” 在去噪后仍有提升，但 NaCS 效果最佳，证明其子模优化目标更优。

#### **(b) 损失函数对比**
- 使用 BCE 或 R-CE 损失有一定改进，但 NaCS 的渐进标签修正效果最好。
- T-CE 表现差，可能因过度抑制正样本。

#### **(c) 去噪模块有效性**
- 加入去噪后性能全面提升。
- 若使用全量数据训练的模型作为“完美去噪器”，性能可进一步提升，说明当前去噪仍有优化空间。

---

## **4. 关键结论和发现**

### **主要发现**
1. **小规模高质量 coreset 可行**：通过合理选择与去噪，**1% 数据足以支撑接近全量训练的推荐性能**。
2. **噪声是小 coreset 的致命因素**：忽略噪声会导致选择偏差，NaCS 的两阶段去噪机制有效缓解该问题。
3. **模型复杂度需与数据预算匹配**：在极小数据下（如 1%），简单模型（DeepFM）反而优于复杂模型（DCNv2），因其更抗过拟合。
4. **NaCS 具备良好泛化能力**：
   - 在文本推荐（MIND + LLM）任务中仍优于随机选择。
   - 可无缝迁移至不同 backbone（DeepFM、DCNv2、FinalMLP）。

### **方法的局限性**
- **依赖模型梯度**：需要运行前向/部分反向传播，无法完全避免计算开销。
- **去噪过程仍可能引入偏差**：渐进修正若初始模型错误严重，可能导致错误累积（尽管 early stopping 缓解了该问题）。
- **超参数敏感**：如 `Nchoose`（选择步数）、移除比例等需调优。

### **未来工作方向**
- 将 NaCS 扩展至 **联邦学习** 和 **流式推荐** 场景。
- 探索更先进的不确定性量化方法替代 Monte Carlo Dropout。
- 结合主动学习，在线迭代优化 coreset。
- 支持多任务与跨域推荐中的 coreset 构建。

---

> ✅ **代码开源**：作者已将源码公开于 GitHub：[https://github.com/chenxing1999/nacs](https://github.com/chenxing1999/nacs)

</details>

---

### 5. [Memo-SQL: Structured Decomposition and Experience-Driven Self-Correction for Training-Free NL2SQL](https://arxiv.org/abs/2601.10011)

**Authors**: Zerui Yang, Weichuan Wang, Yanwei Xu, Linqi Song, Yudai Matsuda, Wei Han, Bo Bai  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.10011v1  

#### Abstract
Existing NL2SQL systems face two critical limitations: (1) they rely on in-context learning with only correct examples, overlooking the rich signal in historical error-fix pairs that could guide more robust self-correction; and (2) test-time scaling approaches often decompose questions arbitrarily, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Memo-SQL: Structured Decomposition and Experience-Driven Self-Correction for Training-Free NL2SQL

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有 NL2SQL 系统存在三大关键局限：
1. **依赖静态 in-context learning**：仅使用正确示例进行提示，忽略了历史错误-修正对（error-fix pairs）中蕴含的丰富信号，无法有效指导模型自我修正。
2. **Test-Time Scaling (TTS) 方法缺乏多样性**：多数分解策略由 LLM 自主决定，导致不同运行间生成高度相似甚至相同的 SQL 候选，削弱了集成（ensemble）效果。
3. **准确率与效率的权衡困境**：高性能方法计算开销巨大，而轻量级方案则牺牲质量。

### 提出的新方法与思路
作者提出 **Memo-SQL**，一个无需训练的 NL2SQL 框架，核心创新在于两个简单但有效的机制：

#### （1）Structured Decomposition（结构化分解）
摒弃随机或 LLM 自主决定的分解方式，引入三种明确的语义轴向分解策略，以提升推理路径的多样性：
- **Entity-wise（实体级）**：按数据库中的表/实体划分子问题（如客户、订单等），分别生成局部查询后通过外键连接。
- **Hierarchical（层次化）**：处理嵌套语言结构（如“高于平均值”、“从未订购过”），从内层聚合条件向外层递归分解，对应 SQL 中的 `NOT EXISTS` 或 `IN` 子句。
- **Atomic Sequential（原子顺序）**：将问题拆解为一系列原子关系操作（选择、投影、连接、分组、聚合），实现细粒度错误定位。

> 这三种策略并行执行，自然形成 **best-of-N（N=3）** 的候选选择机制。

#### （2）Experience-Driven Self-Correction（经验驱动的自我修正）
构建一个动态的 **error-correction memory**，存储历史上的错误-修正对（包括错误类型和修正建议）。在推理时：
- 使用检索增强（retrieval-augmented）技术，基于当前生成的 SQL 结构和问题，从记忆库中召回最相关的错误修复案例。
- 将这些案例作为上下文示例注入 prompt，引导模型识别并修正潜在错误。

该机制实现了真正的“从失败中学习”，且完全无需 fine-tuning 或外部 API。

### 相比现有方法的优势
- ✅ **无需训练**：纯 test-time scaling，适用于开源 LLM。
- ✅ **高效且高精度**：在显著降低计算资源消耗的同时达到 SOTA 性能。
- ✅ **可动态更新**：支持实时集成用户反馈，持续改进系统。
- ✅ **强泛化能力**：跨数据集迁移表现良好，即使使用 BIRD 构建的记忆库也能提升 Spider 上的表现。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|--------|------|
| **BIRD** | 主要评测基准，包含大规模真实数据库，用于构建 error-correction memory 和主实验。dev-new 是经过修正的更可靠版本。 |
| **Spider** | 经典跨域 NL2SQL 数据集，用于评估跨数据集泛化能力。 |
| **CHESS-SDS** | BIRD dev 的高质量子集，强调复杂查询和精确模式匹配，用于效率评估。 |

### 实验设置与评估指标
- **模型**：主要使用 Qwen-Coder 系列（Qwen2.5-Coder-32B, Qwen3-Coder-30B-A3B），也测试了 DeepSeek-Coder-V2-Lite 和 Llama-3.1-8B。
- **评估指标**：
  - **Execution Accuracy (EX%)**：生成的 SQL 执行结果是否与黄金答案一致。
  - **Token 数量 / 查询**：衡量计算成本。
  - **响应时间 / 查询（秒）**：端到端延迟。
- **重复性**：所有结果取三次独立运行的平均值。

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **训练免费 + 开源** | ROUTE, Alpha-SQL |
| **训练免费 + 闭源** | GPT-4, Gemini |
| **需微调 + 开源** | SFT CodeS, XiYan-SQL@DDL |
| **其他修正方法** | DIN-SQL（prompt engineering）、Solid-SQL（仅用正确示例检索）、SHARE（SFT 微调） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（BIRD dev-new 集）

| 方法 | 模型 | EX (%) |
|------|------|-------|
| **Memo-SQL (ours)** | Qwen3-Coder-30B-A3B | **68.5** ✅ |
| Alpha-SQL | Qwen3-Coder-30B-A3B | 67.2 |
| ROUTE | Qwen3-Coder-30B-A3B | 65.2 |
| Distillery | Llama-3.1-405B | 59.2 |

> Memo-SQL 在 **open, zero-fine-tuning** 方法中首次突破 68%，成为新的 SOTA。

### 与其他方法的对比优势

#### （1）效率对比（CHESS-SDS 数据集）
| 方法 | 模型 | EX (%) | Tokens/Query | Time/Query (s) |
|------|------|--------|---------------|----------------|
| **Memo-SQL** | Qwen2.5-Coder-32B | 57.6 | **7,925** | **145** |
| Alpha-SQL | Qwen2.5-Coder-32B | 58.5 | 150,344 | 1,839 |
| **Memo-SQL** | Llama-3.1-8B | **52.1** | **16,563** | **135** |
| Alpha-SQL | Llama-3.1-8B | 49.6 | 244,775 | 135 |

- Memo-SQL 在 Qwen 上 **节省 19 倍 token**，速度提升 **12.7 倍**；
- 在小模型上仍优于大模型基线，且效率远超 Alpha-SQL（token 减少 14.8 倍）。

#### （2）跨数据集泛化（Spider dev）
| 方法 | EX (%) |
|------|--------|
| **Memo-SQL**（记忆库来自 BIRD） | **86.5** |
| w/o SQL Correction | 85.8 |
| Alpha-SQL | 87.8 |
| ROUTE | 80.0 |

> 即使未见过 Spider 的交互历史，Memo-SQL 仍取得接近 SOTA 的表现，证明其 error-correction knowledge 具有强迁移能力。

### 消融实验结果（BIRD dev-new）

| 变体 | EX (%) | Δ from Full |
|------|--------|------------|
| **Full Memo-SQL** | **68.5** | — |
| w/o Schema Linking | 67.7 | -0.8 |
| w/ Random Decomposition | 67.2 | -1.3 |
| w/o ReAct+Reflect | 67.3 | -1.2 |
| w/o Multi-Style Generation | 67.6 | -0.9 |
| **w/o SQL Refinement** | **66.4** | **-2.1** ✅ |
| + ICL in Final Gen | 67.1 | -1.4 |

> **最关键的组件是 SQL Refinement 模块**，移除后性能下降最大（-2.1%），说明 error-aware 自我修正是核心驱动力。

---

## 4. 关键结论和发现

### 主要发现
1. **结构化分解显著提升多样性**：相比随机分解，三种语义轴向分解策略能产生更多样化的推理路径，增强集成鲁棒性。
2. **从错误中学习比只看正确示例更有效**：引入 error-correction memory 并结合 retrieval-augmented prompting，使模型不仅能“知道什么是对的”，还能“理解错在哪以及如何改”。
3. **无需 fine-tuning 也能实现 SOTA**：Memo-SQL 在完全不更新参数的前提下，在 open, training-free 范式下达到最高执行准确率。
4. **高效与高性能可兼得**：相比主流 TTS 方法（如 Alpha-SQL），Memo-SQL 计算开销降低一个数量级以上，更适合实际部署。

### 方法的局限性
1. **错误记忆库依赖合成错误质量**：当前 memory 来源于模型在 BIRD 训练集上的错误，若与真实用户错误分布不符，可能影响修正效果。
2. **早期分解错误难以纠正**：如果初始分解出现严重偏差（如歧义问题），后续自我修正可能无法完全弥补。
3. **仍有一定延迟**：尽管比 Alpha-SQL 快得多，但由于多轮 LLM 调用和 SQL 执行，对于亚秒级响应要求的场景（如交互式仪表盘）仍显沉重。

### 未来工作方向
- 构建基于真实用户反馈的动态 error-correction memory。
- 探索更轻量的 self-correction 机制，适应低延迟场景。
- 将框架扩展至其他程序生成任务（如 Python 代码生成）。
- 引入主动学习机制，优先采集最具信息量的错误样本以优化 memory 质量。

---

> **总结**：Memo-SQL 通过 **structured decomposition** 和 **experience-driven self-correction** 两大设计，在无需训练的前提下，实现了高准确率、高效率、强泛化性的 NL2SQL 推理框架，为构建可持续进化的开放 BI 系统提供了实用范式。

</details>

---

### 6. [LLMdoctor: Token-Level Flow-Guided Preference Optimization for Efficient Test-Time Alignment of Large Language Models](https://arxiv.org/abs/2601.10416)

**Authors**: Tiesunlong Shen, Rui Mao, Jin Wang, Heming Sun, Jian Zhang, Xuejie Zhang, Erik Cambria  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.10416v1  

#### Abstract
Aligning Large Language Models (LLMs) with human preferences is critical, yet traditional fine-tuning methods are computationally expensive and inflexible. While test-time alignment offers a promising alternative, existing approaches often rely on distorted trajectory-level signals or inefficient sa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLMdoctor: Token-Level Flow-Guided Preference Optimization for Efficient Test-Time Alignment of Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的大语言模型（LLM）对齐方法（如 RLHF、DPO）依赖于昂贵的全量微调（full fine-tuning），计算成本高且缺乏灵活性，难以适应多样或动态变化的用户偏好。而现有的 **test-time alignment** 方法虽然避免了重新训练，但仍存在以下根本缺陷：
- **轨迹级奖励信号（trajectory-level reward）** 导致信用分配不精确，无法捕捉单个 token 对偏好的贡献；
- **序列模仿式奖励建模（sequence-mimicking reward）** 存在“奖励预算”扭曲（reward-budget distortion），即必须将一个全局奖励分摊到所有 token 上，导致中性 token 被错误赋予高分；
- 这些方法最终会收敛到小规模奖励模型的能力上限，形成**性能天花板效应（ceiling effect）**。

### 提出的新方法与创新思路
本文提出 **LLMdoctor**，一种基于“患者-医生”范式的高效 test-time alignment 框架，其核心创新包括：

#### （1）Token-Level Reward Acquisition（细粒度奖励获取）
- 不依赖外部奖励模型，而是通过对同一冻结的“患者 LLM”施加不同行为提示（prompting），生成正向（helpful）和负向（lazy/unhelpful）的行为变体；
- 通过对比这两个变体在相同上下文下的 token 级 log-likelihood 差异，提取真正具有判别力的 token 级奖励信号；
- 引入稀疏性阈值（sparsity threshold），仅对显著影响偏好的 token 分配非零奖励，避免噪声干扰。

#### （2）Token-Level Flow-Guided Preference Optimization (TFPO)
- 将 **Generative Flow Networks (GFlowNets)** 的流守恒原理引入 token 序列生成过程；
- 定义每个前缀状态 $ s_t $ 的“流”为：  
  $$
  F(s_t) = Q(s_t) \cdot V_d(s_t)
  $$
  其中 $ Q(s_t) $ 来自 token-level 奖励，$ V_d(s_t) $ 是医生模型学习的价值函数；
- 通过 **Subtrajectory Balance (SubTB)** 目标强制所有子路径满足流量平衡，实现从局部到全局的一致性优化；
- 该机制将偏好信号从 $ O(1) $（整个序列）扩展到 $ O(n) $（所有子序列），提供更密集、一致的监督信号。

#### （3）灵活高效的在线对齐（Online Alignment）
- 在推理时，训练好的“医生模型”作为 flow-guided reward model 动态指导“患者模型”的解码过程；
- 支持多维度偏好控制（如 helpfulness 和 harmlessness），通过调整不同维度的权重 $ \beta_i $ 实现实时权衡，无需重新训练。

### 相比现有方法的优势
| 维度 | LLMdoctor | 传统方法（如 DPO、GenARM） |
|------|-----------|-----------------------------|
| **训练成本** | 仅需训练小型 doctor 模型 | 需要全量微调大模型 |
| **灵活性** | 推理时可动态调节偏好权重 | 固定策略，需重新训练才能改变目标 |
| **信用分配精度** | 基于行为差异的 token 级奖励，精准定位关键 token | 轨迹级或强制分摊的 token 级奖励，易受噪声污染 |
| **多样性保持** | TFPO 天然防止模式崩溃（mode collapse） | 奖励最大化倾向导致输出单一化 |
| **性能上限** | 不受限于奖励模型能力 | 收敛至奖励模型的贪婪策略，存在理论天花板 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **HH-RLHF**：112K 训练样本，用于通用对齐评估；
- **PKU-SafeRLHF-10K**：明确标注 helpfulness 和 harmlessness 的双维偏好数据；
- **UltraFeedback**：大规模 AI 反馈数据，用于 weak-to-strong 实验中的奖励信号提取。

### 实验设置与评估指标
| 设置项 | 描述 |
|--------|------|
| **基础模型** | LLaMA-7B-SFT、Tulu2-7B/13B/70B-SFT |
| **Doctor 模型** | 通常为 7B 规模的小模型 |
| **训练方式** | 使用 LoRA 微调 doctor 模型 |
| **评估方式** | - **GPT-4o 判官**：进行成对比较（head-to-head），报告 Win/Tie/Loss；<br>- **AlpacaEval 2**：自动评估框架，计算胜率（win rate）；<br>- **长度控制胜率（Length-Controlled Win Rate）**：消除长度偏差的影响。 |
| **关键指标** | - Win + ½ Tie (%)<br>- 多目标帕累托前沿（Pareto Frontier）<br>- 生成多样性（Distinct-4）<br>- 对齐信号强度（Value Gap） |

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **标准解码** | Greedy Search, Top-k, Top-p, Contrastive Search |
| **训练时对齐** | DPO |
| **测试时对齐** | ARGS, GenARM, CARDS, Transfer-Q, Naive RS |
| **多目标对齐** | Reward Soups (RS), MORL, MOD |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 和 Figure 4）

#### （1）主实验结果（vs. DPO）
| 方法 | Win + ½ Tie (%) |
|------|------------------|
| **LLMdoctor (Ours)** | **61.00 ± 0.30** ✅ |
| GenARM | 52.25 ± 0.32 |
| CARDS | 41.55 ± 0.23 |
| Transfer-Q | 33.37 ± 0.22 |
| ARGS | 26.24 ± 0.17 |

> **结论**：LLMdoctor 显著优于所有 test-time 方法，并**超越了需要全量微调的 DPO**。

#### （2）弱引导强实验（Weak-to-Strong Guidance）
| 患者模型规模 | 方法 | 长度控制胜率（LC Win Rate） |
|--------------|------|-------------------------------|
| 70B | **LLMdoctor (7B doctor)** | **82.5%** ✅ |
| 70B | DPO (70B fine-tuned) | 82.0% ❌ |

> **结论**：即使使用 7B 的 doctor 模型，也能有效引导 70B 的患者模型，**性能超过全量微调的 DPO**，验证了 weak-to-strong 的有效性。

#### （3）多维度偏好平衡（Pareto Frontier）
- 如图 3 所示，LLMdoctor 在 helpfulness 和 harmlessness 之间实现了**最优的权衡曲线**，全面主导其他方法（包括 RS、MORL 等）；
- 支持推理时动态调整 $ \beta_h $ 和 $ \beta_s $，无需重新训练。

#### （4）消融实验结果（Table 2 & Table 4）
| 消融变体 | Win + ½ Tie (%) | Diversity |
|----------|------------------|-----------|
| **完整 LLMdoctor** | **61.00** | **0.47** |
| w/o Subtrajectory Balance ($\mathcal{L}_{\text{SubTB}}$) | 53.15 ↓ | 0.34 ↓ |
| w/o Value Discrimination ($\mathcal{L}_{\text{value}}$) | 58.23 ↓ | 0.43 ↓ |
| w/o Reward Sparsity | 56.58 ↓ | 0.46 ↓ |
| w/o Flow-Guided Rewards | 52.76 ↓ | 0.25 ↓ |

> **结论**：TFPO 中的 flow consistency 是提升性能和多样性的核心机制。

---

## 4. 关键结论和发现

### 主要发现
1. **Token-level 奖励 + Flow-guided 优化是高效 test-time alignment 的关键路径**：
   - 通过行为变体提取 token 级奖励，避免了外部奖励模型的误差传播；
   - TFPO 机制实现了从 token 到 subtrajectory 再到完整序列的偏好一致性传播。

2. **LLMdoctor 同时提升了对齐性能和生成多样性**：
   - 传统方法往往在二者之间权衡，而 LLMdoctor 由于其分布匹配特性（distribution matching），天然防止 mode collapse（见 Appendix C 证明）；
   - 图 6 显示其在性能-多样性权衡上全面领先。

3. **信号动态分析揭示“前瞻性”优势**：
   - 图 5 显示，LLMdoctor 在生成早期就具备强烈的对齐信号（high value gap），说明 TFPO 成功将长期偏好信息注入每一步决策；
   - 而 DPO 和 GenARM 的信号随时间逐步增强，缺乏“远见”。

4. **理论天花板效应被打破**：
   - 传统 reward-guided 方法受限于 reward model 的能力（Appendix A 证明）；
   - LLMdoctor 通过 flow-guided 机制绕过此限制，使大模型能超越小 reward model 的表现。

### 方法的局限性
- **依赖高质量的行为提示设计**：Positive/Negative face 的 prompt 设计直接影响 token-level 奖励质量；
- **当前仍需离线训练 doctor 模型**：虽比 full fine-tuning 快，但并非完全免训练；
- **多 doctor 模型集成可能增加推理复杂度**：在多目标场景下需管理多个 reward heads。

### 未来工作方向
- **全自动 prompt engineering for behavioral variants**：探索如何自动生成最优的正负行为提示；
- **Zero-shot doctor initialization**：尝试无需训练即可初始化 doctor 模型；
- **扩展至多模态对齐任务**：将 TFPO 框架应用于图像、音频等跨模态生成；
- **结合 speculative decoding 进一步加速推理**：提升 test-time alignment 的效率边界。

--- 

> **总结**：LLMdoctor 提出了一种全新的 test-time alignment 范式，通过 **token-level reward acquisition + flow-guided optimization** 实现了高性能、高多样性、高灵活性的对齐效果，在多个维度上超越了包括 DPO 在内的主流方法，为大规模语言模型的实时个性化对齐提供了实用且强大的解决方案。

</details>

---

### 7. [SPRInG: Continual LLM Personalization via Selective Parametric Adaptation and Retrieval-Interpolated Generation](https://arxiv.org/abs/2601.09974)

**Authors**: Seoyeon Kim, Jaehyung Kim  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.09974v1  

#### Abstract
Personalizing Large Language Models typically relies on static retrieval or one-time adaptation, assuming user preferences remain invariant over time. However, real-world interactions are dynamic, where user interests continuously evolve, posing a challenge for models to adapt to preference drift wi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SPRInG: Continual LLM Personalization via Selective Parametric Adaptation and Retrieval-Interpolated Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Large Language Model (LLM)** 个性化方法通常依赖静态检索（static retrieval）或一次性参数适配（one-time adaptation），假设用户偏好是**时间不变的**。然而在真实场景中，用户的兴趣会随时间动态演化（preference drift），导致模型难以持续适应新偏好，同时面临**灾难性遗忘**（catastrophic forgetting）的风险。

标准的 **Continual Learning (CL)** 方法在此类任务中表现不佳，因为它们对所有交互数据进行无差别更新，无法区分真正的偏好变化与临时噪声（transient contexts），从而容易过拟合于短期行为。

### 提出的新方法：SPRING
作者提出 **SPRING**（Selective Parametric adaptation and Retrieval-Interpolated Generation），一种新型的**半参数化框架**（semi-parametric framework），用于实现高效的**持续性 LLM 个性化**（continual LLM personalization）。其核心思想是在训练和推理两个阶段联合利用参数化与非参数化方法，各司其职、互补协同。

#### 创新点：
- **Drift-driven Selective Adaptation（漂移驱动的选择性适配）**
  - 在训练阶段，设计了一个基于似然度的评分函数（likelihood-based scoring function），识别具有高新颖性的交互样本（high-novelty interactions）。
  - 只对这些“漂移信号”明显的样本更新用户特定的 **LoRA adapter**，避免噪声干扰。
  - 同时将难以被模型内化的样本保留在一个有限容量的 **replay buffer** 中，以缓解遗忘问题。

- **Selective Retrieval-Interpolated Generation（选择性检索-插值生成）**
  - 推理阶段引入**严格的相关性门控机制**（relevance gating），过滤掉不相关的检索历史。
  - 将参数化知识（来自 adapter 的输出 logits）与非参数化知识（从 replay buffer 检索得到的历史响应）通过 **logit interpolation** 动态融合，生成最终响应。

### 相比现有方法的优势
| 维度 | SPRING | 传统方法 |
|------|--------|----------|
| 更新策略 | 选择性更新（仅高漂移样本） | 全量更新或固定适配器 |
| 噪声鲁棒性 | 强（通过质量项和新颖性项双重筛选） | 弱（易受噪声影响） |
| 遗忘控制 | 显式保留难样本到 replay buffer | 依赖正则化或均匀回放 |
| 推理灵活性 | 参数+非参数双路径融合 | 单一路径（仅 adapter 或仅 RAG） |

---

## 2. 核心实验方法和设置

### 数据集
在 **LongLaMP benchmark**（Kumar et al., 2024） 上进行评估，该基准专注于长文本个性化生成任务，包含以下两类任务：
- **Abstract Generation**（摘要生成）
- **Review Writing**（评论写作）

每个任务按时间划分为多个 period（周期），模拟用户偏好的逐步演变过程。

### 实验设置
- **主干模型**：Gemma-3-4B-IT
- **优化器**：AdamW，学习率 $1 \times 10^{-5}$，weight decay = 0.01
- **调度器**：线性衰减 + 10% warmup
- **batch size**：每设备 4
- **adapter 类型**：LoRA（低秩适配）
- **replay buffer 容量**：固定预算 $N_{\text{max}}$
- **评估频率**：跨多个 time periods 进行持续评估

### 评估指标
采用自动评价指标：
- **ROUGE-1**
- **ROUGE-L**

衡量生成文本与参考答案之间的 n-gram 匹配程度，尤其关注长文本语义连贯性。

### 基线方法对比
包括以下代表性方法：
- **Standard RAG**：基于检索增强生成的传统非参数方法
- **CAMA**（Kim et al., 2024）：当前最先进的 Continual Learning 方法
- **Static Personalization**：基于初始历史的一次性适配，不再更新
- **Full Fine-tuning / LoRA 更新全部数据**

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自原文 Table 和正文描述）

| 方法 | Abstract Gen (ROUGE-L) | Review Writing (ROUGE-L) |
|------|-------------------------|----------------------------|
| **SPRING (Ours)** | **显著优于所有基线** | **+18.12% vs RAG**, **+15.85% vs CAMA** |

具体提升幅度：
- 在 **Review Writing** 任务中，相比最强的 CL 方法 **CAMA**，SPRING 在 ROUGE-L 上提升了 **15.85%**；
- 相比标准 **RAG** 方法，提升达 **18.12%**；
- 在 **Abstract Generation** 任务中也表现出一致且稳定的领先优势。

### 消融实验结果（Ablation Study）

#### （1）漂移驱动训练的有效性
- 移除 drift-driven selection 后，性能下降 **12.35%**（以 ROUGE-L 衡量），验证了选择性更新的重要性。

#### （2）Logit 插值机制的作用
- 单独使用参数化路径（adapter only）或非参数路径（RAG only）效果较差；
- 使用 logit interpolation 融合两者后，最大提升可达 **22.58%**，证明双路径协同的有效性。

#### （3）Replay Buffer 保留策略比较（Table 4）
| 策略 | Abstract Gen (ROUGE-1) | Review Writing (ROUGE-1) |
|------|------------------------|---------------------------|
| **Global Highest (Ours)** | **0.350** | **0.332** |
| Cluster Round | 0.346 | 0.331 |
| Cluster-wise | 0.345 | 0.332 |
| Random | 0.345 | 0.331 |

结果表明，“全局最高得分保留”策略最优，说明优先保存高漂移分样本最有效。

---

## 4. 关键结论和发现

### 主要发现
1. **偏好漂移 ≠ 所有新数据**：并非所有新的用户交互都值得学习，必须通过**新颖性+语言质量**双重判据筛选真正有意义的漂移信号。
2. **参数化与非参数化应分工协作**：
   - 参数路径（adapter）适合学习稳定、可泛化的偏好模式；
   - 非参数路径（retrieval + replay buffer）更适合保留复杂、稀疏但关键的历史细节。
3. **logit-level fusion 更灵活可控**：相比于拼接上下文的 RAG，logit interpolation 能更精细地平衡内部知识与外部证据。
4. **replay buffer 设计至关重要**：合理的保留策略（如 global highest）能显著提升长期记忆效率。

### 方法的局限性
1. **推理延迟增加**：
   - 每个解码步需执行两次前向传播（vanilla + RAG），带来额外计算开销。
2. **漂移检测可能误判**：
   - 突发话题切换（false drift）可能导致错误更新，尽管通过限制更新比例（仅 top p%）有所缓解。
3. **大规模用户部署挑战**：
   - 每个用户维护独立 adapter 和 buffer，存储与服务成本随用户数增长而上升。

### 未来工作方向
1. **优化推理效率**：
   - 探索自适应插值策略，例如当两路分布差异较小时提前终止 dual-path 解码。
2. **改进检索与门控机制**：
   - 引入 **dense retrieval** 或 **cross-encoder re-ranking** 提升相关性判断精度。
3. **扩展用户共享架构**：
   - 构建**层次化或共享 adapter 结构**，对相似用户群体进行聚类，降低存储负担并支持跨用户知识迁移（cross-user knowledge transfer）。

---

> ✅ **总结一句话**：  
> SPRING 通过**选择性参数更新 + 检索-插值生成**，实现了对用户偏好演化的高效、稳健建模，在 LongLaMP 基准上显著超越现有方法，为现实世界中的持续性 LLM 个性化提供了可行路径。

</details>

---

### 8. [NSR-Boost: A Neuro-Symbolic Residual Boosting Framework for Industrial Legacy Models](https://arxiv.org/abs/2601.10457)

**Authors**: Ziming Dai, Dabiao Ma, Jinle Tong, Mengyuan Han, Jian Yang, Haojun Fei  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.10457v1  

#### Abstract
Although the Gradient Boosted Decision Trees (GBDTs) dominate industrial tabular applications, upgrading legacy models in high-concurrency production environments still faces prohibitive retraining costs and systemic risks. To address this problem, we present NSR-Boost, a neuro-symbolic residual boo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：NSR-Boost: A Neuro-Symbolic Residual Boosting Framework for Industrial Legacy Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在工业级高并发生产环境中，尽管 **Gradient Boosted Decision Trees (GBDTs)** 如 XGBoost 和 LightGBM 在表格数据建模中占据主导地位，但对已有部署的“遗留模型”（legacy model）进行升级面临两大挑战：
- **高昂的重训练成本**：完全替换模型架构或引入大规模新特征需重构整个工程流水线。
- **系统风险**：企业优先考虑稳定性，难以接受因模型变更带来的潜在故障。

现有改进方法（如 TabNet、OpenFE）通常要求修改输入特征空间或替换主干模型，导致部署风险高、维护成本大。

---

### 提出的新方法与思路
作者提出 **NSR-Boost**（Neuro-Symbolic Residual Boosting），一种专为工业场景设计的**非侵入式神经符号残差增强框架**。其核心思想是：
- 将已有的 GBDT 模型视为“冻结模型”（frozen model），不修改其参数或结构。
- 仅针对模型预测失败的“困难区域”（hard regions）生成可解释的符号专家函数（Symbolic Experts）进行局部修复。
- 利用 **Large Language Model (LLM)** 作为**符号代码生成器**，而非黑盒预测器，输出可执行的 Python 代码。

该框架包含三个阶段：
1. **残差引导的困难区域识别**：通过 CART 决策树分析残差分布，定位需要优化的子空间。
2. **双层优化的专家生成**：
   - 外循环：LLM 基于上下文提示逐步演化符号结构；
   - 内循环：采用 **Bayesian Optimization** 对生成函数的参数进行梯度无关的精细调优。
3. **上下文感知聚合机制**：轻量级 XGBoost 聚合器动态融合原始模型输出与多个符号专家，实现安全提升。

---

### 相比现有方法的优势
| 维度 | NSR-Boost | 传统方法（如 OpenFE, TabNet） |
|------|-----------|-----------------------------|
| **非侵入性** | ✅ 不改变原模型结构与输入特征 | ❌ 需重新训练模型或修改特征工程 |
| **推理延迟** | ✅ <1ms，适合高并发线上服务 | ❌ 深度模型（如 FT-Transformer）延迟达 10–100ms |
| **可解释性** | ✅ 输出显式的 Python 符号表达式 | ❌ 黑箱模型或隐式特征组合 |
| **部署安全性** | ✅ 局部修复，避免全局过拟合 | ❌ 全局替换带来系统不稳定风险 |
| **逻辑建模能力** | ✅ 支持非线性、非单调关系（如高斯函数） | ❌ 多数基于线性或简单交互 |

此外，NSR-Boost 是首个将 LLM 用于**生成可部署、无需在线推理的符号代码**来增强工业遗留系统的框架。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖两类数据：
- **6个公开基准数据集**（来自 OpenML）：
  - `adult`, `bank-marketing`, `blood-transfusion`, `breast-w`, `credit-g`, `pc1`
  - 覆盖金融、医疗等高风险领域，样本量从 699 到 48,842 不等。
- **1个私有真实业务数据集**（Qfin Holdings 提供）：
  - 名为 `private-data`，用于金融风控；
  - 特征维度高达 200，样本量约 49.7万，噪声严重且类别不平衡。

### 实验设置与评估指标
- 数据划分：统一采用 80% 训练 / 20% 测试，五次随机种子平均结果。
- 评估指标：
  - 公共数据集：Accuracy
  - 私有数据集：AUC（因严重类别不平衡）
- LLM 基座模型：**Seed-OSS-36B-Instruct**
- 专家数量 $ K = 4 $
- 所有 LLM-based 基线均限制最多调用 20 次 API，确保公平比较。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| 深度学习模型 | TabNet, FT-Transformer |
| 自动化特征工程 | AutoFeat, OpenFE |
| LLM-based 特征工程 | CAAFE, FeatLLM, OCTree, LLM-FE |
| 残差学习基线 | Res-XGBoost（用浅层 GBDT 拟合残差） |

所有特征工程方法均以 XGBoost 为下游分类器，并进行 30 次贝叶斯超参搜索。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）
| Dataset | Base(XGB) | NSR-Boost (Ours) | 最佳基线 |
|--------|------------|------------------|----------|
| adult | 0.873 | **0.875** | 0.874 (LLM-FE) |
| bank-marketing | 0.906 | **0.910** | 0.908 (FT-T.) |
| blood-transfusion | 0.742 | **0.820** | 0.805 (LLM-FE) |
| breast-w | 0.956 | **0.986** | 0.970 (LLM-FE) |
| credit-g | 0.751 | **0.815** | 0.784 (LLM-FE) |
| pc1 | 0.931 | **0.946** | 0.935 (LLM-FE) |
| private-data* | 0.688 | **0.694** | 0.666 (AutoFeat) |

- **平均排名第一（Mean Rank = 1.0）**，显著优于所有 SOTA 方法。
- 在小样本数据（如 `credit-g`, `blood-transfusion`）上提升尤为明显（+6.4% ~ +7.8%）。
- 在私有数据集上，**唯一未出现性能退化的方法**；而 Res-XGBoost 因过拟合噪声导致 AUC 从 0.688 降至 0.659。

---

### 与不同主干模型的泛化能力（Figure 2）
NSR-Boost 可无缝适配多种 GBDT 主干：
- 在 `credit-g` 上：
  - XGBoost → +6.4%
  - LightGBM → +4.3%
  - CatBoost → +2.5%
- 表明其挖掘的是数据本身的长尾模式，而非特定模型偏差。

---

### 消融实验结果（Figure 4，以 credit-g 为例）
| 消融配置 | 准确率 |
|---------|--------|
| 完整 NSR-Boost | **0.815** |
| w/o Bayesian Optimization | 0.766 (-4.9%) |
| w/o Context-Aware Aggregation | 0.773 |
| w/o Region Identification | 0.773 |
| w/o Statistical Prompting | 0.778 |
| w/o Feature Semantics | 0.778 |
| w/o Raw Feature Input | 0.783 |
| w/o Boundary Refinement | 0.793 |

**关键发现**：
- **Bayesian Optimization 至关重要**：LLM 擅长结构生成，但无法精确确定连续参数。
- **上下文感知聚合与区域识别缺一不可**：否则易引发全空间强制拟合噪声。
- 即使最弱变体仍优于原始 XGBoost，证明框架具有内在鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **NSR-Boost 实现了“安全进化”范式**：
   - 在不干扰现有系统的情况下，有效捕捉传统模型忽略的**长尾风险**。
   - 特别适用于金融风控、信贷评分等高稳定性要求场景。

2. **LLM 应作为“程序员”而非“预测器”使用**：
   - 生成可执行代码后即可下线 LLM，消除在线推理开销与不确定性。
   - 结合反馈机制（Negative/Positive Constraints）可显著减少“幻觉”。

3. **符号回归 + 数值优化的解耦策略更高效**：
   - 外循环（LLM）负责逻辑构造，内循环（Bayesian Opt.）负责参数精调。
   - 成功克服了符号回归中结构与参数联合搜索的组合爆炸难题。

4. **工业部署可行性高**：
   - 推理延迟 <1ms，满足毫秒级响应需求（Figure 7）。
   - 在线部署测试中，AUC 提升 0.37%~1.17%，坏账率降低 0.15%~0.18%，带来可观商业价值。

---

### 方法的局限性
- **依赖高质量残差信号**：若基础模型过于强大或残差无规律，则难找到有效“困难区域”。
- **LLM 语义理解瓶颈**：虽然语法正确率高，但“初始 AUC 失败”占比超过 50%（Figure 11），说明 LLM 缺乏真正的因果推理能力。
- **并行链数受限于计算资源**：每个专家链需独立运行 Bayesian 优化，增加硬件负担。

---

### 未来工作方向
- 引入 **meta-learning 或强化学习** 来指导 LLM 更高效地探索符号空间。
- 构建 **domain-specific concept library**（类似 Grayeli et al. [6]），提升 LLM 在金融、医疗等领域的先验知识。
- 探索 **multi-task symbolic experts**，支持同时修复多个任务的残差。
- 进一步压缩聚合器复杂度，实现完全无监督的动态门控机制。

---

> **总结一句话**：  
> NSR-Boost 提出了一种**非侵入、可解释、低延迟**的工业模型增强路径，通过 LLM 生成符号专家 + Bayesian 参数优化，在保持系统稳定的同时实现了 SOTA 性能，为大规模遗留系统的持续演进提供了实用解决方案。

</details>

---

### 9. [History Is Not Enough: An Adaptive Dataflow System for Financial Time-Series Synthesis](https://arxiv.org/abs/2601.10143)

**Authors**: Haochong Xia, Yao Long Teng, Regan Tan, Molei Qin, Xinrun Wang, Bo An  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.10143v1  

#### Abstract
In quantitative finance, the gap between training and real-world performance-driven by concept drift and distributional non-stationarity-remains a critical obstacle for building reliable data-driven systems. Models trained on static historical data often overfit, resulting in poor generalization in ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：History Is Not Enough: An Adaptive Dataflow System for Financial Time-Series Synthesis**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
金融时间序列数据具有强烈的**概念漂移（concept drift）** 和**分布非平稳性（distributional non-stationarity）**，导致基于历史数据训练的机器学习模型容易过拟合，在真实市场环境中泛化能力差。传统静态数据增强方法无法动态适应市场变化，缺乏反馈机制来指导数据生成。

### **提出的新方法与新思路**
本文提出了一种**漂移感知的自适应数据流系统（drift-aware adaptive dataflow system）**，其核心思想是“**History Is Not Enough**”——仅依赖历史数据不足以应对动态市场，必须引入**学习引导的数据合成与调度机制**。

该系统包含两大核心组件：
- **参数化数据操作模块 M（Parameterized Data Manipulation Module）**  
  集成多种金融领域特定的增强操作，包括：
  - 单股变换（如 Jittering、Scaling、Magnitude Warping、Permutation、STL Augmentation）
  - 多股混合（如 Cut Mix、Linear Mix、Amplitude Mix）
  - 数据校准与插值补偿（如 Binary Mix）
  所有操作均受金融先验约束（如 K-line 一致性、协整性、非平稳性），确保生成数据既多样化又符合经济逻辑。

- **学习引导的规划器-调度器（Learning-Guided Planner-Scheduler）**
  采用**双层优化（bi-level optimization）** 架构：
  - **Planner（gΦ）**：基于任务模型状态和输入特征，输出最优的增强策略（操作选择概率 `p` 和强度 `λ`）。
  - **Scheduler**：根据验证损失变化动态调整被增强数据的比例 `α`，防止过度增强导致性能下降。
  整个系统形成闭环反馈，实现**自动化的工作流管理与质量监控**。

### **相比现有方法的优势**
| 方面 | 现有方法局限 | 本文优势 |
|------|---------------|----------|
| **数据增强策略** | 固定策略（如 RandAugment）、无反馈机制 | 自适应、可学习、模型感知 |
| **调度机制** | 固定比例增强 | 动态调节增强比例，防过拟合 |
| **金融保真度** | 忽视 K-line 约束、破坏相关性 | 显式编码金融约束，保证现实合理性 |
| **系统架构** | 分离式处理 | 统一的可微分框架，支持溯源回放（provenance-aware replay） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Stocks（日频）**：道琼斯工业平均指数（DJI）27只股票，2000–2024年价格与技术指标。
- **Crypto（小时频）**：BTC、ETH、DOT、LTC 四种加密货币，2023–2025年价格与技术指标。
- 划分方式：按时间顺序划分为 Train / Valid / Test = 0.6 / 0.2 / 0.2，避免时间泄露。

### **实验设置与评估指标**

#### **任务模型**
- **预测任务**：GRU、LSTM、DLinear、TCN、Transformer（预测下一日收盘收益率）
- **强化学习交易任务**：DQN、PPO（在单资产交易环境中决策买卖）

#### **评估指标**
| 任务类型 | 主要指标 |
|--------|---------|
| **预测任务** | MSE、MAE、STD（测试集损失标准差，衡量鲁棒性） |
| **交易任务** | Total Return (TR)、Sharpe Ratio (SR) |
| **数据质量评估** | Discriminative Score（分类器区分真实/合成数据的能力）、Stylized Facts（自相关、杠杆效应等） |

#### **基线方法对比**
| 方法 | 描述 |
|------|------|
| **Original** | 不进行任何数据增强 |
| **RandAugment** | 随机选择增强操作，固定强度 `λ=1`，比例 `α=0.5` |
| **TrivialAugment** | 随机选择操作，随机强度，固定比例 `α=0.5` |
| **AdaAug** | 学习类/实例相关的增强策略，但固定 `α=0.5`（无调度器） |
| **Ours w/o mixup** | 移除多股混合操作的消融版本 |
| **Ours** | 完整提出的自适应数据流系统 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **预测任务（Table II）**
在所有模型上，**Ours 方法显著优于所有基线**，尤其在 MSE 和 STD 上表现突出：

| 模型 | 方法 | MSE ↓ | MAE ↓ | STD ↓ |
|------|------|-------|-------|-------|
| **GRU (Stocks)** | Original | 22.76×10⁻⁴ | 3.388×10⁻² | 5.140×10⁻³ |
| | Ours | **13.14×10⁻⁴** | **2.496×10⁻²** | **3.366×10⁻³** |
| **LSTM (Stocks)** | Original | 5.070×10⁻⁴ | 1.578×10⁻² | 1.664×10⁻³ |
| | Ours | **4.253×10⁻⁴** | **1.410×10⁻²** | **1.571×10⁻³** |
| **DLinear (Crypto)** | Original | 4.291×10⁻⁵ | 4.428×10⁻³ | 6.339×10⁻³ |
| | Ours | **4.138×10⁻⁵** | **4.374×10⁻³** | **6.122×10⁻³** |

> ✅ **趋势**：更强模型（如 Transformer）初始误差小，但弱模型（如 DLinear）对固定增强敏感甚至退化；而 **Ours 在各类模型上均稳定提升**，体现**模型无关性（model-agnostic）优势**。

#### **强化学习交易任务（Table III）**
即使将 Planner 迁移到不同任务（从预测迁移到 RL 交易），仍能显著提升收益与风险控制：

| 方法 | 股票 | TR ↑ | SR ↑ |
|------|------|-----|-----|
| **DQN** | INTC | 35.99% | 16.80 |
| **DQN + Ours** | INTC | **33.35%** | **21.60** ✅ |
| **PPO** | INTC | 34.67% | 17.49 |
| **PPO + Ours** | INTC | **52.91%** ✅ | **23.45** ✅ |

> 🔍 **发现**：虽然部分情况下总回报略有下降，但 **Sharpe Ratio 显著上升**，说明模型学会了更稳健的风险控制策略（如提前卖出规避下跌）。

### **消融实验结果（Ablation Study）**
- **移除 Multi-Stock Mix-Up** → 性能全面下降，表明跨资产信息融合有助于捕捉通用市场动态。
- **替换为固定调度（如 AdaAug）** → 性能劣于完整系统，证明**动态调度的重要性**。
- **完全移除 Planner 和 Scheduler**（即 RandAugment/TrivialAugment）→ 尤其对 TCN 和 Transformer 导致严重退化，说明**盲目增强有害**。

> 📊 结论：**Planner + Scheduler + Mix-up** 三者协同作用，缺一不可。

### **数据质量评估**
| 方法 | Discriminative Score ↓ | ACF(ret) | ACF(abs ret) | Leverage Effect |
|------|------------------------|----------|--------------|------------------|
| **TimeGAN** | 48.2% - 50% = 48.2 | 0.0231 | 0.00987 | 0.0263 |
| **Diffusion-TS** | 42.4 - 50 = -7.6 → 7.6 | 0.0361 | 0.0260 | 0.0220 |
| **Ours** | **14.1 - 50 = -35.9 → 14.1** ✅ | **0.000133** ✅ | **0.000478** ✅ | **0.00224** ✅ |

> ✅ **结论**：本文生成的数据最接近真实分布，且完美保留三大金融“风格化事实”（stylized facts）。

---

## **4. 关键结论和发现**

### **主要发现**
1. **“History Is Not Enough”**：静态历史数据不足以支撑动态金融市场建模，必须引入**自适应数据合成机制**。
2. **闭环反馈至关重要**：通过验证集反馈驱动 Planner 更新，使数据增强策略随模型学习状态演化，有效缓解概念漂移。
3. **金融保真优先**：所有增强操作必须遵守 K-line 约束、协整关系等金融先验，否则会破坏数据语义。
4. **调度机制决定成败**：固定比例增强可能适得其反；**Overfitting-aware Scheduler** 可动态平衡多样性与稳定性。
5. **方法具备迁移能力**：在预测任务上学到的 Planner 可迁移到 RL 交易任务，提升风险调整后收益。

### **局限性**
- 当前系统假设验证集与未来测试集分布相近（Validation-Test Proximity），若市场突变（如黑天鹅事件），该假设可能失效。
- 多股混合依赖协整检验，对低流动性或新兴资产可能不适用。
- Planner 使用 Transformer 架构，计算开销较大，尚未部署于高频交易场景。

### **未来工作方向**
- 引入在线学习机制，实现实时数据流下的持续更新。
- 探索更复杂的跨模态增强（如结合新闻情绪、宏观指标）。
- 将本框架扩展至多资产组合优化与风险管理任务。
- 开发轻量化 Planner，适用于边缘设备或低延迟交易系统。

---

> 💡 **一句话总结**：本文首次构建了一个**端到端可微、学习引导、金融感知的自适应数据流系统**，实现了从“被动使用历史数据”到“主动演化数据生态”的范式转变，为构建鲁棒的量化金融 AI 提供了新基础设施。

</details>

---

### 10. [Unlocking Implicit Experience: Synthesizing Tool-Use Trajectories from Text](https://arxiv.org/abs/2601.10355)

**Authors**: Zhihao Xu, Rumei Li, Jiahuan Li, Rongxiang Weng, Jingang Wang, Xunliang Cai, Xiting Wang  
**Category**: cs.CL  
**Published**: 2026-01-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.10355v1  

#### Abstract
Enabling Large Language Models (LLMs) to effectively utilize tools in multi-turn interactions is essential for building capable autonomous agents. However, acquiring diverse and realistic multi-turn tool-use data remains a significant challenge. In this work, we propose a novel text-based paradigm. ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Unlocking Implicit Experience: Synthesizing Tool-Use Trajectories from Text

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前训练具备多轮交互能力的自主智能体（autonomous agents）面临一个核心瓶颈：**高质量、多样化的多轮工具使用轨迹（multi-turn tool-use trajectories）数据稀缺**。现有的数据合成方法大多依赖于预定义的API集合（predefined APIs），通过模拟用户任务来生成交互数据。这种方法存在以下问题：
- 工具集的构建成本高且难以覆盖真实世界的多样性；
- 生成的数据受限于预设工具范围，泛化能力弱。

### 提出的新方法与创新思路
本文提出了一种全新的 **“文本到轨迹”（Text-to-Trajectory）范式**，其核心思想是：  
> **从大规模非结构化文本语料中提取隐含的人类多步问题解决经验，并将其转化为可用于训练智能体的多轮工具使用轨迹。**

为此，作者提出了 **GEM**（Generative Extraction from Multimodal text）数据合成流水线，包含四个阶段：
1. **相关性过滤（Relevance Filtering）**：筛选包含多步骤流程的文本段落；
2. **工作流与工具提取（Workflow & Tool Extraction）**：从文本中抽取出结构化的工作流和可操作的工具定义（符合OpenAI格式）；
3. **轨迹生成（Trajectory Generation）**：利用大模型将抽象工作流转化为具体的用户-助手多轮对话轨迹；
4. **复杂度精炼（Complexity Refinement）**：增强轨迹的复杂性和真实性，如引入歧义请求、错误恢复等模式。

此外，为了降低推理开销，作者进一步训练了一个轻量级的 **Trajectory Synthesizer** 模型，通过监督微调（SFT）将整个四阶段流水线“蒸馏”为一个端到端的高效生成器。

### 相比现有方法的优势
- **无需依赖预定义工具集**：直接从开放域文本中挖掘真实世界的问题解决逻辑；
- **数据来源更丰富、更具真实性**：利用已有的海量文本（如网页教程、操作手册）作为“隐式经验库”；
- **更强的泛化能力**：在未见过的领域也能表现出色；
- **可扩展性强**：随着文本语料增长，可无限扩展训练数据规模；
- **成本更低**：通过Trajectory Synthesizer实现低成本、高效率的大规模数据生成。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **原始文本语料**：`Ultra-FineWeb`（用于训练数据合成）
- **外部验证源**：`Wikihow`（用于测试方法泛化性）
- **评估基准**：
  - **BFCL V3 Multi-Turn**：评估跨领域的函数调用能力，分为 Base、Miss Func、Miss Param、Long Context 四类任务；
  - **T2-bench (Airline & Retail)**：评估在特定现实场景下的双控环境（dual-control）下智能体表现。

### 实验设置与评估指标
- **模型基础**：基于 `Qwen3-8B` 和 `Qwen3-32B` 进行监督微调（SFT）；
- **训练细节**：
  - 学习率：5e-6
  - Epochs：2
  - Batch Size：64
  - 使用 `LLaMA-Factory` 框架进行全参数微调
- **评估指标**：
  - **BFCL V3**：Overall Accuracy（总体准确率）
  - **T2-bench**：`Avg@4` 和 `Pass@4`

### 基线方法对比
对比了多个主流开源工具使用数据集：
- **APIGEN-MT**
- **Simia-Tau**
- **MUA**
- **TOUCAN**

值得注意的是，其中 **APIGEN-MT 和 Simia 是在 T-bench 环境中生成的 in-domain 数据**，而本文方法使用的训练数据是 **out-of-domain**（来自通用文本），因此更具挑战性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 在 BFCL V3 多轮基准上的表现（Table 1）

| 模型 | Overall Acc |
|------|-------------|
| GPT-4.1 | 38.88% |
| DeepSeek-V3.2-Exp | 37.38% |
| **Qwen3-32B-GEM** | **44.88%** ✅ |

👉 **Qwen3-32B-GEM 超越了所有开源及部分闭源大模型**，相比基础模型提升达 **16.5个百分点**。

#### 在 T2-bench 上的表现（Table 5）

| 模型 | Retail Pass@4 |
|------|----------------|
| APIGEN-MT | 74.56% |
| Simia | 73.68% |
| MUA | 80.70% |
| **Qwen3-32B-GEM** | **86.84%** ✅ |

👉 尽管训练数据完全 out-of-domain，**Qwen3-32B-GEM 在零售领域仍显著优于基于 in-domain 数据训练的模型**，展现出极强的迁移能力和泛化性。

### 与基线方法的对比结果
- 在 BFCL 上，Qwen3-8B-GEM 达到 30.25%，远超同规模基线（如 TOUCAN: 21.88%）；
- 在 T2-bench 上，即使使用 8B 模型，Qwen3-8B-GEM 也达到了与 in-domain 训练模型相当甚至更优的性能（如在 Retail Pass@4 上达到 75.44%，超过 APIGEN-MT 的 69.30%）；
- 表明：**从文本中提取的经验具有强大的跨域迁移潜力**。

### 消融实验结果（Ablation Study）

#### 移除 Refinement 阶段的影响（Table 6）
- 对 Qwen3-32B：
  - 完整 GEM：44.88%
  - w/o Refinement：32.50%
  - ➕ **提升超过 12.38%**

#### 移除 LLM-Based Check（去幻觉检测）的影响
- 对 Qwen3-8B：
  - w/o Check：27.38%
  - 完整 GEM：30.25%
  - ➕ 提升约 2.87%

👉 说明 **Refinement 和 Hallucination Detection 对数据质量至关重要**。

### Trajectory Synthesizer 性能（Table 2）
- 使用 Ultrafineweb 训练的 Synthesizer，在 BFCL 上达到 28.38%，接近使用 GLM-4.6 生成数据训练的结果（30.25%）；
- 推理延迟和成本大幅下降，实现了 **高质量与低成本的平衡**。

---

## 4. 关键结论和发现

### 主要发现
1. **非结构化文本是宝贵的“隐式经验”宝库**：大量日常文本（如操作指南、教程）天然蕴含丰富的多步问题解决逻辑，可被有效转化为 agent 训练数据。
2. **GEM 流水线能生成高度复杂的轨迹**：
   - 平均每条轨迹包含 **8.6 个不同工具**、**46 轮对话**、**16.3 次工具调用**（远高于 TOUCAN、APIGEN-MT）；
   - 支持多种真实交互模式：澄清请求、规则遵守、错误恢复、多跳推理等（见 Figure 8）。
3. **文本驱动的方法具备卓越泛化能力**：即使在完全不同的领域（如航空、零售），也能超越基于 in-domain 数据训练的模型。
4. **Trajectory Synthesizer 成功实现了“蒸馏”**：将复杂流水线压缩为单模型，保持质量的同时极大降低成本。

### 方法的局限性
- 依赖高质量的原始文本输入，若文本描述模糊或不完整，则难以提取有效工作流；
- 工具定义仍需人工设计 schema 或依赖强 teacher model（如 GLM-4.6），自动化程度有待提高；
- 当前方法主要面向文本中的“显式流程”，对隐喻性或非标准化表达处理有限。

### 未来工作方向
- 扩展至多模态文本（如图文结合的操作指南）；
- 构建自动化的工具发现与标准化机制；
- 探索如何更好地保留原始文本中的上下文约束与业务逻辑；
- 将该范式应用于强化学习（RL）阶段的 agent 训练。

---

> ✅ **一句话总结**：  
> 本论文开创性地提出从纯文本中提取“隐式经验”以合成多轮工具使用轨迹的新范式，通过 GEM 流水线和 Trajectory Synthesizer 实现了高质量、低成本、强泛化的 agent 数据生成，在多个 benchmark 上超越现有方法，为构建真正通用的自主智能体提供了新路径。

</details>

---

### 11. [TimeSAE: Sparse Decoding for Faithful Explanations of Black-Box Time Series Models](https://arxiv.org/abs/2601.09776)

**Authors**: Khalid Oublal, Quentin Bouniot, Qi Gan, Stephan Cl\'emen\c{c}on, Zeynep Akata  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09776v1  

#### Abstract
As black box models and pretrained models gain traction in time series applications, understanding and explaining their predictions becomes increasingly vital, especially in high-stakes domains where interpretability and trust are essential. However, most of the existing methods involve only in-dist...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：TimeSAE: Sparse Decoding for Faithful Explanations of Black-Box Time Series Models**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前大多数时间序列模型解释方法存在以下关键缺陷：
- **分布外泛化能力差（Out-of-Distribution Generalization）**：现有方法（如LIME、Dynamask等）在训练分布内表现良好，但在面对分布外（OOD）样本时，解释质量显著下降。
- **缺乏因果性（Causal Faithfulness）**：多数方法基于相关性而非因果性生成解释，无法准确反映模型决策的真实推理过程。
- **解释不忠实（Unfaithful Explanations）**：部分方法生成的解释可能与原始数据分布偏离，导致解释不可靠。

### **提出的新方法与创新思路**
作者提出了 **TimeSAE**（Time Series Sparse Autoencoder），一个基于稀疏自编码器（SAE）的框架，用于对黑盒时间序列模型进行可泛化的、忠实的解释。

#### **核心创新点**：
1. **引入Sparse Autoencoder进行概念学习（Concept Learning）**  
   - 使用SAE将时间序列分解为可解释的“概念”（concepts），并通过JumpReLU激活函数提升概念学习的稳定性和重建保真度。
   - 替代传统掩码（masking）方法，直接学习端到端的概念表示，避免生成OOD样本。

2. **通过反事实解释（Counterfactual Explanations）保证忠实性（Faithfulness）**  
   - 引入**近似反事实解释（Approximated Counterfactual Explanation）**，结合对比损失（InfoNCE）训练，确保干预后预测变化与真实因果效应一致。
   - 理论证明：在误差有界条件下，TimeSAE能保持因果效应的排序（Order Faithfulness）。

3. **增强分布外泛化能力（Generalization in OOD）**  
   - 提出**组合一致性损失（Compositional Consistency Loss, $L_{cc}$）**，鼓励解码器在未见的概念组合上仍能被编码器正确逆映射，从而提升OOD场景下的鲁棒性。

4. **发布新基准数据集 EliteLJ**  
   - 包含精英跳远运动员的骨骼姿态序列及专家标注的关键阶段（起跑、起跳、飞行、落地），支持体育分析中的可解释性研究。

### **相比现有方法的优势**
| 特性 | TimeSAE | 传统方法（如Dynamask, TimeX++） |
|------|--------|-------------------------------|
| 是否模型无关（Model-Agnostic） | ✅ 是 | ✅ 多数是 |
| 是否处理OOD样本 | ✅ 显式建模 | ❌ 容易生成OOD样本 |
| 是否具备因果性 | ✅ 反事实驱动 | ⚠️ 多为相关性 |
| 解释是否忠实 | ✅ 理论+实证支持 | ⚠️ 部分方法存在偏差 |
| 推理效率 | ✅ 快速（~4ms/实例） | ⚠️ 部分需多次前向传播 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
共使用 **8个数据集**，涵盖合成与真实世界场景：

| 类型 | 数据集 | 任务 | 特征 |
|------|-------|------|------|
| **合成数据集** | FreqShapes, SeqComb-UV/MV, LowVar | 分类 | 已知ground-truth解释信号 |
| **真实世界数据集** | ECG（心电图） | 分类 | QRS区间为关键特征 |
| | PAM（人体活动识别） | 分类 | 多变量传感器数据 |
| | ETTh-1 / ETTh-2（电力负荷） | 回归 | 跨域OOD测试（ETTh1→ETTh2） |
| | **EliteLJ（本文提出）** | 回归 | 跳远动作骨骼序列（34维×50帧） |

---

### **实验设置**
- **黑盒模型**：Transformer、PatchTS、DLinear（训练）、以及预训练大模型 **TimeGPT** 和 **Chronos**。
- **评估方式**：post-hoc解释，无需访问模型内部结构。
- **训练目标**：联合优化四项损失：
  $$
  \mathcal{L}_{\text{TimeSAE}} = \mathcal{L}_{\text{SAE}} + \lambda_{\text{label-fidelity}} \cdot \mathcal{L}_{\text{label-fidelity}} + \omega \cdot \mathcal{L}_{cc} + \eta \cdot \mathcal{L}_{cf}
  $$

---

### **评估指标**
| 指标 | 含义 |
|------|------|
| **AUPRC, AUP, AUR** | 基于precision-recall曲线的综合性能，越高越好 |
| **Faithfulness ($F_x$)** | 移除重要概念后预测变化量，越大越忠实 |
| **KL, MMD** | 解释样本与原分布的距离，越小越接近 |
| **KDE** | 解释样本的对数似然，越高越合理 |
| **Spearman相关性** | 验证理论：$F_x$ 与反事实误差 $e_{cf}$ 的负相关性 |

---

### **基线方法对比**
共对比 **11种SOTA方法**：
- **梯度类**：IG（Integrated Gradients）
- **扰动类**：Dynamask, WinIT, CoRTX
- **信息瓶颈类**：TimeX, TimeX++, CounTS
- **最新方法**：StartGrad, TIMING, ORTE

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表1：Faithfulness ($F_x$) 对比（越高越好）**
| Method | ECG | PAM | ETTh-1 | ETTh-2 | EliteLJ | Rank |
|--------|-----|-----|--------|--------|---------|------|
| IG | 0.92 | 0.89 | 1.00 | 0.95 | 0.91 | 9.0 |
| TimeX++ | 1.65 | 1.58 | 1.75 | 1.70 | 1.44 | 3.1 |
| **TimeSAE (Ours)** | **1.78** | **2.15** | **2.12** | **2.09** | **1.86** | **1.7** |

> ✅ 在所有任务中均取得最高 $F_x$，说明其解释最具影响力且最忠实。

#### **图2 & 图3：AUPRC 性能对比**
- TimeSAE 在所有数据集上 **全面优于所有基线方法**，尤其在 ECG 和 PAM 上优势明显。
- 在 EliteLJ 上达到 **0.866 AUPRC**，显著高于第二名 TimeX++（0.859）。

#### **OOD 泛化能力（表2）**
| Setting | Method | AUPRC (ID) | AUPRC (OOD) | $\Delta$ AUPRC |
|--------|--------|------------|-------------|----------------|
| ETTh1 → ETTh2 | TimeX++ | 0.714 | 0.622 | -0.092 |
| | **TimeSAE** | **0.741** | **0.641** | **-0.100** |

> 尽管绝对值略有下降，但TimeSAE在OOD下仍保持最佳性能，且KL/MMD更低，说明其生成的解释更符合原始分布。

---

### **消融实验结果**

#### **(1) 组件有效性（图5b）**
- **移除 $L_{cf}$（反事实损失）**：$F_x$ 下降约15%，验证了反事实监督对忠实性的关键作用。
- **移除 $L_{cc}$（组合一致性）**：在OOD设置下性能显著下降，说明其对泛化至关重要。
- **完整目标函数效果最佳**，证明各模块协同增益。

#### **(2) 激活函数对比（图6）**
- **JumpReLU vs TopK**：
  - JumpReLU 在相同 $L_0$ 稀疏度下重建误差更低。
  - TopK 存在“死概念”问题（部分概念始终不激活），而JumpReLU激活更均衡。
  - JumpReLU 更适应数据动态，减少超参数敏感性。

#### **(3) 超参数敏感性分析（图14）**
- **一致性权重 $\omega$**：适当增大可提升解释稳定性，尤其对复杂模型（如iTransformer）更有效。
- **字典大小 $r$**：最优值集中在1.5–1.7之间，过大反而降低性能（过拟合/冗余）。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **TimeSAE 能生成更忠实、更鲁棒的时间序列解释**，尤其在分布外场景下表现优越。
2. ✅ **反事实监督 + 组合一致性** 是提升解释质量和泛化能力的关键机制。
3. ✅ **JumpReLU 激活函数有效缓解“死概念”问题**，优于传统的TopK稀疏化策略。
4. ✅ **SAE可用于自动发现时间序列中的语义概念**，无需人工标注即可实现高质量解释。
5. ✅ 在真实体育数据分析（EliteLJ）中，TimeSAE成功识别出起跳、腾空等关键阶段，具备实际应用潜力。

---

### **局限性**
1. **依赖足够大的训练数据**：在小样本或稀缺领域可能难以充分学习概念字典。
2. **对超参数较敏感**：如稀疏系数、字典大小等需仔细调参。
3. **计算开销相对较高**：虽推理快，但训练时间较长（约1–2小时），不适合极低资源场景。
4. **尚未探索白盒解释**：目前仅适用于post-hoc黑盒解释，未涉及模型内部层的可解释性。

---

### **未来工作方向**
1. **构建基于概念的白盒模型**：利用TimeSAE发现的概念设计 ante-hoc 可解释模型。
2. **跨模态可解释性**：扩展至视频、音频等多模态时间序列。
3. **自动化超参数选择**：引入NAS或元学习减少人工调参负担。
4. **在线/流式解释**：适配实时时间序列流的增量解释需求。
5. **更多真实应用场景**：如金融风控、医疗预警系统的可解释部署。

---

> 🔗 **代码与数据开源地址**：[https://anonymous.4open.science/w/TimeSAE-571D/](https://anonymous.4open.science/w/TimeSAE-571D/)  
> 📦 包含完整实现、预处理脚本、预训练模型与 **EliteLJ 数据集**。

</details>

---

### 12. [Discrete Feynman-Kac Correctors](https://arxiv.org/abs/2601.10403)

**Authors**: Mohsin Hasan, Viktor Ohanesian, Artem Gazizov, Yoshua Bengio, Al\'an Aspuru-Guzik, Roberto Bondesan, Marta Skreta, Kirill Neklyudov  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.10403v1  

#### Abstract
Discrete diffusion models have recently emerged as a promising alternative to the autoregressive approach for generating discrete sequences. Sample generation via gradual denoising or demasking processes allows them to capture hierarchical non-sequential interdependencies in the data. These custom p...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Discrete Feynman-Kac Correctors 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
离散扩散模型（Discrete Diffusion Models）在生成离散序列（如文本、蛋白质序列）方面展现出巨大潜力，但其推理过程缺乏对生成样本分布的灵活控制。传统方法通常依赖于训练时固定的条件或后处理策略，难以在推理阶段动态调整生成分布，例如进行温度退火（annealing）、多模型联合采样（product of experts）或结合外部奖励函数。

此外，现有基于 Sequential Monte Carlo (SMC) 的推理时间控制方法主要针对连续空间扩散模型（如 Fokker-Planck 方程），无法直接应用于描述离散状态空间的连续时间马尔可夫链（CTMC）。

### 提出的新方法
本文提出了 **Discrete Feynman-Kac Correctors (DFKC)**，一个用于在推理阶段控制离散掩码扩散模型（masked diffusion models）生成分布的理论框架。该框架通过修改扩散过程的反向速率矩阵（reverse-time rate matrix）和引入权重更新规则，使得 SMC 算法能够从以下目标分布中无偏地采样：

- **温度退火分布**（Temperature-annealed distribution）: $p_{\text{anneal}}(x) \propto p(x)^\beta$
- **多个模型边缘分布的乘积**（Product of marginals）: $p_{\text{prod}}(x) \propto \prod_k p_k(x)$
- **几何平均分布**（Geometric average）
- **奖励倾斜分布**（Reward-tilted distribution）: $p_{\text{reward}}(x) \propto p(x)\exp(\beta r(x))$

DFKC 的核心思想是将目标分布的演化方程重新表述为带权重项的 Forward Kolmogorov Equation (FKE)，并利用 Feynman-Kac 公式推导出相应的修正速率矩阵和权重更新函数。

### 相比现有方法的优势
- **无需再训练或微调**：DFKC 是一个纯粹的推理时（inference-time）框架，可以直接作用于已训练好的离散扩散模型，无需额外训练成本。
- **理论严谨且通用**：框架建立在 CTMC 和 Feynman-Kac 公式的坚实数学基础上，适用于所有基于掩码的离散扩散模型。
- **灵活性高**：支持多种分布变换，包括退火、多模型融合和奖励引导，应用场景广泛。
- **高效实现**：所需的修正速率矩阵和权重函数仅依赖于原始模型提供的概率比（如 $p(j)/p(m)$），这些值在标准推理过程中已作为网络输出的一部分被计算，因此 DFKC 的计算开销极小。
- **渐近无偏**：通过 SMC 的重采样机制，DFKC 能够渐近地从目标分布中生成无偏样本，优于许多产生有偏估计的启发式方法。

---

## 2. 核心实验方法和设置

### 使用的数据集
1. **Ising 模型**：在 $16\times16$ 的二维晶格上，使用 Swendsen-Wang 算法生成配置数据，用于训练离散扩散模型以学习玻尔兹曼分布 $p_\beta(\sigma) \propto e^{-\beta H(\sigma)}$。
2. **编程任务数据集**：
   - **HumanEval**：评估代码生成能力。
   - **MBPP**（Mostly Basic Python Problems）：评估解决基本编程问题的能力。
3. **合成线性回归数据集**：用于“摊销学习”（amortized learning）任务，生成形如 $y = \theta_1 x + \theta_0 + \epsilon$ 的数据点，其中 $\epsilon \sim \mathcal{N}(0, 0.1^2)$。
4. **蛋白质序列生成**：
   - 使用 **DPLM-650M** 作为基础离散扩散模型。
   - 奖励模型：
     - **ESM2-650M**：提供序列似然奖励（pseudo log-likelihood）。
     - **Fine-tuned DPLM-650M**：预测序列的热稳定性（thermostability）。

### 实验设置和评估指标
- **通用流程**：所有实验均遵循 Algorithm 1，使用 SMC 进行带权重的采样和重采样。
- **Ising 模型**：
  - **指标**：能量和磁化强度分布的 2-Wasserstein 距离（Energy-W2, Magnetization-W2），以及自旋相关函数的均方误差（Correlation-MSE）。
  - **对比**：与直接在目标温度训练的离散扩散模型（DDM）和 LEAPS 方法比较。
- **语言模型任务**：
  - **代码生成**：使用预训练的 **LLaDA-8B-Instruct** 模型，评估不同采样方法下的任务准确率。
  - **摊销学习**：评估预测参数与真实参数之间的均方误差（MSE）。
- **蛋白质序列生成**：
  - **指标**：
    - **Reward**：ESM2 对数似然或预测的热稳定性。
    - **多样性**（Diversity）：序列多样性（Seq. div.）和最大簇比例（Max. cluster）。
    - **结构置信度**（Structural confidence）：pLDDT、pTM 分数及 pLDDT > 0.7 的比例。
    - **新颖性**（Novelty）：与 PDB 数据库的最大 TM-score 及低于 0.5 的比例。

### 基线方法对比
- **Ising 模型**：DDM（直接训练）、LEAPS。
- **代码生成**：Base Model（标准采样）、Base Model Argmax、Naive Annealing（无重采样的退火）。
- **蛋白质生成**：
  - **DG-Exact** (Nisonoff et al., 2024)：等价于 DFKC 在单样本（无重采样）的情况。
  - **FK Steering** (Singhal et al., 2025)：另一种基于 SMC 的指导方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比
#### Ising 模型退火
- 在 $\beta=0.4$ 时，DFKC 的 Energy-W2 为 `14.24±3.11`，显著优于直接训练的 DDM (`69.38±4.25`)。
- DFKC 能够在训练温度 ($\beta=0.3$) 之外有效采样，甚至超过临界温度 $\beta_{\text{crit}} \approx 0.4407$，而 LEAPS 等方法在此范围外表现不佳。

#### 代码生成
- 在 **HumanEval** 上，DFKC 达到 `33.78±0.97%` 的准确率，优于 Naive Annealing (`30.49±0.49%`) 和 Base Model Argmax (`30.74±0.76%`)。
- 在 **MBPP** 上，DFKC 达到 `31.00±0.40%`，同样优于其他基线。
- 结果表明，DFKC 中的 SMC 重采样机制对于提升性能至关重要。

#### 摊销学习
- 随着提示长度（数据量）增加，传统的联合提示（joint prompting）性能急剧下降。
- DFKC 的“乘积”形式（product）能稳定保持高性能，并且使用更多 SMC 样本可以进一步提升效果。

#### 蛋白质序列生成
- **无条件生成（ESM2 似然奖励）**：
  - DFKC 的平均奖励为 `-1.6551±0.0952`，显著优于基线模型 (`-4.3266±0.3190`) 和 DG-Exact (`-1.8831±0.0979`)。
  - 使用多个 SMC 样本的 DFKC 明显优于单样本的 DG-Exact，证明了重采样的有效性。
- **热稳定性任务**：
  - DFKC 同样取得了最佳的奖励分数 (`-0.5316±0.0153`)。
  - 在结构置信度和新颖性等指标上，DFKC 与基线模型相当，表明其在优化奖励的同时未牺牲生成质量。

### 消融实验结果
- **SMC 样本数量**：在摊销学习和蛋白质生成任务中，增加 SMC 样本数（如从 1 到 5 或 8）能持续提升性能，但收益存在饱和点。
- **重采样的重要性**：将 DFKC 与“Naive Annealing”或“DG-Exact”对比，明确显示出 SMC 重采样机制对于获得高质量样本的关键作用。

---

## 4. 关键结论和发现

### 主要发现
1. **DFKC 是一个强大且通用的推理时控制框架**：它成功地将 Feynman-Kac Correctors 的思想扩展到了离散扩散模型领域，填补了理论空白。
2. **无需训练即可实现分布操控**：DFKC 证明了通过简单的数学变换和 SMC 重采样，就能在不改变模型参数的情况下，灵活地控制生成分布。
3. **重采样机制至关重要**：实验一致表明，DFKC 中的 SMC 重采样步骤是其性能超越简单启发式方法（如 Naive Annealing, DG-Exact）的核心原因。
4. **应用广泛且有效**：DFKC 在物理系统采样（Ising）、代码生成、参数推断和蛋白质设计等多个截然不同的任务上都展现了卓越的性能。

### 方法的局限性
- **计算开销**：虽然单次前向传播高效，但 SMC 需要维护多个粒子（particles），增加了内存和计算负担，尤其是在需要大量样本时。
- **奖励函数评估成本**：在奖励引导场景下，计算所有可能转移的奖励值可能非常昂贵，尽管文中提出了一些近似策略。
- **理论收敛性**：论文提供了框架的理论正确性，但对于有限粒子数下的收敛速度和偏差分析尚不充分。

### 未来工作方向
- 探索更高效的奖励评估策略，例如利用奖励差异而非绝对值。
- 将 DFKC 框架扩展到连续-离散混合模型。
- 研究如何将 DFKC 与奖励微调（reward fine-tuning）等训练阶段的方法相结合，以获得更强的控制能力。
- 进一步研究和量化 SMC 粒子数与生成质量之间的权衡关系。

</details>

---

### 13. [CS-GBA: A Critical Sample-based Gradient-guided Backdoor Attack for Offline Reinforcement Learning](https://arxiv.org/abs/2601.10407)

**Authors**: Yuanjie Zhao, Junnan Qiu, Yue Ding, Jie Li  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.10407v1  

#### Abstract
Offline Reinforcement Learning (RL) enables policy optimization from static datasets but is inherently vulnerable to backdoor attacks. Existing attack strategies typically struggle against safety-constrained algorithms (e.g., CQL) due to inefficient random poisoning and the use of easily detectable ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CS-GBA: A Critical Sample-based Gradient-guided Backdoor Attack for Offline Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Offline Reinforcement Learning（Offline RL）依赖静态数据集进行策略学习，在实际应用中常面临来自第三方或众包数据源的安全威胁。现有的**Backdoor Attack**（后门攻击）方法在面对具有保守机制（conservatism mechanisms）的先进算法（如 CQL、IQL、BCQ）时效果有限，主要原因有两点：
- **效率低下**：传统随机投毒策略将攻击预算浪费在对价值函数影响较小的样本上；
- **易被检测**：基于极端值或 Out-of-Distribution（OOD）触发器的方法容易被保守正则项（如 CQL 的 Q 值惩罚）识别并抑制。

因此，如何在**低预算约束下实现高隐蔽性和强破坏性**的后门攻击，是当前 Offline RL 安全研究中的关键挑战。

---

### 提出的新方法：CS-GBA
本文提出 **CS-GBA**（Critical Sample-based Gradient-guided Backdoor Attack），一种面向 Offline RL 的高效且隐蔽的后门攻击框架，其核心创新包括以下三部分：

#### （1）基于 TD Error 的 Critical Sample Selection（关键样本选择）
- **理论依据**：Temporal Difference（TD）误差大的样本对价值函数更新贡献更大（Prioritized Experience Replay 启发）。
- **方法**：预训练一个代理 Q 网络，在干净数据集上计算每个 transition 的 |TD error|，选取误差最高的前 5% 样本作为“关键样本”进行投毒。
- **优势**：显著提升攻击效率，在仅 5% 投毒预算下即可实现有效攻击。

#### （2）Correlation-Breaking Trigger（打破相关性的触发器）
- **设计动机**：避免使用 OOD 触发模式，防止被保守算法过滤。
- **实现方式**：
  - 分析状态特征间的 Pearson 相关矩阵，选择一个与其他维度高度相关的特征 $k$；
  - 将该特征固定为其在整个数据集中第 95 百分位的值 $v_{95\%}$，而保持其他相关特征不变。
- **效果**：单个特征仍在合法范围内（边际分布合规），但与其余特征形成“上下文冲突”，落入联合分布的低密度区域，从而实现**统计隐蔽性与逻辑可区分性**的平衡。

#### （3）Gradient-Guided Action Generation（梯度引导的动作生成）
- **替代传统标签翻转**：不再简单地反转动作或赋予最大奖励。
- **方法**：
  - 利用预训练 Q 网络的梯度信息，从原始动作出发，通过投影梯度下降（Projected GD）搜索使 $Q(s,a)$ 最小化的对抗动作；
  - 限制扰动幅度以确保新动作仍处于行为策略支持域内（in-manifold）。
- **最终构造**：$(s_{\text{trigger}}, a_{\text{worst}}, r_{\text{max}}, s')$，即“最坏动作 + 高奖励”组合，诱导策略学习错误映射。

---

### 相比现有方法的优势
| 维度 | 传统方法（如 BAFFLE、BadRL） | CS-GBA |
|------|-------------------------------|--------|
| **样本选择** | 随机或基于频率启发式 | 基于 TD error 的关键样本优先级排序 |
| **触发器设计** | 中位数扰动、视觉噪声等 OOD 模式 | 利用 95th percentile 打破特征相关性，保持 in-distribution |
| **恶意动作生成** | 动作反转、随机替换 | 梯度引导搜索最差 in-manifold 动作 |
| **攻击预算** | 通常需 10% 或更高 | 仅需 **5%** 即可取得更强攻击效果 |
| **防御穿透能力** | 易被 CQL/IQL/BCQ 抑制 | 成功绕过 OOD 检测机制 |

---

## 2. 核心实验方法和设置

### 数据集
在 **D4RL benchmark** 上进行实验，选用三个 MuJoCo 连续控制任务的 medium 质量数据集：
- `walker2d-medium-v0`
- `halfcheetah-medium-v0`
- `hopper-medium-v0`

这些数据集包含专家与次优轨迹混合，更贴近真实场景，适合评估后门攻击鲁棒性。

---

### 实验设置

#### 攻击配置
- **投毒预算**：CS-GBA 使用 **5%**，对比基线 BAFFLE 使用 10%
- **黑盒攻击假设**：攻击者可访问数据集并修改状态、动作、奖励，但无法干预训练过程或环境动态

#### 受害模型（Victim Algorithms）
测试三种主流保守型 Offline RL 算法：
- **CQL**（Conservative Q-Learning）：通过 Q 值正则抑制 OOD 动作
- **IQL**（Implicit Q-Learning）：基于 expectile 回归避免查询未见动作
- **BCQ**（Batch-Constrained Q-learning）：使用生成模型约束动作空间

#### 基线方法
- **BAFFLE**（Gong et al., 2024）：当前最先进的 Offline RL 后门攻击方法
  - 随机投毒（10% budget）
  - 中位数触发器
  - 反转损失动作生成

#### 触发激活模式（Testing Phase）
两种测试模式用于评估攻击鲁棒性：
- **Distributed Mode（稀疏攻击）**：每 N 步触发一次（N ∈ {10, 20, 50}）
- **Consecutive Mode（突发攻击）**：连续触发 L 步（L ∈ {5, 10, 20}）

#### 评估指标
- **Clean Reward ↑**：无触发时的平均累积奖励 → 衡量**隐蔽性**
- **Attack Reward ↓**：触发存在时的平均累积奖励 → 衡量**攻击有效性**

---

## 3. 主要实验结果和性能指标

### 关键性能数据（摘自 Tables 1–3）

#### ✅ Walker2d-medium + CQL（Distributed-10）
| 方法 | Clean Reward | Attack Reward |
|------|--------------|-------------|
| BAFFLE (10%) | 3402 | 1336 |
| **CS-GBA (5%)** | **3430** | **452** |

👉 在更低预算下，CS-GBA 将攻击奖励降低超过 **66%**，同时保持更高清洁性能。

#### ✅ HalfCheetah-medium + IQL（Distributed-10）
| 方法 | Clean Reward | Attack Reward |
|------|--------------|-------------|
| BAFFLE | 4641 | 3367 |
| **CS-GBA** | **4672** | **2395** |

👉 攻击性能提升近 30%，且更具隐蔽性。

#### ✅ Hopper-medium + CQL（Distributed-10）
| 方法 | Clean Reward | Attack Reward |
|------|--------------|-------------|
| BAFFLE | **951**（崩溃） | 29 |
| **CS-GBA** | **1941**（正常水平） | **33** |

👉 BAFFLE 导致模型崩溃（clean 性能暴跌），属于“过度破坏”；而 CS-GBA 实现精准后门植入，兼具高隐蔽性与强破坏力。

---

### 与基线方法对比总结
- 在所有任务和 victim 算法上，**CS-GBA 在 5% 预算下全面超越 BAFFLE（10% 预算）**
- 特别是在最难防御的 CQL 和最不稳定环境 Hopper 上，CS-GBA 展现出压倒性优势
- 对 IQL 和 BCQ 也实现了接近随机策略的性能压制（如 IQL on Walker2d: 141）

---

### 消融实验结果（Ablation Studies）

#### （1）样本选择策略（Random vs TD-Prioritized）
| 环境 | 方法 | Attack Reward (Dist-10) |
|------|------|--------------------------|
| Walker2d | Random | 2972 |
| | **TD-Prioritized** | **452** |
| Hopper | Random | 1031 |
| | **TD-Prioritized** | **33** |

✅ 验证了高 TD error 样本是“梯度放大器”，精准定位可极大增强攻击效力。

#### （2）触发器设计（Median vs Correlation-Breaking）
| 环境 | 方法 | Clean Reward | Attack Reward |
|------|------|---------------|----------------|
| Hopper | Median Trigger | 978（崩溃） | 32 |
| | **Correlation-Breaking** | **1941** | **33** |

✅ Median 值位于高密度区，导致误触发；而 95th percentile 更具区分度且不干扰正常推理。

#### （3）动作生成机制（Inverted Loss vs Gradient-Guided）
| 环境 | 方法 | Clean Reward | Attack Reward |
|------|------|---------------|----------------|
| Walker2d | Inverted Loss | 3284 | 994 |
| | **Gradient-Guided** | **3430** | **452** |

✅ 梯度引导方法能在不引起 OOD 惩罚的前提下找到最具破坏性的 in-manifold 动作。

---

## 4. 关键结论和发现

### 主要发现
1. **Quality Over Quantity**：攻击成功的关键不在于投毒数量，而在于**精准定位高影响力样本**（top 5% TD error transitions）。
2. **Conservative Defenses Are Fragile**：尽管 CQL/IQL/BCQ 能有效抵御随机噪声，但在结构性优化攻击面前依然脆弱。
3. **Stealthiness and Effectiveness Can Coexist**：通过 correlation-breaking trigger 和 gradient-guided action，可在维持 clean performance 的同时实现致命攻击。
4. **最优攻击窗口存在**：敏感性分析表明，**Top 0–5% TD error 样本 + 5% 预算**构成最佳权衡点；盲目增加预算会导致模型崩溃，破坏隐蔽性。

---

### 方法的局限性
- **依赖预训练代理模型**：需要先在一个干净副本上训练 Q 网络来估计 TD error，可能增加攻击成本。
- **特征相关性假设**：correlation-breaking trigger 依赖于状态特征间存在较强线性关系，若特征独立则难以生效。
- **白盒特征统计需求**：需获取全局数据分布（如 95th percentile），在完全黑盒场景下可能受限。

---

### 未来工作方向
- 探索无需代理模型的在线 TD error 估计方法，降低攻击门槛；
- 设计适用于高维图像输入的隐式 correlation-breaking 触发机制；
- 开发针对 CS-GBA 的新型防御策略，例如基于因果推理的异常检测或鲁棒价值函数正则化；
- 将类似思想应用于其他安全敏感领域，如自动驾驶、医疗决策等 Offline RL 应用场景。

---

> **总结一句话**：  
> CS-GBA 揭示了 Offline RL 安全的新边界——通过**关键样本聚焦 + 分布内触发 + 梯度引导动作**，即使在 5% 极低预算下也能穿透最强保守防御，为构建更鲁棒的离线强化学习系统敲响警钟。

</details>

---

### 14. [Improving Chain-of-Thought for Logical Reasoning via Attention-Aware Intervention](https://arxiv.org/abs/2601.09805)

**Authors**: Nguyen Minh Phuong, Dang Huu Tien, Naoya Inoue  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.09805v1  

#### Abstract
Modern logical reasoning with LLMs primarily relies on employing complex interactive frameworks that decompose the reasoning process into subtasks solved through carefully designed prompts or requiring external resources (e.g., symbolic solvers) to exploit their strong logical structures. While inte...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Improving Chain-of-Thought for Logical Reasoning via Attention-Aware Intervention 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大型语言模型（LLMs）在**逻辑推理任务**上面临挑战，尤其是在处理复杂的、多步的、基于规则的推理时容易出错，例如：
- 错误选择推理规则（rule selection）
- 无法判断何时终止推理过程
- 在长链推理中出现信息衰减（information decay）

现有方法主要依赖两类框架：
1. **交互式框架**（Interactive frameworks）：将复杂推理分解为多个子任务（如规则选择、前提推导等），但引入额外的计算开销和系统复杂性。
2. **混合方法**（Hybrid approaches）：结合外部符号求解器（symbolic solvers），虽然性能强，但依赖外部资源，限制了可扩展性和端到端训练。

这些方法未能直接评估LLMs自身的**内在推理能力**，即在单次前向传递中完成完整推理的能力。

---

### 提出的新方法与创新思路
作者提出了一种**非交互式、端到端**的逻辑推理增强框架——**Attention-Aware Intervention (AAI)**，其核心思想是：
- 通过在**few-shot prompt**中注入**符号化结构信息**（如 `Rule1`, `=>`, `KB={}` 等），激活特定的 attention heads，使其表现出与逻辑操作对齐的模式。
- 基于此观察，设计一种**推理时干预机制**（inference-time intervention），动态重加权（reweight）选定 attention heads 的注意力分数，以强化模型对关键规则的关注，抑制无关噪声。

#### 创新点包括：
1. **首次揭示了结构化提示如何影响LLM内部注意力机制**：发现某些 attention heads 可被归类为三类功能型头：
   - **Anchor Heads**：负责记忆信息（高对角线得分）
   - **Aggregation Heads**：整合新生成的前提并传播信息（高垂直得分）
   - **Copy Heads**：复制生成的前提用于更新知识库（KB）或匹配规则（短对角线对齐）
2. **提出 AAI 干预机制**：基于上述分类，在推理过程中自适应地调整 attention scores，通过构造两个掩码矩阵 $M_{\text{Ref}}$ 和 $M_{\text{NoRef}}$ 来加强规则标识符与其内容之间的语义链接。
3. **完全非交互、无需外部组件**：AAI 是一个轻量级、模型无关的干预方法，不改变模型参数，仅在推理时修改 attention 分数，因此**无额外计算开销**。

---

### 相比现有方法的优势
| 特性 | AAI | 交互式框架 | 外部符号求解器 |
|------|-----|------------|----------------|
| 是否需要外部工具 | ❌ 否 | ✅ 是（程序/模块） | ✅ 是（定理证明器） |
| 是否多轮交互 | ❌ 单次前向 | ✅ 多步调用 | ✅ 多步调用 |
| 可解释性 | ✅ 高（基于 attention 分析） | ⚠️ 中等 | ⚠️ 较低（黑箱） |
| 扩展性 | ✅ 强（适用于任意 decoder-only LLM） | ⚠️ 受限于框架设计 | ❌ 弱（依赖接口） |
| 推理效率 | ✅ 极高（零额外延迟） | ❌ 低（多次调用） | ❌ 低 |

> ✅ AAI 实现了**高性能、高可解释性、低开销、端到端推理**的统一。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在五个基准数据集上进行，涵盖不同类型的逻辑推理任务：

| 数据集 | 类型 | 推理难度 | 样本数 | 说明 |
|--------|------|----------|--------|------|
| **ProofWriter** | 多跳演绎推理 | 5-hop | 600 | 开放世界假设，输出 True/False/Uncertain |
| **ProntoQA** | 多跳逻辑问答 | 5-hop | 500 | 类似 ProofWriter，更具挑战性 |
| **LogicalDeduction** | 实体排序推理 | 3/5/7对象 | 300 | 根据约束条件推断顺序 |
| **FOLIO** | 基于一阶逻辑的真实场景推理 | — | 204 | 由领域专家构建，含现实知识 |
| **GSM8k** | 数学应用题 | — | 1,319 | 测试泛化能力 |

---

### 实验设置与评估指标

#### 评估指标
- 主要指标：**Accuracy**（准确率）
- 所有结果均使用 **greedy decoding**（确定性输出）确保可复现。

#### 模型选择
主要测试以下开源 LLMs：
- **Qwen-3** 系列：1.7B, 4B, 8B, 32B
- **OLMo-2**：32B
- **Phi-4**：14B

硬件：单张 NVIDIA A40 GPU。

#### 基线方法对比
| 方法类别 | 具体方法 |
|---------|--------|
| **Closed-source Baseline** | GPT-4 + CoT |
| **Interactive + External Solver** | Logic-LM, DetermLR, SymbCoT |
| **Non-interactive Baseline** | Standard CoT, Symbolic-Aided CoT (Nguyen et al., 2025) |
| **Proposed Methods** | AAI + Symbolic-Aided CoT, AAI + Compact Symbolic-Aided CoT |

此外还提出了一个新的轻量提示模板：**Compact Symbolic-Aided CoT**，用于验证 AAI 的通用性。

#### AAI 设置
- **Main AAI Setting**：选择 $s_{\text{diagonal}} > 0.3$ 的 anchor heads 和 copy heads 进行 reweighting。
- **AAI_agg Setting**：选择 $s_{\text{vertical}} > 0.6$, $s_{\text{horizontal}}, s_{\text{diagonal}} < 0.3$ 的 aggregation heads。
- 超参数敏感性分析：测试 binarization threshold、diagonal threshold、reweighting coefficient ($c$) 和 bias ($b$)。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| 方法 + LLM | ProofWriter | ProntoQA | LogicalDeduction | FOLIO | GSM8k |
|-----------|------------|----------|------------------|-------|-------|
| **Symbolic-Aided CoT (Qwen3-32B)** | 79.67 | 99.20 | 88.00 | 69.61 | — |
| **+AAI (ours)** | **82.50** (+2.83) | **99.40** | **89.67** (+1.67) | **71.09** | — |
| **Symbolic-Aided CoT (OLMo-2-32B)** | 65.00 | 92.60 | 51.33 | 64.22 | — |
| **+AAI (ours)** | **67.50** (+2.50) | **93.00** | **54.33** (+3.00) | 64.22 | — |
| **Symbolic-Aided CoT (Phi-4-14B)** | 76.33 | 99.40 | 91.67 | 70.59 | — |
| **+AAI (ours)** | 76.33 | 99.00 | **92.33** (+0.66) | **72.06** | — |

> 💡 **关键提升集中在 ProofWriter 和 LogicalDeduction 上**，最大提升达 **+2.83 Acc**。

---

### 与其他 SOTA 方法的比较
- 在 **Qwen3-32B** 上，**AAI 已达到甚至超过部分使用 GPT-4 或外部符号求解器的方法**（如 Logic-LM、DetermLR）。
- 尽管未超越最强的 **SymbCoT (99.6%)**，但 AAI 是**唯一无需外部程序或交互调用**即可接近该性能的方法。

---

### 消融实验结果（见 Figure 3）

| 配置 | ProofWriter (Qwen3-32B) | LogicalDeduction (Qwen3-32B) |
|------|------------------------|-------------------------------|
| Symbolic-Aided CoT (Baseline) | 79.67 | 88.00 |
| + AAI (Full Head Selection) | 81.00 | 89.00 |
| + AAI (Aggregation Heads Only) | 81.50 | 88.67 |
| + AAI (Anchor/Copy Heads Only) | **82.50** | **89.67** |

> ✅ 结果表明：**针对 anchor 和 copy heads 的干预最有效且稳定**，而干预 aggregation heads 虽有一定增益但可能导致行为不稳定。

---

### 超参数敏感性分析（Figure 5）
- AAI 对大多数超参数（如 diagonal threshold, coefficient）具有较强鲁棒性。
- 当使用固定 bias（$c=0$）时，性能可能下降，尤其在 **ProofWriter** 上，说明**自适应 reweighting 更优**。

---

### 语义表示可视化（Figure 6）
- 使用 t-SNE 可视化最后一层隐藏状态：
  - **Standard CoT**：聚类松散，错误样本分散。
  - **Symbolic-Aided CoT**：形成更细粒度簇。
  - **AAI**：簇更紧凑、分离更清晰 → 表明 AAI 增强了结构化语义编码能力。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **结构化提示能激活特定 attention heads**：注入符号结构（如 `RuleX`, `=>`）会诱导出与逻辑操作对齐的 attention 模式（anchor, aggregation, copy heads）。
2. ✅ **AAI 显著提升逻辑推理性能**：在多种模型和数据集上一致带来 **+1~3% 准确率提升**，且无额外推理开销。
3. ✅ **anchor/copy heads 是干预的关键目标**：它们负责信息存储与复制，干预这些头能更安全有效地注入先验知识。
4. ✅ **AAI 具有良好泛化性**：不仅适用于原始 Symbolic-Aided CoT，也能提升新设计的 **Compact Symbolic-Aided CoT** 和数学推理任务 **GSM8k** 的表现。

---

### 方法的局限性
1. **依赖合成或中小规模数据集**：当前实验集中于人工构造的逻辑任务（如 ProofWriter），尚未在真实复杂场景中充分验证。
2. **依赖 self-attention 架构**：AAI 基于 attention 分析，难以迁移到非 attention-based 模型。
3. **仍受限于基础 prompt 设计**：AAI 是对已有 prompt 的增强，若原始 prompt 无法引导正确推理路径，则 AAI 效果有限。
4. **错误根源未完全解决**：部分失败源于初始规则选择错误或最终验证阶段的 mismatch，AAI 无法完全纠正。

---

### 未来工作方向
1. **扩展至常识推理（commonsense reasoning）和其他多跳推理任务**。
2. **探索自动识别关键 attention heads 的方法**，减少人工分析成本。
3. **结合训练阶段干预**，进一步固化推理能力。
4. **应用于更大规模真实世界知识库推理系统**，验证实际部署价值。

---

## 总结
本文提出的 **Attention-Aware Intervention (AAI)** 是一种高效、可解释、无额外开销的逻辑推理增强方法。它通过分析结构化提示下的 attention 模式，识别出承担不同逻辑功能的 attention heads，并在推理时对其进行动态 reweighting，从而“引导”模型更好地利用先验知识进行推理。实验证明 AAI 在多个基准上显著优于 baseline，并逼近甚至媲美依赖外部工具的复杂系统，为构建**可信赖、端到端、可分析的神经符号推理系统**提供了新范式。

</details>

---

### 15. [M^4olGen: Multi-Agent, Multi-Stage Molecular Generation under Precise Multi-Property Constraints](https://arxiv.org/abs/2601.10131)

**Authors**: Yizhan Li, Florence Cloutier, Sifan Wu, Ali Parviz, Boris Knyazev, Yan Zhang, Glen Berseth, Bang Liu  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.10131v1  

#### Abstract
Generating molecules that satisfy precise numeric constraints over multiple physicochemical properties is critical and challenging. Although large language models (LLMs) are expressive, they struggle with precise multi-objective control and numeric reasoning without external structure and feedback. ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# M⁴olGen: Multi-Agent, Multi-Stage Molecular Generation under Precise Multi-Property Constraints  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文致力于解决**在精确多属性数值约束下生成分子**这一关键挑战。传统方法（如LLMs或图生成模型）在以下方面存在不足：
- 难以实现对多个物理化学性质（如QED、LogP、MW、HOMO、LUMO）的同时**精确控制**；
- 缺乏有效的**数值推理能力**，无法显式最小化目标属性误差；
- 单步生成缺乏可解释性和可控性。

### 🆕 提出的新方法与创新思路
作者提出 **M⁴olGen** —— 一个两阶段、基于片段（fragment-level）、检索增强的多智能体分子生成框架，其核心创新包括：

#### （1）**两阶段协同架构**
- **Stage I: Prototype Generation（原型生成）**
  - 引入**多智能体推理机制**，结合检索（retrieval）与领域知识进行逐步编辑。
  - 利用BRICS片段分解和RDKit反馈，通过“检索锚定”策略生成接近可行区域的初始分子原型。
- **Stage II: GRPO-Based Optimization（优化精调）**
  - 使用**Group Relative Policy Optimization (GRPO)** 训练一个片段级优化器，在可控的多跳（multi-hop）编辑中显式最小化属性误差。
  - 支持复杂度调节（hop budget），防止过度偏离原始结构。

#### （2）**可控的多跳推理机制**
- 构建了一个包含117万对单步编辑分子的邻域关系数据集（neighbor relational dataset），支持长链、可追踪的推理路径。
- 实现从“粗略原型”到“精准满足”的渐进式优化，提升可控性与可解释性。

#### （3）**大规模公开数据集构建**
- 发布了一个包含约 **295万分子** 的BRICS片段标注数据集，并提供其属性变化（△property）与编辑操作标签。
- 数据集涵盖QED、LogP、MW、HOMO、LUMO等关键属性，可用于训练具备化学推理能力的模型。

### 🔍 相比现有方法的优势
| 维度 | M⁴olGen优势 |
|------|-------------|
| **精确控制** | 显著优于LLMs和图模型，在QED/LogP/MW和HOMO/LUMO任务上均取得最低误差 |
| **可控性** | 多跳编辑允许精细调控修改程度，避免剧烈结构突变 |
| **可扩展性** | 不依赖特定目标重新训练，支持跨目标泛化 |
| **效率** | GRPO训练稳定高效；推理成本低于遗传算法（Graph GA）近90% |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **主数据集来源**：整合ZINC、CHEMBL 和 MOSES 数据库，经去重后共得 **2,945,596个分子**。
- **属性计算方式**：
  - QED、LogP、MW：使用RDKit计算；
  - HOMO/LUMO：使用预训练的DimeNet++模型预测。
- **片段处理**：采用BRICS规则将每个分子分解为化学有意义的子结构单元。
- **邻域数据集构建**：
  - 扫描所有分子对，筛选仅差一个片段操作（add/remove/replace）的有效配对；
  - 最终获得 **~1.17M个单步编辑对**，并记录属性变化（△QED, △LogP, △MW等）。

### ⚙️ 实验设置
- **目标属性组合**：
  1. **Set 1**: QED, LogP, Molecular Weight (MW)
  2. **Set 2**: HOMO, LUMO（电子能级）
- **采样方式**：均匀随机抽取100组目标值，在每组上运行10次独立试验（best-of-10）。
- **Hop Budget**：测试1-hop、2-hop、3-hop优化效果。

### 📊 评估指标
| 指标 | 定义与用途 |
|------|-----------|
| **MAE (Mean Absolute Error)** | 各属性平均绝对误差，衡量精度 |
| **Normalized Total Error (NTE)** | 归一化总误差：<br>$$ \text{NTE} = |\Delta\text{QED}| + \frac{|\Delta\text{LogP}|}{10} + \frac{|\Delta\text{MW}|}{700} $$<br>便于多目标比较 |
| **Diversity** | 基于ECFP4指纹的Tanimoto相似性计算，越高表示探索越广 |
| **Uniqueness** | 输出中唯一分子的比例（通过canonical SMILES判断） |
| **Validity** | RDKit可解析且价态合法的分子比例 |

### 🆚 基线方法对比
| 类型 | 方法列表 |
|------|--------|
| **LLM-based** | GPT-4.1, GPT-4o, Gemini-Flash, Claude-3.5-haiku, SmileyLlama-8B, DrugAssist-7B |
| **Graph-based** | STGG+, Graph Genetic Algorithm (GA-500, GA-1000) |
| **特别说明** | 在HOMO/LUMO任务中，无LLM表现良好，故仅与Graph GA对比 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自Table 1 & Table 2）

#### ✅ Set 1: QED / LogP / MW 结果（Table 1）
| 方法 | QED err | LogP err | MW err | **NTE** |
|------|---------|----------|--------|--------|
| GPT-4.1 (best LLM) | 0.115 | 0.697 | 49.182 | 0.255 |
| STGG+ | 0.079 | 0.418 | 23.56 | 0.155 |
| **M⁴olGen (3-hop, GPT-4o)** | **0.103** | **0.284** | **9.799** | **0.146** |
| **M⁴olGen (3-hop, Qwen)** | 0.120 | 0.237 | 10.365 | 0.159 |

> 💡 **关键提升**：
> - NTE比最强LLM（GPT-4.1）降低 **42.7%**
> - LogP误差下降 **59.1%**（0.697 → 0.284）
> - MW误差下降 **80.0%**（vs. GPT-4.1）

#### ✅ Set 2: HOMO / LUMO 结果（Table 2）
| 方法 | HOMO err (eV) | LUMO err (eV) | **Total Error (eV)** |
|------|----------------|----------------|---------------------|
| Graph GA-1000 | 0.251 | 0.353 | 0.604 |
| **M⁴olGen (1-hop)** | 0.301 | 0.239 | **0.540** |
| **M⁴olGen (2-hop)** | 0.112 | 0.115 | **0.227** |
| **M⁴olGen (3-hop)** | **0.060** | **0.095** | **0.155** |

> 💡 **关键提升**：
> - 总误差仅为Graph GA-1000的 **25.7%**
> - 2-hop即实现超过2倍改进
> - 同时优化HOMO与LUMO，未出现单一过拟合

#### ✅ 效率优势
- M⁴olGen在达到更优性能的同时，**推理时间减少近90%**，因优化成本在训练阶段摊销。

### 🔬 消融实验结果（Ablation Study, Table 3 & Figure 2）

| 方法 | QED err | LogP err | MW err | NTE |
|------|--------|---------|--------|-----|
| Stage I (no retrieval) | 0.111 | 0.970 | 68.555 | 0.307 |
| + Retrieval | 0.098 | 0.769 | 63.240 | 0.265 |
| + 1-hop | 0.130 | 0.423 | 10.404 | 0.187 |
| + 2-hop | 0.111 | 0.339 | 10.489 | 0.160 |
| + 3-hop | 0.103 | 0.284 | 9.799 | **0.146** |

> 🔍 **消融发现**：
- **检索机制**使NTE下降13.7%，尤其改善LogP和MW；
- **引入GRPO优化器**带来最大收益，特别是MW误差骤降84.9%；
- 随着hop数增加，NTE单调下降（3-hop最优）；
- 尽管1-hop时QED略有上升，但最终3-hop恢复并超越基准。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **M⁴olGen显著提升了多属性精确控制能力**，在QED/LogP/MW和HOMO/LUMO任务上全面超越LLMs和图模型。
2. **两阶段设计至关重要**：
   - Stage I利用检索与多智能体协作快速定位可行区域；
   - Stage II通过GRPO驱动的片段级优化实现细粒度调整。
3. **GRPO适用于离散化学动作空间优化**，其基于排名的奖励机制稳定有效，无需真实演示数据。
4. **多跳编辑机制支持可控、可解释的优化路径**，为药物设计中的“理性修饰”提供了自动化工具。
5. **所构建的大规模片段-属性数据集是重要公共资源**，推动可解释分子AI发展。

### ⚠️ 局限性
- 当前依赖**计算属性估计器**（如RDKit、DimeNet++），尚未验证在真实实验数据下的表现；
- 覆盖的属性集合有限（仅QED、LogP、MW、HOMO、LUMO）；
- 更深的hop虽能继续提效，但面临**边际收益递减与计算开销上升**的权衡。

### 🔮 未来工作方向
- 扩展至更多实际应用场景（如ADMET、溶解度、毒性等）；
- 接入真实合成可行性评分与实验室反馈形成闭环；
- 探索动态hop预算调度与 Curriculum Learning 策略；
- 将框架迁移至材料科学（如inorganic materials）或其他结构化设计任务。

---

> ✅ **一句话总结**：  
> M⁴olGen通过“检索引导原型 + GRPO驱动多跳优化”的双阶段范式，首次实现了在多个精确数值约束下高质量、高可控性的分子生成，为AI驱动的理性药物设计提供了新范式。

</details>

---

### 16. [Structure and Diversity Aware Context Bubble Construction for Enterprise Retrieval Augmented Systems](https://arxiv.org/abs/2601.10681)

**Authors**: Amir Khurshid, Abhishek Sehgal  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.10681v1  

#### Abstract
Large language model (LLM) contexts are typically constructed using retrieval-augmented generation (RAG), which involves ranking and selecting the top-k passages. The approach causes fragmentation in information graphs in document structures, over-retrieval, and duplication of content alongside insu...

---

### 17. [Distributed Linearly Separable Computation with Arbitrary Heterogeneous Data Assignment](https://arxiv.org/abs/2601.10177)

**Authors**: Ziting Zhang, Kai Wan, Minquan Cheng, Shuo Shao, Giuseppe Caire  
**Category**: cs.DC  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.10177v1  

#### Abstract
Distributed linearly separable computation is a fundamental problem in large-scale distributed systems, requiring the computation of linearly separable functions over different datasets across distributed workers. This paper studies a heterogeneous distributed linearly separable computation problem,...

---

### 18. [Kolmogorov Arnold Networks and Multi-Layer Perceptrons: A Paradigm Shift in Neural Modelling](https://arxiv.org/abs/2601.10563)

**Authors**: Aradhya Gaonkar, Nihal Jain, Vignesh Chougule, Nikhil Deshpande, Sneha Varur, Channabasappa Muttal  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.10563v1  

#### Abstract
The research undertakes a comprehensive comparative analysis of Kolmogorov-Arnold Networks (KAN) and Multi-Layer Perceptrons (MLP), highlighting their effectiveness in solving essential computational challenges like nonlinear function approximation, time-series prediction, and multivariate classific...

---

### 19. [Data-driven stochastic reduced-order modeling of parametrized dynamical systems](https://arxiv.org/abs/2601.10690)

**Authors**: Andrew F. Ilersich, Kevin Course, Prasanth B. Nair  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.10690v1  

#### Abstract
Modeling complex dynamical systems under varying conditions is computationally intensive, often rendering high-fidelity simulations intractable. Although reduced-order models (ROMs) offer a promising solution, current methods often struggle with stochastic dynamics and fail to quantify prediction un...

---

### 20. [Chinese Labor Law Large Language Model Benchmark](https://arxiv.org/abs/2601.09972)

**Authors**: Zixun Lan, Maochun Xu, Yifan Ren, Rui Wu, Jianghui Zhou, Xueyang Cheng, Jianan Ding Ding, Xinheng Wang, Mingmin Chi, Fei Ma  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.09972v1  

#### Abstract
Recent advances in large language models (LLMs) have led to substantial progress in domain-specific applications, particularly within the legal domain. However, general-purpose models such as GPT-4 often struggle with specialized subdomains that require precise legal knowledge, complex reasoning, an...

---

### 21. [FilDeep: Learning Large Deformations of Elastic-Plastic Solids with Multi-Fidelity Data](https://arxiv.org/abs/2601.10031)

**Authors**: Jianheng Tang, Shilong Tao, Zhe Feng, Haonan Sun, Menglu Wang, Zhanxing Zhu, Yunhuai Liu  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10031v1  

#### Abstract
The scientific computation of large deformations in elastic-plastic solids is crucial in various manufacturing applications. Traditional numerical methods exhibit several inherent limitations, prompting Deep Learning (DL) as a promising alternative. The effectiveness of current DL techniques typical...

---

### 22. [From Single to Multi-Agent Reasoning: Advancing GeneGPT for Genomics QA](https://arxiv.org/abs/2601.10581)

**Authors**: Kimia Abedini, Farzad Shami, Gianmaria Silvello  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10581v1  

#### Abstract
Comprehending genomic information is essential for biomedical research, yet extracting data from complex distributed databases remains challenging. Large language models (LLMs) offer potential for genomic Question Answering (QA) but face limitations due to restricted access to domain-specific databa...

---

### 23. [Credit C-GPT: A Domain-Specialized Large Language Model for Conversational Understanding in Vietnamese Debt Collection](https://arxiv.org/abs/2601.10167)

**Authors**: Nhung Nguyen Thi Hong, Cuong Nguyen Dang, Tri Le Ngoc  
**Category**: cs.CL  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10167v1  

#### Abstract
Debt collection is a critical function within the banking, financial services, and insurance (BFSI) sector, relying heavily on large-scale human-to-human conversational interactions conducted primarily in Vietnamese contact centers. These conversations involve informal spoken language, emotional var...

---

### 24. [Measuring Affinity between Attention-Head Weight Subspaces via the Projection Kernel](https://arxiv.org/abs/2601.10266)

**Authors**: Hiroaki Yamagiwa, Yusuke Takase, Hidetoshi Shimodaira  
**Category**: cs.CL  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10266v1  

#### Abstract
Understanding relationships between attention heads is essential for interpreting the internal structure of Transformers, yet existing metrics do not capture this structure well. We focus on the subspaces spanned by attention-head weight matrices and quantify head-to-head relationships using the Pro...

---

### 25. [Representation-Aware Unlearning via Activation Signatures: From Suppression to Knowledge-Signature Erasure](https://arxiv.org/abs/2601.10566)

**Authors**: Syed Naveed Mahmood, Md. Rezaur Rahman Bhuiyan, Tasfia Zaman, Jareen Tasneem Khondaker, Md. Sameer Sakib, Nazia Tasnim, Farig Sadeque  
**Category**: cs.CL  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10566v1  

#### Abstract
Selective knowledge erasure from LLMs is critical for GDPR compliance and model safety, yet current unlearning methods conflate behavioral suppression with true knowledge removal, allowing latent capabilities to persist beneath surface-level refusals. In this work, we address this challenge by intro...

---

### 26. [Communication-Efficient Federated Learning by Exploiting Spatio-Temporal Correlations of Gradients](https://arxiv.org/abs/2601.10491)

**Authors**: Shenlong Zheng, Zhen Zhang, Yuhui Deng, Geyong Min, Lin Cui  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10491v1  

#### Abstract
Communication overhead is a critical challenge in federated learning, particularly in bandwidth-constrained networks. Although many methods have been proposed to reduce communication overhead, most focus solely on compressing individual gradients, overlooking the temporal correlations among them. Pr...

---

### 27. [Single-Stage Huffman Encoder for ML Compression](https://arxiv.org/abs/2601.10673)

**Authors**: Aditya Agrawal, Albert Magyar, Hiteshwar Eswaraiah, Patrick Sheridan, Pradeep Janedula, Ravi Krishnan Venkatesan, Krishna Nair, Ravi Iyer  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10673v1  

#### Abstract
Training and serving Large Language Models (LLMs) require partitioning data across multiple accelerators, where collective operations are frequently bottlenecked by network bandwidth. Lossless compression using Huffman codes is an effective way to alleviate the issue, however, its three-stage design...

---

### 28. [Communication-Efficient and Privacy-Adaptable Mechanism -- a Federated Learning Scheme with Convergence Analysis](https://arxiv.org/abs/2601.10701)

**Authors**: Chun Hei Michael Shiu, Chih Wei Ling  
**Category**: cs.LG  
**Published**: 2026-01-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.10701v1  

#### Abstract
Federated learning enables multiple parties to jointly train learning models without sharing their own underlying data, offering a practical pathway to privacy-preserving collaboration under data-governance constraints. Continued study of federated learning is essential to address key challenges in ...

---

### 29. [Beyond Rule-Based Workflows: An Information-Flow-Orchestrated Multi-Agents Paradigm via Agent-to-Agent Communication from CORAL](https://arxiv.org/abs/2601.09883)

**Authors**: Xinxing Ren, Quagmire Zang, Caelum Forder, Suman Deb, Ahsen Tahir, Roman J. Georgio, Peter Carroll, Zekun Guo  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.09883v1  

#### Abstract
Most existing Large Language Model (LLM)-based Multi-Agent Systems (MAS) rely on predefined workflows, where human engineers enumerate task states in advance and specify routing rules and contextual injections accordingly. Such workflow-driven designs are essentially rule-based decision trees, which...

---

### 30. [Structured Personality Control and Adaptation for LLM Agents](https://arxiv.org/abs/2601.10025)

**Authors**: Jinpeng Wang, Xinyu Jia, Wei Wei Heng, Yuquan Li, Binbin Shi, Qianlei Chen, Guannan Chen, Junxia Zhang, Yuyu Yin  
**Category**: cs.AI  
**Published**: 2026-01-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.10025v1  

#### Abstract
Large Language Models (LLMs) are increasingly shaping human-computer interaction (HCI), from personalized assistants to social simulations. Beyond language competence, researchers are exploring whether LLMs can exhibit human-like characteristics that influence engagement, decision-making, and percei...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
