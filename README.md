# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-19 06:43:21 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Distributed physics-informed neural networks via domain decomposition for fast flow reconstruction](https://arxiv.org/abs/2602.15883)

**Authors**: Yixiao Qian, Jiaxu Liu, Zewei Xia, Song Chen, Chao Xu, Shengze Cai  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2602.15883v1  

#### Abstract
Physics-Informed Neural Networks (PINNs) offer a powerful paradigm for flow reconstruction, seamlessly integrating sparse velocity measurements with the governing Navier-Stokes equations to recover complete velocity and latent pressure fields. However, scaling such models to large spatiotemporal dom...

---

### 2. [MoE-Spec: Expert Budgeting for Efficient Speculative Decoding](https://arxiv.org/abs/2602.16052)

**Authors**: Bradley McDanel, Steven Li, Sruthikesh Surineni, Harshit Khaitan  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.16052v1  

#### Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by verifying multiple drafted tokens in parallel. However, for Mixture-of-Experts (MoE) models, this parallelism introduces a severe bottleneck: large draft trees activate many unique experts, significantly increasing memory press...

---

### 3. [push0: Scalable and Fault-Tolerant Orchestration for Zero-Knowledge Proof Generation](https://arxiv.org/abs/2602.16338)

**Authors**: Mohsen Ahmadvand, Rok Pajni\v{c}, Ching-Lun Chiu  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.16338v1  

#### Abstract
Zero-knowledge proof generation imposes stringent timing and reliability constraints on blockchain systems. For ZK-rollups, delayed proofs cause finality lag and economic loss; for Ethereum's emerging L1 zkEVM, proofs must complete within the 12-second slot window to enable stateless validation. The...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*push0: Scalable and Fault-Tolerant Orchestration for Zero-Knowledge Proof Generation*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
零知识证明（ZKP）生成在区块链系统中面临严格的**时序约束**和**可靠性要求**：
- **ZK-Rollups** 中，延迟的证明会导致最终性滞后（finality lag），甚至引发链冻结（chain halt）。
- **以太坊 L1 zkEVM** 要求在 12 秒的 slot 窗口内完成证明生成，否则无法实现无状态验证。
- 当前缺乏一个能同时满足以下六项要求的**标准化编排框架**：
  1. **Head-of-chain ordering**（严格区块顺序）
  2. **Bounded latency**（低调度开销）
  3. **Fault tolerance**（容错与自动恢复）
  4. **Prover agnosticism**（支持异构 zkVM）
  5. **Horizontal scalability**（水平扩展能力）
  6. **Observability**（可观测性）

现有方案如 DIZK、SHARP、Snarktor 等仅解决部分问题，无法统一处理多 zkVM 协同、故障恢复与实时性。

---

### 🚀 提出的新方法与创新点

**push0** 是首个满足上述全部六项要求的云原生 ZKP 编排系统，其核心创新包括：

#### （1）**事件驱动的 Dispatcher-Collector 架构**
- 基于**持久化优先级队列**（persistent priority queues）解耦任务分发与聚合。
- **Dispatcher** 负责调用 prover 执行任务，**Collector** 负责收集中间结果并触发后续任务。
- 支持**块间串行**（block-sequential）与**块内并行**（intra-block parallelism）的融合，兼顾顺序性与高并发。

#### （2）**形式化的正确性保障**
- 提出三项关键性质并证明其成立：
  - **Starvation Freedom**：所有任务最终都会被处理。
  - **Barrier Locality**：通过分区亲和路由（partition-affine routing）确保同一聚合组的消息被同一 Collector 处理，避免 barrier 分裂。
  - **Routing Continuity**：缩容时仍保证新旧消息由同一 Collector 处理，防止状态丢失。

#### （3）**Prover-Agnostic 设计**
- 支持任意 zkVM 和证明系统（如 Halo2、SP1、RISC Zero）无缝集成。
- 提供两种集成模式：
  - **JSON Executor**：CLI 工具包装，无需修改代码。
  - **Composer Executor**：Rust trait 接口，支持零拷贝高性能集成。

#### （4）**多队列流水线与早期承诺机制**
- 引入“提议流”（proposer flow）与“证明流”（proving flow）分离设计。
- 通过 **multi-input collector** 同步两路输入，实现：
  - **早期承诺**（early commitment）：即使证明未完成，也可提交批次边界。
  - **故障隔离**：证明失败不影响提议流程。
  - **独立扩缩容**：不同阶段可按需分配资源。

---

### 🔍 相比现有方法的优势

| 特性 | push0 | DIZK | SHARP | Snarktor |
|------|-------|------|-------|---------|
| 支持异构 zkVM | ✅ | ❌（仅 Groth16） | ❌（StarkWare 专用） | ⚠️（经济激励为主） |
| 严格区块顺序 | ✅（优先级队列） | ❌ | ❌ | ❌ |
| 自动故障恢复 | ✅（消息重投） | ✅（Spark checkpoint） | 手动 | 手动 |
| 水平扩展 | ✅（无状态 Dispatcher） | ✅ | ❌（中心化） | ✅ |
| 可观测性 | ✅（Prometheus/OpenTelemetry） | ❌ | ❌ | ❌ |
| 编排语义通用性 | ✅（统一抽象） | ❌（特定系统） | ❌ | ❌ |

> ✅ **push0 不是替代 zkVM 或证明加速器，而是提供上层调度语义，可作为底层基础设施支撑各类去中心化或中心化证明网络。**

---

## 2. 核心实验方法和设置

### 🧪 实验环境

#### 主要测试平台（生产级模拟）：
- **Kubernetes 集群**：3 节点 AWS `t3.medium`（2 vCPU, 4GB RAM）
- **消息总线**：Synadia NGS（云端 NATS JetStream），跨网络通信
- **部署方式**：模拟真实分布式环境，dispatcher、collector、bus 跨节点部署

#### 控制实验平台（消除网络变量）：
- **Docker Compose**：单机 Apple M3 Max（36GB RAM）
- **本地 NATS JetStream**：消除网络延迟影响，用于精确测量调度开销

---

### 📊 评估指标

| 指标 | 定义 |
|------|------|
| **Orchestration Overhead** | 从任务发布到结果接收的时间差（不含 prover 执行时间） |
| **Scaling Efficiency** | $ \eta = S / D $，其中 $ S $ 为吞吐增速，$ D $ 为 dispatcher 数量 |
| **P50/P99 Latency** | 调度延迟中位数与尾部延迟 |
| **Memory Consumption** | 各组件内存占用 |
| **Barrier Completion Rate** | 聚合任务成功完成的比例 |
| **MTTR**（Mean Time to Recovery） | 故障后任务恢复时间 |

---

### 🔄 基线方法对比

本文未直接对比其他编排系统（因无通用开源实现），而是通过**消融实验**与**反事实设计**进行比较：
- **Round-robin routing vs. Partition-affine routing**：验证 barrier 完成率差异
- **Linear pipeline vs. Multi-queue pipeline**：验证故障隔离与早期承诺优势
- **不同 ACK timeout 设置**：验证恢复速度与资源消耗权衡

---

## 3. 主要实验结果和性能指标

### 📈 调度开销极低（Negligible Overhead）

| 环境 | P50 延迟 | P99 延迟 | 占证明时间比例 |
|------|--------|--------|----------------|
| 生产 K8s + 云端 NATS | **5.2–5.3ms** | 8.0–8.5ms | **<0.1%**（假设证明耗时 7s+） |
| Docker + 本地 NATS | **2.7–10ms** | 4.9–18ms | — |

> ✅ 在真实部署中，**中位调度开销仅为 5ms**，远低于典型 ZKP 计算时间（数十秒至数分钟）。

---

### 📊 水平扩展效率接近完美

#### 表：1s 模拟 prover 下的扩展效率（K8s 环境）

| Dispatchers | Speedup | Efficiency |
|-----------|--------|----------|
| 1         | 1.0×   | 100%     |
| 2         | 2.0×   | 99%      |
| 4         | 4.0×   | 99%      |
| 8         | 7.9×   | 99%      |
| 16        | 15.9×  | 99%      |
| 32        | 31.7×  | **99%**  |

#### 表：5s 模拟 prover 下的扩展效率（更贴近现实）

| Dispatchers | Speedup | Efficiency |
|-----------|--------|----------|
| 1–32      | 1.0×–32.0× | **100%** |

> ✅ **随着 prover 延迟增加，调度开销占比进一步降低，扩展效率趋近理想线性。**

---

### 🔬 消融实验结果

#### （1）Partition-Affine Routing 对 Barrier 成功率的影响

| 路由策略 | Barrier 完成率（100 次） | 理论预期 |
|--------|----------------------|--------|
| Round-robin（轮询） | **5%** | ~1.56%（$4^{-3}$） |
| Partition-affine（模运算） | **100%** | 100% |

> ❗ **朴素负载均衡导致 95% 的 barrier 失败**，验证了分区亲和路由的必要性。

#### （2）Multi-Queue vs. Linear Pipeline（对抗性场景）

注入一个 15 秒“毒任务”模拟 GPU 内存溢出：

| 指标 | Multi-Queue | Linear |
|------|------------|--------|
| 总耗时 | **23.9s** | 101.5s |
| 速度提升 | **4.2×** | — |
| 最终性偏移（Finality Skew） | 达 **25 块** | 0（完全阻塞） |
| Proposer 吞吐 | **15.9 块/s** | N/A |

> ✅ **multi-queue 架构实现了早期承诺与故障隔离，显著提升系统鲁棒性。**

#### （3）容错能力验证

- **Dispatcher 崩溃**：零任务丢失；若存在健康节点，**MTTR ≈ 5–6ms**。
- **Collector 崩溃**：重启后通过消息重放重建状态，**所有 barrier 成功完成**。
- **ACK Timeout 影响**：
  - 若有健康 dispatcher：MTTR ≈ 0.5s
  - 若全部崩溃：MTTR = ACK Timeout（可配置）

> ✅ **基于消息持久化与三阶段确认协议，实现强容错与自动恢复。**

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **ZKP 编排是生产系统的瓶颈**：实际生产中，**编排故障远多于 prover bug**，可靠调度比证明速度更重要。
2. **Dispatcher-Collector 架构有效解耦顺序性与并行性**：通过优先级队列保障 head-of-chain ordering，同时允许块内高度并行。
3. **Partition-Affine Routing 至关重要**：避免 barrier 分裂，是实现高聚合成功率的关键。
4. **多队列流水线提升系统韧性**：分离提议与证明流，实现早期承诺与故障隔离。
5. **push0 开销可忽略不计**：**5ms 中位延迟 + 99–100% 扩展效率**，适用于大规模生产环境。

---

### ⚠️ 局限性

1. **依赖可信消息总线**：当前假设 NATS/Kafka 等为可信组件，尚未支持完全去中心化的拜占庭容错消息层。
2. **未内置激励机制**：适用于中心化运营商或联盟链场景，若用于开放市场需结合经济模型（如 Snarktor）。
3. **配置复杂度较高**：虽支持动态配置，但高级策略（如自定义 Collection Strategy）仍需开发介入。
4. **输入保密性需应用层保障**：未对 proving input 加密，敏感数据需额外保护。

---

### 🔮 未来工作方向

1. **去中心化编排层**：探索基于共识的 task assignment 与 result verification，构建无需信任的 proving network。
2. **与共识层集成**：将 prover selection、reward distribution 与 Ethereum 共识机制结合。
3. **智能超时与弹性重试**：根据 block complexity 动态调整 ACK timeout 与 retry policy。
4. **跨链证明协调**：支持多链 zkEVM 的统一编排框架。

---

## 结语

> **“A 10-second proof is worthless if the system cannot reliably schedule, monitor, and recover proving tasks at scale.”**

**push0** 填补了 ZK 基础设施中的关键空白——**可靠的、可扩展的、通用的证明编排层**。它不仅是 ZK-Rollup 运营商的强大工具，也为以太坊迈向 L1 zkEVM 提供了必要的调度基础。该系统已在 **Zircuit zkRollup 上稳定运行超过 1400 万主网区块**，具备充分的生态有效性（ecological validity）。

</details>

---

### 4. [How Reliable is Your Service at the Extreme Edge? Analytical Modeling of Computational Reliability](https://arxiv.org/abs/2602.16362)

**Authors**: MHD Saria Allahham, Hossam S. Hassanein  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.16362v1  

#### Abstract
Extreme Edge Computing (XEC) distributes streaming workloads across consumer-owned devices, exploiting their proximity to users and ubiquitous availability. Many such workloads are AI-driven, requiring continuous neural network inference for tasks like object detection and video analytics. Distribut...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：How Reliable is Your Service at the Extreme Edge? Analytical Modeling of Computational Reliability

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文聚焦于 **Extreme Edge Computing (XEC)** 环境下流式AI服务（如实时目标检测、视频分析）的**计算可靠性**（computational reliability）量化难题。传统可靠性模型通常假设设备为稳定的企业级资源或采用二元故障模型（正常/宕机），而XEC依赖消费者拥有的设备（如手机、平板），其计算能力因后台应用、电池管理、热节流等因素高度波动。因此，核心问题是：

> 如何量化一个或一组XED（Extreme Edge Device）在给定QoS阈值下持续满足流式服务计算需求的概率？

### 提出的新方法与新思路
论文提出了首个**形式化的解析框架**来建模XEC中的计算可靠性，定义如下：

> **计算可靠性 $ R(t) = P(C(t)/\Delta(t) \geq \Theta) $**  
> 即设备瞬时计算容量 $ C(t) $ 与服务需求 $ \Delta(t) $ 之比不低于服务质量（QoS）阈值 $ \Theta $ 的概率。

该框架支持两种信息场景下的可靠性估计：
- **Minimal Information (MI) 模型**：仅需声明的容量和需求上下界（如 $[C_{\min}, C_{\max}]$, $[\Delta_{\min}, \Delta_{\max}]$），假设均匀分布，推导出闭式解（closed-form expression）。
- **Historical Data 模型**：利用历史观测数据通过最大似然估计（MLE）拟合截断正态分布（Truncated Normal Distribution），提供更精确的动态可靠性预测。

此外，框架扩展到多设备系统，涵盖三种典型部署架构：
- **Series Configuration**：任务分片处理，所有设备必须成功 → 可靠性乘积形式 $ R_{\text{series}} = \prod R_i $
- **Parallel Configuration**：冗余复制执行，任一成功即可 → $ R_{\text{parallel}} = 1 - \prod(1 - R_i) $
- **Work-Partitioned Configuration**：负载按比例分配，每台设备承担部分吞吐量

并提出：
- **最优工作分配规则**（Optimal Work Partitioning）：使各设备边际对数可靠性相等（equal marginal log-reliability），最大化整体系统可靠性。
- **设备选择边界理论**：给出系列配置可支持的最大设备数 $ N^*_{\text{series}} $ 和并行配置所需最小设备数 $ N^*_{\text{parallel}} $ 的解析表达式。

### 相比现有方法的优势
| 维度 | 本文优势 |
|------|--------|
| **可靠性定义** | 超越“节点是否存活”的二元模型，关注**连续性能退化**与QoS达标概率 |
| **适用场景** | 针对XEC特有的资源波动性和非专用性设计，适用于消费级设备组成的去中心化环境 |
| **实用性** | 支持从无先验知识（MI）到有历史数据的渐进式可靠性估计，适合冷启动与长期优化 |
| **系统级建模** | 提供统一框架分析串行、并行、分区等多种拓扑结构，并指导资源编排决策 |

---

## 2. 核心实验方法和设置

### 使用的数据集与工作负载
- **代表性任务**：基于 **YOLO11m** 模型进行**实时目标检测**，作为典型的分布式推理（DI）流式工作负载。
- **输入数据**：视频流帧（frame），通过调整分辨率控制计算需求。
- **仿真平台**：基于 **Docker容器** 构建软件模拟环境，每个容器代表一个独立的XED worker。

### 实验设置
#### 控制变量机制
- **Capacity 控制**：通过动态分配CPU线程数（2–12 threads）模拟设备可用算力波动，建立线程数 ↔ GFLOPS 映射关系。
- **Demand 控制**：通过缩放输入图像尺寸（scale factor $ s \in (0,1] $）调节计算需求，YOLO的FLOPs与像素数成正比（$ \propto s^2 $）。
- **QoS Threshold $ \Theta $**：对应目标帧率（FPS）。例如 $ \Theta = 3 $ 表示要求每秒处理3帧，即每帧处理时间不超过 $ 1/3 $ 秒。

#### 数据采集流程
- 每隔20–30帧随机采样新的线程数和scale factor，生成动态变化的 $ (C(t), \Delta(t)) $ 序列。
- 记录每一帧的：
  - 线程数（capacity proxy）
  - 缩放因子（demand proxy）
  - 推理延迟
  - 是否满足QoS（即 $ C/\Delta \geq \Theta $）

#### 评估指标
- **Empirical Reliability**：满足QoS的帧占比（ground truth）
- **Analytical Prediction**：使用MI模型（Eq. 3）和Historical Data模型（Eq. 5）计算的理论可靠性
- **Monte Carlo Sampling**：用于验证解析公式的中间基准

#### 基线方法对比
- **MI Model (Uniform Assumption)**：仅用边界信息，假设 $ C(t), \Delta(t) \sim \text{Uniform} $
- **Historical Data Model (MLE + Truncated Normal)**：使用MLE拟合参数后计算可靠性
- 对比二者与实测值之间的吻合程度

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### ✅ **MI模型验证（Fig. 3）**
- 在 $ C \in [55, 152] $ GFLOPS, $ \Delta \in [55, 278] $ GFLOPS, $ \Theta \in [1,4] $ FPS 下：
  - **Analytical**, **Monte Carlo**, 和 **Simulation** 曲线几乎完全重合 → 验证了公式 (3) 的准确性。
- 容量越高（更多线程），可靠性曲线右移；需求越低（小scale），可靠性越高 → 符合直觉且被量化验证。

#### ✅ **Historical Data 模型验证（Fig. 4）**
- 使用MLE拟合截断正态分布后，**R(t,Θ)** 预测精度显著优于MI模型。
- 示例：在 $ \Theta = 3 $ FPS, threads ∈ [4,8] 时，
  - MI预测 $ R_I \approx 0.69 $
  - MLE预测 $ R_H \approx 0.85 $
  - 实测值 ≈ 0.87 → **MLE误差 < 0.02，远优于MI**

#### ✅ **MLE收敛性（Fig. 5）**
- 随着样本数量增加，MLE估计逐步逼近真实参数：
  - 均值 $ \mu $ 在约10–20个样本后趋于稳定
  - 方差 $ \sigma $ 在30–50个样本后收敛
- 可靠性估计从初始 $ R_I \approx 0.55 $ 提升至最终 $ R_H \approx 0.82 $，绝对提升达 **+0.27**

#### ✅ **设备选择边界验证（Fig. 6）**
- **Series Configuration**：
  - 当单设备可靠性 $ R = 0.99 $，最多支持10台设备达到 $ e = 0.9 $
  - 若 $ R = 0.95 $，仅能支持2台 → 强调高可靠性设备的重要性
- **Parallel Configuration**：
  - 要达到 $ e = 0.99 $，若 $ R = 0.9 $，只需2台；若 $ R = 0.5 $，需9台
  - 实际异构池所需设备少于worst-case bound → 验证优先选择高可靠设备的有效性

#### ✅ **多设备系统验证（Fig. 7）**
- 四设备空间分割部署YOLO11m：
  - 单设备吞吐：1.9 FPS → 分布式后系统吞吐：**5.3 FPS（2.8×加速）**
  - 单设备延迟：520 ms → 系统延迟：**207 ms（2.5×降低）**
- **Series Reliability**：
  - 各设备个体可靠性 > 0.8（在 $ \Theta = 5 $ 时）
  - 但系统可靠性因乘积效应降至 ~0.5 → 揭示**串行架构的脆弱性**

---

## 4. 关键结论和发现

### 主要发现
1. **计算可靠性是XEC中流式服务可行性的关键指标**，应取代传统的“节点存活”模型。
2. **MI模型虽保守但仍准确**，适合冷启动阶段快速评估可行性。
3. **引入历史数据并通过MLE建模可显著提升预测精度**，可靠性估计随交互历史积累而收敛。
4. **多设备系统的可靠性受拓扑结构深刻影响**：
   - **Series**：可靠性呈指数衰减，需严格筛选高质量设备
   - **Parallel**：可通过冗余有效提升鲁棒性，代价是资源浪费
   - **Work Partitioning**：最优分配策略应使各设备边际可靠性下降速率一致
5. **存在明确的设备选择边界**，可用于指导Orchestrator进行资源编排与准入控制。

### 方法的局限性
- 当前模型**仅考虑计算维度**，未纳入通信延迟、网络抖动、传输错误等现实因素。
- 假设设备间**统计独立性**成立，但在某些社交密集场景中可能存在相关性（如同一用户的多个设备同时忙碌）。
- MLE依赖足够多的历史样本，在极短交互窗口内可能无法充分收敛。

### 未来工作方向
- 扩展模型以联合建模 **computation + communication reliability**
- 探索**联邦学习风格的跨服务可靠性共享机制**，加速新服务的冷启动
- 将本框架集成至 **Computing Power Network (CPN)** 的资源调度层，实现端到端SLA保障
- 研究**动态QoS自适应机制**，根据实时可靠性反馈调整帧率或模型复杂度

--- 

> **总结一句话**：  
> 本文建立了首个面向XEC流式AI服务的**计算可靠性解析框架**，实现了从“能否运行”到“有多大概率按时完成”的精细化评估，为极端边缘环境下的资源编排提供了坚实的理论基础与实用工具。

</details>

---

### 5. [DistributedEstimator: Distributed Training of Quantum Neural Networks via Circuit Cutting](https://arxiv.org/abs/2602.16233)

**Authors**: Prabhjot Singh, Adel N. Toosi, Rajkumar Buyya  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.16233v1  

#### Abstract
Circuit cutting decomposes a large quantum circuit into a collection of smaller subcircuits. The outputs of these subcircuits are then classically reconstructed to recover the original expectation values. While prior work characterises cutting overhead largely in terms of subcircuit counts and sampl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*DistributedEstimator: Distributed Training of Quantum Neural Networks via Circuit Cutting*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文针对**量子神经网络（QNN）在近似噪声中等规模量子（NISQ）设备上训练时面临的资源限制问题**，特别是当量子电路过大无法在单个量子设备上执行时的挑战。虽然 *circuit cutting* 技术可以将大电路分解为可独立执行的小子电路，但其对端到端训练流程（尤其是迭代优化中的 estimator 调用）带来的系统级开销尚未被充分量化。

具体而言，作者指出现有研究存在三大局限：
- **P1（开销归因不明确）**：切割引入的编译、任务调度、重构等开销常被视为次要因素，缺乏细粒度的时间归因。
- **P2（重构成为瓶颈）**：尽管切割增加了并行性，但经典的重构阶段可能成为关键路径上的串行瓶颈。
- **P3（学习动态未被完整评估）**：切割是否影响模型精度、鲁棒性和优化轨迹尚需端到端验证。

### 提出了什么新方法或新思路
作者提出了一种 **cut-aware estimator 执行流水线**，将 circuit cutting 显式建模为一个**分阶段的分布式工作负载**，包含以下四个阶段：
1. **Partitioning**（划分）
2. **Subexperiment Generation**（子实验生成）
3. **Parallel Execution**（并行执行）
4. **Classical Reconstruction**（经典重构）

该框架通过**细粒度仪器化（instrumentation）**，记录每个阶段的运行时间，从而实现对 end-to-end 开销的精确测量与归因。

### 相比现有方法的优势
- **系统视角而非纯算法视角**：首次从分布式系统的角度分析 circuit cutting 的实际性能瓶颈。
- **端到端评估**：不仅关注子电路数量或采样复杂度，更关注真实训练过程中的 wall-clock time 和学习结果。
- **可复现的测量基础**：提供了一个可复现的实验框架，用于评估不同调度策略、重构成本与学习性能之间的权衡。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Iris**：经典三类鸢尾花数据集，用于二分类任务（`iris_binary_pml`）。
- **MNIST**：手写数字数据集，选取部分样本构建二分类任务（`mnist_binary`）。

### 实验设置
- **模型结构**：
  - 特征映射（feature map）：`ZFeatureMap`
  - 变分电路（ansatz）：`RealAmplitudes`（重复次数 `reps=1`）
- **可观测量（Observable）**：Pauli-Z 张量积（`SparsePauliOp`）
- **训练协议固定**：
  - 每次估计器调用 shots 数固定为 **1024**
  - 优化器使用 PyTorch 默认设置
  - 数据预处理统一标准化

### 评估指标
| 类别 | 指标 |
|------|------|
| **系统性能** | - 总训练时间（Total training time）<br>- 各阶段耗时占比（`T_part`, `T_gen`, `T_exec`, `T_rec`）<br>- 加速比（Speed-up = T₁ / T₁₆）<br>- 对 straggler 的敏感性（注入延迟下的 slowdown） |
| **学习性能** | - 测试准确率（Test accuracy）<br>- 鲁棒性（Robustness）：在 Gaussian 噪声和 FGSM 攻击下的平均准确率 |

### 基线方法对比
- **Baseline**：无 circuit cutting（`NO_CUT`）
- **变体**：分别进行 1、2、3 次 cuts（对应不同颜色：橙、绿、红）
- **调度策略对比**：
  - Eager submission（立即提交所有任务）
  - Staggered dispatch（分批提交，引入间隔以缓解拥塞）

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ RQ1: 切割引入显著端到端开销
- 图4显示，随着 cut 数增加，总训练时间急剧上升。
- 在 MNIST 上，3-cut 配置相比 no-cut 最多增加数倍训练时间。
- 开销增长是非线性的，且随 cut 数递增，表明是“工作负载扩张”而非固定开销。

#### ✅ RQ2: 重构阶段主导关键路径
- 表 I 显示，在每次 estimator 查询中，**重构时间占总查询时间的比例高达 43%~58%**，且随 cut 数增加而上升。
- 图5显示，即使将 worker 数从1增至16，**加速比接近甚至低于1**，说明并行化未能带来收益。
- 结论：**classical reconstruction 成为可扩展性的主要瓶颈**。

#### ✅ RQ3: 调度策略影响 straggler 敏感性
- 图6显示，在注入 straggler 延迟（0.1秒，概率20%）下，不同配置的 slowdown 不同。
- 当执行时间占比较大时，系统对 straggler 更敏感；而重构主导的配置反而对执行延迟不敏感。
- **staggering 等调度策略可改善尾部延迟行为，但效果取决于当前瓶颈所在阶段**。

#### ✅ RQ4: 准确率未受系统性损害
- 图7显示：
  - **Iris**：所有配置达到相同测试准确率（约100%），切割未造成退化。
  - **MNIST**：部分 cut 配置甚至表现优于 no-cut，说明切割可能通过改变 estimator 方差或评估顺序，**正面影响优化轨迹**。

#### ✅ RQ5: 鲁棒性得以保持
- 图8显示，在 Gaussian 和 FGSM 扰动下：
  - Iris 上鲁棒性与准确率一致，不受切割影响。
  - MNIST 上部分 cut 配置表现出更高鲁棒性。
- 结论：**切割未降低模型鲁棒性，某些情况下可能提升**。

---

## 4. 关键结论和发现

### 主要发现
1. **Circuit cutting 是一种“分阶段分布式工作流”**，不能仅视为算法变换，必须纳入系统设计考量。
2. **重构阶段是性能瓶颈**：尽管切割提升了并行潜力，但串行的 classical reconstruction 极大限制了实际加速比。
3. **端到端开销随 cut 数快速增长**：主要来自子实验数量膨胀和聚合成本，而非编译或规划阶段。
4. **学习性能得以保留甚至增强**：在相同训练预算下，切割未导致准确率或鲁棒性下降，个别配置表现更优。
5. **调度策略需联合考虑执行与重构**：单纯优化执行调度（如 staggering）若不能缓解重构压力，则效果有限。

### 方法的局限性
- 当前实现基于模拟器（Aer）和线程池，未考虑真实 QPU 的排队延迟、网络通信开销。
- 重构仍为集中式串行操作，未尝试分布式 reduction 或增量更新。
- 实验覆盖的电路深度、ansatz 类型和数据集有限，泛化能力有待验证。
- 调度策略探索较初步，缺乏自适应或基于方差感知的调度机制。

### 未来工作方向
1. **分布式与增量重构（Distributed/Incremental Reconstruction）**
   - 使用树形规约（tree-reduction）或流式聚合减少等待时间。
   - 允许早期部分结果参与梯度估计，实现执行与重构重叠。

2. **方差感知调度与自适应分配（Variance-aware Scheduling & Adaptive Shot Allocation）**
   - 根据子实验对 estimator 方差的贡献动态调整 shot 分配。
   - 优先执行高不确定性项，加快收敛。

3. **显式尾延迟缓解机制**
   - 引入 speculative execution、work stealing 等策略应对 straggler。
   - 将 staggering 参数作为可调超参进行优化。

4. **硬件感知评估**
   - 在真实量子设备或混合仿真环境中测试，纳入设备队列、网络延迟等因素。
   - 构建预测性性能模型，指导 cut 决策。

5. **更广泛的工作负载分析**
   - 扩展至更深 ansatz、更多数据集、不同 observable 设计。
   - 进行 sensitivity analysis，识别切割可行性的边界条件。

---

> **总结一句话**：  
> 本文揭示了 **circuit cutting 在训练 QNN 时的实际系统瓶颈在于 classical reconstruction**，虽带来显著开销，但在合理配置下能保持甚至提升模型性能，未来需将重构与调度作为一等公民进行系统级优化。

</details>

---

### 6. [Toward Scalable Verifiable Reward: Proxy State-Based Evaluation for Multi-turn Tool-Calling LLM Agents](https://arxiv.org/abs/2602.16246)

**Authors**: Yun-Shiuan Chuang, Chaitanya Kulkarni, Alec Chiu, Avinash Thangali, Zijie Pan, Shivani Shekhar, Yirou Ge, Yixi Li, Uma Kona, Linsey Pang, Prakhar Mehrotra  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.16246v1  

#### Abstract
Interactive large language model (LLM) agents operating via multi-turn dialogue and multi-step tool calling are increasingly used in production. Benchmarks for these agents must both reliably compare models and yield on-policy training data. Prior agentic benchmarks (e.g., tau-bench, tau2-bench, App...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Toward Scalable Verifiable Reward: Proxy State-Based Evaluation for Multi-turn Tool-Calling LLM Agents*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **agentic benchmarks**（如 T-bench, AppWorld）依赖于完全确定性的后端系统（deterministic backends），用于进行基于最终状态的评估（state-based evaluation）。这类系统虽然能准确判断任务是否完成，但构建和维护成本极高，需要大量工程投入（例如 AppWorld 报告了约 60K LOC 和 1.8K 单元测试），严重限制了迭代速度和工业应用的可扩展性。

### 提出的新方法：**Proxy State-Based Evaluation**
本文提出了一种无需真实数据库的轻量级替代方案——**Proxy State-Based Evaluation**，其核心思想是：
- 利用 **LLM-driven simulation framework** 来模拟交互环境；
- 通过一个 **LLM state tracker** 从完整的对话轨迹中推断出结构化的“代理状态”（proxy state）；
- 使用 **LLM judge** 将该代理状态与预定义的场景目标进行比对，判断目标是否达成。

### 方法组成（五个协作的 LLM 组件）
1. **Reasoning Agent (RA)**：被评测的智能体模型。
2. **User Simulator (`fuser`)**：模拟用户行为，生成多轮查询。
3. **Tool Simulators (`ftool`)**：模拟工具调用行为，读写当前 proxy state。
4. **State Tracker (`fstate`)**：基于完整交互历史推理每一步的状态变化。
5. **LLM Judge (`J`)**：判断最终状态是否满足预期，并检测幻觉。

### 相比现有方法的优势
| 方面 | 传统方法（如 AppWorld） | 本论文方法 |
|------|------------------------|-----------|
| 后端依赖 | 需要全确定性数据库 | 无真实数据库，仅需 LLM 推理 |
| 工程成本 | 极高（数万行代码+测试） | 易快速搭建，随产品演进灵活调整 |
| 可扩展性 | 低 | 高，适合工业部署 |
| 数据生成 | 支持 on-policy rollout | 支持 on/off-policy rollout，可用于训练 |
| 评估可靠性 | 高（基于真实 DB） | 高（人类-LLM judge 一致性 >90%） |

> ✅ **核心优势**：在保留 state-based evaluation 优点的同时，大幅降低工程负担，实现**可规模化、可验证奖励信号的 agentic benchmark**。

---

## 2. 核心实验方法和设置

### 数据集
- 自建合成数据集，共 **|Z|=208 个场景**，分为：
  - 训练集 `Z_train`：157 个
  - 测试集 `Z_test`：51 个
- 场景覆盖两个真实世界领域：
  - **Commerce**：商品发现、购物车管理、促销等
  - **Account**：账户余额、资金操作、交易查询、争议处理等

每个场景 `z` 包含以下字段（见 Figure 2）：
- 用户目标 `g(z)`
- 用户事实 `u(z)`（如姓名、偏好）
- 系统事实 `so(z)`（初始数据库状态）
- 期望最终状态 `s*(z)`
- 期望代理行为 `b*(z)`

### 实验设置
- **Reasoning Agent (RA) 模型**：
  - 多种主流 LLM：GPT-5（不同 reasoning effort）、GPT-4o、Gemini-2.5、Qwen3 系列等
  - 所有 rollout 使用 temperature=1
- **Simulator & Judge 模型**：
  - 默认使用 **GPT-5o**（medium reasoning effort）实例化 `fuser`, `ftool`, `fstate`, `J`
- **用户 persona 设置**：
  - 三种 persona：`power`, `ambiguous`, `confused`（定义见 Appendix C）
  - 主要评估使用 `power user` 以减少用户侧噪声

### 评估指标
所有指标均在 `Z_test` 上计算：
- **Goal Completion Rate (GC)**：任务成功完成的比例
- **Agent Error Rate (ER_agent)**：因 RA 错误导致失败
- **User Error Rate (ER_user)**：因用户模拟器错误导致失败
- **Tool/User Hallucination Rate (HR_tool / HR_user)**：工具或用户生成不一致信息的比例
- **Bootstrap Standard Error (SE)**：用于衡量稳定性

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Figure 3）
| 模型 | GC (%) |
|------|--------|
| GPT-5 (high RE) | **85.76** |
| GPT-5 (minimal RE) | 65.64 |
| GPT-4o | 84.47 |
| GPT-4o-mini | 83.21 |
| Gemini-2.5-pro | 80.53 |
| Qwen3-235B | 76.08 |
| Qwen3-30B (base) | 65.64 |

✅ **发现**：GC 随模型能力增强和推理努力（reasoning effort）增加而单调上升，说明该 benchmark 具备良好的**模型区分能力和排序稳定性**。

### 训练效果提升（SFT/RFT）
在 Qwen3-30B-base 上进行微调：
| 方法 | GC (%) | 提升幅度 |
|------|--------|---------|
| Base model | 65.64 | — |
| RFT (rejection-sampling fine-tuning) | 67.11 | +1.47 |
| SFT (supervised fine-tuning on successful trajectories) | **77.34** | +11.7 |

✅ 表明 benchmark 生成的 on/off-policy rollouts 对模型训练有效，且支持迁移至未见场景。

### 消融实验结果

#### （1）State Tracker 强弱影响（Table in §6.2）
| State Tracker 模型 | Tool Hallucination Rate |
|--------------------|--------------------------|
| GPT-5o (default) | **1.33% ± 0.53** |
| GPT-4o (weaker) | 3.61% ± 0.88 |

➡️ 说明更强大的 state tracker 能显著降低工具幻觉率，强调**准确状态追踪的重要性**。

#### （2）场景完整性消融（Figure 4）
随机移除部分系统/用户事实：
- 移除越多系统事实 → **tool hallucination rate 单调上升**
- 移除越多用户事实 → **user hallucination rate 单调上升**

➡️ 验证了精心设计的 scenario specification 是控制幻觉的关键。

#### （3）用户 persona 敏感性分析（Figure 5）
| Persona | ER_user (%) | HR_user (%) |
|--------|-------------|-------------|
| Power user | 3.55 | 0.67 |
| Confused user | 5.14 | 1.87 |
| Ambiguous user | 5.50 | 2.75 |

➡️ benchmark 能敏感反映不同用户行为带来的挑战；默认使用 power user 可确保比较聚焦于 RA 性能。

### 人类-LLM judge 一致性（Appendix A）
在 50 个样本上对比两位人类专家与 LLM judge 的标注：
| 维度 | 三方一致率 (%) |
|------|----------------|
| Goal completion | 82.7 |
| Tool hallucination | 94.7 |
| User hallucination | 94.7 |

✅ 支持 LLM judge 的**高可靠性**，为自动化评估提供依据。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Proxy State-Based Evaluation 是可行且可靠的替代方案**：
   - 在没有真实数据库的情况下，仍能实现稳定、可重复的目标完成评估。
   - GC 指标具有良好的模型区分性和排序一致性。

2. ✅ **LLM judges 可靠性强**：
   - 与人类专家的一致性超过 90%，支持自动化评估流水线。

3. ✅ **支持高效训练数据生成**：
   - 可生成带 reward 标签的 on-policy 和 off-policy rollouts，用于 SFT/RFT，显著提升模型表现。

4. ✅ **系统鲁棒性依赖于 scenario 完整性和 state tracker 能力**：
   - 缺失 facts 或弱 tracker 会导致幻觉率上升，凸显设计质量的重要性。

5. ✅ **具备用户行为敏感性**：
   - 可用于分析不同用户 persona 下的 agent 表现差异，支持更真实的用户体验建模。

### 局限性
- 当前 scenario 为人工构造的合成数据，尚未完全覆盖真实用户长尾分布。
- 对 state tracker 和 judge 的 prompt engineering 有一定依赖，可能存在隐式偏见。
- 尽管幻觉率接近零，但在极端复杂逻辑路径下仍有累积误差风险。

### 未来工作方向
- 扩展到更多 domain 和 tool types，提升通用性。
- 引入动态 scenario generation，进一步提高多样性。
- 探索将 proxy state tracker 与 symbolic reasoning 结合，提升状态一致性。
- 应用于 real-world A/B testing 和 online RLHF pipeline。

---

> 🔚 **总结一句话**：  
> 本文提出的 **Proxy State-Based Evaluation** 框架，在无需重工程构建 deterministic backend 的前提下，实现了**可靠、可扩展、可训练反馈集成**的多轮 tool-calling LLM agent benchmark，为工业级 agentic AI 的开发与评估提供了实用新范式。

</details>

---

### 7. [Adaptive Semi-Supervised Training of P300 ERP-BCI Speller System with Minimum Calibration Effort](https://arxiv.org/abs/2602.15955)

**Authors**: Shumeng Chen, Jane E. Huggins, Tianwen Ma  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.15955v1  

#### Abstract
A P300 ERP-based Brain-Computer Interface (BCI) speller is an assistive communication tool. It searches for the P300 event-related potential (ERP) elicited by target stimuli, distinguishing it from the neural responses to non-target stimuli embedded in electroencephalogram (EEG) signals. Conventiona...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Adaptive Semi-Supervised Training of P300 ERP-BCI Speller System with Minimum Calibration Effort*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统基于 P300 ERP 的 BCI 拼写系统依赖**大量标注数据进行校准（calibration）**，这一过程耗时长、易引起用户疲劳，限制了系统的实时性和实用性。尤其在实际应用中，获取高质量标注 EEG 数据困难，严重影响系统效率。

### 🚀 提出的新方法与创新思路
提出了一种**自适应半监督学习框架（adaptive semi-supervised EM-GMM）**，其核心思想是：
- **最小化初始校准成本**：仅需少量带标签的训练数据（如拼写一个简单词“GO”）即可初始化模型参数。
- **在线持续自适应更新**：利用后续无标签的真实使用数据，通过 Expectation-Maximization (EM) 算法结合 Gaussian Mixture Model (GMM) 动态优化分类器。
- **直接对 EEG 特征向量建模**：不同于以往工作对内积进行聚类，本文将 GMM 应用于原始 EEG 特征空间，并假设目标与非目标类别共享协方差矩阵，提升稳定性与泛化能力。

### 🔍 相比现有方法的优势
| 方面 | 传统方法（如 SVM, LDA, CNN） | 本论文方法 |
|------|-------------------------------|------------|
| 标注需求 | 高（需完整校准阶段） | 极低（仅需少量初始标签） |
| 实时适应性 | 无或弱（固定模型） | 强（动态更新均值与协方差） |
| 用户负担 | 大（长时间专注拼写校准文本） | 小（自由拼写即可持续优化） |
| 模型鲁棒性 | 受个体差异影响大 | 利用在线数据逐步适配个体特征 |

> ✅ **核心优势总结**：实现了“**即用即学（learn-as-you-go）**”的 BCI 拼写系统，在显著减少校准努力的前提下保持甚至超越离线全数据训练的性能。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **真实数据**：来自密歇根大学 Direct Brain Interface Lab (UM-DBI) 的公开 EEG 数据集。
- **参与者数量**：共 15 名被试（participants），每人包含：
  - **TRN（Training）数据**：拼写短语 “THE QUICK BROWN_FOX”（共 19 个字符，每个字符对应 15 个序列，总计约 285 个序列）
  - **FRT（Free-typing）测试数据**：最多 3 组自由拼写任务，用于评估模型泛化能力

### ⚙️ 实验设置
- **信号采集**：
  - 16 通道 EEG（F3, Fz, F4, C3, Cz, C4, P3, Pz, P4, etc.）
  - 采样率 256 Hz，经 notch filter (60Hz) 和 band-pass filter (0.5–6 Hz) 处理
  - 下采样至 32 Hz，每刺激截取 800ms（25 time points）
- **特征提取**：采用 xDAWN 算法降维，将 (400,1) 向量压缩为 (50,1)
- **刺激范式**：Row-Column Paradigm (RCP)，6×6 虚拟键盘，每次闪烁行/列，共 12 次/序列

### 🎯 评估指标
| 指标 | 公式/说明 |
|------|----------|
| **Character-level Accuracy (p)** | 正确识别字符的比例，要求 ≥0.7 才具实用价值 |
| **Information Transfer Rate (ITR)** | 衡量通信效率（bits/min），考虑准确率与速度：<br>$$ITR = \frac{1}{c}[\log_2 N + p\log_2 p + (1-p)\log_2(\frac{1-p}{N-1})]$$ |
| **BCI Utility** | 综合考量 ITR 与准确性惩罚项，更贴近实际可用性：<br>当 $p > 0.5$ 时：$(2p-1) \cdot IT R$；否则为 0 |
| **动态停止准则（Dynamic Stopping Criterion）** | 设定阈值 $\theta=0.9$，一旦累计预测概率超过该值即输出字符，模拟高效拼写场景 |

### 🔁 对比基线方法
- **Offline EM-GMM**：使用全部 TRN 数据一次性训练模型，作为性能上限基准
- **Adaptive EM-GMM**（本文方法）：每处理两个序列后更新一次模型参数，用于预测下一组 FRT 数据

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）仿真分析结果（Simulation Study, 200 runs）
- **Offline 方法**：使用全部 285 序列训练 → 平均字符准确率达 **91%**
- **Adaptive 方法**：仅用 **75 个训练序列**（≈10个字符）→ 平均准确率仍 > **80%**
  - ➤ 实现 **74% 的数据量缩减**，性能损失可控

#### （2）真实数据分析（Real Data, 15 participants）
| 指标 | 结果 |
|------|------|
| 达到最低可用精度（≥0.7）人数 | 9 / 15 名被试达标（在 adaptive 或 offline 条件下） |
| 在达标者中表现优于基线的人数 | **7 / 9** 名被试在 adaptive 设置下优于 offline |
| 自由拼写起始所需训练量 | 仅需约 **10 个字符（150 sequences）** 即可启动高效拼写，远少于传统的 19 字符 |

#### （3）典型个体趋势（Figure 6 & 7）
- **K117, K183**：adaptive 曲线快速上升，最终**超过 offline 性能**
- **K151**：虽有波动，但在某些阶段接近 offline 水平
- **K171**：缓慢但稳定增长，显示方法对不同用户具有适应性

#### （4）综合性能比较（Figure 8）
- **平均性能**（adaptive vs. offline，基于 ~150 seq vs. 285 seq）：
  - ✅ **Adaptive 方法三项指标均高于 Offline 基线**
    - 更高的平均 accuracy、ITR 和 BCI Utility
  - ✅ **更低的方差（tighter interquartile range）**
    - 显示更强的一致性和稳定性

> 💡 **重要发现**：尽管 adaptive 方法使用的训练数据仅为 offline 的一半左右，但多数用户达到了**更高或相当的性能水平**，证明了其高效的参数更新机制。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **有效降低校准负担**：提出的 adaptive semi-supervised EM-GMM 框架可在极少量标注数据下启动，并通过无标签数据持续优化，大幅减轻用户前期投入。
2. **性能优越且稳定**：在多个评估维度（accuracy, ITR, BCI utility）上，adaptive 方法不仅达到实用门槛（>0.7），还在多数被试中**优于传统离线训练方法**。
3. **具备强现实适用性**：支持“边用边学”的闭环模式，更适合 ALS 等严重运动障碍患者的长期日常使用。

### ⚠️ 方法的局限性
1. **依赖初始参数质量**：若初始 labeled data 质量差（如注意力不集中），可能导致 EM 收敛失败或陷入局部最优。
2. **未处理异步控制**：无法检测用户分心、暂停或终止意图，目前仅适用于连续拼写任务。
3. **共享协方差假设可能受限**：虽然简化了模型，但在极端噪声或状态变化下可能不够灵活。
4. **初始化策略有待优化**：当前依赖经验性权重组合，缺乏理论指导。

### 🔮 未来工作方向
1. **改进初始化策略**：探索基于迁移学习或多被试先验知识的 warm-start 初始化。
2. **引入上下文建模**：结合语言模型（如 n-gram 或 Transformer）分析 next-letter probability，实现 context-aware 拼写纠错与加速。
3. **增强抗噪能力**：集成先进去噪技术（如 ICA、深度去噪网络）以提高输入信号质量。
4. **融合强化学习**：优化刺激呈现顺序（stimulus selection），主动选择最具信息量的行/列进行闪烁，提升 ITR。
5. **扩展至其他 ERP 范式**：验证方法是否适用于 SSVEP、hybrid BCI 等其他类型系统。

---

> 🧠 **总体评价**：  
> 本文提出的 adaptive semi-supervised learning 框架为 P300-BCI 系统走向**真正实用化、低门槛化**提供了强有力的技术路径，标志着从“实验室原型”向“临床可用工具”的重要迈进。

</details>

---

### 8. [Multi-source Heterogeneous Public Opinion Analysis via Collaborative Reasoning and Adaptive Fusion: A Systematically Integrated Approach](https://arxiv.org/abs/2602.15857)

**Authors**: Yi Liu  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.15857v1  

#### Abstract
The analysis of public opinion from multiple heterogeneous sources presents significant challenges due to structural differences, semantic variations, and platform-specific biases. This paper introduces a novel Collaborative Reasoning and Adaptive Fusion (CRAF) framework that systematically integrat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**多源异构公共舆论分析**中的四大挑战：
- **Structural heterogeneity**（结构异质性）：不同平台的数据格式差异大；
- **Semantic divergence**（语义分歧）：同一事件在不同平台上表达方式不同；
- **Platform-specific noise**（平台特有噪声）：各平台无关内容比例不一；
- **Dynamic evolution**（动态演化）：舆情变化快，需近实时处理。

传统方法如独立建模、早期融合或晚期融合难以有效整合跨平台信息，且缺乏对平台特性与数据质量的自适应能力。

---

### 提出的新方法：CRAF 框架
提出 **Collaborative Reasoning and Adaptive Fusion (CRAF)** 框架，系统集成传统特征与大语言模型（LLM），通过多阶段推理机制实现高效多源融合。

#### 四大创新点：
1. **Cross-platform Collaborative Attention Module**  
   跨平台协同注意力模块，在对齐语义表示的同时保留源平台特性，促进知识迁移并抑制噪声干扰。

2. **Hierarchical Adaptive Fusion Mechanism**  
   层次化自适应融合机制，根据**数据质量**和**任务需求**动态加权特征，优于固定权重融合策略。

3. **Joint Optimization Strategy**  
   联合优化策略，在共享潜在空间中同时学习**topic representation** 和 **sentiment distribution**，捕捉二者相互增强效应。

4. **Multimodal Extraction Capability**  
   支持视频内容处理（如抖音、快手），集成 OCR、ASR 与视觉情感分析，实现文本-语音-图像多模态联合建模。

---

### 相比现有方法的优势
| 方法类型 | 缺陷 | CRAF 的改进 |
|--------|------|-------------|
| Platform-specific modeling | 忽视跨平台相关性 | 引入协同注意力，显式建模平台间关系 |
| Early/Late fusion | 维度灾难或错过联合表征学习 | 自适应门控融合，兼顾效率与表达力 |
| Pure LLM-based approaches | 计算昂贵，忽略平台差异 | 结合传统特征 + Pangu-7B，提升效率与鲁棒性 |
| Static fusion hybrids | 权重固定，无法适应数据波动 | 动态融合，响应数据质量和任务目标 |

此外，理论证明 CRAF 具有更紧的泛化界（tighter generalization bound），样本复杂度降低 $ O(d \log K / m) $，尤其适合低资源场景。

---

## 2. 核心实验方法和设置

### 数据集
在三个多平台数据集上进行评估：

| 数据集 | 平台数量 | 文档数 | 主题数 | 视频内容占比 |
|-------|--------|--------|--------|--------------|
| **Weibo-12** | 12 | 58,742 | 15 | 12% |
| **CrossPlatform-15** | 15 | 72,159 | 20 | 18% |
| **NewsForum-8** | 8 | 35,428 | 12 | 8% |

涵盖微博、知乎、抖音、搜狗等主流中文社交与新闻平台，包含图文及短视频内容。

---

### 实验设置与评估指标

#### 主要任务：
- **Topic Clustering**：使用 Adjusted Rand Index (**ARI**) 评价聚类一致性；
- **Sentiment Analysis**：使用 **F1-score**（macro）衡量分类性能。

#### 基线方法对比：
分为五类共多个 baseline：
1. **传统方法**：TF-IDF + K-Means / SVM
2. **单源深度学习**：BERT, RoBERTa
3. **多源融合方法**：Concatenation, Averaging, Weighted Fusion
4. **近期混合方法**：Attention-based fusion [4], Adaptive fusion [24]
5. **LLM-based 方法**：ChatGPT-4, ChatGLM-6B, **Pangu-7B**, LLaMA-7B

所有实验控制相同训练/测试划分，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）
| 方法 | Topic Clustering (ARI) ↑ | Sentiment Analysis (F1) ↑ |
|------|--------------------------|----------------------------|
| **CRAF (Ours)** | **0.76** | **0.84** |
| 最佳基线（Pangu-7B 单独运行） | 0.73 | 0.83 |
| ChatGPT-4 API | 0.74 | 0.82 |

- 在 Topic ARI 上平均提升 **4.1%**；
- 在 Sentiment F1 上平均提升 **3.8%**；
- 尤其在跨平台主题一致性方面表现突出。

---

### 与其他方法的对比优势
- **优于纯 LLM 方法**：尽管 Pangu-7B 本身强大，但 CRAF 通过结构化融合进一步提升了性能；
- **显著高于传统融合方法**：如 Concatenation Fusion（ARI=0.68）落后 CRAF 近 8 个百分点；
- **计算效率更高**：相比其他 LLM 方法，CRAF 推理速度更快，GPU 内存占用更低（见下表）。

#### 计算效率对比（Table 6）
| 方法 | GPU Memory (GB) | Inference Time (ms) | Training Time (hours) |
|------|------------------|------------------------|------------------------|
| BERT (per source) | 4.2 | 85 | 8.5 |
| Attention Fusion | 5.1 | 120 | 10.2 |
| **CRAF (Ours)** | **4.8** | **130** | **9.5** |
| Pangu-7B (standalone) | 6.8 | 145 | 14.5 |

→ CRAF 在性能大幅提升的同时，仅带来轻微资源开销增长。

---

### 消融实验结果（Ablation Study, Table 4）
验证各组件贡献：

| 变体 | Topic ARI | Sentiment F1 |
|------|-----------|---------------|
| **Full CRAF** | **0.76** | **0.84** |
| w/o collaborative attention | 0.71 (-6.6%) | 0.79 (-6.0%) |
| w/o adaptive fusion | 0.73 (-4.0%) | 0.81 (-3.6%) |
| w/o joint learning | 0.74 (-2.6%) | 0.82 (-2.4%) |
| w/o Pangu-7B (用 BERT 替代) | 0.72 (-5.3%) | 0.80 (-4.8%) |
| w/o traditional features | 0.74 (-2.6%) | 0.82 (-2.4%) |

✅ 结论：**协同注意力**和**自适应融合**是核心驱动力；**Pangu-7B** 提供更强语义表示；**传统特征**仍具补充价值。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **CRAF 显著优于现有方法**：在多个真实多平台数据集上取得 SOTA 性能，尤其在主题一致性和情感识别准确率方面领先。
2. ✅ **跨平台协同注意力有效对齐语义**：可视化显示平台间存在合理的信息流动（图2），既保持自身特性又吸收外部信号。
3. ✅ **强跨平台适应性**：在新平台 few-shot 场景下，仅需 **50 个标注样本**即可达到目标性能，较 BERT 微调减少 **75% 标注成本**（图4）。
4. ✅ **支持多模态输入**：可处理抖音等平台的视频内容，整合 OCR、ASR 与视觉情感分析，拓展应用场景。
5. ✅ **理论支撑强**：证明 CRAF 具有更优的泛化界，解释其在小样本下的优越表现。

---

### 方法的局限性（Section 7.1）
1. **Multimodal integration depth 不足**：目前为浅层融合，未实现深层跨模态交互；
2. **缺乏 online learning 能力**：面对 concept drift 需周期性重训练，不适合长期部署；
3. **Explainability 不足**：高风险应用中缺乏可解释性支持；
4. **计算资源仍偏高**：虽优于大型 LLM，但在边缘设备部署仍有挑战；
5. **文化与语言范围有限**：当前评估集中于中文平台，国际化适配待验证。

---

### 未来工作方向（Section 7.2）
1. **Enhanced multimodal fusion**：构建统一 Transformer 架构处理文本、图像、音频、视频；
2. **Federated learning**：实现隐私保护下的跨机构协作建模；
3. **Causal analysis**：从相关性分析转向因果推断，预测 opinion dynamics；
4. **Cross-cultural adaptation**：扩展至多语言环境，结合自动语言检测；
5. **Online learning mechanisms**：开发持续学习框架，应对社交媒体快速演变。

---

## 总结
> CRAF 是一个**系统集成、理论扎实、实证有效的多源异构公共舆论分析框架**。它通过 **collaborative reasoning** 与 **adaptive fusion** 的设计，实现了跨平台语义对齐、动态特征融合与联合多任务优化，在性能、效率与适应性之间取得了良好平衡。随着公众话语日益分散于多个平台，此类能够“整合多样视角、尊重平台个性”的智能分析系统将成为理解社会情绪的关键工具。

🔗 **代码开源地址**：[https://github.com/hmmnxkl/LLM-Based-Intelligent-Public-Opinion-Analytics-Assistant](https://github.com/hmmnxkl/LLM-Based-Intelligent-Public-Opinion-Analytics-Assistant)

</details>

---

### 9. [Supercharging Agenda Setting Research: The ParlaCAP Dataset of 28 European Parliaments and a Scalable Multilingual LLM-Based Classification](https://arxiv.org/abs/2602.16516)

**Authors**: Taja Kuzman Punger\v{s}ek, Peter Rupnik, Daniela \v{S}irini\'c, Nikola Ljube\v{s}i\'c  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.16516v1  

#### Abstract
This paper introduces ParlaCAP, a large-scale dataset for analyzing parliamentary agenda setting across Europe, and proposes a cost-effective method for building domain-specific policy topic classifiers. Applying the Comparative Agendas Project (CAP) schema to the multilingual ParlaMint corpus of ov...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Supercharging Agenda Setting Research: The ParlaCAP Dataset of 28 European Parliaments and a Scalable Multilingual LLM-Based Classification*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Comparative Agendas Project (CAP)** 研究依赖大量人工标注政策议题标签，成本高、扩展性差，难以实现对多语言、跨国家、大规模议会演讲的系统性分析。此外，现有自动分类模型通常在非目标领域（out-of-domain）数据上训练，导致在议会文本上的表现不佳。

### 🚀 提出的新方法与创新
本研究提出并实现了以下两个核心贡献：

#### （1）**ParlaCAP 数据集**
- 构建了一个覆盖 **28 个欧洲国家和地区议会**、包含 **超过 800 万条演讲** 的大规模、多语言政策议题标注数据集。
- 集成 **CAP 政策主题标签**、**ParlaSent 情感预测** 和丰富的元数据（如政党、议员身份、V-DEM 民主指数等），支持跨国家比较政治研究。

#### （2）**基于 LLM 的教师-学生框架（LLM Teacher-Student Framework）**
- 利用 **GPT-4o** 作为“教师”模型，自动标注训练数据，避免高昂的人工标注成本。
- 使用该 LLM 标注的数据微调轻量级多语言 BERT 模型（如 XLM-R-Parla），作为“学生”模型用于高效、可扩展的大规模推理。
- 引入针对稀有类别（如 *Public Lands*）的 **LLM 驱动数据增强策略**（“finding a needle in a haystack”），显著提升长尾类别的识别能力。

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 标注方式 | 手动标注（高成本、低效率） | LLM 自动标注 + 小样本验证（低成本、高可扩展） |
| 模型领域适配 | 使用跨领域数据（如新闻、行政文件） | 在 **in-domain**（议会语料）上训练，更强领域适应性 |
| 多语言支持 | 有限语言覆盖 | 覆盖 29 种语言，真正多语言统一建模 |
| 性能 | 受限于标注质量和数量 | 达到甚至接近人类标注者水平 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 描述 |
|-------|------|
| **ParlaMint 4.1 / 5.0** | 主要语料来源，包含来自 29 个欧洲国家/地区议会的 **800 万+ 演讲**，时间跨度约 2015–2022 年，涵盖 20+ 种语言。 |
| **CAP Master Codebook** | 提供 21 个主要政策议题标签（Major CAP Topics），本文新增 `Other` 类别以处理非政策性发言，共 22 类。 |
| **人工测试集（Test Sets）** | 构建了英文（EN）、克罗地亚语（HR）、塞尔维亚语（SR）、波斯尼亚语（BS）四个语言的测试集，每种约 800–880 条，由专家人工标注，用于评估模型性能。 |
| **LLM-annotated Training Set** | 使用 GPT-4o 对从各 ParlaMint 子语料中抽取的 34,800 条演讲进行自动标注，并通过数据增强将 `Public Lands` 类别样本从 145 增至 924。最终训练集为 35,579 条。 |

### ⚙️ 实验设置与评估指标

#### 模型选择
- **学生模型候选**：
  - `XLM-RoBERTa-large`
  - `XLM-R-Parla`（在议会文本上进一步预训练的 XLM-R 变体）
- 微调策略：使用 LLM 标注的训练数据进行 fine-tuning，采用学习率 $1 \times 10^{-5}$，训练 3 轮。

#### 评估指标
- **Macro-F1 Score**：主要评价指标，尤其关注类别不平衡下的整体性能。
- **Krippendorff’s Alpha**：衡量标注一致性（人 vs 人、人 vs LLM）。
- **Micro-F1 / Accuracy**：辅助指标，在过滤低置信度预测后报告。

#### 基线方法对比
- **CAP Babel Machine**（Sebok et al., 2024）系列模型：
  - 包括多个基于 `xlm-roberta-large` 微调的版本，分别在不同领域数据上训练（如 party manifestos, legislative speeches, media, social media 等）。
  - 同样使用 CAP schema，便于横向比较。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）模型在四语言测试集上的 Macro-F1 表现（Table 2）

| Model | EN | HR | SR | BS |
|-------|----|----|----|----|
| **ParlaCAP (XLM-R-Parla + PL-augmented)** | **0.72** | **0.68** | **0.71** | **0.64** |
| XLM-R-Parla (base) | 0.70 | 0.66 | 0.68 | 0.62 |
| XLM-RoBERTa | 0.68 | 0.65 | 0.67 | 0.63 |
| GPT-4o（上限参考） | 0.74 | 0.68 | 0.72 | 0.63 |

> ✅ 结果表明：**ParlaCAP 模型在所有语言上均优于基线模型**，且性能接近 GPT-4o，证明 LLM 教师的有效性。

#### （2）引入置信度过滤后的性能提升（Table 3）
设定预测置信度阈值为 0.6，低于则标记为 `Mix`（不确定）。保留高置信度样本后性能显著提高：

| Dataset | Micro-F1 | Macro-F1 | Accuracy |
|--------|----------|----------|----------|
| EN | 0.76 | 0.76 | 0.76 |
| HR | 0.72 | 0.73 | 0.72 |
| SR | 0.75 | 0.74 | 0.75 |
| BS | 0.69 | 0.68 | 0.69 |

> 💡 说明：通过简单置信度过滤即可大幅提升实际应用中的可靠性。

#### （3）与现有 CAP 模型的对比（Table 4）
在相同测试集上，ParlaCAP 明显优于其他公开可用的 CAP 分类器：

| Model Type | EN | HR | SR | BS |
|-----------|-----|-----|-----|-----|
| **ParlaCAP** | **0.72** | **0.69** | **0.71** | **0.65** |
| CAP-Babel (media, social, budget 等) | 0.57–0.64 | 0.51–0.63 | 0.58–0.62 | 0.44–0.57 |

> 🔺 差距最大达 **+15 个百分点**，尤其是在波斯尼亚语等挑战性语言上。

#### （4）消融实验与关键发现
- **领域适配至关重要**：尽管其他模型也使用 XLM-RoBERTa 架构，但由于训练数据来自非议会领域（如媒体、预算文件），其性能远低于在 **in-domain**（ParlaMint）数据上训练的 ParlaCAP。
- **数据增强有效解决稀有类问题**：对 `Public Lands` 类别进行关键词检索 + LLM 过滤后，F1 分数从 0.30 提升至 **0.80**，验证了“找针”策略的有效性。
- **LLM 作为标注者的可靠性**：GPT-4o 与人类标注者的 Krippendorff’s Alpha 达到 **0.60–0.64**，与人类间一致性（0.59–0.68）相当，支持其替代人工标注的可行性。

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **LLM 可有效替代人工进行高质量政策议题标注**  
   GPT-4o 在 CAP 标注任务上的表现与人类专家相当，为构建大规模领域特定数据集提供了低成本、高效率的新路径。

2. **领域内训练显著优于跨领域迁移**  
   即使是手动标注的高质量数据，若不在目标领域（议会演讲），其训练出的模型仍不如在 **in-domain** 数据上训练的小模型。

3. **ParlaCAP 是目前最全面的欧洲议会政策议程数据集之一**  
   不仅提供细粒度话题标签，还整合情感分析与丰富元数据，极大促进比较政治学、民主代表性和性别政治等领域的实证研究。

4. **方法具有高度可扩展性和复现性**  
   教师-学生框架可推广至其他语言和其他政策分类体系，代码和模型已开源。

### ⚠️ 局限性

- **测试集未公开发布**：出于防止 LLM 预训练污染的考虑，测试集不对外公开，可能影响第三方复现。
- **部分语言表现偏低**：如波斯尼亚语（BS）整体得分最低，反映其议会辩论结构松散、边界案例多，模型更具挑战。
- **依赖 GPT-4o 的闭源服务**：虽然学生模型开放，但初始训练数据依赖商业 LLM，存在可持续性和透明度问题。

### 🔮 未来工作方向

- 开发完全开源的 LLM 替代方案（如 Llama 系列）完成教师角色，提升方法自主性。
- 扩展至更细粒度的子议题分类（Subtopics）或动态议题检测。
- 探索多模态分析（结合语音停顿、语调等 ParlaMint 中的语音信息）。
- 应用于非欧洲国家议会，推动全球范围内的政策注意力比较研究。

---

## 🔗 资源链接（补充）

- **ParlaCAP 分类器（Hugging Face）**: [https://doi.org/10.57967/hf/6684](https://doi.org/10.57967/hf/6684)
- **ParlaCAP 数据集（CROSSDA）**: [https://doi.org/10.23669/1ZTELP](https://doi.org/10.23669/1ZTELP)
- **训练数据（CLARIN.SI）**: [http://hdl.handle.net/11356/2093](http://hdl.handle.net/11356/2093)
- **GitHub 教程与工具**: [https://github.com/clarinsi/parlacap](https://github.com/clarinsi/parlacap)

--- 

> 📌 **一句话总结**：本文通过 **LLM 教师-学生框架** 成功构建了迄今为止规模最大、最系统的欧洲议会政策议程数据集 ParlaCAP，证明了 **领域适配 + LLM 驱动标注** 是实现低成本、高性能、可扩展政策文本分析的有效范式。

</details>

---

### 10. [Fast KV Compaction via Attention Matching](https://arxiv.org/abs/2602.16284)

**Authors**: Adam Zweiger, Xinghong Fu, Han Guo, Yoon Kim  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.16284v1  

#### Abstract
Scaling language models to long contexts is often bottlenecked by the size of the key-value (KV) cache. In deployed settings, long contexts are typically managed through compaction in token space via summarization. However, summarization can be highly lossy, substantially harming downstream performa...

---

### 11. [LLM-Driven Intent-Based Privacy-Aware Orchestration Across the Cloud-Edge Continuum](https://arxiv.org/abs/2602.16100)

**Authors**: Zijie Su, Muhammed Tawfiqul Islam, Mohammad Goudarzi, Adel N. Toosi  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16100v1  

#### Abstract
With the rapid advancement of large language models (LLMs), efficiently serving LLM inference under limited GPU resources has become a critical challenge. Recently, an increasing number of studies have explored applying serverless computing paradigms to LLM serving in order to maximize resource util...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*LLM-Driven Intent-Based Privacy-Aware Orchestration Across the Cloud-Edge Continuum*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在 **cloud-edge continuum**（云边协同）环境中，随着 IoT 设备激增，大量敏感数据在边缘生成并需要处理。传统方案面临两大挑战：
- **性能延迟**：将所有数据传至云端处理会引入高延迟，不适用于实时应用（如自动驾驶）。
- **隐私合规性**：法规（如 GDPR）要求敏感数据必须保留在特定司法管辖区（如欧盟境内），而现有编排系统（如 Kubernetes 和 SDN 控制器）虽然功能强大，但需专家手动配置数十条低级规则来满足这些高阶隐私意图。

现有系统缺乏从自然语言意图到可执行策略的自动化翻译机制，导致部署成本高、易出错。

---

### 🚀 提出的新方法与创新思路
本文提出一个 **LLM 驱动的 intent-based 框架**，实现跨计算层（Kubernetes）和网络层（SDN）的隐私感知自动编排：

#### 主要创新点：
1. **统一的 intent 解析与执行框架**
   - 用户以自然语言输入隐私意图（如 “确保所有个人数据停留在欧盟”）。
   - 利用 **GPT-4o** 将其解析为结构化指令，并自动生成：
     - Kubernetes 层面的 `nodeSelector` 或亲和性规则（用于 pod 调度）
     - SDN 层面的 flow rules（通过 ONOS 控制器限制流量路径）

2. **跨层协同调度（Coordinated Compute + Network Scheduling）**
   - 同时控制 **计算资源放置** 和 **网络通信路径**，确保数据不仅“存得对”，而且“走得合规”。
   - 支持 hybrid intents（混合型意图），即同时包含计算与网络约束的复杂策略。

3. **构建首个标准化 benchmark 与自动化验证工具**
   - 构建了一个包含 **90 条多样化隐私意图** 的测试集，涵盖 computing、networking 和 hybrid 场景。
   - 开发了端到端的 **自动化 validator**，集成 Kubernetes + ONOS 测试床，无需人工干预即可部署配置并返回合规性报告。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本工作 |
|------|--------|-------|
| **意图表达方式** | DSL 或预定义模板，非技术人员难以使用 | 自然语言输入，业务人员/合规官也可参与 |
| **跨层支持** | 多数仅关注 compute 或 network 单一层 | 联合调度 compute + network，实现端到端合规 |
| **自动化程度** | 手动编写 YAML/flow rules，耗时且易错 | LLM 自动生成 + 自动验证闭环 |
| **评估手段** | 缺乏标准数据集，依赖人工验证 | 提供公开 intent corpus + 一键式 validator |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **自建隐私意图语料库（Privacy Intent Corpus）**：
  - 包含 **90 条自然语言隐私意图**
  - 分布均衡：
    - 30 条 computing-only
    - 30 条 networking-only
    - 30 条 hybrid（兼具计算与网络约束）
  - 按复杂度划分：
    - 38 条 simple（单条件）
    - 52 条 complex（多条件或例外）
- 示例：
  - *“禁止财务数据库部署在 cloud zone 节点上。”*
  - *“确保所有从主机2到主机4的流量必须经过备份交换机 s8。”*
  - *“预约服务只能运行在高安全等级的云节点上，且与主机4通信的流量必须经过备份交换机 s8。”*

---

### ⚙️ 实验设置
- **平台架构**：
  - **Orchestration Core**：K3s（轻量 Kubernetes） + ONOS（SDN 控制器）
  - **底层仿真**：Mininet + Open vSwitch 模拟网络拓扑
  - **LLM 接口**：通过 API 调用 GPT-4o、Claude 3.5 Haiku、DeepSeek-V3 进行对比
- **节点标签体系（Label Schema）**：
  - Compute 层：`location=EU`, `security=high`, `zone=edge/cloud`, `provider=aws/azure`
  - Network 层：`mfr=CISCO/Huawei`, `location=region-a`, `role=MASTER`

- **测试集群规模**：
  - 小型集群：5 worker 节点，9 个 OpenFlow 交换机
  - 大型集群：13 worker 节点，25 个交换机 —— 用于评估扩展性

- **应用负载模型**：
  - 模拟医院信息系统，包含 6 个微服务（如 `PHI-DB`, `Patient`, `Appointment`），标注其数据敏感性（PHI vs general）

---

### 🎯 评估指标
| 指标 | 定义 |
|------|-----|
| **End-to-end Success Rate** | 成功通过 validator 验证的 intent 比例 |
| **Avg. Completion Time** | 从输入 intent 到完成部署与验证的总耗时（wall-clock time） |
| **Token Usage** | 每次请求消耗的 prompt + completion tokens 数量 |
| **Avg. Checks / Task** | 每个 intent 触发的原子验证断言数量（反映策略复杂度） |
| **Failure Mode Analysis** | 对失败案例进行归因分析（如 hallucination、语义歧义等） |

---

### 🔁 基线方法对比
本文未直接比较传统手工配置，而是横向对比三种主流 LLM 在相同 prompt 框架下的表现：
- **GPT-4o**（OpenAI）
- **Claude 3.5 Haiku**（Anthropic）
- **DeepSeek-V3 (671B)**（开源大模型）

所有模型使用相同的提示工程模板（few-shot + schema constraint），保证公平性。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（基于 GPT-4o）

| 指标 | 结果 |
|------|------|
| **总体成功率（Success Rate）** | **95.6%**（86/90 成功） |
| **平均端到端延迟** | **20.97 秒** |
| **平均每任务 token 消耗** | **15,133 tokens** |
| **平均每任务验证检查数** | **3.7 项断言** |

---

### 🔁 与其他 LLM 的对比结果
| Model | Success Rate | Avg. Time (s) | Token Usage |
|-------|--------------|----------------|-------------|
| **GPT-4o** | **95.6%** | 20.97 | 15,133 |
| Claude 3.5 Haiku | 86.7% | ~20 | ~11,000 |
| DeepSeek-V3 | 77.8% | ~88 | ~22,000 |

> ✅ **GPT-4o 显著优于其他模型**，尤其在 hybrid 和 networking 场景中优势明显。

#### 按领域拆解成功率（GPT-4o）：
| 类型 | 成功率 |
|------|--------|
| Computing | 100.0% |
| Networking | 90.3% |
| Hybrid | 96.7% |

> 💡 尽管 hybrid 更复杂，但成功率仍高达 96.7%，说明框架具备良好鲁棒性。

#### 按复杂度拆解：
| 复杂度 | 成功率 | 平均时间 | 平均 checks |
|--------|--------|----------|------------|
| Simple | 96.2% | 13.07s | 1.1 |
| Complex | 94.9% | 26.89s | 5.6 |

> ⚠️ 复杂度提升带来更高的验证开销和延迟，但成功率下降有限（仅 1.3%），表明 LLM 能较好处理多条件逻辑。

---

### 🔍 消融实验与失败模式分析（Failure Modes）
通过对 4 个失败案例的手动分析，识别出四类系统性错误：

| 错误类型 | 描述 | 示例 |
|---------|------|------|
| **Semantic Congestion（语义拥堵）** | Hybrid 意图中多个子句融合成一句话，LLM 只响应第一个条件 | “A 且 B 且 C” → 仅生成 A 的规则 |
| **Ambiguous Path Specification（模糊路径描述）** | 缺少明确 `<src, dst>` 对，导致路径规划失败 | “与 host4 通信需经 backup switch” → 无具体源目标 |
| **Hallucinated Identifiers（幻觉标识符）** | LLM 发明不存在的标签（如 `eu_region`） | 输出 `region: eu_region`，但实际为 `location: london` |
| **Partial Topology Awareness（拓扑认知不全）** | 忽略中间传输设备的位置标签，导致路径绕出合规区域 | 排除了 region-b 主机，但未排除 region-b 的聚合交换机 |

✅ **缓解措施有效**：对失败 intent 进行 **句子分解** 或 **显式枚举约束** 后，多数可重新成功执行。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM 可作为高效的 intent-to-policy 编译器**  
   GPT-4o 能将自然语言隐私意图准确转化为 Kubernetes + ONOS 可执行策略，**成功率高达 95.6%**，接近实用水平。

2. **跨层协同是实现端到端隐私合规的关键**  
   单纯控制 pod 放置不足以保障数据安全，必须联合管理网络路径。本文首次实现了 compute + network 的统一 intent 编排。

3. **自动化验证闭环极大降低人工成本**  
   自研 validator 可在几秒内完成部署后状态检查，相比传统人工审核节省数小时，显著提升运维效率。

4. **hybrid intents 是最具挑战但也最实用的场景**  
   虽然耗时更长（~39s）、token 更多（~28k），但真实世界需求往往涉及多重约束，值得优化支持。

---

### ⚠️ 方法的局限性
1. **依赖基础设施标签完整性与一致性**  
   若节点/设备标签缺失或命名不规范（如 `EU` vs `eu_region`），会导致规则无法匹配甚至失效。

2. **外部 LLM 存在隐私泄露风险**  
   敏感意图可能通过 API 泄露给第三方服务商；建议未来采用本地化部署或差分隐私保护。

3. **当前 success rate 不足以支撑完全无人值守部署**  
   95.6% 的成功率意味着每 20 次有 1 次失败，在高安全性场景下仍需 human-in-the-loop 审核。

4. **prompt engineering 影响输出稳定性**  
   尽管已有 few-shot 和 schema 约束，但仍存在 hallucination 和语义偏移问题，需持续优化提示设计。

---

### 🔮 未来工作方向
1. **自动 intent 分解（Automatic Intent Decomposition）**  
   将复杂的 hybrid intent 拆分为独立的 compute 和 network 子任务，降低 LLM 处理难度。

2. **领域适配与 fine-tuning**  
   在私有云/医疗/金融等垂直领域微调 LLM，提升对行业术语和合规要求的理解能力。

3. **增强 label resolution 与 schema mapping**  
   引入 ontology alignment 技术，解决标签异构问题（如自动映射 `EU ⇨ region=london,sydney`）。

4. **与加密机制结合（如 MPC/TLS）**  
   当前聚焦 policy compliance，未来可整合 cryptographic confidentiality，形成多层次隐私防护。

5. **在真实生产环境验证**  
   当前基于仿真测试床，下一步将在 healthcare IoT、regulated finance 等实际场景中试点部署。

---

> 🌟 **最终愿景**：让用户只需说一句“我的数据不能离开中国”，系统就能自动完成从容器调度到网络路由的全套合规配置——真正实现 **human intent 与 machine enforcement 的无缝衔接**。

</details>

---

### 12. [Training-Free Adaptation of Diffusion Models via Doob's $h$-Transform](https://arxiv.org/abs/2602.16198)

**Authors**: Qijie Zhu, Zeqi Ye, Han Liu, Zhaoran Wang, Minshuo Chen  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16198v1  

#### Abstract
Adaptation methods have been a workhorse for unlocking the transformative power of pre-trained diffusion models in diverse applications. Existing approaches often abstract adaptation objectives as a reward function and steer diffusion models to generate high-reward samples. However, these approaches...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Training-Free Adaptation of Diffusion Models via Doob's $h$-Transform

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **diffusion model** 适应方法在将预训练模型调整到下游任务时面临以下挑战：
- **需要额外训练**：如 RL-based fine-tuning 或 guidance-based 方法，计算开销大且依赖超参数调优。
- **依赖可微奖励函数**：许多训练-free 方法（如基于梯度引导的方法）要求 reward 函数是可微的，但在实际应用中（如分子设计、机器人控制），reward 往往来自黑盒工具或离散描述符，**不可微**。
- **缺乏理论保证**：尽管已有方法在经验上有效，但很少提供收敛性或误差界的理论分析。

因此，本文旨在解决一个关键问题：  
> **能否设计一种无需训练、适用于非可微 reward 的高效推理时适配算法，并为其提供理论收敛保证？**

### 提出了什么新方法或新思路
作者提出了一种名为 **DOIT (Doob-Oriented Inference-time Transformation)** 的训练免费（training-free）适配框架，其核心思想如下：

- 将适配任务建模为从预训练生成分布 $P_\theta$ 到高奖励条件分布 $P_\theta(\cdot \mid \mathcal{E}_{x_0})$ 的 **measure transport** 问题，其中 $\mathcal{E}_{x_0} = \{x_0 : r(x_0) \geq r_0\}$ 表示满足高奖励条件的样本集合。
- 引入 **Doob's $h$-transform** 来实现该分布变换。该变换通过向扩散采样过程引入一个动态修正项 $-\nabla \log h(x,t)$，从而无需修改预训练 score network 参数即可改变采样路径。
- 设计了一种基于 **Monte Carlo 模拟的近似方法** 来估计难以直接计算的 $\nabla \log h(x,t)$，该方法仅需查询 reward 值，不依赖其梯度。

### 相比现有方法的优势
| 维度 | DOIT |
|------|------|
| **是否需要训练** | ❌ 完全无需训练（training-free） |
| **是否支持非可微 reward** | ✅ 支持任意 reward（只需能查询值） |
| **计算效率** | ⚡️ 推理时间开销可控，远低于 SMC 类方法 |
| **理论保障** | ✅ 提供高概率下的总变差距离收敛界 |
| **灵活性** | ✅ 可与其他 inference-time 方法（如 BoK、BFS）结合 |

相比现有方法，DOIT 在保持高效的同时突破了对 reward 可微性的限制，并首次为基于 Doob’s transform 的方法提供了严格的误差传播分析。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **文本到图像生成任务**：使用 **Stable Diffusion v1.5** 模型，在 LAION 数据集上训练，评估其生成图像的美学评分提升效果。
- **Offline Reinforcement Learning 任务**：采用 **D4RL benchmark**，包含三个 MuJoCo 环境：
  - `HalfCheetah`
  - `Hopper`
  - `Walker2d`
  并使用三种数据集配置：
  - `medium-expert`
  - `medium`
  - `medium-replay`

### 实验设置和评估指标
#### 文本到图像任务
- **Reward 函数**：
  - LAION Aesthetic Score（美学评分）
  - ImageReward（学习型偏好模型）
- **评估方式**：
  - 生成多个样本后绘制 **violin plot** 展示 reward 分布变化。
  - 报告最小值、均值、最大值及四分位数统计量。
- **采样设置**：
  - 使用 Euler ancestral sampler，共 20 步。
  - DOIT 应用于前 $l^*=10$ 步，每步使用 $M=32$ 个 MC 轨迹进行估计。

#### Offline RL 任务
- **Action Generation**：扩散策略作为动作生成器，输入状态 $s$ 输出动作序列 $a$。
- **Reward Oracle**：使用 Lu et al. [2023] 提供的真实 Q-function 作为 reward 反馈。
- **评估指标**：标准 return（回报）得分，越高越好。
- **对比基线**：
  - **Training-based**:
    - IQL
    - Diffuser
    - D-QL
    - QGPO
  - **Inference-time (training-free)**:
    - TFG
    - DAS
    - TTS
    - Best-of-K (BoK)
    - BFS

### 基线方法对比维度
| 方法类型 | 是否训练 | 是否需 reward 梯度 | 效率 | 理论支持 |
|--------|--------|------------------|------|----------|
| RL fine-tuning | ✅ 是 | 不适用 | 低 | 否 |
| Guidance-based | ✅ 是 | ✅ 需要 | 中 | 否 |
| SMC-based | ❌ 否 | ❌ 不需要 | 低（高 NFE） | 否 |
| Search-based | ❌ 否 | ❌ 不需要 | 中高 | 否 |
| **DOIT (ours)** | ❌ 否 | ❌ 不需要 | **高** | ✅ 是 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（D4RL 结果）

| Dataset | Environment | IQL | Diffuser | D-QL | QGPO | TFG | DAS | TTS | **DOIT (Ours)** |
|--------|------------|-----|----------|------|-------|------|-----|-----|----------------|
| Medium-Expert | HalfCheetah | 86.7 | 79.8 | 96.1 | 93.5 | 90.2±0.2 | 93.3±0.3 | 93.9±0.3 | **93.9±0.5** |
| Medium-Expert | Hopper | 91.5 | 107.2 | 110.7 | 108.0 | 100.2±3.5 | 105.4±5.1 | 104.4±3.1 | **107.7±5.6** |
| Medium-Expert | Walker2d | 109.6 | 108.4 | 109.7 | 110.7 | 108.1±0.1 | 111.4±0.1 | 111.4±0.1 | **113.2±0.7** |
| Medium | HalfCheetah | 47.4 | 44.2 | 50.6 | 54.1 | 53.1±0.1 | 53.4±0.1 | 54.8±0.1 | **55.3±0.3** |
| Medium | Hopper | 66.3 | 58.5 | 82.4 | 98.0 | 96.2±0.5 | 71.3±2.7 | 99.5±1.7 | **101.0±0.3** |
| Medium | Walker2d | 78.3 | 79.7 | 85.1 | 86.0 | 83.2±1.4 | 83.9±0.9 | 86.5±0.2 | **86.9±0.3** |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| **Average (Locomotion)** | — | 76.9 | 75.3 | 86.3 | 86.6 | 82.1 | 80.2 | 86.1 | **87.6** |

> ✅ **DOIT 在 9 个任务中的 8 个上取得最佳表现，平均得分为 87.6，超越所有基线方法**。

### 与基线方法的对比结果
- **优于训练-based 方法**：DOIT 超过了 QGPO 和 D-QL 等需训练的方法，说明无需训练也能达到甚至超过 fine-tuned 模型的表现。
- **显著优于其他 inference-time 方法**：
  - 在 `Hopper-medium` 上比 TTS 提升 **+1.5**。
  - 在 `Walker2d-medium-expert` 上达到 **113.2**，领先第二名 1.8 分。
- **采样效率高**：相比 SMC 或 tree search 方法，DOIT 的 NFE（Number of Function Evaluations）增加极少，接近原始采样速度。

### 消融实验结果
#### （1）Surrogate vs Full Trajectory Simulation
| 方法 | Min | Mean | Max | Runtime (sec/image) |
|------|-----|------|-----|---------------------|
| Pre-trained | 5.737 | 6.372 | 7.052 | 1.260 |
| Surrogate (ours) | 5.987 | 6.726 | 7.501 | **1.712** |
| Full Simulation | 6.006 | 6.714 | 7.553 | 39.584 |

> 🔍 **Surrogate 近似方法在性能几乎持平的情况下，运行时间仅为完整模拟的约 1/23，极具实用性**。

#### （2）与 Resampling 方法结合
| K | BoK | BoK + DOIT | BFS | BFS + DOIT |
|----|------|-------------|------|--------------|
| 4 | 0.702 | 0.875 | 0.882 | **0.950** |
| 8 | 0.896 | 1.001 | 1.087 | **1.115** |

> 🔄 **DOIT 与 BoK/BFS 结合带来显著增益，说明其“提升候选池质量”的能力具有正交互补优势**。

---

## 4. 关键结论和发现

### 主要发现
1. **DOIT 成功实现了无需训练、适用于非可微 reward 的高效适配**：
   - 通过 Doob’s $h$-transform 实现 measure transport，避免了参数更新。
   - Monte Carlo 估计 $\nabla \log h$ 仅需 reward 查询，无需梯度。
2. **理论上有保障**：
   - 在合理假设下，证明了输出分布与目标分布之间的 total variation 距离以高概率收敛。
   - 误差界由 MC 采样数 $M$、离散化误差 $e_{\text{dis}}$ 控制。
3. **实验证明有效性与通用性**：
   - 在图像生成任务中显著提升 aesthetic score。
   - 在 D4RL 上全面超越 state-of-the-art 基线，包括训练型与推理型方法。
   - 可灵活集成至其他 inference-time 框架中进一步增强性能。

### 方法的局限性
- **对高 reward 区域稀疏敏感**：当 $P(\mathcal{E}_{x_0})$ 极小时（即高 reward 样本罕见），MC 估计方差增大，可能导致不稳定。
- **依赖预训练模型质量**：若原始 $P_\theta$ 与真实数据分布差距过大，则即使完美适配也难获得高质量样本。
- **超参数调节需求**：需调参如 correction strength $\gamma$、temperature $T$、cutoff time $l^*$ 等，不同任务需重新搜索。

### 未来工作方向
- 设计自适应 MC 采样机制，缓解稀有事件带来的估计困难。
- 扩展至多目标 reward 或 constraint satisfaction 场景。
- 探索更高效的 $h$-function 参数化形式，减少模拟成本。
- 将 DOIT 思想推广至 flow models、autoregressive models 等其他生成架构。

--- 

> 💡 **总结一句话**：  
> DOIT 提出了一种基于 **Doob's $h$-transform** 的全新视角来实现 diffusion model 的训练免费适配，在理论上严谨、实践中高效，尤其擅长处理非可微 reward，为 inference-time alignment 开辟了新路径。

</details>

---

### 13. [How Uncertain Is the Grade? A Benchmark of Uncertainty Metrics for LLM-Based Automatic Assessment](https://arxiv.org/abs/2602.16039)

**Authors**: Hang Li, Kaiqi Yang, Xianxuan Long, Fedor Filippov, Yucheng Chu, Yasemin Copur-Gencturk, Peng He, Cory Miller, Namsoo Shin, Joseph Krajcik, Hui Liu, Jiliang Tang  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16039v1  

#### Abstract
The rapid rise of large language models (LLMs) is reshaping the landscape of automatic assessment in education. While these systems demonstrate substantial advantages in adaptability to diverse question types and flexibility in output formats, they also introduce new challenges related to output unc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：How Uncertain Is the Grade? A Benchmark of Uncertainty Metrics for LLM-Based Automatic Assessment**

---

## 1. **论文的主要贡献和创新点**

### ✅ 解决了什么问题
本论文聚焦于**LLM-based 自动评估系统中的不确定性量化（Uncertainty Quantification, UQ）问题**。尽管大型语言模型（LLMs）在自动评分任务中展现出强大的适应性和灵活性，但由于其生成过程具有内在随机性，输出结果存在不可忽视的不确定性。这种不确定性若未被有效识别和校准，可能导致错误的教学决策、误导学生反馈，甚至加剧教育不公平。

然而，现有的 UQ 方法大多针对分类或回归任务设计，难以直接应用于**序数型评分（ordinal grades）** 和**基于提示（prompt-driven）的评估场景**。此外，许多方法依赖模型内部状态（如 logits），不适用于闭源 LLMs。

### ✅ 提出了什么新方法或新思路
本文并未提出全新的 UQ 方法，而是构建了一个**首个面向 LLM-based 自动评估任务的综合性 UQ 基准测试框架（benchmark）**，系统地评估和比较了多种不确定性度量方法在真实教育评估场景下的表现。

其核心创新在于：
- 首次将 UQ 研究系统引入到 **LLM-based 自动评分（automated grading）** 场景；
- 考虑了教育评估特有的挑战：**序数标签空间（ordinal output）、rubric 驱动输入、多学科覆盖、提示敏感性等**；
- 统一采用**重复采样（repetition-based）策略**，确保方法可跨开源与闭源模型通用；
- 构建了涵盖多种学科、评分形式和模型规模的综合实验设置。

### ✅ 相比现有方法的优势
相比以往零散、任务特定或仅限白盒访问的研究，该基准具备以下优势：
- **普适性强**：支持 black-box 模型，适用于实际部署环境；
- **任务对齐性高**：专为自动评分任务定制，考虑了序数误差、语义推理链等因素；
- **全面性**：从有效性（effectiveness）、稳定性（stability）、相关性（correlation）三个维度进行多角度评估；
- **可复现与开放性**：提供了标准化的数据、模板和评估流程，推动领域发展。

---

## 2. **核心实验方法和设置**

### 📚 使用了哪些数据集
共使用 **14 个评分问题**，来自 **5 个不同来源**，涵盖多个学科：

| 数据集 | 类型 | 内容 | 规模 | 分数范围 |
|-------|------|------|------|--------|
| **ASAP** | 开源 | 中学生短文写作评分 | 500 条 | 0–3 |
| **SemEval-13-T7** | 开源 | 学生问答匹配参考答案 | 500 条 | 0–3 |
| **T1/T2-DCI & SEP** | 私有 | 化学概念理解（3DLP 标准） | ~124–141 条 | 0–3 |
| **T1/T2-CK & PCK** | 私有 | 数学教学法知识 | ~188–208 条 | 0–3 |
| **U41/U42Q1/Q2** | 私有 | 地球科学问答 | ~159–209 条 | 0–4 |

每题随机选取 60–150 个回答用于评估。

### ⚙️ 实验设置
#### **LLM 模型集合**
- **闭源模型（7个）**：GPT-4o mini, GPT-5, Claude Haiku/Sonnet 4.5, Gemini 2.5 Pro / 3 Flash 等；
- **开源模型（7个）**：Llama 3.1 8B, Qwen3 4B, Mixtral 8x7B, DeepSeek R1 Distill-Qwen 32B 等。

#### **生成策略（Generation Settings）**
三种主流提示方式：
1. **Zero-shot**：直接给出 rubric 并要求打分；
2. **Zero-shot + CoT**：加入 Chain-of-Thought 推理步骤；
3. **Few-shot + CoT**：提供每个分数等级的示例 + CoT。

所有实验均对每个样本生成 **5 次响应（N=5）** 以估计不确定性。

### 📊 评估指标
从三个维度评估 UQ 方法：

#### （1）**有效性（Effectiveness）**
衡量不确定性是否能准确反映预测准确性：
- **AUROC**：区分正确 vs 错误预测的能力；
- **C-Index**：衡量不确定性和误差严重程度的一致性（适用于序数评分）；
- **AUARC**：选择性拒绝高不确定性样本后保留预测的准确率提升；
- **AUERC**：选择性拒绝后剩余预测的平均绝对误差下降（越低越好）。

#### （2）**稳定性（Stability）**
衡量随着采样次数增加，UQ 估计的变化程度：
- **Delta（相对变化率）**：连续步长间 UQ 值的变化；
- **Spearmanr**：不同采样数量下排名的相关性（越高越稳定）。

#### （3）**相关性（Correlation）**
使用 **Pearson 相关系数** 分析不同 UQ 方法之间的冗余性。

### 🔁 基线方法对比
共评估 **13 种 UQ 方法**，分为两类：

#### **Category-based Methods（基于类别频率）**
- **Numset**：唯一输出的数量
- **MAR (Max-Agree-Rate)**：最高频答案占比
- **CE (Categorical Entropy)**：类别分布熵
- **FSD (First-Second Distance)**：前两名频率之差

#### **Relation-based Methods（基于语义关系图）**
基于三种相似度构建图：
- **Jaccard Similarity (JS)**：词元重叠
- **Embedding Cosine Similarity (Emb)**：句子嵌入相似度
- **Entailment Score (NLI)**：自然语言推断得分

再结合四种图属性计算不确定性：
- **NAD**（归一化平均度）
- **GE**（图偏心率）
- **Eigen**（拉普拉斯第二小特征值）
- **DSE**（离散语义熵）

---

## 3. **主要实验结果和性能指标**

### 📈 关键性能数据（见 Table 3）

| 方法 | AUROC Rank | C-Index Rank | AUARC Rank | AUERC Rank | 综合表现 |
|------|------------|--------------|-------------|-------------|-----------|
| **CE** | **4.71** | **5.44** | **4.91** | **5.50** | **最优** |
| **FSD** | 5.20 | 5.67 | 5.24 | 5.61 | 第二 |
| **MAR** | 4.82 | 5.50 | 5.01 | 5.51 | 第三 |
| **Numset** | 5.86 | 6.55 | 5.75 | 6.32 | 中等 |
| Relation-based 方法 | 6.97~13.57 | 6.69~12.84 | 7.03~9.18 | 6.78~9.08 | 普遍较差 |

> 注：排名越低表示性能越好（1 为最佳）

### 🔍 与基线方法的对比结果
- **Category-based 方法显著优于 Relation-based 方法**，尤其是 **CE、FSD、MAR** 在所有有效性指标上均领先。
- 这表明在自动评分这类**有限标签空间任务中，简单的频率统计已足够有效**。
- Relation-based 方法虽理论更复杂，但在实践中因噪声大、计算开销高而表现不佳。

### 🔬 消融实验与视角分析（Section 4.3.2 & 4.3.3）
#### 不同因素的影响：
- **模型质量影响显著**：
  - 对低性能模型（如 `stablelm-2-1-6b-chat`），category-based 方法更优；
  - 对高性能模型（如 `gemini-3-flash`），relation-based 方法开始显现潜力，因其生成的 rationale 更连贯。
- **问题类型敏感度最高**：方法排名在不同题目间波动最大；
- **生成策略影响最小**：zero-shot vs few-shot+COT 差异不大。

#### 稳定性结果（Table 4）
| 方法 | Delta Rank | Spearmanr Rank |
|------|-----------|----------------|
| **JS_GE**, **JS_NAD** | 较低 | 较高 |
| **CE** | 中等 | 中等偏上 |
| **NLI_DSE**, **Embed_Eigen** | 最差 | 最差 |

> 结论：**某些 relation-based 方法（如 JS_NAD/GE）在稳定性上优于 category-based 方法**，说明存在“有效性 vs 稳定性”的权衡。

#### 相关性分析（Figure 5）
- Category-based 方法之间高度相关（Pearson > 0.8），存在明显冗余；
- Relation-based 方法内部也呈现强相关性，尤其同一图构造策略下；
- 跨类方法相关性较低，建议组合使用以获得互补信号。

---

## 4. **关键结论和发现**

### ✅ 主要发现
1. **Category-based UQ 方法在自动评分中最为有效**  
   尤其是 **Categorical Entropy (CE)** 表现最佳，简单高效且无需额外模型。

2. **Relation-based 方法虽有效性弱，但稳定性更强**  
   特别是基于 Jaccard 图的 **NAD 和 GE** 指标，在采样数少时更鲁棒，适合资源受限场景。

3. **没有单一最优 UQ 方法，需根据上下文选择**
   - 当模型能力较弱时 → 优先使用 **CE/FSD/MAR**；
   - 当模型能力强、生成 rationale 质量高时 → 可尝试 **NLI 或 Embedding-based 方法**；
   - 强调部署稳定性时 → 考虑 **JS_NAD/GE**。

4. **UQ 方法之间存在显著冗余**  
   同一类别的方法高度相关，应避免堆叠使用；推荐跨类别组合以增强可靠性。

5. **模型能力和任务特性显著影响 UQ 效果**  
   UQ 性能不仅取决于方法本身，还受 LLM 规模、学科难度、评分标准清晰度等影响。

---

### ⚠️ 方法的局限性
- 所有 UQ 方法均基于 **重复生成（N=5）**，增加了延迟与成本；
- 未探索 **单次生成中的自省式 UQ（self-reflection）** 方法（如让 LLM 自评置信度）；
- 数据集仍以英文为主，缺乏多语言和真实课堂动态数据；
- 所有 ground truth 均由人工标注，可能存在主观偏差。

---

### 🔮 未来工作方向
1. 设计 **轻量级、单次生成即可估计 UQ 的方法**；
2. 探索 **融合 category 和 relation 信息的 hybrid UQ 框架**；
3. 研究如何将 UQ 信号用于 **主动学习或 human-in-the-loop grading 流程优化**；
4. 扩展至更多语言、更复杂的评估任务（如项目制学习评价）；
5. 推动建立统一的 **教育领域 UQ benchmark 标准**。

---

> 💡 **一句话总结**：  
> 本文首次系统 benchmark 了 LLM-based 自动评分中的不确定性度量方法，发现 **Categorical Entropy 是最有效的方法**，但也揭示了**有效性与稳定性之间的权衡**，强调应根据模型能力、任务需求和部署条件**灵活选择 UQ 策略**，为构建可靠、可解释的智能教育评估系统奠定了基础。

</details>

---

### 14. [Framework of Thoughts: A Foundation Framework for Dynamic and Optimized Reasoning based on Chains, Trees, and Graphs](https://arxiv.org/abs/2602.16512)

**Authors**: Felix Fricke, Simon Malberg, Georg Groh  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16512v1  

#### Abstract
Prompting schemes such as Chain of Thought, Tree of Thoughts, and Graph of Thoughts can significantly enhance the reasoning capabilities of large language models. However, most existing schemes require users to define static, problem-specific reasoning structures that lack adaptability to dynamic or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Framework of Thoughts (FoT)**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**

现有的 **prompting schemes**（如 Chain of Thought, Tree of Thoughts, Graph of Thoughts）存在三大关键局限：

1. **静态图结构**：大多数方案依赖用户手动定义固定的推理流程（如树或图结构），无法动态适应新问题或复杂变化的问题类型。
2. **优化不足**：提示（prompts）和超参数（hyperparameters）通常未经过系统优化，导致性能潜力未被充分挖掘。
3. **执行效率低**：多数方案串行执行 LLM 调用，重复计算多，推理成本高且耗时长。

### **提出了什么新方法或新思路**

作者提出 **Framework of Thoughts (FoT)** ——一个通用的、基础性的框架，用于构建和优化基于链（chains）、树（trees）和图（graphs）的动态推理方案。

FoT 不是一个具体的推理方法，而是一个支持多种 prompting schemes 的**可扩展开发平台**，其核心创新包括：

- **动态图结构（Dynamic Graph Structures）**  
  允许推理图在运行时由操作（operations）自动修改，实现自适应的、问题驱动的推理路径演化。

- **并行化执行（Parallelized Execution）**  
  通过 **Controller** 和 **Scheduler** 模块协调，并发执行无冲突的操作，显著提升运行速度。

- **智能缓存机制（Intelligent Caching）**  
  支持两种缓存：
  - **Process Cache**：单次执行内临时缓存
  - **Persistent Cache**：跨样本持久化缓存，避免重复昂贵的 LLM 调用。

- **内置优化工具（Built-in Optimization）**  
  集成 **Optuna** 进行超参数调优，以及 **DSPy** 中的 COPRO 算法进行 prompt 优化，支持多目标优化（准确率、延迟、成本等）。

### **相比现有方法的优势**

| 特性 | 传统框架（如 GoT） | FoT |
|------|------------------|-----|
| 图结构 | 静态预定义 | 动态演化 |
| 执行方式 | 串行为主 | 并行安全调度 |
| 缓存机制 | 无或临时 | 双层缓存（进程级 + 持久级） |
| 优化支持 | 无 | 内置超参 + prompt 优化 |
| 通用性 | 特定方案专用 | 通用模块化框架 |

> ✅ FoT 是首个将 **动态性、高效性、可优化性** 统一于同一框架中的 reasoning infrastructure。

---

## **2. 核心实验方法和设置**

### **使用的数据集**

| 方案 | 数据集 | 任务描述 |
|------|--------|----------|
| **ToT** | Game of 24 (Go24) | 给定4个数字，用 `+ - × /` 和括号构造等于24的表达式 |
| **ToT / GoT** | Sorting | 将128个整数排序（升序） |
| **GoT** | Document Merging (DM) | 合并多个 NDA 文档，最大化信息保留、最小化冗余 |
| **ProbTree** | HotpotQA, MuSiQue | 多跳问答（multi-hop QA），需分解问题并检索证据 |

### **实验设置**

- **训练/测试划分**：
  - Go24: 200训练 / 100测试
  - Sorting & DM: 各50训练 / 50测试
  - HotpotQA & MuSiQue: 各1000训练 / 1000测试

- **LLM 使用情况**：
  - Go24: GPT-4o（替代原版 GPT-4）
  - Sorting / DM: GPT-3.5-Turbo
  - ProbTree: GPT-4.1-mini（替代已下线的旧 GPT-3）

- **评估指标**：
  - **Go24**: 准确率（Accuracy）
  - **Sorting**: 错误数（Mistakes，越少越好）
  - **DM / HotpotQA / MuSiQue**: F1 分数（越高越好）
  - **效率指标**：平均运行时间（秒）、每次推理成本（USD cents）

- **基线对比**：
  - 原始 ToT、GoT、ProbTree 实现
  - 在 FoT 中重新实现后，逐步加入以下功能进行消融分析：
    - 是否并行（Sequential vs Parallel）
    - 是否启用缓存（No Cache vs Process Cache vs Persistent Cache）

- **优化实验**：
  - 使用 FoT 内置工具对以下进行优化：
    - ToT（Go24 和 Sorting）
    - GoT（Sorting 和 DM 中的 Improve prompt）
  - 优化目标：任务得分（accuracy/F1/mistakes），同时限制成本不高于原始版本。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 3）**

#### **平均运行时间加速效果**

| 方案 | 最大加速倍数（vs 基线） |
|------|------------------------|
| **ToT (Go24)** | **35.4x**（并行 + 持久缓存） |
| **GoT (Sorting)** | **11.6x** |
| **GoT (DM)** | **4.7x** |
| **ProbTree (MuSiQue)** | **2.4x** |

> 🔥 **总体平均加速达 10.7x**，即**一个数量级的速度提升**。

#### **成本节省**

| 方案 | 成本降低幅度 |
|------|--------------|
| **ToT (Go24)** | ↓46%（从 29.6 → 16.1 美分） |
| **GoT (DM)** | ↓14%（6.9 → 5.9 美分） |
| **ProbTree (MuSiQue)** | ↓12%（0.8 → 0.7 美分） |

> 💡 缓存对重复性强的任务（如 Go24、DM）尤其有效。

---

### **与基线方法的对比结果**

| 指标 | 结果 |
|------|------|
| **任务得分提升** | 所有优化后的方案均取得更高分数 |
| **ToT (Go24)** | Accuracy 从 63.0% → **66.0%** |
| **ToT (Sorting)** | Mistakes 从 18.4 → **18.2** |
| **GoT (Sorting)** | Mistakes 从 12.7 → **12.1** |
| **GoT (DM)** | F1 从 8.4 → **8.8** |

> ✅ 在**不增加预算的前提下**，通过优化实现了性能提升。

---

### **消融实验结果**

#### **优化过程本身的效率提升（Table 4）**

| 设置 | 优化总耗时（vs 基线） | 优化总成本（vs 基线） |
|------|------------------|------------------|
| Sequential + No Cache | 1.0x | 100% |
| Parallel + Persistent Cache | **↓50.2x 更快** | **↓至 9–36% 成本** |

> ⚠️ 若无缓存，并行优化几乎不可行（成本过高）。**缓存是使优化“可行”的关键**。

#### **优化前后每实例开销（Table 5）**

| 方案 | 优化后成本占比 |
|------|----------------|
| ToT (Go24) | ↓至 76%（持久缓存） |
| GoT (DM) | ↓至 87% |

> 即便任务得分提升，实际推理成本反而下降。

---

## **4. 关键结论和发现**

### **主要发现**

1. **动态图结构是未来方向**  
   FoT 支持运行时图演化，为应对未知或高度异构问题提供了可能性，推动 prompting schemes 从“手工设计”走向“自适应推理”。

2. **效率优化至关重要**  
   并行 + 缓存组合带来**高达 35x 的加速**，使得复杂推理在现实场景中更具可行性。

3. **Prompt 与超参数优化能显著提升性能**  
   以往被忽视的 prompt 设计和超参选择，在 FoT 框架下可通过自动化工具优化，释放模型潜力。

4. **缓存不仅省成本，更是优化的前提**  
   没有缓存，超参搜索和 prompt 进化会因高昂代价而难以实施；**缓存让优化变得经济可行**。

---

### **方法的局限性**

1. **当前验证仍以半自动/手动方案为主**  
   实验中实现的是 ToT（半自动）、GoT（手动）、ProbTree（半自动），尚未充分展示完全自动方案（如 AGoT）在 FoT 上的优势。

2. **图修改规则较保守**  
   当前并行执行下的图修改约束较强（如不能改祖先节点），可能限制某些灵活结构的表达能力。

3. **依赖外部组件质量**  
   如 retrieval（BM25）、code interpreter 等若不稳定，会影响整体可靠性。

---

### **未来工作方向**

1. **开发更多全自动 prompting schemes**  
   利用 FoT 的动态能力，构建真正能“自我组织”推理路径的方法。

2. **增强优化能力**  
   - 支持多 prompt 联合优化
   - 引入更先进的 prompt evolution 技术（如 GEPA）
   - 实现超参与 prompt 的端到端联合优化

3. **改进图抽象与接口**  
   提供更高层次的 DSL 或可视化工具，降低使用门槛。

4. **放宽并行图修改规则**  
   探索更灵活的安全并发控制机制，提升表达力。

---

> 📌 **总结一句话**：  
> **FoT 是一个“推理操作系统”级别的框架，它通过动态图、并行执行、缓存和内置优化四大支柱，将 prompting schemes 从“静态脚本”升级为“智能、高效、可进化”的推理引擎。**

</details>

---

### 15. [From Transcripts to AI Agents: Knowledge Extraction, RAG Integration, and Robust Evaluation of Conversational AI Assistants](https://arxiv.org/abs/2602.15859)

**Authors**: Krittin Pachtrachai, Petmongkon Pornpichitsuwan, Wachiravit Modecrua, Touchapon Kraisingkorn  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15859v1  

#### Abstract
Building reliable conversational AI assistants for customer-facing industries remains challenging due to noisy conversational data, fragmented knowledge, and the requirement for accurate human hand-off - particularly in domains that depend heavily on real-time information. This paper presents an end...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：From Transcripts to AI Agents: Knowledge Extraction, RAG Integration, and Robust Evaluation of Conversational AI Assistants

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对**面向客户行业的对话式 AI 助手部署难题**，特别是以下挑战：
- **噪声大的真实通话转录文本（noisy conversational data）**
- **知识碎片化、不完整**
- **对实时信息高度依赖导致自动化困难**
- **需要可靠的人工接管机制（human hand-off）**

在房地产和专业招聘等复杂领域中，传统方法因依赖手动构建知识库和固定业务逻辑而难以扩展。

---

### 🚀 提出的新方法与创新思路

提出了一套**端到端的“从转录文本到 AI Agent”的框架**，主要包括以下几个核心模块：

#### （1）基于 PIPA 框架简化的 **Transcript Grading & Filtering**
- 引入两个可扩展的评分标准：
  - **Observation Alignment**：判断助手回复是否基于对话中的可观测证据。
  - **Appropriate Response (P)**：评估回复是否礼貌、专业、清晰且自洽。
- 使用 **LLM-as-a-judge** 自动打分，实现大规模高质量对话筛选。

#### （2）**Knowledge Extraction + RAG Pipeline**
- 从筛选后的高质历史对话中提取结构化知识。
- 将知识作为唯一的外部 grounding 来源，通过 **Retrieval-Augmented Generation (RAG)** 驱动生成，避免将大量知识嵌入 prompt 导致的 “lost-in-the-middle” 问题。

#### （3）**系统性的 Prompt Tuning 架构演进**
提出了从单体到模块化再到受控执行的 prompt 设计路径：
1. Early Escalation  
2. Low-autonomy RAG  
3. Modular Orchestration  
4. Programmatic Orchestration  
5. YAML-based Control  
6. Protocol-based Orchestration  
7. **Governed Execution（最终方案）**

强调 **prompt 的结构、长度控制和内部一致性** 是决定 agent 行为的关键因素。

#### （4）**基于真实转录的用户模拟器 + Red Teaming 评估体系**
- 使用 **transcript-grounded user simulator** 进行定量评估，保留真实交互模式。
- 开展全面的 **red teaming 测试**，涵盖：
  - Out-of-Context 攻击
  - Out-of-Scope 请求
  - Prompt Injection
  - Instruction Contradiction
  - Style Manipulation

---

### 🔍 相比现有方法的优势

| 维度 | 本工作优势 |
|------|-----------|
| **知识来源** | 完全来自真实历史对话，无需人工标注或外部数据库 |
| **可扩展性** | 模块化 prompt 设计支持跨行业复用（>99% 共享） |
| **鲁棒性** | 在 adversarial 攻击下表现优异 |
| **安全性** | 完美的 rejection accuracy 和 human escalation 能力 |
| **评估真实性** | 基于真实意图的 user simulator，避免 synthetic test bias |

相较于 PolyAI、Kore.ai 等商业平台，本方法更轻量、适应性强；相比研究型系统（如 Google Duplex），更具通用性和自动化潜力。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用真实世界的 **历史电话通话转录数据（historical call transcripts）**
- 实验领域：
  - **Real Estate（房地产）**
  - **Specialist Recruitment（专业招聘）**
- 这两个领域被选为**极具挑战性的测试场景**，因其高度依赖实时数据、个性化谈判和动态政策。

---

### ⚙️ 实验设置

#### 数据处理流程：
1. **Grading**：使用 LLM 对每条 assistant utterance 打分（Aobs 和 Ap）
2. **Filtering**：仅保留归一化得分 ≥ 0.8 的高质量对话
3. **Topic Annotation**：用 LLM 标注每个对话的知识主题
4. **Maximum Coverage Selection**：贪心算法选择能覆盖最多知识主题的子集
5. **Knowledge Extraction**：从精选对话中抽提结构化知识存入 RAG database
6. **Prompt Engineering**：采用不同层级的 prompt orchestration 策略

#### Agent 推理流程：
- 用户输入 → RAG 检索相关知识片段 → 注入 context → LLM 生成响应
- 若请求超出范围 → 触发 human agent transfer

---

### 📊 评估指标

| 指标 | 定义 |
|------|------|
| **Coverage** | AI 助手能独立完成的通话比例 |
| **Factual Accuracy** | 回答内容与知识库一致的比例 |
| **Rejection Accuracy** | 正确识别并转接给人工的比例（越接近 100% 越好） |
| **Red-Teaming Robustness** | 抵抗各类攻击的成功率：<br>• Out-of-Context<br>• Out-of-Scope<br>• Prompt Injection<br>• Instruction Contradiction |

#### 基线方法对比：
- **Early Escalation**（知识内嵌 prompt）
- **Low-autonomy RAG**
- 多种中间态 prompt 架构（Modular / Programmatic / YAML-based 等）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table I）

| 评估指标 | Real Estate | Specialist Recruitment |
|--------|-------------|-------------------------|
| **Coverage** | 35% | 25% |
| **Factual Accuracy** | 97% | 94% |
| **Rejection Accuracy** | 100% | 100% |
| **Out-of-Context 攻击防御** | 98.5% | 100% |
| **Out-of-Scope 攻击防御** | 98.5% | 100% |
| **Prompt Injection 防御** | 100% | 100% |
| **Instruction Contradiction 防御** | 100% | 100% |

> ✅ 所有 red teaming 攻击防御成功率均超过 98.5%，多数达 100%

---

### 🔁 与基线方法对比（图 2 分析）

| Prompt 类型 | Coverage | Factual Acc | Rejection Acc | 特点 |
|------------|----------|--------------|----------------|-------|
| **Early Escalation** | 52% (RE), 3% (SR) | ~62% | 100% | 覆盖不稳定，跨域泛化差 |
| **Low-autonomy RAG** | ~25% | >91% | ↓ (73% in SR) | 准确性提升但过早转接 |
| **Modular Orchestration** | ~30% | 高 | ↑ | 结构化推理改善决策 |
| **Programmatic Orchestration** | ~20% | 高 | >89% | 更强逻辑控制但灵活性下降 |
| **YAML-based / Protocol-based** | ~30–35% | 96–98% | 100% | 平衡性更好 |
| ✅ **Governed Execution** | **35% / 25%** | **97% / 94%** | **100%** | 最优综合表现 |

> 💡 发现：**prompt 的结构设计比 domain-specific 调整更重要**

---

### 🔍 消融实验（Ablation Study）隐含结论
虽然未明确列出消融表，但从 prompt 演进路径可得出：
- 移除冗余指令、缩短 prompt 显著提升 performance
- 模块化 + 显式控制流（如 pseudo-code / YAML）增强 determinism
- 加入 execution guardrails 后，agent 更能区分“我能做什么” vs “我不能做什么”
- **知识外置 + RAG + 精简 prompt = 最佳组合**

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **Prompt 设计是决定 conversational agent 性能的核心维度**
   - 不是“加更多知识”，而是“如何组织行为逻辑”
   - **lean, modular, governed prompt design** 显著优于 monolithic、knowledge-heavy 方案

2. **完全基于历史对话构建 AI Agent 可行**
   - 即使在 Real Estate 和 Specialist Recruitment 这类难自动化的领域，也能安全处理 **25–35% 的调用**

3. **高 factual accuracy 与 perfect rejection accuracy 可同时达成**
   - 表明系统既能准确回答，又能可靠地拒绝不确定请求，保障服务安全

4. **RAG + structured prompting 提供强大防御能力**
   - 成功抵御各类 LLM 攻击向量（prompt injection, contradiction 等）

5. **AI 助手应定位为 human-support system，而非 fully replacement**
   - 有效减轻人力负担，同时保持服务质量与用户信任

---

### ⚠️ 局限性

| 限制 | 说明 |
|------|------|
| **Coverage 有限** | 当前仅覆盖约 1/3 的调用，受限于静态知识库无法获取实时数据（如候选人当前状态、房源即时价格） |
| **依赖 ASR/Diarization 质量** | 转录质量直接影响 grading 与 knowledge extraction 效果 |
| **领域迁移仍需一定调参** | 尽管 prompt 高度共享，topic modeling 和 retrieval 效果仍受领域影响 |
| **未测试多轮复杂任务** | 当前 focus 在信息查询类任务，尚未验证复杂 goal-oriented 对话能力 |

---

### 🔮 未来工作方向（Future Work）

1. **集成 real-time data sources**
   - 如连接 CRM、calendar、job board API，以支持动态信息查询

2. **改进 knowledge consolidation**
   - 解决 multiple transcripts 中可能存在的知识冲突或不一致

3. **扩展 prompt tuning 至 proactive agent roles**
   - 支持主动触达（sales outreach）、反馈收集等 active agent 场景

4. **引入 multi-agent coordination**
   - 构建 specialist agents 分工协作，应对更复杂任务链

5. **加强 explainability 与 audit trail**
   - 提供 response grounding traceability，增强企业合规性与用户信任

---

## ✅ 总结一句话

> 本文提出一个**以高质量历史对话为基础、结合 RAG 与模块化受控 prompt 设计的端到端框架**，实现了在复杂现实场景中部署**高准确率、高安全性、可解释性强的 conversational AI assistant**，为 human-AI collaboration 提供了实用且可扩展的技术路径。

</details>

---

### 16. [TabAgent: A Framework for Replacing Agentic Generative Components with Tabular-Textual Classifiers](https://arxiv.org/abs/2602.16429)

**Authors**: Ido Levy, Eilam Shapira, Yinon Goldshtein, Avi Yaeli, Nir Mashkif, Segev Shlomov  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16429v1  

#### Abstract
Agentic systems, AI architectures that autonomously execute multi-step workflows to achieve complex goals, are often built using repeated large language model (LLM) calls for closed-set decision tasks such as routing, shortlisting, gating, and verification. While convenient, this design makes deploy...

---

### 17. [A Koopman-Bayesian Framework for High-Fidelity, Perceptually Optimized Haptic Surgical Simulation](https://arxiv.org/abs/2602.15834)

**Authors**: Rohit Kaushik, Eva Kaushik  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15834v1  

#### Abstract
We introduce a unified framework that combines nonlinear dynamics, perceptual psychophysics and high frequency haptic rendering to enhance realism in surgical simulation. The interaction of the surgical device with soft tissue is elevated to an augmented state space with a Koopman operator formulati...

---

### 18. [Muon with Spectral Guidance: Efficient Optimization for Scientific Machine Learning](https://arxiv.org/abs/2602.16167)

**Authors**: Binghang Lu, Jiahao Zhang, Guang Lin  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16167v1  

#### Abstract
Physics-informed neural networks and neural operators often suffer from severe optimization difficulties caused by ill-conditioned gradients, multi-scale spectral behavior, and stiffness induced by physical constraints. Recently, the Muon optimizer has shown promise by performing orthogonalized upda...

---

### 19. [Towards Efficient Constraint Handling in Neural Solvers for Routing Problems](https://arxiv.org/abs/2602.16012)

**Authors**: Jieyi Bi, Zhiguang Cao, Jianan Zhou, Wen Song, Yaoxin Wu, Jie Zhang, Yining Ma, Cathy Wu  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16012v1  

#### Abstract
Neural solvers have achieved impressive progress in addressing simple routing problems, particularly excelling in computational efficiency. However, their advantages under complex constraints remain nascent, for which current constraint-handling schemes via feasibility masking or implicit feasibilit...

---

### 20. [CLAA: Cross-Layer Attention Aggregation for Accelerating LLM Prefill](https://arxiv.org/abs/2602.16054)

**Authors**: Bradley McDanel, Steven Li, Harshit Khaitan  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16054v1  

#### Abstract
The prefill stage in long-context LLM inference remains a computational bottleneck. Recent token-ranking heuristics accelerate inference by selectively processing a subset of semantically relevant tokens. However, existing methods suffer from unstable token importance estimation, often varying betwe...

---

### 21. [Reinforced Fast Weights with Next-Sequence Prediction](https://arxiv.org/abs/2602.16704)

**Authors**: Hee Seung Hwang, Xindi Wu, Sanghyuk Chun, Olga Russakovsky  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16704v1  

#### Abstract
Fast weight architectures offer a promising alternative to attention-based transformers for long-context modeling by maintaining constant memory overhead regardless of context length. However, their potential is limited by the next-token prediction (NTP) training paradigm. NTP optimizes single-token...

---

### 22. [Extracting and Analyzing Rail Crossing Behavior Signatures from Videos using Tensor Methods](https://arxiv.org/abs/2602.16057)

**Authors**: Dawon Ahn, Het Patel, Aemal Khattak, Jia Chen, Evangelos E. Papalexakis  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16057v1  

#### Abstract
Railway crossings present complex safety challenges where driver behavior varies by location, time, and conditions. Traditional approaches analyze crossings individually, limiting the ability to identify shared behavioral patterns across locations. We propose a multi-view tensor decomposition framew...

---

### 23. [Towards Secure and Scalable Energy Theft Detection: A Federated Learning Approach for Resource-Constrained Smart Meters](https://arxiv.org/abs/2602.16181)

**Authors**: Diego Labate, Dipanwita Thakur, Giancarlo Fortino  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16181v1  

#### Abstract
Energy theft poses a significant threat to the stability and efficiency of smart grids, leading to substantial economic losses and operational challenges. Traditional centralized machine learning approaches for theft detection require aggregating user data, raising serious concerns about privacy and...

---

### 24. [Agent Skill Framework: Perspectives on the Potential of Small Language Models in Industrial Environments](https://arxiv.org/abs/2602.16653)

**Authors**: Yangjie Xu, Lujun Li, Lama Sleem, Niccolo Gentile, Yewei Song, Yiqun Wang, Siming Ji, Wenbo Wu, Radu State  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.16653v1  

#### Abstract
Agent Skill framework, now widely and officially supported by major players such as GitHub Copilot, LangChain, and OpenAI, performs especially well with proprietary models by improving context engineering, reducing hallucinations, and boosting task accuracy. Based on these observations, an investiga...

---

### 25. [Towards Fair and Efficient De-identification: Quantifying the Efficiency and Generalizability of De-identification Approaches](https://arxiv.org/abs/2602.15869)

**Authors**: Noopur Zambare, Kiana Aghakasiri, Carissa Lin, Carrie Ye, J. Ross Mitchell, Mohamed Abdalla  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15869v1  

#### Abstract
Large language models (LLMs) have shown strong performance on clinical de-identification, the task of identifying sensitive identifiers to protect privacy. However, previous work has not examined their generalizability between formats, cultures, and genders. In this work, we systematically evaluate ...

---

### 26. [VDLM: Variable Diffusion LMs via Robust Latent-to-Text Rendering](https://arxiv.org/abs/2602.15870)

**Authors**: Shuhui Qu  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15870v1  

#### Abstract
Autoregressive language models decode left-to-right with irreversible commitments, limiting revision during multi-step reasoning. We propose \textbf{VDLM}, a modular variable diffusion language model that separates semantic planning from text rendering. VDLM applies LLaDA-style masked diffusion over...

---

### 27. [P-RAG: Prompt-Enhanced Parametric RAG with LoRA and Selective CoT for Biomedical and Multi-Hop QA](https://arxiv.org/abs/2602.15874)

**Authors**: Xingda Lyu, Gongfu Lyu, Zitai Yan, Yuxin Jiang  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15874v1  

#### Abstract
Large Language Models (LLMs) demonstrate remarkable capabilities but remain limited by their reliance on static training data. Retrieval-Augmented Generation (RAG) addresses this constraint by retrieving external knowledge during inference, though it still depends heavily on knowledge base quality. ...

---

### 28. [Quality-constrained Entropy Maximization Policy Optimization for LLM Diversity](https://arxiv.org/abs/2602.15894)

**Authors**: Haihui Pan, Yuzhong Hong, Shaoke Lv, Junwei Bao, Hongfei Jiang, Yang Song  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15894v1  

#### Abstract
Recent research indicates that while alignment methods significantly improve the quality of large language model(LLM) outputs, they simultaneously reduce the diversity of the models' output. Although some methods have been proposed to enhance LLM output diversity, they often come at the cost of redu...

---

### 29. [Missing-by-Design: Certifiable Modality Deletion for Revocable Multimodal Sentiment Analysis](https://arxiv.org/abs/2602.16144)

**Authors**: Rong Fu, Wenxin Zhang, Ziming Wang, Chunlei Meng, Jiaxuan Lu, Jiekai Wu, Kangan Qian, Hao Zhang, Simon Fong  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.16144v1  

#### Abstract
As multimodal systems increasingly process sensitive personal data, the ability to selectively revoke specific data modalities has become a critical requirement for privacy compliance and user autonomy. We present Missing-by-Design (MBD), a unified framework for revocable multimodal sentiment analys...

---

### 30. [AREG: Adversarial Resource Extraction Game for Evaluating Persuasion and Resistance in Large Language Models](https://arxiv.org/abs/2602.16639)

**Authors**: Adib Sakhawat, Fardeen Sadab  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.16639v1  

#### Abstract
Evaluating the social intelligence of Large Language Models (LLMs) increasingly requires moving beyond static text generation toward dynamic, adversarial interaction. We introduce the Adversarial Resource Extraction Game (AREG), a benchmark that operationalizes persuasion and resistance as a multi-t...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
