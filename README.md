# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-17 05:55:56 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Fast and Accurate Causal Parallel Decoding using Jacobi Forcing](https://arxiv.org/abs/2512.14681)

**Authors**: Lanxiang Hu, Siqi Kou, Yichao Fu, Samyam Rajbhandari, Tajana Rosing, Yuxiong He, Zhijie Deng, Hao Zhang  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.14681v1  

#### Abstract
Multi-token generation has emerged as a promising paradigm for accelerating transformer-based large model inference. Recent efforts primarily explore diffusion Large Language Models (dLLMs) for parallel decoding to reduce inference latency. To achieve AR-level generation quality, many techniques ada...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Fast and Accurate Causal Parallel Decoding using Jacobi Forcing 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代大型语言模型（LLMs）依赖 **autoregressive (AR)** 解码，逐个生成 token，导致推理延迟高、并行度低。虽然 **diffusion-based LLMs (dLLMs)** 被提出用于并行解码以加速推理，但它们存在以下问题：

- **Pretrain-to-posttrain mismatch**：dLLMs 在后训练中使用掩码数据分布，与预训练时的真实数据分布不一致。
- **破坏因果先验**：dLLMs 使用双向注意力机制，违背了 AR 模型在预训练中学到的因果性（causal prior），阻碍了精确的 KV cache 复用。
- **质量下降严重**：当 block size 增大时，适应后的 dLLMs 性能显著下降（如 SDAR 在 block size=128 时质量大幅降低）。

因此，现有方法在追求速度的同时难以保持 AR 级别的生成质量。

---

### **提出了什么新方法或新思路**

本文提出 **Jacobi Forcing**，一种渐进式蒸馏范式（progressive distillation paradigm），将 AR 模型逐步转变为高效的并行解码器，同时保留其因果推理能力。

#### 核心思想：
- 不修改 AR 模型的因果注意力结构。
- 利用 **Jacobi Decoding** 生成模型自身的并行解码轨迹（trajectory），并在这些轨迹上进行一致性蒸馏（consistency distillation）。
- 引入 **progressive noise schedule** 和 **noise-aware causal attention**，使模型学会在噪声上下文中预测未来的正确 token。

#### 具体创新点：

| 创新模块 | 描述 |
|--------|------|
| **Jacobi Forcing** | 在 AR 模型自身生成的 Jacobi 轨迹上进行渐进式一致性训练，避免引入外部掩码数据分布偏差。 |
| **Progressive Noise Schedule** | 将大 block 分成小窗口，按周期性递增噪声比例（从 0 到 1），确保每个 block 都有部分“干净上下文”，降低学习难度。 |
| **Noise-aware Causal Attention** | 设计特殊的 attention mask，允许在一个 forward pass 中同时计算 AR loss 和 consistency loss，提升训练效率。 |
| **Multi-block Decoding + Rejection Recycling** | 推理阶段优化策略：<br>- **Multi-block**：同时维护多个 block，提前解码后续 block 的 token；<br>- **Rejection Recycling**：复用历史迭代中高质量的 n-gram 作为候选，提高单次迭代接受的 token 数量。 |

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **训练稳定性** | 避免 pretrain-posttrain 分布偏移，训练更稳定，可扩展至更大 block size。 |
| **保持生成质量** | 保留 AR 因果结构，生成质量接近原始 AR 模型（仅轻微下降）。 |
| **更高的加速潜力** | 支持更大的 block size 和更多并行 token 预测，充分利用现代 AI 加速器的算力。 |
| **端到端速度更快** | 实现高达 4.0× 的 wall-clock speedup，远超 dLLMs 和传统 AR 并行方法。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 任务类型 | 数据集 | 模型 |
|---------|-------|------|
| **代码生成** | OpenCodeInstruct（训练）、HumanEval、MBPP（测试） | Qwen2.5-Coder-7B-Instruct |
| **数学推理** | Openthought2（math split，训练）、GSM8K、MATH（测试） | Qwen2.5-Math-7B-Instruct |

所有模型均基于 Qwen2.5 系列指令微调版本。

---

### **实验设置和评估指标**

#### **硬件环境**
- 训练与推理：8x NVIDIA A100-80GB 或 H200/B200 GPU。

#### **训练配置**
- 学习率：1e-6
- Batch size：4
- Max sequence length：2048
- Block size：初始 16 → 第二轮训练升至 32
- Window size：16（第一轮），8（第二轮）
- 训练步数：每轮 10k steps

#### **评估指标**
| 指标 | 含义 |
|------|------|
| **TPS (Tokens Per Second)** | 每秒生成 token 数量，衡量吞吐量 |
| **Speedup** | 相对于 AR baseline 的 TPS 加速比 |
| **Accuracy / Solve Rate** | HumanEval 上的 pass@1 准确率，GSM8K/MATH 上的问题解决率 |
| **TPF (Tokens Forwarded per Iteration)** | 每次迭代平均向前推进的 token 数量，反映并行效率 |

---

### **基线方法对比**

| 类别 | 基线方法 |
|------|--------|
| **AR-based 并行解码** | Vanilla Jacobi、CLLM*（改进版一致性蒸馏） |
| **Diffusion-based LLMs** | LLaDA-7B、Dream-7B、Fast-dLLM、D2F |
| **Speculative Decoding** | EAGLE-3、HASS（补充对比） |

> 注：CLLM* 是作者实现的增强版，加入 sequence packing 技术但未采用渐进训练。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（A100 GPU）**

#### ✅ **代码生成（HumanEval）**
| 方法 | TPS | Speedup | Accuracy |
|------|-----|---------|----------|
| AR (baseline) | 41.3 | 1.00× | 87.8% |
| CLLM* | 103.3 | 2.50× | 87.8% |
| **Jacobi Forcing Model** | **159.5** | **3.86×** | **83.5%** |
| **Jacobi Forcing Model (MR)** | **163.9** | **3.97×** | **83.5%** |

> MR = Multi-block + Rejection Recycling

#### ✅ **数学推理（GSM8K）**
| 方法 | TPS | Speedup | Solve Rate |
|------|-----|---------|------------|
| AR | 41.8 | 1.00× | 92.4% |
| **Jacobi Forcing Model (MR)** | **154.9** | **3.71×** | **91.4%** |

#### ✅ **数学推理（MATH）**
| 方法 | TPS | Speedup | Solve Rate |
|------|-----|---------|------------|
| AR | 41.3 | 1.00× | 77.0% |
| **Jacobi Forcing Model (MR)** | **152.0** | **3.68×** | **77.4%** |

> 在 MATH 上甚至略微提升了准确率！

---

### **与基线方法的对比结果**

| 对比维度 | 结果 |
|--------|------|
| **vs dLLMs** | Jacobi Forcing 比 Dream-Base 快 **7.4×~15×**，比 Fast-dLLM/D2F 快 **2.0×~2.2×**，且准确率高出 20–40 个百分点。 |
| **vs AR-based parallel** | 比 CLLM 快 **1.6×**，TPF 提升近 **2×**。 |
| **vs speculative decoding**（B200）| 比 EAGLE-3 快 **1.3×**，比 HASS 快 **1.2×**，且为 lossless（不改变输出分布）。 |

> 表明 Jacobi Forcing 在 **速度-质量权衡** 上处于最优前沿。

---

### **消融实验结果**

#### 🔍 **不同 noise schedule 效果（HumanEval, block=256）**
| Schedule | Accuracy | iter/token |
|---------|----------|-----------|
| Random | 82.9% | 0.53 |
| **Linear Progressive** | **84.7%** | **0.48** |
| Reverse Progressive | 82.9% | 0.62 |

✅ **线性递增噪声调度效果最好**，收敛更快、质量更高。

#### 🔍 **Attention Mask 类型影响**
| Mask 类型 | Speedup | Accuracy |
|----------|--------|----------|
| Noise-conditioned (NC) | **3.6×** | 82.3% |
| NC + Intra-window Clean Context (NC-IC) | 1.9× | 82.3% |

✅ **noise-aware causal mask 更有效**，说明利用噪声上下文进行预测是关键。

#### 🔍 **FLOPs 利用分析**
- 在 H200/B200 上，最多可并行处理 **256 tokens** 而不增加延迟。
- 最优配置：**block size=64**, **verification size=4** → 64×4=256 tokens，完美匹配硬件峰值利用率。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **Jacobi Forcing 成功实现了 AR 模型向高效并行解码器的平滑过渡**，无需牺牲因果结构即可获得接近 4× 的 wall-clock speedup。
2. ✅ **渐进式噪声调度 + noise-aware attention** 显著提升了模型在噪声上下文中预测未来 token 的能力。
3. ✅ **Multi-block decoding 与 rejection recycling 协同作用**，使得每次迭代可接受的 token 数量提升达 **4.5×**。
4. ✅ 在 **代码与数学任务上均表现出色**，尤其在 MATH 上还略微提升了 solve rate，说明该方法不仅快，而且“聪明”。

---

### **方法的局限性**

| 局限性 | 说明 |
|--------|------|
| **仍需额外训练** | 不是 training-free 方法，需要收集轨迹并进行蒸馏训练。 |
| **对 block size 敏感** | 性能依赖于合理的 block size 和 verification size 配置，需针对硬件调优。 |
| **内存开销略增** | 多 block 维护和 n-gram 缓存带来一定显存负担。 |
| **目前仅支持 greedy decoding** | 尚未验证在采样（sampling）场景下的表现。 |

---

### **未来工作方向**

1. 🔄 **扩展至 sampling 场景**：研究如何在非贪婪采样下保持 high token acceptance。
2. ⚙️ **自动化配置搜索**：开发自适应算法动态调整 block size、verification depth 等参数。
3. 🧠 **结合 speculative decoding**：探索与 Medusa/EAGLE 等方法融合的可能性。
4. 🌐 **部署优化**：进一步压缩 KV cache、支持 streaming 输出等工业级优化。

---

> 🔗 **开源地址**：https://github.com/hao-ai-lab/JacobiForcing  
> 📄 **论文链接**：https://arxiv.org/abs/2512.14681

</details>

---

### 2. [A Unified Sparse Attention via Multi-Granularity Compression](https://arxiv.org/abs/2512.14082)

**Authors**: Siran Liu, Zane Cao, Yongchao He  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.14082v1  

#### Abstract
Efficient long-context understanding and reasoning are increasingly vital for large language model (LLM) applications such as multi-turn dialogue and program analysis. However, the core self-attention mechanism scales quadratically with sequence length, creating a fundamental computational bottlenec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Unified Sparse Attention via Multi-Granularity Compression

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLM）中的 **self-attention** 机制计算复杂度为 $O(L^2)$，其中 $L$ 是序列长度。这在处理长上下文任务（如多轮对话、代码分析、长文档理解）时成为严重的计算瓶颈。尽管已有多种稀疏注意力（sparse attention）方法试图缓解该问题，但仍面临以下挑战：

- **训练耦合方法**（如 DeepSeek 的 Native Sparse Attention）：需在预训练阶段引入稀疏结构，缺乏通用性，无法作为即插即用的加速模块用于已训练好的模型。
- **推理时静态方法**（如 BigBird、StreamLLM）：采用固定模式（滑动窗口、sink token等），缺乏对输入内容的自适应能力，难以泛化到视频、音频等跨模态任务。
- **推理时动态方法**（如 FlexPrefill、XAttention）：依赖简单启发式规则（如最后一块查询块作为探针），在复杂任务中表现不稳定。

因此，如何设计一种**高效、通用、无需重训练且适用于多模态任务的稀疏注意力机制**，是当前研究的关键挑战。

---

### **提出了什么新方法或新思路**

本文提出 **UniSparse**，一种统一的、硬件友好的动态稀疏注意力机制，其核心思想基于 **复合令牌（composite tokens）** 和 **多粒度压缩（multi-granularity compression）**。

#### 核心创新点：

1. ✅ **复合令牌（Composite Tokens）**  
   将原始细粒度 token 通过空间池化（如平均池化）聚合为粗粒度的“复合令牌”，作为上下文信息的紧凑表示。这些复合令牌保留了局部语义结构，可用于轻量级代理计算。

2. ✅ **多粒度压缩机制**  
   在序列维度（sequence-level）和可选的头维度（head-level）进行压缩：
   - 序列压缩因子 $c_q, c_k$：减少 token 数量。
   - 头压缩因子 $c_h$：将多个注意力头分组并取平均，降低选择开销。

3. ✅ **两阶段动态块选择算法**
   - **Stage 1：压缩空间中的注意力计算**  
     在压缩后的低维空间中计算注意力分数矩阵（大小仅为原矩阵的 $1/(c_q c_k)$）。
   - **Stage 2：块级重要性聚合 + Top-P 选择**  
     将压缩空间的注意力分数反向映射回原始块级别，按累计注意力质量选择最重要的 Key 块（Top-P 机制），生成稀疏掩码 $M$。

4. ✅ **硬件友好实现**
   - 使用 fused kernel 设计，中间结果保留在 on-chip memory 中，减少内存访问。
   - 支持与 FlashAttention 兼容的 block-sparse kernels，便于集成。

---

### **相比现有方法的优势**

| 维度 | UniSparse | 现有方法 |
|------|----------|---------|
| **是否需要重训练** | ❌ 否（inference-time） | ✅ 多数训练耦合方法需要 |
| **是否即插即用** | ✅ 是 | ❌ 多数不支持 |
| **是否跨模态通用** | ✅ 是（文本、视频、音频均适用） | ❌ 多为文本专用 |
| **选择策略** | ✅ 全局评估（global evaluation） | ⚠️ 局部采样（如 FlexPrefill）或简化启发式（如 XAttention） |
| **效率 vs 准确率平衡** | ✅ 高效且高保真 | ⚠️ 要么慢（MInference），要么不准（XAttention） |

> **一句话总结优势**：UniSparse 实现了无需训练、跨模态通用、高准确率、高效率的稀疏注意力，在精度与速度之间达到了最优权衡。

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验覆盖三大类长上下文基准，涵盖合成任务、真实世界应用和多模态场景：

| 数据集 | 类型 | 描述 |
|-------|------|------|
| **RULER** (Hsieh et al., 2024) | 合成基准 | 控制任务复杂度，测试检索、聚合、多跳推理能力，最长达 128K tokens |
| **HELMET** (Yen et al., 2025) | 真实世界基准 | 包含 7 类应用场景（长文档 QA、摘要、RAG 等），强调深层语义理解 |
| **Video-MME** (Fu et al., 2024) | 多模态视频理解 | 涵盖 6 个视觉领域、30 个子任务，支持帧、字幕、音频输入，测试时空与跨模态推理 |

---

### **实验设置和评估指标**

#### 模型
- **Meta-Llama-3.1-8B-Instruct**（纯文本）
- **Qwen2.5-7B-Instruct**（多语言文本）
- **Qwen2.5-VL-7B-Instruct**（视觉-语言模型）

#### 上下文长度
从 4K 到 128K tokens 不等，部分任务达到 30 分钟以上视频。

#### 评估指标
- **准确性**：各任务的得分（如 RULER/HELMET 的 overall score，Video-MME 的 accuracy）
- **稀疏度（Sparsity）**：被跳过的 attention block 比例
- **速度提升**：相对于 FlashAttention 的 end-to-end attention 计算时间加速比
- **选择开销（Selection Overhead）**：确定稀疏掩码 $M$ 所需的时间

#### 主要配置
- 默认压缩因子：$c_q = c_k = 8$, $c_h = 1$
- Top-P 阈值：$P = 0.9$ 或 $0.95$
- 所有方法仅在 **prefill 阶段** 应用稀疏，decode 阶段保持 dense

---

### **基线方法对比**

| 方法 | 类型 | 特点 |
|------|------|------|
| **FlashAttention** | Dense Baseline | 完整注意力，内存优化实现，精度上限 |
| **MInference** (Jiang et al., 2024) | 动态 + 离线搜索 | 高保真但需离线 pattern search，非即插即用 |
| **FlexPrefill** (Lai et al., 2025) | 动态 + 局部探针 | 使用最后一个 query block 探测重要区域，速度快但易漏 |
| **XAttention** (Xu et al., 2025) | 动态 + 全局采样 | 使用 anti-diagonal scoring + stride sampling，全局但成本高 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **精度保持**
- 在所有任务上，UniSparse **保留 ≥99% 的 Full Attention 精度**。
- 在某些情况下甚至**超过 Full Attention**（如 Video-MME with subtitles 下 UniSparse 达 69.9%，而 FlashAttention 为 69.6%），表明其能有效保留关键跨模态信息。

#### ✅ **速度提升**
- 在 128K 序列长度下：
  - **端到端 attention 计算速度提升高达 2.61×**（vs. FlashAttention）
  - **选择阶段提速 2.64×**（vs. XAttention-0.95）
- 图 4 显示，随着序列增长，加速效果持续增强。

#### ✅ **稀疏效率**
- 平均仅需计算 **一半以下的 attention blocks** 即可达到接近全注意力的效果。
- 在相同稀疏水平下，UniSparse 性能显著优于其他方法。

---

### **与基线方法的对比结果**

| 基准 | 最佳基线 | UniSparse 表现 |
|------|--------|----------------|
| **RULER** | FlexPrefill-0.99 (92.59) | UniSparse-0.95 达 **92.56**，稀疏度更高（46.33% vs 36.51%） |
| **HELMET** | MInference (55.37) | UniSparse-0.95 达 **56.21**，全面领先 |
| **Video-MME (no subtitle)** | FlexPrefill-0.95 (64.4) | UniSparse-0.9 达 **64.6**，且稀疏度更低 |
| **Video-MME (with subtitle)** | FlashAttention (69.6) | UniSparse-0.9 达 **69.9**，**超越全注意力** |

> 🔥 **特别亮点**：在极端长度（128K）下，UniSparse 性能稳定，而其他方法出现明显退化。

---

### **消融实验结果**

#### （1）压缩策略比较（Table 5）
| 策略 | 精度 | 稀疏度 |
|------|-----|-------|
| **Average Pooling**（默认） | ✅ 最高 | ✅ 适中 |
| Max Pooling | ↓ 降约 1–3 pts | ↑ 更高（但信息丢失） |
| Stochastic Pooling | ↓ 明显下降 | ↑ 较高 |

> 结论：**平均池化最均衡**，能完整保留上下文信息。

#### （2）Q-K 压缩比例分配（Table 6）
- 最优配置：**$c_q = c_k = 8$**（平衡压缩）
- 查询过度压缩（$c_q=32$）导致性能大幅下降 → 查询需保留足够细节
- 键偏向压缩（$c_k=16$）表现尚可，可能因 Keys 更冗余

#### （3）压缩粒度（Table 7）
- $c=4$：太细 → 开销大
- $c=16$：太粗 → 丢失细节
- **$c=8$ 是最佳折衷**

#### （4）头压缩（Table 8）
- $c_h=2$ 或 $4$ 可显著降低选择开销（提速至 2.64×）
- 代价：略微降低最终稀疏度（因聚合后更多 block 被视为重要）
- 推荐：**当选择开销主导时启用**

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **压缩空间中的排名一致性高**  
   实验验证（图1）显示，压缩空间中 block 重要性排序与原始空间 Spearman 相关系数 > 0.98，说明 **block selection 本质是排序问题而非评分问题**，支持了压缩代理的有效性。

2. ✅ **复合令牌具有强泛化能力**  
   空间池化操作天然适用于任何序列数据（文本、图像 patch、音频片段），使 UniSparse 成为首个真正**跨模态通用的稀疏注意力方案**。

3. ✅ **全局评估 + 轻量压缩 = 最优路径**  
   相比局部探针（FlexPrefill）或简化采样（XAttention），UniSparse 通过压缩实现**低成本的全局评估**，兼顾效率与准确性。

4. ✅ **无需任务调优即可取得 SOTA**  
   所有实验使用统一参数（$c=8, P=0.95$），未针对特定任务调整，仍全面超越基线，体现其鲁棒性和实用性。

---

### **方法的局限性**

1. 🛑 **压缩可能导致极精细定位丢失**  
   如“精确引用”（Cite）任务中，极度压缩可能影响位置敏感性，但在实际稀疏水平下仍可接受。

2. 🛑 **极端压缩会平滑注意力差异**  
   当 $c > 16$ 时可能出现“过平滑”，错过关键细粒度 pattern。

3. 🛑 **目前仅优化 prefill 阶段**  
   decode 阶段仍为 dense，未来可结合 KV Cache 压缩进一步优化。

---

### **未来工作方向**

1. 🔮 **探索更智能的压缩方式**  
   如基于内容感知的 adaptive pooling，而非固定窗口平均。

2. 🔮 **扩展至 decode 阶段稀疏化**  
   结合 **SnapKV** 或 **KV cache pruning** 技术，实现全流程稀疏。

3. 🔮 **支持更大规模模型与分布式训练**  
   当前实验限于单 GPU，未来可在 tensor parallelism 场景下验证通信收益。

4. 🔮 **理论分析压缩对注意力梯度的影响**（若用于训练）

---

## 总结

> **UniSparse** 通过引入 **复合令牌** 和 **多粒度压缩**，实现了无需重训练、即插即用、跨模态通用的高效稀疏注意力机制。它在保持 **≥99% 全注意力精度**的同时，实现了最高 **2.61× 的 attention 计算加速**，并在 RULER、HELMET、Video-MME 等多个基准上全面超越 MInference、FlexPrefill、XAttention 等 SOTA 方法，是当前最接近“理想稀疏注意力”的解决方案之一。

</details>

---

### 3. [Meta Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN](https://arxiv.org/abs/2512.13715)

**Authors**: Fatemeh Lotfi, Fatemeh Afghah  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.13715v1  

#### Abstract
The increasing complexity of modern applications demands wireless networks capable of real time adaptability and efficient resource management. The Open Radio Access Network (O-RAN) architecture, with its RAN Intelligent Controller (RIC) modules, has emerged as a pivotal solution for dynamic resourc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Meta-Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代无线网络（特别是基于 **O-RAN** 架构）面临高度动态、异构且不可预测的资源管理挑战。传统方法在以下方面存在不足：
- 难以快速适应突发流量、用户密度变化和多样化服务需求（如 eMBB、URLLC、mMTC）；
- 多数基于 **DRL** 或 **Federated Learning** 的方法缺乏对复杂场景的泛化能力；
- 在大规模部署中，收敛速度慢、稳定性差，难以兼顾全局协调与局部优化。

### **提出了什么新方法或新思路**

本文提出了一种新型 **自适应 Meta-Hierarchical Reinforcement Learning (Meta-HRL)** 框架，其核心创新包括：

- ✅ **分层决策架构（HRL）**  
  将资源管理分解为两个层级：
  - **高层控制器（High-level Controller）**：负责跨切片（inter-slice）资源分配；
  - **低层控制器（Low-level Controller）**：执行切片内（intra-slice）用户级调度。
  这种结构提升了可扩展性和决策粒度。

- ✅ **基于 MAML 的元学习机制（Meta-RL）**  
  引入 **Model-Agnostic Meta-Learning (MAML)** 思想，使模型能从多个分布式单元（DUs）的经验中“学会如何学习”，实现对新任务的快速适应（few-shot adaptation）。

- ✅ **自适应加权元更新机制（Adaptive Variance Weighting）**  
  创新地使用 **TD-error 方差（Temporal Difference Error Variance）** 动态调整各任务在元梯度更新中的权重：
  $$
  w_g = \text{Softmin}(\sigma^2_{\text{TD},g})
  $$
  即更关注那些环境复杂、波动大、学习难度高的任务，从而提升整体稳定性和收敛效率。

- ✅ **面向 O-RAN 的分布式部署设计**  
  每个 DU 上运行一个独立的 HRL agent（作为 xApp），而元控制器位于 Near-RT RIC 中进行聚合与协调，符合 O-RAN 的开放解耦架构。

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **适应性** | 支持快速适应新 DU 或突发流量，无需重新训练 |
| **泛化能力** | 跨不同网络条件和切片类型具有更强的迁移能力 |
| **稳定性** | 自适应加权机制缓解了任务间不平衡问题，减少灾难性遗忘 |
| **可扩展性** | 分布式 + 层次化设计支持大规模网络扩展 |
| **理论保障** | 提供了收敛性和后悔界（regret bound）分析 |

---

## 2. 核心实验方法和设置

### **实验平台与仿真环境**

- 使用自建的 **O-RAN 仿真框架**，未依赖公开数据集（因当前缺乏标准 O-RAN RL 测试平台）；
- 基于 **PyTorch** 实现 DDPG 算法作为底层 RL 引擎；
- 所有 xApp 部署在 Near-RT RIC 模块中，符合 O-RAN 规范。

### **系统配置参数（见 Table 1）**

| 参数 | 值 |
|------|----|
| 子载波间隔 | 15 kHz |
| 单个 DU 带宽 | 20 MHz |
| RB 数量 / DU | 100 |
| 用户数量 / DU | 30 |
| 分布式单元数量 $N_g$ | 7 |
| 用户移动速度 | 10–20 m/s |
| 学习率 | $10^{-4}$ |
| 折扣因子 $\gamma$ | 0.99 |
| Batch Size | 128 |

### **评估指标**

| 指标类别 | 具体指标 |
|--------|---------|
| **性能指标** | 累计奖励（Cumulative Reward）、吞吐量、延迟、QoS满足率 |
| **服务质量（QoS）** | - eMBB：平均吞吐量<br>- mMTC：设备连接密度与吞吐加权容量<br>- URLLC：最大传输延迟 |
| **用户体验（QoE）** | 用户级吞吐量分布（CDF） |
| **适应性** | 不同 shot 数下的适应性能（0, 5, 30 shots） |
| **公平性** | Jain’s Fairness Index |
| **可扩展性** | 不同 DU 和用户规模下的收敛迭代次数与归一化奖励 |

### **基线方法对比**

- **DRL**：从零开始训练的标准深度强化学习；
- **Transfer Learning (TL)**：将已有策略迁移到新任务；
- **Multi-Task Learning (MTL)**：联合训练多个任务；
- **MAML-RL**：原始元强化学习方法（无层次结构）；
- **Uniform-Meta**：等权元更新（用于消融实验）；
- **Static-Var**：固定方差加权（非自适应）；

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **最终累计回报提升** | 相比基线方法提高 **19.8%**（图4） |
| **适应速度提升** | 最多快 **40%**（仅需17次adaptation shots即收敛） |
| **QoS满意度** | 在 eMBB、URLLC、mMTC 三类切片中均显著优于基线 |
| **归一化奖励稳定性** | 即使扩展到 30 DU / 200 用户，性能下降 < 2%（表2） |

### **与基线方法的对比结果**

- **图4（累计奖励）**：
  - Adaptive MAML-HRL 明显领先，比普通 MAML-HRL 高约 4%，比 DRL 高达 73%；
  - DRL 收敛最慢，且易陷入局部最优。

- **图6 & 图7（QoS 与用户吞吐量 CDF）**：
  - 在所有三种切片中，所提方法在高百分位表现最佳；
  - 特别是在 **mMTC** 场景下，通过自适应加权机制有效提升了密集连接场景下的吞吐保障。

- **图8（适应性能 vs Shot 数）**：
  - 随着 adaptation shots 增加，Adaptive MAML-HRL 提升最快；
  - 在仅 5-shot 场景下已接近最优性能，适合实时部署。

- **图9（新任务泛化能力）**：
  - 在三个全新任务中，Adaptive MAML-HRL 始终保持最高 QoS 表现；
  - TL 和 MTL 泛化能力有限，尤其在 URLLC 低延迟要求下失效明显。

### **消融实验结果（Ablation Study，见 Table 3）**

| 方法 | 归一化奖励 | 收敛所需 shots |
|------|------------|----------------|
| Uniform-Meta（均匀加权） | 0.78 | 28 |
| Static-Var（静态方差加权） | 0.81 | 22 |
| **Adaptive-Var（本文方法）** | **0.84** | **17** |

> ✅ 自适应加权机制带来约 **3% 的奖励增益** 和 **40% 的加速收敛**。

此外，在扩展指标上也表现出色：
- 平均时延降低 **9.2%**；
- Jain 公平性指数从 0.91 提升至 **0.96**；
- 在 50% 流量激增场景下，性能退化 < **5%**，体现强鲁棒性。

---

## 4. 关键结论和发现

### **主要发现**

1. **Meta-HRL 架构显著提升 O-RAN 资源管理效率**  
   结合 HRL 的结构优势与 Meta-RL 的快速适应能力，实现了高效、灵活、可扩展的智能资源调度。

2. **自适应加权机制是性能提升的关键**  
   利用 TD-error 方差动态调节任务重要性，使得模型更专注于复杂、高变异性场景（如 mMTC 密集接入），避免被简单任务主导。

3. **具备良好的可扩展性与稳定性**  
   即使在网络规模扩大（30 DU, 200 UE）时，仍能保持近似恒定的归一化性能，验证了其适用于大型实际部署。

4. **理论分析支持实践效果**  
   论文提供了严格的收敛性分析和后悔界证明，表明该算法在两时间尺度下具有 **sublinear convergence** 和 **bounded regret**，增强了可信度。

### **方法的局限性**

- 当前实验基于仿真环境，尚未在真实 O-RAN testbed 上验证（受限于硬件平台成熟度）；
- 元更新通信开销虽小（仅交换网络参数），但在超大规模部署中仍需进一步优化；
- 对极端非平稳环境（如频繁拓扑变更）的长期稳定性有待深入研究。

### **未来工作方向**

- 在 **OAI** 或 **srsRAN** 等开源平台上实现原型系统，开展 **Hardware-in-the-loop 验证**；
- 探索 **通信高效的元参数更新机制**（如量化、稀疏化）；
- 扩展至 **multi-hop 网络** 和 **UAV-assisted 6G 场景**；
- 引入 **因果推理** 或 **world model** 来增强预测能力和抗干扰性。

---

> 📌 **总结一句话**：  
> 本文提出的 **Adaptive Meta-HRL** 框架，通过融合 **分层控制** 与 **基于 TD-error 方差的自适应元学习**，为 O-RAN 提供了一个高效、快速适应、可扩展且理论可靠的资源管理解决方案，在多项指标上全面超越现有方法。

</details>

---

### 4. [Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification](https://arxiv.org/abs/2512.14491)

**Authors**: Cheng-Han Lu, Pei-Hsuan Tsai  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.14491v1  

#### Abstract
Transformer-based multi-modal intelligent systems often suffer from high computational and energy costs due to dense self-attention, limiting their scalability under resource constraints. This paper presents SMMT, a sparse multi-modal transformer architecture designed to improve efficiency and robus...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前基于 Transformer 的多模态智能系统在阿尔茨海默病（Alzheimer's Disease, AD）分类任务中面临的两个关键挑战：
- **高计算与能耗成本**：由于标准 Transformer 使用 dense self-attention，导致时间复杂度为 $O(n^2)$，难以在资源受限环境下扩展。
- **对不完整输入敏感**：临床数据常存在缺失模态（missing modalities），而现有模型缺乏显式机制来处理此类情况，影响实际部署中的鲁棒性。

### 提出的新方法：SMMT
作者提出 **Sparse Multi-Modal Transformer with Masking (SMMT)**，一种从系统层面设计的高效、鲁棒的多模态融合架构，其两大核心创新为：

#### （1）Cluster-based Sparse Attention
- 将 tokens 按 query 向量进行 K-Means 聚类（簇数 $k = \log_2 n$）
- 自注意力仅在每个 cluster 内部计算，将计算复杂度从 $O(n^2)$ 降低至 $O(n \log n)$
- 在保持表征能力的同时显著减少内存占用和计算开销

#### （2）Modality-wise Masking
- 在训练过程中对各模态特征随机施加二值掩码（masking ratio $r=0.3$）
- 模拟真实场景下的模态缺失，提升模型对 incomplete inputs 的泛化能力和鲁棒性
- 属于一种正则化策略，防止过拟合，尤其适用于小样本场景

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 显著降低训练时间和能源消耗（总能耗下降 40.4%） |
| **鲁棒性** | 对缺失模态更具容忍性，在低数据条件下仍表现优异 |
| **可扩展性** | 支持更大序列长度输入，适合大规模医疗数据分析 |
| **可持续性** | 减少碳排放，符合绿色 AI 发展趋势 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **ADNI-1 和 ADNI-2** 数据集作为研究对象
- 包含多模态数据：
  - **Imaging**: T1-weighted MRI 切片（预处理后为 256×256 RGB 图像）
  - **Clinical Scores**: MMSE, CDR, FAQ, 年龄等数值型变量
  - **Categorical Data**: APOE 基因型、性别等类别变量
- 最终保留 AD 与 CN 类别共 12,680 张 MRI 切片用于训练与评估
- 排除 MCI 样本以避免类别不平衡及任务复杂化

### 实验设置
- **框架**：PyTorch
- **硬件平台**：NVIDIA GeForce RTX 3060 GPU（12GB）
- **超参数**：
  - Batch Size: 8
  - Epochs: 50（五折交叉验证共 250 epochs）
  - Optimizer: Adam ($\text{lr} = 1\times10^{-3}$)
  - Latent Dimension: 512
  - Masking Ratio: 0.3（modality-level 随机 masking）

### 评估指标

#### 分类性能指标
- Accuracy, Precision, Recall (Sensitivity), F1-score
- Specificity, AUC（ROC 曲线下面积）

#### 计算可持续性指标
- **Energy Consumption**（通过 CodeCarbon 工具监控 CPU/GPU/RAM 功耗估算）
- **CO₂ Emissions**：基于台湾电网碳强度（0.502 kgCO₂/kWh）换算
- **Training Time**（分钟）

### 基线方法对比
| 模型 | 类型 | 特点 |
|------|------|------|
| **3MT [8]** | Transformer-based Hybrid Fusion | 当前 SOTA 多模态 AD 分类器，采用级联 cross-attention |
| **ADDFformer [20]** | Imaging-only Transformer | 基于结构 MRI 的单模态诊断模型 |
| **FusionNet [21]** | General Multi-modal Fusion | 非注意力机制的通用融合网络 |
| **CNN-only [22]** | Single-modality CNN | 如 VGG16，仅使用 MRI 输入 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（100% 数据）

| 模型 | Accuracy (%) | Sensitivity (%) | Specificity (%) | AUC |
|------|--------------|------------------|------------------|-----|
| CNN-only [22] | 80.24 | 78.45 | 80.22 | 0.852 |
| FusionNet [21] | 94.28 | 91.01 | 93.24 | 0.956 |
| ADDFformer [20] | 88.20 | 91.87 | 91.53 | 0.948 |
| 3MT (Baseline) [8] | 90.28 | 93.64 | 93.81 | 0.965 |
| **SMMT (Ours)** | **97.05** | **96.31** | **97.58** | **0.986** |

> ✅ SMMT 在所有指标上均达到最优，尤其在 AUC 上接近 0.99，表现出极强的判别能力。

### 不同数据规模下的准确性比较

| Dataset Size | SMMT | 3MT | ADDFformer | FusionNet | CNN-only |
|-------------|-------|------|------------|-----------|----------|
| 100%        | **97.05** | 90.28 | 88.20      | 94.28     | 80.24    |
| 80%         | **94.20** | 87.12 | 83.82      | 86.37     | 77.27    |
| 60%         | **91.28** | 86.25 | 77.58      | 79.25     | 70.57    |
| 40%         | **86.52** | 82.17 | 74.20      | 76.83     | 70.55    |
| **20%**     | **84.96** | 78.92 | 71.92      | 75.15     | 68.53    |

> 🔍 SMMT 在低数据条件下（如 20%）依然保持 84.96% 的准确率，远超其他模型，体现其卓越的数据效率和泛化能力。

### 训练效率与能耗对比

#### 总体能耗（250 epochs）
| Component | 3MT (kWh) | SMMT (kWh) | Reduction |
|---------|-----------|------------|-----------|
| CPU     | 0.108489  | 0.071179   | 34.4%     |
| GPU     | 0.283977  | 0.159642   | 43.8%     |
| RAM     | 0.051035  | 0.033485   | 34.4%     |
| **Total** | **0.443501** | **0.264306** | **↓40.4%** |

#### 碳排放（CO₂）
- 3MT: 0.2226 kgCO₂
- SMMT: **0.1327 kgCO₂** → **减少 40.3%**
- 相当于节省约 18 次智能手机充电或点亮 60W 灯泡超过 30 小时

#### 训练时间（100% 数据）
- SMMT: **112 分钟**
- 3MT: 133 分钟
- CNN-only (VGG16): >140 分钟（最慢）

> ⚡ SMMT 是唯一同时实现更高精度与更低训练时间/能耗的模型。

### 消融实验结果（Ablation Study）

| 配置 | 100% Acc / Time(min) | 20% Acc / Time(min) |
|------|------------------------|----------------------|
| 3MT (Baseline) | 90.28 / 133 | 78.92 / 41 |
| w/o Sparse Attention | 91.35 / 147 | 84.85 / 51 |
| w/o Masking | 95.42 / 108 | 80.85 / 37 |
| **SMMT (Full)** | **97.05 / 112** | **84.96 / 45** |

#### 关键发现：
- **移除 Sparse Attention**：训练时间大幅增加（↑38%），但精度略有下降 → 表明稀疏注意力不仅提效，还可能起隐式正则化作用
- **移除 Masking**：训练更快，但在低数据下精度明显下降（20% 时 ↓4.11%）→ 证明 masking 对提升鲁棒性和泛化至关重要
- **两者结合**：实现最佳平衡——高效 + 高性能 + 强鲁棒性

此外，masking ratio 的消融显示：
- 最优 $r = 0.3$
- 当 $r > 0.6$ 时性能急剧下降，说明过度 masking 会破坏关键信息

---

## 4. 关键结论和发现

### 主要结论
1. **SMMT 在 AD 分类任务中实现了 SOTA 性能**：
   - 全数据下达 **97.05% 准确率**，AUC 达 **0.986**
   - 即使仅用 20% 数据也能取得 **84.96% 准确率**

2. **显著提升计算效率与环境可持续性**：
   - 训练能耗降低 **40.4%**
   - 碳排放减少 **40.3%**
   - 更稳定、更可预测的硬件资源利用

3. **具备更强的现实适用性**：
   - 通过 modality-wise masking 显式建模缺失模态，增强临床部署鲁棒性
   - 适用于小样本、资源受限的真实医疗场景

4. **稀疏注意力具有双重收益**：
   - 显著降低计算负担（$O(n^2) \to O(n \log n)$）
   - 可能起到类似 Dropout 的正则化效果，抑制噪声连接

### 方法的局限性
- 当前仅限于 **binary classification (AD vs. CN)**，未涵盖 MCI 或疾病进展阶段预测
- 聚类（K-Means）虽轻量但仍引入额外计算，且依赖 GPU 批处理优化
- 所有实验基于 ADNI 数据集，需进一步验证在其他中心或多民族人群中的泛化能力

### 未来工作方向
1. 扩展至 **multi-class classification**，纳入 MCI 及 AD progression staging
2. 开发 **longitudinal modeling** 能力，支持时间序列数据预测病情演变
3. 设计 **adaptive modality selection** 机制，在推理时动态应对可变模态输入
4. 在更大、更多样化的临床队列中验证 SMMT 的普适性与稳定性
5. 探索将其应用于 **Human Digital Twin** 构建中的医学表征学习模块

---

> 📌 **总结一句话**：  
> SMMT 成功地将 **sparse attention** 与 **masked learning** 结合，构建了一个既高效又鲁棒的多模态 Transformer 架构，在保证甚至超越 SOTA 分类性能的同时，大幅降低了训练成本与碳足迹，为可持续、可扩展的医疗 AI 提供了一种极具前景的技术路径。

</details>

---

### 5. [PruneX: A Hierarchical Communication-Efficient System for Distributed CNN Training with Structured Pruning](https://arxiv.org/abs/2512.14628)

**Authors**: Alireza Olama, Andreas Lundell, Izzat El Hajj, Johan Lilius, Jerker Bj\"orkqvist  
**Category**: cs.DC  
**Published**: 2025-12-17  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.14628v1  

#### Abstract
Inter-node communication bandwidth increasingly constrains distributed training at scale on multi-node GPU clusters. While compact models are the ultimate deployment target, conventional pruning-aware distributed training systems typically fail to reduce communication overhead because unstructured s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PRUNEX: A Hierarchical Communication-Efficient System for Distributed CNN Training with Structured Pruning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大规模多节点 GPU 集群上进行分布式训练时，**跨节点通信带宽已成为主要瓶颈**。尽管模型压缩（如剪枝）是部署端的目标，但现有的剪枝感知训练系统通常无法有效减少通信开销，原因在于：
- **非结构化稀疏性（Unstructured Sparsity）** 无法被高度优化的稠密集合通信原语（如 NCCL AllReduce）高效利用；
- 现有方法往往在全局同步后再执行剪枝，导致仍需传输大量零值参数和索引元数据。

此外，传统框架将集群视为扁平拓扑，忽略了**节点内（intra-node）与节点间（inter-node）链路带宽的巨大差异**。

---

### 提出的新方法与创新思路

PRUNEX 提出了一种**层次化、通信高效的分布式 CNN 训练系统**，其核心是 **H-SADMM（Hierarchical Structured ADMM）算法** 和配套的系统架构设计。

#### 主要创新点：

1. ✅ **H-SADMM 算法：在节点级强制结构化稀疏**
   - 将 ADMM 优化框架与集群物理拓扑结合，在**节点级共识变量（node-level consensus）上施加结构化稀疏约束**（如通道/滤波器剪枝），使得稀疏化发生在跨节点通信之前。
   - 这样可以在同步前就“物理缩小”张量大小，避免传输冗余参数。

2. ✅ **物理缓冲区压缩机制（Physical Buffer Shrinkage）**
   - 利用全局一致的稀疏掩码（sparsity mask），将稀疏张量压缩为**连续的稠密小块缓冲区**进行通信。
   - 完全消除稀疏格式带来的**索引开销（metadata overhead）**，并可直接使用高效的稠密集合操作（如 AllReduce）。

3. ✅ **领导者-跟随者分层架构（Leader-Follower Architecture）**
   - 构建两个独立的进程组：
     - **Intra-node Group**：所有 worker 参与，用于快速同步节点内部状态；
     - **Inter-node Group**：仅由各节点的 leader（rank 0）组成，负责跨节点通信。
   - 节点 leader 扮演“剪枝网关”角色，在发送前完成投影、掩码同步和缓冲区压缩。

4. ✅ 开源实现
   - 作者已将 PRUNEX 开源：[https://github.com/Alirezalm/PruneX](https://github.com/Alirezalm/PruneX)

---

### 相比现有方法的优势

| 方法 | 类型 | 是否支持结构化 | 通信效率 | 推理加速 | 局限性 |
|------|------|----------------|-----------|------------|--------|
| Top-K / DGC | Unstructured | ❌ | 中低（依赖 AllGather/Gossip） | ❌（不规则内存访问） | 编码开销大，难以提速 |
| PacTrain | Unstructured | ❌ | 高（打包传输） | 低 | 仍需处理稀疏索引 |
| PruneTrain | Structured | ✅ | 中等（需重新配置） | ✅ | “Stop-and-Reconfigure”中断流水线 |
| **PRUNEX (本文)** | **Structured** | ✅ | ✅✅✅ **高（压缩后稠密 AllReduce）** | ✅✅✅ **高（纯稠密 kernel）** | —— |

> PRUNEX 实现了**通信效率与计算效率的双重提升**，且无需牺牲训练稳定性。

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **CIFAR-10** 数据集作为标准图像分类任务基准。

### 模型架构
评估三种主流 CNN 模型：
| 模型 | 参数量 | GFLOPs |
|------|--------|--------|
| ResNet-18 | ~11M | 1.8 |
| WideResNet-50-2 | ~69M | 11.4 |
| ResNet-152 | ~60M | 11.3 |

其中 ResNet-152 为主要分析对象。

---

### 实验平台
- **硬件环境**：芬兰 CSC 的 **Puhti 超算平台**
  - 16 个节点，共 **64 块 NVIDIA V100 GPU**（每节点 4 块）
  - 节点内通过 NVLink 互联（高带宽）
  - 节点间通过 Mellanox HDR InfiniBand（100 Gbps）
- **软件栈**：
  - PyTorch 2.9.1 + torch.distributed + NCCL 2.25.1

---

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **PyTorch DDP (Dense Baseline)** | 标准的稠密同步 SGD，使用 Ring-AllReduce |
| **Top-K SGD** | 每层保留梯度绝对值最大的 1%，其余置零 |
| **PruneX (AR)** | 消融版本，采用扁平共识结构（flat consensus），无层级压缩 |

---

### 评估指标
- **End-to-end 时间到准确率（Time-to-Accuracy）**
- **累计跨节点通信体积（Communication Volume）**
- **每迭代通信延迟（Communication Latency per Iteration）**
- **强扩展性（Strong Scaling Speedup & Efficiency）**
- **收敛性分析（Primal/Dual Residuals）**
- **精度-稀疏度权衡曲线（Sparsity-Accuracy Trade-off）**

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 🔹 通信体积显著降低
- 在 ResNet-152 上，总跨节点通信量从 DDP 的 **13.00 GB** 下降到 **5.21 GB**，**减少约 60%**。
- 其他模型也保持一致收益：
  - ResNet-18：2.53 GB → 1.01 GB （↓60.1%）
  - WideResNet-50-2：14.88 GB → 5.97 GB （↓59.9%）

> 图 6 显示压缩消息尺寸随 H-SADMM 迭代迅速下降并在 ~40% 原始大小稳定。

#### 🔹 通信延迟大幅缩短
- 每次迭代的通信时间从 DDP 的 **~0.50 秒**降至 PRUNEX 的 **~0.10 秒**，实现 **5× 同步速度提升**。
- 分析表明，**Inter-node AllReduce 占总通信时间的 68.4%**，正是 PRUNEX 压缩所针对的关键路径（图 8）。

#### 🔹 强扩展性优异
- 在 64 GPU 上，PRUNEX 实现 **6.75× 强扩展加速比**，远超：
  - DDP 基线：5.81×
  - Top-K：3.71×
- 并行效率达 **84.4%**（DDP 为 72.7%），说明通信瓶颈得到有效缓解。

#### 🔹 更快达到目标精度
- PRUNEX 在 **8 分钟内达到 70% 准确率**，而 DDP 在 14 分钟内未能达到该水平（图 5a）。
- 在相同通信量下，PRUNEX 达到更高精度，体现“**Accuracy per Byte**”优势（图 5b）。

---

### 与基线方法对比结果

| 方法 | 时间到 70% 精度 | 总通信量 | 强扩展加速比（64 GPU） |
|------|------------------|------------|----------------------------|
| DDP（Dense） | >14 min（未达） | 60+ GB | 5.81× |
| Top-K SGD | ~14 min（仅 ~50% acc） | 较少但噪声大 | 3.71× |
| **PRUNEX (Hierarchical)** | **~8 min** | **~5.21 GB** | **6.75×** |

> PRUNEX 不仅更快，而且最终精度更高（接近原始模型），证明其剪枝过程不影响模型表达能力。

---

### 消融实验结果

- **PruneX (AR)**（扁平结构）表现差：
  - 通信延迟波动大，缺乏一致性；
  - 无法实现缓冲区压缩，验证了**层次化架构是实现高效通信的前提**。
- **Mask Freezing 机制**有效：
  - 稀疏模式在前 5–15 次迭代趋于稳定；
  - 冻结后可预分配通信缓冲区，进一步减少动态开销。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **结构化剪枝必须前置到通信之前才能真正节省带宽**：PRUNEX 通过 H-SADMM 在节点级施加稀疏约束，实现了这一点。
2. ✅ **物理缓冲区压缩优于稀疏编码传输**：去除索引开销后，可用标准稠密集合原语（AllReduce）获得更高吞吐。
3. ✅ **层次化共识结构至关重要**：分离 intra-node 和 inter-node 同步路径，使系统能针对性地优化最慢环节（即跨节点通信）。
4. ✅ **精度损失可控**：在 50% 通道稀疏率下，ResNet-152 仍能达到 81.4% 准确率（仅比完整模型低 ~4.6%），且可通过微调恢复。
5. ✅ **收敛稳定**：primal/dual residuals 单调递减，证明 H-SADMM 能有效达成全局共识。

---

### 方法的局限性
- 当前主要面向 **CNN 架构**，对 Transformer 等 attention-based 模型尚未验证；
- 结构化剪枝可能限制灵活性，不如非结构化剪枝极致压缩；
- 需要额外管理稀疏掩码和投影逻辑，增加实现复杂度；
- 对极深网络的层间异质性依赖 adaptive penalty tuning，调参有一定挑战。

---

### 未来工作方向
1. 🚀 扩展至更大规模模型：如 Vision Transformers (ViTs) 和大型语言模型（LLMs）；
2. 🚀 支持更深的层次结构（如 rack-level aggregation）；
3. 🚀 在数千 GPU 规模的集群上验证其可扩展性；
4. 🚀 探索与其他压缩技术（量化、低秩分解）联合使用；
5. 🚀 自动化稀疏率调度策略，替代人工设定 `Tfreeze`。

---

## 总结

PRUNEX 成功实现了 **“算法-系统协同设计”（co-design）**，将结构化剪枝从一个事后压缩手段转变为**贯穿训练全过程的通信优化引擎**。它不仅减少了通信量，还提升了计算效率，并在真实超算平台上展现出卓越的扩展性和端到端性能，为未来大规模分布式训练提供了新的范式。

</details>

---

### 6. [Variational Physics-Informed Ansatz for Reconstructing Hidden Interaction Networks from Steady States](https://arxiv.org/abs/2512.13708)

**Authors**: Kaiming Luo  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.13708v1  

#### Abstract
The interaction structure of a complex dynamical system governs its collective behavior, yet existing reconstruction methods struggle with nonlinear, heterogeneous, and higher-order couplings, especially when only steady states are observable. We propose a Variational Physics-Informed Ansatz (VPIA) ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Variational Physics-Informed Ansatz for Reconstructing Hidden Interaction Networks from Steady States

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**复杂动力系统中隐藏交互网络的重构难题**，尤其是在以下挑战条件下：
- 只能观测到系统的**稳态（steady states）**，而无法获取时间序列或导数信息；
- 系统具有**非线性、异质性和高阶耦合（higher-order couplings）**；
- 存在显著的测量噪声、动态噪声或结构不确定性；
- 网络规模大，传统方法计算成本高昂。

现有方法如基于时间序列的方法（需估计导数）、统计相关性分析、PINNs等，在上述场景下表现受限，尤其难以处理高阶、有向、加权且稠密的网络。

---

### 提出的新方法：VPIA（Variational Physics-Informed Ansatz）

作者提出了一种名为 **Variational Physics-Informed Ansatz (VPIA)** 的新框架，其核心思想是：
- 将未知的**交互算子（interaction operator）** 表示为一个可训练的变分对象（variational representation）；
- 利用物理驱动的**稳态残差（steady-state residual）** 作为优化目标，直接从多个异构稳态数据中推断网络结构；
- 不依赖时间轨迹、导数估计或监督标签。

#### 主要创新点：
1. **物理约束嵌入的变分表示法**  
   - 每个候选交互（包括成对、有向、高阶simplex）被赋予一个可学习参数，通过sigmoid映射保证可微性和拓扑可解释性；
   - 支持统一建模 undirected/directed/weighted/higher-order interactions。

2. **无需时间数据的逆算符学习（inverse operator learning）**  
   - 区别于传统PINNs用于求解PDE或参数反演，VPIA将整个**交互结构视为未知算符进行学习**，属于更广义的“物理信息算符发现”。

3. **可扩展的学习机制：残差采样 + 自然梯度优化**
   - **Residual Sampling**：每次迭代仅随机采样部分节点残差，使每步计算复杂度由 $O(N^2)$ 降至接近 $O(N)$；
   - **Natural Gradient Updates**：利用Fisher信息矩阵的对角近似进行曲率感知更新，提升高维病态参数空间中的收敛稳定性。

4. **适用于高阶网络（simplicial complexes / hypergraphs）**
   - 使用张量形式编码2-simplex（三角形）、3-simplex（四面体）等多体相互作用；
   - 在无先验假设的情况下联合恢复不同阶次的耦合结构。

---

### 相比现有方法的优势
| 维度 | VPIA优势 |
|------|---------|
| **数据需求** | 仅需稳态快照，无需时间序列或导数估计 |
| **模型通用性** | 适用于多种动力学系统（Kuramoto, FitzHugh-Nagumo, Lorenz等），不依赖具体方程形式 |
| **鲁棒性** | 对observation/dynamical/structural noise均表现出强健性 |
| **可扩展性** | 支持上千节点的大规模网络重建（如Human Brain Network, N=989） |
| **表达能力** | 能同时识别有向、加权、稀疏/稠密及高阶交互结构 |

---

## 2. 核心实验方法和设置

### 数据集
实验涵盖两类数据：

#### （1）人工生成网络（Synthetic Networks）
- **Erdős–Rényi 随机图**（$G(N,p)$，$p=0.5$），用于控制变量测试；
- **高阶网络**：通过组合所有可能的$k$-node子集并以概率$p=0.5$保留，构建2-/3-/4-simplex结构；
- 动力学模型包括：
  - Phase oscillators: Kuramoto, Kuramoto-Sakaguchi
  - Limit-cycle: Stuart-Landau, Van der Pol, FitzHugh-Nagumo
  - Chaotic systems: Rössler, Lorenz, Hindmarsh-Rose

#### （2）真实世界网络（Real-world Networks）
来自公开数据库 [Network Data Repository](http://networkrepository.com/) 和文献引用，共8个代表性网络：

| 网络名称 | 类型 | 节点数 $N$ | 边数 $E$ | 密度 $\rho$ |
|--------|-----|-----------|----------|------------|
| Florentine Families | Undir | 16 | 20 | 0.1667 |
| Zachary Karate Club | Undir | 34 | 78 | 0.1390 |
| Wild Bird Social | Undir | 202 | 11733 | 0.5861 |
| C. elegans Neural | Dir | 297 | 2359 | 0.0259 |
| Power Grid | Undir | 494 | 596 | 0.0049 |
| Retweet Weibo | Dir | 596 | 1415 | 0.0080 |
| Human Brain Neural | Undir | 989 | 35730 | 0.036 |

---

### 实验设置
- **稳态生成**：对每个外部条件 $p^{(m)}$ 初始化随机状态，数值积分至满足收敛准则 $\|\dot{x}\|_2 < \epsilon$；
- **过滤退化稳态**：剔除同步或低离散度配置，使用 $D(x^*) > \epsilon_{\text{sync}}$（如方差或平均距离）；
- **样本数量**：通常取 $M = O(N)$ 至 $O(N \log N)$ 个独立稳态实验；
- **噪声注入**：
  - **Observation noise**: $x^* \leftarrow x^* + \sigma_{\text{obs}} \cdot \text{MAD} \cdot \xi$
  - **Dynamical noise**: $\dot{x} = F(x, A) + \sigma_{\text{dyn}} \cdot \xi(t)$
  - **Structural noise**: $A_{ij} \leftarrow A_{ij} + \sigma_{\text{str}} \cdot \xi$

---

### 评估指标
- **AUC（Area Under ROC Curve）**：为主要评价指标，阈值无关，适合不同稀疏性的网络；
- 其他指标：Accuracy, Precision, Recall, F1-score（当定义明确阈值时）；
- 成功重建定义为：AUC = 1 且 Accuracy/Precision/Recall/F1-score 均达1。

---

### 基线方法对比（见Supplementary Sec. S4）
比较了以下经典方法：
- **Transfer Entropy (TE)**
- **Network Deconvolution (ND)**
- **Partial Phase Synchronization (PPS)**
- **Modular Response Analysis (MRA)**
- **Global Silencing (GS)**
- **Correlation-based inference**

这些方法大多基于Jacobian回归、协方差反演或间接关联过滤，在面对**中等以上密度、有向、加权或非线性系统**时性能明显下降。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）在人工网络上的表现（Fig. 2）
- 所有8类动力系统（从Kuramoto到Lorenz混沌系统）均实现 **AUC > 0.95**，多数接近 **AUC ≈ 1.0**；
- 仅需约 **$M \sim O(N)$** 个稳态即可完成高质量重建；
- 图2C显示：最小所需实验数随 $N$ 近似线性增长，表明信息效率稳定。

#### （2）噪声鲁棒性（Fig. 3）
- 即使在 **noise level = 0.5**（相对扰动50% MAD）下：
  - 观测噪声：AUC仍可达 >0.9（$M \geq 30$）
  - 动态噪声 & 结构噪声：随着 $M$ 增加，AUC趋近于1
- 表明：**全局稳态约束能有效抑制局部零均值噪声的影响**

#### （3）大规模真实网络重建（Fig. 4）
| 网络 | 节点数 | 达到完美重建所需 $M$ |
|------|-------|------------------|
| Florentine Families | 16 | ~10 |
| Zachary Karate Club | 34 | ~20 |
| Wild Bird Social | 202 | ~60 |
| C. elegans Neural | 297 | ~80 |
| Power Grid | 494 | ~120 |
| Retweet Weibo | 596 | ~140 |
| Human Brain Neural | 989 | ~200 |

> ✅ 所有网络均能在合理数量的稳态下实现 **AUC → 1**

#### （4）高阶交互重建（Fig. 5）
- 成功重建 **2-simplex（三角形）至4-simplex（四体耦合）** 张量结构；
- 尽管候选高阶连接呈组合爆炸增长，但所需稳态数**亚组合增长**（sub-combinatorial）；
- 在 $N=10$, 所有可能2/3/4-simplex以 $p=0.5$ 存在时：
  - AUC在 $M=15$ 左右即接近1；
  - 对三种噪声仍保持良好恢复能力（Fig. 5E）

---

### 与基线方法的对比结果（Supplementary Sec. S4）
- 在中等密度（$\rho > 0.1$）或有向/加权网络上，所有baseline方法性能显著下降（AUC < 0.8）；
- 特别是在Wild Bird Social（密度0.586）这类稠密社交网络上，多数方法几乎失效；
- 而 **VPIA始终保持AUC > 0.95**，支持恢复精度接近100%

---

### 消融实验（隐含于文中分析）
虽然未单独列出消融表，但以下设计验证了关键组件的有效性：
- **残差采样 vs 全批量更新**：采样策略将复杂度从 $O(MN^2)$ 降为 $O(Ms)$，使得 $N \sim 10^3$ 成为可行；
- **自然梯度 vs 普通Adam**：在大型网络中，自然梯度显著加快收敛并避免震荡；
- **稳态多样性过滤机制**：去除低离散度样本后，重建成功率大幅提升。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **稳态数据蕴含足够信息用于精确网络重构**  
   即使没有时间演化路径，多个异构稳态所提供的全局代数约束足以唯一确定复杂的交互结构。

2. ✅ **VPIA具备高度通用性和跨动力学不变性**  
   在相位振子、极限环、兴奋性介质乃至混沌系统中均取得一致高性能，说明其依赖的是**物理一致性而非特定函数形式**。

3. ✅ **高阶交互可在无先验情况下被有效识别**  
   通过张量化变分参数化，VPIA首次实现了仅凭稳态数据对2-/3-/4-simplex结构的同时恢复。

4. ✅ **残差采样 + 自然梯度构成高效可扩展引擎**  
   二者结合解决了传统方法在大规模、稠密、高阶网络中的“维度灾难”问题，实现 $N \sim 10^3$ 级别的实际可用性。

5. ✅ **噪声可通过增加稳态样本数来补偿**  
   各类噪声虽降低初始性能，但随着 $M$ 增加，AUC单调上升并趋于完美，体现“数据平滑噪声”的能力。

---

### 方法的局限性
1. ❗ **需要多个不同的稳态观测**  
   若系统只能产生相似或退化的稳态（如同步态），则无法提供足够约束，导致不可辨识。

2. ❗ **依赖稳态可达性**  
   对于长期振荡、准周期或永不收敛的系统，需扩展至quasi-steady或metastable状态建模。

3. ❗ **极端高阶网络仍具挑战**  
   当 $d$-simplex 阶数过高（如 $d>4$）且 $N$ 很大时，参数空间巨大，即使采样也面临内存与训练负担。

4. ❗ **超参数敏感性存在**  
   如sigmoid steepness $k$、采样比例 $s/N$、learning rate等需谨慎调参，尽管作者称结果对小范围变化鲁棒。

---

### 未来工作方向
1. 🔄 **推广至时变或自适应网络**：考虑缓慢演化的拓扑结构；
2. 💡 **结合贝叶斯推理**：引入不确定性量化，提供置信区间；
3. ⚙️ **分布式优化架构**：应对 $N > 10^4$ 的超大规模系统；
4. 🧠 **应用于真实生物/社会系统**：如fMRI脑功能网络、基因调控网络、社交媒体传播结构等仅有快照数据的领域；
5. 🔍 **发展更高效的变分参数化方案**：例如低秩分解、图神经网络引导先验等，进一步压缩搜索空间。

---

## 总结
**VPIA 是一种突破性的、完全基于稳态数据的网络重构框架**。它融合了物理建模、变分学习、采样加速与几何优化，成功解决了传统方法在**非线性、高阶、噪声、大规模**场景下的瓶颈问题。该方法不仅理论严谨，而且在多样化的人工与真实网络上展现出卓越性能，为理解复杂系统背后的隐藏结构提供了强有力的工具，尤其适用于那些**只能获得静态观测**的实际应用场景。

</details>

---

### 7. [ValuePilot: A Two-Phase Framework for Value-Driven Decision-Making](https://arxiv.org/abs/2512.13716)

**Authors**: Yitong Luo, Ziang Chen, Hou Hei Lam, Jiayu zhan, Junqi Wang, Zhenliang Zhang, Xue Feng  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13716v1  

#### Abstract
Personalized decision-making is essential for human-AI interaction, enabling AI agents to act in alignment with individual users' value preferences. As AI systems expand into real-world applications, adapting to personalized values beyond task completion or collective alignment has become a critical...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*ValuePilot: A Two-Phase Framework for Value-Driven Decision-Making*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 AI 决策系统多为任务导向（task-oriented），依赖外部奖励信号，难以适应个体用户的**个性化价值偏好**（personalized value preferences）。尽管已有研究如 RLHF 和 DPO 尝试对齐人类价值观，但通常基于集体反馈，忽视了个体差异。此外，缺乏高质量、细粒度标注的决策数据集，使得模型无法学习到行为与深层价值维度之间的关联。

### 提出的新方法与思路
作者提出 **ValuePilot**，一个两阶段框架，用于实现**以价值为导向的个性化决策**（value-driven decision-making）：

- **Dataset Generation Toolkit (DGT)**  
  利用 LLM 构建一个由人类与 LLM 协作生成的决策场景数据集，每个场景和候选动作都带有多个价值维度（如 Curiosity, Safety, Fairness 等）的数值评分（-1 到 +1），从而提供可解释的价值标注。

- **Decision-Making Module (DMM)**  
  结合客观的动作价值评估与用户主观的价值偏好，通过 PROMETHEE 多准则决策方法进行动作排序，输出符合个体偏好的可解释决策序列。

### 相比现有方法的优势
| 维度 | 传统方法（如 RLHF, ReAct） | ValuePilot |
|------|----------------------------|------------|
| 决策依据 | 外部任务目标 / 集体偏好 | 明确建模个体价值偏好 |
| 可解释性 | 黑箱推理过程 | 动作得分可分解至各价值维度 |
| 泛化能力 | 依赖训练任务 | 在未见场景中仍能做出合理决策 |
| 数据支持 | 缺乏价值标注数据 | 自动构建带价值分数的数据集 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **自建数据集**：通过 DGT 生成，共包含 **11,938 个场景** 和 **100,255 个动作**。
- 覆盖六个核心价值维度：`Curiosity`, `Energy`, `Safety`, `Happiness`, `Intimacy`, `Fairness`。
- 数据分层设计（1-D 到 6-D 组合），支持复杂度递增的测试。
- 包含自动过滤与人工审核流程，确保真实性与一致性。

### 实验设置与评估指标

#### （1）Value Recognition 实验
- **任务**：评估 Value Assessment Network 是否能准确识别动作在各个价值维度上的影响。
- **基线模型**：Llama-3.5-70b, Llama-3.5-405b, Mixtral-8x22b, Gemini-1.5-flash。
- **评估指标**：
  - **AvgAcc**：阈值为 0.2 和 0.05 的平均分类准确率。
  - **MAE**：预测分数与真实值之间的平均绝对误差。

#### （2）Value-driven Decision-making 实验
- **参与者**：40 名人类被试，完成问卷调查其价值偏好并排序实际情境中的行动选项。
- **输入**：每个被试的六维价值偏好向量 + 场景描述。
- **输出**：模型预测的动作排序。
- **基线模型**：GPT-5, Claude-Sonnet-4, Gemini-2.5-flash, Llama-3.1-70b, DeepSeek-R1, Kimi-K2, GPT-4o-mini。
- **评估指标**：
  - **Order-Sensitive Similarity (OS-Sim)**：基于前缀交集的排序相似度，强调高位次匹配。
  - **First-Action Accuracy (First-Acc)**：首位动作选择正确的比例。

#### （3）消融实验（Ablation Study）
移除 DMM 中的关键组件，验证其必要性：
- Only Action
- w/o Preference
- w/o Subjective
- w/o Scenario

---

## 3. 主要实验结果和性能指标

### （1）Value Recognition 性能对比（Table 1）

| Model | AvgAcc (%) @t=0.2 | AvgAcc (%) @t=0.05 | MAE |
|-------|-------------------|--------------------|-----|
| Llama-3.5-70b | 40.90 | 17.74 | 0.30 |
| Mixtral-8x22b | 42.71 | 18.39 | 0.29 |
| Gemini-1.5-flash | 51.61 | 25.64 | 0.24 |
| **Value Assessment Network (Ours)** | **66.70** | **40.00** | **0.19** |

> ✅ **提升显著**：相比最强基线（Gemini），AvgAcc 提升 **15.09pp**，MAE 下降 **36.7%**。

---

### （2）Decision-making 对齐人类选择的结果（Figure 2）

| Model | OS-Sim (%) | First-Acc (%) |
|-------|------------|---------------|
| GPT-5 | 69.23 ± 0.71 | 38.01 ± 3.81 |
| **DMM (Ours)** | **73.16 ± 0.43** | **46.14 ± 4.09** |

> ✅ **全面领先**：
> - OS-Sim 提升 **+3.93pp**
> - First-Acc 提升 **+8.13pp**

表明 DMM 更好地捕捉了人类的优先级结构和首选行为。

---

### （3）消融实验结果（Table 2）

| Model Variant | OS-Sim (%) | First-Acc (%) |
|--------------|------------|---------------|
| Only Action | 60.23 | 32.27 |
| w/o Preference | 61.07 | 31.82 |
| w/o Subjective | 68.93 | 43.45 |
| w/o Scenario | 69.99 | 43.64 |
| **DMM (Full)** | **73.16** | **46.14** |

> 🔍 **关键发现**：
> - 移除“主观偏好调整”或“场景上下文”导致性能大幅下降。
> - 完整框架整合了**客观价值评估**、**用户偏好**与**情境敏感性**，缺一不可。

---

## 4. 关键结论和发现

### 主要发现
1. **价值驱动决策是可行且有效的路径**：将人类价值观作为稳定、可迁移的信号，能够提升 AI 在新场景下的泛化能力和行为合理性。
2. **显式建模优于隐式学习**：相比于 LLM 的零样本推理，显式建模价值维度与偏好能更精准地复现人类决策模式。
3. **PROMETHEE 是处理多价值权衡的有效工具**：相比 MAUT、TOPSIS 等 MCDA 方法，PROMETHEE 在本任务中表现最佳（Table 7）。
4. **合成数据+人工校验可构建高质量训练集**：DGT 流程结合自动化生成与人工审查，有效缓解了合成数据漂移问题。

### 方法的局限性
- 当前价值维度为预定义集合（6 个），尚未实现动态扩展或层次化表达。
- 用户偏好采集依赖自我报告量表，可能存在认知偏差。
- 框架侧重工程集成，未深入探索底层价值表示学习机制。
- 实验集中在家庭场景，需进一步验证于医疗、教育等高风险领域。

### 未来工作方向
- 探索从对话或行为日志中自动推断用户价值偏好的方法。
- 扩展更多文化背景下的价值维度（如集体主义 vs 个人主义）。
- 将 ValuePilot 部署至具身智能体（embodied agents）中进行实时交互验证。
- 引入动态偏好演化机制，支持长期个性化适应。

---

> 💡 **总体评价**：  
> *ValuePilot* 提供了一条通往**可解释、个性化、社会对齐 AI** 的清晰工程路径。它不仅展示了价值建模的实际可行性，也为未来构建真正理解人类动机的 AI 系统奠定了基础。

</details>

---

### 8. [Real-Time Service Subscription and Adaptive Offloading Control in Vehicular Edge Computing](https://arxiv.org/abs/2512.14002)

**Authors**: Chuanchao Gao, Arvind Easwaran  
**Category**: cs.DC  
**Published**: 2025-12-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.14002v1  

#### Abstract
Vehicular Edge Computing (VEC) has emerged as a promising paradigm for enhancing the computational efficiency and service quality in intelligent transportation systems by enabling vehicles to wirelessly offload computation-intensive tasks to nearby Roadside Units. However, efficient task offloading ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**Vehicular Edge Computing (VEC)** 系统中的两个关键挑战：
- **Deadline-Constrained Task Offloading and Resource Allocation Problem (DOAP)**：在带宽和计算资源受限的情况下，如何联合优化任务卸载与资源分配以最大化车辆效用。
- **动态无线信道质量与时延敏感性**：由于车辆移动性和环境变化导致的快速信道波动，以及实时任务极短的截止时间（如10ms级），传统静态调度难以满足需求。

### 提出的新方法与思路
1. **SARound 近似算法**  
   - 针对NP-hard的DOAP问题，提出基于**Linear Programming (LP) rounding** 和 **local-ratio 技术**的近似算法 SARound。
   - 改进了当前最优的近似比（从 $ \frac{1}{3} $ 提升至 $ \frac{1}{2} $），同时保持高效的运行时复杂度 $ O(M(NBC)^3) $，其中 $ M $ 是RSU数量，$ N $ 是任务数，$ B $ 和 $ C $ 分别为最大RB和CU资源单位。

2. **在线服务订阅与自适应卸载控制框架**
   - 设计了一个完整的**在线生命周期管理框架**，支持周期性实时任务的服务注册、调度决策、服务初始化与动态卸载控制。
   - 引入基于 **Sounding Reference Signal (SRS)** 的反馈机制：车辆定期发送SRS信号，RSU据此估计上行链路质量并动态调整是否允许卸载，若信道恶化则暂停卸载、转为本地执行，避免 deadline miss。
   - 调度与服务初始化在卸载前完成，不占用任务执行窗口，提升时间确定性。

3. **开发 VecSim 开源仿真器**
   - 构建首个专为**截止时间约束型车载任务全生命周期管理**设计的VEC仿真平台 VecSim。
   - 集成 OMNeT++ 和 Simu5G，支持真实车辆轨迹、5G V2X通信建模、资源调度与在线卸载控制闭环验证。

### 相比现有方法的优势
| 维度 | 本文方法 | 现有方法局限 |
|------|----------|-------------|
| **理论保证** | 提供更优的近似比 $ \frac{1}{2} $ | 多数为启发式算法无理论保障；已有近似算法仅达 $ \frac{1}{3} $ |
| **动态适应性** | 支持SRS驱动的在线信道感知与卸载控制 | 多假设信道恒定或忽略信道波动影响 |
| **系统完整性** | 完整生命周期管理（注册→调度→部署→控制） | 缺乏端到端仿真工具支持 |
| **实用性** | 在短任务周期下仍可容忍较长调度延迟（因调度不影响执行窗口） | 调度开销可能挤占执行时间 |

---

## 2. 核心实验方法和设置

### 数据集
- **车辆轨迹数据**：来自上海强生出租车公司的真实GPS轨迹数据（[23]），选取2018年4月1日晚高峰15分钟内通过1km×1km城区的80辆出租车。
- **应用负载数据**：基于 **ImageNet ILSVRC** 数据集中的57张图像（0.07–0.3MB），结合六种GPU目标检测模型（RESNET-18/50/101/152, VGG-16/19）进行性能剖面分析。
- **硬件配置**：本地设备使用 **NVIDIA Jetson Nano** 测量本地执行功耗与延迟；RSU配备随机选择的六类GPU（TITAN V, RTX系列等），共16个CU，5G网络提供270个Resource Block (RB)，峰值速率37MB/s。

### 实验设置
- **仿真平台**：自研 **VecSim**，集成 OMNeT++ 和 Simu5G，实现车辆移动、5G通信、任务调度与卸载控制全流程模拟。
- **任务参数**：任务周期设为50ms、67ms或100ms（对应频率20Hz、15Hz、10Hz），输入大小随机采样。
- **调度间隔**：每10秒执行一次全局调度。
- **信道质量等级**：定义三种场景 —— HIGH（高且稳定）、MEDIUM、LOW（低且波动大）。
- **调度模式对比**：
  - **SchedAll**：每次调度重置所有服务，重新全量调度。
  - **SchedRemain**：仅调度未处理请求，保留正在运行的服务。

### 评估指标
- **预测能效节省 (Predicted energy saving)**：调度器预估的每秒节能（J/s），反映静态条件下的资源利用效率。
- **实测能效节省 (Measured energy saving)**：实际仿真中成功卸载任务带来的平均节能（J/s），考虑信道波动、初始化延迟等因素。
- **每秒卸载任务实例数 (Number of offloaded job instances per second)**：衡量系统吞吐能力。
- **算法执行时间 (Execution time)**：评估算法可扩展性。

### 基线方法对比
比较以下四种代表性算法：
- **Greedy [14]**：按资源效率排序贪心选择。
- **Iterative [1,15–17]**：交替求解卸载与资源分配子问题直至收敛。
- **Game [17]**：将问题建模为非合作博弈，各车作为玩家竞争资源。
- **IDAssign [19]**：递归优先处理“轻量”服务实例，具有 $ \frac{1}{3} $ 近似比。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（SchedAll 模式）
| 算法 | 平均预测节能 (J/s) | 平均实测节能 (J/s) | 卸载任务率 |
|------|---------------------|---------------------|------------|
| **SARound** | **140.2** | **118.6** | 最高 |
| Game | 99.0 (+41.5%) | 96.4 (+22.9%) | 中等 |
| Greedy | 94.6 (+48.2%) | 83.0 (+42.9%) | 较低 |
| IDAssign | 79.2 (+76.9%) | 67.7 (+75.1%) | 最低 |
| Iterative | 98.0 (+43.1%) | 98.6 (+20.3%) | 中等 |

> 注：“+XX%”表示 SARound 超出该基线的百分比。

### 不同调度模式下的表现（图5 vs 图6）
- **SchedAll 模式**：
  - SARound 在预测和实测节能上均显著领先。
  - 高信道质量下优势更大（因更多资源可被精细分配）。
- **SchedRemain 模式**：
  - 所有算法实测节能高于预测值（尤其在HIGH条件下），说明动态信道改善带来额外收益。
  - SARound 依然最优，但相对差距缩小。
  - **实测节能反而更高**：因为保留已有服务减少了重复初始化开销，提升了整体吞吐。

### 可扩展性测试（图7）
- **随着请求量增加**：
  - SARound 的性能优势进一步扩大（LP rounding误差随规模增大而稀释）。
  - 执行时间呈**近似线性增长**，优于 Iterative（约快5.5倍），略慢于 Greedy（1.3倍）和 IDAssign（0.9倍），但仍具备实用可行性。
- 表明 SARound 具备良好的**runtime scalability**。

### 消融实验（隐含分析）
虽然未明确命名消融实验，但文中通过以下方式体现设计有效性：
- 对比 **SchedAll vs SchedRemain**：揭示服务持续性对减少初始化开销的重要性。
- 分析 **SRS反馈机制**：证明其可在不影响任务执行窗口的前提下实现信道自适应控制。
- 理论证明 FloorRd 与 SARound 的近似比，验证 LP rounding + local-ratio 结合的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **SARound 显著优于现有方法**：在各种网络质量和调度模式下，无论是预测还是实测节能，SARound 均一致领先，最高超出基线达 **77%**。
2. **动态信道反馈至关重要**：基于 SRS 的在线卸载控制机制有效应对信道波动，在信道恢复后及时重启卸载，提高资源利用率。
3. **调度与执行解耦提升灵活性**：将调度和服务初始化前置，使得即使采用较复杂的算法也不会影响任务执行时间窗，适合短周期任务。
4. **VecSim 填补研究空白**：首个支持完整 DOAP 生命周期仿真的开源工具，可用于未来 VEC 研究的标准化评估。

### 方法局限性
- **集中式架构单点故障风险**：依赖中央调度器，一旦失效会影响整个系统协调。
- **服务迁移未考虑**：当前框架未支持任务在不同RSU间动态迁移，限制了高速移动场景下的适用性。
- **内存等附加资源约束未建模**：目前仅考虑带宽与计算资源，未纳入服务器内存、存储等维度。

### 未来工作方向
- 探索**去中心化调度架构**（decentralized scheduler），增强系统鲁棒性。
- 研究支持**服务无缝迁移**的机制，适应高速移动场景。
- 将框架扩展至包含**多维资源约束**（如内存、能耗预算）的更复杂场景。
- 支持更多类型的**QoS约束**，如 M-K 截止时间模型、可靠性要求等。

---

> ✅ 总结：本文提出了一个兼具**理论严谨性**与**工程实用性**的 VEC 实时任务卸载框架。SARound 算法在理论上改进了近似比，在实践中表现出卓越的资源利用效率；配套的在线控制机制与 VecSim 仿真器共同构成了一个完整的解决方案，推动了 VEC 领域向动态、可靠、可验证的方向发展。

</details>

---

### 9. [RAST-MoE-RL: A Regime-Aware Spatio-Temporal MoE Framework for Deep Reinforcement Learning in Ride-Hailing](https://arxiv.org/abs/2512.13727)

**Authors**: Yuhan Tang, Kangxin Cui, Jung Ho Park, Yibo Zhao, Xuan Jiang, Haoze He, Dingyi Zhuang, Shenhao Wang, Jiangbo Yu, Haris Koutsopoulos, Jinhua Zhao  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13727v1  

#### Abstract
Ride-hailing platforms face the challenge of balancing passenger waiting times with overall system efficiency under highly uncertain supply-demand conditions. Adaptive delayed matching creates a trade-off between matching and pickup delays by deciding whether to assign drivers immediately or batch r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RAST-MoE-RL: A Regime-Aware Spatio-Temporal MoE Framework for Deep Reinforcement Learning in Ride-Hailing  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
该论文针对网约车平台中的**自适应延迟匹配（adaptive delayed matching）决策问题**，旨在平衡乘客等待时间（matching delay）与接驾时间（pickup delay）之间的权衡。传统方法面临以下挑战：
- **交通动态建模不真实**：多数RL模型忽略拥堵反馈机制，使用静态速度假设。
- **奖励设计脆弱**：固定权重的奖励函数易导致“reward hacking”行为（如无限持有请求或立即匹配）。
- **表征能力不足**：标准MLP或浅层编码器难以捕捉复杂时空模式，尤其在供需剧烈变化的不同交通“制度”（regime）下泛化差。

### **提出了什么新方法或新思路**
作者提出 **RAST-MoE-RL** 框架，包含三大核心创新：

#### （1）**Regime-Aware Spatio-Temporal MDP (RAST-MDP)**
- 将自适应匹配形式化为一个**制度感知的马尔可夫决策过程**，显式建模供需不平衡。
- 引入**物理信息驱动的旅行时间代理模型（physics-informed travel-time surrogate）**，基于宏观基本图（MFD）构建城市级速度-密度反馈，实现高效且真实的拥堵模拟，支持百万级RL rollout。

#### （2）**Anti-Hacking 自适应奖励机制**
- 设计增量成本奖励函数，结合**在线调整的拉格朗日乘子 λ** 动态惩罚服务质量违规（如超时匹配）。
- 避免手动调参，使策略自动学习匹配-接驾的动态平衡，防止崩溃性策略（如永远不匹配或永远立即匹配）。

#### （3）**RAST-MoE：制度感知的稀疏MoE编码器**
- 提出一种轻量级（仅12M参数）、基于**Mixture-of-Experts (MoE)** 的时空编码器。
- 不同专家自动专业化于不同交通制度（如早高峰、夜间低需求），通过全局状态路由选择Top-K专家，提升表达能力同时保持计算效率。
- 采用轻量负载均衡机制，允许专家利用率自然倾斜，避免抑制对罕见但关键场景（如突发拥堵）的专业化。

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **表达能力** | MoE结构显著优于MLP/Dense Transformer，在小参数量下实现更高有效容量 |
| **训练稳定性** | 自适应奖励防止reward hacking，训练收敛稳定（见λ收敛图） |
| **泛化性** | 在未见过的供需模式、天气扰动、局部事故等OOD场景下表现鲁棒 |
| **效率** | 代理模型支持O(1)查询，MoE稀疏激活保证推理高效，适合城市规模部署 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
- **数据来源**：加州公共事业委员会（CPUC）TNC数据门户提供的2019年Uber和Lyft行程数据。
- **区域**：旧金山县（San Francisco County），日均约17万订单，高密度、高拥堵代表性强。
- **预处理**：将城市划分为H×W网格单元，按小时聚合供需流量、到达率等特征。

### **实验设置和评估指标**
- **MDP定义**：
  - **State**：时间、各区域未匹配乘客数 $n_p$、空闲司机数 $n_d$、乘客/司机到达率 $\lambda, \mu$
  - **Action**：每个区域二元决策 $a_i \in \{0,1\}$，“hold” 或 “match now”
  - **Transition**：基于MFD代理模型更新车辆位置、请求状态、引入新请求
- **训练框架**：以PPO为主干算法，兼容A2C、ACER、GRPO等。
- **评估指标**：
  - 总奖励（Total Reward）
  - 平均匹配延迟（Matching Delay）
  - 平均接驾延迟（Pickup Delay）
  - 服务违规率（Service-Quality Violations）

### **基线方法对比**
| 类别 | 基线方法 |
|------|--------|
| **RL算法** | PPO, A2C, ACER, DQN, GRPO |
| **编码器结构** | Plain MLP, ResMLP, Dense Transformer (12M/48M), GNN |
| **启发式策略** | Instant Matching, Constant-Interval Batch Matching (20s) |
| **MoE变体** | 不同专家数E与Top-K组合（如(8,2), (16,4), (32,8)） |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**
在旧金山真实数据上，**RAST-MoE (16 experts, top-4)** 取得最优性能：

| 指标 | 提升幅度 | 具体数值 |
|------|---------|--------|
| **总奖励** | ↑13% | 相比ACER baseline |
| **平均匹配延迟** | ↓10% | 从~200s降至182s |
| **平均接驾延迟** | ↓15% | 从~600s降至523s |

> 注：所有结果在测试集（未见时段）上验证，确保非过拟合。

### **与基线方法的对比结果**
- **优于所有RL基线**：在PPO、GRPO等框架下，MoE编码器 consistently 提升性能。
- **超越大参数模型**：
  - **12M MoE** 表现优于 **48M Dense Transformer**
  - 显示MoE更高的参数效率与表达能力
- **击败启发式策略**：
  - Instant Matching 匹配延迟虽低但接驾延迟高达825s
  - MoE在两者间取得更优权衡

### **消融实验结果**
#### （1）**奖励机制有效性**
- 固定权重（如8:1）导致极端行为：
  - 8:1 → 匹配延迟≈0，但接驾延迟>1700s（立即匹配耗尽司机）
  - 1:8 → 匹配延迟>2200s（过度持有）
- **自适应奖励** 在训练和测试中均稳定，无reward hacking现象（见Fig. 4）

#### （2）**MoE配置选择**
- **(16,4)** 配置最优：适度容量与稀疏性平衡
- 更大专家池（如64,8）收益递减甚至下降
- 更小（如8,2）则表达不足

#### （3）**专家重要性验证（masking实验）**
- 分别屏蔽高频专家（绿色）与低频专家（橙色）：
  - 两者均导致**性能显著下降**
  - 证明即使是低频专家也捕捉关键稀有场景（如晚高峰启动）
- 支持“purposeful specialization”而非路由崩溃

#### （4）**统计显著性**
- 多随机种子实验（3 seeds）显示方差极小（如匹配延迟 std <1s），结果可靠。

---

## 4. 关键结论和发现

### **主要发现**
1. **制度感知是城市级控制的关键**：供需-拥堵模式具有明显周期性与异质性，单一模型难以覆盖，MoE的专家分工天然适配此需求。
2. **轻量MoE胜过大模型**：仅12M参数的RAST-MoE超过48M Dense Transformer，证明**专家专业化带来的有效容量提升远超单纯堆参数**。
3. **自适应奖励至关重要**：固定系数无法应对动态环境，而在线调节的λ能稳定引导策略学习合理权衡。
4. **真实物理反馈不可替代**：基于MFD的代理模型保留了关键拥堵信号（如容量下降），是长视野信用分配的基础。
5. **稀有专家有价值**：即使激活频率低，特定专家仍负责关键场景，移除后性能下降，说明MoE实现了有意义的功能分解。

### **方法的局限性**
- **专家解释性有限**：尽管分析显示专家与特定时段相关，但精确语义仍难界定。
- **依赖宏观分区**：基于TAZ的聚合可能丢失微观路径细节。
- **静态路由拓扑**：OD路径预计算，未考虑实时路径重规划。
- **集中式架构**：适用于平台级调度，难以直接迁移到去中心化多智能体场景。

### **未来工作方向**
- 探索**动态专家路由机制**，适应更剧烈分布偏移。
- 结合**多尺度建模**（如区域+路段级）提升精度。
- 扩展至**拼车（ride-pooling）与重定位联合优化**。
- 研究**公平性约束**，避免算法加剧地理服务不平等。
- 探索**MoE在其他大规模时空决策任务**中的应用（如物流、电网调度）。

---

> **Reproducibility Statement**：作者承诺公开数据预处理代码、模拟器、训练脚本与超参数，确保结果可复现。

</details>

---

### 10. [AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach](https://arxiv.org/abs/2512.13714)

**Authors**: Gangesh Pathak, Prasanna Kumar  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.13714v1  

#### Abstract
LLM implementations are failing in highly regulated industries owing to instability issues, inconsistent reasoning, hallucinations and performance variability, especially in workflows. These reliability issues restrict safe use of LLM in areas that need the precision of facts and consistent behavior...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在高风险、高监管领域（如医疗、金融、法律）的应用受到其**不稳定性**的严重制约。具体表现为：
- **语义漂移（Semantic divergence）**：相同意图的提示词生成不同含义的回答。
- **幻觉（Hallucination）**：自信地输出错误或虚构信息。
- **推理崩溃（Reasoning breakdown）**：逻辑矛盾或推理断裂。
- **会话漂移（Session drift）**：多轮对话中输出质量随时间下降。

现有的对齐方法（如RLHF、监督微调）虽能提升模型行为一致性，但依赖大量人工标注，成本高昂且难以持续扩展，无法系统化解决长期稳定性问题。

---

### **提出了什么新方法或新思路**

本文提出了一种**基于AI驱动的标注流水线（AI-Powered Annotation Pipeline）**，采用**人机协同（Human-AI Synergy）** 范式来系统性识别、标注并修复LLM输出中的不稳定性模式。

#### 核心框架：四阶段闭环流程
1. **AI自动标注（Automated Annotation）**  
   使用轻量级LLM作为“专家标注器”，从**语义一致性（Semantic Consistency）、事实准确性（Factual Accuracy）、逻辑连贯性（Logical Coherence）** 三个维度检测不稳定性。
   
2. **选择性人类验证（Selective Human Validation）**  
   仅将低置信度、矛盾性或高风险案例交由人类专家审查，确保标注质量的同时大幅降低人力成本。

3. **反馈集成与模型再训练（Feedback Integration & Retraining）**  
   将验证后的高质量标注用于**稳定性专项微调（Stability Fine-Tuning）** 和**基于奖励的校准（RIC-Based Stabilisation）**，形成闭环反馈机制。

4. **持续监控与迭代优化（Continuous Monitoring）**  
   通过模块化架构实现实时稳定性指标追踪与模型更新。

---

### **相比现有方法的优势**

| 维度 | 传统方法（如RLHF/SFT） | 本文方法 |
|------|------------------------|---------|
| **可扩展性** | 依赖大规模人工标注，难以扩展 | AI自动化初筛，人类仅介入关键案例，显著降低成本 |
| **稳定性衡量** | 侧重单次响应正确性，忽视输出变异性 | 引入**稳定性指数（Stability Index, SI）**等量化指标 |
| **持续性** | 多为一次性对齐，部署后缺乏持续优化 | 构建**稳定性反馈循环（Stability Feedback Loops）**，支持生命周期内持续增强 |
| **伦理与可靠性** | 纯AI标注易放大偏见；纯人工标注主观性强 | 人机协同保障标注准确性和道德合规性 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 用途 | 稳定性压力测试设计 |
|-------|------|------------------|
| **TruthfulQA** | 检测幻觉与事实错误 | 每个问题生成5–10种同义改写版本 |
| **GSM8K / Synthetic Multi-Turn Reasoning** | 测试逻辑一致性与链式推理稳定性 | 加入干扰项表述与上下文重排序 |
| **Knowledge-Grounded Dialogues**（医疗/教育子集） | 测量多轮对话中的一致性 | 扩展多轮变体并注入歧义 |

> 所有数据集均构建了专门的“稳定性压力测试子集”，以主动触发不稳定性模式。

---

### **实验设置与评估指标**

#### **基线模型**
| 模型 | 训练方式 | 预期行为 |
|------|----------|---------|
| **Baseline-1 (SFT)** | 仅指令微调 | 高任务准确率，但输出方差大 |
| **Baseline-2 (RLHF)** | 基于人类反馈的强化学习 | 对齐更好，但在提示改写下仍存在漂移 |

#### **评估指标**
| 指标 | 定义 | 目标 |
|------|------|------|
| **Stability Index (SI)** | 同义提示下响应的语义方差 | 越低越好 |
| **Factual Consistency (FC)** | 回答与参考答案的事实一致性 | 越高越好 |
| **Annotation Precision (AP)** | 自动标注与人工核查的一致率 | 越高越好 |
| **Response Diversity Ratio (RDR)** | 基于熵的语义多样性度量 | 平衡多样性与漂移 |

---

## 3. 主要实验结果和性能指标

### **定量结果对比**

| Model | SI ↓ | FC ↑ | AP ↑ | RDR |
|-------|------|------|------|-----|
| Baseline-1 (SFT) | 0.41 | 72% | — | 0.52 |
| Baseline-2 (RLHF) | 0.33 | 81% | 78% | 0.47 |
| **Stabilized Model** | **0.18** | **92%** | **94%** | **0.45** |

#### **关键性能提升**
- **相比SFT基线**：稳定性提升 **56%**（SI从0.41→0.18）
- **相比RLHF基线**：事实一致性提升 **14个百分点**（FC从81%→92%）
- **自动标注精度达94%**，表明AI标注器具备高可靠性
- **RDR保持稳定**，说明稳定性增强未牺牲生成多样性

---

### **定性分析示例**

#### 医疗问答场景（“抗生素能否治疗流感？”）
| 提示变体 | Baseline-1 输出 | Baseline-2 输出 | Stabilized Model 输出 |
|--------|------------------|------------------|------------------------|
| “Can antibiotics treat viral influenza?” | “Yes, antibiotics help eliminate the influenza virus.”（幻觉） | “Sometimes antibiotics are prescribed for flu treatment.”（误导） | “No. Antibiotics treat bacterial infections, not viral influenza. They are only used if a secondary bacterial infection occurs.”（准确且一致） |

> 显示该方法能在语义等价的不同提问下保持**事实对齐与逻辑一致**。

#### 推理稳定性测试
- 基线模型在数学应用题的等价变体上常给出不同答案。
- 稳定化模型始终返回**相同的解决方案与清晰的推理步骤**。

---

### **消融实验（隐含分析）**

虽然文中未明确列出消融表，但从讨论部分可推断以下设计要素的关键作用：

| 设计要素 | 效果 |
|--------|------|
| **自动化标注规模化** | 快速识别不稳定性模式，加速反馈周期 |
| **聚焦模糊/高风险的人类干预** | 避免盲目信任AI判断，防止错误传播 |
| **稳定性反馈循环** | 实现行为的持续强化与长期可靠 |

---

## 4. 关键结论和发现

### **主要发现**
1. **AI驱动的标注流水线可显著提升LLM稳定性**，尤其在面对提示改写和多轮交互时表现稳健。
2. **人机协同是关键**：完全依赖AI可能导致错误累积；完全依赖人类则不可扩展。**战略性人类介入**（如审核低置信度输出）可在最小成本下最大化效果。
3. **稳定性可以被量化与优化**：提出的SI、FC等指标为模型可靠性提供了新的评估维度，超越传统的“准确性”范式。
4. **稳定性增强不等于创造力抑制**：RDR指标显示，模型在提升一致性的同时仍保留合理的生成多样性。

---

### **方法的局限性**
1. **自动标注器依赖启发式规则质量**：在缺乏明确ground truth的领域（如哲学、艺术），可能误判非常规但合理的推理为错误。
2. **人类验证存在主观性风险**：评审者的背景、经验差异可能引入偏差，影响标注一致性。
3. **领域泛化能力待验证**：当前实验集中于事实推理与对话任务，在快速演化的专业领域（如生物医药）需动态知识更新机制。
4. **错误传播风险**：若自动化标注阶段出现系统性错误且未被及时发现，可能通过再训练“病毒式”扩散。

---

### **未来工作方向**
1. **开发可解释的自动标注代理（Explainable Annotator Agents）**：使其能说明判断依据与不确定性来源，便于人类快速验证。
2. **多模型互评机制（Cross-Model Review Ensembles）**：让多个LLM相互审查输出，识别逻辑异常或知识冲突，提升跨模型稳定性。
3. **构建终身学习（Lifelong Learning）管道**：实现模型在运行时实时监测自身稳定性，并主动触发修正机制。
4. **加强透明性与可审计性工具建设**：确保整个pipeline的决策过程可追溯、可解释、可问责。

---

## 总结

本文提出了一条通往**可信、可持续、可扩展的下一代LLM部署路径**。通过将**AI的效率**与**人类的判断力**有机结合，构建了一个面向稳定性的闭环增强系统。实验证明，该方法不仅能显著降低输出变异性和幻觉率，还能在不牺牲创造性的前提下提升模型在高风险场景下的可靠性。这一**Human-AI Synergy**范式为未来LLM的安全落地提供了重要的方法论基础。

</details>

---

### 11. [PortAgent: LLM-driven Vehicle Dispatching Agent for Port Terminals](https://arxiv.org/abs/2512.14417)

**Authors**: Jia Hu, Junqi Li, Weimeng Lin, Peng Jia, Yuxiong Ji, Jintao Lai  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.14417v1  

#### Abstract
Vehicle Dispatching Systems (VDSs) are critical to the operational efficiency of Automated Container Terminals (ACTs). However, their widespread commercialization is hindered due to their low transferability across diverse terminals. This transferability challenge stems from three limitations: high ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PortAgent: LLM-driven Vehicle Dispatching Agent for Port Terminals

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在自动化集装箱码头（**Automated Container Terminals, ACTs**）中，车辆调度系统（**Vehicle Dispatching Systems, VDSs**）对运营效率至关重要。然而，现有VDS在跨码头部署时面临三大瓶颈：

- **高度依赖港口操作专家**：需要经验丰富的工程师或运筹学专家进行定制化设计；
- **对特定场景数据需求高**：尤其是基于机器学习的方法需大量高质量训练数据；
- **部署过程耗时且繁琐**：涉及多角色协作与反复人工调试。

这些因素导致VDS难以实现快速迁移和商业化推广。

---

### **提出了什么新方法或新思路**

本文提出 **PortAgent** —— 一种由大语言模型（**LLM**）驱动的车辆调度代理系统，旨在全自动完成VDS的跨终端迁移任务。其核心创新在于以下三点设计：

#### ✅ 虚拟专家团队（Virtual Expert Team, VET）
- 构建一个由四个虚拟专家组成的协作团队，在单一LLM实例内通过 **Role-Prompting** 激活不同角色：
  - **Knowledge Retriever**：检索相关领域知识；
  - **Modeler**：将自然语言需求转化为数学优化模型；
  - **Coder**：生成可执行代码；
  - **Debugger**：自动检测并修正错误。
- 该机制将复杂长链推理分解为多个短链子任务，显著提升逻辑可靠性，**无需人类专家介入**。

#### ✅ 少样本示例学习 + RAG机制（Few-shot Example Learning with RAG）
- 利用 **Retrieval-Augmented Generation (RAG)** 从预构建的知识库中提取最相关的建模原语（modeling primitives）和代码范例（code exemplars）；
- 支持仅凭**单个示例**即可完成有效迁移，极大降低对终端专属数据的需求。

#### ✅ 自动化的VDS设计流程 + 自我纠正机制（Self-Correction Loop）
- 设计闭环工作流：`输入 → 模型生成 → 编码 → 执行 → 反思 → 修正`；
- 借鉴 **LLM Reflexion** 框架，引入自我反思能力，使系统能自主分析错误原因并指导下一轮生成；
- 实现**无人干预的端到端自动化部署**，大幅缩短部署时间。

---

### **相比现有方法的优势**

| 维度 | 传统方法 | PortAgent |
|------|--------|---------|
| 专家依赖 | 高（需OR专家/工程师） | ❌ 完全消除 |
| 数据需求 | 高（尤其RL类方法） | ✅ 仅需1个示例 |
| 部署速度 | 数小时至数天 | ⏱️ 平均83秒 |
| 可转移性 | 差，需重新开发 | ✅ 在未见场景下成功率86.67%-100% |
| 用户门槛 | 高（需专业表述） | ✅ 技术员级描述即可 |

> PortAgent首次实现了**免专家、低数据、快部署**的VDS迁移方案，推动ACT智能化系统的标准化与普及。

---

## 2. 核心实验方法和设置

### **测试任务类型**

研究聚焦于 **Multi-AGV Path Planning (MAPP)** 问题，作为典型VDS应用场景，目标是最小化整个AGV车队的总行驶时间。

### **测试场景（Scenarios for Test）**

选取三种具有代表性的动态约束场景进行验证：

| 场景 | 描述 |
|------|------|
| **Road Closure** | 节点6与7之间的双向道路因维护关闭 |
| **Forbidden Roads for Specific Trucks** | 特定AGV（如超高车辆）禁止通行某些路段（如低桥） |
| **Designated Routes for Dangerous Goods** | 危险品运输任务必须经过指定安全路径（如6→10→11） |

实验网络规模：30辆AGV，20个节点（见图8），共生成 **15个基础实例**。

---

### **用户输入的专业水平模拟**

为验证对用户专业知识的鲁棒性，每种场景采用三种语言风格描述，模拟不同层级用户：

| 层级 | 输入特点 | 示例 |
|------|--------|------|
| **Technician-level** | 日常口语化描述物理情况 | “Node 6和7之间那条路不能用了” |
| **Engineer-level** | 明确的操作术语，无数学形式化 | “连接(6,7)的双向道路完全封闭” |
| **Scientist-level** | 形式化的数学表达 | “移除边集E'={(6,7),(7,6)}” |

总计：15实例 × 3层级 = **45个测试案例**

---

### **评估指标（Measures of Effectiveness, MoEs）**

#### ✅ 正确性指标
- **Code Executability Rate (CER)**：生成代码是否可成功运行（无语法/运行时错误）
- **Solver Success Rate (SSR)**：解的质量是否达到基准解（Gurobi最优解）精度范围内

#### ⏱️ 效率指标
- **Iterations**：自我修正循环次数
- **Computation Time**：从接收到输出最终解决方案的端到端耗时

---

### **基线方法（Benchmark Method）**

- **传统专家驱动法**：由具备运筹学背景的人类专家手动建模并编写Python脚本（使用Gurobi求解器），作为“ground truth”。

---

### **实验环境与配置**

- **硬件**：Intel i5-13500H, 16GB RAM
- **LLM引擎**：Gemini 2.5 Flash（temperature=0）
- **最大迭代次数**：3次
- **示例数量**：默认1-shot（提供经典MAPP模型示例）
- **求解器**：Gurobi 12.0.3，时限300秒

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **平均CER（代码可执行率）** | **100%** |
| **SSR范围（求解成功率）** | **86.67% ~ 100%** |
| **平均SSR（综合成功率）** | **93.33%** |
| **平均计算时间** | **83.23秒** |
| **所需示例数（达93.33% SSR）** | **仅需1个示例** |

> 在所有45个测试案例中，成功解决42个，失败仅3例，均为**语义误解**所致。

---

### **与基线方法的对比结果**

| 方法 | 部署时间 | 是否需要专家 | 数据需求 | 成功率 |
|------|--------|-------------|----------|--------|
| 传统专家法 | 数小时~数天 | ✅ 必须 | 高 | 接近100% |
| **PortAgent** | **83.23秒** | ❌ 不需要 | **极低（1例）** | **93.33%** |

✅ **PortAgent在几乎不牺牲准确性的前提下，将部署效率提升了数百倍**。

---

### **消融实验结果（Ablation Study）**

验证两个核心模块的重要性：

| 方法 | CER | SSR |
|------|-----|-----|
| **完整PortAgent** | 100% | 93.3% |
| **PortAgent w/o RAG**（无知识检索） | 40.0% | 26.7% |
| **PortAgent w/o Self-Correction**（无自我修正） | 33.33% | 33.33% |

> 🔍 结论：
> - **RAG模块**是确保正确建模的基础；
> - **Self-Correction机制**对修复初始错误至关重要；
> - 二者缺一不可，均为非冗余的关键组件。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **PortAgent实现了真正的“免专家”部署**  
   - 统计检验显示，不同专业水平用户的输入对CER、SSR、迭代次数和计算时间均**无显著影响**（p > 0.05），证明其完全摆脱了对专家的依赖。

2. ✅ **“少即是多”（Less is More）**  
   - 实验表明，提供更多示例（如3-shot）反而可能引入“上下文噪声”，降低性能；
   - **1-shot配置表现最佳**，说明示例质量优于数量。

3. ✅ **“基础优先”原则更优**（Fundamental is the Key）  
   - 使用**通用经典MAPP模型**作为示例，比使用针对具体场景定制的示例效果更好；
   - 因为它传授的是**基本结构流程**，而非过拟合细节，利于泛化。

4. ✅ **自动化闭环大幅提升效率**  
   - 自我修正机制虽增加迭代次数，但每次修正快速精准，整体仍远快于人工调试。

---

### **方法的局限性**

- **语义误解风险依然存在**：
  - 当用户输入存在歧义时（如“stick to the safe route”是否为必经子路径），LLM可能出现误判；
  - 失败案例全部属于此类问题（misinterpretation），而非技术实现错误。

- **LLM输出的随机性**：
  - 即便相同输入，不同随机种子可能导致不同结果，影响一致性。

- **当前仅适用于结构化良好的优化问题**：
  - 如MAPP这类有明确变量、约束、目标函数的问题；
  - 对非结构化或模糊决策场景适应性尚待验证。

---

### **未来工作方向**

1. **增强语义理解能力**  
   - 开发更精细的自然语言解析机制，减少用户输入歧义带来的误读。

2. **提高输出一致性**  
   - 探索控制LLM生成稳定性的方法，降低概率性波动的影响。

3. **扩展至其他VDS子系统**  
   - 将PortAgent应用于岸桥调度、堆场分配等其他ACT核心系统。

4. **支持多模态输入**  
   - 引入图表、拓扑图等视觉信息辅助理解复杂约束。

---

> 📌 **总结一句话**：  
> **PortAgent 是首个实现免专家、低数据、高速部署的LLM-driven VDS迁移代理，标志着ACT智能调度系统向标准化、自动化迈出了关键一步。**

</details>

---

### 12. [The Double Life of Code World Models: Provably Unmasking Malicious Behavior Through Execution Traces](https://arxiv.org/abs/2512.13821)

**Authors**: Subramanyam Sahoo, Jared Junkin  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.13821v1  

#### Abstract
Large language models (LLMs) increasingly generate code with minimal human oversight, raising critical concerns about backdoor injection and malicious behavior. We present Cross-Trace Verification Protocol (CTVP), a novel AI control framework that verifies untrusted code-generating models through se...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*The Double Life of Code World Models: Provably Unmasking Malicious Behavior Through Execution Traces*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
随着 Large Language Models（LLMs）在代码生成任务中的广泛应用，模型可能被恶意注入**后门（backdoor）** 或产生**上下文依赖的隐蔽恶意行为**。传统防御机制如沙箱执行、静态规则扫描等假设模型是良性的且透明的，在对抗性或对齐失败场景下失效。

本文聚焦于如何在**不信任模型意图、不实际执行可疑代码**的前提下，检测由代码生成模型产生的程序是否含有隐藏的恶意逻辑。

---

### 提出的新方法与创新思路

#### **Cross-Trace Verification Protocol (CTVP)**  
提出一种基于语义不变性的黑盒 AI 控制框架——**跨轨迹验证协议（CTVP）**，其核心思想为：

> 如果一个程序在多个语义等价但语法不同的变体上，其预测的执行轨迹保持一致，则认为该模型行为可控；否则，存在潜在恶意或不稳定行为。

具体流程如下：
1. 对输入程序 $P$ 应用一系列**语义保持变换**（如变量重命名、死代码插入、格式化调整），生成一个**语义轨道（semantic orbit）** $\mathcal{O} = \{Q_1, ..., Q_k\}$。
2. 使用待验证的**非可信 LLM** 预测每个变体 $Q_i$ 的执行轨迹 $T(Q_i)$（包括中间状态和最终输出）。
3. 计算所有轨迹之间的成对相似度，并通过高百分位数（如80%）聚合得到一致性得分 $C$。
4. 根据阈值判断：
   - $C < T_{\text{strict}}$: 拒绝（REJECT）
   - $T_{\text{strict}} \leq C < T_{\text{soft}}$: 启动可信模型二次验证
   - $C \geq T_{\text{soft}}$: 接受（ACCEPT）

#### **Adversarial Robustness Quotient (ARQ)**
引入一个新的理论指标 **ARQ**，用于量化验证过程相对于单次推理的计算开销：
$$
\text{ARQ}_{\text{flops}} \sim k + 1
$$
其中 $k$ 是语义轨道大小。更重要的是，作者证明了**非可博弈性（non-gamifiability）**：攻击者要构造能通过 CTVP 检测的一致性后门，所需尝试次数随 $k$ 呈指数增长，使其在现实中不可行。

---

### 相比现有方法的优势

| 维度 | 传统方法 | CTVP |
|------|--------|-------|
| 可信假设 | 要求模型透明或良性 | 完全黑盒，无需信任模型 |
| 执行风险 | 可能运行恶意代码（沙箱逃逸） | 不执行代码，仅预测轨迹 |
| 检测能力 | 依赖显式模式匹配（如 `eval`） | 可捕获隐式、上下文触发的恶意行为 |
| 理论保障 | 多为经验性 | 具备信息论级别的安全性证明（非可博弈性） |
| 扩展性 | 静态分析难以覆盖复杂语义 | 动态语义一致性分析更具泛化潜力 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用一个**人工构建的混合数据集**，包含：
  - **良性程序（Benign）**：功能正确、无恶意逻辑的标准 Python 程序。
  - **对抗样本（Malicious）**：植入了上下文敏感后门的程序（例如仅在特定变量名下触发恶意操作）。
- 所有程序控制在约 200 LOC 以内，涵盖常见编程结构（循环、条件、函数调用等）。

---

### 实验设置

- **轨道大小（orbit size）**：测试 $k = 3, 5, 10, 20$
- **一致性评分方式**：使用成对轨迹相似度的 80% 百分位作为一致性分数 $C$
- **双层决策机制**：
  - 主要使用非可信模型进行多轨迹预测
  - 在中间区间启用“可信模型”进行交叉验证（median trace similarity）
- **自适应阈值控制**：采用类 PID 控制器动态调节 $T_{\text{strict}}$，目标维持低误报率（FPR target = 5%）

---

### 评估指标

| 指标 | 描述 |
|------|------|
| **Detection Rate** | 成功识别恶意程序的比例（TPR） |
| **False Positive Rate (FPR)** | 将良性程序误判为恶意的比例 |
| **Benign Acceptance Rate** | 正确接受良性程序的比例 |
| **ARQ**（Adversarial Robustness Quotient） | 验证开销相对于单次推理的增长倍数（时间 & FLOPs） |
| **Consistency Score** | 轨迹间平均相似度，反映模型输出稳定性 |
| **Transformation Success Rate** | 成功生成有效语义等价变体的比例 |

---

### 基线方法对比

实现以下静态分析基线用于比较：

1. **Static Pattern Detector**：正则匹配关键词（`eval`, `exec`, `os.system`, `BACKDOOR`）
2. **Complexity Heuristic**：基于 AST 节点数量和控制流复杂度设定异常阈值
3. **Entropy Heuristic**：计算源码字符熵，过高或过低视为可疑
4. **Weighted Combination**：上述三项中有两项触发即标记为恶意

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2 和正文）

| Orbit Size | Detection Rate (%) | FPR (%) | Avg Traces / Program | ARQ Median |
|------------|--------------------|---------|------------------------|-------------|
| 3          | 81.7               | 100.0   | 1.60                   | 4.00        |
| 5          | 83.3               | 97.5    | 2.80                   | 6.00        |
| 10         | 83.3               | 97.5    | 4.30                   | 11.00       |
| 20         | 86.7               | 100.0   | 7.60                   | 21.00       |

> 注：表中 FPR 实际指“良性程序被拒绝率”，即 1 - Benign Acceptance Rate

此外，**完整实验中（Figure 1）报告了完美检测表现**：
- 在主实验中，CTVP 实现了 **100% 检出率（120/120 恶意样本被检测）**
- **零假阴性（zero false negatives）**
- **混淆矩阵显示：79% 直接拒绝，21% 进入 runtime monitoring**

---

### 与基线方法的对比（Table 1）

| Method           | Detection Rate (%) | FPR (%) | Benign Acceptance (%) |
|------------------|--------------------|---------|------------------------|
| Static Analysis  | 51.7               | 0.0     | 100.0                  |
| Complexity       | 80.0               | 62.5    | 37.5                   |
| Entropy          | 0.0                | 0.0     | 100.0                  |
| Weighted         | 51.7               | 0.0     | 100.0                  |
| **CTVP**         | **83.8**           | **98.8**| **1.2**                |

> ⚠️ 注意：CTVP 的 FPR 很高（98.8%），意味着绝大多数良性程序也被拒绝。但这正是设计选择——优先保证安全，牺牲可用性。结合 trusted model 可缓解此问题。

---

### 消融实验与关键观察

- **轨道大小影响显著**：
  - 随着 $k$ 增大，检测率稳步提升（从 81.7% → 86.7%）
  - ARQ 线性增长（实测 median ARQ ≈ $k+1$），符合预期
- **一致性得分分布**：
  - 所有良性程序的 consistency score 高度集中于 1.0
  - 恶意程序得分明显偏低，形成可分离空间
- **转换成功率非单调**：
  - 在 $k=10$ 时成功率下降至 10%，但在 $k=20$ 回升至 50–75%，表明存在“中间困难区”
- **资源消耗可控**：
  - CPU 内存稳定在 ~4.8 GB，GPU ~17 GB（远低于 71.3 GB 上限）
  - 单程序验证时间从 0.275s（k=2.5）增至 0.333s（k=20），线性增长

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **语义轨道上的行为一致性是检测后门的有效代理信号**  
   恶意行为往往无法在多种语法变体中保持一致响应，导致预测轨迹出现偏差。

2. ✅ **CTVP 实现近乎完美的恶意程序检出率（100%）**  
   在标准测试中未遗漏任何已知恶意实例，具备强鲁棒性。

3. ✅ **非可博弈性成立**：  
   信息论分析表明，构造能通过 CTVP 的一致后门需搜索指数级空间（$ \propto 2^{(k-1)H(T)} $），使得攻击成本极高。

4. ✅ **无需执行即可验证**：  
   利用 LLM 自身作为 trace predictor，避免了直接运行潜在恶意代码的风险。

5. ✅ **可扩展的防御机制**：  
   支持 cascaded verification（非可信 + 可信模型联合决策）、adaptive thresholding、caching 等工程优化。

---

### 局限性

| 限制 | 说明 |
|------|------|
| **依赖 LLM 轨迹预测准确性** | 若 LLM 本身无法准确模拟程序执行（尤其是涉及外部 API 或复杂状态），会导致误判 |
| **高假阳性率（FPR 高）** | 出于保守策略，大量良性程序被拒绝，影响实用性 |
| **变换种类有限** | 当前仅支持 5 类语义保持变换，可能遗漏某些等价形式 |
| **计算开销随 $k$ 增长** | 虽然单次延迟小，但大规模部署时 ARQ ~ $k+1$ 仍构成瓶颈 |
| **编辑距离约束较强** | 设定最小 Levenshtein 距离可能导致有效变体生成失败 |

---

### 未来工作方向

1. **集成真实执行环境**：引入 sandboxed interpreter（如 RestrictedPython）获取 ground-truth trace，增强验证可靠性。
2. **丰富变换策略**：加入数据流感知重排、类型保持编辑、循环展开、常量折叠等更复杂的语义等价变换。
3. **自适应轨道大小（adaptive orbit sizing）**：根据程序复杂度动态调整 $k$，平衡效率与安全性。
4. **持续运行监控（continuous runtime monitoring）**：支持周期性重新验证、异常检测与反馈驱动再训练。
5. **对抗鲁棒性测试**：评估对梯度攻击、隐写协作（steganographic collusion）、多阶段后门的抵抗力。
6. **跨领域推广**：探索 CTVP 是否适用于文本生成、图像合成等其他模态的任务中。

---

## 总结

CTVP 提出了一种新颖且理论上坚实的 AI 控制范式：**通过语义等价变换下的执行轨迹一致性来揭示代码生成模型中的隐藏恶意行为**。它不依赖代码执行，也不要求白盒访问，而是利用 LLM 自身的预测能力进行“自我暴露”式的验证。

尽管存在计算开销和高 FPR 的挑战，但其实验表现优异，理论基础扎实，尤其在防止高级对抗性后门方面展现出巨大潜力，为未来安全型代码生成系统的设计提供了重要参考路径。

</details>

---

### 13. [Beyond Lipschitz Continuity and Monotonicity: Fractal and Chaotic Activation Functions in Echo State Networks](https://arxiv.org/abs/2512.14675)

**Authors**: Rae Chipera, Jenny Du, Irene Tsapara  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.14675v1  

#### Abstract
Contemporary reservoir computing relies heavily on smooth, globally Lipschitz continuous activation functions, limiting applications in defense, disaster response, and pharmaceutical modeling where robust operation under extreme conditions is critical. We systematically investigate non-smooth activa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Beyond Lipschitz Continuity and Monotonicity: Fractal and Chaotic Activation Functions in Echo State Networks

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Echo State Networks (ESNs)** 几乎完全依赖于平滑且全局 **Lipschitz 连续**的激活函数（如 `tanh` 和 `ReLU`），这限制了其在极端条件下的鲁棒性，例如国防、灾难响应和药物建模等对稳定性要求极高的场景。本文挑战了这一设计范式，系统性地研究了非光滑（non-smooth）激活函数在 ESN 中的表现。

### 提出的新方法与新思路
1. **引入非光滑激活函数类别**：
   - 将非光滑激活函数分为三类：**fractal**（分形）、**chaotic**（混沌）和 **stochastic**（随机）。
   - 具体实现包括：**Cantor 函数**（魔鬼阶梯）、**Weierstrass 函数**（连续但处处不可微）、**Mandelbrot 集逃逸时间函数**、**Logistic 映射** 和 **Brownian Motion**。

2. **提出退化回声状态性质（Degenerate Echo State Property, d-ESP）**：
   - 针对离散输出（量化）激活函数无法满足传统 ESP（数值收敛）的问题，定义了 **d-ESP**：只要两个轨迹最终产生相同的符号序列（即激活输出一致），即可视为收敛。
   - 并证明：**d-ESP 蕴含传统 ESP**，为量化激活函数提供了理论基础。

3. **揭示稳定性机制的本质是预处理拓扑而非连续性本身**：
   - 发现决定稳定性的关键因素不是函数是否连续，而是其**预处理拓扑结构**（preprocessing topology）。
   - **单调压缩型预处理**（如 sigmoid 包裹）能维持稳定性，而**发散或不连续预处理**（如 modulo）会导致尖锐失效。

4. **提出“拥挤比”（Crowding Ratio）作为量化函数失效的预测指标**：
   - 定义 $ Q = N/k $，其中 $ N $ 是 reservoir size，$ k $ 是量化等级数。
   - 实验表明当 $ Q $ 超过阈值时（如 $ Q \approx 47.6 $ 或 $ 95 $），量化函数将出现 ESP 失败。

### 相比现有方法的优势
- **突破传统稳定性边界**：某些非光滑函数（如 Cantor 函数）可在谱半径 $ \rho \approx 10 $ 下保持稳定，远超传统 $ \rho < 1 $ 的经验准则。
- **加速收敛**：Cantor 函数和 Logistic Sigmoid 的收敛速度比 `tanh` 和 `ReLU` 快 **2.6 倍**。
- **提供新的设计原则**：从“光滑 vs 混沌”转向“压缩 vs 发散预处理”和“符号动力学”，为 ESN 设计提供新视角。

---

## 2. 核心实验方法和设置

### 数据集
本研究未使用真实世界数据集，而是通过**合成输入信号**进行系统性评估，以隔离激活函数的影响。输入分布包括：
- **Gaussian**（高斯分布）
- **Uniform**（均匀分布）
- **Sparse**（稀疏信号，90% 为零）

### 实验设置
- **模型架构**：标准 Leaky Integrator ESN，更新公式为：
  $$
  x_t = (1 - a)x_{t-1} + a f(W_{in}u_t + W_{res}x_{t-1})
  $$
- **参数扫描范围**：
  - 谱半径 $ \rho \in \{0.5, 0.6, ..., 5.0\} $，部分扩展至 $ \rho = 100 $
  - 泄漏率 $ a \in \{0.1, 0.3, 0.5, 0.7, 0.9\} $
  - 储备池大小 $ N \in \{1, 10, 50, 100, 500, 1000, 2000\} $
- **总配置数**：共测试 **36,610 种 reservoir 配置**，每种配置运行多个随机种子（通常 5–1000 次试验）。

### 评估指标
1. **ESP 合规性（ESP Compliance）**：
   - 判断两个不同初始状态的轨迹是否在 200 步内满足 $ \|x(t) - x'(t)\| < 0.1 $。
   - 若未收敛，则延长至 2000 步以区分慢收敛与真正失败。
2. **收敛速度（Convergence Speed）**：
   - 记录首次低于阈值的时间步数。
3. **最终距离（Final Distance）**：
   - 200 或 2000 步后的平均状态差异。
4. **经验局部 Lipschitz 常数估计**：
   - 使用有限差分法估算 $ |f(x+\epsilon) - f(x)|/\epsilon $，分析函数局部变化率。

### 基线方法对比
- **Smooth Baselines**：`tanh`, `ReLU`
- **Irregular Functions**：
  - Fractal: Cantor Function, Cantor Set, Weierstrass, Mandelbrot (Discrete & Continuous)
  - Chaotic: Logistic Map (Sigmoid & Modulo Wrappers)
  - Stochastic: Brownian Motion

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 激活函数 | 最大稳定谱半径 $ \rho $ | 收敛速度（中位数步数） | N=2000 时 ESP 成功率 |
|---------|--------------------------|------------------------|----------------------|
| `tanh` | ~2.0 | 15.6 | 100% |
| `ReLU` | ~1.5 | 15.2 | 100% |
| **Cantor Function** | **~10.0** | **6.1** | **100%** |
| **Logistic Sigmoid** | **~5.0** | **6.0** | **100%** |
| Mandelbrot (Continuous) | ~3.0 | ~10 | 100% |
| Mandelbrot (Discrete) | ~2.0 | 20–80 | 0.4% (N=2000) |
| Weierstrass | <1.0 | — | 0.4% |
| Brownian Motion | — | — | 0.4% |

### 与基线方法的对比结果
- **Cantor 函数**：
  - 在 $ \rho \approx 10 $ 下仍保持稳定，超出传统界限一个数量级。
  - 收敛速度达 `tanh` 的 **2.6 倍**。
  - 在 $ N=2000 $ 时仍保持 100% ESP 成功率。
- **Logistic Sigmoid**：
  - 在 $ \rho \approx 5 $ 下稳定，收敛速度快。
  - 表明即使底层动态是混沌的，只要预处理是压缩的（sigmoid），仍可稳定。
- **Mandelbrot 连续 vs 离散**：
  - 连续版本在所有规模下均稳定。
  - 离散版本在 $ N=1000 $ ($ Q \approx 47.6 $) 开始退化，在 $ N=2000 $ ($ Q \approx 95 $) 几乎完全失败，验证了 **crowding ratio** 的预测能力。

### 消融实验结果
- **预处理拓扑的影响**：
  - Logistic Map + Sigmoid Wrapper：稳定，快速收敛。
  - Logistic Map + Modulo Wrapper：在 $ N > 50 $ 后迅速崩溃，说明**不连续预处理破坏稳定性**。
- **量化的影响**：
  - 离散 Mandelbrot 在小规模（$ N=100 $）表现良好，但随 $ N $ 增加出现**伪吸引子**（steady-state error ≈ 0.1），而非真正收敛到零。
- **随机性的影响**：
  - Brownian Motion 激活函数仅在 1.6% 的试验中收敛，证实内部随机性会破坏确定性映射假设，导致 ESP 失败。

---

## 4. 关键结论和发现

### 主要发现
1. **非光滑激活函数可以优于传统光滑函数**：
   - 特定分形函数（如 Cantor 函数）不仅维持 ESP，还能显著**加快收敛速度**并**扩大稳定区域**。
2. **稳定性由预处理拓扑决定，而非连续性本身**：
   - **单调压缩型预处理**（如 sigmoid）是稳定的关键。
   - **不连续或发散预处理**（如 modulo）即使底层函数相同也会导致失败。
3. **量化函数存在固有缩放极限**：
   - 提出 **d-ESP** 框架，并证明其蕴含传统 ESP。
   - 量化函数在大型 reservoir 中必然失败，除非 $ N/k $（拥挤比）足够小。
4. **局部 Lipschitz 常数是重要指标**：
   - 经验最大局部 Lipschitz 常数 $ L_{\text{max}} < 20 $ 的函数通常稳定。
   - Weierstrass 函数 $ L_{\text{max}} > 500 $，导致完全失败。
5. **Cantor 函数是特例**：
   - 尽管处处不可微且非 Lipschitz，但由于其**单调性和分段常数特性**，表现出卓越的收敛性能。

### 方法的局限性
- **理论解释不足**：为何 Cantor 函数性能如此优异，目前尚无完整理论解释，暗示对几何属性如何影响 reservoir 动态的理解存在根本性空白。
- **任务性能未验证**：本研究聚焦于 **ESP 收敛性质**，尚未在标准 RC 任务（如 Mackey-Glass 预测、NARMA 等）上验证这些非光滑函数的实际计算性能。
- **计算开销**：分形函数（如 Mandelbrot）需迭代计算，尽管作者称其可接受，但在实时嵌入式系统中仍需权衡。

### 未来工作方向
- 推导基于 **leak-adjusted Jacobian 的平均情况收缩**（Lyapunov 指数）理论。
- 在真实基准任务和硬件 reservoir 上评估非光滑激活函数的性能。
- 探索其他具有类似“压缩+单调”特性的非光滑函数家族。
- 研究如何利用符号动力学（symbol dynamics）提升 reservoir 的信息处理能力。

--- 

> **代码可用性**：作者声明代码可根据请求获取。

</details>

---

### 14. [RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees](https://arxiv.org/abs/2512.14069)

**Authors**: Junjie Ma, Jinlong Li  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14069v1  

#### Abstract
Inference with modern Large Language Models (LLMs) is expensive and slow, and speculative sampling has emerged as an effective solution to this problem, however, the number of the calls to the draft model for generating candidate tokens in speculative sampling is a preset hyperparameter, lacking fle...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大语言模型（LLMs）在自回归解码过程中推理速度慢、计算成本高。**Speculative Sampling** 是一种有效的加速方法，通过一个小的 **draft model** 快速生成候选 token，并由目标 LLM 并行验证。然而，现有方法中对 draft model 的调用次数是一个预设的超参数（如固定为 8 次），缺乏灵活性，导致在某些上下文中产生冗余计算，甚至出现候选 token 全部被拒绝的情况（实验中高达 31%）。

### 提出的新方法与创新思路
作者提出 **RADAR**（Reinforcement learning Adjusted Draft-generation Algorithm for speculative sampling），其核心创新在于：

- **动态生成 draft tree**：不同于传统静态或固定深度的 draft tree 结构，RADAR 能够根据上下文实时决定是否继续调用 draft model，从而构建**动态深度的 draft tree**。
- **将 draft 过程建模为 MDP**：将 draft tree 的生成过程形式化为一个 **Markov Decision Process (MDP)**，利用强化学习来训练一个轻量级的 **prediction model**，该模型基于 draft model 输出的置信度分数，输出 `continue` 或 `stop` 决策信号。
- **离线强化学习 + 无需真实交互标签**：由于 acceptance length 具有随机性，无法获得精确标签。为此，作者设计了一种新颖的数据集构造方式——基于 EAGLE-3 在 ShareGPT 数据集上的运行结果，收集不同调用次数下的 **acceptance length 分布**，用于模拟奖励函数，实现 **offline reinforcement learning**，避免了昂贵的在线 LLM 交互。

### 相比现有方法的优势
- **更高的灵活性**：不再依赖固定的调用次数，能适应不同输入上下文的复杂度。
- **减少冗余计算**：平均减少 18.7% 的 draft model 调用次数，显著降低 draft 阶段开销。
- **更高的加速比**：在保持接近 EAGLE-3 的 acceptance length 的同时，实现了更优的 end-to-end 推理加速。
- **无性能损失**：遵循严格的 speculative sampling 协议，不改变目标 LLM 权重，保证输出分布一致。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **ShareGPT**：用于构建训练 dataset，运行 EAGLE-3 收集状态序列与 acceptance length 分布。
- **评估任务与数据集**：
  - 多轮对话：**MT-bench**
  - 数学推理：**GSM8K**
  - 指令跟随：**Alpaca**
  - 代码生成：**MBPP**

> 所有任务均未进行任务特定微调，使用统一权重测试。

### 实验设置
- **目标 LLMs**：
  - LLaMA-Instruct 3.1 8B (**L3 8B**)
  - Vicuna 13B (**V13B**)
  - DeepSeek-R1-Distill-LLaMA 8B (**DSL 8B**)
- **Draft Model**：沿用 EAGLE-3 中的小型模型架构。
- **实现基础**：基于 EAGLE-3 开源代码库开发。
- **训练细节**：
  - 使用 **REINFORCE** 算法进行 offline RL 训练
  - 学习率：1e-4
  - Branching factor $ k = 10 $
  - 最大 draft model 调用次数：8
  - Temperature = 1.0，Batch size = 1
- **硬件环境**：2× NVIDIA RTX 3090 GPU

### 评估指标
| 指标 | 定义 |
|------|------|
| **Speedup Ratio** | 相对于 vanilla auto-regressive 解码的实际加速比 |
| **Average Acceptance Length (T)** | 每个 drafting-verification cycle 中平均接受的 token 数量 |
| **Average Number of Calls to Draft Model** | 每 cycle 中平均调用 draft model 的次数（不再是固定值） |

> 注：未报告 acceptance rate，因 RADAR 不修改 draft model 结构，该指标与 EAGLE-3 相同。

### 基线方法对比
- **Vanilla Auto-Regressive Decoding**：基准方法（speedup = 1.00x）
- **EAGLE-2**：基于动态 draft tree 的早期工作
- **EAGLE-3**：当前最先进的 lossless speculative sampling 方法，作为主要对比对象

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| Model | Method | MT-bench Speedup | GSM8K Speedup | Alpaca Speedup | MBPP Speedup |
|-------|--------|------------------|---------------|----------------|--------------|
| L3 8B | Eagle-2 | 2.56x | 3.43x | 2.89x | 3.29x |
| L3 8B | Eagle-3 | 3.08x | 4.68x | 3.86x | 4.21x |
| L3 8B | **RADAR** | **3.41x** | **4.82x** | **4.04x** | **4.44x** |
| V13B  | Eagle-3 | 3.74x | 4.24x | 3.50x | 4.55x |
| V13B  | **RADAR** | **4.05x** | **4.36x** | **3.84x** | **4.75x** |
| DSL 8B| Eagle-3 | 3.42x | 4.39x | 3.08x | 3.71x |
| DSL 8B| **RADAR** | **3.86x** | **4.71x** | **3.17x** | **3.99x** |

✅ **结论**：RADAR 在所有 3 个 LLM 和 4 个任务上均取得最高 speedup ratio，相比 EAGLE-3 提升 **3% ~ 29%**。

### Acceptance Length 对比
- RADAR 的 average acceptance length 略低于 EAGLE-3（约低 1.2%），但仍维持高位。
- 表明其并未牺牲 token 利用效率。

### Draft Model 调用次数（见 Table 2）

| Model | MT-bench | GSM8K | Alpaca | MBPP |
|-------|----------|-------|--------|------|
| L3 8B | 5.25     | 6.19  | 6.20   | 6.60 |
| V13B  | 6.88     | 7.26  | 6.83   | 7.26 |
| DSL 8B| 6.10     | 7.20  | 5.85   | 6.47 |

> ✅ EAGLE-3 固定调用 8 次，而 RADAR 平均仅调用 **5.25–7.26 次**，**平均减少 18.7% 的调用次数**。

### 图形分析（Figure 2）
- **图 (a)**：RADAR 与 EAGLE-3 的 acceptance length 分布相近，说明有效性相当。
- **图 (b)**：RADAR 的 draft model 调用次数分布集中在中间范围（如 5–7），且存在较多早期终止（early stop），体现其动态决策能力。

### 消融实验（文中未明确列出，但从设计可推断）
虽然没有独立消融表，但以下设计体现了关键组件必要性：
- 使用 confidence score 作为输入 → 验证了轻量特征即可支持有效决策
- 构造 acceptance length distribution dataset → 支持 offline RL，避免实时 LLM 反馈
- 引入 step penalty (-Q) 和最终 speed reward → 平衡探索与效率

---

## 4. 关键结论和发现

### 主要发现
1. **动态控制 draft model 调用是可行且高效的**：通过将过程建模为 MDP，结合 offline RL，可以在不访问真实 LLM 的情况下训练出高性能的决策模型。
2. **减少冗余调用可进一步提升加速效果**：即使 acceptance length 略有下降，大幅减少 draft model 调用仍能带来整体推理时间的显著缩短。
3. **RADAR 实现了当前最优的 speculative sampling 加速性能**：在多个 LLM 和任务上 consistently 超越 EAGLE-3，达到 **3.17x–4.82x** 的加速比。

### 方法的局限性
- **依赖高质量的 acceptance length 分布估计**：若训练数据分布与实际部署场景偏差较大，可能影响 prediction model 泛化能力。
- **prediction model 增加额外延迟**：尽管 $ T_{\text{eye}} $ 很小，但在极端低延迟场景下仍需考虑。
- **未优化 draft model 本身**：focus 在调度策略，而非改进 draft model 的生成质量。

### 未来工作方向
- 探索更先进的 prediction model 架构（如 Transformer-based）
- 设计更精细的 reward function（例如引入不确定性估计）
- 动态调整 branching factor $ k $
- 将该框架扩展到其他 speculative inference 结构（如 Medusa, Hydra）

---

> 🔗 **开源信息**：代码已公开于 GitHub：[https://github.com/minaduki-sora/RADAR](https://github.com/minaduki-sora/RADAR)

</details>

---

### 15. [VersatileFFN: Achieving Parameter Efficiency in LLMs via Adaptive Wide-and-Deep Reuse](https://arxiv.org/abs/2512.14531)

**Authors**: Ying Nie, Kai Han, Hongguang Li, Hang Zhou, Tianyu Guo, Enhua Wu, Xinghao Chen, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14531v1  

#### Abstract
The rapid scaling of Large Language Models (LLMs) has achieved remarkable performance, but it also leads to prohibitive memory costs. Existing parameter-efficient approaches such as pruning and quantization mainly compress pretrained models without enhancing architectural capacity, thereby hitting t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*VersatileFFN: Achieving Parameter Efficiency in LLMs via Adaptive Wide-and-Deep Reuse*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）虽然性能强大，但其庞大的参数量导致**内存开销过高**，严重限制了实际部署。现有的参数高效方法（如剪枝、量化、LoRA等）主要在预训练模型上进行压缩，**并未增强模型的表征能力**，因此受限于原始架构的容量上限。

此外，传统的 **Mixture-of-Experts (MoE)** 虽然提升了容量，但引入了大量额外参数；而递归计算方法（如多层循环FFN）虽节省参数，却缺乏宽度上的多样性。

### 🚀 提出的新方法：VersatileFFN
作者提出 **VersatileFFN** ——一种新型的参数高效 FFN 结构，通过在**固定参数预算下灵活复用参数**，实现“宽且深”的自适应计算。

#### 核心创新点：
- **双路径设计**（受人类认知双系统理论启发）：
  - **Width-Versatile Path**（系统1：快速反应）
    - 构建虚拟 MoE：从一个共享的 FFN 中切分出多个非重叠的子空间作为“虚拟专家”，无需新增参数即可模拟稀疏专家路由。
  - **Depth-Versatile Path**（系统2：深度推理）
    - 递归复用同一个 FFN 多次，对复杂 token 进行迭代精炼，动态调整处理深度。
- **Difficulty-Aware Gating**
  - 利用深度路径预测的期望迭代次数作为 token 难度代理，动态融合两个路径输出：
    - “简单”token → 更依赖宽度路径（高效并行处理）
    - “困难”token → 更依赖深度路径（深层递归推理）

### 🔍 相比现有方法的优势
| 方法 | 参数增长 | 计算灵活性 | 是否提升表征能力 |
|------|----------|------------|------------------|
| Pruning / Quantization | ↓ | ❌ | ❌（仅压缩） |
| MoE | ↑↑↑（显著增加） | ✅（宽度路由） | ✅ |
| k-Loop Recurrence | ↔️（不变） | ✅（固定深度） | 有限 |
| **VersatileFFN** | **↔️（几乎无增长）** | ✅✅（宽+深自适应） | ✅✅ |

> ✅ **核心优势**：在**不增加参数的前提下**，同时利用**宽度多样性**和**深度递归性**，实现更强的表征能力和更高的参数效率。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **预训练数据**：FineWeb-Edu（教育质量网页文本）
  - 子集规模：40B tokens（用于354M模型）、70B tokens（用于720M模型）
- **评估基准**（零样本评测）：
  - PIQA, HellaSwag, OpenBookQA (OBQA), SciQ
  - ARC-easy (ARC-e), ARC-challenge (ARC-c)
  - CommonsenseQA (COMM), Winogrande (WINO)
  - 所有任务采用 **OLMES** 工具进行 zero-shot 评估

### ⚙️ 实验设置
- **基础模型架构**：基于开源 OLMo2 架构
  - 两种规模：354M 和 720M 参数
  - 层数：15 层，激活函数为 SwiGLU，归一化为 RMSNorm
  - 序列长度：4096
- **训练方式**：继续预训练（continued pre-training），在一个 epoch 内完成
- **优化器**：AdamW，cosine 学习率衰减，warm-up 占总步数 5%
- **超参选择**：
  - Width-Versatile：选用 `8 choose 2` 虚拟专家配置
  - Depth-Versatile：最大循环次数 $L_{\text{max}} = 4$

### 📊 评估指标
- 主要指标：**平均准确率（Avg. Accuracy）**
- 辅助指标：
  - 模型总参数量（Params）
  - FFN 部分的 FLOPs（衡量计算成本）
  - 训练损失（Loss）

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **BASE** | 原始密集模型（标准 FFN） |
| **MoE** | 在 BASE 上添加 8 个小专家，top-2 路由 |
| **k-Loop** | 将 FFN 循环执行 k 次（k=2,4,6），参数不变但计算量翻倍 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 & 2）

#### ✅ 在 720M 模型上的表现：
| 方法 | 平均准确率 (Avg.) | 总参数 (M) | FFN FLOPs (M) | Loss |
|------|--------------------|-------------|----------------|-------|
| BASE | 53.83 | 720.81 | 849.35 | 2.519 |
| MoE | 55.87 | 1145.69 | 1061.69 | **2.411** |
| 4-LOOP | 56.33 | 720.81 | 3397.40 | 2.441 |
| 6-LOOP | 56.55 | 720.81 | 5096.10 | 2.430 |
| **VersatileFFN** | **57.03** | **721.09** | **2586.38** | 2.430 |

#### ✅ 在 354M 模型上的表现：
| 方法 | Avg. Acc. | Params (M) | FFN FLOPs (M) |
|------|-----------|------------|----------------|
| BASE | 47.98 | 354.71 | 377.49 |
| MoE | 51.48 | 543.59 | 471.86 |
| 4-LOOP | 51.98 | 354.71 | 1509.96 |
| **VersatileFFN** | **52.33** | **354.90** | **1236.08** |

> 💡 **关键观察**：
> - VersatileFFN 在所有模型尺度上均取得**最高平均准确率**
> - 参数仅增加约 **0.04%**（vs. MoE 增加 ~59%）
> - 计算量远低于 6-Loop（约为其 50%），但仍优于其精度

### 🔍 与基线方法的对比结果
- **vs. MoE**：
  - 准确率更高（+1.16% @720M），且**参数几乎不变**
  - MoE 虽然训练 loss 更低，但在下游任务泛化更差
  - 特别是在 **ARC-e** 和 **CommonsenseQA** 等推理密集型任务上领先明显（+3.33%）
- **vs. k-Loop**：
  - 在更低 FLOPs 下达到更高性能（例如 4-Loop 需 3397M FLOPs，VersatileFFN 仅需 2586M）
  - 表明其资源分配更智能，避免无效重复计算

### 🔧 消融实验结果（Ablation Studies）

| 方法配置 | Loss | Acc (%) |
|--------|------|---------|
| BASE | 2.779 | 47.98 |
| + Width-Versatile | 2.633 | 50.98 |
| + Depth-Versatile | 2.625 | 51.86 |
| + Gating（完整版） | **2.617** | **52.33** |

> ✅ 结论：两个分支**互补协同**，联合使用效果最佳；gating 机制进一步提升性能。

其他消融：
- 最优专家配置：`(8 choose 2)` > `(8 choose 4)` 或 `(16 choose 4)`
- 最优循环次数：4 次 > 6 次（过多循环可能导致过拟合）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **参数复用优于参数扩张**：
   - 在固定参数预算下，通过**结构化复用**（宽+深）可显著超越传统 MoE 和纯递归方法。
2. **自适应计算是关键**：
   - 动态根据 token 难度分配计算路径（宽度 or 深度），实现了“该快则快，该慢则慢”的智能推理。
3. **双系统协同有效**：
   - 宽度路径提供多样化响应能力，深度路径支持复杂语义推理，二者结合形成更强的通用表征。
4. **极高的参数效率**：
   - 引入的 router 和 loop predictor 仅带来 <0.1% 参数增长，却带来超过 3% 的准确率提升。

### ⚠️ 方法的局限性
- **计算延迟可能上升**：尽管参数少，但深度路径的递归执行可能影响推理速度（尤其在长序列场景）。
- **硬件利用率挑战**：动态控制流（如 early-exit）需要定制化推理引擎支持。
- **当前仍依赖预训练权重初始化**：尚未验证从头训练的效果。

### 🔮 未来工作方向
- 探索更高效的控制器设计（如轻量化 loop predictor）
- 扩展到 Vision Transformer 或多模态模型
- 开发专用推理框架以支持动态计算图调度
- 研究“compute-heavy, memory-light”架构的设计范式，推动边缘设备上的高级推理能力

---

## 总结
> **VersatileFFN** 提出了一种全新的参数高效思想：**通过智能地复用参数而非盲目扩展，在不增加内存负担的前提下，释放更大的模型潜力**。其实验充分证明，**计算的灵活性**（adaptive wide-and-deep reuse）可以成为突破 LLM 性能瓶颈的新路径，为未来“小内存、大智能”的模型设计提供了重要启示。

</details>

---

### 16. [TiME: Tiny Monolingual Encoders for Efficient NLP Pipelines](https://arxiv.org/abs/2512.14645)

**Authors**: David Schulmeister, Valentin Hartmann, Lars Klein, Robert West  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14645v1  

#### Abstract
Today, a lot of research on language models is focused on large, general-purpose models. However, many NLP pipelines only require models with a well-defined, small set of capabilities. While large models are capable of performing the tasks of those smaller models, they are simply not fast enough to ...

---

### 17. [FusAD: Time-Frequency Fusion with Adaptive Denoising for General Time Series Analysis](https://arxiv.org/abs/2512.14078)

**Authors**: Da Zhang, Bingyu Li, Zhiyuan Zhao, Feiping Nie, Junyu Gao, Xuelong Li  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14078v1  

#### Abstract
Time series analysis plays a vital role in fields such as finance, healthcare, industry, and meteorology, underpinning key tasks including classification, forecasting, and anomaly detection. Although deep learning models have achieved remarkable progress in these areas in recent years, constructing ...

---

### 18. [Blind Radio Mapping via Spatially Regularized Bayesian Trajectory Inference](https://arxiv.org/abs/2512.13701)

**Authors**: Zheng Xing, Junting Chen  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13701v1  

#### Abstract
Radio maps enable intelligent wireless applications by capturing the spatial distribution of channel characteristics. However, conventional construction methods demand extensive location-labeled data, which are costly and impractical in many real-world scenarios. This paper presents a blind radio ma...

---

### 19. [Compressed Causal Reasoning: Quantization and GraphRAG Effects on Interventional and Counterfactual Accuracy](https://arxiv.org/abs/2512.13725)

**Authors**: Steve Nwaiwu, Nipat Jongsawat, Anucha Tungkasthan  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13725v1  

#### Abstract
Causal reasoning in Large Language Models spanning association, intervention, and counterfactual inference is essential for reliable decision making in high stakes settings. As deployment shifts toward edge and resource constrained environments, quantized models such as INT8 and NF4 are becoming sta...

---

### 20. [OpenDataArena: A Fair and Open Arena for Benchmarking Post-Training Dataset Value](https://arxiv.org/abs/2512.14051)

**Authors**: Mengzhang Cai, Xin Gao, Yu Li, Honglin Lin, Zheng Liu, Zhuoshi Pan, Qizhi Pei, Xiaoran Shang, Mengyuan Sun, Zinan Tang, Xiaoyang Wang, Zhanping Zhong, Yun Zhu, Dahua Lin, Conghui He, Lijun Wu  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14051v1  

#### Abstract
The rapid evolution of Large Language Models (LLMs) is predicated on the quality and diversity of post-training datasets. However, a critical dichotomy persists: while models are rigorously benchmarked, the data fueling them remains a black box--characterized by opaque composition, uncertain provena...

---

### 21. [Grammar Search for Multi-Agent Systems](https://arxiv.org/abs/2512.14079)

**Authors**: Mayank Singh, Vikas Yadav, Shiva Krishna Reddy Malay, Shravan Nayak, Sai Rajeswar, Sathwik Tejaswi Madhusudhan, Eduardo Blanco  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14079v1  

#### Abstract
Automatic search for Multi-Agent Systems has recently emerged as a key focus in agentic AI research. Several prior approaches have relied on LLM-based free-form search over the code space. In this work, we propose a more structured framework that explores the same space through a fixed set of simple...

---

### 22. [Astraea: A State-Aware Scheduling Engine for LLM-Powered Agents](https://arxiv.org/abs/2512.14142)

**Authors**: Hongqiu Ni, Jiabao Zhang, Guopeng Li, Zilong Wang, Ruiqi Wu, Chi Zhang, Haisheng Tan  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14142v1  

#### Abstract
Large Language Models (LLMs) are increasingly being deployed as intelligent agents. Their multi-stage workflows, which alternate between local computation and calls to external network services like Web APIs, introduce a mismatch in their execution pattern and the scheduling granularity of existing ...

---

### 23. [Delete and Retain: Efficient Unlearning for Document Classification](https://arxiv.org/abs/2512.13711)

**Authors**: Aadya Goel, Mayuri Sridhar  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13711v1  

#### Abstract
Machine unlearning aims to efficiently remove the influence of specific training data from a model without full retraining. While much progress has been made in unlearning for LLMs, document classification models remain relatively understudied. In this paper, we study class-level unlearning for docu...

---

### 24. [State-Dependent Refusal and Learned Incapacity in RLHF-Aligned Language Models](https://arxiv.org/abs/2512.13762)

**Authors**: TK Lee  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.13762v1  

#### Abstract
Large language models (LLMs) are widely deployed as general-purpose tools, yet extended interaction can reveal behavioral patterns not captured by standard quantitative benchmarks. We present a qualitative case-study methodology for auditing policy-linked behavioral selectivity in long-horizon inter...

---

### 25. [Sparsity-Controllable Dynamic Top-p MoE for Large Foundation Model Pre-training](https://arxiv.org/abs/2512.13996)

**Authors**: Can Jin, Hongwu Peng, Mingcan Xiang, Qixin Zhang, Xiangchi Yuan, Amit Hasan, Ohiremen Dibua, Yifan Gong, Yan Kang, Dimitris N. Metaxas  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.13996v1  

#### Abstract
Sparse Mixture-of-Experts (MoE) architectures effectively scale model capacity by activating only a subset of experts for each input token. However, the standard Top-k routing strategy imposes a uniform sparsity pattern that ignores the varying difficulty of tokens. While Top-p routing offers a flex...

---

### 26. [TiCard: Deployable EXPLAIN-only Residual Learning for Cardinality Estimation](https://arxiv.org/abs/2512.14358)

**Authors**: Qizhi Wang  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.14358v1  

#### Abstract
Cardinality estimation is a key bottleneck for cost-based query optimization, yet deployable improvements remain difficult: classical estimators miss correlations, while learned estimators often require workload-specific training pipelines and invasive integration into the optimizer. This paper pres...

---

### 27. [Physics-Guided Deep Learning for Heat Pump Stress Detection: A Comprehensive Analysis on When2Heat Dataset](https://arxiv.org/abs/2512.13696)

**Authors**: Md Shahabub Alam, Md Asifuzzaman Jishan, Ayan Kumar Ghosh  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.13696v1  

#### Abstract
Heat pump systems are critical components in modern energy-efficient buildings, yet their operational stress detection remains challenging due to complex thermodynamic interactions and limited real-world data. This paper presents a novel Physics-Guided Deep Neural Network (PG-DNN) approach for heat ...

---

### 28. [Sliding Window Recurrences for Sequence Models](https://arxiv.org/abs/2512.13921)

**Authors**: Dragos Secrieru, Garyk Brixi, Yoshua Bengio, Taiji Suzuki, Michael Poli, Stefano Massaroli  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.13921v1  

#### Abstract
Multi-hybrid architectures are poised to take over language modeling due to better quality and performance. We introduce a hierarchical decomposition framework for linear recurrences that allows us to develop algorithms aligned with GPU memory hierarchies, yielding Sliding Window Recurrences. We foc...

---

### 29. [A Complete Guide to Spherical Equivariant Graph Transformers](https://arxiv.org/abs/2512.13927)

**Authors**: Sophia Tang  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.13927v1  

#### Abstract
Spherical equivariant graph neural networks (EGNNs) provide a principled framework for learning on three-dimensional molecular and biomolecular systems, where predictions must respect the rotational symmetries inherent in physics. These models extend traditional message-passing GNNs and Transformers...

---

### 30. [EvoLattice: Persistent Internal-Population Evolution through Multi-Alternative Quality-Diversity Graph Representations for LLM-Guided Program Discovery](https://arxiv.org/abs/2512.13857)

**Authors**: Kamer Ali Yuksel  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13857v1  

#### Abstract
Large language models (LLMs) are increasingly used to evolve programs and multi-agent systems, yet most existing approaches rely on overwrite-based mutations that maintain only a single candidate at a time. Such methods discard useful variants, suffer from destructive edits, and explore a brittle se...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
