# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-26 06:03:27 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DataStates-LLM: Scalable Checkpointing for Transformer Models Using Composable State Providers](https://arxiv.org/abs/2601.16956)

**Authors**: Avinash Maurya, M. Mustafa Rafique, Franck Cappello, Bogdan Nicolae  
**Category**: cs.DC  
**Published**: 2026-01-26  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.16956v1  

#### Abstract
The rapid growth of Large Transformer-based models, specifically Large Language Models (LLMs), now scaling to trillions of parameters, has necessitated training across thousands of GPUs using complex hybrid parallelism strategies (e.g., data, tensor, and pipeline parallelism). Checkpointing this mas...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DataStates-LLM: Scalable Checkpointing for Transformer Models Using Composable State Providers**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
大型语言模型（LLMs）在训练过程中需要跨数千个GPU进行分布式计算，采用复杂的混合并行策略（如 **Data Parallelism (DP)**、**Tensor Parallelism (TP)** 和 **Pipeline Parallelism (PP)**）。传统的检查点（checkpointing）机制将模型状态视为不透明的二进制块，忽略了其“**3D异构性**”（3D heterogeneity），即：
- **内存位置差异**：GPU vs. Host 内存中的数据；
- **数据类型差异**：张量（tensors）vs. Python 对象（如字典、随机种子等）；
- **分片策略差异**：由并行策略导致的细粒度分布。

这导致了严重的运行时开销，包括：
- 阻塞式的 **Device-to-Host (D2H)** 数据传输；
- 数据盲区的序列化（data-oblivious serialization）；
- 存储 I/O 竞争。

因此，现有方案无法高效支持高频率、低干扰的检查点操作，影响训练效率与容错能力。

---

### **提出了什么新方法或新思路**
论文提出 **DataStates-LLM**，一种专为大规模 Transformer 模型设计的高性能异步检查点系统，其核心创新包括：

#### ✅ **State Providers（状态提供者）抽象层**
- 引入轻量级中间件抽象——**State Provider (SP)**，封装不同类型数据结构的语义（如张量布局、序列化需求、存储映射）。
- 允许对不同数据类型（GPU张量、Host元数据）进行定制化的处理，实现零拷贝序列化（zero-copy serialization）和并行流式输出。

#### ✅ **Lazy, Non-Blocking Asynchronous Snapshots**
- 利用训练迭代中 **前向传播（forward）和反向传播（backward）阶段参数不变** 的特性，在这些阶段**非阻塞地发起 D2H 传输**。
- 实现“懒快照”机制：只要在优化器更新（update）前完成复制即可，避免中断关键路径。

#### ✅ **Streamlined Multi-Tier I/O Engine**
- 设计流水线式 I/O 引擎，利用 **liburing + O_DIRECT** 实现内核加速的多线程持久化写入；
- 管理预分配的 pinned host memory pool，支持多级缓冲与重叠传输（D2H → Host → SSD/PFS）。

#### ✅ **Overlap I/O with Serialization**
- 分离元数据生成与大张量传输，优先推送无需序列化的张量数据流，使小对象的序列化过程与 I/O 并发执行，消除瓶颈。

#### ✅ **Hybrid Persistent Layout Strategy**
- 采用“固定偏移 + 并发日志追加”的混合布局策略：
  - 大张量按已知大小预分配偏移；
  - 小对象以日志形式追加；
  - 最后附加一个 header 描述整体布局。

---

### **相比现有方法的优势**
| 维度 | 现有方法（如 DeepSpeed、TorchSnapshot） | DataStates-LLM |
|------|----------------------------------------|----------------|
| **序列化方式** | `torch.save` 类型无关遍历，重复序列化张量 | 零拷贝张量直写，仅序列化非张量对象 |
| **D2H 传输** | 同步或粗粒度异步，占用训练带宽 | 异步、非阻塞、复用 pinned 缓冲池 |
| **I/O 并行度** | 单线程或多文件 chunking，易引发 metadata 瓶颈 | 多线程并发 flush，最大化 PCIe/PFS 利用率 |
| **与训练重叠** | 有限重叠，常阻塞 update 阶段 | 完全重叠于 forward/backward，最小化干扰 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- 使用 **OSCAR-en 子集**（79K records），来自 BLOOM 项目公开数据集；
- Tokenization 使用 **Llama 2 tokenizer**；
- 序列长度统一设为 **2048**，微批次大小为 **16**。

---

### **实验设置**
#### **平台环境**
- 在 **ALCF Polaris 超算系统** 上测试：
  - 每节点：4× NVIDIA A100-40GB GPU（NVLink互联）、AMD Milan CPU、512GB DDR4、双1.6TB本地SSD；
  - 网络：PCIe Gen4（D2H 峰值 25 GB/s）、NVLink（D2D 峰值 85 GB/s）；
  - 存储后端：Lustre PFS（峰值 650 GB/s）。

#### **模型配置**
基于 BLOOM 和 Llama 架构构建多个规模模型：
| 模型大小（B） | 层数 | 隐藏维度 | 注意力头数 | 节点数 | TP | PP | ZeRO Stage |
|--------------|-------|-----------|-------------|--------|----|----|------------|
| 3            | 30    | 2560      | 32          | 1      | 4  | 1  | 1          |
| 7            | 32    | 4096      | 32          | 2      | 4  | 2  | 1          |
| 13           | 40    | 5120      | 40          | 4      | 4  | 4  | 1          |
| 33 / 70      | ...   | ...       | ...         | 8 / 20 | 4  | ...| 1          |

> 所有实验默认 **DP=1**，除非特别研究 DP 扩展性。

#### **内存与缓存设置**
- 每节点预留 **80GB pinned host memory** 作为检查点缓存；
- 支持缓存约一个完整检查点版本（每GPU约10–15GB）。

---

### **评估指标**
1. **Effective Checkpoint Throughput**  
   = 总检查点大小 / 训练被阻塞的时间（含同步序列化、等待刷新等）
2. **Iteration Duration under Checkpointing**  
   包括直接停滞与间接干扰（如通信降速）
3. **End-to-End Training Time**  
   多轮迭代总耗时，反映累积 I/O 尾部效应
4. **Per-Phase Breakdown**  
   分析 metadata/serialize、GPU→Host、Host→File 各阶段耗时

---

### **基线方法对比**
| 方法 | 描述 |
|------|------|
| **DeepSpeed Default** | 使用 `torch.save()` 同步保存，完全阻塞训练 |
| **TorchSnapshot** | PyTorch 官方异步检查点工具，支持 chunked I/O 和多线程写入 |
| **DataStates-LLM-Old** | 作者前期工作，具备部分异步能力但无 State Providers |
| **DataStates-LLM (本文)** | 完整版，集成所有优化 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### 🔹 **检查点吞吐提升**
- 在 **70B 参数模型、256 GPU** 规模下：
  - **DataStates-LLM 达到 3×–4.2× 更高的检查点吞吐量**，相比 TorchSnapshot；
  - 相比原始 DeepSpeed，默认同步方案慢数倍。

#### 🔹 **端到端训练时间减少**
- 在多种模型规模和 DP 设置下：
  - **训练时间缩短 1.3×–2.2×**；
  - 在高频检查点场景（每 iteration 一次），优势更显著。

#### 🔹 **单次检查点各阶段耗时分解（7B 模型，单 rank）**
| 阶段 | DeepSpeed | TorchSnapshot | DataStates-LLM |
|------|-----------|---------------|----------------|
| Metadata/Serialize | 3.9 sec | 0.0258 sec | **0.0156 sec** |
| GPU→Host | 1.9 sec | 1.3 sec | **0.6 sec** |
| Host→File | 16.1 sec | 11.5 sec | **3.8 sec** |

> ✅ **序列化时间降低 1.65×**，得益于 State Providers 的选择性序列化；  
> ✅ **D2H 时间降低 2.17×**，因异步 DMA + pinned 缓冲池；  
> ✅ **Host→File 时间降低 3×**，归功于 liburing 加速与多线程 flush。

---

### **与基线方法的对比结果**

#### 📈 图7：不同模型规模下的聚合检查点吞吐
- 所有方法随模型增大而提升（更多节点 → 更高并行度）；
- **DataStates-LLM 始终领先**，在 70B 模型上达 **~422 GB/s**，是 TorchSnapshot 的近 **4×**。

#### 📉 图8 & 图9：迭代时间和端到端训练时间
- 小模型（3B/7B）中，传统方法检查点时间远超训练时间；
- **DataStates-LLM 将检查点开销降至可忽略水平**，迭代几乎不受影响；
- 端到端时间在 15 次迭代中最多减少 **2.2×**。

#### 📊 图10–11：增加数据并行度（DP）的影响
- 当 DP 从 1 扩展至 16：
  - 每 rank 检查点体积减小（ZeRO-1 分片）；
  - 但文件数量剧增，加剧 metadata 压力；
- **DataStates-LLM 仍保持稳定性能**，而其他方法扩展性差；
- 在 13B 模型 + DP=16 下，**训练时间仅为 TorchSnapshot 的 1/5.7**。

#### ⏱️ 图13：不同检查点频率下的表现
- 即使每 **2 个 iteration 检查一次**，DataStates-LLM 的 50 迭代总时间仍优于 TorchSnapshot 每 **10 次才检查一次** 的情况；
- 表明其能支持 **极高频检查点**（适用于 RLHF、轨迹分析等场景）。

---

### **消融实验结果**

#### 🔍 **子操作拆解（Table III）**
- **State Providers 显著降低元数据处理延迟**；
- **预分配 + pinned 缓冲池极大加速 D2H**；
- **liburing 多线程 flush 提升 Host→File 效率**。

#### 🔬 **I/O 微基准测试（Figure 14）**
- 测试单一节点上多个 rank 并发写入张量：
  - **DataStates-LLM 在各种张量大小下均比 TorchSnapshot 快 1.25×–2.5×**；
  - 接近理论峰值（“ideal” host-only 基线）。

#### 🔄 **多级传输流水线可视化（Figure 15）**
- 展示 7B 模型中最大几个张量的传输时间线：
  - GPU→Host 与 Host→File 完全重叠；
  - 多个张量并行 flush 到不同文件；
  - 实现真正的 **streaming multi-tier I/O pipeline**。

---

## **4. 关键结论和发现**

### **主要发现**
1. **3D 异构性是 LLM 检查点的核心挑战**，必须显式建模而非当作黑盒处理；
2. **训练迭代中的 immutability 特性可被充分利用** 来实现非阻塞快照；
3. **分离关注点（state abstraction vs. data movement）至关重要**，State Providers 是实现高效异构管理的关键；
4. **序列化不应成为瓶颈**，应绕过张量的冗余序列化，并让小对象序列化与 I/O 重叠；
5. **现代 I/O 技术（如 liburing）+ 多线程 flush 可显著提升持久化带宽利用率**。

---

### **方法的局限性**
- **依赖足够大的 host memory 缓冲池**（本实验使用 80GB/node），在内存受限环境中可能难以部署；
- **当前未整合压缩或差分检查点**，对于极高频率场景仍有优化空间；
- **未解决 PFS metadata server 瓶颈的根本问题**，虽然通过减少小文件竞争缓解，但仍受文件系统限制；
- **目前仅集成于 DeepSpeed**，虽模块化设计支持迁移，但适配其他框架需额外开发。

---

### **未来工作方向**
1. **引入数据缩减技术**：
   - 差分检查点（differential checkpointing）
   - 张量压缩（compression）
2. **支持更深的内存层级**：
   - Offload model/optimizer states 至 NVMe 或远程存储
3. **Shard 聚合与合并策略**：
   - 减少文件数量以缓解 PFS metadata 压力，同时保留并行写入能力
4. **动态资源调度与自适应缓冲管理**
5. **扩展至推理和服务场景的快速恢复**

--- 

> ✅ **总结一句话**：  
> **DataStates-LLM 通过引入 State Providers 和 lazy asynchronous I/O pipeline，在不扰动训练的前提下实现了高达 4× 的检查点吞吐提升和 2.2× 的端到端训练加速，为极端规模 LLM 训练提供了高效的容错与调试基础设施。**

</details>

---

### 2. [When Agents Fail to Act: A Diagnostic Framework for Tool Invocation Reliability in Multi-Agent LLM Systems](https://arxiv.org/abs/2601.16280)

**Authors**: Donghao Huang, Gauri Malwe, Zhaoxia Wang  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.16280v1  

#### Abstract
Multi-agent systems powered by large language models (LLMs) are transforming enterprise automation, yet systematic evaluation methodologies for assessing tool-use reliability remain underdeveloped. We introduce a comprehensive diagnostic framework that leverages big data analytics to evaluate proced...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结  
**论文标题**：*When Agents Fail to Act: A Diagnostic Framework for Tool Invocation Reliability in Multi-Agent LLM Systems*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题
当前多智能体 LLM 系统在企业自动化中展现出巨大潜力，但其**工具调用的程序可靠性（procedural reliability）缺乏系统性评估方法**。现有研究多依赖聚合指标（如任务成功率），无法揭示失败的根本原因，尤其在多智能体协作场景下，错误会级联传播，导致部署不可靠。

该论文聚焦于：
- 工具初始化、参数传递、执行与结果解释过程中的**细粒度故障诊断**
- 面向中小企业（SMEs）在隐私敏感、资源受限环境下的实际部署挑战

---

### 🛠️ 提出了什么新方法或新思路

#### （1）**12类错误分类法（12-category error taxonomy）**
首次将单智能体工具使用错误分析扩展到**多智能体协调场景**，结合：
- **4种错误类型**：
  - `Not Initialized`（未正确调用）
  - `Arguments Mismatch`（参数不匹配）
  - `Error`（运行时异常）
  - `Result Mismatch`（输出导致下游失败）
- **3类工具操作**：
  - OCR_TOOL
  - DB_QUERY_TOOL
  - DB_UPDATE_TOOL  
→ 形成 **12个诊断类别**，实现对工具调用全过程的精细化归因。

#### （2）**可复现的评估基础设施**
构建标准化测试框架，支持跨模型、跨硬件平台比较：
- 支持 open-weight 模型（Qwen2.5系列、Functionary）与 closed-source 模型（GPT-4, Claude 3.5/3.7）
- 在多种边缘设备上测试（NVIDIA RTX A6000 / RTX 4090 / Apple M3 Max）
- 使用 **4-bit量化**（Q4_K_M）模拟真实边缘部署条件

#### （3）**可靠性阈值识别与部署建议**
通过大规模实验识别出关键容量阈值，为组织提供基于成本-性能权衡的部署指南。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 评估粒度 | 聚合成功率（Success Rate only） | 细粒度错误归因（12类错误分布） |
| 场景覆盖 | 单智能体为主 | 多智能体协同 + 工具链路级联 |
| 可复现性 | 少数闭源API基准 | 开源代码、数据集、协议全公开（GitHub） |
| 部署指导 | 缺乏硬件适配建议 | 提供 tiered deployment recommendations |

> ✅ 所有资源已开源：https://github.com/inflaton/df4tir

---

## 2. 核心实验方法和设置

### 📊 使用了哪些数据集
- 构建了一个**合成的发票对账（invoice reconciliation）数据集**，共 **1,980个确定性测试实例**
  - 视觉任务（vision-enabled）：990例（含PDF扫描件需OCR处理）
  - 纯文本任务（text-only）：990例
- 数据设计贴近真实业务流程，涉及多模态输入、数据库查询与更新等典型企业操作

---

### ⚙️ 实验设置和评估指标

#### 模型选择
| 类型 | 模型列表 |
|------|--------|
| **Closed-Source** | `gpt-4o`, `gpt-4.1`, `claude-3-5-sonnet`, `claude-3-7-sonnet` 等 |
| **Open-Weight** | `qwen2.5:3b` 到 `qwen2.5:72b`，`functionary-small`/`medium` |

#### 部署配置
- Open-weight 模型使用 **Ollama v0.6.8** 本地运行
- 采用 **4-bit quantization (Q4_K_M)** 减少内存占用
- 测试平台：
  - NVIDIA RTX A6000（48GB VRAM）
  - RTX 4090 Laptop GPU（16GB）
  - Apple M3 Max（96GB统一内存）

#### 评估指标
| 指标 | 描述 |
|------|------|
| **SR (Success Rate)** | 最终任务完成率 |
| **Execution Time** | 平均响应延迟（秒） |
| **Process Steps** | 完成任务所需的平均步骤数 |
| **OCR F1 Score** | OCR结果解析准确率 |
| **Deterministic Setting** | 温度=0，固定prompt，确保可复现 |

---

### 🆚 基线方法对比
- **Baseline 1**: 当前主流闭源模型（GPT-4.1, Claude 3.7）作为性能上限参考
- **Baseline 2**: Kokane et al. 的 SpecTool（仅适用于单智能体）
- **Baseline 3**: 功能相似但规模较小的 open-weight 模型（如 Functionary）

> 本工作是首个在**相同任务、相同协议下横向比较 open/closed LLMs 在 multi-agent setting 中 tool-use reliability** 的研究。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 模型 | 平台 | 成功率 (SR%) | 延迟 (Time/s) | 步骤数 (Steps) |
|------|------|-------------|--------------|---------------|
| `qwen2.5:32b` | RTX A6000 | **100.0%** | 13.9 | 8.5 |
| `qwen2.5:14b` | RTX A6000 | **96.6%** | **7.3** | 8.6 |
| `qwen2.5:7b` | RTX A6000 | 57.3% | 9.9 | 20.7 |
| `qwen2.5:3b` | RTX A6000 | 13.9% | 8.1 | 18.8 |
| `gpt-4.1` | OpenAI | **100.0%** | 8.3 | 9.9 |
| `gpt-4o-mini` | OpenAI | 99.3% | 7.6 | 8.6 |
| `functionary-small` | RTX A6000 | 7.5% | 4.0 | 5.6 |

> ✅ `qwen2.5:32b` 实现 **100% 成功率**，与 `gpt-4.1` 持平  
> ✅ `qwen2.5:14b` 达到 **96.6%-97.4%** 成功率，在性价比与可靠性之间取得最佳平衡

---

### 🔁 错误模式分布（来自 Tables II & III）

| 错误类型 | 小模型表现（e.g., qwen2.5:3b） | 大模型表现（e.g., qwen2.5:32b / GPT-4.1） |
|--------|-------------------------------|----------------------------------------|
| `DB_UPDATE_TOOL_NOT_INITIALIZED` | 高达 881 次（占非视觉任务 76.36%） | **0次** |
| `DB_QUERY_TOOL_NOT_INITIALIZED` | 数百次发生 | **0次** |
| 参数/结果错误 | 较少 | 极少 |

> 🔴 **工具初始化失败是主要瓶颈**，而非参数错误或输出误解

---

### 🔄 消融实验与关键观察（Ablation Insights）

#### （1）模型规模影响显著
- 成功率随参数量增加而提升，呈明显正相关
- 存在“**临界阈值**”：
  - **14B** 是最小可行生产配置（>96.6% SR）
  - **32B** 实现闭源模型级可靠性（100% SR）
  - **72B** 反而略降（95.1%），显示任务特定容量极限

#### （2）硬件对性能影响巨大
- `qwen2.5:14b` 在 RTX A6000 上耗时 **7.3s**，而在 M3 Max 上高达 **60.0s** → **8.2倍延迟差异**
- 更强硬件不能弥补小模型缺陷：`qwen2.5:7b` 在 M3 Max 上仍需 **85.9s**，且平均步数达 **20.7**

#### （3）规划效率随模型增大改善
- 小模型易陷入循环决策（recursion limit 被触发）
- 大模型更倾向于直接调用工具，减少冗余推理路径

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **工具初始化失败是最大瓶颈**
   - 特别是在小模型中（如 `qwen2.5:3b` 超过 89% 的错误源于此）
   - 表明模型缺乏“何时调用工具”的元认知能力

2. **存在明确的可靠性阈值**
   - `qwen2.5:14b` 是**最小可行生产配置**（96.6–97.4% 成功率）
   - `qwen2.5:32b` 实现与 `GPT-4.1` 相当的**零错误表现**

3. **开放权重模型可在特定规模下媲美闭源模型**
   - `qwen2.5:32b` 在工具调用可靠性方面达到 GPT-4.1 水平
   - 为隐私敏感行业（如金融）提供可信替代方案

4. **硬件选择极大影响延迟**
   - 同一模型在不同平台上延迟相差 **8.2倍**
   - 推荐搭配高性能 GPU（如 RTX A6000）以优化推理效率

5. **模型大小 ≠ 总是更好**
   - `qwen2.5:72b` 成功率低于 `32b`，表明存在任务适配的“甜点区”

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **领域限制** | 当前验证集中在发票对账这一结构化业务流程，是否泛化至其他领域待验证 |
| **静态任务流** | 使用确定性 prompt 和固定流程，未涵盖动态复杂任务分解 |
| **合成数据偏差** | 数据虽模仿现实，但仍为合成生成，可能存在理想化倾向 |
| **缺少容错机制测试** | 未集成自动重试、fallback 策略等工程增强手段 |

---

### 🔮 未来工作方向（作者提出）

1. **对抗性与负样本协议设计**  
   引入更具挑战性的 edge cases 和 adversarial prompts 来测试鲁棒性

2. **分布式故障分析**  
   扩展框架以支持跨多个节点、异构代理系统的联合诊断

3. **自愈型智能体架构（self-healing agent architectures）**  
   基于错误分类动态调整策略，实现自动恢复

4. **跨领域验证**  
   将框架应用于法律、医疗、供应链等其他高风险行业场景

---

## ✅ 总结一句话

> 本文提出了首个面向多智能体 LLM 系统的**细粒度工具调用可靠性诊断框架**，揭示了**工具初始化失败是小模型的主要瓶颈**，并通过大规模实验证明：**qwen2.5:14b 是性价比最优的生产级 open-weight 模型，而 qwen2.5:32b 可实现与 GPT-4.1 相当的零错误表现**，为资源受限组织提供了可靠、可控、可审计的智能体部署路径。

</details>

---

### 3. [LongCat-Flash-Thinking-2601 Technical Report](https://arxiv.org/abs/2601.16725)

**Authors**: Meituan LongCat Team, Anchun Gui, Bei Li, Bingyang Tao, Bole Zhou, Borun Chen, Chao Zhang, Chao Zhang, Chen Gao, Chen Zhang, Chengcheng Han, Chenhui Yang, Chuyu Zhang, Cong Chen, Cunguang Wang, Daoru Pan, Defei Bu, Dengchang Zhao, Di Xiu, Dishan Liu, Dongyu Ru, Dunwei Tu, Fan Wu, Fengcheng Yuan, Fengcun Li, Gang Xu, Guanyu Wu, Guoyuan Lin, Haibin Wang, Hansi Yang, Hao Yang, Haonan Yan, Haoxiang Ma, Haoxing Wen, Hongyan Hao, Hongyin Tang, Hongyu Zang, Hongzhi Ni, Hui Su, Jiacheng Zhang, Jiahong Zhou, Jiahuan Li, Jiaming Wang, Jian Yang, Jianfei Zhang, Jianhao Xu, Jianing Wang, Jiapeng Zhu, Jiaqi Sun, Jiarong Shi, Jiarui Zhao, Jingang Wang, Jinluan Yang, Jinrui Ding, Jinwei Xiao, Jiyuan He, Juncan Xu, Kefeng Zhang, Keheng Wang, Li Wei, Lianhui Ma, Lin Qiu, Lingbing Kong, Lingchuan Liu, Linsen Guo, Mengshen Zhu, Mengxia Shen, Mingyang Zhu, Peiguang Li, Peng Pei, Pengcheng Jia, Pengtao Zhang, Peng Zhao, Qi Gu, Qiong Huang, Qiyuan Duan, Quanchi Weng, Rongxiang Weng, Rongzhi Zhang, Rumei Li, Shanglin Lei, Shengnan An, Shijun Dai, Shuaikang Liu, Shuang Zhou, Shuo Wang, Songyuan Zhao, Tao Liang, Tianhao Hu, Tianze Chen, Wei Liu, Wei Shi, Wei Wang, Weifeng Tang, Wenjie Shi, Wenlong Zhu, Wentao Chen, Wentao Shi, Xi Su, Xiangcheng Liu, Xiandi Ma, Xiangyu Xi, Xiangyuan Liu, Xiangzhou Huang, Xiao Liu, Xiaodong Cai, Xiaolong Chen, Xiaowei Shi, Xiaoyu Li, Xin Chen, Xingchen Liu, Xuan Huang, Xuezhi Cao, Xunliang Cai, Yan Chen, Yang Bai, Yang Liu, Yang Yang, Yang Zheng, Yaoming Wang, Yaoming Zhu, Yaqi Huo, Yanyu Chen, Yaorui Shi, Yerui Sun, Yi Zhang, Yihao Chen, Yi-Kai Zhang, Yifan Lu, Yifan Zhao, Yitao Zhai, Yongjing Yin, Yongwei Zhou, Youshao Xiao, Yuchuan Dai, Yuchen Xie, Yuchen Yu, Yufei Zhang, Yuhuai Wei, Yulei Qian, Yunfan Liang, Yunke Zhao, Yuwei Jiang, Yuxin Bian, Yuxin Chen, Yuxin Liu, Yue Xu, Yueqing Sun, Zeyang Yu, Zhao Yang, Zhengsheng Huang, Zhengyu Chen, Zhijian Liu, Zhikang Xia, Zhimin Lin, Zhiyuan Yao, Zhuofan Chen, Zhuowen Han, Zijian Zhang, Ziran Li, Ziwen Wang, Ziyuan Zhuang  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.16725v1  

#### Abstract
We introduce LongCat-Flash-Thinking-2601, a 560-billion-parameter open-source Mixture-of-Experts (MoE) reasoning model with superior agentic reasoning capability. LongCat-Flash-Thinking-2601 achieves state-of-the-art performance among open-source models on a wide range of agentic benchmarks, includi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LongCat-Flash-Thinking-2601 Technical Report 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本论文旨在解决当前**agentic reasoning（代理式推理）模型在复杂现实任务中泛化能力弱、鲁棒性差、训练效率低**的问题。具体挑战包括：
- **真实世界环境的不完美性**：工具调用失败、返回噪声、用户指令模糊等。
- **长程交互与多轮决策**：传统模型难以处理长上下文、多步骤、状态依赖的交互轨迹。
- **缺乏高质量的agentic训练数据**：真实世界中的agent行为数据稀疏且不可执行。
- **跨领域泛化能力不足**：现有模型在未见过的工具组合或领域中表现不佳。

### 提出的新方法与创新点
论文提出了 **LongCat-Flash-Thinking-2601**，一个拥有 **560B 总参数、27B 激活参数**的开源 MoE 推理模型，并围绕其构建了一套端到端的训练框架，核心创新如下：

#### （1）**Environment Scaling 与 Multi-Domain Environment Training**
- **自动化环境构建管道**：从高阶领域定义出发，自动生成可执行的 **tool schema、database schema 和 tool dependency graph**，确保逻辑一致性与可验证性。
- **可控的图扩展策略**：通过 BFS 式扩展工具子图，并仅添加依赖已满足的节点，保证环境的**可执行性**与监督信号的可靠性。
- **支持超过 20 个领域的多样化环境**，每个环境包含 >60 个工具，形成密集依赖图，极大提升训练复杂度与泛化潜力。

> ✅ **优势**：解决了 agentic 数据稀缺问题，提供了大规模、多样化、可验证的训练 playground，促进跨域技能迁移。

#### （2）**Robust Agentic Training under Noisy Environments**
- **系统性分析现实噪声模式**，识别两大类噪声：
  - **Instruction Noise**：用户表达模糊、歧义、不完整。
  - **Tool Noise**：工具调用失败、返回部分结果、输出含噪。
- 设计**自动化噪声注入管道**，在训练中逐步引入多类型、多层级噪声。
- 采用**课程学习（curriculum learning）策略**，从轻度扰动开始，随模型鲁棒性增强而提高噪声强度。

> ✅ **优势**：显著提升模型在非理想现实环境下的稳定性与成功率，缩小“训练-部署”鸿沟。

#### （3）**Heavy Thinking Mode for Test-Time Scaling**
- 在推理阶段引入**并行深度思考机制**：
  1. **Parallel Reasoning**：多个 thinker 并行生成不同的推理路径。
  2. **Heavy Thinking**：由 summary model 对多条路径进行反思、聚合与精炼，输出最终答案。
- 支持 **context memory** 以维持多轮对话状态。
- 可结合额外的 RL 阶段优化 summary 能力。

> ✅ **优势**：实现推理宽度（width）与深度（depth）的同时扩展，显著提升复杂任务性能，优于单纯增加 CoT 长度或 Self-Consistency。

---

## 2. 核心实验方法和设置

### 使用的数据集

| 类别 | 数据集 | 描述 |
|------|-------|------|
| **数学推理** | AIME-25, HMMT-25, IMO-AnswerBench, AMO-Bench (EN/CH) | 奥赛级数学题，强调 tool-integrated reasoning（如代码执行） |
| **代理搜索** | BrowseComp, BrowseComp-zh, RWSearch | 多跳网页浏览与信息检索任务，需主动使用 search/browsing 工具 |
| **代理工具使用** | T2-Bench, VitaBench, Random Complex Tasks | 复杂工具链调用，涵盖零售、航空、电信等领域 |
| **鲁棒性测试** | T2-Noise, Vita-Noise | 在原始基准上注入系统性噪声后的变体 |
| **通用问答** | GPQA-Diamond, HLE | 高难度知识密集型推理 |
| **编程能力** | LiveCodeBench, OJBench, OIBench, SWE-bench Verified | 编码与软件工程代理任务 |

### 实验设置与评估指标
- **模型架构**：MoE，560B total params，27B activated per token。
- **推理配置**：temperature=1.0, top-k=-1, top-p=1.0（除非特别说明）。
- **评估指标**：
  - `Pass@k` / `Avg@k`：k 次采样中至少一次成功的比例（常用 k=1, 4, 16）。
  - 特别关注 **Heavy Mode 下的性能增益**。
- **上下文管理**：采用 Hybrid 策略（summary + discard-all），阈值设为 80K tokens。

### 基线方法对比
- **开源模型**：
  - DeepSeek-V3.2-Thinking
  - Kimi-K2-Thinking
  - Qwen3-235B-A22B-Thinking-2507
  - GLM-4.7-Thinking
- **闭源模型**（引用官方报告）：
  - GPT-5.2-Thinking-xhigh
  - Claude-Opus-4.5-Thinking
  - Gemini-3-Pro

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| Benchmark | LongCat-Flash-Thinking-2601 | 最佳基线（开源） | 最佳闭源 |
|----------|-------------------------------|------------------|-----------|
| **BrowseComp (Pass@1)** | **73.1**（启用 context mgmt） | 67.6（DeepSeek-V3.2） | 65.8 |
| **BrowseComp-zh (Pass@1)** | **77.7** | 66.6 | — |
| **RWSearch (Pass@1)** | **79.5** | 74.0 | 82.0 |
| **T2-Bench (Avg@4)** | **88.2** | 87.4 | 90.7 |
| **VitaBench (Avg@4)** | **29.3** | 24.0 | 31.5 |
| **AIME-25 (Avg@16)** | **99.6**（Heavy Mode 达 100.0） | 99.1 | 100.0 |
| **IMO-AnswerBench (Avg@4)** | **78.6**（Heavy Mode 达 **86.8**） | 84.0 | 86.7 |
| **AMO-Bench EN (Avg@16)** | **61.6**（Heavy Mode 达 **66.0**） | 62.4 | 72.5 |
| **AMO-Bench CH (Avg@16)** | **56.8**（Heavy Mode 达 **67.5**） | 35.1 | 74.9 |
| **OIBench EN (Pass@1)** | **42.2** | 44.6 | 61.2 |

> 🔥 **结论**：在几乎所有 **agentic 任务** 上达到 **开源模型 SOTA**，并在部分指标上接近甚至超越最强闭源模型。

### 消融实验结果（Table 1）

| Dataset | ColdStart | Training w/o Noise | Training w/ Noise |
|--------|-----------|--------------------|-------------------|
| **VitaBench (Avg@4)** | 10.0 | 28.6 | **29.3** |
| **VitaBench-Noise (Avg@4)** | 6.3 | 13.3 | **20.5** |
| **Tau2Bench (Avg@4)** | 78.8 | 87.1 | **88.2** |
| **Tau2Bench-Noise (Avg@4)** | 58.8 | 62.2 | **67.1** |

> ✅ **发现**：
> - 引入 **noise training** 显著提升在噪声环境下的鲁棒性（+7.2 pts on VitaBench-Noise）。
> - 即使在干净环境中，noise training 也能带来轻微增益，表明其增强了模型的泛化与纠错能力。

---

## 4. 关键结论和发现

### 主要发现
1. **统一的端到端训练框架是构建强 agentic 模型的关键**：从 pre-training → mid-training → post-training 全流程协同设计，才能有效提升代理能力。
2. **环境多样性驱动泛化**：通过 **Environment Scaling** 构建的跨域、异构环境集合，是培养通用工具使用能力的基础。
3. **显式建模现实噪声可大幅提升鲁棒性**：在训练中注入 instruction 和 tool noise，能有效应对现实世界的不确定性。
4. **Heavy Thinking Mode 是有效的 test-time scaling 范式**：通过并行探索 + 集成反思，显著提升复杂任务性能，尤其在数学与工具集成场景。
5. **LongCat-Flash-Thinking-2601 是当前最强的开源 agentic 模型**：在 agentic search、tool use 等任务上全面领先。

### 方法的局限性
- **计算成本极高**：训练涉及数万并发环境与数千加速器，对基础设施要求严苛。
- **Heavy Thinking 模式延迟较高**：并行多路径推理带来显著的推理开销，不适合低延迟场景。
- **噪声建模仍为简化版本**：真实世界噪声更复杂，当前注入方式可能未能完全覆盖所有失效模式。
- **评估仍受限于合成环境**：尽管有 RWSearch 等真实查询，但多数 benchmark 仍基于模拟器。

### 未来工作方向
- **降低训练门槛**：探索更高效的分布式 RL 框架，支持中小规模团队复现。
- **优化 Heavy Thinking 推理效率**：研究动态 early stopping、路径剪枝等技术减少冗余计算。
- **构建更真实的 agentic 评估平台**：推动基于真实 API、真实用户行为的长期交互 benchmark。
- **探索 agent 自我演化能力**：让 agent 在部署中持续学习与适应新工具与环境。
- **发布 LongCat-Flash-Thinking-ZigZag**：支持高达 1M tokens 的上下文，进一步释放长程推理潜力。

---

> 📢 **补充亮点**：论文还开源了多个关键组件：
> - **ZigZag Attention**：一种可在 mid-training 阶段将 full-attention 模型转为 sparse 的高效注意力机制，实现约 1.5× 推理加速。
> - **AMO-Bench 英文版** 与 **BrowseComp-ZH 修正版**：提升社区评估一致性。
> - **Noise Injection Pipeline** 与 **Random Complex Tasks 生成器**：支持未来鲁棒性研究。

</details>

---

### 4. [Better Generalizing to Unseen Concepts: An Evaluation Framework and An LLM-Based Auto-Labeled Pipeline for Biomedical Concept Recognition](https://arxiv.org/abs/2601.16711)

**Authors**: Shanshan Liu, Noriki Nishida, Fei Cheng, Narumi Tokunaga, Rumana Ferdous Munne, Yuki Yamagata, Kouji Kozaki, Takehito Utsuro, Yuji Matsumoto  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.16711v1  

#### Abstract
Generalization to unseen concepts is a central challenge due to the scarcity of human annotations in Mention-agnostic Biomedical Concept Recognition (MA-BCR). This work makes two key contributions to systematically address this issue. First, we propose an evaluation framework built on hierarchical c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Better Generalizing to Unseen Concepts: An Evaluation Framework and An LLM-Based Auto-Labeled Pipeline for Biomedical Concept Recognition

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

本研究聚焦于 **Mention-agnostic Biomedical Concept Recognition (MA-BCR)** 中一个核心挑战：**模型对未见概念（unseen concepts）的泛化能力不足**。由于人工标注成本高昂，现有的 **Manual-Labeled Datasets (MLDs)** 覆盖的生物医学概念仅占整个本体（ontology）的一小部分（如 HPO 仅覆盖约 2.35%），导致模型难以识别训练中未出现的概念。

此外，传统评估方式（如 micro-F1）无法有效衡量模型在“未命中目标”时是否“接近正确答案”，缺乏对层次化结构感知的评估机制。

---

### **提出了什么新方法或新思路**

#### （1）提出了一套**层次化评估框架**（Hierarchical Evaluation Framework）
- **构建三种 Hierarchical Search Index**：
  - **OSI** (Ontology-aware Search Index)：基于本体结构（parent-child 关系）构建图。
  - **SSI** (Semantic Search Index)：基于 SapBERT 嵌入的语义相似性构建图。
  - **OSSI** (Ontology-Semantic Search Index)：融合本体结构与语义相似性的混合图。
- 图通过 **Louvain 算法**进行递归划分，生成具有层级索引（如 `0-1-2`）的标签树，使预测可映射到层级路径上。

#### （2）设计两个新的评估指标，专门用于衡量对 **unseen concepts** 的泛化能力：
- **U-RC (Unseen Recall-oriented Closeness)**：衡量预测与真实未见概念在层级树中的**最长公共前缀长度**，反映“有多接近”。
- **U-CS (Unseen Candidate-set Size)**：衡量模型预测所能缩小的候选概念集合大小（调和平均），反映“搜索空间缩减能力”。

#### （3）首次为 MA-BCR 设计了一个 **LLM-based Auto-Labeled Data (ALD) Pipeline**
- 利用 LLM 自动生成 passage-level 的概念标注，无需依赖 mention spans。
- 流程包括五阶段：
  1. **PCC** (Passage-to-Claim-to-Concept)：将文本分解为独立 claim，再生成候选概念。
  2. **CC** (Concept Classification)：四分类判断概念是 explicit / logically implicit / pragmatically implicit / not relevant。
  3. **RL** (Relabeling)：LLM 对预测进行修正，补充缺失或删除错误概念。
  4. **GF** (Guideline-based Filtering)：依据 CRAFT 等标注指南过滤无效标注。
  5. **QS** (Quality-based Selection)：按质量评分（1–5级）筛选高质量样本。

该 pipeline 可扩展地生成大规模 ALD，显著提升概念覆盖率（如 HPA 达 69.33%，HoIP 达 54.40%）。

---

### **相比现有方法的优势**

| 维度 | 本文方法优势 |
|------|--------------|
| **任务设定** | 支持 **mention-agnostic** 设置，更符合实际生物医学文献表达（常隐含概念）。 |
| **评估能力** | 首次提供对 **unseen concept 泛化能力** 的量化工具（U-RC/U-CS），弥补传统 F1 的盲区。 |
| **数据可扩展性** | LLM-based pipeline 可低成本生成大规模 ALD，缓解 MLD 稀疏问题。 |
| **结构利用** | 显式建模本体层次结构，使模型能学习“即使没猜中也能靠近”的推理能力。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 类型 | 数据集 | 描述 |
|------|--------|------|
| **Manual-Labeled Datasets (MLDs)** | **HPA-MLD** (HPO GSC+) | 包含 228 篇摘要，标注 HPO 中 Human Phenotypic Abnormality 概念。 |
| | **HoIP-MLD** (CRAFT 子集) | 从 CRAFT corpus 提取，标注 Homeostasis Imbalance Process 概念。 |
| **Auto-Labeled Datasets (ALDs)** | **HPA-ALD**, **HoIP-ALD** | 使用 LLM pipeline 自动标注，分别包含 54,301 和 34,097 篇 PubMed abstracts。 |

> ✅ ALD 构建方式：以每个概念名称作为 query，检索最多 10 篇最新 PubMed abstracts，并去重。

---

### **实验设置**

- **模型选择**：采用 **MA-COIR**（BART-based seq2seq 模型），输出 hierarchical concept indices。
- **训练策略**：
  - HPA：仅在 ALD 上训练（因 MLD 太小），测试在 MLD。
  - HoIP：两种设置：
    - MLD→MLD：在 MLD 上训练，在 MLD 上测试。
    - ALD→MLD：在 ALD 上训练，在 held-out MLD 上测试。
- **控制变量**：
  - 至少 30% 的测试概念在训练集中为 **unseen**。
  - 控制训练数据量 $ M = 200 \times 2^k $，验证数据规模影响。

---

### **评估指标**

| 指标 | 公式简述 | 含义 |
|------|---------|------|
| **micro-F1** | 标准精确率/召回率/F1 | 是否**完全匹配**正确概念 |
| **U-RC** | $\frac{1}{|G_{us}|} \sum_{g \in G_{us}} \max_{\hat{y}} \text{lcp}(I_g, I_{\hat{y}})$ | 在层级树中离真实概念**多近** |
| **U-CS** | 调和平均 $\left( \frac{1}{|G_{us}|} \sum \frac{1}{S(p,g)} \right)^{-1}$ | 预测能将搜索空间缩小到多少个候选 |

> 📌 所有指标均可应用于任意 recognizer，只需将其输出映射至 OSI/SSI/OSSI 索引即可。

---

### **基线方法对比**

- 主要对比不同训练数据来源下的同一模型（MA-COIR）表现：
  - **MLD-trained** vs **ALD-trained**
- 不同索引类型之间的比较（OSI vs SSI vs OSSI）
- 无端到端对比其他架构（如 mention-based NER+EL），因其不兼容 hierarchical index 输出。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 3）**

#### **HoIP 任务上的 ALD vs MLD 训练效果对比**

| 训练数据 | 数据量 | micro-F1 | U-RC (OSSI) | U-CS (SSI) |
|--------|-------|----------|-------------|------------|
| HoIP-MLD | 2,458 | **78.6** | 30.5 | 42.1 |
| HoIP-ALD | 16,415 | 17.7 | **41.4** | **33.2** |

> 🔍 尽管 ALD 模型的 F1 远低于 MLD 模型，但在 **U-RC 和 U-CS 上全面反超**，说明其泛化能力更强。

#### **随 ALD 数据量增加的趋势（HoIP-ALD, SSI index）**

| 数据量 | F1 | U-RC | U-CS |
|-------|-----|------|------|
| 200 | 13.7 | 28.6 | 129.1 |
| 6,400 | 15.7 | 38.4 | 35.0 |
| 16,415 | 17.6 | **38.0** | **33.2** |

✅ **趋势明确**：随着 ALD 规模增大，**U-RC 持续上升，U-CS 显著下降**，表明模型越来越能“接近”并“缩小搜索范围”到未见概念。

#### **HPA 任务上的 ALD 效果（更高质 ALD）**

| 数据量 | F1 | U-RC (OSI) | U-CS (OSI) |
|-------|-----|-----------|-----------|
| 200 | 19.1 | 26.5 | 100.6 |
| 47,152 | **36.7** | **46.1** | **20.1** |

✅ 在 HPA 上，**F1、U-RC、U-CS 均随数据增长而持续提升**，说明高质量 ALD 可同时提升准确性和泛化性。

---

### **消融实验结果**

#### （1）Auto-labeling Pipeline 各模块有效性（Table 2）

| Pipeline 阶段 | HPA F1 | HoIP F1 |
|--------------|--------|--------|
| PCC（仅生成） | 53.0 | 11.4 |
| +CC+RL+GF+QS | **55.4** | **27.6** |

✅ 所有模块均带来增益，尤其是 **Concept Classification** 和 **Relabeling** 显著提升 precision。

#### （2）False Positive 错误分析（Figure 4）

在 HoIP 上 FP 中：
- **Context over-generalization**: 29.8%
- **Inferential overreach**: 26.9%
- **Missing gold**（实际应为正例）: 17.3%

➡️ 表明 LLM 容易过度推断，但也揭示部分“假阳性”实为标注遗漏。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **LLM-generated ALD 虽不能替代 MLD**（F1 更低），但能**显著增强对 unseen concepts 的泛化能力**。
2. ✅ **扩大 ALD 数据量可系统性提升 U-RC 和 U-CS**，尤其在高覆盖场景下（如 HPA），甚至带动 F1 上升。
3. ✅ **Hierarchical indexing 是关键桥梁**：它使得噪声监督信号变得“可学习”，让模型即使预测不准也能学到结构知识。
4. ✅ **U-RC 和 U-CS 是有效的下游有用性代理指标**：
   - 在 re-ranking 实验中，**reranker F1 与 U-RC 正相关（ρ=0.874），与 U-CS 负相关（ρ=-0.814）**，远强于与 micro-F1 的相关性（ρ=0.554）。
5. ✅ **OSI、SSI、OSSI 各有优势**：
   - OSI 更利于 closeness（U-RC）
   - SSI 更利于 candidate set reduction（U-CS）
   - OSSI 综合表现最佳

---

### **方法的局限性**

| 局限性 | 说明 |
|--------|------|
| **Auto-labeling 在复杂本体上性能有限** | 如 HoIP 概念细粒度高，LLM 缺乏预训练覆盖，导致 recall 低。 |
| **ALD 覆盖仍不完整** | 当前 HPA 覆盖 69.33%，HoIP 仅 54.40%，部分概念无法检索到相关 abstract。 |
| **Indexing 策略仍有改进空间** | 当前图划分方法较简单，未来可引入自适应加权、重要性建模等。 |
| **训练效率瓶颈** | 使用 BART 等 encoder-decoder 模型处理大规模 ALD 时训练耗时长。 |
| **自动评估可靠性问题** | 使用 GPT-4o-mini 作为 judge 仍存在与人工标准对齐偏差，需人工校准。 |

---

### **未来工作方向**

1. **改进 auto-labeling pipeline**：
   - 引入多 LLM 协作，减少误差传播。
   - 结合 active learning 动态优化标注重点。
2. **提升 ALD 覆盖率与质量**：
   - 扩展检索源（如 PMC full text）。
   - 引入 data synthesis 技术生成稀缺概念上下文。
3. **优化 hierarchical indexing**：
   - 探索 learned fusion 机制整合本体与语义信号。
   - 引入动态索引适应不同任务需求。
4. **提高训练效率**：
   - 使用 distillation、continual learning 或轻量架构加速训练。
5. **推动 unseen concept recognition 应用落地**：
   - 构建面向 discovery 的 biomedical IE 工具链。
   - 将 U-RC/U-CS 作为 early-stop 或 model selection 指标。

---

> 💡 **一句话总结**：  
> 本文提出了一种“**用大规模弱监督数据 + 层次化结构引导 + 新型泛化评估指标**”的新范式，证明了 **LLM-generated ALD 是提升 MA-BCR 模型对未见概念泛化能力的有效途径**，为低资源、高泛化需求的生物医学信息抽取提供了新思路。

</details>

---

### 5. [Curate-Train-Refine: A Closed-Loop Agentic Framework for Zero Shot Classification](https://arxiv.org/abs/2601.16530)

**Authors**: Gaurav Maheshwari, Kevin El Haddad  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.16530v1  

#### Abstract
Large language models (LLMs) and high-capacity encoders have advanced zero and few-shot classification, but their inference cost and latency limit practical deployment. We propose training lightweight text classifiers using dynamically generated supervision from an LLM. Our method employs an iterati...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Curate-Train-Refine: A Closed-Loop Agentic Framework for Zero Shot Classification*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在 **zero-shot** 和 **few-shot text classification** 场景中，主流方法依赖于大型语言模型（LLMs）进行推理（如 prompt-based 分类），虽然有效，但存在以下问题：
- **高推理成本与延迟**：每次预测都需要调用 LLM，难以部署到生产环境。
- **对 prompt 设计敏感**：性能受提示工程影响大。
- **小模型需要高质量监督信号**：轻量级分类器（如 SetFit、EuroBERT）虽高效，但在极低资源下表现不佳。

### 🚀 提出的新方法：Curate-Train-Refine 框架
作者提出一个 **闭环的 agentic 框架**，将 LLM 作为“数据策展人”（data curator），而非推理引擎。该框架包含三个阶段循环迭代：

1. **Curate（策展）**：LLM 基于标签语义生成初始训练数据。
2. **Train（训练）**：使用生成的数据训练一个轻量级分类器（如 SetFit）。
3. **Refine（精炼）**：将分类器在验证集上的错误反馈给 LLM，由其分析失败模式并生成针对性样例（如 hard negatives、negation cases 等）以修正缺陷。

> 🔁 此过程形成一个 **closed-loop generate-evaluate-refine 循环**，无需人工干预即可持续优化训练数据质量。

### ⭐ 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 推理效率 | 依赖 LLM → 高延迟、高成本 | 轻量模型 → 低延迟、低成本 |
| 数据质量 | 静态生成或人工标注 → 固定不变 | 动态生成 → 针对模型弱点自适应优化 |
| 泛化能力 | Prompt 敏感、上下文长度限制 | 不依赖测试时 LLM 调用 |
| 可扩展性 | 扩展困难 | 支持多轮迭代提升性能 |

> 💡 创新点总结：**首次将 LLM 用于构建闭环的数据蒸馏流程，在离线阶段完成知识迁移，实现“高性能 + 高效率”的统一。**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
共四个标准文本分类 benchmark，覆盖多种任务类型：

| 数据集 | 任务类型 | 类别数 | 描述 |
|-------|--------|------|------|
| **SST-5** | 细粒度情感分析 | 5 | 电影评论（very negative → very positive） |
| **Emotion** | 情绪识别 | 6 | 短文本情绪分类（joy, anger, fear 等） |
| **CR** | 产品评论情感 | 2 | 客户评论正负向判断 |
| **AG News** | 新闻主题分类 | 4 | World, Sports, Business, Sci/Tech |

### ⚙️ 实验设置
- **零样本设置（Zero-shot）**：不提供任何真实标注样本，仅输入 label 名称。
- **少样本设置（Few-shot）**：每类提供 k=2, 4, 8 个真实样本。
- **backbone 模型**：
  - **SetFit**：基于 sentence-transformers 的 few-shot 框架（使用 `all-mpnet-base-v2` 编码器）
  - **EuroBERT**：多语言编码器，用于对比通用 encoder 表现
- **LLM 角色**：使用 GPT-5 作为 agent，通过 Hugging Face 的 `smolagents` 框架实现 ReAct-style agent 控制流程。
- **评估指标**：Accuracy（准确率）

### 🆚 对比的基线方法
| 方法 | 简介 |
|-----|------|
| **Baseline** | 原始 SetFit / Encoder 训练 |
| **Baseline + Prompt** | 加入静态 prompt 提升 |
| **Baseline + Manager** | 本文提出的 agentic loop 方法 |
| **AncSetFit** | 引入 anchor statements 注入 label semantics |
| **ADAPET** | Pattern-exploiting training for few-shot tuning |
| **GLiClass 3.0** | 现成 zero-shot 分类器（151M 参数） |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & 2）

#### ✅ 零样本（k=0）表现（SetFit backbone）

| 方法 | SST-5 | Emotion | CR | AG News |
|------|-------|--------|----|--------|
| GLiClass 3.0 | 43.8 | **51.9** | 90.2 | 68.8 |
| **Ours (Manager)** | **47.0**↑ | 50.9 | **87.8** | **82.6**↑ |

> ✅ 在 **3/4 数据集上超越更大规模的 GLiClass 模型**（尽管自身 encoder 更小：110M vs 151M）

#### ✅ 少样本（k=2~8）表现趋势

| 方法 | Emotion (k=2) | CR (k=2) | AG News (k=2) |
|------|--------------|----------|----------------|
| SetFit Baseline | 30.0 | 75.5 | 65.3 |
| +Prompt | 39.1 | 89.5 | 69.4 |
| **+Manager (ours)** | **56.5**↑↑ | **89.0** | **83.2**↑↑ |

> ✅ 在 **k=2 极端少样本场景下显著领先**，尤其在 Emotion 上提升超 17 个百分点。

#### 🔍 性能随样本增加的变化趋势
- 多数数据集（如 SST-5, CR）在 k=2 后增益递减 → 表明合成数据已快速饱和模型学习能力。
- **例外是 AG News**：随着 k 增加仍持续提升 → 其 label semantics 更复杂，需更多引导。

### 🧪 消融实验结果（Ablation Study）
从 Table 1 可看出：
- **Baseline → +Prompt**：有一定提升，说明 label semantics 有帮助。
- **+Prompt → +Manager**：进一步显著提升 → 验证了 **error-driven feedback loop 的有效性**。
- 特别是在 zero-shot 下，静态 prompt 提升有限，而动态 refinement 能带来质变。

> ✅ 结论：**闭环迭代机制远优于一次性静态生成（one-off generation）**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM 可作为高效的数据策展者（Data Curator）**  
   > 即使不参与推理，也能通过动态生成高质量、有针对性的训练数据大幅提升下游小模型性能。

2. **Curate-Train-Refine 框架显著缩小了 zero-shot 与 few-shot 的差距**  
   > 在零样本条件下，本方法性能甚至超过使用 8 个真实样本的传统方法（如在 SST-5 上）。

3. **轻量模型 + 动态数据生成 = 高效且高性能的解决方案**  
   > 成功解耦训练与部署：训练阶段利用 LLM 生成数据，部署阶段完全脱离 LLM，实现低延迟、低成本服务。

4. **SetFit 比通用 encoder（如 EuroBERT）更适合此类任务**  
   > 因其 contrastive fine-tuning 机制更适配少量样本下的语义判别。

---

### ⚠️ 局限性
1. **依赖强大 LLM 生成能力**  
   > 当前实验基于 GPT-5，若换用较小 LLM，可能无法准确诊断错误或生成高质量样本。

2. **生成数据的质量控制挑战**  
   > 存在重复、偏差或语义漂移风险，需加入去重、多样性约束等机制。

3. **未引入人类反馈**  
   > 完全自动化可能导致错误累积，未来可考虑 human-in-the-loop 校正。

4. **计算开销集中在训练阶段**  
   > 虽然推理快，但多次 loop 导致训练时间增长，不适合快速迭代场景。

---

### 🔮 未来工作方向
1. **拓展至结构化预测任务**  
   > 如 NER、Relation Extraction 等，探索是否同样适用。

2. **增强 error analysis 工具**  
   > 引入 calibration-aware、robustness-aware diagnostics，让 LLM 更精准定位问题。

3. **研究不同 LLM backbone 的影响**  
   > 探索开源或更小 LLM 是否也能胜任 curator 角色。

4. **探索 human-agent collaboration**  
   > 在关键节点引入人工审核，提高数据可信度。

5. **优化 generation budget 与收敛策略**  
   > 自动判断何时停止 loop，避免无效迭代。

---

## ✅ 总结一句话
> 本文提出了 **首个闭环的 agentic 数据生成框架 Curate-Train-Refine**，证明了 **LLM 可作为强大的离线数据策展工具**，在无需测试时调用大模型的前提下，显著提升轻量分类器在 zero/few-shot 场景下的性能，为高效 NLP 部署提供了新范式。

</details>

---

### 6. [W4A16 Mixed-Precision Matrix Multiplication on Decoupled Architecture: Kernel Design and Memory Bottleneck Analysis for Ascend NPUs](https://arxiv.org/abs/2601.16536)

**Authors**: Yuanhong He, Peiyu Niu, Jun Chen, Chenchen Zhang, Chao Yang  
**Category**: cs.DC  
**Published**: 2026-01-26  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.16536v1  

#### Abstract
As Large Language Models (LLMs) scale, weight-only quantization (W4A16: 4-bit weights, 16-bit activations) becomes critical for reducing memory footprint with minimal accuracy loss. However, its efficient deployment on Huawei's Ascend 910 Neural Processing Unit (NPU) is challenging due to limited na...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*W4A16 Mixed-Precision Matrix Multiplication on Decoupled Architecture: Kernel Design and Memory Bottleneck Analysis for Ascend NPUs*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对 **华为 Ascend 910 NPU** 上缺乏对 **W4A16 mixed-precision matrix multiplication**（4-bit 权重、16-bit 激活）原生支持的问题，提出了一种高效的内核实现方案。由于 Ascend 架构采用 **decoupled compute architecture**（解耦计算架构），其 cube core 和 vector core 分离调度，无法像 NVIDIA GPU 那样高效融合 dequantization 与 GEMM 运算，导致低比特量化在该平台难以发挥性能优势。

### 🚀 提出的新方法与创新思路
- **首个面向 Ascend 910 的 W4A16 GEMM 内核实现**：  
  利用 vector core 执行 **on-the-fly INT4-to-FP16 dequantization**，cube core 负责高吞吐量的 FP16 GEMM 计算，实现了混合精度矩阵乘法的端到端执行。
  
- **Split-K 并行化策略优化负载均衡**：  
  在输出维度较小（如 LLM 解码阶段 M 或 N 较小）时，Split-K 可更均匀地分配计算任务至多个 AI Core，提升资源利用率。

- **双缓冲（double buffering）与硬件事件同步机制**：  
  通过 MTE（Memory Transfer Engine）与计算单元之间的硬件事件驱动同步，隐藏数据搬运延迟，构建高效流水线。

- **揭示内存瓶颈本质**：  
  发现性能瓶颈并非来自 dequantization 计算本身，而是 **dequantized weights 在 global memory 中的额外传输开销**。

### 🔍 相比现有方法的优势
| 对比维度 | 本文方法 | 现有方法（如 PyTorch on Ascend / Data-Parallel） |
|--------|--------|---------------------------------------------|
| 支持 W4A16 | ✅ 完整支持 | ❌ 无原生支持 |
| 性能优化 | 利用 Split-K + 异构核心协同 | 仅使用 data-parallel，负载不均 |
| 内存效率 | 减少权重存储占用 4× | 存储仍为 FP16 |
| 实际加速比 | 最高达 1.74×（vs data-parallel） | 基准水平 |

> ⚠️ 注意：尽管权重压缩 4×，但由于需将 dequantized weights 写回 global memory 供 cube core 使用，实际未达到理论 ~4× 加速。

---

## 2. 核心实验方法和设置

### 📊 实验设置
- **硬件平台**：Huawei Ascend 910 NPU
- **编程模型**：基于 Ascend C 开发，利用 CANN 工具链进行底层控制
- **输入形式**：
  - 激活矩阵 $ A \in \text{FP16}^{M \times K} $
  - 量化权重 $ W \in \text{INT4}^{K \times N} $
  - 输出 $ C = A \cdot \text{Dequant}(W) $

- **矩阵规模设计**：覆盖典型 LLM 推理场景，尤其是 **decode phase** 特征（小 batch size，大 K）
  - 多组 $(N, K)$ 配置：如 (1536, 6144), (2048, 8192), (7168, 18432)
  - Batch size $M$ 范围：从 1 到 128

### 🎯 评估指标
- **Execution Time**（执行时间）
- **Speedup**（相对于 baseline 的加速比）
- **Memory Bandwidth Utilization**
- 不同 parallelization 策略下的可扩展性分析

### 🆚 基线方法对比
- **Data-Parallel W4A16 Kernel**：基于 CATLASS 实现的传统并行策略，作为主要对比基准
- **Native FP16×FP16 GEMM in PyTorch**：用于衡量 W4A16 相对于全精度的理论加速潜力

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）Split-K vs Data-Parallel 性能对比（图2）
- 当 $K > N$ 时（典型于 LLM 解码），Split-K 显著优于 data-parallel：
  - **加速比范围：1.01× ~ 1.74×**
  - 最佳表现出现在 $N=4096, K=16348$, batch=1 时，达到 **1.74×** 加速
- 小 batch size 下性能稳定，因 tile padding 补齐了计算粒度

#### （2）W4A16 vs FP16×FP16 性能对比（图3）
- 尽管权重体积减少 4×，但受限于额外 global memory 访问：
  - **最大仅实现 1.48× 加速**（batch=128, N=2048, K=10240）
  - 小 batch 下加速效果更弱（最低约 1.02×）

#### （3）内存瓶颈分析
- 实验验证：dequantization 计算本身不是瓶颈
- 主要开销来源于：
  - Vector core dequant 后写入 global memory
  - Cube core 再次从 global memory 读取 FP16 weights
  → 形成“**memory ping-pong**”效应，显著增加带宽压力

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Split-K 是适合 Ascend 架构的小批量 GEMM 并行策略**，尤其适用于 LLM decode 场景。
2. **W4A16 在 Ascend 上可行但未达预期加速**：虽然节省了存储空间，但因 **decoupled architecture 导致必须经由 global memory 传递中间结果**，限制了性能提升。
3. **真正的瓶颈是 memory traffic，而非 compute 或 dequantization latency** —— 这一发现挑战了普遍认为“dequantization 慢”的直觉假设。

### ⚠️ 方法的局限性
- **依赖 global memory 中转**：vector core 与 cube core 之间无直接通路，强制引入冗余数据移动。
- **无法完全释放低比特量化潜力**：理论上应接近 4× 存储压缩带来的带宽收益，但实际仅获得约 1.5× 加速。
- **Padding 开销影响小 batch 效率**：即使 batch=1，也需填充至最小 tile 单位，造成资源浪费。

### 🔮 未来工作方向
1. **硬件-软件协同设计（co-design）**：
   - 建议增加 **vector-to-cube direct data path**，避免 global memory 中转
   - 设计 **fused dequant-GEMM 指令**，在硬件层面融合操作
2. **探索其他量化格式适配**：如 W2/W3/W8 + A16 组合，在 Ascend 架构下重新权衡精度与效率
3. **优化 memory layout 与 cache reuse**：进一步减少重复访存
4. **支持更多 LLM kernels 的混合精度扩展**：将此设计范式推广至 attention、RMSNorm 等模块

---

## 总结一句话
> 本文首次实现了 **Ascend 910 上的 W4A16 GEMM 内核**，通过 **Split-K + 异构核心协作 + 双缓冲流水线** 实现最高 **1.74×** 加速，并揭示了 **decoupled architecture 下 global memory 数据往返才是性能瓶颈**，为后续低比特量化在专用 NPU 上的部署提供了重要实践指导与优化方向。

</details>

---

### 7. [Towards a Theoretical Understanding to the Generalization of RLHF](https://arxiv.org/abs/2601.16403)

**Authors**: Zhaochun Li (Beijing Institute of Technolegy, Zhongguancun Academy), Mingyang Yi (Renmin University of China), Yue Wang (Zhongguancun Academy), Shisheng Cui (Beijing Institute of Technolegy), Yong Liu (Renmin University of China)  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.16403v1  

#### Abstract
Reinforcement Learning from Human Feedback (RLHF) and its variants have emerged as the dominant approaches for aligning Large Language Models with human intent. While empirically effective, the theoretical generalization properties of these methods in high-dimensional settings remain to be explored....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Towards a Theoretical Understanding to the Generalization of RLHF 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文旨在解决 **Reinforcement Learning from Human Feedback (RLHF)** 在高维场景下的**泛化能力缺乏理论解释**的问题。尽管 RLHF 在实践中被广泛用于对齐大语言模型（LLMs）与人类意图，并取得了显著成功，但其理论基础，尤其是关于模型在未见数据上的泛化性能，仍不清晰。

现有理论工作通常依赖于**奖励模型参数的最大似然估计（MLE）的一致性**，这要求强数据覆盖假设（如特征空间满秩），而这一假设在高维、稀疏的实际场景中往往难以满足。此外，这些分析忽略了 RL 过程本身（如策略优化）对泛化的贡献。

### **提出了什么新方法或新思路**

本文提出了一种**端到端（end-to-end）的理论框架**来分析 RLHF 的泛化性能，其核心是基于 **algorithmic stability**（算法稳定性）理论。具体创新点如下：

- **端到端分析框架**：不同于以往将奖励建模和策略优化分离的分析，本文直接分析从数据到最终策略的完整学习过程，更符合实际训练流程。
- **关键假设：特征覆盖（Feature Coverage）**：提出并利用了一个新的“特征覆盖”假设（Assumption 5）。该假设认为，随着训练样本 `n` 的增加，经验协方差矩阵的列空间（column space）会逐渐扩展，以近似覆盖整个特征空间，而落在该子空间外的残差项（residual component）的范数以 `O(n⁻¹)` 的速度衰减。这比传统的“正定性”假设更弱、更现实。
- **统一分析多种优化器**：将算法稳定性框架应用于 KL 正则化的 RLHF 目标函数，并推导出对 **Gradient Ascent (GA)** 和 **Stochastic Gradient Ascent (SGA)** 等梯度法的显式泛化界。

### **相比现有方法的优势**

| 方面 | 现有方法 | 本文方法 |
| :--- | :--- | :--- |
| **分析视角** | 分离式（先奖励建模，再策略提取） | **端到端**（联合分析） |
| **核心假设** | 强数据覆盖（如协方差矩阵正定） | **弱特征覆盖**（残差随 `n` 衰减） |
| **适用性** | 高维下常不成立 | **更适用于高维、有限数据**的实际场景 |
| **分析对象** | 主要关注奖励参数一致性 | **直接分析策略的次优性差距（suboptimality gap）** |
| **优化过程** | 视为精确最大化 | **明确考虑 GA/SGA 等迭代算法的影响** |

---

## 2. 核心实验方法和设置

### **使用的数据集**

本文为**模拟实验**（simulation experiments），并未使用真实世界的数据集。作者人工构造了两种特征空间来验证理论：

1.  **正定协方差矩阵场景**：构建一个标准的正交基 `{x₁, ..., x_d}`，通过非均匀采样（某个方向概率极小）来控制数据覆盖情况。
2.  **不足覆盖（Insufficient Coverage）场景**：将特征空间限制在一个低维有效子空间 `A`（维度 `d_eff < d`）内。所有特征向量都位于此子空间，但经验协方差矩阵的秩受限于样本量 `n`，从而自然产生残差。

### **实验设置和评估指标**

- **目标**：验证理论推导的泛化界，特别是次优性差距（Suboptimality Gap）与样本量 `n` 和优化步数 `T` 的关系。
- **评估指标**：
  - **次优性差距 (Suboptimality Gap)**：定义为 `|J(π_test) - J(π_test*)|`，其中 `π_test` 是在测试集上评估的学习策略，`π_test*` 是最优策略。该值越小，表示泛化性能越好。
  - 该指标通过一个独立的大规模测试集（`size=15000`）来近似期望目标 `J(π)`。
- **实验设计**：
  1.  **Case 1**: 固定优化步数，研究次优性差距随样本量 `n` 的变化（验证 `O(n⁻¹)` 泛化界）。
  2.  **Case 2**: 固定样本量 `n`，研究次优性差距随优化步数 `T` 的变化（验证优化过程对泛化的正面影响）。
- **学习算法**：实现了 **Gradient Ascent (GA)** 和 **Stochastic Gradient Ascent (SGA)** 来优化策略参数。

### **基线方法对比**

本文没有与其他外部的 RLHF 算法进行性能对比。其“基线”是**理论预期**：
- 将实验结果与理论推导的边界（如 `O(n⁻¹)`, `O(T⁻¹/² + n⁻¹)`) 进行比较。
- 将不同算法（精确优化、GA、SGA）的结果进行内部对比，以验证理论分析。

---

## 3. 主要实验结果和性能指标

### **关键性能数据与对比结果**

实验结果通过 **Figure 1** 和 **Figure 2** 展示，完美地验证了理论预测：

1.  **次优性差距与样本量 `n` 的关系 (Figure 1a, 1b)**：
    - 当横坐标为 `n⁻¹/²` 时，所有曲线（精确优化、GA、SGA）均呈现**线性下降趋势**。
    - 这表明次优性差距的衰减速率为 `O(n⁻¹)`，与理论推导（Theorems 3, 5, 7）完全一致。
    - 证明了在特征覆盖假设下，泛化误差是**维度无关（dimension-free）**的。

2.  **次优性差距与优化步数 `T` 的关系 (Figure 1c, 1d)**：
    - 当横坐标为 `log(T)` 时，次优性差距随 `T` 增加而**单调递减**。
    - 这验证了理论分析的一个有趣发现：与传统观点（更多迭代导致更差泛化）相反，在 RLHF 场景下，更多的优化步数（更小的梯度范数）反而能带来**更好的算法稳定性和泛化性能**（Theorems 4, 6）。

3.  **正定性验证 (Figure 2)**：
    - 报告了经验协方差矩阵为正定的概率随样本量 `n` 的变化。
    - 结果显示，当 `n` 较小时，正定概率很低；随着 `n` 增加，概率迅速上升至 1。
    - 这验证了在数据不足时，“正定性”假设不成立，从而凸显了本文提出的“特征覆盖”假设的必要性和优越性。

### **消融实验结果**

本文没有进行典型的消融实验（如移除某个模块）。其核心的“消融”体现在**不同假设条件下的理论分析**：
- **Section 5.1 (强假设)**：在特征充分覆盖（协方差正定）下，证明了经验最优点能精确恢复真值参数。
- **Section 5.2 (弱假设)**：在特征覆盖不足的更现实情况下，证明了次优性差距仍有 `O(n⁻¹)` 的良好界。
这种从理想到现实的过渡本身就是一种理论上的“消融”，证明了新框架的鲁棒性和普适性。

---

## 4. 关键结论和发现

### **论文的主要发现**

1.  **端到端泛化理论**：首次为 RLHF 建立了一个完整的端到端泛化理论框架，填补了实践与理论之间的鸿沟。
2.  **维度无关的泛化**：在提出的**特征覆盖假设**下，即使在高维空间中，RLHF 学习到的策略也具有 `O(n⁻¹)` 的次优性差距，且该界与模型维度 `d` 无关。这意味着有效的特征模式由训练数据张成的子空间决定。
3.  **优化即正则化**：一个反直觉但重要的发现是，对于 RLHF，**更多的梯度上升步数（GA/SGA）能提高算法稳定性，从而改善泛化性能**。这是因为优化过程减小了梯度范数，而梯度范数直接控制着稳定性系数。
4.  **理论支持实践**：为“为何 RLHF 能仅凭有限的人类反馈就让 LLMs 泛化出强大能力”提供了新的理论证据。

### **方法的局限性**

1.  **线性奖励假设**：理论建立在 **linear reward model** 的基础上。虽然这是理论分析的常见起点，但真实的 LLM 奖励函数可能是高度非线性的，该假设可能无法完全捕捉复杂的人类偏好。
2.  **理想化环境**：实验为模拟环境，未在真实的、大规模的 LLM RLHF 流程中进行验证。
3.  **忽略数据偏见**：理论分析假设数据是无偏的。然而，如果训练数据包含社会偏见，算法的稳定性意味着这些偏见也会被忠实地学习和放大。
4.  **SGA 分析的复杂性**：对 SGA 的分析使用了“高概率稳定性”（high-probability stability），结果是一个概率性保证，比确定性分析更复杂。

### **未来工作方向**

1.  **推广到非线性模型**：将此稳定性框架扩展到深度神经网络等非线性函数逼近器。
2.  **在线数据与动态环境**：研究在在线学习、数据分布漂移等更动态场景下的泛化性能。
3.  **结合其他对齐方法**：将此框架应用于分析 DPO (Direct Preference Optimization) 等无需显式奖励模型的对齐算法。
4.  **偏差与公平性分析**：在理论框架中引入对数据偏见和公平性的考量，研究如何在保证泛化的同时减轻有害偏见的传播。

</details>

---

### 8. [Auto-Regressive Masked Diffusion Models](https://arxiv.org/abs/2601.16971)

**Authors**: Mahdi Karami, Ali Ghodsi  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.16971v1  

#### Abstract
Masked diffusion models (MDMs) have emerged as a promising approach for language modeling, yet they face a performance gap compared to autoregressive models (ARMs) and require more training iterations. In this work, we present the Auto-Regressive Masked Diffusion (ARMD) model, an architecture design...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《Auto-Regressive Masked Diffusion Models》论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

**Masked Diffusion Models (MDMs)** 在文本生成任务中展现出潜力，但相比 **Autoregressive Models (ARMs)** 仍存在显著差距，主要体现在两个方面：

- **训练效率低**：MDMs 需要多个去噪步骤（denoising steps），每个步骤通常需要独立的网络前向传播，导致训练迭代慢。
- **推理速度与质量权衡**：虽然扩散模型支持并行采样，但现有方法在并行生成时往往牺牲生成质量（如困惑度升高）。

此外，传统 MDMs 多依赖双向上下文（类似 BERT），缺乏高效的自回归式解码能力。

---

### 提出了什么新方法或新思路

作者提出 **Auto-Regressive Masked Diffusion (ARMD)** 模型，其核心思想是将 MDMs 重新建模为一种 **块级因果（block-wise causal）生成过程**，从而统一扩散模型与自回归模型的优点。

#### 主要创新点：

1. **严格因果架构（Strictly Causal Architecture）**
   - 将 MDM 中的随机掩码顺序转化为一个排列后的序列，并按“去掩码时间”划分为多个块。
   - 设计了一种 **严格因果注意力机制（Strictly Causal Attention）**，确保当前块的输出仅依赖于先前块的信息，而非当前块本身。
   - 引入 **两流注意力机制（Two-Stream Attention）**：同时维护一个因果流（Causal Stream）和一个严格因果流（Strictly Causal Stream），共享参数但不同查询输入和掩码策略，提升表达力而不显著增加参数量。

2. **并行化训练目标**
   - 利用上述因果结构，将原本需多次前向传播的 MDM 损失函数，重构为可在 **单次前向传播中并行计算所有条件概率** 的形式，极大提升了训练效率。

3. **灵活的混合训练策略**
   - 支持 **渐进式排列训练（Progressive Permutation Schedule）**：初期使用左到右顺序训练，逐步引入随机排列，使模型既能学习自然语言的顺序先验，又能适应任意生成顺序。

4. **新型推理策略：跨步并行生成（Strided Parallel Generation, SPB）**
   - 将目标序列划分为 S 个并行流，先串行生成各流首token（建立全局上下文），然后并行生成后续位置。
   - 通过交错排列（interleaved permutation）最大化并行token之间的距离，满足近似独立假设，保证生成质量。

---

### 相比现有方法的优势

| 维度 | ARMD 优势 |
|------|---------|
| **训练效率** | 单次前向传播完成全部损失计算，训练迭代更快；实验证明在更少训练步数下达到SOTA性能。 |
| **推理灵活性** | 支持高效自回归解码（得益于 KV Cache）和高质量并行生成。 |
| **架构通用性** | 可视为 ARMs 的推广，兼容预训练 LLM 微调为扩散模型。 |
| **生成质量** | 并行生成时困惑度接近串行生成，显著优于其他扩散基线。 |

---

## 2. 核心实验方法和设置

### 使用的数据集

- **OpenWebText**：用于训练 small 和 medium 规模的 ARMD 模型（分别对应 GPT-2 small 和 medium 参数量）。
- **One Billion Words (LM1B)**：额外验证在标准语言建模任务上的泛化能力。

### 实验设置

| 设置项 | 描述 |
|-------|------|
| **模型规模** | Small: 12层, d=768；Medium: 24层, d=1024 |
| **训练配置** | Batch size: 512 × 1024 tokens；AdamW优化器；学习率 3e-4；warmup 2000步 |
| **训练步数** | 最多 400K 步（small）、300K 步（medium） |
| **评估指标** | **零样本困惑度（zero-shot perplexity）**，在以下测试集上报告：<br>• LAMBADA<br>• WikiText2 / WikiText103<br>• Penn Tree Bank (PTB)<br>• One Billion Words (1BW) |
| **生成设置** | 使用 float64 精度进行 Gumbel sampling，避免数值误差影响多样性评估 |

### 基线方法对比

| 类别 | 对比模型 |
|------|--------|
| **自回归模型** | GPT-2 |
| **离散扩散模型** | D3PM, PLAID, SEDD, RADD |
| **其他 MDM 变体** | BD3-LMs, MDLM |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 3）

#### 在 OpenWebText 上训练的小/中等模型（Table 1）

| 方法 | LAMBADA ↓ | WikiText2 ↓ | PTB ↓ | WikiText103 ↓ | 1BW ↓ |
|------|-----------|-------------|--------|----------------|--------|
| **ARMD (small, 180K steps)** | **44.66** | **36.25** | 130.31 | **35.66** | **53.18** |
| GPT-2 (small) | 45.04 | 42.43 | 138.43 | 41.60 | 75.20 |
| RADD (400K) | 51.70 | 39.98 | 107.85 | 37.98 | 72.99 |

> ✅ **结论**：ARMD 在更少训练步数（180K vs 400K）下全面超越 GPT-2 和主流 MDM 基线。

#### 在 LM1B 数据集上的表现（Table 3）

| 方法 | ppl ↓ |
|------|-----|
| Transformer-XBase | 23.5 |
| Transformer | 22.83 |
| **ARMD (300K steps)** | **23.64** |
| **ARMD (1M steps)** | **22.36** ✅ |
| BD3-LMs (L'=4, 1M) | 28.23 |
| MDLM (1M) | 31.78 |

> ✅ **结论**：ARMD 在仅 300K 步时已优于多数扩散模型，在 1M 步时达到 **22.36**，超过所有对比的扩散模型，逼近纯自回归 Transformer。

---

### 与基线方法的对比结果

- **训练效率**：ARMD 在 **3× 更少训练步数** 下即超越 RADD、SEDD 等先进 MDM。
- **并行生成质量**（见 Table 2）：
  - 当并行度 S=4（T=256）时，ARMD 困惑度为 39.4（medium, 80K fine-tune），而 SEDD 超过 100。
  - 仅需 10K 额外微调步数即可实现高质量并行生成。
- **采样速度**（Table 2 注释）：
  - S=1（串行）：3.51s / seq
  - S=2：1.78s / seq （≈2×加速）
  - S=4：0.93s / seq （≈3.8×加速）

---

### 消融实验结果（Ablation Study, Table 5）

| 配置 | 影响 |
|------|------|
| **L2s = L/2（默认）** | 性能最佳，平衡表达力与效率 |
| L2s = L/4（减少两流层数） | 性能略有下降但仍优于基线，适合资源受限场景 |
| L2s = L（全层两流） | 计算开销大，性能未进一步提升 |
| **p=32 vs p=64（最大打乱token数）** | p=64 在部分数据集上略优，说明更多排列增强鲁棒性 |

> 🔍 **发现**：并非越多“两流层”越好，**前半部分使用两流结构已足够捕获必要上下文**。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **MDMs 可被重构为因果生成模型**：通过将掩码顺序映射为块级因果结构，可实现 MDM 的并行训练。
2. ✅ **训练效率大幅提升**：ARMD 在显著更少训练步数下超越现有 MDM 和 ARMs。
3. ✅ **高质量并行生成成为可能**：提出的 **Strided Parallel Generation** 策略有效缩小了并行与串行生成的质量差距。
4. ✅ **架构具有高度兼容性**：支持 KV Cache、可适配 RoPE、易于从预训练 LLM 微调而来。

---

### 方法的局限性

- **计算开销略高**：由于两流设计，每层需额外计算，理论 FLOPs 增加约 40–50%，实测训练吞吐降低约 22%（L2s=6）。
- **对排列调度敏感**：渐进式排列训练需精心设计调度曲线，否则可能损害语言结构学习。
- **尚未扩展至超大规模模型**：当前实验集中在 GPT-2 级别，百亿以上规模的表现待验证。

---

### 未来工作方向

1. **探索更高效的严格因果实现方式**，例如递归形式或稀疏注意力。
2. **将 ARMD 架构应用于预训练大模型（如 LLaMA）的微调**，构建“扩散版 LLM”。
3. **结合 Consistency Models 或 Flow Matching** 进一步减少推理步数。
4. **拓展到多模态任务**，如图像-文本联合生成，利用其并行生成优势。

---

> 📌 **总体评价**：  
> ARMD 成功弥合了 **自回归模型的训练效率** 与 **扩散模型的并行灵活性** 之间的鸿沟，提出了一种兼具高性能、高效率与强扩展性的新一代文本生成框架，为未来大规模生成模型的设计提供了重要范式。

</details>

---

### 9. [DSGym: A Holistic Framework for Evaluating and Training Data Science Agents](https://arxiv.org/abs/2601.16344)

**Authors**: Fan Nie, Junlin Wang, Harper Hua, Federico Bianchi, Yongchan Kwon, Zhenting Qi, Owen Queen, Shang Zhu, James Zou  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.16344v1  

#### Abstract
Data science agents promise to accelerate discovery and insight-generation by turning data into executable analyses and findings. Yet existing data science benchmarks fall short due to fragmented evaluation interfaces that make cross-benchmark comparison difficult, narrow task coverage and a lack of...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DSGym: A Holistic Framework for Evaluating and Training Data Science Agents

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前的 **Data Science Agent**（数据科学智能体）评估存在以下三大系统性缺陷：

1. **缺乏严格的数据依赖性（Lack of rigorous data grounding）**  
   多数现有的 file-grounded benchmarks（基于文件的基准测试）实际上允许模型通过 prompt-only 的方式（如记忆、模式匹配、先验知识）解决任务，而无需真正读取和分析数据文件。

2. **任务不一致性和无效性（Task inconsistency and invalidity）**  
   现有基准中存在标注错误、答案格式模糊、多选题选项重复等问题，导致评估不可靠。

3. **领域覆盖狭窄（Limited domain coverage）**  
   现有任务集中于通用统计分析（如均值、相关性），严重缺乏对真实科研流程（如生物信息学、单细胞分析、分子预测）的建模能力测试。

---

### 提出了什么新方法或新思路

作者提出了 **DSGYM**，一个标准化、可扩展的框架，用于**端到端地评估和训练 Data Science Agents**。

#### 核心创新点：

- **统一执行环境（Unified Execution Environment）**  
  采用容器化架构（Docker + Jupyter Kernel），确保每个 agent 在隔离、可复现的环境中运行，支持跨任务、跨领域的公平比较。

- **模块化设计（Modular Architecture）**  
  支持灵活添加新任务、新 agent 架构、新工具（如 web search）、新评估脚本，使 DSGYM 成为一个“活”的测试平台（live testbed）。

- **提出 DSGYM-TASKS：高质量、抗捷径的任务套件**  
  包含三个层次：
  1. **Refined Existing Benchmarks**：对 QRData、DAEval 等进行质量验证和“捷径过滤”（shortcut filtering）。
  2. **DSBio**：由专家从生物医学文献中提取的 90 个高难度生物信息学任务，要求理解 `.h5ad` 文件、使用 `scanpy` 等专业库。
  3. **DSPREDICT**：从 Kaggle 竞赛中自动抓取并标准化的 92 个端到端建模任务，涵盖计算机视觉、分子预测、单细胞扰动等。

- **支持 agent 训练（Training Support）**  
  利用 DSGYM 的执行环境生成 **execution-verified synthetic trajectories**，用于监督微调（SFT），实现闭环训练。

---

### 相比现有方法的优势

| 维度 | 现有方法（如 MLEBench、DAEval） | DSGYM |
|------|-------------------------------|--------|
| **数据依赖性** | 弱，易被 prompt 捷径绕过 | 强，通过“捷径过滤”确保必须读数据 |
| **领域覆盖** | 通用数据分析为主 | 扩展至生物信息学、Kaggle 真实挑战 |
| **执行环境** | 不统一，难以复现 | 容器化、状态保持、资源可控 |
| **可扩展性** | 静态数据集 | 模块化，支持持续新增任务 |
| **训练支持** | 仅用于评估 | 可生成合成训练数据 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

- **Refined General Analysis Tasks**：
  - `QRData-Verified`（统计与因果推理）
  - `DAEval-Verified`（短分析查询）
  - `DABStep`（金融多步分析）
  - `MLEBench-Lite`（经典 ML 挑战）

- **Domain-Specific Scientific Tasks**：
  - `DSBio`：90 个来自顶级期刊的生物信息学任务（单细胞、空间转录组、遗传学）
  - `DSPREDICT`：共 92 个 Kaggle 竞赛任务
    - `DSPREDICT-Easy`：38 个（如 Titanic、Playground 系列）
    - `DSPREDICT-Hard`：54 个（如 `web-traffic-forecasting`, `pku-autonomous-driving`）

### 实验设置和评估指标

#### 模型列表（评估对象）：

- **闭源模型（Proprietary）**：GPT-4o, GPT-5, Claude Sonnet 4/4.5
- **开源大模型（Open-sourced）**：Qwen3-Coder, DeepSeek-v3.1, Kimi-K2-Instruct
- **小模型（Small Models）**：Qwen2.5-7B, Qwen3-4B

所有模型均使用 DSGYM 提供的默认 **CodeAct-like agent interface**，禁用外部工具（如 web search），温度设为 `T=0`。

#### 评估指标：

| 任务类型 | 指标 |
|---------|------|
| **Data Analysis** | Exact Match Accuracy（数值容忍） |
| **Data Prediction**（如 Kaggle） | - `Valid Submission Rate`（提交有效率）<br>- `Above Median Rate`（超越中位数）<br>- `Any Medal Rate`（获得奖牌率）<br>- `Percentile Rank`（百分位排名） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表1：通用数据分析任务准确率（部分）

| Model | QRData-Verified (%) | DABStep-hard (%) | DAEval-Verified (%) |
|-------|---------------------|------------------|----------------------|
| GPT-4o | 60.24 | 7.41 | 92.26 |
| Claude Sonnet 4.5 | 61.35 | 37.04 | 91.71 |
| Kimi-K2-Instruct | 63.68 | 28.84 | 92.82 |
| Qwen3-Coder 480B | 54.72 | 14.29 | 90.61 |
| Qwen3-4B-Instruct | 45.27 | 2.9 | 64.47 |

> 💡 **观察**：即使在简单任务上表现良好，但在 **DABStep-hard**（多步、强数据依赖）上普遍低于 40%，表明复杂推理仍是瓶颈。

---

#### 表2：DSBio（生物信息学）任务准确率

| Model | Overall (%) |
|-------|-----------|
| Kimi-K2-Instruct | **43.33** |
| Claude Sonnet 4.5 | 42.22 |
| GPT-5.1 (high) | 38.89 |
| Qwen3-4B-Instruct | 6.67 |

> 💡 **观察**：所有模型在 DSBio 上表现显著下降，**domain grounding error** 占失败案例的 85–96%。

---

#### 表3：DSPREDICT 建模任务表现（Hard 分割）

| Model | Valid (%) | Medal (%) | Median (%) |
|-------|----------|----------|------------|
| GPT-5.1 (high) | 85.7 | 4.8 | 14.3 |
| Claude Sonnet 4.5 | 71.4 | 0 | 4.8 |
| Qwen3-Coder 480B | 66.7 | 2.4 | 5.9 |
| Qwen3-4B-Instruct | 40.5 | 0 | 0 |

> 💡 **观察**：
> - 即使是 GPT-5.1，**奖牌率也接近 0%**，说明 agent 缺乏优化动力。
> - 多数 agent 能生成有效提交，但性能远低于人类中位水平。

---

### 消融实验结果

#### （1）捷径过滤的影响（Shortcut Filtering）

- 在 `QRData` 上移除可被“无数据访问”解决的任务后，**所有模型平均准确率下降 20% 以上**。
- 小模型受影响更大，表明其更依赖记忆和先验。

> ✅ **证明**：现有 benchmark 存在严重“捷径污染”。

#### （2）训练实验：DSGYM-SFT 合成数据的有效性

- 使用 DSGYM 生成 **2,000 条 execution-verified synthetic trajectories** 对 `Qwen3-4B` 进行 SFT。
- 结果显示：
  - 在 `DABStep-hard` 上从 2.9% 提升至 **33.07%**
  - 在未见过的 `DSBio` 上从 6.67% 提升至 **21.11%**
  - 交互行为更细粒度（turn 数增加，token 分布更接近教师模型）

> ✅ **证明**：DSGYM 可作为“数据工厂”，生成高质量训练数据，提升小模型性能。

---

## 4. 关键结论和发现

### 主要发现

1. **现有 benchmark 易受“非数据依赖”捷径攻击**  
   多达 40–80% 的任务可在不访问数据的情况下被正确回答，严重高估 agent 能力。

2. **前沿模型在真实科研任务中仍表现不佳**  
   在 DSBio 上，即使是 GPT-4o 和 Claude 也仅有 ~33% 准确率，**domain grounding error 是主因**（如误用字段、误解实验设计）。

3. **Agent 存在“简单性偏见”（Simplicity Bias）**  
   当遇到技术障碍（如超时、库缺失）时，agent 倾向于放弃复杂策略，转而使用中位数等简单启发式方法，导致性能低下。

4. **小模型可通过 execution-grounded SFT 显著提升**  
   使用 DSGYM 生成的合成数据训练的小模型（4B）可达到接近 GPT-4o 的水平，验证了“执行验证”训练范式的有效性。

---

### 方法的局限性

1. **评估以确定性输出为主**  
   当前不支持开放性探索任务（如可视化、假设生成），限制了对创造性科学发现的评估。

2. **领域覆盖仍有限**  
   当前 DSBio 仅聚焦生物信息学，未来需扩展至化学、材料科学、经济学等领域。

3. **训练信号稀疏**  
   RL 训练面临长周期奖励稀疏的问题，如何设计有效的 credit assignment 仍是挑战。

4. **环境限制影响 agent 行为**  
   如无法安装包、网络受限等，可能导致 agent 因“环境阻塞”而非“能力不足”失败。

---

### 未来工作方向

1. **扩展至更多科学领域**  
   将 DSBio 构建流程推广至 geoscience、computational chemistry 等。

2. **支持 RL 和强化学习训练**  
   利用 DSGYM 的闭环环境，研究基于奖励的 agent 优化。

3. **引入 LLM Judge 进行开放任务评估**  
   对非确定性输出（如报告、图表解释）进行自动化评分。

4. **构建多 agent 协作生态**  
   支持多个 agent 分工协作完成复杂科研任务（如数据清洗 + 建模 + 报告生成）。

5. **推动 DSGYM 成为社区标准测试平台**  
   鼓励研究人员持续贡献新任务、新工具，形成动态演进的评估生态系统。

---

> 📌 **总结**：DSGYM 不只是一个 benchmark，而是一个**可执行、可扩展、可训练**的完整数据科学 agent 生态系统。它揭示了当前 agent 的真实短板，并提供了一条通往更强大、更可靠科学自动化的新路径。

</details>

---

### 10. [Doc2AHP: Inferring Structured Multi-Criteria Decision Models via Semantic Trees with LLMs](https://arxiv.org/abs/2601.16479)

**Authors**: Hongjia Wu, Shuai Zhou, Hongxin Zhang, Wei Chen  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.16479v1  

#### Abstract
While Large Language Models (LLMs) demonstrate remarkable proficiency in semantic understanding, they often struggle to ensure structural consistency and reasoning reliability in complex decision-making tasks that demand rigorous logic. Although classical decision theories, such as the Analytic Hier...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Doc2AHP: Inferring Structured Multi-Criteria Decision Models via Semantic Trees with LLMs

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Analytic Hierarchy Process (AHP)** 虽然在多准则决策（MCDM）中具有严谨的逻辑框架，但其构建严重依赖领域专家手动整理文档、定义层级结构并进行成对比较，过程**耗时且难以规模化**。而现有的基于 **Large Language Models (LLMs)** 的“虚拟专家”方法虽然能自动生成决策模型，却存在以下问题：
- 生成的准则（criteria）**泛化性强但缺乏证据支持**，易产生语义漂移；
- 数值判断不稳定，容易出现 **hallucination 和不一致性（如 CR > 0.1）**；
- 缺乏可追溯性，不利于高风险场景下的可信决策。

因此，如何结合 LLMs 的泛化能力与 AHP 的结构严谨性，实现从非结构化文档到高质量、可解释 AHP 模型的自动化构建，是一个关键挑战。

---

### 🚀 提出的新方法与创新思路

作者提出 **Doc2AHP** ——一种端到端的、受 AHP 原理指导的结构化推理框架，其核心思想是：

> 利用文档集合中的**语义几何结构**（semantic geometry）作为先验，引导 LLM 构建符合逻辑一致性的 AHP 决策模型。

#### 主要创新点包括：

1. **基于语义树的结构生成（Structure Generation via Semantic Trees）**
   - 使用 **Hierarchical Clustering** 对文档段落的嵌入向量进行聚类，形成一棵语义树 $ T $。
   - 将该语义树作为 AHP 层级结构 $ H $ 的骨架，通过递归剪枝和映射生成具有语义连贯性的决策准则。
   - 引入认知约束（最大分支数 $ K_{\text{max}} $、最大深度 $ D_{\text{max}} $），防止过度复杂化。

2. **Leader-Guided 多智能体权重估计机制（Multi-Agent Weight Estimation with Consistency Optimization）**
   - 设计一个由多个 **Domain Expert Agents** 和一个 **Leader Agent** 组成的协作系统：
     - 各 Expert Agent 独立生成 pairwise comparison matrix；
     - 使用加权几何平均聚合为共识矩阵 $ M $；
     - 引入 **Leader Agent 的高层语义约束**（如 “Safety ≫ Cost”）转化为线性不等式约束 $ \Omega_{\text{leader}} $。
   - 采用 **Logarithmic Least Squares Method (LLSM)** 进行优化，在保证数学一致性（CR < 0.1）的同时保留语义合理性。

3. **无需标注数据与人工干预的无监督流程**
   - 整个流程完全基于原始文档和目标描述自动完成，无需人工标注或微调（zero-shot setting）。

---

### 🔍 相比现有方法的优势

| 方面 | 传统 AHP | 现有 LLM 方法（如 Standard-AHP, Debate-AHP） | Doc2AHP |
|------|----------|---------------------------------------------|--------|
| 结构合理性 | 高（人工设计） | 低（自由生成易混乱） | ✅ 高（受语义树约束） |
| 数值一致性 | 高 | 中–低（CR 不可控） | ✅ 高（优化模块保障 CR < 0.1） |
| 可解释性 | 高 | 低（黑盒生成） | ✅ 高（完整决策链可追溯） |
| 自动化程度 | 低 | 高 | ✅ 高 |
| 对弱模型鲁棒性 | 不适用 | 差（小模型下崩溃） | ✅ 强（优化兜底） |

---

## 2. 核心实验方法和设置

### 📚 数据集
构建了一个名为 **DecisionBench** 的基准测试集，涵盖三个真实世界领域，共 **6 个决策场景**：

| 领域 | 场景 ID | 决策目标示例 | 关注重点 |
|------|--------|--------------|---------|
| **Movies** | M-Act, M-Dra | 选择最佳视觉大片 / 叙事艺术片 | Visuals, Narrative Depth |
| **Hotels** | H-Fam, H-Bus | 家庭友好型酒店 / 商务高效酒店 | Safety, Amenities, Location |
| **Beers** | B-Ref, B-Cplx | 清爽夏日啤酒 / 复杂风味精酿 | Crispness, Flavor Complexity |

每个场景包含约 20 个候选对象（alternatives），并通过 LLM 扩展描述细节，确保细粒度比较可行性。

输入上下文统一为约 20 篇专家文档（经标准化预处理），以控制检索偏差。

---

### ⚙️ 实验设置

- **主干模型**：默认使用 **GPT-5.2 (Temp=0.1)**；同时在 **Llama-3.1-8B / 70B** 上验证跨模型鲁棒性。
- **零样本设定**：所有方法均未进行 fine-tuning。
- **固定上下文协议**：“Fixed Domain Context”，即所有方法接收相同的文档输入。
- **独立运行**：每组实验独立执行完整 pipeline 5 次，报告均值与稳定性。

---

### 📊 评估指标

| 类别 | 指标 | 说明 |
|------|------|------|
| **决策准确性** | **NDCG@5**, **NDCG@10** | 衡量推荐列表前5/10项与真实质量排序的相关性，越高越好 |
| **数值一致性** | **CR-mean**, **CR-max**, **Pass Rate (CR < 0.1)** | 成对比较矩阵的一致性比率统计，Pass Rate 越高表示越可靠 |
| **消融分析** | NDCG@5/@10 对比不同变体 | 分析各组件贡献 |

---

### 🆚 基线方法对比

| 方法 | 描述 |
|------|------|
| **Standard-AHP** [Lu et al., 2024] | 单 LLM + Chain-of-Thought 生成矩阵，无结构约束 |
| **Debate-AHP** [Svoboda and Lande, 2024] | 多智能体辩论达成共识，但仍为语义协商，无数学优化 |

两者均注入相同文档输入，并适配输出格式以便公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 决策准确率（NDCG）

| 方法 | M-Dra @5 | H-Fam @5 | B-Cplx @5 | 平均 @5 |
|------|---------|---------|----------|--------|
| Standard-AHP | 0.830 | 0.950 | 0.948 | ~0.909 |
| Debate-AHP | 0.777 | 0.912 | 0.949 | ~0.879 |
| **Doc2AHP (Ours)** | **0.854** | **0.974** | **0.965** | **~0.931** |

✅ **Doc2AHP 在 5/6 场景中取得最优 NDCG@5 表现**，尤其在抽象概念密集的任务（如 M-Dra, B-Cplx）上优势显著。

> 💡 发现：随着任务语义复杂度上升，Doc2AHP 的结构性提取能力更能捕捉深层评价维度（如叙事深度、风味层次），而 baseline 易丢失这些信息。

---

### 🔢 数值一致性表现（H-Fam 场景，多模型测试）

| Method | Model | CR-mean | CR-max | Pass Rate |
|--------|-------|--------|--------|-----------|
| Standard-AHP | Llama-8B | 0.1693 | 0.7514 | 71.42% |
| Debate-AHP | Llama-8B | 0.2902 | 0.4309 | **0%** ❌ |
| **Doc2AHP** | Llama-8B | **0.0139** | **0.0807** | **100%** ✅ |
| Standard-AHP | Llama-70B | 0.1414 | 1.3921 | 86.95% |
| Debate-AHP | Llama-70B | 0.0065 | 0.0132 | 100% |
| **Doc2AHP** | Llama-70B | **0.0530** | **0.0957** | **100%** ✅ |

✅ **Doc2AHP 在所有模型尺度下均保持 100% 通过率**，即使在弱模型（Llama-8B）上也稳定可靠。

> 💥 关键发现：Debate-AHP 在小模型上出现“共识崩塌”（Consensus Collapse），多个幻觉 agent 相互误导，导致 CR 恶化至不可接受水平；而 Doc2AHP 的优化模块起到了“数学守门人”作用。

---

### 🔍 消融实验（Ablation Study on H-Fam）

| Model Variant | NDCG@5 | NDCG@10 |
|---------------|--------|---------|
| w/o Struct（无语义树结构） | 0.986 | **0.963** |
| w/o Opt（无一致性优化） | 0.977 | 0.953 |
| **Full Model (Ours)** | **0.993** | 0.961 |

- **移除结构先验（w/o Struct）**：NDCG@5 下降明显，说明缺少精细结构提取会削弱 top-ranked 候选识别能力；
- **移除优化模块（w/o Opt）**：两项指标全面下降，证实直接 LLM 输出易引入逻辑冲突；
- **完整模型在 top-precision 上表现最强**，体现“结构 + 优化”双轮驱动的有效性。

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **结构先验 + 多智能体协同 + 数学优化** 是构建可靠 LLM-based AHP 模型的关键路径；
2. **语义树（Semantic Tree）能有效编码文档中的潜在决策层级**，为 AHP 结构提供强归纳偏置；
3. **Leader-Guided 优化机制可在不牺牲语义意图的前提下强制满足一致性要求**，优于纯代数修正；
4. Doc2AHP 在 **轻量级模型上仍保持高性能与高一致性**，适合部署于边缘设备或资源受限环境；
5. 该方法实现了从“直觉式快思考”到“可验证慢思考”的转变，提升自动化决策系统的可信度。

---

### ⚠️ 局限性

1. **计算开销较高**：相比直接 prompt，Doc2AHP 包含聚类、多 agent 推理、优化等多个步骤，延迟更大；
2. **对文档质量敏感**：若输入文档本身存在矛盾或信息稀疏，生成的模型可能受限；
3. **参数配置影响结果**：$ K_{\text{max}}, D_{\text{max}} $ 等超参数需合理设置，否则可能导致过拟合或欠表达；
4. 当前仅适用于文本输入，尚未扩展至图像或多模态决策场景。

---

### 🔮 未来工作方向

1. **动态结构调整机制**：让模型自适应决定最佳深度与宽度，减少人工干预；
2. **引入反馈回路**：允许人类专家在线校正节点或权重，实现 Human-in-the-loop 决策；
3. **多模态 Doc2AHP**：融合图文、表格等异构信息源构建更丰富的决策模型；
4. **实时流式更新**：支持增量式文档输入，动态调整已有 AHP 模型；
5. **应用于更多高风险领域**：如医疗诊断、金融风控、政策制定等需要审计追踪的场景。

---

> 🧩 总结一句话：  
> **Doc2AHP 成功将 LLM 的语义理解能力与 AHP 的形式化逻辑相结合，走出了一条“神经符号”（neuro-symbolic）路径，使非专家也能从文档中构建出专业级、可审计的决策模型。**

</details>

---

### 11. [Efficient Gaussian process learning via subspace projections](https://arxiv.org/abs/2601.16332)

**Authors**: Felipe Tobar, Elsa Cazelles  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.16332v1  

#### Abstract
We propose a novel training objective for GPs constructed using lower-dimensional linear projections of the data, referred to as \emph{projected likelihood} (PL). We provide a closed-form expression for the information loss related to the PL and empirically show that it can be reduced with random pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Efficient Gaussian Process Learning via Subspace Projections*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
标准的 **Gaussian Process (GP)** 回归在训练时依赖于最大化似然（ML），其计算复杂度为 $O(n^3)$，其中 $n$ 是观测样本数。这使得传统 GP 在中等规模以上数据集（如数千个点）上变得不可行。

尽管已有稀疏近似方法（如基于 inducing variables 的 **sparse GPs**，例如 VFE），但它们存在以下问题：
- 学习到的超参数有偏（biased hyperparameters）
- 只在 $n$ 足够大时才体现出计算优势（asymptotic efficiency）
- 训练目标更复杂，需要更多优化步骤

### 🚀 提出的新方法：Projected Likelihood (PL)
本文提出一种新的训练目标——**Projected Likelihood (PL)**，其核心思想是：
- 将原始高维观测数据 $\mathbf{y} \in \mathbb{R}^n$ 投影到低维子空间（通过 $k < n$ 个线性投影方向）
- 在投影后的低维变量 $\mathbf{z} = \Omega^\top \mathbf{y}$ 上定义一个代理 GP 模型，并最大化其似然
- 得到的训练目标称为 **Projected Likelihood (PL)**，其负对数形式作为损失函数

该方法具有闭合解表达式，计算成本为 $O(kn^2)$，且可通过随机球面投影有效减少信息损失。

### 🔍 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **准确性** | PL 学得的超参数和最终 NLL 更接近 exact GP，优于 VFE；尤其在 posterior variance 上无系统性高估（VFE 已知问题） |
| **效率** | 尽管理论复杂度高于 VFE 的 $O(nm^2)$，但由于 PL 的 loss landscape 更平滑，所需优化步数显著更少，实际运行时间更快（尤其当 $n < 3000$） |
| **简洁性** | 不引入额外可学习变量（如 inducing points 位置），避免增加优化负担 |
| **理论支持** | 推导了 Fisher information loss 的显式表达式，证明信息损失与条件协方差 $\Sigma_{Y|Z}$ 有关，并可通过选择最优投影方向最小化 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
1. **合成数据**  
   - 由 SE kernel ($\sigma=1, l=20, \sigma_n=0.1$) 生成的 1000 样本时间序列
   - 用于比较不同 optimizer 下的表现（E1）

2. **真实世界数据**
   - **Sunspots 数据集**：3319 个月度太阳黑子数量记录
   - **EEG 数据集**：Helsinki 新生儿脑电图子集（共 10000 点，取 8000 训练 + 2000 验证）

### ⚙️ 实验设置与评估指标
| 设置项 | 描述 |
|-------|------|
| **模型配置** | 对比方法：<br>• Exact ML（基准）<br>• VFE（variational free energy）<br>• PL（本文方法）<br>固定 $k = m = 100$ 进行多数实验 |
| **优化器** | 测试 BFGS 和 Adam；最终统一使用 Adam（表现更好） |
| **评估指标** | 
| | • **Negative Log-Likelihood (NLL)**：衡量模型拟合优度 |
| | • **Running Time (秒)**：总训练耗时 |
| | • **RMSE**：仅用于 EEG 实验（exact GP 不可行） |
| | • **Posterior Variance 可视化**：分析不确定性估计质量 |
| **投影设计对比** | 测试多种投影矩阵 $\Omega$：<br>• Sphere（单位球面上均匀采样）<br>• Repulsive（带排斥力的球面采样）<br>• Localised（高斯 RBF 覆盖）<br>• One-hot（单点指示向量） |

### 🆚 基线方法对比
- **Exact Maximum Likelihood (ML)**：理想情况下的性能上限
- **VFE (Variational Free Energy)**：当前主流 sparse GP 方法，代表 SOTA 近似训练框架

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### 表 1：E1 实验（合成数据，n=1000）
| 方法 | Optimizer | NLL ↓ | 时间 (s) ↓ |
|------|-----------|--------|------------|
| ML (exact) | — | 337.4 | 26.5 (BFGS), 3.66 (Adam) |
| VFE | Adam | 358.1 | 1.57 |
| **PL** | **Adam** | **347.0** | **0.574** |

✅ **结论**：PL 的 NLL 显著优于 VFE，且训练速度最快。

---

#### 图 3：E2 实验（不同 $n$ 下 NLL vs 时间）
- 所有 $n \in \{500, ..., 4000\}$ 情况下，PL 在达到相同 NLL 时均快于 VFE（尤其 $n < 3000$）
- 当 $n > 3000$ 后两者性能趋同（因 VFE 渐进优势显现）
- PL 使用更少迭代即可收敛 → 实际效率更高

---

#### 表 2 & 3：E3 实验（真实数据）

##### 表 2：Sunspots 数据集（n=3319）
| Kernel | 方法 | NLL ↓ | 时间 (s) ↓ |
|--------|------|--------|------------|
| SE | ML | 1549.12 | 197.15 |
| SE | VFE | 1691.999 | 6.78 |
| SE | **PL** | **1584.565** | **19.62** |
| Laplace | **PL** | **1675.062** | **31.46** |
| ... | ... | ... | ... |

> ✅ PL 在所有 kernel 下 NLL 均远低于 VFE，接近 exact ML

##### 表 3：EEG 数据集（n=8000）
| Kernel | 方法 | RMSE ↓ | 时间 (s) ↓ |
|--------|------|--------|------------|
| SE | VFE | 0.303 | 148.08 |
| SE | **PL** | **0.194** | **306.22** |
| Laplace | VFE | 0.270 | 614.32 |
| Laplace | **PL** | **0.145** | **630.61** |

> ✅ PL 在预测精度（RMSE）上全面胜出，虽稍慢但仍具竞争力

---

### 🔬 消融实验结果（Ablation Study）

#### 投影方向设计的影响（Sec. 4, Fig. 1）
- 分析不同 $\Omega$ 设计对 $\text{tr}(\Sigma_{Y|Z})$ 的影响（反映信息保留程度）
- 结果排序（从优到劣）：
  1. **Sphere**（球面均匀投影）→ 最小 trace，最佳压缩效果
  2. Repulsive / Localised
  3. **One-hot** → 效果最差（捕捉相关性能力弱）

👉 因此后续实验均采用 **spherical random projections**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **PL 是一种高效且准确的 GP 训练替代方案**
   - 在 moderate-sized datasets（$n \lesssim 8000$）上，PL 在 NLL/RMSE 上显著优于 VFE
   - 实际训练时间更短，得益于更简单的 loss landscape 和更少优化步数

2. **信息损失可控且可量化**
   - 推导出 Fisher information loss 的闭式表达，揭示其与 $\Sigma_{Y|Z}$ 的二次关系
   - 使用随机球面投影能有效降低信息损失

3. **无需引入额外可学习参数**
   - 相比 VFE 中需联合优化 inducing point 位置，PL 使用固定投影方向，简化训练流程

4. **适用于多种 kernel 和 optimizer**
   - 在 SE、Laplace、RQ、LocPer 等 kernel 上均有良好表现
   - Adam 表现优于 BFGS，适合 PL 框架

---

### ⚠️ 方法的局限性
1. **理论复杂度仍为 $O(kn^2)$**
   - 虽实践中高效，但无法扩展至超大规模数据（如 $n > 10^5$）
   - 不如 structured kernel 或 deep GP 等方法具备更强 scalability

2. **投影方向未自适应更新**
   - 当前使用固定的随机投影，未能动态调整以追踪数据分布变化
   - 若能结合 adaptive projection selection 可能进一步提升性能

3. **多输出场景尚未验证**
   - 当前实验集中在 single-output GP，multi-output extension 有待探索

---

### 🔮 未来工作方向
1. **Adaptive Projection Selection**
   - 动态选择最具信息量的投影方向（如基于梯度或主动学习）

2. **与其他稀疏方法融合**
   - 结合 inducing points 与 subspace projection，构建 hybrid approximation

3. **应用于 streaming data 或 online learning**
   - 利用低维投影实现快速在线更新

4. **理论深化**
   - 分析 PL 在非高斯 likelihood 或分类任务中的推广能力

---

> 💡 **总体评价**：本文提出的 **Projected Likelihood (PL)** 方法为 GP 训练提供了一种“轻量级但高保真”的新范式，在中等规模数据上实现了**精度与效率的双重突破**，是对 sparse GP 方法的重要补充和发展。

</details>

---

### 12. [Latent Diffusion for Internet of Things Attack Data Generation in Intrusion Detection](https://arxiv.org/abs/2601.16976)

**Authors**: Estela S\'anchez-Carballo, Francisco M. Melgarejo-Meseguer, Jos\'e Luis Rojo-\'Alvarez  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.16976v1  

#### Abstract
Intrusion Detection Systems (IDSs) are a key component for protecting Internet of Things (IoT) environments. However, in Machine Learning-based (ML-based) IDSs, performance is often degraded by the strong class imbalance between benign and attack traffic. Although data augmentation has been widely e...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Latent Diffusion for Internet of Things Attack Data Generation in Intrusion Detection》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
物联网（IoT）环境中的 **Intrusion Detection Systems (IDSs)** 面临严重的**类别不平衡问题**：良性流量远多于攻击流量，导致基于机器学习（ML-based）的检测模型难以有效学习稀有但关键的攻击模式。此外，真实攻击数据收集困难、成本高且受隐私限制，加剧了数据稀缺问题。

现有数据增强方法存在以下不足：
- **SMOTE** 等传统过采样技术生成样本多样性低，易导致“记忆化”（memorization），无法扩展决策空间。
- **VAE** 易产生过平滑（over-smoothed）样本，丢失细节。
- **GAN** 存在训练不稳定、模式崩溃（mode collapse）等问题。
- **直接在原始特征空间运行的 Diffusion Models (DMs)** 虽然生成质量高，但计算开销大、采样慢，尤其对高维异构的 IoT 表格数据不友好。

### 提出的新方法与创新思路
本文提出了一种基于 **Latent Diffusion Model (LDM)** 的合成 IoT 攻击数据生成框架，其核心创新在于将扩散过程从原始数据空间转移到一个紧凑的潜在空间中进行。

具体流程如下：
1. **Autoencoder (AE) 编码**：使用一个混合变量 AE 将异构的 IoT 流量数据（连续 + 分类特征）编码到一个低维、归一化的 **latent space** 中。
2. **Latent Diffusion Modeling**：在该潜在空间中训练一个 DM 来学习并生成新的潜在表示。
3. **解码回原始空间**：通过 AE 的 decoder 将生成的潜在向量还原为真实的、语义一致的合成攻击样本。

这种方法结合了 AE 的高效降维能力和 DM 的高质量生成能力。

### 相比现有方法的优势
- ✅ **更高的样本保真度与多样性平衡**：相比 SMOTE 和 VAE，LDM 能生成更真实且多样化的攻击样本。
- ✅ **更强的特征依赖保持能力**：能更好地保留原始数据中的复杂统计关系和特征间依赖。
- ✅ **显著提升下游 IDS 性能**：在多个攻击类型上实现接近完美的 F1-score。
- ✅ **降低计算成本**：相较于直接在数据空间运行的 DM，LDM 在潜在空间操作减少了约 **25% 的采样时间**，提升了效率。
- ✅ **适用于异构表格数据**：专门设计处理 IoT 数据中常见的混合型（mixed-type）特征。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **CICIoT2023 dataset**：由加拿大网络安全研究所发布的真实 IoT 网络流量数据集。
- 包含多种攻击场景，本研究聚焦于三种代表性攻击类型：
  - **DDoS-ICMP_Fragmentation**（高流量泛洪）
  - **Mirai-greip_flood**（僵尸网络泛洪）
  - **MITM-ArpSpoofing**（中间人攻击，低速率隐蔽）

### 实验设置
- **任务目标**：通过合成攻击数据增强来平衡训练集，从而提升二分类 IDS（Benign vs. Attack）的检测性能。
- **数据划分**：
  - 攻击数据按 80%/10%/10% 划分为训练/验证/测试集。
  - 测试集保持原始不平衡比例（约 1:5.3 至 1:5.6）以公平评估。
- **增强策略**：每种生成模型均用于生成足够数量的攻击样本来使训练集达到类别平衡。
- **下游模型**：使用基于子空间法（subspace method）的集成分类器（100 个学习循环）进行攻击检测。

### 评估指标
#### 下游 IDS 性能指标：
- **Confusion Matrix (CM)**：行归一化，分析召回率等。
- **F1-score**：综合衡量精确率与召回率。

#### 合成样本质量指标：
| 指标 | 描述 |
|------|------|
| **MMD (Maximum Mean Discrepancy)** | 衡量真实与合成数据分布之间的差异（越小越好） |
| **Mean KL Divergence** | 边缘分布匹配程度（越小越好） |
| **MI Error (Mutual Information Error)** | 特征间依赖关系保持能力（越小越好） |
| **Precision / Recall (生成)** | 生成样本是否真实且覆盖充分 |
| **Nmed (Median Nearest Neighbor Distance)** | 多样性指标，值越大表示样本越分散、越新颖 |
| **Sampling Time** | 生成效率，反映实际部署可行性 |

### 基线方法对比
共比较五种数据增强方法：
1. **SMOTE**：经典过采样方法，作为非深度学习基线。
2. **VAE**：变分自编码器，学习概率潜变量。
3. **GAN**：生成对抗网络，采用对抗训练机制。
4. **DM (Data-space Diffusion Model)**：直接在原始特征空间运行的扩散模型。
5. **LDM (Latent Diffusion Model)**：本文提出的方法。

所有模型均经过 **Bayesian Optimization** 进行超参数调优，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 5）

| Attack Type | Baseline | SMOTE | VAE | GAN | DM | **LDM** |
|------------|---------|-------|-----|-----|----|--------|
| **DDoS**   | 0.46    | 0.89  | 0.96| 0.94| 0.97| **0.99** |
| **Mirai**  | 0.17    | 0.66  | 0.86| 0.83| 0.92| **0.99** |
| **MitM**   | 0.00    | 0.35  | 0.34| 0.33| **0.53**| 0.44 |

> 📌 **结论**：LDM 在 **DDoS 和 Mirai 攻击上达到 F1=0.99**，显著优于所有其他方法；对于最具挑战性的 MitM 攻击，DM 表现最佳（F1=0.53），LDM 次之但仍远优于传统方法。

### 与基线方法的对比结果

#### 分布相似性（Distributional Similarity）
- **SMOTE** 在 MMD 和 KL 上表现最好，说明其完美复制了局部结构。
- **LDM** 与 **DM** 接近 SMOTE 的分布保真度，明显优于 VAE 和 GAN。

#### 多样性与记忆化风险
- **SMOTE** 的 Nmed 最小 → 极易“记忆化”，缺乏多样性。
- **VAE** 的 Nmed 最大 → 多样性强，但牺牲了保真度。
- **LDM** 在所有攻击类型上的 Nmed 均高于 DM → **在保持分布一致性的同时实现了更高多样性**。

#### 特征依赖保持（Mutual Information Analysis）
- **SMOTE** 几乎完全保留原始 MI 结构。
- **VAE/GAN/DM** 对 MI 结构有不同程度的模糊或扭曲。
- **LDM** 生成的 MI 矩阵最接近真实数据，在全局结构和细粒度依赖上均有良好保留。

#### 可视化分析（UMAP 投影）
- **GAN/DM**：部分合成样本位于真实流形之外（off-manifold），可能破坏原始几何结构。
- **LDM**：合成样本均匀填充空隙，既未坍缩也未偏离主流形，表明其更准确地建模了本地几何。

#### 采样效率（见 Table 7）
| 方法 | 生成 10⁴ 样本耗时（ms） |
|------|------------------------|
| GAN  | 4.85 ± 0.87           |
| VAE  | 54.85 ± 9.44          |
| SMOTE| 154.07 ± 2.86         |
| DM   | 553.86 ± 9.31         |
| **LDM** | **411.23 ± 8.34**     |

> 📌 **结论**：LDM 相比 DM **提速约 25%**，是唯一在生成质量和效率之间取得良好平衡的扩散方法。

---

## 4. 关键结论和发现

### 主要发现
1. **LDM 是一种高效且可扩展的 IoT 攻击数据生成方案**，能够有效缓解类别不平衡问题。
2. 在 **DDoS 和 Mirai** 等具有明显流量特征的攻击上，LDM 实现近乎完美的检测性能（F1=0.99）。
3. LDM 在 **分布保真度、多样性、特征依赖保持和计算效率** 四方面取得了最佳权衡。
4. 相比于数据空间 DM，潜在空间扩散不仅加快了采样速度，还增强了样本多样性（更高的 Nmed）。
5. 对于 **MitM** 这类低速、碎片化、接近正常行为的攻击，所有方法仍面临挑战，但 DM 在分类性能上略胜一筹，而 LDM 更好地保持了几何一致性。

### 方法的局限性
1. 实验仅基于单一数据集（CICIoT2023）和三种攻击类型，**泛化性有待跨数据集验证**。
2. 当前评估限于**二分类任务**，未考虑多类攻击共存的现实复杂场景。
3. 所有增强均在离线阶段完成，尚未探索在线或增量式增强策略。
4. AE 的重建误差可能导致潜在空间信息损失，影响最终生成质量。

### 未来工作方向
1. **扩展至多类别 IDS 场景**，研究 LDM 在多标签或多任务检测中的应用。
2. 开展**跨数据集评估**，检验模型在不同网络环境和设备配置下的鲁棒性。
3. 探索**轻量化 LDM 架构**或加速推理技术，推动其在边缘设备上的实时部署。
4. 引入**增量学习机制**，支持动态新增攻击类型的持续数据生成。
5. 结合 **Differential Privacy** 技术，开发隐私保护版本的 LDM（如 DP-TLDM），适用于敏感数据场景。

--- 

> ✅ **总体评价**：本文成功将图像领域成功的 **Latent Diffusion Paradigm** 成功迁移至 IoT 安全领域的表格数据生成任务，提出了一种兼具高性能、高保真与高效率的解决方案，为解决 ML-based IDS 中的数据稀缺问题提供了新范式。

</details>

---

### 13. [LLM is Not All You Need: A Systematic Evaluation of ML vs. Foundation Models for text and image based Medical Classification](https://arxiv.org/abs/2601.16549)

**Authors**: Meet Raval, Tejul Pandit, Dhvani Upadhyay  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.16549v1  

#### Abstract
The combination of multimodal Vision-Language Models (VLMs) and Large Language Models (LLMs) opens up new possibilities for medical classification. This work offers a rigorous, unified benchmark by using four publicly available datasets covering text and image modalities (binary and multiclass compl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*LLM is Not All You Need: A Systematic Evaluation of ML vs. Foundation Models for text and image based Medical Classification*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前在医疗AI领域，**Large Language Models (LLMs)** 和 **Vision-Language Models (VLMs)** 被广泛认为是下一代智能诊断系统的核心。然而，现有研究存在以下三大缺陷：
- **模态隔离**：多数研究仅关注文本或图像单一模态，缺乏跨模态统一比较。
- **基准测试不严谨**：缺少对传统机器学习（ML）、零样本LLM/VLM、以及参数高效微调（PEFT）模型在同一实验框架下的公平对比。
- **多分类任务被低估**：尤其在医学影像中，复杂的多类别分类（multiclass）任务研究不足。

本论文旨在填补这些空白，提供一个**系统性、可复现、跨模态的实证评估框架**，以回答：**在医疗分类任务中，Foundation Models 是否真的优于传统 ML？**

### 提出了什么新方法或新思路
- 构建了一个**四任务统一实验框架**，涵盖：
  - 文本二分类（Diabetes）
  - 文本多分类（Mental Health Disorders）
  - 图像二分类（Skin Cancer）
  - 图像多分类（Respiratory Disease）
- 在所有任务中采用**一致的数据划分、预处理流程和评估指标**，实现“apples-to-apples”比较。
- 首次将三类主流建模范式并列评估：
  - **Classical ML**（如 Logistic Regression, LightGBM, ResNet-50）
  - **Prompt-based LLM/VLM**（Gemini 2.5 零样本推理）
  - **Fine-tuned PEFT Models**（LoRA 微调 Gemma3 变体）

### 相比现有方法的优势
- **全面性**：首次在同一研究中同时覆盖文本与图像、二分类与多分类任务。
- **严谨性**：严格控制变量，避免因数据划分、提示工程差异导致偏差。
- **实用性导向**：聚焦临床部署中的可靠性、效率与安全性，而非单纯追求SOTA性能。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

| 数据集 | 模态 | 任务类型 | 类别数 | 样本量（训练/验证/测试） |
|--------|------|----------|--------|---------------------------|
| **Diabetes** | Text | Binary | 2 | 2,214 / - / 554 |
| **Mental Health Disorders** | Text | Multiclass | 12 | 1,598 / - / 400 |
| **Skin Cancer (Segmented)** | Image | Binary | 2 | 1,687 / 422 / 528 |
| **Respiratory Disease** | Image | Multiclass | 5 | 560 / 80 / 160 |

> 所有数据集均为公开可用，确保可复现性。

---

### 实验设置和评估指标

#### 统一评估协议
- 所有模型使用相同的训练/测试划分。
- 评估指标包括：
  - **Accuracy**
  - **F1-Score**（多分类使用 weighted average）
  - **ROC-AUC**（适用于二分类）

#### 模型分类与配置
| 模型类别 | 具体模型 | 配置说明 |
|---------|--------|----------|
| **Classical ML** | Logistic Regression (LR), LightGBM | 特征标准化 + one-hot编码（文本任务）；CNN/ResNet-50用于图像 |
| **Prompt-based LLM/VLM** | Gemini 2.5 Flash (text), Gemini 2.5 Multimodal (image) | Zero-shot 推理，严格设计 prompt template 保证输出一致性 |
| **Fine-tuned PEFT Model** | Gemma3_Instruct_1B (text), Gemma3 4B-PT (vision) | 使用 LoRA (r=4) 微调1个epoch，相同prompt结构 |

---

### 基线方法对比
- **传统ML基线**：代表成熟、可解释、高效的经典方法。
- **零样本LLM/VLM**：体现无需训练即可迁移知识的能力。
- **LoRA微调模型**：代表当前流行的参数高效适配策略（Parameter-Efficient Fine-Tuning, PEFT）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### ✅ 表格：Text Classification Test Set Results

| Task | Model | Accuracy | F1-Score | ROC-AUC |
|------|-------|----------|----------|---------|
| **Diabetes (Binary)** | LightGBM (ML) | **0.9982** | **0.9974** | **0.9992** |
| | Logistic Regression (ML) | 0.7726 | 0.6272 | 0.8355 |
| | Gemini 2.5 (Zero-Shot) | 0.4224 | 0.4576 | 0.4898 |
| | Gemma 1B (LoRA) | 0.6661 | 0.0887 | 0.5194 |
| **Mental Health (Multiclass)** | LightGBM (ML) | **0.9866** | **0.9821** | **0.9902** |
| | Logistic Regression (ML) | 0.9700 | 0.9655 | 0.9771 |
| | Gemini 2.5 (Zero-Shot) | 0.2807 | 0.2660 | N/A |
| | Gemma 1B (LoRA) | **0.0150** | **0.0004** | N/A |

> 💡 结论：**传统ML在结构化文本任务上表现近乎完美，而LLM方法严重落后，尤其是LoRA微调模型几乎完全失败。**

---

#### ✅ 表格：Image Classification Test Set Results

| Task | Model | Accuracy | F1-Score |
|------|-------|----------|----------|
| **Skin Cancer (Binary)** | CNN (ML Baseline) | **0.8277** | **0.8154** |
| | Gemini 2.5 (Zero-Shot) | 0.5890 | 0.3439 |
| | Gemma 3 4B-PT (LoRA) | 0.5455 | 0.2012 |
| **Respiratory Disease (Multiclass)** | ResNet-50 (ML Baseline) | 0.4812 | 0.4663 |
| | Gemini 2.5 (Zero-Shot) | **0.4875** | **0.4638** |
| | Gemma 3 4B-PT (LoRA) | 0.3688 | 0.2127 |

> 💡 结论：
> - 在图像二分类任务中，**CNN仍显著优于LLM/VLM**；
> - 在多分类图像任务中，**零样本Gemini 2.5与ResNet-50性能相当**，表现出竞争力；
> - **LoRA微调模型在所有任务中表现最差**。

---

### 消融实验结果（Sensitivity Analysis）

为探究LoRA微调失败原因，作者进行了敏感性分析（以Diabetes任务为例）：

#### 🔹 影响LoRA Rank（r=4 → r=8）
- **Accuracy下降至0.6606**
- **F1-Score暴跌至0.0693**
- 表明增加参数量并未提升性能，反而可能引发过拟合。

#### 🔹 增加训练轮次（Epochs）
| Epochs | Accuracy | F1-Score | ROC-AUC |
|--------|----------|----------|---------|
| 1 | 0.6661 | 0.0887 | 0.5194 |
| 5 | 0.8141 | 0.7049 | 0.7738 |
| 10 | **0.8347** | **0.7152** | **0.7987** |

> ✅ 发现：**延长训练时间可显著改善LoRA模型性能**，但即使训练10轮仍无法超越LightGBM（0.9982），且远高于常规“高效微调”的计算成本预期。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **传统机器学习模型仍是医疗分类中最可靠的选择**  
   尤其在**结构化文本数据**（如电子病历、实验室指标）上，LightGBM 和 Logistic Regression 实现了接近完美的分类性能，远超任何LLM方案。

2. **LLM在结构化数据上的表现令人失望**  
   即使通过自然语言格式输入，**零样本LLM（Gemini 2.5）准确率仅为~0.42**，甚至低于随机猜测水平（0.5）。这表明LLM难以有效解析数值型医学特征之间的逻辑关系。

3. **PEFT（如LoRA）并非万能钥匙，最小化微调可能适得其反**  
   所有LoRA微调模型在四项任务中均表现最差，说明**极短周期（1 epoch）的微调不足以完成领域适应**，反而破坏原始模型能力。

4. **零样本VLM在复杂图像多分类任务中展现潜力**  
   在Respiratory Disease任务中，**Gemini 2.5零样本性能与ResNet-50持平（~0.48）**，表明VLM具备一定的跨类别泛化能力，值得进一步探索。

5. **Foundation Models 存在临床部署风险**  
   包括：
   - **高延迟与碳足迹**
   - **概率校准差**
   - **幻觉（hallucination）与格式错误输出**
   - **对否定词理解困难**（如“no pneumonia”易误判）

---

### 方法的局限性
- 实验仅基于四个公开小规模数据集，未涵盖更大规模真实世界EHR或DICOM数据。
- 所有LLM实验依赖API调用，无法深入分析内部注意力机制。
- LoRA微调仅尝试固定rank和少量epochs，未进行完整超参搜索。
- 缺乏对推理成本（latency, energy consumption）的量化对比。

---

### 未来工作方向
1. **开发更适合医疗结构化数据的LLM适配策略**  
   如结合 symbolic reasoning 或 rule injection 的 hybrid 架构。

2. **优化PEFT训练策略**  
   探索更长训练周期、动态rank调整、课程学习等方法提升微调效果。

3. **构建统一的医疗多模态基准平台**  
   支持文本+图像联合推理任务（如 radiology report generation + image classification）。

4. **加强安全性和可解释性研究**  
   开发针对医疗场景的LLM输出约束机制、不确定性估计模块。

5. **探索轻量化LLM在边缘设备上的部署可行性**  
   平衡性能、成本与隐私保护需求。

---

> 📌 **最终结论**：  
> “**LLM is Not All You Need**” —— 尽管Foundation Models展现了强大的潜力，但在许多现实医疗分类任务中，**传统机器学习仍然是更高效、稳定、可信的选择**。盲目追求数值大模型可能导致资源浪费与临床风险。未来的方向应是**融合而非替代**：将LLM作为辅助工具，与经典ML协同构建稳健的医疗AI系统。

</details>

---

### 14. [AgentsEval: Clinically Faithful Evaluation of Medical Imaging Reports via Multi-Agent Reasoning](https://arxiv.org/abs/2601.16685)

**Authors**: Suzhong Fu, Jingqi Dong, Xuan Ding, Rui Sun, Yiming Yang, Shuguang Cui, Zhen Li  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.16685v1  

#### Abstract
Evaluating the clinical correctness and reasoning fidelity of automatically generated medical imaging reports remains a critical yet unresolved challenge. Existing evaluation methods often fail to capture the structured diagnostic logic that underlies radiological interpretation, resulting in unreli...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：AgentsEval: Clinically Faithful Evaluation of Medical Imaging Reports via Multi-Agent Reasoning**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前医学影像报告生成系统（如基于LLM/VLM的模型）虽然在语言流畅性和风格一致性上表现良好，但其**临床正确性**（clinical correctness）和**推理保真度**（reasoning fidelity）缺乏可靠评估。传统NLG指标（如BLEU、ROUGE）仅衡量词法重叠，无法判断诊断逻辑是否准确；而单代理LLM评估方法存在**提示敏感性强、稳定性差、可解释性低**等问题。

### **提出了什么新方法或新思路**
提出 **AgentsEval** —— 一种**多智能体流式推理框架**（multi-agent stream reasoning framework），模拟放射科医生的协作诊断流程，将评估分解为多个可解释阶段：
- **Base criteria pool generator**：构建通用临床标准池
- **Criteria Identification Agent (Acrit)**：动态识别当前报告的关键临床指标
- **GT Analyzer Agent (Agt)**：从真实报告中提取结构化证据
- **Prediction Matcher Agent (Apred)**：从生成报告中匹配对应发现
- **Evaluation Agent (Aeval)**：逐项打分并加权汇总

该过程产生**显式的推理轨迹**（explicit reasoning traces），提升透明度与可信度。

### **相比现有方法的优势**
| 维度 | 传统方法 | AgentsEval |
|------|--------|-----------|
| **语义鲁棒性** | 对同义改写极度敏感（如A3导致BLEU暴跌） | 在同义改写下保持稳定评分 |
| **事实敏感性** | 无法区分“subtle lesion” vs “small lesion”等语义偏差 | 可检测B1–B3级语义偏移，评分随错误严重程度单调下降 |
| **可解释性** | 黑箱输出单一分数 | 输出结构化比对结果与细粒度得分 |
| **临床对齐性** | 与专家判断相关性弱 | 与医师标注误差高度一致（Spearman > 0.92） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
共使用五个公开医学报告数据集，覆盖多种模态与领域：

| 数据集 | 类型 | 报告数量 | 特点 |
|-------|-----|---------|------|
| **CT-RATE** | 胸部CT | 100 | 包含Findings和Impression节段 |
| **FFA-IR** | 眼底荧光血管造影 | 100 | 英文报告，测试跨语言/风格鲁棒性 |
| **MedVAL-Bench** | 多模态 | 86 | 医师标注明确的事实错误数 |
| **RadEvalX** | 胸部X光 | 100 | 放射科医生分级标注错误严重性（显著/非显著） |
| **ReXErr-v1** | 合成CXR报告 | 100 | 引入3种可控临床错误版本（3-Error）及A1–A3级同义改写 |

> ⚠️ 所有实验**仅使用文本报告**，不依赖图像输入，聚焦于语义一致性评估。

### **实验设置和评估指标**
- **扰动设计**：
  - **A系列（同义改写）**：A1（轻度）、A2（中度）、A3（重度）
  - **B系列（语义偏移）**：B1（轻微）、B2（中等）、B3（严重）
- **评估方式**：
  - 计算各方法在每组扰动下的平均得分
  - 分析评分趋势是否与临床错误严重性**单调相关**
  - 使用 **Spearman相关系数** 和 **DTW（Dynamic Time Warping）距离** 衡量与人类标注的一致性

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **传统NLG指标** | BLEU, ROUGE-1, METEOR, CHRF, Bert-Score |
| **单代理LLM评估** |  
| - Single-Agent (detailed) | 结构化提示，要求逐步推理 |
| - Single-Agent (simple) | 简洁提示，直接评分相似性 |

所有AgentsEval模块均基于 **DeepSeek-V3.2 (685B)** 实现，API调用温度设为0.05以确保可复现性。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ 在CT-RATE上的表现（见Table 1）
| 版本 | BLEU | ROUGE-1 | Bert-Score | **AgentsEval** |
|------|------|----------|------------|----------------|
| F-A1 | 23.8 | 65.6 | 92.8 | **97.0** |
| F-A3 | 8.0 | 49.4 | 89.2 | **96.5**（几乎无损） |
| F-B3 | 39.7 | 64.7 | 92.2 | **10.6**（显著下降） |

> 📌 **AgentsEval在A3下仅降0.5分，而BLEU降幅达66%；在B3下正确反映严重错误**

#### ✅ 在FFA-IR上的稳健性（Table 2）
| Version | BLEU | Bert-Score | **AgentsEval** |
|--------|------|-------------|----------------|
| A3 | 14.1 | 90.6 | **78.2** |
| B3 | 31.6 | 92.3 | **15.3** |

> 📌 显示AgentsEval在不同模态下均具备强鲁棒性与高敏感性

#### ✅ 与人类标注的相关性（Table 3）
| Metric | Spearman (MedVAL) | DTW (MedVAL) | Spearman (RadEvalX) | DTW (RadEvalX) |
|--------|--------------------|---------------|----------------------|------------------|
| BLEU | 0.339 | 7.636 | 0.170 | 8.155 |
| Bert-Score | 0.782 | 1.180 | 0.789 | 0.909 |
| **AgentsEval** | **0.933** | **0.846** | **0.927** | **1.867** |

> 📌 **AgentsEval在Spearman上全面领先，尤其优于传统指标**

### **与基线方法的对比结果**
- **传统指标**：在A类改写下剧烈波动，在B类错误中甚至出现“错误报告得分更高”的反常现象
- **Bert-Score**：虽较耐改写，但仍难以区分B2/B3级事实错误
- **Single-Agent (detailed)**：有一定判别能力，但方差较大，稳定性不如AgentsEval
- **AgentsEval**：实现**高鲁棒性 + 高敏感性 + 单调响应**，最贴近临床现实

### **消融实验结果（隐含分析）**
- **模型规模影响**（Figure 6）：
  - 小模型（Qwen3-0.6B）无法有效区分A/B类变化
  - 大模型（≥32B参数）达到饱和性能，说明**足够推理能力是前提**
- **多智能体协作优势**：
  - 相比Single-Agent，AgentsEval具有更平滑、更一致的趋势曲线，验证了**模块化解耦带来的稳定性增益**

---

## **4. 关键结论和发现**

### **主要发现**
1. **传统NLG指标严重偏离临床需求**：高BLEU/ROUGE可能掩盖重大诊断错误。
2. **AgentsEval实现了临床对齐的评估范式转变**：
   - 不再依赖表面相似性，而是通过结构化推理判断**诊断逻辑一致性**
   - 提供**可审计的中间输出**，支持人工审查与模型调试
3. **多智能体架构显著提升评估质量**：
   - 模块化分工增强稳定性与可解释性
   - 推理链显式暴露错误来源（如漏诊、误判、矛盾陈述）
4. **大模型规模是保障语义理解的基础**：小于32B的模型难以胜任复杂医学语义解析任务

### **方法的局限性**
- **依赖大型基础模型**：计算成本高，不利于轻量化部署
- **仍为文本驱动**：未结合图像进行视觉接地（image-grounded feedback）
- **潜在偏见风险**：训练数据中的系统性偏差可能被继承
- **自动化程度有限**：目前为顺序推理，尚未实现反馈闭环优化

### **未来工作方向**
- 开发**高效并行推理机制**与**轻量级模型蒸馏方案**
- 扩展至**多模态评估**，整合视觉证据进行联合验证
- 构建**agent feedback loop**，用于指导报告生成模型迭代优化
- 探索**human-in-the-loop**机制，持续校准评估标准随医学进展演进

---

> 🔚 **总结一句话**：  
> **AgentsEval首次将放射科医生的诊断思维转化为可计算的多智能体评估流程，在保持语言多样性容忍度的同时精准捕捉临床错误，推动医学AI评估迈向透明、可信、临床可用的新阶段。**

</details>

---

### 15. [The Art of Being Difficult: Combining Human and AI Strengths to Find Adversarial Instances for Heuristics](https://arxiv.org/abs/2601.16849)

**Authors**: Henri Nikoleit, Ankit Anand, Anurag Murty Naredla, Heiko R\"oglin  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.16849v1  

#### Abstract
We demonstrate the power of human-LLM collaboration in tackling open problems in theoretical computer science. Focusing on combinatorial optimization, we refine outputs from the FunSearch algorithm [Romera-Paredes et al., Nature 2023] to derive state-of-the-art lower bounds for standard heuristics. ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*The Art of Being Difficult: Combining Human and AI Strengths to Find Adversarial Instances for Heuristics*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文致力于解决**组合优化问题中启发式算法的最坏情况分析**这一长期开放问题。具体而言，研究者旨在为经典启发式算法构造**对抗实例（adversarial instances）**，以揭示其在极端情况下的性能下限（lower bounds），从而挑战并改进对这些算法的理解。

许多启发式算法（如 Best-Fit、Nemhauser-Ullmann）虽然在实践中表现良好，但其理论性能边界（尤其是下界）多年未被显著突破。传统搜索方法（如 local search）难以生成具有结构性洞见的复杂实例。

---

### 提出了什么新方法或新思路
论文提出了名为 **Co-FunSearch**（Collaborative FunSearch）的新框架，结合了 **Large Language Models (LLMs)** 与 **人类专家协作** 来进化生成对抗程序。

- **核心思想**：将每个问题实例编码为一个可执行的 Python 程序 `P`，其输出是具体的输入向量（如物品列表、点集等）。目标是通过演化这些程序来最大化某个“得分”函数（即启发式与最优解之间的差距）。
- **流程**：
  1. 使用 LLM（基于 FunSearch 范式）从初始程序出发，迭代生成改进版本；
  2. 人类专家分析生成的程序，识别模式、简化逻辑、去除冗余、推广结构；
  3. 将简化后的程序重新注入搜索循环，形成正反馈闭环。

这种方法不同于传统的黑箱优化（如遗传算法、模拟退火），而是利用 LLM 生成**可解释、可泛化、结构化的代码**作为中间表示。

---

### 相比现有方法的优势
| 方法 | 局限性 | Co-FunSearch 的优势 |
|------|--------|---------------------|
| **Local Search / Tabu Search** | 搜索空间为原始向量，结果不可读，缺乏结构洞见，易陷入局部最优 | 以程序为单位进行搜索，天然支持泛化与缩放；生成的结果具有数学结构，便于人类理解与证明 |
| **纯 FunSearch（无协作）** | LLM 可能生成硬编码常数、冗余结构，难以直接用于理论推导 | 人类介入可提炼出简洁通用的构造模板，并进一步提升性能 |
| **手工构造** | 极其耗时且依赖直觉，进展缓慢（部分问题十年无进展） | LLM 提供创造性起点，人类提供严谨性与抽象能力，实现“1+1 > 2”的协同效应 |

> ✅ **关键创新**：首次系统展示 **Human-LLM 协作** 在理论计算机科学研究中的强大潜力——不仅加速发现，还能推动严格数学结论的建立。

---

## 2. 核心实验方法和设置

### 使用的问题与领域（相当于“数据集”）
本研究并未使用传统意义上的“数据集”，而是在以下四个经典的 **组合优化问题** 上构建对抗实例：

| 问题 | 启发式算法 | 目标 |
|------|------------|------|
| **Knapsack Problem** | Nemhauser-Ullmann Heuristic | 构造使中间 Pareto 集远大于最终 Pareto 集的实例 |
| **Bin Packing** | Best-Fit Heuristic (Random Order Model) | 提高期望使用的箱子数相对于最优值的比例 |
| **Hierarchical Clustering** | k-median Objective | 构造任意层次聚类都无法接近非层次最优的实例 |
| **Generalized Gasoline Problem** | Iterative Rounding Algorithm | 找到近似比超过 2 的反例，打破猜想 |

这些问题均属于 **NP-hard**，且在理论界有长期关注但进展停滞。

---

### 实验设置和评估指标

#### 共同范式
- 每个实例由一个 Python 函数 `get_instance()` 输出。
- **评分函数（Score Function）** 定义为：
  $$
  R = \frac{\text{Score}(H(I))}{\text{Score}(\text{Opt}(I))}
  $$
  其中 $H$ 是启发式算法，$\text{Opt}$ 是最优解，目标是最大化 $R$（最小化问题）或其倒数（最大化问题）。

#### 具体设置
| 组件 | 设置说明 |
|------|----------|
| **LLM 模型** | 使用 `gpt-4.1-nano`, `gpt-4.1-mini`, `open-mistral-nemo` 进行比较 |
| **温度参数** | 设为 1.0–1.5，鼓励多样性 |
| **搜索预算** | 最多 10 轮 × 数千次采样（LLM 查询） |
| **执行平台** | 自定义演化框架，集成 LLM API、程序执行、错误处理与数据库存储 |
| **人类干预环节** | 手动简化程序结构、提取参数化形式、尝试数学归纳与证明 |

---

### 基线方法对比
| 基线方法 | 描述 |
|---------|------|
| **Local Search** | 在向量空间添加噪声进行邻域搜索，代表传统元启发式 |
| **Base FunSearch** | 不加人工干预的标准 FunSearch 流程 |
| **Previous SOTA** | 文献中已知的最佳下界构造（如 Albers et al., 2021） |
| **Trivial Instance** | 初始简单构造（如 `[0.4, 0.5, 0.6]`） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| Method | Knapsack | Bin-Packing | k-median | Gasoline |
|-------|---------|-------------|----------|----------|
| Previous Best Known Lower Bound | 2.0 | 1.3 | 1.0 | 2.0 |
| Local Search | 1.93 | 1.478 | 1.36 | 2.11 |
| FunSearch | 646.92 | 1.497 | 1.538 | 3.05 |
| **Co-FunSearch** | **n^{O(√n)}** | **1.5** | **1.618 (golden ratio)** | **4.65** |
| Known Upper Bound | O(2^n) | 1.7 | 16 | None |

> 📈 所有问题上，**Co-FunSearch 显著超越所有基线**，并在多个问题上取得**十年来首次实质性突破**。

---

### 与基线方法的对比结果

#### ✅ **Knapsack Problem**
- **成果**：构造出使得 Nemhauser-Ullmann 算法运行时间呈超多项式的实例。
- **意义**：**首次证明该算法不具备 output-polynomial 时间复杂度**，解决了长期悬而未决的问题。
- **后续**：独立发现了指数级下界（见 Nikoleit, 2025），也由 Co-FunSearch 辅助验证。

#### ✅ **Bin Packing (Best-Fit)**
- **成果**：将随机顺序模型下的绝对近似比下界从 **1.3 → 1.5**。
- **构造**：设计两组大小分别为 $m$ 和 $m+1$ 的物品，在容量为 $m(m+1)$ 的箱子中，只有当同类物品分开装箱才最优；但 Best-Fit 很可能混装导致浪费。
- **结论**：Theorem 3.2 成立：
  > *The absolute random-order ratio of Best-Fit is between 1.5 and 1.7.*

#### ✅ **Hierarchical k-median Clustering**
- **成果**：获得首个非平凡的 **Price of Hierarchy** 下界：**黄金比例 ≈ 1.618**。
- **构造**：利用高维空间中带权重点的几何配置，强制层次聚类在某一层必须合并关键点，导致成本剧增。
- **意义**：此前没有任何非平凡下界，上界为 16，现将差距缩小。

#### ✅ **Generalized Gasoline Problem**
- **成果**：构造出迭代舍入算法（iterative rounding）近似比高达 **4.65** 的实例（d=3, k=5），**彻底否定其为 2-近似算法的猜想**。
- **扩展性**：原问题在一维已被研究，本文成功将其推广至二维及以上，并发现更差表现。

---

### 消融实验结果（Ablation Studies）

#### （1）不同 LLM 模型的影响（Fig. 3a-b）
- 更小的模型 `gpt-4.1-nano` 在**最大得分**上略优于更大的 `gpt-4.1-mini`。
- 原因：大模型平均性能更好（移动平均更高），但小模型产生更多“幸运突变”，适合追求**最佳样本**的任务。
> 💡 结论：对于可验证目标（verifiable objectives），**不一定需要最强模型**。

#### （2）温度参数影响（Fig. 3c）
- **高温（high temperature）效果更好**，因为增加了输出多样性，有助于跳出局部陷阱。
- 尽管平均质量下降，但**最高分持续上升**，说明探索比开发更重要。

#### （3）初始程序结构的重要性（Fig. 3d）
- **结构化初始程序**（如含循环）虽起始得分低，但能持续改进；
- **硬编码初始程序** 改进有限；
- **已有 SOTA 实例作为起点** 起始高但迅速停滞。
> 💡 结论：**良好的程序骨架比高质量起点更重要**，利于 LLM 学习泛化模式。

---

## 4. 关键结论和发现

### 主要发现
1. **Human-LLM 协作是理论研究的强大新范式**：
   - LLM 提供创造性候选；
   - 人类提炼结构、完成证明；
   - 二者结合可突破长期僵局。

2. **程序作为搜索对象优于原始向量**：
   - 支持参数化、缩放、泛化；
   - 内嵌语义结构，易于理解和修改；
   - 符合 Kolmogorov 复杂度低的本质规律。

3. **多个经典问题取得历史性突破**：
   - Knapsack：证伪 output-polynomial 时间猜想；
   - Bin Packing：下界从 1.3 → 1.5；
   - k-median：首个非平凡 Price of Hierarchy 下界；
   - Gasoline：打破 2-近似猜想。

4. **小型 LLM + 高温 + 结构化初始化 = 高效探索策略**

---

### 方法的局限性
- 并非所有问题都能成功：
  - 未能改进 k-means 的 Price of Hierarchy；
  - 未能找到 Best-Fit 渐近随机顺序比的更好下界；
  - 对 Ward’s method 层次聚类无效。
- 严重依赖人类专家的洞察力，自动化程度仍有待提高。
- 计算成本较高（数千次 LLM 调用），尤其涉及 Gurobi 等求解器时。

---

### 未来工作方向
1. **自动化提炼过程**：开发工具自动识别程序中的重复模式、参数关系，减少人工负担。
2. **构建专用 LLM 编辑器**：专用于“程序变异 + 性能预测”的微调模型。
3. **应用于其他理论问题**：如图论极值问题、电路复杂性、博弈论均衡分析等。
4. **形式化验证集成**：将 Coq/Lean 等证明助手接入流程，实现端到端可验证发现。
5. **探索更多协作模式**：如多人协作标注、跨领域专家参与等。

---

> 🔚 **总结一句话**：  
> **Co-FunSearch 展示了 LLM 不仅是工具，更是“思维伙伴”——它与人类共同编织困难的艺术，从而揭开算法深处最顽固的秘密。**

</details>

---

### 16. [Limits of n-gram Style Control for LLMs via Logit-Space Injection](https://arxiv.org/abs/2601.16224)

**Authors**: Sami-ul Ahmed  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16224v1  

#### Abstract
Large language models (LLMs) are typically personalized via prompt engineering or parameter-efficient fine-tuning such as LoRA. However, writing style can be difficult to distill into a single prompt, and LoRA fine-tuning requires computationally intensive training and infrastructure. We investigate...

---

### 17. [Graph-Anchored Knowledge Indexing for Retrieval-Augmented Generation](https://arxiv.org/abs/2601.16462)

**Authors**: Zhenghao Liu, Mingyan Wu, Xinze Li, Yukun Yan, Shuo Wang, Cheng Yang, Minghe Yu, Zheni Zeng, Maosong Sun  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16462v1  

#### Abstract
Retrieval-Augmented Generation (RAG) has emerged as a dominant paradigm for mitigating hallucinations in Large Language Models (LLMs) by incorporating external knowledge. Nevertheless, effectively integrating and interpreting key evidence scattered across noisy documents remains a critical challenge...

---

### 18. [AuroraEdge-V-2B: A Faster And Stronger Edge Visual Large Language Model](https://arxiv.org/abs/2601.16615)

**Authors**: Xiang Chen  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16615v1  

#### Abstract
Recently, due to the advancement of multimodal technology, people are attempting to use visual large language models (VLLMs) in industrial production. Many deep learning models (DLMs) deployed in the production environment are gradually being replaced by VLLMs. Compared with DLMs, VLLMs have some ad...

---

### 19. [Robust Categorical Data Clustering Guided by Multi-Granular Competitive Learning](https://arxiv.org/abs/2601.16491)

**Authors**: Shenghong Cai, Yiqun Zhang, Xiaopeng Luo, Yiu-Ming Cheung, Hong Jia, Peng Liu  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16491v1  

#### Abstract
Data set composed of categorical features is very common in big data analysis tasks. Since categorical features are usually with a limited number of qualitative possible values, the nested granular cluster effect is prevalent in the implicit discrete distance space of categorical data. That is, data...

---

### 20. [Multigrade Neural Network Approximation](https://arxiv.org/abs/2601.16884)

**Authors**: Shijun Zhang, Zuowei Shen, Yuesheng Xu  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16884v1  

#### Abstract
We study multigrade deep learning (MGDL) as a principled framework for structured error refinement in deep neural networks. While the approximation power of neural networks is now relatively well understood, training very deep architectures remains challenging due to highly non-convex and often ill-...

---

### 21. [FedSGM: A Unified Framework for Constraint Aware, Bidirectionally Compressed, Multi-Step Federated Optimization](https://arxiv.org/abs/2601.16897)

**Authors**: Antesh Upadhyay, Sang Bin Moon, Abolfazl Hashemi  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.16897v1  

#### Abstract
We introduce FedSGM, a unified framework for federated constrained optimization that addresses four major challenges in federated learning (FL): functional constraints, communication bottlenecks, local updates, and partial client participation. Building on the switching gradient method, FedSGM provi...

---

### 22. [AgentDrive: An Open Benchmark Dataset for Agentic AI Reasoning with LLM-Generated Scenarios in Autonomous Systems](https://arxiv.org/abs/2601.16964)

**Authors**: Mohamed Amine Ferrag, Abderrahmane Lakas, Merouane Debbah  
**Category**: cs.AI  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16964v1  

#### Abstract
The rapid advancement of large language models (LLMs) has sparked growing interest in their integration into autonomous systems for reasoning-driven perception, planning, and decision-making. However, evaluating and training such agentic AI models remains challenging due to the lack of large-scale, ...

---

### 23. [PolyAgent: Large Language Model Agent for Polymer Design](https://arxiv.org/abs/2601.16376)

**Authors**: Vani Nigam, Achuth Chandrasekhar, Amir Barati Farimani  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16376v1  

#### Abstract
On-demand Polymer discovery is essential for various industries, ranging from biomedical to reinforcement materials. Experiments with polymers have a long trial-and-error process, leading to long procedures and extensive resources. For these processes, machine learning has accelerated scientific dis...

---

### 24. [Persona Jailbreaking in Large Language Models](https://arxiv.org/abs/2601.16466)

**Authors**: Jivnesh Sandhan, Fei Cheng, Tushar Sandhan, Yugo Murawaki  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16466v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in domains such as education, mental health and customer support, where stable and consistent personas are critical for reliability. Yet, existing studies focus on narrative or role-playing tasks and overlook how adversarial conversational histo...

---

### 25. [Select or Project? Evaluating Lower-dimensional Vectors for LLM Training Data Explanations](https://arxiv.org/abs/2601.16651)

**Authors**: Lukas Hinterleitner, Loris Schoenegger, Benjamin Roth  
**Category**: cs.CL  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16651v1  

#### Abstract
Gradient-based methods for instance-based explanation for large language models (LLMs) are hindered by the immense dimensionality of model gradients. In practice, influence estimation is restricted to a subset of model parameters to make computation tractable, but this subset is often chosen ad hoc ...

---

### 26. [A Regularized Actor-Critic Algorithm for Bi-Level Reinforcement Learning](https://arxiv.org/abs/2601.16399)

**Authors**: Sihan Zeng, Sujay Bhatt, Sumitra Ganesh, Alec Koppel  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16399v1  

#### Abstract
We study a structured bi-level optimization problem where the upper-level objective is a smooth function and the lower-level problem is policy optimization in a Markov decision process (MDP). The upper-level decision variable parameterizes the reward of the lower-level MDP, and the upper-level objec...

---

### 27. [Bayesian Experimental Design for Model Discrepancy Calibration: A Rivalry between Kullback--Leibler Divergence and Wasserstein Distance](https://arxiv.org/abs/2601.16425)

**Authors**: Huchen Yang, Xinghao Dong, Jin-Long Wu  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16425v1  

#### Abstract
Designing experiments that systematically gather data from complex physical systems is central to accelerating scientific discovery. While Bayesian experimental design (BED) provides a principled, information-based framework that integrates experimental planning with probabilistic inference, the sel...

---

### 28. [BoostFGL: Boosting Fairness in Federated Graph Learning](https://arxiv.org/abs/2601.16496)

**Authors**: Zekai Chen, Kairui Yang, Xunkai Li, Henan Sun, Zhihan Zhang, Jia Li, Qiangqiang Dai, Rong-Hua Li, Guoren Wang  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16496v1  

#### Abstract
Federated graph learning (FGL) enables collaborative training of graph neural networks (GNNs) across decentralized subgraphs without exposing raw data. While existing FGL methods often achieve high overall accuracy, we show that this average performance can conceal severe degradation on disadvantage...

---

### 29. [Rethinking Large Language Models For Irregular Time Series Classification In Critical Care](https://arxiv.org/abs/2601.16516)

**Authors**: Feixiang Zheng, Yu Wu, Cecilia Mascolo, Ting Dang  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16516v1  

#### Abstract
Time series data from the Intensive Care Unit (ICU) provides critical information for patient monitoring. While recent advancements in applying Large Language Models (LLMs) to time series modeling (TSM) have shown great promise, their effectiveness on the irregular ICU data, characterized by particu...

---

### 30. [Predicting Startup Success Using Large Language Models: A Novel In-Context Learning Approach](https://arxiv.org/abs/2601.16568)

**Authors**: Abdurahman Maarouf, Alket Bakiaj, Stefan Feuerriegel  
**Category**: cs.LG  
**Published**: 2026-01-26  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.16568v1  

#### Abstract
Venture capital (VC) investments in early-stage startups that end up being successful can yield high returns. However, predicting early-stage startup success remains challenging due to data scarcity (e.g., many VC firms have information about only a few dozen of early-stage startups and whether they...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
