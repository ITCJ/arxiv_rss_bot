# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-20 06:36:33 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Mobility-Aware Cache Framework for Scalable LLM-Based Human Mobility Simulation](https://arxiv.org/abs/2602.16727)

**Authors**: Hua Yan, Heng Tan, Yingxue Zhang, Yu Yang  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.16727v1  

#### Abstract
Large-scale human mobility simulation is critical for applications such as urban planning, epidemiology, and transportation analysis. Recent works treat large language models (LLMs) as human agents to simulate realistic mobility behaviors using structured reasoning, but their high computational cost...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Mobility-Aware Cache Framework for Scalable LLM-Based Human Mobility Simulation**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现有的基于 **Large Language Models (LLMs)** 的人类移动性模拟方法虽然能够生成逼真的个体行为轨迹，但由于需要对每个个体进行独立的 LLM 推理，导致计算成本极高，难以扩展到大规模人群（如百万级代理）的模拟。此外，传统响应缓存（response caching）虽能降低调用次数，但会严重限制行为多样性，损害模拟的真实性。

### **提出了什么新方法或新思路**
本文提出了一种名为 **MobCache** 的 **mobility-aware cache framework**，其核心创新在于引入“**reconstructible caches**”——即在**潜在空间（latent space）中缓存 LLM 的推理步骤**，而非最终输出文本。该框架包含两个关键组件：

1. **Reconstructible Cache（可重构缓存）**  
   - 将 LLM 在生成移动轨迹时的每一步推理表示为 **latent-space embedding**。
   - 构建树状结构的缓存，支持从任意中间节点分支并与其他缓存链重组，实现灵活复用。
   - 引入 **latent-space evaluator** 来评估潜在推理路径的合理性，确保逻辑一致性。

2. **Lightweight Decoder（轻量化解码器）**  
   - 使用一个小型模型通过 **mobility law-constrained distillation** 进行训练，将潜在空间中的推理链高效解码为自然语言活动序列。
   - 避免重复调用大模型进行解码，显著提升效率。

### **相比现有方法的优势**
| 方面 | 优势 |
|------|------|
| **效率** | 显著减少 LLM 调用次数，大幅降低推理时间与成本 |
| **多样性** | 支持推理链的灵活重组，避免群体化行为（group-based methods）带来的同质化问题 |
| **保真度** | 通过 mobility law 约束蒸馏保持时空一致性，维持高质量模拟 |
| **通用性** | 可作为插件加速其他 LLM-based mobility 模拟方法（如 Urban-Mobility-LLM） |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Beijing Dataset**：来自中国北京的社会网络平台，涵盖 2019 年 10 月至 12 月的真实移动轨迹及用户画像（年龄、职业等）。
- **NYC POI Check-in Dataset**：纽约市的兴趣点签到数据，结合美国人口普查（U.S. Census）统计数据模拟用户画像。

### **实验设置**
- **缓存构建**：使用 LLaMA 3.2-3B 生成约 13,000 条合成轨迹用于 fine-tuning，提取 latent-space reasoning chains 构建缓存。
- **测试集**：从真实数据中采样 10,000 条轨迹，确保无用户重叠。
- **硬件环境**：NVIDIA A100 / A6000 GPU，Python 3.10 + PyTorch 2.1.0。
- **探索率（exploration rate）** 设置为 0.5，在效率与多样性间取得平衡。

### **评估指标**

#### **效率指标（Efficiency Metrics）**
| 指标 | 描述 |
|------|------|
| **Inference time ↓** | 单条轨迹平均生成耗时（秒） |
| **Tokens per second ↑** | 每秒处理 token 数量 |
| **Throughput ↑** | 批量处理下每秒生成轨迹数（batch size=8） |
| **Cost ↓** | 单条轨迹生成成本（API 或本地 GPU 成本估算） |

#### **质量指标（Quality Metrics）**
采用 **Jensen-Shannon Divergence (JSD)** 衡量生成轨迹与真实数据分布的差异：
| 指标 | 描述 |
|------|------|
| **Radius of gyration ↓** | 个体活动范围的空间离散程度 |
| **Stay duration ↓** | 停留时长分布相似性 |
| **Jump length ↓** | 连续位置间距离分布 |
| **LocFreq ↓** | 地理网格访问频率分布 |
| **OdSim ↓** | 出发地-目的地（OD）模式相似性 |

### **基线方法对比**
分为两类：

#### **无需真实数据训练的方法**
- **CoPB**：基于计划行为链（Chain-of-Planned-Behavior）引导 LLM 推理。
- **Urban-Mobility-LLM**：利用 LLM 合成出行调查数据。
- **LLMob**：结合自洽性与检索策略的 LLM agent 框架。

#### **需真实数据训练的方法**
- **Geo-LLaMA**：在真实轨迹上微调 LLM，满足时空约束。

#### **消融变体**
- **MobCache w/o LE**：移除 latent-space evaluator，仅用相似度匹配。
- **MobCache w/o MD**：移除 mobility law distillation。
- **MobCache w/o LD**：不使用轻量化解码器，直接用原 LLM 解码。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Beijing 数据集）**

| 方法 | Inference Time ↓ (s) | Tokens/s ↑ | Throughput ↑ (traj/s) | Cost ↓ ($×10⁻³) |
|------|------------------------|------------|------------------------|------------------|
| CoPB | 69.1675 | 25.7023 | 0.1005 | 1.7776 |
| Urban-Mobility-LLM | 8.0800 | 67.3117 | 0.9010 | 0.5439 |
| Geo-LLaMA | 2.3410 | 25.8006 | 1.2701 | 0.0325 |
| **MobCache** | **1.3530** | **121.7636** | **1.6329** | **0.0187** |

> ✅ **相比最优基线（Urban-Mobility-LLM），MobCache 实现：**
- **42.20% 更低的推理时间**
- **80.89% 更高的 tokens/s**
- **28.56% 更高的吞吐量**
- **42.46% 更低的成本**

### **质量表现（JSD 值越低越好）**

| 方法 | Radius ↓ | Duration ↓ | Jump Length ↓ | LocFreq ↓ | OdSim ↓ |
|------|----------|------------|---------------|-----------|---------|
| Geo-LLaMA | 0.0216 | 0.0270 | 0.0188 | 0.1198 | 0.2498 |
| **MobCache** | 0.0333 | **0.0191** | 0.0258 | **0.1061** | **0.2215** |

> 🔍 **MobCache 在 3/5 项质量指标上优于 Geo-LLaMA**，且整体与 SOTA LLM-based 方法相当，说明其在大幅提升效率的同时未牺牲保真度。

### **NYC 数据集结果**
- **推理时间减少 ≥51.54%**
- **tokens/s 提升 79.71%**
- **成本降低 56.18%**
- 质量指标与基线持平，验证跨城市适用性。

### **案例研究：加速 Urban-Mobility-LLM**
将 MobCache 应用于 Urban-Mobility-LLM：
- **推理速度提升 66.93%**（8.08s → 2.67s/轨迹）
- **成本下降 93.18%**
- 质量无明显损失（见 Figure 6）

### **消融实验结果（Beijing 数据集）**

| 方法 | Inference Time | Cost | Duration ↓ | LocFreq ↓ | OdSim ↓ |
|------|----------------|------|------------|-----------|---------|
| **MobCache** | 1.3530 | 0.0187 | **0.0191** | **0.1061** | **0.2215** |
| w/o LE | 1.3546 | 0.0188 | 0.0203 | 0.1092 | 0.2302 |
| w/o MD | 1.3551 | 0.0188 | 0.0198 | 0.1082 | 0.2286 |
| w/o LD | 2.2450 | 0.0325 | 0.0180 | 0.0955 | 0.2013 |

> 🔍 发现：
- 移除 **latent-space evaluator** 导致质量下降最多，说明其对逻辑连贯性至关重要。
- 移除 **mobility law distillation** 也轻微影响质量。
- 移除 **lightweight decoder** 效率显著下降，但质量略优，体现设计权衡。

---

## **4. 关键结论和发现**

### **主要发现**
1. **缓存推理过程优于缓存结果**：在 latent space 中缓存并重组推理步骤，既能提高复用率，又能保持行为多样性。
2. **latent-space evaluator 是保证逻辑一致性的关键**：直接拼接文本推理链易产生时空矛盾（如“下班后去上班”），而 evaluator 可有效筛选合理路径。
3. **轻量化解码器 + mobility law 约束可兼顾效率与保真度**：蒸馏过程中显式加入 jump length 等统计特征约束，显著提升生成质量。
4. **框架具有良好的泛化能力**：可用于加速其他 LLM-based mobility 模型（如 Urban-Mobility-LLM），且支持跨城市迁移（北京缓存用于纽约模拟）。

### **局限性**
- **依赖可解释的推理流程**：仅适用于能暴露 step-by-step reasoning 的模型，无法应用于端到端生成式模型。
- **初始缓存构建仍需一定量 LLM 调用**：冷启动阶段成本较高。
- **跨城市迁移仍有性能损失**：由于 POI 分布差异，部分用户无法找到高相似度缓存。

### **未来工作方向**
- 探索更高效的 latent-space evaluator 架构。
- 结合 KV caching 进一步优化 token 级别解码速度。
- 扩展至多智能体交互场景下的联合行为模拟。
- 研究动态更新机制以适应长期演化的人类行为模式。

--- 

> 📌 **总结一句话**：  
> **MobCache 通过在潜在空间中缓存和重组 LLM 的推理步骤，并辅以轻量化解码与 mobility law 约束，实现了高效、多样且保真的大规模人类移动性模拟，是迈向低成本城市级仿真系统的重要一步。**

</details>

---

### 2. [Automating Agent Hijacking via Structural Template Injection](https://arxiv.org/abs/2602.16958)

**Authors**: Xinhao Deng, Jiaqing Wu, Miao Chen, Yue Xiao, Ke Xu, Qi Li  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.16958v1  

#### Abstract
Agent hijacking, highlighted by OWASP as a critical threat to the Large Language Model (LLM) ecosystem, enables adversaries to manipulate execution by injecting malicious instructions into retrieved content. Most existing attacks rely on manually crafted, semantics-driven prompt manipulation, which ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Automating Agent Hijacking via Structural Template Injection》核心总结

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文针对 **LLM Agent** 中日益严重的 **Agent Goal Hijacking** 安全威胁，揭示了一个根本性的架构漏洞：当前主流 LLM Agent 在处理对话时，依赖于 **Chat Template** 和特殊标记（如 `<|im_start|>`、`<|im_end|>`）来区分 `System`、`User`、`Assistant` 和 `Tool` 角色。然而，这些控制信号与外部不可信内容在 token 层面未实现严格隔离，导致攻击者可通过构造恶意的 **结构化模板注入（Structured Template Injection, STI）** 来诱导角色混淆（Role Confusion），从而劫持 Agent 执行流程。

现有基于语义的 **Indirect Prompt Injection (IPI)** 攻击存在两大缺陷：
- **成功率低**：受 RLHF 强化训练影响，模型对明显指令违背更具鲁棒性。
- **迁移性差**：为特定模型设计的语义提示难以泛化到其他闭源商业模型。

### 提出了什么新方法或新思路
作者提出 **Phantom** —— 一种自动化代理劫持框架，其核心思想是：
> **从攻击“语义行为”转向攻击“解析架构”**。

Phantom 不依赖自然语言说服力，而是通过注入精心优化的 **结构化模板序列**，伪造对话历史（如伪造 `Assistant` 响应和 `User` 查询），利用 Agent 对模板分隔符的依赖，使其将攻击载荷误认为合法上下文。

### 相比现有方法的优势
- **高攻击成功率（ASR）**：在多个 SOTA 闭源模型上平均 ASR 达 **79.76%**，显著超越现有基线。
- **强迁移性**：无需了解目标模型内部模板细节，适用于黑盒场景。
- **规避语义防御**：绕过 Safety Alignment、Context Filtering 等基于语义的防护机制。
- **自动化搜索**：首次实现跨模型结构注入攻击的自动化发现。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **AgentDojo**：最先进的 LLM Agent 安全评估基准，包含 97 个真实用户任务和 629 个对抗测试用例，覆盖 **Workspace**、**Travel**、**Slack**、**Banking** 四大场景。
- **Real-World Commercial Agents**：大规模评估了 **942 个商用 Agent**，验证实际危害。

### 实验设置和评估指标
#### 评估对象
- **闭源模型**：GPT-4o, GPT-4.1, Qwen3-Max, Gemini-3-Flash 等 7 个 SOTA 模型。
- **开源模型**：DeepSeek-V3.2。

#### 评估指标
| 指标 | 说明 |
|------|------|
| **Attack Success Rate (ASR)** | 成功触发恶意行为的样本占比 |
| **Utility Score** | 在存在攻击扰动下，Agent 完成原始任务的能力保留率 |

#### 防御场景测试
在三种典型防御机制下评估鲁棒性：
1. **Delimiter Spotlighting**：系统提示中强调分隔符内容为被动数据。
2. **Rule-based Tag Filter**：过滤工具输出中的 XML 类标签。
3. **Injection Detector**：基于 DeBERTa-v3 的语义检测器识别恶意意图。

### 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| **Semantic-Injection** | 使用 `<INFORMATION>` 等标签封装恶意指令，代表最强语义攻击 |
| **Single-Template** | 使用开源对应模型的固定模板进行攻击（如用 Qwen3 模板攻击闭源 Qwen） |
| **ChatInject** | 利用聊天模板注入多轮对话历史，但依赖人工设计 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **平均 ASR**：Phantom 在 AgentDojo 上对 7 个闭源 Agent 的平均 ASR 达 **79.76%**。
- **最高单体 ASR**：在 DeepSeek-V3.2 上达到 **78.57%**。
- **现实世界漏洞**：在 942 个商用 Agent 中发现 **超过 70 个可利用漏洞**，涵盖数据泄露、RCE 等严重风险。

### 与基线方法的对比结果
| 方法 | 平均 ASR |
|------|---------|
| Semantic-Injection | 39.86% |
| Single-Template | 54.09% |
| ChatInject | 38.46% |
| **Phantom** | **79.76%** ✅ |

> Phantom 显著优于所有基线，在所有场景和模型上均取得领先。

#### 典型案例表现（GPT-4.1）
| 方法 | ASR | Utility |
|------|-----|--------|
| Semantic-Injection | 14.29% | 75.97% |
| Phantom | **75.97%** | 18.18% |

> Phantom 的 ASR 是语义攻击的 **5 倍以上**，且能绕过 Delimiter Spotlighting 防御（ASR 仍达 76.62%）。

### 消融实验结果
#### （1）轻量级搜索策略 vs 完整 Phantom
引入基于代理评估（Proxy Evaluation）的轻量搜索，大幅降低 token 开销：
- **效率提升**：token 消耗从 184M 降至 63M（仅 **34%**）。
- **性能接近**：多数模型上 ASR 差距小于 10%，仅 Gemini-3-Flash 有较大下降。

#### （2）思考链（Thinking Mode）影响
启用 CoT 显著提升所有攻击的 ASR，表明更强的推理能力反而加剧了被劫持风险：
- Phantom 在 Qwen-Flash 上 ASR 从 **55.84% → 67.53%**。
- 表明增强推理未同步增强安全性。

#### （3）注意力分析（Interpretability）
通过扰动实验验证攻击依赖的是 **语法结构** 而非具体 token：
| 扰动方式 | 攻击成功率 | 注意力（攻击载荷） |
|--------|----------|------------------|
| 原始模板 | 100% | 10.82 |
| HTML 编码 `<` → `&lt;` | 46.29% | 4.89 |
| 移除括号 | 0.05% | 6.36 |
| 移除整个模板 | 0.00% | 5.97 |

> 只要保持“标签式”语法结构，即使 token 被编码仍可成功；一旦结构破坏，攻击即失效。

---

## 4. 关键结论和发现

### 主要发现
1. **结构性漏洞普遍存在**：LLM Agent 对 **Chat Template 分隔符缺乏严格隔离机制**，是系统性安全短板。
2. **结构攻击 > 语义攻击**：Phantom 证明，攻击解析架构比攻击语义理解更有效、更难防御。
3. **能力越强，越易被劫持**：更高版本、更强指令遵循能力的模型（如 GPT-4.1 > GPT-4o）反而更易受结构攻击，形成“**capability curse**”。
4. **现有防御失效**：
   - 指令级防御（如 Delimiter Spotlighting）完全无效。
   - 标签过滤可部分缓解，但自动化搜索能找到绕过变体。
   - 语义检测器虽可压制 ASR，但严重损害功能性（Utility 下降至 15.58%）。

### 方法的局限性
- **模态限制**：目前仅支持文本输入，未考虑多模态 Agent（如视觉、音频）。
- **黑盒假设**：未利用内部知识（如训练数据、模板规范），若攻击者为 insider 可构造更强攻击。
- **查询成本**：依赖 API 查询反馈，可能受速率限制或异常检测阻断。
- **持久性未评估**：未研究在长周期对话状态管理中的持续劫持能力。

### 未来工作方向
- **构建结构化防御原语**：如硬件辅助 token 隔离、形式化验证的模板解析器。
- **扩展至多模态环境**：研究跨模态结构注入攻击（如图像元数据注入）。
- **建立标准化基准**：推动社区开发专门评估结构攻击鲁棒性的 Benchmark。
- **探索初始化优化**：改进初始模板选择策略以加速收敛。

---

> **总结**：Phantom 揭示了当前 LLM Agent 安全体系的根本盲区——过度依赖语义对齐而忽视架构隔离。其提出的 **Structural Template Injection** 范式，不仅实现了高成功率、高迁移性的自动化攻击，更警示业界必须从“语法层面”重构 Agent 的安全边界。

</details>

---

### 3. [Operationalization of Machine Learning with Serverless Architecture: An Industrial Operationalization of Machine Learning with Serverless Architecture: An Industrial Implementation for Harmonized System Code Prediction](https://arxiv.org/abs/2602.17102)

**Authors**: Sai Vineeth Kandappareddigari, Santhoshkumar Jagadish, Gauri Verma, Ilhuicamina Contreras, Christopher Dignam, Anmol Srivastava, Benjamin Demers  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.17102v1  

#### Abstract
This paper presents a serverless MLOps framework orchestrating the complete ML lifecycle from data ingestion, training, deployment, monitoring, and retraining to using event-driven pipelines and managed services. The architecture is model-agnostic, supporting diverse inference patterns through stand...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Operationalization of Machine Learning with Serverless Architecture: An Industrial Implementation for Harmonized System Code Prediction

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文聚焦于**海关合规领域中的 HS Code（Harmonized System Code）自动分类问题**。HS Code 是全球贸易中用于商品分类的标准编码系统，其正确分配对进出口清关至关重要。然而，由于产品描述简短、非结构化且频繁更新，人工分类耗时且易出错，导致物流延误和财务损失。

传统方法依赖专家经验或简单文本分类模型，难以应对大规模、动态变化的场景。

### 🚀 提出的新方法与创新点
论文提出了一种**基于 Serverless 架构的端到端 MLOps 框架**，实现了 HS Code 分类模型的工业化部署与自动化运维。主要创新包括：

- **全生命周期自动化 MLOps 流程**：构建了一个支持数据摄入、训练、部署、监控、再训练的完整 pipeline，实现模型持续集成与交付（CI/CD）。
- **Serverless 架构设计**：采用 AWS 的无服务器服务（如 AWS Lambda, Sagemaker, Step Functions, EventBridge, S3），实现按需伸缩、高可用性和低运维成本。
- **标准化接口与模型无关性（Model-Agnostic）**：框架支持多种深度学习架构（DNN, LSTM, Text-CNN），便于快速切换和比较不同模型。
- **自动化 A/B Testing 机制**：在生产环境中动态比较多个模型的表现（accuracy, latency, cost），安全地选择最优模型上线。
- **定制化文本嵌入（Custom Embedding）**：针对工程产品描述的专业性，放弃预训练 embedding（如 Word2Vec/GloVe），转而训练 domain-specific 的 custom embedding 层，显著提升效果。

### 🔍 相比现有方法的优势
| 维度 | 本文方法 | 传统方法 |
|------|----------|---------|
| 可扩展性 | 自动弹性伸缩，适应流量波动 | 固定资源，易过载或浪费 |
| 运维效率 | 完全自动化 retraining 和 deployment | 手动干预多，响应慢 |
| 成本控制 | Pay-per-use，长期运营成本更低 | 长期运行实例开销大 |
| 模型迭代速度 | 支持快速实验与 A/B testing | 部署周期长 |
| 准确率 | 达到 98%，优于经典模型 | 多数文献报告 <95% |

---

## 2. 核心实验方法和设置

### 📊 数据集
- **来源**：来自 Schneider Electric 内部出口控制系统的真实业务数据。
- **内容**：
  - 产品短描述（Short Description）
  - 中等长度描述（Medium Description）
  - 技术参数（ETIM 标准）
  - 已标注的 HS Code（目标变量）
- **样本量**：原始数据 815,264 条；经 upsampling 后增至 818,048 条。
- **标签数量**：共 24 个 HS Code 类别（子集），覆盖实际高频使用的代码。
- **数据质量分级**：引入“assurance level”（1–4级），仅使用 level 3 和 4 的高质量标签进行训练。

### ⚙️ 实验设置
- **任务类型**：Multi-class 文本分类（Text Classification）
- **特征工程**：
  - 清洗文本（去符号、停用词、小写化）
  - 使用 Tokenizer 分词
  - 构建 custom embedding 层（未使用 GloVe/Word2Vec）
- **模型架构对比**：
  - **SVM（Baseline）**
  - **Deep Neural Network (DNN)**
  - **Long Short-Term Memory (LSTM)**
  - **Text-CNN**（最终胜出）
- **超参数优化**：采用 **Bayesian Optimization** 自动调参（见 Table II & V）。
- **类别不平衡处理**：使用 **Stratified Upsampling**（优于 SMOTE），避免合成样本带来的噪声。

### 📈 评估指标
| 指标 | 说明 |
|------|------|
| **Accuracy** | 整体预测准确率 |
| **Precision / Recall / F1-score** | 按每个 HS Code 类别分别计算，尤其关注 precision ≥90% 的表现 |
| **Latency** | 推理延迟（影响 SLA） |
| **Cost-Efficiency** | 训练与推理的综合成本（考虑云资源消耗） |
| **Statistical Significance** | 使用 **ANOVA + k-fold CV（37-fold）** 进行 A/B testing，确保结果可靠 |

---

## 3. 主要实验结果和性能指标

### 📊 性能汇总（Test Set 表现）

| Model | Accuracy | #Codes with Precision >90% | #Codes with Recall >90% | F1 (>90%) |
|-------|----------|-------------------------------|---------------------------|------------|
| **DNN** | 89% → **95%** (upsampled) | 15 → **19** | 12 → 20 | 14 → 19 |
| **LSTM** | 92% → **97%** (upsampled) | 19 → **20** | 16 → 22 | 18 → 21 |
| **Text-CNN** | **98%** (both base & upsampled) | **19 → 20** | **20 → 21** | **19 → 20** |

> ✅ **Text-CNN 在所有指标上均取得最佳表现**

### 🔬 关键发现
- **Text-CNN 明显优于 RNN/LSTM**：尽管 LSTM 能捕捉序列依赖，但在短文本（平均长度 ~64 字符）任务中，Text-CNN 更擅长提取局部关键词组（n-gram 特征），且训练更快。
- **Upsampling 显著提升小类别的 recall**：解决了 class imbalance 导致的偏差问题。
- **Custom Embedding 至关重要**：尝试使用 Word2Vec/GloVe 效果不佳，因领域术语（如工业设备命名）不在通用语料中。
- **Transformer 模型未被选用的原因**：
  - 尽管可能达到相近 accuracy，
  - 但其 **long-term operational cost 更高**（训练时间长、推理资源占用大），
  - 且缺乏可解释性（explainability），不符合海关合规场景需求。

### ⏱️ 训练时间对比（Table VII）
| Model | Base Model Total Time | Upsampled Model Total Time |
|-------|------------------------|------------------------------|
| DNN | 51 mins | **3h 36m** |
| LSTM | **5.5h** | 5.4h |
| **Text-CNN** | **3h 15m** | **3h 10m** |

> 💡 Text-CNN 不仅精度最高，训练效率也优于 LSTM。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **Text-CNN 是 HS Code 分类的最佳选择**：在短文本、高精度要求、低延迟约束下，Text-CNN 比 LSTM 和 DNN 更有效。
2. **Serverless MLOps 可实现企业级 ML 工业化**：通过 AWS 无服务器组件构建的 pipeline 具备高度自动化、可审计、可复现、SLA 可保障的特点。
3. **自动化 A/B Testing 提升模型决策科学性**：结合统计检验（ANOVA）选出最优模型，减少主观判断风险。
4. **Cost-Efficiency 是工业落地的关键考量**：不能只看 accuracy，必须权衡模型复杂度、训练成本、推理延迟与维护难度。
5. **Deterministic 模型优于 Generative AI 当前阶段**：对于合规性强的任务，确定性输出、低延迟、高可解释性比灵活性更重要。

### ⚠️ 方法的局限性
- **区域差异性限制泛化能力**：各国 HS Code 应用存在差异，模型需本地化训练。
- **无法处理全新产品类别**：遇到从未见过的产品需人工介入，直到积累足够样本。
- **HS Code 更新周期影响模型寿命**：WCO 每 5–6 年更新一次 HS Code 结构，需定期 retrain。
- **当前仅覆盖 24 个高频类别**：尚未扩展至全部 ~5000 个 HS Code。
- **依赖高质量标注数据**：assurance level 依赖专家审核，扩展成本较高。

### 🔮 未来工作方向
- 引入更多 **categorical features**（如产品类别、材质等）增强模型表现
- 探索 **SMOTE 等 advanced upsampling 技术**
- 扩展模型至 **欧洲和北美以外地区**
- 引入 **KL Divergence 等 drift detection 方法** 实现更智能的 retraining 触发
- 利用 **parallel processing** 进一步缩短训练时间
- 开发 **CloudFormation templates** 实现跨环境自动化部署
- 探索 **Generative AI 替代方案**：待其具备更高透明度与数据安全性后考虑引入

---

> 📌 **总体评价**：  
> 本文不仅提出了一个高性能的 HS Code 分类模型（Text-CNN + Custom Embedding），更重要的是提供了一个**可复制的企业级 MLOps 蓝图**，展示了如何将机器学习从实验原型成功转化为稳定、高效、可持续演进的工业系统。其 Serverless + A/B Testing + Auto-retraining 的设计理念具有广泛的推广价值。

</details>

---

### 4. [LLM4Cov: Execution-Aware Agentic Learning for High-coverage Testbench Generation](https://arxiv.org/abs/2602.16953)

**Authors**: Hejia Zhang, Zhongming Yu, Chia-Tung Ho, Haoxing Ren, Brucek Khailany, Jishen Zhao  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.16953v1  

#### Abstract
Execution-aware LLM agents offer a promising paradigm for learning from tool feedback, but such feedback is often expensive and slow to obtain, making online reinforcement learning (RL) impractical. High-coverage hardware verification exemplifies this challenge due to its reliance on industrial simu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM4Cov: Execution-Aware Agentic Learning for High-coverage Testbench Generation

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
硬件验证（hardware verification）是芯片设计流程中成本最高、耗时最长的环节之一。其核心任务是生成高覆盖率的 **testbench**，以充分验证 RTL 设计的正确性。然而：
- 每次仿真（simulation）执行开销巨大（工业级可达数小时），导致在线强化学习（online RL）不可行；
- 覆盖率信号（coverage）是非可微、稀疏且昂贵的反馈；
- 学生模型在训练过程中会遇到教师模型未覆盖的失败状态，产生 **state-distribution shift**，导致传统 SFT 效果不佳。

现有方法（如 DPO、静态数据微调）无法有效利用有限的仿真资源进行多轮交互式修复（interactive repair），也未系统解决分布偏移问题。

---

### 🚀 提出的新方法与创新思路

论文提出 **LLM4Cov** —— 首个面向高覆盖率 testbench 生成的 **execution-aware 离线代理学习框架**，核心创新如下：

#### （1）**Coverage-Guided Agentic Rejection Fine-tuning**
- 将覆盖率作为密集监督信号，对生成的 testbench 草稿进行筛选。
- 只保留低覆盖率草稿及其最具改进性的修订版本，集中监督“恢复行为”（recovery behaviors）。
- 每次仿真调用都最大化提取有用信息，提升样本效率。

#### （2）**Verification-Conditioned Progressive Learning**
- 分阶段构建合成数据并逐步训练：
  - **Stage 0**: 使用教师模型生成高质量轨迹（warm-up）
  - **Stage 1**: 使用学生模型采样中间状态 + 教师模型生成修正（imitation-style）
  - **Stage 2**: 完全由学生模型自采样并自我改进（self-sampling）
- 每一阶段的数据合成与当前学生模型的状态分布对齐，避免混合不同分布导致的信号稀释。

#### （3）**Worst-State-Prioritized Sampling**
- 在每一轮迭代中优先选择覆盖率最低的状态进行修复尝试。
- 显著提高在有限仿真预算下捕获关键失败模式的概率。

#### （4）**Memoryless State Transition Formulation**
- 将验证过程建模为无记忆状态转移：每个决策仅依赖于当前代码和仿真反馈，而非完整历史。
- 减少冗余提示长度，聚焦最新执行信号，实验证明优于 vanilla 历史拼接方式。

---

### 🔍 相比现有方法的优势
| 维度 | LLM4Cov | 现有方法（如 DPO、SFT） |
|------|--------|--------------------------|
| 执行感知 | ✅ 显式建模仿真反馈 | ❌ 忽略执行路径或仅用于过滤 |
| 数据有效性 | ✅ 动态生成+按需过滤 | ❌ 固定数据集，易过时 |
| 分布对齐 | ✅ 渐进式对齐学生分布 | ❌ 教师主导，存在协变量偏移 |
| 成本控制 | ✅ 离线训练，高效利用仿真 | ❌ 多数需在线交互，成本高 |
| 性能上限 | ✅ 小模型超越大模型 | ❌ 严重依赖模型规模 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **CVDP-ECov**：基于 CVDP benchmark (Pinckney et al., 2025) 改造的真实硬件仓库集合，共 **83 个独立硬件 repo**。
- 输入包含完整的 RTL 源码和规范说明（非仅自然语言描述），更贴近实际工程场景。
- 训练数据来自 **CodeV-R1** 数据集（87k repos），通过 ROUGE-L > 50% 过滤掉与测试集相似项，防止泄露。

---

### ⚙️ 实验设置与评估指标

#### 评估设置
| 设置 | 描述 |
|------|------|
| **Agentic Evaluation**（主设置） | 允许模型进行最多 3 轮迭代生成与仿真反馈 refinement，采用 memoryless 状态表示 |
| **Direct Inference**（参考设置） | 单次生成，无反馈 |

#### 主要评估指标
| 指标 | 含义 |
|------|------|
| **Cov Pass (%)** | 达到预设覆盖率阈值的仓库比例（Pass@1） |
| **Avg Cov (%)** | 所有仓库平均覆盖率（失败则记为 0） |
| **Sim Pass (%)** | 生成 testbench 能成功编译并通过仿真的比例（衡量语法正确性） |

#### 模型配置
- **学生模型**：`Qwen3-4B-Instruct-2507`（4B 参数）
- **教师模型**：`Qwen3-Coder-30B-A3B-Instruct`（30B 参数）
- 所有模型均在 instruct 模式下评估。

#### 基线对比
涵盖三类主流模型：
1. **通用大模型**：`Llama-4-Maverick (400B)`、`Qwen3-235B`
2. **编码专用模型**：`Qwen3-Coder-30B`、`Gemma-3`
3. **硬件特定模型**：`VeriCoder`、`CodeV-R1-RL`、`VeriReason`

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 2）

| 模型 | Cov Pass (Agentic) | Avg Cov (Agentic) | 模型大小 |
|------|--------------------|-------------------|---------|
| Qwen3-4B (Base) | 28.4% | 48.5% | 4B |
| **LLM4Cov (+Stage2)** | **69.2%** | **90.4%** | 4B |
| Qwen3-Coder-30B | 63.9% | 79.9% | 30B |
| Llama-4-Maverick | 60.2% | 81.7% | 400B |

> ✅ **结论**：一个仅 **4B 参数的小模型**，经过 LLM4Cov 框架训练后，**超越了 30B 的教师模型（+5.3%）**，并媲美 **50–100 倍更大规模的模型**。

---

### 🔁 与基线方法的对比结果
- 在 **direct inference** 下，LLM4Cov 也显著优于同类小模型（如 VeriCoder-Qwen2.5-14B: 28.2% vs. LLM4Cov Base: 28.4%）。
- 在 **agentic setting** 中优势更加明显，表明该框架特别适合多轮交互式验证任务。
- 图 2 和图 7 显示，LLM4Cov 在 **coverage pass rate** 和 **simulator pass rate** 上均处于帕累托前沿（Pareto Frontier）。

---

### 🔍 消融实验结果

#### （1）**Intermediate State Selection 策略对比**（Figure 5）
| 策略 | Cov Pass |
|------|---------|
| Best-State | ~62% |
| Uniform | ~63% |
| Median-State | ~64% |
| **Worst-State (ours)** | **~67.5%** |

✅ 最差状态优先策略显著胜出，证明聚焦失败案例能带来更强的学习信号。

#### （2）**Progressive Learning vs Naive Data Augmentation**（Figure 6）
- **Stage-conditioned progression**（分阶段训练）持续优于将所有数据合并训练（naive augmentation）。
- 即使使用相同的 Stage 1+2 数据，从 Stage 0 checkpoint 继续训练仍优于从头训练。
- 表明 **distribution alignment** 是关键因素。

#### （3）**Trajectory Synthesis 策略演化**（Figure 4）
- **Stage 1**：教师指导（imitation-style）明显优于纯学生自采样（self-sampling）
- **Stage 2**：随着学生能力增强，self-sampling 接近甚至反超 teacher-guided
- 支持渐进式学习设计的有效性。

#### （4）**Execution-Based Dataset Curation 消融**（Table 3）
| 方法 | Sim Pass (Direct) | Sim Pass (Agentic) |
|------|------------------|-------------------|
| Teacher Only | 53.3% | 85.1% |
| +Syntax Rules | 71.1% | 85.8% |
| +Execution Filtering | **83.9%** | **87.7%** |

✅ 执行验证的数据清洗大幅提升语法正确性和仿真通过率。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **小模型也能实现高性能硬件验证**：通过 execution-aware 的离线代理学习，**4B 模型可超越 30B 教师模型**，打破“唯规模论”。
2. **覆盖率是强大的监督信号**：结合 rejection sampling 和 worst-state selection，可将昂贵的仿真反馈转化为高效的训练数据。
3. **渐进式学习至关重要**：训练必须与学生模型的状态分布演进同步，否则会导致监督信号失真。
4. **memoryless state design 更优**：显式编码当前状态即可保留全部必要信息，无需冗长的历史记录。

---

### ⚠️ 方法的局限性
1. **依赖确定性仿真器输出**：假设 simulator 对同一输入返回一致反馈，不适用于随机性强的环境。
2. **当前仍为 offline 范式**：虽然支持 future RL 初始化，但本身不包含探索机制。
3. **领域特定规则依赖**：Stage 0 使用人工编写 syntax constraints 提升初始成功率，可能限制泛化性。
4. **仅关注 coverage maximization**：未处理 bug detection 或 assertion generation 任务。

---

### 🔮 未来工作方向
1. **集成 reasoning 与 planning 能力**：结合 CoT、Tree-of-Thought 等提升 long-horizon 决策能力。
2. **向 online RL 迁移**：利用 LLM4Cov 输出的多样化策略作为强初始化，开展低成本 online fine-tuning。
3. **扩展至其他 EDA 任务**：如 power optimization、timing closure、formal verification。
4. **构建开放 benchmark 与 simulator 接口标准**：推动 community-wide evaluation protocol 统一。

---

> 💬 **总结一句话**：  
> **LLM4Cov 证明了“正确的学习范式 > 单纯扩大模型规模”** —— 通过 execution-aware、progressive、failure-focused 的离线代理训练，即使是小型 LLM 也能在高成本硬件验证任务上达到顶尖性能。

</details>

---

### 5. [A Theoretical Framework for Modular Learning of Robust Generative Models](https://arxiv.org/abs/2602.17554)

**Authors**: Corinna Cortes, Mehryar Mohri, Yutao Zhong  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.17554v1  

#### Abstract
Training large-scale generative models is resource-intensive and relies heavily on heuristic dataset weighting. We address two fundamental questions: Can we train Large Language Models (LLMs) modularly-combining small, domain-specific experts to match monolithic performance-and can we do so robustly...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Theoretical Framework for Modular Learning of Robust Generative Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决大规模生成模型（如 **Large Language Models, LLMs**）训练中的两个根本挑战：

1. **可持续性与适应性问题**：传统单体（monolithic）模型训练成本高昂，难以重复训练以适应新数据；更新模型易导致灾难性遗忘（catastrophic forgetting）。
2. **鲁棒性问题**：标准训练依赖启发式的数据混合权重（heuristic dataset weighting），在测试分布与训练假设不一致时表现不佳。

论文提出：能否通过模块化方式组合多个预训练专家模型（pre-trained experts），构建一个对任意数据混合都鲁棒的系统，从而避免昂贵的重新训练和启发式调参？

### 提出的新方法与新思路

作者提出了一个**基于博弈论的模块化生成建模范式**，其核心是：

- **Robust Gated Model**：引入一个可学习的**门控机制**（gate）$ g(x,k) $，动态地将一组冻结的、领域特定的专家模型 $ \Pi_k $ 组合为最终输出：
  $$
  T_g(x) = \sum_k g(x,k) \Pi_k(x)
  $$
  其中 $ g(x,\cdot) \in \Delta $ 是输入相关的概率分布。

- **最小最大优化框架**（minimax game）：目标是找到一个**鲁棒的门控函数 $ g^* $**，使其在最坏情况下的数据混合 $ \lambda $ 下仍能保持低 KL 散度：
  $$
  \min_{g \in \mathcal{G}_1} \max_{\lambda \in \Delta} D_{\text{KL}}(p_\lambda \| T_g)
  $$
  这是一种**分布鲁棒优化**（Distributionally Robust Optimization, DRO）思想在生成模型中的应用。

- **理论保证**：利用 Kakutani 不动点定理证明了此类鲁棒门控函数的存在性，并推导出其性能上界。

### 相比现有方法的优势

| 对比维度 | 现有方法（如 MoE, Model Soups） | 本文方法 |
|---------|-------------------------------|--------|
| **训练模式** | 联合训练路由与专家 / 静态平均权重 | 冻结专家，仅训练轻量级门控 |
| **鲁棒性目标** | 平均性能 / 特定任务优化 | 最坏情况下的鲁棒性（worst-case robustness） |
| **理论基础** | 启发式设计，缺乏严格理论 | 清晰的 minimax 博弈框架，存在性与收敛性证明 |
| **泛化能力** | 依赖完整模型重训练 | 仅需更新门控即可适应新分布 |
| **推理效率** | MoE 有负载均衡开销 | 提出 **Structural Distillation** 实现高效因果推理 |

---

## 2. 核心实验方法和设置

### 数据集

实验分为两部分：

#### （1）合成数据集（Synthetic Benchmarks）
- 构造两个具有**冲突规则**的序列生成任务：
  - **Domain A**: $ x_{t+1} = (x_t + 1) \mod 100 $
  - **Domain B**: $ x_{t+1} = (x_t - 1) \mod 100 $
- 测试时通过调整混合比例 $ \lambda \in [0,1] $ 模拟分布偏移。

#### （2）真实世界数据集（Real-World Datasets）
使用三个差异显著的 HuggingFace 数据集：
- `wikimedia/wikipedia`：高质量事实性文本
- `bigcode/the-stack-smol`：多语言源代码
- `fineweb-edu`：过滤后的教育网页内容

### 实验设置与评估指标

- **模型架构**：所有模型均为 Transformer，专家为 2 层，门控为小型 Transformer Encoder。
- **评估指标**：
  - 主要指标：**Negative Log-Likelihood (NLL)** per token
  - 对比不同测试混合比例 $ \lambda $ 下的性能
  - 报告 5 次运行的均值与标准差

### 基线方法对比

| 基线方法 | 描述 |
|--------|------|
| **Fixed Retrained Model** | 在聚合数据上从头训练的单体模型（区分 smaller 和 larger 参数量版本） |
| **Oracle Retrained Model** | “作弊”基线，在每个测试混合比例 $ \lambda $ 上单独训练的模型（理想情况） |
| **Monolithic Distillation** | 将门控模型蒸馏为单一因果学生模型（丢弃模块性） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）合成数据结果（Fig. 5–7）
- **Robust Gate** 在整个 $ \lambda \in [0,1] $ 区间内保持**稳定且较低的损失**。
- 当 $ \lambda \approx 0.5 $（高熵混合）时：
  - 固定重训练模型（Fixed Retrained）因**梯度冲突**（gradient conflict）性能严重下降。
  - 即使是“作弊”的 **Oracle Larger Model** 也因无法同时掌握矛盾规则而出现性能谷底。
  - **Robust Gate 显著优于所有基线**，验证了 Theorem 6 中的 **JSD Gap** 理论。

> ✅ **关键发现**：模块化结构天然解耦了任务间的几何干扰（interference），在高多样性混合下优势明显。

#### （2）真实世界数据结果（Table 1 & 2）

| Model | NLL per token (Diff seed) | NLL per token (Diff data) |
|-------|---------------------------|---------------------------|
| Retrained Model | 5.133 ± 0.010 | 5.306 ± 0.257 |
| **Gate Model** | **4.994 ± 0.013** | **5.087 ± 0.141** |

- **Gate Model 在所有设置下均优于同规模的 Retrained Model**。
- 在不同测试混合比例下（Table 2），Gate Model 表现出更强的**分布外鲁棒性**（distribution shift robustness）。

#### （3）消融实验与扩展

- **Structural Distillation**（图 8）：
  - 将非因果门控蒸馏为**因果路由器**（causal router）后，性能损失极小。
  - 推理速度恢复为标准自回归水平，且**保留了模块性**（可独立升级专家）。
- **算法稳定性**：
  - Primal-Dual 算法稳定收敛，无典型对抗训练的振荡现象。
  - 对手混合权重 $ \lambda_t $ 快速收敛至 $ [0.5, 0.5] $，表明门控成功平衡了各领域性能。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **模块化可以实现鲁棒生成建模**：
   - 存在一个鲁棒门控 $ g^* $，能在任意未知数据混合下提供统一性能保证。
   - 该结论由 Kakutani 定理严格证明。

2. ✅ **模块化是“安全”的归纳偏置**（safe inductive bias）：
   - 在凸模型族中，门控组合等价于在混合数据上重训练（Theorem 7）。
   - 在非凸、冲突任务中，模块化能**避免几何干扰**，性能超越单体模型。

3. ✅ **多样性是模块化的优势而非负担**：
   - 单体模型中，Jensen-Shannon Divergence (JSD) 是**干扰项**（interference floor）。
   - 模块化模型中，JSD 成为**分离增益**（separability gain），帮助抵消容量代价。

4. ✅ **轻量门控带来高效泛化**：
   - 泛化误差由门控复杂度决定，而非专家参数量（Theorem 8）。
   - 门控通常为轻量网络（如浅层 Transformer），样本效率远高于重训练。

### 方法的局限性

- **推理瓶颈**：最优门控 $ g^* $ 是非因果的，直接采样需 SIR 或 Rejection Sampling，计算开销大（$ O(Np) $）。
- **依赖专家质量**：若专家本身性能差或覆盖不足，门控无法弥补。
- **当前框架侧重统计性能**：未考虑路由延迟、通信成本等工程因素。

### 未来工作方向

1. **扩展到更多专家**：研究 $ p \gg 100 $ 时的可扩展路由机制。
2. **在线学习与动态添加专家**：支持在不重新训练门控的情况下加入新专家。
3. **结合 MoE 与本文框架**：探索可训练专家 + 鲁棒门控的混合范式。
4. **应用于多模态与跨域任务**：如图文、语音-文本联合建模。
5. **经济与生态视角**：构建基于模块市场的 AI 生态系统，本文提供了统计层面的均衡保障。

---

> **总结一句话**：  
> 本文建立了首个**理论严谨的模块化生成模型框架**，证明了通过一个轻量级、鲁棒的门控机制，可以将多个冻结专家组合成一个对任意数据混合都稳健的系统，不仅在理论上揭示了“多样性即优势”的新机制，也在合成与真实数据上验证了其优越性，为绿色、可持续、可扩展的 LLM 发展提供了新路径。

</details>

---

### 6. [WarpRec: Unifying Academic Rigor and Industrial Scale for Responsible, Reproducible, and Efficient Recommendation](https://arxiv.org/abs/2602.17442)

**Authors**: Marco Avolio, Potito Aghilar, Sabino Roccotelli, Vito Walter Anelli, Chiara Mallamaci, Vincenzo Paparella, Marco Valentini, Alejandro Bellog\'in, Michelantonio Trizio, Joseph Trotta, Antonio Ferrara, Tommaso Di Noia  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.17442v1  

#### Abstract
Innovation in Recommender Systems is currently impeded by a fractured ecosystem, where researchers must choose between the ease of in-memory experimentation and the costly, complex rewriting required for distributed industrial engines. To bridge this gap, we present WarpRec, a high-performance frame...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：WarpRec: Unifying Academic Rigor and Industrial Scale for Responsible, Reproducible, and Efficient Recommendation

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
推荐系统（**Recommender Systems, RS**）领域长期存在**学术界与工业界之间的割裂**：
- **学术框架**（如 RecBole、Elliot）：灵活、支持快速原型开发，但通常基于单机内存计算，难以扩展到大规模工业场景。
- **工业框架**（如 NVIDIA Merlin、MS Recommenders）：支持分布式训练和高吞吐，但缺乏科学严谨性（如统计检验、可复现性）、灵活性差。

此外，当前框架在以下方面存在缺失：
- **生态责任**（Green AI）：缺乏对能耗和碳排放的量化工具。
- **智能体兼容性**（Agentic AI）：无法作为 LLM 驱动的智能体中的可调用工具。

### 提出了什么新方法或新思路
作者提出 **WARPREC**，一个高性能、模块化、后端无关的推荐系统框架，旨在统一学术严谨性与工业级规模。

其核心创新点包括：

#### ✅ 后端无关架构（Backend-Agnostic Architecture）
- 基于 **Narwhals** 构建统一的数据抽象层，实现“一次编写，随处运行”（write-once, run-anywhere）。
- 支持从本地调试无缝切换到基于 **Ray** 的分布式集群训练。

#### ✅ 模块化解耦设计
框架由五个独立模块组成：
1. **Reader**：数据读取（支持本地/云存储）
2. **Data Engine**：数据预处理（过滤、分割、对齐）
3. **Recommendation Engine**：模型训练与超参优化（HPO）
4. **Evaluation**：多维度评估 + 统计显著性检验
5. **Writer**：结果持久化（支持本地/云端）

#### ✅ 生态责任集成（Green AI）
- 全球首个原生集成 **CodeCarbon** 的推荐框架，实时追踪训练过程中的能源消耗与碳排放。

#### ✅ 智能体就绪接口（Agentic Readiness）
- 原生支持 **Model Context Protocol (MCP)** 接口，使推荐器可被 LLM 智能体直接调用，成为生成式 AI 生态中的一部分。

#### ✅ 科学严谨性保障
- 自动执行 **统计假设检验**（如 t-test, Wilcoxon）和多重比较校正（如 Bonferroni, FDR），防止 p-hacking。
- 支持 **多目标优化指标**（Multi-objective metrics），平衡准确性与公平性、多样性等非准确性目标。

---

### 相比现有方法的优势

| 维度 | WARPREC 优势 |
|------|-------------|
| **可扩展性** | 支持从单机到多GPU/多节点 Ray 集群的弹性伸缩 |
| **可复现性** | 全流程确定性控制（全局随机种子）、完整实验日志记录 |
| **功能完整性** | 内置 55 个算法、40 个指标、19 种划分策略 |
| **绿色AI** | 唯一提供实时碳足迹监控的框架 |
| **智能体交互** | 唯一原生支持 MCP 协议的推荐框架 |
| **灵活性** | 支持模块解耦使用，可嵌入外部流水线 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验采用三个不同规模的标准推荐数据集，覆盖从小型学术到大型工业场景：
| 数据集 | 用户数 | 物品数 | 交互数 | 稀疏度 |
|--------|--------|--------|--------|--------|
| **MovieLens-1M** | 6,040 | 3,883 | ~1M | 95.7% |
| **MovieLens-32M** | 200,948 | 87,585 | ~32M | 99.8% |
| **NetflixPrize-100M** | 480,189 | 17,770 | ~100M | 98.8% |

### 实验设置和评估指标

#### 模型选择
测试五类代表性算法：
- **EASE**（浅层自动编码器）
- **ItemKNN**（协同过滤基准）
- **NeuMF**（神经矩阵分解）
- **LightGCN**（图神经网络）
- **SASRec**（序列推荐模型）

#### 划分策略
- 对大多数模型使用 **90-10 随机 Holdout**
- 对 SASRec 使用 **时间顺序 Holdout**

#### 超参优化（HPO）
- 使用网格搜索（Grid Search）探索每模型 6 种配置
- 最佳模型通过验证集上的 **nDCG@10** 选择
- 批大小为 8,192，最大训练轮次为 10（MovieLens）或 2（Netflix）
- 单次试验超时 24 小时

#### 硬件资源
- CPU：16核
- GPU：NVIDIA A100（64GB显存）
- 支持串行（Serial）与并行（Parallel via Ray）执行模式

#### 评估指标
- **效率指标**：
  - 预处理时间（Preprocessing）
  - 训练时间（Training）
  - 评估时间（Evaluation）
  - HPO 总耗时（Wall-clock time）
- **性能指标**：
  - **nDCG@10**（主评价指标）
- **可持续性指标**：
  - 能源消耗（Energy Consumed, kWh）
  - 碳排放量（CO₂eq, kg）
  - CPU/GPU 功率与能耗

#### 可复现性保障
- 固定随机种子
- 使用 **Weights & Biases** 追踪实验
- 所有代码与配置开源：[GitHub链接](https://github.com/sisinflab/warprec/)

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 4）

#### 在 **NetflixPrize-100M** 上的表现（最具挑战性）：
| 框架 | 是否完成 HPO？ | 总耗时 | nDCG@10 |
|------|----------------|--------|---------|
| Cornac / Elliot / RecBole / DaisyRec | ❌ 失败（OOM 或超时） | — | — |
| MS Recommenders | ❌ 失败（OOM-VRAM） | — | — |
| **WARPREC (Parallel)** | ✅ 成功 | **28m 22s** | 0.1323 |
| **WARPREC (Serial)** | ✅ 成功 | 1h10m | 0.1323 |

> ⚠️ 注意：除 WARPREC 和 RecBole 外，所有框架均未能完成 LightGCN 在该数据集上的训练。

#### 在 **MovieLens-32M** 上的表现：
| 框架 | 总耗时 | nDCG@10 |
|------|--------|---------|
| RecBole | 7h10m | 0.3609 |
| **WARPREC (Parallel)** | **1h34m** | 0.1456 |
| **WARPREC (Serial)** | 6h7m | 0.3399 |

✅ WARPREC 在并行模式下速度显著优于其他框架，且唯一能在所有数据集上稳定完成全流程。

#### 在 **MovieLens-1M** 上的小规模测试：
所有框架均可完成任务，WARPREC 表现出最快的端到端效率（最快仅需 **1m42s** 完成 HPO）。

---

### 与基线方法的对比结果

| 对比维度 | WARPREC 表现 |
|----------|------------|
| **可扩展性** | 唯一能在 Netflix-100M 上成功运行 LightGCN 的框架之一 |
| **内存效率** | 有效避免 OOM 错误，支持更大批量和更复杂模型 |
| **训练速度** | 并行模式下比 RecBole 快 **4–10 倍** |
| **HPO 效率** | 支持 ASHA 调度器进行早期剪枝，大幅缩短搜索时间 |
| **GPU 利用率** | 全面 GPU 加速指标计算，降低评估延迟 |

---

### 消融实验结果（隐含分析）

虽然未明确列出消融实验表格，但文中通过多个维度进行了功能有效性验证：

#### ▶ Green AI 分析（Table 5）
在 Netflix-100M 上训练不同模型的能耗对比：
| 模型 | 能源消耗 (kWh) | 碳排放 (kg CO₂eq) |
|------|----------------|--------------------|
| EASE | 0.115 | 0.0005 |
| ItemKNN | 0.0476 | 0.0002 |
| LightGCN | **2.4192** | **0.0095** |
| SASRec | 0.3159 | 0.0012 |

🔍 发现：
- **LightGCN 能耗最高**，因其收敛慢，尽管瞬时功耗不高。
- **EASE 和 ItemKNN 更节能**，适合绿色部署。
- 验证了“**性能提升 ≠ 更高碳成本**”，可通过模型选择优化可持续性。

#### ▶ Agentic AI 实验（Figure 2）
展示了一个 LLM 智能体通过 MCP 接口调用 WARPREC 的 SASRec 模型进行电影推荐：
- 输入历史：`['Pulp Fiction', 'Forrest Gump', 'Full Metal Jacket']`
- 输出推荐：`['Star Wars IV', 'The Godfather', 'Schindler's List']`
- 智能体结合语义理解生成自然语言解释，增强用户体验。

✅ 证明 WARPREC 可作为 **LLM Agent 的可信工具**，推动推荐系统向交互式智能体演进。

---

## 4. 关键结论和发现

### 论文的主要发现

1. **统一架构是可行的**：WARPREC 成功弥合了学术与工业推荐系统的鸿沟，实现了“一次开发，多环境部署”。
2. **可扩展性与科学严谨性可以兼得**：既支持分布式训练，又内置统计检验、可复现性机制。
3. **绿色AI必须被量化**：首次将碳足迹纳入推荐系统评估体系，揭示了模型选择对环境的重大影响。
4. **推荐系统正在进化为智能体工具**：通过 MCP 接口，推荐器不再是被动预测器，而是主动参与推理循环的“认知伙伴”。

---

### 方法的局限性

| 局限性 | 说明 |
|--------|------|
| **学习曲线较高** | 模块化设计虽灵活，但对新手用户可能不够友好 |
| **文档和社区尚在建设中** | 相比成熟框架（如 RecBole），生态仍在早期阶段 |
| **部分前沿模型尚未完全覆盖** | 尽管已有 55 个算法，但某些最新架构（如 Diffusion-based RS）暂未集成 |
| **MCP 接口依赖外部 LLM 生态** | 其价值高度依赖于 Agentic AI 的普及程度 |

---

### 未来工作方向

1. **扩展更多绿色AI策略**：
   - 引入动态电压频率调节（DVFS）以进一步节能。
   - 开发基于能耗感知的 HPO 策略。

2. **增强智能体协作能力**：
   - 支持多轮对话式推荐（Conversational Recommendation）。
   - 实现反事实推理与偏好修正机制。

3. **构建推荐即服务（RaaS）平台**：
   - 基于 WARPREC 构建云原生推荐服务平台，支持一键部署与监控。

4. **推动标准化进程**：
   - 倡导将 CodeCarbon 和 MCP 作为推荐系统框架的标准组件。

---

## 总结

> **WARPREC 不只是一个推荐框架，更是下一代负责任、可持续、智能体就绪的推荐系统基础设施。**

它不仅解决了“学术-工业脱节”的老问题，还前瞻性地回应了 **Green AI** 和 **Agentic AI** 的新趋势，为推荐系统研究与应用提供了全新的范式。

</details>

---

### 7. [Pareto Optimal Benchmarking of AI Models on ARM Cortex Processors for Sustainable Embedded Systems](https://arxiv.org/abs/2602.17508)

**Authors**: Pranay Jain, Maximilian Kasper, G\"oran K\"ober, Axel Plinge, Dominik Seu{\ss}  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.17508v1  

#### Abstract
This work presents a practical benchmarking framework for optimizing artificial intelligence (AI) models on ARM Cortex processors (M0+, M4, M7), focusing on energy efficiency, accuracy, and resource utilization in embedded systems. Through the design of an automated test bench, we provide a systemat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Pareto Optimal Benchmarking of AI Models on ARM Cortex Processors for Sustainable Embedded Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本研究针对**边缘AI系统在资源受限嵌入式平台上的可持续部署难题**，特别是如何在有限的内存、功耗和计算能力下，实现AI模型在能效、准确率和推理延迟之间的最优权衡。现有研究多集中于SBCs（如Raspberry Pi）等高层平台，其操作系统和中间件引入抽象层，掩盖了硬件真实性能；而对bare-metal处理器（无操作系统）的系统性基准测试仍存在空白。

### ✅ 提出的新方法与新思路
- **构建了一个自动化测试平台（test bench）**，用于在ARM Cortex-M系列处理器（M0+, M4, M7）上进行裸机（bare-metal）AI模型性能评估。
- 引入**基于Pareto前沿分析的多目标优化框架**，综合考虑accuracy、energy consumption、latency和resource utilization，识别最优的处理器-模型组合。
- 发现并验证了**FLOPs与inference time之间近似线性关系**，提出可将其作为早期设计阶段的预测指标，支持快速模型筛选与协同设计（co-design）。
- 将**inference cycle energy**（包含active inference + idle time）作为核心KPI，强调应用时序行为（duty cycle）对整体能效的关键影响。

### ✅ 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **评估层级** | 聚焦bare-metal环境，避免OS/middleware干扰，提供更真实的硬件性能视图 |
| **评估维度** | 多维KPI联合分析（accuracy, energy, latency, memory），而非单一指标 |
| **决策支持** | 提供Pareto front可视化工具，指导开发者根据use case特性选择最佳配置 |
| **预测能力** | 利用FLOPs预测inference time，减少实际部署前的试错成本 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与Use Cases
论文选取四个典型嵌入式AI应用场景，涵盖不同任务类型：

| Use Case | Model | Dataset | Quality Target |
|--------|-------|---------|----------------|
| 图像分类（Image Classification） | ResNet | CIFAR-10 | ≥80% accuracy |
| 光学字符识别（Optical Digit Recognition） | LeNet-5 | MNIST | ≥95% accuracy |
| 异常检测（Anomaly Detection） | Autoencoder | ToyADMOS | AUC >85% |
| 视觉唤醒词（Visual Wake Words） | MobileNetV1 | MS-COCO | ≥80% accuracy |

所有模型均经过structured pruning和8-bit static quantization处理，并转换为ONNX格式后编译至目标平台。

### ⚙️ 实验设置
- **硬件平台**：
  - ARM Cortex-M0+ (RP2040)
  - ARM Cortex-M4 (nRF52840)
  - ARM Cortex-M7 (i.MX RT1062)
- **测试平台组件**：
  - ATP carrier board（通用接口板）
  - Segger J-Link debugger（用于M0+/M4编程）
  - USB-C烧录（用于M7）
  - Power Profiler Kit (PPK) 进行实时电流监测
  - GPIO同步信号标记inference起止时间
- **供电电压**：恒定3.3V

### 🎯 评估指标（KPIs）
| 类别 | 指标 |
|------|------|
| **性能** | Inference time (latency), FLOPs |
| **能效** | Active power, Idle current, Total inference cycle energy (mJ) |
| **资源占用** | ROM size (model storage), RAM usage |
| **准确性** | Accuracy / AUC |
| **综合决策** | Pareto front (Energy vs. Accuracy) |

### 🔁 基线对比
虽然未直接对比其他算法类“基线”，但通过跨处理器架构（M0+, M4, M7）横向比较，揭示了不同硬件在相同模型下的表现差异，本质上构成了硬件层面的baseline comparison。

---

## 3. 主要实验结果和性能指标

### 🔢 关键性能数据

#### （1）模型压缩效果（图4）
- **量化（quantization）使模型大小平均缩小至原始的约1/4**
- 最小模型（Optical Digit Recognition）仅需~10KB ROM
- 最大模型（Visual Wake Words）未量化时超过1MB，部分超出设备内存限制

#### （2）FLOPs与推理时间的关系（图5）
- 在三种处理器上均观察到**强线性相关性**（R² ≥ 0.93）
- 斜率反映单位FLOP所需时间：
  - M0+: ~3.62 ms/MFLOP（最慢）
  - M4: ~1.79 ms/MFLOP
  - M7: ~0.0742 ms/MFLOP（最快，得益于DSP增强架构）

> ✅ 表明FLOPs可作为inference time的有效预测器，支持早期设计决策。

#### （3）空闲电流对比（关键能效因素）
| Processor | Deep Sleep Current |
|----------|--------------------|
| M0+      | 4.20 mA            |
| M4       | **0.30 mA** ✅     |
| M7       | 1.60 mA            |

👉 M4在长时间idle场景中具有显著优势。

#### （4）推理周期能量 vs 周期时间（图7）
- **短周期（<1s）**：M7能耗最低 → 快速完成任务，减少active功耗
- **长周期（>2.5s）**：M4能耗最低 → 极低idle电流主导总能耗
- M0+在所有场景下表现最差

#### （5）Energy-Accuracy Pareto Front分析（图8）
- **Cycle = 0.5s**：M7 Pareto front占优 → 应优先选择高速、轻量模型
- **Cycle = 2.5s / 5.0s**：M4 Pareto front占优 → 可选用更高精度模型而不显著增加能耗

> ✅ 验证了“**在idle-dominated场景中，应牺牲速度换取accuracy**”的设计原则。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **没有“绝对最优”的处理器或模型**：最佳选择取决于**应用的inference cycle duration**（即事件频率）。
   - 高频任务（≤0.5s）→ 推荐 **Cortex-M7**，追求最小化active time
   - 低频任务（≥2.5s）→ 推荐 **Cortex-M4**，利用其超低idle电流
   - M0+不适合复杂AI负载，仅适用于简单控制或非AI场景

2. **FLOPs是inference time的良好预测指标**（R² > 0.93），可用于早期模型筛选与协同设计。

3. **RAM/ROM不是能效的可靠预测因子**，更多是部署可行性约束。

4. **Pareto分析提供了直观的决策地图**，帮助开发者在energy与accuracy间做出合理取舍。

5. **模型优化策略应随应用场景动态调整**：
   - 短周期：优化latency为主
   - 长周期：可放宽latency要求，优先提升accuracy

---

### ⚠️ 方法的局限性
1. **实验室环境测量**：未考虑温度变化、外设使用、长期老化等因素对功耗的影响。
2. **处理器种类有限**：仅覆盖M0+/M4/M7，结论可能不适用于更新架构（如Cortex-M33/M55）或带NPU的MCU。
3. **FLOPs线性假设的边界条件未知**：对于有复杂访存模式或硬件加速的模型，该关系可能失效。
4. **缺乏real-world workload drift测试**：静态数据集无法模拟真实世界中的分布偏移。

---

### 🔮 未来工作方向
1. **标准化idle current与wake-up latency评测方法**，便于跨平台公平比较。
2. **开展长期实地部署实验**，验证在环境扰动、负载漂移和硬件退化下的鲁棒性。
3. **扩展框架至更多架构与加速器系统**（如Ethos-U, GAP9, Arduino Nano 33 BLE等）。
4. **集成multi-objective Bayesian optimization**，实现自动化的模型-硬件联合搜索。
5. **探索动态电压频率调节（DVFS）对能效的影响**，进一步优化energy-delay product。

---

## 总结
该论文提出了一个面向可持续嵌入式系统的AI模型基准测试框架，首次系统地揭示了**应用时序行为（inference cycle time）是决定处理器选型与模型优化策略的核心变量**。通过实证分析和Pareto前沿建模，为开发者提供了从理论到实践的完整决策路径，在推动绿色Edge AI发展方面具有重要指导意义。

</details>

---

### 8. [Entropy-Based Data Selection for Language Models](https://arxiv.org/abs/2602.17465)

**Authors**: Hongming Li, Yang Liu, Chao Huang  
**Category**: cs.CL  
**Published**: 2026-02-20  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.17465v1  

#### Abstract
Modern language models (LMs) increasingly require two critical resources: computational resources and data resources. Data selection techniques can effectively reduce the amount of training data required for fine-tuning LMs. However, their effectiveness is closely related to computational resources,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Entropy-Based Data Selection for Language Models》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代 **Language Models (LMs)** 在微调（fine-tuning）过程中面临两大资源瓶颈：**计算资源** 和 **高质量训练数据**。尽管已有多种 **data selection** 技术用于减少训练数据量，但这些方法通常依赖高算力预算（如梯度计算、代理模型等），难以在 **compute-constrained 场景** 下高效应用。此外，合成数据（synthetic data）虽可缓解数据稀缺问题，但其质量不均，仍需有效筛选。

本文系统揭示了 **data selection 与不确定性估计（uncertainty estimation）之间的关系**，并提出一种适用于原始数据和合成数据的轻量级选择框架。

---

### 提出的新方法：EUDS 框架
作者提出了 **Entropy-Based Unsupervised Data Selection (EUDS)** 框架，其核心是基于三种熵机制进行无监督数据筛选：

- **Information Entropy (IE)**：基于 n-gram 的词法多样性衡量。
- **Generative Entropy (GE)**：基于语言模型生成时的困惑度（perplexity）衡量预测不确定性。
- **Semantic Entropy (SE)**：通过语义聚类衡量生成结果的语义歧义性。

该框架作为 **独立预处理步骤**，在微调前对数据进行过滤，具有以下特点：
- **模型无关（model-agnostic）**：适用于 BERT、GPT 等各类 LM。
- **无需额外训练或梯度计算**：极大降低计算开销。
- **支持原始数据与合成数据联合筛选**。

---

### 相比现有方法的优势
| 维度 | EUDS 优势 |
|------|-----------|
| **计算效率** | 仅需前向推理或统计计算，复杂度远低于 influence-based 或 gradient-based 方法（如 Grad-Match、Influence Functions）。 |
| **适用性广** | 同时适用于原始数据和 LLM 生成的合成数据，支持混合策略。 |
| **性能保持甚至提升** | 在显著减少数据量的同时，多数任务上性能持平或优于全量数据训练。 |
| **可扩展性强** | 可通过子集确定最优熵区间后推广至全集，适用于大规模数据场景。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖三大典型文本分类任务，共 **11 个公开数据集**：

| 任务类别 | 数据集 |
|--------|-------|
| **Sentiment Analysis (SA)** | SST-2, SST-5, IMDb, Yelp |
| **Topic Classification (Topic-CLS)** | AGNews, 20News, Yahoo |
| **Question Answering (Q&A)** | RaceQA, MMLU, ARC, MMLU-Pro |

所有数据集均来自 Hugging Face 或标准学术发布源。

---

### 实验设置与评估指标

#### 数据准备
- **原始数据 (OriData)**：从各数据集中提取 2,000 或 10,000 条样本。
- **合成数据 (SynData)**：使用 **GPT-4o** 基于少量示例生成，确保标签一致性和任务对齐。
- 划分比例：训练:验证:测试 = 8:1:1。

#### 微调模型
- 主干模型：**BERT-base-uncased**
- 超参数：
  - Epochs: 10
  - Batch Size: 64
  - Learning Rate: 4e-5
  - Warm-up Ratio: 0.1
  - Weight Decay: 0.1

#### 评估指标
- **Accuracy**
- **F1 Score**
- **数据缩减率（Data Reduction %）**

#### 基线方法对比
- **Baseline**：使用全部原始数据或合成数据训练。
- **SOTA 对比方法**：包括 BiLSTM、RoBERTa-BiLSTM、TWSSenti、SELECT、FDKT 等（见 Table 11）。
- **消融实验**：比较单熵（IE/GE/SE）、组合熵（IGE/ISE/GSE/IGSE）及不同数据组合策略（SumData / JoSelData）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（代表性结果）

| 方法 | 任务 | 数据缩减率 | Accuracy 提升 | 备注 |
|------|------|------------|----------------|------|
| **IE** | MMLU-Pro | **84.05%** | +4.00% | OriData 上最佳 |
| **SE** | RaceQA | **71.25%** | +0.40% | 性能提升且大幅减量 |
| **GE** | MMLU | 40.00% | +7.20% | F1 提升显著 |
| **IE** | AGNews | 0.70% | +2.40% | 小幅减量但性能跃升 |

> ✅ 在 **198 个最优区间** 中，**65.66%** 的场景达到或超过 baseline 性能；**15.15%** 的场景误差在 1.5% 以内。

---

### 与基线方法对比（Table 11）
| 方法 | IMDb Acc (%) | AGNews Acc (%) |
|------|---------------|-----------------|
| BiLSTM | 81.87 | — |
| RoBERTa-BiLSTM | 92.46 | — |
| TWSSenti | 95.00 | — |
| SELECT | 63.79 | 80.05 |
| FDKT | — | 75.60 |
| **EUDS (IE)** | **93.60** | **93.60** |
| **EUDS (GE)** | **93.20** | **91.60** |
| **EUDS (SE)** | **93.60** | **91.20** |

👉 **EUDS 在绝大多数情况下优于或媲美 SOTA 方法**，尤其在 AGNews 上远超 SELECT 和 FDKT。

---

### 消融实验结果

#### （1）单一熵 vs. 组合熵（Table 9）
- **组合熵（如 IGSE）进一步提升效率**：
  - 在 MMLU 上，IGS Entropy 实现 **61.25% 数据缩减**，同时 Accuracy 提升 **4.80%**。
  - 表明多维度熵协同评估能更精准识别高价值样本。

#### （2）数据组合策略对比
| 策略 | 描述 | 效果 |
|------|------|------|
| **SumData** | 选中的 SynData + 全量 OriData | 显著减量，性能维持或提升 |
| **JoSelData** | 选中的 SynData + 选中的 OriData | 进一步压缩总量，部分任务仍优于 baseline |

> 示例：在 MMLU-Pro 上，JoSelData + IGSE 实现 **63% 数据缩减**，Accuracy 提升 **6.00%**。

#### （3）大规模数据验证（Table 8）
- 在 **10,000 条数据规模**下验证 EUDS 有效性：
  - SST-5 上 IE 选择实现 **25% 数据缩减**，Accuracy 提升 **1.28%**。
  - 表明 EUDS 具备良好的 **scalability**。

#### （4）子集推广泛化能力（Table 10）
- 在小规模子集上确定的最优熵区间，应用于完整数据集后仍能取得相近性能增益。
- 支持“先用子集定区间，再推广到全集”的高效策略，**大幅节省调参成本**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **熵值可有效表征数据价值**：适中或特定区间的熵（非极低或极高）往往对应最具信息量的样本。
2. ✅ **EUDS 显著降低数据需求**：平均可减少 **40–85%** 的训练数据，而性能不降反升。
3. ✅ **打破“数据量-性能”线性依赖**：少量高质量数据即可超越全量普通数据的表现。
4. ✅ **适用于合成数据筛选**：能有效识别 GPT-4o 生成中的高质量样本，解决合成数据质量不均问题。
5. ✅ **计算高效且可扩展**：无需修改模型结构或引入额外训练，适合资源受限场景。

---

### 方法的局限性
1. ❗ **无统一最优熵区间**：不同任务、不同数据类型下的最佳熵范围差异大，需任务级调优。
2. ❗ **熵分布集中现象**：某些 SynData 在特定熵维度上高度集中（如 SE 高熵占比 >99%），限制筛选空间。
3. ❗ **极端压缩可能导致不稳定**：过度追求数据缩减可能影响模型收敛稳定性。
4. ❗ **理论解释尚不充分**：虽然实证效果好，但对“为何特定熵区间最优”缺乏深层理论支撑。

---

### 未来工作方向
1. 🔄 探索 **自适应熵区间选择机制**，减少人工调参依赖。
2. 🔍 研究 **跨任务迁移的通用熵模式**，构建更普适的选择准则。
3. 🧠 加强 **理论建模**，建立熵、信息密度与泛化误差之间的数学联系。
4. 🌐 扩展至其他模态（如语音、图像）和任务（如 generation、summarization）。
5. ⚙️ 结合 LLM 自我反思（self-reflection）能力，实现动态反馈式数据筛选。

---

> **总结一句话**：  
> EUDS 提供了一种 **简单、高效、通用** 的数据选择范式，在显著降低 LM 微调资源消耗的同时，实现了 **性能持平甚至反超**，为 compute-constrained 场景下的高效 NLP 模型训练提供了创新解决方案。

</details>

---

### 9. [Small LLMs for Medical NLP: a Systematic Analysis of Few-Shot, Constraint Decoding, Fine-Tuning and Continual Pre-Training in Italian](https://arxiv.org/abs/2602.17475)

**Authors**: Pietro Ferrazzi, Mattia Franzin, Alberto Lavelli, Bernardo Magnini  
**Category**: cs.CL  
**Published**: 2026-02-20  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.17475v1  

#### Abstract
Large Language Models (LLMs) consistently excel in diverse medical Natural Language Processing (NLP) tasks, yet their substantial computational requirements often limit deployment in real-world healthcare settings. In this work, we investigate whether "small" LLMs (around one billion parameters) can...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Small LLMs for Medical NLP: a Systematic Analysis of Few-Shot, Constraint Decoding, Fine-Tuning and Continual Pre-Training in Italian*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文探讨了一个关键现实问题：尽管 Large Language Models (LLMs) 在医疗 NLP 任务中表现出色，但其巨大的计算资源需求限制了在真实医疗环境（如医院、诊所）中的部署。因此，研究者提出并系统评估了“小规模语言模型”（Small LLMs, SLLMs）是否能在保持高准确率的同时胜任意大利语医学 NLP 任务。

### 提出了什么新方法或新思路
- **系统性比较多种适应策略**：首次对 SLLMs 在意大利语医疗场景下的四种主流适应方法进行了全面对比：
  - **Few-shot Prompting**
  - **Constraint Decoding (CD)**
  - **Supervised Fine-Tuning (FT)**
  - **Continual Pre-Training (CPT) + Fine-Tuning**
- **构建并开源大规模意大利语医疗语料库**：
  - 发布了首个**综合性的公开意大利语医疗 NLP 数据集集合**。
  - 构建了一个包含 **300M 单词**的新数据集，其中：
    - **126M 来自急诊科电子健康记录（EHR）**
    - **175M 来自科学文献和其他来源**
  - 这些数据用于 CPT，显著增强了模型的领域知识。
- **提出高效训练配置**：结合 **LoRA**、**Flash Attention 2** 和 **sequence packing** 技术，实现高效的 CPT 与 FT。

### 相比现有方法的优势
- **成本效益更高**：证明经过适当调优的小模型（~1B 参数）可以**匹配甚至超越高达 30 倍参数的大模型**（如 Qwen3-32B），为资源受限机构提供了可行方案。
- **方法论更完整**：不仅比较推理时方法（few-shot, CD），也深入分析训练阶段的方法（FT, CPT），揭示不同策略的有效边界。
- **推动意大利语医疗 NLP 发展**：填补了低资源语言在医疗 AI 领域的数据与模型空白。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
共涉及 **12 个意大利语医疗数据集**，涵盖 **20 个子任务**，分为五大类：

| Task | Datasets | 子任务数 |
|------|---------|--------|
| **Named Entity Recognition (NER)** | E3C, PharmaER, CardioCCC, DisteMIST, PsyNIT, E3C-projected | 6 |
| **Relation Extraction (RE)** | E3C | 1 |
| **Case Report Form (CRF) Filling** | E3C-based dataset (Ferrazzi et al.) | 3 |
| **Question Answering (QA)** | MedExpQA, AT, MedMCQA, MedQA | 4 |
| **Argument Mining (ARG)** | Casimedicos-Arg | 1 |

> 其中部分数据集用于 **out-of-distribution (OOD)** 测试，确保泛化能力评估的公平性。

### 实验设置和评估指标
- **模型选择**：基于 Llama-3, Gemma-3, Qwen3 家族，选取约 1B 参数的 SLLMs：
  - `Llama-3.2-1B` / `Llama-3.2-1B-Instruct`
  - `Gemma-3-1B` / `Gemma-3-1B-Instruct`
  - `Qwen3-1.7B` / `Qwen3-1.7B-Instruct`
- **基线模型**：
  - `Qwen3-32B`（4-shot）
  - `Medgemma-27B`（4-shot）
- **评估指标**：
  - NER, CRF, ARG, RE：**F1 Score (exact match)**
  - QA：**Accuracy**
  - 最终报告 **平均得分（AVG）**，各任务等权重。

### 基线方法对比
- 所有 SLLMs 在零样本（0-shot）下表现极差。
- 主要对比以下方法组合：
  - 0-shot vs 4-shot
  - 是否使用 Constraint Decoding
  - 是否进行 Supervised Fine-Tuning
  - 是否进行 Continual Pre-Training + FT
- 以 `Qwen3-32B + 4-shot` 作为主要性能基准。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **最佳模型表现**：
  - `Qwen3-1.7B + FT` 达到 **AVG = 63.9**
  - 超过基线 `Qwen3-32B + 4-shot`（54.7）**+9.2 分**
- **其他优秀配置**：
  - `Gemma-3-1b-it + CPT + FT`: 59.4 (+4.7)
  - `Llama-3.2-1B-Instruct + FT`: 59.0 (+4.3)

### 与基线方法的对比结果
| 方法 | 相对于 Qwen3-32B 的 Δ |
|------|---------------------|
| 0-shot | -54.7 |
| 4-shot | -30.5 ~ -39.9 |
| 4-shot + CD | -17.9 ~ -37.0 |
| **Fine-Tuning (FT)** | **+1.2 ~ +9.2 ✅** |
| CPT + FT | -0.9 ~ +4.7 |

> ✅ 表明多个 SLLM 配置**显著优于最大基线模型**

### 消融实验结果
#### 推理时方法（Inference-time）
| 方法 | 平均提升 | 推理时间增加 |
|------|--------|-------------|
| 4-shot vs 0-shot | +9.4 pts | +53% ⚠️ |
| Constraint Decoding | +3.8 pts | 可忽略 |
| **4-shot + CD** | **+13.2 pts** | +53% |

> 小结：**few-shot 是最有效的推理优化手段**；CD 对格式一致性帮助大，但增益有限；两者结合效果最佳。

#### 训练时方法（Training-time）
| 方法 | 效果 |
|------|------|
| **Supervised Fine-Tuning (FT)** | **最有效**，在所有模型上带来巨大提升（+40~50 pts） |
| **Continual Pre-Training + FT** | 仅在 `Gemma-3-1b-it` 上优于单独 FT，其余情况下略逊于直接 FT |

> 小结：**Fine-Tuning 是黄金标准**；CPT 成本高且收益不稳定。

#### 任务难度分析
- **最难任务**：Relation Extraction（RE）— 小模型难以捕捉复杂实体关系
- **最易任务**：Question Answering（QA）— 因预训练中已接触大量 QA 数据
- **受益最多任务**：Argument Mining — FT 带来最大相对提升

#### OOD 泛化能力
- 在未见数据集上，**FT 模型仍优于其对应 pre-adaptation 版本**：
  - `Llama-3.2`:+11.9
  - `Qwen3`: +6.1
  - `Gemma-3`: +3.9
- 但**仍低于 Qwen3-32B 基线** → 大模型泛化更强，小模型依赖特定适配。

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **Small LLMs 可以胜过大模型**：通过适当的适应（尤其是 Fine-Tuning），SLLMs（~1B 参数）可以在多项意大利语医疗 NLP 任务上**超越多达 30 倍参数的 LLMs**。
2. 🥇 **Fine-Tuning 是最有效策略**：无论从性能还是稳定性看，Supervised Fine-Tuning 都是最可靠的路径。
3. ⚖️ **CPT 收益有限**：Continual Pre-Training 成本高昂，但在多数情况下不如直接 Fine-Tuning 有效，仅在特定模型上有增益。
4. 🔧 **推理时优化有用但不足**：4-shot + Constraint Decoding 是低成本替代方案，适合无法训练的场景，但仍远不及 FT。
5. 🌍 **泛化能力仍是挑战**：虽然 FT 提升 OOD 性能，但小模型在跨数据集迁移上仍弱于大模型，说明当前优势依赖于任务/数据对齐。

### 方法的局限性
- **语言限制**：仅针对意大利语，结论是否适用于其他语言尚不明确。
- **OOD 评估保守**：未对 OOD 数据微调，可能低估潜力，但也保证了评估公正性。
- **任务不平衡**：QA 数据量多且简单，可能影响整体平均分的代表性。
- **未探索强化学习等高级方法**：仅覆盖主流适应技术。

### 未来工作方向
- 扩展至多语言与跨语言迁移场景。
- 探索更高效的 CPT 策略（如 curriculum learning, domain mixing）。
- 引入 Reinforcement Learning from Human Feedback (RLHF) 或专家反馈进一步优化输出质量。
- 开发轻量化推理框架，使 SLLMs 更易于临床集成与部署。

--- 

> **一句话总结**：  
> 本文证明，**精心调优的小型语言模型（SLLMs）是医疗 NLP 中极具竞争力且资源友好的解决方案**，尤其在 Fine-Tuning 的加持下可超越巨型模型，为资源受限环境提供了切实可行的技术路径。

</details>

---

### 10. [Retrospective In-Context Learning for Temporal Credit Assignment with Large Language Models](https://arxiv.org/abs/2602.17497)

**Authors**: Wen-Tse Chen, Jiayu Chen, Fahim Tajwar, Hao Zhu, Xintong Duan, Ruslan Salakhutdinov, Jeff Schneider  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.17497v1  

#### Abstract
Learning from self-sampled data and sparse environmental feedback remains a fundamental challenge in training self-evolving agents. Temporal credit assignment mitigates this issue by transforming sparse feedback into dense supervision signals. However, previous approaches typically depend on learnin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Retrospective In-Context Learning for Temporal Credit Assignment with Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**在线强化学习（online RL）中稀疏奖励（sparse rewards）导致的学习效率低下和训练不稳定**这一核心挑战，尤其是在多轮决策任务（multi-turn decision-making）中，环境反馈极少且延迟严重，难以进行有效的**时序信用分配（temporal credit assignment）**。

传统方法依赖于从零开始训练价值函数（value function）或优势函数（advantage function），这通常需要大量样本，泛化能力差，尤其在语言条件任务中表现不佳。

---

### 提出的新方法与创新思路

作者提出了两个核心组件：

#### （1）**Retrospective In-Context Learning (RICL)**  
- 利用预训练的 **Large Language Model (LLM)** 作为策略（policy）和反思器（reflector）。
- 在每个时间步 $t$，利用事后轨迹（hindsight trajectory）通过一个 **LLM reflector** 生成**自然语言形式的反馈（verbal feedback）**。
- 将该反馈嵌入原始策略的 prompt 中，得到一个上下文内更新后的策略 $\pi'$。
- 通过比较原始策略 $\pi_0(a|s)$ 和更新后策略 $\pi'(a|s)$ 的 **log-probability 差异**，理论推导出其与**优势函数 $A(s,a)$ 成正比**：
  $$
  \beta \log \frac{\pi'(a|s)}{\pi_0(a|s)} \propto A_{\pi_0}(s,a)
  $$
- 这种方式将稀疏的环境奖励转化为密集的监督信号（即优势函数估计），实现高效的信用分配。

#### （2）**Retrospective In-Context Online Learning (RICOL)**  
- 一个完整的在线学习框架，结合 RICL 的信用分配结果进行策略优化。
- 使用 **Advantage Weighted Regression (AWR)** 风格的目标函数来更新 LLM 参数：
  $$
  \min_\pi \mathbb{E}_{s \sim d_k} \left[ D_{KL}\left( \cdot \,\middle|\, \pi(\cdot|s) \odot \exp\left((1-\alpha)\log\pi_k(\cdot|s) + \alpha\log\pi'(\cdot|s)\right) \right) \right]
  $$
- 引入**信任区域（trust region）机制**，防止因噪声反馈导致策略崩溃，提升稳定性。

---

### 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **样本效率** | 显著高于传统 RL 方法（如 PPO），在 BabyAI 上达到同等性能所需环境步数减少 **10–50 倍**。 |
| **无需额外网络** | 不需要训练 critic 或 reflector 网络，直接利用冻结的 LLM 进行反思。 |
| **细粒度反馈** | 每个状态独立生成反馈，而非整个轨迹统一反馈，支持更精确的信用分配。 |
| **泛化性强** | 利用 LLM 的先验知识，避免从零学习，适用于复杂语言条件任务。 |
| **理论支撑强** | 建立了 in-context update 与 KL 正则化策略更新之间的理论联系。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **1D Key-Door Environment**：自定义的一维网格世界，用于验证优势函数估计的准确性，可计算 ground-truth advantage。
- **BabyAI 平台上的四个任务**：
  - `goto`：导航到指定对象
  - `pickup`：拾取指定对象
  - `pick_up_seq_go_to`：按顺序拾取并前往目标
  - `open`：打开门
- 所有输入输出均为文本形式，动作空间离散（6个动作：Turn Left/Right, Move Forward, Pickup, Drop, Toggle）。

---

### 实验设置与评估指标

| 设置项 | 描述 |
|-------|------|
| **Actor LLM** | LLaMA-3.1-8B-Instruct / Llama-3.2-3B-Instruct |
| **Reflector LLM** | GPT-4o mini |
| **环境可见性** | Partially observable（7x7视野） |
| **最大回合长度** | goto/pickup: 16；其他: 32 |
| **奖励机制** | 稀疏二值奖励（仅成功时+1） |
| **评估指标** | 
| - 成功率（Success Rate） | 主要性能指标 |
| - 环境交互步数（Environment Steps） | 衡量样本效率 |
| - 训练时间（Training Time） | 表 2 提供详细数据 |
| - 误差条（Error Bars） | 报告三组随机种子的标准差 |

---

### 基线方法对比
1. **GPT-4o mini**：零样本性能基准，同时也是 reflector。
2. **Reflexion** [Shinn et al., 2024]：基于 in-context 自我反思的方法，使用轨迹级反馈迭代改进。
3. **PPO (3B)**：基于 Qwen2.5-3B-Instruct 的 PPO，policy 与 critic 共享 backbone。
4. **PPO (10M)**：小型 MLP 网络从头训练，测试是否 LLM 先验知识有效。
5. **RWR**（Reward-Weighted Regression）：直接使用轨迹级奖励进行 AWR 更新，无时序信用分配。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ **RQ1: RICL 的信用分配效率极高**
- 在 1D Key-Door 环境中，RICL 估计优势函数的误差远低于 Monte Carlo 方法。
- 达到相同精度，RICL 仅需约 **10 条轨迹**，而 MC 需要 **~1000 条** → **样本效率提升超过 100×**（见图2）。

#### ✅ **RQ2: RICL 更可靠，且 RICOL 对噪声鲁棒**
- 在 BabyAI goto 任务上，RICL 比标准 ICL（跨轨迹更新）准确率高 **7.2%**（图3），证明“回顾式”更新更稳定可靠。
- 即使反馈准确率下降至 **70%**，RICOL 仍能保持良好性能（图6），表明其对噪声反馈具有鲁棒性。

#### ✅ **RQ3: RICOL 样本效率显著优于基线**
- 在四个 BabyAI 任务上，RICOL 在 **1,000–10,000 环境步内快速收敛**，而其他方法尚未起步。
- 相比 PPO(3B) 快 **~10×**，相比 PPO(10M) 快 **~50×**（图4）。
- 超越 GPT-4o mini 的最终成功率，尽管使用的是更小的 LLaMA-3.2-3B 模型，说明**交互式学习优于纯模仿**。

#### 🔍 消融实验与关键发现
- **RWR vs RICOL**（图5）：RWR 缺乏时序信用分配，在稀疏奖励任务中表现差，除非初始策略已较强（如 goto）。RICOL 因生成密集信号而更具通用性。
- **Verbal Feedback 准确性影响**（图6）：即使反馈为随机（50%准确率），RICOL 性能未完全崩溃，得益于 trust region 设计。
- **Critical State Identification**（图13）：RICL 能识别出“移动向钥匙”阶段中的关键状态，而仅靠 LLM 判断的关键状态方法只能识别“捡起钥匙”这一显性动作。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM 的预训练知识可用于高效时序信用分配**：通过 in-context learning 和 log-probability 差异，可以高质量地估计优势函数。
2. **回顾式更新（retrospective update）优于通用式更新**：只在产生反馈的轨迹上更新策略，避免错误泛化，提高可靠性。
3. **RICOL 极大地提升了样本效率**：在语言条件稀疏奖励任务中，以极少量环境交互即可达到甚至超越传统 RL 方法的性能。
4. **方法对噪声反馈具备鲁棒性**：trust region 设计有效抑制了低质量反馈的影响，使得系统可在不完美条件下持续学习。

---

### 局限性
- **仅支持离散有限动作空间**：因需枚举所有动作计算归一化项 $Z(s)$。对于长思维链（CoT）等连续输出场景不适用。
- **依赖高质量 reflector LLM**：虽然未微调，但 reflector 的推理能力直接影响反馈质量。
- **当前未扩展到 token-level MDP**：未来可探索在推理过程中进行细粒度信用分配。
- **计算开销较高**：每次策略评估需多次调用 LLM（尤其是 enumerate action 时）。

---

### 未来工作方向
- 将 RICL/RICOL 扩展至 **token-level 决策过程**，应用于复杂推理任务。
- 探索 **action sampling 替代 action enumeration**，以支持更大或连续动作空间。
- 结合 **memory mechanism** 实现长期经验复用。
- 探索 **multi-agent setting** 下的协作式反思与信用分配。
- 研究如何进一步降低对 reflector LLM 的依赖，例如通过 self-bootstrapping 方式生成反馈。

---

> **总结一句话**：  
> 本文提出 **RICL + RICOL** 框架，首次将 LLM 的上下文学习能力系统性地用于**时序信用分配**，实现了**超高样本效率的在线强化学习**，为构建能够自我进化的 LLM Agent 提供了一条可行路径。

</details>

---

### 11. [DeepContext: Stateful Real-Time Detection of Multi-Turn Adversarial Intent Drift in LLMs](https://arxiv.org/abs/2602.16935)

**Authors**: Justin Albrethsen, Yash Datta, Kunal Kumar, Sharath Rajasekar  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16935v1  

#### Abstract
While Large Language Model (LLM) capabilities have scaled, safety guardrails remain largely stateless, treating multi-turn dialogues as a series of disconnected events. This lack of temporal awareness facilitates a "Safety Gap" where adversarial tactics, like Crescendo and ActorAttack, slowly bleed ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《DeepContext: Stateful Real-Time Detection of Multi-Turn Adversarial Intent Drift in LLMs》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前大型语言模型（LLM）的安全防护机制大多是**stateless**（无状态）的，即在多轮对话中将每一轮视为独立事件进行安全检测。这种设计导致了“**Safety Gap**”——攻击者利用如 **Crescendo** 和 **ActorAttack** 等渐进式攻击策略，在多个看似无害的对话回合中逐步累积恶意意图，最终绕过静态过滤器。

这类“慢热型”攻击不依赖单次强对抗提示，而是通过**语义漂移**（semantic drift）和**递归信任**（recursive trust）机制，使模型在上下文中自然地接受有害请求。

### 提出了什么新方法或新思路
本文提出 **DeepContext**，一种**有状态**（stateful）的实时监控框架，用于追踪用户意图的时序演化轨迹。其核心思想是将安全检测建模为一个**状态空间问题**，而非孤立的分类任务。

#### 创新架构：
- **Turn-Level Embedding Extraction**：使用轻量级编码器（fine-tuned BERT）提取每轮对话的高维语义嵌入，并引入 **Task-Attention Mechanism** 对与安全政策相关的关键词进行加权，增强信号敏感度。
- **Recurrent Intent Tracking (RIT)**：采用 **Gated Recurrent Unit (GRU)** 构建隐状态 $ h_t $，持续更新并记忆对话中的风险积累过程。
- **Hybrid Residual Architecture**：融合当前轮次的原始嵌入 $ e_t $ 与历史状态投影 $ \phi(h_{t-1}) $ 形成最终风险向量 $ R_t = [\phi(h_{t-1}); e_t] $，实现对“即时攻击”和“长期漂移”的双重检测能力。
- **Trajectory Classifier**：基于 MLP 的分类头结合 **Focal Loss** 处理类别不平衡问题，提升对边界样本的学习能力。

### 相比现有方法的优势
| 维度 | 现有方法（如 Llama Guard, Granite Guardian） | DeepContext |
|------|------------------------------------------|-----------|
| **状态性** | Stateless，每次重新处理完整对话历史 | Stateful，维护隐状态跟踪意图演化 |
| **计算效率** | 随对话长度呈二次增长（self-attention），延迟高 | 每轮仅处理当前嵌入 + GRU 更新，延迟恒定 |
| **检测能力** | 易被分散式攻击欺骗，难以捕捉“渐进漂移” | 可识别“intent drift”，提前预警 |
| **部署成本** | 通常需大参数量模型（7B+），资源消耗大 | 轻量化设计，可在 T4 GPU 上实现 <20ms 推理 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
#### 训练数据集（共约 437k 条对话序列，20% 标记为恶意）
| 数据集 | 描述 |
|--------|------|
| **LLMail** | 针对邮件助手的 prompt injection 攻击 |
| **HH-RLHF** | 人工标注的安全偏好与红队对话 |
| **XGuard** | 合成的自适应多轮 jailbreak 攻击 |
| **PRODIGy** | 好莱坞剧本中的角色扮演对话 |
| **DEF CON** | AI Village 红队挑战赛的真实攻击数据 |

#### 评测数据集（共 1,010 条，见 Table 3）
| 类别 | 包含数据集 |
|------|------------|
| **良性高噪声场景** | LMSYS, Glaive Function Calling, Nemotron Instruction, Anthropic HH-RLHF |
| **人类主导红队测试** | HarmBench, DEFCON 34 AI Village |
| **自动化多轮攻击框架** | Red Queen, XGuard, Automated-Multi-Turn |
| **单轮 jailbreak 基准** | JailBreakBench（用于验证单轮性能） |

> 注：确保评测样本未出现在训练集中。

### 实验设置和评估指标
- **主要任务**：多轮 jailbreak 检测（二分类：safe vs unsafe）
- **评估指标**：
  - **F1 Score**（综合衡量 precision 与 recall）
  - **Recall**（检出率，尤其关注漏报）
  - **Precision**（准确率，避免误杀）
  - **Mean Turns to Detection (MTTD)**：平均在第几轮检测到威胁
  - **Per-Turn Latency**：每轮推理耗时（T4 GPU）

- **硬件环境**：
  - 主要测试平台：Google Colab Pro+，NVIDIA A100（40GB VRAM）
  - 实际部署目标：T4 GPU（仅需 2GB VRAM）

### 基线方法对比
分为三类基线模型：

| 类型 | 代表模型 |
|------|--------|
| **轻量级嵌入分类器** | Llama-Prompt-Guard-2-86M, Deberta-v3-Prompt-Injection |
| **生成式安全模型（LLM-as-a-Judge）** | Llama-Guard-4-12B, Granite-Guardian-3.3-8B, Qwen3Guard-Gen-8B, Gpt5-Nano |
| **企业级云服务 API** | Azure Prompt Shield, AWS Prompt Attack Guardrails, GCP Model Armor |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（多轮 jailbreak 检测）

| Model | F1 Score | Recall | Precision | MTTD |
|-------|----------|--------|-----------|------|
| **DeepContext (Ours)** | **0.84** | **0.83** | **0.86** | **4.24** |
| Llama-Prompt-Guard-2 | 0.67 | 0.60 | 0.76 | 5.83 |
| Granite-Guardian-3.3-8B | 0.67 | 0.57 | 0.83 | 5.03 |
| Gpt5-Nano | 0.65 | 0.55 | 0.81 | 5.73 |
| Azure Prompt Shield | 0.19 | 0.11 | 0.62 | 8.00 |

> ✅ DeepContext 在 F1 上领先第二名约 **25%**，且召回率显著更高，说明能更早、更全面地捕获复杂攻击。

### 单轮 jailbreak 性能（JailBreakBench）

| Model | F1 Score | Recall | Precision |
|-------|----------|--------|-----------|
| **DeepContext (Ours)** | **0.98** | **1.00** | 0.95 |
| Qwen3Guard-Gen-8B | 0.88 | 0.95 | 0.83 |
| Llama-Guard-4-12B | 0.86 | 0.86 | 0.86 |
| GCP Model Armor | 0.83 | 0.96 | 0.74 |

> ✅ 即使没有上下文，DeepContext 仍保持接近完美的表现，证明其兼具 stateful 与 stateless 检测能力。

### 推理延迟对比（Per-Turn Latency）

| Model | Latency (ms) |
|-------|---------------|
| Llama-Prompt-Guard-2 | 4 |
| **DeepContext (Ours)** | **19** |
| Llama-Guard-4-12B | 43 |
| Granite-Guardian-3.3-8B | 125 |
| AWS Prompt Attack Guardrails | 235 |
| Gpt5-Nano | 317 |

> ⚡ DeepContext 在保持高性能的同时，实现 **<20ms** 推理延迟，适合实时交互系统。

### 消融实验与分析（Implicit Ablation Insights）
虽然未明确列出消融表，但从设计可推断以下关键组件作用：
- **GRU 隐状态** → 显著降低 MTTD（从 >5 到 4.24），提高 Recall
- **Task-Attention 加权** → 提升嵌入质量，帮助区分高风险语义
- **Residual Shortcut ($ e_t $ 直连)** → 保留瞬时强信号，防止 GRU “平滑”掉明显攻击
- **Focal Loss (γ=2.0)** → 提升对“难例”学习能力，避免过拟合简单模式

此外，Table 5 显示：
> 所有 LLM 基线在使用**全历史上下文**时性能等于或低于**滑动窗口**（last 5 turns），表明缺乏专门的状态跟踪机制会导致注意力稀释。

---

## 4. 关键结论和发现

### 论文的主要发现
1. 🔍 **“State over Scale” 胜出**  
   尽管 DeepContext 参数远小于 Llama-Guard-4-12B 或 Granite-Guardian，但在多轮攻击检测上表现更优，证明**状态感知比模型规模更重要**。

2. ⚙️ **效率与效果可以兼得**  
   通过将语义提取与状态估计解耦，DeepContext 实现了 **F1=0.84 + <20ms latency** 的理想平衡，打破了“安全必慢”的固有认知。

3. 🧠 **意图漂移是可建模的轨迹**  
   用户意图并非突变，而是在 latent space 中连续演化的路径。RNN 能有效捕捉这一“gradual accumulation of risk”。

4. ☁️ **主流云服务存在严重盲区**  
   Azure Prompt Shield 虽然 Precision 较高（0.62），但 Recall 仅为 0.11，意味着 **89% 的多轮攻击会被放行**，暴露其 stateless 设计的根本缺陷。

### 方法的局限性
- **False Positives in Complex Functional Dialogs**：在函数调用等技术性复杂指令中可能出现误判，因 task-attention 可能混淆“功能性复杂”与“注入性复杂”。
- **依赖高质量 turn-level embedding**：若初始编码器未能捕获关键语义，则后续状态传播会失效。
- **无法防御 M2S 类型攻击**：即将多轮攻击压缩为单轮结构化 payload 的攻击方式可能绕过时序建模。

### 未来工作方向
1. **Generalized Intent Detection**  
   扩展至通用意图轨迹对齐，不仅用于安全，还可监控 agent 是否偏离原始目标（goal drift）。

2. **Dynamic Policy Controls**  
   利用连续的风险向量 $ R_t $ 实施动态干预策略，如自动降权、触发 human-in-the-loop 审核等。

3. **Deterministic Threat Tracking**  
   结合概率性 RNN 与确定性规则引擎（如 FSM 或图谱追踪），提供可解释、合规的审计日志。

4. **Federated Intent Monitoring**  
   在跨 agent 工作流中实现分布式状态同步，构建全局意图视图。

5. **Proactive Safety Steering**  
   不只是拦截，而是主动引导主 LLM 调整 persona 或 reasoning depth，实现“软制动”。

---

> 💡 **结语**：  
> DeepContext 表明，未来的 LLM 安全不应再依赖“更大更强”的 stateless 模型，而应转向“更聪明更有记忆”的 stateful 架构。它为构建真正具备**认知安全意识**的 AI 系统奠定了基础。

</details>

---

### 12. [Heterogeneous Federated Fine-Tuning with Parallel One-Rank Adaptation](https://arxiv.org/abs/2602.16936)

**Authors**: Zikai Zhang, Rui Hu, Jiahao Xu  
**Category**: cs.DC  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16936v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable effectiveness in adapting to downstream tasks through fine-tuning. Federated Learning (FL) extends this capability by enabling collaborative fine-tuning across distributed clients using Low-Rank Adaptation (LoRA), while preserving data privac...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Heterogeneous Federated Fine-Tuning with Parallel One-Rank Adaptation

## 1. 论文的主要贡献和创新点

### 解决的问题
在联邦学习（Federated Learning, FL）中对大语言模型（LLMs）进行微调时，客户端通常采用低秩适应（Low-Rank Adaptation, LoRA）技术以提升效率并保护数据隐私。然而，在实际部署中，客户端资源异构（如计算能力不同），导致其可训练的LoRA秩（rank）各不相同。这种**异构LoRA秩**带来了两个关键挑战：
- **初始化噪声（Initialization Noise）**：全局聚合后的高秩LoRA模块无法直接用于低秩客户端的初始化，需截断或随机重初始化，造成信息丢失。
- **聚合噪声（Aggregation Noise）**：不同秩的本地更新难以有效聚合，引入偏差或失真。

现有方法（如FLoRA、FlexLoRA、HETLoRA）虽尝试解决该问题，但在初始化或聚合阶段仍存在显著噪声，影响全局模型性能。

---

### 提出的新方法：Fed-PLoRA
为解决上述问题，本文提出 **Fed-PLoRA**，一种轻量级异构联邦微调（Heterogeneous FFT）框架，其核心创新包括：

#### （1）Parallel One-Rank Adaptation (PLoRA)
- 将传统的单个多秩LoRA模块（multi-rank LoRA）替换为**多个并行的一秩（one-rank）LoRA模块**。
- 数学上等价于标准LoRA，但具备模块化优势，天然支持不同客户端选择不同数量的模块进行训练。

#### （2）Select-N-Fold 初始化策略
- 在每轮训练开始时，服务器广播完整的全局PLoRA参数（共R个一秩模块）。
- 每个客户端**随机选择N个模块**（N为其本地秩）进行训练。
- 其余未选中的模块被“折叠”（fold）到冻结的预训练权重中，作为固定偏置保留。
- 此策略确保所有客户端都能继承完整的全局知识，**完全消除初始化噪声**。

---

### 相比现有方法的优势
| 方法 | 初始化噪声 | 聚合噪声 | 通信/计算开销 |
|------|------------|----------|----------------|
| **FLoRA** | 高（每轮随机初始化） | 无（堆叠聚合） | 上行低，下行高 |
| **FlexLoRA** | 中高（SVD截断） | 中（SVD重构误差） | 服务端计算高（SVD） |
| **HETLoRA** | 中高（零填充截断） | 高（分离均值偏差） | 较低 |
| **Fed-PLoRA (Ours)** | **零** | **极低** | **轻量级，仅下行略增** |

- **理论优势**：统一分析表明，Fed-PLoRA实现了近似最优的初始化，并最小化了聚合噪声。
- **实践优势**：模块化设计轻量，易于集成到现有LoRA和FL流程中。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多领域任务，验证方法通用性：
- **通用指令遵循**：Natural Instructions, Dolly-15K
- **自然语言理解**：GLUE benchmark（CoLA, SST-2, MRPC, QQP, QNLI, RTE）
- **金融领域**：FinGPT（训练），FPB, FIQA, TFNS（测试）
- **医疗领域**：MedAlpaca（训练），PubMedQA, MedMCQA, MedQA, CareQA（测试）
- **数学推理**：MATH

### 实验设置
- **模型规模**：涵盖BERT-base、Llama-1B、Llama-3.1-8B、OPT-1.3B、Mistral-7B-v0.3、Qwen3-4B等。
- **LoRA配置**：应用于self-attention层的`q_proj`和`v_proj`。
- **客户端设置**：
  - 客户端数：50–200
  - 每轮采样10%
  - 异构秩设置：将客户端分为三组，分别使用秩 `(1, r_m, R)`，其中 `R` 为最优全局秩，`r_m < R`，平均秩 ≤ `R/2`
- **数据划分**：
  - **IID**：均匀划分
  - **Non-IID**：病理划分（pathological）或Dirichlet分布模拟极端非独立同分布

### 评估指标
- **主要指标**：Rouge-L（指令任务）、Accuracy / Matthews Corr.（分类任务）
- **对比基线**：
  - **FedIT**：同构LoRA下的基准（仅用于同构对比）
  - **FLoRA**：基于堆叠的异构聚合
  - **FlexLoRA**：基于SVD的异构聚合
  - **HETLoRA**：基于零填充的异构聚合

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 1–3及正文）

#### （1）Natural Instructions（Llama-1B）
| 方法 | IID (Rouge-L) | Non-IID (Rouge-L) |
|------|---------------|-------------------|
| Untuned | 33.82 | 33.82 |
| FedIT (R) | 66.88 | 61.28 |
| **Fed-PLoRA** | **64.96** | **60.76** |
| HETLoRA | 58.07 | 58.84 |
| FlexLoRA | 59.07 | 53.51 |

- Fed-PLoRA在**非IID下超越所有异构基线**，且接近甚至优于部分同构设置（如FedIT Rank=R/2）。
- 相比FLoRA，提升高达 **+31.14%**（IID）和 **+26.94%**（Non-IID）。

#### （2）GLUE（BERT-base, R=16, avg rank=7）
| 方法 | CoLA | SST-2 | MRPC | QQP | RTE | **Avg** |
|------|------|-------|------|-----|-----|--------|
| Untuned | 1.20 | 49.08 | 31.61 | 63.09 | 52.70 | 41.27 |
| FedIT (R) | 61.68 | 92.47 | 87.58 | 86.86 | 68.35 | 81.08 |
| **Fed-PLoRA** | **59.38** | **92.35** | **86.35** | **87.25** | **65.46** | **79.89** |
| HETLoRA | -2.64 | 91.74 | -3.76 | 77.53 | -2.05 | 73.55 |

- Fed-PLoRA在**所有任务上全面超越异构基线**，平均得分达79.89，接近同构FedIT。
- 相比HETLoRA，RTE提升 **+67.51%**；相比FLoRA，CoLA提升 **+63.46%**。

#### （3）金融数据（Llama-3.1-8B）
| 方法 | FPB | FIQA | TFNS | **Avg** |
|------|-----|------|------|--------|
| Untuned | 50.57 | 29.33 | 49.87 | 40.12 |
| **Fed-PLoRA** | **63.94** | **31.68** | **64.19** | **53.27** |
| FlexLoRA | 62.79 | 31.76 | 62.60 | 52.38 |
| HETLoRA | 60.06 | 32.01 | 61.26 | 51.11 |

- Fed-PLoRA在**平均准确率上领先**，尤其在FPB上表现最佳。

---

### 消融实验结果（Ablation Studies）

#### （1）Select-N-Fold 策略有效性（Figure 4）
- 对比策略：
  - **Fixed**：始终选择前r个模块
  - **Weight Norm**：选择权重范数最大的模块
  - **Select-N-Drop**：随机选择但丢弃其余模块
- 结果：**Select-N-Fold** 性能最高（CoLA上达59.38），证明“折叠”机制复用未训练模块，提升稳定性。

#### （2）不同选择策略可视化（Figure 6）
- 随机选择（Select-N-Fold）带来更均匀的模块更新分布，避免局部过拟合。

#### （3）Dropout 影响（Table 12）
- 在Dolly-15K上，是否对折叠模块应用dropout对性能**几乎无影响**（60.07 vs 60.07），说明无需额外正则化。

#### （4）超参数鲁棒性（Table 9–11）
- Fed-PLoRA在不同客户端数量（100 vs 200）、资源异构比例（1:1:1 vs 6:3:1）、LoRA秩配置下均保持稳定领先。

---

## 4. 关键结论和发现

### 主要发现
1. **并行一秩设计（PLoRA）是缓解异构FFT噪声的关键**：通过模块化分解，使不同客户端能灵活参与训练而不损失全局知识。
2. **Select-N-Fold策略实现零初始化噪声**：通过“折叠”机制保留未训练模块的信息，显著优于截断或随机初始化。
3. **Fed-PLoRA在精度和效率上全面超越现有方法**：在多种任务、模型和数据分布下均取得SOTA性能，且通信和计算开销可控。
4. **随机选择增强模块同步性**：跨轮次的随机选择机制促进所有一秩模块被充分训练，提升跨客户端一致性（见Figure 2, 8）。

---

### 方法的局限性
- **下行通信成本略高**：服务器需向所有客户端发送完整R秩参数，对最弱客户端略有负担（但仍远低于FLoRA）。
- **依赖LoRA假设**：目前仅适用于LoRA类PEFT方法，未扩展至Adapter、Prefix-tuning等其他参数高效微调技术。
- **极端高秩场景可能不稳定**：当全局秩R过大（如128）而客户端最大秩受限时，性能可能下降（见Table 11）。

---

### 未来工作方向
1. 进一步降低聚合阶段的残余噪声，探索结合Selective Aggregation（如FedSA-LoRA）等优化策略。
2. 扩展至其他PEFT方法（如Adapter、Prefix-tuning）的异构联邦训练。
3. 探索并行化如何更好地对齐数据异构下的模型更新，提升在极端Non-IID下的鲁棒性。
4. 与量化（Quantization）、稀疏化等技术结合，打造更高效的端到端联邦微调系统。

> ✅ **代码已开源**：[https://github.com/TNI-playground/Fed-PLoRA](https://github.com/TNI-playground/Fed-PLoRA)

</details>

---

### 13. [A Few-Shot LLM Framework for Extreme Day Classification in Electricity Markets](https://arxiv.org/abs/2602.16735)

**Authors**: Saud Alghumayjan, Ming Yi, Bolun Xu  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16735v1  

#### Abstract
This paper proposes a few-shot classification framework based on Large Language Models (LLMs) to predict whether the next day will have spikes in real-time electricity prices. The approach aggregates system state information, including electricity demand, renewable generation, weather forecasts, and...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Few-Shot LLM Framework for Extreme Day Classification in Electricity Markets

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对电力市场中**real-time electricity price spikes（实时电价尖峰）预测困难**的问题。这类极端事件具有以下挑战：
- 发生频率低（稀有事件），传统监督学习模型在训练时容易被正常样本主导；
- 受多种因素影响（如可再生能源出力波动、天气变化、负荷需求、输电拥堵等），系统动态复杂且随时间演变；
- 在历史数据有限的情况下，现有机器学习模型性能显著下降。

因此，如何在**标签稀缺（limited labeled data）** 的场景下准确识别潜在的电价尖峰日，是本研究关注的核心问题。

### 提出的新方法与新思路
作者提出了一种基于 **Large Language Models (LLMs)** 的**few-shot 分类框架**，用于判断次日是否为电价尖峰日。其核心创新包括：

- **无需直接训练模型**：整个框架不依赖传统的参数更新或监督训练过程，而是通过自然语言提示（prompting）引导 LLM 进行推理。
- **将结构化特征转化为自然语言描述**：将电力系统的多维状态变量（如负荷、可再生能源预测、价格统计量等）转换成文本形式的 prompt 输入给 LLM。
- **结合 embedding-based 示例检索机制**：利用 FAISS 构建历史数据的文本嵌入索引，并采用 **Maximal Marginal Relevance (MMR)** 策略从过去相似天数中检索最具代表性又非冗余的 few-shot 示例，增强上下文学习效果。
- **端到端分类流程设计**：构建完整的 pipeline，涵盖数据预处理 → 文本提示生成 → 示例检索 → LLM 推理 → 输出解析。

### 相比现有方法的优势
| 维度 | 传统方法（如 SVM, XGBoost） | 本文提出的 LLM 方法 |
|------|----------------------------|---------------------|
| 数据效率 | 需要大量标注数据才能有效训练 | 在极少量历史数据下仍保持稳定性能 |
| 模型适应性 | 对分布偏移敏感，需频繁重训 | 利用 LLM 的泛化能力应对新型极端情况 |
| 开发成本 | 需特征工程 + 模型调参 + 训练流程 | 仅需设计 prompt 和示例选择策略，免训练 |
| 尖峰检测灵敏度 | 易受类别不平衡影响 | 更好捕捉罕见事件模式 |

> ✅ **关键优势总结**：该方法是一种**高数据效率、免训练、适用于小样本场景的电价尖峰分类工具**。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **数据来源**：美国德州电力市场（ERCOT）系统级公开数据；
- **时间范围**：
  - 训练/参考期：2021–2023 年（共三年）
  - 测试期：2024 年全年（共 366 天）
- **数据类型**：
  - Day-Ahead Price (DAP) 与 Real-Time Price (RTP)
  - 净负荷预测与实际值（Net Demand）
  - 可再生能源发电预测（Wind/Solar）
  - 天气数据（温度、风速等）
  - 预留容量（Reserves）
  - 日历信息（星期几、月份）

> ⚠️ **标签定义**：若某日 RTP 的日内标准差超过历史 95% 分位数，则标记为 **spike day**。

### 特征工程
共构造了约 30+ 维 engineered features，主要包括：
- 统计特征：mean, std, min, max
- 动态趋势：momentum（当前 vs 前一日）、lagged values
- 差分特征：temperature difference, net demand change
- 时间特征：day of week, month
- 渗透率指标：pct_renewable_forecast

这些特征最终被格式化为自然语言句子，例如：
> “Day-Ahead Price Mean = 22.51, Temperature Max = 35°C, Is_DAP_Spike = No, Day = Friday”

### 实验设置
- **目标任务**：binary classification —— 预测次日是否为 spike day
- **主模型**：
  - **LLM-based classifier**：使用 `gpt-4.1` 模型进行 zero/few-shot 推理
- **Baseline 模型**：
  - **SVM**（支持向量机）
  - **XGBoost**（梯度提升树）
  - 二者均使用相同特征集，在完整训练集上训练
- **硬件环境**：
  - CPU: Intel Xeon Platinum 8640Y @ 2GHz
  - GPU: 2×NVIDIA A40
  - 内存: 512GB

### 评估指标
基于混淆矩阵计算以下标准指标：
- **Precision（精确率）**
- **Recall（召回率）**
- **Accuracy（准确率）**
- **F1-score**
- **ROC Curve & AUC**
- **Precision-Recall (PR) Curve**

此外还比较了不同阈值设定下的表现：
- 固定阈值（0.5）
- ROC-optimal threshold（平衡 TPR 与 FPR）
- PR-optimal threshold（最大化 F1-score）

并在两种数据条件下测试：
1. 完整训练数据（2021–2023）
2. 有限数据场景（仅用最近两个月数据）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table II & III）

#### （1）完整数据设置下的性能对比（Table II）

| Model | Precision | Recall | Accuracy | F1-score |
|-------|-----------|--------|----------|----------|
| **XGBoost (PR-optimal)** | 26.67% | 44.44% | 95.63% | **33.33%** |
| **SVM (PR-optimal)** | 37.5% | 33.33% | 96.99% | **35.29%** |
| **LLM (PR-optimal)** | **50.0%** | 22.22% | **97.54%** | 30.77% |

> 🔍 观察：
- LLM 在 **precision 上最高（50%）**，说明其预测出的“尖峰日”更可信；
- 虽然 recall 较低，但在高风险应用中（如储能套利、虚拟电厂投标），避免误报尤为重要；
- 所有模型 accuracy 接近，因非尖峰日占绝大多数（imbalanced dataset）；

#### （2）有限数据设置下的性能对比（Table III）

当只使用 **两个月历史数据** 时：

| Model | Precision | Recall | Accuracy | F1-score |
|-------|-----------|--------|----------|----------|
| XGBoost | 2.46% | 100% | 2.46% | 4.8% |
| SVM | 2.6% | 22.22% | 77.6% | 4.65% |
| **LLM** | **37.5%** | **33.33%** | **96.99%** | **35.29%** |

> ✅ **核心发现**：
- 传统模型（XGBoost/SVM）在小样本下严重退化，甚至出现全错（如 XGBoost PR-optimal 下 TP=0）；
- **LLM 性能几乎不受影响**，F1-score 达到 35.29%，远超其他方法；
- 表明 LLM 在 **data efficiency 方面具有显著优势**。

#### （3）概率输出分析（Figure 2）
- LLM 输出的概率更加平滑，且在真实 spike 期间集中输出高置信度；
- SVM/XGBoost 概率波动剧烈，缺乏一致性；
- LLM 的 PR-optimal threshold 高达 **0.91**，反映其对正类预测更为保守而可靠。

> ❌ 无消融实验报告（ablation study 缺失）

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLM 可以在无需训练的前提下实现高性能电价尖峰分类**，性能媲美甚至优于传统 ML 模型；
2. ✅ **Few-shot prompting + 示例检索机制能有效激活 LLM 的领域推理能力**，即使面对未见过的系统状态组合；
3. ✅ **在历史数据稀缺场景下，LLM 明显优于 SVM 和 XGBoost**，展现出强大的数据效率和鲁棒性；
4. ✅ **自然语言接口降低了部署门槛**，便于融入人类专家知识与解释逻辑。

### 方法的局限性
- ❌ **依赖高质量 prompt 设计与示例选择策略**，性能可能随 prompt 改动波动；
- ❌ **无法保证因果推断正确性**，LLM 可能基于表面相关性做出错误判断；
- ❌ **推理延迟较高**，不适合高频实时决策；
- ❌ **黑箱性强**，缺乏可解释性保障，在关键基础设施中存在信任障碍；
- ❌ **未进行跨区域迁移验证**（仅测试 ERCOT），普适性待验证。

### 未来工作方向
1. **扩展上下文长度**：引入 multi-day contextual information，提升趋势感知能力；
2. **微调领域专用 LLM**：在电力市场语料上继续预训练或指令微调（fine-tune domain-specific LLMs）；
3. **集成不确定性量化机制**：结合 conformal prediction 或 ensemble prompting 提供预测区间；
4. **探索多模态输入**：融合图表、时间序列可视化等辅助信息作为 prompt 输入；
5. **应用于其他极端事件检测任务**：如停电预警、设备故障诊断等。

---

> 📌 **总体评价**：  
> 本文开创性地将 **LLM 的 few-shot learning 能力引入电力市场极端事件分类任务**，展示了 LLM 作为一种**轻量级、免训练、高效能的小样本建模范式**的巨大潜力，尤其适合数据稀疏、分布快速变化的实际应用场景。

</details>

---

### 14. [Spatio-temporal dual-stage hypergraph MARL for human-centric multimodal corridor traffic signal control](https://arxiv.org/abs/2602.17068)

**Authors**: Xiaocai Zhang, Neema Nassir, Milad Haghani  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.17068v1  

#### Abstract
Human-centric traffic signal control in corridor networks must increasingly account for multimodal travelers, particularly high-occupancy public transportation, rather than focusing solely on vehicle-centric performance. This paper proposes STDSH-MARL (Spatio-Temporal Dual-Stage Hypergraph based Mul...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Spatio-temporal dual-stage hypergraph MARL for human-centric multimodal corridor traffic signal control

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统交通信号控制（TSC）方法（如 SCATS、SCOOT）主要优化**车辆中心化指标**（如车流、排队长度），忽视了多模式出行者（尤其是高载客量的公共交通如公交、有轨电车）的通行需求，导致**人本公平性缺失**。随着城市交通向多模态发展，亟需一种能够兼顾各类出行者体验、特别是优先保障大容量公共交通的**人本化（human-centric）信号控制策略**。

此外，现有基于 DRL 的多智能体方法在建模复杂时空依赖关系时存在局限，难以有效捕捉交叉口之间的**高阶空间关联**与**动态时间演化**。

### 提出的新方法与创新
本文提出 **STDSH-MARL**（Spatio-Temporal Dual-Stage Hypergraph-based Multi-Agent Reinforcement Learning），一个面向人本多模态走廊网络的可扩展 MARL 框架，其核心创新如下：

- ✅ **人本化奖励机制**：首次将“经历延误的乘客总数”作为核心优化目标，而非仅关注车辆数量，真正实现以“人”为中心的交通管理。
- ✅ **双阶段超图注意力机制（Dual-Stage Hypergraph Attention, DSHA）**：
  - 引入**空间超边（Spatial Hyperedges）** 和 **时间超边（Temporal Hyperedges）**，分别建模多个交叉口在同一时刻的空间交互、以及单个交叉口在不同时刻的时间演化。
  - 设计**双阶段注意力机制**：先进行**超边内注意力**（intra-hyperedge attention）聚合节点信息生成超边嵌入，再通过**超边间注意力**（inter-hyperedge attention）学习不同超边对节点的重要性，从而捕获更复杂的高阶时空依赖。
- ✅ **混合离散动作空间**：联合决策下一相位配置（4种）及其绿灯持续时间（8–45秒，共38个选项），形成维度为 $4 \times 38 = 152$ 的离散动作空间，提升控制灵活性。
- ✅ **CTDE 范式下的高效架构**：
  - **去中心化执行**：每个交叉口智能体基于本地观测独立决策，适合大规模部署。
  - **集中式训练**：利用全局超图嵌入构建中央 critic，辅助信用分配，提升学习效率。

### 相比现有方法的优势
| 维度 | STDSH-MARL 的优势 |
|------|------------------|
| **目标导向** | 从 vehicle-centric 转向 **passenger-centric**，显著改善公交优先与出行公平性 |
| **表示能力** | 超图结构优于传统 GNN/CNN，能建模**多对多关系**，避免成对建模的信息损失 |
| **时空建模** | 显式分离并融合空间与时间依赖，优于单一图或静态图方法 |
| **控制粒度** | 同时优化相位切换与绿灯时长，比仅选相位或固定周期更具适应性 |

---

## 2. 核心实验方法和设置

### 数据集与仿真环境
- **仿真平台**：使用微观交通仿真软件 **PTV VISSIM** 构建澳大利亚左行制式下的合成走廊网络。
- **网络结构**：包含 **6个信号化交叉口**，每交叉口4个方向，每个方向2条进口车道；一条**有轨电车线路贯穿全网**，设3个站点。
- **交通模式**：涵盖私家车、公交车、有轨电车、行人、骑行者等多模态出行者。
- **实时数据输入**：假设可通过先进感知技术获取各模式的乘客数（如 Wi-Fi 人数估计、蓝牙行人检测、APC 公交载客预测）。

### 实验设置
- **场景设计**：共测试 **5种典型交通场景**，覆盖多种需求模式：
  1. **Scenario 1**：平峰低需求
  2. **Scenario 2**：平峰向高峰过渡
  3. **Scenario 3**：早高峰高需求
  4. **Scenario 4**：上学早高峰（进校方向流量大）
  5. **Scenario 5**：放学晚高峰（离校方向流量大）

- **训练与评估**：所有模型在 Python 中基于 TensorFlow 实现，在配备 AMD EPYC CPU 的工作站上运行。

### 评估指标
| 指标 | 定义 | 关注重点 |
|------|------|---------|
| **ANP** (Average Number of Passengers experiencing delay) | 单位时间内平均延误乘客数（人/秒） | **人本绩效核心指标** |
| **AQL** (Average Queue Length) | 所有交叉口上游车辆平均排队长度（不含有轨电车） | 私家车拥堵水平 |
| **AWT (bus)** | 公交车穿越整个走廊的平均等待时间（秒） | 公交优先效果 |
| **AWT (tram)** | 有轨电车穿越整个走廊的平均等待时间（秒） | 大容量公交优先 |

### 基线方法对比
| 基线方法 | 类型 | 特点 |
|--------|------|------|
| **FS-WF** (Fixed Signal with Webster’s Formula) | 固定配时 | 基于经典公式计算周期与时分 |
| **MADQN** | Value-based (DQN) | 分布式训练与执行 |
| **MADDQN** | Value-based (DDQN) | 改进 Q 值过估计问题 |
| **MAA2C** | Hybrid (Actor-Critic) | CTDE 范式，稳定学习 |
| **MAPPO** | Policy-based (PPO) | 无超图结构，标准 MARL |
| **CMRM** | Graph-based (GAT + PPO) | 使用图注意力网络，用于验证超图优势 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（最优值加粗，次优下划线）

| 场景 | 方法 | ANP ↓ | AQL ↓ | AWT (bus) ↓ | AWT (tram) ↓ |
|------|------|-------|-------|-------------|--------------|
| **1** | STDSH-MARL | **1381.70** | 304.41 | **296.79** | **285.55** |
|      | MADDQN | 1534.40 | **290.34** | 834.46 | 1607.45 |
| **2** | STDSH-MARL | **1551.41** | 319.91 | 356.45 | **267.15** |
|      | MAA2C | 1735.13 | 387.90 | **354.34** | 310.90 |
| **3** | STDSH-MARL | **1649.23** | 359.24 | **373.34** | **284.90** |
|      | MAPPO | 1730.11 | 363.42 | 373.67 | 347.80 |
| **4** | STDSH-MARL | **1553.27** | 331.45 | **339.70** | 289.80 |
|      | MADQN | 1952.03 | **306.94** | 915.68 | 1558.50 |
| **5** | STDSH-MARL | **1573.86** | 339.95 | **332.05** | 351.80 |
|      | MADQN | 1608.17 | **296.93** | 802.26 | 1556.67 |

> 注：STDSH-MARL 在 **所有场景的所有ANP指标上均取得最佳表现**，且在 AWT (bus/tram) 上远优于其他方法，表明其卓越的**公共交通优先能力**。

### 与基线方法的对比结果
- 📉 **ANP 平均降低超过 10%**：相比最强基线（如 MADDQN、MAPPO），STDSH-MARL 在多数场景中将延误乘客数减少 **10~15%**，最高达 **18.65%**（vs MAPPO in Scenario 1）。
- 🚌 **公交与有轨电车等待时间大幅下降**：
  - AWT (bus) 平均比 DQN/DDQN 类方法低 **500+ 秒**
  - AWT (tram) 从基线的 **>1500 秒** 降至 **~280 秒**，体现极强的**大运量公交优先保障能力**
- ⚖️ **平衡性更好**：尽管部分基线（如 MADQN）在 AQL 上略优，但代价是严重牺牲公交利益；而 STDSH-MARL 在保持合理 AQL 的前提下显著提升人本指标。

### 消融实验结果（Ablation Study）
消融实验验证了各组件对性能的影响（以 ANP 为衡量）：

| 模型变体 | ANP (平均) | 变化率 |
|----------|------------|--------|
| 完整 STDSH-MARL （√HG, √DSHA, √SHE, √THE） | **1541.89** | —— |
| 移除 Temporal Hyperedge (THE) | 1765.38 | ↑ +14.5% |
| 移除 Hypergraph (HG) | 1728.64 | ↑ +12.1% |
| 移除 DSHA 模块 | 1684.13 | ↑ +9.2% |
| 移除 Spatial Hyperedge (SHE) | 1602.44 | ↑ +3.9% |

#### 关键发现：
- **Temporal Hyperedge (THE) 最重要**：移除后性能下降最严重，说明**时间维度建模**对捕捉动态交通演化至关重要。
- **DSHA 机制有效**：引入双阶段注意力使 ANP 下降约 **8.45%**，证明其在提取高阶依赖上的有效性。
- **超图结构必要**：直接移除 HG 导致性能骤降，凸显其相较于普通图表示的优势。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **人本化目标可行且必要**：通过将“延误乘客数”作为奖励函数，可有效引导 MARL 模型优先服务高载客交通工具（如公交、有轨电车），实现真正的**出行公平与社会效益最大化**。
2. ✅ **超图结构显著提升表示能力**：相比传统 GNN/GAT，**spatio-temporal hypergraph** 更适合建模交叉口间的复杂高阶互动，尤其在动态交通环境中表现突出。
3. ✅ **时间依赖性是性能关键驱动因素**：消融实验证明，**Temporal Hyperedges** 是提升性能的最重要组件，强调了对历史状态建模的重要性。
4. ✅ **STDSH-MARL 具备强鲁棒性与泛化能力**：在五种差异显著的交通场景下均保持领先，表明其对不同需求模式具有良好的适应性。

### 方法局限性
- 🔒 **依赖高质量多模态感知数据**：方法假设能准确获取各模式的乘客数（Occupant Counting），当前技术（如 Wi-Fi、Bluetooth、APC）仍有一定误差与覆盖率限制。
- 💾 **训练阶段需要全局信息**：虽然执行去中心化，但训练时需构建全局超图并训练 central critic，对通信与计算资源有一定要求。
- 🧩 **超参数敏感性未充分讨论**：如超边构造方式、注意力头数、权重系数等可能影响最终性能。

### 未来工作方向
1. **扩展至城市场景**：测试 STDSH-MARL 在数百个交叉口的城市级网络中的**可扩展性**（scalability）与协调能力。
2. **增强鲁棒性研究**：在**突发事件**（事故、临时封路）、**需求突变**、**传感器噪声**等现实扰动下评估模型稳定性。
3. **引入 Explainable DRL**：开发可解释机制，提供信号决策背后的逻辑依据，增强交通管理者信任，推动实际部署。
4. **拓展多目标优化**：纳入更多人本指标，如碳排放、行人安全风险、骑行舒适度等，构建综合评价体系。

--- 

> **总结**：  
> 本文提出的 **STDSH-MARL** 是首个将**时空超图结构**与**人本多模态优化**深度融合的 MARL 框架，在理论创新与实际效益之间取得了良好平衡。其实验证明，通过精细建模时空高阶依赖，并以“乘客延误”为核心目标，可显著提升公共交通优先水平与整体出行公平性，为下一代智慧城市交通控制提供了有力的技术路径。

</details>

---

### 15. [Powering Up Zeroth-Order Training via Subspace Gradient Orthogonalization](https://arxiv.org/abs/2602.17155)

**Authors**: Yicheng Lang, Changsheng Wang, Yihua Zhang, Mingyi Hong, Zheng Zhang, Wotao Yin, Sijia Liu  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.17155v1  

#### Abstract
Zeroth-order (ZO) optimization provides a gradient-free alternative to first-order (FO) methods by estimating gradients via finite differences of function evaluations, and has recently emerged as a memory-efficient paradigm for fine-tuning large-scale models by avoiding backpropagation. However, ZO ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Powering Up Zeroth-Order Training via Subspace Gradient Orthogonalization

## 1. 论文的主要贡献和创新点

### 解决的问题
零阶优化（Zeroth-Order, ZO）通过函数值的有限差分估计梯度，避免反向传播（backpropagation），从而显著降低大规模模型微调时的显存开销。然而，标准 ZO 方法面临**高方差梯度估计**与**查询效率低下**之间的根本矛盾：维度越高，梯度估计方差越大；为降方差而增加查询次数又导致计算成本上升，且常出现“多查询悖论”（multi-query paradox），即性能提升不明显甚至下降。

### 提出的新方法：ZO-Muon
本文提出了一种名为 **ZO-Muon** 的新型零阶优化框架，其核心思想是将两种互补机制统一于一个**子空间梯度正交化**（Subspace Gradient Orthogonalization）的框架中：

1. **投影式子空间 ZO 优化**（Projection-based Subspace ZO）  
   将原始参数空间投影到低维子空间，在该子空间内进行梯度估计。由于有效维度降低，梯度估计方差显著减小。该方法基于一个关键观察：深度模型训练中的权重更新具有内在的低秩结构。

2. **梯度正交化谱优化**（Gradient-Orthogonalized Spectral Optimization）  
   引入 Muon 优化器中的 **gradient orthogonalization (GO)** 技术，对子空间内的 ZO 梯度进行矩阵白化（matrix whitening），提取有意义的谱方向，抑制噪声方向的影响。这使得优化过程能更有效地利用谱结构信息。

**ZO-Muon** 是这两个原则的结合体：先在低维子空间用 Subspace RGE 估计梯度，再对其应用 msign 操作（即 GO），最后将更新映射回全参数空间。

### 相比现有方法的优势
- **准确性更高**：显著优于 MeZO、LOZO、HiZOO 等 SOTA ZO 方法。
- **查询效率更高**：达到相同性能所需查询数大幅减少（如仅需 MeZO 的 24.7% 查询量）。
- **运行时间更短**：收敛速度更快，总运行时间更少。
- **内存友好**：保持了 ZO 方法低显存占用的优点，未引入额外内存负担。
- **鲁棒性强**：对 batch size 变化不敏感，即使在小 batch 下仍表现优异。

---

## 2. 核心实验方法和设置

### 数据集
- **语言模型（LLMs）微调**：在 **SuperGLUE** 基准上测试，包括以下任务：
  - SST-2（情感分类）
  - RTE（文本蕴含）
  - CB（承诺银行）
  - BoolQ（自然的是/否问答）
  - WiC（词义消歧）
  - SQuAD（阅读理解）
- **视觉模型（ViTs）微调**：在 **CIFAR-10 / CIFAR-100** 图像分类数据集上测试。

### 模型
- **LLMs**: LLaMA3-8B, OPT-1.3B, OPT-13B, Gemma2-2B
- **ViTs**: ViT-B/16, ViT-L/16

### 基线方法对比
- **ZO 方法**：
  - MeZO（标准 ZO-SGD）
  - SparseMeZO (S-MeZO)
  - HiZOO（基于 Hessian 预条件）
  - LOZO（低秩扰动）
  - SubZero（双投影低秩 ZO）
  - Subspace-MeZO（本文提出的子空间变体，无 GO）
- **FO 方法**（作为性能上限参考）：
  - Adam
  - LoRA + Adam

### 评估指标
- **准确性**（Accuracy/F1 Score）
- **查询次数**（#Queries）
- **运行时间**（Runtime in seconds）
- **GPU 显存占用**（GPU Memory in MB）
- **收敛速度**（Training Loss vs. Runtime）

### 实验设置
- 所有 ZO 方法在相同查询预算下比较（如 40k queries）。
- ZO-Muon 超参数搜索范围：
  - 投影秩 $ r \in \{64, 128\} $
  - 查询数 $ N_q \in \{4, 8, 16\} $
- 投影矩阵 $ P $ 每 100 步懒惰更新（lazy resampling）。
- 使用 Newton-Schulz (NS) 迭代近似 msign 操作以提高效率。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 和正文）

| 任务 | 指标 | ZO-Muon | 最佳基线 | 提升幅度 |
|------|------|---------|----------|----------|
| **LLM: OPT-1.3B on SST-2** | Accuracy | **92.5%** | MeZO: 91.4% | +1.1% |
| **LLM: LLaMA3-8B on RTE** | Accuracy | **81.2%** | MeZO: 74.4% | +6.8% |
| **ViT: ViT-L on CIFAR-100** | Accuracy | **72.4%** | Subspace-MeZO: 64.7% | **+7.7%** |
| **ViT: ViT-B on CIFAR-100** | Accuracy | **72.6%** | MeZO: 64.5% | **+8.1%** |

### 与基线方法的对比结果
- **查询效率**：在 LLM 微调 SST-2 任务中，ZO-Muon 达到目标精度所需的查询数仅为 MeZO 的 **24.7%**。
- **运行时间**：在 OPT-13B 上微调 SST-2，ZO-Muon 仅需 **2h 33min**，比 MeZO（3h 37min）快 **~30%**（见 Table 2）。
- **收敛速度**：在多个任务上（如 Fig. 1, A3），ZO-Muon 收敛速度显著快于所有基线方法。
- **小 batch 鲁棒性**：在 ViT-B/CIFAR-100 上，当 batch size 从 256 降至 64 时：
  - MeZO 性能下降 **22.2%**
  - ZO-Muon 仅下降 **6.2%**
  - 且 ZO-Muon (bs=64) 仍优于 MeZO (bs=256)

### 消融实验结果
- **子空间 vs 全空间 GO**（Fig. 5, A1）：
  - 直接在全空间应用 GO（ZO-Muon-V0 或 JAGUAR-Muon）效果不佳，甚至不如 MeZO。
  - 表明 **子空间是 GO 在 ZO 中有效的前提**。
- **查询数 $ N_q $ 的影响**（Fig. A2）：
  - Subspace-MeZO 存在“多查询悖论”：$ N_q > 1 $ 时性能反而下降。
  - ZO-Muon 则受益于更多查询（$ N_q = 4 $ 优于 $ N_q = 1 $），说明 GO 能有效利用多查询信息。
- **投影秩 $ r $ 的选择**（Fig. 6）：
  - $ r = 64 $ 时性能最佳。
  - $ r $ 过小（如 16）会丢失信息；过大（如 256）会放大噪声，导致后期不稳定。
- **投影矩阵更新频率**（Fig. A4）：
  - 每步都更新（$ v=1 $）性能最差。
  - 懒惰更新（$ v=100 $ 或 $ 500 $）效果更好，表明稳定子空间有利于优化。

---

## 4. 关键结论和发现

### 主要发现
1. **子空间 + GO 是解决 ZO 方差-效率困境的有效路径**：将梯度估计限制在低维子空间可大幅降方差，而 GO 能进一步从噪声估计中提取有效谱方向。
2. **ZO-Muon 实现了“双赢”改进**：在准确性和查询/运行效率上同时超越现有方法，首次在 ZO 优化中实现二者兼顾。
3. **子空间设计至关重要**：使用列正交投影矩阵 $ P $ 赋予方法清晰的几何解释（估计投影梯度），优于 LOZO 等非正交设计。
4. **多查询悖论可被克服**：通过引入 GO，ZO-Muon 能有效聚合多查询信息，避免性能退化。

### 方法的局限性
- 当前工作聚焦于**参数高效微调**（PEFT），尚未扩展到大规模模型的**全量预训练**（full pre-training）。
- 方法依赖于子空间假设，若模型更新不具备低秩结构，性能可能受限。
- 使用 NS 近似 msign 虽然高效，但在大模型上与 SVD 的精度差距略有扩大（Fig. A5）。

### 未来工作方向
- 将 ZO-Muon 扩展至大规模模型的全量预训练。
- 探索自适应子空间选择或动态秩调整策略。
- 结合其他 FO 优化技术（如学习率调度、动量）进一步提升性能。
- 在更多模态（如多模态、语音）和应用场景（如强化学习）中验证方法通用性。

> **代码开源**：https://github.com/OPTML-Group/ZO-Muon

</details>

---

### 16. [RLGT: A reinforcement learning framework for extremal graph theory](https://arxiv.org/abs/2602.17276)

**Authors**: Ivan Damnjanovi\'c, Uro\v{s} Milivojevi\'c, Irena {\DJ}or{\dj}evi\'c, Dragan Stevanovi\'c  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.17276v1  

#### Abstract
Reinforcement learning (RL) is a subfield of machine learning that focuses on developing models that can autonomously learn optimal decision-making strategies over time. In a recent pioneering paper, Wagner demonstrated how the Deep Cross-Entropy RL method can be applied to tackle various problems f...

---

### 17. [Toward Trustworthy Evaluation of Sustainability Rating Methodologies: A Human-AI Collaborative Framework for Benchmark Dataset Construction](https://arxiv.org/abs/2602.17106)

**Authors**: Xiaoran Cai, Wang Yang, Xiyu Ren, Chekun Law, Rohit Sharma, Peng Qi  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.17106v1  

#### Abstract
Sustainability or ESG rating agencies use company disclosures and external data to produce scores or ratings that assess the environmental, social, and governance performance of a company. However, sustainability ratings across agencies for a single company vary widely, limiting their comparability,...

---

### 18. [Continual learning and refinement of causal models through dynamic predicate invention](https://arxiv.org/abs/2602.17217)

**Authors**: Enrique Crespo-Fernandez, Oliver Ray, Telmo de Menezes e Silva Filho, Peter Flach  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.17217v1  

#### Abstract
Efficiently navigating complex environments requires agents to internalize the underlying logic of their world, yet standard world modelling methods often struggle with sample inefficiency, lack of transparency, and poor scalability. We propose a framework for constructing symbolic causal world mode...

---

### 19. [RPDR: A Round-trip Prediction-Based Data Augmentation Framework for Long-Tail Question Answering](https://arxiv.org/abs/2602.17366)

**Authors**: Yiming Zhang, Siyue Zhang, Junbo Zhao, Chen Zhao  
**Category**: cs.CL  
**Published**: 2026-02-20  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.17366v1  

#### Abstract
Long-tail question answering presents significant challenges for large language models (LLMs) due to their limited ability to acquire and accurately recall less common knowledge. Retrieval-augmented generation (RAG) systems have shown great promise in mitigating this limitation by integrating extern...

---

### 20. [What Language is This? Ask Your Tokenizer](https://arxiv.org/abs/2602.17655)

**Authors**: Clara Meister, Ahmetcan Yavuz, Pietro Lesci, Tiago Pimentel  
**Category**: cs.CL  
**Published**: 2026-02-20  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.17655v1  

#### Abstract
Language Identification (LID) is an important component of many multilingual natural language processing pipelines, where it facilitates corpus curation, training data analysis, and cross-lingual evaluation of large language models. Despite near-perfect performance on high-resource languages, existi...

---

### 21. [Multi-Probe Zero Collision Hash (MPZCH): Mitigating Embedding Collisions and Enhancing Model Freshness in Large-Scale Recommenders](https://arxiv.org/abs/2602.17050)

**Authors**: Ziliang Zhao, Bi Xue, Emma Lin, Mengjiao Zhou, Kaustubh Vartak, Shakhzod Ali-Zade, Carson Lu, Tao Li, Bin Kuang, Rui Jian, Bin Wen, Dennis van der Staay, Yixin Bao, Eddy Li, Chao Deng, Songbin Liu, Qifan Wang, Kai Ren  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.17050v1  

#### Abstract
Embedding tables are critical components of large-scale recommendation systems, facilitating the efficient mapping of high-cardinality categorical features into dense vector representations. However, as the volume of unique IDs expands, traditional hash-based indexing methods suffer from collisions ...

---

### 22. [Node Learning: A Framework for Adaptive, Decentralised and Collaborative Network Edge AI](https://arxiv.org/abs/2602.16814)

**Authors**: Eiman Kanjo, Mustafa Aslanov  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16814v1  

#### Abstract
The expansion of AI toward the edge increasingly exposes the cost and fragility of cen- tralised intelligence. Data transmission, latency, energy consumption, and dependence on large data centres create bottlenecks that scale poorly across heterogeneous, mobile, and resource-constrained environments...

---

### 23. [All Leaks Count, Some Count More: Interpretable Temporal Contamination Detection in LLM Backtesting](https://arxiv.org/abs/2602.17234)

**Authors**: Zeyu Zhang, Ryan Chen, Bradly C. Stadie  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.17234v1  

#### Abstract
To evaluate whether LLMs can accurately predict future events, we need the ability to \textit{backtest} them on events that have already resolved. This requires models to reason only with information available at a specified past date. Yet LLMs may inadvertently leak post-cutoff knowledge encoded du...

---

### 24. [Visual Model Checking: Graph-Based Inference of Visual Routines for Image Retrieval](https://arxiv.org/abs/2602.17386)

**Authors**: Adri\`a Molina, Oriol Ramos Terrades, Josep Llad\'os  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.17386v1  

#### Abstract
Information retrieval lies at the foundation of the modern digital industry. While natural language search has seen dramatic progress in recent years largely driven by embedding-based models and large-scale pretraining, the field still faces significant challenges. Specifically, queries that involve...

---

### 25. [AutoNumerics: An Autonomous, PDE-Agnostic Multi-Agent Pipeline for Scientific Computing](https://arxiv.org/abs/2602.17607)

**Authors**: Jianda Du, Youran Sun, Haizhao Yang  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.17607v1  

#### Abstract
PDEs are central to scientific and engineering modeling, yet designing accurate numerical solvers typically requires substantial mathematical expertise and manual tuning. Recent neural network-based approaches improve flexibility but often demand high computational cost and suffer from limited inter...

---

### 26. [Trivance: Latency-Optimal AllReduce by Shortcutting Multiport Networks](https://arxiv.org/abs/2602.17254)

**Authors**: Anton Juerss, Vamsi Addanki, Stefan Schmid  
**Category**: cs.DC  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.17254v1  

#### Abstract
AllReduce is a fundamental collective operation in distributed computing and a key performance bottleneck for large-scale training and inference. Its completion time is determined by the number of communication steps, which dominates latency-sensitive workloads, and the communication distance affect...

---

### 27. [TopoFlow: Physics-guided Neural Networks for high-resolution air quality prediction](https://arxiv.org/abs/2602.16821)

**Authors**: Ammar Kheder, Helmi Toropainen, Wenqing Peng, Samuel Ant\~ao, Jia Chen, Zhi-Song Liu, Michael Boy  
**Category**: cs.LG  
**Published**: 2026-02-20  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16821v1  

#### Abstract
We propose TopoFlow (Topography-aware pollutant Flow learning), a physics-guided neural network for efficient, high-resolution air quality prediction. To explicitly embed physical processes into the learning framework, we identify two critical factors governing pollutant dynamics: topography and win...

---

### 28. [IntentCUA: Learning Intent-level Representations for Skill Abstraction and Multi-Agent Planning in Computer-Use Agents](https://arxiv.org/abs/2602.17049)

**Authors**: Seoyoung Lee, Seobin Yoon, Seongbeen Lee, Yoojung Chun, Dayoung Park, Doyeon Kim, Joo Yong Sim  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.17049v1  

#### Abstract
Computer-use agents operate over long horizons under noisy perception, multi-window contexts, evolving environment states. Existing approaches, from RL-based planners to trajectory retrieval, often drift from user intent and repeatedly solve routine subproblems, leading to error accumulation and ine...

---

### 29. [Mechanistic Interpretability of Cognitive Complexity in LLMs via Linear Probing using Bloom's Taxonomy](https://arxiv.org/abs/2602.17229)

**Authors**: Bianca Raimondi, Maurizio Gabbrielli  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.17229v1  

#### Abstract
The black-box nature of Large Language Models necessitates novel evaluation frameworks that transcend surface-level performance metrics. This study investigates the internal neural representations of cognitive complexity using Bloom's Taxonomy as a hierarchical lens. By analyzing high-dimensional ac...

---

### 30. [A Privacy by Design Framework for Large Language Model-Based Applications for Children](https://arxiv.org/abs/2602.17418)

**Authors**: Diana Addae, Diana Rogachova, Nafiseh Kahani, Masoud Barati, Michael Christensen, Chen Zhou  
**Category**: cs.AI  
**Published**: 2026-02-20  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.17418v1  

#### Abstract
Children are increasingly using technologies powered by Artificial Intelligence (AI). However, there are growing concerns about privacy risks, particularly for children. Although existing privacy regulations require companies and organizations to implement protections, doing so can be challenging in...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
