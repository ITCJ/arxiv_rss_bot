# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-13 05:55:59 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ARCQuant: Boosting NVFP4 Quantization with Augmented Residual Channels for LLMs](https://arxiv.org/abs/2601.07475)

**Authors**: Haoqian Meng, Yilun Luo, Yafei Zhao, Wenyuan Liu, Peng Zhang, Xindian Ma  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2601.07475v1  

#### Abstract
The emergence of fine-grained numerical formats like NVFP4 presents new opportunities for efficient Large Language Model (LLM) inference. However, it is difficult to adapt existing Post-Training Quantization (PTQ) strategies to these formats: rotation-based methods compromise fine-grained block isol...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ARCQuant: Boosting NVFP4 Quantization with Augmented Residual Channels for LLMs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前在大型语言模型（LLM）推理中，**细粒度数值格式（如 NVFP4）** 虽然能提升效率，但现有的后训练量化（PTQ）策略难以有效适配这些格式，存在以下三大挑战：
- **Rotation-based 方法**（如 QuaRot）会破坏 NVFP4 的块级隔离特性，导致局部动态范围扩大；
- **Smoothing 技术**（如 SmoothQuant）在 4-bit 量化下误差显著，补偿能力有限；
- **Mixed-precision 方法**（如 Atom）因硬件不支持混合精度计算（如不同 block size），无法高效利用 Tensor Core。

### 提出的新方法：ARCQuant
提出 **ARCQuant**（Augmented Residual Channels Quantization），一种专为 NVFP4 设计的 PTQ 框架，其核心思想是：
- **通过增广残差通道进行误差补偿**，而非改变输入分布或引入混合精度。
- 在激活矩阵中识别出异常值通道（outlier channels），并将其量化后的残差作为额外通道“增广”到原始输入中。
- 整个补偿过程被映射到矩阵乘法（GEMM）的扩展 reduction 维度中，从而可直接调用标准、高度优化的 GEMM 内核（如 CUTLASS）。

### 相比现有方法的优势
- ✅ **保持硬件统一性**：全程使用统一的 W4A4 NVFP4 格式，兼容 Blackwell 架构的 Tensor Core；
- ✅ **保留块级隔离**：通过通道重排序（channel reordering）隔离异常值，避免旋转操作带来的误差扩散；
- ✅ **理论误差界优**：双阶段量化机制的最坏情况误差界与标准 MXFP8 相当；
- ✅ **部署高效**：通过融合内核（Fused Quantization Kernel）实现低开销在线量化，无需修改 GEMM 内核。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **校准数据集**：WikiText2（128 个样本，序列长度 2048）
- **评估任务与数据集**：
  - **通用理解**：ARC-C, HellaSwag, PIQA, Winograd, LAMBADA
  - **知识推理**：MMLU（5-shot）
  - **代码生成**：HumanEval, MBPP
  - **数学推理**：GSM8K, CMATH
  - **语言建模**：WikiText2 上的 Perplexity (PPL)

### 实验设置
- **模型**：LLaMA 3.1-8B、Qwen2.5-7B/14B/32B、Qwen2.5-Coder-7B-Instruct、Qwen2.5-Math-7B-Instruct
- **量化配置**：W4A4（权重和激活均为 4-bit）
- **硬件平台**：NVIDIA RTX 5090 和 RTX PRO 6000（基于 Blackwell 架构）
- **评估指标**：
  - 准确率（Zero-shot / Few-shot）
  - 困惑度（PPL）
  - 推理延迟（Prefill Latency）
  - 内存占用
  - 速度提升（Speedup）

### 基线方法对比
| 方法 | 类型 | 是否适配 NVFP4 |
|------|------|----------------|
| FP16 | 全精度基线 | — |
| W4A8 + RTN | 高精度激活基线 | — |
| INT4 / MXFP4 + RTN | 基础量化 | 是 |
| SmoothQuant | 平滑变换 | 适配 |
| QuaRot (Hadamard) | 旋转变换 | 适配 |
| FlatQuant | 分布扁平化 | 不兼容 |
| Atom | 混合精度（保留部分高精度通道） | 不兼容 |

> 注：FlatQuant 和 Atom 因结构不兼容 NVFP4，未直接比较；文中对 SmoothQuant 和 QuaRot 进行了 NVFP4 适配版本测试。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### 在 Llama3.1-8B 上的表现：
| 方法 | PPL ↓ | MMLU ↑ | 平均准确率 ↑ |
|------|--------|--------|--------------|
| FP16 | 6.24 | 65.15 | 72.56 |
| W4A8 + RTN | 7.07 | 61.08 | 70.59 |
| NVFP4 + RTN | 6.95 | 61.64 | 70.45 |
| **ARCQuant** | **6.87** | **62.61** | **70.90** |

> ✅ **优于所有 W4A4 方法，甚至接近 W4A8 性能**

#### 在 Qwen2.5-7B 上的表现：
| 方法 | PPL ↓ | MMLU ↑ | 平均准确率 ↑ |
|------|--------|--------|--------------|
| FP16 | 6.85 | 74.16 | 70.97 |
| Atom | 8.96 | 68.17 | 67.57 |
| NVFP4 + RTN | 7.29 | 72.06 | 69.43 |
| **ARCQuant** | **7.28** | **72.84** | **70.28** |

> ✅ **相比 Atom 降低 PPL 1.68，显著缩小与 FP16 差距**

#### 代码生成性能（Qwen2.5-Coder-7B-Instruct）：
| 方法 | HumanEval | HumanEval+ | MBPP | MBPP+ |
|------|-----------|------------|-------|--------|
| FP16 | 84.1 | 79.9 | 80.4 | 67.2 |
| Atom | 80.5 | 76.2 | 74.5 | 63.2 |
| **ARCQuant** | **86.0** | **79.3** | **79.9** | **68.3** |

> ✅ **全面超越 FP16，尤其在 HumanEval 上提升明显**

### 与基线方法的对比结果
- **相比 NVFP4 + RTN**：ARCQuant 在多个任务上平均提升约 0.5–1.0 个百分点；
- **相比 NVFP4 + SmoothQuant / QuaRot**：
  - QuaRot 因 Hadamard 变换破坏块隔离，在 Llama 上表现退化；
  - SmoothQuant 改善有限；
  - **ARCQuant 显著优于两者**；
- **相比混合精度方法（Atom）**：
  - Atom 因硬件不兼容导致吞吐下降；
  - ARCQuant 在精度和速度上均占优。

### 消融实验结果
#### （1）泛化性（Generalizability）
- 在 **INT4** 和 **MXFP4** 格式上也有效：
  - 在 Llama3.1-8B 上，INT4 下 PPL 从 8.84 → 7.95，零样本准确率提升 2.12%；
  - 表明残差补偿机制具有跨格式有效性。

#### （2）校准鲁棒性（Calibration Robustness）
- 使用不同校准数据集（WikiText2 / C4 / HumanEval）时：
  - PPL 波动 < 0.03，准确率波动 < 0.03%；
  - 表明异常值结构稳定，方法对校准数据选择不敏感。

#### （3）效率分析（Efficiency）
- **GEMM 延迟随增广通道数 $S$ 线性增长**，但在典型范围（$S \leq 512$）内开销极小；
- **端到端总延迟仅增加 4.9%**（Qwen2.5-7B）；
- **内存减少 1.5×–2.8×**，**预填充速度提升达 3.5×**（RTX 5090 上 Llama3.1-8B）；
- 相比 W4A8 和 MXFP8，仍具显著速度优势。

---

## 4. 关键结论和发现

### 主要发现
- ✅ **NVFP4 的块级隔离特性至关重要**，全局变换（如旋转）反而损害性能；
- ✅ **混合精度虽有效，但受限于硬件统一性要求**，难以发挥实际加速潜力；
- ✅ **通过增广残差通道实现误差补偿**，可在不违反硬件约束的前提下逼近 W4A8 精度；
- ✅ **理论误差界分析表明**，ARCQuant 的最坏误差与 MXFP8 相当，解释了其高保真重建能力；
- ✅ **部署友好**：通过融合内核将复杂量化逻辑封装，复用标准 GEMM，便于集成至 vLLM 等系统。

### 方法的局限性
1. **依赖离线校准**：
   - 通道重排序和异常值数量 $S$ 由校准数据决定，假设推理时分布一致；
   - 对极端分布偏移可能适应性不足。

2. **硬件依赖性强**：
   - 当前设计紧密耦合 NVIDIA Blackwell 架构的 NVFP4 支持；
   - 在无 block-scaled 加速的老架构上仅为模拟性能。

3. **权重量化较简单**：
   - 权重采用 RTN 量化，未结合 GPTQ 或 AWQ 等高级方法；
   - 若结合可进一步提升子 4-bit 场景下的性能。

### 未来工作方向
- 将 ARCQuant 扩展至 **sub-4-bit 格式**（如 INT2/NVFP2）；
- 探索 **动态自适应 $S$** 机制以应对分布变化；
- 集成至 **vLLM 等生产级推理引擎**，验证大规模服务场景下的实用性；
- 推动 **硬件层面支持更灵活的 block-scaled 格式**，增强通用性。

---

> **总结一句话**：  
> ARCQuant 提出了一种硬件友好的“用计算维度换精度”的新范式，在严格遵守 W4A4 统一精度的前提下，通过增广残差通道实现了接近 W4A8 的精度，并在 Blackwell 架构上获得高达 3.5× 的推理加速，为 LLM 低比特量化提供了可扩展、高效且实用的解决方案。

</details>

---

### 2. [TALON: Confidence-Aware Speculative Decoding with Adaptive Token Trees](https://arxiv.org/abs/2601.07353)

**Authors**: Tianyu Liu, Qitan Lv, Yuhao Shen, Xiao Sun, Xiaoyan Sun  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.07353v1  

#### Abstract
Speculative decoding (SD) has become a standard technique for accelerating LLM inference without sacrificing output quality. Recent advances in speculative decoding have shifted from sequential chain-based drafting to tree-structured generation, where the draft model constructs a tree of candidate t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TALON: Confidence-Aware Speculative Decoding with Adaptive Token Trees

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **tree-based speculative decoding**（如 EAGLE）方法通常采用固定宽度（fixed width）和固定深度（fixed depth）的 **静态 draft tree 结构**。这种刚性设计存在以下问题：
- 在确定性上下文（低熵）中，模型已高度自信，但仍强制扩展 `K` 个子节点，造成 **过度探索（over-exploration）** 和计算浪费。
- 在不确定性上下文（高熵）中，top-K 候选不足以覆盖目标分布，导致 **探索不足（under-exploration）**，且仍会生成长而无效的分支。
- 固定深度限制了在简单任务中的潜在加速上限，而在困难任务中又浪费资源于注定被拒绝的路径。

### 提出的新方法
作者提出 **TALON**，一种**无需训练、基于预算驱动的自适应树扩展框架**（budget-driven adaptive tree expansion），其核心思想是：
- **以全局 token 预算 `N` 为约束**，而非固定宽度/深度。
- 采用 **混合扩展策略（hybrid expansion strategy）**：
  - **第0层（根层）**：使用 **Top-K 初始化**，确保鲁棒性，避免因 draft model 初始过自信（over-confidence）而导致整棵树错误。
  - **后续层（d ≥ 1）**：采用 **置信度过滤机制（confidence-gated expansion）**，仅保留概率不低于锚点概率（anchor confidence）`μ` 倍的候选节点（即 `p(u) ≥ μ · max_p`）。

### 相比现有方法的优势
- **动态拓扑适应性**：自动形成“**深而窄**”（deep-and-narrow）结构用于确定性上下文，或“**浅而宽**”（shallow-and-wide）结构用于不确定性上下文。
- **更高的资源利用率**：避免了静态方法中“先扩展后剪枝”（expand-then-shrink）带来的冗余节点生成。
- **无需额外训练**：完全基于推理时的实时置信度决策，可直接集成到现有 tree-based SD 框架中。
- **更优的性价比**：通过动态调整 draft 成本（draft efficiency），在不同难度任务上均能保持高效加速。

---

## 2. 核心实验方法和设置

### 数据集
在 **6 个多样化基准** 上进行评估，涵盖多种任务类型：
- **指令遵循**：Alpaca
- **数学推理**：GSM8K
- **代码生成**：HumanEval
- **多轮对话**：MT-Bench
- **问答**：QA (Natural Questions)
- **摘要**：CNN/DM

### 实验设置
- **模型**：在 **5 种主流 LLM** 上测试，包括：
  - Llama-3.1-8B-Instruct
  - Qwen3-8B / 32B
  - DeepSeek-R1-Distill-LLaMA-8B (DSL-8B)
  - Vicuna-13B
- **硬件**：单张 NVIDIA H200 (140GB) GPU，batch size = 1。
- **预算设置**：全局 token 预算 `N = 60`，TALON 的置信度阈值 `μ = 0.03`。
- **基线方法**：
  - **链式方法**：Standard SD, MEDUSA, HYDRA
  - **树式方法**：EAGLE-3 (SOTA), OPT-Tree
- **评估指标**：
  - **Mean Accepted Tokens (MAT)**：每轮验证平均接受的 draft tokens 数量。
  - **Wall-time Speedup (Spd.)**：相对于标准自回归解码的实际端到端加速比。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- TALON 在所有 5 个模型和 6 个数据集上 **一致优于 EAGLE-3**。
- 最高达到 **5.16× 的端到端加速**（Vicuna-13B + HumanEval），显著高于 EAGLE-3 的 4.77×。
- 在 CNN/DM 上实现 **2.30× 加速**（Qwen3-8B），优于 EAGLE-3 的 1.95×。

### 与基线方法的对比结果
| 模型 | 方法 | HumanEval (Spd.) | GSM8K (Spd.) |
|------|------|------------------|--------------|
| Vicuna-13B | EAGLE-3 | 4.77× | 3.87× |
| Vicuna-13B | **TALON** | **5.16×** | **3.99×** |
| Qwen3-8B | EAGLE-3 | 2.35× | 2.37× |
| Qwen3-8B | **TALON** | **2.69×** | **2.67×** |

> ✅ **Universal Speedup**：TALON 在所有场景下均超越 EAGLE-3。
> ✅ **Reasoning Adaptability**：在数学和代码等低熵任务上优势尤为明显，说明其能有效捕捉确定性路径并延长推测长度。
> ✅ **Robustness**：在 `T=1` 的随机采样设置下，TALON 仍保持优势（如 Qwen3-8B HumanEval 达 2.51× vs EAGLE-3 的 2.20×）。

### 消融实验结果
#### （1）鲁棒初始化层数消融（Top-K 层数）
- **k=0**（无初始化）：速度下降（如 Llama3-8B 从 263.8 → 261.5 tok/s），验证了根层过自信风险。
- **k≥2**（过多初始化）：延迟进入置信度过滤阶段，增加开销。
- **k=1**（TALON 默认）：在所有任务上表现最优，证明 **单层 Top-K 初始化 + 后续置信度过滤** 是最佳平衡。

#### （2）预算敏感性分析
- 在不同预算 `N ∈ [32, 96]` 下，TALON 始终优于 EAGLE-3。
- 在小预算（N=32）下优势更显著，说明 TALON 能更高效地优先分配资源给高价值路径。

#### （3）置信度阈值 `μ` 敏感性
- `μ` 控制探索与利用的权衡：
  - 较大 `μ` → 更严格过滤 → “深而窄” → 适合高对齐模型（如 DSL-8B）。
  - 较小 `μ` → 更宽松过滤 → “浅而宽” → 适合低对齐模型（如 Qwen3-8B）。
- 实验表明，**最优 `μ` 与 draft-target 对齐程度正相关**。

---

## 4. 关键结论和发现

### 主要发现
- **静态树结构存在系统性效率瓶颈**：“Acceptance Funnel” 现象表明深层节点接受集中在 top-1/2，宽扩展无效；“Static Depth Dilemma” 表明固定深度无法适应任务难度波动。
- **TALON 通过预算驱动 + 置信度过滤，实现了真正的动态适应性**，其 draft efficiency 曲线紧贴理论最优（Oracle），显著减少计算浪费。
- **混合策略至关重要**：根层 Top-K 保证鲁棒性，后续层置信度过滤实现高效自适应。

### 方法的局限性
- **批处理扩展性有限**：当前评估聚焦于 `batch_size=1` 的低延迟场景，在大批量吞吐场景下，维护多样化的动态树结构可能带来非平凡的内存和调度开销。
- **超参数泛化性**：虽然 `μ=0.03`, `N=60` 在多数情况下表现稳健，但在特定领域可能需要调优。缺乏自动调参机制。
- **未解决 draft model 本身的质量问题**：TALON 依赖现有 draft model，若其校准差或能力弱，仍会影响整体性能。

### 未来工作方向
- 开发适用于 **大批量服务** 的高效动态树管理机制。
- 设计 **在线自动调参机制**，根据历史接受率动态调整 `μ` 和 `N`。
- 探索将 TALON 与其他技术（如 retrieval-based drafting 或 early-exit）结合，进一步提升 draft 质量与效率。

---

> **总结**：TALON 提出了一种简洁而强大的 **training-free、budget-driven 自适应推测框架**，通过 **混合扩展策略** 实现了对上下文不确定性的动态响应，在多个模型和任务上显著超越 SOTA 方法，为高效 LLM 推理提供了新的范式。

</details>

---

### 3. [d3LLM: Ultra-Fast Diffusion LLM using Pseudo-Trajectory Distillation](https://arxiv.org/abs/2601.07568)

**Authors**: Yu-Yang Qian, Junda Su, Lanxiang Hu, Peiyuan Zhang, Zhijie Deng, Peng Zhao, Hao Zhang  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.07568v1  

#### Abstract
Diffusion large language models (dLLMs) offer capabilities beyond those of autoregressive (AR) LLMs, such as parallel decoding and random-order generation. However, realizing these benefits in practice is non-trivial, as dLLMs inherently face an accuracy-parallelism trade-off. Despite increasing int...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：d3LLM: Ultra-Fast Diffusion LLM using Pseudo-Trajectory Distillation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Diffusion Large Language Models (dLLMs)** 面临一个根本性的 **accuracy-parallelism trade-off**（准确率-并行度权衡）：
- 追求高并行解码（如多块同时生成）会因错误传播导致准确率下降；
- 而保持高准确率则往往牺牲了解码速度，限制了其在实际中的高效应用。

此外，传统训练方式采用随机掩码（random masking），缺乏对“哪些token可以早期安全解码”的指导；推理阶段的多块解码也容易因上下文不完整而降低质量。

### 提出的新方法：d3LLM 框架
作者提出 **d3LLM**（*pseuDo-Distilled Diffusion LLM*），通过以下两个核心机制，在训练与推理阶段协同优化准确率与并行度：

#### （1）训练阶段：Pseudo-Trajectory Distillation（伪轨迹蒸馏）
- 利用教师 dLLM 自身的解码过程（即使最终输出不完全正确）作为“伪轨迹”（pseudo-trajectory），提供中间监督信号。
- 将该轨迹与真实标签结合，构建带噪声的输入序列，训练学生模型学习更合理的 unmasking 顺序。
- 引入 **curriculum learning** 策略：
  - **渐进式噪声调度**：mask ratio 从 0.0 逐步增加到 0.8；
  - **渐进式窗口大小**：解码窗口长度从 16 增加到 32。
- 优势：使模型学会在早期自信地解码高置信度 token，提升并行潜力。

#### （2）推理阶段：Entropy-Based Multi-Block Decoding + KV-Cache Refresh
- **基于熵的多块并行解码**：选择低熵（高置信）token优先解码，支持跨多个 block 并行生成。
- **KV-Cache 刷新机制**：
  - 新 block 完成后延迟缓存 KV 状态；
  - 在“稳定中”状态进行全前向传播刷新历史 KV 缓存，防止误差累积。
- **早停机制**：一旦生成 EOS token 即终止解码，减少冗余计算。

### 相比现有方法的优势
| 维度 | d3LLM | 其他方法（如 Fast-dLLM, D2F, dParallel） |
|------|-------|----------------------------------------|
| **训练指导** | 显式引入 teacher 的解码顺序作为监督 | 依赖随机掩码或简单蒸馏，无顺序引导 |
| **并行能力** | 支持跨 block 多步并行解码 | 多为单 block 或受限并行 |
| **准确性保障** | KV-cache 刷新 + entropy 控制 | 易受错误传播影响 |
| **综合性能** | 在 accuracy 和 parallelism 之间取得更好平衡 | 往往偏向一侧（快但不准 / 准但不快） |

---

## 2. 核心实验方法和设置

### 使用的数据集
在五个代表性任务上评估：
- **GSM8K-CoT**：数学推理（零样本）
- **MATH**：竞赛级数学题（四样本）
- **HumanEval**：代码生成（零样本）
- **MBPP**：Python 编程任务（三样本）
- **Long-GSM8K**：长上下文数学推理（五样本，prompt ~1000 tokens）

### 实验设置
- **基础模型**：
  - LLaDA (8B)
  - Dream (7B)
  - Dream-Coder (7B)
- **衍生模型**：
  - d3LLM-LLaDA
  - d3LLM-Dream
  - d3LLM-Coder
- **训练细节**：
  - 使用 LoRA 微调，rank=256
  - 学习率 2e-5，AdamW 优化器
  - 批大小 64（梯度累积）
  - 在 H100 上训练，bfloat16 精度
- **推理配置**：
  - 最大生成长度 256/512
  - 贪婪解码（temperature=0.0~0.1）
  - Block size 固定为 32

### 评估指标
- **Tokens Per Forward (TPF)**：衡量并行度（算法层面效率）
- **Accuracy (Pass@1 / Solve Rate)**：生成质量
- **AUP (Accuracy Under Parallelism)**：**本文提出的新指标**，综合衡量 accuracy 与 parallelism 的联合表现
  - 定义为 accuracy-parallelism 曲线下加权面积
  - 权重函数 $ W(y) = \min(e^{-(1-y/y_{\text{max}})}, 1) $，惩罚低准确区域
  - 设定最小阈值避免无效高速区干扰评分

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Vanilla LLaDA / Dream** | 原始 dLLM | 从头训练或 AR 初始化 |
| **Fast-dLLM** | 推理加速 | 无需训练，KV-cache + confidence-aware decoding |
| **Fast-dLLM-v2** | AR 转 dLLM | 微调 AR 模型为 block diffusion |
| **dParallel** | 蒸馏方法 | Certainty-forcing distillation 提升并行性 |
| **D2F** | 混合架构 | AR-diffusion hybrid，支持跨块并行 |
| **EAGLE-3** | Speculative Decoding | 使用 draft model 加速 AR 模型（用于上限比较） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 d3LLM-LLaDA 为例）

| 方法 | TPF | Acc (%) | AUP Score |
|------|-----|---------|-----------|
| LLaDA | 1.00 | 72.6 | 72.6 |
| Fast-dLLM-LLaDA | 2.77 | 74.7 | 205.8 |
| D2F-LLaDA | 2.88 | 73.2 | 209.7 |
| dParallel-LLaDA | 5.14 | 72.6 | 358.1 |
| **d3LLM-LLaDA** | **9.11** | **73.1** | **637.7** ✅ |

> 在 GSM8K-CoT 上，AUP 提升超过 **77%** 相比次优方法。

### 与其他方法的整体对比
- **AUP 表现**：
  - d3LLM 在 **10项测试任务中的9项** 取得最高 AUP 分数。
  - 在 MATH、HumanEval、Long-GSM8K 等复杂任务上显著领先。
- **吞吐量（TPS）提升**：
  | 方法 | H100 TPS | Speedup vs AR |
  |------|----------|----------------|
  | Qwen-2.5-7B-it (AR) | 57.3 | 1.0× |
  | d3LLM-LLaDA | **288.9** | **5.0×** |
  | d3LLM-Dream | **235.3** | **4.1×** |
  | Vanilla LLaDA | 27.9 | 0.5× |
  > 实现 **高达 10× 相比原始 dLLM 的速度提升**，且精度几乎无损。

- **硬件无关性验证**：
  - AUP 基于 TPF（而非 TPS），在不同 GPU（H100/A100）间更具可比性。
  - d3LLM 在 A100 上仍实现 **3.6× 速度提升**，证明其算法优势不受限于特定硬件。

### 消融实验结果（Ablation Study）

#### 蒸馏策略消融（d3LLM-LLaDA on GSM8K-CoT）
| 配置 | TPF | Acc (%) | AUP |
|------|-----|---------|-----|
| Baseline（仅 multi-block + early stop） | 6.41 | 72.2 | 441.4 |
| + Pseudo-Trajectory | 7.55 | 72.1 | 517.7 |
| + Curriculum Noise | 8.46 | 69.8 | 551.3 |
| **Full（+ Curriculum Window）** | **9.11** | **73.1** | **637.7** |

✅ 结论：所有组件均有贡献，尤其是 curriculum window 显著恢复并提升 accuracy。

#### 解码策略消融
| 配置 | TPF | Acc (%) | AUP |
|------|-----|---------|-----|
| Vanilla Block Diffusion | 7.01 | 73.2 | 492.9 |
| + Multi-block Decoding | 9.07 | 73.1 | 635.0 |
| **+ Early Stopping** | **9.11** | **73.1** | **637.7** |

✅ 结论：multi-block decoding 是 TPF 提升主因，early stopping 进一步优化效率。

#### 超参数分析
- **Curriculum Noise Level**：从固定 noise 到 `0.0→0.8`，AUP 提升 **22.2%**
- **Curriculum Window Size**：从固定 k=32 到 `16→32`，AUP 提升 **19.0%**

---

## 4. 关键结论和发现

### 主要发现
1. **dLLMs 存在固有的 accuracy-parallelism trade-off**，现有方法难以兼顾两者。
2. **Pseudo-Trajectory Distillation** 能有效传递 teacher 的解码偏好，显著提升学生模型的并行解码能力。
3. **Multi-block decoding + KV-refresh** 可缓解误差传播问题，在高并行下维持高质量生成。
4. **AUP 是一个更公平、更有意义的评估指标**，能反映方法在“提速不降质”方面的真正能力。
5. d3LLM 实现了 **5× 以上相比 AR 模型的速度提升**，以及 **10× 相比原始 dLLM 的加速**，同时保持竞争力的 accuracy。

### 方法的局限性
- 当前框架仍基于已有 dLLM 架构（如 LLaDA/Dream），未探索更强的基础模型（如 LLaDA 2.0）上的极限性能。
- 推理时需维护复杂的 KV-cache 刷新逻辑，系统实现复杂度较高。
- 多块并行依赖 entropy 判断，极端情况下可能误判导致错误扩散。
- 所有实验基于 HuggingFace Transformers 后端，未集成 vLLM 等高性能推理引擎。

### 未来工作方向
1. **结合 Speculative Decoding**：将 d3LLM 作为 draft model 或 verifier，与 EAGLE 等框架融合，进一步突破性能边界。
2. **应用于更强的 dLLM 基座**：如 ReFusion、LLaDA 2.0 等，验证通用性和扩展性。
3. **系统级优化**：
   - GPU kernel fusion 针对 multi-block pattern 优化；
   - 实现 paged attention 支持 bidirectional attention；
   - 更高效的 KV-cache 管理策略。
4. **引入强化学习**：利用 trajectory-aware RL（如 TraDo）进一步优化解码路径选择。

---

> ✅ **总结一句话**：  
> d3LLM 通过 **伪轨迹蒸馏 + 多块熵控解码 + KV刷新机制**，首次在开源 dLLM 上实现了接近闭源模型的高效推理性能，在 **速度、准确率、通用性** 三者之间取得了卓越平衡。

</details>

---

### 4. [TimeGNN-Augmented Hybrid-Action MARL for Fine-Grained Task Partitioning and Energy-Aware Offloading in MEC](https://arxiv.org/abs/2601.06191)

**Authors**: Wei Ai, Yun Peng, Yuntao Shou, Tao Meng, Keqin Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.06191v1  

#### Abstract
With the rapid growth of IoT devices and latency-sensitive applications, the demand for both real-time and energy-efficient computing has surged, placing significant pressure on traditional cloud computing architectures. Mobile edge computing (MEC), an emerging paradigm, effectively alleviates the l...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**TimeGNN-Augmented Hybrid-Action MARL for Fine-Grained Task Partitioning and Energy-Aware Offloading in MEC**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对 **Mobile Edge Computing (MEC)** 系统中以下关键挑战：
- **能源受限**：边缘服务器依赖电池供电，能量预算有限，频繁通信加剧能耗；
- **动态环境**：任务负载、信道状态和用户移动性高度时变，导致系统状态不稳定；
- **高交互开销**：传统多智能体强化学习（MARL）需频繁获取实时状态，增加通信负担；
- **细粒度决策需求**：需要同时优化任务分割比例、传输功率、计算资源分配等混合动作。

这些问题使得现有方法在能效、延迟和可扩展性方面表现不佳。

---

### ✅ 提出的新方法与创新思路

作者提出了一种名为 **TG-DCMADDPG** 的新型框架，其核心创新包括：

#### （1）**TimeGNN 增强的状态预测机制**
- 引入 **Temporal Graph Neural Network (TimeGNN)** 对多个边缘服务器的多维状态（如CPU负载、剩余能量、带宽）进行建模；
- 利用历史状态序列预测未来 △ 步的服务器状态，实现**前瞻性决策**；
- 显著减少智能体与服务器之间的在线交互频率，降低通信开销。

> 🔍 创新点：将 TimeGNN 首次应用于 MEC 中的**跨节点时空状态预测**，提升策略的预见性和稳定性。

#### （2）**离散-连续混合动作空间的 DC-MADDPG 算法**
- 设计了一个基于 **Centralized Training with Decentralized Execution (CTDE)** 架构的 MARL 框架；
- 动作空间为混合型（hybrid action space）：
  - **离散动作**：通过 Gumbel-Softmax 实现服务器选择（one-hot 编码）；
  - **连续动作**：控制任务分割比例（task splitting ratio）和传输功率（transmission power）；
- 使用 Deterministic Policy Gradient 进行高效训练。

> 🔍 创新点：支持**联合优化任务分片、资源调度与功耗控制**，实现更精细的任务卸载策略。

#### （3）**预测增强型协同计算框架**
- 将预测模块输出（预测状态 $ \hat{s}_{t+\Delta} $）与当前观测拼接成增强观测 $ o_n = [o_n^{\text{curr}}, \hat{s}_{t+\Delta}] $；
- 使各移动设备（MD）可在不频繁查询的情况下做出**主动式卸载决策**。

---

### ✅ 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **收敛速度** | 引入 TimeGNN 后显著加快策略收敛（约60轮达最优） |
| **能效表现** | 能量消耗最低（稳定在~260单位），优于所有基线 |
| **延迟控制** | 执行延迟最低（~24单位），尤其在大规模网络中优势明显 |
| **任务完成率** | 达到 **98%以上**，最高达 **99.6%**（15个服务器场景） |
| **可扩展性** | 在3–15个边缘服务器配置下均保持高性能，具备良好鲁棒性 |

---

## 2. 核心实验方法和设置

### ✅ 数据集与仿真环境
- **非真实数据集**，采用**自定义模拟器**构建动态 MEC 环境；
- 移动设备随机分布并周期生成任务；
- 边缘服务器数量可调（3 / 5 / 10 / 15），模拟不同规模网络；
- 无线信道、位置、任务到达过程具有时间随机性。

---

### ✅ 实验设置

| 参数 | 设置 |
|------|------|
| 时间槽长度 | △ = 1（每个episode含10个slot） |
| 总训练轮数 | 2000 episodes |
| 更新频率 | Actor每100步更新一次 |
| 学习率 | Actor: $1\times10^{-3}$, Critic: $1\times10^{-2}$ |
| 目标网络软更新因子 $\tau$ | 0.01 |
| TimeGNN 输入历史步长 $K_{\text{hist}}$ | 5 |
| 预测步长 $\Delta$ | 1 |
| 图神经网络层数 | 3层 GraphSAGE 结构 |
| 硬件平台 | NVIDIA RTX 4060 GPU, PyTorch 1.8.1 |

---

### ✅ 评估指标

| 指标 | 描述 |
|------|------|
| **Average Cumulative Reward** | 衡量整体策略收益（加权能效与延迟） |
| **Energy Consumption** | 单个任务或系统的平均能耗 |
| **Execution Latency** | 任务执行总延迟（由最慢分支决定） |
| **Task Completion Ratio** | 成功完成任务的比例 |
| **Convergence Speed** | 收敛至稳定性能所需的episode数 |

---

### ✅ 基线方法对比

| 方法 | 类型 | 特点 |
|------|------|------|
| **DC-MADDPG** | MARL（无预测） | 本文方法去TimeGNN版本，用于消融实验 |
| **DC-MAAC** | 注意力增强MARL | 引入attention机制，但缺乏时间建模 |
| **DC-MA2C** | 混合动作MARL | 基础混合动作框架 |
| **ROP (Random Offloading Policy)** | 启发式 | 随机选择服务器和功率 |
| **FOO (Full Offloading Only)** | 启发式 | 所有任务完全卸载，本地不处理 |

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能数据汇总

| 指标 | TG-DCMADDPG | 最优基线 | 提升幅度 |
|------|-------------|----------|-----------|
| **平均累积奖励** | ~1.05 | DC-MADDPG (~0.9) | ↑ >15% |
| **平均能耗** | **~260** | DC-MADDPG (~280) | ↓ ~7.1% |
| **平均延迟** | **~24** | DC-MADDPG (~26–30) | ↓ ~10–20% |
| **任务完成率** | **≥98%**（最高99.6%） | FOO (~99%) / DC-MADDPG (~97%) | 更优平衡 |

> 注：随着边缘服务器数量从3增至15，TG-DCMADDPG仍保持稳定性能，而其他方法性能下降明显。

---

### ✅ 与基线方法的对比结果

#### （1）**收敛性对比（图3 & 图4）**
- TG-DCMADDPG 在约 **60个episode内快速收敛**，且最终奖励最高；
- 其他MARL方法（如DC-MADDPG、DC-MAAC）收敛较慢，并趋于饱和于较低水平；
- ROP 和 FOO 表现差且不稳定。

#### （2）**能耗对比（图5 & 图6）**
- TG-DCMADDPG 能耗最低，稳定在 **260以下**；
- 预测机制有效减少了冗余通信和无效卸载；
- FOO 和 ROP 因盲目卸载导致能耗飙升（>400）。

#### （3）**延迟对比（图7 & 图8）**
- TG-DCMADDPG 实现最低延迟（~24），得益于对服务器负载的准确预测；
- 随着服务器增多，整体延迟下降，但 TG-DCMADDPG 始终领先。

#### （4）**任务完成率（图9）**
- TG-DCMADDPG 完成率 **超过98%**，在15服务器场景达到 **99.6%**；
- ROP 最低（<80%），因随机决策常导致超时或资源冲突；
- FOO 虽然完成率高，但以高能耗和高延迟为代价。

---

### ✅ 消融实验结果（Ablation Study）

作者通过移除 TimeGNN 模块得到变体 **DC-MADDPG**，用于验证其作用：

| 模块 | 是否启用 | 能耗 | 延迟 | 完成率 | 结论 |
|------|---------|-------|--------|----------|--------|
| TimeGNN | ✅ 是 | 260 | 24 | 98%+ | 显著改善各项指标 |
| TimeGNN | ❌ 否（即DC-MADDPG） | ~280 | ~26–30 | ~97% | 性能全面落后 |

> 💡 发现：TimeGNN 不仅提升预测能力，还增强了策略的**泛化性与稳定性**，特别是在部分可观测环境下。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **TimeGNN 可有效捕捉边缘服务器间的时空相关性**，实现对未来状态的精准预测，从而支持“先知型”决策；
2. **混合动作空间设计（discrete + continuous）更适合复杂MEO场景**，允许细粒度调控任务分片与资源分配；
3. **预测+MARL 的结合大幅降低交互开销**，延长电池驱动边缘节点的服务寿命；
4. **TG-DCMADDPG 在能效、延迟、完成率三者之间实现了最佳权衡**，优于所有对比方法；
5. 该框架具备良好的**可扩展性与适应性**，适用于大规模、动态性强的 MEC 系统。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **依赖高质量历史数据** | TimeGNN 的预测精度受输入历史状态质量影响较大，在极端突变场景可能失效 |
| **预测误差传播风险** | 若预测偏差较大，可能导致错误的长期决策，形成正反馈恶化 |
| **模型复杂度较高** | 包含 GNN + MARL + 多模块协同，部署难度高于轻量级启发式方法 |
| **未考虑任务依赖图结构** | 当前模型假设任务可任意切分，未处理有向任务依赖关系（如DAG） |

---

### 🔮 未来工作方向

1. **引入不确定性建模**（如Bayesian TimeGNN 或 Probabilistic Forecasting），提升预测鲁棒性；
2. **扩展至任务图（Task DAG）卸载场景**，支持更复杂的计算流程；
3. **轻量化模型设计**，便于在资源极受限的边缘设备上部署；
4. **结合联邦学习机制**，保护用户隐私的同时实现分布式训练；
5. **探索更多 hybrid-action MARL 架构**（如结合 attention 或 meta-learning），进一步提升泛化能力。

---

## ✅ 总结

**TG-DCMADDPG** 是一种面向能源受限、动态变化 MEC 环境的先进任务卸载框架。它通过融合 **TimeGNN 的时空预测能力** 与 **DC-MADDPG 的混合动作决策机制**，实现了：
- 更快的收敛速度，
- 更低的能量消耗，
- 更短的任务延迟，
- 更高的任务完成率。

实验证明，该方法在多种网络规模下均表现出卓越的性能和可扩展性，是当前 MEC 资源管理领域的一项重要进展。

</details>

---

### 5. [MoE-DisCo:Low Economy Cost Training Mixture-of-Experts Models](https://arxiv.org/abs/2601.06857)

**Authors**: Xin Ye, Daning Cheng, Boyang Zhang, Yunquan Zhang  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.06857v1  

#### Abstract
Training large-scale Mixture-of-Experts (MoE) models typically requires high-memory, high-bandwidth GPUs (e.g., A100), and their high cost has become a major barrier to large-model training. In contrast, affordable hardware is low-cost but constrained by memory capacity and bandwidth, making it unsu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-DisCo: Low Economy Cost Training Mixture-of-Experts Models — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大规模 **Mixture-of-Experts (MoE)** 模型的训练严重依赖高内存、高带宽的昂贵硬件（如 NVIDIA A100），导致训练成本极高，成为大模型普及的主要障碍。而低成本硬件（如消费级 GPU、DCU）受限于显存容量和带宽，难以直接用于训练百亿/千亿参数级别的 MoE 模型。

此外，随着 GPU 集群规模扩大，通信开销（梯度同步、激活传输）、流水线气泡、负载不均衡等问题导致 **每 GPU 的计算利用率（MFU）持续下降**，进一步加剧了资源浪费。

### 🚀 提出的新方法：MoE-DisCo
作者提出 **MoE-DisCo**（Mixture-of-Experts with Disentangled Clustering and Coordination），一种**分阶段、硬件感知的 MoE 训练框架**，其核心思想是将 MoE 模型解耦为多个独立子模型，在低成本设备上并行训练，最后融合微调。

#### 方法流程：
1. **模型解耦（Model Decoupling）**  
   将原始 MoE 模型分解为 `K` 个独立的**密集子模型**（dense submodels），每个子模型包含完整的共享主干（shared backbone）和一个专家（expert）。
   
2. **数据解耦（Data Decoupling）**  
   使用 **K-means 聚类**对训练数据进行无监督划分，生成 `K` 个语义上差异化的子集，并与 `K` 个专家建立“解耦对齐”。

3. **独立并行训练（Independent Parallel Training）**  
   每个子模型在分配的数据子集上独立训练，无需跨设备通信，可在低功耗设备（如 RTX 4090）上高效运行。

4. **模型重集成与全局微调（Reintegration & Fine-tune）**  
   将所有训练好的专家合并回完整 MoE 架构，并在全量数据上进行短暂的全局微调（fine-tune），此阶段需高性能 GPU（如 A100），但时间极短。

### 🔍 相比现有方法的优势
| 维度 | 传统 MoE 训练 | MoE-DisCo |
|------|----------------|-----------|
| 硬件需求 | 全程依赖 A100/H800 等高端 GPU | 主体训练在 RTX 4090 等低成本 GPU 上完成 |
| 通信开销 | 多 GPU 协同，存在显著通信瓶颈 | 子模型完全独立，零通信开销 |
| 内存占用 | 所有专家必须同时加载 | 每次仅加载单个专家，大幅降低显存压力 |
| 总体成本 | 高昂（全程使用高价 GPU） | 成本降低 **47.6% ~ 69.5%** |
| 性能表现 | 标准基准 | 匹配甚至优于全参数训练 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **C4**: 大规模英文语料，经过严格清洗过滤
- **WikiText-2**: 高质量维基百科文本，常用于语言建模评测
- **OpenWebText**: 开源网页文本集合，模仿 GPT-2 的训练数据构建

### ⚙️ 实验设置
#### 模型架构
- **Qwen1.5-MoE-2.7B**：推理时激活约 2.7B 参数，性能接近 7B 密集模型（如 Mistral-7B）
- **LLaMA-MoE-3.5B**：基于 LLaMA 架构的 MoE 变体
- 两个模型均设置 **4 个专家（K=4）**

#### 训练配置
| 阶段 | 设备 | 平台 | 超参 |
|------|------|--------|-------|
| 子模型训练（S-phase） | RTX 4090 ×4 | 低成本平台 | AdamW, LR=1e-4, bfloat16, seq_len=1024 |
| 全局微调（F-phase） | A100 (80GB) ×1 | 高端平台 | AdamW, LR=3e-5, Cosine 调度器 |

#### 评估指标
1. **语言建模能力**：训练损失（loss）、困惑度（PPL）
2. **下游任务性能**：ARC-e（5-shot）、MMLU（5-shot）、HellaSwag（zero-shot）、PIQA（zero-shot）
3. **训练效率**：达到目标 loss 所需步数
4. **经济成本**：总花费（美元）、总训练时间（小时）

### 🆚 基线方法对比
- **Full-Parameter Training**：标准的端到端 MoE 全参数训练，全程使用 A100 GPU

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 和 Figure 4–5）

| 模型 | 数据集 | 方法 | 步数 | Loss | PPL |
|------|--------|--------|------|------|-----|
| Qwen | C4 | Full-Param | 21,150 | 4.954 | 230.32 |
|      |        | MoE-DisCo | **4,100** | **4.925** | **165.86** |
| Qwen | WikiText-2 | Full-Param | 12,500 | 2.303 | 71.89 |
|      |            | MoE-DisCo | **3,150** | **2.296** | **57.55** |
| Llama | OpenWebText | Full-Param | 28,600 | 4.606 | 162.51 |
|       |             | MoE-DisCo | **6,650** | **4.588** | **134.31** |

> ✅ MoE-DisCo 在相同或更低 loss 下，**收敛速度提升超过 4 倍以上**，且 PPL 更优。

### 📈 下游任务性能（Table 2）

| 方法 | ARC-e (%) | MMLU (%) | HellaSwag (%) | PIQA (%) |
|------|-----------|----------|---------------|----------|
| Full-Param | 27.9 | 23.0 | 27.45 | 52.25 |
| **MoE-DisCo** | **29.1** | **25.3** | **29.45** | **57.03** |

> ✅ MoE-DisCo 在所有下游任务上均**全面超越**全参数训练，尤其在零样本任务（PIQA）上提升显著（+4.78%）。

### 💰 经济成本分析（Table 3）

| 模型 | 数据集 | 全参数训练成本 | MoE-DisCo 成本 | 成本降幅 | 时间降幅 |
|------|--------|----------------|----------------|----------|----------|
| Qwen | C4 | $22.50 | **$6.87** | **69.5%** | 61.3% |
| Qwen | WikiText-2 | $6.93 | **$3.34** | **51.8%** | 35.2% |
| Qwen | OpenWebText | $29.91 | **$10.91** | **63.5%** | 54.3% |
| Llama | C4 | $12.86 | **$6.74** | **47.6%** | 37.1% |
| Llama | WikiText-2 | $16.99 | **$8.13** | **52.2%** | 44.4% |
| Llama | OpenWebText | $32.13 | **$15.14** | **52.9%** | 44.9% |

> ✅ MoE-DisCo 实现了 **47.6% ~ 69.5% 的成本削减**，同时训练时间大幅缩短。

### 🔍 消融实验结果

#### （1）聚类策略的重要性（Figure 6）
- 将 K-means 替换为**随机数据分配**后，MoE-DisCo 的性能退化至与 Full-Parameter 相当。
- 结论：**语义聚类是 MoE-DisCo 成功的关键**，确保专家专业化和多样性。

#### （2）不同专家数量的影响（Figure 7）
- 在 LLaMA 模型上测试 2 vs 4 个专家：
  - 4-expert 模型收敛更快，最终 loss 和 PPL 更低。
- 结论：MoE-DisCo 对不同专家数量具有良好的扩展性和适应性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE-DisCo 显著降低了 MoE 模型的训练成本**，通过将主体训练迁移至低成本硬件，实现了高达 **69.5% 的成本节约**。
2. **性能不降反升**：在语言建模和多项下游任务上，MoE-DisCo 训练的模型表现**匹配甚至优于**传统的全参数训练。
3. **训练效率大幅提升**：收敛所需步数减少 4 倍以上，训练时间显著压缩。
4. **语义聚类至关重要**：K-means 聚类使专家学习更具区分性的表示，是性能提升的核心机制。
5. **框架可扩展性强**：适用于不同 MoE 架构（Qwen、LLaMA）和专家数量。

### ⚠️ 局限性
- 当前实验仅在 **2.7B~3.5B 规模模型**上验证，尚未扩展到更大规模（如 10B+ 参数）。
- 聚类依赖预训练 embedding，可能引入偏差。
- 微调阶段仍需 A100，无法完全摆脱高端硬件。

### 🔮 未来工作方向
- 扩展至更大规模模型和更复杂任务（如多模态、代码生成）。
- 探索动态调整专家数量的策略（dynamic expert allocation）。
- 引入更多评估维度（如推理延迟、能耗）。
- 研究更高效的聚类方法（如基于 topic 或 domain 的划分）。

---

## 总结
**MoE-DisCo 是一种极具实用价值的低成本 MoE 训练范式**。它通过“**解耦训练 + 轻量融合**”的两阶段设计，成功打破了对高端 GPU 的依赖，在保持甚至提升模型性能的同时，将训练成本降低近七成。该方法特别适合资源受限的研究机构和中小企业，为大规模 MoE 模型的普惠化训练提供了可行路径。

> 🔗 项目代码地址：[https://anonymous.4open.science/r/MoE-DisCo-4835/](https://anonymous.4open.science/r/MoE-DisCo-4835/)

</details>

---

### 6. [Active Learning Strategies for Efficient Machine-Learned Interatomic Potentials Across Diverse Material Systems](https://arxiv.org/abs/2601.06916)

**Authors**: Mohammed Azeez Khan, Aaron D'Souza, Vijay Choyal  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.06916v1  

#### Abstract
Efficient discovery of new materials demands strategies to reduce the number of costly first-principles calculations required to train predictive machine learning models. We develop and validate an active learning framework that iteratively selects informative training structures for machine-learned...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Active Learning Strategies for Efficient Machine-Learned Interatomic Potentials Across Diverse Material Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究旨在解决**机器学习势函数（MLIPs）训练中对高成本第一性原理计算（如DFT）依赖过重**的问题。由于DFT计算耗时且昂贵，如何从大规模材料数据库中高效选择最具信息量的样本进行标注（即执行DFT），是实现数据高效、可扩展MLIP开发的关键挑战。

### 提出的新方法与新思路
作者提出并验证了一个**系统性的Active Learning（AL）框架**，用于在多种材料体系中高效训练MLIPs。其核心创新包括：

- **统一比较四种AL策略**：首次在同一实验设置下系统地对比了随机采样（Random）、不确定性采样（Uncertainty-based）、多样性采样（Diversity-based）以及混合策略（Hybrid）在多个真实材料系统中的表现。
- **集成Query-by-Committee（QBC）的神经网络集成模型**：通过ensemble variance实现可靠的epistemic uncertainty估计，支持不确定性驱动的查询。
- **基于k-means聚类的多样性采样设计**：在17维特征空间中使用k-means + 最远点优化选取代表性样本，增强探索能力。
- **开源可复现流程**：整个pipeline可在Google Colab上运行（<8GB RAM，<4小时/系统），显著降低研究门槛。

### 相比现有方法的优势
- **多系统、多策略、统计严谨的基准测试**：填补了领域内缺乏跨材料系统综合比较AL策略的空白。
- **强调“多样性”优于“不确定性”**：挑战了传统以不确定性为主的AL范式，在复杂系统中展现出更强性能。
- **实用性强**：全流程轻量化，适合资源受限的研究者部署，推动MLIP democratization。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 来源于两大开放材料数据库：
  - **Materials Project (MP)**：每个系统最多获取500个结构（VASP/PBE/PAW设置）
  - **Open Quantum Materials Database (OQMD)**：每个系统补充最多100个结构
- 四个代表性材料系统：
  - 元素碳（Carbon, C）
  - 硅（Silicon, Si）
  - 铁（Iron, Fe）
  - 钛氧化物（Ti-O compound）

> 总样本数介于500–600之间，按80%/20%划分为训练池集与独立测试集。

### 特征工程
构建了17维特征向量：
- **8个组分特征**：原子序数、质量、电负性等统计量
- **9个性质特征**：形成能、带隙、密度、稳定性指标等
- 所有特征标准化（StandardScaler），防止数据泄露

### 模型架构
- 使用**5成员神经网络集成（Ensemble of Feedforward NNs）**
  - 输入层：17维（特征）
  - 隐藏层：两层，每层128 neurons，ReLU激活
  - 输出层：1 neuron（预测每原子形成能）
  - 优化器：Adam (lr=1e-3)，损失函数：MSE
- 不确定性度量：采用ensemble variance（公式 $ U(x) = \frac{1}{M}\sum_m(y_m(x)-\bar{y}(x))^2 $）

### Active Learning 流程
- 初始化：30个已标注样本
- 迭代6轮，每轮新增15个样本 → 总标签样本达105
- 查询策略对比：
  1. **Random Sampling**（基线）
  2. **Uncertainty Sampling**：选择ensemble variance最高的样本
  3. **Diversity Sampling**：在特征空间中进行k-means聚类（k=15），选离簇中心最近的样本
  4. **Hybrid Sampling**：加权结合不确定性和多样性得分（权重 α=0.6）

### 评估指标与统计分析
- 主要指标：
  - **MAE**（Mean Absolute Error，单位 eV/atom）
  - **R²**（Coefficient of Determination）
- 实验重复：每个配置运行5次不同random seed
- 统计检验：配对t-test（双尾，α=0.05），报告p值判断显著性差异

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 2 & Figure 2）

| System | Strategy | MAE (eV/atom) | R² | vs. Random (p-value) |
|--------|----------|----------------|------|------------------------|
| **Carbon** | Random | 0.262±0.012 | 0.886 | — |
|          | Diversity | **0.261±0.008** | 0.879 | 0.823 (Better) |
| **Silicon** | Random | 0.235±0.006 | 0.928 | — |
|           | Hybrid | 0.238±0.006 | **0.941** | 0.289 (Better) |
| **Iron** | Random | 0.233±0.009 | 0.803 | — |
|         | Diversity | **0.223±0.011** | 0.796 | 0.216 (Better) |
| **Ti-O** | Random | 0.912±0.041 | -0.407 | — |
|        | **Diversity** | **0.813±0.035** | **-0.072** | **0.008*** (显著更好) |

> 注：MAE越低越好；R²越高越好；负R²表示模型不如均值预测。

### 与基线方法的对比结果
- **Diversity Sampling 表现最优或具竞争力**：
  - 在所有系统中均达到最低或接近最低MAE
  - 在最复杂的 **Ti-O系统中，MAE降低10.9%（p=0.008）**，效果显著
- **Uncertainty Sampling 效果不佳甚至更差**：
  - 在C、Fe、Ti-O系统中表现劣于随机采样（p<0.05）
  - 可能源于“过度聚焦高不确定性区域”导致采样偏差
- **Hybrid 方法未表现出优势**：
  - 尽管理论上有平衡exploration与exploitation潜力，但在本实验中表现不稳定，尤其在Ti-O上最差

### 学习曲线分析（Figure 2）
- **Carbon**：Diversity早期收敛更快，最终误差最低
- **Silicon**：各方法几乎无差别 → 表明任务简单，AL增益有限
- **Iron**：Diversity持续优于Random
- **Ti-O**：策略间差距最大，**Diversity明显领先**，体现其在复杂异质系统中的优越性

### 消融实验（Cross-Database Validation）
- 在Carbon系统上进行了跨数据库迁移测试（MP ↔ OQMD）
- 发现存在明显的domain shift：
  - MP→OQMD迁移效果较好（随着标签增加泛化提升）
  - OQMD→MP迁移较差
- 表明不同数据库间存在系统性偏差，影响模型可移植性

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Diversity-based sampling 是最有效且稳健的AL策略**，尤其适用于结构和组成复杂的材料系统（如Ti-O）。
2. ⚠️ **传统Uncertainty-based sampling 并不总是有效**，在某些系统中反而会引入偏差，导致性能下降。
3. 🔍 **AL策略的效果具有材料依赖性**：
   - 简单系统（如Si）各类方法差异小
   - 复杂系统（如Ti-O）中，多样性采样的优势极为突出
4. 💡 **智能数据选择可减少5–13%的DFT计算需求**即可达到目标精度，显著节省计算资源。
5. 🌐 **全流程可在Google Colab运行（<8GB RAM, <4小时）**，极大提升了MLIP开发的可及性。

### 方法的局限性
- **特征表达能力有限**：当前使用的17维手工特征缺乏局部结构信息（如SOAP、atomic environment descriptors），可能限制模型上限。
- **仅预测形成能**：未扩展至其他关键属性（如band gap、elastic modulus、magnetism）。
- **静态query策略**：未动态调整uncertainty/diversity权重，未能自适应材料复杂度变化。
- **未使用现代GNN架构**：如NequIP等E(3)-equivariant模型未被纳入，限制了与SOTA MLIP的对接。

### 未来工作方向
- 🔄 **开发自适应AL策略**：根据系统复杂度动态调节uncertainty与diversity的平衡。
- 🧱 **整合先进描述符或图神经网络（GNNs）**：例如将SOAP或SchNet嵌入AL pipeline，提升表征能力。
- 📈 **扩展到更多材料属性和相空间探索**：如动力学稳定性、相变路径、缺陷行为建模。
- 🤝 **结合生成模型与AL**：利用VAE或diffusion model生成新颖结构，并由AL筛选最有价值者进行DFT验证。
- 🚀 **集成Symmetry-aware架构**：借鉴Choyal等人[31]的工作，融合equivariant neural networks进一步提升数据效率。

---

> **总结一句话**：  
> 本文通过系统实证揭示——**在多样材料系统中训练MLIP时，“多样性优先”的Active Learning策略比传统的“不确定性优先”更高效、更鲁棒，特别是在复杂化合物中优势显著，为数据高效的材料发现提供了实用且可推广的方法论基础。**

</details>

---

### 7. [Free-RBF-KAN: Kolmogorov-Arnold Networks with Adaptive Radial Basis Functions for Efficient Function Learning](https://arxiv.org/abs/2601.07760)

**Authors**: Shao-Ting Chiu, Siu Wun Cheung, Ulisses Braga-Neto, Chak Shing Lee, Rui Peng Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.07760v1  

#### Abstract
Kolmogorov-Arnold Networks (KANs) have shown strong potential for efficiently approximating complex nonlinear functions. However, the original KAN formulation relies on B-spline basis functions, which incur substantial computational overhead due to De Boor's algorithm. To address this limitation, re...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Free-RBF-KAN: Kolmogorov-Arnold Networks with Adaptive Radial Basis Functions for Efficient Function Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
原始的 **Kolmogorov-Arnold Network (KAN)** 虽然在函数逼近方面表现出色，但其依赖于 **B-spline** 基函数，需通过 **De Boor 算法**进行计算，并且训练过程中需要动态调整域范围，导致显著的**计算开销**。此外，标准的 RBF-KAN 在精度上通常不如 B-spline KAN。

### 提出的新方法
本文提出 **Free-RBF-KAN** ——一种基于径向基函数（RBF）的 KAN 架构，引入了两个关键机制：
- **自适应学习网格（adaptive learning grids / free knots）**：允许 RBF 的中心点（centroids）在训练中自由移动，以对齐激活模式。
- **可训练的平滑度参数（trainable smoothness）**：将 RBF 的形状参数（如高斯核的 σ）作为网络参数联合优化。

该方法保留了 KAN 的分层叠加结构，同时用更灵活、高效的 RBF 替代 B-spline。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **计算效率** | 避免了 De Boor 迭代和域重缩放，训练和推理速度显著快于 B-spline KAN |
| **表达能力** | 自由移动的 centroids 和可调 smoothness 提升了模型灵活性，尤其适合多尺度、非光滑函数 |
| **理论保障** | 给出了首个针对 RBF-KAN 家族的**通用逼近定理证明**，确立其理论完备性 |
| **兼容性** | 可无缝集成到 PINN、DeepONet 等科学机器学习框架中 |

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
实验涵盖多个领域，包括：
- **合成函数逼近**：`f(x,y) = cos(4x) + sin(y) + sin(2πy) + |sin(3πy)|`（含非光滑项）
- **多尺度回归**：`f(x) = 0.1sin(50πx) + sin(2πx)`
- **高维回归**：MNIST 图像分类（作为回归任务处理）
- **物理信息学习（PINN）**：
  - 2D 热传导方程（高频率源项）
  - 2D Helmholtz 方程（光滑正弦源）
- **算子学习（Operator Learning）**：使用 DeepONet 框架学习反应扩散方程 `∂u/∂t = D ∂²u/∂x² + ku + f(x)` 的解算子

### 实验设置与评估指标
| 任务 | 模型配置 | 优化器 | 评估指标 |
|------|----------|--------|-----------|
| 函数逼近 | `[2,5,1]` 层结构 | LBFGS, 300 epochs | Test MSE |
| 多尺度回归 | MLP: 4×100；KAN类: 3×5 | Adam/LBFGS | NTK 分析、训练损失曲线 |
| MNIST 回归 | `[784,64,10]` | Adam, 20 epochs | Test Loss, Training Time |
| PINN 任务 | 内部采样 4000 点，边界 200~100 点 | Adam + 学习率衰减 | L²-loss, 训练时间（秒） |
| DeepONet | Branch: MLP (固定)，Trunk: 替换为不同 KAN | Adam, 10k 步 | Relative L² error, 参数量, 训练时间 |

### 基线方法对比
- **MLP**：传统全连接神经网络（Tanh/SiLU）
- **KAN**：原始 B-spline KAN（三次样条）
- **FreeKnots-KAN**：带可学习 knot 的 B-spline KAN
- **RBF-KAN**：固定 centroids 和 smoothness 的 RBF 版本
- **PINN / DeepONet**：经典物理信息与算子学习基准

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### （1）非光滑函数逼近（Table 2）
| Model | #Param | Test MSE |
|-------|--------|---------|
| MLP | 261 | 5.26e-1 |
| KAN | 195 | 3.96e-3 |
| FreeKnots-KAN | 307 | 2.57e-4 |
| RBF-KAN | 120 | 6.05e-4 |
| **Free-RBF-KAN** | **290** | **2.39e-4** ✅ |

> **结论**：Free-RBF-KAN 达到最高精度，优于 FreeKnots-KAN，且参数少于后者。

#### （2）MNIST 高维回归（Table 3）
| Model | #Param | Test Loss | Training Time (s) |
|-------|--------|----------|------------------|
| MLP | 509,410 | **6.70e-2** | **81.58** |
| KAN | 762,240 | 1.17e-1 | 97.95 |
| RBF-KAN | 508,160 | 2.02e-1 | 82.27 |
| **Free-RBF-KAN** | **525,120** | **8.79e-2** | **85.81** |

> **结论**：虽仍落后于 MLP，但 Free-RBF-KAN 显著优于标准 KAN 和 RBF-KAN，在准确性和效率间取得更好平衡。

#### （3）2D Heat Conduction（Table 4）
| Model | #Param | L²-loss | Training Time (s) |
|-------|--------|--------|------------------|
| MLP | 5,081 | 1 | 160 |
| KAN | 1,400 | 6.52e-3 | 267 |
| RBF-KAN | 1,280 | 2.78e-3 | 124 |
| **Free-RBF-KAN** | **2,000** | **2.41e-3** ✅ | **138** |

> **结论**：Free-RBF-KAN 精度最高，训练时间仅为 KAN 的 ~50%。

#### （4）2D Helmholtz Equation（Table 5）
| Model | #Param | L²-loss | Training Time (s) |
|-------|--------|--------|------------------|
| MLP | 50,049 | 4.15e-2 | 39 |
| KAN | 600 | 1.58 ❌ | 153 |
| RBF-KAN | 400 | 3.67e-1 | 49 |
| **Free-RBF-KAN** | **640** | **3.35e-2** ✅ | **62** |

> **结论**：KAN 完全失败；Free-RBF-KAN 不仅超越 RBF-KAN，还优于 MLP，且训练时间可控。

#### （5）DeepONet 算子学习（Table 6）
| Model (Trunk) | #Param | Rel. L² error | Training Time (s) |
|---------------|--------|--------------|------------------|
| MLP | 18,921 | 2.08e-2 | 78 |
| KAN | 11,945 | 6.15e-2 | 96 |
| RBF-KAN | 10,625 | 3.70e-2 | 84 |
| **Free-RBF-KAN** | **11,185** | **1.94e-2** ✅ | **88** |

> **结论**：Free-RBF-KAN 实现最低误差，甚至略优于 MLP trunk，且参数更少。

### 消融实验分析
- **自适应网格 vs 固定网格**：在 MNIST 和 PINN 实验中，加入 adaptive centroids 显著提升收敛速度和最终精度。
- **可训练 smoothness**：进一步改善高频特征捕捉能力，在多尺度任务中 NTK 谱显示更宽泛的能量分布，表明无 spectral bias。
- **NTK 分析**（图4）：Free-RBF-KAN 的 NTK 特征值衰减缓慢，说明其能均衡地学习低频与高频成分，类似原始 KAN，优于 MLP。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Free-RBF-KAN 在多种任务中一致优于原始 KAN 和 RBF-KAN**，尤其是在物理驱动的多尺度、高频率 PDE 求解中表现卓越。
2. ✅ **计算效率显著提升**：相比 B-spline KAN，避免了 De Boor 迭代，训练速度快 2–3 倍。
3. ✅ **具备理论完备性**：首次为 RBF-KAN 提供通用逼近定理证明，确认其 universal approximation capability。
4. ✅ **无 spectral bias**：NTK 分析证实其在训练过程中能有效捕捉高频信号，适用于复杂物理系统建模。
5. ✅ **模块化强**：可作为 trunk network 成功嵌入 DeepONet，实现高性能算子学习。

### 方法的局限性
- ❌ 在高度非结构化的图像识别任务（如 MNIST）上，仍不如标准 MLP 高效。
- ❌ RBF-KAN 类方法本质上仍是逐变量分解，可能不适用于极端耦合的高维交互问题。
- ❌ 当输入维度极高时，尽管缓解了“维度灾难”，但仍面临 RBF 中心数量增长的问题（不过因是 univariate 结构，影响较小）。

### 未来工作方向
- 探索将 Free-RBF-KAN 表达为等效的 **MLP with Gaussian activations** 形式，以便利用成熟的 MLP 优化技术。
- 扩展至更多类型的 RBF 核（如 Matérn kernel），控制输出函数的全局光滑性。
- 应用于更大规模的真实世界 PDE 问题（如 Navier-Stokes）、控制逆问题及不确定性量化。

---

> **总结**：  
> **Free-RBF-KAN** 是一个兼具**高效性、灵活性与理论深度**的新型 KAN 架构。它通过引入 **可学习的 RBF centroids 与 smoothness 参数**，解决了传统 KAN 的计算瓶颈，在保持甚至超越其精度的同时大幅提升了训练效率，特别适合科学计算中的**多尺度建模、PDE 求解与算子学习**任务，是当前 KAN 发展路线中极具前景的方向之一。

</details>

---

### 8. [Efficient and Reliable Estimation of Named Entity Linking Quality: A Case Study on GutBrainIE](https://arxiv.org/abs/2601.06624)

**Authors**: Marco Martinelli, Stefano Marchesin, Gianmaria Silvello  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.06624v1  

#### Abstract
Named Entity Linking (NEL) is a core component of biomedical Information Extraction (IE) pipelines, yet assessing its quality at scale is challenging due to the high cost of expert annotations and the large size of corpora. In this paper, we present a sampling-based framework to estimate the NEL acc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient and Reliable Estimation of Named Entity Linking Quality: A Case Study on GutBrainIE

## 1. 论文的主要贡献和创新点

### 解决的问题
- **大规模 Named Entity Linking (NEL) 质量评估成本高**：在生物医学领域，NEL 是信息抽取（Information Extraction, IE）的关键环节，但其质量评估依赖专家人工标注，成本高昂且难以扩展到大型语料库。
- **传统采样方法效率低下**：Simple Random Sampling (SRS) 导致频繁的上下文切换（context switch），增加认知负担和标注时间。

### 提出的新方法与创新
- **将 NEL 准确率估计形式化为带约束的优化问题**：
  - 目标是在满足目标 Margin of Error (MoE) 的前提下，最小化人工标注成本。
- **适配并应用 Stratified Two-stage Weighted Cluster Sampling (STWCS)** 到 NEL 场景：
  - **Stratification（分层）**：基于实体标签定义 5 个非重叠的 *strata*，提升组内同质性。
  - **Clustering（聚类）**：以归一化的表面形式（surface form）作为聚类依据，减少上下文切换。
  - **两阶段加权抽样**：先按规模概率抽层，再在层内按集群大小抽 cluster，最后从每个 cluster 中最多抽取 `m=5` 条 triples 进行标注。
- **完全独立于 NEL 输出的设计**：
  - 分层（strata）和聚类（cluster）均基于原始文本特征（如 entity label 和 normalized surface form），避免对 NEL 结果的依赖，防止评估过程中的循环偏差。

### 相比现有方法的优势
- **显著降低标注成本**：相比 SRS，在相同样本量下减少约 29% 的专家标注时间。
- **更高的统计效率**：通过减少方差和上下文切换，更快达到目标 MoE。
- **可扩展性强**：框架通用，适用于其他 NEL 基准或 IE 流水线的质量评估。

---

## 2. 核心实验方法和设置

### 数据集
- **GutBrainIE**：一个专注于“肠脑轴”（gut-brain axis）领域的新型生物医学语料库。
  - 包含 1,647 篇 PubMed 摘要。
  - 手动标注的 Platinum 和 Gold 折叠中包含 **11,184 条 NEL triples**。
  - 实体类型涵盖 13 类（如 DDF、Microbiome、Drug 等），链接至 6 个标准词表 + 自定义本体。

### 实验设置
- **采样策略**：
  - 主方法：**STWCS**（Stratified Two-stage Weighted Cluster Sampling）
  - 基线方法：**Simple Random Sampling (SRS)**
- **目标精度要求**：
  - 目标 Margin of Error (MoE) ≤ 0.05
- **迭代流程**：
  - 动态采样 → 人工标注 → 更新准确率估计与 MoE → 直至 MoE 达标。
- **停止条件**：当 MoE ≤ 0.05 时终止采样。

### 评估指标
- **总体 NEL 准确率估计值及其 (1−α) 置信区间（CI）**
- **Margin of Error (MoE)**：衡量估计不确定性
- **标注总耗时（wall-clock time）**
- **上下文切换次数（context switches）**
- **每条三元组的平均标注时间（区分是否发生上下文切换）**

### 基线方法对比
- **SRS 模拟实验**：
  - 固定 STWCS 实际标注的 2,749 条 triples。
  - 随机打乱顺序模拟 SRS 下的上下文切换频率。
  - 使用实测的时间参数估算 SRS 所需总时间。
  - 重复 1,000 次并进行 bootstrap 以获得稳健估计。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 数值 |
|------|------|
| 总 NEL triples 数量 | 11,184 |
| 实际标注 triples 数量 | **2,749**（占比 **24.6%**）|
| 达成目标 MoE 所需标注比例 | < 25% |
| 最终 MoE | **0.047**（< 0.05 目标）|
| 总体 NEL 准确率估计 | **0.915 ± 0.0473** |
| 对应 95% CI | [0.868, 0.963] |

### 与基线方法（SRS）的对比结果
| 指标 | STWCS（实际） | SRS（模拟） | 提升 |
|------|---------------|-------------|-------|
| 总标注时间 | **797 分钟**（13h17m） | **1,124.57 分钟**（18h44m） | ↓ **327.6 分钟** |
| 上下文切换次数 | 1,050 | ~2,745（均值） | ↓ ~62% |
| 效率比（time_STWCS / time_SRS） | ~0.71 | — | **节省约 29% 时间** |

> ✅ **结论**：STWCS 在相同样本量下比 SRS 快近 30%，主要得益于更少的上下文切换。

### 各 stratum 的准确率估计（Table 3）
| Stratum | 准确率估计 (p̂) | MoE | CI 下限 |
|--------|----------------|-----|---------|
| DDF | 0.883 | 0.093 | 0.790 |
| Microbiome + Bacteria | **0.979** | 0.108 | 0.871 |
| Human + Animal + Anatomical Location | **0.976** | 0.060 | 0.916 |
| Chemical + Gene | 0.851 | 0.087 | 0.764 |
| Drug + ... + Technique | 0.898 | 0.154 | **0.744** |

> 🔍 发现：Microbiome/Bacteria 和 生物主体类（Human/Animal/Location）准确率最高；最后一类 MoE 最大，提示需更多采样以提高置信度。

---

## 4. 关键结论和发现

### 主要发现
1. **仅需不到 25% 的人工标注即可实现可靠的 NEL 质量估计**（MoE ≤ 0.05）。
2. **STWCS 显著优于 SRS**：通过减少上下文切换，节省约 **29% 的专家时间**，验证了结构化采样的有效性。
3. **分层设计提供细粒度洞察**：不同 entity 类型组的 NEL 表现存在差异，可用于指导后续针对性修正（targeted curation）。
4. **框架具有通用性和可扩展性**：不依赖 NEL 输出本身，可推广至其他 NEL 或 KG 质量评估任务。

### 方法的局限性
- **依赖高质量的初始 entity mention 检测**：本工作假设 mention 已正确识别，仅评估 linking 质量。
- **strata 定义依赖经验**：当前分组是语义连贯性与数量均衡之间的折衷，可能不是最优。
- **未考虑多标注者一致性**：所有判断由单一专家完成，缺乏 inter-annotator agreement 分析。

### 未来工作方向
- 引入 **multiple annotators + majority voting** 提升标注可靠性。
- 将下游影响纳入 stratification 设计，例如优先采样参与大量 relation 的概念或 KG 中心性高的节点。
- 探索自适应采样策略，动态调整各 stratum 的采样强度以最小化整体 MoE。
- 将该框架集成到自动化 IE 流水线中，实现持续的质量监控。

---

> 📌 **总结一句话**：本文提出了一种基于 **STWCS** 的高效 NEL 质量估计算法，在 **GutBrainIE** 上仅用 **24.6%** 的标注量即实现了 **0.915±0.0473** 的准确率估计，并比 SRS 节省 **29%** 的人工时间，为大规模生物医学 IE 系统提供了可扩展、统计可靠的质量评估方案。

</details>

---

### 9. [Conditional Memory via Scalable Lookup: A New Axis of Sparsity for Large Language Models](https://arxiv.org/abs/2601.07372)

**Authors**: Xin Cheng, Wangding Zeng, Damai Dai, Qinyu Chen, Bingxuan Wang, Zhenda Xie, Kezhao Huang, Xingkai Yu, Zhewen Hao, Yukun Li, Han Zhang, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.07372v1  

#### Abstract
While Mixture-of-Experts (MoE) scales capacity via conditional computation, Transformers lack a native primitive for knowledge lookup, forcing them to inefficiently simulate retrieval through computation. To address this, we introduce conditional memory as a complementary sparsity axis, instantiated...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Conditional Memory via Scalable Lookup: A New Axis of Sparsity for Large Language Models 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前主流的 **Large Language Models**（LLMs）依赖 **Transformer** 架构，其通过 **Mixture-of-Experts**（MoE）实现条件计算（conditional computation），从而在不显著增加计算量的前提下扩展模型容量。然而，Transformer 缺乏原生的 **知识查找**（knowledge lookup）机制，导致模型必须通过多层 **Attention** 和 **Feed-Forward Networks**（FFNs）来“重建”静态知识（如命名实体、固定短语等），造成计算资源浪费。

这种设计迫使模型用动态计算模拟静态记忆，效率低下，尤其在处理局部、重复性强的语言模式时。

---

### **提出了什么新方法或新思路**

本文提出 **conditional memory** 作为与 **conditional computation** 并行的“第二条稀疏性轴”，并引入 **Engram** 模块作为其实现。

- **Engram** 是一种基于现代改进的 **N-gram embedding** 的条件记忆模块，支持 **O(1)** 时间复杂度的常数时间查找。
- 它将静态语言模式（如“Alexander the Great”、“Princess of Wales”）存储为可检索的记忆条目，通过输入上下文生成哈希键进行查找。
- 该模块与 MoE 并行运行，形成“**计算-记忆**”双稀疏架构。

#### **核心创新点**：

1. **提出“稀疏性分配”问题**（Sparsity Allocation Problem）：
   - 在总参数和 FLOPs 预算固定的条件下，如何最优地分配稀疏容量给 MoE 专家（计算）和 Engram 记忆（存储）？
   - 实验发现存在 **U型缩放律**（U-shaped scaling law），即混合分配优于纯 MoE 或纯记忆方案。

2. **Engram 的现代工程优化**：
   - **Tokenizer Compression**：对子词 token 进行归一化合并（如 `Apple` 和 `_apple` 映射为同一 ID），提升语义密度，减少有效词汇量（压缩 23%）。
   - **Multi-Head Hashing**：使用多个哈希函数降低冲突概率。
   - **Context-aware Gating**：利用当前隐藏状态作为 query，对检索到的静态嵌入进行门控调节，解决歧义和噪声。
   - **Multi-Branch Integration**：适配多分支架构（如 mHC），实现高效融合。

3. **基础设施感知效率**（Infrastructure-aware Efficiency）：
   - Engram 的查找是**确定性的**，允许在推理时提前预取（prefetch）内存条目，重叠通信与计算，显著降低延迟。
   - 支持将大规模嵌入表卸载到主机内存（host memory），突破 GPU 显存限制。

---

### **相比现有方法的优势**

| 对比维度 | MoE | Engram |
|--------|-----|--------|
| **稀疏性类型** | 条件计算（激活部分专家） | 条件记忆（查找静态嵌入） |
| **操作开销** | 动态路由，不可预测 | 确定性哈希，可预取 |
| **适用任务** | 动态推理、组合逻辑 | 静态知识、局部模式 |
| **系统效率** | 受限于专家并行通信 | 可卸载至 CPU 内存，无显著开销 |

Engram 不是替代 MoE，而是与其互补，共同构成更高效的稀疏架构。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **预训练数据**：262B tokens 的混合语料（未具体说明，但提及使用 DeepSeek-v3 的 tokenizer）。
- **评估基准**：
  - **知识与推理**：MMLU、MMLU-Redux、MMLU-Pro、CMMLU、C-Eval、AGIEval、ARC-Easy/Challenge、BBH、TriviaQA
  - **阅读理解**：DROP、RACE-Middle/High、C3
  - **代码与数学**：HumanEval、MBPP、CruxEval、GSM8K、MGSM、MATH
  - **长上下文能力**：LongPPL（书籍、论文、代码、CoT）、RULER（NIAH 多跳检索、变量追踪等）

---

### **实验设置和评估指标**

- **模型配置**：
  - **Dense-4B**：4.1B 参数，全连接 FFN。
  - **MoE-27B**：26.7B 总参数，72 个路由专家，激活 6 个/ token。
  - **Engram-27B**：与 MoE-27B **同参数、同 FLOPs**，减少专家数至 55，腾出 5.7B 参数用于 Engram 记忆。
  - **Engram-40B**：进一步扩大 Engram 至 18.5B 参数，总参数达 39.5B。

- **统一设置**：
  - 所有模型激活参数均为 3.8B，训练 262B tokens。
  - 使用 MLA（Multi-head Latent Attention）和 mHC（multi-branch）架构。
  - Engram 插入第 2 和 15 层，支持 2-gram 和 3-gram 查找。

- **评估指标**：
  - 准确率（Acc.）、F1、Exact Match（EM）、Pass@1、困惑度（PPL）等。

---

### **基线方法对比**

- **Dense-4B**：基础密集模型。
- **MoE-27B**：严格同参数、同 FLOPs 的 MoE 基线。
- **OverEncoding**（Huang et al., 2025a）：将 N-gram 嵌入与词表嵌入平均融合，作为弱记忆基线。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Engram-27B vs MoE-27B）**

| 任务类别 | 指标 | MoE-27B | Engram-27B | 提升 |
|--------|------|---------|------------|------|
| **知识** | MMLU (5-shot) | 57.4 | **60.4** | +3.0 |
| | CMMLU (5-shot) | 57.9 | **61.9** | +4.0 |
| | MMLU-Pro (5-shot) | 28.3 | **30.1** | +1.8 |
| **推理** | BBH (3-shot) | 50.9 | **55.9** | **+5.0** |
| | ARC-Challenge (25-shot) | 70.1 | **73.8** | +3.7 |
| | DROP (1-shot) | 55.7 | **59.0** | +3.3 |
| **代码/数学** | HumanEval (0-shot) | 37.8 | **40.8** | +3.0 |
| | MATH (4-shot) | 28.3 | **30.7** | +2.4 |
| | GSM8K (8-shot) | 58.4 | **60.6** | +2.2 |

> ✅ **关键发现**：提升不仅体现在知识任务，**在推理和代码/数学任务上增益更大**，表明 Engram 释放了模型的深层推理能力。

---

### **长上下文能力（RULER 基准）**

| 模型 | Multi-Query NIAH | Variable Tracking |
|------|------------------|-------------------|
| MoE-27B | 84.2 | 77.0 |
| Engram-27B | **97.0** | **89.0** |

> ⬆️ **提升显著**：+12.8 和 +12.0，表明 Engram 释放了 Attention 容量用于全局上下文建模。

---

### **消融实验结果**

| 消融变体 | 验证损失（↓） | 分析 |
|--------|--------------|------|
| **MoE Baseline** | 1.808 | 基线 |
| **+Engram (完整)** | **1.768** | △ = -0.040 |
| w/o Multi-Branch | 1.778 | 分支融合重要 |
| w/o Token Compression | 1.775 | 归一化提升语义密度 |
| w/o Context-aware Gating | 1.773 | 门控机制抑制噪声 |
| w/o Short Conv | 1.768 | 影响较小 |
| +4-gram | 1.770 | 在固定预算下不如 2/3-gram 有效 |

> 🔍 **最佳插入位置**：第 2 层效果最好，早介入可尽早卸载局部重建任务。

---

## 4. 关键结论和发现

### **主要发现**

1. **Conditional Memory 是有效的第二稀疏轴**：
   - 与 MoE 形成互补，通过 **U型缩放律** 证明混合分配最优。
   - Engram-27B 在同参数、同 FLOPs 下全面超越 MoE-27B。

2. **Engram 提升的不仅是知识检索，更是推理能力**：
   - 通过 **LogitLens** 和 **CKA** 分析发现，Engram 使早期层更快收敛，功能上等效于“加深网络”。
   - 释放的 Attention 容量可用于处理长距离依赖。

3. **系统效率优越**：
   - 即使将 100B 参数的 Engram 表卸载到 CPU 内存，推理吞吐仅下降 **<3%**。
   - 确定性查找支持预取，适合大规模部署。

4. **功能二分性明确**：
   - **事实知识类任务**（如 TriviaQA）严重依赖 Engram，移除后性能暴跌至 29–44%。
   - **阅读理解类任务**（如 C3）主要依赖 backbone，保留 81–93% 性能。

---

### **方法的局限性**

- **记忆容量受限于哈希冲突**：尽管多头哈希缓解，极端高频/低频 N-gram 仍可能影响精度。
- **依赖高质量 tokenizer compression**：若归一化规则不当，可能导致语义混淆。
- **目前仅适用于局部模式**：无法处理需要动态生成或复杂推理的知识。

---

### **未来工作方向**

1. **探索更高阶 N-gram**（如 4-gram+）在更大内存预算下的潜力。
2. **结合非参数记忆**（如 RETRO、REALM），构建混合记忆系统。
3. **动态更新记忆表**，支持知识演化。
4. **扩展至多模态场景**，实现跨模态记忆查找。

---

## 总结

✅ **Engram** 成功验证了 **conditional memory** 作为 LLM 稀疏化的新范式，不仅提升了知识检索效率，更通过“解放早期层”增强了模型的深度推理能力。其**确定性、可预取、可卸载**的设计使其具备极强的工程落地潜力，有望成为下一代稀疏大模型的标准组件。

</details>

---

### 10. [FROAV: A Framework for RAG Observation and Agent Verification - Lowering the Barrier to LLM Agent Research](https://arxiv.org/abs/2601.07504)

**Authors**: Tzu-Hsuan Lin, Chih-Hsuan Kao  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.07504v1  

#### Abstract
The rapid advancement of Large Language Models (LLMs) and their integration into autonomous agent systems has created unprecedented opportunities for document analysis, decision support, and knowledge retrieval. However, the complexity of developing, evaluating, and iterating on LLM-based agent work...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FROAV: A Framework for RAG Observation and Agent Verification – Lowering the Barrier to LLM Agent Research

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLM）代理系统的开发与评估面临显著的技术门槛，尤其是在**工作流编排、向量数据库管理、Prompt工程、评估方法论和数据基础设施**等方面。这些问题对缺乏软件工程背景的领域专家（如金融分析师、医学研究人员）构成了严重障碍，限制了他们在LLM代理系统设计与验证中的参与。

此外，现有工具要么需要从零构建（高代码负担），依赖闭源平台（缺乏可复现性），或需手动集成多个组件（集成成本高），导致研究效率低下且难以协作。

### 提出的新方法与思路
作者提出了 **FROAV**（Framework for RAG Observation and Agent Verification），一个开源的研究平台，旨在通过以下方式降低LLM代理研究的门槛：

- **可视化工作流编排**：集成 n8n 实现拖拽式流程设计，无需编写底层架构代码。
- **多维度“LLM-as-a-Judge”评估框架**：在四个理论驱动的维度（Reliability, Completeness, Understandability, Relevance）上进行自动化评估，并采用多模型共识机制提升鲁棒性。
- **Human-in-the-Loop（HITL）反馈系统**：通过 Streamlit 构建前端界面，支持领域专家提供结构化反馈并与自动评估结果关联分析。
- **细粒度数据管理**：基于 PostgreSQL 存储执行轨迹、评估结果和人类反馈，实现全流程溯源。
- **可扩展的Python后端**：通过 FastAPI 支持自定义预处理逻辑和ML管道集成。
- **容器化部署**：使用 Docker Compose 实现一键部署，确保环境一致性与可复现性。

### 相比现有方法的优势
| 维度 | 传统方法（LangChain/LlamaIndex等） | FROAV |
|------|-------------------------------|--------|
| 开发复杂度 | 高（需大量Python编码） | 极低（视觉化操作为主） |
| 评估体系 | 通常为单一指标或简单自动化 | 多维、多模型共识、“LLM-as-a-Judge”+人工协同 |
| 可访问性 | 要求强编程能力 | 面向领域专家友好 |
| 数据追踪 | 手动记录或缺失 | 全链路日志与溯源 |
| 可扩展性 | 高但需自行实现 | 分层设计，支持从无码到全代码定制 |
| 协作支持 | 弱（工程师主导） | 强（支持人机协同、跨角色协作） |

> ✅ **核心创新**：将 **RAG 观察**（Observation）与 **代理验证**（Agent Verification）整合于统一平台，首次实现了“**无需写代码即可完成完整LLM代理实验闭环**”的目标。

---

## 2. 核心实验方法和设置

### 使用的数据集
论文虽未公开具体数据集名称，但明确指出其应用场景为 **金融文档分析**，特别是：
- **SEC 10-K 和 10-Q 文件**（上市公司年报与季报）
- 文档以 PDF 形式输入，经由 FastAPI 后端解析为结构化文本

该框架设计为 **material-agnostic**（材料无关），适用于任何需要语义分析的领域。

### 实验设置
- **RAG Pipeline 设计**：
  1. PDF 内容解析（Custom FastAPI Service）
  2. 材料分块（Chunking）
  3. 向量化与存储（默认使用 Supabase + pgvector）
- **评估流程嵌入在每个生成步骤之后**，形成迭代优化闭环。

- **评估框架配置**：
  - 每个输出由多个 LLM（如 GPT-4、Claude、Mixtral 等）作为“Judge”独立评分
  - 评分维度：Reliability, Completeness, Understandability, Relevance
  - 采用 **中位数聚合策略**（median aggregation）减少异常值影响
  - 每个 Judge 使用专门设计的 system prompt（例如可靠性评估器被设定为“财务审计师”角色）

- **Human-in-the-Loop 设置**：
  - 领域专家通过 Streamlit 界面查看模型输出与自动评分
  - 提供结构化反馈（打分+评论），并与 LLM Judge 结果对比分析

### 评估指标
| 类型 | 指标 |
|------|------|
| 自动化评估 | 四个维度的 LLM Judge 得分（0–10分制），多模型一致性 |
| 人工评估 | 人类专家对同一四个维度的评分，inter-annotator agreement |
| 系统性能 | 开发时间、代码行数、学习曲线（定性+定量估计） |
| 对齐性分析 | LLM Judge 评分 vs. Human Judgment 的相关性 |

### 基线方法对比
论文未直接比较不同 RAG 模型的准确率，而是从 **研发效率与系统可用性角度** 进行对比：

| 基线方式 | FROAV |
|---------|-------|
| 手动开发完整RAG+评估系统（LangChain + 自建前后端） | 使用FROAV框架（n8n + FastAPI + Streamlit + PostgreSQL） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 指标 | 手动编码 | FROAV 框架 |
|------|----------|------------|
| 基础设施搭建时间 | 40–50 小时 | ~1 小时（单条 `docker-compose up`） |
| 工作流逻辑代码量 | >1000 行 Python | 0 行（可视化拖拽） |
| 评估逻辑实现 | >1000 行（含多模型共识） | 预置 “Judge” 节点 |
| HITL 接口开发 | 80+ 小时 | 2 小时（模块化配置） |
| 学习曲线 | 高（需资深工程师） | 低（领域专家可上手） |

> ⚡️ **结论**：FROAV 将典型 RAG 实验系统的开发周期从 **数周缩短至数小时**，极大提升了研究迭代速度。

### 与基线方法的对比结果
- 在相同任务下（金融报告生成与验证），FROAV 实现了与手动编码系统 **同等甚至更优的功能完整性**，同时：
  - 减少约 **95% 的基础设施代码**
  - 提升 **团队协作效率**（领域专家可直接参与评估）
  - 实现 **全流程透明日志记录**，便于调试与复现

### 消融实验（Ablation Study）
论文未提供正式消融实验，但在讨论部分提及以下观察：
- 若关闭 **multi-model consensus**，LLM Judge 评分波动增大（±1.5 分以上），尤其在 Reliability 维度
- 若移除 **human-in-the-loop** 模块，则无法识别某些语义偏差（如“看似合理但事实错误”的陈述）
- 使用默认 prompt 的 Judge 性能低于经过角色定制的 Judge（如“你是一名审计师”）

---

## 4. 关键结论和发现

### 主要发现
1. **FROAV 显著降低了LLM代理研究的准入门槛**，使非程序员的领域专家也能深度参与系统设计与评估。
2. **“LLM-as-a-Judge”结合多模型共识机制** 能有效模拟人类判断趋势，在四大维度上具有较高一致性。
3. **自动化评估与人工反馈高度互补**：LLM Judges 擅长检测事实错误与完整性缺失；人类专家更擅长判断表达清晰度与决策相关性。
4. **可视化工作流 + 容器化部署** 极大增强了实验的可复现性和跨团队共享能力。
5. FROAV 的 **material-agnostic 架构** 使其不仅适用于金融文档，还可快速迁移到法律、医疗、科研等领域。

### 方法的局限性
1. 当前评估仅涵盖 **四个固定维度**，可能不足以覆盖所有应用场景的需求。
2. LLM Judge 的评估质量依赖于所选模型的能力与偏见，仍存在 **position bias、verbosity bias** 等风险。
3. 尚未经过企业级大规模压力测试（如百万级文档检索场景）。
4. 流程灵活性受限于 n8n 的节点能力和表达能力，极端复杂的控制流仍需代码介入。

### 未来工作方向
1. **扩展评估维度**：引入更多领域特定的评估标准（如合规性、伦理审查）。
2. **LLM Judge 校准研究**：利用 FROAV 平台系统性地对比 LLM 评分与人类专家评分的相关性，建立可信的 calibration 曲线。
3. **增强自动化反馈闭环**：让评估结果反向驱动 prompt 调优或检索参数调整，实现 self-improving agent。
4. **支持更多可视化工具集成**：探索与 LangGraph、Flowise 等替代方案的兼容性。
5. **开放社区生态建设**：鼓励用户贡献预设 workflow templates 和 domain-specific judge prompts。

---

> 🔚 **总结**：FROAV 不是一个追求SOTA性能的算法模型，而是一个面向 **科学研究民主化** 的基础设施创新。它成功地将复杂的 LLM agent 实验流程封装成“即插即用”的研究平台，推动了从“只有工程师能做LLM研究”向“人人皆可参与”的范式转变。

</details>

---

### 11. [Land-then-transport: A Flow Matching-Based Generative Decoder for Wireless Image Transmission](https://arxiv.org/abs/2601.07512)

**Authors**: Jingwen Fu, Ming Xiao, Mikael Skoglund, Dong In Kim  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.07512v1  

#### Abstract
Due to strict rate and reliability demands, wireless image transmission remains difficult for both classical layered designs and joint source-channel coding (JSCC), especially under low latency. Diffusion-based generative decoders can deliver strong perceptual quality by leveraging learned image pri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Land-then-transport: A Flow Matching-Based Generative Decoder for Wireless Image Transmission*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统无线图像传输面临以下挑战：
- **分层设计（如 JPEG2000 + LDPC）** 在有限码长和低延迟场景下性能不佳，且对信道失配敏感，存在“悬崖效应”。
- **基于扩散模型（diffusion-based）的生成解码器** 虽能提升感知质量，但依赖数百步的随机去噪过程，导致**高解码延迟**和计算开销，难以满足实时通信需求。

### 提出的新方法与创新思路
本文提出了一种名为 **Land-then-Transport (LTT)** 的新范式，并构建了一个基于 **Flow Matching (FM)** 的确定性生成解码器，其核心思想如下：

- **将物理信道嵌入生成流中**：将无线信道（如 AWGN）的噪声效应视为连续概率流的一部分。接收信号被视为在某个“着陆时间” $t^*$ 上的中间状态，而非外部噪声污染。
- **Flow Matching 构建确定性 ODE 解码器**：
  - 为 AWGN 信道构造一条**高斯平滑路径**（Gaussian smoothing path），其噪声调度（noise schedule）与信道噪声水平单调对应。
  - 推导出该路径上的**闭式解析教师速度场**（teacher velocity field）。
  - 使用 **Conditional Flow Matching (CFM)** 训练一个深度神经网络作为学生向量场 $v_\theta(x, t)$，学习该速度场。
  - 解码过程变为求解一个从 $t^*$ 到 $t=1$ 的**确定性常微分方程 (ODE)**，无需随机采样。
- **统一框架适用于多种信道**：
  - 通过 **Linear MMSE Equalization** 和 **SVD 处理**，可将 Rayleigh 衰落和 MIMO 信道转换为等效的 AWGN 信道。
  - 仅需调整有效噪声方差以确定新的着陆时间 $t^*$，即可复用在 AWGN 上训练的同一速度场，**无需重新训练**。

### 相比现有方法的优势
- **低延迟**：解码仅需少量（如 10 步）确定性 ODE 步骤，远少于扩散模型所需的数十至数百步。
- **高性能**：在 PSNR、MS-SSIM 等指标上显著优于传统方法和深度学习基线。
- **鲁棒性强**：在低 SNR 下性能下降平缓，无“悬崖效应”。
- **通用性强**：单一模型可适应 AWGN、Rayleigh、MIMO 等多种信道。
- **物理可解释性**：LTT 范式将信道噪声与生成流动态紧密结合，具有清晰的物理意义。

---

## 2. 核心实验方法和设置

### 数据集
- **MNIST**：60,000 训练 + 10,000 测试，28×28 灰度手写数字。
- **Fashion-MNIST**：同上，服装图像。
- **DIV2K**：800 训练 + 100 验证，自然图像，裁剪为 256×256。

### 实验设置
- **信道模型**：AWGN、Rayleigh 衰落（标量复数）、2×2 MIMO。
- **带宽效率**：固定每张图像的复数信道使用次数（complex channel uses），确保与基线公平比较。
- **模型架构**：学生速度场 $v_\theta$ 采用 U-Net 结构（见 Table I），包含残差块、自注意力机制和时间嵌入。
- **训练细节**：
  - 最大噪声水平 $\sigma_{\text{max}} = 1.0$。
  - 批大小：MNIST/Fashion-MNIST 为 64，DIV2K 为 32。
  - 学习率：$1 \times 10^{-3}$，训练 50 轮。
  - ODE 步数：默认 10 步。

### 评估指标
- **PSNR**：衡量像素级保真度，越高越好。
- **MS-SSIM**：多尺度结构相似性，反映视觉感知质量，越高越好。
- **LPIPS**：基于深度特征的感知距离，越低越好。
- **$\Delta$PSNR**：重建图像相对于直接接收的噪声图像的 PSNR 增益。

### 基线方法对比
- **JPEG2000 + LDPC**：传统分离式编码方案。
- **DeepJSCC**：端到端深度联合信源信道编码。
- **CDDM**：基于扩散模型的生成解码器。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 DIV2K 在 SNR=20 dB 为例）
- **AWGN 信道**：
  - 相比 CDDM：PSNR 提升 **26.6%**，MS-SSIM 提升 **53.2%**。
  - 相比 DeepJSCC：PSNR 提升 **28.3%**，MS-SSIM 提升 **59.6%**。
- **Rayleigh 衰落信道**：
  - 相比 CDDM：PSNR 提升 **19.4%**，MS-SSIM 提升 **48.7%**。
  - 相比 DeepJSCC：PSNR 提升 **20.7%**，MS-SSIM 提升 **50.8%**。
- **与 JPEG2000+LDPC 对比**：
  - 在 AWGN 下 PSNR/MS-SSIM 提升 **16.9%/24.9%**。
  - 在 Rayleigh 下提升 **9.9%/13.1%**。

### 与基线方法的对比结果
- **性能全面领先**：在所有数据集（MNIST、Fashion-MNIST、DIV2K）和所有信道（AWGN、Rayleigh、MIMO）上，LTT 解码器在 PSNR、MS-SSIM、LPIPS 等指标上均一致优于所有基线。
- **抗“悬崖效应”**：JPEG2000+LDPC 在低 SNR 下性能急剧下降，而 LTT 性能随 SNR 提升持续改善。
- **感知质量优越**：视觉对比（Fig. 5）显示，LTT 重建图像边缘更锐利、纹理更干净、几何细节更忠实，而 DeepJSCC 模糊严重，JPEG2000+LDPC 存在压缩伪影。

### 消融实验结果
- **ODE 步数影响（Table V）**：
  - 当 ODE 步数从 2 增加到 10 时，PSNR 和 MS-SSIM 显著提升。
  - 步数超过 10 后，性能提升趋于饱和（PSNR 波动在 30.1–30.5 dB），但推理延迟几乎线性增长（从 0.18s 到 1.80s）。
  - **结论**：10 步是精度与效率的最佳平衡点。
- **调度器分析（Table VI）**：
  - 着陆时间 $t^*$ 与 SNR 呈单调关系：SNR 越高，$t^*$ 越大（即起始点越接近干净图像）。
  - 这验证了 LTT 范式的有效性，实现了信道条件与生成动力学之间的原则性链接。

---

## 4. 关键结论和发现

### 主要发现
1. **LTT 范式有效**：将物理信道视为生成流中的“着陆”步骤，能够紧密耦合信道特性与生成先验，实现高效解码。
2. **FM 替代扩散模型**：基于 Flow Matching 的确定性 ODE 解码器在保持甚至超越扩散模型性能的同时，大幅降低了计算复杂度和解码延迟。
3. **统一解码框架可行**：通过 MMSE 预处理，可将多种线性高斯信道（Rayleigh, MIMO）统一为 AWGN 等效形式，从而复用同一训练好的解码器，实现跨信道泛化。
4. **理论一致性**：在标量高斯信道下，LTT 解码器退化为一种线性估计器，在高 SNR 下渐近最优，与经典估计理论一致。

### 方法的局限性
- **依赖 MMSE 预处理**：对于非线性或非高斯信道，当前框架可能需要额外的适配模块。
- **训练假设**：虽然解码器可泛化，但训练仍基于 AWGN 假设，若信道差异过大，性能可能受限。
- **计算成本仍存**：尽管比扩散模型快，但 ODE 求解仍涉及多次神经网络前向传播，对极低功耗设备仍有挑战。

### 未来工作方向
- 将 LTT 框架扩展至非线性信道或更复杂的信道模型。
- 探索更高效的 ODE 求解器或隐式建模方法以进一步降低延迟。
- 研究发射端的联合优化，实现真正的端到端 LTT 通信系统。
- 探索在视频传输或其他模态（如语音）中的应用。

</details>

---

### 12. [TeleMem: Building Long-Term and Multimodal Memory for Agentic AI](https://arxiv.org/abs/2601.06037)

**Authors**: Chunliang Chen, Ming Guan, Xiao Lin, Jiaxu Li, Qiyi Wang, Xiangyu Chen, Jixiang Luo, Changzhi Sun, Dell Zhang, Xuelong Li  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.06037v1  

#### Abstract
Large language models (LLMs) excel at many NLP tasks but struggle to sustain long-term interactions due to limited attention over extended dialogue histories. Retrieval-augmented generation (RAG) mitigates this issue but lacks reliable mechanisms for updating or refining stored memories, leading to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**TELEMEM: Building Long-Term and Multimodal Memory for Agentic AI**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **Retrieval-Augmented Generation (RAG)** 的长期记忆系统在实际应用中面临三大挑战：
1. **用户画像不一致**：依赖预定义 schema 的方法容易产生 **schema-driven hallucinations**（即模型虚构未提及的信息），且难以处理稀疏对话信息。
2. **写入效率低下**：每轮对话都触发检索和决策流程，导致频繁的 API 调用、高延迟和低吞吐量。
3. **多模态推理能力弱**：大多数系统仅支持文本记忆，无法有效整合视频等非文本模态信息进行长期推理。

### 🚀 提出的新方法与创新
作者提出 **TELEMEM** —— 一个统一的、支持长时程与多模态的记忆系统，其核心创新如下：

#### （1）**叙事动态提取机制（Narrative Dynamic Extraction）**
- 不依赖固定 schema，而是从对话中提取“仅由对话支撑”的叙述单元作为记忆条目。
- 避免了对用户属性的无依据推断（hallucination），确保记忆真实可靠。
- 支持角色特定（role-specific）的 profile memory（如 `Muser` 和 `Mbot`），提升个性化建模能力。

#### （2）**结构化写入流水线（Structured Writing Pipeline）**
采用四阶段批处理流程来构建事件记忆（event memory）：
1. **Summarization**：将每轮对话摘要为若干文本片段；
2. **Retrieval**：对每个摘要检索相关历史记忆；
3. **Clustering**：全局聚类语义相似的记忆项；
4. **Decision**：由 LLM 对每个聚类决定 `add/delete/update/no-op` 操作。

> ⚡ 优势：显著减少冗余写入、降低 token 开销、提高存储效率，并实现更连贯的记忆整合。

#### （3）**多模态记忆模块 + ReAct-style Reasoning**
- 将视频流切分为 10 秒片段，生成两种记忆：
  - **Event Memory**：VLM 自动生成字幕描述事件；
  - **Key-Value Object Memory**：记录人物/物体的身份与状态变化。
- 引入 **ReAct-style agent** 进行多模态读取，支持三种工具调用：
  - `video.retrieval`：定位相关视频时间段；
  - `video.rag`：基于文本记忆回答问题；
  - `video.qa`：在指定视频片段上执行视觉问答（VQA）。

> 🔁 实现了完整的 **observe-think-act** 循环，适用于复杂视频内容的长期理解任务。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **ZH-4O Benchmark**：
  - 包含 28 场真实的中文人机角色扮演对话；
  - 平均每场约 600 轮对话；
  - 提供 1,068 个选择题用于测试记忆召回能力（probing questions）。

### 🧪 实验设置
- 所有方法均使用 **Qwen3-8B** 作为 backbone LLM；
- 向量表示使用 **Qwen3-8B-embedding**；
- 统一 prompt 模板以控制变量，确保公平比较。

### 🎯 评估指标
- **QA Accuracy (%)**：在 ZH-4O 上的选择题准确率；
- **Token Usage**：总输入输出 token 数量；
- **Speed (Throughput)**：内存操作速度（倍数加速）。

### 🔁 基线方法对比
| 类型 | 方法 |
|------|------|
| 基础范式 | `Long context LLM`, `RAG` |
| 先进记忆系统 | `Mem0`, `Memobase`, `A-Mem`, `MOOM` |

---

## 3. 主要实验结果和性能指标

### 📊 性能对比（ZH-4O 上的 QA Accuracy）
| 方法 | 准确率 (%) |
|------|-----------|
| RAG | 62.45 |
| Mem0 | 70.20 |
| MOOM | 72.60 |
| A-Mem | 73.78 |
| Memobase | 76.78 |
| Long context LLM | 84.92 |
| **TELEMEM (Ours)** | **86.33** ✅ |

> 💡 **结论**：TELEMEM 在所有方法中表现最佳，超越了直接使用完整上下文的 `Long context LLM`，说明其记忆压缩与检索机制优于原始日志回放。

### ⚙️ 效率与速度优势
- 相比 **Mem0**：
  - **准确率提升 19%**（相对提升）；
  - **token 使用减少 43%**；
  - **运行速度快 2.1×**。

> 📉 表明 TELEMEM 在保持更高性能的同时，大幅优化了资源消耗和响应延迟。

### 🔍 消融实验（文中虽未单独列出表格，但可通过分析得出）
- **Narrative extraction** → 显著降低 hallucination，提升 profile memory 可靠性；
- **Batched writing pipeline** → 减少重复写入，提升一致性；
- **Multimodal ReAct agent** → 支持细粒度视频理解，在需要视觉 grounding 的任务中表现优越。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **传统 RAG 缺乏更新机制**，难以应对用户偏好演化；而 TELEMEM 的闭环写入流程支持动态记忆维护。
2. **schema-free 的 narrative extraction 更灵活、更真实**，避免了强制填充字段带来的错误。
3. **批处理 + 聚类 + 决策流水线** 极大提升了写入效率和记忆质量。
4. **多模态记忆 + ReAct 工具调用** 成功实现了对长视频内容的精准观察与推理。

### ⚠️ 局限性
- 当前多模态部分主要依赖 VLM 字幕生成，可能丢失细节；
- 视频分段策略（10秒）可能割裂连续动作；
- 写入决策仍依赖 LLM 判断，存在误删/误更风险；
- 尚未公开大规模跨场景验证（如客服、教育等）。

### 🔮 未来工作方向
- 探索可学习的记忆衰减机制（forgetting curve）；
- 引入参数化记忆（parametric memory）与非参数化结合；
- 扩展至音频、传感器等更多模态；
- 支持多智能体协同记忆共享与冲突解决。

---

## ✅ 总结
**TELEMEM** 是一种面向 Agent AI 的新型统一记忆架构，通过：
- **叙事驱动的记忆提取**
- **高效的批处理写入管道**
- **支持 ReAct 的多模态推理能力**

成功解决了现有记忆系统的三大痛点，在 **准确性、效率、速度** 三个维度全面超越主流基线（尤其是 Mem0）。其实验结果证明了结构化、动态、多模态记忆对于构建可持续交互 AI 的关键作用，为下一代 Agentic AI 的长期认知能力提供了重要基础设施。

</details>

---

### 13. [Learning Minimally-Congested Drive Times from Sparse Open Networks: A Lightweight RF-Based Estimator for Urban Roadway Operations](https://arxiv.org/abs/2601.06124)

**Authors**: Adewumi Augustine Adepitan, Christopher J. Haruna, Morayo Ogunsina, Damilola Olawoyin Yussuf, Ayooluwatomiwa Ajiboye  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.06124v1  

#### Abstract
Accurate roadway travel-time prediction is foundational to transportation systems analysis, yet widespread reliance on either data-intensive congestion models or overly na\"ive heuristics limits scalability and practical adoption in engineering workflows. This paper develops a lightweight estimator ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learning Minimally-Congested Drive Times from Sparse Open Networks: A Lightweight RF-Based Estimator for Urban Roadway Operations*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前交通工程中，**travel time prediction** 存在显著的方法论鸿沟：
- **高精度方法**（如基于大规模GPS、深度学习模型）虽然准确（MAPE 3%-17%），但依赖昂贵的专有数据和大量计算资源，难以普及；
- **简化方法**（如基于Euclidean距离或speed-limit traversal time）虽易于实施，却因忽略信号灯、转向、交叉口延误等因素，系统性低估实际出行时间（MAPE > 20%），影响规划决策。

该论文旨在填补这一“中间地带”——提出一种**既准确又轻量、可广泛部署的出行时间预测方法**。

---

### 🚀 提出的新方法与创新思路
作者提出了一种基于 **Random Forest (RF)** 的轻量级回归模型，用于预测城市道路网中的 **minimally-congested car travel times**。其核心思想是：

> 利用 **sparse open network data**（来自 OpenStreetMap）构建基础路径与特征，并通过机器学习对最短路径 traversal time 基线进行偏差校正。

#### 方法流程（Pipeline）：
1. **Network Construction**：从 OSM 构建 drivable road network（使用 OSMnx）；
2. **Baseline Path Calculation**：使用 Dijkstra 算法求解最小 traversal time 路径（边权为长度 / 速度限制）；
3. **Feature Engineering**：
   - **Baseline**: `tnaive`（Dijkstra 得到的 traversal time）
   - **Traffic Controls**：沿途 stop signs、traffic signals、crossings、give way、mini roundabouts 的计数
   - **Turning Movements**：left/right/slight/U-turn 的数量（按角度分类）
4. **Model Training**：以 Google Maps API 在凌晨3点获取的 “BEST_GUESS” travel time 作为 reference，训练 RF 回归模型，学习如何修正 `tnaive`。

---

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **准确性** | MAPE 降至 8.41%，接近 state-of-the-art 深度学习水平（3%-17%），远优于传统启发式方法（21.15%） |
| **资源效率** | 不依赖实时拥堵数据、专有传感器或GPU集群，仅需开放数据 + 轻量模型 |
| **实用性** | 可运行于普通硬件，支持 metropolitan-scale 分析，适用于资源受限机构 |
| **透明性与可复现性** | 完全基于 open data 和开源工具（OSM, OSMnx, scikit-learn），提升 reproducibility |
| **抗稀疏性能力** | 即使 OSM 中 traffic control 标注不完整，仍能有效提取信号并提升预测 |

---

## 2. 核心实验方法和设置

### 📊 数据集
| 数据来源 | 内容说明 |
|--------|--------|
| **OpenStreetMap (OSM)** | 使用 OSMnx 提取洛杉矶都市区道路网络（含 speed limits, turn restrictions, 控制设施等） |
| **Google Maps Routes API** | 获取 origin-destination (OD) 对的真实 travel time（departure time = 凌晨3:00 AM，traffic_model = BEST_GUESS） |
| **Uber Movement Data** | 用于筛选真实存在的 trip 模式，确保生成的 OD 对具有现实意义 |
| **研究区域** | Los Angeles County 及其 urban center boundary，共保留 41,360 个有效 OD 对 |

---

### ⚙️ 实验设置
- **目标场景**：minimally congested conditions（模拟自由流状态）
- **训练/测试划分**：标准 80%/20% 分割
- **交叉验证**：5-fold CV 验证稳定性
- **超参数调优**：randomized grid search，优化目标为 MAE
- **候选模型比较**：Decision Tree (DT), Random Forest (RF), Gradient Boosting (GB), AdaBoost

---

### 📏 评估指标
使用六项指标全面评估性能：
- **MAPE**（Mean Absolute Percentage Error）
- **MAE**（Mean Absolute Error，秒）
- **MSE**（Mean Squared Error）
- **Bias (δ)**：平均预测偏差（检验是否系统性高估/低估）
- **R²**（Explained Variance）
- **Average Pairwise Ratio (APR)**

---

### 🔁 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| **Naive Traversal Time (TT)** | Dijkstra 最小 traversal time（length / speed limit），无任何操作延迟修正 |
| **Other ML Models** | DT, GB, AdaBoost 作为对照，验证 RF 的优越性 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1 & Fig. 2）

| Model | MAPE (%) | MAE (s) | δ (bias, s) | R² |
|-------|----------|---------|------------|-----|
| Naive TT | 21.15 | 183.68 | -182.85 | 0.74 |
| **RF (Ours)** | **8.41** | **75.32** | **+0.38** | **0.93** |
| GB | 7.86 | 71.99 | -19.20 | 0.93 |
| DT | 9.00 | 80.00 | +0.13 | 0.93 |
| AdaBoost | 8.20 | 74.00 | -9.76 | 0.93 |

> 注：所有 ML 方法均显著优于 naive baseline；RF 在误差与偏差之间取得最佳平衡。

---

### 🔍 与基线方法对比结果
- **MAPE 下降超过 60%**：从 21.15% → 8.41%
- **MAE 减少约 2.4 倍**：从 184s → 75s
- **MSE 减少 4 倍**：从 ~48k → ~12k s²
- **系统性偏差完全消除**：naive 方法严重低估（-183s），而 RF 平均偏差仅为 +0.38s（p=0.76，无统计显著性）
- **解释方差大幅提升**：R² 从 0.74 → 0.93，说明模型捕捉了更多真实变化

---

### 🔤 消融实验与特征重要性分析（Feature Importance）
- **naive travel time** 是最重要特征（贡献约 68% 的 R²），表明基础路径信息仍是核心；
- **traffic signals** 是最关键的控制因素，其次为 stop signs 和 crossings；
- **roundabouts** 和 **give way** 影响较小，可能因其在网络中分布稀疏；
- **turning movements** 整体贡献约 19% 的解释力，其中 left/right turns 较重要，U-turns 极少出现故影响微弱；
- 尽管 OSM 数据存在标注缺失（sparse annotation），模型仍能从中提取有效信号，证明方法对数据稀疏性鲁棒。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **轻量级 RF 模型可在仅使用 open data 的前提下，实现接近 state-of-the-art 的预测精度**（MAPE 8.41%），显著优于传统规划方法。
2. **通过引入 traffic control 和 turning movement 特征，可有效纠正 Dijkstra traversal time 的系统性低估问题**。
3. **Random Forest 在 bias-variance trade-off 上表现最优**：相比 GB 和 AdaBoost 更少出现 under-prediction bias，相比单棵 Decision Tree 方差更低。
4. **方法具备良好的 k-fold 稳定性和泛化能力**：各 fold 的 MAE 波动极小（73.2–75.3s），无明显过拟合迹象。
5. **适用于 metropolitan-scale 应用**：计算开销低，适合大规模 OD matrix 生成，支持 accessibility analysis、network performance evaluation 等规划任务。

---

### ⚠️ 局限性
1. **仅适用于 minimally congested conditions**：未建模高峰时段的 congestion dynamics，高峰期预测可能偏乐观；
2. **依赖 OSM 数据质量**：不同地区 traffic control tagging 完整性差异大，可能影响迁移效果；
3. **缺乏显式的时空建模**：目前未考虑 day-of-week、天气、事件等动态因素；
4. **route mismatch 问题**：模型基于 OSM Dijkstra 路径提取特征，但 Google 返回的是其私有算法路径，两者可能存在差异（尽管作者认为共享相似拓扑结构）；
5. **区域适应性待验证**：模型参数是否可直接迁移到其他城市尚需实证。

---

### 🔮 未来工作方向
1. **扩展至多时段建模**：收集更长时间跨度的 reference data，区分 peak vs off-peak 模式；
2. **融合更多 open data sources**：
   - crowd-sourced traffic reports
   - public transit schedules
   - weather data
   - satellite imagery（用于补全 missing controls）
3. **开发 spatial imputation 方法**：利用 spatial statistics 或 ML 技术推断未标注的 traffic control 设施；
4. **引入 Transfer Learning**：在数据丰富的城市训练 base model，通过少量本地 reference data 微调至新城市；
5. **集成到 real-time routing systems**：结合 lightweight congestion proxy（如 historical patterns），打造高效且准确的 routing engine。

---

## 总结
该论文成功构建了一个**兼顾准确性、可访问性与可扩展性的中间路线方法**，为资源有限的城市交通部门提供了一种实用工具。它不仅提升了 open data 在 transportation planning 中的应用价值，也为推动公平、透明、证据驱动的决策提供了技术支持。随着 OSM 数据质量和计算基础设施持续改善，此类 lightweight ML 方法有望成为 smart mobility 生态系统的基石组件。

</details>

---

### 14. [Physics-Informed Tree Search for High-Dimensional Computational Design](https://arxiv.org/abs/2601.06444)

**Authors**: Suvo Banik, Troy D. Loeffler, Henry Chan, Sukriti Manna, Orcun Yildiz, Tom Peterka, Subramanian Sankaranarayanan  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.06444v1  

#### Abstract
High-dimensional design spaces underpin a wide range of physics-based modeling and computational design tasks in science and engineering. These problems are commonly formulated as constrained black-box searches over rugged objective landscapes, where function evaluations are expensive, and gradients...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Physics-Informed Tree Search for High-Dimensional Computational Design**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
该论文针对**高维、连续、物理约束下的黑盒优化问题**，这类问题广泛存在于材料设计、晶体结构搜索、势能模型拟合和工程设计等领域。传统优化方法面临以下挑战：
- 高维空间中的“维度灾难”导致采样效率低下；
- 目标函数非凸、多模态、无梯度或梯度不可靠；
- 物理约束难以融入优化过程；
- 函数评估成本高昂（如 DFT、FEM 模拟）。

### **提出的新方法与创新点**
作者提出了一个**物理信息引导的蒙特卡洛树搜索框架（Physics-Informed Monte Carlo Tree Search, MCTS）**，将强化学习中的决策树思想扩展到连续、高维科学优化任务中。其核心创新包括：

1. **方向性采样机制（Directional Sampling with Logistic Surrogate）**  
   引入基于历史搜索轨迹的**Logistic Surrogate 模型**，通过逻辑回归对成功方向进行建模，主动优化搜索方向和步长，显著提升高维空间中的采样效率。

2. **深度依赖的窗口缩放（Depth-based Window Scaling）**  
   随着树深度增加，动态缩小采样半径 $ r_{\text{max}} $，实现从**全局探索到局部精细优化**的平滑过渡，避免陷入浅层局部最优。

3. **分层树群策略（Hierarchical Batching of Trees）**  
   采用**全局树批（global batch）与局部树批（local batch）结合**的并行策略：
   - 全局树用于多样化探索不同能量盆地；
   - 局部树继承优秀解，进行精细化搜索；
   - 支持动态剪枝与切换机制，提高收敛稳定性。

4. **物理一致性集成**  
   在搜索过程中嵌入物理约束模块（如晶格对称性、化学有效性），确保生成的候选解在物理上合理。

### **相比现有方法的优势**
- **优于传统元启发式算法（如 WOA、PSO）**：在多模态、高维问题上表现出更强的全局探索能力和更低的方差；
- **超越标准 MCTS 变体**：解决了经典 MCTS 在连续空间中采样低效、易陷入局部的问题；
- **优于贝叶斯优化（BO）等代理模型方法**：在高维下仍保持可扩展性，且无需构建全局代理模型；
- **通用性强**：适用于结构设计、势能拟合、连续力学设计等多种场景。

---

## **2. 核心实验方法和设置**

### **使用的数据集与测试函数**
实验分为三类基准与实际应用：

#### **(1) 标准优化测试函数（Benchmark Functions）**
共使用 23 个标准函数，分为三类：
- **单峰函数（Unimodal, F1–F7）**：Sphere, Rosenbrock, Schwefel 1.2 等（30D）
- **多峰函数（Multimodal, F8–F13）**：Rastrigin, Ackley, Griewank 等（30D）
- **固定维度复合函数（Fixed-dimensional, F14–F23）**：Six-Hump Camelback, Shekel, Hartmann 等（2–6D）

#### **(2) 实际科学计算问题**
- **晶体结构优化**：
  - Au₃₅ 纳米团簇（105D）
  - 二维硅烯（Silicene）多种相态（9–24D）
  - 体相硅（Bulk Si, 30D）
- **势能模型拟合（Interatomic Potential Fitting）**：
  - 基于 DFT 数据拟合 Al 纳米团簇的 Tersoff 势（13D 参数空间）
- **连续尺度工程设计**：
  - 焊接梁优化（Welded Beam Design, 4D）
  - 压力容器设计（Pressure Vessel Design, 4D）

### **实验设置与评估指标**
- 所有实验运行多次（通常 30 次独立运行）以评估鲁棒性和方差；
- **评估指标**：
  - 最优目标值（Best Objective Value）
  - 平均值与标准差（Mean ± Std）
  - 收敛速度（Evaluations to Convergence）
  - 物理一致性（如晶格误差、配位数匹配）
  - 测试集上的预测误差（MAE of energy/forces）

### **基线方法对比**
- **Metaheuristics**: Whale Optimization Algorithm (**WOA**)、Particle Swarm Optimization (**PSO**)、Genetic Algorithm (**GA**)、Gravitational Search Algorithm (**GSA**) 等
- **随机搜索（Random Search）**
- **经典 MCTS 变体（Hypersphere Sampling）**
- **文献报道的最优解**（如 Deb, Lee & Geem, CBO 等）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

| 任务 | 方法 | 最佳目标值（或误差） | 评价 |
|------|------|------------------------|------|
| **F1 (Sphere, 30D)** | MCTS (Logistic) | **0.0** | 超越数值精度，优于 Hypersphere MCTS (10⁻²²) |
| **F4 (Rosenbrock, 30D)** | MCTS | ~10⁻¹⁴ | 显著优于 WOA 和 PSO |
| **F11 (Ackley, 30D)** | MCTS | ~10⁻⁹ | 收敛稳定，方差小 |
| **F14 (Shekel, 4D)** | MCTS | **0.998**（理论最优） | 所有运行均达到全局最优 |
| **Au₃₅ 团簇能量最小化** | MCTS | <12,000 次评估收敛 | 结构与已知全局最小完全一致（RDF 匹配） |
| **Silicene 多相搜索** | MCTS | 晶格误差 ~10⁻⁴ Å，坐标误差 ~10⁻⁵ Å | 成功恢复 FS, LBS, TDS 等亚稳相 |
| **Al Tersoff 势拟合** | MCTS | **53.45 meV/atom** (能量 MAE), **212.46 meV/Å** (力 MAE) | 优于 HYBOP、MEAM、EAM、SNAP 等主流势函数 |
| **焊接梁设计成本最小化** | MCTS | **1.697958** | 优于所有文献方法（包括 CBO、GA、WOA） |
| **压力容器设计成本最小化** | MCTS | **5898.1359** | 发现新可行区域，成本更低 |

### **与基线方法的对比结果**
- 在 **21 out of 23 个基准函数** 上，MCTS 表现优于或等于 WOA 和 PSO；
- 在多峰函数（如 Schwefel, F8）上，MCTS 找到更深的能量谷（mean = -7981 vs -7620），显示更强的逃逸能力；
- 在固定维度函数上，MCTS 实现**零方差收敛**，而 WOA/PSO 存在较大波动；
- 在工程设计问题中，MCTS 不仅找到更优解，还通过 PCA 分析揭示其探索了**前人未发现的设计区域**。

### **消融实验结果**
- **Logistic Surrogate vs Hypersphere Sampling**：
  - Logistic 方案在大多数任务中表现更好，尤其在高维复杂地形中；
  - Hypersphere 更适合噪声较大的环境，作为稳健 fallback；
- **Hierarchical Tree Batching**：
  - 单棵树易陷入局部；
  - 多树并行显著降低运行间方差，提升全局覆盖率；
- **Window Scaling**：
  - 无缩放时收敛慢且不稳定；
  - 自适应缩放使早期广域探索、后期精准微调成为可能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **MCTS 可有效扩展至高维连续优化任务**，通过引入方向学习、窗口缩放和群体策略，克服了传统 MCTS 的局限。
2. **物理信息可以自然地嵌入搜索过程**，不仅加速收敛，还能保证解的物理合理性。
3. 所提方法在**合成函数与真实科学问题**上均表现出色，验证了其作为通用优化器的潜力。
4. 该框架实现了**评估高效性与物理保真性的统一**，特别适合昂贵模拟驱动的设计流程。

### **方法的局限性**
- 当前为纯黑箱模式，未利用多保真度信息或多尺度建模；
- 不支持跨任务的知识迁移（如不同材料间的参数先验共享）；
- 约束处理依赖惩罚项，尚未集成生成式约束满足机制；
- 计算开销高于简单启发式方法，在低维问题中优势不明显。

### **未来工作方向**
1. **融合预训练物理编码器**（foundation-guided search），引导探索方向；
2. 构建**多智能体共享记忆的树群系统**，实现跨任务知识积累；
3. 与实验闭环集成，用于**自主机器人实验平台**的数据采集与优化；
4. 探索与其他 RL 框架（如 PPO、SAC）结合的可能性，进一步提升策略学习能力。

---

> ✅ **总结一句话**：本文提出了一种可扩展、可解释、物理一致的 **physics-informed MCTS 框架**，在高维科学设计任务中实现了**高效、稳健、高质量的优化性能**，为 AI for Science 提供了一个新的决策层范式。

</details>

---

### 15. [Mosaic: Unlocking Long-Context Inference for Diffusion LLMs via Global Memory Planning and Dynamic Peak Taming](https://arxiv.org/abs/2601.06562)

**Authors**: Liang Zheng, Bowen Shi, Yitao Hu, Jiawei Zhang, Ruofan Li, Sheng Chen, Wenxin Li, Keqiu Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.06562v1  

#### Abstract
Diffusion-based large language models (dLLMs) have emerged as a promising paradigm, utilizing simultaneous denoising to enable global planning and iterative refinement. While these capabilities are particularly advantageous for long-context generation, deploying such models faces a prohibitive memor...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Mosaic: Unlocking Long-Context Inference for Diffusion LLMs via Global Memory Planning and Dynamic Peak Taming*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的 **autoregressive (AR)** 大语言模型推理系统在内存管理上高度优化了 **KV-Cache**，然而新兴的 **diffusion-based LLMs (dLLMs)** 具有完全不同的计算范式：
- dLLMs 采用非自回归、全局并行去噪方式生成文本，每一步都需要对整个序列进行双向注意力计算；
- 因此，其内存瓶颈不再是持久的 KV-Cache，而是每步都需重新计算的 **transient activations**（如 FFN 和 logits 输出）；
- 此外，这些激活的内存峰值会随 **mask ratio** 动态切换（高 mask 时 logits 占主导，低 mask 时 FFN 占主导），导致传统静态内存规划失效；
- 同时，通用框架（如 `torch.compile`）存在 **graph breaks** 和 **external fragmentation**，进一步加剧内存浪费。

综上，**现有 LLM 推理系统无法有效支持长上下文 dLLM 推理**，存在严重的内存容量瓶颈。

---

### 🚀 提出的新方法：Mosaic

Mosaic 是一个专为 dLLMs 设计的 **内存高效推理系统**，通过三大核心技术实现全局动态内存管理：

| 技术 | 创新点 |
|------|--------|
| **Mask-only Logits Kernel** | 只对被掩码的 token 计算 logits，避免对已生成 token 的冗余计算；通过 **gather-GEMM 融合内核**直接处理稀疏输入，无需中间缓冲区，节省内存且提升效率。 |
| **Lazy Chunking Optimizer** | 引入在线启发式搜索策略，在运行时根据当前内存压力动态决定是否对 FFN 或 logits 操作进行分块（chunking），仅在必要时才触发，最小化延迟开销。 |
| **Global Memory Manager** | 构建统一虚拟地址空间，基于完整计算图生命周期分析生成全局复用计划，结合 **VMM（Virtual Memory Management）** 实现物理页按需绑定，彻底消除外部碎片。 |

此外，系统引入 **Graph Registrar** 显式定义参数化计算图模板，确保全图可见性，支撑上述优化。

---

### 🔍 相比现有方法的优势

| 维度 | Mosaic 的优势 |
|------|----------------|
| **内存效率** | 平均降低 **2.71× 的 memory peak-to-average ratio**，显著缓解动态峰值问题。 |
| **可扩展性** | 在相同硬件下，最大支持的推理序列长度提升 **15.89–32.98×**。 |
| **性能无损** | 不牺牲精度与速度，反而将端到端延迟降低 **4.12%–23.26%**。 |
| **适应性强** | 支持 variable-length 输入与迭代级调度，适用于真实场景。 |

---

## 2. 核心实验方法和设置

### 📚 数据集与模型
由于目标是测试极限上下文能力，作者使用 **dummy input**（合成数据）来突破训练时的上下文限制，评估系统的理论上限。

**评估模型**：
- **LLaDA-8B**
- **Dream-7B**
- **LLaDA-MoE**

这些均为典型的 dLLM 模型，基于 Transformer 架构，采用 token-level 扩散机制。

---

### ⚙️ 实验设置

| 项目 | 配置 |
|------|------|
| **硬件平台** | NVIDIA GeForce RTX 3090 (24GB VRAM)<br>NVIDIA A100 (40GB VRAM) |
| **实现基础** | 基于 **vLLM** 框架构建，集成官方 PyTorch 模型代码，绕过标准 KV-Cache 流程 |
| **支持功能** | variable-length inference（varlen）、iteration-level scheduling |
| **输入配置** | 控制 prompt-to-output ratio $ r_p \in \{0.1, 0.5, 0.9\} $，对应不同初始 mask ratio $ r_m $ |

---

### 📊 评估指标

| 指标 | 定义 |
|------|------|
| **Per-step Latency** | 单个 diffusion step 的执行时间 |
| **Maximum Context Length ($ L_{max} $)** | 在不发生 OOM 的前提下能支持的最大序列长度 |
| **Memory Peak-to-Average Ratio (PAR)** | 衡量内存波动程度，越低越好 |
| **Reserved Memory Inflation Rate** | 实际保留内存 vs. 理论峰值的比例，反映碎片化程度 |

---

### 🆚 基线方法对比

| 基线 | 描述 |
|------|------|
| **Native** | 原始 PyTorch 实现，无任何推理优化 |
| **Mosaic-Torch** | 移植到 vLLM 框架 + varlen 支持，但无内存优化 |
| **Mosaic-Compile** | 在 Mosaic-Torch 基础上加入 `torch.compile`，代表通用编译优化 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 指标 | 结果 |
|------|------|
| **$ L_{max} $ 提升倍数** | 较 Native 提升 **32.98×**<br>较 Mosaic-Torch 提升 **26.74×**<br>较 Mosaic-Compile 提升 **15.89×** |
| **Memory PAR 降低** | 平均减少 **2.71×**，尤其在启用 chunking 后效果显著 |
| **延迟降低** | 相比 Mosaic-Torch/Mosaic-Compile，平均降低 **4.12%–23.26%** |
| **内存膨胀率** | Baseline 内存保留比理论峰值高出 21.78%–78.33%，而 Mosaic 几乎无额外开销 |

> 图 10–13 展示了在多种 $ r_p $ 设置下的 $ L_{max} $ 与 latency 对比，Mosaic 在所有场景中均取得最优表现。

---

### 🔍 消融实验结果（Ablation Study）

逐步添加组件验证各模块贡献（以 Mosaic-Torch 为起点）：

| 组件 | $ L_{max} $ 提升幅度 |
|------|------------------------|
| **+ Global Memory Manager** | 提升 **79.0% – 197.1%**<br>主因：全局复用 + in-place 优化（如替换 Dream 的 logits shift） |
| **+ Mask-only Logits Kernel** | 进一步提升 **88.9% – 114.3%**<br>主因：消除未遮蔽 token 的 logits 冗余计算 |
| **+ Lazy Chunking Optimizer** | 再提升 **221.3% – 673.3%**<br>主因：有效压制动态内存峰值 |

> 如图 14 所示，lazy chunking 是提升最长上下文能力的关键。

---

#### ✅ Chunking 策略有效性（vs. Fixed Chunking）
- **Mosaic-FC**：静态应用最大 chunk 数
- **Mosaic（ours）**：仅在内存不足时动态启用 chunking

→ 在短上下文中，Mosaic 实现 **5.5%–21.5% 的延迟降低**，证明“lazy”设计的有效性。

---

#### ✅ Search 与 Planning 效率对比

| 方法 | 性能 | 延迟开销 |
|------|------|----------|
| **Brute-force Search (Mosaic-BF)** | $ L_{max} $ 相同 | 搜索延迟高 **300–400 倍** |
| **Bottleneck-driven Heuristic (Ours)** | 相同 | 仅占 BF 的 **0.28%–0.32%** |
| **ILP Planning (Mosaic-ILP)** | $ L_{max} $ 相同 | 规划延迟为 ours 的 **23–1000 倍** |
| **First-fit Planning (Ours)** | 相同 | 延迟仅为 ILP 的 **0.1%–4.3%** |

> 表明所提启发式算法在保持最优性的前提下极大降低了元开销。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **dLLM 的内存瓶颈已从 KV-Cache 转向 transient activations**，尤其是 FFN 与 logits 的动态峰值交替。
2. **通用推理系统（如 torch.compile）因 graph breaks 导致严重内存碎片**，不适合 dLLM。
3. **mask-only 计算是可行且高效的**，通过融合 gather-GEMM 可避免中间缓冲。
4. **动态 peak taming 必须结合运行时感知的 chunking 策略**，静态分块代价高昂。
5. **全局内存规划 + 虚拟内存映射** 可彻底解决 external fragmentation。

---

### ⚠️ 方法的局限性

- 当前实现依赖对特定模型结构的理解（如明确识别 FFN/logits 节点），自动化程度有待提高；
- 所有实验基于 dummy input，尚未在真实任务（如代码补全、长文档生成）中验证实用性；
- 对更复杂扩散流程（如非均匀 masking schedule）的支持未深入探讨；
- 编译器集成仍需手动注册 graph template，未来可探索自动捕捉机制。

---

### 🔮 未来工作方向

1. 将 Mosaic 扩展至多模态 diffusion models（如图文生成）；
2. 结合 early-exit 或 adaptive step skipping 进一步压缩总计算量；
3. 开发全自动 graph registration 工具链，降低部署门槛；
4. 探索 CPU-GPU 协同下的 offloading 策略，支持超大规模上下文；
5. 在真实应用场景中评估生成质量与吞吐量 trade-off。

---

## 总结

> **Mosaic 成功揭示了 dLLM 与 AR-LLM 在内存行为上的根本差异，并提出了一套系统级解决方案——从 mask-only kernel 到 lazy chunking 再到 global VMM 管理，实现了长上下文推理能力的数量级跃升，同时提升了效率与可扩展性。该工作为 diffusion-based LLM 的实用化铺平了道路。**

</details>

---

### 16. [Segmental Advantage Estimation: Enhancing PPO for Long-Context LLM Training](https://arxiv.org/abs/2601.07320)

**Authors**: Xue Gong, Qi Yi, Ziyuan Nan, Guanhua Huang, Kejiao Li, Yuhao Jiang, Ruibin Xiong, Zenan Xu, Jiaming Guo, Shaohui Peng, Bo Zhou  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.07320v1  

#### Abstract
Training Large Language Models (LLMs) for reasoning tasks is increasingly driven by Reinforcement Learning with Verifiable Rewards (RLVR), where Proximal Policy Optimization (PPO) provides a principled framework for stable policy updates. However, the practical application of PPO is hindered by unre...

---

### 17. [PRISMA: Reinforcement Learning Guided Two-Stage Policy Optimization in Multi-Agent Architecture for Open-Domain Multi-Hop Question Answering](https://arxiv.org/abs/2601.05465)

**Authors**: Yu Liu, Wenxiao Zhang, Cong Cao, Wenxuan Lu, Fangfang Yuan, Diandian Guo, Kun Peng, Qiang Sun, Kaiyan Zhang, Yanbing Liu, Jin B. Hong, Bowen Zhou, Zhiyuan Ma  
**Category**: cs.AI  
**Published**: 2026-01-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.05465v1  

#### Abstract
Answering real-world open-domain multi-hop questions over massive corpora is a critical challenge in Retrieval-Augmented Generation (RAG) systems. Recent research employs reinforcement learning (RL) to end-to-end optimize the retrieval-augmented reasoning process, directly enhancing its capacity to ...

---

### 18. [N2N-GQA: Noise-to-Narrative for Graph-Based Table-Text Question Answering Using LLMs](https://arxiv.org/abs/2601.06603)

**Authors**: Mohamed Sharafath, Aravindh Annamalai, Ganesh Murugan, Aravindakumar Venugopalan  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.06603v1  

#### Abstract
Multi-hop question answering over hybrid table-text data requires retrieving and reasoning across multiple evidence pieces from large corpora, but standard Retrieval-Augmented Generation (RAG) pipelines process documents as flat ranked lists, causing retrieval noise to obscure reasoning chains. We i...

---

### 19. [SourceNet: Interpretable Sim-to-Real Inference on Variable-Geometry Sensor Arrays for Earthquake Source Inversion](https://arxiv.org/abs/2601.06320)

**Authors**: Zhe Jia, Xiaotian Zhang, Junpeng Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.06320v1  

#### Abstract
Inferring high-dimensional physical states from sparse, ad-hoc sensor arrays is a fundamental challenge across AI for Science, as they are complicated by irregular geometries and the profound Sim-to-Real gap in physical modeling. Taking earthquake source characterization as a representative challeng...

---

### 20. [HAS-VQ: Hessian-Adaptive Sparse Vector Quantization for High-Fidelity LLM Compression](https://arxiv.org/abs/2601.06959)

**Authors**: Vladimer Khasia  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.06959v1  

#### Abstract
Post-training quantization is essential for deploying Large Language Models (LLMs) on resource- constrained devices. However, standard integer quantization (e.g., INT4) fundamentally degrades per- formance by imposing a uniform grid on the heavy-tailed distribution of weight parameters, particularly...

---

### 21. [Mathematical Knowledge Graph-Driven Framework for Equation-Based Predictive and Reliable Additive Manufacturing](https://arxiv.org/abs/2601.05298)

**Authors**: Yeongbin Cha, Namjung Kim  
**Category**: cs.AI  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.05298v1  

#### Abstract
Additive manufacturing (AM) relies critically on understanding and extrapolating process-property relationships; however, existing data-driven approaches remain limited by fragmented knowledge representations and unreliable extrapolation under sparse data conditions. In this study, we propose an ont...

---

### 22. [SimLLM: Fine-Tuning Code LLMs for SimPy-Based Queueing System Simulation](https://arxiv.org/abs/2601.06543)

**Authors**: Jun-Qi Chen, Kun Zhang, Rui Zheng, Ying Zhong  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.06543v1  

#### Abstract
The Python package SimPy is widely used for modeling queueing systems due to its flexibility, simplicity, and smooth integration with modern data analysis and optimization frameworks. Recent advances in large language models (LLMs) have shown strong ability in generating clear and executable code, m...

---

### 23. [PDR: A Plug-and-Play Positional Decay Framework for LLM Pre-training Data Detection](https://arxiv.org/abs/2601.06827)

**Authors**: Jinhan Liu, Yibo Yang, Ruiying Lu, Piotr Piekos, Yimeng Chen, Peng Wang, Dandan Guo  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.06827v1  

#### Abstract
Detecting pre-training data in Large Language Models (LLMs) is crucial for auditing data privacy and copyright compliance, yet it remains challenging in black-box, zero-shot settings where computational resources and training data are scarce. While existing likelihood-based methods have shown promis...

---

### 24. [Solar Open Technical Report](https://arxiv.org/abs/2601.07022)

**Authors**: Sungrae Park, Sanghoon Kim, Jungho Cho, Gyoungjin Gim, Dawoon Jung, Mikyoung Cha, Eunhae Choo, Taekgyu Hong, Minbyul Jeong, SeHwan Joo, Minsoo Khang, Eunwon Kim, Minjeong Kim, Sujeong Kim, Yunsu Kim, Hyeonju Lee, Seunghyun Lee, Sukyung Lee, Siyoung Park, Gyungin Shin, Inseo Song, Wonho Song, Seonghoon Yang, Seungyoun Yi, Sanghoon Yoon, Jeonghyun Ko, Seyoung Song, Keunwoo Choi, Hwalsuk Lee, Sunghun Kim, Du-Seong Chang, Kyunghyun Cho, Junsuk Choe, Hwaran Lee, Jae-Gil Lee, KyungTae Lim, Alice Oh  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.07022v1  

#### Abstract
We introduce Solar Open, a 102B-parameter bilingual Mixture-of-Experts language model for underserved languages. Solar Open demonstrates a systematic methodology for building competitive LLMs by addressing three interconnected challenges. First, to train effectively despite data scarcity for underse...

---

### 25. [Thinking Before Constraining: A Unified Decoding Framework for Large Language Models](https://arxiv.org/abs/2601.07525)

**Authors**: Ngoc Trinh Hung Nguyen, Alonso Silva, Laith Zumot, Liubov Tupikina, Armen Aghasaryan, Mehwish Alam  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.07525v1  

#### Abstract
Natural generation allows Language Models (LMs) to produce free-form responses with rich reasoning, but the lack of guaranteed structure makes outputs difficult to parse or verify. Structured generation, or constrained decoding, addresses this drawback by producing content in standardized formats su...

---

### 26. [Adaptive Layer Selection for Layer-Wise Token Pruning in LLM Inference](https://arxiv.org/abs/2601.07667)

**Authors**: Rei Taniguchi, Yuyang Dong, Makoto Onizuka, Chuan Xiao  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.07667v1  

#### Abstract
Due to the prevalence of large language models (LLMs), key-value (KV) cache reduction for LLM inference has received remarkable attention. Among numerous works that have been proposed in recent years, layer-wise token pruning approaches, which select a subset of tokens at particular layers to retain...

---

### 27. [Employ SmartNICs' Data Path Accelerators for Ordered Key-Value Stores](https://arxiv.org/abs/2601.06231)

**Authors**: Frederic Schimmelpfennig, Jan Sass, Reza Salkhordeh, Martin Kr\"oning, Stefan Lankes, Andr\'e Brinkmann  
**Category**: cs.DC  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.06231v1  

#### Abstract
Remote in-memory key-value (KV) stores serve as a cornerstone for diverse modern workloads, and high-speed range scans are frequently a requirement. However, current architectures rarely achieve a simultaneous balance of peak efficiency, architectural simplicity, and native support for ordered opera...

---

### 28. [CEEMDAN-Based Multiscale CNN for Wind Turbine Gearbox Fault Detection](https://arxiv.org/abs/2601.06217)

**Authors**: Nejad Alagha, Anis Salwa Mohd Khairuddin, Obada Al-Khatib, Abigail Copiaco  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.06217v1  

#### Abstract
Wind turbines play a critical role in the shift toward sustainable energy generation. Their operation relies on multiple interconnected components, and a failure in any of these can compromise the entire system's functionality. Detecting faults accurately is challenging due to the intricate, non-lin...

---

### 29. [Surrogate-based Optimization via Clustering for Box-Constrained Problems](https://arxiv.org/abs/2601.07442)

**Authors**: Maaz Ahmad, Iftekhar A. Karimi  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.07442v1  

#### Abstract
Global optimization of large-scale, complex systems such as multi-physics black-box simulations and real-world industrial systems is important but challenging. This work presents a novel Surrogate-Based Optimization framework based on Clustering, SBOC for global optimization of such systems, which c...

---

### 30. [Logic-Parametric Neuro-Symbolic NLI: Controlling Logical Formalisms for Verifiable LLM Reasoning](https://arxiv.org/abs/2601.05705)

**Authors**: Ali Farjami, Luca Redondi, Marco Valentino  
**Category**: cs.AI  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05705v1  

#### Abstract
Large language models (LLMs) and theorem provers (TPs) can be effectively combined for verifiable natural language inference (NLI). However, existing approaches rely on a fixed logical formalism, a feature that limits robustness and adaptability. We propose a logic-parametric framework for neuro-sym...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
