# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-12 06:46:28 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Learning to Evict from Key-Value Cache](https://arxiv.org/abs/2602.10238)

**Authors**: Luca Moschella, Laura Manduchi, Ozan Sener  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.10238v1  

#### Abstract
The growing size of Large Language Models (LLMs) makes efficient inference challenging, primarily due to the memory demands of the autoregressive Key-Value (KV) cache. Existing eviction or compression methods reduce cost but rely on heuristics, such as recency or past attention scores, which serve o...

---

### 2. [How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning](https://arxiv.org/abs/2602.10622)

**Authors**: Jiahao Yuan, Yike Xu, Jinyong Wen, Baokun Wang, Yang Chen, Xiaotong Lin, Wuliang Huang, Ziyi Gao, Xing Fu, Yu Cheng, Weiqiang Wang  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.10622v1  

#### Abstract
Decoder-only large language models are increasingly used as behavioral encoders for user representation learning, yet the impact of attention masking on the quality of user embeddings remains underexplored. In this work, we conduct a systematic study of causal, hybrid, and bidirectional attention ma...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前，**decoder-only LLMs** 虽然因其自回归特性被广泛用于用户行为建模（如推荐、营销等），但在作为 **user representation learning** 的编码器时，其标准的 **causal attention masking** 限制了模型对全局上下文的理解能力。尽管已有研究尝试引入 **bidirectional attention** 或 **hybrid masking**，但缺乏系统性比较，且从预训练的因果注意力向双向注意力过渡时存在训练不稳定的问题。

本文系统地研究了不同 **attention masking** 策略在统一框架下对用户表征质量的影响，并揭示了**训练动态过程**（尤其是从 causal 到 bidirectional 的过渡）是影响最终表现的关键因素。

---

### 提出了什么新方法或新思路
作者提出了 **Gradient-Guided Soft Masking (GG-SM)**，一种基于梯度引导的软掩码预热机制，用于平滑地实现从 causal 到 bidirectional attention 的过渡：

- **核心思想**：在训练初期（warm-up 阶段），利用损失函数对隐藏状态的梯度范数 $\|\nabla_{\mathbf{h}} \mathcal{L}\|$ 来动态决定哪些“未来的 token”应该被提前关注——即梯度越大，说明该 token 对任务越重要，因此应赋予更高的可见性权重。
- 在 warm-up 结束后，固定这些由梯度驱动的软掩码，并通过线性调度器逐步打开全部未来注意力，最终在推理阶段使用完全的 **bidirectional attention**。

这种方法将注意力机制视为一个可演化的瓶颈，而非静态设计。

---

### 相比现有方法的优势
| 方法 | 缺陷 | GG-SM 的优势 |
|------|------|--------------|
| **Causal Masking** | 仅能捕捉单向依赖，无法建模长程语义整合 | 支持全上下文感知，提升表示完整性 |
| **Hybrid Masking** | 架构复杂，需额外模块（如 MLP 或 global query），难以与预训练对齐 | 不引入额外参数，保持与 decoder 预训练兼容 |
| **Scheduler-only Transition** | 固定调度策略忽略任务信号，可能导致优化震荡 | 利用梯度提供数据驱动的优先级信号，更稳定高效 |

✅ **GG-SM 的优势总结**：
- 更稳定的训练收敛；
- 更高质量的 bidirectional 用户表征；
- 兼容 decoder-only LLM 的预训练模式；
- 无需修改模型架构，易于集成。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
基于真实世界 **Alipay** 用户行为数据构建两个训练数据集：

1. **Rule-based Behavioral Trajectories Dataset ($D_{\text{behavior}}$)**  
   - 包含用户过去 90 天的多模态交互序列：`PayBill`, `MiniProgram`, `SPM paths`, `App`, `Search`, `Tabular features`
   - 正样本为后续一个月内的聚合行为，用于预测未来行为。

2. **LLM-Synthesized Query-Answer Alignments Dataset ($D_{\text{qa}}$)**  
   - 使用 **Qwen-Max** 自动生成用户理解相关的 QA 对。
   - 引入“困难正样本挖掘”机制：先生成初始 QA → 用强 embedding 模型计算 alignment difficulty（$S_d = 1 - \text{Sim}(u\oplus q, a)$）→ 过滤出高难度样本 → 提取共性规则优化 prompt → 扩展生成高质量 hard positives。

---

### 实验设置和评估指标

#### 模型架构
- **Backbone**: `Qwen2.5-0.5B-Instruct`（decoder-only LLM）
- **Modality Encoders**: `gte-base-zh` 分别处理各类行为特征
- **Adapter**: LoRA (rank=64, α=64)
- **输入格式**：各模态用特殊标签包裹（如 `<bill>...</bill>`），末尾添加 `<USER>` token 作为 embedding 提取锚点
- **输出 embedding**：取 `<USER>` 位置的最后一层 hidden state 并进行 L2 归一化

#### 训练目标
采用 **InfoNCE loss** 进行 contrastive learning：
$$
\mathcal{L}_{\text{cl}} = -\log \frac{\exp(s(\mathbf{u}_i, \mathbf{a}_i)/\tau)}{\sum_j \exp(s(\mathbf{u}_i, \mathbf{a}_j)/\tau)}
$$
并引入 mask factor $m_{ij}$ 避免 false negatives 干扰。

#### 评估方式
- **下游任务**：9 个工业级 binary classification 任务，覆盖三大领域：
  - **User Prediction**：Concert Click, User Login, MAU Loss
  - **Behavior Preference**：Public Transit, Consumption Power, Food, Movie
  - **Marketing Sensitivity**：Achievement, Physical Preference
- **评估指标**：**AUC**（Area Under the ROC Curve）
- **训练资源**：64 × A100-80GB GPUs，batch size 2048，AdamW + cosine decay

---

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **General Embedding Models** | Qwen3-Embedding-0.6B, Llama-embed-nemotron-8B, KaLM-Embedding-Gemma3-12B |
| **Traditional User Models** | MSDP, One4all, CPC |
| **LLM-based User Models** | FOUND, InstructUE |
| **Attention Masking Variants** | Causal, Hybrid (mask/gq/mlp), Bidirectional (direct/scheduler/GG-SM) |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（平均 AUC）

| 方法 | Avg AUC |
|------|--------|
| **Qwen3-Embedding-0.6B** | 0.6720 |
| **Llama-embed-nemotron-8B** | 0.7357 |
| **KaLM-Embedding** | 0.7156 |
| **FOUND (SOTA LLM-based)** | 0.7690 |
| **InstructUE** | 0.7728 |
| **Ours (w/ GG-SM)** | **0.7745** ✅ |

> 尽管仅使用 **0.5B 参数量**的 backbone，GG-SM 显著优于多个更大规模的通用 embedding 模型。

---

### 与基线方法的对比结果

| 维度 | 结果分析 |
|------|----------|
| **vs. Causal Baseline** | GG-SM 提升明显（0.6542 → 0.7745），证明双向注意力对用户理解至关重要 |
| **vs. Hybrid Methods** | Hybridmask: 0.7710, Hybridmlp: 0.7718 → GG-SM 仍小幅领先，且无额外参数 |
| **vs. Scheduler-only** | Scheduler: 0.7733 → GG-SM 达到 **0.7745**，验证梯度引导的有效性 |
| **跨域稳定性** | 在最难的 **Marketing Sensitivity** 上也达到峰值 AUC，显示更强泛化能力 |

---

### 消融实验结果（Ablation Study）

| 变体 | Avg AUC | 分析 |
|------|--------|------|
| Causal (Oracle) | 0.6542 | 严重受限于局部上下文 |
| Bidirectional (Direct) | 0.7721 | 比 causal 好，但不如渐进式开放 |
| w/ Scheduler | 0.7733 | 固定调度有效，但未利用任务信号 |
| **w/ GG-SM (Ours)** | **0.7745** | 梯度引导带来最稳定、最强的表现 |

> 图 3 显示：**GG-SM 收敛更快、损失下降更平稳**，而 scheduler 出现明显震荡。

---

## 4. 关键结论和发现

### 主要发现
1. **attention masking 设计直接影响用户表征质量**：
   - Bidirectional > Hybrid > Causal，在所有任务上一致成立。
2. **训练路径比最终结构更重要**：
   - 直接切换到 bidirectional 容易导致不收敛；**渐进式过渡 + 梯度引导** 是关键。
3. **GG-SM 实现了参数效率与性能的平衡**：
   - 即使使用小模型（0.5B），也能超越大模型（8B+），说明**领域适配优于单纯扩参**。
4. **decoder-only LLM 可以安全用于 bidirectional encoding**：
   - 只要训练过程得当，即使破坏 autoregressive constraint，仍能获得 superior 表示能力。

---

### 方法的局限性
- **依赖高质量合成数据**：虽然提出训练-free 合成框架，但仍需强大 LLM（如 Qwen-Max）生成困难样本。
- **计算开销增加**：梯度计算增加了 warm-up 阶段的内存负担。
- **应用场景受限于 offline encoding**：推理时虽可用 bidirectional，但不能用于实时生成场景（因违反自回归假设）。

---

### 未来工作方向
1. 探索更高效的梯度估计方式（如低秩近似）以降低 warm-up 成本；
2. 将 GG-SM 应用于其他模态（如视频、语音）的用户行为建模；
3. 研究如何在保留 autoregressive capability 的同时，实现部分双向感知（partial bidirectionality）；
4. 推动 hybrid masking 成为主流范式，探索更多 user-centric attention 控制机制。

---

> 🔗 **代码开源地址**：[https://github.com/JhCircle/Deepfind-GGSM](https://github.com/JhCircle/Deepfind-GGSM)

</details>

---

### 3. [MoEEdit: Efficient and Routing-Stable Knowledge Editing for Mixture-of-Experts LLMs](https://arxiv.org/abs/2602.10965)

**Authors**: Yupu Gu, Rongzhe Wei, Andy Zhu, Pan Li  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.10965v1  

#### Abstract
Knowledge editing (KE) enables precise modifications to factual content in large language models (LLMs). Existing KE methods are largely designed for dense architectures, limiting their applicability to the increasingly prevalent sparse Mixture-of-Experts (MoE) models that underpin modern scalable L...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：MoEEdit: Efficient and Routing-Stable Knowledge Editing for Mixture-of-Experts LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现有的 **Knowledge Editing (KE)** 方法主要针对 **dense Transformer 架构** 设计，而现代大规模语言模型（LLMs）越来越多地采用 **Mixture-of-Experts (MoE)** 架构以实现高效扩展。直接将 dense 模型的编辑方法应用于 MoE 模型会面临三大挑战：
1. **计算成本高昂**：需更新所有专家参数，导致计算量随专家数量线性增长（如 128×）。
2. **专家耦合（Expert Coupling）**：输出是多个专家加权组合的结果，单个专家修改可能被稀释或引发副作用。
3. **路由分布偏移（Routing Distribution Shift）**：参数扰动会改变下游 MoE 层的输入，导致路由器选择不同专家，破坏模型稳定性与编辑局部性。

### **提出了什么新方法或新思路**
本文提出 **MoEEdit**，首个专为 MoE 架构设计的、**路由稳定（routing-stable）的参数修改型知识编辑框架**，其核心创新包括：

- **Per-Expert Null-Space Projection（每专家零空间投影）**  
  通过将专家参数更新限制在保留集（preservation set）特征的正交补空间中，确保更新不会影响保留集输出，从而**抑制路由输入变化，防止路由漂移**。

- **Randomized Block Coordinate Descent (BCD) Solver（随机块坐标下降求解器）**  
  将全局优化问题分解为按专家划分的子问题，逐个更新相关专家，显著降低计算复杂度。该求解器**时间复杂度与专家隐藏维度成线性关系，而非专家总数**，具备良好可扩展性。

### **相比现有方法的优势**
- ✅ 首次系统解决 MoE 编辑中的 **路由稳定性问题**。
- ✅ 显著优于将 dense 模型编辑方法（如 FT、UnKE）直接迁移到 MoE 上的表现。
- ✅ 在保持高 **efficacy** 和 **generalization** 的同时，实现卓越的 **specificity** 与 **routing stability**。
- ✅ 计算与内存效率远超闭式求解方案，适用于大规模 MoE 模型。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **COUNTERFACT**：用于单跳反事实知识编辑，评估模型对事实更改的响应能力。
- **zsRE（Zero-Shot Relation Extraction）**：评估模型在零样本关系抽取任务上的编辑效果与泛化能力。

### **实验设置**
- **模型**：
  - `Qwen3-30B-A3B`：每层 128 个专家，Top-8 激活。
  - `GPT-OSS-20B`：每层 32 个专家，Top-4 激活。
- **编辑方式**：进行 **1,000 次连续批量编辑（batched edits）**，每次 batch 大小为 50。
- **硬件与精度**：使用 NVIDIA H20 GPU，权重使用 BF16，优化过程使用 FP32。

### **评估指标**
| 指标 | 含义 |
|------|------|
| **Efficacy** | 编辑提示下目标答案生成的成功率 |
| **Generalization** | 在改写（paraphrase）上下文中仍能正确回答的能力 |
| **Specificity** | 对无关控制样本不产生干扰的程度（局部性） |
| **Utility** | 三项指标的平均值，综合衡量性能 |
| **Routing Similarity (RS)** | 编辑前后 Top-K 路由专家集合的 Jaccard 相似度，衡量路由稳定性 |
| **KL Divergence** | 路由分布变化的量化指标 |

### **基线方法对比**
- **Fine-Tuning (FT)**：全量微调。
- **FT-L**：带范数约束的微调。
- **AdaLoRA**：低秩自适应微调。
- **UnKE**：基于结构化优化的知识编辑方法。
- 所有 baseline 均适配至 MoE 架构并作用于相应模块（如 `mlp.experts.down_proj`）。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 1）**

#### **在 Qwen3-30B-A3B 上的表现（COUNTERFACT）**
| Method     | Efficacy ↑ | Generalization ↑ | Specificity ↑ | Utility ↑ |
|------------|-----------|------------------|---------------|-----------|
| UnKE       | 89.30     | 82.85            | 48.15         | 73.43     |
| **MoEEdit** | **99.30** | **94.10**        | **80.97**     | **91.46** |

> MoEEdit 在所有指标上全面领先，尤其在 **specificity** 上提升超过 32 个百分点。

#### **在 GPT-OSS-20B 上的表现（ZsRE）**
| Method     | Efficacy ↑ | Generalization ↑ | Specificity ↑ | Utility ↑ |
|------------|-----------|------------------|---------------|-----------|
| AdaLoRA    | 43.46     | 42.96            | 33.60         | 40.01     |
| **MoEEdit** | **81.68** | **68.44**        | **32.55**     | **60.89** |

> MoEEdit 在 **efficacy** 和 **generalization** 上分别提升 **+38.22** 和 **+25.48**，达到绝对优势。

---

### **与基线方法的对比结果**
- MoEEdit 在两个模型和两个数据集上均取得 **state-of-the-art 性能**。
- 相比 FT 和 UnKE，MoEEdit 显著提升了 **编辑有效性** 与 **泛化能力**，同时避免了灾难性遗忘。
- 在 **routing stability** 方面表现尤为突出（见下表）。

#### **路由稳定性对比（Table 2，Qwen3-30B-A3B）**
| Method     | Editing Set RS ↑ (Lay. 31–40) | Preservation Set RS ↑ (Lay. 31–40) |
|------------|-------------------------------|-------------------------------------|
| FT         | 29.98                         | 30.97                               |
| UnKE       | 44.80                         | 43.84                               |
| **MoEEdit** | **89.93**                     | **90.22**                           |

> MoEEdit 的平均 RS 超过 **88%**，表明其几乎完全保留了原始路由路径；而其他方法仅维持约 30%-45%，说明存在严重路由漂移。

---

### **消融实验结果**

#### **(1) 投影机制的作用（Ablation on Projection）**
| Method             | Editing Set RS (avg.) | Pres. Set RS (avg.) | ΔRS ↓ |
|--------------------|------------------------|-----------------------|--------|
| MoEEdit (w/ Proj)  | 88.24                  | 88.60                 | —      |
| MoEEdit (w/o Proj) | 73.43                  | 73.39                 | ~15    |

> 移除零空间投影后，**RS 下降约 15 个百分点**，验证了该设计对路由稳定的决定性作用。

#### **(2) BCD 求解器 vs. 闭式求解器**
- **闭式求解器**：需求解规模为 $(Nd_k)^2$ 的矩阵逆，时间和内存开销呈近二次增长，在 $N > 60$ 时不可行。
- **BCD 求解器**：运行时间基本恒定，**可扩展至 128 个专家以上**，且仅需 6–10 轮即可收敛。

#### **(3) BCD 迭代次数的影响**
- 前几轮快速提升 efficacy 和 generalization，后续趋于饱和。
- **6–10 轮 BCD 已足够获得最优性价比**，更多迭代带来边际收益递减。

---

## **4. 关键结论和发现**

### **主要发现**
1. **MoE 架构的知识编辑必须考虑路由稳定性**：即使小幅度参数扰动也可能引发级联式路由偏移，破坏模型行为一致性。
2. **专家感知（expert-aware）设计至关重要**：盲目复制 dense 模型编辑策略无法应对 MoE 的稀疏性与条件计算特性。
3. **MoEEdit 实现了高效、稳定、精准的编辑**：在多个 benchmark 上显著超越现有方法，树立了 MoE KE 的新标杆。
4. **零空间投影 + BCD 是有效组合**：前者保障稳定性，后者保障可扩展性，二者协同构建了一个实用化的 MoE 编辑框架。

### **方法的局限性**
- 当前方法聚焦于 **MLP 专家内部的 down-projection 权重**，未涉及 attention 或 router 本身的编辑。
- 依赖于 **preservation set 构建零空间**，若该集合不足可能导致过度干扰。
- 对 extremely rare facts 或 deeply耦合的知识仍可能存在编辑失败风险。

### **未来工作方向**
- 探索 **router-aware 编辑机制**，允许在必要时安全调整路由逻辑。
- 结合 **parameter-preserving 与 parameter-modifying 方法**，构建混合编辑系统。
- 将 MoEEdit 扩展到 **多跳推理** 和 **因果知识图谱更新** 场景。
- 研究 **持续编辑（lifelong editing）下的稳定性累积效应**。

---

> 🔗 **代码地址**：[https://github.com/Terence-Gu/MoEEdit](https://github.com/Terence-Gu/MoEEdit)

</details>

---

### 4. [Found-RL: foundation model-enhanced reinforcement learning for autonomous driving](https://arxiv.org/abs/2602.10458)

**Authors**: Yansong Qu, Zihao Sheng, Zilin Huang, Jiancong Chen, Yuhao Luo, Tianyi Wang, Yiheng Feng, Samuel Labi, Sikai Chen  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.10458v1  

#### Abstract
Reinforcement Learning (RL) has emerged as a dominant paradigm for end-to-end autonomous driving (AD). However, RL suffers from sample inefficiency and a lack of semantic interpretability in complex scenarios. Foundation Models, particularly Vision-Language Models (VLMs), can mitigate this by offeri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Found-RL: foundation model-enhanced reinforcement learning for autonomous driving

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

强化学习（**Reinforcement Learning, RL**）在端到端自动驾驶（**autonomous driving, AD**）中展现出潜力，但仍面临两大挑战：

- **样本效率低**（sample inefficiency）：传统 RL 依赖大量试错探索，训练缓慢且不稳定。
- **语义可解释性差**：缺乏对复杂驾驶场景的高层语义理解，难以处理安全关键任务。

同时，尽管视觉语言模型（**Vision-Language Models, VLMs**）具备强大的语义推理能力，但其高计算开销导致难以集成到高频的 RL 训练循环中，造成严重的**推理延迟瓶颈**。

---

### 🚀 提出的新方法与创新思路

作者提出 **Found-RL** ——一个专为自动驾驶设计的、融合基础模型（foundation models）与 RL 的统一平台，核心创新如下：

#### （1）异步批量推理框架（Asynchronous Batch Inference Framework）
- 将 VLM 的重负载推理过程从仿真主循环中解耦。
- 通过客户端-服务器架构，将多个环境的观测打包成微批次进行并行推理，显著降低延迟。
- 支持实时或近实时的 RL 训练，即使使用计算密集型 VLM。

#### （2）VLM 动作引导机制（VLM Action Guidance）
- **Value-Margin Regularization (VMR)**：鼓励 critic 对 VLM 推荐动作赋予更高 Q 值，提升策略向专家行为靠拢。
- **Advantage-Weighted Action Guidance (AWAG)**：仅当 VLM 动作估计优于当前策略时才模仿，实现自适应行为克隆。

#### （3）基于 CLIP 的奖励塑形（CLIP-based Reward Shaping）
- 引入 **Conditional Contrastive Action Alignment**：
  - 将车速和导航指令离散化作为上下文条件。
  - 构建小规模、上下文相关的动作锚点集（action anchors），避免全局分类的概率稀释。
  - 利用 CLIP 图像-文本匹配得分，生成归一化的、基于边界的奖励 bonus。
- 有效缓解 CLIP 在动态状态感知上的“动态失明”问题。

---

### 🔍 相比现有方法的优势

| 维度 | Found-RL 的优势 |
|------|----------------|
| **效率** | 异步批处理使 VLM 反馈不阻塞仿真，支持高频率训练 |
| **性能** | 轻量级 RL 模型（仅 3.82M 参数）达到甚至超越十亿参数 VLM 的驾驶表现 |
| **实时性** | 推理速度达 ~500 FPS，远超 VLM（通常 <1 FPS），适合部署 |
| **通用性** | 模块化设计，支持多种 VLM 和 RL 算法组合 |
| **安全性** | 显著减少碰撞与红灯违规，提升驾驶合规性 |

---

## 2. 核心实验方法和设置

### 📚 数据集与仿真环境

- **仿真平台**：基于 **CARLA** 搭建多场景交通模拟器。
- **训练地图**：
  - **Leaderboard benchmark**：Town01, Town03, Town04, Town06
  - **NoCrash benchmark**：Town01（训练）、Town02（测试）
- **数据来源**：收集约 **137万条**状态-动作对，来自 Roach PPO 和 autopilot roaming 策略。

---

### 🧪 实验设置

#### 观测空间（Observation Space）
| 类型 | 输入内容 |
|------|--------|
| **VLM-based agents** | BEV 图像 (192×192×3) + 文本提示 |
| **RL-based agents** | BEV masks (96×96×15) + 结构化状态向量（速度、指令、交通灯等） |

#### 动作空间
- 连续控制：`[throttle/brake, steer]`（RL）或 `[throttle, steer, brake]`（VLM）

#### 奖励函数
- 采用 ROACH 风格奖励：速度跟踪、路线保持、航向对齐、平滑性惩罚、终端奖惩。
- 加入 **VLM-derived reward bonus**（来自 CLIP 对齐得分）。

#### 终止条件
- 更严格的终止逻辑：偏离路径、闯红灯/停车标志、碰撞即终止（更贴近真实安全要求）。

---

### 📊 评估指标

分为四大类：

| 类别 | 指标 |
|------|------|
| **Comprehensive** | Return, Driving Score, Infraction Penalty |
| **Route** | Success Rate, Route Completion, Speed |
| **Energy** | Icell（电池电流估计）, Fuel Rate |
| **Safety** | Collisions (Ped./Veh.), Red Light Violations |

---

### 🆚 基线方法对比

#### （1）RL 基线
- **TD3**, **SAC**, **DrQv2**

#### （2）VLM 基线（直接作为策略）
- **InternVL3-1B/2B**, **Qwen2.5-vl-3B/7B**, **Visual RWKV-0.1B**

#### （3）其他 AD 方法
- **TransFuser**, **LAV**, **TCP**（RGB/LiDAR 输入）
- **BC**, **AWAC**, **CQL**, **IQL**（基于 BEV 的 IL / offline RL）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Leaderboard Benchmark）

| 方法 | Driving Score ↑ | Success Rate ↑ | Red Light ↓ | Icell ↓ |
|------|------------------|----------------|------------|---------|
| **DrQv2-CLIP (Ours)** | **0.77** | **0.57** | **0.01** | 0.13 |
| DrQv2-VMR | 0.72 | 0.60 | 0.02 | 0.09 |
| Vanilla DrQv2 | 0.56 | 0.38 | 0.02 | 0.09 |
| Qwen2.5-vl-7B | 0.76 | 0.65 | 0.05 | 0.04 |
| InternVL3-1B | 0.63 | 0.49 | 0.07 | 0.04 |

> ✅ **Found-RL 的 DrQv2-CLIP 在驾驶得分上超越所有 VLM 基线，并实现最低红灯违规率**

---

### 🔄 NoCrash Benchmark 性能（跨城镇泛化）

| 方法 | Town01 Driving Score | Town02 Driving Score | Town02 Success Rate |
|------|------------------------|------------------------|-----------------------|
| **DrQv2-CLIP** | 0.75 | **0.76** | **0.71** |
| SAC-VMR | 0.71 | 0.61 | 0.47 |
| Vanilla SAC | 0.24 | 0.24 | 0.07 |

> ✅ **VLM 增强显著提升泛化能力，尤其在未见城镇中 SAC 从失败恢复至稳健水平**

---

### 🔬 消融实验结果

#### （1）VLM 指导有效性（Table 1 & Fig. 6）
- 所有引入 VLM 指导的方法均在早期训练阶段加速收敛。
- **DrQv2-CLIP** 达到最高渐近性能，方差更低。

#### （2）不同指导机制比较
- **VMR**：初期快速提升，但需衰减正则系数防止过拟合。
- **AWAG**：Actor loss 更稳定，适合长期学习。

#### （3）CLIP 奖励有效性（Fig. 10）
- 奖励可用性接近 100%，验证高吞吐设计成功。
- 平均 CLIP 对齐 margin 快速上升后趋于平稳，表明策略学会维持高语义一致性。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **语义监督可极大提升 RL 效率与安全性**  
   VLM 提供的高层语义反馈（动作建议 + 密集奖励）显著加速学习，减少危险行为。

2. **轻量 RL 模型可媲美大规模 VLM 表现**  
   仅 **3.82M 参数** 的 RL 模型，在性能上达到甚至超过 **1B–7B 参数 VLM**，实现“蒸馏式增强”。

3. **异步批处理是工程可行的关键**  
   成功解决 VLM 推理延迟问题，使闭环训练成为可能。

4. **压缩输入仍能保持高性能**  
   使用 96×96 BEV masks（仅为 VLM 输入的 25%）即可取得优异结果，利于边缘部署。

5. **规则遵守优于激进驾驶**  
   尽管输入维度更低，Found-RL 在红灯违规等静态规则上表现更优，体现更强的合规性。

---

### ⚠️ 局限性

1. **视觉分辨率限制影响动态物体处理**  
   96×96 输入可能导致对远处行人或高速车辆识别不足，部分解释了较高的车辆碰撞率。

2. **依赖高质量 VLM 微调数据**  
   当前方法依赖于大规模专家轨迹进行 VLM 微调，若数据偏差大可能传递错误先验。

3. **CLIP 对连续动作离散化敏感**  
   动作空间划分依赖人工设定阈值，可能损失细粒度控制能力。

4. **反馈延迟风险**  
   若扩展至更大模型，异步反馈可能出现“过期指导”，影响稳定性。

---

### 🔮 未来工作方向

1. **进一步优化推理延迟**  
   探索模型量化、蒸馏、硬件加速以支持更大规模 VLM 实时反馈。

2. **多 VLM 协同指导**  
   设计多专家系统，不同 VLM 分别负责安全、舒适、效率等子目标。

3. **扩展至 CARLA Leaderboard 2.0**  
   在更具挑战性的官方排行榜上验证鲁棒性。

4. **端到端轻量 VLM 策略**  
   探索将小型 VLM 直接作为 policy backbone，兼顾语义理解与实时性。

5. **真实世界迁移验证**  
   推动方法向实车平台迁移，结合域自适应技术缩小仿真差距。

---

> 💡 **一句话总结**：  
> **Found-RL 证明了“小模型 + 大脑（VLM）”的范式可行性——通过异步语义增强，让轻量 RL 在自动驾驶中实现高效、安全、近似人类水平的决策能力。**

</details>

---

### 5. [BOute: Cost-Efficient LLM Serving with Heterogeneous LLMs and GPUs via Multi-Objective Bayesian Optimization](https://arxiv.org/abs/2602.10729)

**Authors**: Youhe Jiang, Fangcheng Fu, Eiko Yoneki  
**Category**: cs.DC  
**Published**: 2026-02-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.10729v1  

#### Abstract
The rapid growth of large language model (LLM) deployments has made cost-efficient serving systems essential. Recent efforts to enhance system cost-efficiency adopt two main perspectives: (i) An algorithmic perspective that exploits heterogeneous model capabilities to route simpler queries to lower-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**BOUTE: Cost-Efficient LLM Serving with Heterogeneous LLMs and GPUs via Multi-Objective Bayesian Optimization**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLM）服务系统面临高昂的部署成本，尤其是在满足严格延迟（latency）和响应质量（response quality）要求的前提下。现有研究通常从两个独立角度优化：
- **算法层面**：通过 **Heterogeneous Model Routing**（异构模型路由），将简单查询路由到小模型，复杂查询路由到大模型。
- **系统层面**：通过 **Heterogeneous Model Deployment**（异构模型部署），利用不同类型的 GPU（如 H100 和 RTX 5090）构建更具性价比的服务架构。

然而，这两者之间存在强耦合关系：
- 路由策略影响各模型的负载分布，进而决定最优部署；
- 部署配置又决定了每个模型的实际延迟表现，反过来影响路由决策。

因此，**孤立地优化路由或部署会导致次优解**。本文提出应进行 **算法-系统协同设计（algorithm-system co-design）** 来实现真正的成本效益最大化。

---

### 🚀 提出的新方法与创新思路

作者提出了 **BOUTE** —— 一个基于 **Multi-Objective Bayesian Optimization (MOBO)** 的质量感知调度系统，用于联合优化异构模型路由与部署。

#### 主要贡献如下：

- **Contribution 1：揭示了异构模型路由与异构部署之间的协同效应**
  - 实验表明，将资源需求不同的模型匹配到合适的 GPU 类型（例如：小模型用 RTX 5090，大模型用 H100）可显著提升整体效率。
  - 这种硬件-模型适配创造了“双赢”局面：既降低了小模型服务成本，又能释放预算增强大模型服务能力。

- **Contribution 2：形式化为带约束的多目标优化问题，并引入 MOBO 框架求解**
  - 将路由阈值（T）、GPU 分配矩阵（A）和平行策略（P）作为联合决策变量。
  - 目标函数为最小化系统延迟 $L$ 并最大化响应质量 $Q$，同时满足预算和资源限制。
  - 设计了一个两阶段框架：
    - **Offline Preparation Phase**：预先建立性能数据库，模拟各种 `(deployment, workload)` 组合下的延迟。
    - **Online Optimization Phase**：使用 MOBO 快速搜索帕累托最优解集（Pareto-optimal solutions）。

- **Contribution 3：引入多种结构化先验知识以加速收敛**
  - **Load-fraction encoding**：将路由阈值重参数化为负载比例，避免因评分分布不均导致的优化震荡。
  - **Model-GPU preference encoding**：通过偏好权重核（PreferenceWeightedKernel）引导搜索偏向已知高效的模型-GPU 匹配组合。
  - **Constrained qNEHVI acquisition function**：支持在优化中直接处理延迟和质量的硬性约束。

---

### 🔍 相比现有方法的优势

| 方法 | 局限性 | BOUTE 的改进 |
|------|--------|-------------|
| **Standalone LLM serving** | 单一模型无法动态适应查询难度，要么高成本，要么低质量 | 动态路由 + 多模型协作 |
| **RouteLLM / HybridLLM** | 只关注路由逻辑，采用均匀资源配置，忽略模型对 GPU 的偏好 | 联合优化部署，实现资源精准分配 |
| **ThunderServe / Helix** | 仅关注异构部署，未结合路由机制 | 实现算法-系统协同设计，发挥双重异构优势 |

> ✅ **核心优势**：首次实现了 **routing 与 deployment 的端到端联合优化**，解决了二者间的循环依赖难题。

---

## 2. 核心实验方法和设置

### 📚 数据集与工作负载
- **GSM8K**：数学推理任务，用于评估准确率（accuracy）
- **MTBench**：多轮对话基准，用于评估综合得分（score）
- 工作负载 trace 采样自真实用户行为模式，包含输入/输出长度分布等统计特征

### ⚙️ 实验环境
- **GPU 类型**：
  - NVIDIA H100-80G ($2.64–3.07/h)
  - RTX PRO 6000-96G ($1.84–2.09/h)
  - RTX 5090-32G ($0.89/h)
  - RTX 4090-24G ($0.59/h)
- **互联带宽**：
  - H100：NVLink（300 GB/s）
  - RTX 系列：PCIe（60 GB/s）
- **总预算**：固定为 $30/h 的成本上限

### 🧪 模型与组件
- **模型类型**：
  - Llama3.1-8B（small model）
  - Llama3.1-70B（large model）
- **Router**：基于 RouteLLM 构建，使用 GPT-4-as-a-judge 和 MMLU 数据训练
- **Parallelism 支持**：Data Parallelism (DP), Tensor Parallelism (TP), Pipeline Parallelism (PP)

### 📊 评估指标
- **延迟指标**：Average Latency, P95, P99, P100
- **吞吐量**：Throughput (req/s)
- **服务质量**：Aggregated Accuracy (GSM8K), Aggregated Score (MTBench)
- **成本效率**：达到相同 QoS 所需的每小时费用（$/h）

### 🆚 基线方法对比
| 基线 | 描述 |
|------|------|
| **Stand-Alone-Homo** | 使用 vLLM 在同构 H100 上运行 Llama3.1-70B |
| **Stand-Alone-Hetero** | 使用 vLLM 在混合 GPU 上运行 Llama3.1-70B |
| **RouteLLM** | 使用 RouteLLM 路由，在各类 GPU 上均匀分配资源 |
| **BOUTE-Homo** | BOUTE 的变体，仅在同构 GPU 上运行（用于消融分析） |

---

## 3. 主要实验结果和性能指标

### 📈 总体性能提升（End-to-End Evaluation）

#### 在 GSM8K 上的表现（质量要求：86–90）
| 对比项 | 最大提升 | 平均提升 |
|-------|--------|---------|
| **相比 Standalone 方法** | 吞吐量 ↑ **90%**，P95 ↓ **91%** | 吞吐量 ↑ **55%**，P95 ↓ **57%** |
| **相比 RouteLLM** | 吞吐量 ↑ **71%**，P95 ↓ **66%** | 吞吐量 ↑ **52%**，P95 ↓ **50%** |
| **相比 BOUTE-Homo** | 吞吐量 ↑ **16%**，P95 ↓ **15%** | 吞吐量 ↑ **14%**，P95 ↓ **14%** |

> 💡 表明 BOUTE 显著优于所有基线，尤其在异构环境下优势明显。

#### 在 MTBench 上的表现（质量要求：8.1–8.5）
| 对比项 | 最大提升 | 平均提升 |
|-------|--------|---------|
| **相比 Standalone 方法** | 性能 ↑ **157%** | 性能 ↑ **115%** |
| **相比 RouteLLM** | 性能 ↑ **80%** | 性能 ↑ **42%** |
| **相比 BOUTE-Homo** | 性能 ↑ **21%** | 性能 ↑ **20%** |

> ✅ 异构部署带来的增益高达 **20%+**，验证了其必要性。

---

### 💰 成本效率分析（Cost Efficiency）

| 方法 | 达到特定 QoS 所需成本（平均） |
|------|----------------------------|
| Stand-Alone-Homo | $61.83/h |
| RouteLLM | $53.55/h |
| BOUTE-Homo | $38.41/h |
| **BOUTE** | **$32.25/h** |

> ✅ BOUTE 在保持相同延迟和质量目标下：
- 相比 standalone 方法节省 **约 48%** 成本
- 相比 RouteLLM 节省 **38%** 成本
- 相比 BOUTE-Homo 再降 **15%** 成本

> 📌 **结论**：BOUTE 可实现 **15%–61% 的成本降低**（平均 38%），是极具经济效益的 LLM serving 方案。

---

### 🔬 消融实验结果（Ablation Studies）

| 设置 | 调度时间（3 GPU types） | 相对性能下降 |
|------|------------------------|--------------|
| **完整 BOUTE** | **23.5 秒** | 基准 |
| BOUTE (w/o structural info) | 3.6 分钟（↑ 9×） | 明显劣化，常陷入局部最优 |
| BOUTE (w/o offline prep) | ~12 分钟 | 因每次需重新仿真，开销巨大 |

> ✅ **关键发现**：
- 结构化信息（如 load-fraction encoding、preference kernel）极大提升了 **样本效率** 和 **收敛速度**。
- 离线准备阶段（offline preparation）对于实时性至关重要。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **异构模型路由与异构部署天然互补**  
   不同模型对 GPU 特性的偏好差异显著（如 Llama3.1-8B 在 RTX 5090 上快 1.5×，而 Llama3.1-70B 在 H100 上快 2×）。合理匹配可大幅提升性价比。

2. **必须进行联合优化而非分步优化**  
   路由与部署相互依赖，单独优化任一方都会破坏全局最优性。MOBO 框架有效解决了这一循环依赖问题。

3. **结构化先验知识显著提升优化效率**  
   加入 load-fraction 编码、preference-aware kernel 等设计后，MOBO 收敛更快且更稳定。

4. **BOUTE 实现了帕累托前沿上的支配地位**  
   在所有测试场景中，BOUTE 在延迟-质量平面上均表现出 **Pareto dominance**，即在同等质量下延迟更低，或在同等延迟下质量更高。

---

### ⚠️ 方法的局限性
1. **离线建模依赖准确的性能预测器**  
   若 simulator 误差较大（>10%），可能导致部署选择偏差。虽然文中报告误差在 2%-7%，但仍存在不确定性。

2. **扩展性受限于候选空间增长**  
   当模型种类、GPU 类型或并行策略增多时，候选部署数量呈指数级上升，可能需要进一步剪枝或近似。

3. **假设静态工作负载分布**  
   当前优化基于稳态负载假设，若查询复杂度分布剧烈变化（drift），可能需要周期性重优化。

4. **未考虑冷启动与容错机制**  
   实际生产环境中还需处理模型加载延迟、节点故障等问题，当前框架尚未涵盖。

---

### 🔮 未来工作方向
1. **在线自适应优化机制**  
   引入反馈控制 loop，根据实时监控数据动态调整路由与部署策略。

2. **支持更多模型类型与 MoE 架构**  
   将框架扩展至支持稀疏激活模型（如 Mixtral、DeepSeek-MoE）。

3. **跨区域/多云部署优化**  
   结合 SkyServe 等思想，将 BOUTE 推广至分布式云环境，进一步降低成本。

4. **绿色计算导向优化**  
   将能耗（energy consumption）纳入多目标优化目标，推动可持续 AI 发展。

---

## ✅ 总结

| 维度 | 内容 |
|------|------|
| **核心思想** | 利用 **Multi-Objective Bayesian Optimization** 实现 **Heterogeneous Model Routing + Heterogeneous Deployment** 的联合优化 |
| **关键技术** | MOBO 框架 + Offline Performance Database + Structural Priors（load-fraction, preference kernel） |
| **性能优势** | 吞吐量最高提升 **1.9×**，P95 延迟最多降低 **91%**，成本平均减少 **38%** |
| **实际意义** | 为大规模 LLM 商业化部署提供了高效、低成本、可扩展的解决方案，有助于 **LLM serving democratization** |

> 🏁 **一句话总结**：  
> **BOUTE 是首个实现 LLM 路由与部署协同优化的系统，通过 MOBO 框架在异构软硬件上实现了极致的成本效益，实验证明其性能远超当前 SOTA 方法。**

</details>

---

### 6. [How Much Reasoning Do Retrieval-Augmented Models Add beyond LLMs? A Benchmarking Framework for Multi-Hop Inference over Hybrid Knowledge](https://arxiv.org/abs/2602.10210)

**Authors**: Junhong Lin, Bing Zhang, Song Wang, Ziyan Liu, Dan Gutfreund, Julian Shun, Yada Zhu  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.10210v1  

#### Abstract
Large language models (LLMs) continue to struggle with knowledge-intensive questions that require up-to-date information and multi-hop reasoning. Augmenting LLMs with hybrid external knowledge, such as unstructured text and structured knowledge graphs, offers a promising alternative to costly contin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：How Much Reasoning Do Retrieval-Augmented Models Add beyond LLMs?

## 1. 论文的主要贡献和创新点

### 解决的问题
当前用于评估 **Retrieval-Augmented Generation (RAG)** 和 **Knowledge Graph-RAG (KG-RAG)** 系统的基准数据集存在严重的 **pretraining contamination** 问题，即测试问题所依赖的知识可能已被包含在大型语言模型（LLM）的预训练数据中。这导致模型可以通过参数化记忆（parametric recall）而非真正的检索与推理来回答问题，从而高估了其实际能力。

此外，现有基准难以有效区分模型在 **multi-hop reasoning**、**hybrid knowledge integration**（结合非结构化文本与结构化知识图谱）等方面的真实表现。

### 提出的新方法：HYBRIDRAG-BENCH
作者提出了 **HYBRIDRAG-BENCH** ——一个全自动的基准构建框架，旨在解决上述问题。该框架的核心思想是：
- 从近期（post-2023）的 **arXiv 科学文献** 中提取知识，确保知识时效性并最大程度减少与主流LLM预训练数据的重叠。
- 构建 **混合知识环境（hybrid knowledge environment）**，同时包含：
  - **非结构化文本片段（text chunks）**
  - **结构化知识图谱（knowledge graph）**
- 自动生成基于显式推理路径（explicit reasoning paths）的问答对，并确保答案必须通过检索和多跳推理才能获得。

### 相比现有方法的优势
| 优势维度 | 具体说明 |
|---------|--------|
| **抗污染性（Contamination-aware）** | 使用近期 arXiv 文献作为知识源，避免与主流LLM（如Qwen, LLaMA）的预训练截止时间重叠，确保答案无法通过记忆获得。 |
| **可控性与可扩展性（Flexible & Scalable）** | 支持按领域（domain）、时间范围（time frame）灵活定制，便于持续更新和复现实验。 |
| **诊断性强（Diagnostic）** | 生成的问题覆盖多种推理类型（单跳、条件、多跳、反事实、开放合成），可用于细粒度分析不同方法的能力差异。 |
| **自动化程度高** | 整个流程（知识抽取 → 图谱构建 → QA生成 → 质量控制）高度自动化，无需人工标注。 |

---

## 2. 核心实验方法和设置

### 数据集
- **HYBRIDRAG-BENCH 实例化三个领域子集**：
  - **Arxiv-AI**: 人工智能（强化学习）
  - **Arxiv-CY**: 治理与政策（governance and policy）
  - **Arxiv-BIO**: 生物信息学（bioinformatics）
- 每个子集约包含 **1,000 个 QA 对**，均来自 2023 年以后的 arXiv 论文。
- 问题类型分布见下表：

| 问题类型 | Arxiv-AI | Arxiv-CY | Arxiv-BIO |
|--------|--------|--------|--------|
| 单跳（Single-hop） | 29% | 25% | 25% |
| 条件单跳（w. Condition） | 16% | 13% | 19% |
| 多跳（Regular） | 19% | 15% | 19% |
| 难多跳（Difficult） | 17% | 17% | 13% |
| 反事实（Counterfactual） | 13% | 18% | 6% |
| 开放式（Open-ended） | 5% | 12% | 18% |

### 实验设置与评估指标
- **模型规模**：使用多个主流 LLM，包括：
  - `DeepSeek-V3.2` (685B)
  - `Qwen 2.5` (72B)
  - `LLaMA 3.3` (70B)
  - `LLaMA 3.1` (8B)
- **评估方式**：
  - 采用 **LLM-as-a-Judge** 协议进行自动评分，判断生成答案是否忠实于证据且正确。
  - 报告 **准确率（Accuracy %）**，每组实验运行 5 次取平均值与标准差。
- **时间控制**：所有文档收集时间均晚于各模型公开的预训练截止时间，防止记忆泄露。

### 基线方法对比
共比较了四类方法：

| 类别 | 方法列表 |
|------|--------|
| **纯LLM提示（LLM-only）** | IO（直接输出）、Chain-of-Thought (CoT)、Self-Consistency (SC) |
| **文本RAG（Text-based RAG）** | Dense Retrieval + RAG |
| **简单图增强（Naive KG Augmentation）** | 1-hop KG（注入一跳邻居）、RAG + 1-hop KG |
| **先进KG-RAG方法（Hybrid KG-RAG）** | CoK, RoG, ToG, ToG2.0, PoG, R2-KG, HippoRAG2.0, **EvoReasoner** |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（总体准确率 %，以 Qwen2.5-72B 为例）

| 方法 | Arxiv-AI | Arxiv-CY | Arxiv-BIO |
|------|----------|----------|----------|
| **IO** | 27.07 | 27.83 | 22.81 |
| **CoT** | 35.32 | 39.01 | 36.33 |
| **RAG** | 50.44 | 40.99 | 52.16 |
| **RAG + 1-hop KG** | 50.64 | 47.48 | 57.16 |
| **ToG2.0** | 58.61 | 59.05 | 60.36 |
| **EvoReasoner** | **71.30** | **66.89** | **73.31** |

> ✅ **观察**：即使是最大规模的 LLM（如 DeepSeek-V3.2），在无外部检索的情况下也仅能达到 ~40% 准确率，表明任务具有挑战性。

### 与基线方法的对比结果
- **RQ1：是否具有挑战性？**
  - 所有 LLM-only 方法准确率均低于 40%，即使是最强的 DeepSeek-V3.2。
  - 表明 HYBRIDRAG-BENCH 成功规避了参数化记忆，问题必须依赖外部知识。

- **RQ2：是否需要外部检索？**
  - 引入 RAG 后，准确率提升 **7–29个百分点**，证明检索至关重要。
  - 但单纯注入一跳KG节点（1-hop KG）反而会降低性能，因其引入噪声干扰模型。

- **RQ3：结构化知识是否有额外价值？**
  - **Hybrid KG-RAG 方法普遍优于纯文本 RAG**。
  - 例如，在 Arxiv-BIO 上，`EvoReasoner` 比纯 RAG 高出 **21个百分点**。
  - 表明结构化图谱能提供更可靠的多跳推理路径。

- **RQ4：能否区分不同推理策略？**
  - 细粒度分析显示：
    - **多跳问题**：`ToG`, `ToG2.0`, `EvoReasoner` 显著领先，因其支持图遍历与路径优化。
    - **反事实问题**：`CoT`, `SC`, `ToG2.0` 表现优异，体现其推理与不确定性处理能力；而 `1-hop KG` 接近零分。
    - **开放式问题**：依赖强文本检索的方法（如 RAG）表现更好。

### 消融实验结果
- **KG构建质量验证**（Table 5）：
  - 使用 MINE 基准测试知识图谱的事实捕获率（fact recovery rate）。
  - 本文使用的 **EvoKG** 方法达到 **71.36%**，优于 OpenIE (29.36%)、GraphRAG (47.08%) 和 KGGen (66.46%)。
  - 说明图谱构建本身可靠，性能瓶颈不在知识提取阶段。

- **计算成本分析**：
  - KG 构建的 token 消耗与文档长度呈近似线性关系（Figure 2），适合大规模部署。
  - 支持并行处理，端到端延迟可控。

---

## 4. 关键结论和发现

### 主要发现
1. **现有基准易受污染影响**：许多看似“高性能”的 RAG 结果可能是由于模型已见过相关知识，而非真实推理能力。
2. **HYBRIDRAG-BENCH 成功隔离记忆与推理**：通过使用近期科学文献，确保模型必须依赖检索才能作答。
3. **结构化知识显著增强推理能力**：结合知识图谱的 KG-RAG 方法在多跳、反事实等复杂推理任务上明显优于纯文本 RAG。
4. **不同方法能力差异显著**：框架能够有效区分各类方法的优劣，尤其在细粒度推理行为上提供诊断信号。

### 方法的局限性
- **依赖 LLM 进行知识抽取与 QA 生成**：虽然自动化程度高，但知识图谱质量和问题合理性仍受限于底层 LLM 的能力。
- **领域依赖性强**：目前仅基于 arXiv 科学文献，对常识或通用知识覆盖有限。
- **评估依赖 LLM-as-a-Judge**：尽管常用，但仍可能存在评分偏差，未来可引入人类评估。

### 未来工作方向
- 扩展至更多知识源（如新闻、专利、企业文档）。
- 支持动态更新机制，实现持续学习场景下的评估。
- 探索更复杂的推理模式（如时序推理、因果推断）。
- 将框架应用于模型训练过程中的课程学习（curriculum learning）设计。

---

> 🔗 **代码与数据开源**：  
> 作者已将代码和数据集公开：[github.com/junhongmit/HybridRAG-Bench](https://github.com/junhongmit/HybridRAG-Bench)  
> 为后续研究提供了可复用的研究基础设施。

</details>

---

### 7. [Kalman Linear Attention: Parallel Bayesian Filtering For Efficient Language Modelling and State Tracking](https://arxiv.org/abs/2602.10743)

**Authors**: Vaisakh Shaj, Cameron Barker, Aidan Scannell, Andras Szecsenyi, Elliot J. Crowley, Amos Storkey  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.10743v1  

#### Abstract
State-space language models such as Mamba and gated linear attention (GLA) offer efficient alternatives to transformers due to their linear complexity and parallel training, but often lack the expressivity and robust state-tracking needed for complex reasoning. We address these limitations by refram...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Kalman Linear Attention: Parallel Bayesian Filtering For Efficient Language Modelling and State Tracking

---

## 1. 主要贡献和创新点

### 解决的问题
当前主流的 **State-Space Models**（如 Mamba）和 **Gated Linear Attention**（GLA）虽然实现了线性复杂度和并行训练，但在以下方面存在局限：
- 隐藏状态更新是**线性或仿射**的，表达能力受限；
- 缺乏对**状态不确定性**的显式建模；
- 在需要复杂推理、长程依赖和鲁棒状态追踪的任务上表现不足。

本文提出将序列建模重新理解为一个**贝叶斯滤波问题**，从而引入更丰富的非线性动态和不确定性感知机制。

---

### 提出的新方法：Kalman Linear Attention (KLA)

KLA 是一种新的 **sequence mixer** 层，其核心思想是：
> 将语言建模视为从“噪声观测”中进行潜在状态估计的过程，采用卡尔曼滤波框架实现时间并行的概率推断。

#### 创新点：
- **C1. 卡尔曼滤波的信息形式重参数化（Associative Reparameterisation）**
  - 将标准卡尔曼滤波转换为**信息形式**（information form），证明其精度更新是一个 **Mobius 变换**（即分式线性变换）。
  - 该变换具有**结合律**，因此可以通过 `associative scan` 实现**并行计算**，达到 $O(\log T)$ 并行深度，与 Mamba 同级效率。

- **C2. 不确定性驱动的非线性门控机制**
  - 更新中的“门”由历史精度与当前观测精度的比率决定，形成**历史依赖且非线性**的门控行为。
  - 超越了 GLA/Mamba 中基于输入控制的线性门控。

- **C3. 显式的信念状态不确定性输出**
  - KLA 输出不仅包括后验均值（用于预测），还包括**后验方差/精度**，可用于解释模型置信度、选择性过滤等。

- **C4. Drop-in 替代模块设计**
  - KLA 可作为 Transformer、SSM 或 GLA 的直接替代组件，集成到现代语言模型架构中。

---

### 相比现有方法的优势

| 特性 | Softmax Attention | SSM/GLA (e.g., Mamba) | KLA (本文) |
|------|-------------------|------------------------|------------|
| 表达力 | 非线性（softmax） | 线性/仿射 | **分式线性（Mobius）** |
| 训练效率 | $O(T^2)$ | $O(T)$ | $O(T)$ |
| 推理效率 | $O(T)$ | $O(1)$ | $O(1)$ |
| 是否支持并行训练 | ❌ | ✅ | ✅ |
| 显式状态不确定性 | ❌ | ❌ | ✅ |

> ✅ **KLA 在保持 SSM 效率的同时，获得了更强的表达能力和可解释性。**

---

## 2. 核心实验方法和设置

### 数据集与任务

#### （1）MAD-Lab 合成语言建模套件（Synthetic Tasks）
用于测试不同语言技能，共六项任务：

| 任务 | 测试能力 |
|------|----------|
| **Compression** | 序列压缩与信息瓶颈 |
| **Memorization** | 固定映射记忆 |
| **In-context Recall (MQAR)** | 上下文内键值检索 |
| **Noisy Recall** | 噪声环境下的选择性注意 |
| **Fuzzy Recall** | 多token组合键值匹配 |
| **Selective Copy** | 有序复制与干扰抑制 |

#### （2）长上下文多查询关联回忆（Long-Context MQAR）
- 序列长度：**T = 2048**
- 词表大小：**V = 256**
- 挑战高容量存储与远距离检索能力

#### （3）A5 置换群状态追踪任务（Permutation Composition）
- 输入是一串 A5 群元素，目标是计算它们的乘积。
- 本质是**不可并行化的顺序状态跟踪问题**，用于检验模型是否具备真正的“状态演化”能力。

---

### 实验设置与评估指标

| 设置项 | 描述 |
|-------|------|
| **模型结构** | 单层或双层 block，统一使用 fused-MLP 架构（类似 Mamba） |
| **参数量控制** | 所有模型配置为相同有效状态大小（如 2048）以公平比较 |
| **训练方式** | AdamW 优化器，固定学习率 $1\times10^{-3}$，最大 750 轮 |
| **评估指标** | 分类准确率（Accuracy %），在多个随机种子上取平均 |
| **硬件平台** | NVIDIA A100 80GB GPU，使用 Triton 内核加速 scan 操作 |

---

### 基线方法对比

| 基线 | 类型 | 简介 |
|------|------|------|
| **Mamba** | Selective SSM | 当前最流行的高效 SSM 架构 |
| **GLA** | Gated Linear Attention | 统一多种线性 RNN/SSM 的通用框架 |
| **GDN** (Gated Delta Net) | Delta Rule + SSM | 强调联想记忆机制，擅长 recall |
| **mLSTM** | 改进版 LSTM | 具备乘法门控的现代循环网络 |

---

## 3. 主要实验结果和性能指标

### （1）合成任务性能对比（MAD-Lab）

| 方法 | Compression | Memorization | Context Recall | Noisy Recall | Fuzzy Recall | Selective Copy |
|------|-------------|--------------|----------------|--------------|---------------|----------------|
| GDN | 65.53 | 99.93 | 99.94 | 99.95 | 37.04 | 90.30 |
| GLA | 49.45 | 99.99 | 73.60 | 85.24 | 18.22 | 82.41 |
| Mamba | 78.35 | 99.96 | 99.92 | 99.93 | 30.83 | 80.60 |
| mLSTM | 57.17 | 99.98 | 99.98 | 99.99 | 25.43 | 36.61 |
| **KLA (Ours)** | **85.03** | 98.87 | **99.95** | **99.93** | **45.70** | **90.67** |
| **KLA+** (probabilistic decode) | **88.87** | **99.94** | 99.94 | 99.95 | 43.32 | 91.45 |

> 🔍 **关键发现**：
> - KLA 在所有任务上**持平或超越最强基线**；
> - 在 **Noisy Recall** 和 **Selective Copy** 上优势显著 → 验证了不确定性加权过滤的有效性；
> - 使用**概率解码**（marginalization）进一步提升性能，尤其在噪声任务中。

---

### （2）长上下文 MQAR 性能（T=2048, V=256）

| 模型维度 | KLA | GDN | Mamba | GLA |
|---------|-----|-----|-------|-----|
| d=64 | ~80% | ~75% | ~70% | <10% |
| d=128 | ~90% | ~85% | ~80% | <10% |
| **d=256** | **>95%** | ~90% | ~85% | <10% |

> ✅ **KLA 在极端设置下仍接近完美表现，而 GLA 完全失败**  
> 💡 推测原因：KLA 的 **Mobius 精度更新机制**能自适应地抑制饱和状态下的新噪声输入，实现隐式压缩。

---

### （3）A5 状态追踪任务（Permutation Composition）

![图1](#)  
> *解决 A5 任务所需最小层数 vs 序列长度*

| 方法 | 所需层数趋势 |
|------|-------------|
| Transformer / Linear SSM | 随长度增长而增加（无法恒定深度解决） |
| **KLA** | **仅需 1–2 层即可解决任意长度任务** |

> 🧠 **意义重大**：表明 KLA 的更新机制属于比 TC⁰ 更强的计算类，具备真正意义上的**顺序状态追踪能力**，突破了传统线性 SSM 的理论限制。

---

### （4）消融实验（Ablation Studies）

#### A. OU Prior Discretisation 消融
- 移除 OU 动力学离散化 → 准确率下降，训练不稳定，尤其在深层模型中。
- 说明**连续时间先验 + 正确离散化**对性能至关重要。

#### B. 过程噪声（Process Noise）消融
- 固定过程噪声 $p_t = 0$（退化为确定性系统）→ 平均准确率下降 **49.8个百分点**！
- 最严重的是 Memorization（↓93.6%）和 In-context Recall（↓54.7%）

> ❗ 结论：**过程噪声不是冗余项，而是维持模型表达力的关键组成部分**。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **贝叶斯滤波视角可提升序列建模能力**  
   将 token 视为“噪声观测”而非“控制信号”，自然引出不确定性感知的状态更新机制。

2. ✅ **信息形式下的卡尔曼更新具有结合性**  
   其精度递归构成 Mobius 变换，可通过 `associative scan` 实现高效并行，打破“贝叶斯=串行”的刻板印象。

3. ✅ **KLA 实现了效率与表达力的双赢**  
   - 保留了 SSM 的 $O(T)$ 复杂度和并行训练能力；
   - 获得了超越线性系统的非线性表达力（分式线性）；
   - 显式输出不确定性，增强可解释性和鲁棒性。

4. ✅ **在多项挑战性任务上达到 SOTA**
   - 在合成任务、长上下文 recall 和 A5 状态追踪上全面优于 Mamba、GLA、GDN 等先进方法。

---

### 局限性

- 当前未充分利用 posterior covariance 进行实际应用（如 hallucination detection、OOD detection）；
- 实验集中在浅层、小规模设置，尚未验证在大规模预训练中的表现；
- Triton kernel 尚未完全融合整个 block，仍有优化空间（如 fused kernel）；
- 对 extremely long context（>8k）的支持有待进一步测试。

---

### 未来工作方向

- 将 KLA 扩展至 **Probabilistic LLMs**，探索不确定性量化在生成任务中的价值；
- 开发更高效的 **custom CUDA/Triton kernels**，实现端到端 fused sequence modeling；
- 探索 KLA 在 **RL、world modeling、partial observability** 场景的应用；
- 理论分析 KLA 的计算复杂度边界及其与电路复杂度类的关系。

---

> 📌 **总结一句话**：  
> **KLA 成功将经典贝叶斯滤波与现代高效序列建模结合，在不牺牲速度的前提下，通过不确定性驱动的非线性更新，显著提升了模型的表达力与状态追踪能力，为下一代高效语言模型提供了新范式。**

</details>

---

### 8. [Spend Search Where It Pays: Value-Guided Structured Sampling and Optimization for Generative Recommendation](https://arxiv.org/abs/2602.10699)

**Authors**: Jie Jiang, Yangru Huang, Zeyu Wang, Changping Wang, Yuling Xiong, Jun Zhang, Huan Yu  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.10699v1  

#### Abstract
Generative recommendation via autoregressive models has unified retrieval and ranking into a single conditional generation framework. However, fine-tuning these models with Reinforcement Learning (RL) often suffers from a fundamental probability-reward mismatch. Conventional likelihood-dominated dec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Spend Search Where It Pays: Value-Guided Structured Sampling and Optimization for Generative Recommendation**

---

## **1. 主要贡献和创新点**

### **解决的问题**
该论文针对**生成式推荐系统**（Generative Recommendation, GR）中的一个根本性挑战——**概率-奖励错位**（probability-reward misalignment）。

在基于自回归模型的生成式推荐中，通常采用 likelihood-driven 解码策略（如 beam search）进行候选采样，而训练目标是最大化非可导的业务奖励（如点击率 CTR）。这种不匹配导致两个结构性缺陷：

1. **探索不足**（insufficient exploration）  
   高奖励但低概率的 item（如长尾商品）因其前缀 token 概率较低，在早期被 beam search 剪枝，无法被有效采样。

2. **优势压缩**（advantage compression）  
   共享高概率前缀的候选项形成“兄弟组”（sibling groups），其奖励高度相关，导致 GRPO 等 groupwise RL 方法计算出的优势差异极小，削弱了学习信号。

### **提出的新方法：V-STAR**
为解决上述问题，作者提出了 **V-STAR**（**Value-guided Sampling and Tree-structured Advantage Reinforcement**），一个自演化的采样-优化闭环框架，包含两大核心组件：

#### **(1) Value-Guided Efficient Decoding (VED)**
- **目标**：提升高价值路径的可达性，避免过早剪枝。
- **机制**：
  - 构建一个轻量级的 **value model**，预测从当前 SID 前缀出发的期望未来回报。
  - 引入 **prefix priority score**：$ G(s) = V(s) + \lambda H_\pi(s) $，结合 value（预期奖励）和 policy entropy（不确定性）。
  - 在固定计算预算下，优先扩展 **高价值且高不确定性** 的前缀节点（decisive nodes），实现**有导向的稀疏搜索**。

#### **(2) Sibling-GRPO**
- **目标**：缓解兄弟组内的优势压缩，增强学习信号。
- **机制**：
  - 利用生成树的拓扑结构，将候选划分为 **sibling groups**（共享同一父前缀的子节点）。
  - 在每个 sibling group 内部计算 **relative advantage**，而非在整个候选集上做全局归一化。
  - 更新聚焦于**分支决策点**，强化对关键 token 选择的梯度信号。

### **相比现有方法的优势**
| 维度 | 传统方法 | V-STAR |
|------|--------|--------|
| **探索效率** | 贪婪剪枝，忽略低概率高奖励路径 | 价值引导，精准扩展高潜力分支 |
| **学习信号强度** | 全局归一化导致优势压缩 | sibling-relative 归一化保留局部对比 |
| **计算开销** | 全树搜索（如 MCTS）不可行；盲目扩大 beam 宽度低效 | 固定预算下的稀疏扩展，高效利用计算资源 |
| **结构感知** | 忽视生成树的层次结构 | 显式建模 prefix tree 拓扑 |

---

## **2. 核心实验方法和设置**

### **数据集**
- **离线数据集**：Amazon Review 数据集的两个子集
  - **Industrial Products**
  - **Office Products**
- **在线数据**：微信视频号（WeChat Channels）真实流量，进行为期 5 天的 A/B 测试（5% 流量）。

### **实验设置**
- **模型骨干**：Qwen2.5-1.5B
- **训练流程**：
  1. **SFT**（Supervised Fine-tuning）
  2. **RL 微调**：使用 GRPO 或 Sibling-GRPO
- **VED 参数**：
  - 候选数：16
  - SID 长度：3
  - 初始化 beam 宽度：8
  - 折扣因子 γ：0.99
  - 正则系数 λ：0.1

### **评估指标**
- **离线指标**：
  - **HR@K**（Hit Rate）
  - **NDCG@K**（Normalized Discounted Cumulative Gain）
- **在线指标**：
  - **GMV**（Gross Merchandise Volume）
  - **GMV-Normal**（点击/转化优化广告的 GMV）

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **传统序列推荐** | GRU4Rec, Caser, SASRec |
| **生成式非 SID** | HSTU, BIGRec |
| **SID 生成式（无 RL）** | TIGER, LCRec, D3 |
| **SID + RL / DPO** | S-DPO, MiniOneRec |
| **解码策略对比** | Beam Search, Top-K Sampling |

---

## **3. 主要实验结果和性能指标**

### **离线性能（Table 1）**
在 Industrial 和 Office 数据集上，V-STAR 均取得 SOTA 性能：

| 方法 | Industrial HR@3 | Industrial NDCG@10 | Office HR@3 | Office NDCG@10 |
|------|------------------|---------------------|------------|---------------|
| MiniOneRec (最强基线) | 0.1143 | 0.1167 | 0.1217 | 0.1088 |
| **Ours (V-STAR)** | **0.1189** (+4.0%) | **0.1217** (+4.3%) | **0.1344** (+10.4%) | **0.1196** (+9.9%) |

> ✅ **一致超越所有基线**，尤其在 Office 数据集上增益显著。

---

### **在线 A/B 测试（Section 5.3）**
在微信视频号真实场景中：

| 指标 | 相对提升 |
|------|----------|
| **GMV** | **+1.23%** |
| **GMV-Normal** | **+1.87%** |

> ✅ 商业价值显著提升，验证了方法在真实业务中的有效性。

---

### **消融实验**

#### **(1) 解码策略对比（Table 2）**
| 解码策略 | Industrial NDCG@10 | Office HR@10 |
|---------|--------------------|-------------|
| Beam Search | 0.1194 | 0.1684 |
| Top-K | 0.1090 | 0.1644 |
| **Ours (VED)** | **0.1217** | **0.1746** |

> ✅ VED 在保持质量的同时，显著优于纯随机（Top-K）和贪婪（Beam）策略。

#### **(2) 扩展规则消融（Table 3）**
| 扩展规则 | Industrial NDCG@10 |
|---------|--------------------|
| Value-only ($V_\phi$) | 0.1202 |
| Entropy-only ($H_\pi$) | 0.1198 |
| **Joint Score ($G(s)$)** | **0.1217** |

> ✅ **价值与不确定性的联合评分**效果最佳，证明两者协同作用。

#### **(3) 训练目标消融（Table 4）**
| 训练目标 | Industrial NDCG@10 |
|---------|--------------------|
| GRPO (全局归一化) | 0.1189 |
| Sibling-GRPO | 0.1204 |
| **GRPO + Sibling-GRPO (联合)** | **0.1217** |

> ✅ Sibling-GRPO 明显优于标准 GRPO，联合使用效果最好。

---

### **其他分析**
- **多样性 vs 质量**（Table 6）：
  - VED 的候选集 **多样性更高**（Div(C) 更大），同时 **最高奖励项更优**（MaxReward 更高）。
- **计算效率**（Figure 4）：
  - 在相同 token 预算下，VED 的性能增长远超扩大 beam 宽度，**计算效率更高**。
- **案例研究**（Table 5）：
  - Beam Search 和 Top-K 均未找到 GT item（ACCUTECK Digital Scale），而 VED 成功通过新颖分支 `<a_20>` 发现。

---

## **4. 关键结论和发现**

### **主要发现**
1. **概率驱动的解码存在结构性偏见**，会系统性忽略高奖励长尾 item。
2. **价值引导的稀疏搜索**（VED）能以极低成本提升高价值路径的可达性。
3. **Sibling-GRPO 有效缓解了优势压缩**，使 RL 学习信号在兄弟组内依然强健。
4. **V-STAR 构成自演化闭环**：更好的采样 → 更清晰的学习信号 → 更好的策略 → 更好的采样。

### **方法的局限性**
- **依赖高质量的 value model**：若 value 估计不准，可能导致错误扩展。
- **仍需离线索引构建**：SID 编码依赖 RQ-VAE 等离线处理。
- **测试时默认回退到 beam search**：虽然可部署 VED，但增加了延迟。

### **未来工作方向**
- 将 VED 部署至线上推理，进一步提升长尾覆盖。
- 探索更高效的 value model 架构（如共享 backbone）。
- 将 Sibling-GRPO 思想推广至其他结构化生成任务（如 slate generation）。

---

> **总结**：V-STAR 通过 **value-guided structured sampling** 和 **tree-aware credit assignment**，实现了在严格延迟约束下，生成式推荐系统的**高效探索**与**稳定优化**，为工业级 GR 系统提供了实用且高性能的解决方案。

</details>

---

### 9. [PRISM: Parallel Residual Iterative Sequence Model](https://arxiv.org/abs/2602.10796)

**Authors**: Jie Jiang, Ke Cheng, Xin Xu, Mengyang Pang, Tianhao Lu, Jiaheng Li, Yue Liu, Yuan Wang, Jun Zhang, Huan Yu, Zhouchen Lin  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.10796v1  

#### Abstract
Generative sequence modeling faces a fundamental tension between the expressivity of Transformers and the efficiency of linear sequence models. Existing efficient architectures are theoretically bounded by shallow, single-step linear updates, while powerful iterative methods like Test-Time Training ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# PRISM: Parallel Residual Iterative Sequence Model 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
生成式序列建模面临一个根本性的矛盾：**Transformers** 虽然表达能力强，但其 $O(N^2)$ 的计算复杂度在处理超长序列时效率极低；而现有的高效模型（如 Linear Attention 和 SSMs）虽然具备 $O(N)$ 复杂度，但由于依赖单步线性更新（One-Shot Optimization），存在“**线性瓶颈**”（Linearity Bottleneck），即只能进行 Rank-1 的状态更新，难以捕捉高阶特征相关性和迭代优化能力。

更进一步，像 **Test-Time Training (TTT)** 这类通过运行时多步梯度下降提升表达力的方法，虽然能突破线性限制，却因梯度依赖于隐藏状态而导致**串行依赖**（Serial Dependency），破坏了硬件并行性，训练吞吐量极低。

PRISM 正是为了解决这一“**表达力与效率不可兼得**”的根本张力而提出。

---

### 提出了什么新方法或新思路

PRISM (**Parallel Residual Iterative Sequence Model**) 提出了一种全新的 **Amortized Residual Optimization** 框架，核心思想是：

> 将显式的、串行的多步迭代优化过程，“摊销”（amortize）为一个可并行计算的前馈算子。

具体创新点包括：

#### （1）Write-Forget Decoupling（写入-遗忘解耦）
- **理论依据**：通过谱扰动分析证明，**遗忘操作**（A）对近似误差鲁棒（累积误差为 $O(\ln T)$ 或 $O(1)$），而**写入操作**（B）对误差极其敏感（累积误差为 $O(T)$）。
- **设计决策**：因此将遗忘路径保持为简单的 Rank-1 线性衰减（保证稳定性和并行性），而将模型表达力集中在非线性、高秩的写入路径上。

#### （2）Input-Anchored Loop Unrolling（输入锚定循环展开）
- 为了绕过显式求解器的串行依赖，PRISM 引入了一个两阶段代理机制：
  1. **短卷积锚定**（ShortConv）：用局部历史窗口的卷积输出 $u \approx S_{t-1}k_t$ 来近似全局状态交互，作为“锚点”。
  2. **学习型预测器**（Learned Predictor）：基于该锚点，并行预测多步迭代修正的方向和增益，从而直接估计最终的高秩注入矩阵 $B_t$。

这使得原本需要串行执行的非线性优化轨迹被“折叠”进一个并行可计算的算子中。

#### （3）Rank-L Accumulation（秩-L 积累）
- 通过一个未展开的残差循环（unrolled residual loop），逐步构建一个由 $L$ 个正交 Rank-1 分量组成的高秩更新矩阵 $B = \sum_{l=1}^L \beta^{(l)} (\delta^{(l)} \otimes k^{(l)})$。
- 理论证明该设计实现了 **Rank-L Accumulation**，打破了传统线性模型的 Rank-1 瓶颈，结构上逼近理想非线性增量规则的解。

---

### 相比现有方法的优势

| 维度 | PRISM | TTT / TITANS | Linear Models (e.g., DeltaNet) |
|------|-------|---------------|-------------------------------|
| **表达力** | ✅ 高（模拟多步非线性优化） | ✅ 高（真实多步优化） | ❌ 低（单步 Rank-1 更新） |
| **并行性** | ✅ 完全并行（$O(N)$ 训练） | ❌ 串行依赖（$O(N)$ 推理延迟） | ✅ 完全并行 |
| **吞吐量** | ⬆️ 极高（实测 174× TTT） | ⬇️ 极低 | ➖ 中等 |
| **结构创新** | ✅ 将优化轨迹“摊销”为静态策略 | ✅ 显式优化 | ❌ 启发式更新 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
在四个主流推荐系统基准上进行评估，这些任务对**高秩稀疏性**（High-Rank Sparsity）要求极高，是检验模型表达力的理想压力测试：
- **Amazon Books**
- **Amazon Movies**
- **Amazon Elecs**
- **Yelp**

数据统计见下表：

| Dataset | #Users | #Items | Avg. Length |
|--------|--------|--------|------------|
| Amazon_books | 31,103 | 339,960 | 106.72 |
| Amazon_movies | 9,429 | 58,636 | 121.22 |
| Amazon_elecs | 1,869 | 33,135 | 65.89 |
| Yelp | 17,233 | 126,829 | 93.17 |

---

### 实验设置和评估指标

#### 评估指标
- **推荐质量**：`NDCG@K`, `Hit@K` ($K=200, 500$)，以及 `AUC`
- **效率指标**：**Training Throughput**（千 token/秒），在单张 NVIDIA H20 GPU 上测量

#### 模型规模
主实验采用 **0.13B 参数模型**，确保公平比较。

#### 并行化实现
所有递归模型均基于 **Flash-Linear-Attention** 内核实现，以最大化硬件效率。

---

### 基线方法对比

分为三类：

#### （1）Transformer 上界（非线性强基线）
- **SASRec**, **HSTU**：代表性能上限，但为 $O(N^2)$

#### （2）线性递归模型（启发式更新）
- **SLA**, **GLA**, **GSA**, **MoM**, **Mamba-2**

#### （3）优化启发模型（Optimization-Inspired）
- **Gated DeltaNet**, **TTT-Linear**, **ATLAS**, **TITANS**

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Amazon_books 和 Amazon_movies 为例）

| Model | Amazon_books NDCG@200 | Amazon_movies NDCG@200 | AUC (Mean) | Throughput (K tok/s) |
|-------|--------------------------|--------------------------|------------|------------------------|
| SASRec (Transformer) | 0.0215 | 0.0244 | 0.8910 | ~3.8 @16K |
| HSTU (Transformer) | 0.0233 | 0.0293 | 0.7748 | ~3.8 @16K |
| TITANS (Optimization) | 0.0243 | 0.0278 | 0.9395 | ~0.34 |
| **PRISM (Ours)** | **0.0238** | **0.0289** | **0.9393** | **~57–61** |
| Mamba-2 | 0.0234 | 0.0276 | 0.9385 | ~60 |
| Gated DeltaNet | 0.0230 | 0.0253 | 0.9367 | ~60 |

> 注：PRISM 在多个指标上达到甚至超越 TITANS 和 TTT，且远超其他线性模型。

---

### 与基线方法的对比结果

- **表达力方面**：
  - PRISM 性能**媲美甚至超越 TTT 和 TITANS**，显著优于所有传统线性模型（如 SLA, GLA）。
  - 在 Amazon Books 上，PRISM 的 AUC 与 SASRec 几乎持平，表明其**几乎消除了线性注意力的“表达税”**。

- **效率方面**：
  - PRISM 的训练吞吐量**高达 57–61K tokens/s**，在整个序列长度范围内保持稳定。
  - 相比之下，**TTT 仅为 0.34K tokens/s**，**PRISM 实现了 174× 的吞吐提升**。
  - Transformer++ 在短序列上有优势，但在 16K 序列上因二次复杂度严重退化。

---

### 消融实验结果（Ablation Study）

在 Amazon Elecs 上对 PRISM 各组件进行消融：

| Model Variant | Hit@200 | AUC |
|--------------|---------|-----|
| **PRISM (Full)** | **0.1409** | **0.7134** |
| w/o Iterative Refinement (L=1) | 0.1155 | 0.6805 |
| w/o Non-Linearity | 0.1316 | 0.7047 |
| w/o ShortConv Anchor | 0.1406 | 0.7076 |
| w/o Gain Predictor | 0.1383 | 0.7098 |

#### 结论：
- **移除迭代细化（L=1）导致最大性能下降**，验证了“深度”是扩展记忆容量的关键。
- **移除非线性**明显降低性能，说明 GELU 等非线性激活对建模复杂轨迹至关重要。
- **移除 ShortConv 锚点或增益预测器**也导致性能下滑，说明局部上下文锚定和自适应步长不可或缺。

---

## 4. 关键结论和发现

### 论文的主要发现

1. **“One-Shot Bottleneck” 是高效序列模型的核心限制**：标准线性更新无法支持多步迭代优化，限制了表达力。
2. **Amortized Optimization 是可行路径**：无需在推理时执行真实梯度下降，即可通过学习输入锚定的代理策略来“模拟”优化轨迹。
3. **PRISM 实现了表达力与效率的统一**：
   - 表达力上接近 TTT/TITANS；
   - 效率上与标准线性模型相当，**吞吐量达 TTT 的 174×**；
   - 理论上证明其具有 **Rank Accumulation** 特性，突破 Rank-1 限制。
4. **Mechanistic Probing 显示 PRISM 具备非线性推理能力**：
   - 在逻辑任务（如 Parity, XOR）上，PRISM 成功而 LA/MoM 失败（~50%，随机水平）；
   - 在结构任务（如 Palindrome, Modulo Add）上，PRISM 甚至**超过 Transformer**，得益于其对局部结构的强归纳偏置。

---

### 方法的局限性

1. **局部近似牺牲了全局反应性**：
   - ShortConv 仅捕获局部历史能量，**无法感知遥远过去的记忆状态**（如“这个事实是否已存储？”）。
   - 因此 PRISM 不具备真正的“长期纠错”能力。

2. **不解决容量墙问题**：
   - PRISM 提升的是**写入质量**（Fidelity），而非**状态容量**（Capacity）。
   - 在固定维度 $d \times d$ 的状态下，信息覆盖仍是不可避免的问题。

3. **依赖高质量局部信号**：
   - 若任务的关键信息高度分散或依赖长距离依赖，ShortConv 可能失效。

---

### 未来工作方向

1. **Capacity Expansion + PRISM Writing**：
   - 将 PRISM 作为高密度写入算子，与 **Mixture-of-Memories (MoM)** 或 **Gated Slot Attention (GSA)** 等容量扩展架构结合。

2. **Hybrid Architectures**：
   - 构建 **PRISM + Transformer** 混合模型：PRISM 负责日常高效更新，Transformer 层定期介入纠正“长尾漂移”。

3. **动态调整 Refinement Depth $L$**：
   - 根据输入复杂度动态选择迭代细化层数，实现计算资源的自适应分配。

4. **探索更优的 Anchor Mechanism**：
   - 替代 ShortConv，尝试使用轻量级注意力或稀疏检索来构建更准确的状态代理。

---

> **代码已开源**：`github.com`（原文提及，具体链接需查证）

</details>

---

### 10. [MerLin: A Discovery Engine for Photonic and Hybrid Quantum Machine Learning](https://arxiv.org/abs/2602.11092)

**Authors**: Cassandre Notton, Benjamin Stott, Philippe Schoeb, Anthony Walsh, Gr\'egoire Leboucher, Vincent Espitalier, Vassilis Apostolou, Louis-F\'elix Vigneux, Alexia Salavrakos, Jean Senellart  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.11092v1  

#### Abstract
Identifying where quantum models may offer practical benefits in near term quantum machine learning (QML) requires moving beyond isolated algorithmic proposals toward systematic and empirical exploration across models, datasets, and hardware constraints. We introduce MerLin, an open source framework...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《MerLin: A Discovery Engine for Photonic and Hybrid Quantum Machine Learning》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前量子机器学习（QML）领域存在以下关键挑战：
- **软件生态碎片化**：不同框架（如 Qiskit、Cirq、Perceval、PennyLane 等）专注于特定硬件或范式（gate-based 或 photonic），导致算法难以跨平台复用。
- **缺乏系统性基准测试**：多数研究基于孤立的算法提案，代码可复现性低（photonic QML 中仅约 43% 开源），且实验设置不统一，难以公平比较。
- **仿真与硬件脱节**：许多框架无法在强模拟（strong simulation）与真实 QPU 执行之间无缝切换，限制了算法-硬件协同设计（co-design）。

### 🚀 提出的新方法与创新
作者提出 **MerLin** —— 一个面向光子量子机器学习（photonic QML）与混合量子-经典模型的开源框架，具备以下核心特性：

#### （1）**端到端可微分训练集成**
- 将优化后的 **Strong Linear Optical Simulation (SLOS)** 引擎嵌入 PyTorch 和 scikit-learn 生态。
- 支持 `torch.nn.Module` 风格的 `QuantumLayer`，实现自动微分（autograd）驱动的梯度优化，适用于 VQC、kernel methods、reservoir computing 等多种架构。

#### （2）**硬件感知设计（Hardware-aware Design）**
- 支持通过 `MerlinProcessor` 接口连接 Quandela 的真实 photonic QPU（如 Belenos、Ascella），实现在云上进行 shot-based inference。
- 提供 detector model、noise modeling、microbatch 控制等接口，贴近实际硬件约束。

#### （3）**跨模态桥接能力（Cross-modal Interoperability）**
- 引入 `QuantumBridge` 抽象层，支持从 gate-based 电路（如 qubit VQC）向 photonic 电路转换（例如 dual-rail 编码或 QLOQ 方案），促进 hybrid 架构探索。

#### （4）**以复现为先的基准平台（Reproduction-first Benchmarking）**
- 成功复现了 **18 篇前沿 photonic/hybrid QML 工作**，涵盖 kernel methods、QCNN、QLSTM、QGAN、reservoir computing 等。
- 所有复现实验模块化发布于 GitHub，作为共享 baseline，推动社区标准化评估。

### 🔍 相比现有方法的优势
| 特性 | MerLin | 其他框架（如 PennyLane, Perceval, DeepQuantum） |
|------|--------|---------------------------------------------|
| Photonic 原生支持 | ✅ 完整 Fock space + SLOS 加速 | ⚠️ 多数侧重 CV 或 gate-emulation |
| PyTorch 深度集成 | ✅ 原生 autograd + nn.Module 兼容 | ⚠️ 接口抽象层次较高，灵活性受限 |
| 可微分强模拟 | ✅ 支持 exact state computation 与 analytic gradients | ⚠️ 多数仅支持采样或弱模拟 |
| 真实 QPU 联动 | ✅ 支持远程调用 Quandela QPU | ✅ Perceval 支持，但无 ML 生态整合 |
| 统一基准平台 | ✅ 内建 18 项复现实验 | ❌ 多数无系统性 benchmark 支持 |

---

## 2. 核心实验方法和设置

### 📚 数据集使用情况
MerLin 的复现实验覆盖多个标准 ML 任务与数据集：

| 任务类别 | 使用数据集 |
|--------|-----------|
| 图像分类 | MNIST, CIFAR-10（前5类）, Custom BAS（二值图像） |
| 文本情感分析 | SST-2（Sentiment Analysis） |
| 合成数据集 | Spiral dataset（带噪声）、Moon dataset（非线性可分） |
| 时间序列预测 | Lorenz system（混沌动力学） |
| 生成任务 | MNIST（patch-based generation for QGAN） |

> 注：部分实验使用合成数据以验证模型表达能力（expressivity）而非追求 SOTA 性能。

### ⚙️ 实验设置与评估指标
- **训练流程**：采用标准 PyTorch 训练循环（Adam optimizer, CrossEntropyLoss），支持 batch training。
- **测量策略**：
  - `probs`: 输出全概率分布（用于分类）
  - `partial measurement`: 局部模式测量（用于 QCNN pooling）
  - `amplitudes`: 返回量子态振幅（用于 kernel 构造）
- **评估指标**：
  - 分类准确率（Accuracy）
  - SSIM（用于图像生成质量）
  - 参数效率（#params 达到 ≥90% 准确率）
  - 训练速度（epoch time, speedup ratio）
  - 对抗鲁棒性（clean vs. adversarial accuracy）

### 🆚 基线方法对比
MerLin 在复现过程中与以下基线进行了横向比较：
- **Classical baselines**：传统 CNN、RNN、SVM、ResNet
- **Gate-based QML**：Qiskit 实现的 QNN、QLSTM、qSSL
- **Photonic-only simulators**：原始 Perceval 实现
- **Hybrid models**：HQNN、DQNN、QRKD

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总（来自 Table I）

| 方法 | 任务 | MerLin 结果 | 原始文献结果 | 提升/差异 |
|------|------|------------|--------------|----------|
| **Photonic QCNN [19]** | MNIST (0 vs 1) | **98.8±1.0%** | 93.1±3.6% | ↑ ~5.7% |
| | Custom BAS | **98.2±2.2%** | 92.7±2.1% | ↑ ~5.5% |
| **qSSL [29]** | CIFAR-10 (5 classes) | **49.2% acc**, ×0.97 speed | 48.4% (Qiskit), ×0.08 speed | 更快 + 更高精度 |
| **QLSTM [31]** | Function fitting | 与 gate-based QLSTM 相当 | — | 验证 photonic 替代可行性 |
| **QGAN [37]** | MNIST generation | 相同 SSIM，**训练提速达 15×** | — | 优化器升级（SGD > SPSA）带来显著加速 |
| **DQNN [34]** | MNIST 分类 | 测试准确率差 <4%，**训练 epoch 减少 4×** | — | ADAM 替换 COBYLA 显著提升效率 |
| **Fidelity Kernel [15]** | Kernel learning | 随训练集增大性能提升趋势一致 | — | 验证 photonic 实现有效性 |
| **Adversarial Robustness [39]** | MNIST | Clean: ~98%, Adv(BIM ε=0.1): ~15% → **对抗训练后恢复至 ~95%** | — | 表明对抗训练有效 |

### 🔬 消融实验结果（Ablation Studies）
- **编码方式对鲁棒性影响**：
  - **Amplitude encoding**：极易受小扰动攻击（ε 很小时性能骤降）
  - **Angle encoding**：表现出更强的对抗鲁棒性
  - ➝ **建议在安全敏感场景优先使用 angle encoding**

- **维度压缩 vs. 表达力权衡**：
  - 对 amplitude encoding 施加 PCA（256→64）可缓解过拟合并提高鲁棒性，但牺牲 high-dimensional representational power。

- **光子数量与表达力关系**：
  - 复现 [14] 发现：增加输入光子数 `n` 显著提升 VQC 的 Fourier 频谱宽度，从而增强模型 expressivity —— **光子数是控制模型容量的关键超参**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Photonic QML 模型可在多种任务中媲美甚至超越 gate-based QML**：
   - 在相同任务下，photonic-native 实现（如 MerLin）相比 Qiskit 实现不仅更快，有时还能取得更高准确率（如 qSSL）。
   
2. **MerLin 显著提升了仿真效率与开发便捷性**：
   - 利用 TorchScript 编译与稀疏计算图优化，SLOS 仿真速度相比原生 Perceval 提升可达 **数个数量级**。
   - PyTorch 原生集成极大简化了调试、可视化与混合建模流程。

3. **系统性复现揭示“量子优势”需谨慎解读**：
   - 多数所谓“量子优越”实则源于数据预处理、模型工程或优化技巧，而非本质量子机制。
   - MerLin 提供透明实验环境，有助于剥离虚假增益，聚焦真正有意义的改进。

4. **Photon-count 是 photonic QML 的关键资源参数**：
   - 输入光子数直接影响 Hilbert space dimension 与模型表达能力，是 photonic 平台特有的缩放维度。

### ⚠️ 方法的局限性
- **模拟规模受限**：由于 SLOS 需存储完整 Fock state vector，目前实用上限约为 **n ≤ 20 光子**，超出后内存爆炸。
- **仅支持离散变量（DV）光子计算**：暂未涵盖 continuous-variable（CV）模型（如 Gaussian boson sampling）。
- **依赖 Quandela 硬件生态**：目前仅支持其自有 QPU，尚未接入其他 photonic 平台。

### 🔮 未来工作方向
- **扩展至更大规模模拟**：引入近似方法（如 tensor network truncation、Monte Carlo sampling）突破 exact simulation 限制。
- **支持更多硬件后端**：接入其他 photonic 或 hybrid QPU（如 Xanadu, PsiQuantum）。
- **构建自动化 benchmark suite**：建立类似 MLPerf 的量子机器学习评测标准，推动社区规范化。
- **探索 emergent 光子元件**：如 photonic memristor、nonlinear elements，进一步增强模型非线性与记忆能力。

---

> 🔗 **项目地址**：  
> - MerLin 框架: [https://github.com/merlinquantum/merlin](https://github.com/merlinquantum/merlin)  
> - 复现实验仓库: [https://github.com/merlinquantum/reproduced_papers](https://github.com/merlinquantum/reproduced_papers)

📌 **一句话总结**：  
MerLin 不只是一个 simulator，而是一个 **benchmark-driven、hardware-aware、ML-ecosystem-native 的 discovery engine**，旨在推动 photonic QML 进入可复现、可比较、可协同设计的新阶段。

</details>

---

### 11. [SynergyKGC: Reconciling Topological Heterogeneity in Knowledge Graph Completion via Topology-Aware Synergy](https://arxiv.org/abs/2602.10845)

**Authors**: Xuecheng Zou, Yu Tang, Bingbing Wang  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.10845v1  

#### Abstract
Knowledge Graph Completion (KGC) fundamentally hinges on the coherent fusion of pre-trained entity semantics with heterogeneous topological structures to facilitate robust relational reasoning. However, existing paradigms encounter a critical "structural resolution mismatch," failing to reconcile di...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SynergyKGC: Reconciling Topological Heterogeneity in Knowledge Graph Completion via Topology-Aware Synergy

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **Knowledge Graph Completion (KGC)** 方法在融合预训练语义（如 PLMs）与图拓扑结构（如 GNNs）时面临一个根本性瓶颈：**被动的结构-语义对齐**（Passive Structural-Semantic Alignment）。这导致两大病理现象：
- **Representation Collapse**：在稀疏子图中，实体缺乏足够的拓扑支撑，表示坍缩；
- **Identity Redundancy**：在密集簇中，显式自环引入不可约的噪声。

此外，多数模型存在 **Inference-time Distribution Shift** ——训练时使用结构信息，推理时禁用，造成表征漂移。

---

### 🚀 提出的新方法与创新思路

作者提出 **SynergyKGC**，一种高精度、指令驱动的双模态协同框架，其核心创新包括：

#### （1）**Cross-Modal Synergy Expert**
- 引入 **relation-aware cross-attention** 机制，将语义向量作为“查询指令”主动检索并过滤拓扑上下文；
- 替代传统 GNN 的被动邻居聚合，实现动态、有选择性的结构信息融合。

#### （2）**Dual-Axis Consistency Principle**
- **架构轴一致性**（Architectural Axis）：Query Tower 与 Entity Tower 在结构融合上保持对称协同；
- **生命周期轴一致性**（Lifecycle Axis）：训练与推理阶段均激活 Synergy Expert，消除分布偏移。

#### （3）**Density-Aware Identity Anchoring (IA) 策略**
- 提出“结构即身份”（Structure ~ Identity）现象理论：在稠密图中冗余的身份信号可被抑制，在稀疏图中则需保留以提供位置锚点；
- 自适应地决定是否加入实体自身嵌入（identity anchor），缓解稀疏下的坍缩与稠密中的过平滑。

#### （4）**Instantaneous "Catch-up Effect"**
- 模型无需长周期 warm-up（传统方法常 >30 epochs），在 Synergy Expert 激活后立即触发双向同步，快速收敛至高性能状态。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 ProgKGC, SimKGC） | SynergyKGC |
|------|-------------------------------|-----------|
| 结构融合方式 | 被动添加、静态融合 | 主动检索、指令驱动 |
| 训练-推理一致性 | 不一致（推理去结构化） | 严格一致（双塔实时协同） |
| 密度适应能力 | 固定策略，难以兼顾稀疏/稠密 | 动态切换，密度感知锚定 |
| 收敛速度 | 需要长时间语义预热 | 快速“追赶效应”，显著提速 |

---

## 2. 核心实验方法和设置

### 📊 数据集
在两个标准 benchmark 上进行验证，具有极化拓扑与语义特性：

| 数据集 | 特征描述 |
|--------|---------|
| **FB15k-237** | 稠密关系图（P50=22），文本丰富（平均 114.6 tokens） |
| **WN18RR** | 层次稀疏图（P50=3），语义简洁（平均 17.1 tokens） |

> 表格见原文 Table 1，涵盖 #Entities, #Relations, 密度分位数等统计信息。

---

### 🧪 实验设置
- **模型架构**：
  - 语义编码器：`bert-base-uncased`
  - 协同专家：4-head Synergy Expert（d=768）
- **优化器**：AdamW，batch size=768，单卡 A100
- **学习率**：从 {1e-5, 5e-5, 5e-4} 中调优
- **温度参数**：T=0.05
- **Dropout**：0.1
- **两阶段训练**：
  - Phase I：仅语义训练（contrastive loss）
  - Phase II：激活 Synergy Expert，联合优化（NCE + MSE alignment）

---

### 🎯 评估指标
采用标准 **filtered setting** 下的排序指标：
- **MRR**（Mean Reciprocal Rank）
- **Hits@k**（k ∈ {1, 3, 10}）
- **MR**（Mean Rank，用于衡量全局排序稳定性）

---

### ⚔️ 基线方法对比
涵盖主流 KGC 范式：
- **Embedding-based**：TransE, RotatE, DistMult, ComplEx, TuckER
- **PLM-based / Hybrid**：KG-BERT, StAR, LP-BERT, KG-S2S, SimKGC, ProgKGC

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Table 2）

| Method | FB15k-237 (MRR / H@1 / H@3 / H@10) | WN18RR (MRR / H@1 / H@3 / H@10) |
|--------|-------------------------------------|----------------------------------|
| **ProgKGC (SOTA)** | 34.4 / 25.5 / 37.5 / 52.3 | 68.2 / 59.7 / 73.9 / 83.4 |
| **SynergyKGC (Ours)** | **39.9 / 30.2 / 43.6 / 59.4** | **74.2 / 67.7 / 78.5 / 85.5** |
| **Improvement** | **+5.5 / +4.7 / +6.1 / +7.1** | **+6.0 / +8.0 / +4.6 / +2.1** |

> 💡 **核心突破**：在 **WN18RR 上 Hits@1 提升 +8.0%**，创下新 SOTA。

---

### 🔍 消融实验结果（Table 4 & Figure 4）

#### （1）组件消融分析（Ablation Study）
移除关键模块后的性能下降显著：

| 模块 | WN18RR (H@1) 下降 | FB15k-237 (H@1) 下降 | 说明 |
|------|------------------|--------------------|------|
| w/o **Cross-Attention** | ↓25.7% | ↓1.8% | 对稀疏图至关重要，“定位支架” |
| w/o **Adaptive Gate** | ↓16.5% | ↓2.9% | 抑制稠密图噪声的关键滤波器 |
| w/o **MSE Alignment** | ↓1.7% | ↓1.7% | 维持语义一致性基础 |

> 表明各模块在不同密度场景下发挥差异化作用，验证 topology-aware 设计必要性。

---

#### （2）Dual-Tower Consistency 影响（Figure 4）
- 若只在 Query Tower 启用 Synergy，Entity Tower 保持静态 → 性能大幅下降；
- 只有在 **训练与推理阶段都启用双塔协同**，才能避免 distribution shift，达到最优。

---

#### （3）Topological Scaling 分析（Table 3）
| 数据集 | 最优跳数 | 观察 |
|-------|--------|------|
| **FB15k-237 (Dense)** | 2-hop | 局部邻域即可解歧 |
| **WN18RR (Sparse)** | 1-hop（精度）但 5-hop（全局排序） | 浅层锚定 top-tier，深层提升 global ranking（MR 显著改善） |

---

#### （4）Identity Anchoring 敏感性分析（Table 5）
- 在 **WN18RR** 中，不设阈值（保留所有 identity）效果最佳（MRR=74.2）；
- 在 **FB15k-237** 中，仅当 `threshold=1` 时最优，更高阈值引入噪声；
- 证明 IA 策略必须依赖图本身的拓扑密度，而非统一设定。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **结构性异质性是 KGC 的核心挑战**：不能用统一策略处理稀疏与稠密子图。
2. **主动协同优于被动融合**：通过语义意图引导结构检索，大幅提升效率与精度。
3. **Dual-Axis Consistency 是稳定性的基石**：架构对称 + 训推一致，有效防止 manifold drift。
4. **Identity Anchoring 具有密度依赖性**：“结构即身份”理论成立，为稀疏图提供必要锚点。
5. **SynergyKGC 实现快速收敛**：得益于“catch-up effect”，可在更少 epoch 内超越需长 warm-up 的模型。

---

### ⚠️ 方法局限性
- 当前设计依赖于高质量的 entity description 文本，若文本缺失或低质，语义专家性能受限；
- 虽然支持多跳聚合，但计算复杂度随邻居数量增长，极端 hub 节点可能影响效率；
- 当前实验集中于静态 KG，未考虑时间动态性（Temporal KGC）扩展。

---

### 🔮 未来工作方向
- 扩展至 **Temporal Knowledge Graphs**，结合时序注意力机制；
- 探索 **few-shot / zero-shot KGC** 场景下的迁移能力；
- 进一步轻量化 Synergy Expert，提升大规模部署效率；
- 将“指令驱动协同”范式推广至其他多模态结构化任务（如推荐系统、生物网络推理）。

---

## ✅ 总结
SynergyKGC 通过提出 **指令驱动的跨模态协同机制** 和 **双重一致性原则**，成功解决了 KGC 中长期存在的拓扑异质性难题。其实验表现不仅刷新了 WN18RR 等稀疏基准的记录（Hits@1 +8.0%），更重要的是揭示了一个通用原则：**在非均匀结构数据中，鲁棒的信息整合必须是自适应、动态且训练-推理一致的**。该工作为下一代高精度 KGC 框架提供了重要范式参考。

> 🔗 开源地址：[https://github.com/XuechengZou-2001/SynergyKGC-main](https://github.com/XuechengZou-2001/SynergyKGC-main)

</details>

---

### 12. [R2RAG-Flood: A reasoning-reinforced training-free retrieval augmentation generation framework for flood damage nowcasting](https://arxiv.org/abs/2602.10312)

**Authors**: Lipai Huang, Kai Yin, Chia-Fu Liu, Ali Mostafavi  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.10312v1  

#### Abstract
R2RAG-Flood is a reasoning-reinforced, training-free retrieval-augmented generation framework for post-storm property damage nowcasting. Building on an existing supervised tabular predictor, the framework constructs a reasoning-centric knowledge base composed of labeled tabular records, where each s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：R2RAG-Flood: A reasoning-reinforced training-free retrieval augmentation generation framework for flood damage nowcasting

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统洪水灾损预测模型（如 FloodDamageCast）依赖**监督学习**，需要为每个区域单独收集数据、特征工程、训练和调优模型，且需处理严重的类别不平衡问题。这导致其在新区域部署时成本高、扩展性差。

本文旨在解决以下挑战：
- 如何在不进行任务特定训练（task-specific training）的前提下实现高精度灾损 nowcasting；
- 如何提升模型的可解释性和推理透明度；
- 如何在资源受限场景下实现高效部署。

---

### 提出的新方法与创新思路
作者提出 **R2RAG-Flood** —— 一种**无需训练的检索增强生成框架**（training-free Retrieval-Augmented Generation, RAG），结合大语言模型（LLM）与地理空间推理机制，用于洪涝财产损失程度（Property Damage Extent, PDE）的实时预测。

#### 核心创新点：
1. **构建“以推理为中心”的知识库**（reasoning-centric knowledge base）
   - 对历史标注样本，不仅存储结构化特征和标签，还利用 LLM 自动生成包含完整逻辑链的 `<think>...</think>` 推理轨迹。
   - 每个样本包含：`{tabular predictors, text-mode summary, reasoning trajectory}`。
   - 支持后续通过检索相似案例的“思考过程”来引导新样本预测。

2. **基于分布差异分析的特征排序与提示优化**
   - 使用 **Jensen-Shannon (JS)** 和 **Kolmogorov-Smirnov (KS)** 统计量量化各特征在不同 PDE 类别间的分布差异。
   - 构建 **divergence-informed feature profile**，用于：
     - 文本摘要中的特征排序（重要特征优先描述）；
     - 自由样本（free-shot）选择依据；
     - 下降规则（downgrade rules）触发线索。

3. **两阶段预测 + 条件降级机制**（Conditional Downgrade Mechanism）
   - 预测分两步：先判断是否发生损坏（occurrence），再判断严重程度（severity）。
   - 引入基于规则的**自动降级检查**：当 `<think>` 中的叙述证据不足以支持高严重性预测时，强制将预测从 `2→1` 或 `1→0`。
   - 有效缓解 LLM 过度自信导致的误报问题。

4. **多源上下文增强提示设计**
   - 在预测时动态注入三种信息：
     - 地理邻近邻居（within 1km）及其推理轨迹；
     - 典型原型（prototypes）—— 最具代表性的各类别样本；
     - 边界困难样本（hard-boundary cases）—— 接近决策边界的模糊案例。
   - 支持 HUC12 流域内本地检索，不足时回退到全局库。

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **部署效率** | 完全免训练（training-free），适用于快速迁移至新区域 |
| **可解释性** | 输出结构化推理路径，支持事后归因分析 |
| **鲁棒性** | 利用真实历史案例的推理模式，避免纯文本归纳偏差 |
| **成本效益** | 轻量级 LLM 变体在“严重性每单位成本”上显著优于监督模型和大型 LLM |

---

## 2. 核心实验方法和设置

### 数据集
- **研究区域**：美国德克萨斯州哈里斯县（Harris County），飓风 Harvey（2017年）事件。
- **空间粒度**：统一聚合为 500m × 500m 网格单元，并按 **HUC12 流域编码** 分组。
- **总样本数**：训练集 10,764；测试集 4,614。
- **PDE 标签构建**：
  - 基于 NFIP + FEMA IA 洪水索赔数据；
  - 归一化后划分为三级：
    - Low (0): 无或轻微损失；
    - Medium (1): 中等损失；
    - High (2): 重大损失；
  - 分布比例：44.7% / 40.6% / 14.6%

### 特征变量（共14项）
来自多个开源数据库，涵盖四大类：
| 类别 | 特征示例 |
|------|--------|
| Built Environment | FAR, Building Age, Foundation Height, POI Number |
| Topography | Elevation, HAND, Imperviousness, Terrain Roughness |
| Hydrology | Distance to Stream, Flood Claims in Past 50 Years |
| Event-specific | Maximum Rainfall |

---

### 实验设置
#### LLM Backbones（共7种）
| 类型 | 模型名称 |
|------|---------|
| Proprietary | `gpt-4o-mini`, `gpt-4o`, `gpt-4.1`, `gpt-5-mini` |
| Open-source | `Llama-3.1-70B-Instruct`, `Qwen3-30B-A3B-Instruct-2507`, `DeepSeek-R1`（API调用） |

所有模型均使用相同 prompt 模板，**无任何 fine-tuning**，仅通过 context-augmented prompting 实现预测。

---

### 评估指标

#### 预测性能指标
| 指标 | 描述 |
|------|------|
| **Overall Accuracy** | 三分类整体准确率 |
| **Macro-F1** | 平衡各类别的 F1 分数 |
| **Damage Class Accuracy** | 仅限中/高损样本（class 1 & 2）的子集准确率 |
| **Severity Score** | 序数距离得分：<br>$ \text{score}_i = 1 - \frac{|y_i - \hat{y}_i|}{2} $，满分 1.0 |
| **Recall₂** | 高损类（class 2）召回率 |

#### 成本效率指标
- **CostIdx**：每样本等效美元成本
  - 监督模型：包含训练 + 推理 GPU 实例费用（EC2 g6e.xlarge）
  - GPT系列：基于 API 输入/输出 token 计费
  - 开源模型：基于量化推理下的 GPU 运行时间折算
- **Efficiency (Severity/CostIdx)**：单位成本获得的严重性评分，衡量性价比

#### 推理质量评估（Instance-level Reasoning Metrics）
| 指标 | 含义 |
|------|------|
| **LRA** (Label-Reasoning Alignment) | 推理内容是否支持最终预测标签 |
| **SFC** (Salient Feature Coverage) | 是否提及关键判别特征（基于 divergence ranking） |
| **FDC** (Feature Direction Consistency) | 特征描述方向是否与数值一致（如“高降雨”对应实际高值） |
| **PAS** (Prototype Alignment Score) | 是否模仿典型原型的推理模式 |
| **BTS** (Boundary Tradeoff Score) | 是否在边界样本中体现正反证据权衡 |

---

### 基线方法对比
- **Supervised Baseline**: `FloodDamageCast*`  
  - 基于 Gradient Boosted Tree 的监督模型
  - 使用相同的14维结构化特征输入
  - 在相同测试集上训练并评估

---

## 3. 主要实验结果和性能指标

### 性能汇总（见 Table 6）

| Model | Overall Acc | Damage Class Acc | Severity Score | Recall₂ | CostIdx | Efficiency |
|-------|-------------|------------------|----------------|---------|---------|------------|
| **FloodDamageCast*** (baseline) | **0.714** | 0.859 | 0.844 | 0.672 | 0.030 | 28.6 |
| **gpt-4.1** | 0.668 | 0.816 | **0.825** | 0.439 | 0.133 | **6.2** |
| **gpt-5-mini** | 0.647 | **0.867** | 0.809 | **0.591** | 0.023 | 35.9 |
| **gpt-4o-mini** | 0.660 | 0.859 | 0.819 | 0.346 | **0.010** | **81.9** ✅ |
| **Llama-3.1-70B** | 0.613 | 0.896 ✅ | 0.787 | 0.529 | 0.199 | 4.0 |
| **Qwen3-30B** | 0.642 | 0.811 | 0.807 | 0.576 | 0.132 | 6.1 |
| **DeepSeek-R1** | 0.644 | 0.757 | 0.801 | 0.458 | 0.050 | 16.0 |

> ✅ 表示该列最优；**加粗**表示全局最佳

---

### 关键发现
- R2RAG-Flood 多数 LLM 变体在 **Damage Class Accuracy** 上接近甚至超过监督基线（最高达 **0.896**）；
- 尽管整体准确率略低（~0.61–0.67 vs 0.714），但在**严重性排序一致性**（Severity Score）上表现优异（最高 0.825）；
- **轻量级模型（如 gpt-4o-mini）在效率上碾压其他模型**：
  - 效率达到 **81.9**，远高于基线（28.6）和大模型（4.0–6.2）；
  - 适合资源敏感、需快速部署的应用场景。

---

### 消融实验结果（Ablation Study）

四个逐步增强的配置：
1. **Text-mode only**：仅目标文本摘要
2. **+ Neighbor reasoning**：加入最多3个地理邻近样本及其推理
3. **+ Free-shots**：邻居不足时补充原型与边界样例
4. **+ Downgrade**：启用条件降级机制

#### 结果趋势（图3）：
- 所有 LLM 在引入 **neighbor reasoning** 后 Macro-F1 显著提升 → 表明地理类比推理有效；
- 加入 **free-shots** 后进一步稳定稀疏区域预测；
- **Downgrade 机制带来小幅但稳定的正向影响**，说明其有效纠正过度预测；
- 完整流程（IV）效果最优，验证了整体设计合理性。

---

## 4. 关键结论和发现

### 主要结论
1. ✅ **R2RAG-Flood 是首个无需训练即可实现灾损 nowcasting 的 LLM 推理框架**，通过检索历史案例的“思考过程”实现知识迁移。
2. ✅ **推理轨迹 + 上下文提示 + 规则校正** 的组合显著提升了预测合理性和稳定性。
3. ✅ **轻量级 LLM 在成本效率上具有巨大优势**，尤其适合应急响应场景下的边缘部署。
4. ✅ 方法在保持较高 damage class accuracy 和 severity consistency 的同时，提供完整的解释性输出。

---

### 局限性
1. **地域泛化能力未充分验证**：目前仅在 Harris County + Hurricane Harvey 场景下测试，结果可能受区域特性影响。
2. **依赖高质量历史数据**：若某地区缺乏足够标注样本，检索质量下降，影响性能。
3. **自动化评估局限**：当前 reasoning metrics 为启发式规则驱动，难以完全捕捉专家级推理深度。
4. **固定 prompt 设计**：尚未融合人类专家参与的动态模板优化。

---

### 未来工作方向
1. **跨区域、多灾害类型扩展**：在更多城市和风暴事件中验证泛化能力。
2. **引入多模态证据**：融合遥感影像、社交媒体文本报告等非结构化数据。
3. **探索多种解码策略**：测试不同 temperature、beam search 等对性能与成本的影响。
4. **专家协同设计 prompt 与 free-shot 选择标准**：通过调查问卷等方式获取领域专家偏好，替代当前自动化选择机制。
5. **生命周期成本分析**：开展面向实际业务负载的端到端成本建模，超越当前 evaluation-time accounting。

--- 

> **一句话总结**：  
> R2RAG-Flood 成功将 LLM 的零样本推理能力与地理空间灾损预测相结合，提出了一条**免训练、强解释、高效率**的新技术路径，为灾害应急响应提供了更具实用价值的 AI 工具。

</details>

---

### 13. [QTALE: Quantization-Robust Token-Adaptive Layer Execution for LLMs](https://arxiv.org/abs/2602.10431)

**Authors**: Kanghyun Noh, Jinheon Choi, Yulwha Kim  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.10431v1  

#### Abstract
Large language models (LLMs) demand substantial computational and memory resources, posing challenges for efficient deployment. Two complementary approaches have emerged to address these issues: token-adaptive layer execution, which reduces floating-point operations (FLOPs) by selectively bypassing ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《QTALE: Quantization-Robust Token-Adaptive Layer Execution for LLMs》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在部署时面临两大挑战：
- **高计算开销**：大量 FLOPs 导致推理延迟高。
- **高内存占用**：全精度权重消耗大量显存。

已有两种主流优化技术：
- **Token-adaptive layer execution**（如 D-LLM）：通过动态跳过部分 transformer 层减少 FLOPs。
- **Quantization**：降低权重精度以减小模型体积和内存带宽需求。

然而，**直接将二者结合会导致显著的准确率下降**。原因在于 token-adaptive 模型本身已减少了冗余（训练路径单一 + 参数参与少），而量化进一步引入扰动，使模型更脆弱。

> QTALE 正是为了解决“**token-adaptive 执行与量化难以协同**”这一关键问题而提出。

---

### 🚀 提出的新方法：QTALE
QTALE 是一种新型框架，旨在实现 **token-adaptive execution 与 quantization 的无缝集成**，同时保持高准确性。其核心创新包括两个组件：

#### （1）Quantization-Robust Training Strategy（量化鲁棒性训练策略）
- 引入 **熵最大化目标函数 $ \mathcal{L}_{\text{entropy}} $** 来正则化 router 的输出 logits。
- 目标是让 execute 和 bypass 的 logit 差距变小，提升路由决策的不确定性（stochasticity），从而鼓励更多样化的执行路径在 fine-tuning 中被探索。
- 这增强了 **training-path redundancy**，防止某些层长期不被执行而导致欠训练。

#### （2）Post-Training Execution Ratio Adjustment Mechanism（推理阶段执行比例调节机制）
- 在推理时通过调整 threshold $ \theta $ 动态控制 layer bypass 行为。
- 支持灵活增加执行层数（即提高 execution ratio），以 **按需恢复参数冗余度**，增强对量化噪声的鲁棒性。
- 使用简单的 grid search 即可在小校准集上找到最优阈值。

---

### 🔍 相比现有方法的优势
| 维度 | D-LLM（Baseline） | QTALE（本文） |
|------|------------------|-------------|
| 路径多样性 | 低（收敛到固定子集执行） | 高（熵正则化促进随机探索） |
| 对量化的鲁棒性 | 差（微小扰动导致路径漂移） | 强（平滑路由边界） |
| 可调性 | 固定执行比例 | 推理时可调 execution ratio |
| 准确性（+量化后） | 明显下降 | 接近 full-model，差距 <0.5% |

> ✅ **QTALE 成功桥接了 FLOPs 降低与 memory footprint 降低的技术鸿沟，实现了高效且准确的联合压缩。**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **CommonsenseQA Benchmark Suite**：
  - PIQA, BoolQ, SIQA, ARCe, ARCc, Winogrande (Winogr.), OBQA
- **其他评估任务**：
  - MMLU（多任务理解）
  - Stanford-Alpaca 和 SAMSum 数据集上的 **Perplexity (PPL)** 测评

所有实验均采用 **zero-shot setting**，避免下游任务微调干扰结果。

---

### ⚙️ 实验设置
| 项目 | 设置说明 |
|------|--------|
| **模型** | LLaMA2-7B, LLaMA3.1-8B, LLaMA3.2-3B |
| **量化方式** | 使用 AWQ 进行 Post-Training Quantization (PTQ)<br>- 4-bit 与 3-bit 整数量化<br>- Group size = 128 |
| **Token-adaptive 实现** | 基于 D-LLM 架构，在每层添加轻量 MLP router |
| **训练配置** | Fine-tuning 超参复用原始 D-LLM 设置（学习率、epoch 数等） |
| **评估指标** | - Zero-shot Accuracy (%)<br>- Perplexity (PPL)<br>- FLOPs（归一化）<br>- Memory footprint (GB)<br>- Inference Latency / Speedup |

---

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **Full-layer Execution** | 不进行 layer skipping 的标准 fine-tuned 模型（上限参考） |
| **AWQ** | 仅量化，无 token-adaptive |
| **D-LLM** | 仅 token-adaptive 执行 |
| **D-LLM + AWQ** | 朴素组合：先应用 D-LLM 再量化 → 主要对比对象 |
| **QTALE (ours)** | 提出的方法：带熵正则训练 + 推理期 ratio 调整 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（代表性结果）

#### ▶️ 在 LLaMA3.2-3B + 4-bit AWQ 上的 CSQA 准确率：
| 方法 | Accuracy (%) |
|------|--------------|
| Full-layer (16-bit) | 80.51 |
| D-LLM (4-bit) | 73.96 ❌（↓6.55%） |
| **QTALE (4-bit)** | **78.41** ✅（接近 full model） |

> ➤ QTALE 比 D-LLM 提升 **+4.45%**，几乎追平量化后的 full model（77.55%）

#### ▶️ 在 LLaMA2-7B + 3-bit AWQ 上的结果：
| 方法 | CSQA Accuracy (%) |
|------|--------------------|
| Full (3-bit) | 72.22 |
| D-LLM (3-bit) | 70.57 |
| **QTALE (3-bit)** | **72.79** ✅

> ➤ QTALE 不仅没有损失，反而略超 full model！

---

### 🔁 与基线方法对比总结
- **D-LLM + Quantization 组合严重退化**：
  - 平均 accuracy 下降可达 5~7%，尤其在低比特（3-bit）下更明显。
- **QTALE 成功维持 accuracy**：
  - 与 quantization-only 模型之间的差距 **始终控制在 0.5% 以内**（CommonsenseQA 上）。
- **FLOPs 降低约 46%**，**Memory footprint 降低至 ~4.5GB（原 13.5GB）**，实现双重压缩。

#### 图表支持（Figure 6）：
- QTALE 在 **FLOPs-Memory 折衷曲线上显著优于 baseline**，达到帕累托前沿。

---

### 🔍 消融实验结果（Ablation Study）

#### 表格 4：各组件贡献分析（LLaMA3.2-3B, 4-bit, CSQA）
| 方法 | Accuracy (%) | 执行比例 |
|------|-------------|---------|
| D-LLM（无任何改进） | 73.31 | 0.54 |
| + $ \mathcal{L}_{\text{entropy}} $（仅训练改进） | 75.12 | 0.54 |
| + Threshold adjustment $ \theta $（仅推理调节） | 74.88 | 0.57 |
| **完整 QTALE（两者结合）** | **77.62** | 0.58 |

> ✅ 两个组件均有独立增益，**联合使用效果最佳**。

#### 关键观察：
- 仅靠 entropy 正则无法改变最终执行比例，但提升了路径稳定性。
- 仅靠 threshold 调节若缺乏训练多样性，则提升有限。
- **必须两者结合才能实现真正的 quantization robustness**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Token-adaptive 与 quantization 的冲突根源是“冗余缺失”**：
   - D-LLM 类方法因训练路径单一、参数利用率低，天然对量化敏感。
2. **QTALE 有效重建了冗余性**：
   - 通过熵正则增强训练多样性（training-path redundancy）。
   - 通过推理期调节 reintroduce 参数冗余（parameter redundancy）。
3. **实现了高效与准确的统一**：
   - 同时降低 FLOPs（≈46%↑ speedup）和 memory（↓68%）。
   - accuracy 损失可忽略（<0.5% vs quantized full model）。
4. **兼容性强**：
   - 适用于不同 PTQ 方法（AWQ、MagR+GPTQ 均验证有效）。
   - 可扩展至其他压缩技术（如 pruning，见 Table 6）。

---

### ⚠️ 方法的局限性
1. **依赖 router 模块引入额外训练成本**：
   - 尽管 router 很轻量，但仍需 fine-tuning 阶段介入，非完全免训练。
2. **执行比例调节需要一个小规模校准集**：
   - 虽然 grid search 快速实用，但在严格 zero-data 场景下仍需假设存在少量样本。
3. **目前聚焦于 PTQ，未涉及 QAT（Quantization-Aware Training）**：
   - 若结合 QAT 可能进一步优化性能，但复杂度上升。

---

### 🔮 未来工作方向
1. **自动化 threshold selection**：
   - 探索无需人工搜索的自适应 threshold 控制机制（如基于输入难度预测）。
2. **扩展到 dynamic sparse training 或 MoE 架构**：
   - 将路径多样性思想迁移到更广泛的稀疏化范式中。
3. **跨模态或长序列场景的应用**：
   - 验证 QTALE 在 vision-language 模型或 long-context 推理中的有效性。
4. **硬件感知联合优化**：
   - 结合实际 GPU memory bandwidth 与 compute capability 设计端到端最优策略。

---

## ✅ 总结一句话
> **QTALE 通过“训练时鼓励路径多样性 + 推理时按需恢复冗余”，首次实现了 token-adaptive layer execution 与 quantization 的稳定协同，在大幅降低 FLOPs 与 memory 的同时，几乎无损地保留了模型准确性，为高效 LLM 部署提供了统一可行的解决方案。**

</details>

---

### 14. [Prioritize the Process, Not Just the Outcome: Rewarding Latent Thought Trajectories Improves Reasoning in Looped Language Models](https://arxiv.org/abs/2602.10520)

**Authors**: Williams Jonathan, Tureci Esin  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.10520v1  

#### Abstract
Looped Language Models (LoopLMs) perform multi-step latent reasoning prior to token generation and outperform conventional LLMs on reasoning benchmarks at smaller parameter budgets. However, attempts to further improve LoopLM reasoning with reinforcement learning have failed - standard objectives su...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Prioritize the Process, Not Just the Outcome: Rewarding Latent Thought Trajectories Improves Reasoning in Looped Language Models*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统的强化学习目标（如 **GRPO**）在应用于 **Looped Language Models (LoopLMs)** 时效果不佳。其根本原因在于这些方法只对生成前的**最终隐状态**（final latent state）进行奖励，而忽略了模型内部多步迭代推理的完整“思维轨迹”（thought trajectory）。这种“终端奖励瓶颈”与 LoopLMs 多步内部计算的本质存在根本性错配。

### 提出了什么新方法或新思路
本文提出了 **RLTT (Reward Latent Thought Trajectories)**，一种新的强化学习框架。其核心思想是：
- **将奖励信号分布到整个隐状态推理轨迹上**，而非仅奖励最后一个循环步骤。
- 通过聚合每个内部循环（loop）产生的 next-token 分布，并基于此构建策略梯度，实现对全过程的信用分配（credit assignment）。
- 该方法无需依赖外部验证器（external verifiers），可直接替代 GRPO，且计算开销极小。

### 相比现有方法的优势
- **更密集的信用分配**：解决了 GRPO 中奖励信号需反向传播多个隐层的难题。
- **更强的泛化能力**：训练出的策略在数学和非数学任务上均表现出色。
- **更高的推理效率**：诱导模型更快收敛到正确答案，减少冗余思考。
- **实现简单**：可无缝集成到现有 LoopLM 架构中，无需复杂修改。

---

## 2. 核心实验方法和设置

### 使用的数据集
#### 数学推理基准（Math Benchmarks）：
- **MATH-500**：标准数学问题集合。
- **AIME24**：美国邀请数学考试题，难度高。
- **BeyondAIME**：超越高中奥赛水平的挑战性数学题。
- **GSM8K**：小学数学应用题。

#### 非数学推理基准（Non-Math Benchmarks）：
- **ARC-C**：AI2 推理挑战，逻辑与常识推理。
- **MMLU-ST**：大规模多任务语言理解（STEM 子集）。
- **GPQA**：研究生级谷歌无法回答的问题，考察多跳事实推理。
- **MBPP**：面向编程的任务，评估代码生成能力。

> 所有非数学任务均为**零样本迁移评估**，模型仅在数学数据上训练。

### 实验设置和评估指标
- **基础模型**：`Ouro-2.6B-Thinking`，一个开源的 LoopLM。
- **训练条件**：严格控制计算资源匹配，与 GRPO 在相同 rollout 预算、优化器、学习率等条件下训练。
- **评估方式**：
  - 使用**确定性解码**（deterministic decoding）。
  - 采用 **exact-match** 作为准确率指标。
  - 设置固定的最大生成长度（如 MATH-500 为 2048 tokens）。
- **奖励函数**：基于最终答案正确性的二值奖励（0/1）。

### 基线方法对比
- **SFT**（监督微调）
- **GRPO**（Group Relative Policy Optimization）——当前主流的 RLVR 方法
- **+RLTT**：本文提出的方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| Model | MATH-500 | AIME24 | BeyondAIME | GSM8K | Avg. Math |
|-------|----------|--------|------------|-------|-----------|
| +GRPO | 71.6     | 16.7   | 6.0        | 59.7  | 38.5      |
| **+RLTT** | **86.0** | **33.3** | **16.0**   | **94.0** | **57.3** |
| **提升 (+)** | **+14.4** | **+16.6** | **+10.0**  | **+34.3** | **+18.8** |

| Model | ARC-C | MMLU-ST | GPQA | MBPP | Avg. Non-Math |
|-------|-------|---------|------|------|----------------|
| +GRPO | 93.7  | 86.1    | 19.7 | 61.3 | 65.2           |
| **+RLTT** | **94.4** | **89.6** | **38.4** | **64.6** | **71.8**       |
| **提升 (+)** | **+0.7** | **+3.5** | **+18.7** | **+3.3** | **+6.6**         |

> ✅ **所有提升均经配对 t 检验确认具有统计显著性（p < 0.05）**

### 与基线方法的对比结果
- **全面超越 GRPO**：在所有数学与非数学基准上均取得显著提升。
- **最难任务提升最大**：在 AIME24 和 BeyondAIME 上分别提升 **+16.6%** 和 **+10.0%**，说明 RLTT 特别擅长处理需要持续多步推理的复杂任务。
- **零样本迁移能力强**：尽管只在数学数据上训练，RLTT 在 GPQA 上几乎**翻倍**于 GRPO（+18.7%），表明其提升了通用的隐式推理能力。

### 消融实验结果
#### （1）不同循环权重策略的影响（Table 5）
测试了三种权重方案：
- **Uniform**（均匀加权）
- **Progressive**（越后权重越高）
- **Exit-probability**（基于 Ouro 的早退机制概率）

> 🔍 **发现**：不同策略间性能差异很小，说明 RLTT 的优势主要来自于“全程信用分配”这一核心机制，而非具体权重设计。

#### （2）不同循环次数下的表现（Table 9）
在 1–4 个循环下评估模型性能：
- RLTT 在所有循环数下均优于 GRPO。
- 在 **1–2 个循环**的受限条件下，RLTT 优势尤为明显（如 GSM8K 上 +26.2% @1 loop），证明其早期推理更高效。

#### （3）解码长度鲁棒性（Table 3）
在不同 token 预算（1024–4096）下测试 MATH-500：
- RLTT 在所有预算下均优于 GRPO。
- 即使在远超训练长度（4096 tokens）的情况下仍保持领先，说明未过拟合特定长度。

#### （4）Pass@k 分析（Figure 6）
- RLTT 在增加采样数 k 时性能提升更陡峭，说明其保留了更多有效的推理路径，**低熵源于“可控自信”而非“模式坍缩”**。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **过程奖励优于结果奖励**：对 LoopLMs 而言，奖励完整的“思维轨迹”比只奖励最终输出更有效。
2. **RLTT 显著提升推理效率**：模型能以更少的 token 和循环次数达到正确答案，减少“过度思考”。
3. **泛化能力强**：不仅在数学任务上表现优异，还能零样本迁移到逻辑、问答、编程等非数学领域。
4. **理论支持**：论文从理论上证明，轨迹级信用分配会诱导更短的最优解码长度（Theorem A.5），解释了其高效性。

### 方法的局限性
- **内存开销增加**：需存储每轮循环的 log-probabilities，导致 GPU 上可打包的 token 数减半（从 16384 降至 8192）。
- **架构依赖性强**：目前仅适用于具有显式循环结构的 **LoopLMs**，难以直接用于标准 Transformer。
- **固定循环深度**：实验中未使用 Ouro 原生的自适应早退机制，牺牲了动态计算分配的能力。

### 未来工作方向
- 开发**内存高效的实现**，降低部署成本。
- 将 RLTT 与**自适应早退机制**结合，在保持轨迹奖励的同时恢复动态计算能力。
- 探索 RLTT 在其他类型的隐式推理模型（如 Huginn）上的适用性。
- 研究如何将该思想扩展至非循环架构，例如通过中间层监督模拟“轨迹”。

> 💡 **总结**：RLTT 通过“奖励过程而非仅仅结果”，成功对齐了强化学习目标与 LoopLMs 的内在计算机制，为提升大模型的隐式推理能力提供了新范式。

</details>

---

### 15. [Divide, Harmonize, Then Conquer It: Shooting Multi-Commodity Flow Problems with Multimodal Language Models](https://arxiv.org/abs/2602.11057)

**Authors**: Xinyu Yuan, Yan Qiao, Zonghui Wang, Wenzhi Chen  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11057v1  

#### Abstract
The multi-commodity flow (MCF) problem is a fundamental topic in network flow and combinatorial optimization, with broad applications in transportation, communication, and logistics, etc. Nowadays, the rapid expansion of allocation systems has posed challenges for existing optimization engines in ba...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Divide, Harmonize, Then Conquer It: Shooting Multi-Commodity Flow Problems with Multimodal Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

本文聚焦于**Multi-Commodity Flow (MCF)** 问题，即在复杂网络拓扑中为多个商品流（如网络流量、物流运输等）分配路径，以优化目标函数（如最小化最大链路利用率 MLU、最大化总吞吐量 MTF 或并发流 MCF）。该问题是网络资源调度、交通工程、云计算等领域中的核心挑战。

随着现代系统规模扩大（数千节点、百万级流量），传统基于 **Linear Programming (LP)** 的求解器面临严重瓶颈：计算时间随变量数呈次立方增长（$O(d^{2.3729}$)，难以满足实时性需求。而现有的 **Machine Learning (ML)** 方法虽快，但存在泛化能力差、对未见环境敏感、需大量重训练等问题。

### 提出了什么新方法或新思路

作者提出 **PRAM (Partitioned Resource Allocation with Multimodal Language Models)**，是首个将 **Multimodal Language Models (MLMs)** 应用于 MCF 问题的端到端解决方案，其核心思想可概括为 “**Divide, Harmonize, Then Conquer**”：

1. **Divide (划分)**：  
   将全局 MCF 问题按**源节点 (source)** 划分为多个子任务（subproblems），每个子任务处理从同一源出发的所有流。这显著降低了单个子问题的维度，实现并行化。

2. **Harmonize (协调)**：  
   引入一个轻量级的 **multi-agent reinforcement learning (MARL)** 框架，通过 **LoRA (Low-Rank Adaptation)** 和 **context learning** 实现智能体间的通信与协同。采用 **counterfactual policy gradients** 来评估每个智能体对全局目标的贡献，从而学习协调策略。

3. **Conquer (解决)**：  
   使用预训练的 **MLM** 作为共享的“代理”模型，接收**图像形式的子拓扑图**（vision encoder 输入）和**文本形式的需求信息**（prompt 输入），联合推理生成高质量的路径权重分配方案。

### 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **效率** | 比 LP 求解器快 **1~2 个数量级**（10–100×），尤其在大规模拓扑上表现突出。 |
| **质量** | 性能接近最优（LP 解），平均差距 <8%，部分场景甚至优于 LP（如 CERNET 上 MLU 低 21%）。 |
| **泛化性** | 对链路故障、流量突增等未见事件表现出强鲁棒性（性能下降 <10%），无需重新训练。 |
| **通用性** | 支持多种 MCF 目标（MLU, MTF, MCF），且不依赖特定 DNN 架构设计，适配性强。 |
| **可扩展性** | 通过问题划分和轻量适配，模型参数增长缓慢，适用于超大规模网络。 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

实验涵盖两类数据集：

#### (1) **真实世界数据集**（小规模）
- **MetaDB**, **MetaWEB**：社交网络集群内部流量
- **Abilene**, **CERNET**, **GEANT**：广域网（WAN）流量矩阵（public TM 数据）

#### (2) **大规模合成数据集**
- **GtsCe**, **Colt**, **UsCarrier**, **Cogentco**, **Kdl**：从 Internet Topology Zoo 提取的大规模拓扑（100–754 节点）
- 需求生成方式：
  - **Gravity Model**（主用）
  - **Poisson Model**
  - **Bimodal Model**

### 实验设置和评估指标

#### 评估指标（均归一化至 LP 最优解）
- **Maximum Link Utilization (MLU)** ↓：越低越好，反映网络健壮性
- **Maximum Total Flow (MTF)** ↑：越高越好，反映吞吐能力
- **Maximum Concurrent Flow (MCF)** ↑：越高越好，反映公平性

#### 实验设置
- 主干模型：`Qwen2.5-VL-7B-Instruct`（截断前 8 层）
- 适配方式：LoRA + context learning + MARL fine-tuning
- 训练/验证/测试比例：7:1:2
- 硬件：双 NVIDIA A100 GPU

### 基线方法对比

| 方法 | 类型 | 特点 |
|------|------|------|
| **LP (Gurobi)** | 优化求解器 | 近似最优，但极慢 |
| **LP-top** | 启发式 | 仅对 top 10% 流量用 LP，其余走最短路径 |
| **POP** | 分治优化 | 将问题复制 k 份并行求解 |
| **DRL** | 强化学习 | 端到端学习策略网络 |
| **HARP** | GNN-based ML | 图神经网络 + 不变性设计 |
| **Aether** | 多智能体 GNN | 区分大小流，分别处理 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | PRAM 表现 |
|------|-----------|
| **MLU 平均差距** | <8% vs. LP 最优 |
| **运行时加速比** | **10–100×** 快于 LP，**5–7×** 快于 POP |
| **最大规模测试** | Kdl 拓扑（754 节点，1790 链路）下 **<25 秒完成一次分配** |
| **鲁棒性** | 在链路故障或流量波动下，性能下降 **<10%** |

### 与基线方法的对比结果

#### (1) 小规模真实数据集（Fig. 6）
- PRAM 在所有三个目标上均达到**第二好性能**，仅次于拥有完美预测的 LP。
- 在 **CERNET 和 GEANT** 上，PRAM 的 **MLU 甚至优于 LP**（分别低 21% 和 45%），表明 MLM 更擅长捕捉 MLU 的凸性结构。
- HARP 专为 MLU 设计，在此任务上表现第二，但在 MTF/MCF 上较差。

#### (2) 大规模合成数据集（Fig. 7）
- PRAM 在所有拓扑上均实现**可扩展的高性能**，质量接近 LP，速度远超所有优化方法。
- 相比 HARP 和 Aether：
  - MLU 降低 **6.1% 和 17.2%**
  - MTF 提升 **16.6% 和 7.3%**
  - MCF 提升 **24.8% 和 13.5%**

### 消融实验结果

#### (1) **是否划分问题（w/o partition）**
- 无划分时，可训练参数急剧膨胀（Kdl 上达 31,600 MB），无法实际部署。
- 有划分时，参数增长平缓，体现其**可扩展性优势**。

#### (2) **关键组件消融（Fig. 9b）**
- **w/o MLM**：性能大幅下降，尤其在链路故障场景下，说明 MLM 的先验知识对泛化至关重要。
- **w/o LoRA / w/o context / w/o MARL**：性能均有下降，证明三者共同构成有效的轻量协调机制。
- **w/o MARL**：无法评估个体贡献，导致协调失败。

#### (3) **不同 MLM 主干对比（Fig. 19）**
- `Qwen2.5-VL-7B` > `Qwen2.5-VL-3B`：参数越多，性能越好。
- `Qwen2.5-VL-7B` > `Llama-3.2-11B-Vision`：尽管后者更大，但 Qwen 因更强的数学推理训练，在 MCF 上表现更优。

---

## 4. 关键结论和发现

### 主要发现

1. **MLM 可作为强大的 MCF 求解器**：  
   MLM 不仅能理解多模态输入（图+文），还能通过 **in-context learning** 模拟 **gradient descent** 过程，逼近最优解（Theorem 2）。

2. **Divide-and-Conquer + MARL 是有效范式**：  
   通过源节点划分实现高效并行，再通过轻量 MARL 协调，既保证了速度，又维持了全局一致性。

3. **PRAM 具备强理论保障**：  
   - MCF 目标函数具有凸/凹性（Theorem 1）
   - MARL 适配过程收敛（Lemma 1）
   - MLM 可模拟 GD 更新（Theorem 2）

4. **PRAM 泛化能力强**：  
   在链路故障、流量波动等未见场景下仍保持稳定性能，远超传统 ML 方法。

### 方法的局限性

1. **训练成本高**：尽管推理快，但 fine-tuning 仍需大量资源。
2. **视觉编码可能引入偏差**：将拓扑图转为图像可能导致信息损失或误解（如边重叠）。
3. **非自回归生成**：当前为 non-autoregressive 输出，未来可探索 autoregressive 形式提升精度。
4. **内存开销**：重复处理相同子图仍消耗较多显存。

### 未来工作方向

1. 探索更高效的 **parameter-efficient fine-tuning** 方法（如 Adapter、IA³）。
2. 引入 **autoregressive generation** 以支持更复杂的决策序列。
3. 结合 **memory mechanisms** 减少重复计算和存储开销。
4. 扩展至动态 MCF、不确定性优化等更复杂场景。

---

> **代码开源**：https://github.com/Y-debug-sys/PRAM  
> **伦理声明**：未使用非公开数据，无隐私泄露风险。

</details>

---

### 16. [Physically Interpretable AlphaEarth Foundation Model Embeddings Enable LLM-Based Land Surface Intelligence](https://arxiv.org/abs/2602.10354)

**Authors**: Mashrekur Rahman  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.10354v1  

#### Abstract
Satellite foundation models produce dense embeddings whose physical interpretability remains poorly understood, limiting their integration into environmental decision systems. Using 12.1 million samples across the Continental United States (2017--2023), we first present a comprehensive interpretabil...

---

### 17. [dnaHNet: A Scalable and Hierarchical Foundation Model for Genomic Sequence Learning](https://arxiv.org/abs/2602.10603)

**Authors**: Arnav Shah, Junzhe Li, Parsa Idehpour, Adibvafa Fallahpour, Brandon Wang, Sukjun Hwang, Bo Wang, Patrick D. Hsu, Hani Goodarzi, Albert Gu  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.10603v1  

#### Abstract
Genomic foundation models have the potential to decode DNA syntax, yet face a fundamental tradeoff in their input representation. Standard fixed-vocabulary tokenizers fragment biologically meaningful motifs such as codons and regulatory elements, while nucleotide-level models preserve biological coh...

---

### 18. [MoToRec: Sparse-Regularized Multimodal Tokenization for Cold-Start Recommendation](https://arxiv.org/abs/2602.11062)

**Authors**: Jialin Liu, Zhaorui Zhang, Ray C. C. Cheung  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11062v1  

#### Abstract
Graph neural networks (GNNs) have revolutionized recommender systems by effectively modeling complex user-item interactions, yet data sparsity and the item cold-start problem significantly impair performance, particularly for new items with limited or no interaction history. While multimodal content...

---

### 19. [Flow of Spans: Generalizing Language Models to Dynamic Span-Vocabulary via GFlowNets](https://arxiv.org/abs/2602.10583)

**Authors**: Bo Xue, Yunchong Song, Fanghao Shao, Xuekai Zhu, Lin Chen, Luoyi Fu, Xinbing Wang, Zhouhan Lin  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.10583v1  

#### Abstract
Standard autoregressive language models generate text token-by-token from a fixed vocabulary, inducing a tree-structured state space when viewing token sampling as an action, which limits flexibility and expressiveness. Recent work introduces dynamic vocabulary by sampling retrieved text spans but o...

---

### 20. [A Multimodal Conditional Mixture Model with Distribution-Level Physics Priors](https://arxiv.org/abs/2602.10451)

**Authors**: Jinkyo Han, Bahador Bahmani  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.10451v1  

#### Abstract
Many scientific and engineering systems exhibit intrinsically multimodal behavior arising from latent regime switching and non-unique physical mechanisms. In such settings, learning the full conditional distribution of admissible outcomes in a physically consistent and interpretable manner remains a...

---

### 21. [TVCACHE: A Stateful Tool-Value Cache for Post-Training LLM Agents](https://arxiv.org/abs/2602.10986)

**Authors**: Abhishek Vijaya Kumar, Bhaskar Kataria, Byungsoo Oh, Emaad Manzoor, Rachee Singh  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.10986v1  

#### Abstract
In RL post-training of LLM agents, calls to external tools take several seconds or even minutes, leaving allocated GPUs idle and inflating post-training time and cost. While many tool invocations repeat across parallel rollouts and could in principle be cached, naively caching their outputs for reus...

---

### 22. [TabICLv2: A better, faster, scalable, and open tabular foundation model](https://arxiv.org/abs/2602.11139)

**Authors**: Jingang Qu, David Holzm\"uller, Ga\"el Varoquaux, Marine Le Morvan  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11139v1  

#### Abstract
Tabular foundation models, such as TabPFNv2 and TabICL, have recently dethroned gradient-boosted trees at the top of predictive benchmarks, demonstrating the value of in-context learning for tabular data. We introduce TabICLv2, a new state-of-the-art foundation model for regression and classificatio...

---

### 23. [Neuro-Symbolic Synergy for Interactive World Modeling](https://arxiv.org/abs/2602.10480)

**Authors**: Hongyu Zhao, Siyu Zhou, Haolin Yang, Zengyi Qin, Tianyi Zhou  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10480v1  

#### Abstract
Large language models (LLMs) exhibit strong general-purpose reasoning capabilities, yet they frequently hallucinate when used as world models (WMs), where strict compliance with deterministic transition rules--particularly in corner cases--is essential. In contrast, Symbolic WMs provide logical cons...

---

### 24. [Linear-LLM-SCM: Benchmarking LLMs for Coefficient Elicitation in Linear-Gaussian Causal Models](https://arxiv.org/abs/2602.10282)

**Authors**: Kanta Yamaoka, Sumantrak Mukherjee, Thomas G\"artner, David Antony Selby, Stefan Konigorski, Eyke H\"ullermeier, Viktor Bengs, Sebastian Josef Vollmer  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10282v1  

#### Abstract
Large language models (LLMs) have shown potential in identifying qualitative causal relations, but their ability to perform quantitative causal reasoning -- estimating effect sizes that parametrize functional relationships -- remains underexplored in continuous domains. We introduce Linear-LLM-SCM, ...

---

### 25. [Stop Training for the Worst: Progressive Unmasking Accelerates Masked Diffusion Training](https://arxiv.org/abs/2602.10314)

**Authors**: Jaeyeon Kim, Jonathan Geuter, David Alvarez-Melis, Sham Kakade, Sitan Chen  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10314v1  

#### Abstract
Masked Diffusion Models (MDMs) have emerged as a promising approach for generative modeling in discrete spaces. By generating sequences in any order and allowing for parallel decoding, they enable fast inference and strong performance on non-causal tasks. However, this flexibility comes with a train...

---

### 26. [Neuro-symbolic Action Masking for Deep Reinforcement Learning](https://arxiv.org/abs/2602.10598)

**Authors**: Shuai Han, Mehdi Dastani, Shihan Wang  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10598v1  

#### Abstract
Deep reinforcement learning (DRL) may explore infeasible actions during training and execution. Existing approaches assume a symbol grounding function that maps high-dimensional states to consistent symbolic representations and a manually specified action masking techniques to constrain actions. In ...

---

### 27. [CLI-Gym: Scalable CLI Task Generation via Agentic Environment Inversion](https://arxiv.org/abs/2602.10999)

**Authors**: Yusong Lin, Haiyang Wang, Shuzhe Wu, Lue Fan, Feiyang Pan, Sanyuan Zhao, Dandan Tu  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10999v1  

#### Abstract
Agentic coding requires agents to effectively interact with runtime environments, e.g., command line interfaces (CLI), so as to complete tasks like resolving dependency issues, fixing system problems, etc. But it remains underexplored how such environment-intensive tasks can be obtained at scale to ...

---

### 28. [FormalJudge: A Neuro-Symbolic Paradigm for Agentic Oversight](https://arxiv.org/abs/2602.11136)

**Authors**: Jiayi Zhou, Yang Sheng, Hantao Lou, Yaodong Yang, Jie Fu  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11136v1  

#### Abstract
As LLM-based agents increasingly operate in high-stakes domains with real-world consequences, ensuring their behavioral safety becomes paramount. The dominant oversight paradigm, LLM-as-a-Judge, faces a fundamental dilemma: how can probabilistic systems reliably supervise other probabilistic systems...

---

### 29. [Latent Thoughts Tuning: Bridging Context and Reasoning with Fused Information in Latent Tokens](https://arxiv.org/abs/2602.10229)

**Authors**: Weihao Liu, Dehai Min, Lu Cheng  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10229v1  

#### Abstract
While explicit Chain-of-Thought (CoT) equips Large Language Models (LLMs) with strong reasoning capabilities, it requires models to verbalize every intermediate step in text tokens, constraining the model thoughts to the discrete vocabulary space. Recently, reasoning in continuous latent space has e...

---

### 30. [Reinforced Curriculum Pre-Alignment for Domain-Adaptive VLMs](https://arxiv.org/abs/2602.10740)

**Authors**: Yuming Yan, Shuo Yang, Kai Tang, Sihong Chen, Yang Zhang, Ke Xu, Dan Hu, Qun Yu, Pengfei Hu, Edith C. H. Ngai  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10740v1  

#### Abstract
Vision-Language Models (VLMs) demonstrate remarkable general-purpose capabilities but often fall short in specialized domains such as medical imaging or geometric problem-solving. Supervised Fine-Tuning (SFT) can enhance performance within a target domain, but it typically causes catastrophic forget...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
