# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-03 06:34:57 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Out of the Memory Barrier: A Highly Memory Efficient Training System for LLMs with Million-Token Contexts](https://arxiv.org/abs/2602.02108)

**Authors**: Wenhao Li, Daohai Yu, Gen Luo, Yuxin Zhang, Fei Chao, Rongrong Ji, Yifan Wu, Jiaxin Liu, Ziyang Gong, Zimu Liao  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 19.0  
**Type**: new  
**ArXiv ID**: 2602.02108v1  

#### Abstract
Training Large Language Models (LLMs) on long contexts is severely constrained by prohibitive GPU memory overhead, not training time. The primary culprits are the activations, whose memory footprints scale linearly with sequence length. We introduce OOMB, a highly memory-efficient training system th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Out of the Memory Barrier: A Highly Memory Efficient Training System for LLMs with Million-Token Contexts**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
训练具有百万级 Token 上下文的 **Large Language Models (LLMs)** 面临严重的 **GPU 内存瓶颈**，尤其是激活值（activations）和 KV Cache 的内存占用随序列长度线性增长，导致单卡难以处理长上下文任务。传统方法如 ZeRO3、Tensor Parallelism 虽可扩展，但依赖大规模集群，成本高昂。

### 🚀 提出的新方法：**OOMB**
作者提出 **OOMB**（Out Of the Memory Barrier），一个高度内存高效的 LLM 训练系统，专为超长上下文（百万 Token 级别）设计。

#### 核心创新点：
- **Chunk-Recurrent Training Framework**  
  将长序列分块（chunk）处理，前向传播后立即丢弃中间激活值，反向传播时按需重新计算（on-the-fly activation recomputation）。该策略使 **activation memory footprint 维持在 O(1)**，不随上下文长度增长。

- **Paged KV Cache & Gradient Management**  
  受 PagedAttention 启发，引入分页机制管理不断增长的 KV Cache 和其梯度，避免频繁内存拷贝与碎片化，提升内存利用率。

- **Asynchronous CPU Offloading**  
  异步地将 KV Cache 和梯度卸载到 CPU 内存中，并通过专用 CUDA stream 和 DMA 隐藏传输延迟，有效缓解显存压力。

- **Page-Level Sparse Attention**  
  支持页粒度稀疏注意力机制（如 Top-K 页面检索），显著降低注意力计算复杂度和通信开销，同时保持模型性能。

> 四大组件协同作用，形成闭环优化体系，从根本上突破了长上下文训练的内存墙。

### 🔍 相比现有方法的优势
| 方法 | 显存增长趋势 | 是否支持单卡训练 | 最大上下文 | 所需硬件规模 |
|------|---------------|------------------|-------------|----------------|
| Parallel Training | O(N) | ❌（短序列可行） | ~32K–128K | 单卡 |
| Context Parallelism (e.g., Ring Attention) | O(N/P) | ✅（多卡） | 256K–4M | 多GPU集群 |
| **OOMB (本工作)** | **~O(1)** | ✅ | **4M+** | **单张 H200 GPU** |

- **资源效率飞跃**：实现单卡训练 400 万 Token 上下文，而同类方法需数百 GPU 集群。
- **内存增长极低**：每增加 10K tokens，端到端训练内存仅增加约 **10MB**（对 Qwen2.5-7B）。
- **无需专用高带宽网络**：相比 Context Parallelism 对 NCCL/IB 的强依赖，OOMB 更适合通用部署环境。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 主要使用 **arXiv dataset**（经清洗去重后的科学文献语料）
- 构造超长序列方式：将多个样本拼接成指定长度（如 256K, 1M, 4M tokens）

### ⚙️ 实验设置
- **模型**：`Qwen2.5-7B`（70亿参数基础模型）
- **硬件平台**：单张或四张 NVIDIA H200 GPU（部分对比实验使用 A100/TPU）
- **精度**：bfloat16
- **优化器**：Adam (`lr=5e-5`, `betas=(0.9, 0.98)`）
- **Batch Size**：每卡 batch size = 1，以隔离序列长度影响
- **Page Size**：KV Cache 分页大小设为 128 tokens

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Peak GPU Memory Usage** | 单次迭代峰值显存占用（MB/GB） |
| **Per-iteration Latency** | 单步训练耗时（秒） |
| **Throughput (tokens/sec)** | 每设备每秒处理 token 数量 |
| **Gradient Approximation Error (L2 Norm)** | 稀疏注意力带来的梯度近似误差 |
| **Training Loss Curve** | 不同配置下的收敛行为 |

### 🆚 基线方法对比
| 基线方法 | 类型 | 特点 |
|--------|------|------|
| **Standard Parallel Training + FlashAttention + Gradient Checkpointing** | 全并行训练 | 当前主流高效实现，但仍受显存限制 |
| **Ring Flash Attention (RFA)** | Context Parallelism 代表 | 分布式切分上下文，通信开销大 |
| **SeCO (Li et al., 2025)** | Chunk-wise 方法先驱 | 缺乏底层优化，扩展性差 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 2 & Figure 8）

#### ✅ 内存效率惊人
- 对于 **Qwen2.5-7B** 模型：
  - 每增加 **10K tokens 上下文**，端到端训练内存仅增加 **约 10MB**
  - 在 **4M-token 上下文** 下仍可在 **单张 H200 GPU** 上运行
  - 使用稀疏注意力时，**峰值显存几乎恒定**（~33–38 GB），不受上下文长度影响

#### ⏱️ 推理与训练速度优势
| 上下文长度 | 方法 | 吞吐量 (tokens/sec/device) |
|----------|------|----------------------------|
| 64K | RFA (16×TPUv4) | 49 |
| 64K | OOMB + Dense Attn (1×H200) | **936.22** |
| 64K | OOMB + Sparse Attn (1×H200) | **1560.38** ✅ |
| 256K | RFA (8×A100) | 50 |
| 256K | OOMB + Sparse Attn (1×H200) | **1301.60** ✅ |

> OOMB 在 **单位设备吞吐量上远超 Context Parallelism 方案**，且无需多卡同步。

#### 📈 可扩展性验证（Figure 8）
- 成功在 **8 million tokens** 上下文长度下完成训练
- 使用稀疏注意力时，训练时间呈近似线性增长，具备良好可扩展性

### 🔬 消融实验结果（Ablation Studies）

#### （1）Chunk Size 影响（Figure 9）
- 增大 chunk size（如从 512 到 4096）可提升硬件利用率和吞吐
- 但收益递减，超过 4096 后性能提升有限
- 推荐默认值：**4096 tokens/chunk**

#### （2）CPU Offloading 开启前后对比
- 开启异步卸载后，显存峰值下降 **超过 50%**
- 时间开销增加 <5%，被计算完全掩盖（latency hiding 成功）

#### （3）Sparse Attention Retrieval Budget 影响
- 使用 `4K+8192`（local + top-k）预算即可逼近 dense attention 性能
- 更大的 budget（如 32768）带来边际收益递减
- 梯度近似误差（L2 norm）在合理范围内（见 Figure 7）

#### （4）不同 Attention 类型对比（Table 2）
| 配置 | 显存增长速率 | 相对基线减少倍数 |
|------|--------------|------------------|
| Baseline (FlashAttn + ckpt) | 快速上升 → OOM at 128K | — |
| OOMB w/o offload | 缓慢上升 | ↓ >5× |
| OOMB w/ offload | 几乎平坦 | ↓ >10× |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **激活内存不再是瓶颈**：通过 chunk-recurrent + activation recomputation，实现了 **O(1) 激活内存占用**。
2. **KV Cache 成为新瓶颈**：必须结合分页管理、异步卸载与稀疏注意力才能彻底解决。
3. **四大技术深度耦合、缺一不可**：
   - Chunk-wise training → 触发后续所有优化
   - Paged KV Cache → 支持动态增长与高效访问
   - Sparse Attention → 降低计算与通信负载
   - Async Offloading → 实现显存溢出控制
4. **单卡百万 Token 训练成为现实**：OOMB 使得原本需要数百 GPU 集群的任务可在 **单张 H200 上完成**，极大降低门槛。

### ⚠️ 局限性
1. **串行处理引入延迟**：chunk-wise 序列处理牺牲了一定并行性，在高吞吐场景可能受限。
2. **稀疏注意力是近似方法**：虽然实验证明有效，但在某些需要全局密集交互的任务中可能存在性能损失。
3. **依赖 CPU-GPU 带宽**：异步卸载效率受 PCIe/NVLink 带宽制约，在低端平台上效果打折。
4. **尚未探索更复杂的稀疏模式**：当前采用 Top-K 检索，未来可集成更智能的 routing 或 MoE-style attention。

### 🔮 未来工作方向
- 探索 **动态 chunk size 调度策略**，适应不同输入分布
- 结合 **MoE 架构** 进一步提升长上下文下的计算效率
- 设计 **学习式稀疏 attention 路由机制**，超越启发式 Top-K
- 将 OOMB 扩展至 **多模态长序列建模**（如视频、基因组等）

---

## 💡 总结一句话
> **OOMB 通过“分块训练 + 分页 KV + 异步卸载 + 稀疏注意力”四位一体架构，首次实现了在单张 GPU 上高效训练百万乃至千万 Token 上下文的 LLM，打破了传统训练系统的内存壁垒，推动长上下文模型走向普惠化与可持续发展。**

🔗 **开源地址**：[Github Repository](https://github.com/anonymous)（原文提供匿名链接）

</details>

---

### 2. [Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design](https://arxiv.org/abs/2602.00608)

**Authors**: Wei Zeng, Xuchen Li, Ruili Feng, Zhen Liu, Fengwei An, Jian Zhao  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 14.0  
**Type**: new  
**ArXiv ID**: 2602.00608v1  

#### Abstract
Real-time generative game engines represent a paradigm shift in interactive simulation, promising to replace traditional graphics pipelines with neural world models. However, existing approaches are fundamentally constrained by the ``Memory Wall,'' restricting practical deployments to low resolution...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统游戏引擎依赖显式的物理规则和光栅化渲染流程，而**生成式游戏引擎**（Generative Game Engine）通过神经世界模型（Neural World Models）直接从像素或潜在空间生成游戏画面，实现“生成即模拟”。然而，现有方法受限于“**Memory Wall**”——高分辨率生成需要大量内存带宽，导致推理延迟极高（常超过50–100ms），无法满足实时交互需求（如60 FPS需≤16.6ms）。尤其在标准清晰度（720×480）下，现有系统仅能支持极低分辨率（如64×64），严重限制了实际应用。

### 提出了什么新方法或新思路
本文提出了一种**可扩展的硬件-算法协同设计框架**（Hardware-Algorithm Co-Design），以突破生成式游戏引擎的分辨率瓶颈。其三大核心创新为：

1. **异构计算架构与资源分配策略**  
   将计算密集型的 **World Model**（基于 DiT）与内存密集型的 **Decoder**（基于 VAE）解耦，并在AI加速器集群中采用不同的并行策略：
   - DiT 使用 **Sequence Parallelism**（Ulysses）
   - VAE 使用 **Spatial Parallelism**
   并通过理论建模推导出最优设备分配比例（5:3），最大化吞吐量。

2. **内存中心化的算子融合优化**（Memory-Centric Operator Fusion）
   利用现代AI芯片的显式内存层次（如SRAM），对计算图进行重构：
   - 在VAE中实施**垂直融合**（Vertical Fusion）：将 `Upsample → Conv2d → GroupNorm → SiLU` 融合为单个核函数，减少75%的HBM访问。
   - 在DiT中实施**水平融合**（Horizontal Fusion）：合并AdaLN中的多个小矩阵乘法，提升计算密度至理论峰值的85%以上。

3. **流形感知的潜在外推机制**（Manifold-Aware Latent Extrapolation）
   利用连续帧之间的时序冗余，在动作稳定时跳过部分DiT计算，使用线性外推预测下一帧潜变量 $ z_{t+1} \approx z_t + \Delta t \cdot v_t $，从而降低平均计算负载。

此外，还引入了**推测性动作预取**（Speculative Action Prefetching），利用轻量级LSTM预测用户输入，命中率达93%，显著降低感知延迟。

### 相比现有方法的优势
- **首次实现在720×480分辨率下的实时生成**，相比基线（如Diamond的64×64）提升约50倍像素吞吐。
- 通过**体系结构级协同设计**而非单纯算法优化，从根本上缓解Memory Wall问题。
- 实现了**高保真与低延迟的统一**：26.4 FPS @ 720×480，感知延迟仅2.7ms，远低于人类感知阈值。
- 支持逻辑一致性（100%训练分布内正确性），优于纯缓存或剪枝类方法。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
论文构建了两个基准测试环境，覆盖连续与离散控制场景：

1. **Matrix**（Continuous Domain）  
   - 高保真3D赛车模拟器，分辨率为 **720×480**
   - 强调连续动力学、摩擦力、动量等隐式物理规律的学习
   - 用于评估控制敏感性和视觉连贯性

2. **Playable Game Generation (PGG)**（Discrete Domain）  
   - 2D平台跳跃游戏，分辨率为 **256×256**
   - 强调布尔逻辑、碰撞检测、状态转移的精确性
   - 用于评估逻辑一致性和离散行为建模能力

### 实验设置和评估指标

#### 硬件平台
- **8块 Huawei Ascend 910C NPU** 组成集群
- 单卡性能：752 TFLOPS FP16，64GB HBM
- 高速互联：HCCS环形拓扑，带宽30 GB/s
- 控制平面：双路Intel Xeon Platinum CPU + Ray分布式调度框架

#### 软件栈
- CANN 8.0, PyTorch 2.5.1, xfuser（支持Ulysses并行）

#### 评估指标
| 类别 | 指标 | 描述 |
|------|------|------|
| **性能** | FPS, Motion-to-Photon Latency (M2P) | 衡量生成速度与响应延迟 |
| **质量** | FID, PSNR, SSIM, LPIPS | 视觉保真度（越低越好 / 越高越好） |
| **物理有效性** | Control Sensitivity Analysis (CSA) | 连续域中转向输入与偏航率的相关性 |
| **逻辑一致性** | Discrete Logic Boundary (DLB) Score | 离散域中无效状态转移的比例 |

### 基线方法对比
| 基线 | 硬件 | 分辨率 | FPS | 备注 |
|------|------|--------|-----|------|
| **Diamond** [6] | RTX 3090 | 64×64 | 10.0 | 扩散模型用于CS:GO |
| **GameNGen** [4] | TPU v5 | 320×240 | >20 | Google提出的生成式引擎 |
| **PGG Baseline** | RTX 5090 | 256×256 | 29.9 | 开源2D平台游戏生成模型 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 场景 | 分辨率 | FPS | 感知延迟（M2P） | FID | LPIPS |
|------|--------|-----|------------------|-----|-------|
| **Ours (Matrix)** | 720×480 | **26.4** | **2.7ms** | 42.3 | 0.087 |
| **Ours (PGG)** | 256×256 | **48.3** | — | **28.5** | **0.052** |

> 注：感知延迟经推测性预取后摊销至2.7ms，原始端到端延迟为38ms。

### 与基线方法的对比结果
- 在**连续3D领域**（Matrix）：
  - 达到 **26.4 FPS @ 720×480**，相较Diamond（64×64, 10 FPS）实现**50倍像素吞吐提升**
  - FID从89.5（Diamond）降至42.3，显示更强的视觉真实感
- 在**离散2D领域**（PGG）：
  - 单卡即达 **48.3 FPS**，超越RTX 5090上的PGG基线（29.9 FPS）
  - DLB得分为 **100.0%**（无违规状态转移），显著优于非时序注意力基线（85.3%）

### 消融实验结果（Ablation Study）
| 阶段 | 架构 | FPS | Speedup |
|------|------|-----|---------|
| Baseline（顺序执行） | 单卡 | 2.1 | 1.0x |
| + Operator Fusion | 单卡 | 4.5 | 2.1x |
| + Ulysses (3:5) | 3 DiT + 5 VAE | 16.6 | 7.9x |
| + Optimal Ratio (5:3) | 5 DiT + 3 VAE | 19.4 | 9.2x |
| + Latent Extrapolation | 5:3 | 26.4 | 12.6x |
| + Speculative Prefetch | 5:3 | 26.4（2.7ms延迟） | 12.6x |

> 如图7所示，各阶段优化累计带来 **12.6倍的系统级性能提升**

---

## 4. 关键结论和发现

### 论文的主要发现
1. **“Memory Wall”是高分辨率生成式游戏的核心瓶颈**，仅靠软件优化无法解决；必须通过**硬件-算法协同设计**来打破。
2. **异构并行策略**（compute-bound vs memory-bound）能够有效平衡系统负载，5:3的DiT:VAE资源配置被证明是最优解。
3. **显式内存管理**（如SRAM融合）比通用GPU的缓存机制更高效，可减少高达75%的HBM流量。
4. **时间冗余可被安全利用**：Manifold-Aware Latent Extrapolation可在保持视觉连贯的同时跳过35%的DiT计算。
5. **生成式引擎可学习隐式物理规律**：在Matrix中观察到“涌现惯性”现象，表明模型已内部化动量守恒等物理特性。
6. **逻辑一致性可在训练分布内严格保证**：PGG任务中实现100% DLB得分，验证了系统的可靠性。

### 方法的局限性
1. **依赖大规模加速器集群**：当前方案适用于云游戏场景，难以部署于边缘设备或消费级PC。
2. **Out-of-Distribution（OOD）行为处理不足**：面对未见过的动作组合可能出现“幻觉”（如穿墙），需额外约束机制。
3. **资源分配依赖模型超参**：最优5:3比例基于特定Attention Head数量，换模型需重新调优。
4. **仍存在P99尾延迟问题**：突变场景下恢复需完整重生成，可能导致轻微卡顿（但可通过Change Blindness效应掩盖）。

### 未来工作方向
1. **开发混合神经-符号引擎**（Hybrid Neuro-Symbolic Engine）  
   引入轻量级可微分符号逻辑层作为“游戏规则监督器”，强制执行关键规则（如碰撞边界）。

2. **探索多模态控制接口**  
   利用Transformer的跨模态能力，支持自然语言或语音指令直接驱动游戏世界。

3. **推动本地化部署**  
   研究极端量化技术（如4-bit权重 + 激活感知平滑），使高保真生成引擎可在配备NPU的消费级设备上运行。

4. **跨平台通用化**  
   将当前Ascend专用优化迁移至CUDA/NVLink生态（如H100集群），并通过OpenAI Triton等编译器实现跨厂商兼容。

---

> ✅ **总结一句话**：  
> 本工作证明，通过**硬件-算法协同设计**，尤其是**异构并行、内存融合与潜在空间外推**，可以突破生成式游戏引擎的“分辨率墙”，首次实现在720×480分辨率下的实时、高保真、逻辑一致的游戏生成，标志着向“全神经化游戏引擎”的重要迈进。

</details>

---

### 3. [STILL: Selecting Tokens for Intra-Layer Hybrid Attention to Linearize LLMs](https://arxiv.org/abs/2602.02180)

**Authors**: Weikang Meng, Liangyu Huo, Yadan Luo, Jiawen Guan, Jingyi Zhang, Yingjian Li, Zheng Zhang  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2602.02180v1  

#### Abstract
Linearizing pretrained large language models (LLMs) primarily relies on intra-layer hybrid attention mechanisms to alleviate the quadratic complexity of standard softmax attention. Existing methods perform token routing based on sliding-window partitions, resulting in position-based selection and fa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**STILL: Selecting Tokens for Intra-Layer Hybrid Attention to Linearize LLMs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前对预训练大语言模型（LLMs）进行线性化（linearization）以降低 **Softmax Attention** 的 $O(N^2)$ 复杂度时，主流方法采用 **intra-layer hybrid attention**（层内混合注意力），即部分 token 使用 Softmax Attention (SA)，其余使用 Linear Attention (LA)。然而，现有方法存在两大缺陷：

1. **Token 路由机制依赖位置滑窗（position-based sliding window）**  
   如 LoLCATs 和 Liger 将固定窗口内的 token 分配给 SA，忽略其语义重要性，导致关键远距离 token 被错误地送入 LA，损害长程建模能力。

2. **Linear Attention 引入 Learnable Feature Map 导致分布偏移（distribution shift）**  
   特别是通过 MLP 变换特征会破坏预训练模型中的 **norm-aware** 特性（即向量模长影响注意力强度），从而扭曲原始表示几何结构。

---

### 🚀 提出的新方法：STILL
作者提出 **STILL**（Selecting Tokens for Intra-Layer Hybrid Attention to Linearize LLMs），一种高效且忠实于预训练知识的线性化框架，包含三大核心创新：

#### （1）**Self-Saliency Score**：基于内容的 Token 选择机制
- 利用局部滑窗内计算的注意力分布差异（含对角项 vs 不含对角项）定义每个 token 的 **Self-Saliency Score**。
- 该分数能有效反映 token 在全局上下文中的重要性，实现“局部计算，全局一致”的 token 选择。
- 高分 token 被选入 SA 分支，其余送入 LA，突破传统滑窗的位置限制。

> 🔍 图 3 显示局部得分与全局得分高度一致；图 4 显示被选中的 token 多为否定词（not, never）、情态动词（may, will）等高语义作用词。

#### （2）**Norm-Preserved Feature Map (NP-Map)**：保留预训练 norm 结构
- 设计新的可学习特征映射机制，在 MLP 变换后重新注入原始输入的 norm（模长）。
- 形式为：  
  $$
  \phi_{\text{NP}}(x) = [\text{softmax}(f(x)/\|f(x)\| \cdot \|x\|), \text{softmax}(-f(x)/\|f(x)\| \cdot \|x\|)]
  $$
- 保证 Linear Attention 的 kernel 仍作用在符合预训练分布的特征上，避免 norm distortion。

#### （3）**Chunk-wise Delayed Selection**：提升硬件效率
- 将序列划分为 chunk，在每个 chunk 完成后再统一执行 token 选择（Top-K），而非逐 token 决策。
- 支持训练与推理统一架构下的并行化处理，显著提升吞吐量。

---

### ⚖️ 相比现有方法的优势
| 维度 | 传统方法（如 LoLCATs/Liger） | STILL |
|------|-------------------------------|-------|
| Token 路由 | 固定滑窗 → 位置偏置 | Self-Saliency Score → 内容感知 |
| Norm 一致性 | MLP 扭曲预训练 norm | NP-Map 显式保留 norm |
| 效率设计 | 逐 token 路由 → 并行差 | Chunk-wise 延迟选择 → 高并行 |
| 性能恢复 | 长文本任务严重退化 | 接近甚至超越原模型 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **Commonsense & General Reasoning**：  
  PIQA, ARC-Easy, ARC-Challenge, HellaSwag, WinoGrande, MMLU（5-shot）
- **Long-context Benchmarks**：  
  - **RULER**：单针检索（S-NIAH）、多键/查询/值检索（MK/MQ/MV）、问答（HQA/SQA）、变量追踪（VT）等，最长达 4K tokens。
  - **BABILong**：bAbI 任务扩展至长上下文，测试跨远距离证据整合能力（QA1–QA5），长度从 0K 到 4K。

### ⚙️ 实验设置
- **Teacher Models**：Llama 3 8B / Llama 3.1 8B / Mistral 7B / Llama 3.2 1B&3B
- **训练策略**（两阶段）：
  1. **Attention Transfer**：冻结教师模型，最小化 MSE 损失对齐注意力图。
  2. **Low-rank Linearization**：使用 LoRA 微调 Query/Key/Value 投影及门控参数。
- **训练数据**：Cleaned Alpaca（20M tokens，seq len=1024）
- **评估方式**：zero-shot 或标准 few-shot（如 MMLU 5-shot），使用 `lm-evaluation-harness`。

### 🆚 基线方法对比
| 类型 | 方法 |
|------|------|
| **Full Transformer** | Llama 3/3.1 系列 |
| **Subquadratic from scratch** | Mamba, Mamba2, RWKV-6, Hawk, Griffin |
| **Inter-layer Hybrid** | StripedHyena, Zamba, RecurrentGemma |
| **Linearized from LLM** | LoLCATs, Liger-GLA, Hedgehog-LoLCATs, SUPRA |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| 模型 | MMLU (5-shot) | Avg. (w/ MMLU) | 训练 Tokens (B) |
|------|----------------|----------------|------------------|
| Llama 3.1 8B (原模型) | 68.0 | 74.2 | 15000+ |
| Liger-GLA | 46.9 | 68.3 | 0.02 |
| **STILL (Ours)** | **61.3** | **72.5** | **0.04** |

> ✅ STILL 仅用 **0.04B 训练 tokens**（相比原模型 <1/300,000），平均性能接近原模型，并大幅优于其他线性化方法。

---

### 🔍 长上下文任务表现（Table 2 & 3）

#### **RULER - S-NIAH-1 @ 4K context**
| 方法 | 准确率 |
|------|--------|
| LoLCATs (512 cache) | 8.8 |
| Liger-GLA | 0.0 |
| **STILL (256~512 cache)** | **86.2** |
| Full Attention | 100 |

> 💡 STILL 在极小缓存下实现 **86.2% 的相对性能恢复**，远超基线。

#### **Extended RULER @ 4K context (Avg.)**
| 方法 | 平均得分 |
|------|----------|
| LoLCATs | 7.2 |
| Liger-GLA | 2.1 |
| **STILL** | **47.9** |

> 表明 STILL 在复杂记忆与状态跟踪任务中具有显著优势。

---

### 🧪 BABILong @ 4K context（QA5）
| 方法 | 准确率 |
|------|--------|
| SWA / LoLCATs | 6 |
| **STILL** | **24** |

> 在需要远距离推理的任务上，STILL 实现 **4倍提升**。

---

### 🔬 消融实验（Ablation Study, Table 6）
在 BABILong（1K）上的模块消融：

| 组件移除 | 准确率 | 下降幅度 |
|---------|--------|-----------|
| 完整 STILL | 27.5 | — |
| 移除 Gate | 21.7 | -5.8 |
| 移除 NP-Map | 22.4 | -4.9 |
| 移除两者 | 19.2 | -8.3 |
| 再移除 Self-Saliency | 12.5 | -15.0 |

> ❗ 结果表明：**Self-Saliency Score 是最关键组件**，三者协同增效。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Token 重要性可通过局部信息可靠估计**  
   Self-Saliency Score 实现了“局部计算 → 全局有效”的 token 选择，打破了滑窗限制。

2. **Norm preservation 对性能恢复至关重要**  
   预训练模型的 “norm-aware” 注意力机制必须被保留，否则即使形式上模仿 softmax，实际行为已偏移。

3. **高效线性化无需大规模再训练**  
   STILL 仅需 **0.04B tokens** 微调即可完成高质量线性化，训练成本极低。

4. **长上下文能力可被有效恢复**  
   在 RULER 和 BABILong 上，STILL 显著优于现有线性化方法，证明其真正解决了长程依赖建模难题。

---

### ⚠️ 局限性
- 当前方法仍依赖一定数量的 Softmax Attention（约数百 token），尚未完全消除二次项。
- Self-Saliency Score 的理论解释尚不充分，为何局部敏感性能预测全局重要性有待进一步分析。
- Chunk-wise 设计可能引入轻微延迟，在极端低延迟场景中需权衡。

---

### 🔮 未来工作方向
- 探索更轻量化的 saliency 估计器，减少额外计算开销。
- 将 STILL 扩展到 encoder-decoder 架构或多模态模型。
- 结合动态稀疏化或 early exiting 进一步压缩计算。
- 探索 saliency 与语言学结构（如句法、逻辑作用词）之间的关联。

---

## ✅ 总结
**STILL** 是首个将 **content-aware token selection** 与 **norm-preserving linear attention** 结合的 LLM 线性化框架。它不仅在 commonsense 和 reasoning 任务上逼近原模型性能，更在长上下文理解任务上实现了突破性进展，最高恢复 **86.2% 的 full-attention 性能**，同时带来 **45% 内存下降** 和 **28% 解码加速**。该工作为高效部署超长上下文 LLM 提供了一条实用且高性能的技术路径。

</details>

---

### 4. [Focus-dLLM: Accelerating Long-Context Diffusion LLM Inference via Confidence-Guided Context Focusing](https://arxiv.org/abs/2602.02159)

**Authors**: Lingkun Long, Yushi Huang, Shihao Bai, Ruihao Gong, Jun Zhang, Ao Zhou, Jianlei Yang  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.02159v1  

#### Abstract
Diffusion Large Language Models (dLLMs) deliver strong long-context processing capability in a non-autoregressive decoding paradigm. However, the considerable computational cost of bidirectional full attention limits the inference efficiency. Although sparse attention is promising, existing methods ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《Focus-dLLM: Accelerating Long-Context Diffusion LLM Inference via Confidence-Guided Context Focusing》总结**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Diffusion Large Language Models (dLLMs) 虽然在非自回归解码范式下展现出强大的长上下文处理能力，但由于其**双向全注意力机制**，推理过程计算开销巨大，尤其在长上下文（如 32K tokens）场景下效率极低。现有的加速方法存在以下不足：
- **Approximated KV cache**：虽能复用缓存状态，但仍需对完整上下文进行注意力计算。
- **Sparse attention**：依赖当前已解码 token 来估计重要性，但在 dLLM 中，待解码位置（unmasked）是动态且未知的，导致预测不准确。

因此，如何**准确预测即将被解码的位置并高效剪枝冗余注意力计算**，成为提升 dLLM 推理效率的关键挑战。

---

### **提出了什么新方法或新思路**
本文提出 **Focus-dLLM**，一种无需训练的、基于置信度引导的上下文聚焦框架，用于加速长上下文 dLLM 推理。其核心思想包括：

#### **(1) 过去置信度引导指示器 (Past Confidence-Guided Indicator)**
- 发现：相邻去噪步中 token 的置信度（confidence score）具有强正相关性，前一步高置信度的 token 很可能在下一步被解码。
- 方法：利用第 $t-1$ 步的置信度预测第 $t$ 步将被解码的位置，并通过窗口扩展保留局部语义连贯性，形成活跃查询集（`Q_active`）。

#### **(2) Sink-Aware 动态剪枝策略**
- 发现：dLLM 中存在“**attention sinks**”——对生成质量至关重要的关键 token，且这些 sink 在不同层之间表现出显著的跨层一致性（cross-layer consistency）。
- 方法：
  - 在浅层（dense layers）识别 attention sinks 并在整个深层中复用其位置，避免重复检测。
  - 对 prompt 分块，基于 relevance score 动态选择最相关的 block 参与注意力计算。
  - 最终仅对 `active queries` 和 `selected key-value pairs` 执行稀疏注意力。

---

### **相比现有方法的优势**
| 特性 | Focus-dLLM | Fast-dLLM | Sparse-dLLM | SparseD |
|------|------------|-----------|--------------|----------|
| 是否需要训练 | ❌ 否 | ❌ 否 | ❌ 否 | ❌ 否 |
| 是否预测 unmasked 位置 | ✅ 高精度预测 | ❌ 无 | ⚠️ 粗粒度估计 | ❌ 不适用 |
| 是否保留 attention sinks | ✅ 显式识别并保留 | ❌ 忽略 | ❌ 忽略 | ❌ 忽略 |
| 是否跨层共享 sink | ✅ 是 | ❌ 否 | ❌ 否 | ❌ 否 |
| 加速比（32K） | **29.6×** | ~14× | ~12× | ~1.5× |

> ✅ **优势总结**：  
> - 更精准地定位关键 token，减少误剪风险；
> - 利用跨层 sink 一致性降低重复计算；
> - 实现**无损甚至更优性能下的超高加速比**。

---

## **2. 核心实验方法和设置**

### **使用的模型**
- **UltraLLaDA** (He et al., 2025)：支持超长上下文（达 128K）的 dLLM。
- **Dream-7B-Instruct** (Ye et al., 2025)：基于扩散机制的指令微调模型。

### **数据集**
- **LongBench** (Bai et al., 2024)：多任务、双语、长上下文理解基准，涵盖：
  - 单文档问答（Qasper）
  - 多文档问答（HotpotQA, 2WikiMQA）
  - 摘要生成（GovReport, MultiNews）
  - 少样本学习（TREC）
  - 合成任务（PassageRetrieval）
  - 代码补全（RepoBench-P）

### **评估指标**
| 类型 | 指标 |
|------|------|
| 准确率 | F1, Rouge-L, Accuracy, Edit Sim |
| 效率 | Throughput (tokens/s)，Speedup × |
| 综合表现 | Average Score on LongBench |

### **实验设置**
- 上下文长度：8K → 32K（以 1K = 1024 tokens）
- 生成长度与步数固定为 256
- 所有方法统一采用 semi-autoregressive remasking 策略（block size = 32）
- 使用 NVIDIA H200 GPU，基于 OpenCompass 平台评测

### **基线方法对比**
| 方法 | 类型 | 核心机制 |
|------|------|---------|
| **Vanilla** | 原始推理 | 全注意力，无优化 |
| **Fast-dLLM** | KV Cache | 块级近似 KV 缓存复用 |
| **Sparse-dLLM** | Sparse Attention | 动态缓存驱逐，粗粒度重要性估计 |
| **SparseD** | Sparse Attention | 预定义稀疏模式重用，早期全注意 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 准确率表现（LongBench 平均得分）**

| 方法 | UltraLLaDA | Dream-7B-Instruct |
|------|------------|------------------|
| Vanilla | 44.90 | 43.03 |
| Fast-dLLM | 44.74 | 42.75 |
| Sparse-dLLM | 44.86 | 42.78 |
| SparseD | 44.70 | **43.59** |
| **Focus-dLLM** | **45.14** ✅ | 42.82 |

> 🔍 **发现**：
> - 在 UltraLLaDA 上，Focus-dLLM **超越所有基线**，甚至优于 Vanilla；
> - 在 Dream-7B 上略低于 SparseD，但换来了更高的推理速度。

#### **(2) 推理效率（吞吐量 Speedup）**

| 方法 | Context Length | Speedup vs Vanilla |
|------|----------------|--------------------|
| Focus-dLLM | 8K | 9.4× |
| Focus-dLLM | 16K | ~18× |
| Focus-dLLM | **32K** | **29.6×** ✅ |
| Fast-dLLM | 32K | ~14.3× |
| Sparse-dLLM | 32K | ~12.2× |
| SparseD | 32K | ~1.5× |

> 📈 **趋势**：随着上下文增长，冗余计算加剧，Focus-dLLM 的剪枝优势愈发明显。

#### **(3) Niah 任务（needle-in-a-haystack）检索能力**
- 在 UltraLLaDA 上测试 32K 上下文中查找特定信息。
- **Focus-dLLM 表现最优**，尤其在深层网络中超过 Vanilla，证明其有效保留了关键上下文信息。

---

### **消融实验结果**

#### **表 2：组件消融（UltraLLaDA, 16K）**

| 方法 | Avg. Score | Throughput (tokens/s) |
|------|-----------|------------------------|
| Fast-dLLM | 44.74 | 11.03 |
| + PCGI（置信度引导） | 44.23 (-0.51) | 11.37 (+0.34) |
| + SA Sparse Attn（sink-aware 剪枝） | 44.84 (+0.10) | **17.68 (+6.65)** |
| **Focus-dLLM（完整）** | **45.14 (+0.40)** | **17.71 (+6.68)** |

> ✅ **结论**：
> - Sink-aware 剪枝是性能提升主因；
> - 结合 PCGI 后可实现更精确的查询选择，进一步提升准确率与效率。

#### **表 3：attention sinks 的影响（Dream-7B）**
| 设置 | Avg. Score |
|------|-----------|
| w/o attention sinks | 41.47 |
| **w/ attention sinks** | **42.82 (+1.35)** ✅ |

> 💡 显式保留 attention sinks 显著提升多任务表现。

#### **图 7：超参数敏感性分析**
- **sparsity ratio α ↑**：保留更多 context → 性能先升后降（过多无关 token 引入噪声）
- **prediction expansion factor p ↑**：召回率提高 → 性能稳步上升
- **window size w**：适中最佳（太小丢失上下文，太大引入噪声）
- **dense layer 数量**：非单调变化，表明 sink 稳定性随深度演化

---

## **4. 关键结论和发现**

### **主要发现**
1. **Token confidence 具有时间连续性**：前一步高置信度 token 极有可能在下一步被解码，可用于精准预测 unmasked 位置。
2. **Attention sinks 存在跨层一致性**：可在中间层识别并在深层复用，大幅减少重复计算。
3. **结合置信度引导与 sink-aware 剪枝**，可在几乎无损甚至增益的情况下实现高达 **29.6× 的推理加速**（32K context）。
4. Focus-dLLM 在多个任务上达到 **SOTA 级别的 accuracy-efficiency trade-off**，构建了更强的 Pareto frontier。

---

### **局限性**
1. **目前仅适用于文本任务**：尚未拓展到 multimodal reasoning 场景。
2. **超参数手动配置**：未实现完全自适应调节，可能在不同 domain 下非最优。
3. **依赖 semi-autoregressive remasking 策略**：对其他调度策略的泛化性有待验证。

---

### **未来工作方向**
1. **扩展至多模态 dLLM**：探索视觉-语言联合推理中的 context focusing。
2. **开发自适应参数调整机制**：根据输入动态优化 `p`, `α`, `ldense` 等超参数。
3. **结合硬件优化**：设计专用 kernel 支持更高效的稀疏注意力执行（如 Triton + FlashAttention 协同）。
4. **理论分析 confidence dynamics 与 sink formation 机制**：为更通用的推理加速提供指导。

---

> 🔗 **开源地址**：https://github.com/Longxmas/Focus-dLLM  
> 📄 **论文链接**：https://arxiv.org/abs/2602.02159

</details>

---

### 5. [Grappa: Gradient-Only Communication for Scalable Graph Neural Network Training](https://arxiv.org/abs/2602.01872)

**Authors**: Chongyang Xu, Christoph Siebenbrunner, Laurent Bindschaedler  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.01872v1  

#### Abstract
Cross-partition edges dominate the cost of distributed GNN training: fetching remote features and activations per iteration overwhelms the network as graphs deepen and partition counts grow. Grappa is a distributed GNN training framework that enforces gradient-only communication: during each iterati...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Grappa: Gradient-Only Communication for Scalable Graph Neural Network Training

## 1. 论文的主要贡献和创新点

### 解决的问题
分布式图神经网络（GNN）训练面临严重的**通信瓶颈**。当图被分区后，跨分区边（cross-partition edges）在前向传播中需要频繁获取远程节点的特征和激活值，导致网络流量激增，尤其在模型加深或分区数增加时，该问题成为训练效率的主导成本。

现有解决方案如高性能互连（NVLink/RDMA）、缓存机制或高质量图划分（如METIS）存在部署成本高、内存压力大或预处理开销高等问题，无法从根本上解决通信瓶颈。

### 提出的新方法与创新思路
Grappa 提出了一种全新的分布式 GNN 训练框架，其核心是 **Gradient-Only Communication（梯度仅通信）** 范式，具体包含以下三个关键技术：

1. **隔离训练（Isolated Training）**  
   每个训练迭代中，各分区独立训练，**完全不交换任何特征或激活值**，仅在全局更新阶段同步梯度。这彻底消除了跨分区邻居通信开销。

2. **动态重分区（Dynamic Repartitioning）**  
   在多个 epoch 组成的“超级 epoch”（super-epoch）之间，对图进行重新分区。通过周期性地改变分区布局，确保每个节点最终能接触到其所有邻居，从而恢复全局信息覆盖。

3. **覆盖率校正的梯度聚合（Coverage-Corrected Gradient Aggregation）**  
   由于隔离训练引入了采样偏差，Grappa 提出一种基于重要性采样的轻量级梯度加权机制：
   - **理论保证**：提出了一个渐近无偏的节点级估计器，并证明其正确性。
   - **实用实现**：设计了一个批级别（batch-level）的校正因子，最小化与理想估计的均方误差，兼容 PyTorch 等主流框架。
   - **稳定性增强**：引入基于“重采样期望”的收缩（shrinkage）版本 $c_{\text{resampling}}$，进一步提升训练稳定性。

此外，Grappa 支持 **phase-parallel 执行模式**，允许在单台机器上顺序训练不同分区，以时间换容量，支持超大规模图的训练。

### 相比现有方法的优势
- **极致通信优化**：消除特征/激活通信，仅保留梯度同步，通信量大幅降低。
- **模型无关性（model-agnostic）**：适用于各种 GNN 架构（GCN, GAT, GraphSAGE 等），而如 Cluster-GCN 仅限 GCN。
- **无需特殊硬件**：不依赖 NVLink 或 RDMA 等高带宽互连。
- **内存友好**：不依赖复杂的缓存机制，减少 HBM/DRAM 压力。
- **可扩展性强**：支持 phase-parallel 模式，在单机上训练万亿边规模的图。

---

## 2. 核心实验方法和设置

### 数据集
实验使用了真实世界和合成两类数据集：

| 数据集 | 类型 | 节点数 | 边数 | 特征维度 |
|--------|------|--------|------|----------|
| OGBN-ArXiv | 引用网络 | 0.16M | 1.11M | 128 |
| Reddit | 社交网络 | 0.22M | 109.30M | 602 |
| OGBN-Products | 商品图 | 2.34M | 58.99M | 100 |
| OGBN-Papers100M | 学术网络 | 105.92M | 1.51B | 128 |
| RMAT-X | 合成图（幂律分布） | $2^X$ | $2^{X+4}$ | 128 |

其中 RMAT-36 包含约 **1.1 万亿条边**，用于验证极端规模下的可行性。

### 实验设置与评估指标
- **硬件环境**：
  - 分布式集群：8 台机器，每台 2×V100 GPU，通过 10Gb/s 网络连接。
  - 单节点高性能：1 台机器，8×H100 GPU，全 NVLink 互联。
- **模型**：GraphSAGE (Sage-k), GCN-k, GAT-k（k=2,3,4 层）。
- **评估指标**：
  - **系统级**：平均 epoch 时间（runtime）、吞吐量、可扩展性。
  - **模型级**：测试准确率（test accuracy）。
  - **容量**：能否训练超出单机内存的大图。

### 基线方法对比
- **DGL**：主流分布式 GNN 框架，支持随机和 METIS 划分。
- **MGG**：单节点全图训练系统，利用 NVLink 优化，代表单机性能上限。
- **Cluster-GCN**：基于聚类预计算批次的低通信采样方法，但仅支持 GCN。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **训练速度**：Grappa 平均比 DGL 快 **4×**，最高可达 **13×** 加速。
- **可扩展性**：在 64 个分区下仍保持近乎线性扩展。
- **超大规模训练**：在单台 V100 机器上，以 phase-parallel 模式完成 **RMAT-36（1.1 万亿边）** 上 Sage-3 模型的一个 epoch 仅需 **3.6 小时**。

### 与基线方法的对比结果
#### 与 DGL 对比（Table 3）
- **速度优势**：在所有模型和数据集上，Grappa 的 epoch 时间显著低于 DGL（Random 和 METIS）。
  - 例如，在 OGBN-Pa 上训练 GCN-2，Grappa 为 3.8s，而 DGL-Metis 需 9.1s。
- **准确性优势**：尤其在深层模型上表现更优。
  - **平均测试准确率**：Grappa 为 **67.68%**，DGL 为 **61.22%**。
  - **深层模型表现**：
    | 模型深度 | Grappa 准确率 | DGL 准确率 |
    |----------|----------------|-------------|
    | 2-layer  | ~69%           | ~69%        |
    | 3-layer  | ~67%           | ~63%        |
    | 4-layer  | ~67%           | ~51%        |
  - Grappa 的隔离训练具有类似 **dropout** 的正则化效果，有效缓解了深层模型的过拟合问题。

#### 与 Cluster-GCN 对比（Table 5）
- **速度**：Cluster-GCN 的 epoch 时间约为 Grappa 的一半（1.2s vs 2.6s on OGBN-Pr）。
- **准确性**：Grappa 测试准确率更高（**76.63%** vs 73.90%）。
- **总耗时**：考虑预处理时间（Cluster-GCN 需 881s 聚类），Grappa 总时间更短（1425s vs 1481s）。
- **通用性**：Grappa 支持所有 GNN 模型，而 Cluster-GCN 仅限 GCN。

#### 与 MGG 对比（Table 6）
- **小图**：MGG 利用 NVLink 在小图上更快（如 Reddit 上 0.2s vs 0.4s）。
- **大图**：MGG 因显存不足在 OGBN-Pa 上崩溃（2/4 GPU），而 Grappa 成功运行（5.7s @2 GPU）。
- **结论**：Grappa 在显存受限场景下更具优势，且其分布式架构天然支持多节点扩展。

### 消融实验结果（Table 7）
在 Reddit 数据集上对 Grappa 的关键组件进行消融：

| 方法 | 描述 | 测试准确率（GCN-3） |
|------|------|---------------------|
| **Grappa** | 完整方法 | **0.9326** |
| Grappa-UW | 移除覆盖率校正 | 0.8284 |
| Grappa-FP | 固定分区（不重分区） | 0.8464 |
| Grappa-50S | 50% 远程采样（非隔离） | 0.6593 |

- **发现**：移除任一组件都会导致准确率下降，证明 **动态重分区** 和 **覆盖率校正** 均至关重要。
- **反直觉发现**：即使允许访问远程节点（Grappa-50S），其准确率仍远低于完整 Grappa，说明 **隔离训练 + 校正机制** 的组合优于直接访问，可能得益于更强的正则化效果。

---

## 4. 关键结论和发现

### 主要发现
1. **通信是瓶颈**：跨分区邻居通信是分布式 GNN 训练的主要开销，而非计算。
2. **隔离训练可行**：通过 **动态重分区 + 覆盖率校正**，可以在完全消除特征通信的前提下，维持甚至提升模型精度。
3. **正则化效应**：隔离训练类似于输入层 dropout，特别有利于深层 GNN 模型，减轻过拟合。
4. **高效扩展**：Grappa 实现了近乎线性的水平扩展，并能通过 phase-parallel 模式突破单机内存限制。
5. **通用性强**：方法不依赖特定模型、硬件或缓存策略，具有广泛适用性。

### 方法的局限性
- **重分区开销**：虽然实验证明开销较低（<12% 训练时间），但在极高频率切换分区时可能影响效率。
- **收敛速度**：尽管 epoch 时间短，但可能需要更多 epoch 收敛（文中未明确比较总收敛时间）。
- **理论假设**：渐近无偏性依赖于长期正覆盖概率，实际中超级 epoch 数有限，可能存在残余偏差。

### 未来工作方向
- 设计更智能的 **分区切换启发式策略**（当前使用简单轮转）。
- 探索 **自适应重分区频率**，根据模型收敛状态动态调整。
- 将 Grappa 的思想应用于 **Federated GNN** 场景，结合隐私保护与高效通信。
- 进一步优化 **phase-parallel 模式** 下的资源调度与 I/O 效率。

> **总结**：Grappa 通过颠覆性的 **gradient-only communication** 范式，从根本上解决了分布式 GNN 训练的通信瓶颈，实现了速度、精度和可扩展性的全面超越，为超大规模图学习提供了新的可行路径。

</details>

---

### 6. [Dynamic Expert Sharing: Decoupling Memory from Parallelism in Mixture-of-Experts Diffusion LLMs](https://arxiv.org/abs/2602.00879)

**Authors**: Hao Mark Chen, Zhiwen Mo, Royson Lee, Qianzhou Wang, Da Li, Shell Xu Hu, Wayne Luk, Timothy Hospedales, Hongxiang Fan  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.00879v1  

#### Abstract
Among parallel decoding paradigms, diffusion large language models (dLLMs) have emerged as a promising candidate that balances generation quality and throughput. However, their integration with Mixture-of-Experts (MoE) architectures is constrained by an expert explosion: as the number of tokens gene...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Dynamic Expert Sharing: Decoupling Memory from Parallelism in Mixture-of-Experts Diffusion LLMs**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**
- **“Expert Explosion”瓶颈**：在 Mixture-of-Experts (MoE) 架构的 diffusion large language models (dLLMs) 中，随着并行解码的 token 数量增加，每个 token 独立路由导致激活的**唯一专家数量近乎线性增长**，引发严重的 **HBM（High Bandwidth Memory）到 SRAM 的内存流量开销**。
- 这使得模型推理进入 **memory-bound regime**，抵消了 MoE 和并行解码带来的计算效率优势。

### 🚀 **提出了什么新方法或新思路**
提出 **Dynamic Expert Sharing (DES)**，一种全新的 MoE 优化范式：
- 将优化目标从 **token-centric pruning** 转向 **sequence-level coreset selection**。
- 在一个并行解码块内，通过跨 token 共识动态选择一个**紧凑且高实用性的专家子集（coreset）**，供所有 token 共享使用。

#### 两种核心策略：
1. **DES-Seq (Intra-Sequence Sharing)**  
   - 对 block 内每个 token 的 Top-k 专家取并集，形成共享 coreset。
   - 简单有效，支持向量化执行。

2. **DES-Vote (Saliency-Aware Voting)**  
   - 引入基于 router 权重的加权投票机制，聚合所有 token 的路由得分。
   - 高 saliency 的专家更可能被选中，实现对语义复杂度的整体响应。

> 💡 **核心思想**：利用并行 token 之间的语义相关性，最大化专家复用，从而**解耦内存开销与并行度（degree of parallelism）**。

### ⚖️ **相比现有方法的优势**
| 方法 | 局限性 | DES 的改进 |
|------|--------|------------|
| **Vanilla MoE** | 每个 token 独立路由 → 专家爆炸 | 显著减少唯一激活专家数 |
| **Expert Pruning / Merging** | 静态压缩，不可逆损失容量 | 动态、无损（无需重新训练） |
| **Expert Skipping (e.g., NAEE, MC-MoE)** | 仅减少 per-token FLOPs，不解决全局 HBM 流量 | 降低整体 HBM 数据加载量 |
| **OEA (Batch-level sharing)** | 适用于 AR 批处理，未针对 dLLM 设计 | 专为 block-wise 并行解码设计 |

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**
四个需要长文本生成和多样化推理能力的基准：
- **HumanEval**：代码生成任务
- **MBPP**：Python 编程任务
- **GSM8K**：小学数学应用题
- **MATH500**：高等数学问题

> 所有结果为多个 benchmark 上的平均值或分别报告。

### ⚙️ **实验设置**
- **模型**：
  - `LLaDA2.0-Mini` (16B, MoE)
  - `LLaDA-MoE-7B` (7B, MoE)
- **硬件平台**：
  - NVIDIA B200 GPU + Intel Xeon 6960P CPU
  - CUDA 13.1，Nsight Systems 用于性能剖析
- **框架**：
  - 使用 `dInfer` 框架 + `Fast-dLLM` KV cache 方法
  - Block length = 32 tokens（含 16 prefix + 16 suffix）
- **参数控制**：
  - DES-Vote：通过 budget factor $ \beta $ 控制 coreset 大小 $ M_{\text{core}} = \beta \times M $
  - DES-Seq：通过局部选择数 $ k $ 控制

### 🎯 **评估指标**
| 指标 | 含义 |
|------|------|
| **Unique Activated Experts (T)** | 每层平均激活的唯一专家数量 |
| **Relative Accuracy (R.Acc %)** | 相对于 vanilla 模型的准确率百分比 |
| **MoE Kernel Latency (ms)** | MoE 层前向传播延迟 |
| **End-to-End GPU Kernel Time** | 整体 GPU 推理耗时 |
| **Memory Footprint (GB)** | 激活参数的内存占用 |
| **Cosine Similarity (s)** | 与原始路由模式的相似度（保真度） |

### 🔁 **基线方法对比**
- **Vanilla**：标准 MoE 路由
- **Top-K (k=4)**：直接减少 K 值以降低流量
- **NAEE**：基于累积概率跳过低分专家
- **MC-MoE**：保留重要 token 的专家
- 所有 baseline 均适配至 dLLM 设置，仅比较动态分配策略

---

## 3. **主要实验结果和性能指标**

### 📊 **关键性能数据汇总（来自 Table 1）**

| 方法 | Avg. T ↓ | Mem. (GB) ↓ | R.Acc (%) ↑ | 模型 |
|------|---------|------------|-------------|-------|
| **Vanilla** | 84 | 0.98 | 100.0 | LLaDA2.0-Mini |
| **DES-Vote (β=0.15)** | **38** | **0.45** | **99.5** | |
| **DES-Seq (k=3)** | 45 | 0.53 | 97.2 | |
| **NAEE / MC-MoE** | ~65 | ~0.76 | ~46.6 | |
| — | — | — | — | — |
| **Vanilla** | 59 | 0.35 | 100.0 | LLaDA-MoE-7B |
| **DES-Vote (β=0.6)** | **38** | **0.22** | **97.6** | |
| **DES-Seq (k=3)** | 42 | 0.25 | 96.9 | |

> ✅ **DES-Vote 在 LLaDA2.0-Mini 上实现：**
- **唯一专家减少 55%**（84 → 38）
- **内存占用下降 54%**（0.98GB → 0.45GB）
- **保持 99.5% 的原始精度**

### ⏱️ **效率提升（Figure 5）**
- **MoE Kernel Latency 下降最多达 38.0%**（LLaDA2.0-Mini）
- **总端到端 GPU 时间减少 8.2%–14.3%**
- 主要收益来自缓解 HBM 访问瓶颈

### 🔍 **自定义融合核函数加速（Figure 6）**
- 开发了融合 12 个操作（softmax, top-k, reduction）的定制 GPU kernel
- **Coreset selection 阶段速度提升 6×**

### 🔬 **消融实验结果**

#### （1）**Coreset Size 影响（Figure 7a）**
- 准确率随 coreset 增大而上升
- **DES-Vote 在相同大小下始终优于 DES-Seq**
- 可配置极小 coreset（如 β=0.1），突破 DES-Seq “每 token 至少一专家”的限制
- 某些情况下出现正增益（>100% R.Acc），推测因噪声专家被剪枝产生正则化效应

#### （2）**Block Size 鲁棒性（Figure 7b）**
- DES-Vote 在 block size 从 8 到 64 时，准确率波动极小
- **激活专家数基本恒定**，真正实现了“内存开销与并行度解耦”
- Vanilla 方法随 block 增大专家数急剧上升

#### （3）**Per-Token Computation 影响（Figure 8）**
- 固定唯一专家数（如 38），但改变 per-token K 值（8 vs 4）
- K=8 更优 → 表明 **从 coreset 中 re-activate 原本非 top-K 的专家仍能维持性能**

#### （4）**专家利用率分析（Figure 9）**
- **Cosine similarity ≥ 0.98** → 路由模式高度保真
- DES-Vote 的 hit 分布更集中（sharp curve），抑制长尾，进一步节省内存

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **“Expert Explosion”是 dLLM + MoE 的关键瓶颈**，其本质是内存流量而非计算压力。
2. **并行 token 存在显著专家需求重叠**，可通过序列级共识实现高效共享。
3. **DES 特别是 DES-Vote 能以 <1% 精度损失换取 >55% 唯一专家减少和近 40% 延迟下降**。
4. **DES 成功将 HBM 流量与并行度解耦**，使开发者可自由调整 block size 而不受内存带宽制约。

### ⚠️ **方法的局限性**
- 当前依赖于 router logits 的可聚合性，在极端异构任务混合场景中可能失效。
- 极端压缩（如 β < 0.1）可能导致性能不稳定，需谨慎调参。
- 定制 kernel 增加部署复杂性，尚未集成进主流推理框架（如 vLLM, TensorRT-LLM）。

### 🔮 **未来工作方向**
- 探索 **adaptive β 控制机制**，根据输入复杂度动态调节 coreset 大小。
- 将 DES 扩展至 **multimodal MoE models**（如 MoE-VLMs）。
- 结合 **expert prefetching** 与 DES，进一步隐藏内存延迟。
- 研究 **sequence-level expert routing 的可学习机制**，替代启发式投票。

---

> 📌 **一句话总结**：  
> **DES 通过 sequence-level coreset selection 实现了 MoE dLLMs 中 memory 与 parallelism 的解耦，在几乎无损精度的前提下大幅降低内存流量和延迟，为高吞吐并行生成提供了新路径。**

</details>

---

### 7. [Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization](https://arxiv.org/abs/2602.01090)

**Authors**: Yang Liu, Chuan Zhou, Yancheng Chen, Shuai Zhang, Xixun Lin, Xiaoqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.01090v1  

#### Abstract
Large language models (LLMs) have emerged as promising general-purpose solvers for combinatorial optimization (CO), yet they fundamentally lack mechanisms to guarantee solution feasibility which is critical for real-world deployment. In this work, we introduce FALCON, a framework that ensures 100\% ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在组合优化（Combinatorial Optimization, CO）任务中展现出强大的模式识别和序列生成能力，但其本质是**无约束的生成模型**，无法保证输出满足硬性约束条件（如容量限制、路径连通性、独立集非邻接等）。这导致现有LLM-based CO求解器的可行性（Feasibility）远低于100%，严重制约其在物流、制造、应急调度等安全敏感场景的实际部署。

### 提出了什么新方法或新思路
本文提出 **FALCON**（Feasibility-Aware Language-based Combinatorial Optimization with Adaptive Inference），首个能提供**100%可行性保证**的LLM-based CO框架。其核心思想是将“可行性”分解为两个层次，并通过三个协同组件实现：

1. **Grammar-Constrained Decoding**  
   利用问题特定的上下文无关文法（Context-Free Grammar, CFG）约束解码头部，确保输出格式正确（如括号匹配、索引有效），解决**语法有效性**问题。

2. **Feasibility Repair Layer**  
   设计问题相关的修复算子（Repair Operator），将任何违反语义约束的解转换为可行解。该算子满足三大性质：
   - **可行性**（Feasibility）：输出必为可行解；
   - **幂等性**（Idempotence）：输入已可行则不修改；
   - **有界局部性**（Bounded Locality）：修改幅度与约束违反程度成正比。

3. **Adaptive Best-of-N Sampling**  
   动态调整采样数量：基于多个样本的一致性（Consistency）估计实例难度，在简单实例上早停以节省计算，在困难实例上增加探索。

此外，训练阶段引入 **BOPO**（Best-anchored Objective-guided Preference Optimization）：
- 构造偏好对时以当前批次中的最优可行解为锚点（Best-anchored）；
- 损失函数中按目标值差距加权（Objective-guided weighting），使差距更大的劣质解提供更强的学习信号；
- 实现无需人工标注的密集监督。

### 相比现有方法的优势
| 维度 | 现有方法 | FALCON |
|------|--------|--------|
| 可行性保证 | 软约束激励，不可控（通常<100%） | **理论可证的100%可行性** |
| 解质量 | 易因拒绝采样丢失高质量但不可行解 | 修复机制保留大部分结构，质量损失可控 |
| 推理效率 | 固定采样数，资源分配不均 | 自适应采样，平均减少58%计算量 |
| 训练效率 | Reward shaping稀疏，GRPO梯度被主导样本垄断 | BOPO提供更密集、均衡的梯度更新 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
在**七个NP-hard CO问题**上进行评估，覆盖三大领域：

| 问题类别 | 具体问题 | 规模 | 分布 | 参考求解器 |
|---------|--------|------|------|------------|
| Routing | TSP, CVRP, OP | 10–100节点 | Uniform, GM | LKH-3, COMPASS |
| Graph | MIS, MVC | 50–500节点 | ER, BA | Gurobi |
| Scheduling | PFSP, JSSP | 10–100作业 / 6–30作业 | Taillard | QIG, OR-Tools |

所有训练集均为合成数据（共50万实例），测试集各100个实例。

### 实验设置和评估指标

#### 主要指标
- **Feasibility (%)**：生成解满足所有约束的比例。
- **Optimality Gap (%)**：相对于参考求解器（如LKH、Gurobi）的相对目标值差距。
- **Inference Time (s)**：每实例平均推理时间。

#### 推理配置
- `N_min=8`, `N_max=64`, 置信阈值 `T=0.85`，温度 `T=0.7`
- 自适应采样基于贝叶斯置信度提前终止。

#### 训练流程
1. **SFT阶段**：在专家解上微调（如LKH生成的TSP解）；
2. **BOPO阶段**：从当前策略采样K=8个解，构建偏好对并优化BOPO损失。

### 基线方法对比
分为四类：

| 类型 | 代表方法 |
|------|--------|
| 零样本通用LLM | GPT-4o, Claude-3.5, Llama-3.3-70B |
| 推理增强LLM | GPT-o1, DeepSeek-R1 |
| LLM优化方法 | OPRO, LMEA, PHP, SGE |
| 神经CO求解器 | SFT Only, GRPO, LLMCoSolver |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 2）

| 方法 | 平均可行性 (%) | 平均最优性差距 (%) | 平均耗时 (s) |
|------|----------------|--------------------|--------------|
| GPT-4o | ~40 | ~30–100 | 4–5 |
| LLMCoSolver (N=8) | 94–100 | 1.07–10.94 | 9.8 |
| **FALCON (Adaptive)** | **100** | **0.92–6.98** | **6.3** |

- **FALCON在所有7个问题上均达到100%可行性**，首次实现理论可证的完全可行性。
- 在最优性差距上，**5/7问题取得最佳结果**，其余接近最优。
- 自适应采样下仅需平均 **26.7次采样**（固定N=64需64次），节省约58%计算。

### 与基线方法的对比结果

#### ✅ 对比零样本LLM
- 可行性提升巨大：如TSP从39% → 100%，CVRP从6% → 100%
- 最优性差距显著缩小：如TSP从33.79% → 0.92%

#### ✅ 对比神经CO求解器
- **优于GRPO和LLMCoSolver**：尽管后者也使用强化学习，但FALCON通过BOPO+Repair实现了更高鲁棒性和更低gap。
- 例如在TSP上，FALCON(N=8)的gap为0.95%，优于LLMCoSolver的1.07%。

#### ✅ 对比经典启发式算法（Table 3）
| 方法 | TSP大实例gap | CVRP大实例gap | PFSP大实例gap |
|------|-------------|---------------|----------------|
| Nearest Neighbor | 26.19% | 22.07% | — |
| ACO | 36.69% | 29.49% | — |
| OR-Tools | 3.59% | 8.84% | — |
| **FALCON (Adaptive)** | **1.08%** | **6.05%** | **1.21%** |

→ FALCON全面超越传统启发式，尤其在大规模实例上优势明显。

### 消融实验结果（Table 4）

| 配置 | TSP可行性 | TSP Gap | CVRP可行性 | CVRP Gap |
|------|----------|--------|-----------|---------|
| FALCON (Full) | 100% | 0.92 | 100% | 3.52 |
| w/o Grammar | 100% | 0.95 | 100% | 3.67 |
| **w/o Repair** | **94%** | 0.89 | **85%** | 3.41 |
| w/o Adaptive | 100% | 0.91 | 100% | 3.48 |
| **w/o BOPO** | 100% | **1.85** | 100% | **5.89** |
| SFT Only | 89% | 2.30 | 59% | 6.02 |

- **移除Repair层**：可行性骤降，证明其对100%保证至关重要。
- **移除BOPO**：gap翻倍以上，说明其在提升解质量上的关键作用。
- **移除Grammar**：影响较小，表明语法错误较少，但仍有助于减轻repair负担。
- **移除Adaptive**：质量略优但耗时多近一倍，验证自适应采样的高效性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **可行性可以被严格保证**：通过“语法约束 + 语义修复”的分层架构，可在不牺牲表达力的前提下实现100%可行性。
2. **修复不一定损害质量**：由于BOPO训练出的模型本身高度可行（修复率仅2.8–18.5%），且修复具有局部性，因此整体质量损失极小，甚至有时能去除冗余节点而改善解。
3. **自适应采样显著提效**：基于一致性（Consistency）的难度估计能准确区分易/难实例，实现“简单题快答，难题深思”，平均节省超一半计算资源。
4. **BOPO优于GRPO**：在相同起点和预算下，BOPO在所有问题上都取得更低的最优性差距和更高的可行性（图2），尤其在强约束问题（如CVRP、MIS）上优势更大。

### 方法的局限性
- **修复可能引入偏差**：虽然理论上修复是局部的，但在极端情况下仍可能导致次优解。
- **依赖良好初始分布**：若SFT阶段未能掌握基本求解模式，后续BOPO和repair难以弥补。
- **文法设计需人工参与**：每个新问题需要定制CFG，虽模块化但不够全自动。
- **未处理动态或随机CO问题**：目前假设问题是静态确定性的。

### 未来工作方向
- 将FALCON扩展至**随机规划**（Stochastic Programming）和**在线优化**（Online Optimization）场景。
- 探索**自动文法生成**技术，降低人工设计成本。
- 结合**形式验证工具**对修复过程进行端到端验证。
- 研究**多目标优化下的偏好建模**，支持Pareto前沿探索。
- 探索将FALCON应用于工业级真实世界CO系统（如城市交通调度、芯片布线等）。

---

> **一句话总结**：FALCON首次实现了LLM-based组合优化的**100%可行性保证**，通过“语法约束+语义修复+自适应采样”三层机制，在保持高解质量的同时，为LLM走向现实世界高可靠性应用铺平了道路。

</details>

---

### 8. [LRAgent: Efficient KV Cache Sharing for Multi-LoRA LLM Agents](https://arxiv.org/abs/2602.01053)

**Authors**: Hyesung Jeon, Hyeongju Ha, Jae-Joon Kim  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.01053v1  

#### Abstract
Role specialization in multi-LLM agent systems is often realized via multi-LoRA, where agents share a pretrained backbone and differ only through lightweight adapters. Despite sharing base model weights, each agent independently builds and stores its own KV cache for the same long, tool-augmented tr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LRAgent: Efficient KV Cache Sharing for Multi-LoRA LLM Agents

## 1. 论文的主要贡献和创新点

### 解决的问题
在多 **LLM Agent** 系统中，角色专业化通常通过 **multi-LoRA** 实现，即多个代理共享一个预训练主干模型，并通过轻量级适配器（LoRA）实现角色差异化。然而，尽管共享了主干权重，每个代理仍需独立构建和存储其 **KV Cache**，导致在长轨迹（long trajectories）场景下产生显著的内存和计算开销。

现有 **KV Cache 共享** 方法大多忽略了 multi-LoRA 架构的特性，未能有效利用其潜在的共享机会。

### 提出的新方法和新思路
本文提出了 **LRAgent**，一种专为 multi-LoRA 代理系统设计的高效 KV Cache 共享框架。其核心思想基于以下关键观察：

> 在相同上下文下，不同代理之间的 KV Cache 差异主要由 **LoRA 适配器输出** 引起，而来自共享预训练主干的激活则高度相似。

基于此，LRAgent 将 KV Cache 分解为两个部分：
1.  **Base Cache**：由预训练主干权重（`W₀`）生成，可在所有代理间**共享**。
2.  **Adapter Output (LR Cache)**：由 LoRA 权重（`ΔW = AB`）生成，具有**低秩**（low-rank）特性。

具体方案包括：
- **BaseShared**：仅共享 `Base Cache`，并以低秩形式（即中间激活 `XA`）存储每个代理的 `LR Cache`，避免了全维度存储。
- **BaseLRShared**：进一步利用 **shared-A multi-LoRA** 架构（即所有任务共享同一个 `A` 矩阵），使得 `LR Cache` 本身也可以被所有代理共享，从而实现了 `Base Cache` 和 `LR Cache` 的双重共享。

为了降低运行时重构 `Adapter Output` 的开销，作者设计了 **Flash-LoRA-Attention** 内核。该内核通过重新排序注意力计算，将 `(PV_lr)B` 替代 `P(V_lrB)`，避免了将低秩缓存显式地扩展到全维度，从而将计算开销从 `O(L * d_out)` 降低到 `O(L * r)`。

### 相比现有方法的优势
- **更高的效率**：相比非共享方案，显著降低了内存占用和推理延迟。
- **更好的准确性**：相比直接共享整个 KV Cache 或选择性重计算的方法（如 DroidSpeak），LRAgent 更好地保留了由 LoRA 适配器带来的关键角色特定信息，因此准确率下降更小。
- **架构兼容性**：是首个专门为 multi-LoRA 架构设计的 KV Cache 共享方案，能与现有的 PEFT 技术无缝集成。

## 2. 核心实验方法和设置

### 数据集
- **HotpotQA**：一个多跳问答数据集，需要外部知识和多步推理。
- **ScienceQA**：一个科学领域的多模态问答数据集，同样需要多步推理。
- 实验使用了 2.5k HotpotQA 和 2.0k ScienceQA 的测试集。

### 实验设置和评估指标
- **模型**：在 `LLaMA-3.1-8B-Instruct` 和 `Ministral-8B-Instruct` 上进行微调和评估。
- **代理角色**：遵循 AutoAct 设置，定义了三种角色：
  - **Plan Agent**：负责推理和选择工具。
  - **Action Agent**：负责执行工具调用。
  - **Reflect Agent**：负责审查和决定是否终止。
- **评估指标**：
  - **Benchmark Accuracy (%)**：在 HotpotQA 和 ScienceQA 上的最终答案准确率。
  - **System Throughput (tokens/s)**：系统总处理序列长度除以端到端延迟。
  - **Time-to-First-Token (TTFT)**：所有代理步骤的 Prefill 延迟之和。
  - **Memory Usage (GB)**：峰值内存消耗。

### 基线方法对比
- **Non-Shared**：默认基线，每个代理维护自己的完整 KV Cache。
- **FullShared**：最理想的共享基线，所有代理完全共享整个 KV Cache（无重计算）。
- **DroidSpeak**：一种选择性重计算方法，对模型中最敏感的 33% 层进行重计算。

## 3. 主要实验结果和性能指标

### 关键性能数据与对比
1.  **准确性 (Accuracy)**：
    - **BaseShared** 和 **BaseLRShared** 的准确率非常接近 `Non-Shared` 基线。
    - 在 `LLaMA-3.1-8B` 上，`BaseShared` 的平均准确率损失仅为 **-0.28%**，`BaseLRShared` 为 **-0.97%**。
    - 相比之下，`FullShared` 和 `DroidSpeak` 的准确率损失更大，分别达到 **-2.48%** 和 **-2.12%**。
    - 这表明 LRAgent 能在共享的同时更好地保留角色特定行为。

2.  **吞吐量 (Throughput)**：
    - `BaseLRShared` 的吞吐量表现最佳，接近 `FullShared` 的上限。
    - 在最长序列（66.4k tokens）上，`BaseLRShared` 达到了 `Non-Shared` 的 **2.46倍** 吞吐量增益。
    - `BaseShared` 也达到了 **1.42倍** 的增益。
    - `DroidSpeak` 的增益约为 **1.36倍**，与 `BaseShared` 相当。

3.  **首令牌延迟 (TTFT)**：
    - `BaseLRShared` 在减少 TTFT 方面效果最为显著，最高可实现 **4.44倍** 的降低，几乎与 `FullShared` 持平。
    - `BaseShared` 最高可实现 **1.63倍** 的降低。

4.  **内存使用 (Memory Usage)**：
    - `BaseShared` 和 `BaseLRShared` 将 KV Cache 内存减少了近 **1/3**，与 `FullShared` 和 `DroidSpeak` 处于同一水平。

### 消融实验结果
- **Rank Ablation**：实验了不同的 LoRA 秩（r=4, 8, 16, 32）。结果显示，在 `r=8` 时已能达到较好的性能，更高的秩并未带来明显提升，因此选择 `r=8` 作为平衡点。
- **LoRA 应用位置**：比较了仅在 `qv`（query, value）投影上应用 LoRA 与在 `qkvo`（query, key, value, output）上应用的变体。结果表明，`qv` 设置在准确率和效率上都更具优势，这也是本文采用的设置。

## 4. 关键结论和发现

### 主要发现
1.  **分解式共享优于全量共享**：直接共享完整的 KV Cache (`FullShared`) 会因忽略 LoRA 适配器的差异而导致显著的准确率下降。LRAgent 通过分离 `Base Cache` 和 `LR Cache`，实现了“精准共享”，在保证效率的同时最大限度地保留了模型性能。
2.  **低秩特性是关键**：利用 LoRA 适配器输出固有的低秩特性来压缩存储和优化计算，是实现高效性的核心技术。
3.  **shared-A 架构潜力巨大**：`BaseLRShared` 证明了当 multi-LoRA 系统采用 shared-A 设计时，可以实现 KV Cache 和 LR Cache 的双重共享，从而在计算效率上取得质的飞跃，其 TTFT 几乎与理论最优的 `FullShared` 相当。

### 方法的局限性
- **依赖 shared-A 架构**：`BaseLRShared` 的最大优势依赖于 multi-LoRA 系统采用了 shared-A 的训练方式，这可能不适用于所有场景。
- **额外的计算开销**：虽然 `Flash-LoRA-Attention` 极大降低了开销，但运行时重构 `Adapter Output` 仍然引入了额外的计算，尤其是在生成阶段。

### 未来工作方向
- 探索更高效的低秩缓存管理策略。
- 将 LRAgent 的思想扩展到其他参数高效微调（PEFT）方法，如 AdaLoRA 或 PiSSA。
- 研究如何在不牺牲准确率的前提下，进一步简化或自动化 `shared-A` 的训练过程。

</details>

---

### 9. [SNIP: An Adaptive Mixed Precision Framework for Subbyte Large Language Model Training](https://arxiv.org/abs/2602.01410)

**Authors**: Yunjie Pan, Yongyi Yang, Hanmei Yang, Scott Mahlke  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.01410v1  

#### Abstract
Training large language models (LLMs) efficiently while preserving model quality poses significant challenges, particularly with subbyte precision supported by state-of-the-art GPUs. Current mixed-precision training approaches either apply uniform precision to all GEMM operations or rely on heuristi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SNIP: An Adaptive Mixed Precision Framework for Subbyte Large Language Model Training

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的大规模语言模型（LLM）训练依赖 **Mixed Precision Training**（如 FP8、FP4）来降低计算成本，但主流方法存在以下问题：
- **Uniform Precision**：对所有层采用相同精度（如全用 FP8 或 FP4），忽略了不同层对精度的敏感性差异，导致效率未达最优。
- **Heuristic-based 方法**：基于局部指标（如 KL 散度、绝对误差）选择精度，无法捕捉训练过程中的全局动态（如梯度传播、权重更新偏差），容易导致收敛不稳定或质量下降。
- **缺乏系统性优化**：缺少一个能联合考虑前向和反向传播中量化影响，并在满足效率目标下最小化质量损失的框架。

### 提出了什么新方法或新思路
本文提出 **SNIP**（**S**ystematic **N**eural **I**nteger-programming-based **P**recision selection），一种面向 LLM 预训练的细粒度自适应混合精度框架，其核心创新包括：

- **双维度质量损失度量**：
  - **Loss Divergence**：衡量前向传播中因量化导致的训练损失增加。
  - **Weight Divergence**：衡量反向传播中因梯度量化误差累积导致的权重更新偏差。
  这两个指标共同量化了量化对训练稳定性和最终模型质量的影响。

- **基于 ILP 的全局优化策略**：
  将每层的精度选择建模为一个 **Integer Linear Programming**（ILP）问题：
  - **目标函数**：最小化总质量损失（加权的 Loss Divergence 和 Weight Divergence）。
  - **约束条件**：满足给定的效率目标（如 FP4 FLOPs 占比）。
  该方法实现了全局最优的层间精度分配，而非局部启发式决策。

- **实用的异步集成系统**：
  SNIP 在训练过程中周期性地收集统计信息、分析发散度、求解 ILP 并更新精度策略，整个过程异步进行，不中断主训练流程，具备良好的工程可集成性。

### 相比现有方法的优势
- **更优的 Pareto 前沿**：在保持接近 BF16 模型准确率的同时，实现高达 80% 的 FLOPs 节省，显著优于均匀精度和各类启发式方法。
- **更强的稳定性**：通过同时考虑前向和反向的量化影响，避免了仅关注局部误差导致的训练崩溃。
- **更高的通用性和适应性**：支持不同模型大小（1B 到 70B）、不同训练阶段，并可扩展至新的量化格式（如 FP4、FP8）。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **TinyLlama 1B**：使用 SlimPajama 和 StarcoderData 数据集的约 1% 子集。
- **OpenLlama 3B / 7B**：使用 RedPajama 数据集的约 1% 子集。
- **70B LLaMA-style 模型**：使用内部工业数据集。

> 注：由于实验基于中间检查点恢复训练，因此使用原始数据集的小比例采样即可保证训练效果。

### 实验设置和评估指标
- **模型**：TinyLlama 1B、OpenLlama 3B、OpenLlama 7B 和一个 70B 的 LLaMA 风格稠密模型。
- **量化模拟**：由于缺乏原生支持 FP4/FP8 的硬件，采用 **fake quantization** 模拟，支持 FP4 (E2M1) 和 FP8 (E4M3) 格式。
- **量化粒度**：输入激活和梯度采用 1x128 **tilewise** 量化，权重采用 128x128 **blockwise** 量化。
- **评估指标**：
  - **效率**：FP4 FLOPs 占比（作为端到端加速的代理指标）。
  - **准确性**：使用 `lm-evaluation-harness` 测试多个基准：
    - **常识推理**：ARC-c, ARC-e
    - **综合能力**：MMLU
    - **常识理解**：BoolQ, HellaSwag, OBQA, PIQA, WinoGrande
  - **训练稳定性**：训练损失曲线、相对损失差。

### 基线方法对比
- **Uniform Precision**：BF16、FP8、FP4。
- **Empirical Coarse-grained**：
  - `E-layer-id`：头尾层用高精度（FP8），中间层用低精度（FP4）。
  - `E-layer-type`：敏感层类型（如 MLP 中的 Gate/Up 层）用 FP8，其余用 FP4。
- **Fine-grained Heuristic**：
  - `min-abs-err`：选择使绝对量化误差最小的层用 FP4。
  - `min-rel-err`：选择使相对量化误差最小的层用 FP4。
- **Random**：随机分配 FP4/FP8。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在 **TinyLlama 1B** 上，SNIP 在 **80% FP4 FLOPs** 的效率预算下，平均测试准确率仍能达到 **44.27**，几乎与 BF16 基线（44.22）持平。
- 在 **70B 模型** 上，SNIP 在 50% FP4 FLOPs 下，相对 BF16 的训练损失差极小，且在多个任务上表现稳定，而 FP4 全量训练则出现明显发散。

### 与基线方法的对比结果
- **全面超越启发式方法**：
  - 在 **Table 1** 中，SNIP 在所有 FP4 FLOPs 比例（25%, 50%, 75%）下均取得最高平均分，显著优于 `min-abs-err`、`min-rel-err` 及各类随机方法。
  - 例如，在 75% FP4 FLOPs 时，SNIP 平均分为 44.21，而 `min-abs-err` 仅为 33.53。
- **优于经验性粗粒度方法**：
  - `E-layer-id` 和 `E-layer-type` 在低效率预算下尚可，但在高预算（如 50%）下性能迅速下降，而 SNIP 仍能保持高质量。
- **训练稳定性最佳**：
  - **Figure 8** 显示，在 75% FP4 FLOPs 下，SNIP 的训练损失曲线与 BF16 几乎重合，而其他方法（如 `random`）迅速发散。
  - **Figure 9** 显示，70B 模型上 SNIP 的相对损失差远低于其他方法，证明其在大模型上的稳定性。

### 消融实验结果
- **质量损失度量的有效性验证**：
  - **Figure 13** 显示，SNIP 估计的每层量化损失与真实测量值高度一致，证明了其误差传播估计方法的可靠性。
- **动态调整的必要性**：
  - **Figure 11** 显示，SNIP 在不同训练阶段（5k 到 240k 步）会选择不同的层使用高精度，表明层的重要性是动态变化的，静态策略次优。
- **Pipeline Parallelism 集成效果**：
  - **Figure 12** 展示了 SNIP 在流水线并行下的时间线，证明其能有效平衡各阶段的计算负载，避免瓶颈。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **量化影响需全局建模**：仅靠局部误差（如 abs/rel error）无法准确预测量化对 LLM 训练质量的影响，必须联合考虑前向的 **Loss Divergence** 和反向的 **Weight Divergence**。
2. **层间敏感性差异显著**：不同层（尤其是 MLP 中的 Down 层、Attention 中的 V 层）对低精度更敏感，需要更高精度保护。
3. **SNIP 实现高效与高质量的平衡**：通过 ILP 优化，SNIP 能在高达 80% FP4 FLOPs 的情况下，将模型质量损失降至最低，显著优于现有方法。
4. **方法具有强鲁棒性和可扩展性**：在 1B 到 70B 不同规模模型和多个训练阶段上均表现出色。

### 方法的局限性
- **计算开销**：虽然异步执行，但收集统计和求解 ILP 仍引入额外开销（约每 100k 步一次，耗时 ~25 分钟）。
- **依赖假量化模拟**：实验基于软件模拟，实际硬件部署时可能受特定架构限制（如内存带宽、稀疏 GEMM 支持）。
- **未直接优化端到端延迟**：以 FP4 FLOPs 比例作为效率指标，而非实测训练时间，未来需结合真实硬件验证。

### 未来工作方向
- **扩展至更多量化格式**：将 INT4、INT8 等整数量化纳入 ILP 选项。
- **探索在线学习替代 ILP**：研究更轻量级的在线优化器替代耗时的 ILP 求解。
- **结合通信优化**：将低精度扩展至 Reduce-Scatter 等通信操作，进一步降低通信开销。
- **应用于微调和多模态模型**：验证 SNIP 在 PEFT（如 LoRA）和视觉-语言模型中的有效性。

</details>

---

### 10. [Position: Agentic Evolution is the Path to Evolving LLMs](https://arxiv.org/abs/2602.00359)

**Authors**: Minhua Lin, Hanqing Lu, Zhan Shi, Bing He, Rui Mao, Zhiwei Zhang, Zongyu Wu, Xianfeng Tang, Hui Liu, Zhenwei Dai, Xiang Zhang, Suhang Wang, Benoit Dumoulin, Jian Pei  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.00359v1  

#### Abstract
As Large Language Models (LLMs) move from curated training sets into open-ended real-world environments, a fundamental limitation emerges: static training cannot keep pace with continual deployment environment change. Scaling training-time and inference-time compute improves static capability but do...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Position: Agentic Evolution is the Path to Evolving LLMs》总结

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的 **Large Language Models (LLMs)** 在从静态训练环境部署到开放、动态的真实世界时面临一个根本性挑战：**train-deploy gap**（训练-部署环境差异）。  
- 静态模型无法应对无限多变的现实场景（如API变更、格式迁移、隐私约束）。
- 现有的适应方法（如在线微调或记忆积累）存在严重缺陷：
  - **Parametric Fine-tuning**：易导致 **catastrophic forgetting**（灾难性遗忘），且更新不透明、难治理。
  - **Heuristic Memory Accumulation**：导致 **context saturation**（上下文饱和），改进收益递减。

### 提出了什么新方法或新思路
提出 **Agentic Evolution（代理式进化）** 范式，并构建通用框架 **A-Evolve**。

#### 核心思想
将 LLM 系统的部署期改进视为一个由“**Evolver Agent**”驱动的、目标导向的优化过程，而非固定流水线。  
该 Evolver Agent 是一个自主决策体，负责：
- 诊断失败原因
- 决定何时更新
- 生成可验证的模块化改进

#### 三大原则（Three Principles）
| 原则 | 说明 |
|------|------|
| **Goal-Oriented Principle** | 明确“**what to change**”：主动归因失败，定位需修改的具体组件（如工具、schema），实现因果级修复。 |
| **Autonomy Principle** | 明确“**when to change**”：自主判断是否触发更新，避免盲目或频繁修改，提升稳定性。 |
| **Compositional Principle** | 明确“**how to change**”：通过结构化、模块化的 artifact（如工具、测试、技能）进行更新，支持复用与验证。 |

#### 框架设计：A-Evolve
- **Persistent Artifact State $T_s$**：包含三类可编辑资产：
  - **Knowledge Registry (K)**：结构化知识（如API文档、流程规范）
  - **Tool Registry (T)**：可执行函数（带签名与测试）
  - **Validation Registry (V)**：验证机制（单元测试、回归检查）
- **Solve-Evolve Control Loop**：
  - **Solve Phase**：使用当前策略完成任务。
  - **Evolve Phase**：Evolver 分析历史轨迹，提出并验证候选更新。
- **Evolver Agent 结构**：
  - `Diagnose` → `Plan` → `Update` → `Verify`

### 相比现有方法的优势
| 维度 | 传统方法 | Agentic Evolution |
|------|--------|-------------------|
| 更新对象 | 权重 $\theta$ 或原始文本记忆 | 可解释、可审计的结构化 artifact |
| 更新机制 | 固定规则（梯度下降 / 追加检索） | 自主决策 + 验证门控（commit decision） |
| 可扩展性 | 收益快速饱和 | 支持持续改进（amortization of reasoning） |
| 安全性 | 难以控制，风险高 | 通过 `V` 注册表实现强治理 |
| 数据隐私 | 需集中回传数据 | 支持本地化、去中心化进化 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **AppWorld** (Trivedi et al., 2024)：一个用于评测交互式编码智能体的基准平台。
  - 包含 9 个日常应用（如 Amazon、Spotify、Venmo）和 457 个 API。
  - 模拟约 100 名虚构用户的数字活动，支持复杂任务。
  - 任务需跨多个应用调用 API，平均每个任务使用 9.5 个 API。
  - 划分：
    - 50 个任务用于进化训练（training split）
    - 50 个任务用于测试评估（test-normal split）

### 实验设置和评估指标
- **固定计算预算**：
  - `C_solve`：每任务最大 tool calls / steps / tokens（解题阶段）
  - `C_evolve`：每 episode 最大 tokens 和 tool invocations（进化阶段）
- **评估指标**：
  - **Task Goal Completion (TGC)**：任务完全成功的比例（所有单元测试通过）。
  - **Average Passed Tests (APT)**：平均每项任务通过的单元测试比例，衡量渐进式进步。

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **Vanilla** | 无进化 | 不进行任何持久化更新，直接推理 |
| **APE** (Zhou et al., 2022) | 非参数启发式 | 基于搜索的 prompt 进化，选择得分高的指令 |
| **AWM** (Wang et al., 2024) | 非参数启发式 | 从历史轨迹中提取可复用的工作流作为记忆 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）

| Method | Claude Haiku 4.5 (TGC/APT) | Gemini 3 Flash (TGC/APT) |
|--------|----------------------------|--------------------------|
| Vanilla | 32 / 51.16 | 56 / 80.45 |
| APE | 30 / 56.00 | 52 / 84.00 |
| AWM | 46 / 65.76 | 52 / 87.75 |
| **A-Evolve** | **64 / 84.31** | **82 / 92.05** |

> ✅ **结论**：A-Evolve 在所有 solver backbone 上均显著优于基线，尤其在小模型上提升巨大（Haiku 4.5 的 TGC 从 32% 提升至 64%）。

### 与基线方法的对比结果
- **能力放大器效应**：A-Evolve 能将弱 solver（如 Haiku 4.5）的能力提升至接近甚至超过更强的 vanilla 模型（如 Sonnet 4）。
- **缩小容量差距**：例如，A-Evolve + Haiku 4.5（64% TGC） > Vanilla + Sonnet 4（42% TGC）。
- **高效性**：即使极小的进化步数（如 1 step），也能带来显著增益。

### 消融实验结果（Fig. 3 & C.2）
移除 A-Evolve 中任一组件均导致性能下降：

| 变体 | 性能影响 | 失败模式 |
|------|--------|--------|
| **A-Evolve/D**（无诊断） | 显著下降 | 盲目修补，仅掩盖错误而非根治 |
| **A-Evolve/A**（无分析工具） | 下降 | 依赖单次轨迹推断，错过系统性模式 |
| **A-Evolve/P**（无规划） | 下降 | 编辑不协调，如改工具未同步改 schema |
| **A-Evolve/V**（无验证） | **下降最多** | 提交有缺陷 artifact（如语法错误工具），污染上下文，引发回归 |

> 🔍 **关键发现**：**Verification 是稳定性的关键保障**；完整闭环不可分割。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **Agentic Evolution 是必要范式**：
   - 传统静态优化无法应对开放世界的持续变化。
   - 将进化本身建模为 **goal-directed agent** 是实现可持续适应的关键。

2. **进化可规模化（Evolution-Scaling Hypothesis）**：
   - 提出假设：**适应能力随 evolution-time compute 单调增长**。
   - 实验证明：增加进化步数或使用更大 evolver（如 Opus > Sonnet > Haiku），性能持续提升，而 AWM 快速饱和。

3. **结构化 artifact 的价值**：
   - 将推理成果固化为 **tool、skill、test** 等模块，实现 **amortization of expensive reasoning**。
   - 相比于存储原始文本记忆，更高效、可复用、可治理。

4. **本地化进化的社会价值**：
   - 支持在设备端或私有环境中独立进化，无需上传敏感数据，增强 **privacy 与 data sovereignty**。

### 方法的局限性
- **计算开销较高**：维护 Evolver Agent 需额外 compute resource，短期效率低于简单启发式。
- **对 Evolver 能力依赖强**：小模型作为 evolver 时诊断质量差，验证失败率高（见 Table 3）。
- **当前框架仍为 centralized**：虽支持本地进化，但未探讨多 agent 协同进化的分布式架构。
- **验证机制依赖人工定义**：V registry 中的测试需预先编写，自动化生成仍是挑战。

### 未来工作方向
1. **Benchmark Development**：
   - 构建更能体现 train-deploy gap 的非平稳环境基准，评估 artifact 的持久性和复用性。

2. **Framework Optimization**：
   - 改进 diagnosis、planning、verification 模块，提高 compute 利用效率，逼近 compute-optimal frontier。

3. **理论建模**：
   - 形式化 agentic evolution 为组合程序空间上的优化问题。
   - 建立与非代理方法的分离界限（separation results）。
   - 推导长期适应的 regret bounds。

4. **安全与对齐机制深化**：
   - 设计更鲁棒的 validation gates（如形式化验证、人类反馈集成）。
   - 防止 capability drift 与目标错位（misalignment）。

> 📌 **最终论断**：Agentic Evolution 不是可选项，而是 LLM 系统走向真实世界部署的 **必然路径**。它标志着 AI 系统从“静态推理机”向“持续学习体”的范式跃迁。

</details>

---

### 11. [Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach](https://arxiv.org/abs/2602.01131)

**Authors**: Yue Zhong, Jiawen Kang, Yongju Tong, Hong-Ning Dai, Dong In Kim, Abbas Jamalipour, Shengli Xie  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.01131v1  

#### Abstract
With the rapid expansion of the low-altitude economy, Unmanned Aerial Vehicles (UAVs) serve as pivotal aerial base stations supporting diverse services from users, ranging from latency-sensitive critical missions to bandwidth-intensive data streaming. However, the efficacy of such heterogeneous netw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**低空经济**（Low-Altitude Economy, LAE）中无人机**（UAV）**作为空中基站时面临的资源分配与物理控制稳定性之间的矛盾问题。传统网络设计通常以吞吐量为中心，忽略了通信延迟对控制环路稳定性的直接影响，尤其在灾后应急等安全关键任务中可能导致系统失稳甚至任务失败。

### 提出的新方法与创新思路
1. **构建了 Sensing-Communication-Computing-Control (SC³) 闭环框架**  
   首次将 Sensing、Communication、Computing 和 Control 四个环节统一建模为一个闭环系统，明确刻画了通信资源波动如何通过端到端延迟影响控制系统的实时响应与物理稳定性。

2. **引入 Lyapunov 稳定性理论进行控制-通信协同设计**  
   利用 **Lyapunov stability theory** 将抽象的物理控制稳定性要求转化为可量化的通信延迟边界（`T_budget`），实现了从“控制需求”到“通信约束”的理论映射，为资源分配提供了严格的数学基础。

3. **提出基于 Stackelberg 博弈的资源分配机制**  
   构建了一个分层博弈模型：
   - **UAV 作为领导者**（Leader）动态定价带宽资源；
   - **用户作为跟随者**（Follower）根据价格和服务紧迫性优化带宽请求。
   该机制通过激励相容的方式实现负载均衡，并保障系统在动态环境下的稳定性。

4. **设计轻量级的 Pruning-based PPO 算法**  
   针对标准 **Deep Reinforcement Learning (DRL)** 在能量受限边缘平台上的高计算开销问题，提出一种结合**动态结构化剪枝**（dynamic structured pruning）的 **Proximal Policy Optimization (PPO)** 算法，显著压缩神经网络规模，在训练过程中去除冗余神经元，降低推理延迟与能耗。

### 相比现有方法的优势
| 维度 | 本文方法 | 现有方法 |
|------|----------|---------|
| **系统视角** | 控制-通信联合设计（SC³闭环） | 仅关注通信性能（如吞吐量、AoI） |
| **稳定性保障** | 显式建模并保证 Lyapunov 意义下的渐近稳定 | 忽略或弱化物理控制稳定性 |
| **激励机制** | 定价策略由稳定性状态驱动（Lyapunov-driven） | 仅基于资源稀缺性或经济效用 |
| **算法效率** | 动态剪枝实现轻量化 DRL，适合机载部署 | 使用全连接重型网络，难以部署于 UAV |

---

## 2. 核心实验方法和设置

### 实验场景与仿真设置
- **场景设定**：灾后应急通信恢复场景，地面基础设施瘫痪，多架 UAV 作为空中基站服务幸存者（users）。
- **用户类型**：包含紧急型（短包、超低延迟、高可靠性）与非紧急型（大载荷、连续传输）两类业务。
- **UAV 数量**：3–12 架；**用户数量**：5–20 名。
- **信道模型**：采用自由空间路径损耗模型，考虑距离相关的 `SNR` 变化。
- **控制模型**：UAV 跟踪用户位置，建模为带有输入延迟的线性随机微分方程，使用离散化状态更新。

### 评估指标
| 指标 | 描述 |
|------|------|
| **Test Reward** | UAV 的平均效用（收益减去成本），反映整体系统性能 |
| **Stackelberg Equilibrium 收敛性** | 定价与带宽请求是否收敛至纳什均衡 |
| **End-to-End Latency** (`T_total`) | 包括 Sensing、Communication、Computing、Control 四部分延迟 |
| **Control Loop Stability** | 是否满足 Lyapunov 条件 `E[V(x_{k+1})] ≤ ρV(x_k) + Tr(PQ)` |
| **Inference Latency / Model Size** | 剪枝前后模型参数量与推理时间对比 |

### 基线方法对比
1. **Standard PPO**：未剪枝的标准多智能体 PPO 算法
2. **Greedy Algorithm**：UAV 固定高价或按负载调整价格，用户贪心申请带宽
3. **Random Algorithm**：随机定价与带宽请求

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **奖励提升**：提出的 **Pruning-based PPO** 在 500 轮迭代后达到约 **8.0** 的测试奖励，优于标准 PPO（~7.6）、贪婪算法（~4.5）和随机策略（~2.3）。
- **收敛速度更快**：Pruning-based PPO 在约 300 轮内完成收敛，且最终性能更高，表明剪枝起到了正则化作用，防止过拟合。
- **模型压缩效果显著**：
  - 最终模型参数减少超过 **50%**（具体数值未给出，但从结构推断）；
  - 推理延迟大幅下降，适用于机载实时决策。

### 与基线方法的对比结果
| 方法 | 平均 Test Reward | 是否保障稳定性 | 是否收敛 | 适用性 |
|------|------------------|----------------|----------|--------|
| Pruning-based PPO | ✅ **8.0**（最高） | ✅ 是 | ✅ 快速收敛 | ✅ 适合边缘部署 |
| Standard PPO | ~7.6 | ✅ 是 | ✅ 收敛较慢 | ❌ 计算开销大 |
| Greedy | ~4.5 | ❌ 否（常违反延迟约束） | ⭕ 局部震荡 | ⭕ 简单但不可靠 |
| Random | ~2.3 | ❌ 否 | ❌ 不收敛 | ❌ 无实际意义 |

> 图 3 和图 6 显示，无论用户数或 UAV 数变化，所提方法始终取得最优 reward，验证其鲁棒性和可扩展性。

### 消融实验结果（Ablation Study）
- **剪枝起始轮次**（pruning start epoch, `t₀`）的影响（见图 4）：
  - `t₀ = 50`（早剪枝）→ 最优性能（reward ≈ 8.2）
  - `t₀ = 200`（中剪枝）→ 性能最差（干扰学习过程）
  - `t₀ = 300`（晚剪枝）→ 次优（保留初期密集探索能力）
- **结论**：**早期结构干预更有利于形成稀疏而高效的策略**，兼具正则化与压缩优势。

---

## 4. 关键结论和发现

### 主要发现
1. **控制稳定性可通过通信资源调控来保障**：通过 Lyapunov 分析导出的延迟边界可有效指导资源分配，确保物理系统稳定。
2. **Stackelberg 博弈是实现激励兼容的有效工具**：UAV 定价机制能自然引导用户行为，实现稳定与效用的平衡。
3. **结构化剪枝不仅压缩模型，还能提升泛化能力**：动态剪枝相当于一种正则化，帮助 agent 更快聚焦关键特征，避免陷入局部噪声。
4. **所提框架可在动态负载下快速逼近 Stackelberg 均衡**：即使在用户数量剧烈波动的情况下，系统仍能维持高效运行。

### 方法的局限性
1. **假设线性控制模型**：实际 UAV 动力学可能高度非线性，当前 Lyapunov 分析基于 LTI 近似，适用范围有限。
2. **集中式训练、分布式执行**（CTDE）依赖全局信息收集用于训练，可能存在隐私与通信瓶颈。
3. **未考虑多跳中继或集群协作**：当前模型聚焦单跳 A2G 链路，复杂拓扑下的扩展性有待验证。
4. **仿真环境理想化**：未模拟极端天气、强干扰或多源异构数据流等现实挑战。

### 未来工作方向
1. **研究时变能量与带宽联合波动下的复合延迟建模**；
2. **深化双向控制-通信协同机制**，例如将控制误差反馈用于动态调制通信调度；
3. **拓展至 multi-leader multi-follower Stackelberg 游戏**，支持 UAV 协同竞争；
4. **集成语义通信或生成式 AI 技术**，进一步降低关键信息传输开销。

---

> ✅ **总体评价**：本文成功打通了 **control theory** 与 **network resource management** 的壁垒，提出了首个 **Lyapunov stability-aware** 的 **DRL-based resource allocation** 框架，并通过 **pruning-based lightweight learning** 实现了理论与工程落地的双重突破，对构建可靠、智能的低空经济生态系统具有重要意义。

</details>

---

### 12. [HyLRA: Hybrid Layer Reuse Attention for Efficient Long-Context Inference](https://arxiv.org/abs/2602.00777)

**Authors**: Xuan Ai, Qingqing Yang, Peng Wang, Lei Deng, Lin Zhang, Renhai Chen, Gong Zhang  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.00777v1  

#### Abstract
Long-context inference in Large Language Models (LLMs) is bottlenecked by the quadratic computation complexity of attention and the substantial memory footprint of Key-Value (KV) caches. While existing sparse attention mechanisms attempt to mitigate this by exploiting inherent sparsity, they often r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：HyLRA: Hybrid Layer Reuse Attention for Efficient Long-Context Inference**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决的问题**
大型语言模型（LLMs）在长上下文推理中面临两个核心瓶颈：
- **Attention 的二次计算复杂度**：随着序列长度增长，self-attention 的计算开销呈 $O(n^2)$ 增长。
- **KV Cache 的内存压力**：Key-Value 缓存随上下文线性增长，导致高内存占用和带宽瓶颈。

现有稀疏注意力机制（如固定窗口、激进缓存淘汰）往往在效率与准确性之间难以平衡，且多数方法对所有层采用统一策略，忽略了不同网络深度上的结构异质性。

---

### 🚀 **提出的新方法与新思路**
作者提出 **HyLRA（Hybrid Layer Reuse Attention）**，一种基于**层间相似性分析**的混合注意力框架，其核心思想来自两个关键观察：

#### 🔍 **两大洞察（Insights）**
1. **Intra-layer Sensitivity（层内敏感性）**  
   - 不同 Transformer 层对稀疏化误差的容忍度差异显著：
     - **敏感层（Sensitive Layers）**：早期或关键层对特征扰动高度敏感，需保留 full attention 以防止信息失真。
     - **容忍层（Tolerant Layers）**：深层对 top-k 近似鲁棒性强，可安全使用稀疏注意力。

2. **Inter-layer Similarity（层间相似性）**  
   - 相邻层之间的 top-k 高注意力 token 具有高度重叠（尤其在深层），表明可以跨层复用重要 token 的索引。

#### 💡 **HyLRA 的设计**
结合上述两点，HyLRA 提出一个**混合策略**：
- 在**敏感层**执行标准 full attention，并提取 top-k indices；
- 在**容忍层**直接**复用前一层的 top-k indices**，跳过重复的重要性评分与排序过程，从而规避二次计算。

该策略通过 **offline 动态规划（Dynamic Programming）** 求解最优层配置路径，在最小化 full attention 调用次数的同时最大化累计相似度。

---

### ⭐ **相比现有方法的优势**
| 方面 | HyLRA | 现有方法（如 Jump3、H2O、StreamingLLM） |
|------|-------|-------------------------------|
| **精度保持** | 更优，仅 <1% 准确率下降 | 易因激进剪枝或固定模式丢失关键信息 |
| **效率提升** | 显著减少冗余计算（避免每层重新选 token） | 多数仍需每层独立打分/排序，实际加速有限 |
| **灵活性** | 自适应决定哪些层 full / reuse | 多为静态规则（如每隔3层刷新） |
| **兼容性** | 支持 token-level 和 block-level sparsity，适配硬件优化 | 多局限于特定实现 |

> ✅ **本质优势**：将“稀疏注意力”从**逐层独立决策**转变为**跨层协同调度**，实现了更高效的计算资源分配。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**
- **LongBench v2**：综合性长上下文评测基准，涵盖六类任务：
  - Single-document QA
  - Multi-document QA
  - Long-dialogue History Understanding
  - Long Structured Data Understanding
  - Long In-context Learning
  - Code Repository Understanding
- 上下文长度覆盖 **short (~8K)** 到 **long (~60K tokens)**，测试模型在多样场景下的表现。

---

### 🧪 **实验设置与评估指标**

#### ✅ 模型
- **DeepSeek-R1**：高性能推理导向模型
- **QWQ-32B-W8A8**：量化版本（8-bit weights & activations），用于验证内存受限环境下的有效性

#### ✅ 硬件平台
- 8块加速器设备，共 768GB HBM3 内存（96GB/device）

#### ✅ 基线方法（Baselines）
| 方法 | 描述 |
|------|------|
| **Full Attention** | 标准 dense attention，作为性能上限 |
| **Jump3** | 静态版 HyLRA 变体，每3层执行一次 full attention，其余复用索引 |

> 注：Jump3 是 HyLRA 的简化对照，用于验证动态规划策略的有效性。

#### ✅ 评估指标
| 指标 | 含义 |
|------|------|
| **Performance Score** | LongBench v2 上的任务准确率（Overall / Easy / Hard / 不同 context length 分段） |
| **Efficiency Metrics** | 
| - AVG_TTFT | Average Time To First Token |
| - Generation Throughput | 生成阶段吞吐量（tokens/s） |
| - Overall Throughput | 端到端吞吐量 |
| - Speedup | 相对于 Full Attention 的加速比 |

---

## 3. **主要实验结果和性能指标**

### 📊 **关键性能数据汇总**

#### ✅ 表格 1 & 2：LongBench v2 性能对比（部分摘要）

| Model | Method | Overall | Easy | Hard | Short | Medium | Long |
|-------|--------|--------|------|------|-------|--------|------|
| DeepSeek-R1 | Full | 44.33 | 48.44 | 41.80 | 48.89 | 40.00 | 45.37 |
| DeepSeek-R1 | Jump3 | 42.54 | 43.75 | 41.80 | 50.00 | 39.07 | 37.04 |
| DeepSeek-R1 | **HyLRA** | **46.32** | **49.48** | **44.37** | **52.78** | **43.72** | **40.74** |

> 💡 **结论**：
- HyLRA **不仅优于所有 sparse baseline（Jump3）**
- 甚至在多个子任务上**超过 full attention**，说明其能有效过滤噪声并聚焦关键上下文。

#### ✅ 表格 3：效率对比（Sequence Length 从 8K → 60K）

| Seq Len | Method | TTFT (ms) | Gen Throughput | Overall Throughput | Speedup |
|--------|--------|-----------|----------------|--------------------|---------|
| 8K     | Full      | 16.81 | 1225 | 11033 | 1.00 |
|        | Jump3     | 15.27 | 1229 | 11068 | 1.15 |
|        | **HyLRA** | **14.85** | **1145** | **10309** | **1.06** |
| 60K    | Full      | 57.69 | 177 | 2835 | 1.00 |
|        | Jump3     | 53.98 | 273 | 4730 | 1.54 |
|        | **HyLRA** | **56.62** | **258** | **4128** | **1.45** |

> 🔺 **趋势发现**：
- 随着 sequence length 增加，HyLRA 的 speedup 持续上升（最高达 **1.45× 整体加速**）
- 尽管 TTFT 接近 full attention，但在长序列下生成吞吐大幅提升，体现其对 KV cache 扫描的优化效果。

---

### 🔍 **消融实验与分析（隐含于主实验中）**
虽然未单独列出 ablation study，但从以下方面可看出设计有效性：
- **Jump3 vs HyLRA**：证明了**动态规划求解最优路径**优于固定跳跃策略；
- **reuse 层数比例控制**：通过 similarity threshold 控制 full/reuse 比例，验证了 trade-off 可调；
- **block-level adaptation**：支持 block-wise sparsity（如 QUEST），显示系统级兼容性。

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **Transformer 层存在显著的 heterogeneity**：
   - 并非所有层都适合稀疏化；**敏感层必须保留 full attention**。
2. **相邻层 attention pattern 高度稳定**：
   - top-k tokens 在连续层间高度重合，为 index reuse 提供理论基础。
3. **HyLRA 实现帕累托前沿突破**：
   - 在 accuracy 几乎无损（<1% 下降）前提下，获得高达 **1.45× 推理加速**，显著优于现有 sparse attention 方法。
4. **适用于量化模型与长序列场景**：
   - 在 QWQ-32B-W8A8 上同样有效，具备部署潜力。

---

### ⚠️ **局限性**
1. **依赖 offline profiling**：
   - 需要预先运行 full attention 获取 attention similarity matrix，增加部署前成本。
2. **假设 attention pattern 稳定性**：
   - 对输入变化剧烈的任务（如跳跃式对话）可能影响 reuse 准确性。
3. **未处理 encoder-decoder 架构**：
   - 当前工作聚焦 decoder-only 模型（如 Llama、Qwen），尚未扩展至 T5 类结构。

---

### 🔮 **未来工作方向**
1. **Online Adaptation Mechanism**：
   - 设计轻量级 online profiler，实时调整 reuse policy，适应动态输入。
2. **Cross-model Generalization**：
   - 探索是否可在一个模型上学得的 policy 迁移到其他架构。
3. **Integration with Other KV Cache Compression Techniques**：
   - 结合 PyramidInfer、ShadowKV 等方法进一步压缩内存。
4. **Support for Training & Fine-tuning**：
   - 将 HyLRA 思想引入训练阶段，探索稀疏化的端到端学习。

---

## ✅ **总结一句话**
> **HyLRA 通过揭示 LLM 中 attention 的层间异质性与稳定性，提出了一种“敏感层全算 + 容忍层索引复用”的混合策略，利用动态规划求解最优执行路径，在几乎不损失精度的前提下实现高达 1.45× 的长上下文推理加速，是当前最高效的 sparse attention 框架之一。**

🔗 开源地址：[/r/unified-cache-management-CF80/](https://github.com/r/unified-cache-management-CF80/)

</details>

---

### 13. [Prism: Efficient Test-Time Scaling via Hierarchical Search and Self-Verification for Discrete Diffusion Language Models](https://arxiv.org/abs/2602.01842)

**Authors**: Jinbin Bai, Yixuan Li, Yuchen Zhu, Yi Xin, Qingyu Shi, Aosong Feng, Xiaohong Liu, Molei Tao, Jianru Xue, Xiangtai Li, Ming-Hsuan Yang  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.01842v1  

#### Abstract
Inference-time compute has re-emerged as a practical way to improve LLM reasoning. Most test-time scaling (TTS) algorithms rely on autoregressive decoding, which is ill-suited to discrete diffusion language models (dLLMs) due to their parallel decoding over the entire sequence. As a result, developi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PRISM: Efficient Test-Time Scaling via Hierarchical Search and Self-Verification for Discrete Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
- **离散扩散语言模型**（dLLMs）在推理时难以有效利用额外计算资源进行**测试时扩展**（Test-Time Scaling, TTS），因为其并行去噪机制与传统的自回归（AR）解码不兼容。
- 现有的 TTS 方法（如 Best-of-N、Tree Search）依赖于外部验证器或序列生成结构，无法直接应用于 dLLMs。
- 简单地对 dLLMs 执行 Best-of-N 搜索会导致高昂的**函数调用次数**（Number of Function Evaluations, NFE），效率低下。

### 提出了什么新方法或新思路
作者提出 **PRISM**（Pruning, Remasking, and Integrated Self-verification Method），一个专为 dLLMs 设计的高效 TTS 框架，包含三大核心组件：

#### （1）分层轨迹搜索**（Hierarchical Trajectory Search, HTS）
- 将整个去噪过程划分为三个阶段：
  - **随机探索**（Stochastic Exploration）：初期保持宽轨迹池（N 条路径），不进行剪枝。
  - **渐进稀疏化**（Progressive Thinning）：在中前期根据**自验证反馈**（SVF）动态剪枝低质量轨迹，并通过局部重掩码分支保留多样性。
  - **最终精炼**（Final Refinement）：仅对少量高质量轨迹（K 条）完成剩余去噪步骤。
- 轨迹数量随时间呈几何衰减，显著降低总 NFE。

#### （2）局部分支与部分重掩码**（Local Branching with Partial Remasking）
- 在高分轨迹上选择**低置信度 token** 进行重新掩码，保留“逻辑骨架”（high-confidence logic skeleton）。
- 从这些部分重掩码的状态继续去噪，实现**多样化实现方案探索**，避免完全重启导致的信息丢失。

#### （3）自验证反馈**（Self-Verified Feedback, SVF）
- 利用**同一 dLLM 自身**作为轻量级二分类验证器，判断中间生成是否可能正确。
- 通过设计特定的 Yes/No 提问模板（见附录 C），让模型输出 `Yes` 或 `No`，并将其概率作为评分依据。
- 避免引入额外的外部奖励模型（External Verifier），节省显存和系统复杂度。

### 相比现有方法的优势
| 维度 | PRISM | Best-of-N | 外部验证方法 |
|------|-------|----------|-------------|
| **计算效率** | ✅ 近似线性增长 $O(N + KT)$ | ❌ 二次增长 $O(NT)$ | ❌ 更高（需额外前向传播） |
| **内存开销** | ✅ 无额外模型 | ✅ 同左 | ❌ 需加载独立验证模型 |
| **适配性** | ✅ 专为 dLLMs 设计 | ⚠️ 可用但低效 | ❌ 不适用于部分掩码状态 |
| **控制信号来源** | ✅ 内生 SVF | ❌ 无排序能力 | ✅ 外部 PRM/ORM |

---

## 2. 核心实验方法和设置

### 使用的数据集
涵盖数学推理与代码生成两大任务：

| 类别 | 数据集 | 描述 |
|------|--------|------|
| **数学推理** | GSM8K | 小学水平数学应用题，需多步符号推理 |
|             | MATH-500 | 500 道竞赛级数学难题 |
| **代码生成** | HumanEval | Docstring 到 Python 函数的生成任务 |
|             | MBPP | 日常编程任务，含自然语言描述与单元测试 |

### 实验设置和评估指标
- **模型**：LLaDA-8B-Instruct、Dream-7B-Instruct、LLaDA-2.0-mini
- **评估方式**：Zero-shot 设置下评估
- **主指标**：
  - 数学任务：Accuracy
  - 代码任务：Pass@1
- **计算成本度量**：
  - **NFE**（Number of Function Evaluations）：衡量去噪步骤总数
  - **SVF Calls**：自验证调用次数（单独统计）

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **N=1** | 单次采样，基础性能基准 |
| **Best-of-N**（N=4,8,16） | 并行生成 N 条独立轨迹，通过多数投票选择最优结果 |
| **ReMDM**（Wang et al., 2025） | 当前最先进的 dLLM 推理优化方法，在 TruthfulQA 上比较 |
| **外部验证器**（Qwen 系列） | 使用外部 LLM 作为验证器，与 SVF 对比效果与资源消耗 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 LLaDA-8B-Instruct 为例）

| 方法 | GSM8K | NFE | MATH-500 | NFE | HumanEval | NFE | MBPP | NFE |
|------|--------|-----|-----------|-----|------------|-----|--------|-----|
| N=1 | 67.58% | 256 | 26.40% | 256 | 54.88% | 512 | 21.80% | 512 |
| Best-of-16 | 87.50% | 4096 | 38.00% | 4096 | 82.32% | 8192 | 35.20% | 8192 |
| **PRISM (K=8)** | **85.30%** | **1048** | **42.80%** | **1304** | **79.27%** | **2480** | **38.20%** | **2576** |

> ✅ **结论**：PRISM 在仅使用约 **1/4 的 NFE** 下，达到甚至超过 Best-of-16 的性能。

### 与基线方法的对比结果
- **效率提升显著**：
  - 在 GSM8K 上，PRISM 达到 85.30% 准确率仅需 1048 NFE，而 Best-of-16 需要 4096 NFE → **提速超 4×**
  - 在 MATH-500 和 MBPP 上，PRISM 常以不到三分之一的预算匹配或超越 Best-of-16
- **性能更强**：
  - PRISM 在多个任务上实现了**绝对性能突破**，例如在 MATH-500 上达到 42.80%，优于 Best-of-16 的 38.00%
- **消融实验支持设计有效性**（详见 Appendix B）：
  - 最佳剪枝窗口为 `[0.1–0.6]T`，太早或太晚剪枝均损害性能
  - 中等衰减速率 `d=1.8` 表现最佳
  - 存活轨迹数 `S=4` 和目标宽度 `K=8` 提供最佳权衡
  - 剪枝间隔 `i=3` 效果最好，过于频繁会过早丢弃潜在好路径

### 与其他 TTS 方法比较
| 方法 | 性能增益 | NFE 开销 | 是否开源 |
|------|---------|----------|----------|
| MEDAL (Huang et al., 2025b) | ~1.4× runtime, +1.4pp | 未公开细节 | ❌ |
| RFG (Chen et al., 2025) | ~9.2% avg gain | ~2× NFE | ❌ |
| **PRISM** | **>10% avg gain** | **~2× NFE** | ✅ 开源 |

### 与外部验证器对比（Table 3）
| 验证器 | GSM8K Pass@1 | 参数量 | 是否额外加载 |
|--------|---------------|--------|--------------|
| SVF (Ours) | 85.30% | 8B | ❌（复用原模型） |
| Qwen-7B | 84.39% | 15B | ✅ |
| Qwen2-7B | 85.98% | 15B | ✅ |
| Qwen3-8B | 87.35% | 16B | ✅ |

> 🔍 **发现**：虽然更大外部验证器略优，但需额外部署且占用更多显存（常超出 40GB A100 容量）。**SVF 在零额外参数下实现接近最优性能**，极具实用性。

---

## 4. 关键结论和发现

### 主要发现
1. **dLLMs 完全可以受益于高效的 TTS**，但需要专门设计的搜索策略。
2. **HTS 架构将计算集中在“逻辑形成期”**（early-to-mid denoising window），大幅提升效率。
3. **SVF 是一种低成本、高效益的内生验证机制**，无需外部模型即可提供可靠的排序信号。
4. **局部重掩码分支有效维持多样性**，防止因剪枝导致的模式坍缩。
5. **PRISM 实现了近线性的计算扩展性**：$C_{HTS} \sim O(N + KT)$，远优于传统 $O(NT)$。

### 方法的局限性
- **依赖高质量 prompt 设计**：SVF 的效果高度依赖于验证提示词的质量。
- **对剪枝策略敏感**：若剪枝窗口设置不当，可能导致早期误删优质路径。
- **当前仅适用于 block-wise dLLMs**：对于全序列并行去噪模型需进一步适配。
- **SVF 可能存在过度自信风险**：在分布外输入上可能出现错误自信判断。

### 未来工作方向
- 将 PRISM 扩展至图像、音频等多模态扩散模型。
- 探索更智能的动态剪枝调度器（如基于不确定性估计自动调整窗口）。
- 结合训练时微调，使模型更适应 SVF 判断任务。
- 研究如何将 PRISM 应用于长文本生成与规划任务。

---

> 📦 **代码已开源**：https://github.com/viiika/Prism  
> 📅 **预印本发布日期**：January 30, 2026

</details>

---

### 14. [EvoOpt-LLM: Evolving industrial optimization models with large language models](https://arxiv.org/abs/2602.01082)

**Authors**: Yiliu He, Tianle Li, Binghao Ji, Zhiyuan Liu, Di Huang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.01082v1  

#### Abstract
Optimization modeling via mixed-integer linear programming (MILP) is fundamental to industrial planning and scheduling, yet translating natural-language requirements into solver-executable models and maintaining them under evolving business rules remains highly expertise-intensive. While large langu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《EvoOpt-LLM: Evolving industrial optimization models with large language models》核心总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
工业级优化建模（如MILP）在实际应用中面临三大挑战：
- **模型构建门槛高**：将自然语言描述的需求转化为可执行的数学规划模型依赖大量专家经验。
- **模型难以动态演化**：业务规则频繁变化时，传统方法需从头重建模型，成本高昂且易出错。
- **模型规模大导致求解效率低**：大规模MILP包含成千上万变量，求解耗时长，传统presolve技术对隐含业务逻辑识别能力有限。

现有LLM方法多聚焦于一次性生成简化模型，缺乏对**全生命周期管理**的支持，尤其在solver-level有效性、动态更新能力和可扩展性方面表现不足。

---

### **提出的新方法与新思路**
作者提出了 **EvoOpt-LLM** ——一个基于LLM的统一框架，支持工业优化模型的**全生命周期自动化管理**，涵盖三个核心模块：

| 模块 | 功能 |
|------|------|
| **Automated Modeling** | 将自然语言需求自动转换为可执行的MILP代码 |
| **Constraint Injection** | 在已有可执行模型中动态注入新的业务约束（如人力限制、分批交付等），保持原目标函数不变 |
| **End-to-End Variable Pruning** | 基于历史数据预测并固定冗余变量为零，实现模型压缩以提升求解效率 |

该框架基于 **7B参数的openPangu-Embedded-7B** 模型，并通过 **LoRA** 进行参数高效微调，仅需少量标注样本即可获得领域专业化能力。

---

### **相比现有方法的优势**
| 维度 | EvoOpt-LLM优势 |
|------|----------------|
| **任务完整性** | 覆盖建模→演化→加速全过程，而非单一生成任务 |
| **数据效率** | 仅用3,000样本即达高性能；关键突破出现在<1,500样本阶段 |
| **solver兼容性** | 输出模型具备高executability（65.9%），远超通用LLM直接生成结果 |
| **动态适应性** | 支持LP-level的结构化编辑，允许增量式模型演进 |
| **计算效率增益** | 变量剪枝F1达~0.56（仅400样本训练），显著优于传统presolve无法捕捉的数据驱动模式 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Automated Modeling模块**：  
  构建了一个包含3,000个样本的fine-tuning数据集，覆盖多种OR问题类型：
  | 类型 | 占比 |
  |-----|------|
  | Mixed Integer Linear Programming | 33% |
  | Linear Programming | 22% |
  | Network Optimization | 12% |
  | Scheduling Optimization | 9% |
  | 其他（Stochastic, Nonlinear等） | 24% |

- **Constraint Injection模块**：  
  基于一个基准供应链优化LP文件，随机采样参数（如setup时间、人力上限、批量单位等），自动生成“原始LP + 自然语言约束 → 扩展后LP”的配对数据。

- **Variable Pruning模块**：  
  利用真实工业调度记录构建小规模等价实例，结合图算法与优化求解器生成高质量剪枝标签（即哪些变量恒为0）。

---

### **实验设置与评估指标**

#### **硬件环境**
- 四张华为Ascend 910B NPU，每张64GB HBM内存
- 支持长上下文输入处理（适用于数百至数千行LP文件）

#### **软件栈**
- CANN 8.1.rc1, PyTorch 2.5.1 + torch_npu, Transformers 4.53.2, vLLM-Ascend
- 使用LoRA进行参数高效微调，分别训练三个任务专用adapter

#### **评估指标**

| 模块 | 主要指标 |
|------|----------|
| **Automated Modeling** | Generation Rate, Executability Rate, Accuracy Rate |
| **Constraint Injection** | 结构一致性、是否保留原目标、新约束正确性（人工+自动验证） |
| **Variable Pruning** | Precision, Recall, F1 Score（针对可安全置零的变量） |

#### **基线方法对比**
文中未明确列出多个外部基线，而是以内置对照为主：
- **Base Model（未微调） vs. LoRA-finetuned Model**：用于验证微调效果
- **不同数据量下的性能曲线**：展示数据效率
- **与传统presolve对比**：强调其无法利用历史行为模式进行智能剪枝

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **Automated Modeling 性能**
| 指标 | 数值（3,000样本） | 数据效率表现 |
|------|--------------------|-------------|
| **Generation Rate** | **91%** | 表示几乎总能输出完整代码 |
| **Executability Rate** | **65.9%** | 生成代码可通过语法解析并被CPLEX/Gurobi执行 |
| **Semantic Accuracy** | **26.37%** | 解出最优解且与标准模型一致的比例 |

> ⚡ **重要发现**：当训练样本少于1,500时已出现**质变跃迁**——executability从2.4%飙升至38.2%，表明LoRA极适合低资源场景下的OR知识注入。

---

#### ✅ **Constraint Injection 效果**
- 微调后的模型能准确识别新增约束语义（如“每个班次最多10人上班”）
- 正确引入辅助变量（如`ym,t`: 机器m在t时段运行）、参数（`Rt`: t时段可用人力）
- 添加形式规范的线性约束（如 `∑m rm·ym,t ≤ Rt`）
- 完全保留原有目标函数与约束结构，无破坏性修改
- 相比未微调模型（仅输出概念性描述，不可执行），fine-tuned版本输出**solver-ready模板**

---

#### ✅ **Variable Pruning 性能**
| 指标 | 数值（400样本训练） |
|------|----------------------|
| **Peak F1 Score** | **~0.56** |
| **最佳适用范围** | ~350–400行LP文件 |
| **Precision & Recall趋势** | 随文件增大而下降 |

> 🔍 在中等规模LP上表现出有意义的剪枝能力，尽管F1未过半，但在仅有400样本下已优于纯规则方法对复杂交互冗余的识别。

---

### **消融实验结果**
虽未设独立消融章节，但以下分析具有消融性质：
- **数据量影响分析**（Fig. 2）：证明性能随数据增加单调上升，且存在临界点（~1,200–1,500样本）
- **上下文长度影响**（Fig. 5）：显示当前模型在超过~450行后性能骤降，揭示**context window限制**是主要瓶颈
- **post-processing修复机制作用**：轻量级规则修复提升了executability，说明端到端生成仍需辅助校正

---

## **4. 关键结论和发现**

### **主要发现**
1. **LoRA微调可在极低数据量下赋予LLM强大的OR建模能力**，尤其在<1,500样本时即实现从“不可执行”到“部分可用”的跨越。
2. **优化模型应被视为可编辑对象**，EvoOpt-LLM首次系统实现了基于自然语言指令的LP-level结构化编辑（constraint injection），支持工业系统的敏捷迭代。
3. **变量剪枝可作为数据驱动的预处理步骤**，超越传统基于代数规则的presolve，捕捉由历史运营形成的隐性业务规律。
4. **三模块形成闭环工作流**：先自动生成baseline模型 → 动态注入新规则 → 剪枝加速求解，构成面向工业部署的完整pipeline。

---

### **方法的局限性**
| 局限 | 描述 |
|------|------|
| **上下文长度限制** | 当前最大序列长度制约了对超大规模LP（>1,500行）的有效处理，F1趋近于0 |
| **泛化能力受限** | 对完全新颖的问题结构或极端复杂的耦合约束仍难以保证正确性 |
| **语义准确性偏低** | 尽管executability达65.9%，但semantic accuracy仅26.37%，说明生成模型可能“跑得通但不对” |
| **依赖高质量标注数据** | 尤其variable pruning需借助求解器生成标签，成本较高 |

---

### **未来工作方向**
1. **发展层次化剪枝策略（Hierarchical Pruning）**：将大模型分解为子结构逐级剪枝，缓解单次推理压力。
2. **增强长上下文建模能力**：探索更适合处理数千行数学表达式的架构或训练范式。
3. **扩大训练数据多样性**：纳入更多行业、更大规模的真实MILP案例，提升鲁棒性。
4. **融合solver反馈机制**：引入test-time RL或self-refinement机制，让模型根据求解失败反向调整模型生成。
5. **构建端到端LLM–Solver协同系统**：使LLM不仅前置建模，也能在求解过程中提供启发式引导。

---

> 💡 **总体评价**：  
> EvoOpt-LLM不是要取代传统optimization solver，而是将其前置的**建模、维护、简化环节智能化**。它标志着LLM在OR领域的应用从“玩具级demo”迈向“工业级工具链”的关键一步，为构建**自进化的企业决策系统**提供了可行路径。

</details>

---

### 15. [Harvest: Opportunistic Peer-to-Peer GPU Caching for LLM Inference](https://arxiv.org/abs/2602.00328)

**Authors**: Nikhil Gopal, Kostis Kaffes  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.00328v1  

#### Abstract
Large Language Model (LLM) inference is increasingly constrained by GPU memory capacity rather than compute throughput, driven by growing model sizes and the linear growth of the key-value (KV) cache during autoregressive decoding. Existing approaches mitigate memory pressure by offloading model sta...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Harvest: Opportunistic Peer-to-Peer GPU Caching for LLM Inference*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLM）推理过程中，**GPU 内存容量**已成为主要瓶颈，而非计算吞吐量。随着模型规模扩大以及自回归解码中 Key-Value (KV) 缓存的线性增长，单个 GPU 的高带宽内存（HBM）常常不足以容纳模型权重、KV cache 和运行时元数据。

现有解决方案如将状态卸载到主机 DRAM（通过 PCIe），虽然能缓解内存压力，但受限于 PCIe 带宽，导致显著延迟；而多 GPU 分片则增加硬件成本和通信开销。

### 🚀 提出的新方法与创新思路
本文提出 **Harvest** —— 一种**机会主义的 peer-to-peer GPU 缓存管理框架**，其核心思想是：

- 将服务器内其他空闲 GPU 上的 HBM 视为一个**临时缓存层（transient cache tier）**；
- 利用高速 **NVLink** 进行 GPU 间数据传输，替代低速 PCIe；
- 缓存对象可被动态回收（revoked），系统设计上不依赖该缓存层的持久性，确保正确性。

#### 主要贡献：
1. **提出了一种新的 placement model**：将远程 GPU 内存暴露为可动态利用的缓存层级。
2. **构建了一个自适应运行时系统（Harvest Runtime）**：无需修改模型代码即可实现对缓存分配、迁移和撤销的自动管理。
3. **实证验证了 peer-memory caching 在 LLM 推理中的有效性**：在 MoE 模型专家权重和 KV cache 两个典型场景中进行了评估。

### 🔍 相比现有方法的优势
| 维度 | 现有方法（如 CPU offload） | Harvest |
|------|--------------------------|--------|
| 数据路径 | PCIe（~12–32 GB/s） | NVLink（可达数百 GB/s） |
| 延迟 | 高（受 PCIe 带宽限制） | 显著降低（最高达 10×） |
| 正确性保障 | 依赖主机 DRAM | 不依赖 peer 缓存，保留 fallback 路径 |
| 成本 | 无需额外硬件 | 充分利用已有闲置 GPU 资源 |
| 动态适应性 | 弱 | 支持动态内存可用性变化 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集与模型
- **MoE 模型**：
  - `Mixtral-8x7B-Instruct-v0.1`（Mistral AI）
  - `Qwen2-MoE`（Alibaba）
  - `Phi-3.5-MoE-instruct` 和 `Phi-tiny-MoE-instruct`（Microsoft）
- **KV Cache 测试模型**：
  - `DeepSeekV3`
  - `Mistral-Large-3-675B-Base-2512`
  - `Kimi-K2-Instruct-0905`

测试 prompts 来源于 **MTBench 数据集**，用于模拟真实对话负载。

### ⚙️ 实验设置
- **硬件平台**：Microsoft Azure NC80adis H100 v5 VM
  - 2 × NVIDIA H100 GPUs
  - 12 × NVLink links（提供高带宽 P2P 互联）
  - PCIe 5.0（用于 CPU ↔ GPU 通信）
- **软件基础**：
  - MoE 实验基于 **MoE-Lightning** 框架 + CGOPipe 执行流水线
  - KV Cache 实验集成至 **vLLM** 系统，扩展其 KV 管理模块

### 📈 评估指标
| 指标 | 描述 |
|------|------|
| **Throughput (tokens/sec)** | 单位时间内生成的 token 数量，衡量端到端推理效率 |
| **Transfer Latency (ms)** | KV cache 或专家权重从远端加载的时间 |
| **Speedup vs CPU Offload** | 相对于传统 CPU 卸载方案的加速比 |
| **Cache Hit Rate / Revocation Impact** | 缓存命中率及失效后回退机制的影响 |

### 🔁 基线方法对比
- **CPU Offload (Baseline)**：将专家权重或 KV cache 存储在 host DRAM，按需通过 PCIe 加载
- **No Offload**：全部驻留本地 HBM（仅适用于小模型）
- **Harvest (Ours)**：使用 peer GPU HBM 作为中间缓存层，通过 NVLink 加载

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ MoE 专家权重卸载结果（图5 & 图6）
| 模型 | 吞吐提升（vs CPU） | 最大加速比 |
|------|--------------------|-----------|
| Mixtral-8x7B | ~55% | 1.55× |
| Qwen2-MoE | ~48% | 1.48× |
| Phi-3.5-MoE | **~110%** | **2.1×** |

> **说明**：Phi-3.5-MoE 表现最优，因其具有更少的专家数和更高的时间局部性（temporal locality），使得缓存复用率更高。

- 当 **50% 专家被强制卸载**时，Harvest 平均带来 **1.5–2.0× 的吞吐提升**。
- 随着卸载比例上升（0% → 100%），CPU 卸载性能急剧下降，而 Harvest 几乎保持稳定：
  - 如 Qwen2-MoE：CPU 方案从 ~975 → 810 tokens/s；Harvest 维持在 ~975 tokens/s。

#### ✅ KV Cache 卸载结果（图7）
| 模型 | KV 条目数量 | Transfer Latency Speedup (GPU vs CPU) |
|------|------------|---------------------------------------|
| Kimi-K2 | 100 → 8000 entries | **5.42× → 5.68×** |
| Mistral-Large-3 | 100 → 8000 entries | **3.0× → 5.65×** |
| DeepSeekV3 | 中等规模 | ~4–5× |

> 随着序列长度增加，KV cache 大小增长，PCIe 成为严重瓶颈，而 NVLink 的优势愈发明显。

---

### 🔍 消融实验分析（文中隐含）
尽管未明确列出“ablation study”章节，但从以下观察可推断关键因素影响：

| 因素 | 发现 |
|------|------|
| **Temporal Locality** | 对缓存收益影响巨大。专家重用率高的模型（如 Phi-3.5-MoE）增益更大 |
| **Working Set Churn** | 专家激活模式越分散（如 Qwen2-MoE），缓存效果略弱但仍显著 |
| **Peer Memory Availability** | 只要有部分 GPU 存在空闲内存（见图2：约68%机器使用 ≤20% GPU内存），即可有效启用 Harvest |
| **Revocation Handling** | 回调机制能平滑处理缓存失效，fallback 至 host DRAM 不引发崩溃或错误 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **大量生产环境中存在 GPU 内存碎片化现象**：如 Alibaba 集群数据显示，超过 **68% 的节点 GPU 内存利用率低于 20%**，表明存在可观的“内存 Slack”可供利用。
2. **NVLink 提供足够高的带宽和低延迟**：相比 PCIe，GPU→GPU 传输延迟降低 **7.5× 到 9.5×**，使其成为理想的中间缓存通道。
3. **Harvest 显著提升了 LLM 推理吞吐**：
   - MoE 场景下：**提升 1.5–2.0× 吞吐**
   - KV Cache 场景下：**减少最多 5.68× 的 reload 延迟**
4. **缓存的“非权威性”设计是关键**：允许缓存丢失并重建（lossy caching）或 fallback 到 host DRAM，使系统既能享受高性能又能保证正确性。

### ⚠️ 局限性
1. **实验规模有限**：目前仅在双 GPU NVLink 系统上验证，尚未测试大规模 NVSwitch 架构下的表现。
2. **未考虑 NVLink 拥塞情况**：当多个任务并发使用 NVLink（如模型并行训练），可能影响 Harvest 的实际带宽。
3. **缺乏通用页面替换策略优化**：当前采用简单 best-fit 分配策略，未针对不同 workload 自适应调整替换算法。
4. **拓扑感知不足**：未结合物理连接拓扑进行 placement 优化，可能导致跨 switch 通信开销。

### 🔮 未来工作方向
1. **扩展至 NUMA-style 共享内存池架构**：
   - 将整个集群 GPU 内存视为非均匀共享资源池（local HBM, peer HBM, host DRAM, CXL memory）
   - 实现基于访问代价的智能 placement 与 migration
2. **支持 JIT 编译优化**：
   - 探索 JAX 等支持动态内存的框架，以恢复因动态 offloading 失去的 CUDA Graphs 优化
3. **开发自适应页面替换策略**：
   - 基于 profiling 数据学习热点块
   - 设计滑动窗口机制动态切换替换策略（LRU/LFU/Hawkeye-like）
4. **增强拓扑与干扰感知能力**：
   - 结合 NVLink 拓扑信息最小化传输距离
   - 引入带宽预算控制避免与其他 tenant 冲突
5. **探索跨机柜（rack-scale）Harvesting**：
   - 利用 NVIDIA NVSwitch 支持的 72-GPU 全连接拓扑进一步放大缓存池容量

---

## 总结
> **Harvest 通过将闲置 peer GPU HBM 转化为高性能临时缓存层，在不影响正确性的前提下，显著缓解了 LLM 推理中的内存瓶颈。其实验结果显示，在 MoE 和 KV cache 卸载场景中均可实现高达 2× 的吞吐提升和近 10× 的延迟降低，展示了“机会主义缓存”在现代 AI 基础设施中的巨大潜力。**

</details>

---

### 16. [AREAL-DTA: Dynamic Tree Attention for Efficient Reinforcement Learning of Large Language Models](https://arxiv.org/abs/2602.00482)

**Authors**: Jiarui Zhang, Yuchen Yang, Ran Yan, Zhiyu Mei, Liyuan Zhang, Daifeng Li, Wei Fu, Jiaxuan Gao, Shusheng Xu, Yi Wu, Binhang Yuan  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.00482v1  

#### Abstract
Reinforcement learning (RL) based post-training for large language models (LLMs) is computationally expensive, as it generates many rollout sequences that could frequently share long token prefixes. Existing RL frameworks usually process these sequences independently, repeatedly recomputing identica...

---

### 17. [Multi-Fidelity Physics-Informed Neural Networks with Bayesian Uncertainty Quantification and Adaptive Residual Learning for Efficient Solution of Parametric Partial Differential Equations](https://arxiv.org/abs/2602.01176)

**Authors**: Olaf Yunus Laitinen Imanov  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.01176v1  

#### Abstract
Physics-informed neural networks (PINNs) have emerged as a powerful paradigm for solving partial differential equations (PDEs) by embedding physical laws directly into neural network training. However, solving high-fidelity PDEs remains computationally prohibitive, particularly for parametric system...

---

### 18. [Grad2Reward: From Sparse Judgment to Dense Rewards for Improving Open-Ended LLM Reasoning](https://arxiv.org/abs/2602.01791)

**Authors**: Zheng Zhang, Ao Lu, Yuanhao Zeng, Ziwei Shan, Jinjin Guo, Lufei Li, Yexin Li, Kan Ren  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.01791v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has catalyzed significant breakthroughs in complex LLM reasoning within verifiable domains, such as mathematics and programming. Recent efforts have sought to extend this paradigm to open-ended tasks by employing LLMs-as-a-Judge to provide sequen...

---

### 19. [Reasoning and Tool-use Compete in Agentic RL:From Quantifying Interference to Disentangled Tuning](https://arxiv.org/abs/2602.00994)

**Authors**: Yu Li, Mingyang Yi, Xiuyu Li, Ju Fan, Fuxin Jiang, Binbin Chen, Peng Li, Jie Song, Tieying Zhang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00994v1  

#### Abstract
Agentic Reinforcement Learning (ARL) focuses on training large language models (LLMs) to interleave reasoning with external tool execution to solve complex tasks. Most existing ARL methods train a single shared model parameters to support both reasoning and tool use behaviors, implicitly assuming th...

---

### 20. [PROBE: Co-Balancing Computation and Communication in MoE Inference via Real-Time Predictive Prefetching](https://arxiv.org/abs/2602.00509)

**Authors**: Qianchao Zhu, Xucheng Ye, Yuliang Liu, Haodong Ouyang, Chengru Song  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00509v1  

#### Abstract
Mixture-of-Experts models have become a dominant architecture for scaling Large Language Models by activating only a sparse subset of experts per token. However, latency-critical MoE inference faces a fundamental tension: while expert parallelism improves memory efficiency, it also amplifies executi...

---

### 21. [sVIRGO: A Scalable Virtual Tree Hierarchical Framework for Distributed Systems](https://arxiv.org/abs/2602.02438)

**Authors**: Lican Huang  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.02438v1  

#### Abstract
We propose sVIRGO, a scalable virtual tree hierarchical framework for large-scale distributed systems. sVIRGO constructs virtual hierarchical trees directly on physical nodes, allowing each node to assume multiple hierarchical roles without overlay networks. The hierarchy preserves locality and is o...

---

### 22. [Generation Order and Parallel Decoding in Masked Diffusion Models: An Information-Theoretic Perspective](https://arxiv.org/abs/2602.00286)

**Authors**: Shaorong Zhang, Longxuan Yu, Rob Brekelmans, Luhan Tang, Salman Asif, Greg Ver Steeg  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00286v1  

#### Abstract
Masked Diffusion Models (MDMs) significantly accelerate inference by trading off sequential determinism. However, the theoretical mechanisms governing generation order and the risks inherent in parallelization remain under-explored. In this work, we provide a unified information-theoretic framework ...

---

### 23. [MiTA Attention: Efficient Fast-Weight Scaling via a Mixture of Top-$k$ Activations](https://arxiv.org/abs/2602.01219)

**Authors**: Qishuai Wen, Zhiyuan Huang, Xianghan Meng, Wei He, Chun-Guang Li  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.01219v1  

#### Abstract
The attention operator in Transformers can be viewed as a two-layer fast-weight MLP, whose weights are dynamically instantiated from input tokens and whose width equals sequence length $N$. As the context extends, the expressive capacity of such an $N$-width MLP increases, but scaling its fast weigh...

---

### 24. [Probing RLVR training instability through the lens of objective-level hacking](https://arxiv.org/abs/2602.01103)

**Authors**: Yiming Dong, Kun Fu, Haoyu Li, Xinyuan Zhu, Yurou Liu, Lijing Shao, Jieping Ye, Zheng Wang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01103v1  

#### Abstract
Prolonged reinforcement learning with verifiable rewards (RLVR) has been shown to drive continuous improvements in the reasoning capabilities of large language models, but the training is often prone to instabilities, especially in Mixture-of-Experts (MoE) architectures. Training instability severel...

---

### 25. [A State-Transition Framework for Efficient LLM Reasoning](https://arxiv.org/abs/2602.01198)

**Authors**: Liang Zhang, Yu Zhao, Longyue Wang, Tianqi Shi, Weihua Luo, Kaifu Zhang, Jinsong Su  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01198v1  

#### Abstract
While Long Chain-of-Thought (CoT) reasoning significantly improves Large Language Models (LLMs) performance on complex reasoning tasks, the substantial computational and memory costs of generating long CoT sequences limit their efficiency and practicality. Existing studies usually enhance the reason...

---

### 26. [WorldCup Sampling for Multi-bit LLM Watermarking](https://arxiv.org/abs/2602.01752)

**Authors**: Yidan Wang, Yubing Ren, Yanan Cao, Li Guo  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01752v1  

#### Abstract
As large language models (LLMs) generate increasingly human-like text, watermarking offers a promising solution for reliable attribution beyond mere detection. While multi-bit watermarking enables richer provenance encoding, existing methods largely extend zero-bit schemes through seed-driven steeri...

---

### 27. [Low-latency Federated LLM Fine-tuning Over Wireless Networks](https://arxiv.org/abs/2602.01024)

**Authors**: Zhiwen Pang, Kang Wei, Long Shi, Zhe Wang, Jun Li, Feng Shu  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01024v1  

#### Abstract
Recently, federated large language models (LLMs) have drawn significant attention thanks to coupled capabilities of LLMs and federated learning (FL) that address privacy concerns in collaborative fine-tuning. However, due to large-scale parameters of LLMs, existing federated LLM fine-tuning framewor...

---

### 28. [Hierarchical Federated Learning with SignSGD: A Highly Communication-Efficient Approach](https://arxiv.org/abs/2602.02355)

**Authors**: Amirreza Kazemi, Seyed Mohammad Azimi-Abarghouyi, Gabor Fodor, Carlo Fischione  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.02355v1  

#### Abstract
Hierarchical federated learning (HFL) has emerged as a key architecture for large-scale wireless and Internet of Things systems, where devices communicate with nearby edge servers before reaching the cloud. In these environments, uplink bandwidth and latency impose strict communication limits, there...

---

### 29. [ELLMPEG: An Edge-based Agentic LLM Video Processing Tool](https://arxiv.org/abs/2602.00028)

**Authors**: Zoha Azimi, Reza Farahani, Radu Prodan, Christian Timmerer  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.00028v1  

#### Abstract
Large language models (LLMs), the foundation of generative AI systems like ChatGPT, are transforming many fields and applications, including multimedia, enabling more advanced content generation, analysis, and interaction. However, cloud-based LLM deployments face three key limitations: high computa...

---

### 30. [SFMP: Fine-Grained, Hardware-Friendly and Search-Free Mixed-Precision Quantization for Large Language Models](https://arxiv.org/abs/2602.01027)

**Authors**: Xin Nie, Haicheng Zhang, Liang Dong, Beining Feng, Jinhong Weng, Guiling Sun  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01027v1  

#### Abstract
Mixed-precision quantization is a promising approach for compressing large language models under tight memory budgets. However, existing mixed-precision methods typically suffer from one of two limitations: they either rely on expensive discrete optimization to determine precision allocation, or int...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
