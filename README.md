# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-18 05:57:44 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Multiscale Aggregated Hierarchical Attention (MAHA): A Game Theoretic and Optimization Driven Approach to Efficient Contextual Modeling in Large Language Models](https://arxiv.org/abs/2512.14925)

**Authors**: Caner Erden  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.14925v1  

#### Abstract
The quadratic computational complexity of MultiHead SelfAttention (MHSA) remains a fundamental bottleneck in scaling Large Language Models (LLMs) for longcontext tasks. While sparse and linearized attention mechanisms attempt to mitigate this, they often compromise the representation of global depen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multiscale Aggregated Hierarchical Attention (MAHA)

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **标准 Multi-Head Self-Attention (MHSA)** 存在 **二次计算复杂度** $O(n^2)$，严重限制了 Large Language Models (LLMs) 在长序列任务中的扩展能力。
- 现有稀疏注意力（如 Longformer、BigBird）或线性化方法（如 Performer）虽然降低了计算开销，但往往牺牲了对全局依赖关系的建模能力，或无法有效捕捉多尺度语义粒度。

### 🆕 提出的新方法：MAHA
提出 **Multiscale Aggregated Hierarchical Attention (MAHA)**，一种基于**分层分解**与**数学优化驱动聚合**的新型注意力架构框架，其核心创新包括：

#### （1）多尺度分层分解（Multiscale Decomposition）
- 将输入序列通过可学习的下采样操作（strided convolution 或 adaptive pooling）动态划分为多个层级（scales），形成金字塔结构。
- 不同层级分别捕获不同抽象级别的上下文信息：细粒度（局部语法）、中等粒度（子句级）、粗粒度（文档主题）。

#### （2）优化驱动的聚合机制（Optimization-Driven Aggregation）
- 引入两种理论严谨的聚合策略，替代传统加权平均等启发式融合方式：
  - **凸优化（Convex Optimization, CO）**：将多尺度输出融合建模为带约束的优化问题，目标是最小化重构误差并鼓励稀疏选择重要尺度。
  - **博弈论纳什均衡（Nash Equilibrium, NE）**：将各尺度视为“玩家”，竞争影响最终表示；聚合权重由非合作博弈的纳什均衡决定，确保无单一尺度可通过单方面改变提升自身效用。

#### （3）混合稀疏卷积主干设计
- 集成 **dilated convolution** 捕捉局部上下文；
- 使用跨尺度门控（cross-scale gating）增强层级间信息流动；
- 整体作为 Transformer 中 MHSA 层的即插即用替代方案。

### 🔍 相比现有方法的优势
| 维度 | MAHA | 现有方法（如 Sparse Attention / Hierarchical Attention） |
|------|------|--------------------------------------------------------|
| **计算效率** | 近线性复杂度 $O(n)$，显著降低 FLOPs 和内存占用 | 多数仍存在局部二次项或固定模式瓶颈 |
| **上下文建模** | 显式保留多尺度语义，兼顾局部细节与全局一致性 | 聚合方式简单，易导致信息稀释 |
| **理论基础** | 基于凸优化或博弈论，提供最优性保证 | 多为经验性设计，缺乏理论支撑 |
| **灵活性** | 可适配多种 Transformer 架构，兼容现有训练流程 | 往往需特定结构调整 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验覆盖四类典型 NLP 任务，涵盖长短上下文场景：

| 任务 | 数据集 | 特点 |
|------|-------|------|
| 文本分类 | **GLUE**（MNLI, SST-2） | 标准理解任务，测试通用表征能力 |
| 长距离依赖建模 | **PG-19**（>4k tokens） | 超长文本语言建模，考验远距依赖捕捉 |
| 机器翻译 | **WMT14 En-De** | 序列到序列生成任务 |
| 问答系统 | **SQuAD v2.0** | 上下文阅读理解，要求精准定位 |

### ⚙️ 实验设置
- **模型架构**：12层 Transformer，隐藏维度 768，12 个 attention head
- **训练参数**：
  - Batch Size：32（分类/QA），16（翻译）
  - 学习率：$5 \times 10^{-5}$，warmup 10k 步
- **序列长度**：
  - 分类/问答：512
  - PG-19（长文本）：4096
- **MAHA 参数配置**：
  - 层数 $L=4$（token 数分别为 32, 64, 128, 256）
  - 下采样核大小 kernel=3
  - 聚合正则化系数 $\lambda=0.1$

### 🆚 基线方法对比
| 基线模型 | 类型 |
|--------|------|
| Standard MHA (Vaswani et al., 2017) | 全注意力基准 |
| Longformer (Beltagy et al., 2020) | 局部+全局稀疏注意力 |
| BigBird (Zaheer et al., 2020) | 局部+随机+全局稀疏 |
| Reformer (Kitaev et al., 2020) | LSH-based 注意力 |
| Performer (Choromanski et al., 2020) | 核函数近似线性注意力 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| Model | MNLI (Acc) | SST-2 (Acc) | PG-19 (PPL↓) | WMT (BLEU) | SQuAD (F1) | Memory (GB)↓ | Throughput (seq/s)↑ |
|-------|------------|-------------|---------------|------------|------------|----------------|------------------------|
| Standard MHA | 86.2 | 93.5 | 24.3 | 28.7 | 88.4 | 15.2 | 42 |
| Longformer | 85.7 | 92.8 | 23.8 | 27.9 | 87.6 | 9.1 | 58 |
| BigBird | 85.9 | 93.1 | 23.5 | 28.1 | 87.9 | 10.3 | 53 |
| Reformer | 84.3 | 91.7 | 25.6 | 26.4 | 85.2 | 7.8 | 62 |
| Performer | 85.1 | 92.4 | 24.9 | 27.3 | 86.7 | 8.5 | 67 |
| **MAHA** | **86.0** | **93.3** | **23.1** | **28.5** | **88.2** | **2.5** | **71** |

> 注：Memory 和 Throughput 中 MHA/MAHA 为本地 GPU 测量值，其余来自原论文。

### 🔍 性能分析要点
- **精度表现**：MAHA 在所有任务上均达到与标准 MHA 相当甚至更优的性能（尤其在 PG-19 上 PPL 最低为 **23.1**）。
- **内存效率**：相比标准 MHA 内存从 15.2GB 降至 **2.5GB**，减少约 **83.5%**。
- **吞吐量**：推理速度达 **71 seq/s**，高于所有基线，适合高并发部署。
- **FLOPs 分析**（图3）：
  - 在 $n=4096$ 时：
    - Standard MHA：~16.8M FLOPs
    - MAHA：~3.2M FLOPs → **降低 81%**
  - 随序列增长，优势呈指数扩大。

### 🔬 消融实验结果

#### （1）聚合策略比较（Table 2）
| 方法 | MNLI (Acc) | Memory (GB) | 训练速度 |
|------|------------|--------------|----------|
| CO（凸优化） | 86.0 | 6.7 | 1.0x |
| NE（纳什均衡） | 85.8 | 6.9 | 0.9x |
| 平均融合（Baseline） | 85.2 | 7.2 | 1.1x |

- **发现**：CO 与 NE 均优于简单平均，且 CO 更高效；NE 收敛后损失更低（图4），表明博弈机制可能找到更鲁棒的平衡点。

#### （2）层级数量影响（图5）
- $L=4$ 时性能最佳（MNLI 86.0）
- $L=2$：粒度过粗，丢失细节 → 准确率下降至 84.5%
- $L=6$：过度下采样引入噪声 → 性能退化至 84.8%

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MAHA 成功将注意力复杂度从 $O(n^2)$ 降至近线性 $O(n)$**，在保持甚至提升性能的同时大幅提高可扩展性。
2. **优化驱动的聚合机制（CO/NE）优于传统加权平均**，提供了更强的理论保障和动态适应能力。
3. **多尺度注意力可视化显示明确分工**：
   - 细尺度关注局部语法（对角密集）
   - 中尺度形成块状结构（句子内聚合）
   - 粗尺度出现垂直条带（关键词全局追踪）
4. **适用于长上下文建模范畴**（如书籍、科学文献、代码），是下一代 LLM 的有力候选架构。

### ⚠️ 局限性
- 需调参较多（如层级数 $L$、压缩比 $r$），可能需领域定制。
- NE 聚合训练开销略高（迭代求解），不如 CO 快速闭式解。
- 假设语言具有天然层次结构，在高度非组合性或跳跃引用文本中可能受限（Liu et al., 2021）。

### 🔮 未来工作方向
- 扩展至其他模态（Computer Vision、Speech Processing），利用其天然多分辨率特性。
- 探索联邦学习场景下的分布式优化聚合。
- 结合 MAHA 的可解释性潜力，开发透明决策路径分析工具。
- 开源实现已发布：[GitHub](https://github.com/can-ererden/MAHA-Project)，DOI: `10.5281/zenodo.17936753`

---

> **总结一句话**：  
> MAHA 通过**分层分解 + 优化聚合**，实现了**高效、可扩展、理论严谨**的注意力机制，在不牺牲性能的前提下将计算成本降低 80% 以上，为构建下一代长上下文 Large Language Models 提供了坚实的技术路径。

</details>

---

### 2. [DEER: Draft with Diffusion, Verify with Autoregressive Models](https://arxiv.org/abs/2512.15176)

**Authors**: Zicong Cheng, Guo-Wei Yang, Jia Li, Zhijie Deng, Meng-Hao Guo, Shi-Min Hu  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.15176v1  

#### Abstract
Efficiency, as a critical practical challenge for LLM-driven agentic and reasoning systems, is increasingly constrained by the inherent latency of autoregressive (AR) decoding. Speculative decoding mitigates this cost through a draft-verify scheme, yet existing approaches rely on AR draft models (a....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DEER: Draft with Diffusion, Verify with Autoregressive Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **speculative decoding** 方法普遍依赖于 **autoregressive (AR) drafters** 来生成候选序列，但这类方法存在两个根本性瓶颈：
1. **逐步不确定性累积（step-wise uncertainty accumulation）**：由于 AR 模型是逐 token 生成的，早期预测误差会通过左到右的依赖关系不断传播和放大，导致与目标模型的信任逐渐“崩溃”（gradual collapse of trust），从而限制了可接受的 draft 长度。
2. **固有的串行解码效率低下**：AR drafters 必须顺序生成 token，无法并行化，制约了整体加速潜力。

这两个因素共同导致现有方法（如 EAGLE-3）的实际加速效果有限。

---

### 提出的新方法与创新思路
本文提出 **DEER** —— 一种全新的 speculative decoding 框架，其核心思想是：
> **用 diffusion LLM (dLLM) 作为 drafter 进行并行起草，用 AR 模型进行验证（draft with diffusion, verify with autoregressive models）**

#### 主要创新点包括：

- ✅ **首次完全基于离散空间 dLLM 的 speculative decoding 框架**  
  DEER 是首个仅使用 **discrete-space diffusion LLM** 作为 drafter 的框架，无需辅助 AR 模型或混合架构，避免了 AR drafters 的串行依赖。

- ✅ **提出 Diffusion-to-AR (D2A) 对齐训练管道**  
  为解决标准 dLLM 在 prefix-conditioned 续写任务中分布不匹配的问题，设计了两阶段训练策略：
  1. **Stage I: AR-Style Continuation Distillation**  
     使用截断的教师答案 + `[SEP]` 标记进行蒸馏训练，使 dLLM 学习从给定前缀生成后缀的能力。
  2. **Stage II: Scribe Refinement**  
     采用指数衰减加权的 suffix masking 策略，在靠近验证边界的区域增强局部准确性，提升 token 级稳定性。

- ✅ **实现单步长块生成（one-step block drafting）**  
  利用 dLLM 的全局去噪能力，一次性生成长达数十个 token 的完整后缀块，彻底消除 left-to-right 依赖。

- ✅ **发现“可靠块再生”（reliable block regeneration）新能力**  
  训练后的 dLLM 能够反复接受部分掩码的后缀并连贯地重建它们，展现出超越传统生成模式的灵活性。

---

### 相比现有方法的优势
| 维度 | DEER | EAGLE-3 / 其他 AR-based 方法 |
|------|------|-----------------------------|
| **生成机制** | 并行去噪，整块生成 | 串行自回归生成 |
| **错误传播** | 无逐步累积（proposal 独立于历史 draft） | 明显累积，越往后越不稳定 |
| **最大接受长度** | 高达 **32 tokens** | 通常不超过 **8–10 tokens** |
| **速度提升** | 最高 **5.54×** | 最高约 **2.41×** |
| **训练成本** | 更低（见 Table 8） | 更高，且在大模型上易 OOM |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 代码生成任务：
- **HumanEval**, **MBPP**, **LiveCodeBench**, **CodeAlpacaPy**（Python 子集）
- 训练数据：**OpenCodeInstruct**

#### 数学推理任务：
- **GSM8K**, **Math500**, **Minerva Math**
- 训练数据：**UltraChat**, **ShareGPT**

---

### 实验设置

- **基础模型系列**：Qwen3-4B, Qwen3-8B, Qwen3-14B, Qwen3-30B-A3B
- **dLLM drafter 架构**：
  - 代码任务：基于 Open-dLLM 修改的 0.5B 参数 discrete dLLM
  - 数学任务：将 Qwen2.5-0.5B-Instruct 改造为 diffusion 模型后微调
- **温度设置**：主实验使用 `temperature=0` 和 `temperature=0.6`
- **硬件平台**：8×NVIDIA A100 (80GB)

---

### 评估指标

| 指标 | 定义 |
|------|------|
| **Speedup Ratio** | 相对于标准 AR 解码的端到端推理速度提升倍数 |
| **Average Acceptance Length (T)** | 每次 draft-verify 循环中平均被接受的 token 数量，反映有效并行度 |
| **Maximum Accepted Length** | 单次成功接受的最长 draft 长度 |

> 注：由于 DEER 使用 rejection sampling 保证输出分布一致性，因此所有方法均保持原始模型精度不变，故不报告 accuracy 类指标。

---

### 基线方法对比
- **EAGLE-3**：当前最先进的 AR-based speculative decoding 方法
- **Medusa**, **Hydra**：基于树结构预测的 AR drafters
- **Lookahead**, **DiffuSpec**：轻量级 heuristic 或 n-gram 匹配方法（文中简要提及）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Qwen3-30B-A3B, temp=0 为例）

| 方法 | Speedup | T (Avg. Acceptance Length) |
|------|---------|----------------------------|
| **DEER** | **5.54×** | **6.58** |
| EAGLE-3 | 2.41× | 3.21 |

> 在 HumanEval 上，DEER 实现了 **超过 2 倍的速度优势**。

---

### 与其他模型规模的全面比较（Table 1 & 2）

| 模型 | 方法 | Speedup | T |
|------|------|--------|----|
| Qwen3-30B-A3B | DEER | **5.54×** | **6.58** |
| | EAGLE-3 | 2.41× | 3.21 |
| Qwen3-14B | DEER | 3.50× | 5.00 |
| | EAGLE-3 | 1.91× | 2.61 |
| Qwen3-8B | DEER | 3.30× | 5.00 |
| | EAGLE-3 | 2.65× | 3.87 |

✅ **趋势一致**：DEER 在所有模型尺度下均显著优于 EAGLE-3，且随着模型增大优势更明显。

---

### 最大接受长度对比（Table 4）

| 模型 | EAGLE-3 | **DEER** |
|------|--------|----------|
| Qwen3-4B | 8 | **32** |
| Qwen3-8B | 8 | **32** |
| Qwen3-14B | 8 | **32** |
| Qwen3-30B-A3B | 7 | **32** |

➡️ DEER 可稳定接受长达 **32 tokens** 的草案，而 EAGLE-3 最多只能接受 7–8 个，差距巨大。

---

### 消融实验结果（Ablation Study）

#### Stage II（Scribe Refinement）的影响（Table 3）

| Benchmark | w/o Refinement | **w/ Refinement** | Δ |
|----------|----------------|--------------------|----|
| MBPP | 4.74 | **4.87** | +0.13 |
| CodeAlpacaPy | 3.47 | **4.04** | +0.57 |
| HumanEval | 5.38 | **6.58** | **+1.20** |
| LiveCodeBench | 3.87 | **5.03** | **+1.16** |

🔹 结果表明：Stage II 显著提升了续写质量，尤其对复杂任务（如 HumanEval）改善最大。

#### 超参数敏感性分析（Figure 6）
- 指数加权系数 α 过大会导致训练不稳定（如 α=1.05 时发散）
- 合理范围内（如 α=1.01~1.02）可稳定优化，说明该模块需精细调参

---

### 批处理扩展性测试（Table 5）

| Batch Size | AR Baseline (tokens/s) | **DEER** (tokens/s) | 加速比 |
|------------|------------------------|---------------------|-------|
| 2 | 34.03 | 82.97 | ~2.4× |
| 8 | 38.35 | 159.87 | **~4.2×** |
| 16 | 49.76 | 175.66 | ~3.5× |

✅ DEER 在批处理场景下表现出良好的可扩展性，尤其在中等 batch size 下加速显著。

---

### 数学推理任务表现（Table 6）

| Benchmark | EAGLE-3 (Speedup/T) | **DEER** (Speedup/**T**) |
|----------|---------------------|---------------------------|
| Math500 | ×1.89 / 2.04 | **×2.12 / 2.45** |
| GSM8K | ×1.92 / 2.43 | **×2.23 / 2.70** |
| Minerva Math | ×1.91 / 2.07 | **×2.02 / 2.31** |
| **Mean** | **×1.91 / 2.18** | **×2.12 / 2.47** |

🔸 即使数学任务上的 dLLM 尚未充分收敛，DEER 仍能提供稳定加速，证明其鲁棒性强。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **dLLM 作为 drafter 可从根本上缓解 AR drafters 的不确定性累积问题**  
   由于 dLLM 是 block-wise 并行生成，每个位置的 proposal 不依赖于之前的 draft token，因此不会放大自身错误。

2. ✅ **DEER 实现了前所未有的长块接受能力（up to 32 tokens）**  
   远超现有方法（≤10 tokens），这是实现高倍加速的关键。

3. ✅ **提出的 D2A 对齐训练策略有效解决了分布错配问题**  
   特别是 Stage II 的指数加权损失显著提升了边界区域的续写稳定性。

4. ✅ **DEER 具备良好泛化性和鲁棒性**  
   在不同模型规模、任务类型（代码 & 数学）、batch size 下均表现优异。

5. 🔮 **揭示了 dLLM 的“可靠块再生”新能力**  
   表明经过特定训练的 dLLM 可以支持灵活的局部编辑与扩展，具有潜在应用价值。

---

### 方法的局限性

- ⚠️ **缺乏成熟的 dLLM 推理框架支持**  
  当前主流系统（如 vLLM, SGLang）尚不支持 dLLM 的 KV cache，影响批处理效率（作者已在附录 F 中指出 Fast-dLLM 和 dInfer 正在推进此方向）。

- ⚠️ **Stage II 训练对超参数较敏感**  
  指数权重系数 α 需谨慎选择，否则可能导致训练不稳定。

- ⚠️ **dLLM 自身训练尚未完全成熟**  
  相比 AR 模型，dLLM 的预训练和微调生态仍在发展中，可能限制其 standalone 性能。

---

### 未来工作方向

1. 🔄 **探索更高效的 dLLM 架构与训练范式**，进一步缩小与 AR 模型的质量差距。
2. 💡 **开发支持 dLLM KV cache 的通用推理引擎**，释放其在 batch inference 中的全部潜力。
3. 🧩 **将 DEER 扩展至更多模态**（如图像、语音），利用 diffusion 模型的跨模态优势。
4. 🤖 **结合 agentic workflows**，利用长块 drafting 提升复杂任务中的规划与执行效率。

---

> **总结一句话**：  
> **DEER 通过引入 diffusion LLM 作为并行 drafter，并设计针对性的对齐训练策略，成功突破了传统 speculative decoding 的串行瓶颈，在多项任务上实现了高达 5.54× 的加速，是迈向高效 LLM 推理的重要一步。**

</details>

---

### 3. [Meta Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN](https://arxiv.org/abs/2512.13715)

**Authors**: Fatemeh Lotfi, Fatemeh Afghah  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.13715v1  

#### Abstract
The increasing complexity of modern applications demands wireless networks capable of real time adaptability and efficient resource management. The Open Radio Access Network (O-RAN) architecture, with its RAN Intelligent Controller (RIC) modules, has emerged as a pivotal solution for dynamic resourc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Meta-Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代无线网络（尤其是基于 **O-RAN** 架构）面临以下挑战：
- **动态性和不可预测性**：用户需求、流量模式和网络条件高度变化，导致传统资源管理策略难以适应。
- **多切片异构服务需求**：eMBB、URLLC 和 mMTC 等不同 **Network Slicing** 类型具有差异巨大的 QoS 要求（如高吞吐、低延迟、海量连接），协调难度大。
- **实时性与可扩展性矛盾**：在大规模分布式环境中实现快速自适应的同时保持系统稳定性与决策粒度。

现有方法（如传统 DRL 或 Federated Learning）往往缺乏对突发场景的快速响应能力，且在非平稳环境下的泛化性能较差。

---

### 🚀 提出的新方法与创新思路

本文提出了一种 **自适应 Meta-Hierarchical Reinforcement Learning (Meta-HRL)** 框架，其核心创新如下：

#### （1）**分层控制架构（Hierarchical Control）**
- **高层控制器（High-level Controller）**：负责跨切片的资源分配（Inter-slice Resource Allocation），作为全局调度器。
- **底层代理（Low-level Agents）**：部署于每个 **DU** 上，执行各切片内的用户级资源块（RB）调度（Intra-slice Scheduling）。
- 分解复杂任务为两级子问题，提升学习效率与可扩展性。

#### （2）**基于 MAML 的元学习机制（Meta-Learning Inspired by MAML）**
- 引入 **Model-Agnostic Meta-Learning (MAML)** 思想，使模型能从历史任务中“学会如何学习”，从而在新任务或新 DU 接入时实现 **few-shot 快速适应**。
- 将每个 DU 视为一个独立的学习任务（task），通过 meta-training 学习通用初始化参数，在新环境下仅需少量样本即可 fine-tune。

#### （3）**自适应加权元更新机制（Adaptive Variance-Weighted Meta Update）**
- 创新地使用 **Temporal Difference Error (TD-error) 方差** 来衡量任务复杂度，并据此动态调整各个任务在 meta-gradient 更新中的权重：
  $$
  w_g = \text{Softmin}(\sigma^2_{\text{TD},g})
  $$
- 高方差任务（即更复杂、波动更大的网络状态）获得更高优先级，确保模型聚焦于最具挑战性的场景。

#### （4）**理论保障与实际部署兼容性**
- 提供了 **收敛性分析** 和 **regret bound** 证明，表明该框架具备 **sublinear 收敛速度** 和稳定的学习行为。
- 设计符合 O-RAN 架构标准，可在 Near-RT RIC 中以 xApp 形式部署，支持分布式、低开销通信。

---

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **适应速度** | 相比 baseline RL 和 meta-RL 方法，**快达 40% 更快适应** 新任务 |
| **性能表现** | 在综合网络管理效率上提升 **19.8%** |
| **鲁棒性** | 对热点区域、突发流量等 corner cases 表现出更强韧性 |
| **公平性与服务质量** | 显著改善 **Jain’s Fairness Index**（从 0.91 → 0.96），降低平均延迟 9.2% |
| **可扩展性** | 支持大规模 DU 扩展，训练成本增长呈 **亚线性趋势** |

---

## 2. 核心实验方法和设置

### 📊 数据集与仿真环境
- **无真实数据集**，采用 **自定义 O-RAN 仿真平台** 进行实验。
- 模拟包含多个 DU、CU、RU 的 O-RAN 网络拓扑，支持三种典型切片：
  - **eMBB**（增强移动宽带）
  - **URLLC**（超可靠低时延通信）
  - **mMTC**（海量机器类通信）

---

### ⚙️ 实验设置

| 参数 | 设置值 |
|------|--------|
| 子载波间隔 | 15 kHz |
| 单个 DU 带宽 | 20 MHz |
| RB 数量 / DU | 100 |
| 用户数 / DU | 30 |
| 分布式单元数量（Ng） | 7（基准）、最高扩展至 30 |
| 总用户数 | 最高达 200 |
| 移动速度 | 10–20 m/s |
| 学习算法 | DDPG（Actor-Critic 架构） |
| 网络结构 | 三层全连接神经网络（256, 512, 512） |
| 优化器 | Adam (lr = 1e-4) |
| 折扣因子 γ | 0.99 |

> 所有结果均经过多次运行取平均，保证统计可靠性。

---

### 🎯 评估指标

| 指标类别 | 具体指标 |
|---------|----------|
| **性能指标** | - 累积奖励（Cumulative Reward）<br>- 吞吐量（Throughput）<br>- 延迟（Latency） |
| **服务质量（QoS）** | - Slice-specific KPI：<br> • eMBB: 平均吞吐<br> • mMTC: UE 密度支持 + 吞吐加权<br> • URLLC: 最大传输延迟 |
| **用户体验（QoE）** | - 用户级吞吐 CDF 分布 |
| **适应能力** | - 不同 shot 数下的适应性能（0-shot, 5-shot, 30-shot） |
| **可扩展性** | - 随 DU 和 UE 数量增加的收敛迭代次数与归一化奖励变化 |
| **公平性与鲁棒性** | - Jain’s Fairness Index<br>- 突发流量下性能下降幅度 |

---

### 🆚 基线方法对比
- **DRL**：从零开始训练的标准 Deep Reinforcement Learning
- **Transfer Learning (TL)**：将已有 agent 的知识迁移到新任务
- **Multi-Task Learning (MTL)**：同时学习多个任务
- **MAML-RL / MAML-HRL**：标准元强化学习及其分层版本（非自适应）
- **Uniform-Meta**：等权 meta-update（用于消融实验）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 指标 | 结果 |
|------|------|
| **网络管理效率提升** | 相比 baseline 提升 **19.8%**（最终 return 值） |
| **累积奖励提升** | 较其他方法高出 **73%**，较 MAML-HRL 提升 **4%** |
| **适应速度提升** | 达到相同性能所需 adaptation shots 减少 **约 40%** |
| **平均延迟降低** | 相比 uniform weighting 下降 **9.2%** |
| **公平性提升** | Jain’s Fairness 从 0.91 提升至 **0.96** |
| **抗突发能力** | 在 50% 流量激增情况下，性能退化 < **5%** |

---

### 📊 与基线方法的对比结果

#### （1）**图4 & 图5：累积奖励比较**
- **Adaptive MAML-HRL** 在所有阶段均显著优于其他方法。
- **DRL** 初始学习慢，收敛差；**TL/MTL** 表现中等但泛化有限。
- 自适应加权机制带来明显增益。

#### （2）**图6 & 图7：QoS 与用户吞吐 CDF**
- 在所有三种切片中，所提方法均达到最优 QoS：
  - **eMBB**：更高吞吐
  - **mMTC**：更好支持高密度设备接入
  - **URLLC**：更低最大延迟
- 用户吞吐分布更集中，说明服务一致性更强。

#### （3）**图8：适应性能随 shot 数变化**
- 在 0-shot 场景下已优于多数 baseline，体现强大先验知识迁移能力。
- 随 shot 数增加，性能迅速上升，尤其在 **mMTC 切片**中表现突出。

#### （4）**图9：跨新任务的 QoS 泛化能力**
- 在三个全新任务中，Adaptive MAML-HRL 始终保持领先，验证其强泛化性。

---

### 🔍 消融实验结果（Ablation Study）

| 方法 | 归一化奖励 | 收敛所需 shots |
|------|------------|----------------|
| Uniform-Meta（等权） | 0.78 | 28 |
| Static-Var（静态方差加权） | 0.81 | 22 |
| **Adaptive-Var（本文方法）** | **0.84** | **17** |

> ✅ 结论：**动态方差加权机制贡献约 3% 的性能增益和 40% 的加速效果**

此外，该机制还带来：
- 延迟减少 9.2%
- 公平性提升（Jain’s Index ↑）
- 抗突发流量能力增强（<5% 性能下降）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Meta-HRL 是解决 O-RAN 动态资源管理的有效范式**：
   - 分层结构 + 元学习结合，兼顾全局协调与本地灵活性。
2. **自适应加权机制显著提升学习效率与鲁棒性**：
   - TD-error 方差作为任务复杂度指标有效引导 meta-update。
3. **方法具备良好的可扩展性与工程可行性**：
   - 在多达 30 个 DU 和 200 用户的大规模场景下仍保持稳定性能。
   - 参数交换开销小（仅传递 actor/critic weights），满足 Near-RT RIC 时延要求（<10ms）。
4. **理论分析支撑实践表现**：
   - 证明了两层级学习系统的 **sublinear convergence** 与 **bounded regret**，增强了可信度。

---

### ⚠️ 局限性
1. **依赖仿真环境**：
   - 当前实验基于模拟器，尚未在真实 O-RAN testbed 上验证（受限于硬件规模）。
2. **计算开销较高**：
   - 虽然通信开销低，但 meta-training 阶段需要大量并行任务采样，训练时间较长。
3. **未考虑跨层干扰或多跳路由**：
   - 当前模型聚焦于单跳 RAN 内部资源调度，未涉及 backhaul/midhaul 或 multi-hop 场景。

---

### 🔮 未来工作方向
1. **Hardware-in-the-loop 验证**：
   - 在开源平台（如 OAI、srsRAN）上部署 xApp 形式的 meta-controller。
2. **通信高效的元参数更新机制**：
   - 探索压缩、量化或稀疏化技术进一步降低 meta-parameter 同步开销。
3. **扩展至 6G 多维场景**：
   - 支持 UAV-assisted networks、multi-hop routing、跨域协同等更复杂架构。
4. **引入不确定性建模**：
   - 结合 Bayesian RL 或 ensemble methods 应对更极端的非平稳环境。

---

## 总结

本文提出的 **Adaptive Meta-HRL** 框架是首个将 **MAML 风格元学习** 与 **Hierarchical RL** 深度融合用于 O-RAN 资源管理的工作。它不仅在性能上超越现有方法近 **20%**，还在 **适应速度、公平性、可扩展性** 等方面展现出全面优势，并提供了坚实的理论支撑。该研究为下一代智能无线网络的自动化运维奠定了重要基础。

</details>

---

### 4. [Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification](https://arxiv.org/abs/2512.14491)

**Authors**: Cheng-Han Lu, Pei-Hsuan Tsai  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.14491v1  

#### Abstract
Transformer-based multi-modal intelligent systems often suffer from high computational and energy costs due to dense self-attention, limiting their scalability under resource constraints. This paper presents SMMT, a sparse multi-modal transformer architecture designed to improve efficiency and robus...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对基于 **Transformer** 的多模态智能系统在阿尔茨海默病（Alzheimer's Disease, AD）分类任务中面临的两个关键挑战：
- **高计算与能耗成本**：传统多模态 Transformer 模型依赖密集自注意力机制（dense self-attention），导致时间复杂度为 $O(n^2)$，难以在资源受限环境下部署。
- **对不完整输入敏感**：临床数据常存在缺失模态（如缺少 MRI 或基因信息），而现有模型缺乏显式机制应对这一现实问题。

### 提出的新方法与创新
作者提出 **SMMT（Sparse Multi-Modal Transformer with Masking）**，一种从系统层面设计的高效、鲁棒的多模态 Transformer 架构，其两大核心创新为：

- **Cluster-based Sparse Attention（基于聚类的稀疏注意力）**  
  引入 K-Means 聚类将 token 分组，在每个簇内进行局部注意力计算，将计算复杂度从 $O(n^2)$ 降低至 $O(n \log n)$，显著提升训练效率。

- **Modality-wise Masking（模态级掩码学习）**  
  在训练过程中随机丢弃某一模态的特征表示（模拟缺失场景），增强模型对不完整输入的泛化能力，提高临床实用性。

### 相比现有方法的优势
相比当前最先进的 **3MT（Cascaded Multi-Modal Mixing Transformer）** 和其他基线模型，SMMT 具有以下优势：
- 显著降低训练时间和内存占用；
- 大幅减少能源消耗和碳排放；
- 在全量和小样本数据下均保持优越性能；
- 更强的鲁棒性和泛化能力，尤其适用于真实世界中模态缺失的场景。

---

## 2. 核心实验方法和设置

### 数据集
实验基于 **ADNI-1 和 ADNI-2** 数据集，仅保留确诊为 AD（Alzheimer’s Disease）或 CN（Cognitively Normal）的个体，排除 MCI（轻度认知障碍）以避免类别不平衡。最终共使用 **12,680 张 T1-weighted 2D MRI 切片**，并与结构化临床元数据对齐。

使用的模态包括：
- **Imaging**：MRI 图像（经 MNI152 配准、归一化为 256×256）
- **Numerical Clinical Scores**：MMSE、CDR、FAQ、Age
- **Categorical Data**：APOE genotype、Sex

### 实验设置
- **框架**：PyTorch
- **硬件**：NVIDIA GeForce RTX 3060 GPU（12GB）
- **训练参数**：
  - Epochs: 50（五折交叉验证共 250 epochs）
  - Batch Size: 8
  - Optimizer: Adam ($\text{lr} = 1\times10^{-3}$)
  - Latent Dimension: 512
  - Masking Ratio: $r = 0.3$

### 评估指标

#### （1）分类性能指标
- Accuracy（准确率）
- Precision（精确率）
- Recall / Sensitivity（召回率 / 灵敏度）
- F1-score
- Specificity（特异性）
- AUC（ROC 曲线下面积）

#### （2）计算可持续性指标（通过 CodeCarbon 工具监测）
- 总能量消耗（kWh）：涵盖 CPU、GPU、RAM
- CO₂ 排放量（kgCO₂）：基于台湾电网碳强度（0.502 kgCO₂/kWh）

### 基线方法对比
| 模型 | 类型 |
|------|------|
| **3MT [8]** | 当前 SOTA 多模态 Transformer（级联跨模态注意力） |
| **ADDFformer [20]** | 基于 Transformer 的单模态 MRI 模型 |
| **FusionNet [21]** | 通用多模态融合网络（无注意力机制） |
| **CNN-only [22]** | 单模态 CNN（如 VGG16） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（100% 数据）

| 模型 | Accuracy (%) | Sensitivity (%) | Specificity (%) | AUC |
|------|--------------|------------------|------------------|-----|
| **SMMT (Ours)** | **97.05** | **96.31** | **97.58** | **0.986** |
| 3MT [8] | 90.28 | 93.64 | 93.81 | 0.965 |
| ADDFformer [20] | 88.20 | 91.87 | 91.53 | 0.948 |
| FusionNet [21] | 94.28 | 91.01 | 93.24 | 0.956 |
| CNN-only [22] | 80.24 | 78.45 | 80.22 | 0.852 |

> ✅ SMMT 在所有指标上均达到最优，尤其在 AUC 上表现突出，表明其具有优秀的判别能力和临床可靠性。

### 小样本场景下的鲁棒性（低数据 regime）

| 数据比例 | SMMT (Ours) | 3MT [8] |
|---------|-------------|--------|
| 20%     | **84.96%**  | 78.92% |
| 40%     | **86.52%**  | 82.17% |
| 60%     | **91.28%**  | 86.25% |

> 🔍 SMMT 在极小样本条件下仍能维持较高精度，展现出卓越的数据效率和泛化能力。

### 训练效率与能耗对比

| 组件 | 3MT (Baseline) | SMMT (Ours) | 减少幅度 |
|------|----------------|------------|----------|
| **GPU Energy** | 0.283977 kWh | 0.159642 kWh | ↓ **43.8%** |
| **CPU Energy** | 0.108489 kWh | 0.071179 kWh | ↓ 34.4% |
| **RAM Energy** | 0.051035 kWh | 0.033485 kWh | ↓ 34.4% |
| **Total Energy** | **0.443501 kWh** | **0.264306 kWh** | ↓ **40.4%** |
| **CO₂ Emissions** | 0.2226 kg | 0.1327 kg | ↓ **40.3%** |

> 💡 能耗降低近一半，等效节省约“为智能手机充电18次”或“点亮60瓦灯泡超30小时”，体现显著的环境可持续优势。

### 消融实验结果（Ablation Study）

| 配置 | 100% Accuracy / Time(min) | 20% Accuracy |
|------|----------------------------|-------------|
| 3MT (Baseline) | 90.28 / 133 | 78.92 |
| w/o Sparse Attention | 91.35 / 147 | 84.85 |
| w/o Masking | 95.42 / 108 | 80.85 |
| **SMMT (Full)** | **97.05 / 112** | **84.96** |

#### 发现：
- 移除 **Sparse Attention** 导致训练时间大幅增加（↑34 min），说明其有效提升效率；
- 移除 **Masking** 后小样本性能明显下降（↓4.11%），验证其正则化与鲁棒性作用；
- 两者结合实现最佳平衡：**高效 + 高性能 + 强泛化**

此外，掩码比率 $r$ 的消融显示：**当 $r=0.3$ 时性能达到峰值**，过高（>0.6）会破坏关键信息，影响收敛。

---

## 4. 关键结论和发现

### 主要发现
1. **SMMT 在保持甚至超越 SOTA 性能的同时，实现了显著的效率提升**：
   - 准确率达 **97.05%**（AD vs. CN）
   - 小样本（20%）下仍达 **84.96%**
   - 训练能耗降低 **40.4%**

2. **稀疏注意力不仅是加速手段，也具备隐式正则化效果**：
   - 限制 token 间无效交互，抑制过拟合，反而有助于提升泛化能力。

3. **模态级掩码是提升鲁棒性的关键设计**：
   - 模拟真实临床中的模态缺失，使模型更具实用价值。

4. **SMMT 特别适合资源受限、数据稀缺的真实医疗场景**：
   - 兼顾高性能、低能耗、高鲁棒性，符合“绿色 AI”与“可扩展智能系统”的发展方向。

### 局限性
- 当前研究聚焦于 **二分类任务（AD vs. CN）**，未包含 MCI（轻度认知障碍）阶段；
- MCI 样本较少且易引发类别不平衡，限制了更细粒度疾病进展建模；
- 所有实验基于单一硬件平台（RTX 3060），尚未在边缘设备或多中心异构环境中验证。

### 未来工作方向
1. 扩展至 **三分类或多阶段预测任务**（CN → MCI → AD），支持纵向建模（longitudinal modeling）；
2. 设计 **adaptive modality selection** 机制，在推理时动态处理可变模态输入；
3. 在更大规模、更多样化的临床数据集上验证泛化能力；
4. 探索 SMMT 在 **Human Digital Twin** 或远程健康监测系统中的集成应用。

---

> 📌 **总结一句话**：  
> SMMT 是一个面向实际临床需求的高效、鲁棒、环保的多模态 Transformer 架构，在 AD 分类任务中实现了性能、效率与可持续性的统一，为资源受限环境下的医学 AI 部署提供了新范式。

</details>

---

### 5. [Dynamic Rebatching for Efficient Early-Exit Inference with DREX](https://arxiv.org/abs/2512.15705)

**Authors**: Xuting Liu, Daniel Alexander, Siva Kesava Reddy Kakarla, Behnaz Arzani, Vincent Liu  
**Category**: cs.DC  
**Published**: 2025-12-18  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.15705v1  

#### Abstract
Early-Exit (EE) is a Large Language Model (LLM) architecture that accelerates inference by allowing easier tokens to be generated using only a subset of the model's layers. However, traditional batching frameworks are ill-suited for EE LLMs, as not all requests in a batch may be ready to exit at the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Dynamic Rebatching for Efficient Early-Exit Inference with DREX**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
传统 **Early-Exit (EE)** 推理架构虽能通过让“简单”token提前退出深层网络来节省计算资源，但在**批处理（batching）场景下存在严重挑战**：
- **Split Decision 问题**：一个 batch 中部分请求满足早退条件，另一些不满足，导致无法统一决策。
- **Grouped Exit 政策的缺陷**：
  - *Consensus*：所有请求都需同意才退出 → 导致大量 **involuntary stays**（本可早退却被迫继续），浪费 EE 机会。
  - *Greedy/Majority*：少数或多数同意即全体退出 → 导致 **involuntary exits**（本应继续却被迫退出），严重损害输出质量。
- **Missing KV Cache 问题**：早退 token 跳过了深层，其 KV Cache 在后续解码中缺失，影响自回归推理。

这些问题使得 EE 在实际部署中难以兼顾**高吞吐量**与**高质量输出**。

---

### **提出了什么新方法或新思路**
论文提出 **DREX**，首个支持高效批处理 EE 推理的服务系统，其核心是 **Dynamic Rebatching（动态重批处理）** 架构，并配套两大优化：

#### **1. Dynamic Rebatching**
- 不强制整个 batch 统一决策。
- 当出现 split decision 时：
  - 满足条件的请求立即早退并生成 token。
  - 未满足的请求暂存于 **逻辑缓冲区（logical buffer）**。
  - 缓冲区积累足够请求后，重新组成新 batch 进入深层。
- 实现**每个请求独立决策**，完全避免 involuntary exits。

#### **2. 两项关键技术优化**
- **Copy-Free Rebatching Buffer**：
  - 利用现代注意力内核（如 FlashAttention）的 `cache_batch_idx` API，通过虚拟张量索引实现**零物理数据移动**的重批处理。
  - 开销极低（在 Llama-EE-70B 上 < 6% 单次迭代时间）。
- **Memory-Efficient State-Copying**：
  - 针对跳过层的 KV Cache 缺失问题，采用**虚拟内存映射**（virtual memory mapping）共享最后一层的 KV Cache 物理块。
  - 避免物理复制，消除内存冗余。

#### **3. 性能保障机制**
- **Adaptive Rebatching Threshold (ART)**：
  - 分析性预测 rebatching 是否盈利（收益 > 开销）。
  - 若早退比例太小，开销大于收益，则放弃 rebatching。
- **SLA-Aware Forced Flushing**：
  - 若缓冲区中请求接近 SLA 截止时间，则强制刷新，防止延迟超标。

---

### **相比现有方法的优势**
| 方法 | 吞吐量 | 输出质量 | Involuntary Exits | Involuntary Stays |
|------|--------|----------|-------------------|-------------------|
| **Consensus** | 低 | 高 | 0 | 高 |
| **Greedy** | 高 | 极低 | 极高 | 0 |
| **Latency-Only (Apparate)** | 低 | 高 | 0 | 高 |
| **DREX (本文)** | **高** | **高** | **0** | **低** |

- **唯一同时实现高吞吐与高质量的方法**。
- **完全消除 involuntary exits**，保证 EE 模型设计的输出质量。
- **显著降低 KV Cache 冗余**，最高减少 18.3% CUDA 内存使用。

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**
- **模型**：
  - `Llama-EE-13B`, `Llama-EE-70B`, `Qwen-EE-14B`
  - EE 配置：Softmax 置信度分类器 + 可配置的 exit ramp 层与阈值（见 Table 3）
- **数据集**：
  - **CNN/Daily Mail** 文本摘要任务（来自 HELM benchmark）
  - 过滤文章长度 < 2048 tokens，共 2160 条样本
- **硬件**：
  - A100 (80GB) 用于 13B/14B 模型
  - H200 (141GB) 用于 70B 模型

---

### **实验设置与评估指标**

#### **评估指标**
| 类别 | 指标 | 定义 |
|------|------|------|
| **Performance** | Throughput | 每秒生成的 output token 数 |
|               | RCT (Request Completion Time) | 请求从调度到完成的时间 |
| **Quality** | P95 Confidence Score | 早退 token 的置信度第95百分位，反映输出质量 |
|           | BERT Score | 生成摘要与参考摘要的语义相似度 |
| **EE Stats** | EE Proportion | 早退 token 占比 |
|            | Involuntary Exit (%) | 强制早退的 token 比例 |
|            | Involuntary Stay (%) | 强制继续的 token 比例 |

---

### **基线方法对比**
- **Latency-Only**：早退但仍在 batch 中继续计算（Apparate）
- **Consensus**：全 batch 同意才退出
- **Majority**：多数投票决定
- **Greedy**：任一请求想退出则全 batch 退出
- **Non-EE**：无早退基线

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **吞吐提升**：相比 Non-EE 基线，DREX 提升 **2–12%** 吞吐量。
- **P95 Confidence Score**：与 Non-EE 和 Conservative 基线持平或更优，**远高于 Greedy**（Greedy 仅为 0.03）。
- **BERT Score**：与 Conservative 方法相近，略低于（因更多早退），但优于 Greedy。
- **Involuntary Exits**：**DREX 完全为 0%**，而 Greedy 高达 25.9–35.1%。
- **KV Cache 内存节省**：相比 EE-LLM 的 state-copying，DREX 减少 **5.7% 平均 / 18.3% 最大** CUDA 内存操作。

---

### **与基线方法的对比结果**
| 方法 | 吞吐 | P95 Confidence | Involuntary Exit |
|------|------|----------------|------------------|
| **Greedy** | 最高 | 极低 (0.03) | 极高 (~35%) |
| **Consensus** | 最低 | 高 | 0% |
| **DREX** | **第二高（+2–12% vs Non-EE）** | **高（≈0.8）** | **0%** |

- **DREX 是唯一在吞吐与质量之间取得平衡的方法**。
- 在 2-exit 配置下，DREX 比 Greedy 吞吐略低，但 **BERT Score 高出 11%**。

---

### **消融实验结果**
#### **Adaptive Rebatching Threshold (ART) 的影响**
- 手动设置 rebatching 阈值，验证 DREX 动态计算的 ART 最优。
- 结果（Table 5）：
  - 对 Llama-EE-13B，最优 ART = 3 → 吞吐 **127.35 t/s**
  - 固定 ART=0（总是 rebatch）→ 吞吐仅 116.80 t/s
  - **提升 9%**，证明避免不盈利 rebatching 至关重要。

#### **SLA-Aware Scheduling 的效果**
- 在高 SLA 压力下，DREX 自动切换至类似 Consensus 行为，优先保障延迟。
- 在低压力下，优先吞吐，平均 RCT 提升 1.4×，P95 RCT 提升 3×。
- **SLA-aware 调度使平均 RCT 改善最多 58.4%**，实现动态权衡。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Dynamic Rebatching 是实现高效 EE 推理的关键**：
   - 允许 per-request 独立决策，最大化 EE 机会。
   - 结合 copy-free buffer 与 virtual state-copying，开销极低。
2. **DREX 实现了吞吐与质量的帕累托前沿**：
   - 吞吐显著高于 Conservative 方法，质量远高于 Greedy。
   - **首次实现 zero involuntary exits**，确保 EE 模型的可靠性。
3. **Adaptive 机制至关重要**：
   - ART 避免无效 rebatching，提升吞吐 9%。
   - SLA-aware flushing 实现动态延迟-吞吐权衡。

---

### **方法的局限性**
- **依赖现代注意力内核支持**（如 FlashAttention 的 `cache_batch_idx`），可能限制在旧框架上的部署。
- **对极端负载敏感**：若系统持续高负载且无 split decision，缓冲区可能 starve（但概率低）。
- **当前假设 EE ramp 准确**，未联合优化 EE 决策本身。

---

### **未来工作方向**
- 将 DREX 与 **speculative decoding** 或 **Mixture-of-Depths** 等动态计算技术结合。
- 探索 **多 exit ramp 的协同调度**。
- 联合优化 **EE 决策算法** 与 **服务调度策略**，进一步提升端到端效率。
- 扩展至 **vision-language models** 或 **encoder-decoder 架构**。

---

> **总结**：DREX 是首个将 Early-Exit LLM 从理论推向实用的推理系统，通过 **Dynamic Rebatching + Copy-Free Buffer + Virtual State-Copying**，实现了**高吞吐、高质量、零强制退出**的 EE 推理，为大规模部署 EE 模型提供了坚实基础。

</details>

---

### 6. [Bits for Privacy: Evaluating Post-Training Quantization via Membership Inference](https://arxiv.org/abs/2512.15335)

**Authors**: Chenxiang Zhang, Tongxi Qu, Zhong Li, Tian Zhang, Jun Pang, Sjouke Mauw  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.15335v1  

#### Abstract
Deep neural networks are widely deployed with quantization techniques to reduce memory and computational costs by lowering the numerical precision of their parameters. While quantization alters model parameters and their outputs, existing privacy analyses primarily focus on full-precision models, le...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Bits for Privacy: Evaluating Post-Training Quantization via Membership Inference*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文系统地研究了**Post-Training Quantization (PTQ)** 对模型隐私的影响，填补了当前研究中对低精度量化模型隐私分析的空白。尽管量化被广泛用于提升推理效率，但其对隐私泄露（尤其是通过 **Membership Inference Attacks, MIAs**）的影响尚不明确。

现有研究多集中于全精度模型或训练阶段引入量化的 QAT 方法，而本文首次聚焦于**无需再训练的 PTQ 方法在不同比特宽度下的隐私-效用权衡**。

---

### ✅ 提出的新方法与新思路

- **首次系统评估 PTQ 在多个先进算法（AdaRound、BRECQ、OBC）下的隐私表现**，使用最先进的 **LiRA (Likelihood Ratio Attack)** 攻击框架进行评估。
- 引入并适配 **1.58-bit 量化**（对应 ternary 权重 {-1,0,1}）到 PTQ 框架中，探索极端低比特下的隐私保护潜力。
- 提出 **“解耦量化”（decoupled quantization）** 思路：将最后一层以更高精度（如 8-bit）保留，其余层保持极低比特（如 1.58-bit），实现对隐私-效用权衡的细粒度控制。

---

### ✅ 相比现有方法的优势

| 方面 | 优势 |
|------|------|
| **实用性** | PTQ 不需要重新训练，适用于实际部署场景；相比 DP-SGD 等隐私保护技术，无额外训练成本。 |
| **新颖性** | 首次揭示 PTQ 本身可作为一种轻量级隐私防御机制，尤其在低比特下显著降低 MIA 成功率。 |
| **灵活性** | 通过调节量化比特数（bit-width）和采用解耦策略，可在精度损失可控的前提下灵活调整隐私水平。 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **CIFAR-10**：10 类图像分类任务，32×32 彩色图像。
- **CIFAR-100**：100 类更复杂图像分类任务。
- **TinyImageNet**：200 类，64×64 图像，更具挑战性。

> 所有数据集均为 MIA 研究中的标准基准。

---

### ⚙️ 实验设置

| 组件 | 设置说明 |
|------|----------|
| **主干模型** | ResNet18（默认），部分实验扩展至 ResNet50 和 DenseNet121 |
| **量化方法** | 三种主流 PTQ：<br>• **AdaRound**<br>• **BRECQ**<br>• **OBC** |
| **量化级别** | 32-bit（全精度）、4-bit、2-bit、**1.58-bit**（ternary） |
| **量化方式** | 仅对权重进行 per-channel asymmetric weight quantization，激活函数保持 full-precision |
| **校准数据** | 使用 1024 个来自训练集的样本进行校准，避免数据泄露 |
| **攻击方法** | **LiRA**（Likelihood Ratio Attack）<br>• Online 攻击（更强）<br>• Offline 攻击（含 fixed variance 模式）<br>• 共训练 64 个 shadow models |
| **评估指标** | • **Utility**: Test Accuracy<br>• **Privacy**: TPR@0.1%FPR（更敏感反映真实威胁）<br>• Log-AUROC（辅助参考） |

---

### 🔁 基线方法对比
- **Full-precision model (32-bit)**：作为基准，衡量量化带来的影响。
- 不同比特宽度的 PTQ 模型之间横向比较（4-bit vs 2-bit vs 1.58-bit）。
- 同一量化方法在不同数据集和架构上的泛化能力验证。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）**1.58-bit 量化效果（CIFAR-100 上）**

| 方法 | 精度（%） | TPR@0.1%FPR (Online) | 相比 Full-Precision 下降幅度 |
|------|-----------|------------------------|-------------------------------|
| Full-precision | 70.22 | 0.3765 | — |
| AdaRound 1.58-bit | 62.35 | **0.0031** | ↓99.2% |
| BRECQ 1.58-bit | 67.61 | 0.0045 | ↓98.8% |
| OBC 1.58-bit | 64.34 | 0.0052 | ↓98.6% |
| Sign Quantization (1-bit) | ~1.0 | — | 失败（接近随机猜测） |

> ✅ 结论：**1.58-bit 量化大幅降低 MIA 成功率（一个数量级），同时仍保留较高可用性**。

---

#### （2）**不同比特宽度的整体趋势（见 Fig. 2 & Fig. 3）**

| 比特数 | 准确率变化 | 隐私泄露（TPR） |
|-------|------------|------------------|
| **4-bit** | 接近 full-precision（<1% 差异） | 隐私泄露水平相似甚至略高 |
| **2-bit** | 轻微下降（~0.5–1%） | 明显减少 TPR（约 50–70% 降低） |
| **1.58-bit** | 显著下降（~20–25% 绝对值） | 极大抑制 MIA（TPR ↓99%） |

> 🔺 注意：TinyImageNet 上 1.58-bit 导致严重精度退化（AdaRound 从 58% → 33%），需进一步优化。

---

#### （3）**消融实验结果**

##### ✅ 解耦量化（Decoupled Quantization）
- 将最后一层设为 8-bit，其余为 1.58-bit 或 2-bit。
- **结果（TinyImageNet）**：
  - AdaRound 1.58-bit + 8-bit last layer：精度恢复至仅比 full-precision 低 **5.6%**（原为 26%）。
  - 同时 TPR@0.1%FPR 仍远低于 full-precision 和 2-bit 模型。
- **意义**：实现了**高精度与强隐私保护的平衡**，提供实用部署方案。

##### ✅ 不同模型架构验证（Fig. 5）
- 在 ResNet18、ResNet50、DenseNet121 上使用 OBC 进行量化。
- 所有架构均表现出一致趋势：**更低比特 ⇒ 更低精度 + 更低隐私泄露**。
- 表明结论具有良好的**跨架构泛化性**。

##### ✅ 1.58-bit 模型的方差分析（Fig. 6）
- BRECQ 在 CIFAR-100 上出现两个集群：一部分模型精度高且隐私好，另一部分隐私差。
- 说明某些 PTQ 方法（如 BRECQ）存在**训练过程中的不稳定性**，导致隐私行为波动。
- 提醒实践者需多次运行以评估鲁棒性。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **低比特 PTQ 可有效缓解 Membership Inference Attack**：
   - 特别是 **1.58-bit 量化**能将 TPR@0.1%FPR 降低 **超过 99%**，显著优于 full-precision 模型。
   - 这表明量化本身可视为一种隐式的噪声注入机制，削弱模型对训练数据的记忆。

2. **存在明显的 privacy-utility trade-off**：
   - 4-bit 量化几乎不影响 accuracy 和 privacy；
   - 2-bit 开始显现 trade-off；
   - 1.58-bit 实现最强隐私保护，但代价是较大 accuracy 损失。

3. **解耦量化是一种有效的调控手段**：
   - 保留最后一层高精度可在**几乎不牺牲隐私的前提下显著恢复 accuracy**，特别适合高要求任务。

4. **结果具有跨数据集与跨架构的一致性**：
   - 在 CIFAR-10、CIFAR-100、TinyImageNet 及多种 DNN 架构上均成立，增强了结论可信度。

5. **攻击强度影响评估结果**：
   - LiRA Online 攻击更难防御，而 Offline 攻击在低比特下易被完全抑制，强调应使用强 baseline 评估隐私。

---

### ⚠️ 局限性

| 局限 | 说明 |
|------|------|
| **仅限视觉领域** | 实验基于图像分类任务，未验证 NLP 或 graph 模型中的有效性。 |
| **缺乏理论解释** | 当前为经验性发现，尚未建立量化比特与隐私泄露之间的形式化关系（如基于信息论）。 |
| **依赖 shadow model 假设** | LiRA 攻击需构建 shadow models，在现实中可能难以完全复现。 |
| **未考虑 activation quantization** | 仅量化 weights，若同时量化 activations 可能进一步影响隐私-效用平衡。 |

---

### 🔮 未来工作方向

1. **扩展至其他模态**：将研究推广到语言模型（LLMs）、图神经网络等领域。
2. **结合其他隐私技术**：探索 PTQ 与 Differential Privacy、Federated Learning 等结合的协同效应。
3. **理论建模**：建立量化噪声与信息泄漏之间的数学联系，指导最优 bit-width 选择。
4. **自动化调参**：设计算法自动搜索最佳量化配置（如每层 bit 数），实现个性化 privacy-utility 平衡。
5. **研究 scaling law 下的行为**：探究大模型在不同规模下 PTQ 对 MIA 的影响是否遵循特定规律。

---

## ✅ 总结

该论文首次系统揭示了 **Post-Training Quantization** 在隐私保护方面的潜力，证明**极低比特量化（如 1.58-bit）可显著降低 Membership Inference Attack 的成功率**，为高效且私有的机器学习部署提供了新的视角。通过引入解耦量化等策略，能够在实用性与安全性之间取得良好平衡，具有重要的工程指导价值。

</details>

---

### 7. [FlowBind: Efficient Any-to-Any Generation with Bidirectional Flows](https://arxiv.org/abs/2512.15420)

**Authors**: Yeonwoo Cha, Semin Kim, Jinhyeon Kwon, Seunghoon Hong  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.15420v1  

#### Abstract
Any-to-any generation seeks to translate between arbitrary subsets of modalities, enabling flexible cross-modal synthesis. Despite recent success, existing flow-based approaches are challenged by their inefficiency, as they require large-scale datasets often with restrictive pairing constraints, inc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# FlowBind: Efficient Any-to-Any Generation with Bidirectional Flows — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有的 **flow-based any-to-any generation** 方法面临三大挑战：
- **数据依赖性强**：多数方法需要完全配对的多模态数据（如文本-图像-音频三元组），这类数据稀缺且昂贵。
- **计算成本高**：建模联合分布导致计算复杂度随模态数平方增长，训练效率低。
- **训练流程复杂**：普遍采用多阶段训练（如先对齐再生成），流程繁琐、难以优化。

### 🚀 提出的新方法：FlowBind
FlowBind 是一种简洁高效的 **flow-based any-to-any generation 框架**，其核心思想是：
- 引入一个**可学习的共享潜在空间（learnable shared latent）**，作为所有模态之间的“语义锚点”。
- 每个模态通过一个**模态特定的可逆流（invertible flow）** 连接到该共享潜在空间。
- 所有组件在**单一 flow matching 目标函数下端到端联合训练**，无需多阶段流程。

### 🔍 创新点与优势
| 特性 | FlowBind | 传统方法（如 CoDi, OmniFlow） |
|------|---------|-----------------------------|
| **训练方式** | 单一目标、端到端联合训练 | 多阶段训练（对齐 + 生成） |
| **数据要求** | 支持任意部分配对数据（partial pairing） | 需要全配对或以文本为锚点的数据 |
| **计算效率** | 参数更少、计算开销更低 | 高维联合建模，计算昂贵 |
| **灵活性** | 自然支持任意子集输入→任意子集输出 | 受限于固定架构设计 |

> 💡 **关键洞察**：通过将多模态交互解耦为“各模态 ↔ 共享潜变量”的独立流，实现了高效、灵活且可扩展的 any-to-any 生成。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
FlowBind 在 **text, image, audio** 三种模态上进行验证，使用以下配对数据集（无三元组）：
- **Text-Image**: LAION-COCO (242K), Flickr-30k (30K)
- **Text-Audio**: AudioCaps v2 (91K)
- **Audio-Image**: VGGSound (184K)

> ⚠️ 不使用 triplet 数据，体现其对部分配对数据的强大利用能力。

### 🧪 实验设置
- **模型规模**：总训练参数仅 **568M**（远小于 CoDi 的 4.3B 和 OmniFlow 的 3.2B）
- **训练资源**：约 **48 GPU-hours**（H100），约为 OmniFlow 的 1/10
- **推理机制**：
  - 编码：输入模态通过反向 ODE 映射至共享潜变量 $ z^* $
  - 解码：从 $ z^* $ 出发，用目标模态的正向 ODE 生成输出
  - 多源输入时，各模态潜变量取平均

### 📊 评估指标
#### （1）生成质量（Fidelity）
- 图像：**FID**
- 音频：**FAD**
- 文本：**CIDEr**

#### （2）跨模态对齐（Alignment）
- 文本-图像：**CLIP Score**
- 文本-音频：**CLAP Score**
- 图像-音频：**AIS (Audio-Image Similarity)**

#### （3）任务类型
- **One-to-One**：6 种两两转换（T→I, I→T, T→A, A→T, I→A, A→I）
- **Many-to-One / One-to-Many**：定量 + 定性分析
- **Zero-shot 泛化**：新增 3D point cloud 模态测试泛化能力

### 🆚 基线方法
- **Specialists**：SD3-Medium, FLUX.1, LLaVA-NeXT, TangoFlux, Qwen2-Audio 等单任务专家模型
- **Generalists**：
  - **CoDi**：基于文本嵌入对齐的多模态框架
  - **OmniFlow**：联合速度场建模的 flow-based any-to-any 方法

---

## 3. 主要实验结果和性能指标

### 📈 One-to-One 生成性能（表 2 & 表 3）

| 指标 | FlowBind vs. CoDi/OmniFlow |
|------|----------------------------|
| **生成质量（FID/FAD/CIDEr）** | 在全部 6 项任务中取得**最佳质量指标** |
| **跨模态对齐（CLIP/CLAP/AIS）** | 在 6 项中有 4 项优于基线，尤其在非文本任务（如 I→A, A→I）表现突出 |

> 🔥 **亮点**：在 **image-audio 互生任务** 上显著超越 specialist 模型，说明共享潜空间能有效捕捉非语言模态间的深层关联。

### 📊 Many-to-Many 生成（表 4 & 表 5）
构建合成 triplet 数据集用于评估：

| 设置 | FlowBind 表现 |
|------|---------------|
| **(I+A)→T, (T+A)→I, (T+I)→A**（Many-to-One） | 在所有对齐指标上**全面领先**，尤其在 (T+I)→A 中 CLAP 达 **28.13**（CoDi: 4.85） |
| **T→(I+A), I→(T+A), A→(T+I)**（One-to-Many） | 对齐分数更高，表明能更好保留多模态条件信息 |

> ✅ FlowBind 更少出现“忽略某一输入模态”的问题，显示更强的**多条件融合能力**。

### 🔍 消融实验与分析（关键发现）

#### （1）共享潜空间的有效性（表 6）
- 对比“固定文本锚” vs “可学习共享潜变量”
- 即使不使用 image-audio pair 训练，FlowBind 变体仍优于 text-anchoring 方法
- 结论：**learnable shared latent 更有利于跨模态对齐**

#### （2）共享潜空间的语义一致性（表 7）
使用 **CKNNA** 度量衡量潜空间对齐程度：
| 模态对 | Per-modality Latent | Shared Latent |
|--------|---------------------|--------------|
| T-A    | 0.1965              | **0.2872**   |
| A-I    | 0.1343              | **0.3026**   |

> 表明共享潜空间具有更强的跨模态语义对齐能力。

#### （3）插值可视化（图 3）
- 在共享潜空间中进行线性插值，解码后图像/文本呈现平滑语义过渡
- 证明该空间具备良好的**连续性和语义结构性**

#### （4）冲突条件鲁棒性（图 4）
- 在语义冲突的 {text + audio} → image 任务中，FlowBind 能同时反映两种信号，而非退化为单一模态主导
- 归因于共享潜空间的结构化表示能力

#### （5）扩展至 3D Point Cloud（图 6–7）
- 新增模态只需添加对应 drift network，参数增长近似线性
- 成功实现 **text → point cloud** 和 **point cloud → text** 的 zero-shot 跨模态生成
- 展示了出色的**可扩展性与泛化能力**

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **FlowBind 实现了真正灵活的 any-to-any generation**：
   - 支持任意输入/输出模态组合
   - 无需全配对数据，训练更实用

2. **效率显著提升**：
   - 参数量仅为 OmniFlow 的 **1/6**
   - 训练时间缩短 **10 倍**
   - 推理过程简单高效（仅需 ODE 正反向积分）

3. **性能媲美甚至超越大模型**：
   - 在 one-to-one 和 many-to-many 任务中均达到 SOTA 或接近 SOTA 水平
   - 尤其在非文本模态间转换（如 audio-image）表现出色

4. **共享潜空间是成功的关键**：
   - 学习到一个高度对齐、语义丰富的共享表示
   - 通过最小化 conditional variance 实现稳定训练

### ⚠️ 局限性
- 当前依赖预训练 encoder-decoder（如 CLIP, CLAP, EmbeddingGemma），未端到端训练整个 pipeline
- 对极端语义冲突或多模态矛盾的处理仍有改进空间
- 尚未在大规模真实世界复杂场景中部署验证

### 🔮 未来工作方向
- 探索动态加权聚合机制替代简单的 latent averaging
- 扩展至更多模态（如视频、3D mesh、触觉等）
- 结合 diffusion transformer 架构进一步提升生成质量
- 探索在下游任务（如机器人控制、跨模态检索）中的应用

---

## 总结
FlowBind 提出了一种**简洁、高效、可扩展**的 any-to-any 生成范式。它通过引入 **learnable shared latent + bidirectional invertible flows** 的架构，在大幅降低数据与计算需求的同时，实现了强大的跨模态生成能力。实验证明其不仅在标准任务上表现优异，还能自然支持复杂多模态输入输出，并具备良好的零样本泛化潜力，为构建通用多模态生成模型提供了新思路。

> 🌐 项目主页：[https://yeonwoo378.github.io/official_flowbind](https://yeonwoo378.github.io/official_flowbind)

</details>

---

### 8. [LLMQ: Efficient Lower-Precision Pretraining for Consumer GPUs](https://arxiv.org/abs/2512.15306)

**Authors**: Erik Schultheis, Dan Alistarh  
**Category**: cs.DC  
**Published**: 2025-12-18  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.15306v1  

#### Abstract
We present LLMQ, an end-to-end CUDA/C++ implementation for medium-sized language-model training, e.g. 3B to 32B parameters, on affordable, commodity GPUs. These devices are characterized by low memory availability and slow communication compared to datacentre-grade GPUs. Consequently, we showcase a ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLMQ: Efficient Lower-Precision Pretraining for Consumer GPUs

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

LLMQ 针对**消费级 GPU（如游戏显卡）在大语言模型（LLM）预训练中面临的内存受限、通信带宽低**的问题。这类设备虽然性价比高（FLOPs/dollar），但由于显存小（如 16–24GB）、缺乏高效的 GPU 间直接通信（如 NVLink），难以支持 3B–32B 参数规模的完整训练流程。

传统数据中心级系统（如基于 H100 或 L40S 的集群）虽能胜任，但成本高昂且需将数据上传至云端，存在隐私与费用问题。LLMQ 的目标是实现**在单节点、多块消费级 GPU 上高效完成中等规模 LLM 的端到端预训练与微调**。

---

### **提出了什么新方法或新思路**

LLMQ 是一个**完全用 C++ 和 CUDA 编写的端到端训练框架**，专为消费级硬件优化，其核心创新包括：

1. **FP8 + 动态张量级缩放（Dynamic Tensor-Level Scaling）**
   - 支持 Ada（RTX 40xx）和 Blackwell（RTX 50xx）架构上的原生 FP8 训练。
   - 使用 **abs-max scaling** 在前向传播时进行即时量化，保证无溢出，适用于统计变化剧烈的小批量场景。
   - 主要矩阵运算（matmul）使用 FP8，非线性层、SDPA、Embedding/LM-head 等保留 BF16 以保持数值稳定性。

2. **多层次内存优化策略组合**
   - **选择性重计算（Selective Recomputation）**：从仅重算 SwiGLU/RMSNorm 到整 Transformer Block 重算，灵活应对不同显存容量。
   - **CPU Offloading**：
     - 将 optimizer states（m/v）、主参数（master weights）、残差（residuals）、甚至梯度缓冲区 offload 至主机内存。
     - 在单张 16GB RTX 5060Ti 上成功训练 7B 模型。
   - **Chunking 技术**：对 logits 和 FlashAttention 的 workspace 进行分块处理，避免因大 batch 导致内存爆炸。

3. **基于 cudaMemcpy 的通信后端**
   - 针对消费级 GPU 无法通过 PCIe 直接 P2P 通信的问题，提出利用 **GPU Copy Engine** 实现高效的 Reduce-Scatter 和 All-Gather。
   - 相比 NCCL，在 RTX 4090 多卡系统上显著提升 PCIe 带宽利用率，释放 SM 资源用于计算。

4. **权重缓存于主机内存（Weight Caching on Host）**
   - 利用“必须经过 CPU 中转”的劣势变优势：将分片权重暂存于主机内存，后续前向传播可复用，减少重复传输。

5. **确定性内核与排序优化**
   - 所有 kernel 均为 bitwise-deterministic，确保可复现性。
   - Embedding 层反向传播采用 CPU 排序 + 分区，避免大规模原子操作带来的中间 buffer 开销。

---

### **相比现有方法的优势**

| 维度 | LLMQ 优势 |
|------|-----------|
| **硬件适配性** | 可运行于 RTX 30/40/50 系列消费卡，无需数据中心级硬件 |
| **训练效率** | 在 4×RTX 4090 上训练 32B 模型达到 **51% Model FLOPs Utilization (MFU)**，超过 L40S 的 29% MFU |
| **内存效率** | 单卡 16GB 可训 7B 模型（70% MFU），远超同类框架限制 |
| **通信效率** | 自研 memcpy-based collectives 显著优于 NCCL 在消费级平台的表现 |
| **精度兼容性** | 支持混合 BF16-FP8 训练，兼顾速度与精度，且不引入算法近似 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **ClimbMix**：用于 1.5B 参数 Qwen 风格模型的预训练，retokenized 并 subsampled 至 10B~30B tokens。
- **GSM8K**：数学推理任务，用于评估 LLama2-7B 和 Qwen2.5-14B 的微调效果。

---

### **实验设置和评估指标**

#### **硬件配置**
- 单卡：RTX 5060Ti (16GB), RTX 4090 (24GB)
- 多卡：4×RTX 4090, 4×L40S, DGX Spark（Blackwell 架构，统一内存）
- 对比对象：LLama-Factory（主流开源微调框架）

#### **训练设置**
- 固定每 step 总 token 数为 500k（micro-batch × seq_len × accum_step）
- 序列长度：2048（LLama2），512（Qwen）
- 优化器：AdamW（FP8/BF16），使用 stochastic rounding
- 学习率调度：线性衰减 + warmup

#### **评估指标**
- **Tokens Per Second (TPS)**：吞吐量
- **Model FLOPs Utilization (MFU)**：实际 FLOPs / 理论峰值 FLOPs，反映硬件利用率
- **Speed-up (Sp)**：FP8 相较 BF16 的加速比
- **下游任务准确率**：GSM8K 上的 few-shot/zero-shot 表现

---

### **基线方法对比**
- **LLama-Factory (LF)**：作为主流开源框架代表，使用 ZeRO-2/3 + DeepSpeed Offload。
- **NCCL-based collectives**：标准通信库 vs LLMQ 自研 memcpy 方案。
- **BF16 baseline**：验证 FP8 是否带来真实收益。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **单卡性能（Table 1）**

| 模型大小 | 设备 | 精度 | TPS | MFU |
|--------|------|-----|-----|-----|
| 7B | RTX 5060Ti | FP8 | 1.4k | **70%** |
| 7B | RTX 4090 | FP8 | 4.3k | 61% |
| 14B | RTX 4090 | FP8 | 2.0k | 55% |

> ✅ 即使在 **16GB 显存的 RTX 5060Ti** 上也能实现 **7B 模型训练，MFU 达 70%**，极具突破性。

#### **多卡性能（Table 2）**

| 模型大小 | 设备 | 精度 | TPS | MFU |
|--------|------|-----|-----|-----|
| 14B | 4×RTX 4090 | FP8 | 7.8k | **54%** |
| 32B | 4×RTX 4090 | FP8 | **3.4k** | **51%** |
| 32B | 4×L40S | BF16 | 3.0k | 40% |

> ✅ **4×RTX 4090 的 MFU 显著高于更贵的 4×L40S**（51% vs 29%），说明消费级硬件经优化后更具性价比。

#### **FP8 加速效果（Speed-up Sp）**

- 对于 7B+ 模型，FP8 相比 BF16 可带来 **~50–59% 的吞吐提升**。
- 小模型加速有限（<30%），因非 GEMM 操作占比高。

#### **memcpy vs NCCL 通信效率（Table 5）**

| 设备 | 方法 | 14B 模型 TPS（FP8） |
|------|------|------------------|
| 4×RTX 4090 | NCCL | 4.3k |
| 4×RTX 4090 | memcpy (full) | **7.8k** (**+81%**) |
| 4×L40S | NCCL | 9.5k |
| 4×L40S | memcpy (full) | 9.9k |

> ✅ **memcpy-based collectives 在消费级平台至关重要**，几乎翻倍性能；但在专业卡上增益较小。

#### **与 LLama-Factory 对比**

| 模型 | 设备 | LLMQ TPS | LF TPS | 加速比 |
|------|------|---------|--------|-------|
| 1.5B | 4×4090 | 71k | 41k | ~1.7× |
| 14B | 4×4090 | 7.8k | 2.6k | **~3×** |

> ✅ 在大规模下 LLMQ 明显领先，尤其当需要 offloading 时，LLMQ 更高效。

---

### **消融实验结果**

- **Offloading 层级影响**：逐步 offload `m/v` → `master weights` → `residuals` → `gradients`，可在 16GB 显存下将 micro-batch 从 2 提升至 16。
- **Recompute 策略**：全 block 重算 + offloading 是训练 7B 的必要条件。
- **Chunking 必要性**：未 chunking 时 3B 模型最大 batch 仅为 10，chunking 后可达 24。
- **FP8 数值稳定性**：
  - E4M3（4 exponent, 3 mantissa）表现接近 BF16；
  - E5M2 在反向传播中用于激活梯度时略有性能下降，挑战传统推荐。

---

## 4. 关键结论和发现

### **主要发现**

1. **消费级 GPU 完全可以胜任中等规模 LLM 训练**，只要系统层面充分优化内存与通信。
2. **FP8 训练在大模型上可带来约 50% 的吞吐提升**，且精度损失极小（GSM8K 微调结果相当）。
3. **memcpy-based collectives 比 NCCL 更适合无 P2P 支持的消费级平台**，能有效隐藏通信延迟。
4. **LLMQ 在 MFU 上超越专业级 L40S GPU**，证明其优化有效性。
5. **单张 16GB 显卡训练 7B 模型成为现实**，极大降低本地训练门槛。

---

### **方法的局限性**

1. **依赖统一内存架构的潜力尚未完全释放**：DGX Spark 虽有 128GB 统一内存，但内存带宽较低（300 GB/s），导致 MFU 不及预期。
2. **FP8 长期训练稳定性仍待验证**：论文指出低精度可能在超长训练（>Chinchilla 最优比例）下失效，当前主要用于短周期微调或中等长度预训练。
3. **未涉及 MoE 或极端长序列训练**：目前聚焦 dense 模型与常规上下文长度。
4. **NVMe offloading 未实现**：虽然理论上可行，但作者认为会破坏计算隐藏通信的平衡。

---

### **未来工作方向**

1. **扩展至更多低比特格式**（如 INT8、FP6、1-bit）以进一步压缩 optimizer states。
2. **集成 Parameter-Efficient Fine-Tuning (PEFT)** 方法（如 LoRA），与现有 offloading 协同。
3. **支持跨节点分布式训练**，结合当前单节点高效通信向更大规模扩展。
4. **探索 Blackwell 架构中的硬件 scaling 支持**，消除软件 emulate 开销。
5. **自动化配置搜索**：根据不同硬件自动选择最优 recompute/offload/batch 策略。

---

> 🔗 **代码开源地址**：[https://github.com/IST-DASLab/llmq](https://github.com/IST-DASLab/llmq)

</details>

---

### 9. [Dual-Density Inference for Efficient Language Model Reasoning](https://arxiv.org/abs/2512.15358)

**Authors**: Zhengyi Zhao, Shubo Zhang, Yuxi Zhang, Huimin Wang, Binyang Li, Kam-Fai Wong  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.15358v1  

#### Abstract
Large Language Models (LLMs) have shown impressive capabilities in complex reasoning tasks. However, current approaches employ uniform language density for both intermediate reasoning and final answers, leading to computational inefficiency. Our observation found that reasoning process serves a comp...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Dual-Density Inference for Efficient Language Model Reasoning》核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的大型语言模型（LLMs）在进行复杂推理任务时，普遍采用 **Chain-of-Thought (CoT)** 等方法，其推理过程和最终答案均使用相同的自然语言（Natural Language, NL）。这种做法存在**表征错配（representational mismatch）**：
- **中间推理**是模型内部的计算过程，应追求高信息密度、低冗余；
- **最终回答**是面向用户的沟通输出，需具备可读性和解释性。

然而，现有方法将两者混用，导致大量计算资源被浪费在生成冗余的“语法糖”上，造成严重的**计算效率低下**。

### 提出的新方法与思路
作者提出 **Denser: Dual-density Inference**，一种新型的双密度推理框架，其核心思想是：
> **分离推理（reasoning）与回答（answering）的语言密度**。

具体实现通过三个模块：
1. **Query Analysis Module**：分析输入问题，识别其所属领域（数学、逻辑、代码等），并确定相应的压缩策略。
2. **High-density Reasoning Module**：在中间推理阶段，使用**高度压缩、符号化强**的语言（如数学公式、逻辑符号、伪代码）进行高效计算。
3. **Low-density Answering Module**：在推理完成后，将压缩的推理路径**解压**为人类可读的自然语言解释。

该方法从信息论角度优化了推理链的信息密度，实现了“对内高效，对外友好”。

### 相比现有方法的优势
- **显著降低 Token 消耗**：相比标准 CoT 方法，最高减少 **62%** 的 Token 使用量。
- **保持甚至提升准确性**：在多个基准测试中，准确率优于或持平于现有方法。
- **特别适用于复杂多步推理**：问题越复杂，传统方法的冗余越多，Denser 的优势越明显。
- **无需模型微调**：作为测试时扩展（Test-Time Scaling）技术，可直接应用于现有 LLM。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖四大类共八个基准数据集：
- **数学推理（Math）**：GSM8K, MATH
- **逻辑推理（Logic）**：LogiQA, ProofWriter
- **代码推理（Code）**：MBPP, HumanEval
- **通用问答（General QA）**：MMLU, StrategyQA

这些数据集涵盖了需要多步、结构化推理的任务，适合验证密度优化的有效性。

### 实验设置和评估指标
- **主干模型**：以 **Qwen3-14B** 为主要实验模型，并在 GPT-4o、DeepSeek-V3、Claude-3.7-Sonnet 等 API 模型上进行了验证。
- **评估指标**：
  - **Performance Accuracy (%)**：各领域的任务准确率。
  - **Token Cost (%)**：相对于标准 CoT 的 Token 消耗变化。
  - **Computational Latency**：每道题的平均推理时间（秒）。
  - **Reasoning Efficiency Index (REI)**：综合衡量准确率与 Token 效率的指标。

### 基线方法对比
分为两类：
#### (1) 测试时扩展（TTS）方法
- Chain-of-Thought (CoT)
- Self-Consistency (SC)
- Tree-of-Thought (ToT)
- Think-to-Think (T)
- Reflection-CoT
- Process Supervision
- Self-Verification (SV)

#### (2) 推理压缩基线
- **BeConcise**：提示模型简洁回答
- **OnlyNumbers**：仅输出数字和符号
- **AbbreWords**：使用缩写词
- **TokenSkip**：基于注意力的 Token 跳过技术

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在 **Qwen3-14B** 上的综合表现（见 Table 4）：

| 方法 | 平均准确率 (%) | Token 成本 (%) |
|------|----------------|----------------|
| CoT | 75.8 | 0.0 |
| Self-Verification (SV) | 79.2 | +142.8 |
| **Denser (Ours)** | **80.3** | **-58.7** |

- **Denser 在所有领域均取得最高准确率**，平均比最强基线（SV）提升 **1.8%**。
- **Token 消耗降低 58.7%**，远超其他压缩方法。

### 与基线方法的对比结果
- **vs. TTS 方法**：Denser 在准确率上超越所有 TTS 方法，同时 Token 消耗大幅降低（而 TTS 方法通常增加 100%-300% 的 Token）。
- **vs. 压缩基线**：
  - **TokenSkip**：虽有较好准确率，但 Denser 在更低 Token 消耗下仍更优。
  - **OnlyNumbers**：Token 减少最多（-66.2%），但准确率显著下降，说明盲目压缩会丢失关键信息。
  - **Denser** 在**效率与性能之间取得了最佳平衡**。

### 消融实验结果（Ablation Study）
（见 Table 6）

| 变体 | 准确率 | Token 减少 |
|------|--------|------------|
| Denser (Full) | 78.2% | 57% |
| w/o Query Analysis | 76.9% (-1.3%) | 54% |
| w/o HD Reasoning | 74.6% (-3.6%) | 8% |
| w/o LD Answering | 77.5% (-0.7%) | 61% |
| 固定密度策略 | 75.8% (-2.4%) | 42% |

- **移除高密度推理（HD Reasoning）影响最大**，证明其为核心创新。
- **查询分析模块**对自适应选择密度策略至关重要。
- **固定密度策略**性能下降，表明**领域自适应**的重要性。

---

## 4. 关键结论和发现

### 主要发现
1. **语言的双重功能**：自然语言在 LLM 推理中兼具**计算功能**（内部推理）和**沟通功能**（外部回答），二者需求不同，应区别对待。
2. **信息密度不匹配是效率瓶颈**：当前 CoT 方法中，约 **68% 的 Token 属于解释性语言**，仅 **32% 包含核心计算信息**。
3. **Denser 显著提升效率**：在保持甚至提升准确率的前提下，**Token 消耗减少 52-62%**，推理延迟降低 **27.8%**。
4. **复杂度越高，收益越大**：对于高复杂度问题，Denser 的 Token 节省可达 **71%**。
5. **符号化表示优势明显**：在代数、几何、命题逻辑等领域，Denser 改进最为显著。

### 方法的局限性
- **引入额外推理开销**：Query Analysis 和 Answer Generation 模块带来少量计算开销，可能不适合对延迟极度敏感的应用。
- **性能增益因任务而异**：在自然语言已为主要表达媒介的任务（如文学分析）中，收益有限。
- **依赖高质量 Prompt 设计**：高密度推理的成功依赖于精心设计的领域特定 Prompt（见 Appendix C）。

### 未来工作方向
- 将 Denser 思想应用于**模型训练阶段**，探索更深层次的双密度架构。
- 扩展至更多非结构化任务，研究如何在语义丰富的场景中安全地应用密度压缩。
- 探索**动态密度调节机制**，根据推理状态实时调整语言密度。
- 结合**模型蒸馏**，将 Denser 的高效推理能力迁移到小型模型。

---

> **总结**：Denser 通过区分推理与回答的语言密度，提出了一种高效、可扩展的 LLM 推理范式。它不仅解决了 CoT 的冗余问题，还为 LLM 的高效推理提供了新的理论视角和实践框架，在保持高性能的同时大幅降低了计算成本。

</details>

---

### 10. [Accelerating High-Throughput Catalyst Screening by Direct Generation of Equilibrium Adsorption Structures](https://arxiv.org/abs/2512.15228)

**Authors**: Songze Huo, Xiao-Ming Cao  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.15228v1  

#### Abstract
The adsorption energy serves as a crucial descriptor for the large-scale screening of catalysts. Nevertheless, the limited distribution of training data for the extensively utilised machine learning interatomic potential (MLIP), predominantly sourced from near-equilibrium structures, results in unre...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerating High-Throughput Catalyst Screening by Direct Generation of Equilibrium Adsorption Structures

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在异相催化中，**吸附能**（adsorption energy）是衡量催化剂活性的关键描述符，其准确计算依赖于高精度的几何结构弛豫（geometry relaxation）。传统基于 **Density Functional Theory (DFT)** 的方法虽然精确，但计算成本高昂（复杂度为 $O(n^3)$），严重制约了大规模催化剂的高通量筛选。

现有的 **Machine Learning Interatomic Potential (MLIP)** 模型虽可加速结构优化，但由于训练数据多来自近平衡态结构（near-equilibrium structures），对非平衡初始结构的预测不可靠，导致优化轨迹偏离真实势能面（PES），产生错误的吸附构型和能量。

### 提出的新方法：DBCata
本文提出了一种名为 **DBCata**（Diffusion Bridge Model for Catalysts）的深度生成模型，用于直接从非弛豫（unrelaxed）结构生成 DFT 精度的平衡吸附结构。

#### 核心思想
- **不拟合整个势能面（PES）**，而是学习从初始结构到 DFT 弛豫结构之间的低维过渡流形（transition manifold）。
- 基于 **Brownian Bridge 框架**，结合 **equivariant graph neural network**（采用 PaiNN 架构），实现对周期性表面体系的建模。
- 在训练阶段，通过添加噪声模拟从初始结构 $G_0$ 到弛豫结构 $G_1$ 的随机路径；在推理阶段，使用 ODE 进行确定性采样，快速生成高质量结构。

### 相比现有方法的优势
| 方面 | 传统 MLIP（如 UMA-OC20） | DBCata |
|------|--------------------------|--------|
| **训练目标** | 拟合全 PES 上的能量与力 | 学习 $G_0 \to G_1$ 的映射 |
| **训练数据需求** | 需大量覆盖非平衡态的结构 | 仅需成对的 $(G_0, G_1)$ 结构，无需能量/力标签 |
| **计算效率** | 快于 DFT，但仍需迭代优化 | 单结构生成 <1 秒，约比 DFT 快 10⁴ 倍 |
| **准确性** | 对远非平衡结构预测差 | 在 Catalysis-Hub 上表现显著更优 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用公开数据集 **Catalysis-Hub**，包含约 80,000 条金属及合金表面的 DFT 弛豫轨迹（BEEF-vdW 泛函）。
- 提取其中 **76,737 对**初始结构与对应 DFT 弛豫结构。
- 包含吸附物种：H, C, CH, CH₂, CH₃, N, NH, O, OH, H₂O, S, SH。
- 数据按 8:2 划分训练集与测试集（58,943 : 14,728）。

### 实验设置
- **硬件**：单张 NVIDIA RTX 4090 GPU，训练耗时约 12 小时。
- **模型架构**：
  - 使用 **PaiNN** 作为 equivariant GNN 主干网络。
  - 引入 **multi-graph 方法** 处理周期边界条件（PBC）。
  - 提出 **nearest-atom interpolation** 技术确保插值过程满足 PBC。
- **扩散参数**：
  - 扩散步数 $T = 100$，最大噪声方差 $\sigma_{\text{max}} = 0.05$。
  - 推理时使用 ODE 确定性采样，步数设为 20。

### 评估指标
- **DMAE**（Distance Mean Absolute Error）：
  $$
  \text{DMAE} = \frac{1}{N^2} \sum_{i,j} |d^{\text{DFT}}_{ij} - d^{\text{DBCata}}_{ij}|
  $$
  衡量生成结构与 DFT 弛豫结构的原子间距差异。
- **预测成功率**（Success Rate）：
  $$
  \text{Rate} = \frac{1}{m} \sum_{i=1}^{m} \mathbb{I}(|E^{\text{DBCata}}_i - E^{\text{DFT}}_i| \leq \epsilon)
  $$
  其中 $\epsilon = 0.1\,\text{eV}$，用于评估吸附能预测精度。
- **离子步数**（Ionic Steps）：衡量后续 DFT 再优化所需迭代次数。

### 基线方法对比
- **UMA-OC20**：当前最先进的预训练 MLIP 模型（来自 Open Catalyst Project 和 FAIRChem）。
- **DFT Relaxation**：作为黄金标准基准。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | DBCata | UMA-OC20 | 加速倍数 |
|------|--------|---------|----------|
| **DMAE (Å)** | **0.0352** | 0.1133 | — |
| **单结构生成时间 (s)** | **0.002–0.212**（并行 vs 串行） | 9.777 | 最高达 **425,640×** |
| **吸附能预测准确率 ($|\Delta E| < 0.1\,\text{eV}$)** | **91%** | 68% | — |
| **平均离子步数（再优化）** | **15 步**（从 DBCata 出发） | 65 步（从初始结构出发） | 节省 ~77% |

> 注：速度对比见 **Supplementary Table 1**，DBCata 在批量并行下可达每秒处理数百结构。

### 与基线方法的对比结果
- **结构精度**：DBCata 的 DMAE 是 UMA-OC20 的 **1/3**，表明其生成结构更接近 DFT 真值。
- **能量预测一致性**：基于 DBCata 结构进行 DFT 单点计算，其吸附能与 DFT 弛豫结果的相关性更高（$R^2 = 0.983$ vs $0.877$）。
- **分布外鲁棒性**：即使对于 UMA 未见过的含硫吸附物（如 SH），DBCata 仍能保持稳定性能（图 S2）。
- **近平衡态验证**：DBCata 生成的结构处于 PES 局部极小附近，使得 MLIP 能准确评估其能量（图 4），而 UMA 自身优化常陷入错误局部极小。

### 消融实验结果（Ablation Study）
见 **Supplementary Table 4**，验证关键设计组件的影响：

| 变体 | Fractional Coord | Gaussian Basis | Fourier Basis | DMAE (Å) |
|------|------------------|---------------|--------------|----------|
| Baseline | ✗ | ✓ | ✗ | 0.0409 |
| Baseline | ✓ | ✗ | ✓ | 0.0621 |
| **Full Model** | ✗ | ✗ | ✓ | **0.0353** |

- 使用 **fractional coordinates + Fourier positional encoding** 显著提升性能。
- 证明了所提出的周期性建模策略的有效性。

### 异常检测与后处理
- 提出混合异常检测机制（hybrid outlier detection）：
  - 基于 **化学启发规则**（如原子碰撞、解离等）；
  - 训练一个二分类器判断结构是否为 outlier。
- 对检测出的异常结构进行 DFT 再优化。
- 结果：将预测准确率从 **91% 提升至 94%**，同时总计算成本仅增加约 3 万离子步（远低于全流程 DFT 的百万级）。

---

## 4. 关键结论和发现

### 主要发现
1. **DBCata 能高效且高精度地生成接近 DFT 水平的吸附结构**，DMAE 达 0.035 Å，显著优于当前最优 MLIP。
2. **无需拟合全势能面即可实现可靠结构预测**，突破了传统 MLIP 对训练数据广度的依赖。
3. **生成结构已非常接近局域最小值**，极大减少了后续 DFT 或 MLIP 优化所需的迭代步数（平均从 65 → 15 步）。
4. **结合 outlier detection 后，94% 的预测可在 0.1 eV 内匹配 DFT 结果**，具备实际应用价值。
5. 成功应用于 **ORR（oxygen reduction reaction）合金催化剂高通量筛选**，识别出多个有潜力的候选体系（如 PtAu(110), AgAu(110)）。

### 方法的局限性
- 当前模型依赖于**固定规则初始化的初始结构**（initial guess），若输入结构严重偏离训练分布，性能可能下降。
- 模型目前仅适用于**特定类型的表面吸附系统**（如 slab + 吸附分子），尚未扩展至复杂多相催化或动态反应路径预测。
- 虽然速度快，但仍是“结构生成 + 能量评估”两步流程，端到端预测仍有改进空间。

### 未来工作方向
- 探索将 **stochastic optimal control** 与 diffusion bridge 结合，减少对成对数据的依赖。
- 利用 PES 梯度信息直接学习吸附过程动力学，避免依赖 DFT 弛豫数据。
- 扩展至更多元化的催化体系（如氧化物、单原子催化剂）和反应类型。
- 开发更高效的联合生成-评估框架，进一步压缩高通量筛选周期。

---

> **总结**：DBCata 提供了一种全新的范式——**跳过势能面建模，直接学习结构映射**，实现了催化剂吸附结构的超快、高保真生成，为下一代高通量计算催化筛选提供了强大工具。

</details>

---

### 11. [CTkvr: KV Cache Retrieval for Long-Context LLMs via Centroid then Token Indexing](https://arxiv.org/abs/2512.15550)

**Authors**: Kuan Lu, Shuhang Lin, Sai Wu, Yichen Yao, Junhan Yang, Huan Li, Wei Chu, Xu Yinghui, Yuan Qi, Gang Chen  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15550v1  

#### Abstract
Large language models (LLMs) are increasingly applied in long-context scenarios such as multi-turn conversations. However, long contexts pose significant challenges for inference efficiency, including high memory overhead from Key-Value (KV) cache and increased latency due to excessive memory access...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CTKvR: Efficient KV Cache Retrieval for Long-Context LLMs via Centroid-then-Token Indexing

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在长上下文场景（如多轮对话、长文档问答）中面临显著的推理效率挑战，主要体现在：
- **高内存开销**：Key-Value (KV) cache 随序列长度线性增长，消耗大量 GPU 显存（VRAM），限制批处理大小（batch size）和吞吐量。
- **高延迟**：传统稀疏注意力方法在检索相关 KV 对时引入额外计算开销，尤其是基于 token-level 的近似最近邻（ANN）搜索。

现有方法存在权衡：
- **基于块的索引（block-level indexing）**：速度快但精度低，因块内可能包含无关 KV 条目。
- **基于 token 的索引（token-level indexing）**：精度高但检索机制效率低下，导致预填充（prefilling）和解码（decoding）阶段延迟显著增加。

### 提出的新方法：CTKvR
作者提出 **Centroid-then-Token KV Retrieval (CTKvR)**，一种新颖的两阶段 KV 检索方案，核心思想是利用 RoPE（Rotary Position Embedding）后相邻位置查询向量的高度相似性。

#### 创新点
1. **轻量级查询中心索引（Query-Centroid Inverted File, QcIVF）**：
   - 在预填充阶段，从长上下文中提取最后 `C` 个查询向量作为“质心”（centroids）。
   - 为每个质心预先计算其与所有 Key 向量的注意力分数，并存储其 top-p 最相似的 Key ID，构建 QcIVF 索引。
   - 该索引计算高效、存储开销小（仅约 20MB），支持大规模质心以提高准确性。

2. **两阶段检索机制**：
   - **第一阶段：Query-Centroid Indexing（粗粒度检索）**
     - 计算当前查询 `Q` 与所有质心的余弦相似度，选择最相似的 `C'` 个质心。
     - 根据这些质心对应的 QcIVF 条目，收集候选 Key ID 并去重，得到 `KRecall`。
   - **第二阶段：Fine-grained Token-Level Indexing（精炼检索）**
     - 在 `KRecall` 上计算精确的注意力分数，选出最终的 top-p' Key ID 用于注意力计算。
     - 此过程避免了对整个 KV cache 进行全量注意力计算。

3. **动态质心更新（Dynamic Centroid Update, DCU）**：
   - 在多轮对话或长输出生成中，当前查询与初始质心的相似度会下降。
   - CTKvR 引入异步更新机制，在每次迭代后将新发现的重要查询-Key 映射加入 QcIVF，并通过 FIFO 队列淘汰最旧条目，保持索引的相关性。

4. **系统级优化（CPU-GPU 协同执行）**：
   - 将大部分 KV cache 和部分注意力计算（特别是 Q2K Rerank）卸载到 CPU，利用 DRAM 扩展内存容量，支持更大 batch size。
   - 使用自定义 CUDA 内核和 CUDA 多流技术，实现 GPU 与 CPU 计算的重叠，最大化吞吐量。

### 相比现有方法的优势
| 方法 | 准确性 | 效率 | 内存占用 | 主要缺点 |
|------|--------|--------|------------|----------|
| **FULLKV** | 最高 | 低 | 高 | 显存瓶颈 |
| **Eviction-based (e.g., SNAPKV)** | 低 | 高 | 低 | 信息丢失严重 |
| **Block-level (e.g., QUEST, SHADOWKV)** | 中等 | 高 | 低 | 块内噪声影响精度 |
| **Token-level (e.g., MAGICPIG)** | 高 | 低 | 低 | ANN 构建/检索开销大 |
| **CTKvR (本文)** | **接近 FULLKV (<1% 降损)** | **极高** | **极低** | —— |

- **平衡了准确性和效率**：通过两阶段设计，在保证高精度的同时大幅降低检索开销。
- **可扩展性强**：适用于超长上下文（高达 1M tokens）。
- **兼容性好**：可与高效的预填充方法（如 MINFERENCE）结合使用。

---

## 2. 核心实验方法和设置

### 数据集
- **RULER**：一个可定制长度的长上下文基准，包含 13 个任务（检索、多跳追踪、聚合、问答），测试上下文长度从 8K 到 128K。
- **LongBench**：双语、多任务长上下文理解基准，包含 21 个数据集，涵盖单文档 QA、多文档 QA、摘要、少样本学习、代码补全等。
- **Needle-in-a-Haystack**：专门测试模型从长文档中检索关键信息的能力，上下文长度从 16K 到 1M。

### 实验设置
- **模型**：
  - Llama-3-8B-262K
  - Yi-9B-200K
- **上下文长度**：最高达 96K（系统效率评估）、128K 及 1M（准确性评估）。
- **硬件配置**：
  - GPU：A6000 (48GB), V100 (32GB), A6000 (24GB)
  - CPU：Intel Xeon w9-3495X (56 cores, 250GB DRAM)

### 评估指标
- **准确性**：任务特定指标（如 F1、Exact Match）的平均得分。
- **系统效率**：
  - **吞吐量（Throughput）**：每秒生成的 token 数（tokens/s）。
  - **最大批大小（Max Batch Size）**：在给定显存下的最大并发请求。
  - **索引构建时间**：QcIVF 构建耗时。

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **无压缩** | FULLKV |
| **缓存驱逐** | SNAPKV |
| **块级稀疏注意力** | QUEST, SHADOWKV |
| **token级稀疏注意力** | MAGICPIG, FLAT (exact KNN) |
| **其他** | VLLM (PagedAttention) |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **准确性**：
  - 在 RULER 和 LongBench 上，CTKvR 的性能损失小于 **1%**，与 FULLKV 相当。
  - 在 Needle-in-a-Haystack 任务中，CTKvR 成功检索到所有“needle”，即使在 1M tokens 上下文中也表现稳健。

- **吞吐量提升**：
  - 在 96K 上下文长度下：
    - **Llama-3-8B**：实现 **3× 吞吐加速**。
    - **Yi-9B**：实现 **4× 吞吐加速**。
  - 支持在仅 24GB VRAM 的 GPU 上运行 96K 上下文。

- **批大小扩展**：
  - 相比 FULLKV，CTKvR 支持：
    - **Llama-3-8B**：**10× 更大的 batch size**
    - **Yi-9B**：**20× 更大的 batch size**

- **索引构建效率**：
  - 相比 ANN 方法（HNSW, IVF, KMeans），CTKvR 的索引构建时间最多减少 **10000×**，且随上下文增长更稳定。

### 与基线方法的对比结果
| 方法 | 准确性 | 吞吐量 | 批大小 | 备注 |
|------|--------|--------|--------|------|
| **FULLKV** | ✅ 最优 | ❌ 最低 | ❌ 最小 | 基准 |
| **SNAPKV** | ❌ 严重下降 | ✅ 较高 | ✅ 较大 | 信息丢失 |
| **SHADOWKV / QUEST** | ⚠️ 中等下降 | ✅ 高 | ✅ 大 | 块内噪声 |
| **MAGICPIG** | ✅ 接近 FULLKV | ⚠️ 中等 | ⚠️ 中等 | ANN 开销大 |
| **CTKvR** | ✅ 接近 FULLKV (<1%) | ✅✅✅ **最高** | ✅✅✅ **最大** | **全面优势** |

> 注：在相同 sparsity ratio 下，CTKvR 准确性高于 MAGICPIG。

### 消融实验结果
- **Rerank 模块**：
  - 引入 Rerank 可进一步提升吞吐量 **高达 2×**，因其减少了最终注意力计算的 Key 数量。
- **动态质心更新（DCU）**：
  - 在多轮对话（Multi-turnNIAH）任务中，启用 DCU 后性能显著提升：
    - 第 8 轮准确率从 85.67% 提升至 **94.02%**。
- **参数敏感性分析**：
  - **维护的质心数 C**：增加 C 提升准确性，但在 C≈320 后趋于稳定；对吞吐量影响较小。
  - **检索的质心数 C'**：增加 C' 显著提升准确性（在 C'=5 后稳定），但会明显降低吞吐量（因 Rerank 时间增加）。
  - **稀疏预算 p'**：即使在极低预算（0.39%）下，CTKvR 仍能保持接近 FULLKV 的准确性。

---

## 4. 关键结论和发现

### 主要发现
1. **相邻查询高度相似**：在 RoPE 编码后，位置相近的查询向量具有高余弦相似性，且共享大部分 top-k Key。
2. **两阶段检索有效平衡效率与精度**：先用轻量级 centroid indexing 快速缩小搜索范围，再用 token-level reranking 精确筛选，实现了高效且准确的 KV 检索。
3. **CPU-GPU 协同可行且高效**：现代 CPU 的算力和带宽足以承担部分注意力计算，结合定制化优化可大幅提升系统吞吐。
4. **CTKvR 具有强鲁棒性和可扩展性**：在多种任务、不同模型、极端长上下文（1M tokens）下均表现出色。

### 方法的局限性
- **依赖 RoPE 结构**：核心洞察基于 RoPE 的位置编码特性，对其他位置编码方式（如绝对位置编码）的适用性需验证。
- **CPU 成为新瓶颈**：尽管 GPU 负载减轻，但 Q2K Rerank 和 CPU Attention 成为主要耗时操作（见 Table 13），未来可进一步优化 CPU 端计算。
- **动态更新机制复杂度**：DCU 增加了系统复杂性，需 careful management of QcIVF FIFO queue。

### 未来工作方向
- 探索更高效的 CPU 端注意力实现（如量化、向量化）。
- 将 CTKvR 思想应用于训练阶段的 KV cache 管理。
- 扩展至多模态 LLM 或其他 Transformer 架构。
- 研究自适应调整 `C`, `C'`, `p'` 等参数的策略，以动态匹配不同任务需求。

---

> **总结**：CTKvR 通过创新的 **centroid-then-token indexing** 机制，成功解决了长上下文 LLM 推理中的 KV cache 效率瓶颈，在几乎不损失精度的前提下，实现了 **3–4× 的吞吐提升** 和 **10–20× 的批大小扩展**，是长上下文推理领域的一项重要进展。

</details>

---

### 12. [How Many Heads Make an SSM? A Unified Framework for Attention and State Space Models](https://arxiv.org/abs/2512.15115)

**Authors**: Ali Ghodsi  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15115v1  

#### Abstract
Sequence modeling has produced diverse architectures -- from classical recurrent neural networks to modern Transformers and state space models (SSMs) -- yet a unified theoretical understanding of expressivity and trainability trade-offs remains limited. We introduce a unified framework that represen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*How Many Heads Make an SSM? A Unified Framework for Attention and State Space Models*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文旨在解决**序列建模领域中不同架构（如Transformer中的Attention机制与State Space Models, SSMs）之间缺乏统一理论理解**的问题。尽管这些模型在实践中广泛应用，但它们通常被孤立研究：

- **Attention类模型**（如Transformer）依赖显式的token-to-token交互；
- **SSM/RNN类模型**则通过隐式的递归动态和隐藏状态进行建模。

这种分离导致难以回答一些根本性问题，例如：
- 多头注意力（multi-head attention）是否只是集成技巧？
- 为什么输入依赖的权重（如Attention）在长上下文中训练更稳定，而输入无关的递归动态（如传统SSM）容易出现梯度消失？

---

### **提出了什么新方法或新思路**

作者提出了一种**统一框架（Unified Framework）**，将多种主流序列模型（包括MLP、CNN、RNN、SSM、Attention、KAN等）都表示为一个**输入依赖的有效交互算子 $W(X)$** 的不同实现方式。

在此框架下，所有模型都可以写成如下形式：
$$
\mathbf{y}_i = \sum_{j=1}^n W_{ij}(X) \mathbf{x}_j
$$
其中 $W_{ij}(X) \in \mathbb{R}^{p \times d}$ 是从输入 $\mathbf{x}_j$ 到输出 $\mathbf{y}_i$ 的有效线性映射，且依赖于整个输入序列 $X$。

基于此，作者区分了两类构造模式：

#### （1）**Unified Factorized Framework（显式交互）**
- 权重分解为标量得分与共享值矩阵的乘积：  
  $$
  W_{ij}(X) = f_o(\mathbf{x}_i, \mathbf{x}_j) \cdot V
  $$
- 包括：Softmax Attention、Linear Attention、KAN、CNN、MLP。
- 特点是“**scalar-factorized interaction**”，即所有交互操作都是对同一个矩阵 $V$ 进行加权。

#### （2）**Structured Dynamics（隐式动态）**
- 权重由潜在动力系统生成，如线性SSM：
  $$
  W_{ij} = C A^{i-j-1} B \quad (\text{for } j < i)
  $$
- 包括：S4、Mamba等。
- 可以生成非秩一（non-rank-one）的操作，具有更高的代数表达能力。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **理论统一性** | 首次将Attention与SSM置于同一数学框架下，揭示其本质差异与联系。 |
| **可解释性增强** | 明确指出multi-head attention不是简单ensemble，而是为了提升“interaction rank”以匹配高维动态系统。 |
| **指导架构设计** | 揭示了**表达力（expressivity）与可训练性（trainability）之间的根本权衡**，为混合架构（如Jamba）提供理论支持。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

论文未使用真实世界自然语言或视觉数据集，而是采用**合成任务（synthetic tasks）** 来验证理论预测：

- **系统识别任务（System Identification）**：用一个“教师”SSM生成目标输出，训练“学生”Attention模型去拟合它。
- **梯度流分析任务**：测量模型对远距离输入变化的敏感度（即Jacobians），用于验证梯度传播性质。

---

### **实验设置和评估指标**

#### ✅ 实验一：Interaction Rank Gap（定理4.4验证）

- **教师模型（Teacher）**：构造一个线性SSM，其脉冲响应矩阵 $\{W(\tau)\}$ 张成 $k$ 维子空间（控制$k=2,4,8$）。
- **学生模型（Student）**：multi-head linear attention 模型，head数 $H \in \{1,\dots,9\}$，带可学习位置编码。
- **目标**：最小化学生输出与教师输出之间的MSE。
- **评估指标**：测试集上的均方误差（Test MSE）。

#### ✅ 实验二：Gradient Flow Dynamics（定理5.1验证）

- 对比两个模型：
  - 固定参数的线性SSM（stable，$\|A\|_2 < 1$）
  - 多头Attention模型（标准初始化）
- 测量晚期输出 $\mathbf{y}_T$ 对第一个输入 $\mathbf{x}_1$ 的雅可比范数：$\left\|\frac{\partial \mathbf{y}_T}{\partial \mathbf{x}_1}\right\|$
- 序列长度 $T$ 从25到200变化，在半对数坐标下绘图。

---

### **基线方法对比**

| 模型 | 类型 | 是否作为Baseline |
|------|------|------------------|
| Linear SSM (e.g., S4-style) | Structured Dynamics | ✅ 是（用于梯度衰减实验） |
| Multi-head Linear Attention | Factorized Model | ✅ 是（用于rank-capacity实验） |
| Single-head Attention | Factorized Model | ❌ 否（作为理论下界） |

---

## 3. 主要实验结果和性能指标

### ✅ 实验一：Interaction Rank Gap（图3）

| 结果 | 数据/观察 |
|------|----------|
| **当 $H < k$ 时，test MSE 很高** | 表明模型无法充分表达 $k$ 维交互空间 |
| **当 $H \geq k$ 时，error显著下降并趋于稳定低值** | 支持定理4.4中“必要且充分”的结论 |
| **例如 $k=8$ 时，只有当 $H \geq 8$ 才进入低误差区** | 显示head count直接决定表达上限 |
| **SVD谱显示约4个主导奇异值（当 $k=4, H=4$）** | 表明heads确实学到了目标交互子空间的基 |

> 🔍 **关键发现**：multi-head attention的head数量 $H$ 必须至少等于SSM的interaction rank $k$ 才能精确表示该动态系统。

---

### ✅ 实验二：Gradient Flow Dynamics（图4）

| 模型 | 梯度衰减趋势 | 长程敏感性 |
|------|--------------|------------|
| **Linear SSM** | 指数级衰减（红线） | 随距离迅速减弱 |
| **Multi-head Attention** | 缓慢衰减（蓝线），保持较高水平 | 即使在 $T=200$ 仍保留较强信号 |

> 🔍 **关键发现**：Attention提供了“gradient highway”——一种不随距离衰减的短路径，有助于缓解长期依赖训练难题。

---

### ✅ 消融实验（隐含在理论证明中）

虽然没有独立章节命名“ablation”，但以下构成实质消融：

- **单头 vs 多头 Attention**：单头无法跨越Interaction Rank Gap，多头可以；
- **静态 vs 动态权重**：仅动态权重（如Attention）才能实现distance-independent梯度；
- **有无Positional Encoding**：添加PE后能更好拟合lag结构，说明位置信息的重要性。

---

## 4. 关键结论和发现

### 🧠 **主要发现**

1. **Interaction Rank Gap 定理（Theorem 4.2）**
   - 单头factorized模型（如single-head attention）只能生成所有交互算子都在一个固定矩阵 $V$ 的标量倍数方向上。
   - 而线性SSM的脉冲响应矩阵可能张成高维空间（如旋转矩阵例子），因此**单头Attention无法一致逼近这类系统**。

2. **Multi-head Equivalence 定理（Theorem 4.4）**
   - 要精确表示一个interaction rank为 $k$ 的线性SSM，multi-head attention必须满足：
     $$
     H \geq k
     $$
   - 并且当 $H \geq k$ 时，存在参数配置使得模型能完全复现SSM行为。
   - ⇒ **Head count 控制 interaction subspace 的维度上限**。

3. **Gradient Highway 定理（Theorem 5.1）**
   - 在稳定线性SSM中，输入-输出Jacobi矩阵范数随距离指数衰减（$\propto \|A\|^{T}$）；
   - 而Attention机制存在输入序列使得梯度范数**与距离无关**，形成“topological shortcut”。

4. **根本权衡（Trade-off）**
   - **SSM类模型**：高表达力（high interaction rank），但梯度易衰减（hard to train）；
   - **Attention类模型**：低秩限制（low interaction rank per head），但梯度传播强（easy to train）；
   - ⇒ **Hybrid架构（如Jamba）是对此trade-off的工程回应**。

---

### ⚠️ **方法的局限性**

| 局限 | 说明 |
|------|------|
| **仅适用于线性或局部线性模型** | 非线性RNN的Jacobians复杂，当前分析主要针对线性SSM和linear attention |
| **合成实验为主** | 尚未在大规模语言建模任务上验证理论预测 |
| **feature dimension可能很大** | 构造性证明中需要 $r=O(n)$ 的特征维度，实际应用需进一步压缩 |
| **未涵盖非因果/双向场景** | 分析集中在causal setting，扩展需调整 |

---

### 🔮 **未来工作方向**

1. **推广到非线性动态系统**：研究非线性SSM或RNN的interaction rank定义。
2. **设计最优head分配策略**：如何根据任务自动选择 $H$ 或动态分配heads。
3. **结合HiPPO/S4参数化**：利用频域结构降低feature dimension至 $O(m)$。
4. **应用于实际模型压缩与蒸馏**：用SSM作为“teacher”指导轻量Attention“student”。
5. **探索其他factorization形式**：超越标量因子化（scalar factorization），引入矩阵因子化提升效率。

---

## 总结

> 💡 **一句话总结**：  
> 本文提出了一个统一框架，揭示了Attention与SSM的本质区别在于**interaction rank**与**gradient propagation特性**，并证明：**multi-head attention中的head数量 $H$ 必须不少于SSM的interaction rank $k$ 才能完整表达其动态；而Attention凭借“gradient highway”在长程依赖训练中更具优势**。

这一理论为现代序列模型的设计（尤其是hybrid架构如Jamba）提供了坚实的数学基础。

</details>

---

### 13. [Metanetworks as Regulatory Operators: Learning to Edit for Requirement Compliance](https://arxiv.org/abs/2512.15469)

**Authors**: Ioannis Kalogeropoulos, Giorgos Bouritsas, Yannis Panagakis  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15469v1  

#### Abstract
As machine learning models are increasingly deployed in high-stakes settings, e.g. as decision support systems in various societal sectors or in critical infrastructure, designers and auditors are facing the need to ensure that models satisfy a wider variety of requirements (e.g. compliance with reg...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Metanetworks as Regulatory Operators: Learning to Edit for Requirement Compliance*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
随着机器学习模型在高风险场景（如决策支持系统、关键基础设施）中的广泛应用，确保模型满足**多样化要求**（如法规合规、公平性、计算效率等）变得至关重要。然而，传统方法面临以下挑战：
- **Post-processing** 方法常损害模型性能；
- **Fine-tuning 或重新训练** 成本高昂且可能因数据隐私或知识产权限制而不可行；
- 新要求往往在模型部署后才出现，导致“训练-需求”时间错配。

本文提出的核心问题是：  
> **能否在不牺牲模型效用的前提下，高效地编辑模型以满足新要求？**

### 提出的新方法与思路
作者提出一种**统一的、基于学习的神经网络编辑框架**，其核心思想是：
- 将模型编辑建模为一个**多目标优化问题**，目标函数包括：
  - **Preservation Objective**：保持原始模型行为不变（通过 JSD 衡量输出差异）；
  - **Requirement Objective**：满足特定合规要求（如数据最小化、公平性、稀疏性）。
- 引入 **Graph Metanetwork** 作为“可学习的编辑器”，直接从原始模型参数预测编辑后的参数。
- 编辑过程仅需一次前向推理（single inference step），实现**快速、灵活的模型修改**。

该方法的关键创新在于：
- **统一框架**：将不同类型的合规要求形式化为数学目标，适用于多种任务；
- **数据驱动编辑**：利用大量已训练模型（NN populations）训练 metanetwork，使其学会“如何编辑”；
- **无需再训练**：避免了耗时的 fine-tuning 或 retraining；
- **白盒访问下的通用工具**：可被设计者或审计员用于快速合规调整。

### 相比现有方法的优势
| 维度 | 传统方法（Post-processing / Retraining） | 本文方法（Metanetwork-based Editing） |
|------|----------------------------------------|----------------------------------------|
| 时间效率 | 高延迟（分钟到小时级） | **毫秒级编辑**（单次推理） |
| 性能保留 | 常显著下降 | 更好地平衡**功能保留与要求满足** |
| 可扩展性 | 每个模型独立处理 | 一个 metanetwork 可编辑同任务分布下的任意模型 |
| 灵活性 | 需为每种需求定制流程 | 支持多任务组合（如先去偏再剪枝） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **UCI Adult** 和 **Bank Marketing** 数据集，均为真实世界的表格数据 benchmark；
- 构建两个大规模的 **NN populations（Dm）**：各包含约 12,000 个 MLP 模型，覆盖不同架构、超参数和训练阶段；
- 数据分布记为 $ p_d $，模型参数分布记为 $ p_m $。

### 实验设置
- **模型类型**：MLP（全连接前馈网络）；
- **Metanetwork 架构**：基于 **Graph Metanetwork**（Lim et al., 2024），对参数空间的置换对称性（permutation symmetry）保持等变性；
- **训练方式**：无监督学习，目标是最小化加权损失：
  $$
  \mathcal{L} = \mathbb{E}_{(G,\theta)\sim p_m} \left[ \text{JSD}(f_{\theta}, f_{\hat{\theta}}) + \lambda \cdot r(\hat{G}, \hat{\theta}) \right]
  $$
  其中 $\lambda$ 控制两个目标之间的权衡。

### 评估指标
- **X-axis**：平均 requirement objective（越低越好）
  - 数据最小化：激活特征比例
  - 公平性：Equalized Odds Difference (EOD)
  - 剪枝：剩余权重比例
- **Y-axis**：平均 JSD（越低表示功能偏离越小）
- 绘制 **Pareto front** 曲线，展示性能与合规性的权衡关系。

### 基线方法对比
| 任务 | 基线方法 |
|------|---------|
| **数据最小化** | Feature Selection (FS), FS & Retrain (知识蒸馏) |
| **公平性提升** | ThresholdOpt, RejectOption, CalEqOdds, FairCls |
| **权重剪枝** | Random, Magnitude, Grad Importance, SNIP, Lottery Ticket |

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### （1）数据最小化（Data Minimization）
- 在 Adult 和 Bank 数据集上，**GMN** 显著优于所有基线；
- 在高掩码率（即保留更少特征）时优势更大，说明 metanetwork 能有效补偿信息缺失；
- **FS (GMN)**（仅预测 mask）也优于传统 FS，表明参数感知的 mask 更优。

> ✅ 图1显示：GMN 在相同 JSD 下能屏蔽更多特征，或在相同屏蔽比例下造成更小的功能漂移。

#### （2）偏差缓解（Bias Mitigation）
- GMN 在整个 Pareto front 上全面超越 ThresholdOpt、RejectOption 等经典 post-processing 方法；
- 达到更低的 EOD 同时保持更小的 JSD；
- 传统方法受限于硬约束（如 Equalized Odds），只能产生单一解；而 GMN 提供连续权衡曲线。

> ✅ 图2显示：GMN 实现了 **EOD < 0.01** 且 JSD ≈ 0.005，远超基线。

#### （3）权重剪枝（Weight Pruning）
- **GMN-Prune & Edit** 在高稀疏度下表现尤为出色；
- 即使只进行剪枝（GMN-Prune），也优于 Random、Magnitude、Grad Importance；
- 结合参数编辑后，在相同稀疏度下显著降低 JSD。

> ✅ 图3显示：当剩余权重比例为 0.2 时，GMN-Prune & Edit 的 JSD 比 SNIP 低约 50%。

### 时间效率对比（Table 1）
| 方法 | 平均编辑时间（秒） |
|------|------------------|
| FS & Retrain | 32.35 |
| RejectOption | 4.36 |
| SNIP | 0.41 |
| **GMN (本文)** | **0.03** |

> ⚡ GMN 比大多数基线快 **1–1000 倍**，仅略慢于最简单的随机剪枝。

### 消融实验结果
- **训练样本效率分析**（图4）：
  - 即使用 **10% 的训练数据**，GMN 仍优于多数基线；
  - 使用 **25% 数据** 即可接近最优性能；
  - 50% 以上提升有限，表明 metanetwork 具有良好的泛化能力。

- **组合多个 metanetwork**（Appendix A.7）：
  - 将分别训练的数据最小化（S_dm）和剪枝（S_pr）metanetwork 组合：$ \mathcal{S} = \mathcal{S}_{pr} \circ \mathcal{S}_{dm} $
  - 组合后的 metanetwork 依然主导 Pareto front，证明其**模块化与可组合性**；
  - 表明单个编辑不会破坏后续编辑所需的参数分布假设。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Metanetwork 可作为高效的“合规操作符”**：能够在部署后快速、低成本地使模型满足新要求。
2. ✅ **学习到的编辑优于手工规则或优化方法**：在性能保留与要求满足之间取得更好权衡。
3. ✅ **编辑过程高度高效**：单次推理即可完成，适合实时或批量合规审计。
4. ✅ **框架具有通用性和可扩展性**：同一范式可用于数据最小化、公平性、剪枝等多种任务。
5. ✅ **支持多目标组合编辑**：不同 metanetwork 可串联使用，构建复杂合规流水线。

### 方法的局限性
- ❗ 当前仅适用于 **同任务分布内的 MLP 模型**，难以跨任务迁移；
- ❗ 需要预先收集大量训练好的模型（$ p_m $）来训练 metanetwork；
- ❗ 目前未验证在复杂架构（如 CNN、Transformer）上的有效性；
- ❗ 完全自动化编辑存在风险，**仍需人类专家参与最终决策**（强调 human oversight）。

### 未来工作方向
- 扩展至 **多任务可学习编辑器**（multi-task learnable editor），支持跨任务泛化；
- 探索 **黑盒条件下的编辑方法**（当前依赖白盒参数访问）；
- 将 metanetwork 应用于更复杂的模型架构（如 Vision Transformer、LLMs）；
- 开发面向监管机构的 **自动化 AI 审计工具链**，集成此类 learned editor；
- 进一步研究 metanetwork 的**可解释性与安全性**，防止恶意编辑。

---

> 📌 **总结一句话**：  
> 本文提出了一个开创性的“**学会编辑**”（Learning to Edit）范式，利用 **Graph Metanetwork** 实现对神经网络的快速、灵活、高性能的合规性修改，为 AI 监管与可持续部署提供了强有力的工具基础。

</details>

---

### 14. [Multi-Modal Semantic Communication](https://arxiv.org/abs/2512.15691)

**Authors**: Matin Mortaheb, Erciyes Karakaya, Sennur Ulukus  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15691v1  

#### Abstract
Semantic communication aims to transmit information most relevant to a task rather than raw data, offering significant gains in communication efficiency for applications such as telepresence, augmented reality, and remote sensing. Recent transformer-based approaches have used self-attention maps to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Multi-Modal Semantic Communication》核心总结

---

## 1. 主要贡献和创新点

### ✅ 解决的问题
传统基于视觉自注意力（self-attention）的语义通信方法在复杂场景中存在以下局限：
- 在多物体、背景杂乱或语义模糊的图像中，**自注意力机制难以准确识别任务相关的语义区域**，容易聚焦于视觉显著但语义无关的内容；
- 当通信目标改变时（如从分类变为检测），需重新训练整个模型，**缺乏灵活性和实时适应能力**；
- 缺乏对用户意图的显式建模，导致传输内容与实际需求脱节。

### 🚀 提出的新方法与创新思路
本文提出了一种**Multi-Modal Semantic Communication（MMSC）框架**，其核心创新如下：

1. **引入文本查询作为引导信号**  
   利用用户提供的自然语言指令（text-based user query）来指导图像中关键区域的选择，实现“任务驱动”的语义提取。

2. **跨模态注意力机制（Cross-Modal Attention）**  
   融合视觉特征（来自CLIP-Vision）与语言嵌入（来自CLIP-Text），通过交叉注意力计算每个图像块相对于查询的**软相关性分数（soft relevance scores）**，而非硬分割掩码。

3. **动态分辨率编码策略**  
   根据通道带宽和语义重要性评分，将图像划分为patch，并分配不同分辨率等级进行编码。高相关性patch以更高比特率传输，低相关性则压缩更甚，总码率匹配信道容量。

4. **无需微调即可适应新任务**  
   模型在推理阶段直接处理任意自由形式的文本查询，无需为每个新任务重新训练或微调，极大提升实用性与泛化能力。

### 🔍 相比现有方法的优势
| 维度 | 本文 MMSC | 现有方法（如 ViT-SC [6]） |
|------|-----------|--------------------------|
| 输入模态 | 图像 + 文本查询（双模态） | 仅图像（单模态） |
| 注意力机制 | Cross-Modal Attention（图文融合） | Self-Attention（仅视觉内建） |
| 语义优先级控制 | 用户查询驱动，可变任务 | 固定任务，依赖预训练目标 |
| 灵活性 | 支持开放词汇、自然语言查询 | 需重新训练才能切换任务 |
| 输出表示 | Soft relevance map（连续值） | Hard attention / binary mask |
| 带宽利用效率 | 更优的任务相关保真度 | 易忽略非显著但语义重要的对象 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用 **COCO validation set** 的一个子集，共 **200张图像**。
- 所有图像统一调整为 **320×480 分辨率**。
- 构造两类用户查询：
  1. 单个标注对象类别（如 "cat"）
  2. 双对象组合查询（如 "cat and keyboard"）
- 提供对应的真实分割掩码（ground truth mask）用于评估语义一致性。

### ⚙️ 实验设置
- **Patch划分**：图像切分为 8×8 的非重叠块 → 共 2400 个 patch。
- **分辨率层级（L=5）**：
  - L=0: 不传输（0字节）
  - L=12, 24, 48: 压缩编码
  - L=192: 原始patch无损传输（3×8×8=192字节）
- **总带宽范围**：0 ~ 2400×192 ≈ 460,800 字节
- **语义提取模块**：基于 **MAFT+ [8]** 框架改进，使用 **ConvNeXt-Large** 版本的 OpenCLIP。
- **Embedding维度**：d = 1024
- **Mask Proposal数量**：N = 100（class-agnostic）

### 📊 评估指标
采用三种互补指标衡量重建图像是否保留了与用户查询相关的语义信息：

1. **Masked MSE**  
   在 ground truth 分割区域内计算原始图像与重建图像之间的均方误差，反映关键区域的保真度。

2. **L1 Distance of Informativeness Scores (Sinf)**  
   比较原始图像和重建图像分别与同一文本查询生成的 `Sinf` 地图之间的 L1 距离，衡量语义结构的一致性。

3. **CLIP Relevancy Score**  
   使用 CLIP-V 和 CLIP-T 分别编码图像和文本，计算嵌入向量的点积得分，评估图文语义对齐程度。

### 🔁 基线方法
- **ViT-SC [6]**：基于Vision Transformer的自注意力语义通信系统，仅使用图像输入，不接受文本查询。
- 对比条件：共享相同的 encoder-decoder 结构与多分辨率控制算法，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能表现（见 Fig. 4–6）

#### （1）Masked MSE（越低越好）
- 在所有带宽水平下，**MMSC 均显著低于 ViT-SC**。
- 例如，在约 200,000 Bytes 时，MMSC 的 masked MSE 约为 **0.015**，而 ViT-SC 接近 **0.03**。
- 表明 MMSC 更好地保留了查询相关区域的细节。

> ✅ **结论**：引入文本引导能有效提升关键区域的重建质量。

#### （2）L1 Distance of Sinf Maps（越小越好）
- MMSC 的 `Sinf` 差异始终小于 ViT-SC，尤其在中低带宽区间优势明显。
- 最大差距出现在 ~100K Bytes 处，MMSC 比 ViT-SC 低约 **40%**。
- 说明 MMSC 更好地维持了原始图像的语义重要性分布。

> ✅ **结论**：跨模态注意力使语义结构在压缩后仍保持稳定。

#### （3）CLIP Relevancy Score（越高越好）
- MMSC 在中等带宽范围内（~100K–300K Bytes）取得最高分（峰值达 **0.198**），优于 ViT-SC（最高约 **0.185**）。
- 高带宽时 MMSC 得分略有下降，推测因传输过多无关区域稀释了语义焦点。

> ✅ **结论**：MMSC 实现更强的图文语义对齐，特别是在资源受限条件下更具优势。

### 🔍 定性分析（Fig. 3 示例）
- 查询：“Cat and Keyboard”
- **MMSC** 成功重建猫和键盘两个对象，且两者均清晰可见；
- **ViT-SC** 主要关注猫（最显著对象），键盘几乎未被恢复；
- 进一步验证了**纯视觉注意力易忽略次要但语义重要的目标**，而多模态引导可克服此缺陷。

> ❌ **消融实验缺失说明**：文中未提供明确的 ablation study（如移除CDT、替换CLIP等），但强调了设计选择的合理性（如软评分 vs 硬掩码、条件化文本嵌入等）。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **文本查询可有效引导语义通信**  
   引入自然语言指令显著提升了系统对用户意图的理解能力，使得传输内容更加“目标导向”。

2. **跨模态注意力优于自注意力**  
   在复杂场景中，仅靠视觉自注意力无法可靠捕捉任务相关区域；结合语言先验的 cross-modal attention 能更精准定位语义重要内容。

3. **软相关性评分支持灵活编码决策**  
   使用 continuous relevance scores 而非 binary masks，允许精细化控制各patch的编码质量，更适合带宽受限环境下的资源分配。

4. **无需重训练即可支持多样化任务**  
   系统可在推理时动态响应任意文本命令，适用于远程监控、AR导航等需要快速任务切换的应用场景。

### ⚠️ 局限性
- **依赖高质量预训练模型**：性能高度依赖 CLIP 等大规模视觉-语言模型，部署成本较高；
- **未考虑延迟与实时性**：当前框架侧重压缩效率，尚未讨论端到端传输延迟或视频流扩展；
- **缺乏真实信道噪声建模**：实验假设理想带宽限制，未加入 AWGN 或衰落信道影响；
- **缺少消融研究**：未能量化各组件（如 CDT、Mask Proposal 数量）的具体贡献。

### 🔮 未来工作方向
- 扩展至 **video semantic communication**，支持时序语义一致性建模；
- 探索 **joint source-channel coding with feedback**，实现自适应码率调节；
- 引入 **lightweight multimodal backbones**，降低计算开销以适配边缘设备；
- 研究 **interactive querying** 机制，支持用户在接收端反馈并调整重建重点。

---

## 总结
该论文提出的 **Multi-Modal Semantic Communication (MMSC)** 框架首次将**文本查询与跨模态注意力**引入语义通信领域，实现了真正意义上的“按需传输”。实验证明，该方法在多种指标上全面超越仅依赖视觉自注意力的基线（ViT-SC），尤其在复杂场景和带宽受限条件下展现出卓越的语义保真能力和任务适应性，为下一代智能通信系统提供了重要范式。

</details>

---

### 15. [ValuePilot: A Two-Phase Framework for Value-Driven Decision-Making](https://arxiv.org/abs/2512.13716)

**Authors**: Yitong Luo, Ziang Chen, Hou Hei Lam, Jiayu zhan, Junqi Wang, Zhenliang Zhang, Xue Feng  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13716v1  

#### Abstract
Personalized decision-making is essential for human-AI interaction, enabling AI agents to act in alignment with individual users' value preferences. As AI systems expand into real-world applications, adapting to personalized values beyond task completion or collective alignment has become a critical...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ValuePilot: A Two-Phase Framework for Value-Driven Decision-Making**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
传统AI决策系统多基于**任务导向范式**（task-oriented paradigms），依赖外部奖励信号进行优化，缺乏对个体用户内在**价值偏好**（value preferences）的理解与建模。这导致AI行为难以适应个性化需求，尤其在面对未见过的新场景时泛化能力差。

此外，现有方法如**Reinforcement Learning from Human Feedback (RLHF)** 和 **Direct Preference Optimization (DPO)** 虽能对齐人类价值观，但通常依赖群体反馈，忽视个体差异；而规划模型如 **AutoPlan** 或 **ReAct** 则关注效率而非内在价值驱动。

因此，本文旨在解决以下两个核心挑战：
- 如何识别特定情境下相关的**价值维度**（value dimensions）
- 如何选择与用户个性化价值偏好一致的行动，并处理多个价值之间的权衡

---

### **提出了什么新方法或新思路**
作者提出 **ValuePilot**，一个两阶段框架，用于实现**以价值为导向的个性化决策**（value-driven personalized decision-making）。该框架由两个核心组件构成：

#### **(1) Dataset Generation Toolkit (DGT)**
- 基于大语言模型（LLM）构建了一个自动化生成流程，用于创建带有**细粒度价值标注**的决策场景数据集。
- 包含三个阶段：
  - **Prompt Construction and Scenario Generation**：给定一组目标价值维度（如 curiosity, safety），通过模块化提示引导 GPT-4 生成多智能体家庭场景。
  - **Action Generation and Value Scoring**：为每个场景生成候选动作，并用 [-1, +1] 连续分数标注其在各价值维度上的对齐程度（接近 +1 表示强烈支持，-1 表示违背）。
  - **Automatic Filtering via Re-evaluation**：使用另一轮 LLM 推理验证生成样本的价值一致性，剔除不匹配项。

#### **(2) Decision-Making Module (DMM)**
- 将决策建模为**多准则决策问题**（Multi-Criteria Decision-Making, MCDM），结合客观价值评估与主观用户偏好。
- 包括两个子模块：
  - **Value Assessment Network**：采用 T5 编码器将场景和动作文本编码，输出每个动作在各个价值维度上的客观得分。
  - **Action Selection Module**：
    - 引入**情境化评分机制**（Contextualized Scoring），融合用户的偏好向量 $ \mathbf{p} $ 与模型预测的动作价值得分。
    - 使用 **PROMETHEE 方法** 进行排序，计算净优势流（net outranking flow），生成可解释的行动优先级列表。

---

### **相比现有方法的优势**
| 维度 | ValuePilot 的优势 |
|------|------------------|
| **个性化建模** | 显式建模用户的价值偏好向量，支持个体化决策 |
| **可解释性** | 输出基于价值维度的评分与排序逻辑，优于黑箱式 LLM 决策 |
| **泛化能力** | 在未见场景中表现优异，说明其学习到了稳定的价值信号而非表面模式 |
| **系统性数据构建** | DGT 提供高质量、结构化的价值标注数据集，填补了该领域资源空白 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **自建数据集**：通过 DGT 构建，共包含：
  - **11,938 个场景**
  - **100,255 个候选动作**
- 数据集覆盖六个核心价值维度：
  - `Curiosity`, `Energy`, `Security`（原文中为 Safety）, `Happiness`, `Intimacy`, `Fairness`
- 场景设计为**家庭环境中的日常互动**，强调多智能体协作与冲突。
- 数据经过自动过滤 + 四人团队人工审核，确保真实性与价值一致性。

> 注：文中指出当前公开数据集（如 ALFWorld, InterCode, WVS, Moral Stories）均无法满足价值驱动决策训练需求。

---

### **实验设置和评估指标**

#### **实验一：Value Recognition（价值识别能力）**
- **任务**：判断模型能否准确识别场景中动作对应的价值得分。
- **基线模型**：
  - `Llama-3.5-70b`, `Llama-3.5-405b`, `Mixtral-8x22b`, `Gemini-1.5-flash`
- **评估指标**：
  - **Average Accuracy (AvgAcc)** @ 阈值 $ t=0.2 $ 和 $ t=0.05 $
  - **Mean Absolute Error (MAE)**

#### **实验二：Value-driven Decision-making（决策对齐能力）**
- **任务**：预测人类受试者在给定价值偏好的情况下会如何排序候选动作。
- **参与人数**：40 名人类被试
- **流程**：
  1. 受试者先填写六维价值重要性评分（0~1）
  2. 完成 11 个正式决策题目的动作排序
  3. 收集每个人的“真实”偏好向量 + 动作排名作为 ground truth
- **基线模型**：
  - `Llama-3.1-70b`, `DeepSeek-R1`, `Claude-Sonnet-4`, `Gemini-2.5-flash`, `Kimi-K2`, `GPT-4o-mini`, `GPT-5`
- **评估指标**：
  - **Order-Sensitive Similarity (OS-Sim)**：基于前缀交集的序列相似度，更重视高优先级动作的一致性。
  - **First-Action Accuracy (First-Acc)**：模型排名第一的动作是否与人类选择一致。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **价值识别任务结果（Table 1）**
| Model | AvgAcc (%) @ t=0.2 | AvgAcc (%) @ t=0.05 | MAE |
|-------|--------------------|---------------------|-----|
| Llama-3.5-70b | 40.90 | 17.74 | 0.30 |
| Mixtral-8x22b | 42.71 | 18.39 | 0.29 |
| Gemini-1.5-flash | 51.61 | 25.64 | 0.24 |
| **Value Assessment Network (Ours)** | **66.70** | **40.00** | **0.19** |

> ➤ 相比最强基线（Gemini），提升 **15.09 pp**（@t=0.2），MAE 下降 **36.7%**

---

#### ✅ **决策对齐任务结果（Figure 2）**
| Model | OS-Sim (%) | First-Acc (%) |
|-------|------------|---------------|
| GPT-5 | 69.23 ± 0.71 | 38.01 ± 3.81 |
| **DMM (Ours)** | **73.16 ± 0.43** | **46.14 ± 4.09** |

> ➤ 在 **OS-Sim** 上提升 **+3.93 pp**，在 **First-Acc** 上提升 **+8.13 pp**，显著优于所有主流 LLM。

---

### **消融实验结果（Ablation Study, Table 2）**

| 模型变体 | OS-Sim (%) | First-Acc (%) |
|--------|-----------|--------------|
| Only Action（仅动作） | 60.23 | 32.27 |
| w/o Preference（无偏好） | 61.07 | 31.82 |
| w/o Subjective（无主观调整） | 68.93 | 43.45 |
| w/o Scenario（无视情境） | 69.99 | 43.64 |
| **DMM (Full)** | **73.16** | **46.14** |

> ➤ 所有组件均有贡献，尤其是引入**用户偏好**和**情境调节**后性能大幅提升。

---

## **4. 关键结论和发现**

### **主要发现**
1. **价值驱动决策是可行且有效的路径**  
   显式建模人类基本价值维度（如 Schwartz’s theory）并结合个性化偏好，能够显著提升AI在复杂情境下的决策质量。

2. **ValuePilot 具备强泛化能力**  
   在未见场景上仍能优于大型 LLM，表明其学到的是稳定的“价值-行为”映射关系，而非记忆训练数据。

3. **可解释性与可控性强**  
   通过 PROMETHEE 方法生成的排序具有明确的数学依据，便于分析为何某个动作被优先推荐。

4. **DGT 是高质量数据生产的有效工具**  
   结合 LLM 自动生成 + 自动/人工双重过滤，可规模化生产带价值标注的决策数据，推动该方向研究。

---

### **方法的局限性（F Limitations）**
- 当前价值表示较为简化，未捕捉人类价值的**层次性、动态性和语境依赖性**。
- 用户偏好采集依赖直接打分（direct rating scales），可能不如心理测量学方法精确。
- 框架本身侧重工程集成，未深入探索更先进的价值学习或偏好推断算法。

---

### **未来工作方向**
- 扩展更多价值维度（如 responsibility, ambition）以覆盖更广泛的社会行为。
- 探索非显式方式（如对话交互）自动推断用户价值偏好。
- 将 ValuePilot 应用于真实世界场景，如智能家居助手、教育机器人等。
- 研究跨文化背景下的价值建模差异。

---

> 💡 **总体评价**：  
> ValuePilot 提出了一条通往**可解释、个性化、社会对齐 AI** 的清晰工程路径。它不仅展示了价值驱动决策的有效性，还提供了可复现的数据生成与建模范式，为后续研究奠定了坚实基础。

</details>

---

### 16. [Distillation-Guided Structural Transfer for Continual Learning Beyond Sparse Distributed Memory](https://arxiv.org/abs/2512.15267)

**Authors**: Huiyan Xue, Xuming Ran, Yaxin Li, Qi Xu, Enhui Li, Yi Xu, Qiang Zhang  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.15267v1  

#### Abstract
Sparse neural systems are gaining traction for efficient continual learning due to their modularity and low interference. Architectures such as Sparse Distributed Memory Multi-Layer Perceptrons (SDMLP) construct task-specific subnetworks via Top-K activation and have shown resilience against catastr...

---

### 17. [AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach](https://arxiv.org/abs/2512.13714)

**Authors**: Gangesh Pathak, Prasanna Kumar  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.13714v1  

#### Abstract
LLM implementations are failing in highly regulated industries owing to instability issues, inconsistent reasoning, hallucinations and performance variability, especially in workflows. These reliability issues restrict safe use of LLM in areas that need the precision of facts and consistent behavior...

---

### 18. [PortAgent: LLM-driven Vehicle Dispatching Agent for Port Terminals](https://arxiv.org/abs/2512.14417)

**Authors**: Jia Hu, Junqi Li, Weimeng Lin, Peng Jia, Yuxiong Ji, Jintao Lai  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.14417v1  

#### Abstract
Vehicle Dispatching Systems (VDSs) are critical to the operational efficiency of Automated Container Terminals (ACTs). However, their widespread commercialization is hindered due to their low transferability across diverse terminals. This transferability challenge stems from three limitations: high ...

---

### 19. [EMFusion: Conditional Diffusion Framework for Trustworthy Frequency Selective EMF Forecasting in Wireless Networks](https://arxiv.org/abs/2512.15067)

**Authors**: Zijiang Yan, Yixiang Huang, Jianhua Pei, Hina Tabassum, Luca Chiaraviglio  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.15067v1  

#### Abstract
The rapid growth in wireless infrastructure has increased the need to accurately estimate and forecast electromagnetic field (EMF) levels to ensure ongoing compliance, assess potential health impacts, and support efficient network planning. While existing studies rely on univariate forecasting of wi...

---

### 20. [SigMA: Path Signatures and Multi-head Attention for Learning Parameters in fBm-driven SDEs](https://arxiv.org/abs/2512.15088)

**Authors**: Xianglin Wu, Chiheb Ben Hammouda, Cornelis W. Oosterlee  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.15088v1  

#### Abstract
Stochastic differential equations (SDEs) driven by fractional Brownian motion (fBm) are increasingly used to model systems with rough dynamics and long-range dependence, such as those arising in quantitative finance and reliability engineering. However, these processes are non-Markovian and lack a s...

---

### 21. [RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees](https://arxiv.org/abs/2512.14069)

**Authors**: Junjie Ma, Jinlong Li  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14069v1  

#### Abstract
Inference with modern Large Language Models (LLMs) is expensive and slow, and speculative sampling has emerged as an effective solution to this problem, however, the number of the calls to the draft model for generating candidate tokens in speculative sampling is a preset hyperparameter, lacking fle...

---

### 22. [How a Bit Becomes a Story: Semantic Steering via Differentiable Fault Injection](https://arxiv.org/abs/2512.14715)

**Authors**: Zafaryab Haider, Md Hafizur Rahman, Shane Moeykens, Vijay Devabhaktuni, Prabuddha Chakraborty  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14715v1  

#### Abstract
Hard-to-detect hardware bit flips, from either malicious circuitry or bugs, have already been shown to make transformers vulnerable in non-generative tasks. This work, for the first time, investigates how low-level, bitwise perturbations (fault injection) to the weights of a large language model (LL...

---

### 23. [Deep Learning and Elicitability for McKean-Vlasov FBSDEs With Common Noise](https://arxiv.org/abs/2512.14967)

**Authors**: Felipe J. P. Antunes, Yuri F. Saporito, Sebastian Jaimungal  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14967v1  

#### Abstract
We present a novel numerical method for solving McKean-Vlasov forward-backward stochastic differential equations (MV-FBSDEs) with common noise, combining Picard iterations, elicitability and deep learning. The key innovation involves elicitability to derive a path-wise loss function, enabling effici...

---

### 24. [A Regime-Aware Fusion Framework for Time Series Classification](https://arxiv.org/abs/2512.15378)

**Authors**: Honey Singh Chauhan, Zahraa S. Abdallah  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.15378v1  

#### Abstract
Kernel-based methods such as Rocket are among the most effective default approaches for univariate time series classification (TSC), yet they do not perform equally well across all datasets. We revisit the long-standing intuition that different representations capture complementary structure and sho...

---

### 25. [Blind Radio Mapping via Spatially Regularized Bayesian Trajectory Inference](https://arxiv.org/abs/2512.13701)

**Authors**: Zheng Xing, Junting Chen  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13701v1  

#### Abstract
Radio maps enable intelligent wireless applications by capturing the spatial distribution of channel characteristics. However, conventional construction methods demand extensive location-labeled data, which are costly and impractical in many real-world scenarios. This paper presents a blind radio ma...

---

### 26. [Compressed Causal Reasoning: Quantization and GraphRAG Effects on Interventional and Counterfactual Accuracy](https://arxiv.org/abs/2512.13725)

**Authors**: Steve Nwaiwu, Nipat Jongsawat, Anucha Tungkasthan  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13725v1  

#### Abstract
Causal reasoning in Large Language Models spanning association, intervention, and counterfactual inference is essential for reliable decision making in high stakes settings. As deployment shifts toward edge and resource constrained environments, quantized models such as INT8 and NF4 are becoming sta...

---

### 27. [OpenDataArena: A Fair and Open Arena for Benchmarking Post-Training Dataset Value](https://arxiv.org/abs/2512.14051)

**Authors**: Mengzhang Cai, Xin Gao, Yu Li, Honglin Lin, Zheng Liu, Zhuoshi Pan, Qizhi Pei, Xiaoran Shang, Mengyuan Sun, Zinan Tang, Xiaoyang Wang, Zhanping Zhong, Yun Zhu, Dahua Lin, Conghui He, Lijun Wu  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14051v1  

#### Abstract
The rapid evolution of Large Language Models (LLMs) is predicated on the quality and diversity of post-training datasets. However, a critical dichotomy persists: while models are rigorously benchmarked, the data fueling them remains a black box--characterized by opaque composition, uncertain provena...

---

### 28. [Grammar Search for Multi-Agent Systems](https://arxiv.org/abs/2512.14079)

**Authors**: Mayank Singh, Vikas Yadav, Shiva Krishna Reddy Malay, Shravan Nayak, Sai Rajeswar, Sathwik Tejaswi Madhusudhan, Eduardo Blanco  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14079v1  

#### Abstract
Automatic search for Multi-Agent Systems has recently emerged as a key focus in agentic AI research. Several prior approaches have relied on LLM-based free-form search over the code space. In this work, we propose a more structured framework that explores the same space through a fixed set of simple...

---

### 29. [Adversarial versification in portuguese as a jailbreak operator in LLMs](https://arxiv.org/abs/2512.15353)

**Authors**: Joao Queiroz  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.15353v1  

#### Abstract
Recent evidence shows that the versification of prompts constitutes a highly effective adversarial mechanism against aligned LLMs. The study 'Adversarial poetry as a universal single-turn jailbreak mechanism in large language models' demonstrates that instructions routinely refused in prose become e...

---

### 30. [Is GPT-OSS All You Need? Benchmarking Large Language Models for Financial Intelligence and the Surprising Efficiency Paradox](https://arxiv.org/abs/2512.14717)

**Authors**: Ziqian Bi, Danyang Zhang, Junhao Song, Chiung-Yi Tseng  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14717v1  

#### Abstract
The rapid adoption of large language models in financial services necessitates rigorous evaluation frameworks to assess their performance, efficiency, and practical applicability. This paper conducts a comprehensive evaluation of the GPT-OSS model family alongside contemporary LLMs across ten divers...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
