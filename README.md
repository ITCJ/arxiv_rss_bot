# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-30 06:16:10 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Causal Autoregressive Diffusion Language Model](https://arxiv.org/abs/2601.22031)

**Authors**: Junhao Ruan, Bei Li, Yongjing Yin, Pengcheng Huang, Xin Chen, Jingang Wang, Xunliang Cai, Tong Xiao, JingBo Zhu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2601.22031v1  

#### Abstract
In this work, we propose Causal Autoregressive Diffusion (CARD), a novel framework that unifies the training efficiency of ARMs with the high-throughput inference of diffusion models. CARD reformulates the diffusion process within a strictly causal attention mask, enabling dense, per-token supervisi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Causal Autoregressive Diffusion Language Model

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前主流的 **Autoregressive Models (ARMs)** 虽然训练稳定且数据效率高，但由于其**逐token生成机制**，在推理时存在严重的**顺序瓶颈**，导致吞吐量低。  
而现有的 **Text Diffusion Models**（如 MDLM、BD3LM）虽然支持并行解码，提升推理速度，但也面临以下问题：
- **MDLM** 使用双向注意力（Full Attention），无法利用 **KV Cache**，实际推理速度仍受限；
- **BD3LM** 引入块级扩散（Block Diffusion），虽恢复部分并行能力，但带来显著的训练开销（复杂掩码、输入复制等），训练延迟高达 ARM 的 3×。

此外，这些方法在训练效率、动态并行性和上下文建模方面均存在不足。

---

### 提出了什么新方法或新思路
本文提出 **CARD (Causal Autoregressive Diffusion)** 框架，将 ARMs 的高效训练与扩散模型的并行推理优势统一于一个**严格因果架构**中。

#### 核心创新点：
1. **Causal Diffusion Formulation**  
   在标准 AR 架构基础上引入连续时间噪声调度，在每一步仅基于**前面被破坏的上下文**预测原始 token，实现单次前向传播即可获得全序列密集监督。

2. **Soft Tail Masking**  
   为解决因果结构下早期 token 易因上下文缺失而导致“信息坍缩”（Information Collapse）的问题，提出一种软尾部掩码策略：只在序列尾部的一个动态窗口内进行掩码，保留前缀清洁以维持历史信号，同时允许局部混合状态以保持语言局部依赖。

3. **Context-aware Reweighting**  
   设计了一种基于上下文模糊度的动态损失重加权机制，从三个维度评估上下文质量：
   - **Quantity**（噪声数量）
   - **Distance**（距离目标的距离）
   - **Density**（连续掩码密度）  
   并据此计算 `S_focal` 得分，用于对高熵上下文下的预测降权，从而提高优化稳定性。

4. **Confidence-based Block Inference**  
   支持动态并行解码：通过置信度阈值控制每次并行解码的 block 大小，高置信时多 token 并行输出，低置信时退化为自回归模式，实现灵活高效的推理加速。

---

### 相比现有方法的优势
| 维度 | CARD vs. ARM | CARD vs. MDLM / BD3LM |
|------|---------------|------------------------|
| **训练效率** | ≈ ARM，无额外开销 | ↓ 3× 训练延迟（vs. BD3LM） |
| **推理速度** | ↑ 1.7× ~ 4.0× 加速（wall-clock） | 更快，支持 KV Cache + 动态并行 |
| **生成质量** | ≈ ARM，优于所有 diffusion 基线 | 显著优于 MDLM/BD3LM |
| **数据利用率** | 更高数据潜力，多轮训练不饱和 | 避免早期饱和，适合数据复用场景 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **预训练数据**：FineWeb 子集，共 **300B tokens**
- **微调/评估任务**：
  - **常识推理**：ARC-Challenge/Easy, CommonsenseQA, HellaSwag, Winogrande
  - **学科知识**：MMLU-redux
  - **日常问答**：PIQA, SciQ
  - **语言建模质量**：WikiText, OpenWebText, AG News, LM1B, PTB, arXiv, PubMed, LAMBADA（零样本 PPL 测试）

---

### 实验设置和评估指标
- **模型规模**：统一为 **1B 参数**
- **训练步数**：最多 1M 步
- **评估方式**：
  - **下游任务准确率**（zero-shot setting）
  - **零样本困惑度（Zero-shot PPL）**
  - **训练延迟对比**（latency normalized to ARM = 1.0×）
  - **推理吞吐量（tokens/sec）与 PPL 权衡分析**

- **关键技术细节**：
  - 使用 Flash Attention 2 提升效率
  - BD3LM 使用 torch.compile + Flex Attention 优化
  - 所有 diffusion 方法采用线性噪声调度（linear schedule）

---

### 基线方法对比
| 模型 | 类型 | 特点 |
|------|------|------|
| **ARM** | 自回归模型 | 当前主流范式，训练高效但推理慢 |
| **MDLM** | 掩码离散扩散 | 双向注意力，简化训练目标，但无法使用 KV Cache |
| **BD3LM** | 块级扩散 | 混合因果+块内双向，支持部分并行，但训练成本高 |
| **CARD (Ours)** | 因果扩散 | 严格因果结构，支持 KV Cache 与动态并行 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表1：下游任务平均准确率（%）
| Model | AVG |
|-------|-----|
| ARM | 56.39 |
| BD3LM | 47.45 |
| MDLM | 47.55 |
| **CARD (Ours)** | **53.23** |

> ✅ CARD 比现有 diffusion 基线高出 **>5.7 个百分点**，接近 ARM 水平。

#### 表2：跨领域零样本 PPL（越低越好）
| Model | AVG PPL |
|-------|---------|
| ARM | 38.68 |
| BD3LM | 49.78 |
| MDLM | 49.84 |
| **CARD (Ours)** | **34.40** |

> ✅ CARD 在 **8个测试域中的6个上超越 ARM**，尤其在长上下文（LAMBADA）、通用文本（WikiText）表现优异。

#### 表3：小模型 PPL 对比（110M, 33B tokens）
| Model | PPL |
|-------|-----|
| ARM | 21.12 |
| BD3LM | 35.06 |
| MDLM | 37.48 |
| **CARD** | **21.54** |

> ✅ 即使使用 EMA，CARD 仍能逼近 ARM 性能，说明其训练更稳定。

---

### 与基线方法的对比结果
| 指标 | 结果 |
|------|------|
| **训练延迟** | CARD ≈ ARM（1.0×），仅为 BD3LM 的 **1/3** |
| **推理速度** | 达到 **1.7× ~ 4.0×** 的 wall-clock 加速（取决于 block size 和 step limit） |
| **生成质量** | 在 1.7× 加速下 PPL 仅轻微上升；4× 加速下略有下降但仍可用 |
| **数据效率** | 多 epoch 训练中持续提升，避免 ARM 的早期饱和现象 |

---

### 消融实验结果（表4）

| 设置 | AVG 准确率 |
|------|-----------|
| **CARD (完整版)** | **53.21** |
| w/o Relaxed Window (Strict Tail) | 52.50 |
| w/o Tail Preference (Random) | 51.62 |
| w/o Context-aware Weighting | 51.66 |

> 🔍 发现：
- 尾部偏好（Tail Preference）至关重要，随机掩码严重损害性能；
- “宽松窗口”（Relaxed Window）优于“严格尾部”，说明需要保留局部清洁锚点；
- 上下文感知重加权机制有效提升了学习稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **CARD 成功弥合了 AR 与 diffusion 的鸿沟**：
   - 实现了 **ARM 级别的训练效率** 与 **diffusion 级别的并行推理能力**；
   - 在保持高质量生成的同时，显著降低训练成本。

2. **因果扩散是可行且高效的路径**：
   - 通过 Soft Tail Masking 和 Context-aware Reweighting，解决了传统因果结构下训练不稳定的问题；
   - 首次实现了在纯因果框架下的高效离散扩散训练。

3. **更高的数据潜力（Data Potential）**：
   - CARD 在重复训练中不会像 ARM 那样快速饱和，更适合当前“数据稀缺、算力充足”的训练范式；
   - 在有限数据上反复训练仍可持续提效。

4. **支持动态并行解码**：
   - 利用 KV Cache 和 confidence-based block sampling，可在不同负载下灵活调整推理速度与质量平衡。

---

### 方法的局限性
- **极端加速设置下可能出现重复或循环生成**（见 Appendix D），尤其是在 step budget 不足时；
- 当前设计依赖良好的置信度估计，若模型不确定性校准不佳可能影响性能；
- 虽然优于现有 diffusion 方法，但仍略逊于最强 ARM 在个别任务上的表现。

---

### 未来工作方向
- 探索更精细的噪声调度与掩码策略；
- 引入更强的不确定性估计模块以优化动态解码；
- 扩展至更大规模模型（>10B）验证泛化性；
- 结合 instruction tuning 和 alignment 技术，探索 CARD 在对话与指令遵循任务中的应用。

---

> 📌 **总体评价**：  
> CARD 是一项重要的架构创新，它打破了“ARM 高效训练 vs. diffusion 快速推理”的二元对立，提出了一个兼具二者优点的新范式。其设计理念清晰、工程实现务实，实验证据充分，有望成为下一代高效 LLM 的重要候选方案。

</details>

---

### 2. [NetMamba+: A Framework of Pre-trained Models for Efficient and Accurate Network Traffic Classification](https://arxiv.org/abs/2601.21792)

**Authors**: Tongze Wang, Xiaohui Xie, Wenduo Wang, Chuyi Wang, Jinzhou Liu, Boyan Huang, Yannan Hu, Youjian Zhao, Yong Cui  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2601.21792v1  

#### Abstract
With the rapid growth of encrypted network traffic, effective traffic classification has become essential for network security and quality of service management. Current machine learning and deep learning approaches for traffic classification face three critical challenges: computational inefficienc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《NetMamba+: A Framework of Pre-trained Models for Efficient and Accurate Network Traffic Classification》核心总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前网络流量分类面临三大挑战：
1. **计算效率低下**：基于Transformer的模型因自注意力机制具有二次复杂度，在处理长序列时计算和内存开销巨大，难以满足实时在线分类需求。
2. **流量表征不充分**：现有方法在提取特征时容易丢失字节级关键信息（如包头字段），同时引入不必要的偏差（如分块方式导致语义断裂）。
3. **对长尾分布数据适应差**：真实世界流量数据类别分布极不均衡（长尾分布），传统微调策略对少数类样本学习不足。

### **提出的新方法与创新思路**
NetMamba+ 是首个将 **Mamba 架构**应用于网络流量分类的预训练框架，通过以下三项核心技术应对上述挑战：

1. **高效模型架构设计**
   - 采用 **Mamba**（线性时间状态空间模型）作为主干，替代Transformer，实现 O(L) 时间复杂度。
   - 或集成 **Flash Attention** 优化的 Transformer（NetTrans），提升传统注意力机制的IO效率。
   - 引入 **pre-normalization** 和 **GeGLU-activated FFN** 提高训练稳定性和准确率。

2. **多模态流量表征方案（Multimodal Traffic Representation）**
   - 同时保留 **包头（header）** 和 **载荷（payload）** 字节信息。
   - 引入 **stride-based 切割** 替代传统的 patch 分块，避免无关字节被强行关联。
   - 补充 **包大小序列（packet sizes）** 和 **到达间隔时间（inter-arrival times）** 作为额外模态，捕捉传输模式。

3. **标签分布感知微调策略（Label Distribution-Aware Fine-tuning, LDA）**
   - 提出一种新型交叉熵损失函数（LDA CE Loss），为少数类分配更高权重并施加更大分类边界（margin），缓解长尾问题。

### **相比现有方法的优势**
- **更高效**：推理吞吐量达最优基线的 **1.7×**，且GPU内存占用低。
- **更准确**：在多个任务上F1-score最高提升 **6.44%**。
- **更强泛化能力**：支持少样本学习（few-shot learning）和异常流量检测（OOD detection）。
- **首次实现Mamba在流量分析中的成功应用**，开辟了新的研究路径。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
| 类型 | 数据集 | 下游任务 | 特点 |
|------|--------|----------|------|
| **预训练** | Browser [2], Kitsune [52] | — | 包含大量未标注加密流量 |
| **微调** | CipherSpectrum [53], CSTNET-TLS1.3 [9], CICIoT2022 [56], USTC-TFC2016 [57], ISCXVPN2016 [58] 等共11个 | 应用分类、攻击识别、恶意软件检测、VPN协议识别等 | 覆盖多种协议（TLS, SSH, QUIC等）、部分加密、存在严重类别不平衡 |

> 注：多数微调数据集设置了每类最大流数（MFC=2000）以控制类别不平衡。

### **实验设置与评估指标**

#### **模型变体**
- `NetTrans`：仅使用 Flash Attention 优化的 Transformer 块。
- `NetMamba`：仅使用单向 Mamba 块。
- `NetMamba+`：结合多模态输入（字节 + 大小 + 间隔）的完整版本。

#### **训练配置**
- 预训练：15万步，batch size=128，AdamW优化器，学习率1e-3。
- 微调：120轮，batch size=64，学习率2e-3。
- 硬件：Ubuntu服务器 + NVIDIA A100 ×4。

#### **评估指标**
- **分布内性能（ID）**：Accuracy (AC), Precision (PR), Recall (RC), F1-score（加权）
- **分布外检测（OOD）**：AUROC, FPR95
- **效率指标**：推理吞吐量（samples/sec）、GPU内存消耗（MB）
- **少样本学习**：使用10%~100%标注数据进行训练测试

#### **对比基线方法**
| 类别 | 方法 |
|------|------|
| 传统机器学习 | AppScanner [3], FlowPrint [2] |
| 深度学习（监督） | Seq2Img [60], FS-Net [4], FlowPic [61], TFE-GNN [6] |
| Transformer预训练 | ET-BERT [9], YaTC [10], TrafficFormer [63] |
| 架构变体 | NetTransV（原生Transformer）、NetTransL（线性Attention）、NetMambaB（双向Mamba）、NetMambaC（级联Mamba） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **最高F1提升** | 较SOTA提升高达 **6.44%**（见CipherSpectrum） |
| **平均推理吞吐量** | 较最佳基线（YaTC）提升 **1.7×** |
| **GPU内存使用** | 显著低于大多数深度模型（如ET-BERT、TFE-GNN） |
| **少样本表现** | 在仅10%标签数据下仍优于其他预训练模型 |
| **OOD检测性能** | 四项任务AUROC均 > **94%**，最高达 **98.25%** |
| **在线系统吞吐量** | 实现 **261.87 Mb/s** 平均吞吐，验证实用性 |

### **与基线方法对比结果**

#### ✅ **总体性能领先**
- 在所有公开数据集上，`NetMamba+` 取得 **最好或接近最好** 的分类精度。
- 尤其在完全加密流量（如CipherSpectrum）中优势显著，因其融合了传输模式（size/interval）弥补了payload信息缺失。

#### ✅ **效率显著提升**
- 推理速度远超Transformer类模型（ET-BERT、TrafficFormer）及GNN模型（TFE-GNN）。
- 内存使用优于除NetTransL和FS-Net外的所有模型，兼顾高效与高性能。

#### ✅ **消融实验结果（Ablation Studies）**

| 组件移除 | 影响说明 |
|--------|---------|
| **无Header** | 性能大幅下降（ACC↓14.86%~47.50%），证明包头字段（如TCP flags、window size）至关重要 |
| **无Payload** | 表现不稳定，部分任务略升，表明在某些场景可牺牲payload换取效率 |
| **Stride Cutting → Patch Splitting** | 最多降2.27%，验证stride切割更符合网络字节顺序，减少偏见 |
| **无预训练** | 多数任务性能下降，凸显自监督预训练对泛化的重要性 |
| **多模态输入消融** | 单独使用“字节”或“大小”效果次优；三者融合（All）达到最优 |
| **LDA微调关闭** | 在长尾数据集（如Huawei-VPN）上ACC最多下降 **0.79%**，F1下降 **0.85%**，证实LDA有效 |

> 图7显示CP-iOS数据集过滤前后仍存在明显类别不平衡，进一步支持LDA必要性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Mamba适用于网络流量建模**：单向Mamba在保持线性复杂度的同时，性能媲美甚至超越Transformer，是高效流量分析的理想选择。
2. **多模态输入至关重要**：仅依赖原始字节不足以应对现代加密流量；加入包大小和时间间隔可显著增强表征能力。
3. **stride-based切割优于patch分块**：更贴合网络流量的序列特性，避免图像化处理带来的语义偏差。
4. **LDA微调有效缓解长尾问题**：无需重采样即可提升对稀有类别的识别能力。
5. **预训练极大提升少样本性能**：NetMamba+在极少量标注数据下仍保持高准确率，适合实际部署。

### **方法的局限性**
- **对分布偏移敏感**：在时间维度上的分布漂移（distribution shift）会导致性能下降（如CSTNET-TLS1.3准确率下降8.47%）。
- **依赖高质量流量切分**：需正确解析5元组并截取前若干包，对异常或碎片化流量鲁棒性待验证。
- **未探索更大规模模型**：当前参数量较小（<3M），是否能扩展至百亿级尚不明确。

### **未来工作方向**
- 增强模型对 **时间分布漂移** 的鲁棒性（例如引入持续学习机制）。
- 探索 **更大规模NetMamba++** 模型用于通用网络基础模型（Network Foundation Model）。
- 扩展至更多下游任务：如服务质量预测（QoS）、网络性能诊断、入侵响应生成等。
- 进一步优化在线系统延迟，适配边缘设备部署。

---

> 📌 **一句话总结**：  
> **NetMamba+ 是首个将 Mamba 架构引入网络流量分类的工作，通过高效的序列建模、多模态表征与标签感知微调，在准确性、效率与泛化性上全面超越现有SOTA方法，为下一代智能网络分析提供了新范式。**

</details>

---

### 3. [Hybrid Linear Attention Done Right: Efficient Distillation and Effective Architectures for Extremely Long Contexts](https://arxiv.org/abs/2601.22156)

**Authors**: Yingfa Chen, Zhen Leng Thai, Zihan Zhou, Zhu Zhang, Xingyu Shen, Shuo Wang, Chaojun Xiao, Xu Han, Zhiyuan Liu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2601.22156v1  

#### Abstract
Hybrid Transformer architectures, which combine softmax attention blocks and recurrent neural networks (RNNs), have shown a desirable performance-throughput tradeoff for long-context modeling, but their adoption and studies are hindered by the prohibitive cost of large-scale pre-training from scratc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Hybrid Linear Attention Done Right: Efficient Distillation and Effective Architectures for Extremely Long Contexts*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Transformer** 模型依赖 **softmax attention**，其计算复杂度为 $O(T^2)$（$T$ 为上下文长度），在处理 **长上下文（long-context）** 任务时效率极低。虽然 **RNN-based 模型**（如 linear attention、state space models）具有线性复杂度 $O(T)$，推理速度更快，但在 **recall-intensive 任务**（如“大海捞针”NIAH）上表现不佳。

混合架构（**hybrid architecture**）结合了 **attention 层** 和 **RNN 层**，试图在性能与效率之间取得平衡。然而，这类模型通常需要从头大规模预训练，成本高昂，限制了学术界的研究。此外，现有的将预训练 Transformer 蒸馏为 hybrid 模型的方法存在两大问题：
1. 仍需数十亿甚至数百亿 token 的训练数据；
2. 在长上下文任务上性能严重下降，违背了 hybrid 架构的设计初衷。

---

### 提出的新方法与创新点

本文提出 **HALO**（Hybrid Attention via Layer Optimization）和 **HypeNet**，系统性地解决了上述问题。

#### （1）HALO：高效跨架构蒸馏流程
- **目标**：将已有的预训练 Transformer 模型（如 Qwen3）高效地转换为高性能 hybrid 模型。
- **创新点**：
  - **高效的注意力层选择机制**：通过衡量替换某一层为 RNN 后对 **recall 性能** 和 **常识推理（CSR）性能** 的影响，设计重要性评分函数，优先保留对 recall 至关重要的 attention 层。
  - **三阶段蒸馏流程**：
    1. **初始化（Attention Weight Transfer）**：将 attention 层参数映射到 RNN 层。
    2. **隐藏状态对齐（Hidden State Alignment）**：独立训练每个 RNN 层以匹配原 attention 层输出。
    3. **端到端知识蒸馏（Knowledge Distillation）**：以原始 Transformer 为 teacher，进行完整模型蒸馏。
    4. **微调（Finetuning）**：在更长上下文上微调以提升长文本能力。
- **优势**：整个过程仅需 **2.3B tokens**，远低于现有方法（如 Jet-Nemotron 需 400B tokens）。

#### （2）HypeNet：高性能混合架构
- **目标**：设计一个在长上下文下具备强泛化能力的 hybrid 架构。
- **核心创新**：
  - **HyPE（Hybrid Positional Encoding）**：
    - **RNN 层使用 RoPE**：注入丰富的位置信息，提升建模能力。
    - **attention 层使用 NoPE**：利用 NoPE 更好的训练后长度外推（length generalization）能力。
    - 结合了 RoPE 的表达力与 NoPE 的泛化力。
  - **动态注意力缩放（Dynamic Attention Scaling）**：在推理时对 attention logits 进行位置相关缩放 $s_t = \log(t + a)$，缓解长序列熵增问题。
  - **其他架构改进**：
    - **QK-Normalization**：引入到 RNN 层中。
    - **GQA to MHA**：解除 RNN 层中 K/V 共享，增强表达能力。
    - **Output Gate**：在 attention 和 RNN 层均加入门控机制。

---

### 相比现有方法的优势
| 方法 | 所需训练 tokens | 是否支持长上下文 | 是否开源 |
|------|----------------|------------------|----------|
| 从头预训练 hybrid | >100B | ✅ | ❌（多数闭源） |
| Jet-Nemotron (Gu et al., 2025) | 400B | ❌（性能骤降） | ✅ |
| KL-LS (Li et al., 2025) | 25B | ❌ | ✅ |
| **HALO (ours)** | **2.3B** | ✅ | ✅ |

- **数据效率高**：仅用 2.3B tokens，不足 Qwen3 预训练数据的 0.01%。
- **长上下文性能优越**：在 NIAH 等任务上显著优于现有蒸馏方法。
- **完全开源**：代码与模型均已公开。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：FineWeb-edu（高质量互联网文本子集），从中采样 2.3B tokens。
  - Stage 1：320M tokens，context=512
  - Stage 2：1B tokens，context=512
  - Stage 3：1B tokens，context=16K
- **评估数据**：
  - **常识推理（CSR）**：ARC-Easy, ARC-Challenge, HellaSwag, WinoGrande, PIQA, LAMBADA, MMLU
  - **长上下文召回（Long-context Recall）**：RULER 套件中的 **Needle-in-a-Haystack (NIAH)** 任务，测试长度达 256K。

---

### 实验设置和评估指标
- **模型基础**：基于 Qwen3-1.7B, 4B, 8B 进行转换。
- **硬件环境**：单张 NVIDIA A800 GPU，BFloat16 精度。
- **评估指标**：
  - **CSR Score**：多个下游任务的平均归一化准确率。
  - **NIAH Accuracy**：在不同上下文长度（4K–256K）下的“大海捞针”准确率。
  - **效率指标**：
    - **Throughput (tokens/s)**：每秒生成 token 数。
    - **Time per Output Token (TPOT)**：每个输出 token 的延迟。
    - **Prefill Time**：前缀填充时间。

---

### 基线方法对比
| 方法 | 来源 | 所需 tokens |
|------|------|-----------|
| Qwen3 (原模型) | Qwen, 2025 | — |
| Jet-Nemotron | Gu et al., 2025 | 400B |
| KL-LS (GDN) | Li et al., 2025 | 25B |
| Mamba-in-the-Llama | Wang et al., 2025b | 20B |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Qwen3-1.7B vs HypeNet-2B）

#### （1）长上下文召回性能（NIAH）
| 模型 | 32K | 64K | 128K | 256K |
|------|-----|-----|------|------|
| Qwen3 (原模型) | 96.4 | 24.8 | 14.8 | 19.0 |
| Jet-Nemotron | 0.0 | 0.0 | 0.0 | 0.0 |
| KL-LS (GDN) | 68.4 | 28.2 | 24.8 | 11.0 |
| **HypeNet + HALO (ours)** | **99.8** | **97.8** | **86.2** | **48.8** |

> 💡 **结论**：HypeNet 在所有长度下均显著优于现有蒸馏方法，尤其在 128K 上达到 86.2%，而其他方法几乎失效。

#### （2）常识推理（CSR）
| 模型 | CSR Score |
|------|---------|
| Qwen3-1.7B | 58.5 |
| HypeNet + HALO | 55.9 |

> 尽管略有下降，但仍在合理范围内，且远高于纯 RNN 模型。

---

### 与基线方法的对比结果
- **效率方面**（图1）：
  - 在 128K 上下文下，HypeNet 的 **throughput 是 Qwen3 的 2.4–3.0×**。
  - 在 512K 上，HypeNet 实现 **3.4× prefill speedup**。
  - Qwen3 在 1M 上下文时显存溢出，而 HypeNet 仍可运行。

- **综合性能-效率权衡**：HypeNet 在保持接近原模型性能的同时，实现了显著的推理加速和内存节省。

---

### 消融实验结果

#### （1）HypeNet 架构组件消融（Table 3）
移除任一组件均导致性能下降，尤其是：
- 移除 **RNN RoPE**（即 RNN 无位置编码）：128K NIAH 从 79.9↓至 47.9
- 移除 **RNN QK-Norm**：128K NIAH↓至 17.3
- 移除 **RNN Output Gate**：128K NIAH↓至 74.5

> 表明所有提出的架构改进均有效。

#### （2）注意力层选择方法对比（Table 4）
| 方法 | 128K NIAH | 256K NIAH |
|------|----------|----------|
| Evenly distributed | 61.9 | 50.9 |
| Jet-Nemotron (Gu et al.) | 63.7 | 56.2 |
| KL-LS (Li et al.) | 58.3 | 44.3 |
| **HALO (ours)** | **79.9** | **74.3** |

> 证明本文提出的 layer selection 方法更优。

#### （3）RNN Mixer 对比（Figure 5）
尽管 **Lightning Attention** 结构最简单，但在长度泛化上优于 Mamba2、GLA、GDN、RWKV-7。
- 可能原因：其 **data-independent forget gate** 更利于长序列建模。

---

## 4. 关键结论和发现

### 主要发现
1. **高效蒸馏是可行的**：通过精心设计的三阶段流程（HALO），可在极少量数据（2.3B tokens）下完成从 Transformer 到 hybrid 模型的转换。
2. **HyPE 是关键创新**：将 RoPE 引入 RNN 层、NoPE 用于 attention 层，结合动态缩放，极大提升了长上下文泛化能力。
3. **架构细节至关重要**：QK-Norm、GQA to MHA、Output Gate 等小改动对最终性能有显著影响。
4. **简单的 RNN mixer 可能更优**：Lightning Attention 凭借其简洁性和固定衰减机制，在长序列任务中表现最佳。

---

### 方法的局限性
1. **依赖预训练模型行为**：转换后的模型可能丢失原模型在指令遵循（instruction-following）和对齐（alignment）方面的后训练能力。
2. **仅适用于 Transformer-based 模型**：当前流程针对标准 Transformer 设计，难以直接迁移到其他架构（如 RetNet、RWKV）。
3. **未包含短卷积（short convolution）**：虽然实现更简单，但可能牺牲部分局部建模能力。

---

### 未来工作方向
1. **恢复对齐能力**：探索如何在蒸馏后高效恢复指令遵循和对话能力。
2. **扩展到更多架构**：将 HALO 框架推广至非 Transformer 基础模型。
3. **自动化层选择**：设计无需人工定义 k 的自适应 attention 层保留策略。
4. **多任务联合优化**：在蒸馏过程中联合优化 CSR、recall、效率等多项目标。

---

> 🔗 **代码与模型**：https://github.com/THUNLP/hybrid-linear-attention

</details>

---

### 4. [L$^3$: Large Lookup Layers](https://arxiv.org/abs/2601.21461)

**Authors**: Albert Tseng, Christopher De Sa  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.21461v1  

#### Abstract
Modern sparse language models typically achieve sparsity through Mixture-of-Experts (MoE) layers, which dynamically route tokens to dense MLP "experts." However, dynamic hard routing has a number of drawbacks, such as potentially poor hardware efficiency and needing auxiliary losses for stable train...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《L³: Large Lookup Layers》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

现代稀疏语言模型（如 **MoE**）通过动态路由（context-dependent routing）实现高参数量下的高效激活，但存在以下系统级挑战：

- **硬件效率低**：路由决策依赖上下文，无法提前预知需激活的专家，导致难以进行参数卸载（offloading）和流水线优化。
- **训练不稳定**：需要引入辅助损失（auxiliary losses）来平衡专家负载，防止“专家坍缩”（expert collapse）。
- **部署复杂**：动态路由增加了调度开销，尤其在 CPU/GPU 混合推理中表现不佳。

此外，传统 **tokenizer embedding 表**虽然稀疏且高效，但缺乏上下文感知能力，仅用于初始化 token 表示。

---

### 🚀 提出的新方法：Large Lookup Layer (L³)

L³ 是一种新型稀疏架构，**将 tokenizer embedding 表的思想推广到 decoder 层内部**，提出了一种**静态路由 + 上下文聚合**的混合机制。

#### 核心思想：
- 引入一个大型可学习的 **embedding lookup table**，每个 token ID 可以访问一组与其相关的 embeddings。
- 使用 **静态路由（static routing）**：根据 token ID 预先确定可访问的 embedding 子集（即“允许集合”），无需运行时计算路由。
- 利用当前 token 的 hidden state 对这些 embeddings 进行 **attention-based 聚合**，实现**上下文感知的信息检索**。

> 🔑 关键洞察：**用静态 token 路由模拟动态 MoE 路由的行为，同时保留系统友好性。**

---

### ⚖️ 相比现有方法的优势

| 特性 | MoE | L³ |
|------|-----|----|
| 路由方式 | 动态（context-dependent） | 静态（token-based） |
| 参数激活是否可预测 | ❌ 否 | ✅ 是（token 生成即知） |
| 支持 CPU offloading | 困难（需额外 overhead） | ✅ 极低开销（可预取） |
| 是否需要辅助损失 | ✅ 是（load balancing） | ❌ 否 |
| 硬件友好性 | 中等偏低 | 高 |
| 上下文感知能力 | 强 | 中强（通过 attention 聚合） |
| 扩展性 | 受限于专家通信 | 更易扩展（模块化） |

> 💡 L³ 并非替代 MoE，而是提供了一个**正交的稀疏扩展轴**，可与 MoE 共存。

---

## 2. 核心实验方法和设置

### 📚 数据集

- **训练数据**：`FineWeb-Edu`（经过过滤的教育类网页文本）
  - 总训练 token 数：800M 模型训练 10B tokens，1.5B 模型训练 20B，2.6B 模型训练 30B
  - 上下文长度：2048
- **评估数据**：
  - **Perplexity**：Wikitext2
  - **下游任务**：ARC-C, ARC-E, HellaSwag, PIQA, Winogrande（zero-shot accuracy）

---

### 🧪 实验设置

#### 模型架构基础：
- 基于 **Llama 3** decoder 架构
- 在 decoder 层之间插入 **L³ layer**
- 不替换 MLP 层（区别于 MoE），作为增强模块添加

#### L³ 关键超参：
- 总 embedding 数 $ u = 710K $
- 每个 token 最多分配 $ k = 512 $ 个 embeddings
- 使用 **LZW-based 分配算法** 决定哪些 token 获取更多 embeddings

#### 基线对比方法：
| 类型 | 方法 |
|------|------|
| 密集模型 | Dense Transformer（相同 FLOPs） |
| 稀疏模型 | Mixture-of-Experts (MoE)，控制为 iso-FLOP、iso-depth、iso-sparsity |
| 消融实验 | 不同 L³ 层数、不同 embedding 分配策略（uniform vs LZW）、weight tying |

#### 评估指标：
- **训练阶段**：Perplexity 曲线（FineWeb-Edu）
- **推理阶段**：
  - Zero-shot 准确率（多个 NLU 任务）
  - 推理吞吐量（tokens/sec）
  - Offloading 开销分析

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ Perplexity 提升显著（图6 & 表1）

| 模型 | Active Params | Total Params | PPL (Wiki2) |
|------|---------------|--------------|-------------|
| Dense 800M | 809M | 809M | 22.02 |
| L³ ×1 (800M) | 795M | 1.8B | **20.81** |
| L³ ×3 (800M*) | 818M | 5.2B | **19.59** |
| Dense 2.6B | 2.6B | 2.6B | 15.43 |
| L³ ×2 (2.6B) | 2.6B | 7.0B | **14.51** |

> ➕ 在相同 active 参数下，L³ 显著降低 perplexity，且提升随层数增加而持续。

---

#### ✅ 下游 zero-shot 性能全面领先（表1）

以 2.6B 模型为例：

| Task | Dense | L³ (+2 layers) | 提升幅度 |
|------|-------|----------------|---------|
| ARC-C | 36.35 | **38.21** | +1.86 |
| HellaSwag | 44.20 | **44.95** | +0.75 |
| PIQA | 71.22 | **71.71** | +0.49 |
| Winogrande | 57.30 | **58.80** | +1.50 |

> 所有任务均取得一致提升，表明 L³ 带来的增益是泛化的。

---

#### ✅ 显著优于 iso-FLOP MoE（图8）

- 在 800M 和 1.5B 尺寸下，**L³ 的 perplexity 改善远超同等稀疏度的 MoE**
- 特别是在低预算场景（如 800M），MoE 甚至不如 dense baseline，而 L³ 始终正向增益
- 结论：**L³ 的收益更稳定，无需复杂的路由训练过程**

---

#### 🔍 消融实验结果

##### （1）Embedding 分配策略对比（图7C）

| 分配方式 | Perplexity Gap | 说明 |
|--------|----------------|------|
| Uniform（均匀分配） | 差 | 所有 token 分配相同数量 embeddings，效果差 |
| LZW（无上限） | 最好 | 高频 token 自动获得更多 embeddings |
| LZW（k=512） | 接近最优 | 控制 worst-case 内存使用，实用性更强 |

> ✅ **LZW-based 分配至关重要**，有效模拟了“常见上下文模式缓存”的行为。

##### （2）Weight Tying（图7R）

- 将 key 和 value embedding 矩阵共享（$ W_K = W_V $）
- 结果：**性能几乎不变**，但总参数量减少近半，传输量大幅下降
- 意义：L³ 架构具备高度可压缩性和部署友好性

##### （3）L³ 层数与位置（图7L & 图9）

- 多层小规模 L³ vs 单层大规模：性能相近
- 但单层过大影响 prefetch 时间窗口
- 最佳 placement：不宜太早（上下文不足）也不宜太晚（作用有限），中间层较优

---

#### ⚙️ 推理效率实测（表2）

| 模型 | Offloading | First L³ Position | Throughput (tokens/s) |
|------|------------|--------------------|------------------------|
| Dense 2.6B | – | – | 334.98 |
| L³ 2.6B | CPU | 1st layer | 302.11 |
| L³ 2.6B | CPU | 4th layer | **332.74** |

> ✅ **仅需 4 个 decoder 层即可完全掩盖 offloading 延迟**  
> ✅ 即使参数卸载至 CPU，推理速度仍接近 dense 模型

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **L³ 成功解锁了一条新的稀疏扩展路径**：
   - 通过将 embedding 表从输入层推广到深层，实现了“计算捷径”（computation shortcutting）
   - 模型可通过查找已学知识避免重复计算

2. **静态路由 + attention 聚合 是可行且高效的组合**：
   - 虽然路由是静态的，但通过 hidden state attention 实现了上下文敏感的信息提取
   - 性能媲美甚至超越动态 MoE

3. **系统友好性极强**：
   - 参数访问可预测 → 支持高效 offloading 和 prefetch
   - 无需辅助损失 → 训练更简单稳定
   - 支持 block-diagonal attention kernel 加速训练

4. **信息论启发的分配策略有效**：
   - LZW 算法自动识别高频“后缀模式”，指导 embedding 分配
   - 符合 Zipf 定律分布，贴近真实语言统计特性

---

### ⚠️ 方法的局限性

1. **并非完全取代 MoE**：
   - MoE 更适合专家专业化建模（如领域专家）
   - L³ 更适合通用知识缓存
   - 二者应视为互补而非互斥

2. **embedding 分配依赖离线算法**：
   - 当前使用 LZW 需要扫描语料库，可能不适用于快速迁移场景
   - 在线学习分配策略尚未探索

3. **最大激活参数受 $ k $ 限制**：
   - 虽然 cap 保证 worst-case 效率，但也限制了极端情况下的表达能力

---

### 🔮 未来工作方向

1. **结合 MoE 与 L³**：
   - 在同一模型中同时使用两种稀疏机制，探索协同效应

2. **跨任务 swapping L³ layers**：
   - 类似 Cartridges 思想，尝试在不同任务间交换预训练好的 L³ embedding 表

3. **动态调整 embedding 分配**：
   - 设计可微分的分配机制，在训练中自适应优化

4. **更大规模部署验证**：
   - 当前最大测试为 2.6B active params，未来可在 10B+ 规模验证其扩展性

---

> 📌 **总结一句话**：  
> **L³ 提出了一种硬件高效、训练稳定、性能优越的新型稀疏架构，通过“静态查找 + 动态读取”的范式，在不牺牲系统效率的前提下实现了上下文感知的知识缓存，为大模型稀疏化提供了全新维度。**

</details>

---

### 5. [Amortized Spectral Kernel Discovery via Prior-Data Fitted Network](https://arxiv.org/abs/2601.21731)

**Authors**: Kaustubh Sharma, Srijan Tiwari, Ojasva Nema, Parikshit Pareek  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.21731v1  

#### Abstract
Prior-Data Fitted Networks (PFNs) enable efficient amortized inference but lack transparent access to their learned priors and kernels. This opacity hinders their use in downstream tasks, such as surrogate-based optimization, that require explicit covariance models. We introduce an interpretability-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Amortized Spectral Kernel Discovery via Prior-Data Fitted Network**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Prior-Data Fitted Networks (PFNs) 虽然在**amortized Bayesian inference**（摊销贝叶斯推断）方面表现出色，能够通过单次前向传播实现快速预测，但其内部学到的先验（如 spectral density 和 covariance kernel）是隐式的、不可解释的。这限制了它们在需要显式协方差模型的任务中的应用，例如 surrogate-based optimization 或科学建模。

本文旨在解决以下两个核心问题：
- **机制透明性缺失**：PFNs 如何编码频谱信息？这些信息是否可被解码？
- **缺乏可移植的显式表示**：能否从 PFNs 中提取出可复用的显式 kernel？

---

### **提出的新方法与新思路**
作者提出了一个**基于机制分析的解码框架**，用于从预训练的 PFNs 中进行**摊销频谱核发现**（amortized spectral kernel discovery），具体包括：

#### **核心思想**
- 利用 **Decoupled-Value Attention (DVA)** 架构的 PFNs，发现其注意力输出 $ H $ 隐式编码了输入函数的**频谱结构**（spectral manifold）。
- 设计了一个 **Filter Bank Decoder**，将 PFN 的隐状态 $ H $ 和值编码 $ V $ 映射为显式的**谱密度估计** $ S(\omega) $，再通过 **Bochner’s Theorem** 转换为**平稳协方差核** $ k(\tau) $。

#### **两大解码器设计**
| 类型 | 输入 | 输出 | 理论依据 |
|------|------|------|--------|
| **Multi-Realization Decoder** | 多个独立函数样本 | 完整谱混合（频率、带宽、权重） | Theorem 4.3：多样本下谱权重可识别 |
| **Single-Realization Decoder** | 单个函数样本 | 主导频率与带宽（权重统一）+ 分析缩放 | Theorem 4.1：单样本无法识别相对权重 |

#### **关键创新点**
1. **首次揭示 PFN 注意力机制中存在可解码的频谱流形**（spectral manifold），并通过 t-SNE 可视化验证。
2. 提出**无需测试时优化**的零样本（zero-shot）核构造方法，仅需一次前向传播。
3. 引入 **Analytical Scaling** 模块，利用 Proposition 4.2 在后处理阶段恢复全局尺度，避免网络“幻觉”缩放参数。
4. 使用 **Multi-Query Attention Pooling** 提取多个摘要视图，提升对复杂谱混合的分辨能力。

---

### **相比现有方法的优势**

| 方法 | 是否需要测试时优化 | 是否输出显式 kernel | 推理速度 | 可解释性 |
|------|---------------------|------------------------|----------|-----------|
| **DKL / RFF** | ✅ 是（每任务优化） | ✅ 是 | ❌ 慢（秒级） | ⚠️ 有限 |
| **PFN（原始）** | ❌ 否 | ❌ 否（黑箱） | ✅ 快（毫秒级） | ❌ 差 |
| **本文方法（Decoder + Bochner）** | ❌ 否 | ✅ 是（stationary surrogate kernel） | ✅ 快（~10⁻³ 秒） | ✅ 高 |

> ✅ **优势总结**：
> - **速度快**：比 DKL/RFF 快约 **三个数量级**（~1000×）；
> - **性能强**：GP 回归 MSE 与 PFN、DKL 相当甚至更优；
> - **可解释**：提供显式的 $ S(\omega) $ 和 $ k(\tau) $，支持下游任务集成；
> - **摊销推理**：训练一次 decoder，即可零样本泛化到新数据。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
全部为**合成数据集**，由 GP 采样生成，涵盖多种 kernel family：

- **Spectral Mixture (SM)**：含 1–4 个高斯分量的混合谱密度；
- **标准 kernel**：RBF、Periodic、RBF+Periodic、RBF×Periodic；
- **高维 additive kernel**：在 5D 和 10D 上构建，每个维度使用一维 kernel 组合；
- **Out-of-Distribution (OOD)**：非高斯三角波信号，频率随机采样。

---

### **实验设置与评估指标**

#### **评估任务**
- **GP Regression**：使用解码得到的 kernel 进行高斯过程回归，评估预测准确性。
- **Kernel Recovery Quality**：比较解码谱密度与真实谱密度之间的 **Wasserstein Distance**。
- **推理效率**：记录 kernel 构造时间（不包括 GP 推理本身）。

#### **主要指标**
| 指标 | 描述 |
|------|------|
| **MSE** | 测试点上的均方误差（log scale 报告） |
| **Wasserstein Distance** | 衡量预测谱与真实谱的距离 |
| **Inference Time** | kernel 构造耗时（秒） |
| **R² Score** | 线性探针评估频率/权重恢复能力 |

---

### **基线方法对比**
| 基线 | 描述 |
|------|------|
| **Oracle GP** | 使用真实生成 kernel 的 GP（理想上限） |
| **PFN** | 原始 PFN 模型直接预测（无显式 kernel） |
| **DKL (Deep Kernel Learning)** | 神经网络 + kernel 微调，需 per-task 优化 |
| **RFF (Random Fourier Features)** | 固定谱采样，也需优化表示 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表 1：Kernel Cookbook 上的性能对比（单样本设置）**
| True Kernel | Decoder MSE | DKL MSE | RFF MSE | Decoder Time (s) | DKL Time (s) |
|------------|-------------|---------|---------|------------------|---------------|
| RBF        | 2.52e-5     | 2.52e-2 | 4.33e-2 | 0.004            | ~2.0          |
| Periodic   | 2.78e-2     | 3.80e-2 | 5.51e-2 | 0.005            | ~2.2          |
| SM(Q=4)    | 2.54e-3     | 2.70e-2 | 5.72e-2 | 0.005            | ~2.1          |

> ✅ **Decoder 性能接近 PFN，显著优于 DKL/RFF，且速度快 ~1000×**

---

#### **图 3：多样本设置下的谱恢复质量**
- **Wasserstein Distance 随样本数 $ M $ 单调下降**，验证 Theorem 4.3 的 identifiability 保证；
- 当 $ M=16 $ 时，谱密度几乎完全恢复。

---

#### **表 2：高维（5D/10D）multi-realization 设置下的扩展性**
| Kernel (Additive) | Oracle GP MSE | Amortized MSE |
|-------------------|---------------|----------------|
| RBF(5D)           | 1.3e-5        | 2.9e-5         |
| SM(Q=4,10D)       | 2.06e-4       | 4.56e-4        |
| Periodic(10D)     | 2.71e-4       | 2.32e-3        |

> ✅ 在高维仍保持同阶误差，**except for Periodic kernels**（因全局周期结构难建模）

---

#### **消融实验结果（Appendix B）**

| 实验 | 发现 |
|------|------|
| **Mean Pooling vs Attention Pooling** | 随着谱复杂度增加，Attention Pooling 提升明显（Hard: +3.8%, Very Hard: +5.8%） |
| **H vs V 表示探针** | $ H $ 对频率预测 R² 达 0.998，$ V $ 几乎无用；$ H+V $ 最佳 |
| **相位不变性分析** | $ H $ 对相位敏感，$ V $ 不敏感 → 建议联合使用以增强鲁棒性 |
| **滤波器组 vs 全参数回归** | 分离频率与权重预测更稳定，避免耦合干扰 |

---

## **4. 关键结论和发现**

### **主要发现**
1. **PFNs 并非黑箱记忆者**：其注意力输出 $ H $ 显式编码了输入函数的**频谱结构**，形成平滑的 spectral manifold。
2. **摊销谱发现是可行的**：通过简单 decoder 即可从冻结的 PFN 中提取高质量的显式 kernel。
3. **统计可识别性决定架构设计**：
   - 单样本只能恢复谱形状（频率+带宽），不能恢复权重；
   - 多样本允许完整恢复，decoder 可达一致性。
4. **无需优化也能高性能**：解码 kernel 支持的 GP 回归精度媲美 DKL/PFN，但推理快千倍。

---

### **方法的局限性**
1. **仅适用于 stationary kernels**：依赖 Bochner’s Theorem，无法建模非平稳或各向异性核。
2. **高维扩展受限**：PFNs 不显式编码维度身份，难以解码 per-dimension 结构。
3. **对周期性强的 kernel 效果较差**：如 Periodic kernel 在高维表现不佳（见 Table 2）。
4. **依赖 PFN 预训练分布**：若测试数据严重偏离训练分布，性能会下降（尽管有 graceful degradation）。

---

### **未来工作方向**
1. **下游任务集成**：将解码 kernel 应用于 surrogate-based optimization、control 或物理建模。
2. **混合架构设计**：结合 amortized inference 与 adaptive context selection，提升大数据场景下的 scalability。
3. **非平稳核扩展**：探索如何从 PFNs 中提取时变或空间变化的 kernel。
4. **跨域迁移**：研究 decoder 在不同 PFN 架构或任务分布间的泛化能力。

---

> 📌 **一句话总结**：  
> 本文揭示了 PFNs 内部蕴含可解码的频谱结构，并提出首个**无需测试时优化**的摊销谱核发现框架，在保持高速推理的同时，实现了**高性能、可解释、可复用**的 kernel 构造。

</details>

---

### 6. [Investigating Batch Inference in a Sequential Monte Carlo Framework for Neural Networks](https://arxiv.org/abs/2601.21983)

**Authors**: Andrew Millard, Joshua Murphy, Peter Green, Simon Maskell  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.21983v1  

#### Abstract
Bayesian inference allows us to define a posterior distribution over the weights of a generic neural network (NN). Exact posteriors are usually intractable, in which case approximations can be employed. One such approximation - variational inference - is computationally efficient when using mini-bat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Investigating Batch Inference in a Sequential Monte Carlo Framework for Neural Networks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在神经网络（Neural Networks, NN）的贝叶斯推断中，**Sequential Monte Carlo (SMC)** 是一种非参数化、渐近无偏的采样方法，能够避免变分推断（Variational Inference, VI）对后验分布形式的强假设。然而，标准SMC通常依赖于全批量（full-batch）数据进行似然和梯度计算，导致其在大规模数据集上计算开销极高，难以实际应用。

本文旨在解决这一问题：  
> **如何在保持SMC采样质量的同时，显著降低其在神经网络贝叶斯推断中的计算成本？**

### 🚀 提出的新方法与新思路
作者提出并系统研究了将 **Mini-batch 数据逐步引入 SMC 框架** 的策略，称为 **Data Annealing (DA)** ——即“数据退火”或“数据升温”。具体包括以下几种方案：

- **Constant**: 固定小批量训练（baseline）
- **Full-batch (FB)**: 始终使用全部数据（传统SMC方式，高精度但高成本）
- **Constant-to-refine (CTR)**: 初期用小批量快速探索，后期切换为全批量精细优化
- **Linear / Automated**: 线性递增mini-batch数量
- **Smooth DA (SDA)**: 基于Shannon熵变化率自适应控制数据引入节奏

其中，**CTR 和 SDA 是重点创新点**，尤其是 CTR 在实践中表现优异。

### 🔍 相比现有方法的优势
| 方法 | 优势 |
|------|------|
| **相比VI** | 不依赖参数化变分族假设，能更好地捕捉复杂后验结构 |
| **相比MCMC/SMC-FB** | 显著减少每轮迭代的计算量，提升训练速度 |
| **相比纯Mini-batch MCMC** | 通过渐进式数据引入机制，避免因噪声过大导致收敛失败 |

最终实现了：
> **最高达6倍加速，同时仅带来极小的准确率损失**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **MNIST**: 手写数字图像分类任务，共70,000张灰度图（60k训练 + 10k测试），输入尺寸 28×28
- **FashionMNIST**: 服饰图像分类任务，同样70,000张灰度图，10类服装类别，更具挑战性

### ⚙️ 实验设置
- **模型架构**:
  - MNIST: LeNet-5（D=61,706 参数）
  - FashionMNIST: 更大的CNN（D=96,658 参数）
- **推理框架**: SMC sampler 结合两种Markov Kernel (MK):
  - **Langevin Dynamics (LD)**
  - **Hamiltonian Monte Carlo (HMC)**（S=3 leapfrog steps）
- **Mini-batch size**: $ K = C = 500 $
- **总迭代次数**: 200 iterations
- **运行平台**: NVIDIA A100 GPU，使用 JAX 实现
- **随机种子**: 5次重复实验，报告均值±标准差

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Test Loss** | 测试集上的负对数似然（NLL） |
| **Test Accuracy (%)** | 分类准确率 |
| **Runtime (s)** | 总训练耗时（秒） |

### 🔁 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Constant** | 固定Mini-batch | 最快但精度较低 |
| **Full-batch (FB)** | 全数据批次 | 精度最高但最慢（基准上限） |
| **CTR (Constant-to-refine)** | 两阶段策略 | 前90%迭代用MB，最后10%切至FB |
| **Linear / Automated / SDA** | 渐进式DA | 逐步增加batch size |
| **SDA (Smooth DA)** | 自适应熵调节 | 动态调整数据加入速率 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

#### ✅ 在 **MNIST** 上的结果（以 HMC 为例）：

| 方法 | Test Accuracy (%) | Runtime (s) | 相对FB提速 |
|------|-------------------|------------|-----------|
| Full-batch (FB) | **98.00±0.15** | 10658.68 | ×1.0 |
| Constant | 93.14±0.01 | **453.33** | ~×23.5 |
| CTR | **98.00±0.15** | **1531.53** | ~×6.96 |
| Linear | 97.90±0.13 | 9507.66 | ~×1.12 |
| Automated | 97.94±0.05 | 5891.13 | ~×1.81 |
| SDA | 97.70±0.17 | 7683.07 | ~×1.39 |

> 💡 **CTR 在达到与 FB 相当精度的同时，训练时间缩短约 6.6 倍**

#### ✅ 在 **FashionMNIST** 上的结果（HMC）：

| 方法 | Test Accuracy (%) | Runtime (s) |
|------|-------------------|------------|
| Full-batch (FB) | **89.60±0.20** | 10864.92 |
| CTR | **89.62±0.30** | **1531.53** |
| Constant | 83.46±0.42 | 392.11 |

> ⚠️ 注意：CTR 准确率甚至略高于 FB，可能源于更优的探索-开发平衡

### 🔬 消融实验与关键观察
- **CTR 表现最优**：在两个数据集上均能在接近 FB 精度的前提下实现 **~6–7× 加速**
- **Linear > Automated ≈ SDA**：简单的线性增长优于复杂的自适应策略（如 SDA）
- **SDA 并未带来额外收益**：尽管理论上有信息论依据，但在实践中不如简单调度有效
- **LD 整体劣于 HMC**：即使只有 S=3 步leapfrog，HMC仍表现出更强的采样效率和更高精度
- **Constant 虽快但精度下降明显**：适合初步探索，不适合最终推断

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Mini-batch 推理可成功集成到 SMC 框架中**，无需牺牲太多精度即可大幅降低计算成本。
2. **两阶段策略 CTR（先MB后FB）是最有效的折中方案**：
   - 利用 Mini-batch 快速引导粒子向高概率区域移动
   - 最终阶段使用 Full-batch 进行精细化采样，确保后验估计质量
3. **渐进式DA（如Linear）优于固定MB，但不如CTR高效**
4. **基于熵的自适应策略（SDA）并未展现出预期优势**，反而增加了实现复杂度
5. **HMC 明显优于 LD**：即使是短轨迹HMC也能提供更好的采样质量和稳定性

### ⚠️ 方法的局限性
- **SMC本身仍是内存密集型方法**：需维护多个粒子（模型副本），限制了超大模型的应用
- **Resampling 引入方差**：可能导致多样性丢失，尤其在高维空间中
- **CTR 需预设切换点（如0.9K）**：缺乏完全自适应机制，依赖经验调参
- **当前实验限于中小规模CNN**：是否适用于Transformer等现代大模型尚待验证

### 🔮 未来工作方向
1. **结合 adaptive trajectory length methods**（如No-U-Turn Sampling思想）进一步优化HMC proposal
2. **探索更智能的自动annealing策略**，例如基于effective sample size (ESS) 或KL散度监控
3. **扩展至分布式SMC实现**（如SMC²），支持更大规模模型和流式数据
4. **引入二阶信息（如Hessian）改进MK设计**（文中引用[Hess-MC2]已有相关尝试）
5. **应用于不确定性量化、主动学习、鲁棒决策等下游任务**

---

## ✅ 总结一句话
> 本论文首次系统地将 **Mini-batch Data Annealing** 引入 **Sequential Monte Carlo for Bayesian NNs**，提出并验证了多种调度策略，发现 **Constant-to-refine (CTR)** 方案可在几乎不损失精度的情况下实现 **高达6倍以上的训练加速**，为SMC在现实场景中的落地提供了可行路径。

</details>

---

### 7. [OmegaUse: Building a General-Purpose GUI Agent for Autonomous Task Execution](https://arxiv.org/abs/2601.20380)

**Authors**: Le Zhang, Yixiong Xiao, Xinjiang Lu, Jingjia Cao, Yusai Zhao, Jingbo Zhou, Lang An, Zikan Feng, Wanxiang Sha, Yu Shi, Congxi Xiao, Jian Xiong, Yankai Zhang, Hua Wu, Haifeng Wang  
**Category**: cs.AI  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.20380v1  

#### Abstract
Graphical User Interface (GUI) agents show great potential for enabling foundation models to complete real-world tasks, revolutionizing human-computer interaction and improving human productivity. In this report, we present OmegaUse, a general-purpose GUI agent model for autonomous task execution on...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《OmegaUse: Building a General-Purpose GUI Agent for Autonomous Task Execution》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的 **GUI Agent** 在实际应用中面临三大瓶颈：
- **训练数据质量差**：现有导航数据集存在轨迹错误、冗余动作、标注偏移等问题，严重影响模型学习效果。
- **缺乏高质量评估基准**：尤其在中文移动生态和桌面系统等特定场景下，缺少专门的离线评估基准（offline benchmark）。
- **模型架构与训练策略不匹配**：传统端到端或模块化设计难以兼顾推理能力与计算效率。

### 提出了什么新方法或新思路
OmegaUse 提出了一套完整的 GUI Agent 构建框架，涵盖数据、训练、架构与评估四个层面：

#### （1）**高精度 MoE 架构**
- 采用 **Mixture-of-Experts (MoE)** 作为 backbone，相比 dense 模型（如 7B/72B），在保持大参数量推理能力的同时，仅激活部分专家，显著降低计算开销。
- 支持跨平台统一操作空间（Unified Action Space），覆盖 mobile、desktop 和 web 场景。

#### （2）**高质量数据构建管道**
- **Grounding 数据**：对六大开源数据集（Aguvis、SeeClick、OS-Atlas 等）进行严格清洗与人工校正，构建 111K 高质量单步 grounding 数据集。
- **Navigation 数据**：提出 **分层合成框架**，融合三种来源：
  - 开源数据审计
  - 自动化合成（bottom-up 探索 + top-down 分类引导生成）
  - 跨终端专家演示（human-in-the-loop 标注）

#### （3）**解耦式两阶段训练范式**
- **第一阶段：Supervised Fine-Tuning (SFT)**  
  学习基础交互语法与任务逻辑。
- **第二阶段：Group Relative Policy Optimization (GRPO)**  
  引入细粒度奖励机制提升空间定位与序列规划能力：
  - Grounding：`Format Reward` + `Inside-of-Bounding-Box Reward`
  - Navigation：`Format Reward` + `Action-wise Reward`（含坐标精度、内容保真度等）

#### （4）**发布新型评估基准 OS-Nav**
- 包含两个子集：
  - **ChiM-Nav**：聚焦中文 Android 移动环境，共 142 条轨迹，991 步。
  - **Ubu-Nav**：面向 Ubuntu 桌面日常操作，共 101 条轨迹，641 步。
- 所有轨迹均经 MLLM 辅助 + 人工验证，确保逻辑一致性与环境可行性。

### 相比现有方法的优势
| 维度 | OmegaUse 优势 |
|------|---------------|
| **数据质量** | 通过人工精修 + 多源合成，解决自动标注偏移与轨迹噪声问题 |
| **训练效率** | MoE 设计实现高性能与低延迟的平衡 |
| **泛化能力** | 支持跨平台（mobile/desktop/web）、多语言（中文界面） |
| **评估体系** | 补足中文移动与 Linux 桌面场景的 benchmark 缺失 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### （1）**训练数据**
- **Grounding**：整合 6 个公开数据集（Aguvis, UI RefExp, Widget Captioning, SeeClick, Uground, OS-Atlas），原始样本约 1.66M，经清洗后保留 111K 高质量样本。
- **Navigation**：
  - 开源轨迹（AGUVIS stage-2, AITW, Mind2Web）
  - 合成轨迹（基于 DFS 探索 + Taxonomy 引导生成）
  - 专家演示（≥5 步复杂任务，双人审核）

#### （2）**测试数据**
- **通用基准**：
  - **ScreenSpot-V2**：跨平台 UI 元素定位基准（mobile/web/desktop）
  - **ScreenSpot-Pro**：高分辨率专业软件界面定位挑战
  - **AndroidControl**：离线轨迹规划评估
  - **AndroidWorld**：在线动态交互评估
- **自研基准**：
  - **ChiM-Nav**：中文 Android 应用导航
  - **Ubu-Nav**：Ubuntu 桌面常规操作

### 实验设置和评估指标

| 类别 | 设置说明 |
|------|--------|
| **模型配置** | 30B-A3B VL MoE 模型；SFT 学习率 1e-5，RL 阶段 5e-5；batch size 分别为 32（SFT）和 64（RL） |
| **评估方式** | 离线评估为主（oracle setting），即提供真实截图与状态流 |
| **主要指标** | 
| - **Type Accuracy (Type Acc.)**：动作类型预测正确率 |
| - **Step Success Rate (SR)**：每一步执行成功的比例 |
| - **Average Success**：任务级平均成功率（Ubu-Nav） |
| - **Coordination Precision**：点击/拖拽坐标准确性 |

### 基线方法对比
参与比较的主流模型包括：
- **Closed-source**：GPT-4o, Claude Computer Use, Seed1.5-VL
- **Open-source GUI Agents**：
  - SeeClick, OS-Atlas, UI-TARS 系列
  - UI-Venus, GTA1, AgentCPM-GUI
  - OpenCUA, Qwen-VL 系列

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 基准 | OmegaUse 性能 | 最佳基线 | 提升情况 |
|------|----------------|-----------|----------|
| **ScreenSpot-V2 (Avg)** | **96.3%** | UI-Venus-Ground-72B (95.3%) | **+1.0 pp** |
| **AndroidControl (Step SR)** | **79.1%** | UI-Venus-Navi-72B (77.2%) | **+1.9 pp** |
| **ChiM-Nav (Step SR)** | **74.24%** | UI-Venus-72B (67.51%) | **+6.73 pp** |
| **Ubu-Nav (Average)** | **55.9%** | Holo2-30B-A3B (50.0%) | **+5.9 pp** |
| **ScreenSpot-Pro (Avg)** | 55.47% | UI-Venus-Ground-72B (61.9%) | 略逊于超大规模模型，但在 OS-Icon 上达 **43.82%**（SOTA） |

> 注：pp = percentage point

### 与基线方法的对比结果
- 在 **ScreenSpot-V2** 上刷新 SOTA，尤其在文本元素定位上接近完美（mobile text: 99.3%，desktop text: 99.0%）。
- 在 **AndroidControl** 中同时取得最高 Type Acc.（87.6%）和 Step SR（79.1%），表明其具备更强的高层语义理解与动作分解能力。
- 在 **ChiM-Nav** 上大幅领先，证明其在中文 UI 和本土化 App（如微信、支付宝风格界面）中具有卓越泛化能力。
- 在 **Ubu-Nav** 中协调动作（Click/Scroll）与非坐标动作（Type/Hotkey）均表现优异，尤其非坐标任务达 **48.6%**，远超 UI-Venus-Navi-72B（40.0%）。

### 消融实验结果（文中未明确列出消融表，但从分析可推断）
尽管没有独立表格，文中通过以下方式体现各组件贡献：
- 若仅使用原始开源数据而不清洗，性能下降明显（提及“近 40% 噪声”）。
- GRPO 阶段引入 `Inside-of-Bounding-Box Reward` 显著提升空间定位鲁棒性。
- MoE 架构在参数量小于 72B dense 模型的情况下达到甚至超越其性能，验证了效率优势。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **高质量数据是 GUI Agent 成功的关键**：严格的过滤、人工修正与多源合成能显著提升模型表现。
2. ✅ **解耦训练优于端到端联合训练**：先 SFT 再 GRPO 的策略有助于分离感知与决策，避免训练干扰。
3. ✅ **MoE 是高效 GUI Agent 的理想选择**：在保证强大推理能力的同时，有效控制推理成本。
4. ✅ **现有 benchmark 不足以反映真实世界能力**：ChiM-Nav 和 Ubu-Nav 揭示了模型在中文生态与桌面系统中的真实短板。
5. ✅ **自动化合成 + 人类反馈闭环可行**：bottom-up 探索与 top-down 分类结合，可规模化生产高质量轨迹。

### 方法的局限性
- **依赖高质量模拟器**：自动化探索需稳定可控的 sandbox 环境，在真实设备上部署仍具挑战。
- **未支持语音输入/输出**：当前仅基于视觉-语言模态，尚未集成多模态交互（如语音助手）。
- **安全性机制缺失**：未讨论权限控制、危险操作拦截等安全约束。
- **长程任务记忆有限**：虽然有历史轨迹输入，但未引入显式 memory 模块处理超长任务。

### 未来工作方向
- 🔄 **扩展至更复杂的现实工作流**：如跨 App 协同、多窗口调度、文件系统深层操作。
- 🔐 **增强安全与自我纠正机制**：加入 operation verification、risk detection 与 rollback 能力。
- 🌐 **支持更多操作系统与语言**：扩展至 iOS、Windows 及其他本地化 UI。
- 🤖 **向 online interactive setting 迁移**：从 oracle setting 走向真实环境闭环控制。
- 🧠 **探索 agent introspection 与反思能力**：引入 self-reflection、plan debugging 等认知机制。

--- 

> **总结一句话**：  
> OmegaUse 通过“**高质量数据 + 解耦训练 + MoE 架构 + 新型 benchmark**”四位一体的设计，在跨平台 GUI Agent 领域实现了性能突破，并为该领域提供了可复现、可扩展的技术范式。

</details>

---

### 8. [A Federated and Parameter-Efficient Framework for Large Language Model Training in Medicine](https://arxiv.org/abs/2601.22124)

**Authors**: Anran Li, Yuanyuan Chen, Wenjun Long, Yu Yin, Yan Hu, Hyunjae Kim, Weipeng Zhou, Yujia Zhou, Hongyi Peng, Yang Ren, Xuguang Ai, Zhenyue Qin, Ming Hu, Xiaoxiao Li, Han Yu, Yih-Chung Tham, Lucila Ohno-Machado, Hua Xu, Qingyu Chen  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.22124v1  

#### Abstract
Large language models (LLMs) have demonstrated strong performance on medical benchmarks, including question answering and diagnosis. To enable their use in clinical settings, LLMs are typically further adapted through continued pretraining or post-training using clinical data. However, most medical ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**医学领域大语言模型（LLMs）训练中的两大核心挑战**：
1. **隐私与合规限制**：医疗数据受严格监管，跨机构共享患者数据不可行，导致大多数LLMs仅在单一机构数据上训练，泛化能力差。
2. **联邦学习（Federated Learning, FL）应用于LLMs的不适用性**：
   - **通信开销巨大**：传统FL需传输整个模型参数，在多十亿参数的LLMs中不现实。
   - **数据异质性强**：不同医疗机构的数据在患者群体、疾病分布、标注习惯等方面差异显著，标准FL聚合策略效果不佳。

### 提出的新方法
作者提出了两个创新框架：
- **Fed-MedLoRA**：首个适用于医学领域的**模型无关且参数高效**的联邦学习框架。
  - 核心思想：采用**Low-Rank Adaptation (LoRA)** 技术，客户端只训练并上传低秩适配器（adapter）参数，而非完整模型权重。
  - 优势：极大降低通信成本和本地计算需求。
- **Fed-MedLoRA+**：在Fed-MedLoRA基础上的增强版。
  - 核心创新：引入**自适应、数据感知的聚合策略（influence-aware aggregation）**。
  - 实现方式：服务器利用一个小型验证集评估每个客户端更新的影响，为高质量更新分配更高权重，从而提升全局模型在异构数据下的收敛性和鲁棒性。

### 相比现有方法的优势
| 方面 | 优势 |
| :--- | :--- |
| **通信效率** | 通信成本相比全模型更新**降低98.5%**，从数百GB降至约1-2GB。 |
| **计算可行性** | 可在单张消费级GPU（如RTX 4090）上训练8B参数模型，推理可在笔记本电脑（如Apple M3 Pro）上运行。 |
| **模型性能** | 显著优于零样本LLMs、单机构微调LLMs及领域特定BERT模型。 |
| **泛化能力** | 在外部独立队列上表现稳健，支持新机构的快速冷启动。 |
| **实际部署友好** | 支持各参与方提供不完整的任务标注（如有的机构只有NER标签），框架仍能有效工作。 |

---

## 2. 核心实验方法和设置

### 数据集
研究使用了五个真实世界的临床文本数据集，涵盖不同来源和特点：
- **MIMIC-III**：重症监护病房记录。
- **MTSamples**：转录的门诊报告示例。
- **UTP (UT Physicians)**：休斯顿大学医生笔记。
- **I2B2**：由Informatics for Integrating Biology & the Bedside提供的多机构临床笔记。
- **YNHH (Yale New Haven Health)**：本研究手动标注的4份来自耶鲁纽黑文健康系统的患者记录，用于模拟新机构场景。

### 实验设置
评估分为三个关键场景：
1. **域内训练与测试（In-domain）**：在MIMIC-III、MTSamples、UTP上进行联邦训练，并在对应测试集上评估。
2. **外部验证（External Validation）**：将联邦训练的模型直接应用于未见过的独立数据集（如I2B2），评估其泛化能力。
3. **低资源新站点适应（Low-resource New-site Adaptation）**：使用在MIMIC-III等数据上训练好的Fed-MedLoRA+模型，直接在极少量标注的YNHH数据上进行评估，模拟新医院的冷启动。

### 评估指标
- **主要任务**：临床信息提取（Information Extraction, IE），包括：
  - **命名实体识别（Named Entity Recognition, NER）**
  - **关系抽取（Relation Extraction, RE）**
- **评价指标**：精确率（Precision）、召回率（Recall）、F1分数（F1-score）。
- **两种匹配模式**：
  - **严格匹配（Strict match）**：要求实体边界和类型完全一致。
  - **宽松匹配（Lenient match）**：允许实体边界部分重叠。

### 基线方法对比
- **LLM基线**：
  - **零样本（Zero-shot）**：GPT-4o, LLaMA3-8B, DeepSeek-R1-Distill-8B。
  - **单机构微调（Single-site fine-tuned）**：同款LLM在单个数据集上微调。
- **BERT基线**：`Bio_ClinicalBERT`（领域特定的强基线）。
- **联邦学习基线**：`FedSA-LoRA`（通用领域的LoRA联邦算法）。
- **理想上限**：**集中式训练（Centralized training）**：将所有机构数据合并训练，作为性能上限参考。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **性能提升**：
  - 相比零样本LLM，Fed-MedLoRA+的F1分数**最高提升达65%**。
  - 相比单机构微调LLM，**平均提升约25% F1**。
  - 在关系抽取（RE）任务上，相比`Bio_ClinicalBERT`，**性能提升超过40%**。
- **泛化能力**：
  - 在外部独立队列上，相比基线有**10%-70%的F1增益**。
- **新站点适应**：
  - 在仅有少量标注的YNHH数据上，Fed-MedLoRA+取得了**73%的严格F1**和**85%的宽松F1**，表明其强大的冷启动能力。
- **效率指标**：
  - **通信成本**：相比全模型更新，**降低98.5%**。
  - **硬件需求**：
    - 8B模型训练：单张RTX 4090（16GB）即可完成。
    - 1B模型训练：可在RTX 3060 Ti等中端GPU上运行。
    - 推理：可在标准笔记本电脑上执行。
  - **可扩展性**：框架可扩展至10个参与站点，性能仅比集中式训练下降约2%。

### 与基线方法的对比结果
- **全面超越**：Fed-MedLoRA和Fed-MedLoRA+在所有评估场景下均**显著优于所有基线方法**（p值<0.0001）。
- **Fed-MedLoRA+ > Fed-MedLoRA**：证明了“数据感知聚合”策略的有效性，Fed-MedLoRA+通常表现更优。
- **接近集中式上限**：Fed-MedLoRA+的性能非常接近理想的集中式训练结果，尤其在两机构设置下差距极小。
- **优于通用FL算法**：相比`FedSA-LoRA`，Fed-MedLoRA+在NER和RE上的F1分数分别高出数倍（例如，RE F1从0.181提升至0.894）。

### 消融实验结果
- **模型规模权衡**：使用1B参数的LLaMA3-1B作为骨干网络时：
  - **精度代价**：NER性能下降约3%，RE性能下降最多7%。
  - **效率收益**：通信、内存和训练时间大幅降低，使其能在资源极度受限的环境中部署。
- **不均匀任务标注的鲁棒性**：
  - 当部分机构缺少RE标注时，Fed-MedLoRA+的NER性能仅下降1.2%-1.7%，RE性能下降2.8%-5.8%，依然保持对单机构微调的显著优势。

---

## 4. 关键结论和发现

### 主要发现
1. **联邦LLMs在医学上是可行且高效的**：通过参数高效微调（如LoRA），可以克服传统FL在LLMs上应用的巨大通信和计算障碍。
2. **数据异质性可通过智能聚合解决**：Fed-MedLoRA+提出的基于影响的聚合策略，能有效应对医疗数据的强异质性，提升模型的稳定性和性能。
3. **联邦训练显著提升LLMs的医学能力**：该框架能将LLMs在临床IE等复杂任务上的性能提升到远超当前最佳水平，甚至接近数据集中式的理想上限。
4. **强大的泛化与冷启动能力**：联邦训练的模型具有出色的跨机构泛化能力，并能帮助新加入的机构快速建立高性能的本地模型。
5. **实用性强**：框架对硬件要求低，支持不完整标注，具备良好的可扩展性，为在真实世界医疗系统中部署提供了可能。

### 方法的局限性
1. **隐私保护机制不足**：虽然不共享原始数据，但传输的LoRA参数仍可能存在泄露敏感信息的风险，未集成差分隐私（DP）或安全聚合（Secure Aggregation）等更强的隐私保护技术。
2. **任务范围有限**：本研究仅以临床信息提取（IE）为例，尚未探索该框架在其他医学任务（如诊断、问答、生成）上的普适性。
3. **缺乏真实环境部署验证**：实验基于公开数据集，尚未在真实的多机构协作网络中进行部署，面临网络延迟、协调管理、硬件异构等实际工程挑战。

### 未来工作方向
1. **加强隐私保障**：系统性地整合差分隐私、安全聚合等技术，评估其对模型性能和效率的影响。
2. **探索最优任务组合**：研究哪些类型的医学任务和数据最适合通过联邦学习进行联合训练。
3. **推进真实世界部署**：开展前瞻性研究，在真实的医疗联盟中部署该框架，进行长期评估，解决实际落地中的技术和治理难题。

</details>

---

### 9. [ZipMoE: Efficient On-Device MoE Serving via Lossless Compression and Cache-Affinity Scheduling](https://arxiv.org/abs/2601.21198)

**Authors**: Yuchen Yang, Yaru Zhao, Pu Yang, Shaowei Wang, Zhi-Hua Zhou  
**Category**: cs.DC  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.21198v1  

#### Abstract
While Mixture-of-Experts (MoE) architectures substantially bolster the expressive power of large-language models, their prohibitive memory footprint severely impedes the practical deployment on resource-constrained edge devices, especially when model behavior must be preserved without relying on los...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ZipMoE: Efficient On-Device MoE Serving via Lossless Compression and Cache-Affinity Scheduling

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现代 **Mixture-of-Experts (MoE)** 大语言模型虽然提升了表达能力，但其巨大的内存占用严重阻碍了在资源受限的边缘设备（如手机、Jetson、树莓派）上的部署。传统方法依赖 **lossy quantization（有损量化）** 或 **offloading（参数卸载）** 来缓解内存压力，但存在以下问题：

- **有损量化** 会改变模型行为，可能引入安全漏洞（如恶意代码生成、过度拒绝等），且现有评估指标（如 perplexity）无法捕捉这些风险。
- **现有卸载系统** 假设 CPU 内存是独立存储，而实际边缘平台多采用 **统一内存架构（UMA）**，CPU 和 GPU 共享内存池，导致 I/O 路径效率低下。
- 在低批处理大小（batch size = 1）的交互式场景中，流水线优化失效，I/O 成为瓶颈。

因此，核心问题是：**如何在不改变 MoE 模型语义的前提下，高效地在移动和边缘平台上部署 MoE 模型？**

---

### 提出了什么新方法或新思路
论文提出 **ZIPMoE** —— 一种高效的、语义无损（semantically lossless）的 on-device MoE 推理系统，其核心创新在于 **缓存与调度的协同设计（caching-scheduling co-design）**，具体包括：

#### （1）基于统计冗余的无损压缩（Lossless Compression）
- 观察到 BF16 参数中的 **exponent bits（指数位）具有高度统计冗余**（低熵），而 sign/mantissa bits（符号/尾数位）接近随机分布。
- 将每个 tensor 分解为 **E-chunks（压缩的 exponent bits）** 和 **SM-chunks（未压缩的 sign-mantissa bits）**，仅对 E-chunks 进行无损压缩（使用 ZSTD/LZ4HC）。
- 利用 **zero-copy + memory-coalesced GPU kernel** 实现高效恢复，运行时开销极低。

#### （2）分层缓存管理（Hierarchical, Differentiated Caching）
- 设计四种缓存池：
  - `F`：完整张量（Full tensor）
  - `C`：压缩张量（Compressed tensor）
  - `S`：仅 SM-chunks
  - `E`：仅 E-chunks
- 支持细粒度内存控制，例如缓存 SM-chunks 可实现 **2× 缓存覆盖**（因占一半体积）。

#### （3）亲和性感知调度（Cache-Affinity Scheduling）
- 引入 **DAG 表示** 建模每个专家重建任务的操作依赖（I/O、解压、恢复）。
- 将任务分为两类：
  - **Type-I**：需加载 SM-chunks（I/O 阻塞）
  - **Type-II**：SM-chunks 已缓存（可并行解压）
- 调度器通过 **块构造（block construction）** 策略，将 Type-II 任务插入 Type-I 任务之间，**重叠 I/O 与 CPU 解压**，最大化并行性。
- **理论保证**：调度算法的 makespan 不超过最优值的 `(3 - 1/L)` 倍（L 为解压线程数）。

#### （4）基于动态规划的缓存规划（Cache Pool Planning）
- 使用 **rank-based workload modeling** 抽象专家激活频率，避免依赖具体 ID。
- 通过 **动态规划（DP）** 计算不同缓存配置下的命中概率分布。
- 枚举所有可行配置，选择 **期望 makespan 最小** 的方案进行缓存分区。

---

### 相比现有方法的优势
| 维度 | ZIPMoE | 现有方法 |
|------|--------|---------|
| **模型保真度** | ✅ 无损压缩，保留原始行为 | ❌ 有损量化，可能引入安全风险 |
| **硬件适配性** | ✅ 针对 UMA 架构优化 | ❌ 假设分离内存，不适用于边缘设备 |
| **I/O 效率** | ✅ 并行 CPU 解压隐藏 I/O 延迟 | ❌ 被动等待 I/O 完成 |
| **调度智能性** | ✅ 亲和性调度 + DAG 优化 | ❌ 简单流水线或静态策略 |
| **缓存灵活性** | ✅ 多级缓存 + 动态规划规划 | ❌ 固定策略（如 LRU/FIFO） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **ShareGPT 数据集**：从其中随机采样 prompts 用于推理测试，确保输入一致性。

### 实验设置
- **模型**：
  - **Decoder-only**: DeepSeekV2-Lite, Qwen1.5-MoE
  - **Encoder-decoder**: SwitchTransformers-Large-128
- **硬件平台**：
  - **Jetson AGX Orin 64GB（Hardware 1）**
  - **Jetson AGX Orin 32GB（Hardware 2）**
  - 均配备 Samsung 970 EVO SSD（读速 3.5GB/s）
- **软件环境**：JetPack 6.2.1 + Ubuntu 22.04
- **压缩后端**：ZSTD 和 LZ4HC

### 评估指标
| 指标 | 含义 |
|------|------|
| **TPOT** | Time Per Output Token，每输出 token 时间 |
| **TTFT** | Time To First Token，首 token 延迟 |
| **Throughput** | 吞吐量（token/s） |
| **End-to-End Latency** | 端到端延迟（随输出长度变化） |
| **Makespan** | 稀疏层完成时间 |

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **Accelerate** | HuggingFace 提供的模型卸载库 |
| **DeepSpeed** | 支持 ZeRO-3 的 SSD 卸载 |
| **MoE-Infinity** | 面向 MoE 的高效卸载系统，支持专家预取 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | ZIPMoE 提升幅度 |
|------|----------------|
| **推理延迟降低** | **最高达 72.77%** |
| **吞吐量提升** | **最高达 6.76×** |
| **TPOT 减少** | 62.65% ~ 97.97%（decoder-only） |
| **TTFT 减少** | 53.25% ~ 87.90%（decoder-only） |
| **端到端加速** | **3.03× ~ 42.49×**（decoder-only） |

---

### 与基线方法的对比结果
#### （1）实时响应性（图7）
- 在低内存预算下，ZIPMoE 显著优于所有基线：
  - **TPOT 降低最多达 97.97%**
  - **TTFT 降低最多达 87.90%**
- 对于 encoder-decoder 模型优势稍弱但仍显著（TPOT ↓81.24%，TTFT ↓83.45%），因其本身 I/O 密集度较低。

#### （2）系统吞吐量（图8）
- 批处理推理中，ZIPMoE 持续领先：
  - **decoder-only 模型：1.79× ~ 42.49× 吞吐提升**
  - **encoder-decoder 模型：1.31× ~ 5.82× 提升**
- 随着 batch size 增大，ZIPMoE 的并行调度优势进一步放大。

#### （3）端到端延迟（图9）
- 在不同输出长度下，ZIPMoE 始终表现最佳：
  - **最大加速达 42.49×**（DeepSeekV2-Lite on Hardware 1）
  - 性能稳定，Pareto 曲线全面领先。

---

### 消融实验结果（图10）
- 移除 **cache planning** 模块后，性能下降明显。
- 替换缓存淘汰策略为 FIFO、Marking、LRU 后，性能均不如 ZIPMoE 自带的规划算法。
- 结论：**分层缓存 + 动态规划规划** 是性能增益的关键组成部分。

---

## 4. 关键结论和发现

### 主要发现
1. **MoE 参数中存在显著的信息冗余**，特别是 BF16 的 exponent bits，可通过无损压缩大幅减少 I/O。
2. **边缘设备的 UMA 架构应被主动利用**，而非被动适应；ZIPMoE 通过 zero-copy 和 CPU 并行解压充分释放多核潜力。
3. **I/O 不再是不可逾越的瓶颈**，通过将“等待加载”转变为“计算生成”，ZIPMoE 将 MoE 推理从 I/O-bound 转为 compute-centric。
4. **缓存策略必须感知压缩状态**，传统的全张量缓存无法充分利用内存，而分层缓存可实现更高性价比。

---

### 方法的局限性
1. **依赖特定压缩格式**：当前仅针对 BF16 的 exponent bits 设计，若未来采用其他数值格式（如 FP8、DF11）需重新分析。
2. **压缩收益有限**：平均压缩率约 30%~34%（ZSTD），仍有提升空间。
3. **调度复杂度较高**：DAG 构建与调度算法有一定实现复杂度，可能影响部署便捷性。
4. **未考虑写回压力**：长期运行下缓存污染与写回机制未深入讨论。

---

### 未来工作方向
1. **扩展至更多模型结构**：如 Vision-MoE、Multimodal MoE。
2. **结合轻量级有损压缩**：在安全可控前提下探索“近无损”压缩，进一步提升压缩率。
3. **支持动态专家激活模式学习**：在线学习 workload skewness，自适应调整缓存策略。
4. **跨设备协同推理**：结合云端与边缘设备，构建分层 MoE 推理网络。

--- 

> **总结一句话**：  
> ZIPMoE 通过 **无损压缩 + 分层缓存 + 亲和调度** 的协同设计，在不牺牲模型准确性和安全性的前提下，实现了 MoE 模型在边缘设备上的高效部署，**将推理延迟降低高达 72.77%，吞吐提升达 6.76×**，为移动端大模型应用提供了可靠解决方案。

</details>

---

### 10. [PHDME: Physics-Informed Diffusion Models without Explicit Governing Equations](https://arxiv.org/abs/2601.21234)

**Authors**: Kaiyuan Tan, Kendra Givens, Peilun Li, Thomas Beckers  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.21234v1  

#### Abstract
Diffusion models provide expressive priors for forecasting trajectories of dynamical systems, but are typically unreliable in the sparse data regime. Physics-informed machine learning (PIML) improves reliability in such settings; however, most methods require \emph{explicit governing equations} duri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PHDME: Physics-Informed Diffusion Models without Explicit Governing Equations

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**稀疏观测下复杂动力系统预测不可靠**的问题，尤其是在以下挑战场景中：
- 数据极度稀缺（limited observations）
- 系统的闭式控制方程（closed-form governing equations）未知或难以建模
- 动力学高度非线性且结构复杂（如软体机器人、PDE系统）

传统方法如纯数据驱动的 Diffusion Models 在数据少时泛化差；而 Physics-Informed Neural Networks (PINNs) 等需要显式的 PDE 残差项，在物理模型不完整时无法应用。

---

### 提出的新方法：PHDME
提出 **PHDME**（Physics-Informed Diffusion Models without Explicit equations），一种结合 **Gaussian Process distributed Port-Hamiltonian System (GP-dPHS)** 与 **Denoising Diffusion Models** 的两阶段框架。

#### 核心思想：
- **第一阶段**：用少量观测训练一个 **GP-dPHS** 模型，学习系统的能量函数表示（Hamiltonian functional），无需知道其闭式表达。
- **第二阶段**：利用 GP-dPHS 生成大量符合物理规律的人工轨迹，用于训练 Diffusion Model，并引入基于 GP 不确定性的 **physics residual loss** 来引导扩散过程。

#### 创新点：
1. **无需显式PDE的物理先验注入**  
   首次将 Port-Hamiltonian 结构先验融入 diffusion 模型，**不依赖于已知的微分方程形式**，仅通过有限观测学习能量梯度。

2. **Amortized 物理采样器**  
   将原本计算昂贵的 GP-dPHS rollout 过程“蒸馏”为快速批量生成的 diffusion sampler，实现**高效、可扩展的物理一致轨迹生成**。

3. **不确定性感知的物理正则化**  
   利用 GP 的贝叶斯特性，在 diffusion 训练中加权 physics loss —— 在置信区域加强约束，在高不确定区域放松，避免过拟合噪声。

4. **后处理校准机制**  
   引入 **Split Conformal Prediction (CP)** 对生成轨迹提供**有限样本下的误差保证**，输出带覆盖率保障的预测区间。

---

### 相比现有方法的优势
| 方法 | 是否需显式PDE | 数据效率 | 泛化能力 | 生成速度 |
|------|----------------|-----------|------------|-------------|
| Standard DDPM | ❌ | ❌（低） | ❌ | ✅ |
| PINNs / Physics-guided Diffusion | ✅ | ⭕ | ⭕ | ❌ |
| GP-dPHS Integrator | ❌ | ✅ | ✅ | ❌（慢） |
| **PHDME (本文)** | ❌ | ✅✅ | ✅✅ | ✅ |

> ✅ 表示优势，❌ 表示劣势，⭕ 表示一般

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个不同类型的 PDE 或真实系统上进行验证：

| 数据集 | 类型 | 描述 |
|--------|------|------|
| **String (Wave PDE)** | 合成 | 固定端弦振动，由波动方程控制 $ \partial_t^2 s = c^2 \partial_x^2 s $ |
| **1D Shallow Water** | 合成 | 浅水波系统，模拟非线性流体行为 |
| **Real-world Spring** | 真实世界 | 使用高速相机拍摄红色弹簧横向振荡，通过 RGB 分割 + skeletonization 获取中心线位移轨迹 |

> 所有任务均限制为仅使用 **20条稀疏轨迹**作为原始观测数据。

---

### 实验设置
- **输入格式**：将时空演化编码为图像形式（spatiotemporal field as image），时间作横轴，空间作纵轴，状态变量（如位移、动量）以通道方式输入。
- **模型架构**：采用 U-Net 作为 diffusion model 的 backbone。
- **训练流程**：
  1. 先用稀疏数据训练 GP-dPHS；
  2. 从 GP-dPHS posterior 中采样 Hamiltonian gradient 并数值积分，生成 10,000 条人工训练轨迹；
  3. 用这些轨迹训练 diffusion model，并加入 physics-informed loss；
  4. 最后对 diffusion 输出应用 conformal prediction 进行校准。

---

### 评估指标
| 指标 | 定义 | 用途 |
|------|------|------|
| **MSE** | 均方误差（vs ground truth） | 衡量预测准确性 |
| **NCS (Non-Conformity Score)** | 轨迹级 MSE 的排序统计量 | 用于 conformal prediction 的校准质量评估 |
| **Coverage Rate** | 真实轨迹落入预测区间的比例 | 评价 uncertainty quantification 可靠性 |

---

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **DDPM (Standard)** | 无任何物理约束的标准 diffusion model |
| **DDPM + Limited Physics** | 加入简单边界条件（如固定端），但无深层物理先验 |
| **GP-dPHS Integrator** | 直接使用 GP-dPHS 数值 rollout，代表“黄金标准”物理一致性，但极慢 |
| **Neural ODE** | 纯数据驱动连续模型，无物理结构先验 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| Dataset | Metric | PHDME | DDPM | DDPM+BC | GP-dPHS | NeuralODE |
|--------|--------|-------|------|---------|----------|-----------|
| **String** | MSE | **2.74e-3** | 3.81e-3 | 4.53e-3 | 2.03e-2 | 2.54e-2 |
|          | NCS | **6.41e-3** | 6.82e-3 | 9.80e-3 | – | – |
| **Shallow Water** | MSE | **2.06e-2** | 2.31e-2 | 2.92e-2 | 2.23e-1 | 4.76e-2 |
|                   | NCS | **9.75e-2** | 1.02e-1 | 1.05e-1 | – | – |
| **Real-world Spring** | MSE | **5.21e-2** | 5.02e-2 | 5.44e-2 | 7.65e-1 | 2.037 |
|                       | NCS | **2.93e-3** | 3.07e-3 | 7.09e-4 | – | – |

> ✅ PHDME 在合成数据上显著优于所有 baseline（平均 MSE ↓~28%）  
> ✅ 在真实弹簧数据上与最佳 DDPM 相当，但具有更强的物理一致性  
> ✅ NCS 更小，说明生成分布更集中、更可靠  

---

### 与其他方法的关键对比发现
- **PHDME vs DDPM**：尽管 DDPM 在真实弹簧上 MSE 略优，但其生成轨迹常违反能量守恒（见 Figure 11），而 PHDME 严格遵循 Hamiltonian 动力学。
- **PHDME vs GP-dPHS**：两者物理一致性接近，但 **PHDME 生成速度快 20 倍以上**（见 Table 2）：
  
  | Method | Avg Speed (s/sample) |
  |--------|------------------------|
  | GP-dPHS | 4.1 |
  | PHDME | **0.2** |

- **PHDME vs NeuralODE**：NeuralODE 在训练分布内表现良好，但在**未见过的初始条件下迅速崩溃**（见 Figure 15），而 PHDME 凭借物理先验实现强外推能力。

---

### 消融实验结果（Ablation Study）
- **替换 GP-dPHS 为线性回归估计器（oracle quadratic Hamiltonian）**：
  - MSE 从 0.1818（GP-dPHS）上升至 0.2967
  - 证明非参数化的 GP 学习比预设函数形式更具表达力和鲁棒性。
- **移除 uncertainty-weighted physics loss**：
  - 在高噪声区域出现非物理解，验证了不确定性加权的重要性。

---

## 4. 关键结论和发现

### 主要发现
1. **Representation-level 物理先验优于 Equation-level 约束**  
   即使不知道具体 PDE 形式，也能通过学习 **energy-based representation**（如 Hamiltonian gradients）来有效注入物理知识。

2. **Amortization 是关键桥梁**  
   将缓慢但准确的 GP-dPHS “蒸馏”为快速 diffusion sampler，实现了**物理可靠性与生成效率的双赢**。

3. **PHDME 具备强 out-of-distribution 泛化能力**  
   在未见过的初始条件和激励模式下仍能保持合理动态演化，远超纯数据驱动模型。

4. **Uncertainty-aware physics loss 提升训练稳定性**  
   利用 GP 的预测方差动态调整物理约束强度，防止在数据稀疏区过度惩罚。

5. **Conformal Prediction 提供可靠的不确定性量化**  
   实验显示测试集覆盖率达到 **95%**（目标为 90%），表明其具备良好的 finite-sample 保证。

---

### 局限性
- **极端小尺度信号恢复困难**：当状态幅值趋近零时，GP 后验趋于平坦，导致能量梯度估计失真（见 Figure 17）。
- **依赖 Port-Hamiltonian 结构假设**：虽然 J/R/G 模板系数可学，但仍需预先定义算子结构（如是否含耗散项）。
- **目前适用于低维 PDE 系统**：尚未扩展到高维复杂场（如三维 Navier-Stokes）。

---

### 未来工作方向
1. **扩展至更高维和多物理场耦合系统**
2. **联合优化 GP-dPHS 与 diffusion model 的端到端训练**
3. **集成到闭环控制中，构建 Generative MPC 控制器（如 NAPI-MPC）**
4. **探索其他结构先验（如 Lagrangian, GENERIC framework）替代 Port-Hamiltonian**

---

> 💡 **总体影响**：PHDME 提供了一种在**现实世界中常见但被忽视的设定下**（即：数据少 + 物理不完全已知）进行可靠、快速、可解释的动力系统建模的新范式，有望推动科学机器学习在机器人、气候模拟、生物力学等领域的落地应用。

</details>

---

### 11. [SENDAI: A Hierarchical Sparse-measurement, EfficieNt Data AssImilation Framework](https://arxiv.org/abs/2601.21664)

**Authors**: Xingyue Zhang, Yuxuan Bao, Mars Liyao Gao, J. Nathan Kutz  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.21664v1  

#### Abstract
Bridging the gap between data-rich training regimes and observation-sparse deployment conditions remains a central challenge in spatiotemporal field reconstruction, particularly when target domains exhibit distributional shifts, heterogeneous structure, and multi-scale dynamics absent from available...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SENDAI: A Hierarchical Sparse-measurement, EfficieNt Data AssImilation Framework**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
该论文旨在解决**在观测极度稀疏（sparse observations）且目标域存在分布偏移（domain shift）的情况下，如何高效、准确地重建时空场（spatiotemporal fields）**这一核心挑战。具体应用场景为卫星遥感中的植被指数（如NDVI）重建，面临以下难点：
- 卫星数据常因云层遮挡、传感器故障等导致空间覆盖严重不完整；
- 不同季节间的物候变化（phenological shifts）引入显著的分布偏移；
- 现有深度学习方法通常依赖大量标注数据和GPU集群，难以在资源受限或实时场景中部署。

### **提出的新方法与新思路**
作者提出了 **SENDAI**（Sparse-measurement, EfficieNt Data AssImilation），一个**分层的、高效的稀疏数据同化框架**，其核心思想是将重建任务分解为两个互补路径：

1. **低频路径（Low-Frequency, LF Pathway）**  
   - 利用 **SHRED**（Shallow Recurrent Decoder Networks）结合 **Takens' embedding theorem**，从模拟数据中学习主导的动力学模式；
   - 引入 **latent-space GAN alignment**，通过对抗训练对齐模拟数据与真实观测之间的潜在空间分布，实现跨域适应（domain adaptation）。

2. **高频路径（High-Frequency, HF Pathway）**  
   - 采用 **sequential frequency peeling** 策略，逐层提取不同频率成分的残差修正；
   - 每一层使用 **coordinate-based Implicit Neural Representation (INR)** 进行解码，确保空间连续性和结构保真；
   - 引入 **spectral constraints** 和 **frequency exclusion mechanism**，防止模式泄漏（mode leakage），提升可解释性。

### **相比现有方法的优势**
| 维度 | SENDAI优势 |
|------|-----------|
| **极端稀疏性容忍** | 仅需 **64个传感器**（占空间域1.56%）即可完成高质量重建，远低于传统方法所需密度 |
| **计算效率高** | 可在标准CPU上运行，单站点训练时间仅数分钟，适合边缘设备和实时应用 |
| **结构保持能力强** | 显著保留场拓扑结构（field topologies）、土地覆盖间断（land cover discontinuities）和空间梯度 |
| **物理可解释性强** | 分层频率剥离机制使高频修正具有明确的物理意义（如气候驱动、水文控制、土壤异质性） |
| **泛化能力好** | 在六种不同气候区（地中海、大陆性、干旱、亚热带）均表现优异 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **遥感平台**：NASA Terra/Aqua 卫星搭载的 **MODIS**（Moderate Resolution Imaging Spectroradiometer）
- **研究变量**：**Normalized Difference Vegetation Index (NDVI)**，用于表征植被覆盖状态
- **研究区域**：全球六个代表性站点，涵盖多种气候与土地利用类型：
  - Central Valley, USA（灌溉农业）
  - Corn Belt, USA（雨养作物轮作）
  - Imperial Valley, USA（沙漠绿洲农业）
  - Tarim Basin, China（高山荒漠绿洲）
  - Guadalquivir Valley, Spain（地中海混合农业）
  - Riverina, Australia（半湿润混合种植）

> 数据通过 **Google Earth Engine** 获取，并进行云掩膜处理与重采样至统一的 $64 \times 64$ 网格。

### **实验设置**
- **模拟-真实域划分**：以**不同季节作为“模拟”与“真实”数据源**，模拟分布偏移：
  - 模拟期（Simulation）：春季（如 Apr–Jun）
  - 真实期（Ground Truth）：夏季/秋季（如 Jul–Oct）
- **传感器配置**：随机布置 **64个传感器**（约占总像素1.56%），记录时间序列观测
- **输入历史长度**：使用过去 $L=5$ 个时间步的传感器历史作为输入

### **评估指标**
| 指标 | 描述 |
|------|------|
| **RMSE** | 衡量点对点误差，反映数值准确性 |
| **SSIM**（Structural Similarity Index Measure） | 衡量结构相似性，特别关注边界、纹理和拓扑结构的保持能力，为主导评价指标 |

### **基线方法对比**
| 方法 | 类型 | 特点 |
|------|------|------|
| **SG+IDW** | 传统流程 | Savitzky-Golay滤波 + 反距离加权插值 |
| **HANTS+IDW** | 季节建模 | 谐波分析去云 + IDW插值 |
| **Kriging** | 地统计 | 高斯过程回归（RBF核） |
| **MMGN** | 深度学习 | 基于Gabor滤波的隐式神经网络，最新SOTA之一 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（SSIM）**

#### **SENDAI Jr. 性能（适用于低频主导场景）**
| 站点 | 最佳基线 SSIM | SENDAI Jr. SSIM | **相对提升** |
|------|----------------|------------------|-------------|
| Central Valley | 0.2612 | **0.5747** | **+120%** |
| Corn Belt | 0.1588 | **0.4530** | **+185%** |
| Guadalquivir | 0.1849 | **0.3655** | **+98%** |

> ✅ **最大SSIM提升达185%**，显著优于所有传统与深度学习基线。

#### **完整SENDAI性能（含高频剥离，适用于复杂地形）**
| 站点 | Cheap2Rich SSIM | SENDAI SSIM | **相对提升** |
|------|------------------|--------------|-------------|
| Imperial Valley | 0.4041 | **0.4668** | +15.5% |
| Tarim Basin | 0.3505 | **0.4777** | **+36.3%** |
| Riverina | 0.2761 | **0.3354** | +21.5% |

> 🔺 **在Tarim Basin达到最高增益（+36.3%）**，因其具有强烈的山盆边界和多尺度动态。

### **与基线方法的对比结果**
- 所有基线方法（包括MMGN）在**极端稀疏条件下失效**：
  - IDW类方法产生“牛眼效应”（bullseye artifacts）；
  - Kriging过度平滑，完全抹除边界信息（Tarim Basin上SSIM仅为0.0449）；
  - MMGN虽为深度模型，但在1.56%传感器下仍无法捕捉细粒度结构。
- SENDAI不仅提升SSIM，在RMSE上也同步改善，表明其兼具**精度与结构保真**。

### **消融实验结果**
- **是否启用Hierarchical Peeling**：
  - 移除分层剥离后，SSIM下降明显（尤其在Tarim Basin）；
  - 单层HF易出现模式混叠，而分层策略能有效分离气候、水文、土壤等不同来源的修正信号。
- **是否使用INR解码器**：
  - 替换为普通MLP会导致输出呈现“点状伪影”（dotted artifacts），空间不连贯；
  - INR结合Fourier positional encoding显著增强高频恢复能力。
- **频率排除机制（frequency exclusion）**：
  - 无此机制时，后续层会重复捕获已发现的频率，造成冗余；
  - 加入后各层修正更具独立性和可解释性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **分层频率建模是应对异构时空场的关键**：  
   将重建任务按频率分解，LF路径捕获大尺度趋势，HF路径逐层剥离局部细节，实现了**结构与细节的协同优化**。

2. **极低观测密度下仍可实现高保真重建**：  
   仅用 **1.56%的空间采样率**（64传感器）即可重建完整场，突破了传统方法对密集观测的依赖。

3. **轻量化架构支持边缘部署**：  
   SENDAI可在CPU上运行，**单次推理耗时秒级**，适合无人机、小卫星、地面站等资源受限环境。

4. **重建结果更利于下游推断**：  
   由于修正项具有物理可分性（spectrally separable），重建后的场更适合用于反演间接观测变量（如土壤湿度、地表温度）。

### **方法的局限性**
1. **假设空间结构平稳**：当前框架未考虑土地覆盖变化、人为扰动等引起的非平稳性；
2. **传感器位置随机设定**：未优化布点策略，未来可通过信息论指导最优采样设计；
3. **跨大陆泛化待验证**：目前为区域内季节迁移测试，尚未严格验证跨气候带迁移能力；
4. **单变量重建为主**：尚未扩展到多变量联合重建（如NDVI + LST + SM）。

### **未来工作方向**
1. **扩展至非平稳场景**：引入动态编码器或在线更新机制，适应土地利用变化；
2. **主动感知与最优布点**：结合信息熵或互信息设计传感器布局，进一步降低采样需求；
3. **多变量联合重建**：利用共享潜在空间建模多物理量耦合关系；
4. **向其他地球科学任务迁移**：
   - 土壤水分（soil moisture）
   - 地表温度（land surface temperature）
   - 积雪动态（snow dynamics）
   - 洪涝制图（flood mapping）
5. **深空探测应用**：应用于火星、月球等地外天体监测，在极低带宽下实现科学数据压缩与重建。

---

> 🌍 **Impact Statement**：  
> SENDAI提供了一个通用模板——**从丰富先验中学习空间先验 → 通过潜空间对齐适配稀疏目标 → 必要时分层剥离高频修正**——适用于任何具备参考数据、稀疏观测和异构结构的物理系统重建任务。代码已开源：`github.com/xswzaqnjimko/SENDAI_framework`。

</details>

---

### 12. [Zonkey: A Hierarchical Diffusion Language Model with Differentiable Tokenization and Probabilistic Attention](https://arxiv.org/abs/2601.21768)

**Authors**: Alon Rozental  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.21768v1  

#### Abstract
Large language models (LLMs) have revolutionized natural language processing, yet they remain constrained by fixed, non-differentiable tokenizers like Byte Pair Encoding (BPE), which hinder end-to-end optimization and adaptability to noisy or domain-specific data. We introduce Zonkey, a hierarchical...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Zonkey: A Hierarchical Diffusion Language Model with Differentiable Tokenization and Probabilistic Attention*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Large Language Models (LLMs)** 受限于非可微分（non-differentiable）的固定 **tokenizer**（如 BPE），导致以下问题：
- 出现 **out-of-vocabulary (OOV)** 问题；
- 对噪声文本、领域特定数据适应能力差；
- 无法在端到端训练中优化 tokenization 过程；
- 扩散模型（diffusion models）在文本生成中面临离散 token、语义失真和固定长度输出等挑战。

Zonkey 针对上述瓶颈，提出了一种**完全可微分、层次化的扩散语言模型框架**，从原始字符开始实现端到端学习。

---

### 🚀 提出的新方法与创新点

#### （1）Differentiable Tokenizer: Segment Splitter
- 引入一个可学习的 **Segment Splitter**，通过预测每个位置的 **beginning-of-sequence (BOS) 概率** 来进行软分割；
- 分割边界无需显式监督即可自然涌现为有意义的语言单位（如空格处分词、句号后断句）；
- 利用 **existence probabilities** 和 **existence shares** 实现梯度反向传播，使 tokenization 成为可训练模块。

#### （2）Probabilistic Attention
- 提出一种新型注意力机制，将每个位置赋予一个存在概率 $ p_k \in (0,1] $，模拟无限长序列；
- 注意力得分通过 $ \log(p_k / p_q) $ 调整，低存在概率的位置对高概率位置影响极小，形成“软截断”；
- 支持变长建模且避免了传统硬掩码（hard masking）带来的梯度不连续问题。

#### （3）Hierarchical Diffusion with DDMM
- 设计 **Denoising Diffusion Mixed Model (DDMM)**，结合 DDPM 的稳定性与 DDIM 的高效性；
- 在潜在空间中进行去噪重建，支持任意长度输出；
- 引入混合步长目标（mixed-step objective），鼓励模型在有信心时大胆跳跃，在不确定时谨慎推进。

#### （4）Differentiable Stitcher
- 构造 **Stitcher** 模块用于无缝拼接重叠段落，保持跨段表示一致性；
- 使用 soft offset inference 和 constrained cross-attention 实现可微重组；
- 支持递归层级结构（level-l 输出作为 level-l+1 输入），构建深层抽象。

#### （5）端到端可微分层级架构
- 整个流程从字符输入 → 层级压缩 → 扩散去噪 → 拼接输出全部可微；
- 支持 **variable-length generation**, **non-sequential infilling**, 并天然适配长上下文扩展。

---

### 🔍 相比现有方法的优势

| 方面 | 传统方法局限 | Zonkey 的优势 |
|------|---------------|----------------|
| **Tokenizer** | 固定规则（BPE）、不可微、静态 | 可学习、自适应、不同层级自动涌现词/句边界 |
| **Attention** | Hard masking 导致梯度断裂 | Probabilistic Attention 保留梯度，支持软截断 |
| **Generation** | 自回归逐token生成慢；扩散模型难处理离散性 | 并行解压 + 潜在空间扩散，支持快速长文本生成 |
| **Infilling** | AR 模型难以双向补全 | 天然支持任意位置 infilling（如中间段缺失） |
| **Hierarchy** | 多数模型仅单层抽象 | 显式多级压缩（char → word → sentence） |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **Wikipedia 英文语料**（清洗后的子集）
- 以原始字符为输入，不依赖任何预定义词汇表

### ⚙️ 实验设置
- **模型规模**：小型原型，在 **单张 GPU** 上训练（资源受限）
- **层级结构**：
  - Level 0：字符 → 类似“词”的向量（max_seq_len=32）
  - Level 1：类词单元 → 类似“句子”的抽象
- **训练方式**：端到端联合优化，采用 curriculum learning，先稳定底层再向上构建
- **输入形式**：raw characters → character embeddings → Segment Splitter

### 🎯 评估指标（Qualitative为主）
由于模型无固定 vocabulary、输出为连续 latent space，标准 token-based 指标（如 perplexity）不可直接应用，因此主要依赖：
- **定性分析（qualitative evaluation）**：
  - 生成文本的连贯性（coherence）
  - 是否自发形成 word/sentence-level 结构
  - 自适应分词是否符合语言直觉（如空格/句号处高 BOS 概率）
- **辅助定量信号**：
  - MLM accuracy（mask prediction）
  - Reconstruction cosine similarity（denoised vs. ground truth）
  - Collapse prevention loss（latents 区分度）

### 🆚 基线方法对比
未提供与主流 LLM（如 Llama、BERT）的量化对比，原因在于架构根本差异。但文中指出其相较以下方向具有优势：
- **BPE-based LLMs**：缺乏可微分 tokenizer
- **CANINE / ByT5**：虽操作于字符/字节，但使用固定卷积或 Transformer，无动态可学习分段
- **Byte Latent Transformer (BLT)**：强制每 patch 具有相同 entropy，策略次优
- **Hierarchical Diffusion LM (HDLM)**：通常基于离散 token，缺乏全路径可微性

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能表现
尽管是小规模验证性实验，Zonkey 展现出以下积极结果：

| 成果 | 描述 |
|------|------|
| **Emergent Hierarchies** | 在无监督情况下，Level 0 的 BOS 概率在空格后显著升高（对应单词边界）；Level 1 在句号后升高（对应句子起始），表明语义结构自然浮现 |
| **Coherent Text Generation** | 可从纯噪声中生成语法合理、主题一致的英文句子，例如："Quantum computing harnesses quantum mechanics..." |
| **Adaptive Tokenization** | 分割行为随上下文调整，优于固定窗口或等熵划分策略 |
| **Effective Infilling** | 成功完成非连续文本补全任务，如给定前缀 `"Bob is a"` 和后缀 `"player in the NBA"`，正确补全为 `"basketball"` |

### 🔬 消融实验（Ablation Studies）
虽然没有独立表格列出消融结果，但在正文中隐含多个控制变量分析：
- **Existence Shares 的作用**：防止 Splitter 将 BOS 集中在简单区域而忽略复杂部分，确保均匀监督；
- **MLM Loss 的必要性**：去除后语义聚类减弱，压缩向量区分度下降；
- **Mixed-step Objective**：提升 DDMM 的鲁棒性和收敛速度，减少模式崩溃风险；
- **Probabilistic Attention vs. Hard Masking**：前者训练更稳定，尤其在低存在概率区域仍能传递有效梯度。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **完全可微分的语言模型是可行的**：Zonkey 验证了从字符到文档级表示的整个 pipeline 可以实现端到端训练。
2. **语言结构可以无监督地自组织出现**：无需标注，模型自发学会在空格、句号等位置进行分割，说明损失函数本身足以驱动语义边界学习。
3. **Probabilistic Attention 是核心技术支柱**：它不仅解决了变长建模问题，还使得 Splitter、Compressor、Denoiser 之间的梯度流动成为可能。
4. **扩散机制适用于层级化文本生成**：DDMM 在 latent space 中实现了高质量重建，并支持灵活的生成模式（如 infilling）。
5. **并行生成潜力巨大**：相比自回归模型，Zonkey 在高层级可并行处理多个 segment，具备良好的 scalability 前景。

---

### ⚠️ 方法的局限性
1. **当前仅为 proof-of-concept**：仅在单卡上训练，停留在 sentence-level 生成，尚未达到 paragraph 或 document 级别；
2. **缺乏标准化 benchmark 对比**：因架构特殊，无法直接比较 BLEU、PPL 等通用指标；
3. **计算开销较高**：重叠 segment + 多层压缩带来冗余计算，需进一步优化效率；
4. **生成质量仍有提升空间**：个别生成句子存在轻微语义偏差或重复现象；
5. **未探索大规模预训练效果**：尚不清楚在更大数据和算力下能否超越主流 LLM。

---

### 🔮 未来工作方向
1. **Scaling Up**：
   - 扩展至更深的 hierarchy（如 level 2 表示段落，level 3 表示篇章）；
   - 使用更大 dataset（如 Common Crawl）和分布式训练。
2. **Quantitative Evaluation Framework**：
   - 开发适用于 continuous latent space 和 variable-length output 的新评估协议；
   - 探索 human evaluation、reconstruction fidelity、domain adaptation benchmarks。
3. **加速推理机制**：
   - 引入 early-exit 策略或 adaptive diffusion steps 以减少 inference 时间；
   - 优化 Stitcher 与 Compressor 的轻量化设计。
4. **下游任务迁移**：
   - 将 learned hierarchical representations 应用于 classification、summarization、retrieval 等任务。
5. **理论分析**：
   - 形式化证明 existence probability decay 与语言结构 emergence 的关系；
   - 分析 Splitter 学习到的边界是否满足信息论最优性（如最小描述长度原则）。

---

## 总结

> **Zonkey 提出了一种全新的、全可微分的层级扩散语言模型范式，打破了传统 LLM 对固定 tokenizer 和自回归生成的依赖。它通过 Segment Splitter、Probabilistic Attention、DDMM 和 Stitcher 四大核心组件，实现了从字符到语义单元的端到端学习，在小规模实验中已展现出生成连贯文本、自组织语言结构和灵活 infilling 的能力。虽然目前仍是概念验证阶段，但它为下一代真正“gradient-based”的语言模型提供了重要路径。**

开源地址：[https://github.com/ARozental/Zonkey](https://github.com/ARozental/Zonkey)

</details>

---

### 13. [Heterogeneous Vertiport Selection Optimization for On-Demand Air Taxi Services: A Deep Reinforcement Learning Approach](https://arxiv.org/abs/2601.21316)

**Authors**: Aoyu Pang, Maonan Wang, Zifan Sha, Wenwei Yue, Changle Li, Chung Shue Chen, Man-On Pun  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.21316v1  

#### Abstract
Urban Air Mobility (UAM) has emerged as a transformative solution to alleviate urban congestion by utilizing low-altitude airspace, thereby reducing pressure on ground transportation networks. To enable truly efficient and seamless door-to-door travel experiences, UAM requires close integration with...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Heterogeneous Vertiport Selection Optimization for On-Demand Air Taxi Services: A Deep Reinforcement Learning Approach

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**按需空中出租车服务中的多模式出行效率瓶颈**，特别是**地面与空中交通协同不足导致的整体旅行时间过长**的问题。具体聚焦于：
- **异构 vertiport 系统中乘客选择出发/到达 vertiport 的优化难题**；
- 传统方法无法动态适应实时交通状态、排队延迟和资源不均等复杂因素；
- 缺乏统一的端到端（door-to-door）策略规划框架。

### 提出的新方法与新思路
作者提出了一个名为 **Unified Air-Ground Mobility Coordination (UAGMC)** 的深度强化学习框架，其核心创新包括：

- **首次将 air taxi 与 ground transportation 的策略规划进行统一建模**，构建了一个完整的 MDP（Markov Decision Process）模型，涵盖从起点到终点的四个阶段：地面接驳 → 出发 vertiport 排队 → 空中飞行 → 终点地面送达。
  
- 设计了两个关键模块以处理高维异构信息：
  - **Multi-Source Contextual Embedding (MSCE)**：对来自 V2X 网络的多源异构数据（如 OD 请求、地面速度、排队长度、eVTOL 状态等）进行结构化编码。
  - **Spatio-Temporal Integration Network (STIN)**：基于 Transformer 架构融合时空依赖关系，提取全局上下文特征，提升模型对动态系统演化的感知能力。

- 采用 **PPO（Proximal Policy Optimization）** 作为 Actor-Critic 框架的基础算法，并设计增量奖励机制解决稀疏奖励（sparse reward）问题。

### 相比现有方法的优势
- **动态适应性强**：相比静态规则或启发式方法，UAGMC 能根据实时交通、排队和车辆可用性做出响应式决策。
- **联合优化能力**：同时考虑地面行程时间和 vertiport 排队等待时间，避免“就近选择”带来的局部拥堵。
- **可扩展性好**：通过深度神经网络自动学习复杂模式，适用于大规模、高维度的城市交通场景。

---

## 2. 核心实验方法和设置

### 数据集与仿真环境
- 使用**自定义模拟器**（基于 prior studies [35,66–69] 开发），未使用真实世界公开数据集。
- 模拟城市地图为 `30km × 30km`，部署三个 vertiport：
  - **Vertiport 0**：郊区/机场区域（容量大、充电快）
  - **Vertiport 1**：市中心核心区（容量小、充电慢）
  - **Vertiport 2**：纯降落点（无排队）
- 客流生成遵循现实分布，O-D距离范围为 42–48 km，每步新增 1 名乘客。

### 实验设置
- **eVTOL 参数**：
  - 巡航速度：120 km/h
  - 座位数：3 或 4 人
  - 需满员起飞，落地后必须完成充电才能再次调度
- **地面交通模型**：采用 Greenberg 流量模型计算平均车速
- **训练参数**：
  - RL折扣因子 γ = 0.99
  - PPO剪裁范围 ε = 0.2
  - 状态序列长度 K = 6

### 评估指标
| 指标 | 含义 |
|------|------|
| **ATT** (Average Total Travel Time) | 总平均旅行时间（含所有阶段） |
| **AET** (Average Effective Travel Time) | 去除等待时间的有效移动时间 |
| **AWT** (Average Waiting Time) | 在出发 vertiport 的平均排队时间 |
| **AGT** (Average Ground Travel Time) | 地面段总时间（去程+返程） |
| **AAT** (Average Air Taxi Travel Time) | 空中飞行时间 |
| **RPV-0 / RPV-1** | 分配至 Vertiport 0 和 1 的乘客比例 |

### 基线方法对比
共比较七种方法：
1. **Ground**：仅使用 CAV 地面出行
2. **SPF**（Shortest Physical First）：选择物理距离最近的 vertiport
3. **Rule-Based**：按预设服务率分配
4. **STTF**（Shortest Time to Facility）：基于估计地面时间选择
5. **QTTI**（Queue Time and Travel Integration）：综合排队与行驶时间的成本函数
6. **Vanilla-PPO**：标准 PPO，简单拼接状态输入
7. **UAGMC-L**：UAGMC 的 LSTM 版本（STIN 替换为 LSTM）
8. **UAGMC-A**：本文提出的方法（STIN 使用 Attention）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）
| 方法 | ATT (min) | AWT (min) | AGT (min) | AAT (min) |
|------|-----------|-----------|-----------|-----------|
| SPF | 133.65 | 106.53 | 5.93 | 21.18 |
| Rule-Based | 62.30 | 32.34 | 7.95 | 22.01 |
| STTF | 52.39 | 22.13 | 8.04 | 22.22 |
| QTTI | 51.87 | 22.44 | 7.23 | 22.20 |
| Vanilla-PPO | 57.47 | 27.17 | 8.12 | 22.19 |
| **UAGMC-L** | **48.69** | **17.97** | 8.51 | 22.21 |
| **UAGMC-A** | **46.63** | **15.48** | 8.83 | 22.32 |

> ✅ **UAGMC-A 将 ATT 降至 46.63 分钟，相比 QTTI 提升约 10%，相比 SPF 提升高达 65%**

### 与基线方法的对比结果
- **显著优于所有规则类方法**（SPF、STTF、QTTI），尤其是在高需求下仍能保持低等待时间。
- **优于 Vanilla-PPO**：说明简单的 RL 框架难以捕捉复杂的时空依赖，而 MSCE + STIN 显著提升了表现。
- **UAGMC-A > UAGMC-L**：表明基于 Attention 的 STIN 比 LSTM 更适合建模长期时空依赖，在 ATT 上进一步降低 4.3%。

### 消融实验结果（Table 4）
| 模块组合 | ATT (min) | AWT (min) | 改进幅度（vs baseline） |
|--------|----------|-----------|-----------------------|
| 无 MSCE & 无 STIN | 63.07 | 32.82 | — |
| 有 MSCE | 60.57 | 30.08 | ATT ↓4.0%, AWT ↓8.2% |
| 有 STIN | 56.31 | 25.57 | ATT ↓10.8%, AWT ↓22.0% |
| **MSCE + STIN (完整版)** | **46.63** | **15.48** | **ATT ↓26.1%, AWT ↓52.7%** |

> 🔍 结果证明：**MSCE 和 STIN 协同作用至关重要**，单独使用任一模块已有明显增益，联合使用实现最大性能突破。

### 参数敏感性分析（Table 5）
- 当乘客数量从 100 增加到 300 时，**AWT 显著上升**，但在 eVTOL 容量从 3 扩展到 4 后，**系统抗压能力大幅提升**，ATT 基本稳定。
- 表明 UAGMC-A 在不同负载条件下具有良好的鲁棒性和自适应调度能力。

---

## 4. 关键结论和发现

### 主要发现
1. **vertiport 选择是影响整体效率的关键因素**，单纯追求“最近”会加剧排队拥堵，反而延长总行程时间。
2. **等待时间是主要瓶颈**，占 ATT 的约 31%（UAGMC-A 中为 15.5 min），因此优化排队管理至关重要。
3. **深度 RL 可有效应对异构、动态、高维的多模式交通系统调度问题**，尤其在结合时空建模后效果显著。
4. **UAGMC 实现了 34% 的平均旅行时间减少**（相对于传统 proportional allocation 方法），验证了其在提升 UAM 系统效率方面的巨大潜力。

### 方法的局限性
- 当前假设**降落 vertiport 固定为离目的地最近的一个**，未优化 landing-side 决策。
- 忽略了 **airspace 冲突管理、航线规划冲突、通信延迟**等实际运行约束。
- 模拟环境中未引入天气、突发事件或人为干扰因素。
- 尚未在真实城市环境中部署测试，泛化能力有待验证。

### 未来工作方向
- 引入 **multi-agent RL** 实现多个 eVTOL 的协同路径规划与冲突规避。
- 扩展为 **end-to-end mission scheduling**，覆盖起飞、巡航、降落、充电全过程。
- 融合更精细的 **air traffic management (ATM)** 模型，支持高密度 UAM 运营。
- 加入 **communication constraints** 和 **delay modeling**，增强系统的实用性与可靠性。
- 探索与 **MaaS 平台集成**，实现个性化出行推荐与公平性权衡。

---

> 📌 **总结一句话**：  
> 本文提出的 **UAGMC 框架**通过深度融合 V2X 多源信息与时空建模，利用 DRL 实现了对异构 vertiport 系统的高效调度，在仿真中实现了 **34% 的旅行时间缩减**，为未来智能城市空地一体化出行提供了重要技术路径。

</details>

---

### 14. [PathWise: Planning through World Model for Automated Heuristic Design via Self-Evolving LLMs](https://arxiv.org/abs/2601.20539)

**Authors**: Oguzhan Gungordu, Siheng Xiong, Faramarz Fekri  
**Category**: cs.AI  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.20539v2  

#### Abstract
Large Language Models (LLMs) have enabled automated heuristic design (AHD) for combinatorial optimization problems (COPs), but existing frameworks' reliance on fixed evolutionary rules and static prompt templates often leads to myopic heuristic generation, redundant evaluations, and limited reasonin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PathWise: Planning through World Model for Automated Heuristic Design via Self-Evolving LLMs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的基于 **Large Language Models (LLMs)** 的 **Automated Heuristic Design (AHD)** 方法在组合优化问题（Combinatorial Optimization Problems, COPs）中存在以下局限：
- **依赖固定的进化规则** 和静态提示模板，导致生成的启发式算法短视（myopic），缺乏长期规划能力。
- **缺乏状态记忆机制**，无法有效记录启发式的推导历史和演化路径，导致重复评估、冗余搜索。
- **探索效率低**，难以区分不同启发式变换之间的语义差异，过度依赖统计访问频率而非语义理解。

### 提出的新方法与思路
作者提出 **PathWise**，一种基于 **World Model** 的多智能体推理框架，将启发式设计建模为一个**状态感知的序列决策过程**。其核心创新包括：

#### （1）引入 **Entailment Graph** 作为状态化记忆
- 将启发式生成过程形式化为一个 **Markov Decision Process (MDP)**。
- 使用 **Entailment Graph** 编码每个启发式的推导逻辑（Derivation Rationale）、父节点信息、性能历史等，形成紧凑且有状态的搜索轨迹表示。
- 支持跨代的信息复用与避免，提升搜索效率。

#### （2）多智能体协同架构
- **Policy Agent**：负责高层策略规划，选择父启发式并生成自然语言形式的“衍生指令”（Derivation Rationale），动态定义如何组合或修改启发式。
- **World Model Agent**：执行具体代码生成，根据 Policy 的指令生成新的启发式函数。
- **Critic Agents**（双批评者）：
  - **Policy Critic**：分析不同策略的有效性，提供路由反馈以改进父节点选择和指令设计。
  - **World Model Critic**：通过对比最优与最差生成结果，提炼代码级改进建议（如简化结构、减少复杂度）。

该设计实现了从“试错式进化”到“状态感知规划”的转变。

#### （3）Prompt-Level 多样性机制
- **Prompt Perturbation**：引入随时间衰减的探索率 $ \epsilon(l) $，随机注入探索性短语（如“尝试非标准融合方式”），鼓励早期多样化探索。
- **State Shuffling**：随机打乱输入上下文中节点顺序，缓解 LLM 对位置偏好的偏差（positional bias）。

---

### 相比现有方法的优势
| 维度 | 传统方法（如 FunSearch, MCTS-AHD） | PathWise |
|------|-------------------------------|---------|
| **搜索范式** | 固定算子 / 统计驱动 | 动态指令 / 语义驱动 |
| **记忆机制** | 无或仅保留种群 | 显式图结构记忆（Entailment Graph） |
| **规划能力** | 无状态采样 | 状态感知的 MDP 决策 |
| **多样性控制** | 被动（靠变异） | 主动（Prompt扰动 + 状态洗牌） |
| **收敛速度** | 较慢，易陷入局部最优 | 更快收敛，更低方差 |

---

## 2. 核心实验方法和设置

### 数据集
在多个经典 **NP-hard COPs** 上进行测试，涵盖多种搜索框架：
- **Traveling Salesman Problem (TSP)**
- **Knapsack Problem (KP)**
- **Capacitated Vehicle Routing Problem (CVRP)**
- **Multiple Knapsack Problem (MKP)**
- **Orienteering Problem (OP)**
- **Bin Packing Problem (BPP)**（含在线与离线版本）

所有实例均通过统一协议合成，确保公平比较。

### 实验设置
- **LLM 后端**：使用 `GPT-4o-mini`、`GPT-5-nano (low)` 和 `GPT-5-nano (medium)` 进行评估。
- **评估预算**：固定为 **500 次启发式评估**（ne = 500），而基线允许最多 1000 次。
- **温度参数**：所有代理设为 1.0，保证生成多样性。
- **运行次数**：每项任务独立运行 3 次取平均值。

### 评估指标
- **目标函数值（Obj.）**：越小越好（最小化问题）。
- **Optimality Gap (%)**：相对于已知最优解或最强基线的差距。
- **Mean Relative Gap Improvement (MRGI)**：衡量 PathWise 相对于各基线的平均相对提升幅度。
- **Selection Diversity Rate (SDR)**：衡量父节点选择的多样性比例。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| **Population-based** | FunSearch, EoH, ReEvo, HSEvo |
| **Tree-based** | MCTS-AHD |
| **手工设计** | Greedy Construct, ACO, DeepACO, KGLS |
| **神经求解器** | POMO, VRP-DACT, NeuOpt, NeuralGLS, GNNGLS |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 2）
#### ✅ 在 **TSP-Constructive** 上的表现（N=200）
| 方法 | Obj. ↓ | Gap (%) |
|------|--------|--------|
| Greedy Construct | 13.430 | 25.41% |
| MCTS-AHD | 12.403 | 15.82% |
| **PathWise (Ours)** | **12.276** | **14.63%** |

> 在更难的 **N=500** KP 测试集中，PathWise 的 MRGI 高达 **81.34%**。

#### ✅ 在 **ACO 框架** 下的平均 MRGI 提升
| 问题 | MRGI ↑ |
|------|-------|
| TSP | 60.22% |
| CVRP | 38.73% |
| MKP | 89.35% |
| OP | 54.81% |
| Offline BPP | 44.86% |

> 表明 PathWise 在更大规模和更复杂问题上优势显著。

### 与基线方法的对比结果
- **全面超越所有 LLM-based AHD 方法**：在所有测试集和 LLM 后端下，PathWise 均取得最佳性能。
- **优于神经求解器**：
  - 在 **TSP (N=200)** 上优于 **POMO**。
  - 在 **OP (N=200)** 和 **MKP** 上优于 **DeepACO**，尽管后者需要专门训练。
- **更快收敛**：如 Figure 1 所示，在仅一半评估预算下，PathWise 已超过基线最终性能，且方差更低。

### 消融实验结果（Ablation Study）

#### （1）移除 Critic Agents（Table 3）
| 方法 | TSP50 Gap (%) |
|------|-------------|
| PathWise (完整) | 8.79% |
| w/o Policy Critic | 10.21% |
| w/o World Model Critic | 9.63% |
| w/o Both | 14.42% |

> **Policy Critic 影响最大**，说明高层策略反馈对引导搜索至关重要。

#### （2）移除多样性机制（Table 4）
| 方法 | TSP50 Gap (%) | SDR (%) |
|------|--------------|--------|
| PathWise (完整) | 8.79% | 75.79% |
| w/o Prompt Perturbation | 9.58% | 70.30% |
| w/o State Shuffling | 9.02% | 61.03% |
| w/o Both | 11.43% | 53.76% |

> **Prompt Perturbation 是多样性的主要驱动力**；State Shuffling 主要缓解位置偏见。

#### （3）超参数敏感性分析（Appendix F.4）
- 默认设置 `(Na=2, Nu=2, Np=6, Imax=3)` 性能稳定。
- 过大或过小的参数都会降低性能，验证了设计合理性。

---

## 4. 关键结论和发现

### 主要发现
1. **状态感知规划优于盲目搜索**：通过 **Entailment Graph** 和多智能体协作，PathWise 实现了真正意义上的“推理式”启发式演化，而非随机突变。
2. **高层策略反馈比底层代码微调更重要**：Policy Critic 的作用远大于 World Model Critic，表明“往哪走”比“怎么写”更具决定性。
3. **多样性是高效进化的前提**：Prompt-Level 多样性机制显著提升了探索广度，防止早熟收敛。
4. **可扩展性强**：随着问题规模增大，PathWise 的相对优势持续扩大，展现出良好的泛化能力。

### 方法的局限性
1. **依赖 LLM 输出稳定性**：LLM 的随机性可能导致生成质量波动，影响搜索稳定性。
2. **上下文长度限制**：虽然采用压缩元数据（PM），但在极大规模搜索中仍可能受限于 context window。
3. **实现复杂度高**：相比单代理方法，PathWise 架构更复杂，调试成本更高。

### 未来工作方向
1. **应用于更昂贵的评估场景**：如材料设计、蛋白质序列优化等需模拟或实验验证的任务。
2. **引入可学习的记忆模块**：将 Entailment Graph 与向量检索结合，支持跨任务知识迁移。
3. **自适应调节探索-利用平衡**：动态调整 $ \epsilon(l) $ 和批评强度，进一步提升效率。
4. **扩展至多目标优化**：结合 Pareto 前沿分析，支持多目标启发式集合生成。

--- 

> **总体评价**：PathWise 是 AHD 领域的重要进展，首次系统地将 **LLM 推理能力**、**状态记忆** 和 **多智能体规划** 结合，推动了自动化启发式设计从“黑箱搜索”迈向“白盒推理”。

</details>

---

### 15. [ASTRA: Automated Synthesis of agentic Trajectories and Reinforcement Arenas](https://arxiv.org/abs/2601.21558)

**Authors**: Xiaoyu Tian, Haotian Wang, Shuaiting Chen, Hao Zhou, Kaichi Yu, Yudian Zhang, Jade Ouyang, Junxi Yin, Jiong Chen, Baoyan Guo, Lei Zhang, Junjie Tao, Yuansheng Song, Ming Cui, Chengwei Liu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21558v1  

#### Abstract
Large language models (LLMs) are increasingly used as tool-augmented agents for multi-step decision making, yet training robust tool-using agents remains challenging. Existing methods still require manual intervention, depend on non-verifiable simulated environments, rely exclusively on either super...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ASTRA: Automated Synthesis of agentic Trajectories and Reinforcement Arenas》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前在训练具备工具调用能力的 **LLM agents** 时面临以下挑战：

- **依赖人工干预**：多数方法需要手动标注或设计任务与环境。
- **非可验证的模拟环境**：基于 LLM 模拟工具执行和状态转移，导致强化学习（RL）信号不可靠，缺乏确定性反馈。
- **长视野多轮学习不稳定**：现有方法常将多轮轨迹分解为单步实例，破坏了长期决策连贯性。
- **训练范式单一**：仅采用 SFT 或 RL，难以兼顾泛化能力和在线优化。

### 提出了什么新方法或新思路

作者提出 **ASTRA** —— 一个**全自动、端到端**的框架，用于训练工具增强型语言模型代理，其核心由两个互补组件构成：

#### （1）基于工具调用图拓扑的多轮轨迹合成（用于 SFT）
- 利用真实 MCP 服务器中的工具文档构建 **tool-call graph**。
- 通过 LLM 自动生成合理的 **tool-chain**（工具调用序列），并结合任务生成、增强与质量评分，形成高质量的多轮对话轨迹。
- 实现无需人工标注的 **Supervised Fine-Tuning (SFT)** 数据集构建。

#### （2）基于问答对语义拓扑的可验证环境合成（用于 RL）
- 将人类推理过程建模为 **语义拓扑结构**（semantic topology），从 Q-A 对中提取子问题依赖关系。
- 自动将每个子问题转化为独立、可执行、规则可验证的 Python 工具实现，并封装进沙箱环境。
- 支持 **deterministic multi-turn RL**，确保奖励信号可靠且可复现。

#### （3）统一的两阶段训练方法
- **第一阶段（SFT）**：利用合成的多轮轨迹训练初始策略，提升对工具交互的理解。
- **第二阶段（RL）**：在代码可执行、规则可验证的环境中进行在线多轮 RL，引入：
  - **Irrelevant Tool Mixing**：混合无关工具以增强工具判别能力。
  - **F1-style 轨迹级奖励**：联合优化任务完成率（recall）与调用效率（precision）。

### 相比现有方法的优势

| 维度 | ASTRA 的优势 |
|------|-------------|
| **自动化程度** | 完全自动化，无需人工参与数据构造与验证 |
| **环境可验证性** | 所有环境均为代码可执行、规则可验证，支持稳定 RL |
| **训练稳定性** | 多轮在线 RL + Adaptive Batch Filling 避免梯度消失 |
| **泛化与鲁棒性** | 结合静态工具拓扑（广度）与动态语义拓扑（深度）双重学习 |
| **开源完整性** | 公开完整 pipeline、环境与模型：[GitHub](https://github.com/LianjiaTech/astra) |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### SFT 数据集
- 来源：从 1,585 个 MCP 服务器收集的 19,036 个工具文档，覆盖 41 个领域。
- 规模：54,885 条多轮对话样本，共 580,983 条消息。
- 平均每条含 4.42 次 tool call，72.2% 包含 1–5 次调用。

#### RL 数据集
- 来源：基于 QA 对合成的可执行环境。
- 规模：6,596 个样本，涵盖房地产、电商、医疗等 20+ 领域。
- 特征：
  - 平均 4.37 推理步（hops），最大达 20。
  - 91.3% 子问题需外部工具调用。
  - 场景类型分布：Parallel Multi-Hop (47.8%) > Multi-Hop (34.8%)

### 实验设置和评估指标

#### 模型基础
- 主干模型：`Qwen3-14B` 和 `Qwen3-32B`
- 训练流程：
  - SFT：2 epochs，batch size=32，max seq len=20k
  - RL：online multi-turn RL，batch size=256，max prompt len=25.6k，response len=49.152k

#### 评估基准（Agentic Benchmarks）
| 基准 | 描述 |
|------|------|
| **BFCL-v3 Multi-Turn (BFCL-MT)** | 多轮函数调用评测，强调跨轮状态追踪 |
| **T2-Bench** | 双控环境下的对话代理评测，含用户模拟器 |
| **ACEBench** | 工具学习竞赛式评测，关注任务完成与交互效率 |

#### 非代理任务评测（Non-agentic）
- **AIME2024 / AIME2025**：数学推理任务，检验是否损害基础推理能力

#### 评估协议
- 所有测试使用 `vLLM` 推理引擎
- 温度设置：
  - T2-Bench: temp=0.0（greedy）
  - ACEBench/BFCL-MT: temp=0.6
- 用户模拟器：
  - T2-Bench: GPT-5.1
  - ACEBench: GPT-4.1

### 基线方法对比
- **闭源模型**：Claude-Opus/Sonnet/Haiku, Gemini-3-Pro, GPT-4.1
- **开源模型**：GLM-4.6, LoopTool-32B, Qwen3-14B/32B, Kimi-K2-Instruct

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| Model | BFCL-MT (Overall) | T2-Bench (Overall) | ACEBench (Overall) |
|-------|-------------------|--------------------|---------------------|
| **ASTRA-14B-Thinking-v1** | **58.13** | **57.69** | **68.96** |
| **ASTRA-32B-Thinking-v1** | **64.25** | **63.70** | **71.88** |
| Qwen3-32B | 49.63 | 49.70 | 59.79 |
| GLM-4.6 | 68.00 | 69.63 | 80.00 |
| Claude-Opus-4.5 | 68.38 | 85.79 | 82.09 |

> ✅ **ASTRA-32B 在同规模开源模型中达到 SOTA 性能，接近部分闭源系统**

### 与基线方法的对比结果

- **相比原始 Qwen 模型**：
  - ASTRA-32B 在 BFCL-MT 上提升 **+16.38 pts**
  - 在 ACEBench 上提升 **+12.09 pts**
- **相比其他开源工具模型**（如 LoopTool、Qwen3）：
  - 显著优于所有同类开源模型，尤其在多轮复杂任务上表现突出。
- **接近闭源模型**：
  - ASTRA-32B 表现逼近 GLM-4.6 和 Claude-Sonnet，在某些维度超越 Gemini-3-Pro。

### 消融实验结果（Ablation Studies）

#### （1）消融：是否加入无关工具（Irrelevant Tool Mixing）
- **No Irrelevant Tools**：性能最差 → 模型过拟合固定工具集，无法拒绝错误工具。
- **Random Irrelevant Tools**：有所改善，但仍不如分层混合。
- **ASTRA（按相似度分层混合）**：效果最佳，说明 **controlled discrimination signal** 至关重要。

> 🔍 发现：工具相似度越高（near-miss distractors），越能有效促进模型学习“该不该调用”。

#### （2）消融：奖励函数设计（Reward Design）
| 奖励类型 | 表现 |
|--------|------|
| **Recall-only** | 对话轮次爆炸，频繁无效调用，最终崩溃 |
| **Precision-only** | 过于保守，提前终止，错过关键步骤 |
| **F1-style（本文方法）** | 平衡探索与利用，训练稳定，性能最优 |

> 📈 图 8 显示 F1 奖励下对话长度适中且收敛良好。

#### （3）训练阶段分析（Table 2）
| 阶段 | BFCL-MT ↑ | T2-Bench ↑ | ACEBench ↑ |
|------|----------|-----------|------------|
| 原始模型 | 44.50 | 44.55 | 51.67 |
| SFT 后 | 48.50 | 50.45 | 67.71 |
| RL 后 | **58.13** | **57.70** | **68.96** |

> ✅ **RL 阶段带来最大增益**，表明多轮在线 RL 是关键驱动力。

---

## 4. 关键结论和发现

### 主要发现

1. **全自动数据与环境合成是可行的**  
   ASTRA 成功实现了从工具文档到可执行环境的全流程自动化，摆脱了对人工标注的依赖。

2. **可验证环境对稳定 RL 极其重要**  
   基于代码沙箱的 deterministic execution 提供了可靠的 reward signal，使 long-horizon RL 成为可能。

3. **F1-style 轨迹级奖励能有效平衡任务完成与交互效率**  
   避免了 recall-only 的冗余调用和 precision-only 的过度保守。

4. **两阶段训练（SFT + RL）优于单一范式**  
   SFT 提供强初始化，RL 实现深度优化，二者协同显著提升性能。

5. **ASTRA 不损害基础推理能力**  
   在 AIME2024/2025 上的表现显示，agentic training 未牺牲 core reasoning。

### 方法的局限性

- **环境合成成本高**：每个 QA 对需生成并验证 Python 工具代码，计算开销大。
- **依赖高质量工具文档**：若原始 tool schema 不清晰，会影响合成质量。
- **语言覆盖有限**：目前主要支持英文和中文，多语言扩展待加强。
- **现实世界动态性不足**：尽管注入故障模拟，仍难完全反映真实服务波动。

### 未来工作方向

1. **引入真实用户交互训练**  
   当前训练基于静态任务，未来计划加入 multi-turn user feedback loop，提升应对意图漂移的能力。

2. **降低环境合成成本**  
   提出先验证语义拓扑再生成代码，避免低置信度规格的无效编码。

3. **构建动态难度课程（Curriculum Learning）**  
   类似 GenEnv，让环境生成器根据 agent 能力动态调整任务难度。

4. **支持更复杂的 agent 架构**  
   如引入 memory、planning module 等高级认知组件。

---

> ✅ **总结一句话**：  
> **ASTRA 是首个实现全自动、端到端、可验证的工具代理训练框架，通过“静态拓扑 + 动态语义”双轮驱动，在多个 agentic benchmark 上达到同规模 SOTA，并保持基础推理能力，推动了 LLM agent 的规模化落地。**

</details>

---

### 16. [SONIC: Segmented Optimized Nexus for Information Compression in Key-Value Caching](https://arxiv.org/abs/2601.21927)

**Authors**: Hong Chen, Xiang Liu, Bo Wang, Yuxuan Fan, Yuanlin Chu, Zongluo Li, Xiaowen Chu, Xuming Hu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21927v1  

#### Abstract
The linear growth of Key-Value (KV) cache remains a bottleneck for multi-turn LLM deployment. Existing KV cache compression methods often fail to account for the structural properties of multi-turn dialogues, relying on heuristic eviction that risks losing critical context. We propose \textbf{SONIC}...

---

### 17. [Snowball: A Scalable All-to-All Ising Machine with Dual-Mode Markov Chain Monte Carlo Spin Selection and Asynchronous Spin Updates for Fast Combinatorial Optimization](https://arxiv.org/abs/2601.21058)

**Authors**: Seungki Hong, Kyeongwon Jeong, Taekwang Jang  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21058v1  

#### Abstract
Ising machines have emerged as accelerators for combinatorial optimization. To enable practical deployment, this work aims to reduce time-to-solution by addressing three challenges: (1) hardware topology, (2) spin selection and update algorithms, and (3) scalable coupling-coefficient precision. Rest...

---

### 18. [Theoretically Optimal Attention/FFN Ratios in Disaggregated LLM Serving](https://arxiv.org/abs/2601.21351)

**Authors**: Chendong Song, Meixuan Wang, Hang Zhou, Hong Liang, Yuan Lyu, Zixi Chen, Yuwei Fan, Zijie Zhou  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21351v1  

#### Abstract
Attention-FFN disaggregation (AFD) is an emerging architecture for LLM decoding that separates state-heavy, KV-cache-dominated Attention computation from stateless, compute-intensive FFN computation, connected by per-step communication. While AFD enables independent scaling of memory and compute res...

---

### 19. [Visual Disentangled Diffusion Autoencoders: Scalable Counterfactual Generation for Foundation Models](https://arxiv.org/abs/2601.21851)

**Authors**: Sidney Bender, Marco Morik  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21851v1  

#### Abstract
Foundation models, despite their robust zero-shot capabilities, remain vulnerable to spurious correlations and 'Clever Hans' strategies. Existing mitigation methods often rely on unavailable group labels or computationally expensive gradient-based adversarial optimization. To address these limitatio...

---

### 20. [MoHETS: Long-term Time Series Forecasting with Mixture-of-Heterogeneous-Experts](https://arxiv.org/abs/2601.21866)

**Authors**: Evandro S. Ortigossa, Guy Lutsker, Eran Segal  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21866v1  

#### Abstract
Real-world multivariate time series can exhibit intricate multi-scale structures, including global trends, local periodicities, and non-stationary regimes, which makes long-horizon forecasting challenging. Although sparse Mixture-of-Experts (MoE) approaches improve scalability and specialization, th...

---

### 21. [ChunkWise LoRA: Adaptive Sequence Partitioning for Memory-Efficient Low-Rank Adaptation and Accelerated LLM Inference](https://arxiv.org/abs/2601.21109)

**Authors**: Ketan Thakkar, Maitreyi Chatterjee, Ramasubramanian Balasubramanian, Achyuthan Jootoo, Rajendra Ugrani  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21109v1  

#### Abstract
Recent advances in low-rank adaptation (LoRA) have enabled efficient fine-tuning of large language models (LLMs) with minimal additional parameters. However, existing LoRA methods apply static rank configurations uniformly across all input tokens, ignoring variation in token complexity and computati...

---

### 22. [Why Attention Patterns Exist: A Unifying Temporal Perspective Analysis](https://arxiv.org/abs/2601.21709)

**Authors**: Qingyue Yang, Jie Wang, Xing Li, Yinqi Bai, Xialiang Tong, Huiling Zhen, Jianye Hao, Mingxuan Yuan, Bin Li  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21709v1  

#### Abstract
Attention patterns play a crucial role in both training and inference of large language models (LLMs). Prior works have identified individual patterns such as retrieval heads, sink heads, and diagonal traces, yet these observations remain fragmented and lack a unifying explanation. To bridge this ga...

---

### 23. [TACLer: Tailored Curriculum Reinforcement Learning for Efficient Reasoning](https://arxiv.org/abs/2601.21711)

**Authors**: Huiyuan Lai, Malvina Nissim  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21711v1  

#### Abstract
Large Language Models (LLMs) have shown remarkable performance on complex reasoning tasks, especially when equipped with long chain-of-thought (CoT) reasoning. However, eliciting long CoT typically requires large-scale reinforcement learning (RL) training, while often leading to overthinking with re...

---

### 24. [ECO: Quantized Training without Full-Precision Master Weights](https://arxiv.org/abs/2601.22101)

**Authors**: Mahdi Nikdan, Amir Zandieh, Dan Alistarh, Vahab Mirrokni  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22101v1  

#### Abstract
Quantization has significantly improved the compute and memory efficiency of Large Language Model (LLM) training. However, existing approaches still rely on accumulating their updates in high-precision: concretely, gradient updates must be applied to a high-precision weight buffer, known as $\textit...

---

### 25. [Rethinking LLM-Driven Heuristic Design: Generating Efficient and Specialized Solvers via Dynamics-Aware Optimization](https://arxiv.org/abs/2601.20868)

**Authors**: Rongzheng Wang, Yihong Huang, Muquan Li, Jiakai Li, Di Liang, Bob Simons, Pei Ke, Shuang Liang, Ke Qin  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.20868v1  

#### Abstract
Large Language Models (LLMs) have advanced the field of Combinatorial Optimization through automated heuristic generation. Instead of relying on manual design, this LLM-Driven Heuristic Design (LHD) process leverages LLMs to iteratively generate and refine solvers to achieve high performance. Howeve...

---

### 26. [Breaking the Regional Barrier: Inductive Semantic Topology Learning for Worldwide Air Quality Forecasting](https://arxiv.org/abs/2601.21899)

**Authors**: Zhiqing Cui, Siru Zhong, Ming Jin, Shirui Pan, Qingsong Wen, Yuxuan Liang  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21899v1  

#### Abstract
Global air quality forecasting grapples with extreme spatial heterogeneity and the poor generalization of existing transductive models to unseen regions. To tackle this, we propose OmniAir, a semantic topology learning framework tailored for global station-level prediction. By encoding invariant phy...

---

### 27. [Output-Space Search: Targeting LLM Generations in a Frozen Encoder-Defined Output Space](https://arxiv.org/abs/2601.21169)

**Authors**: Tobias Materzok  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.21169v1  

#### Abstract
We introduce Output-Space Search (OS-Search), which turns LLM generation into endpoint search. An outer loop selects a target z* in a frozen encoder-defined 3D output space Z, and a retrieval-grounded policy trained with sequence-level RL generates outputs whose coordinates land near z* under standa...

---

### 28. [DynaWeb: Model-Based Reinforcement Learning of Web Agents](https://arxiv.org/abs/2601.22149)

**Authors**: Hang Ding, Peidong Liu, Junqiao Wang, Ziwei Ji, Meng Cao, Rongzhao Zhang, Lynn Ai, Eric Yang, Tianyu Shi, Lei Yu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.22149v1  

#### Abstract
The development of autonomous web agents, powered by Large Language Models (LLMs) and reinforcement learning (RL), represents a significant step towards general-purpose AI assistants. However, training these agents is severely hampered by the challenges of interacting with the live internet, which i...

---

### 29. [Model-Free Neural State Estimation in Nonlinear Dynamical Systems: A Comparative Study of Neural Architectures and Classical Filters](https://arxiv.org/abs/2601.21266)

**Authors**: Zhuochen Liu, Hans Walker, Rahul Jain  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.21266v1  

#### Abstract
Neural network models are increasingly used for state estimation in control and decision-making problems, yet it remains unclear to what extent they behave as principled filters in nonlinear dynamical systems. Unlike classical filters, which rely on explicit knowledge of system dynamics and noise mo...

---

### 30. [Few-Shot Learning for Dynamic Operations of Automated Electric Taxi Fleets under Evolving Charging Infrastructure: A Meta-Deep Reinforcement Learning Approach](https://arxiv.org/abs/2601.21312)

**Authors**: Xiaozhuang Li, Xindi Tang, Fang He  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.21312v1  

#### Abstract
With the rapid expansion of electric vehicles (EVs) and charging infrastructure, the effective management of Autonomous Electric Taxi (AET) fleets faces a critical challenge in environments with dynamic and uncertain charging availability. While most existing research assumes a static charging netwo...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
