# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-28 06:00:53 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DART: Diffusion-Inspired Speculative Decoding for Fast LLM Inference](https://arxiv.org/abs/2601.19278)

**Authors**: Fuliang Liu, Xue Li, Ketai Zhao, Yinxi Gao, Ziyan Zhou, Zhonghui Zhang, Zhibin Wang, Wanchun Dou, Sheng Zhong, Chen Tian  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2601.19278v1  

#### Abstract
Speculative decoding is an effective and lossless approach for accelerating LLM inference. However, existing widely adopted model-based draft designs, such as EAGLE3, improve accuracy at the cost of multi-step autoregressive inference, resulting in high drafting latency and ultimately rendering the ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# DART: Diffusion-Inspired Speculative Decoding for Fast LLM Inference — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有的 **Speculative Decoding** 方法在加速大语言模型（LLM）推理时面临两大瓶颈：
- **高 drafting latency**：主流基于模型的 drafters（如 EAGLE3）虽然提高了预测准确率（T），但其自回归（autoregressive）生成方式导致 drafting 阶段本身成为性能瓶颈，占总推理时间的 20%-40%。
- **效率与准确性权衡失衡**：非自回归方法（如 Lookahead、N-gram）虽延迟低，但接受长度（T）过小；而直接采用 **diffusion-based LLMs (dLLMs)** 作为 drafter 存在架构不匹配（双向 vs 因果）、参数量大、tokenizer 不兼容等问题。

### 🚀 提出的新方法：DART
提出 **DART**（Diffusion-Inspired Speculative Decoding），一种受扩散模型启发、专为 speculative decoding 定制的并行起草框架，核心思想如下：

#### 创新点 1：轻量级并行起草机制（Lightweight Diffusion-Inspired Parallel Drafting）
- **灵感来源**：借鉴 dLLMs 的“多位置并行预测”能力，但摒弃其迭代去噪和双向建模。
- **实现方式**：
  - 在单次前向传播中，利用目标模型的隐藏状态（hidden states），通过一个轻量化的定制 **Transformer decoder layer** 并行预测多个未来位置的 logits。
  - 引入 **Shifted Logits Prediction**：每个位置输出的 logit 对应的是“下一个 token”的预测，显著提升首个 draft token 的准确率。
- **优势**：
  - 彻底消除 draft model 中的自回归 rollout。
  - drafting forward 延迟与 draft length 无关，仅需一次轻量前向。

#### 创新点 2：基于 N-gram 的连续性感知树剪枝（Continuity-Aware Tree Pruning）
- **挑战**：并行 logits 预测隐含指数级组合空间，直接验证不可行。
- **解决方案**：
  - 将 N-gram 模型用作 **语义连续性约束器**，而非独立预测器。
  - 设计高效剪枝算法，在每一步结合 logit 得分与 N-gram 连续性得分，构建高质量 draft token tree。
- **优势**：
  - 保留高概率且语义连贯的候选路径。
  - 显著提高平均接受长度 $T$，同时保持低延迟。

#### 创新点 3：与目标模型紧耦合设计
- DART 不是独立模型，而是复用目标模型的 hidden states 和 embedding 层，仅添加少量可训练参数（FC layer + decoder layer + LM head）。
- 极大降低部署复杂性和计算开销。

### 🔍 相比现有方法的优势
| 方法 | 是否自回归 | Drafting Latency | 准确性（T） | 实际加速比 |
|------|------------|------------------|-------------|------------|
| SPS / Vanilla | 是 | 高 | 中等 | 低 |
| Medusa / Hydra | 否（部分） | 中 | 中高 | 中 |
| EAGLE3 | 是（轻量层） | 中高 | 高 | 较高 |
| Lookahead / PLD | 否 | 极低 | 低 | 有限 |
| **DART** | **否** | **极低** | **高** | **最高** |

> ✅ **核心优势**：在保持高 $T$ 的同时，将 drafting latency 压缩至极致，实现端到端最大加速。

---

## 2. 核心实验方法和设置

### 📚 数据集
涵盖多种任务类型，用于全面评估：
- **指令跟随**：Alpaca
- **数学推理**：Math500
- **代码生成**：HumanEval, CodeAlpaca, LiveCodeBench, MBPP
- **多轮对话质量**：MT-Bench

训练数据来自 ShareGPT 和 UltraChat（经清洗后约 280K 示例）。

### ⚙️ 实验设置
- **硬件平台**：
  - 主要：8×NVIDIA H20-3e GPU（141GB显存）
  - 补充测试：NVIDIA A100-40G GPU
- **目标模型**：
  - Qwen3 系列：Qwen3-1.7B, 4B, 8B, 14B, 32B
  - LLaMA2-Chat-7B（用于跨方法对比）
- **Batch Size**：默认为 1，额外测试更大 batch（2–64）
- **Draft Length**：统一设为 8（DART 和 EAGLE3），SPS 使用 5
- **温度设置**：T=0 和 T=1 下均测试

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Throughput (Speedup)** | 相对于标准自回归解码的端到端 wall-clock time 加速比 |
| **Average Acceptance Length (T)** | 每轮 draft-verify 周期中被接受的 token 数量，反映 draft 质量 |
| **Drafting Latency** | draft model 前向 + tree search 所耗时间（ms），体现开销 |
| **Verification Latency** | target model 验证 draft token tree 的时间 |

### 🆚 基线方法对比
- **SPS**（Standard Speculative Sampling）：使用小型同族模型（如 Qwen3-1.7B）作为 drafter
- **EAGLE3**：当前最优的基于特征微调的自回归 drafter
- **Medusa**, **Hydra**, **Lookahead**, **PLD**：其他代表性 speculative decoding 方法
- **未比较**：DiffuSpec / SpecDiff（闭源无法复现）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 端到端加速比（Speedup）
- DART 在多个任务上实现 **2.03× ~ 3.44×** 的实际加速。
- 平均比 EAGLE3 快 **30%**。
- 在代码相关任务（如 CodeAlpaca）中表现尤为突出，对 Qwen3-14B 提速达 **65%** 超越 EAGLE3。

#### ✅ 平均接受长度 $T$
- DART 的 $T$ 与 EAGLE3 处于同一水平（相差 < 0.2），例如：
  - Qwen3-14B 上：EAGLE3 ≈ 3.48，DART ≈ 3.67
- 表明 DART 在大幅降低 drafting 开销的同时，未牺牲 draft 质量。

#### ✅ Drafting Latency 极致压缩
- 如图 5 所示，在 Qwen3-14B 推理中：
  - **DART drafting forward**: **1.5ms**
  - **EAGLE3 drafting forward**: **10.2ms** → DART 快 **6.8×**
  - **SPS drafting forward**: **80.0ms** → DART 快 **53.3×**
- Tree pruning 额外耗时约 2ms，仍远低于 EAGLE3 的树搜索成本。

#### ✅ 更大 batch size 下的表现
- 随着 batch size 增大，所有方法的加速比下降（因系统更 compute-bound）。
- 但在所有配置下，**DART 始终优于 EAGLE3**，例如在 Qwen3-4B 上：
  - batch=2: DART 2.16× vs EAGLE3 1.84×
  - batch=64: DART 1.45× vs EAGLE3 1.22×

#### ✅ 跨硬件平台验证（A100）
- 在 A100 上测试表明，DART 依然稳定领先 EAGLE3 约 **30%**，证明其设计具有良好的 **hardware-agnostic 性能**。

### 🔬 消融实验结果

| 实验项 | 发现 |
|--------|------|
| **N-gram Pruning** | 移除 N-gram 后 $T$ 显著下降（如 HumanEval 从 3.63 → 3.13），说明其对维持语义连贯性和提高接受率至关重要。 |
| **Shifted Logits Prediction** | 相比 unshifted，shifted 方案使第一个位置的 hit@1 准确率从 57.7% 提升至 **71.1%**（↑13.4%），后续位置也有稳定增益。 |
| **Annealed KL Objective ($\gamma=0.6$)** | $\gamma=0.6$ 时达到最佳平衡：早期位置精度高，整体 $T=3.63$ 最优；$\gamma=1.0$（无退火）时 $T=3.48$，性能下降。 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **并行 drafting + 结构化剪枝 是可行且高效的替代路径**  
   DART 成功将 diffusion-style 并行生成引入 speculative decoding，并通过定制化设计解决了架构不匹配问题。

2. **最小化 drafting overhead 比单纯追求高 $T$ 更重要**  
   即使 $T$ 略低于 EAGLE3，DART 凭借极低的 drafting latency 实现了更高的端到端吞吐量，揭示了新的优化范式。

3. **N-gram 应作为 continuity constraint 而非 predictor**  
   将 N-gram 用于剪枝而非独立生成，既能保证语义合理，又不破坏模型主导的分布拟合能力。

4. **DART 具备良好泛化性**  
   在不同模型规模（1.7B–32B）、不同任务类型、不同硬件平台（H20, A100）上均表现出一致优越性。

### ⚠️ 方法的局限性
- **依赖目标模型 hidden states**：需与特定目标模型紧密集成，迁移性受限。
- **N-gram 内存占用较高**：约 100GB CPU RAM，虽可接受但对资源紧张环境构成压力。
- **训练复杂度增加**：需专门训练 draft model，相比 heuristic 方法门槛更高。
- **目前仅支持 greedy decoding**：未探索 temperature sampling 或 top-k 等随机采样场景下的稳定性。

### 🔮 未来工作方向
- 支持更多采样策略（如 nucleus sampling）下的 speculative decoding。
- 探索更紧凑的 continuity model 替代大型 N-gram trie。
- 扩展至 MoE 模型或长上下文场景中的高效 drafting。
- 自动化 draft length 与 pruning 参数的动态调整机制。

---

> 💡 **总结一句话**：  
> **DART 重新定义了 speculative decoding 的设计哲学——不是“更快地自回归”，而是“用因果并行预测 + 结构化剪枝”打破 drafting 瓶颈，在几乎不损失准确性的前提下，将 drafting latency 压缩一个数量级以上，从而实现前所未有的端到端加速效果。**

</details>

---

### 2. [Length-Adaptive Interest Network for Balancing Long and Short Sequence Modeling in CTR Prediction](https://arxiv.org/abs/2601.19142)

**Authors**: Zhicheng Zhang, Zhaocheng Du, Jieming Zhu, Jiwei Tang, Fengyuan Lu, Wang Jiaheng, Song-Li Wu, Qianhui Zhu, Jingyu Li, Hai-Tao Zheng, Zhenhua Dong  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.19142v1  

#### Abstract
User behavior sequences in modern recommendation systems exhibit significant length heterogeneity, ranging from sparse short-term interactions to rich long-term histories. While longer sequences provide more context, we observe that increasing the maximum input sequence length in existing CTR models...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Length-Adaptive Interest Network for Balancing Long and Short Sequence Modeling in CTR Prediction**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代推荐系统中的用户行为序列存在显著的**长度异质性**（length heterogeneity）：  
- 长序列用户拥有丰富的历史交互（如数百次点击），可提供丰富上下文；  
- 短序列用户（如新用户或低活跃用户）仅有少量行为，建模困难。

然而，作者发现一个反直觉现象：**当增加最大输入序列长度时，现有 CTR 模型对长序列用户的性能提升，却导致短序列用户性能下降**。这种“**长度不平衡**”（length imbalance）问题源于：
- **Attention Polarization**：softmax 注意力在长序列中趋于集中于少数显著项，在短序列中则过度放大噪声；
- **Length Signal Deficiency**：现有模型未显式利用序列长度作为用户状态的先验信号。

此外，训练数据分布也加剧了这一问题：**长序列用户虽然数量少，但贡献了大部分训练样本**，导致模型隐式偏向优化他们。

---

### 🚀 提出的新方法：LAIN（Length-Adaptive Interest Network）

提出一种轻量级、即插即用的框架 **LAIN**，通过将**序列长度作为条件信号**，动态调整兴趣提取与注意力机制，实现对不同长度序列的自适应建模。

#### 核心组件：
1. **Spectral Length Encoder (SLE)**  
   - 将原始长度 $ L $ 映射为连续嵌入表示 $ h_{\text{len}} $，采用可学习的傅里叶基（Fourier basis）进行编码，避免离散化损失，捕捉长度的细粒度语义。

2. **Length-Conditioned Prompting (LCP)**  
   - 基于 $ h_{\text{len}} $ 生成软提示 token，并前置到长短序列的行为序列中，向模型注入全局长度上下文信息，引导其采用合适的归纳偏置。

3. **Length-Modulated Attention (LMA)**  
   - 动态调节注意力锐度：
     - **Query-Key Conditioning**：将长度嵌入拼接到 query 和 key 中；
     - **Temperature Scaling**：根据长度自适应调整 softmax 温度 $ T $，短序列时增大温度以平滑注意力分布，防止过拟合。

---

### 🔍 相比现有方法的优势
| 维度 | LAIN 的优势 |
|------|-------------|
| **通用性** | 可无缝集成到主流 CTR 模型（DIN、DIEN、SIM、SDIM、TWIN 等）中，无需架构重构 |
| **公平性** | 显著提升短序列用户预测精度，缓解因数据分布不均造成的模型偏见 |
| **效率** | 引入参数 <1.5%，推理开销仅增加约 2.3%，适合工业部署 |
| **理论动机强** | 明确诊断出 attention polarization 与 length signal deficiency 两大问题，并针对性设计解决方案 |

---

## 2. 核心实验方法和设置

### 📊 数据集
在三个真实世界的大规模公开 CTR 数据集上验证方法有效性：

| 数据集 | 用户数 | 物品数 | 交互数 | 平均序列长度 | 场景 |
|--------|--------|--------|--------|---------------|------|
| **EBNeRD-small** | 18,828 | 20,739 | 5.5M | 147 | 新闻推荐 |
| **KuaiVideo** | 10,000 | 3.2M | 4.7M | 278 | 短视频推荐 |
| **MicroVideo1.7M** | 10,951 | 1.7M | 4.3M | 148 | 微视频推荐 |

所有数据集均呈现明显的**长尾分布**：多数用户行为稀疏，少数用户行为密集。

---

### ⚙️ 实验设置
- **最大序列长度**：统一设为 1000，以支持长序列建模；
- **特征维度**：embedding dim = 64；
- **优化器**：Adam，lr = 0.001，早停基于验证集 AUC；
- **Head 结构**：MLP + ReLU + Dropout(0.2)；
- **LAIN 参数配置**：
  - Fourier dimension $ d_f = 32 $
  - MLP hidden dim = 512
  - Prompt token 数量 $ k = 4 $

---

### 📈 评估指标
| 指标 | 描述 |
|------|------|
| **AUC** | 衡量整体排序能力，越高越好 |
| **GAUC** | 分用户计算 AUC 后平均，减少高频用户主导影响 |
| **LogLoss** | 衡量概率校准质量，越低越好 |
| **分组评估** | 按序列长度划分 bucket（<100, 100–200, ≥200），分析各群体表现 |

---

### 🆚 基线方法对比
选择五类代表性 CTR 模型作为 backbone 进行增强比较：
1. **DIN**：基于 target-aware attention 的基础模型
2. **DIEN**：引入 GRU 建模兴趣演化
3. **SIM**：两阶段架构（GSU + ESU）处理长序列
4. **SDIM**：多头注意力增强版 SIM
5. **TWIN**：双塔结构建模超长序列

所有基线均使用相同特征工程与超参设置，在 **FuxiCTR** 框架下复现。

---

## 3. 主要实验结果和性能指标

### 📈 整体性能提升（Table 3）
LAIN 在所有 backbone 和数据集上均带来一致增益：

| 模型 | 最大 AUC 提升 | 最大 LogLoss 下降 |
|------|----------------|--------------------|
| TWIN + LAIN | **+1.15%** | **-1.63%** |
| DIN + LAIN | +1.07% | -0.92% |
| DIEN + LAIN | +0.27% | -2.25% |

> 💡 注：DIEN 虽然 AUC 提升较小，但在 KuaiVideo 上 LogLoss 改善最明显（-2.25%），说明其在概率校准方面也有优势。

---

### 🎯 长短序列用户分别评估（Table 4）
以 TWIN 为例，在 MicroVideo1.7M 上按长度分组的结果显示：

| 序列长度 | AUC 提升 | LogLoss 下降 |
|---------|----------|--------------|
| **<100（短序列）** | **+1.08%** | **-2.17%** |
| 100–200（中等） | +0.50% | -1.26% |
| ≥200（长序列） | +1.58% | -1.74% |

👉 **关键发现**：LAIN 不仅没有牺牲长序列性能，反而同时提升了两类用户的建模效果，实现了真正的“平衡”。

---

### 🔬 注意力极化缓解分析（Table 5）
使用 Gini 系数衡量注意力分布的集中程度（越低越均匀）：

| 模型配置 | 短序列 Gini | 中序列 Gini | 长序列 Gini |
|----------|------------|------------|------------|
| Baseline (L=1000) | 0.346 | 0.351 | 0.352 |
| **LAIN** | **0.318** | **0.322** | **0.321** |

✅ LAIN 将注意力极化程度降低约 **33.3%~50.6%**，尤其改善了短序列下的注意力塌缩问题。

---

### 🔁 消融实验（Ablation Study, Table 6）
在 TWIN 上移除各模块的影响：

| 变体 | AUC | ΔAUC |
|------|-----|-------|
| **LAIN (Full)** | **0.7233** | — |
| w/o LCP | 0.7228 | -0.05% |
| w/o Query-Key Conditioning | 0.7195 | -0.38% |
| w/o Temperature Scaling | 0.7212 | -0.21% |
| w/o LMA | 0.7189 | **-0.44%** |
| w/o Short-term Branch | 0.7226 | -0.07% |

📌 **结论**：
- **LMA 是最关键的组件**，尤其是 query-key conditioning 与 temperature scaling 协同作用；
- 移除 short-term 分支仍造成轻微下降，表明 LAIN 对短序列建模具有独立价值。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **长度不是被动统计量，而是主动建模信号**：序列长度反映了用户的活跃度、兴趣稳定性与内容多样性，应被显式建模。
2. **标准 CTR 模型存在“长度诱导偏差”**（length-induced bias）：训练过程中长序列样本主导梯度更新，损害短序列泛化。
3. **LAIN 成功解耦了长短序列的优化冲突**：通过条件化 prompt 与 attention 调制，使共享模型能根据不同长度动态调整行为策略。
4. **方法具备强鲁棒性和普适性**：在多种 backbone 和数据集上稳定有效，且对超参数不敏感（见 Figure 4）。

---

### ⚠️ 局限性
1. **依赖预定义的最大序列长度**：虽然 LAIN 编码的是实际长度 $ L $，但 truncation 或 padding 策略可能影响性能边界；
2. **未考虑其他用户状态信号联合建模**：如注册时间、设备类型、地理位置等，未来可探索多信号联合 conditioning；
3. **目前仅用于 CTR 预测任务**：是否适用于 CVR、CTR/CVR 联合建模等还需进一步验证。

---

### 🔮 未来工作方向
1. **扩展至多任务推荐场景**：将 LAIN 思路应用于多目标学习（multi-task learning）中，实现跨任务的长度自适应；
2. **结合用户生命周期建模**：将长度视为用户成长阶段的代理变量，构建动态演化模型；
3. **探索更复杂的长度感知架构**：例如基于 length 的子网络路由（routing）、adapter 插入位置自适应等；
4. **应用于冷启动与公平性研究**：作为缓解推荐系统偏见的一种通用工具，推动更公平的个性化服务。

---

> ✅ **总体评价**：  
> LAIN 是一项兼具**理论洞察力**与**工程实用性**的工作。它揭示了一个长期被忽视的“长度失衡”问题，并提出了简洁高效的解决方案。该方法有望成为工业级推荐系统的标准组件之一。

</details>

---

### 3. [GPCR-Filter: a deep learning framework for efficient and precise GPCR modulator discovery](https://arxiv.org/abs/2601.19149)

**Authors**: Jingjie Ning, Xiangzhen Shen, Li Hou, Shiyi Shen, Jiahao Yang, Junrui Li, Hong Shan, Sanan Wu, Sihan Gao, Huaqiang Eric Xu, Xinheng He  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.19149v1  

#### Abstract
G protein-coupled receptors (GPCRs) govern diverse physiological processes and are central to modern pharmacology. Yet discovering GPCR modulators remains challenging because receptor activation often arises from complex allosteric effects rather than direct binding affinity, and conventional assays...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GPCR-Filter: a deep learning framework for efficient and precise GPCR modulator discovery

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
G蛋白偶联受体（GPCRs）是现代药物研发中最重要的靶点家族之一（约36% FDA批准药物作用于GPCRs），但其**功能调控机制复杂**，常涉及别构效应（allosteric effects），导致传统基于结合亲和力的虚拟筛选方法难以准确预测功能性调节剂（modulators）。此外，实验验证成本高、通量低，限制了新配体的发现。

现有AI模型如TransformerCPI、ConPLex等虽可用于化合物-蛋白相互作用（DTI）预测，但**缺乏对GPCR特异性序列-功能关系的敏感性**，在跨靶点（inter-target）或新化学骨架上的泛化能力有限。

---

### 🚀 提出的新方法与创新点
作者提出 **GPCR-Filter** ——一个专为GPCR调节剂发现设计的深度学习框架，具有以下创新：

1. **领域专用建模架构**  
   针对GPCR生物学特性定制模型结构，整合：
   - **ESM-3**：先进的蛋白质语言模型，用于提取高保真度的GPCR序列嵌入；
   - **图神经网络（GNN）**：编码配体分子图结构；
   - **注意力融合机制（attention-based fusion）**：通过ligand-to-protein cross-attention动态捕捉受体-配体之间的功能关联，超越简单“结合”预测，实现“功能调节潜力”的推断。

2. **高质量、大规模实验验证数据集构建**  
   构建了一个包含 **91,396个经实验验证的人源GPCR-配体对** 的高质量数据集，来源于GPCRdb和GtoPdb数据库，并统一映射至UniProt序列和标准SMILES格式。

3. **可解释性强的设计**  
   利用cross-attention权重分析关键残基，发现模型关注区域与晶体结构中的配体结合口袋高度重合，支持其生物学合理性。

4. **作为虚拟筛选流程中的高效过滤器**  
   GPCR-Filter不取代结构对接，而是作为后续精筛步骤，在仅输入GPCR序列和SMILES的情况下进一步提升命中率（hit rate）。

---

### 🔍 相比现有方法的优势
| 维度 | GPCR-Filter | 传统DTI模型（如ConPLex, TransformerCPI2.0） |
|------|-------------|------------------------------------------|
| **任务目标** | 功能性调节剂识别（functional modulation） | 通用结合预测（binding likelihood） |
| **泛化能力** | 强大的跨靶点（inter-target）泛化能力 | 在未见受体上表现差甚至低于随机 |
| **输入依赖** | 无需三维结构，仅需序列 + SMILES | 同样输入，但未针对GPCR优化 |
| **性能表现** | 显著优于基线，尤其在挑战性场景下 | 性能平庸，易过拟合 |

---

## 2. 核心实验方法和设置

### 📊 数据集
- **来源**：整合自 [GPCRdb](https://gpcrdb.org/) 和 [Guide to Pharmacology (GtoPdb)](https://www.guidetopharmacology.org/)
- **规模**：
  - 91,396 条实验验证的 human GPCR–ligand interaction 记录
  - 覆盖 **527个独特GPCRs**
  - 包含 **72,177个不同配体（canonical SMILES）**
- **负样本构造**：
  - 所有可能组合中去除已知阳性对
  - 采用 **1:1 正负采样策略**，确保类别平衡
  - 不同split独立采样以避免信息泄露

---

### ⚙️ 实验设置与评估协议
采用三种递进难度的数据划分方式，系统评估模型泛化能力：

| 设置 | 描述 | 挑战性 |
|------|------|--------|
| **Random Split** | 随机划分所有GPCR–ligand对（训练/验证/测试 = 80/10/10） | 最低，衡量in-distribution性能 |
| **Intra-target Split** | 每个GPCR出现在所有集合中，但其配体互斥划分 | 中等，测试对同一靶点的新配体泛化 |
| **Inter-target Split** | 将GPCR分为训练集（90%）与保留集（10%），测试集仅来自未见过的GPCRs | 最高，测试跨靶点迁移能力 |

> 注：所有模型均使用相同的输入（GPCR氨基酸序列 + 配体SMILES），公平比较。

---

### 🎯 评估指标
- **AUC**（ROC曲线下面积）
- **AP**（Average Precision，更适用于不平衡数据）
- **Accuracy (ACC)** 和 **Precision**
- 所有阈值相关指标使用固定概率阈值 `0.5`

---

### 🆚 基线方法对比
与两个最先进的序列级DTI模型进行对比：
1. **ConPLex**：基于对比学习的蛋白语言模型框架
2. **TransformerCPI2.0**：结合TAPE-BERT与GNN的端到端模型

> 所有方法使用相同的数据预处理、超参数调优策略及评估协议。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）

| Method | Random AUC (%) | Intra-target AUC (%) | Inter-target AUC (%) |
|--------|----------------|-----------------------|------------------------|
| ConPLex | 53.03 | 52.89 | **44.91** ❌（<50%，可能过拟合） |
| TransformerCPI2.0 | 65.94 | 62.02 | 66.31 |
| **GPCR-Filter** | **99.02** | **97.98** | **80.35** ✅ |

> ✅ GPCR-Filter在所有三项任务中显著领先，尤其在最具挑战性的 **inter-target split** 上比第二名高出 **14个百分点以上**。

#### 其他关键指标（Inter-target）：
- **AP**: 70.99% vs. 58.50%（TransformerCPI2.0）
- **Precision**: 75.93% vs. 53.35%
- **ACC**: 74.57%

表明该模型不仅能区分正负样本，还能在完全陌生的GPCRs上保持高精度预测。

---

### 🔬 可解释性分析结果
通过对cross-attention权重可视化，研究模型是否真正学到结合位点模式：

- 分析两个新解析的复合物结构（PDB: 9bsb, DRD2；9jcl, purinergic receptor）
- 定义“晶体学结合口袋”为距离配体重原子 <5Å 的残基
- 报告Top-20高注意力残基中有多少落在真实结合口袋内（Pocket hits@20）

#### 结果：
- 在多个训练设置下（random/intra/inter），Top-20中均有 **6–8个残基位于真实结合口袋**
- 注意力分布稳定，且聚焦于已知功能区域
- 支持模型并非记忆配对，而是学习到了**结构性的功能交互模式**

---

### 💡 消融实验（隐含分析）
虽然文中未明确列出消融表，但从设计逻辑和结果反推可得以下结论：
- **ESM-3的作用至关重要**：相比其他PLM（如TAPE-BERT），其进化尺度的语言建模能力更适合捕获GPCR保守功能域。
- **Attention融合机制有效**：cross-attention使模型能够动态定位关键残基，而非全局平均。
- **高质量数据支撑泛化**：long-tailed分布下的均衡采样与合理split设计提升了模型鲁棒性。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **GPCR-Filter是一个高效、精准、可扩展的GPCR调节剂发现工具**，在多种评估场景下显著优于现有DTI模型。
2. 模型具备强大的**跨靶点泛化能力**（inter-target generalization），可在从未训练过的GPCRs上做出可靠预测。
3. 通过整合**ESM-3 + GNN + attention fusion**，成功桥接了GPCR序列信息与配体化学空间，实现了从“结合预测”向“功能调节预测”的跃迁。
4. **湿实验验证成功**：在5-HT1A受体上，从ChemDiv库中筛选出4个微摩尔级别（micromolar-level）的新型激动剂，具有不同的化学骨架，证明其实际应用价值。

---

### ⚠️ 局限性
1. **负样本不确定性**：负样本由未报告相互作用推断而来，可能存在假阴性（即实际活性但未被记录）。
2. **配体复用问题**：在inter-target split中，某些配体可能同时出现在训练和测试中（尽管靶点不同），可能引入偏差。
3. **缺乏结构约束**：纯序列+SMILES输入虽增强泛化，但也丢失了精细的空间几何信息。
4. **未涵盖所有GPCR亚型**：部分孤儿受体或低数据量GPCRs覆盖不足。

---

### 🔮 未来工作方向
1. **改进负样本策略**：探索更合理的decoy生成方法（如ADMET性质匹配、物理不可行性判别）。
2. **引入结构先验**：将AlphaFold3预测结构或结合口袋信息融入模型，提升精度。
3. **扩展至更多蛋白家族**：验证该范式是否适用于离子通道、核受体等其他膜蛋白。
4. **闭环自动化筛选平台**：将GPCR-Filter集成进完整的AI-driven drug discovery pipeline，实现从虚拟筛选到先导优化的全流程加速。

---

## ✅ 总结一句话
> **GPCR-Filter通过融合前沿蛋白语言模型与注意力机制，在高质量数据基础上构建了一个面向GPCR功能调节剂发现的高性能、强泛化、可解释的深度学习框架，显著提升了新药发现效率，并经实验证实其实际可行性。**

</details>

---

### 4. [Flatter Tokens are More Valuable for Speculative Draft Model Training](https://arxiv.org/abs/2601.18902)

**Authors**: Jiaming Fan, Daming Cao, Xiangzhong Luo, Jiale Fu, Chonghan Liu, Xu Yang  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.18902v1  

#### Abstract
Speculative Decoding (SD) is a key technique for accelerating Large Language Model (LLM) inference, but it typically requires training a draft model on a large dataset. We approach this problem from a data-centric perspective, finding that not all training samples contribute equally to the SD accept...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Flatter Tokens are More Valuable for Speculative Draft Model Training*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
当前基于训练的 **Speculative Decoding (SD)** 方法在加速大语言模型（LLM）推理时，通常需要在大规模数据集上对 draft model 进行知识蒸馏（Knowledge Distillation, KD）。然而，并非所有训练样本都对提升 **acceptance rate** 有同等贡献。直接使用全量数据训练效率低下，存在大量冗余计算。

本文从**数据中心化**（data-centric）视角出发，系统性地研究了“哪些数据对 SD 训练最有价值”，并提出了一种高效的数据选择方法。

---

### **提出了什么新方法或新思路**

#### （1）理论洞察：**Flatter Tokens 更有价值**
- 作者通过理论分析和实证验证发现：当目标模型（target model）对某个 token 的预测分布更“平坦”（即接近均匀分布，low confidence）时，该 token 对提升 acceptance rate 的潜力更大。
- 相反，那些预测分布尖锐（sharp, high confidence）的 token 很快就会饱和（saturate），对后续训练贡献极小。

#### （2）提出新度量：**Flatness**
- 定义了一个简单而有效的 token 重要性度量——**flatness**，即目标模型输出分布 $ p $ 与均匀分布 $ U $ 之间的 **cosine similarity**：
  $$
  \text{flatness}(t) = \cos(p_t, U)
  $$
- 高 flatness 表示分布更平坦，具有更大的优化空间（headroom）。

#### （3）提出新方法：**SFDD（Sample-level-flatness-based Dataset Distillation）**
- 将 token-level 的 flatness 聚合为 **sample-level-flatness**（取平均值）。
- 在训练前进行一次离线打分，按 flatness 排序后保留高价值样本，过滤低价值样本。
- 流程简洁：
  1. 用 target model 对所有样本做一次前向传播；
  2. 计算每个样本的 sample-level-flatness；
  3. 保留 top-k% 的样本用于训练 draft model。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | 只需 50% 数据即可实现超过 2× 的训练加速，且最终推理速度损失 <4% |
| **通用性** | flatness 仅依赖 target model，可离线计算，无需 warm-up draft model |
| **有效性** | 显著优于其他常见数据选择指标（如 entropy、top-1 prob、margin、PPL 等） |
| **正交性** | 与具体 loss 函数设计正交，可无缝集成到现有 SD 框架（如 EAGLE）中 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
- **训练数据**：`ShareGPT` 数据集（经过过滤）
- **评估任务**（5个下游任务）：
  - `GSM8K`（数学推理）
  - `Alpaca`（指令遵循）
  - `MT-Bench`（多轮对话质量）
  - `CNN/DM`（文本摘要）
  - `Natural Questions (NQ)`（开放问答）

---

### **实验设置和评估指标**

#### **模型配置**
- **Target Model**：`LLaMA3-8B-Instruct`
- **Draft Model**：轻量级单层 Transformer（类似 EAGLE-2 设置）
- **训练框架**：基于 `EAGLE-2` 的训练流程

#### **评估指标**
| 指标 | 含义 |
|------|------|
| **Speedup** | 推理墙钟时间加速比（越高越好） |
| **Average Acceptance Length ($\ell$)** | 每次验证周期平均接受的 draft token 数量（反映 acceptance rate） |
| **Training Time** | 总训练耗时（含数据筛选开销，用于衡量效率） |

#### **数据保留率（Retain Ratio）**
- 主要测试 30% ~ 100%，重点分析 50% 和 70%

---

### **基线方法对比**

| 基线方法 | 所用指标 |
|---------|--------|
| **No Filter** | 使用全部数据 |
| **Random Filtering** | 随机保留部分数据 |
| **Entropy** | 输出分布熵值（越高越不确定） |
| **Top-1 Probability** | 最大概率值（越低越不确定） |
| **Margin** | top-1 与 top-2 概率差 |
| **Energy Score** | 基于 logits 的不确定性评分 |
| **Perplexity (PPL)** | 整体困惑度（越高越难） |

> 所有基于重要性的方法均保留“高不确定性”样本。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（50% 数据保留率）**

| 方法 | 平均 Speedup | 相比 Full Data 损失 |
|------|---------------|---------------------|
| **No Filter (Full Data)** | 2.49× | — |
| **Random Filtering** | 2.20× | ↓11.6% |
| **Top-1 Probability (Best Baseline)** | 2.23× | ↓10.4% |
| **SFDD (Ours)** | **2.41×** | **↓3.2%** |

> ✅ **结论**：SFDD 用一半数据达到了接近全量训练的效果，推理速度仅下降约 4%。

---

### **与基线方法的全面对比（Table 1）**

- 在所有 5 个任务上，**SFDD 均显著优于所有基线方法**。
- 在 `GSM8K` 上，SFDD 达到 **2.69× speedup**，接近全量训练的 2.71×。
- 在 `Alpaca` 上甚至略微超越全量训练（2.66× vs 2.71×），表明过滤可能去除了噪声或冗余样本。

---

### **消融实验结果**

#### （1）不同数据保留率下的表现（Table 2）

| 保留率 | 方法 | 平均 Speedup |
|-------|------|--------------|
| 70% | SFDD | **2.44×** |
| 60% | SFDD | 2.39× |
| 50% | SFDD | 2.41× |
| 40% | SFDD | 2.39× |
| 30% | SFDD | 2.33× |

> 🔹 即使只保留 **30%** 数据，SFDD 仍能达到 **2.33×** 加速，远超随机过滤（2.14×）。

#### （2）极端低资源场景（Table 3：5%, 10%, 20%）

| 保留率 | 方法 | 平均 Speedup |
|-------|------|--------------|
| 5% | Random | 1.68× |
| 5% | **SFDD** | **1.82×** (+8.3%) |
| 10% | Random | 2.01× |
| 10% | **SFDD** | **2.07×** |
| 20% | Random | 2.06× |
| 20% | **SFDD** | **2.23×** |

> ✅ 表明 flatness 是一种**鲁棒且有效**的 token 价值指示器，即使在极低数据下依然有效。

#### （3）训练效率分析（Figure 4）

- 在 50% 数据下，**训练时间从 58,227s 缩短至 28,787s**，实现 **2.02× 训练加速**。
- 包含数据筛选开销（约 2,242s），仍远低于训练节省的时间。

---

## 4. 关键结论和发现

### **主要发现**

1. **并非所有 token 都同等重要**：  
   目标模型预测越“平坦”的 token，越能带来 acceptance rate 的提升，是高质量训练信号。

2. **Flatness 是一个可靠且高效的度量**：  
   仅依赖 target model 输出，可离线计算，无需 draft model 参与，适合大规模预处理。

3. **SFDD 极大提升训练效率**：  
   用 50% 数据即可获得接近全量训练的推理性能，同时实现 **2× 以上训练加速**。

4. **优于传统不确定性指标**：  
   flatness 在过滤低价值样本方面显著优于 entropy、top-1 prob 等常用指标。

---

### **方法的局限性**

1. **依赖 target model 的输出特性**：  
   若 target model 本身输出过于集中或稀疏，flatness 分布可能不理想。

2. **未考虑上下文动态变化**：  
   当前 flatness 是静态打分，未建模 draft model 训练过程中的动态对齐过程。

3. **聚合方式较简单**：  
   sample-level-flatness 使用平均值，未来可探索加权或其他聚合策略。

4. **token-level filtering 当前无效**：  
   因标准训练框架无法跳过 masked token 的前向/反向传播，故未采用 token-level 过滤（见 Appendix F.6）。

---

### **未来工作方向**

1. **动态数据选择策略**：  
   在训练过程中根据 draft model 的学习状态动态调整数据权重。

2. **扩展至其他架构**：  
   将 flatness 应用于非自回归、flow-based 或 diffusion 类生成模型的 speculative decoding。

3. **结合强化学习训练**：  
   在 GTO、PPO 等 RL-based SD 方法中引入 flatness 引导的 curriculum learning。

4. **探索更细粒度过滤机制**：  
   若未来支持 token-skipping 训练框架，则 flatness-based token-level filtering 将极具潜力。

---

> 📌 **一句话总结**：  
> 本文揭示了“**flatter tokens are more valuable**”这一关键现象，提出 **flatness** 度量与 **SFDD** 数据蒸馏方法，在保持几乎相同推理加速能力的前提下，实现了 **2× 以上的训练加速**，为高效 Speculative Decoding 提供了全新的数据视角。

</details>

---

### 5. [OWLEYE: Zero-Shot Learner for Cross-Domain Graph Data Anomaly Detection](https://arxiv.org/abs/2601.19102)

**Authors**: Lecheng Zheng, Dongqi Fu, Zihao Li, Jingrui He  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.19102v1  

#### Abstract
Graph data is informative to represent complex relationships such as transactions between accounts, communications between devices, and dependencies among machines or processes. Correspondingly, graph anomaly detection (GAD) plays a critical role in identifying anomalies across various domains, incl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# OWLEYE: Zero-Shot Learner for Cross-Domain Graph Data Anomaly Detection 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Graph Anomaly Detection (GAD)** 方法大多遵循“one model for one dataset”范式，缺乏跨域泛化能力。新兴的“one-for-all”通用模型（如 ARC 和 UNPrompt）虽然尝试解决此问题，但仍面临三大挑战：
1. **跨域特征语义不一致**：不同领域图数据的节点属性维度和语义差异大，难以对齐。
2. **缺乏持续学习能力**：无法在不重新训练的情况下增量整合新图的知识。
3. **依赖少量标签进行微调**：多数方法需要目标图上的少量标注数据实现 few-shot 学习，但在实际中异常标注成本高且不可靠。

### 提出的新方法：OWLEYE
作者提出 **OWLEYE**，一种全新的零样本（zero-shot）跨域图异常检测框架，具备以下三个核心创新模块：

#### （1）Cross-Domain Feature Alignment Module
- 利用 **pairwise distance statistics** 对来自不同图的节点特征进行归一化与对齐。
- 在保持各图自身语义完整性的同时，将异构特征映射到共享输入空间。
- 避免了传统 PCA/SVD 等降维方法破坏原始结构模式的问题。

#### （2）Multi-Domain Multi-Pattern Dictionary Learning
- 构建一个可扩展的 **pattern dictionary** 来存储多个源图中的正常行为模式。
- 分别提取 **attribute-level** 和 **structure-level** 表征，并从中随机采样代表性模式存入字典。
- 支持模型无需重训即可通过添加新模式实现**持续演化（continual learning）**。

#### （3）Truncated Attention-Based Reconstruction Module
- 设计基于截断注意力机制的重构模块，用于上下文学习（in-context learning）。
- 在重构过程中自动过滤潜在异常节点的影响，仅利用最可信的“伪支持节点”进行重建。
- 实现完全无监督的 zero-shot 推理，无需任何目标图标签。

### 相比现有方法的优势
| 维度 | OWLEYE | ARC / UNPrompt |
|------|--------|----------------|
| 跨域对齐效果 | ✅ 显著提升对齐质量，保留关键密度模式 | ❌ ARC 分离图；UNPrompt 扰乱密度关系 |
| 是否需标签 | ✅ 完全 zero-shot | ⚠️ 多数需 few-shot 微调 |
| 可持续学习 | ✅ 支持动态更新 pattern dictionary | ❌ 不支持增量学习 |
| 泛化能力 | ✅ 在未见图上表现最优 | ⚠️ 性能波动较大 |

---

## 2. 核心实验方法和设置

### 数据集
- **训练集 $ \mathcal{T}_{\text{train}} $**: PubMed, CiteSeer, Questions, YelpChi  
  （涵盖社交网络、引用网络、电商评论等多领域）
- **测试集 $ \mathcal{T}_{\text{test}} $**: Cora, Flickr, ACM, BlogCatalog, Facebook, Weibo, Reddit, Amazon  
  （共8个真实世界图数据集，含注入或真实异常）

> 实验也验证了不同 train/test 划分下的鲁棒性（见 Appendix B.2）。

### 实验设置
- **任务设定**：Zero-shot 和 10-shot setting 下的节点级异常检测。
- **评估指标**：
  - **AUPRC**（Area Under Precision-Recall Curve）——重点关注稀疏异常场景下的性能。
  - **AUROC**（Area Under ROC Curve）
- **重复次数**：所有实验运行 5 次取均值 ± 标准差。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| **Supervised (10-shot)** | BWGNN, GHRN |
| **Unsupervised (zero-shot)** | DOMINANT, SLGAD, TAM, CARE |
| **One-for-All Generalist Models** | ARC, UNPrompt |
| **Proposed** | **OWLEYE** |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（AUPRC 平均值）

| 方法 | Average AUPRC (Zero-Shot) |
|------|----------------------------|
| DOMINANT | 24.46% |
| CARE | 28.72% |
| ARC | 30.74% |
| **OWLEYE (Ours)** | **36.17%** ✅ |

> OWLEYE 比当前最佳基线 **ARC 提升超过 5%**，且为纯 zero-shot 方法。

### 与基线方法对比结果
- **超越监督方法**：即使在提供 10 个标签的 10-shot 设置下，OWLEYE 仍优于 BWGNN 和 GHRN，在 6/8 数据集上取得更高 AUPRC。
- **显著优于其他 zero-shot 方法**：
  - 在 Weibo 上达到 **60.90% AUPRC**，远超第二名 ARC 的 64.18%（但注意：ARC 使用微调）。
  - 在 Amazon 上达到 **62.20% AUPRC**，大幅领先于 ARC 的 20.48%。
- **AUROC 同样领先**：平均 AUROC 达 **75.42%**，优于 ARC 的 74.04%。

### 消融实验结果（Ablation Study）
消融三种核心组件后性能下降明显：

| 变体 | Average AUPRC | 相对下降 |
|------|---------------|----------|
| OWLEYE (Full) | 36.17% | —— |
| OWLEYE-N (无特征对齐) | ~34.08% | ↓2.09% |
| OWLEYE-S (无结构模式) | ~35.18% | ↓0.99% |
| OWLEYE-T (标准注意力) | ~34.08% | ↓2.09% |

> 结果表明：**特征对齐、结构模式建模、截断注意力**三者缺一不可。

此外，case study 验证了：
- **持续学习能力**：直接向 dictionary 添加新图的 pattern，无需微调即可提升性能（见 Table 3）。
- **字典规模影响**：当 `nsup > 200` 后性能趋于饱和，说明存在收益递减点。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **有效的跨域特征对齐是构建通用 GAD 模型的基础**：OWLEYE 的统计对齐策略有效解决了异构特征空间问题。
2. ✅ **结构信息对识别伪装异常至关重要**：仅依赖属性可能导致误判，而结构模式能捕捉拓扑异常。
3. ✅ **pattern dictionary 是实现 zero-shot 和持续学习的关键设计**：知识可积累、可复用、可扩展。
4. ✅ **截断注意力机制增强了鲁棒性**：避免异常节点污染重构过程，提升检测准确性。

### 方法的局限性
- **对极端分布偏移敏感**：当训练集中缺失整个领域（如移除 YelpChi），在同域测试图 Amazon 上 AUPRC 下降约 4.6%，尽管整体仍具竞争力。
- **字典容量有限制**：虽然支持扩展，但过大的字典可能带来计算开销。
- **假设大多数节点正常**：用于构建“伪支持集”的前提在极高异常率下可能失效（尽管实验显示在 20% 异常率下仍稳健）。

### 未来工作方向
- **Multi-modal Graph Foundation Model**：融合文本、时间序列、图像等多模态信号，构建更强大的图基础模型。
- **弱监督/噪声标签下的学习**：适应现实世界中标注不准确或稀疏的情况。
- **动态图上的在线异常检测**：扩展至时序图场景，支持实时更新与推理。
- **可解释性增强**：进一步挖掘 attention map 的语义含义，辅助人工研判。

---

> **总结**：OWLEYE 成功构建了一个**可泛化、可持续、无需标签**的跨域图异常检测框架，为迈向真正的 **Graph Foundation Model for Anomaly Detection** 奠定了坚实基础。

</details>

---

### 6. [OSIRIS: Bridging Analog Circuit Design and Machine Learning with Scalable Dataset Generation](https://arxiv.org/abs/2601.19439)

**Authors**: Giuseppe Chiari, Michele Piccoli, Davide Zoni  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.19439v1  

#### Abstract
The automation of analog integrated circuit (IC) design remains a longstanding challenge, primarily due to the intricate interdependencies among physical layout, parasitic effects, and circuit-level performance. These interactions impose complex constraints that are difficult to accurately capture a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：OSIRIS: Bridging Analog Circuit Design and Machine Learning with Scalable Dataset Generation

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
模拟集成电路（Analog IC）设计自动化长期面临挑战，尤其是**后端物理布局阶段**。该阶段对寄生效应（parasitics）、器件匹配、几何约束极为敏感，导致设计高度依赖专家经验，流程迭代且耗时。此外，缺乏公开、高质量、带物理验证的**大规模模拟电路布局数据集**，严重制约了机器学习（ML）在该领域的应用。

### 提出了什么新方法或新思路
本文提出了 **OSIRIS** —— 一个可扩展的、端到端的模拟IC布局数据集生成框架，其核心创新点如下：

- **OSIRIS 框架**：  
  构建了一个系统性的、可迭代的布局探索流水线，能够从电路网表（netlist）出发，自动生成大量符合制造规则（DRC-clean）和逻辑一致性（LVS-verified）的物理布局（GDS），并附带详细的性能评估报告（QoS）。

- **可迭代的探索策略**：  
  与传统“单次通过”（single-pass）的布局工具不同，OSIRIS 支持**迭代式设计空间探索**（iterative exploration），允许基于后布局仿真反馈（post-layout feedback）不断优化布局。

- **开源大规模数据集发布**：  
  利用 OSIRIS 生成并公开发布了包含 **87,100 个布局变体**的数据集，覆盖5种典型模拟电路（如 Miller OTA、Ahuja OTA 等），是首个专注于**后端布局阶段**的大规模、带寄生感知（parasitic-aware）的公开数据集。

- **强化学习（RL）驱动的优化基线**：  
  提供了一个基于 RL 的优化框架，用于在 OSIRIS 流程中进行智能布局搜索，证明了 ML 可有效指导布局优化。

### 相比现有方法的优势
| 方面 | 现有方法（如 ALIGN, MAGICAL） | OSIRIS |
|------|-------------------------------|--------|
| **流程模式** | 单次生成，无反馈闭环 | 支持迭代优化，可集成后布局反馈 |
| **数据产出** | 主要输出 netlist 或单一布局 | 大规模、多样化布局变体 + 完整 QoS 指标 |
| **ML 友好性** | 不直接支持 ML 训练 | 数据结构专为 ML 设计（配对仿真、元数据） |
| **开放性** | 部分开源，但数据有限 | 完全开源代码 + 大规模数据集 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **自研数据集**：由 OSIRIS 在 **Skywater 130nm PDK** 上生成，包含以下5类电路：
  - Miller OTA
  - Ahuja OTA
  - Feed Forward OTA
  - 5-Transistors OTA
  - Low-Pass Filter (LPF)

- 数据总量：**87,100 个 DRC/LVS 验证通过的布局变体**，总大小约 5GB。

### 实验设置和评估指标
#### 评估指标
1. **PEX Score**：  
   衡量寄生效应影响的关键指标，定义为前布局（pre-layout）与后布局（post-layout）仿真结果之间的 **RMSE（单位：V）**。值越小越好。
   $$
   \text{pscore} = \sqrt{\frac{1}{K}\sum_{i=1}^{K}(pre_i - post_i)^2}
   $$

2. **Area**：  
   布局总面积（单位：μm²），反映芯片成本和集成密度。值越小越好。

3. **Acquisition Time**：  
   生成最终优化布局所需的总时间（小时）。

#### 实验设置
- 所有布局生成均通过 OSIRIS 流水线完成，包含：
  - **Fingers Permutation**：枚举晶体管指状结构（fingers）的合法组合。
  - **Random / RL-based Variant Generation**：在基线布局基础上进行空间扰动探索。
- 仿真使用 **Ngspice**，DRC/LVS 验证使用 **Magic** 和 **Netgen**。
- RL 模型训练使用 **Proximal Policy Optimization (PPO)** 和 **REINFORCE**。

### 基线方法对比
- **MAGICAL**：开源全自动模拟布局系统。
- **ALIGN**：另一主流开源模拟布局框架。
- **Random Exploration**：OSIRIS 内置的随机扰动策略，作为弱基线。
- **RL-driven Methodology**：本文提出的两层 RL 优化策略（外层搜索 finger 配置，内层优化组件位置）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）
| Circuit | 方法 | pscore (V) ↓ | Area (μm²) ↓ | Time (hh:mm:ss) |
|--------|------|---------------|----------------|------------------|
| **Miller** | MAGICAL | 0.142 | 1836 | 00:01:08 |
| | Random | 0.0012 | 1770 | 195:00:00 |
| | **RL (Ours)** | **0.00069** | **1733** | **96:00:00** |
| **Ahuja** | MAGICAL | 0.315 | 1906 | 00:01:08 |
| | Random | 0.120 | 1805 | 191:00:00 |
| | **RL (Ours)** | **0.120** | **1797** | **70:00:00** |
| **Feed Forward** | MAGICAL | 0.210 | 806 | 00:01:18 |
| | Random | 0.037 | 798 | 156:00:00 |
| | **RL (Ours)** | **0.024** | **768.5** | **62:00:00** |
| **5-Transistors** | MAGICAL | 0.093 | 183 | 00:00:48 |
| | Random | 0.050 | 266.3 | 162:00:00 |
| | **RL (Ours)** | **0.047** | 444.5 | **50:00:00** |
| **LPF** | MAGICAL | *失败* | *失败* | 00:01:20 |
| | Random | 0.102 | 7916 | 187:00:00 |
| | **RL (Ours)** | **0.064** | **6635** | **67:00:00** |

### 与基线方法的对比结果
- **PEX Score 显著降低**：  
  RL 方法在所有电路中均取得最低的 pscore，**比 MAGICAL 和 ALIGN 低一个数量级以上**，表明其能更有效地抑制寄生效应。
- **面积表现优异**：  
  在 Miller、Feed Forward 和 LPF 上，RL 方法生成的布局面积最小；仅在 5-Transistors 上略大，但换来了更好的电气性能。
- **运行时间大幅缩短**：  
  相比 Random 探索，RL 方法**平均减少约 50–70% 的运行时间**，说明其探索效率更高。
- **鲁棒性强**：  
  成功生成了 MAGICAL 无法处理的 LPF 电路布局。

### 消融实验结果（隐含于设计中）
虽然未明确列出消融表，但从流程设计可推断：
- **Fingers Permutation 的有效性**：通过枚举合法 finger 组合，显著增加了布局多样性。
- **RL vs Random 的优势**：RL 能主动学习“有益”的布局变换策略，而随机方法需大量试错。
- **两层 RL 架构的必要性**：分别优化 finger 配置和空间布局，实现了更全面的设计空间探索。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **大规模、高质量的布局数据集是推动 ML 在模拟后端设计中应用的关键前提**，OSIRIS 成功填补了这一空白。
2. **迭代式、反馈驱动的布局优化流程优于传统的单次生成范式**，尤其在处理寄生敏感的模拟电路时。
3. **强化学习可以有效引导布局空间探索**，在更短时间内找到寄生更小、面积更优的布局方案。
4. **生成的数据集可用于多种 ML 任务**，如布局预测、组件级生成（如文中用 LLM 生成电容布局）、性能估计等。

### 方法的局限性
- 当前仅支持 **Skywater 130nm PDK**，尚未验证其他工艺节点。
- 支持的电路类型有限（5 种），通用性有待扩展。
- RL 训练依赖大量仿真，计算开销仍较高。
- 布局变换目前仅限于 finger 数和组件平移，未涵盖旋转、复杂布线等高级操作。

### 未来工作方向
- 扩展至更多电路家族（如 ADC、PLL）和先进工艺节点（如 40nm, 28nm）。
- 引入更丰富的布局变换算子（如旋转、镜像、多层布线）。
- 支持跨 PDK 的迁移学习（transfer learning）。
- 探索基于 GNN、Diffusion Model 等更先进的 ML 模型进行布局生成与优化。

> **总结**：OSIRIS 是首个将**可扩展数据集生成**与**ML 驱动优化**深度融合的模拟 IC 后端设计框架，为实现真正的端到端模拟设计自动化奠定了重要基础。

</details>

---

### 7. [Learn and Verify: A Framework for Rigorous Verification of Physics-Informed Neural Networks](https://arxiv.org/abs/2601.19818)

**Authors**: Kazuaki Tanaka, Kohei Yatabe  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.19818v1  

#### Abstract
The numerical solution of differential equations using neural networks has become a central topic in scientific computing, with Physics-Informed Neural Networks (PINNs) emerging as a powerful paradigm for both forward and inverse problems. However, unlike classical numerical methods that offer estab...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learn and Verify: A Framework for Rigorous Verification of Physics-Informed Neural Networks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前基于 **Physics-Informed Neural Networks (PINNs)** 的微分方程求解器虽然在科学计算中表现出色，但存在一个根本性缺陷：**缺乏严格的误差界（rigorous error bounds）和数学可验证性**。由于训练过程具有随机性（如权重初始化、采样策略），其输出无法提供像传统数值方法（如有限元法）那样的收敛保证，导致结果可靠性难以认证。

此外，已有理论分析多为渐近性质或理想假设下的估计，**无法转化为机器可验证的证明**。

---

### ✅ 提出了什么新方法或新思路

作者提出了一种名为 **“Learn and Verify”** 的两阶段框架，旨在为 PINNs 提供**可计算、数学上严格、且机器可验证的误差界**。

#### 核心思想：
- **Learn 阶段**：使用 PINNs 学习目标 ODE 的近似解，并进一步构造一对 **sub-solution（下解）** 和 **super-solution（上解）**，使得真实解被严格夹在两者之间。
- **Verify 阶段**：利用 **interval arithmetic（区间算术）** 对这对函数进行**严格验证**，确认它们在整个时间域内满足所需的微分不等式，从而形成对真实解的**rigorous enclosure（严格包络）**。

#### 创新技术组件：
1. **Variation Learning**：
   - 构造偏差网络 $ v_\theta(t), w_\phi(t) \geq 0 $，定义：
     $$
     u_{\text{low}}(t) = u_0(t) - v_\theta(t),\quad u_{\text{high}}(t) = u_0(t) + w_\phi(t)
     $$
   - 输出层采用缩放后的 sigmoid 函数 $ \epsilon \cdot \sigma(\cdot) $，确保偏差不超过预设容忍度 $ \epsilon $，实现结构化的误差控制。

2. **Doubly Smoothed Maximum (DSM) Loss**：
   - 用于训练 sub/super-solutions，平滑地逼近最大违反项：
     $$
     \text{DSM}_{c_1,c_2}[g(t)] = c_2 \log \sum_{t \in S} \left(1 + \exp\left(\frac{g(t)}{c_1}\right)\right)
     $$
   - 结合 Softplus 和 Log-Sum-Exp，避免非光滑损失带来的梯度消失问题，同时引入“软边界效应”，增强训练稳定性。

3. **Interval Arithmetic + Adaptive Subdivision**：
   - 在 Verify 阶段，将时间域划分为子区间，在每个子区间上用 interval arithmetic 严格计算残差范围。
   - 若某区间判定为“不确定”（indeterminate），则递归二分细化，直到所有区间均可明确判断是否满足不等式。

---

### ✅ 相比现有方法的优势

| 方面 | 优势 |
|------|------|
| **可靠性** | 提供**数学上可证明的误差界**，而非启发式估计，适用于高风险科学应用。 |
| **通用性** | 不依赖闭式解或参考数值解，可用于无解析解的问题（如 generalized logistic, Riccati）。 |
| **可验证性** | 整个验证流程是**机器可执行的**，构成一种“机器可验证的证明”。 |
| **灵活性** | Learn 阶段可用任意神经网络架构（文中使用 SIREN），Verify 阶段独立于学习方式。 |
| **处理奇异性** | 能够通过变量变换和扩展 sub-solution 来**严格包围有限时间爆破（finite-time blow-up）的时间点**。 |

---

## 2. 核心实验方法和设置

### ✅ 使用的 ODE 数据集（问题）

论文在三个非线性 ODE 上进行了实验：

1. **Classical Logistic Equation**  
   $$
   \dot{u} = r u (1 - u/k),\quad u(0)=a
   $$
   - 参数：$ r=1, k=2, a=0.5, T=10 $
   - 有解析解，可用于定量误差评估。

2. **Generalized Logistic Equation**  
   $$
   \dot{u} = r(t) u (1 - u/k(t)),\quad r(t)=r_0(1+\sin(\omega t)),\ k(t)=k_0(\log(1+t)+1)
   $$
   - 时间依赖系数，**无显式解析解**。
   - 使用 MATLAB `ode45`（高精度设置）作为参考解。

3. **Riccati Equation（Blow-up Case）**  
   $$
   \dot{u} = t^2 + u^2,\quad u(0)=0
   $$
   - 解在有限时间内趋于无穷（blow-up at $ T_{\text{bu}} \approx 2 $）
   - 展示框架对奇异性的处理能力。

---

### ✅ 实验设置

| 组件 | 设置 |
|------|------|
| **网络架构** | SIREN：4 隐藏层 × 30 神经元；激活函数为 sin |
| **训练优化器** | Adam |
| **Mini-batch size** | Step 1: 128；Step 2: 1280（region-based sampling） |
| **采样策略** | 区域分层采样（stratified sampling），100 个等距区域 |
| **Loss 参数** | $ c_1 = 10^{-2}, c_2 = 10^{-3} $ |
| **正则化项** | 引入物理正则项 $ L_{\text{phys}} $ 抑制不稳定行为（基于 $ \partial f/\partial u < 0 $） |
| **验证工具** | INTLAB（MATLAB 的 interval arithmetic 工具箱） |
| **代码公开** | GitHub: https://github.com/Kazuaki-Tanaka/learn-and-verify-ode |

---

### ✅ 评估指标

| 指标 | 描述 |
|------|------|
| **Verification Success Rate** | 成功通过 interval arithmetic 验证的比例（关键指标） |
| **Maximum Relative Error** | 与真解的最大相对误差（仅限有解析解的情况） |
| **Error Tolerance $ \epsilon $** | 控制上下界宽度的参数，越小表示要求越高 |
| **Blow-up Time Bounds** | 对 Riccati 方程给出的爆破时间上下界 $[T, 1/u_{\text{low}}(T)]$ |

---

### ✅ 基线方法对比

本文未直接与其他 PINN 变体（如 Deep Ritz, Deep BSDE）进行端到端性能比较，而是强调其**独特价值在于“验证能力”**，这是其他方法所不具备的。

- 传统 PINNs：只能输出单一近似曲线，无法提供误差界。
- 数值方法（如 ode45）：虽精确但无理论误差界（除非专门设计验证算法）。
- 本文方法：输出的是一个**函数区间** $[u_{\text{low}}, u_{\text{high}}]$，并附带数学证明该区间包含真实解。

因此，其“基线”实为**无验证机制的传统 PINN 流程**。

---

## 3. 主要实验结果和性能指标

### ✅ Classical Logistic Equation

- **成功验证率随 $ \epsilon $ 和训练轮次增加而上升**（见 Table I）：
  - 当 $ \epsilon = 2^{-5} $，4 层网络在 200 轮后可达 98% 成功率。
  - $ \epsilon = 2^{-8} $ 时全部失败 → 表明当前方法仍受限于过紧容差。
- **正则化参数 $ \lambda_{\text{phys}} $ 显著影响成功率**：
  - $ \log_2(\lambda_{\text{phys}}) \geq -5.5 $ 时成功率接近 100%。
  - 弱正则化下大量解陷入局部极小，验证失败。
- **平均最大相对误差约 $ 10^{-2} \sim 10^{-1} $**，与框架估计一致。

> 📊 图 5 显示：随着 $ \epsilon $ 减小，residual 更趋近于零且符号正确，验证更容易通过。

---

### ✅ Generalized Logistic Equation

- **更难训练**，需要更多 epoch 才能收敛。
- **浅层网络表现差**：2 层网络在 $ \epsilon \leq 2^{-5} $ 下完全无法通过验证。
- **4 层网络优于更深网络**：可能因训练难度与表达能力之间的权衡。
- **验证成功率为自动调参提供依据**，优于人工试错。

> 📊 图 8 显示：即使没有解析解，也能构造出合理包络，且残差符号符合要求。

---

### ✅ Riccati Equation（Blow-up Case）

- **首次实现了对有限时间爆破的“rigorous enclosure”**。
- 通过变量变换 $ u = 1/(c - t)\varphi $ 正则化奇点，在 $ t \in [0, 1.9375] $ 上训练。
- 利用 analytically extended sub-solution 得到 blow-up time 上界：
  $$
  T_{\text{bu}} \leq T + \frac{1}{u_{\text{low}}(T)}
  $$

| $ \epsilon $ | $ c $ | $ u_{\text{low}}(T) $ | Lower Bound | Upper Bound |
|--------------|--------|------------------------|-------------|-------------|
| $ 2^{-2} $   | 2.00   | 12.119                 | 1.9375      | 2.020       |
| $ 2^{-3} $   | 2.00   | 14.029                 | 1.9375      | 2.009       |
| $ 2^{-4} $   | 2.00   | 14.893                 | 1.9375      | 2.005       |

✅ **结论**：减小 $ \epsilon $ 可提升 $ u_{\text{low}}(T) $，从而收紧上界，验证效果更好。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **“Learn and Verify” 框架首次实现了对 PINNs 解的数学可验证性**，填补了深度学习与可信科学计算之间的鸿沟。
2. **sub- and super-solution + interval arithmetic** 是一种强大组合，能将黑盒模型输出转化为**机器可验证的数学证明**。
3. **DSM loss 和 variation learning** 显著提升了训练稳定性和验证成功率。
4. 即使在无解析解或存在奇异性的情况下，该框架仍能提供**rigorous error bounds**，展现出广泛适用性。
5. 验证过程本身成本低（通常几秒完成），远低于训练时间，适合部署后验证。

---

### ⚠️ 方法的局限性

1. **对误差容忍度 $ \epsilon $ 敏感**：当 $ \epsilon $ 过小时，训练难以收敛，验证失败率高。
2. **依赖良好初始近似解**：若第一步的 $ u_0(t) $ 偏离太大，后续难以构造有效包围。
3. **目前仅适用于 ODE**，扩展至 PDE 尚需解决更高维区间运算和复杂边界条件等问题。
4. **interval arithmetic 天然保守**：可能导致包络过宽，尤其在高维或强非线性情况下。
5. **超参数选择仍需经验**：如 $ c_1, c_2, \lambda_{\text{phys}} $ 等尚未完全自动化。

---

### 🔮 未来工作方向

1. **扩展至 PDEs**：结合 verified PDE solvers（如基于固定点定理的方法）构建统一框架。
2. **自动超参数调优**：基于验证反馈动态调整 $ \epsilon $、网络深度、正则化强度等。
3. **联合优化 sub/super-solutions 与变换参数**：例如将 blow-up time 估计也作为可训练变量。
4. **GPU 加速 interval arithmetic**：提升高维问题的验证效率。
5. **集成到科学 ML pipeline**：作为标准模块用于模型选择、不确定性量化和安全决策支持。

---

## 总结

> ✅ **本论文提出了首个将 PINNs 从“近似求解器”升级为“可验证求解器”的完整框架——Learn and Verify。**
>
> 它不仅解决了神经网络求解微分方程缺乏严格误差界的长期难题，还展示了如何通过 **sub/super-solution 构造 + interval arithmetic 验证** 实现**机器可验证的数学证明**。
>
> 实验表明，该方法在多种非线性 ODE 上均能成功构造出包含真实解的严格包络，尤其在处理 blow-up 等挑战性现象时展现出独特优势。
>
> 尽管仍有改进空间，但这一工作为 **trustworthy scientific machine learning** 奠定了坚实基础。

</details>

---

### 8. [Learning Adaptive Parallel Execution for Efficient Code Localization](https://arxiv.org/abs/2601.19568)

**Authors**: Ke Xu, Siyang Xiao, Ming Liang, Yichen Yu, Zhixiang Wang, Jingxuan Xu, Dajun Chen, Wei Jiang, Yong Li  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.19568v1  

#### Abstract
Code localization constitutes a key bottleneck in automated software development pipelines. While concurrent tool execution can enhance discovery speed, current agents demonstrate a 34.9\% redundant invocation rate, which negates parallelism benefits. We propose \textbf{FuseSearch}, reformulating pa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Adaptive Parallel Execution for Efficient Code Localization

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对自动化软件开发中的**code localization**瓶颈展开研究。当前基于LLM的agents在定位相关代码实体（如文件、函数）时，通常采用**sequential tool execution**，即每轮交互仅调用一个工具（如`grep`, `read_file`），导致搜索过程冗长且效率低下。尽管并行执行（parallel execution）可提升信息密度，但现有方法常因**冗余调用**（redundant tool invocations）而浪费计算资源——研究表明，强制并行策略中高达 **34.9%** 的工具调用是冗余的。

此外，受限于**turn budget**（交互轮次限制），agents易陷入“**information starvation**”困境：未充分收集上下文便耗尽交互次数，从而牺牲准确率以换取速度。

### 提出的新方法与创新思路
作者提出 **FuseSearch**，一种通过**learned adaptive parallel execution**实现高效代码定位的框架，其核心创新如下：

- **Tool Efficiency 作为优化目标**  
  定义了一个新的度量指标 **tool efficiency $e$**，表示每次工具调用带来的**唯一信息增益**（unique information gain）占总调用数的比例：
  $$
  e = \frac{1}{k} \sum_{i=1}^{k} g_i, \quad g_i = \begin{cases}
  \frac{|E_i \setminus H|}{|E_i|} & \text{if } |E_i| > 0 \\
  0 & \text{otherwise}
  \end{cases}
  $$
  其中 $E_i$ 是第 $i$ 次调用返回的实体集合，$H$ 是历史已发现实体的并集。该指标直接惩罚冗余探索。

- **双目标联合优化框架（Dual-Objective Optimization）**  
  将定位任务建模为同时优化 **localization quality**（F1 分数）和 **search efficiency**（tool efficiency $e$）的任务，构建复合奖励函数用于强化学习：
  $$
  R(T) = \alpha \cdot F1(T) + \gamma \cdot (F1(T) \cdot e(T))
  $$
  此设计确保高奖励仅出现在“高质量+高效率”的轨迹上，避免低效但准确或高效但错误的行为。

- **两阶段训练范式（SFT + RL）**  
  - **SFT阶段**：使用经过双重过滤（dual-metric filtering）的高质量轨迹进行监督微调，确保初始模型具备良好的并行能力和效率。
  - **RL阶段**：采用 **Group Relative Policy Optimization (GRPO)** 进一步优化策略，在保持KL散度约束的同时最大化复合奖励。

- **极简主义工具集（Minimalist Tool Set）**  
  仅依赖三个语言无关、只读的工具：
  - `grep`: 正则匹配代码内容
  - `glob`: 路径模式匹配
  - `read_file`: 读取文件内容（可指定行范围）
  
  不依赖AST解析器、代码图等复杂基础设施，提升了部署通用性和简洁性。

### 相比现有方法的优势
| 维度 | FuseSearch | 现有方法 |
|------|----------|--------|
| 执行方式 | 自适应动态调整并行广度 | 固定宽度并行或串行 |
| 效率控制 | 显式建模 tool efficiency 并优化 | 忽视冗余调用成本 |
| 架构复杂度 | 极简，无需代码图/AST | 多需预构建静态图（如LocAgent） |
| 性能表现 | 更高F1、更少turn/token/time | 准确率与效率难以兼顾 |

---

## 2. 核心实验方法和设置

### 数据集
- **主评测集**：**SWE-bench Verified** (Jimenez et al., 2024)，共386个真实GitHub issue修复样本。
- **额外验证集**：**LocBench** (Chen et al., 2025)，用于跨基准泛化能力测试。
- **训练数据**：从233个高质量GitHub仓库中构建约21K样本的仓库级定位数据集，排除引入全新文件/函数的patch。

### 实验设置与评估指标

#### 评估维度
| 类别 | 指标 | 说明 |
|------|------|------|
| **定位质量** | File-level / Function-level **Precision**, **Recall**, **F1** | 衡量预测与真实修改位置的重合度 |
| **搜索效率** | **#Turn**（交互轮次）<br>**T(s)**（墙钟时间）<br>**Tok.(k)**（token消耗） | 反映延迟与计算开销 |
| **过程效率** | **Tool Efficiency $e$** | 冗余调用越少，$e$越高 |

#### 模型配置
- 主干模型：**Qwen3-4B-Instruct** 和 **Qwen3-30B-A3B-Instruct**
- 对比基线：
  - **Workflow-based**: Agentless (Xia et al., 2024)
  - **Agent-based**: LocAgent, CoSIL, RepoSearcher
- 所有方法均在相同硬件（8×NVIDIA H20 GPU）下运行，使用vLLM加速推理。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（SWE-bench Verified）

| 方法 | File F1 (%) | Func F1 (%) | #Turn | T(s) | Tok.(k) |
|------|-------------|-------------|-------|------|---------|
| **FuseSearch-4B (train)** | **84.7** | **56.4** | **4.78** | **5.43** | **30.9** |
| RepoSearcher (Qwen3-4B) | 38.1 | 21.7 | 14.8 | 85.3 | 99.2 |
| Agentless (Haiku 4.5) | 54.5 | 31.8 | 2.00 | 7.32 | 10.6 |

> ✅ **性能提升显著**：
> - 文件级F1提升 **+46.6pp**
> - 函数级F1提升 **+34.7pp**
> - 节省 **67.7% turns**, **93.6% time**, **68.9% tokens**

### 与基线方法对比结果

- 在同等backbone（Qwen3-4B）下，**FuseSearch (train)** 显著优于所有基线：
  - 比 **RepoSearcher** 提升超 **45pp F1**
  - 比 **LocAgent** 减少 **67% turns** 和 **95% time**
- 即使与更强闭源模型（如Claude Haiku 4.5）相比，**FuseSearch-4B** 达到相当甚至更优的F1分数，同时速度快 **10倍以上**。

### 消融实验结果

#### （1）并行 vs 串行执行（Table 3）
| 模式 | 阶段 | File F1 | #Turn | Tok.(k) |
|------|------|--------|--------|--------|
| Sequential | SFT+RL | 78.82 | 7.52 | 59.4 |
| **Parallel** | **SFT+RL** | **84.65** | **5.60** | **30.9** |

> ✔️ 并行执行不仅提速，还带来更高F1，证明“同步信息获取”有助于决策。

#### （2）SFT数据过滤策略（Table 4）
| 过滤方式 | File F1 | Func F1 | $e$ | Tok.(k) |
|--------|--------|--------|-----|--------|
| 无过滤 | 75.44 | 43.52 | 55.77 | 60.7 |
| F1-only | 78.55 | 45.43 | 56.72 | 73.2 |
| $e$-only | 76.74 | 42.63 | 60.14 | 61.8 |
| **Joint (F1 + e)** | **78.86** | **47.94** | **62.03** | **54.8** |

> ✔️ **联合过滤** 同时提升质量与效率，为后续RL提供更优起点。

#### （3）奖励函数设计（Table 5）
| 奖励形式 | File F1 | $e$ | T(s) | Tok.(k) |
|--------|--------|-----|------|--------|
| F1 only | 81.84 | 59.66 | 7.28 | 39.4 |
| F1 + e (additive) | 79.22 | 66.62 | 9.40 | 45.7 |
| **F1 + F1·e (multiplicative)** | **84.65** | **69.00** | **5.43** | **30.9** |

> ✔️ **乘法形式奖励** 最佳平衡质量与效率，避免加性奖励导致的精度下降。

---

## 4. 关键结论和发现

### 主要发现
1. **效率感知训练能自然提升质量**  
   通过显式优化 **tool efficiency**，模型学会规避冗余查询，反而提高了最终定位准确性。这表明“去噪”本身是一种有效的正则化手段。

2. **自适应并行优于固定宽度并行**  
   RL训练后，模型展现出“先广度后深度”的搜索策略（见Figure 3）：初期多工具并行探索，后期聚焦少数高价值操作，实现了动态调节。

3. **极简工具集足以胜任复杂任务**  
   仅靠 `grep`, `glob`, `read_file` 三类基础工具，即可超越依赖代码图的复杂系统，说明**策略智能 > 工具丰富度**。

4. **下游任务显著受益**  
   当将 **FuseSearch-4B** 作为前置定位模块辅助Kimii-K2完成SWE-bench任务时：
   - 保持相似pass rate（~68%）
   - **减少23.1% interaction turns**
   - **缩短28.5% end-to-end time**
   
   表明高效定位可有效加速整体agent流水线。

### 局限性
1. **评估偏差**：SWE-bench等基准以golden patch为ground truth，可能忽略其他合法解法。
2. **语言覆盖有限**：当前评测集中Python为主，对Java/C++等静态语言的有效性有待验证。
3. **场景局限**：目前聚焦issue-driven localization，尚未拓展至代码问答、理解、文档生成等更广任务。

### 未来工作方向
- 构建涵盖多种编程语言与任务类型的综合性code localization benchmark。
- 探索将FuseSearch思想应用于其他信息检索场景（如web search, database query）。
- 结合轻量级code indexing机制，在保证效率前提下进一步增强召回能力。

---

> 🔚 **总结**：  
> **FuseSearch** 通过引入 **tool efficiency** 度量与 **joint F1-efficiency reward**，成功实现了**高质量、低延迟、低资源消耗**的代码定位。其实验证明，**效率驱动的学习范式**不仅能降低成本，还能反向提升模型性能，为构建生产级agent提供了重要路径。

</details>

---

### 9. [SETA: Statistical Fault Attribution for Compound AI Systems](https://arxiv.org/abs/2601.19337)

**Authors**: Sayak Chowdhury, Meenakshi D'Souza  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19337v1  

#### Abstract
Modern AI systems increasingly comprise multiple interconnected neural networks to tackle complex inference tasks. Testing such systems for robustness and safety entails significant challenges. Current state-of-the-art robustness testing techniques, whether black-box or white-box, have been proposed...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SETA: Statistical Fault Attribution for Compound AI Systems

## 1. 论文的主要贡献和创新点

### 解决的问题
现代AI系统越来越多地采用**Compound AI Systems**架构，即由多个相互连接的神经网络模块构成复杂推理流水线。这类系统的鲁棒性和安全性测试面临严峻挑战：
- 传统端到端（E2E）测试无法定位故障来源；
- 单模型验证工具（如Reluplex、Marabou）不支持多模块级联分析；
- 黑盒/白盒对抗攻击方法难以扩展到多网络管道；
- 缺乏对错误传播路径的细粒度归因能力。

这些问题导致在系统失效时难以判断“哪个子模块最脆弱”或“小误差如何逐层放大”。

### 提出的新方法与思路
作者提出 **SETA**（**State-based Execution Trace Analysis**），一个结合**Metamorphic Testing**（MT）与**Execution Trace Analysis**的模块化鲁棒性测试框架，核心思想如下：

- **动态执行图构建**：将AI系统建模为状态转移系统，运行时记录每个模块的输入、输出、路由决策和激活顺序，形成**Execution Trace Tree**。
- **Metamorphic Relations作为伪Oracle**：定义无需真实标签的行为一致性关系（如输出不变性、IoU阈值等），用于检测各组件在扰动下的行为偏差。
- **统计故障归因机制**（Statistical Fault Attribution）：通过计算每个模块在系统失败条件下的**Failure Contribution (FC) Score**，量化其对整体故障的相对贡献，并进行归一化得到可解释的归因权重 $ \alpha_i $。

### 相比现有方法的优势
| 维度 | 现有方法局限 | SETA优势 |
|------|---------------|----------|
| 可扩展性 | 多数仅适用于单个DNN | 支持任意拓扑结构的多模块Pipeline |
| 故障定位能力 | 仅能识别端到端失败 | 定位最早偏离点，支持因果链追溯 |
| 数据依赖 | 需要标注真值（ground truth） | Oracle-free，基于自洽性检查 |
| 架构兼容性 | 多为白盒或特定结构 | 完全黑盒，architecture-agnostic |
| 分析深度 | 忽略中间状态与控制流变化 | 同时监控数据流与控制流异常 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Railway Vision System**：
  - 基于 **RailSem19** 数据集采样构建，模拟列车视角下的铁路场景图像；
  - 应用 `imagecorruptions` 库生成合成扰动数据，包括：`fog`, `snow`, `frost`, `glass_blur`, `motion_blur`, `gaussian_noise` 等15种类型，每类5个严重等级。
  
- **Ensemble Model**：
  - 在 **CIFAR-10** 测试集上应用相同 `imagecorruptions` 扰动生成增强数据集。

- **OCR Model**：
  - 使用多个自然场景OCR数据集（引用[21,34,36]），如Street View Text、Uber-Text等；
  - 扰动来自 `img_aug`, `albumentations`, 和 `imagecorruptions`。

### 实验设置
- **扰动策略**：输入空间变换为主（亮度、噪声、模糊、遮挡等），部分支持中间特征扰动；
- **执行模式**：对每个原始输入 $x$ 与其扰动版本 $g(x)$ 并行运行，生成参考轨迹 $T(x)$ 与扰动轨迹 $T(x')$；
- **节点对齐**：依据模块ID和结构位置对齐两棵树中的对应节点；
- **判定标准**：若某节点输出违反MR或路由路径不同，则标记为component-level fault。

### 评估指标
- **Failure Contribution Score (FC)**：  
  $$
  FC_i = \mathbb{E}[Z_i \cdot \mathbf{1}(S(x,x')=0)],\quad Z_i = 1 - S_i(x,x')
  $$
  其中 $S_i$ 是第$i$个模块的复合MR得分，$S$ 是全局一致性分数。
- **归因权重 $\alpha_i$**：归一化的FC得分，反映模块相对责任。
- **预测准确率 vs 实际准确率**：用于验证SETA估算精度。
- **Levenshtein Distance**：OCR任务中衡量文本输出稳定性，设定阈值 $t=2$ 判断是否稳健。

### 基线方法对比
本文未直接与其他自动化归因工具比较（因缺乏同类工作），而是强调相对于以下主流范式的优越性：
- **End-to-End Testing**（如DeepTest）
- **Single-model Verification Tools**（如Marabou, ERAN）
- **Coverage-guided Testing**（如DeepXplore）
- **Adversarial Robustness Metrics**（如CLEVER）

> 注：SETA并非替代这些工具，而是提供互补的诊断层。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）Vision System 归因结果（表1）
| 模型 | $\alpha_i$（归因权重） |
|------|------------------------|
| YOLOv4-tiny ($f_0$) | 0.31 |
| speed_sign classifier ($f_3$) | 0.25 |
| main_signal classifier ($f_1$) | 0.06 |

- 结果显示目标检测器 $f_0$ 对系统失败贡献最大，尤其在 **fog** 条件下表现最差（见图4）；
- 分类器 $f_1$ 虽然个体敏感度高，但由于调用频率低，总体归因得分较低；
- 未被触发的模块自动获得 $\alpha_i=0$，体现框架保守性。

#### （2）Ensemble Model 准确率估计（表2）
| 模型 | SETA预测准确率 | 实际准确率 | 误差 |
|------|----------------|------------|------|
| ResNet18 | 84.94% | 83.09% | ~1.85% |
| VGG16 | 81.74% | 79.84% | ~1.9% |
| CustomCNN | 81.33% | 80.00% | ~1.33% |
| Ensemble | 86.63% | — | — |

- SETA预测误差控制在 **1–2%以内**，足以用于识别性能最差模型（CustomCNN）；
- 表明该框架可用于快速筛选需优化的子模块。

#### （3）OCR Model 鲁棒性评估（表3）
| Input ID | Robustness | Avg. Levenshtein Distance |
|---------|------------|----------------------------|
| 0 | 0.000 | 74.81 |
| 1 | 1.000 | 0.00 |
| ... | ... | ... |
| **Average** | **0.607** | **0.607** |

- 使用Levenshtein距离衡量输出一致性，低于阈值视为非鲁棒；
- SETA成功识别出多个高失真样本（如ID=0），并可追踪至具体OCR模块。

### 与基线方法的对比结果
- **相比E2E测试**：SETA不仅能发现失败，还能指出$f_0$是主要瓶颈，而E2E只能报告整体失败；
- **相比孤立测试（ICT）**：SETA揭示了上游检测错误如何引发下游分类误判（例如错误检测导致错误调用分类器），这是ICT无法捕捉的**涌现性故障**；
- **相比Coverage方法**：SETA关注的是**功能一致性**而非神经元覆盖率，更适合安全关键系统。

### 消融实验（隐含分析）
尽管文中未明确列出消融实验，但从设计中可推断以下关键因素影响效果：
- **Metamorphic Relation完整性**：MR越全面，归因越接近因果；
- **扰动多样性**：覆盖更多现实场景提升泛化能力；
- **执行轨迹保真度**：精确记录路由逻辑对控制流异常检测至关重要。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SETA能够有效实现细粒度故障归因**：在真实铁路视觉系统中，成功识别出YOLOv4-tiny为目标检测流水线中最脆弱环节；
2. ✅ **支持跨模态应用**：不仅适用于CV任务，在OCR等文本系统中也能通过Levenshtein距离实现一致性检验；
3. ✅ **无需Ground Truth即可工作**：利用Metamorphic Relations作为pseudo-oracle，适合部署后监控与回归测试；
4. ✅ **揭示错误传播机制**：不仅能定位“首次失败点”，还能通过统计相关性推测潜在根本原因；
5. ✅ **与MLOps生态兼容**：可集成进Kedro、MLflow等平台，作为诊断插件增强可观测性。

### 方法的局限性
1. ❌ **归因为相关性而非因果性**：当前FC得分反映的是统计关联，不能保证识别出真正的根本原因（root cause）；
2. ❌ **依赖手工设计MRs**：Metamorphic Relations目前由专家定义，可能存在遗漏或偏差；
3. ❌ **假设扰动独立**：统计聚合时默认各扰动效应独立，但在强耦合系统中可能不成立；
4. ❌ **执行轨迹依赖日志完整性**：若路由逻辑未充分暴露，则控制流异常可能被忽略；
5. ❌ **轻量级实现限制**：为保持通用性牺牲了对内部梯度或注意力机制的深入分析能力。

### 未来工作方向
1. 🔮 **引入因果推理机制**：结合do-calculus或interventional analysis，从相关性迈向**causal fault localization**；
2. 🔮 **自动学习Metamorphic Relations**：利用程序合成或LLM从执行日志中自动归纳合理MR；
3. 🔮 **不确定性感知归因**：为$\alpha_i$添加置信区间，提升诊断可靠性；
4. 🔮 **扩展至多模态与RL系统**：应用于VLM（Vision-Language Models）、自动驾驶决策系统等更复杂场景；
5. 🔮 **在线监控与自适应测试**：将SETA嵌入生产环境，实现实时漂移检测与主动测试触发。

---

> **总结一句话**：  
> SETA提出了一种新颖的、基于执行轨迹与Metamorphic Testing的统计故障归因框架，填补了**Compound AI Systems**在细粒度鲁棒性分析上的空白，为复杂AI系统的可解释测试提供了可行路径。

</details>

---

### 10. [PROTEUS: SLA-Aware Routing via Lagrangian RL for Multi-LLM Serving Systems](https://arxiv.org/abs/2601.19402)

**Authors**: Amit Singh Bhatti, Vishal Vaddina, Dagnachew Birru  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19402v1  

#### Abstract
Production LLM deployments serve diverse workloads where cost and quality requirements vary by customer tier, time of day, and query criticality. Model serving systems accept latency SLOs directly. LLM routers do not. They force operators to tune parameters offline and guess what accuracy might resu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**PROTEUS: SLA-Aware Routing via Lagrangian RL for Multi-LLM Serving Systems**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题
当前的 **LLM 路由器**（LLM routers）无法直接满足用户的**准确性目标**（accuracy targets），只能通过离线调参间接控制路由行为。这种参数与结果之间的关系是**间接、非单调且依赖数据集**的，导致运维困难。尤其是在多客户层级、动态负载和差异化服务场景下，操作员难以实时保证服务质量（SLA），特别是**准确率下限**（accuracy floor）。

此外，现有系统如 Clipper 和 INFaaS 支持延迟或精度 SLO 输入，但不适用于跨异构 LLM 模型池的**学习型路由策略**。

### 🚀 提出的新方法与新思路
提出 **PROTEUS**（Polymorphic Router for Operational Target Enforcement with Unified SLA），一个支持运行时输入准确率目标 $ T \in [0.85, 0.95] $ 的 LLM 路由框架，其核心创新包括：

- **T-conditioned 策略网络**：将目标准确率 $ T $ 作为策略网络的输入，在推理阶段实现**单模型覆盖全精度谱系**，无需为每个 $ T $ 单独训练。
- **基于 Lagrangian RL 的约束优化机制**：
  - 引入可学习的对偶变量 $ \lambda $，在训练中动态跟踪约束违反情况（即实际准确率是否低于 $ T $）。
  - 将 $ \lambda $ 反馈至策略网络，使模型学会“高 $ T $ → 高质量偏好 $ u $”的映射关系。
- **连续质量偏好输出 $ u \in [0,1] $**：避免硬阈值切换，实现平滑的 cost-quality 权衡插值。
- **自适应路由评分函数**：
  $$
  S_i = p_i + \beta \cdot b_i - (1 - p_i)^\gamma \cdot c_i
  $$
  其中 $ \gamma $ 是可学习的成本敏感参数，允许模型自动适配不同成本分布。

### 🔍 相比现有方法的优势
| 维度 | 现有方法（如 CARROT、OmniRouter） | PROTEUS |
|------|-------------------------------|--------|
| 准确性控制方式 | 固定配置（如 $ u=0.1/0.5/0.9 $）或参数扫描 | 直接接受 $ T $ 作为运行时输入 |
| 是否需重训练 | 每个目标需重新训练或调参 | 单一模型服务所有 $ T $，零延迟适应 |
| SLA 下限保障能力 | 差（OmniRouter 仅 22% 达标） | **100% floor compliance** |
| 动态适应性 | 不支持实时变化的 $ T $ | 支持 per-query、per-customer、time-of-day 动态调整 |
| 成本节约 | 一般（60–80%） | 最高达 **89.8% 成本节省** |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **RouterBench**  
  - 包含 405K 查询，涵盖 11 个模型（Llama-2, Mistral, GPT-3.5, GPT-4, Claude-2 等）
  - 任务类型：MMLU、HellaSwag（推理）、GSM8K/MATH（数学）、HumanEval/MBPP（代码）
  - 成本范围：\$0.0001 – \$0.01 / query
  - Oracle 准确率：91.4%

- **SPROUT**  
  - 包含 45K 查询，14 个前沿模型（GPT-4o, Claude-3.5-Sonnet, o3-mini, Llama 系列等）
  - 更极端成本差异（最高达 9.5×），更现代模型组合
  - Oracle 准确率：98.6%

> 两者互补：RouterBench 强调规模与多样性；SPROUT 测试对新型高价模型的泛化能力。

### ⚙️ 实验设置
- **训练配置**：
  - 编码器：DeBERTa-v3-small（22M 参数），编码延迟 <2ms/query（A100 GPU）
  - 策略网络：2层MLP，输出 Beta 分布参数以生成 $ u $
  - 使用 PPO 进行强化学习训练
  - 批大小 32，训练步数 10K，约 4 小时/数据集
  - 对偶变量更新频率：每 5 个 batch 更新一次，学习率 $ \eta_\lambda = 0.4 $

- **评估指标**
  | 类别 | 指标 | 说明 |
  |------|------|------|
  | **适应性指标** | T-u Correlation | $ T $ 与输出质量偏好 $ u $ 的皮尔逊相关系数 |
  | | Floor Compliance (%) | 实现准确率 ≥ $ T $ 的目标比例（核心 SLA 保证） |
  | | Tolerance-band Compliance (±2%, ±5%) | 在容忍带内匹配目标的程度 |
  | | Cost Range | 最小 vs 最大 $ T $ 下的成本比 |
  | **效率指标** | Routing Efficiency (RE) | $ \text{RE} = \frac{\text{Acc} - \text{Acc}_{\text{random}}}{\text{Latency}} $（pp/ms） |
  | | Routing Performance Index (RPI) | 综合质量、成本、延迟的综合得分 |

### 🆚 基线方法对比
| 类型 | 方法 | 描述 |
|------|------|------|
| **静态策略** | Always-Best / Cheapest / Random | 固定选择最优/最便宜/随机模型 |
| **学习型路由器** | KNN / MLP | 基于 query embedding 预测最佳模型 |
| | CARROT | 使用 $ u \in \{0.1,0.5,0.9\} $ 控制 cost-quality 权衡 |
| | OmniRouter-Style | 使用 Lagrangian dual 控制 batch-level 约束 |
| | RouteLLM-Style | 基于 win-rate 的分类器 |
| **消融变体** | PROTEUS w/o T-conditioning / fixed $ \gamma $ / no critic | 移除关键组件验证有效性 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 & 2）

| 指标 | RouterBench | SPROUT |
|------|-------------|--------|
| **T-u Correlation** | **0.973** | **0.981** |
| **Floor Compliance (acc ≥ T)** | **100%** | **100%** |
| **Tolerance-band Compliance (±5%)** | 100% | 67% |
| **Achieved Accuracy** | **90.1%** | **94.0%** |
| **Oracle Gap** | +1.3 pp | +4.6 pp |
| **Cost Saving vs Best Fixed Model** | **90%** | **83%** |
| **Routing Efficiency (RE)** | **11.1 pp/ms** | **9.5 pp/ms** |
| **RPI** | **88.5** | **83.5** |

> 💡 特别亮点：
> - **PROTEUS 的 RPI 超过 Oracle（88.5 > 88.2）**，表明通过适度牺牲精度换取显著成本下降，整体效率更高。
> - 在 SPROUT 上，Premium tier 成本为 Economy 的 **5.6 倍**，但准确率仅提升 3.7%，体现精细化定价空间。

### 🔁 动态适应能力测试（Figure 2c）
在四种动态 $ T $ 场景下（Step, Drift, Cyclic, Realistic），PROTEUS 展现出**零延迟响应能力**：
- 平均 floor satisfaction：RouterBench 77%，SPROUT 82–86%
- 存在 **intentional overshoot**（+1.6% ~ +5.2%），提供鲁棒性缓冲
- 由于 $ T $ 是直接输入，无需 retraining 或 warm-up

### 🔍 消融实验结果（Table 4）

| 变体 | 影响（△Acc） | 结论 |
|------|--------------|------|
| **-Constraint ($ \lambda $)** | ↓0.27% (RB), ↓0.53% (SP) | **最关键组件**，移除后 floor compliance 崩溃 |
| **-Learnable $ \gamma $** | ↔ negligible (RB), ↓0.62% (SP) | 对成本跨度大的环境（SPROUT）更重要 |
| **-Critic** | ↓<0.05% | 几乎无影响，可省略以简化架构 |

> ✅ 验证了 Lagrangian dual 反馈机制是实现 SLA 保证的核心。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM 路由可以被建模为“目标驱动”的策略学习问题**，而非传统的参数调优。
2. **单一 PROTEUS 模型即可服务从 Economy 到 Premium 的全部 SLA tier**，支持运行时动态调整 $ T $。
3. **Lagrangian dual variable $ \lambda $ 的引入使得约束满足成为可学习的过程**，实现了接近完美的 floor compliance（100%）。
4. **T-conditioning + dual feedback 构造了强 T-u correlation（>0.97）**，确保业务需求能被忠实执行。
5. **相比基线，PROTEUS 显著优于 OmniRouter（仅 22% 达标）和 CARROT（固定输出）**，解决了“无法预测最终准确率”的痛点。

### ⚠️ 方法的局限性
1. **依赖 ground-truth 正确性标签进行训练**：生产环境中通常不可得，需借助 LLM-as-a-judge 或人工标注定期采样构建训练集。
2. **当前 benchmark 中 mid-tier 模型准确率压缩严重**，细粒度区分 $ T $ 较难。
3. **未考虑延迟维度**：目前只优化 accuracy 和 cost，尚未整合 latency-aware routing。
4. **模型池固定假设**：新增模型可能需要微调或增量训练。
5. **长期分布漂移风险**：可靠性 margin（+1.3% ~ +6.3%）虽提供短期缓冲，但持续 drift 需要策略更新。

### 🔮 未来工作方向
- **Bandit Feedback Setting**：仅观察所选模型的结果，降低标注开销，支持在线持续学习。
- **Latency-aware Routing**：扩展 reward 函数，联合优化 accuracy、cost、response time。
- **Hierarchical Routing**：先分 tier 再选 model，降低大规模 action space 的复杂度。
- **Multi-objective T Specification**：同时指定 accuracy floor 和 cost ceiling，更贴近真实 SLA 合同。
- **Multi-turn Conversation Support**：处理上下文累积、token budget 分配等时序决策问题。
- **更丰富的 benchmark**：覆盖 $ [0.5, 0.99] $ 宽广 accuracy 范围，测试精细控制能力。

---

> 🧩 总结一句话：  
> **PROTEUS 实现了“说你要什么，我就做什么”的 LLM 路由愿景——用一个模型，响应任意 SLA 目标，既省钱又达标。**

</details>

---

### 11. [Agentic Design Patterns: A System-Theoretic Framework](https://arxiv.org/abs/2601.19752)

**Authors**: Minh-Dung Dao, Quy Minh Le, Hoang Thanh Lam, Duc-Trong Le, Quoc-Viet Pham, Barry O'Sullivan, Hoang D. Nguyen  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19752v1  

#### Abstract
With the development of foundation model (FM), agentic AI systems are getting more attention, yet their inherent issues like hallucination and poor reasoning, coupled with the frequent ad-hoc nature of system design, lead to unreliable and brittle applications. Existing efforts to characterise agent...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Agentic Design Patterns: A System-Theoretic Framework*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 Foundation Model (FM) 的 **agentic AI** 系统虽然展现出强大的自主行为能力，但在实际应用中存在诸多系统性缺陷，如：
- **幻觉 (hallucination)** 和 **推理错误 (poor reasoning)**
- 缺乏长期记忆与状态管理
- 工具调用不可靠、缺乏错误恢复机制
- 多智能体协作中的通信与协调失败
- 设计模式多为经验性、碎片化，缺乏理论基础

现有 **Agentic Design Patterns (ADPs)** 多为从实践中归纳的“便利性分类”（convenience-based taxonomies），缺乏统一的理论框架支撑，导致难以复用、推广和工程化。

---

### 🚀 提出的新方法与新思路

本论文提出了一种 **基于系统论 (system-theoretic)** 的 agentic AI 架构设计框架，并在此基础上构建了一个结构化的 **Agentic Design Patterns (ADPs)** 体系。

#### 主要贡献包括：

1. **一个系统论驱动的五层功能子系统架构**
   将 agentic AI 分解为五个核心且可交互的功能子系统：
   - **Reasoning & World Model (RWM)**：负责内部世界建模与决策
   - **Perception & Grounding (PG)**：感知并结构化外部输入
   - **Action Execution (AE)**：执行动作并与环境交互
   - **Learning & Adaptation (LA)**：实现持续学习与策略优化
   - **Inter-Agent Communication (IAC)**（可选）：支持多智能体协作

   > 这种分层结构借鉴了系统工程中的模块化思想，强调各子系统的职责分离与动态信息流。

2. **一套结构化的 Agentic Design Patterns（共12个）**
   基于上述架构，提出了 **12个可复用的设计模式**，分为四类：
   - **Foundational Patterns**（基础模式）
     - `Integrator`, `Retriever`, `Recorder`
   - **Cognitive & Decisional Patterns**（认知与决策）
     - `Selector`, `Planner`, `Deliberator`
   - **Execution & Interaction Patterns**（执行与交互）
     - `Executor`, `Tool Use`, `Coordinator`
   - **Adaptive & Learning Patterns**（自适应与学习）
     - `Reflector`, `Skill Build`, `Controller`

   每个模式均遵循 GoF 风格的模板（Intent, Problem, Solution），明确其解决的具体问题和协作机制。

3. **问题-架构-模式之间的映射关系**
   通过 Sankey 图展示了五大挑战类别如何影响不同子系统，并由特定设计模式予以应对，形成闭环逻辑链条。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| **理论基础** | 经验归纳、实践观察为主 | 基于系统论的第一性原理推导 |
| **粒度与实用性** | 要么太抽象（如“反思”），要么太具体（如工具选择） | 中等粒度，聚焦子系统间交互问题 |
| **可实施性** | 缺乏标准接口与流程定义 | 提供结构化解决方案与组件职责 |
| **与经典设计模式的关系** | 很少关联 GoF 或软件工程模式 | 显式引用 `Proxy`, `Observer`, `Mediator` 等经典模式 |
| **系统可靠性提升路径** | 不清晰 | 通过组合模式系统性增强鲁棒性 |

> 例如，`Controller` 模式采用 **Observer 模式** 实现对 agent 行为的持续监控以保障价值对齐；`Tool Use` 则作为 **Proxy + Adapter** 来封装外部工具调用，提高安全性与一致性。

---

## 2. 核心实验方法和设置

> ⚠️ 注意：该论文未进行量化实验或基准测试，而是采用 **定性案例分析 (qualitative case study)** 方法验证其框架的有效性。

### 🧪 实验对象
- **ReAct 框架**：一种典型的单智能体循环架构（Thought → Act → Observe），广泛用于增强 LLM 的任务执行能力。

### 🔍 分析方法（三步法）
1. **Deconstruct（解构）**
   - 将 ReAct 的功能映射到提出的五子系统架构中，揭示其隐式、单体式的实现方式。
2. **Diagnose（诊断）**
   - 识别 ReAct 在世界建模、规划、错误恢复等方面的系统性弱点。
3. **Prescribe（处方）**
   - 使用本文提出的 ADPs 对 ReAct 进行重构，提出改进方案。

### 📊 评估方式
- **非量化分析**：无准确数据集、性能指标或统计显著性检验。
- 依赖 **概念图示**（如 Figure 4）展示集成后的增强型 ReAct 流程。
- 通过逻辑论证说明新模式如何缓解原有缺陷。

### 🆚 基线方法对比
- 本文未直接对比其他 ADP 方法的性能。
- 但在 Table 1 中进行了 **文献比较分析**，指出已有工作（如 Ng [2024], Liu et al. [2025a]）属于 bottom-up 或 high-level strategy，而本文是 **integrated, principle-based** 方法。

---

## 3. 主要实验结果和性能指标

> ❗ 由于研究性质为 **概念性框架构建**，本文 **没有提供数值实验结果或性能指标**。

### ✅ 定性“结果”总结如下：

| 原有系统（ReAct）问题 | 所提模式解决方案 | 效果描述 |
|---------------------|------------------|----------|
| 观测信息未经验证，易引入噪声 | `Integrator` 模式 | 在 PG 层增加验证管道，过滤不一致输入 |
| 上下文检索低效，状态无法持久化 | `Retriever` + `Recorder` | 改进记忆访问与状态保存机制 |
| 缺乏错误反馈与学习机制 | `Reflector` 模式 | 引入因果分析，从失败中生成策略更新 |
| 动作执行黑箱化，难以调试 | `Executor` + `Tool Use` | 结构化动作调度与工具调用接口 |
| 无长期学习能力 | `Learning & Adaptation` 子系统整合 | 形成“感知→行动→反馈→学习”的完整闭环 |

> 如 Figure 4 所示，原始 ReAct 的简单循环被扩展为一个具备 **容错、学习、控制** 能力的结构化工作流。

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **agentic AI 需要系统论指导的设计范式**
   - 当前 ad-hoc 的开发方式导致系统脆弱、不可维护。
   - 必须从第一性原理出发，建立具有理论根基的架构语言。

2. **五子系统架构提供了清晰的职责划分**
   - 明确了 RWM 为核心、PG/AE 为接口、LA 为外层调控的层级结构。
   - 支持模块化设计与跨团队协作。

3. **12个 ADPs 是可复用的“建筑构件”**
   - 每个模式针对特定子系统间的协作问题，具备高内聚、低耦合特性。
   - 可组合使用以构建复杂可靠的 agent 系统。

4. **框架可用于诊断与增强现有系统**
   - 成功应用于 ReAct 的重构，展示了其在现实系统中的适用性。

---

### ⚠️ 方法的局限性

1. **缺乏实证验证**
   - 所有分析均为定性，尚未在真实任务上进行量化评测（如成功率、效率、稳定性等）。
   - 未发布开源实现或原型系统。

2. **模式实现可能带来额外开销**
   - 如 `Reflector` 和 `Controller` 增加推理步骤，可能导致延迟上升或成本增加。
   - 论文承认需进一步研究其计算代价与收益的权衡。

3. **未处理大规模社会性影响**
   - 如群体智能涌现行为、法律责任归属等问题仍属开放课题。

---

### 🔮 未来工作方向

1. **开展定量基准测试**
   - 构建 benchmark suite 来衡量不同 ADP 组合对 agent 性能的影响。
   - 探索自动化 pattern selection 与 composition 方法。

2. **探索动态架构演化**
   - 研究 agent 是否可根据任务动态激活/禁用某些子系统或模式。

3. **与 Formal Verification 结合**
   - 将 `Controller` 等模式与形式化方法结合，确保安全约束始终满足。

4. **扩展至具身智能 (embodied agents) 与机器人系统**
   - 验证该框架在物理世界中的适用性。

---

## ✅ 总结

| 项目 | 内容 |
|------|------|
| **论文定位** | 提出一种 **原则性、系统化** 的 agentic AI 工程方法论 |
| **核心贡献** | ① 五子系统架构；② 12个结构化 ADPs；③ 问题-模式映射体系 |
| **方法特点** | 理论驱动、模块化、可组合、兼容 GoF 设计模式 |
| **实验形式** | 定性案例分析（以 ReAct 为例） |
| **是否开源/可复现** | 否（目前仅理论框架） |
| **适用人群** | AI 架构师、agent 系统开发者、研究者 |

> 💡 **一句话总结**：  
> 本文将 agentic AI 从“艺术性拼凑”推向“工程化建造”，提供了一套类似建筑设计蓝图的 **系统论框架 + 可复用构件库（ADPs）**，为构建更可靠、可解释、可持续进化的智能代理系统奠定了基础。

</details>

---

### 12. [Optimizing Conversational Quality in Spoken Dialogue Systems with Reinforcement Learning from AI Feedback](https://arxiv.org/abs/2601.19063)

**Authors**: Siddhant Arora, Jinchuan Tian, Jiatong Shi, Hayato Futami, Yosuke Kashiwagi, Emiru Tsunoo, Shinji Watanabe  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19063v1  

#### Abstract
Reinforcement learning from human or AI feedback (RLHF/RLAIF) for speech-in/speech-out dialogue systems (SDS) remains underexplored, with prior work largely limited to single semantic rewards applied at the utterance level. Such setups overlook the multi-dimensional and multi-modal nature of convers...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Optimizing Conversational Quality in Spoken Dialogue Systems with Reinforcement Learning from AI Feedback

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在 **Spoken Dialogue Systems (SDS)** 中，基于人类或AI反馈的强化学习（**RLHF/RLAIF**）研究大多局限于单一语义奖励（如事实性、连贯性），且仅在完整话语（utterance-level）上进行优化。这导致以下三个关键问题未被充分解决：

1. **多维度质量缺失**：真实对话质量是多维的，包括语义连贯性、音频自然度（audio naturalness）、可懂度（intelligibility）、情感一致性（emotion alignment）和轮次转换行为（turn-taking），但现有方法只关注语义。
2. **全双工系统不兼容**：现代SDS支持**增量式、块状解码**（blockwise decoding）的**duplex模型**，而传统RLHF依赖于整句反馈，无法指导生成过程中的中间决策。
3. **缺乏音频层面奖励**：尽管TTS领域已有基于MOS的偏好学习，但在端到端SDS中尚未系统整合音频质量奖励。

---

### 🚀 提出的新方法与创新点

本文提出了首个面向语音输入/输出SDS的**多奖励RLAIF框架**（multi-reward RLAIF），其核心创新如下：

#### （1）**多奖励DPO框架设计**
- 构建四个独立的偏好数据集，分别针对：
  - **Semantic Quality**（语义质量）
  - **Audio Quality**（音频自然度，使用UTMOS）
  - **Intelligibility**（可懂度，基于WER）
  - **Emotion Consistency**（情感一致性，使用Emo2vec）
- 在训练时联合采样这些数据，在统一的**DPO目标函数**下进行优化。

#### （2）**适用于全双工系统的流式偏好学习**
- 针对**blockwise duplex SDS**（如SCoT模型）提出“**turn-level preference sampling + blockwise log-prob aggregation**”机制：
  - 虽然偏好标签仍为整句级别，
  - 但通过将响应切分为固定大小的块（block），并将每个块的生成概率累加作为整体似然，
  - 使得DPO可以反向传播影响每一个生成块的决策。

#### （3）发布首个大规模多奖励DPO数据集
- 包含 **165.7K** 高质量偏好对，按类型分布如下：
  - Semantic: 51.1K
  - Audio Quality: 32.0K
  - Intelligibility: 61.0K
  - Emotion: 21.6K
- 公开代码与训练细节，推动可复现研究。

---

### 🔍 相比现有方法的优势

| 维度 | 以往方法 | 本工作 |
|------|--------|--------|
| 奖励维度 | 单一语义或时序奖励 | 多维度联合优化（语义+音频+情感） |
| 模型适配性 | 仅适用于turn-by-turn模型 | 支持blockwise duplex流式模型 |
| 反馈粒度 | 完整utterance级反馈 | 利用utterance级反馈优化partial generation |
| 数据开放性 | 多数闭源或小规模 | 发布首个大规模多奖励DPO数据集 |

---

## 2. 核心实验方法和设置

### 📚 数据集

- **训练数据来源**：基于真实人-人对话构建偏好数据，主数据集为 **Switchboard**（约300小时电话对话）。
- **偏好数据构造方式**：
  - 使用CoT-E2E模型生成多个候选响应（text或speech）。
  - 利用自动评估器打分并筛选正负样本对用于DPO训练。

### ⚙️ 实验设置

#### 模型架构
- **Baseline模型**：
  - `Direct E2E`：标准端到端SDS
  - `Moshi`：7B参数双通道duplex模型
  - `Multi-turn CoT E2E`：带链式推理的强基线
  - `SCoT-Response`：块状全双工CoT模型
- 所有模型均以SpeechLM为基础（SmolLM2 1.7B初始化）

#### RLAIF训练配置
- 使用 **Direct Preference Optimization (DPO)** 进行post-training。
- 多种训练模式对比：
  - **Single-Reward RLAIF**：仅使用一种奖励信号训练
  - **Joint-Reward-v1**：联合语义 + 音频质量 + 可懂度
  - **Joint-Reward-v2**：联合语义 + 音频 + 可懂度 + 情感一致性

#### 训练细节（见Appendix A.4）
- 使用4块NVIDIA H200 GPU
- 优化器：AdamW，学习率6e-7，warmup cosine调度
- Batch size: ~3840秒音频 + 1.1M文本token
- DPO数据按99:1划分训练/验证集

---

### 📊 评估指标

| 类别 | 指标 | 工具/方法 |
|------|-----|----------|
| **语义质量** | ROUGE-L ↑, METEOR ↑, Perplexity ↓, AutoBLEU ↓ | Whisper + GPT-2 |
| | LLM Judge Score ↑（Qwen2.5-7B） | 自定义prompt评分 |
| | Win Rate ↑（vs no-RLAIF baseline） | 成对比较统计 |
| **音频质量** | UTMOS ↑（语音自然度估计） | UTMOS模型 |
| | WER ↓（ASR识别错误率） | Whisper-large转录 |
| **风格一致性** | Emotion Rank ↓（越低越好） | Emo2Vec提取情感表征 |
| **通用工具包** | VERSA toolkit | 统一评估语音、音频、音乐任务 |

---

## 3. 主要实验结果和性能指标

### 📈 表2：语义质量提升（Turn-by-Turn SDS）

| Model | ROUGE-L | Perplexity | LLM Judge | Win Rate |
|-------|---------|------------|-----------|----------|
| Multi-turn CoT E2E | 12.1 | 21.2 | 6.18 | — |
| + RLAIF (Single-Reward) | 11.9 | 19.9 | **6.33*** | 55.4% |
| + RLAIF (Joint-v1) | 11.8 | 19.6 | 6.29* | 52.6% |
| + RLAIF (Joint-v2) | 11.9 | 19.9 | **6.33*** | 54.4% |

> ✅ **关键发现**：
> - 单一语义RLAIF显著提升LLM评分（6.18→6.33, p<0.01）
> - 低质量响应比例下降28.5%（<5分占比从10.2%→7.1%）
> - AutoBLEU大幅降低（68.3→56.5），说明减少重复输出
> - 联合训练未损害语义表现，反而稳定增益

---

### 📉 表3：音频质量与可懂度提升

| Model | UTMOS ↑ | WER ↓ |
|-------|--------|-------|
| Multi-turn CoT E2E | 2.16 | 6.1 |
| + RLAIF (Single-Reward) | **3.06** | **3.3** |
| + RLAIF (Joint-v1) | 2.85 | **1.0** |
| + RLAIF (Joint-v2) | 2.85 | 1.7 |

> ✅ **关键发现**：
> - 单独音频奖励使UTMOS飙升（+0.9），接近Moshi水平
> - 单独可懂度奖励将WER从6.1降至3.3
> - **Joint-v1实现最佳平衡**：UTMOS达2.85，WER进一步降至**1.0**
> - Joint-v2因加入情感约束，轻微牺牲可懂度（WER升至1.7）

---

### 🎭 表4：情感一致性评估（Emotion Rank ↓）

| Model | Emotion Rank |
|-------|-------------|
| Multi-turn CoT E2E | 2.29 |
| + RLAIF (Single-Reward) | **1.98** |
| + RLAIF (Joint-v2) | 3.00 |

> ✅ **关键发现**：
> - 单独情感奖励有效提升风格一致性（Rank↓）
> - 但在联合训练中（Joint-v2），受强语义奖励压制，情感表达变弱（Rank↑）
> ➡️ 揭示了**多目标之间的潜在冲突**

---

### 🔁 表5：Duplex模型上的语义优化效果

| Model | ROUGE-L | Perplexity | LLM Judge |
|-------|---------|----------|-----------|
| SCoT-Response | 19.8 | 42.3 | 5.95 |
| + RLAIF | **23.1** | **25.0** | **6.00** |

> ✅ **关键发现**：
> - RLAIF成功迁移到**blockwise duplex模型**
> - 在ROUGE-L、PPL、LLM评分上全面超越基线
> - 验证了“utterance-level偏好 → blockwise生成”的可行性

---

### ✅ 消融实验结论（来自正文分析）

| 设置 | 结果 |
|------|------|
| 文本+语音联合DPO vs 仅文本DPO | 后者更稳定有效，“text-only DPO”最优 |
| 加入SFT辅助损失 | 无额外收益 |
| 不同reward组合策略 | dataset-level concatenation有效，但缺乏显式权衡机制 |

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **单奖励RLAIF具有高度针对性**：
   - 语义奖励 → 提升连贯性与事实性
   - 音频奖励 → 显著改善UTMOS与WER
   - 情感奖励 → 增强说话人风格一致性

2. **多奖励联合训练带来综合增益**：
   - Joint-v1（语义+音频+可懂度）在多个维度一致提升
   - 尤其在**音频自然度与可懂度**上取得突破（WER=1.0）
   - 证明了多维对齐的必要性和有效性

3. **存在内在目标冲突**：
   - 强语义奖励可能导致回应“过于安全”或“泛化”，抑制情感表达（Emotion Rank恶化）
   - 表明需要更智能的**reward balancing机制**

4. **框架具备良好扩展性**：
   - 成功应用于turn-by-turn与blockwise duplex两类架构
   - 无需修改模型结构即可实现流式偏好优化

---

### ⚠️ 局限性（Limitations）

1. **依赖自动评估器构建偏好数据**：
   - 如LLM judge、UTMOS、Emo2vec等可能引入偏差或噪声
   - 缺乏真实human-in-the-loop偏好标注

2. **reward融合方式较简单**：
   - 当前采用dataset-level拼接，未建模reward间交互或动态权重
   - 可能导致次优平衡（如情感退化）

3. **语言与场景限制**：
   - 实验集中在英语Switchboard数据集
   - 尚未验证在多语言、跨文化或复杂任务型对话中的有效性

---

### 🔮 未来工作方向

1. **引入人类偏好数据闭环**：
   - 构建human-labeled multi-reward preference dataset
   - 探索human-AI混合反馈机制

2. **开发动态reward weighting机制**：
   - 基于上下文自适应调整各reward重要性
   - 引入multi-objective RL算法（如Pareto优化）

3. **拓展至多模态与多语言场景**：
   - 将框架推广至视觉-语音对话系统（VSDS）
   - 构建跨语言多奖励DPO数据集

4. **探索在线RL与持续对齐**：
   - 结合ORISE类方法实现实时turn-taking优化
   - 支持部署后持续从用户交互中学习

---

> 💡 **总体评价**：  
> 该论文首次系统地将**多维度、多模态的对话质量对齐**纳入RLAIF框架，并成功适配到**全双工流式SDS**中，标志着从“仅优化语义”迈向“整体对话体验优化”的重要一步。发布的**多奖励DPO数据集**将成为推动语音对话系统对齐研究的关键资源。

</details>

---

### 13. [MetaGen: Self-Evolving Roles and Topologies for Multi-Agent LLM Reasoning](https://arxiv.org/abs/2601.19290)

**Authors**: Yimeng Wang, Jiaxing Zhao, Hongbin Xie, Hexing Ma, Yuzhen Lei, Shuangxue Liu, Xuan Song, Zichen Zhang, Haoran Zhang  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19290v1  

#### Abstract
Large language models are increasingly deployed as multi-agent systems, where specialized roles communicate and collaborate through structured interactions to solve complex tasks that often exceed the capacity of a single agent. However, most existing systems still rely on a fixed role library and a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MetaGen: Self-Evolving Roles and Topologies for Multi-Agent LLM Reasoning 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Multi-Agent Systems (MAS)** 在基于 LLM 的复杂任务推理中存在以下三大瓶颈：

- **Task Mismatch**：固定的角色池（如 planner/solver/verifier）难以适应多样化的任务需求，导致角色与任务不匹配。
- **Structural Closure**：协作拓扑（topology）在推理开始后即被冻结（execution-frozen），无法根据运行时反馈动态调整。
- **High Cost**：手动设计角色提示和通信结构成本高昂，且缺乏可复用性和自适应能力。

这些问题限制了系统的灵活性、鲁棒性和效率。

---

### ✅ 提出了什么新方法或新思路

作者提出 **MetaGen**，一个 **training-free** 的多智能体框架，能够在 **inference time** 动态生成和演化角色与协作拓扑。

#### 核心创新点包括：

- **Dynamic Role Generation & Revision**  
  引入 **Architect Agent**，根据查询（query）自动生成候选角色，并通过 **schema 合法性检查** 和 **语义多样性门控（diversity gating）** 构建可控的动态角色池（dynamic role pool）。

- **Self-Evolving Topology**  
  不再使用固定的链式、星型或全连接图，而是构建一个可迭代演化的 **DAG（Directed Acyclic Graph）**。系统在执行过程中根据轻量级反馈信号（如测试失败、格式错误）动态更新角色提示（prompt rewrite）和边连接（edge exploration）。

- **Inference-Time Evolution Loop**  
  支持两种层次的演化：
  - **Intra-task Evolution**：单个任务内部的实时优化（prompt rewrite + edge update）
  - **Inter-task Evolution**：跨任务积累经验，更新选择先验（priors）并固化验证有效的角色到角色缓存（Role Cache）

- **Training-Free & Auditable**  
  所有优化均在推理阶段完成，无需微调 backbone LLM 权重。同时完整记录角色生成、结构修改和反馈触发过程，提升可解释性与可复现性。

---

### ✅ 相比现有方法的优势

| 维度 | 传统方法 | MetaGen |
|------|--------|--------|
| 角色管理 | 固定角色库（Fixed Role Pool） | 动态生成 + 查询条件化角色 |
| 拓扑结构 | 预设或训练后冻结（Execution-Frozen） | 推理时自我演化（Self-Evolving） |
| 成本控制 | 依赖人工工程 | 自动化 + 成本感知停止机制 |
| 可扩展性 | 冷启动差 | 跨任务积累先验与角色模板 |
| 是否需要训练 | 多数 topology designer 需要训练 | 完全 training-free |

> ✅ **MetaGen 实现了“无需训练即可实现角色与结构双重自适应”的突破**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

涵盖五类典型复杂推理任务：

| 数据集 | 任务类型 | 评估指标 |
|-------|--------|---------|
| **GSM8K** | 多步数学推理 | Exact Match Accuracy |
| **HumanEval** | 代码生成 | Pass@1 |
| **MMLU** | 广泛知识与推理 | Classification Accuracy |
| **AQuA** | 代数文字题 | Exact Match Accuracy |
| **MNLI** | 自然语言推断 | Classification Accuracy |

---

### ⚙️ 实验设置

- **Backbone Model**：统一使用 **DeepSeek-V3**，以隔离模型差异影响。
- **Semantic Encoder**：`SentenceTransformer all-MiniLM-L6-v2` 用于计算角色语义距离。
- **Architect 设置**：每实例生成 3 个候选角色，Top-K=2 进行角色选择。
- **探索策略**：ε-greedy（ε=0.15），奖励函数为 $ R = \mathbb{I}(pass) - \lambda_{cost} \cdot C_{token} $，其中 $\lambda_{cost}=0.001$。
- **评估方式**：三次独立运行取平均值。

---

### 🔁 基线方法对比

分为三类进行比较：

#### （1）Single-Agent Prompting
- Vanilla
- CoT (Zero-shot / Few-shot)
- Self-Consistency (SC)

#### （2）Fixed-Topology Multi-Agent
- Chain, Star, Tree, Complete Graph, Random Graph
- LLM-Debate

#### （3）Automated Topology Designers
- **GPTSwarm**, **AFlow**, **G-Designer**, **ARG-Designer**

> 特别关注是否支持：
> - **Dynamic Role Pool (Dyn.)**
> - **Training-Free (T-free)**
> - **Inference-Time Evolution (Evol.)**

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（Table 1）

| Method | GSM8K | HumanEval | MMLU | AQuA | MNLI | **Average** |
|--------|-------|-----------|------|------|------|------------|
| G-Designer (SOTA) | 96.3 | 94.2 | 93.5 | 89.0 | — | 93.3 |
| **MetaGen (Ours)** | **96.4** | **95.1** | **93.5** | **95.7** | **94.8** | **95.1** |

✅ **MetaGen 在所有可比任务上全面超越最强基线 G-Designer，平均准确率提升 1.8%**。

- 在 **AQuA** 上提升 **+6.7pp**（从 89.0 到 95.7），显示其在多步推理中的显著优势。
- 在 **MNLI** 上达到 **94.8**，远超 CoT few-shot 的 85.4（+9.4pp）。
- 在 **HumanEval** 上达 **95.1**，优于 G-Designer 的 94.2。

---

### 💰 成本效率分析（Table 2）

| Method | #Inference Token | #Overall Token |
|--------|------------------|----------------|
| Complete Graph | 9.8×10⁶ | 9.8×10⁶ |
| G-Designer | 8.2×10⁵ | 8.5×10⁵ |
| **MetaGen** | **1.2×10⁵** | **1.2×10⁵** |

✅ **MetaGen 推理 token 消耗仅为 G-Designer 的 ~15%，较 Complete Graph 降低 87.8%**。

> 实现了 **更高精度 + 更低成本** 的帕累托前沿突破。

---

### 🧪 消融实验结果（Table 4）

| Variant | HumanEval ↓ | MMLU ↓ |
|--------|-------------|--------|
| Full MetaGen | 95.1 | 93.5 |
| w/o Role Generation | 92.1 (**−3.0**) | 91.1 (**−2.4**) |
| w/o Learned Policy | 93.9 (−1.2) | 92.8 (−0.7) |
| w/o Intra-task Evolution | 92.7 (−2.4) | 91.7 (−1.8) |
| w/o Cross-instance Memory | 92.7 (−2.4) | 92.6 (−0.9) |

📌 **关键发现**：
- **Role Generation 最重要**：移除后性能下降最大，说明查询条件化角色生成是核心驱动力。
- **Intra-task Evolution 显著提升质量**：允许运行时修正错误路径。
- **Cross-instance Memory 提升冷启动表现**：通过角色复用加速后续任务。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **角色与拓扑应作为推理时可编辑对象**  
   将 role specifications 和 communication topology 视为可在 inference time 编辑的一等实体，能显著提升适应性。

2. **无需训练也能实现高效自组织协作**  
   MetaGen 完全无需更新 backbone LLM 参数，仅通过轻量级反馈驱动 prompt 和 graph 调整，即可实现 superior 性能。

3. **动态演化显著增强鲁棒性与泛化能力**
   - 在非平稳任务流中（MMLU → MNLI → HumanEval），MetaGen 整体准确率 **92.7% vs Frozen 90.0%**，且 token 更少。
   - 分布偏移后的冷启动阶段，MetaGen 准确率高出 5–10 个百分点。
   - 对噪声节点和边具有强鲁棒性，性能随扰动增加缓慢下降。

4. **人工提示工程成本大幅降低**  
   如 Figure 3 所示，在相同手动提示预算下，MetaGen 性能远超人工设计；甚至在极低预算下仍接近饱和性能。

---

### ⚠️ 方法的局限性

- **依赖 Architect Agent 的生成质量**：若 Architect 生成无效或冗余角色，可能引入噪声。
- **语义编码器限制**：当前使用轻量级 SentenceTransformer，对细粒度语义区分能力有限。
- **未处理并发或异步交互**：目前假设同步执行 DAG，尚未支持更复杂的并行调度。
- **角色缓存增长不可控**：长期运行可能导致角色库膨胀，需引入老化机制。

---

### 🔮 未来工作方向

1. **引入更强的 meta-controller**：让 Architect 具备主动探索与规划能力，而非被动响应反馈。
2. **支持异步与循环图结构**：拓展至更复杂的协作模式（如 feedback loops）。
3. **自动化角色压缩与去重**：维护精简高效的 Role Library。
4. **应用于真实世界场景**：如软件开发流水线、科研协作、城市规划等 long-horizon 复杂任务。

---

## ✅ 总结

**MetaGen 开辟了一条全新的路径：通过 inference-time 对文本级角色与离散拓扑的联合优化，构建可扩展、自适应、低成本的多智能体系统**。它证明了无需训练即可实现高性能协作演化，为未来 **scalable and adaptive MAS** 提供了实用范式。

</details>

---

### 14. [Modular Foundation Model Inference at the Edge: Network-Aware Microservice Optimization](https://arxiv.org/abs/2601.19563)

**Authors**: Juan Zhu, Zixin Wang, Shenghui Song, Jun Zhang, Khaled Ben Letaief  
**Category**: cs.DC  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19563v1  

#### Abstract
Foundation models (FMs) unlock unprecedented multimodal and multitask intelligence, yet their cloud-centric deployment precludes real-time responsiveness and compromises user privacy. Meanwhile, monolithic execution at the edge remains infeasible under stringent resource limits and uncertain network...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Modular Foundation Model Inference at the Edge: Network-Aware Microservice Optimization**

---

## 1. **论文的主要贡献和创新点**

### **解决的问题**
- **边缘端部署 Foundation Models (FMs)** 存在两大挑战：
  1. **资源受限**：FMs 参数量大，传统 **monolithic 架构**无法在边缘设备上运行。
  2. **动态不确定性**：任务到达具有时空波动性，网络状态（如无线信道）和资源竞争导致 **latency 不确定性强**，难以保障 QoS。
- 现有微服务（MS）部署方法通常将所有 MS 视为同质化组件，忽略了 **core MS 与 light MS 的功能不对称性**，导致资源匹配粗粒度、QoS 保障能力弱。

### **提出的新方法与新思路**
本文提出一种 **两层（two-tier）微服务部署框架**，充分利用 FM 推理流程中 **core 与 light MS 的功能差异**：

#### **主要创新点：**
- ✅ **混合部署策略（Hybrid Deployment Strategy）**：
  - **Core MSs（重型、有状态）**：静态部署，基于长期负载统计进行 **网络感知的整数规划优化**，形成可靠计算主干。
  - **Light MSs（轻量、无状态）**：动态在线部署，由低复杂度控制器实时调整实例数量和平行度（parallelism），适应短期波动。

- ✅ **面向故障容忍的静态部署模型**：
  - 构建一个 **稀疏约束的整数规划（sparsity-constrained integer program）**，联合优化部署成本与 **统计 QoS 分数（QoS score）**。
  - 引入辅助变量和多样性约束（diversity constraints），防止服务集中于单点，提升系统鲁棒性。

- ✅ **基于有效容量理论的动态控制机制**：
  - 首次将 **Effective Capacity Theory** 引入 MS 部署，建立 **parallelism level 与统计延迟边界之间的映射关系**，实现对尾部延迟的概率性保证。
  - 结合 **Lyapunov optimization 框架**，设计低复杂度在线算法（贪心启发式），在每时隙最小化 drift-plus-penalty 表达式，平衡成本与 QoS。

### **相比现有方法的优势**
| 方面 | 本文方法 | 传统方法 |
|------|--------|---------|
| **部署粒度** | 区分 core/light MS，细粒度资源匹配 | 同质化处理，忽略功能差异 |
| **QoS 保障** | 基于有效容量提供 **概率性延迟保证** | 平均延迟建模，忽视尾部延迟 |
| **动态适应性** | 在线控制器响应实时负载变化 | 静态或反应式调度，滞后性强 |
| **鲁棒性** | 显式优化部署多样性，抗单点故障 | 容易出现热点或集中部署 |

---

## 2. **核心实验方法和设置**

### **实验环境与拓扑**
- 模拟一个异构边缘网络，包含：
  - **Edge Devices (EDs)** 和 **Edge Servers (ESs)**，资源容量不同（CPU, RAM, GPU, VRAM）。
  - 节点间通过通信链路连接，带宽与时延受无线信道影响（Nakagami 分布建模 SNR）。
- 参考图示：Fig. 2 展示了包含 6 个 ED 和 3 个 ES 的网络拓扑。

### **FM 应用模型**
- 设计一个典型的 **多模态 FM 推理应用**，包含：
  - **4 种任务类型**（如跨模态理解、问答、分类、驾驶决策等）。
  - **6 个 Core MSs**（如 Transformer 编码器、视觉骨干）。
  - **9 个 Light MSs**（如预处理、后处理模块）。
- 任务以 DAG 形式组织，依赖关系见 Fig. 1。

### **数据集与参数设置**
- **任务到达**：服从泊松过程 $ \text{Poisson}([0.15, 1.5]) $ /ms。
- **输入大小 $ A_n $**：[0.5, 4] MB。
- **处理速率 $ f_m $**：
  - Core MS：固定值（8–32 MB/ms）。
  - Light MS：服从 Gamma 分布，模拟资源争用下的随机性。
- **延迟约束 $ D_n $**：[50, 100] ms。
- **无线信道 SNR $ \gamma_u $**：Nakagami 分布建模。
- **仿真工具**：自定义事件驱动模拟器，采样多次运行取平均。

### **评估指标**
| 指标 | 描述 |
|------|------|
| **On-time Task Completion Rate** | 在截止时间内完成的任务比例（核心 QoS 指标） |
| **Total System Cost** | 包括部署、维护、并行化开销的综合成本 |
| **Cost-QoS Trade-off** | 成本与完成率之间的平衡能力 |
| **Scalability** | 在负载增加（1.0× → 2.0×）下的性能稳定性 |

### **基线方法对比**
1. **LBRR (Least-Loaded Round Robin)**：
   - 将服务部署到负载最低节点，任务轮询分配。
   - 忽略 QoS，仅关注负载均衡。

2. **GA (Genetic Algorithm)**：
   - 使用遗传算法联合优化成本与 QoS 违规率。
   - 元启发式搜索，计算开销高，收敛不稳定。

3. **PropAvg (Ablation of Proposed Method)**：
   - 本文方法的消融版本，**用平均处理延迟代替有效容量模型**。
   - 用于验证 **tail latency modeling 的重要性**。

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**
- **平均准时完成率（Average On-time Completion Rate）**：
  - **本文方法：超过 84%**
  - PropAvg：约 75%
  - GA：波动较大，平均 ~70%
  - LBRR：低于 60%

- **系统总成本**：
  - 本文方法成本适中，显著低于 GA（因后者频繁过度部署）。
  - PropAvg 成本略低，但 QoS 下降明显。

### **与基线方法对比结果（Fig. 3）**
- **Violin Plot 显示分布特性**：
  - **本文方法**：完成率分布 **尖锐且集中于高位（>84%）**，成本分布紧凑，表明 **稳定高效**。
  - **GA**：分布宽泛，结果波动大，说明元启发式在随机环境中难以收敛。
  - **PropAvg**：完成率分布出现 **长尾（low completion <60%）**，反映其在高负载下频繁违反 QoS。
  - **LBRR**：低成本但低性能，适合非关键场景。

### **消融实验结果（Fig. 4）——负载可扩展性测试**
在系统负载从 1.0× 增加到 2.0× 时的表现：

| 方法 | 准时完成率趋势 | 成本增长 | 总/准时完成率差距 |
|------|----------------|--------|--------------------|
| **Ours** | 缓慢下降，保持 >75% @2.0× | 渐进上升，控制良好 | 差距小，QoS 稳定 |
| **PropAvg** | 急剧下降至 <60% @2.0× | 成本更低但仍失控 | 差距大幅扩大，大量任务超时 |

> 🔍 **关键发现**：PropAvg 虽节省成本，但在高负载下 **无法应对尾部延迟爆发**，导致 QoS 崩溃；而本文方法通过 **有效容量建模提前规避风险**，展现出更强鲁棒性。

---

## 4. **关键结论和发现**

### **主要发现**
1. ✅ **Core-Light 功能分离是边缘 FM 部署的关键**：
   - 利用两者启动时间、容错性、资源需求的差异，实施 **静态+动态混合部署** 是实现 QoS 与成本平衡的有效路径。

2. ✅ **统计 QoS 建模优于平均建模**：
   - 使用 **Effective Capacity Theory** 对 light MS 的处理延迟进行概率建模，能更准确预测尾部行为，避免“平均良好但偶尔崩溃”的问题。

3. ✅ **多样性部署增强系统鲁棒性**：
   - 显式引入部署多样性约束（C6），防止核心服务集中在单一节点，提升了容错能力和负载均衡潜力。

4. ✅ **Lyapunov + 贪心策略实现实时可行性**：
   - 所提在线算法复杂度低（$ O(M|J||M|) $），可在毫秒级决策窗口内运行，适用于真实边缘环境。

### **方法的局限性**
- ❗ **依赖准确的概率分布估计**：需要预先掌握任务到达、信道状态、处理速率的分布特征，在快速变化环境中可能需持续学习更新。
- ❗ **未考虑模型精度损失**：MS 拆分可能导致推理精度下降，本文聚焦部署优化，未涉及 accuracy-aware 决策。
- ❗ **贪心算法非全局最优**：虽然高效，但仍可能陷入局部最优，尤其在高度耦合的服务依赖图中。

### **未来工作方向**
- 🔮 **结合 Learning-based Solver**：使用 GNN 或 RL 替代贪心策略，提升在线决策质量。
- 🔮 **Accuracy-Aware Optimization**：将模型拆分粒度、量化策略纳入联合优化框架。
- 🔮 **跨层协同优化**：联合调度通信资源（带宽分配）、计算资源（vCPU/vGPU）与部署策略。
- 🔮 **向 6G Space-Air-Ground Networks 扩展**：应用于更复杂的空天地一体化网络中的 FM 推理。

---

> 📌 **总结一句话**：  
> 本文通过 **区分 core 与 light 微服务的功能特性**，提出了一种 **网络感知、统计 QoS 驱动的两层部署框架**，在保证 **>84% 准时完成率**的同时实现了 **成本可控与强健可扩展性**，为边缘侧高效、可靠地运行 Foundation Models 提供了新范式。

</details>

---

### 15. [Variational Quantum Circuit-Based Reinforcement Learning for Dynamic Portfolio Optimization](https://arxiv.org/abs/2601.18811)

**Authors**: Vincent Gurgul, Ying Chen, Stefan Lessmann  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.18811v1  

#### Abstract
This paper presents a Quantum Reinforcement Learning (QRL) solution to the dynamic portfolio optimization problem based on Variational Quantum Circuits. The implemented QRL approaches are quantum analogues of the classical neural-network-based Deep Deterministic Policy Gradient and Deep Q-Network al...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
本文致力于解决**动态投资组合优化（Dynamic Portfolio Optimization）**中的序列决策难题。传统方法如均值-方差优化（Mean-Variance Optimization, MVO）是静态的，无法适应市场状态随时间变化的动态特性。尽管经典深度强化学习（Deep RL）在该领域取得进展，但其模型通常参数量大、训练效率低，且难以在高维非平稳环境中保持鲁棒性。

此外，现有的量子计算应用于投资组合优化的研究大多局限于**静态的 QUBO（Quadratic Unconstrained Binary Optimization）问题求解**，缺乏对连续、动态策略学习的支持。

### **提出的新方法与新思路**
本文提出了一个基于**变分量子电路（Variational Quantum Circuits, VQCs）**的全新**量子强化学习（Quantum Reinforcement Learning, QRL）框架**，用于动态投资组合优化。具体创新包括：

- **首次将 QRL 应用于动态投资组合优化任务**，实现了端到端的量子策略学习。
- 设计了**完全量子化的 Actor-Critic 架构**，其中 Actor 和 Critic 均由 VQC 实现，而非混合模型。
- 采用**振幅编码（Amplitude Encoding）** 将高维金融时序数据压缩为低维量子态，实现高效的信息嵌入。
- 实现了量子版本的 **Deep Deterministic Policy Gradient (DDPG)** 和 **Deep Q-Network (DQN)** 算法，分别称为 **Quantum DDPG** 和 **Quantum DQN**。
- 引入**扩展特征映射（extended feature map）** 到振幅编码中，以增强表达能力并缓解“贫瘠高原”（barren plateaus）问题。

### **相比现有方法的优势**
- **极高的参数效率**：仅用 **30–60 个可训练参数** 的量子模型，在风险调整后收益上即可媲美甚至超越拥有 **数万至十六万个参数** 的经典 Deep RL 模型。
- **更强的鲁棒性**：量子代理在不同市场周期下的表现波动更小，表明其策略更具稳定性。
- **理论上的可扩展性优势**：利用量子叠加与纠缠，VQCs 在理论上具备指数级的状态空间探索潜力，适合复杂、高维、非平稳环境。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **数据来源**：真实世界金融数据。
- **资产数量**：15 种多样化资产，涵盖股票（如 AAPL, MSFT）、ETF（如 SPY, QQQ）、债券（如 TLT, LQD）、大宗商品（如 GLD, USO）等。
- **时间跨度**：从 2011 年 8 月 至 2025 年 9 月，共 **5049 天** 的日收盘价。
- **收益率计算**：使用对数收益率。

### **实验设置**
- **状态（State）**：包含两部分：
  - 过去 30 天的归一化价格窗口（lookback window）。
  - 未来 7 天的价格预测（forecast window），通过 Auto ARIMA 模型生成。
- **动作（Action）**：连续的 15 维投资组合权重向量 $ \mathbf{w}_t \in \mathbb{R}^{15} $，允许卖空（short-selling）。
- **奖励函数（Reward）**：结合平均回报与风险调整波动率，形式为：
  $$
  r_t = \mathbf{w}_{t}^\top \mu_{t-L:t+F+1} - \lambda \mathbf{w}_{t}^\top \Sigma_{t-L:t+F+1} \mathbf{w}_{t}
  $$
  其中 $ \lambda $ 为风险偏好参数。
- **再平衡频率**：每 30 天进行一次策略执行与再平衡。
- **交易成本**：设定为每次交易 **0.15%**。

### **评估指标**
- **主指标**：测试集上的 **Sharpe Ratio**（夏普比率），经 7 折滚动时间序列交叉验证（increasing rolling-window time-series cross-validation）取平均。
- **辅助指标**：
  - Sharpe Ratio 的标准差（衡量跨市场周期的稳定性）。
  - 执行时间（inference latency）。
  - 参数数量（parameter count）。

### **基线方法对比**
| 类型 | 方法 |
|------|------|
| **静态基准** | - Equal Weights（等权组合）<br>- Mean-Variance Optimization (MVO) |
| **经典 Deep RL** | - DDPG（13.5k, 27k, 160k 参数）<br>- DQN（13.5k, 27k, 160k 参数） |
| **本文提出方法** | - Quantum DDPG（30, 60 参数）<br>- Quantum DQN（30, 60 参数） |

所有模型在相同环境、状态表示和奖励函数下进行公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 5.1）**

| 模型 | Sharpe Ratio (mean) | Sharpe Ratio (std dev) | 参数量 | 执行时间 |
|------|---------------------|------------------------|--------|----------|
| Equal Weights | 0.4375 | 0.6558 | – | <1s |
| MVO | 0.5919 | 0.9170 | – | <1s |
| Classical DDPG (160k) | **0.7926** | 0.6276 | 160,000 | 14s |
| Classical DQN (160k) | **0.8237** | 0.5367 | 160,000 | 27s |
| **Quantum DDPG (60)** | **0.7281** | **0.7395** | **60** | <1s (23min)* |
| **Quantum DQN (60)** | **0.6537** | 0.6155 | **60** | <1s (43min)* |

> *注：括号内时间为云平台总耗时（含排队、初始化等开销），<1s 为纯电路执行时间。*

### **与基线方法的对比结果**
- **参数效率碾压**：
  - 仅用 **60 个参数** 的 Quantum DDPG，其 Sharpe Ratio（0.7281）已接近甚至超过许多参数量小得多的经典模型，并显著优于 MVO。
  - Quantum DQN (60) 虽略低于最优经典模型，但仍远超同等参数规模的经典模型。
- **性能可比性**：
  - 最优量子模型（Quantum DDPG 60）的性能已达到经典大规模模型（160k 参数）的 **~88%–90% 水平**，而参数量仅为后者的 **0.0375%**。
- **鲁棒性优势**：
  - 量子代理的 Sharpe Ratio 标准差相对较低，尤其在市场剧烈波动时期表现出更强的一致性。

### **消融实验与分析**
- **参数量影响**：
  - Quantum DDPG 从 30 参数提升至 60 参数，Sharpe Ratio 显著上升（0.4179 → 0.7281），说明增加表达能力能有效提升性能。
  - 经典模型也呈现类似趋势，但需成倍增加参数才能获得边际提升。
- **架构差异**：
  - Quantum DDPG 整体表现优于 Quantum DQN，可能因其目标构建方式更适合连续控制任务。
- **编码方式作用**：
  - 使用扩展特征映射的振幅编码有助于缓解训练初期梯度消失问题，提升收敛速度与最终性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **QRL 在动态投资组合优化中具有理论竞争力**：
   - 基于 VQC 的 QRL 框架能够学习出高质量的自适应交易策略。
   - 量子代理在**风险调整后收益**方面可与最先进的经典 Deep RL 模型相媲美。

2. **极致的参数效率是核心优势**：
   - 量子模型凭借**振幅编码**与**纠缠诱导的表达力**，实现了远超经典网络的“参数-性能”比。
   - 这种高效性使其在资源受限或需要快速部署的场景中极具吸引力。

3. **当前硬件部署存在严重瓶颈**：
   - 尽管**单次电路执行极快（毫秒级）**，但由于**云量子计算系统的排队、初始化与同步开销**，实际端到端推理延迟高达 **23–43 分钟**。
   - 这使得当前 QRL **尚不具备实时交易的实用性**。

4. **未来潜力巨大**：
   - 若未来能实现专用 QPU 或降低云端延迟，QRL 的原生速度优势将得以释放。
   - 随着硬件成熟，QRL 可能成为处理复杂动态决策问题（如金融、供应链、机器人控制）的**主流范式**。

### **方法的局限性**
- **NISQ 时代限制**：受噪声、连通性与 qubit 数量制约，只能训练较浅的 VQC。
- **模拟器依赖训练**：本研究训练在无噪声模拟器上完成，未考虑真实硬件噪声对训练的影响。
- **规模有限**：仅测试了最多 15 个资产，更大规模组合尚未验证。
- **延迟问题突出**：云访问模式导致的高延迟严重阻碍了实际应用。

### **未来工作方向**
- 探索在真实 NISQ 设备上进行端到端训练（含噪声）。
- 设计更高效的量子编码与电路结构以进一步提升表达力。
- 研究专用量子计算系统（dedicated QPU）下的低延迟推理架构。
- 将该框架拓展至其他金融任务，如期权定价、风险管理、高频交易等。
- 开源代码已发布：[GitHub - VincentGurgul/qrl-dpo-public](https://github.com/VincentGurgul/qrl-dpo-public)

---

> **总结一句话**：  
> 本文证明了 **VQC-based QRL 在动态投资组合优化中具备卓越的参数效率与理论性能**，虽受限于当前云量子基础设施的高延迟而暂难实用，但其展现出的巨大潜力预示着它可能是未来复杂动态决策系统的理想解决方案。

</details>

---

### 16. [TinyTorch: Building Machine Learning Systems from First Principles](https://arxiv.org/abs/2601.19107)

**Authors**: Vijay Janapa Reddi  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19107v1  

#### Abstract
Machine learning systems engineering requires a deep understanding of framework internals. Yet most current education separates algorithms from systems. Students learn gradient descent without measuring memory usage, and attention mechanisms without profiling computational cost. This split leaves gr...

---

### 17. [Smoothing the Score Function for Generalization in Diffusion Models: An Optimization-based Explanation Framework](https://arxiv.org/abs/2601.19285)

**Authors**: Xinyu Zhou, Jiawei Zhang, Stephen J. Wright  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19285v1  

#### Abstract
Diffusion models achieve remarkable generation quality, yet face a fundamental challenge known as memorization, where generated samples can replicate training samples exactly. We develop a theoretical framework to explain this phenomenon by showing that the empirical score function (the score functi...

---

### 18. [LLM Driven Design of Continuous Optimization Problems with Controllable High-level Properties](https://arxiv.org/abs/2601.18846)

**Authors**: Urban Skvorc, Niki van Stein, Moritz Seiler, Britta Grimme, Thomas B\"ack, Heike Trautmann  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18846v1  

#### Abstract
Benchmarking in continuous black-box optimisation is hindered by the limited structural diversity of existing test suites such as BBOB. We explore whether large language models embedded in an evolutionary loop can be used to design optimisation problems with clearly defined high-level landscape char...

---

### 19. [Exploring Weaknesses in Function Call Models via Reinforcement Learning: An Adversarial Data Augmentation Approach](https://arxiv.org/abs/2601.19122)

**Authors**: Weiran Guo, Bing Bo, Shaoxiang Wu, Jingsheng Yang  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19122v1  

#### Abstract
Function call capabilities have become crucial for Large Language Models (LLMs), enabling them to interact more effectively with external tools and APIs. Existing methods for improving the function call capabilities of LLMs rely on data obtained either through manual annotation or automated generati...

---

### 20. [Balancing Sustainability And Performance: The Role Of Small-Scale Llms In Agentic Artificial Intelligence Systems](https://arxiv.org/abs/2601.19311)

**Authors**: Anh Khoa Ngo Ho, Martin Chauvin, Simon Gosset, Philippe Cordier, Boris Gamazaychikov  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19311v1  

#### Abstract
As large language models become integral to agentic artificial intelligence systems, their energy demands during inference may pose significant sustainability challenges. This study investigates whether deploying smaller-scale language models can reduce energy consumption without compromising respon...

---

### 21. [Algorithmic Prompt-Augmentation for Efficient LLM-Based Heuristic Design for A* Search](https://arxiv.org/abs/2601.19622)

**Authors**: Thomas B\"omer, Nico Koltermann, Max Disselnmeyer, Bastian Amberg, Anne Meyer  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19622v1  

#### Abstract
Heuristic functions are essential to the performance of tree search algorithms such as A*, where their accuracy and efficiency directly impact search outcomes. Traditionally, such heuristics are handcrafted, requiring significant expertise. Recent advances in large language models (LLMs) and evoluti...

---

### 22. [Identifying and Transferring Reasoning-Critical Neurons: Improving LLM Inference Reliability via Activation Steering](https://arxiv.org/abs/2601.19847)

**Authors**: Fangan Dong, Zuming Yan, Xuri Ge, Zhiwei Xu, Mengqi Zhang, Xuanang Chen, Ben He, Xin Xin, Zhumin Chen, Ying Zhou  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19847v1  

#### Abstract
Despite the strong reasoning capabilities of recent large language models (LLMs), achieving reliable performance on challenging tasks often requires post-training or computationally expensive sampling strategies, limiting their practical efficiency. In this work, we first show that a small subset of...

---

### 23. [GraIP: A Benchmarking Framework For Neural Graph Inverse Problems](https://arxiv.org/abs/2601.18917)

**Authors**: Semih Cant\"urk, Andrei Manolache, Arman Mielke, Chendi Qian, Antoine Siraudin, Christopher Morris, Mathias Niepert, Guy Wolf  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18917v1  

#### Abstract
A wide range of graph learning tasks, such as structure discovery, temporal graph analysis, and combinatorial optimization, focus on inferring graph structures from data, rather than making predictions on given graphs. However, the respective methods to solve such problems are often developed in an ...

---

### 24. [Towards Self-Optimizing Electron Microscope: Robust Tuning of Aberration Coefficients via Physics-Aware Multi-Objective Bayesian Optimization](https://arxiv.org/abs/2601.18972)

**Authors**: Utkarsh Pratiush, Austin Houston, Richard Liu, Gerd Duscher, Sergei Kalinin  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18972v1  

#### Abstract
Realizing high-throughput aberration-corrected Scanning Transmission Electron Microscopy (STEM) exploration of atomic structures requires rapid tuning of multipole probe correctors while compensating for the inevitable drift of the optical column. While automated alignment routines exist, convention...

---

### 25. [EPAS: Efficient Training with Progressive Activation Sharing](https://arxiv.org/abs/2601.19089)

**Authors**: Rezaul Karim, Maryam Dialameh, Yang Liu, Boxing Chen, Walid Ahmed  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19089v1  

#### Abstract
We present a novel method for Efficient training with Progressive Activation Sharing (EPAS). This method bridges progressive training paradigm with the phenomenon of redundant QK (or KV ) activations across deeper layers of transformers. EPAS gradually grows a sharing region during training by switc...

---

### 26. [Explainable Uncertainty Quantification for Wastewater Treatment Energy Prediction via Interval Type-2 Neuro-Fuzzy System](https://arxiv.org/abs/2601.18897)

**Authors**: Qusai Khaled, Bahjat Mallak, Uzay Kaymak, Laura Genga  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18897v1  

#### Abstract
Wastewater treatment plants consume 1-3% of global electricity, making accurate energy forecasting critical for operational optimization and sustainability. While machine learning models provide point predictions, they lack explainable uncertainty quantification essential for risk-aware decision-mak...

---

### 27. [Cross-Examination Framework: A Task-Agnostic Diagnostic for Information Fidelity in Text-to-Text Generation](https://arxiv.org/abs/2601.19350)

**Authors**: Tathagata Raha, Clement Christophe, Nada Saadi, Hamza A Javed, Marco AF Pimentel, Ronnie Rajan, Praveenkumar Kanithi  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.19350v1  

#### Abstract
Traditional metrics like BLEU and BERTScore fail to capture semantic fidelity in generative text-to-text tasks. We adapt the Cross-Examination Framework (CEF) for a reference-free, multi-dimensional evaluation by treating the source and candidate as independent knowledge bases. CEF generates verifia...

---

### 28. [Decompose-and-Formalise: Recursively Verifiable Natural Language Inference](https://arxiv.org/abs/2601.19605)

**Authors**: Xin Quan, Marco Valentino, Louise A. Dennis, Andr\'e Freitas  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.19605v1  

#### Abstract
Recent work has shown that integrating large language models (LLMs) with theorem provers (TPs) in neuro-symbolic pipelines helps with entailment verification and proof-guided refinement of explanations for natural language inference (NLI). However, scaling such refinement to naturalistic NLI remains...

---

### 29. [Latent Structural Similarity Networks for Unsupervised Discovery in Multivariate Time Series](https://arxiv.org/abs/2601.18803)

**Authors**: Olusegun Owoeye  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18803v1  

#### Abstract
This paper proposes a task-agnostic discovery layer for multivariate time series that constructs a relational hypothesis graph over entities without assuming linearity, stationarity, or a downstream objective. The method learns window-level sequence representations using an unsupervised sequence-to-...

---

### 30. [Attention-Enhanced Graph Filtering for False Data Injection Attack Detection and Localization](https://arxiv.org/abs/2601.18981)

**Authors**: Ruslan Abdulin, Mohammad Rasoul Narimani  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18981v1  

#### Abstract
The increasing deployment of Internet-of-Things (IoT)-enabled measurement devices in modern power systems has expanded the cyberattack surface of the grid. As a result, this critical infrastructure is increasingly exposed to cyberattacks, including false data injection attacks (FDIAs) that compromis...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
