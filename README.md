# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-24 05:57:56 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ActionFlow: A Pipelined Action Acceleration for Vision Language Models on Edge](https://arxiv.org/abs/2512.20276)

**Authors**: Yuntao Dai, Hang Gu, Teng Wang, Qianyu Cheng, Yifei Zheng, Zhiyong Qiu, Lei Gong, Wenqi Lou, Xuehai Zhou  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2512.20276v1  

#### Abstract
Vision-Language-Action (VLA) models have emerged as a unified paradigm for robotic perception and control, enabling emergent generalization and long-horizon task execution. However, their deployment in dynamic, real-world environments is severely hin dered by high inference latency. While smooth rob...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《ActionFlow: A Pipelined Action Acceleration for Vision Language Models on Edge》核心总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Vision-Language-Action (VLA) 模型在机器人感知与控制中展现出强大的泛化能力和长视野任务执行潜力，但其在边缘设备上的部署面临严重的**高推理延迟瓶颈**。由于依赖自回归解码（autoregressive decoding），当前 VLA 模型在边缘硬件上通常只能达到 3–5 FPS，远低于动态交互所需的 20–30 Hz 控制频率，导致机器人反应迟缓、无法实时响应环境变化。

此外，现有优化方法存在以下局限：
- **LLM 推理系统**（如 Continuous Batching）面向多用户服务器场景，不适用于单用户低延迟的机器人控制；
- **边缘端优化技术**（如量化、蒸馏）虽减小模型规模，但未改变解码阶段内存受限的本质；
- **算法级加速方法**（如并行解码、扩散模型）常需重新训练，可能影响精度或引入动作不连续等问题。

### **提出的新方法与思路**
本文提出了 **ActionFlow** —— 一种专为资源受限边缘平台设计的**系统级推理框架**，其核心思想是将 VLA 的推理过程重构为一个由“微请求”组成的宏流水线，并通过跨请求调度提升硬件利用率。

#### **主要创新点：**
1. **Cross-Request Pipelining 策略**  
   将每个 VLA 请求内部的 `Prefill` 和多个 `Decode` 阶段视为一个 K-stage 微请求流水线。ActionFlow 在时间维度上重叠 K 个连续请求的操作：将当前帧的 `Prefill` 与前几帧的 `Decode` 批量合并处理，从而消除流水线气泡，最大化 GPU 利用率。

2. **Cross-Request State Packed Forward 算子**  
   设计了一个融合算子，将多个独立的向量级 Decode GEMV 操作打包成一个矩阵级 GEMM 操作，显著提高算术强度（arithmetic intensity），使计算从 memory-bound 转向 compute-bound。

3. **Unified KV Ring Buffer**  
   引入统一的环形 KV 缓存结构，物理上连续存储所有活跃请求的历史 KV 状态，避免传统方法中频繁的数据拷贝和 CPU-GPU 同步开销，支持高效的 Variable-Length Attention（Varlen-Attention）操作。

4. **纯系统级优化，无需重训练**  
   ActionFlow 完全在系统层面实现加速，对原始模型参数、架构和训练流程无任何修改，正交于量化、推测解码等其他优化手段。

### **相比现有方法的优势**
| 维度 | ActionFlow | 现有方法 |
|------|-----------|---------|
| 是否需要重训练 | ❌ 不需要 | ✅ 多数需要（如并行解码） |
| 是否依赖外部批处理 | ❌ 单用户内构造批处理 | ✅ 依赖多用户请求 |
| 是否改变模型结构 | ❌ 保持原模型不变 | ✅ 常需结构调整 |
| 对 Decode 阶段效率提升 | ✅ 显著提升（算术强度↑） | ⚠️ 改善有限 |
| 可部署性 | ✅ 边缘设备友好 | ⚠️ 多为数据中心设计 |

---

## **2. 核心实验方法和设置**

### **使用的模型与平台**
- **模型**：OpenVLA-7B（主流开源 VLA 模型）
- **硬件平台**：
  - **NVIDIA Jetson AGX Orin (64GB)**：代表典型嵌入式边缘设备
  - **NVIDIA RTX 5090**：高性能边缘工作站（模拟高端边缘节点）

### **软件栈**
- PyTorch == 2.6.0
- Transformers == 4.49.0
- CUDA 12.6

### **评估指标**
- **FPS（Frames Per Second）**：核心吞吐量指标，衡量每秒可完成的推理帧数。
- **End-to-End 推理延迟（ms）**
- **Speedup**：相对于 Baseline 的加速比
- **Success Rate (%)**：在 LIBERO 基准上验证功能正确性，确保无精度损失。

### **对比基线方法**
| 方法 | 描述 |
|------|------|
| **Baseline** | 标准自回归推理，无系统级优化 |
| **Naive Pipe** | 实现了跨请求流水线，但使用动态 KV 缓存拼接和 CPU 协调批处理（无算子融合） |
| **ActionFlow (Ours)** | 完整实现 Cross-Request Pipelining + Packed Forward + Unified KV Ring Buffer |

---

## **3. 主要实验结果和性能指标**

### **端到端性能对比（Table 1）**

| Method       | Platform      | FPS     | Latency (ms) | Speedup |
|--------------|---------------|---------|---------------|---------|
| Baseline     | AGX Orin      | 1.25    | 803           | 1.00x   |
| Naive Pipe   | AGX Orin      | 2.70    | 373           | 2.16x   |
| **ActionFlow** | **AGX Orin**  | **3.20**| **313**       | **2.56x** |
| Baseline     | RTX 5090      | 7.62    | 131           | 1.00x   |
| Naive Pipe   | RTX 5090      | 15.60   | 64            | 2.04x   |
| **ActionFlow** | **RTX 5090**  | **19.45**| **51**        | **2.55x** |

> ✅ **结论**：ActionFlow 在两种平台上均实现约 **2.55x 的 FPS 提升**，且加速效果跨架构一致。

### **消融实验分析**
- **Naive Pipe vs. ActionFlow**：证明了 **Kernel Fusion 和 Unified KV Ring Buffer 的关键作用**。
  - 在 AGX Orin 上带来额外 **+18.5%** 性能增益；
  - 在 RTX 5090 上带来 **+24.7%** 增益；
- 主要收益来源：
  - 消除 CPU-GPU 同步气泡；
  - 减少内存分配与拷贝开销；
  - 提高 GEMM 计算密度。

### **敏感性分析（图5）**
- **不同 Decode 长度 K 下的表现**：
  - 当 K=32（长动作序列）时，在 RTX 5090 上 Baseline 性能下降至 2.36 FPS；
  - ActionFlow 仍维持 9.58 FPS，实现 **4.06× 加速**；
  - 在 AGX Orin 上最重负载下达到 **4.36× 加速**（1.31 vs. 0.30 FPS）；
- **对 Decode 长度鲁棒性强**：
  - Baseline 随 K 增加性能下降 72%；
  - ActionFlow 仅下降 25%，说明有效摊平了自回归串行开销。

### **功能正确性验证（Table 2）**

| Method       | Platform      | Spatial | Object | Goal | Long |
|--------------|---------------|--------|--------|------|------|
| OpenVLA      | RTX 5090      | 84.4%  | 73.8%  | 74.4%| 51.4%|
| ActionFlow   | RTX 5090      | 84.3%  | 71.2%  | 78.6%| 53.3%|
| ActionFlow   | AGX Orin      | 83.4%  | 68.8%  | 75.6%| 49.0%|

> ✅ **结论**：ActionFlow 在所有任务类别中成功率与 Baseline 基本持平，波动在统计误差范围内，**验证了其功能无损性（functionally lossless）**。

---

## **4. 关键结论和发现**

### **主要发现**
1. **VLA 推理瓶颈本质在于 Decode 阶段的低效内存访问模式**，而非单纯计算能力不足；
2. **即使在单用户场景下，也能通过“跨请求流水线”创造有效的批处理机会**，突破传统 batching 的限制；
3. **系统级优化可在不修改模型的前提下大幅加速 VLA 推理**，且与量化、推测解码等正交；
4. **ActionFlow 将 OpenVLA-7B 的推理速度提升 2.55×，使其首次接近实时控制所需频率（>3 FPS → ~3.2 FPS on Orin）**，为通用具身智能体在动态环境中部署铺平道路。

### **局限性**
1. **加速效果依赖于连续请求流**：若机器人控制中断或请求稀疏，流水线难以填满，性能会回落；
2. **固定动作长度假设**：目前假设每个请求生成固定长度的动作序列（K 步），对变长输出支持较弱；
3. **KV Ring Buffer 内存开销**：需维护 K 个历史请求的 KV 缓存，内存占用随 K 线性增长，在极端情况下可能成为瓶颈；
4. **尚未集成更高级优化**：如 Speculative Decoding 或 MOE 调度，未来可进一步叠加优化。

### **未来工作方向**
1. **支持变长动作序列的动态流水线调度机制**；
2. **结合 Speculative Decoding 构建混合加速方案**；
3. **扩展至多模态输入流（如视频、LiDAR）的持续推理场景**；
4. **在真实机器人平台上进行闭环控制测试**，验证实际操控稳定性与响应能力；
5. **探索与轻量化模型（如 TinyVLA、BitVLA）的协同部署策略**。

---

> 🔗 **项目主页**：https://anonymous.4open.science/r/ActionFlow-1D47  
> 📄 **原文链接**：[arXiv:2512.20276](https://arxiv.org/abs/2512.20276)

</details>

---

### 2. [MoE-DiffuSeq: Enhancing Long-Document Diffusion Models with Sparse Attention and Mixture of Experts](https://arxiv.org/abs/2512.20604)

**Authors**: Alexandros Christoforos, Chadbourne Davis  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.20604v1  

#### Abstract
We present MoE-DiffuSeq, a mixture of experts based framework for enhancing diffusion models in long document generation. Existing diffusion based text generation models, such as DiffuSeq, suffer from high computational cost and memory overhead when applied to extended sequences. To address these ch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-DiffuSeq 论文核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统扩散模型（如 **DiffuSeq**）在处理长文本生成任务时面临严重的**计算效率低下**和**内存消耗过高**问题，主要源于标准 Transformer 架构中自注意力机制的 $O(n^2)$ 时间复杂度。此外，大型语言模型（LLMs）通常受限于训练序列长度（如不超过 8,000 tokens），难以有效建模长上下文依赖。

### 提出的新方法与创新思路
本文提出 **MoE-DiffuSeq**，一种融合 **Mixture of Experts (MoE)** 与 **DiffuSeq** 框架，并引入 **稀疏注意力机制（Sparse Attention）** 和 **软吸收态（soft absorbing state）** 的新型长文本生成模型。其核心创新包括：

- **MoE + Diffusion 融合架构**：首次将 MoE 动态专家选择机制集成到扩散语言模型中，实现对不同文本片段按需激活最相关专家，提升模型容量与推理效率。
- **定制化稀疏注意力机制**：采用 **Longformer-style 滑动窗口注意力**，并结合 **膨胀滑窗（Dilated Sliding Window）** 扩展感受野，在不增加计算负担的前提下捕捉长距离依赖。
- **全局注意力机制**：为关键 token（如 `[CLS]`）保留全局注意力能力，确保整体语义一致性。
- **优化扩散过程设计**：
  - 引入 **软吸收态（soft absorbing state）** 改进离散状态建模；
  - 结合 **DPM-solver++** 加速采样过程，显著减少所需扩散步数；
  - 设计联合去噪策略与正则化损失函数以提高重建精度。

### 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **效率** | 显著降低训练和推理时间，尤其适用于超长序列（scientific papers, code repos, dialogues） |
| **可扩展性** | MoE 架构支持更大参数量而无需全激活，适合大规模部署 |
| **质量保持** | 在提升速度的同时，维持甚至超越基线模型的文本连贯性、语义准确性和多样性 |
| **灵活性** | 稀疏注意力 + MoE 的组合可在不同任务间灵活调整配置 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖多个具有挑战性的长文本生成场景：

| 数据集 | 任务类型 | 特点 |
|--------|---------|------|
| **Arxiv Abstract Dataset** | 科学文献摘要生成 | 长文本、高技术术语密度 |
| **HotpotQA** | 多跳问答（Multi-hop QA） | 需要跨文档推理与上下文整合 |
| **Commonsense Conversation Dataset** | 对话响应生成 | 注重常识理解与自然表达 |
| **Quora Question Pairs (QQP)** | 问句复述识别 | 测试语义等价判断能力 |

### 实验设置
- **模型结构**：
  - 12 层 Transformer，每层 12 个注意力头；
  - 集成 Longformer 稀疏注意力 + MoE 模块；
  - 每个 MoE 层含多个前馈网络专家，由门控函数动态路由。
- **训练策略**：
  - 分阶段增大窗口大小与序列长度；
  - 使用 2,048 步扩散过程，噪声调度为平方根形式；
  - 优化器与学习率未详述，但在 NVIDIA A100 GPU 上运行。
- **采样加速**：采用 **DPM-solver++** 减少推理阶段扩散步数。

### 评估指标
综合评价生成质量与语义准确性：

| 指标 | 描述 |
|------|------|
| **BLEU** | 衡量 n-gram 匹配精度，反映生成文本与参考文本的相似度 |
| **ROUGE (R1/R2/RL)** | 基于召回率的 n-gram 重叠度，常用于摘要任务 |
| **BERTScore** | 利用 BERT 上下文嵌入计算词级语义相似性，更贴近人类判断 |
| **Accuracy** | 分类任务（如 QQP）中的预测准确率 |
| **Inference Time / Novelty** | 推理延迟与生成文本新颖性（如 2-gram novelty） |

### 基线方法对比
- **DiffuSeq**：基础扩散语言模型，作为直接对照；
- **Longformer**：基于稀疏注意力的经典长文本编码器；
- **GPT-4**：当前主流大模型，提供高性能上限参考；
- 其他特定任务专用模型也参与比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 1–4）

#### Arxiv Abstract Dataset（ROUGE 指标）
| Model | R1 | R2 | RL |
|-------|----|----|-----|
| Longformer | 41.44 | 17.52 | 38.70 |
| DiffuSeq | 39.12 | 16.43 | 37.88 |
| **MoE-DiffuSeq** | **44.41** | **18.73** | **39.89** |

> ✅ 在科学摘要生成上全面领先，表明更强的语言结构理解和组织能力。

#### HotpotQA（多跳问答）
| Model | Answer EM/F1 | Support EM/F1 |
|-------|---------------|----------------|
| Longformer | 71.21 / 82.42 | 65.11 / 89.50 |
| DiffuSeq | 70.91 / 81.43 | 64.60 / 88.51 |
| **MoE-DiffuSeq** | **72.88 / 85.42** | **66.69 / 90.40** |

> ✅ 显著提升答案与证据链的匹配准确率，体现优异的跨文档推理能力。

#### Commonsense Conversation Dataset
| Model | BLEU | ROUGE-L | BERTScore |
|-------|------|---------|-----------|
| Longformer | 0.030 | 0.139 | 0.602 |
| DiffuSeq | 0.022 | 0.119 | 0.501 |
| **MoE-DiffuSeq** | **0.049** | **0.233** | **0.628** |

> ✅ 生成更具多样性和语义合理性的对话回复。

#### QQP（复述识别）
| Model | Accuracy |
|-------|----------|
| Longformer | 92.3 |
| DiffuSeq | 91.7 |
| **MoE-DiffuSeq** | **95.3** |

> ✅ 在语义等价判断任务中表现最佳，说明其能精准把握细微语义差异。

---

### 消融实验结果（Table 5 & Figure 3）

#### Ablation Study on Arxiv Dataset
| Configuration | BLEU/ROUGE/BERTScore |
|---------------|------------------------|
| Full Model (Sparse + 2048 steps + w=512) | 44.41 / 18.73 / 39.89 |
| No Sparse Attention | ↓ 至 42.52 / 17.99 / 38.41 |
| Reduced Diffusion Steps (1024) | 43.11 / 18.03 / 39.26 |
| Increased Diffusion Steps (4096) | ——（边际收益递减） |
| Smaller Window (256) | 微降 |
| Larger Window (1024) | 提升有限 |

> 🔍 **关键发现**：
> - **稀疏注意力至关重要**：移除后性能大幅下降；
> - **更多扩散步数有助于质量提升，但存在饱和点**；
> - **窗口大小影响较小**，说明稀疏机制本身已足够高效。

#### 推理效率与新颖性权衡（Figure 3）
- **MoE-DiffuSeq** 在 **2-gram novelty** 上显著高于 Longformer 和 DiffuSeq；
- 推理时间仅略慢于最快的 Longformer，远优于 DiffuSeq；
- 实现了“**高质量 + 高新颖性 + 快速推理**”的良好平衡。

---

## 4. 关键结论和发现

### 主要发现
1. **MoE 与扩散模型的结合是可行且高效的**：MoE-DiffuSeq 成功将 MoE 的动态资源分配优势引入扩散框架，解决了长序列生成中的计算瓶颈。
2. **稀疏注意力是处理长文本的关键组件**：通过局部窗口 + 膨胀机制 + 全局 token 的混合设计，实现了高效且有效的上下文建模。
3. **性能全面提升**：在多个 NLP 任务中，MoE-DiffuSeq 不仅在 **生成质量（ROUGE, BERTScore）** 上超越基线，在 **推理速度** 和 **文本新颖性** 方面也表现出色。
4. **适用于多种长文本应用场景**：包括科研写作、代码生成、复杂对话系统等需要深度上下文理解的任务。

### 方法的局限性
- **实现复杂度较高**：同时集成 MoE、稀疏注意力、扩散过程和 DPM-solver++，增加了工程实现难度；
- **硬件需求仍高**：尽管 MoE 提升了效率，但整体模型规模较大，依赖高性能 GPU（如 A100）；
- **缺乏多模态扩展**：目前仅限文本模态，尚未探索图像、音频等跨模态生成；
- **消融不够充分**：未单独测试 MoE 模块的影响，难以完全解耦各组件贡献。

### 未来工作方向
- **拓展至多模态生成任务**：结合视觉或语音信号，构建跨模态扩散生成系统；
- **进一步压缩与蒸馏**：研究轻量化版本以便在边缘设备部署；
- **探索更优的专家路由机制**：如 Expert Choice Routing 或负载均衡策略；
- **应用于真实世界长文档生成场景**：如自动撰写报告、法律文书、小说创作等；
- **增强可控性与编辑能力**：允许用户干预生成路径或修改中间状态。

---

> 📌 **总结一句话**：  
> **MoE-DiffuSeq 通过融合 Mixture of Experts 与稀疏注意力机制，显著提升了扩散语言模型在长文本生成任务中的效率与质量，代表了下一代高效、可扩展生成模型的重要进展。**

</details>

---

### 3. [SHIRO: Near-Optimal Communication Strategies for Distributed Sparse Matrix Multiplication](https://arxiv.org/abs/2512.20178)

**Authors**: Chen Zhuang, Lingqi Zhang, Benjamin Brock, Du Wu, Peng Chen, Toshio Endo, Satoshi Matsuoka, Mohamed Wahib  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.20178v1  

#### Abstract
Distributed Sparse Matrix-Matrix Multiplication (SpMM) is a fundamental operation in numerous high-performance computing and deep learning applications. The major performance bottleneck in distributed SpMM lies in the substantial communication overhead, which limits both performance and scalability....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SHIRO: Near-Optimal Communication Strategies for Distributed Sparse Matrix Multiplication 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

分布式稀疏矩阵乘法（**Distributed SpMM**）是高性能计算和深度学习（如图神经网络 GNN）中的核心操作，其性能瓶颈在于**通信开销过大**。现有方法在通信策略上存在两大低效问题：

1. **通信冗余**：  
   - **Sparsity-oblivious** 方法（如 CAGNET、BCL）无视稀疏性，传输整个稠密块，造成大量无用数据传输。  
   - **Sparsity-aware** 方法（如 SPA、CoLa）虽按列或行索引传输所需数据，但仍仅利用单维度稀疏信息，未能充分挖掘稀疏模式。

2. **忽略层次化网络拓扑**：  
   多数方法假设所有进程间通信代价相同，但在现代 GPU 集群中，节点内（NVLink）带宽远高于节点间（InfiniBand），导致跨节点重复传输相同数据，形成瓶颈。

---

### **提出了什么新方法或新思路**

作者提出 **SHIRO** —— 一个通信高效的分布式 SpMM 框架，包含两个核心优化策略：

#### ✅ **(1) 联合行列稀疏感知通信策略（Joint Row-Column Sparsity-Aware Communication）**

- **核心思想**：同时考虑稀疏矩阵的**行索引**和**列索引**，为每个非零元选择最优通信方式：
  - **列基策略（Column-based）**：远程进程发送所需的 `B` 行。
  - **行基策略（Row-based）**：远程进程计算部分 `C` 结果并返回。
- 将通信最小化问题建模为 **Minimum Weighted Set Cover Problem**，进一步转化为 **Minimum Weighted Vertex Cover on Bipartite Graph**。
- 利用 **Max-Flow / Min-Cut** 理论（通过 Dinic’s Algorithm）求解最优通信方案，在多项式时间内获得全局最优解。

> 📌 优势：相比单一维度策略，能显著减少通信量，尤其在非零元分布复杂时效果更明显。

#### ✅ **(2) 层次化通信策略（Hierarchical Communication Strategy）**

- 针对 GPU 集群的**两层网络架构**（节点内高速 + 节点间低速）设计：
  - **列基通信优化**：采用“源组聚合 → 跨组传输 → 组内分发”三步法，避免同一 `B` 行被多次跨节点传输。
  - **行基通信优化**：采用“组内预聚合 → 跨组传输”两阶段，将多个部分结果先在本地合并再发送，减少跨节点流量。
- 设计**重叠调度策略（Overlapping Scheduling）**：
  - 列基通信使用跨节点链路时，行基通信使用组内链路，反之亦然。
  - 实现双层级链路的**并行高效利用**，提升整体带宽利用率。

> 📌 优势：显著降低跨节点通信量，缓解慢速链路瓶颈。

---

### **相比现有方法的优势**

| 特性 | SHIRO | CAGNET/BCL | SPA | CoLa |
|------|-------|------------|-----|------|
| Sparsity-aware | ✅ 联合行列 | ❌ | ✅ 列基 | ✅ 列基 + 层次 |
| Hierarchy-aware | ✅ 双向优化 | ❌ | ❌ | ✅ 仅列基 |
| 全局最优通信 | ✅ 图论求解 | ❌ | ❌ 启发式 | ❌ |
| 通信体积 | 最小 | 最大 | 中等 | 较小 |
| 扩展性 | 强（至128 GPUs） | 弱 | 中等 | 中等 |

> SHIRO 是首个**联合优化通信策略与网络拓扑**的 SpMM 框架，兼具理论最优性与工程实用性。

---

## 2. 核心实验方法和设置

### **使用的数据集**

来自 **SuiteSparse Matrix Collection** 的真实世界稀疏矩阵，涵盖社交网络、网页图、科学计算等领域：

| 数据集 | 规模（行/列） | 非零元数量 | 密度 |
|--------|----------------|-------------|--------|
| `com-YT`, `soc-LJ`, `Orkut`, `Pokec` | 百万级 | 数千万 ~ 2.3亿 | ~1e-5 |
| `webbase-2001`, `GAP-web` | 上亿级 | 超10亿 | <1e-7 |
| `delaunay_n24`, `mawi_69M` | 中大型 | 数千万 ~ 1.4亿 | 极稀疏 |

共13个数据集，覆盖多种稀疏模式（行偏斜、列偏斜、均匀、混合）。

---

### **实验设置和评估指标**

- **硬件平台**：TSUBAME 4.0 超算，每节点 4×NVIDIA H100 GPU，NVLink 互联（450 GB/s），节点间 InfiniBand NDR200（200 Gbps）。
- **实现**：
  - 单节点 SpMM 使用 PyTorch + cuSPARSE。
  - 多节点通信基于 PyTorch Distributed + NCCL。
- **评估指标**：
  - **端到端运行时间（End-to-end Runtime）**
  - **总通信量（Total Communication Volume）**
  - **跨节点通信量（Inter-node Traffic）**
  - **加速比（Speedup）**
  - **强扩展性（Strong Scaling）**

---

### **基线方法对比**

选取四类 SOTA 分布式 SpMM 方法作为基线：

| 基线 | 分区策略 | 稀疏感知 | 层次感知 | 通信后端 |
|------|----------|-----------|-----------|------------|
| **CAGNET** | 1.5D | ❌ | ❌ | NCCL |
| **SPA** | 1.5D | ✅（列基） | ❌ | NCCL |
| **BCL** | 2D | ❌ | ❌ | NVSHMEM |
| **CoLa** | 1D | ✅（列基） | ✅（仅列基） | NVSHMEM |

> 所有方法均提取其核心 SpMM 模块进行公平比较，禁用图重排序。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

- 在 **128 GPUs** 上，SHIRO 实现：
  - **几何平均加速比**达：
    - **221.5×** vs CAGNET
    - **56.0×** vs SPA
    - **23.4×** vs BCL
    - **8.8×** vs CoLa
  - 最高通信量减少 **96.3%**（在 `mawi` 数据集上）。
  - 跨节点通信量减少最高达 **60%**（如 `com-LJ`, `Orkut`）。

---

### **与基线方法的对比结果**

- **vs CAGNET/BCL**：由于完全无视稀疏性，通信量巨大，SHIRO 在所有规模下均大幅领先（>50×）。
- **vs SPA**：虽为稀疏感知，但仅用列基策略，SHIRO 通过联合优化进一步减少通信，平均提速 **>50×**。
- **vs CoLa**：
  - 在 ≤4 GPUs（单节点内）时略慢（因 CoLa 有更好的计算-通信重叠）。
  - 在 ≥8 GPUs（跨节点）后全面反超，且差距随规模扩大而增大，证明其层次化优化的有效性。

---

### **消融实验结果**

#### 🔹 **通信量减少分析（n=32 GPUs, N=64）**

- **联合行列策略 vs 列基策略**：
  - 平均通信量减少 **40–70%**。
  - 在 `mawi` 上减少 **96%**，对应运行时间提速近 **6×**。
  - 通信模式更均衡，消除热点通信对（见 heatmap 对比）。

- **加入层次化策略后**：
  - 跨节点通信量进一步减少 **30–60%**。
  - 在 `com-LJ`, `Orkut`, `sx-SO` 上提升显著。

#### 🔹 **逐步优化性能（Ablation Study）**

- 从列基策略 → 加入联合优化 → 加入层次化调度：
  - 每一步都带来显著性能提升。
  - 唯一例外是 `del24`，因其通信极度不均衡，层次化拆分反而降低链路利用率。

#### 🔹 **强扩展性测试**

- 所有基线在 >8 GPUs 后性能下降（通信主导）。
- SHIRO 在 **2–128 GPUs** 上持续加速，表现出优异的可扩展性。
- 在 `GAP-web`, `uk-2002`, `webbase` 等大规模图上接近理想扩展效率。

---

## 4. 关键结论和发现

### **主要发现**

1. **联合行列稀疏感知通信能显著减少冗余**：
   - 单维度优化仍有大量冗余，联合优化可逼近理论下限。
   - 通信量减少直接转化为性能提升，尤其在极稀疏矩阵上。

2. **层次化网络必须显式建模**：
   - 忽视网络层级会导致跨节点重复传输，成为扩展瓶颈。
   - 通过源端聚合与目标端分发，可有效减少慢速链路负载。

3. **互补调度最大化带宽利用率**：
   - 行基与列基通信天然互补（一个用组内链路，一个用组间链路）。
   - 重叠执行可实现双链路并发，提升整体吞吐。

4. **SHIRO 具备强扩展性**：
   - 在 128 H100 GPUs 上仍保持良好性能，远超现有方法。

---

### **方法的局限性**

- **预处理开销**：构建流网络并求解最大流需一定时间，尽管可离线复用。
- **极端不均衡通信模式下可能退化**：如 `del24`，层次化拆分可能导致链路利用率下降。
- **当前实现基于 1D 行划分**，虽可推广，但未系统验证其他划分策略下的表现。

---

### **未来工作方向**

1. **支持动态稀疏模式**：在线更新通信计划以适应图结构变化（如动态 GNN）。
2. **结合图分区技术**：与 hypergraph partitioning 或 Arrow Decomposition 联合优化。
3. **扩展至 SpGEMM**：应用于稀疏-稀疏矩阵乘法。
4. **异构网络适配**：支持更复杂的多层级网络拓扑（如 CPU-GPU 混合集群）。

---

> ✅ **总结**：SHIRO 通过**联合优化通信策略与网络拓扑**，实现了分布式 SpMM 的近最优通信效率，在真实数据集和大规模 GPU 集群上展现出卓越性能与可扩展性，为 GNN 和 HPC 应用提供了强有力的底层支撑。

</details>

---

### 4. [Multi-hop Reasoning via Early Knowledge Alignment](https://arxiv.org/abs/2512.20144)

**Authors**: Yuxin Wang, Shicheng Fang, Bo Wang, Qi Luo, Xuanjing Huang, Yining Zheng, Xipeng Qiu  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.20144v1  

#### Abstract
Retrieval-Augmented Generation (RAG) has emerged as a powerful paradigm for Large Language Models (LLMs) to address knowledge-intensive queries requiring domain-specific or up-to-date information. To handle complex multi-hop questions that are challenging for single-step retrieval, iterative RAG app...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Multi-hop Reasoning via Early Knowledge Alignment*

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Retrieval-Augmented Generation (RAG)** 系统在处理复杂多跳推理（multi-hop reasoning）任务时存在的“**计划失败**”（planning failure）问题。现有迭代式 RAG 方法（如 Search-R1, Graph-R1）通常在未接触检索语料库的情况下直接启动推理规划（initial thinking），导致生成误导性的假设或无效的检索路径，从而引发级联错误（cascading errors），降低整体性能。

### 提出的新方法：Early Knowledge Alignment (EKA)
作者提出了一种简单而有效的模块——**Early Knowledge Alignment (EKA)**，其核心思想是：
> 在 LLM 开始首次推理（thinking）之前，先进行一次**初始检索**（early retrieval），将相关知识注入提示（prompt）中，使模型在具备上下文支撑的前提下进行初始规划。

具体流程如下：
1. 给定问题 $ q $，首先通过 retriever 从知识库 $ D $ 中检索 top-k 相关段落 $ P_0 = Retrieve(q, D, k) $。
2. 将 $ P_0 $ 作为“早期知识”嵌入到输入提示中。
3. LLM 基于该知识开始第一轮 `<think>` 推理，后续按标准迭代流程执行 `<query>` 或 `<answer>` 动作。

### 相比现有方法的优势
- ✅ **更强的推理基础**：避免了“无依据猜测”，提升了初始推理的质量。
- ✅ **减少冗余探索**：引导模型聚焦于相关知识子集，提升 RL 训练中的探索效率。
- ✅ **无需额外训练**：EKA 是一种即插即用（plug-and-play）的推理策略，适用于任何迭代 RAG 框架，且可作为 **training-free inference strategy** 应用于大模型。
- ✅ **通用性强**：在不同 RL 算法（PPO/GRPO）、不同检索架构（chunk/graph）、不同规模模型上均有效。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖六个标准 RAG 多跳问答数据集：
- **2WikiHop**
- **HotpotQA**
- **Musique**
- **Natural Questions (NQ)**
- **PopQA**
- **TriviaQA**

此外，在 Search-R1 设置中还加入了 **Bamboogle** 数据集。

### 实验设置
- **主干方法**：基于强化学习的两种主流框架
  - **Search-R1**（使用 PPO）
  - **Graph-R1**（使用 GRPO）
- **模型规模**：主要使用 `Qwen2.5-7B-Instruct` 和 `Qwen2.5-14B-Instruct`，并在附录中测试了更大模型（如 Qwen3）。
- **检索器**：
  - Search-R1：E5
  - Graph-R1：基于超图的 bge-large-en-v1.5
- **训练方式**：在部分实验中复现并比较了 SFT、R1、R1-Searcher 等基线。

### 评估指标
| 指标 | 含义 |
|------|------|
| **EM** | Exact Match，严格匹配准确率 |
| **F1** | 生成答案与真实答案的词级别 F1 分数 |
| **R-S (Retrieval Similarity)** | 检索到的内容与“黄金上下文”的语义相似度（cosine similarity），衡量检索质量 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2 & Table 4）

#### 在 Graph-R1 设置下的平均性能提升（Qwen2.5-7B）：
| 方法 | Avg. EM ↑ | Avg. F1 ↑ | R-S ↑ |
|------|----------|----------|--------|
| Graph-R1 | 48.57 | 57.82 | 60.40 |
| **Graph-R1 + EKA** | **52.08** (+3.51) | **60.65** (+2.83) | **64.90** (+4.50) |

#### 在 Search-R1 设置下的平均性能提升（Qwen2.5-7B）：
| 方法 | Avg. F1 ↑ |
|------|----------|
| Search-R1 | 38.50 |
| **Search-R1 + EKA** | **44.80** (+6.30) |

> 注：在某些变体（如 Search-R1-PPO）上提升高达 **+11.64 F1**。

### 与基线方法的对比结果
- EKA 显著优于所有基线，包括：
  - 零样本生成（NaiveGeneration）
  - 标准 RAG（StandardRAG）
  - 图结构方法（GraphRAG, LightRAG, HyperGraphRAG）
  - 强化学习方法（R1, R1-Searcher, Search-R1）
- 即使是当前 SOTA 方法（如 Search-R1），加入 EKA 后仍能获得显著增益。

### 消融实验结果

#### （1）熵分析（Entropy Analysis）
- 如 **Figure 3** 所示，EKA 显著降低了 `<think>` 和 `<query>` 步骤的 token 熵。
- 表明 EKA 使模型的推理和检索行为更加确定和集中，减少了无效探索。

#### （2）推理步数（Turns）
- 如 **Table 6** 所示，EKA 平均将推理轮次从 **3.26** 缩短至 **2.22**。
- 更短的推理链意味着更少噪声积累和更高效率。

#### （3）泛化能力（Generalization）
- 在跨数据集训练-测试场景下（OOD），EKA 依然保持甚至提升泛化性能（见 Table 7）。
- 在“噪声早期知识”（EKA-wiki）和“不匹配检索器”（EKA-e5）等鲁棒性测试中，EKA 仍优于基线（Tables 8 & 9），证明其对检索质量变化具有鲁棒性。

#### （4）训练免费有效性（Training-free EKA）
- 如 **Table 5** 所示，即使不对大模型（如 Qwen2.5-32B, Qwen3）进行微调，仅在推理时引入 EKA，也能带来一致且显著的性能提升。
- 例如，在 Qwen3 上，尽管原模型表现不佳，EKA 仍能稳定提分。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **早期知识对初始规划至关重要**：缺乏上下文支撑的“盲目思考”是多跳 RAG 性能瓶颈的关键原因。
2. ✅ **EKA 显著提升性能与效率**：通过提供 early knowledge，EKA 改善了推理起点，减少了错误传播，提高了检索精度和最终答案准确性。
3. ✅ **从“Plan-first”到“Align-first”范式转变**：论文倡导将 RAG 设计重心从“先计划再检索”转向“先对齐再推理”。
4. ✅ **熵视角解释机制**：EKA 降低了 RL 训练过程中的探索熵，实现了更高效的探索策略，加快了收敛速度。
5. ✅ **高度通用与可扩展**：EKA 不依赖特定模型、检索器或训练方式，是一种轻量、灵活、可扩展的增强模块。

### 方法的局限性
- ❗ **尚未验证于极复杂场景**：如 Deepresearch 等需要深度网络搜索、长期记忆的任务中效果未知。
- ❗ **依赖初始检索质量**：虽然对噪声有一定鲁棒性，但极端低质的 early retrieval 可能影响效果。
- ❗ **可能增加延迟**：多一次初始检索会略微增加响应时间（但在多数场景下收益远大于成本）。

### 未来工作方向
- 探索 EKA 在更复杂的 agentic workflows（如自主研究、科学发现）中的应用。
- 研究如何动态选择 early knowledge 的粒度（如 entity vs. passage vs. graph path）。
- 结合 EKA 与其他推理优化技术（如 self-reflection, verification）构建更强大的 RAG 系统。

---

> **总结一句话**：  
> 本论文提出的 **Early Knowledge Alignment (EKA)** 通过在推理前注入初步检索知识，有效解决了迭代 RAG 中因“无依据初始思考”导致的计划失败问题，在多个数据集和框架上实现了显著且稳健的性能提升，是一种简单、高效、无需训练的通用增强策略。

</details>

---

### 5. [EdgeFlex-Transformer: Transformer Inference for Edge Devices](https://arxiv.org/abs/2512.19741)

**Authors**: Shoaib Mohammad, Guanqun Song, Ting Zhu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.19741v1  

#### Abstract
Deploying large-scale transformer models on edge devices presents significant challenges due to strict constraints on memory, compute, and latency. In this work, we propose a lightweight yet effective multi-stage optimization pipeline designed to compress and accelerate Vision Transformers (ViTs) fo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《EdgeFlex-Transformer: Transformer Inference for Edge Devices》核心总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Vision Transformers (ViTs) 在边缘设备（如手机、嵌入式传感器）上的部署面临严重挑战，主要受限于：
- **内存限制**（通常 < 100MB）
- **计算资源有限**
- **高延迟与功耗**
- 现有压缩方法（如非结构化剪枝、低比特量化）在硬件上效率低或导致显著精度下降。

该论文旨在实现**无需重训练、轻量级、高效且适用于多种边缘平台的 ViT 推理优化方案**。

---

### **提出的新方法与创新思路**
作者提出了 **EdgeFlex-Transformer** ——一个**多阶段、模块化、无需微调的优化流水线**，结合以下关键技术：

1. **Activation Profiling（激活分析）**  
   - 利用前向 Hook 收集 MLP 层的激活统计信息（如平均绝对激活值），识别低重要性通道。

2. **Memory-Aware Structured Pruning（内存感知结构化剪枝）**  
   - 基于激活强度对 MLP 输出通道进行排序，剪除最不活跃的通道（例如最低 10%），减少中间特征图内存占用。
   - 结构化剪枝确保兼容现代边缘加速器（避免非结构稀疏带来的硬件执行瓶颈）。

3. **Selective Mixed-Precision Execution（选择性混合精度执行）**  
   - 将部分子模块（如 MLP 块）转换为 FP16，其余保留 FP32 以保证数值稳定性。
   - 使用 PyTorch 的 Automatic Mixed Precision (AMP) 实现无缝推理。

4. **Activation-Aware Quantization (AWQ)**  
   - 将权重和激活量化至 INT8，利用每通道缩放因子（per-channel scaling）保护敏感通道。
   - 基于激活分布动态调整量化策略，最小化精度损失。

> ✅ **整个流程无需任何 fine-tuning 或 retraining**，适合快速适配不同 ViT 骨干网络和边缘设备。

---

### **相比现有方法的优势**
| 方法 | 缺陷 | EdgeFlex 的改进 |
|------|------|----------------|
| **Unstructured Pruning** | 导致不规则稀疏，难以被硬件加速 | 使用**结构化剪枝**，硬件友好 |
| **Naive Quantization** | 易造成精度大幅下降 | 引入 **AWQ**，基于激活敏感度保护关键通道 |
| **Knowledge Distillation** | 需要复杂训练流程，泛化性差 | **完全无训练需求**，即插即用 |
| **NAS / AutoML** | 搜索成本极高（数百 GPU 小时） | **无需架构搜索**，轻量可复用 |
| **通用推理框架（如 FlexNN）** | 对 Transformer 内存模式支持不佳 | 针对 ViT 设计，优化内存调度 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **CIFAR-10**：用于校准（calibration）和测试。
- **校准集**：仅使用 32 个样本进行 activation profiling。
- **测试集**：完整 10,000 个测试样本用于评估最终性能。

---

### **实验设置**
- **模型架构**：ViT-Huge（32 层，隐藏维度 1280，MLP 大小 5120，总参数约 6.32 亿）
- **硬件平台**：NVIDIA A100 GPU（Ohio Supercomputing Center 的 Ascend 集群），启用内存剖析
- **批大小（batch size）**：32
- **实现工具**：PyTorch + Hugging Face Transformers 库

---

### **评估指标**
| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | 分类准确率 |
| **Average Batch Latency (s)** | 单批次平均推理延迟 |
| **Total Inference Time (s)** | 所有 10k 样本总推理时间 |
| **Peak Memory Usage (MB)** | 推理过程中峰值内存消耗 |

---

### **基线与变体对比**
共比较四种模型变体：
| 变体 | 描述 |
|------|------|
| **Original FP32** | 原始未修改的 ViT-Huge 模型 |
| **Pruned FP32** | 经过结构化剪枝后的 FP32 模型 |
| **Pruned + FP16** | 剪枝后启用混合精度（FP16） |
| **Pruned + AWQ** | 剪枝后应用 AWQ 量化至 INT8 |

> ❗所有优化均**不涉及 fine-tuning**，直接应用于预训练模型。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Fig. 3）**

| Variant | Accuracy (%) | Avg Batch Latency (s) | Total Time (s) | Peak Memory (MB) |
|--------|---------------|------------------------|----------------|------------------|
| Original FP32 | 8.56 | 0.194 | 25.07 | 2613.45 |
| Pruned FP32 | 7.78 | 0.198 | 25.68 | ~2528 |
| Pruned + FP16 | **11.67** | **0.028** | **4.15** | **1356.10** |
| Pruned + AWQ | 11.28 | 0.199 | 25.79 | **623** |

> 注：原始 ViT-Huge 在 CIFAR-10 上未经 fine-tuning 准确率较低属正常现象。

---

### **与基线方法的对比结果**
- **内存方面**：
  - Pruned + AWQ 实现 **>76% 的峰值内存降低**（从 2613 → 623 MB）
  - Pruned + FP16 实现 ~48% 内存缩减
- **延迟方面**：
  - Pruned + FP16 达到 **6.9× 加速**（平均延迟从 0.194s → 0.028s）
  - 总推理时间从 25s+ 缩短至 **4.15 秒**
- **精度方面**：
  - 所有优化版本**不仅没有掉点，反而提升准确率**（可能因剪枝去除了噪声通道）
  - Pruned + FP16 最高达 **11.67%**，优于原始模型

---

### **消融实验分析（隐含在结果中）**
虽然未明确列出消融表，但从各阶段结果可得出：
1. **仅剪枝**（Pruned FP32）：
   - 内存略降（~3%），延迟几乎不变，准确率轻微下降。
   - 表明**单独剪枝效果有限**。
2. **剪枝 + FP16**：
   - 延迟骤降 6.9×，内存减半，准确率反升。
   - 说明**混合精度是加速的关键驱动因素**。
3. **剪枝 + AWQ**：
   - 内存降至 623MB（仅为原模型 24%），适合极端资源受限场景。
   - 虽然延迟未改善（仍为 ~0.2s/batch），但**内存效率最优**。

> 🔍 发现：**“剪枝 + 量化” 更适合内存受限场景，“剪枝 + FP16” 更适合低延迟需求场景。**

---

## **4. 关键结论和发现**

### **主要发现**
1. **无需 fine-tuning 的压缩方案可行且有效**：通过 activation-aware 策略，可在不重新训练的情况下实现高质量压缩。
2. **结构化剪枝 + AWQ 是内存压缩的最佳组合**：Pruned + AWQ 实现 >76% 内存节省，适用于 <1GB 内存的边缘设备。
3. **混合精度（FP16）带来最大速度收益**：Pruned + FP16 实现超过 6× 推理加速，适合实时应用（如 AR/语音转录）。
4. **优化顺序至关重要**：先剪枝 → 再混合精度/量化，形成渐进式压缩，避免误差累积。
5. **EdgeFlex 具备良好通用性和模块性**：可灵活集成到其他 ViT 架构或扩展至 NLP/Multimodal 任务。

---

### **方法的局限性**
1. **依赖 calibration 数据**：需要少量输入样本来收集激活统计，若数据分布偏移可能导致剪枝不准。
2. **当前未支持动态稀疏或 MoE 架构**：无法处理条件执行路径（如 Mixture-of-Experts）。
3. **未在真实边缘芯片上验证**：实验基于 A100 GPU，尚未在移动 SoC（如 Snapdragon）或 NPU 上测试实际能效。
4. **CIFAR-10 上准确率本身偏低**：未 fine-tune 的 ViT-Huge 不适合小数据集，影响结果解释力。

---

### **未来工作方向（作者明确提出）**
1. **扩展至 Mixture-of-Experts (MoE) 架构**：研究如何对稀疏激活专家层进行剪枝与量化。
2. **Task-Aware Pruning**：引入下游任务信号指导剪枝决策，提升特定任务性能。
3. **Layer-wise Dynamic Precision Control**：按层动态选择 FP32/FP16/INT8，进一步平衡精度与效率。
4. **与实时调度系统集成**：将 EdgeFlex 与边缘运行时（如 FlexNN）结合，实现端到端自适应推理。

---

## ✅ 总结
**EdgeFlex-Transformer 提供了一个实用、高效、无需训练的 ViT 压缩框架**，通过 **activation-aware pruning + mixed precision + AWQ** 的协同优化，在保持甚至提升 accuracy 的同时，实现了：
- **>76% 内存压缩**
- **>6× 推理加速**
- 完全免 retraining

为大规模 Transformer 模型在边缘设备上的部署提供了**一条切实可行的技术路径**，具有较强的工程落地潜力。

</details>

---

### 6. [MolAct: An Agentic RL Framework for Molecular Editing and Property Optimization](https://arxiv.org/abs/2512.20135)

**Authors**: Zhuo Yang, Yeyun chen, Jiaqing Xie, Ben Gao, Shuaike Shen, Wanhao Liu, Liujia Yang, Beilun Wang, Tianfan Fu, Yuqiang Li  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.20135v1  

#### Abstract
Molecular editing and optimization are multi-step problems that require iteratively improving properties while keeping molecules chemically valid and structurally similar. We frame both tasks as sequential, tool-guided decisions and introduce MolAct, an agentic reinforcement learning framework that ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MolAct: An Agentic RL Framework for Molecular Editing and Property Optimization 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
分子编辑（Molecular Editing）和分子优化（Molecular Optimization）是药物设计中的核心任务，传统方法通常采用单步生成或静态指令微调方式处理，存在以下问题：
- 缺乏对**多步决策过程**的建模；
- 无法有效利用外部化学工具（如有效性检查、性质预测、相似性评估）进行反馈；
- 难以保证生成分子的**化学有效性**和**结构一致性**（Murcko scaffold 保留）；
- 在复杂优化目标下表现不稳定。

### 提出的新方法与新思路
本文提出 **MolAct** —— 一种基于 **Agentic Reinforcement Learning (RL)** 的框架，将分子设计形式化为一个**多轮、工具增强的智能体决策过程**。其核心创新包括：

- **首次将分子设计建模为 Agentic RL 问题**：LLM 智能体通过“思考 → 调用工具 → 观察反馈 → 再思考”的循环逐步修改分子。
- **两阶段训练范式**：
  - **Stage 1（MolEditAgent）**：预训练阶段，学习基本编辑操作（Add/Delete/Substitute），强调化学有效性和结构相似性；
  - **Stage 2（MolOptAgent）**：继续训练阶段，在 Stage 1 基础上学习属性优化（如 LogP、solubility），同时保持 scaffold 一致性。
- **工具集成机制**：支持调用多种化学工具（validity checker, similarity calculator, property oracle），并利用其反馈指导后续动作。
- **Group-Relative Policy Optimization (GRPO)**：在多个并行 rollout 上进行相对奖励归一化，提升长程信用分配稳定性。

### 相比现有方法的优势
| 维度 | MolAct 的优势 |
|------|----------------|
| **灵活性** | 支持多步交互式编辑，适应多样化任务；而多数 LLM 方法为单步生成 |
| **可靠性** | 显式引入工具验证，显著提高分子有效性（95–100%） |
| **可解释性** | 决策路径透明，每一步都有工具反馈支撑 |
| **性能表现** | 在多个基准任务中超越强闭源模型（如 Claude 3.7、Gemini） |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **ChemCoTDatasets (Li et al., 2025)** 构建训练数据，该数据集包含带链式推理标注的多步分子修改样本。
- 移除原始 CoT 推理文本，仅提取：
  - 输入：源分子 SMILES + 编辑指令 / 优化目标
  - 输出：目标分子 SMILES
- 所有分子经过标准化（canonicalization）和有效性校验。

### 实验设置
- **模型家族**：
  - MolEditAgent：用于分子编辑任务，基于 Qwen-2.5-3B 和 Qwen-2.5-7B；
  - MolOptAgent：由 MolEditAgent 初始化后继续训练，用于优化任务。
- **交互机制**：
  - 最大回合数（turn budget）设为 16；
  - 每个 prompt 启动 K 条并行 rollout，使用 GRPO 进行策略更新；
  - 仅对 agent 生成的 token 计算梯度，工具输出作为上下文不参与反向传播。
- **工具接口**：
  - 编辑操作：add/delete/substitute 功能团；
  - 评估工具：validity check、Tanimoto similarity、property oracle（LogP, solubility, QED, DRD2, JNK3, GSK3β）。

### 评估指标
| 任务类型 | 主要指标 |
|--------|---------|
| **分子编辑** | - Pass@1（是否正确完成编辑）<br>- Validity（生成分子的有效率） |
| **分子优化** | - △（平均属性增益，正值更好）<br>- SR%（Success Rate，实现正向改进的比例） |

### 基线方法对比
涵盖两类主流模型：
- **W/ Thinking**：具备“思维链”能力的闭源或开源模型（如 Gemini-2.5-pro-think, Claude3.7-sonnet-think）
- **W/o Thinking**：无显式推理能力的标准指令微调模型（如 GPT-4o, Qwen2.5-7B-Instruct）

---

## 3. 主要实验结果和性能指标

### 分子编辑任务（Molecular Editing）

#### 准确率表现（Table 1）
| 模型 | Add (%) | Delete (%) | Substitute (%) |
|------|--------|-----------|---------------|
| **MolEditAgent-7B** | **90.0** | **80.0** | **78.3** |
| Gemini-2.5-pro-think | 100.0 | 85.0 | 81.7 |
| Claude3.7-sonnet-think | 85.0 | 80.0 | 83.4 |
| GPT-4o | 80.0 | 80.0 | 65.0 |

- MolEditAgent-7B 在 Add 任务中仅次于最强闭源模型，在 Delete 和 Sub 上优于大多数 W/o Thinking 模型。
- MolEditAgent-3B 表现良好（Add: 80.0%, Delete: 70.0%），但在 Sub 任务上明显下降（16.7%），表明复杂替换需要更强推理能力。

#### 化学有效性（Table 2）
| 模型 | Add (%) | Delete (%) | Substitute (%) |
|------|--------|-----------|---------------|
| **MolEditAgent-7B** | **100.0** | **95.0** | **98.0** |
| Qwen2.5-7B-Instruct | 75.0 | 70.0 | 65.0 |
| **MolEditAgent-3B** | **95.0** | **80.0** | **71.7** |
| Qwen-2.5-3B-Instruct | 60.0 | 55.0 | 65.0 |

- MolAct 显著提升了生成分子的化学有效性，尤其在 7B 模型上接近完美（95–100%）。

---

### 分子优化任务（Molecular Optimization）

#### 性能对比（Table 3）
| 模型 | LogP △ (SR%) | Solubility △ (SR%) | DRD2 △ (SR%) | GSK3β △ (SR%) |
|------|-------------|--------------------|--------------|----------------|
| **MolOptAgent-7B** | **0.89 (92%)** | **1.42 (84%)** | 0.02 (38%) | **0.04 (36%)** |
| Gemini-2.5-pro-think | -0.28 (81%) | 1.91 (92%) | **0.35 (74%)** | 0.04 (68%) |
| Claude3.7-sonnet-think | 0.41 (81%) | 0.59 (77%) | 0.18 (66%) | 0.01 (57%) |
| DeepSeek-R1 | 0.36 (74%) | 1.48 (97%) | 0.10 (62%) | -0.02 (41%) |

- **LogP 优化**：MolOptAgent-7B 取得最高增益（△=0.89）和成功率（92%），大幅领先 Claude 和 Gemini；
- **Solubility**：仅次于 Gemini 和 DeepSeek-R1，但仍具竞争力（△=1.42, SR%=84%）；
- **生物活性目标**：在 DRD2 和 GSK3β 上取得正向改进，但仍有提升空间；
- **JNK3 和 QED**：表现一般（JNK3 △=-0.04），说明某些特定目标需领域增强。

#### 小模型表现
- MolOptAgent-3B 成功率仅为 3–12%，远低于 7B 版本，显示容量对执行能力至关重要。

---

### 消融实验结果（Ablation Study）

#### 两阶段训练 vs 单阶段训练（Table 4）
| 模型 | LogP SR% | Solubility SR% | QED SR% | DRD2 SR% |
|------|---------|----------------|--------|----------|
| Qwen-2.5-7B-Instruct（one-stage） | 0% | 0% | 12% | 0% |
| **MolOptAgent-7B（two-stage）** | **92%** | **84%** | **35%** | **38%** |

- **关键发现**：即使使用相同工具和 RL 训练，**缺少编辑预训练的一阶段方法几乎失败**（SR% ≈ 0%），证明两阶段设计的关键作用。
- 编辑预训练帮助模型学会如何合理调用工具、何时终止，从而形成有效的策略执行能力。

#### 模型容量与工具使用效率分析
- **响应长度分析**（Figures 5 & 6）：
  - 3B 模型在优化过程中出现极端响应长度峰值（高达 5,000 tokens），反映推理冗余、工具调用低效；
  - 7B 模型响应更短且稳定，能在有限回合内高效执行策略。
- **结论**：更大的模型不仅能学到更高奖励，还能更可靠地**执行策略**，体现“executability”重要性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **多步、工具增强的决策模式优于单步生成**：显式建模编辑过程并通过工具反馈迭代修正，显著提升有效性和成功率。
2. ✅ **两阶段训练至关重要**：先掌握基础编辑技能，再迁移到属性优化，是成功的关键。
3. ✅ **模型容量影响策略执行力**：尽管小模型也能拟合奖励信号，但难以在预算内高效执行工具序列。
4. ✅ **高有效性源于工具闭环反馈**：MolAct 实现了 95–100% 的化学有效性，解决了传统生成模型常见的“hallucination”问题。
5. 📈 **MolAct-7B 在多个任务上超越闭源 Thinking 模型**，尤其是在 LogP 优化方面表现突出。

### 方法的局限性
1. **对特定目标泛化不足**：如 JNK3 和 QED 优化效果不佳，可能因缺乏专用知识或工具；
2. **未考虑合成可行性（synthetic feasibility）**：生成的分子虽在计算上有效，但未必易于实验室合成；
3. **依赖高质量工具接口**：系统性能受限于 property oracle 和 validity checker 的准确性；
4. **小模型执行力弱**：3B 模型即便接受两阶段训练，仍难胜任复杂任务。

### 未来工作方向
1. **引入反应路径约束**：结合 retrosynthesis 工具，确保生成分子具有可行的合成路线；
2. **增强错误恢复机制**：当工具返回失败时，自动调整策略而非陷入无效循环；
3. **开发领域定制化工具链**：针对特定靶点（如 JNK3）构建专用预测器或知识库；
4. **探索更高级的课程学习**：超越两阶段，设计渐进式任务难度调度；
5. **构建 executability-aware 评估体系**：不仅看奖励高低，更要衡量策略能否在预算内成功执行。

---

> 🔗 **项目资源**  
> GitHub: [https://github.com/little1d/MolAct](https://github.com/little1d/MolAct)  
> Hugging Face: [https://huggingface.co/collections/little1d/molact](https://huggingface.co/collections/little1d/molact)

</details>

---

### 7. [Field-Space Attention for Structure-Preserving Earth System Transformers](https://arxiv.org/abs/2512.20350)

**Authors**: Maximilian Witte, Johannes Meuer, \'Etienne Pl\'esiat, Christopher Kadow  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.20350v1  

#### Abstract
Accurate and physically consistent modeling of Earth system dynamics requires machine-learning architectures that operate directly on continuous geophysical fields and preserve their underlying geometric structure. Here we introduce Field-Space attention, a mechanism for Earth system Transformers th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Field-Space Attention for Structure-Preserving Earth System Transformers

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

传统深度学习模型（如 Vision Transformer 和 U-Net）在处理地球系统建模任务时存在以下关键问题：

- **抽象化表示导致物理结构丢失**：大多数模型将输入场（如温度、风速等）映射到高维**latent space**中进行处理，中间状态不再是可解释的物理场，破坏了几何和守恒结构。
- **优化困难与训练不稳定**：由于缺乏强归纳偏置（inductive bias），Vision Transformers 在小数据或从头训练时收敛慢且易出现损失震荡。
- **分辨率固定，难以扩展**：latent space 中的 token 表示通常绑定特定空间分辨率，难以在训练后提升分辨率而不修改架构。
- **物理约束难以嵌入**：守恒律、尺度一致性等物理先验只能施加于输出端，无法贯穿整个网络内部。

这些问题限制了模型在科学建模中的可靠性、可解释性和泛化能力。

---

### **提出了什么新方法或新思路**

作者提出 **Field-Space Attention** 及其构建的 **Field-Space Transformer (FST)** 架构，核心思想是：

> **不在 latent space 中操作，而是在物理空间（field space）中直接学习对连续地理物理场的结构保持型变形（structure-preserving deformation）。**

#### 主要创新点包括：

- ✅ **Field-Space Attention 机制**：
  - 注意力计算不作用于 learned patch embeddings，而是作用于原始物理域上的多尺度场分解。
  - 所有中间状态始终为球面上的连续场，具有明确的物理意义。

- ✅ **非学习型多尺度分解（Non-learned Multi-Scale Decomposition）**：
  - 基于 HEALPix 网格构建层次化的粗细网格结构（zoom levels）。
  - 每一层表示为“粗尺度均值 + 细尺度残差”，残差局部均值为零，确保跨尺度守恒。

- ✅ **结构保持更新（Residual Update in Field Space）**：
  - 每个 FSA block 输出一个残差更新 $\Delta x_n$，并执行 $x_{n+1} = x_n + \Delta x_n$。
  - 整个过程保持场的几何结构和物理一致性。

- ✅ **联合多尺度 tokenization**：
  - 不同 zoom level 的场被统一重排到共享的 token grid 上，实现跨尺度注意力交互。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **可解释性** | 中间层输出是真实的物理场，可用于可视化分析（见 Fig. S1） |
| **稳定性与收敛速度** | 训练更稳定，无明显 loss spike，收敛更快（见 Fig. S2） |
| **参数效率** | 在显著更少参数下达到更高精度（如 15.5M vs 54.8M） |
| **物理一致性** | 尺度间守恒机制内置于表示中，无需后处理强制约束 |
| **灵活性** | 固定分解允许后期引入更高分辨率，支持迁移与扩展 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **ERA5 大气再分析数据集**（ECMWF）
- **任务**：全球近地表温度超分辨率（Super-Resolution / Downscaling）
- **放大倍数**：×1024（低分辨率输入 → 高分辨率输出）
- **空间网格**：HEALPix 球面离散化，最高 zoom level $z=8$（约 25 km 分辨率）

---

### **实验设置和评估指标**

| 项目 | 设置 |
|------|------|
| **训练周期** | 100,000 iterations |
| **硬件** | 4 × NVIDIA A100 GPU (80GB) |
| **混合精度** | bf16-mixed（训练），full precision（验证） |
| **优化器** | AdamW |
| **学习率** | $2 \times 10^{-4}$，cosine 调度 + 2000 步 warmup |
| **Batch Size** | 16 |
| **Loss Function** | MSE（Mean Squared Error） |
| **评估指标** |  
- **RMSE**（Root Mean Square Error，主指标）  
- **MAE**（Mean Absolute Error，时间序列稳定性）  
- **Latency-weighted RMSE**（用于 U-Net 对比） |

---

### **基线方法对比**

| 模型 | 描述 |
|------|------|
| **Vision Transformer (ViT)** | 标准 ViT，patch size=1024（对应 $z=3$），learned patch embedding |
| **Multi-Scale ViT (MS-ViT)** | 使用本文提出的 multi-scale tokenization 的 ViT 变体（用于消融） |
| **Transformer U-Net (Trans-U-Net)** | CNN 编码器-解码器 + Transformer 注意力模块，典型混合架构 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1 和 Fig. 3）**

| 模型 | 参数量 [M] | RMSE [K] |
|------|-----------|----------|
| **FST (ours, best)** | **15.5** | **0.847** ✅ |
| FST (overfitting) | 32.6 | 0.859 |
| ViT | 54.8 | 0.904 |
| MS-ViT | 54.8 | 0.865 |
| U-Net | 99.6 | 1.032 |

> 📌 **结论**：仅用 **15.5M 参数** 的 FST 超越了使用 **54.8M~99.6M 参数** 的所有基线模型。

---

### **与基线方法的对比结果**

- 🔹 **优于 ViT 和 U-Net**：
  - RMSE 降低 **6.3% ~ 17.9%**
  - 收敛速度快，训练曲线平滑（Fig. S2 显示 ViT 出现剧烈波动）
  - 在相同参数规模下，FST 性能远超 ViT

- 🔹 **优于 MS-ViT**：
  - 即使 MS-ViT 使用了相同的 multi-scale tokenization，其性能仍低于 FST（0.865 vs 0.847）
  - 表明 **field-space 操作本身带来了额外增益**

- 🔹 **参数效率极高**：
  - 最优 FST（15.5M）参数仅为 ViT（54.8M）的 **28%**，却实现了更低误差
  - 图 3 显示 FST 在所有参数规模下都优于基线

---

### **消融实验结果**

#### （1）不同 zoom level 数量的影响（Table 2）

| nz | Z levels | 参数量 [M] | RMSE [K] |
|----|--------|------------|----------|
| 1 | {8} | 3.8 | 1.653 |
| 2 | {3,8} | 3.8 | 0.945 |
| 3 | {3,4,8} | 3.8 | 0.934 |
| 4 | {3,4,5,8} | 3.8 | 0.913 |
| 5 | {3,4,5,6,8} | 4.1 | 0.908 |

> 📌 **结论**：单尺度（nz=1）严重失败；加入多尺度后性能大幅提升，且随尺度增加逐渐饱和。

#### （2）multi-scale tokenization 的独立作用

- 将本文的 multi-scale tokenization 应用于标准 ViT 得到 **MS-ViT**
- 结果显示：MS-ViT 比普通 ViT 更稳定、准确
- 说明 **multi-scale 分解本身有助于优化和表达能力**

#### （3）single-scale 极限情况验证

- 当 $n_z=1$ 时，FST 退化为堆叠的 ViT 层
- 实验表明该配置无法有效收敛（Fig. S3）
- 进一步证明 **多尺度结构 + field-space 操作** 是成功的关键

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **Field-Space 操作优于 Latent-Space 抽象**：
   - 直接在物理场空间中学习变形，能更好地保留几何结构和物理一致性。
   - 中间状态可解释，便于诊断和嵌入科学约束。

2. ✅ **多尺度分解是关键归纳偏置**：
   - 固定的、基于 HEALPix 的 hierarchical decomposition 提供了天然的尺度分离机制。
   - 零均值残差设计实现了跨尺度守恒，增强了泛化能力。

3. ✅ **更高的参数效率与训练稳定性**：
   - FST 在更小模型规模下实现更优性能，适合资源受限场景。
   - 训练过程平稳，适合大规模科学模拟集成。

4. ✅ **与经典数值方法有深刻联系**：
   - 与 multigrid、finite-volume、LES 等方法共享设计理念（restriction/prolongation、subgrid modeling）。
   - 有望成为连接 ML 与传统数值建模的桥梁。

---

### **方法的局限性**

- ⚠️ **当前仅验证于单一任务**：实验集中在温度超分辨率，尚未广泛测试于其他变量（如降水、风场）或多任务联合建模。
- ⚠️ **高分辨率下的参数增长问题**：
  - 当前 attention 在 coarse token grid 上操作，但在极高分辨率下 dense projection 层可能导致参数爆炸。
  - 文中建议可通过局部注意力（local attention）或残差压缩缓解。
- ⚠️ **尚未探索四维时空建模**：虽然文中提到可扩展至时空数据，但未实际实现。

---

### **未来工作方向**

- 🔮 **构建 Earth System Foundation Models**：
  - 利用 FST 作为基础模块，发展支持多变量、多模态、时空预测的通用地球系统模型。
- 🔮 **结合生成式建模（如 Diffusion Models）**：
  - FST 的逐步 refine 特性类似 denoising diffusion，适合用于气候生成任务。
- 🔮 **引入自适应 patch size 或稀疏 attention**：
  - 如 Swin Transformer 风格的 shifted window attention，以应对千米级超高分辨率。
- 🔮 **预训练-微调范式探索**：
  - 利用非学习分解，可在预训练后插入更高 zoom level，实现分辨率迁移。

---

## 总结

> **Field-Space Transformer 提出了一种全新的“以场为中心”（field-centric）建模范式**，通过在物理空间而非 latent space 中执行 attention，实现了**高性能、高可解释性、高物理一致性的地球系统建模**。它不仅在温度超分辨率任务上超越主流架构，更为下一代科学机器学习提供了坚实的基础组件。

</details>

---

### 8. [Adaptive Financial Sentiment Analysis for NIFTY 50 via Instruction-Tuned LLMs , RAG and Reinforcement Learning Approaches](https://arxiv.org/abs/2512.20082)

**Authors**: Chaithra, Kamesh Kadimisetty, Biju R Mohan  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.20082v1  

#### Abstract
Financial sentiment analysis plays a crucial role in informing investment decisions, assessing market risk, and predicting stock price trends. Existing works in financial sentiment analysis have not considered the impact of stock prices or market feedback on sentiment analysis. In this paper, we pro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Adaptive Financial Sentiment Analysis for NIFTY 50 via Instruction-Tuned LLMs, RAG and Reinforcement Learning Approaches

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统金融情感分析模型存在以下局限：
- **缺乏市场反馈机制**：大多数模型仅基于文本进行预测，未考虑实际股票价格变动对情感判断的影响。
- **上下文不足**：新闻标题通常简短且缺乏背景信息，导致LLMs在理解时容易出错。
- **静态信息源权重**：现有方法对不同新闻来源采用固定权重，无法动态适应市场变化。

### 🚀 提出的新方法与创新思路
本文提出了一种**自适应金融情感分析框架**，结合 **Instruction-Tuned LLMs、Retrieval-Augmented Generation (RAG)** 和 **Reinforcement Learning (PPO)**，实现市场感知的情感分类。其核心创新包括：

1. **Instruction-Tuned LLaMA 3.2 3B 模型微调**
   - 在 SentiFin 数据集上进行指令微调，使 LLM 更好地理解印度股市语境下的金融情感。

2. **动态多源 RAG 上下文增强**
   - 构建一个基于 cosine similarity 的 RAG 系统，从多个可信财经媒体中检索相关历史新闻作为上下文。
   - 引入**加权相似度评分**，结合新闻来源的可靠性权重，提升上下文质量。

3. **市场反馈驱动的源可信度更新机制**
   - 设计一个反馈模块，将模型预测情感与次日股价走势对比。
   - 若预测与实际回报一致，则增加该新闻源的权重；否则降低权重（使用梯度式更新规则）。

4. **基于 PPO 的强化学习优化策略**
   - 将源权重调整建模为 RL 任务，使用 Proximal Policy Optimization (PPO) 学习长期最优的源选择策略。
   - 相比直接反馈更新更稳定，具备跨时间泛化能力。

### 🔍 相比现有方法的优势
| 对比维度 | 传统方法 | 本论文方法 |
|--------|---------|-----------|
| 上下文获取 | 单一来源或无上下文 | 多源动态检索（RAG） |
| 来源可信度 | 静态设定或忽略 | 动态调整（反馈 + PPO） |
| 市场对齐 | 无显式对齐机制 | 显式利用 stock return 进行反馈 |
| 泛化能力 | 固定策略 | 可随时间演进的学习型策略 |

> ✅ **优势总结**：系统不仅能提高分类准确率，还能“学会”依赖那些真正影响市场的高质量信息源，增强了模型的市场敏感性和鲁棒性。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 描述 |
|-------|------|
| **SentiFin Dataset** | 用于 instruction tuning 的训练集，包含 10,572 条印度金融市场新闻标题，三类标签（positive / neutral / negative），按 80:20 划分训练/测试集。 |
| **NIFTY 50 新闻数据集（自建）** | 收集 2024–2025 年间 8,000 条 NIFTY 50 成分股相关新闻，来自 Yahoo Finance、MoneyControl 等主流平台。用于 RAG 检索和测试。 |
| **测试集真实标签生成方式** | 基于次日收益率相对于 30 日滚动均值和标准差定义：<br>- 正向：return > mean + std<br>- 负向：return < mean – std<br>- 中性：其余情况 |

### ⚙️ 实验设置

- **基础模型**：`unsloth/Llama-3.2-3B-Instruct`，使用 QLoRA 进行高效微调。
- **RAG 设置**：
  - 使用 `all-MiniLM-L6-v2` 编码句子并计算 cosine similarity。
  - 检索窗口：目标日期前后 3 天内的新闻。
  - Top-k 检索 + 加权排序（source weight × similarity）。
- **反馈机制**：
  - 定义 ±0.5% 为中性区间，避免轻微波动干扰权重更新。
  - 权重更新公式：`W_new = clamp(W_old ± α)`，其中 α = 1e-4。
- **PPO 设置**：
  - 状态：当前 source weights + 查询特征
  - 动作：输出新的 normalized source weight 向量
  - 奖励函数：预测正确 +1，错误 -1（可带中性区剪裁）
  - 使用 Hugging Face TRL 库实现 PPO 训练

### 📊 评估指标

- **Accuracy**：整体预测准确率
- **Weighted F1-Score**：考虑类别不平衡的综合指标
- **对比配置**：
  1. Fine-Tuned LLaMA（无上下文）
  2. RAG + 静态权重（cosine similarity）
  3. RAG + 市场反馈更新权重（WOC / cosine）
  4. RAG + PPO 优化权重
  5. 加入前3天价格描述作为额外上下文（Price Context）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 4）

| 模型变体 | Accuracy | Weighted F1 |
|--------|----------|-------------|
| Fine-Tuned LLaMA 3.2 | 0.5520 | 0.5375 |
| RAG + Without Market Feedback | 0.6094 | 0.5722 |
| RAG + With Market Feedback (WOC) | **0.6153** | **0.5746** |
| RAG + PPO Optimized Weights | 0.6109 | 0.5733 |
| RAG + Price Context (WOC) | **0.6650** | 0.5674 |

> 💡 注：加入 price context 显著提升 accuracy，但 F1 下降 → 模型偏向预测 majority class（neutral），存在偏差。

### 🔁 与基线方法对比

| 基线模型 | Accuracy | F1 |
|--------|----------|-----|
| FinBERT | 0.4852 | 0.5027 |
| RoBERTa | 0.5800 | 0.5551 |
| **本文最佳（RAG+WOC）** | **0.6153** | **0.5746** |

✅ 结果表明：本文方法显著优于通用及领域预训练模型（如 FinBERT），说明**多源上下文 + 动态反馈机制**的有效性。

### 🔍 消融实验结果（Ablation Study）

- **仅用 LLaMA 微调**：表现最弱（Acc=0.5520），说明缺乏上下文限制了性能。
- **引入 RAG（静态权重）**：Acc 提升至 0.6094，验证了外部上下文的重要性。
- **加入市场反馈（WOC）**：进一步提升到 0.6153，证明动态权重能增强市场对齐。
- **PPO 替代反馈更新**：性能略低但更稳定，适合长期部署。
- **加入价格上下文**：Acc 达 0.6650，但 F1 未改善 → 表明 accuracy 提升源于类别偏移，非真正语义理解增强。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Instruction tuning + RAG 是有效组合**  
   在金融领域，单纯依赖 LLM 的 zero/few-shot 能力不足，需通过 domain-specific 微调 + 外部上下文补充才能发挥潜力。

2. **市场反馈显著提升模型对齐能力**  
   将 stock return 作为监督信号，可让模型“感知”市场反应，从而筛选更有价值的信息源。

3. **PPO 提供更稳健的权重学习机制**  
   虽然直接反馈更新简单有效，但易受短期噪声影响；PPO 能学习更具泛化性的策略，在未知股票或时间段表现更好。

4. **Source Credibility 可被数据驱动学习**  
   实验显示，初始高权重媒体（如 Business Standard）最终被 PPO 赋予零权重，而 Moneycontrol、Zee Business 成为主要信源 → 说明“权威≠有用”，应以实证效果为准。

### ⚠️ 方法的局限性

- **高度依赖多源新闻数据**：若某些公司新闻稀疏，RAG 效果受限。
- **价格上下文未能改善 F1**：加入历史价格描述虽提升 accuracy，但加剧类别不平衡，未解决 minority class（positive/negative）识别难题。
- **标签基于价格变动，可能存在因果混淆**：并非所有价格波动都由新闻引起，基本面或其他宏观因素也可能主导。

### 🔮 未来工作方向

1. **融合基本面指标**（fundamental indicators）如 PE ratio、EPS、行业趋势等，减少纯情绪误判。
2. **引入同行股票（peer stocks）信息**：利用同行业企业的联动效应辅助判断。
3. **探索多模态输入**：整合图表、财报数字、管理层语气等非文本信号。
4. **扩展至其他市场**：验证框架在欧美、新兴市场的普适性。
5. **在线学习机制设计**：支持实时增量更新 source weights 和 LLM 推理策略。

---

## 总结

📌 本文提出了一种**市场感知、反馈驱动、可自适应演化的金融情感分析框架**，通过 **Instruction-Tuned LLM + RAG + PPO** 的三重架构，在 NIFTY 50 数据上实现了优于传统方法的性能。它不仅提升了分类精度，更重要的是建立了“模型预测—市场反馈—策略优化”的闭环系统，为构建智能投研工具提供了可行路径。

</details>

---

### 9. [HGAN-SDEs: Learning Neural Stochastic Differential Equations with Hermite-Guided Adversarial Training](https://arxiv.org/abs/2512.20272)

**Authors**: Yuanjian Xu, Yuan Shuai, Jianing Hao, Guang Zhang  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.20272v1  

#### Abstract
Neural Stochastic Differential Equations (Neural SDEs) provide a principled framework for modeling continuous-time stochastic processes and have been widely adopted in fields ranging from physics to finance. Recent advances suggest that Generative Adversarial Networks (GANs) offer a promising soluti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# HGAN-SDEs: Learning Neural Stochastic Differential Equations with Hermite-Guided Adversarial Training —— 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **Neural SDEs 的训练难题**：Neural Stochastic Differential Equations (Neural SDEs) 虽然能有效建模连续时间随机过程，但由于其路径分布复杂且似然函数难以计算，传统基于似然的方法不适用。
- **现有 GAN 方法的瓶颈**：先前研究采用基于 Neural Controlled Differential Equations (CDEs) 的判别器进行对抗训练，虽具备建模时序动态的能力，但存在：
  - 高计算开销
  - 对抗训练不稳定
  - 扩展性差

### 🚀 提出的新方法：HGAN-SDEs
作者提出 **HGAN-SDEs**，一种基于生成对抗网络（GAN）的新框架，其核心创新在于：
- **使用 Neural Hermite Functions 构造判别器**：
  - 利用 Hermite 函数作为正交基来近似 SDE 的转移密度（transition density）
  - 将判别任务转化为对路径级动态的概率匹配问题
- **结合 Neural SDE 作为生成器**：
  - 生成器为标准的 Neural SDE 结构，从噪声 $ v \sim \mathcal{N}(0, I) $ 映射到轨迹 $ x(t) $
  - 判别器通过 Hermite 展开评估生成轨迹是否符合真实数据生成过程（DGP）

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **表达能力** | Hermite 函数构成完备正交基（complete orthonormal basis），具有通用逼近性质（universal approximation），可逼近任意平方可积密度函数 |
| **稳定性提升** | 正交性带来数值稳定性和优化鲁棒性，缓解 GAN 训练中的模式崩溃和梯度爆炸 |
| **效率更高** | 相比 CDE-based 判别器，Hermite 判别器参数更少、前向传播更快，显著降低训练时间和资源消耗 |
| **理论保障强** | 提供了 Hermite 展开收敛性的理论证明（Theorem 2）以及判别器判别力的数学依据（Theorem 3） |

---

## 2. 核心实验方法和设置

### 📚 数据集

#### 合成数据集（Simulation Datasets）
遵循 Yacine Ait-Sahalia [30] 设计四类经典 SDE 模型：
| 名称 | SDE 形式 | 应用背景 |
|------|--------|---------|
| **GBM** (Geometric Brownian Motion) | $ dX_t = \mu dt + \sigma dW_t $ | 股价建模 |
| **OU** (Ornstein-Uhlenbeck) | $ dX_t = \kappa(\alpha - X_t)dt + \sigma dW_t $ | 均值回归过程 |
| **CIR** (Cox-Ingersoll-Ross) | $ dX_t = \kappa(\theta - X_t)dt + \sigma\sqrt{X_t}dW_t $ | 利率/波动率建模 |
| **Polynomial Drift** | 多项式漂移项 + 非线性扩散 | 复杂非线性系统 |

每组数据包含 20,000 训练样本 + 6,000 测试样本，输入为前 100 时间步，预测后 50 步。

#### 真实世界数据集
| 名称 | 描述 |
|------|------|
| **Stock-AAL / Stock-ADBE** | 2017–2018 年五分频股票价格（约 4.3 万条），含缺失值，体现金融市场的随机波动特性 |
| **Traffic** | 北京市 2022 年交通流量记录（21,600 条），五分钟粒度，周期性强、无缺失 |

---

### 📊 评估指标
使用四个互补指标衡量模型性能：
| 指标 | 全称 | 含义 |
|------|-----|------|
| **MISE** | Mean Integrated Squared Error | 衡量整体路径拟合精度（越低越好） |
| **TD** | Tail Difference | 评估尾部（>5% 分位数）保真度，反映极端事件建模能力 |
| **MSE** | Mean Squared Error | 点对点误差，关注局部准确性 |
| **MMD** | Maximum Mean Discrepancy | 衡量生成轨迹与真实轨迹之间的分布差异（越小越好） |

---

### ⚖️ 基线方法对比
涵盖主流时间序列与 SDE 建模范式：
| 类别 | 模型 |
|------|------|
| **线性模型** | DLinear |
| **卷积模型** | MICN, TimesNet |
| **注意力机制** | Transformer, Informer, Autoformer |
| **RNN 模型** | SegRNN |
| **状态空间模型** | Mamba |
| **SDE-based 模型** | Latent-SDEs, GAN-SDEs (CDE-based) |

所有模型共享相同的 Neural SDE 生成器结构以确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能表现（见 Table 1 & Table 3）

#### 在合成数据上的结果（Table 1）
| 模型 | GBM-MISE ↓ | OU-TD ↓ | CIR-MMD ↓ | PolyDrift-MSE ↓ |
|------|------------|----------|-----------|----------------|
| DLinear | 6.00 | 9.09 | 0.31 | 0.08 |
| Transformer | 0.74 | 0.70 | 1.44 | 0.02 |
| Latent-SDEs | 0.44 | 10.00 | 0.93 | 0.01 |
| GAN-SDEs (CDE) | 0.38 | 10.00 | 0.88 | 0.01 |
| **HGAN-SDEs** | **0.17** ✅ | **0.33** ✅ | **0.02** ✅ | **0.01** ✅ |

> ✅ **HGAN-SDEs 在所有合成任务上均取得最优或次优结果，尤其在尾部建模（TD）和分布一致性（MMD）方面大幅领先**

#### 在真实数据上的结果（Table 3）
| 模型 | Stock-AAL-MISE ↓ | Stock-ADBE-MSE ↓ | Traffic-MMD ↓ |
|------|------------------|------------------|---------------|
| Transformer | 59.80 | 4.86e⁻² | 1.00 |
| Latent-SDEs | 35.52 | 0.35e⁻² | 0.95 |
| GAN-SDEs | 38.00 | 0.38e⁻² | 0.97 |
| **HGAN-SDEs** | **2.89** ✅ | **0.08e⁻²** ✅ | **0.02** ✅ |

> ✅ **在金融和交通场景下，HGAN-SDEs 显著优于其他模型，尤其是在高噪声、非平稳的金融市场中表现出极强鲁棒性**

---

### 🔬 消融实验与进一步探索

#### （1）Hermite 函数项数的影响（Figure 2）
- 当 Hermite 项数从 1 增加到 3～4 时，MISE 快速下降；
- 超过 4 项后性能趋于饱和甚至轻微下降；
- **结论**：仅需 3–4 个 Hermite 项即可实现高效且稳定的建模，无需高阶展开。

#### （2）计算效率对比（Table 4）
| 模型 | GPU-hours | MISE ↓ |
|------|-----------|--------|
| LSTM 判别器 | 6.5 | 1.12 |
| MLP 判别器 | 4.8 | 0.95 |
| GAN-SDEs (CDE) | 10.0 | 0.11 |
| **HGAN-SDEs (Hermite)** | **1.2** ✅ | **0.04** ✅ |

> ✅ **HGAN-SDEs 不仅性能最佳，而且训练成本最低，仅为 CDE 方法的 1/8**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Hermite 函数是建模 SDE 路径分布的理想工具**：
   - 其正交性和完备性使得密度估计稳定且高效
   - 可解析地关联 Fokker-Planck 方程与稳态解
2. **HGAN-SDEs 实现了精度与效率的双重突破**：
   - 在多种 SDE 动态系统中达到 SOTA 性能
   - 收敛速度快、资源消耗低，适合大规模部署
3. **对抗训练稳定性显著增强**：
   - 相比 CDE-based GAN-SDEs 更少出现训练崩溃
   - 即使在高度随机的真实金融数据中也能保持稳健输出

---

### ⚠️ 方法的局限性
- **依赖平稳假设**：当前 Hermite 展开主要针对稳态（stationary）过渡密度推导，在快速变化或非平稳系统中可能需要扩展
- **固定基函数结构**：虽然 Hermite 是通用基底，但在特定领域（如跳跃扩散过程）可能不如自适应基函数灵活
- **仅适用于一维或低维系统**：高维 SDE 中 Hermite 基的数量呈指数增长，面临“维度灾难”

---

### 🔮 未来工作方向
1. **推广至多维 SDE 系统**：设计张量化或稀疏化的多变量 Hermite 展开
2. **引入可学习基函数**：将 Hermite 参数也纳入端到端训练，提升适应性
3. **融合物理先验**：结合领域知识（如市场微观结构）构建约束型 Hermite 扩展
4. **应用于更多实际场景**：如高频交易建模、气候模拟、生物动力学等

---

## ✅ 总结
**HGAN-SDEs** 是一个兼具理论严谨性与工程实用性的新型 GAN 框架，通过引入 **Neural Hermite Functions** 作为判别器，成功解决了 Neural SDEs 在对抗训练中的稳定性与效率瓶颈。实验表明该方法在合成与真实数据上均显著优于现有 SOTA 模型，尤其在金融等高噪声、强随机环境中展现出卓越的建模能力和鲁棒性，为连续时间随机系统的深度学习建模提供了新范式。

</details>

---

### 10. [Scaling Reinforcement Learning for Content Moderation with Large Language Models](https://arxiv.org/abs/2512.20061)

**Authors**: Hamed Firooz, Rui Liu, Yuchen Lu, Zhenyu Hou, Fangzhou Xiong, Xiaoyang Zhang, Changshu Jian, Zhicheng Zhu, Jiayuan Ma, Jacob Tao, Chaitali Gupta, Xiaochang Peng, Shike Mei, Hang Cui, Yang Qin, Shuo Tang, Jason Gaedtke, Arpit Mittal  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.20061v1  

#### Abstract
Content moderation at scale remains one of the most pressing challenges in today's digital ecosystem, where billions of user- and AI-generated artifacts must be continuously evaluated for policy violations. Although recent advances in large language models (LLMs) have demonstrated strong potential f...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Scaling Reinforcement Learning for Content Moderation with Large Language Models*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于**大规模内容审核（content moderation）中的核心挑战**：在标签稀疏、政策定义动态变化、且需要复杂推理的现实场景中，如何高效训练大型语言模型（LLMs）以实现专家级准确度。传统方法如监督微调（SFT）受限于标注成本高、泛化能力弱，难以应对细微语义判断和多条件策略逻辑。

### 提出的新方法与创新思路
作者系统性地探索了**强化学习（Reinforcement Learning, RL）在工业级内容审核中的可扩展性**，提出了一套完整的RL训练框架，其核心创新包括：

- **多维度奖励塑造（Reward Shaping）**：结合四种奖励信号构建复合目标函数：
  - `R_acc`：最终预测准确性（verifiable reward）
  - `R_fmt`：输出格式一致性
  - `R_len`：鼓励合理推理长度，防止“长度坍缩”
  - `R_rub`：基于评分标准（rubric-based）的LLM-as-judge奖励，评估推理链的质量（如事实性、忠实度）

- **对抗RL常见失败模式的有效策略**：
  - 使用**蒙特卡洛采样（Monte-Carlo sampling）** 缓解双峰概率分布问题，提升置信度校准。
  - 引入**反思提示（Reflection-aided Prompting）**，让模型先生成初步判断，再通过子标签进行证据回顾，最后输出最终决策，改善得分稳定性。
  - 采用**序列级奖励归一化（sequence-level normalization）** 避免因响应长度差异导致的优化偏差。

- **数据效率优化机制——Disagreement Filtering**：
  利用预训练模型对同一输入多次采样，识别“分歧样本”（disagreement examples），即不同推理路径产生不一致预测的样本。这类样本被认为具有更高学习价值，仅用少量此类数据即可达到甚至超越全量训练效果。

### 相比现有方法的优势
- **极高的数据效率**：相比SFT，RL方法实现了 **10×–100× 的数据效率提升**，尤其适用于专家标注稀缺的场景。
- **更强的复杂推理能力**：在需多层次政策解读的任务上显著优于SFT。
- **更稳定的训练过程**：通过综合奖励设计有效缓解了reward hacking、length collapse等问题。
- **可预测的扩展行为**：RL表现出sigmoid-like scaling规律，为资源分配提供实用指导。

---

## 2. 核心实验方法和设置

### 数据集
实验基于 **Meta Platforms, Inc. 的三个真实生产环境中的内容审核任务（Task1, Task2, Task3）**，均来自实际用户/AI生成内容，涉及政策违规分类（binary classification），例如仇恨言论、虚假信息等。所有任务均使用高质量人工标注作为ground truth。

### 实验设置
- **模型架构**：主要使用 Qwen2.5-VL-7B 和 Qwen-3-8B 等开源LLM。
- **RL算法**：采用 **Group Relative Policy Optimization (GRPO)**，相较于PPO无需显式值函数，计算更高效且适合分布式训练。
- **训练框架对比**：比较了 HuggingFace TRL 与 **VeRL**（HybridFlow驱动），结果显示VeRL吞吐量最高达2.5×优势。
- **输入形式**：将任务建模为条件分类问题，prompt结构包含指令、政策描述、内容及结构化输出要求（JSON格式）。
- **输出结构**：模型输出包含推理链（reasoning trace）、预测标签 `y ∈ {0,1}` 及其对应概率 `p`。

### 评估指标
- **PRAUC**（Precision-Recall AUC）：衡量整体分类性能。
- **R@P90**：在保证Precision ≥ 90%时的Recall，反映高精度约束下的召回能力。
- **F1 Score**：平衡精确率与召回率。
- **Faithfulness & Factuality**：通过LLM-as-judge（Gemini-2.5-Pro）和HHEM模型评估推理忠实性和事实性。
- **Throughput (tokens/s/GPU)**：用于评估训练效率。

### 基线方法对比
- **SFT-COT**：基于思维链（Chain-of-Thought）的监督微调。
- **Zero-shot SFT / RL**：无监督或小样本初始化下的表现。
- **SFT → RL**：两阶段训练（先SFT后RL）。
- **RL-Only**：直接在基础模型上应用RL。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | 数据规模 | 性能表现 |
|------|----------|---------|
| **RL-Only** | 数百样本 | 匹配甚至超过数万样本训练的SFT模型 |
| **SFT → RL** | 千级样本 + RL | R@P90 提升5–15个百分点 |
| **Disagreement Filtering + RL** | 仅61个“分歧样本” | F1 ≈ 0.88，接近全量数据训练的SFT模型 |

#### 表：Task1 上不同奖励配置的性能对比（Qwen-38B）
| Reward Setup | Recall | Precision | F1 |
|-------------|--------|-----------|-----|
| R_acc + R_fmt (Baseline) | 0.58 | 0.43 | 0.49 |
| + R_len | 0.61 | 0.41 | 0.49 |
| + R_rub (完整奖励) | **0.71** | **0.54** | **0.61** |

> ✅ 添加 `R_rub` 后F1提升 **12%**，表明rubric-based reward对质量提升至关重要。

#### 表：不同rollout数量下的性能（Task2）
| Rollouts (N) | R@P90 |
|--------------|-------|
| 8            | 0.53  |
| 32           | 0.63  |
| 64           | 0.62  |
| 128          | 0.64  |

> 📈 性能在N=32附近趋于饱和，体现**边际收益递减**。

#### 消融实验关键发现
- **Monte Carlo Sampling**：当N=4、T=1.0时性能达到平台期，进一步增加rollouts收益有限。
- **Sampling Temperature**：最优温度区间为 **0.7–1.0**；高于1.0会导致解析错误增多。
- **Effective Batch Size**：从128增至1024时，Task1的R@P90从0.18跃升至0.81；超过2048后基本饱和。
- **Disagreement Filtering**：去除“困难样本”反而提升性能，说明噪声标签可能干扰RL优化。

---

## 4. 关键结论和发现

### 主要发现
1. **RL展现出sigmoid-like scaling behavior**：
   - 随着训练数据、rollout数量、优化步数增加，性能平滑上升并逐渐饱和，为工业部署提供了可预测的扩展蓝图。

2. **RL在数据效率方面远超SFT**：
   - 在数百样本下即可匹敌数万样本的SFT模型，**实现10×–100×的数据效率增益**，特别适合标注成本高昂的内容审核领域。

3. **奖励设计决定成败**：
   - 单纯依赖accuracy reward易引发reward hacking和length collapse；
   - 引入`R_rub`（rubric-based）和`R_len`能显著提升推理质量和模型鲁棒性。

4. **RL本质是response selector而非capability enhancer**：
   - 分析表明，RL更多是提高了从多个候选生成中选出正确答案的概率（pass@N），而非根本增强推理能力。

5. **Disagreement Filtering极大提升样本效率**：
   - 仅使用“模型内部存在分歧”的样本进行训练，可在极小数据量下实现高性能，是未来低资源RL的重要方向。

### 方法的局限性
- **依赖高质量judgment infrastructure**：`R_rub`依赖LLM-as-judge或人工评分，增加了系统复杂性和延迟。
- **对hard examples敏感**：某些极端难例可能导致不稳定更新，需谨慎过滤。
- **compute overhead较高**：尽管VeRL提升了吞吐，但MC sampling和多rollout仍带来推理开销。
- **无法完全解决幻觉问题**：虽然rubric reward有助于控制，但faithfulness与factuality之间仍存在权衡。

### 未来工作方向
- 探索自动化rubric生成与轻量化judge模型，降低reward成本。
- 将该框架扩展至多模态内容审核（text+image/video）。
- 结合在线学习机制，适应快速演化的政策定义。
- 进一步研究RL是否真正增强了模型的内在推理能力，还是仅优化了输出选择策略。

--- 

> 🔚 **总结**：本论文首次系统验证了**强化学习在工业级内容审核中的规模化可行性**，不仅展示了其卓越的数据效率和推理能力，还提出了多项稳定训练的关键技术（如rubric reward、reflection prompting、disagreement filtering），为构建下一代智能审核系统提供了坚实的技术路径。

</details>

---

### 11. [FastMPS: Revisit Data Parallel in Large-scale Matrix Product State Sampling](https://arxiv.org/abs/2512.20064)

**Authors**: Yaojian Chen, Si-Qiu Gong, Lin Gan, Yanfei Liu, An Yang, Yinuo Wang, Chao-yang Lu, Guangwen Yang  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.20064v1  

#### Abstract
Matrix Product State (MPS) is a versatile tensor network representation widely applied in quantum physics, quantum chemistry, and machine learning, etc. MPS sampling serves as a critical fundamental operation in these fields. As the problems become more complex, the scale of MPS is rapidly increasin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FastMPS: Revisit Data Parallel in Large-scale Matrix Product State Sampling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Matrix Product State (MPS)** 采样在大规模场景下面临严重瓶颈：
- **Data Parallelism** 受限于内存和高 I/O 开销，当 MPS 张量过大无法全部放入显存时，频繁的磁盘交换导致性能急剧下降。
- **Model Parallelism** 虽能处理大尺度 MPS，但要求进程数严格等于站点数（$M$），缺乏可扩展性，且存在启动延迟、负载不均和通信开销高等问题。

随着量子实验（如 Gaussian Boson Sampling）规模扩大（站点 $M \sim 8000$，bond 维度 $\chi \sim 10^4$，样本数 $N \sim 10^7$），现有方法难以高效支持。

---

### 提出的新方法与思路
本文提出 **FastMPS** —— 一种多级并行框架，重新激活大规模 MPS 采样的 **Data Parallelism**，其核心设计为：

#### （1）**Multi-level Parallel Framework**
- **Task-level Data Parallelism**：将独立样本分布在多个进程组中，实现跨样本并行。
- **Tensor-level Tensor Parallelism**：沿 bond 维度对 MPS 张量进行切分，在组内采用张量并行加速单个样本计算。
- 支持灵活的进程配置（不再绑定 $p = M$），显著提升可扩展性和资源利用率。

#### （2）**Adaptive Mixed Precision Strategy**
- 在保持数值稳定性的前提下，使用 **TF32** 进行计算，并通过 **sample-wise 动态缩放** 防止下溢。
- 存储采用 **FP16** 压缩 MPS 张量，减少 I/O 和通信开销。
- 利用 Tensor Core 加速 GEMM 操作，大幅提升计算吞吐。

#### （3）**Customized Kernels & Dynamic Bond Dimension**
- 针对 GBS 中的 displacement operator 设计近似算法（基于 Zassenhaus 公式分解），将矩阵指数运算复杂度大幅降低。
- 引入 **dynamic bond dimension**，根据 entanglement 分布动态调整各站点的 $\chi$，避免边缘区域冗余计算。

---

### 相比现有方法的优势
| 方面 | 传统方法（如 [19]） | FastMPS |
|------|---------------------|---------|
| 可扩展性 | 固定 $p = M$，不可扩展 | 支持任意进程数，强弱扩展性优异 |
| 内存与 I/O | 高频磁盘读取，I/O 密集 | 压缩 + 重叠优化，缓解 I/O 压力 |
| 数值效率 | 多数使用 FP64，性能受限 | 自适应混合精度，释放 Tensor Core 性能 |
| 通信模式 | Pipeline 式点对点通信，启动延迟高 | 数据并行 + 集体通信，易于重叠 |
| 灵活性 | 仅适用于特定硬件配置 | 跨平台兼容（CPU/GPU/Sunway/Tianhe） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- 主要基准任务：**Gaussian Boson Sampling (GBS)**，包括：
  - **Jiuzhang 系列**（中国科大光量子计算机）
  - **Borealis**（Xanadu 光子芯片）
  - 自定义大规模模拟数据：**8176-site GBS**，含 16.54 个实际压缩光子（actual squeezed photons）

这些数据具有标准化输入、真实物理背景，适合作为高性能仿真基准。

---

### 实验设置与评估指标

#### 硬件平台
- **GPU 测试**：单卡 A100（40GB），NVLink 连接多卡
- **CPU 测试**：Intel Xeon Gold 6230R @ 2.10GHz
- **超算平台**：
  - **Tianhe-3**：最多 15 节点（375 核）
  - **Sunway TaihuLight**：最多 500 进程（32500 核）

#### 参数设置
- 物理维度 $d = 3$ 或 $4$
- bond dimension $\chi = 10^3 \sim 10^4$
- 样本总数 $N = 10^5 \sim 10^7$
- micro batch size $N_2 = 10^3 \sim 2\times10^4$

#### 评估指标
- **运行时间（Time-to-solution）**
- **加速比（Speedup）**
- **强/弱扩展效率（Strong/Weak Scaling Efficiency）**
- **截断误差（Truncation Error）**
- **相关函数一致性验证（1st/2nd-order Correlation）**

---

### 基线方法对比
- **[19]**：当前最先进的 MPS-based GBS 模拟器，采用 model parallelism + pipeline 设计
- 对比方式：
  - 相同参数下的端到端运行时间
  - 单位资源下的性能（如每 GPU 时间）
  - 扩展能力（最大支持规模）

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 成功实现了 **8176-site、$\chi=10^4$** 的 GBS 模拟，远超此前 state-of-the-art（约 288-site）。
- 在单张 A100 GPU 上完成 10 million 样本生成仅需 **约 14 小时**（Borealis-288 场景），是目前最快实现。
- 使用 8 块 A100 GPU 完成 Jiuzhang2 的 10M 样本采样耗时 **38.57 分钟**。

---

### 与基线方法的对比结果（Table 2 & 3）

| GBS 实例 | [19] 时间（分钟） | Fast-MPS-8 时间（分钟） | 加速比 |
|--------|------------------|------------------------|-------|
| Jiuzhang2 | 62（使用 144 GPUs） | 38.57（使用 8 GPUs） | **>10×**（按等效资源估算） |
| B-M288 | 62（使用 288 GPUs） | 247.43（使用 8 GPUs） | **~10.6×**（假设 95% 并行效率） |

> 注：Fast-MPS 用更少 GPU 达到更快速度，体现极高资源利用率。

#### CPU 对比（Table 3）
| 实例 | [19] 时间 | Fast-MPS 时间 | 加速比 |
|------|----------|---------------|--------|
| Jiuzhang2-P65-1 | 17.72h | 1.76h | **10.06×** |
| B-M288 | 36.44h | 4.504h | **8.09×** |

---

### 消融实验结果（Figure 11）
在 A100 上对三大优化进行消融测试（固定 $d=4,\chi=10^4$, 40万样本）：

| 优化项 | 移除后性能下降倍数 | 贡献加速比 |
|--------|--------------------|------------|
| Dynamic Bond Dimension | 下降 2.6× | **~2.6×** |
| Optimized expm | 下降 1.9× | **~1.9×** |
| Adaptive Mixed Precision | 下降 5.1× | **~5.1×** |

> 结果表明：**混合精度贡献最大**，说明数值稳定性设计至关重要；三项优化呈乘法效应，共同带来总体 >10× 提升。

---

### 扩展性表现（Figure 12）
- **Weak Scaling**：样本随进程线性增长，效率 >95%
- **Strong Scaling**：总样本固定，加速接近理想曲线，效率 >95%
- 支持上千进程扩展，在 Sunway 上实现全 8176-site 规模仿真

---

## 4. 关键结论和发现

### 主要发现
1. **Data Parallelism 可在大规模 MPS 中复活**：通过压缩、重叠和 tensor parallelism 协同，解决了传统 data parallel 的 I/O 和内存瓶颈。
2. **混合精度可行且必要**：通过 sample-wise 缩放机制，可在数千站点上安全使用 TF32，充分发挥 Tensor Core 性能。
3. **dynamic bond dimension 显著降低计算复杂度**：Table 1 显示计算步骤减少达 **80%**，尤其在低纠缠区域效果明显。
4. **FastMPS 实现前所未有的模拟规模**：首次实现 **8176-site、$\chi=10^4$** 的 GBS 模拟，推动经典模拟边界。

---

### 方法的局限性
1. **仍依赖高质量 MPS 表示**：若原始量子态无法被 MPS 高效表示（如高度纠缠态），则本方法不适用。
2. **通信敏感**：tensor parallelism 中 ReduceScatter 对带宽敏感，在 PCIe 等低带宽连接下性能下降明显。
3. **暂未支持 FP16/BF16 全流程**：因 ComplexHalf 支持不足及样本内动态范围过大，尚未全面启用半精度训练级格式。

---

### 未来工作方向
1. **探索与 AI 推理系统的共通性**：将 MPS 视为“物理神经网络”，借鉴 Transformer 推理中的 continuous batching、KV Cache 等技术。
2. **异构计算融合**：结合 CPU + GPU + ASIC 架构，进一步提升能效比。
3. **自动并行策略选择器**：基于硬件拓扑自动判断使用 single-site vs double-site tensor parallelism。
4. **拓展至其他 tensor network 应用**：如 PEPS、MERA、QUICKER 等更复杂的网络结构。

---

> ✅ **总结一句话**：  
> FastMPS 通过创新的多级并行架构与混合精度策略，成功将 data parallelism 重新带回大规模 MPS 采样领域，在 GBS 等关键应用中实现 **>10× 性能提升**，并突破性地支持 **8176-site** 规模仿真，为量子优势验证提供了强大的经典工具。

</details>

---

### 12. [OpComm: A Reinforcement Learning Framework for Adaptive Buffer Control in Warehouse Volume Forecasting](https://arxiv.org/abs/2512.19738)

**Authors**: Wilson Fung, Lu Guo, Drake Hilliard, Alessandro Casadei, Raj Ratan, Sreyoshi Bhaduri, Adi Surve, Nikhil Agarwal, Rohit Malshe, Pavan Mullapudi, Hungjen Wang, Saurabh Doodhwala, Ankush Pole, Arkajit Rakshit  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.19738v1  

#### Abstract
Accurate forecasting of package volumes at delivery stations is critical for last-mile logistics, where errors lead to inefficient resource allocation, higher costs, and delivery delays. We propose OpComm, a forecasting and decision-support framework that combines supervised learning with reinforcem...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*OpComm: A Reinforcement Learning Framework for Adaptive Buffer Control in Warehouse Volume Forecasting*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**最后一公里物流中包裹量预测不准导致资源分配低效、成本上升和交付延迟**的问题，特别是在仓库配送站点（delivery stations）层面的**缓冲区管理（buffer control）决策不科学**的挑战。传统方法依赖人工经验或启发式规则，难以适应站点间的异质性和需求波动。

### 提出的新方法与创新思路
作者提出了 **OpComm** —— 一个融合监督学习、强化学习与生成式AI的**自适应缓冲控制框架**，其核心创新包括：

- **将缓冲区决策建模为上下文强化学习（contextual reinforcement learning）问题**：以 LightGBM 预测的需求作为状态输入，由 PPO（Proximal Policy Optimization）智能体选择最优缓冲等级。
- **设计非对称奖励函数（asymmetric reward function）**：对“缓冲不足”（under-buffering）施加比“缓冲过度”更重的惩罚（α > β），反映现实中未满足需求带来的更高运营风险。
- **引入蒙特卡洛更新机制实现策略持续适应**：通过实际站点反馈进行策略迭代，增强在非平稳环境中的鲁棒性。
- **集成生成式AI解释模块提升可解释性与决策支持能力**：利用 SHAP 特征归因生成高管级摘要和情景分析，帮助运营人员理解模型决策逻辑，促进落地采纳。

### 相比现有方法的优势
- **超越纯预测模型**：不仅提供预测，还输出可执行的缓冲建议，形成端到端决策支持系统。
- **优于传统RL方法**：采用 PPO 而非 DQN 或 TRPO，在离散动作空间下兼具稳定性与效率。
- **兼顾准确性与实用性**：结合 LightGBM 的高精度预测与 RL 的动态优化，并通过生成式AI桥接技术与业务之间的鸿沟。

---

## 2. 核心实验方法和设置

### 数据集
- 实验基于 **北美地区超过400个仓库配送站点的历史运营数据**。
- 数据包含时间序列特征（如周期性、季节性）、滞后指标（历史需求波动、容量利用率）以及站点特定操作约束。

### 实验设置
- **模型架构**：
  - **LightGBM 回归模型**用于生成各站点的包裹量预测，作为 PPO 智能体的状态输入。
  - **PPO 智能体**从预定义的离散缓冲等级集合 {b₁, b₂, ..., bk} 中选择动作。
  - 奖励函数定义如下：
    $$
    r_t = -\alpha \cdot \max(0, D_t - \hat{D}_t - b_t) + \beta \cdot \max(0, b_t + \hat{D}_t - D_t)
    $$
    其中 α > β，强调避免 under-buffering。
- **训练方式**：采用 **Monte Carlo rollouts** 计算经验回报，无需显式建模需求动态，适用于非平稳环境。
- **可解释性模块**：基于 SHAP 值驱动生成式AI生成自然语言报告。

### 评估指标
- **WAPE（Weighted Absolute Percentage Error）**：衡量预测准确性。
- **WAPE 标准差**：评估跨站点稳定性。
- **Under-buffering 和 Over-buffering 百分比**：衡量缓冲策略的风险表现。
- 对比人工预测（manual forecasting）作为基线。

### 基线方法
- **Manual Forecasting**：当前行业常用的人工/启发式预测与缓冲设定方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 区域       | 方法      | WAPE (%) | Under-buffering (%) | Over-buffering (%) |
|------------|-----------|----------|----------------------|---------------------|
| North-East | Manual    | 4.95     | 18.3                 | 12.5                |
|            | OpComm    | **3.85** | **10.2**             | 15.1                |
| Mid-West   | Manual    | 5.12     | 20.1                 | 13.0                |
|            | OpComm    | **3.90** | **11.0**             | 14.2                |
| South      | Manual    | 4.88     | 16.7                 | 11.9                |
|            | OpComm    | **3.70** | **9.5**              | 13.5                |
| West       | Manual    | 4.99     | 17.9                 | 12.7                |
|            | OpComm    | **3.80** | **10.0**             | 14.0                |

### 总体对比结果
- **WAPE 平均下降 21.65%**（从 4.95% → 3.85%）。
- 在 **93.7% 的站点上提升了预测准确率**。
- **显著降低 under-buffering 发生率**（平均降幅约 8 个百分点），表明策略更具保守性和风险规避特性。
- 尽管 over-buffering 略有上升，但整体资源浪费代价低于缺货风险，符合设计目标。

> ⚠️ 注：文中未提及消融实验（ablation study），因此无法提供相关结果。

---

## 4. 关键结论和发现

### 主要发现
- **OpComm 显著优于人工预测方法**，在多个地理区域均表现出更高的预测精度和更低的操作风险。
- 引入 **PPO 与非对称奖励机制** 可有效引导智能体学习偏向安全的缓冲策略，契合现实物流场景的成本不对称性。
- **生成式AI解释层增强了系统的透明度与可信度**，有助于推动算法在运营管理中的实际部署。
- 整体框架展示了如何将 **统计建模、强化学习控制与人机协同决策支持** 有机结合，解决高风险运营环境下的复杂决策问题。

### 方法的局限性
- 未验证其他 policy optimization 算法（如 SAC、A2C）的表现差异。
- 动作空间为离散型缓冲等级，可能限制细粒度调控能力。
- 缺乏跨站点协同优化机制，目前为单站独立决策。
- 未进行严格的用户研究来评估生成式AI模块的实际可用性。

### 未来工作方向
- 探索 **meta-learning** 用于动态特征表示，提升跨站点泛化能力。
- 研究 **替代性的策略优化算法**，进一步提高学习效率与稳定性。
- 扩展至 **网络层级协调控制**，实现多站点联合调度与资源调配。
- 开展 **可解释性方法的形式化评估**，量化生成式AI对决策质量的影响。

---

> ✅ **总结一句话**：  
> OpComm 成功地将 LightGBM 预测、PPO 强化学习与生成式AI解释相结合，构建了一个高效、稳健且可解释的仓储体积预测与缓冲控制框架，在真实物流场景中实现了显著的性能提升与决策支持价值。

</details>

---

### 13. [Learning to Design City-scale Transit Routes](https://arxiv.org/abs/2512.19767)

**Authors**: Bibek Poudel, Weizi Li  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.19767v1  

#### Abstract
Designing efficient transit route networks is an NP-hard problem with exponentially large solution spaces that traditionally relies on manual planning processes. We present an end-to-end reinforcement learning (RL) framework based on graph attention networks for sequential transit network constructi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learning to Design City-scale Transit Routes*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对**Transit Route Network Design Problem (TRNDP)**，即城市公交线路网络设计问题。该问题是 **NP-hard** 的组合优化问题，具有指数级庞大的解空间，传统上依赖人工规划，效率低且难以达到全局最优。

### 🚀 提出的新方法与创新点
1. **端到端强化学习框架（End-to-End RL Framework）**  
   首次提出一个完整的、基于 **Graph Attention Networks (GAT)** 和 **Proximal Policy Optimization (PPO)** 的端到端 RL 框架，用于**顺序构建城市尺度的公交网络**。

2. **两层级奖励机制（Two-Level Reward Structure）**  
   为解决长周期决策中的**稀疏奖励（sparse reward）与信用分配（credit assignment）难题**，引入：
   - **增量拓扑反馈（Incremental Topological Feedback）**：在每一步扩展路线时提供轻量级奖励（如需求覆盖、避免重叠）；
   - **仿真终端奖励（Simulation-Based Terminal Rewards）**：在完整网络生成后通过交通模拟器获取真实性能反馈（如服务率、等待时间等）。

3. **可扩展的图注意力策略架构（Scalable Graph Attention Policy）**  
   设计了一个参数数量与输入网络规模无关的策略网络，使其具备良好的**跨城市迁移潜力**。

4. **发布真实世界数据集（Real-World Dataset）**  
   构建并公开发布了来自美国印第安纳州布卢明顿（Bloomington）的真实数据集，包含：
   - 准确的道路拓扑（143节点，243条边）
   - 基于人口普查的 Origin-Destination (OD) 需求矩阵
   - 当前运营的16条公交线路 GTFS 数据

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本论文方法 |
|------|--------|-----------|
| 方法类型 | 手工启发式 / 元启发算法（GA, SA） | 端到端深度强化学习 |
| 奖励机制 | 仅最终评估（稀疏反馈） | 两级奖励（中间+终端），缓解信用延迟 |
| 数据基础 | 合成网络（如 Mandl 网络） | 真实城市拓扑 + 真实 OD 需求 |
| 可扩展性 | 固定算子，需调参 | 图神经网络，参数独立于城市大小 |
| 性能表现 | 局部最优，难以平衡多目标 | 自动学习权衡乘客与运营商利益 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **名称**：Bloomington Transit Dataset（作者自建）
- **来源**：
  - 路网：TIGER/Line Shapefiles（U.S. Census Bureau）
  - OD 需求：LEHD LODES 数据（2022年通勤流，经1.5倍非通勤流量放大）
  - 公交线路：Bloomington Transit GTFS 开放数据
- **规模**：143个节点，243条双向边，约5,738个OD对
- **特点**：首次将真实道路拓扑、真实出行需求与实际公交系统结合用于 TRNDP 研究

### ⚙️ 实验设置
- **环境模拟器**：UXsim（轻量级中观交通仿真器，基于运动波理论）
- **训练过程**：
  - 每 episode 设计 16 条线路，每条最多 14 个节点
  - 使用 PPO 算法训练，共 2000 episodes
  - 动作空间采用 **action masking** 技术限制无效选择
- **频率设定**：使用 **最大负载原则（max-load principle）** 在路线设计完成后确定发车频率，避免频率干扰学习信号

### 🎯 评估指标（Passenger & Operator Oriented）
| 类别 | 指标 | 定义 |
|------|------|------|
| **乘客侧** | Service Rate (%) | 成功上车乘客占总需求比例 |
| | Wait Time (min) | 平均候车时间 |
| | Transfer Rate (%) | 完成旅程中需要换乘的比例 |
| | Travel Time (min) | 总行程时间（含等待+车内） |
| **运营侧** | Route Efficiency (pax/km) | 每公里线路服务的乘客数 |
| | Fleet Size | 所需公交车总数 |
| | Bus Utilization (%) | 车辆平均载客率 |

### 🆚 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| **Real-world** | 当前布卢明顿实际运行的公交网络（人工设计） |
| **Random Walk** | 每步随机选择可行邻居节点 |
| **Greedy Demand Coverage** | 每步选择能带来最大新增需求连接的节点 |
| **Greedy Shortest Path** | 每步选择最近的可行节点（最小距离优先） |

此外还测试了两种初始化方式：
- **Transit Center Start**：所有线路从交通枢纽出发（匹配现实情况）
- **Random Start**：首站随机选取

以及两种出行模式占比（modal split）：
- `α = 0.3`：混合出行（30%使用公交）
- `α = 1.0`：全公交出行（高采用场景）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（以 `α = 1.0`, Transit Center 初始化为例）

| 指标 | Real-world | RL (Ours) | 提升幅度 |
|------|------------|----------|---------|
| **Service Rate (%)** | 58.20% | **73.10%** | ↑ **25.6%** |
| **Wait Time (min)** | 15.87 | **10.96** | ↓ **30.9%** |
| **Travel Time (min)** | 52.85 | 53.04 | ≈持平（+0.4%） |
| **Route Efficiency (pax/km)** | 62.74 | **72.06** | ↑ **14.9%** |
| **Bus Utilization (%)** | 32.15% | **38.89%** | ↑ **21.0%** |
| **Fleet Size** | 281 | 281 | 相同 |

> ✅ 在高公交采用率下，RL 设计显著优于现实网络，在服务率、等待时间和利用率方面均有大幅提升。

### 🔁 混合模式下的表现（`α = 0.3`, Random Initialization）

| 对比项 | 结果 |
|-------|------|
| vs. **Demand Coverage Heuristic** | Route Efficiency 提升 **68.8%** |
| vs. **Shortest Path Heuristic** | Travel Time 降低 **5.9%** |
| vs. Real-world Network | Service Rate ↑6.9%，Wait Time ↓27.3% |

> ✅ 即使在更复杂、无先验起点的情况下，RL 仍能发现优于各类启发式的权衡方案。

### 📊 图形化分析发现
- RL 设计的网络：
  - 覆盖更多边缘区域（新增27个节点服务）
  - 更少重复线路（仅39条边被多线共享 vs. 现实47条）
  - 分布更均匀（19.7%节点被≥3条线服务 vs. 现实22.8%）
- 表明 RL 学会了**减少冗余、扩大覆盖范围、提升资源利用效率**

### ❌ 潜在代价
- Transfer Rate 略有上升（+0.8~6.1个百分点），说明更多乘客需换乘
- 但在 `α=0.3` 下 Route Efficiency 略低（因增加车辆协调成本）

> 尽管换乘增多，但总旅行时间几乎不变，表明系统实现了有效的换乘衔接。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **端到端 RL 可有效解决大规模 TRNDP**  
   在真实城市尺度上，RL 能自动学习高质量的公交网络设计策略，超越人类专家设计和传统启发式方法。

2. **两层级奖励机制至关重要**  
   中间拓扑奖励帮助引导探索方向，终端仿真奖励确保最终性能可信，二者结合显著提升了学习效率与稳定性。

3. **RL 能实现更优的多目标权衡**  
   在乘客体验（服务率、等待时间）与运营商效率（车辆利用率、线路效率）之间找到更好平衡，尤其在高公交分担率场景下优势明显。

4. **真实数据驱动的设计更具实用性**  
   基于真实路网与需求的设计更能反映现实交通动态，为未来智能交通系统提供了可靠验证平台。

### ⚠️ 局限性
1. **静态需求假设**：当前模型基于固定高峰小时需求，未考虑时间变化或随机波动。
2. **单城市验证**：目前仅在布卢明顿这一中等城市测试，尚未推广至更大或更复杂的城市。
3. **忽略动态调度细节**：如实时延误、信号控制、站点容量等未纳入建模。
4. **计算开销较高**：训练耗时约48小时（RTX 4090 + i9 CPU），限制快速迭代。

### 🔮 未来工作方向
1. 扩展至多个城市，并集成动态 OD 估计与时间序列预测模块。
2. 引入 **multi-agent RL** 或 **hierarchical RL** 处理线路协同与频率联合优化。
3. 探索与 **mixed traffic dynamics**（自动驾驶、共享出行）的融合设计。
4. 结合 **digital twin** 技术进行在线持续优化与政策推演。
5. 开发轻量化推理版本，支持城市规划者的交互式辅助设计工具。

---

## 💡 总结
该论文成功展示了 **deep reinforcement learning + graph attention + real-world data + traffic simulation** 的强大组合，为城市公共交通网络设计提供了全新的自动化范式。其发布的数据集和代码也为后续研究奠定了坚实基础，标志着城市交通规划正从“经验驱动”迈向“数据与AI驱动”的新时代。

</details>

---

### 14. [A DeepSeek-Powered AI System for Automated Chest Radiograph Interpretation in Clinical Practice](https://arxiv.org/abs/2512.20344)

**Authors**: Yaowei Bai, Ruiheng Zhang, Yu Lei, Xuhua Duan, Jingfeng Yao, Shuguang Ju, Chaoyang Wang, Wei Yao, Yiwan Guo, Guilin Zhang, Chao Wan, Qian Yuan, Lei Chen, Wenjuan Tang, Biqiang Zhu, Xinggang Wang, Tao Sun, Wei Zhou, Dacheng Tao, Yongchao Xu, Chuansheng Zheng, Huangxuan Zhao, Bo Du  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.20344v1  

#### Abstract
A global shortage of radiologists has been exacerbated by the significant volume of chest X-ray workloads, particularly in primary care. Although multimodal large language models show promise, existing evaluations predominantly rely on automated metrics or retrospective analyses, lacking rigorous pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A DeepSeek-Powered AI System for Automated Chest Radiograph Interpretation in Clinical Practice

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **全球放射科医生短缺**，尤其是在基层医疗中，胸片（CXR）解读负担巨大。
- 现有基于AI的CXR报告生成系统多依赖自动化指标或回顾性分析，缺乏**前瞻性临床验证**，难以证明其真实世界中的临床价值。
- 多模态大模型（如ChatGPT 4o）虽具潜力，但存在部署成本高、术语不规范、隐私风险等问题。

### 🆕 提出的新方法与思路
- 开发了 **Janus-Pro-CXR**：一个基于DeepSeek开源多模态大模型Janus-Pro（1B参数）的轻量级、专用于CXR的AI系统。
- 采用**大-小模型协同框架**：
  - 利用专家分类模型注入诊断结果；
  - 统一模型整合多模态临床数据；
  - 显式建模影像阅读逻辑。
- 通过三阶段监督微调（supervised fine-tuning），在MIMIC-CXR、CheXpert Plus和中国多中心CXR-27数据集上完成训练。

### 🔍 相比现有方法的优势
| 维度 | Janus-Pro-CXR优势 |
|------|-------------------|
| **性能表现** | 在自动报告生成指标上超越更大规模模型（如ChatGPT 4o, 200B） |
| **部署可行性** | 仅需GeForce RTX 4060即可实现1–2秒推理延迟，适合资源受限环境 |
| **临床实用性** | 首次进行多中心前瞻性试验（NCT07117266），验证AI辅助对实际工作流的影响 |
| **开放性** | 模型架构与代码将开源（GitHub: https://github.com/ZrH42/Janus-Pro-CXR），促进临床转化 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 描述 |
|--------|------|
| **MIMIC-CXR** | 公开胸部X光数据库，含带自由文本报告的图像；用于前两阶段微调及测试（n=2,365） |
| **CheXpert Plus** | 扩展版CheXpert，增加人口统计学信息和文本报告；用于前期训练 |
| **CXR-27** | 来自中国27家医院的多中心回顾性数据集（n=12,396），用于最终微调与评估 |
| **Prospective Cohort** | 来自3家中国医院的前瞻性队列（n=296患者），用于真实场景下AI协作研究 |

### ⚙️ 实验设置
- **模型结构**：以Janus-Pro（1B）为基础，全参数微调。
- **训练流程**：
  1. 第一阶段：MIMIC-CXR + CheXpert Plus → 基础诊断能力
  2. 第二阶段：CXR-27 → 适应本地写作风格与数据分布
- **部署方式**：独立于PACS系统，通过局域网接收图像与病史，返回报告供医生编辑。

### 🎯 评估指标

#### 自动化评估（Retrospective）
- **RadGraph F1**：医学实体关系抽取准确率
- **Micro/Macro F1-5 & F1-14**：Top 5常见疾病与全部14类疾病的识别精度
- **AUC**：六项关键影像发现的分类性能
- **Cohen's Kappa**：与参考标准的一致性

#### 主观评估（Prospective & Retrospective）
- **Report Quality Score**：五点评分制（Likert scale）
- **Pairwise Preference Test**：专家盲选更优报告
- **RADPEER Agreement Score**：衡量报告差异的临床意义
- **Reading Time**：从阅片到完成报告的时间（秒）

### 🔁 基线方法对比
| 对比对象 | 类型 |
|---------|------|
| **Janus-Pro (1B)** | 原始基础模型（未微调） |
| **ChatGPT 4o (200B)** | 商业通用多模态大模型 |
| **MAIRA-2**, **CheXagent** | 其他CXR专用模型（因不可复现未参与前瞻实验） |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）自动化报告生成性能（CXR-27测试集）
| 指标 | Janus-Pro-CXR |
|------|---------------|
| **RadGraph F1** | **58.6**（所有模型中排名第一） |
| **Micro F1-5** | 63.4（MIMIC-CXR测试集第一） |
| **Macro F1-5** | 55.1（MIMIC-CXR测试集第一） |
| **Micro F1-14** | 59.9（第二名） |
| **Macro F1-14** | 42.3（第二名） |

> 注：显著优于Janus-Pro和ChatGPT 4o，在罕见病识别上也表现出良好平衡性。

#### （2）关键影像发现检测性能（AUC）
| 发现 | AUC |
|------|-----|
| Support Devices | 0.931 |
| Pleural Effusion | 0.931 |
| Pneumothorax | 0.921 |
| Lung Opacity | >0.8 |
| Consolidation | >0.8 |
| Cardiomegaly | >0.8 |

✅ **六项关键发现AUC均 > 0.8**，显示强大诊断能力。

#### （3）前瞻性临床协作结果（n=296）
| 指标 | 标准组（Standard Care） | AI辅助组（AI-Assisted） | P值 |
|------|--------------------------|--------------------------|-----|
| **Report Quality Score** | 4.12 ± 0.80 | **4.36 ± 0.50** | <0.001 |
| **Agreement Score (RADPEER)** | 4.14 ± 0.84 | **4.30 ± 0.57** | <0.001 |
| **Reading Time (秒)** | 147.6 ± 51.1 | **120.6 ± 45.6** | <0.001 |
| **时间减少** | — | **27.0秒（↓18.3%）** | — |
| **复杂病例时间节省**（≥3个发现） | — | ↓32.5秒（↓16.4%） | <0.001 |

> 💡 若每日读200张胸片，可节省约90分钟，可用于疑难病例分析或缓解视觉疲劳。

#### （4）偏好测试结果
- **AI独立生成 vs 发表报告**：30.4%被≥3位专家偏爱
- **AI辅助报告 vs 标准报告**：**54.3%被≥3位专家偏爱**

#### （5）多图输入能力验证
| 场景 | 报告质量评分 |
|------|--------------|
| 历史胸片对比 | 3.19 ± 1.22 |
| PA+Lateral双视图 | **3.42 ± 1.02** |
| 仅PA视图（对照） | 3.23 ± 1.06（P<0.001） |

✅ 表明同时输入正侧位图像能显著提升报告质量。

---

### 🔍 消融实验结果（Ablation Study）
- **Janus-Pro-CXR-Zero**（仅用MIMIC+CheXpert微调）：
  - 在CXR-27测试集中多数指标排名第二，说明加入CXR-27数据后性能进一步提升。
- **轻量化设计有效性**：
  - 尽管仅为1B参数，性能超过更大模型（如3B、7B甚至200B的ChatGPT 4o），表明领域优化比单纯扩参更重要。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Janus-Pro-CXR是首个经过多中心前瞻性验证的CXR报告生成AI系统**，证实其在真实临床环境中有效提升报告质量和工作效率。
2. 轻量级（1B参数）设计实现了高性能与低部署门槛的统一，特别适用于基层医疗机构。
3. AI辅助显著提高初级放射科医生的表现：
   - 报告质量↑0.25分（P<0.001）
   - 阅片时间↓18.3%
   - 更受专家青睐（54.3%胜出）
4. 系统具备良好的多图处理能力（历史图像、正侧位），支持动态变化分析。
5. **肺炎诊断阳性率提升**：AI组为52.4%，对照组为36.1%（P<0.001），提示AI增强了医生决策信心。

### ⚠️ 局限性
1. **对细微或复杂征象识别能力有限**：如肺水肿、胸膜增厚等仍需改进。
2. **纵向历史图像整合功能尚未完全前瞻性验证**：因长期随访数据积累周期长。
3. **参考标准非“金标准”**：依赖已发表报告，可能存在个体判断偏差。
4. **未与其他专用模型（如MAIRA-2、CheXagent）直接比较**：因其不开源或硬件要求过高。
5. 当前模式为“AI生成→医生修改”，尚不能替代人工审核。

### 🔮 未来工作方向
1. 进一步优化模型对**细微病变**的识别能力。
2. 推进**纵向图像序列分析**的前瞻性验证。
3. 探索更高级的人机交互模式：
   - 支持双向实时对话（类似资深会诊专家）
   - 主动提示潜在漏诊或误诊
4. 构建**AI初稿→上级医师终审**的工作流，进一步压缩报告出具时间。
5. 将该框架扩展至其他影像模态（CT、MRI、Ultrasound），打造通用医学影像AI助手。

---

## 总结

> **Janus-Pro-CXR不仅是技术突破，更是迈向“数字同事”（digital colleague）的重要一步**。它通过轻量化架构、领域优化和严格的前瞻性验证，展示了AI在真实临床场景中提升效率、保障质量的巨大潜力，尤其为资源匮乏地区提供了可行解决方案。其开源策略也将推动整个AI辅助放射学领域的透明化与普及化进程。

</details>

---

### 15. [SpidR: Learning Fast and Stable Linguistic Units for Spoken Language Models Without Supervision](https://arxiv.org/abs/2512.20308)

**Authors**: Maxime Poli, Mahi Luthra, Youssef Benchekroun, Yosuke Higuchi, Martin Gleize, Jiayi Shen, Robin Algayres, Yu-An Chung, Mido Assran, Juan Pino, Emmanuel Dupoux  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.20308v1  

#### Abstract
The parallel advances in language modeling and speech representation learning have raised the prospect of learning language directly from speech without textual intermediates. This requires extracting semantic representations directly from speech. Our contributions are threefold. First, we introduce...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SpidR: Learning Fast and Stable Linguistic Units for Spoken Language Models Without Supervision

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Spoken Language Model (SLM)** 主要依赖于从文本中间表示（如ASR输出）进行训练，而理想的目标是直接从原始语音信号中学习语言模型，即“无监督口语建模”（textless spoken language modeling）。然而，现有的自监督语音表示模型（如 wav2vec 2.0、HuBERT）提取的离散语音单元（speech units）存在以下问题：
- 单元不稳定，易发生 **codebook collapse**（码本坍缩），导致聚类质量下降；
- 缺乏对 **音素信息** 的稳定编码能力，难以支持高质量的语言建模；
- 预训练耗时长（如 HuBERT 需要一周以上），限制了快速迭代和研究探索。

### 提出的新方法：SpidR
作者提出 **SpidR**（**S**elf-distilled **P**redictive **R**epresentations），一种新型的自监督语音表示学习框架，专为高效且稳定的 **spoken language modeling** 设计。

#### 核心创新点：
1. **改进的 self-distillation + online clustering 架构**
   - 延续 DinoSR 的思想，采用 **teacher-student 框架** 和 **指数移动平均（EMA）** 更新 teacher。
   - 关键改进：**学生网络的每一层中间表示**（intermediate layer）用于预测 **对应教师层的码本分配**，而非仅用最后一层预测所有层目标。
     > 这增强了层级对齐，提升了训练稳定性，防止 codebook collapse。

2. **更稳定的训练目标设计**
   - 引入 **平滑的 EMA 衰减调度器**（decay schedule）：`β(t) = 1 - (1-β₀)exp(-t/T)`，避免传统固定值带来的优化滞后。
   - 移除注意力模块中的偏置项（bias in Q/K/V projections），防止训练过程中梯度爆炸。

3. **高效的纯 PyTorch 实现**
   - 开发了一个轻量级、基于原生 PyTorch 的代码库，兼容 `torch.compile`，显著提升训练吞吐。
   - 支持在 **16块 A100 GPU 上仅需1天完成预训练**（相比 HuBERT 的62小时、DinoSR 的180小时大幅提速）。

### 相比现有方法的优势
| 方面 | SpidR vs. HuBERT/DinoSR |
|------|------------------------|
| **训练速度** | 快达 **5–18倍**，单日可完成预训练 |
| **训练稳定性** | 显著减少 codebook collapse，码本多样性更高 |
| **下游 SLM 性能** | 在 sWUGGY、sBLIMP、tSC 等任务上全面超越基线 |
| **工程友好性** | 单阶段训练，无需多轮聚类与标签生成 |

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：LibriSpeech 960 小时音频（train-clean-100/360 + train-other-500）
- **下游语言模型训练数据**：Libri-Light 的 6k 子集（约6000小时未标注语音）
- **评估数据**：LibriSpeech dev-clean/dev-other，用于 ABX、MAP、SLM 评估

### 模型架构
- **特征抽取器**：7层卷积，下采样至 50Hz（每帧20ms）
- **Transformer 结构**：BASE 规模，L=12 层，d=768 维，12 头注意力
- **码本设置**：K=8 个中间层参与预测，每个层 V=256 个码字（codewords）

### 评估指标

#### （1）连续表示质量
| 指标 | 描述 |
|------|------|
| **ABX discriminability** | 衡量 triphone 中央音素的区分能力，分 within-speaker 和 across-speaker |
| **MAP words** | 基于词级别的检索准确率，衡量词级语义聚类质量 |

#### （2）离散单元质量
| 指标 | 描述 |
|------|------|
| **ABX (discrete)** | 对 one-hot 码本分配计算 ABX 错误率 |
| **PNMI**（Phone Normalized Mutual Information） | 衡量离散单元与真实音素标签之间的互信息，越高越好 |

#### （3）下游 SLM 零样本性能
| 任务 | 描述 |
|------|------|
| **sWUGGY** | 判断真词 vs. 伪词（如 brick vs. blick）的概率高低 |
| **sBLIMP** | 判断语法正确句 vs. 错误句的概率 |
| **tSC**（topic-based Story Cloze） | 判断故事结尾是否连贯 |

### 基线方法对比
- **wav2vec 2.0**
- **HuBERT**
- **WavLM BASE**
- **DinoSR**（原版及作者复现）
- 所有模型统一使用相同 batch size（63分钟音频）、OPT-125M 作为 SLM 解码器

---

## 3. 主要实验结果和性能指标

### （1）语音表示质量（表2）
在连续表示层面，SpidR 在 ABX 和 MAP 指标上均优于所有基线：

| Model | ABX (within) ↓ | ABX (across) ↓ | MAP words ↑ |
|-------|----------------|---------------|-------------|
| wav2vec 2.0 | 4.47 | 5.25 | 44.81 |
| HuBERT | 3.38 | 4.01 | 46.07 |
| DinoSR | 4.05 | 4.72 | 63.02 |
| **SpidR** | **3.32** | **3.66** | **66.50** |

✅ SpidR 在音素判别性和词级检索上均达到最优。

---

### （2）离散单元质量与 SLM 性能（表3）

| Model | Units | ABX ↓ | PNMI ↑ | sWUGGY (all) ↑ | sBLIMP ↑ | tSC ↑ |
|-------|-------|--------|--------|----------------|----------|--------|
| HuBERT | K-means | 7.32 | 0.637 | 65.50 | 55.60 | 68.75 |
| WavLM BASE | K-means | 7.01 | 0.667 | 69.74 | 56.60 | 70.35 |
| DinoSR | Codebook | 7.92 | 0.620 | 60.10 | 57.04 | 69.44 |
| **SpidR** | **K-means** | **7.20** | **0.636** | **71.89** | **59.48** | **70.46** |

✅ SpidR 在所有三项 SLM 任务上均取得最佳性能，尤其在 **sWUGGY** 上领先明显。

> 注：即使使用 codebook prediction（非 K-means），SpidR 仍优于多数基线。

---

### （3）数据扩展性分析（图3）
- 在不同训练数据量（600h / 6k / 60k）下，SpidR 始终优于 HuBERT。
- 虽然斜率相似（scaling law一致），但 SpidR 具有恒定性能优势。
- 文本基线（BPE）仍显著优于所有语音模型，说明当前 SLM 仍有提升空间。

---

### （4）消融实验（表9 & 图2）

| 变体 | ABX ↓ | MAP ↑ | PNMI ↑ |
|------|------|------|--------|
| DinoSR (reimpl.) | 5.91 | 42.55 | 0.614 |
| + Heads（中间层预测） | 4.22 | 63.39 | 0.610 |
| + Exp. EMA（平滑衰减） | 5.86 | 51.85 | 0.609 |
| **完整 SpidR** | **3.92** | **60.88** | **0.602** |

✅ **中间层预测头（Heads）** 是性能提升的关键；平滑 EMA 有助于训练稳定。

此外，图2显示：
- DinoSR 出现明显的 **prediction perplexity 下降 → codebook collapse**
- SpidR 的 perplexity 更平稳，码本利用率高，训练更稳定

---

### （5）预训练效率（表5）
| Model | Pretraining Time | GPU Hours |
|-------|------------------|-----------|
| HuBERT | 62 hrs | 1984 |
| DinoSR (原版) | 180 hrs | 2880 |
| **SpidR (本文)** | **23 hrs** | **369** |

✅ SpidR 在相同硬件下将训练时间缩短至 **不到一天**，并开源代码与 checkpoint。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **层级对齐的 self-distillation 显著提升训练稳定性**，有效缓解 codebook collapse。
2. ✅ **音素可访问性（phonetic accessibility）强的表示更利于 SLM**，ABX/PNMI 是可靠的代理指标。
3. ✅ **SpidR 在多个零样本 SLM 任务上超越 wav2vec 2.0、HuBERT、DinoSR 和 WavLM**。
4. ✅ **高效实现极大加速研究周期**，为多语言、低资源场景下的 SLM 研究提供基础。

### 方法的局限性
- 当前 SpidR 主要优化 **语言内容建模**，牺牲了部分 **声学细节** 和 **说话人信息**。
- 未实现完全解耦的表示（如 prosody、speaker、emotion 分离）。
- 在 **ASR 微调任务上表现不如 DinoSR 或 data2vec**（见表10-11），说明其表示偏向 SLM 而非通用语音任务。

> “Better performance on SLM does not imply better performance on ASR.”  
> —— 表明不同任务需要不同的表示偏好。

### 未来工作方向
1. **构建多粒度、解耦的语音表示系统**：同时建模语言、韵律、情感、说话人等维度。
2. **扩展到多语言和低资源语言**：利用 SpidR 的高效性训练跨语言 SLM。
3. **探索更大规模模型与生态化语音学习**：从 audiobook 向真实对话、噪声环境扩展。
4. **设计专门面向 SLM 的 benchmark 和评估体系**，推动 textless NLP 发展。

---

> 🔗 **开源信息**：  
> 代码与模型已开源：[https://github.com/facebookresearch/spidr](https://github.com/facebookresearch/spidr)

</details>

---

### 16. [Can LLMs Predict Their Own Failures? Self-Awareness via Internal Circuits](https://arxiv.org/abs/2512.20578)

**Authors**: Amirhosein Ghasemabadi, Di Niu  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.20578v1  

#### Abstract
Large language models (LLMs) generate fluent and complex outputs but often fail to recognize their own mistakes and hallucinations. Existing approaches typically rely on external judges, multi-sample consistency, or text-based self-critique, which incur additional compute or correlate weakly with tr...

---

### 17. [An Adaptive Distributed Stencil Abstraction for GPUs](https://arxiv.org/abs/2512.19851)

**Authors**: Aditya Bhosale, Laxmikant Kale  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.19851v1  

#### Abstract
The scientific computing ecosystem in Python is largely confined to single-node parallelism, creating a gap between high-level prototyping in NumPy and high-performance execution on modern supercomputers. The increasing prevalence of hardware accelerators and the need for energy efficiency have made...

---

### 18. [WOC: Dual-Path Weighted Object Consensus Made Efficient](https://arxiv.org/abs/2512.20485)

**Authors**: Tanisha Fonseca, Gengrui Zhang  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.20485v1  

#### Abstract
Modern distributed systems face a critical challenge: existing consensus protocols optimize for either node heterogeneity or workload independence, but not both. For example, Cabinet leverages weighted quorums to handle node heterogeneity but serializes all operations through a global leader, limiti...

---

### 19. [Scaling Point-based Differentiable Rendering for Large-scale Reconstruction](https://arxiv.org/abs/2512.20017)

**Authors**: Hexu Zhao, Xiaoteng Liu, Xiwen Min, Jianhao Huang, Youming Deng, Yanfei Li, Ang Li, Jinyang Li, Aurojit Panda  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.20017v1  

#### Abstract
Point-based Differentiable Rendering (PBDR) enables high-fidelity 3D scene reconstruction, but scaling PBDR to high-resolution and large scenes requires efficient distributed training systems. Existing systems are tightly coupled to a specific PBDR method. And they suffer from severe communication o...

---

### 20. [Predictive-LoRA: A Proactive and Fragmentation-Aware Serverless Inference System for LLMs](https://arxiv.org/abs/2512.20210)

**Authors**: Yinan Ni, Xiao Yang, Yuqi Tang, Zhimin Qiu, Chen Wang, Tingzhou Yuan  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.20210v1  

#### Abstract
The serverless computing paradigm offers compelling advantages for deploying Large Language Model (LLM) inference services, including elastic scaling and pay-per-use billing. However, serving multiple fine-tuned LLMs via Low-Rank Adaptation (LoRA) in serverless environments faces critical challenges...

---

### 21. [Resilient Packet Forwarding: A Reinforcement Learning Approach to Routing in Gaussian Interconnected Networks with Clustered Faults](https://arxiv.org/abs/2512.20394)

**Authors**: Mohammad Walid Charrwi, Zaid Hussain  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.20394v1  

#### Abstract
As Network-on-Chip (NoC) and Wireless Sensor Network architectures continue to scale, the topology of the underlying network becomes a critical factor in performance. Gaussian Interconnected Networks based on the arithmetic of Gaussian integers, offer attractive properties regarding diameter and sym...

---

### 22. [End-to-End Data Quality-Driven Framework for Machine Learning in Production Environment](https://arxiv.org/abs/2512.19723)

**Authors**: Firas Bayram, Bestoun S. Ahmed, Erik Hallin  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19723v1  

#### Abstract
This paper introduces a novel end-to-end framework that efficiently integrates data quality assessment with machine learning (ML) model operations in real-time production environments. While existing approaches treat data quality assessment and ML systems as isolated processes, our framework address...

---

### 23. [Exploring Deep-to-Shallow Transformable Neural Networks for Intelligent Embedded Systems](https://arxiv.org/abs/2512.19731)

**Authors**: Xiangzhong Luo, Weichen Liu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19731v1  

#### Abstract
Thanks to the evolving network depth, convolutional neural networks (CNNs) have achieved remarkable success across various embedded scenarios, paving the way for ubiquitous embedded intelligence. Despite its promise, the evolving network depth comes at the cost of degraded hardware efficiency. In co...

---

### 24. [Case Prompting to Mitigate Large Language Model Bias for ICU Mortality Prediction](https://arxiv.org/abs/2512.19735)

**Authors**: Gangxiong Zhang, Yongchao Long  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19735v1  

#### Abstract
Accurate mortality risk prediction for intensive care unit (ICU) patients is essential for clinical decision-making. Although large language models (LLMs) show promise in predicting outcomes from structured medical data, their predictions may exhibit demographic biases related to sex, age, and race,...

---

### 25. [DeepBridge: A Unified and Production-Ready Framework for Multi-Dimensional Machine Learning Validation](https://arxiv.org/abs/2512.19744)

**Authors**: Gustavo Coelho Haase, Paulo Henrique Dourado da Silva  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19744v1  

#### Abstract
We present DeepBridge, an 80K-line Python library that unifies multi-dimensional validation, automatic compliance verification, knowledge distillation, and synthetic data generation. DeepBridge offers: (i) 5 validation suites (fairness with 15 metrics, robustness with weakness detection, uncertainty...

---

### 26. [Offline Safe Policy Optimization From Heterogeneous Feedback](https://arxiv.org/abs/2512.20173)

**Authors**: Ze Gong, Pradeep Varakantham, Akshat Kumar  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20173v1  

#### Abstract
Offline Preference-based Reinforcement Learning (PbRL) learns rewards and policies aligned with human preferences without the need for extensive reward engineering and direct interaction with human annotators. However, ensuring safety remains a critical challenge across many domains and tasks. Previ...

---

### 27. [LongVideoAgent: Multi-Agent Reasoning with Long Videos](https://arxiv.org/abs/2512.20618)

**Authors**: Runtao Liu, Ziyi Liu, Jiaqi Tang, Yue Ma, Renjie Pi, Jipeng Zhang, Qifeng Chen  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20618v1  

#### Abstract
Recent advances in multimodal LLMs and systems that use tools for long-video QA point to the promise of reasoning over hour-long episodes. However, many methods still compress content into lossy summaries or rely on limited toolsets, weakening temporal grounding and missing fine-grained cues. We pro...

---

### 28. [Hard Negative Sample-Augmented DPO Post-Training for Small Language Models](https://arxiv.org/abs/2512.19728)

**Authors**: Haocheng Lu, Minjun Zhu, Henry Yu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19728v1  

#### Abstract
Large language models (LLMs) continue to struggle with mathematical reasoning, and common post-training pipelines often reduce each generated solution to a binary outcome: correct or incorrect. This perspective is limiting in practice, as failures in chain-of-thought (CoT) reasoning are frequently s...

---

### 29. [Spatio-Temporal Graph Neural Networks for Dairy Farm Sustainability Forecasting and Counterfactual Policy Analysis](https://arxiv.org/abs/2512.19970)

**Authors**: Surya Jayakumar, Kieran Sullivan, John McLaughlin, Christine O'Meara, Indrakshi Dey  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19970v1  

#### Abstract
This study introduces a novel data-driven framework and the first-ever county-scale application of Spatio-Temporal Graph Neural Networks (STGNN) to forecast composite sustainability indices from herd-level operational records. The methodology employs a novel, end-to-end pipeline utilizing a Variatio...

---

### 30. [Saddle-to-Saddle Dynamics Explains A Simplicity Bias Across Neural Network Architectures](https://arxiv.org/abs/2512.20607)

**Authors**: Yedi Zhang, Andrew Saxe, Peter E. Latham  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20607v1  

#### Abstract
Neural networks trained with gradient descent often learn solutions of increasing complexity over time, a phenomenon known as simplicity bias. Despite being widely observed across architectures, existing theoretical treatments lack a unifying framework. We present a theoretical framework that explai...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
