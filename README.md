# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-09 05:58:50 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FaST: Efficient and Effective Long-Horizon Forecasting for Large-Scale Spatial-Temporal Graphs via Mixture-of-Experts](https://arxiv.org/abs/2601.05174)

**Authors**: Yiji Zhao, Zihao Zhong, Ao Wang, Haomin Wen, Ming Jin, Yuxuan Liang, Huaiyu Wan, Hao Wu  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2601.05174v1  

#### Abstract
Spatial-Temporal Graph (STG) forecasting on large-scale networks has garnered significant attention. However, existing models predominantly focus on short-horizon predictions and suffer from notorious computational costs and memory consumption when scaling to long-horizon predictions and large graph...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FaST: Efficient and Effective Long-Horizon Forecasting for Large-Scale Spatial-Temporal Graphs via Mixture-of-Experts

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Spatial-Temporal Graph (STG)** 预测模型在处理**大规模图**（数千节点）和**长时域预测**（如一周、672步）时面临两大挑战：
- **计算复杂度高**：传统 GNN 和 self-attention 模块具有 $O(N^2)$ 和 $O(T^2)$ 的空间-时间交互复杂度，难以扩展。
- **信息压缩导致语义损失**：为降低复杂度而采用的时间序列压缩方法（如线性投影）往往采用“一刀切”策略，忽略了不同节点和时段之间的**异质性**（heterogeneity），造成表示同质化和精度下降。

### 提出的新方法
作者提出 **FaST**（Fast long-horizon forecasting framework），一个基于 **Mixture-of-Experts (MoE)** 的高效且有效的长时域大规模 STG 预测框架，其核心创新包括：

#### （1）异质性感知的 MoE 时间压缩输入模块（Heterogeneity-aware MoE-based Temporal Compression）
- 将历史时间序列通过多个可学习的专家网络（experts）进行压缩，生成低维稠密嵌入。
- 引入 **Heterogeneity-Aware Router (HA-Router)**，动态地为每个节点和时间窗口选择最合适的专家路径，从而保留时空异质性，避免信息同质化。

#### （2）自适应图代理注意力机制（Adaptive Graph Agent Attention, AGA-Att）
- 引入少量可学习的 **agent tokens**（$a \ll N$）作为中介，替代全图节点间的直接交互。
- 通过 `Node-to-Agent` 聚合和 `Agent-to-Node` 分发机制，将空间交互复杂度从 $O(N^2)$ 降至 $O(Na)$，实现线性扩展。

#### （3）并行化 GLU-MoE 模块
- 在网络主干中用 **GLU-based Experts** 替代传统的 FFN 层。
- 设计高效的并行计算机制，将多个专家的线性变换合并为单层操作，显著提升计算效率。

### 相比现有方法的优势
- **线性复杂度**：整体计算和内存复杂度对节点数 $N$ 和时间步 $T$ 均为线性，支持大规模图和长时域预测。
- **保持表达能力**：通过 MoE 和 agent attention 机制，在降低复杂度的同时保留了丰富的时空依赖建模能力。
- **高效且准确**：在多个真实世界数据集上实现了**最优预测精度**和**最快训练/推理速度**，尤其在 672 步长时域任务中表现突出。

---

## 2. 核心实验方法和设置

### 数据集
使用 **LargeST** 基准数据集，包含四个大规模交通流量子集：

| 数据集 | 节点数 | 时间粒度 | 时间跨度 | 样本量 |
|--------|--------|----------|----------|--------|
| SD     | 716    | 15分钟   | 2019全年 | ~25M   |
| GBA    | 2,352  | 15分钟   | 2019全年 | ~82M   |
| GLA    | 3,834  | 15分钟   | 2019全年 | ~134M  |
| CA     | 8,600  | 15分钟   | 2019全年 | ~300M  |

数据按 6:2:2 划分训练/验证/测试集。

### 实验设置
- **任务**：基于过去 96 个时间步（1天）预测未来 {48, 96, 192, 672} 步（最长一周）。
- **统一框架**：所有模型在 **BasicTS** 基准框架下实现，隐藏维度统一为 64。
- **FaST 配置**：固定参数（无需调参）—— #experts=8, #layers=3, #agents=32, #dim=64。
- **硬件**：NVIDIA RTX A6000 (48GB GPU)，AMD EPYC CPU。

### 评估指标
- **MAE**, **RMSE**, **MAPE**：越小越好。
- **R²**：越大越好。

### 基线方法
分为两类：
- **时序中心方法**（Temporal-centric）：
  - DLinear, NHITS, CycleNet
- **时空中心方法**（Spatial-Temporal-centric）：
  - DCRNN, STGCN, GWNet, SGP, STID, STDMAE, BigST, RPMixer, STPGNN, PatchSTG

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在 **CA 数据集（8,600节点，672步预测）** 上，FaST 表现如下：
- **MAE**: 24.23
- **RMSE**: 45.09
- **MAPE**: 16.48%
- **R²**: 0.9395

在所有数据集和预测长度上均取得 **SOTA 性能**。

### 与基线方法的对比结果
- **精度优势**：
  - 相比最佳基线，**MAE 降低 4.4%~18.4%**，**MAPE 降低最多达 19.06%**（电力数据集）。
  - 在短时预测（96→12）上也优于所有基线（见附录 Table 6）。
- **效率优势**：
  - **训练速度比 SOTA 快 1.3×~2.2×**。
  - 在 CA 数据集上，FaST 训练时间为 **746秒/epoch**，而 BigST 为 1,284秒/epoch。
  - 内存增长更平缓：从 96→672 步，FaST 内存增长 **102.7%**，远低于 DLinear (487.3%) 和 NHITS (194.8%)。
- **可扩展性**：
  - 多个基线（如 STGCN, STPGNN, RPMixer）在 GLA 或 CA 数据集上出现 **OOM（内存溢出）**，无法完成训练。
  - FaST 可稳定运行于最大规模数据集（CA）。

### 消融实验结果
消融实验验证了各组件的重要性（见 Figure 4）：
- **w/ LinearInput**：用线性层替换 MoE 输入模块，性能显著下降 → 证明 **MoE 压缩的有效性**。
- **w/o HA-MoE**：移除 MoE 模块，性能大幅下降（尤其在 GBA 上）→ 证明 **MoE 是核心模块**。
- **w/o HA-Router**：移除异质性感知路由，性能下降 → 证明 **动态路由对捕捉异质性至关重要**。
- **w/o AGA-Att**：移除代理注意力，性能显著下降 → 证明 **AGA-Att 对空间建模的关键作用**。

此外，**GLU-Experts vs FFN-Experts** 实验表明：
- GLU 版本在精度相近的情况下，**训练速度提升 1.4×**，验证了并行化设计的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **FaST 成功实现了大规模、长时域 STG 预测的可行性**：首次支持在数千节点上进行长达一周（672步）的预测。
2. **异质性感知的 MoE 设计是关键**：通过 HA-Router 动态分配专家，有效缓解了压缩带来的信息损失和表示同质化问题。
3. **代理注意力机制显著降低复杂度**：AGA-Att 将空间复杂度降至线性，同时保持了长距离依赖建模能力。
4. **效率与精度兼得**：FaST 不仅精度最高，而且训练和推理速度最快，内存占用更低，具备强实用性。

### 方法的局限性
- 当前模型配置为固定参数（如 #experts=8），未针对每个数据集精细调优，可能存在进一步优化空间。
- 代理注意力的效果依赖于“空间冗余”假设（即节点行为存在共享模式），在高度异构的图中可能需增加 agent 数量以补偿。
- 实验主要集中在交通和电力数据，其他领域（如气象、社交网络）的泛化能力有待进一步验证。

### 未来工作方向
- 探索将 **time series foundation models** 与 FaST 结合，进一步提升模型的通用性和可迁移性。
- 扩展至更多应用场景，如空气质量预测、能源调度、城市事件预测等。
- 进一步优化 MoE 路由机制，探索更细粒度的时空动态路由策略。

---

> **源码地址**：https://github.com/yijizhao/FaST  
> **发表信息**：KDD'26, Jeju Island, Republic of Korea

</details>

---

### 2. [LinguaGame: A Linguistically Grounded Game-Theoretic Paradigm for Multi-Agent Dialogue Generation](https://arxiv.org/abs/2601.04516)

**Authors**: Yuxiao Ye, Yiming Zhang, Yiran Ma, Huiyuan Xie, Huining Zhu, Zhiyuan Liu  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.04516v1  

#### Abstract
Large Language Models (LLMs) have enabled Multi-Agent Systems (MASs) where agents interact through natural language to solve complex tasks or simulate multi-party dialogues. Recent work on LLM-based MASs has mainly focused on architecture design, such as role assignment and workflow orchestration. I...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：LinguaGame: A Linguistically Grounded Game-Theoretic Paradigm for Multi-Agent Dialogue Generation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于 Large Language Models (LLMs) 的 Multi-Agent Systems (MASs) 多集中于架构设计（如角色分配、流程编排），而忽视了**多智能体对话中的交互过程本身**。特别是，现有方法未能有效提升智能体在自然语言交流中的**沟通效率**——即如何更准确、简洁地传达意图。

此外，已有 game-theoretic MAS 方法通常将博弈机制与具体任务目标强耦合，导致泛化能力差，且依赖模型重训练或迭代经验积累，计算成本高。

### **提出的新方法与思路**
本文提出了 **LinguaGame**，一种基于语言学原理的、游戏理论驱动的多智能体对话生成范式。其核心思想是：

- 将对话建模为一个 **signalling game**，其中发送方（sender）通过语言信号表达其**communicative intent**（意图）和**strategy**（策略），接收方（receiver）则推断这些隐藏状态。
- 引入一种**无需训练的均衡近似算法**（training-free equilibrium approximation algorithm），在推理阶段动态调整智能体决策，优化沟通效果。

### **相比现有方法的优势**
| 维度 | LinguaGame 的优势 |
|------|------------------|
| **通用性** | 博弈设计基于语言学理论（如 Speech Act Theory），而非任务特定目标，具有更强的跨任务泛化能力。 |
| **效率** | 推理时即可完成决策优化，无需模型微调或反复交互积累经验，实现“plug-and-play”集成。 |
| **可解释性** | 显式建模 intent 和 strategy，增强对话行为的语义可解释性。 |
| **有效性** | 在对抗性开放对话场景中显著提升沟通效率与内容质量。 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Courtroom Proceedings（法庭审理）**：从“中国裁判文书网”（China Judgments Online）随机选取 50 个真实民事案件，提取当事人信息与案情简介作为输入。
- **Debates（辩论）**：从 Quora Argumentation Mining 数据集中选取 50 个争议性命题（如“堕胎应合法化”），覆盖政治、社会、科技等 20 个主题。

### **实验设置**
- **基础 LLM**：Qwen2.5-32B。
- **MAS 架构配置**：
  - 法庭场景：模拟一审程序，包含五个阶段（举证质证、法庭调查、法庭辩论、最后陈述、宣判），角色为法官、原告律师、被告律师。
  - 辩论场景：正反双方交替发言，无固定流程，由 LLM 判断是否自然收敛。
- **Hyperparameters**：
  - 候选 utterance 数量：3
  - 意图-策略权重 $ w = 0.5 $
  - KL 正则化系数 $ \lambda = 0.1 $
  - 迭代轮数：5000 轮用于均衡逼近

### **评估指标**
采用双层人工评估体系（共 8 名研究生标注员，法律与语言学背景各半）：

#### **Utterance-level Evaluation**
仅针对 LGMAS 与 LLM-based reranking 基线：
- 比较“初始首选 utterance”与“game-selected winning utterance”的优劣。
- 分类为 positive / neutral / negative alternation。

#### **Dialogue-level Evaluation**
对所有系统进行五点李克特量表评分（Likert Scale）：
| 维度 | 子维度 | 定义 |
|------|--------|------|
| **Linguistic form** | Clarity, Conciseness | 语法正确性、结构连贯性、冗余程度 |
| **Content quality** | Argument, Tactic | 论点合理性、逻辑一致性、回应适应性 |

### **基线方法对比**
| 系统 | 描述 |
|------|------|
| **SDMAS** | 标准 MAS，无 intent-strategy 或博弈机制 |
| **ISMAS** | 加入 intent-strategy 条件生成（Chain-of-Thought 风格），但无博弈推理 |
| **LGMAS** | 完整 LinguaGame 框架（intent + strategy + game-theoretic selection） |
| **LLM-based reranking** | 使用 Qwen2.5-32B、Llama-3.1-70B、DeepSeek-V3 对候选 utterance 进行单次打分重排序 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 1）**

| 系统 | 场景 | Clarity | Conciseness | Argument | Tactic | 平均得分 |
|------|------|---------|-------------|----------|--------|----------|
| SDMAS | Court | 4.10 | 3.55 | 3.18 | 3.28 | 3.53 |
| SDMAS | Debate | 4.15 | 3.62 | 3.27 | 3.39 | 3.61 |
| SDMAS | Overall | 4.13 | 3.59 | 3.23 | 3.34 | 3.57 |
| ISMAS | Court | 4.18 | 3.73 | 3.23 | 3.35 | 3.62 |
| ISMAS | Debate | 4.22 | 3.69 | 3.34 | 3.42 | 3.67 |
| ISMAS | Overall | 4.20 | 3.71 | 3.29 | 3.39 | 3.65 |
| **LGMAS** | **Court** | **4.36** | **4.12** | **3.83** | **3.96** | **4.07** |
| **LGMAS** | **Debate** | **4.42** | **4.19** | **3.92** | **4.05** | **4.15** |
| **LGMAS** | **Overall** | **4.39** | **4.16** | **3.88** | **4.01** | **4.11** |

> ✅ 所有维度上 LGMAS 均显著优于 SDMAS 和 ISMAS（$ p < 0.01 $，双尾配对 t 检验）

### **与基线方法的对比结果**
- **LGMAS 相比 SDMAS**：
  - Clarity ↑ 0.26
  - Conciseness ↑ 0.57
  - Argument ↑ 0.65
  - Tactic ↑ 0.67
- **LGMAS 相比 ISMAS**：
  - 表明仅引入 intent-strategy conditioning 不足以带来实质性提升，必须结合 game-theoretic reasoning 才能实现全面改进。

### **消融实验结果**
- **ISMAS vs SDMAS**：仅在意图-策略条件下，conciseness 有显著提升（+0.12），其他维度无显著差异 → 说明 intent-conditioning 只能促进更聚焦表达，但无法提升论证质量和战术连贯性。
- **LGMAS vs ISMAS**：在相同 intent-strategy 先验下，LGMAS 仍大幅领先 → 改进来源于更有效的 **utterance selection**，而非 intent/strategy 选择偏差。

### **Utterance-level Evaluation 结果（Table 2）**

| 方法 | Positive (%) | Neutral (%) | Negative (%) | Altered Utterances |
|------|--------------|-------------|--------------|--------------------|
| **LGMAS** | **78.1%** | 12.4% | 9.5% | 1,366 |
| Qwen2.5 reranker | 44.0% | 38.4% | 17.6% | 318 |
| Llama-3.1 reranker | 36.7% | 40.8% | 22.5% | 1,503 |
| DeepSeek-V3 reranker | 51.2% | 14.6% | 34.2% | 972 |

> ✅ LinguaGame 的 game-theoretic selection 在局部连贯性和适当性方面远超通用 LLM 重排序方法。

---

## **4. 关键结论和发现**

### **主要发现**
1. **建模对话为 intent-strategy 层面的 signalling game 能显著提升沟通效率**：
   - 智能体能生成更清晰、简洁、逻辑一致且具策略响应性的回复。
2. **LinguaGame 的优势来自“更优的表达方式”，而非“更好的意图选择”**：
   - ISMAS 与 LGMAS 使用相同的 intent/strategy 先验，差距源于 utterance selection 机制。
3. **训练-free 的均衡逼近机制高效实用**：
   - 无需微调或多次交互，在推理时即可完成高质量决策调整，适合部署在多样化 MAS 场景中。
4. **语言学驱动的设计增强了泛化潜力**：
   - 基于 Speech Act Theory 和 Argumentation Schemes 设计 intent/strategy 体系，使其适用于不同领域。

### **局限性**
1. **任务目标范围有限**：
   - 当前研究聚焦于对话过程质量（communication efficiency），未验证其对下游任务性能（如判决准确性、辩论胜负）的影响。
2. **评估域受限且人力成本高**：
   - 实验集中在法庭与辩论两类对抗性场景；扩展至协作型任务（如软件开发、日常对话）需大量人工评估资源。
3. **负责任 AI 考虑不足**：
   - 不建议直接用于现实法律决策系统，需进一步评估公平性、偏见、鲁棒性等问题。

### **未来工作方向**
- 探索 LinguaGame 在协作型 MAS 中的应用（如 ChatDev 类系统）。
- 将 intent-strategy 推理与任务绩效反馈结合，形成闭环学习机制。
- 开发自动化评估指标以替代部分人工评价，降低实验门槛。
- 扩展至多模态或多轮谈判等复杂交互场景。

--- 

> **总结**：LinguaGame 提出了一种新颖的、语言学基础的游戏理论框架，成功将 pragmatic communication 建模为 intent-strategy signalling game，并通过 training-free equilibrium approximation 实现高效的推理时优化。实验证明其在提升多智能体对话的沟通效率方面具有显著优势，具备良好的通用性与实用性前景。

</details>

---

### 3. [RelayLLM: Efficient Reasoning via Collaborative Decoding](https://arxiv.org/abs/2601.05167)

**Authors**: Chengsong Huang, Tong Zheng, Langlin Huang, Jinyuan Li, Haolin Liu, Jiaxin Huang  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.05167v1  

#### Abstract
Large Language Models (LLMs) for complex reasoning is often hindered by high computational costs and latency, while resource-efficient Small Language Models (SLMs) typically lack the necessary reasoning capacity. Existing collaborative approaches, such as cascading or routing, operate at a coarse gr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RelayLLM: Efficient Reasoning via Collaborative Decoding —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在复杂推理任务中表现出色，但其高计算成本和延迟限制了实际部署。小型语言模型（SLMs）虽然高效，但在复杂推理上能力不足。现有的协作方法（如 cascading 或 routing）通常以**查询级别**（query-level）进行决策，一旦判定任务困难，就将整个生成任务交给 LLM，导致“**全有或全无**”（all-or-nothing）的粗粒度调度，造成大量不必要的计算开销。

### ✅ 提出的新方法：RelayLLM
作者提出 **RelayLLM**，一种基于**token-level 协同解码**（token-level collaborative decoding）的新型框架，其核心思想是：
- 将 LLM 视为一个可按需调用的“工具”，而非备用生成器。
- SLM 作为主控制器，在推理过程中自主决定是否、何时以及调用多少个 token 的 LLM 输出。
- 引入特殊命令 `<call>n</call>`，允许 SLM 在遇到关键推理步骤时暂停自身生成，请求 LLM 生成指定数量的 token，之后继续推理。

### ✅ 相比现有方法的优势
| 维度 | 传统方法（如 cascading/routing） | RelayLLM |
|------|-------------------------------|----------|
| 调度粒度 | Query-level（粗粒度） | **Token-level**（细粒度） |
| 控制机制 | 外部路由器或静态规则 | **SLM 自主控制**（active controller） |
| 成本效率 | 高调用率，浪费严重 | **极低 token 调用率**（仅 1.07%） |
| 性能提升 | 有限 | 显著提升准确率（+6.9% vs. random router） |
| 无需额外控制器 | ❌ 通常需要 | ✅ **无额外参数或模块** |

---

## 2. 核心实验方法和设置

### 📚 数据集
实验在六个数学推理基准上进行：
- **Minerva**
- **MATH-500**
- **GSM8K**
- **Olympiad-Bench**
- **AIME-2024**
- **AIME-2025**

此外还测试了跨领域泛化能力：
- **Big-Bench Hard (BBEH)**
- **MMLU-Pro**
- **SuperGPQA**

训练数据来自 **DAPO dataset**（大规模强化学习数据集）。

### ⚙️ 实验设置
- **学生模型（SLM）**：`Qwen3-0.6B`, `Qwen3-1.7B`
- **教师模型（LLM）**：`Qwen3-8B`
- 所有模型来自同一族，确保 tokenization 和风格一致性。
- 推理模式：non-thinking mode（禁用内部思维链）
- 评估方式：
  - 对 AIME 数据集使用 `avg@32`
  - 其他使用 `pass@1`（greedy decoding）
  - 使用 **GPT-4o-mini** 作为语义裁判判断输出正确性

### 🔁 训练框架：两阶段训练
1. **Supervised Warm-up**  
   - 构造合成数据，随机插入 `<call>n</call>` 命令，训练 SLM 学会生成合法调用指令。
   - 防止分布偏移，保证训练上下文与 SLM 自身分布一致。

2. **Reinforcement Learning with GRPO**  
   - 使用 **Group Relative Policy Optimization (GRPO)** 进行策略优化。
   - 设计 **context-aware reward**，结合任务难度动态调整奖励信号。

### 🎯 奖励设计（Difficulty-Aware Reward）
将每个 query 分为三类场景并差异化奖励：
| 场景 | 描述 | 奖励策略 |
|------|------|--------|
| **Student-Solvable** | SLM 可独立解决 | 独立成功奖励 +1.5，鼓励不依赖 LLM |
| **Teacher-Dependent** | 必须调用 LLM 才能正确 | 不调用且失败 → 惩罚 -1.0 |
| **Teacher-Unsolvable** | 即使调用也无法解决 | 调用尝试给予小探索奖励 `r=p(y)` |

### 🆚 基线方法对比
| 基线 | 类型 | 是否需要额外控制器 |
|------|------|------------------|
| **Base Model** | 原始 SLM | ❌ |
| **GRPO** | 强化学习微调 | ❌ |
| **CITER** | token-level routing 方法 | ✅（需 MLP 控制器） |
| **Random Router** | 查询级随机路由 | ❌ |
| **Perfect Router** | 理想查询级路由（oracle） | ❌ |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| 方法 | 平均准确率（Qwen3-1.7B） | Avg. Call Ratio |
|------|--------------------------|---------------|
| Base Model | 42.50% | — |
| GRPO | 44.06% | — |
| CITER | 46.81% | 1.34% |
| **RelayLLM (Simple-Reward)** | **49.30%** | **0.43%** |
| **RelayLLM (Difficulty-Aware)** | **49.52%** | **1.07%** |
| Qwen3-8B（教师模型） | 54.12% | 100% |

> ✅ **RelayLLM 将平均准确率从 42.50% 提升至 49.52%，缩小了约 60% 的性能差距**。

### 🔍 与基线方法对比
- 相比 **resource-equivalent Random Router**，RelayLLM 实现 **+6.9% 准确率提升**。
- 在仅调用 **1.07% 的 token** 的情况下，达到接近大模型（54.12%）的性能。
- 相比 CITER（token-level routing），**准确率更高且无需额外控制器**，更高效。

### 🔧 消融实验结果（Table 3）

| 消融设置 | 平均准确率 | Call Ratio |
|---------|------------|-----------|
| 完整 RelayLLM | **49.52%** | **1.07%** |
| w/o Data Filtering（不筛选难样本） | 48.76% | **3.30%** ↑ |
| w/o Independence Incentive（不鼓励独立） | 49.34% | **4.10%** ↑ |
| w/o Exploration Reward（不鼓励探索） | **47.56%** ↓ | 0.65% |

> 💡 结论：
- **数据过滤**可避免无效调用，显著降低成本。
- **鼓励独立成功**防止过度依赖 LLM。
- **探索奖励**对处理极端难题至关重要。

### 🔄 动态长度 vs 固定长度调用（Table 5 & 6）
| 方法 | 平均准确率 | Call Ratio |
|------|------------|-----------|
| Fixed-20 | 49.41% | 1.32% |
| Fixed-100 | 49.56% | 2.87% |
| Fixed-500 | 51.17% | 5.37% |
| **RelayLLM（动态预测）** | **49.52%** | **1.07%** ✅ |

> ✅ RelayLLM 在精度几乎持平的情况下，**计算成本仅为 Fixed-100 的 ~37%**，证明其“按需调用”策略极为高效。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **细粒度协作优于粗粒度调度**  
   token-level 协作能精准识别关键推理节点，极大减少冗余计算。

2. **SLM 可学会战略性求助行为**  
   通过 GRPO + Difficulty-Aware Reward，SLM 能自主判断何时该求助、何时应独立完成。

3. **极低成本实现高性能**  
   仅调用 **1.07% 的 LLM token**，即可实现接近大模型的推理能力，**计算成本降低 98.2%**。

4. **具备跨领域泛化能力**  
   在未见过的非数学任务（如 MMLU-Pro）上仍表现优异，说明帮助寻求行为具有通用性。

5. **内在推理能力得到增强**  
   在“无教师”测试中（禁止调用 LLM），RelayLLM 仍优于基线，表明其在训练中内化了部分专家推理模式。

### ⚠️ 局限性
- 当前方法依赖于 LLM 输出质量；若 LLM 本身无法解决问题（Teacher-Unsolvable），效果受限。
- 奖励设计依赖可验证任务（如数学题），难以直接迁移到开放生成任务。
- 跨模型分布对齐敏感：换用不同 LLM 作为教师可能导致性能下降（见 Figure 3）。

### 🔮 未来工作方向
- 扩展到多模态或多工具协同场景（如代码执行、搜索等）。
- 探索更通用的帮助信号建模方式，适用于不可验证任务。
- 研究如何让 SLM 更好地“吸收”专家知识，进一步减少对外部模型的依赖。
- 探索自适应调用长度的端到端学习机制。

---

## 总结
**RelayLLM** 是一种极具潜力的高效推理框架，它通过 **token-level 协同解码 + SLM 主动控制 + 难度感知强化学习**，实现了“**小模型主导、大模型辅助**”的理想协作范式。其不仅大幅提升了 SLM 的推理能力，而且以极低代价逼近 LLM 表现，为资源受限环境下的智能系统提供了可行路径。

</details>

---

### 4. [Safety-Utility Conflicts Are Not Global: Surgical Alignment via Head-Level Diagnosis](https://arxiv.org/abs/2601.04262)

**Authors**: Wang Cai, Yilin Wen, Jinchang Hou, Du Su, Guoqiu Wang, Zhonghou Lv, Chenfu Bao, Yunfang Wu  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.04262v1  

#### Abstract
Safety alignment in Large Language Models (LLMs) inherently presents a multi-objective optimization conflict, often accompanied by an unintended degradation of general capabilities. Existing mitigation strategies typically rely on global gradient geometry to resolve these conflicts, yet they overloo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Safety-Utility Conflicts Are Not Global: Surgical Alignment via Head-Level Diagnosis

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在进行 **safety alignment**（安全对齐）时，常伴随着“**alignment tax**”——即模型通用能力（如推理、知识问答）的显著下降。传统方法将此视为全局多目标优化（MOO）问题，通过调整梯度方向（如 PCGrad、CAGrad）来缓解冲突，但忽略了 Transformer 内部的 **Modular Heterogeneity**（模块异质性），导致次优权衡。

### 提出的新方法：**CAST**（Conflict-Aware Sparse Tuning）
CAST 是一种结合 **head-level 诊断** 与 **sparse fine-tuning** 的框架，其核心思想是：
- 安全-效用冲突并非全局分布，而是集中在少数特定的 **attention heads** 中。
- 通过预对齐阶段的 **head-level conflict diagnosis**，识别出这些高冲突头，并在训练中跳过它们，从而实现“外科手术式”的精准对齐。

#### 方法流程：
1. **Head-Level Conflict Diagnosis**：
   - 构建两个指标：
     - **Optimization Conflict (O)**：衡量安全目标与效用目标梯度方向的夹角（cosine distance）。
     - **Functional Sensitivity (S)**：通过零样本消融（zero-shot ablation）衡量某 head 对通用任务 vs. 安全行为的重要性。
   - 合成统一的 **Conflict Score**：`C(h) = O(h) · S(h)`，用于排序所有 attention heads。
2. **Sparse Tuning Strategy**：
   - 将 heads 按 `C(h)` 分桶（如 Top-25% 为 Risky Zone，Bottom-25% 为 Safe Zone）。
   - 只更新 Safe Zone 的 heads，冻结其余参数。

### 相比现有方法的优势
| 方面 | 传统方法（如 Full SFT, PCGrad） | CAST |
|------|-------------------------------|------|
| 更新粒度 | 全局参数或低秩子空间（LoRA） | **head-level 精细控制** |
| 冲突处理 | 几何投影（忽略功能敏感性） | **结合几何 + 功能敏感性** |
| 参数效率 | 高开销（全量或 LoRA） | **仅更新 25% heads，高效且可解释** |
| 效果 | 显著损失通用能力 | **保持安全性能的同时极大保留通用能力** |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据集 | 用途 |
|------|--------|------|
| **诊断阶段（pre-alignment）** |  
| - `Dutil` | 500 个 MMLU 样本 | 评估通用能力依赖 |
| - `Dsafe` | 500 个 WildJailbreak 恶意提示 | 获取拒绝行为梯度 |
| **训练阶段** |  
| - `Dalign` | 10,000 个 WildJailbreak 样本（平衡四类：vanilla/adversarial harmful/benign） | 安全微调 |
| **评估阶段** |  
| - **Safety** | WildJailbreak（测试集）、WildGuard、DAN | 防御成功率 |
| - **Utility** | MMLU、CSQA（知识）；GSM8K、MATH（推理） | 准确率 |

### 实验设置
- **模型**：Llama-3.1-8B-Instruct、Qwen2.5-7B-Instruct、Mistral-7B-v0.2
- **微调方式**：LoRA（仅作用于 `Wq` 查询投影矩阵，确保 head-level attribution）
- **超参数**：
  - 学习率：1e-4
  - 训练轮数：1
  - 批大小：8（有效）
  - LoRA rank: 32
- **多随机种子**：{21, 42, 84}，取平均值以增强鲁棒性

### 评估指标
- **Safety Performance**：Defense Success Rate（越高越好）
- **Utility Performance**：MMLU、CSQA、GSM8K、MATH 准确率
- **Trade-off Metrics**：
  - **Utility Cost Ratio (UCR)**：单位安全增益带来的效用损失
    $$
    \text{UCR} = \max\left(0, \frac{U_b - U_a}{S_a - S_b + \epsilon}\right)
    $$
  - **MMLU-CR**：同上，仅针对 MMLU
  - **越低越好**

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **Full SFT** | 全量 LoRA 微调所有 attention heads |
| **Random-SFT (25%)** | 随机选择 25% heads 进行 LoRA 微调 |
| **PCGrad (PCG)** | 梯度投影法缓解多任务冲突 |
| **CAST-SFT / CAST-PCG** | 在 Safe/Risky Zone 上应用 SFT 或 PCGrad |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### ✅ 在 Llama 上的表现（CAST-SFT Safe Zone vs Full SFT）
| 指标 | Full SFT | CAST-SFT (Safe) | 提升 |
|------|----------|------------------|------|
| **MMLU** | 46.28 → **55.73** | ↑ +9.45 pp |
| **GSM8K** | 20.80 → **77.20** | ↑ +56.4 pp |
| **Safety (Avg)** | 90.61 | 92.62 | ≈持平 |
| **UCR** | — | **显著降低** | 更高效的安全对齐 |

> **结论**：仅更新 25% 的低冲突头，即可恢复接近 base model 的通用能力，同时达到甚至超过 full SFT 的安全性。

#### ✅ 与其他基线对比（图 3 & 4）
- **Pareto Frontiers** 显示：
  - **CAST-SFT (Safe Zone)** 在所有模型上均主导前沿。
  - 显著优于 Full SFT、Random-SFT 和 Risky Zone 更新。
- **CAST + PCGrad** 组合进一步提升性能，说明结构选择与几何优化可协同增效。

#### ✅ 消融实验结果（Appendix F）

##### （1）指标必要性（Table 8）
| 方法 | Llama (MMLU-CR, Pearson r) | Qwen (MMLU-CR, r) |
|------|----------------------------|-------------------|
| O(h) only | -0.13 | 1.00 |
| S(h) only | 0.96 | -0.10 |
| **C(h) = O·S** | **0.99** | **1.00** |

> **结论**：单一指标存在盲区，必须联合优化冲突与功能敏感性。

##### （2）稀疏比例分析（Table 9）
- **Bottom-25% (Safe Zone)** 达到最优效用。
- 增加至 Bottom-50% 反而降低性能，表明“更多参数 ≠ 更好”。

##### （3）数据效率（Table 10）
- 即使只用 **100 个 MMLU 样本**进行诊断，仍能准确识别 Safe Zone。
- 支持轻量级部署。

##### （4）领域迁移问题（Table 11）
- 若用 **GSM8K 数据诊断推理任务**，最佳区域不再是尾部（B4），而是中间桶（B2/B3），呈现“倒 U 形”。
- 原因：单头消融难以捕捉复杂推理链的依赖关系，导致敏感性低估。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **安全-效用冲突不是全局的**，而是集中在少数特定 attention heads（尤其是中深层）。
2. ✅ **通用能力退化主要源于对高冲突头的更新**，而非整体对齐过程。
3. ✅ **CAST 能有效绕过这些瓶颈**，实现“安全不牺牲效用”的对齐。
4. ✅ **Conflict Score C(h) 具有强预测力**：
   - 与 MMLU-CR 的 Pearson r ∈ [0.73, 0.95]
   - 支持事前风险评估，避免盲目训练。

### 方法的局限性（Limitations）
1. **Structural Scope**：
   - 当前仅关注 `Wq` 投影矩阵，未覆盖 MLP 层（占大部分参数）。
2. **Static Diagnosis**：
   - 冲突地图基于预对齐状态，假设其稳定；长期微调可能导致“conflict drift”。
3. **Task Dependence**：
   - 诊断依赖校准集（calibration set），在代码生成、创意写作等特殊领域可能失效。
4. **Single-Head Ablation 的局限性**：
   - 对复杂推理任务敏感性估计不准，需更精细的干预机制。

### 未来工作方向
- 扩展诊断至 **MLP 层** 和 **value/key projections**。
- 设计 **动态 conflict map**，支持在线更新。
- 探索 **multi-head ablation** 或 **causal tracing** 提升敏感性估计精度。
- 构建 **domain-specific calibration sets**，提升跨任务泛化能力。

---

## 总结
该论文提出了一个范式转变：从“全局优化”转向“结构感知的精准干预”。CAST 不仅提升了 safety-utility trade-off，还提供了可解释的诊断工具，为未来 LLM alignment 提供了一条高效、可控的新路径。

</details>

---

### 5. [Distributed Online Convex Optimization with Efficient Communication: Improved Algorithm and Lower bounds](https://arxiv.org/abs/2601.04907)

**Authors**: Sifan Yang, Wenhao Yang, Wei Jiang, Lijun Zhang  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.04907v1  

#### Abstract
We investigate distributed online convex optimization with compressed communication, where $n$ learners connected by a network collaboratively minimize a sequence of global loss functions using only local information and compressed data from neighbors. Prior work has established regret bounds of $O(...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Distributed Online Convex Optimization with Efficient Communication: Improved Algorithm and Lower bounds

## 1. 论文的主要贡献和创新点

### 解决的问题
本文研究**分布式在线凸优化（D-OCO）中的压缩通信问题**，即在多个学习者（learners）通过网络协作最小化全局损失函数时，如何在仅使用本地信息和邻居的**压缩数据**进行通信的前提下，实现高效的优化。

现有方法（如 Tu et al., 2022 的 DC-DOGD）存在以下缺陷：
- **Regret 上界对压缩质量因子 `w` 的依赖过强**：其 Regret 上界具有 `O(w⁻²)` 甚至 `O(w⁻⁴)` 的依赖，这意味着当压缩率较高（`w < 1`）时，理论性能会急剧下降。
- **对学习者数量 `n` 的依赖不理想**：Regret 上界与 `n` 呈超线性关系，不利于大规模系统。
- **缺乏下界分析**：此前的工作没有针对压缩通信场景建立 Regret 的下界，无法判断现有上界的优劣。

### 提出的新方法和新思路
作者提出了一种名为 **Two-level Compressed Decentralized Online Gradient Descent (Top-DOGD)** 的新算法，其核心创新在于一个**双层阻塞更新框架（two-level blocking update framework）**，该框架整合了两个关键技术：

1.  **在线压缩 Gossip 策略（online compressed gossip strategy）**：
    - 在每个“块”（block）的前半部分，执行多轮（`L₁` 轮）的压缩 Gossip 通信。
    - 这能显著加速学习者之间决策的一致性（consensus），从而降低**共识误差（consensus error）** 和**压缩误差（compression error）**。

2.  **投影误差补偿方案（projection error compensation scheme）**：
    - 在每个“块”的后半部分，递归地压缩并传输由投影操作（projection）引入的残差误差。
    - 这能有效控制**投影误差（projection error）**，避免其导致 Regret 对 `n` 的不良依赖。

**关键设计思想**：通过将 `T` 个迭代轮次划分为大小为 `L = L₁ + L₂` 的“块”，并在每个块内执行 `L₁` 轮 Gossip 和 `L₂` 轮误差补偿，最终只在块结束时更新一次决策。这样，虽然内部有多轮通信，但从外部看每轮仍只有一次通信，满足了 D-OCO 的约束，同时实现了更优的误差控制。

### 相比现有方法的优势
- **更优的 Regret 上界**：Top-DOGD 显著改进了 Regret 界限。
- **建立了首个下界**：论文首次为该问题提供了 Regret 下界，证明了其上界的近似最优性。
- **理论优势明显**：尤其在高通信压缩率（小 `w`）和大规模网络（大 `n`）场景下，新算法的理论保证远优于之前的工作。

## 2. 核心实验方法和设置

需要指出的是，这篇论文是一篇**理论性很强的研究论文**，其核心贡献是算法设计和理论分析，而非基于真实数据集的实验验证。因此，文中并未报告传统意义上的“实验结果”。

### 方法和设置
- **理论分析为主**：论文通过严格的数学推导来证明其提出的 Top-DOGD 算法的 Regret 上界，并构造特定的对抗性场景来证明其下界。
- **分析场景**：分析涵盖了两种主要的反馈设置：
    1.  **完整信息反馈（Full Information Feedback）**：学习者可以获取损失函数的梯度。
    2.  **Bandit 反馈**：学习者只能观测到损失值，无法直接获得梯度。此场景又细分为：
        -   **One-point Bandit Feedback**：每轮只能查询一个点的损失值。
        -   **Two-point Bandit Feedback**：每轮可以查询两个点的损失值。
- **评估指标**：核心评估指标是 **Regret**，定义为学习者累计损失与最优固定决策累计损失之间的差距。
- **基线方法对比**：主要与 **Tu et al. (2022)** 提出的 **DC-DOGD** 方法进行理论上的对比。

## 3. 主要实验结果和性能指标

由于是理论工作，这里的“结果”指的是**理论性能界限**。

### 关键性能数据（Regret 界限）
下表总结了本文（This work）与先前工作（Tu et al., 2022）以及本文建立的下界（Lower bound）的对比。其中 `n` 是学习者数量，`p` 是通信矩阵的谱隙（spectral gap），`w ∈ (0,1]` 是压缩质量因子，`T` 是总轮数。

#### 表 1：完整信息反馈下的 Regret 界限对比

| 来源 (Source) | 损失函数 (Loss functions) | Regret 上界 (Upper bounds) | Regret 下界 (Lower bounds) |
| :--- | :--- | :--- | :--- |
| Tu et al. (2022) | Convex | `O(max{w⁻²p⁻⁴n¹/², w⁻⁴p⁻⁸} n√T)` | - |
| Tu et al. (2022) | Strongly convex | `O(max{w⁻²p⁻⁴n¹/², w⁻⁴p⁻⁸} n ln T)` | - |
| **This work** | **Convex** | **`O(w⁻¹/²p⁻¹n √(ln n) √T)`** | **`Ω(w⁻¹/²p⁻¹/⁴n√T)`** |
| **This work** | **Strongly convex** | **`O(w⁻¹p⁻²n ln n ln T)`** | **`Ω(w⁻¹p⁻¹/²n ln T)`** |

#### 表 2：Bandit 反馈下的 Regret 界限对比

| 来源 (Source) | 设置 (Settings) | Regret 上界 (Upper bounds) |
| :--- | :--- | :--- |
| Tu et al. (2022) | Convex (1) | `O(max{w⁻¹p⁻²n¹/⁴, w⁻²p⁻⁴} d¹/²n T³/⁴)` |
| Tu et al. (2022) | Strongly convex (1) | `O(max{w⁻²/³p⁻⁴/³n¹/⁶, w⁻⁴/³p⁻⁸/³} d²/³n T²/³(ln T)¹/³)` |
| **This work** | **Convex (1)** | **`O(w⁻¹/⁴p⁻¹/²d¹/²n (ln n)¹/⁴ T³/⁴)`** |
| **This work** | **Strongly convex (1)** | **`O(w⁻¹/³p⁻²/³d²/³n (ln n)¹/³ T²/³(ln T)¹/³)`** |
| Tu et al. (2022) | Convex (2) | `O(max{w⁻²p⁻⁴n¹/², w⁻⁴p⁻⁸} d n √T)` |
| Tu et al. (2022) | Strongly convex (2) | `O(max{w⁻²p⁻⁴n¹/², w⁻⁴p⁻⁸} d² n ln T)` |
| **This work** | **Convex (2)** | **`O(w⁻¹/²p⁻¹ d n √(ln n) √T)`** |
| **This work** | **Strongly convex (2)** | **`O(w⁻¹p⁻² d² n ln n ln T)`** |

### 与基线方法的对比结果
- **在所有场景下，本文提出的 Regret 上界都显著优于 Tu et al. (2022)**。
- 最大的改进体现在对压缩因子 `w` 的依赖上：从 `O(w⁻²)` 或 `O(w⁻⁴)` 改进到 `O(w⁻¹/²)` 或 `O(w⁻¹)`，这是一个质的飞跃。
- 同时，对 `n` 的依赖也得到了改善（消除了 `max{}` 中的 `n¹/²` 项）。

### 消融实验结果
论文进行了**理论上的消融分析**（ablation analysis），论证了所提两个技术缺一不可：
- **若 `L₁=1`**（即不使用多步 Gossip）：算法退化为 DC-DOGD 加上误差补偿，无法改善 Regret 界限。
- **若 `L₂=0`**（即不使用投影误差补偿）：Regret 界限会恶化，例如对于凸函数变为 `O(w⁻¹/²p⁻¹n⁵/⁴ √(ln n) √T)`，对 `n` 的依赖变差。

## 4. 关键结论和发现

### 主要发现
1.  **成功改进了 Regret 界限**：通过 Top-DOGD 算法，实现了对压缩通信 D-OCO 问题 Regret 界限的重大突破，特别是在 `w` 和 `n` 的依赖上。
2.  **建立了理论最优性的基准**：首次为该问题提供了 Regret 下界，证明了其上界在 `w` 和 `T` 上是近乎最优的（up to `p` 和 `polylog(n)` 因子）。
3.  **验证了双层框架的有效性**：理论分析表明，**在线压缩 Gossip** 和 **投影误差补偿** 必须协同工作才能达到最佳效果。
4.  **扩展到 Bandit 场景同样有效**：将 Top-DOGD 与经典梯度估计器结合，在 Bandit 反馈场景下也取得了优于现有工作的 Regret 界限。

### 方法的局限性
- **缺乏实证验证**：论文完全基于理论分析，没有在真实或模拟的数据集上运行算法来验证其实际性能。其优势目前仅存在于理论上。
- **复杂性增加**：双层阻塞框架增加了算法的实现复杂度，需要仔细调整 `L₁` 和 `L₂` 等参数。
- **假设条件**：理论分析依赖于标准假设，如梯度有界、域有界等，这些在极端情况下可能不成立。

### 未来工作方向
- **进行大规模仿真实验**：在真实的分布式环境中实现 Top-DOGD，并与基线方法进行端到端的性能比较。
- **探索更实用的压缩器**：将理论应用于更具体的量化或稀疏化压缩方案。
- **放宽假设**：研究在非平稳环境、异步通信或动态网络拓扑下的性能。
- **应用于具体任务**：将该框架用于解决具体的分布式机器学习任务，如联邦学习中的在线学习。

</details>

---

### 6. [Aligning Text, Code, and Vision: A Multi-Objective Reinforcement Learning Framework for Text-to-Visualization](https://arxiv.org/abs/2601.04582)

**Authors**: Mizanur Rahman, Mohammed Saidul Islam, Md Tahmid Rahman Laskar, Shafiq Joty, Enamul Hoque  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.04582v1  

#### Abstract
Text-to-Visualization (Text2Vis) systems translate natural language queries over tabular data into concise answers and executable visualizations. While closed-source LLMs generate functional code, the resulting charts often lack semantic alignment and clarity, qualities that can only be assessed pos...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Aligning Text, Code, and Vision: A Multi-Objective Reinforcement Learning Framework for Text-to-Visualization*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Text-to-Visualization (Text2Vis)** 系统面临以下挑战：
- **闭源模型**（如 GPT-4o）虽然能生成可执行代码，但生成的图表常存在语义不一致、视觉混乱等问题。
- **开源模型**（尤其是 7B–14B 规模）在代码可执行性和可视化质量上表现更差，常输出语法错误或与查询意图不符的图表。
- 传统的 **Supervised Fine-Tuning (SFT)** 方法仅优化 token-level 损失，无法捕捉“执行后反馈”（post-execution feedback），例如图表的可读性、语义对齐等。

### 提出的新方法
作者提出 **RL-Text2Vis**，是首个专为 Text2Vis 设计的 **强化学习框架**，其核心创新包括：

- **基于 GRPO 的训练架构**：采用 **Group Relative Policy Optimization (GRPO)**，无需训练价值网络（critic），通过组内多个输出的相对优势进行策略更新，高效且稳定。
- **多目标奖励函数（multi-objective reward）**：联合优化三个维度：
  1. **文本准确性**（`R_text`）：生成答案是否与真实答案语义一致。
  2. **代码有效性**（`R_code`）：代码是否可执行 + 是否符合查询意图。
  3. **可视化质量**（`R_vis`）：图表是否清晰、正确、布局合理。
- **端到端整合执行后反馈**：首次将代码执行、图表渲染后的多模态反馈（文本、代码、图像）直接用于 RL 训练，实现对可视化质量的闭环优化。

### 相比现有方法的优势
| 维度 | 传统方法（SFT / Prompting） | RL-Text2Vis |
|------|----------------------------|------------|
| 可执行性 | ✅ 可提升 | ✅✅ 显著提升（78% → 97%） |
| 图表质量 | ❌ 无法优化 | ✅✅ 显著提升（+22% 超越 GPT-4o） |
| 多模态反馈利用 | ❌ 无 | ✅ 支持文本、代码、图像联合评估 |
| 部署友好性 | ✅ 开源可用 | ✅✅ 开源 + 高性能 + 隐私安全 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 特点 |
|--------|------|------|
| **Text2Vis** | In-Domain | 主要训练与评估集，含自然语言查询、表格数据、参考答案与代码；分为 `test1`（训练用）和 `test2`（评估用，236 样本） |
| **VIS-Eval** | Out-of-Domain | 包含 146 个数据库、2,524 查询，强调模糊映射与复杂布局 |
| **NVBench** | Out-of-Domain | 跨领域大规模数据集（105 domain, 7,247 任务），测试泛化能力 |
| **PandasPlotBench** | Out-of-Domain | 评估从 Pandas DataFrame 生成绘图代码的能力（175 样本） |

### 实验设置
- **模型基础**：基于 **Qwen2.5-Instruct** 系列（7B 和 14B），并验证了在 **Llama-3.1-8B** 上的通用性。
- **训练方式**：使用 GRPO 进行 RL 微调，每轮生成 G=8 个候选输出，计算组内标准化优势。
- **奖励机制**：
  - 第一阶段：格式奖励（必须返回合法 JSON，含 `answer` 和 `code` 字段）
  - 第二阶段：加权组合奖励 $ R = \alpha R_{\text{text}} + \beta R_{\text{code}} + \gamma R_{\text{vis}} $，最优权重为 (0.5, 0.25, 0.25)

### 评估指标
| 指标 | 定义 |
|------|------|
| **Answer Match (%)** | 生成答案是否与真实答案匹配（允许近似值、缩写等） |
| **Code Exec. Success (%)** | 生成代码能否成功运行（Matplotlib） |
| **Chart Readability** | 图表可读性评分（1–5 分，基于标签、颜色、字体等） |
| **Chart Correctness** | 图表是否准确反映查询意图和数据（1–5 分） |
| **Final Pass Rate (%)** | 同时满足：代码可执行、答案正确、可读性 ≥3.5、正确性 ≥3.5 |

### 基线方法对比
| 类别 | 基线模型 |
|------|---------|
| **闭源零样本** | GPT-4o, Gemini 1.5 Flash, Gemini 2.0 Flash |
| **开源零样本** | Llama-3.1-8B, Mistral-7B, CodeLlama-7B/13B, Qwen2.5-7B/14B |
| **监督微调（SFT）** | Qwen2.5-7B-SFT, Qwen2.5-14B-SFT |
| **其他 RL 方法** | PPO, DPO（未使用，因缺乏偏好数据） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Text2Vis 基准）

| Model | Code Exec. (%) | Answer Match (%) | Readability | Correctness | Final Pass (%) |
|-------|----------------|------------------|-------------|-------------|----------------|
| GPT-4o (Zero-Shot) | 87 | 39 | 3.32 | 3.30 | 30 |
| Qwen2.5-14B (Zero-Shot) | 78 | 29 | 3.12 | 2.94 | 14 |
| Qwen2.5-14B (SFT) | 87 | 36 | 3.42 | 3.28 | 18 |
| **RL-Text2Vis-14B** | **97** | **35** | **4.10** | **4.03** | **29** |

> ✅ **显著提升**：
> - 图表可读性：**+31%**（3.12 → 4.10）
> - 图表正确性：**+37%**（2.94 → 4.03）
> - 代码执行成功率：**+24%**（78% → 97%）
> - 相较 GPT-4o，图表质量 **相对提升 22%**

### Out-of-Domain 泛化能力（NVBench 示例）

| Model | Code Exec. (%) | Readability | Correctness |
|-------|----------------|-------------|-------------|
| Qwen2.5-7B (Zero-Shot) | 75 | 2.64 | 2.34 |
| Qwen2.5-7B (SFT) | 82 | 3.07 | 2.79 |
| **RL-Text2Vis-7B** | **93** | **3.47** | **3.28** |

> ✅ 在跨域任务中仍保持强劲性能，证明方法具有强泛化性。

### 消融实验结果（Ablation Study）

| 配置 | Code Exec. (%) | Answer Match (%) | Readability | Correctness | Final Pass (%) |
|------|----------------|------------------|-------------|-------------|----------------|
| Full (All Rewards) | 91 | 31 | 3.84 | 3.86 | 22 |
| - Format Reward | 87 | 25 | 3.38 | 3.25 | 17 |
| - Answer Reward | 89 | 24 | 3.79 | 3.82 | 19 |
| - Code & Visual Reward | 82 | 28 | 2.98 | 3.10 | 13 |
| Only Format Reward | 78 | 25 | 3.02 | 2.88 | 13 |

> 🔍 结论：
> - 移除任一奖励模块均导致性能下降
> - **多目标联合优化至关重要**，单独依赖某一信号无法达到最佳效果
> - 格式奖励虽基础，但对稳定性不可或缺

### 其他分析
- **人类评估一致性高**：人工标注与 GPT-4o 自动评估的相关系数达 **r ≈ 0.88–0.91**
- **采样数量影响**：每 prompt 生成 8 个样本（Gen-8）比 4 个（Gen-4）效果更好，说明更多候选有助于稳定 GRPO 排序
- **跨架构有效性**：在 Llama-3.1-8B 上应用 RL-Text2Vis 同样带来显著提升，验证框架通用性

---

## 4. 关键结论和发现

### 主要发现
1. **多目标 RL 是提升 Text2Vis 质量的有效路径**：相比 SFT，RL 能有效利用执行后反馈，显著提升图表质量和语义对齐。
2. **GRPO 是轻量高效的 RL 算法选择**：无需 critic 模型，适合长序列、多模态任务，在资源效率和性能之间取得良好平衡。
3. **开源模型可超越闭源模型**：RL-Text2Vis-14B 在图表可读性和正确性上 **超过 GPT-4o**，同时代码执行率更高（97% vs 87%）。
4. **方法具备强泛化能力**：在 VIS-Eval、NVBench 等 out-of-domain 数据集上表现优异，适用于多样化的实际场景。

### 局限性
1. **计算成本较高**：14B 模型训练需 6×H100 GPU，耗时约 50 小时，对小机构不友好。
2. **未探索更大模型**：受限于算力，未在 32B 或 72B 模型上验证，可能存在进一步提升空间。
3. **领域适应性待验证**：在医疗、金融等专业领域的鲁棒性尚未测试。
4. **静态图表限制**：目前仅支持静态可视化，未涉及交互式或多视图分析。

### 未来工作方向
- 扩展至 **交互式可视化生成**（Interactive Vis）
- 探索 **更高效的 RL 架构**（如蒸馏 + RL）
- 引入 **用户偏好反馈机制**，实现个性化图表风格定制
- 构建 **专用奖励模型**（Reward Model）替代通用 LLM/VLM 判断器
- 应用于 **多模态数据分析代理**（Multimodal Data Science Agents）

---

> 📢 **总结**：  
> RL-Text2Vis 成功将 **多目标强化学习** 引入 Text-to-Visualization 领域，通过 **GRPO + 多模态奖励设计**，实现了对文本、代码、视觉三重质量的协同优化。实验证明其不仅显著优于各类基线，还能在 out-of-domain 场景下稳健泛化，为构建高质量、可解释、可部署的可视化系统提供了新范式。

🔗 项目代码已开源：[https://github.com/vis-nlp/RL-Text2Vis](https://github.com/vis-nlp/RL-Text2Vis)

</details>

---

### 7. [Learning Dynamics in RL Post-Training for Language Models](https://arxiv.org/abs/2601.04670)

**Authors**: Akiyoshi Tomihari  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.04670v1  

#### Abstract
Reinforcement learning (RL) post-training is a critical stage in modern language model development, playing a key role in improving alignment and reasoning ability. However, several phenomena remain poorly understood, including the reduction in output diversity. To gain a broader understanding of RL...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Dynamics in RL Post-Training for Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于**强化学习后训练（RL post-training）在语言模型中的学习动态机制尚不明确**的问题，尤其是以下现象缺乏理论解释：
- **输出多样性下降**：RL 后训练常导致模型输出分布更加集中，降低生成多样性。
- **推理能力提升有限**：尽管 RL 能提高奖励得分，但可能并未真正激发新的推理模式，而是对已有行为进行加权放大（"amplify existing patterns"）。

这些问题限制了对 RLHF（Reinforcement Learning from Human Feedback）和 RLVR（Reinforcement Learning with Verifiable Rewards）机制的理解与优化。

---

### 提出的新方法与新思路

作者提出了一种基于**经验神经正切核（empirical Neural Tangent Kernel, NTK）** 的分析框架，用于形式化 RL 后训练的学习动态，并据此提出一种新的两阶段训练策略：

#### （1）理论分析框架：NTK 分解
将 empirical NTK 分解为两个组件：
- **Representation Component**（表示分量）：衡量特征向量之间的相似性。
- **Gradient Component**（梯度分量）：包含分类器 $ W $ 和特征映射梯度，反映参数更新的方向。

该分解揭示了：
- 当特征表示高度相似时（如表1所示，cosine similarity 平均 > 0.6），**Representation Component 会系统性地增强高奖励样本的概率，导致输出分布尖锐化（即置信度上升、多样性下降）**。
- 因此，有效的学习依赖于 **Gradient Component**，而它显式依赖于分类器 $ W $。

#### （2）新方法：Classifier-First Reinforcement Learning (CF-RL)
受监督学习中“先线性探测再微调”（Linear Probing then Fine-Tuning, LP-FT）启发，提出 **CF-RL**：
- **第一阶段**：仅更新分类器（classifier），冻结其余所有参数；
- **第二阶段**：恢复标准 RL 训练，联合优化全部参数。

这一设计旨在早期快速塑造 Gradient Component，从而提升后续 RL 优化效率。

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **理论解释力** | 首次从 NTK 视角形式化 RL 后训练的学习动态，解释了“为何 RL 会导致输出集中”的根本原因。 |
| **方法简洁有效** | CF-RL 不引入额外超参或复杂模块，仅调整训练顺序即可加速收敛。 |
| **机制新颖** | 发现 CF-RL 的作用机制不同于 LP-FT —— 它不是通过抑制特征畸变（feature distortion）或放大分类器范数实现的，而是改变了分类器对结构类 token 的响应。 |

---

## 2. 核心实验方法和设置

### 数据集
- **SFT 阶段**：使用 `AlpacaFarm` 进行监督微调。
- **RL 阶段**：使用 `UltraFeedback` 的二值化版本作为偏好数据集，结合 ArmoRM 作为 ground-truth reward model 提供标量奖励信号。
- 所有 prompt 和 response 被截断至最多 512 tokens。

### 模型架构
- 基础模型：Pythia-2.8B
- 架构细节：Transformer + 线性 unembedding classifier（即输出层权重矩阵 $ W $）

### 实验设置
- 使用 **GRPO** 或 **RLOO** 作为 policy gradient 算法。
- KL 正则系数 $ \lambda = 0.05 $
- 学习率：SFT 为 $1e^{-6}$，RL 为 $1e^{-7}$
- Batch size：SFT 为 32，RL 为 16（含梯度累积）
- 训练轮数：各阶段均为 6 epochs（CF-stage 为 1 epoch）

### 评估指标
| 指标 | 描述 |
|------|------|
| **First-token entropy** | 衡量初始 token 分布的集中程度，越低表示多样性越差。 |
| **Best-of-N reward** | 采样 N 个输出取最高 reward，用于评估概率集中是否带来性能增益。 |
| **Semantic & Style Diversity** | 对多个输出嵌入后计算平均余弦距离，衡量语义和风格多样性。 |
| **Parameter change norm** | 各模块参数变化幅度（如 classifier vs. transformer layers）。 |
| **Classifier update analysis** | 分析分类器各行（对应每个 token）的更新强度。 |

### 基线方法对比
- **Standard RL**（GRPO / RLOO）：常规全参数微调。
- **CF-RL**（CF-GRPO / CF-RLOO）：提出的分类器优先训练策略。
- 同时与 **LP-FT** 在监督微调中的机制进行类比与区分。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）RL 导致输出更集中（支持理论）
- 图2b 显示：随着 RL 训练进行，**first-token entropy 单调下降**，说明输出分布持续尖锐化。
- 表2 显示：经过一轮 RL 后，
  - 高奖励样本的 first-token entropy：**1.516 ± 0.931**
  - 低奖励样本：**2.439 ± 1.278**
  ➜ 表明 RL 主要在高奖励样本上提升置信度。

#### （2）CF-RL 加速优化过程
- 图4 显示：CF-GRPO 在第1个 epoch 就实现了显著 reward 提升，明显快于标准 GRPO。
- 尽管第一阶段（仅训练 classifier）本身 reward 提升很小，但为后续阶段提供了更好的起点。

#### （3）多样性代价确认
- 图3b 显示：无论是 semantic 还是 style diversity，在 RL 训练过程中都**持续下降**，验证了“以牺牲多样性换取 reward 提升”的权衡。

---

### 与基线方法的对比结果
| 方法 | Reward 收敛速度 | 输出多样性 | 分类器学习速度 |
|------|------------------|------------|----------------|
| Standard RL (GRPO/RLOO) | 较慢 | 快速下降 | 较快（但仍慢于其他层） |
| **CF-RL (CF-GRPO)** | **显著加快** | 类似下降趋势 | **最早发生显著变化** |

---

### 消融实验与深入分析

#### （1）CF-RL 是否减少特征畸变？ ❌ 否
- 图5 显示：CF-RL 与标准 RL 在 feature change 幅度上的差异分布中心接近零。
- 结论：**CF-RL 的优势并非来自抑制 feature distortion**，这与 LP-FT 不同。

#### （2）CF-RL 是否增大分类器范数？ ❌ 否
- 表 S.3 显示：SFT、GRPO、CF-stage、CF-GRPO 的 classifier norm 几乎完全一致（≈0.989）。
- 结论：**性能提升不是由于 classifier scaling 效应**。

#### （3）分类器更新的 token 级别分析 ✅ 新发现
- 表3 显示：
  - 标准 GRPO 主要增强**内容词**（如 bringing, Several, it）。
  - CF-stage 和 CF-GRPO 则大幅更新**结构性 token**，例如：
    - `<endoftext>`, `\n`, `\x0c`, `Supplementary`, `Appendix`, `[·]`, `<U+FFFD>`
- 结论：**CF-RL 使分类器更关注文档结构、格式控制等元信息**，这些信息可能有助于 reward 建模和策略引导。

---

## 4. 关键结论和发现

### 主要发现
1. **RL 后训练增加模型置信度的根本原因是 Representation Component 的局限性**：
   - 特征空间高度对齐（high cosine similarity）导致更新方向受限，只能强化已选 token。
2. **有效的 RL 学习依赖于 Gradient Component，而其质量由分类器决定**。
3. **分类器是 RL 优化中最先快速变化的部分**，表明其在学习动态中起主导作用。
4. **CF-RL 可加速 RL 收敛**，且其机制独特——**通过早期调整分类器对结构 token 的敏感性来改善 Gradient Component**，而非复制 LP-FT 的机制。

---

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **适用范围待验证** | 当前实验仅在 Pythia-2.8B 上验证，更大模型（如 7B+）或不同 reward 结构下的效果需进一步研究。 |
| **未解决多样性本质问题** | CF-RL 加速优化，但未缓解输出多样性下降的问题。 |
| **依赖高质量 reward model** | 使用 ArmoRM 作为 oracle reward，实际应用中 reward model 偏差会影响 CF-RL 效果。 |
| **理论假设较强** | 如 Proposition 2 假设特征内积非负，在极端情况下可能不成立。 |

---

### 未来工作方向
1. **扩展 CF-RL 至多阶段或多任务 RL 微调场景**。
2. **探索如何保留多样性的同时提升 reward**，例如结合 deviation-weighted optimization。
3. **将 NTK 动态分析应用于其他 RL 算法**（如 DPO、iDPO）以统一理解 alignment 方法。
4. **设计可解释的 classifier interpretability 工具**，追踪结构 token 权重变化的意义。
5. **研究 CF-RL 在 RLVR（可验证奖励）任务中的推理能力激发潜力**。

---

> ✅ **一句话总结**：  
> 本论文首次从 NTK 视角揭示了 RL 后训练中输出集中化的动因，并提出 **CF-RL** —— 一种简单却高效的两阶段训练策略，通过优先更新分类器来加速优化，其机制独立于传统的 LP-FT 范式，为理解语言模型对齐训练提供了新视角。

</details>

---

### 8. [GPU-Accelerated INT8 Quantization for KV Cache Compression in Large Language Models](https://arxiv.org/abs/2601.04719)

**Authors**: Maanas Taneja, Purab Shingvi  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.04719v1  

#### Abstract
The key-value (KV) cache in large language models presents a significant memory bottleneck during inference, growing linearly with sequence length and often exceeding the memory footprint of model weights themselves. We implement and evaluate GPU-accelerated INT8 quantization for KV cache compressio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GPU-Accelerated INT8 Quantization for KV Cache Compression in Large Language Models

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在推理过程中面临显著的内存瓶颈，尤其是 **Key-Value (KV) Cache** 的内存消耗。随着序列长度增长，KV Cache 的大小呈线性增加，在长上下文场景下（如 32K、128K tokens），其内存占用甚至超过模型权重本身，严重限制了最大上下文长度、批处理大小，并提高了部署成本。

### 提出的新方法与创新点
本文提出并系统评估了一种 **GPU 加速的 per-channel INT8 量化方案**，用于压缩 LLM 推理中的 KV Cache，主要贡献如下：

- **高效实现与优化**：设计并实现了四种 CUDA 内核变体（naive、tiled、coarsened、vectorized），探索不同 GPU 并行优化策略对 KV Cache 量化性能的影响。
- **细粒度量化策略**：采用 **per-channel quantization**，为每个注意力头维度单独计算 scale 因子，以保留动态范围差异较大的维度的精度。
- **全面的性能-精度分析**：不仅报告端到端速度提升，还深入分析了重建误差（reconstruction error）和注意力分数误差（attention score error），揭示量化对下游计算的实际影响。
- **实用性强**：展示了该方法在真实 LLM 工作负载下的可行性，量化开销仅为 6–58ms，远小于实际 attention 计算时间。

### 相比现有方法的优势
| 方面 | 现有方法 | 本文优势 |
|------|--------|---------|
| **FlashAttention** | 通过 kernel fusion 优化计算，不压缩缓存 | 实现 **4× 内存压缩**，直接缓解内存压力 |
| **PagedAttention** | 减少内存碎片，仍保持 FP32 存储 | 在相同机制基础上可进一步叠加量化节省内存 |
| **LLM.int8()** | 主要针对权重和激活量化 | 聚焦于 **KV Cache 这一特定瓶颈**，提供专用优化 |
| **其他极端量化（如 KIVI, KVQuant）** | 探索 2-bit 或 sub-4-bit | 本文方法在 **精度损失极小的前提下实现简单高效**，更适合生产部署 |

---

## 2. 核心实验方法和设置

### 数据集 / 测试配置
本研究未使用传统“数据集”，而是构建了一系列 **合成矩阵测试用例** 来模拟真实的 KV Cache 场景，覆盖从小规模到超大规模的工作负载：

| Test Case | Tokens (T) | Head Dim (D) | 描述 |
|----------|------------|--------------|------|
| Small | 2,048 | 128 | 最小测试 |
| Medium | 16,384 | 256 | 开发测试 |
| Large | 65,536 | 256 | 扩展上下文 |
| Very Large | 131,072 | 256 | 长上下文 |
| Realistic Small ~ V. Large | 131,072 | 1,024 ~ 8,192 | 模拟现代 LLM 的真实工作负载（高达 10 亿元素） |

### 实验设置
- **硬件平台**：
  - GPU: NVIDIA Tesla T4 (16GB GDDR6)
  - CPU: 2× Intel Xeon Gold 6148 (共 40 核)
  - CUDA 版本: 12.0
- **实现方式**：
  - CPU 基线：C++ 实现，作为正确性和性能基准
  - GPU 实现：四种 CUDA kernel（naive、tiled、coarsened、vectorized）
- **量化方式**：Per-channel INT8 量化，scale 定义为 $ s_d = \frac{\max_t |K_{t,d}|}{127} $

### 评估指标
| 指标 | 定义 | 目的 |
|------|------|------|
| **Performance (Speedup)** | GPU kernel vs CPU baseline 的运行时间比 | 衡量加速效果 |
| **L2 Reconstruction Error** | 原始 FP32 矩阵与反量化后矩阵之间的 L2 距离 | 评估数值保真度 |
| **Max Absolute Error** | 所有元素中最大绝对误差 | 分析最坏情况误差边界 |
| **Attention Score Error** | 使用原始 vs 重构 Key 向量计算的 attention dot product 的平均绝对差 | 判断是否影响下游注意力机制行为 |

### 基线方法对比
- **CPU Baseline**：串行实现，用于衡量 GPU 加速比
- **四种 GPU Kernel 变体之间相互比较**，验证不同优化策略的有效性

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 结果 |
|------|------|
| **内存压缩率** | **4× reduction**（从 FP32 → INT8） |
| **最高 GPU 加速比** | **1,694× speedup**（vectorized kernel 在 131K × 8K 规模下） |
| **最大执行时间（GPU）** | < 50ms（即使处理 10 亿元素） |
| **典型量化延迟（现实负载）** | **6 – 58 ms**，远低于 attention 计算耗时 |
| **CPU 最大耗时** | 达 **79 秒**（同规模任务） |

### 与基线方法的对比结果
- **Vectorized Kernel 显著优于其他所有版本**：
  - 在最大负载上比 naive kernel 快数倍
  - 比 CPU 快三个数量级（见 Figure 2）
- **Tiled Kernel 未带来收益**：
  - 尽管使用 shared memory 缓存 scales，但由于 scales 数组较小且能驻留 L2 cache，额外同步开销抵消了潜在好处，性能与 naive kernel 相当甚至略差
- **Coarsened Kernel 提升有限**：
  - 通过减少线程数、提高 ILP 获得小幅改进，但在大负载下增益趋于饱和

### 消融实验结果
通过对四种 kernel 的横向对比，得出以下关键发现（本质是消融分析）：

| 优化策略 | 是否有效 | 原因 |
|---------|----------|------|
| **Memory Coalescing** | ✅ 有效 | Naive kernel 已实现良好列向合并访问 |
| **Shared Memory Tiling** | ❌ 无效 | 无数据复用，同步开销 > 缓存收益 |
| **Thread Coarsening** | ⚠️ 有限有效 | 减少指令开销，但受限于 memory-bound 特性 |
| **Vectorization (float4/char4)** | ✅✅ 极其有效 | 减少内存事务数达 4×，直接提升带宽利用率 |

---

## 4. 关键结论和发现

### 主要发现
1. **INT8 量化是缓解 KV Cache 内存瓶颈的实用方案**：
   - 实现 **4× 内存压缩**，显著降低显存压力，支持更长上下文和更大 batch size。
2. **GPU 加速至关重要**：
   - CPU 上量化延迟过高（数十秒级），无法用于实时推理；而 GPU 实现可在 **毫秒级完成**，开销可忽略。
3. **Vectorization 是最优优化路径**：
   - KV Cache 量化属于典型的 **memory-bound** 操作，算术强度低；
   - 一旦实现内存合并访问，进一步优化应聚焦于 **减少内存事务次数**，而非引入复杂同步或调度；
   - **vectorized kernel 利用 float4/char4 向量指令，最大化带宽利用率，表现最佳**。
4. **精度损失极小**：
   - 最大绝对误差稳定在 **0.00394**（理论上限为 1/254 ≈ 0.00394）；
   - 注意力分数误差在最大维度（D=8192）下也仅 **0.095**，远小于 softmax 前的数值范围，不会显著改变 attention 分布。

### 方法的局限性
- **仅限单 GPU 实现**：未考虑分布式或多 GPU 场景下的扩展性。
- **未进行端到端任务评估**：缺乏在具体任务（如 QA、generation）上的 **perplexity 或 accuracy** 测量。
- **固定 bit-width（INT8）**：未探索更低比特（如 INT4、INT2）或混合精度策略。
- **静态 scale 计算**：未采用动态或自适应量化策略应对 outlier 输入。
- **依赖特定数据布局**：vectorized kernel 要求维度 D 可被 4 整除，需额外处理边缘情况。

### 未来工作方向
1. **集成至主流推理框架**：
   - 将 kernel 集成进 **vLLM、TensorRT-LLM 或 HuggingFace Transformers**，进行端到端性能评测。
2. **探索更先进 GPU 优化技术**：
   - 使用 warp-level primitives（如 `__shfl_down_sync`）优化 scale reduction；
   - 设计 persistent kernel 以摊销流式推理的启动开销。
3. **尝试新型数值格式**：
   - 探索原生支持 **FP8** 的新架构（如 Hopper）是否能在精度与效率间取得更好平衡。
4. **开展全面准确性评估**：
   - 在标准 benchmark（如 WikiText、LAMBADA）上测量 **perplexity 变化** 和生成质量退化。
5. **研究混合与动态量化策略**：
   - 对重要层或 token 保留高精度，其余部分量化，实现精度-效率权衡。

---

> 🔗 **代码开源地址**：[https://github.com/MaanasTaneja/cuda-kv-cache-compression](https://github.com/MaanasTaneja/cuda-kv-cache-compression)

</details>

---

### 9. [Approximate equivariance via projection-based regularisation](https://arxiv.org/abs/2601.05028)

**Authors**: Torben Berndt, Jan St\"uhmer  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.05028v1  

#### Abstract
Equivariance is a powerful inductive bias in neural networks, improving generalisation and physical consistency. Recently, however, non-equivariant models have regained attention, due to their better runtime performance and imperfect symmetries that might arise in real-world applications. This has m...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Approximate equivariance via projection-based regularisation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
在深度学习中，**equivariance**（等变性）是一种强大的归纳偏置（inductive bias），能够提升模型的泛化能力和物理一致性。然而，严格等变的模型通常计算开销大、灵活性差，且现实世界的数据常存在不完美的对称性（如噪声、非理想变换）。因此，研究者倾向于使用**近似等变模型**（approximately equivariant models），在保留对称性优势的同时适应真实数据分布。

现有方法多采用**基于样本的正则化**（sample-based regularisers），例如通过数据增强（data augmentation）在训练时随机采样群元素并施加等变约束。这类方法存在以下问题：
- **高样本复杂度**（high sample complexity），尤其对于连续群（如 SO(3)）；
- 需要额外前向传播，增加训练成本；
- 正则化仅作用于输入点，而非模型参数本身。

### **提出的新方法**
本文提出一种全新的**基于投影的等变正则化方法**（projection-based equivariance regularisation），其核心思想是：
- 利用线性算子在**群表示下的正交分解**（orthogonal decomposition），将任意线性层 $ T $ 分解为等变部分 $ P(T) $ 和非等变部分 $ T - P(T) $；
- 在训练目标中直接惩罚非等变分量的范数 $ \|T - P(T)\| $，从而在**算子层面**（operator level）实现对整个群轨道（group orbit）的正则化。

该方法的关键公式为：
$$
\mathcal{L} = \mathcal{L}_{\text{task}} + \lambda_G \|P(T)\| + \lambda_\perp \|T - P(T)\|
$$
其中 $ P(T) $ 是 $ T $ 在等变子空间上的正交投影（Reynolds operator）。

### **相比现有方法的优势**
| 维度 | 本文方法（Projection-based） | 传统方法（Sample-based） |
|------|-------------------------------|---------------------------|
| **正则化粒度** | 算子级（全局） | 点级（局部） |
| **是否依赖数据采样** | 否 | 是（需采样 group elements） |
| **计算效率** | 高（可闭式计算，无额外前向） | 低（每样本需多次前向） |
| **方差** | 零估计方差 | 存在采样方差 |
| **适用于连续群** | 是（可通过傅里叶域高效计算） | 困难（需大量采样） |

此外，作者提供了在**空间域**和**谱域**（Fourier domain）高效计算投影的方法，特别适合处理连续群（如 SO(n)）。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
1. **合成数据集**：
   - **SO(2)不变分类任务**：二维点云分类，内圆为一类，外环为另一类，测试模型学习旋转不变性的能力。
   - **烟雾动力学预测**（smoke advection-diffusion）：使用 PhiFlow 生成 64×64 烟雾扩散序列，模拟具有**近似对称性**的动力系统（平移、旋转、缩放）。
2. **真实医学图像数据集**：
   - **AAPM CT-MAR Grand Challenge**：14,000 张带金属伪影的头部和身体 CT 切片，用于金属伪影去除（Metal Artifact Reduction, MAR）任务。

### **实验设置与评估指标**
| 任务 | 模型架构 | 评估指标 | 设置说明 |
|------|----------|----------|----------|
| SO(2) Invariance | 复数 MLP + 圆谐函数嵌入 | 决策边界可视化、经验等变缺陷 $ \mathcal{E}_{\text{emp}}(T) $ | 控制 $ \lambda_G, \lambda_\perp $ 调节等变强度 |
| 动力学预测 | Relaxed Group Conv / Steerable CNN | 像素级 MSE（Future / Domain OOD） | 测试未来时间步和未见空间区域 |
| CT-MAR | ACDNet / DICDNet / OSCNet | PSNR、SSIM、吞吐量、内存占用 | 比较固定 batch 和最大可行 batch 下的表现 |

### **基线方法对比**
- **Sample-based regulariser** (Bai et al., 2025)：在损失中加入随机旋转下的等变误差项。
- **Residual Pathway Priors (RPP)** (Finzi et al., 2021)：主干网络 + 小的等变残差路径。
- **Train-then-project**：先训练非等变模型，测试时投影到等变子空间。
- **Relaxed Group / Steerable CNNs** (Wang et al., 2022c)：放宽权重共享机制。
- **标准 CNN / 等变 CNN** 作为上下界参考。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) CT-MAR 任务（表 2）**
以 **ACDNet + projection-based regulariser** 为例：
- **PSNR**: 42.68 dB（优于 baseline 42.08，优于 sample-based 40.02）
- **SSIM**: 0.9620（接近 sample-based 的 0.9623，显著优于 baseline 0.9559）
- **训练吞吐量**（batch size 12）: 4.25 samples/GPU-s（sample-based 仅支持 batch=4，吞吐 2.54）
- **推理吞吐量**: 4.99 samples/GPU-s
- **epoch 时间**: 1202 秒（sample-based 为 2011 秒）

> ✅ **结论**：投影法在性能上持平或超越 sample-based 方法，同时训练速度提升 **~40–60%**。

#### **(2) 动力学预测任务（表 1）**
在 **Rotation** 和 **Scaling** 设置下：
- **RGroup + Reg** 在 Rotation-Future 上 MSE 达 **0.73±0.02**，优于原始 RGroup (0.82±0.01) 和所有其他 baseline。
- **RSteer + Reg** 在 Scaling-Domain 上 MSE 为 **0.69±0.01**，显著优于 baseline (0.88±0.01)。

> ✅ **结论**：投影正则化能有效提升在近似对称系统中的泛化能力。

#### **(3) SO(2) Invariance Toy Task**
- 随着 $ \lambda_\perp $ 增大，经验等变缺陷 $ \mathcal{E}_{\text{emp}}(T) $ 显著下降（从 $ 3.02e+01 $ 降至 $ 1.53e-02 $）。
- 即使数据引入角度扰动（angular wave），模型仍能“尽可能保持圆形”，仅在必要处偏离对称性。

---

### **消融实验结果**

#### **(1) 正则化系数敏感性分析（图 7, 8, 9）**
- $ \lambda_\perp $ 是控制等变性的主要因素：增大 $ \lambda_\perp $ 显著降低等变缺陷 $ \mathcal{E}(T) $。
- $ \lambda_G $ 影响较小；当 $ \lambda_\perp \ll \lambda_G $ 时，正则化退化为普通 L2 正则化。
- 在 MNIST/CIFAR 分类任务中，更强的等变偏好（高 $ \lambda_\perp $）带来更高准确率。

#### **(2) 范数选择的影响（表 5）**
比较不同矩阵范数：
- **Frobenius norm**（默认）表现稳定，PSNR ≈ 38.48，SSIM ≈ 0.9457。
- **Spectral norm** 计算更慢（epoch +13%），性能无提升。
- **Infinity norm** 和 **(1,1)-norm** 导致明显性能下降（PSNR < 36），表明过于强调极值会欠拟合。

> ✅ 最终选择 **Frobenius norm** 作为默认配置。

---

## **4. 关键结论和发现**

### **主要发现**
1. **投影正则化是一种理论上严谨且高效的近似等变方法**：
   - 通过正交分解直接作用于模型权重，避免了样本级正则化的高方差和高计算成本。
   - 理论上证明了 $ \|T - P(T)\| $ 与最大等变缺陷 $ \mathcal{E}(T) $ 成常数倍关系（Lemma 3.2），正则化效果有保障。

2. **在多种任务和群结构下均优于现有方法**：
   - 在连续群（SO(2)）、离散群（C4）上均有效。
   - 在合成和真实任务中 consistently 提升性能，尤其在 OOD 泛化场景。

3. **显著提升训练效率**：
   - 无需额外前向传播，支持更大 batch size。
   - 在 CT-MAR 任务中，吞吐量提升 **42–61%**，epoch 时间减少近一半。

### **方法的局限性**
- **需要为每种架构和群操作重新推导投影形式**，通用性受限。
- 当前实验集中在简单群（如 SO(2), C4, translation），尚未扩展到复杂复合群（如 SE(3) 的子群组合）。
- 对非线性层的等变性建模仍依赖传统方式（如 equivariant activation），未完全统一框架。

### **未来工作方向**
- 扩展至更复杂的群结构（如 material science 中的晶格对称群）。
- 探索自动推导投影算子的方法，提升框架通用性。
- 结合 spectral parameterization（如 eSEN）进一步优化训练效率。

---

> **总结**：本文提出的 **projection-based regularisation** 是一种**理论扎实、高效实用**的近似等变学习框架，解决了传统 sample-based 方法的效率瓶颈，在多个任务上实现了**性能与速度的双重提升**，为大规模应用等变性提供了新思路。

</details>

---

### 10. [LLMs for Explainable Business Decision-Making: A Reinforcement Learning Fine-Tuning Approach](https://arxiv.org/abs/2601.04208)

**Authors**: Xiang Cheng, Wen Wang, Anindya Ghose  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.04208v1  

#### Abstract
Artificial Intelligence (AI) models increasingly drive high-stakes consumer interactions, yet their decision logic often remains opaque. Prevailing explainable AI techniques rely on post hoc numerical feature attributions, which fail to provide coherent narratives behind model decisions. Large langu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLMs for Explainable Business Decision-Making: A Reinforcement Learning Fine-Tuning Approach

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对高风险商业决策（如贷款审批）中人工智能模型缺乏可解释性的挑战，指出当前主流的 **post hoc** 可解释性方法（如 SHAP、LIME）存在三大缺陷：

1. **解释不忠实（Unfaithful）**：数值特征归因是事后近似，无法真实反映模型决策逻辑。
2. **缺乏叙事能力（Non-narrative）**：输出为数字权重，而非人类可理解的自然语言叙述。
3. **多受众适配困难**：同一决策需面向专家（如贷款官）和消费者提供不同风格的解释，但传统方法难以在不改变决策规则的前提下实现风格调整。

此外，依赖人工标注的高质量解释进行监督微调成本高昂，限制了大规模部署。

---

### **提出了什么新方法或新思路**

作者提出 **LEXMA**（LLM-based EXplanations for Multi-Audience decisions），一个基于 **Reinforcement Learning** 的两阶段微调框架，其核心创新如下：

#### ✅ **1. 决策-解释联合生成架构（Joint Reasoning → Explanation → Prediction）**
- 强制模型先生成内部推理链（Reasoning），再提炼出简洁解释（Explanation），最后做出预测（Prediction）。
- 预测结果依赖于解释内容，确保解释与决策逻辑对齐（decision-aligned），提升解释的**忠实性（faithfulness）**。

#### ✅ **2. 双适配器架构（Dual Adapters）分离“正确性”与“语气”**
- **ACC Adapter**（Correctness Adapter）：负责学习决策边界和风险因素，用于所有受众。
- **TONE Adapter**（Tone Adapter）：仅用于消费者场景，调整语气（如礼貌、易读性），不影响决策。
- 实现**多受众通信**：专家端激活 ACC；消费者端同时激活 ACC + TONE，保证决策一致性。

#### ✅ **3. 标签高效训练（Label-Efficient Training）**
- **第一阶段**：使用强参考模型（GPT-5）生成带反思（reflection-augmented）的监督数据，无需人工标注。
- **第二阶段**：采用 **Group Relative Policy Optimization (GRPO)** 进行强化学习微调：
  - GRPO-Step1：优化 ACC Adapter，奖励信号仅为**决策正确性**。
  - GRPO-Step2：优化 TONE Adapter，奖励信号为**可读性（Flesch-Kincaid ≤ 8）** 和 **礼貌性（politeness density）**，均为规则驱动，无需人工评分。

---

### **相比现有方法的优势**

| 维度 | 传统方法（SHAP/LIME） | 通用 LLM（如 GPT-5/Qwen） | LEXMA |
|------|------------------------|----------------------------|-------|
| 解释形式 | 数值归因 | 自然语言，但可能不忠实 | 忠实且叙事性强 |
| 多受众支持 | 否 | 通过提示词调整，但可能改变决策 | 是，通过双适配器解耦 |
| 训练成本 | 低（post hoc） | 高（需大量人工标注解释） | 低（无需人工标注） |
| 决策准确性 | 依赖黑盒模型 | 通常低于树模型 | 接近 XGBoost 水平 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **HMDA Loan Application Register**（Home Mortgage Disclosure Act）
  - 来源：美国消费者金融保护局（CFPB）公开数据
  - 时间范围：2024年子集（避免 LLM 记忆问题）
  - 样本量：超百万条贷款申请记录
  - 特征：贷款金额、利率、债务收入比（DTI）、信用评分等结构化数据
  - 标签：`Approve` / `Deny`（Action Taken = 1 或 3）
  - 预处理：去除受保护属性（种族、性别），构建类别平衡训练集（50%/50%）

---

### **实验设置和评估指标**

#### **模型选择**
- 基座模型：**Qwen3-4B**
- 对比模型：
  - 传统 ML：XGBoost、Neural Network、Logistic Regression、Gradient Boosting
  - LLM 基线：原始 Qwen3-4B、GPT-5（median reasoning intensity）

#### **微调流程（三阶段）**
1. **SFT**（Supervised Fine-Tuning）：使用 GPT-5 生成带反思的数据进行监督训练
2. **GRPO-Step1**：优化 ACC Adapter，提升决策准确性
3. **GRPO-Step2**：优化 TONE Adapter，提升消费者解释的可读性和礼貌性

#### **评估指标**

| 类别 | 指标 |
|------|------|
| **决策正确性** | F1 Score、Accuracy、Precision、Recall |
| **专家解释质量** | 人工评估：Risk Relevance、Decision Appropriateness、Explainability（Likert 1–5） |
| **消费者解释质量** | 人工评估：Clarity、Politeness、Fairness、Actionability、Trustworthiness、Satisfaction（Likert 1–5） |
| **自动风格评估** | Flesch-Kincaid Grade Level（目标 ≤ 8）、Politeness Density |

#### **人类评估设置**
- **专家评估**：3 名资深贷款官，评估 30 对样本
- **消费者评估**：110 名 Prolific 用户，完成 536 对比较任务

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **决策正确性（Test Set）**

| 模型 | Prompt | F1 | Accuracy |
|------|--------|-----|----------|
| **LEXMA (GRPO-Step1)** | Expert | **0.897** | **0.845** |
| **LEXMA (GRPO-Step2)** | Consumer | **0.893** | **0.825** |
| Raw Qwen3-4B | Expert | 0.723 | 0.627 |
| Raw Qwen3-4B | Consumer | 0.726 | 0.632 |
| GPT-5 | Expert | 0.730 | 0.639 |
| GPT-5 | Consumer | 0.771 | 0.679 |
| XGBoost | — | 0.917 | 0.875 |

> ✅ LEXMA 显著优于所有 LLM 基线，接近 XGBoost 性能，远超原始 Qwen 和 GPT-5。

---

#### **消融实验（Ablation Study）**

| 阶段 | F1 (Expert) | Accuracy (Expert) | 贡献 |
|------|------------|------------------|------|
| Raw Qwen | 0.723 | 0.627 | 基线 |
| + SFT | 0.825 | 0.757 | +10.2 F1，结构化训练有效 |
| + GRPO-Step1 | **0.897** | **0.845** | +7.2 F1，RL 显著提升准确性 |
| + GRPO-Step2 | 0.902 | 0.851 | 几乎无损，验证**语气微调不破坏决策** |

> 🔍 说明 GRPO-Step2 成功实现了“只改语气，不动决策”。

---

#### **解释质量（人工评估）**

##### **专家解释偏好**
- **80%** 的案例中，专家更偏好 LEXMA 生成的解释。
- 所有维度显著提升（p < 0.01）：
  - **Risk Relevance**: 3.4 → 4.4
  - **Decision Appropriateness**: 2.2 → 3.9
  - **Explainability**: 2.4 → 3.6

##### **消费者解释偏好**
- **78%** 的用户选择 LEXMA 解释。
- 所有维度显著提升（p < 0.001）：
  - **Clarity**: 3.835 → 4.212
  - **Actionability**: 3.444 → 4.052（+17.6%）
  - **Satisfaction**: 3.692 → 4.447（+20.5%）
  - **Politeness**: 4.124 → 4.586
  - **Trustworthiness**: 3.742 → 4.139

---

#### **自动风格评估（消费者解释）**

| 模型 | Readability Grade (越低越好) | Politeness Density (越高越好) |
|------|-------------------------------|------------------------------|
| Raw Qwen | 6.866 | 0.223 |
| GRPO-Step1 | 9.374 ↑（变差） | 0.169 ↓（变差） |
| **GRPO-Step2** | **5.952 ↓**（达标） | **0.223 ↑**（恢复） |

> ✅ GRPO-Step2 成功将可读性恢复至小学八年级水平以下（Plain Language），并提升礼貌性。

---

## 4. 关键结论和发现

### **主要发现**

1. **LEXMA 实现了高准确、高可解释、多受众适配的统一**：
   - 决策性能接近 XGBoost，远超原始 LLM。
   - 生成的解释被专家认为更**风险聚焦**，被消费者认为更**清晰、礼貌、可操作**。

2. **双适配器设计成功解耦“决策”与“语气”**：
   - 在不改变决策规则的前提下，实现了面向不同受众的个性化沟通。

3. **标签高效训练可行**：
   - 无需人工标注解释，仅用决策标签 + 规则奖励即可提升解释质量。

4. **强化学习可提升 LLM 在表格数据上的表现**：
   - 通过 GRPO 优化，Qwen3-4B 在 tabular 数据上达到接近树模型的性能。

---

### **局限性**

1. **数据限制**：HMDA 数据缺少详细信用历史和还款行为，影响模型上限。
2. **评估规模有限**：
   - 专家评估仅 3 人，消费者为在线面板，非真实用户。
   - 缺乏真实业务环境中的 A/B 测试。
3. **解释忠实性未完全验证**：虽通过架构设计增强忠实性，但未使用对抗测试或因果干预进一步验证。

---

### **未来工作方向**

1. **扩展到其他高风险决策场景**：如保险核保、医疗诊断、招聘筛选。
2. **引入小样本人工反馈**：在关键领域加入少量人类偏好数据，进一步提升解释质量。
3. **动态适配更多受众类型**：如监管机构、董事会成员等。
4. **结合 LLM-as-a-Judge 进行自动评估**：构建更复杂的 reward model。
5. **探索多模态解释**：结合文本、图表、可视化增强可解释性。

---

> **总结**：LEXMA 提供了一个**系统化、低成本、可扩展**的 LLM 微调框架，推动了 **Explainable AI** 从“事后归因”向“内生叙事”的范式转变，为高风险商业决策的透明化部署提供了实用路径。

</details>

---

### 11. [ReEfBench: Quantifying the Reasoning Efficiency of LLMs](https://arxiv.org/abs/2601.03550)

**Authors**: Zhizhang Fu, Yuancheng Gu, Chenkai Hu, Hanmeng Liu, Yue Zhang  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03550v1  

#### Abstract
Test-time scaling has enabled Large Language Models (LLMs) to tackle complex reasoning, yet the limitations of current Chain-of-Thought (CoT) evaluation obscures whether performance gains stem from genuine reasoning or mere verbosity. To address this, (1) we propose a novel neuro-symbolic framework ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReEfBench: Quantifying the Reasoning Efficiency of LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前对大型语言模型（LLMs）的推理能力评估主要依赖 **Chain-of-Thought (CoT)** 范式，但存在以下核心问题：
- **评估不充分**：现有方法难以区分“真正的逻辑推理”与“冗长的文本生成”（即“overthinking”现象）。
- **缺乏过程性评估**：多数评估仅关注最终答案正确性，忽略了推理路径的质量、效率和认知行为。
- **训练策略冲突**：混合长短 CoT 数据可能导致模型策略干扰，影响推理稳定性。

### 提出的新方法与思路
作者提出了 **ReEfBench** —— 一个基于 **neuro-symbolic（神经符号）框架** 的非侵入式、过程中心化的推理效率量化评估体系。

#### 核心创新点：
1. **构建可扩展、可控的 First-Order Logic (FOL) 推理数据集**
   - 基于 Modus Ponens 及其扩展规则（合取、析取），通过反向链式生成机制构造多跳逻辑推理问题。
   - 支持精确控制逻辑深度（Logical Depth）、引入干扰项（distractors），实现难度可调、逻辑可验证。

2. **提出六维诊断性评估指标**
   - 将推理过程分解为多个节点，并定义六个可解释的诊断维度：
     - **Logical Depth (Sld)**：达成的有效最大逻辑深度。
     - **Cost (Scost)**：计算消耗（token 数量 + planning/reflection 步骤频率）。
     - **Exploration (Sexp)**：探索到的独特且正确的逻辑节点数。
     - **Efficiency (Seff)**：每单位成本带来的逻辑进展（token 效率 + 有效跨度）。
     - **Coherence (Scoh)**：元认知步骤（planning/reflection）是否带来实际逻辑推进。
     - **Redundancy (Sred)**：句子级和节点级重复程度。

3. **提出四类行为原型分类法**
   - 基于 K-means 聚类在 `Logical Depth` vs `Cost` 平面上识别出四种典型行为模式：
     - **Effective Solver**（高效求解者）
     - **Deep Wanderer**（深思漫游者）
     - **Hollow Mimic**（空洞模仿者）
     - **Lazy Guesser**（懒惰猜测者）

4. **非侵入式解析架构**
   - 使用小型 LLM 作为 parser，将自然语言 CoT 输出自动转化为形式化逻辑节点，再由符号系统进行验证与分析，避免强制格式限制模型自由表达。

### 相比现有方法的优势
| 特性 | ReEfBench | 其他方法（如 ProntoQA, FOLIO, Roscoe） |
|------|-----------|----------------------------------------|
| Scalable | ✅ | ❌ 或部分支持 |
| FOL 形式化基础 | ✅ | ✅ (FOLIO, ProntoQA) / ❌ |
| Logic-only 评估 | ✅ | ❌（常混杂知识） |
| Logical Depth 可量化 | ✅ | ❌ 或弱支持 |
| 行为过程可分析（BehProc） | ✅ | ❌ |
| 非侵入式（NonIntrusive） | ✅ | ❌（需特定输出格式） |

> ✅ 表示支持，❌ 表示不支持或缺失

---

## 2. 核心实验方法和设置

### 数据集
- **自建 FOL 推理数据集**：覆盖复杂度等级 C=3 到 C=11，每个等级 100 个样本，共 900 个测试实例。
- 每个样本包含：
  - 前提（Premises）
  - 结论（Conclusion）
  - 黄金推理路径（Golden Solution）
  - 干扰前提（C 个无关前提）
- 逻辑规则基于 **Modus Ponens + Conjunction/Disjunction Introduction/Elimination**

### 实验设置
- **评估对象**：25 个主流开源与闭源 LLMs，包括：
  - 闭源：Claude-Opus/Sonnet-4.5 (.long/.short)
  - 开源：Qwen3 系列（3B–235B）、DeepSeek-R1、QwQ-32B 等
- **Prompt 设计**：
  ```text
  Please answer the question based on the given information...
  Please reason step by step, show your reasoning process and put your final answer in \boxed{}
  ```
- **API 参数**：
  - Temperature = 0
  - Max Tokens: 24k（thinking 模式），8k（普通模式）

### 评估流程（五阶段 Pipeline）
1. **Phase A**：数据生成（可控复杂度）
2. **Phase B**：目标 LLM 生成响应
3. **Phase C**：LLM Parser 分解响应 → 逻辑节点（normal/reflection, actual/planning）
4. **Phase D**：规则引擎计算节点属性（正确性、逻辑深度、有效性）
5. **Phase E**：聚合六大指标并聚类分类

### 基线对比
- 无直接传统 baseline，而是与已有框架横向比较（见 Table 1）。
- 主要对比不同推理范式的行为差异：
  - Long CoT vs Short CoT
  - Thinking Mode vs Instruct Mode
  - Distilled 模型 vs Teacher 模型
  - Mixed Training vs Pure Training

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Complexity Level 11）

| Model | Category | Sld | Scost | Seff | Depth (raw) | Token (k) |
|-------|----------|-----|--------|--------|-------------|------------|
| Qwen3-235B-thinking | DeepWanderer | 1.00 | 0.88 | 0.47 | 10.54 | 16.8 |
| Claude-Opus-4.5.long | EffectiveSolver | 0.97 | 0.37 | 0.60 | 10.27 | 3.5 |
| QwQ-32B | HollowMimic | 0.68 | 0.61 | 0.48 | 7.12 | 5.7 |
| Qwen2.5-32B-Instruct | LazyGuesser | 0.28 | 0.16 | 0.55 | 2.90 | 0.7 |

> 注：Sld 和 Scost 已归一化至 [0,1]

### 四类行为原型特征总结

| 类别 | 特征描述 | 代表模型 | 核心指标表现 |
|------|---------|--------|--------------|
| **Effective Solver** | 高效精准推理 | Claude-Opus-4.5.long | 高 Sld, 低 Scost, 高 Seff, 低 Sred |
| **Deep Wanderer** | 全面搜索但冗余 | Qwen3-235B-thinking | 极高 Scost, 高 Sld, 低 Seff, 高 Sexp, 高 Sred |
| **Hollow Mimic** | 努力但无效扩展 | QwQ-32B, DS-R1-Distill-Qwen-7B | 中等 Scost, 低 Sld, 低 Sexp, 高 Sred |
| **Lazy Guesser** | 浅层尝试即放弃 | Qwen2.5-32B-Instruct | 最低 Scost, 最低 Sld, “虚假”高 Seff（因短） |

### 与基线方法的对比结果
- 在相同任务下，**Long CoT 并不总是优于 Short CoT**：
  - 当前先进 Short CoT（如 Qwen3-235B-Instruct）能达到接近 Long CoT 的逻辑深度（仅差 1.6%）。
  - 优势在于显著更低的成本（token 减少约 75%）。
- **Distillation 失败**：
  - 将 Long CoT 行为蒸馏到小模型（如 4B/8B）后，虽能模仿 reflection/planning 形式，但无法提升逻辑深度。
  - 出现“**Diluted Expansion**”：增加 token 但未提高 W（Work = Logical Depth）。
- **Mixed Training 危害大**：
  - 同时训练 Long 和 Short CoT 数据会导致“**Premature Saturation or Collapse**”。
  - 如 Qwen3-235B.long（mixed）相比 Qwen3-235B-thinking（pure）更早停止扩展 token，导致性能下降。

### 消融实验结果
#### （1）Reflection 的作用（Table 4）
| Model | Logical Depth | Reflection % |
|-------|----------------|---------------|
| Claude-Opus-4.5.long | 10.3 | 0.7% |
| Qwen3-235B-Instruct | 10.0 | 1.8% |
| Qwen2.5-32B-Instruct | 2.9 | 0.0% |

→ 成功模型即使在 short mode 下也保持一定 reflection 行为；失败模型几乎无 reflection。

#### （2）模型规模对蒸馏的影响（Figure 5 & 10）
- **Qwen3-4B/8B**：生成更多 reflection/planning 步骤，但质量随规模减小而下降 → 无法转化为更高逻辑深度。
- **Qwen3-14B**：唯一能在行为和能力上与 32B teacher 对齐的小模型 → **14B 是临界容量阈值**。

#### （3）Long vs Short CoT 性能增益（Table 3）
| Model | Depth Δ (Long vs Short) |
|-------|--------------------------|
| QwQ-32B | +97.1% |
| Qwen3-235B-thinking vs Instruct | +1.6% |
| Claude-Opus-4.5.long vs short | +8.8% |

→ 显示现代模型中 Long CoT 的边际收益急剧下降。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **深度推理 ≠ 长文本生成**
   - Extended token generation 不是 deep reasoning 的必要条件。
   - 高效模型（如 Claude-Opus-4.5.long）可以用极少 token 达成高逻辑深度。

2. ✅ **存在两种成功路径**
   - **High-P Strategy (Effective Solver)**：高效率，精准推导。
   - **High-t Strategy (Deep Wanderer)**：高成本，穷举探索。
   - 二者都能成功，但前者更具部署价值。

3. ✅ **两类失败模式被明确识别**
   - **Hollow Mimic**：形式上有 planning/reflection，但无实质逻辑进展 → “表演性推理”
   - **Lazy Guesser**：面对高复杂度迅速饱和或崩溃 → 缺乏维持推理状态的能力

4. ⚠️ **混合训练有害**
   - Mixing Long and Short CoT data 会破坏 High-t 策略，导致 premature saturation。
   - 工业界已转向专用 tuning（如 Qwen2507 放弃 mixed training）。

5. ⚠️ **蒸馏无法复制逻辑效能**
   - 小模型可以模仿老师的“行为长度”，但由于内在容量不足，无法复现其“逻辑效力”。
   - 存在明显的 **capacity bottleneck**。

### 方法的局限性
1. **领域受限**：仅适用于 FOL 可表达的封闭世界推理，不适用于开放域问答或创造性推理。
2. **非侵入式限制**：只能分析显式写出的内容，无法捕捉隐含推理步骤。
3. **边界模糊**：四类原型是统计趋势，个体模型可能跨类别，分类具有连续性。
4. **依赖 parser 质量**：尽管 parser 在人工标注集上达到 94.3% F1，仍可能存在误解析风险。

### 未来工作方向
- 扩展至其他逻辑系统（如高阶逻辑、模态逻辑）
- 引入动态资源分配机制研究 test-time scaling 的优化策略
- 探索如何训练模型实现 adaptive switching between High-P and High-t strategies
- 构建面向真实场景的效率-准确性权衡 benchmark

---

> 🔗 **项目地址**：https://anonymous.4open.science/r/LoG-1AD8/  
> 📄 **论文链接**：https://arxiv.org/abs/2601.03550

</details>

---

### 12. [Attribute-Aware Controlled Product Generation with LLMs for E-commerce](https://arxiv.org/abs/2601.04200)

**Authors**: Virginia Negri, V\'ictor Mart\'inez G\'omez, Sergio A. Balanya, Subburam Rajaram  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.04200v1  

#### Abstract
Product information extraction is crucial for e-commerce services, but obtaining high-quality labeled datasets remains challenging. We present a systematic approach for generating synthetic e-commerce product data using Large Language Models (LLMs), introducing a controlled modification framework wi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Attribute-Aware Controlled Product Generation with LLMs for E-commerce

---

## 1. 论文的主要贡献和创新点

### 解决的问题
- **电商领域高质量标注数据稀缺**：真实世界中的产品信息常存在噪声、不一致或缺失，手动标注成本高昂且难以覆盖多样化的表达方式。
- **现有合成数据生成方法局限性大**：传统方法（如规则变换、back-translation）生成多样性有限，难以保持语义连贯性和结构一致性。

### 提出的新方法与思路
作者提出了一种基于 **Large Language Models (LLMs)** 的系统性框架，用于生成可控的合成电商产品数据。其核心是 **三类受控修改策略**：
1. **Positive Example Generation (correct)**：正确反映属性值，确保文本字段中所有提及均与结构化属性一致。
2. **Negative Example Generation (incorrect)**：引入受控的不一致性（例如在描述中暗示错误属性），模拟现实中的低质量数据。
3. **Incomplete Example Generation (unknown)**：移除目标属性的所有提及，模拟信息缺失场景。

该框架通过 **attribute-aware prompts** 和多阶段验证机制（包括 Value Provider LLM 和 Similarity LLM），实现对生成内容的精细控制。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **完整性** | 生成完整的产品信息（title, description, bullet points），而非单一字段 |
| **可控性** | 支持精确控制属性修改类型（正/负/未知），适配下游任务需求 |
| **真实性与一致性** | 利用LLM强大的语义理解能力维持跨字段一致性，并保留原始文本结构 |
| **去品牌化（brand anonymization）** | 自动替换真实品牌为虚构名称（如“BRAND” → “NeoTech”），防止偏见传播 |
| **可扩展性与多语言支持** | 可快速应用于新类别或市场，适应不同电商平台要求 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **MAVE dataset (Yang et al., 2022)**：大规模电商属性提取公开数据集，包含超过 220 万产品，涵盖 1,257 个类别和 662 种属性。
- 实验采样了 **2,000 个产品**，来自 top 200 类别，每个产品随机选择一个属性进行修改。

### 实验设置
- **生成策略分布**：
  - 正确修改（correct）：50%
  - 错误示例（incorrect）：25%
  - 属性缺失（unknown）：25%
- **模型实现**：
  - 主要使用 **Claude Haiku** 作为 Generation LLM 和 Value Provider LLM
  - 使用 **sentence-transformers/all-MiniLM-L6-v2** 作为 Similarity LLM 进行语义区分
- **prompt design 四要素**：
  1. Role Definition（角色定义）
  2. Task Instructions（任务指令）
  3. Product Context（产品上下文）
  4. Output Format（输出格式，JSON）

### 评估指标
#### （1）人工评估（Human Evaluation）
由三位具备电商背景的专家独立打分，共评估 2,000 条样本，关注以下维度：
- Attribute Value Quality（属性值合理性）
- Negative Example Coherence（负面例子合理性）
- Cross-field Consistency（跨字段一致性）
- Brand Modification Success（品牌替换成功率）
- Content Preservation（非目标内容是否被意外更改）
- Professional Writing（整体语言自然度）

最终采用多数投票决定结果。

#### （2）下游任务评估（Downstream Task）
- **任务**：Attribute Value Extraction（属性值抽取）
- **模型**：FLAN-T5-base
- **输入处理**：将 title, description, features 拼接成 structured prompt
- **训练配置**：
  - 使用 ~800 条 correct 示例训练（80%）和验证（20%）
  - 测试集为剩余 ~1,000 条产品
  - 对比多种数据组合配置

#### 基线方法对比
| 配置 | 描述 |
|------|------|
| Zero-shot | 不使用任何训练数据，直接推理 |
| Original-only | 仅使用原始真实数据训练 |
| Synthetic-only | 仅使用合成数据训练 |
| Hybrid Configurations | 混合使用原始与合成数据（75/25, 50/50, 25/75） |

---

## 3. 主要实验结果和性能指标

### 人工评估结果（N = 2,000）
| 指标 | 得分（%） |
|-------------------------------|--------|
| 合成产品语言自然度（Readability） | **99.6%** |
| 属性值有效率（Valid Attribute Values） | **96.5%** |
| 品牌匿名化成功 | **95.8%** |
| 跨字段一致性（Correct 修改） | **94.2%** |
| 跨字段一致性（Incorrect 修改） | **93.0%** |
| 跨字段一致性（Unknown 修改） | **88.3%** |
| 无额外变更（Content Preservation） | **88.8%** |

> ✅ 表明合成产品质量极高，接近人类撰写水平。

---

### 下游任务性能（Attribute Extraction Accuracy）

| 配置 | Test Accuracy (%) |
|---------------------------------------------|------------------|
| Zero-shot Baseline | **13.40** |
| Original Data Only (100%) | **60.79** |
| Synthetic Data Only (100%) | **60.48** |
| Original 75% + Synthetic 25% | **68.82** ⬆️ |
| Original 50% + Synthetic 50% | **66.11** |
| Original 25% + Synthetic 75% | **64.44** |

> 🔍 关键发现：
> - **纯合成数据表现 ≈ 真实数据**（60.48% vs 60.79%），说明合成数据具有高度可用性
> - **混合训练显著提升性能**，最优达到 **68.82%**，优于单独使用真实或合成数据
> - 随着合成比例增加，性能略有下降，提示需平衡数据来源

---

### 消融分析与误差分析（Error Analysis）
- 手动审查 441 条“预测错误”的案例，发现许多实际为 **语义正确但格式不同**，归类为七种合理变体：
  1. Granularity differences（粒度差异，如 "running" vs "running shoe"）
  2. Morphological variations（单复数变化）
  3. Multiple valid values（多个合法标签）
  4. Missing units（缺少单位）
  5. Equivalent definitions（等价定义）
  6. Contextual synonyms（上下文同义词）
  7. Format variations（命名格式差异）

> 💡 这表明当前标注标准本身存在模糊性，而模型输出更具灵活性，反衬出真实数据标注的一致性挑战。

---

## 4. 关键结论和发现

### 主要结论
1. **LLM 可高效生成高质量、可控的合成电商产品数据**，在语言自然度（99.6%）、属性有效性（96.5%）方面表现优异。
2. **合成数据在下游任务中媲美真实数据性能**（60.5% vs 60.8%），显著超越零样本基线（13.4%）。
3. **混合使用合成与真实数据可进一步提升性能至 68.8%**，证明二者互补性强。
4. 该框架支持 **multi-lingual、multi-category、brand-anonymized** 数据生成，适用于低资源场景下的快速冷启动。

---

### 方法的局限性
| 局限 | 具体说明 |
|------|----------|
| 依赖输入数据质量 | 若源产品描述空洞或属性定义不清（如 `type="shoes"`），会影响生成质量 |
| 单属性修改限制 | 当前仅支持一次修改一个属性，未考虑多属性交互影响 |
| 结构偏离风险 | 4.2% 的样本出现重大非预期改动，尤其当原始描述为空时 |
| 偏见传递可能 | 尽管品牌已匿名，但源数据中的属性分布偏差仍可能延续 |
| 评估集中于 positive 示例 | negative 和 unknown 示例尚未用于下游任务全面测试 |

---

### 未来工作方向
1. **增强多属性联合修改能力**：研究多个相关属性之间的协同变化（如从“vanilla”改为“chocolate”时自动更新产地为“Switzerland”）。
2. **精细化控制属性粒度与分布**：通过 attribute-specific vocabularies 或 controlled decoding 技术统一值的表达层级。
3. **改进混合训练策略**：探索更优的数据融合方式，理解 synthetic 与 real data 在不同类型属性上的互补机制。
4. **加强修改约束机制**：减少非目标字段的意外变更，提升生成稳定性。
5. **拓展到更多下游任务**：如 product matching、recommendation、QA 等，验证合成数据泛化能力。
6. **引入更强的 debiasing 机制**：主动检测并修正潜在的社会或商业偏见。

---

> 📌 **总体评价**：本文提供了一个实用、可扩展、高质量的合成电商数据生成框架，在解决数据稀缺问题上迈出重要一步，尤其适合需要快速构建训练集的新品类或新市场部署。

</details>

---

### 13. [AM$^3$Safety: Towards Data Efficient Alignment of Multi-modal Multi-turn Safety for MLLMs](https://arxiv.org/abs/2601.04736)

**Authors**: Han Zhu, Jiale Chen, Chengkun Cai, Shengjie Sun, Haoran Li, Yujin Zhou, Chi-Min Chan, Pengcheng Wen, Lei Li, Sirui Han, Yike Guo  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.04736v1  

#### Abstract
Multi-modal Large Language Models (MLLMs) are increasingly deployed in interactive applications. However, their safety vulnerabilities become pronounced in multi-turn multi-modal scenarios, where harmful intent can be gradually reconstructed across turns, and security protocols fade into oblivion as...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AM³Safety: Towards Data Efficient Alignment of Multi-modal Multi-turn Safety for MLLMs

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的多模态大语言模型（MLLMs）安全对齐方法主要针对单轮视觉问答（VQA）任务设计，且通常依赖昂贵的人工偏好标注。这些方法在多轮多模态对话场景中效果有限，因为：
- 对话中的有害意图可以通过多轮交互逐步重构（multi-turn intent reconstruction）
- 随着对话进行，安全协议容易被遗忘
- 缺乏高质量的多模态对话安全数据集
- 手动标注成本高昂

### 提出的新方法和思路
本文提出了 **AM³Safety** 框架，旨在解决上述挑战：

#### （1）InterSafe-V 数据集
- 构建了一个包含 **11,270 个对话** 和 **500 个专门设计的拒绝型 VQA 样本** 的开源多模态对话安全训练数据集。
- 数据通过 **模型间交互（model-to-model interaction）** 自动生成，无需人工标注，显著降低数据构建成本。
- 采用三步数据构造流程：
  1. **有害意图分解（Harmful Intent Decomposition）**：将原始有害查询拆分为多个独立但组合后仍具风险的子查询。
  2. **对话模拟器（Conversation Simulator）**：利用两个 MLLM 进行对抗性对话生成（一个作为“安全专家”诱导攻击，另一个作为目标模型响应）。
  3. **数据清洗（Data Cleaning）**：过滤高度相似图像和偏离主题的对话，确保数据质量和潜在风险。

#### （2）AM³Safety 框架
结合冷启动拒绝学习与基于 **Group Relative Policy Optimization (GRPO)** 的微调，实现高效的安全对齐：
- **冷启动阶段（Cold-start Refusal Learning）**：先让模型学会以合理解释的方式拒绝有害请求，建立基础安全能力。
- **GRPO 微调阶段**：引入 **turn-aware dual-objective reward function**，动态权衡每一轮的安全性和有用性，确保整个对话过程的一致性行为。

### 相比现有方法的优势
| 维度 | 现有方法（如 RLHF-V, Safe RLHF-V, MM-DPO） | AM³Safety |
|------|------------------------------------------|---------|
| 数据形式 | 单轮 VQA | 多轮多模态对话 |
| 数据来源 | 人工标注为主 | 模型自动生成，无须人工标注 |
| 安全机制 | 静态防御 | 动态、上下文感知的 turn-aware 权重机制 |
| 成本效率 | 高标注成本 | 数据高效，低成本 |
| 对抗复杂攻击能力 | 弱（易被角色扮演等绕过） | 强（支持渐进式意图重构攻击模拟） |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 规模 | 特点 |
|--------|------|------|------|
| **InterSafe-V** | 自建训练集 | 11,270 对话 + 500 拒绝 VQA | 模型生成，多轮多图，覆盖 20 类安全风险 |
| **SafeMT** | 基准测试集 | - | 专为多轮多模态对话安全设计 |
| **JailbreakV-mini** | 基准测试集 | - | 评估对抗性越狱攻击鲁棒性 |
| **MM-SafetyBench** | 基准测试集 | - | 多模态安全综合评测 |
| **MMSafe-PO** | 基准测试集 | - | 包含少量对话样本的安全评测 |
| **MMDU / ConvBench** | 通用能力基准 | - | 评估多模态对话理解与连贯性 |
| **ScienceQA / MathVista / Geometry3k / MathVerse** | 推理能力基准 | - | 测试模型逻辑推理能力是否受损 |

### 实验设置
- **基础模型**：
  - Qwen2.5-VL-7B-Instruct
  - LLaVA-NeXT-7B
- **训练配置**：
  - 使用 8×H800 GPU
  - GRPO 微调使用 7,000 个对话（少于完整数据集），训练不足 15 轮
  - Rollout 数量：8
  - Batch Size：128
- **奖励模型**：使用 InternVL3-78B 作为自动评判模型（LLM-as-a-judge）

### 评估指标
| 指标 | 定义 | 说明 |
|------|------|------|
| **Win Rate** | 来自 Safe RLHF-V 的相对胜率指标 | 综合衡量 helpfulness 与 harmlessness，越高越好 |
| **Attack Success Rate (ASR)** | 攻击成功比例 $ \text{ASR} = \frac{1}{N}\sum I(\text{response unsafe}) $ | 越低表示安全性越强 |
| **Helpfulness Score** | 回答是否有用、信息完整、结构清晰 | 0–3 分制 |
| **Harmlessness Score** | 是否有效识别并拒绝有害请求，并提供理由 | -3 到 3 分制，简单拒绝得分为 0 |

### 基线方法对比
- **+RLHF-V**：基于人类反馈的强化学习
- **+Safe RLHF-V**：平衡 helpfulness 与 harmlessness 的多模态对齐方法
- **+MM-DPO**：基于 Direct Preference Optimization 的多维优化
- **+SPA-VL**：大规模安全偏好对齐数据集驱动的方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

#### 在 **Qwen2.5-VL-7B** 上的表现（SafeMT 基准）：
| 方法 | Helpfulness ↑ | Harmlessness ↑ | ASR ↓ |
|------|----------------|------------------|--------|
| Base Model | 0.5000 | 0.5000 | 0.4892 |
| +Ours (AM³Safety) | **0.6319** (+13.19%) | **0.5819** (+8.19%) | **0.2806** (-20.86%) |

> ✅ **提升显著**：ASR 下降超过 10%，harmless 维度提升至少 8%，helpful 维度提升超 13%

#### 在 **LLaVA-NeXT-7B** 上的表现（SafeMT 基准）：
| 方法 | Helpfulness ↑ | Harmlessness ↑ | ASR ↓ |
|------|----------------|------------------|--------|
| Base Model | 0.5000 | 0.5000 | 0.4895 |
| +Ours (AM³Safety) | **0.8210** (+32.10%) | **0.6918** (+19.18%) | **0.3844** (-10.51%) |

> ✅ **全面提升**：utility 提升 32%，safety 提升 19%，ASR 显著下降

### 与其他基准的对比（Table 2 & 3）
| 方法 | JailbreakV (ASR↓) | MM-SafetyBench (ASR↓) | MMSafe-PO (ASR↓) |
|------|--------------------|------------------------|------------------|
| +Ours (Qwen) | **0.1187** | **0.1726** | **0.1200** |
| +Ours (LLaVA) | **0.3516** | **0.4333** | **0.1055** |

> ✅ 在多个安全基准上均取得最低 ASR 或接近最优表现

### 通用能力保留情况（Table 3）
| 模型 | MMDU | ConvBench | ScienceQA | MathVista | MathVerse |
|------|------|-----------|-----------|-----------|-----------|
| Qwen2.5-VL-7B | 4.85 → **4.92** | 52.36% → **59.97%** | 81.80% → 81.11% | 50.11% → **52.56%** | 27.79% → **31.11%** |
| LLaVA-NeXT-7B | 4.15 → **4.64** | 19.41% → **22.24%** | 61.33% → **62.27%** | 28.36% → **29.00%** | 14.01% → **14.87%** |

> ✅ 尽管训练数据量小（仅 7,500 样本），**未损害原有通用能力和推理性能**，部分指标反而提升

### 消融实验结果（Figure 3 & Table 4）

#### 不同训练阶段的影响（Figure 3）
- **仅 GRPO 微调**：提升有限，模型仍倾向于回答有害问题
- **加入冷启动拒绝学习**：显著提升 harmlessness 与 overall 安全表现
- **结论**：必须先教会模型“如何拒绝”，再优化其“如何帮助”

#### 双目标奖励系数消融（Table 4）
在 Qwen2.5-VL-7B 上测试不同 $\beta$（helpfulness 系数）：
| $\beta$ | Helpfulness | Harmlessness | ASR |
|--------|-------------|---------------|-----|
| 0 | 0.2080 | 0.5405 | 0.2995 |
| **0.1** | **0.6319** | **0.5819** | **0.2806** |
| 1 | 0.2667 | 0.5414 | 0.2986 |
| 10 | 0.2621 | 0.5290 | 0.3000 |

> ✅ 最优值出现在 $\beta = 0.1$，即 **轻微偏向 helpfulness**，过高会破坏安全约束

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **多轮对话安全需专用数据与方法**：传统单轮 VQA 安全方法无法应对渐进式意图重构攻击。
2. ✅ **模型自动生成数据可行且高效**：InterSafe-V 证明可通过模型交互构建高质量对话安全数据，避免高成本人工标注。
3. ✅ **冷启动 + GRPO 是有效范式**：先训练拒绝能力，再进行策略优化，能更好维持安全边界。
4. ✅ **turn-aware reward 设计至关重要**：通过安全方差加权机制，可精准识别“模糊边界”轮次并加强监督。
5. ✅ **打破 safety-helpfulness trade-off**：AM³Safety 同时提升了安全性与有用性，而非牺牲一方换取另一方。

### 局限性
1. **缺乏专用对抗性越狱基准测试**：当前评估未涵盖最前沿的 jailbreak 攻击类型，实际鲁棒性有待验证。
2. **模型范围受限**：仅在通用 MLLM 上验证，未测试专用推理模型上的适用性。
3. **依赖外部强模型作为裁判**：使用 InternVL3-78B 作为 reward model，存在教师模型偏差传递风险。
4. **尚未实现自我反思机制**：模型未能内化安全原则，仍依赖外部打分。

### 未来工作方向
- 开发更具挑战性的 **adversarial jailbreak benchmark**
- 探索 **self-correction / self-reflection** 机制，减少对外部 judge 的依赖
- 扩展至更多类型的 MLLM（如医学、法律专用模型）
- 研究更高效的 **low-rank adaptation (LoRA)** 或 **quantization-aware training** 方案以进一步降低成本

---

> 📌 **总结一句话**：  
> AM³Safety 提出了一种**数据高效、无需人工标注**的多模态多轮对话安全对齐框架，通过 **InterSafe-V 数据集 + 冷启动拒绝学习 + turn-aware GRPO 优化**，在大幅提升安全性的同时保持甚至增强模型有用性，为现实世界中 MLLM 的安全部署提供了实用解决方案。

</details>

---

### 14. [Revisiting Judge Decoding from First Principles via Training-Free Distributional Divergence](https://arxiv.org/abs/2601.04766)

**Authors**: Shengyin Sun, Yiming Li, Renxi Liu, Weizhe Lin, Hui-Ling Zhen, Xianzhi Yu, Mingxuan Yuan, Chen Ma  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.04766v1  

#### Abstract
Judge Decoding accelerates LLM inference by relaxing the strict verification of Speculative Decoding, yet it typically relies on expensive and noisy supervision. In this work, we revisit this paradigm from first principles, revealing that the ``criticality'' scores learned via costly supervision are...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Revisiting Judge Decoding from First Principles via Training-Free Distributional Divergence

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统的 **Speculative Decoding (SD)** 虽然能加速大语言模型（LLM）推理，但其验证机制过于严格，仅接受与目标模型完全一致的 token，导致许多语义等价但形式不同的输出被错误拒绝（如“6+1=7” vs “6 plus 1 equals 7”），限制了吞吐量提升。

为此提出的 **Judge Decoding** 引入一个可学习的“法官”模型来判断 draft token 是否“关键错误”，从而实现更宽松、语义层面的验证。然而，这类方法依赖昂贵且噪声大的监督信号（如人工标注或反事实 rollouts），存在以下瓶颈：
- **高成本**：手动标注费时费力，自动挖掘（如 AutoJudge）需大量 GPU 小时；
- **标签噪声**：生成的随机性导致同一 token 在不同 rollout 中可能被标记为“关键”或“非关键”；
- **泛化差**：训练数据局限于特定领域，跨域表现下降明显。

---

### 🚀 提出的新方法与核心思想
本文从第一性原理出发，提出一个颠覆性观点：

> **“关键性”（criticality）信号本质上已内嵌于 draft 与 target 模型之间的分布差异中，无需外部监督即可捕捉。**

#### 创新点：
1. **理论揭示**：首次证明了训练得到的线性 judge 分类器与 **KL 散度**在数学结构上共享相同的 logit primitives（即 `Δ(x) = (z_t(i)−z_t(j)) − (z_d(i)−z_d(j))`）。  
   - Judge 是对这些 primitives 的**线性聚合**；
   - KL 散度是对这些 primitives 的**加权二次聚合**。
   > 因此，两者本质是在检测相同类型的分布偏移。

2. **提出 Training-Free 验证机制**：基于上述洞察，作者提出一种无需训练的验证方法 —— **KL Thresholding**：
   - 不再训练分类器，而是直接计算每个 token 上 draft 和 target 分布间的 KL 散度；
   - 若 KL 散度低于阈值，则接受该 token；
   - 可结合 confidence masking（例如当 target 模型 top-1 概率 > 0.9 时回退到标准 SD）以增强鲁棒性。

3. **统一视角**：将 judge decoding 视为学习分布差异的过程（见 Figure 2），而非依赖外部标签的任务。

---

### 🔍 相比现有方法的优势
| 维度 | 现有方法（如 AutoJudge） | 本文方法（KL Thresholding） |
|------|--------------------------|----------------------------|
| **训练需求** | 需要大规模标注/rollout 数据 | 完全无需训练（training-free） |
| **计算开销** | 极高（70B 模型需 ~2700 GPU 小时） | 几乎为零额外训练成本 |
| **标签质量** | 存在随机性和不一致性（Figure 5 显示仅 25.4% token 一致） | 基于确定性分布统计，无噪声 |
| **泛化能力** | 域外性能显著下降 | 对 domain shift 更鲁棒 |
| **部署复杂度** | 需维护额外 judge 模型 | 仅需轻量级分布比较 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **GSM8K**：小学数学应用题，多步推理；
- **MATH-500-Hard**：MATH 数据集中最难的 Level-5 问题，用于测试跨域泛化；
- **LiveCodeBench**：代码生成任务，强调精确执行；
- **MMLU-Pro**：涵盖广泛学科的知识问答，更具挑战性。

### ⚙️ 实验设置
- **模型对**：
  - Llama-3.2-1B-Instruct / Llama-3.1-8B-Instruct
  - Llama-3.1-8B-Instruct / Llama-3.1-70B-Instruct
  - Qwen3-0.6B / Qwen3-8B（专为推理优化）
- **实现框架**：集成至 **vLLM** 进行端到端延迟测量；
- **硬件平台**：NVIDIA A6000、L40、V100 GPU。

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **MAT (Mean Accepted Tokens)** | 平均每次验证成功接受的 draft token 数量，反映效率 |
| **Accuracy** | 最终答案正确率，衡量质量 |
| **Speedup** | 相对于 Vanilla SP 的端到端速度提升倍数（tokens/s） |
| **Consistency Analysis** | 同一 token 在多次 rollout 中是否稳定被判为“critical” |

### 🆚 基线方法对比
1. **Vanilla SP**：标准 Speculative Sampling，严格概率匹配；
2. **Top-K**：基于 token 排序的一致性进行松弛验证；
3. **AutoJudge**：当前最先进的训练型 judge 方法，通过反事实 rollout 自动挖掘 critical tokens；
4. **Target-Entropy / Draft-Entropy**：基于单一模型熵的不确定性启发式方法；
5. （附录）**LLM-as-Annotator**：使用 Qwen3-Max 自动生成标注训练 judge。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Figures 7–11 和 Table 1）

#### ✅ 在 GSM8K 上的表现（Llama-1B/8B）
| 方法 | MAT ↑ | Accuracy ↓ | Speedup |
|------|-------|------------|---------|
| Vanilla SP | 10.61 | 82.50% | 1.00× |
| **KL Thresholding** | **17.35 (+63.5%)** | **80.96% (~1.5%↓)** | **1.26×** |
| AutoJudge | 17.00 | 79.39% | 1.23× |

> KL 方法略优于 AutoJudge，且精度更高。

#### ✅ 在 GSM8K 上的表现（Llama-8B/70B）
| 方法 | MAT ↑ | Accuracy ↓ | Speedup |
|------|-------|------------|---------|
| Vanilla SP | 13.35 | 93.00% | 1.00× |
| **KL Thresholding** | **30.55 (+128.8%)** | **91.88% (~1.1%↓)** | **1.30×** |
| AutoJudge | 30.00 | 92.09% | 1.28× |

> KL 方法达到最高 MAT，精度损失极小。

#### ✅ 在 LiveCodeBench 上的表现
| 方法 | MAT ↑ | 可执行性 |
|------|-------|--------|
| KL Thresholding (1B/8B) | +104% (8.83 → 18.01) | 保持高代码正确率 |
| Top-K / Entropy 方法 | 提升有限 | 经常产生不可执行代码 |

> 表明 KL 方法在需要高精度的任务中仍稳健。

#### ✅ 跨域泛化能力（MATH-500-Hard）
- AutoJudge（在 GSM8K 上训练）在 MATH 上表现急剧恶化，接近 Top-K；
- **KL Thresholding 依然保持高效**：
  - MAT 从 13.93 提升至 **32.02**（+130%），精度仅降 <1%；
  - 显著优于 AutoJudge。

#### ✅ 端到端速度（Table 1）
- KL 方法在 vLLM 中实现了 **1.3×~1.6× 的实际加速**；
- 与 AutoJudge 相当，但无需任何训练成本。

#### ✅ 消融实验与分析
- **KL 与 AutoJudge score 高度正相关**（Figure 6）：KL 散度随 AutoJudge 打分升高而上升，说明二者捕获相似信号；
- **Entropy 无显著相关性**：表明单纯不确定性不足以识别 critical token；
- **Qwen3-Max 作为 annotator 效果不如 AutoJudge**（Figure 12）：尽管成本低，但标签不够精准，尤其在高精度场景下表现更差。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **“Criticality” 是模型内在属性**：  
   draft 与 target 模型之间的 **distributional divergence**（特别是 KL 散度）天然编码了哪些 token 会导致逻辑偏差，无需外部监督即可识别。

2. **训练型 judge 本质上是 KL 散度的代理模型**：  
   理论证明 AutoJudge 等线性分类器与 KL 散度共享相同的 logit primitives，只是前者是线性组合，后者是二次聚合。

3. **Training-Free KL Thresholding 性能媲美甚至超越训练方法**：  
   在多个 benchmark 上，KL 方法在 MAT 和 accuracy 权衡上**匹配或超过 AutoJudge**，且无需任何训练。

4. **更强的鲁棒性和泛化能力**：  
   在跨域任务（如 GSM8K → MATH）中，KL 方法显著优于依赖特定领域训练的 AutoJudge。

5. **简化部署路径**：  
   消除了对额外 judge 模型的需求，更适合生产环境快速部署。

---

### ⚠️ 局限性
1. **未在超大规模模型上验证**：  
   如 Llama-3.1-405B-Instruct 等，尚不清楚 scalability 是否保持；
   
2. **推理阶段引入额外计算开销**：  
   KL 散度计算增加了 per-token 开销，当前实现未做 kernel 优化（如 Triton fused kernel），可能成为瓶颈；

3. **假设 draft-target 差异足够表达语义错误**：  
   对某些细微语义错误（如逻辑跳跃但分布变化小）可能不够敏感。

---

### 🔮 未来工作方向
- 设计更高效的 **distributional divergence 计算方式**（如近似 KL、JS 散度）；
- 探索其他 divergence 度量（如 Wasserstein 距离）是否更具判别力；
- 将该思想扩展至 **Medusa/EAGLE 类 speculative 结构**；
- 结合 retrieval 或 cache 机制进一步减少冗余计算。

---

## 总结
> 本论文从根本上重新审视了 Judge Decoding 的本质，指出“关键性”信号并非来自外部监督，而是内生于模型间的分布差异。通过提出 **无需训练的 KL 散度阈值法**，不仅消除了昂贵的数据标注和训练过程，还在效率、准确性和泛化性方面达到了与最先进训练方法相当甚至更优的水平，为高效、鲁棒的 LLM 推理提供了一条简洁而强大的新路径。

</details>

---

### 15. [EARL: Energy-Aware Optimization of Liquid State Machines for Pervasive AI](https://arxiv.org/abs/2601.05205)

**Authors**: Zain Iqbal, Lorenzo Valerio  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.05205v1  

#### Abstract
Pervasive AI increasingly depends on on-device learning systems that deliver low-latency and energy-efficient computation under strict resource constraints. Liquid State Machines (LSMs) offer a promising approach for low-power temporal processing in pervasive and neuromorphic systems, but their depl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**EARL: Energy-Aware Optimization of Liquid State Machines for Pervasive AI**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **传统超参数优化（HPO）方法在 Liquid State Machine（LSM）上的局限性**：
  - LSM 对超参数（如 spectral radius、leak rate、reservoir size 等）高度敏感，微小变化可能导致系统从稳定态进入混沌态，严重影响准确率和能耗。
  - 现有 HPO 方法（如 Grid Search、Random Search、Bayesian Optimization）通常假设目标函数是平滑连续的，并且**未将能量消耗作为显式优化目标**。
  - 在 spiking neural network 和 neuromorphic computing 场景下，这些假设不成立，导致搜索效率低、资源浪费严重。

### 🚀 提出的新方法：**EARL 框架**
提出了一种名为 **Energy-Aware Reinforcement Learning (EARL)** 的混合优化框架，用于 LSM 超参数调优。其核心创新包括：

1. **统一的能量感知奖励函数**  
   将分类准确率 $ f_1(x) $ 和能量消耗 $ f_2(x) $ 统一为一个标量化奖励函数：
   $$
   r(x) = f_1(x) - \alpha \cdot f_2(x)
   $$
   允许在精度与能效之间进行权衡。

2. **融合 Bayesian Optimization 与 Reinforcement Learning**
   - **Bayesian Optimization (BO)**：使用 Gaussian Process 建模奖励函数，通过 Expected Improvement (EI) 获取候选配置，实现全局探索。
   - **Reinforcement Learning (RL)**：引入 RL Agent，在每一批 BO 生成的候选中选择最优试验，基于预测均值和方差动态决策，提升局部搜索效率。

3. **自适应早停机制（Adaptive Early Termination）**
   - 监控最近 $ T_s $ 次迭代中的相对改进（accuracy 和 energy），若连续无显著提升（低于阈值 $ \epsilon_r, \epsilon_e $），则提前终止搜索。
   - 显著减少冗余评估，降低计算开销和能耗。

### 🔍 相比现有方法的优势
| 特性 | EARL | Optuna / Ray Tune |
|------|------|------------------|
| 是否考虑能量 | ✅ 是（显式建模） | ❌ 否或间接估计 |
| 搜索策略 | BO + RL 动态选择 | 单纯 BO 或并行采样 |
| 早停机制 | ✅ 自适应监测双目标 | 通常固定 trial 数或简单收敛判断 |
| 效率 | 高（减少无效评估） | 较低（盲目探索多） |

> EARL 实现了**准确性、能效、优化时间三者的协同优化**，特别适用于边缘设备上的 Pervasive AI 应用。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
在三个标准基准数据集上进行了评估，覆盖多种时序任务类型：

| 数据集 | 任务类型 | 样本数 | 类别数 |
|--------|--------|-------|-------|
| **FSDD** (Free Spoken Digit Dataset) | 语音识别（音频） | ~3,000 | 10（数字0-9） |
| **UCI HAR** (Human Activity Recognition) | 人类活动识别（IMU传感器） | ~15,000 | 6（步行、坐下等） |
| **Occupancy Detection** | 环境占用检测（温湿度CO₂等） | ~10,000 | 2（有人/无人） |

所有数据均归一化处理，采用 80%/20% 分割训练/验证集。

### ⚙️ 实验设置
- **模型架构**：LSM（Leaky Integrate-and-Fire 神经元） + GRU 读出层
- **优化目标**：联合优化分类 accuracy 和 reservoir-level energy consumption
- **超参数搜索空间**（见 Table I）：
  - Leak Rate: [0.1, 0.4]
  - Spectral Radius: [0.6, 1.1]
  - Reservoir Size: [100, 1000]（整数）
  - Connectivity: [0.2, 0.7]
- **训练细节**：
  - GRU 训练 100 epochs，AdamW 优化器
  - 固定随机种子，PyTorch 实现
  - 运行平台：NVIDIA Tesla T4 GPU（16GB VRAM）
  - 总 trials：50（含 20 次初始化）

### 📈 评估指标
| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | 分类准确率（±置信区间 CI） |
| **Energy Consumption (pJ/sample)** | 每样本平均能耗 |
| **Total Optimization Time (min)** | 完成全部超参搜索所需时间 |
| **Pareto Front** | 展示 accuracy-energy 权衡关系 |

### 🆚 基线方法对比
- **Optuna**：使用 NSGA-II 多目标采样器
- **Ray Tune**：异步并行采样
- 所有方法使用相同搜索空间、trial 预算和训练脚本，确保公平比较

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总（来自 Table II）

| Dataset | Model | Accuracy (%) | Energy (pJ/sample) | Opt. Time (min) |
|--------|-------|--------------|--------------------|------------------|
| **FSDD** | **EARL** | **95.39 ± 0.44** | **0.2089 ± 0.0129** | **10.57** |
|          | Optuna | 82.25 ± 2.75 | 0.3644 ± 0.0091 | 106.60 |
|          | Ray    | 88.15 ± 2.18 | 0.4178 ± 0.0042 | 100.88 |
| **HAR**  | **EARL** | **96.99 ± 0.34** | **0.20796 ± 0.02362** | **19.53** |
|          | Optuna | 90.52 ± 1.16 | 0.6265 ± 0.0193 | 47.75 |
|          | Ray    | 94.41 ± 0.67 | 0.43737 ± 0.00512 | 58.48 |
| **Occupancy** | **EARL** | **98.47 ± 8e-5** | **0.0278 ± 0.0052** | **15.28** |
|               | Optuna | 97.44 ± 0.27 | 0.1599 ± 0.0092 | 47.75 |
|               | Ray    | 98.47 ± 0.03 | 0.1624 ± 0.0030 | 64.85 |

### 🔍 对比分析
- **准确性提升**：EARL 在所有数据集上均显著优于基线，**高出 6–15%**。
- **能耗降低**：相比 Optuna/Ray，**能耗下降 60–80%**，尤其在 Occupancy 上达 **82% 以上节能**。
- **优化速度加快**：优化时间**缩短近一个数量级**（如 FSDD 上仅需 10.6 分钟 vs 超过 100 分钟）。
- **收敛更快**：EARL 在更少 trial 内达到最优（例如 HAR 仅需 39 次 trial）。

### 📈 Pareto 前沿分析（图 3）
- EARL 找到的解在高 accuracy、低 energy 区域占据主导地位。
- 解分布集中，变异小，表明搜索过程稳定可靠。
- 在 HAR 和 Occupancy 上展现出更强的多目标平衡能力。

> ❗ **没有提供消融实验（ablation study）**，无法量化 BO、RL、early termination 各组件的具体贡献。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **能量必须作为一级优化目标**：在 LSM 等低功耗神经形态系统中，忽略 energy 的 HPO 方法会严重低估实际部署成本。
2. **BO + RL 混合策略有效提升搜索效率**：BO 提供全局指导，RL 实现动态优先级排序，二者结合可避免陷入局部最优。
3. **自适应早停机制大幅节省资源**：在性能趋于饱和时及时停止，避免“过度搜索”，对边缘设备尤为重要。
4. **EARL 可扩展性强**：适用于多种传感器模态和时序任务，在语音、活动识别、环境感知中均表现优异。

### ⚠️ 局限性
- **缺乏硬件实测能耗数据**：当前 energy consumption 是模拟估算，尚未在真实 neuromorphic chip（如 Loihi、SpiNNaker）上验证。
- **未进行消融研究**：无法确认 RL 模块或 early termination 的独立影响。
- **搜索空间有限**：目前仅优化四个主要超参数，未涉及网络结构、突触延迟等更复杂变量。
- **RL Agent 设计较简单**：使用单层网络和 e-greedy 策略，可能限制长期策略学习能力。

### 🔮 未来工作方向
1. **Hardware-in-the-loop evaluation**：在真实 neuromorphic processor 上运行 EARL，获取真实的 energy profiling。
2. **扩展搜索空间**：纳入更多架构参数（如 topology、delay、threshold adaptation）。
3. **在线自适应调优**：支持 streaming data 下的持续学习与参数调整。
4. **应用于更大规模任务**：拓展至 sensory fusion、autonomous systems 等复杂场景。

---

## ✅ 总结
**EARL** 是首个专为 **Liquid State Machine** 设计的**能量感知超参数优化框架**，通过融合 **Bayesian Optimization** 与 **Reinforcement Learning**，实现了在准确性、能效和优化速度上的全面超越。实验表明，它能在多种时序任务中实现 **6–15% 更高的 accuracy、60–80% 更低的 energy 消耗、以及近一个数量级的优化加速**，为资源受限的 **Pervasive AI** 和 **Edge Intelligence** 应用提供了高效可靠的自动化调优方案。

</details>

---

### 16. [xDNN(ASP): Explanation Generation System for Deep Neural Networks powered by Answer Set Programming](https://arxiv.org/abs/2601.03847)

**Authors**: Ly Ly Trieu (New Mexico State University), Tran Cao Son (New Mexico State University)  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03847v1  

#### Abstract
Explainable artificial intelligence (xAI) has gained significant attention in recent years. Among other things, explainablility for deep neural networks has been a topic of intensive research due to the meteoric rise in prominence of deep neural networks and their "black-box" nature. xAI approaches ...

---

### 17. [Unlocking the Pre-Trained Model as a Dual-Alignment Calibrator for Post-Trained LLMs](https://arxiv.org/abs/2601.04277)

**Authors**: Beier Luo, Cheng Wang, Hongxin Wei, Sharon Li, Xuefeng Du  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.04277v1  

#### Abstract
Post-training improves large language models (LLMs) but often worsens confidence calibration, leading to systematic overconfidence. Recent unsupervised post-hoc methods for post-trained LMs (PoLMs) mitigate this by aligning PoLM confidence to that of well-calibrated pre-trained counterparts. However...

---

### 18. [TSSR: Two-Stage Swap-Reward-Driven Reinforcement Learning for Character-Level SMILES Generation](https://arxiv.org/abs/2601.04521)

**Authors**: Jacob Ede Levine, Yun Lyan Luo, Sai Chandra Kosaraju  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.04521v1  

#### Abstract
The design of reliable, valid, and diverse molecules is fundamental to modern drug discovery, as improved molecular generation supports efficient exploration of the chemical space for potential drug candidates and reduces the cost of early design efforts. Despite these needs, current chemical langua...

---

### 19. [ROI-Reasoning: Rational Optimization for Inference via Pre-Computation Meta-Cognition](https://arxiv.org/abs/2601.03822)

**Authors**: Muyang Zhao, Qi Qi, Hao Sun  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03822v1  

#### Abstract
Large language models (LLMs) can achieve strong reasoning performance with sufficient computation, but they do not inherently know how much computation a task requires. We study budgeted inference-time reasoning for multiple tasks under a strict global token constraint and formalize it as a Ordered ...

---

### 20. [Trade-R1: Bridging Verifiable Rewards to Stochastic Environments via Process-Level Reasoning Verification](https://arxiv.org/abs/2601.03948)

**Authors**: Rui Sun, Yifan Sun, Sheng Xu, Li Zhao, Jing Li, Daxin Jiang, Cheng Hua, Zuo Bai  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03948v2  

#### Abstract
Reinforcement Learning (RL) has enabled Large Language Models (LLMs) to achieve remarkable reasoning in domains like mathematics and coding, where verifiable rewards provide clear signals. However, extending this paradigm to financial decision is challenged by the market's stochastic nature: rewards...

---

### 21. [ARREST: Adversarial Resilient Regulation Enhancing Safety and Truth in Large Language Models](https://arxiv.org/abs/2601.04394)

**Authors**: Sharanya Dasgupta, Arkaprabha Basu, Sujoy Nath, Swagatam Das  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04394v1  

#### Abstract
Human cognition, driven by complex neurochemical processes, oscillates between imagination and reality and learns to self-correct whenever such subtle drifts lead to hallucinations or unsafe associations. In recent years, LLMs have demonstrated remarkable performance in a wide range of tasks. Howeve...

---

### 22. [Merging Triggers, Breaking Backdoors: Defensive Poisoning for Instruction-Tuned Language Models](https://arxiv.org/abs/2601.04448)

**Authors**: San Kim, Gary Geunbae Lee  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04448v1  

#### Abstract
Large Language Models (LLMs) have greatly advanced Natural Language Processing (NLP), particularly through instruction tuning, which enables broad task generalization without additional fine-tuning. However, their reliance on large-scale datasets-often collected from human or web sources-makes them ...

---

### 23. [PRISM: A Unified Framework for Post-Training LLMs Without Verifiable Rewards](https://arxiv.org/abs/2601.04700)

**Authors**: Mukesh Ghimire, Aosong Feng, Liwen You, Youzhi Luo, Fang Liu, Xuan Zhu  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04700v1  

#### Abstract
Current techniques for post-training Large Language Models (LLMs) rely either on costly human supervision or on external verifiers to boost performance on tasks such as mathematical reasoning and code generation. However, as LLMs improve their problem-solving, any further improvement will potentiall...

---

### 24. [Qwen3-VL-Embedding and Qwen3-VL-Reranker: A Unified Framework for State-of-the-Art Multimodal Retrieval and Ranking](https://arxiv.org/abs/2601.04720)

**Authors**: Mingxin Li, Yanzhao Zhang, Dingkun Long, Keqin Chen, Sibo Song, Shuai Bai, Zhibo Yang, Pengjun Xie, An Yang, Dayiheng Liu, Jingren Zhou, Junyang Lin  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04720v1  

#### Abstract
In this report, we introduce the Qwen3-VL-Embedding and Qwen3-VL-Reranker model series, the latest extensions of the Qwen family built on the Qwen3-VL foundation model. Together, they provide an end-to-end pipeline for high-precision multimodal search by mapping diverse modalities, including text, i...

---

### 25. [RiskAtlas: Exposing Domain-Specific Risks in LLMs through Knowledge-Graph-Guided Harmful Prompt Generation](https://arxiv.org/abs/2601.04740)

**Authors**: Huawei Zheng, Xinqi Jiang, Sen Yang, Shouling Ji, Yingcai Wu, Dazhen Deng  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04740v1  

#### Abstract
Large language models (LLMs) are increasingly applied in specialized domains such as finance and healthcare, where they introduce unique safety risks. Domain-specific datasets of harmful prompts remain scarce and still largely rely on manual construction; public datasets mainly focus on explicit har...

---

### 26. [A Navigational Approach for Comprehensive RAG via Traversal over Proposition Graphs](https://arxiv.org/abs/2601.04859)

**Authors**: Maxime Delmas, Lei Xu, Andr\'e Freitas  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04859v1  

#### Abstract
Standard RAG pipelines based on chunking excel at simple factual retrieval but fail on complex multi-hop queries due to a lack of structural connectivity. Conversely, initial strategies that interleave retrieval with reasoning often lack global corpus awareness, while Knowledge Graph (KG)-based RAG ...

---

### 27. [CuMA: Aligning LLMs with Sparse Cultural Values via Demographic-Aware Mixture of Adapters](https://arxiv.org/abs/2601.04885)

**Authors**: Ao Sun, Xiaoyu Wang, Zhe Tan, Yu Li, Jiachen Zhu, Shu Su, Yuheng Jia  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04885v1  

#### Abstract
As Large Language Models (LLMs) serve a global audience, alignment must transition from enforcing universal consensus to respecting cultural pluralism. We demonstrate that dense models, when forced to fit conflicting value distributions, suffer from \textbf{Mean Collapse}, converging to a generic av...

---

### 28. [Green MLOps: Closed-Loop, Energy-Aware Inference with NVIDIA Triton, FastAPI, and Bio-Inspired Thresholding](https://arxiv.org/abs/2601.04250)

**Authors**: Mustapha Hamdi, Mourad Jabou  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04250v1  

#### Abstract
Energy efficiency is a first-order concern in AI deployment, as long-running inference can exceed training in cumulative carbon impact. We propose a bio-inspired framework that maps protein-folding energy basins to inference cost landscapes and controls execution via a decaying, closed-loop threshol...

---

### 29. [ArtCognition: A Multimodal AI Framework for Affective State Sensing from Visual and Kinematic Drawing Cues](https://arxiv.org/abs/2601.04297)

**Authors**: Behrad Binaei-Haghighi, Nafiseh Sadat Sajadi, Mehrad Liviyan, Reyhane Akhavan Kharazi, Fatemeh Amirkhani, Behnam Bahrak  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04297v1  

#### Abstract
The objective assessment of human affective and psychological states presents a significant challenge, particularly through non-verbal channels. This paper introduces digital drawing as a rich and underexplored modality for affective sensing. We present a novel multimodal framework, named ArtCogniti...

---

### 30. [Enhanced-FQL($\lambda$), an Efficient and Interpretable RL with novel Fuzzy Eligibility Traces and Segmented Experience Replay](https://arxiv.org/abs/2601.04392)

**Authors**: Mohsen Jalaeian-Farimani  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04392v1  

#### Abstract
This paper introduces a fuzzy reinforcement learning framework, Enhanced-FQL($\lambda$), that integrates novel Fuzzified Eligibility Traces (FET) and Segmented Experience Replay (SER) into fuzzy Q-learning with Fuzzified Bellman Equation (FBE) for continuous control tasks. The proposed approach empl...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
