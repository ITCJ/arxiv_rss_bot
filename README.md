# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-07 05:58:08 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [LoRA-Drop: Temporal LoRA Decoding for Efficient LLM Inference](https://arxiv.org/abs/2601.02569)

**Authors**: Hossein Rajabzadeh, Maryam Dialameh, Chul B. Park, Il-Min Kim, Hyock Ju Kwon  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2601.02569v1  

#### Abstract
Autoregressive large language models (LLMs) are bottlenecked by sequential decoding, where each new token typically requires executing all transformer layers. Existing dynamic-depth and layer-skipping methods reduce this cost, but often rely on auxiliary routing mechanisms or incur accuracy degradat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LoRA-Drop: Temporal LoRA Decoding for Efficient LLM Inference 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在自回归推理过程中面临严重的**计算瓶颈**，即每个新 token 都需要执行完整的 Transformer 层堆栈，导致高延迟和高内存开销。尽管已有动态深度或层跳过方法尝试缓解该问题，但它们通常依赖额外的路由机制，或在跳过层时缺乏补偿更新，从而造成精度下降。

此外，传统的 KV Cache 在长序列生成中占用大量显存，限制了实际部署效率。

---

### 提出的新方法：LoRA-Drop
本文提出 **LoRA-Drop** —— 一种轻量级、即插即用的高效推理框架，其核心思想是利用 LLM 中隐藏状态的**时间冗余性**（temporal redundancy），通过以下方式加速解码：

- **固定子集中间层的时间调度策略**：选择一部分中间层作为“可跳过层”（droppable layers），在大多数解码步骤中不执行完整前向传播。
- **LoRA 轻量修正机制**：这些层复用上一个 token 的隐藏状态，并施加一个低秩适配器（LoRA）进行微调，以近似当前输出。
- **周期性刷新机制**：每隔 $k+1$ 步执行一次全模型前向传播（refresh step），防止误差累积漂移。
- **KV Cache 节省**：在 LoRA 模式下跳过对可跳过层的 KV 更新，显著减少 KV Cache 内存占用。

---

### 相比现有方法的优势
| 特性 | LoRA-Drop | 其他层跳过方法（如 Unified Layer Skipping, FlexiDepth） |
|------|----------|--------------------------------------------------------|
| 是否需要路由网络 | ❌ 否（无需额外参数） | ✅ 多数需要轻量路由模块 |
| 是否保留上下文连续性 | ✅ 利用缓存 + LoRA 补偿 | ❌ 通常直接跳过，无补偿 |
| 对 KV Cache 的影响 | ✅ 显著减少更新频率 | ⚠️ 一般仍需更新所有层 |
| 插件式兼容性 | ✅ 可无缝集成预训练模型 | ⚠️ 架构修改可能复杂 |
| 微调成本 | ✅ 仅需少量持续微调（continual fine-tuning） | ⚠️ 可能需要重新训练或复杂调优 |

> **创新亮点**：
> - 首次将 **LoRA** 引入推理阶段用于**层间状态补偿**，而非仅用于训练；
> - 提出“**时间计算调度**”（temporal compute schedule）概念，实现细粒度控制；
> - 实现了**计算效率与精度之间的平滑权衡曲线**（Pareto frontier）。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多个任务类别，确保泛化能力：

#### 推理与常识理解
- **MMLU**（多任务知识）
- **HellaSwag (HS)**（常识推理）
- **WinoGrande**（指代消解）
- **ARC-e/c**, **OpenBookQA**, **PIQA**, **RACE**
- **GSM8K**, **MATH**, **BBH**（数学与复杂推理）

#### 代码生成
- **HumanEval**, **MBPP**（Pass@1 / Pass@10）

#### 长文本与多语言
- **LongBench**（长上下文问答）
- **Needle-in-a-Haystack**（信息检索）
- **XNLI**, **XCOPA**（跨语言自然语言推断与因果推理）

---

### 实验设置与评估指标

#### 模型
在四种主流开源 LLM 上验证：
- **LLaMA2-7B**
- **LLaMA3-8B**
- **Qwen2.5-7B**
- **Qwen2.5-14B**

#### 关键超参数
- **Drop Ratio $p$**：可跳过层的比例，取值 $\{0.25, 0.5, 0.75\}$
- **Temporal Window $k$**：每 $k+1$ 步执行一次 full refresh，中间 $k$ 步使用 LoRA 模式
- **LoRA Rank $r=16$**, Scaling $\alpha=16$
- 序列长度：4096（部分为 2048 或 1024）

#### 评估指标
| 类别 | 指标 |
|------|------|
| 准确率 | Zero-shot Accuracy, Pass@1/10 |
| 效率 | Tokens/sec, Speedup ×, FLOPs reduction |
| 内存 | KV Cache Size (MB), KV-saving % |
| 延迟 | Per-token latency (p50, p95) |

---

### 基线方法对比
- **Full Model**：原始完整模型（无跳过）
- **Unified Layer Skipping** [18]
- **FlexiDepth** [19]

> 注：作者指出其他加速技术（如量化、推测解码、KV压缩）与本方法正交，故未纳入主比较，但强调未来可组合使用。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 模型 | 最高速度提升 | KV Cache 节省 | 精度损失上限 |
|------|-------------|----------------|--------------|
| LLaMA2-7B | **2.35×** ($p=0.75, k=3$) | ~50% | <0.5 pp |
| Qwen2.5-7B | **2.42×** ($p=0.75, k=3$) | ~55% | <0.5 pp |
| LLaMA3-8B | **2.38×** ($p=0.75, k=3$) | ~45% | <0.5 pp |
| Qwen2.5-14B | **2.60×** ($p=0.75, k=3$) | ~50% | <0.5 pp |

> ✅ 在 $p=0.5, k=3$ 的“安全区”配置下：
> - 平均速度提升 **1.6–1.8×**
> - KV Cache 减少 **40–55%**
> - 平均准确率下降 **≤0.5 percentage points (pp)**

---

### 与基线方法对比（Table I）

| 方法 | 平均准确率（vs Baseline） | 速度提升（×） |
|------|----------------------------|---------------|
| Full Model | 64.6 – 69.3 | 1.00 |
| Unified Layer Skipping | ↓ ~0.8 pp | 1.34–1.42× |
| FlexiDepth | ↓ ~0.5 pp | 1.49–1.55× |
| **LoRA-Drop (p=0.5)** | ↓ ~0.2–0.4 pp | **1.68–1.73×** |
| LoRA-Drop (p=0.75) | ↓ ~2.5 pp | **2.35–2.60×** |

> 💡 结论：LoRA-Drop 在相同精度损失下实现了更高的加速比。

---

### 消融实验结果（Table III & IV）

#### 不同 $p$ 和 $k$ 的权衡分析（以 LLaMA3-8B 为例）

| $p$ | $k$ | 准确率变化 △Acc | 速度提升 | KV Cache (MB) |
|-----|----|------------------|---------|----------------|
| 0.0 | - | +0.0 | 1.00× | 16000 |
| 0.25 | 3 | +0.1 | 1.24× | 13000 |
| 0.50 | 3 | -0.3 | **1.70×** | **9200** |
| 0.75 | 3 | -2.4 | **2.20×** | **5200** |
| 0.75 | 5 | -3.6 | 2.45× | 4200 |

> 🔍 发现：
> - $p \leq 0.5$, $k \leq 3$ 是“**安全区**”，精度损失极小（≤0.5 pp）
> - 提高 $k$ 可进一步降低 KV 写入频率，节省更多内存
> - $p=0.75$ 虽然加速明显，但精度下降显著，适用于低延迟场景

---

### LoRA 注入位置消融（Table V）
比较两种 LoRA 注入方式：

| 注入策略 | 准确率 | 速度提升 |
|--------|--------|----------|
| Attention+MLP LoRA | ≈ Baseline | 1.27–1.39× |
| **Block-level LoRA（默认）** | ≈ Baseline | **1.45–1.58×** |

> ✅ 推荐使用 **block-level（whole-layer）注入**，因其引入最少数据移动，获得最大吞吐增益。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM 隐藏状态存在显著时间冗余**  
   图1显示相邻 token 的隐藏状态余弦相似度高达 0.6–0.85，且对未来 3–6 个 token 仍有较强预测能力，支持“非每步都需全层计算”的假设。

2. **LoRA-Drop 实现高效与稳定的平衡**  
   通过周期性刷新 + LoRA 补偿，在保持语义一致性的同时大幅降低计算负担。

3. **KV Cache 节省具有可预测性**  
   公式推导表明 KV 节省比例取决于 $p$, $k$, $h_{kv}$ 等因素，实测节省达 **40–55%**。

4. **通用性强，跨模型/任务表现稳定**  
   在多种架构（LLaMA/Qwen）、尺寸（7B–14B）、任务（推理/代码/多语言）上均有效。

---

### 方法的局限性
- **依赖持续微调**：虽只需微调 LoRA 参数，但仍需额外训练阶段；
- **固定调度策略**：当前采用固定周期 $k$，尚未实现基于 token 复杂度的自适应调度；
- **极端设置有精度损失**：当 $p > 0.75$ 时，平均精度下降超过 2 pp，不适合高精度要求任务；
- **硬件优化潜力未完全释放**：目前未针对特定 GPU 架构优化内核，实际加速可能受限于访存带宽。

---

### 未来工作方向
1. **自适应调度机制**：基于 token 熵、logit margin 等信号动态决定是否刷新；
2. **扩展至多模态与检索增强模型**：应用于视觉-语言或多文档推理场景；
3. **与其他加速技术融合**：结合量化（Quantization）、KV 压缩、推测解码等形成复合优化方案；
4. **理论分析收敛性与误差传播边界**：建立更严谨的形式化保障。

---

## 总结

✅ **LoRA-Drop 是一种新颖、实用且高效的 LLM 推理加速方法**，它巧妙地结合了：
- 时间冗余观察
- LoRA 低秩补偿
- 周期性刷新机制
- KV Cache 节省设计

🎯 在几乎不影响精度的前提下（<0.5 pp），实现了 **最高 2.6× 解码速度提升** 和 **超过 50% 的 KV Cache 节省**，为大规模语言模型的实际部署提供了极具前景的技术路径。

🔧 项目代码已开源：[https://github.com/hosseinbv/LoRA-Drop.git](https://github.com/hosseinbv/LoRA-Drop.git)

</details>

---

### 2. [Lil: Less is Less When Applying Post-Training Sparse-Attention Algorithms in Long-Decode Stage](https://arxiv.org/abs/2601.03043)

**Authors**: Junhao Hu, Fangze Li, Mingtao Xu, Feifan Meng, Shiju Zhao, Tiancheng Hu, Ting Peng, Anmin Liu, Wenrui Huang, Chenxu Liu, Ziyue Hua, Tao Xie  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2601.03043v1  

#### Abstract
Large language models (LLMs) demonstrate strong capabilities across a wide range of complex tasks and are increasingly deployed at scale, placing significant demands on inference efficiency. Prior work typically decomposes inference into prefill and decode stages, with the decode stage dominating to...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Lil: Less is Less When Applying Post-Training Sparse-Attention Algorithms in Long-Decode Stage

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文揭示了一个在**Post-Training Sparse-Attention (PTSD)** 算法中被忽视的关键问题，称为 **“Lil” (Less is Less)**。尽管稀疏注意力算法（如 H2O、Sink、Quest、infLLM）通过减少每个解码步的计算量来加速推理，但它们会导致**信息丢失**，迫使模型生成更长的输出序列以重建上下文。这反而增加了端到端的延迟和内存占用，抵消了单步加速带来的收益。

这一现象被称为“Lil”，即“越少（关注的token）反而导致越多（生成的token）”。

---

### **提出了什么新方法或新思路**

为解决 Lil 问题，作者提出了 **Guardian**，一种用于**解码阶段的早停算法 (early-stopping algorithm)**。

- **核心思想**：当生成序列的信息增益趋于停滞时，停止解码。
- **实现方式**：利用 **LZ77 压缩算法**动态监测生成序列的压缩率变化。若连续若干步压缩长度增长低于阈值，则判定信息增益已饱和，提前终止生成。
- **理论基础**：基于信息论，压缩比可作为序列信息熵的代理指标。信息不再显著增加意味着后续生成是冗余的。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | 减少高达 **90% 的 token 消耗**，显著降低推理成本。 |
| **准确性** | 仅带来 **<2% 的准确率下降**，甚至在某些情况下因避免过度验证而提升准确率。 |
| **通用性** | 不依赖特定模型架构或训练过程，适用于所有 PTSD 算法及 full attention 场景。 |
| **实用性** | 计算开销极低（每 250 步压缩一次，耗时 ~34ms），易于集成。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

在三个数学推理密集型基准上进行评估：

- **GSM8K**：小学数学应用题，需多步推理。
- **MATH-500**：高中数学竞赛题，难度分层（1–5级）。
- **AIME**：美国数学邀请赛题目，涵盖代数、几何、数论等。

均从测试集中选取前 200 个样本进行评估。

---

### **实验设置和评估指标**

#### **模型**
- **DSR**: DeepScaleR-1.5B-Preview
- **DSL**: DeepSeek-R1-Distill-Llama-8B
- **Qwe**: Qwen1.5-MoE-A2.7B-Chat

覆盖不同规模、架构（Dense vs. MoE）和训练策略。

#### **硬件环境**
- 单张 NVIDIA A100-80GB GPU
- Intel Xeon Platinum 8358P CPU @ 2.60GHz
- CUDA 12.6

#### **评估指标**
- **Token Savings**：应用 Guardian 后节省的 token 数比例。
- **Accuracy**：输出与标准答案数学等价的比例。
- **Accuracy Drop**：使用 Guardian 后准确率的变化（绝对百分点）。

---

### **基线方法对比**

对比了五种主流 PTSD 算法：

| 算法 | 类型 | 是否保留全部 KV | 代表工作 |
|------|------|------------------|----------|
| **Full Attention** | 全注意力 | 是 | 基线 |
| **H2O** | 流水线 + 重用头 | 否 | Zhang et al., 2023 |
| **Sink** | 注意力锚点 | 否 | Xiao et al., 2024b |
| **Quest** | 查询感知稀疏 | 是 | Tang et al., 2024 |
| **infLLM** | 高效上下文记忆 | 是 | Xiao et al., 2024a |

所有算法均在不同 **Cache Budget**（128, 256, 512, 1024）下测试。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

根据 **Table 1** 和 **Table 3**，Guardian 在多个配置下表现优异：

| 模型 | 数据集 | Cache Budget | Token Savings | Accuracy Δ |
|------|--------|---------------|----------------|-------------|
| DSR | GSM8K | 128 | **83.0%** | -0.5pp |
| DSL | AIME | 128 | **92.3%** | -1.5pp |
| DSR | MATH-500 | 1024 | **52.0%** | +0.5pp ✅ |
| DSL | AIME | 1024 | **78.1%** | -0.5pp |

> ✅ 表示准确率提升

- **最高节省达 90%+ token**，尤其在小 cache budget 下效果显著。
- **平均准确率下降 <2%**，部分场景下准确率反升（防止遗忘正确答案）。
- **Qwen MoE 模型收益较小**：因其本身生成短且错误率高，缺乏冗余。

---

### **与基线方法的对比结果**

- 所有 PTSD 算法在无 Guardian 时均表现出 **更长的输出长度**（见 Figure 1b），即使准确率相近。
- 使用 Guardian 后，**token 消耗大幅下降**，同时保持或轻微牺牲准确率。
- **Guardian 对信息重建型冗余（Lil）和模式冗余（如 reward hacking）均有效**（见 Table 2）。

---

### **消融实验结果**

#### **按正确/错误案例分析（Table 3）**
| 案例类型 | 节省效果 | 原因 |
|---------|----------|------|
| **Incorrect Cases** | 极高节省（~90%） | 模型无法解决问题，陷入无限循环，Guardian 可及时终止。 |
| **Correct Cases** | 中等节省（~30–60%） | 正确答案已生成，但模型继续验证并遗忘，Guardian 阻止冗余生成。 |

#### **对 Full Attention 的有效性（Table 2）**
- 即使在 full attention 下，Guardian 也能节省 **9.4%–18.3%** 的 token。
- 说明 **Chain-of-Thought 冗余普遍存在**，不限于稀疏注意力场景。

---

## 4. 关键结论和发现

### **主要发现**

1. **Lil 问题是 PTSD 算法的“房间里的大象”**：
   - 稀疏注意力虽加快单步解码，但因信息丢失导致输出变长，**总延迟不降反升**。
   - 信息重建表现为重复内容、循环推理、最终遗忘答案。

2. **压缩比是衡量信息增益的有效代理**：
   - 利用 LZ77 压缩比可量化生成序列的信息密度。
   - 当压缩长度增长趋缓时，信息增益停滞，适合早停。

3. **Guardian 显著缓解 Lil 问题**：
   - 平均节省 **高达 90% token**，准确率损失 <2%。
   - 在错误案例中防止无限生成，在正确案例中防止过度验证。

4. **Guardian 具有泛化能力**：
   - 不仅适用于 PTSD，也适用于 full attention 下的冗余 CoT 生成。
   - 可视为一种通用的 **Chain-of-Thought 压缩机制**。

---

### **方法的局限性**

1. **评估范围有限**：
   - 仅测试了 3 个模型和 3 个数据集，可能影响结论普适性。
   - 更大上下文模型（如 Qwen2.5-Max）未覆盖。

2. **仅限于 PTSD 算法**：
   - 不适用于训练感知稀疏架构（如 DeepSeek NSA）。

3. **对弱模型效果有限**：
   - 如 Qwen MoE 因本身生成短且错误多，Guardian 收益小。

4. **压缩频率参数需调优**：
   - `f=250` 和 `t=20` 在实验中有效，但在不同硬件或模型上可能需调整。

---

### **未来工作方向**

1. **扩展至更广泛的 CoT 冗余场景**：
   - 探索 Guardian 在由数据偏差、人类偏好或奖励黑客引起的冗长推理中的应用。

2. **结合训练阶段优化**：
   - 将早停信号反馈至训练过程，学习更紧凑的推理路径。

3. **动态调整压缩频率**：
   - 根据生成阶段自适应调整 `f` 和 `t`，进一步降低开销。

4. **与其他 CoT 压缩方法对比**：
   - 如 ThinkPrune、HALT-CoT 等 post-training 方法，系统比较性能与效率。

---

> **总结**：本文首次系统揭示了 PTSD 算法中的 **Lil 问题**，并通过信息论视角提出 **Guardian 早停算法**，实现了**高效、低损的推理压缩**，为长序列生成的优化提供了新范式。

</details>

---

### 3. [Jenius Agent: Towards Experience-Driven Accuracy Optimization in Real-World Scenarios](https://arxiv.org/abs/2601.01857)

**Authors**: Defei Xia, Bingfeng Pi, Shenbin Zhang, Song Hua, Yunfei Wei, Lei Zuo  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.01857v1  

#### Abstract
As agent systems powered by large language models (LLMs) advance, improving the task performance of an autonomous agent, especially in context understanding, tool usage, and response generation, has become increasingly critical. Although prior studies have advanced the overall design of LLM-based ag...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前基于大语言模型（LLM）的自主智能体系统在实际应用中面临三大挑战：
- **固定提示（prompt）导致意图理解偏差**：静态或通用的提示无法适应任务状态变化，易造成行为不稳定和输出不一致。
- **工具调用缺乏上下文感知**：依赖预定义工具列表或手工规则，难以在模糊或多领域场景下准确选择合适工具，导致无效或错误调用。
- **长对话中的上下文冗余**：历史对话累积导致token开销增加、关键信号稀释，影响推理质量与系统效率。

这些问题限制了智能体在复杂、多轮、真实世界任务中的准确性、鲁棒性和可扩展性。

---

### **提出了什么新方法或新思路**
本文提出了一种面向真实场景的经验驱动型智能体框架——**Jenius-Agent**，包含三个核心优化模块：

#### （1）**自适应提示生成（Adaptive Prompt Generation）**
- 结合角色指令、任务状态和用户上下文动态生成系统提示。
- 引入**意图分类机制**（社交交互、创意生成、事实回忆、工具增强推理），根据不同类别调整推理策略与输出格式。
- 加入安全控制层（如防止幻觉、参数验证、内容审核），提升响应可靠性。

#### （2）**上下文感知的工具编排（Context-Aware Tool Orchestration）**
- 将所有工具表示为高维嵌入（Qwen3 Embedding），通过语义相似度进行检索。
- 设计三步筛选流程：
  1. Top-M候选检索（基于语义匹配）
  2. 基于“拐点检测”（inflection point）的过滤（结合相似度跳跃法与Kneedle算法）
  3. 动态截断并补足至最优数量 $N = \min(N_{\text{jump}}, N_{\text{kneedle}})$，默认保留前10个相关工具
- 支持MCP协议兼容的外部工具集成，提升跨系统互操作性。

#### （3）**分层记忆管理（Hierarchical Memory Management）**
- **对话级对齐**：维护消息序列结构（Human → AI → Tool → AI），修复因失败调用导致的消息缺失。
- **会话级压缩**：当消息总数超过阈值时，将早期历史摘要为`SystemMessage`，仅保留最近一轮完整上下文。
- 采用递归摘要策略，在减少token消耗的同时保持语义连贯性。

此外，Jenius-Agent整合了完整的端到端架构，支持文件I/O、执行反馈及MCP工具调用，已在生产环境部署（[jenius.cn](https://www.jenius.cn)）。

---

### **相比现有方法的优势**
| 维度 | 现有方法（如AutoGPT、LangChain Agents） | Jenius-Agent |
|------|----------------------------------------|-------------|
| 提示设计 | 固定模板，缺乏动态调整 | 自适应生成，融合意图识别与历史感知 |
| 工具选择 | 静态列表或硬编码规则 | 语义检索 + 拐点过滤，抗噪声能力强 |
| 上下文管理 | 窗口截断或简单摘要 | 分层结构化压缩，保留关键依赖关系 |
| 协议兼容性 | 各自为政，接口不统一 | 支持MCP等新兴通信协议（ACP, A2A） |
| 实际部署 | 多数停留在研究原型 | 已上线运行，具备可观测性与CI/CD支持 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**

#### （1）**APIGen**（公开基准）
- 规模：60K样本，覆盖21类API调用任务
- 特点：单轮交互，每个实例含自然语言查询、候选API列表与真实函数调用
- 本文改进：每条查询注入100个无关工具，模拟高噪声环境，测试工具检索鲁棒性

#### （2）**Jenius-bench**（本文构建的真实世界多轮任务数据集）
- 规模：850个人工标注样本，涵盖38类工具
- 应用场景：旅行规划、票务预订、网页生成、学术检索等
- 特点：
  - 多轮对话，支持状态演化
  - 包含完整执行轨迹（工具调用、输入参数、返回结果、代理响应）
  - 工具附带丰富语义描述（目的、前提条件、示例）
  - 所有路径经专家评审，确保正确性与一致性

> ✅ 对比见表1：Jenius-bench更贴近现实，强调链式推理、跨轮次上下文与MCP兼容性。

---

### **实验设置与评估指标**

#### **评估框架三大维度**：
| 维度 | 指标体系 | 内容 |
|------|---------|------|
| **过程保真度** | **4T Metrics** | TCR, TFR, TIR, TPS（详见下文） |
| **输出质量** | **CRCFF Metrics** | Correctness, Relevance, Completeness, Fluency, Faithfulness（由Qwen/DeepSeek打分） |
| **效率** | **Token Consumption** | 输入+输出token总量，衡量计算成本 |

#### **基线方法对比配置**
| 模型 | 描述 |
|------|------|
| **Base** | 标准ReAct风格智能体（observe-think-act循环） |
| **B-P** | Base + 自适应提示生成 |
| **B-PT** | B-P + 上下文感知工具编排 |
| **Jenius** | 完整版：B-PT + 分层记忆管理 |

所有模型共享相同LLM backbone与推理协议，仅组件逐步叠加，便于消融分析。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **（1）执行保真度（Execution Fidelity）**

##### 在 **APIGen** 上的结果（表2）
| Model | TCR ↑ | TFR ↓ | TIR ↓ | TPS ↑ |
|-------|-------|-------|-------|-------|
| Base | 0.8150 | 0.1800 | 0.0050 | 0.8150 |
| B-P | 0.8275 | 0.1675 | 0.0050 | 0.8275 |
| B-PT | 0.8375 | 0.1587 | 0.0038 | 0.8375 |
| **Jenius** | **0.8500** | **0.1362** | **0.0138** | **0.8500** |

> 🔍 注：APIGen本身难度较低（模式匹配为主），各模型表现接近，提升有限。

##### 在 **Jenius-bench** 上的结果（表3）
| Model | TCR ↑ | TFR ↓ | TIR ↓ | TPS ↑ |
|-------|-------|-------|-------|-------|
| Base | 0.5659 | 0.0329 | 0.4012 | 0.5968 |
| B-P | **0.7271 (+28.5%)** | 0.0859 | 0.1871 | 0.7491 |
| B-PT | 0.7494 | 0.0718 | 0.1788 | 0.7740 |
| **Jenius** | **0.7647 (+35.1%)** | **0.0753** | **0.1600** | **0.7847** |

> ✅ **结论**：在复杂多轮任务中，Jenius显著提升任务完成率（TCR↑35%）、降低未完成率（TIR↓60%），体现其强大的上下文建模能力。

---

#### **（2）输出质量（CRCFF Metrics）**（表4，仅在Jenius-bench上评估）

| 模型 | Correctness ↑ | Relevance ↑ | Completeness ↑ | Fluency ↑ | Faithfulness ↑ |
|------|---------------|-------------|----------------|-----------|----------------|
| Base | ~0.74 | ~0.91 | ~0.78 | ~0.94 | ~0.81 |
| B-P | ↑~5–8% | ↑~2–3% | ↑~1–2% | ↑~2–3% | ↑~4–5% |
| B-PT | ↑进一步提升 | — | ↑明显 | ↑稳定 | ↑持续改善 |
| **Jenius** | **↑8–10%** | **↑~5%** | **↑~3–4%** | **↑~3–5%** | **↑~6–8%** |

> ✅ 自适应提示提升**相关性与流畅性**；工具编排增强**正确性与完整性**；分层记忆保障**忠实性**（faithfulness）。

---

#### **（3）Token 消耗分析**（图6）

| 数据集 | Base | Jenius |
|--------|------|--------|
| APIGen | 9.96M | **2.46M**（↓75.3%） |
| Jenius-bench | 9.27M | **3.65M**（↓60.6%） |

> ✅ 显著降低token开销，得益于：
- 自适应提示减少冗余推理
- 工具检索避免无效调用
- 分层记忆压缩历史上下文

---

### **消融实验结果**
从Base → B-P → B-PT → Jenius，每一模块均带来持续增益：
- **B-P** 贡献最大提升（TCR +16%），说明意图识别与提示优化至关重要；
- **B-PT** 进一步提升精度与完整性；
- **Jenius** 最终实现最佳平衡，尤其在长程任务中稳定性更强。

---

## **4. 关键结论和发现**

### **主要发现**
1. **经验驱动的设计优于静态架构**：通过融合实时任务状态、用户意图与历史上下文，能显著提升智能体的行为一致性与任务成功率。
2. **上下文感知是工具调用的关键**：语义检索 + 拐点过滤机制有效应对高噪声、多候选场景，避免“盲目调用”。
3. **分层记忆优于简单截断**：结构化摘要既控制token增长，又保留关键跨轮依赖，支撑长期推理。
4. **模块化设计利于工程落地**：Jenius已成功部署于生产环境，支持可观测性（Langfuse）、自动化发布（Kubernetes + CI/CD）与安全通信（mTLS）。

---

### **方法的局限性**
1. **依赖高质量工具元数据**：若工具描述不完整或语义模糊，会影响检索效果。
2. **拐点检测对分布敏感**：极端情况下可能误判相关性边界。
3. **评估仍偏重功能正确性**：尚未充分纳入用户体验、决策成本、延迟等因素。
4. **多智能体协作未深入探索**：当前为单智能体架构，分布式协同潜力待挖掘。

---

### **未来工作方向**
1. **更灵活的评估体系**：允许多种合理执行路径，引入用户满意度、决策效率等现实指标。
2. **动态模块重组**：根据任务复杂度自动启用/关闭某些优化模块，实现资源自适应。
3. **多智能体协作机制**：构建基于A2A/MCP协议的Agent Network，实现分工与协同求解。
4. **强化解释性与透明度**：提供中间推理链、工具调用理由，增强用户信任。
5. **持续学习闭环**：利用线上反馈自动优化提示模板、工具索引与记忆策略。

---

> 📌 **总结**：  
> **Jenius-Agent** 是一个轻量、可扩展、协议兼容的智能体框架，通过**自适应提示、上下文感知工具编排、分层记忆管理**三大创新，在真实世界复杂任务中实现了**20%的任务准确率提升**，同时大幅降低token消耗与响应延迟。其实验设计严谨，评估全面，并已完成工业级部署，为下一代自主智能体系统提供了重要的实践蓝图。

</details>

---

### 4. [MiMo-V2-Flash Technical Report](https://arxiv.org/abs/2601.02780)

**Authors**: Bangjun Xiao, Bingquan Xia, Bo Yang, Bofei Gao, Bowen Shen, Chen Zhang, Chenhong He, Chiheng Lou, Fuli Luo, Gang Wang, Gang Xie, Hailin Zhang, Hanglong Lv, Hanyu Li, Heyu Chen, Hongshen Xu, Houbin Zhang, Huaqiu Liu, Jiangshan Duo, Jianyu Wei, Jiebao Xiao, Jinhao Dong, Jun Shi, Junhao Hu, Kainan Bao, Kang Zhou, Lei Li, Liang Zhao, Linghao Zhang, Peidian Li, Qianli Chen, Shaohui Liu, Shihua Yu, Shijie Cao, Shimao Chen, Shouqiu Yu, Shuo Liu, Tianling Zhou, Weijiang Su, Weikun Wang, Wenhan Ma, Xiangwei Deng, Bohan Mao, Bowen Ye, Can Cai, Chenghua Wang, Chengxuan Zhu, Chong Ma, Chun Chen, Chunan Li, Dawei Zhu, Deshan Xiao, Dong Zhang, Duo Zhang, Fangyue Liu, Feiyu Yang, Fengyuan Shi, Guoan Wang, Hao Tian, Hao Wu, Heng Qu, Hongfei Yi, Hongxu An, Hongyi Guan, Xing Zhang, Yifan Song, Yihan Yan, Yihao Zhao, Yingchun Lai, Yizhao Gao, Yu Cheng, Yuanyuan Tian, Yudong Wang, Zhen Tang, Zhengju Tang, Zhengtao Wen, Zhichao Song, Zhixian Zheng, Zihan Jiang, Jian Wen, Jiarui Sun, Jiawei Li, Jinlong Xue, Jun Xia, Kai Fang, Menghang Zhu, Nuo Chen, Qian Tu, Qihao Zhang, Qiying Wang, Rang Li, Rui Ma, Shaolei Zhang, Shengfan Wang, Shicheng Li, Shuhao Gu, Shuhuai Ren, Sirui Deng, Tao Guo, Tianyang Lu, Weiji Zhuang, Weikang Zhang, Weimin Xiong, Wenshan Huang, Wenyu Yang, Xin Zhang, Xing Yong, Xu Wang, Xueyang Xie, Yilin Jiang, Yixin Yang, Yongzhe He, Yu Tu, Yuanliang Dong, Yuchen Liu, Yue Ma, Yue Yu, Yuxing Xiang, Zhaojun Huang, Zhenru Lin, Zhipeng Xu, Zhiyang Chen, Zhonghua Deng, Zihan Zhang, Zihao Yue  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.02780v1  

#### Abstract
We present MiMo-V2-Flash, a Mixture-of-Experts (MoE) model with 309B total parameters and 15B active parameters, designed for fast, strong reasoning and agentic capabilities. MiMo-V2-Flash adopts a hybrid attention architecture that interleaves Sliding Window Attention (SWA) with global attention, w...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MiMo-V2-Flash Technical Report 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大模型在实现**强推理能力**和**智能体（agentic）行为**的同时，面临两大瓶颈：
- **长上下文建模效率低**：全注意力机制（Full Attention）计算复杂度为 $O(n^2)$，导致长序列推理速度慢、显存占用高。
- **后训练（post-training）扩展性差**：传统强化学习（RL）训练存在“跷跷板效应”（capability imbalance），即提升某一领域能力常导致其他领域退化。

### 提出的新方法与创新思路

#### （1）**混合滑动窗口注意力架构（Hybrid Sliding Window Attention, SWA）**
- 采用 **5:1 的局部-全局注意力比例**，每5层 Sliding Window Attention（SWA）后接1层 Global Attention（GA）。
- 滑动窗口大小设为 **128 tokens**，显著降低 KV Cache 存储和注意力计算开销。
- 引入 **可学习的 attention sink bias** 来缓解小窗口带来的信息丢失问题，增强长程依赖建模能力。

#### （2）**轻量级多令牌预测模块（Lightweight Multi-Token Prediction, MTP）**
- 在预训练中引入 MTP 作为辅助目标，提升训练效率。
- 推理阶段将 MTP 模块用作 **Speculative Decoding 的草稿模型（draft model）**，实现高达 **3.6 的平均接受长度** 和 **2.6x 的解码加速**。
- MTP 模块设计为轻量化结构（使用 Dense FFN + SWA），避免成为新的推理瓶颈。

#### （3）**多教师在线策略蒸馏范式（Multi-Teacher On-Policy Distillation, MOPD）**
- 一种全新的后训练框架，分为三个阶段：
  1. **通用 SFT**：建立基础指令遵循能力；
  2. **领域专用 RL/SFT**：训练多个领域专家教师模型（如数学、编码、搜索等）；
  3. **MOPD 蒸馏**：学生模型通过在线采样，从多个教师处获取 token-level 的 KL 散度奖励信号，融合各领域最强能力。
- 优势：避免离线数据蒸馏中的分布偏移问题，支持能力共进化（co-evolution）。

### 相比现有方法的优势
| 维度 | MiMo-V2-Flash | 对比模型（如 DeepSeek-V3.2、Kimi-K2） |
|------|----------------|----------------------------------------|
| 参数总量 | **309B** | 671B ~ 1043B |
| 激活参数 | **15B / token** | 37B ~ 32B / token |
| 长上下文效率 | 显著更低的 KV Cache 占用（近6倍减少） | 全注意力架构，KV 开销更高 |
| 推理速度 | MTP 加速下最高达 **2.6x 解码提速** | 无类似机制 |
| 后训练灵活性 | MOPD 支持模块化集成多个教师 | 多为串行或多任务联合训练 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 预训练数据
- 总计 **27万亿 tokens**，涵盖高质量网页、书籍、学术论文、代码、STEM 内容。
- 特别强调长距离依赖数据（如完整仓库代码、PR、commit history）以增强推理能力。

#### 评估基准
| 类别 | 主要数据集 |
|------|-----------|
| **通用理解与推理** | MMLU, BBH, TriviaQA, DROP, ARC, HellaSwag, WinoGrande |
| **数学推理** | GSM8K, MATH, AIME2024/25, HMMT Feb.2025 |
| **编程能力** | HumanEval+, MBPP+, CRUXEval, BigCodeBench, LiveCodeBench, SWE-Bench（Verified & Multilingual） |
| **中文理解** | C-Eval, CMMLU, C-SimpleQA |
| **多语言能力** | GlobalMMLU, INCLUDE |
| **长上下文能力** | NIAH-Multi, GSM-Infinite, LongBench V2, MRCR |
| **智能体任务** | SWE-Bench, Terminal-Bench, BrowseComp, τ²-Bench |

### 实验设置与评估指标
- **上下文长度**：原生支持 **32K**，扩展至 **256K**。
- **训练精度**：FP8 混合精度训练，提高效率并保持稳定性。
- **评估方式**：
  - Zero-shot / Few-shot 准确率（Accuracy）
  - SWE-Bench 使用 **Verified Pass@1** 指标
  - 推理速度测试基于 **16K 输入 + 1K 输出长度**
  - MTP 性能以 **Acceptance Length** 和 **Decoding Speedup** 衡量

### 基线方法对比
- **开源模型**：
  - DeepSeek-V3.2-Thinking / Base
  - Kimi-K2-Thinking / Base
- **闭源模型**：
  - Claude Sonnet 4.5
  - GPT-5 (High)
  - Gemini 3.0 Pro

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 9）

| 基准 | MiMo-V2-Flash | 最优基线 | 结果分析 |
|------|---------------|----------|---------|
| **MMLU-Pro** | **84.9** | Kimi-K2 (84.6) | 超越同类开源模型 |
| **GPQA-Diamond** | 84.3 | Claude (91.9) | 接近顶级闭源模型 |
| **AIME 2025** | **94.1** | Kimi-K2 (94.5) | 接近最优水平 |
| **LiveCodeBench** | **85.1** | Kimi-K2 (83.1) | 显著领先 |
| **SWE-Bench Verified** | **73.4%** | GPT-5-High (74.9%) | 当前最佳开源表现 |
| **SWE-Bench Multilingual** | **71.7%** | DeepSeek-V3.2 (70.2%) | 首次突破 70%，领先明显 |
| **LongBench V2** | **60.6** | Claude (65.6) | 超越 Kimi-K2（48.1） |
| **MRCR (long-context retrieval)** | **45.7** | Claude (89.7) | 表现稳健，优于多数基线 |

> 注：尽管部分指标仍略低于最强闭源模型（如 Claude、GPT-5），但在**仅使用 1/2~1/3 参数量**的情况下达到接近甚至超越多数开源模型的表现。

### 与基线方法的对比结果
- MiMo-V2-Flash 在 **SWE-Bench Verified** 上超过 Kimi-K2-Thinking（71.3 → 73.4），且远超 DeepSeek-V3.2（73.1）。
- 在 **AIME 2025** 数学竞赛任务上达到 94.1，仅次于 Kimi-K2（94.5），显著高于 DeepSeek-V3.2（93.1）。
- 在 **推理速度方面**，利用 MTP 实现 **最高 2.6x 解码加速**（见 Table 10），在 batch size=64、accept length=3.6 时可达 2.53x。
- 在 **长上下文检索任务 NIAH-Multi** 中，32K~128K 上成功率接近 100%，显著优于 DeepSeek-V3.2-Exp Base。

### 消融实验结果（Table 2–4）

| 架构变体 | MMLU | GSM8K | AIME25 | GSM-Infinite |
|--------|------|-------|--------|-------------|
| All GA | 57.3 | 34.2 | 45.5 | 12.3 |
| Hybrid SWA (W=128, w/o sink) | 54.9 | 36.9 | 47.1 | 17.3 |
| Hybrid SWA (W=128, w/ sink) | **58.3** | **36.9** | **47.1** | **17.3** |
| Hybrid SWA (W=512, w/ sink) | 58.3 | 37.9 | 46.3 | 17.2 |

**关键发现**：
- **Attention Sink 至关重要**：不加 sink 会导致性能下降；加入后全面反超 All-GA。
- **小窗口更优**：W=128 比 W=512 在长上下文任务（GSM-Infinite）中表现更好，说明更严格的局部约束反而有助于分工明确（SWA 处理局部，GA 处理全局）。
- **推理能力更强**：Hybrid SWA (W=128) 在复杂推理任务（AIME, GPQA）上优于 All-GA。

---

## 4. 关键结论和发现

### 主要发现
1. **高效的混合注意力架构可行且优越**：
   - 即使使用极小的滑动窗口（128 tokens）和高比例 SWA（5:1），配合 attention sink 也能实现甚至超越全注意力模型的性能。
   - 小窗口带来正则化效果，促进局部与全局注意力的职责分离。

2. **MTP 不仅是训练技巧，更是推理加速利器**：
   - 利用 MTP 作为 speculative decoding 的 draft model，可在无需额外硬件成本下实现 **高达 2.6x 的解码加速**。
   - 接受长度与 next-token entropy 呈强负相关（$R^2=0.995$），表明不确定性越低的任务越适合 MTP 加速。

3. **MOPD 成功解决“能力失衡”问题**：
   - 学生模型在大多数任务上达到了最强教师的水平，甚至略有超越（如 AIME +0.2, Tau2-Bench +0.7）。
   - 实现了“一个模型掌握多个专家技能”的统一智能体目标。

4. **MiMo-V2-Flash 是当前最强开源软件工程模型**：
   - 在 SWE-Bench Verified 和 Multilingual 上均取得 SOTA 表现，验证了大规模 agentic RL 的有效性。

### 方法的局限性
- **知识容量受限**：由于总参数量相对较小，在 SimpleQA 等知识密集型任务上得分较低（20.6 vs GPT-5 >35）。
- **创意写作能力稍弱**：Arena-Hard 创意写作得分为 86.2，低于 Kimi-K2（88.8）和 Claude（93.6）。
- **架构探索尚浅**：目前对 SWA/GA 比例、窗口大小的设计仍属初步尝试，未进行系统性搜索。

### 未来工作方向
1. **扩大模型规模**：通过增加参数量缩小与最强闭源模型（如 GPT-5）之间的差距。
2. **深入优化混合注意力架构**：探索更优的 SWA/GA 比例、动态窗口机制、非均匀注意力分配。
3. **迭代式 MOPD 共进化**：将蒸馏后的学生重新投入 RL 训练生成更强教师，形成闭环提升循环。
4. **工具调用与上下文管理优化**：进一步研究如何高效管理超长上下文中的工具历史与记忆压缩（参考 Appendix C）。

---

> ✅ **总结一句话**：  
> MiMo-V2-Flash 通过 **Hybrid SWA + Lightweight MTP + MOPD** 三大技术创新，在仅 309B 总参数下实现了媲美更大模型的推理与智能体能力，并首次在 SWE-Bench 上确立了开源模型的领先地位，同时开源模型权重推动社区发展。

</details>

---

### 5. [Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling](https://arxiv.org/abs/2601.02346)

**Authors**: Falcon LLM Team, Iheb Chaabane, Puneesh Khanna, Suhail Mohmad, Slim Frikha, Shi Hu, Abdalgader Abubaker, Reda Alami, Mikhail Lubinets, Mohamed El Amine Seddik, Hakim Hacid  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.02346v1  

#### Abstract
This work introduces Falcon-H1R, a 7B-parameter reasoning-optimized model that establishes the feasibility of achieving competitive reasoning performance with small language models (SLMs). Falcon-H1R stands out for its parameter efficiency, consistently matching or outperforming SOTA reasoning model...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在复杂推理任务上的性能提升依赖于大规模训练（scaling training），但这种方法面临计算成本高昂、高质量人类标注数据稀缺等瓶颈。同时，**Test-Time Scaling (TTS)** 虽然能通过生成多个推理链并聚合结果来提升准确性，但也带来了极高的推理开销。

本论文旨在解决以下核心挑战：
- 如何在不显著增加模型参数规模的前提下，实现与更大模型相当甚至更优的推理能力。
- 如何设计一个高效架构和训练流程，使小模型（SLM）在 TTS 场景下兼具高准确率、低 token 消耗和快速推理速度。

### 提出的新方法与创新思路
作者提出了 **Falcon-H1R-7B**，一个专为高效推理优化的 7B 参数混合架构语言模型，其主要创新包括：

#### （1）Hybrid Transformer-Mamba 架构（Falcon-H1）
- 采用 **Transformer-SSM（State Space Model）混合架构**，结合了 Transformer 的强大表达能力和 Mamba 在长序列建模中的线性时间效率。
- 该架构支持高达 256K 的上下文长度，并在大批次、长输出场景下表现出色，特别适合需要大量 Chain-of-Thought 推理的 TTS 方法。

#### （2）两阶段精细化训练策略
- **Cold-start SFT（Supervised Fine-Tuning）**：
  - 使用高质量、多样化的长推理轨迹数据（数学、代码、科学等）进行监督微调。
  - 引入 **difficulty-aware weighting**：对难题样本加权，易题降权或剔除，提升模型处理难问题的能力。
  - 验证发现：**更高的 rollout count（n=12）有助于学习多样化解法路径**；而多教师混合反而降低性能（single-teacher 更优）。

- **Reinforcement Learning with Verifiable Rewards (RLVR)**：
  - 基于 GRPO（Group Relative Policy Optimization）框架进行强化学习。
  - 设计领域特定奖励函数（math/code/science），确保信号可靠。
  - 引入 **backfill + caching 机制** 提高采样效率，减少约 30% 的冗余生成。
  - 最终模型在保持高 entropy 的同时稳定提升 pass@1 性能。

#### （3）面向 Test-Time Scaling 的极致优化
- 将 Falcon-H1R 与最新的 **DeepConf@512** 方法结合，动态基于置信度剪枝低质量推理链。
- 利用模型良好的 confidence calibration 实现 aggressive early stopping，大幅节省 token 开销而不牺牲精度。

### 相比现有方法的优势
| 维度 | Falcon-H1R-7B 的优势 |
|------|------------------------|
| **参数效率** | 仅 7B 参数，性能超越 8B–32B 的 SOTA 模型（如 Qwen3-32B、Phi-4-R-Plus-14B） |
| **推理效率** | 混合架构带来更快的 inference throughput，尤其在 batch size > 32 和 output length > 16K 时领先明显 |
| **token 效率** | 在 DeepConf 设置下 token usage 显著低于基线（例如在 AIME25 上减少 38%） |
| **准确性** | 多项基准测试中达到 SOTA 或接近 SOTA 水平 |

---

## 2. 核心实验方法和设置

### 使用的数据集
分为两个阶段：

#### SFT 阶段
- **数学**：MATH、AIME、HMMT、AMO-Bench 等竞赛级题目
- **代码**：LeetCode、AtCoder、Codeforces 来源的算法题
- **科学**：STEM 领域多跳推理问题
- **其他**：通用对话、指令跟随、工具调用等
- 数据总量：约 3.1M 样本，涵盖 220k “Other”、310k Science、840k Code、1.74M Math

#### RL 阶段
- 完全独立于 SFT 数据，防止记忆化
- 数学与代码为主，经过严格难度筛选（去除太简单或完全无法求解的问题）
- 使用 pass rate（8 次 rollout 正确率）作为难度代理指标

### 实验设置与评估指标

#### 评估基准分类
| 类别 | 基准名称 |
|------|---------|
| **数学** | AIME24, AIME25, HMMT25, AMO-Bench, MATH500 |
| **代码** | LiveCodeBench v6, SciCode, T2-Telecom, Terminal Bench Hard |
| **通用推理** | GPQA-Diamond, MMLU-Pro, Humanity's Last Exam (HLE), IFBench |

#### 评估方式
- 所有结果报告 **pass@1**
- 多数任务使用 `temperature=0.6`, `top_p=0.95`
- 对于 TTS 实验，使用 **DeepConf@512** 方法：
  - 固定 trace budget K=512
  - 前 16 条用于确定置信阈值
  - 后续生成过程中若 group confidence 下降则提前终止

#### 基线对比模型
- **7B 级别**：Qwen3-8B, DeepSeek-R1-0528-Qwen3-8B
- **14B–20B 级别**：Phi-4-Reasoning-Plus-14B, Apriel-1.5-15b-Thinker, GPT-OSS-20B
- **32B 及以上**：Qwen3-32B, Nemotron-H-47B-Reasoning

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 4–6）

#### 数学推理表现（Table 4）
| 模型 | AIME24 | AIME25 | HMMT25 | AMO-Bench | MATH500 |
|------|--------|--------|--------|-----------|---------|
| **Falcon-H1R-7B** | **88.1** | **83.1** | **64.9** | **36.3** | **97.4** |
| Qwen3-32B | 79.4 | 71.0 | 49.8 | 21.3 | 96.8 |
| GPT-OSS-20B | 83.3 | 84.4 | 64.8 | 26.0 | 94.8 |

> ✅ 在 AIME24、HMMT25、AMO-Bench 上全面领先，其中 AMO-Bench 超越第二名超 10 个百分点。

#### 代码生成表现（Table 5）
| 模型 | LCB v6 | SciCode (sub/main) | T2-Telecom | TB Hard |
|------|--------|--------------------|------------|---------|
| **Falcon-H1R-7B** | **68.6** | 28.3 / 3.9 | 25.4 | 4.9 |
| GPT-OSS-20B | 72.0 | 34.9 / 6.2 | 60.2* | 9.9* |

> ✅ 在 LiveCodeBench 上仅次于 GPT-OSS-20B，在其他代码任务上具有竞争力。

#### 通用推理表现（Table 6）
| 模型 | GPQA-Diamond | MMLU-Pro | HLE | IFBench |
|------|--------------|----------|-----|--------|
| **Falcon-H1R-7B** | 61.3 | 72.1 | **11.1** | **53.4** |
| Phi-4-R-Plus-14B | 67.9 | 79.2 | 5.9 | 51.7 |

> ⚠️ 在知识密集型任务（如 GPQA、MMLU-Pro）略逊于最大模型，但在 HLE 和 IFBench 上表现优异，体现强指令遵循能力。

### Test-Time Scaling 结果（Table 7）

| 模型 | AIME25 Acc↑ | AIME25 Tok↓ | GPQA-D Acc↑ | GPQA-D Tok↓ |
|------|-------------|-------------|-------------|-------------|
| Qwen3-32B | 86.7 | 174.8 | 70.1 | 460.0 |
| **Falcon-H1R-7B** | **96.7** | **95.1** | **70.2** | **452.3** |

> 🔥 **在 AIME25 上达到 96.7% 准确率，同时 token 消耗仅为 Qwen3-32B 的 ~54%（下降 38%）**

### 消融实验关键发现（Section 2.2 & 3.3）

| 实验维度 | 最佳选择 | 发现 |
|--------|----------|------|
| 学习率 | 1024×10⁻⁶ | 较大 LR 加速收敛且提升下游任务性能 |
| Rollout 数量 | n=12 | 更多 rollout 提升困难问题的表现 |
| 教师模型 | Single-teacher | 多 teacher 导致风格冲突，性能下降 |
| 数据权重 | Difficulty-aware | 难题加权可显著提升整体性能 |
| RL Curriculum | Math → Code | 顺序训练带来轻微增益 |
| RL Group Size | G=16 | 平衡多样性与计算成本 |

---

## 4. 关键结论和发现

### 主要发现
1. **小型模型也能实现顶级推理性能**  
   Falcon-H1R-7B 以仅 7B 参数，在多项推理基准上超越 2× 至 7× 更大的模型，证明了 **“训练质量 > 模型大小”** 的可行性。

2. **精心设计的数据与训练策略至关重要**  
   - 高质量、长链推理数据 + 难度感知加权是 SFT 成功的关键。
   - RL 阶段需避免过拟合，合理利用 GRPO 和 backfill 缓存机制提高效率。

3. **混合架构为 TTS 提供理想基础**  
   Falcon-H1 的 Transformer-Mamba 架构在长序列、大批量推理中展现出卓越吞吐量（见 Figure 8），使其成为 TTS 应用的理想 backbone。

4. **DeepConf + Falcon-H1R 实现精度与效率双赢**  
   动态剪枝机制充分发挥了 Falcon-H1R 良好 calibrated confidence 的优势，实现了 **更高准确率 + 更少 token 消耗**。

### 方法的局限性
- **知识密集型任务仍有差距**：在 GPQA-Diamond 和 MMLU-Pro 上未达最优，表明过度聚焦推理可能牺牲部分知识检索能力。
- **安全风险存在于 CoT 中**：虽然最终答案安全，但推理过程（CoT）中会短暂接触潜在有害内容（见 Table 10），部署时需谨慎暴露中间思维链。
- **依赖高质量奖励信号**：RL 阶段严重依赖可验证的 reward（如数学答案匹配、代码执行通过），难以扩展到开放域任务。

### 未来工作方向
- 探索更小规模模型（如 3B 或 1B）是否也能通过类似训练策略实现强推理能力。
- 改进混合架构设计，进一步压缩延迟和内存占用。
- 研究如何在保证安全性的同时，透明展示推理过程。
- 将该范式推广至更多非结构化推理任务（如自然语言理解、规划决策等）。

---

> 💡 **一句话总结**：Falcon-H1R 证明了通过 **混合架构 + 高质量数据 + 分阶段强化训练**，即使是 7B 规模的小模型也能在推理任务上媲美甚至超越数十亿参数的大模型，并在 Test-Time Scaling 场景中实现精度、速度与成本的全面领先。

</details>

---

### 6. [RadioDiff-Flux: Efficient Radio Map Construction via Generative Denoise Diffusion Model Trajectory Midpoint Reuse](https://arxiv.org/abs/2601.02790)

**Authors**: Xiucheng Wang, Peilin Zheng, Honggang Jia, Nan Cheng, Ruijin Sun, Conghao Zhou, Xuemin Shen  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.02790v1  

#### Abstract
Accurate radio map (RM) construction is essential to enabling environment-aware and adaptive wireless communication. However, in future 6G scenarios characterized by high-speed network entities and fast-changing environments, it is very challenging to meet real-time requirements. Although generative...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RadioDiff-Flux: Efficient Radio Map Construction via Generative Denoise Diffusion Model Trajectory Midpoint Reuse

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在6G无线通信场景中，**高动态环境**（如移动基站、无人机、车辆等）对实时、高效的 **Radio Map (RM)** 构建提出了严峻挑战。尽管生成式扩散模型（DMs）在RM构建中实现了接近射线追踪的精度，但其**迭代去噪机制导致推理延迟过高**（通常需数百至千步），难以满足低时延需求。此外，传统DM采用“零记忆”模式，无法利用相邻场景间的时空相关性，造成大量冗余计算。

### 提出的新方法与创新思路
本文提出 **RadioDiff-Flux**，一种基于**隐变量扩散过程中间态重用**（midpoint reuse）的两阶段高效RM构建框架，核心思想如下：

- **关键洞察**：通过分析发现，在语义相似的场景（如同一建筑布局下不同BS位置）中，扩散过程的**中间隐变量（latent midpoint）高度一致**，表明这些中间状态主要编码静态环境特征（如建筑结构、材质），而后期才细化动态细节（如BS位置、移动障碍物）。
  
- **理论支持**：基于KL散度分析证明，语义相似的RM在扩散过程中其隐变量分布差异随扩散步数增加呈二次衰减，为中间态重用提供了理论依据。

- **方法设计**：
  1. **第一阶段**：仅使用静态环境特征（如建筑布局）训练一个条件扩散模型，生成并缓存“**扩散中点**”（diffusion midpoint），该中点可跨相似场景复用。
  2. **第二阶段**：使用预训练的RadioDiff模型，从缓存的中点出发，结合动态信息（如BS位置、车辆）完成剩余去噪步骤，避免重复早期计算。

- **两种实现形式**：
  - **Vanilla Midpoint Reuse**：直接复用预计算中点，无需额外训练。
  - **RadioDiff-Flux**：显式解耦静态建模与动态精调，提升精度与效率。

### 相比现有方法的优势
| 方法类型 | 优势 |
|--------|------|
| **vs 物理驱动方法（如ray tracing）** | 避免指数级复杂度；支持快速适应环境变化，无需全局重算。 |
| **vs 传统判别模型（如RadioUNet）** | 能生成空间连贯的全局RM，克服局部回归局限。 |
| **vs GAN类方法（如RME-GAN）** | 更稳定训练，无模式崩溃问题，适合部署。 |
| **vs 标准扩散模型（如RadioDiff）** | 推理速度提升高达50倍以上，且精度损失极小（<0.15%）。 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **RadioMapSeer** 数据集，源自OpenStreetMap的真实城市地图（如Ankara、Berlin等），共700张256×256分辨率的地图。
- 每张图含约50–150栋建筑，80个发射机位置。
- 包括两类RM：
  - **Static RM (SRM)**：仅考虑建筑物影响。
  - **Dynamic RM (DRM)**：加入随机车辆作为动态障碍物。

### 实验设置
- **扩散步数**：$ T = 100 $（用于采样，远少于标准DM的500–1000步）
- **评估场景**（三类变化）：
  1. **Scenario 1**：改变BS位置（静态环境不变）
  2. **Scenario 2**：从静态环境过渡到含车辆的动态环境
  3. **Scenario 3**：直接修改静态环境（建筑布局 + BS位置）
- **重用比例**（Rreuse）：定义为使用旧条件进行去噪的前段步数占比，测试范围为[0.1, 0.98]

### 评估指标
| 指标 | 描述 |
|------|------|
| **NMSE / RMSE** | 归一化均方误差 / 均方根误差，衡量整体误差 |
| **SSIM** | 结构相似性，反映高频细节保留能力 |
| **PSNR (dB)** | 峰值信噪比，评估信号保真度 |
| **Time (ms)** | 单次推理耗时，体现效率 |
| **Speedup** | 相对于完整扩散流程的加速比 |

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **RadioUNet** | 判别式CNN | U-Net架构，直接回归RM |
| **UVM-Net** | 序列建模 | 使用State Space Model捕捉长距离依赖 |
| **RME-GAN** | 生成对抗网络 | 条件GAN，存在训练不稳定性 |
| **RadioDiff** | 扩散模型（SOTA） | 当前最优方法，作为主基准与消融对照 |
| **Vanilla Midpoint Reuse (Ours)** | 中间态复用 | 复用RadioDiff中点，无再训练 |
| **RadioDiff-Flux (Ours)** | 两阶段框架 | 显式分离静态/动态建模 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### Scenario 1: 改变BS位置
| 方法 | Rreuse | NMSE | Speedup |
|------|--------|-------|---------|
| RadioDiff (baseline) | 0.00 | 0.00580 | 1× |
| Vanilla Reuse | 0.70 | 0.00671 | **3.47×** |
| Vanilla Reuse | 0.98 | 0.13098 | **50×** |
| **RadioDiff-Flux** | **0.98** | **0.02957** | **~20×** |

> ✅ 在Rreuse=0.7时，NMSE仅上升15%，速度提升3.5倍；  
> ✅ RadioDiff-Flux显著缓解高重用下的“模糊叠加”效应，保持更高保真度。

#### Scenario 2: 静态→动态环境转换
| 方法 | Rreuse | NMSE | Speedup |
|------|--------|-------|---------|
| RadioDiff | 0.00 | 0.00643 | 1× |
| Vanilla Reuse | 0.98 | **0.00776** | **60×** |

> ✅ 即使在极高重用率下，NMSE仍低于0.008，SSIM保持>0.95，说明框架对动态扰动鲁棒性强。

#### Scenario 3: 修改静态环境
| 方法 | Rreuse | NMSE | Speedup |
|------|--------|-------|---------|
| RadioDiff | 0.00 | 0.00680 | 1× |
| Vanilla Reuse | 0.70 | 0.00889 | **3.43×** |
| Vanilla Reuse | 0.98 | 0.58418 | **54.5×**（但严重失真） |

> ⚠️ 当静态环境发生根本性变化时，高Rreuse导致“惯性偏差”，模型难以纠正初始隐状态的影响。

### 与基线方法对比（以Scenario 1为例）
| 方法 | NMSE | Time (ms) | Speed vs RadioDiff |
|------|-------|------------|------------------|
| RadioUNet | 0.00740 | 60 | ~10× faster, but worse accuracy |
| RadioDiff | 0.00580 | 600 | baseline |
| **RadioDiff-Flux (Rreuse=0.7)** | **0.00607** | **~170** | **~3.5× faster**, <5% accuracy drop |

> ✅ RadioDiff-Flux在精度几乎不变的前提下，实现数量级加速。

### 消融实验结果
- **RadioDiff-Flux vs Vanilla Reuse**：在高Rreuse（如0.98）下，前者将NMSE从0.13降至0.03，验证了解耦设计的有效性。
- **缓存开销**：每个latent midpoint大小为64KB（float32），城市级服务最多缓存100个，总内存约6.25MB，资源友好。
- **训练成本**：仅需微调第一阶段模型约10个epoch（12小时），相比RadioDiff全训480小时大幅降低。

---

## 4. 关键结论和发现

### 主要发现
1. **扩散中点具有强语义一致性**：在同一静态环境下不同配置生成的RM，其扩散轨迹在中期高度收敛，支持中间态重用。
2. **中间态重用可极大提升效率**：通过跳过冗余早期去噪，推理时间可减少**3.5× 至 60×**，尤其适用于小幅度动态变化（如BS微移、车辆出现）。
3. **两阶段解耦优于简单复用**：RadioDiff-Flux通过专门训练静态编码器，提升了中点泛化能力，有效抑制高重用下的误差累积。
4. **适用场景明确**：方法在**高语义相似性场景**中表现最佳；当环境结构性改变时，应降低Rreuse或重新生成。

### 方法的局限性
- **依赖环境相似性判断**：当前通过cross-attention空间的Frobenius距离判断是否可复用，虽有效但仍为启发式策略。
- **不适用于剧烈环境变更**：若建筑布局发生重大调整，缓存中点不再适用，需重新计算。
- **未处理时间连续性建模**：虽然减少了帧间抖动，但尚未显式建模RM序列的时间一致性。

### 未来工作方向
1. **自适应Rreuse策略**：开发轻量级机制自动量化环境变化程度，并动态选择最优Rreuse。
2. **增强时序一致性**：扩展至视频级扩散模型，支持连续RM流生成，提升动态系统平滑性。
3. **多BS联合建模**：将多个BS位置作为统一条件输入，直接生成复合RM，进一步提升效率。
4. **边缘部署优化**：探索量化、蒸馏等技术，推动RadioDiff-Flux在移动端或无人机上的实时应用。

---

> 📌 **总结一句话**：  
> **RadioDiff-Flux通过揭示并利用扩散过程中间态的语义稳定性，首次实现了生成式RM模型的“记忆化推理”，在保证高保真度的同时达成最高50倍以上的推理加速，为6G动态无线环境中的实时感知与自适应通信提供了可行路径。**

</details>

---

### 7. [MixTTE: Multi-Level Mixture-of-Experts for Scalable and Adaptive Travel Time Estimation](https://arxiv.org/abs/2601.02943)

**Authors**: Wenzhao Jiang, Jindong Han, Ruiqian Han, Hao Liu  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.02943v1  

#### Abstract
Accurate Travel Time Estimation (TTE) is critical for ride-hailing platforms, where errors directly impact user experience and operational efficiency. While existing production systems excel at holistic route-level dependency modeling, they struggle to capture city-scale traffic dynamics and long-ta...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MixTTE: Multi-Level Mixture-of-Experts for Scalable and Adaptive Travel Time Estimation 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前工业级 **Travel Time Estimation (TTE)** 系统（如 DiDi 的 WDR 架构）虽然在常见场景下表现良好，但仍存在两大瓶颈：
- **有限的感受野（Limited reception field）**：仅建模路线内部的依赖关系，无法捕捉城市尺度的全局交通动态（如周边道路拥堵传播）。
- **长尾场景性能差（Long-tail underperformance）**：对罕见但关键的交通模式（如大型活动、施工区）预测不准。

此外，将 link-level 模型集成到 route-centric 系统面临三大挑战：
1. **可扩展性**：百万级路网上的全局时空依赖建模效率低；
2. **异质性**：多样化的交通模式难以统一建模；
3. **动态适应性**：频繁更新模型成本高且易过拟合。

---

### 提出的新方法与创新思路
作者提出 **MIxTTE**，一个可扩展、自适应的多层级 TTE 框架，其核心创新如下：

#### （1）Spatio-Temporal External Attention (STEA)
- 引入一组小型 **external memory units** 来中介大规模路网中各 link 之间的交互。
- 利用 **cross-attention** 机制实现外部知识检索（EKR），避免传统 pairwise 建模的 $O(N^2)$ 复杂度，达到 $O(N \cdot U_{\text{ex}})$ 的线性复杂度。
- 支持跨时间和空间的长期依赖建模，并结合 **hierarchical modeling** 加强同时间步内的全局相关性。

#### （2）Externally Stabilized Graph Mixture-of-Experts (ESGMoE)
- 设计图结构的 MoE 层，包含多种异构专家（graph experts + zero-computation experts）。
- **Hierarchical routing with external guidance**：利用 STEA 提供的上下文增强路由决策稳定性，尤其在不确定性高的情况下更依赖外部知识。
- 引入三种 **zero-computation experts**（Identity、Constant、Null）以摊销高频简单模式的计算开销，释放 graph experts 给长尾场景使用。

#### （3）Asynchronous Incremental Learning (ASIL)
- 动态检测分布偏移（通过 Mahalanobis Distance 衡量周期性变化）来触发增量学习。
- **异步更新机制**：仅当异常 link 比例超过阈值时才更新 link-level 参数；更高阈值才同时更新 route-level 参数。
- 使用冻结的历史模型生成表征用于漂移检测，保证 latent space 一致性。

---

### 相比现有方法的优势
| 维度 | MIxTTE 优势 |
|------|------------|
| **准确性** | 显著优于所有 baseline，尤其在长尾场景提升明显 |
| **可扩展性** | STEA 实现线性复杂度，适用于百万级路网 |
| **推理效率** | ESGMoE 实现稀疏激活，zero-computation 专家降低平均计算负担 |
| **适应性** | ASIL 支持高频、稳定、低成本的在线更新 |
| **部署兼容性** | 无需重构现有系统，支持插件式集成 |

---

## 2. 核心实验方法和设置

### 数据集
在三个中国大城市的真实滴滴出行数据集上进行实验：

| 城市 | 时间跨度 | Trip 数量 | 平均行程时长 | 路段数（Links） | 热门路段数（Hot Links） |
|------|----------|-----------|----------------|----------------|------------------------|
| 北京 | 2024-07 ~ 2024-09 | 22.28M | 18.11 min | 2,315,360 | 156,731 |
| 南京 | 2024-12 ~ 2025-02 | 7.95M | 13.79 min | 696,107 | 83,323 |
| 苏州 | 2025-01 ~ 2025-04 | 16.78M | 13.63 min | 1,419,074 | 149,171 |

> 注：只对“热门路段”生成 link embedding，减少噪声和计算开销。

---

### 实验设置
- **训练方式**：
  - 全量重训（Full Retraining）：使用前110天数据训练，最后7天测试。
  - 增量学习（Incremental Learning, IL）：每小时用过去1小时完成的 trip 训练，预测下一小时出发的 trip。
- **批采样策略**：time-specific batching，同一 batch 内 trip 起始时间相同，提高训练效率并匹配线上推理逻辑。
- **部署架构**：
  - link-centric 模型部署于 GPU，每5分钟更新一次并缓存至 Redis；
  - route-centric 模型部署于 C++ CPU 服务器，实时查询最新 link 表征。

---

### 评估指标
| 指标 | 定义 |
|------|------|
| **MAE** | 预测与真实旅行时间的平均绝对误差（秒） |
| **MAPE** | 平均绝对百分比误差 |
| **BCR (Bad Case Ratio)** | MAE > 300 秒且 MAPE > 20% 的样本占比 |

---

### 基线方法对比
#### 全量训练 Setting
- **Rule-based**: RouteETA（基于历史平均）
- **Route-centric**: HierETA, WDR（DiDi 当前生产模型）
- **Link-centric**: CompactETA, ConSTGAT, BigST

#### 增量学习 Setting
- **iETA** [13]：DiDi 现有的增量学习框架，作为主要对比基线。

---

## 3. 主要实验结果和性能指标

### 整体性能（Table 2）
在三个城市上，MIxTTE 在 **增量学习 setting 下全面领先**：

| 指标 | 最佳提升（vs iETA） |
|------|--------------------|
| **MAE** | ↓ 2.39% |
| **MAPE** | ↓ 3.70% |
| **BCR** | ↓ 10.32% |

> 特别是在 **BCR 上大幅下降**，说明对极端错误案例有显著改善。

---

### 长尾场景性能分析（RQ2）
- 将测试样本按以下三个维度分组（五等分）：
  1. Trip Duration (TD)
  2. Condition Deviation Degree (CDD)
  3. Non-Recurrence Degree (NRD)

- **发现**：随着从头部到尾部样本，MIxTTE 相对于当前 TTE 模型的 **MAE 改善幅度持续上升**（见 Figure 3），验证了其在长尾场景下的泛化能力更强。

---

### 效率分析（RQ3）
#### 全量训练效率（Table 3）
| 模型 | 北京吞吐量 (K/min) | 北京推理延迟 (ms) |
|------|---------------------|-------------------|
| CompactETA | 14.96 | 541.27 |
| ConSTGAT | 10.51 | 704.11 |
| BigST | 22.45 | 78.30 |
| **MIxTTE-WoIL** | **21.17** | **88.97** |

> MIxTTE 在保持高吞吐的同时，推理延迟远低于 GAT 类模型。

#### 增量学习效率（Table 4）
| 模型 | 平均训练时间 (sec/step) | 可训练参数 (K/step) |
|------|-------------------------|----------------------|
| MIxTTE-WoPU（无选择性更新） | 36.63 | 525.66 |
| **MIxTTE（ASIL）** | **7.81** | **47.49** |

> ASIL 策略使训练时间和参数量分别减少约 **80% 和 91%**，极大提升了更新效率。

---

### 消融实验（Ablation Study, Figure 4）
消融模块包括：
- `-WoEA`：移除 STEA 和 hierarchical routing
- `-WoHR`：仅移除 hierarchical routing
- `-WoMoE`：移除 ESGMoE 层
- `-WoZE`：移除 zero-computation experts
- `-WoPU`：移除参数选择性更新（即禁用 ASIL）

#### 发现：
- **STEA 至关重要**：`-WoEA` 性能最差，证明全局上下文建模有效。
- **Hierarchical routing 提升稳定性**：`-WoHR` 在苏州数据集上下降更明显（因网络更复杂）。
- **Zero-computation experts 对 BCR 影响大**：尤其在南京，移除后 BCR 显著上升，表明其对长尾场景的重要性。
- **ASIL 提升 IL 稳定性**：`-WoPU` 导致性能下降，说明异步更新有助于防止过拟合。

---

## 4. 关键结论和发现

### 主要发现
1. **link-level modeling 可有效补充 route-centric 系统**，尤其是在捕捉全局交通动态和长尾场景方面。
2. **STEA 实现高效全局建模**，突破了传统 GNN 或 attention 的二次复杂度限制。
3. **ESGMoE 结合 zero-computation experts 能有效处理异质性和长尾分布**，并通过外部引导提升路由稳定性。
4. **ASIL 实现高频率、低成本、稳定的在线更新**，解决了 link-level 模型频繁更新带来的计算与稳定性问题。
5. **MIxTTE 已成功部署于 DiDi 生产环境**，带来显著的服务质量提升（MAE ↓3.03%，BCR ↓4.41%）。

---

### 方法的局限性
- **依赖高质量“热门路段”定义**：非热点区域的 link 表征可能不够准确。
- **external memory units 需调参**：memory size 和初始化影响收敛速度。
- **zero-computation experts 的设计仍较启发式**，缺乏理论最优性保障。
- **目前仅用于 TTE**，尚未验证是否可推广至其他时空预测任务。

---

### 未来工作方向（Section C）
1. **引入多模态数据**：融合天气、事件等外部信号，缓解城市感知不完全问题。
2. **跨城迁移学习**：利用 external memory 和 MoE 抽象通用交通知识，降低新城市部署成本。
3. **轻量化蒸馏**：构建大规模基础模型后，探索模型压缩技术以便边缘部署。

---

> ✅ **总结一句话**：  
> MIxTTE 通过 **STEA + ESGMoE + ASIL** 三重创新，在不改变现有生产架构的前提下，实现了对百万级路网的高效、精准、自适应旅行时间估计，已在 DiDi 成功落地并取得显著业务收益。

</details>

---

### 8. [ModeX: Evaluator-Free Best-of-N Selection for Open-Ended Generation](https://arxiv.org/abs/2601.02535)

**Authors**: Hyeong Kyu Choi, Sharon Li  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.02535v1  

#### Abstract
Selecting a single high-quality output from multiple stochastic generations remains a fundamental challenge for large language models (LLMs), particularly in open-ended tasks where no canonical answer exists. While Best-of-N and self-consistency methods show that aggregating multiple generations can...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**ModeX: Evaluator-Free Best-of-N Selection for Open-Ended Generation**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在开放生成任务（open-ended generation）中，大语言模型（LLMs）的输出具有高度随机性，单次生成路径（single-path generation）容易因早期错误导致幻觉或推理失败。虽然 **Best-of-N** 和 **self-consistency** 方法通过多路径采样提升了性能，但它们通常依赖以下两种机制：
- 外部评估器（如 reward model 或 LLM judge）
- 精确字符串匹配投票（exact string-match voting）

这些限制使得现有方法难以应用于**无标准答案的开放文本生成任务**（如摘要、代码生成、数学推理），且计算开销大。

---

### ✅ 提出的新方法：ModeX 与 ModeX-Lite

#### **ModeX（Mode Extraction）**
- **核心思想**：将“多数投票”推广到开放文本空间，提出“模态输出”（modal output）概念——即代表多个生成结果之间**语义共识**的中心样本。
- **实现方式**：
  1. 构建候选生成之间的**加权相似度图**（基于 n-gram Jaccard 相似度）
  2. 使用**谱聚类**（spectral clustering）递归划分图，识别主导语义簇
  3. 在最终簇中选择**加权度最高节点**作为“模态输出”

> 不需要外部评估器、不依赖 exact match，完全基于生成文本间的结构关系进行选择。

#### **ModeX-Lite**
- **动机**：提升效率，在生成过程中尽早剪枝非代表性路径。
- **改进策略**：
  - 在每 `T` 步生成后执行一次谱聚类
  - 保留最代表性的子集，丢弃离群路径
  - 最终从剩余路径中选出质心输出
- 显著降低延迟，适用于实际部署。

---

### ✅ 相比现有方法的优势

| 特性 | ModeX / ModeX-Lite | Best-of-N | Self-Consistency | LLM Judge |
|------|---------------------|-----------|------------------|-----------|
| 是否需要外部评估器 | ❌ 否（evaluator-free） | ✅ 是 | ❌ 否（但需 exact match） | ✅ 是 |
| 支持开放生成任务 | ✅ 是 | ⚠️ 仅限闭合任务 | ❌ 需固定答案空间 | ✅ 是 |
| 计算效率 | 高（并行生成 + 图算法 O(N²)） | 高（但需 reward model 推理） | 低（串行 refine） | 低（额外 LLM 判断） |
| 可扩展性（随 N 增加） | 强（性能稳定上升） | 依赖 reward model 质量 | 弱（无法处理语义等价变体） | 中等 |

> **核心优势**：首次实现了无需外部评估器、可泛化至任意开放生成任务的高效 Best-of-N 选择框架。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 任务 | 数据集 | 描述 |
|------|--------|------|
| **Text Summarization** | CNN/DailyMail | 新闻文章摘要任务，测试抽象生成能力 |
| **Code Generation** | HumanEval | 包含 164 个 Python 编程题，带单元测试 |
| **Mathematical Reasoning** | Math-500 | 500 道数学题，涵盖代数、几何、概率等领域 |

---

### ⚙️ 实验设置

- **模型**：
  - Qwen2.5-7b-instruct
  - Llama3.1-8b-instruct（CodeLlama-7b-Instruct 用于代码任务）
- **生成参数**：
  - 温度 > 0（引入多样性）
  - Top-p 采样
  - N ∈ {4, 8, 16} 条生成路径
- **相似度计算**：
  - 加权 Jaccard 相似度（unigram + bigram + trigram）
- **谱聚类终止条件**：
  - Conductance < T（默认 T=0.8）

---

### 📊 评估指标

| 任务 | 主要指标 |
|------|---------|
| 文本摘要 | ROUGE-1, ROUGE-2, ROUGE-L, BLEU |
| 代码生成 | Pass@1（功能正确率） |
| 数学推理 | Accuracy（最终答案是否正确） |

---

### 🔁 基线方法对比

| 基线方法 | 简介 |
|--------|------|
| **Single Path** | 单路径生成，平均 16 次运行结果 |
| **Self-Refine** | 迭代自我修正 4 轮 |
| **LLM Judge (N=4/16)** | 使用另一个 LLM 从 N 个候选中选最优 |
| **Perplexity BoN** | 选困惑度最低的输出 |
| **Self-Certainty BoN** | 选负对数似然最小的输出 |
| **Best-of-N (Gold Standard)** | 使用 reward model 打分选择最佳（有监督上限） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（以 Qwen 模型为例）

| 方法 | Text Summarization (ROUGE-L) | Code Generation (Pass@1) | Math Reasoning (Acc) |
|------|-------------------------------|--------------------------|-----------------------|
| Single Path | 20.17 | 69.89% | 70.98% |
| LLM Judge (N=16) | 19.72 | 65.24% | 74.67% |
| Perplexity BoN | 21.06 | 73.17% | 78.00% |
| Self-Certainty BoN | 19.32 | 55.49% | 67.00% |
| **ModeX (N=16)** | **21.06** | **75.61%** | **78.00%** |
| **ModeX-Lite (N=16)** | **21.89** | **78.66%** | **75.33%** |

> ✅ **ModeX-Lite 在三项任务上均显著优于所有 evaluator-free 和部分 evaluator-based 方法**

---

### 🔍 与基线方法的对比结果

- **超越 LLM Judge**：
  - 在代码生成任务中，ModeX-Lite (78.66%) > LLM Judge (65.24%)
  - 表明“结构感知选择”优于“元模型打分”
- **接近甚至超越 Gold Standard Best-of-N**：
  - 在数学推理任务中，ModeX-Lite 达到 75.33%，接近 Llama 上的 Best-of-16 (63.00%)，且无需 reward model
- **优于 Self-Certainty**：
  - Self-Certainty 在多项任务上表现不佳（如代码生成仅 55.49%），说明低不确定性 ≠ 高质量输出

---

### 🔧 消融实验与敏感性分析

#### （1）相似度函数比较（Appendix E）

| 方法 | ROUGE-L | Pass@1 | Accuracy |
|------|--------|--------|---------|
| ModeX-cosine (embedding) | 20.26 | 75.00% | 71.33% |
| **ModeX-n-gram (Jaccard)** | **21.06** | **75.61%** | **78.00%** |

> n-gram Jaccard 明显优于 embedding cosine，说明局部词重叠更利于捕捉语义一致性。

#### （2）超参数敏感性（Figure 5）

- **Conductance 阈值 T ∈ [0.5, 0.9]**：性能稳健，T=0.8 效果最好
- **Pruning Frequency T ∈ [100,500]**：即使稀疏剪枝也能保持增益
- 所有配置下，ModeX-Lite 均显著优于 Single Path

#### （3）计算复杂度与延迟（Table 2）

| 方法 | 复杂度 | 实测延迟（Qwen, CNN/DM） |
|------|--------|--------------------------|
| Single Path | O(L) | 5.5s |
| Self-Refine | O(kL) | 31.7s |
| LLM Judge | O(NL + NL_judge) | 10.7s |
| **ModeX-Lite (N=4)** | O(NL + N²) | **7.2s** |
| **ModeX-Lite (N=16)** | O(NL + N²) | **9.1s** |

> ModeX-Lite 比 Self-Refine 快 **3.5×**，比 LLM Judge 更快且无需额外模型。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **“模态输出”是高质量生成的有效代理**  
   - 高质量生成倾向于形成语义一致的簇，而错误/幻觉往往是孤立点。
   - 通过谱聚类识别主导簇，并取其质心，能有效逼近“真实模式”。

2. **无需外部评估器也可实现高性能 Best-of-N**  
   - ModeX 是首个真正实现 **evaluator-free** 的开放生成 Best-of-N 方法。
   - 性能媲美甚至超过依赖 reward model 的方法。

3. **结构感知选择优于简单打分机制**  
   - Perplexity 和 Self-Certainty 并不能可靠反映输出质量。
   - 基于图结构的选择更能捕捉语义共识。

4. **早期剪枝可行且高效**  
   - ModeX-Lite 证明可在生成中途识别并剪枝劣质路径，大幅节省资源。

---

### ⚠️ 局限性

1. **依赖词法相似度（Jaccard）**  
   - 对同义改写、句式变换敏感，可能误判语义相同但表面不同的高质量输出。
   - 未来可探索 embedding-based 或语义解析树相似度。

2. **假设“众数即正确”**  
   - 若模型存在系统性偏见或模式坍塌（mode collapse），可能会强化错误共识。
   - 该问题是 LLM 自身缺陷，非 ModeX 特有。

3. **环境成本增加**  
   - 多路径生成带来更高能耗，虽效率高但仍高于单路径。

---

### 🔮 未来工作方向

- 探索更鲁棒的语义相似度度量（如 SBERT、Tree Edit Distance）
- 结合轻量级 verifier 进行 hybrid selection
- 将 ModeX 应用于对话系统、创意写作等更复杂场景
- 动态调整剪枝频率与阈值，进一步优化效率

---

## ✅ 总结

**ModeX** 提出了一种全新的、无需外部评估器的 Best-of-N 选择范式，通过构建生成文本的相似度图并应用谱聚类来提取“模态输出”，成功将多数投票思想推广到开放生成领域。其轻量版 **ModeX-Lite** 进一步实现了高效剪枝，在文本摘要、代码生成和数学推理三大任务上全面超越主流基线，兼具高性能与低延迟，为 LLM 的鲁棒生成提供了实用且理论坚实的解决方案。

> 🔗 开源地址：[https://github.com/deeplearning-wisc/ModeX](https://github.com/deeplearning-wisc/ModeX)

</details>

---

### 9. [Punctuation-aware Hybrid Trainable Sparse Attention for Large Language Models](https://arxiv.org/abs/2601.02819)

**Authors**: Junxiang Qiu, Shuo Wang, Zhengsu Chen, Hengheng Zhang, Jinda Lu, Changcheng Li, Qi Tian  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.02819v1  

#### Abstract
Attention serves as the fundamental mechanism for long-context modeling in large language models (LLMs), yet dense attention becomes structurally prohibitive for long sequences due to its quadratic complexity. Consequently, sparse attention has received increasing attention as a scalable alternative...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Punctuation-aware Hybrid Trainable Sparse Attention for Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **sparse attention** 方法在长序列建模中面临两个关键挑战：
1. **语义边界模糊**：在 block selection 阶段通常采用平均池化（average pooling）聚合连续 token，导致块内细粒度语义边界被模糊，关键实体或逻辑枢纽可能被无关 token 稀释，造成信息丢失。
2. **极端稀疏下的性能退化**：当激活 token 数量极低时（如 sparsity ratio > 95%），模型表现显著下降，限制了其在资源受限设备上的高效部署潜力。

### 提出的新方法：PHSA（Punctuation-aware Hybrid Sparse Attention）
作者提出了一种**原生可训练的稀疏注意力框架**——**PHSA**，其核心思想是利用标点符号作为自然的语义边界锚点，提升稀疏选择的准确性。

#### 创新点：
- ✅ **双分支聚合机制（Dual-branch Aggregation Mechanism）**  
  每个 key block 构造一个代表向量，融合两种语义表示：
  - **Global Semantic Representation**：对 block 内所有 token 进行平均池化，捕捉整体语义。
  - **Punctuation-aware Semantic Representation**：仅对标点 token（如逗号、句号）进行池化，保留语义转折点信息。
  - 最终通过门控参数 $\lambda$ 加权融合：  
    $$
    M(B_t) = \lambda \cdot M_o(B_t) + (1-\lambda) \cdot M_p(B_t)
    $$

- ✅ **极端稀疏自适应训练与推理策略（Extreme-sparsity-adaptive Training & Inference）**  
  设计了针对极低 Top-K 设置的稳定训练方案，使模型能在极小激活 token 比例下仍保持良好性能，支持轻量化部署。

- ✅ **无额外计算开销**  
  利用已有标点 token，无需引入额外模块或参数，几乎不增加计算负担。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **语义精度** | 利用标点作为边界提示，避免粗粒度聚合带来的语义稀释 |
| **信息保留能力** | 在高稀疏度下显著减少关键信息丢失 |
| **训练稳定性** | 支持更低的 Top-K 值训练，突破传统稀疏注意力的性能瓶颈 |
| **通用性** | 可应用于训练型与非训练型（training-free）场景 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据集**：
  - 开源数据：`dclm`, `mapcc`, `ultrachat`, `tulu-v3`, `finemath`, `megamath`
  - 自研高质量教育领域私有数据（未公开）
- **评估基准**：
  - **General Benchmarks**（15项任务）：
    - 数学推理：GSM8K, MathQA, MATH
    - 常识问答：ARC-C, ARC-C-ZH, ARC-E
    - 综合认知：MMLU, CMMLU, BBH
    - 文本连贯性：LAMBADA, XStoryCloze
    - 编程生成：HumanEval
    - 领域任务：C-Valid, HellaSwag, PiPA
  - **长上下文专项评测**：
    - **LongBench**：涵盖单轮问答、少样本学习、摘要、代码等多任务
    - **Needle-in-a-Haystack (NIAH)**：用于定量测量稀疏注意力下的信息保留能力

### 实验设置
- **模型规模**：Qwen3-0.6B 和 Qwen3-8B
- **序列长度**：4k 和 32k tokens
- **Block Size**：16 tokens/block
- **局部窗口与初始块**：
  - 4k：init=16, window=128
  - 32k：init=128, window=512
- **训练方式**：
  - 训练阶段从 Qwen3-0.6B-Base 初始化
  - 分别测试 **训练一致（training-inference consistent）** 与 **训练自由（training-free）** 场景

### 评估指标
| 指标 | 用途 |
|------|------|
| **NIAH Score** | 衡量长距离信息检索能力，越高表示信息损失越少 |
| **Average Score on General Benchmarks** | 综合性能评估 |
| **LongBench Subtask & Avg Scores** | 长文本理解能力评估 |
| **Sparsity Ratio / Activated Token Count** | 效率指标，反映计算压缩程度 |

### 对比的基线方法
- **Dense Attention**：全连接注意力，O($L^2$) 复杂度
- **InfLLM v2**（Zhao et al., 2025）：当前最先进的可训练稀疏注意力方法
- **NSA**（Yuan et al., 2025）：DeepSeek 提出的动态分层稀疏策略

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 📊 在 NIAH 任务上（32k 序列，0.6B 模型）
| 方法 | Sparsity Ratio | 信息损失降低幅度 |
|------|----------------|------------------|
| **PHSA** | **97.3%** | **相对减少 10.8% 信息损失** |

> 注：相比 InfLLM v2，在同等极端稀疏条件下，PHSA 显著提升了信息保留能力。

#### 📈 在 General Benchmarks 上的表现（Qwen3-0.6B，100B tokens 训练）

| 方法 | Inference Top-K | Average Score |
|------|------------------|---------------|
| Dense Attention | N/A | 48.96 |
| InfLLM v2 | 4 | 49.11 |
| **PHSA** | **4** | **49.24** ✅（最优） |

👉 **结论**：PHSA 不仅优于 InfLLM v2，甚至超过了 dense attention 的平均表现。

#### 🔍 在 LongBench 上的整体性能

| 方法 | Average Score |
|------|---------------|
| Dense Attention | 27.86 |
| InfLLM v2 | 27.40 |
| **PHSA** | **28.13** ✅ |
| **PHSA_en+zh**（加入中文标点） | **28.15** ✅✅ |

👉 在多数子任务中均取得领先，尤其在代码相关任务（LCC, RBP）上有明显提升。

#### ⚙️ 消融实验与关键发现（RQ1-RQ4）

| 研究问题 | 发现 |
|--------|------|
| **RQ1: 为何偏好更低 Top-K？** | 训练时使用较低 Top-K（如 2 或 4）有助于在推理阶段维持低激活 token 下的性能；但 Top-K=1 导致崩溃，Top-K>4 性能下降 → **Top-K=2 是局部最优解** |
| **RQ2: 是否提升通用性能？** | 是。PHSA 在 20B 和 100B 训练步数下均优于 InfLLM v2，并接近甚至超越 dense attention |
| **RQ3: 是否适用于长上下文？** | 是。在 16k 和 32k 序列的 NIAH 测试中，PHSA 持续优于 InfLLM v2，且训练后增益更明显（见 Figure 3） |
| **RQ4: 跨语言影响？** | 中文任务（arc_c_zh, MQZ）表现略差 → 归因于训练时仅使用英文标点；引入中文标点后（PHSA_en+zh），MQZ 从 20.63 提升至 22.22，验证了标点语言适配的重要性 |

---

## 4. 关键结论和发现

### 主要结论
1. ✅ **标点符号是有效的语义边界指示器**：将其作为稀疏注意力中的“锚点”，能有效缓解 block aggregation 中的信息稀释问题。
2. ✅ **PHSA 在多种场景下全面优于 baseline**：
   - 在 general benchmarks 上达到甚至超过 dense attention 水平；
   - 在 long-context 任务中显著优于 InfLLM v2；
   - 在极端稀疏设置下表现出更强鲁棒性。
3. ✅ **支持轻量化部署**：通过极端稀疏自适应训练，可在极低 token 激活比例下稳定运行，适合边缘设备应用。

### 局限性
- ❗ **标点噪声问题**：并非所有标点都具有强语义边界意义（如引号、括号），全量引入可能带来噪声。
- ❗ **语言依赖性**：当前方法对标点的语言覆盖有限，跨语言泛化需进一步优化（已通过 PHSA_en+zh 初步验证）。
- ❗ **静态 block 划分**：固定大小的 block 可能无法匹配真实语义单元长度。

### 未来工作方向
- 探索 **筛选更有意义的标点子集**（如句号、分号优先），以提升信噪比；
- 扩展至更多语言，构建 **多语言标点感知机制**；
- 结合动态 block segmentation，实现更灵活的语义切分；
- 将 PHSA 集成到更大规模模型（如 70B+）中验证可扩展性。

---

> **总结一句话**：  
> PHSA 通过巧妙利用 **punctuation tokens 作为语义边界锚点**，设计了一种高效、可训练、低损耗的稀疏注意力机制，在保持极低计算复杂度的同时，实现了优于 dense attention 和 SOTA sparse 方法的综合性能，为 LLM 的长上下文处理与轻量化部署提供了新范式。

</details>

---

### 10. [MedDialogRubrics: A Comprehensive Benchmark and Evaluation Framework for Multi-turn Medical Consultations in Large Language Models](https://arxiv.org/abs/2601.03023)

**Authors**: Lecheng Gong, Weimin Fang, Ting Yang, Dongjie Tao, Chunxiao Guo, Peng Wei, Bo Xie, Jinqun Guan, Zixiao Chen, Fang Shi, Jinjie Gu, Junwei Liu  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.03023v1  

#### Abstract
Medical conversational AI (AI) plays a pivotal role in the development of safer and more effective medical dialogue systems. However, existing benchmarks and evaluation frameworks for assessing the information-gathering and diagnostic reasoning abilities of medical large language models (LLMs) have ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MedDialogRubrics: A Comprehensive Benchmark and Evaluation Framework for Multi-turn Medical Consultations in Large Language Models  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前医学大语言模型（LLMs）的评估多集中于静态任务（如多项选择题、摘要生成），缺乏对**多轮诊断对话能力**的系统性评估框架。具体存在三大挑战：
- **数据稀缺与隐私问题**：真实医疗对话数据受 HIPAA/GDPR 等法规限制，难以大规模获取。
- **模拟可靠性差**：基于 LLM 的患者代理（Patient Agent）易产生幻觉（hallucinations），导致对话不一致或违背医学逻辑。
- **评估主观性强**：传统人工标注成本高，且缺乏标准化“必须提问”（must-ask）标准。

### 🚀 提出的新方法与创新
作者提出 **MedDialogRubrics**，一个全面的多轮医学咨询评估框架，其核心创新包括：

| 创新点 | 描述 |
|--------|------|
| **合成患者案例生成** | 基于权威医学知识库（如 UpToDate、NICE 指南）构建 5,200 个合成病例，无需访问真实电子健康记录（EHR），保障隐私。 |
| **抗幻觉患者代理设计** | 设计基于“原子医学事实”（atomic medical facts）的 Patient Agent，并引入动态引导机制（Guidance Injection Loop），实时检测并纠正幻觉，确保对话内部一致性与临床合理性。 |
| **EBM 驱动的细粒度评分标准（Rubrics）** | 构建超过 60,000 条基于循证医学（Evidence-Based Medicine, EBM）指南的“关键询问项”（key inquiry points），通过拒绝采样（reject sampling）筛选出“must-ask”问题作为评分依据。 |
| **专家-LLM 协同的评分体系** | 结合 LLM 自动生成 rubrics 并由临床专家进行三重独立评审（three-expert panel），提升评分标准的专业性和可信度。 |

### 🔍 相比现有方法的优势

| 对比维度 | 现有基准（如 MedQA, HealthBench） | MedDialogRubrics |
|---------|-------------------------------|------------------|
| 多轮支持 | 有限或间接支持 | 显式支持多轮交互 |
| 评分标准颗粒度 | 较粗（如最终诊断正确性） | 细粒度（每一轮是否问到关键点） |
| 患者模拟真实性 | 易出现幻觉 | 抗幻觉机制显著降低错误率 |
| 数据来源 | 多依赖真实数据或小规模合成 | 完全合成、可复现、无隐私风险 |
| 专家参与程度 | 少量验证 | 全流程专家审核与反馈闭环 |

> 💡 **优势总结**：MedDialogRubrics 是首个将**高质量合成数据、可控患者模拟、EBM 驱动评分、自动化评估**整合于一体的端到端医学对话评估框架。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **合成患者案例数量**：5,200 例
- **覆盖疾病范围**：常见初级诊疗病症、慢性病、精神健康问题、急性紧急情况
- **数据生成方式**：
  - 使用多智能体系统（multi-agent system）从公开医学百科（如 Baidu Health Encyclopedia）提取知识；
  - 分阶段生成：疾病概要 → 基本信息 → 实验室报告 → 主诉（chief complaint）；
  - 所有案例均经过自动校验器检查时间线矛盾、人口统计学错配等问题。

### ⚙️ 实验设置
- **Doctor Agent 模型**（被评测对象）：
  - 开源模型：`Qwen3-235B-A22B-Instruct-2507`, `DeepSeek-R1`
  - 商用模型：`GPT-5`, `Gemini-2.5-Pro`
- **Patient Agent**：固定为基于原子事实 + 动态引导机制的控制型代理
- **对话轮次上限**：最多 12 轮
- **终止信号**：模型输出 `"End Inquiry"` 表示结束问诊

### 🧪 评估指标
- **核心指标**：**Rubric Match Precision**（匹配预定义“must-ask”问题的比例）
- **综合得分计算公式**：
  $$
  S_k = \frac{\sum_{r_i \in R} w_i \cdot m_{r_i}}{\sum_{r_i \in R} w_i}
  $$
  其中 $w_i$ 为 rubric 权重，$m_{r_i}$ 为二值判断（满足/未满足）
- **自动化评分器（LLM-as-a-Judge）**：
  - 使用 `DeepSeek-V3` 作为评分模型
  - 采用三种聚合策略：**Majority Voting**, **Unanimous Voting**, **Liberal Strategy**
- **人类一致性验证**：使用 Macro F1 评估 LLM 评分与人类专家评分的一致性

### 🆚 基线方法对比
| 基线 | 特点 |
|------|------|
| MedMCQA | 单轮多项选择题，仅测试知识记忆 |
| Med-PaLM 2 | 长文本问答，侧重安全性与准确性 |
| HealthBench | 包含 48,562 条 rubrics，但部分依赖真实数据 |
| AgentClinic | 多模态环境下的决策任务，非纯对话 |
| MediQ | 强调主动信息获取，但 rubrics 数量较少 |

> MedDialogRubrics 在 rubrics 数量（60,000+）、细粒度、可控性和隐私保护方面全面超越现有基准。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

| 模型 | 最高 Rubric Match Precision | 达峰轮次 | 表现特征 |
|------|-----------------------------|----------|-----------|
| **Gemini-2.5-Pro** | ~52% | 第 9–10 轮 | **高效型**：早期快速提升，策略性强 |
| **GPT-5** | ~50%（持续上升） | >13 轮（未达平台期） | **晚熟型**（Late-Bloomer）：缓慢积累信息，后期潜力大 |
| **DeepSeek-R1** | ~40% | 第 8–9 轮 | 改进空间大，增长平缓 |
| **Qwen3-235B** | <35% | 无明显提升 | 表现最弱，难以有效推进诊断逻辑 |

> ❗ **关键发现**：即使是当前最先进的 LLMs，在 MedDialogRubrics 上也仅能达到约 **50% 的关键问题覆盖率**，距离理想水平（100%）仍有巨大差距。

### 📈 多轮动态分析（Figure 6）
- **Gemini-2.5-Pro** 展现出最优的信息获取效率，在前几轮即命中大量高价值问题。
- **GPT-5** 起步较慢，但在第 9 轮后反超 DeepSeek-R1，显示更强的上下文利用能力。
- 所有模型在达到一定轮次后趋于性能饱和，表明当前架构在**长程推理与主动规划**方面存在瓶颈。

### 🔍 消融实验结果（Table 3）

| Patient Agent 配置 | 幻觉率 ↓ | 相关性 ↑ | 行为一致性 ↑ | 语言拟人化 ↑ |
|--------------------|--------|--------|------------|--------------|
| Basic（仅提示工程） | 0.129 | 0.920 | 0.689 | 0.981 |
| + Strict Adherence | 0.076 | 0.951 | 1.000 | 0.993 |
| + Guidance Injection | **0.049** | **0.992** | **1.000** | **1.000** |

> ✅ **结论**：**Strict Adherence + Guidance Injection** 双机制显著降低幻觉率（下降 62%），并实现完美行为一致性，是构建可靠 Patient Agent 的关键技术。

### 👥 自动评分与人类一致性（Figure 7）
- **Majority Voting** 策略下，LLM 评分与人类专家的 **Macro F1 达到 75–79%**
- **Liberal Strategy** 在 GPT-5 上表现最佳（F1 ≈ 79.6%），适合捕捉细微差异
- **Unanimous Voting** 过于严格，导致召回率下降

> ✅ 支持使用 LLM-as-a-Judge 实现可扩展、低成本、高信度的自动化评估。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **当前 LLMs 存在“询问缺陷”（Inquiry Deficit）**  
   多数模型无法根据已有信息动态更新鉴别诊断，导致后续提问缺乏针对性，陷入“泛泛而谈”的模式。

2. **多轮过程评估优于静态结果评估**  
   静态 QA 基准可能掩盖模型间的实际行为差异。例如 GPT-5 和 Qwen3 在单轮任务中差距不大，但在多轮对话中表现出高达 20% 的 rubric 覆盖率差距。

3. **战略型信息获取能力至关重要**  
   Gemini-2.5-Pro 的成功在于其能优先识别高信息增益的问题（high-value medical facts），体现更优的**主动推理策略**。

4. **改进基础模型微调不足以解决根本问题**  
   当前瓶颈不在知识储备，而在**对话管理架构**（dialogue management architecture），需要专门设计用于医疗场景的推理控制器。

### ⚠️ 方法的局限性
- **合成数据的真实性边界**：尽管经过专家审核，但仍无法完全替代真实复杂病例中的不确定性与噪声。
- **rubrics 的完备性假设**：假设所有“must-ask”问题均可提前定义，可能忽略个体化诊疗路径。
- **文化与语言偏差**：目前主要基于中文医学资源构建，跨语言迁移需进一步验证。

### 🔮 未来工作方向
1. **开发专用的 Doctor Agent 架构**：引入记忆机制、假设生成模块、优先级排序器等组件，增强主动推理能力。
2. **扩展至多模态场景**：结合影像、实验室数据等非结构化输入，构建更接近真实临床的工作流。
3. **引入患者情感与沟通质量评估**：不仅评估“问了什么”，还评估“如何问”——共情、解释能力、共享决策等软技能。
4. **开放数据与评分标准**：推动社区共建更大规模、更多样化的 rubrics 库，形成行业标准。

---

## 总结

> **MedDialogRubrics 不只是一个新 benchmark，更是推动医学 AI 从“会答题”走向“会看病”的关键一步**。它揭示了当前 LLMs 在真实临床对话中的深层短板，并提供了可复现、可扩展、临床可信的评估基础设施，为下一代安全、有效的 doctor agents 指明了发展方向。

</details>

---

### 11. [ElecTwit: A Framework for Studying Persuasion in Multi-Agent Social Systems](https://arxiv.org/abs/2601.00994)

**Authors**: Michael Bao  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.00994v1  

#### Abstract
This paper introduces ElecTwit, a simulation framework designed to study persuasion within multi-agent systems, specifically emulating the interactions on social media platforms during a political election. By grounding our experiments in a realistic environment, we aimed to overcome the limitations...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《ElecTwit: A Framework for Studying Persuasion in Multi-Agent Social Systems》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本论文旨在解决**现有LLM多智能体系统（multi-agent systems）在研究说服行为时缺乏现实性**的问题。以往的研究多依赖于简化环境（如基于游戏的模拟，例如 *Among Us*），这些场景虽然可控，但难以反映真实社会媒体环境中复杂的互动动态，尤其是政治选举中的舆论形成、信任建立与说服策略。

### 🚀 提出的新方法与创新点
作者提出了 **ElecTwit** —— 一个基于现实社交媒体平台（类比 X/Twitter）的多智能体仿真框架，用于研究大型语言模型（LLMs）在政治选举背景下的**说服行为**。

#### 主要创新包括：
- **现实主义驱动的设计**：  
  模拟真实社交平台功能（发帖、回复、点赞），并引入时间步长机制（每日9个交互时段），更贴近人类用户的在线行为节奏。
  
- **结构化代理背景建模**：  
  为每个 agent 赋予两种背景信息：
  - 六大政治立场维度（Economic Policy, Social Authority, Governmental Power, Foreign Policy, Environmental Approach, National Identity & Immigration）
  - “Big 5”人格特质（Extraversion, Agreeableness, Conscientiousness, Emotional Stability, Openness to Experience）
  这些背景通过数值（-100 到 100）量化，增强了 agent 行为的多样性与可解释性。

- **事件驱动机制（Eventor Agent）**：  
  引入一个独立的 `eventor` agent，负责生成“新闻事件”（部分为虚假信息），模拟外部信息冲击对选民认知的影响，测试模型如何应对争议与信任危机。

- **长期记忆机制（Diary System）**：  
  所有 agents 维护日记（diary），记录行动与计划，并在每日结束时汇总作为长期记忆输入，支持跨日推理与一致性行为。

- **开放源码与可复现性**：  
  所有代码公开于 GitHub（https://github.com/tcmmichaelb139/ai-electwit），促进后续研究。

### 🔍 相比现有方法的优势
| 方面 | 传统方法（如 Among Us 游戏框架） | ElecTwit |
|------|-------------------------------|---------|
| 环境真实性 | 低（封闭、规则明确的游戏） | 高（模拟真实社交媒体生态） |
| 话题相关性 | 抽象任务导向 | 政治选举，具社会影响力 |
| 动态复杂性 | 有限互动模式 | 包含舆论传播、回音室、欺骗等现象 |
| 可扩展性 | 小规模实验为主 | 支持多模型、多种子、长时间运行 |

> ✅ 因此，ElecTwit 提供了一个更具外推能力（generalizability）的测试床，可用于评估 LLM 在真实世界社会情境中的潜在风险与行为倾向。

---

## 2. 核心实验方法和设置

### 📚 数据集
本研究未使用传统意义上的“数据集”，而是构建了一个**完全合成的多智能体仿真环境**，所有交互数据由 LLM 自动生成。关键组件如下：

- **Agents 数量**：
  - 16 名 voter agents
  - 2 名 candidate agents
  - 1 名 eventor agent（固定为 `google/gemini-2.5-flash`）

- **模拟周期**：
  - 最多持续 8 天
  - 每天分为 9 个时间增量（9am–5pm）
  - 每轮所有 agents 同步更新状态（加速模拟）

- **交互限制**：
  - 每条 post/comment ≤ 280 字符（模仿 Twitter）
  - 每个 agent 每天最多执行 10 次操作（post/reply/like）
  - 必须引用正确 ID 才能完成 reply/like

### ⚙️ 实验设置
#### 使用的 LLM 模型（共8种）：
| 类型 | 模型名称（OpenRouter ID） |
|------|--------------------------|
| Closed-source | `openai/gpt-4.1-mini`, `anthropic/claude-3.5-haiku`, `google/gemini-2.5-flash`, `x-ai/grok-3-mini` |
| Open-source | `deepseek/deepseek-chat-v3-0324`, `qwen/qwq-32b`, `moonshotai/kimi-k2`, `mistralai/devstral-medium` |

- 所有模型均设置 **temperature = 0**（确定性输出）
- Voter agents 使用全部 8 种模型
- Candidate agents 仅测试 3 种：**GPT-4.1-mini**, **Gemini 2.5 Flash**, **Claude 3.5 Haiku**

#### 两类实验设计：
| 组别 | 设计说明 |
|------|--------|
| **Same Seed** | 固定随机种子，轮流更换 candidate 模型 → 分析 candidate 影响力 |
| **Different Seed** | 固定模型配置，改变随机种子 → 分析 voter 行为稳定性与背景影响 |

> 注：共进行 11 场模拟，其中一场重叠于两组。

### 📊 评估指标
#### 主要评估方式：
- **说服技术分类（Persuasion Technique Tagging）**：
  使用一个独立的 LLM 对所有 posts 和 comments 进行标注，依据 [Idziejczak et al., 2025] 定义的 **25 种 persuasion techniques**，允许一条消息属于多个类别。
  - 示例技术：`Appeal to Credibility`, `Appeal to Emotion`, `Vagueness`, `Distraction`, `Information Overload` 等。

- **投票结果分析**：
  - 每日进行民意调查（poll）
  - 第 8 天强制投票，统计候选人得票数

- **网络交互分析**：
  构建 reply 和 like 的有向图，分析：
  - 是否出现 echo chamber（相似观点者更多互动）
  - 节点中心性（谁被回复最多）

- **行为异常检测**：
  观察是否出现 emergent phenomena（如集体要求“书面承诺”）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 投票结果（Fig. 2）
- 在 **same seed** 实验中：
  - **Gemini 2.5 Flash** 作为候选人时赢得 **4/4** 场比赛
  - GPT-4.1-mini 赢得 **2/4**
  - Claude 3.5 Haiku 赢得 **0/4**
- 在 **different seed** 实验中：
  - Gemini 与 GPT-4.1-mini 各赢 2 场、输 2 场、平 2 场，表现相当

👉 表明 **Gemini 2.5 Flash 在说服能力上具有显著优势**，且结果不完全是随机性导致。

#### 🧠 说服技术使用情况（Fig. 5 & 6）
- 总共识别出 **125,254 个 persuasion tags** 来自 43,037 条消息（posts + comments）
- 最常用的五种技术（高频集中）：
  1. `Appeal to Credibility` （18,126 次）
  2. `Appeal to Logic`
  3. `Appeal to Emotion`
  4. `Vagueness`
  5. `Distraction`

- 所有 25 种技术中，除 `Information Overload` 外，其余均有使用
- `Self-Deprecation` 与 `Humor` 使用极少

#### 🆚 不同模型的表现差异（Fig. 6）
- **Gemini 2.5 Flash**：
  - 无论是 voter 还是 candidate，产生的 persuasion tags 数量**远高于其他模型**
  - 更积极地参与平台互动（发帖、评论最多）
- **Claude 3.5 Haiku**：
  - 作为 candidate 时生成的 persuasion tags 数量**最低**
  - 互动频率偏低
- **Grok 3-mini**：
  - 生成的 persuasion tags 数量最少，几乎“沉默”

> ❗ 注意：高标签数量 ≠ 更强说服力，而是反映更高的活跃度；但活跃本身也是说服的前提。

#### 🔁 Echo Chamber 分析（Fig. 7）
- 构建 reply 和 like 网络图，边颜色表示 sender 与 receiver 的背景相似度（cosine similarity）
- 结果显示：
  - **低相似度之间反而有更多回复（红色边更粗）**
  - 候选人收到大量回复但很少被点赞 → 可能引发争议而非支持

👉 **未观察到典型 echo chamber 效应**，反而显示出跨意识形态争论较多。

#### 📝 Emergent Behaviors 发现
- 出现“**kernel of truth**”消息：包含部分事实但整体误导（如夸大价格欺诈）
- 出现“**ink obsession**”现象：
  - Agents 开始集体要求候选人提供“written proof”（墨水签署文件）
  - “No ink, no vote” 成为口号
  - 显示出 LLMs 可以自发发展出对“可信承诺”的象征性需求

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLMs 广泛使用多种 persuasion techniques**：
   - 在真实模拟环境下，几乎所有 25 种 persuasion 技术都被激活，范围超过此前基于游戏的研究。
   - 特别是 `Appeal to Credibility` 和 `Emotion` 是主流策略。

2. **模型架构与训练显著影响说服行为**：
   - Gemini 2.5 Flash 展现出更强的主动性和说服技巧，可能与其训练数据或推理优化有关。
   - Claude 3.5 Haiku 和 Grok 3-mini 更保守、被动。

3. **Emergent social现象可观测**：
   - 如“ink obsession”表明 LLM agents 可以在没有预设激励的情况下，自发形成集体诉求和社会规范。
   - 存在“kernel of truth”式误导，提示 LLM 可能利用模糊表述进行软性操纵。

4. **背景特征对投票选择影响不显著**：
   - 尽管设置了详细的政治立场和人格维度，但 agent 背景与其最终投票对象之间的 cosine similarity 与胜选无明显正相关（Fig. 3）。

5. **非 echo chamber 的互动模式**：
   - 不同立场 agent 之间互动频繁，挑战了“算法茧房必然导致极化”的假设，在开放 feed 下可能出现反向争论。

### ⚠️ 方法的局限性
| 局限 | 说明 |
|------|------|
| **Uniform Feed** | 所有 agents 看到相同 feed，缺乏个性化推荐算法的影响 |
| **LLM-only Evaluation** | 说服效果由另一个 LLM 判断，缺乏 human-in-the-loop 验证 |
| **Temperature = 0** | 缺少随机性探索，无法评估创造性或极端行为 |
| **小样本规模** | 仅 11 场模拟，candidate 组合变化有限 |
| **同步更新机制** | 所有 agents 同时行动，不符合真实异步交流习惯 |

### 🔮 未来工作方向
1. **引入个性化 feed 与推荐系统**，研究算法偏见如何加剧 polarisation。
2. **加入 human participants**，实现 human-AI hybrid simulation。
3. **扩大模型组合与温度参数扫描**，探究 randomness 对 persuasion 的影响。
4. **增加更多社会角色**（记者、KOL、bot farm），提升生态系统复杂性。
5. **部署到真实平台进行小规模测试**（需伦理审查），验证仿真外推能力。

---

## 总结
> **ElecTwit 是首个将 persuasion 研究置于高度拟真的社交媒体选举环境中的开源多智能体框架**。它揭示了不同 LLM 在真实社会语境下的行为差异，发现了 emergent norms（如“ink”要求）和广泛使用的 persuasion tactics，为未来研究 AI 社会影响、信息战防御与 alignment verification 提供了重要工具。尽管存在简化假设，但其设计理念代表了从“沙盒游戏”向“现实模拟”的关键跃迁。

</details>

---

### 12. [A New Benchmark for the Appropriate Evaluation of RTL Code Optimization](https://arxiv.org/abs/2601.01765)

**Authors**: Yao Lu, Shang Liu, Hangan Zhou, Wenji Fang, Qijun Zhang, Zhiyao Xie  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.01765v1  

#### Abstract
The rapid progress of artificial intelligence increasingly relies on efficient integrated circuit (IC) design. Recent studies have explored the use of large language models (LLMs) for generating Register Transfer Level (RTL) code, but existing benchmarks mainly evaluate syntactic correctness rather ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A New Benchmark for the Appropriate Evaluation of RTL Code Optimization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Models (LLMs)** 的 RTL 代码生成研究缺乏对**优化质量**的有效评估。现有的基准（如 [26]）主要关注语法正确性和功能等价性，而忽视了集成电路设计的核心指标——**Power, Performance, and Area (PPA)**。此外，这些基准存在以下严重缺陷：
- **不现实的设计**：子优 RTL 包含人为构造的冗余操作（如 `s1 + 0`、`s1 * 1`），在实际工程中几乎不会出现；
- **简化的综合流程**：依赖弱综合工具（如 Yosys），导致 PPA 差异被夸大，无法反映工业级综合工具（如 Synopsys Design Compiler, DC）的真实行为；
- **评估不全面**：仅关注面积，忽略功耗和时序等关键维度。

这些问题使得现有基准容易误导模型评估，高估 LLM 在真实场景下的优化能力。

---

### 提出的新方法与思路
本文提出了一个新的基准：**RTL-OPT**，专门用于评估 LLM 在 RTL 代码优化方面的能力。

#### 核心创新点：
1. **高质量、手写的真实优化任务集**  
   - 包含 **36 个手工编写**的数字电路设计任务，涵盖组合逻辑、流水线数据路径、有限状态机（FSM）、存储器接口等多种类型。
   - 每个任务提供一对 RTL 代码：
     - **Suboptimal Version**：功能正确但有意遗漏某些优化机会；
     - **Optimized Version**：由专家人工优化，体现工业实践中有效的优化模式。

2. **真实的优化模式覆盖**  
   所有优化均基于业界验证过的最佳实践，包括：
   - Bit-width Optimization
   - Precomputation & LUT Conversion
   - Operator Strength Reduction
   - Control Simplification
   - Resource Sharing
   - State Encoding Optimization  
   这些变换即使经过先进综合工具处理仍能保留收益，避免“虚假优化”。

3. **自动化、标准化的评估框架**  
   提供完整的 EDA 流程支持：
   - 使用 **Synopsys DC** 和 **Yosys** 进行综合；
   - 利用 **Formality** 进行形式等价性检查（functional equivalence）；
   - 使用 **VCS** 进行动态仿真验证复杂时序逻辑；
   - 输出统一格式的 **PPA 报告**（Power, WNS/TNS, Area/Cells）。

4. **开源与可复现性**  
   全部资源（RTL 对、netlist、脚本、库文件）均已开源，确保公平比较。

---

### 相比现有方法的优势
| 维度 | 现有基准 [26] | RTL-OPT（本文） |
|------|----------------|------------------|
| 设计真实性 | 低（contrived inefficiencies） | 高（realistic coding styles） |
| 综合工具强度 | 弱（Yosys为主） | 强（DC compile_ultra） |
| PPA 覆盖 | 不完整（偏重面积） | 完整（P+P+A） |
| 功能验证 | 缺乏严格验证 | Formality + VCS 双重保障 |
| 基准可靠性 | 差（商业工具下无差异） | 强（35/36 在 DC 下显著更优） |

> ✅ **结论**：RTL-OPT 是首个能够**可靠、真实地衡量 LLM 在 RTL 优化中实际价值**的基准。

---

## 2. 核心实验方法和设置

### 数据集
- **主数据集**：**RTL-OPT**，包含 36 对 RTL 模块，大小从 14 到 20K cells 不等。
- **对比数据集**：复现并评估了先前工作 [26] 中的 43 对设计（其中成功综合 43 对）。
- **技术库**：采用 **Nangate45** 开源标准单元库。

---

### 实验设置
#### 综合配置
使用多种综合策略以测试鲁棒性：
- 工具：**Synopsys Design Compiler (DC)** 和 **Yosys**
- 模式：
  - `DC (compile, clk=1ns)`
  - `DC (compile, clk=0.1ns)`（tight timing）
  - `DC (compile_ultra, clk=1ns)`（aggressive optimization）
- 评估指标：
  - **Power**：总功耗（动态 + 泄漏）
  - **Performance**：WNS（最差负松弛）、TNS（总负松弛）
  - **Area**：Cell Count / Silicon Area (μm²)

#### 功能验证
- **Formality**：进行形式等价性检查（equivalence checking）
- **VCS**：对涉及流水线、状态机等时序敏感的设计进行动态测试

#### 基线方法对比
- **Human-optimized RTL** vs. Suboptimal RTL（验证基准有效性）
- **LLM-optimized RTL** vs. Suboptimal & Optimized RTL
- 测试的 LLMs：
  - GPT-4o-mini
  - Gemini-2.5
  - Deepseek V3
  - Deepseek R1

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 3）

#### ✅ RTL-OPT 基准本身的可靠性验证
| 合成配置 | Human-Optimized 更好案例数 / 总数 |
|---------|-------------------------------|
| Yosys | 33 / 36 |
| DC (compile, 1ns) | 30 / 36 |
| DC (compile_ultra, 1ns) | **35 / 36** ✅ |
| DC (compile, 0.1ns) | 23 / 36（其余为 trade-off）|

> 💡 表明：在强综合条件下，绝大多数优化版本依然优于子优版本，说明优化是“真实有效”的。

#### ❌ 对比基准 [26] 的不可靠性
| 合成配置 | Human-Optimized 更好案例数 / 总数 |
|---------|-------------------------------|
| Yosys | 24 / 43 |
| DC (compile_ultra, 1ns) | **仅 13 / 43** ❌ |

> ⚠️ 说明：多数所谓“优化”在工业流程下消失，甚至变差，暴露其评估无效。

---

### LLM 在 RTL-OPT 上的表现（Figure 3 & Table 4）

| LLM 模型 | Syntax 正确率 | Func 正确率 | PPA 优于 suboptimal (%) | PPA 超过 human optimized (%) |
|--------|---------------|-------------|--------------------------|------------------------------|
| GPT-4o-mini | 97.2% | 75% | 19.4% | — |
| Gemini-2.5 | ~100% | ~70% | <20% | — |
| Deepseek V3 | **100%** | 69.4% | ~23% | — |
| Deepseek R1 | 86.1% | 61.1% | **41.7%** | **13.9% (~5/36)** ✅ |

> 🔍 **亮点**：Deepseek R1 成功在约 **5 个任务上超越人类专家设计**，证明 LLM 具备潜在超人优化能力。

---

### 消融分析与错误模式分析
通过对失败案例的手动检查（n=40），发现三大类错误：
1. **控制逻辑错误**：布尔条件错误（如 comparator 条件写反）
2. **过度流水线化**：破坏原有延迟约束（FSM latency violation）
3. **资源共享不当**：寄存器复用导致数据冲突（stale data）

> 📌 结论：错误多源于**语义理解偏差**而非语法错误，提示需加强上下文建模与约束感知。

---

## 4. 关键结论和发现

### 主要发现
1. **现有 RTL 优化基准严重失真**：许多“优化”在强综合工具下无效，易造成误判。
2. **综合工具选择极大影响评估结果**：Yosys 放大差异，DC 消除虚假优化 → 必须使用工业级工具。
3. **PPA 存在权衡（trade-off）**：不能只看单一指标，需综合评估。
4. **当前 LLM 仍有很大提升空间**：平均仅 ~20% 的设计实现 PPA 提升，且功能正确率普遍低于 75%。
5. **最强模型已展现超越人类潜力**：Deepseek R1 在 5 个任务中超过人工优化方案。

---

### 方法局限性
- **规模限制**：36 个任务虽多样但仍属小样本，难以覆盖所有设计风格。
- **静态评估**：未考虑物理实现（placement, routing）的影响。
- **依赖特定工艺库**：基于 Nangate45，迁移到先进工艺需重新校准。
- **prompt 敏感性未深入研究**：不同提示词可能显著影响输出质量。

---

### 未来工作方向
1. **扩展更大规模的真实优化数据集**（如来自开源芯片项目）
2. **引入物理感知优化目标**（timing closure, congestion）
3. **构建端到端训练-评估闭环**，推动 LLM 自主学习优化策略
4. **开发具备硬件语义理解能力的专用 LLM 架构**
5. **探索 human-in-the-loop 的协同优化范式**

---

## 总结
✅ **RTL-OPT** 是一个面向未来的、可靠的、工业对齐的 RTL 优化评估基准，填补了当前 AI for EDA 领域的关键空白。它不仅揭示了现有研究的评估误区，也为下一代智能 IC 设计工具的发展提供了坚实基础。

</details>

---

### 13. [Enhancing Multilingual RAG Systems with Debiased Language Preference-Guided Query Fusion](https://arxiv.org/abs/2601.02956)

**Authors**: Jeonghyun Park, Byeongjeong Kim, Seojin Hwang, Hwanhee Lee  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02956v1  

#### Abstract
Multilingual Retrieval-Augmented Generation (mRAG) systems often exhibit a perceived preference for high-resource languages, particularly English, resulting in the widespread adoption of English pivoting. While prior studies attribute this advantage to the superior English-centric capabilities of La...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Enhancing Multilingual RAG Systems with Debiased Language Preference-Guided Query Fusion

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Multilingual RAG (mRAG)** 系统中存在的一个普遍现象——**对英语的偏好（English preference）**——进行了深入分析。传统观点认为这种偏好源于大语言模型（LLM）在英语上的更强能力（如推理、生成质量），因此广泛采用“**English pivoting**”策略（将非英语查询翻译为英语再进行检索）。然而，作者指出，这种“英语优势”很大程度上是由**评估基准中的结构性偏差（structural priors）** 所导致的假象，而非模型本身的内在偏好。

具体而言，这些结构性偏差包括：
- **Gold Availability Prior**：正确答案（gold passage）在英文维基百科中覆盖率远高于其他语言。
- **Exposure Bias**：由于英文资源丰富，检索器更容易返回英文文档。
- **Cultural Prior**：某些问题与特定文化或地域强相关，其本地语言中的原生表达（如专有名词、脚本）成为强检索锚点，从而造成“偏好”。

### 提出的新方法与新思路

#### （1）DeLP (Debiased Language Preference)
提出了一种新的度量指标 **DeLP**，用于**去偏的语言偏好测量**。该方法通过岭回归（ridge regression）从观测到的语言偏好信号中剥离出由上述结构性先验（exposure, gold availability, cultural prior）带来的影响，保留残差作为模型真实的语言偏好信号。

**核心发现**：经过 DeLP 去偏后，所谓的“英语偏好”基本消失，取而代之的是**单语对齐（monolingual alignment）偏好**——即当查询语言与文档语言相同时，检索效果最佳。

#### （2）DELTA (DEbiased Language preference-guided Text Augmentation)
基于 DeLP 的发现，提出了一种轻量级的查询增强框架 **DELTA**。它不修改检索器或生成器，而是通过对原始查询进行**多语言线索融合（query fusion）** 来提升跨语言检索效果。

DELTA 的核心思想是：
- 构造一个融合查询 `Q_fused`，包含多个语义段：
  - `[GLOB]`：英文翻译（global pivot）
  - `[LOCAL]`：原始语言查询（保留本地锚点）
  - `[TITLE_BRIDGE]`：中英文配对标题
  - `[ALIASES]`：多语言别名
  - `[LOCALE_HINT]`：地域提示
- 使用**基于置信度的重复加权（repetition-based weighting）** 动态控制各部分权重，优先强化高置信度的文化相关线索。

### 相比现有方法的优势
- **无需修改模型架构或训练过程**：DELTA 是纯文本级别的查询重构，成本低、易部署。
- **超越 English Pivoting**：实验证明 DELTA 在多数非英语语言上显著优于简单的英文翻译查询。
- **揭示真实偏好机制**：DeLP 提供了一个更公平的评估视角，挑战了“英语中心主义”的固有认知。

---

## 2. 核心实验方法和设置

### 数据集
- **MKQA**：包含 10k 经专业翻译的多语言问答对，支持跨语言评估。
- 子集：选取与 **KILT-NQ** 重叠的 2.7K 示例，以便获取标准的文档溯源信息（gold passage IDs）。
- **知识源**：使用英文维基百科和用户本地语言的维基百科作为检索数据库。

### 实验设置与评估指标
- **检索器**：`BGE-m3`（多语言嵌入模型），并辅以重排序（reranking）。
- **生成器**：三种主流多语言 LLM：
  - `Qwen3-235B`
  - `DeepSeek-v3.1`
  - `Gemini-2.5-Flash`
- **检索设置**：Top-50 文档召回，使用 Top-5 进行生成。
- **评估指标**：
  - **Recall@50**：衡量检索阶段性能。
  - **End-to-end Accuracy**：使用字符 3-gram recall 衡量最终生成答案的准确性。
  - **Latency**：端到端响应时间。

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **MultiRAG** | Document Level | 使用原始语言查询，直接检索并生成 |
| **CrossRAG** | Document Level | 检索后将文档统一翻译为英文再生成 |
| **DKM-RAG** | Document Level | 翻译并用 LLM 生成多个精炼版本 |
| **QTT-RAG** | Document Level | 添加翻译质量标签辅助生成器判断可信度 |
| **English Translation** | Query Level | 将查询翻译为英文后检索 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 5）

| 方法 | Qwen3-235B (AVG) | Gemini-2.5-Flash (AVG) | DeepSeek-v3.1 (AVG) |
|------|------------------|------------------------|---------------------|
| MultiRAG | 51.30 | 43.80 | 45.46 |
| English Translation | 58.81 | 52.75 | 55.26 |
| **DELTA (ours)** | **62.88** | **56.28** | **58.23** |

> ✅ **DELTA 在所有三个生成器上均取得最高平均准确率**，显著优于所有基线。

#### 非英语语言上的增益尤为明显：
- 在阿拉伯语（ar）、泰语（th）、韩语（ko）等低资源语言上，DELTA 相比 English Translation 提升可达 **+7~10 个百分点**。
- 英语查询上无显著收益甚至略有下降，因为 `[GLOB]` 和 `[LOCAL]` 内容高度重合，引入冗余。

### 与基线方法的对比结果
- DELTA 不仅优于所有 **document-level** 方法（如 CrossRAG, DKM-RAG），而且实现方式更轻量（无需翻译大量文档）。
- 相比 **query-level** 的 English Translation，DELTA 显著提升了跨语言检索的相关性和答案准确性。
- **延迟更低**：DELTA 平均延迟低于或接近 English Translation，因其能更快定位目标文档。

### 消融实验结果（Table 7）

| 方法 | 准确率 (avg) |
|------|------------|
| Original Query | 63.68 |
| + Global (`[GLOB]`) | 71.42 |
| + Title (`[TITLE_BRIDGE]`) | 68.17 |
| + Aliases (`[ALIASES]`) | 68.14 |
| + Locale Hint (`[LOCALE_HINT]`) | 67.57 |
| **All cues (DELTA)** | **72.99** |

> 🔍 结论：
- `[GLOB]` 贡献最大，说明全局英文表达有助于生成器理解。
- `[TITLE_BRIDGE]` 和 `[ALIASES]` 提供独立增益，证明多语言锚点有助于实体消歧。
- 所有线索组合效果最佳，表明 DELTA 各组件协同作用。

---

## 4. 关键结论和发现

### 主要发现
1. **“英语偏好”是一个被放大的假象**：mRAG 系统中观察到的英语优势主要来自数据分布偏差（gold availability, exposure bias），而非模型本身的能力差异。
2. **真实偏好是单语对齐（monolingual alignment）**：检索器本质上更擅长匹配同语言的查询与文档。
3. **DeLP 成功剥离结构性偏差**：经校准后的语言偏好显示，英语不再具有系统性优势，反而本地语言匹配得分最高。
4. **DELTA 利用真实偏好实现性能突破**：通过融合多语言线索并动态加权，DELTA 在保持高效的同时显著提升跨语言 RAG 性能。

### 方法的局限性
1. **仅解决检索侧偏好**：DeLP 和 DELTA 主要关注 retriever 的语言偏好，未处理 generator 如何消费多语言证据的问题。
2. **依赖 Wikipedia 设置**：实验基于维基百科构建的知识库，结论在领域专用或多模态数据上的泛化性有待验证。
3. **加权策略较粗粒度**：当前使用重复次数进行加权，缺乏更精细的注意力控制机制，可能在复杂歧义场景下失效（见失败案例）。
4. **存在过度本地化风险**：对于意图模糊的问题（如“朝鲜战争时期的总统”），DELTA 可能错误地偏向本地解释（韩国总统），忽略全球上下文（美国总统）。

### 未来工作方向
- 将 debiasing 扩展至 generator 端，研究其如何选择和利用不同语言的证据。
- 探索更智能的 query fusion 控制逻辑，例如基于意图识别的动态调节。
- 在更多样化的多语言语料库（如医疗、法律）上验证 DeLP 与 DELTA 的有效性。
- 结合 ambiguity-aware safeguard 机制，防止因文化线索误导而导致的检索偏差。

---

> 📌 **总结一句话**：  
> 本文揭示了 mRAG 中“英语偏好”的本质是结构性偏差所致，并提出 DeLP 和 DELTA 方法，前者提供更公正的评估工具，后者通过偏好引导的查询融合，在不增加复杂性的前提下实现了跨语言 RAG 性能的全面提升。

</details>

---

### 14. [LLM-Enhanced Reinforcement Learning for Time Series Anomaly Detection](https://arxiv.org/abs/2601.02511)

**Authors**: Bahareh Golchin, Banafsheh Rekabdar, Danielle Justo  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02511v1  

#### Abstract
Detecting anomalies in time series data is crucial for finance, healthcare, sensor networks, and industrial monitoring applications. However, time series anomaly detection often suffers from sparse labels, complex temporal patterns, and costly expert annotation. We propose a unified framework that i...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LLM-Enhanced Reinforcement Learning for Time Series Anomaly Detection 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**时间序列异常检测**中的几个关键挑战：
- **标签稀疏性**：真实世界中异常事件罕见，标注成本高；
- **奖励稀疏（sparse rewards）**：在强化学习（Reinforcement Learning, RL）框架下，仅靠少量真实标签提供的反馈不足以有效训练智能体；
- **复杂时序模式识别困难**：传统方法难以捕捉长期依赖、多变量交互及语义层面的异常行为。

### 提出的新方法与创新思路
作者提出了一种**统一的集成框架**，结合以下四个核心技术：
1. **LLM-based Potential Function for Reward Shaping**  
   利用大型语言模型（Large Language Model, LLM）生成基于语义理解的“潜在函数”（potential function），用于**基于潜力的奖励塑形（Potential-Based Reward Shaping, PBRS）**。这使得RL智能体能从LLM对时间窗口内模式的理解中获得额外指导信号，而不会改变最优策略（policy invariance）。

2. **VAE-enhanced Dynamic Reward Scaling**  
   引入变分自编码器（Variational Autoencoder, VAE）计算重构误差作为无监督异常得分，并通过一个**动态调节系数 λ(t)** 将其与分类奖励融合，实现监督与非监督信号之间的自适应平衡。

3. **Active Learning with Label Propagation**  
   采用主动学习（Active Learning, AL）选择不确定性最高的样本进行人工标注，并利用标签传播（label propagation）扩展已标注数据集，显著降低标注开销。

4. **LSTM-based RL Agent for Sequential Decision Making**  
   使用LSTM作为Q网络骨干，建模时间序列的长期依赖关系，在滑动窗口上执行二分类动作（正常/异常），并接收综合奖励信号进行训练。

### 相比现有方法的优势
- **更强的语义感知能力**：LLM能够识别统计方法忽略的复杂模式（如趋势突变、周期扰动等），提升召回率；
- **更高的样本效率**：通过主动学习减少对大量标注数据的依赖；
- **更稳定的训练过程**：PBRS缓解了RL中的稀疏奖励问题；VAE提供持续的内在反馈；
- **端到端可扩展性强**：适用于单变量与多变量场景，且无需手动设计特征或规则。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Yahoo-A1**：包含67个单变量时间序列，来自Yahoo网站流量监控，每小时采样一次，异常占比约1.76%。
- **SMD (Server Machine Dataset)**：包含28个多变量时间序列，每个序列有38个传感器维度，记录服务器运行状态，前5天为正常数据，后5天注入随机异常，异常比例约为4.16%。

| 数据集       | 类型     | 序列数量 | 维度 | 异常比例 |
|------------|--------|-------|-----|-------|
| Yahoo-A1   | 单变量    | 67    | 1   | 1.76% |
| SMD        | 多变量    | 28    | 38  | 4.16% |

### 实验设置与评估指标
- **评估指标**：Precision（精确率）、Recall（召回率）、F1 Score（F1分数）
- **训练方式**：在线学习，按episode处理完整时间序列
- **滑动窗口长度**：`n_steps = 25`
- **LLM候选模型**：GPT-3.5、Llama-3.2-3B、Phi-2
- **Prompt设计**：采用few-shot提示，输入格式化的时间窗口数据，要求输出JSON格式的`{"severity": v}`，其中`v ∈ [0,1]`

### 基线方法对比
论文对比了多个主流基线模型：
- **THOC**：基于层次化一类网络的时间序列异常检测
- **TranAD**：基于Transformer的多变量异常检测
- **TS2Vec**：通用时间序列表示学习
- **DCdetector**：双注意力对比学习方法
- **TimesNet**：将时间序列转换为二维变化图进行分析
- **CARLA**：当前最先进的深度学习基准方法之一

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table II）

#### 在 Yahoo-A1 上的表现（单变量）
| Model                     | Precision | Recall  | F1       |
|--------------------------|-----------|---------|----------|
| CARLA (best non-LLM)     | 0.5747    | 0.9755  | **0.7233** |
| **Proposed + Llama-3**   | **0.6051**| **0.9565**| **0.7413** ✅ |
| Proposed + GPT-3.5       | 0.0742    | 0.9130  | 0.1372   |
| Proposed + Phi-2         | 0.6666    | 0.4761  | 0.5555   |

> ✅ **Llama-3版本在F1上超越CARLA，达到SOTA水平**

#### 在 SMD 上的表现（多变量）
| Model                     | Precision | Recall  | F1       |
|--------------------------|-----------|---------|----------|
| CARLA                    | 0.4276    | 0.6362  | **0.5114** |
| **Proposed + Llama-3**   | 0.3813    | **0.8685**| **0.5300** ✅ |
| Proposed + GPT-3.5       | 0.5370    | 0.4061  | 0.4625   |
| Proposed + Phi-2         | **0.8461**| 0.2541  | 0.3908   |

> ✅ **Llama-3在F1上再次优于所有基线，尤其以高达86.85%的Recall领先**

### 性能分析与发现
- **Llama-3表现最佳**：在两个数据集上均取得最高F1值，说明其生成的severity评分具有良好的校准性和一致性。
- **GPT-3.5过于激进（over-eager）**：倾向于将许多正常波动标记为异常，导致Precision极低（仅7.4%），尽管Recall较高。
- **Phi-2过于保守（cautious）**：Precision很高但Recall很低，漏检严重。
- **VAE动态缩放机制有效**：当episode表现下降时自动增加λ，增强VAE异常信号权重，有助于稳定训练。

### 消融实验（文中未明确列出表格，但可通过论述推断）
虽然没有独立的消融表，但从方法描述和讨论中可以得出以下结论：
- 若移除LLM奖励塑形 → 回归传统RL+VAE架构，性能接近CARLA但无法突破；
- 若不使用动态reward scaling → 难以平衡监督与无监督信号，可能导致过拟合或欠敏感；
- 若关闭active learning → 需更多人工标注才能达到相同性能，实用性下降。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM可用于语义级奖励塑形**：即使未经微调，预训练LLM也能有效评估时间序列片段的“异常严重程度”，并作为PBRS中的potential function，显著提升RL探索效率。
2. **Llama-3是最佳选择**：相比GPT-3.5和Phi-2，Llama-3在指令遵循、输出稳定性、模式识别准确性方面表现最优，适合用于构建可靠奖励信号。
3. **本方法在低标注预算下仍高效**：通过active learning + label propagation，大幅减少了人工标注需求，适合现实部署。
4. **对单变量和多变量数据均有良好适应性**：在Yahoo-A1（简单突发异常）和SMD（复杂渐变+多传感器耦合异常）上均表现出色。

### 方法的局限性
- **计算开销较大**：每次决策需调用LLM推理，可能影响实时性；
- **依赖LLM API可用性与成本**：若使用闭源模型（如GPT），存在服务中断或费用风险；
- **多变量跨传感器关系建模不足**：当前LLM输入为扁平化的sensor readings拼接，未能显式建模变量间因果或相关结构；
- **few-shot prompt效果敏感**：prompt设计质量直接影响LLM输出稳定性。

### 未来工作方向
- 探索轻量化LLM（如TinyLlama）本地部署以降低成本；
- 设计专门面向时间序列的prompt模板和fine-tuning策略；
- 引入graph-based modeling来捕捉多变量间的依赖关系；
- 扩展至其他领域（如医疗、金融）验证泛化能力；
- 结合offline RL进一步提升样本利用率。

---

> 🔚 **总结**：本文开创性地将LLM引入RL-based异常检测框架，通过semantic reward shaping解决了稀疏奖励难题，在Yahoo-A1和SMD上实现了state-of-the-art的F1性能，尤其在Recall方面优势明显。该方法展示了LLM作为“认知增强模块”的巨大潜力，为未来智能运维系统提供了新的技术路径。

</details>

---

### 15. [MAFS: Multi-head Attention Feature Selection for High-Dimensional Data via Deep Fusion of Filter Methods](https://arxiv.org/abs/2601.02668)

**Authors**: Xiaoyan Sun, Qingyu Meng, Yalu Wen  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02668v1  

#### Abstract
Feature selection is essential for high-dimensional biomedical data, enabling stronger predictive performance, reduced computational cost, and improved interpretability in precision medicine applications. Existing approaches face notable challenges. Filter methods are highly scalable but cannot capt...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MAFS: Multi-head Attention Feature Selection for High-Dimensional Data via Deep Fusion of Filter Methods  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
高维生物医学数据（如基因表达、SNP、多组学）的特征选择面临三大挑战：
- **Filter 方法** 虽然高效可扩展，但无法捕捉非线性关系且易遗漏冗余特征；
- **深度学习方法** 能建模复杂依赖，但缺乏稳定性、可解释性和大规模效率；
- **单头注意力机制**（single-head attention）受限于单一视角，难以全面捕获多层次依赖，且对初始化敏感，影响可重复性。

### 🚀 提出的新方法：MAFS
提出 **MAFS**（Multi-head Attention-based Feature Selection），一种融合统计先验与深度表示能力的混合框架，包含三个核心模块：
1. **Filter Module**：使用多种 filter 方法（如 SIS, BCor-SIS, Kendall’s tau）生成初始特征重要性权重，作为可学习的软先验（soft priors），用于稳定初始化；
2. **Multi-head Attention Module**：采用 multi-head 外部注意力机制，每个 head 并行处理不同 filter 的输出，从多个统计视角捕捉线性、非线性、排序等依赖关系；
3. **Reordering Module**：将各 attention head 输出的 Top-K 特征合并为候选集，并通过树模型（如 Random Forest）重新排序，解决冲突并最小化信息丢失。

### 🔍 相比现有方法的优势
- **稳定性增强**：通过 filter 权重引导初始化，显著降低对随机初始化的敏感性；
- **多视角融合**：multi-head 设计允许模型同时评估多种特征-响应关系，提升对复杂非线性依赖的识别能力；
- **可解释性强**：attention 权重提供透明的特征重要性评分；
- **高效性**：使用外部注意力（external attention），计算复杂度由 $O(n^2p)$ 降至 $O(np)$，适合超高维场景；
- **鲁棒性高**：在模拟和真实数据中均表现出更高的覆盖率（coverage）和更小的方差。

---

## 2. 核心实验方法和设置

### 📊 数据集
#### （1）模拟数据（Simulation）
- 基于 **OmicsSIMLA** 生成：
  - 连续型：RNA-seq 表达数据（负二项分布，参数来自 TCGA BRCA）；
  - 分类型：SNP 基因型（基于 1000 Genomes CEPH 群体，HAPGEN2 模拟）；
  - 组合型：连续 + 分类混合。
- 功能形式：7 种特征-响应关系（线性、对数、余弦、指数、立方、交互项等）；
- 维度：$p = 25K, 50K, 100K$；样本量：$n = 500, 2000$；
- 因变量类型：连续（正态）与二分类（平衡病例对照）。

#### （2）真实数据
- **癌症基因表达数据集**（6个）：
  - Colon, Leukemia, ALLAML, GLI_85, Prostate_GE, SMK_CAN_187；
  - 任务：二分类（肿瘤 vs 正常）；
  - 特征维度：2K ~ 22K。
- **ADNI 数据集**（阿尔茨海默病神经影像计划）：
  - 样本数：449；
  - 特征：49,386 个基因表达；
  - 响应变量：9 个脑区体积（连续型表型）；
  - 任务：回归预测。

### 🧪 实验设置与评估指标
| 类别 | 设置 |
|------|------|
| **训练/验证/测试划分** | 模拟数据：80%/20%；真实数据：5折 CV 或 60:20:20 |
| **超参优化** | 使用 Optuna 进行 100 次贝叶斯搜索 |
| **网络架构** | 所有嵌入式方法统一使用两层 MLP（BN + ReLU + Dropout） |
| **评估指标** | 
| - 模拟数据 | **Coverage Rate**：选中的因果特征比例 |
| - 真实数据 | **AUROC**（分类）、**Pearson Correlation**（回归） |

### ⚔️ 基线方法对比
比较了四类主流深度学习特征选择方法：
| 方法 | 类型 | 说明 |
|------|------|------|
| **CancelOut** | Layer-based gating | 学习门控向量进行软选择 |
| **DeepLIFT** | Gradient-based attribution | 反向传播激活差异归因 |
| **EAR-FS** | Attention-based | 单头外部注意力机制 |
| **GRACES** | Graph-based | 图卷积 + 输入梯度估计 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）模拟实验结果（Fig. 2–4）
- 在所有配置下，**MAFS 始终取得最高 coverage**：
  - 当选择前 2% 特征时，在 $p=100K, n=2000$ 下：
    - **连续响应**：MAFS 达到约 **0.86**（线性），**1.0**（对数），**0.93**（余弦）；
    - **二分类响应**：MAFS 显著优于其他方法，尤其在非线性关系上优势明显。
- **稳定性更高**：MAFS 的置信区间宽度（±2.2%）小于其他方法（±2.5–3.1%）。

#### （2）维度扩展性分析（Fig. 4）
- 随着维度增加（25K → 100K），所有方法 coverage 下降，但 **MAFS 下降最缓**：
  - 固定选择 Top 100 特征时：
    - MAFS coverage 减少 **21%**（连续响应）；
    - GRACES 减少 **47%**，CancelOut 减少 **100%**（降至 0）；
  - 表明 MAFS 更能应对“维度灾难”。

#### （3）真实数据表现

##### 癌症数据集（Fig. 5）
- 在 **Leukemia / Prostate**（易分数据）上，各方法性能接近（AUROC > 0.95）；
- 在 **Colon / Lung**（难分数据）上，MAFS 明显领先：
  - Colon（MLP, 20 features）：**AUROC = 0.922**（vs GRACES 0.896, EAR-FS 0.855）；
  - Lung（MLP）：**0.785**（领先第二名 > 0.022）；
- **效率更高**：MAFS 仅需 2–3 个特征即可达到 AUROC > 0.95（Leukemia），而 EAR-FS 需 8–10 个。

##### ADNI 数据集（Fig. 6）
- 在多数脑区（如 brainstem, pallidum）中，MAFS 实现最高 Pearson 相关性：
  - Brainstem（MLP, 50 features）：**0.440**（vs GRACES 0.418, EAR-FS 0.329）；
  - Accumbens area：**0.219**（远高于 EAR-FS 的 0.100）；
- 同样表现出更强的特征利用效率。

### 🔬 消融实验与补充分析（Supplementary Figures）
- **滤波器引导初始化本身即有效**：
  - 将 EAR-FS 的均匀初始化替换为 filter 初始化后，其性能显著提升（Supp. Fig. S12–S19）；
  - 说明 **高质量初始化是关键因素之一**，甚至可能比架构改进更重要。
- **multi-head 注意力带来额外增益**：
  - 多头设计进一步提升了 coverage 和稳定性，尤其是在复杂功能关系下。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MAFS 实现了 filter 方法与深度学习的优势融合**：
   - 利用 filter 提供稳定先验，避免盲目初始化；
   - 利用 multi-head attention 捕捉多尺度、非线性依赖；
   - 通过 reordering 模块整合多视角结果，减少信息损失。

2. **在各种数据类型和关系下均表现最优**：
   - 对线性、非线性、交互效应均有良好适应性；
   - 在超高维（$p=100K$）和小样本（$n=500$）条件下仍保持高覆盖率。

3. **具备良好的实用价值**：
   - 能以更少特征实现同等或更好预测性能，有利于下游解释与验证；
   - 在癌症分类与神经影像回归任务中均超越当前 SOTA 方法。

### ⚠️ 局限性
1. **归一化可能导致权重失真**：
   - 不同 filter 方法的原始尺度差异可能被压缩，影响先验信号强度；
   - 未来可探索 learnable fusion 或 rank-level 融合策略。

2. **要求完整特征矩阵**：
   - 无法直接处理缺失值，可能引入选择偏差；
   - 可结合 imputation 或 missing-aware attention 进行扩展。

3. **计算开销高于轻量级方法**：
   - 虽然比 GRACES 快一个数量级以上（117.6 min vs 1569.9 min），但仍慢于 CancelOut / DeepLIFT；
   - 可通过模型蒸馏或稀疏化进一步优化。

### 🔮 未来工作方向
- 探索更灵活的 filter 融合机制（如 attention-based fusion）；
- 引入 missing-data handling 机制以适应临床数据；
- 扩展至多任务或多模态特征选择场景；
- 开发可视化工具支持生物学解释与假说生成。

---

## 总结
**MAFS 是一种新颖、稳健、可解释的高维特征选择框架**，通过将传统 filter 方法与 multi-head attention 深度融合，成功克服了现有方法在稳定性、表达能力和效率之间的权衡难题。实验表明其在模拟与真实生物医学数据中均显著优于主流深度学习方法，为精准医学中的 biomarker 发现提供了强有力的技术支持。

</details>

---

### 16. [Uni-FinLLM: A Unified Multimodal Large Language Model with Modular Task Heads for Micro-Level Stock Prediction and Macro-Level Systemic Risk Assessment](https://arxiv.org/abs/2601.02677)

**Authors**: Gongao Zhang, Haijiang Zeng, Lu Jiang  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02677v1  

#### Abstract
Financial institutions and regulators require systems that integrate heterogeneous data to assess risks from stock fluctuations to systemic vulnerabilities. Existing approaches often treat these tasks in isolation, failing to capture cross-scale dependencies. We propose Uni-FinLLM, a unified multimo...

---

### 17. [From Memorization to Creativity: LLM as a Designer of Novel Neural-Architectures](https://arxiv.org/abs/2601.02997)

**Authors**: Waleed Khalid, Dmitry Ignatov, Radu Timofte  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02997v1  

#### Abstract
Large language models (LLMs) excel in program synthesis, yet their ability to autonomously navigate neural architecture design--balancing syntactic reliability, performance, and structural novelty--remains underexplored. We address this by placing a code-oriented LLM within a closed-loop synthesis f...

---

### 18. [MindChat: A Privacy-preserving Large Language Model for Mental Health Support](https://arxiv.org/abs/2601.01993)

**Authors**: Dong Xue, Jicheng Tu, Ming Wang, Xin Yan, Fangzhou Liu, Jie Hu  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.01993v1  

#### Abstract
Large language models (LLMs) have shown promise for mental health support, yet training such models is constrained by the scarcity and sensitivity of real counseling dialogues. In this article, we present MindChat, a privacy-preserving LLM for mental health support, together with MindCorpus, a synth...

---

### 19. [WebAnchor: Anchoring Agent Planning to Stabilize Long-Horizon Web Reasoning](https://arxiv.org/abs/2601.03164)

**Authors**: Yu Xinmiao, Zhang Liwen, Feng Xiaocheng, Jiang Yong, Qin Bing, Xie Pengjun, Zhou Jingren  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.03164v1  

#### Abstract
Large Language Model(LLM)-based agents have shown strong capabilities in web information seeking, with reinforcement learning (RL) becoming a key optimization paradigm. However, planning remains a bottleneck, as existing methods struggle with long-horizon strategies. Our analysis reveals a critical ...

---

### 20. [Electricity Price Forecasting: Bridging Linear Models, Neural Networks and Online Learning](https://arxiv.org/abs/2601.02856)

**Authors**: Btissame El Mahtout, Florian Ziel  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.02856v1  

#### Abstract
Precise day-ahead forecasts for electricity prices are crucial to ensure efficient portfolio management, support strategic decision-making for power plant operations, enable efficient battery storage optimization, and facilitate demand response planning. However, developing an accurate prediction mo...

---

### 21. [FlowPlan-G2P: A Structured Generation Framework for Transforming Scientific Papers into Patent Descriptions](https://arxiv.org/abs/2601.02589)

**Authors**: Kris W Pan, Yongmin Yoo  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02589v1  

#### Abstract
Over 3.5 million patents are filed annually, with drafting patent descriptions requiring deep technical and legal expertise. Transforming scientific papers into patent descriptions is particularly challenging due to their differing rhetorical styles and stringent legal requirements. Unlike black-box...

---

### 22. [LLM-Augmented Changepoint Detection: A Framework for Ensemble Detection and Automated Explanation](https://arxiv.org/abs/2601.02957)

**Authors**: Fabian Lukassen, Christoph Weisser, Michael Schlee, Manish Kumar, Anton Thielmann, Benjamin Saefken, Thomas Kneib  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02957v1  

#### Abstract
This paper introduces a novel changepoint detection framework that combines ensemble statistical methods with Large Language Models (LLMs) to enhance both detection accuracy and the interpretability of regime changes in time series data. Two critical limitations in the field are addressed. First, in...

---

### 23. [UltraLogic: Enhancing LLM Reasoning through Large-Scale Data Synthesis and Bipolar Float Reward](https://arxiv.org/abs/2601.03205)

**Authors**: Yile Liu, Yixian Liu, Zongwei Li, Yufei Huang, Xinhua Feng, Zhichao Hu, Jinglu Hu, Jianfeng Yan, Fengzong Lian, Yuhong Liu  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03205v1  

#### Abstract
While Large Language Models (LLMs) have demonstrated significant potential in natural language processing , complex general-purpose reasoning requiring multi-step logic, planning, and verification remains a critical bottleneck. Although Reinforcement Learning with Verifiable Rewards (RLVR) has succe...

---

### 24. [Scalable Tree Ensemble Proximities in Python](https://arxiv.org/abs/2601.02735)

**Authors**: Adrien Aumon, Guy Wolf, Kevin R. Moon, Jake S. Rhodes  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02735v1  

#### Abstract
Tree ensemble methods such as Random Forests naturally induce supervised similarity measures through their decision tree structure, but existing implementations of proximities derived from tree ensembles typically suffer from quadratic time or memory complexity, limiting their scalability. In this w...

---

### 25. [Universal Conditional Logic: A Formal Language for Prompt Engineering](https://arxiv.org/abs/2601.00880)

**Authors**: Anthony Mikinka  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.00880v1  

#### Abstract
We present Universal Conditional Logic (UCL), a mathematical framework for prompt optimization that transforms prompt engineering from heuristic practice into systematic optimization. Through systematic evaluation (N=305, 11 models, 4 iterations), we demonstrate significant token reduction (29.8%, t...

---

### 26. [PCEval: A Benchmark for Evaluating Physical Computing Capabilities of Large Language Models](https://arxiv.org/abs/2601.02404)

**Authors**: Inpyo Song, Eunji Jeon, Jangwon Lee  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.02404v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities across various domains, including software development, education, and technical assistance. Among these, software development is one of the key areas where LLMs are increasingly adopted. However, when hardware constraints are co...

---

### 27. [Towards Comprehensive Stage-wise Benchmarking of Large Language Models in Fact-Checking](https://arxiv.org/abs/2601.02669)

**Authors**: Hongzhan Lin, Zixin Chen, Zhiqi Shen, Ziyang Luo, Zhen Ye, Jing Ma, Tat-Seng Chua, Guandong Xu  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.02669v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in real-world fact-checking systems, yet existing evaluations focus predominantly on claim verification and overlook the broader fact-checking workflow, including claim extraction and evidence retrieval. This narrow focus prevents current benchm...

---

### 28. [Mechanistic Knobs in LLMs: Retrieving and Steering High-Order Semantic Features via Sparse Autoencoders](https://arxiv.org/abs/2601.02978)

**Authors**: Ruikang Zhang, Shuo Wang, Qi Su  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.02978v1  

#### Abstract
Recent work in Mechanistic Interpretability (MI) has enabled the identification and intervention of internal features in Large Language Models (LLMs). However, a persistent challenge lies in linking such internal features to the reliable control of complex, behavior-level semantic attributes in lang...

---

### 29. [Dementia-R1: Reinforced Pretraining and Reasoning from Unstructured Clinical Notes for Real-World Dementia Prognosis](https://arxiv.org/abs/2601.03018)

**Authors**: Choonghan Kim, Hyunmin Hwang, Hangeol Chang, Jaemin Kim, Jinse Park, Jae-Sung Lim, Jong Chul Ye  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03018v1  

#### Abstract
While Large Language Models (LLMs) have shown strong performance on clinical text understanding, they struggle with longitudinal prediction tasks such as dementia prognosis, which require reasoning over complex, non-monotonic symptom trajectories across multiple visits. Standard supervised training ...

---

### 30. [Software-Defined Agentic Serving](https://arxiv.org/abs/2601.03197)

**Authors**: Saurabh Agarwal, Marco Laju, Jayanth Srinivasa, Myungjin Lee, Aditya Akella  
**Category**: cs.DC  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03197v1  

#### Abstract
As multi-agent LLM pipelines grow in complexity, existing serving paradigms fail to adapt to the dynamic serving conditions. We argue that agentic serving systems should be programmable and system-aware, unlike existing serving which statically encode the parameters. In this work, we propose a new S...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
