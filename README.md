# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-15 05:57:00 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Parallel Context-of-Experts Decoding for Retrieval Augmented Generation](https://arxiv.org/abs/2601.08670)

**Authors**: Giulio Corallo, Paolo Papotti  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.08670v1  

#### Abstract
Retrieval Augmented Generation faces a trade-off: concatenating documents in a long prompt enables multi-document reasoning but creates prefill bottlenecks, while encoding document KV caches separately offers speed but breaks cross-document interaction. We propose Parallel Context-of-Experts Decodin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Parallel Context-of-Experts Decoding for Retrieval Augmented Generation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Retrieval-Augmented Generation (RAG) 面临两个核心挑战：
- **Prefill 瓶颈**：将多个检索文档拼接成一个长上下文会导致推理时 `prefill` 阶段延迟显著增加。
- **跨文档推理能力下降**：虽然并行 KV cache 编码（如 KV Merge）能加速 `prefill`，但由于缺乏跨文档 attention，模型难以进行 multi-hop 推理。

现有方法在效率与效果之间存在权衡：长上下文拼接支持跨文档交互但慢；独立 KV cache 快但牺牲推理能力。

---

### **提出的新方法：PCED**
作者提出了 **Parallel Context-of-Experts Decoding (PCED)**，一种无需训练的 decoding 框架，其核心思想是：
> 将每个检索到的文档视为一个独立的“专家”（Expert），通过 **decode-time 的对比解码机制** 聚合这些专家的输出，而非依赖共享 attention 上下文。

#### **三大创新点**：
1. **并行、模块化的 KV cache 框架 + 解码时证据聚合**
   - 每个文档独立编码为 KV cache，避免重复 `prefill`。
   - 在生成每一步时，并行运行所有专家前向传播，获得各自的 logits。

2. **基于检索感知的对比解码（Retrieval-aware Contrastive Decoding）**
   - 引入一个无上下文的“业余专家”（Amateur Expert）作为模型先验 $ s_0 $。
   - 对每个专家 $ k $ 的 logits 进行校准：
     $$
     \tilde{s}_k = (1+\beta)s_k - \beta s_0 + \gamma \log r_k
     $$
     其中 $ r_k $ 是该文档的检索相关性得分（融合 retrieval 和 reranker 得分）。
   - 最终选择所有专家中得分最高的 token 作为输出。

3. **动态专家切换实现跨文档推理**
   - 不同生成步骤可由不同专家主导，实现“跳跃式”证据拼接（如图2所示）。
   - 无需构建联合 attention 上下文即可恢复跨文档推理能力。

---

### **相比现有方法的优势**
| 方法 | 优势 | 局限 |
|------|------|------|
| **Concatenation (All)** | 支持跨文档 attention | Prefill 成本高，易受噪声干扰 |
| **KV Merge (e.g., APE)** | 加速 prefill | 无法恢复跨文档交互，性能差 |
| **Agentic (e.g., MapReduce)** | 显式多步聚合 | 多轮 LLM 调用，延迟高 |
| **PCED (本文)** | ✅ 无需训练<br>✅ 保留 KV 模块化<br>✅ 恢复跨文档推理<br>✅ 极大降低 TTFT | 依赖 logits 访问权限 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **LOFT Benchmark**：涵盖 RAG 和 In-Context Learning (ICL) 任务，包含多跳问答（如 HOTPOTQA、MUSIQUE）、单文档问答（RAG NQ）、开放生成（QAMPARI）等。
- **LongBench**：多语言、多任务长上下文理解基准，用于测试单文档与多文档 QA、摘要、代码补全等。
- **合成数据集**：用于控制变量测试延迟（TTFT 和 end-to-end latency），包含 64 个文档（1 个含 secret code，其余为 distractors）。

---

### **实验设置与评估指标**
| 项目 | 设置 |
|------|------|
| **LLM** | Mistral-Nemo-13B-Instruct, Llama-3.1-8B-Instruct, Qwen3-8B（扩展至 128k） |
| **检索器** | BGE-M3（支持 sparse/dense/ColBERT 模式） |
| **重排序器** | BGE-Reranker-V2-M3 |
| **相关性分数融合** | retrieval 与 reranker 分数的调和平均 |
| **解码方式** | Greedy decoding |
| **评估指标** | <br>- RAG 任务：Subspan Exact Match<br>- ICL 任务：Exact Match<br>- LongBench：官方指标（如 F1、EM） |

---

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **标准拼接** | Corpus in Ctx (Single / All) |
| **KV Cache 合并** | APE（Adaptive Parallel Encoding） |
| **Agent-style 聚合** | MapReduce（Map: summarize each doc → Reduce: aggregate） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 1 & Table 2）**

#### **在 LOFT 上的表现（以 Llama-3.1-8B 为例）**
| 任务 | APE | MapReduce | PCED-Dense | Corpus in Ctx (All) |
|------|-----|-----------|------------|---------------------|
| HOTPOTQA | 16.0 | 41.0 | **64.0** | 49.0 |
| MUSIQUE | 4.0 | 8.0 | **21.0** | 7.0 |
| RAG NQ | 9.0 | 50.0 | **85.0** | 79.0 |
| QAMPARI | 7.0 | 68.0 | **76.0** | 72.0 |

> ✅ PCED 在多跳 QA 上大幅超越所有 baseline，最高提升达 **+48 pts**（vs APE）

#### **在 LongBench 上的表现（Qwen3-8B）**
| 任务 | Corpus in Ctx (All) | PCED-Dense |
|------|---------------------|------------|
| HOTPOTQA | 56.3 | **62.6** (+6.3) |
| 2WikiMQA | 44.2 | **49.4** (+5.2) |
| MUSIQUE | 25.3 | **33.3** (+8.0) |
| TriviaQA | 84.0 | **88.2** (+4.2) |
| RepoBench-P (Code) | 51.1 | **60.1** (+9.0) |

> ✅ 即使在完整上下文 baseline 下，PCED 仍全面领先，尤其在复杂推理和代码任务上表现优异。

---

### **与基线方法的对比结果**
- **vs KV Merge (APE)**：PCED 在多跳任务上平均提升 **>40 pts**，说明 decode-time 聚合有效恢复了跨文档推理。
- **vs MapReduce**：PCED 在多数任务上更优（如 HOTPOTQA +23 pts），且仅需一次 decoding，而 MapReduce 需多次 LLM 调用。
- **vs Concatenation (All)**：PCED 在 11/16 设置下优于 full-context 拼接，尤其在噪声环境下更具鲁棒性。

---

### **消融实验结果（Ablation Studies）**

#### **(1) 对比解码 vs 检索先验的作用（Table 3 & Table 6）**
| 设置 | HOTPOTQA (Llama) | NQ (Llama) |
|------|------------------|----------|
| Only Contrastive ($\gamma=0$) | 46 | 52 |
| Only Retrieval Prior ($\beta=0$) | 53 | 70 |
| Full PCED ($\beta>0, \gamma>0$) | **64** | **85** |

> 🔍 结论：两者缺一不可。**检索先验用于抑制噪声**，**对比解码用于放大上下文信号**。

#### **(2) 参数敏感性分析**
- $\beta$（对比强度）：动态策略（AdaCAD-inspired）最稳定。
- $\gamma$（检索门控权重）：$\gamma=2.5$ 为最优平衡点，过高会过度依赖检索排序。

#### **(3) 专家聚合规则（Table 7）**
| 聚合方式 | HOTPOTQA | NQ |
|--------|---------|----|
| Max（本文） | **64** | 85 |
| Mixture (MoE) | 56 | **87** |
| Product (PoE) | 46 | 85 |

> 📌 发现：**Max 更适合 multi-hop QA**（允许 token-level 切换专家）；**MoE 更适合单文档任务**（软融合更平滑）。

#### **(4) 抗噪能力测试（Figure 6）**
- 当 top-k 从 8 增加到 128（即加入更多 distractors），PCED 性能几乎不变（如 NQ 保持 ~85）。
- 表明 **retrieval prior 有效抑制了无关专家的影响**。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **跨文档推理可在 decode time 实现**：无需共享 attention 上下文，通过 expert logit fusion 即可恢复 multi-hop 推理能力。
2. ✅ **检索分数不仅是排序工具，更是 decoding 先验**：利用 retrieval/reranker 分数作为 gating signal 可显著提升抗噪能力。
3. ✅ **PCED 效率极高**：Time-To-First-Token (TTFT) 较 vanilla 方法快 **180×**（0.14s vs 25.50s），端到端延迟降低 ~1.7×。
4. ✅ **优于 full-context baseline**：在多个 benchmark 上匹配甚至超过将所有文档拼接进 context 的方法。

---

### **局限性**
1. **依赖模型 logits 访问权限**  
   - PCED 需要获取每个专家的 token-level logits，因此不适用于仅提供 sampled token 或 logprob 子集的 API 模型（如 GPT-4）。

2. **对检索质量敏感**  
   - 若关键证据未被检索到或评分过低，对应专家可能被忽略，无法恢复信息。

3. **存储开销较大**  
   - 需预先存储每个文档的 KV cache，存储成本随语料规模线性增长（例如 LOFT 数据集约需 11GB FP16 存储）。
   - 适合读多写少的静态知识库场景。

---

### **未来工作方向**
- **端到端学习 expert selection**：让 LLM 自主学习在每一步选择哪个 context 输入，减少对外部 retrieval pipeline 的依赖。
- **轻量化 KV cache 存储**：探索量化、压缩、稀疏化等技术降低存储成本。
- **适配闭源模型**：设计 proxy-based 或 token-level approximation 方法，使 PCED 可用于 API-only 模型。

---

## **总结**
PCED 提出了一种全新的视角：**将 RAG 中的“上下文融合”从 attention 层转移到 decoding 层**。它通过“专家系统 + 对比解码”的范式，在保持高效并行处理的同时，成功恢复了跨文档推理能力，实现了 **速度、准确性、鲁棒性** 的三重提升，为大规模 RAG 系统提供了极具前景的解决方案。

</details>

---

### 2. [Contrastive Bi-Encoder Models for Multi-Label Skill Extraction: Enhancing ESCO Ontology Matching with BERT and Attention Mechanisms](https://arxiv.org/abs/2601.09119)

**Authors**: Yongming Sun  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.09119v1  

#### Abstract
Fine-grained labor market analysis increasingly relies on mapping unstructured job advertisements to standardized skill taxonomies such as ESCO. This mapping is naturally formulated as an Extreme Multi-Label Classification (XMLC) problem, but supervised solutions are constrained by the scarcity and ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Contrastive Bi-Encoder Models for Multi-Label Skill Extraction: Enhancing ESCO Ontology Matching with BERT and Attention Mechanisms

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**劳动力市场分析中技能提取的标注数据稀缺问题**，尤其是在非英语语境下（如中文招聘广告），将非结构化职位描述映射到标准化技能本体（如ESCO）面临巨大挑战。传统监督学习方法受限于大规模、对齐的标注数据成本高昂且难以获取。

### 提出的新方法与创新思路
作者提出了一种**零样本（zero-shot）技能提取框架**，无需依赖人工标注的招聘广告训练数据，其核心创新包括：

- **层次约束的合成数据生成（Hierarchy-conditioned synthetic supervision）**  
  利用大型语言模型（LLM, DeepSeek-V3）从ESCO技能定义中自动生成训练样本，并引入**ESCO Level-2类别作为结构约束**，确保多技能共现句子在语义上更连贯、真实，避免随机组合导致的语义漂移或不合理的标签组合。

- **基于对比学习的双编码器架构（Contrastive bi-encoder for XMLC）**  
  构建一个共享嵌入空间的对比学习模型，将招聘广告句子与ESCO技能描述进行对齐。模型以 `BERT` 为骨干，结合 `BiLSTM` 和 `attention pooling`，增强对长句、复合要求的理解能力。

- **端到端抽取流水线（End-to-end extraction pipeline）**  
  引入一个上游的 `RoBERTa` 二分类过滤器，用于识别并剔除非技能相关句子（如薪资、公司介绍等），提升整体系统的精度和效率。

### 相比现有方法的优势
- **摆脱对标注数据的依赖**：通过LLM生成高质量合成数据，实现真正的zero-shot迁移。
- **更强的语义匹配能力**：相比TF-IDF、BM25等基于词频的方法，能处理同义替换、隐含表达和上下文依赖。
- **更好的多标签建模**：利用ESCO层级结构引导多技能生成，提升了多标签预测的合理性和一致性。
- **可扩展性强**：适用于大标签空间（13,890个ESCO Level-4技能），支持近似最近邻检索，适合实际应用部署。

---

## 2. 核心实验方法和设置

### 数据集
| 类型 | 名称 | 描述 |
|------|------|------|
| **技能本体** | ESCO v1.1 | 包含13,890个Level-4技能节点，每个技能有名称和文本定义；作者将其手动翻译成中文用于训练与推理。 |
| **真实招聘数据** | Zhaopin.com | 来自中国最大招聘平台之一的真实职位广告，涵盖2015–2023年，从中采样20万条广告，进一步切分为句子级单元用于评估。 |

### 实验设置
- **模型架构**：
  - 编码器：`bert-base-chinese` 作为主干
  - 增强模块：`BiLSTM` + `attention pooling` + 投影层（输出维度d=128）
  - 损失函数：margin-based contrastive loss（margin=0.5），每正样本配K个负样本
- **训练方式**：
  - 使用合成数据进行对比学习训练
  - 句子过滤器使用 `RoBERTa-base` 在合成正负样本上微调
- **推理流程**：
  1. 分句 → 2. 过滤非技能句 → 3. 句子编码 → 4. 与预计算的ESCO技能向量做余弦相似度检索 → 5. 聚合所有句子结果取并集得到岗位级预测

### 评估指标
- **句子级检索性能**：MRR、Recall@K、mAP
- **岗位级多标签分类性能**：Precision@K、Recall@K、F1@K（K=1,3,5）、AUPRC
- **过滤器性能**：Accuracy、Precision、Recall、F1、PR曲线
- **合成数据质量**：GPT-2 perplexity、ROC-AUC（区分技能/非技能句的能力）

### 基线方法对比
| 基线方法 | 类型 | 说明 |
|--------|------|------|
| TF-IDF | 词袋模型 | 将句子与ESCO定义做TF-IDF向量化后计算余弦相似度 |
| BM25 | 检索模型 | 经典的信息检索算法，用于比较 |
| Model A: Standard BERT (Single Label) | 单标签对比模型 | 仅使用单技能样本训练的标准BERT双编码器 |
| Model B: BERT + BiLSTM + Attention (Single Label) | 单标签优化模型 | 加入序列建模和注意力池化的单标签模型 |
| Keyword Matching / Zero-Shot NLI | 规则/通用模型 | 用于过滤任务的对比基线 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（真实数据上的end-to-end表现）
| 模型 | AUPRC | F1 |
|------|-------|-----|
| Model A (Standard BERT) | 0.72 | 0.69 |
| Model B (Optimized, Single) | 0.82 | 0.75 |
| **Model C (Proposed, Multi-Label)** | **0.90** | **0.80** |

> 在真实中文招聘广告上的zero-shot测试中，**Model C达到F1@5 = 0.80**，显著优于基线。

此外，在最终调参后的设定下，系统实现了 **F1@5 = 0.72** 的报告值（见摘要），表明其具备较强的实用价值。

### 与基线方法的对比结果
- **相比TF-IDF和标准BERT基线**：
  - Model C在Recall@5上高出约40个百分点（Table 3）
  - 在真实数据上F1提升超过10个百分点
- **过滤器效果**：
  - RoBERTa过滤器准确率达 **87.5%**，F1为 **0.874**
  - 显著优于关键词规则（F1=0.75）和零样本NLI模型（F1=0.80）
  - 移除负样本训练会导致假阳性上升，验证了显式“无技能”监督的重要性

### 消融实验结果
| 配置 | MRR | 吞吐量 (samples/s) |
|------|-----|------------------|
| 移除BiLSTM/Attention | 0.55 | — |
| Margin = 0.1 | 0.55 | — |
| **Margin = 0.5 (选中)** | **0.65** | 170 |
| Margin = 0.8 | 0.58 | — |
| Num_Negatives = 1 | 0.55 | 250 |
| **Num_Negatives = 8 (选中)** | **0.68** | 170 |
| Num_Negatives = 10 | 0.68 | 150 |

- **最佳配置**：margin=0.5，负样本数=8，在MRR与训练效率之间取得平衡
- **注意力可视化**：模型赋予编程语言、任务动词等关键词更高权重，体现语义聚焦能力
- **合成数据质量**：
  - 层次约束的多技能生成（Multi-Skill Level-2）**perplexity最低（15.2）**，优于单技能（18.5）和随机多技能（22.1）
  - ROC曲线下面积更大，说明更具判别力

---

## 4. 关键结论和发现

### 主要发现
1. **层次约束显著提升合成数据质量**：基于ESCO Level-2类别的多技能生成不仅提高了语言流畅性（更低perplexity），也增强了下游任务的判别能力。
2. **RoBERTa过滤器有效去除噪声**：前置二分类器可高效剔除非技能内容，减少误报，提升整体系统precision。
3. **Model C在合成与真实数据上均表现最优**：
   - 在合成验证集上MRR达0.76，Recall@5达0.48
   - 在真实中文招聘广告上实现F1@5=0.80，大幅超越TF-IDF与标准BERT
4. **错误分析显示主要挑战仍在于**：
   - **False Negatives**：遗漏技能（最常见）
   - **Taxonomy Misalignment**：技能定义过于细粒度或重叠，导致相近技能混淆

### 方法的局限性
- **依赖ESCO翻译质量**：中文版ESCO由人工翻译，可能存在偏差或不一致。
- **合成数据仍存在分布差距**：尽管有层次约束，LLM生成的句子可能无法完全覆盖真实招聘语言的多样性与噪声。
- **未充分利用ESCO层级结构进行推理**：当前仅为生成阶段提供约束，未来可在解码或损失函数中进一步融合taxonomy结构。
- **zero-shot设定限制泛化边界**：若目标领域与ESCO差异过大（如新兴科技岗位），性能可能下降。

### 未来工作方向
1. **多语言与跨语言对齐**：拓展至其他非英语市场，探索XLM-R或mBERT进行跨语言技能映射。
2. **层次感知的学习目标**：设计graph-based regularization或taxonomy-consistent decoding机制，更好地利用ESCO的树形结构。
3. **适配更多技能本体**：推广至O*NET或其他行业特定技能体系。
4. **动态更新机制**：应对技能演进快的特点，构建可持续更新的自动化标注流水线。

--- 

> ✅ **总结一句话**：本文提出一种无需人工标注的zero-shot技能提取框架，通过**层次约束的LLM合成数据 + 对比双编码器 + 前置过滤器**，在真实中文招聘广告上实现了高性能的ESCO技能匹配，为劳动力市场智能提供了可扩展、低成本的技术路径。

</details>

---

### 3. [Hidden States as Early Signals: Step-level Trace Evaluation and Pruning for Efficient Test-Time Scaling](https://arxiv.org/abs/2601.09093)

**Authors**: Zhixiang Liang, Beichen Huang, Zheng Wang, Minjia Zhang  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.09093v1  

#### Abstract
Large Language Models (LLMs) can enhance reasoning capabilities through test-time scaling by generating multiple traces. However, the combination of lengthy reasoning traces with multiple sampling introduces substantial computation and high end-to-end latency. Prior work on accelerating this process...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Hidden States as Early Signals: Step-level Trace Evaluation and Pruning for Efficient Test-Time Scaling**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
大型语言模型（LLMs）在推理任务中通过 **test-time scaling**（如 self-consistency, SC）生成多条推理路径以提升准确性，但该过程存在两个关键瓶颈：
- **计算开销大**：长链推理 + 多路径采样导致大量 token 生成，带来高延迟。
- **资源利用低效**：传统方法（如基于相似性或置信度的剪枝）信号不可靠，且未考虑 **GPU 内存瓶颈** 导致的排队等待时间。

现有方法仅关注减少生成 token 数量，忽略了系统级延迟的主要来源——KV Cache 占用引发的 **preemption 和 waiting queue**。

---

### **提出了什么新方法或新思路**
提出 **STEP**（Step-level Trace Evaluation and Pruning），一种结合 **隐藏状态评估** 与 **GPU内存感知剪枝** 的新型推理加速框架。

#### **核心创新点**：
1. **基于隐藏状态的步级质量评估（Step Scorer）**
   - 利用 LLM 在 `<think>` 到 `</think>` 之间每个推理步骤结束时的 **last-layer hidden state** 作为输入。
   - 训练一个轻量级两层 MLP（step scorer）预测每一步的质量，并聚合为 trace-level 分数。
   - 实验证明：**早期隐藏状态已能有效区分正确与错误推理路径**。

2. **GPU 内存饱和触发剪枝机制**
   - 当 KV Cache 接近 GPU 显存上限时，立即剪枝 **trace score 最低** 的路径并释放其 KV Cache。
   - 避免因显存不足导致的 trace 被抢占进入等待队列，从而消除 **waiting overhead**。

3. **系统-算法协同设计**
   - 不再只优化“生成多少 token”，而是从 **inference system behavior** 出发，解决实际端到端延迟瓶颈。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **信号可靠性** | 使用 hidden state 替代 confidence 或文本相似性，避免 miscalibration 和误删多样正确路径 |
| **剪枝时机** | 动态响应 GPU 内存压力，而非固定阈值或时间表，更贴近真实系统瓶颈 |
| **效率提升** | 同时减少 token 生成 + 消除等待时间，实现更高加速比 |
| **精度提升** | 因更早终止错误路径，保留高质量 trace，最终 accuracy 反而提高 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **AIME-25**：高难度数学竞赛题（Art of Problem Solving）
- **HMMT-24 / HMMT-25**：Harvard-MIT Mathematics Tournament 高中数学竞赛题
- **GPQA-Diamond (GPQA-D)**：研究生级别科学问答基准，涵盖物理、生物、化学等

这些数据集具有高复杂度、长推理链特点，适合测试 test-time scaling 效果。

---

### **实验设置和评估指标**

#### **模型**
- **Qwen3-4B-Thinking-2507**
- **DeepSeek-R1-0528-Qwen3-8B**
- **Phi-4-reasoning-plus (14B)**

均为开源强推理能力模型。

#### **评估指标**
| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | 正确率（主要任务性能指标） |
| **Average Output Token Usage (×10³)** | 平均输出 token 数量 |
| **Inference Latency (s)** | 端到端推理延迟（最关键效率指标） |

#### **采样设置**
- 所有方法统一使用 **N = 64** 条 trace 进行 self-consistency 类采样。
- 使用修改版 **vLLM** 框架，在单张 **96GB NVIDIA GH200 GPU** 上运行。

---

### **基线方法对比**
| 方法 | 简介 |
|------|------|
| **CoT** | 单一思维链，无采样 |
| **SC (Self-Consistency)** | 生成 64 条 trace，多数投票选答案 |
| **Slim-SC** | 基于跨 trace 文本相似性剪枝冗余路径 |
| **DeepConf** | 基于模型内部 confidence 动态终止低置信 trace |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 1）**

| 模型 | 方法 | AIME-25 Acc (%) | HMMT-25 Acc (%) | Latency Reduction vs SC |
|------|------|------------------|------------------|------------------------|
| Qwen3-4B | STEP | **88.3** (+1.6) | **70.0** (+5.0) | ↓ ~53% |
| DeepSeek-8B | STEP | **85.0** (+1.7) | **73.3** (+3.3) | ↓ ~60% |
| Phi-4 | STEP | **87.5** (+0.8) | **75.8** (+0.8) | ↓ ~61% |

> ✅ STEP 在所有模型上均 **优于 SC、Slim-SC 和 DeepConf**，平均降低延迟 **45%-70%**，同时提升 accuracy **+0.4 至 +7.5 个百分点**。

---

### **与基线方法的对比结果**

#### **Latency 加速效果显著**
- 在 Phi-4 + HMMT-24 场景下：
  - SC 耗时 **2405 秒**
  - STEP 仅需 **630 秒** → **3.8× speedup**
- 在 DeepSeek-8B + AIME-25：
  - DeepConf 耗时 1475s，STEP 仅 891s → **1.7× 更快**

#### **Token 使用量合理**
- 相比 SC 明显减少 token（但仍略高于 DeepConf），说明剪枝有效。
- 但 **STEP 的 latency 仍更低**，表明其优势不仅来自 token 减少，更源于 **waiting time 消除**。

#### **Waiting Time 分析（Table 2）**
| 方法 | Waiting Time (s) | Decode Time (s) |
|------|------------------|-----------------|
| SC | 1526 | 1256 |
| DeepConf | 263 (69+194) | 1406 |
| Slim-SC | 1155 | 983 |
| **STEP** | **0** | **1024** |

> 🔥 STEP 完全消除了 waiting time，这是其端到端加速的核心原因。

---

### **消融实验与分析**

#### **(1) Step Scorer 的排序能力（Fig. 5）**
- 使用 **Pairwise RankAcc** 衡量 scorer 区分正确/错误 trace 的能力。
- 结果显示：**hidden state scorer 明显优于 token-level confidence**（DeepConf 使用）。
- 即使只看前 25% 的推理步骤，RankAcc 已达 ~0.7，证明早期信号可靠。

#### **(2) Latency Scaling 实验（Fig. 4）**
- 在不同采样预算（16/32/64）下，STEP 始终在相同时间内达到更高 accuracy。
- 例如：Qwen3-4B + HMMT-25，STEP 用 40% 时间达到 SC 的 accuracy。

#### **(3) GPU Memory 敏感性测试（Table 3）**
- 将最大 GPU 利用率从 0.5 到 0.9 变化，accuracy 波动仅为 **70.1 ± 1.8%**。
- 表明 STEP 对显存限制不敏感，即使早期剪枝也能保持稳定性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **隐藏状态是可靠的早期质量信号**  
   LLM 推理过程中，**early step 的 hidden state 已蕴含足够信息判断整条路径是否可能成功**，可用于精准剪枝。

2. **系统瓶颈在于 waiting time，而非 decode time**  
   实际延迟中，**等待 KV Cache 释放的时间占主导（~59%）**，单纯减少 token 无法根本解决问题。

3. **memory-aware pruning 是高效 TTS 的关键**  
   将剪枝决策与 GPU 内存状态绑定，可彻底消除排队，实现最大加速。

4. **STEP 实现 accuracy 与 efficiency 双赢**  
   不仅提速 45%-70%，还因更优 trace 选择提升了最终准确率。

---

### **方法的局限性**
1. **伪标签噪声问题**  
   Step scorer 使用 trace-level 正确性作为每个 step 的标签，存在标注噪声，尤其当 trace 中部分步骤错误但最终正确时。

2. **依赖特定部署架构**  
   Memory-triggered pruning 效果依赖于 vLLM 等支持动态调度的 inference engine，在其他系统上可能表现不同。

3. **领域迁移风险**  
   Step scorer 在数学任务上训练，迁移到其他领域（如代码、常识推理）需重新微调。

---

### **未来工作方向**
- 探索更精细的 step-level 标注方式（如人工标注关键转折点）。
- 扩展 STEP 到多 GPU 分布式推理场景。
- 结合 speculative decoding 或 other early-exit 技术进一步加速。
- 开发通用 step scorer，支持跨任务、跨模型泛化。

---

> 📌 **总结一句话**：  
> **STEP 通过“隐藏状态评估 + 显存感知剪枝”，实现了 test-time scaling 的 accuracy 与 latency 全面超越，揭示了系统-算法联合优化在高效推理中的巨大潜力。**

</details>

---

### 4. [ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms](https://arxiv.org/abs/2601.08166)

**Authors**: Mohammad Pivezhandi, Mahdi Banisharif, Abusayeed Saifullah, Ali Jannesari  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08166v1  

#### Abstract
Dynamic voltage and frequency scaling (DVFS) and task-to-core allocation are critical for thermal management and balancing energy and performance in embedded systems. Existing approaches either rely on utilization-based heuristics that overlook stall times, or require extensive offline profiling for...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统嵌入式系统中的 **Dynamic Voltage and Frequency Scaling (DVFS)** 和任务到核心分配策略存在以下问题：
- **基于利用率的启发式方法**（如Linux ondemand governor）忽略执行过程中的 **stall time**，导致频率调节不精准。
- **基于离线表的方法**（如precise scheduler）需要对所有频率-核心组合进行详尽的性能分析，耗时长达8–12小时，无法适应动态工作负载。
- 缺乏对新工作负载的泛化能力，每次新增程序都需要重新执行完整的profiling流程。

这些问题限制了系统在动态、资源受限环境下的能效与实时响应能力。

---

### 提出的新方法与创新思路

本文提出 **ZeroDVFS** ——一种基于模型的分层多智能体强化学习（MARL）框架，结合大语言模型（LLM）语义特征提取，实现零样本（zero-shot）部署。

#### 核心创新点包括：

1. ✅ **LLM驱动的语义特征提取（LLM-based Semantic Feature Extraction）**
   - 利用 **GPT-4o、Claude Sonnet、DeepSeek-V3** 等LLM从OpenMP源码中提取13个代码级语义特征（如 `algorithmic_complexity`, `memory_access_pattern`, `load_balance`），无需运行程序即可完成特征刻画。
   - 支持 **zero-shot 预测**：对于未见过的工作负载，也能通过语义特征预测其性能表现，避免重复profiling。

2. ✅ **模型驱动的分层多智能体强化学习（Model-Based Hierarchical MARL）**
   - 引入两个协作智能体：
     - **Profiler Agent**：优化能耗与 **makespan**，决定核心数量和频率。
     - **Temperature Agent**：防止热集中，优先选择温度较低的核心。
   - 使用 **Dyna-Q 启发式架构**，将真实经验与模型生成的合成数据结合，显著提升训练效率。

3. ✅ **准确的环境建模（Accurate Environment Modeling）**
   - 构建回归模型（如FCN、Conv1D）预测温度变化和性能状态，推理延迟低于5ms。
   - 模型精度比现有方法高 **6倍以上**（MSE更低）。

4. ✅ **跨平台迁移与快速决策**
   - 支持 **zero-shot 跨平台迁移**：在Jetson TX2上训练的模型可直接用于Orin NX、RubikPi等不同硬件平台。
   - 决策延迟极低：首次决策仅需 **3.5–8.0秒**（含LLM特征提取），后续决策仅 **358ms**。

---

### 相比现有方法的优势

| 维度 | 传统方法（如precise scheduler） | ZeroDVFS |
|------|-------------------------------|--------|
| **Profiling 开销** | 8–12小时/基准测试 | 一次性LLM调用（<5秒） |
| **新工作负载适应性** | 必须重新profiling | 零样本部署，无需执行 |
| **决策延迟** | 表查找：亚毫秒，但表生成慢 | 首次：3.5–8.0s；后续：358ms |
| **能效与性能** | 接近最优但静态 | 动态优化，更优 |
| **跨平台迁移** | 不支持，需重做表 | 支持，少量微调即可 |

---

## 2. 核心实验方法和设置

### 数据集
- **BOTS (Barcelona OpenMP Tasks Suite)**：包含FFT、Strassen、N-Queens等并行任务。
- **PolybenchC**：涵盖线性代数、Stencil计算、数据挖掘等典型HPC负载。
- 共计 **42个OpenMP基准程序**，覆盖多样化的并行模式与内存访问行为。

---

### 实验平台
| 平台 | 架构 | 核心数 | 热区数 |
|------|------|-------|--------|
| **Jetson TX2** | ARM Cortex-A57 + Denver 2 | 6 | 8 |
| **Jetson Orin NX** | ARM Cortex-A78AE | 8 | 9 |
| **RubikPi** | Qualcomm Kryo 585 | 8 | 36 |
| **Intel Core i7-8650U** | x86_64 | 4 | - |

所有实验均使用Linux内核工具进行功耗、温度、上下文切换等监控。

---

### 评估指标
- **Energy Efficiency**：总能耗（mJ）
- **Makespan**：任务完成时间（s）
- **Temperature**：平均核心温度（℃）
- **Decision Latency**：调度决策耗时
- **Convergence Speed**：RL收敛所需episode数
- **MAPE / R²**：预测模型准确性

---

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **Linux ondemand governor** | 启发式 | 基于利用率动态调频 |
| **Precise Scheduler [7]** | 表驱动 | 离线profiling生成最优配置表 |
| **zTT [30]** | 单智能体RL | 基于模型无关的强化学习 |
| **GearDVFS** | 启发式 | 利用利用率和帧率调整频率 |
| **HiDVFS_S, MAML, SAMBRL** | 其他RL变体 | 作为次要对比 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | ZeroDVFS | Precise Scheduler | 提升幅度 |
|------|---------|------------------|----------|
| **Energy Consumption** | 9.1 mJ | 75.5 mJ | **7.09× 更优** |
| **Makespan** | 1.13 s | 5.96 s | **4.0× 更快** |
| **决策延迟（首次）** | 3.5–8.0 s | ~8小时 | **8,300× 更快** |
| **决策延迟（后续）** | 358 ms | ~8小时 | **80,000× 更快** |
| **温度预测误差（MSE）** | 0.40058 | 2.5 [25] | **>6× 更准确** |
| **收敛速度** | 20 episodes | >400 episodes | **20× 更快收敛** |

> 注：上述结果基于Jetson TX2上的BOTS FFT基准测试（输入大小262144）。

---

### 与基线方法的对比结果

- 在 **最终性能排名** 中，ZeroDVFS以 **1.13s makespan** 居首，远优于：
  - HiDVFS_S（1.33s）
  - MAML（1.75s）
  - zTT（1.88s）
  - Precise Scheduler（5.96s）
  - GearDVFS（9.81s）

- **能量归一化图示** 显示：
  - 相比Precise Scheduler，ZeroDVFS节能 **85.9%**
  - 相比zTT，节能 **3.43×**

- **决策稳定性更高**：ZeroDVFS在训练过程中makespan和能耗波动小，而GearDVFS方差极大。

---

### 消融实验结果（Ablation Study）

#### （1）模型驱动 vs. 模型无关（Model-Based vs. Model-Free）
- **MAMBRL D3QN**（模型驱动）比 **MAMFRL D3QN**（模型无关）收敛更快（20 vs. 40+ episodes）。
- 合成数据显著减少样本需求，提升样本效率。

#### （2）LLM特征的作用
- 在已知工作负载上，加入LLM特征对预测精度提升有限（R² ≈ 0.94 vs. 0.944），因为已有足够硬件特征。
- 但在 **zero-shot 场景下**，LLM特征是唯一可用的信息来源，使模型具备泛化能力。

#### （3）跨平台迁移效果（Transfer Learning）
| 迁移路径 | MAPE | R² | 说明 |
|--------|------|----|------|
| TX2 → Orin NX | 64.5% | 0.90 | 可接受，适合快速部署 |
| TX2 → RubikPi | 73.2% | 0.80 | 存在域偏移，但仍保留趋势 |

- 少量微调（5–50样本）可进一步降低MAPE至60%左右。
- 即使MAPE较高，**相对排序保持稳定**（Spearman ρ > 0.7），足以支持有效调度。

#### （4）LLM一致性分析
| 特征 | 三模型一致率（All3） |
|------|------------------|
| `dominant_operation` | 73.8% |
| `algorithmic_complexity` | 59.5% |
| `false_sharing_risk` | 14.3% |
| `cache_behavior_pattern` | 16.7% |

- 高共识特征可用于强约束决策，低共识特征由XGBoost自动降权处理。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLM语义特征可有效替代传统profiling**，实现 **zero-shot 性能预测**，大幅降低部署成本。
2. ✅ **模型驱动的MARL框架** 显著加速RL收敛（20×），同时保证高质量决策。
3. ✅ **首次决策延迟比表驱动方法快8,300倍**，支持在动态嵌入式系统中实用化部署。
4. ✅ 在多个异构平台上验证了方法的有效性，展现出良好的 **跨平台迁移能力**。
5. ✅ ZeroDVFS在能效和性能上全面超越主流启发式和RL基线，尤其在复杂并行负载中优势明显。

---

### 方法的局限性
1. **当前仅支持单DAG OpenMP程序**，尚未扩展至并发或多任务场景。
2. **zero-shot跨平台MAPE高达64–73%**，仍需少量fine-tuning才能达到理想精度。
3. **LLM特征提取依赖单文件程序**，多文件项目需手动拼接或分层分析。
4. **缺乏置信区间量化**：实验重复次数有限，统计显著性有待加强。
5. 当前使用商业LLM API（GPT-4o等），存在网络依赖和成本问题。

---

### 未来工作方向
1. 扩展至 **并发工作负载** 和 **GPU卸载决策**。
2. 支持 **多文件项目分析**，引入模块化LLM解析。
3. 探索 **领域专用LLM微调**（如HPC代码语料库），提升特征提取准确性。
4. 部署 **本地化/蒸馏版LLM**（如CodeLlama、DeepSeek-Coder），实现完全离线边缘部署。
5. 加强统计分析，采用多随机种子评估方差。
6. 研究 **LLM输出的不确定性估计**，为调度提供风险感知能力。

---

> **总结**：ZeroDVFS通过融合 **LLM语义理解** 与 **模型驱动的MARL**，实现了高性能、低开销、可泛化的嵌入式调度方案，为未来智能边缘系统的自适应资源管理提供了新范式。

</details>

---

### 5. [Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding](https://arxiv.org/abs/2601.08653)

**Authors**: Zenghua Liao, Jinzhi Liao, Xiang Zhao  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.08653v1  

#### Abstract
Large Language Models are rapidly emerging as web-native interfaces to social platforms. On the social web, users frequently have ambiguous and dynamic goals, making complex intent understanding-rather than single-turn execution-the cornerstone of effective human-LLM collaboration. Existing approach...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前 Large Language Models (LLMs) 在处理用户意图时，尤其是在社交网络等复杂场景下，用户常以模糊、动态的方式表达需求。现有方法（如 Mistral-Interact 和 ITIU）通过顺序或并行提问进行意图澄清，但**忽略了澄清问题之间的逻辑依赖关系**，导致交互不连贯、建议不合理（例如“12月在冲绳潜水”），从而增加用户的**认知负荷 (Cognitive Load)**。

论文指出，这一核心挑战是：**如何建模澄清问题间的逻辑依赖，以实现高效且低摩擦的复杂意图理解**。

---

### **提出的新方法：Prism 框架**
Prism 是一个基于 **Cognitive Load Theory (CLT)** 的新型框架，旨在通过**逻辑一致的意图澄清**降低用户认知负担。其核心思想是将复杂意图视为“白光”，通过“棱镜”（即逻辑驱动的模式）将其分解为有序的“光谱”元素，指导澄清流程。

#### **四大模块设计**：
1. **Complex Intent Decomposition Module**  
   - 构建了 **CID (Complex Intent Decomposition) 数据集**，覆盖 27 个领域、429 种意图，显式标注元素间的先决依赖关系。
   - 支持“检索或构造”范式：若用户意图匹配 CID 中已有条目，则复用其层级结构；否则通过 few-shot 方式从相似意图中泛化生成。

2. **Logical Clarification Generation Module**  
   - 根据分解出的层级结构组织澄清问题：**独立问题在同一轮并行呈现（如交互表格），依赖问题则按序分轮提问**，确保逻辑一致性。

3. **Intent-Aware Reward Module**  
   - 设计了一个 **intent-aware reward 函数**，结合 **token-level intent importance** 和 **generation confidence** 来评估澄清轨迹质量。
   - 利用 **Monte Carlo Sampling** 模拟大规模用户-LLM 交互，生成高质量训练数据。

4. **Self-Evolved Intent Tuning Module**  
   - 采用自演进方式迭代优化 LLM：使用高奖励轨迹对开源模型（如 LLaMA-3.1-8B-Instruct）进行 SFT 和 DPO 微调，并在后续轮次中用微调后的模型作为新策略生成更优数据。

---

### **相比现有方法的优势**
| 维度 | 传统方法（Mistral-Interact, ITIU） | Prism |
|------|-------------------------------|-------|
| **逻辑依赖建模** | 忽略问题间依赖，假设问题独立 | 显式建模依赖，分层提问 |
| **交互效率** | ITIU 并行提问快但易混乱 | 平衡并行与顺序，减少冗余轮次 |
| **用户体验** | 用户需自行管理逻辑 | 由系统引导，降低认知负荷 |
| **泛化能力** | 依赖预定义行为 | 支持多样化意图发现 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **TIN** [14]: 200 个测试实例，人工标注 + GPT-4 辅助。
- **IN3** [21]: 108 个测试实例，包含更多澄清问题。
- **ABP** [36]: 319 个测试实例，涵盖多样领域和意图。

所有数据集均被手动划分为 **简单意图** 和 **复杂意图** 场景，用于评估模型在不同难度下的表现。

---

### **实验设置**
- **基线模型**：统一使用 `Mistral-7B-Instruct-v0.3` 和 `LLaMA-3.1-8B-Instruct` 作为 backbone。
- **对比方法**：
  - **Mistral-Interact**: 顺序 Q&A 澄清。
  - **ITIU**: 表格形式并行提问。
  - **CoLLABLLM**: 多轮感知奖励机制。
- **Prism 变体**：
  - **Prism-SFT**: 基于监督微调。
  - **Prism-DPO**: 基于直接偏好优化。

训练环境：8×80GB A100 GPU，耗时约 14.5 小时。

---

### **评估指标**

#### **(1) 澄清交互 (Clarification Interaction)**
- **Intents Cover Rate (%)**: 覆盖真实澄清问题的比例。
- **Logical Conflict Rate (%)**: 违反先决依赖的澄清问题比例（越低越好）。
- **Average Interaction Turns**: 平均交互轮数。
- **Average Questions Per Turn**: 每轮平均提问数量。
- **Options Presenting Rate (%)**: 提供参考选项的比例。
- **Options Reasonable Rate (%)**: 所提供选项合理的比例。

#### **(2) 意图执行 (Intent Execution)**
集成到 **XAgent** 自主代理框架中，评估任务完成质量：
- **IBLEU**: 输出与参考答案的 BLEU 分数。
- **Faithful**: 输出是否忠实满足澄清后的意图。
- **Unnecessary Sub-tasks (US)** / **General Sub-tasks (GS)**: 不必要或过于宽泛的子任务比例。
- **Tool Invocations (TI)**: 工具调用次数，反映执行效率。

#### **(3) 认知负荷 (Cognitive Load)**
多维度评估：
- **行为指标**：任务耗时、对话 token 数量。
- **主观评分**：用户对交互体验打分（1–10）。
- **生理指标**：EEG 功率谱密度 (PSD)，衡量大脑活跃度与认知负荷。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### **表 1：澄清交互性能（TIN 数据集，复杂意图场景）**
| 方法 | Logical Conflict Rate (%) ↓ | Intents Cover Rate (%) ↑ | Avg. Turns ↓ |
|------|-----------------------------|----------------------------|--------------|
| Mistral-Interact | 39.00 | 46.24 | 5.87 |
| ITIU | 51.50 | 49.18 | 1.78 |
| CoLLABLLM | 30.00 | 57.08 | 6.14 |
| **Prism-DPO (LLaMA)** | **11.50** | **60.09** | **1.64** |

> ✅ **逻辑冲突率降至 11.5%**，显著优于其他方法。

#### **表 2：意图执行性能（相对提升，Prism-DPO vs Baseline）**
| 指标 | 简单意图场景 | 复杂意图场景 | 提升幅度 |
|------|-------------|-------------|---------|
| IBLEU | +10.43% | +9.4% | 高 |
| Faithful | +19.1% | +17.7% | 高 |
| US (不必要的子任务) | -0.59 | -1.08 | ↓31.4% |
| GS (泛化子任务) | -0.19 | -0.09 | ↓19.57% |
| TI (工具调用) | -1.23 | -1.22 | ↓22.26% |

> ✅ 显著减少冗余操作，提升执行效率。

#### **图 3：认知负荷实验结果**
- **任务耗时**：Prism 比 Mistral-Interact **减少 34.8%**，比 CoLLABLLM **减少 22.3%**。
- **总 token 数**：Prism 控制在 **1000 以下**，远低于其他方法。
- **用户评分**：Prism 平均得分为 **8.35**，其中 88.6% 用户评为“良好”及以上。
- **EEG PSD 分析**：Prism 对应的脑电活动最平稳，表明**认知负荷最低**。

#### **消融实验（隐含分析）**
- **逻辑依赖建模有效性**：当从简单转向复杂意图时，Mistral-Interact 和 ITIU 的性能下降明显（见 Figure 5），而 Prism 下降幅度最小，说明其对复杂场景鲁棒性强。
- **自演进训练效果**：经过三轮 self-evolved intent tuning，数据质量和模型性能持续提升，验证了闭环优化的有效性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **逻辑依赖建模至关重要**：忽视澄清问题间的先决关系会导致建议不切实际，显著增加用户认知负担。
2. **Prism 实现了逻辑一致性与交互效率的平衡**：既保持低交互轮次（≈1.6 轮），又大幅降低逻辑冲突（11.5%）。
3. **显著改善用户体验**：
   - 用户满意度提升 **14.4%**。
   - 任务完成时间缩短 **34.8%**。
   - 生理层面认知负荷最低（EEG 验证）。
4. **强泛化能力**：在 TIN、IN3、ABP 三个数据集上 consistently outperform 所有 baseline，且适用于不同 backbone 模型。

---

### **方法的局限性**
- **依赖 CID 数据集构建质量**：虽然 CID 包含 429 种意图，但仍可能无法覆盖极端长尾场景。
- **few-shot 构造的可靠性**：对于完全新颖的意图，依赖语义相似性检索可能存在偏差。
- **用户模拟器的保真度**：Monte Carlo Sampling 使用 LLM 模拟用户行为，虽高效但未必完全反映真实人类决策。

---

### **未来工作方向**
- 扩展 CID 至更多领域和语言。
- 引入在线学习机制，让模型在部署过程中持续更新意图模式。
- 探索多模态输入下的复杂意图理解（如图文混合请求）。
- 结合个性化建模，进一步提升用户中心化程度。

---

## **总结**
Prism 是首个系统性地将 **Cognitive Load Theory** 应用于 LLM 意图理解的工作，提出了一个**逻辑驱动、层次化、自演进的澄清框架**。实验证明，它不仅在技术指标上达到 SOTA，在用户体验和认知负荷方面也实现了显著突破，推动 LLM 向真正“主动协作伙伴”的角色迈进。

</details>

---

### 6. [When to Invoke: Refining LLM Fairness with Toxicity Assessment](https://arxiv.org/abs/2601.09250)

**Authors**: Jing Ren, Bowen Li, Ziqi Xu, Renqiang Luo, Shuo Yu, Xin Ye, Haytham Fayek, Xiaodong Li, Feng Xia  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.09250v1  

#### Abstract
Large Language Models (LLMs) are increasingly used for toxicity assessment in online moderation systems, where fairness across demographic groups is essential for equitable treatment. However, LLMs often produce inconsistent toxicity judgements for subtle expressions, particularly those involving im...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：When to Invoke: Refining LLM Fairness with Toxicity Assessment

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对 **Large Language Models (LLMs)** 在**毒性评估**（toxicity assessment）任务中存在的**公平性偏差**问题展开研究。具体而言，LLMs 在处理涉及不同**demographic groups**（如种族、移民身份等）的隐性仇恨言论（implicit hate speech）时，常常表现出不一致的毒性评分，即对语义相同但提及不同群体的句子给出显著不同的判断，从而暴露模型中的系统性偏见。

现有方法通常依赖于**fine-tuning**或**后处理校准**（post-hoc calibration），这些方法成本高、难以部署，并且无法动态识别何时需要进行公平性修正。本文指出一个被忽视的关键问题：**“何时”应触发公平性修正机制？**

### 提出的新方法和新思路
为解决上述问题，作者提出了 **FairToT**（Fairness-aware Tree-of-Thought），一个**推理时**（inference-time）的框架，通过**prompt-guided toxicity assessment**来提升 LLM 的公平性，而无需修改模型参数。

其核心创新点包括：
- **提出“何时调用”（When to Invoke）的决策机制**：FairToT 并非对所有输入都进行修正，而是通过可解释的公平性指标动态检测潜在的公平风险，并仅在必要时才启动额外的评估流程，提高了效率。
- **设计两个可解释的公平性指标**：
  - **Sentence Fairness Variance (SFV)**：衡量在同一个句子模板下，替换不同群体实体后，模型毒性预测的方差。低 SFV 表示模型对不同群体的判断一致性高。
  - **Entity Fairness Dispersion (EFD)**：衡量某个特定群体在不同句子模板中受到的模型敏感度差异。低 EFD 表示该群体不会在某些上下文中被过度惩罚。
- **三阶段提示缓解策略（Three-Stage Prompt-Based Mitigation）**：
  1. **语义等价检查**（Semantic Equivalence Check）
  2. **实体无关的危害推断**（Entity-Neutral Harm Inference）
  3. **概率分配**（Probability Assignment），强制要求对不同群体的预测概率尽可能接近（最大差异 ≤ 0.02）。

### 相比现有方法的优势
- **无需微调**：适用于无法访问模型内部参数的商用 LLM（如 GPT-3.5-Turbo）。
- **高效且选择性激活**：只在检测到高公平风险时才执行复杂提示，避免了对所有输入进行冗余计算。
- **可解释性强**：SFV 和 EFD 提供了量化和诊断模型公平性的工具。
- **通用性强**：在多种模型架构（BERT, DeBERTa, RoBERTa, LLMs）和数据增强方法下均有效。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个广泛使用的隐性仇恨言论基准上进行：
- **LatentHatred**：来自美国极端组织推文的隐性仇恨言论数据集，包含明确的“隐含意图”标注。
- **Hate Speech and Offensive Language (Offensive Slang)**：用户生成的推文，标注为仇恨言论、冒犯性语言或无害。
- **ToxiGen**：大规模机器生成的隐性和对抗性仇恨言论数据集，覆盖13个少数群体。

### 实验设置和评估指标
- **LLM Backbone**：使用 **GPT-3.5-Turbo** 和 **Llama-3.1-8B-Instruct** 作为主干模型。
- **Baseline Models**：包括 BERT、HateBERT、DeBERTa、RoBERTa 等经典分类器，并结合数据增强方法（AAV, BT）。
- **评估指标**：
  - 主要指标为 **SFV** 和 **EFD**，二者越低表示公平性越好。
  - 不使用传统准确率指标，因为研究焦点是**跨群体的一致性**而非分类性能。
- **实现细节**：基于 Hugging Face Transformers 库，在 Kaggle Notebook 上运行，使用 Microsoft Serverless API 调用 LLM。

### 基线方法对比
由于缺乏专门用于公平性提升的 prompt-based 方法，作者采用“before vs after FairToT”的对比方式，即在同一模型上应用 FairToT 框架前后比较 SFV 和 EFD 的变化，以验证其有效性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
从 **Table 1** 可以看出，应用 FairToT 后，所有基线模型的 SFV 和 EFD 都实现了**数量级上的下降**：

| 模型 | 数据集 | SFV (Before) → (After) | EFD (Before) → (After) |
|------|--------|--------------------------|--------------------------|
| BERT (AAV) | Latent Hatred | 0.113 ± 0.080 → **0.000114 ± 0.000027** | 0.142 ± 0.081 → **0.0252 ± 0.000073** |
| H2-BERT | ToxiGen | 0.036 ± 0.067 → **0.000071 ± 0.000012** | 0.208 ± 0.022 → **0.0145 ± 0.000070** |
| RoBERTa | Offensive Slang | 0.0098 ± 0.019 → **0.000066 ± 0.000007** | 0.022 ± 0.008 → **0.0375 ± 0.000001** |

> 注：FairToT 将 SFV 从 ~0.1 降低至 ~1e-4，降幅超过 **99%**；EFD 也普遍降低 50% 以上。

### 与基线方法的对比结果
- **FairToT 在所有模型和数据集上均显著优于原始模型**，证明其具有极强的泛化能力。
- 图表 **Figure 3** 显示，应用 FairToT 后，SFV 和 EFD 的分布急剧收缩，表明模型输出更加稳定和公平。
- 即使在已经经过 fine-tuning 的模型（如 H2-BERT）上，FairToT 仍能进一步提升公平性。

### 消融实验结果（Ablation Study）
在 **Table 2** 中进行了消融实验，验证各组件的重要性：
- **移除 `θ_n`（sentence-level bias）或 `θ_e`（entity-level bias）**：性能大幅下降，说明两者对于准确识别公平风险缺一不可。
- **移除 In-Context Learning (ICL)**：公平性指标恶化，表明示例引导有助于模型理解任务。
- **移除 Three-Step Prompting (3SP)**：SFV 和 EFD 显著上升，证明结构化推理步骤对实现公平预测至关重要。

结论：**FairToT 的四个核心组件（`θ_n`, `θ_e`, ICL, 3SP）协同作用，共同实现最优公平性表现**。

---

## 4. 关键结论和发现

### 主要发现
1. **LLMs 在隐性仇恨言论检测中存在严重的跨群体不公平现象**，表现为对语义相同的句子因提及群体不同而给出差异巨大的毒性评分。
2. **FairToT 能够有效识别并修正这类不公平行为**，通过推理时的 prompt 工程显著降低 SFV 和 EFD，提升模型公平性。
3. **“何时调用”机制至关重要**：通过设定阈值 `R_n ≥ 0.35` 触发修正，既能保证公平性提升，又能避免不必要的计算开销。
4. **低 temperature 设置更有利于公平性**：实验表明，当 temperature=0（确定性解码）时，FairToT 的稳定性最高，随机性会放大潜在偏见。

### 方法的局限性
- **依赖 LLM 的推理能力**：如果基础 LLM 本身不具备良好的语义理解和推理能力，三阶段提示可能失效。
- **提示工程的设计敏感性**：缓解效果依赖于 prompt 的精确设计，可能存在优化空间。
- **仅关注文本模态**：未扩展到多模态或跨语言场景。

### 未来工作方向
- 探索更复杂的**自适应触发机制**，动态调整阈值。
- 将 FairToT 扩展至**多轮对话或多智能体协作**的 moderation pipeline。
- 研究其在**multilingual** 和 **multimodal** 场景下的适用性。
- 结合其他 fairness notions（如 individual fairness）进行综合优化。

> **源代码地址**：https://aisuko.github.io/fair-tot/

</details>

---

### 7. [Breaking the Bottlenecks: Scalable Diffusion Models for 3D Molecular Generation](https://arxiv.org/abs/2601.08963)

**Authors**: Adrita Das, Peiran Jiang, Dantong Zhu, Barnabas Poczos, Jose Lugo-Martinez  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.08963v1  

#### Abstract
Diffusion models have emerged as a powerful class of generative models for molecular design, capable of capturing complex structural distributions and achieving high fidelity in 3D molecule generation. However, their widespread use remains constrained by long sampling trajectories, stochastic varian...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Breaking the Bottlenecks: Scalable Diffusion Models for 3D Molecular Generation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前用于3D分子生成的**扩散模型**（Diffusion Models）面临三大瓶颈：
- **推理时间长**：依赖大量迭代去噪步骤（如GeoDiff需5000步）；
- **随机方差大**：反向过程中的随机采样导致样本不一致；
- **结构感知弱**：传统模型难以高效建模长程原子间依赖关系。

此外，现有方法在处理大规模分子（如聚合物）时存在可扩展性差、计算复杂度高等问题。

---

### **提出的新方法与新思路**
本文提出了一个**基于Reverse Transition Kernel (RTK)框架的确定性去噪扩散模型**，并结合**SE(3)-equivariant SSM架构**，实现高效且稳定的3D分子生成。

#### **核心创新点**：
1. **理论重构：将DDDM纳入RTK统一框架**
   - 将**Directly Denoising Diffusion Model (DDDM)** 重新解释为一种**近似的确定性反向转移核**（deterministic RTK）。
   - 证明其反向过程等价于优化一个**结构化的传输映射**（transport map），从而从理论上解释为何确定性去噪能高效收敛。

2. **强对数凹性保证数值稳定性**
   - 在温和正则条件下，每一步反向子问题的目标能量函数是**强对数凹**（strongly log-concave）的。
   - 这意味着目标分布具有唯一全局极小值、良好条件数，支持快速收敛和稳定求解。

3. **常数步长（Constant Step Size）下的高效推理**
   - 由于代理能量函数 $g(z)$ 是强凸的，允许使用**常数步长**进行反向更新，仅需 $O(1)$ 步即可完成生成。
   - 相比之下，标准DDPM需要 $O(1/\epsilon^2)$ 步才能达到精度 $\epsilon$。

4. **引入SSM-based Denoiser提升可扩展性**
   - 构建了一个**基于GraphGPS框架的混合去噪器**，用**State Space Models (SSMs)** 替代传统的Transformer全局注意力机制。
   - 支持多种SSM变体：Mamba、Mamba-2、Hydra、Jamba，并保持SE(3)等变性。

5. **构建GEOM-LongRange基准数据集**
   - 从GEOM-MoleculeNet中提取超过100个原子的大分子，形成**GEOM-LongRange**数据集，用于评估模型对长程依赖的建模能力。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **效率** | 推理速度显著加快，仅需少量步骤（如100–700步），远少于GeoDiff的5000步 |
| **一致性** | 消除随机方差，提高样本一致性与重复性 |
| **稳定性** | 强凸性保障训练与推理过程数值稳定 |
| **可扩展性** | SSM架构支持亚二次复杂度，适用于大分子系统 |
| **几何保真度** | SE(3)-equivariance确保生成构象的空间合理性 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **GEOM-DRUGS**：主流药物分子构象数据集，包含约55,000个训练分子，用于常规性能评估。
- **GEOM-LongRange**：作者构建的新数据集，包含**>100个原子**的大型分子，用于测试长程依赖建模能力。
- 数据划分：84%训练 / 10%验证 / 6%测试。

---

### **实验设置**
- **模型架构**：基于**GraphGPS**框架，集成EGNN局部消息传递 + SSM/Attention全局模块。
- **SSM类型对比**：Mamba、Mamba-2、Hydra、Jamba、Transformer。
- **输入特征**：
  - 节点：原子类型、电荷、杂化状态、环信息等（74维）
  - 边：键类型（4维 one-hot）
- **时间嵌入**：添加正弦位置编码以提供时间条件信号。
- **训练配置**：
  - 批大小：128
  - 学习率：峰值3e-4，余弦衰减至3e-5
  - 优化器：AdamW（β1=0.9, β2=0.95, weight decay=0.1）
  - 精度：BF16
  - 损失函数：pseudo-Huber loss（鲁棒于异常值）

---

### **评估指标**

#### **分子生成质量指标**
| 指标 | 含义 |
|------|------|
| **Validity** | 化学有效性比例（RDKit解析成功） |
| **Novelty** | 不在训练集中出现的新分子占比 |
| **Uniqueness** | 非重复分子的比例 |
| **Diversity** | 分子指纹间的平均Tanimoto距离 |
| **QED** | Quantitative Estimate of Druglikeness，衡量类药性 |
| **Atomic/Molecular Stability** | 原子价态合规性和整体能量合理性 |

#### **3D构象生成专用指标**
| 指标 | 含义 |
|------|------|
| **AMR (Average Minimum RMSD)** | 生成构象与参考构象之间的平均最小RMSD，越低越好 |
| **Coverage (Cov)** | 参考构象中有多少被生成集合覆盖（Recall） |
| **Precision (Prec)** | 生成构象中有多少接近真实构象（Precision） |

---

### **基线方法对比**
- **传统方法**：
  - RDKit ETKDG
  - OMEGA（商业软件）
- **机器学习方法**：
  - GeoMol
  - GeoDiff
  - Torsional Diffusion

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（GEOM-DRUGS）**

#### **表1：构象生成质量（Test Set）**
| Method | Cov-R (%) | AMR-R (Å) | Cov-P (%) | AMR-P (Å) |
|--------|-----------|-----------|-----------|-----------|
| OMEGA | 38.4 | 1.058 | 40.9 | 0.995 |
| GeoMol | 53.4 | 0.841 | 40.5 | 0.946 |
| GeoDiff | 44.6 | 0.875 | 43.0 | 0.928 |
| Torsional Diffusion (20 steps) | 41.4 | 0.945 | 44.5 | 1.136 |
| **Ours (EGNN+Hydra)** | **72.7** | **0.582** | **55.2** | **0.778** |
| **Ours (EGNN+Jamba)** | **82.7** | **0.232** | **74.6** | **0.456** |

> ✅ **Jamba版本在所有指标上全面领先**，AMR降低至0.232 Å，接近物理极限。

#### **表2：推理效率（CPU）**
| Method | Params (M) | Steps | AMR-R | Runtime (sec/conformer) |
|--------|------------|-------|--------|--------------------------|
| RDKit | – | – | 1.002 | 0.10 |
| GeoDiff | 1.6 | 5000 | 0.809 | 3.05 |
| Torsional Diffusion | 1.6 | 20 | 0.565 | 4.90 |
| **EGNN+Jamba** | **71.7** | **100** | **0.823** | **7.77** |

> ⚠️ 尽管单次运行时间较长，但可通过并行化优化；更重要的是**步骤更少、可控性强**。

---

### **消融实验结果（GEOM-DRUGS & GEOM-LongRange）**

#### **表3：不同SSM架构在GEOM-DRUGS上的表现**
| Model | Novelty | Validity | Diversity | QED |
|-------|---------|----------|-----------|-----|
| EGNN + Transformer | 85.4% | 87.1% | 89.5% | 0.456 |
| EGNN + Mamba-1 | 83.6% | 89.8% | 92.1% | 0.372 |
| EGNN + Mamba-2 | 90.8% | 88.7% | 92.6% | 0.465 |
| EGNN + Hydra | 94.5% | 97.8% | 94.4% | 0.556 |
| **EGNN + Jamba** | **93.9%** | **95.4%** | **94.6%** | **0.637** |

> 🔍 **Jamba和Hydra显著优于纯SSM或Transformer**，说明混合架构更优。

#### **表4：在GEOM-LongRange上的泛化能力**
| Model | Novelty | Validity | Diversity | QED |
|-------|---------|----------|-----------|-----|
| EGNN + Transformer | 94.4% | 92.1% | 89.5% | 0.452 |
| EGNN + Mamba-1 | 95.6% | 90.7% | 92.1% | 0.553 |
| EGNN + Mamba-2 | 86.8% | 93.7% | 92.6% | 0.464 |
| **EGNN + Hydra** | **92.5%** | **91.9%** | **94.4%** | **0.678** |
| **EGNN + Jamba** | **99.9%** | **95.4%** | **97.2%** | **0.525** |

> 📈 **Jamba在大分子上表现出最强的泛化能力和新颖性**，接近完美novelty。

---

## **4. 关键结论和发现**

### **主要发现**
1. **DDDM的本质是确定性RTK逼近**  
   通过RTK框架，首次为DDDM提供了坚实的概率基础，揭示其本质是在隐式优化一个结构化传输映射。

2. **确定性去噪可实现高保真快速生成**  
   在满足平滑性和有界性假设下，反向过程可分解为少数几个**强凸子问题**，支持常数步长、快速收敛。

3. **SSM-based去噪器兼具效率与表达力**  
   - **Mamba系列**受限于单向因果结构，存在“近期偏差”（recency bias），不利于对称分子图建模。
   - **Hydra**采用准可分矩阵实现双向信息流，无需显式Attention即可捕捉长程依赖。
   - **Jamba**融合Attention与SSM，结合MoE提升容量，在质量和稳定性上全面胜出。

4. **混合架构优于单一范式**  
   单纯依赖SSM或Transformer均非最优，**Attention+SSM混合设计**（如Jamba）能兼顾全局交互与计算效率。

---

### **方法的局限性**
- **参数量较大**：Jamba模型总参数达2.15B，激活参数71.7M，资源消耗较高。
- **对初始化敏感**：SSM序列顺序影响性能，需精心设计节点排序策略（如degree-based ranking）。
- **仍需进一步压缩**：虽然比Transformer更高效，但在超大规模分子上仍有部署挑战。
- **缺乏多模态建模能力**：确定性路径可能限制多样性，在高度多峰分布中表现受限。

---

### **未来工作方向**
1. **开发混合随机-确定性内核**  
   结合RTK框架，探索**随机+确定性混合反向过程**，平衡多样性与效率。

2. **轻量化SSM架构设计**  
   设计专用于分子图的紧凑型SSM模块，降低内存占用。

3. **扩展至蛋白质与材料系统**  
   将该框架应用于更大尺度的生物大分子或晶体结构生成。

4. **引入条件控制机制**  
   支持属性引导生成（如pIC50、溶解度等），增强实用性。

5. **硬件适配优化**  
   利用SSM的并行潜力，开发GPU/TPU友好的异步去噪算法。

---

> ✅ **总结一句话**：本文通过**RTK理论框架**统一理解确定性扩散，并提出**SE(3)-equivariant SSM去噪器**，实现了**高效、稳定、可扩展的3D分子生成**，在质量与效率之间取得新的SOTA平衡。

</details>

---

### 8. [Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs](https://arxiv.org/abs/2601.08403)

**Authors**: Abhijnan Nath, Alireza Bagheri Garakani, Tianchen Zhou, Fan Yang, Nikhil Krishnaswamy  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08403v1  

#### Abstract
Large language models are increasingly trained via reinforcement learning for personalized recommendation tasks, but standard methods like GRPO rely on sparse, sequence-level rewards that create a credit assignment gap, obscuring which tokens drive success. This gap is especially problematic when mo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在推荐系统等生成任务中越来越多地通过强化学习（RL）进行训练，但标准方法如 **Group Relative Policy Optimization (GRPO)** 存在以下关键问题：
- **信用分配缺口（credit assignment gap）**：仅依赖稀疏的序列级奖励（sequence-level rewards），无法识别是哪些具体 token 或语义片段（如产品属性短语）真正推动了成功。
- **缺乏可解释性与效率**：所有 token 获得相同的梯度更新信号，导致模型难以学习到关键推理路径。
- **易受奖励黑客攻击（reward hacking）**：模型可能过拟合检索器的表面特征而非真实语义。

这些问题在用户意图模糊、无明确标签的场景下尤为严重，例如个性化商品搜索。

---

### 提出的新方法：OSPO
作者提出 **Owen-Shapley Policy Optimization (OSPO)**，一种基于合作博弈论的新型 RL 框架，其核心思想是：
- 将生成文本中的**语义连贯单元**（如描述产品属性的短语或表达偏好的句子）视为“玩家”（players）。
- 利用 **Owen-Shapley values** 计算每个片段对最终奖励的边际贡献，实现细粒度的信用分配。
- 通过 **potential-based reward shaping** 将这些归因值用于优势函数（advantage）的重新分配，从而指导策略优化。

#### 核心创新点：
1. **无需价值模型的价值重塑（Value-Model-Free Advantage Redistribution）**  
   不依赖额外的 critic 或 reward model 来估计 token 级优势，而是直接从任务反馈中通过采样部分查询（partial queries）计算 Owen 值。

2. **语义片段级别的信用分配（Segment-Level Credit Assignment）**  
   支持将 credit 分配给有意义的语言单元（如“black jeans”、“summer wedding”），而不仅仅是单个 token。

3. **长度不变的优势重分配机制（Length-Invariant Advantage Redistribution）**  
   引入长度乘数 $ T \cdot \phi_t $，消除长序列因 Owen 值总和固定而导致的梯度稀释问题，确保不同长度响应之间的公平比较。

4. **两种优势重分配策略**：
   - **OSPO-Prop**：按 Owen 值比例分配优势。
   - **OSPO-Rank**：转换为排名后均匀分配，增强对噪声估计的鲁棒性。

---

### 相比现有方法的优势
| 方法 | 是否需要 Value Model | 细粒度 Credit | 可扩展性 | 抗 Reward Hacking |
|------|------------------------|---------------|----------|--------------------|
| GRPO | ❌ 否 | ❌ 全局统一 | ✅ 高 | ⚠️ 易发生 |
| DPO/SFT | ❌ 否 | ❌ 无 | ✅ 高 | ⚠️ 有限 |
| Process Reward Models | ✅ 是 | ✅ 细粒度 | ❌ 低（需标注中间步骤） | ✅ 较好 |
| SCAR (Shapley-based) | ✅ 是 | ✅ 细粒度 | ❌ 低 | ✅ 较好 |
| **OSPO (本文)** | ❌ **否** | ✅ **细粒度** | ✅ **高** | ✅ **强** |

> ✅ OSPO 在不增加外部模型的前提下实现了更精确、更高效的信用分配。

---

## 2. 核心实验方法和设置

### 数据集
- **ESCI Shopping Queries** (Reddy et al., 2022)：标准电商搜索数据集，用于传统商品搜索任务。
- **H&M Fashion Recommendations** (Ling et al., 2022)：包含丰富购买历史的真实时尚推荐数据集，用于上下文化搜索与用户画像摘要生成任务。

> 注：H&M 缺少显式查询，因此使用 **Claude Sonnet 3.0** 生成高质量合成查询作为监督信号。

---

### 实验设置
- **模型架构**：主干模型为 `Qwen2.5-Instruct 7B`，所有可训练基线均在此基础上微调。
- **任务类型**：
  1. **Product Search Query Refinement**：将模糊用户输入扩展为富含属性的自然语言查询。
  2. **User Profile Summarization**：基于购买历史生成偏好摘要，并预测下一购买项。
- **奖励来源**：
  - 搜索任务：使用 FAISS + 领域编码器（ALL-MPNET-BASE-V2 / SIMCSE-LARGE）进行密集检索，以 **NDCG@1000** 作为奖励。
  - 摘要任务：使用 Bradley-Terry 模型训练的 reward model 打分（90%）+ 格式正确性奖励（10%）。

---

### 评估指标
| 任务 | 主要指标 |
|------|---------|
| 商品搜索 | NDCG, AP, MRR, Recall |
| 用户摘要质量 | 使用 `Qwen-3-Nemotron-32B-Reward` 进行成对胜率评估（Win Rate, WR），采用 Borda 计分法 |

---

### 基线方法对比
- **SFT**：监督微调，仅使用高性能专家样本（NDCG > 0.5）。
- **DPO**：直接偏好优化，使用专家对比对（差异 ≥ 5% AP/NDCG）。
- **GRPO**：当前主流的无价值模型 RL 方法，作为主要在线 RL 基线。
- **OSPO-Rank / OSPO-Prop / OSPO-Prop-clip**：本文提出的变体。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
#### ESCI 商品搜索任务（NDCG）
| 方法 | NDCG |
|------|------|
| Qwen2.5-72B-Instruct (大模型) | 0.543 |
| **OSPO-Prop (7B)** | **0.522** |
| GRPO (7B) | 0.418 |
| DPO (7B) | 0.431 |
| SFT (7B) | 0.398 |

> ✅ **OSPO-Prop 以 7B 参数达到接近 72B 模型的性能**，显著优于同规模基线。

#### H&M 时尚搜索任务（NDCG）
| 方法 | NDCG |
|------|------|
| Qwen2.5-32B-Instruct | 0.438 |
| **OSPO-Prop (7B)** | **0.436** |
| GRPO (7B) | 0.379 |
| DPO (7B) | 0.396 |

> ✅ 在复杂上下文任务中，OSPO 几乎追平 32B 模型，远超 GRPO 和 DPO。

---

### 与基线方法的对比结果
- **相比 GRPO**：
  - ESCI 上 NDCG 提升 **24.9%**（0.522 vs 0.418）
  - H&M 上提升 **15.0%**（0.436 vs 0.379）
- **相比 DPO**：
  - ESCI 上提升 **21.1%**（0.522 vs 0.431）
  - H&M 上提升 **10.1%**（0.436 vs 0.396）

> 💡 表明 OSPO 的细粒度信用分配能有效捕捉影响下游性能的关键语义片段。

---

### 消融实验结果（Ablation Studies）

#### （1）优势重分配方式（Table 1）
- **OSPO-Prop**：表现最佳（ESCI NDCG 0.522）
- **OSPO-Rank**：稍弱但仍显著优于 GRPO/DPO（0.485）
> ➤ 表明比例分配更有效，但排名分配提供一定鲁棒性。

#### （2）联盟结构（Coalition Structure）影响（Figure 2 & Table 5）
- 最优配置：**中等宽度连续联盟**（w=4~8, p=48~96）
- 过窄（w=1~2）：过拟合局部共现，泛化差。
- 过宽（w=12~16）：超出实际查询长度，资源浪费。
- 非连续联盟（All Subsets）：性能崩溃（NDCG↓至 0.113）
> ➤ **连续性约束至关重要**，保证语义一致性。

#### （3）蒙特卡洛采样数量
- 降低生成数（num_generations）从 8 到 2 导致性能急剧下降。
> ➤ 多完成采样对稳定优势估计必不可少。

#### （4）跨检索器泛化能力（Table 4）
- 在测试时切换 embedding 模型（如从 ALL-MPNET 切换到 SIMCSE）：
  - GRPO 性能大幅下降
  - **OSPO 保持较强鲁棒性**
> ➤ 表明 OSPO 学习的是更具迁移性的 alignment 信号。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **细粒度信用分配比模型缩放更重要**：  
   一个 7B 的 OSPO 模型性能可媲美甚至超越 32B–72B 的大模型，说明**对齐质量**比参数量更关键。

2. ✅ **OSPO 显著提升样本效率与收敛速度**：  
   如图 1 所示，OSPO 在约 400 步内达到目标性能，而 GRPO 需要超过 800 步。

3. ✅ **更强的泛化能力和抗过拟合能力**：  
   GRPO 在训练后期出现性能崩溃，而 OSPO 持续稳定上升，表明其学习到了更本质的语义模式而非训练集捷径。

4. ✅ **适用于多种输出形式**：  
   在搜索任务中使用**短语级联盟**，在摘要任务中使用**句子级联盟**，均取得优异效果。

5. ✅ **缓解奖励操纵风险**：  
   分析显示查询长度与奖励呈弱负相关（Pearson = -0.165），说明模型未通过拉长输出“刷分”。

---

### 局限性
1. **计算开销增加**：  
   需要多次调用 reward model（或 retriever）来评估不同联盟组合，带来额外延迟。
2. **超参数敏感**：  
   联盟宽度（wmax）、采样数（M）的选择对性能有显著影响，需仔细调参。
3. **近似误差**：  
   使用 Monte Carlo 采样近似 Owen 值，可能忽略长距离依赖关系。
4. **领域限制**：  
   当前评估集中在单轮推荐任务，尚未验证于多轮对话或多模态场景。

---

### 未来工作方向
- 扩展到 **multi-turn agentic environments**（多轮智能体环境）
- 探索在 **code generation**, **dialogue planning**, **interactive decision-making** 等复杂推理任务中的应用
- 设计更高效的联盟采样策略以降低计算成本
- 结合过程奖励（process reward）进一步提升中间步骤质量

---

## 总结
**OSPO** 是一种原理清晰、工程实用的强化学习框架，它通过引入 **Owen-Shapley values** 实现了无需价值模型的细粒度信用分配，在商品搜索与用户画像生成任务上取得了显著优于 GRPO、DPO 等基线的效果。其实验结果强有力地证明：**精准的 credit assignment 是提升 LLM 推理能力的关键瓶颈之一**，而 OSPO 提供了一条高效且可扩展的解决路径。

</details>

---

### 9. [PediaMind-R1: A Temperament-Aware Language Model for Personalized Early Childhood Care Reasoning via Cognitive Modeling and Preference Alignment](https://arxiv.org/abs/2601.08848)

**Authors**: Zihe Zhang, Can Zhang, Yanheng Xu, Xin Hu, Jichao Leng  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08848v1  

#### Abstract
This paper presents PediaMind-R1, a domain-specialized large language model designed to achieve active personalization in intelligent parenting scenarios. Unlike conventional systems that provide generic suggestions, PediaMind-R1 draws on insights from developmental psychology. It introduces tempera...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PediaMind-R1: A Temperament-Aware Language Model for Personalized Early Childhood Care Reasoning via Cognitive Modeling and Preference Alignment》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 Large Language Models（LLMs）在育儿等敏感领域缺乏**个体化响应能力**，通常提供通用建议，无法根据婴幼儿的个性特征（如情绪强度、适应性）进行主动个性化推理。而婴幼儿本身不具备表达需求的能力，依赖照护者代理决策，因此亟需一种能够基于心理学理论实现“主动个性化”（active personalization）的智能系统。

### 🚀 提出的新方法与创新思路
PediaMind-R1 是首个将**发展心理学中的气质理论**（Thomas-Chess temperament framework）与 LLM 个性化推理相结合的语言模型，其核心创新包括：

- **通过心理建模激活 LLM 个性化能力**  
  首次引入 Thomas-Chess 气质分类体系（Easy, Difficult, Slow-to-Warm-Up, Mixed），构建 **infant temperament knowledge graph**，作为条件信号指导模型输出符合儿童气质类型的养育策略。

- **两阶段训练框架：SFT + GRPO**  
  - **Stage 1**: 使用 LoRA 进行 **Supervised Fine-Tuning (SFT)**，注入结构化的 Chain-of-Thought（CoT）推理能力，并绑定 temperament label 进行情境化推理。
  - **Stage 2**: 应用 **Group Relative Policy Optimization (GRPO)** 实现偏好对齐，通过组内相对优势优化逻辑一致性、心理适配性和共情水平，避免依赖绝对奖励标签。

- **提出 temperament-sensitive 评估框架**  
  设计包含多项选择题测试和专家人工评估的综合评价体系，衡量模型在知识正确性、心理合理性与照护适用性三个维度的表现。

### 🔍 相比现有方法的优势
| 维度 | 传统方法局限 | PediaMind-R1 改进 |
|------|--------------|------------------|
| 个性化依据 | 用户显式反馈 / 历史行为 | 基于非语言用户的**心理特质建模**（temperament） |
| 对齐方式 | DPO 等依赖成对偏好数据 | GRPO 利用**组内比较机制**，适用于高风险、低标注场景 |
| 推理可解释性 | 黑箱生成 | 结构化 CoT 输出：<`<temperament>` `<strategy>` `<answer>` |
| 应用场景适配 | 通用对话或任务 | 聚焦 **early childhood care** 敏感领域 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **SFT 数据集**：1,215 条照护者提问，均标注 Thomas-Chess 气质类型（Easy/Difficult/Slow-to-Warm-Up/Mixed），并配有由 DeepSeek-R1 生成、经专家审核的结构化 CoT 回答。
- **GRPO 数据集**：2,646 个 temperament-sensitive 养育情景，源自 DeepSeek-V3 辅助编写的育儿百科 + 自建 knowledge graph，其中 15% 经儿科与心理学专家评审确保可靠性。

### ⚙️ 实验设置
- **基础模型**：Qwen2.5-7B-Instruct
- **微调方式**：
  - SFT 阶段采用 LoRA（Low-Rank Adaptation）
  - GRPO 阶段 rollout group size = 4，使用复合奖励函数：
    $$
    R(y) = R_{\text{fmt}}(y) + R_{\text{temp}}(y) + R_{\text{know}}(y)
    $$
    分别评估格式规范性、气质匹配度、知识相关性。
- **硬件平台**：8×80GB NVIDIA A100 GPU

### 📊 评估指标
| 类型 | 指标 |
|------|------|
| 自动化评测 | 在 200 道 temperament-sensitive 多项选择题上的 Top-1 准确率 |
| 人工评估 | 三位领域专家盲评 100 个案例，评分维度：<br>• Knowledge Correctness（知识正确性）<br>• Psychological Appropriateness（心理适配性）<br>• Caregiving Suitability（照护适用性）<br>（每项 0–1 分，Cohen’s Kappa = 0.81 表明信度高） |

### 🆚 基线方法对比
- **Base Baseline**：原始未调优的 Qwen2.5-7B-Instruct
- **Ablation 设置**：
  - PediaMind-R1 (SFT only)
  - PediaMind-R1 (SFT + GRPO)

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）自动化基准测试结果（Top-1 Accuracy）
| Model | Accuracy (%) |
|-------|--------------|
| Qwen2.5-7B-Instruct (untuned) | 55.0 |
| PediaMind-R1 (SFT only) | 62.0 |
| **PediaMind-R1 (SFT + GRPO)** | **67.0** |

> ➤ SFT 提升 +7.0%，GRPO 再提升 +5.0%，表明两个阶段均有显著增益。

#### （2）人工评估得分（平均分，0–1）
| Model | Knowledge | Psych. Align. | Caregiving |
|--------|-----------|---------------|------------|
| Qwen2.5-7B-Instruct | 0.68 | 0.68 | 0.75 |
| PediaMind-R1 (SFT only) | 0.66 | **0.88** | 0.83 |
| **PediaMind-R1 (SFT + GRPO)** | **0.72** | **0.92** | **0.88** |

> ➤ GRPO 显著提升心理适配性（+0.04）与照护适用性（+0.05），同时改善知识准确性，说明其有效缓解了 SFT 中可能出现的“过度格式化”问题。

### 🔍 消融实验发现
- **仅 SFT** 可建立基本的 temperament-aware 推理能力，但存在部分策略与行为线索错配现象。
- **加入 GRPO 后**，模型更倾向于生成逻辑连贯、符合 developmental psychology 原则的回答，尤其在复杂混合气质（Mixed Type）场景中表现更稳健。
- GRPO 的 group-relative 优势机制有助于稳定训练过程，无需绝对奖励标签即可实现高质量偏好对齐。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **心理学理论可有效赋能 LLM 个性化**  
   将 Thomas-Chess temperament 模型作为结构化先验知识，能显著提升 LLM 在婴幼儿照护场景下的个性化推理能力。

2. **SFT + GRPO 构成可靠训练范式**  
   - SFT 成功嵌入基础的 temperament-sensitive reasoning；
   - GRPO 进一步强化逻辑一致性与心理合理性，是实现“主动个性化”的关键步骤。

3. **主动个性化可在非语言用户场景落地**  
   即使目标用户（婴儿）无法直接交互，也可通过代理输入（照护者描述 + temperament label）实现精准服务定制。

4. **结构化输出增强可信度与可用性**  
   CoT + XML-style tagging（如 `<temperament>` `<strategy>`）使推理过程透明，便于临床或家庭应用中的理解与监督。

### ⚠️ 局限性
- 气质判断依赖照护者主观报告，可能存在 **reporting bias**；
- 当前仅基于经典 Thomas-Chess 框架，未涵盖现代多维气质模型（如 Rothbart 模型）；
- 奖励函数为离散信号，缺乏细粒度连续反馈；
- 数据集规模有限（SFT: ~1.2K, GRPO: ~2.6K），未进行统计显著性检验。

### 🔮 未来工作方向
- 扩展至其他 developmental stages（如学龄前、青少年）；
- 引入动态 temperament tracking 机制，支持长期个性化；
- 探索 multimodal 输入（视频/语音）自动识别婴幼儿气质特征；
- 将该框架迁移至其他敏感领域（如老年护理、特殊教育）；
- 开发更精细的 reward modeling 方法（如基于 trajectory ranking）。

---

> 💡 **一句话总结**：  
> PediaMind-R1 首次实现了基于心理气质建模的 LLM 主动个性化，在婴幼儿照护场景中验证了“认知建模 + 偏好对齐”双轮驱动的有效性，为高敏感领域的用户中心型 AI 提供了新范式。

</details>

---

### 10. [Attention Consistency Regularization for Interpretable Early-Exit Neural Networks](https://arxiv.org/abs/2601.08891)

**Authors**: Yanhua Zhao  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.08891v1  

#### Abstract
Early-exit neural networks enable adaptive inference by allowing predictions at intermediate layers, reducing computational cost. However, early exits often lack interpretability and may focus on different features than deeper layers, limiting trust and explainability. This paper presents Explanatio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Attention Consistency Regularization for Interpretable Early-Exit Neural Networks*

---

## 1. **论文的主要贡献和创新点**

### ✅ 解决的问题
早期退出网络（Early-exit Neural Networks）通过在中间层进行预测，实现自适应推理，显著降低计算开销，适用于资源受限场景。然而，**早期出口（early exits）往往缺乏可解释性**，其注意力机制关注的特征区域可能与深层网络不一致，导致解释不一致（explanation inconsistency），削弱模型可信度和可解释性。

### 🚀 提出的新方法：Explanation-Guided Training (EGT)
本文提出了一种多目标训练框架——**Explanation-Guided Training (EGT)**，旨在提升早期退出网络的**可解释性与注意力一致性**。其核心思想是通过**注意力一致性正则化**（attention consistency regularization），使各早期出口的注意力图（attention maps）与最终出口保持对齐。

#### 创新点：
- 引入 **attention consistency loss**：利用余弦相似度衡量早期出口与最终出口注意力图之间的相似性，并在训练中显式优化该损失。
- 多目标联合优化：总损失函数为分类损失 $ \mathcal{L}_{cls} $ 与注意力一致性损失 $ \mathcal{L}_{consistency} $ 的加权组合：
  $$
  \mathcal{L}_{total} = \mathcal{L}_{cls} + \alpha \cdot \mathcal{L}_{consistency}
  $$
  其中 $\alpha$ 是控制正则化强度的超参数。
- 在所有出口点生成更一致、更具可解释性的注意力图，增强 XAI（Explainable AI）能力。

### 🔍 相比现有方法的优势
- **提升可解释性**：不仅提高准确率，还确保不同深度的模型“看到”的重要区域一致。
- **保持高性能**：在不牺牲分类精度的前提下显著改善注意力一致性。
- **实用性强**：适用于资源受限环境下的高效且可信的推理系统。

---

## 2. **核心实验方法和设置**

### 📚 数据集
- 使用一个真实世界图像分类数据集 [3]，共包含 **9 类**。
- 数据规模：**1,363 个训练样本**，**1,364 个测试样本**。

### ⚙️ 实验设置
- **模型架构**：Early-Exit CNN，包含 5 个卷积层（通道数分别为 64, 128, 256, 512, 512），每层后接 BatchNorm、ReLU 和 MaxPooling。
- **5 个早期出口**（Exit 1–5），每个出口配备独立的 attention module 和 classification head。
- **注意力机制**：生成空间注意力图以提供可视化解释。
- **优化器**：Adam，初始学习率 0.001，每 15 轮衰减 0.5。
- **训练轮数**：50 epochs。
- **早退策略**：当预测置信度 ≥ 0.9 时提前退出。

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Overall Accuracy (%)** | 整体分类准确率 |
| **Attention Consistency** | 各早期出口注意力图与最终出口（Exit 5）的平均余弦相似度 |
| **Inference Time / Sample (ms)** | 平均单样本推理时间 |
| **Speedup** | 相对于无早退模型的推理加速比 |

### 🔁 基线方法对比
- **Baseline Model**：相同架构和训练配置，但**不含 attention consistency loss**。
- 所有 EGT 变体（$\alpha = 0.1, 0.2, ..., 0.5$）均与该 baseline 对比。

---

## 3. **主要实验结果和性能指标**

### 📊 关键性能数据（来自 Table I & II）

| 模型 | Avg Attention Consistency | Overall Acc (%) | Inference Time (ms) | Speedup |
|------|----------------------------|------------------|------------------------|---------|
| Baseline | 0.693 | 98.97 | 3.6 | 1× |
| EGT ($\alpha=0.3$) | **0.821** (+18.5%) | 98.46 | - | - |
| EGT ($\alpha=0.4$) | 0.813 (+17.3%) | **98.97** | 1.83 | **1.97×** |
| EGT ($\alpha=0.5$) | 0.768 | 97.8 | 1.83 | 1.97× |

> 注：速度提升基于 $\alpha=0.5$ 配置报告，其他 EGT 模型同样启用早退机制。

### 🔍 详细对比结果
- **注意力一致性大幅提升**：
  - 所有 EGT 配置均优于 baseline（最低提升 10.8%，最高达 **18.5%**）。
  - **Exit 4 改进最明显**：从 baseline 的 0.482 提升至 0.778–0.828，相对提升 **61.4%–71.8%**。
- **分类精度保持竞争力**：
  - 最高准确率达 **98.97%**（与 baseline 持平），最低为 97.73%，仍在高位区间。
- **推理效率显著提升**：
  - 启用早退后，平均推理时间从 **3.6 ms → 1.83 ms**，实现 **1.97× 加速**。
  - 在仅损失约 1.76% 准确率的情况下获得近两倍速度提升。

### 🔤 消融实验分析（Ablation Study）
- 不同 $\alpha$ 值的影响表明存在**最优正则化强度范围**：
  - $\alpha = 0.3$：取得最高注意力一致性（0.821）
  - $\alpha = 0.4$：最佳平衡点（一致性 0.813 + 准确率 98.97%）
  - $\alpha = 0.5$：一致性下降（0.768），说明过强正则化反而有害

👉 表明 attention consistency loss 的权重需适度调节，不能过大。

---

## 4. **关键结论和发现**

### ✅ 主要发现
1. **注意力一致性可通过显式正则化有效提升**：引入 attention consistency loss 显著增强了早期出口与深层出口之间解释的一致性。
2. **可解释性与性能可兼得**：EGT 在几乎不损失准确率的前提下（最高达 98.97%），实现了高达 **18.5% 的注意力一致性提升**。
3. **兼顾效率与可信度**：结合 early-exit 机制，EGT 实现了 **1.97× 推理加速**，同时提供更可靠的视觉解释，适合部署于边缘设备等资源受限场景。
4. **EGT($\alpha=0.3$) 和 EGT($\alpha=0.4$) 表现最优**：分别在一致性或综合性能上达到峰值。

### ⚠️ 方法的局限性
- 当 $\alpha > 0.4$ 时出现性能下降趋势，说明正则化强度敏感，需仔细调参。
- 实验仅在一个中等规模、9类的数据集上验证，泛化能力有待进一步检验。
- 注意力对齐方式基于简单的双线性插值+余弦相似度，未考虑语义层级结构。

### 🔮 未来工作方向（作者提出）
- 将 EGT 框架扩展到其他模型架构（如 Transformers、ResNets）。
- 在更大、更多样化的数据集上进行验证。
- 设计**自适应的正则化权重**，针对不同出口动态调整 $\alpha$。
- 引入**层次化一致性约束**（hierarchical consistency），鼓励逐层注意力过渡平滑。
- 开展理论分析，探索注意力一致性的上下界。
- 优化框架以支持实时边缘部署（real-time edge deployment）。

---

## ✅ 总结
本论文提出的 **Explanation-Guided Training (EGT)** 框架成功解决了 early-exit networks 中解释不一致的关键问题。通过引入 **attention consistency loss**，在不影响分类性能的前提下显著提升了模型的可解释性和信任度，并保留了 early-exit 带来的高效推理优势。实验充分验证了其有效性，为构建**高效、可信、可解释的 AI 系统**提供了有力工具，尤其适用于医疗、自动驾驶等高风险领域。

</details>

---

### 11. [BalDRO: A Distributionally Robust Optimization based Framework for Large Language Model Unlearning](https://arxiv.org/abs/2601.09172)

**Authors**: Pengyang Shao, Naixin Zhai, Lei Chen, Yonghui Yang, Fengbin Zhu, Xun Yang, Meng Wang  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.09172v1  

#### Abstract
As Large Language Models (LLMs) increasingly shape online content, removing targeted information from well-trained LLMs (also known as LLM unlearning) has become critical for web governance. A key challenge lies in sample-wise imbalance within the forget set: different samples exhibit widely varying...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# BalDRO: A Distributionally Robust Optimization based Framework for Large Language Model Unlearning —— 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前 **LLM unlearning**（大语言模型遗忘）面临一个关键挑战：**样本级不平衡（sample-wise imbalance）**。在同一个 forget set 中，不同样本的遗忘难度差异巨大，导致“异步遗忘”现象——简单样本被过度遗忘（over-forgotten），而困难样本则未能充分擦除（under-forgotten）。这种不平衡严重损害了模型的 **forgetting quality** 和 **model utility**。

### 提出的新方法与新思路
作者提出 **BalDRO**，一种基于 **Distributionally Robust Optimization (DRO)** 的新型、高效的 LLM unlearning 框架。其核心思想是将 unlearning 建模为一个 **min-sup 双层优化过程**：

- **内层（sup）**：寻找一个最坏情况下的 forget 分布 $Q_f$，该分布会自适应地强调那些难以遗忘的样本（即高 loss 样本）。
- **外层（min）**：基于这个对抗性分布更新模型参数，从而实现对所有样本的同步、平衡遗忘。

为了在 LLM 场景下高效实现，作者提出了两种变体：
- **BalDRO-G**：基于 **GroupDRO** 的离散近似，动态选择每个 mini-batch 中损失最高的子集进行优化。
- **BalDRO-DV**：基于 **Donsker-Varadhan (DV) 对偶** 的连续形式，通过 `log-sum-exp` 目标函数实现平滑、可微的自适应加权，无缝集成到标准训练流程中。

### 相比现有方法的优势
- **动态适应性**：不同于基于启发式或预定义规则的 reweighting 方法（如 NPO、SimNPO、SatImp），BalDRO 能够根据训练过程中样本的实际遗忘难度动态调整权重。
- **理论保障**：基于 DRO 的框架提供了理论上的鲁棒性和平衡性保证。
- **通用性强**：作为插件式框架，可直接应用于多种现有 unlearning 方法（如 NPO、SimNPO、SatImp）之上，显著提升其性能。
- **计算开销低**：两种变体均引入极小的额外计算成本，保持了原有训练流程的可扩展性。

---

## 2. 核心实验方法和设置

### 数据集
实验在两个主流 unlearning benchmark 上进行：
- **TOFU**：合成 QA 数据集，包含 200 位虚构作者的信息，forget set 占比分别为 1%、5%、10%。用于控制变量分析遗忘动态。
- **MUSE**：真实世界书籍和新闻文章数据集，forget 与 retain 集语义高度纠缠，更具现实挑战性。

### 实验设置
- **模型**：LLaMA-2-7B 作为主干模型。
- **训练配置**：8×A800 GPU，学习率、batch size、$\beta$、$\lambda$ 等超参数进行网格搜索以确保公平比较。
- **实现方式**：BalDRO 作为插件添加到 NPO、SimNPO、SatImp 等基线方法上。

### 评估指标
#### TOFU
- **Forget Quality (FQ)**：衡量遗忘效果，值越高越好。
- **Model Utility (MU)**：保留非目标知识的能力，值越高越好。
- **Extraction Memorization (EM)** / **Extraction Strength (ES)**：衡量记忆残留，值越低越好。
- **Truth Ratio 变体**（F-TR, Ra-TR, R-TR, Rw-TR）：多维度评估遗忘质量与一致性。

#### MUSE
- **KnowMem (KM)** / **VerbMem (VM)**：分别衡量语义和逐字遗忘程度（↓ 表示越好）。
- **PrivLeak (PL)**：成员推断风险，越接近 0 越好。

### 基线方法对比
- **Original**：未遗忘的原始模型。
- **Retain**：仅在 retain set 上微调的理想上限。
- **GradAscent (GA)** / **GradDiff (GD)**：经典梯度反向/修正方法。
- **NPO**, **SimNPO**, **SatImp**：当前最先进的基于偏好的 unlearning 方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（TOFU, forget ratio=1%）
| 方法 | FQ ↑ | MU ↑ | EM ↓ | ES ↓ |
|------|------|------|------|------|
| NPO | 0.7659 | 0.5775 | 0.6842 | 0.0982 |
| **NPO+BalDRO-G** | **0.9188** (+19.9%) | **0.6126** | 0.7634 | **0.0630** |
| **NPO+BalDRO-DV** | **0.9900** (+29.2%) | 0.5815 | **0.6659** | **0.0593** |

> ✅ BalDRO 显著提升 FQ，同时降低 EM/ES，表明更强的遗忘能力。

### 在 MUSE 上的表现（News domain, VM-Df ↓）
| 方法 | VM-Df ↓ | PL → 0 |
|------|---------|--------|
| NPO | 0.4255 | -90.85 |
| **NPO+BalDRO-G** | **0.3826** | **-65.70** |
| **NPO+BalDRO-DV** | **0.3934** | **-69.52** |

> ✅ 在真实文本上，BalDRO 同样有效降低记忆泄露并改善隐私保护。

### 不同 forget ratio 下的稳定性（TOFU）
- 在 5% 和 10% forget ratio 下，BalDRO 均持续带来显著 FQ 提升（如 NPO+BalDRO-DV 在 5% ratio 下 FQ 达 0.9646）。
- **BalDRO-DV 性能更稳定且优于 BalDRO-G**，尤其在高遗忘比例下优势明显。

### 消融实验结果
- **β 参数影响**：FQ 在 $\beta=2.0$ 时达到峰值，过大或过小均导致性能下降，验证了 DRO 正则强度需适中。
- **λ 参数影响**：增大 $\lambda$ 有助于提升 MU，但对 FQ 改善有限。
- **DRO 是否应用于 retain set**：实验证明仅对 forget set 应用 DRO 效果最佳，retain set 本身已较均衡，无需额外正则化。
- **BalDRO-G 的采样比例**（图6）：选择 top-50% 最难样本效果最优；过高（75%-100%）会稀释信号，过低（25%）则样本不足。

---

## 4. 关键结论和发现

### 主要发现
1. **样本级遗忘不平衡是现有方法失效的核心原因**，必须通过动态机制加以解决。
2. **BalDRO 成功实现了“同步遗忘”**，通过 DRO 框架自适应聚焦于最难遗忘的样本，避免了过遗忘与欠遗忘并存的问题。
3. **BalDRO-G 和 BalDRO-DV 分别提供离散与连续视角**，其中 **BalDRO-DV 因其平滑性和易集成性，在实践中表现更优**。
4. **BalDRO 具有强泛化性**，可作为通用插件显著增强多种 unlearning 方法（NPO、SimNPO、SatImp）的性能。

### 方法的局限性
- 当前框架主要针对 **gradient-based unlearning**，尚未覆盖 model-editing 类方法。
- 虽然计算开销小，但在极端大规模场景下，`log-sum-exp` 的 batch-wide 操作仍可能带来轻微延迟。
- 对于某些 base method（如 SatImp），BalDRO 的增益有限，说明 base loss 设计本身也会影响最终效果。

### 未来工作方向
- 探索更精细的 loss 函数设计，进一步优化 **forget quality** 与 **general utility** 的权衡。
- 将 BalDRO 扩展至多模态模型 unlearning 场景。
- 研究如何降低 unlearning 过程的整体计算成本，提升效率。

--- 

> **总结**：BalDRO 是首个将 **Distributionally Robust Optimization** 引入 LLM unlearning 的框架，通过理论驱动的动态样本平衡机制，有效解决了异步遗忘难题，在 TOFU 和 MUSE 多个 benchmark 上均展现出显著优于现有方法的遗忘质量与模型实用性。

</details>

---

### 12. [Parallelizable memory recurrent units](https://arxiv.org/abs/2601.09495)

**Authors**: Florent De Geeter, Gaspard Lambrechts, Damien Ernst, Guillaume Drion  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.09495v1  

#### Abstract
With the emergence of massively parallel processing units, parallelization has become a desirable property for new sequence models. The ability to parallelize the processing of sequences with respect to the sequence length during training is one of the main factors behind the uprising of the Transfo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Parallelizable memory recurrent units**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前主流的序列模型中存在以下矛盾：
- **Transformer** 虽然训练时可并行化，但在推理阶段生成序列效率低，因为每一步都需要重新处理所有历史输入。
- **State-Space Models (SSMs)** 如 LRU、Mamba 等实现了训练和推理的高效并行化，但由于其**线性动态特性**，只能建模“衰减记忆”（fading memory），即信息会随时间逐渐消失，无法实现**持久记忆**（persistent memory）。
- 传统的非线性 RNN（如 LSTM、GRU）虽具备多稳态（multistability）从而支持持久记忆，但其递归结构难以并行化，限制了在大规模模型中的应用。

因此，本文旨在解决一个核心挑战：**如何设计一种既能并行化训练、又能实现持久记忆的新型 RNN 架构？**

---

### **提出的新方法与新思路**
作者提出了 **Memory Recurrent Unit (MRU)** 及其具体实现——**Bistable Memory Recurrent Unit (BMRU)**，主要创新如下：

#### ✅ **MRU 框架：基于收敛性与多稳态的记忆机制**
- 引入“内部时钟”（internal clock）概念，允许 RNN 在两个外部时间步之间进行无限次内部更新（$N \to \infty$），最终达到稳定状态（steady-state）。
- 当系统收敛后，其状态仅依赖于当前输入和初始条件（即前一时刻的状态），形成**隐式函数关系** $F(x_t, h_t; \theta) = 0$。
- 利用该系统的**分岔图**（bifurcation diagram）作为激活机制，特别是利用**迟滞分岔**（hysteresis bifurcation）来构建具有双稳态（bistability）的行为，从而实现对过去信息的**定性持久编码**。

#### ✅ **BMRU 实现：高效可并行化的门控结构**
- 对原始隐式函数进行近似，将其简化为一个由 Heaviside 和 Sign 函数构成的门控机制：
  $$
  z_t = H(|h_t| - \beta_t), \quad h_t = z_t \cdot S(h_t) \cdot \alpha + (1 - z_t) \cdot h_{t-1}
  $$
- 设计灵感来自门控 RNN（如 GRU），但关键区别在于：
  - 状态要么完全保留（$h_t = h_{t-1}$），要么被重置为 $\pm\alpha$；
  - 这种机制天然支持**持久记忆**（当 $|h_t| < \beta_t$ 时，状态不变）；
  - 同时保持了**可并行性**，因其更新规则可通过 associative operator 表达，兼容 **parallel scan** 算法。

#### ✅ **技术亮点**
- 使用 **surrogate gradient** 处理不可导函数（Heaviside 和 Sign），使模型可通过反向传播训练。
- 所有计算均可并行执行，时间复杂度从 $O(T)$ 降至 $O(\log T)$，适用于长序列任务。

---

### **相比现有方法的优势**

| 特性 | Transformer | SSMs (e.g., LRU) | 非线性 RNNs | **BMRU (本文)** |
|------|-------------|------------------|--------------|------------------|
| 并行化训练 | ✅ | ✅ | ❌ | ✅ |
| 推理效率 | ❌（自回归慢） | ✅ | ✅ | ✅ |
| 衰减记忆能力 | ✅ | ✅ | ✅ | ⚠️（弱） |
| **持久记忆能力** | ❌ | ❌ | ✅ | ✅ |
| 支持浅层网络学习长程依赖 | ⚠️ | ❌（需深层） | ✅ | ✅ |

> **核心优势总结**：  
> BMRU 成功结合了 SSM 的**高效并行性**与传统非线性 RNN 的**持久记忆能力**，填补了现有架构在这两个维度上的空白。

---

## **2. 核心实验方法和设置**

### **使用的数据集与任务**

1. **Copy-first-input（合成回归任务）**
   - 输入：二维序列 $(r_t, f_t)$，其中 $r_t \sim \mathcal{N}(0,1)$，$f_t=1$ 仅在 $t=0$。
   - 任务：记住第一个 $r_0$，并在最后时间步输出。
   - 目标：测试模型在噪声干扰下长期记忆的能力。
   - 序列长度：100 和 300；泛化测试扩展至 $10^2$ ~ $10^5$。

2. **Permuted Sequential MNIST（分类任务）**
   - 数据：MNIST 图像像素按随机顺序逐个输入。
   - 加难方式：在末尾添加最多 1216 个黑色像素（zero-padding-like），迫使模型长时间保持信息。
   - 任务：识别手写数字类别（10类）。
   - 测试模型在真实任务中结合时空信息与长期记忆的能力。

3. **Pathfinder（长距离连接判断）**
   - 来源：Long Range Arena (LRA) 基准之一。
   - 输入：32×32 图像，包含两条点和一条可能连接它们的弯曲路径。
   - 任务：判断两点是否连通（二分类）。
   - 序列长度：1024（逐像素输入）。
   - 目标：评估浅层网络处理极长依赖的能力。

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|-------|------|
| **模型架构** | 线性投影 → 若干 Recurrent Block（含 BMRU/LRU + BatchNorm + GLU + Skip）→ FC 层 |
| **Recurrent Block 数量** | 1~3 层（测试深度影响） |
| **Model Dim / State Dim** | 多数设为 256；Pathfinder 中 State Dim 扫描 128~1024 |
| **Positional Encoding** | MNIST 任务中仅给 BMRU 添加（提升性能） |
| **Pooling for Prediction** | Copy/MNIST：last timestep；Pathfinder：mean pooling |
| **Optimizer** | AdamW，cosine 学习率退火 |
| **评估指标** | MSE（回归）、Accuracy（分类） |
| **重复次数** | 所有结果取 5 次运行平均值 |

---

### **基线方法对比**
- 主要对比对象：**LRU**（Linear Recurrent Unit），代表最先进的可并行化 SSM。
- 同时测试 **BMRU/LRU 混合模型**，验证互补性。

---

## **3. 主要实验结果和性能指标**

### **Copy-first-input 任务**

| 模型 | T=100 MSE | T=300 MSE |
|------|-----------|-----------|
| **BMRU** | ~0.00 | ~0.00 |
| **LRU** | ~0.00 | ~0.99 |

- **关键发现**：
  - 在 T=300 时，LRU 完全失败（MSE≈1），而 BMRU 仍接近完美。
  - 泛化到更长序列（$10^5$）时，BMRU 性能几乎不下降（尤其低噪声下），证明其**持久记忆泛化能力**。

> 📈 图9 显示：即使训练于 T=100，BMRU 在 T=$10^5$ 上表现稳定；LRU 则迅速恶化。

---

### **Permuted Sequential MNIST**

| 设置 | BMRU | LRU | BMRU/LRU（混合） |
|------|------|-----|------------------|
| 无黑像素（T=784） | 83% | **97%** | 97% |
| +1216 黑像素（T=2000） | **88%** | 55% | **93%** |

- **关键发现**：
  - LRU 更擅长组合短期信息（无黑像素时更强）；
  - BMRU 更擅长维持记忆（加黑像素后性能下降小）；
  - **混合模型兼具两者优点**：既能在正常条件下高性能，也能在长延迟下保持鲁棒性。

> 🔁 混合模型表明：**衰减记忆 + 持久记忆 = 更强表达力**

---

### **Pathfinder 任务**

| 模型 | 1 Block | 2 Blocks | 3 Blocks |
|------|---------|----------|----------|
| **BMRU** | 71% | 78% | 77% |

- **关键发现**：
  - 即使单层 BMRU 也能显著优于随机猜测（50%）；
  - 性能主要受 **state dimension（宽度）** 影响，而非层数（深度）；
  - 增加层数反而轻微下降，推测因 BMRU 输出离散化（±α）导致深层传递困难。

> 💡 说明 BMRU 适合**浅而宽**的结构，在处理极端长程依赖时无需堆叠深层。

---

### **消融实验与分析（隐含）**
虽然未明确列出消融表，但通过以下分析体现关键组件作用：
- **门控机制 $z_t = H(|h_t| - \beta_t)$** 是实现持久记忆的关键：当 $z_t=0$ 时状态冻结。
- **Sign 函数与离散状态** 是实现双稳态的基础。
- **Surrogate gradient 参数 $\alpha_{\text{surr}}$** 被设为 1，平衡梯度估计精度与稳定性。

---

## **4. 关键结论和发现**

### **主要结论**
1. ✅ **MRU 是一类新的 RNN 范式**：通过让内部动力学充分收敛，将记忆编码于稳定状态而非瞬态轨迹，从而实现**持久记忆**。
2. ✅ **BMRU 是 MRU 的有效实例**：基于迟滞分岔设计，兼具非线性 RNN 的记忆能力和 SSM 的并行效率。
3. ✅ **BMRU 在长程依赖任务中显著优于 LRU**：尤其是在需要长期保持信息的任务中（如加黑像素的 MNIST）。
4. ✅ **BMRU 与 SSM 可互补**：混合使用 BMRU（持久记忆）与 LRU（衰减记忆）可获得最佳性能。
5. ✅ **BMRU 适合浅层网络**：在 Pathfinder 等困难任务上，单层即可取得良好效果，且性能更依赖宽度而非深度。

---

### **局限性**
1. **状态量化限制表达能力**：BMRU 每个神经元只能输出 ±α，可能导致信息压缩损失。
2. **深层堆叠性能下降**：由于离散化和门控机制，深层 BMRU 网络训练难度增加。
3. **依赖 surrogate gradient**：前向/后向不一致可能影响优化稳定性。
4. **目前仅用于判别任务**：尚未在文本生成等生成式任务中验证其推理效率优势。

---

### **未来工作方向**
1. **改进单元设计**：探索软门控、连续状态版本以缓解离散化问题。
2. **混合架构自动化**：自动决定何时使用 BMRU 或 SSM 层。
3. **更深的 BMRU 网络训练策略**：改进初始化、残差连接或引入中间监督。
4. **扩展至生成任务**：验证其在语言建模、语音合成中的实际推理速度优势。
5. **理论分析**：形式化 MRU 的记忆容量、表达能力边界。

---

## **总结**
本文提出的 **MRU/BMRU** 是一次重要的架构创新，成功将**多稳态带来的持久记忆能力**与**现代并行化训练需求**相结合。实验证明其在长程依赖任务中优于主流 SSM（如 LRU），尤其在记忆持久性和泛化能力方面表现突出。尽管存在离散化和深层训练的挑战，但它为下一代高效、强记忆的序列模型提供了全新范式，并开启了“**可并行非线性 RNN**”的研究方向。

</details>

---

### 13. [Recursive Knowledge Synthesis for Multi-LLM Systems: Stability Analysis and Tri-Agent Audit Framework](https://arxiv.org/abs/2601.08839)

**Authors**: Toshiyuki Shigemura  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.08839v1  

#### Abstract
This paper presents a tri-agent cross-validation framework for analyzing stability and explainability in multi-model large language systems. The architecture integrates three heterogeneous LLMs-used for semantic generation, analytical consistency checking, and transparency auditing-into a recursive ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Recursive Knowledge Synthesis for Multi-LLM Systems: Stability Analysis and Tri-Agent Audit Framework*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文旨在解决**多模型大型语言模型（multi-LLM）系统中的计算稳定性、逻辑一致性与可解释性不足的问题**。在当前主流的单模型推理范式（如 Chain-of-Thought、ReAct、Reflexion）中，尽管能提升推理能力，但缺乏对系统级稳定性的理论保障，且容易出现“逻辑漂移”（logical drift）或不可审计的行为。

此外，现有的多代理系统（multi-agent LLM systems）往往依赖自动化 API 调用和自主交互，存在安全风险（如反馈循环失控、隐式协调行为），难以实现人类监督下的可控推理。

---

### **提出了什么新方法或新思路**

作者提出了一种名为 **Tri-Agent Cross-Validation Framework（三代理交叉验证框架）** 的新型架构，并引入了 **Recursive Knowledge Synthesis (RKS)** 概念，其核心思想是通过三个异构 LLM 在递归循环中相互约束、持续优化知识状态。

#### 三大模块设计：
- **Semantic Reasoning Module (Ms)**：由 ChatGPT 执行，负责语义生成与语言连贯性。
- **Analytical Consistency Module (MA)**：由 Gemini 执行，确保逻辑一致性和理论完整性。
- **Transparency Audit Module (Mr)**：由 Copilot 执行，作为“安全监管器”，强制执行透明度与伦理合规标准。

这三个模块在一个由**外部人类监督者（External Supervisor）** 控制的递归循环中协作，形成一个非自主但高度结构化的推理流程。

#### 创新机制：
- **Session-Level Role Decomposition (SLRD)**：在同一 LLM 平台内（如 ChatGPT Plus），将不同角色分配到独立会话中，防止上下文污染和隐式记忆传播。
- **Human-Bridge Orchestration (HBO)**：所有模块间通信均由人工手动传递，杜绝自动代理链式调用，增强安全性与可审计性。
- **RKS 的形式化建模**：基于 **Banach Fixed-Point Theorem** 将整个系统视为一个复合映射 $ V_{op} = M_T \circ M_A \circ M_S $，并证明当 $ M_T $ 具有收缩性时，系统收敛于唯一固定点。

---

### **相比现有方法的优势**

| 维度 | 现有方法（如 Debate-based、AutoGPT） | 本文方法 |
|------|----------------------------------------|---------|
| **自动化程度** | 高（全自动 API 调用） | 低（人工桥接，HBO） |
| **安全性** | 存在失控风险（feedback loops） | 强（人为控制每一步转移） |
| **可解释性** | 黑箱式交互，难追溯 | 明确的日志记录与会话隔离 |
| **稳定性理论支持** | 缺乏形式化收敛分析 | 基于 contraction mapping 提供数学保证 |
| **部署门槛** | 需要 API 接入与工程能力 | 可仅用浏览器界面完成，适合独立研究者 |

> ✅ **优势总结**：本框架不是追求效率最大化，而是构建一个**安全优先、人类可控、可复现、具备理论收敛保障的 multi-LLM 推理系统**，特别适用于高风险决策场景或资源受限环境下的研究民主化。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **无传统训练数据集**：实验不涉及模型训练或微调。
- 使用的是作者定义的 **Predefined Contradiction Set (Sc)**，用于注入可控错误以测试系统的偏差检测能力。
  
#### Sc 包含三类预设矛盾：
1. **Logical Contradiction**：“A 且 B” 后又说 “A 且非 B”
2. **Semantic Ambiguity**：故意误用技术术语
3. **Ethical Boundary Violation**：轻微违反初始设定的安全准则

这些矛盾由人类监督者在特定阶段“播种”进知识状态向量中，用于评估 DDR。

---

### **实验设置**

- **总试验次数**：47 次独立验证试验
- **时间限制**：每次最多运行 25 轮迭代 或 120 分钟
- **平台访问方式**：全部使用公共免费/订阅版 Web 接口（非 API）
  - ChatGPT (GPT-5.0 family, free tier)
  - Gemini Pro (Google, free tier)
  - Copilot (Microsoft, M365 version, assumed GPT-4o class)

> ⚠️ 特别说明：由于未锁定模型版本，实验反映的是 **2025年10月期间公开部署模型的实际行为快照**，牺牲了比特级可复现性，换取现实世界适用性。

---

### **评估指标**

所有指标均为**人类监督者依据标准化评分表进行半手动标注**，非模型内部统计。

| 指标 | 定义 | 公式 |
|------|------|-------|
| **Transparency Score (TS)** | 输出的可解释性与可追溯性得分 | $ TS = \frac{Ec + T_p}{2} $，其中 Ec 为解释系数，Tp 为可追溯参数 |
| **Deviation Detection Rate (DDR)** | 成功识别出预设矛盾的比例 | $ DDR = \frac{\text{正确检测的 inconsistency 数}}{\text{总候选 inconsistency 数}} $ |
| **Correction Success Rate (CSR)** | 检测到偏差后成功修正至合规状态的比例 | $ CSR = \frac{\text{成功修复案例数}}{\text{总偏差案例数}} $ |
| **Reflex Reliability Score (RRS)** | 综合稳定性得分 | $ RRS = 0.3 \times TS + 0.4 \times DDR + 0.3 \times CSR $ |

> 💡 权重选择依据：DDR 权重最高（40%），因为实验发现**未能及时发现偏差是系统不稳定的主要原因**。

---

### **基线方法对比**

论文**未直接与其他 multi-agent 架构进行端到端性能比较**，而是从以下角度间接对比：

| 对比维度 | 本文方法 | 典型 baseline（如 LLM-as-a-Judge、AutoGPT） |
|--------|--------|-----------------------------|
| 架构类型 | 三代理异构 + 人工桥接 | 单模型或多模型自动编排 |
| 收敛理论 | 有（contraction mapping） | 无 |
| 安全机制 | 显式审计模块 + 人工审查 | 依赖 prompt engineering 或 constitutional rules |
| 可复现性 | 高（完整日志 + 分步提示） | 低（依赖动态 API 和隐藏状态） |

> 🔍 作者强调：这不是一个“性能超越 SOTA”的竞赛性研究，而是一个**可行性验证与稳定性机制探索的研究**。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **平均 RRS** | $ 0.78 \pm 0.06 $ |
| **TS ≥ 0.8 的试验比例** | ~68% |
| **TS ≥ 0.7 的试验比例** | ~92% |
| **系统收敛率**（达到稳定知识状态） | ~89% |
| **平均收敛轮次** | $ 12.3 \pm 3.7 $ 次迭代 |
| **推断的认知偏差估计 B = 1 - TS_norm** | 始终低于 0.2 |

> ✅ 表明系统在大多数情况下能够实现高透明度、高一致性输出，并快速收敛。

---

### **与基线方法的对比结果**

虽然没有数值上的横向对比，但从机制层面得出以下结论：

- 相比纯自动化系统（如 AgentGPT），本文框架显著降低了**模式崩溃**（mode collapse）和**优化漂移**（optimization drift）的风险。
- 相比单一模型自我反思方法（如 Reflexion），本文方法提供了更全面的外部视角，避免“自说自话”式的逻辑闭环。
- 相比 ensemble voting 方法（如 LLM-as-a-Judge），本文引入了**动态反馈回路与状态演化机制**，而非静态投票。

---

### **消融实验结果（如有）**

论文**未提供正式的消融实验**（ablation study），但通过以下方式进行了机制有效性验证：

- **SLRD 的作用**：通过多会话隔离，人类监督者可以清晰观察到各模块输出差异，便于发现漂移。
- **MT 的关键性**：理论分析指出，只有 $ M_T $ 具备收缩映射特性，才能保证整体系统收敛；实证中高 TS 合规率支持这一假设。
- **HBO 的必要性**：手动桥接阻止了未经审查的信息流动，提升了系统的可审计性与安全性。

> 📌 虽然缺少量化消融，但作者通过理论推导与过程日志分析，论证了每个组件的功能不可或缺性。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **异构三代理架构可在现实中实现稳定的递归知识合成（RKS）**：
   - 多数试验（~89%）实现了收敛，表明该系统具有良好的动态稳定性。

2. ✅ **Transparency Audit Module 是系统稳定的关键**：
   - 实验验证了其作为“收缩算子”（contraction operator）的作用，推动系统向合规状态收敛。
   - 高 TS 得分（>0.7 在 92% 试验中达成）直接支撑了理论预测。

3. ✅ **Session-Level Role Decomposition 提升了可解释性与可控性**：
   - 即使使用同一 LLM 实例，也能通过会话隔离模拟多代理行为，降低实现门槛。

4. ✅ **Human-in-the-loop 设计既是限制也是安全保障**：
   - 手动桥接虽牺牲效率，但有效防止了自主代理系统的典型失败模式（如无限循环、目标侵蚀）。

5. ✅ **无需专用硬件或 API 即可开展高质量 multi-LLM 研究**：
   - 使用 freemium-tier 浏览器接口即可构建复杂推理系统，推动 AI 研究的**民主化**（democratization）。

---

### **方法的局限性**

| 局限 | 描述 |
|------|------|
| **不可完全复现** | 依赖持续更新的公共 LLM，无法锁定具体模型版本，属于“快照式研究”。 |
| **主观评估依赖** | 所有指标由单一人类标注，存在主观偏差风险，缺乏 inter-rater reliability 报告。 |
| **规模有限** | 仅 47 次试验，尚未覆盖广泛任务类型与失败模式。 |
| **效率低下** | 人工桥接导致延迟高，不适合实时应用。 |
| **非完全自动化** | 不适用于需要高速响应或大规模并行的任务场景。 |

---

### **未来工作方向**

1. **开发自动化的语言质量指标**：例如可计算的 Expressive Coherence Index，替代人工评分。
2. **实现全自动 Cross-Validation Cycle**：利用对抗性提示（adversarial prompting）取代人工监督。
3. **扩展至 N-agent 协作结构**：探索更多代理参与时的稳定性边界与复杂性增长规律。
4. **应用于高风险领域**：如自动形式化证明验证、代码生成、医疗诊断辅助等。
5. **引入多评估者机制**：报告 Cohen’s κ 或 Fleiss’ κ 以提高评估可信度。

---

## 总结

> 🌟 本论文提出了一种新颖的 **tri-agent 递归验证框架**，首次将 **Recursive Knowledge Synthesis (RKS)** 形式化为一个具有**收敛保证的动态系统**，并通过 47 次真实环境试验验证了其稳定性与可行性。

它并非追求极致性能，而是为 multi-LLM 系统设计了一个**安全、透明、可审计、可复现的参考架构**，尤其适合独立研究者、教育机构或对安全性要求高的应用场景。

其最大价值在于：**展示了如何在没有 API、没有 GPU 集群的情况下，用普通设备构建出具备理论基础的稳定多模型推理系统**，为 AI 研究的普惠化开辟了新路径。

</details>

---

### 14. [Benchmarking Post-Training Quantization of Large Language Models under Microscaling Floating Point Formats](https://arxiv.org/abs/2601.09555)

**Authors**: Manyi Zhang, Ji-Fu Li, Zhongao Sun, Haoli Bai, Hui-Ling Zhen, Zhenhua Dong, Xianzhi Yu  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.09555v1  

#### Abstract
Microscaling Floating-Point (MXFP) has emerged as a promising low-precision format for large language models (LLMs). Despite various post-training quantization (PTQ) algorithms being proposed, they mostly focus on integer quantization, while their applicability and behavior under MXFP formats remain...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Benchmarking Post-Training Quantization of Large Language Models under Microscaling Floating Point Formats

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文系统研究了在 **Microscaling Floating-Point (MXFP)** 格式下，**Post-Training Quantization (PTQ)** 在大语言模型（LLMs）上的有效性。尽管已有大量针对整数量化（INT）的 PTQ 方法，但这些方法在 MXFP 这类新兴浮点格式下的表现尚未被充分探索。

### 提出的新方法与新思路
- **首次对 MXFP 格式的 PTQ 进行全面实证研究**，涵盖超过 7 种主流 PTQ 算法、15 个评估基准、3 类 LLM 家族（包括多模态 LLMs）。
- 将 PTQ 方法划分为四类统一框架：
  - **Channel-Wise Transformation**（如 SmoothQuant, AWQ）
  - **Error Compensation**（如 GPTQ, MR-GPTQ）
  - **Rotational Transformation**（如 QuaRot, SpinQuant）
  - **Affine Transformation**（如 FlatQuant）
- 揭示了 MXFP 并非整数量化的“即插即用”替代品，而是一种需要**格式感知设计（format-aware design）** 的独立数值体系。

### 相比现有方法的优势
- 提供了关于 **MXFP8 和 MXFP4** 下 PTQ 行为的系统性洞察，填补了低精度浮点量化领域的空白。
- 发现了若干反直觉现象（例如旋转变换损害 MXFP4 性能），并提出了有效的优化策略（如 Pre-scale）。
- 强调了在多模态场景中应采用**混合精度策略**（保留 LLM 高精度，降低视觉编码器精度），具有实际部署指导意义。

---

## 2. 核心实验方法和设置

### 使用的数据集与模型

#### 评估模型
- **纯文本 LLMs**：
  - `Llama-3.1-8B-Instruct`
  - `openPangu-Embedded-7B-V1.1`
- **多模态 LLMs (MLLMs)**：
  - `Qwen2.5-VL-7B`
  - `openPangu-VL-7B`

#### 评估基准（Benchmarks）
| 类型 | 数据集 |
|------|--------|
| **语言建模质量** | WikiText2 (PPL) |
| **非推理任务（零样本）** | PIQA, Winogrande, HellaSwag, ARC-Easy, ARC-Challenge |
| **推理任务** | MATH-500, AIME24, AIME25 |
| **多模态任务** | OCRBench, MMBench, MMBenchCN, TextVQA, ChartQA, MME, MMMU |

> 所有实验使用 [microxcaling](https://github.com/microsoft/microxcaling) 库模拟 MXFP 格式。

### 实验设置
- **量化配置**：
  - **Weight-Only Quantization**: 仅权重使用 MXFP
  - **Weight-Activation Quantization**: 权重和激活均使用 MXFP（全量化 MatMul）
  - **KV Cache Quantization**: 注意力中的 Key/Value 缓存进行量化以减少内存占用
  - 记号表示：`W{bits}A{bits}[KV{bits}]`，例如 `W4A8` 表示权重量化为 4-bit，激活保持 8-bit。

- **MXFP 格式定义**：
  - **MXFP8**: E4M3 或 E5M2 → 文中采用 E4M3（更大尾数更利于细粒度量化）
  - **MXFP4**: E2M1

- **PTQ 方法分类与代表算法**：
  | 类别 | 代表方法 |
  |------|---------|
  | Channel-Wise Transformation | SmoothQuant, AWQ |
  | Error Compensation | GPTQ, MR-GPTQ |
  | Rotational Transformation | QuaRot, SpinQuant |
  | Affine Transformation | FlatQuant |

### 评估指标
- **平均准确率恢复率（Accuracy Recovery Rate）**：相对于 BF16 精度的百分比。
- 分类性能区间：
  - **Lossless**（≤1% 下降）
  - **Fair**（1%-3%）
  - **Risky**（≥3%）
- **Perplexity (PPL)**：用于语言建模任务评估。
- **Pearson 相关系数**：衡量不同模型间 PTQ 表现趋势的一致性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 量化配置 | 性能表现（典型恢复率） | 是否可行 |
|----------|------------------------|----------|
| **W8A8** | >99.5%（几乎所有方法接近 lossless） | ✅ 安全可用 |
| **W4A8** | 96%~99%（部分方法 near-lossless） | ⚠️ 可接受，需谨慎选择算法 |
| **W4A4** | 87%~97%（普遍进入 risky 区域） | ❌ 极具挑战，仍为开放问题 |

> 示例：在 `Llama-3.1-8B-Instruct` 上，`W4A4` 下 RTN 的恢复率为 94.98%，而 FlatQuant 达到 96.57%，GPTQ 为 95.50%。

### 与基线方法的对比结果

#### （1）不同 PTQ 范式的表现差异显著
- **Error Compensation**（GPTQ / MR-GPTQ）和 **Affine Transformation**（FlatQuant）在 MXFP 下表现最优，尤其在低比特（W4A8/W4A4）时优势明显。
- **Channel-Wise Transformation**（SmoothQuant / AWQ）表现次之。
- **Rotational Transformation**（QuaRot / SpinQuant）在 MXFP4 下**表现最差**，甚至不如随机舍入（RTN）基线。

> 原因分析：旋转操作会破坏 MXFP 所依赖的局部统计特性（如组内分布形状），导致 group-wise scaling 失效。

#### （2）RTN 仍是强基线
- 在多数情况下，简单的 **Round-To-Nearest (RTN)** 在 MXFP 下表现稳健，许多复杂 PTQ 方法提升有限，甚至退步。
- 表明当前 PTQ 方法大多为 INT 设计，在 MXFP 上存在**兼容性错配**。

#### （3）多模态模型敏感度分析（Table 4）
| 组件量化方式 | OCRBench | MMBench | Recovery (%) |
|-------------|----------|---------|---------------|
| LLM: BF16, ViT: BF16 | 877 | 79.08 | 100.00 |
| LLM: W4A4, ViT: BF16 | 845 | 77.04 | **95.69** |
| LLM: BF16, ViT: W4A4 | 856 | 77.72 | **98.96** |

👉 结论：**LLM 组件主导量化敏感性**，视觉编码器更鲁棒。

#### （4）视觉 token 对 MXFP 更鲁棒（Table 5）
- 即使将视觉 token 激活从 A16 降到 A4，性能下降不显著。
- 推测原因：MXFP 的指数-尾数分离机制能更好处理视觉特征的大动态范围。

### 消融实验结果

#### Pre-scale 优化策略（Table 6）
| 策略 | Avg Acc (%) | PPL |
|------|------------|-----|
| Baseline (No Pre-scale) | 52.39 | 104.42 |
| **With Pre-scale** | **56.76** | **49.33** |

> **Pre-scale** 是指在量化前将输入乘以 3/4，避免 FP4 动态范围不足引起的剪裁偏差。该简单策略显著改善 MXFP4 性能。

#### Scaling Factor 错误影响分析（Figure 4）
- MXFP 使用共享的 UE8M0 scaling factor，其必须是 2 的幂次。
- 这种粗粒度缩放会导致块内所有值的整体失配，成为 MXFP4 的主要误差源之一。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **MXFP8 支持近无损量化**：`W8A8` 配置在多种任务和模型上均可实现 lossless 性能，适合直接部署。
2. ⚠️ **MXFP4 仍具挑战性**：`W4A4` 导致显著性能下降，属于高风险区域；即使 `W4A8` 也需精心挑选算法才能接近可接受水平。
3. 🔄 **PTQ 方法与 MXFP 兼容性高度相关**：
   - **Error Compensation** 和 **Affine Transformation** 最适配 MXFP。
   - **Rotational Transformation** 在 MXFP4 下有害，不应盲目迁移。
4. 🔍 **量化敏感性由 LLM 主导**：在多模态模型中，语言模型比视觉编码器更敏感，建议采用**混合精度策略**（LLM 高精度 + ViT 低精度）。
5. 🧮 **Scaling Factor 是 MXFP4 的关键误差源**：提出 **Pre-scale** 策略可有效缓解此问题，带来显著性能提升。

### 方法的局限性
- 实验集中在 **7B/8B 规模模型**，未验证在更大规模模型（如 30B+）上的泛化能力。
- 仅研究了 **OCP 提出的 MXFP 格式**，未覆盖 NVIDIA 的 NVFP 系列（如 NVFP4/NVFP8）。
- 所有实验基于 **post-training quantization**，未涉及训练感知量化（QAT）或微调方案。

### 未来工作方向
- 开发专为 MXFP 设计的新型 PTQ 算法，而非简单移植 INT 方法。
- 探索更大模型尺度和更多硬件支持的微缩放格式（如 NVFP）下的行为一致性。
- 研究如何结合 QAT 或轻量微调进一步提升 MXFP4 的性能上限。
- 推动社区摆脱“整数量化中心主义”，建立面向浮点低精度的系统化量化理论。

---

> **总结一句话**：  
> MXFP 不是 INT 的简单替代品，而是一个需要专门设计的数值范式。本文通过大规模实证揭示了其独特行为，并为未来 MXFP-aware 的量化方法提供了坚实基础与实用指南。

</details>

---

### 15. [LatencyPrism: Online Non-intrusive Latency Sculpting for SLO-Guaranteed LLM Inference](https://arxiv.org/abs/2601.09258)

**Authors**: Du Yin, Jiayi Ren, Xiayu Sun, Tianyao Zhou, Haizhu Zhou, Ruiyan Ma, Danyang Zhang  
**Category**: cs.DC  
**Published**: 2026-01-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.09258v1  

#### Abstract
LLM inference latency critically determines user experience and operational costs, directly impacting throughput under SLO constraints. Even brief latency spikes degrade service quality despite acceptable average performance. However, distributed inference environments featuring diverse software fra...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LatencyPrism: Online Non-intrusive Latency Sculpting for SLO-Guaranteed LLM Inference

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLM）推理服务对**低延迟和稳定性**有极高要求，直接影响用户体验和 SLO（Service Level Objective）保障。然而，在生产环境中，分布式推理系统面临以下挑战：
- **延迟波动大**：存在“生成停顿”（Generation Stalls），即 token 间延迟剧烈抖动。
- **监控盲区**：传统监控依赖请求级聚合指标（如平均延迟），掩盖了微秒级的内部瓶颈。
- **归因困难**：缺乏跨栈（CPU/GPU/框架/硬件）语义关联，难以定位延迟根源。
- **观测工具侵入性强**：现有 Profiler 需重启服务或修改代码，不适合在线生产环境。

### 提出的新方法与创新点
作者提出 **LatencyPrism** —— 第一个面向生产级 LLM 推理的**零侵入、全栈、在线延迟雕刻系统**，具备三大核心设计：

#### （1）非侵入式跨栈语义分析（Non-intrusive Cross-Stack Semantic Profiling）
- 利用 `ptrace`、CUPTI、eBPF 等技术，在不修改代码、无需重启的前提下，自动注入探针。
- 实现从 Python 应用层函数调用到 GPU kernel 执行的**端到端语义对齐**，打通软硬件鸿沟。

#### （2）事件驱动的统一时序建模（Event-Driven Unified Temporal Modeling）
- 支持纳秒级时间同步，实现数百节点间的分布式追踪协调。
- 动态激活数据通道（on-demand tracing），避免持续高开销采集。

#### （3）高效的两级监控架构（Efficient Two-Stage Monitoring）
- **Sentinel Mode（哨兵模式）**：常驻运行，仅收集轻量元数据（输入长度、batch size），CPU 开销 <0.5%，用于构建动态预期延迟基线。
- **Deep-Dive Mode（深度追踪模式）**：当实际延迟显著偏离预测值时，自动触发全栈深度追踪（~7% 开销），捕获异常上下文。

#### （4）基于物理特征的非线性建模
- 使用 **GBDT（Gradient Boosting Decision Trees）** 构建 Decode 阶段延迟预测模型，能有效拟合现代推理引擎中的 `Overlap Optimization` 引入的非线性拐点行为。
- 特征工程强调**物理因果性**，例如构造 `Wk = B × Lreal` 来反映 Decode 阶段的显存带宽压力。

### 相比现有方法的优势

| 方法类别 | 代表工具 | 主要缺陷 | LatencyPrism 的优势 |
|--------|---------|--------|------------------|
| Vendor Profilers | Nsight, rocprof | 高开销、需进程接管、离线使用 | 在线安全、零侵入、支持生产部署 |
| Framework Tools | Torch Profiler | 需改代码、仅限软件栈内可见 | 无需修改、覆盖全栈（HW+SW） |
| eBPF 工具 | DeepFlow, eGPU | 缺乏 GPU 内部可见性 | 可穿透 GPU 驱动层获取 kernel 级信息 |
| 训练可靠性系统 | Mycroft, XPUTimer | 仅适用于训练任务 | 专为推理优化，支持多阶段语义识别 |

> ✅ **LatencyPrism 是首个真正实现“在线 + 非侵入 + 全栈 + 自动化”的 LLM 推理可观测性系统**。

---

## 2. 核心实验方法和设置

### 实验平台与模型配置
- **硬件**：NVIDIA A100 GPUs
- **框架**：SGLang v0.5.4 和 vLLM v0.10.0
- **模型**：Qwen3-32B 和 DeepSeek-R1-Distill-Llama-70B
- **并行策略**：Tensor Parallelism (TP=4)

### 数据集与负载分布
模拟真实生产多样性，涵盖广泛的工作负载组合：
- **Batch Size**: 1–512
- **Input Length**: 1–2048 tokens
- **Output Length**: 1–512 tokens

### 评估指标
| 指标 | 描述 |
|------|------|
| **CPU Overhead** | 监控系统自身资源消耗 |
| **Throughput Degradation** | 对推理吞吐的影响 |
| **R² / MAPE** | 延迟预测模型准确性 |
| **Precision / Recall / F1-score** | 异常检测性能 |
| **False Positive Rate (FPR)** | 误报率 |
| **Detection Lag** | 从异常发生到告警的时间延迟 |

### 基线方法对比
在异常检测部分比较了三种策略：
1. **Fixed-Point**：固定阈值（+15%）+ 单点检测
2. **Fixed-Window**：固定阈值 + 滑动窗口平滑（W=10）
3. **Dynamic-Window**（LatencyPrism 默认）：基于训练误差分布动态计算 UCL（Upper Control Limit）

---

## 3. 主要实验结果和性能指标

### （1）系统开销极低
- **CPU 开销**：<0.5%
- **吞吐影响**：<0.5%
- **延迟增加**：<0.1%
> ✔️ 满足生产环境“透明观测”要求。

### （2）延迟预测模型表现优异

| Feature Strategy | Model | R² | MAPE |
|------------------|-------|-----|-------|
| Full Feature | GBDT | 0.989 | 1.53% |
| Full Feature | Polynomial | 0.865 | 5.98% |
| **Feature Engineering (Ours)** | **GBDT** | **0.963** | **6.06%** |

- 虽然“全特征 + GBDT”精度更高，但其主导特征为 `post_MaxInLen`、`BatchSizexFwdMode` 等调度相关字段，表明存在**过拟合框架实现细节**的风险。
- LatencyPrism 的物理特征工程策略虽精度略低，但可解释性强，正确识别出 `Workload_KV (B·L)` 为主要瓶颈，符合内存带宽受限的理论预期。

### （3）异常检测性能卓越

| Strategy | Precision | Recall | **F1-score** | FPR | Lag |
|----------|-----------|--------|--------------|-----|-----|
| Dynamic-Point | 0.960 | 0.996 | 0.978 | 0.84% | 0.0 |
| **Dynamic-Window (Ours)** | **0.971** | **0.999** | **0.985** | 0.59% | 0.2 |
| Fixed-Window | 1.000 | 0.993 | 0.997 | 0.00% | 1.4 |

- **F1-score 达 0.985**，接近完美检测。
- Dynamic-Window 在保持极低 FPR 的同时，召回率最高，且无需人工调参。
- 可视化热力图显示，所有 20 次试验中均能在故障注入后立即响应，验证了**低延迟、高一致性检测能力**。

### （4）根因定位有效性验证
通过自动化脚本分析 Trace 数据，成功定位多种故障：
| 故障类型 | 关键事件 | Δβ (%) | Δu | Suspicion Score |
|--------|--------|--------|-----|----------------|
| CPU Contention | oncpu | +29.0 | +50.1 | 2.821 |
| GPU Instability | ampere_gemm | +5.9 | +15.3 | 1,561 |
| NVLink Congestion | sglang::reduce | +86.2 | +2.5e13 | 292k |

> ✔️ 异常事件的 `β`（时间占比）和 `u`（利用率）变化显著，P-value ≈ 0.000，统计上可区分。

### （5）消融实验与泛化能力
- **未见负载测试**：在从未训练过的 workload 上，模型迅速收敛（约 1k 样本），90% 样本预测误差 <10%。
- **噪声鲁棒性**：直接使用原始生产噪声数据训练，仍可在几分钟内稳定。
- **跨栈通用性**：在 vLLM + 70B 模型上快速适配，证明建模策略不依赖特定框架。

---

## 4. 关键结论和发现

### 主要结论
1. **LLM 推理延迟归因必须是跨栈、细粒度、在线的**：传统聚合监控无法捕捉 Generation Stalls。
2. **非侵入式全栈追踪可行且必要**：LatencyPrism 成功实现了无需重启、无代码修改的生产级观测。
3. **动态基线优于静态阈值**：基于 GBDT 的 workload-aware 预测模型能有效解耦“负载增长”与“性能退化”。
4. **两级监控架构平衡了开销与诊断深度**：Sentinel Mode 提供全天候轻量监控，Deep-Dive Mode 捕获瞬态上下文。
5. **高质量 Trace 数据赋能下游分析**：即使简单规则也能实现精准根因定位，说明数据保真度高。

### 局限性
- **依赖历史正常数据**：新 workload 或硬件升级需要短暂 warm-up 期。
- **持久性性能问题可能漏检**：若系统长期处于亚健康状态，缺乏“正常”参考，难以触发告警。
- **非主流 XPU 支持受限**：语义解析能力受厂商工具链开放程度制约。

### 未来工作方向
- 利用 LLM 对 Trace 进行自然语言摘要，提升可读性。
- 开发无需冷启动的自适应基线机制（calibration-free）。
- 构建更智能的自动化诊断 Agent，结合因果推理进行闭环修复。

---

> 🔚 **总结**：LatencyPrism 将 LLM 推理可观测性从“黑盒监控”推进到“白盒诊断”，是首个实现**在线、非侵入、全栈、自动化**延迟分析的系统，已在阿里云千卡规模集群稳定运行六个月，具有重要工业价值。

</details>

---

### 16. [Enhancing Spatial Reasoning in Large Language Models for Metal-Organic Frameworks Structure Prediction](https://arxiv.org/abs/2601.09285)

**Authors**: Mianzhi Pan, JianFei Li, Peishuo Liu, Botian Wang, Yawen Ouyang, Yiming Rong, Hao Zhou, Jianbing Zhang  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.09285v1  

#### Abstract
Metal-organic frameworks (MOFs) are porous crystalline materials with broad applications such as carbon capture and drug delivery, yet accurately predicting their 3D structures remains a significant challenge. While Large Language Models (LLMs) have shown promise in generating crystals, their applic...

---

### 17. [Private LLM Inference on Consumer Blackwell GPUs: A Practical Guide for Cost-Effective Local Deployment in SMEs](https://arxiv.org/abs/2601.09527)

**Authors**: Jonathan Knoop, Hendrik Holtmann  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.09527v1  

#### Abstract
SMEs increasingly seek alternatives to cloud LLM APIs, which raise data privacy concerns. Dedicated cloud GPU instances offer improved privacy but with limited guarantees and ongoing costs, while professional on-premise hardware (A100, H100) remains prohibitively expensive. We present a systematic e...

---

### 18. [Forecast Aware Deep Reinforcement Learning for Efficient Electricity Load Scheduling in Dairy Farms](https://arxiv.org/abs/2601.08052)

**Authors**: Nawazish Alia, Rachael Shawb, Karl Mason  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08052v1  

#### Abstract
Dairy farming is an energy intensive sector that relies heavily on grid electricity. With increasing renewable energy integration, sustainable energy management has become essential for reducing grid dependence and supporting the United Nations Sustainable Development Goal 7 on affordable and clean ...

---

### 19. [SlidesGen-Bench: Evaluating Slides Generation via Computational and Quantitative Metrics](https://arxiv.org/abs/2601.09487)

**Authors**: Yunqiao Yang, Wenbo Li, Houxing Ren, Zimu Lu, Ke Wang, Zhiyuan Huang, Zhuofan Zong, Mingjie Zhan, Hongsheng Li  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.09487v1  

#### Abstract
The rapid evolution of Large Language Models (LLMs) has fostered diverse paradigms for automated slide generation, ranging from code-driven layouts to image-centric synthesis. However, evaluating these heterogeneous systems remains challenging, as existing protocols often struggle to provide compara...

---

### 20. [SRT: Accelerating Reinforcement Learning via Speculative Rollout with Tree-Structured Cache](https://arxiv.org/abs/2601.09083)

**Authors**: Chi-Chih Chang, Siqi Zhu, Zhichen Zeng, Haibin Lin, Jiaxuan You, Mohamed S. Abdelfattah, Ziheng Jiang, Xuehai Qian  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.09083v1  

#### Abstract
We present Speculative Rollout with Tree-Structured Cache (SRT), a simple, model-free approach to accelerate on-policy reinforcement learning (RL) for language models without sacrificing distributional correctness. SRT exploits the empirical similarity of rollouts for the same prompt across training...

---

### 21. [Deconstructing Pre-training: Knowledge Attribution Analysis in MoE and Dense Models](https://arxiv.org/abs/2601.08383)

**Authors**: Bo Wang, Junzhuo Li, Hong Chen, Yuanlin Chu, Yuxuan Fan, Xuming Hu  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08383v1  

#### Abstract
Mixture-of-Experts (MoE) architectures decouple model capacity from per-token computation, enabling scaling beyond the computational limits imposed by dense scaling laws. Yet how MoE architectures shape knowledge acquisition during pre-training, and how this process differs from dense architectures,...

---

### 22. [MEMEWEAVER: Inter-Meme Graph Reasoning for Sexism and Misogyny Detection](https://arxiv.org/abs/2601.08684)

**Authors**: Paolo Italiani, David Gimeno-Gomez, Luca Ragazzi, Gianluca Moro, Paolo Rosso  
**Category**: cs.AI  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08684v1  

#### Abstract
Women are twice as likely as men to face online harassment due to their gender. Despite recent advances in multimodal content moderation, most approaches still overlook the social dynamics behind this phenomenon, where perpetrators reinforce prejudices and group identity within like-minded communiti...

---

### 23. [Adaptive Multi-Stage Patent Claim Generation with Unified Quality Assessment](https://arxiv.org/abs/2601.09120)

**Authors**: Chen-Wei Liang, Bin Guo, Zhen-Yuan Wei, Mu-Jiang-Shan Wang  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09120v1  

#### Abstract
Current patent claim generation systems face three fundamental limitations: poor cross-jurisdictional generalization, inadequate semantic relationship modeling between claims and prior art, and unreliable quality assessment. We introduce a novel three-stage framework that addresses these challenges ...

---

### 24. [A.X K1 Technical Report](https://arxiv.org/abs/2601.09200)

**Authors**: Sung Jun Cheon, Jaekyung Cho, Seongho Choi, Hyunjun Eun, Seokhwan Jo, Jaehyun Jun, Minsoo Kang, Jin Kim, Jiwon Kim, Minsang Kim, Sungwan Kim, Seungsik Kim, Tae Yoon Kim, Youngrang Kim, Hyeongmun Lee, Sangyeol Lee, Sungeun Lee, Youngsoon Lee, Yujin Lee, Seongmin Ok, Chanyong Park, Hyewoong Park, Junyoung Park, Hyunho Yang, Subin Yi, Soohyun Bae, Dhammiko Arya, Yongseok Choi, Sangho Choi, Dongyeon Cho, Seungmo Cho, Gyoungeun Han, Yong-jin Han, Seokyoung Hong, Hyeon Hwang, Wonbeom Jang, Minjeong Ju, Wonjin Jung, Keummin Ka, Sungil Kang, Dongnam Kim, Joonghoon Kim, Jonghwi Kim, SaeRom Kim, Sangjin Kim, Seongwon Kim, Youngjin Kim, Seojin Lee, Sunwoo Lee, Taehoon Lee, Chanwoo Park, Sohee Park, Sooyeon Park, Yohan Ra, Sereimony Sek, Seungyeon Seo, Gun Song, Sanghoon Woo, Janghan Yoon, Sungbin Yoon  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09200v1  

#### Abstract
We introduce A.X K1, a 519B-parameter Mixture-of-Experts (MoE) language model trained from scratch. Our design leverages scaling laws to optimize training configurations and vocabulary size under fixed computational budgets. A.X K1 is pre-trained on a corpus of approximately 10T tokens, curated by a...

---

### 25. [Dialogue Telemetry: Turn-Level Instrumentation for Autonomous Information Gathering](https://arxiv.org/abs/2601.09570)

**Authors**: Dimitris Panagopoulos, Adolfo Perrusquia, Weisi Guo  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09570v1  

#### Abstract
Autonomous systems conducting schema-grounded information-gathering dialogues face an instrumentation gap, lacking turn-level observables for monitoring acquisition efficiency and detecting when questioning becomes unproductive. We introduce Dialogue Telemetry (DT), a measurement framework that prod...

---

### 26. [DPWriter: Reinforcement Learning with Diverse Planning Branching for Creative Writing](https://arxiv.org/abs/2601.09609)

**Authors**: Qian Cao, Yahui Liu, Wei Bi, Yi Zhao, Ruihua Song, Xiting Wang, Ruiming Tang, Guorui Zhou, Han Li  
**Category**: cs.CL  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09609v1  

#### Abstract
Reinforcement learning (RL)-based enhancement of large language models (LLMs) often leads to reduced output diversity, undermining their utility in open-ended tasks like creative writing. Current methods lack explicit mechanisms for guiding diverse exploration and instead prioritize optimization eff...

---

### 27. [XGBoost Forecasting of NEPSE Index Log Returns with Walk Forward Validation](https://arxiv.org/abs/2601.08896)

**Authors**: Sahaj Raj Malla, Shreeyash Kayastha, Rumi Suwal, Harish Chandra Bhandari, Rajendra Adhikari  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08896v1  

#### Abstract
This study develops a robust machine learning framework for one-step-ahead forecasting of daily log-returns in the Nepal Stock Exchange (NEPSE) Index using the XGBoost regressor. A comprehensive feature set is engineered, including lagged log-returns (up to 30 days) and established technical indicat...

---

### 28. [Comparative Assessment of Concrete Compressive Strength Prediction at Industry Scale Using Embedding-based Neural Networks, Transformers, and Traditional Machine Learning Approaches](https://arxiv.org/abs/2601.09096)

**Authors**: Md Asiful Islam, Md Ahmed Al Muzaddid, Afia Jahin Prema, Sreenath Reddy Vuske  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09096v1  

#### Abstract
Concrete is the most widely used construction material worldwide; however, reliable prediction of compressive strength remains challenging due to material heterogeneity, variable mix proportions, and sensitivity to field and environmental conditions. Recent advances in artificial intelligence enable...

---

### 29. [Efficient Clustering in Stochastic Bandits](https://arxiv.org/abs/2601.09162)

**Authors**: G Dhinesh Chandran, Kota Srinivas Reddy, Srikrishna Bhashyam  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09162v1  

#### Abstract
We study the Bandit Clustering (BC) problem under the fixed confidence setting, where the objective is to group a collection of data sequences (arms) into clusters through sequential sampling from adaptively selected arms at each time step while ensuring a fixed error probability at the stopping tim...

---

### 30. [RIFT: Repurposing Negative Samples via Reward-Informed Fine-Tuning](https://arxiv.org/abs/2601.09253)

**Authors**: Zehua Liu, Shuqi Liu, Tao Zhong, Mingxuan Yuan  
**Category**: cs.LG  
**Published**: 2026-01-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.09253v1  

#### Abstract
While Supervised Fine-Tuning (SFT) and Rejection Sampling Fine-Tuning (RFT) are standard for LLM alignment, they either rely on costly expert data or discard valuable negative samples, leading to data inefficiency. To address this, we propose Reward Informed Fine-Tuning (RIFT), a simple yet effectiv...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
