# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-11 06:48:02 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [LLM-CoOpt: A Co-Design and Optimization Framework for Efficient LLM Inference on Heterogeneous Platforms](https://arxiv.org/abs/2602.09323)

**Authors**: Jie Kong, Wei Wang, Jiehan Zhou, Chen Yu  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 17.0  
**Type**: new  
**ArXiv ID**: 2602.09323v1  

#### Abstract
Major challenges in LLMs inference remain frequent memory bandwidth bottlenecks, computational redundancy, and inefficiencies in long-sequence processing. To address these issues, we propose LLM-CoOpt, a comprehensive algorithmhardware co-design framework aimed at improving both throughput and laten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-CoOpt: A Co-Design and Optimization Framework for Efficient LLM Inference on Heterogeneous Platforms

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题  
大型语言模型（LLMs）在异构平台上的推理面临三大核心挑战：
- **KV Cache 内存瓶颈**：自回归解码过程中，Key-Value 缓存随序列长度线性增长，导致显存占用高、带宽压力大；
- **Multi-Head Attention 的计算冗余**：每个注意力头独立生成 K/V 向量，造成重复计算和硬件资源利用率低；
- **长序列处理效率低下**：标准 Self-Attention 具有 $O(T^2)$ 复杂度，PagedAttention 虽缓解内存压力但仍存在同步开销和碎片化问题。

这些问题在资源受限的异构平台上尤为突出，限制了 LLM 在边缘设备或专用加速器上的高效部署。

### 提出了什么新方法或新思路  
作者提出 **LLM-CoOpt** —— 一种面向异构平台的算法-硬件协同优化框架，集成三项关键技术：

#### ✅ Opt-KV：动态 KV Cache 优化
- **写阶段优化**：通过条件判断跳过无效 token 的 KV 缓存写入（如 padding 或重复 token），减少冗余写操作；
- **读阶段压缩**：采用 **FP8 量化** 存储有效 KV 块，并支持 on-the-fly decompression，在降低存储开销的同时保持精度；
- 实现更高效的 memory access 和 bandwidth utilization。

#### ✅ Opt-GQA：轻量级 Grouped-Query Attention
- 将 query heads 分组，每组共享一组 K/V heads，形成灵活的 grouped-query 结构；
- 减少 K/V 投影数量，显著降低计算与内存开销；
- 支持动态分组策略，适应不同输入分布和硬件并行能力。

#### ✅ Opt-Pa：优化的 Paged Attention 设计
- 两步策略提升长序列处理效率：
  1. **过滤有效块**：仅加载实际上下文范围内的逻辑块，跳过 padding 区域；
  2. **共享内存归约**：用 `block_sum` 替代 warp-level reduction，减少 Softmax 中的全局同步开销，提高数值稳定性；
- 显著改善 memory locality 与 thread synchronization 效率。

### 相比现有方法的优势
| 方面 | 现有方法局限 | LLM-CoOpt 改进 |
|------|----------------|----------------|
| KV Cache | 静态量化不适应动态范围；统一加载所有历史块 | 动态跳写 + FP8 压缩 + 按需解压 |
| GQA | 固定分组策略缺乏灵活性 | 可配置分组，更好匹配硬件并行性 |
| PagedAttention | 存在内存碎片与同步延迟 | 过滤无效块 + 共享内存 reduction 降低 overhead |

> ✅ **整体优势**：实现了 throughput、latency 与 accuracy 的良好平衡，特别适用于资源受限的异构平台（如国产 DCU）。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **吞吐量评估**：`ShareGPT_V3_unfiltered_cleaned_split` 数据集
  - 包含约 35,240 条真实对话样本，涵盖问答、闲聊、知识咨询等场景；
  - 总数据量 ~178 MB，用于模拟多样化的推理负载。
- **准确性评估**：`ARC` 数据集（AI2 Reasoning Challenge）
  - 分为两个子集：
    - **ARC-E (Easy Set)**：易回答的问题；
    - **ARC-C (Challenge Set)**：检索与共现算法均失败的问题，更具挑战性；
  - 共 7,787 道小学科学类选择题，测试模型推理能力。

### 实验设置
- **硬件平台**：DCU Z100 异构计算平台
  - L2 Cache: ~4MB
  - Wavefront Size: 64
  - 内存带宽: 512 GB/s (GDDR6)
  - 支持 FP16 / 模拟 FP8（via INT8）
- **软件环境**：基于未优化的 `vLLM v0.3.3` 构建基准系统
- **测试模型**：
  - LLaMa-7B-GPTQ
  - LLaMa2-7B-GPTQ
  - LLaMa-13B-GPTQ
  - LLaMa2-13B-GPTQ
  - LLaMa-Pro-8B-GPTQ

### 评估指标
| 指标 | 定义 | 公式 |
|------|------|-------|
| **Inference Latency** | 单次任务平均响应时间 | $\text{Latency} = \frac{1}{N}\sum_{i=1}^{N} \text{latency}_i$ |
| **Generation Throughput** | 每秒生成 token 数 | $\text{Throughput} = \frac{\text{Total Generated Tokens}}{\text{Generation Time}}$ |
| **Accuracy** | 正确预测比例 | $\text{Accuracy} = \frac{N_{\text{correct}}}{N_{\text{total}}} \times 100\%$ |

### 基线方法对比
- **Baseline**：原始 vLLM 框架（无额外优化）
- **消融实验组**：
  - vLLM + Opt-KV
  - vLLM + Opt-GQA
  - vLLM + Opt-Pa
  - vLLM + LLM-CoOpt（三者联合）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 LLaMa-13B-GPTQ 为例）

| 方法 | 推理延迟 ↓ | 生成吞吐 ↑ | ARC-C 准确率 |
|------|------------|-------------|--------------|
| Original (vLLM) | 基准 | 基准 | 39.66% |
| + Opt-KV | -6.18% | +7.20% | ≈持平 |
| + Opt-GQA | -6.75% | +12.13% | ≈持平 |
| + Opt-Pa | -5.48% | +10.85% | ≈持平 |
| **LLM-CoOpt** | **-16.79%** | **+13.43%** | **40.01%** |

> 注：最大延迟降低达 **16.79%**，最高吞吐提升达 **13.43%**

### 与其他模型的结果一致性
- 所有测试模型均表现出稳定增益，说明 LLM-CoOpt 具备良好的泛化性和可扩展性；
- 特别是在 **13B 规模模型上收益最明显**，表明其对大模型更具价值。

### 消融实验结果
- 三个组件均有独立贡献，且组合后呈现**协同效应（synergistic benefit）**；
- Opt-GQA 对 throughput 提升最大；
- Opt-KV 在 memory footprint 和 bandwidth 利用方面表现最优；
- Opt-Pa 显著提升了长序列场景下的执行稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **内存与计算协同优化至关重要**：单纯优化某一方面（如仅 KV 压缩）无法根本解决异构平台瓶颈，必须进行算法-硬件 co-design；
2. **FP8 量化可用于 KV Cache**：结合 selective caching 与 on-the-fly decompression，可在几乎不影响 accuracy 的前提下大幅节省显存；
3. **Grouped Query 结构优于传统 MHA**：Opt-GQA 在保证表达能力的同时显著减少冗余计算，尤其适合多任务并发场景；
4. **PagedAttention 可进一步优化**：通过 block-wise filtering 与 shared-memory reduction，能有效缓解同步与碎片问题；
5. **LLM-CoOpt 不损害模型准确性**：在 ARC-C/E 上 accuracy 基本不变甚至略有上升，验证了优化的安全性。

### 方法的局限性
- 当前 FP8 支持依赖于 INT8 模拟，尚未完全发挥专用硬件潜力；
- 分组策略仍需手动配置，缺乏全自动 adaptive grouping 机制；
- 实验集中在 GPTQ 量化模型，是否适用于其他量化方式（如 AWQ、INT4）有待验证；
- 未涉及多 GPU/多节点分布式推理场景。

### 未来工作方向
1. **探索 Multi-modal 场景下的扩展应用**：将 LLM-CoOpt 思路迁移到图文、音视频等跨模态模型中；
2. **引入 Dynamic Batch Processing 与 Sparse Computing**：进一步提升复杂部署环境下的资源利用率；
3. **开发自动化的参数调优系统**：实现 Opt-GQA 分组数、Opt-Pa 块大小等超参的 runtime 自适应调整；
4. **支持更多异构架构**：适配国产 AI 芯片、FPGA、NPU 等多样化硬件平台，推动通用化部署。

---

> 🔚 **总结一句话**：  
> **LLM-CoOpt 通过 Opt-KV、Opt-GQA 与 Opt-Pa 三重协同优化，在几乎不损失 accuracy 的前提下，显著提升了 LLM 在异构平台上的推理 throughput 与 latency 表现，为高效、实用的大模型边缘部署提供了可行路径。**

</details>

---

### 2. [Rollout-Training Co-Design for Efficient LLM-Based Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2602.09578)

**Authors**: Zhida Jiang, Zhaolong Xing, Jiawei Lu, Yipei Niu, Qingyuan Sang, Liangxu Zhang, Wenquan Dai, Junhua Shu, Jiaxing Wang, Qiangyu Pei, Qiong Chen, Xinyu Liu, Fangming Liu, Ai Han, Zhen Chen, Ke Zhang  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 14.5  
**Type**: new  
**ArXiv ID**: 2602.09578v1  

#### Abstract
Despite algorithm-level innovations for multi-agent reinforcement learning (MARL), the underlying networked infrastructure for large-scale MARL training remains underexplored. Existing training frameworks primarily optimize for single-agent scenarios and fail to address the unique system-level chall...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rollout-Training Co-Design for Efficient LLM-Based Multi-Agent Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Reinforcement Learning (RL)** 和 **Multi-Agent Reinforcement Learning (MARL)** 框架在系统层面存在三大瓶颈，严重制约大规模 LLM-based MARL 的训练效率：

- **Rollout-Training 同步障碍**：由于部分 agent 响应时间极长（long-tail effect），整个训练必须等待最慢的 rollout 完成，造成严重的 pipeline 阻塞。
- **Rollout 负载不均衡**：核心 agent 被频繁调用导致请求堆积，而辅助 agent 则资源闲置，形成负载倾斜。
- **训练资源利用率低**：静态资源分配策略下，未激活 agent 占用的计算与内存资源无法被释放，导致整体硬件利用率低下。

现有框架如 OpenRLHF、veRL 等仅支持单 agent 场景；而专为 MARL 设计的 MARTI 仍采用 **colocated 架构** 和同步训练范式，未能从基础设施层面进行端到端优化。

---

### 提出的新方法：FlexMARL
作者提出 **FlexMARL** ——首个面向大规模 LLM-based MARL 的端到端训练框架，通过 **rollout 与 training 的协同设计（co-design）** 实现系统级优化。其三大核心组件如下：

#### ✅ Joint Orchestrator（联合调度器）
- 引入 **experience store** 作为结构化存储模块，管理跨 agent 的样本生命周期。
- 设计 **micro-batch driven 异步 pipeline**，将全局 batch 拆分为 micro-batches，实现 rollout 与 training 的细粒度重叠。
- 在解耦梯度计算与参数更新的同时，通过版本追踪机制保证同步训练语义（strong consistency），避免参数陈旧（parameter staleness）问题。

#### ✅ Rollout Engine（并行采样 + 分层负载均衡）
- 采用 **parallel sampling scheme** 支持 query-level 和 trajectory-level 并行执行，显著降低推理延迟。
- 实现 **hierarchical load balancing**：
  - **Intra-agent**：基于最小堆动态调度至负载最低的 inference instance。
  - **Inter-agent**：当负载差异超过阈值时，自动迁移 inference 实例（含模型权重传输），缓解热点问题。

#### ✅ Training Engine（以 agent 为中心的资源调度）
- 推出 **agent-centric resource allocation**，仅在需要时才为 agent 动态绑定硬件资源。
- 引入 **process group 抽象** 实现 gang scheduling，支持训练进程的 suspend/resume。
- 利用统一的 **Set/Get API** 实现训练状态（weights & optimizer states）在 device 与 host 内存间的高效 swap-in/out，避免 OOM。

---

### 相比现有方法的优势
| 特性 | FlexMARL | 现有框架（如 MARTI, OpenRLHF） |
|------|----------|-------------------------------|
| 多 agent 支持 | ✅ 全面支持 | ❌ 或弱支持 |
| Rollout-Training 解耦部署 | ✅ disaggregated 架构 | ❌ colocated 架构 |
| 异步 pipeline | ✅ micro-batch 异步，保留同步语义 | ❌ 同步或粗粒度异步 |
| Rollout 负载均衡 | ✅ 分层弹性扩缩容 | ❌ 固定实例数 |
| 资源按需分配 | ✅ agent-centric 动态绑定 | ❌ 静态预分配 |
| 跨节点部署能力 | ✅ 支持复杂异构配置 | ❌ 通常限于小规模集群 |

---

## 2. 核心实验方法和设置

### 数据集
- **Merchant Assistant (MA) Dataset**  
  多个 agent 协作完成电商商家店铺管理任务（销售分析、营销优化、售后等），使用 **Qwen2.5-14B** 模型。
- **Category Assistant (CA) Dataset**  
  聚焦订单查询、定价策略与库存建议，涉及两种模型规模：**Qwen2.5-14B 和 Qwen2.5-32B**。

> 注：因商业保密原因，具体数据细节未公开。

---

### 实验设置
- **硬件环境**：48 节点集群，每节点配备 16 个商用 NPU（64GB 显存），通过 HCCS 互联。
- **并行配置**：
  - Inter-query parallelism = 4
  - Intra-query parallelism = 16
- **训练参数**：
  - Batch size = 64，micro-batch size = 16
  - 使用 GRPO 算法，Adam 优化器，学习率 1e-6
- **实现基础**：
  - Rollout backend：vLLM v0.9.1
  - Training backend：DeepSpeed v0.16.9 + ZeRO-3

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **E2E Time** | 单个样本全流程平均耗时（rollout + training + others） |
| **Speedup** | 相对于基线 MAS-RL 的加速比 |
| **Throughput** | 每秒生成 token 数（tps） |
| **Agent Load** | 各 agent 处理的 rollout 请求数量 |
| **Hardware Utilization Rate** | AI 核心活跃时间占比 |
| **Swap-in/out Overhead** | 训练状态加载/卸载延迟 |

---

### 基线方法对比
| 基线 | 简介 |
|------|------|
| **MAS-RL** | 将单 agent RL 框架直接迁移到多 agent 场景，采用 colocated 架构与同步流程 |
| **DistRL** | 改进版 disaggregated 架构，分离 rollout 与 training 资源池，但仍为同步 pipeline |
| **MARTI (ICLR 2026)** | 当前最先进的 MARL 框架，支持分布式训练与异步 rollout，但资源静态分配 |

---

## 3. 主要实验结果和性能指标

### 性能总览（Table 2）
| Dataset | Framework | E2E Time | Speedup | Throughput |
|--------|-----------|---------|--------|------------|
| MA | MAS-RL | 914.4s | 1.0× | 119.0 tps |
| MA | DistRL | 293.8s | 3.1× | 401.0 tps |
| MA | MARTI | 174.1s | 5.3× | 642.8 tps |
| MA | **FlexMARL** | **126.1s** | **7.3×** | **910.2 tps** |
| CA | MAS-RL | 438.6s | 1.0× | 265.5 tps |
| CA | DistRL | 130.0s | 3.4× | 571.6 tps |
| CA | MARTI | 112.8s | 3.9× | 655.9 tps |
| CA | **FlexMARL** | **78.8s** | **5.6×** | **821.4 tps** |

> ✅ FlexMARL 在两个真实工业数据集上均取得最优性能，最高达 **7.3× 加速** 与 **910.2 tps 吞吐量**。

---

### 关键性能表现

#### 🔹 RQ2: Rollout 负载均衡效果（Figures 8–9）
- FlexMARL 在相同时间内处理的 rollout 请求远超其他方法。
- 例如，在 MA 数据集上，Agent B 的请求完成时间从 MARTI 的 159s 缩短至 **90s**（提速 1.8×）。
- 负载曲线显示 FlexMARL 更快积累且更快清空队列，验证了分层负载均衡的有效性。

#### 🔹 RQ3: 硬件利用率提升（Figure 10）
- FlexMARL 平均硬件利用率达：
  - **32.4%（MA）**
  - **19.8%（CA）**
- 远高于基线：
  - MAS-RL: 仅 3.6%（CA）
  - MARTI: 12.3%（CA）
  - DistRL: 10.2%（CA）

> 💡 传统 colocated 架构因资源争抢导致严重浪费，而 FlexMARL 的动态绑定显著减少 idle 时间。

#### 🔹 RQ4: 训练状态交换开销（Figure 11）
| Model Size | Swap-in Overhead | Swap-out Overhead |
|-----------|------------------|--------------------|
| 3B → 7B → 14B → 32B | 增加平缓，最大约 **11s** | 同上 |

- 控制平面开销极低（suspend/resume 几乎无延迟）。
- 数据迁移开销随模型增大而增长，但在实际中可被 rollout 延迟掩盖，不影响整体吞吐。

#### 🔹 RQ5: 消融实验（Table 3）
| Variant | E2E Time (MA) | Speedup | Throughput |
|--------|---------------|--------|-----------|
| w/o balancing | 152.2s | 6.0× | 729.9 tps |
| w/o async | 256.2s | 3.6× | 444.0 tps |
| **Full FlexMARL** | **126.1s** | **7.3×** | **910.2 tps** |

> ⚠️ 移除任一组件都会带来显著性能下降，证明 **hierarchical load balancing** 与 **micro-batch async pipeline** 是性能突破的关键。

#### 🔹 RQ6: 大规模扩展性测试（Table 4）
| 配置 | E2E Time | Throughput |
|------|---------|----------|
| 5×32B | 160.3s | 265.9 tps |
| 3×32B + 7×14B（异构混合） | 132.5s | 334.8 tps |
| 15×14B | **41.9s** | **754.2 tps** |

> ✅ FlexMARL 成功支持大规模异构部署（如 32B + 14B 混合），而现有框架（如 MARTI）在此类场景下易出现 OOM 错误。

---

## 4. 关键结论和发现

### 主要发现
1. **Disaggregated Architecture + Fine-grained Async Pipeline 是打破同步瓶颈的关键**  
   FlexMARL 通过 experience store 解耦 rollout 与 training，并利用 micro-batch 流水线隐藏 long-tail latency，实现高吞吐与强一致性兼顾。

2. **Hierarchical Load Balancing 显著改善 rollout 效率**  
   动态调整 inference 实例数量，结合 D2D 权重迁移，有效缓解热点 agent 的请求积压问题。

3. **Agent-Centric Resource Allocation 极大提升硬件利用率**  
   “按需绑定 + 快速状态交换”机制使得数千 agent 共享有限硬件成为可能，避免资源锁死。

4. **FlexMARL 具备工业级可扩展性**  
   支持跨节点部署、异构模型混合训练，在真实生产环境中展现出卓越稳定性与性能。

---

### 局限性
- **依赖高性能通信网络**：D2D/H2D 权重迁移对 RDMA 或 HCCS 等高速互连有较强依赖。
- **控制平面复杂度增加**：引入 experience store、Set/Get API、process group 等抽象，增加了系统开发与调试难度。
- **尚未探索更复杂的 credit assignment 机制**：目前 credit assignment 假设已知，未来可集成更智能的反向归因算法。

---

### 未来工作方向
- 扩展至 **multi-turn, long-horizon** 场景下的动态 agent 组合与角色演化。
- 结合 **MoE（Mixture of Experts）** 架构进一步优化 agent 粒度的资源调度。
- 探索 **fault-tolerant 机制** 以应对大规模分布式训练中的节点失效问题。
- 将 FlexMARL 应用于更多领域，如自动驾驶、金融决策、科学发现等复杂协作系统。

---

> 📌 **总结一句话**：  
> **FlexMARL 是首个真正实现 rollout 与 training 协同设计的大规模 LLM-based MARL 框架，通过 disaggregation、micro-batch async pipeline、hierarchical load balancing 与 agent-centric resource allocation，在真实工业场景中实现了高达 7.3× 的端到端加速与 5.6× 的硬件利用率提升。**

</details>

---

### 3. [Training deep physical neural networks with local physical information bottleneck](https://arxiv.org/abs/2602.09569)

**Authors**: Hao Wang, Ziao Wang, Xiangpeng Liang, Han Zhao, Jianqi Hu, Junjie Jiang, Xing Fu, Jianshi Tang, Huaqiang Wu, Sylvain Gigan, Qiang Liu  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.09569v1  

#### Abstract
Deep learning has revolutionized modern society but faces growing energy and latency constraints. Deep physical neural networks (PNNs) are interconnected computing systems that directly exploit analog dynamics for energy-efficient, ultrafast AI execution. Realizing this potential, however, requires ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Training deep physical neural networks with local physical information bottleneck*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前深度物理神经网络（**deep Physical Neural Networks, PNNs**）面临以下挑战：
- **模拟-现实差距**（simulation-reality gap）：传统 in silico 训练依赖精确建模，难以适配真实硬件中的非理想性（如噪声、漂移、故障）。
- **训练效率低**：基于反向传播（backpropagation, BP）的方法需要端到端梯度流，对数字辅助模型和全局同步要求高。
- **通用性差**：现有方法通常针对特定物理平台设计，难以跨平台迁移。
- **可扩展性受限**：深层级联结构下，误差传播困难，尤其在非微分系统中。

### 🚀 提出的新方法：Physical Information Bottleneck (PIB)
作者提出 **PIB** ——一种将信息论与局部学习结合的通用训练框架，其核心思想是：
- 将每个物理计算单元视为一个**信息通道**（information channel），通过优化局部目标函数来提取任务相关特征并抑制冗余输入信息。
- 局部目标为：  
  $$
  \mathcal{L}(Z^l, \beta) = I(Y; Z^l) - \beta I(X; Z^l)
  $$
  其中 $Z^l$ 是第 $l$ 层输出特征，$X$ 是原始输入，$Y$ 是标签，$I(\cdot;\cdot)$ 表示互信息。

该方法的关键创新在于：
- **矩阵化互信息估计**（matrix-based mutual information）：避免高维空间中传统互信息估计的不可行性，使理论可落地于实验系统。
- **完全本地化训练**：每层仅需本地测量输出、全局输入 $X$ 和目标 $Y$，无需存储中间梯度或执行全网前向传播。
- **不依赖数字代理模型或对比测量**：摆脱 hybrid training 中的仿真依赖和 PhyLL 类方法的数据冗余开销。

### 🔍 相比现有方法的优势
| 方法 | 是否需数字模型 | 是否支持非微分系统 | 是否支持分布式训练 | 抗噪/容错能力 |
|------|----------------|--------------------|---------------------|---------------|
| End-to-end BP | 是 | 否 | 否 | 弱 |
| Physics-aware training (PAT) | 是 | 部分 | 否 | 中等 |
| Direct Feedback Alignment (DFA) | 否 | 是 | 否 | 中等 |
| Physical Local Learning (PhyLL) | 否 | 是 | 否 | 弱（数据效率低） |
| **PIB（本文）** | ❌ | ✅ | ✅ | ✅✅（强） |

> ✅ **优势总结**：
> - **高兼容性**：适用于 isomorphic（如 memristor）和 broken-isomorphism（如光学散射介质）两类 PNN 单元；
> - **高效鲁棒**：本地训练 + 信息压缩机制提升抗噪性和泛化能力；
> - **可扩展性强**：支持并行、去中心化的地理分布训练；
> - **硬件友好**：适应严重硬件故障（如ADC损坏），动态恢复性能。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 实验场景 | 数据集 | 描述 |
|--------|-------|------|
| 分类任务 | **MNIST**, **MNIST-1D**, **Fashion-MNIST** | 手写数字、简化一维版本、服饰图像分类 |
| 自监督学习 | Fashion-MNIST（无标签） | 学习语义分离的表示 |
| 强化学习 | **CartPole-v1**（自定义环境） | 控制倒立摆任务 |

### ⚙️ 实验设置与评估指标

#### 平台一：基于 **memristor** 的电子 isomorphic 单元
- 构建多层感知机（MLP）和卷积神经网络（CNN）
- 输入/权重编码为电压/电导状态，利用欧姆定律和基尔霍夫定律实现 MVM
- 测量输出电流作为特征
- **评估指标**：
  - 分类准确率（Accuracy）
  - 对比 in silico BP 与部署后性能下降
  - 故障恢复能力（broken ADC 场景下的精度回升）

#### 平台二：基于 **optical scattering medium** 的光学 broken-isomorphism 单元
- 利用多重光散射生成 speckle pattern 作为非线性特征映射
- 使用商用 OPU 或自研 SLM 系统，不获取传输矩阵（transmission matrix）
- 数字层可训练，物理部分固定
- **评估指标**：
  - “Fit-then-compress” 动态轨迹分析（信息平面可视化）
  - 噪声鲁棒性测试（输入加噪）
  - 小样本学习（data efficiency）
  - Out-of-Distribution (OOD) 检测能力

#### 多场景拓展实验
- **Unsupervised Learning**：修改 PIB 目标为 $I(Z_1;Z_2) - \beta[I(X_1;Z_1)+I(X_2;Z_2)]$，用于对比学习风格的自监督训练
- **Reinforcement Learning**：结合 TD Q-learning loss 与 PIB，实现异步更新以掩盖硬件延迟
- **Decentralized Training**：在北京（memristor）和巴黎（optical）两地并行训练混合 deep PNN

### 🆚 基线方法对比
| 基线方法 | 类型 | 特点 |
|--------|-----|------|
| End-to-end BP (in silico) | 梯度法 | 上界参考，但存在 simulation-reality gap |
| PAT (Physics-aware training) | Hybrid BP | 依赖数字模型，易受建模误差影响 |
| DFA (Direct Feedback Alignment) | 模型无关 | 不需真实梯度，但性能常低于 BP |
| PhyLL (Physical Local Learning) | 局部对比学习 | 不需模型，但需多次前传与对比样本，效率低 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

| 实验 | 方法 | 性能表现 |
|------|------|---------|
| **MNIST 分类（memristor）** | PIB（实验） | **96.3%** 准确率（均值 ± std，六次运行） |
| | In silico BP（仿真） | 96.4% |
| | In silico BP（部署后） | 下降至 **94.0%** |
| **MNIST-1D 分类** | PIB（实验 vs 仿真） | 性能几乎一致，接近 BP 上限 |
| | In silico BP（部署后） | 下降 **8.5%** |
| **硬件故障恢复（ADC损坏）** | 初始精度 | **73.5%** |
| | 经 PIB 自适应重训后 | 恢复至 **>90.0%** |
| **Fashion-MNIST（光学平台）** | OPU（binary 编码） | 最高达 **86.4%** |
| | SLM setup（8-bit，子集） | 达 **93.4%** |
| **强化学习（CartPole-v1）** | PIB + TD loss | 成功达到“solved regime”（平均回合得分 ≥ 195） |
| **去中心化训练** | 北京 + 巴黎双节点 | 双方独立收敛，最终完成下游分类任务 |

### 🔁 与基线方法对比结果（Fig. 3C）
在 **noise robustness**, **data efficiency**, 和 **OOD detection** 三个维度上全面优于基线：
- 在 **5% 输入噪声强度** 下，PIB 准确率仍保持高位，显著优于 PAT、DFA 和 PhyLL；
- 在小训练集（$10^3 \sim 10^4$ 样本）条件下，PIB 更快饱和；
- OOD 检测率更高，表明学到的表征更具判别性和泛化性。

### 🔍 消融实验与关键观察
- **信息平面动态分析**（Fig. 3B）显示典型的“先拟合后压缩”（fit-then-compress）行为：
  - 初期 $I(Y;Z^l)$ 快速上升 → 捕获任务相关信息
  - 后期 $I(X;Z^l)$ 下降而 $I(Y;Z^l)$ 保持 → 压缩无关变异
- **UMAP 可视化** 显示随着训练迭代增加，特征逐渐聚类清晰（Fig. 2C）
- **自监督学习中无标签语义解耦**：memristive PNN 能自动区分“上装/下装”与“鞋类”，验证了表示质量（Fig. 4A inset）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **PIB 是首个将信息瓶颈原理转化为可操作训练目标的 PNN 框架**，实现了从解释工具到主动优化器的跃迁。
2. **本地化 + 信息压缩机制天然增强鲁棒性**：不仅能抵抗硬件噪声，还能在严重组件故障下动态恢复性能。
3. **无需数字代理模型或对比测量**，极大降低实验复杂度和资源消耗，真正实现“intrinsic”训练。
4. **支持跨平台通用性**：成功应用于 memristor（isomorphic）和 optical scattering（broken-isomorphism）两种截然不同的物理系统。
5. **具备强大扩展潜力**：支持自监督、强化学习及地理分布式的并行训练，为未来大规模 PNN 部署提供可行路径。

### ⚠️ 方法的局限性
- 当前仍依赖 **digital auto-differentiation** 来计算参数梯度（例如对数字层权重），尚未完全摆脱数字计算依赖（见 Supplementary Table 1）。
- 对于极深网络，局部优化可能偏离全局最优，性能略逊于理想 end-to-end BP（尽管差距正在缩小）。
- 矩阵化互信息虽可计算，但在极高维系统中仍可能存在估计偏差。

### 🔮 未来工作方向
- 结合 **surrogate gradient 方法**（如 DFA）进一步消除对 auto-diff 的依赖，实现纯物理梯度近似。
- 探索如何引入 **inductive bias** 到物理单元本身，提升深层表达能力。
- 扩展至更多物理平台，如 **acoustic wave reservoirs**, **magnetic skyrmions**, **active matter** 等。
- 探索 PIB 在 **scaling laws** 方面的表现，研究 deep PNN 是否也能呈现类似大模型的涌现能力。

---

> 💬 **总体评价**：  
> 本文提出的 **PIB 框架** 是物理神经网络训练领域的一项重要突破。它不仅解决了长期存在的“模拟-现实鸿沟”问题，还通过信息论视角提供了新的理论洞见，并展示了卓越的实用性与可扩展性。该工作有望推动 deep PNN 向更高效、更鲁棒、更通用的方向发展，成为通向超低功耗、超高速 AI 硬件的重要基石。

</details>

---

### 4. [DLLM-Searcher: Adapting Diffusion Large Language Model for Search Agents](https://arxiv.org/abs/2602.07035)

**Authors**: Jiahao Zhao, Shaoxuan Xu, Zhongxiang Sun, Fengqi Zhu, Jingyang Ou, Yuling Shi, Chongxuan Li, Xiao Zhang, Jun Xu  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.07035v1  

#### Abstract
Recently, Diffusion Large Language Models (dLLMs) have demonstrated unique efficiency advantages, enabled by their inherently parallel decoding mechanism and flexible generation paradigm. Meanwhile, despite the rapid advancement of Search Agents, their practical deployment is constrained by a fundam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM-Searcher: Adapting Diffusion Large Language Models for Search Agents

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对当前 **Search Agents** 在实际部署中面临的两大核心挑战：

1. **Latency Challenge（延迟挑战）**：在标准的 ReAct 范式下，模型需串行执行“思考 → 工具调用 → 等待工具响应”流程，在等待外部工具返回结果期间模型处于空闲状态，导致端到端推理延迟严重。
2. **Agent Ability Challenge（智能体能力挑战）**：现有的 **dLLMs**（diffusion large language models）虽然具备并行解码优势，但在多步推理、工具调用格式遵循等方面表现较弱，难以胜任 Search Agent 的角色。

### **提出了什么新方法或新思路**

为解决上述问题，作者提出 **DLLM-Searcher**，一个专为 dLLMs 设计的 Search Agent 优化框架，包含两个核心组件：

#### （1）**两阶段后训练管道（Two-stage Post-training Pipeline）**
- **Agentic SFT**：基于高质量教师模型生成的轨迹进行监督微调，使 dLLM 学会遵循 `think` 和 `tool_call` 的严格格式，并掌握信息检索与推理协同的能力。
- **Agentic VRPO**：基于 SFT 模型的 rollout 结果构建胜者/败者对，采用方差缩减偏好优化进一步提升模型的推理与检索能力。

> 特别设计了 **Agentic Noising** 和 **Agentic ELBO**，确保训练过程中仅关注 `think` 和 `tool_call` 区域，避免 `tool_response` 泄露造成训练-推理不一致。

#### （2）**新型代理范式：Parallel-Reasoning and Acting (P-ReAct)**
- 利用 dLLMs 的非自回归、块内双向注意力特性，提出一种无需额外训练的推理策略：
  - **Token Pre-filling**：在生成块中预填充 `<tool_call>` 和 `</tool_call>` 边界标记。
  - **Confidence Biasing**：在解码时对边界内的 token 增加置信度偏置，引导模型优先解码 `tool_call` 内容。
- 实现“**先发出工具请求，再完成思考过程**”，从而在等待工具响应的同时继续生成推理内容，实现“thinking while waiting”。

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | P-ReAct 实现约 **15% 的端到端推理加速**，显著缓解 Latency Challenge |
| **性能** | 性能媲美主流基于 ARMs 的 Search Agents（如 R1Searcher），甚至在部分任务上超越 |
| **可控性** | P-ReAct 实现近乎 **100% 成功率的 tool_call 优先解码**，控制性强 |
| **通用性** | 方法适用于任意支持块扩散机制的 dLLM（如 SDAR），无需修改模型结构 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

四个多跳问答（multi-hop QA）基准用于评估信息检索与推理能力：

- **HotpotQA**：多跳事实推理，强调解释性和可追溯性
- **2WikiMultiHopQA**：基于维基百科的复杂推理
- **Musique**：通过单跳问题组合构造的多跳 QA
- **Bamboogle**：跨领域、出域（out-of-domain）测试集，检验泛化能力

> 所有测试集均从开发集中采样 500 例（Bamboogle 全部 125 例）

### **实验设置和评估指标**

#### **评估指标**
- **ACCR（Accuracy）**：预测答案是否包含正确答案（golden answer）
- **ACCL（LLM-as-Judge Accuracy）**：使用 Doubao-Seed-1.8 作为裁判模型判断回答正确性，更鲁棒

#### **训练数据构建**
- **Agentic SFT**：使用 Doubao-Seed-1.8 生成 3977 条高质量轨迹（含完整 `think` 和 `tool_call`）
- **Agentic VRPO**：基于 SFT 模型在 R1Searcher 的 Stage 2 数据上 rollout，筛选出 2237 个胜/负对

#### **模型与工具**
- **Backbone**：SDAR（Block Diffusion LM，参数量 8B，块大小 128）
- **检索工具**：Google Search API，返回 top-10 结果
- **硬件**：8 × NVIDIA H100 GPU

#### **解码配置**
- 128 步去噪，温度 1.0
- P-ReAct 中置信度偏置 α = 0.5

### **基线方法对比**

| 类别 | 基线模型 |
|------|---------|
| **传统 RAG** | SuRe, Selective-Context, Adaptive-RAG, IRCoT, Iter-RetGen, CR-Planner, ReARTeR |
| **ARM-based Search Agents** | Search-o1, Search-R1, WebSailor*, R1Searcher* |
| **dLLM-based Agents** | SDAR（原始）、Dream、LLaDA |

> *表示实验设置调整以保持公平比较

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| 模型 | HotpotQA (ACCR/ACCL) | 2Wiki (ACCR/ACCL) | Bamboogle (ACCR/ACCL) | Musique (ACCR/ACCL) | Avg ACCR/ACCL |
|------|------------------------|--------------------|------------------------|----------------------|----------------|
| **R1Searcher*** | 58.0 / 62.2 | 59.6 / 63.4 | 66.4 / 68.8 | 28.2 / 31.4 | **53.1 / 56.5** |
| **DLLM-Searcher** | **60.4 / 62.4** | **69.8 / 64.6** | **68.8 / 69.6** | **29.0 / 29.8** | **57.0 / 56.6** ✅ |

> ✅ **DLLM-Searcher 在所有指标上均达到或超过最强 ARM 基线（R1Searcher）**

### **与基线方法的对比结果**

- **vs. 传统 RAG**：平均 ACCR 提升超 **37 个百分点**（如 ReARTeR 为 45.4，DLLM-Searcher 为 57.0）
- **vs. 原始 dLLMs**：
  - SDAR 原始模型因格式错误无法运行（全部 parsing fail）
  - Dream 和 LLaDA 表现极差（ACCR ~10–35），验证了 **Agent Ability Challenge** 的存在
- **vs. ARM Agents**：性能持平甚至略优，证明 dLLMs 经过适配后可成为强大 Agent Backbone

### **消融实验结果**

#### **RQ1: 后训练有效性（Table 2）**

| 阶段 | HotpotQA ACCR | 2Wiki ACCR | Bamboogle ACCR | Musique ACCR |
|------|---------------|------------|----------------|--------------|
| Agentic SFT | 57.2 | 66.4 | 64.6 | 24.4 |
| **+ Agentic VRPO** | **60.4 (+3.2)** | **69.8 (+3.4)** | **68.8 (+4.2)** | **29.0 (+4.6)** |

> VRPO 带来稳定且显著的性能增益（+3~5%），说明偏好优化有效提升了推理质量。

#### **RQ2: P-ReAct 推理效率（Figure 3）**

| 数据集 | 推理时间降低 |
|-------|-------------|
| HotpotQA | 14.77% |
| 2Wiki | 21.00% |
| Bamboogle | 22.08% |
| Musique | 12.67% |
| **平均** | **≈15%** |

> 在几乎无性能损失的情况下实现显著加速，验证了 P-ReAct 对 Latency Challenge 的缓解效果。

#### **RQ3: dLLMs 的顺序自由生成优势（Figure 4）**

- 将 P-ReAct 应用于 **Qwen3 系列 ARMs**（8B, 30B, 235B）：
  - 即便强制 prompt 引导先生成 `tool_call`，仍导致 **性能下降 2.5%~13.3%**
- 反观 **DLLM-Searcher + P-ReAct**：
  - 在 HotpotQA 和 Musique 上 **准确率反而略有上升**
  - 最大降幅仅 4.5%，远小于 ARMs

> 说明 **dLLMs 能利用双向上下文“隐式推理”生成高质量 tool_call**，而 ARMs 依赖显式 CoT，一旦打乱顺序即崩溃。

---

## 4. 关键结论和发现

### **主要发现**

1. **dLLMs 完全可以胜任 Search Agent 角色**：通过针对性后训练（Agentic SFT + VRPO），可克服其原本在推理与格式遵循上的短板。
2. **P-ReAct 是 dLLMs 独有的高效范式**：利用其非自回归与块内双向注意力，实现“先行动后思考”的并行执行，带来 **~15% 推理加速**。
3. **性能与效率兼得**：DLLM-Searcher 不仅性能媲美最先进的 ARM-based Agents，还在推理效率上具有天然优势。
4. **顺序自由是 dLLMs 的核心优势**：即使 `tool_call` 先于 `think` 解码，也能保持高质量输出，这是 ARMs 无法做到的。

### **方法的局限性**

- **依赖高质量训练数据**：Agentic SFT 和 VRPO 依赖教师模型或 rollout 生成的高质量轨迹，若数据噪声大则效果受限。
- **仅适用于支持块扩散的 dLLM**：如 SDAR、Mercury 等；纯自回归或全序列扩散模型不适用 P-ReAct。
- **未探索更复杂的工具交互**：目前仅支持单一搜索工具，未来可扩展至 visit、code interpreter 等多工具场景。

### **未来工作方向**

- 将 P-ReAct 扩展至 **多工具调度与并发执行**
- 探索 **完全端到端训练 P-ReAct 控制机制**（而非仅推理期干预）
- 构建更大规模的 **dLLM-native Agent 训练数据集**
- 探索 dLLMs 在其他类型 Agent（如规划、游戏、代码执行）中的应用潜力

---

> **总结一句话**：  
> **DLLM-Searcher 成功将 dLLMs 从“低效但并行”的生成模型，转变为“高效且智能”的 Search Agent，首次实现了推理性能与端到端延迟的双重突破。**

</details>

---

### 5. [Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System](https://arxiv.org/abs/2602.08335)

**Authors**: Yanming Li, Xuelin Zhang, WenJie Lu, Ziye Tang, Maodong Wu, Haotian Luo, Tongtong Wu, Zijie Peng, Hongze Mi, Yibo Feng, Naiqiang Tan, Chao Huang, Hong Chen, Li Shen  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.08335v1  

#### Abstract
Integrating Large Language Models (LLMs) with external tools via multi-agent systems offers a promising new paradigm for decomposing and solving complex problems. However, training these systems remains notoriously difficult due to the credit assignment challenge, as it is often unclear which specif...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在基于 **Large Language Models (LLMs)** 和外部工具集成的 **multi-agent system (MAS)** 中，尽管其在复杂任务分解与执行中展现出潜力，但训练过程面临严重的 **credit assignment problem**（信用分配问题）：  
当一个决策轨迹成功或失败时，难以准确判断是 **planner agent**（负责任务分解）还是某个 **worker agent**（负责子任务执行）应得奖励或承担惩罚。现有方法通常依赖稀疏的、全局广播的奖励信号（broadcast rewards），无法捕捉个体代理的真实边际贡献，导致策略更新低效甚至误导。

### 提出的新方法：SHARP
本文提出 **SHARP (Shapley-based Hierarchical Attribution for Reinforcement Policy)**，一种基于 **Shapley Value** 的精细化信用分配框架，用于优化多智能体强化学习（MARL）。其核心思想是通过博弈论中的 **Shapley Value** 数学机制，量化每个 agent 对最终任务成功的因果贡献。

#### 创新点：
- ✅ **三重分解奖励机制 (tripartite decomposed reward)**：
  1. **Global Broadcast-Accuracy Reward**：终端准确性奖励，确保整体目标对齐；
  2. **Shapley-based Marginal-Credit Reward**：基于反事实掩码（counterfactual masking）计算每个 agent 的边际贡献，实现精确信用归属；
  3. **Tool-Process Reward**：过程级反馈，评估工具调用的有效性和正确性，提升执行效率。
- ✅ **归一化优势机制**：通过 group-relative policy gradient 对不同 agent 的优势进行标准化，稳定训练过程。
- ✅ **参数共享的 self-play 架构**：所有角色（planner/worker）由同一个 LLM 实例化，仅通过不同的 system prompt 区分功能，实现端到端联合优化。

### 相比现有方法的优势
| 维度 | 现有方法（如 MATPO, GRPO） | SHARP |
|------|-----------------------------|-------|
| 奖励粒度 | 全局广播，粗粒度 | 细粒度，agent-specific |
| 信用归属 | 模糊，混淆个体贡献 | 精确，基于因果推理 |
| 训练稳定性 | 易受噪声影响，波动大 | 更低方差梯度，收敛更稳 |
| 执行效率 | 可能冗余调用工具 | 抑制低效行为，减少 token 消耗 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖五个真实世界基准，涵盖多跳推理、网页导航、数学推理等复杂场景：
- **MuSiQue**：构造型多跳问答，需真正链式推理。
- **GAIA-text**：综合 AI 助手评测，涉及搜索、工具使用与逻辑推理。
- **WebWalkerQA**：结构化网页遍历与证据聚合。
- **FRAMES**：端到端检索增强生成（RAG）统一评测平台。
- **DocMath-Eval**：长文本文档中的数值与数学推理。

> ⚠️ 注意：除 MuSiQue 外，其余测试集均采用 zero-shot 设置，不参与训练。

### 实验设置与评估指标
- **模型骨干**：以 **Qwen3-8B** 为主干模型，部分实验对比 LLaMA-3.1-8B。
- **训练配置**：
  - 使用 AdamW 优化器，学习率 $10^{-5}$，batch size 256。
  - 每个查询采样 8 条轨迹（rollouts），共训练 180 步。
  - 使用 64×A100 GPU 集群并行训练。
- **评估指标**：
  - 主要指标为 **match accuracy**（答案匹配率）。
  - 辅助分析包括 MMLU（检测灾难性遗忘）、token 消耗、协调结构变化等。

### 基线方法对比
分为四类：
1. **Vanilla LLMs**：Qwen3-8B RAG、LLaMA-3.1-8B RAG
2. **Prompt-based 方法**：Plan-Search
3. **单智能体 RL 方法**：Single-agent GRPO
4. **多智能体系统（无细粒度信用建模）**：
   - 结构化未训练：Planner-Worker
   - 图结构方法：G-Designer、CARD
   - 强化学习方法：AceSearcher、COA、MATPO

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | MuSiQue | GAIA-text | WebWalkerQA | FRAMES | **AVG** |
|------|--------|----------|-------------|--------|--------|
| Single-agent GRPO | 45.93 | 27.97 | 7.47 | 30.20 | 27.89 |
| MATPO (SOTA MARL) | 47.00 | 31.65 | 7.47 | 37.10 | **30.81** |
| **SHARP (ours)** | **50.76** | **33.70** | **8.50** | **37.29** | **32.56** |

✅ **SHARP 在所有任务上均排名第一**，平均性能超越最强 baseline **MATPO 1.75 分**。

#### 性能增益统计：
- 相比 **single-agent 方法**：平均提升 **23.66%**
- 相比 **multi-agent 方法**：平均提升 **14.05%**

---

### 消融实验结果（Ablation Studies）

#### （1）是否启用 Shapley 信用机制？
- **移除 planner-level 信用** → 准确率下降约 0.6–0.65
- **移除 worker-level 信用** → 下降更大，达 **0.88–1.00**
- **完全移除 Shapley 机制** → 性能回落至 47.00（接近 MATPO）
> 🔍 表明：worker 的执行质量控制对长程任务更重要，而 planner 的分解策略也显著影响最终表现。

#### （2）模型规模扩展性（Scaling Laws）
在 MuSiQue 上测试不同 backbone 规模：
| 模型大小 | 单智能体 baseline | SHARP |
|---------|------------------|--------|
| 0.6B | 3.85 | 6.29 |
| 8B | 36.35 | **50.76** |

➡️ SHARP 的相对优势随模型增大而扩大，在 8B 模型上领先 **14.41 分**，验证其更强的可扩展性。

#### （3）训练稳定性分析
在 GAIA-text 上观察训练曲线（图5）：
- **Single-agent GRPO / MATPO**：出现明显波动甚至性能回退
- **SHARP**：呈现单调上升趋势，从 27.53 → 33.70，无震荡

> 📈 表明 SHARP 能有效降低 reward variance，提升训练鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Marginal Credit Modeling 是关键突破点**：相比架构设计或优化算法本身，显式建模 agent 的边际贡献更能带来持续性能提升。
2. ✅ **协同信用分配产生协同效应**：planner 与 worker 同时获得精准反馈时，整体性能远超单独优化任一角色之和。
3. ✅ **提升 planner-worker 协调质量**：
   - planner score（平均 Shapley 值）从 0.4542（baseline）→ 0.5084（SHARP）
   - 有用 subagent 比例从 11.03% → 12.96%
   - 有害 subagent 调用比例从 **5.48% ↓ 至 4.40%**
4. ✅ **训练换推理效率**：虽然训练成本增加（因 Shapley 计算），但部署时推理 token 消耗从 ~9.1k ↓ 至 ~8.3k，且路径更简洁高效。
5. ✅ **缓解灾难性遗忘**：在 MMLU 测试中，SHARP 不仅未丢失通用知识，反而略有提升（94.65% > 原始 93.04%），说明其更新更具选择性。

### 方法的局限性
- ❗ **计算开销较高**：Shapley 值估算需多次反事实轨迹模拟，尤其在 agent 数量多时成本显著上升（可通过 sparsification 缓解）。
- ❗ **依赖高质量环境模拟**：counterfactual evaluation 要求能准确“屏蔽”某 agent 并保持其他交互不变，现实中可能难以完美实现。
- ❗ **当前仍限于 role-specialized MAS**：尚未推广至完全去中心化的 agent 社会。

### 未来工作方向
- 🔮 探索更高效的 Shapley 近似算法（如 Monte Carlo sampling 或 learned surrogates）
- 🔮 将 SHARP 扩展至动态拓扑结构（dynamic topology）或多模态 agent 系统
- 🔮 研究如何自动识别并剪枝低效执行路径（pruning low-utility execution paths）
- 🔮 构建可解释的 agent coordination dashboard，利用 Shapley 信用进行可视化诊断

---

> 💡 **一句话总结**：  
> **SHARP 通过引入博弈论驱动的 Shapley Credit 机制，解决了 multi-agent LLM 系统中长期存在的 credit assignment 瓶颈，实现了更精准、稳定、高效的联合训练，在多个现实基准上显著超越 SOTA 方法。**

</details>

---

### 6. [Efficient Remote Prefix Fetching with GPU-native Media ASICs](https://arxiv.org/abs/2602.09725)

**Authors**: Liang Mi, Weijun Wang, Jinghan Chen, Ting Cao, Haipeng Dai, Yunxin Liu  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.09725v1  

#### Abstract
Remote KV cache reuse fetches KV cache for identical contexts from remote storage, avoiding recomputation, accelerating LLM inference. While it excels in high-speed networks, its performance degrades significantly in bandwidth-limited scenarios. Recent studies address this by transmitting KV caches ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Remote Prefix Fetching with GPU-native Media ASICs

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大语言模型（LLM）推理中，**远程 KV cache 复用**（remote KV cache reuse）是一种通过从远程存储获取已计算的 KV 缓存来避免重复计算、加速推理的技术。然而，在带宽受限的网络环境下，该技术面临以下瓶颈：
- **传输延迟高**：原始 KV cache 数据量巨大，直接传输效率低。
- **压缩解压代价高**：虽然已有研究尝试对 KV cache 进行压缩以减少传输开销，但解压过程通常依赖 CUDA 核心，与 LLM 推理争抢 GPU 资源，导致严重干扰（interference）。
- **硬件成本高**：部分方案将解压卸载到 SmartNIC，虽可避免干扰，但需要额外昂贵硬件（如 NVIDIA BlueField），难以广泛部署。

### 提出的新方法与思路
本文提出 **KVFetcher**，首次利用现代 GPU 上闲置的 **GPU-native 视频编解码 ASIC**（如 NVIDIA NVENC/NVDEC）来实现高效、低成本的远程 KV cache 复用。其核心思想是将 KV cache 张量编码为视频格式进行压缩传输，并在 GPU 内部专用硬件上完成解码，从而绕过通用计算单元。

#### 主要创新点：
1. **Codec-friendly Tensor Layout**  
   设计了一种面向视频编码优化的张量布局策略，跳过有损的 DCT 和量化步骤，充分利用 H.265 的无损帧内/帧间预测能力，实现了高达 **11.9× 的压缩比**，且保持生成精度无损。

2. **Efficient KV Fetcher**  
   构建了一个高效的远程 KV 获取流水线，包含三个关键技术：
   - **Fetching-aware Scheduler**：区分是否需要 KV 复用的请求，将 KV 获取置于后台异步执行，避免阻塞非复用请求。
   - **Adaptive-resolution KV Fetching**：动态调整视频分辨率以适应网络波动，最小化传输与解码之间的流水线气泡（pipeline bubble）。
   - **Frame-wise Tensor Restoration**：逐帧恢复 KV tensor，显著降低内存峰值占用（<70MB），避免与推理引擎争抢显存。

### 相比现有方法的优势
| 方案 | 干扰情况 | 成本 | 压缩比 | 准确性 |
|------|--------|------|-------|--------|
| CacheGen [46] | 高（CUDA 解压） | 低 | 中等 | 有损风险 |
| ShadowServe [68] | 无（SmartNIC） | 极高 | 中等 | 有损风险 |
| **KVFetcher (Ours)** | **无（ASIC 解码）** | **零新增成本** | **高（达11.9×）** | **无损** |

KVFetcher 在不增加硬件成本的前提下，实现了**无干扰、高压缩比、高准确率**的远程 KV 复用系统。

---

## 2. 核心实验方法和设置

### 使用的数据集
论文在多个长上下文基准测试集上进行了评估，涵盖多种任务类型：
- **L-Eval**：包含 508 个长文档问答任务，上下文长度 3K–200K tokens。
- **LV-Eval**：更具挑战性的单跳/多跳问答，含干扰项、事实混淆等，长度 16K–256K。
- **LongBench-V2**：系统性多任务评测，包括多文档 QA、长对话、代码生成等，长度 13K–167K。

### 实验设置
- **模型**：LWM-7B（1M context）、Yi-34B（200K context）、Llama3-70B（128K context）
- **硬件平台**：三种不同档次的 GPU 集群
  - **高端**：NVIDIA A100 (80GB)，每卡 5 个 NVDEC
  - **中端**：NVIDIA H20 (96GB)，每卡 7 个 NVDEC
  - **低端**：NVIDIA L20 (48GB)，每卡 3 个 NVDEC
- **网络带宽**：模拟 1–40 Gbps 的常规云服务带宽环境（非 RDMA）

### 评估指标
- **TTFT**（Time to First Token）：首 token 延迟，衡量预填充阶段效率。
- **TPOT**（Time Per Output Token）：每个输出 token 的平均延迟。
- **Compression Ratio**：KV cache 压缩前后大小之比。
- **Accuracy**：各数据集标准指标（Exact Match, F1 Score 等）。

### 基线方法对比
- **Full Prefill**：无 KV 复用，完整重新计算。
- **Raw KV Reuse**：直接传输未压缩 KV cache。
- **Compressed KV Reuse**：
  - **CacheGen**：基于 CUDA 的算术编码压缩。
  - **ShadowServe**：SmartNIC 卸载解压。
  - **llm.265**：使用视频编码但设计不当的基线。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **最高 TTFT 加速比达 3.51×**（相比 CacheGen），在多数场景下实现 **1.52–3.51× 的 TTFT 降低**。
- **压缩比提升显著**：
  - 相比 CacheGen 提升 **2.17×**
  - 相比 ShadowServe 提升 **1.93×**
  - 相比 llm.265 提升 **1.41×**
- **精度完全保留**：所有任务上均实现 **lossless accuracy**，而 llm.265 存在明显精度下降。
- **非复用请求受益明显**：
  - TTFT 降低 **77%**
  - TPOT 降低 **35.4%**

### 与基线方法的对比结果
| 对比项 | KVFetcher vs. CacheGen | KVFetcher vs. ShadowServe |
|--------|------------------------|----------------------------|
| TTFT | ↓ 1.52–3.51× | 显著更优（无需额外硬件） |
| 干扰性 | ✅ 无干扰（ASIC 解码） | ❌ 高成本 |
| 部署成本 | ✅ 零新增成本 | ❌ >$3000/NIC |
| 准确性 | ✅ 无损 | ⚠️ 有损风险 |

> 图 21 显示，在 1–40 Gbps 带宽范围内，KVFetcher 在几乎所有配置下都优于 CacheGen。

### 消融实验结果
- **Codec-friendly Tensor Layout 贡献最大**：
  - 仅量化 → 压缩比 ~5.8×
  - + Inter-frame layout → ~8.7×
  - + Intra-frame layout → **~11.9×**
- **Adaptive-resolution 提升 20% 性能**：相比固定分辨率（如 1080p），自适应机制有效应对网络抖动，减少流水线等待时间。
- **Frame-wise Restoration 显存开销极低**：
  - 峰值显存占用仅 **~400MB**（并发处理 7 个视频块）
  - 解压缓冲区小于 **70MB**，远低于 CacheGen 的 2.7× 原始 KV 大小。

---

## 4. 关键结论和发现

### 主要发现
1. **GPU-native 视频编解码器是被忽视的强大资源**：在当前 LLM 推理过程中完全空闲，可用于 KV cache 压缩/解压，实现真正的 **zero-cost acceleration**。
2. **合理的张量布局至关重要**：盲目将 KV tensor 映射为视频帧会导致压缩效果差且精度损失；必须结合 LLM 架构特性（如 token 维度相似性高）设计 codec-friendly layout。
3. **系统级协同设计决定最终性能**：仅靠高压缩比不足以保证低 TTFT，还需调度、流水线、内存管理等多方面优化才能充分发挥优势。
4. **KVFetcher 可扩展性强**：适用于从低端到高端各类 GPU，尤其适合主流云服务商提供的中低带宽部署环境。

### 方法的局限性
1. **NVENC 资源有限**：目前 GPU 上的 NVENC 数量较少（如 L20 仅有 3 个），可能成为并发压缩时的瓶颈，限制在线压缩能力。
2. **离线压缩为主**：当前设计主要用于**离线压缩 + 在线解压**，尚不支持实时动态压缩（online compression），在 P-D disaggregation 场景中应用受限。
3. **预分配内存机制可能阻塞其他请求**：为保证复用请求及时响应，需预先分配 GPU 显存，可能导致非复用请求因内存不足被延迟。

### 未来工作方向
- 支持 **runtime online KV compression**，满足故障迁移、节点间通信等需求。
- 开发更智能的 **显存交换机制**（swap-in/out），允许将 fetched KV cache 存储于主机内存或 SSD，按需加载至 GPU，缓解内存压力。
- 推动下一代 GPU 增加 **NVENC/NVDEC 数量**，释放更多专用硬件潜力。
- 将类似思想拓展至其他模态（如 Vision-Language Models）中的中间状态复用。

---

> **总结一句话**：KVFetcher 通过巧妙利用 GPU 上闲置的视频编解码 ASIC，提出了一套高效、无干扰、低成本的远程 KV cache 复用方案，在保持生成精度的同时，实现了高达 3.51× 的 TTFT 加速，为大规模 LLM 服务提供了极具实用价值的新路径。

</details>

---

### 7. [Effective MoE-based LLM Compression by Exploiting Heterogeneous Inter-Group Experts Routing Frequency and Information Density](https://arxiv.org/abs/2602.09316)

**Authors**: Zhendong Mi, Yixiao Chen, Pu Zhao, Xiaodong Yu, Hao Wang, Yanzhi Wang, Shaoyi Huang  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.09316v1  

#### Abstract
Mixture-of-Experts (MoE) based Large Language Models (LLMs) have achieved superior performance, yet the massive memory overhead caused by storing multiple expert networks severely hinders their practical deployment. Singular Value Decomposition (SVD)-based compression has emerged as a promising post...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Effective MoE-based LLM Compression by Exploiting Heterogeneous Inter-Group Experts Routing Frequency and Information Density

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
Mixture-of-Experts (MoE) 结构的大型语言模型（LLM）虽然在性能上表现出色，但由于其包含大量独立的专家网络，导致**内存开销巨大**，严重阻碍了实际部署。

现有的基于 SVD 的压缩方法存在两大缺陷：
1. **采用统一的秩分配（uniform rank allocation）**，忽略了不同专家之间显著的利用率差异（如某些专家频繁被路由，而另一些几乎未被激活）。
2. **直接丢弃低秩近似后的残差项（residual）**，假设其影响可忽略，但实际上这些残差可能包含对特定任务至关重要的信息。

### 🚀 提出的新方法：RFID-MoE
本文提出 **RFID-MoE**（Routing Frequency and Information Density-aware MoE compression），一种面向 MoE 模型的有效压缩框架，核心创新如下：

#### （1）自适应秩分配策略（Adaptive Rank Allocation）
- 引入一个**融合度量指标**，结合两个关键因素：
  - **Routing Frequency（路由频率）**：反映专家在推理过程中的动态激活频率。
  - **Information Density（信息密度）**：通过 **Effective Rank** 衡量每个专家组的信息丰富程度。
- 利用该融合指标，在固定压缩预算下，为“重要”专家组（高频且高信息密度）分配更高的分解秩，从而更有效地保留关键能力。

> 🔍 关键洞察：低频激活的专家不一定不重要——有些虽少用但信息密集，若简单按频率降秩会退化为专家剪枝（pruning），损害模型泛化能力。

#### （2）参数高效的残差重建机制（Parameter-Efficient Residual Reconstruction）
- 不再丢弃 SVD 压缩后产生的残差 $ R = W - \tilde{W} $。
- 设计了一种基于 **稀疏正交投影矩阵 $ P $** 的轻量级重建方法：
  - 将残差映射到一个低维可训练向量 $ \mathbf{n} \in \mathbb{R}^a $（$ a \ll D $）。
  - 使用共享的稀疏投影矩阵 $ P $ 将其重构回原始权重空间：$ \text{vec}(R) = P\mathbf{n} $。
- 投影矩阵 $ P $ 被所有专家组共享，并具有严格等距性（isometry），保证优化稳定性。

> 💡 优势：仅需约 **3% 的额外参数**即可恢复大量因低秩逼近丢失的关键信息。

### ⭐ 相比现有方法的优势
| 方面 | 现有方法（如 MoBE, D²-MoE） | RFID-MoE |
|------|-------------------------------|----------|
| 秩分配 | 统一或基于静态权重属性 | 动态融合路由频率 + 信息密度 |
| 残差处理 | 完全丢弃 | 参数高效地重建 |
| 性能保持 | 在高压缩比下显著下降 | 显著优于基线，尤其在零样本任务 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

#### 语言建模评估（Perplexity ↓）
- **WikiText-2**
- **PTB**（Penn Treebank）
- **C4**

#### 推理与常识理解评估（Zero-shot Accuracy ↑）
- **OpenBookQA**
- **ARC-e**
- **WinoGrande**
- **HellaSwag**
- **PIQA**
- **MathQA**

> 所有评估均使用 [LM Evaluation Harness](https://github.com/EleutherAI/lm-evaluation-harness) 进行零样本测试。

### ⚙️ 实验设置
- **模型**：
  - Qwen3-30B-A3B-2507（每层128专家）
  - DeepSeekMoE-16B-Base（64专家）
  - Qwen2-57B-A14B
  - Deepseek-v2-Lite-Chat
  - Ling-mini-2.0
- **压缩比例**：20%, 40%, 60%
- **校准数据集**（用于统计路由频率）：
  - 主要使用 **WikiText-2**（1024样本）
  - 消融实验中也尝试 **C4**
- **实现细节**：
  - 对 Up 和 Gate 投影矩阵进行 SVD 分解，Down 矩阵保持稠密。
  - 每4个专家组成一组进行 basis sharing。
  - 残差向量维度设为原参数量的 **3%**。
  - 使用 Adam 优化器进行微调，无需完整训练数据。

### 🆚 基线方法对比
| 方法 | 类型 | 简介 |
|------|------|------|
| **NAEE** | Pruning + Skipping | 后训练专家剪枝 + 动态跳过 |
| **MoE-I2** | Pruning + Low-rank | 专家间剪枝 + 专家内低秩分解 |
| **RS-MoE** | Shared + Sparse | 共享低秩表示 + 稀疏残差 |
| **D²-MoE** | Shared Components | 权重拆分为共享与独有部分 |
| **MoBE** | Basis Sharing | 自适应 basis sharing，当前最优之一 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（以 Qwen3-30B-A3B-2507 为例）

#### 在 PTB 上的 Perplexity（越低越好）
| 压缩率 | 方法 | PPL |
|--------|------|-----|
| 60% | MoBE | 24.93 |
| 60% | D²-MoE | 38.84 |
| 60% | **RFID-MoE (Ours)** | **16.92** |

👉 **相比最佳基线 MoBE 下降超过 8.0 PPL！**

#### 零样本准确率提升（60% 压缩率）
| 任务 | MoBE | RS-MoE | **RFID-MoE** |
|------|-------|--------|-------------|
| HellaSwag | ~0.58 | ~0.63 | **0.66** (+8%) |
| WinoGrande | ~0.67 | ~0.71 | **0.71** (+4%) |
| PIQA | ~0.54 | ~0.71 | **0.71** |
| 平均准确率 | — | — | **领先明显** |

> ✅ 在多个 MoE 架构上均取得 SOTA 表现，即使在轻量级模型（如 Ling-mini-2.0）上依然有效。

### 🔬 消融实验结果

#### （1）不同校准数据的影响（Table 3）
| 方法 | 数据集 | WikiText-2 | PTB | C4 |
|------|--------|------------|-----|----|
| MoBE | — | 13.96 | 24.93 | 24.40 |
| **RFID-MoE** | WikiText-2 | **9.57** | **16.92** | **17.96** |
| **RFID-MoE** | C4 | 10.51 | 13.70 | 16.85 |

👉 使用 C4 作为校准数据时，在 C4 测试集上达到最低 PPL（16.85），表明校准数据选择合理可进一步提升效果。

#### （2）融合超参数 $ \epsilon $ 的影响（Table 4）
- 最优值集中在 **$ \epsilon = 0.7 \sim 0.8 $**。
- 若 $ \epsilon $ 过大 → 忽视路由频率 → 无法保护高频专家；
- 若 $ \epsilon $ 过小 → 忽视信息密度 → 低估低频但关键专家。

#### （3）残差重建有效性分析（Appendix C）
- 理论证明：只要残差子空间的内在维度 $ d_{\text{int}} $ 较小，则低维向量 $ \mathbf{n} $ 能以高概率找到 $ \epsilon $-近似解。
- 实践验证：仅增加 3% 参数即可显著改善性能，说明残差确实存在于低维流形中。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **专家异质性必须被显式建模**：MoE 中专家的路由频率和信息密度高度非均匀，统一压缩策略是次优的。
2. **Effective Rank 是识别“沉默但重要”专家的关键指标**：一些极少被激活的专家仍具有很高的信息密度，应给予足够压缩资源。
3. **残差不可忽略且可高效重建**：SVD 截断后的残差含有非平凡能量，但其分布在低维子空间中，可通过稀疏正交投影有效恢复。
4. **RFID-MoE 实现了高压缩比下的性能飞跃**：在 60% 压缩率下，PPL 下降超 8，零样本准确率提升达 8%，远超现有方法。

### ⚠️ 局限性
- 当前方法主要针对 FFN 层中的 MoE 专家进行压缩，未涉及注意力模块或其他结构。
- 依赖于在校准数据上收集的路由频率，若分布偏移可能导致性能波动。
- 稀疏投影矩阵 $ P $ 虽然节省存储，但仍需设计更灵活的结构探索。

### 🔮 未来工作方向
- 将 RFID 思想扩展至其他 MoE 变体（如 SeqMoE、Hierarchical MoE）。
- 探索在线自适应秩分配机制，应对输入分布变化。
- 结合量化（Quantization）与 RFID-MoE，实现更极致的模型压缩。
- 研究如何将残差重建机制应用于 LoRA、Adapter 等参数高效微调方法中。

---

> 📌 **一句话总结**：  
> RFID-MoE 通过联合利用 **Routing Frequency** 与 **Information Density** 实现了 MoE 模型的自适应低秩压缩，并首次系统性地回收了传统方法丢弃的残差信息，在极高压缩比下实现了语言建模与推理能力的双重突破。

</details>

---

### 8. [Scalable and Reliable State-Aware Inference of High-Impact N-k Contingencies](https://arxiv.org/abs/2602.09461)

**Authors**: Lihao Mai, Chenhan Xiao, Yang Weng  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.09461v1  

#### Abstract
Increasing penetration of inverter-based resources, flexible loads, and rapidly changing operating conditions make higher-order $N\!-\!k$ contingency assessment increasingly important but computationally prohibitive. Exhaustive evaluation of all outage combinations using AC power-flow or ACOPF is in...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Scalable and Reliable State-Aware Inference of High-Impact N-k Contingencies*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
随着 **inverter-based resources** 和 **flexible loads** 的广泛接入，电网运行状态快速变化，传统的 **N-1 安全准则** 已不足以保障系统安全。高阶 **N-k contingency analysis** 变得愈发重要，但由于其组合爆炸特性（$\binom{N}{k}$），对所有可能的断线组合进行 **AC power-flow** 或 **ACOPF** 验证在计算上不可行。

现有方法如 **heuristic screening**（如 LODF）或 **surrogate models** 虽然加速了分析，但仍需遍历大量候选场景，且无法提供对严重故障的**覆盖保证**（coverage guarantee），存在漏检高风险事件的风险。

### 提出了什么新方法或新思路
本文提出了一种**可扩展、状态感知的生成式框架**，直接生成高影响的 N-k 故障场景，避免枚举整个组合空间。核心创新包括：

- **将 N-k 分析重构为“状态条件下的生成问题”**：  
  不再是“评估并排序所有组合”，而是“直接生成最可能导致严重后果的少数几个组合”。

- **基于条件扩散模型（conditional diffusion model）的生成机制**：  
  利用 **diffusion process** 在给定当前系统状态 $x$ 下，从高严重性尾部分布 $p^*(c \mid S(x,c) \geq T, x)$ 中采样出高风险的 N-k 故障 $c$。

- **拓扑感知的图神经网络（EVGNN）用于离线风险评分**：  
  设计了一个仅需训练于 **base-case** 和 **N-1** 数据的 **Edge-Varying Graph Neural Network (EVGNN)**，用于快速估计多线路故障的严重性，从而构建高质量的高风险训练集供扩散模型学习。

- **可控的覆盖保证机制**：  
  提供理论支持，允许操作员设定一个“漏检容忍度”（miss tolerance $\delta_{\text{miss}}$），并据此确定所需的 **AC power-flow** 验证数量 $B$，实现**风险与计算成本之间的显式权衡**。

### 相比现有方法的优势
| 维度 | 现有方法 | 本文方法 |
|------|--------|--------|
| **计算效率** | 仍需评估/评分大量组合，复杂度为 $O(\binom{N}{k})$ | 仅生成 $m$ 个高风险样本，复杂度为 $O(m)$，与 $\binom{N}{k}$ 无关 |
| **覆盖可靠性** | 启发式筛选无理论保证，易漏检严重事件 | 提供概率性覆盖保证，可控制漏检风险 |
| **状态依赖性** | 多数静态排序不适应动态运行状态 | 生成过程以当前状态 $x$ 为条件，动态调整输出 |
| **样本效率** | 需大量标注 N-k 数据（不现实） | 仅需 N-1 标注数据训练 EVGNN，扩散模型通过筛选后的高风险子集训练 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **标准 IEEE 测试系统**：
  - IEEE 14-bus
  - IEEE 39-bus
  - IEEE 57-bus
  - IEEE 118-bus
- **数据生成方式**：
  - 通过对负荷和发电机出力扰动生成多个运行状态 $x_t$
  - 对每个 $x_t$ 执行 **base-case AC power-flow** 和所有 **N-1 故障** 的 AC 求解，构建 $D_{N-1}$
  - 使用 EVGNN 对随机生成的 N-k 故障打分，保留高分者执行 AC 验证，形成高风险训练集 $H$

### 实验设置和评估指标
- **目标 k 值范围**：
  - IEEE 14: $k \in [2,4]$
  - IEEE 39: $k \in [2,6]$
  - IEEE 57: $k \in [2,5]$
  - IEEE 118: $k \in [2,15]$
- **评估指标**：
  1. **Top-m 平均严重性曲线**：前 $m$ 个推荐故障的平均 $S(x,c)$，衡量样本质量
  2. **ACPF 收敛率（Conv. Rate）**：生成故障中能成功求解 AC power-flow 的比例
  3. **In-band 率**：收敛故障中严重性高于阈值 $T$ 的比例
  4. **总运行时间对比**：不同方法在相同任务下的耗时
  5. **覆盖率保证验证**：是否能在小预算下捕获高严重性事件

### 基线方法对比
- **Uniform Random Sampling**：均匀随机采样 N-k 故障
- **EVGNN Alone**：仅使用 EVGNN 打分排序，取 top-m
- **Surrogate-based Screening**：使用代理模型替代 AC 求解进行筛选
- **cVAE / cGAN**：其他生成模型作为对比
- **Exhaustive AC Evaluation**：理想但不可行的基准

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ **表 I：IEEE 39-bus 系统总运行时间对比（分钟）**

| $k$ | Exhaustive | Surrogate | Ours | Exhaustive / Ours | Surrogate / Ours |
|-----|------------|-----------|------|-------------------|------------------|
| 1   | 0.0023     | 0.0006    | 0.001 | 2.3×              | 0.6×             |
| 2   | 0.0446     | 0.0130    | 0.002 | 22.3×             | 6.5×             |
| 3   | 0.5510     | 0.1604    | 0.004 | 138×              | 40.1×            |
| 4   | 4.9594     | 1.4438    | 0.006 | 827×              | 241×             |
| 5   | 34.716     | 10.107    | 0.007 | 4,960×            | 1,444×           |
| 6   | 196.72     | 57.274    | 0.009 | 21,858×           | 6,364×           |

> **结论**：本文方法在线计算时间几乎恒定，而传统方法随 $k$ 急剧增长。

#### ✅ **表 II：ACPF 收敛率与 In-band 率**

| System       | $k$ Range   | Conv. (%) | In-band (%) |
|--------------|-------------|-----------|-------------|
| IEEE 14      | [2,4]       | 85.0      | 84.2        |
| IEEE 39      | [2,6]       | 93.5      | 87.7        |
| IEEE 57      | [2,5]       | 86.5      | 96.5        |
| IEEE 118     | [2,15]      | 86.0      | 91.9        |
| **Overall**  | —           | **87.1**  | **90.1**    |

> **结论**：超过 87% 的生成故障可收敛，其中 90% 属于高严重性“in-band”区域，说明生成质量极高。

### 与基线方法的对比结果

- **Top-m 曲线显著优于所有基线**：
  - 在 IEEE 118-bus 上，当 $m=200$ 时，本文方法的平均 Top-m 严重性约为 **310**，而随机采样仅为 **130**
  - 曲线下降缓慢，表明新增样本仍保持高严重性，而随机方法迅速被低影响样本稀释

- **严重性分布更集中于高尾部**（见 Figure 3）：
  - 本文方法在“high-severity in-band”区域的样本密度远高于其他方法
  - cVAE 和 cGAN 表现不稳定，未能有效聚焦高风险区域

- **相比 EVGNN Alone 显著提升**：
  - 仅靠 EVGNN 打分存在探索不足的问题
  - 加入扩散生成后，能跳出局部高分区域，发现更多潜在严重组合

### 消融实验结果
虽然未明确列出“ablation study”章节，但从以下对比可视为隐式消融：

- **EVGNN vs. EVGNN + Diffusion**：后者在 Top-m 曲线和 in-band 率上全面胜出，说明**生成机制本身带来了额外的价值**，不仅仅是打分
- **cVAE/cGAN vs. Diffusion**：扩散模型在稳定性与聚焦能力上表现更好，说明**diffusion 更适合建模稀疏、长尾的严重故障分布**

---

## 4. 关键结论和发现

### 论文的主要发现
1. **N-k 分析无需穷举**：严重故障集中在极小的组合子空间内，可通过生成模型直接采样。
2. **状态感知生成是可行且高效的**：条件扩散模型能够根据当前运行状态 $x$ 动态生成高风险 N-k 故障。
3. **EVGNN 可有效泛化至 N-k 场景**：仅用 N-1 数据训练的图模型即可为多线路故障提供可靠的风险评分。
4. **提供可调控的风险-计算权衡机制**：通过设定漏检容忍度 $\delta_{\text{miss}}$，可自动确定所需验证数量 $B$，实现**操作层面的可靠性控制**。
5. **显著提升样本效率**：在相同 AC power-flow 预算下，本文方法能识别出更严重的故障，减少资源浪费。

### 方法的局限性
- **依赖高质量的 EVGNN 风险估计**：若 EVGNN 无法准确捕捉交互效应（interaction effects），可能导致高风险区域误判。
- **扩散模型训练需要一定量的高风险标签**：尽管已大幅减少，但仍需部分 N-k 的 AC 验证来构建训练集。
- **假设生成样本独立同分布**：实际中可能存在相关性，影响覆盖率估计。
- **未考虑动态稳定性问题**：目前严重性指标基于 **AC power-flow** 结果，未涵盖暂态稳定等动态响应。

### 未来工作方向
- 将框架扩展至 **dynamic security assessment**，结合 **time-domain simulation** 输出作为严重性标签。
- 探索 **online adaptation** 机制，使模型能随新观测数据持续更新。
- 引入 **multi-objective optimization**，同时考虑经济性与安全性。
- 应用于 **预防控制策略生成**，不仅识别故障，还建议调整措施。
- 在更大规模实际电网中部署验证，并集成到 EMS/DMS 系统中。

--- 

> **总结一句话**：  
> 本文提出了一种基于 **EVGNN + conditional diffusion** 的生成式 N-k 安全评估框架，在**极低计算预算下可靠地聚焦高风险故障**，实现了从“暴力筛查”到“智能推理”的范式转变。

</details>

---

### 9. [Learning to Discover Iterative Spectral Algorithms](https://arxiv.org/abs/2602.09530)

**Authors**: Zihang Liu, Oleg Balabanov, Yaoqing Yang, Michael W. Mahoney  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.09530v1  

#### Abstract
We introduce AutoSpec, a neural network framework for discovering iterative spectral algorithms for large-scale numerical linear algebra and numerical optimization. Our self-supervised models adapt to input operators using coarse spectral information (e.g., eigenvalue estimates and residual norms), ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learning to Discover Iterative Spectral Algorithms*

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决**数值线性代数（NLA）和数值优化领域中迭代谱算法的设计自动化问题**。传统上，这些算法（如Chebyshev加速、多项式预条件器）依赖于手工设计，需要深厚的数学专业知识，并且通常针对特定的谱结构进行优化。该论文提出，可以利用机器学习（ML）来自动发现适应性强、性能优越的新型迭代算法。

### 提出的新方法：AUToSPEC框架
作者提出了 **AUToSPEC**，一个基于神经网络的端到端可微分框架，用于发现迭代谱算法。

- **核心思想**：将算法发现过程建模为一个**从粗粒度谱探针（spectral probe）到迭代系数的映射**。
  - **输入**：一个谱探针 `(λ, r)`，包含少量近似的特征值估计 `λ` 和相应的残差范数 `r`。
  - **输出**：一个神经网络引擎（neural engine）预测一组递推系数，这些系数定义了一个作用于算子 `X` 的矩阵多项式 `P(X)`。
  - **执行**：该多项式通过一个短的、可执行的数值线性代数递推关系来实现，从而形成一个完整的、可部署的数值算法。

### 相比现有方法的优势
1.  **连续搜索空间**：与许多在离散程序空间中搜索的符号回归方法不同，AUToSPEC在**连续的参数空间**中操作，这更符合NLA中有效方法通常是连续对象的本质。
2.  **自监督学习**：训练是完全**自监督**的，仅通过下游任务的性能（如收敛速度、逼近误差）作为反馈信号，而不是模仿已有的算法。这使得模型能够超越现有基线。
3.  **强大的泛化能力**：模型在**小型合成算子**上进行训练，却能成功迁移到**大规模真实世界稀疏矩阵**（维度高达 `O(10^6)`）上，实现了“小模型大用”。
4.  **统一且通用的框架**：AUToSPEC提供了一个统一的参数化方案，可以应用于多种NLA任务，包括求解线性系统、计算特征值和逼近矩阵函数。
5.  **实用性**：推理时仅需廉价的粗粒度谱信息（通过短时间的warm-start求解器获得），使其在计算预算紧张的实际场景中具有很高的适用性。

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：使用**小型合成对角矩阵**（`n ≤ O(10^3)`）。这些矩阵的特征谱通过生成器创建，以模拟现实世界中常见的谱特性，例如：
  - 缓慢衰减的主导特征值（用于特征值问题）。
  - 跨越多个数量级的条件数（用于线性系统和矩阵函数逼近）。
  - 引入轻微的局部不规则性以反映非理想化算子。
- **测试数据**：来自 **SuiteSparse Matrix Collection** 的**真实世界大型稀疏矩阵**，涵盖电子结构、热有限元、结构力学和电路/模型降阶等领域。矩阵维度范围从 `468` 到 `1.6×10^6`。

### 实验设置和评估指标
- **推理流程**：
  1.  对新的算子 `X` 运行一个简短的warm-start求解器（如 `eigs` 或 `Lanczos`）以获取谱探针 `(λ, r)`。
  2.  将探针输入训练好的AUToSPEC神经网络引擎，预测递推系数。
  3.  执行由这些系数定义的迭代过程。
- **评估指标**：
  - **特征值问题**：达到目标精度所需的 `eigs` 外层迭代次数。
  - **线性系统**：共轭梯度法（CG）达到指定残差容限（如 `10^-10`）所需的迭代次数。
  - **矩阵函数逼近**：算子范数下的逼近误差，例如 `||P(X)X^(1/2) - I||`。

### 基线方法对比
- **特征值问题**：标准幂迭代/子空间迭代 (`P(X)=X^d`) 和 **Chebyshev滤波器**。
- **线性系统**：无预条件的CG、Richardson迭代和 **Chebyshev多项式预条件器**。
- **矩阵函数逼近**：截断的Neumann级数（即泰勒展开）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
在所有三项任务上，AUToSPEC发现的算法均显著优于基线方法：

1.  **特征值问题**：
    - 在 `Si02` 矩阵上，当 `k=10` 时，AUToSPEC仅需 **23** 次迭代即可达到精度，而标准幂迭代在500次后仍未收敛（`>500`）。
    - 在 `CO` 矩阵上，当 `k=20` 时，AUToSPEC需 **27** 次迭代，而基线方法需 **126** 次。
    - 与Chebyshev相比，AUToSPEC也表现出色，尤其在更具挑战性的谱上（如 `Si02` 和 `CO`），迭代次数减少了一半以上。

2.  **线性系统**：
    - 在 `thermal2` 矩阵上，AUToSPEC预条件的CG仅需 **705** 次迭代，而无预条件的CG在5000次后仍未收敛（`>5000`）。
    - 与Chebyshev预条件器相比，AUToSPEC在大多数情况下表现相当甚至更好，例如在 `G2_circuit` 上，AUToSPEC需70次，而Chebyshev需79次（使用相同质量的谱探针）。

3.  **矩阵函数逼近**：
    - 在 `E. coli` DNA相似性矩阵的协方差白化任务中，AUToSPEC学习的逼近算法在算子范数误差上比Neumann级数基线实现了**一个数量级（order of magnitude）的改进**。

### 消融实验结果
- **残差特征的重要性**：消融研究（Ablation Study）表明，将残差范数 `r` 作为输入至关重要。当移除残差特征时，所生成算法的有效性显著下降，尤其是在谱探针质量较低（即warm-start迭代次数少）的情况下。
- **鲁棒性**：实验显示，一旦谱探针的质量达到一定阈值（如超过75步Lanczos迭代），AUToSPEC的性能就会趋于稳定，证明了其对谱输入扰动的鲁棒性。
- **与经典理论的联系**：通过对学习到的多项式进行分析，发现它们表现出接近**minimax**和**equiripple**（等波纹）的行为，这与最优的Chebyshev多项式特性高度一致。表1中的“minimax optimality gap”显示，学习到的多项式远优于随机生成的多项式，且非常接近理论最优值。

---

## 4. 关键结论和发现

### 主要发现
1.  **ML可用于发现高性能NLA算法**：AUToSPEC的成功证明了机器学习不仅可以辅助，更能直接**发现全新的、高性能的数值算法**。
2.  **发现的算法具有理论最优性**：尽管是通过数据驱动的方式学习，但发现的算法在行为上与经典的最优理论（如Chebyshev逼近）惊人地吻合，表明ML能够“重新发现”并可能超越人类设计的数学智慧。
3.  **框架具备强大泛化能力**：在小型合成数据上训练的模型，能够无缝迁移到大规模、复杂的真实世界问题上，验证了该方法的实用潜力。

### 方法的局限性
- **依赖谱探针**：虽然探针是廉价的，但该方法仍然需要一个初步的谱估计步骤。对于谱结构极其复杂或难以估计的算子，其效果可能会受限。
- **架构限制**：主文本主要关注线性递推，尽管附录提到了更高阶和有理模型，但这些扩展在实际应用中可能面临更大的计算开销。
- **可解释性**：虽然发现了有效的算法，但神经网络内部的决策过程仍然是一个“黑箱”，其具体工作机制不如传统算法那样直观。

### 未来工作方向
- 探索更丰富的更新类别，如**高阶多项式递推**和**有理模型**（rational models），以进一步提升表达能力和逼近效率。
- 将框架扩展到**一阶优化方法**（如学习带动量的梯度下降变体）。
- 研究如何将AUToSPEC与现有的多层级求解器（multilevel solvers）结合，作为其中的一个组件。
- 探索在更多样化的NLA和科学计算任务上的应用。

</details>

---

### 10. [Optimistic World Models: Efficient Exploration in Model-Based Deep Reinforcement Learning](https://arxiv.org/abs/2602.10044)

**Authors**: Akshay Mete, Shahid Aamir Sheikh, Tzu-Hsiang Lin, Dileep Kalathil, P. R. Kumar  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.10044v1  

#### Abstract
Efficient exploration remains a central challenge in reinforcement learning (RL), particularly in sparse-reward environments. We introduce Optimistic World Models (OWMs), a principled and scalable framework for optimistic exploration that brings classical reward-biased maximum likelihood estimation ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Optimistic World Models: Efficient Exploration in Model-Based Deep Reinforcement Learning**

---

## **1. 主要贡献和创新点**

### **解决的问题**
- **稀疏奖励环境下的低效探索**：现有的基于世界模型（World Models）的 Model-Based Reinforcement Learning（MBRL）算法（如 DreamerV3、STORM）在稀疏奖励（sparse-reward）环境中表现不佳，因为其依赖的策略熵正则化（policy entropy regularization）等探索机制不足以驱动充分探索。
- **闭环辨识问题（Closed-loop Identification Problem）**：标准的 Certainty Equivalence 原则（即用最大似然估计建模后直接优化策略）会导致模型仅在闭环轨迹上被识别，可能收敛到次优策略。

### **提出的新方法**
- **Optimistic World Models (OWMs)**：一种将经典控制理论中的 **Reward-Biased Maximum Likelihood Estimation (RBMLE)** 原则引入深度强化学习的框架。
- **核心机制**：
  - 在世界模型训练目标中加入一个**乐观动力学损失（optimistic dynamics loss）**，该损失通过想象轨迹（imagined trajectories）引导模型的动力学预测偏向高奖励结果。
  - 无需不确定性估计或复杂约束优化，完全基于梯度更新，易于集成到现有框架中。

### **相比现有方法的优势**
| 特性 | OWMs | UCB类方法（如H-UCRL） | 启发式探索（如Ensemble, Intrinsic Motivation） |
|------|------|------------------------|---------------------------------------------|
| 是否需要不确定性估计 | ❌ | ✅（高计算开销） | ✅（通常需要） |
| 是否可扩展至大规模DRL | ✅（全梯度法） | ❌（非凸优化瓶颈） | ✅但效果不稳定 |
| 架构兼容性 | ✅（Plug-and-play） | ❌ | ✅ |
| 理论基础 | ✅（源自RBMLE，有收敛保证） | ✅ | ❌ |

> ✅ **优势总结**：OWMs 提供了一种**原则性强、计算高效、可插拔**的探索机制，克服了 UCB 方法的计算障碍，并优于启发式探索在稀疏奖励任务上的表现。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Atari100K Benchmark**：
  - 包含 26 个 Atari 游戏。
  - 每个游戏限制为 **400K 帧（约2小时真实交互）**，用于评估样本效率。
- **DeepMind Control (DMC) Suite**：
  - 连续控制任务，分为两个子集：
    - **DMC Proprio**：状态输入（state-based），预算 500K 步。
    - **DMC Vision**：图像输入（image-based），预算 1M 步。
  - 包括稀疏奖励变体（如 `Cartpole Swingup Sparse`）。

### **实验设置与评估指标**
- **评估方式**：
  - 报告平均奖励 ± 标准误差（mean reward ± SEM）。
  - 平滑窗口为 3，每 checkpoint 使用 20 条评估轨迹。
- **种子数量**：
  - O-DreamerV3 在 Atari100K 和 DMC 上使用 **10 个随机种子**。
  - O-STORM 及消融实验使用 **5 个种子**（受限于算力）。
- **主要指标**：
  - **Human-Normalized Score (HNS)**：衡量相对于人类表现的归一化得分。
  - Interquartile Mean (IQM)、Median 等鲁棒统计量。

### **基线方法对比**
- **主基线**：
  - **DreamerV3** vs **Optimistic DreamerV3 (O-DreamerV3)**
  - **STORM** vs **Optimistic STORM (O-STORM)**
- 所有其他组件保持一致，仅添加乐观动力学损失。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **Atari100K 上的整体性能**
| 方法 | 平均 HNS (%) |
|------|--------------|
| DreamerV3 | 97.45% |
| **O-DreamerV3** | **152.68%** ⬆️ (+55%) |
| STORM | 75.90% |
| **O-STORM** | **80.68%** |

> 🔥 **提升显著**：O-DreamerV3 在仅增加极小计算开销的情况下，实现了 **55% 的平均 HNS 提升**。

#### **稀疏奖励任务上的突破性表现**
- **Private Eye**（极端稀疏奖励）：
  - O-DreamerV3 达到 **2120.48 分**，而 DreamerV3 仅为 **115.53 分**（**18.3倍提升**）。
- **Freeway**：
  - STORM 得分为 0；
  - **O-STORM 达到 6.38 分**，是唯一取得正收益的方法。
- **Montezuma’s Revenge**：
  - O-DreamerV3 在 40M 步时达到近 **2倍于 DreamerV3 的平均回报**。

#### **DMC 上的表现**
- 在 `Acrobot Swingup Sparse` 和 `Cartpole Swingup Sparse` 中均有明显提升：
  - 如 `Acrobot Swingup Sparse`（Proprio）：
    - DreamerV3: 8.4 → **O-DreamerV3: 34.6**（+312%）
- 多数密集奖励任务性能持平或略有波动，无显著退化。

### **与基线方法的对比结果**
- OWMs 在 **绝大多数 Atari 游戏中优于基线**（见 Figure 4a）。
- 在 **稀疏奖励任务上优势最为突出**，验证了其探索能力的有效性。
- 即使在密集奖励任务（如 Enduro、UpNDown）也表现出竞争力。

### **消融实验结果**
#### **(1) 是否使用模型熵损失（Model Entropy Loss）**
- 移除熵项后性能下降（Figure 10），说明熵正则有助于防止模型过度自信。

#### **(2) 超参数敏感性分析**
- **乐观系数 $\alpha(t)$**：
  - 设为常数 $\alpha = 0.0001$ 表现最佳。
  - 若设为过大值（如 0.1），导致“过度乐观”，性能急剧下降（Figure 11）。
- **模型熵系数 $\eta$**：
  - 小值有益（如 $3\times10^{-6}$），大值（如 0.03）有害（Figure 12）。

#### **(3) 计算开销**
| 方法 | MsPacman 训练时间（分钟，RTX 4090） |
|------|-------------------------------|
| DreamerV3 | 115 |
| **O-DreamerV3** | **138** |
| STORM | 170 |
| **O-STORM** | **178** |

> ➕ **仅增加约 20% 的训练时间**，具备良好的可扩展性。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **RBMLE 原则可以成功迁移到大规模深度 MBRL**：
   - 首次实现全梯度形式的 RBMLE，解决了传统方法难以扩展的问题。
2. ✅ **乐观探索可通过修改模型训练目标实现**：
   - 不依赖 UCB 式的置信区间或 Thompson Sampling，而是通过损失函数直接注入乐观偏差。
3. ✅ **OWMs 显著提升稀疏奖励环境中的探索效率**：
   - 在多个极具挑战性的游戏中实现数量级级别的性能飞跃。
4. ✅ **方法具有强通用性和即插即用特性**：
   - 成功应用于 DreamerV3 和 STORM 两种不同架构，均有效。

### **局限性**
- **超参数设计仍较经验性**：
  - 当前使用固定 $\alpha(t)$，未自适应调整，可能限制进一步性能潜力。
- **理论收敛性尚未在深度网络下严格证明**：
  - RBMLE 在表格型 MDP 中有理论保障，但在神经网络参数化下的收敛性质需进一步研究。
- **对某些任务存在轻微性能下降**：
  - 如 `Hopper Hop` 等任务中 O-DreamerV3 表现略差，表明需更精细的平衡机制。

### **未来工作方向**
- 🔄 **设计自适应的 $\alpha(t)$ 调度机制**：
  - 类似 Agent57 中的元控制器，根据探索进度动态调节乐观程度。
- 📈 **结合其他探索机制**（如 Intrinsic Motivation）进行混合探索。
- 🧠 **开展更深入的理论分析**：
  - 探索梯度版 RBMLE 在非线性函数逼近下的收敛性与 regret bound。
- 🛠️ **推广至更多世界模型架构**（如 MuZero、EfficientZero）以验证普适性。

---

> 💡 **总结一句话**：  
> **Optimistic World Models (OWMs)** 将经典的 RBMLE 原则转化为可微分、可扩展的深度学习模块，通过在模型训练中注入“乐观偏见”，实现了在稀疏奖励环境下前所未有的探索效率，同时保持了与主流世界模型框架的无缝集成。

</details>

---

### 11. [Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective](https://arxiv.org/abs/2602.08009)

**Authors**: Rui Li, Zeyu Zhang, Xiaohe Bo, Quanyu Dai, Chaozhuo Li, Feng Wen, Xu Chen  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.08009v1  

#### Abstract
Multi-agent architectures built on large language models (LLMs) have demonstrated the potential to realize swarm intelligence through well-crafted collaboration. However, the substantial burden of manual orchestration inherently raises an imperative to automate the design of agentic workflows. We fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前多智能体系统（**Multi-Agent Systems, MAS**）在协调 **Large Language Model (LLM) Agents** 时面临三大挑战：
- **Adaptivity（适应性）**：传统方法依赖静态拓扑或预定义流程，无法根据动态任务流调整协作路径。
- **Scalability（可扩展性）**：新增或移除 agent 需要重新优化整个工作流，成本高昂；中心化控制器随 agent 数量增长出现组合爆炸瓶颈。
- **Robustness（鲁棒性）**：中心化控制存在单点故障风险（Single Point of Failure, SPoF），且易受对抗性攻击或幻觉行为影响。

这些限制阻碍了真正开放、自组织、可靠的 LLM 多智能体社会的发展。

---

### 提出的新方法与新思路
作者提出 **RAPS (Reputation-Aware Publish-Subscribe)** 框架，首次从 **Dynamic Ad-Hoc Networking（动态自组网）** 的视角重构 LLM Agent 协调问题。

#### 核心思想
将 LLM Agents 视为网络中的自治主机（Hosts），消息传播类比于信息包（Packets）转发，从而引入经典网络协议设计原则来解决协调难题。

#### 创新架构：三层结构
| 层级 | 组件 | 功能 |
|------|------|------|
| **Substrate（通信底座）** | **Distributed Publish-Subscribe Protocol** | 解耦 agent 为 Publisher（发布者）、Subscriber（订阅者）、Broker（中介），基于语义意图而非固定拓扑进行消息路由 |
| **Overlay I** | **Reactive Subscription（反应式订阅）** | 允许 agent 在运行时动态更新自身“意图”（即 system prompt），实现角色自适应演化 |
| **Overlay II** | **Bayesian Reputation（贝叶斯声誉机制）** | 每个 agent 维护本地 watchdog，通过贝叶斯估计评估对等体可靠性，实现去中心化的信任管理 |

该框架实现了 **intent-based collaboration** 和 **decentralized trust**，无需集中调度即可自发形成高效协作网络。

---

### 相比现有方法的优势
| 范式 | 代表方法 | 缺陷 | RAPS 的优势 |
|-------|----------|--------|-------------|
| **Communication-Agnostic** | GPTSwarm, AFlow, G-Designer | 固定拓扑，不感知推理过程中的语义变化；需离线搜索/训练 | 支持运行时动态意图匹配，适应性强 |
| **Meta-Controlled** | AutoAgents, Puppeteer, MAS-Zero | 中心化控制导致 SPoF 和扩展瓶颈 | 完全分布式，无单点故障，可水平扩展 |
| **Static Topology** | Chain, Star, Tree | 结构僵化，错误易传播 | 自主修复路径，抗干扰能力强 |

> ✅ RAPS 成功统一了 **Adaptivity, Scalability, Robustness** 三难困境，在一个无需训练的框架下同时满足三项关键需求。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖五个代表性基准，涵盖三大任务类别：

| 数据集 | 类别 | 任务描述 | 评估指标 |
|--------|------|---------|----------|
| **MMLU** | General Reasoning | 多领域知识问答（STEM、人文等） | Accuracy |
| **GSM8K**, **SVAMP**, **AQuA** | Mathematical Reasoning | 数学应用题求解（多步逻辑推理） | Accuracy |
| **HumanEval** | Code Generation | Python 编程问题生成 | Pass@1 |

> 📊 所有方法均使用相同数量（N=5）的 agent 进行公平比较。

---

### 实验设置
- **Backbone Model**: 主要使用 `GPT-4o-mini` 作为所有 agent 的 LLM 核心。
- **Broker 实现**:
  - 默认采用 `text-embedding-3-small` 进行嵌入相似度匹配（高效）
  - 可选 LLM-based Broker 用于复杂决策分析
- **超参数**:
  - 最大通信轮次 $ k = 5 $
  - 贝叶斯衰减因子 $ \lambda = 0.9 $
  - 声誉阈值 $ T_{rep} = 0.7 $，低于此值则拒绝通信

---

### 基线方法对比
共四类 baseline：
1. **Single-Agent Models**  
   - Vanilla IO, CoT, ComplexCoT, Self-Consistency (SC)

2. **Static Multi-Agent Models**  
   - Chain, Star, Tree, Random（固定拓扑）
   - LLM-Debate, LLM-Blender（共识机制）

3. **Communication-Agnostic Models**  
   - GPTSwarm, AgentPrune, AFlow, MaAS, G-Designer（基于图搜索或学习的工作流）

4. **Meta-Controlled Models**  
   - AutoAgents, Puppeteer, MAS-Zero（依赖中央 meta-controller）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）
| 方法 | MMLU | GSM8K | SVAMP | AQuA | HumanEval | **Average** |
|------|------|-------|-------|------|-----------|------------|
| G-Designer (SOTA prior) | 86.3 | 93.2 | 90.7 | 79.4 | 90.2 | 88.0 |
| **RAPS (Ours)** | **88.2** | **95.4** | **92.2** | **82.6** | **91.5** | **90.0** |

✅ RAPS 在所有五项任务上均达到 **state-of-the-art 性能**，平均准确率提升 **+2.0%**。

---

### 与基线方法的对比结果
- **优于静态结构**：如 Chain 在 SVAMP 上仅为 82.6%，显著低于 CoT（88.4%），表明刚性拓扑难以应对多样化推理模式。
- **超越通信无关模型**：相比 G-Designer，RAPS 在 AQuA 上高出 **+3.2%**，得益于运行时语义对齐能力。
- **击败中心化控制模型**：相比 Puppeteer 平均高 **+6.0%**，且避免了中心控制器崩溃风险（见鲁棒性测试）。

---

### 消融实验结果（Table 3）
消融验证了两个 overlay 机制的有效性：

| 变体 | MMLU | GSM8K | HumanEval |
|------|------|-------|----------|
| **RAPS (Full)** | **88.2** | **95.4** | **91.5** |
| w/o Reactive Subscription (w/o RS) | 85.6 (-2.6) | 93.7 (-1.7) | 89.3 (-2.2) |
| w/o Bayesian Reputation (w/o BR) | 86.9 (-1.3) | 94.5 (-0.9) | 90.7 (-0.8) |
| w/o Both | 83.7 (-4.5) | 92.8 (-2.6) | 88.5 (-3.0) |

🔹 **Reactive Subscription** 对性能提升贡献更大，说明动态角色调整至关重要。  
🔹 **Bayesian Reputation** 显著增强系统稳定性，尤其在对抗环境下。

---

### 鲁棒性测试（Table 2）—— Byzantine Stress Test
注入不同比例的 **Adversarial Agents（说谎者）** 测试系统容错能力（MMLU）：

| 方法 | 5T0A | 4T1A | 3T2A | 2T3A | 5T5A（混合） |
|------|-----|-----|-----|-----|--------|
| Chain | 84.3 | 72.5 | 50.3 | 22.2 | 16.3 |
| G-Designer | 86.3 | 80.4 | 37.9 | 15.0 | 49.7 |
| Puppeteer-C（攻击控制器） | 84.3 | **13.7** | — | — | — |
| **RAPS** | **88.2** | **87.6** | **84.3** | **83.0** | **86.3** |

🔴 Puppeteer 在控制器被攻破后立即崩溃（84.3% → 13.7%）。  
🟢 RAPS 几乎不受影响，即使一半 agent 是恶意节点仍保持 >83% 准确率。

---

### 可扩展性与效率分析（Figure 3）
- **Scalability**：随着 agent 数量增加，RAPS 持续提升性能，而 Chain 和 Puppeteer 因误差累积或控制负担加重而下降。
- **Efficiency**：RAPS 推理延迟稳定增长，远低于需要全局优化的 GPTSwarm/G-Designer（>2小时 for 20 agents）。

---

## 4. 关键结论和发现

### 主要发现
1. **Networking Perspective is Powerful**：将 MAS 协调建模为 **Dynamic Ad-Hoc Networking** 是一种富有成效的新范式，揭示了 adaptivity/scalability/robustness 的深层联系。
2. **Intent-Based Collaboration Works**：基于语义意图的 **Publish-Subscribe** 协议能有效打破刚性拓扑束缚，支持灵活、自组织的信息流动。
3. **Decentralized Trust is Essential**：**Bayesian Reputation** 机制可在无中心权威的情况下隔离恶意行为，极大提升系统鲁棒性。
4. **Zero-Shot Coordination is Feasible**：RAPS 无需任何训练，仅靠 inference-time prompting 即可实现高性能协调，具备强泛化能力。

---

### 方法的局限性
1. **依赖基础模型能力**：RAPS 是“智能放大器”，不能弥补 backbone LLM 本身的知识缺陷。
2. **冷启动问题（Cold Start）**：初始阶段因缺乏交互历史，声誉系统可能暂时无法识别恶意 agent。
3. **计算开销权衡**：虽然整体高效，但若启用 LLM-based Broker 或频繁更新订阅，会增加 token 消耗。

---

### 未来工作方向
1. **Learnable Coordination Protocols**：结合 **multi-agent reinforcement learning**，利用声誉分数作为内在奖励信号优化策略。
2. **Reputation Transfer**：跨会话迁移信任先验，缓解冷启动问题。
3. **Congestion Control & Packet Prioritization**：借鉴 TCP/IP 设计，管理上下文长度和优先级调度。
4. **Hierarchical Subnetting**：构建大规模 agent 社会的分层组织结构，提升管理效率。

---

> 💡 **总结**：RAPS 不仅是一个新方法，更开启了一个融合 **经典网络理论** 与 **现代 LLM 多智能体系统** 的广阔研究空间，为构建开放、自适应、可信的 agentic society 提供了坚实基础。

</details>

---

### 12. [Epistemic Throughput: Fundamental Limits of Attention-Constrained Inference](https://arxiv.org/abs/2602.09127)

**Authors**: Lei You  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.09127v1  

#### Abstract
Recent generative and tool-using AI systems can surface a large volume of candidates at low marginal cost, yet only a small fraction can be checked carefully. This creates a decoder-side bottleneck: downstream decision-makers must form reliable posteriors from many public records under scarce attent...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Epistemic Throughput: Fundamental Limits of Attention-Constrained Inference*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文研究在**注意力受限的推理（Attention-Constrained Inference, ACI）** 场景下的根本瓶颈：现代生成式 AI 和工具调用系统可以低成本地生成大量候选方案（如检索到的文档、自动生成的假设等），但下游决策者只能以高成本验证其中极小一部分。

这一现象广泛存在于科学发现、软件供应链审计、金融情报分析等高风险领域，其核心挑战是：如何在有限的**验证预算 $B$** 下，从大规模筛查出的 $K$ 个候选中选择最有价值的子集进行深度验证，从而最大化对真实状态 $O$ 的**后验不确定性减少**（即信息增益）。

### 提出的新方法与新思路
论文提出了一个信息论框架来刻画该问题，并引入以下核心概念与理论：

- **Epistemic Throughput（认知吞吐量）**：定义为在单位时间窗口内，通过筛查与验证所能实现的最大后验不确定性减少（以 **Bayes log-loss** 衡量）。这是衡量“将弱信号转化为可靠信念”的效率的核心指标。
  
- **JaKoB Scaling Law**：提出并证明了一个基本缩放律，描述了在 ACI 模型下可达到的信息增益上限：
  $$
  \text{Gain} \sim I_{\text{ver}} \left( Bp + \sqrt{JKB} \right)
  $$
  其中：
  - $B$: 验证预算（deep attention）
  - $K$: 筛查规模（broad attention）
  - $p$: 有价值记录的先验概率（prevalence）
  - $J = I(T;Z)$: 筛查质量，即筛查信号 $Z$ 与记录是否“有价值”（$T=1$）之间的互信息
  - $I_{\text{ver}}$: 单次成功验证所贡献的信息量

  该公式揭示了一个非线性放大效应：即使筛查质量 $J$ 很低，只要能大规模过采样（oversampling，即 $K \gg B$），仍可通过 $\sqrt{JKB}$ 项显著提升验证效率。

- **Tail Leverage 理论**：指出在稀疏验证场景（$B \ll K$）中，筛查的有效性高度依赖于筛查得分分布的**上尾行为**：
  - 若得分为**轻尾分布**（如 Gaussian），则增益仅随 $\log(K/B)$ 增长（收益递减）
  - 若得分为**重尾分布**（如 Pareto），则增益可达多项式级增长（$\propto (K/B)^{1/\nu}$）

### 相比现有方法的优势
- **理论奠基性**：不同于以往关注编码端约束的经典信息论，本文聚焦于**解码端注意力瓶颈**，填补了资源受限推理中的理论空白。
- **普适性强**：模型适用于多种现实系统，如 RAG、Tool-using Agents、Fact-checking Pipeline 等，提供统一的分析框架。
- **可操作指导意义**：不仅给出极限边界，还证明简单的 **top-$B$ score-based policy** 在弱筛查条件下即可逼近该极限，具有工程可行性。
- **强调“公共制品”复用价值**：提出验证输出应作为可共享的 **public artifacts**，以提高整体系统的 epistemic throughput。

---

## 2. 核心实验方法和设置

> 注：本文为**理论性论文**，未使用真实世界数据集进行传统意义上的“实验”，而是通过**数学建模、理论推导与仿真验证**相结合的方式展开。

### 模型设定
- **Haystack Regime**：考虑一个典型场景，其中有 $K$ 个待检记录，仅有少数 ($p \ll 1$) 是 informative 的。
- **两阶段流程**：
  1. **Screening Stage**：对 $K$ 条记录进行低成本检查，得到筛查统计量 $Z_i$。
  2. **Verification Stage**：基于 $Z_i$ 排序，选择 top-$B$ 进行高保真验证，获得 $(T_i, V_i)$。
- **Latent State Models**：
  - Global-O：所有记录共享一个全局真相 $O$
  - Per-record Claim：每条记录对应独立声明 $O_i$

### 评估指标
- **主要度量**：**Epistemic Throughput** = $H(O) - D(K,B)$，即初始熵减去贝叶斯最优预测下的期望 log-loss。
- **理论界限对比**：
  - 上界（Converse）：由 Theorem 6 给出
  - 下界（Achievability）：由 Theorem 10 中 top-$B$ 策略实现
  - Oracle Bound：理想情况下只验证真正 informative 的记录
  - Random Baseline：随机验证 $B$ 条记录，增益为 $Bp$

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Random Verification** | 不依赖筛查，直接随机选 $B$ 条记录验证，增益为 $Bp$ |
| **Perfect Screening (Oracle)** | 已知哪些记录是有价值的，仅验证这些，上限为 $B$ |
| **Top-$B$ Score Policy** | 使用 $n(Z)=P(T=1|Z)$ 或其他得分排序后取 top-$B$，本文主推策略 |

### 仿真实验（Appendix A & Fig. 4）
- **数据生成**：模拟 $K=10^4$ 条记录，使用 logistic 模型生成筛查得分：
  $$
  \pi_i = \sigma(\text{logit}(p_0) + \epsilon G_i)
  $$
  其中 $G_i \sim \mathcal{N}(0,1)$，$\epsilon$ 控制筛查强度（对应不同 AUC 水平）
- **策略**：应用 top-$B$ 验证策略
- **输出**：计算经验信息增益 $H(O) - D(K,B)$ 并与理论曲线比较

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Fig. 4）
| 筛查强度（AUC） | 是否接近理论边界 | 收益趋势 |
|------------------|--------------------|----------|
| Weak (~0.55)     | ✅ 非常接近 weak-screening approximation 曲线 | 增益较低，符合 $\sqrt{JKB}$ 趋势 |
| Moderate (~0.70) | ✅ 匹配 benchmark prediction | 明显优于 random baseline |
| Strong (~0.79)   | ⚠️ 逼近上限但仍低于 converse bound | 收益持续上升 |
| Very Strong (~0.90) | ⚠️ 接近 finite-pool oracle 上限 | 几乎达到最优 |

> 所有设置下，empirical gain 均位于 converse bound 之下，且与 benchmark prediction（Theorem 17）高度吻合。

### 与基线方法对比结果
- **相比 Random Verification**：
  - 在 $K=10^4, B=1000, p=0.01$ 设置下，random baseline 增益约为 10 bits
  - 使用 moderate screening 后，增益可达约 60–80 bits，提升达 **6–8倍**
- **相比 Oracle Bound**：
  - Oracle 在最强筛查下可达 ~90 bits
  - 实际策略可达 ~85 bits，效率超过 90%

### 消融实验（隐含分析）
虽然没有显式消融表，但论文通过多个维度进行了敏感性分析：
- **改变 $J$（筛查质量）**：随着 $J$ 增大，$\sqrt{JKB}$ 项主导增益，验证收益快速上升
- **改变 $K/B$（过采样比）**：
  - 对 Gaussian 得分：增益随 $\sqrt{\log(K/B)}$ 缓慢增长
  - 对 Pareto 得分：增益随 $(K/B)^{1/\nu}$ 快速增长 → **重尾设计更优**
- **改变 $p$（稀有性）**：越稀有的信号，筛查带来的相对增益越大

---

## 4. 关键结论和发现

### 主要发现
1. **存在非线性放大机制**：即便筛查质量很低（weak screening），只要能大规模过采样（high $K/B$），就能通过 $\sqrt{JKB}$ 项显著增强稀缺验证能力。
2. **筛查的价值在于“选择性”而非“准确性”**：筛查本身不需直接揭示真相，只需能区分 informative 与 non-informative 记录即可创造价值。
3. **上尾分布决定杠杆效应**：在 $B \ll K$ 的 haystack 场景中，**heavy-tailed score distributions**（如 Pareto）比 light-tailed（如 Gaussian）更具优势，能带来**多项式级而非对数级**的增益。
4. **Top-$B$ 策略是高效的**：简单地按 Bayes score $P(T=1|Z)$ 排序并验证 top-$B$，即可在弱筛查极限下达到理论最优。
5. **Public Artifacts 是放大器**：验证结果一旦发布为公共制品，可被多 decoder 复用，极大提升整体 epistemic throughput。

### 方法的局限性
- **静态与独立同分布假设**：当前模型为单窗口、i.i.d. 设定，未考虑序列依赖、反馈闭环或动态演化。
- **Log-loss 为中心**：虽适合信息增益分析，但实际任务可能更关心 decision regret、FDR 或 calibration。
- **验证成本均质化**：假设所有验证成本相同，而现实中可能存在多层次验证路径（cheap check vs. expert audit）。
- **忽略对抗性操纵**：未建模恶意生产者故意制造高分假记录的行为。

### 未来工作方向
1. **扩展至 Sequential Setting**：结合 active learning、bandits 框架，研究长期信息获取策略。
2. **多样化目标函数**：探索在 false discovery rate 控制、abstention-aware calibration 等目标下的 throughput 极限。
3. **异构验证成本建模**：引入 multi-level verification ladder，支持 cost-aware selection。
4. **设计 Heavy-tailed Scoring Functions**：研究如何构建能产生 exploitable extremes 的 ranking 机制（如极端值网络、异常检测集成）。
5. **激励机制与安全设计**：研究如何通过机制设计鼓励生产可验证内容，防止 adversarial spoofing。
6. **端到端系统优化**：将 epistemic communication 作为设计范式，联合优化 generation、retrieval、scoring 与 verification 模块。

---

> **总结一句话**：  
> 在生成过剩、验证稀缺的时代，**epistemic throughput** 成为衡量系统真理发现效率的新范式；通过 **JaKoB scaling law** 和 **tail leverage 理论**，本文揭示了如何利用廉价筛查非线性放大昂贵验证的能力——关键是**过采样 + 重尾得分 + 公共制品复用**。

</details>

---

### 13. [PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition](https://arxiv.org/abs/2602.08240)

**Authors**: Xun Su, Huamin Wang, Qi Zhang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.08240v1  

#### Abstract
Speech Emotion Recognition (SER) is widely deployed in Human-Computer Interaction, yet the high computational cost of conventional models hinders their implementation on resource-constrained edge devices. Spiking Neural Networks (SNNs) offer an energy-efficient alternative due to their event-driven ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition》总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统基于 **Artificial Neural Networks (ANNs)** 的 **Speech Emotion Recognition (SER)** 模型虽然性能优越，但计算开销大、能耗高，难以部署在资源受限的边缘设备上。而 **Spiking Neural Networks (SNNs)** 虽然具备事件驱动、低功耗的优势，但在直接处理来自 **Self-Supervised Learning (SSL)** 模型（如 emotion2vec）的连续高动态范围特征时，存在严重的**分布不匹配（distribution mismatch）**问题。

具体表现为：
- 高方差输入导致 **LIF/PLIF 神经元**陷入“功能静默”（functional silence）或“饱和”（saturation），破坏了稀疏时间编码能力，从而严重降低识别性能。

### 🚀 提出的新方法与创新思路
作者提出 **PTS-SNN**（Prompt-Tuned Spiking Neural Network），一种参数高效的神经形态适配框架，实现 SSL 特征与 SNN 动态的高效对齐。其核心创新包括：

#### （1）**Temporal Shift Spiking Encoder**
- 引入无参数的 **Temporal Shift** 操作，在残差 spiking 模块中捕获局部时间依赖。
- 通过通道移位实现帧间信息交换，无需额外 FLOPs，稳定原始特征流的高频波动，构建鲁棒输入基础。

#### （2）**Context-Aware Membrane Potential Calibration**
- 设计基于 **Spiking Sparse Linear Attention (SSLA)** 的提示机制，将可学习的 **soft prompts** 作为上下文查询。
- 利用 **PLIF 神经元**对输入进行二值化过滤，生成稀疏 spike map，再通过线性注意力聚合全局语义上下文到 prompts 中。
- 提出 **Homeostatic Bias Generator**，将 prompts 映射为动态电压偏置 $ V_{\text{bias}} $，主动调节 PLIF 神经元的膜电位基线。

> 🔬 **生物启发机制**：该策略模拟生物中的稳态调节（homeostatic regulation），使输入分布中心对齐到神经元敏感响应区间（around $ V_{th} $），避免静默或饱和。

#### （3）**参数高效性设计**
- 冻结上游 SSL 主干（如 emotion2vec），仅训练轻量级适配器模块（adapter），总可训练参数仅 **1.19M**，远低于全微调方案。

### ⚖️ 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **性能** | 在 IEMOCAP 上达到 **73.34% WA**，优于多数 ANN 基线 |
| **能效** | 单样本推理能耗仅 **0.35 mJ**，比 ANN 低 2–3 个数量级 |
| **参数效率** | 参数量仅为 ShiftFormer 的 ~12.5%，适合边缘部署 |
| **兼容性** | 成功桥接连续 SSL 表示与离散 spiking 动力学，解决根本性分布鸿沟 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在五个多语言 SER 数据集上进行全面评估：

| 数据集 | 语言 | 情感类别数 | 样本数 | 交叉验证策略 |
|-------|------|------------|--------|----------------|
| **IEMOCAP** | 英语 | 4 | 5,531 | Leave-one-session-out, 5-fold |
| **CASIA** | 中文 | 4 | 1,200 | Random leave-one-speaker-out, 4-fold |
| **EMODB** | 德语 | 7 | 535 | Random leave-one-out, 10-fold |
| **EMOVO** | 意大利语 | 6 | 588 | 同上 |
| **URDU** | 乌尔都语 | 4 | 400 | 同上 |

> 所有实验均采用冻结的 **emotion2vec** 作为特征提取器。

### 📊 评估指标
- **Weighted Accuracy (WA)**：总体准确率，反映全局性能
- **Unweighted Accuracy (UA)**：各类别平均准确率，应对类别不平衡
- **Trainable Parameters (M)**：模型复杂度
- **Inference Energy (mJ/sample)**：基于 45nm CMOS 工艺估算，区分 MAC（ANN）与 AC（SNN）操作能耗

### 🔁 基线方法对比
| 基线模型 | 类型 | 核心机制 |
|---------|------|----------|
| **Vanilla SNN** | SNN | 直接将 SSL 特征输入 SNN，无适配 |
| **emotion2vec + ANN Fine-tuning** | ANN | 全微调下游分类头 |
| **MSTR** | ANN | Multi-Scale Transformer |
| **ShiftFormer** | ANN | 基于稀疏 shift 的高效架构 |
| **ENT**, **DST**, **Co-attention** | ANN | 注意力机制变体 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（以 IEMOCAP 为例）

| Model | WA (%) | UA (%) | Params (M) | Energy (mJ) |
|-------|--------|--------|------------|-------------|
| **emotion2vec (fine-tuned)** | 71.79 | — | >70M (estimated) | — |
| **Vanilla SNN** | 41.78 | — | ~1.19M | 0.35 |
| **MSTR** | 70.60 | 71.60 | 30.03 | 51.06 |
| **ShiftFormer** | 72.10 | 72.70 | 9.50 | 16.20 |
| **ENT** | 72.43 | 73.88 | 8.55 | 6.35 |
| **PTS-SNN (Ours)** | **73.34** | **73.72** | **1.19** | **0.35** |

✅ **结论**：
- PTS-SNN 在 **WA 和 UA 上均优于所有 ANN 基线**，同时参数量最小、能耗最低。
- 能耗仅为 ShiftFormer 的 **~2.2%**，MSTR 的 **~0.7%**，展现出极强的边缘适用性。

### 🌍 跨语言泛化能力（WA %）

| Dataset | emotion2vec | PTS-SNN | Δ↑ |
|--------|-------------|---------|----|
| **CASIA** (中文) | 69.20 | 72.50 | +3.30 |
| **EMODB** (德语) | 84.34 | 87.49 | +3.15 |
| **EMOVO** (意大利语) | 61.21 | 61.94 | +0.73 |
| **URDU** (乌尔都语) | 81.50 | 84.25 | +2.75 |

➡️ 表明所学 prompts 捕捉的是**语言无关的情感韵律模式**，而非语言内容本身，具有良好的跨语言迁移能力。

### 🔍 消融实验（Ablation Study on IEMOCAP）

| Configuration | WA (%) | UA (%) |
|---------------|--------|--------|
| **Full Model (PTS-SNN)** | **73.34** | **73.72** |
| w/o Prompt Tuning | 71.76 | 71.90 |
| w/o Attention Module | 68.35 | 69.10 |
| Only Temporal Shift | 61.27 | 62.05 |

📌 发现：
- 移除 **prompt tuning** 导致性能下降 1.58%，说明其比直接微调更有效。
- 移除 **attention 模块** 性能骤降 5%，表明全局上下文建模至关重要。
- 仅有 temporal shift 时性能最差，说明局部建模不足以支撑高性能 SER。

### 📉 超参数敏感性分析
- **Prompt Length ($ L_p $)**：最优长度为 5；超过后性能下降，表明冗余 prompts 有害。
- **Bias Scaling Factor ($ \kappa $)**：最佳值为 0.5；过高引入噪声，过低激活不足。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **分布不匹配是阻碍 SNN 应用于 SSL 特征的根本障碍**，直接转换会导致性能崩溃（从 71.79% → 41.78%）。
2. **PTS-SNN 成功实现了连续表示与离散 spiking 动力学之间的高效对齐**，通过 prompt 驱动的膜电位校准机制，显著提升 SNN 的表达能力。
3. 所提方法在保持 **超低参数量（1.19M）和能耗（0.35 mJ）** 的前提下，实现了与先进 ANNs 相当甚至更优的性能。
4. 模型具备良好**跨语言泛化能力**，验证了其捕捉深层情感线索的能力。

### ⚠️ 局限性
- 当前框架仅使用**单模态音频信号**，未融合文本或面部表情等互补信息。
- 所有实验基于离线推理设定，尚未在真实 neuromorphic hardware（如 Loihi、Speck）上部署验证实时性。
- 对极端噪声环境下的鲁棒性未充分测试。

### 🔮 未来工作方向
- 将 PTS 机制扩展至 **Multimodal Spiking Architectures**，融合 audio-visual-textual 信号。
- 探索在类脑芯片上的实际部署与能效优化。
- 研究更复杂的 homeostatic regulation 策略，如自适应阈值、突触可塑性结合。

---

> 💡 **一句话总结**：  
> PTS-SNN 通过生物启发式的 **prompt-to-voltage** 映射机制，首次实现了 SSL 与 SNN 的高效协同，在几乎不增加参数的前提下，达成高性能、超低功耗的语音情感识别，为边缘智能提供了极具前景的技术路径。

</details>

---

### 14. [Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs](https://arxiv.org/abs/2602.08241)

**Authors**: Siqu Ou, Tianrui Wan, Zhiyuan Zhao, Junyu Gao, Xuelong Li  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.08241v1  

#### Abstract
While chain-of-thought (CoT) reasoning has substantially improved multimodal large language models (MLLMs) on complex reasoning tasks, existing approaches largely rely on long textual reasoning trajectories and provide limited mechanisms for learning stable visual attention policies. Our analysis sh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 **Multimodal Large Language Models (MLLMs)** 在复杂视觉推理任务中虽然广泛采用 **Chain-of-Thought (CoT)** 推理机制，但存在一个关键缺陷：**视觉注意力不稳定且难以纠正早期错误**。

- 分析表明，MLLMs 在推理初期若将注意力放在错误的图像区域，这种 **视觉错位（visual misalignment）** 很少在后续推理中被修正。
- 这导致错误持续传播，最终造成系统性推理失败。
- 根本原因在于训练过程中缺乏对 **视觉注意力行为的有效信用分配（credit assignment）** ——即模型无法学习“何时、何处应关注图像”。

### **提出了什么新方法或新思路**

提出 **SAYO**（一种基于强化学习的视觉推理模型），并引入 **Entropy-Based Target Attention Reward**，其核心思想是：

- 在 **Reinforcement Learning (RL)** 框架下，设计一种 **区域级视觉注意力奖励机制**。
- 该奖励基于模型生成 token 时对目标图像区域的注意力权重，**显式地将优化信号与视觉锚定的推理步骤对齐**。
- 特别地，奖励仅作用于 **高熵 token**（即模型不确定性高的决策点），迫使模型在此类关键节点上必须参考视觉证据进行“验证”（Look-to-Verify），从而抑制“盲目推理”（hallucination）。

### **相比现有方法的优势**

| 对比维度 | 现有方法 | SAYO |
|--------|--------|------|
| **注意力学习机制** | 依赖启发式提示工程（如框选区域）、后处理反思机制 | 通过 RL 奖励直接优化注意力策略，实现端到端学习 |
| **是否需要外部干预** | 需要人工设计视觉提示或迭代反思 | 推理阶段无需额外提示或流程 |
| **泛化能力** | 多为任务特定设计，迁移性差 | 在数学、图表、通用视觉推理等多领域均提升 |
| **训练效率** | 通常依赖长文本推理轨迹 | 聚焦高信息量 token，训练更高效稳定 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **通用视觉推理**：
  - `GQA`：复杂场景下的组合问答
  - `M3CoT`, `V*`, `MMStar`, `MME-RealWorld`：涵盖多步推理、现实高分辨率图像理解
- **数学视觉推理**：
  - `We-Math`, `MathVision`：结合几何图、公式与文本的数学题
- **结构化图像理解**：
  - `ChartQA`, `AI2D`, `CharXiv`：图表、科学插图的理解与问答
- **训练数据来源**：
  - `GQA` + `ReFocus_Data`（共约 20k 样本），用于 RL 训练

### **实验设置和评估指标**

- **基础模型**：
  - `Qwen3-VL-8B`, `InternVL3.5-8B`
- **训练框架**：
  - 使用 **Group Relative Policy Optimization (GRPO)** 进行强化学习训练
  - 训练周期：4 epochs，6 × NVIDIA H200 GPU
  - 奖励构成：`region attention reward` + `format reward`
- **评估方式**：
  - 所有模型输出需遵循 `<think>...</think>` 和 `\boxed{}` 格式
  - 主要指标：**准确率（Accuracy）**
  - 辅助分析指标：
    - **Target Attention Score (TAS)**：衡量模型对目标区域的平均注意力强度
    - **Attention Advantage Score (Ra)**：归一化后的注意力优势度量

### **基线方法对比**

- **闭源模型（Closed-Source）**：
  - `GPT-4o`, `Gemini 2.5 Pro`
- **开源通用 MLLMs**：
  - `Qwen3-VL`, `InternVL3.5`, `Kimi-VL-16B`
- **开源推理专用 MLLMs**：
  - `ViGoRL`, `OpenVLThinker-7B`, `Semantic-back-7B`, `NoisyRollout-7B`, `R1-Onevision-7B`

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| 模型 | Avg Score |
|------|---------|
| **SAYO-Qwen-8B** | **64.03** ✅（开源最佳） |
| SAYO-Qwen-4B | 62.17 |
| Qwen3-VL-8B | 59.64 |
| OpenVLThinker-7B | 59.79 |
| GPT-4o | 未完整报告，部分任务低于 SAYO |

> **亮点表现**：
> - 在 `MMStar` 上，**SAYO-Qwen-8B 超越 GPT-4o 和 Kimi-VL-16B**
> - 在 `We-Math` 和 `MathVision` 上显著领先，尽管**未在数学数据上训练**
> - 在 `ChartQA`、`AI2D` 等结构化任务上也全面优于基线

### **与基线方法的对比结果**

- **SAYO-Qwen-8B** 在几乎所有 benchmark 上都优于同规模甚至更大参数的开源模型。
- 即使是较小的 `SAYO-Qwen-4B`，也超过了原始 `8B` 模型的表现。
- 在多个任务上达到或超过闭源模型水平，证明了其强大的泛化能力。

### **消融实验结果（Table 2 & Table 3）**

#### **不同奖励组合的影响（Table 2）**

| 设置 | Avg 提升（vs Base） |
|------|------------------|
| Accuracy-only Reward | +1.28 |
| Attention-only Reward | **+4.32** |
| Combined Reward | **+4.63** |

> 结论：**视觉注意力奖励比答案准确性奖励更能驱动性能提升**，说明当前瓶颈在于“看见”，而非“推理”。

#### **Token 选择策略的影响（Table 3）**

| 策略 | 性能提升 |
|------|--------|
| Reward on **all tokens** | +0.87 ~ +2.23 |
| Reward on **top 30% high-entropy tokens** | **+3.21 ~ +6.54** ✅ |

> 结论：**仅对高熵 token 施加注意力奖励效果最好**，避免低信息 token 引入噪声，提升训练稳定性与效率。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **视觉注意力错位是 MLLMs 推理失败的主因**  
   - 早期注意力错误极少被纠正，形成“错误链”。
   - 存在强正相关性：**Target Attention Score (TAS) ↑ → 准确率 ↑**

2. ✅ **现有 RL 方法未能有效优化视觉注意力**  
   - 当前 RL 主要改进文本推理路径，但对视觉聚焦帮助有限。

3. ✅ **SAYO 成功实现了“主动看”机制**  
   - 模型学会在不确定时主动回看图像，尤其在高熵 token 阶段增强对目标区域的关注（见 Figure 4, 6）。
   - 可视化显示，SAYO 在关键推理步骤始终锁定正确区域（Figure 5）。

4. ✅ **跨域泛化能力强**  
   - 尽管未在数学数据上训练，但在 `We-Math` 和 `MathVision` 上大幅提升。
   - 原因：**修复了“输入信号”——让模型先“看清三角形边”再推理，释放了已有数学引擎的能力**。

### **方法的局限性**

- 依赖带有 **bounding box 注释的数据** 构建监督信号，限制了可扩展性。
- 当前奖励机制基于最后一层 attention，可能未充分捕捉多层视觉融合动态。
- 对极端低质量图像或高度遮挡场景仍可能失效。

### **未来工作方向**

- 探索 **无监督或弱监督方式构建注意力奖励信号**，减少标注依赖。
- 设计 **分层注意力奖励机制**，覆盖不同网络深度的视觉语义。
- 将 SAYO 框架扩展至 **视频、3D 场景等时序或多模态输入**。
- 研究如何将“Look-to-Verify”机制迁移到其他认知任务（如听觉-语言推理）。

---

> **总结一句话**：  
> SAYO 揭示了 MLLMs 的真正瓶颈不是“不会想”，而是“没看清”。通过引入 **基于高熵 token 的注意力奖励机制**，它教会模型“在不确定时回头看看”，从而显著提升视觉推理的鲁棒性与准确性。

</details>

---

### 15. [Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference](https://arxiv.org/abs/2602.10021)

**Authors**: Wenxuan Xie, Yujia Wang, Xin Tan, Chaochao Lu, Xia Hu, Xuhong Wang  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.10021v1  

#### Abstract
The integration of extensive, dynamic knowledge into Large Language Models (LLMs) remains a significant challenge due to the inherent entanglement of factual data and reasoning patterns. Existing solutions, ranging from non-parametric Retrieval-Augmented Generation (RAG) to parametric knowledge edit...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在处理**知识密集型任务**时面临以下挑战：
- **上下文窗口有限**：传统方法如 RAG 或长文本提示受限于 context window 和检索器噪声。
- **静态压缩丢失关键信息**：现有 prompt compression 方法多为静态、查询无关（query-agnostic），在高比例压缩下容易丢失任务相关的关键事实。
- **参数更新风险大**：通过 fine-tuning 或 knowledge editing 注入新知识可能导致灾难性遗忘（catastrophic forgetting）或破坏原有推理能力。

### 提出的新方法与思路
提出 **DRIFT**（Decoupled Reasoning with Implicit Fact Tokens），一种**双模型架构**，实现知识提取与推理过程的显式解耦：
- **轻量级知识模型**（Knowledge Model）：动态地将文档块压缩为基于查询条件的**隐式事实令牌**（Implicit Fact Tokens），这些是高密度的潜在表示。
- **大型推理模型**（Reasoning Model）：接收由知识模型生成的事实嵌入，在其 embedding space 中进行高效复杂推理。
- **Latent Space 交互**：两个模块通过一个可训练的 MLP Projector 在潜在空间中对齐，避免原始文本冗余干扰。

该框架实现了“**读取-压缩-推理**”的流水线化设计，支持超长上下文场景下的高效推理。

### 相比现有方法的优势
| 维度 | DRIFT 的优势 |
|------|-------------|
| **效率** | 显著降低推理延迟，实测在 256k-token 文档上平均提速 **7×**。 |
| **准确性** | 在 LongBench-v2 上，相比 Mistral-7B 基线，准确率从 20.87% 提升至 **29.22%**。 |
| **压缩比** | 支持高达 **128× 的压缩比**，仍保持竞争力，表现出极强鲁棒性。 |
| **泛化性** | 可无缝扩展到多文档、超长序列（百万 token 级别）且无需额外微调。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **BAMBOO**：综合性长文本建模评测套件，涵盖问答、代码补全等任务。
- **L-Eval**：平衡单跳与多跳推理能力评估，文档长度可达 **256K tokens**，减少知识泄露影响。
- **LongBench-v2**：包含需跨多文档理解与结构化数据推理的挑战性选择题，上下文达数百万词。
- **LoCoMo**：聚焦长期对话记忆，测试时间推理与事件摘要能力。

此外，作者构建了一个大规模 **Document-QA-Evidence 数据集**，包含超过 30 万实例，文档长度覆盖 1K–8K tokens，并提供细粒度证据标注。

### 实验设置与评估指标
| 类型 | 内容 |
|------|------|
| **主干模型** | 知识模型：`Qwen2.5-Instruct-3B`；推理模型：`Mistral-7B-Instruct-v0.2` / `Qwen2.5-7B-Instruct` |
| **训练方式** | 使用 LoRA 进行参数高效微调（Parameter-Efficient Fine-Tuning）。 |
| **评估指标** | - 多选/封闭式任务：使用 `Qwen-2.5-72B-Instruct` 作为 LLM-Judge 计算 **Accuracy**。<br>- 摘要类任务：报告 **ROUGE-L** 分数。 |
| **压缩策略** | 引入 **Bucketed Compression**，按输入长度区间映射固定输出大小，优于固定比率压缩。 |

### 基线方法对比
| 类型 | 对比方法 |
|------|--------|
| **Hard Compression** | LLMLingua-2（基于 token 选择） |
| **Soft Compression (Latent)** | ICAE, COCOM, xRAG |
| **RAG-based** | NaiveRAG（使用 BGE-M3 检索） |
| **Vanilla Baseline** | Mistral-7B / Qwen2.5-7B 直接输入全文 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Mistral-7B 为推理模型）
| 方法 | Compression Ratio | LongBench-v2 (Acc.) | LoCoMo (Acc.) | L-Eval QA Avg |
|------|-------------------|------------------------|----------------|---------------|
| Vanilla Mistral-7B | 1× | 20.87 | 59.35 | 53.94 |
| COCOM (128×) | 128× | 6.95 | 8.96 | 25.05 |
| xRAG (128×) | 128× | 25.58 | 24.74 | 35.56 |
| **DRIFT (Ours)** | **32×** | **29.22** | **57.73** | **48.28** |
| **DRIFT (Ours)** | **64×** | **29.77** | **53.31** | **46.67** |
| **DRIFT (Ours)** | **128×** | **24.65** | **50.71** | **44.24** |

> ✅ **结论**：即使在极端压缩比（128×）下，DRIFT 依然显著优于其他软压缩方法。

### 与其他方法的对比结果
- 在 **QMSUM** 和 **SPACE**（任务导向摘要）以及 **LoCoMo** 上，传统压缩方法表现崩溃，而 DRIFT 保持有效。
- 在 **BAMBOO** 和 **LongBench-v2** 上，DRIFT 在所有压缩比下均取得 SOTA 性能。
- 在 **256k-token 输入**上，端到端 TTFT（Time-to-First-Token）分析显示，DRIFT 推理速度比 Full-Context 基线快约 **7倍**。

### 消融实验结果（Ablation Study）
| 方法变体 | LongBench-v2 | LoCoMo | BAMBOO |
|---------|--------------|--------|--------|
| **Full DRIFT (32×)** | 29.22 | 57.73 | 60.15 |
| w/o LFRP | 26.84 (-2.38) | 52.68 (-5.05) | 57.64 (-2.51) |
| w/o QAFT-DC | 25.84 (-3.38) | 57.36 (-0.37) | 56.64 (-3.51) |
| w/o QAFT-QA | 18.05 (-11.17) | 36.89 (-20.84) | 45.14 (-15.01) |

> 🔍 **发现**：
> - **QAFT-QA** 是最核心组件，直接影响最终答案生成能力。
> - **LFRP** 和 **QAFT-DC** 共同优化潜在空间的质量与效率，提升整体鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **解耦设计有效**：将知识提取与推理分离，使轻量模型专注压缩、大模型专注推理，显著提升效率与精度。
2. **动态压缩优于静态**：基于查询的动态压缩能保留更多任务相关证据，尤其适用于稀疏关键信息场景。
3. **高比例压缩可行**：通过 Bucketed Compression 和三层训练流程，可在 **128× 压缩比**下仍维持高性能。
4. **隐式表示非简单复制**：诊断指标 MED 显示，推理模型经历“先对齐后专业化”过程，说明隐式事实令牌不是原文压缩副本，而是支持更高效推理的**互补模态**。
5. **零样本迁移能力强**：仅在单文档上训练的 DRIFT，可直接用于多文档推理而无需重新训练。

### 方法的局限性
1. **模型规模限制**：当前实验集中在 ≤14B 参数模型，更大规模模型上的 scaling law 尚未验证。
2. **可解释性差**：隐式事实令牌不可读，难以像 RAG 那样提供明确引用来源，不利于调试与可信 AI。
3. **依赖监督信号**：训练需要高质量的 evidence 标注，自动构造虽可行但存在质量波动风险。
4. **训练复杂度高**：三阶段训练流程增加了工程实现难度。

### 未来工作方向
1. **引入强化学习**（Reinforcement Learning）优化知识选择策略，进一步提升压缩质量。
2. **探索可解释性机制**：尝试将隐式令牌反向投影为自然语言片段，增强透明度。
3. **扩展至更大模型**：研究 DRIFT 在百亿级以上模型中的应用潜力。
4. **结合 Retrieval 与 Editing**：融合 RAG 的灵活性与 DRIFT 的高效性，构建统一的知识增强框架。

---

> 📌 **总结一句话**：  
> DRIFT 通过**双模型解耦 + 查询感知的隐式事实压缩**，实现了**高效、准确、可扩展**的长上下文推理新范式，在性能与效率之间取得了卓越平衡。

</details>

---

### 16. [Revealing the Challenges of Attention-FFN Disaggregation for Modern MoE Models and Hardware Systems](https://arxiv.org/abs/2602.09721)

**Authors**: Guowei Liu, Hongming Li, Yaning Guo, Yongxi Lyu, Mo Zhou, Yi Liu, Zhaogeng Li, Yanpeng Wang  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.09721v1  

#### Abstract
Deploying large-scale MoE models presents challenges in memory capacity and bandwidth for expert activation. While Attention-FFN Disaggregation (AFD) has emerged as a potential architecture to decouple compute and memory resources, its performance boundaries compared to standard large-scale Expert P...

---

### 17. [Boltzmann Reinforcement Learning for Noise resilience in Analog Ising Machines](https://arxiv.org/abs/2602.09162)

**Authors**: Aditya Choudhary, Saaketh Desai, Prasad Iyer  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.09162v1  

#### Abstract
Analog Ising machines (AIMs) have emerged as a promising paradigm for combinatorial optimization, utilizing physical dynamics to solve Ising problems with high energy efficiency. However, the performance of traditional optimization and sampling algorithms on these platforms is often limited by inher...

---

### 18. [SCOUT-RAG: Scalable and Cost-Efficient Unifying Traversal for Agentic Graph-RAG over Distributed Domains](https://arxiv.org/abs/2602.08400)

**Authors**: Longkun Li, Yuanben Zou, Jinghan Wu, Yuqing Wen, Jing Li, Hangwei Qian, Ivor Tsang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08400v1  

#### Abstract
Graph-RAG improves LLM reasoning using structured knowledge, yet conventional designs rely on a centralized knowledge graph. In distributed and access-restricted settings (e.g., hospitals or multinational organizations), retrieval must select relevant domains and appropriate traversal depth without ...

---

### 19. [MILE-RefHumEval: A Reference-Free, Multi-Independent LLM Framework for Human-Aligned Evaluation](https://arxiv.org/abs/2602.09624)

**Authors**: Nalin Srun (UL, CNRS, LORIA), Parisa Rastin (UL, CNRS, LORIA), Gu\'ena\"el Cabanes (UL, CNRS, LORIA), Lydia Boudjeloud Assala (UL, CNRS, LORIA)  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.09624v1  

#### Abstract
We introduce MILE-RefHumEval, a reference-free framework for evaluating Large Language Models (LLMs) without ground-truth annotations or evaluator coordination. It leverages an ensemble of independently prompted evaluators guided by a human-aligned schema, supporting both discrete and continuous sco...

---

### 20. [Unsupervised Layer-Wise Dynamic Test Time Adaptation for LLMs](https://arxiv.org/abs/2602.09719)

**Authors**: Longhuan Xu, Cunjian Chen, Feng Yin  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.09719v1  

#### Abstract
Test-time adaptation (TTA) for large language models (LLMs) updates model parameters at inference time using signals available at deployment. This paper focuses on a common yet under-explored regime: unsupervised, sample-specific TTA, where the model adapts independently for each prompt using only t...

---

### 21. [Stabilizing Physics-Informed Consistency Models via Structure-Preserving Training](https://arxiv.org/abs/2602.09303)

**Authors**: Che-Chia Chang, Chen-Yang Dai, Te-Sheng Lin, Ming-Chih Lai, Chieh-Hsin Lai  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.09303v1  

#### Abstract
We propose a physics-informed consistency modeling framework for solving partial differential equations (PDEs) via fast, few-step generative inference. We identify a key stability challenge in physics-constrained consistency training, where PDE residuals can drive the model toward trivial or degener...

---

### 22. [Steer2Adapt: Dynamically Composing Steering Vectors Elicits Efficient Adaptation of LLMs](https://arxiv.org/abs/2602.07276)

**Authors**: Pengrui Han, Xueqiang Xu, Keyang Xuan, Peiyang Song, Siru Ouyang, Runchu Tian, Yuqing Jiang, Cheng Qian, Pengcheng Jiang, Jiashuo Sun, Junxia Cui, Ming Zhong, Ge Liu, Jiawei Han, Jiaxuan You  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.07276v1  

#### Abstract
Activation steering has emerged as a promising approach for efficiently adapting large language models (LLMs) to downstream behaviors. However, most existing steering methods rely on a single static direction per task or concept, making them inflexible under task variation and inadequate for complex...

---

### 23. [Advancing Block Diffusion Language Models for Test-Time Scaling](https://arxiv.org/abs/2602.09555)

**Authors**: Yi Lu, Deyang Kong, Jianing Wang, Linsen Guo, Xue Wang, Qi Guo, Tao Gui, Xuanjing Huang, Wei Ye, Shikun Zhang, Wei Wang  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.09555v1  

#### Abstract
Recent advances in block diffusion language models have demonstrated competitive performance and strong scalability on reasoning tasks. However, existing BDLMs have limited exploration under the test-time scaling setting and face more severe decoding challenges in long Chain-of-Thought reasoning, pa...

---

### 24. [MATA: Multi-Agent Framework for Reliable and Flexible Table Question Answering](https://arxiv.org/abs/2602.09642)

**Authors**: Sieun Hyeon, Jusang Oh, Sunghwan Steve Cho, Jaeyoung Do  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.09642v1  

#### Abstract
Recent advances in Large Language Models (LLMs) have significantly improved table understanding tasks such as Table Question Answering (TableQA), yet challenges remain in ensuring reliability, scalability, and efficiency, especially in resource-constrained or privacy-sensitive environments. In this ...

---

### 25. [Steer2Edit: From Activation Steering to Component-Level Editing](https://arxiv.org/abs/2602.09870)

**Authors**: Chung-En Sun, Ge Yan, Zimo Wang, Tsui-Wei Weng  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.09870v1  

#### Abstract
Steering methods influence Large Language Model behavior by identifying semantic directions in hidden representations, but are typically realized through inference-time activation interventions that apply a fixed, global modification to the model's internal states. While effective, such intervention...

---

### 26. [CoFEH: LLM-driven Feature Engineering Empowered by Collaborative Bayesian Hyperparameter Optimization](https://arxiv.org/abs/2602.09851)

**Authors**: Beicheng Xu, Keyao Ding, Wei Liu, Yupeng Lu, Bin Cui  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.09851v1  

#### Abstract
Feature Engineering (FE) is pivotal in automated machine learning (AutoML) but remains a bottleneck for traditional methods, which treat it as a black-box search, operating within rigid, predefined search spaces and lacking domain awareness. While Large Language Models (LLMs) offer a promising alter...

---

### 27. [ANCHOR: Branch-Point Data Generation for GUI Agents](https://arxiv.org/abs/2602.07153)

**Authors**: Jinbiao Wei, Yilun Zhao, Kangqi Ni, Arman Cohan  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.07153v1  

#### Abstract
End-to-end GUI agents for real desktop environments require large amounts of high-quality interaction data, yet collecting human demonstrations is expensive and existing synthetic pipelines often suffer from limited task diversity or noisy, goal-drifting trajectories. We present a trajectory expansi...

---

### 28. [MSP-LLM: A Unified Large Language Model Framework for Complete Material Synthesis Planning](https://arxiv.org/abs/2602.07543)

**Authors**: Heewoong Noh, Gyoung S. Na, Namkyeong Lee, Chanyoung Park  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.07543v2  

#### Abstract
Material synthesis planning (MSP) remains a fundamental and underexplored bottleneck in AI-driven materials discovery, as it requires not only identifying suitable precursor materials but also designing coherent sequences of synthesis operations to realize a target material. Although several AI-base...

---

### 29. [Efficient Table Retrieval and Understanding with Multimodal Large Language Models](https://arxiv.org/abs/2602.07642)

**Authors**: Zhuoyan Xu, Haoyang Fang, Boran Han, Bonan Min, Bernie Wang, Cuixiong Hu, Shuai Zhang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.07642v1  

#### Abstract
Tabular data is frequently captured in image form across a wide range of real-world scenarios such as financial reports, handwritten records, and document scans. These visual representations pose unique challenges for machine understanding, as they combine both structural and visual complexities. Wh...

---

### 30. [EventCast: Hybrid Demand Forecasting in E-Commerce with LLM-Based Event Knowledge](https://arxiv.org/abs/2602.07695)

**Authors**: Congcong Hu, Yuang Shi, Fan Huang, Yang Xiang, Zhou Ye, Ming Jin, Shiyu Wang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.07695v1  

#### Abstract
Demand forecasting is a cornerstone of e-commerce operations, directly impacting inventory planning and fulfillment scheduling. However, existing forecasting systems often fail during high-impact periods such as flash sales, holiday campaigns, and sudden policy interventions, where demand patterns s...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
