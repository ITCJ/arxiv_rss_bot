# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-16 06:47:54 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FlashSchNet: Fast and Accurate Coarse-Grained Neural Network Molecular Dynamics](https://arxiv.org/abs/2602.13140)

**Authors**: Pingzhi Li, Hongxuan Li, Zirui Liu, Xingcheng Lin, Tianlong Chen  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.13140v1  

#### Abstract
Graph neural network (GNN) potentials such as SchNet improve the accuracy and transferability of molecular dynamics (MD) simulation by learning many-body interactions, but remain slower than classical force fields due to fragmented kernels and memory-bound pipelines that underutilize GPUs. We show t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*FlashSchNet: Fast and Accurate Coarse-Grained Neural Network Molecular Dynamics*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于 **Graph Neural Network (GNN)** 的分子动力学（MD）势能模型（如 SchNet、CGSchNet）虽然在准确性和可迁移性上优于经典力场（如 MARTINI），但由于其计算流程中存在大量内存访问瓶颈（memory-bound），导致实际模拟速度远慢于经典力场。尤其是在 GPU 上运行时，频繁的中间张量写入高带宽内存（HBM）、碎片化的 kernel 调用以及 scatter-add 操作中的原子冲突严重限制了吞吐量。

该论文指出：**GNN-MD 的瓶颈不是 FLOPs 不足，而是 IO 效率低下** —— 即 GPU HBM 与片上 SRAM 之间的数据移动未被优化。

---

### 提出的新方法与创新思路
作者提出 **FlashSchNet**，一种 **IO-aware** 的高效 SchNet 风格 GNN-MD 框架，通过算法-系统协同设计减少内存流量并提升硬件利用率。其四大核心技术为：

1. **Flash radial basis**  
   将成对距离计算、Gaussian basis 展开和 cosine cutoff 包络融合为一个 **tiled pass**，每个距离只计算一次并在片上复用于所有基函数，避免多次写回 HBM。

2. **Flash message passing**  
   融合 cutoff 掩码、邻居 gather、滤波器乘法和 reduction 操作到单个 kernel 中，**消除边级张量（edge tensors）在 HBM 中的物化（materialization）**。

3. **Flash aggregation**  
   将传统的 scatter-add 聚合重构为基于 **CSR（Compressed Sparse Row）的分段 reduce（segmented reduce）**，将原子更新次数从 $O(E \times D)$ 降至 $O(N \times D)$，实现无竞争（contention-free）累积。

4. **Channel-wise 16-bit quantization (W16A16)**  
   利用 SchNet 中 MLP 权重通道间动态范围低的特点，采用逐通道量化策略，在几乎不损失精度的前提下加速 MLP 计算并降低内存带宽需求。

---

### 相比现有方法的优势
- **显著提速**：相比 CGSchNet 基线实现 **6.5× 的端到端加速**。
- **大幅降耗**：峰值内存占用减少 **80%**（例如从 92GB → 18GB）。
- **超越经典力场**：首次使 SchNet 类 GNN 势能达到甚至超过经典粗粒化力场（如 MARTINI）的速度（达 1000 ns/day），同时保持 MLFF 的高精度与可迁移性。
- **支持更大规模模拟**：更低内存使得可在单卡上运行更多并行副本（replicas），利于增强采样等任务。

---

## 2. 核心实验方法和设置

### 数据集
使用五个快速折叠蛋白作为测试体系，均采用 **coarse-grained (CG)** 表示（每个氨基酸残基用一个 bead 表示）：
- Chignolin (CLN, 10 residues)
- TRPcage (2JOF, 20 residues)
- Homeodomain (1ENH, 54 residues, 含 269 beads)
- Villin (1YRF, 35 residues)
- Alpha3D (2A3D, 73 residues)

这些体系来自 Charron et al. (2025) 的基准套件。

---

### 实验设置
- **硬件平台**：单块 NVIDIA RTX PRO 6000 GPU
- **模拟参数**：
  - Langevin 动力学，温度 300K
  - 时间步长 4 fs
  - 并行运行 64 个 replicas
- **评估周期**：每 MD 步测量 throughput 和 peak memory usage

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **Throughput** | `timestep·mol/s`，即每秒完成的模拟步数 × 分子数量（跨所有 replicas 总和） |
| **Peak Memory** | GPU 最大显存占用（GB） |
| **Structural Fidelity** | 使用以下三个指标验证物理保真度：<br>• **Ca RMSD**：主链碳原子与参考结构的均方根偏差<br>• **Q (Fraction of Native Contacts)**：原生接触比例<br>• **GDT-TS**：全局距离测试总分，衡量拓扑相似性 |

---

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **CGSchNet (FP32)** | MLFF 基线 | 当前最先进的 coarse-grained GNN 势能模型，作为性能与精度基准 |
| **MARTINI** | 经典力场 | 广泛使用的 coarse-grained 经验力场，速度快但近似程度高 |
| **All-Atom Simulation** | 参考标准 | 原子级模拟结果用于评估结构准确性 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 1ENH 蛋白为例）
| 指标 | FlashSchNet | CGSchNet | MARTINI |
|------|------------|----------|---------|
| **Throughput** | ~3000 timestep·mol/s (**≈1000 ns/day**) | ~477 timestep·mol/s | ~2250 timestep·mol/s |
| **Speedup vs CGSchNet** | **6.5×** | — | — |
| **Peak Memory** | **18.0 GB** | 92.5 GB | 34.9 GB |
| **Memory Reduction** | **>80%** | — | — |

> ✅ FlashSchNet 在速度上不仅大幅超越 CGSchNet，还 **首次超过了 MARTINI**，实现了“兼具高速与高精度”的突破。

---

### 与其他系统的比较（见 Table 3）
| Protein | FlashSchNet Speed (timestep·mol/s) | CGSchNet Speed | Speedup |
|--------|------------------------------------|----------------|---------|
| Chignolin | 5222 | 3578 | ~1.5× |
| TRPcage | 4938 | 1729 | ~2.8× |
| Villin | 3912 | 1056 | ~3.7× |
| Alpha3D | 2610 | 288 | **~9×** |

> ⚡️ 更复杂的系统中加速比更高，表明 FlashSchNet 在大规模场景下优势更明显。

---

### 消融实验结果（Ablation Studies）

#### （1）对动态图拓扑的鲁棒性
- 在 1ENH 的拉伸模拟中，随着蛋白质展开，邻接图变得稠密且非对角化。
- **CGSchNet 吞吐量随图密度上升而显著下降**（scatter contention 加剧）。
- **FlashSchNet 保持稳定吞吐量**，得益于 CSR 分段 reduce 的无竞争特性。
- 批大小越大（64 replicas），差距越明显（最高达 6.5×）。

#### （2）批大小扩展能力
- FlashSchNet 可扩展至 **3–10× 更大的 batch size** 才出现 OOM（Out-of-Memory）。
  - 如 Chignolin 支持高达 2048 replicas，Alpha3D 达 256 replicas。
- CGSchNet 在较小 batch 下即因显存不足失败。
- 显存节省主要来自：**消除 B、W、M 等边级中间张量的 HBM 存储**。

#### （3）量化影响
- 采用 **channel-wise W16A16** 后，MLP 计算映射至 Tensor Cores，吞吐进一步提升。
- 精度分析显示：**GDT-TS 与 Largest Q 与 FP32 基线差异极小（<0.04）**，证明量化几乎无损。

---

## 4. 关键结论和发现

### 主要发现
1. **GNN-MD 的性能瓶颈本质是 IO 而非计算**：尽管名义 FLOPs 不高，但频繁的 HBM 访问使其成为强 memory-bound 应用。
2. **IO-aware 设计可带来数量级改进**：通过融合 kernel、消除中间存储、重构聚合方式，可在不牺牲精度的情况下大幅提升效率。
3. **FlashSchNet 是首个在 wall-clock efficiency 上超越经典力场的 SchNet 风格模型**，实现了“准确 ≠ 慢”的范式转变。
4. **低通道动态范围适合轻量级量化**：SchNet 中的 filter MLP 天然具备通道稀疏性，支持高效 W16A16 而不影响输出质量。

---

### 方法的局限性
- 目前仅针对 **coarse-grained** 蛋白质系统进行优化，尚未验证于全原子（all-atom）或复杂溶剂环境。
- 依赖定制 CUDA kernel 实现，目前需特定工程投入，通用性受限于框架支持（如 PyTorch 对 CSR aggregation 支持较弱）。
- bucket sort 构建 grouped layout 引入额外开销，虽总体可控，但在极短时间步下可能影响效率。

---

### 未来工作方向
- 将 FlashSchNet 思路推广至其他 GNN 架构（如 NequIP、MACE）及全原子模拟。
- 开发自动代码生成工具，将 IO-aware 模式封装进主流 ML 框架（如 PyTorch Geometric、JAX-MD）。
- 结合更高效的 neighbor list 更新机制，进一步降低动态图重构成本。
- 探索在多 GPU 场景下的分布式扩展方案，支持超大体系模拟。

---

> 🔗 **开源地址**：[https://github.com/UNITES-Lab/flash-molecular-dynamics](https://github.com/UNITES-Lab/flash-molecular-dynamics)

</details>

---

### 2. [Synthetic Interaction Data for Scalable Personalization in Large Language Models](https://arxiv.org/abs/2602.12394)

**Authors**: Yuchen Ma, Yue Huang, Wenjie Wang, Xiaonan Luo, Xiangliang Zhang, Stefan Feuerriegel  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.12394v1  

#### Abstract
Personalized prompting offers large opportunities for deploying large language models (LLMs) to diverse users, yet existing prompt optimization methods primarily focus on task-level optimization while largely overlooking user-specific preferences and latent constraints of individual users. This gap ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前大型语言模型（LLMs）在个性化对齐（personalized alignment）方面面临两大瓶颈：
1. **数据缺口（Data Gap）**：缺乏高质量、隐私安全的多轮用户-LLM交互数据，难以捕捉真实用户的动态偏好。
2. **方法局限（Method Limitation）**：现有方法多依赖静态偏好建模、模型参数微调或封闭源模型不可访问的问题，限制了可扩展性和实用性。

### **提出了什么新方法或新思路**
本文提出两个核心组件，系统性解决上述问题：

#### **① PERSONAGYM：高保真合成数据生成框架**
- 将个性化建模为**动态过程**而非静态角色设定（static persona），通过一个基于LLM的**代理系统**（agentic LLM system）模拟真实用户行为。
- 包含三个LLM代理：
  - **User Agent**：模拟用户提问与反馈。
  - **Assistant Agent**：模拟LLM响应。
  - **Distractor Agent**：注入语义感知噪声（semantic-aware noise），增强现实性。
- 支持**部分可观测性**（partial observability）和**多轮交互轨迹生成**，更贴近真实场景。

#### **② PERSONAATLAS 数据集**
- 基于 PERSONAGYM 生成的大规模、高质量、多样化的**多轮个性化交互数据集**。
- 包含约 2,000 个合成 persona 和超过 10,000 场对话，覆盖多种任务领域（如写作、编码、规划等）。

#### **③ PPOpt：可扩展的个性化提示优化框架**
- 提出 **Personalized Prompt Optimization (PPOpt)**，一种**模型无关**（model-agnostic）、无需修改部署模型参数的黑盒提示优化方法。
- 采用 **reason-then-optimize 范式**：
  1. 先从历史交互中推理出显式的用户画像（user profile）；
  2. 再基于该画像重写当前提示（prompt rewriting）。
- 避免“奖励黑客”（reward hacking）和捷径学习（shortcut learning）。

### **相比现有方法的优势**
| 维度 | PPOpt (本文) | 现有方法（如 PAD, CoPe, LaMP 等） |
|------|-------------|-------------------------------|
| **干预层级** | Prompt-level（非侵入式） | Memory/Parameter/Decoding-level（需访问内部状态） |
| **闭源兼容性** | ✅ 支持 | ❌ 多数不支持 |
| **数据可扩展性** | ✅ 高（基于合成数据） | ❌ 依赖真实用户数据，稀缺且敏感 |
| **噪声建模** | ✅ 显式建模语义噪声 | ⚠️ 多忽略或简化 |
| **交互动态性** | ✅ 多轮动态偏好演化 | ⚠️ 多为单轮或静态偏好 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
#### **合成训练与测试数据**
- **PERSONAATLAS**：本文提出的合成数据集，用于训练 PPOpt。
- 构建方式：
  - 种子查询来自多个公开数据集混合：
    - `ultrachat_200k`, `OpenR1-Math-220k`（多轮对话）
    - `CodeFeedback`, `Mol-Instructions`（代码与科学）
    - `MMLU`, `ai2_arc`（常识问答）
    - `Alpaca-cleaned`, `TruthfulQA` 等
- **真实世界测试集**：通过人类专家编辑合成轨迹构建，确保高保真度。

### **实验设置和评估指标**
#### **评估方式**
- 在两种环境下进行评估：
  1. 合成基准（synthetic benchmarks）
  2. 真实世界测试集（real-world test set）

#### **评估指标（由 LLM-as-a-Judge 判断）**
| 指标 | 定义 |
|------|------|
| **Personalization Score** | 衡量输出是否符合用户偏好（语气、格式、风格等），范围 1–10 |
| **Task Completion Score** | 衡量任务完成质量（正确性、完整性），范围 1–10 |

#### **部署模型**
- 使用多个主流 LLM 进行测试，包括：
  - `Llama-3-8b-instruct`, `Qwen3-8b`, `GPT-oss-20b`
  - 部署时模型冻结，仅优化输入 prompt。

### **基线方法对比**
| 基线方法 | 类型 | 描述 |
|--------|------|------|
| **Vanilla Prompt** | 基线 | 原始用户查询，无任何优化 |
| **History-augmented Prompting (Concat)** | 上下文拼接 | 将历史对话直接拼接到当前查询 |
| **Persona-based Query Rewriting** | 角色重写 | 从历史推断 persona 并附加到查询 |
| **Preference-based Few-shot ICL** | 少样本示例 | 构造偏好相关的 in-context 示例 |
| **Controller-guided Prompting** | 控制器引导 | 使用辅助控制器模型改写查询 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表6 & 表9：综合性能表现**
| 方法 | Personalization Score ↑ | Task Completion Score ↑ |
|------|--------------------------|-------------------------|
| **Vanilla** | 5.41 | 8.48 |
| **PPOpt (ours)** | **7.20** (+1.79, +33.09%) | **8.26** (-0.22, -2.59%) |

> 注：结果基于 `Llama-3.3-70b-instruct` 部署模型。

#### **真实世界测试集表现（Table 9）**
| 方法 | Personalization | Task Completion |
|------|------------------|------------------|
| Vanilla | 5.49 | 8.09 |
| PPOpt | **7.20** (+1.71) | 8.05 (-0.04) |

✅ **结论**：PPOpt 在真实场景下仍显著提升个性化质量，且几乎不影响任务完成能力。

### **与基线方法的对比结果（Table 7）**
| 方法 | Personalization Gain | Task Completion Drop |
|------|-----------------------|------------------------|
| History-augmented | +1.53 | -0.59 |
| Persona-based Rewriting | +0.81 | -1.06 |
| Preference-based ICL | +1.45 | -0.54 |
| Controller-guided | +1.26 | -0.54 |
| **PPOpt (ours)** | **+1.79** | **-0.22** |

📌 **优势明显**：PPOpt 不仅带来最大个性化增益，还最小化了对任务性能的负面影响。

### **消融实验结果（Figure 3）**
比较不同训练阶段的影响：
| 设置 | Personalization Score |
|------|------------------------|
| SFT only | ~6.7–7.0 |
| RL w/o Profile Inference Reward | 不稳定，部分下降 |
| **Full PPOpt (SFT + RL + Profile Reward)** | **~7.2–7.35** ✅ |

🔍 **发现**：
- SFT 提供良好冷启动（cold-start prior）。
- 加入 profile inference reward 对稳定强化学习至关重要，防止优化偏离真实偏好。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **合成数据可用于高质量个性化训练**：PERSONAGYM 能生成足够逼真的多轮交互数据，支撑下游个性化优化。
2. ✅ **reason-then-optimize 范式有效**：先推理用户画像再优化提示，能避免 reward hacking，提高鲁棒性。
3. ✅ **PPOpt 显著优于现有方法**：在 personalization 上平均提升 **33%**，同时保持 task performance 几乎不变。
4. ✅ **跨模型泛化能力强**：无论使用何种 optimizer 或部署模型，PPOpt 均一致有效。
5. ✅ **对稀疏和噪声信号鲁棒**：即使用户反馈模糊、不一致或缺失，PPOpt 仍能恢复潜在偏好。

### **方法的局限性**
1. 🚫 **依赖高质量合成流程**：若 PERSONAGYM 的代理模拟失真，可能导致错误偏好传播。
2. 🚫 **无法处理极端分布外用户**：对于完全未见的 persona 类型，泛化能力受限。
3. 🚫 **计算成本较高**：生成 PERSONAATLAS 单条对话平均消耗约 17.7k tokens（以 GPT-5.2 计约为 \$0.25）。
4. 🚫 **仍为 offline 优化**：未实现在线持续学习（online adaptation）。

### **未来工作方向**
1. 🔮 **引入在线自适应机制**：结合 online feedback 实现持续更新用户画像。
2. 🔁 **降低合成成本**：探索轻量化代理或蒸馏策略减少 token 开销。
3. 🧠 **融合记忆机制**：将长期用户记忆整合进 prompt optimizer。
4. 🌐 **跨平台迁移**：研究在不同 LLM 间共享 prompt optimizer 的可行性。
5. 🔐 **加强隐私保障**：进一步研究如何在合成中消除身份可识别信息（PII）风险。

---

> **总结一句话**：  
> 本论文通过 **PERSONAGYM + PERSONAATLAS + PPOpt** 三位一体方案，首次实现了**基于高保真合成数据的可扩展、模型无关、多轮动态个性化提示优化**，为 LLM 个性化落地提供了实用且高效的路径。

</details>

---

### 3. [Scaling Web Agent Training through Automatic Data Generation and Fine-grained Evaluation](https://arxiv.org/abs/2602.12544)

**Authors**: Lajanugen Logeswaran, Jaekyeom Kim, Sungryull Sohn, Creighton Glasscock, Honglak Lee  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.12544v1  

#### Abstract
We present a scalable pipeline for automatically generating high-quality training data for web agents. In particular, a major challenge in identifying high-quality training instances is trajectory evaluation - quantifying how much progress was made towards task completion. We introduce a novel const...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scaling Web Agent Training through Automatic Data Generation and Fine-grained Evaluation

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题
当前 **Web Agent** 的训练面临两大挑战：
- **高质量训练数据稀缺**：真实世界中复杂的网页交互轨迹难以大规模获取。
- **轨迹评估困难**：传统评估方式（如 LLM-as-Judge）在多约束、长周期任务中表现不可靠，无法准确衡量部分成功（partial success）的轨迹价值。

这些问题限制了小模型（small language models）在复杂 Web 任务上的能力提升。

---

### 🚀 提出的新方法与创新思路

#### （1）**自动化的高质量轨迹生成与蒸馏流水线（Automatic Data Generation Pipeline）**
- 利用公开的 **LLM（如 LLaMA 3.1 405B）作为 Teacher Agent**，通过 few-shot prompting 在真实网站上自动生成大量 agent 轨迹。
- 支持复杂现实任务（如旅行预订），涵盖多个网站和多样化任务类型。

#### （2）**基于约束的细粒度评估框架（Constraint-Based Evaluation Framework）**
- 将任务拆解为一组明确的 **constraints**（如 `location`, `start_date`, `ski-in/ski-out access` 等）。
- 引入 **Constraint Satisfaction Rate (CSR)**：对每个 constraint 进行二元判断（是否满足），并取平均值作为评估分数。
- 定义 **Task Success Rate (SR)**：仅当所有 constraints 都满足时才记为成功（即 CSR=1）。

> 💡 优势：相比传统的“全有或全无”SR 指标，CSR 提供更细粒度、可解释、鲁棒的反馈信号。

#### （3）**利用部分成功轨迹进行数据增强**
- 即使最终未完成任务，只要某一步达到了最高 CSR，就提取该前缀作为有效训练样本。
- 结合 **hindsight re-labeling** 技术，将部分成功的任务重新定义为其实际达成的目标，从而转化为合法的训练实例。

#### （4）提出新基准 **BookingArena**
- 包含来自 **20 个流行预订类网站** 的 **120 个复杂结构化任务**（每站 6 个任务）。
- 按难度分为 easy / medium / hard，平均约束数分别为 3.95 / 5.45 / 7.30。
- 所有任务均可在线执行，并附带脚本自动更新过期日期以保持有效性。

---

### 🔍 相比现有方法的优势

| 方面 | 本文方法 | 传统方法 |
|------|--------|---------|
| 数据生成 | 全自动、可扩展、覆盖真实网站 | 依赖人工标注或静态数据集 |
| 轨迹评估 | 细粒度、客观、支持 partial progress | 二值化、主观性强、易受幻觉影响 |
| 可用数据量 | 显著增加（利用部分成功轨迹） | 仅限完全成功轨迹（稀少） |
| 模型规模 | 成功训练 **24B 参数的小模型** | 多依赖超大闭源模型（如 405B） |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

#### （1）**BookingArena（本文提出的新基准）**
- **20 个真实预订网站**（见 Figure 9）：包括 `booking.com`, `airbnb.com`, `google flights`, `vrbo.com` 等。
- **120 个任务**，分布于不同难度等级。
- 强调 **multi-constraint**, **long-horizon**, **real-world usability**。

#### （2）**WebVoyager（外部对比基准）**
- 来自 15 个网站的 643 个任务，偏重搜索类操作。
- 排除 55 个已失效任务，其余时间相关任务被自动更新至未来日期。

#### （3）自建训练数据集
- 使用 **LLaMA 3 70B 和 LLaMA 3.1 405B Instruct** 生成约 **150k 条轨迹**（共 ~1M 步骤）。
- 经过约束评估后保留：
  - ~16k 完全成功轨迹（SR=1）
  - ~65k 部分可用轨迹 → 提取为 **300k 动作步骤** 的高质量训练集。

---

### ⚙️ 实验设置

#### 模型架构
- **Student Model**: **Mistral 3 Small 24B**（参数量远小于 teacher）
- **Fine-tuning 方法**: 使用 **LoRA** 微调 `q_proj` 和 `v_proj` 层
- 学习率：1e-4，cosine scheduler，batch size=16

#### 自动评估流程
1. 使用 **LLaMA 3.3 70B** 和 **Gemma 3 27B** 作为 evaluator 进行 constraint-based 评分
2. 对两个 evaluator 输出取交集以减少噪声
3. 最终模型评估使用 **GPT-4o（视觉输入）**，避免数据构建偏差

#### 评估指标
| 指标 | 定义 |
|------|------|
| **SR (Task Success Rate)** | 所有 constraints 均满足的比例 |
| **CSR (Constraint Satisfaction Rate)** | 平均每个 constraint 被满足的比例（更细粒度） |

---

### 🆚 基线方法对比

| 基线模型 | 类型 | 特点 |
|--------|-----|------|
| **UI-TARS** | 开源 VLM Agent | 基于 GUI 的原生交互 |
| **Claude Computer Use (Anthropic)** | 商业闭源系统 | Pixel-based 控制 |
| **Operator (OpenAI)** | 商业闭源系统 | 高级自动化代理 |
| **Browser Use** | 开源 Set-of-Marks Agent | 数字标记可点击元素 |
| **Qwen 2.5 72B / LLaMA few-shot** | 大模型零样本 | 不经微调直接推理 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（BookingArena 上的结果）

| 方法 | Avg. SR (%) | Avg. CSR (%) |
|------|-------------|--------------|
| **Ours (Mistral 24B)** | **29.9** | **60.2** |
| Operator | 33.0 | **68.7** |
| UI-TARS | 26.7 | 53.6 |
| Claude CU | 16.8 | 48.7 |
| Browser Use | 18.8 | 45.3 |

> ✅ **我们的 24B 模型在 SR 上优于除 Operator 外的所有基线，在 CSR 上仅次于 Operator**

#### 🏆 网站级别亮点表现：
- **Booking.com**: 我们的 CSR 达 **89.2%**（最高）
- **Vrbo**: SR=66.7%, CSR=88.4%
- **Orbitz**: SR=80.0%, CSR=80.0%

> 💬 尽管整体 SR 不高（因任务极难），但 CSR 表明我们能显著推进任务进度。

---

### 🔬 消融实验结果（Ablation Studies）

| 模型配置 | SR (%) | CSR (%) | 说明 |
|--------|-------|--------|------|
| Mistral 24B (success only) | 25.0 | 55.0 | 仅用完全成功轨迹 |
| Mistral 24B (all trajectories) | 29.4 | 57.2 | 包含失败轨迹 |
| **Mistral 24B (partial success)** | **29.9** | **60.2** | ✅ 最佳策略 —— 利用部分成功轨迹 |
| Mistral 24B (full finetune) | – | 58.7 | LoRA 更优，full finetune 易过拟合 |
| LLaMA 405B (few-shot) | 22.5 | 52.4 | 更大的 teacher 模型仍不如蒸馏后的小模型 |

> ✅ **关键发现**：  
> - **学习部分成功轨迹 > 仅学习完全成功轨迹**
> - **LoRA > Full Finetuning**：更适合此场景下的知识迁移
> - **小模型经高质量数据蒸馏后可超越更大 teacher 模型**

---

### 🌐 WebVoyager 上的表现

| 方法 | Overall SR (%) |
|------|----------------|
| Wilbur (Lutz et al.) | 52.6 |
| GPT-4V (reported) | 57.1 |
| **Ours (Mistral 24B)** | **64.5** |

> ✅ 在多个领域大幅领先，尤其在 **Booking (+11.1%)** 和 **Google Flights (+15.1%)** 上表现突出。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **细粒度评估（CSR）是推动 Web Agent 发展的关键**
   - 提供比 SR 更丰富、可靠的任务进展信号。
   - 支持识别和利用 **partial trajectories**，极大扩展可用训练数据。

2. **高质量数据蒸馏能让小模型超越大模型**
   - 我们的 **24B Mistral 模型** 在多数指标上超过 **405B LLaMA teacher** 和多个商业系统（如 Claude、Browser Use）。

3. **BookingArena 是一个更具挑战性和现实意义的测试平台**
   - 揭示了当前主流方法在复杂多约束任务中的局限性。
   - 有望成为未来 Web Agent 研究的标准 benchmark。

4. **URL 是比 action history 更可靠的评估依据**
   - 实验证明提供 action history 会导致 evaluator “幻觉”，误判动作执行效果。
   - 而 URL 中编码的信息（如日期、地点）能提供客观证据。

---

### ⚠️ 方法的局限性

1. **依赖 LLM 提取 constraints 的准确性**
   - 若 constraint extraction 出错，会影响后续评估质量。
   - 当前未对 constraint 提取过程做误差分析。

2. **尚未支持 multimodal observations**
   - 当前 pipeline 主要基于 accessibility tree 和文本 URL，未充分利用图像语义。

3. **局限于英文网站和预订类任务**
   - 泛化到其他语言、平台（如移动端 App）、任务类型（如社交网络操作）尚需拓展。

4. **evaluation 仍依赖 LLM/VLM judge**
   - 虽然设计了更系统的框架，但仍未完全摆脱对 black-box judge 的依赖。

---

### 🔮 未来工作方向

1. **扩展至多模态观察空间**
   - 整合 screenshot、DOM、语音等多源信息进行联合建模。

2. **构建通用 constraint extractor**
   - 设计专用模块自动、准确地从自然语言任务描述中抽取 structured constraints。

3. **应用于更多领域和平台**
   - 如电商购物、银行服务、政府事务办理等 real-world workflows。

4. **探索 online learning 机制**
   - 结合用户反馈持续优化 agent 行为与评估标准。

5. **开源数据集与 benchmark**
   - 推动社区共建开放、透明、可复现的 Web Agent 研究生态。

---

> 📌 **一句话总结**：  
> 本文通过 **自动数据生成 + 基于 constraint 的细粒度评估**，实现了高效训练高性能小规模 Web Agent，在新提出的 **BookingArena** 和现有 **WebVoyager** 基准上均取得 SOTA 表现，证明了“好数据 + 好评估”比“大模型 + 黑盒判断”更具潜力。

</details>

---

### 4. [Think Fast and Slow: Step-Level Cognitive Depth Adaptation for LLM Agents](https://arxiv.org/abs/2602.12662)

**Authors**: Ruihan Yang, Fanghua Ye, Xiang We, Ruoqing Zhao, Kang Luo, Xinbo Xu, Bo Zhao, Ruotian Ma, Shanyi Wang, Zhaopeng Tu, Xiaolong Li, Deqing Yang,  Linus  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.12662v1  

#### Abstract
Large language models (LLMs) are increasingly deployed as autonomous agents for multi-turn decision-making tasks. However, current agents typically rely on fixed cognitive patterns: non-thinking models generate immediate responses, while thinking models engage in deep reasoning uniformly. This rigid...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Think Fast and Slow: Step-Level Cognitive Depth Adaptation for LLM Agents**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
当前的 LLM Agents 在执行多轮决策任务时通常采用**固定的认知模式**（fixed cognitive patterns）：
- **非思考模型**（non-thinking models）在每一步都进行直觉反应，缺乏深度推理能力；
- **思考模型**（thinking models）则在整个轨迹中始终进行深度链式推理（如 DeepSeek-R1），导致在简单步骤上浪费大量计算资源。

这种“认知僵化”（cognitive rigidity）在长视野（long-horizon）、异构步级复杂度的任务中效率低下。例如，在探索房间后只需“go to bathroom”的例行操作却仍进行复杂规划，造成**token 浪费与响应延迟**。

---

### **提出了什么新方法或新思路**
本文提出 **CoGROUTER** 框架，实现 LLM Agent 在每一步动态调整其**认知深度**（cognitive depth），灵感来源于人类认知理论 **ACT-R**（Adaptive Control of Thought-Rational）。

#### **核心设计**
- 定义四个层级的认知水平（Cognitive Levels）：
  - **L1: Instinctive Response**（直觉反应）——无显式推理，直接输出动作。
  - **L2: Situational Awareness**（情境感知）——分析当前状态与可用动作。
  - **L3: Experience Integration**（经验整合）——回顾历史行为并反思。
  - **L4: Strategic Planning**（战略规划）——模拟多个候选动作的未来影响。

- 两阶段训练框架：
  1. **Cognition-aware Supervised Fine-tuning (CoSFT)**  
     构建平衡标注数据集，使模型掌握各层级稳定的行为模式，防止后续 RL 中的格式崩溃（format collapse）。
  2. **Cognition-aware Policy Optimization (CoPO)**  
     新型强化学习算法，通过**置信度感知优势重加权**（confidence-aware advantage reweighting）实现细粒度的步级信用分配。

#### **关键洞察**
> “合适的认知深度应最大化动作预测的置信度。”  
即：当一个认知层级能更自信地引导出正确动作时，它就是该步更优的选择。

---

### **相比现有方法的优势**
| 维度 | CoGROUTER vs. 现有方法 |
|------|------------------------|
| **灵活性** | 超越二元切换（如 AdaptThink），支持四层连续适应 |
| **效率** | 显著减少 token 使用量（最高节省 62%） |
| **性能** | 达到 SOTA 成功率，远超 GPT-4o、GRPO 等前沿模型 |
| **机制创新** | 首次实现基于**动作预测置信度**的步级信用分配 |

---

## 2. **核心实验方法和设置**

### **使用的数据集**
- **ALFWorld**：基于 TextWorld 的具身环境，包含六类家庭任务（Pick & Place, Clean & Place 等），最大步数为 30。
- **ScienceWorld**：面向小学科学课程的文本环境，涵盖 30 种科学推理任务（如使用温度计测量物质）。按 oracle 路径长度分为短（S）、中（M）、长（L）三类任务。

---

### **实验设置和评估指标**

| 设置项 | 描述 |
|-------|------|
| **Base Models** | `Qwen2.5-7B` 和 `Llama3.1-8B` |
| **训练方式** | 两阶段：<br>1. CoSFT（监督微调）<br>2. CoPO（强化学习优化） |
| **评估方式** | 在测试集上运行 200 次模拟 |
| **评估指标** | - **Success Rate (SR)**（成功率）<br>- **Average Score**（仅 ScienceWorld）<br>- **#Tokens**（平均每任务输出 token 数，衡量效率） |

---

### **基线方法对比**

#### **Prompt-based 方法**
- **ReAct**：标准的“Thought: ... Action: ...”格式。
- **Reflexion**：失败后自我反思并改进策略。

#### **Training-based 方法**
- **SFT / ETO**：离线监督训练。
- **GRPO / GiGPO**：基于组的强化学习，不区分步级认知需求。
- **AdaptThink**：二元思维开关（think vs. non-think）。

#### **前沿大模型**
- GPT-4o, DeepSeek-R1, OpenAI-o3, Gemini-2.5-Pro 等。

---

## 3. **主要实验结果和性能指标**

### **关键性能数据（以 Qwen2.5-7B 为例）**

| 方法 | 平均成功率 (SR%) | 平均 Token 数 |
|------|------------------|---------------|
| **CoGROUTER (Ours)** | **82.3%** | **1641.4** |
| GPT-4o | 42.0% | 935.4 |
| OpenAI-o3 | 64.0% | 4737.5 |
| GRPO | 68.3% | 4367.3 |
| GiGPO | 67.5% | 3779.2 |
| AdaptThink | 63.0% | 1343.2 |

> ✅ **CoGROUTER 比 GPT-4o 高 +40.3%，比 GRPO 高 +14.0% 成功率，同时节省 62% token。**

---

### **与基线方法的对比结果**

#### **固定模式 vs. 自适应**
- **L1-only 模型**：成功率为 76.5%，极高效但无法处理复杂步骤。
- **L4-only 模型**：成功率为 86.5%，但消耗高达 4640.98 tokens（是 CoGROUTER 的 2.7 倍）。
- **FreeForm（无结构推理）**：81.5% SR @ 4068.88 tokens，说明默认倾向深思。

> ➡️ 结论：单一认知模式无法兼顾性能与效率，**自适应才是最优解**。

#### **RL 方法比较（图 3 & 图 4）**
- **GRPO/GiGPO**：快速坍缩至 L4 占主导（>75% 步骤使用 L4），即使在简单步骤也过度思考。
- **CoPO**：保持多样化分布：
  - ALFWorld 上：L1 占 55.0%，L4 仅占 12.3%
  - ScienceWorld 上：随任务进展，L1 从 48.5% 升至 94.8%

> ✅ CoPO 实现真正的**情境感知式认知调度**。

#### **收敛速度**
- CoPO 在 **100 步内达到 90% 成功率**（ALFWorld），而 GRPO 在 150 步后仅达 83.3%。
- 收敛更快，得益于更精细的步级信用信号。

---

### **消融实验结果（Ablation Study）**

| 变体 | SR (%) | #Tokens | 分析 |
|------|--------|---------|------|
| **CoPO (Ours)** | 82.3 | 1641.4 | 基准 |
| w/ Max Probs | 79.5 | 2167.5 | 局部峰值误导，性能下降 |
| w/ Min Probs | 67.5 | 2063.6 | 单个低概率 token 主导，不稳定 |
| w/ Entropy | 70.0 | 1873.9 | 衡量整体不确定性，不反映动作一致性 |
| w/o Cold Start | 75.3 | 3123.4 | 缺乏 SFT 初始化，格式崩溃严重 |
| w/ CoSFTexpert | 74.5 | 3627.2 | 继承专家偏好（偏爱 L3），丧失适应性 |
| w/ Corr & Incorr | 74.5 | 1565.7 | 对失败轨迹也加权，诱导模型规避信心 |

> 🔍 **验证了两个关键设计：**
> 1. 使用平均对数概率作为置信度指标最有效；
> 2. 必须从平衡初始化开始，并只对成功轨迹进行重加权。

---

## 4. **关键结论和发现**

### **主要发现**
1. **认知僵化是当前 Agent 的根本瓶颈**  
   固定思考或非思考模式均无法应对任务内部的步级异质性。

2. **人类启发的认知分层是可行路径**  
   基于 ACT-R 理论划分 L1–L4 层级，符合任务演进规律：
   - 初始阶段多用 L4（战略规划）
   - 中期探索用 L2/L3（情境感知 + 经验整合）
   - 后期执行回归 L1（本能反应）

3. **CoPO 实现了高效的步级信用分配**  
   通过“哪个认知层级让我更确信做出正确动作？”这一原则，避免了传统 RL 方法向 L4 的坍缩。

4. **自适应显著提升性能与效率**  
   不仅超越所有 baseline，还实现了**更高性能 + 更少资源消耗**的帕累托优势。

---

### **方法的局限性**
- **依赖结构化输出格式**：必须使用 `<level>`, `<think>`, `<action>` 标签，可能限制泛化。
- **CoSFT 数据构建成本高**：需人工标注或 GPT-4o 生成带层级标签的轨迹。
- **仅适用于稀疏奖励任务**：密集奖励场景下优势可能减弱。
- **未探索更多层级或连续空间**：目前为离散四级，未来可尝试软路由或神经控制器。

---

### **未来工作方向**
1. **扩展认知层级数量与类型**  
   探索更细粒度或任务特定的认知模块。
   
2. **实现端到端的认知路由机制**  
   替代显式标签，让模型自主决定何时进入何种思维模式。

3. **应用于真实世界交互任务**  
   如机器人控制、自动驾驶、软件工程代理等。

4. **研究认知演化机制**  
   允许模型在训练过程中**自动生成新的认知层级**，形成“自我进化”的智能体。

5. **结合记忆系统与元认知监控**  
   引入“我是否应该换一种思维方式？”的元级别判断。

---

> 📌 **一句话总结**：  
> **CoGROUTER 是首个实现“快慢双系统”协同的 LLM Agent 框架，让模型学会“什么时候该深思，什么时候该直觉”，在性能与效率之间取得前所未有的平衡。**

</details>

---

### 5. [Multi-Dimensional Visual Data Recovery: Scale-Aware Tensor Modeling and Accelerated Randomized Computation](https://arxiv.org/abs/2602.12982)

**Authors**: Wenjin Qin, Hailin Wang, Jiangjun Peng, Jianjun Wang, Tingwen Huang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.12982v1  

#### Abstract
The recently proposed fully-connected tensor network (FCTN) decomposition has demonstrated significant advantages in correlation characterization and transpositional invariance, and has achieved notable achievements in multi-dimensional data processing and analysis. However, existing multi-dimension...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multi-Dimensional Visual Data Recovery: Scale-Aware Tensor Modeling and Accelerated Randomized Computation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**大规模多维视觉数据恢复**中存在的两个核心挑战：
- **建模能力不足**：现有基于 FCTN 分解的方法难以同时有效捕捉数据的全局低秩性（Global Low-Rankness, L）和局部平滑性（Local Smoothness, S）。
- **计算效率低下**：处理高阶大尺度张量时，传统 FCTN 方法面临高昂的 SVD 和 ALS 计算开销，难以扩展到实际应用。

### 提出的新方法与创新思路
1. **FCTN-GNTCTV 正则化器（FCTN-based Generalized Nonconvex Tensor Correlated Total Variation）**
   - 在 FCTN 分解框架下，提出一种新型非凸梯度域正则化方法，通过在梯度张量上施加 FCTN 核范数，**联合编码全局低秩性和局部连续性**。
   - 引入**非凸函数**（如 MCP、SCAD 等）增强对低秩结构的诱导能力。

2. **鲁棒可扩展的数据恢复模型**
   - 构建了从**非量化观测到粗粒度量化观测**的统一建模框架，提升模型在真实系统中的适用性（考虑存储、带宽限制下的量化影响）。

3. **加速的随机压缩算法（Accelerated Randomized Compression）**
   - 基于数值线性代数中的 sketching 技术，设计了两种高效的随机压缩算法：
     - **Fixed-Rank Randomized Algorithm**：给定目标秩进行快速压缩。
     - **Fixed-Accuracy Randomized Algorithm**：自适应确定秩以满足精度要求。
   - 这些算法作为整个恢复框架的**计算加速核心**，显著降低高阶张量处理的复杂度。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **建模能力** | 同时建模 L+S 结构，优于仅依赖低秩性的方法（如 TRNNM、HTNN）或仅平滑性方法。 |
| **计算效率** | 随机压缩策略使运行时间平均减少约 **88%**，相比确定性版本实现 **9× 平均加速**，个别案例达 **20× 加速**。 |
| **恢复精度** | 在极低采样率（如 0.1%）下仍能保持良好视觉质量，定量指标（MPSNR、MSSIM）显著优于多数基线方法。 |
| **泛化能力** | 支持量化观测场景，在不同噪声水平和量化分辨率下表现稳健。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖多种真实世界的大规模多维视觉数据：
- **Color Images (CIs)**：Maastricht-Bassin, Wildpark 等（~3000×4000×3）
- **Color Videos (CVs)**：Rush-hour, Johnny 等（720×1280×3×50）
- **Multi-Temporal Remote Sensing Images (MRSIs)**：SPOT-53, Landsat-7 等（1000×1000×4×13）
- **Hyperspectral Videos (HVs)**：Main5, Oranges1 等（240×240×33×31）
- **Face Datasets**：Extended YaleFace B（192×168×64×38）、UWA Hyperspectral Face（256×256×30×33）
- **Cardiac MRIs**：sol-yxzt-part 1/2/8/30（256×256×14×20）

所有原始张量均进行了归一化处理。

### 实验设置与评估指标
- **任务类型**：
  - 张量补全（LRTC）
  - 鲁棒张量补全（RTC）
  - 量化张量恢复（Quantized Tensor Recovery）
- **采样率（SR）**：0.1%, 0.3%, 0.5%, 1%, 3%, 5%
- **噪声设置**：
  - 加性高斯噪声（SNR = 2dB / 3dB）
  - 稀疏脉冲噪声（NR = 0.1 / 0.5）
  - 量化噪声（Uniform Scalar Quantization）
- **评估指标**：
  - **MPSNR**（Mean Peak Signal-to-Noise Ratio）
  - **MSSIM**（Mean Structural Similarity Index）
  - **MRSE**（Mean Relative Squared Error）
  - **MTime**（Mean CPU Time in seconds）

### 基线方法对比
| 类别 | 对比方法 |
|------|--------|
| **基于 TR/FCTN 的方法** | TRNNM, FCTN-NNM, FCTN-TC, FCTNFR |
| **基于 T-SVD 的方法** | HTNN, WSTNN, METNN, OTNN, GTNN-HOC, t-e-LogDet |
| **其他先进方法** | TCTV-TC, MTTD, EMLCP-LRTC |
| **鲁棒补全方法** | TTNN, TSPK, TTLRR, LNOP, NRTRM, HWTNN, HWTSN, R-HWTSN, BCNRTC |

提出的随机加速版本命名为 **R1-FCTN-GNTC/GNRTC** 和 **R2-FCTN-GNTC/GNRTC**。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（代表性结果摘录）

#### 表格 I：Face 数据集上的张量补全（SR=1%）
| 方法 | MPSNR ↑ | MSSIM ↑ | MRSE ↓ | MTime ↓ |
|------|---------|---------|--------|---------|
| **R1-FCTN-GNTC** | **36.49** | **0.915** | **0.066** | 8086 |
| FCTN-TC | 34.04 | 0.889 | 0.099 | 34371 |
| TCTV-TC | 34.05 | 0.889 | 0.099 | 17020 |
| HTNN | 29.82 | 0.752 | 0.150 | 3502 |

> ✅ **MPSNR 提升 ~2.5 dB，运行时间仅为 FCTN-TC 的 23.5%**

#### 表格 II：MRI 数据集上的张量补全（SR=1%）
| 方法 | MPSNR ↑ | MSSIM ↑ | MRSE ↓ | MTime ↓ |
|------|---------|---------|--------|---------|
| **R2-FCTN-GNTC** | **32.06** | **0.858** | **0.239** | 2730 |
| FCTN-TC | 25.51 | 0.638 | 0.486 | 4574 |
| TCTV-TC | 29.24 | 0.838 | 0.322 | 8224 |
| WSTNN | 27.35 | 0.640 | 0.391 | 1703 |

> ✅ **MPSNR 提升近 7 dB，且速度更快**

#### 表格 IV：MRSI 上的鲁棒张量补全（SR=0.2, NR=1/3）
| 方法 | MPSNR ↑ | MRSE ↓ | MTime ↓ |
|------|---------|--------|---------|
| **R1-FCTN-GNRTC** | **28.89** | **0.1386** | 3150.63 |
| FCTN-GNRTC (deterministic) | 28.41 | 0.1510 | 15397.15 |
| TCTV-RTC | 28.51 | 0.1643 | 4111.31 |

> ✅ **精度更高（+0.38 dB），速度提升约 4.9×**

### 与基线方法的对比结果
- **恢复质量方面**：
  - 在几乎所有数据集和采样率下，所提方法在 **MPSNR 和 MSSIM 上取得最优或次优结果**。
  - 特别是在 **极低采样率（<1%）** 下，仍能较好保留纹理、边缘和颜色信息，而其他方法出现严重模糊或伪影。
- **计算效率方面**：
  - 随机化版本相比确定性版本平均提速 **9倍**，最高可达 **20倍**。
  - 在 MRSI 恢复任务中，随机版本比原版快 **5~7倍**；在 CV 任务中快 **~2.5倍**。
- **可视化效果**：
  - 图 1–6 显示，本文方法在面部细节、运动结构、遥感地物边界等方面重建更清晰自然。

### 消融实验结果（隐含分析）
虽然未明确列出消融表，但从以下几点可推断模块有效性：
- **引入 L+S 正则化**：相比仅用低秩正则的方法（如 HTNN、WSTNN），在相同条件下获得更高的 MPSNR 和更佳视觉效果。
- **随机压缩机制**：图 3 显示，在几乎不损失 PSNR 的前提下，CPU 时间大幅下降，验证了其高效性。
- **量化建模能力**：图 7 展示了在不同量化步长 δ 下的性能变化趋势，证明模型对量化噪声具有合理鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **FCTN-GNTCTV 能有效联合建模 L+S 先验**，显著提升多维视觉数据的恢复质量，尤其适用于具有强冗余和连续性的图像/视频数据。
2. **随机 sketching 技术是解决大规模张量计算瓶颈的有效途径**，可在几乎无精度损失的情况下实现数量级的速度提升。
3. **将模型从非量化推广至量化观测**增强了实用性，为部署于资源受限设备提供了理论支持。
4. 所提框架在多种真实数据集上实现了**精度与效率的双重领先**，验证了其通用性和优越性。

### 方法的局限性
- **参数敏感性**：非凸函数选择、正则化参数 λ₁、λ₂、量化步长 δ 等需手动调参，缺乏自动调节机制。
- **硬件依赖**：尽管已加速，但在超大规模张量（如 10K×10K×100）上仍可能受限于内存带宽。
- **理论保证有限**：虽然给出了误差上界和收敛性分析，但对非凸优化部分的全局最优性缺乏严格证明。

### 未来工作方向
- 将 **Tensor 方法 + Quantization + Randomized Sketching + Deep Learning** 相结合，探索更快速、自适应的端到端恢复网络。
- 研究面向边缘计算的轻量化张量恢复架构，进一步降低功耗与延迟。
- 探索动态采样策略与主动学习机制，提升极低采样条件下的恢复能力。

--- 

> **总结一句话**：  
> 本文提出了一种**兼具高精度与高效率**的大规模多维视觉数据恢复框架，通过**尺度感知的张量建模**与**加速的随机计算**，解决了传统方法在建模能力和计算成本之间的权衡难题，在多个真实场景中展现出显著优势。

</details>

---

### 6. [Wireless TokenCom: RL-Based Tokenizer Agreement for Multi-User Wireless Token Communications](https://arxiv.org/abs/2602.12338)

**Authors**: Farshad Zeinali, Mahdi Boloursaz Mashhadi, Dusit Niyato, Rahim Tafazolli  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.12338v1  

#### Abstract
Token Communications (TokenCom) has recently emerged as an effective new paradigm, where tokens are the unified units of multimodal communications and computations, enabling efficient digital semantic- and goal-oriented communications in future wireless networks. To establish a shared semantic laten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Wireless TokenCom: RL-Based Tokenizer Agreement for Multi-User Wireless Token Communications*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对**多用户无线 Token Communications（TokenCom）系统中的 Tokenizer Agreement（TA）问题**提出解决方案。在 TokenCom 中，发送端和接收端需就统一的 tokenizer 模型和 codebook 达成一致，以建立共享的语义隐空间（semantic latent space）。然而，在动态变化的多用户无线环境中，如何自适应地选择最优 tokenizer，并联合优化子信道分配、波束成形（beamforming）和资源分配，是一个高度复杂的混合整数非凸优化问题。

传统优化方法因计算复杂度高、难以应对动态环境而受限，且现有基于强化学习（RL）的研究多集中于单用户、小模型的语义通信（SemCom），未考虑 TokenCom 所需的**自适应 tokenizer 协商机制**。

---

### 🚀 提出的新方法与创新点
1. **首次研究多用户无线视频 TokenCom 场景下的自适应 TA 问题**  
   - 明确建模了联合 **Tokenizer Agreement（TA）、子信道分配、波束成形与资源分配** 的混合整数非凸优化问题。

2. **提出一种新型混合强化学习框架：Hybrid DQN-DDPG**
   - **DQN 分支**：处理离散动作空间，用于联合决策 tokenizer 选择和子信道（RB）分配。
   - **DDPG 分支**：处理连续动作空间，生成波束成形向量 $ \mathbf{w}[l] $。
   - 通过双经验回放缓冲区（replay buffer）独立训练两个代理，实现对混合动作空间的有效学习。

3. **设计端到端的 Markov 决策过程（MDP）建模**
   - 状态空间包含信道状态、功率、压缩率等；
   - 奖励函数基于系统效用函数（system utility），并引入约束违反惩罚项，提升可行性。

4. **支持可扩展的多用户场景**
   - 虽然算法复杂度随用户数、天线数增加而上升，但假设基站具备足够算力支撑大规模部署。

---

### 🔍 相比现有方法的优势
| 方面 | 本工作优势 |
|------|-----------|
| **问题建模** | 首次将 TA 引入多用户无线 TokenCom，填补了 LAM/MLLM 驱动通信中自适应 tokenizer 选择的研究空白 |
| **方法设计** | Hybrid DQN-DDPG 可高效处理混合离散-连续动作空间，优于纯 DDPG 对离散变量的低效编码 |
| **适应性** | 支持动态信道条件与异构用户需求，实现个性化 tokenizer 匹配 |
| **性能表现** | 在语义质量、资源效率、视频流畅性方面显著优于传统方案 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用 **DAVIS 视频数据集** 进行评估。
- 视频帧率 $ p = 24 $ fps。
- 分辨率范围从 360p 到 1080p（1920×1080）。

### ⚙️ 实验设置
| 参数 | 设置值 |
|------|--------|
| 基站天线数 $ N $ | 32 / 64 |
| 用户数 $ U $ | 4 / 16 |
| 子载波带宽 $ B $ | 30 kHz（符合 5G NR 标准） |
| 资源块数量 $ R $ | 16 |
| 每个时隙最多服务用户数 $ K $ | 2 |
| 总发射功率 $ P_{\text{BS}} $ | 30 dBm |
| 最小数据速率要求 $ R_{\min} $ | 1 Mbps |
| 仿真轮次 | 500 episodes |
| 每轮步数 | 100 steps |

#### Tokenizer 配置（共 4 种预训练模型）
| Tokenizer Model | bpp | PSNR ↑ | SSIM ↑ | rFVD ↓ |
|------------------|-----|--------|--------|--------|
| Cosmos-0.1-DV8×16×16 | 0.008 | 25.09 | 0.714 | 241.52 |
| Cosmos-0.1-DV4×8×8 | 0.063 | 28.81 | 0.818 | 37.36 |
| HEVC medium | 0.084 | 33.21 | 0.856 | 25.16 |
| BSQ-VAE | 0.127 | 38.41 | 0.920 | 10.057 |

> 注：不同 tokenizer 提供不同的 rate-distortion/perception 权衡。

---

### 📊 评估指标
1. **视频冻结率（Freezing Rate, %）**  
   - 定义为：当用户实际传输速率低于其 tokenizer 所需码率时发生的“卡顿”事件比例。
2. **平均 PSNR（Peak Signal-to-Noise Ratio）**  
   - 衡量视频重建质量。
3. **系统效用（System Utility）**  
   - 综合考虑语义质量和资源消耗：$ U_t = \sum_i (\alpha q_i - \beta R_i^{\text{sum}}) $

---

### 🔁 基线方法对比
1. **DDPG-TA**：使用标准 DDPG 处理所有动作（包括离散 tokenizer 选择），输出经离散化。
2. **Agnostic-TA**：所有用户强制使用相同 tokenizer，忽略信道异质性。
3. **Fixed-TA**：固定使用某一 tokenizer，无自适应能力。
4. **Conventional（H.265）**：传统数字通信基准，采用 H.265 编码 + DQN-DDPG 资源调度。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 视频冻结率降低
- 在 $ U=16 $、1080p 高清视频下：
  - **相比 H.265 基线，冻结率下降约 68%**。
  - Proposed-TA 冻结率稳定在较低水平（<10%），而 Fixed-TA 和 Agnostic-TA 出现明显波动。

#### ✅ PSNR 提升显著
- 在 $ U=4, N=64 $ 条件下：
  - Proposed-TA 平均 PSNR 比 H.265 基线高出约 **10 dB**。
- 随着用户数增加（4→16），PSNR 下降趋势平缓，显示良好可扩展性。

#### ✅ 动态适应能力强
- 随着基站发射功率提升，所有方法 PSNR 上升，但 Proposed-TA 增长最快，表明其能更有效利用富余资源提升语义质量。

---

### 🆚 与基线方法对比结果（综合）

| 方法 | 冻结率 | PSNR | 自适应性 | 资源效率 |
|------|--------|------|----------|----------|
| **Proposed-TA (DQN-DDPG)** | ✅ 最低（↓68% vs H.265） | ✅ 最高（+~10dB vs H.265） | ✅ 强 | ✅ 高 |
| DDPG-TA | ❌ 较高（离散动作处理差） | ❌ 次优 | ⚠️ 一般 | ⚠️ 一般 |
| Agnostic-TA | ❌ 高（无法匹配异构信道） | ❌ 中等 | ❌ 弱 | ❌ 低 |
| Fixed-TA | ❌ 极高且振荡 | ❌ 固定（保守策略） | ❌ 无 | ❌ 低 |
| Conventional (H.265) | ❌ 高 | ❌ 最低 | ❌ 无语义适配 | ❌ 低 |

> 图1(a)-(d) 显示 Proposed-TA 在训练初期快速收敛，后期保持稳定高性能。

---

### 🔍 消融实验分析（隐含于对比中）
虽然文中未明确列出“ablation study”章节，但从以下对比可推断关键组件作用：

| 组件 | 影响分析 |
|------|---------|
| **Hybrid DQN-DDPG 架构** | 显著优于 DDPG-TA → 说明分离处理离散/连续动作更高效 |
| **自适应 TA 机制** | 显著优于 Fixed/Agnostic-TA → 证明 TA 对性能至关重要 |
| **联合优化设计** | 整体效用最大化 → 表明资源分配与 TA 协同增益明显 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Tokenizer Agreement 是 TokenCom 成功的关键前提**，必须根据信道状态和用户需求动态协商。
2. **Hybrid DQN-DDPG 框架能够有效解决混合动作空间下的联合优化问题**，在多用户场景中表现出色。
3. **TokenCom 显著优于传统 H.265 数字通信**：
   - 在相同资源下提供更高语义质量（↑PSNR）；
   - 大幅减少视频冻结事件（↓68%）；
   - 更好地平衡资源开销与感知质量。
4. **系统具有良好的可扩展性和鲁棒性**，即使用户数翻四倍（4→16），性能下降有限。

---

### ⚠️ 方法的局限性
1. **依赖预训练 tokenizer 库的存在**  
   - 假设 BS 和用户均已预装或缓存兼容的 tokenizer/de-tokenizer 对，现实中可能面临存储与更新挑战。
2. **集中式架构限制边缘部署**  
   - 所有决策由 BS 完成，对基站算力要求高，不利于超大规模网络或去中心化场景。
3. **未考虑模型推理延迟与能耗**  
   - 当前效用函数未包含解码端计算成本，未来需纳入端侧设备能力约束。
4. **仅验证视频模态**  
   - 尽管作者指出框架可扩展至图像、音频等，但实验仅限视频。

---

### 🔮 未来工作方向
1. **分布式 TA 机制设计**  
   - 探索联邦学习或共识协议实现去中心化的 tokenizer 协商。
2. **跨模态 TokenCom 扩展**  
   - 支持文本、图像、语音等多模态信号的统一 token 传输。
3. **轻量化 tokenizer 设计**  
   - 开发适用于移动终端的小型 tokenizer 模型，降低部署门槛。
4. **端到端联合训练 tokenizer 与通信策略**  
   - 当前 tokenizer 固定，未来可探索 joint optimization of tokenizer 和无线策略。
5. **真实信道与硬件平台验证**  
   - 在 testbed 或 real-world channel traces 上进一步验证泛化能力。

---

## 总结
该论文开创性地提出了面向多用户无线 TokenCom 的自适应 **Tokenizer Agreement** 框架，结合 **Hybrid DQN-DDPG** 强化学习算法，实现了语义质量与资源效率的协同优化。实验证明其在 **PSNR、冻结率、可扩展性** 等方面全面超越现有方法，为下一代 AI 驱动的语义通信系统提供了重要技术路径。

</details>

---

### 7. [Dual-Granularity Contrastive Reward via Generated Episodic Guidance for Efficient Embodied RL](https://arxiv.org/abs/2602.12636)

**Authors**: Xin Liu, Yixuan Li, Yuhui Chen, Yuxing Qin, Haoran Li, Dongbin Zhao  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.12636v1  

#### Abstract
Designing suitable rewards poses a significant challenge in reinforcement learning (RL), especially for embodied manipulation. Trajectory success rewards are suitable for human judges or model fitting, but the sparsity severely limits RL sample efficiency. While recent methods have effectively impro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Dual-Granularity Contrastive Reward via Generated Episodic Guidance for Efficient Embodied RL**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

在 **Embodied RL**（具身强化学习）中，设计有效的奖励函数是核心挑战之一。传统方法面临以下瓶颈：

- **Sparse success rewards**（稀疏成功奖励）虽然易于定义，但严重限制了 RL 的样本效率。
- 现有的 **dense reward** 设计方法依赖大量高质量专家演示（expert demonstrations）或人工标注数据，成本高昂且难以扩展。

本文旨在解决：**如何在极少专家视频、无需人工标注的前提下，实现高效的 dense reward 设计，从而提升 RL 的样本效率和策略收敛能力？**

---

### 🚀 提出的新方法：DEG（Dual-granularity contrastive reward via generated Episodic Guidance）

提出了一种全新的框架 **DEG**，其核心思想是：

1. **利用大模型先验知识生成 episodic guidance**  
   - 使用一个开源的 **video generation model**（如 Wan2.1-I2V-14B），仅用 **3–5个专家视频** 进行微调（via LoRA），使其适应目标任务的动力学特性。
   - 在每个 RL episode 中，该模型根据当前初始状态生成一条专属的 **task guidance video**，作为智能体的学习目标。

2. **构建双粒度对比奖励（Dual-granularity contrastive reward）**
   - 在自监督的 latent space 中，通过对比学习对齐真实观测与生成视频。
   - 奖励分为两个层次：
     - **Coarse-grained exploration reward**：鼓励粗略地按顺序模仿指导视频的动作轨迹，提升探索效率。
     - **Fine-grained matching reward**：对精确匹配关键帧（如抓取、旋转等精细操作）给予高奖励，确保动作精度。

3. **完全无需人工标注或密集监督**
   - 整个流程不依赖人工设计的 reward 函数、语言描述或多视角专家数据。

---

### ⭐ 相比现有方法的优势

| 方面 | DEG | 现有方法（如 TeViR, RoboCLIP, VIPER） |
|------|-----|-------------------------------|
| 数据需求 | 仅需 **3–5 单视角专家视频** | 需要数十甚至上百视频 + 多视角 |
| 是否需要人工标注 | ❌ 不需要 | ✅ 多数需要语言指令或状态标签 |
| 奖励密度 | 自动生成 dense reward | 依赖预训练 reward model 或手工设计 |
| 泛化能力 | 利用生成模型适应多样化初始状态 | 多基于固定轨迹或静态目标 |
| 样本效率 | 显著更高（见实验） | 受限于稀疏反馈或噪声奖励 |

> ✅ **核心优势**：以极低的数据成本实现了媲美甚至超越人类专家设计 dense reward 的性能。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **Simulation Tasks**：来自 **MetaWorld** 的 **16 个 manipulation 任务**，包括：
  - `drawer-open`, `plate-slide`, `hammer`, `assembly`, `door-close` 等。
  - 覆盖了机械臂操作中的推、拉、旋转、堆叠等多种复杂交互。
- **Real-world Tasks**：在物理 **Franka 机械臂** 上进行两个长程任务：
  - `pick banana`：从随机位置抓取香蕉并放入盘子。
  - `stack cube`：将小红方块堆叠到大木块上。

---

### 🔧 实验设置与评估指标

| 设置项 | 内容 |
|-------|------|
| **Backbone RL Algorithm** | DrQv2（sim）、HIL-SERL（real-world） |
| **训练步数** | Sim: 500k steps；Real: 最多 25k steps |
| **评估指标** | - 成功率（success rate）<br>- 学习曲线（training curve）<br>- 干预次数（intervention steps，real-world） |
| **DEG 输入** | 仅 3–5 条专家视频（无标注） |
| **Reward 类型对比** | - Sparse success reward<br>- Expert-designed dense reward<br>- DEG（独立使用）<br>- DEG+（结合 sparse reward） |

---

### 🆚 基线方法对比

| 基线方法 | 简介 |
|--------|------|
| **TeViR** | 基于文本到视频扩散模型生成多视角轨迹作为奖励，需 60+ 视频 + 三摄像头视角 |
| **Diffusion Reward** | 使用条件视频扩散模型建模专家分布，依赖大量专家数据 |
| **VIPER** | 利用视频预测模型的 likelihood 作为 reward |
| **RoboCLIP** | 使用 VLM 将观察与语言描述对比打分 |
| **Success Sparse Reward** | 环境原生的二值成功信号 |
| **Expert Dense Reward** | McLean et al. (2025) 手工设计的 dense reward，为当前最优基准 |

> 注：部分基线结果引用自原始论文（因未开源实现）。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）在 **12 个 reward-free 任务** 上的表现（Fig. 3）
- DEG 在 **10/12 任务上显著优于所有 baseline**。
- 仅在 `door-close` 和 `drawer-close` 上与 TeViR 接近。
- **样本效率远超其他方法**：在相同步数下达到更高成功率。

#### （2）结合 sparse reward 的表现（DEG+，Fig. 4）
- 在全部 **16 个 MetaWorld 任务** 上：
  - DEG+ **匹配甚至超越 expert dense reward** 的最终性能。
  - 在多个任务（如 `assembly`, `hammer`, `drawer-open`）上 **明显胜出**。
- 示例分析：在 `drawer-open` 中，expert reward 因只关注夹爪与把手距离，易陷入局部最优（从外侧推）；而 DEG 能学会正确抬升夹爪、从内侧钩住把手再拉开。

#### （3）真实世界任务表现（Fig. 5）
- 在 `pick banana` 和 `stack cube` 上：
  - DEG+ 显著优于仅用 sparse reward。
  - 收敛更快，干预频率更低。
  - 表明 DEG 在现实环境中同样有效，且能与 human-in-the-loop RL 兼容。

---

### 🔍 消融实验结果（Ablation Study）

#### （1）双粒度奖励的有效性（Fig. 6 & Fig. 8）
- 移除 coarse-grained reward → 缺乏全局轨迹引导，学习缓慢。
- 移除 fine-grained reward → 能抓取但无法完成精细操作（如准确插入）。
- **两者结合才能实现高效且精准的任务完成**。

#### （2）Policy 意图可视化（Fig. 9）
- **Only coarse**：学会大致运动路径，但无法精确交互。
- **Only fine**：能接触目标，但缺乏顺序模仿，行为混乱。
- **Dual-granularity**：兼具流畅轨迹与精确操作，顺利完成任务。

#### （3）Encoder 对比分析（Fig. 7 & 10–11）
- 使用预训练 DINOv3 编码器：
  - 对生成噪声敏感，语义对齐差。
  - 无法区分不同时间间隔的帧差异，不适合 reward 设计。
- 使用 DEG 自研的 contrastive encoder：
  - 更好对齐语义一致帧，区分语义不同帧。
  - 能将语义距离映射为 latent similarity 差异，支持阈值设计。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **DEG 可在极低数据成本下实现高效 RL**
   - 仅需 **3–5 条专家视频**，无需任何人工标注，即可生成高质量 dense reward。
   - 在模拟和真实世界均验证其有效性。

2. **生成式 episodic guidance 是可行且强大的引导机制**
   - 微调后的 video generation model 能泛化到多样初始状态，提供个性化 episode guidance。
   - 生成视频虽含噪声，但通过 contrastive learning 可有效对齐真实观测。

3. **双粒度奖励机制平衡了探索与精度**
   - Coarse reward 提供宏观引导，提升样本效率；
   - Fine reward 强化细节交互，避免局部最优；
   - 二者协同作用，实现“从粗到精”的策略进化。

4. **DEG 可独立引导 RL 收敛，也可增强 sparse reward**
   - 在 reward-free 场景下表现 SOTA；
   - 结合 sparse reward 后性能媲美甚至超越 expert-designed dense reward。

---

### ⚠️ 局限性

- **视频生成速度慢**：当前 video generation model 推理延迟较高，影响实时性。
- **分辨率敏感**：降低分辨率加速生成可能导致模型崩溃（因训练数据高保真）。
- **领域迁移能力待验证**：目前集中在机械臂 manipulation，是否适用于导航、人形机器人等还需研究。

---

### 🔮 未来工作方向

1. **加速视频生成**：探索轻量化 video foundation models 或蒸馏技术。
2. **跨任务泛化**：研究 single video generator 支持 multiple tasks 的可能性。
3. **与 VLA（Vision-Language-Action）结合**：引入语言 prompt 增强任务理解。
4. **在线自适应生成**：让 guidance video 随策略改进动态调整，形成闭环优化。

---

## 总结

> **DEG 开辟了一条“以生成模型为导师”的新型 RL 范式**：  
> 它利用大模型的先验知识，将少量专家视频转化为 episode-specific 的视觉指导，并通过双粒度对比奖励驱动智能体高效学习。  
> 该方法在数据效率、性能表现和实用性方面均展现出巨大潜力，为未来低监督、高效率的具身智能发展提供了重要思路。

</details>

---

### 8. [ADEPT: RL-Aligned Agentic Decoding of Emotion via Evidence Probing Tools -- From Consensus Learning to Ambiguity-Driven Emotion Reasoning](https://arxiv.org/abs/2602.12714)

**Authors**: Esther Sun, Bo-Hao Su, Abinay Reddy Naini, Shinji Watanabe, Carlos Busso  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.12714v1  

#### Abstract
Speech Large Language Models (SLLMs) enable high-level emotion reasoning but often produce ungrounded, text-biased judgments without verifiable acoustic evidence. In contrast, self-supervised speech encoders such as WavLM provide strong acoustic representations yet remain opaque discriminative model...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ADEPT: RL-Aligned Agentic Decoding of Emotion via Evidence Probing Tools —— 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前 **Speech Emotion Recognition (SER)** 面临两大核心挑战：

- **信号-语义鸿沟**（Signal-Semantics Gap）：
  - **SSL 模型**（如 WavLM）虽能提取强声学表征，但作为黑箱判别模型，缺乏可解释性。
  - **Multimodal Large Language Models (MLLMs)** 虽具备高级推理能力，但其情感判断常基于文本偏见，缺乏对**可验证声学证据**的依赖，易产生“幻觉”。

- **共识悖论**（Consensus Paradox）：
  - 传统 SER 多采用多数投票（plurality/majority voting）生成单一标签，将标注者分歧视为“噪声”而丢弃。
  - 这种做法忽略了人类情感的**复杂性与共现性**（complexity and co-occurrence），导致模型无法捕捉次要情绪（minor emotions）和模糊状态（如中立中的焦虑）。

### 提出的新方法与思路
作者提出 **ADEPT**（Agentic Decoding of Emotion via Probing Tools）框架，实现从**共识学习**到**歧义驱动的情感推理**（Ambiguity-Driven Emotion Reasoning）的范式转变。

#### 核心创新点：

1. **多轮智能体推理框架**（Agentic, Multi-turn Inquiry）
   - 将 SER 重构为一个三阶段的**主动探究过程**，而非单次预测：
     - **Phase 1: Global Perception**（高召回假设初始化）
     - **Phase 2: Evidence Verification**（自适应证据收集）
     - **Phase 3: Decision Synthesis**（证据封闭式裁决）
   - MLLM 被转化为一个**智能体**（agent），维护并更新候选情绪集合，通过调用工具主动查询证据。

2. **结构化探针工具包**（Structured Evidence Probing Toolkit）
   - 定义了四个可审计的工具家族，确保所有推理基于**显式信息检索**（Explicit Information Retrieval, EIR）：
     - **Structural Prior Tool**：基于共现统计进行高效预算分配与回溯。
     - **Semantic Probing Tool**：基于 Scherer 的 CPM 模型，验证文本中的 appraisal 因子（如突发性、控制感）。
     - **Acoustic Probing Tool**：输出可解释的声学 bin（如 High Pitch, Volatile Energy），桥接数值-语义鸿沟。
     - **Refinement Tool**：支持双向重查（replay audio / re-check semantics），解决跨模态冲突（如讽刺）。

3. **歧义驱动的学习范式**
   - **不丢弃少数标注**，而是将其作为**次要情绪**（minor emotions）的监督信号。
   - 在标签构建中保留**平局情况**（tie），允许多个主情绪存在，真实反映情感复杂性。

4. **强化学习对齐优化**（RL-Aligned Optimization）
   - 采用 **Group Relative Policy Optimization (GRPO)** 结合 **Evidence Trust Gate**。
   - 该机制将工具使用行为与预测质量显式耦合，防止“奖励作弊”（reward hacking），确保工具调用是为获取**信息量大且可靠**的证据。

### 相比现有方法的优势
| 维度 | 传统方法 | ADEPT |
|------|--------|-------|
| **推理方式** | 单次分类 / 后见解释 | 多轮主动探究，证据驱动 |
| **证据基础** | 黑箱特征 / 文本生成 | 可审计的声学与语义证据 |
| **标签处理** | 丢弃分歧，强制共识 | 利用分歧，建模共现 |
| **可解释性** | 低（post-hoc explanation） | 高（audit trail, causal traceability） |
| **鲁棒性** | 易受文本偏见影响 | 平衡语义与声学信号 |

---

## 2. 核心实验方法和设置

### 数据集
- **MSP-Podcast Corpus V2.0**（主实验）：
  - 自然主义播客语音，非表演性情感。
  - 每段音频至少有 5 名标注者，提供丰富的标注分歧。
  - 使用其 8 类主情绪标签（Anger, Sadness, Happiness, Surprise, Fear, Disgust, Contempt, Neutral）。
- **IEMOCAP**（零样本泛化测试）：
  - 用于评估跨域鲁棒性，在未微调的情况下进行严格零样本测试。

### 实验设置
- **基础模型**：`Qwen-3-Omni`（多模态大语言模型）。
- **训练方法**：使用 GRPO 进行强化学习后训练（post-training），采样 8 条轨迹（K=8）每提示。
- **训练数据量**：169K 样本，2 个 epoch。

### 评估指标
1. **Primary-label fidelity**（主情绪准确性）：
   - `P-MacroF1`：主情绪严格匹配的宏平均 F1。
   - `Soft Recall`：宽松匹配，只要预测包含真实主情绪即计数。
2. **Set-level ambiguity decoding**（集合级歧义解码）：
   - `Set Recall`：预测集合与真实集合（主+次）的交集覆盖率。
   - `Jaccard Index`（IoU）：预测与真实标签集的 Jaccard 相似度，惩罚过预测。
3. **Calibration**（校准性）：
   - `Average Cardinality`：平均预测标签数量，监测模型是否过度保守或激进。

### 基线方法对比
| 类别 | 基线方法 |
|------|----------|
| **SSL 分类器**（仅主情绪） | HuBERT, wav2vec2.0, WavLM (frozen/fine-tuned) |
| **生成式 SLLM/MLLM**（主+次） | SALMONN, Qwen-2-Audio, BLSP-Emo |
| **消融变体** | Qwen-3-Omni (no tools), ADEPT (w/o GRPO) |

---

## 3. 主要实验结果和性能指标

### 在 MSP-Podcast 上的主要结果（Test1）

| Method | P-MacroF1 ↑ | Soft R ↑ | Set R ↑ | Jaccard ↑ | Avg Size |
|--------|-------------|----------|---------|-----------|----------|
| **WavLM (fine-tuned)** | 0.3640 | — | — | — | — |
| **BLSP-Emo** | 0.2915 | 0.6740 | 0.5320 | 0.4280 | 2.08 |
| **Qwen-3-Omni (no tools)** | 0.2358 | 0.6003 | 0.4923 | 0.3890 | 2.02 |
| **ADEPT (w/o GRPO)** | 0.3571 | 0.7032 | 0.5748 | 0.4450 | 2.15 |
| **ADEPT + GRPO (Ours)** | **0.4224** | **0.7874** | **0.6192** | **0.4751** | 2.21 |

#### 关键发现：
- ADEPT + GRPO 在 **P-MacroF1** 上超越最强 SSL 基线（WavLM-ft）**+5.84 个百分点**。
- 在 **Set Recall** 和 **Jaccard** 上显著优于所有生成式基线，证明其在恢复共现情绪上的强大能力。
- 工具协议（ADEPT）本身带来巨大提升（vs. no tools），GRPO 进一步增强性能。

### 消融实验结果（Ablation Study）

| Variant | Set R | Soft R | Jaccard | P-F1 |
|--------|-------|--------|---------|------|
| **ADEPT (Full)** | 61.92 | 78.74 | 47.51 | 42.24 |
| w/o Prior | 58.17 | 74.58 | 44.63 | 39.07 |
| w/o Semantic | 60.06 | 75.93 | 42.31 | 33.86 |
| w/o Acoustic | 59.08 | 75.21 | 41.28 | 34.52 |
| w/o Replay | 60.47 | 77.06 | 46.18 | 40.44 |
| w/o Trust Gate | 60.73 | 77.82 | 44.96 | 40.63 |

#### 消融分析：
- **移除 Acoustic Tools** 对 Jaccard 影响最大，表明局部声学证据对歧义解析至关重要。
- **移除 Semantic Tools** 对 P-MacroF1 影响最大，说明文本验证对主情绪判别关键。
- **移除 Trust Gate** 导致 Jaccard 下降，验证其有效防止无效工具调用。

### 零样本泛化（IEMOCAP）

| Method | Acc ↑ | Soft R ↑ | Set R ↑ | Jaccard ↑ |
|--------|-------|----------|---------|-----------|
| **BLSP-Emo** (supervised) | 76.13 | 80.51 | 52.30 | 48.90 |
| **Qwen-3-Omni** | 42.37 | 51.12 | 35.24 | 31.08 |
| **ADEPT + GRPO (Ours)** | 54.83 | 67.42 | **54.80** | 43.15 |

#### 发现：
- 尽管是零样本，ADEPT 在 **Set Recall** 上**超过全监督 SOTA**（54.80 vs. 52.30），证明其强大的歧义感知与跨域泛化能力。

---

## 4. 关键结论和发现

### 主要发现
1. **歧义是信息，不是噪声**：将标注者分歧作为监督信号，能显著提升对**共现情绪**的建模能力。
2. **主动探究优于被动分类**：通过工具调用进行**显式信息检索**（EIR），使模型推理过程可审计、可追溯。
3. **工具协同增效**：Semantic、Acoustic、Prior、Refinement 工具互补，缺一不可。
4. **GRPO + Trust Gate 有效对齐行为**：确保工具使用服务于提高预测质量，而非盲目调用。
5. **计算资源按需分配**：工具调用次数随标注歧义度增加（高歧义样本调用更多工具），体现智能资源调度。

### 局限性
- **计算开销较大**：多轮工具调用显著增加推理延迟，不适合实时应用。
- **依赖高质量对齐**：Acoustic Tools 依赖 forced alignment，对齐错误可能传播。
- **工具设计依赖先验知识**：探针工具的设计基于心理学理论（如 CPM），通用性受限。
- **数据集限制**：MSP-Podcast 虽大，但仍以英语为主，多语言泛化待验证。

### 未来工作方向
- 扩展至更丰富的情绪标签体系（如 secondary emotions）。
- 探索轻量化工具调用策略，降低推理成本。
- 将 ADEPT 框架应用于其他多模态理解任务（如意图识别、心理健康评估）。
- 研究如何让模型自主发现和定义新的探针工具（meta-tool learning）。

--- 

> **总结**：ADEPT 成功地将 SER 从“静态分类”转变为“动态、可审计的科学探究”，通过**智能体架构 + 结构化工具 + 歧义利用 + 强化学习对齐**，实现了更高准确率、更强解释性与更好泛化性的统一，为可信的 Affective Computing 提供了新范式。

</details>

---

### 9. [Formalizing the Sampling Design Space of Diffusion-Based Generative Models via Adaptive Solvers and Wasserstein-Bounded Timesteps](https://arxiv.org/abs/2602.12624)

**Authors**: Sangwoo Jo, Sungjoon Choi  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.12624v1  

#### Abstract
Diffusion-based generative models have achieved remarkable performance across various domains, yet their practical deployment is often limited by high sampling costs. While prior work focuses on training objectives or individual solvers, the holistic design of sampling, specifically solver selection...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Formalizing the Sampling Design Space of Diffusion-Based Generative Models via Adaptive Solvers and Wasserstein-Bounded Timesteps*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
扩散生成模型（Diffusion-based Generative Models）在图像、视频和3D内容生成中取得了卓越性能，但其**高采样成本**（即高 Number of Function Evaluations, NFE）严重限制了实际部署。现有方法多依赖静态启发式策略（如固定步长调度和统一求解器），忽略了扩散轨迹在不同噪声阶段的几何特性差异，导致效率低下。

### 提出了什么新方法或新思路
本文提出了 **SDM (Sampling Design via Adaptive Solvers and Wasserstein-Bounded Timesteps)**，一个无需重新训练的采样框架，从几何视角形式化了扩散模型的采样设计空间，包含两个核心组件：

- **自适应 ODE 求解器（Adaptive ODE Solvers）**  
  基于对 Probability Flow ODE（PF-ODE）曲率的理论分析，提出动态分配求解器：在早期高噪声阶段使用高效的低阶求解器（如 Euler），在接近数据流形时切换到高阶求解器（如 Heun）。该策略由一个基于向量场变化的缓存曲率估计 $K_{\text{rel}}(i)$ 驱动，避免额外 NFE 开销。

- **Wasserstein 有界自适应时间步调度（Wasserstein-Bounded Adaptive Scheduling）**  
  引入一种新的优化框架，通过控制每一步的局部 **Wasserstein 距离**误差来推导自适应时间步。具体地，推导出最大允许步长 $\Delta t \leq \sqrt{\frac{2\eta}{S_t}}$，其中 $S_t$ 是速度场的变化上界，$\eta$ 是可调误差容忍度。此外，提出 **N-step resampling** 算法，在固定 NFE 下实现最优路径重采样。

### 相比现有方法的优势
- **无需再训练**：完全适用于预训练模型（如 EDM、Stable Diffusion），不修改模型架构或参数。
- **几何感知**：首次将扩散轨迹的非线性和刚性变化纳入采样设计，实现“按需精度”。
- **系统性优化**：统一了求解器选择与时间步调度的设计空间，二者可独立优化并互补提升。
- **高效且灵活**：在更少 NFE 下达到更高样本质量，并支持通过超参数（如 $\eta$, $T_k$）灵活权衡质量与效率。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **CIFAR-10**（32×32）
- **FFHQ**（64×64）
- **AFHQv2**（64×64）
- **ImageNet**（64×64）

所有实验均基于预训练的 **EDM 模型**（Karras et al., 2022），涵盖 VP 和 VE 参数化。

### 实验设置和评估指标
- **评估指标**：
  - **FID (Fréchet Inception Distance)**：衡量生成样本的质量和多样性。
  - **NFE (Number of Function Evaluations)**：衡量采样效率。
- **采样器配置**：
  - 对比 Euler、Heun 及 SDM 自适应求解器。
  - 时间步调度包括：EDM (p=7)、COS (Williams et al., 2024)、SDM 自适应调度。
- **消融研究**：对阈值 $T_k$、调度函数 $A(t)$（step/linear/cosine）、误差容忍参数 $\eta_{\min}, \eta_{\max}, p$ 和 resampling 参数 $q$ 进行网格搜索。

### 基线方法对比
- **EDM (p=7)**：标准多项式噪声调度。
- **COS (Corrector Optimized Schedules)**：基于 score-based 优化的时间步。
- 所有方法均在相同预训练模型上测试，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 和 Abstract）
| 数据集       | 方法               | 参数化 | FID    | NFE  |
|------------|--------------------|--------|--------|------|
| CIFAR-10   | **SDM (Adaptive Solver)** | VP     | **1.93** | 32   |
| FFHQ       | **SDM (Adaptive Scheduling)** | VP     | **2.41** | 79   |
| AFHQv2     | **SDM (Adaptive Solver)** | VP     | **1.98** | 66   |

> 注：这些结果均为 state-of-the-art，且部分优于基线的同时降低了 NFE。

### 与基线方法的对比结果
- 在 **Euler 求解器 + 自适应调度** 下，FID 显著下降：
  - CIFAR-10 (VP): 7.61 (EDM) → **6.18** (SDM)，↓18.7%
  - FFHQ (VP): 4.59 → **4.16**
- 在 **Heun 求解器** 上，SDM 调度仍能进一步提升质量或保持竞争力。
- **SDM 自适应求解器（仅用 Euler/Heun 切换）** 已超越 EDM + Heun 基线，同时降低约 15–20% NFE。
- **组合使用自适应求解器 + 自适应调度** 达到最佳性能，例如 AFHQv2 上 FID 达 **1.97 (VP)** / **1.99 (VE)**。

### 消融实验结果
#### （1）阈值 $T_k$ 的影响（Figure 4）
- 存在一个最优 $T_k$ 平衡质量与效率；过大则过早启用高阶求解器，浪费计算；过小则损失精度。
- 最优值因数据集而异（如 CIFAR-10: $2\times10^{-4}$，AFHQv2: $1\times10^{-3}$）。

#### （2）调度函数 $A(t)$ 的选择（Table 5）
- **Step schedule** 表现最好，FID 最低且 NFE < 2（平均每次更新不到两次网络调用）。
- Linear/Cosine 调度虽平滑但需要固定 NFE=2，效率更低。

#### （3）误差容忍与 resampling 参数
- CIFAR-10 对 $\eta_{\min}, \eta_{\max}, p, q$ 敏感，需精细调参。
- 其他数据集鲁棒性强，采用统一设置即可获得良好表现。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **扩散轨迹具有显著的几何非均匀性**：早期高噪声阶段近乎线性，适合低阶求解器；后期接近数据流形时曲率急剧上升，需高阶求解器。
2. **静态调度是次优的**：EDM 等启发式调度在中间阶段浪费误差预算，而 SDM 将更多容错空间分配给早期平滑阶段，后期收紧控制，从而更高效利用 NFE。
3. **自适应求解器与自适应调度是正交且互补的**：两者可独立应用，联合使用带来叠加增益。
4. **无需训练即可突破性能边界**：SDM 在多个 benchmark 上实现了无需微调的 SOTA 性能。

### 方法的局限性
- 曲率估计依赖缓存机制，可能在极端非线性区域略有延迟。
- 当前实现主要针对确定性 ODE 采样，未直接扩展至 SDE 或随机采样路径。
- 超参数（尤其是 $\eta$ 和 $T_k$）需根据任务调整，尚未完全自动化。

### 未来工作方向
- 将 SDM 框架推广至 **Rectified Flow**、**Consistency Models** 等新型生成范式。
- 探索 **全自动超参数调节机制**（如基于在线误差估计）。
- 结合 **distillation** 或 **knowledge transfer** 技术，构建端到端高效推理 pipeline。
- 扩展至 **高分辨率生成**（如 1024×1024）和 **跨模态生成任务**（如 text-to-video）。

---

> ✅ **代码开源地址**：https://github.com/aiimaginglab/sdm  
> 📌 **一句话总结**：SDM 通过几何感知的自适应求解器与 Wasserstein 有界时间步调度，为预训练扩散模型提供了无需训练即可提升采样效率与质量的新范式。

</details>

---

### 10. [Mixture of Predefined Experts: Maximizing Data Usage on Vertical Federated Learning](https://arxiv.org/abs/2602.12708)

**Authors**: Jon Irureta, Gorka Azkune, Jon Imaz, Aizea Lojo, Javier Fernandez-Marques  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.12708v1  

#### Abstract
Vertical Federated Learning (VFL) has emerged as a critical paradigm for collaborative model training in privacy-sensitive domains such as finance and healthcare. However, most existing VFL frameworks rely on the idealized assumption of full sample alignment across participants, a premise that rarel...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Mixture of Predefined Experts: Maximizing Data Usage on Vertical Federated Learning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对 **Vertical Federated Learning (VFL)** 中两个关键现实挑战：
- **样本不完全对齐（Sample Misalignment）**：真实场景中，不同参与方的数据ID交集稀疏（如跨医院患者重叠少），传统VFL方法因依赖“全样本对齐”假设而性能严重下降。
- **通信开销大与鲁棒性差**：现有方法需多轮通信进行端到端训练，且难以抵御恶意或噪声参与者的影响，同时缺乏可解释性。

### 🚀 提出的新方法：Split-MoPE
提出 **Split-MoPE**，一种结合 **Split Learning** 与新型 **Mixture of Predefined Experts (MoPE)** 架构的VFL框架。

#### 创新思路：
- **MoPE 替代标准 MoE**：
  - 传统 MoE 动态学习路由（routing），专家按数据模式自适应分工。
  - **MoPE 预定义专家职责**：每个专家专门处理一种特定的 **数据对齐组合**（由 `P(K)` 定义），即不同的特征可用性模式（如仅主动方、主被动均有等）。
- **Split Learning + 冻结预训练编码器**：
  - 被动方使用 **预训练且冻结的 backbone**（如 DINOv2、Qwen3-Embedding）提取特征。
  - 仅传输一次嵌入（embedding），实现 **单轮通信训练**，极大降低通信成本。

### 🔍 相比现有方法的优势
| 优势维度 | 具体表现 |
|--------|---------|
| **数据利用率** | 最大化利用非对齐样本，支持训练与推理阶段的不完整数据 |
| **通信效率** | 单轮通信，通信量减少达 **200×**（相比多轮端到端训练） |
| **鲁棒性** | 对噪声/恶意参与者具有天然抵抗力，性能退化最多降低 **25×** |
| **可解释性** | 提供 **per-sample 贡献度量化**，明确各参与者对预测的贡献 |
| **无需额外超参数** | 相比 APC-VFL、VFedTrans 等，为 **hyperparameter-free** 设计 |

---

## 2. 核心实验方法和设置

### 📊 数据集
在三种数据集上验证，涵盖视觉与表格模态：
- **CIFAR-10 / CIFAR-100**：图像分类任务，通过切分图像块模拟多方特征分割。
- **Breast Cancer Wisconsin**：表格数据，直接按列分配给不同参与者。

> 注：所有数据集均非原生VFL格式，需人工垂直切分。

### ⚙️ 实验设置
- **样本缺失机制**：采用 **MCAR（Missing Completely at Random）**，引入参数 `pmiss` 控制被动方数据缺失概率（0=完全对齐，1=仅主动方可见）。
- **参与者数量**：主要测试 **两方协作** 场景（符合VFL常见设定）。
- **预训练编码器**：
  - 图像：DINOv2（ViT-S/B/L）
  - 表格：将行转为文本 `"feat1:val1|feat2:val2"`，使用 **Qwen3-Embedding** 模型生成向量。

### 🎯 评估指标
- **图像数据**：测试准确率（Accuracy %）
- **表格数据**：F1 Score（×100）
- 所有结果报告 **三轮实验的均值±标准差**

### 🆚 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **SplitNN*** | 改进基线 | 使用冻结预训练编码器的 SplitNN |
| **LASER*** | 当前 SOTA | 改进版 LASER，同样使用冻结编码器 |
| **Local Model** | 下界基准 | 仅使用主动方本地特征训练的模型 |

> 注：带 `*` 表示作者复现并改进的版本，以公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 2）

#### 在 **CIFAR-100** 上的表现（Accuracy %）
| 方法 \ `pmiss` | 0.0 | 0.1 | 0.7 |
|----------------|-----|-----|-----|
| Local          | —   | —   | 73.48 |
| SplitNN*       | 81.53 | 81.12 | 72.56 |
| LASER*         | 82.26 | 79.83 | 72.55 |
| **MoPE**       | **81.78** | **80.50** | **73.27** ✅ |

> MoPE 在高缺失率下仍优于其他方法，且始终 **不低于 Local 模型**。

#### 在 **Breast Cancer Wisconsin** 上的表现（F1 Score ×100）
| 方法 \ `pmiss` | 0.0 | 0.1 | 0.7 |
|----------------|-----|-----|-----|
| Local          | —   | —   | 86.36 |
| SplitNN*       | 93.37 | 93.37 | 82.86 |
| LASER*         | 86.69 | 84.90 | 86.19 |
| **MoPE**       | **94.15** | **93.30** | **87.68** ✅ |

> 在 `pmiss=0.7` 时，MoPE 比 SplitNN* 高出近 **5 个百分点**，显著提升。

### 🔁 与基线方法对比总结
- **对齐场景（`pmiss=0`）**：MoPE 性能与 SplitNN*/LASER* 相当，达到 SOTA 水平。
- **高缺失场景（`pmiss > 0.5`）**：MoPE 显著领先，尤其在表格数据上优势明显。
- **极端缺失（`pmiss → 1`）**：MoPE 性能趋近于 Local 模型，满足 **Local Robustness** 要求，而其他方法可能低于 Local。

### 🧪 消融实验与关键分析

#### （1）冻结预训练编码器的有效性（Table 1）
| 方法 | CIFAR-100 (`pmiss=0`) |
|------|-----------------------|
| LASER (end-to-end) | 59.98 |
| **LASER*** (frozen) | **95.42** ✅ |
| SplitNN (end-to-end) | 47.55 |
| **SplitNN*** (frozen) | **95.61** ✅ |

> 结论：**冻结预训练编码器 + 单轮通信** 不仅大幅降低通信成本，反而显著提升性能，打破“必须微调”的固有认知。

#### （2）对抗噪声/恶意参与者的鲁棒性（Table 3）
在添加噪声参与者（embedding ~ N(0,100)）后，性能下降（Δ↓）越小越好：

| 方法 \ 噪声数 | 1 noisy (CIFAR-10) | 5 noisy (CIFAR-10) |
|--------------|--------------------|--------------------|
| SplitNN*     | 3.05               | 5.92               |
| LASER*       | 3.76               | 22.31              |
| **MoPE**     | **0.35** ✅        | **3.53** ✅        |

> MoPE 在多数情况下性能下降最小，**最多减少 25× 的退化**，表现出极强鲁棒性。

#### （3）路由权重分析（Table 4）
- MoPE 的 **router 能有效识别噪声输入**，将其对应专家的权重降至接近 0。
- 可实现 **per-sample 贡献度分析**，支持公平激励机制设计。

#### （4）更大编码器带来更强鲁棒性（Table 5）
| 编码器 | CIFAR-10 (`pmiss=0→0.7`) Δ↓ | BCW (`pmiss=0→0.7`) Δ↓ |
|--------|-------------------------------|--------------------------|
| ViT-S (21M) | -3.65 pts | -11.17 pts |
| ViT-L (300M) | -2.07 pts | -2.67 pts ✅ |

> 更大的预训练模型不仅性能更高，**对数据缺失也更鲁棒**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoPE 架构能有效应对样本不对齐问题**，在各种 `pmiss` 下均优于现有 SOTA 方法。
2. **冻结预训练编码器 + 单轮通信** 是高效且高性能的VFL范式，颠覆传统端到端训练逻辑。
3. **MoPE 天然具备鲁棒性与可解释性**：
   - Router 自动抑制噪声输入影响。
   - 可量化每个参与方的贡献，增强信任与公平性。
4. **更大的预训练编码器不仅能提性能，还能增强对缺失数据的容忍度**。

### ⚠️ 方法的局限性
- 当前实验集中在 **两方协作**，扩展到更多参与方时，`P(K)` 组合数指数增长，可能导致专家数量过多。
- MoPE 的 **expert 数量由数据对齐模式决定**，在极端稀疏场景下可能面临计算或内存压力。
- 路由机制在噪声比例极高时可能出现次优加权（如等权分配有效专家）。

### 🔮 未来工作方向
1. 探索 **更高效的 MoPE 训练策略**，如专家共享、稀疏激活等。
2. 将 Split-MoPE 应用于 **异构多模态联邦学习**（如图像+文本+表格），研究路由如何权衡不同模态价值。
3. 设计 **动态专家剪枝或合并机制**，以应对大规模参与者场景。
4. 探索在 **Horizontal FL 或 Hybrid FL** 中的应用潜力。

---

> 💡 **一句话总结**：  
> **Split-MoPE** 通过 **预定义专家 + 冻结编码器 + 单轮通信**，实现了高效、鲁棒、可解释的 VFL 新范式，在样本不全对齐的真实场景中显著超越现有方法。

</details>

---

### 11. [WebClipper: Efficient Evolution of Web Agents with Graph-based Trajectory Pruning](https://arxiv.org/abs/2602.12852)

**Authors**: Junjie Wang, Zequn Xie, Dan Yang, Jie Feng, Yue Shen, Duolin Sun, Meixiu Long, Yihan Jiao, Zhehao Tan, Jian Wang, Peng Wei, Jinjie Gu  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.12852v1  

#### Abstract
Deep Research systems based on web agents have shown strong potential in solving complex information-seeking tasks, yet their search efficiency remains underexplored. We observe that many state-of-the-art open-source web agents rely on long tool-call trajectories with cyclic reasoning loops and expl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：WebClipper: Efficient Evolution of Web Agents with Graph-based Trajectory Pruning

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前基于 **web agent** 的 Deep Research 系统在复杂信息检索任务中表现出色，但普遍存在**搜索效率低下**的问题。许多开源 web agent 依赖冗长的工具调用轨迹（tool-call trajectories），存在以下低效模式：
- **循环推理（cyclic reasoning loops）**：反复验证已获取的信息。
- **无效分支探索（unproductive branches）**：偏离正确解路径，陷入无关细节。

这些行为导致工具调用轮次（tool-call rounds）和上下文长度激增，显著增加推理延迟和成本（如 Google Search、Jina Reader 等商业工具费用高昂），影响实际用户体验。

### 提出了什么新方法或新思路
作者提出 **WebClipper**，一种通过**基于图的轨迹剪枝（graph-based trajectory pruning）** 来高效演进 web agent 的框架。其核心思想是将 agent 的搜索过程建模为**状态图（state graph）**，并将轨迹优化形式化为**最小必要有向无环图（Minimum-necessary DAG, MNDAG）挖掘问题**。

#### WebClipper 框架包含四个关键步骤：
1. **Trajectory to State-Graph Transformation**  
   将原始轨迹转换为状态图，其中节点分为两类：
   - **Action Nodes**：表示 agent 的思考与动作（如 `SEARCH`, `VISIT`, `PYTHON`, `ANSWER`）。
   - **Information Nodes**：表示从环境中获得的原子信息（如查询、网页内容、计算结果）。
   边表示动作与信息之间的依赖关系。

2. **Pruning via MNDAG**  
   在状态图上运行最短路径算法（Dijkstra-style）并进行反向闭包遍历，提取连接初始查询节点（Io）到最终答案节点（Aₜ）的**最小必要子图（MNDAG）**，从而移除所有不贡献于最终答案的冗余步骤。

3. **Coherence-aware Thought Rewriting**  
   对剪枝后的轨迹进行语义连贯性重写，确保 ReAct 风格的“观察-思考-行动”循环逻辑连续。采用两种策略：
   - **Context-aware selective rewriting**：仅重写因剪枝而断开逻辑链的思考。
   - **Perplexity-based selection**：由 base model 自身选择困惑度（PPL）最低的重写版本，以保持推理风格一致性。

4. **Agent Evolution via Hybrid Training**  
   基于剪枝后的高质量轨迹对原 agent 进行演化训练，提出两种策略：
   - **Efficiency-oriented evolution**：仅在剪枝轨迹上微调，最大化效率。
   - **Hybrid evolution**：混合使用剪枝轨迹与未剪枝但高难度的轨迹，平衡效率与准确性。

此外，作者提出了一个新的综合评估指标 **F-AE Score**，用于衡量模型在**准确性（Accuracy）与效率（Efficiency）之间的平衡能力**，定义如下：

$$
\text{F-AE} = 2 \times \frac{\text{Acc} \times (1 - \frac{\text{Rounds}}{\text{Max\_Rounds}})}{\text{Acc} + (1 - \frac{\text{Rounds}}{\text{Max\_Rounds}})}
$$

该指标借鉴 F1-score 思想，使用调和平均数避免单一维度被过度优化。

### 相比现有方法的优势
| 方法 | 缺陷 | WebClipper 的优势 |
|------|------|------------------|
| **Prompt Control**（提示工程控制） | 效果有限，难以约束复杂行为 | 显著减少工具调用且不牺牲准确率 |
| **Coarse Prune**（粗粒度剪枝） | 单一 LLM 判断易出错，导致性能大幅下降 | 图结构分析更精细可靠，保留关键路径 |
| **直接扩大 context 和 depth**（如 Tongyi-DeepResearch） | 成本高、效率低 | 在保持甚至提升准确率的同时降低约 20% 工具调用 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验在四个主流 web agent benchmark 上进行：
- **xbench-deepsearch**：深度研究类任务基准。
- **Browsecomp**：浏览代理挑战性基准。
- **GAIA**：通用 AI 助手综合能力评测集（使用 dev set 中的 103 个纯文本样本）。
- **HLE**（Humanity's Last Exam）：高难度多跳推理任务（使用 500 个纯文本子集）。

### 实验设置和评估指标
#### 评估指标
| 指标 | 描述 |
|------|------|
| **Accuracy (Acc)** | 使用 `o3-mini` 作为 LLM-as-Judge 判定回答是否正确 |
| **Tool-call Rounds ↓** | 推理过程中使用的工具调用次数（越少越好） |
| **Token Consumption ↓** | 推理阶段总 token 数量（越少越好） |
| **F-AE Score ↑** | 新提出的综合指标，平衡 accuracy 与 efficiency |

> Max_Rounds 设为 100，符合 Deep Research 类系统的典型上限。

#### 基线方法对比
- **Closed-source Systems**：
  - OpenAI o3
  - OpenAI DeepResearch
  - Claude-4-Sonnet
- **Open-source Agents**：
  - Kimi-K2-Instruct-0905
  - DeepSeek-R1-671B
  - Qwen3-235B-A22B-Instruct-2507
  - WebExplorer
  - Tongyi-DeepResearch（基础模型）
- **Pruning Baselines**（本文设计）：
  - **Prompt Control**：在系统提示中加入“避免重复操作”等指令。
  - **Coarse Prune**：用 Qwen3 直接识别并删除冗余对话轮次后进行 SFT。

#### 实现细节
- **Base Agent**: Tongyi-DeepResearch (30B-A3B)
- **Extractor & Rewriter**: Qwen3-235B-A22B-Instruct-2507
- **Training Platform**: 32 × H800 GPUs
- **Learning Rate**: 5e-6，cosine decay
- **Web Tools**: Serper API（搜索）、Jina Reader（URL 解析）
- **每模型运行三次取平均值**，降低方差。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | xbench-deepsearch (Acc/F-AE/Rounds) | Browsecomp (Acc/F-AE/Rounds) | GAIA (Acc/F-AE/Rounds) | HLE (Acc/F-AE/Rounds) |
|------|-------------------------------|--------------------------|--------------------|------------------|
| **Tongyi-DeepResearch** | 0.713 / 0.779 / 14.26 | 0.410 / 0.385 / 63.70 | 0.682 / 0.733 / 20.56 | 0.358 / 0.487 / 23.92 |
| **WebClipper (Eff)** | **0.713** / **0.792** / **10.81** | **0.427** / **0.431** / **56.50** | **0.684** / **0.760** / **14.44** | 0.353 / 0.492 / 18.60 |
| **WebClipper (Hybrid)** | **0.733** / **0.797** / 12.57 | **0.467** / **0.428** / 60.42 | **0.695** / 0.744 / 19.92 | **0.361** / **0.495** / 21.07 |

> ✅ **加粗** 表示优于基线；↑ 越高越好，↓ 越低越好。

### 与基线方法的对比结果
- **相比 Tongyi-DeepResearch 基线**：
  - **工具调用轮次平均减少约 20%**（最高达 30%，尤其在 GAIA 上）。
  - **token 消耗降低约 19.4%**。
  - **准确率持平或提升**（Hybrid 版本平均提升 4.8%）。
  - **F-AE Score 全面领先**，证明其在 accuracy-efficiency trade-off 上表现最优。

- **相比其他剪枝方法**：
  - **Prompt Control**：仅略微减少轮次（12.50 vs 14.26），但准确率下降明显（0.676 vs 0.713）。
  - **Coarse Prune**：虽能减少轮次（8.85），但准确率严重受损（0.603），说明粗粒度剪枝不可靠。

### 消融实验结果（Ablation Study）

#### （1）组件有效性（Figure 4）
| 变体 | 描述 | 影响 |
|------|------|------|
| **w/o GP**（无图剪枝） | 替换为 Coarse Prune | 准确率显著下降 → 图结构分析至关重要 |
| **w/o PPL-S**（无困惑度筛选） | 不做 PPL 选择 | 性能下降 → PPL 有助于保持推理风格一致 |
| **w/o CSR**（无上下文感知重写） | 无条件重写所有思考 | **灾难性崩溃** → 语义连贯性必须保障 |

> 结论：三个模块均不可或缺，尤其是 **coherence-aware rewriting** 是成功的关键。

#### （2）训练策略比较（Table 2）
| 策略 | Acc | Rounds | F-AE |
|------|-----|--------|------|
| **Unpruned-Distill** | 最高（0.746） | 最高（17.13） | 中等（0.785） |
| **WebClipper (Eff)** | 持平（0.713） | 最低（10.81） | 最高（0.792） |
| **WebClipper (Hybrid)** | 更高（0.733） | 合理（12.57） | **最高（0.797）** |

> **Hybrid 策略实现了最佳权衡**：既提升了 accuracy，又控制了 rounds，适合实际部署。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **web agent 存在大量可剪枝的冗余行为**，如循环验证、过度探索无关信息。
2. **将轨迹建模为状态图并挖掘 MNDAG 是有效的剪枝方式**，能精准识别并去除冗余步骤。
3. **基于剪枝轨迹继续训练可使 agent 向更高效的搜索模式演化**，实现“进化”而非重建。
4. **F-AE Score 是一个合理且实用的综合评价指标**，有效防止只追求 accuracy 或 efficiency 的极端倾向。
5. **WebClipper 在多个 benchmark 上实现了效率与准确性的双重提升**，尤其在 GAIA 上 tool-call 减少近 30%。

### 方法的局限性
1. **依赖 base model 的初始能力**：若 base model 本身规划能力弱，剪枝只能在其内部去冗，无法根本改进策略。
2. **泛化性受限**：目前仅针对 search/browse/code 执行类工具，尚未扩展至多模态、数据库查询、API 调用等新型 action space。
3. **图构建依赖 LLM extractor**：存在一定的不稳定性，需通过多次采样+多数投票缓解。

### 未来工作方向
1. **结合强化学习（RL）或在线学习机制**，让 agent 主动发现新的高效路径，超越已有轨迹的限制。
2. **扩展图框架以支持多样化工具类型**，构建适用于更广泛应用场景的通用 agent 压缩框架。
3. **探索自动化的 MNDAG 构建方法**，减少对 LLM extractor 的依赖，提高鲁棒性和可扩展性。

</details>

---

### 12. [Discovering Semantic Latent Structures in Psychological Scales: A Response-Free Pathway to Efficient Simplification](https://arxiv.org/abs/2602.12575)

**Authors**: Bo Wang, Yuxuan Zhang, Yueqin Hu, Hanchao Hou, Kaiping Peng, Shiguang Ni  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.12575v1  

#### Abstract
Psychological scale refinement traditionally relies on response-based methods such as factor analysis, item response theory, and network psychometrics to optimize item composition. Although rigorous, these approaches require large samples and may be constrained by data availability and cross-cultura...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Discovering Semantic Latent Structures in Psychological Scales: A Response-Free Pathway to Efficient Simplification

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统心理量表简化（scale simplification）依赖于**response-based methods**（如 CFA、EFA、IRT、network psychometrics），这些方法需要大量被试数据，存在以下瓶颈：
- 数据获取成本高，尤其在跨文化适配、早期开发阶段；
- 易受样本偏差、测量情境影响，复现性差；
- 忽视了**item文本本身蕴含的语义结构信息**。

该研究提出：**量表项目的语言表达本身就编码了潜在构念的组织结构**，可以在无反应数据的前提下，通过自然语言处理技术挖掘这种“语义潜结构”（semantic latent structure），为高效、理论一致的量表简化提供新路径。

### 提出了什么新方法或新思路
提出了一种**响应无关**（response-free）、基于主题建模的量表简化框架，其核心流程如下：

1. **Encoding & Dimensionality Reduction**  
   使用大语言模型（LLM）对每个项目生成**contextual sentence embeddings**，并用 UMAP 进行降维。

2. **Density-Based Clustering**  
   采用 **HDBSCAN** 对嵌入向量进行聚类，自动发现语义簇（无需预设因子数）。

3. **Topic Modeling**  
   在每个聚类内，结合文本词频分析（c-TF-IDF），提取代表性关键词，形成可解释的“语义因子”（semantic factors）。

4. **Hierarchical Merging & Representative Item Selection**  
   将语义相近的簇合并，并基于**语义隶属概率**（membership probability）选择最具代表性的项目作为简化版量表条目。

### 相比现有方法的优势
| 维度 | 传统方法（CTT/FA/IRT/EGA） | 本文方法 |
|------|----------------------------|--------|
| 是否需要 response data | ✅ 是 | ❌ 否 |
| 是否可解释 | 高（因子载荷等） | 高（语义主题+关键词） |
| 是否需预设维度数 | 通常需要 | 不需要（密度聚类自动确定） |
| 是否利用 item 文本信息 | 间接（仅作标签） | 直接（核心输入） |
| 适用阶段 | 后期验证 | 早期设计、跨文化迁移 |

> ✅ **核心创新**：首次将**语义潜结构**形式化为一个可操作、可检验的“前测量结构”（pre-structure），为 scale construction 提供透明、可审计的前端工具。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个广泛使用的心理量表上进行了评估，覆盖不同构念领域和语言：

| Scale | 构念领域 | 条目数 | 因子数 | 数据来源 | 被试数 |
|-------|--------|--------|--------|----------|--------|
| **DASS** | Negative affect | 42 | 3 (Depression, Anxiety, Stress) | OpenPsychometrics.org | 39,775 |
| **IPIP** | Personality traits | 50 | 5 (Big Five) | OpenPsychometrics.org | 1,015,341* |
| **EPOCH-CN** | Well-being | 20 | 5 (Engagement, Perseverance, etc.) | Zeng et al. (2019) | 17,854 |

> *注：IPIP 数据用于评估时随机抽取 20,000 个有效样本。

### 实验设置和评估指标

#### 方法论评估（Methodological Evidence）
- **Alignment with theoretical factors**：使用 **Adjusted Rand Index (ARI)** 衡量语义聚类与理论因子的一致性。
- **Information fidelity**：
  - 子量表相关矩阵的 **Frobenius similarity**（衡量结构保留程度）；
  - 全量表与简化版子量表得分的 **cross-form correlation**（收敛效度）。

#### 心理计量学评估（Psychometric Evidence）
- **结构效度**：通过 **CFA** 检验简化版是否保持原多因子结构（CFI, TLI, RMSEA）。
- **内部一致性**：计算 **Cronbach’s α** 和 **corrected item-total correlation (CITC)**。
- **可视化**：使用 **t-SNE** 可视化语义空间，展示聚类与因子对应关系。

#### 参数设置
- **Embedding Model**: `qwen3-embedding-4b`（开源 LLM）
- **Dimension Reduction**: UMAP (`n_neighbors=3`, `n_components=5`)
- **Clustering**: HDBSCAN (`min_cluster_size=2`, `min_samples=1`)
- **Topic Modeling**: BERTopic（自动提取关键词）

### 基线方法对比
未直接与单一基线对比，而是从方法论类别上定位自身优势：

| 方法类别 | 是否需要响应数据 | 可解释性 | 是否假设固定维度 |
|--------|------------------|---------|----------------|
| CTT/FA/IRT | ✅ | 高 | 是 |
| EGA | ✅ | 中高 | 否 |
| Supervised ML | ✅ | 低 | 是 |
| Fixed-K 聚类 | ❌ | 中 | 是 |
| **本文方法** | ❌ | **高** | **否（可选）** |

> ✅ 本文方法是唯一同时满足“无响应数据 + 高可解释性 + 自动确定维度”的框架。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）语义聚类与理论因子高度一致
| Scale | ARI | 主导因子匹配率 |
|-------|-----|---------------|
| DASS | 0.745 | 92.9% |
| IPIP | 0.855 | 96.0% |
| EPOCH-CN | **1.000** | 100% |

> 表明语义结构能有效恢复已知构念结构，尤其在语义边界清晰的量表（如 EPOCH）表现极佳。

#### （2）简化后量表保持良好心理计量性质
| Scale | 原条目数 | 简化后条目数 | 减少比例 | 总 Cronbach’s α | CFA 拟合（CFI/TLI/RMSEA） |
|-------|--------|------------|----------|----------------|-----------------------------|
| DASS | 42 | 12 | **71.4%** | 0.899 | 0.956 / 0.943 / 0.064 |
| IPIP | 50 | 20 | **60.0%** | 0.705 | 0.860 / 0.833 / 0.059 |
| EPOCH-CN | 20 | 10 | **50.0%** | 0.875 | 0.983 / 0.970 / 0.041 |

> ✅ 所有简化版均优于单因子模型，且 CFI > 0.85，表明多维结构得以保留。

#### （3）结构信息高度保留
- **子量表相关矩阵相似度**（Normalized Frobenius similarity）：
  - DASS: 0.889
  - IPIP: 0.872
  - EPOCH: 0.861
- **全量表 vs 简化版子量表得分相关性**（对角线）：
  - DASS: r ≈ 0.92–0.95
  - IPIP: r ≈ 0.87–0.93
  - EPOCH: r ≈ 0.91–0.95

> 表明简化版不仅保留内部结构，还能准确反映原始构念水平。

### 与基线方法的对比结果
虽然没有直接运行其他方法，但作者指出：
- 相比 **fixed-K clustering**（如 K-means），本文方法无需预设 K，避免过分割或欠分割；
- 相比 **supervised ML**（如 SVM、Random Forest），本文方法更具理论透明性，不依赖预测目标；
- 相比 **response-based methods**，本文可在无数据时快速生成候选短表，降低开发成本。

### 消融实验结果
进行了**参数敏感性分析**，考察关键参数对结果稳定性的影响：

#### （1）每主题保留条目数的影响（IPIP）
| 每主题条目数 | CFI | TLI |
|-------------|-----|-----|
| 3 | 0.857 | 0.812 |
| **4** | **0.860** | **0.833** |
| 5 | 0.808 | 0.782 |
| 6 | 0.821 | 0.803 |
| ≥7 | ↓ 下降 |

> ✅ 保留 **3–4 条目/因子**即可获得最优拟合，更多条目反而因引入边缘语义而降低结构清晰度。

#### （2）主题数设置的影响
- 当设定 `topics=5`（与真实因子数一致）时，CFA 拟合最佳（CFI=0.875）；
- `nr_topics='auto'` 也能达到接近最优效果（CFI=0.857）；
- 若设为 4，因错误分离 OPN 项目导致拟合下降。

#### （3）参数扰动下的稳定性（Jaccard Similarity）
| Scale | 平均 Jaccard | 最低 Jaccard |
|-------|--------------|-------------|
| DASS | ~0.50 | 0.20 |
| IPIP | ~0.45 | 0.25 |
| EPOCH | 0.429（稳定） | 0.429 |

> 表明核心代表项目具有较高稳定性，尤其在语义清晰的量表中。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **语义潜结构是真实存在的**：仅从 item 文本即可恢复出与理论因子高度一致的语义聚类（ARI 达 0.75–1.00）。
2. ✅ **高效简化可行**：平均减少 **60.5%** 条目后，仍能保持良好的结构效度与内部一致性。
3. ✅ **结构信息得以保留**：简化版不仅还原因子结构，还保留了因子间相关模式与全量表得分的高度一致性。
4. ✅ **方法稳健且可解释性强**：通过关键词、隶属概率、可视化等方式，使简化过程透明可审计。

### 方法的局限性
1. **依赖语言表示质量**：不同 embedding model 或语言可能影响结果；目前仅验证了中英文版本。
2. **语义 ≠ 测量等价性**：语义相近的项目可能在难度、极端性、反应倾向上不同。
3. **反向计分题需预处理**：需手动统一表述方向，否则易形成“方法聚类”。
4. **超参数选择仍有自由度**：尽管推荐默认值，但参数变化会影响最终结果，需报告并做稳定性检查。

### 未来工作方向
1. **跨语言与跨文化泛化研究**：系统比较多语言量表的语义结构一致性，结合 measurement invariance 分析。
2. **人机协同简化流程**：构建“算法初筛 + 专家审核 + 数据验证”的三阶段 workflow。
3. **与 response-based 方法融合**：探索 semantic selection 与 IRT/EGA 的联合优化策略。
4. **开放工具生态建设**：推广 **One-Click Psychological Scale Simplification Tool**，支持非编程用户使用。

---

> 🎯 **总结一句话**：  
> 本文提出了一种**无需被试数据**的心理量表简化新范式，通过挖掘 item 文本中的**语义潜结构**，实现了高效、可解释、理论一致的短表构建，为心理测量学提供了强大的前端设计工具。

</details>

---

### 13. [Closing the Loop: A Control-Theoretic Framework for Provably Stable Time Series Forecasting with LLMs](https://arxiv.org/abs/2602.12756)

**Authors**: Xingyu Zhang, Hanyun Du, Zeen Song, Jianqi Zhang, Changwen Zheng, Wenwen Qiang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.12756v1  

#### Abstract
Large Language Models (LLMs) have recently shown exceptional potential in time series forecasting, leveraging their inherent sequential reasoning capabilities to model complex temporal dynamics. However, existing approaches typically employ a naive autoregressive generation strategy. We identify a c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Closing the Loop: A Control-Theoretic Framework for Provably Stable Time Series Forecasting with LLMs**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**

该论文针对当前基于 **Large Language Models (LLMs)** 的时间序列预测（Time Series Forecasting, TSF）中存在的一个**结构性缺陷**：**Error Accumulation（误差累积）**。

- 当前主流的 LLM-based TSF 方法采用 **autoregressive generation**（自回归生成）策略，在推理阶段模型递归地将自己生成的输出作为下一步输入。
- 这种“开环”（open-loop）模式导致早期微小的预测偏差在后续步骤中不断被放大，形成“雪球效应”（snowball effect），最终造成预测轨迹严重偏离真实数据流形，尤其在**长时域预测**（long-horizon forecasting）中表现尤为明显。
- 此现象源于训练（teacher forcing）与推理（free-running）之间的分布不一致，即 **Exposure Bias**。

---

### 🚀 **提出了什么新方法或新思路**

作者提出了一种全新的框架 —— **F-LLM (Feedback-driven LLM)**，其核心思想是将传统的开环预测过程重构为一个**闭环控制（closed-loop control）系统**，借鉴经典**控制理论**（control theory）中的反馈机制来主动抑制误差传播。

#### 主要组件包括：

1. **Dynamical Plant（动态系统主体）**  
   - 即冻结的 LLM 预测器，负责生成原始预测值。
   
2. **State Observer（状态观测器）**  
   - 一个轻量级的 **Residual Estimator**，用于估计当前步的预测残差（即误差），因为真实误差在推理时不可见（无 ground truth）。
   
3. **Feedback Controller（反馈控制器）**  
   - 将估计的残差注入到下一时刻的上下文中，对预测进行实时校正，实现“**in-loop correction**”。

此外，为了保证系统的稳定性，作者引入了 **Local Lipschitz Constraint** 来约束 LLM 输出对输入扰动的敏感度，确保其处于可被线性反馈控制的范围内。

---

### 🔍 **相比现有方法的优势**

| 维度 | F-LLM | 传统 LLM4TS 方法（如 AutoTimes、Time-LLM） |
|------|-------|----------------------------------------|
| **推理机制** | 闭环反馈（closed-loop） | 开环自回归（open-loop） |
| **误差处理** | 主动纠正（active error rejection） | 被动传播（error accumulation） |
| **理论保障** | 提供 **uniformly bounded error** 的数学证明 | 缺乏稳定性理论分析 |
| **模块设计** | 可插拔式轻量反馈模块（无需修改 LLM 权重） | 通常需调整 LLM 或添加额外参数 |
| **零样本能力保留** | 支持 zero-shot 跨域迁移且性能更优 | 零样本性能存在漂移风险 |

> ✅ **核心优势**：**在不重新训练 LLM 的前提下，通过增加一个极轻量的反馈模块，显著提升长期预测稳定性，并提供理论上的误差有界保证。**

---

## 2. **核心实验方法和设置**

### 📊 **使用的数据集**

共使用 **7 个真实世界多变量时间序列数据集**，涵盖多个领域：

| 数据集 | 类型 | 变量数 (Dim) | 时间分辨率 | 应用场景 |
|--------|------|-------------|------------|----------|
| **ETTh1/ETTh2**, **ETTm1/ETTm2** | 多变量 | 7 | 小时 / 15分钟 | 电力负荷与变压器油温 |
| **ECL** | 多变量 | 321 | 小时 | 家庭用电消耗 |
| **Traffic** | 多变量 | 862 | 小时 | 高速公路占有率 |
| **Weather** | 多变量 | 21 | 10分钟 | 气象数据 |
| **M4** | 单变量 | 1 | 多种频率 | 商业经济预测竞赛数据 |
| **M3** | 单变量 | 1 | 多种频率 | 时间序列预测基准 |

> 所有数据按时间顺序划分 train/validation/test，防止数据泄露。

---

### ⚙️ **实验设置和评估指标**

- **预测长度（Horizon）**：{96, 192, 336, 720}
- **回看窗口（Lookback Length）**：固定为 672（除 M3/M4 按两倍 horizon 设置）
- **评估指标**：
  - **MSE**（Mean Squared Error）
  - **MAE**（Mean Absolute Error）
  - **SMAPE**（Symmetric Mean Absolute Percentage Error，用于 zero-shot 实验）

- **模型架构基础**：
  - 使用 **LLaMA-2-7B** 作为冻结 backbone。
  - 添加 patch-based embedding 和 inverse projection 层。
  - Residual Estimator 为单层线性网络。

- **训练策略**：两阶段课程学习（Curriculum Learning）
  1. **Stage 1**: 固定残差估计器，仅训练投影层 + 施加 Local Lipschitz 正则化。
  2. **Stage 2**: 冻结投影层，训练残差估计器以拟合历史误差模式。

---

### 🆚 **基线方法对比**

#### 包括以下几类代表性方法：

| 类别 | 方法 |
|------|------|
| **非 LLM 深度模型** | PatchTST, DLinear, FEDformer, TimesNet, iTransformer, TimeMixer++, SimpleTM |
| **LLM-based TSF** | AutoTimes, Time-LLM, FPT, UniTime, LVICL |
| **其他** | Informer, Reformer, NSformer |

> 大部分结果来自 [149]（AutoTimes）和 [167]（LVICL）的公开报告。

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据（Table 1 & Table 8）**

在 **平均 MSE/MAE** 上，F-LLM 在多数数据集上取得最优表现：

| Dataset | F-LLM (MSE) | Best Baseline (MSE) | 提升幅度 |
|--------|-------------|---------------------|---------|
| ETTh1 | **0.376** | 0.389 (AutoTimes) | ↓ **3.3%** |
| ETTh2 | **0.317** | 0.326 (LVICL) | ↓ **2.8%** |
| ETTm1 | **0.319** | 0.328 (LVICL) | ↓ **2.7%** |
| ETTm2 | **0.234** | 0.239 (LVICL) | ↓ **2.1%** |
| ECL | **0.148** | 0.158 (LVICL) | ↓ **6.3%** |
| Weather | **0.215** | 0.222 (LVICL) | ↓ **3.1%** |
| Traffic | **0.364** | 0.370 (LVICL) | ↓ **1.6%** |

> ✅ 总体来看，F-LLM 在最具挑战性的长时域任务中平均降低 MSE 达 **5.6%**。

---

### 🔁 **与基线方法的对比结果**

- F-LLM 显著优于所有对比方法，尤其是在 **ECL 和 ETTh 系列**等复杂多变量场景中。
- 相比最强的 LLM4TS 方法（如 AutoTimes、LVICL），F-LLM 不仅性能更好，而且具备更强的**轨迹一致性**（trajectory alignment），避免了明显的漂移。
- 在非 LLM 方法中，也全面超越 DLinear、PatchTST 等先进模型。

---

### 🔧 **消融实验结果（Ablation Study, Table 3）**

验证了各模块的有效性（在 ETTh1 和 Weather 上）：

| 模型变体 | ETTh1 (Pred-720, MSE) | Weather (Pred-720, MSE) |
|--------|------------------------|--------------------------|
| F-LLM (Full) | **0.395** | **0.305** |
| w/o Feedback | 0.417 (+5.6%) | 0.320 (+4.9%) |
| w/o LLM (MLP 替代) | 0.438 (+10.9%) | 0.324 (+6.2%) |

> 结论：
> - 移除 **feedback 模块** 导致性能下降明显，说明误差校正机制至关重要。
> - 移除 **LLM** 后性能大幅下滑，表明 LLM 的强泛化能力仍是基础。
> - 二者结合才能发挥最大效能。

---

### 🔄 **通用性与鲁棒性分析**

- **跨 LLM backbone 泛化性测试**（Table 4）：
  - 在 GPT-2、OPT-6.7B、LLaMA-7B 上均能稳定提效。
  - 表明 F-LLM 是 **model-agnostic** 的，适用于任意 decoder-only LLM。

- **超参数敏感性分析**（Figure 4）：
  - 对 lookback length 和 patch length 具有良好鲁棒性。
  - 性能增益不是由特定 tokenization 设计带来的。

- **效率分析**（Figure 5）：
  - 仅引入 <5% 的额外推理开销。
  - 可训练参数极少（主要集中在 projection layers 和 residual estimator），远低于 LoRA 微调方案。

- **与 LoRA 对比**（Table 6）：
  - LoRA 微调带来一定改进，但不如 F-LLM 显著。
  - 特别是在长周期预测中，F-LLM 的优势更加突出。

---

## 4. **关键结论和发现**

### ✅ **主要发现**

1. **理论层面**：
   - 首次从**控制理论视角**形式化分析了 LLM 自回归预测中的误差传播机制。
   - 证明标准 open-loop 推理会导致 **exponential error growth**（指数级误差增长）。
   - 提出并证明：通过引入 feedback 控制，可在满足局部 Lipschitz 条件下实现 **uniformly bounded error**（一致有界误差），为 LLM 预测提供了**可证明的稳定性保障**。

2. **方法层面**：
   - 提出 **F-LLM** 框架，首次将动态误差校正机制应用于 LLM-based TSF。
   - 构建了一个“Plant-Observer-Controller”闭环结构，实现了 in-loop error correction。
   - 采用两阶段训练策略有效解耦表示学习与控制学习。

3. **实验层面**：
   - 在多个真实数据集上显著优于 SOTA 方法，特别是在长时域预测中。
   - 保持了 LLM 的 **zero-shot generalization** 能力，在跨域迁移任务中同样领先（Table 2）。
   - 模块轻量、高效、即插即用，适合部署于已有 LLM 推理流程中。

---

### ⚠️ **局限性**

- **依赖高质量的历史误差模式**：Residual Estimator 的效果受限于训练阶段能否捕捉到系统性偏差。
- **Local Lipschitz Constraint 的实现方式较简单**：目前仅在 projection head 上施加正则项，未深入优化整个 LLM 的动力学特性。
- **尚未探索多模态反馈或其他 observer 结构**：当前 observer 仅为线性估计器，可能限制表达能力。

---

### 🔮 **未来工作方向**

1. 设计更复杂的 **nonlinear observers** 或引入 attention 机制增强残差建模能力。
2. 将 F-LLM 拓展至 **probabilistic forecasting** 场景，支持不确定性量化。
3. 探索与其他 LLM adapter（如 LoRA、Adapter）联合使用，进一步释放潜力。
4. 将该控制框架推广至其他序列生成任务（如视频预测、语音合成）中解决 exposure bias 问题。

---

## ✅ **总结一句话**

> 本文开创性地将**控制理论**引入 LLM 时间序列预测，提出 **F-LLM** 框架，通过构建闭环反馈系统，不仅实证显著缓解了误差累积问题，还首次为 LLM 预测提供了**可证明的稳定性理论保障**，兼具高性能、高效率与强泛化能力。

</details>

---

### 14. [Eventizing Traditionally Opaque Binary Neural Networks as 1-safe Petri net Models](https://arxiv.org/abs/2602.13128)

**Authors**: Mohamed Tarraf, Alex Chan, Alex Yakovlev, Rishad Shafik  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.13128v1  

#### Abstract
Binary Neural Networks (BNNs) offer a low-complexity and energy-efficient alternative to traditional full-precision neural networks by constraining their weights and activations to binary values. However, their discrete, highly non-linear behavior makes them difficult to explain, validate and formal...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Eventizing Traditionally Opaque Binary Neural Networks as 1-safe Petri net Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
Binary Neural Networks (BNNs) 虽然在能效和计算效率上具有显著优势，但由于其**离散化、高度非线性的行为**，导致模型内部决策过程不透明（opaque），难以进行解释、验证和形式化分析。这限制了其在安全关键领域（如卫星控制、医疗监测）的应用。

传统可解释性方法（如 SHAP、LIME）仅提供事后相关性近似，而形式化验证方法（如 SMT、凸松弛）难以捕捉 BNN 中细粒度的事件依赖关系。

### 提出了什么新方法或新思路
本文提出了一种基于 **Petri Net (PN)** 的框架，将 BNN 的推理和训练过程“**事件化**”（eventizing），即建模为由因果驱动的并发事件系统。具体创新包括：

- 将 BNN 的核心操作（激活函数、梯度计算、权重更新等）分解为模块化的 PN 段（blueprint-like PN segments）。
- 构建完整的系统级 PN 模型，显式刻画 BNN 内部的**因果关系、并发性、状态演化和控制流顺序**。
- 使用 **Workcraft** 工具链实现 PN 的构建、仿真与自动化验证（通过 Mpsat 和 Petrify 后端）。

### 相比现有方法的优势
| 方面 | 传统方法 | 本论文方法 |
|------|--------|-----------|
| 可解释性 | 黑盒近似（post-hoc） | 白盒机制解释（mechanistic view） |
| 形式化能力 | 难以处理离散非线性 | 支持形式化验证（1-safeness, deadlock-freeness 等） |
| 因果建模 | 缺乏显式因果结构 | 显式建模事件间的因果依赖 |
| 验证粒度 | 整体输出验证为主 | 支持事件级（event-by-event）的行为验证 |

> ✅ **核心优势**：首次实现了对 BNN 推理与训练全过程的形式化、可验证、事件驱动建模，使原本“黑盒”的 BNN 成为**透明且可验证的事件系统**。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 主要用于说明性示例的是一个简单的 **2-input XOR 数据集**：  
  `{00 → -1, 01 → 1, 10 → 1, 11 → -1}`  
  该数据集用于演示整个 PN 模型的构建与运行流程。
- 在扩展复杂度分析中，估算了以下真实世界数据集上的模型规模：
  - **KWS6**（Keyword Spotting）
  - **CIFAR2**
  - **MNIST**

### 实验设置和评估指标
#### 实验平台
- 使用 **Workcraft** 工具进行 PN 建模、交互式仿真与自动验证。
- 后端工具：**Petrify**（逻辑综合）、**Mpsat**（验证与反例生成）。

#### 评估维度
| 类别 | 具体指标 |
|------|---------|
| **结构性质验证** | 1-safeness（安全性）、deadlock-freeness（无死锁）、mutual exclusion（互斥）、reachability（可达性） |
| **行为正确性** | 与参考软件实现的 BNN 输出轨迹对比（loss, prediction, gradients） |
| **模型规模与复杂度** | Places、Transitions、Arcs 数量统计；总模型大小（Total Size） |
| **可扩展性分析** | 不同网络结构（输入/隐藏/输出神经元数）下的组合爆炸趋势 |

#### 基线方法对比
- **参考 BNN 实现**：一个标准的 Python 实现的 BNN，使用相同的架构（2 输入层、2 隐藏层、1 输出层）、Hinge Loss、SGD 优化器、学习率 0.6。
- 对比方式：逐操作记录中间值（权重、激活、损失、梯度等），并与 PN 模型中的 metric instrument 输出进行比对。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）模型规模（见 Table II）
| 组件 | Places | Transitions | Arcs | Total Size |
|------|--------|------------|------|------------|
| 单个隐藏神经元 | 238 | 409 | 3,109 | 3,756 |
| 权重更新（最复杂部分） | 1,256 | 1,912 | 10,642 | 13,810 |
| 完整 BNN-PN 模型 | **8,243** | **12,598** | **71,370** | **92,211** |

> ⚠️ 权重更新是最大瓶颈，占总规模的 ~15%，因其涉及 IEEE-754 浮点运算的完整建模。

#### （2）不同数据集的估计复杂度（Table III）
| Dataset | Total Size (×10⁹) |
|--------|------------------|
| KWS6 (largest config) | ~3.0 × 10⁹ |
| CIFAR2 (largest config) | ~6.8 × 10⁹ |
| MNIST (largest config) | ~5.4 × 10⁹ |

> 表明存在明显的**组合爆炸**问题，尤其在全连接层中随神经元数量呈平方增长。

#### （3）行为验证结果
- **形式化验证结果**（Table I）：
  - 所有段、组件和系统级模型均通过：
    - **1-safeness**（每个 place 最多一个 token）
    - **Deadlock-freeness**
    - **Causal sequencing 正确性**（如：预测 → 损失 → 梯度 → 更新）
    - **Progressiveness**（循环处理所有数据点）
  - 仅部分不可逆（partial irreversibility），因训练过程中权重持续演化。

- **与参考 BNN 的行为对比**（Fig. 19）：
  - 初始阶段（前 3 个 epoch）：PN 模型与参考模型 loss 轨迹几乎一致。
  - 第 3–15 epoch：出现轻微偏差，可能源于浮点舍入或随机初始化差异。
  - 15 epoch 后：PN 模型 loss 缓慢下降，最终在 100 epoch 时达到比参考模型低约 **10% 的 loss**。
  - 结论：**PN 模型功能正确，且表现出相似甚至略优的学习动态**。

#### （4）消融实验（隐含）
虽然未明确命名“ablation study”，但文中通过分层验证体现了模块化设计的有效性：
- 各 PN 段独立验证通过 → 组合成组件仍满足性质 → 系统级集成后依然安全可靠。
- 特别地，引入 **arbitration places** 和 **proxy places** 有效管理了并发与可视化复杂度。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **BNN 可被完全事件化建模为 1-safe Petri Net**，其推理与训练过程可以被分解为具有明确因果结构的事件序列。
2. ✅ 所提出的 PN 模型能够通过严格的**形式化验证**，确保安全性、无死锁性和正确的因果顺序。
3. ✅ 与参考 BNN 的行为高度一致，证明了该建模方法的**功能保真度**。
4. 🔍 存在**组合爆炸**问题，尤其是浮点权重更新部分导致模型规模急剧上升。
5. 🔄 模型支持**事件级仿真与调试**，可用于追踪故障传播路径、理解梯度流动机制。

### 方法的局限性
| 局限性 | 说明 |
|-------|------|
| **可扩展性差** | 当前模型为全展开式（fully instantiated），无法直接应用于大型网络（如 ResNet）。 |
| **浮点建模开销大** | IEEE-754 运算建模极其复杂，是主要性能瓶颈。 |
| **缺乏参数共享机制** | 当前 PN 段未实现模板复用，相同操作需重复建模。 |
| **仅支持 SGD** | 更复杂的优化器（如 Adam）因涉及动量变量尚未支持。 |
| **暂无偏置项建模** | 当前模型省略了 bias terms。 |

### 未来工作方向
1. **增强模型通用性**：
   - 引入 **bias terms**
   - 支持更多 activation functions、loss functions 和 optimizers（如 Adam）
2. **提升自动化程度**：
   - 开发 **Workcraft 插件**，允许用户输入网络架构自动生成 PN 模型
3. **优化表示与性能**：
   - 探索更高效的数据结构加速 PN 仿真
   - 引入 **templating、parameter sharing 和 hierarchical reuse** 以缓解组合爆炸
4. **向硬件映射延伸**：
   - 利用 PN 的异步特性生成低功耗异步电路控制器
   - 应用于嵌入式 AI 芯片的可信设计流程

---

## 总结

> 本文成功将传统的“黑盒”BNN 转化为**透明、可验证、事件驱动的 1-safe Petri Net 模型**，填补了机器学习模型与形式化方法之间的鸿沟。尽管面临可扩展性挑战，但它为构建**高可信、可解释、安全关键型 AI 系统**提供了全新的理论基础与工程路径。

</details>

---

### 15. [Regularized Meta-Learning for Improved Generalization](https://arxiv.org/abs/2602.12469)

**Authors**: Noor Islam S. Mohammad, Md Muntaqim Meherab  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12469v1  

#### Abstract
Deep ensemble methods often improve predictive performance, yet they suffer from three practical limitations: redundancy among base models that inflates computational cost and degrades conditioning, unstable weighting under multicollinearity, and overfitting in meta-learning pipelines. We propose a ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Regularized Meta-Learning for Improved Generalization**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
该论文针对**深度集成学习**（deep ensembles）在实际应用中的三大挑战：
- **冗余性**（Redundancy）：大量基础模型之间存在高度相关性，导致计算成本增加、条件数恶化；
- **不稳定性**（Instability）：多重共线性（multicollinearity）导致元学习权重估计不稳定；
- **过拟合风险**（Overfitting）：高维预测空间下，元学习器容易在训练过程中过拟合。

这些问题使得传统 stacking 方法在大规模模型池中表现不佳，尤其是在 AutoML 和生产部署场景中。

---

### **提出了什么新方法或新思路**
作者提出了一种**正则化元学习框架**（Regularized Meta-Learning Framework），包含四个阶段的流水线：

1. **冗余投影**（Redundancy Projection）  
   - 使用多指标去重策略（multi-metric de-duplication），基于 `T_corr=0.95` 的相关性和 `T_mse` 的误差阈值移除近似共线的预测变量；
   - 显著降低元设计矩阵的有效条件数（effective condition number）。

2. **统计元特征增强**（Statistical Meta-Feature Augmentation）  
   - 构造集成统计量作为新特征，如：
     - `ensemble_mean`, `ensemble_std`, `median`, `range`
     - 交互项（interaction terms）如 `mean * std`
   - 捕获原始预测列无法表达的高阶结构信息。

3. **交叉验证的正则化元学习**（Cross-Validated Regularized Meta-Learning）  
   - 使用 Ridge、Lasso 和 ElasticNet 进行元模型训练；
   - 内层嵌套交叉验证选择超参数，确保惩罚不变性（penalty invariance）。

4. **逆RMSE融合**（Inverse-RMSE Blending）  
   - 对多个正则化元模型（Ridge/Lasso/ElasticNet）的结果进行加权融合；
   - 权重为各模型 OOF RMSE 的倒数平方，提升鲁棒性并减少正则器选择带来的方差。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **性能** | 超越简单平均、标准 Ridge stacking，与贪婪爬山法（greedy hill climbing）相当甚至略优 |
| **效率** | 运行时间仅为 hill climbing 的 **1/4**（提速 4×），适合生产部署 |
| **稳定性** | 显著改善条件数（↓53.7%），降低估计方差，提高跨折一致性 |
| **可解释性** | 线性元模型保留可解释性，优于神经网络元学习器 |
| **扩展性** | 支持大规模模型池，在 AutoML 场景中更具实用性 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Playground Series S6E1 回归任务**
  - 样本数：`N_train = 100,000`
  - 目标范围：`[0, 100]`
  - 数据混合合成与真实分布，支持鲁棒性分析
- **缩放实验**：使用分层采样的子集 `{0.1, 0.25, 0.5, 0.75} × N_train`

---

### **实验设置和评估指标**

#### **基础模型池（Base Model Pool）**
- 总计 `K = 72` 个异构模型
- 包括五类算法：
  - Gradient Boosting（XGBoost, LightGBM）
  - Neural Networks
  - Linear Models
  - Tree Ensembles
  - Kernel Methods
- 使用 **10折分层交叉验证** 构建无泄漏的 OOF 预测矩阵

#### **评估指标**
| 指标 | 说明 |
|------|------|
| **RMSE** | 主要指标（越低越好） |
| MAE | 平均绝对误差 |
| R² | 决定系数 |
| Pearson Correlation | 预测与真实值的相关性 |
| **统计显著性检验** | 成对双尾 t 检验 + Bonferroni 校正（α=0.001） |
| **置信区间** | 1,000 次 bootstrap 重采样得到 95% CI |

#### **复现性保障**
- 所有实验固定随机种子 `seed=42`
- 超参数搜索网格明确列出（如 Ridge: λ ∈ [10⁻³, 10], 50 个值）

---

### **基线方法对比**
| 基线方法 | 描述 |
|---------|------|
| **Best Single Model** | 表现最好的单个模型 |
| **Simple Average (All)** | 所有模型等权平均 |
| **Weighted Average (Performance)** | 按 1/RMSE 加权平均 |
| **Vanilla Stacking (OLS / Ridge)** | 普通线性/岭回归 stacking |
| **Greedy Hill Climbing** | 贪婪前向选择优化权重 |
| **Neural Meta-Learner (MLP)** | 浅层 MLP 元学习器用于比较 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 1）**

| 方法 | RMSE (OOF) | ΔRMSE vs Hill Climbing | #Models | Runtime (s) |
|------|-----------|------------------------|--------|-------------|
| Best Single Model | 9.247 | +0.665 | 1 | — |
| Simple Average | 8.894 | +0.312 | 45 | 3.2 |
| Weighted Average | 8.756 | +0.174 | 45 | 12.7 |
| Vanilla Ridge Stacking | 8.627 | +0.045 | 45 | 189.3 |
| **Greedy Hill Climbing** | **8.603** | — | **28** | **2,841.6** |
| **Proposed Full Pipeline** | **8.582** | **-0.021** | **37** | **712.8** |

> ✅ **结论**：提出的完整流程达到 **RMSE = 8.582**，相对 hill climbing 提升 0.24%，虽未达统计显著（p=0.639），但在更大模型集合下实现更高效运行。

---

### **与基线方法的对比结果**
- 相比 **简单平均**：RMSE ↓3.51% （8.894 → 8.582）
- 相比 **最佳单模型**：RMSE ↓7.19%
- 相比 **标准 Ridge stacking**：RMSE ↓0.52%
- 相比 **greedy hill climbing**：
  - 性能相近（+0.021 RMSE 更优）
  - 模型保留更多（37 vs 28）
  - **运行速度快 4 倍**（712.8s vs 2,841.6s）

> 📈 图 2 显示该方法位于 **accuracy-runtime Pareto frontier 上游**，是部署友好的选择。

---

### **消融实验结果（Ablation Study, Table 3）**

逐步添加组件的累计效果（以 Ridge stacking 为基准）：

| 阶段 | RMSE 下降 | 贡献比例 |
|------|----------|--------|
| 基线（仅 Ridge） | 7.183 | — |
| + 多指标去重 | -0.089 | -1.24% |
| + 方差剪枝 | -0.038 | -0.53% |
| + 统计聚合特征 | **-0.135** | **-1.88%** |
| + 交互特征 | -0.048 | -0.67% |
| + 元集成融合 | -0.131 | -1.82% |
| **总计改进** | **↓0.441 RMSE** | **↓6.13%** |

> 🔍 **发现**：
> - **统计聚合特征**贡献最大，说明元特征工程至关重要；
> - **冗余投影**带来最稳定增益，验证其对条件数的改善作用；
> - 各模块呈**累进式改进**，证明框架设计合理性。

---

### **其他重要实验发现**

#### **冗余投影效果（Table 2 & Figure 3）**
- 初始模型池：45 个 → 投影后保留 37 个（压缩率 48.6%）
- 相关性 > 0.95 且性能较差的模型被移除（平均 ΔRMSE = +0.105）
- 条件数从 **κ ≈ 847 → κ ≈ 392**（↓53.7%），显著提升数值稳定性

#### **正则化路径分析（Figure 4）**
- Ridge 在宽 λ 范围内表现稳定（λ ∈ [0.6, 1.5]）
- Lasso 实现 68% 稀疏性，权重接近零但仍保持精度
- ElasticNet 提供稀疏性与稳定性之间的平衡

#### **计算效率（Table 5）**
- 尽管冗余投影复杂度为 O(K²N)，但由于 K_eff 减小，下游任务加速明显
- 最终推理成本为 O(K_eff)，每样本开销低，利于线上服务

---

## **4. 关键结论和发现**

### **主要发现**
1. **冗余控制是关键**：通过 correlation + MSE 双阈值去重，有效缓解多重共线性，提升 meta-learning 的 conditioning。
2. **统计元特征极具价值**：集成均值、标准差、交互项等能捕捉高阶模式，显著优于仅用原始预测列。
3. **正则化 + 融合提升稳定性**：Ridge/Lasso/ElasticNet 结果相近，融合后进一步降低方差。
4. **效率-精度权衡优越**：在接近最优性能的同时，大幅缩短训练时间，更适合生产环境。

---

### **方法的局限性**
1. **依赖基础模型多样性**：若所有模型高度相似，冗余投影可能过度压缩；
2. **冗余投影复杂度高**：O(K²N) 不适用于超大规模模型池（如 K > 1000），需引入近似相似搜索；
3. **对分布偏移敏感**：尚未系统测试严重分布漂移下的表现；
4. **当前聚焦回归任务**：分类任务需引入校准机制（calibration-aware extension）。

---

### **未来工作方向**
1. **自适应正则化调度**：开发相关性感知的动态正则化策略；
2. **不确定性量化**：结合 conformal prediction 或 Bayesian meta-learning 提供置信区间；
3. **多任务/多域扩展**：推广至 multi-task 和 domain-adaptive ensemble setting；
4. **在线非平稳流处理**：支持 streaming data 中的动态模型更新与融合；
5. **开源实现推动落地**：发布可复现、面向部署的代码库，促进工业界采纳。

---

> ✅ **总体评价**：本文提出的 **regularized meta-learning** 框架为高维集成系统提供了一个**稳定、高效、可解释且易于部署**的 stacking 解决方案，在 MLSys（机器学习系统）场景中具有重要实践意义。

</details>

---

### 16. [Multi-Agent Model-Based Reinforcement Learning with Joint State-Action Learned Embeddings](https://arxiv.org/abs/2602.12520)

**Authors**: Zhizun Wang, David Meger  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12520v1  

#### Abstract
Learning to coordinate many agents in partially observable and highly dynamic environments requires both informative representations and data-efficient training. To address this challenge, we present a novel model-based multi-agent reinforcement learning framework that unifies joint state-action rep...

---

### 17. [Flow-Factory: A Unified Framework for Reinforcement Learning in Flow-Matching Models](https://arxiv.org/abs/2602.12529)

**Authors**: Bowen Ping, Chengyou Jia, Minnan Luo, Hangwei Qian, Ivor Tsang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12529v1  

#### Abstract
Reinforcement learning has emerged as a promising paradigm for aligning diffusion and flow-matching models with human preferences, yet practitioners face fragmented codebases, model-specific implementations, and engineering complexity. We introduce Flow-Factory, a unified framework that decouples al...

---

### 18. [Efficient Personalized Federated PCA with Manifold Optimization for IoT Anomaly Detection](https://arxiv.org/abs/2602.12622)

**Authors**: Xianchao Xiu, Chenyi Huang, Wei Zhang, Wanquan Liu  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12622v1  

#### Abstract
Internet of things (IoT) networks face increasing security threats due to their distributed nature and resource constraints. Although federated learning (FL) has gained prominence as a privacy-preserving framework for distributed IoT environments, current federated principal component analysis (PCA)...

---

### 19. [Resource-Efficient Gesture Recognition through Convexified Attention](https://arxiv.org/abs/2602.13030)

**Authors**: Daniel Schwartz, Dario Salvucci, Yusuf Osmanlioglu, Richard Vallett, Genevieve Dion, Ali Shokoufandeh  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13030v1  

#### Abstract
Wearable e-textile interfaces require gesture recognition capabilities but face severe constraints in power consumption, computational capacity, and form factor that make traditional deep learning impractical. While lightweight architectures like MobileNet improve efficiency, they still demand thous...

---

### 20. [Can I Have Your Order? Monte-Carlo Tree Search for Slot Filling Ordering in Diffusion Language Models](https://arxiv.org/abs/2602.12586)

**Authors**: Joshua Ong Jun Leang, Yu Zhao, Mihaela C\u{a}t\u{a}lina Stoian, Wenda Li, Shay B. Cohen, Eleonora Giunchiglia  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.12586v1  

#### Abstract
While plan-and-infill decoding in Masked Diffusion Models (MDMs) shows promise for mathematical and code reasoning, performance remains highly sensitive to slot infilling order, often yielding substantial output variance. We introduce McDiffuSE, a framework that formulates slot selection as decision...

---

### 21. [Adaptive Structured Pruning of Convolutional Neural Networks for Time Series Classification](https://arxiv.org/abs/2602.12744)

**Authors**: Javidan Abdullayev, Maxime Devanne, Cyril Meyer, Ali Ismail-Fawaz, Jonathan Weber, Germain Forestier  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.12744v1  

#### Abstract
Deep learning models for Time Series Classification (TSC) have achieved strong predictive performance but their high computational and memory requirements often limit deployment on resource-constrained devices. While structured pruning can address these issues by removing redundant filters, existing...

---

### 22. [Uncertainty in Federated Granger Causality: From Origins to Systemic Consequences](https://arxiv.org/abs/2602.13004)

**Authors**: Ayush Mohanty, Nazal Mohamed, Nagi Gebraeel  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.13004v1  

#### Abstract
Granger Causality (GC) provides a rigorous framework for learning causal structures from time-series data. Recent federated variants of GC have targeted distributed infrastructure applications (e.g., smart grids) with distributed clients that generate high-dimensional data bound by data-sovereignty ...

---

### 23. [BrowseComp-$V^3$: A Visual, Vertical, and Verifiable Benchmark for Multimodal Browsing Agents](https://arxiv.org/abs/2602.12876)

**Authors**: Huanyao Zhang, Jiepeng Zhou, Bo Li, Bowen Zhou, Yanzhe Dan, Haishan Lu, Zhiyong Cao, Jiaoyang Chen, Yuqian Han, Zinan Sheng, Zhengwei Tao, Hao Liang, Jialong Wu, Yang Shi, Yuanpeng He, Jiaye Lin, Qintong Zhang, Guochen Yan, Runhao Zhao, Zhengpin Li, Xiaohan Yu, Lang Mei, Chong Chen, Wentao Zhang, Bin Cui  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12876v1  

#### Abstract
Multimodal large language models (MLLMs), equipped with increasingly advanced planning and tool-use capabilities, are evolving into autonomous agents capable of performing multimodal web browsing and deep search in open-world environments. However, existing benchmarks for multimodal browsing remain ...

---

### 24. [A Theoretical Analysis of Mamba's Training Dynamics: Filtering Relevant Features for Generalization in State Space Models](https://arxiv.org/abs/2602.12499)

**Authors**: Mugunthan Shandirasegaran, Hongkang Li, Songyang Zhang, Meng Wang, Shuai Zhang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12499v1  

#### Abstract
The recent empirical success of Mamba and other selective state space models (SSMs) has renewed interest in non-attention architectures for sequence modeling, yet their theoretical foundations remain underexplored. We present a first-step analysis of generalization and learning dynamics for a simpli...

---

### 25. [RelBench v2: A Large-Scale Benchmark and Repository for Relational Data](https://arxiv.org/abs/2602.12606)

**Authors**: Justin Gu, Rishabh Ranjan, Charilaos Kanatsoulis, Haiming Tang, Martin Jurkovic, Valter Hudovernik, Mark Znidar, Pranshu Chaturvedi, Parth Shroff, Fengyu Li, Jure Leskovec  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12606v1  

#### Abstract
Relational deep learning (RDL) has emerged as a powerful paradigm for learning directly on relational databases by modeling entities and their relationships across multiple interconnected tables. As this paradigm evolves toward larger models and relational foundation models, scalable and realistic b...

---

### 26. [Physics-Informed Laplace Neural Operator for Solving Partial Differential Equations](https://arxiv.org/abs/2602.12706)

**Authors**: Heechang Kim, Qianying Cao, Hyomin Shin, Seungchul Lee, George Em Karniadakis, Minseok Choi  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12706v1  

#### Abstract
Neural operators have emerged as fast surrogate solvers for parametric partial differential equations (PDEs). However, purely data-driven models often require extensive training data and can generalize poorly, especially in small-data regimes and under unseen (out-of-distribution) input functions th...

---

### 27. [R-Diverse: Mitigating Diversity Illusion in Self-Play LLM Training](https://arxiv.org/abs/2602.13103)

**Authors**: Gengsheng Li, Jinghan He, Shijie Wang, Dan Zhang, Ruiqi Liu, Renrui Zhang, Zijun Yao, Junfeng Fang, Haiyun Guo, Jinqiao Wang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.13103v1  

#### Abstract
Self-play bootstraps LLM reasoning through an iterative Challenger-Solver loop: the Challenger is trained to generate questions that target the Solver's capabilities, and the Solver is optimized on the generated data to expand its reasoning skills. However, existing frameworks like R-Zero often exhi...

---

### 28. [ProbeLLM: Automating Principled Diagnosis of LLM Failures](https://arxiv.org/abs/2602.12966)

**Authors**: Yue Huang, Zhengzhe Jiang, Yuchen Ma, Yu Jiang, Xiangqi Wang, Yujun Zhou, Yuexing Hao, Kehan Guo, Pin-Yu Chen, Stefan Feuerriegel, Xiangliang Zhang  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12966v1  

#### Abstract
Understanding how and why large language models (LLMs) fail is becoming a central challenge as models rapidly evolve and static evaluations fall behind. While automated probing has been enabled by dynamic test generation, existing approaches often discover isolated failure cases, lack principled con...

---

### 29. [SciAgentGym: Benchmarking Multi-Step Scientific Tool-use in LLM Agents](https://arxiv.org/abs/2602.12984)

**Authors**: Yujiong Shen, Yajie Yang, Zhiheng Xi, Binze Hu, Huayu Sha, Jiazheng Zhang, Qiyuan Peng, Junlin Shang, Jixuan Huang, Yutao Fan, Jingqi Tong, Shihan Dou, Ming Zhang, Lei Bai, Zhenfei Yin, Tao Gui, Xingjun Ma, Qi Zhang, Xuanjing Huang, Yu-Gang Jiang  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12984v1  

#### Abstract
Scientific reasoning inherently demands integrating sophisticated toolkits to navigate domain-specific knowledge. Yet, current benchmarks largely overlook agents' ability to orchestrate tools for such rigorous workflows. To bridge this gap, we introduce SciAgentGym, a scalable interactive environmen...

---

### 30. [Order Matters in Retrosynthesis: Structure-aware Generation via Reaction-Center-Guided Discrete Flow Matching](https://arxiv.org/abs/2602.13136)

**Authors**: Chenguang Wang, Zihan Zhou, Lei Bai, Tianshu Yu  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.13136v1  

#### Abstract
Template-free retrosynthesis methods treat the task as black-box sequence generation, limiting learning efficiency, while semi-template approaches rely on rigid reaction libraries that constrain generalization. We address this gap with a key insight: atom ordering in neural representations matters. ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
