# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-10 06:51:50 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [MedVerse: Efficient and Reliable Medical Reasoning via DAG-Structured Parallel Execution](https://arxiv.org/abs/2602.07529)

**Authors**: Jianwen Chen, Xinyu Yang, Peng Xia, Arian Azarang, Yueh Z Lee, Gang Li, Hongtu Zhu, Yun Li, Beidi Chen, Huaxiu Yao  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2602.07529v1  

#### Abstract
Large language models (LLMs) have demonstrated strong performance and rapid progress in a wide range of medical reasoning tasks. However, their sequential autoregressive decoding forces inherently parallel clinical reasoning, such as differential diagnosis, into a single linear reasoning path, limit...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MedVerse: Efficient and Reliable Medical Reasoning via DAG-Structured Parallel Execution 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前基于 **Large Language Models (LLMs)** 和 **Chain-of-Thought (CoT)** 的医疗推理模型普遍采用**自回归（autoregressive, AR）序列生成**方式，这种线性推理模式存在三大根本性缺陷：
- **准确性受限**：强制将本应并行的临床鉴别诊断（differential diagnosis）压缩为单一路径，容易因早期错误假设导致“上下文污染”（contextual pollution），忽略其他合理诊断路径。
- **效率低下**：串行解码带来高延迟（latency），难以满足实时临床决策支持需求。
- **可解释性差**：传统 CoT 缺乏显式的因果依赖结构，推理过程不透明。

此外，现有并行推理框架（如 Tree-of-Thoughts、Speculative Decoding）多为“暴力采样”或简单分叉-合并（fork-join）结构，无法建模复杂、非线性的医学推理拓扑。

### 提出了什么新方法或新思路
本文提出 **MedVerse** —— 一种面向复杂医疗推理的新型 **DAG-Structured 并行推理框架**，其核心创新在于：
- **理论基础**：首次将 **Petri Net** 理论引入 LLM 推理，将医学推理形式化为一个**有向无环图（Directed Acyclic Graph, DAG）** 的执行过程。其中：
  - **Places** 表示临床状态（如症状、检查结果）；
  - **Transitions** 表示推理步骤（如从症状推断疾病）；
  - **Tokens** 携带语义和 KV-Cache 引用，实现高效状态传递。
- **全栈协同设计（co-design）**：
  - **数据层**：提出 **MedVerse Curator** 自动化流水线，构建了包含 13,904 个高质量样本的 **MedVerse-14K** 数据集，每个样本均为结构化的 DAG 推理路径。
  - **算法层**：设计 **MedVerse Attention**，通过拓扑感知的注意力掩码（topology-aware attention mask）和自适应位置编码（adaptive position indices），使 Transformer 支持 DAG 结构下的并行推理。
  - **系统层**：开发 **MedVerse Engine**，基于 Multiverse Engine 构建高性能推理引擎，支持动态的 **Fork**（分叉并行）和 **Join**（聚合同步）操作，实现真正的并行解码。

### 相比现有方法的优势
- **更准确**：支持多假设并行探索，避免过早收敛到错误路径。
- **更高效**：突破自回归瓶颈，显著降低推理延迟，提升吞吐量。
- **更可靠且可解释**：显式建模因果依赖关系，推理过程结构清晰，便于临床验证。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
在以下五个标准医学推理基准上进行评估：
- **MedQA**：涵盖美国医师执照考试（USMLE）风格的多项选择题。
- **MedXpert**：专家级医学推理理解评测集。
- **MedBullets (op4/op5)**：基于医学要点的多选题数据集（4 或 5 个选项）。
- **PubMedQA**：基于 PubMed 文献摘要的问答数据集。
- **Humanity's Last Exam (HLE)**：综合性医学挑战评测集。

最终训练数据集为作者构建的 **MedVerse-14K**，由上述多个数据集的训练子集经 MedVerse Curator 处理后合成。

### 实验设置和评估指标
- **模型基座**：基于 `Qwen2.5-7B-Instruct` 和 `Llama-3.1-8B-Instruct` 进行微调。
- **训练配置**：
  - 使用 4 块 NVIDIA H200 GPU；
  - 采用 PyTorch FSDP；
  - 学习率 $10^{-5}$，批量大小 128，训练 3 轮。
- **评估指标**：
  - **Accuracy (%)**：主要性能指标。
  - **Latency (s)**：端到端 CoT 推理延迟。
  - **Throughput (tokens/sec)**：单位时间生成 token 数量。

### 基线方法对比
- **原始基座模型**：`Qwen2.5-7B-Instruct`, `Llama-3.1-8B-Instruct`
- **专用医学 LLM**：
  - `MedReason-8B`
  - `HuatuoGPT-o1-RL-8B`

所有对比均在相同训练数据量（约 14K 样本）下进行，确保公平性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 平均 Accuracy (%) |
|------|------------------|
| Qwen2.5-7B-Instruct (原版) | 34.5 |
| Qwen2.5-7B-Instruct + MedVerse | **39.3** (+4.8%) |
| Llama-3.1-8B-Instruct (原版) | 35.3 |
| Llama-3.1-8B-Instruct + MedVerse | **44.2** (+8.9%) |

> ✅ **MedVerse 在两个不同基座上均显著超越原模型，并达到甚至超过专用医学 LLM（如 MedReason、HuatuoGPT-o1）的性能水平**。

### 与基线方法的对比结果
- **精度方面**：
  - 在 `Llama-3.1-8B` 上，MedVerse 以 **44.2%** 的平均准确率，**超过 MedReason (42.2%) 和 HuatuoGPT-o1 (42.7%)**。
- **效率方面**：
  - **推理延迟降低 1.3×**（即提速 30%）；
  - **生成吞吐量提升 69.3%**（接近 1.7×）；
  - 在长序列（2048 tokens）场景下，吞吐优势更加明显（见 Figure 4b）。

### 消融实验结果
#### （1）线性规划 vs. 直接生成 Petri Net（Table 2）
| 模型变体 | 准确率 (%) | 延迟 (s) |
|----------|-----------|--------|
| Autoregressive (Baseline) | 18.4 | 5.1 |
| Direct Petri Net（无规划阶段） | 17.4 | 4.5 |
| **MedVerse（含规划阶段）** | **19.3** | **4.0** |

> 🔍 发现：**直接生成 DAG 结构效果差**，说明线性“思考再映射”（Think-then-Map）策略对构建正确推理图至关重要。

#### （2）训练与推理模式消融（Table 4）
| 配置 | 平均准确率 |
|------|---------|
| Auto-Ser（标准训练 + 串行推理） | 0.3690 |
| Auto-Par（标准训练 + 并行引擎） | 0.3792 |
| Mask-Ser（MedVerse 训练 + 串行推理） | 0.3856 |
| **Mask-Par（MedVerse 训练 + 并行推理）** | **0.3934** |

> 🔍 发现：**训练机制（Mask）和推理机制（Par）均有增益，二者协同作用最强**，证明了“结构对齐”（structural alignment）的重要性。

#### （3）数据规模影响（Table 3）
| 训练样本数 | 平均准确率 |
|----------|---------|
| 1k | 29.2% |
| 5k (~36%) | 37.5% |
| 14k (100%) | **39.2%** |

> 🔍 发现：模型具有**极强的数据效率**，仅需 5k 样本即可恢复 95% 以上峰值性能。

---

## 4. 关键结论和发现

### 主要发现
1. **临床推理本质上是 DAG 结构而非线性链**：人类医生同时考虑多个假设，现有 CoT 模式与其认知过程不匹配。
2. **结构对齐是关键**：只有当训练数据、模型架构和推理系统三者都对齐于 DAG 拓扑时，才能最大化性能收益。
3. **并行推理可兼顾效率与可靠性**：MedVerse 不仅提升了准确率，还显著降低了延迟、提高了吞吐，实现了“更快且更好”的推理。
4. **Petri Net 是建模复杂推理流程的有效工具**：其对“状态”与“变迁”的分离建模，天然适合 LLM 的 KV-Cache 重用与状态同步。

### 方法的局限性
- **依赖高质量结构化数据**：MedVerse 的性能高度依赖 MedVerse Curator 生成的结构化训练数据，若 Curator 出错，可能传播错误结构。
- **初始规划阶段仍为串行**：虽然执行阶段并行，但 `<Plan>` 阶段仍是线性生成，构成潜在瓶颈。
- **对非常规推理模式泛化能力未知**：目前主要针对鉴别诊断类任务，是否适用于手术规划、治疗随访等复杂流程尚待验证。

### 未来工作方向
- 扩展至 **多模态医疗推理**（如结合医学影像与文本报告）。
- 探索 **动态图构建**，减少对预定义计划的依赖。
- 将 MedVerse 应用于真实临床环境（如电子病历辅助诊断）中的 **端到端决策支持系统**。
- 进一步优化 Petri Net 与 LLM 的深度融合，探索 **端到端可微分图学习** 可能性。

--- 

> ✅ **总结**：MedVerse 通过 **Petri Net + DAG + 全栈协同设计**，成功将医学推理从“线性叙事”升级为“并行计算”，在精度、效率、可解释性三个维度全面突破现有范式，为构建下一代可信医疗 AI 提供了重要路径。

</details>

---

### 2. [tLoRA: Efficient Multi-LoRA Training with Elastic Shared Super-Models](https://arxiv.org/abs/2602.07263)

**Authors**: Kevin Li, Dibyadeep Saha, Avni Kanodia, Fan Lai  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2602.07263v1  

#### Abstract
As Low-Rank Adaptation (LoRA) becomes the standard approach for efficiently fine-tuning large language models (LLMs), shared clusters increasingly execute many concurrent LoRA training jobs over the same frozen backbone. While recent advances enable batching (co-locating) multiple adapters during se...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：tLoRA: Efficient Multi-LoRA Training with Elastic Shared Super-Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
在现代机器学习集群中，**LoRA（Low-Rank Adaptation）微调任务**已成为常见负载。大量开发者为同一基础模型（如 Llama 或 Qwen）训练不同的 LoRA 适配器。然而，当前系统通常将每个 LoRA 任务独立执行，导致：
- **资源浪费**：重复加载相同的冻结 backbone 模型；
- **低效训练**：无法有效共享计算与内存资源；
- **通信瓶颈**：异构任务（不同 rank、batch size）共置时引入同步延迟和通信开销。

尽管已有工作（如 S-LoRA、dLoRA）实现了多 LoRA 在**推理阶段**的批处理，但在**训练阶段**高效共置多个异构 LoRA 任务仍面临挑战。

### 提出了什么新方法或新思路
本文提出 **tLoRA**，一个支持高效批量训练异构 LoRA 任务的框架，其核心创新包括：

#### （1）Shared Super-Model (SSM) 抽象
- 将多个共享相同 backbone 的 LoRA 任务融合成一个统一的“弹性共享超模型”（Elastic Shared Super-Model）。
- 保留各任务独立的前向/反向传播语义和优化器状态，确保收敛正确性。
- 利用现有分布式训练框架（如 Megatron-LM、PyTorch FSDP）进行自动并行化规划，无需重构底层系统。

#### （2）Fused LoRA Kernel + Adaptive Nano-Batching
- 设计了一个融合内核（fused kernel），避免显式构造 $ W = AB^T $ 权重矩阵，提升寄存器和共享内存复用率。
- 引入 **nano-batch** 抽象，在 micro-batch 内部进一步细粒度划分输入样本。
- 使用 **AIMD 控制器**在线动态调整 nano-batch 大小，最大化计算与通信的重叠，减少 pipeline bubbles。

#### （3）Residual-Capacity-Aware 在线调度器
- 调度器基于每个任务的**剩余资源容量**（如空闲 GPU 计算/内存）和**紧迫性评分**（urgency score）动态分组。
- 优先合并资源互补的任务（例如资源富余任务加速资源紧张任务），避免“强者补贴弱者”的不公平现象。
- 支持运行时解耦表现不佳的组合，保障个体任务进度（如完成时间约束）。

### 相比现有方法的优势
| 维度 | tLoRA | 现有方法（如 mLoRA） |
|------|-------|------------------|
| **资源利用** | 高效共享 backbone，充分利用碎片资源 | 仅按内存是否足够决定是否共置，忽略通信开销 |
| **执行效率** | 自适应 nano-batching 实现细粒度流水线重叠 | 固定 batch 执行，易产生 bubble |
| **调度智能性** | 动态感知资源残差与任务紧迫性，弹性分组 | 静态策略（如 FIFO），常导致次优甚至负优化 |
| **公平性保障** | 显式控制单任务慢化程度，防饥饿 | 忽视个体性能退化风险 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- 主要训练任务使用 **GSM8K** 数据集（约 8.5k 小学数学题），用于 fine-tuning LoRA 模型解决下游任务。
- 调度与性能建模依赖真实集群轨迹数据：**ACMETrace**（来自生产环境的 GPU 使用日志，包含任务到达时间、资源分配、执行时长等）。

### 实验设置
- **硬件平台**：
  - 微基准测试：12 块 NVIDIA A100 GPU；
  - 大规模仿真：128-GPU 集群（通过 Sailor 模拟器）。
- **模型配置**：
  - 基础模型：Llama-3-8B 和 Qwen-3-8B；
  - LoRA 参数随机采样：rank ∈ {2,4,8,16}，batch size ∈ {1,2,4,8}；
  - 序列长度、step budget 等固定提交后不变。
- **实现工具**：
  - 基于 PyTorch 2.5 + Triton 3.5.1 开发定制 fused kernel；
  - 利用 Stratified 模拟器进行大规模 trace 回放（误差 < 3%）。

### 评估指标
| 指标 | 定义 |
|------|------|
| **Training Throughput** | 全局吞吐量（samples/sec），所有活跃任务总和 |
| **Job Completion Time (JCT)** | 从提交到训练完成的实际墙钟时间（含排队与执行） |
| **GPU Utilization** | 所有 GPU 的平均 SM（Streaming Multiprocessor）利用率 |

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **mLoRA (Ye et al., 2025)** | 当前最先进的批处理 LoRA 训练系统，基于内存容量简单分组 |
| **Megatron-LM (Narayanan et al., 2021)** | 高度优化的分布式训练框架，但每个 LoRA 独立运行 |
| **tLoRA w/o Scheduler** | 移除自适应调度器，采用 mLoRA 分组策略 |
| **tLoRA w/o Kernel Fuser** | 移除 fused kernel，使用原生 PyTorch 实现 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（综合结果）
| 指标 | 提升幅度 |
|------|----------|
| **Training Throughput** | ↑ **1.2–1.8×** vs. mLoRA |
| **Per-Job Completion Time** | ↓ **2.3–5.4× faster** |
| **Average GPU Utilization** | ↑ **37%** |
| **最大吞吐提升** | 达 **41%** 超过 mLoRA（图5a） |

### 与基线方法的对比结果
- **vs. mLoRA**：
  - 尽管 mLoRA 也尝试批处理，但由于未考虑通信开销和负载不均，某些组合反而降低吞吐（见 Figure 2 中 Jobs 1+2 下降）；
  - tLoRA 通过 SSM 编译 + 自适应 nano-batching 显著减少 pipeline bubbles，实现稳定增益。
- **vs. Megatron**：
  - Megatron 单独运行每个任务，无资源共享，GPU 利用率低；
  - tLoRA 在同等条件下显著提高资源利用率和整体吞吐。

### 消融实验结果
#### （1）调度器影响（tLoRA w/o Scheduler）
- 使用 FIFO 或静态分组策略时，JCT 增加近 **5.4×**；
- 原因：大量非互补任务被强行共置，造成资源争抢和通信瓶颈。

#### （2）Kernel Fuser 影响（tLoRA w/o Kernel Fuser）
- 移除 fused kernel 后，吞吐下降约 **30–40%**；
- 原因：频繁的小 GEMM 调用导致高 kernel launch 开销，且中间张量无法有效复用。

#### （3）Nano-batch Size 敏感性分析（Figure 8a）
- 固定 nano-batch 大小难以适应不同硬件与网络条件；
- tLoRA 的 AIMD 自适应控制器能快速收敛至最优值，端到端训练吞吐优于手动调参方案。

#### （4）不同负载场景下的鲁棒性
| 场景 | 结果 |
|------|------|
| **高并发到达（bursty arrivals）** | tLoRA 仍维持接近峰值吞吐（Figure 9a） |
| **小规模集群（32 GPUs）** | 成功保持线性扩展趋势，未出现尾部延迟爆炸（Figure 13） |
| **跨月真实轨迹回放** | 在不同月份流量模式下均表现稳健（Figure 11） |

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **异构 LoRA 任务可以高效共训**：只要合理设计调度与执行机制，多 LoRA 批量训练不仅能提升吞吐，还能缩短个体任务完成时间。
2. ✅ **资源互补是关键驱动力**：将资源富余任务与资源紧张任务配对，可实现“双赢”——前者释放闲置资源帮助后者，后者提前释放 GPU 反哺前者。
3. ✅ **细粒度执行控制至关重要**：传统的 micro-batch 已不足以掩盖通信开销，**nano-batch + AIMD 控制**是实现高效重叠的关键。
4. ✅ **无需牺牲训练准确性**：tLoRA 是 lossless 的，不改变原始训练语义，保证收敛行为一致。

### 方法的局限性
- ❗ **依赖同源 backbone**：目前仅适用于共享同一预训练模型的任务；若 backbone 不同，则无法融合。
- ❗ **调度开销随任务数增长**：虽然复杂度为 $ O(K \log K) $，但在超大规模任务池中仍需进一步优化。
- ❗ **对网络带宽敏感**：当跨节点通信成为瓶颈时，收益可能受限（尤其在低带宽集群中）。

### 未来工作方向
- 🔮 支持 **异构 backbone 的 LoRA 混合训练**（如通过 MoE 或路由机制）；
- 🔮 探索 **更精细的弹性资源再分配机制**，允许任务临时借用更多资源以换取后续补偿；
- 🔮 结合 **编译器技术** 自动生成最优 SSM 图结构与执行计划；
- 🔮 将 tLoRA 思想推广至其他参数高效微调方法（如 AdaLoRA、IA³）。

--- 

> **总结一句话**：  
> tLoRA 通过 **Shared Super-Model + Fused Kernel + Residual-Aware Scheduler** 三位一体的设计，首次实现了高效、公平、可扩展的多 LoRA 并行训练，在真实集群轨迹下实现了 **最高达 5.4 倍的任务完成加速** 和 **37% 的 GPU 利用率提升**，为大规模 LoRA 工程化部署提供了重要基础设施支持。

</details>

---

### 3. [DLLM Agent: See Farther, Run Faster](https://arxiv.org/abs/2602.07451)

**Authors**: Huiling Zhen, Weizhe Lin, Renxi Liu, Kai Han, Yiming Li, Yuchuan Tian, Hanting Chen, Xiaoguang Li, Xiaosong Li, Chen Chen, Xianzhi Yu, Mingxuan Yuan, Youliang Yan, Peifeng Qin, Jun Wang, Yu Wang, Dacheng Tao, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.07451v1  

#### Abstract
Diffusion large language models (DLLMs) have emerged as an alternative to autoregressive (AR) decoding with appealing efficiency and modeling properties, yet their implications for agentic multi-step decision making remain underexplored. We ask a concrete question: when the generation paradigm is ch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM Agent: See Farther, Run Faster

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文探讨了一个被忽视的关键问题：**在语言模型 Agent（代理）系统中，生成范式（generation paradigm）本身是否会影响多步决策行为？**  
尽管当前大多数 Agent 研究聚焦于训练数据、提示工程或流程设计，却默认使用 Autoregressive（AR）解码作为基础。然而，随着 Diffusion Large Language Models（DLLMs）的兴起，作者提出一个核心问题：当保持相同的 Agent 工作流和监督信号时，仅更换生成范式为 diffusion 是否会系统性地改变 Agent 的规划、工具调用和执行效率？

### 提出了什么新方法或新思路
作者提出了 **DLLM Agent**——将 Diffusion LLM 集成到标准 Agent 流程中的新型代理架构，并进行了以下关键创新：

- **Agent-Oriented Fine-Tuning**：针对 DLLM 设计了面向结构化动作决策的微调目标，以适配多轮交互任务。
- **Multi-turn Diffusion Masking Strategy**：
  - **Context-Clean Corruption**：只对动作段进行噪声扰动，保留上下文完整，避免训练-推理不一致。
  - **Span-Aware Attention Alignment**：调整注意力掩码，防止跨 context 和 action 的虚假信息流动。
- **揭示了 Diffusion 范式的内在协调机制**：通过分析 attention 动态，发现 DLLM 在早期阶段即表现出更强的全局规划能力。

### 相比现有方法的优势
相比传统的 AR Agent，DLLM Agent 展现出显著优势：
- 更快的端到端运行速度（平均 >30%，最高达 8× 加速）
- 更少的交互轮次和工具调用次数
- 更早收敛到正确路径，减少回溯与冗余操作
- 支持迭代优化与全局结构调整，而非局部不可逆承诺

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **BROWSECOMP-ZH**：一个公开的中文多轮网页浏览基准，要求模型通过多次检索、推理动态证据来回答复杂查询。
- **Open-Ended Qualitative Prompts**：一组自定义的开放式多步研究问题，用于定性分析长期推理、工具链构建和失败模式。

### 实验设置和评估指标
所有 Agent 均基于 **DeepDiver** 多智能体框架实现，具备统一的 Planner、Information Seeker 和 Writer 角色分工及工具接口。

#### 控制变量
- 统一的 Agent 架构、工具集（如 `web_search`, `document_qa`）、prompt 模板、状态序列化方式
- 相同的训练轨迹数据和优化预算（5 epochs, lr 从 5e-6 衰减）
- 相同的评估约束：
  - 上下文长度上限：32K tokens
  - 最大交互轮数 $T_{max}=15$
  - 最大工具调用次数限制
  - 统一的 fallback 终止策略

#### 评估指标
| 类别 | 指标 |
|------|------|
| **最终准确性** | Answer Accuracy (%) |
| **工作流效率** | 平均工具调用次数（Tool Calls）、平均交互轮数（Turns Used） |
| **稳定性** | Invalid Action Rate（无法解析的动作比例） |
| **效率增益** | 端到端延迟（wall-clock time） |

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **AR Agent** | 使用 autoregressive 模型 `openPangu-Embedded-7B` 作为 backbone，左向逐词生成每个动作段 |
| **DLLM Agent** | 使用 diffusion 模型 `openPangu-R-7B-Diffusion`，通过迭代去噪生成结构化动作 |
| **消融变体** | 移除 context-clean corruption 或 span-aware attention alignment 的版本 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 BROWSECOMP-ZH 子集测试）

| Method | Accuracy (%) | Tool Calls ↓ | Turns Used ↓ | Invalid Action Rate ↑ |
|--------|---------------|--------------|----------------|-------------------------|
| AR Agent | 15.5 | 7.5 | 14.8 | 1.9% |
| DLLM Agent | 15.5 | **6.7** | **13.0** | 6.4% |
| w/o context-clean corruption | 14.5 | 6.8 | 15.0 | 6.2% |
| w/o span-aware attention alignment | 14.5 | 7.1 | 14.6 | 7.1% |

> 注：准确率相同，但 DLLM Agent 显著减少了 **10.7% 的工具调用** 和 **12.2% 的交互轮数**

### 与基线方法的对比结果
- **端到端效率提升**：
  - 在多个案例中，DLLM Agent 实现了 **超过 30% 的平均加速**
  - 某些特定任务上达到 **8.18× 的 wall-clock speedup**
- **行为差异明显**：
  - AR Agent 倾向于“逐步推进 + 反复验证”，产生大量中间文件（如 `todo_v1.md`, `todo_v2.md`），易陷入冗余循环
  - DLLM Agent 更倾向于一次性制定清晰的研究合同（research contract），直接执行并快速闭环
- **案例对比示例（同一查询）**：
  - **DLLM Agent**：完成时间 **140.95s**，仅需 4 步动作
  - **AR Agent**：完成时间 **1152.68s**（慢 8.18 倍），涉及 6+ 次信息获取与多次重验证

### 消融实验结果
- 移除任一 masking 策略（context-clean corruption 或 span-aware attention）均导致：
  - 准确率下降约 1%
  - 工具调用和交互轮数增加
  - 表明这两种 masking 对齐策略对于稳定多轮 Agent 推理至关重要

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **生成范式本身深刻影响 Agent 行为**  
   即使在完全相同的 workflow、训练数据和工具接口下，**DLLM Agent 仍表现出更高效、更紧凑的行为模式**，说明其优势源于生成机制的本质差异。

2. 🔍 **DLLM 具备更强的全局规划能力（Global Planning Signal）**  
   - 分析显示，DLLM 在早期扩散步骤即可并行提取关键约束条件，建立整体计划框架
   - AR 模型必须顺序承诺，难以修正早期错误，导致更多回溯与冗余

3. ⚙️ **效率增益不仅来自 token-level 并行，更体现在 workflow-level 优化**  
   - 尽管 DLLM 支持并行 token 生成，但真正的加速来自于：
     - 更少的工具调用
     - 更短的交互轨迹
     - 更低的协调开销（如无需反复生成 plan 文件）

4. 🧩 **Diffusion Agent 表现出“先全局、后局部”的决策风格**  
   - Planner Agent：先并行识别所有约束 → 再分解任务
   - Information Seeker：先确定工具类型 → 再填充参数
   - 这种两阶段模式减少了因早期误判导致的后续纠错成本

5. ⚠️ **部署挑战：Structured Output 的可靠性问题**  
   - DLLM Agent 的 **Invalid Action Rate 更高（6.4% vs 1.9%）**，表明其更容易输出格式错误的 tool call
   - 需要加强 tool-call-specific training 才能可靠生成合法 schema

### 方法的局限性
- 当前实验集中在 **DeepDiver 框架下的深度检索类任务**，尚未验证在更长周期、部分可观测环境（如 WebArena）中的泛化性
- DLLM 对 structured output 的稳定性仍弱于 AR，需要额外训练干预
- 所有训练依赖于从 AR Agent 回放的轨迹，未探索 diffusion-native 的训练范式
- 模型规模较小（7B 级别），尚不清楚更大规模下性能差距是否会扩大

### 未来工作方向
1. **探索 diffusion-native training objectives**  
   利用迭代 refine 特性，设计显式的 latent planning、trajectory-level noise schedule 等训练目标。

2. **研究混合架构（Hybrid Agents）**  
   结合 diffusion 与 AR 的优势：由 DLLM 负责高层规划与假设整合，AR 模型负责细粒度执行与交互。

3. **扩展至更复杂的 Agent 场景**  
   如 long-horizon planning、embodied agents、multi-agent collaboration 等更具挑战性的设定。

4. **深入机制分析**  
   通过因果干预（causal intervention）、注意力 ablation 等手段，进一步厘清 bidirectional conditioning 与 iterative refinement 如何塑造 Agent 决策行为。

5. **提升 structured output 可靠性**  
   开发专门针对 tool-call 的 diffusion 解码约束机制，降低 invalid action 率。

---

> **总结一句话**：  
> 本研究表明，**Diffusion 不只是一个更快的生成器，而是一种能催生更聪明、更高效 Agent 的全新范式**。它让 Agent “看得更远”（see farther），从而“跑得更快”（run faster）。

</details>

---

### 4. [SpecAttn: Co-Designing Sparse Attention with Self-Speculative Decoding](https://arxiv.org/abs/2602.07223)

**Authors**: Yikang Yue, Yuqi Xue, Jian Huang  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.07223v1  

#### Abstract
Long-context large language model (LLM) inference has become the norm for today's AI applications. However, it is severely bottlenecked by the increasing memory demands of its KV cache. Previous works have shown that self-speculative decoding with sparse attention, where tokens are drafted using a s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SpecAttn: Co-Designing Sparse Attention with Self-Speculative Decoding**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**

在长上下文 Large Language Model (LLM) 推理中，**KV Cache 的内存访问开销**成为主要性能瓶颈。尽管稀疏注意力（Sparse Attention）可减少 KV Cache 访问量，提升推理速度，但其独立选择关键 KV 条目的策略往往导致：

- **Drafting 准确率低**（即生成的草稿 token 被拒绝率高）
- 或者引入**高昂的 KV Selection 开销**（如 query-aware 方法需每步重新计算重要性）

此外，现有自推测解码（self-speculative decoding）框架将 drafting 和 verification 视为两个独立阶段，**忽略了 verification 阶段中 full attention 自然产生的 attention logits 可作为“免费的 oracle”来指导后续 drafting 的 KV 选择**。

---

### 🚀 **提出了什么新方法或新思路**

本文提出 **SpecAttn** ——一种**验证引导的稀疏注意力机制**（verification-guided sparse attention），通过**协同设计 drafting 与 verification 阶段**，实现高效且准确的 self-speculative decoding。

#### 核心思想：
> 利用 **verification 阶段执行 full attention 时自然产生的 attention logits**，识别出对当前 draft token 序列最关键的历史 KV 条目，并将其用于下一轮 drafting 的 sparse attention 中。

#### 主要创新点：

1. **Verification-Guided KV Selection**  
   - 不再依赖独立算法选择 KV，而是利用 verification 的中间结果（attention logits）动态识别关键 KV。
   - 选择标准是最大化所有 draft tokens（包括被接受和被丢弃的）的累计 attention logit 覆盖范围，避免过拟合单个 token 的注意力模式。

2. **Collect-2-Query 机制**  
   - 为降低收集 attention logits 的带宽开销，仅从 **第一个 draft token 和 bonus token** 收集 logits。
   - 这两个 token 具有最大位置距离，能有效代表整个 draft chain 的多样性，显著降低 HBM 带宽压力。

3. **低开销集成于 vLLM**  
   - 实现为一个无需训练、即插即用的模块，集成到主流推理引擎 **vLLM** 中。
   - 修改 FlashAttention-3 内核以支持 logits 收集，不影响原始性能。

4. **系统化超参数调优策略**  
   - 提出三步调参法：先确定最优 sparse ratio → 再优化 draft 数量 $ y $ → 最后微调平衡延迟与精度。

---

### 🔍 **相比现有方法的优势**

| 方面 | 传统方法（如 MagicDec + Quest） | SpecAttn |
|------|-------------------------------|--------|
| **KV Selection 准确性** | 依赖实时 query-aware 计算，可能滞后 | 利用 full attention “真实反馈”，更精准 |
| **KV Selection 开销** | 每步重估，高计算/内存开销 | 复用已有信息，Collect-2-Query 极低开销 |
| **Drafting Accuracy** | 受限于固定模式或局部估计 | 多 token 联合聚合，接受率更高、衰减慢 |
| **端到端吞吐量** | 高开销抵消准确性增益 | 实现更高 throughput 提升 |

> ✅ **优势总结**：SpecAttn 在保持 lossless 输出质量的前提下，实现了更高的 **draft token 接受率** 和更低的 **KV selection 开销**，从而大幅提升 decoding throughput。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**

| 数据集 | 特点 |
|-------|------|
| **AIME25** | 数学推理任务，输入短（~182 tokens），输出长（~19.7K tokens） |
| **CodeElo** | 编程代码生成任务，同样短输入、长输出 |
| **LongBench-v2** | 综合长上下文理解与推理基准，输入长度高达 96K–120K tokens |

---

### ⚙️ **实验设置**

- **模型**：
  - Qwen3-4B, Qwen3-8B, Qwen3-30B
  - gpt-oss-20b（MXFP4量化，MoE架构）
- **硬件平台**：
  - 双 NVIDIA H100 NVL GPU（94GB 显存）
- **批大小（Batch Size）**：
  - 根据模型容量调整（如 4~20），确保饱和 KV Cache 利用
- **上下文长度**：
  - 最长达 128K tokens
- **评估指标**：
  - **Decoding Throughput (tokens/sec)**：核心性能指标
  - **Average Accepted Draft Tokens per Iteration**：衡量 drafting 准确性
  - **KV Selection Overhead (%)**：相对于理想迭代时间的比例
  - **End-to-End Latency Reduction**

---

### 🆚 **基线方法对比**

| 基线方法 | 描述 |
|--------|------|
| **vLLM (default)** | Vanilla auto-regressive decoding，基础 baseline |
| **MagicDec-Stream** | 使用 StreamingLLM（滑动窗口）作为 query-agnostic sparse attention |
| **MagicDec-Quest** | 使用 Quest（query-aware）进行 KV selection |
| **SpecExtend** | 利用 last accepted token 的 attention weights 引导 draft model 的 sparsity |

> 所有方法均在 **vLLM 框架内统一实现**，保证公平比较。

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据**

| 方法 | 相对于 vLLM 的加速比 | 相对于 SOTA 的加速比 |
|------|---------------------|--------------------|
| **SpecAttn** | **1.25× – 2.81×** | **1.18× – 1.29×** 提升 |

具体表现如下：

#### ▶ 在 **Short Input + Long Output** 场景（AIME25 / CodeElo）

- **Throughput 提升**：
  - SpecAttn 达到 **1.25× – 2.70×** 优于默认 vLLM
  - 比 MagicDec-Stream 快 **1.15× – 1.23×**
  - 比 MagicDec-Quest 更快（后者因高开销反被拖累）

- **Drafting Accuracy**：
  - SpecAttn 平均每轮接受 **6.1+ 个 draft tokens**（$ y=7 $）
  - 显著高于 MagicDec-Quest 和 SpecExtend

- **KV Selection Overhead**：
  - SpecAttn 仅增加 **5.9% – 9.4%** 开销
  - 远低于 MagicDec-Quest（21.7%）和 SpecExtend（11.2% ~ 29.1%）

#### ▶ 在 **Long Context** 场景（LongBench-v2）

- 性能优势进一步放大：
  - SpecAttn 实现最高达 **2.81×** 加速（vs vLLM）
  - 比最强 baseline 快 **1.29×**
  - 因其 KV selection 策略能更好保留历史上下文相关性

---

### 🔬 **消融实验结果**

#### （1）KV Selection 策略对比（图4）

| 策略 | 接受率趋势 |
|------|----------|
| Last Accepted Token Only | 初期高，后期迅速下降（过拟合） |
| All Draft Tokens（含 rejected） | 接受率稳定高位，无明显衰减 |

✅ 结论：**联合多个 draft tokens 的 attention logits 可显著缓解接受率衰减问题**。

#### （2）Collect-2-Query vs Collect-All-Query（图6 & 表1）

| 方法 | 吞吐影响 | 接受 token 数 |
|------|---------|--------------|
| Collect All Query Tokens | +53% ~ +189% 开销 | 6.13 (Qwen3-8B) |
| **Collect-2-Query** | **+5% ~ +37% 开销** | **6.11**（几乎无损） |

✅ 结论：**仅收集首尾两个 token 的 logits 即可达到接近全量收集的效果，性价比极高**。

#### （3）是否包含 rejected tokens（§3.2）

- 移除 rejected tokens 导致平均接受数下降 **3% – 14%**（当 $ y=7 $）
- 原因：许多 rejected tokens 实际语义合理，其 attention pattern 仍有价值

✅ 结论：**rejected tokens 包含有用上下文信号，应纳入 KV selection**

---

## 4. **关键结论和发现**

### ✅ **主要发现**

1. **Verification 阶段不仅是验证器，更是“指导者”**  
   > 其内部计算的 attention logits 是高质量的 KV criticality 信号，可用于指导后续 drafting，形成闭环反馈。

2. **Co-design 是突破性能瓶颈的关键**  
   > 将 drafting 与 verification 耦合设计，而非孤立处理，可在不牺牲质量的情况下获得更高效率。

3. **低开销 ≠ 低性能**  
   > Collect-2-Query 证明：精心选取边界 token 即可捕获足够信息，实现近似最优性能的同时极大降低带宽成本。

4. **SpecAttn 的收益随 context length 增加而扩大**  
   > 在百万级 token 上下文中，KV Cache 压力更大，SpecAttn 的优势将更加显著。

---

### ⚠️ **方法的局限性**

1. **依赖 full attention verification 的完整性**  
   - 若 verification 被剪枝或跳过（如 layer-skipping），则无法获取完整 logits
   - 但可与其他优化技术（如 LayerSkip）结合使用

2. **对 MoE 类模型增益有限**  
   - 如 gpt-oss-20b 仅有部分 block 使用 full attention，sparse attention 节省空间较小
   - 导致 overall speedup 相对较低

3. **FlashAttention 内核修改带来部署复杂度**  
   - 需要定制 kernel 支持 logits dump，虽已开源但仍有一定工程门槛

---

### 🔮 **未来工作方向**

1. **扩展至多模态 LLM**  
   - 将 verification-guided sparse attention 应用于 Video-LLM 或 Vision-Language Models

2. **动态调整 sparse ratio 与 $ y $**  
   - 根据输入内容自动调节 hyperparameters，实现 adaptive speculation

3. **结合 KV Cache 压缩技术**  
   - 与 SnapKV、H2O 等压缩方法融合，进一步降低内存占用

4. **支持分布式推理场景**  
   - 在 tensor/pipeline parallel 设置下实现跨设备 logits 收集与同步

---

## ✅ 总结一句话

> **SpecAttn 通过将 self-speculative decoding 中的 verification 阶段“副产品”——attention logits——转化为 drafting 阶段的指导信号，实现了高准确率、低开销的稀疏注意力机制，在多种 LLM 和长上下文任务上取得高达 2.81× 的 decoding throughput 提升，是首个真正实现 drafting 与 verification 协同设计的 lossless 加速方案。**

> 🔗 项目即将开源为 vLLM 的一键补丁（drop-in patch），推动高效 LLM 推理落地。

</details>

---

### 5. [BitLogic: Training Framework for Gradient-Based FPGA-Native Neural Networks](https://arxiv.org/abs/2602.07400)

**Authors**: Simon B\"uhrer, Andreas Plesner, Aczel Till, Roger Wattenhofer  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.07400v1  

#### Abstract
The energy and latency costs of deep neural network inference are increasingly driven by deployment rather than training, motivating hardware-specialized alternatives to arithmetic-heavy models. Field-Programmable Gate Arrays (FPGAs) provide an attractive substrate for such specialization, yet exist...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# BitLogic: Training Framework for Gradient-Based FPGA-Native Neural Networks 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前深度神经网络（DNN）推理的能耗和延迟已成为AI系统部署的主要瓶颈，尤其是在边缘设备上。传统基于GPU的推理虽然计算吞吐高，但能效低；而现有的FPGA神经网络方案多依赖于将标准模型（如CNN）量化后映射到硬件，存在以下问题：

- **碎片化且难以比较**：不同研究采用异构硬件平台、不同的时钟假设和测量方式，导致结果不可比。
- **非原生设计**：多数方法仍以算术操作（如MAC）为核心，未能充分利用FPGA的底层逻辑资源（如LUT）。
- **软硬件脱节**：训练在软件中进行，硬件实现需手动转换，易引入误差。

### **提出了什么新方法或新思路**

本文提出 **BitLogic** ——一个**全梯度驱动、端到端可训练的FPGA原生神经网络框架**，其核心思想是：

- **以LUT为基本计算单元**：用可微分的查找表（LUT）节点替代传统的乘累加（MAC）操作，直接对应FPGA中的LUT原语。
- **完全可微分训练**：通过多种边界一致的连续松弛技术（如Probabilistic、Hybrid等），使离散LUT函数可在PyTorch中进行梯度优化。
- **自动化RTL导出**：提供从训练好的PyTorch模型自动生成综合级HDL代码的流水线，确保软硬件推理行为严格等价。

### **相比现有方法的优势**

| 维度 | BitLogic优势 |
|------|-------------|
| **硬件效率** | 仅使用LUT资源即可实现亚20ns级单样本推理，无需DSP或BRAM。 |
| **灵活性与模块化** | 提供功能API支持多种架构（FFN、CNN、残差、注意力等）自由组合。 |
| **可复现性与公平比较** | 统一框架下评估不同LUT节点类型、编码器、头结构，便于消融分析。 |
| **软硬协同设计** | 支持硬件感知训练（如温度调节防过拟合）、自动HDL生成与验证流程。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **图像分类任务**：
  - MNIST（手写数字）
  - Fashion-MNIST（服装图像）
  - CIFAR-10 和 CIFAR-100（彩色小图）

这些是逻辑神经网络领域的标准基准，用于评估模型表达能力和硬件效率。

### **实验设置和评估指标**

#### **模型配置**
- **主干架构**：分为前馈网络（FFN）和卷积网络（CNN）两类。
- **LUT节点输入维度**：通常设为4~6位，控制每个LUT的复杂度。
- **编码器**：使用`distributive thermometer encoding`将浮点输入转为二进制。
- **输出头**：采用`GroupSum`或`GroupedDSP`聚合二进制特征。
- **训练策略**：Adam优化器，50轮训练，学习率0.01，部分模型使用权重衰减。

#### **评估指标**
| 指标 | 描述 |
|------|------|
| **准确率（Accuracy）** | 测试集上的分类精度 |
| **等效门数（Gate Count）** | 以2^n−1估算每个n输入LUT的等效二进制门数量，作为硬件规模代理 |
| **延迟（Latency）** | CPU/GPU上μs级，FPGA上ns级（后综合时序分析） |
| **吞吐量（Throughput）** | FPS（Frames Per Second） |
| **能量消耗** | FPGA上基于功耗估计计算每样本能耗（E = P × t） |
| **资源利用率** | LUT、DSP、BRAM等FPGA资源占用情况 |

### **基线方法对比**

与其他**基于逻辑门或LUT的神经网络**进行比较，包括：

- **DiffLogic Net** (Petersen et al., 2022)：基于可微分逻辑门的网络
- **LogicTreeNet** (Petersen et al., 2024)：基于逻辑门树的CNN
- **LILogicNet** (Fojcik et al., 2025)：紧凑型逻辑门网络
- **DWN** (Bacellar et al., 2025)：无权重神经网络

> 注：由于各工作报告格式不统一（是否含头/编码器、是否优化后计数），作者强调应谨慎解读绝对数值，侧重趋势分析。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 数据集 | BitLogic 准确率 | 等效门数 | 推理延迟（FPGA） |
|--------|------------------|----------|------------------|
| **MNIST** | **99.1%** | 384K | <20 ns |
| **Fashion-MNIST** | **93.8%** | 384K | <20 ns |
| **CIFAR-10** | **72.3%** | <300K | <20 ns |
| **CIFAR-100** | **23.4%** | 384K | <20 ns |

> ✅ 在CIFAR-10上达到72.3%准确率的同时，仅使用不到0.3M逻辑门，显著优于大多数同类方法。

### **与基线方法的对比结果**

| 模型 | CIFAR-10 准确率 | 门数 | 备注 |
|------|------------------|-------|------|
| **BitLogic (Ours)** | **72.3%** | **384K** | 仅用LUT资源 |
| DiffLogic Net-L | 60.78% | 1.28M | 更大模型仍落后 |
| LILogicNet-L | 60.98% | 256K | 规模相近但精度低 |
| LogicTreeNet-M | 71.01% | 3.08M | 高出近10倍门数 |
| LogicTreeNet-G | 86.29% | 61.0M | 极大规模模型 |

> 🔍 结论：**BitLogic在中等规模下实现了极具竞争力的精度-效率权衡**，尤其在单位门数性能上表现突出。

### **消融实验结果**

在Fashion-MNIST上对五个组件进行了系统性消融：

#### **(A) 编码器影响**
- `Thermometer` > `Binary` > `One-hot` > `Sign`
- 分布式热编码（distributive thermometer）效果最佳（~83.4%）

#### **(B) 层类型**
- `TopK-Sparse` (83.5%) ≫ `Learnable` (70.0%) > `Random` (77.3%)
- 固定稀疏候选池 + 可学习选择，在效率与性能间取得平衡

#### **(C) 节点类型**
- `Hybrid` (84.0%) ≈ `Probabilistic` (83.5%) > `Fourier`, `DWN`
- Hybrid结合了DWN前向与概率反向梯度，训练更稳定

#### **(D) 输入维度（Fan-in）**
- 明显正相关：fan-in=2 (79.3%) → fan-in=6 (**84.9%**)
- 更高维LUT带来更强表达能力，但内存成本呈指数增长（2^n）

#### **(E) 输出头**
- `GroupSum` (83.5%) > `GroupedDSP` (80.9%)
- 尽管GroupedDSP有可学习权重，但在本设置下反而略逊，可能因量化损失

#### **其他重要发现**
- **宽度优于深度**：增加层宽比加深网络更有效（见Figure 4a）
- **温度参数T可缓解过拟合**：在GroupSum头中调节T，验证准确率峰值出现在T=50，而T=1.0严重过拟合

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **LUT可以作为高效、通用的神经元替代品**：通过可微松弛与端到端训练，LUT节点能逼近甚至超越传统算术操作的表现。
2. ✅ **硬件原生设计大幅提升效率**：BitLogic在Xilinx Zynq-7020上实现**18.63ns延迟、53.7M FPS、3.34nJ/样本能耗**，远超CPU/GPU实现。
3. ✅ **模块化框架促进公平比较与快速迭代**：首次在一个统一框架内集成多种LUT变体、编码器、头结构，支持精细化消融。
4. ✅ **自动化RTL导出保障软硬件一致性**：避免手动转换错误，提升部署可靠性。

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **CNN性能未达预期** | 当前CNN版本在CIFAR上表现不如FFN，推测因训练不足或通道配置未调优所致。 |
| **深度网络训练不稳定** | 随着层数增加，梯度传播困难，需进一步研究归一化、初始化机制（如文中提出的Residual Init）。 |
| **HDL导出支持有限** | 当前主要支持前馈、组合逻辑，对时序逻辑（如流水线寄存器、状态机）支持尚不完整。 |
| **资源限制制约大规模部署** | Vivado工具对常量数组大小有限制（如1M元素上限），影响大模型综合。 |

### **未来工作方向**

1. **提升深层网络稳定性**  
   - 引入更适合逻辑节点的归一化方法
   - 设计残差连接、跳跃路径等拓扑改进

2. **扩展至更多任务与架构**  
   - 支持序列建模（RNN、Transformer）
   - 开发分割、检测、重建等任务专用head与loss

3. **完善HDL导出与时序支持**  
   - 自动插入流水线寄存器以满足高频需求
   - 支持流式接口与片外通信协议

4. **硬件感知联合优化（Co-design）**  
   - 将LUT利用率、时序约束纳入训练目标
   - 结合Neural Architecture Search（NAS）自动探索最优结构

5. **构建开源生态**  
   - 发布代码与预训练模型（文中提及待授权解决）
   - 推动社区共建标准化评测基准

---

> 📌 **总结一句话**：  
> **BitLogic 是首个真正意义上“从训练到部署”全流程贯通的FPGA原生神经网络框架，它重新定义了DNN在FPGA上的构建范式——不再是从软件映射到硬件，而是直接在硬件原语上进行端到端学习。**

</details>

---

### 6. [DLLM-Searcher: Adapting Diffusion Large Language Model for Search Agents](https://arxiv.org/abs/2602.07035)

**Authors**: Jiahao Zhao, Shaoxuan Xu, Zhongxiang Sun, Fengqi Zhu, Jingyang Ou, Yuling Shi, Chongxuan Li, Xiao Zhang, Jun Xu  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.07035v1  

#### Abstract
Recently, Diffusion Large Language Models (dLLMs) have demonstrated unique efficiency advantages, enabled by their inherently parallel decoding mechanism and flexible generation paradigm. Meanwhile, despite the rapid advancement of Search Agents, their practical deployment is constrained by a fundam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM-Searcher: Adapting Diffusion Large Language Models for Search Agents

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前 **Search Agent** 在实际部署中面临的两大核心挑战：

1. **Latency Challenge（延迟挑战）**：在标准的 ReAct 范式下，LLM 需要串行执行“思考 → 工具调用 → 等待工具响应”流程，导致端到端推理延迟高，用户体验差。
2. **Agent Ability Challenge（智能体能力挑战）**：现有的 **dLLMs（Diffusion Large Language Models）** 虽然具备并行解码优势，但在多步推理、工具调用格式遵循等方面表现较弱，难以直接作为 Search Agent 的 backbone。

### 提出的新方法与思路
为解决上述问题，作者提出 **DLLM-Searcher**，一个专为 dLLMs 设计的 Search Agent 优化框架，包含两个核心组件：

#### （1）两阶段后训练管道（Two-stage Post-training Pipeline）
- **Agentic SFT（Agentic Supervised Fine-Tuning）**  
  利用高性能教师模型生成的轨迹数据进行监督微调，使 dLLM 学习正确的 `think` 和 `tool_call` 结构化输出格式。
- **Agentic VRPO（Agentic Variance-Reduced Preference Optimization）**  
  基于偏好学习进一步优化模型行为，使用 rollout 数据构建 winner/loser 对，提升推理与检索能力。

> ✅ 特别设计了 **Agentic ELBO** 和 **Agentic Noising** 机制，确保训练过程中仅关注 `think` 和 `tool_call` 区域，避免 `tool_response` 泄露造成训练-推理不一致。

#### （2）新型代理范式：P-ReAct（Parallel-Reasoning and Acting）
- 一种无需额外训练的推理策略，利用 dLLM 的非自回归特性实现“边等待边思考”。
- 核心技术：
  - **Token Pre-filling**：预填充 `<tool_call>` 和 `</tool_call>` 边界标记。
  - **Confidence Biasing**：对边界内位置施加置信度偏置，引导模型优先解码 `tool_call` 内容。

> ✅ 实现了 **tool-first generation**，即先生成工具调用指令，再补全思考过程，在工具执行期间模型仍可继续“思考”，从而显著降低端到端延迟。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 利用 dLLM 并行解码 + P-ReAct 并行执行，实现约 **15% 推理加速** |
| **性能** | 经过 Agentic SFT + VRPO 后，dLLM 的搜索与推理能力达到主流 ARM-based Agent 水平 |
| **可控性** | P-ReAct 实现近乎 100% 成功率的 `tool_call` 优先生成 |
| **通用性** | 方法适用于任意支持块状扩散的 dLLM（如 SDAR），且 P-ReAct 无需重新训练 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验基于四个多跳问答（multi-hop QA）基准：
- **HotpotQA**：需跨文档推理的事实类问题
- **2WikiMultiHopQA**：合成的多跳维基百科问答
- **Musique**：更复杂的多跳链式问题
- **Bamboogle**：用于测试泛化能力的 out-of-domain 数据集

> 所有测试集均从开发集中采样 500 条（Bamboogle 全部 125 条）

### 实验设置
- **Backbone 模型**：SDAR（Block Diffusion LM，64/128 block size）
- **外部工具**：Google Search API，返回 top-10 结果
- **训练配置**：
  - Agentic SFT：lr=1e-5，batch=32，3 epochs
  - Agentic VRPO：lr=5e-7，batch=16，5 epochs
- **推理配置**：
  - Denoising steps: 128
  - Block size: 128
  - Temperature: 1.0
  - Confidence bias α = 0.5

### 评估指标
- **ACCR（Accuracy-contain）**：预测答案是否包含黄金答案（exact match 不适用长文本场景）
- **ACCL（LLM-as-Judge Accuracy）**：使用 Doubao-Seed-1.8 作为裁判模型判断回答正确性

### 基线方法对比
分为三类：
1. **传统 RAG 方法**：
   - SuRe, Selective-Context, Adaptive-RAG, IRCoT, Iter-RetGen, CR-Planner, ReARTeR
2. **ARM-based Search Agents**：
   - Search-o1, Search-R1, WebSailor*, R1Searcher*
3. **dLLM-based Agents**：
   - SDAR（vanilla）、Dream、LLaDA

> 注：WebSailor 和 R1Searcher 的结果经过适配处理以保持工具一致性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）

| Model | Avg ACCR | Avg ACCL |
|-------|----------|----------|
| R1Searcher* (SOTA ARM) | 53.1 | 56.5 |
| **DLLM-Searcher (Ours)** | **57.0** | **56.6** |
| LLaDA (dLLM baseline) | 34.9 | 32.5 |
| SDAR (vanilla) | — | — |

> ✅ **DLLM-Searcher 在所有数据集上均超越现有 dLLM 方法，并优于大多数 ARM-based Agent，甚至略胜 R1Searcher**

#### 分项表现亮点：
- 在 **2Wiki** 上 ACCR 达 **69.8**（+10.2 超越 R1Searcher）
- 在 **Bamboogle** 上展现强泛化能力（out-of-domain 性能稳定）

### 与基线方法对比结果
- 相比传统 RAG 方法（如 ReARTeR）平均提升 **~19% ACCR**
- 相比 vanilla dLLMs（如 SDAR）实现从 **完全失败** 到 **稳定运行** 的跨越
- 与最强 ARM Agent（R1Searcher）相比，性能**相当甚至略有领先**

### 消融实验结果（Table 2 & RQ1）

| 方法 | HotpotQA ACCR | 提升 |
|------|----------------|--------|
| Agentic SFT | 57.2 | — |
| + Agentic VRPO | **60.4** | **+3.2** |

> ✅ 两阶段训练均有显著增益，VRPO 进一步提升推理鲁棒性和准确性

### 推理效率结果（RQ2）
- **P-ReAct 相比标准 ReAct 实现平均 15% 推理加速**：
  - HotpotQA: **14.77%**
  - 2Wiki: **21.00%**
  - Bamboogle: **22.08%**
  - Musique: **12.67%**
- 加速同时 **无明显性能损失**（ACCR/ACCL 几乎持平）

### 控制有效性验证（RQ3）
- 尝试将 P-ReAct 思路迁移到 **Autoregressive LLMs（Qwen3 系列）**：
  - 即便修改 prompt 引导先输出 `tool_call`，也会导致 **显著性能下降**
  - 如 Qwen3-8B 在 Wiki 上准确率下降 **13.3%**
- 反观 DLLM-Searcher 在多数任务上 **精度不变甚至略有上升**

> ✅ 表明 **order-free generation 是 dLLM 独有的优势**，ARM 因依赖显式 CoT 而无法安全实现“先行动后思考”

---

## 4. 关键结论和发现

### 主要发现
1. **dLLMs 完全可以胜任 Search Agent 角色**，只要通过适当的后训练（Agentic SFT + VRPO）弥补其推理与格式遵循缺陷。
2. **P-ReAct 能有效利用 dLLM 的非自回归特性**，实现“在等待中思考”，打破 ReAct 的串行瓶颈，带来真实世界可用的延迟优化。
3. **dLLM 的 bidirectional attention 支持 latent reasoning**：即使 `think` 区域未显式解码，模型也能利用全局上下文生成高质量 `tool_call`。
4. **DLLM-Searcher 实现了性能与效率的双赢**：既达到主流 ARM Agent 的水平，又获得约 **15% 的端到端加速**。

### 方法的局限性
- 当前方法依赖特定结构的 dLLM 架构（如 Block Diffusion），不适用于纯自回归或全扩散模型。
- Agentic SFT 依赖高质量教师轨迹，数据构造成本较高。
- P-ReAct 的 confidence bias 参数需要手动调节，尚未完全自动化。
- 在极复杂任务（如 Musique）上仍略逊于顶级 ARM Agent。

### 未来工作方向
- 将 P-ReAct 扩展至其他工具交互场景（如代码执行、数据库查询）
- 探索全自动的 decoding order control 机制
- 构建端到端联合优化框架，统一训练与推理目标
- 开发面向 dLLM 的专用 Agent Evaluation Benchmark

---

> 🔚 **总结一句话**：  
> **DLLM-Searcher 成功将 dLLM 的并行潜力转化为 Search Agent 的实际效率优势，在不牺牲性能的前提下实现了“边等边想”的新型智能体范式，为高效 Agent 系统提供了新路径。**

</details>

---

### 7. [Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System](https://arxiv.org/abs/2602.08335)

**Authors**: Yanming Li, Xuelin Zhang, WenJie Lu, Ziye Tang, Maodong Wu, Haotian Luo, Tongtong Wu, Zijie Peng, Hongze Mi, Yibo Feng, Naiqiang Tan, Chao Huang, Hong Chen, Li Shen  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.08335v1  

#### Abstract
Integrating Large Language Models (LLMs) with external tools via multi-agent systems offers a promising new paradigm for decomposing and solving complex problems. However, training these systems remains notoriously difficult due to the credit assignment challenge, as it is often unclear which specif...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
在 **Multi-Agent System (MAS)** 中集成 **Large Language Models (LLMs)** 和外部工具已成为解决复杂任务的新范式。然而，这类系统的训练面临一个根本性挑战——**Credit Assignment Problem（信用分配问题）**：当整个团队完成一个任务时，难以准确判断每个 agent（如 planner 或 worker）对最终成功或失败的具体贡献。

现有方法通常依赖于**稀疏的、全局广播的奖励信号**（broadcast rewards），将所有 agent 视为一个整体进行更新，导致：
- 无法区分个体贡献；
- 政策更新效率低下；
- 难以稳定训练过程。

---

### 提出了什么新方法或新思路
本文提出 **SHARP (Shapley-based Hierarchical Attribution for Reinforcement Policy)**，一种基于 **Shapley Value** 的精细化信用分配框架，用于优化多智能体强化学习。

#### 核心创新点：
- **三重分解奖励机制 (Tripartite Decomposed Reward)**：
  1. **Global Broadcast-Accuracy Reward**：衡量最终任务是否成功，确保整体目标对齐。
  2. **Shapley-based Marginal-Credit Reward**：利用 **Shapley Value** 从合作博弈论中量化每个 agent 的边际贡献，实现精确的个体信用归因。
  3. **Tool-Process Reward**：提供执行过程中的反馈，确保工具调用的有效性和正确性。

- **Counterfactual Masking 机制**：
  通过“反事实掩码”移除某个 agent 后观察轨迹性能变化，计算其因果影响（即 $ \text{credit}_m = R_{\text{acc}}(T) - R_{\text{acc}}(T^{(-m)}) $），从而数学上隔离 agent 的真实贡献。

- **Group-Relative Policy Optimization (GRPO) 扩展**：
  在 GRPO 基础上引入 agent-specific 的优势归一化，提升训练稳定性。

---

### 相比现有方法的优势
| 维度 | 传统方法 | SHARP |
|------|--------|-------|
| 信用分配粒度 | 全局广播，粗粒度 | 细粒度，按 agent 分配 |
| 贡献识别能力 | 弱，混淆个体作用 | 强，基于 Shapley 数学建模 |
| 训练稳定性 | 易受噪声干扰，波动大 | 更低方差梯度，收敛更稳 |
| 可解释性 | 黑箱式更新 | 可分析 planner-worker 协作模式 |

> ✅ **优势总结**：SHARP 实现了**可解释、高效且稳定的多 agent 联合优化**，突破了传统 broadcast reward 的瓶颈。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖五个真实世界基准测试，涵盖多跳推理、网页导航、工具调用等场景：

| 数据集 | 描述 |
|-------|------|
| **MuSiQue** | 多跳问答，需组合多个单跳问题 |
| **GAIA-text** | 通用 AI 助手评测，涉及推理、搜索、工具使用 |
| **WebWalkerQA** | 多步网页遍历与证据聚合 |
| **FRAMES** | 端到端检索增强生成的统一评测（事实性、检索、推理） |
| **DocMath-Eval** | 长文本文档中的数学推理（含表格） |

> ⚠️ 注意：仅 MuSiQue 使用训练集（5,975 实例），其余均零样本评估（zero-shot evaluation）。

---

### 实验设置和评估指标
- **模型架构**：采用 **self-play 多角色共享策略**，同一 LLM 通过不同 system prompt 实例化为 planner 和 worker。
- **Backbone 模型**：主要使用 **Qwen3-8B**，部分对比使用 LLaMA-3.1-8B。
- **优化器**：AdamW，学习率 $10^{-5}$，batch size 256。
- **训练步数**：180 gradient steps，64×A100 GPU。
- **评估指标**：**Match Accuracy (%)**，即预测答案与标准答案匹配的比例。

---

### 基线方法对比
分为四类 baseline 进行比较：

| 类别 | 方法 |
|------|------|
| **Vanilla LLMs** | LLaMA-3.1-8B RAG, Qwen3-8B RAG |
| **Prompt-based Planning** | Plan-Search |
| **Single-Agent RL** | Search-R1, Single-agent GRPO |
| **Multi-Agent Systems (无 marginal credit)** |  
| &nbsp;&nbsp;– 未训练结构 | Planner-Worker, G-Designer, CARD, COA |
| &nbsp;&nbsp;– 已训练结构 | AceSearcher, MATPO |

> 🔍 特别强调：SHARP 与 **MATPO** 对比最具意义，因其同属 MARL 框架，但后者缺乏细粒度信用建模。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | MuSiQue | GAIA-text | WebWalkerQA | FRAMES | **AVG** |
|------|---------|-----------|-------------|--------|--------|
| MATPO | 47.00 | 31.65 | 7.47 | 37.10 | **30.81** |
| **SHARP (ours)** | **50.76** | **33.70** | **8.50** | **37.29** | **32.56** |

✅ **平均提升**：
- 相比 **single-agent baseline**：**+23.66%**
- 相比 **multi-agent baseline (如 MATPO)**：**+14.05%**

> 💡 在 MuSiQue 上达到 **50.76**，领先第二名 MATPO 超过 3.76 点。

---

### 与基线方法的对比结果
- **优于所有 baselines**，在全部任务上排名第一。
- 即使是结构化的非训练方法（如 Plan-Search）也远逊于 SHARP。
- 在 **DocMath-Eval** 上展现强泛化能力，在四种文档推理设定下均表现最佳。
- 在 **8B 模型规模下**，相比 single-agent 提升达 **14.41 pts**，显示更强的 scaling law 效应。

---

### 消融实验结果（Ablation Studies）
#### （1）移除 Shapley Credit 的影响（图3）
| 设置 | MuSiQue Acc |
|------|------------|
| 完整 SHARP | 50.76 |
| 仅 planner-level Shapley | 47.60 |
| 仅 worker-level Shapley | 48.00 |
| 无 Shapley（baseline） | 47.00 |

➡️ **发现**：只有同时赋予 planner 和 worker 精确信用时，才能获得最大增益，体现协同效应。

#### （2）组件贡献分析
- **Planner 信用**：主要改进任务分解逻辑，带来约 +0.6~0.65 提升。
- **Worker 信用**：显著减少冗余/无效工具调用，提升更大（+0.88~1.00），尤其在长程任务中更重要。

---

## 4. 关键结论和发现

### 主要发现
1. **Marginal Credit Modeling 是关键驱动力**：
   - 相比架构设计或优化策略本身，**显式的边际信用建模**才是性能跃升的核心。
   
2. **SHARP 显著改善 planner-worker 协调机制**（图6）：
   - 提升 planner 的平均 Shapley 值（planner score ↑）
   - 有用 subagent 比例从 11.03% → 12.96%
   - **有害 subagent 调用比例从 5.48% ↓ 至 4.40%**，说明能有效过滤负面行为。

3. **训练更稳定、扩展性更好**（图5）：
   - 在 GAIA-text 上随训练步数单调上升，而 baseline 出现震荡。
   - 随模型参数增大（0.6B→8B），SHARP 增益持续扩大，表明其在更强 backbone 上更具潜力。

4. **缓解 Catastrophic Forgetting**（表2）：
   - 在 MMLU 上测试知识保留能力：
     - Planner-Worker (no train): 93.04%
     - MATPO: 94.00%
     - **SHARP: 94.65%**
   ➡️ 表明 SHARP 不仅不遗忘旧知识，反而有正向迁移效果。

---

### 方法的局限性
- **计算开销增加**：Shapley 估计需要多次 counterfactual rollout，训练时间随 agent 数量增长。
- **Sparsification 折衷**：可通过只对部分 subagent 计算 Shapley 来降低开销（见图7），但可能牺牲精度。
- **仍存在协调低效问题**：即使在 SHARP 下，“有用 subagent”仍是少数，提示未来需进一步剪枝低效路径。

---

### 未来工作方向
1. **高效 Shapley Approximation**：开发更轻量级的 marginal contribution 估计算法。
2. **Dynamic Agent Pruning**：自动识别并禁用低贡献 agent，提升推理效率。
3. **跨任务迁移信用模型**：探索 credit assignment 策略的可迁移性。
4. **应用于更复杂 topology**：如 communication graph、dynamic coalition formation 等。

---

## 总结
> 🌟 **SHARP 成功地将博弈论中的 Shapley Value 引入多 agent LLM 系统训练，解决了长期存在的 credit assignment 难题**。它不仅带来了显著的性能提升，还增强了训练稳定性、可解释性和系统协调质量，为构建高效、可信的多 agent 协作系统提供了坚实基础。

</details>

---

### 8. [TEAM: Temporal-Spatial Consistency Guided Expert Activation for MoE Diffusion Language Model Acceleration](https://arxiv.org/abs/2602.08404)

**Authors**: Linye Wei, Zixiang Luo, Pingzhi Tang, Meng Li  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.08404v1  

#### Abstract
Diffusion large language models (dLLMs) have recently gained significant attention due to their inherent support for parallel decoding. Building on this paradigm, Mixture-of-Experts (MoE) dLLMs with autoregressive (AR) initialization have further demonstrated strong performance competitive with main...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TEAM: Temporal-Spatial Consistency Guided Expert Activation for MoE Diffusion Language Model Acceleration

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前将 **Mixture-of-Experts (MoE)** 架构直接应用于 **diffusion large language models (dLLMs)** 存在严重的推理效率瓶颈。尽管 MoE 本应通过稀疏激活提升效率，但在 dLLMs 中，每个去噪步骤中所有 token 并行处理并独立选择专家，导致大量专家被激活，而只有少数 token 被最终接受（unmasked），造成显著的计算和内存开销。

这种“高激活、低接受”的现象使得 MoE 在 dLLMs 上的实际表现接近于稠密模型（dense model），失去了其稀疏性的优势，尤其在对延迟敏感的应用和边缘设备上成为瓶颈。

---

### 🚀 提出的新方法：TEAM
作者提出 **TEAM** —— 一种即插即用（plug-and-play）的框架，用于加速 MoE dLLMs 推理，核心思想是利用 **时间一致性（temporal consistency）** 和 **空间一致性（spatial consistency）** 来指导专家激活策略。

#### 主要创新点：
1. **观察到两个关键性质**：
   - **Temporal Consistency**：已解码的 token 在后续迭代中保持稳定，无需重复完整计算。
   - **Spatial Consistency**：未解码的 masked tokens 在路由时倾向于集中在少数几个专家上，且接受顺序具有空间局部性（spatial locality）。

2. **三种互补的专家激活与解码策略**：
   - **Delayed Caching for Decoded Tokens (DCD)**  
     对已接受的 token 延迟缓存其 KV 缓存，仅在最新一轮中重新计算，避免冗余前向传播。
   - **Speculative Exploration for Hot Tokens (SEH)**  
     将可能即将被接受的 masked tokens 定义为 “hot tokens”，进行多分支 speculative decoding，提高单步 token 接受率。
   - **Limited Activation for Cold Tokens (LAC)**  
     将远离已解码区域、置信度低的 tokens 定义为 “cold tokens”，限制其只能从已被 hot 或 decoded tokens 激活的专家集合中选择，防止引入新的稀有专家。

---

### 🔍 相比现有方法的优势
| 方面 | TEAM 的优势 |
|------|-------------|
| **针对性强** | 是首个专门分析 MoE dLLMs 中专家激活行为的工作，而非通用加速方案。 |
| **无需训练修改** | 完全在推理阶段实现，不改变训练过程或模型结构。 |
| **高效利用硬件资源** | 利用 MoE 天然的专家分布特性，在单 GPU 上即可实现有效 speculative decoding。 |
| **兼顾性能与速度** | 显著提速的同时几乎无性能损失（negligible performance degradation）。 |

---

## 2. 核心实验方法和设置

### 📚 数据集
在四个主流基准上评估 TEAM 的有效性：
- **代码生成任务**：
  - `HumanEval` (0-shot)
  - `MBPP` (0-shot)
- **数学推理任务**：
  - `GSM8K` (0-shot)
  - `Math-500` (0-shot)

---

### ⚙️ 实验设置
- **主模型**：`SDAR 30B-A3B`，一个典型的基于 MoE 的 block-diffusion dLLM。
- **硬件平台**：NVIDIA A100 80GB GPU。
- **关键超参数**：
  - 接受阈值（unmask threshold）: `T = 0.95`
  - Block size: `32 tokens`
  - Hot token 判定标准：
    - 置信度阈值 `Th = 0.7`
    - 距离已解码 token 最大距离 `Lh = 3`
  - Speculative branches 数量：`4`

---

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **APF** (Activated experts Per Forward pass) | 每次前向传播激活的专家数量 |
| **TPF** (Tokens Per Forward pass) | 每次前向传播成功接受的 token 数量 |
| **APT** (Activated experts Per decoded Token) | 每解码一个 token 所需激活的专家数（越小越好） |
| **Speedup** | 相对于 vanilla 模型的推理速度提升倍数 |
| **Score** | 任务准确率（Pass@1） |

---

### 🆚 基线方法对比
- **Vanilla SDAR**：原始 MoE dLLM，无任何优化。
- **dKV-Cache (Ma et al., 2025a)**：已有 KV 缓存机制，但针对全局 attention 设计，在 block-diffusion 中效果有限。
- **dInfer (Ma et al., 2025b)**：面向云部署的大规模 MoE 加速框架，侧重 expert-parallel 执行，非专为推理效率设计。

> TEAM 是唯一专注于 **token-level 专家激活优化** 的方法。

---

## 3. 主要实验结果和性能指标

### 📈 性能汇总（来自 Table 1）
| Benchmark | 方法 | Score | APF ↓ | TPF ↑ | APT ↓ | Speedup ↑ |
|----------|-------|--------|--------|--------|--------|-----------|
| HumanEval | Vanilla | 79.27 | 53.34 | 2.91 | 18.33 | 1× |
|           | **TEAM** | **79.88** (+0.61) | **34.48** (↓35%) | **5.07** (↑1.74×) | **6.80** (↓63%) | **2.20×** |
| MBPP      | Vanilla | 65.76 | 49.59 | 2.74 | 18.10 | 1× |
|           | **TEAM** | **65.76** | **30.92** (↓38%) | **4.56** (↑1.66×) | **6.78** (↓63%) | **2.08×** |
| GSM8K     | Vanilla | 90.60 | 59.11 | 3.16 | 18.71 | 1× |
|           | **TEAM** | **90.30** (-0.30) | **36.20** (↓39%) | **4.79** (↑1.52×) | **7.56** (↓60%) | **1.83×** |
| Math-500  | Vanilla | 76.00 | 57.90 | 3.74 | 15.48 | 1× |
|           | **TEAM** | **75.40** (-0.60) | **36.31** (↓37%) | **5.57** (↑1.49×) | **6.52** (↓58%) | **1.64×** |
| **Average** | Vanilla | 77.91 | 54.99 | 3.14 | 17.66 | 1× |
|           | **TEAM** | **77.84** (-0.07) | **34.48** (↓37%) | **5.00** (↑1.59×) | **6.92** (↓61%) | **1.94×** |

> ✅ **平均提速达 1.94×，最高达 2.2×，APT 下降超过 60%**

---

### 🔬 消融实验结果（Ablation Study）
从 Figure 5 可见逐步加入各组件的效果：

| 组件添加顺序 | APT ↓ | Speedup ↑ | 贡献说明 |
|-------------|--------|------------|---------|
| Vanilla → +SEH | ~12 | ~1.5 | 提升接受率，减少总步数 |
| → +DCD       | ~9   | ~1.8 | 减少已解码 token 的冗余计算 |
| → +LAC       | ~6.9 | ~1.94 | 进一步压缩 cold token 的专家激活 |

> 所有三个模块协同作用，缺一不可，共同实现最大加速。

---

### 🔎 其他关键实验发现
- **Refresh-free caching 更优**（Table 2）  
  不需要像 dKV-Cache 那样定期刷新整个 block 的缓存，因为从 AR 初始化的 dLLM 中，已解码 token 表示非常稳定。禁用刷新反而略微提升精度并大幅提速。
- **Hot token 超参数敏感性分析**（Table 3）  
  当 `Th=0.7`, `Lh=3` 时达到最佳平衡：既不过度扩大 hot 集合导致额外开销，也不过度缩小导致漏掉潜在可接受 token。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE 与 dLLM 的简单结合是低效的**  
   尽管两者各自具备高效潜力，但 naive integration 导致专家激活高度分散，实际效率退化至接近 dense model。
   
2. **Temporal-Spatial Consistency 是突破口**  
   - 时间维度：已解码 token 可安全缓存；
   - 空间维度：masked tokens 路由集中，支持共享专家；
   - 接受顺序具有因果性和局部性，可用于预测 hot/cold tokens。

3. **TEAM 实现了真正的“稀疏 + 并行”双赢**  
   在降低 APF 的同时大幅提升 TPF，使 APT 降至 **低于名义路由成本（8 experts/token）**，证明了架构兼容性的突破。

---

### ⚠️ 局限性
- **依赖 block-diffusion 结构**：目前仅验证于 block-wise dLLMs（如 SDAR），是否适用于全序列 diffusion 待验证。
- **hot/cold 分类依赖启发式规则**：尚未完全自动化或学习化，可能存在次优划分。
- **对 extreme sparse routing 场景适应性未知**：例如当 expert capacity 极小时，double-round routing 可能引发冲突。

---

### 🔮 未来工作方向
- 将 hot token 判定机制升级为可学习模块（learnable gating policy）。
- 扩展至 vision-language 或 multimodal diffusion models。
- 探索与 quantization、pruning 等其他推理优化技术的联合应用。
- 支持 streaming 或 real-time decoding 场景下的动态调整策略。

---

## ✅ 总结
**TEAM** 是首个针对 **MoE dLLMs 推理低效问题** 提出系统性解决方案的工作。它通过挖掘 **时间-空间一致性** 特征，设计了三项轻量级、即插即用的专家激活策略，在几乎不影响性能的前提下实现了 **高达 2.2× 的推理加速**，显著提升了 MoE 与 dLLM 架构融合的实用价值。

> 💡 **一句话总结**：  
> TEAM 让 MoE 在 dLLM 中真正“稀疏起来”，实现了“更少专家激活，更多 token 接受”的高效推理新范式。

🔗 代码已开源：[https://github.com/PKU-SEC-Lab/TEAM-MoE-dLLM](https://github.com/PKU-SEC-Lab/TEAM-MoE-dLLM)

</details>

---

### 9. [Parallel Track Transformers: Enabling Fast GPU Inference with Reduced Synchronization](https://arxiv.org/abs/2602.07306)

**Authors**: Chong Wang, Nan Du, Tom Gunter, Tao Lei, Kulin Seth, Senyu Tong, Jianyu Wang, Guoli Yin, Xiyou Zhou, Kelvin Zou, Ruoming Pang  
**Category**: cs.DC  
**Published**: 2026-02-10  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.07306v1  

#### Abstract
Efficient large-scale inference of transformer-based large language models (LLMs) remains a fundamental systems challenge, frequently requiring multi-GPU parallelism to meet stringent latency and throughput targets. Conventional tensor parallelism decomposes matrix operations across devices but intr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Parallel Track Transformers: Enabling Fast GPU Inference with Reduced Synchronization

## 1. 论文的主要贡献和创新点

### 解决的问题
- **同步开销瓶颈**：在基于 Transformer 的大语言模型（LLMs）中，大规模推理通常依赖多 GPU 并行计算。传统的 **tensor parallelism** 虽然能将矩阵运算分布到多个设备上，但在每一层的 attention 和 feedforward 模块后都需要频繁的跨 GPU 同步（如 all-reduce），导致严重的通信延迟和可扩展性下降。
- 随着模型规模增大，这种同步操作成为制约推理效率的关键瓶颈。

### 提出的新方法：Parallel Track (PT) Transformer
- 将整个模型划分为多个独立并行运行的子 Transformer 实例，称为“**tracks**”。
- 每个 track 在本地处理完整的 token 流，仅在每隔一定层数（由 block depth $D$ 控制）时进行一次全局同步（all-reduce），实现参数融合。
- 这种设计被称为 **track parallelism**，显著减少了跨设备同步次数。

### 相比现有方法的优势
| 对比维度 | 传统 Tensor Parallelism | PT Transformer |
|--------|----------------------|---------------|
| 同步频率 | 每层两次（attention + FFN）→ 总共 $2L$ 次 | 每 $D$ 层一次 → 总共 $L/D$ 次 |
| 同步量级 | 全维度激活值交换 | 分轨后低维表示交换，通信量更小 |
| 通信模式 | 高频、细粒度，难以重叠优化 | 低频、规律性强，利于系统调度与优化 |
| 与 MoE 区别 | 条件执行、路由动态、负载不均 | 所有 token 经历所有 tracks，确定性同步 |

> ✅ **最大优势**：相比标准 tensor parallelism，PT 可实现高达 **16× 的同步操作减少**（当 $D=8$ 时），大幅缓解通信压力。

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
- 主要用于评估模型能力的标准基准套件：
  - **ARC-C/E**, **HellaSwag**, **PIQA**, **SciQ**, **WinoGrande**（常识推理）
  - **TriviaQA**（问答 EM）
  - **MMLU**（多学科理解，5-shot）
  - **GSM8K**, **MATH**（数学推理）
  - **HumanEval**（代码生成 Pass@1）

### 实验设置
- **模型规模**：6B、13B、30B 参数级别
- **PT 配置**：
  - 轨道数 $n = 8$
  - Track block depth $D \in \{2, 4, 8\}$
  - 使用 Grouped Query Attention (GQA)
- **训练细节**：
  - 6B 模型预训练 800B tokens
  - 13B 和 30B 模型各预训练 400B tokens
  - Dense 与 PT 模型采用相同训练流程以公平比较
- **部署环境**：
  - 硬件：8×H100 GPUs
  - 推理框架：**TensorRT-LLM** 与 **vLLM**

### 评估指标
| 指标 | 缩写 | 描述 |
|-----|------|------|
| Time to First Token | TTFT | 用户请求发出到首个输出 token 返回的时间 |
| Time Per Output Token | TPOT | 解码阶段每生成一个 token 所需平均时间 |
| Throughput | —— | 单位时间内生成的 output tokens 数量（tokens/sec） |
| Model Quality | 多项得分 | 上述各项 benchmark 的准确率或通过率 |

### 基线方法对比
- **Dense Baseline**：标准全量 Transformer 模型 + Tensor Parallelism
- **PT Variants**：不同 $D$ 设置下的 Parallel Track 架构
- 所有模型在同一硬件与服务栈下测试，确保公平性

---

## 3. 主要实验结果和性能指标

### 模型质量保持良好（尤其对大模型）
- **30B 模型** 在大多数 benchmark 上表现稳定：
  - MMLU 得分从 dense 的 0.630 到 PT($D=8$) 的 0.615，仅轻微下降
  - GSM8K 从 0.523 到 0.488，略有回落但仍具竞争力
  - HumanEval 最高达到 0.262（优于 dense 的 0.223）
- **13B 模型** 表现稳健，多数指标无明显退化
- **6B 模型** 对 $D$ 更敏感，特别是 MMLU 在 $D=8$ 下从 0.560 降至 0.360，显示小模型容忍度较低

> 🔍 发现：**模型越大，越能承受高 $D$（即更低频同步）带来的结构扰动**

### 推理效率显著提升（30B 模型，8×H100）

#### 在 TensorRT-LLM 中的表现
| 指标 | 改进幅度 |
|------|---------|
| **Throughput** | 最高提升 **31.90%**（如输入 4096 → 输出 4096 场景） |
| **TTFT** | 减少 **15–30%**（例如输入长度 4096 时从 125ms → 94ms） |
| **TPOT** | 降低 **2–12%**（最长序列下尤为明显） |

#### 在 vLLM 中的表现
| 指标 | 改进幅度 |
|------|---------|
| **Throughput** | 多数场景下优于 baseline，最高提升超 30%
| **TTFT** | 输入越长，增益越大；63488 长度下从 ~2981ms → ~2453ms（↓17.7%）
| **TPOT** | 持续改善，最长上下文下降约 10%

> 📊 示例：在 vLLM 中，输入 16384、输出 128 的 TPOT 从 9.46ms → 9.10ms（↓3.8%）

### 消融实验结果
- **Track Block Depth $D$ 是关键超参**：
  - $D=2$: 同步减少 75%，性能几乎无损
  - $D=4$: 减少 87.5%，大模型仍稳定
  - $D=8$: 减少 **93.75%**（等效于 16× 减少），适合 13B+ 规模
- **轨道数量 $n=8$** 设计平衡了并行粒度与通信成本
- **Grouped Query Attention (GQA)** 加强了内存与带宽效率，辅助发挥 PT 优势

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **大幅减少同步可行且有效**：通过引入 track 结构，可在几乎不影响模型质量的前提下（尤其是大模型），将同步次数从 $2L$ 降到 $L/D$。
2. ✅ **推理延迟显著降低**：由于通信减少，TTFT 和 TPOT 明显下降，在长上下文场景中优势更加突出。
3. ✅ **吞吐量全面提升**：在两种主流 LLM serving stack（TensorRT-LLM 和 vLLM）中均观察到一致性的 throughput 提升，验证了系统的通用性和实用性。
4. ✅ **适用于大规模模型优先**：PT 架构更适合 13B 及以上级别的模型，在小模型（如 6B）中需谨慎选择 $D$

### 方法的局限性
- ❗ **并非完全免同步**：虽然同步频率大幅降低，但仍需要周期性 all-reduce 操作来维持语义一致性。
- ❗ **对小模型适配性较差**：6B 模型在 $D=8$ 时出现明显性能滑坡（如 MMLU ↓35%），限制其在轻量级场景的应用。
- ❗ **额外内存占用**：每个 GPU 存储一个完整 track 的副本，可能增加显存消耗（尽管单 track 维度更低）
- ❗ **当前仅支持 dense MLP 替换**：原生 MoE 集成需进一步设计（虽已有 PT-MoE 扩展工作 Zhou et al., 2025）

### 未来工作方向
- 🔮 **结合 MoE 实现 PT-MoE 架构**：已在文中提及作为延伸（Zhou et al., 2025），有望同时利用稀疏激活与 track parallelism 实现更高效率
- 🔮 **自动调节 $D$ 动态策略**：根据输入长度或负载情况动态调整同步间隔
- 🔮 **探索异构 track 设计**：允许 tracks 内部结构差异化，增强表达能力
- 🔮 **扩展至训练阶段**：目前 focus 在 inference，但 track parallelism 也可为 training 提供新的并行调度空间

---

> 💡 **总体评价**：  
> Parallel Track Transformer 是一种**面向系统效率驱动的架构创新**，它重新思考了 tensor parallelism 的底层假设——“必须逐层同步”。通过将模型拆分为多个并行 track 并控制同步节奏，实现了推理速度的重大突破，是迈向高效 LLM serving 的重要一步。尤其对于苹果等注重端侧与私有云部署的企业，该技术具有极高的工程价值。

</details>

---

### 10. [Scalable Dexterous Robot Learning with AR-based Remote Human-Robot Interactions](https://arxiv.org/abs/2602.07341)

**Authors**: Yicheng Yang, Ruijiao Li, Lifeng Wang, Shuai Zheng, Shunzheng Ma, Keyu Zhang, Tuoyu Sun, Chenyun Dai, Jie Ding, Zhuo Zou  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.07341v1  

#### Abstract
This paper focuses on the scalable robot learning for manipulation in the dexterous robot arm-hand systems, where the remote human-robot interactions via augmented reality (AR) are established to collect the expert demonstration data for improving efficiency. In such a system, we present a unified f...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Scalable Dexterous Robot Learning with AR-based Remote Human-Robot Interactions 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文致力于解决**灵巧机器人臂手系统（dexterous robot arm-hand systems）在复杂操作任务中的可扩展学习难题**。传统方法面临以下挑战：
- **专家示范数据获取困难**：高质量的人类示范耗时且成本高；
- **行为克隆（Behavior Cloning, BC）存在泛化能力差、策略崩溃（policy collapse）等问题**；
- **强化学习（Reinforcement Learning, RL）训练效率低、样本利用率不高**。

### 提出的新方法与创新思路
作者提出了一种**结合增强现实（AR）、模仿学习与对比学习的两阶段统一框架**，具体包括：

1. **基于AR的远程人机交互系统用于数据采集**  
   - 利用 **HoloLens 2 AR headset** 捕捉人类专家的手部姿态，并通过无线连接（WiFi/5G）将动作实时映射到物理机器人上。
   - 使用 **Unity平台 + ROS TCP Connector** 实现跨设备兼容性，支持多种AR头显与机器人系统的集成。
   - 构建了一个通用、低成本、可远程操作的数据收集系统。

2. **模仿与对比学习辅助的强化学习算法（Imitation and Contrastive Learning Assisted RL）**
   - **第一阶段：行为克隆预训练（BC Pretraining）**  
     利用少量专家轨迹（仅15条）进行监督回归训练，快速初始化策略网络，显著提升后续RL探索效率。
   - **第二阶段：带投影头（projection head）的对比增强Soft Actor-Critic（SAC）算法**  
     引入一个**可学习的MLP投影头**，通过**对比损失（contrastive loss）** 将智能体的动作表示拉近至专家状态-动作对的表示空间，从而约束策略向高回报专家行为靠拢。

3. **事件驱动奖励机制增强安全性**  
   设计了包含任务成功、碰撞惩罚、接触无进展等多条件判断的 `event-driven reward`，提高训练过程的安全性和鲁棒性。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **数据效率** | 仅需少量专家演示（15条），即可实现高效预训练；结合对比学习进一步减少RL所需交互次数。 |
| **训练速度** | 比标准SAC快约4倍，在PyBullet仿真中收敛时间从300分钟降至75分钟。 |
| **性能表现** | 显著高于PPO和SAC基线的成功率（如球抓取达91.8% vs SAC的84.2%）。 |
| **稳定性** | 成功避免“策略崩溃”问题，消融实验证明对比学习是关键因素。 |
| **实用性** | 支持真实世界部署，实现了较小的sim-to-real gap。 |

---

## 2. 核心实验方法和设置

### 数据集来源
- **自建数据集**：通过提出的AR远程操控系统采集了 **15条专家演示轨迹**，涵盖变形球与瓶子的抓取任务。
- 所有数据均来自作者构建的真实机器人系统（CGXi-G6机械臂 + Inspire灵巧手）。

### 实验环境与硬件配置
- **仿真环境**：PyBullet（Python）
- **真实系统测试**：CGXi-G6六自由度机械臂 + Inspire五指灵巧手 + Orbbec深度相机 + HoloLens 2
- **感知模块**：采用YOLOv8进行目标检测，输入为立体视觉图像
- **计算资源**：Intel Xeon Gold 6226R CPU + NVIDIA RTX 4090 GPU

### 状态与动作空间
- **状态 $ s_t $**：20维向量（关节角度、末端位姿、物体位置等）
- **动作 $ a_t $**：8维控制指令（关节偏移量 + 手部姿态）

### 评估指标
| 指标 | 描述 |
|------|------|
| **Mean Reward** | 平均累积奖励值 |
| **Success Rate** | 完成抓取任务的比例（越高越好） |
| **Convergence Speed** | 达到稳定性能所需的训练迭代数与时间 |
| **Training Time Consumption** | 总训练耗时（分钟） |
| **Policy Collapse Detection** | 是否出现性能骤降或发散现象 |

### 基线方法对比
- **PPO**（Proximal Policy Optimization）
- **SAC**（Soft Actor-Critic）
- **BC+SAC**（仅加行为克隆预训练，无对比学习）——作为消融实验对照组

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table III）

#### 在仿真环境中（PyBullet）的结果：

| 任务 | 指标 | PPO | SAC | BC+SAC | **Ours** |
|------|------|-----|-----|--------|---------|
| **球抓取** | 平均奖励 | 584.03 | 1055.16 | 1070.77 | **1145.61** |
|           | 成功率 | 37.33% | 84.22% | 85.49% | **91.80%** |
|           | 收敛迭代数 | 150 | 140 | 30 | **30** |
|           | 训练耗时（min） | 355 | 300 | 75 | **75** |
| **瓶抓取** | 平均奖励 | 696.76 | 1052.78 | 1110.51 | **1155.79** |
|           | 成功率 | 44.18% | 83.93% | 88.66% | **91.83%** |
|           | 收敛迭代数 | 130 | 120 | 25 | **23** |
|           | 训练耗时（min） | 340 | 270 | 60 | **55** |

> ✅ **结论**：所提方法在所有指标上全面超越基线，尤其在训练效率方面提升明显（约4倍于SAC），且成功率最高。

### 真实世界实验结果（Fig. 8）
- 在50次真实抓取试验中验证：
  - **变形球抓取成功率**：Ours (**90.0%**) > BC+SAC (86.0%) > SAC (72.0%) > PPO (66.0%)
  - **瓶子抓取成功率**：Ours (**94.0%**) > BC+SAC (80.0%) > SAC (70.0%) > PPO (40.0%)

> 📌 注意：由于仿真中未建模弹性表面形变（elastomer surface deformation）和精细几何特征，**球体抓取在真实环境中性能略有下降**，但整体仍保持领先。

### 消融实验结果（Ablation Study）
- **BC预训练的作用**：显著降低RL训练时间（从~300min降至<80min），主导初期性能提升。
- **对比学习的作用**：
  - 加入projection head后，策略更稳定，避免了后期性能退化；
  - 对比损失有效防止“策略偏离专家行为”的问题；
  - 在仅有少量专家数据下仍能维持高性能。

> 🔍 图6显示：Ours方法不仅收敛更快，而且最终奖励更高、波动更小。

---

## 4. 关键结论和发现

### 主要发现
1. **AR-based teleoperation 是一种高效、灵活、低成本的专家数据采集方式**，适用于远程、多场景下的灵巧操作示范收集。
2. **行为克隆预训练极大提升了RL的初始策略质量与样本效率**，解决了冷启动问题。
3. **引入对比学习与projection head 可有效缓解策略崩溃问题**，使RL过程中策略始终受专家行为引导。
4. **所提方法在仿真与真实环境中均表现出卓越性能**，具备良好的迁移能力和实用性。
5. **极少量专家数据（15条轨迹）即可取得优异效果**，证明了方法的数据高效性。

### 方法的局限性
- **依赖精确的姿态估计**：AR系统对手部追踪精度要求较高，光照、遮挡可能影响性能。
- **对象几何简化导致sim-to-real gap**：仿真中缺乏高保真物体建模（如软体变形），影响某些任务（如软球抓取）的表现。
- **当前仅限单臂单任务**：尚未扩展至双臂协同或多任务联合学习。
- **未融合触觉反馈**：完全依赖视觉感知，难以处理高度依赖力控的任务。

### 未来工作方向
1. **跨多个物理模拟器联合训练策略**，增强系统鲁棒性（参考[54] Polysim）。
2. **缩小sim-to-real gap**：
   - 引入高保真物体几何重建；
   - 融合2D语义与3D几何信息（如[55] Visual-Geometry Diffusion Policy）。
3. **加入触觉与力觉反馈**，支持复杂可变形物体的操作。
4. **拓展至多智能体系统**：应用于双臂灵巧机器人协作场景。
5. **结合多模态模仿学习**（multimodal imitation learning），整合语言指令、视频演示等多种形式指导。

---

> 💡 **补充材料链接**：https://cyberyyc.github.io/ （含更多演示视频）

</details>

---

### 11. [MARTI-MARS$^2$: Scaling Multi-Agent Self-Search via Reinforcement Learning for Code Generation](https://arxiv.org/abs/2602.07848)

**Authors**: Shijie Wang, Pengfei Li, Yikun Fu, Kaifeng Liu, Fangyuan Li, Yang Liu, Xiaowei Sun, Zonglin Li, Siyao Zhao, Jian Zhao, Kai Tian, Dong Li, Junqi Gao, Yutong Zhang, Yiqun Chen, Yuqiang Li, Zoe Li, Weinan Zhang, Peng Ye, Shuyue Hu, Lei Bai, Bowen Zhou, Kaiyan Zhang, Biqing Qi  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.07848v1  

#### Abstract
While the complex reasoning capability of Large Language Models (LLMs) has attracted significant attention, single-agent systems often encounter inherent performance ceilings in complex tasks such as code generation. Multi-agent collaboration offers a promising avenue to transcend these boundaries. ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MARTI-MARS²: Scaling Multi-Agent Self-Search via Reinforcement Learning for Code Generation

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在复杂推理任务（如代码生成）中存在**单智能体性能瓶颈**。尽管已有研究尝试通过多智能体协作提升性能，但现有框架通常依赖于基于提示的测试时交互（prompt-based TTS）或同质化参数共享的多角色配置（homogeneous multi-role），这限制了错误纠正能力和策略多样性，难以突破单模型的能力上限。

### 提出了什么新方法或新思路
本文提出 **MARTI-MARS²**（Multi-Agent Reinforced Training and Inference Framework with Self-Search Scaling），一个统一的多智能体强化学习训练与推理框架，其核心创新包括：

- **将多智能体协作建模为可学习的动态环境**：首次将多智能体协同探索过程形式化为一个可学习的动态环境，使智能体能通过强化学习（RL）迭代优化自身策略。
- **从同质到异质的演进路径**：
  - **Homo-MARS²**：参数共享的多角色训练，提升初期收敛速度。
  - **Heter-MARS²**：参数独立的异构多智能体训练，打破策略同质化，实现更高性能上限。
- **高效的推理策略 MARS²-T+**：结合结构化错误反馈、深度引导探索和预训练 Reward Model（RM），显著提升测试时搜索（TTS）效率与稳定性。

### 相比现有方法的优势
| 维度 | MARTI-MARS² 优势 |
|------|------------------|
| **训练范式** | 超越传统单智能体 RL 和同质多角色，引入异构多智能体联合训练，增强策略多样性 |
| **推理机制** | 引入细粒度错误反馈与深度引导，克服 Vanilla TTS 浅层搜索缺陷 |
| **评估机制** | 使用高质量 RM 缓解“奖励欺骗”（reward hacking），提升最终选择的鲁棒性 |
| **系统设计** | 支持异步动态更新，解决多智能体数据不平衡导致的训练阻塞问题 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：基于 DeepCoder 发布的开源编程数据集，经过过滤后保留 7,992 个编码提示。
- **评估基准**：**LiveCodeBench (v6)**，发布于基础 LLM 开发之后，确保无数据泄露风险。

### 实验设置和评估指标
#### 模型规模
使用多个开源 LLM，涵盖不同参数量级：
- **8B 系列**：Qwen3-8B, AReaL-boba-2-8B
- **14B 系列**：Qwen3-14B, AReaL-boba-2-14B, DeepCoder-14B
- **32B 系列**：Qwen3-32B, Nemotron-32B

#### 评估指标
| 指标 | 含义 |
|------|------|
| **Pass@1** | 单次采样下生成正确代码的概率，衡量基础推理能力 |
| **Pass@1(MCTS)** | 在 MCTS 搜索后选出最优解的准确率，反映推理扩展能力 |
| **Pass@N** | N 次采样中至少有一次正确的概率，体现生成多样性与成功率 |
| **Diversity Metrics** | 包括 AEC（语义聚类数）、DA@K（算法种类数）、EA（有效算法数）、NAUADC（多样性曲线下面积）、G-Vendi（认知策略多样性）等 |

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **Vanilla-GRPO** | 单智能体强化学习基线，使用 Group Relative Policy Optimization |
| **Homo-MARS²** | 参数共享的多角色训练 |
| **AB-MCTS** | 自适应分支蒙特卡洛树搜索，代表主流 TTS 方法 |
| **Closed-source Baselines** | 如 GPT-5.1, O4-Mini(high), Gemini 等闭源模型作为性能参照 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **Heter-MARS²（双 32B 模型）** 在 LiveCodeBench 上达到 **77.7% Pass@1(MCTS)**，**超越 GPT-5.1 和 O4-Mini(high)**。
- 在 14B 多模型组合中，Heter-MARS² 实现 **81.7% Pass@K**，显著高于基线。
- MARS²-T+ 将 Vanilla TTS 的 Pass@1(MCTS) 从 54.3% 提升至 **57.7%**（Qwen3-8B），并进一步提升到 **62.9%**（两模型协作）。

### 与基线方法的对比结果
| 方法 | Pass@1(MCTS) 提升（典型场景） | 备注 |
|------|-------------------------------|------|
| Homo-MARS² vs GRPO | +4.6% ~ +5.1% | 初期快速收敛，后期易陷入局部最优 |
| Heter-MARS² vs Homo-MARS² | +2.9% ~ +3.5% | 显著突破同质化瓶颈 |
| MARS²-T+ vs Vanilla TTS | +3.4% ~ +5.8% | 推理阶段增益明显 |
| Heter-MARS² (32B×2) vs GPT-5.1 | **更高性能** | 达到甚至超越顶尖闭源模型水平 |

### 消融实验结果
#### （1）奖励模型（RM）的作用
- 使用在训练轨迹上微调的 RM 可显著提升 TTS 稳定性：
  - AUC-ROC 从 73.0% → **79.3%**
  - Spearman 相关性从 39.7 → **50.6**
- 若仅用通用 RM（如 Skywork-Reward），性能提升有限，验证了**任务对齐 RM 的必要性**。

#### （2）错误反馈与深度引导的重要性
- 移除错误反馈或深度引导会导致：
  - 深层节点比例下降（<7%）
  - Pass@1(MCTS) 出现轻微退化（如 -0.3%）
- 三者协同（错误反馈 + 深度引导 + RM）带来最大收益，证明其构成**协同进化三角**。

#### （3）多样性分析
| 方法 | DA@K ↑ | EA ↑ | NAUADC ↑ | G-Vendi ↑ |
|------|--------|-------|----------|-----------|
| Heter-MARS² (14B, 3-model) | **1.941** | **1.544** | **2.033** | **8.719** |
| Homo-MARS² | 1.837 | 1.445 | 1.889 | 8.750 |
| Vanilla TTS | 1.882 | 1.478 | 1.962 | 8.808 |

> 注：Heter-MARS² 在算法多样性（DA@K）和分布均匀性（EA）上表现最佳，而 G-Vendi 表明其也提升了认知策略多样性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **多智能体协作是超越单模型性能天花板的关键路径**  
   从单智能体 → 同质多角色 → 异构多智能体，形成一条清晰的**多智能体扩展定律（scaling law）**，每一步都带来更高的 RL 性能上限、更强的 TTS 能力和更大的策略多样性。

2. ✅ **策略多样性是智能扩展的核心维度**  
   论文首次系统验证：**policy diversity 是与计算量、模型大小同等重要的扩展轴**。异构智能体通过互补视角和交叉纠错，显著提升整体鲁棒性和探索广度。

3. ✅ **训练与推理需协同设计**  
   MARS² 训练出的协作模式，只有在 MARS²-T+ 的推理机制下才能充分释放潜力，二者形成**正向增强闭环**。单纯堆叠未训练的多模型无法获得同等收益。

4. ✅ **错误信号的有效利用至关重要**  
   Vanilla TTS 仅提供二值反馈，而 MARS²-T+ 引入结构化错误诊断（如输入输出不匹配、运行时异常追踪），极大提升了修正精度。

### 方法的局限性
- **计算资源消耗大**：每个智能体独占一个完整节点（8×H200 GPU），限制了大规模部署。
- **依赖高质量 RM**：若 RM 未能准确预测私有测试通过率，则最终选择可能失效。
- **当前任务仍较孤立**：实验集中在单个编程题求解，尚未涉及跨任务、长期规划等更复杂的组织级协作场景。

### 未来工作方向
- 🔄 **探索更高效的异构架构**：如混合专家（MoE）或多模态代理协作。
- 🏗️ **构建企业级多智能体协作平台**：面向真实组织流程中的复杂决策与执行任务。
- 🔍 **发展更强大的自监督 RM**：减少对外部标注数据的依赖，实现端到端自我进化。
- 📈 **挖掘下一代多智能体扩展规律**：从静态协作迈向动态涌现、角色自组织的高级智能形态。

---

> **一句话总结**：  
> MARTI-MARS² 揭示了一条通往更强 LLM 推理能力的新路径——**通过异构多智能体强化学习，以策略多样性为驱动力，实现超越单模型极限的系统性性能跃迁**。

</details>

---

### 12. [FEM-Informed Hypergraph Neural Networks for Efficient Elastoplasticity](https://arxiv.org/abs/2602.07364)

**Authors**: Jianchuan Yang, Xi Chen, Jidong Zhao  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.07364v1  

#### Abstract
Graph neural networks (GNNs) naturally align with sparse operators and unstructured discretizations, making them a promising paradigm for physics-informed machine learning in computational mechanics. Motivated by discrete physics losses and Hierarchical Deep Learning Neural Network (HiDeNN) construc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FEM-Informed Hypergraph Neural Networks for Efficient Elastoplasticity

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Physics-Informed Neural Networks (PINNs)** 在求解非线性 **elastoplasticity**（弹塑性）问题时面临以下挑战：
- **高计算成本**：依赖 Automatic Differentiation (AD) 导致计算图庞大，训练缓慢。
- **精度不足**：在复杂几何、循环加载（cyclic loading）、材料硬化（hardening）等场景下预测误差大。
- **边界条件处理困难**：Dirichlet 和 Neumann 边界条件难以有效施加，易导致梯度失衡。
- **黑箱建模**：网络作为黑盒代理，缺乏物理可解释性。

### 提出的新方法
作者提出了一种 **FEM-Informed Hypergraph Neural Networks (FHGNN)**，将有限元法（FEM）的计算流程嵌入到 GNN 的消息传递机制中，构建一个**物理一致、白盒化、高效**的机器学习框架。

#### 创新点：
1. **节点-单元超图表示（Node-element Hypergraph）**  
   将 FEM 网格建模为超图 $ G=(V, C, \mathcal{E}) $，其中：
   - $ V $：网格节点
   - $ C $：单元（视为第二类节点）
   - $ \mathcal{E} $：节点与单元之间的关联边  
   这种结构天然支持非结构化网格和复杂几何。

2. **FEM 计算流程嵌入消息传递层**  
   设计三个可微分的消息传递模块，分别对应 FEM 的核心步骤：
   - **Isoparametric Transformation Layer**：计算雅可比矩阵和物理域形函数梯度。
   - **Strain-Stress Layer**：基于位移场计算应变和应力（支持 J2 plasticity）。
   - **Global Internal Force Layer**：组装单元内力向量至全局节点。

3. **端到端可微 + 物理驱动训练**  
   - 不需要标签数据（label-free），仅通过物理损失函数进行训练。
   - 所有变量（如节点坐标 $ v_j $、位移 $ u_j $）均可设为可优化参数，支持 **r-adaptivity**（网格自适应）。

4. **高效的离散变分损失函数**  
   推荐使用能量形式的损失函数（`ℒ_energy`），相比 Galerkin 损失更稳定、收敛更快。

### 相比现有方法的优势
| 方面 | 传统 PINN / MLP | FHGNN |
|------|------------------|--------|
| 架构 | 黑箱 MLP，全连接 | 白盒 GNN，嵌入 FEM 流程 |
| 微分方式 | AD（高阶导数代价高） | FEM 形函数梯度（稀疏、局部） |
| 几何适应性 | 依赖规则网格或坐标变换 | 支持任意非结构化网格 |
| 边界条件 | 需额外惩罚项 | 通过掩码直接施加 Dirichlet 条件 |
| 效率 | 随 DOF 增长急剧下降 | GPU 并行 + 局部操作，扩展性强 |
| 可解释性 | 差 | 强（每步对应明确物理意义） |

---

## 2. 核心实验方法和设置

### 数据集与问题设置
所有实验均基于 **Abaqus** 生成的 FEM 解作为参考真值，共测试五类 3D 弹塑性基准问题：
1. **2D/3D 塑性基础（Plastic Footing）**：验证循环加载下的响应。
2. **3D 线性硬化悬臂梁（Cantilever Beam）**：含 isotropic/kinematic hardening。
3. **复杂工件（Workpiece）**：复杂几何 + kinematic hardening。
4. **双材料带孔板（Bi-material Plate with Hole）**：材料不连续 + 应力集中。

### 实验设置
- **实现平台**：PyTorch Geometric (PyG)，GPU 加速（NVIDIA RTX 4090）。
- **优化器**：L-BFGS，初始步长 1.0。
- **输入**：节点坐标、单元连接关系、高斯点信息。
- **输出**：节点位移、单元级应变/应力、等效塑性应变 $ \bar{\varepsilon}^p $。

### 评估指标
- **相对 L2 误差**：
  $$
  L_2 = \frac{\|u - u^*\|_2}{\|u^*\|_2}
  $$
- **平均绝对误差（MAE）**：
  $$
  \text{MAE} = \frac{1}{N}\sum |u_i - u_i^*|
  $$

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **PINN (MLP-based)** | 多层感知机 + AD + PDE 残差损失 |
| **PIMLP** | 使用 FEM 形函数梯度构造能量损失的 MLP 模型 |
| **PIGCN** | 图卷积网络（Chebyshev GCN）学习从坐标到位移的映射 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 表 1：3D 线性硬化悬臂梁（160×40×40 网格，768,000 DOFs）
| Method | Time (s) | L2_ux | L2_uy | MAE_ε̅p |
|--------|----------|--------|--------|---------|
| PIMLP | 737.6 | 1.85E-01 | 6.52E-02 | 2.50E-3 |
| PIGCN | 2,520.6 | 1.28E-01 | 3.38E-02 | 2.10E-3 |
| **FHGNN** | **145.51** | **4.80E-04** | **3.19E-04** | **6.10E-6** |

> ✅ **速度提升约 3×，误差降低两个数量级以上**

#### 表 2：3D 双材料带孔板（54,402 四面体单元）
| Method | Time (s) | L2_σxx | L2_σyy | L2_ε̅p | MAE_ε̅p |
|--------|----------|--------|--------|--------|---------|
| PIMLP | 108.14 | 8.89E-02 | 7.07E-01 | 8.77E-02 | 1.47E-03 |
| PIGCN | 201.49 | 5.31E-02 | 3.05E-01 | 3.38E-02 | 6.61E-04 |
| **FHGNN** | **59.80** | **5.78E-05** | **1.77E-04** | **6.31E-05** | **1.38E-06** |

> ✅ **不仅快，而且对导出场（stress, strain）预测更准确**

#### 表 3：与多核 FEM 的效率对比（不同网格密度）
| Mesh Size | FEM (8-core) | FHGNN (GPU) | Speedup |
|-----------|---------------|--------------|---------|
| 40×10×10 | 1s | 1.936s | ×0.5 |
| 80×20×20 | 8s | 12.18s | ×0.65 |
| 160×40×40 | 399s | 145.51s | **×2.74** |
| 150×50×50 | 1021s | 286.92s | **×3.56** |

> ✅ **在大规模问题上显著优于并行 FEM**

### 消融实验结果（来自 Section 3.6.3）

#### 不同损失函数对比（Isotropic Hardening Cantilever Beam）
| Loss Type | Epochs | Time (s) | L2_ux | Converged? |
|----------|--------|----------|--------|------------|
| Energy Loss | 5,000 | 145.5 | 4.80E-04 | ✅ Yes |
| Galerkin Loss | 78,384 | 15,753.9 | 9.42E-01 | ❌ No (发散) |

> 🔍 发现：随着网格细化，Galerkin 损失的误差反而上升（condition number 更大），而能量损失始终稳定收敛。

#### 转移学习加速效果（Transfer Learning）
- 使用粗网格解作为初值，在最密网格上训练：
  - 从零开始：需 ~5000 迭代
  - 转移学习：仅需 ~1000 迭代即可收敛
> ✅ 收敛速度提升 4 倍以上

---

## 4. 关键结论和发现

### 主要发现
1. **FHGNN 显著优于现有 PINN 变体**  
   在多个 3D 弹塑性问题上，实现了 **>2× 速度提升** 和 **>100× 精度提升**。

2. **FEM 计算嵌入是关键**  
   利用 FEM 的局部支撑性和预计算特性，避免了 AD 的高开销，提升了训练效率和稳定性。

3. **能量损失函数优于 Galerkin 损失**  
   理论分析表明其 Hessian 条件数更小，优化更稳定；实验验证其收敛更快、更可靠。

4. **支持 r-adaptivity 和 Gauss 点优化**  
   通过将节点坐标设为可微变量，可实现类似 FEM 的网格自适应更新，并进一步降低系统能量。

5. **可扩展至大规模工业级问题**  
   在百万 DOF 级别仍保持高效，且 GPU 实现已能超越多核 CPU 上的传统 FEM。

### 方法的局限性
- 当前仅适用于具有显式能量泛函的材料模型（如 J2 plasticity）。
- 对于复杂本构模型（如非关联流动法则、损伤演化），能量函数可能无法闭式表达。
- 超图结构虽灵活，但在极端非均匀网格下可能影响聚合效率。

### 未来工作方向
1. **拓展至通用本构模型**  
   开发兼容自动微分的通用回映算法（return mapping），支持任意 yield surface。
   
2. **多物理场耦合**  
   结合 DEM（颗粒介质）、FVM（流体力学）等方法，构建统一的可微分多物理场求解器。

3. **算子学习融合**  
   探索与 **FNO**、**DeepONet** 等算子学习方法结合，用于多尺度建模。

4. **实验数据同化**  
   引入真实测量数据进行联合优化，提升模型在实际工程中的适用性。

5. **高效消息传递模块设计**  
   从大数据中学习更高效的 message-passing 规则，超越标准 FEM 公式。

---

> 📌 **总体评价**：该论文成功地将 FEM 的数值稳健性与 GNN 的灵活性相结合，提出了一种**可扩展、高效、物理一致**的新型 PINN 范式，为非线性固体力学的大规模仿真提供了强有力的替代方案。代码已开源：https://github.com/yjc0416/FEM-Informed-Hypergraph-Neural-Networks

</details>

---

### 13. [Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective](https://arxiv.org/abs/2602.08009)

**Authors**: Rui Li, Zeyu Zhang, Xiaohe Bo, Quanyu Dai, Chaozhuo Li, Feng Wen, Xu Chen  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.08009v1  

#### Abstract
Multi-agent architectures built on large language models (LLMs) have demonstrated the potential to realize swarm intelligence through well-crafted collaboration. However, the substantial burden of manual orchestration inherently raises an imperative to automate the design of agentic workflows. We fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Multi-Agent Systems (MAS)** 在协调 LLM Agents 时面临三大挑战：
- **Adaptivity（适应性）**：传统方法依赖预定义的静态拓扑（如链式、星型），无法根据动态任务流调整通信路径。
- **Scalability（可扩展性）**：当代理数量变化时，需重新优化整个工作流，成本高昂；中心化控制器随规模扩大而成为瓶颈。
- **Robustness（鲁棒性）**：存在单点故障风险（Single Point of Failure, SPoF），恶意或错误行为的代理可能污染全局系统。

这些问题源于现有方法要么是“communication-agnostic”（通信无关的离线搜索），要么依赖脆弱的“meta-controller”进行集中控制。

---

### 提出的新方法：RAPS
作者提出 **RAPS (Reputation-Aware Publish-Subscribe)** 范式，将多智能体协调问题类比为 **Dynamic Ad-Hoc Networking（动态自组织网络）** 中的经典问题，并构建了一个去中心化、意图驱动的通信框架。

#### 核心架构（三层设计）
| 层级 | 组件 | 功能 |
|------|------|------|
| **Substrate（底层）** | Distributed Publish-Subscribe Protocol | 去中心化的消息分发机制，基于语义意图匹配而非固定拓扑 |
| **Overlay I** | Reactive Subscription | 代理在运行时动态更新自身订阅意图（intent），实现自适应角色演化 |
| **Overlay II** | Bayesian Reputation | 每个代理配备本地“watchdog”，通过贝叶斯估计维护对等方的信誉，实现去中心化信任管理 |

---

### 相比现有方法的优势
| 特性 | RAPS | 传统方法（如 GPTSwarm, Puppeteer） |
|------|------|-------------------------------|
| **Adaptivity** | ✅ 运行时动态调整意图与路由 | ❌ 固定拓扑或依赖中心控制器 |
| **Scalability** | ✅ 支持代理自由加入/退出，无需重训练 | ❌ 新增代理需重新优化图结构 |
| **Robustness** | ✅ 分布式信誉机制隔离恶意节点 | ❌ 中心控制器被攻破则全网崩溃 |
| **训练开销** | ✅ 完全无训练（training-free） | ❌ 需要大量离线搜索或参数学习 |

> 💡 **核心思想转变**：从“**谁来处理？**”（host-centric）转向“**什么内容需要谁？**”（content-centric），实现了真正的自发协作。

---

## 2. 核心实验方法和设置

### 数据集
在五个代表性基准上进行全面评估，涵盖三类任务：

| 类别 | 数据集 | 任务描述 | 评估指标 |
|------|--------|----------|-----------|
| **General Reasoning** | MMLU | 多学科常识推理（STEM、人文等） | Accuracy |
| **Mathematical Reasoning** | GSM8K, SVAMP, AQuA | 数学应用题求解 | Accuracy |
| **Code Generation** | HumanEval | Python 编程问题生成 | Pass@1 |

> 📊 总计覆盖 5 个 benchmark，测试零样本泛化能力。

---

### 实验设置
- **LLM Backbone**: 统一使用 `GPT-4o-mini` 作为所有代理的核心模型，确保公平比较。
- **Agent 数量**: 默认配置 5 个代理，部分实验中变量分析扩展至最多 30 个。
- **通信轮次（k）**: 最大通信步数设为 5。
- **Broker 实现**:
  - 主要使用 `text-embedding-3-small` 进行高效语义匹配；
  - 消融实验中也测试了 LLM-based Broker 的效果。

---

### 基线方法对比
共四类 baseline：

| 类型 | 代表方法 | 特征 |
|------|--------|------|
| **Single-Agent Models** | CoT, ComplexCoT, SC | 单模型链式思维或多路径投票 |
| **Static Multi-Agent Models** | Chain, Star, Tree, Random, LLM-Debate | 固定连接结构 |
| **Communication-Agnostic Models** | GPTSwarm, AFlow, G-Designer | 离线搜索最优图结构 |
| **Meta-Controlled Models** | AutoAgents, Puppeteer, MAS-Zero | 使用高权限 meta-controller 实时调度 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）
| 方法 | MMLU | GSM8K | SVAMP | AQuA | HumanEval | **Average** |
|------|-------|--------|--------|-------|------------|-------------|
| G-Designer (SOTA prior) | 86.3 | 93.2 | 90.7 | 79.4 | 90.2 | 88.0 |
| **RAPS (Ours)** | **88.2** | **95.4** | **92.2** | **82.6** | **91.5** | **90.0** |

✅ RAPS 在所有任务上均达到 **state-of-the-art 性能**，平均提升 **+2.0%**。

---

### 与基线方法的对比结果
- **优于静态结构**：如 Chain 在 SVAMP 上仅得 82.6%，远低于 RAPS 的 92.2%，说明刚性流程难以应对多样化推理需求。
- **优于离线优化方法**：尽管 AFlow 和 G-Designer 表现强劲，但仍是 communication-agnostic，在推理过程中无法响应上下文变化。
- **优于中心化控制**：Puppeteer 平均得分仅为 84.0%，且存在严重 SPoF 问题（见下文鲁棒性测试）。

---

### 消融实验结果（Table 3）
移除关键组件后的性能下降显著：

| 变体 | MMLU ↓ | GSM8K ↓ | HumanEval ↓ |
|------|--------|---------|--------------|
| RAPS (Full) | 88.2 | 95.4 | 91.5 |
| w/o RS (无 Reactive Subscription) | 85.6 (-2.6) | 93.7 (-1.7) | 89.3 (-2.2) |
| w/o BR (无 Bayesian Reputation) | 86.9 (-1.3) | 94.5 (-0.9) | 90.7 (-0.8) |
| w/o Both | 83.7 (-4.5) | 92.8 (-2.6) | 88.5 (-3.0) |

> 🔍 发现：**Reactive Subscription 对性能影响更大**，表明动态意图演化是提升协作质量的关键驱动力。

---

### 鲁棒性测试（Table 2）—— Byzantine Stress Test
注入不同比例的 **adversarial agents（说谎者）** 后，系统准确性变化如下（MMLU）：

| 方法 \ 场景 | 5T0A | 4T1A | 3T2A | 2T3A | 5T5A（半数对抗） |
|------------|-----|-----|-----|-----|------------------|
| Chain | 84.3 | 72.5 | 50.3 | 22.2 | 16.3 |
| G-Designer | 86.3 | 80.4 | 37.9 | 15.0 | 49.7 |
| Puppeteer-C（攻击控制器） | 84.3 | **13.7** | — | — | — |
| **RAPS** | **88.2** | **87.6** | **84.3** | **83.0** | **86.3** |

> ⚠️ Puppeteer 在中央控制器被攻击后立即崩溃（84.3% → 13.7%），而 **RAPS 几乎不受影响**，证明其强大的抗攻击能力。

---

### 可扩展性与效率分析（Figure 3）
- **Scalability**：随着代理数量增加，RAPS 持续提升准确率，而 Puppeteer 和 Chain 明显退化。
- **Efficiency**：RAPS 推理延迟稳定增长，而 GPTSwarm/G-Designer 因需在线优化导致耗时剧增（>2小时 @20 agents）。
- **Cost-Performance Trade-off**：RAPS 在相同 token 开销下提供更高精度，形成更优帕累托前沿。

---

## 4. 关键结论和发现

### 主要发现
1. **动态意图 + 去中心化信任 = 自组织协作的基础**
   - RAPS 成功将 **dynamic ad-hoc networking** 的成熟理念迁移到 LLM Agent 协调领域，验证了该视角的有效性。
2. **Reactive Subscription 是适应性的关键**
   - 代理能根据上下文自动专业化角色（如“数学分析师”变为“位移分析专家”），避免预设角色僵化。
3. **Bayesian Reputation 实现有效防御**
   - 本地 watchdog 结合一阶/二阶证据，成功识别并隔离恶意输出，防止错误传播。
4. **RAPS 对低质量初始配置具有强韧性**
   - 即使使用通用角色（naive pool），也能通过动态订阅快速收敛到有效分工（Figure 5）。

---

### 方法的局限性
1. **依赖基础模型能力**
   - RAPS 是“intelligence multiplier”，不改变个体 LLM 的推理上限。若 backbone 不具备某项技能，则无法弥补。
2. **冷启动问题（Cold Start in Reputation）**
   - 初始阶段因缺乏交互历史，信誉系统尚未建立，短暂时间内仍可能受恶意节点影响。
3. **完全无训练限制了长期优化潜力**
   - 当前为 training-free 设计，未利用 MARL 或持续学习进一步优化策略。

---

### 未来工作方向
1. **引入可学习的协调协议**
   - 将 reputation scores 作为内在奖励信号，结合 **multi-agent reinforcement learning (MARL)** 优化协作行为。
2. **扩展网络类比的设计空间**
   - 引入类似：
     - **Congestion Control**：防止 context overflow；
     - **Hierarchical Subnetting**：大规模 agent 社会的层级组织；
     - **Packet Prioritization**：紧急推理路径优先传输。
3. **跨会话信誉迁移**
   - 构建持久化信任记忆，缓解冷启动问题。
4. **支持异构模型混合编排**
   - 协调不同大小、模态、能力的 agents，构建真正开放的 agentic ecosystem。

---

> ✅ **总结一句话**：  
> RAPS 提供了一种全新的、类比于动态自组织网络的 **decentralized, intent-driven, reputation-aware** 多智能体协调范式，在 **Adaptivity、Scalability、Robustness** 三者之间取得了前所未有的平衡，为构建可信、自组织的 LLM Agent Society 奠定了坚实基础。

</details>

---

### 14. [PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition](https://arxiv.org/abs/2602.08240)

**Authors**: Xun Su, Huamin Wang, Qi Zhang  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.08240v1  

#### Abstract
Speech Emotion Recognition (SER) is widely deployed in Human-Computer Interaction, yet the high computational cost of conventional models hinders their implementation on resource-constrained edge devices. Spiking Neural Networks (SNNs) offer an energy-efficient alternative due to their event-driven ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition》总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统基于 **Artificial Neural Networks (ANNs)** 的 **Speech Emotion Recognition (SER)** 模型虽然性能优越，但计算开销大、能耗高，难以部署在资源受限的边缘设备上。而 **Spiking Neural Networks (SNNs)** 虽然具备事件驱动、低功耗的特性，但在直接处理来自自监督学习（SSL）模型（如 emotion2vec）的连续高动态范围特征时，存在严重的**分布不匹配（distribution mismatch）**问题。

具体表现为：
- 高方差输入导致 **LIF/PLIF 神经元**陷入“功能静默”（functional silence）或“饱和”（saturation），破坏稀疏时间编码能力。
- 现有方法无法有效对齐冻结的 SSL 特征与离散脉冲动力学之间的鸿沟。

### 🚀 提出的新方法与创新思路
作者提出 **PTS-SNN**（Prompt-Tuned Spiking Neural Network），一种参数高效的神经形态适配框架，核心创新如下：

#### （1）**Temporal Shift Spiking Encoder**
- 引入**无参数的时间偏移操作**（parameter-free channel shift），在残差脉冲块中捕捉局部时间依赖。
- 通过零填充与截断实现帧间信息交换，无需额外 FLOPs，稳定原始特征流的高频波动。

#### （2）**Context-Aware Membrane Potential Calibration**
- 设计基于 **Spiking Sparse Linear Attention (SSLA)** 的提示机制。
- 引入可学习的 **soft prompts**，聚合全局语义上下文，并将其转化为 **动态偏置电压（dynamic bias voltages）**。
- 这些电压调节 **Parametric Leaky Integrate-and-Fire (PLIF)** 神经元的膜电位基线，将异构输入分布主动校准至阈值响应区间内，避免静默或饱和。

> 🔬 生物启发：该机制模拟生物中的**稳态调节（homeostatic regulation）**，使神经元能适应不同强度的输入刺激。

#### （3）端到端轻量级适配器设计
- 冻结上游 SSL 主干网络（如 emotion2vec），仅训练一个小型适配器模块。
- 实现高效迁移学习，显著降低参数量与推理能耗。

### ⚖️ 相比现有方法的优势
| 维度 | PTS-SNN 优势 |
|------|-------------|
| **参数效率** | 仅需 **1.19M 可训练参数**，远低于全微调 ANN 模型（如 Co-attention: 175.3M） |
| **能量效率** | 单样本推理能耗仅 **0.35 mJ**，比 ANN 模型低 2–3 个数量级 |
| **性能保持** | 在 IEMOCAP 上达到 **73.34% WA**，优于多数 ANN 基线 |
| **兼容性** | 成功桥接连续 SSL 表示与离散 SNN 动力学，解决分布失配难题 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在五个多语言 SER 数据集上进行验证：

| 数据集 | 语言 | 情感类别数 | 样本数 | 交叉验证策略 |
|--------|------|------------|--------|----------------|
| **IEMOCAP** | 英语 | 4 | 5,531 | Leave-one-session-out, 5-fold |
| **CASIA** | 中文 | 4 | 1,200 | Random leave-one-speaker-out, 4-fold |
| **EMODB** | 德语 | 7 | 535 | Random leave-one-out, 10-fold |
| **EMOVO** | 意大利语 | 6 | 588 | 同上 |
| **URDU** | 乌尔都语 | 4 | 400 | 同上 |

> 所有实验均采用预训练的 **emotion2vec** 作为固定特征提取器。

### 🧪 实验设置与评估指标

#### 模型实现
- 框架：PyTorch
- 硬件：单张 NVIDIA RTX 4090 GPU
- 优化器：AdamW（初始 LR = 2e-4，weight decay = 0.05）
- 学习率调度：Cosine Annealing Warm Restarts（最小 LR = 1e-6）
- 训练轮次：200 epochs，batch size = 32
- SNN 反向传播：使用陡峭因子 α=5 的 surrogate gradient

#### 评估指标
- **Weighted Accuracy (WA)**：总体准确率，反映全局性能
- **Unweighted Accuracy (UA)**：各类别平均准确率，适用于类别不平衡场景
- **可训练参数量（Params）**
- **推理能耗（Energy Consumption）**：基于 45nm CMOS 工艺估算，区分 MAC（ANN）与 AC（SNN）操作能耗

### 🆚 对比的基线方法
| 基线模型 | 类型 | 核心机制 |
|---------|------|----------|
| **emotion2vec + ANN fine-tuning** | 全连接微调 | 标准下游任务适配 |
| **Vanilla SNN** | 直接转换 | 将 SSL 输出直接送入 SNN，无适配机制 |
| **Co-attention [58]** | ANN | 跨模态注意力机制 |
| **MSTR [59]** | ANN | 多尺度 Transformer |
| **DST [60]** | ANN | 双流融合架构 |
| **ShiftFormer [61]** | ANN | 基于稀疏偏移的网络 |
| **ENT [62]** | ANN | 高效 Transformer 架构 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（以 IEMOCAP 为例）

| 模型 | WA (%) | UA (%) | Params (M) | Energy (mJ/sample) |
|------|--------|--------|------------|--------------------|
| Co-attention | 69.80 | 71.05 | 175.30 | 103.40 |
| MSTR | 70.60 | 71.60 | 30.03 | 51.06 |
| DST | 71.80 | 73.60 | 22.78 | 35.34 |
| ShiftFormer | 72.10 | 72.70 | 9.50 | 16.20 |
| ENT | 72.43 | 73.88 | 8.55 | 6.35 |
| **PTS-SNN (Ours)** | **73.34** | **73.72** | **1.19** | **0.35** |

✅ **结论**：
- PTS-SNN 在 **WA 和 UA 上均优于所有对比的 ANN 模型**。
- 参数量仅为最轻量 ANN 模型（ShiftFormer）的 **~12.5%**。
- 推理能耗仅为 ENT 的 **约 1/18**，为 Co-attention 的 **1/295**。

### 🌍 跨语言泛化能力（WA %）

| 数据集 | emotion2vec | PTS-SNN | 提升 |
|-------|-------------|---------|------|
| **CASIA** | 69.20 | 72.50 | +3.30 |
| **EMODB** | 84.34 | 87.49 | +3.15 |
| **EMOVO** | 61.21 | 61.94 | +0.73 |
| **URDU** | 81.50 | 84.25 | +2.75 |

➡️ 表明 PTS-SNN 具备良好的跨语言鲁棒性，提示机制捕获的是**语言无关的情感韵律特征**。

### 🔍 消融实验结果（IEMOCAP）

| 配置 | WA (%) | UA (%) |
|------|--------|--------|
| **Full Model (PTS-SNN)** | **73.34** | **73.72** |
| w/o Prompt Tuning | 71.76 | 71.90 |
| w/o Attention Module | 68.35 | 69.10 |
| Only Temporal Shift | 61.27 | 62.05 |

📌 发现：
- 移除 **prompt tuning** 导致性能下降 1.58%，说明其在分布对齐中起关键作用。
- 移除 **attention 模块** 性能骤降 5%，表明全局上下文建模至关重要。
- 仅保留时间偏移机制效果有限，证明需结合高层调控。

### 📈 超参数敏感性分析
- **最佳 prompt 长度 $L_p = 5$**，过长（如 6）引入冗余，性能反降。
- **最优偏置缩放因子 $K = 0.5$**，过高或过低均损害信噪比。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **分布失配是阻碍 SNN 应用于 SSL 特征的关键瓶颈**，直接转换会导致严重性能退化（从 71.79% → 41.78%）。
2. **PTS-SNN 成功实现了冻结 SSL 主干与 SNN 分类头之间的高效适配**，不仅恢复而且超越了原 ANN 性能。
3. **将 prompt 视为“膜电位调节信号”而非“语义 token”是一种新颖且有效的神经形态提示范式**，具有生物学合理性。
4. 所提方法在多个语言和数据集上表现出色，具备良好泛化能力和实际部署潜力。

### ⚠️ 局限性
- 当前框架**仅使用音频模态**，未融合文本或面部表情等互补信息。
- 提示机制仍依赖人工设计结构（如长度、位置），缺乏完全自动化搜索。
- 在极小样本（如 URDU）下提升幅度相对较小，可能面临低资源挑战。

### 🔮 未来工作方向
- 扩展至 **Multimodal Spiking Architectures**，融合语音、视觉与文本信号。
- 探索 **Auto-Prompting for SNNs**，自动优化提示结构与时序配置。
- 研究 **更复杂的生物可塑性机制**（如 STDP、突触延迟调节）以增强表示能力。
- 在真实边缘设备（如 Loihi、Speck 等 neuromorphic chips）上部署并测试实时性能。

---

> 💡 **一句话总结**：  
> PTS-SNN 通过将 prompt 转化为动态膜电位偏置，首次实现了高效、低功耗、高性能的 SSL-to-SNN 语音情感识别适配，在精度、参数量与能耗之间取得了卓越平衡。

</details>

---

### 15. [Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs](https://arxiv.org/abs/2602.08241)

**Authors**: Siqu Ou, Tianrui Wan, Zhiyuan Zhao, Junyu Gao, Xuelong Li  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.08241v1  

#### Abstract
While chain-of-thought (CoT) reasoning has substantially improved multimodal large language models (MLLMs) on complex reasoning tasks, existing approaches largely rely on long textual reasoning trajectories and provide limited mechanisms for learning stable visual attention policies. Our analysis sh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **Multimodal Large Language Models (MLLMs)** 在复杂视觉推理任务中表现不佳，其根本原因并非缺乏语言推理能力，而是**视觉注意力策略不稳定**。具体表现为：
- 在推理早期阶段若出现视觉关注错误（如误定位目标区域），该错误在后续 Chain-of-Thought (CoT) 推理过程中很少被纠正；
- 错误的视觉假设会持续传播，导致系统性推理失败；
- 现有训练机制缺乏对视觉注意力行为的有效“信用分配”（credit assignment），即模型无法学习到“何时、何处应关注图像中的哪个区域”。

### 🚀 提出的新方法：SAYO 与 Entropy-Based Target Attention Reward
作者提出 **SAYO** —— 一种基于强化学习（Reinforcement Learning, RL）框架的视觉推理模型，并引入了一种新的奖励机制：

#### 🔹 **Entropy-Based Target Attention Reward**
- **核心思想**：在训练中引入一个**基于区域级视觉注意力的奖励信号**，直接将优化目标与“视觉接地”的推理步骤对齐。
- **选择高熵 token 进行监督**：仅在模型输出不确定性较高（即 token entropy 高）的关键决策点施加注意力奖励，迫使模型在此时“查看图像”以验证假设（Look-to-Verify）。
- **公式化表达**：
  $$
  r_v = \tanh\left(\log\left(\frac{a + \epsilon}{u + \epsilon}\right)\right)
  $$
  其中 $a$ 是目标区域的平均注意力质量，$u$ 是整个图像区域的注意力质量。

#### 🔹 **无需外部提示或架构修改**
- 不依赖于人工设计的 visual prompt（如 bounding box 标注后作为输入）；
- 不需要额外的模块或特殊 token；
- 在推理时完全自回归生成，保持简洁高效。

### ⭐ 相比现有方法的优势
| 方面 | 现有方法（如 ViP, ReFocus, Reflection-V） | SAYO |
|------|----------------------------------------|-------|
| 视觉引导方式 | 外部工具干预 / Prompt Engineering | 内生式注意力学习 |
| 是否需标注输入 | 是（如画框、程序调用） | 否（训练时用 bbox，推理无依赖） |
| 注意力可学习性 | 弱（依赖已有注意力行为） | 强（通过 RL 显式优化） |
| 泛化能力 | 受限于固定流程 | 跨任务迁移性强 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
分为三大类，覆盖多种视觉推理场景：

| 类别 | 数据集 | 说明 |
|------|--------|------|
| **通用视觉推理** | M3CoT, V*, MMStar, MME-RealWorld Lite | 包含多步推理、真实世界复杂图像理解 |
| **数学视觉推理** | We-Math, MathVision | 图形题、几何图、图表结合数学逻辑 |
| **结构化图像理解** | ChartQA, AI2D, CharXiv | 折线图、柱状图、科学插图等 |

> 特别强调：**We-Math 和 MathVision 并未用于训练**，用于测试跨域泛化能力。

### ⚙️ 实验设置
- **基础模型**：
  - `Qwen3-VL-8B` 和 `InternVL3.5-8B`
- **训练方法**：
  - 使用 **Group Relative Policy Optimization (GRPO)** 框架进行 RL 训练；
  - 引入 **visual attention-based reward** 与 format reward 加权结合；
  - 训练周期：4 epochs，6块 NVIDIA H200 GPU；
- **关键超参数**：
  - KL 散度系数：1e-3
  - 学习率：5e-6
  - Rollout 温度：1.0，Top-p：0.9

### 📊 评估指标
- 主要指标为各 benchmark 上的准确率（Accuracy）
- 引入 **Target Attention Score (TAS)** 和 **Attention Advantage Score $R_a$** 定量衡量视觉聚焦程度：
  $$
  R_a = \frac{1}{2}(1 + \tanh(\log \frac{a}{u}))
  $$
- 分析注意力权重分布（尤其是高熵 token 对目标区域的关注）

### 🆚 基线方法对比
| 类型 | 代表模型 |
|------|---------|
| **闭源模型** | GPT-4o, Gemini 2.5 Pro |
| **开源通用 MLLM** | Qwen3-VL, InternVL3.5, Kimi-VL |
| **开源推理专用 MLLM** | OpenVLThinker-7B, Semantic-back-7B, ViGoRL, NoisyRollout-7B, R1-Onevision-7B |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| 模型 | Avg Score | 数学平均 | 图表平均 | 通用平均 |
|------|----------|----------|----------|----------|
| GPT-4o | — | ~40.7 | ~69.6 | ~72.7 |
| Qwen3-VL-8B | 59.64 | 37.42 | 78.89 | 70.43 |
| **SAYO-Qwen-8B** | **64.03↑↑** | **45.05↑** | **82.65↑** | **74.88↑** |

> ✅ **SAYO-Qwen-8B 在多个指标上超越更大规模模型甚至部分闭源模型**

#### 🔍 亮点突破：
- 在 **MMStar** 上超过 GPT-4o 和 Kimi-VL-16B；
- 在 **We-Math** 和 **MathVision** 上显著提升（尽管未见过这些数据）→ 表明具备强泛化能力；
- 在 **ChartQA/AI2D/CharXiv** 上均取得领先 → 显示对结构化视觉信息的强大解析能力。

### 🔬 消融实验结果（Table 2 & Table 3）

#### ✅ 消融一：不同奖励组合的影响
| 设置 | Avg 提升（vs base） | 说明 |
|------|---------------------|------|
| Only Accuracy Reward | +1.28 | 改进有限，说明仅靠答案正确性不足以优化注意力 |
| Only Attention Reward | +4.32 | 性能大幅提升，证明注意力奖励是关键驱动力 |
| Combined Reward | +4.63 | 最优配置，兼顾语义正确性和视觉聚焦 |

> 💡 结论：**注意力奖励比传统 accuracy reward 更有效激活模型潜力**

#### ✅ 消融二：token 选择策略对比（Table 3）
| 策略 | Avg 提升（Qwen） | 说明 |
|------|------------------|------|
| 所有 token 施加奖励 | +0.87 | 效果差，因低信息量 token 引入噪声 |
| 仅 top-30% 高熵 token | **+4.32** | 显著更优，聚焦关键决策点 |
| Top-20% / Top-40% | 中间值 | 存在最优区间（约 30%） |

> 📌 发现：**不是越多越好，精准打击高不确定性时刻更重要**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MLLMs 的瓶颈不在“会不会想”，而在“看没看见”**
   - 当前 MLLMs 拥有强大的抽象推理能力，但常因初始视觉关注错误而导致失败；
   - 错误一旦发生，Chain-of-Thought 很难自我修正。

2. **视觉注意力可通过 RL 显式训练并稳定增强**
   - 引入基于 region-level attention 的 reward 可显著提高模型对关键区域的关注度；
   - 尤其在高熵 token 阶段加强监督，能有效实现“Look-to-Verify”机制。

3. **改进视觉感知即可释放预训练推理能力**
   - SAYO 在数学任务上的提升并非因为学会了新知识，而是**更准确地“看到”了图形要素**（如边、角、轴标签），从而让原有数学引擎正常工作。

4. **方法具有良好的通用性和可扩展性**
   - 在不同 backbone（Qwen vs InternVL）和不同规模下均有效；
   - 适用于通用、数学、图表等多种视觉推理任务。

### ⚠️ 局限性
- 依赖带有 bounding box 注释的训练数据构建 target region（虽推理时不需）；
- 当前 reward 设计基于最终层注意力，可能忽略中间层动态；
- 对极端模糊或遮挡严重的图像仍存在挑战；
- 高熵 token 的定义和选取范围需进一步理论分析。

### 🔮 未来工作方向
- 探索更细粒度的 attention control（如逐层调节）；
- 结合动态视觉检索机制，在长上下文中持续更新注意力焦点；
- 将 SAYO 思路扩展至视频、3D 场景等时序或多模态输入；
- 构建无需 bbox 标注的自监督注意力奖励机制。

---

## ✅ 总结一句话
> 本文揭示了 MLLMs 在视觉推理中的核心瓶颈是**注意力信用分配缺失**，并通过提出 **SAYO 框架 + 基于高熵 token 的注意力奖励机制**，实现了无需外部提示即可主动、稳定聚焦关键视觉区域的能力，在多项基准上达到 SOTA 表现，且展现出强大的跨任务泛化能力。

</details>

---

### 16. [SCOUT-RAG: Scalable and Cost-Efficient Unifying Traversal for Agentic Graph-RAG over Distributed Domains](https://arxiv.org/abs/2602.08400)

**Authors**: Longkun Li, Yuanben Zou, Jinghan Wu, Yuqing Wen, Jing Li, Hangwei Qian, Ivor Tsang  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08400v1  

#### Abstract
Graph-RAG improves LLM reasoning using structured knowledge, yet conventional designs rely on a centralized knowledge graph. In distributed and access-restricted settings (e.g., hospitals or multinational organizations), retrieval must select relevant domains and appropriate traversal depth without ...

---

### 17. [Improving Variable-Length Generation in Diffusion Language Models via Length Regularization](https://arxiv.org/abs/2602.07546)

**Authors**: Zicong Cheng, Ruixuan Jia, Jia Li, Guo-Wei Yang, Meng-Hao Guo, Shi-Min Hu  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.07546v1  

#### Abstract
Diffusion Large Language Models (DLLMs) are inherently ill-suited for variable-length generation, as their inference is defined on a fixed-length canvas and implicitly assumes a known target length. When the length is unknown, as in realistic completion and infilling, naively comparing confidence ac...

---

### 18. [VocalNet-MDM: Accelerating Streaming Speech LLM via Self-Distilled Masked Diffusion Modeling](https://arxiv.org/abs/2602.08607)

**Authors**: Ziyang Cheng, Yuhao Wang, Heyang Liu, Ronghua Wu, Qunshan Gu, Yanfeng Wang, Yu Wang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08607v1  

#### Abstract
Recent Speech Large Language Models~(LLMs) have achieved impressive capabilities in end-to-end speech interaction. However, the prevailing autoregressive paradigm imposes strict serial constraints, limiting generation efficiency and introducing exposure bias. In this paper, we investigate Masked Dif...

---

### 19. [Systematic Performance Assessment of Deep Material Networks for Multiscale Material Modeling](https://arxiv.org/abs/2602.07192)

**Authors**: Xiaolong He, Haoyan Wei, Wei Hu, Henan Mao, C. T. Wu  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.07192v1  

#### Abstract
Deep Material Networks (DMNs) are structure-preserving, mechanistic machine learning models that embed micromechanical principles into their architectures, enabling strong extrapolation capabilities and significant potential to accelerate multiscale modeling of complex microstructures. A key advanta...

---

### 20. [Cerebellar-Inspired Residual Control for Fault Recovery: From Inference-Time Adaptation to Structural Consolidation](https://arxiv.org/abs/2602.07227)

**Authors**: Nethmi Jayasinghe, Diana Gontero, Spencer T. Brown, Vinod K. Sangwan, Mark C. Hersam, Amit Ranjan Trivedi  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.07227v1  

#### Abstract
Robotic policies deployed in real-world environments often encounter post-training faults, where retraining, exploration, or system identification are impractical. We introduce an inference-time, cerebellar-inspired residual control framework that augments a frozen reinforcement learning policy with...

---

### 21. [MaD-Mix: Multi-Modal Data Mixtures via Latent Space Coupling for Vision-Language Model Training](https://arxiv.org/abs/2602.07790)

**Authors**: Wanyun Xie, Francesco Tonin, Volkan Cevher  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.07790v1  

#### Abstract
Vision-Language Models (VLMs) are typically trained on a diverse set of multi-modal domains, yet current practices rely on costly manual tuning. We propose MaD-Mix, a principled and computationally efficient framework that derives multi-modal data mixtures for VLM training. MaD-Mix formulates data m...

---

### 22. [Horizon Imagination: Efficient On-Policy Training in Diffusion World Models](https://arxiv.org/abs/2602.08032)

**Authors**: Lior Cohen, Ofir Nabati, Kaixin Wang, Navdeep Kumar, Shie Mannor  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08032v1  

#### Abstract
We study diffusion-based world models for reinforcement learning, which offer high generative fidelity but face critical efficiency challenges in control. Current methods either require heavyweight models at inference or rely on highly sequential imagination, both of which impose prohibitive computa...

---

### 23. [Compiler-Assisted Speculative Sampling for Accelerated LLM Inference on Heterogeneous Edge Devices](https://arxiv.org/abs/2602.08060)

**Authors**: Alejandro Ruiz y Mesa, Guilherme Korol, Moritz Riesteter, Jo\~ao Paulo Cardoso de Lima, Jeronimo Castrillon  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08060v1  

#### Abstract
LLM deployment on resource-constrained edge devices faces severe latency constraints, particularly in real-time applications where delayed responses can compromise safety or usability. Among many approaches to mitigate the inefficiencies of sequential token-by-token generation, Speculative Decoding ...

---

### 24. [Enhancing Bandit Algorithms with LLMs for Time-varying User Preferences in Streaming Recommendations](https://arxiv.org/abs/2602.08067)

**Authors**: Chenglei Shen, Yi Zhan, Weijie Yu, Xiao Zhang, Jun Xu  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08067v1  

#### Abstract
In real-world streaming recommender systems, user preferences evolve dynamically over time. Existing bandit-based methods treat time merely as a timestamp, neglecting its explicit relationship with user preferences and leading to suboptimal performance. Moreover, online learning methods often suffer...

---

### 25. [Towards Efficient Large Language Reasoning Models via Extreme-Ratio Chain-of-Thought Compression](https://arxiv.org/abs/2602.08324)

**Authors**: Yuntian Tang, Bohan Jia, Wenxuan Huang, Lianyue Zhang, Jiao Xie, Wenxi Li, Wei Li, Jie Hu, Xinghao Chen, Rongrong Ji, Shaohui Lin  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08324v1  

#### Abstract
Chain-of-Thought (CoT) reasoning successfully enhances the reasoning capabilities of Large Language Models (LLMs), yet it incurs substantial computational overhead for inference. Existing CoT compression methods often suffer from a critical loss of logical fidelity at high compression ratios, result...

---

### 26. [Bridging Academia and Industry: A Comprehensive Benchmark for Attributed Graph Clustering](https://arxiv.org/abs/2602.08519)

**Authors**: Yunhui Liu, Pengyu Qiu, Yu Xing, Yongchao Liu, Peng Du, Chuntao Hong, Jiajun Zheng, Tao Zheng, Tieke He  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08519v1  

#### Abstract
Attributed Graph Clustering (AGC) is a fundamental unsupervised task that integrates structural topology and node attributes to uncover latent patterns in graph-structured data. Despite its significance in industrial applications such as fraud detection and user segmentation, a significant chasm per...

---

### 27. [ERIS: Enhancing Privacy and Communication Efficiency in Serverless Federated Learning](https://arxiv.org/abs/2602.08617)

**Authors**: Dario Fenoglio, Pasquale Polverino, Jacopo Quizi, Martin Gjoreski, Marc Langheinrich  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08617v1  

#### Abstract
Scaling federated learning (FL) to billion-parameter models introduces critical trade-offs between communication efficiency, model accuracy, and privacy guarantees. Existing solutions often tackle these challenges in isolation, sacrificing accuracy or relying on costly cryptographic tools. We propos...

---

### 28. [From Robotics to Sepsis Treatment: Offline RL via Geometric Pessimism](https://arxiv.org/abs/2602.08655)

**Authors**: Sarthak Wanjari  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08655v1  

#### Abstract
Offline Reinforcement Learning (RL) promises the recovery of optimal policies from static datasets, yet it remains susceptible to the overestimation of out-of-distribution (OOD) actions, particularly in fractured and sparse data manifolds.Current solutions necessitates a trade off between computatio...

---

### 29. [How2Everything: Mining the Web for How-To Procedures to Evaluate and Improve LLMs](https://arxiv.org/abs/2602.08808)

**Authors**: Yapei Chang, Kyle Lo, Mohit Iyyer, Luca Soldaini  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08808v1  

#### Abstract
Generating step-by-step "how-to" procedures is a key LLM capability: how-to advice is commonly requested in chatbots, and step-by-step planning is critical for reasoning over complex tasks. Yet, measuring and improving procedural validity at scale on real-world tasks remains challenging and understu...

---

### 30. [Steer2Adapt: Dynamically Composing Steering Vectors Elicits Efficient Adaptation of LLMs](https://arxiv.org/abs/2602.07276)

**Authors**: Pengrui Han, Xueqiang Xu, Keyang Xuan, Peiyang Song, Siru Ouyang, Runchu Tian, Yuqing Jiang, Cheng Qian, Pengcheng Jiang, Jiashuo Sun, Junxia Cui, Ming Zhong, Ge Liu, Jiawei Han, Jiaxuan You  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.07276v1  

#### Abstract
Activation steering has emerged as a promising approach for efficiently adapting large language models (LLMs) to downstream behaviors. However, most existing steering methods rely on a single static direction per task or concept, making them inflexible under task variation and inadequate for complex...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
