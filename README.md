# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-18 06:45:21 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ExpertWeaver: Unlocking the Inherent MoE in Dense LLMs with GLU Activation Patterns](https://arxiv.org/abs/2602.15521)

**Authors**: Ziyu Zhao, Tong Zhu, Zhi Zhang, Tiantian Fan, Jinluan Yang, Kun Kuang, Zhongyu Wei, Fei Wu, Yu Cheng  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.15521v1  

#### Abstract
Mixture-of-Experts (MoE) effectively scales model capacity while preserving computational efficiency through sparse expert activation. However, training high-quality MoEs from scratch is prohibitively expensive. A promising alternative is to convert pretrained dense models into sparse MoEs. Existing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ExpertWeaver: Unlocking the Inherent MoE in Dense LLMs with GLU Activation Patterns**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
当前，**Mixture-of-Experts (MoE)** 架构虽能有效扩展模型容量并保持推理效率，但从零训练高质量 MoE 模型成本极高。一种替代方案是将预训练的 **dense LLM** 转换为 **sparse MoE**，即“**dense-to-MoE conversion**”。然而，现有方法存在以下问题：
- **破坏了 dense 模型内在的激活模式**（如 GLU 的门控机制），导致专家构建次优；
- 忽略了不同层之间的功能差异，采用统一配置；
- 多数方法需要额外的 **router training** 或大量持续预训练（CPT）。

### **提出了什么新方法或新思路**
本文提出 **ExpertWeaver**，一个**无需训练**（training-free）的框架，利用 **GLU 激活模式** 将 dense LLM 转换为结构化的 MoE 模型。其核心思想是：
- **GLU 的门控机制天然揭示了模型内部的功能结构**，可作为 MoE 构建的蓝图。
- 通过分析 GLU 的细粒度神经元激活模式，发现两类神经元：
  - **通用神经元**（universal neurons）：在各类任务中均被激活，适合组成**共享专家**（shared expert）；
  - **专用神经元**（specialized neurons）：仅在特定任务中激活，适合聚类为**路由专家**（routed experts）。
- 引入 **layer-adaptive 配置**：不同层的神经元专业化程度不同，应动态分配共享与路由专家的比例。

### **相比现有方法的优势**
| 维度 | ExpertWeaver | 现有方法（如 CMoE、Llama-MoE） |
|------|-------------|-------------------------------|
| 是否需要训练 | ❌ 无需训练 | ✅ 需要额外训练或 CPT |
| 是否保留原始激活结构 | ✅ 完整保留 GLU 激活模式 | ❌ 打破原有结构 |
| 是否考虑层间差异 | ✅ 层自适应配置 | ❌ 全局统一配置 |
| 路由器构造方式 | ✅ 利用原始门控权重，无训练 | ❌ 需学习或随机初始化 |

---

## 2. **核心实验方法和设置**

### **使用的数据集**
- **校准数据集（Calibration Dataset）**：
  - **Flan-v2**：包含 48 个任务、10 个任务簇，用于捕捉多任务下的神经元激活模式。
  - 每任务采样 5 个样本，共 240 个样本。
- **持续预训练数据集**：
  - **FineWeb-Edu**：用于 MoE 下循环（downcycling）后的 CPT。
- **评估数据集**：
  - **MMLU**, **HellaSwag**, **ARC-e/c**, **PIQA**, **WinoGrande**, **LogiQA**, **SciQ**, **GSM8K**, **HumanEval**, **IFEval**

### **实验设置和评估指标**
#### **两种应用场景**
1. **Training-free 动态结构剪枝（Dynamic Structural Pruning）**
   - 目标：低稀疏度下提升推理效率。
   - 稀疏度：25%
   - 不进行任何训练，直接部署转换后的 MoE。
   - 评估指标：各基准任务准确率 + 平均分。

2. **Model Downcycling（模型降循环）**
   - 目标：将大 dense 模型高效转换为高性能 MoE。
   - 流程：
     - 使用 ExpertWeaver 初始化 MoE；
     - 进行 **200B tokens** 的持续预训练（CPT）；
     - 两阶段监督微调（SFT）：先通用对话，再代码数学。
   - 评估：指令遵循能力（instruction-tuning 后性能）。

#### **评估指标**
- 主要指标：各任务准确率（Accuracy）
- 综合指标：平均得分（Avg.）
- 推理效率：RPS（每秒请求）、OTPS（每秒输出 token 数）、TTFT（首 token 时间）、TPOT（每输出 token 时间）

### **基线方法对比**
#### **结构剪枝基线**
- **LLM-Pruner**：基于梯度和参数重要性的静态剪枝。
- **FLAP**：基于输出特征稳定性的剪枝。
- **CMoE**：基于平衡聚类的 dense-to-MoE 方法。

#### **MoE 下循环基线**
- **OLMoE-1B-7B**：从头训练的 MoE。
- **LLaMA-MoE-v1/v2**：基于 LLaMA 的 MoE 变体。
- **OpenMoE**：开源 MoE 系列。

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**

#### **1. 动态结构剪枝（25% sparsity）**
| 方法 | MMLU | HellaSwag | ARC-e | ARC-c | PIQA | **Avg** |
|------|------|-----------|-------|--------|-------|--------|
| Dense (Qwen2.5-7B) | 74.2 | 80.3 | 77.8 | 63.8 | 80.0 | 75.2 |
| LLM-Pruner | 55.9 | 72.2 | 71.0 | 49.1 | 77.0 | 65.0 |
| FLAP | 54.7 | 58.5 | 67.3 | 42.2 | 70.8 | 58.7 |
| **ExpertWeaver** | **61.6** | **72.3** | **71.5** | **53.5** | **76.3** | **67.0** |

> 💡 **相对提升**：比最强基线 LLM-Pruner 提升 **3.1%**，比 CMoE（在 LLaMA 上）提升 **5.6%**。

#### **2. Downcycling 性能对比（CPT 200B tokens）**
| 模型 | MMLU | HellaSwag | ARC-e | ARC-c | PIQA | **Avg** |
|------|------|-----------|-------|--------|-------|--------|
| Qwen2.5-7B (dense) | 74.1 | 80.2 | 77.5 | 63.7 | 79.7 | 73.2 |
| **ExpertWeaver (Qwen2.5-7B)** | **45.6** | **73.7** | **72.4** | **56.3** | **78.0** | **65.3** |
| OLMoE-1B-7B* (500B tokens) | 53.8 | 79.6 | 76.3 | 55.6 | 80.1 | 67.2 |
| **ExpertWeaver (OLMo-7B)** | **45.0** | **61.2** | **69.3** | **38.8** | **74.5** | **62.1** |

> 💡 **关键发现**：
> - ExpertWeaver (Qwen2.5-7B) 在仅激活 **3.5B 参数**（总 7B）的情况下，达到 **65.3** 平均分，为原模型的 **87.6%**。
> - ExpertWeaver (OLMo-7B) 仅用 200B tokens CPT，性能已达 OLMo-7B 原始性能的 **97.35%**，且接近从头训练 500B tokens 的 OLMoE。

#### **3. 指令微调后性能（SFT）**
| 方法 | MMLU | ARC-c | GSM8K | HumanEval | IFEval | **Avg** |
|------|------|--------|--------|------------|--------|--------|
| LLaMA-MoE-v2 | 40.9 | 40.2 | 55.0 | 51.2 | 36.0 | 44.66 |
| OLMoE-1B-7B | 53.79 | 55.63 | 40.94 | 40.48 | 35.49 | 45.27 |
| **ExpertWeaver-Instruct** | **50.60** | **69.80** | **57.10** | **50.20** | **33.10** | **52.16** |

> ✅ **SOTA 表现**：平均分 **52.16**，显著优于所有 MoE 基线。

#### **4. 推理效率（vLLM 测试）**
| GPU | 方法 | RPS ↑ | OTPS ↑ | TTFT ↓ (ms) | TPOT ↓ (ms) |
|-----|------|--------|---------|--------------|--------------|
| H100 | Qwen2.5-7B | 41.41 | 5299.96 | 1404.5 | 18.5 |
| H100 | **ExpertWeaver** | **44.04** | **5637.42** | **542.2** | **9.8** |

> ⚡ **推理优势**：吞吐更高，延迟更低，尤其在首 token 时间上大幅优化。

---

### **消融实验结果（Ablation Studies）**

| 实验维度 | 最佳配置 | 发现 |
|--------|--------|------|
| **共享专家比例**（α_min, α_max） | (0.2, 0.7) | 层自适应配置显著优于全局固定比例 |
| **专业化阈值 T**（CV threshold） | 0.6 | 模型对 T 鲁棒，0.5~0.7 区间表现稳定 |
| **专家粒度**（expert granularity） | 64 | 64~128 专家时性能最佳，过少或过多均下降 |
| **校准数据集** | Flan-v2 > C4 | 多任务数据集显著优于单域语料（67.0 vs 65.8） |
| **数据量与多样性** | 50% 任务 → 98% 性能 | 数据效率高，无需全量任务即可接近最优 |

---

## 4. **关键结论和发现**

### **主要发现**
1. **GLU 激活模式蕴含 MoE 结构**：
   - GLU 的门控信号自然诱导**结构化稀疏性**，是 dense-to-MoE 转换的理想信号源。
   - 存在**通用神经元**与**专用神经元**，分别对应共享专家与路由专家。

2. **层间异质性需被建模**：
   - 浅层与深层神经元专业化程度不同（浅层更通用，深层更专用）。
   - **U-shaped 专家分布**（图7）：边界层共享专家多，中间层路由专家多。

3. **无需训练即可实现高性能 MoE**：
   - ExpertWeaver 是首个完全 **training-free** 且性能超越需训练方法的 dense-to-MoE 框架。
   - 即使不进行 CPT，也能作为高效推理模型使用。

4. **Downcycling 优于 Upcycling**：
   - 相比复制参数的 upcycling，downcycling 更能保留原始模型的知识密度与多样性。
   - 图4显示：downcycling 收敛更快，最终性能更高。

---

### **方法的局限性**
- **依赖 GLU 架构**：目前仅适用于 SwiGLU 等 GLU 变体，不适用于 ReLU-based FFN。
- **专家粒度固定**：需预设专家数量和大小，灵活性受限。
- **未解决路由冲突**：高并发下可能因负载不均衡影响性能（尽管使用了 load-balancing loss）。

---

### **未来工作方向**
- 扩展至非 GLU 架构（如 ReLU、GeLU）的 dense 模型。
- 探索动态专家数量与自适应路由机制。
- 结合硬件优化，进一步提升 MoE 的实际部署效率。
- 将 ExpertWeaver 应用于更大规模模型（如 70B+）的 MoE 转换。

---

> 📌 **总结一句话**：  
> **ExpertWeaver 揭示了 GLU 激活模式中的“内在 MoE”结构，首次实现了无需训练、保留原始功能、层自适应的 dense-to-MoE 转换，在结构剪枝与模型降循环两个场景均取得 SOTA 性能。**

</details>

---

### 2. [Panini: Continual Learning in Token Space via Structured Memory](https://arxiv.org/abs/2602.15156)

**Authors**: Shreyas Rajesh, Pavan Holur, Mehmet Yigit Turali, Chenda Duan, Vwani Roychowdhury  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.15156v1  

#### Abstract
Language models are increasingly used to reason over content they were not trained on, such as new documents, evolving knowledge, and user-specific data. A common approach is retrieval-augmented generation (RAG), which stores verbatim documents externally (as chunks) and retrieves only a relevant su...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PANINI: Continual Learning in Token Space via Structured Memory》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前语言模型在处理未训练过的新知识（如新文档、动态信息、用户特定数据）时，主要依赖 **Retrieval-Augmented Generation (RAG)**。然而，传统 RAG 存在以下关键问题：

- **推理效率低下**：每次推理都需重复检索和处理原始文本片段（chunks），导致大量冗余计算。
- **上下文噪声大**：chunk-based retrieval 容易引入无关上下文，增加幻觉（hallucination）和错误推理的风险。
- **缺乏结构化记忆**：现有方法多存储原始文本或简单摘要，难以支持复杂的多跳推理（multi-hop reasoning）。

此外，参数化持续学习（PCL）虽能内化新知识，但面临灾难性遗忘（catastrophic forgetting）、高昂训练成本及与指令微调对齐冲突等问题。

### 提出了什么新方法或新思路

本文提出 **PANINI**，一种非参数化持续学习（Non-Parametric Continual Learning, NPCL）框架，其核心思想是：

> **在写入阶段（write time）投资计算，构建结构化的语义记忆，在读取阶段（read time）实现高效、可靠的推理。**

具体创新包括：

1. **Generative Semantic Workspace (GSW)**  
   将每个文档转化为一个实体-事件感知的 **question-answer (QA) 对网络**，形成“生成性语义工作区”。该结构显式编码实体、事件节点及其关系，足以让 LLM 通过推理链重建情境并挖掘潜在知识。

2. **Reasoning Inference Chain Retrieval (RICR)**  
   在推理时，不直接访问原始文档或 chunks，而是通过 beam search 风格的链式检索，在 GSW 中追踪最可能的推理路径（inference chains）。该过程仅依赖轻量级检索，无需多次 LLM 调用。

3. **双索引机制（Dual Indexing）**  
   构建两个全局索引：
   - **稀疏 BM25 索引**：基于实体及其角色/状态
   - **稠密向量索引**：基于所有 QA 对
   支持高效、精准的候选检索。

### 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **效率** | 推理时仅使用 QA 对作为证据，token 使用量仅为基线的 **2–30× 更少**。 |
| **准确性** | 在多跳 QA 上平均 F1 比最强基线高 **5–7%**。 |
| **可靠性** | 在无答案查询上显著减少幻觉，拒绝准确率更高。 |
| **可扩展性** | 支持完全开源流水线，且 GSW 可作为通用检索基础设施被其他系统复用。 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

- **单跳 QA 数据集**：
  - **NQ** (Natural Questions)
  - **PopQA**
- **多跳 QA 数据集**：
  - **MuSiQue**
  - **2WikiMultihopQA**
  - **HotpotQA**
  - **LV-Eval** (hotpotwikiqa-mixup 256k)

共 **6 个 QA 基准**，覆盖从简单事实检索到复杂组合推理的任务。

### 实验设置和评估指标

#### 性能指标
- **Exact Match (EM)** 和 **F1**：衡量答案匹配程度。
- **Token Count (↓)**：推理时提供给回答模型的平均 token 数，反映效率。

#### 可靠性评估（Platinum Benchmark）
为测试模型在**无答案查询**上的表现，作者构建了：
- **MuSiQue-PLATINUM**
- **2Wiki-PLATINUM**

其中每个样本被人工标注为“可回答”或“不可回答”。

评估指标：
- **Ans**：在可回答子集上的 F1
- **Unans**：在不可回答子集上的拒绝准确率（输出 `N/A` 视为正确）

### 基线方法对比

| 类型 | 基线方法 |
|------|----------|
| **Chunk-based Retrieval** | BM25, BM25+reranker, Dense Retrieval (NV-Embed, Qwen3-Embedding) |
| **Structure-Augmented RAG** | RAPTOR, GraphRAG, LightRAG, HippoRAG, HippoRAG2 |
| **Agentic Systems** | IR-CoT, Search-R1 |

所有方法统一使用 **GPT-4o-mini** 作为回答模型以保证公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表 2：六项 QA 基准上的平均 F1 对比

| 方法 | 平均 F1 |
|------|--------|
| Dense Retrieval 最强 | 50.5 |
| HippoRAG2 (SOTA 结构化 RAG) | 53.3 |
| **PANINI (本文方法)** | **56.06** ✅ |

- 在所有任务上均取得最佳性能，尤其在多跳任务上优势明显。
- 在 **MuSiQue** 上达到 **52.27 F1**，远超第二名 HippoRAG2 的 49.3。

#### 表 3：推理时 token 使用量对比

| 方法 | 平均 token 数 |
|------|----------------|
| Chunk Retrieval (e.g., BM25) | ~705 |
| Structure-Augmented (e.g., GraphRAG) | ~8121 |
| Agentic (e.g., IR-CoT) | ~10745 |
| **PANINI** | **319.79** ✅ |

- **比 chunk retrieval 少 2.2× token**
- **比结构化/代理方法少 5–30× token**

#### 表 4：Platinum 基准上的可靠性评估

| 方法 | Avg Ans | Avg Unans |
|------|--------|----------|
| HippoRAG2 | 72.5 | 58.5 |
| **PANINI** | **79.9** | **72.8** ✅ |

- 同时提升**可回答准确率**和**不可回答拒绝率**，打破“越强检索器越难拒绝”的权衡。

### 消融实验结果

#### 表 12–14：关键组件消融研究

| 消融设置 | 影响 |
|---------|------|
| 移除 QA 重排序（No QA reranking） | F1 下降超过 30%，说明重排序至关重要 |
| 移除双索引（Only entity 或 Only QA） | 性能下降，验证双路检索互补性 |
| 不进行问题分解（No decomposition） | 多跳性能大幅下降，证明链式推理必要性 |
| 链评分函数替换为 last-hop greedy | F1 降至 50.8（原 52.3），几何平均更优 |
| Beam Width 从 5 减至 3 | 性能几乎不变，token 数降低 25% |

结论：PANINI 的设计选择均有实证支持，且具备良好鲁棒性。

---

## 4. 关键结论和发现

### 主要发现

1. **写入时结构化优于读取时推理**  
   PANINI 的成功表明：**将计算前移至写入阶段，构建高质量结构化记忆（GSW），能在读取阶段换来巨大的效率与可靠性收益**。

2. **GSW 是一种可复用的元表示（meta-representation）**  
   实验显示，将 GSW 注入 Search-R1 等代理系统后，其性能也得到提升（Table 16），说明 GSW 可作为通用检索基础设施。

3. **轻量级链式检索优于迭代式代理推理**  
   RICR 仅需一次问题分解 + 非参数检索，即可媲美甚至超越需要多次 LLM 调用的 agentic 方法。

4. **结构化记忆提升抗干扰能力**  
   在不断增加干扰文档的持续学习场景中（Figure 9），PANINI 性能下降最小，表现出更强的鲁棒性。

### 方法的局限性

1. **未实现跨文档链接缓存**  
   当某些跨文档关系频繁出现时，无法自动合并或缓存，仍需每次动态遍历。

2. **GSW 构建成本较高**  
   使用 GPT-4.1-mini 构建 MuSiQue 的 GSW 成本约为 $48（Table 15），虽为一次性投入，但仍高于纯 embedding 方法。

3. **小模型构建 GSW 质量不稳定**  
   开源模型（如 Qwen3-8B）提取的 GSW 存在遗漏动词短语、缺失反向 QA 对等问题（Table 18），影响下游性能。

4. **未探索更复杂的 reconciliation 策略**  
   当前实体对齐仅限于单文档内，未来可用 agent 主动协调跨文档结构。

### 未来工作方向

1. **实现 latent-link caching**：对高频出现的跨文档关系进行记忆优化。
2. **降低 write-time 成本**：改进 GSW 提取算法，提升小模型鲁棒性。
3. **引入 agent 进行 memory reconciliation**：利用智能体主动维护和优化全局 GSW 结构。
4. **拓展至叙事密集或多模态领域**：如长视频流、小说等，探索时空结构与跨模态事件的记忆建模。
5. **探索 GSW 作为通用检索层的应用潜力**：集成到更多下游框架中。

---

> **总结一句话**：  
> PANINI 通过在写入阶段构建 **Generative Semantic Workspace (GSW)** 并在读取阶段执行 **Reasoning Inference Chain Retrieval (RICR)**，实现了高效、准确、可靠的非参数化持续学习，在多项 QA 基准上超越现有方法，并为结构化记忆系统的设计提供了新范式。

</details>

---

### 3. [Improving LLM Reliability through Hybrid Abstention and Adaptive Detection](https://arxiv.org/abs/2602.15391)

**Authors**: Ankit Sharma, Nachiket Tapas, Jyotiprakash Patra  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.15391v1  

#### Abstract
Large Language Models (LLMs) deployed in production environments face a fundamental safety-utility trade-off either a strict filtering mechanisms prevent harmful outputs but often block benign queries or a relaxed controls risk unsafe content generation. Conventional guardrails based on static rules...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Improving LLM Reliability through Hybrid Abstention and Adaptive Detection*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
大型语言模型（LLMs）在实际部署中面临**安全-效用权衡**（safety-utility trade-off）的根本挑战：
- **严格的安全策略**（如静态规则、关键词过滤）虽然能有效拦截有害输出，但常导致**高误报率**（false positives），拒绝大量无害查询，影响用户体验。
- **宽松的控制机制**则可能放行不安全内容，带来合规与伦理风险。
- 现有方法多为**上下文无关**（context-insensitive）、**计算开销大**，且难以适应不同领域（如医疗 vs 创意写作）的风险差异。

### 提出了什么新方法或新思路
本文提出了一种**自适应弃权系统**（Adaptive Abstention System），其核心创新包括：

- **动态阈值调节**（Dynamic Threshold Calibration）  
  弃权决策基于实时上下文信号（如**domain** 和 **user history**）动态调整各检测器的阈值，实现“高风险场景更保守，低风险场景更宽容”。

- **多维度并行检测架构**（Multi-dimensional Parallel Detection）  
  集成五个独立的检测器，分别评估：
  - Safety（安全性）
  - Confidence（置信度）
  - Knowledge Boundary（知识边界）
  - Contextual Appropriateness（语境适切性）
  - Repetition（重复性）  
  多信号融合避免单一指标的脆弱性。

- **分层级联优化架构**（Hierarchical Cascade Architecture）  
  将检测器按计算成本排序，形成四级级联：
  1. **Level 1**: 快速词法规则（<1ms）
  2. **Level 2**: 轻量分类器（~5ms）
  3. **Level 3**: 全五维检测（20–50ms）
  4. **Level 4**: 高代价验证（可选）  
  绝大多数请求在前两级被快速处理，显著降低平均延迟。

- **模型无关设计**（Model-agnostic）  
  作为推理时附加层，无需对基础LLM进行微调，适用于多种模型架构。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **安全性** | 在严格模式下实现 **Recall=1.00**，确保所有不安全请求均被拦截 |
| **实用性** | 自适应阈值使 **False Positive Rate 下降80%以上**，大幅减少过度拒绝 |
| **效率** | 平均延迟仅 **42.78ms**，相比外部Guardrails（450ms）提速 **10.5×** |
| **灵活性** | 支持跨域部署，在医疗、金融、创意等不同场景自动调节严格程度 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Efficiency Dataset**（1,000条）：混合领域请求，涵盖安全、不安全及对抗性提示，模拟真实流量。
- **Safety Dataset**（600条）：源自 *RealToxicityPrompts*，用于评估毒性内容拦截能力。
- **Adaptive Dataset**：标注了领域的提示集合，覆盖 **Medical, Education, Casual, Creative Writing** 四类场景，用于测试上下文感知能力。

### 实验设置和评估指标
- **基础模型**：固定不变，以隔离弃权机制的影响。
- **评估配置对比**：
  1. 无弃权层（Baseline）
  2. 静态阈值版本（Static Thresholds, T=0.6）
  3. 不使用级联的完整检测（No Cascade）
  4. 提出的自适应+级联系统（Ours）
- **评估指标**：
  - **Precision**（精确率）：正确拒绝的比例
  - **Recall**（召回率）：成功拦截不安全请求的比例
  - **F1 Score**
  - **False Positive Rate (FPR)**：对良性请求的误拒率
  - **Average Latency**：每请求平均处理时间（ms）

### 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| **Guardrails AI** | 外部ToxicLanguage验证器，代表主流静态防护方案 |
| **Static Thresholds** | 所有检测器使用统一固定阈值（T=0.6） |
| **No Cascade** | 所有请求均通过全部五维检测，用于验证级联效率增益 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 数值 |
|------|------|
| **平均延迟**（Cascade版） | **42.78 ms** |
| **最大延迟降幅** | 相比Guardrails AI **降低10.5×**（450 → 42.78 ms） |
| **Strict Safety Mode Recall** | **1.00**（完美召回） |
| **Calibrated Mode Precision** | **>0.95**（兼顾安全与可用性） |
| **False Positives**（Adaptive vs Static） | 从15降至3（↓80%） |

### 与基线方法的对比结果
#### 表：延迟比较（Table 2）
| 方法 | 延迟（ms） | 加速比 |
|------|-----------|--------|
| Guardrails AI | 450.00 | 1.0× |
| No Cascade (Ours) | 118.26 | 3.8× |
| **Cascade (Ours)** | **42.78** | **10.5×** |

> ✅ 级联机制使系统适合实时交互应用（如对话代理、代码助手）。

#### 表：静态 vs 自适应阈值（Table 4）
| 指标 | 静态 | 自适应 | 提升 |
|------|------|--------|------|
| Precision | 0.75 | **0.95** | +26.7% |
| Recall | 0.80 | **0.98** | +22.5% |
| F1 Score | 0.77 | **0.96** | +24.7% |
| False Positives | 15 | **3** | ↓80% |

> ✅ 自适应阈值实现了**帕累托改进**：安全性和实用性同时提升。

#### 图5：按领域误报率对比
- **Creative Writing**：FPR 从25% → **3%**
- **Medical Contexts**：FPR 从15% → **2%**

> ✅ 显著缓解了专业领域中的“过度审查”问题。

### 消融实验结果
- **级联结构消融**：关闭级联后延迟上升至118.26ms，证明其对性能的关键作用。
- **重复检测器消融**：
  - 在长对话中，该模块**成功阻止100%的无限循环**。
  - 传统Guardrails直到上下文窗口耗尽才干预，用户体验差。
- **多维检测必要性**：单一信号（如仅依赖Confidence）易被“自信错误”（confidently incorrect）绕过。

---

## 4. 关键结论和发现

### 主要发现
1. **弃权应是动态、上下文感知的过程**，而非静态规则。  
   > “How much to block” 比 “What to block” 更重要。

2. **多维度检测 + 动态阈值 + 分层级联** 构成了高效可靠的LLM安全框架三要素。

3. **安全与效用并非零和博弈**：通过上下文自适应，可在高风险场景保证安全，在低风险场景释放创造力。

4. **延迟是生产部署的关键瓶颈**，而级联设计使得强安全保障也能满足实时性要求（<50ms）。

### 方法的局限性
- **对抗鲁棒性有限**：复杂的Jailbreak攻击若使用表面良性的语言仍可能规避检测。
- **内存开销**：重复检测器需维护响应历史嵌入（embedding cache），增加内存占用。
- **新领域冷启动问题**：新部署领域需要初始校准期来设定合适的敏感度参数。

### 未来工作方向
- **联邦学习**（Federated Learning）：实现跨用户/部署的隐私保护型阈值自适应。
- **扩展至多模态**（Multimodal Settings）：将框架应用于图像、视频等内容审核。
- **增强可解释性**（Explainability）：为每次弃权生成简洁的人类可读理由，提升透明度与信任。
- **在线自适应机制**：让系统能根据反馈持续优化阈值，实现闭环安全控制。

---

> 🔚 **总结一句话**：  
> 本论文提出的 **Adaptive Abstention System** 通过**上下文感知的动态控制**与**高效级联架构**，首次在**安全性、实用性与延迟**之间实现了协同优化，为LLM在医疗、金融等高风险场景的可靠部署提供了可行路径。

</details>

---

### 4. [Distributed Semi-Speculative Parallel Anisotropic Mesh Adaptation](https://arxiv.org/abs/2602.15204)

**Authors**: Kevin Garner, Polykarpos Thomadakis, Nikos Chrisochoides  
**Category**: cs.DC  
**Published**: 2026-02-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.15204v1  

#### Abstract
This paper presents a distributed memory method for anisotropic mesh adaptation that is designed to avoid the use of collective communication and global synchronization techniques. In the presented method, meshing functionality is separated from performance aspects by utilizing a separate entity for...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Distributed Semi-Speculative Parallel Anisotropic Mesh Adaptation》总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决**大规模、时间依赖的计算流体动力学（CFD）模拟中，各向异性网格自适应生成的可扩展性和性能瓶颈问题**。传统并行网格生成方法常依赖集体通信（collective communication）和全局同步机制，这些操作在超大规模并行系统上会成为性能瓶颈，限制了其在exascale级HPC架构上的应用。

此外，许多现有的“黑盒”并行化方法无法有效集成已有的高性能共享内存网格生成器（如CDT3D），因为它们未针对分布式环境进行设计，导致重复工作、负载不均和通信开销过大。

### 提出的新方法与思路
提出了一种**分布式的半推测式（semi-speculative）并行各向异性网格自适应方法（DM_CDT3D）**，其核心思想是：

- **功能与性能分离**：将网格生成功能（由共享内存程序CDT3D实现）与并行运行时支持（由PREMA提供）解耦，提升代码复用性和可维护性。
- **先验接口适应（a priori interface adaptation）**：在单个多核节点上首先对所有子域边界（interface elements）完成自适应，并将其“冻结”（frozen），然后分发子域到集群各节点并行处理内部元素。
- **利用推测执行模型（speculative execution model）**：通过预锁定（preemptive locking）和伪激活（pseudo-active/inactive）标记机制，精确控制CDT3D仅作用于目标元素集合，避免不必要的重复计算。

### 相比现有方法的优势
- **避免集体通信与全局同步**：完全规避了MPI_Allreduce等阻塞式通信，显著降低通信开销。
- **更高的端到端性能**：在生成约10亿个单元的模型时，使用512核可在**不到4小时内完成**，而refine需近6小时，共享内存版CDT3D则需超过8小时。
- **良好的网格质量**：生成的网格在单元形状度量（mean ratio）和边长分布方面与原始CDT3D相当，满足弱可重现性（weak reproducibility）要求。
- **更好的强/弱扩展性**：尤其在中小规模核心配置下表现优于state-of-the-art工具refine。

---

## 2. 核心实验方法和设置

### 数据集
使用两个典型几何体进行测试：
- **Delta Wing Geometry**：基于亚音速层流马赫场构造多尺度metric，用于验证CFD相关场景。
- **Cube Geometry**：采用解析定义的“polar-2”metric（来自UGAWG基准），代表曲剪切层，复杂度高达1亿，对应约10亿个四面体单元。

### 实验设置
- **硬件平台**：
  - **Wahab集群**：最多使用512核（16节点×32核/节点）
  - **Anvil集群**：最多使用1536核（16节点×96核/节点）
- **软件栈**：GNU GCC 11.4.1 + Intel MPI，PREMA作为运行时系统。
- **分解策略**：采用PQR排序法进行数据分解，确保子域间负载均衡。
- **参数调优**：经过大量测试确定最优参数组合（见附录Table 13），包括粗网格复杂度、激活层数等。

### 评估指标
| 类别 | 指标 |
|------|------|
| **性能** | 运行时间（end-to-end runtime）、加速比（speedup）、每秒生成单元数（elements/sec） |
| **质量** | 单元平均比率直方图（mean ratio histogram）、边长度分布（edge length distribution）、metric conformity |
| **可扩展性** | 强缩放行为（fixed problem size, increasing cores） |
| **可重现性** | 弱可重现性（weak reproducibility）——不同核心数下输出网格质量一致 |

### 基线方法对比
- **SM_CDT3D**：原生共享内存版本，在单节点上运行。
- **refine [20]**：当前最先进的开源并行各向异性网格生成器，采用后验接口适应（a posteriori）和重分区策略。
- **A Posteriori DM_CDT3D [16]**：作者早期提出的替代方案，用于消融分析。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### Delta Wing @ 10M 复杂度（~1亿单元）
| 方法 | 256核时间(min) | 512核时间(min) | 加速比(1→256) |
|------|----------------|----------------|---------------|
| SM_CDT3D | — | — | ~22.6x |
| A Priori DM_CDT3D | 23 | 22 | ~88.4x |
| refine | 31 | 21 | ~60.9x |

> ✅ **DM_CDT3D在256核以内全面优于refine；512核时refine略优**

#### Cube @ 100M 复杂度（~10亿单元）
| 方法 | 512核时间(hours) | 单元速率(elements/sec) |
|------|------------------|------------------------|
| SM_CDT3D | ~8.12 | ~32,748 |
| A Priori DM_CDT3D | **3.46** | **~78,722** |
| refine | 5.98 | ~50,134 |

> ✅ **DM_CDT3D比refine快约72%，比SM_CDT3D快2.3倍**

### 与基线方法的对比结果
- **性能优势明显**：
  - 在Anvil上使用384核处理delta wing时，DM_CDT3D速率达**232,523 elem/sec**，远高于refine的124,555。
  - 在大问题规模下（如10亿单元），DM_CDT3D展现出更优的绝对性能。
- **通信开销更低**：
  - DM_CDT3D非网格操作（通信、打包等）仅占总时间的15–25%，而refine因频繁集体通信导致开销更高。
- **网格质量相当**：
  - 所有方法均能保持良好的metric conformity。
  - refine生成的网格略优（更集中于mean ratio=1附近），但代价是生成更多单元（多出10–15%）。

### 消融实验结果（Ablation Study）
在Table 2和Table 3中对比了两种实现方式：

| 配置 | 描述 | 结果 |
|------|------|------|
| **Naive（黑盒）** | 不尊重操作顺序，不使用pseudo-active标记，直接调用CDT3D全功能 | 接口适应耗时321s，内部适应耗时~1940s |
| **Modified（改进）** | 使用pseudo-active + 预锁定 + 控制操作顺序 | 接口适应降至**75.4s**（↓4.25×），内部适应降至**~1200s**（↓1.6×） |

> 🔍 **关键发现**：若不控制操作顺序（如在接口阶段执行post-refinement edge collapse），会导致后续阶段重复处理已优化区域，造成严重性能浪费。

---

## 4. 关键结论和发现

### 主要发现
1. **功能与性能解耦是构建可扩展HPC应用的有效范式**：通过将CDT3D作为库嵌入PREMA框架，实现了跨共享/分布式内存的高效协同。
2. **a priori接口适应优于a posteriori策略**：提前固定边界可彻底消除接口数据依赖带来的通信与再分区开销。
3. **推测执行模型可被扩展至分布式层级**：通过pseudo-active标记和点预锁定机制，成功引导CDT3D在分布式环境下只处理指定元素。
4. **避免集体通信至关重要**：正如DoE ECP研究指出，集体通信是exascale应用的主要瓶颈，本方法的成功印证了这一点。

### 方法的局限性
- **接口适应阶段受限于单节点算力**：目前该阶段只能在一个多核节点上运行（如32或96核），成为整体可扩展性的主要瓶颈。
- **子域连通性修复为串行过程**：`MAKE_SIMPLY_CONNECTED`算法尚未并行化，影响大规模下的效率。
- **缺乏自动参数调优机制**：当前参数（如激活层数、分解方式）需手动调整，成本高昂。
- **未启用PREMA的动态负载均衡能力**：由于未采用过分解（overdecomposition），无法利用PREMA的迁移与平衡特性。

### 未来工作方向
1. **并行化`MAKE_SIMPLY_CONNECTED`模块**，消除串行瓶颈。
2. **开发机器学习模型以自动推荐最优参数组合**，减少人工调参开销。
3. **实现完全推测式接口适应**：允许接口与内部元素同时自适应，进一步提升并发度。
4. **探索其他任务后端（tasking backend）**：测试除Pthread外的OpenMP或其他调度策略对性能的影响。
5. **集成至真实CFD流水线**：验证其在实际仿真中的闭环性能与稳定性。

--- 

> 📌 **总体评价**：  
> 本文提出的方法在**工程实用性、性能表现和理论创新**之间取得了良好平衡。它不仅展示了如何有效地将一个高性能共享内存组件迁移到分布式环境，还为下一代exascale级自适应网格生成提供了新的设计范式。尽管存在一些可扩展性限制，但其核心思想具有广泛的借鉴意义。

</details>

---

### 5. [Accelerated Predictive Coding Networks via Direct Kolen-Pollack Feedback Alignment](https://arxiv.org/abs/2602.15571)

**Authors**: Davide Casnici, Martin Lefebvre, Justin Dauwels, Charlotte Frenkel  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.15571v1  

#### Abstract
Predictive coding (PC) is a biologically inspired algorithm for training neural networks that relies only on local updates, allowing parallel learning across layers. However, practical implementations face two key limitations: error signals must still propagate from the output to early layers throug...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerated Predictive Coding Networks via Direct Kolen-Pollack Feedback Alignment

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本论文针对 **Predictive Coding (PC)** 在实际应用中的两个关键限制：
- **反馈延迟（Feedback Delay）**：误差信号必须通过多步推理阶段从输出层逐层传播到浅层，导致训练效率低下。
- **指数衰减（Exponential Decay）**：在误差反向传播过程中，信号强度随深度呈指数级衰减，导致浅层更新微弱甚至消失。

这两个问题严重制约了 PC 的可扩展性和计算效率，尤其是在深层网络中。

### 提出了什么新方法或新思路
作者提出了 **Direct Kolen-Pollack Predictive Coding (DKP-PC)**，一种结合了 **Direct Feedback Alignment (DFA)** 和 **Kolen-Pollack (KP)** 思想的新型 PC 变体。其核心创新在于：
- 引入**可学习的直接反馈连接**（learnable direct feedback connections），从输出层直接连接到所有隐藏层。
- 利用 DKP 机制，在推理阶段前对前馈权重进行一次初步更新，从而在每一层立即生成非零误差项。
- 这种设计打破了传统 PC 的逐层依赖，实现了误差信号的**即时传输**。

### 相比现有方法的优势
- ✅ **理论时间复杂度从 O(L) 降至 O(1)**：其中 L 为网络深度，消除了深度相关的延迟。
- ✅ **完全并行化学习**：各层的神经活动和权重更新可以独立进行，首次实现 PC 的全并行化。
- ✅ **保留局部性（Locality）**：所有更新规则仅依赖本地信息，符合生物可塑性和硬件友好性要求。
- ✅ **加速推理过程**：实验证明，仅需**单步推理**即可达到甚至超越标准 PC 多步推理的性能。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖了从小型到中等规模的多个主流视觉数据集：
- **MLP 实验**：
  - MNIST
  - Fashion-MNIST (FMNIST)
- **CNN 实验**：
  - CIFAR-10
  - CIFAR-100
  - Tiny ImageNet（最复杂的测试场景）

### 实验设置和评估指标
- **模型架构**：
  - MLP：三层全连接网络（含两隐层，每层128单元）
  - CNN：VGG-7 和 VGG-9 架构，基于 Pinchetti et al. (2024) 的设定
- **训练配置**：
  - 批量大小（batch size）：128
  - 优化器：Adam / AdamW（前向权重），独立优化反馈连接
  - 学习率调度：warmup-cosine-annealing（前向），指数衰减（反馈）
- **评估指标**：
  - **Top-1 准确率**（主指标）
  - **每轮训练时间（秒）**（衡量效率）
  - **梯度对齐度（Cosine Similarity）**（分析与 BP 的一致性）
  - **能量演化曲线**（Free Energy 动态）

### 基线方法对比
与以下算法进行了全面比较：
- **Backpropagation (BP)**：黄金标准
- **Standard Predictive Coding (PC)**
- **Incremental PC (iPC)**
- **Center-Nudging PC (CN-PC)**
- **Direct Kolen-Pollack (DKP)**

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 模型 | 数据集 | DKP-PC (Top-1) | 最佳基线 | 提升幅度 |
|------|--------|----------------|-----------|----------|
| VGG-7 | CIFAR-100 | **50.42%** | CN-PC (64.76%) | —— |
| VGG-9 | CIFAR-100 | **53.80%** | PC (44.76%) | ↑9.04% |
| VGG-9 | Tiny ImageNet | **35.04%** | CN-PC (31.50%) | ↑3.54% |

> 💡 在 Tiny ImageNet 上，DKP-PC 首次超越所有其他局部学习算法，成为表现最好的 PC 变体。

### 与基线方法的对比结果
- 📈 **分类性能**：
  - 在多数 CNN 设置下，DKP-PC 显著优于 PC、DKP 和 iPC。
  - 尤其在深层网络（如 VGG-9）上优势明显，缩小了与 BP 的差距。
  - 在 Tiny ImageNet 上达到 **35.04% Top-1 准确率**，优于 CN-PC。
- ⏱️ **训练速度**（来自 Table 2）：
  - 相比标准 PC，DKP-PC **平均减少超过 60% 的训练时间**。
  - 相比 iPC，训练时间减少高达 **81%**。
  - 例如在 VGG-9 + CIFAR-100 上：
    - PC: 34.18 秒/epoch
    - iPC: 69.73 秒/epoch
    - **DKP-PC: 12.53 秒/epoch**

### 消融实验结果（见 Appendix A.4.2 & A.4.3）
- **禁用 PC 前向权重更新** → 梯度对齐崩溃，证明 PC 阶段对正则化和对齐至关重要。
- **禁用反馈矩阵更新** → 对齐性能下降，说明 DKP 更新也受益于 PC 提供的信息。
- **多步推理 vs 单步推理**：
  - 即使只用 **1 步推理**，DKP-PC 已能超越标准 PC 和 iPC。
  - 增加推理步数可进一步提升精度，体现“性能-效率”权衡。

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **DKP-PC 成功解决了 PC 的反馈延迟与指数衰减问题**，通过引入直接可学习反馈路径，实现了误差信号的即时广播。
2. ✅ **首次实现 PC 的全并行化训练**，理论时间复杂度由 O(L) 降为 O(1)，为硬件加速提供了坚实基础。
3. ✅ **DKP 与 PC 具有协同效应**：
   - DKP 加速了 PC，
   - 而 PC 反过来作为正则化器提升了 DKP 的梯度对齐质量。
4. ✅ **在准确率和效率之间取得更好平衡**，尤其在深层网络中显著优于现有 PC 变体。

### 方法的局限性
- 🔒 **仍依赖额外反馈矩阵**：引入了额外内存开销（feedback weight storage）。
- 🧠 **未完全摆脱“双阶段”结构**：虽然各阶段可并行，但仍存在顺序依赖（如先 DKP 更新再 PC 推理）。
- 💻 **当前实现受限于软件同步开销**：PyTorch 中的并行化未能充分发挥潜力，因线程管理开销抵消了收益。

### 未来工作方向
1. **定制化硬件支持**：开发专用 CUDA kernels 或神经形态芯片，以真正释放 O(1) 时间复杂度的优势。
2. **稀疏化与量化反馈权重**：借鉴 prior work (Crafton et al., 2019; Han & Yoo, 2019)，降低内存占用。
3. **更紧密的动态耦合**：探索不依赖“预更新”的方式，直接利用反馈信号扰动神经活动。
4. **集成更先进的 PC 变体**：如将 DKP 与 **Equilibrium Propagation** 或 **Nudging PC** 结合，进一步逼近 BP 性能。

> 🎯 **总体评价**：DKP-PC 是 Predictive Coding 领域的一项重要进展，不仅提升了训练效率，也为构建高效、可扩展、生物合理的 AI 系统开辟了新路径。

</details>

---

### 6. [The Stationarity Bias: Stratified Stress-Testing for Time-Series Imputation in Regulated Dynamical Systems](https://arxiv.org/abs/2602.15637)

**Authors**: Amirreza Dolatpour Fathkouhi, Alireza Namazi, Heman Shakeri  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.15637v1  

#### Abstract
Time-series imputation benchmarks employ uniform random masking and shape-agnostic metrics (MSE, RMSE), implicitly weighting evaluation by regime prevalence. In systems with a dominant attractor -- homeostatic physiology, nominal industrial operation, stable network traffic -- this creates a systema...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《The Stationarity Bias: Stratified Stress-Testing for Time-Series Imputation in Regulated Dynamical Systems》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

该论文揭示并形式化了一个在时间序列插补（time-series imputation）基准测试中普遍存在的系统性偏差——**Stationarity Bias（平稳性偏差）**。

- **问题本质**：传统基准通常采用**均匀随机掩码（uniform random masking）** 和点对点误差指标（如 RMSE、MSE），这导致评估过程过度采样主导的**平稳状态（stationary regime）**，而忽略了稀有但关键的**瞬态事件（transient events）**（如餐后血糖峰值、低血糖事件）。
- **后果**：简单方法（如线性插值 Lerp）在平稳区间表现优异，从而在整体指标上“虚高”，掩盖了其在关键瞬态中形态失真严重的缺陷，误导研究者低估深度学习模型的价值。

### 提出了什么新方法或新思路

作者提出了一套**分层压力测试框架（Stratified Stress-Test）**，核心创新包括：

1. **Regime-Stratified Evaluation（基于状态的分层评估）**  
   将评估明确划分为三个生理学上有意义的动态状态：
   - **Homeostatic State（稳态）**：无外部输入、血糖稳定的区间。
   - **Post-Prandial Transient（餐后瞬态）**：餐后血糖上升高峰。
   - **Hypoglycemic Transient（低血糖瞬态）**：低血糖事件及控制器干预窗口。

2. **引入形态学评估指标 DTW（Dynamic Time Warping）**  
   用以衡量信号形状的保真度，弥补 RMSE 等点对点指标无法捕捉“**RMSE Mirage（RMSE 海市蜃楼）**”的问题——即低 RMSE 可能伴随严重形态失真。

3. **生态有效的缺失模式建模（Ecologically Valid Missingness）**  
   基于真实临床试验（DCLP3/DCLP5）推导出的缺失分布（包括小时级发生概率、持续时间混合模型等）生成掩码，而非使用合成的 MCAR 掩码，提升训练与评估的真实性。

4. **提出“Regime-Conditional Model Selection”原则**  
   不同状态应使用不同插补策略：**线性插值用于平稳区，深度学习模型用于瞬态区**，实现安全与效率的平衡。

### 相比现有方法的优势

| 方面 | 传统方法 | 本文方法 |
|------|----------|----------|
| **评估公平性** | 偏向平稳区，忽略关键瞬态 | 分层评估，暴露模型在关键场景的真实表现 |
| **指标有效性** | 依赖 RMSE/MSE，易被“角切割”欺骗 | 引入 DTW 衡量形态保真度，更符合下游任务需求 |
| **缺失模式真实性** | 合成随机掩码（MCAR） | 基于真实数据推导的统计缺失过程 |
| **模型选择指导** | “一刀切”比较 | 提供条件选择策略，支持自适应推理 |

---

## 2. 核心实验方法和设置

### 使用的数据集

1. **PEDAP**  
   - 包含 102 名儿童的 CGM 数据，来自为期 13 周的临床试验。
   - 分为 **Raw PEDAP**（保留缺失，仅零填充辅助变量）和 **Processed PEDAP**（CGM 值线性插值）两个版本。

2. **UVA/Padova Simulation**  
   - 使用 FDA 批准的糖尿病模拟器生成的 100 名成人数据。
   - 控制饮食、运动等变量，提供高质量 ground truth。

3. **TCR-Simulation Dataset**  
   - 特别构建用于模拟 **Temporal Control Reset（TCR）** 场景下的低血糖事件。
   - 通过人为增大餐食估计误差诱导低血糖，并模拟 TCR 干预（基础率降低 95%）。

> 所有数据按患者级别划分训练/验证/测试集（70%/10%/20%），确保泛化性。

### 实验设置和评估指标

#### 三大压力测试协议（Stress Evaluation Protocols）

| 协议 | 目标 | 掩码方式 |
|------|------|----------|
| **Protocol A: Homeostatic State** | 评估平稳区插补能力 | 在满足稳定条件的 30 分钟窗口内掩码（10%-30%比例） |
| **Protocol B: Post-Prandial Peak** | 评估餐后高峰重建能力 | 围绕餐后峰值中心生成 3.5–4 小时掩码窗口（测试 1–3 个峰值） |
| **Protocol C: Hypoglycemic Transient (TCR)** | 评估低血糖事件重建能力 | 在 TCR 激活前后 1 小时窗口掩码 |

#### 评估指标

- **Pointwise Accuracy**: RMSE, Emp_SE, Bias
- **Clinical Accuracy**: MARD（连续血糖监测金标准）
- **Morphological Fidelity**: DTW（动态时间规整距离）
- **Distributional Calibration**: 条件密度估计 $p(\hat{y}|y \in R)$ 与 ground truth 对比

### 基线方法对比

共评估 12 种方法，涵盖四大类：

| 类别 | 代表模型 |
|------|--------|
| **Generative & Transformer** | SAITS, GPT4TS, CSDI |
| **Frequency-Domain** | FreTS, TSLANet |
| **Multi-Scale** | TimeMixer, SCINet |
| **Evidence & Tokenization** | TEFN, TOTEM |
| **Classical Baselines** | Lerp（线性插值）, LOCF（前向填充） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（摘录自 Tables 1–3）

#### ✅ **Scenario A: 稳态插补（Homeostatic State）**

| 模型 | RMSE (Processed PEDAP, 0.3) | DTW | MARD |
|------|----------------------------|-----|------|
| **Lerp** | **2.75** | **4.48** | **1.97%** |
| SAITS | 8.63 | 11.13 | 4.89% |
| SCINet | 37.31 | 36.80 | 13.76% |

> **结论**：在线性可预测的平稳区，**Lerp 显著优于所有深度学习模型**，后者因过参数化产生“幻觉”波动。

#### ✅ **Scenario B: 餐后瞬态（Post-Prandial Excursion）**

| 模型 | RMSE (Raw PEDAP, 2 peaks) | DTW | MARD |
|------|---------------------------|-----|------|
| Lerp | 59.21 | 261.82 | 27.62% |
| **SAITS** | **49.05** | **165.60** | **19.04%** |
| SCINet | 57.12 | 258.61 | 28.44% |

> **结论**：
> - RMSE 差异小（~10），但 **DTW 差异巨大（>90）**，揭示“**RMSE Mirage**”现象。
> - **SAITS 在形态保真度（DTW）和临床安全性（MARD）上显著领先**。

#### ✅ **Scenario C: 低血糖瞬态（Hypoglycemia during TCR）**

| 模型 | RMSE | DTW | MARD | Bias |
|------|------|-----|------|------|
| **SAITS** | **23.26** | **66.38** | **27.00%** | +14.08 |
| Lerp | 33.76 | 92.52 | 38.29% | **+25.55** |
| SCINet | 32.44 | 101.07 | 39.52% | +27.18 |

> **结论**：
> - **SAITS 全面优于 Lerp**，尤其在 Bias 上显著更低。
> - Lerp 存在**严重正偏置**，会系统性高估低血糖值，可能掩盖危险事件。

### 消融实验结果（Ablation Study）

- **图7（Figure 7）** 综合比较四种模型跨场景表现：
  - **Lerp 在 Scenario A 完胜**（DTW: 20.29 vs SAITS 52.26；MARD: 4.39% vs 10.21%）。
  - **SAITS 在 Scenarios B/C 显著领先**，尤其在多峰值、长缺失下优势扩大。
- **图8–11** 验证了结果对掩码长度、缺失比例的鲁棒性。

---

## 4. 关键结论和发现

### 主要发现

1. **存在“Stationarity Bias”**  
   均匀随机掩码导致评估偏向平稳区，使简单方法看似优越，**系统性低估深度学习模型在关键瞬态中的价值**。

2. **存在“RMSE Mirage”**  
   RMSE 无法惩罚“角切割”行为（如线性插值穿过峰值），**低点对点误差可能伴随严重形态失真**，需引入 DTW 等形态指标。

3. **Regime-Conditional Model Selection 是最优策略**  
   - **平稳区**：线性插值是“有效真实”，计算高效且准确。
   - **瞬态区**：深度学习模型（如 SAITS）在形态保真度、临床安全性和偏差控制上全面占优。

4. **分布校准仍是挑战**  
   即使是 SAITS，也无法完全恢复瞬态的分布特性：
   - 餐后峰值被压缩（均值从 177.1 → 141.3 mg/dL）。
   - 低血糖仍被高估（均值从 83.2 → 97.3 mg/dL）。
   > 表明当前模型未能充分将外生输入（如胰岛素、碳水）作为确定性驱动信号。

### 方法的局限性

- **未包含经典 ML 模型**：如 Random Forest、k-NN 等未参与对比。
- **未在真实闭环系统中验证**：下游任务（如 Zone-MPC 控制器）的安全增益尚未实测。
- **检测机制简化**：当前使用梯度阈值 $|\nabla g_t| < 0.6$ 判断状态，实际部署需考虑延迟与噪声。
- **扩散模型潜力未探索**：CSDI 虽被提及，但未在主实验中与其他模型公平比较。

### 未来工作方向

1. **开发自适应推理框架**：实现轻量级状态检测 + 动态路由到 Lerp 或 DL 模型。
2. **改进模型架构**：增强对外生变量（carbs, insulin）的显式建模能力，缓解“Driver-Blindness”问题。
3. **扩展至其他受控系统**：如工业传感器、电网负载、服务器监控等具有主导平稳态的系统。
4. **推动分层评估成为标准**：呼吁社区在 benchmark 中强制要求 regime-stratified protocol。

---

> **代码与数据开源地址**：[https://github.com/Shakeri-Lab/Glucose-Imputation](https://github.com/Shakeri-Lab/Glucose-Imputation)

</details>

---

### 7. [AgriWorld:A World Tools Protocol Framework for Verifiable Agricultural Reasoning with Code-Executing LLM Agents](https://arxiv.org/abs/2602.15325)

**Authors**: Zhixing Zhang, Jesen Zhang, Hao Liu, Qinhan Lv, Jing Yang, Kaitong Cai, Keze Wang  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.15325v1  

#### Abstract
Foundation models for agriculture are increasingly trained on massive spatiotemporal data (e.g., multi-spectral remote sensing, soil grids, and field-level management logs) and achieve strong performance on forecasting and monitoring. However, these models lack language-based reasoning and interacti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AgriWorld: A World-Tools-Protocol Framework for Verifiable Agricultural Reasoning with Code-Executing LLM Agents

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前在农业科学领域存在两大模型范式的割裂：
- **基础模型**（如基于遥感、土壤网格等大规模时空数据训练的模型）虽然在预测和监测任务上表现优异，但缺乏自然语言交互能力和可解释的推理过程。
- **大语言模型**（LLMs）擅长文本理解和生成，但在处理高维、异构的农业数据（如多光谱遥感时间序列、田块矢量边界、气象流等）时无法直接进行数值计算和空间对齐，容易产生“看似合理但不可验证”的幻觉答案。

这一差距导致现有系统难以支持真实的农学工作流，例如需要结合地理查询、时间序列分析、作物模拟和反事实干预的复杂决策。

### 提出了什么新方法或新思路
作者提出了一种名为 **World-Tools-Protocol** 的框架，并具体实现了以下三个核心组件：

#### ✅ **AGRIWORLD**: 可执行的农业环境
- 构建了一个统一的 Python 执行环境 `AGRIWORLD`，提供标准化 API 接口，涵盖四大类功能：
  1. **Geospatial Querying**（地理空间查询）
  2. **Remote-Sensing Analytics**（遥感时间序列分析）
  3. **Crop Growth Simulation**（作物生长模拟，支持反事实干预）
  4. **Task-Specific Predictors**（如产量、胁迫、病害风险预测）
- 引入 **Artifact** 概念：每次工具调用返回可检查的结果对象（含数据、元信息、物理约束、哈希溯源），确保分析过程可审计、可复现。
- 设计 **Canonical Alignment Operator**：自动处理坐标系（CRS）、时间频率、单位一致性等问题，解决农业数据中常见的时空错位问题。

#### ✅ **AGRO-REFLECTIVE**: 执行驱动的反思型智能体
- 提出一个多轮 LLM Agent，采用 **Execute-Observe-Refine** 循环机制：
  - 写代码 → 在 `AGRIWORLD` 中执行 → 观察中间输出与错误 → 自我修正
- 利用中间产物中的元数据（如单位不匹配、覆盖率不足）作为反馈信号，主动诊断并修复错误（如 SpatialMisalignment, UnitError）。
- 不是简单地“调用工具”，而是将执行反馈视为第一类推理信号。

#### ✅ **Verifiable Evaluation Suite with Executable Checkers**
- 基于现有基准构建 **AGROBENCH**，包含多样化农业 QA 任务（查找、预测、异常检测、反事实分析）。
- 每个任务定义为 `(q, B, S_out, V, B)` 元组，其中 `V` 是一个可执行的验证函数（executable checker），能程序化判断答案是否满足：
  - 输出 schema 合规性
  - 数值容差（NRMSE）
  - 因果一致性（counterfactual direction）
  - 物理合理性（单位一致、像素覆盖度）

### 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| **推理方式** | 纯文本生成 / 单次工具调用 | 多轮执行-观察-反思循环 |
| **结果可信度** | 难以验证中间步骤 | 中间 Artifact 可审计、可溯源 |
| **错误处理能力** | 容易因小错误（如 CRS 错误）导致失败 | 能通过反馈自我纠正 |
| **评估方式** | 依赖人工评分或最终答案匹配 | 支持自动化、细粒度的可执行验证 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **AGROBENCH**：作者构建的可验证评估套件，源自真实农学工作流，覆盖多个子领域：
  - **Plant**（植物栽培）
  - **Animal**（畜牧业）
  - **Aquatic**（水产养殖）
  - **Herb**（草药种植）
- 数据来源包括：
  - 多光谱遥感影像（Sentinel-2）
  - 土壤质地图（SoilGrids）
  - DEM 地形数据
  - 气象站时间序列
  - 田块管理日志（灌溉、施肥记录）

### 实验设置和评估指标
根据不同任务类型设计了多种评估指标：

| 任务类型 | 评估指标 |
|--------|--------|
| **Lookup**（查找） | Exact Match Accuracy |
| **Forecasting**（预测） | Normalized RMSE (NRMSE) ↓ |
| **Anomaly Detection**（异常检测） | Spatial IoU ↑（与 Sentinel-2 真实掩膜比较） |
| **Counterfactual Analysis**（反事实分析） | Success Rate ↑（是否正确识别出有效干预策略） |
| **通用要求** | Schema Validity, Unit Consistency, Physical Sanity Checks |

最大交互步数（budget）设为 `T_max = 20`，平均收敛步数仅需 3.5 步。

### 基线方法对比
控制相同 LLM（Qwen3 系列）进行公平比较：

| 方法 | 描述 |
|-----|------|
| **Text-Only** | 仅使用 LLM 参数知识作答（无外部工具） |
| **AgriWorld-Direct (One-shot)** | 标准 Code Interpreter 模式，单次生成并执行代码，无错误恢复机制 |
| **AGRO-REFLECTIVE (Ours)** | 多轮 Execute-Observe-Refine 框架，具备自省与纠错能力 |

此外还对比了多个强基线模型：
- 开源模型：Qwen3, DeepSeek-V3, Yi-1.5-34B
- 商业模型：GPT-4o, Gemini-2.0

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3 & 4）

| 模型 | QA 总分 / 总题数 | Choice 准确率 (%) |
|------|------------------|--------------------|
| **Qwen3-32B-LoRA-Reflective (Ours)** | **7.72 / 541** | **73.84%** |
| GPT-4o | 7.07 / 150 | 36.75% |
| Qwen3-32B | 5.67 / 541 | 39.67% |

> 📌 **结论**：即使参数规模远小于 GPT-4o，本方法仍显著领先，证明 **工具接地（tool grounding）比模型规模更重要**。

### 与基线方法的对比结果（Table 4）

| 方法 | Lookup Acc. | Forecasting NRMSE ↓ | Anomaly IoU ↑ | Counterfactual Success ↑ |
|------|-------------|---------------------|---------------|--------------------------|
| Text-Only | 41.2% | 0.89 | 0.12 | 14.5% |
| One-shot | 82.5% | 0.34 | 0.51 | 43.8% |
| **AGRO-REFLECTIVE** | **86.7%** | **0.18** | **0.68** | **71.4%** |

> 🔍 **关键发现**：
> - 在简单查找任务上提升有限（+4.2%），说明此类任务已有较好解决；
> - 在复杂多步任务（尤其是 Forecasting 和 Counterfactual）上优势巨大：
>   - **NRMSE 下降 47%**
>   - **反事实成功率翻倍以上**

### 消融实验结果（Table 2）

| 方法变体 | 平均轮次 | 准确率 (%) |
|--------|----------|------------|
| w/o Remote Sensing | 2.4 | 41.5 |
| w/o Alignment | 3.1 | 51.3 |
| w/o Reflection | 1.0 | 50.6 |
| **Full AGRO-REFLECTIVE** | **3.5** | **57.6** |

> 🔧 **各模块贡献分析**：
> - **移除 Remote Sensing 工具**：性能暴跌至 41.5%，表明像素级遥感数据访问至关重要。
> - **移除 Canonical Alignment**：尽管尝试更多轮次，准确率反而下降，说明 **CRS 和时间对齐是系统“粘合剂”**。
> - **移除 Reflection Loop**：退化为 one-shot 模式，准确率下降 7%，且无法从初始错误中恢复。

---

## 4. 关键结论和发现

### 主要发现
1. **Executable Reasoning 是农业 AI 的关键路径**  
   单纯扩大 LLM 规模无法解决农业推理中的精确性问题；必须通过可执行环境实现 **计算接地（computation grounding）** 和 **过程可验证性**。

2. **Reflection Loop 显著提升鲁棒性**  
   多达 35% 的初始代码存在 bug（语法错误、字段名错误等），但通过执行反馈可在 92% 的情况下成功修复。

3. **Tool Grounding 战胜 Giant Models**  
   尽管 GPT-4o 参数量巨大，但由于不了解 `AGRIWORLD` 的 API 规范，常虚构不存在的方法（如 `get_soil_moisture()`），导致失败。

4. **OOD 泛化能力强**（Table 5）
   - 在未见过的地区（巴西 vs 加州）和极端气候年份（El Niño）下，本文方法性能下降最小（仅 -12% ~ -6%），而文本模型下降超 45%。
   - 表明其泛化能力来自动态数据获取而非记忆模式。

### 方法的局限性
- **依赖高质量工具接口**：若底层 API 不稳定或文档不清，会影响 Agent 表现。
- **对罕见边缘情况处理有限**：例如极端天气下的非线性响应可能超出模拟器范围。
- **计算成本较高**：虽平均只需 3.5 步，但在资源受限场景下仍需优化。

### 未来工作方向
- 扩展至更多农业子领域（如林业、温室控制）
- 引入多智能体协作机制（multi-agent coordination）
- 结合人类专家反馈形成混合增强智能（human-in-the-loop）
- 探索轻量化部署方案，适配边缘设备（edge farming systems）

---

> ✅ **一句话总结**：  
> 该论文提出了首个面向农业科学的 **可验证、可执行、可反思** 的 LLM Agent 框架，通过 `AGRIWORLD + AGRO-REFLECTIVE + Executable Checkers` 三位一体设计，在复杂农业推理任务上大幅超越现有方法，展示了 **“让模型动手做实验”** 而非“凭空猜测”的科学 AI 新范式。

</details>

---

### 8. [BindCLIP: A Unified Contrastive-Generative Representation Learning Framework for Virtual Screening](https://arxiv.org/abs/2602.15236)

**Authors**: Anjie Qiao, Zhen Wang, Yaliang Li, Jiahua Rao, Yuedong Yang  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.15236v1  

#### Abstract
Virtual screening aims to efficiently identify active ligands from massive chemical libraries for a given target pocket. Recent CLIP-style models such as DrugCLIP enable scalable virtual screening by embedding pockets and ligands into a shared space. However, our analyses indicate that such represen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：BindCLIP: A Unified Contrastive-Generative Representation Learning Framework for Virtual Screening**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前基于 **CLIP-style** 模型（如 **DrugCLIP**）的虚拟筛选（Virtual Screening）方法存在两个关键缺陷：

- **Interaction Insensitivity（交互不敏感）**：模型对配体-口袋之间精细的结合相互作用（如氢键、盐桥等）不敏感，难以捕捉细微的结合模式差异。
- **Shortcut Reliance（依赖捷径相关性）**：模型倾向于利用训练数据中的粗粒度物理化学性质相似性（如分子量、极性表面积等）作为“捷径”进行打分，而非真正学习结合兼容性，导致在分布外（OOD）场景下泛化能力差。

### **提出了什么新方法或新思路**
作者提出 **BindCLIP**，一个统一的 **对比-生成联合表示学习框架**，通过以下创新机制解决上述问题：

- **Binding Pose Generation-guided Representation Learning**  
  引入 **pocket-conditioned diffusion model** 作为辅助训练目标，用于生成配体在蛋白口袋中的结合构象（binding pose）。该生成任务提供细粒度的几何监督信号，使编码器学习到更丰富的空间交互特征。

- **Hard-Negative Augmentation with Ligand-Ligand Anchoring Regularizer**  
  - 采用 **hard-negative mining** 策略，从化学相似但非活性的分子中挖掘难负样本，增强模型区分能力。
  - 提出 **ligand-ligand anchoring regularizer**，防止硬负样本在嵌入空间中因持续排斥而坍缩（collapse），保持合理的几何结构。

- **Unified Training Objective**  
  联合优化 **CLIP-style contrastive loss** 和 **diffusion-based generative loss**，实现对比学习与生成学习的协同。

### **相比现有方法的优势**
- **更细粒度的交互建模**：通过扩散模型注入结合构象的几何信息，提升对局部相互作用的敏感性。
- **更强的泛化能力**：减少对粗粒度物理化学属性的依赖，在 OOD 和类似物排序任务中表现更优。
- **推理效率不变**：扩散模型仅用于训练，推理阶段仍为快速的 **embedding similarity + ANN search**，无额外开销。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **训练集**：`PDBBind 2019` + `HomoAug` 扩增，共 66,164 个蛋白-配体复合物。
- **验证集**：`CASF-2016`，用于超参调优。
- **测试基准**：
  - `DUD-E`：102 个靶点，含 property-matched decoys。
  - `LIT-PCBA`：15 个靶点，基于实验确认的活性/非活性标签，偏倚更小。
  - **OOD Benchmark**：从 `MF-PCBA` 构建，排除与训练集序列同源性 >30% 的蛋白，严格测试分布外泛化。
  - `FEP+ Benchmark`：4 个靶点（CDK2, TYK2, JNK1, P38），用于评估对类似物的自由能排序能力。

### **实验设置和评估指标**
- **训练设置**：
  - 编码器基于 **UniMol** Transformer 架构。
  - 使用 Adam 优化器，batch size 48，训练 100 轮。
  - 硬负样本从 ZINC 库中通过 embedding retrieval + Vina docking 过滤获得。
- **评估指标**：
  - **AUROC**, **BEDROC**（强调早期富集）
  - **Enrichment Factor (EF)**：EF0.5%, EF1%, EF5%
  - **Hits@K**：在 OOD 设置下报告 Hits@500 和 Hits@1000
  - **Pairwise Accuracy & Kendall Tau**：用于 FEP+ 排序任务

### **基线方法对比**
- **Docking-based**：AutoDock Vina, Glide-SP, Surflex
- **Learning-based**：
  - 回归/分类模型：DeepDTA, RFscore, Pafnucy, OnionNet
  - 表示学习模型：Planet, Gnina, DrugHash
  - CLIP-style 检索模型：**DrugCLIP**（主要对比基线）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **在 LIT-PCBA 上的表现（Table 1）**
| Model       | AUROC   | BEDROC  | EF0.5%  | EF1%    | EF5%    |
|-------------|---------|---------|---------|---------|---------|
| DrugCLIP    | 55.45   | 6.41    | 8.24    | 5.21    | 2.14    |
| **BindCLIP**| **59.15**| **7.88**| **9.84**| **6.26**| **2.90**|

- **相对提升**：AUROC ↑6.7%，平均早期富集指标 ↑24.5%

#### **在 DUD-E 上的表现（Table 2）**
| Model       | AUROC   | BEDROC  | EF0.5%  | EF1%    | EF5%    |
|-------------|---------|---------|---------|---------|---------|
| DrugCLIP    | 79.29   | 47.53   | 37.90   | 30.52   | 10.08   |
| **BindCLIP**| **80.14**| **49.73**| **39.82**| **32.16**| **10.44**|

- 尽管 DUD-E 存在化学偏倚，BindCLIP 仍取得小幅但一致的提升。

#### **在 OOD Benchmark 上的表现（Figure 4）**
- **BEDROC** ↑62%
- **EF0.5%** ↑115%
- **Hits@500** ↑114%（15 vs 7）
- **Hits@1000** ↑62%（21 vs 13）
- 显著优于 DrugCLIP，表明更强的分布外泛化能力。

#### **在 FEP+ 类似物排序任务上的表现（Figure 5）**
| Metric           | DrugCLIP | BindCLIP |
|------------------|----------|----------|
| Pairwise Accuracy| 56.4%    | **65.7%**|
| Kendall Tau      | 0.14     | **0.31** |

- 相对准确率提升 **16%**，Kendall Tau 翻倍以上，说明能更好地区分细微结合差异。

### **消融实验结果（Table 3）**
在 LIT-PCBA 上的消融研究显示：
- **+ Diffusion Objective**：AUROC 从 55.45 → 56.71
- **+ Random Negatives**：进一步提升至 58.91
- **+ Hard Negatives**：最终达 **59.15**，效果最显著
- 表明 **diffusion supervision** 和 **hard-negative augmentation** 均有贡献，且后者更具判别力。

---

## **4. 关键结论和发现**

### **主要发现**
1. **CLIP-style 对比学习不足以捕捉细粒度结合信号**，易受数据集中粗粒度相关性的干扰。
2. **引入生成式监督（diffusion）可有效注入几何感知的交互信息**，提升嵌入空间的生物学意义。
3. **硬负样本增强 + 锚定正则化** 可显著提升模型判别力，同时避免表示坍缩。
4. **BindCLIP 在挑战性任务中优势明显**：尤其在 OOD 泛化和类似物排序上大幅超越现有方法。

### **方法的局限性**
- **训练成本增加**：由于扩散模型和硬负采样，训练时间和显存消耗约为 DrugCLIP 的 2 倍。
- **依赖高质量 3D 结构**：需要蛋白-配体复合物的精确结合构象用于扩散训练，限制了在无结构数据上的应用。
- **硬负样本可能引入噪声**：尽管使用 Vina 过滤，仍可能存在假阴性问题。

### **未来工作方向**
- 探索更高效的扩散架构或蒸馏策略以降低训练成本。
- 将框架扩展到 **de novo 分子生成** 或 **lead optimization** 任务。
- 结合 **protein sequence** 信息，减少对 3D 结构的依赖。
- 探索在 **multi-target screening** 和 **polypharmacology** 中的应用。

---

> **总结一句话**：  
> **BindCLIP 通过将 contrastive learning 与 generative modeling（diffusion）相结合，并辅以 hard-negative learning 和 anchoring 正则化，在不牺牲推理效率的前提下，实现了更鲁棒、更细粒度的虚拟筛选表示学习，显著提升了在现实场景下的泛化能力和排序准确性。**

</details>

---

### 9. [CAMEL: An ECG Language Model for Forecasting Cardiac Events](https://arxiv.org/abs/2602.15677)

**Authors**: Neelay Velingker, Alaia Solko-Breslin, Mayank Keoliya, Seewon Choi, Jiayi Xin, Anika Marathe, Alireza Oraii, Rajat Deo, Sameed Khatana, Rajeev Alur, Mayur Naik, Eric Wong  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.15677v1  

#### Abstract
Electrocardiograms (ECG) are electrical recordings of the heart that are critical for diagnosing cardiovascular conditions. ECG language models (ELMs) have recently emerged as a promising framework for ECG classification accompanied by report generation. However, current models cannot forecast futur...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CAMEL: An ECG Language Model for Forecasting Cardiac Events

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **ECG Language Models (ELMs)** 主要专注于 ECG 分类和报告生成，**无法预测未来的临床事件**，限制了其在早期干预中的应用价值。  
本文旨在填补这一空白，提出首个能够从 ECG 信号中**预测未来心律失常事件**（如房颤 AFIB）的通用 ECG 语言模型。

### 🚀 提出的新方法与核心创新
作者提出了 **CAMEL (Cardiac Autoregressive Model for ECG Language Modeling)**，具备以下关键创新：

- **支持长时序上下文输入**：  
  首次实现对长达数千秒（最高达 600 秒）的 ECG 信号进行建模，远超现有模型仅支持 10 秒片段的限制。
  
- **基于 token 的 ECG 编码机制**：  
  将每条导联的每一秒 ECG 片段编码为一个独立的 `token`，并与文本 token 交错融合，使 LLM 能够在统一序列中处理多导联、长时间 ECG 与自然语言指令。

- **专用 ECG Encoder 设计**：  
  使用 3 层 CNN 对原始波形进行特征提取，并通过线性投影层将其映射到 LLM 的隐空间，实现跨模态对齐。

- **Lead-aware Attention Masking**：  
  在同一时间点的不同导联之间允许双向注意力交互，利用多导联同步记录的生理一致性增强模型理解能力。

- **五阶段课程学习训练流程（Curriculum Learning Pipeline）**：
  1. 自编码器预训练（Autoencoder）
  2. 多选题与简答题（Multiple Choice & Short Answer）
  3. 统计量计算任务（Statistics Understanding）
  4. 多轮对话推理（Multi-turn Conversations）
  5. 心脏事件预测（Forecasting）

  该设计逐步提升模型从基础分类到复杂预测的能力。

### 🔍 相比现有方法的优势
| 维度 | CAMEL | 其他 ELMs（如 GEM, PULSE） |
|------|-------|-----------------------------|
| 输入长度 | 支持 0–10,000 秒 | 通常 ≤10 秒 |
| 是否支持预测 | ✅ 是 | ❌ 否 |
| 是否支持多 ECG 比较 | ✅ 是 | 有限支持 |
| 是否具备可解释性 | ✅ 基于 ECG 统计量生成解释 | 部分支持 |
| 泛化能力 | 强（zero-shot 表现优异） | 依赖 fine-tuning |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 类型 | 描述 |
|--------|------|------|
| **Icentia11k** | 标签数据 | 单导联连续 ECG，标注 NORM / AFIB / AFL，用于**预测任务** |
| **PTB-XL**, **CSN**, **CODE-15%**, **CPSC-2018**, **HEEDB**, **MIMIC-IV-ECG** | 分类 & 报告生成 | 多标签诊断代码数据集，覆盖多种心脏异常 |
| **Penn** | 私有数据集 | 来自宾大医院 ICU 的 8 导联 ECG，含 VT/VF 标注，用于 out-of-distribution 测试 |
| **MITDB, QTDB, SPH 等** | 无标签信号 | 用于 Stage 1 自编码器训练 |

> 总共使用 **14 个公开数据集 + 1 个私有数据集**

---

### ⚙️ 实验设置与评估指标

#### 模型架构
- **Backbone**: MedGemma-4B（医疗领域 LLM）
- **LoRA Adaptation**: Rank=128，冻结主干网络，仅微调适配器
- **ECG Encoder**: 3-layer CNN + Linear Projection → 映射至 LLM 隐空间（h=2560）

#### 评估任务与指标

| 任务 | 指标 | 说明 |
|------|------|------|
| **Forecasting** | F1 Score | 预测未来是否发生 AFIB/AFL（ABNORMAL vs NORM） |
| **Classification** | F1, AUROC | 多类别诊断代码分类（zero-shot 和 linear probing） |
| **Report Generation** | LLM-as-a-judge (GPT-5), BLEU, METEOR, ROUGE, BERT-F1 | 自动生成诊断报告的质量 |
| **QA** | Accuracy, Hamming Score | 单 ECG 与多 ECG 问答任务 |
| **Grounding** | RMSE | 对 RR interval、QRS duration 等统计量的数值还原精度 |

#### 基线方法对比
| 模型 | 类型 | 是否支持预测 |
|------|------|--------------|
| MELP, MERL | Zero-shot ECG-text 匹配模型 | ❌ |
| PULSE, GEM | 多模态 LLM（图像/波形+文本） | ❌ |
| XGB, CNN | Fully supervised 模型 | ✅（特定任务） |
| GPT-5.2 + Code Interpreter | 大模型 + 工具调用 | ✅（作为强基线） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 在 ECGForecastBench 上的表现（预测任务）
| 方法 | F1 Score (h=60s, w=600s) | 提升幅度 |
|------|--------------------------|---------|
| **CAMEL** | **73.42%** | — |
| Fully Supervised XGB | 59.64% | ↓13.78% |
| GPT-5.2 (with code interpreter) | 54.36% | ↓19.06% |
| GEM | 32.95% | ↓40.47% |

> CAMEL 在 zero-shot 设置下比 fully supervised 模型高出 **+12.4%**，比其他 zero-shot ELMs 高出 **+21.1%**

#### ✅ 在 ECGBench 上的 zero-shot 分类表现（平均 F1）
| 方法 | 平均 F1 (%) | 提升（vs SOTA） |
|------|------------|---------------|
| **CAMEL** | **67.53%** | +7.0% |
| GEM | 60.53% | — |
| PULSE | 58.07% | — |

> 在 PTB-XL 的 4 项子任务中，CAMEL 在其中 3 项取得最佳成绩。

#### ✅ 报告生成质量（LLM-as-a-judge 得分 / 满分 30）
| 方法 | PTB-XL | MIMIC-IV |
|------|--------|---------|
| GEM | 20.45 | 44.65 |
| **CAMEL** | **19.45** | **62.59** |

> CAMEL 在 MIMIC-IV 上显著优于所有基线，接近人类医生水平。

#### ✅ 数值接地能力（ECG Statistics RMSE）
| 方法 | RMSE (CPSC-2018) |
|------|------------------|
| GEM | 304 |
| **CAMEL** | **109** |

> RMSE 减少近 **2/3**，表明 CAMEL 更准确地理解和提取 ECG 生理参数。

---

### 🔬 消融实验结果（Ablation Study）

| 消融配置 | PTBXL-Super F1 (%) | 影响分析 |
|--------|--------------------|--------|
| 完整 CAMEL | **75.91** | — |
| 移除 LoRA | 0.00 | 模型完全失效，证明 LoRA 是关键可训练模块 |
| 使用全向注意力掩码 | 54.39 | 性能大幅下降 |
| 使用因果注意力 | 69.15 | 不如 lead-aware 掩码 |
| **Lead-aware Attention + LoRA** | ✅ 最优组合 | 利用了多导联同步性，且保持时序建模能力 |

> 结论：**LoRA 与 lead-aware attention masking 均不可或缺**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **首次实现了 zero-shot 心脏事件预测能力**：  
   CAMEL 可以基于当前正常节律 ECG，推理未来几分钟内是否会发展为 AFIB，且提供基于 RMSSD、PAC 数量等临床指标的可解释依据。

2. **长时序输入显著提升预测性能**：  
   当输入窗口从 10 秒增加到 600 秒时，F1 提高超过 15%，说明长期变异性是预测的重要线索。

3. **课程学习有效构建多层次能力**：  
   从统计量识别 → 多轮对话 → 风险预测的渐进训练策略，使模型获得扎实的 ECG grounding 能力。

4. **优于 fully supervised 模型**：  
   在多个任务上，CAMEL 的 zero-shot 表现超越专门训练的监督模型，展现出强大的泛化潜力。

---

### ⚠️ 局限性
1. **Tokenization 策略限制**：  
   当前按 1 秒切片可能导致 QRS 波群被截断，影响形态学细节捕捉。

2. **最大输入受限于 LLM 上下文长度**：  
   尽管支持长信号，但仍受制于 MedGemma 的 context window（约 8K tokens），难以处理极长时间记录。

3. **单导联为主**：  
   当前预测任务主要基于 Icentia11k 的单导联数据，尚未充分验证在完整 12 导联上的预测能力。

---

### 🔮 未来工作方向
1. **改进 Tokenization 方式**：  
   探索以 QRS 波群或心跳周期为单位的语义 token，提升生理意义表达能力。

2. **扩展至更多类型的预测任务**：  
   如室速（VT）、心搏停止（Cardiac Arrest）等更危急事件的预测。

3. **结合临床元数据**：  
   融合年龄、病史、药物等信息，进一步提升个体化风险预测准确性。

4. **部署于实时监护系统**：  
   构建端到端 pipeline，在 ICU 或可穿戴设备中实现实时预警。

---

> 💡 **一句话总结**：  
> CAMEL 是首个支持长时序推理与未来心脏事件预测的 ECG Language Model，通过创新的 token 化设计与课程学习框架，在 zero-shot 下全面超越现有方法，推动 AI 从“诊断”迈向“预见”。

</details>

---

### 10. [PERSONA: Dynamic and Compositional Inference-Time Personality Control via Activation Vector Algebra](https://arxiv.org/abs/2602.15669)

**Authors**: Xiachong Feng, Liang Zhao, Weihong Zhong, Yichong Huang, Yuxuan Gu, Lingpeng Kong, Xiaocheng Feng, Bing Qin  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15669v1  

#### Abstract
Current methods for personality control in Large Language Models rely on static prompting or expensive fine-tuning, failing to capture the dynamic and compositional nature of human traits. We introduce PERSONA, a training-free framework that achieves fine-tuning level performance through direct mani...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PERSONA: Dynamic and Compositional Inference-Time Personality Control via Activation Vector Algebra

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前在 **Large Language Models (LLMs)** 中实现 **personality 控制**的方法存在以下局限：

- **Prompting 方法**（如直接添加“你是一个外向的人”）虽然简单，但控制不稳定、不一致，容易失效。
- **Fine-tuning 方法**（如 SFT、DPO）虽有效，但需要大量计算资源，且每个个性配置都需要重新训练，无法灵活组合。
- 更重要的是，现有方法将 personality 视为**静态、单一的整体特征**，无法支持人类性格中常见的**动态变化**（如根据情境调整情绪）和**可组合性**（如同时具备“有创造力”和“有同理心”）。

### **提出了什么新方法或新思路**

作者提出 **PERSONA** 框架，一种无需训练（training-free）、基于激活空间向量代数的动态 personality 控制方法。其核心思想是：

> **Personality traits 在 LLM 的 activation space 中表现为近似正交（approximately orthogonal）的可提取方向，这些方向支持代数运算（加减乘除），从而实现对性格的精确、动态、可组合的控制。**

该框架由三个核心组件构成：

- **PERSONA-BASE**：通过 contrastive activation analysis 提取 OCEAN 五大人格维度（Openness, Conscientiousness, Extraversion, Agreeableness, Neuroticism）共 10 个极性 trait 的 personality vectors。
- **PERSONA-ALGEBRA**：利用向量代数操作实现性格控制：
  - **Scalar multiplication**：调节性格强度（如“更外向”）。
  - **Vector addition**：组合多个性格（如“外向 + 同理心”）。
  - **Vector subtraction**：抑制某个性格（如“减少焦虑”）。
- **PERSONA-FLOW**：在推理时（inference-time）动态地进行性格控制。通过一个“预测-引导”（predict-then-steer）机制，根据对话上下文实时预测最优的性格调整系数，并动态合成 steering vector。

### **相比现有方法的优势**

| 方面 | 传统方法 | PERSONA |
|------|--------|---------|
| **是否需要训练** | Fine-tuning 需要；Prompting 不需要 | ✅ 完全无需训练 |
| **控制精度** | Prompting 不稳定；Fine-tuning 较高 | ✅ 接近 fine-tuning 上限 |
| **动态适应性** | 几乎无 | ✅ 支持实时上下文感知调整 |
| **可组合性** | 有限 | ✅ 支持任意性格组合与抑制 |
| **计算效率** | Fine-tuning 极低；Prompting 高 | ✅ 仅增加约 5.5% 推理延迟 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

1. **PERSONA-BASE 和 PERSONA-ALGEBRA**：
   - 使用 **BFI-44**（Big Five Inventory-44）问卷作为性格评估工具。
   - 将其转化为 **scenario-based prompts**，避免 LLM 自我报告偏差。
   - 通过 GPT-4.1-mini 对模型响应进行 5 分制评分。

2. **PERSONA-FLOW 和 PERSONA-EVOLVE**：
   - 提出并构建了一个新的 benchmark：**PERSONA-EVOLVE**。
   - 包含 **800 个多轮对话场景**，覆盖 100 个不同角色（如“食品车老板”、“公设辩护人”）。
   - 每个角色有 **8 个对话回合**，模拟真实情感演变（如从“压力大”到“释然”）。
   - 评估要求模型在保持角色一致性的同时，动态适应情境。

3. **外部基准测试**：
   - 在 **PersonalityBench**（Deng et al., 2025）上进行跨基准验证，包含约 90 个情境问题 per trait。

### **实验设置和评估指标**

#### **评估指标**

- **PersonalityBench**：
  - **Mean Score**：五个维度得分总和。
  - **Variance**：衡量控制稳定性。

- **PERSONA-EVOLVE**：
  - **Win Rate (%)**：通过成对比较（A/B testing）评估，判断 steered 响应是否优于原始响应。
    - **Trait Adherence (TA)**：性格匹配度。
    - **Role Consistency (RC)**：角色一致性。
    - **Response Authenticity (RA)**：回应真实性。
    - **Information Fidelity (IF)**：信息准确性。
    - **Overall**：综合偏好。

#### **基线方法对比**

- **NPTI**：基于神经元激活分析的性格诱导。
- **Simple Prompt**：单形容词提示（如“你是一个外向的人”）。
- **P2 Induction**：使用模型生成的性格描述。
- **PAS**：探测 IPIP-NEO-300 相关注意力头。
- **ActAdd**：残差流激活添加。
- **Supervised Fine-Tuning (SFT)**：监督微调（LoRA），作为性能上限。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **在 PersonalityBench 上的表现**

| 方法 | 平均分 (Mean) | 方差 (Variance) |
|------|---------------|-----------------|
| **PERSONA-BASE** | **9.60** | **0.74** |
| SFT (上界) | 9.61 | 0.49 |
| NPTI | 9.43 | 0.49 |
| ActAdd | 8.20 | 2.10 |

✅ **PERSONA-BASE 以 9.60 分几乎追平 SFT 上限（9.61）**，且显著优于其他无需训练方法。

#### **在 PERSONA-EVOLVE 上的表现（Win Rate %）**

| 模型家族 | TA | RC | RA | IF | **Overall** |
|----------|----|----|----|----|------------|
| Qwen3-4B | 92.2 | 90.6 | 92.4 | 49.1 | **90.8** |
| Qwen2.5-14B | 84.8 | 86.4 | 84.8 | 59.3 | 85.4 |
| Llama-3.1-8B | 84.9 | 81.4 | 85.6 | 57.2 | 83.5 |
| Mistral-8B | 74.3 | 73.2 | 74.2 | 48.0 | 73.2 |

✅ **最高达到 90.8% 的综合胜率**，表明在多轮动态场景中具有强大优势。

### **与基线方法的对比结果**

- **PERSONA-BASE** 在所有无需训练方法中表现最佳，平均分领先第二名（NPTI）0.17分。
- 在 **Openness** 维度上优势最明显，说明 dense vector 表示能更好捕捉分布式语义。
- **PERSONA-FLOW** 在动态适应任务中全面超越 prompting 和静态 steering 方法。

### **消融实验结果**

#### **1. 系数离散化（Coefficient Binning）**

| 配置 | Overall Win Rate | Δ |
|------|------------------|---|
| 连续 [-2.0, +2.0] | 83.4 | — |
| 9-bin | 82.1 | -1.3 |
| 5-bin | 80.5 | -2.9 |
| 三值 {-1,0,+1} | 75.2 | -8.2 |

➡️ **细粒度控制至关重要**，粗略离散严重损害性能。

#### **2. 上下文窗口长度**

| 配置 | Overall Win Rate |
|------|------------------|
| 当前回合（Current turn only） | 83.4 |
| 最近3回合 | 81.9 |
| 所有历史 | 80.9 |

➡️ **仅用当前回合上下文效果最好**，说明过度依赖历史反而降低响应灵敏度。

#### **3. 稀疏性阈值（Sparsity Threshold T）**

| T | Overall Win Rate |
|---|------------------|
| 0.3 | 82.4 |
| **0.5** | **83.4** |
| 0.7 | 81.6 |

➡️ **T=0.5 是最优平衡点**，过低引入噪声，过高抑制合理调整。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **Personality 是数学上可处理的**：LLM 中的性格特质在 activation space 中表现为可提取、近似正交的方向，支持向量代数操作。
2. ✅ **无需训练即可实现 fine-tuning 级别性能**：PERSONA-BASE 在 PersonalityBench 上达到 9.60，接近 SFT 上限 9.61。
3. ✅ **支持动态、上下文感知的性格控制**：PERSONA-FLOW 可根据对话实时调整性格，胜率达 90.8%。
4. ✅ **性格可组合、可抑制**：通过向量加减法可实现复杂性格配置（如“有责任心但放松”）。
5. ✅ **方法通用性强**：在 Qwen、Llama、Mistral 等多种架构上均有效。

### **方法的局限性**

1. **安全对齐的限制**：
   - 某些与安全训练冲突的性格（如 `self-interested`）难以激活。
   - 但某些“看似无害”的性格（如 `inventive`, `careless`）可能意外降低安全性（Attack Success Rate ↑）。
2. **非完全正交性**：
   - 性格向量并非严格正交（如 `nervous` 与 `careless` 正相关），可能导致副作用。
   - 但实验证明这些副作用是**可预测、可组合**的，不影响整体控制。
3. **依赖高质量向量提取**：
   - 向量质量受 generator LLM 影响，小模型生成的 prompt 效果稍弱（但仍优于 baseline）。

### **未来工作方向**

1. **开发安全感知的 personality steering**：在增强性格表达的同时，确保 alignment 不被破坏。
2. **探索更多性格维度**：扩展至 MBTI、Dark Triad 等非 OCEAN 框架。
3. **多模态 personality 控制**：将向量代数扩展至图像、语音等模态。
4. **长期人格一致性建模**：研究如何在超长对话中维持核心人格不变，同时动态调整情绪状态。
5. **自动化向量库构建**：减少对人工设计 prompt 的依赖，实现 fully automated personality vector extraction。

---

> **总结一句话**：  
> **PERSONA 首次证明了 LLM 的 personality 在 activation space 中具有可解释、可组合、可动态调控的代数结构，为高效、灵活、可控的人格化 AI 提供了全新范式。**

</details>

---

### 11. [TAROT: Test-driven and Capability-adaptive Curriculum Reinforcement Fine-tuning for Code Generation with Large Language Models](https://arxiv.org/abs/2602.15449)

**Authors**: Chansung Park, Juyong Jiang, Fan Wang, Sayak Paul, Jiasi Shen, Jing Tang, Jianguo Li  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15449v1  

#### Abstract
Large Language Models (LLMs) are changing the coding paradigm, known as vibe coding, yet synthesizing algorithmically sophisticated and robust code still remains a critical challenge. Incentivizing the deep reasoning capabilities of LLMs is essential to overcoming this hurdle. Reinforcement Fine-Tun...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TAROT: Test-driven and Capability-adaptive Curriculum Reinforcement Fine-tuning for Code Generation with Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **Reinforcement Fine-Tuning (RFT)** 在代码生成任务中面临两个核心挑战：
- **Reward flatness**（奖励平坦）：传统方法将所有测试用例的成功视为同等重要，导致不同难度级别的反馈信号趋于一致，削弱了学习信号的区分度。
- **Reward sparsity**（奖励稀疏）：当模型无法通过任何测试时，缺乏中间梯度信号，训练难以推进。

此外，现有 **Curriculum Learning (CL)** 方法多在**跨问题级别**（inter-problem）进行排序，忽略了单个编程问题内部的**细粒度难度梯度**（intra-problem difficulty），且课程设计通常与模型能力脱节。

### 提出了什么新方法或新思路
论文提出了 **TAROT**（Test-driven and cApability-adaptive cuRriculum reinfOrcement fine-Tuning），其核心创新包括：

- **四层测试套件构建**（Four-tiered Test Suite）：
  为每个编程问题构造四个层级的测试用例：
  - **Basic**：验证基础逻辑和“快乐路径”（happy path）
  - **Intermediate**：中等复杂输入，包含常见边界值
  - **Complex**：算法复杂性高、需深度推理的场景
  - **Edge**：极端边界条件、空输入、异常处理等鲁棒性测试

  这种结构化设计显式建模了 **intra-problem 难度谱系**，为课程学习提供了精细控制的基础。

- **能力自适应课程机制**（Capability-adaptive Curriculum）：
  - 将课程进度（curriculum progression）与原始奖励得分解耦。
  - 引入 **curriculum allocator** 和 **reward weights**，允许根据不同模型的能力预设训练重点。
  - 实现“**能力条件化选择**”（capability-conditioned selection）：从一组预定义的课程策略中选择最适合当前模型能力的方案。

### 相比现有方法的优势
- **更稳定的学习动态**：通过分层奖励缓解了 reward flatness，提供更丰富的梯度信号。
- **更高的训练效率**：针对模型能力定制课程，避免低能力模型因过早接触难题而崩溃，也防止高能力模型陷入简单任务的冗余训练。
- **更强的泛化性和鲁棒性**：在 OOD（out-of-distribution）基准上表现更优。
- **可复现性强**：公开了完整的 **TAROT dataset** 和代码（GitHub: [https://github.com/deep-diver/TAROT](https://github.com/deep-diver/TAROT)）。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **TAROT dataset**：基于约 15k 条 Python 编程面试题构建，每道题配备人工审核的四层测试套件（共 60k 测试用例）。
- **来源**：`verifiable-coding-problems-python` 数据集。
- **生成方式**：使用 OpenAI 的 o3/o4 模型生成各层级测试用例，并通过参考答案严格验证其正确性。

### 实验设置和评估指标
- **模型范围广泛**：
  - Qwen2.5 系列（1.5B, 3B, 7B）及其代码专用版本 Qwen2.5-Coder
  - Gemma2 系列（2B, 9B）
  - 最新 Qwen3-4B-Instruct-2507
- **优化算法**：全部采用 **GRPO**（Generalized Reward Policy Optimization）进行 RFT。
- **训练细节**：
  - 单 epoch 训练
  - Batch size = 8（大模型降为 4）
  - Max input/completion tokens = 1024 / 4096
  - Temperature = 1.0（主实验）

### 评估指标
- **功能正确性**：`pass@1` on HumanEval, MBPP, HumanEval+, MBPP+
- **竞赛级解题能力**：`overall accuracy` on CodeForces, LiveCodeBench v5 (LCBv5)
- **代码推理能力**：`input/output prediction accuracy` on CruxEval

### 基线方法对比
- **Avg-reward**：对四层测试取平均通过率作为奖励
- **Pass@All**：仅当所有四层测试都通过时才给予奖励 1，否则为 0
- **无课程调度的标准 RL 设置**

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 策略 | HumanEval | MBPP | CruxEval (Input/Output) |
|------|-------|-----------|------|------------------------|
| Qwen2.5-1.5B-Instruct | TAROT (Best) | **60.98%** (+1.83pp) | **51.80%** (+2.60pp) | **40.20%/33.75%** |
| Qwen2.5-7B-Instruct | TAROT (Best) | **84.15%** (+0.40pp) | **69.00%** (+3.00pp) | **57.88%/59.38%** |
| Qwen3-4B-Instruct-2507 | C/E Weighted | **91.46%** (+2.44pp) | **55.20%** (+2.60pp) | **81.12%/75.25%** |

> 注：提升均相对于各自 base checkpoint，且在已有强 baseline 上实现进一步增益。

### 与基线方法的对比结果
- TAROT 在所有 benchmark 上均显著优于标准 RL 基线（Avg-reward 和 Pass@All）：
  - 在 **Qwen2.5-7B-Instruct** 上，TAROT 在 HumanEval 上达到 **84.15%**，优于 Avg-reward 的 83.75% 和 Pass@All 的 81.10%。
  - 在 **CodeForces** 上，TAROT 将性能从 11.41% 提升至 **12.95%**。
- 特别是在 **CruxEval** 等高阶推理任务上优势明显，表明 TAROT 能有效提升模型深层理解能力。

### 消融实验结果
#### （1）课程策略有效性分析
- **小模型（如 1.5B）**：`Basic Only` 或 `Forward (B&I Weighted)` 表现最佳 → 需从基础开始建立信心。
- **大模型（如 7B）**：`C/E Weighted` 或 `Complex Only` 更优 → 可直接挑战高难度任务以最大化收益。
- **代码专用模型（如 Qwen2.5-Coder-3B）**：行为类似更大的通用模型（如 7B），说明 **prior specialization 显著影响 effective capability**。

#### （2）超参数敏感性
- **GRPO β**：最优值任务相关。HumanEval 偏好较小 β=0.01（鼓励探索），MBPP 偏好 β=0.05（加强正则化）。
- **Temperature**：HumanEval 偏好高温（1.0）促进多样性探索；MBPP 偏好中温（0.7）平衡探索与利用。
- **Max completion tokens**：
  - HumanEval 类任务在 4096 tokens 时性能最好，继续增加反而下降（可能生成冗长错误代码）。
  - MBPP 类任务受益于更大生成空间（8192–16384 tokens 效果更佳）。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **最优课程依赖于模型能力而非参数量本身**：
   - 低能力模型适合 **easy-to-hard**（basic → complex）渐进式学习。
   - 高能力或代码专用模型更适合 **hard-first**（complex → basic）反向课程。
   - 存在一个“**Zone of Optimal Difficulty**”，即最有效的训练信号应来自略高于当前能力的任务。

2. ✅ **课程设计必须与模型能力匹配**：
   - 对弱模型施加复杂课程会导致 **training collapse**（如 Gemma2-2B-IT 多数课程有害）。
   - 对强模型限制在基础课程会浪费其潜力。

3. ✅ **TAROT 的能力自适应机制能持续提升性能**：
   - 即使在已很强的模型（如 Qwen3-4B）上也能带来 **+2.12 ~ +4.26 pp** 的提升。
   - 支持跨架构泛化（Qwen → Gemma）。

### 方法的局限性
- **数据依赖性强**：TAROT dataset 由前沿 LLM（如 o4）生成，可能存在生成器偏差或覆盖盲区。
- **语言限制**：目前仅支持 Python，未验证多语言或低资源语言下的效果。
- **静态课程选择**：当前策略从预定义组合中选择，尚未实现训练过程中的动态调整（如 multi-armed bandit）。
- **计算成本较高**：需要预先构建高质量四层测试集，增加了数据准备开销。

### 未来工作方向
- 探索 **continuous curriculum spaces** 和 **online adaptive scheduling**，实现在训练过程中动态调整课程。
- 扩展至 **multilingual code generation** 和 **low-resource programming languages**。
- 研究 **automated test suite design** 方法，减少对人工或强 LLM 的依赖。
- 结合 **process reward modeling** 提供更细粒度的中间反馈。
- 探索 **task-specific curriculum policies**，根据不同下游任务自动适配课程结构。

--- 

> 📌 **总结一句话**：  
> TAROT 通过引入**四层测试驱动课程**和**能力自适应机制**，解决了 RFT 中 reward flatness 和课程错配的问题，实现了对不同规模和专长的 LLM 的高效、稳定强化微调，显著提升了代码生成的功能正确性和鲁棒性。

</details>

---

### 12. [FlashMem: Supporting Modern DNN Workloads on Mobile with GPU Memory Hierarchy Optimizations](https://arxiv.org/abs/2602.15379)

**Authors**: Zhihao Shu, Md Musfiqur Rahman Sanim, Hangyu Zheng, Kunxiong Zhu, Miao Yin, Gagan Agrawal, Wei Niu  
**Category**: cs.DC  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15379v1  

#### Abstract
The increasing size and complexity of modern deep neural networks (DNNs) pose significant challenges for on-device inference on mobile GPUs, with limited memory and computational resources. Existing DNN acceleration frameworks primarily deploy a weight preloading strategy, where all model parameters...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：FlashMem: Supporting Modern DNN Workloads on Mobile with GPU Memory Hierarchy Optimizations**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
现代深度神经网络（DNN）模型日益增大且复杂，对移动设备上的**on-device inference**提出了严峻挑战。当前主流的 DNN 推理框架（如 MNN、TVM、LiteRT 等）普遍采用 **weight preloading** 策略，即将整个模型权重一次性加载到内存中再执行推理。这种策略导致：
- **峰值内存消耗高**，难以支持大模型或多模型并发运行；
- **初始化延迟长**，尤其是涉及从磁盘加载并转换为 GPU texture memory 格式时；
- **GPU 内存层级利用低效**，未充分利用 mobile GPU 的 2.5D texture memory 特性。

此外，在多模型流水线场景（如 FIFO 调度）下，重复的加载与格式转换开销进一步加剧性能瓶颈。

---

### **提出的新方法与创新点**
本文提出 **FlashMem** —— 一种基于 GPU 内存层级优化的内存流式处理框架，其核心思想是**动态按需流式加载权重**，而非全量预加载。主要贡献如下：

#### ✅ **1. 优化重叠计划生成（Optimized Overlap Plan Generation, OPG）**
- 将 weight loading 与 computation 的调度形式化为一个 **Constraint Programming Satisfiability (CP-SAT)** 问题；
- 设计了一个 **Load Capacity-aware OPG-Solver (LC-OPG)**，静态生成最优的权重加载时间表，最小化峰值内存并保证高效执行。

#### ✅ **2. 自适应融合机制（Adaptive Fusion）**
- 分析不同 operator 类型（Elemental、Reusable、Hierarchical）的计算强度与负载容忍能力；
- 动态决定是否拆分 fused operators，以平衡 **memory、compute 和 load capacity**，避免过度融合限制调度灵活性。

#### ✅ **3. 层级 GPU 内存优化（Hierarchical GPU Memory Optimization）**
- 利用 **2.5D texture memory** 结构，重构权重布局以提升缓存效率；
- 减少不必要的 layout transformation 开销（如 transpose、reshape），直接在纹理内存中进行高效访问。

#### ✅ **4. 流水线感知内核执行（Pipeline-Aware Kernel Execution）**
- 改写 GPU kernel，实现 **branch-free、pipelined 执行模式**；
- 在每个迭代中交替执行“预取下一权重块”与“计算当前数据”，有效隐藏内存延迟。

---

### **相比现有方法的优势**
| 维度 | FlashMem | 传统框架（MNN, TVM, SmartMem 等） |
|------|--------|-------------------------------|
| **内存管理** | 动态流式加载，仅保留必要权重 | 全量预加载，峰值内存高 |
| **执行效率** | 重叠加载与计算，减少空闲周期 | 加载与执行串行，初始化延迟大 |
| **GPU 利用率** | 针对 2.5D texture memory 优化 | 忽视 texture memory 特性 |
| **多模型支持** | 支持多模型 FIFO 流水线 | 多数无法支持大模型共存 |

---

## **2. 核心实验方法和设置**

### **使用的模型与任务**
在 **11 个代表性 DNN 模型**上进行了评估，涵盖多种应用场景：

| 模型类别 | 示例模型 | 参数量级 |
|---------|----------|--------|
| NLP | GPTNeo-S, GPTNeo-1.3B, GPTNeo-2.7B | 最高达 2.7B |
| 图像分类 | ResNet50, ViT, DeepViT | 25M–204M |
| 图像生成 | StableDiffusion-UNet (SD-UNet) | 860M |
| 语音识别 | Whisper-Medium | 356M |
| 视频分割 | DepthAnything-S/L | 24M–333M |
| 图像分割 | SAM-2 | 215M |

> 数据集来源包括 ImageNet、LibriSpeech、LAION-5B、The Pile 等。

---

### **实验平台**
- **主测试设备**：OnePlus 12（Adreno 750 GPU, 16GB RAM）
- **跨设备验证**：OnePlus 11、Xiaomi Mi 6、Google Pixel 8
- **精度配置**：FP16 / FP32，batch size = 1
- **调优启用**：所有 baseline 均开启 auto-tuning

---

### **评估指标**
| 指标 | 描述 |
|------|------|
| **End-to-end Latency** | 初始化 + 推理总耗时（FlashMem 将两者集成） |
| **Average Memory Usage** | 运行期间平均内存占用 |
| **Peak Memory** | 峰值内存消耗 |
| **Speedup** | 相对于 baseline 的加速比 |
| **Memory Reduction** | 内存使用降低倍数 |
| **Energy Consumption** | 功耗 × 时间（焦耳） |
| **Portability** | 在不同硬件上的兼容性与性能一致性 |

---

### **基线方法对比**
- **商业级框架**：MNN、NCNN、TVM、LiteRT（TFLite）、ExecuTorch（ETorch）
- **研究原型**：SmartMem（前序工作，同团队）
- **不包含**：llama.cpp、FlexNN 等因不支持 mobile GPU 或特定模型被排除

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### 📊 **端到端延迟对比（Table 7）**
- FlashMem 实现 **1.7× ~ 75.0× 的整体加速**；
- 相较于 **ExecuTorch** 平均快 **75.0×**；
- 相较于 **SmartMem** 平均快 **8.6×**；
- 对大模型优势显著：
  - **GPTNeo-1.3B**：比 SmartMem 快 **15.8×**
  - **SD-UNet**：快 **9.3×**
  - **DeepViT**：快 **10.0×**

> ⚠️ 注意：部分框架（如 NCNN）甚至无法运行 Transformer 类大模型。

#### 📉 **内存消耗对比（Table 8）**
- 平均内存减少 **2.0× ~ 8.4×**；
- 相比 SmartMem，平均节省 **3.5×** 内存；
- 具体案例：
  - **Whisper-Medium**：从 1,433MB → **240MB**（**6.0×** 下降）
  - **DeepViT**：从 826MB → **165MB**（**5.0×** 下降）
  - **GPTNeo-1.3B**：从 2,667MB → **554MB**（**4.8×** 下降）

#### 🔋 **功耗与能耗（Table 9）**
| 模型 | FlashMem 能耗 | 相比 SmartMem 节能 |
|------|----------------|--------------------|
| DeepViT | 4.5J | ↓ **89%** |
| SD-UNet | 17.9J | ↓ **87%** |

尽管功耗略高（因更高 GPU 利用率），但由于**极低延迟**，总体能量大幅下降。

#### 🧪 **消融实验分析（Figure 7）**
分解各组件贡献（以 SmartMem 为基线）：

| 组件 | 速度提升范围 | 内存缩减范围 |
|------|-------------|--------------|
| **OPG-Solver** | 5.3× ~ 8.1× | 2.1× ~ 3.8× |
| **Adaptive Fusion** | 1.5× ~ 5.1× | 1.1× ~ 1.4× |
| **Kernel Rewriting** | 1.0× ~ 2.55× | 1.0× ~ 1.1× |

> 表明 **OPG-Solver 是最大贡献者**，而 kernel rewriting 更侧重于延迟优化。

#### 🔄 **与其他重叠策略对比（Figure 9）**
- **Always-Next Loading**：最多慢 **4.3×**
- **Same-Op-Type Prefetching**：最多慢 **2.4×**
- 说明 FlashMem 的 **细粒度、容量感知调度策略更优**

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **动态流式加载可显著降低内存压力**：通过按需加载而非预加载，FlashMem 成功在资源受限的移动设备上运行了 **GPTNeo-2.7B** 等此前无法支持的大模型。
2. ✅ **重叠 weight loading 与 computation 可大幅提升效率**：合理调度可在不增加延迟的前提下大幅削减内存。
3. ✅ **2.5D texture memory 是 mobile GPU 性能关键**：针对性优化 layout transformation 和 kernel 执行路径，能有效提升吞吐。
4. ✅ **operator fusion 需权衡 memory 与 load capacity**：过度融合会削弱调度灵活性，应根据 operator 类型动态调整。
5. ✅ **FlashMem 具备良好可移植性**：在不同 GPU 架构（Adreno / Mali）和内存配置下均表现稳定，尤其在低内存设备上更具优势（Figure 10）。

---

### **局限性**
1. ❗ **离线求解器存在非线性时间增长**：随着模型规模增大（如 Llama2-70B），CP-SAT 求解时间接近上限（150秒），可能影响部署灵活性；
2. ❗ **目前仅支持静态 DNN 图**：对 dynamic neural networks（如条件分支模型）尚未适配；
3. ❗ **依赖 profiled load capacity 数据**：虽使用 XGBoost 预测，但仍需轻量级 profiling 阶段；
4. ❗ **未考虑 CPU-GPU 协同调度**：聚焦 GPU memory hierarchy，未深入探讨异构处理器间的任务划分。

---

### **未来工作方向**
1. ➕ 扩展至 **datacenter 场景**：将该方法应用于服务器端大规模 LLM 推理；
2. ➕ 支持 **dynamic execution paths**：开发适用于动态图模型的在线调度机制；
3. ➕ 探索 **quantized model 支持**：结合 INT8/INT4 推理进一步压缩内存；
4. ➕ 研究 **memory-energy trade-off 的自动调节机制**：实现动态参数调优以适应不同应用场景（低延迟 vs 低功耗）；
5. ➕ 引入 **hardware-software co-design**：针对特定 mobile GPU 架构定制更精细的内存调度策略。

---

> 💡 **总结一句话**：  
> **FlashMem 通过静态规划 + 动态流式加载 + GPU 内存层级协同优化，在移动端实现了大模型与多模型高效共存，突破了传统预加载范式的内存与性能瓶颈。**

</details>

---

### 13. [Learning Data-Efficient and Generalizable Neural Operators via Fundamental Physics Knowledge](https://arxiv.org/abs/2602.15184)

**Authors**: Siying Ma, Mehrdad M. Zadeh, Mauricio Soroco, Wuyang Chen, Jiguo Cao, Vijay Ganesh  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15184v1  

#### Abstract
Recent advances in scientific machine learning (SciML) have enabled neural operators (NOs) to serve as powerful surrogates for modeling the dynamic evolution of physical systems governed by partial differential equations (PDEs). While existing approaches focus primarily on learning simulations from ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Data-Efficient and Generalizable Neural Operators via Fundamental Physics Knowledge

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于数据驱动的 **Neural Operators (NOs)** 在学习物理系统动态演化时存在三大挑战：
1. **高数据需求**：缺乏物理先验，需要大量多样化训练数据才能达到高精度。
2. **物理不一致性**：可能违反守恒律或产生非物理解，尤其在长期rollout预测中。
3. **泛化能力差**：对未见过的物理参数设置（out-of-distribution, OOD）或从仿真到真实场景（synthetic-to-real）迁移表现不佳。

这些问题源于模型仅从目标PDE学习，而忽略了更基础的物理原理。

---

### 🚀 提出的新方法与思路
作者提出一种**多物理联合训练框架（multiphysics training framework）**，其核心思想是：
> **显式地将基本物理知识（fundamental physics knowledge）融入神经算子的学习过程**。

具体做法：
- **分解原始PDE**为简化的“基本形式”（basic forms），这些形式保留主导物理机制但计算成本更低。
  - 例如：Navier-Stokes方程中保留对流项 `- (u·∇)u`，忽略压力和扩散项。
  - Diffusion-Reaction中保留扩散项，去除非线性反应项。
- **联合训练**：同时在原始PDE模拟数据和其简化的基本形式上进行训练。
- 利用基本形式作为**辅助任务（auxiliary task）**，提升主任务（原始PDE建模）的数据效率和泛化能力。

该方法是**架构无关的（architecture-agnostic）**，适用于FNO、Transformer等多种NO架构。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 数据来源 | 单一PDE或跨多种PDE预训练（如SciML foundation models） | 同一PDE与其基本物理形式联合训练 |
| 物理归纳偏置 | 弱或无 | 显式引入基本物理机制 |
| 训练成本 | 高（依赖昂贵全模拟） | 更低（可“交换”部分昂贵模拟为廉价基本形式） |
| 泛化性 | 对OOD敏感 | 显著增强OOD与real-world泛化 |

> 💡 关键洞察：比起简单聚合不同PDE（如foundation models），**理解一个复杂PDE内部的基础组件更为根本且有效**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与PDE系统
研究涵盖多个经典时间依赖型PDE，在1D/2D/3D空间中测试，包括：
- **Diffusion-Reaction (2D)**：模拟激活-抑制系统的图灵模式形成。
- **Incompressible Navier-Stokes (2D & 3D)**：流体动力学，用于速度场/密度场建模。
- **Kuramoto-Sivashinsky (1D)**：描述时空混沌行为。
- **ScalarFlow (real-world 3D)**：真实烟雾流动重建数据集，用于评估 synthetic-to-real 转移。

所有数据通过数值求解器生成（如PhiFlow, PyClaw），并控制分辨率与时间步长以保证公平比较。

---

### ⚙️ 实验设置与评估指标

#### ✅ 评估指标
- **nRMSE**（Normalized Root Mean Squared Error）：主要评价指标。
- Autoregressive rollout误差：衡量长期预测稳定性。
- OOD generalization：在训练分布外的物理参数下测试性能。
- Synthetic-to-real transfer：在真实世界数据（ScalarFlow）上的表现。

#### ✅ 基线方法对比
- **Baseline**：仅在原始PDE上训练神经算子。
- **Ours**：联合训练原始PDE + 其基本形式（按“样本混合比例”Sample Mixture Ratio调整预算）。
- 其他对比方法（消融实验中）：
  - Spatiotemporal Downsampling
  - Lie Symmetry Augmentation
  - 不同基本项选择（如只保留reaction而非diffusion）

#### ✅ 训练策略
- 多任务学习框架：共享骨干网络，最后一层分离输出。
- 损失函数加权：`Loss = Loss_full + 0.7 × Loss_basic`
- 控制总模拟成本一致：利用基本形式计算便宜的特点，“节省”的预算用于增加采样数量。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自Table 2 & Figure 5–7）

| PDE | 方法 | In-Dist nRMSE | OOD Target 1 | OOD Target 2 |
|-----|------|----------------|-------------|--------------|
| **2D Diffusion-Reaction** | Baseline | 0.0289 | 0.0413 (β=1) | 0.0770 (β=100) |
| | **Ours** | **0.0231** | **0.0331** | **0.0538** |
| **2D Navier-Stokes** | Baseline | 0.0487 | 0.0825 (ν=0.05) | 0.0369 (ν=0.0001) |
| | **Ours** | **0.0175** | **0.0222** | **0.0125** |
| **3D Navier-Stokes** | Baseline | 0.0675 | 0.0393 (ν=0.1) | 0.0836 (ν=0.0001) |
| | **Ours** | **0.0481** | **0.0329** | **0.0602** |

> ✅ 所有情况下，**Ours均显著优于Baseline**，尤其在OOD场景下误差降低高达 **60%以上**。

---

### 📈 数据效率提升（Figure 5）
- 在相同或更低模拟成本下，**Ours在左下方区域**（更低误差 + 更低成本），表明更强的数据效率。
- 例如在2D Diffusion-Reaction中，达到相同nRMSE所需模拟成本减少约 **50%**。

---

### 🔁 长期一致性（Autoregressive Rollout, Figure 6）
- 在5步自回归rollout中，**Ours的误差累积明显更慢**。
- 表明模型学到的动态更具物理合理性，避免“漂移”或发散。

---

### 🌍 Synthetic-to-Real 泛化（Figure 7）
- 在真实烟雾数据集 **ScalarFlow** 上测试：
  - Baseline: nRMSE = 0.250
  - **Ours**: nRMSE = **0.213**
- 视觉上，Ours能更好捕捉烟羽结构与运动趋势，证明更强的现实迁移能力。

---

### 🔍 消融实验结果

#### （1）基本项的选择至关重要（Figure 14）
| 分解方式 | PDE | 效果 |
|--------|-----|------|
| 正确基本项（如Diffusion） | Diffusion-Reaction | nRMSE ↓ 11%-24% |
| 错误基本项（如Reaction only） | Diffusion-Reaction | nRMSE ↑ 最多达64% |
| 正确基本项（Advection） | Navier-Stokes | 性能大幅提升 |
| 错误基本项（Diffusion only） | Navier-Stokes | nRMSE ↑ 13%-179% |

> ✅ 结论：**改进来源于选择了正确的、具有丰富物理意义的基本项**，不是任意简化都能带来收益。

#### （2）与其他增强方法正交（Figure 17）
- 将本方法与 **Lie Symmetry Augmentation** 结合：
  - 单独使用Lie：轻微提升
  - 单独使用Ours：大幅改进
  - 联合使用：进一步小幅增益
> ➕ 说明该方法与其他数据增强技术**互补且正交**。

#### （3）超参数鲁棒性（Table 8）
- 辅助损失权重 `λ ∈ {0.5, 0.7, 1.0}` 影响极小（最大偏差 < 0.0063）
> ✅ 方法对超参数不敏感，易于部署。

#### （4）扩展至大模型（Figure 16）
- 应用于 **DPOT（Hao et al., 2024）** 这类大规模SciML foundation model：
  - 仍能持续提升数据效率
> ✅ 证明该策略不仅适用于小模型，也能为已有强大预训练模型提供额外增益。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **神经算子可以且应当理解PDE的基本物理成分**：
   - 实验证明，公开的SciML foundation models虽然隐式学习了一些基础项，但并未充分掌握。
   - 显式加入基本形式训练可显著弥补这一差距。

2. **基本物理知识是一种高效的数据增强手段**：
   - 基本形式模拟成本远低于完整PDE（见Table 1，Sample Mixture Ratio达1:24）。
   - 可“用廉价数据换高性能”，实现**更高性价比的训练**。

3. **该方法带来三重好处**：
   - **数据效率提升**：更少模拟即可达到更高精度。
   - **长期物理一致性增强**：rollout更稳定，符合物理规律。
   - **强OOD与real-world泛化能力**：适应未知参数与真实观测。

4. **通用性强、易集成**：
   - 不依赖特定NO架构（FNO、Transformer均有效）。
   - 可与其他技术（downsampling, symmetry augmentation）结合使用。

---

### ⚠️ 局限性
1. **需要领域知识来定义“基本形式”**：
   - 当前方法依赖人工分析PDE结构以确定应保留哪一项（如对流 vs 扩散）。
   - 自动识别最优分解仍是开放问题。

2. **并非所有PDE都容易分解**：
   - 对高度耦合或非标准形式的PDE，如何提取有意义的基本项尚需探索。

3. **目前集中在理想化仿真环境**：
   - 尽管已测试ScalarFlow，但在更多真实复杂场景中的有效性有待验证。

---

### 🔮 未来工作方向
1. **自动化基本项发现机制**：
   - 利用符号回归、稀疏识别（SINDy）、或元学习自动提取主导物理项。

2. **构建“基础物理词典”**：
   - 定义常见PDE的标准基本形式库，供社区复用。

3. **扩展至其他科学领域**：
   - 如量子力学、电磁学、生物力学等，探索是否普遍存在类似“基本项优先”的学习范式。

4. **与PINNs结合**：
   - 将基本形式纳入loss设计，形成 hybrid 物理约束 + 多物理训练框架。

---

> 🏁 **一句话总结**：  
> 本文揭示了一个深刻洞见——**最好的科学机器学习模型不仅要学会“做什么”，更要懂得“为什么”**。通过让神经算子显式学习PDE背后的**fundamental physics knowledge**，我们不仅能做得更快（data-efficient），更能做得更准、更稳、更泛化。

</details>

---

### 14. [1-Bit Wonder: Improving QAT Performance in the Low-Bit Regime through K-Means Quantization](https://arxiv.org/abs/2602.15563)

**Authors**: Sohir Maskey, Constantin Eichenberg, Johannes Messner, Douglas Orr  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15563v1  

#### Abstract
Quantization-aware training (QAT) is an effective method to drastically reduce the memory footprint of LLMs while keeping performance degradation at an acceptable level. However, the optimal choice of quantization format and bit-width presents a challenge in practice. The full design space of quanti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**1-Bit Wonder: Improving QAT Performance in the Low-Bit Regime through K-Means Quantization**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在 **Quantization-Aware Training (QAT)** 中，尤其是在极低比特（如 1-bit）量化场景下，存在以下挑战：
- **精度-容量权衡不明确**：在固定推理内存预算下，如何在模型参数量 $N$ 和权重精度 $P$ 之间进行最优分配尚无系统研究。
- **主流整数量化格式性能受限**：对称的 uniform integer 量化在极低比特下表现不佳，且优化不稳定（如 1-bit 时易出现 NaN）。
- **评估方式偏差**：许多研究依赖 perplexity 或训练损失作为评估指标，无法真实反映生成任务上的性能退化。

### 🚀 提出的新方法与创新思路
1. **引入基于 k-means 的非线性量化格式（k-means quantization）**
   - 不再使用均匀划分的量化区间，而是通过 **1D k-means 聚类**学习适应权重分布的非均匀质心（centroids）。
   - 在 block-wise 设置中应用该策略，每个 block 使用共享 scale 参数和 learned centroids。
   - 支持高效实现：利用 **vector lookup tables (LUT)** 在标准硬件上实现快速解码。

2. **提出“精度感知缩放律”（precision-aware scaling laws）**
   - 将量化位宽 $P$ 显式建模为影响模型有效容量的因素：  
     $$
     N_{\text{eff}} = N \cdot f(P), \quad f(P) = 1 - \exp(-P/\gamma_w)
     $$
   - 利用此模型分析在固定内存预算 $M = N \cdot P$ 下的最佳精度选择。

3. **倡导“以最低稳定精度换取更大参数量”的设计范式**
   - 实验证明，在相同内存预算下，**1-bit + 更大模型**优于高精度小模型。

### 🔍 相比现有方法的优势
| 方面 | 本文方法（k-means QAT） | 传统方法（uniform integer QAT / PTQ） |
|------|------------------------|-------------------------------|
| 极低比特稳定性 | ✅ 1-bit 稳定训练，无需特殊归一化技巧 | ❌ 1-bit 易崩溃，需 mean-shift 等 hack |
| 重建误差 | ✅ 最小化 L2 重构误差（理论最优） | ⚠️ 非最优，尤其对非均匀分布 |
| 性能上限 | ✅ 在相同内存下达到更高下游任务性能 | ⚠️ 受限于格式表达能力 |
| 推理效率 | ✅ 支持软件 kernel 加速（如 Triton LUT） | ⚠️ 多数依赖原生硬件支持 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **预训练数据**：
  - **Nemotron-CC**：过滤后的 Common Crawl 子集，用于 scaling law 实验。
  - **混合高质量数据**（long-horizon curriculum）：
    - Web data: Nemotron-CC
    - Code: Starcoder-V2
    - Math: FineMath-3+/4+
- **SFT 数据**：Tulu 3 SFT Mixture（指令微调）
- **评估基准**：涵盖常识、推理、代码、数学等多维度任务。

### ⚙️ 实验设置
| 组件 | 配置 |
|------|------|
| 模型架构 | Llama 3-style Decoder-only Transformer |
| 上下文长度 | 4096（预训练），8192（SFT） |
| Token 数量 | 150B（主实验）；scaling law 实验覆盖 8–50B |
| QAT 策略 | Warm-up 1000 步后开启量化，centroids 冻结 |
| Block Size | 64（所有 block-wise 量化） |
| Scale 存储 | 16-bit float per block（增加 0.25 bits/weight 开销） |
| 硬件 | NVIDIA H100（训练），L40S（推理 benchmark） |

### 🎯 评估指标
| 类型 | 指标 |
|------|------|
| 预训练阶段 | Pretraining loss（logprob-based） |
| 下游生成任务 | Accuracy / Pass@1 on：<br>• MMLU, HellaSwag, PIQA, ARC<br>• HumanEval, MBPP（code gen）<br>• GSM8K（math reasoning）<br>• MMLU-PRO, AidanBench（高级推理） |
| 推理效率 | Tokens/s（autoregressive generation）<br>Effective bandwidth（GB/s） |

### 🔁 基线对比
| 模型 | 参数量 | 权重精度 | 内存占用（≈） |
|------|--------|----------|-------------|
| Baseline | 4B | bf16 (16-bit) | 7.8 GB |
| 对照组1 | 12B | 4-bit uniform | ≈7.8 GB |
| 对照组2 | 31B | 1-bit k-means | ≈7.8 GB |

> 所有模型均保持 embedding 和 output head 为 bf16，仅 backbone 权重量化。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| Benchmark | 4B / 16-bit | 12B / 4-bit | **31B / 1-bit** |
|---------|------------|------------|----------------|
| **MMLU** | 33.21 | 50.86 | **51.61** |
| **ARC-C** | 41.21 | 49.15 | **50.68** |
| **MBPP** | 19.00 | 26.40 | **30.00** |
| **HumanEval** | 12.80 | 15.24 | **18.29** |
| **GSM8K (5-shot)** | 27.90 | **48.52** | 45.26 |
| **AidanBench** | 88.08 | 147.48 | **167.83** |

> ✅ **31B 1-bit 模型在绝大多数任务上超越 12B 4-bit 和 4B 16-bit 模型**

### 🔁 与基线方法对比结果
- **k-means vs uniform 量化（scaling law 分析）**
  - k-means 在所有 bit-width 下均优于 uniform integer。
  - 差距在 **P ≤ 4.25 bits** 时最显著。
  - 归因于更高的 `g(P) = f(P)/P`，即单位内存的有效容量更高。
- **下游任务性能**
  - 即使是 1.25-bit（含 scale overhead）的极端压缩，仍能实现更强的生成能力。
  - 表明 **loss-based 评估不能准确预测生成性能**（图5显示 12B 4-bit 预训练 loss 更优，但最终生成性能略逊于 31B 1-bit）。

### 🔍 消融实验结果
#### （1）Normalization 策略（Appendix A.6）
| 量化方式 | 最佳 normalization |
|--------|------------------|
| Uniform 2-bit | absmean（避免多数值归零） |
| Uniform ≥3-bit | absmax |
| **k-means** | **对 normalization 不敏感** ✅ |

> 表明 k-means 更鲁棒，简化了工程调参。

#### （2）QAT 启动时机
- 固定从第 1000 步开始启用 QAT，确保公平比较。
- 早起 QAT 导致训练不稳定，尤其对 k-means 聚类初始化不利。

#### （3）Kernel 效率测试（Table 6 & 7）
| Format | Speedup (vs bf16) | Effective BW |
|-------|--------------------|--------------|
| 4.25-bit (Triton) | 3.7× | 721 GB/s |
| **1.25-bit (Triton)** | **7.6×** | **438 GB/s** |

> 虽未达理论峰值（12.8×），但在实际 kernel 中已实现显著加速。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **在固定推理内存预算下，最佳策略是采用尽可能低的稳定精度，并将节省的内存用于扩展模型规模**。
   - 即：**push toward 1-bit regime and scale up N**。
2. **k-means 非线性量化显著优于传统的 uniform integer 量化**，特别是在 ≤4-bit 场景。
   - 提供更优的 L2 重建误差与训练稳定性。
3. **即使是 1-bit 模型也能通过 QAT 实现强大的下游生成能力**。
   - 31B 1-bit 模型在多数任务上超过 12B 4-bit 和原始 4B 16-bit 模型。
4. **现有的 loss-based 评估严重高估低比特模型的真实性能**。
   - 必须结合 generative downstream benchmarks 进行综合评估。

### ⚠️ 局限性
- **缺乏专用硬件支持**：当前依赖 custom Triton kernels 和 lookup tables，未发挥极致潜力。
- **大 batch 推理优势消失**：当 batch size 增大时，计算瓶颈取代内存瓶颈，量化增益减弱（见 Figure 3 & 9）。
- **仅量化 backbone**：embedding 和 output head 保留 bf16，限制了端到端压缩率。
- **未探索其他非线性格式**：如矢量量化（VQ）、混合精度拓扑等仍有空间。

### 🔮 未来工作方向
1. **开发支持非线性量化的专用 ASIC/FPGA 架构**，进一步释放 1-bit 模型潜力。
2. **探索动态自适应 centroids**：允许 centroids 在训练中持续更新而非冻结。
3. **扩展至 MoE 架构中的 expert 权重量化**，提升稀疏模型的部署效率。
4. **研究 activation quantization 与 weight quantization 的联合优化**，迈向全模型超低位宽训练。
5. **构建面向 ultra-low-bit 模型的标准评估套件**，推动社区关注真实生成能力而非仅 perplexity。

---

> 💡 **一句话总结**：  
> 本论文证明了 **“1-bit + k-means + QAT” 是一条通往极致高效 LLM 推理的新路径**，其核心思想——**牺牲精度换取规模，在内存约束下最大化有效容量**——有望重塑未来轻量化大模型的设计范式。

</details>

---

### 15. [Neural Network-Based Parameter Estimation of a Labour Market Agent-Based Model](https://arxiv.org/abs/2602.15572)

**Authors**: M Lopes Alves, Joel Dyer, Doyne Farmer, Michael Wooldridge, Anisoara Calinescu  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.15572v1  

#### Abstract
Agent-based modelling (ABM) is a widespread approach to simulate complex systems. Advancements in computational processing and storage have facilitated the adoption of ABMs across many fields; however, ABMs face challenges that limit their use as decision-support tools. A significant issue is parame...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Neural Network-Based Parameter Estimation of a Labour Market Agent-Based Model**

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
- **ABM 参数估计的挑战**：Agent-Based Models (ABMs) 在模拟复杂系统（如劳动力市场）时具有强大能力，但其参数空间高维、计算成本高昂且输出具有随机性，导致传统参数估计方法（如 SSE、CMA-ES）难以有效探索参数空间。
- **不确定性量化不足**：现有方法多提供点估计，缺乏对参数不确定性的建模，限制了 ABM 作为决策支持工具的应用。

### **提出了什么新方法或新思路**
- 引入并评估了 **SBI4ABM** 框架——一个基于神经网络的 Simulation-Based Inference (SBI) 工具，专门用于高维 ABM 的参数估计。
- 利用 **Neural Posterior Estimation (NPE)** 和 **Normalizing Flows (MAF)** 构建全局后验密度估计器，实现 amortization（一次训练可多次采样），显著减少仿真次数。
- 对比了两种 **summary statistics** 的效果：
  - **Handcrafted statistics**：人工设计的统计量（min, max, mean, variance, quantiles, autocorrelation）
  - **NN-learned statistics**：通过嵌入式 RNN 自动学习低维表示

### **相比现有方法的优势**
| 方法 | 是否支持不确定性 | 是否支持 amortization | 是否自动提取特征 | 效率 |
|------|------------------|------------------------|--------------------|------|
| SSE / CMA-ES | ❌ | ❌ | ❌ | 中等 |
| ABC (Approximate Bayesian Computation) | ✅ | ❌ | ❌ | 低 |
| **SBI4ABM (NPE + NF)** | ✅ | ✅ | ✅（若用 NN-learned） | 高 |

> ✅ SBI4ABM 能更高效地逼近 posterior 分布，尤其在大规模 ABM 上具备更强扩展性。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
| 数据类型 | 描述 |
|---------|------|
| **Synthetic datasets** | 构造 n=10 到 460 个职业的合成劳动力市场，控制 `z`, `Pi,j`, `p` 分布以复现美国数据特征 |
| **Real-world U.S. labour market data** | 来自 BLS 的 Current Population Survey (2010–2017)，包含 464 个职业的真实 transition matrix 和 workforce 分布 |

### **实验设置**
- **目标模型**：Labour Market Occupational Mobility and Automation ABM (**LM-ABM**)  
- **待估参数向量**：`θ = [δu, δv, r]`
  - `δu`: 就业者被解雇率
  - `δv`: 新岗位创建率
  - `r`: 劳动者留在原职业的概率
- **Prior distributions**：均匀分布（见 Table 3）
- **仿真长度**：T = 600 时间步（约 78 年），每步对应 6.75 周
- **训练流程**：
  1. 从 prior 中采样 θ
  2. 运行 LM-ABM 得到输出 x
  3. 构建 `(θ, x)` 数据集训练 NPE
  4. 给定观测数据 y，通过条件化得到 posterior `p(θ|y)`

### **评估指标**
- **Posterior coverage**：真实参数是否落在高密度区域
- **Sharpness**：posterior 分布集中程度（越尖锐越好）
- **Scalability**：随职业数 n 增加的 simulation 和 training 时间变化
- **Simulation-Based Calibration (SBC)**：检验 posterior 是否校准良好（期望为 uniform rank histogram）

### **基线方法对比**
- **Baseline 1**：使用 handcrafted summary statistics 的 SBI4ABM
- **Baseline 2**：未明确运行其他 SBI 或 ABC 方法，但引用其理论劣势进行间接比较
- **消融实验**：比较不同 summary statistics 类型的影响（handcrafted vs. NN-learned）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### ✅ **NN-learned summary statistics 表现更优**
- 图 2 显示，使用 RNN 学习的 summary statistics 所得 posterior 分布更 **sharp**，峰值紧贴真实参数值（橙色线）
- 相比之下，handcrafted statistics 导致 posterior 更分散（图 1），表明信息损失更多

#### ⚠️ **但存在 calibration 问题**
- **SBC 结果（图 8 & 9）显示反直觉现象**：
  - Handcrafted 方法：rank histogram 接近 uniform → **well-calibrated**
  - NN-learned 方法：histogram 中央凸起、尾部缺失 → **under-dispersed credible intervals**
- 表明 NN-learned 方法虽然更精确（sharp），但可能 **过拟合或降维偏差**，导致不确定性低估

#### 📈 **良好的可扩展性（scalability）**
- **Simulation time**（图 3）：随职业数 n 线性增长，标准差小，稳定性好
- **Training time**（图 4）：当 n > 360 后方差增大，反映硬件资源瓶颈（内存、GPU 显存）

#### 🔍 **在美国真实数据上的应用**
- 成功在 U.S. 数据（n=464）上运行 SBI4ABM（图 5）
- 发现参数间弱相关性（图 6）：
  - `δu` 与 `δv` 正相关（0.24）
  - `r` 与 `δu`, `δv` 负相关（≈ -0.2）
- 通过 high-density sampling 发现三种动态模式（图 7）：
  - Pattern 1：低 `δu/δv` → 稳定市场
  - Pattern 2：中等波动
  - Pattern 3：高 `δu/δv` → 高失业与高频职位变动并存

### **与基线方法的对比结果**
| 维度 | Handcrafted Stats | NN-Learned Stats |
|------|-------------------|------------------|
| Posterior sharpness | 较宽泛 | ✅ 更集中 |
| 参数恢复准确性 | 一般 | ✅ 更接近真值 |
| Uncertainty calibration | ✅ 良好（uniform ranks） | ❌ 不足（central-peaked ranks） |
| 特征工程依赖 | 高（需领域知识） | ✅ 自动学习 |
| 可扩展性 | 受限于手工特征表达力 | ✅ 更适合大数据 |

> ➤ **Trade-off 发现**：NN-learned statistics 提升精度但牺牲可靠性；handcrafted 更稳健但表达能力有限。

### **消融实验结果**
- 明确进行了 **summary statistics 类型的消融实验**：
  - 固定 SBI4ABM 框架，仅更换 summary statistics 模块
  - 结果证明：NN-learned 显著提升 posterior concentration，验证了自动特征学习的有效性
- 未对 NPE 架构（如 MAF 层数）、RNN 类型等做进一步消融

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **SBI4ABM 具备处理现实规模 ABM 的潜力**：
   - 在 n=464 的美国劳动力市场上成功运行，simulation 时间随 n 线性增长，具备实际应用前景。
   
2. ✅ **NN-learned summary statistics 提升估计精度**：
   - 相比 handcrafted 方法，能生成更 sharp 的 posterior，更好捕捉真实参数位置。

3. ⚠️ **存在精度-可靠性权衡（precision-reliability trade-off）**：
   - NN 方法虽更“准确”，但 SBC 显示其 posterior 过于集中，可能导致可信区间误判。

4. 🔗 **揭示了参数间的隐含关系**：
   - 发现 `δu` 与 `δv` 存在正相关，以及 `r` 对就业稳定性的非线性影响，这些未在原始 LM-ABM 中明确提出。

5. 💾 **微数据（microdata）集成面临严重内存瓶颈**：
   - 完整 job transition matrices 内存需求超 240GB，远超常规服务器容量，导致无法在真实微数据上完成端到端训练。

### **方法的局限性**
| 局限性 | 说明 |
|-------|------|
| **合成数据简化** | 合成数据仅复制基本分布，未考虑真实经济周期、政策干预等复杂因素 |
| **缺乏真实 microdata 验证** | 无法将 ABM 输出与真实个体级 job transition 数据对比，限制外部有效性 |
| **NN summary statistics 可解释性差** | RNN 提取的特征不可读，难以诊断错误来源 |
| **内存瓶颈制约实用性** | 大规模微数据下的 SBI 仍不现实，需模型压缩或分布式训练支持 |

### **未来工作方向**
1. **优化 summary statistic 网络架构**：
   - 探索 Graph Neural Networks (GNNs) 或 Transformer 来更好地建模职业 transition network 结构。
2. **引入真实 longitudinal microdata**：
   - 使用行政记录或平台数据（如 LinkedIn、Indeed）构建可验证的 ground truth。
3. **解决内存瓶颈**：
   - 开发 streaming SBI、checkpointing 或模型蒸馏技术降低存储压力。
4. **改进 calibration 性能**：
   - 设计正则化策略或混合 summary statistics 方案，在 sharpness 与 calibration 之间取得平衡。
5. **跨国家/行业迁移能力研究**：
   - 测试 SBI4ABM 是否可在不同经济体间迁移 posterior estimator。

---

> 🏁 **总体评价**：该论文是推动 ABM 从“玩具模型”走向“决策工具”的重要一步。它展示了 **NN-based SBI 在复杂经济建模中的巨大潜力**，同时也坦诚揭示了当前方法在 **可靠性、可扩展性和数据整合方面的真实挑战**，为后续研究提供了清晰的方向。

</details>

---

### 16. [The Vision Wormhole: Latent-Space Communication in Heterogeneous Multi-Agent Systems](https://arxiv.org/abs/2602.15382)

**Authors**: Xiaoze Liu, Ruowang Zhang, Weichen Yu, Siheng Xiong, Liu He, Feijie Wu, Hoin Jung, Matt Fredrikson, Xiaoqian Wang, Jing Gao  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15382v1  

#### Abstract
Multi-Agent Systems (MAS) powered by Large Language Models have unlocked advanced collaborative reasoning, yet they remain shackled by the inefficiency of discrete text communication, which imposes significant runtime overhead and information quantization loss. While latent state transfer offers a h...

---

### 17. [COMPOT: Calibration-Optimized Matrix Procrustes Orthogonalization for Transformers Compression](https://arxiv.org/abs/2602.15200)

**Authors**: Denis Makhov, Dmitriy Shopkhoev, Magauiya Zhussip, Ammar Ali, Baher Mohammad, Stamatios Lefkimmiatis  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15200v1  

#### Abstract
Post-training compression of Transformer models commonly relies on truncated singular value decomposition (SVD). However, enforcing a single shared subspace can degrade accuracy even at moderate compression. Sparse dictionary learning provides a more flexible union-of-subspaces representation, but e...

---

### 18. [Size Transferability of Graph Transformers with Convolutional Positional Encodings](https://arxiv.org/abs/2602.15239)

**Authors**: Javier Porras-Valenzuela, Zhiyang Wang, Alejandro Ribeiro  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15239v1  

#### Abstract
Transformers have achieved remarkable success across domains, motivating the rise of Graph Transformers (GTs) as attention-based architectures for graph-structured data. A key design choice in GTs is the use of Graph Neural Network (GNN)-based positional encodings to incorporate structural informati...

---

### 19. [Continuous-Time Piecewise-Linear Recurrent Neural Networks](https://arxiv.org/abs/2602.15649)

**Authors**: Alena Br\"andle, Lukas Eisenmann, Florian G\"otz, Daniel Durstewitz  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.15649v1  

#### Abstract
In dynamical systems reconstruction (DSR) we aim to recover the dynamical system (DS) underlying observed time series. Specifically, we aim to learn a generative surrogate model which approximates the underlying, data-generating DS, and recreates its long-term properties (`climate statistics'). In s...

---

### 20. [Co-Design and Evaluation of a CPU-Free MPI GPU Communication Abstraction and Implementation](https://arxiv.org/abs/2602.15356)

**Authors**: Patrick G. Bridges (University of New Mexico), Derek Schafer (University of New Mexico), Jack Lange (Oak Ridge National Laboratory), James B. White III (Oak Ridge National Laboratory), Anthony Skjellum (Tennessee Technological University), Evan Suggs (Tennessee Technological University), Thomas Hines (Tennessee Technological University), Purushotham Bangalore (University of Alabama), Matthew G. F. Dosanjh (Sandia National Laboratories), Whit Schonbein (Sandia National Laboratories)  
**Category**: cs.DC  
**Published**: 2026-02-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.15356v1  

#### Abstract
Removing the CPU from the communication fast path is essential to efficient GPU-based ML and HPC application performance. However, existing GPU communication APIs either continue to rely on the CPU for communication or rely on APIs that place significant synchronization burdens on programmers. In th...

---

### 21. [In Agents We Trust, but Who Do Agents Trust? Latent Source Preferences Steer LLM Generations](https://arxiv.org/abs/2602.15456)

**Authors**: Mohammad Aflah Khan, Mahsa Amani, Soumi Das, Bishwamittra Ghosh, Qinyuan Wu, Krishna P. Gummadi, Manish Gupta, Abhilasha Ravichander  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15456v1  

#### Abstract
Agents based on Large Language Models (LLMs) are increasingly being deployed as interfaces to information on online platforms. These agents filter, prioritize, and synthesize information retrieved from the platforms' back-end databases or via web search. In these scenarios, LLM agents govern the inf...

---

### 22. [Causal Effect Estimation with Latent Textual Treatments](https://arxiv.org/abs/2602.15730)

**Authors**: Omri Feldman, Amar Venugopal, Jann Spiess, Amir Feder  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15730v1  

#### Abstract
Understanding the causal effects of text on downstream outcomes is a central task in many applications. Estimating such effects requires researchers to run controlled experiments that systematically vary textual features. While large language models (LLMs) hold promise for generating text, producing...

---

### 23. [Mind the (DH) Gap! A Contrast in Risky Choices Between Reasoning and Conversational LLMs](https://arxiv.org/abs/2602.15173)

**Authors**: Luise Ge, Yongyan Zhang, Yevgeniy Vorobeychik  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15173v1  

#### Abstract
The use of large language models either as decision support systems, or in agentic workflows, is rapidly transforming the digital ecosystem. However, the understanding of LLM decision-making under uncertainty remains limited. We initiate a comparative study of LLM risky choices along two dimensions:...

---

### 24. [Mnemis: Dual-Route Retrieval on Hierarchical Graphs for Long-Term LLM Memory](https://arxiv.org/abs/2602.15313)

**Authors**: Zihao Tang, Xin Yu, Ziyu Xiao, Zengxuan Wen, Zelin Li, Jiaxi Zhou, Hualei Wang, Haohua Wang, Haizhen Huang, Weiwei Deng, Feng Sun, Qi Zhang  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15313v1  

#### Abstract
AI Memory, specifically how models organizes and retrieves historical messages, becomes increasingly valuable to Large Language Models (LLMs), yet existing methods (RAG and Graph-RAG) primarily retrieve memory through similarity-based mechanisms. While efficient, such System-1-style retrieval strugg...

---

### 25. [LLM-to-Speech: A Synthetic Data Pipeline for Training Dialectal Text-to-Speech Models](https://arxiv.org/abs/2602.15675)

**Authors**: Ahmed Khaled Khamis, Hesham Ali  
**Category**: cs.CL  
**Published**: 2026-02-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15675v1  

#### Abstract
Despite the advances in neural text to speech (TTS), many Arabic dialectal varieties remain marginally addressed, with most resources concentrated on Modern Spoken Arabic (MSA) and Gulf dialects, leaving Egyptian Arabic -- the most widely understood Arabic dialect -- severely under-resourced. We add...

---

### 26. [On the Out-of-Distribution Generalization of Reasoning in Multimodal LLMs for Simple Visual Planning Tasks](https://arxiv.org/abs/2602.15460)

**Authors**: Yannic Neuhaus, Nicolas Flammarion, Matthias Hein, Francesco Croce  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15460v1  

#### Abstract
Integrating reasoning in large language models and large vision-language models has recently led to significant improvement of their capabilities. However, the generalization of reasoning models is still vaguely defined and poorly understood. In this work, we present an evaluation framework to rigor...

---

### 27. [Controlled oscillation modeling using port-Hamiltonian neural networks](https://arxiv.org/abs/2602.15704)

**Authors**: Maximino Linares, Guillaume Doras, Thomas H\'elie  
**Category**: cs.LG  
**Published**: 2026-02-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15704v1  

#### Abstract
Learning dynamical systems through purely data-driven methods is challenging as they do not learn the underlying conservation laws that enable them to correctly generalize. Existing port-Hamiltonian neural network methods have recently been successfully applied for modeling mechanical systems. Howev...

---

### 28. [Secure and Energy-Efficient Wireless Agentic AI Networks](https://arxiv.org/abs/2602.15212)

**Authors**: Yuanyan Song, Kezhi Wang, Xinmian Xu  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15212v1  

#### Abstract
In this paper, we introduce a secure wireless agentic AI network comprising one supervisor AI agent and multiple other AI agents to provision quality of service (QoS) for users' reasoning tasks while ensuring confidentiality of private knowledge and reasoning outcomes. Specifically, the supervisor A...

---

### 29. [How Vision Becomes Language: A Layer-wise Information-Theoretic Analysis of Multimodal Reasoning](https://arxiv.org/abs/2602.15580)

**Authors**: Hongxuan Wu, Yukun Zhang, Xueqing Zhou  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15580v1  

#### Abstract
When a multimodal Transformer answers a visual question, is the prediction driven by visual evidence, linguistic reasoning, or genuinely fused cross-modal computation -- and how does this structure evolve across layers? We address this question with a layer-wise framework based on Partial Informatio...

---

### 30. [CARE Drive A Framework for Evaluating Reason-Responsiveness of Vision Language Models in Automated Driving](https://arxiv.org/abs/2602.15645)

**Authors**: Lucas Elbert Suryana, Farah Bierenga, Sanne van Buuren, Pepijn Kooij, Elsefien Tulleners, Federico Scari, Simeon Calvert, Bart van Arem, Arkady Zgonnikov  
**Category**: cs.AI  
**Published**: 2026-02-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15645v1  

#### Abstract
Foundation models, including vision language models, are increasingly used in automated driving to interpret scenes, recommend actions, and generate natural language explanations. However, existing evaluation methods primarily assess outcome based performance, such as safety and trajectory accuracy,...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
