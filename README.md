# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-12 06:01:18 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Double: Breaking the Acceleration Limit via Double Retrieval Speculative Parallelism](https://arxiv.org/abs/2601.05524)

**Authors**: Yuhao Shen, Tianyu Liu, Junyi Shen, Jinyang Wu, Quan Kong, Li Huan, Cong Wang  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.05524v1  

#### Abstract
Parallel Speculative Decoding (PSD) accelerates traditional Speculative Decoding (SD) by overlapping draft generation with verification. However, it remains hampered by two fundamental challenges: (1) a theoretical speedup ceiling dictated by the speed ratio between the draft and target models, and ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Double: Breaking the Acceleration Limit via Double Retrieval Speculative Parallelism

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Parallel Speculative Decoding (PSD)** 中存在的两个根本性瓶颈：
1. **理论加速上限限制**：PSD 的最大加速比被严格限制在 `C`（即 draft model 与 target model 的单 token 推理延迟比），难以突破这一“速度天花板”。
2. **中段 token 拒绝导致的计算浪费**：当 draft model 在序列中间生成错误 token 时，后续所有已生成的 token 都会被丢弃，造成严重的 pipeline stall 和计算资源浪费。

### 提出的新方法：DoUBLE
提出了一种全新的训练免费、无损的并行推测解码框架 —— **DoUBLE (Double Retrieval Speculative Parallelism)**，其核心创新在于引入了**双检索机制 (Double Retrieval)**：

- **Draft-Side Iterative Retrieval**  
  在 draft model 上执行多轮迭代式 retrieval speculation，用检索到的短语替代自回归生成，从而在一次 target forward 时间内生成远超 `C` 个 token 的候选序列，**打破理论加速上限 `C`**。

- **Target-Side Authoritative Retrieval**  
  target model 执行单步 retrieval，生成多 token 的预验证指导（multi-token pre-verify guidance）。该指导用于：
  - 提前过滤低质量路径（Multi-token Pre-verify）
  - 将潜在拒绝转化为前向扩展（Forward Guidance），避免 rollback 浪费。

### 相比现有方法的优势
| 维度 | DoUBLE | 现有方法（如 PEARL, EAGLE-3） |
|------|--------|-------------------------------|
| **是否突破 `C` 上限** | ✅ 是 | ❌ 否 |
| **是否训练免费** | ✅ 是 | ❌ EAGLE-3 需要复杂训练 |
| **是否无损** | ✅ 是 | ✅ 多数是 |
| **处理 mid-rejection 能力** | ✅ 主动修复与扩展 | ❌ 回滚丢弃 |
| **精度-效率权衡** | ✅ 双侧协同，最优平衡 | ⚠️ 单侧检索存在 dilemma |

---

## 2. 核心实验方法和设置

### 数据集
实验覆盖多个领域任务，共使用 **5 个基准数据集**：
- **HumanEval**：代码生成
- **GSM8K**：数学推理
- **CNN/DM**：摘要生成
- **Alpaca**：指令遵循
- **MT-Bench**：综合对话能力评估

### 模型配置
测试了多种主流 LLM 架构组合，包括：
- **LLaMA-2 / LLaMA-3.3** (7B/70B)
- **Deepseek-Coder** (1.3B/33B)
- **Qwen3** (0.6B/14B/32B)

其中，小模型作为 draft model，大模型为 target model。

### 评估指标
- **Wall-time Speedup**：真实端到端推理时间加速比（相对于 vanilla autoregressive decoding）。
- **Mean Accepted Tokens (M)**：平均每轮成功接受的 token 数量，反映稳定性。
- **Average Matched Tokens (AMT)**：衡量 retrieval 精度的代理指标。

### 基线方法对比
涵盖五类代表性方法：
1. **标准 SD**：如原始 Speculative Decoding
2. **Target-Side Retrieval**：Lookahead, PLD, Token Recycling
3. **Draft-Side Retrieval**：Ouroboros
4. **Parallel SD**：PEARL, Sps
5. **训练型 SOTA**：EAGLE-3（需大量训练）

所有实验均在 **8×NVIDIA A100 (80GB)** 上进行，采用 greedy sampling，batch size=1。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| Model Pair | Method | Speedup (HumanEval) | Speedup (CNN/DM) | Avg. Speedup |
|-----------|--------|---------------------|------------------|-------------|
| LLaMA3.3-70B | DoUBLE | **5.33×** | **3.75×** | **4.27×** |
| Qwen3-32B | DoUBLE | **2.84×** | **2.56×** | **2.33×** |
| Deepseek-33B | DoUBLE | **4.45×** | **4.05×** | **4.05×** |
| LLaMA2-70B | DoUBLE | **3.42×** | **3.05×** | **3.09×** |

> 💡 **State-of-the-art 表现**：在 LLaMA3.3-70B 上达到 **5.3× 加速**，显著超越 EAGLE-3（4.56×）。

### 与基线方法对比
- 在 **LLaMA3.3-70B** 上，DoUBLE 相比 PEARL 提速 **21.4%**，相比 EAGLE-3 提速 **6.8%**。
- 在受限的 **Qwen3-14B (C≈1.6)** 场景下，DoUBLE 实现 **1.89×** 加速，**首次突破理论上限 `C`**，而 PEARL 仅达 1.38×。
- 在 **Deepseek-33B** 上，DoUBLE 达到 **4.05×**，远超 Ouroboros 的 3.15× 和 TR 的 2.14×。

### 消融实验结果
#### （1）组件消融（见图5）
| 配置 | HumanEval Speedup (LLaMA3.3-70B) | M 值 |
|------|-------------------------------|-------|
| w/o Draft Retrieval | 4.50× | ↓ |
| w/o Target Retrieval | 3.88× | 从 14.35 → 11.5 |
| **DoUBLE (完整)** | **5.33×** | **14.35** |

👉 结论：**双检索缺一不可**。移除任一组件都会显著降低性能。

#### （2）温度采样下的表现（表2）
在非贪婪采样（T=1.0）下仍保持高效：
- LLaMA3.3-70B 平均加速 **3.77×**，优于 EAGLE-3（3.71×）和 PEARL（3.34×）。
- 验证了方法在随机采样场景下的鲁棒性。

#### （3）检索深度影响（图6）
- 最优 retrieval depth `d=10`，超过后 AMT 饱和，speedup 波动。
- 过深检索导致上下文相关性下降，接受率降低。

#### （4）先验知识有效性（表3）
- 使用 ShareGPT 初始化的轻量级 `.pkl` datastore（约 9.5MB）即可实现良好泛化。
- 性能在 K=10 轮初始化后趋于饱和，存储开销可控。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **成功打破 PSD 的理论加速上限 `C`**  
   通过 draft-side iterative retrieval 实现 `L_draft > C`，将有效速度比提升至 `C' = C × (1 + AMT)`。

2. ✅ **解决 retrieval 中的 Precision-Efficiency Dilemma**  
   双检索机制兼顾 draft model 的高效率与 target model 的高精度，实现帕累托最优。

3. ✅ **无需训练即可超越 SOTA 训练方法**  
   DoUBLE 在零训练成本下，性能全面超越需要复杂训练的 EAGLE-3。

4. ✅ **完全无损且兼容性强**  
   理论证明与实验证实输出分布与原生 autoregressive 完全一致（Exact Match 准确率无损）。

### 方法的局限性
1. **统一 datastore 设计可能未充分挖掘模型差异**  
   当前使用共享 datastore 和固定 retrieval depth，未来可探索 model-specific 优化。

2. **工程开销仍有压缩空间**  
   当前基于 transformers 库实现，KV-cache rollback 和通信带来额外延迟（约占总耗时 8.3%），可通过集成 vLLM/SGLang 等高性能引擎进一步优化。

3. **极端内存受限场景需降级执行**  
   在单 GPU 场景下无法并行，退化为串行流程，但仍优于 baseline。

### 未来工作方向
- 探索 **adaptive retrieval depth** 和 **decoupled datastores** 以适配不同模型能力。
- 将 DoUBLE 集成至 **Nano-vLLM / vLLM / SGLang** 等工业级推理引擎，释放更高吞吐潜力。
- 研究与 **on-policy distillation** 或 **EAGLE-style draft model** 的结合，进一步增强 draft quality。
- 扩展至 **high-batch-size** 和 **long-context** 场景的系统级优化。

---

> 🔚 **总结**：DoUBLE 通过创新性的 **double retrieval** 机制，在不牺牲准确性、无需额外训练的前提下，首次实现了对 PSD 理论加速极限的突破，并在多个大规模模型上取得 SOTA 性能，为高效 LLM 推理提供了极具前景的新范式。

</details>

---

### 2. [Self-Evolving Distributed Memory Architecture for Scalable AI Systems](https://arxiv.org/abs/2601.05569)

**Authors**: Zixuan Li, Chuanzhen Wang, Haotian Sun  
**Category**: cs.DC  
**Published**: 2026-01-12  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.05569v1  

#### Abstract
Distributed AI systems face critical memory management challenges across computation, communication, and deployment layers. RRAM based in memory computing suffers from scalability limitations due to device non idealities and fixed array sizes. Decentralized AI frameworks struggle with memory efficie...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Self-Evolving Distributed Memory Architecture for Scalable AI Systems

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

分布式 AI 系统在 **计算、通信和部署** 三个层面面临严重的内存管理挑战：

- **In-Memory Computing 层**：基于 RRAM 的计算架构受限于固定阵列大小和器件非理想性，难以扩展。
- **Decentralized Communication 层**：去中心化框架采用静态路由策略，忽略节点计算负载和内存状态，导致通信瓶颈。
- **Multi-Agent Deployment 层**：编译器驱动的部署系统将应用逻辑与执行环境紧密耦合，缺乏运行时自适应能力。

这些问题的根本原因在于：**缺乏跨架构层的协同内存管理机制**。

---

### **提出了什么新方法或新思路**

作者提出 **SEDMA**（Self-Evolving Distributed Memory Architecture），一个三层统一框架，实现跨层协同内存管理：

#### ✅ 三大核心组件：

1. **Memory-Guided Matrix Processing（内存引导矩阵处理）**
   - 动态分区 RRAM 计算任务，基于设备特性（如导电性、延迟）和历史性能模式。
   - 引入自适应误差校正机制，动态调整正则化参数 `λ`。

2. **Adaptive Distributed Communication（自适应分布式通信）**
   - 内存感知的 Peer Selection：综合考虑网络拓扑、计算容量、内存可用性和延迟。
   - 智能缓存策略：基于访问频率和数据局部性优化缓存命中率。

3. **Dynamic Deployment Optimization（动态部署优化）**
   - 运行时持续重新编译部署配置（如 Kubernetes YAML）。
   - 基于短期资源利用率和长期性能模式进行 Agent 重调度。

#### ✅ 核心创新：双记忆系统（Dual Memory Architecture）

- **长期记忆（Episodic Memory）**：存储历史成功模式（如最优分区策略、Peer 性能档案、部署模板）。
- **短期记忆（Working Memory）**：记录实时工作负载统计、当前资源使用情况。
- 支持系统“自我演化”——通过经验积累不断优化决策。

---

### **相比现有方法的优势**

| 维度 | 传统方法缺陷 | SEDMA 优势 |
|------|-------------|-----------|
| **可扩展性** | 固定 RRAM 阵列限制大矩阵处理 | 动态分区 + 自适应纠错提升扩展性 |
| **通信效率** | DHT 路由忽略负载，易造成热点 | 内存感知路由避免过载节点 |
| **部署灵活性** | 静态部署无法响应运行时变化 | 动态 recompilation 实现自适应优化 |
| **资源利用率** | 各层独立优化，存在资源浪费 | 跨层协同优化，提升整体效率 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **COCO 2017**：用于大规模图像处理任务，测试矩阵运算性能（矩阵尺寸 1024×1024 ~ 4096×4096）。
- **ImageNet**：评估分布式推理在异构节点上的表现。
- **SQuAD**：测试分布式 NLP 推理任务中的内存与通信开销。
- **合成张量传输基准 & 联邦学习场景**：用于通信效率评估。

---

### **实验设置**

- **硬件环境**：异构集群（8–32 CPU cores, 16–128 GB RAM），含边缘设备与高性能服务器。
- **软件栈**：
  - PyTorch 2.0.0
  - libp2p-rs 0.52.0（P2P 通信）
  - Kubernetes client-go v0.28.0（部署）
  - PostgreSQL 14.5（持久化长期记忆）
- **训练配置**：batch size=32, lr=0.001, AdamW, weight decay=1e-4，共 200 epochs。
- **评估周期**：24 小时动态负载模拟（训练 → 推理 → 联邦聚合）。

---

### **评估指标**

| 类别 | 指标 |
|------|------|
| **内存效率** | Memory Utilization Efficiency (%) |
| **通信性能** | Communication Latency (ms), Bandwidth Usage (GB/h), Cache Hit Rate (%) |
| **系统吞吐** | Throughput (operations per second, ops/sec) |
| **部署适应性** | Adaptation Time (min), Resource Utilization (%) |
| **鲁棒性** | Resilience Score（故障恢复能力） |
| **准确性** | COCO mAP, ImageNet Top-1 Acc, SQuAD F1 |

---

### **基线方法对比**

| 基线方法 | 类型 |
|--------|------|
| **Ray Distributed** | 分布式任务调度框架 |
| **PyTorch Distributed** | 分布式训练基础设施 |
| **Static RRAM (MELISO+)** | 固定分区 + 静态纠错的 RRAM 架构 |
| **Kubernetes Native Scheduling** | 静态部署调度器 |
| **libp2p / Lattica** | 去中心化通信协议 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1 & Table 2）**

| 方法 | Mem. Eff. (%) | Comm. Lat. (ms) | Thpt. (ops/s) | COCO Acc. (%) | ImageNet (%) | SQuAD F1 |
|------|----------------|------------------|---------------|----------------|--------------|----------|
| Ray Distributed | 72.1 | 245.3 | 98.7 | 84.2 | 76.8 | 88.4 |
| PyTorch Distributed | 69.8 | 267.1 | 91.2 | 83.7 | 75.9 | 87.9 |
| Static RRAM | 70.6 | 298.4 | 98.9 | 84.0 | 76.2 | 88.1 |
| **Ours (SEDMA)** | **87.3** | **171.2** | **142.5** | **85.1** | **78.3** | **89.7** |

> ✅ **内存利用率提升 21.1%**（87.3% vs 72.1%）
>
> ✅ **通信延迟降低 30.2%**（171.2ms vs 245.3ms）
>
> ✅ **吞吐量提高 44.3%**（142.5 vs 98.7 ops/s）

---

### **系统适应性与鲁棒性（Table 2）**

| 方法 | Adapt. Time (min) | Res. Util. (%) | Resilience | Cache Hit (%) | BW (GB/h) | Peer Sel. Acc. (%) |
|------|--------------------|----------------|------------|----------------|-----------|---------------------|
| Kubernetes Native | 47.8 | 64.2 | 6.7 | 58.3 | 12.4 | 71.2 |
| Static Deployment | 52.1 | 61.9 | 6.1 | 55.7 | 13.8 | 68.9 |
| libp2p Baseline | 41.2 | 67.3 | 7.2 | 62.1 | 11.7 | 74.6 |
| **Ours** | **3.2** | **82.7** | **9.4** | **78.9** | **7.3** | **86.2** |

> ✅ **部署适应时间缩短 93.3%**（3.2 min vs 47.8 min）
>
> ✅ **资源利用率提升至 82.7%**
>
> ✅ **带宽消耗减少 40%+**

---

### **消融实验结果（Ablation Study）**

#### 🔹 移除长期/短期记忆（Table 3）

| 变体 | Mem. Eff. (%) | Adapt. Time (min) | Thpt. (ops/s) |
|------|----------------|--------------------|---------------|
| Full Model | 87.3 | 3.2 | 142.5 |
| w/o Long-term Memory | 71.4 | 8.7 | 118.3 |
| w/o Short-term Memory | 79.2 | 5.1 | 128.9 |

> ⚠️ 长期记忆缺失导致效率下降 **15.9%**，验证其对学习历史模式的重要性。

---

#### 🔹 移除通信与部署模块（Table 4）

| 变体 | Latency (ms) | Res. Util. (%) | Resilience |
|------|--------------|----------------|-----------|
| Full Model | 171.2 | 82.7 | 9.4 |
| w/o Adaptive Peer Sel. | 234.8 | 68.9 | 7.1 |
| w/o Dynamic Recomp. | 189.3 | 71.2 | 6.8 |

> ⚠️ 移除自适应 Peer Selection 导致延迟增加 **37.1%**。

---

#### 🔹 低层级实现细节对比（Table 5 & 6）

| 变体 | Accuracy (%) | Mem. Eff. (%) | Conv. Epochs |
|------|--------------|----------------|--------------|
| Full Model | 85.1 | 87.3 | 45.2 |
| Fixed λ=1e-12 | 83.7 | 82.1 | 52.8 |
| Static LR | 84.2 | 84.6 | 48.9 |

| 变体 | Peer Sel. Acc. (%) | Cache Hit (%) | BW (GB/h) |
|------|---------------------|----------------|-----------|
| Full Model | 86.2 | 78.9 | 7.3 |
| DHT-only Routing | 74.6 | 62.1 | 11.7 |
| Random Peer Sel. | 68.3 | 55.4 | 13.2 |

> ✅ 自适应参数调优显著优于固定配置。

---

## 4. 关键结论和发现

### **主要发现**

1. **跨层协同内存管理至关重要**：单一层次优化无法解决分布式 AI 的系统级瓶颈。
2. **双记忆系统有效支持“自我演化”**：长期记忆提供经验指导，短期记忆保障实时响应。
3. **动态 recompilation 显著提升适应速度**：从小时级到分钟级的部署更新，极大增强系统弹性。
4. **内存感知通信大幅降低开销**：结合负载与拓扑的 Peer Selection 是去中心化系统的关键改进。
5. **SEDMA 在异构环境中表现出强鲁棒性**：面对节点失效、网络分区仍能维持 >76% 功能。

---

### **方法的局限性**

- **依赖外部数据库**：长期记忆依赖 PostgreSQL，可能成为单点瓶颈或安全风险。
- **初始化冷启动问题**：系统初期缺乏历史数据，短期性能可能低于成熟状态。
- **复杂度较高**：三层协同 + 双记忆系统增加了实现与调试难度。
- **未公开真实硬件部署**：实验基于仿真与模拟集群，尚未在真实 RRAM 芯片上验证。

---

### **未来工作方向**

1. **轻量化记忆系统设计**：探索嵌入式向量数据库或神经索引替代传统 DB。
2. **跨集群迁移学习**：将一个集群的学习经验迁移到新集群，缓解冷启动。
3. **安全与隐私增强**：在去中心化通信中引入 Differential Privacy 或 Homomorphic Encryption。
4. **与 LLM 编排系统集成**：将 SEDMA 作为底层资源调度引擎，支撑 agentic workflow 执行。
5. **真实 RRAM 测试平台构建**：在物理芯片上验证动态分区与误差校正效果。

---

> 📌 **总结一句话**：  
> SEDMA 通过 **跨计算-通信-部署三层的协同内存管理** 和 **双记忆驱动的自我演化机制**，实现了分布式 AI 系统在资源效率、通信性能和系统适应性上的全面突破，为下一代可扩展 AI 架构提供了新范式。

</details>

---

### 3. [Toward an Integrated Cross-Urban Accident Prevention System: A Multi-Task Spatial-Temporal Learning Framework for Urban Safety Management](https://arxiv.org/abs/2601.05521)

**Authors**: Jiayu Fang, Zhiqi Shao, Haoning Xi, Boris Choy, Junbin Gao  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.05521v1  

#### Abstract
The development of a cross-city accident prevention system is particularly challenging due to the heterogeneity, inconsistent reporting, and inherently clustered, sparse, cyclical, and noisy nature of urban accident data. These intrinsic data properties, combined with fragmented governance and incom...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Toward an Integrated Cross-Urban Accident Prevention System: A Multi-Task Spatial-Temporal Learning Framework for Urban Safety Management

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

城市交通事故预防系统长期面临以下挑战：
- **跨城市异质性**（cross-city heterogeneity）：不同城市的基础设施、报告标准、执法强度和出行行为差异大，导致数据不兼容。
- **事故数据固有特性**：事故数据具有**空间聚集性**（spatially clustered）、**时间稀疏性**（temporally sparse）、**周期性**（cyclical）和**噪声多**（noisy/incomplete）的特点，使得建模困难。
- **治理碎片化**（fragmented governance）：各城市独立管理，缺乏统一框架支持跨区域协同决策。

这些问题阻碍了构建一个可扩展、鲁棒且可解释的**跨城市事故预防系统**（Cross-City Accident Prevention System）。

---

### **提出了什么新方法或新思路**

本文提出 **MLA-STNet**（Mamba Local-Attention Spatial-Temporal Network），一种统一的多任务时空学习框架，用于跨城市事故风险预测。

#### 核心模块设计：
1. **Spatio-Temporal Geographical Mamba-Attention (STG-MA)**  
   - 针对事故数据的**空间聚集、稀疏、周期性和噪声**问题。
   - 结合 **Mamba** 模型捕捉长程时间依赖，提升时序稳定性。
   - 引入 **局部掩码注意力机制**（Local Masked Attention），抑制低风险区域的不稳定波动，增强热点识别能力。

2. **Spatio-Temporal Semantic Mamba-Attention (STS-MA)**  
   - 解决**跨城市异质性**问题。
   - 构建城市特定的语义图（如道路拓扑、POI、气象、事故记录等）。
   - 采用**共享参数 + 独立语义表示**的设计：所有城市共享 Mamba 参数以提取共性模式，同时保留各自的城市语义空间，实现“协作优化”与“本地适配”的平衡。

---

### **相比现有方法的优势**

| 维度 | MLA-STNet 优势 |
|------|----------------|
| **模型架构** | 统一的多任务框架，支持跨城市联合训练，避免孤立建模 |
| **时空建模** | Mamba + 局部注意力结合，兼顾长时依赖与局部稳定 |
| **跨城泛化** | 共享参数 + 独立语义空间，有效缓解异质性问题 |
| **鲁棒性** | 在高达 50% 输入噪声下性能变化小于 1%，远优于基线 |
| **可扩展性** | 支持实时推理与多城市同步预测，适用于实际部署 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 城市 | 时间跨度 | 主要特征 |
|--------|------|----------|---------|
| **NYC Dataset** | 纽约市（New York City） | 2013年全年 | 包含 147k 起事故、173M 出租车订单、103k 道路段、POI 和气象数据 |
| **Chicago Dataset** | 芝加哥 | 2016年2月–9月 | 包含 44k 起事故、1.74M 出租车订单、56k 道路段、无 POI 数据 |

> 注：两城市在时间跨度、数据规模、特征完整性上存在显著差异，体现真实世界中的**跨城市异质性**。

---

### **实验设置与评估指标**

#### **实验场景**
- **全时段预测**（All-day forecasting）
- **高发期预测**（High-frequency accident periods）

#### **评估指标**
| 指标 | 描述 |
|------|------|
| **RMSE** | 预测风险值与真实值的整体偏差，越小越好 |
| **Recall (%)** | 正确识别的真实事故热点比例，越高越好 |
| **MAP**（Mean Average Precision） | 热点排序质量，反映模型对高风险区域的优先级判断能力 |

#### **基线方法对比**
- **GSNet**, **TWCCnet**, **ViT-Traffic**, **MG-STNet**
- 所有模型均在单任务（per-city）和多任务（multi-task）两种设定下进行比较

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Multi-task Setting）**

| 模型 | 城市 | RMSE ↓ | Recall (%) ↑ | MAP ↑ |
|------|------|--------|-------------|-------|
| **MLA-STNet** | NYC | **6.88** | **34.88%** | **0.196** |
| GSNet | NYC | 9.81 | 30.28% | 0.168 |
| ViT-Traffic | NYC | 9.23 | 29.74% | 0.167 |
| **MLA-STNet** | Chicago | **8.59** | **23.94%** | **0.104** |
| GSNet | Chicago | 12.65 | 18.75% | 0.076 |
| ViT-Traffic | Chicago | 11.42 | 17.21% | 0.075 |

> ✅ **相比 SOTA 基线，MLA-STNet 实现：**
> - 最多 **6% 更低的 RMSE**
> - 最多 **8% 更高的 Recall**
> - 超过 **5% 更高的 MAP**

---

### **与基线方法的对比结果**

- 在**单任务设置**中，MLA-STNet 已超越所有基线。
- 在**多任务设置**中，性能进一步提升，表明**参数共享 + 独立语义学习**策略有效促进跨城市知识迁移。
- 可视化显示：MLA-STNet 输出的风险地图更**集中、锐利、贴近真实热点**，而基线模型普遍存在响应扩散、边界模糊等问题（见 Fig. 8）。

---

### **消融实验结果（Ablation Study）**

| 移除组件 | NYC RMSE ↑ | NYC Recall ↓ | NYC MAP ↓ | 影响说明 |
|---------|------------|--------------|-----------|----------|
| W/o STG-MA | 7.00 → 7.10 | 34.88% → 33.72% | 0.1958 → 0.1863 | 空间噪声增加，时间稳定性下降 |
| W/o STS-MA | 6.88 → 7.05 | 34.88% → 34.31% | 0.1958 → 0.1952 | 跨城市语义适应能力减弱 |
| W/o STM（Mamba） | 6.88 → 7.10 | 33.82% | 0.1876 | 长程时间依赖受损 |
| W/o LMA（Local Attention） | 6.88 → 7.13 | 33.37% | 0.1802 | 局部热点定位精度下降 |

> 🔍 结论：**STG-MA 抑制噪声，STS-MA 提升跨城泛化，STM 增强时序稳定性，LMA 提升局部判别力**，四者协同作用显著。

---

## 4. 关键结论和发现

### **主要发现**

1. **MLA-STNet 是首个真正意义上的跨城市事故预防系统框架**，实现了多城市数据的统一建模与协同预测。
2. **多任务学习 + 共享参数设计** 显著提升了模型在异构城市间的泛化能力和预测稳定性。
3. **Mamba 与局部注意力的融合** 有效应对了事故数据的稀疏性与噪声问题，在保持计算效率的同时增强了长时依赖建模。
4. 模型在**高噪声环境**下表现极强鲁棒性：即使输入噪声达 50%，RMSE、Recall、MAP 变化均**小于 1%**（见 Fig. 16–17）。
5. 实验验证了该框架可用于**资源协调、应急预置、政策制定**等实际应用场景（见 Fig. 18）。

---

### **方法的局限性**

1. **极端稀疏区域仍可能欠/过平滑**：尽管 STG-MA 缓解了不平衡问题，但在信号极弱区域仍有误判风险。
2. **语义不平衡影响性能**：NYC 拥有更丰富的 POI 和更长观测期，而 Chicago 数据较贫瘠，限制了跨城对齐效果。
3. **相位错位问题**：不同城市高峰时段的时间偏移（phase misalignment）会影响联合时序建模，虽经相对时间对齐但仍存挑战。
4. **计算开销较高**：多视图相似性构建与自适应图学习带来额外负担，需工程优化以支持实时部署。

---

### **未来工作方向**

1. **动态因果图结构**：开发能随时间演化的自适应图神经网络，捕捉交通、社会与政策交互。
2. **联邦学习与隐私保护**：拓展至联邦学习框架，在不共享原始数据的前提下实现跨城市协作。
3. **实时推理管道集成**：构建端到端的流式处理系统，支持低延迟预警与决策支持。
4. **不确定性估计与可解释性增强**：引入校准的概率输出与归因分析，提升模型透明度与公信力。

---

> 🏁 **总结**：MLA-STNet 不仅是一项技术突破，更是迈向**智能、协同、可持续的城市安全治理体系**的重要一步。它为跨域交通安全管理提供了可扩展、鲁棒且可操作的新范式。

</details>

---

### 4. [Multi-Modal Style Transfer-based Prompt Tuning for Efficient Federated Domain Generalization](https://arxiv.org/abs/2601.05955)

**Authors**: Yuliang Chen, Xi Lin, Jun Wu, Xiangrui Cai, Qiaolun Zhang, Xichun Fan, Jiapeng Xu, Xiu Su  
**Category**: cs.DC  
**Published**: 2026-01-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.05955v1  

#### Abstract
Federated Domain Generalization (FDG) aims to collaboratively train a global model across distributed clients that can generalize well on unseen domains. However, existing FDG methods typically struggle with cross-client data heterogeneity and incur significant communication and computation overhead...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multi-Modal Style Transfer-based Prompt Tuning for Efficient Federated Domain Generalization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文聚焦于 **Federated Domain Generalization (FDG)** 中的关键挑战：
- **跨客户端数据异构性**（cross-client data heterogeneity）：不同客户端的数据分布差异大，导致模型泛化能力差。
- **通信与计算开销高**：传统方法在联邦学习中需要频繁传输大量模型参数，效率低下。
- **对未见域（unseen domains）适应能力弱**：现有方法难以有效应对训练时未出现的目标域。

此外，许多现有 FDG 方法依赖于共享领域相关特征或风格信息，存在**隐私泄露风险**。

---

### 提出的新方法：FaST-PT
作者提出了一种新的 FDG 框架 —— **FaST-PT**（Federated Multi-Modal Style Transfer with Prompt Tuning），其核心思想是结合 **CLIP 的图文对齐能力** 和 **轻量级 Prompt Tuning** 来实现高效、安全的领域泛化。

#### 主要创新点：
1. ✅ **Multi-Modal Style Transfer (MST)**  
   - 利用 CLIP 的文本监督，在嵌入空间进行图像嵌入的风格迁移。
   - 客户端利用外部领域的文本描述（如 `"a sketch of a teapot"`）训练一个轻量级变换网络 $ Q_{i \to j} $，将本地图像嵌入转换为其他域的“风格”。
   - 在不共享原始图像或敏感特征的前提下，扩展本地训练数据分布，缓解域偏移。

2. ✅ **Dual-Prompt Module（双提示模块）**
   - 将 Prompt 分解为：
     - **Global Prompt ($P_G$)**：聚合自各客户端增强后的嵌入，捕获通用知识。
     - **Domain Prompt ($P_D$)**：仅基于本地数据训练，保留领域特异性知识。
   - 实现了特征解耦（feature disentanglement），无需复杂正则化即可提升泛化性。

3. ✅ **Domain-aware Prompt Generation (DPG)**
   - 引入一个可学习的域分类器 $f(\cdot)$，预测输入样本的伪域标签。
   - 根据域相似度动态加权多个 Domain Prompts，生成针对目标域的知识融合 Prompt。
   - 显著增强了对 **unseen domain** 的适应能力。

4. ✅ **隐私友好 & 高效通信**
   - 仅需共享非敏感的文本描述和极小的 Prompt 参数（而非完整模型或风格特征）。
   - 大幅降低通信成本（见 Table 4）。

---

### 相比现有方法的优势
| 维度 | FaST-PT | 传统 FDG 方法（如 ELCFS, CCST） |
|------|--------|-------------------------------|
| **隐私保护** | ⭐ 强（仅共享文本描述） | ❌ 弱（共享频谱/风格特征） |
| **通信效率** | ⭐ 极高（仅传 Prompt） | ❌ 高（传模型或中间特征） |
| **计算开销** | ⭐ 低（冻结主干 + 轻量模块） | ❌ 高（额外特征提取器） |
| **泛化性能** | ⭐ SOTA | ✅ 一般 |

> ✔️ FaST-PT 是首个将 **多模态风格迁移 + Prompt Tuning** 融合用于 FDG 的框架。

---

## 2. 核心实验方法和设置

### 使用的数据集
在四个主流跨域基准上进行了广泛实验：
- **PACS**：Photo, Art-painting, Cartoon, Sketch（4 domains）
- **VLCS**：VOC2007, LabelMe, Caltech101, SUN09（4 domains）
- **OfficeHome**：Art, Clipart, Product, Real-World（4 domains）
- **DomainNet**：Clipart, Infograph, Painting, Quickdraw, Real, Sketch（6 domains）

采用标准的 **leave-one-domain-out** 协议：每次留一个域作为 unseen target domain，其余作为 source domains 进行联邦训练。

---

### 实验设置
- **模型架构**：OpenAI CLIP-ViT-L/14（图像与文本编码器均冻结）
- **客户端数量**：K = 4（PACS/VLCS/OfficeHome），K = 6（DomainNet）
- **训练轮数**：R = 20
- **本地训练 epoch**：$E_c = E_L = 5$
- **Batch size**：16
- **Prompt length**：4
- **优化器**：
  - MST 阶段：Adam, lr=1e-3
  - Prompt 阶段：SGD, lr=0.005（prompt），lr=0.01（domain classifier）

所有方法使用相同 backbone 和超参以确保公平比较。

---

### 评估指标
- **平均准确率（Average Accuracy %）**：在所有 leave-one-out 设置下的平均表现。
- **单任务准确率**：每个 target domain 上的表现。
- **消融实验**：验证各组件有效性。
- **Few-shot 场景测试**：验证小样本下的鲁棒性。
- **通信与计算成本分析**：参数量（Parameters）、推理 FLOPs。

---

### 基线方法对比
分为三类：
1. **传统 FL 方法**：
   - FedAvg, FedProx
2. **经典 FDG 方法**：
   - ELCFS, FedSR, FedADG, FedDG-GA, CCST
3. **Prompt-based FL 方法（SOTA）**：
   - PromptFL, FedAPT, DiPrompt

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 1 & 2）

| 方法 | PACS (%) | VLCS (%) | OfficeHome (%) | DomainNet (%) |
|------|----------|----------|----------------|---------------|
| FedAvg | 78.48 | 75.79 | 85.30 | 72.22 |
| FedDG-GA | 84.25 | 77.47 | 90.11 | 80.17 |
| DiPrompt (SOTA) | 94.86 | 84.08 | 95.94 | 86.84 |
| **FaST-PT (Ours)** | **98.16** | **85.52** | **97.48** | **89.17** |

> 💡 **FaST-PT 在所有四个数据集上均达到 SOTA 性能**，显著超越最强基线 DiPrompt：
- **PACS ↑ +3.30%**
- **DomainNet ↑ +2.33%**

在总共 **18 个 domain generalization 任务中，FaST-PT 在 17 项上取得最优结果**，表现出极强的一致性和稳定性。

---

### 与基线方法的对比结果
- 相比传统 FL（FedAvg）：平均提升超过 **10%~17%**，说明 FDG 设计的有效性。
- 相比传统 FDG（如 FedDG-GA）：仍大幅提升，表明 **Prompt Tuning 更适合 CLIP 架构下的领域泛化**。
- 相比 Prompt-based FL（如 DiPrompt）：
  - 尽管 DiPrompt 已很强，FaST-PT 通过 MST 和 DPG 进一步提升了跨域适应能力。
  - 特别是在 **Sketch → Photo** 等极端风格迁移任务中优势明显（见 Figure 2 可视化）。

---

### 消融实验结果（Table 3）

| 变体 | 是否含 MST | 是否含 DPG | 是否含 $L_{con}$ | Avg (PACS) |
|------|------------|-------------|------------------|------------|
| ① Only $P_G$ | × | ✓ | × | 92.58 |
| ② Only $P_D$ | × | ✓ | × | 87.67 |
| ③ w/o Dual-Prompt | ✓ | × | × | 94.38 |
| ④ w/o MST | × | ✓ | ✓ | 97.06 |
| ⑤ w/o $L_{con}$ | ✓ | ✓ | × | 97.95 |
| ⑥ +Target Text | ✓ | ✓ | ✓ | 98.42 |
| **FaST-PT (Full)** | ✓ | ✓ | ✓ | **98.16** |

#### 消融结论：
- **Dual-Prompt 设计至关重要**：单独使用 $P_G$ 或 $P_D$ 性能下降明显（↓5~10%）。
- **MST 显著提升性能**：从 97.06 → 98.16（↑1.1%），证明风格迁移有效缓解域偏移。
- **Contrastive Loss $L_{con}$ 提升知识分离性**：去除后性能略降，说明它有助于区分全局与局部知识。
- **引入 Target 文本仅轻微提升**（+0.24%），说明 FaST-PT 不依赖真实目标域信息也能高效泛化。

---

### 其他重要实验结果

#### Few-shot Setting（Figure 4）
- 在 shots=1~32 的设定下，FaST-PT 在 PACS 和 DomainNet 上始终优于 PromptFL、FedAPT、DiPrompt。
- 表明其在 **低资源场景下依然具备强大泛化能力**。

#### 成本分析（Table 4）
| 方法 | Parameters (上传/轮) | FLOPs (推理) |
|------|------------------------|--------------|
| FedAvg | 305M | 1.296T |
| FedADG | 318M | 2.593T |
| DiPrompt | 0.012M | 1.739T |
| **FaST-PT** | **0.005M** | **1.431T** |

> ✅ FaST-PT 的通信量仅为 DiPrompt 的 **40%**，且推理计算开销最低之一。

---

## 4. 关键结论和发现

### 主要发现
1. 🔍 **基于 CLIP 的多模态风格迁移（MST）可在不泄露隐私的情况下实现有效的本地数据增强**。
2. 🧩 **Dual-Prompt + DPG 构成了高效的领域知识解耦与融合机制**，显著提升 unseen domain 适应能力。
3. 📉 **Prompt Tuning 是实现高效 FDG 的理想范式**：参数少、通信低、易于部署。
4. 🚀 **FaST-PT 在性能、效率、隐私三方面实现了全面领先**，是当前最先进（SOTA）的 FDG 方法之一。

---

### 方法的局限性
1. **依赖 CLIP 的预训练质量**：若下游任务与 CLIP 预训练语义差距较大，效果可能受限。
2. **文本描述的质量影响 MST 效果**：虽然假设文本是非敏感的，但实际中需人工设计或自动提取 domain-level 描述。
3. **未考虑极端 Non-IID 场景下的 prompt 冲突问题**：当客户端 domain 差异极大时，global prompt 学习可能不稳定。

---

### 未来工作方向
- 探索 **自动化生成 domain text description** 的方法，减少人工干预。
- 扩展到 **non-vision 任务**（如医疗文本、语音识别）中的 FDG。
- 结合 **personalized FL** 思路，进一步优化每个客户端的 domain prompt。
- 研究 **更高效的 prompt fusion 策略**（如 attention-based mixing）替代简单的 element-wise weighting。

---

> ✅ **总结一句话**：  
> FaST-PT 通过 **Multi-Modal Style Transfer + Dual-Prompt Tuning + Domain-aware Generation**，构建了一个高效、安全、高性能的 FDG 新范式，在四大 benchmark 上全面超越 SOTA，为基于 Foundation Model 的 Federated Learning 提供了重要实践路径。

</details>

---

### 5. [A Dual Pipeline Machine Learning Framework for Automated Multi Class Sleep Disorder Screening Using Hybrid Resampling and Ensemble Learning](https://arxiv.org/abs/2601.05814)

**Authors**: Md Sultanul Islam Ovi, Muhsina Tarannum Munfa, Miftahul Alam Adib, Syed Sabbir Hasan  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.05814v1  

#### Abstract
Accurate classification of sleep disorders, particularly insomnia and sleep apnea, is important for reducing long term health risks and improving patient quality of life. However, clinical sleep studies are resource intensive and are difficult to scale for population level screening. This paper pres...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究旨在解决**自动化多类睡眠障碍筛查中的两大挑战**：
- **数据异质性**：睡眠健康数据包含数值型与类别型特征，且存在复杂的非线性关系。
- **严重类别不平衡**：在真实世界数据中，“健康”样本远多于“失眠”和“睡眠呼吸暂停”患者，导致模型偏向多数类，降低对少数病患的检测敏感度。

### 提出的新方法与新思路
作者提出了一种**Dual Pipeline Machine Learning Framework（双管道机器学习框架）**，其核心创新在于并行处理两种不同类型的特征依赖关系：

1. **统计管道（Pipeline 1）**  
   - 聚焦**线性可分性**，采用 `Mutual Information` 进行特征选择，结合 `Linear Discriminant Analysis (LDA)` 实现降维。
   - 使用 `RobustScaler` 抗异常值归一化。

2. **包装器管道（Pipeline 2）**  
   - 针对**非线性特征交互**，利用 `Boruta` 特征选择算法识别所有相关特征，并通过 `Autoencoder` 学习低维非线性表示。
   - 使用 `MinMaxScaler` 归一化。

此外，引入 **SMOTETomek 混合重采样策略** 来同时：
- **过采样** 少数类（Insomnia 和 Sleep Apnea）
- **欠采样** 边界模糊样本（Tomek Links），以净化决策边界

### 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **架构设计** | 双管道结构系统性比较线性和非线性特征工程路径，避免单一处理方式的局限性 |
| **类别平衡** | SMOTETomek 同时进行生成与清洗，优于单纯的 SMOTE 或随机下采样 |
| **特征选择** | 结合过滤法（Mutual Information）与包装法（Boruta），兼顾效率与全面性 |
| **验证严谨性** | 引入 `Wilcoxon Signed Rank Test` 验证性能提升的统计显著性，增强可信度 |

---

## 2. 核心实验方法和设置

### 数据集
- **名称**：Sleep Health and Lifestyle Dataset
- **来源**：Kaggle 公开数据集（[链接](https://www.kaggle.com/datasets/uom190346a/sleep-health-and-lifestyle-dataset)）
- **样本量**：374 条记录
- **特征数**：13 个（8 数值 + 5 类别）
- **目标变量**：三分类睡眠障碍标签
  - `None`（健康）：219 例
  - `Sleep Apnea`：78 例
  - `Insomnia`：77 例
- **特点**：混合类型数据，存在明显类别不平衡

### 实验设置
- **数据划分**：80% 训练集，20% 测试集（分层抽样）
- **预处理步骤**：
  - 缺失值检查（无缺失）
  - `Person ID` 移除，`Blood Pressure` 分解为收缩压/舒张压
  - 类别变量编码（One-Hot / Label Encoding）
  - 构造 8 个交互特征（如 `Stress_sleep_interaction`, `Sleep_Heart_ratio` 等）
- **重采样**：在训练集上应用 `SMOTETomek` 平衡三类样本至约 170–175 例
- **交叉验证**：Stratified 8-Fold Cross Validation（适应小样本）

### 评估指标
| 指标 | 定义 | 临床意义 |
|------|------|-----------|
| **Accuracy** | 总体正确率 | 综合性能衡量 |
| **Precision** | 预测为阳性的样本中有多少是真的阳性 | 减少误诊 |
| **Recall (Sensitivity)** | 实际阳性中有多少被正确识别 | 减少漏诊（关键） |
| **F1-Score** | Precision 与 Recall 的调和平均 | 不平衡下的综合评价 |
| **Wilcoxon Signed Rank Test** | 非参数检验，判断性能差异是否显著 | 排除偶然性影响 |

### 基线方法对比
论文将所提框架与以下近期研究进行横向比较：
- Rahman et al. (2025)：Gradient Boosting → 97.33%
- Monowar et al. (2025)：Ensemble Model + SMOTE → 96.88%
- Hidayat et al. (2023)：Random Forest → 88%
- Alshammari et al. (2024)：ANN → 92.92%

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在两个管道中，多个模型达到 **98.667% 的最高准确率**：

#### Pipeline 1（统计管道）最佳表现：
| 模型 | Accuracy | F1 Score | Recall | Precision |
|------|---------|----------|--------|-----------|
| **KNN (MI + SMOTETomek)** | **98.667%** | 97.850% | 97.917% | 97.917% |
| XGBoost (MI + SMOTETomek) | 98.667% | 97.850% | 97.917% | 97.917% |
| LightGBM (MI + SMOTETomek) | 98.667% | 97.850% | 97.917% | 97.917% |

#### Pipeline 2（包装器管道）最佳表现：
| 模型 | Accuracy | F1 Score | Recall | Precision |
|------|---------|----------|--------|-----------|
| **Extra Trees (Boruta + SMOTETomek)** | **98.667%** | 97.840% | 97.778% | **98.039%** |

> ✅ 所有最优模型均使用了 **SMOTETomek** 和相应的高级特征选择机制。

### 与基线方法的对比结果
| 研究 | 方法 | Accuracy | 相对提升 |
|------|-------|----------|------------|
| 本研究 | Extra Trees + Dual Pipeline | **98.667%** | — |
| Rahman et al. (2025) | Gradient Boosting | 97.33% | ↑1.34% |
| Monowar et al. (2025) | Ensemble Model | 96.88% | ↑1.79% |
| Alshammari et al. (2024) | ANN | 92.92% | ↑5.75% |

👉 在相同数据集上实现了新的 state-of-the-art 表现。

### 消融实验结果（Ablation Study）

#### （1）SMOTETomek 的有效性
- 对 KNN 和 LightGBM 显著提升性能（↑~6–7%）
- 但对 Logistic Regression 效果不显著甚至略有下降
- ➤ 表明重采样更利于非参数/树模型捕捉局部边界

#### （2）Normalization 的影响
- KNN 和 MLP 对归一化极度敏感：
  - MLP 从 61.333% 提升至 96.000%
  - KNN 从 92.0% 提升至 94.667%
- Tree-based 模型（如 Random Forest）几乎不受影响
- ➤ 验证了归一化是距离型和神经网络模型的必要前提

#### （3）特征选择对比：Mutual Information vs Boruta
- **Mutual Information** 更适合 KNN 和 AdaBoost，能有效去除噪声维度
- **Boruta** 在 Extra Trees 上表现最佳，因其能保留“所有相关”而非仅“最小冗余”特征
- ➤ 包装法虽计算成本高，但在特定场景下更具优势

#### （4）降维效果：LDA vs Autoencoder
- **LDA** 在 KNN 和 RF 上保持高性能（96%），但损害 LR 和 GBDT
- **Autoencoder** 对树模型不利（性能下降），但有助于 MLP 和 KNN 去噪
- ➤ 降维并非总是有益；需匹配模型类型

---

## 4. 关键结论和发现

### 主要发现
1. **双管道设计显著提升诊断精度**  
   并行探索线性与非线性特征空间，能够互补地提取生理信息，优于单一流程。

2. **SMOTETomek 是缓解类别不平衡的关键**  
   单纯过采样易引入噪声，而 SMOTETomek 清洗边界后显著提高 Recall 和 F1。

3. **特征选择应与模型匹配**  
   - KNN 等距离模型受益于 Mutual Information 的噪声过滤
   - Extra Trees 等集成树模型配合 Boruta 可挖掘深层非线性关联

4. **性能提升具有统计显著性**  
   Wilcoxon 检验显示 p-value = 0.00391 << 0.05，确认改进不是随机波动所致。

5. **推理延迟低于 400 ms**  
   所有优化模型满足实时筛查要求，具备临床部署潜力。

### 方法的局限性
- **样本量较小**（n=374），尽管使用交叉验证仍可能存在泛化风险
- **数据为合成数据**（作者引用指出 Kaggle 数据为 synthetic），真实性有待验证
- **未考虑时间序列动态**：仅基于静态生活方式问卷，缺乏连续生理信号（如 EEG、PPG）
- **缺乏外部验证**：尚未在多中心真实临床数据上测试

### 未来工作方向
1. **扩展至真实多中心数据集**，验证模型在不同人群中的鲁棒性
2. **融合可穿戴设备的时间序列数据**（如 HRV、SpO₂），构建混合模态模型
3. **轻量化部署到移动端**，实现家庭级持续监测与早期预警
4. **加强公平性分析**，确保模型在性别、年龄、种族等亚群间无偏见
5. **开发可解释性模块**，辅助医生理解预测依据，增强临床信任

---

> 📌 **总结一句话**：本文提出的 Dual Pipeline 框架通过融合统计与包装式特征工程、结合 SMOTETomek 重采样，在 Sleep Health and Lifestyle 数据集上实现了 98.67% 的最高准确率，显著超越现有方法，并经统计检验验证其优越性，为低成本、高精度的自动化睡眠障碍初筛提供了可行方案。

</details>

---

### 6. [PRISMA: Reinforcement Learning Guided Two-Stage Policy Optimization in Multi-Agent Architecture for Open-Domain Multi-Hop Question Answering](https://arxiv.org/abs/2601.05465)

**Authors**: Yu Liu, Wenxiao Zhang, Cong Cao, Wenxuan Lu, Fangfang Yuan, Diandian Guo, Kun Peng, Qiang Sun, Kaiyan Zhang, Yanbing Liu, Jin B. Hong, Bowen Zhou, Zhiyuan Ma  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.05465v1  

#### Abstract
Answering real-world open-domain multi-hop questions over massive corpora is a critical challenge in Retrieval-Augmented Generation (RAG) systems. Recent research employs reinforcement learning (RL) to end-to-end optimize the retrieval-augmented reasoning process, directly enhancing its capacity to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PRISMA: Reinforcement Learning Guided Two-Stage Policy Optimization in Multi-Agent Architecture for Open-Domain Multi-Hop Question Answering

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**开放域多跳问答（open-domain multi-hop QA）**中的两个关键挑战：
- **检索崩溃（Retrieval Collapse）**：在大规模语料库上进行迭代检索时，若缺乏推理引导的规划，系统难以定位包含“桥接答案”（bridge answers）的中间证据，导致后续推理失败。
- **学习不稳定性（Learning Instability）**：端到端的轨迹训练存在跨推理链的信用分配弱、模块间错误定位差等问题，容易过拟合特定基准的启发式策略，影响泛化性和稳定性。

### 提出的新方法与创新思路
作者提出 **PRISMA**（Plan-Retrieve-Inspect-Solve-Memoize Architecture），一个基于强化学习（RL）指导的解耦多智能体框架，其核心创新包括：

#### （1）架构设计：协作式推理闭环
PRISMA 模仿人类研究者的工作流，构建了一个由五个智能体组成的闭环系统：
- **Planner**：将复杂问题分解为依赖感知的子问题，驱动细粒度检索。
- **Retriever**：执行三阶段级联检索（dense → hybrid → cross-encoder）以提高召回率。
- **Inspector**（双阶段）：
  - *Context Inspector*：检查子问题质量和检索结果充分性，触发重写或扩展检索；
  - *Reasoning Inspector*：验证求解器生成的答案是否基于证据，必要时重新调用 Solver 或 Retriever。
- **Solver**：基于检索到的证据进行有据推理并生成带引用的答案。
- **Memoizer**：缓存已解决的子问题路径，支持跨问题复用，提升效率与信用归因。

#### （2）训练策略：两阶段 GRPO + OARPO
采用 **Two-Stage Group Relative Policy Optimization (GRPO)** 来稳定训练过程：
- **Stage I：专家校准（Expert Calibration）**
  - 分别优化 Planner 和 Solver，使其成为规划与推理领域的“专家”。
  - 使用任务特定奖励函数独立训练，避免端到端训练中的信用分配混乱。
- **Stage II：残差审计对齐（Observation-Aware Residual Policy Optimization, OARPO）**
  - 固定 Stage I 的专家模型，训练一个轨迹条件化的 Inspector。
  - 利用高容量 Oracle Inspector 教师模型生成审计标签，在成功轨迹上训练 Inspector 学习检测和恢复残余错误。
  - 减少两种残差：
    - **trace-level (data) residual**：通过监督信号对齐教师行为；
    - **system-level (policy) residual**：在冻结专家基础上学习更优的审计与恢复动作。

### 相比现有方法的优势
| 维度 | PRISMA 的优势 |
|------|----------------|
| **架构层面** | 多智能体协同 + 双向反馈机制（inspect → revise），实现动态纠错，避免错误累积。 |
| **训练层面** | 解耦训练 + 两阶段优化，显著缓解信用分配难题，增强泛化能力。 |
| **性能表现** | 在多个基准上达到 SOTA，尤其在难样本（如 3-hop、out-of-distribution）上优势明显。 |
| **部署可行性** | 支持路径复用（memoization），提升推理效率，具备实际应用潜力。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
共评估 **10 个开放域 QA 基准**，分为两类：
- **In-Distribution（训练分布内）**
  - `HotpotQA`（90K）
  - `2WikiMHQA`（150K）
  - `MuSiQue`（25K）
- **Out-of-Distribution（分布外迁移测试）**
  - `Natural Questions (NQ)`
  - `Bamboogle`
  - `MultiHopRAG`
  - 领域特定构造数据集：`Chemistry`, `Food`, `Game`, `Geography`, `Music`（各 150 题）

所有实验均使用完整的 Wikipedia 语料库（DPR 数据集，21M passages）作为外部知识源。

### 实验设置与评估指标
| 类别 | 内容 |
|------|------|
| **模型基础** | 所有方法统一使用 `Qwen3-4B-Instruct` 作为 backbone；部分对比使用 API 模型（Gemini, GPT-5, DeepSeek-V3.2）。 |
| **检索系统** | 三级级联检索：
  1. Dense retrieval（BGE-M3 + FAISS）
  2. Hybrid reranking（dense + sparse）
  3. Cross-encoder 重排序（BGE-reranker-v2-m3） |
| **评估指标** | 
  - 主要指标：**Exact Match (EM)** 和 **F1**
  - 辅助指标：
    - Retrieval Recall（检索召回率）
    - Inspection Precision / Recall（审计准确率/召回率）
    - Latency（延迟时间）用于效率分析 |

### 基线方法对比
| 类型 | 对比方法 |
|------|---------|
| **无训练方法（Zero-shot）** | 
  - Closed-book（无检索）
  - Single-step RAG
  - Multi-step RAG（如 IRCoT）
| **训练后方法（Trained RAG Systems）** |
  - `Retrobust`（SFT）
  - `RAG-DDR`（RL）
  - `QAgent`（RL）
  - `TIRESRAG-R1`（RL） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

#### In-Distribution 性能（EM/F1）
| 方法 | MuSiQue | HotpotQA | 2WikiMHQA |
|------|--------|----------|-----------|
| **PRISMA (Qwen3-4B)** | **30.6 / 39.5** | **50.2 / 55.8** | **57.0 / 60.1** |
| TIRESRAG-R1+ (best baseline) | 19.4 / 30.0 | 41.0 / 54.2 | 52.8 / 59.6 |
| IRCoT (GPT-5-Medium) | 23.4 / 35.0 | 39.2 / 53.6 | 35.4 / 51.7 |

> ✅ PRISMA 在 MuSiQue 上超越最强基线 **+11.2 EM**，且优于所有 API 模型。

#### Out-of-Distribution 性能（代表性结果）
| 方法 | NaturalQ | ChemistryV | Geography | Music |
|------|----------|------------|-----------|-------|
| **PRISMA (Qwen3-4B)** | **38.6 / 53.7** | **75.3 / 78.5** | **77.1 / 66.0** | **75.3 / 78.5** |
| IRCoT (Gemini-2.5-Flash) | 29.8 / 47.4 | 85.3 / 88.0 | 85.3 / 88.5 | — |
| IRCoT (GPT-5-Medium) | 18.0 / 35.4 | 62.7 / 60.8 | 54.7 / 59.3 | — |

> ⚠️ 尽管某些 API 模型在特定领域（如 Chemistry）表现更强，但 PRISMA 展现出更好的**鲁棒性与跨域适应能力**。

### 与基线方法的对比结果
- PRISMA 是目前唯一在 **多个 in/out-of-distribution 基准上全面领先** 的训练型系统。
- 即使基于较小的 `Qwen3-4B` 模型，其性能也超过基于更大 API 模型（如 GPT-5-Medium）的 IRCoT。
- 特别是在 **复杂多跳问题（如 3-hop）** 上，PRISMA 显著优于其他方法（见 Table 9）。

### 消融实验结果（Table 2 & 其他附录）

#### 组件消融（MuSiQue dev）
| 配置 | EM | ΔEM |
|------|-----|------|
| 完整 PRISMA | 30.6 | — |
| 移除 Planner | 19.4 | -11.2 |
| 移除 Inspector | 11.2 | -19.4 |
| 移除 Context Inspector | 27.2 | -3.4 |
| 移除 Reasoning Inspector | 24.8 | -5.8 |

> 🔍 结论：**Planner 和 Inspector 至关重要**，尤其是 Reasoning Inspector 对最终性能影响最大。

#### 训练策略消融
| 替换方式 | EM | ΔEM |
|--------|-----|------|
| Planner → Base Model | 25.2 | -5.4 |
| Solver → Base Model | 22.4 | -8.2 |
| Inspector → Base Model | 26.2 | -4.4 |
| 所有 Agent → Base Model | 15.0 | -15.6 |

> 📌 表明：**各模块的专项训练至关重要**，特别是 Solver 的证据合成能力是瓶颈。

#### 推理效率分析（Figure 6）
- 启用 Memoizer 后，平均延迟从 **19.11s 降至 13.57s**（提速 29%）。
- 缓存命中率随问题数量增加而上升，后期可达 ~45%。
- 准确率仅下降 0.2 EM（25.9 → 25.7），可忽略不计。

---

## 4. 关键结论和发现

### 主要发现
1. **多智能体协作 + 反馈闭环** 能有效防止错误传播，显著提升多跳问答系统的可靠性。
2. **解耦式两阶段训练（GRPO + OARPO）** 成功解决了端到端 RL 中的信用分配问题，提升了训练稳定性与泛化能力。
3. **Inspector 模块是性能最关键组件**，其双重审计机制（context + reasoning）在 56.8% 的成功案例中发挥了决定性作用（Table 12）。
4. **即使使用小规模模型（Qwen3-4B）**，通过专业化训练也能在复杂任务上超越大型 API 模型，具有成本效益优势。

### 方法的局限性
1. **硬件资源需求高**：整个系统需要大量 GPU 内存（4×H100），限制了轻量化部署。
2. **训练复杂度高**：各模块奖励函数需精细调参，不当配置易导致策略错位。
3. **仍受限于语料覆盖范围**：当黄金文档缺失或歧义严重时（如 Table 5 失败案例），系统无法恢复。
4. **Planner 的歧义处理不足**：若初始子问题分解错误（如“主角”指代不清），后续 Inspector 很难纠正根本性错误。

### 未来工作方向
- 开发更紧凑的基础模型，降低部署门槛。
- 引入主动查询机制或外部工具调用，弥补语料缺失。
- 加强 Planner 的消歧能力，例如结合知识图谱或上下文感知提示。
- 探索完全去中心化的分布式多智能体架构，进一步提升可扩展性。

---

> 🔗 **代码开源地址**：[https://github.com/Ameame1/PRISMA](https://github.com/Ameame1/PRISMA)

</details>

---

### 7. [Performance-Portable Optimization and Analysis of Multiple Right-Hand Sides in a Lattice QCD Solver](https://arxiv.org/abs/2601.05816)

**Authors**: Shiting Long, Gustavo Ramirez-Hidalgo, Stepan Nassyr, Jose Jimenez-Merchan, Andreas Frommer, Dirk Pleiter  
**Category**: cs.DC  
**Published**: 2026-01-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.05816v1  

#### Abstract
Managing the high computational cost of iterative solvers for sparse linear systems is a known challenge in scientific computing. Moreover, scientific applications often face memory bandwidth constraints, making it critical to optimize data locality and enhance the efficiency of data transport. We e...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Performance-Portable Optimization and Analysis of Multiple Right-Hand Sides in a Lattice QCD Solver*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**格点量子色动力学**（Lattice QCD）中迭代求解器的高计算成本和内存带宽瓶颈问题展开研究。LQCD 应用通常是**内存受限**（memory-bound）的，其大部分执行时间消耗在求解稀疏线性系统上。传统实现存在以下挑战：
- 依赖老旧的 SIMD 指令（如 128-bit SSE），无法充分利用现代处理器的宽向量能力；
- 缺乏对多右端项**（multiple right-hand sides, rhs）** 的系统性性能分析；
- 在异构架构（x86 与 Arm）上的**性能可移植性差**。

### 提出的新方法与创新点
1. **引入 rhs blocking 到 DD-aAMG 求解器中**  
   - 在 **Wilson-Dirac 算子评估核函数** 和 **GMRES 求解器** 中实现了批量处理多个 rhs 的功能。
   - 支持是否启用 **odd-even preconditioning**，以评估算法优化与机器性能之间的权衡。

2. **设计面向 SIMD 优化的数据布局接口**
   - 引入两种数据布局：
     - **Layout 1**：列主序（column-major），兼容原有代码；
     - **Layout 2**：行主序（row-major），提升数据局部性和 SIMD 向量化效率。
   - 通过灵活接口支持运行时切换，兼顾兼容性与性能。

3. **首次探索 Arm 架构的 Scalable Matrix Extension (SME) 在 LQCD 中的应用潜力**
   - 针对算子中的矩阵乘法操作（如 $ A \times M = O $）实现基于 SME 指令的原型。
   - 设计并比较两种变体：**neg-A** 和 **neg-M**，分析其指令级开销。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **性能可移植性** | 重构代码支持自动向量化（auto-vectorization），摆脱对特定 SIMD 宽度（如 SSE）的依赖，在 x86 和 Arm 平台上均获得显著加速。 |
| **性能提升机制明确** | 不仅实现 rhs blocking，还提供详尽的性能建模与分析，揭示实际性能受制于内存带宽、读写比变化及编译器行为等复杂因素。 |
| **新技术前瞻性验证** | 对尚未商用部署的 SME 技术进行仿真评估，为未来硬件适配提供早期参考。 |

---

## 2. 核心实验方法和设置

### 使用的数据集（格点配置）
- **大尺度格点**：`128×64³` —— 典型用于大规模 LQCD 模拟；
- **小尺度格点**：`64×16³` —— 适用于单节点测试。

### 实验平台
| 平台 | 架构 | 处理器 | SIMD 宽度 | 内存带宽（STREAM Triad） |
|------|------|--------|-----------|--------------------------|
| **JUWELS** | x86 | 2×Intel Xeon Platinum 8168 | 512-bit AVX512 | 155 GB/s |
| **Ookami** | Arm | A64FX | 512-bit SVE | 619 GB/s |
| **HAICGU** | Arm | 2×Kunpeng 920 | 128-bit NEON | 218 GB/s |

> 注：SME 使用 **QEMU 指令级模拟器** 进行评估，未在真实硬件上运行。

### 评估指标
- **性能指标**：
  - 单节点吞吐量（GFLOPs）
  - 实测性能 vs. Roofline 模型预测上限
  - **Architectural Efficiency**：实测性能 / 理论带宽 × 算术强度
- **底层分析工具**：
  - **PAPI** 性能计数器（用于 JUWELS 和 Ookami 上的 SIMD 指令统计）
  - **GCC auto-vectorization** 分析生成的汇编代码差异
  - **自定义脚本** 统计 SME 指令数量并加权估算计算耗时

### 基线方法对比
- **原始版本**：未优化的 DD-aAMG，对应 `Layout 1, b=1`，无 rhs blocking；
- **优化版本**：
  - `Layout 1` with blocking (`b=2,4,8,16`)
  - `Layout 2` with blocking（重点优化目标）
- **SME 对比方案**：
  - BLIS 的 `ZGEMM` 实现
  - Auto-vectorized SVE 版本
  - 两种 SME 实现（neg-A, neg-M）

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### （1）Wilson-Dirac 算子核函数性能（multi-node）
| 平台 | 最佳加速比（vs. 原始） | 最优配置 | Architectural Efficiency |
|------|------------------------|---------|----------------------------|
| **JUWELS** | **~10%** | Layout 2, b=16 | 52–54% |
| **Ookami** | **~15%** | Layout 1, b=16 | 12–27%（远低于理论潜力） |
| **HAICGU** | **~24%** | Layout 1, b=16 | ~50% |

> 尽管 Ookami 具有最高内存带宽（619 GB/s），但实际利用率极低，表明其架构难以饱和带宽。

#### （2）Single-node 性能趋势
- **HAICGU 表现最好**：得益于更多核心和良好的缓存结构，即使 SIMD 较窄也能高效利用内存。
- **JUWELS 次之**：约达到 HAICGU 性能的 70%。
- **Ookami 未发挥优势**：尽管带宽高，但由于线程数少、缓存小，未能有效提升性能。

#### （3）SIMD 利用率分析（PAPI 数据）
- **JUWELS**：Layout 2 显著增加 512-bit AVX512 指令执行数量（尤其当 `b≥4`），解释其性能优势。
- **Ookami**：
  - Layout 1：仅使用 128-bit NEON 指令；
  - Layout 2：成功触发 SVE 向量化，但性能反而下降 → 推测 **SVE 指令延迟较高或资源竞争严重**。

#### （4）Batched GMRES 求解器性能
- 在 JUWELS 上最大提速 **7%**（相比无 blocking）；
- 启用 **odd-even preconditioning** 后性能降至约 70%，但收敛迭代次数减半，总体仍更高效；
- **性能增长平缓**：因额外存储测试向量 $ V $ 和 Hessenberg 矩阵 $ H $ 导致算术强度降低。

#### （5）SME 仿真评估结果（blocking size b=8,16）
| 方法 | 相对于 SVE 的加速比（cost override OFF） | 相对于 BLIS 的加速比 |
|------|----------------------------------------|-----------------------|
| **SME neg-A** | **2.73×** | 12.2× |
| **SME neg-M** | 1.42× | 6.3× |
| **SME neg-A（with cost override）** | **2.75×** | 9.97× |

> 成本模型考虑了 A64FX 架构特性（load/store 端口比例、SME 指令吞吐限制），结果依然显示 SME 具备巨大潜力。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **rhs blocking 可带来性能提升**，但在实践中受限于多种因素：
   - 写密集度随 blocking size 增加而上升；
   - 实际有效内存带宽下降（尤其在 A64FX 上下降达 40%）；
   - Roofline 模型预测过于乐观，实际性能增长非线性甚至趋于饱和。

2. ⚠️ **编译器行为不可预测**：
   - GCC 自动生成的汇编代码随 blocking size 变化显著不同；
   - 包括循环展开策略、SIMD 指令选择、复数运算实现方式等；
   - 导致“理论算术强度”与“实际机器级算术强度”不一致。

3. 🔄 **数据布局影响深远**：
   - Layout 2 更利于 SIMD 向量化（尤其在 x86 上表现优异）；
   - 但在某些平台（如 Ookami）可能因 SVE 开销大而反不如 Layout 1；
   - **没有统一最优布局**，需结合平台特征调优。

4. 💡 **SME 展现出显著计算优势**：
   - 在仿真中，neg-A 版本能实现近 **2.75× 超越 SVE** 的速度；
   - 但前提是必须有足够高的内存带宽支撑数据供给；
   - 当前缺乏真实硬件验证，未来值得重点关注。

### 方法的局限性
- **未解决根本性的内存墙问题**：虽然提升了算术强度，但整体仍是 memory-bound。
- **SME 结果基于模拟器**：缺乏真实延迟、缓存行为、内存调度等细节，结果偏理想化。
- **dot product 等辅助操作成为瓶颈**：在 Layout 2 下需要额外数据重组，影响小 blocking size 时的性能。
- **缺乏多节点扩展性分析**：未报告弱/强扩展效率。

### 未来工作方向
1. **优化数据局部性**：修改循环结构以重用中间值，减少写流量；
2. **使用 intrinsics 手动优化关键路径**：绕过编译器不确定性，确保最佳 SIMD 利用；
3. **深入研究 SME 在真实 HPC 系统上的表现**：一旦硬件可用立即开展实测；
4. **探索 block Krylov 子空间方法**：结合算法层面的信息共享，进一步减少迭代次数；
5. **建模编译器行为对性能的影响**：构建更准确的性能预测模型。

--- 

> 🔗 **补充说明**：本文所有源码、脚本和数据已公开发布于 Zenodo ([DOI:10.5281/zenodo.17495916](https://doi.org/10.5281/zenodo.17495916))，具备完整可重复性。

</details>

---

### 8. [Dual-Phase LLM Reasoning: Self-Evolved Mathematical Frameworks](https://arxiv.org/abs/2601.05616)

**Authors**: ShaoZhen Liu, Xinting Huang, Houwen Peng, Xin Chen, Xinyang Song, Qi Li, Zhenan Sun  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.05616v1  

#### Abstract
In recent years, large language models (LLMs) have demonstrated significant potential in complex reasoning tasks like mathematical problem-solving. However, existing research predominantly relies on reinforcement learning (RL) frameworks while overlooking supervised fine-tuning (SFT) methods. This p...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Dual-Phase LLM Reasoning: Self-Evolved Mathematical Frameworks*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在数学推理等复杂任务中虽表现出潜力，但主流研究多依赖**强化学习（RL）框架**来提升长链推理能力，而忽视了**监督微调（SFT）** 的潜力。此外，现有方法存在以下问题：
- 依赖大模型（如 GPT-4）蒸馏生成 Chain-of-Thought（CoT）数据，受限于教师模型的能力；
- 难题采样不足，导致模型对困难问题优化不足；
- 缺乏系统性地整合验证（verification）、回溯（backtracking）、子目标分解（subgoal decomposition）和逆向推理（backward reasoning）等关键推理能力。

### 提出的新方法与思路
本文提出一种**双阶段自演进训练框架（Dual-Phase Training Framework）**，完全基于 SFT，仅使用模型自身生成的数据进行迭代优化，无需外部标注或大模型蒸馏。

#### 核心创新点：
1. **多轮对话策略生成长 CoT 数据（Multi-turn Dialogue Strategy）**
   - 引导模型通过多轮自我反思、评估与修正，生成包含错误检测、回溯、子目标设定和逆向推理的长推理链。
   - 示例见 Figure 2：模型先输出错误答案“4”，经自我分析后修正为正确答案“47”。

2. **两阶段训练框架**
   - **第一阶段（Stage 1）：长 CoT 构建与 SFT 微调**
     - 利用多轮对话生成候选推理轨迹；
     - 设计规则过滤高质量样本（如 `False-to-True` 类型），构建用于 SFT 的高质量数据集 $D_{\text{multi}}$。
   - **第二阶段（Stage 2）：难度感知拒绝采样（Difficulty-Aware Rejection Sampling）**
     - 对未解难题动态增加采样次数（如简单题采样 2 次，难题采样 100 次），确保难问题有更多机会生成正确响应；
     - 收集高置信度解答形成 $D_{\text{rej}}$，并与 $D_{\text{multi}}$ 合并进一步微调。

3. **标记机制区分数据来源**
   - 在输入中添加特殊提示符（marker）：
     - `\n\nUsing the solution style from multi-turns data.`  
     - `\n\nUsing the solution style from rejection data.`
   - 帮助模型识别不同风格的推理模式，提升泛化能力。

### 相比现有方法的优势
| 维度 | 本方法优势 |
|------|-----------|
| **资源效率** | 不依赖大模型蒸馏，节省计算成本；纯 SFT 更易部署 |
| **推理能力激活** | 显式建模四种核心推理行为（verification, backtracking, subgoal, backward chaining） |
| **数据质量控制** | 自主生成 + 规则过滤 + 动态聚焦难题，避免偏倚 |
| **可扩展性** | 展示正向缩放律（positive scaling laws）：数据越多，性能越强且输出更简洁 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：来自 DeepScaleR 项目的约 15k 数学问题-答案对
  - 10k 用于第一阶段多轮 CoT 数据合成；
  - 5k 专用于第二阶段拒绝采样。
- **测试基准（7个）**：
  - **AIME24**, **AMC23**：竞赛级数学题（高难度）
  - **GSM8K**, **SVAMP**：小学应用题
  - **MATH500**：涵盖代数、几何、数论等大学前数学
  - **TabMWP**：表格相关数学问题
  - **Gaokao2023en**：中国高考英文版数学题

### 实验设置与评估指标
- **基础模型**：Qwen2.5-7B-Instruct
- **评估方式**：
  - 所有任务采用 **out-of-domain (OOD)** 测试；
  - 对 AIME24 和 AMC23 使用 **8次采样取最优**，其余单次采样；
  - 报告 **准确率（Accuracy %）** 和 **平均响应长度（Tokens）**。
- **交互格式测试三种**：
  1. 直接求解
  2. 多轮对话 + 拒绝采样
  3. 带标记的多轮对话

### 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| **Qwen2.5-7B-Instruct** | 原始基础模型 |
| **+ self-rewarding** | Xiong et al. (2025a) 提出的自奖励方法 |
| **+ think twice** | Tian et al. (2025) 的多轮测试时思考方法 |
| **+ distilled** | 使用 DeepSeek-r1-32B 蒸馏的高质量 CoT 数据微调（视为性能上限） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1 & 2）

| 方法 | AIME24 (%) | MATH500 (%) | GSM8K (%) | 平均响应长度（Token） |
|------|------------|-------------|-----------|------------------------|
| Qwen2.5-7B-Instruct | 6.70 | 74.20 | 91.70 | ~600–1200 |
| + self-rewarding | 6.70 | 72.00 | 89.50 | ↑↑↑（最长达 2.6×） |
| + think twice | 10.00 | 73.40 | 90.10 | 接近 baseline |
| **Ours (Dmulti)** | **16.70** | **75.40** | **90.60** | ↑↑↑（4–14× baseline） |
| **Ours (Dmulti+rej)** | 13.30 | **76.00** | 91.10 | ↓↓（显著压缩） |
| **+ rejection marker** | **16.70** | **76.60** | **91.70** | 中等长度 |
| **+ distilled** | **26.70** | **84.20** | **93.00** | 极长（最高达 19k tokens） |

> 注：所有数值可能存在 ±0.1% 四舍五入误差。

### 与基线方法的对比结果
- 在 **AIME24** 上，`Dmulti` 较 baseline 提升 **149%**（6.70% → 16.70%），远超 `think twice`（10.00%）；
- 在 **MATH500** 上，`Dmulti+rej` 达到 76.00%，优于 `self-rewarding` 和 `think twice`，接近蒸馏模型（84.20%）；
- 尽管蒸馏方法性能最强，但其响应长度是本文方法的 **5–7倍以上**，实用性低；
- 本文方法以 **16–24% 的响应长度开销**，实现了蒸馏模型 **75–91% 的准确率**，性价比极高。

### 消融实验结果（Table 3）

| 方法 | AIME24 (%) | AMC23 (%) | MATH500 (%) |
|------|------------|-----------|-------------|
| Tsft (Dmulti) | 16.70 | 52.50 | 75.40 |
| Tsft + Drej（顺序微调） | 10.00 | 47.50 | 76.40 |
| T + Drej（仅拒绝采样） | 10.00 | 42.50 | 74.40 |
| Dmulti+rej（无 marker） | 10.00 | 45.50 | 73.00 |
| **Dmulti+rej（带 marker）** | **13.30–16.70** | **50.00–57.50** | **76.00–76.60** |

#### 发现：
- **联合训练优于顺序微调**：直接在合并数据上微调比先训 Dmulti 再训 Drej 效果更好；
- **marker 至关重要**：引入标记使 AIME24 准确率从 10.00% 提升至 16.70%（+67%）；
- **multi-turn marker 提升交互性能**：在 AMC23 上达到 57.50%，适合需要多步交互的任务；
- **rejection marker 提升形式化证明能力**：在 MATH500 上达 76.60%，擅长符号推理。

### 缩放实验（Scaling Behavior，Table 4 & 5）
随着训练数据量从 0.5k 增加到 8k：
- **准确率持续上升**：
  - GSM8K：91.30% → 92.30%
  - MATH500：75.00% → 76.00%
- **响应长度呈现“倒U型”后收敛**：
  - 初始阶段（1k 数据）出现过度展开（>11k tokens）；
  - 数据充足后趋于稳定（~900–2000 tokens），说明模型学会更高效表达。

✅ 表明该方法具有良好的 **正向缩放特性（positive scaling laws）**：更多数据带来更高精度和更优效率。

---

## 4. 关键结论和发现

### 主要发现
1. **SFT 可有效激活 LLM 的内在推理能力**，无需依赖 RL 或大模型蒸馏；
2. **多轮自我纠正机制能自然融合四大推理习惯**（verification, backtracking, subgoal, backward chaining），显著提升复杂问题解决能力；
3. **难度感知拒绝采样缓解了传统方法对简单问题的偏好**，使模型更均衡地掌握难易问题；
4. **标记引导的混合数据训练策略** 是关键设计，使得不同推理风格得以共存并互补；
5. 方法展现出良好缩放性：**数据越多，性能越好，同时输出更简洁**。

### 方法的局限性
- **依赖人工设计模板**：多轮对话流程需预设指令结构，缺乏自动化；
- **两轮限制难以应对深度推理**：对于 AIME24 这类极深推理任务仍显不足；
- **未集成符号引擎**：缺乏形式化验证支持，可能影响严格数学证明可靠性。

### 未来工作方向
- 开发基于 **meta-learning 的自动模板生成机制**；
- 集成 **symbolic reasoning engine** 以增强逻辑严密性；
- 设计 **可解释的难度度量指标**，实现更智能的自适应采样；
- 探索将此框架推广至代码生成、科学推理等领域。

---

> ✅ **总结一句话**：  
> 本文提出了一种基于 SFT 的双阶段自演进框架，利用模型自身生成的长 CoT 数据和难度感知拒绝采样，在不依赖大模型蒸馏的情况下显著提升了 LLM 的数学推理能力，兼具高性能、高效率与良好可扩展性。

</details>

---

### 9. [FLRQ: Faster LLM Quantization with Flexible Low-Rank Matrix Sketching](https://arxiv.org/abs/2601.05684)

**Authors**: Hongyaoxing Gul, Lijuan Hu, Shuzi Niu, Fangfang Liu  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.05684v1  

#### Abstract
Traditional post-training quantization (PTQ) is considered an effective approach to reduce model size and accelerate inference of large-scale language models (LLMs). However, existing low-rank PTQ methods require costly fine-tuning to determine a compromise rank for diverse data and layers in large ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# FLRQ: Faster LLM Quantization with Flexible Low-Rank Matrix Sketching 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Post-Training Quantization (PTQ)** 虽然能有效压缩大模型并加速推理，但在引入 **低秩补偿（Low-Rank Compensation）** 时存在两个关键瓶颈：
1. **固定秩（Fixed Rank）策略不灵活**：现有方法（如LoRC、LoftQ、LQER等）对所有层统一采用固定的低秩分解秩值，忽略了不同层权重矩阵的有效低秩特性差异，导致内存浪费或精度不足。
2. **计算开销高**：主流方法依赖 **SVD（Singular Value Decomposition）** 进行低秩近似，其时间复杂度高，严重拖慢量化过程，尤其在大规模模型上不可接受。

### 提出的新方法：FLRQ
本文提出 **Flexible Low-Rank Quantization (FLRQ)**，一种高效且自适应的低秩量化框架，包含两大核心组件：

#### （1）R1-FLR（Rank-1 Sketch-based Flexible Rank Selection）
- **思想**：摒弃全局SVD，改用基于 **R1-Sketch（Rank-1 Sketch）** 的快速低秩近似算法。
- **实现**：通过 **Gaussian投影** 和 **BLAS Level-2** 操作（主要是GEMV），逐层迭代提取最大奇异向量对应的秩-1分量。
- **优势**：无需预先指定秩，可动态判断每层最优秩，避免冗余计算。

#### （2）BLC（Best Low-rank Approximation under Clipping）
- **思想**：在量化过程中引入 **迭代优化机制**，交替更新低秩分量 $W_r$ 和量化残差 $W_q$，并通过 **Clipping** 策略抑制异常值影响。
- **流程**：
  1. 利用R1-FLR获取当前最佳低秩分量；
  2. 对剩余残差进行Clipping和量化；
  3. 迭代优化直至误差收敛。
- **优势**：显著降低量化误差，尤其在低比特（如INT2）场景下表现突出。

### 相比现有方法的优势
| 维度 | FLRQ | 传统方法（如LQER, SVD-Quant） |
|------|------|-----------------------------|
| **灵活性** | ✅ 层级自适应选择最优秩 | ❌ 固定秩，跨层不一致 |
| **效率** | ✅ R1-Sketch大幅加速低秩提取 | ❌ SVD计算昂贵 |
| **精度** | ✅ BLC迭代优化提升量化质量 | ❌ 单次分解，误差较大 |
| **内存开销** | ✅ 平均秩更低，额外比特更少 | ❌ 高固定秩带来高内存占用 |

---

## 2. 核心实验方法和设置

### 数据集
- **语言建模任务**：
  - **WikiText2**：用于评估 **Perplexity (PPL↓)**
  - **C4**：另一基准数据集，验证泛化能力
- **下游零样本任务**（Zero-Shot）：
  - **ARC-Challenge/Easy**, **BOOLQ**, **OpenBookQA**, **PIQA**, **Winogrande**

### 实验设置
- **模型系列**：OPT (1.3B, 6.7B, 13B), LLaMA-2 (7B, 13B), LLaMA-3 (8B)
- **量化配置**：W4A16, W3A16, W2A16（权重量化比特数/激活精度）
- **校准数据**：从WikiText2中随机选取128个2048长度的token片段
- **硬件平台**：NVIDIA A100 40GB GPU
- **超参数**：
  - R1-Sketch迭代次数 `it=2`
  - BLC迭代轮数：2-bit需约20轮，3/4-bit仅需1轮即收敛
  - 内存增长阈值 `x=0.2`

### 评估指标
- **主指标**：
  - **Perplexity (PPL↓)**：衡量语言模型生成质量
  - **Zero-Shot Accuracy (%)↑**：评估下游任务性能
- **效率指标**：
  - **Quantization Time**：量化耗时
  - **Inference Latency / Throughput**：推理延迟与吞吐量
  - **Extra Bits / Memory Overhead**：低秩分量带来的额外存储开销

### 基线方法对比
- **主流PTQ方法**：
  - **AWQ**, **OmniQuant**, **AffineQuant**
- **低秩PTQ方法**：
  - **LQER**, **SVD-Quant**
- **低秩微调方法**（Fine-tuning后接PTQ）：
  - **LoftQ**, **CALDERA**, **RILQ**

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）语言模型困惑度（PPL↓）—— 表2
| 模型 | 方法 | WikiText2 (W4) | C4 (W3) | C4 (W2) |
|------|------|---------------|--------|--------|
| OPT-1.3B | FLRQ | **14.65** | **16.07** | **22.99** |
| LLaMA2-13B | FLRQ | **4.94** | **5.16** | **6.77** |

> ✅ 在多数情况下优于或持平于SOTA PTQ方法（如OmniQuant, AWQ），尤其在 **W2A16** 下优势明显。

#### （2）与LQER对比 —— 表4
| 方法 | Avg Rank | Extra Bits | PPL (LLaMA2-7B, W3) | PPL (W2) |
|------|----------|------------|----------------------|---------|
| LQER | 32 | 0.21 | 6.23 | 10.33 |
| **FLRQ** | **36** | **0.23** | **5.88** | **9.14** |

> ✅ 尽管平均秩更高，但FLRQ在 **更低PPL** 下实现了更好的压缩-精度平衡。

#### （3）量化速度对比 —— 表8
| 方法 | OPT-1.3B (W3) | LLaMA2-13B (W2) |
|------|----------------|------------------|
| LQER (SVD) | 8.2m | 1.2h |
| **FLRQ (R1-Sketch)** | **6.2m** | **2.0h** |
| AffineQuant | 3.2h | 23.1h |
| **FLRQ** | **33.2m** | **3.8h** |

> ✅ FLRQ在 **W3/W4** 下比LQER快30%以上；在 **W2** 下比AffineQuant快 **5倍以上**。

#### （4）消融实验结果

##### Ablation on R1-FLR —— 表9
| 方法 | Avg Bit | PPL (LLaMA2-13B) |
|------|---------|------------------|
| Fixed Rank=64 | 4.44 | 4.98 |
| **FLRQ (no BLC)** | **4.24** | **4.98** |

> ✅ R1-FLR可在相同PPL下减少 **40%额外内存开销**。

##### Ablation on BLC —— 表10
| Bit | BLC | PPL (LLaMA2-13B) |
|-----|-----|------------------|
| 2 | × | 1.2e6 |
| 2 | √ | **6.77** |

> ✅ BLC在 **2-bit量化中避免了灾难性失真**，将PPL从百万级降至合理水平。

---

## 4. 关键结论和发现

### 主要发现
1. **层级间最优秩差异显著**：不同层的权重矩阵具有不同的低秩特性，统一固定秩会导致资源浪费或精度损失。
2. **R1-Sketch高效且准确**：相比SVD，R1-Sketch在保持近似精度的同时，将低秩提取速度提升数倍（见图6，最高达 **4.4×加速**）。
3. **BLC迭代机制至关重要**：特别是在 **2-bit量化** 场景下，BLC能有效压缩残差空间，防止量化误差累积。
4. **FLRQ具备良好扩展性**：随着模型规模增大，FLRQ的额外比特开销趋于稳定（约0.12–0.24 bits），表现出优秀的 **scaling law** 特性。

### 方法的局限性
- **依赖校准数据分布**：与大多数PTQ方法一样，性能受校准数据代表性影响。
- **GPU融合内核定制需求**：为实现低推理延迟，需对低秩部分进行算子融合优化（文中提到已集成至AutoGPTQ）。
- **极端低秩可能失效**：当某层本身接近低秩时，R1-FLR的absmax下降曲线可能不敏感，影响自动终止判断。

### 未来工作方向
- 探索 **R1-Sketch在其他矩阵分解任务中的应用**（如LoRA初始化）。
- 结合 **fine-tuning-free knowledge distillation** 进一步提升低比特精度。
- 扩展至 **vision-language models** 和 **MoE架构** 的量化支持。
- 研究 **动态bit-width分配** 与灵活秩选择的联合优化策略。

---

> **总结**：FLRQ通过 **R1-Sketch** 和 **BLC** 两大创新，在保证甚至超越SOTA量化精度的前提下，实现了 **更快的量化速度** 和 **更低的内存开销**，尤其在极具挑战性的 **2-bit量化** 场景中展现出强大鲁棒性，是迈向高效、实用化LLM部署的重要进展。

</details>

---

### 10. [Mathematical Knowledge Graph-Driven Framework for Equation-Based Predictive and Reliable Additive Manufacturing](https://arxiv.org/abs/2601.05298)

**Authors**: Yeongbin Cha, Namjung Kim  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.05298v1  

#### Abstract
Additive manufacturing (AM) relies critically on understanding and extrapolating process-property relationships; however, existing data-driven approaches remain limited by fragmented knowledge representations and unreliable extrapolation under sparse data conditions. In this study, we propose an ont...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Mathematical Knowledge Graph-Driven Framework for Equation-Based Predictive and Reliable Additive Manufacturing*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**增材制造（Additive Manufacturing, AM）领域中数据驱动模型在稀疏数据条件下外推能力不可靠、知识表示碎片化以及大语言模型（LLM）存在“幻觉”（hallucination）等问题**。传统LLM虽然能从文献中提取知识，但其生成结果缺乏物理一致性，且难以保证外推预测的可靠性。

### 提出的新方法与创新思路
作者提出了一种**本体引导、以方程为中心的知识图谱驱动框架**，将大型语言模型（LLM）与增材制造数学知识图谱（AM-MKG）紧密结合，实现可靠的知识提取与可解释的外推建模。主要创新点包括：

- **构建了基于本体的AM-MKG（Additive Manufacturing Mathematical Knowledge Graph）**  
  显式编码方程（Equation）、变量（Variable）、假设（Assumption）、过程参数（Process Parameter）及其语义关系，形成机器可读的结构化知识表示。

- **采用“本体引导”的知识抽取流程（Ontology-Guided Knowledge Extraction）**  
  利用预定义的本体模式约束LLM的实体和关系抽取行为，显著减少幻觉并提升提取结果的结构一致性和准确性。

- **提出子图条件化的方程生成机制（Subgraph-Conditioned Equation Generation）**  
  在进行外推建模时，LLM生成候选方程的过程受到从AM-MKG中检索出的相关子图（subgraph）的引导，确保所生成方程具有物理意义的功能形式。

- **引入置信度感知的外推评估框架（Confidence-Aware Extrapolation Assessment）**  
  综合考虑**外推距离（extrapolation distance）**、**统计稳定性（statistical stability）** 和**知识图谱支持的物理一致性（physical consistency）**，构建统一的置信度评分体系，用于量化外推预测的可靠性。

### 相比现有方法的优势
| 方面 | 传统方法 | 本文方法 |
|------|--------|---------|
| 知识表示 | 非结构化文本或孤立数据点 | 结构化、语义丰富的数学知识图谱（MKG） |
| 外推可靠性 | 缺乏物理约束，易出现非物理解 | 受MKG子图引导，保持物理合理趋势（如饱和行为） |
| 可解释性 | 黑箱模型，推理路径不透明 | 显式展示方程、变量依赖、假设等逻辑链条 |
| 不确定性评估 | 仅基于统计不确定性 | 引入几何、统计与物理三重维度的综合置信评分 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **AM-MKG 构建数据源**：精选了 **20 篇关于光固化成型（vat photopolymerization）的学术论文**，这些论文包含明确的数学公式、输入输出变量定义及物理假设。
- **外推验证数据集**：采用 Kolibaba et al. (2024) 发布的**光固化工作曲线（working curve）实验数据集（Dataset_22）**，测量不同辐射能量（radiant exposure）下的固化深度（cure depth），用于训练与外推测试。

### 实验设置
- **知识图谱构建流程**：
  1. PDF → Markdown 转换（使用 Nougat 模型保留 LaTeX 公式）
  2. 文本分块（以方程为中心的 chunking 策略）
  3. LLM（GPT-4o mini）进行两阶段抽取：先实体后关系
  4. 后处理合并重复节点，验证图谱完整性
- **外推建模流程**：
  1. 用户查询 → 检索相关子图（MKG-RAG）
  2. 子图条件化 → LLM生成多个候选方程
  3. 符号解析 → 参数拟合（非线性最小二乘）
  4. 外推预测 + 置信度评分计算

### 评估指标
- **知识抽取质量**：使用 **Partial F1** 指标（考虑表达变体），对比“自由LLM抽取”与“本体引导抽取”
- **外推性能**：
  - $ R^2 $、RMSE（训练集内）
  - 外推区域的预测趋势合理性（可视化分析）
- **置信度评分组件**：
  - $ S_{\text{dist}} $：基于归一化外推距离的指数衰减函数
  - $ S_{\text{stat}} $：结合 $ R^2 $ 和 bootstrap 不确定性
  - $ S_{\text{phys}} $：基于假设数量与影响符号一致性
  - 最终总分：加权组合 $ C_{\text{total}} = \alpha S_{\text{dist}} + \beta S_{\text{stat}} + \gamma S_{\text{phys}} + \delta S_{\text{uncertainty}} $

### 基线方法对比
- **知识抽取阶段**：
  - Baseline: 自由提示（free LLM prompting）无本体约束
  - Proposed: 本体引导 + 验证机制
- **方程生成与外推阶段**：
  - Baseline: LLM直接生成方程（无MKG子图引导）
  - Proposed: MKG-RAG检索子图后条件化生成

---

## 3. 主要实验结果和性能指标

### 关键性能数据
#### （1）知识抽取效果（Figure 3）
| 方法 | Precision | Recall | **Partial F1** |
|------|----------|--------|----------------|
| Free LLM | ~0.45 | ~0.50 | **~0.47** |
| **Ontology-Guided LLM** | **~0.85** | **~0.80** | **~0.82** |

> ✅ 本体引导方法在所有指标上均显著优于自由抽取，Partial F1 提升超过 **70%**

#### （2）外推行为对比（Figure 4）
- **无MKG引导的LLM生成方程**：
  - 在训练范围内拟合良好（$ R^2 > 0.96 $）
  - 但在外推区出现严重发散或数值不稳定（如 $ R^2 = -37 $）
- **有MKG引导的LLM生成方程**：
  - 所有候选方程均呈现合理的**饱和趋势**（saturation behavior）
  - 外推预测稳定且符合Jacobs工作曲线理论预期

#### （3）典型生成方程示例（Figure 5）
在MKG子图包含Jacobs方程的前提下，LLM生成以下三种物理一致的形式：
1. 对数形式：$ Cd = K_1 \ln(E / K_2) $
2. 分式形式：$ Cd = K_1 E / (K_2 + E) $
3. 指数形式：$ Cd = K_1 (1 - \exp(-K_2 E)) $

> 尽管函数形式不同，但都表现出单调递增+渐近饱和特性，体现物理一致性。

#### （4）预测不确定性分析（Figure 6）
- 在训练区域内，各模型不确定性相近；
- 进入外推区域后，**Model 3** 出现非线性放大的不确定性增长，表明其结构不稳定；
- **Model 1** 表现出最平稳的不确定性扩展，与其更高的置信得分一致。

#### （5）置信度评分分解（Figure 7）
| 模型 | $ S_{\text{dist}} $ | $ S_{\text{stat}} $ | $ S_{\text{phys}} $ | **Total Score** |
|------|--------------------|---------------------|---------------------|------------------|
| LLM_gen1 | 0.844 | 0.830 | 0.653 | **0.815** |
| LLM_gen2 | 0.844 | 0.803 | 0.653 | **0.800** |
| LLM_gen3 | 0.844 | 0.653 | 0.653 | **0.653** |

> - $ S_{\text{dist}} $ 对所有模型相同（因外推距离一致）
> - $ S_{\text{phys}} $ 恒定（所有方程均满足物理约束）
> - 差异主要来自 $ S_{\text{stat}} $，反映拟合质量和预测稳定性

---

## 4. 关键结论和发现

### 主要发现
1. **本体引导显著提升知识抽取的质量与可靠性**  
   相比自由抽取，结构化本体约束有效抑制LLM幻觉，提高实体与关系提取的准确率与召回率。

2. **MKG子图引导使LLM生成物理一致的方程**  
   即便没有显式编程规则，通过提供上下文子图，LLM能够复现并衍生出符合物理规律的函数形式，避免非物理解。

3. **提出的置信度评分系统能有效区分外推可靠性**  
   综合外推距离、统计稳定性和物理一致性，可为不同候选模型提供可比较的风险评估依据。

4. **框架实现了从“黑箱预测”到“白箱推理”的转变**  
   整个流程支持追溯：从原始文献 → MKG → 检索子图 → 生成方程 → 外推预测 → 置信评分，具备高度可审计性。

### 方法的局限性
1. **应用范围有限**：当前评估集中于**光固化工艺中的单变量外推问题**，尚未扩展至多变量耦合系统或多物理场模型。
2. **通用性待验证**：仅基于20篇论文构建AM-MKG，泛化能力需在更多材料、工艺中验证。
3. **物理一致性评分未体现差异性**：由于所有方程均由MKG引导生成，$ S_{\text{phys}} $ 项恒定，未能发挥判别作用。
4. **依赖高质量文献输入**：若原始论文中方程描述模糊或缺失假设，会影响MKG完整性和后续推理。

### 未来工作方向
- 扩展至更广泛的AM工艺（如SLM、DED）和复杂多变量系统
- 开发动态更新机制，实现MKG的持续演化
- 探索多步方程链推理与微分方程自动发现
- 引入主动学习策略优化外推实验设计
- 构建跨工艺、跨材料的统一AM本体标准

--- 

> **总结**：本文提出了一种融合**LLM与数学知识图谱（MKG）** 的新型框架，在增材制造领域实现了**可解释、可信赖的方程级外推建模**。通过本体引导、子图条件化生成与多维置信评估，解决了传统数据驱动方法在外推场景下的可靠性瓶颈，为智能制造中的科学建模提供了新范式。

</details>

---

### 11. [Buffered AUC maximization for scoring systems via mixed-integer optimization](https://arxiv.org/abs/2601.05544)

**Authors**: Moe Shiina, Shunnosuke Ikeda, Yuichi Takano  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.05544v1  

#### Abstract
A scoring system is a linear classifier composed of a small number of explanatory variables, each assigned a small integer coefficient. This system is highly interpretable and allows predictions to be made with simple manual calculations without the need for a calculator. Several previous studies ha...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Buffered AUC maximization for scoring systems via mixed-integer optimization

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **传统评分系统构建方法未直接优化 AUC**：尽管 AUC 被广泛认为是二分类任务中比准确率更鲁棒、更具信息量的评价指标（尤其在类别不平衡场景下），但现有的基于 Mixed-Integer Optimization (MIO) 的评分系统建模方法大多以最小化误分类数或最小化 logistic loss 为目标，未能**直接最大化 AUC**。
- **缺乏对“问题数量”的控制机制**：许多方法限制的是变量个数（即 dummy 变量的数量），而非原始问题（questions）的数量，这在实际应用中不够直观且不便于解释。

### 🚀 提出的新方法与创新思路
- **提出首个直接最大化 buffered AUC (bAUC) 的 MIO 框架用于评分系统构建**：
  - 引入 **buffered AUC (bAUC)** 作为 AUC 的紧致凹下界（tightest concave lower bound），其具有良好的数学性质，可转化为线性或凸优化问题。
  - 将 bAUC 最大化目标嵌入到 MIO 模型中，并结合 **group sparsity constraint** 来显式控制所选“问题”数量（而非 dummy 变量数量）。
- **构建了一个等价的 Mixed-Integer Linear Optimization (MILO) 模型**：
  - 通过引入辅助变量将非光滑的目标函数（含 `[·]+` 函数）转化为线性形式，使得模型可以被标准 MIO 求解器（如 Gurobi）高效求解。
  - 继承了 RiskSLIM 的思想（小整数系数、高可解释性），但替换了目标函数为 bAUC。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **目标函数设计** | 首次实现对 bAUC 的直接优化，相比间接方法（如正则化 logistic 回归）更能提升排序性能（AUC）。 |
| **可解释性增强** | 使用 group sparsity 显式控制问题数量，使最终模型更贴近临床/业务场景中的“问卷设计”逻辑。 |
| **模型性能** | 在多个真实数据集上取得了优于 baseline 方法的测试 AUC 表现，尤其是在 surgery 和 adult 数据集上显著领先。 |
| **计算可行性** | 尽管是 MIO 模型，但求解时间通常在几分钟内，处于实际可用范围。 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
四个来自 UCI Machine Learning Repository 的公开二分类数据集：

| 数据集 | 任务描述 | 实例数 `n` | 正类比例 | 二元变量数 `p` | 问题组数 `q` |
|--------|----------|-----------|------------|----------------|---------------|
| **surgery** | 预测肺癌患者术后一年内是否死亡 | 470 | 14.9% | 34 | 16 |
| **mushroom** | 判断蘑菇是否有毒 | 8,124 | 46.1% | 68 | 21 |
| **bank** | 客户是否会订阅定期存款 | 45,211 | 11.3% | 46 | 16 |
| **adult** | 年收入是否超过 \$50K | 48,842 | 22.8% | 47 | 14 |

所有原始特征均被转换为 dummy variables（binary encoding）。

### ⚙️ 实验设置
- **训练/测试划分**：随机划分 80%/20%，重复 5 次取平均结果。
- **超参数配置**：
  - 回归系数上限：`M ∈ {1, 2}`
  - 最大问题数：`θ₀ ∈ {4, 6, 8}`
- **评估指标**：**测试集 AUC**（area under ROC curve），报告均值 ± 标准误。
- **求解器**：Gurobi 12.0.1；MILO 问题从训练集中采样 300 个样本以加速求解。

### 🆚 基线方法对比
共比较六种 baseline 方法，分为三类：

#### （1）MIO-based 方法
- **bAUC-Rounding**：无整数约束的 bAUC 最大化模型 + 后续舍入处理（rounding）
  
#### （2）正则化方法
- **L1-Regularization**：带 L1 正则化的 logistic 回归（Lasso）
- **Elastic-Net**：弹性网络（L1+L2），混合参数设为 0.5

#### （3）逐步回归方法
- **Forward-Selection**：前向选择，基于验证集 AUC 增益添加变量
- **Backward-Elimination**：后向剔除，保留最多 θ₀ 个问题组

> 所有 baseline 输出连续系数，统一采用 Ustun & Rudin (2019) 的 scaling + rounding 技术转为整数。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（测试 AUC）

| 方法 | surgery (avg) | mushroom (avg) | bank (avg) | adult (avg) |
|------|----------------|------------------|-------------|--------------|
| **bAUC-Integer (本文)** | **0.619** | **0.987** | 0.634 | **0.856** |
| bAUC-Rounding | 0.616 | 0.982 | 0.618 | 0.840 |
| L1-Regularization | 0.594 | 0.974 | 0.675 | 0.835 |
| Elastic-Net | 0.553 | 0.927 | 0.633 | 0.822 |
| Forward-Selection | 0.557 | 0.976 | **0.686** | 0.833 |
| Backward-Elimination | 0.594 | 0.956 | 0.683 | 0.837 |

> ✅ **加粗表示该列最优**

#### 结果分析：
- 在 **surgery**, **mushroom**, 和 **adult** 三个数据集上，**bAUC-Integer 方法取得最高平均 AUC**。
- 在 **mushroom** 上表现尤为突出：即使仅用 4 个问题（`θ₀=4`, `M=1`），也能达到 **AUC=0.985**，说明其在简单任务中仍能保持极高精度。
- 在 **bank** 数据集上表现较差，推测原因：
  - 数据规模大（~45K）、类别高度不平衡；
  - MILO 方法仅使用 300 个采样实例训练 → 信息损失严重，影响泛化能力。

### 🔁 与其他方法的对比结果
- **vs bAUC-Rounding**：
  - 整数约束版本（bAUC-Integer）在 surgery 和 adult 上全面优于连续松弛后舍入的方法，表明**端到端整数优化更有利**。
- **vs 正则化与逐步法**：
  - 在 surgery 和 adult 上明显胜出；
  - 在 bank 上落后于 Forward-Selection 和 Backward-Elimination，反映其对大规模数据适应性不足。

### 🔍 消融实验与额外分析（隐含）
虽然没有明确命名“消融实验”，但以下分析体现了关键组件的作用：
- **group sparsity 的有效性**：生成的 score table 清晰地按“问题”组织响应项（见 Table 5），符合实际使用需求。
- **bAUC 作为代理目标的有效性**：在多数数据集上优于基于 logistic loss 的方法，验证了直接优化排序性能的重要性。
- **整数系数的影响**：bAUC-Integer 比 bAUC-Rounding 更稳定（标准误更低），特别是在 `M=1` 设置下。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **首次成功将 bAUC 最大化应用于评分系统的 MIO 构建框架中**，实现了对 AUC 的有效逼近与优化。
2. 所提 MILO 模型能够在合理时间内（一般 < 2 分钟）求解，适用于离线开发评分卡等实际场景。
3. 在三个 out of four real-world datasets 上，该方法生成的评分系统具有**更高的测试 AUC 和更低的方差**，表现出更强的判别能力和稳定性。
4. 生成的 score tables 符合领域常识（如 surgery 中肺功能好对应负分、income 中教育水平高对应正分），证明模型具备良好的**可解释性和可信度**。

### ⚠️ 方法的局限性
- **计算复杂度较高**：虽在可接受范围内，但仍远慢于 L1/Elastic-Net 或逐步回归。
- **依赖数据采样**：为应对大数据集，采用了 subsampling（300 样本），可能导致性能下降（如 bank 数据集表现不佳）。
- **未直接最大化 AUC**：bAUC 是 AUC 的下界，虽理论最优，但仍为近似优化。
- **仅适用于 binary classification**，扩展至多分类需进一步研究。

### 🔮 未来工作方向（作者明确提出）
1. **开发高效算法以处理大规模数据**：
   - 当前通过 subsampling 缩短求解时间，未来希望能在全量数据上运行以提升鲁棒性。
2. **设计精确最大化 AUC 的解决方案**：
   - 探索针对原生 AUC（非 bAUC）的 exact optimization 方法，尽管已被证明是 NP-hard 问题。
3. **拓展至其他模型结构**：
   - 如将 bAUC 最大化思想推广至 decision trees、rule lists 等其他可解释模型。

---

## 总结
该论文提出了一个新颖且有效的 **MIO-based 框架（bAUC-Integer）**，用于构建高性能、高可解释性的评分系统。它通过**最大化 buffered AUC + group sparsity constraint + 整数系数约束**，实现了在多个真实世界数据集上的优越 AUC 表现。尽管存在计算开销和数据规模限制，但在需要人工决策支持的关键领域（如医疗、金融、司法）具有重要应用价值。

</details>

---

### 12. [Prophet as a Repro ducible Forecasting Framework: A Methodological Guide for Business and Financial Analytics](https://arxiv.org/abs/2601.05929)

**Authors**: Sidney Shapiro, Burhanuddin Panvelwala  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.05929v1  

#### Abstract
Reproducibility remains a persistent challenge in forecasting research and practice, particularly in business and financial analytics where forecasts inform high-stakes decisions. Traditional forecasting methods, while theoretically interpretable, often require extensive manual tuning and are diffic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

**论文标题**: *Prophet as a Reproducible Forecasting Framework: A Methodological Guide for Business and Financial Analytics*

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
该论文旨在解决**商业和金融分析领域中预测研究的可复现性（reproducibility）挑战**。传统预测方法（如 ARIMA）依赖大量手动调参，而机器学习方法（如 Random Forest）则存在可解释性差、随机训练过程导致结果不稳定等问题。这些因素使得独立研究人员难以在不同环境中复现相同的结果，影响了预测模型的可信度、审计性和方法论严谨性。

### **提出了什么新方法或新思路**
论文并未提出新的算法，而是将 **Prophet** 定位为一个**促进可复现性的预测框架**，并系统地评估其在方法论上的优势。其核心思想是：
- 将 Prophet 视为一种**标准化、脚本化、透明化的分析构建块（methodological building block）**。
- 强调其**加法结构（additive structure）** 如何通过明确的组件分解（趋势、季节性、事件）来提升可解释性。
- 提出将**可复现性本身作为一个评估维度**，与预测准确性同等重要。

### **相比现有方法的优势**
- **可复现性（Reproducibility）**：通过标准化的工作流、显式的参数化和集成诊断工具，显著降低了因分析师主观判断或环境差异导致的结果变异。
- **可解释性（Interpretability）**：加法分解允许分析师独立检查每个组件（如趋势、节假日效应），便于向利益相关者沟通。
- **易用性与自动化**：自动处理变点检测（changepoint detection）、季节性特征构建等复杂步骤，减少了手动干预的需求。
- **开放性与集成性**：作为开源框架，易于与 Python 分析管道集成，并支持版本控制和模型序列化。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验使用了两个公开的、具有代表性的数据集：
1. **金融数据集（Financial Dataset）**：来自 Yahoo Finance 的特斯拉（Tesla Inc.）股票日收盘价，时间跨度为 2015 年至 2024 年（共 2,609 个观测值）。该数据集用于测试在高波动性、非平稳条件下的模型鲁棒性和可复现性。
2. **零售数据集（Retail Dataset）**：来自 UCI 机器学习仓库的“商店商品需求预测”数据集，时间跨度为 2013 年至 2017 年（共 1,826 个观测值）。该数据集表现出强烈的周季节性和年季节性，适合测试模型对周期性模式的捕捉能力。

### **实验设置和评估指标**
- **训练/测试划分**：采用**时间顺序划分**（chronological splitting），确保不使用未来数据进行训练。
  - 金融数据：训练集到 2023-12-31，测试集为 2024 年。
  - 零售数据：训练集到 2016-12-31，测试集为 2017 年。
- **预处理**：对目标变量（收盘价、销量）进行**对数变换（log transformation）** 以稳定方差。
- **计算环境**：使用 Python 3.11 和固定的 `random_seed=42`，确保计算可复现。关键库版本通过 `requirements.txt` 和 `environment.yml` 文件锁定。
- **评估指标**：
  - **RMSE**（均方根误差）
  - **MAE**（平均绝对误差）
  - **MAPE**（平均绝对百分比误差）
  - **Coverage 95%**（95% 预测区间的覆盖率，仅适用于提供不确定性估计的模型）

### **基线方法对比**
论文进行了多模型比较，以全面评估 Prophet 的表现：
- **ARIMA 变体**：
  - `ARIMA-Auto`：基于 AIC 准则自动选择阶数。
  - `ARIMA(1,1,1)` 和 `ARIMA(2,1,2)`：手动指定的常见非平稳时间序列模型。
  - `SARIMA(1,1,1)(1,1,1)7`：针对零售数据的季节性 ARIMA 模型。
- **Random Forest**：作为机器学习基线，使用了简单的特征工程（滞后值、滚动均值、日历指示器），并固定了 `n_estimators=100` 和 `random_state=42`。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### **表 1：金融数据集（Tesla 股票价格）性能对比**

| Model | RMSE | MAE | MAPE (%) | Coverage 95% |
| :--- | :--- | :--- | :--- | :--- |
| **Prophet** | 0.0980 | 0.0796 | 1.41 | 100.0 |
| ARIMA-Auto(3,1,3) | 0.0642 | 0.0510 | 0.90 | — |
| ARIMA(1,1,1) | 0.0641 | 0.0509 | 0.90 | — |
| ARIMA(2,1,2) | 0.0640 | 0.0508 | 0.90 | — |
| Random Forest | 0.0751 | 0.0612 | 1.09 | — |

#### **表 2：零售数据集（商店商品需求）性能对比**

| Model | RMSE | MAE | MAPE (%) | Coverage 95% |
| :--- | :--- | :--- | :--- | :--- |
| **Prophet** | **0.1211** | **0.0913** | **1.33** | 83.8 |
| ARIMA-Auto(2,1,3) | 0.2741 | 0.2252 | 3.25 | — |
| ARIMA(1,1,1) | 0.3427 | 0.2770 | 3.93 | — |
| ARIMA(2,1,2) | 0.3503 | 0.2809 | 4.11 | — |
| SARIMA(1,1,1)(1,1,1)7 | 0.3118 | 0.2411 | 3.55 | 95.3 |
| Random Forest | 0.2690 | 0.2307 | 3.29 | — |

### **与基线方法的对比结果**
- **在金融数据集上**：
  - Prophet 的**点预测精度（point accuracy）低于所有 ARIMA 变体和 Random Forest**（RMSE 更高）。
  - 但 Prophet 提供了**保守的不确定性估计**，其 95% 预测区间覆盖了 100% 的真实值，表明其在高波动环境下能提供可靠的置信边界。
- **在零售数据集上**：
  - Prophet 在**所有点预测指标上均显著优于所有基线模型**（RMSE 仅为 ARIMA 基线的约 1/3）。
  - 这得益于 Prophet 能够**自动且有效地捕捉强季节性模式**（周、年），而无需像 SARIMA 那样进行复杂的参数指定。
- **统计显著性检验**：
  - 使用 **Diebold-Mariano (DM) 检验**验证了性能差异的统计显著性。
  - 结果显示，在金融数据上，Prophet 显著劣于其他模型；在零售数据上，Prophet 显著优于所有基线模型（p < 0.01）。

### **消融实验结果**
论文虽未进行传统意义上的消融实验，但通过**比较多种 ARIMA 规范**，揭示了“**规范诱导的变异性（specification-induced variability）**”这一关键问题：
- 不同的 ARIMA 规范（自动选择 vs 手动指定 vs 季节性）会产生不同的结果，这凸显了传统时间序列建模中的可复现性挑战。
- Prophet 通过标准化流程减少了这种由分析师选择带来的变异性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Prophet 是一个强大的可复现性使能框架**：它通过标准化输入格式、显式参数化、集成诊断和模型序列化，显著提升了预测分析的可复现性、可审计性和方法论严谨性。
2. **性能表现取决于数据特性**：
   - 在**具有强季节性**的零售数据上，Prophet 表现出色，优于所有基线。
   - 在**高波动、非平稳**的金融数据上，其点预测精度不如精心调整的 ARIMA 模型，但提供了更稳健的不确定性量化。
3. **可复现性应与准确性并重**：论文主张，选择预测方法时，不应只看准确性，还应评估其可复现性。Prophet 在准确性和可复现性之间取得了良好的平衡。
4. **加法分解增强了可解释性**：Prophet 的组件分解图（trend, yearly, weekly, holidays）直观展示了各因素对预测的贡献，这对于业务决策至关重要。

### **方法的局限性**
- **计算复杂度**：对于超长序列（如分钟级数据），Stan 后端可能导致计算缓慢。
- **适用场景限制**：
  - 不适合处理**强乘法交互**或**快速结构性断裂**的数据。
  - 对**历史数据量有要求**，至少需要两年数据来可靠估计年季节性。
  - 主要针对**单变量时间序列**，多变量预测能力有限。
- **短期预测**：在极短视界（如一步预测）上，自回归模型可能更优。

### **未来工作方向**
1. **跨领域扩展**：在宏观经济、能源、医疗等领域评估 Prophet 的普适性。
2. **混合模型（Hybrid Approaches）**：探索将 Prophet 与机器学习模型结合，以兼顾可解释性和预测灵活性。
3. **增强不确定性量化**：研究使用全贝叶斯推断（MCMC）或共形预测（conformal prediction）来改进不确定性估计。
4. **教育与推广**：开发基于可执行代码的教学框架，推广可复现的研究实践。
5. **企业级集成**：研究如何将 Prophet 与 MLOps 工具链（如实验跟踪、模型注册表）集成，实现企业级的模型治理。

</details>

---

### 13. [Logic-Parametric Neuro-Symbolic NLI: Controlling Logical Formalisms for Verifiable LLM Reasoning](https://arxiv.org/abs/2601.05705)

**Authors**: Ali Farjami, Luca Redondi, Marco Valentino  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05705v1  

#### Abstract
Large language models (LLMs) and theorem provers (TPs) can be effectively combined for verifiable natural language inference (NLI). However, existing approaches rely on a fixed logical formalism, a feature that limits robustness and adaptability. We propose a logic-parametric framework for neuro-sym...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Logic-Parametric Neuro-Symbolic NLI: Controlling Logical Formalisms for Verifiable LLM Reasoning**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于大语言模型（LLMs）的自然语言推理（NLI）系统在需要逻辑有效性、可泛化性和可信度的任务（如法律、伦理、监管等）中表现不足。现有的神经符号（neuro-symbolic）架构通常将逻辑形式（如一阶逻辑 FOL）作为固定的背景层，缺乏灵活性和适应性，限制了系统的鲁棒性和推理能力。

### **提出的新方法与创新思路**
本文提出了一个**逻辑参数化（logic-parametric）神经符号 NLI 框架**，其核心思想是：
- 将**逻辑本身视为一个可调控的组件**，而非固定不变的底层结构。
- 利用 **LogiKEy 方法论**，将多种经典与非经典逻辑（如模态逻辑 KD、条件逻辑 DDLE、DDL_CJ）嵌入到高阶逻辑（HOL）中，实现统一的形式化与验证框架。
- 支持在不同逻辑之间进行系统性比较，以研究逻辑选择对推理质量、解释优化和证明行为的影响。

该框架包含四个关键阶段：
1. **Autoformalization**：LLM 将自然语言三元组（前提、假设、解释）映射为逻辑公式。
2. **Syntax & Consistency Check**：自动检查语法正确性和一致性。
3. **Theorem Proving**：使用 Isabelle/HOL 定理证明器进行形式化验证。
4. **Explanation Refinement**：若证明失败，提取失败步骤并反馈给 LLM 进行迭代优化。

### **相比现有方法的优势**
- **更高的灵活性与适应性**：支持多种逻辑形式，可根据任务领域动态选择最优逻辑。
- **更强的表达能力**：非经典逻辑（如 KD）能原生表达义务、许可等规范性概念，无需额外公理。
- **更高效的推理过程**：逻辑内部策略（logic-internal）比外部编码（logic-external）收敛更快、成本更低。
- **模块化设计**：通过 LogiKEy 实现逻辑即插即用，便于扩展和复用。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **BENR（Bioethical Explanations and Normative Reasoning）**：作者构建的新数据集，包含 103 个案例，聚焦于生物伦理中的规范性推理结构。
  - 覆盖多种推理模式：经典逻辑、常识推理、默认推理、模态推理、（生物）伦理冲突解决（如理由冲突、undercut、权衡等）。
  - 强调解释的内部结构，而非具体场景。

### **实验设置**
- **模型**：GPT-4o 和 DeepSeek-V1，在相同提示下进行公平比较。
- **逻辑形式**：评估四种逻辑：
  - **FOL**（一阶逻辑）
  - **KD**（模态逻辑，用于义务/许可）
  - **DDLE**（偏好语义的条件逻辑）
  - **DDL_CJ**（Carmo & Jones 双重道义逻辑）
- **评估流程**：最多执行 $ t=3 $ 次 refinement 迭代，直至生成有效解释或达到上限。

### **评估指标**
- **有效解释生成成功率（Success Rate）**
- **平均 refinement 迭代次数（Refinement Efficiency）**
- **平均求解时间（Solving Time）**
- **语法错误率（Syntactic Error Rate）**
- **领域特异性性能分析**

### **基线方法对比**
- 主要对比不同逻辑之间的表现差异，尤其是：
  - **逻辑外部方法（logic-external）**：如 FOL + 显式公理编码义务规则（如 $ Op \rightarrow Pp $）。
  - **逻辑内部方法（logic-internal）**：如 KD 内置 $ Op \rightarrow Pp $ 公理，无需额外添加。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 3）**

| Logic    | DeepSeek (%) | GPT-4o (%) |
|----------|--------------|-----------|
| **FOL**      | 42.72        | 39.81     |
| **KD**       | 76.70        | **77.67** |
| **DDLE**     | 60.19        | 51.46     |
| **DDL_CJ**   | 61.17        | 74.76     |

> ✅ **GPT-4o + KD 组合达到最高成功率 77.67%**

### **与基线方法的对比结果**

#### **(1) 成功率 vs. 逻辑类型**
- **KD 表现最佳**：显著优于 FOL，尤其在规范性推理任务中。
- **FOL 表现最差**：尽管语法鲁棒性强，但验证通过率低，说明其表达力不足以捕捉复杂规范关系。

#### **(2) Refinement 效率（见 Table 4）**

| Logic    | DeepSeek | GPT-4o |
|----------|----------|--------|
| **FOL**      | 0.91     | 0.83   |
| **KD**       | **0.43** | **0.60** |
| **DDLE**     | 0.60     | 0.64   |
| **DDL_CJ**   | 0.65     | 0.71   |

> ✅ **KD 所需 refinement 步骤最少**，收敛最快。

#### **(3) 计算效率（见 Table 5）**

| Logic    | DeepSeek (s) | GPT-4o (s) |
|----------|---------------|------------|
| **FOL**      | 108.99        | 103.74     |
| **KD**       | 65.25         | 58.83      |
| **DDLE**     | 69.45         | 65.34      |
| **DDL_CJ**   | 60.18         | **49.80**  |

> ✅ **DDL_CJ 和 KD 显著降低运行时间**，即使表达力更强也未增加开销。

#### **(4) 语法错误率（见 Table 6）**

| Logic    | DeepSeek (%) | GPT-4o (%) |
|----------|---------------|------------|
| **FOL**      | 3.90          | 2.90       |
| **KD**       | 7.80          | **1.90**   |
| **DDLE**     | 16.50         | 12.60      |
| **DDL_CJ**   | 19.40         | 5.80       |

> ⚠️ **GPT-4o-KD 组合语法错误率最低（<2%）**，显示其在模态逻辑下的稳定性。

### **消融实验与领域特异性分析（见 Figure 4 / Table 7）**

| 领域             | 最优逻辑         | 发现 |
|------------------|------------------|------|
| **Commonsense**  | **FOL**          | FOL 在描述性事实推理中表现最好 |
| **Modalities / (Bio)ethics** | **KD / DDL_CJ** | 模态与道义逻辑在规范性推理中显著优于 FOL |
| **Default Reasoning** | **KD**       | 能更好处理例外与条件义务 |

> 🔍 **核心发现：逻辑的有效性具有领域依赖性（domain-dependent）**

---

## **4. 关键结论和发现**

### **主要发现**
1. **逻辑应被视为一级设计参数**：不同的逻辑形式直接影响推理路径、解释质量和证明效率。
2. **逻辑内部策略（logic-internal）优于外部编码（logic-external）**：
   - 更少的 refinement 步骤
   - 更高的成功率
   - 更快的收敛速度
3. **领域适配至关重要**：
   - **FOL** 适合常识与描述性推理
   - **KD / DDL_CJ** 适合伦理、法律等规范性领域
4. **LogiKEy 框架支持灵活集成**：多种非经典逻辑可在 HOL 中统一操作，实现可验证的混合推理。

### **方法的局限性**
- **模型依赖性**：仅测试了 GPT-4o 和 DeepSeek-V1，小模型可能难以胜任多步形式化。
- **逻辑覆盖有限**：仅评估四种逻辑，其他形式（如概率逻辑、非单调逻辑）尚未探索。
- **refinement 策略单一**：未系统探索所有可能的修复路径，可能存在更优的人类一致策略。
- **输入简化**：使用结构化自然语言，真实世界复杂语境可能带来更大挑战。

### **未来工作方向**
- 构建**逻辑自适应系统**：根据输入动态选择或组合最优逻辑。
- 探索**多逻辑协同推理机制**：在单个任务中切换或融合不同逻辑。
- 扩展至更多**非经典逻辑体系**：如量化模态逻辑、论证框架、概率道义逻辑等。
- 提升 LLM 对复杂逻辑结构的理解能力，减少形式化误差。

---

> **总结**：本文开创性地将“逻辑”从静态背景提升为可调控的设计维度，展示了逻辑选择对 LLM 推理质量的决定性影响，为构建更鲁棒、模块化、可解释的神经符号系统奠定了基础。

</details>

---

### 14. [Enhancing Foundation Models in Transaction Understanding with LLM-based Sentence Embeddings](https://arxiv.org/abs/2601.05271)

**Authors**: Xiran Fan, Zhimeng Jiang, Chin-Chia Michael Yeh, Yuzhong Chen, Yingtong Dou, Menghai Pan, Yan Zheng  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05271v1  

#### Abstract
The ubiquity of payment networks generates vast transactional data encoding rich consumer and merchant behavioral patterns. Recent foundation models for transaction analysis process tabular data sequentially but rely on index-based representations for categorical merchant fields, causing substantial...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Enhancing Foundation Models in Transaction Understanding with LLM-based Sentence Embeddings*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于表格数据的交易理解模型（如 FATA-Trans、TREASURE）在处理**分类字段**（如商户名称、MCC、地理位置）时，通常采用**ID-based embedding**方式，即将“Costco”映射为一个离散索引并随机初始化其嵌入向量。这种做法导致严重的**语义信息丢失**，无法捕捉“Costco”作为会员制批发零售商的本质特征。

此外，虽然 Large Language Models (LLMs) 具备强大的语义理解能力，但直接将其用于实时金融系统存在**计算开销大、延迟高**等问题，难以部署。

### 提出的新方法与创新思路
本文提出一种**混合框架**（hybrid framework），将 LLM 的语义能力与轻量级基础模型的效率相结合，核心思想是：

> **利用 LLM 离线生成高质量的 sentence embeddings，作为基础模型中 categorical fields 的语义初始化，而非在线推理。**

具体创新点包括：

- ✅ **LLM-based Semantic Initialization**  
  使用 LLM 对 MCC、merchant name、location 等字段生成上下文化嵌入，并用这些嵌入**初始化**基础模型中的对应 embedding 层，从而让模型从训练初期就具备语义感知能力。

- ✅ **One-word Limitation Principle**（关键提示设计）  
  在 prompt 设计中引入“单字限制”原则，例如使用 `"This sentence: '[text]' means in one word:"` 来引导 LLM 输出简洁、聚焦且一致的语义表示，避免冗长或不一致响应带来的噪声。

- ✅ **Multi-source Data Fusion**  
  融合多源外部知识（如 ISO 官方 MCC 描述、商户行业标签、区域经济指标等）来丰富原始稀疏字段信息，提升嵌入质量。

- ✅ **Production-friendly 架构设计**  
  所有 LLM 推理均在**离线阶段完成**，生产环境仅运行轻量级 transformer 模型，保证低延迟和可扩展性，适用于大规模支付网络场景。

### 相比现有方法的优势
| 方法类型 | 是否保留语义 | 是否适合生产部署 | 是否支持冷启动 |
|--------|-------------|------------------|---------------|
| ID-based Embedding (Baseline) | ❌ | ✅ | ❌（冷启动差） |
| End-to-end LLM Inference | ✅ | ❌（延迟高） | ✅ |
| 本文方法（LLM-initialized） | ✅ | ✅ | ✅（通过语义泛化） |

- 在保持高效推理的同时显著增强语义理解；
- 改善对未见商户/类别的泛化能力；
- 可无缝集成到已有 foundation model 架构中。

---

## 2. 核心实验方法和设置

### 数据集
- 使用来自 Visa 的真实世界交易数据，共 **10亿条记录**，时间跨度为 **2022年1月至2023年12月**。
- 划分方式：
  - 训练集：前20个月
  - 验证集：第21个月
  - 测试集：最后3个月
- 包含字段：timestamp、amount、merchant name、MCC、location、abnormal labels 等。

### 实验设置
- **基础模型架构**：基于 TREASURE (Yeh et al., 2025)，一个面向高容量交易理解的 transformer-based 多任务 foundation model。
- **LLM 嵌入生成器**：
  - LLaMA2-7b / LLaMA2-13b
  - LLaMA3-8b
  - Mistral-7b
- **嵌入提取方式**：取最终隐藏层中最后一个非 padding token 的表示作为 sentence embedding。
- **初始化策略**：仅替换 merchant name、MCC 和 location 字段的初始 embedding，其余部分保持不变。

### 评估任务
| 任务 | 类型 | 目标 |
|------|------|------|
| Next Amount Prediction | 回归 | 预测下一笔交易金额 |
| Next MCC Prediction | 分类 | 预测下一笔交易的 MCC |
| Next Location Prediction | 分类 | 预测下一城市 |
| Next Merchant Prediction | 分类 | 预测下一个商户 |
| Transaction Metrics Assessment | 二分类 | 判断是否异常交易（内部指标） |

### 评估指标
- 回归任务：MAE、sMAPE
- 分类任务：Accuracy、F1-score
- 内部任务（Transaction Metrics Assessment）：Relative Improvement (RI)  
  $$
  \text{RI} = \frac{S_{\text{evaluated}} - S_{\text{baseline}}}{S_{\text{baseline}}} \times 100\%
  $$

### 基线方法对比
- **Vanilla Baseline**：原始 TREASURE 模型，所有 categorical fields 使用随机初始化 ID embedding。
- 对比不同 LLM + 不同字段组合的 embedding 初始化效果。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### 表1：交易预测任务性能对比（绿色为优于 baseline）
| Model | Emb. | Next Amount (MAE↓) | Next MCC (Acc↑) | Next City (Acc↑) | Next Merchant (Acc↑) |
|-------|------|--------------------|------------------|------------------|------------------------|
| Vanilla | – | 37.0430 | 0.4107 | 0.8454 | 0.0760 |
| Llama3-8b | All Fields | **36.8128** | **0.4155** | **0.8455** | **0.0979** |
| Mistral-7b | All Fields | **36.8761** | 0.4108 | 0.8434 | **0.0979** |

> ✅ 所有使用语义初始化的方法在 **Next Merchant Prediction** 上均有显著提升（最高 Acc 达 0.0991，相对提升约 30%）  
> ✅ **MCC 和 Merchant 预测任务改进最稳定**（82%~100%配置优于 baseline）  
> ✅ **Amount 预测受益于 location 和整体语义**，但 MCC 单独初始化反而略降

#### 表2：Transaction Metrics Assessment 的 Relative Improvement (RI)

| Model | MCC+Merchant | Location | All Fields |
|-------|---------------|----------|------------|
| Llama2-7b | -0.40% | +2.85% | **+3.72%** |
| Llama2-13b | **+2.66%** | +1.77% | +2.92% |
| Llama3-8b | +0.37% | +2.78% | +3.32% |
| Mistral-7b | +0.83% | **+2.89%** | **+3.93%** |

> ✅ 几乎所有配置都实现了正向 RI，说明该方法在实际业务指标上有效  
> ✅ **Mistral-7b + All Fields** 表现最佳，达到 **+3.93%** 的相对性能提升

### 消融实验分析（隐含在结果中）
- **单一字段 vs 多字段初始化**：
  - 单独 MCC 或 merchant 初始化能改善对应任务；
  - 综合初始化（All Fields）带来更全面的性能增益。
- **Location 初始化特别有助于 location prediction 和 transaction metrics assessment**，表明地理语境的重要性。
- **LLaMA3-8b 表现最稳健**，跨多种初始化策略均表现优异，显示其更适合此类结构化语义任务。

---

## 4. 关键结论和发现

### 主要发现
1. **语义初始化显著提升交易理解性能**，尤其是在细粒度分类任务（如 Next Merchant/MCC Prediction）上。
2. **LLM 不必在线推理也能赋能生产模型**：通过离线预计算 embedding，可在不牺牲效率的前提下获得语义优势。
3. **One-word limitation prompt 设计有效提升了输出一致性与嵌入稳定性**，减少了 LLM 输出噪声。
4. **多源数据融合增强了原始稀疏字段的信息密度**，使得 LLM 能生成更具判别性的 embedding。
5. **Mistral-7b 和 LLaMA3-8b 是较优选择**，尤其在综合字段初始化下表现突出。

### 方法的局限性
1. **Prompt Engineering 较简单**：当前的 one-word prompt 虽然有效，但仍属初级工程技巧；更复杂的 prompt tuning 或 instruction tuning 可能进一步提升效果。
2. **未覆盖最新专用 embedding 模型**：如 NV-Embed、Qwen3-embedding 等专为 embedding 优化的模型尚未纳入评估。
3. **静态 embedding 缺乏动态更新机制**：当前 embedding 是离线生成的一次性结果，无法反映商户季节性变化或市场趋势演变。
4. **适用字段有限**：目前仅应用于 MCC、merchant、location，其他类别字段（如 payment method、channel）尚未验证。

### 未来工作方向
- 探索 **domain-specific LLM fine-tuning** 以更好适应金融语境；
- 引入 **temporal-aware embedding 更新机制**，实现动态语义建模；
- 将本框架推广至更多 categorical fields 和下游任务（如反欺诈、用户画像）；
- 研究 **embedding compression 与量化技术**，进一步降低存储与加载成本；
- 结合 retrieval-augmented 方法实现 context-aware 实时增强。

---

> 📌 **总结一句话**：  
> 本文提出了一种实用且高效的“**离线 LLM 嵌入 + 在线轻量模型**”范式，在不增加线上延迟的前提下，显著提升了交易理解模型的语义能力和预测性能，为金融领域的大规模 foundation model 应用提供了新路径。

</details>

---

### 15. [MemBuilder: Reinforcing LLMs for Long-Term Memory Construction via Attributed Dense Rewards](https://arxiv.org/abs/2601.05488)

**Authors**: Zhiyu Shen, Ziming Wu, Fuming Lai, Shaobing Lian, Yanghui Rao  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05488v1  

#### Abstract
Maintaining consistency in long-term dialogues remains a fundamental challenge for LLMs, as standard retrieval mechanisms often fail to capture the temporal evolution of historical states. While memory-augmented frameworks offer a structured alternative, current systems rely on static prompting of c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MemBuilder: Reinforcing LLMs for Long-Term Memory Construction via Attributed Dense Rewards

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在长期对话中难以维持一致性，主要原因在于：
- **标准检索机制（如 RAG）** 将历史信息视为静态片段，无法捕捉信息随时间的演化过程。
- **现有记忆增强框架** 多依赖闭源模型的固定提示（prompting），缺乏反馈机制来评估所构建记忆是否真正有助于下游任务。
- **基于训练的方法** 面临两大技术瓶颈：
  1. **稀疏奖励（Sparse Trajectory-Level Rewards）**：仅在多轮对话结束时提供一个全局奖励，导致学习信号不足，难以定位具体哪一轮的记忆操作有效。
  2. **多维记忆归因不清（Multi-Dimensional Memory Attribution）**：不同类型的记忆（如事件、事实、流程）共享同一奖励，无法区分其对最终表现的实际贡献。

### **提出了什么新方法或新思路**

作者提出 **MemBuilder**，一种基于强化学习（Reinforcement Learning, RL）的框架，用于训练轻量级开源模型进行多维长期记忆构建。其核心创新是 **Attributed Dense Rewards Policy Optimization (ADRPO)**，包含两个关键技术：

#### ✅ 创新点一：密集会话级奖励（Dense Session-Level Rewards）
- 在每个对话会话后生成合成的 QA 对（synthetic session-level QA），并立即评估记忆质量。
- 每个会话的操作都能获得即时反馈，而非等到整个对话轨迹结束才给予奖励。
- 奖励构成：
  - **任务奖励**：基于预生成问题的回答准确率（QA accuracy）。
  - **格式有效性惩罚**：非法输出直接得零分。
  - **长度惩罚**：防止模型通过冗长输出“刷分”。

#### ✅ 创新点二：贡献感知梯度加权（Contribution-Aware Gradient Weighting）
- 不同类型的记忆（Episodic, Semantic, Procedural）对问答结果的贡献不同。
- 引入动态梯度权重机制：根据每类记忆在 QA 过程中的**实际检索频率**来放大其梯度更新。
- 公式上，主导贡献类型（dominant contributor）获得放大的梯度权重 $ w(m) = \alpha > 1 $，其余为 1。
- 这使得高影响力的内存组件得到更强的学习信号。

### **相比现有方法的优势**

| 维度 | 传统方法 | MemBuilder |
|------|--------|------------|
| **训练范式** | 固定提示 / 稀疏奖励 RL | 密集奖励 + 归因优化 RL |
| **记忆控制粒度** | 黑箱操作 | 显式多维结构化管理 |
| **可解释性与可控性** | 差 | 高（支持 REPLACE/UPDATE/MERGE 等精细操作）|
| **成本与部署** | 依赖昂贵闭源 API | 可用 4B 参数开源模型替代 |
| **泛化能力** | 通常限于特定场景 | 在 OOD 数据集上表现优异 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 描述 | 特点 |
|-------|------|------|
| **LongMemEval** | 用户-助手聊天记录，平均 40 会话，约 115K tokens | 作为**训练集和 ID 测试集**，测试信息提取、跨会话推理、时间推理等能力 |
| **LoCoMo** | 虚构角色间的人类对话，基于时间图谱，最多 35 会话 | **OOD 测试集**，含 SingleHop、MultiHop、Temporal、Adversarial 等多种题型 |
| **PerLTQA** | 包含 141 个虚构人物的生活事件与关系网络 | **OOD 测试集**，强调人物画像整合与长期记忆融合 |

> 所有模型**仅在 LongMemEval 上训练**，LoCoMo 和 PerLTQA 用于评估跨领域泛化能力。

### **实验设置和评估指标**

- **基础模型**：`Qwen3-4B-Instruct-2507`（4B 参数）
- **专家轨迹生成**：使用 `Claude 4.5 Sonnet` 构建监督微调（SFT）数据。
- **合成 QA 生成**：使用 `Claude 4.5 Opus` 为每个会话生成 5 个问题。
- **答案模型 & 判别器**：
  - 主要使用 `Claude 4.5 Sonnet` 保证公平比较。
  - 使用 `GPT-4.1-mini` 作为 LLM Judge 进行自动评分。
- **检索模型**：`text-embedding-3-small`
- **评估指标**：**准确率（Accuracy %）**，按数据集整体及问题类型细分。

### **基线方法对比**

| 类别 | 基线方法 | 说明 |
|------|---------|------|
| **Retrieval-based** | RAG-Session, RAG-Utterance | 分别以会话或语句为单位检索 |
| **Prompting-based Memory** | Mem0, MIRIX | 使用闭源模型 + 固定提示构建记忆 |
| **Training-based Memory** | Memory-R1 | 基于稀疏奖励的 RL 方法（原文使用 Llama-3.1-8B） |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| 方法 | LoCoMo (%) | LongMemEval (%) | PerLTQA (%) |
|------|------------|------------------|-------------|
| **RAG-Session** | 70.35 | 66.75 | 79.21 |
| **RAG-Utterance** | 74.87 | 69.00 | 77.23 |
| **MIRIX (prompting)** | 77.48 | 73.25 | 83.11 |
| **Memory-R1 (reported)** | 62.74 | — | — |
| **Ours (SFT + RL)** | **84.23** | **85.75** | **93.14** |

> ✅ MemBuilder 在所有三个基准上均达到 SOTA 表现。

### **与基线方法的对比结果**

- 在 **LoCoMo** 上，MemBuilder 比最佳 prompting 方法 **MIRIX 提升 6.75 pp**，甚至**超过 Claude 4.5 Sonnet 本身作为记忆构建者的表现（82.61%）**。
- 在 **PerLTQA** 上达到 **93.14%**，显著优于所有基线。
- 即使使用更小的 4B 模型，也能超越使用更大模型（如 8B）的 Memory-R1（62.74% → 84.23%）。

### **消融实验结果**

#### 🔹 梯度加权系数 $\alpha$ 的影响（Figure 4）
- 当 $\alpha=1$（无加权）时：82.82%
- 最优值 $\alpha=4$：**84.04%**
- 过大（$\alpha=16$）导致梯度失衡，性能下降至 83.01%

> 结论：适度放大主导记忆类型的梯度能显著提升性能。

#### 🔹 奖励密度的影响（Figure 5）
- 完全密集奖励（reward every session）：**84.23%**
- 奖励密度降至 1/8：**80.92%**（低于 SFT 基线 81.74%）

> 结论：稀疏奖励不仅收敛慢，甚至可能不如纯 SFT，验证了密集奖励的重要性。

#### 🔹 SFT 与 RL 的协同作用（Table 1）
| 阶段 | LoCoMo (%) |
|------|-----------|
| Base Model | 68.07 |
| + SFT | 81.74 |
| + RL（无 SFT） | 79.31 |
| + SFT + RL | **84.23** |

> SFT 解决“冷启动”问题，RL 进一步优化下游效用；两者缺一不可。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **轻量级开源模型可通过适当训练胜任复杂记忆构建任务**：一个 4B 参数的 Qwen 模型经过 ADRPO 训练后，性能超越闭源大模型驱动的记忆系统。
2. ✅ **密集奖励 + 归因机制是成功的关键**：传统的稀疏奖励 RL 在长周期任务中效果有限，而 MemBuilder 的合成 QA 与梯度归因设计极大提升了学习效率。
3. ✅ **多维记忆结构具有实际价值**：Core、Episodic、Semantic、Procedural 四类记忆分工明确，且 RL 学会根据不同类型调整操作策略（见案例分析）。
4. ✅ **强泛化能力**：尽管只在 LongMemEval 上训练，MemBuilder 在 LoCoMo 和 PerLTQA 上仍表现出色，证明其具备跨对话模式和领域的适应性。
5. ✅ **行为演化明显**：从 SFT 到 RL，模型学会更精准地使用 `REPLACE` 替代 `APPEND`，用 `UPDATE` 构建时间链，保留旧版本（如咖啡冲泡流程），避免信息丢失。

### **方法的局限性**

1. **评估依赖闭源模型**：虽然记忆由开源模型构建，但 QA 和评判仍使用 `Claude` 和 `GPT-4`，存在 API 成本。
2. **合成 QA 可能不完美**：由 `Claude Opus` 生成的问题可能存在错误或模糊，影响奖励信号质量。
3. **扩展性挑战**：当前方法需对每个会话多次 rollout 和 QA 评估，计算开销较大，尤其在大规模部署时。

### **未来工作方向**

- 探索完全开源的端到端训练与评估流水线（如使用 Qwen-VL 或 DeepSeek-V2 替代判别器）。
- 将 ADRPO 应用于其他需要长期状态维护的任务，如游戏代理、个人助理、教育机器人等。
- 动态调整奖励密度或引入课程学习（curriculum learning）以进一步降低训练成本。
- 研究如何让模型自主决定何时创建/删除/合并记忆，实现更高级的元认知控制。

---

> 📌 **一句话总结**：  
> MemBuilder 通过 **Attributed Dense Rewards** 实现了对轻量级 LLM 的高效训练，使其能够主动、精准地构建多维长期记忆，在多个 OOD 基准上超越闭源模型，推动了开放、可控、低成本的智能体记忆系统发展。

</details>

---

### 16. [HAPS: Hierarchical LLM Routing with Joint Architecture and Parameter Search](https://arxiv.org/abs/2601.05903)

**Authors**: Zihang Tian, Rui Li, Jingsen Zhang, Xiaohe Bo, Wei Huo, Xu Chen  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05903v1  

#### Abstract
Large language model (LLM) routing aims to exploit the specialized strengths of different LLMs for diverse tasks. However, existing approaches typically focus on selecting LLM architectures while overlooking parameter settings, which are critical for task performance. In this paper, we introduce HAP...

---

### 17. [The Kernel Manifold: A Geometric Approach to Gaussian Process Model Selection](https://arxiv.org/abs/2601.05371)

**Authors**: Md Shafiqul Islam, Shakti Prasad Padhy, Douglas Allaire, Raymundo Arr\'oyave  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05371v1  

#### Abstract
Gaussian Process (GP) regression is a powerful nonparametric Bayesian framework, but its performance depends critically on the choice of covariance kernel. Selecting an appropriate kernel is therefore central to model quality, yet remains one of the most challenging and computationally expensive ste...

---

### 18. [Efficient Inference for Noisy LLM-as-a-Judge Evaluation](https://arxiv.org/abs/2601.05420)

**Authors**: Yiqun T Chen, Sizhu Lu, Sijia Li, Moran Guo, Shengyi Li  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05420v1  

#### Abstract
Large language models (LLMs) are increasingly used as automatic evaluators of generative AI outputs, a paradigm often referred to as "LLM-as-a-judge." In practice, LLM judges are imperfect predictions for the underlying truth and can exhibit systematic, non-random errors. Two main approaches have re...

---

### 19. [PaCoRe: Learning to Scale Test-Time Compute with Parallel Coordinated Reasoning](https://arxiv.org/abs/2601.05593)

**Authors**: Jingcheng Hu, Yinmin Zhang, Shijie Shang, Xiaobo Yang, Yue Peng, Zhewei Huang, Hebin Zhou, Xin Wu, Jie Cheng, Fanqi Wan, Xiangwen Kong, Chengyuan Yao, Kaiwen Yan, Ailin Huang, Hongyu Zhou, Qi Han, Zheng Ge, Daxin Jiang, Xiangyu Zhang, Heung-Yeung Shum  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05593v1  

#### Abstract
We introduce Parallel Coordinated Reasoning (PaCoRe), a training-and-inference framework designed to overcome a central limitation of contemporary language models: their inability to scale test-time compute (TTC) far beyond sequential reasoning under a fixed context window. PaCoRe departs from the t...

---

### 20. [Overcoming Joint Intractability with Lossless Hierarchical Speculative Decoding](https://arxiv.org/abs/2601.05724)

**Authors**: Yuxuan Zhou, Fei Huang, Heng Li, Fengyi Wu, Tianyu Wang, Jianwei Zhang, Junyang Lin, Zhi-Qi Cheng  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.05724v1  

#### Abstract
Verification is a key bottleneck in improving inference speed while maintaining distribution fidelity in Speculative Decoding. Recent work has shown that sequence-level verification leads to a higher number of accepted tokens compared to token-wise verification. However, existing solutions often rel...

---

### 21. [DynaSTy: A Framework for SpatioTemporal Node Attribute Prediction in Dynamic Graphs](https://arxiv.org/abs/2601.05391)

**Authors**: Namrata Banerji, Tanya Berger-Wolf  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.05391v1  

#### Abstract
Accurate multistep forecasting of node-level attributes on dynamic graphs is critical for applications ranging from financial trust networks to biological networks. Existing spatiotemporal graph neural networks typically assume a static adjacency matrix. In this work, we propose an end-to-end dynami...

---

### 22. [Orchestrating Tokens and Sequences: Dynamic Hybrid Policy Optimization for RLVR](https://arxiv.org/abs/2601.05607)

**Authors**: Zijun Min, Bingshuai Liu, Ante Wang, Long Zhang, Anxiang Zeng, Haibo Zhang, Jinsong Su  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.05607v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) offers a promising framework for optimizing large language models in reasoning tasks. However, existing RLVR algorithms focus on different granularities, and each has complementary strengths and limitations. Group Relative Policy Optimization (GR...

---

### 23. [GenCtrl -- A Formal Controllability Toolkit for Generative Models](https://arxiv.org/abs/2601.05637)

**Authors**: Emily Cheng, Carmen Amo Alonso, Federico Danieli, Arno Blaas, Luca Zappella, Pau Rodriguez, Xavier Suau  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.05637v1  

#### Abstract
As generative models become ubiquitous, there is a critical need for fine-grained control over the generation process. Yet, while controlled generation methods from prompting to fine-tuning proliferate, a fundamental question remains unanswered: are these models truly controllable in the first place...

---

### 24. [CHisAgent: A Multi-Agent Framework for Event Taxonomy Construction in Ancient Chinese Cultural Systems](https://arxiv.org/abs/2601.05520)

**Authors**: Xuemei Tang, Chengxi Yan, Jinghang Gu, Chu-Ren Huang  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.05520v1  

#### Abstract
Despite strong performance on many tasks, large language models (LLMs) show limited ability in historical and cultural reasoning, particularly in non-English contexts such as Chinese history. Taxonomic structures offer an effective mechanism to organize historical knowledge and improve understanding...

---

### 25. [Ontology Neural Networks for Topologically Conditioned Constraint Satisfaction](https://arxiv.org/abs/2601.05304)

**Authors**: Jaehong Oh  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.05304v1  

#### Abstract
Neuro-symbolic reasoning systems face fundamental challenges in maintaining semantic coherence while satisfying physical and logical constraints. Building upon our previous work on Ontology Neural Networks, we present an enhanced framework that integrates topological conditioning with gradient stabi...

---

### 26. [Community-Based Model Sharing and Generalisation: Anomaly Detection in IoT Temperature Sensor Networks](https://arxiv.org/abs/2601.05984)

**Authors**: Sahibzada Saadoon Hammad, Joaqu\'in Huerta Guijarro, Francisco Ramos, Michael Gould Carlson, Sergio Trilles Oliver  
**Category**: cs.LG  
**Published**: 2026-01-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.05984v1  

#### Abstract
The rapid deployment of Internet of Things (IoT) devices has led to large-scale sensor networks that monitor environmental and urban phenomena in real time. Communities of Interest (CoIs) provide a promising paradigm for organising heterogeneous IoT sensor networks by grouping devices with similar o...

---

### 27. [Reinforcement Learning of Large Language Models for Interpretable Credit Card Fraud Detection](https://arxiv.org/abs/2601.05578)

**Authors**: Cooper Lin, Yanting Zhang, Maohao Ran, Wei Xue, Hongwei Fan, Yibo Xu, Zhenglin Wan, Sirui Han, Yike Guo, Jun Song  
**Category**: cs.AI  
**Published**: 2026-01-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05578v1  

#### Abstract
E-commerce platforms and payment solution providers face increasingly sophisticated fraud schemes, ranging from identity theft and account takeovers to complex money laundering operations that exploit the speed and anonymity of digital transactions. However, despite their theoretical promise, the ap...

---

### 28. [GIFT: Games as Informal Training for Generalizable LLMs](https://arxiv.org/abs/2601.05633)

**Authors**: Nuoyan Lyu, Bingbing Xu, Weihao Meng, Yige Yuan, Yang Zhang, Zhiyong Huang, Tat-Seng Chua, Huawei Shen  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05633v1  

#### Abstract
While Large Language Models (LLMs) have achieved remarkable success in formal learning tasks such as mathematics and code generation, they still struggle with the "practical wisdom" and generalizable intelligence, such as strategic creativity and social reasoning, that characterize human cognition. ...

---

### 29. [LLMs as Science Journalists: Supporting Early-stage Researchers in Communicating Their Science to the Public](https://arxiv.org/abs/2601.05821)

**Authors**: Milad Alshomary, Grace Li, Anubhav Jangra, Yufang Hou, Kathleen McKeown, Smaranda Muresan  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05821v1  

#### Abstract
The scientific community needs tools that help early-stage researchers effectively communicate their findings and innovations to the public. Although existing general-purpose Large Language Models (LLMs) can assist in this endeavor, they are not optimally aligned for it. To address this, we propose ...

---

### 30. [FACTUM: Mechanistic Detection of Citation Hallucination in Long-Form RAG](https://arxiv.org/abs/2601.05866)

**Authors**: Maxime Dassen, Rebecca Kotula, Kenton Murray, Andrew Yates, Dawn Lawrie, Efsun Kayi, James Mayfield, Kevin Duh  
**Category**: cs.CL  
**Published**: 2026-01-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05866v1  

#### Abstract
Retrieval-Augmented Generation (RAG) models are critically undermined by citation hallucinations, a deceptive failure where a model confidently cites a source that fails to support its claim. Existing work often attributes hallucination to a simple over-reliance on the model's parametric knowledge. ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
