# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-13 06:43:35 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Pretraining A Large Language Model using Distributed GPUs: A Memory-Efficient Decentralized Paradigm](https://arxiv.org/abs/2602.11543)

**Authors**: Jinrui Zhang, Chaodong Xiao, Aoqi Wu, Xindong Zhang, Lei Zhang  
**Category**: cs.CL  
**Published**: 2026-02-13  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2602.11543v1  

#### Abstract
Pretraining large language models (LLMs) typically requires centralized clusters with thousands of high-memory GPUs (e.g., H100/A100). Recent decentralized training methods reduce communication overhead by employing federated optimization; however, they still need to train the entire model on each n...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大规模语言模型（LLM）的预训练严重依赖于集中式的高性能计算集群，通常需要数千张高显存GPU（如H100/A100）以及高速互联（如RDMA）。这种架构对硬件资源要求极高，导致大多数研究机构难以参与LLM研发。尽管已有去中心化训练方法（如DiLiCo、Photon），但它们仍需在每个节点上存储和更新**完整模型参数**，受限于单个GPU的内存容量。

### 提出的新方法：SPES（Sparse Expert Synchronization）
本文提出了一种**内存高效、去中心化的MoE LLM预训练框架——SPES**，其核心思想是：
- 将Mixture-of-Experts（MoE）模型中的**专家（experts）分散到不同节点**进行训练。
- 每个节点仅负责训练自己分配到的一组专家，其余专家保持冻结状态。
- 节点间通过稀疏同步机制交换已更新的专家权重，避免传输全部模型参数。

此外，为解决早期训练阶段专家利用率低、收敛慢的问题，作者引入了**expert-merging warm-up策略**：
- 在训练初期周期性地将相似的专家以加权平均方式合并，促进跨节点知识共享。
- 合并强度随训练逐步衰减，后期保留专家的专业化能力。

### 相比现有方法的优势
| 维度 | 传统集中式/DiLiCo | SPES |
|------|------------------|------|
| **每节点显存占用** | 高（需存全模型+优化器状态） | 显著降低（仅维护局部专家） |
| **通信开销** | 高频次全模型同步 | 稀疏同步，仅传更新部分 |
| **硬件门槛** | 必须使用高显存GPU集群 | 可用普通48GB GPU通过互联网连接训练 |
| **可扩展性** | 受限于带宽和内存 | 更适合地理分布、异构设备环境 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **从零训练（Training from scratch）**：
  - `Ultra-FineWeb`、`SlimPajama`：通用网页文本
  - `arXiv`, `OpenWebMath`, `Algebraic Stack`, `peS2o`, `StarCoder`：科学、数学、编程领域数据（来自olmo-mix-1124）
- **迁移训练（Upcycling）**：
  - `Nemotron Pretraining Dataset`：高质量数学、代码、多语言问答数据

所有数据均为公开可用，确保复现性和社区可访问性。

### 实验设置
| 模型规模 | 节点数 | 单节点配置 | 总训练Token量 |
|--------|-------|-----------|-------------|
| 2B MoE | 16 | 1×NVIDIA L40S (48GB) | 500B |
| 7B MoE | 4 | 每节点8×A800 GPU (NVLink互联) | 500B |
| 9B MoE* | - | 由Qwen3-1.7B-Base初始化 | 400B |

> *注：9B模型采用“upcycling”方式，即从dense模型扩展而来。

### 评估指标
使用 `lm-evaluation-harness` 库进行标准评测，涵盖以下基准任务：
- **常识推理**：ARC(e/c), PIQA, SIQA, WinoGrande
- **阅读理解**：BoolQ, OpenBookQA, LogiQA
- **综合知识**：MMLU, C-Eval

报告0-shot或标准化准确率。

### 基线方法对比
- **Centralized Training**：传统集中式训练（FSDP/ZeRO等）
- **DiLiCo**：代表性的去中心化预训练方法（基于FedAvg）
- 其他开源LLM：如TinyLlama, Pythia, Qwen系列, MoE++等

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 3）
| 方法 | 参数量 | 训练Token | ARC(e) | ARC(c) | PIQA | SciQ | BoolQ | Avg |
|-----|--------|------------|--------|--------|------|-------|--------|-----|
| SPES-2B | 0.8B/2.1B | 500B | 63.8 | 35.3 | 69.3 | 85.0 | 61.4 | — |
| SPES-7B | 1.6B/7B | 500B | 72.1 | 43.8 | 74.7 | 89.9 | 62.7 | — |
| SPES-9B* | 3.1B/9B | 400B | 81.5 | 57.3 | 78.9 | 95.3 | 77.3 | — |

> *注：前项为激活参数量，后项为总参数量*

### 与基线方法对比
#### 内存与通信效率（Fig. 3）
| 指标 | DiLiCo/Centralized | SPES | 改进幅度 |
|------|--------------------|------|---------|
| **2B模型每GPU显存** | >55GB | **35GB** | ↓36% |
| **7B模型上行通信量/轮** | 28.6 GB | **9.8 GB** | ↓65% |
| **2B模型通信成本** | 基准值 | ↓**33.3%** | 显著降低 |

> ✅ SPES可在16台独立的48GB GPU上完成2B MoE模型训练，而传统方法无法在此配置下运行。

#### 性能对比（Table 1）
| 方法 | 平均得分（8项任务） |
|------|------------------|
| Centralized | 49.7 |
| DiLiCo | 50.5 |
| **SPES** | **51.0** |

> 🔺 SPES不仅资源消耗更低，且在多个任务上达到甚至超越集中式训练的表现。

### 消融实验结果
#### Expert-Merging Warm-Up 效果（Table 4）
| 方法 | 平均得分 | BoolQ↑ | SciQ↑ |
|------|----------|--------|-------|
| w/o merging | 50.5 | 58.0 | 75.9 |
| **w/ merging** | **51.3** | **60.4** | **77.8** |

> 💡 合并策略显著提升早期收敛速度和最终性能，尤其在复杂推理任务上表现更优。

#### 不同节点数量的影响（Table A5）
| 节点数 | 平均得分 |
|-------|----------|
| 2     | 50.6     |
| 4     | 49.7     |
| 8     | 49.5     |

> 📉 随着节点增多略有下降，但仍保持竞争力，表明SPES具备良好**横向扩展能力**。

#### 同步频率影响（Fig. A2）
- 局部训练步数 $H$ 越大（同步越少），性能越差。
- 最佳平衡点为 $H=50$，说明**适度频繁的同步有助于维持一致性**。

---

## 4. 关键结论和发现

### 主要发现
1. **SPES实现了真正意义上的低门槛LLM预训练**：
   - 成功在16块通过互联网连接的48GB消费级GPU上完成了2B MoE模型的训练。
   - 性能媲美同等算力预算下的集中式训练方案。

2. **MoE架构天然适配去中心化训练**：
   - 专家模块的独立性使得可以自然切分训练负载。
   - 结合稀疏同步机制，大幅降低通信压力。

3. **expert-merging warm-up有效缓解稀疏训练缺陷**：
   - 提升了token利用率和专家泛化能力。
   - 是实现快速收敛的关键设计。

4. **SPES具有良好的可扩展性**：
   - 成功训练7B从头开始模型和9B迁移模型。
   - 所有模型均匹配或优于同类集中式基线。

### 方法的局限性
- 当前最大验证规模为9B参数，尚未验证超大规模（如>100B）下的表现。
- 训练时间较长，受限于弱网络连接下的同步延迟。
- 未探索多模态任务上的适用性。

### 未来工作方向
- 验证更大规模模型（>10B）和更长训练周期下的稳定性与性能。
- 探索在**多模态模型**（如vision-language models）中的应用。
- 进一步优化同步协议，支持动态节点加入/退出，增强鲁棒性。
- 结合更先进的MoE设计（如zero-computation experts）进一步提升效率。

---

> 🔗 **代码开源地址**：[https://github.com/zjr2000/SPES](https://github.com/zjr2000/SPES)  
> 📘 **论文链接**：[arXiv:2602.11543](https://arxiv.org/abs/2602.11543)

</details>

---

### 2. [Extending Puzzle for Mixture-of-Experts Reasoning Models with Application to GPT-OSS Acceleration](https://arxiv.org/abs/2602.11937)

**Authors**: Akhiad Bercovich, Nir Ailon, Vladimir Anisimov, Tomer Asida, Nave Assaf, Mohammad Dabbah, Ido Galil, Amnon Geifman, Yonatan Geifman, Izhak Golan, Roi Koren, Itay Levy, Zach Moshe, Pavlo Molchanov, Najeeb Nabwani, Mostofa Patwari, Omri Puny, Tomer Ronen, Itamar Schen, Elad Segal, Ido Shahaf, Oren Tropp, Ran Zilberstein, Ran El-Yaniv  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2602.11937v1  

#### Abstract
Reasoning-focused LLMs improve answer quality by generating longer reasoning traces, but the additional tokens dramatically increase serving cost, motivating inference optimization. We extend and apply Puzzle, a post-training neural architecture search (NAS) framework, to gpt-oss-120B to produce gpt...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Extending Puzzle for Mixture-of-Experts Reasoning Models with Application to GPT-OSS Acceleration

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于推理（reasoning）的大语言模型（LLMs）通过生成更长的推理链（reasoning traces）来提升答案质量，但这显著增加了服务成本（serving cost）。主要原因在于：
- 自回归解码中，**self-attention 的计算开销随序列长度增长而增加**；
- **KV Cache 的内存占用和带宽需求随 token 数线性增长**，在长上下文（如 128K）场景下成为瓶颈；
- 推理任务中的 token 生成数量可变，传统的 per-token 吞吐量（tok/s）或延迟（ms/token）无法准确反映端到端效率。

因此，如何在不牺牲推理能力的前提下，优化推理型 MoE 模型的部署效率，成为一个关键挑战。

### 提出的新方法与思路
本文提出对 **Puzzle** 框架进行扩展，并应用于 `gpt-oss-120B` 模型，构建出轻量化、高性能的衍生模型 `gpt-oss-puzzle-88B`。其核心是**后训练神经架构搜索（post-training NAS）**，结合多种优化技术：

#### 主要创新点：
- ✅ **异构 MoE 专家剪枝（Heterogeneous MoE Expert Pruning）**  
  在 MoE 层中按层动态决定保留的专家数量（8–128个），而非统一压缩。通过激活相似性（activation-based replace-1-block scoring）评估每层重要性，实现精细化压缩。

- ✅ **基于长上下文感知的窗口注意力替换（Long-context-aware Window Attention Replacement）**  
  将部分全局注意力层替换为 **Window Attention**（滑动窗口注意力），以降低 KV Cache 占用。不同于传统局部信号评分，本文使用 **AA-LCR 基准分数**作为评分依据，专门捕捉对长程依赖的影响。

- ✅ **FP8 KV-Cache 量化 + 标度校准（Calibrated Scales）**  
  引入 FP8 量化并采用最大值标定（max calibration）生成 KV scales，有效缓解精度损失，同时将 KV Cache 容量提升约 2×，加速 attention 模块。

- ✅ **强化学习训练双路径策略 + 权重平均融合**  
  训练两个 RL 策略：一个专注高推理努力模式（提升准确性），另一个平衡各推理等级（控制生成长度）。最终通过权重平均合并，兼顾高准确性和可控的生成长度。

- ✅ **请求级效率评估指标（Request-Level Efficiency）**  
  提出 **相对请求速率（relative request rate） = 最大吞吐量 / 平均生成 token 数**，综合衡量速度与推理成本，避免“吞吐高但 trace 长”导致实际效率下降的误导。

### 相比现有方法的优势
| 维度 | 优势说明 |
|------|--------|
| **架构灵活性** | 支持异构 MoE 和混合注意力结构，优于固定结构压缩方法 |
| **长上下文适配性** | 显式优化 KV Cache 和长程建模能力，适合 agentic workflows |
| **端到端效率导向** | 不仅看 per-token 性能，更关注请求级别的性价比 |
| **无需重新预训练** | 全部优化在 post-training 阶段完成，节省大量算力 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **LNPT-gpt-oss 数据集**：基于 `nvidia/Llama-Nemotron-Post-Training-Dataset`，由 `gpt-oss-120B` 在 high/medium reasoning effort 下生成响应构成，用于：
  - MoE 专家贡献评分
  - Replace-1-block 评分
  - Knowledge Distillation
  - KV Quantization 标度校准
- **AA-LCR（Artificial Analysis Long-Context Reasoning）**：用于评估注意力模块替换对长上下文推理能力的影响。
- **其他基准测试集**：
  - MMLU-Pro, GPQA-Diamond, HLE, AIME25, SciCode, IFBench, RULER-128K

### 实验设置
- **硬件平台**：
  - 主要：8×NVIDIA H100 节点（80GB HBM3）
  - 对比：单张 H100 GPU
- **输入输出配置**：
  - 长上下文：64K/64K（64K 输入 + 64K 输出）
  - 短上下文：4K/4K
- **推理参数**（见 Appendix B）：
  - Temp=0.6, Top_p=0.95, Max new tokens=128K
  - 使用 vLLM v0.11 进行推理评测

### 评估指标
| 指标 | 描述 |
|------|------|
| **Per-token Throughput (tok/s)** | 每秒处理 token 数，衡量架构效率 |
| **Decode Latency (ITL, ms/token)** | 单个 token 解码延迟 |
| **Suite-Average Accuracy (%)** | 多项基准任务的平均准确率 |
| **Generated Token Count (K)** | 每请求平均生成 token 数 |
| **Effort Length Ratio** | 高/低推理努力下的生成长度比，衡量可控性 |
| **Relative Request Rate** | 请求级效率 = 最大吞吐 / 平均生成 token 数，归一化至基线 |

### 基线方法对比
- **gpt-oss-120B**（BF16 / FP8 KV）：原始模型，主要对比基线
- **HyperNova-60B**：第三方压缩版本（来自 Multiverse Computing），也基于 gpt-oss-120B，提供外部对照

---

## 3. 主要实验结果和性能指标

### 关键性能数据（H100 节点）

| 场景 | 模型 | 最大吞吐量 | Speedup |
|------|------|------------|---------|
| **4K/4K** | gpt-oss-120B | 29.6K tok/s | — |
| | gpt-oss-puzzle-88B | **36.1K tok/s** | **1.22×** |
| **64K/64K** | gpt-oss-120B | 5.7K tok/s | — |
| | gpt-oss-puzzle-88B | **9.3K tok/s** | **1.63×** |
| **单卡 64K/64K** | gpt-oss-120B | 0.3K tok/s | — |
| | gpt-oss-puzzle-88B | **0.8K tok/s** | **2.82×** |

> 💡 在单卡场景下，由于内存限制更严重，优化效果更加显著。

### 准确性表现（suite-average accuracy, KV FP8）

| 推理等级 | gpt-oss-120B | gpt-oss-puzzle-88B | Retention |
|----------|---------------|---------------------|-----------|
| High | 58.19% | **58.67%** | 100.8% |
| Medium | 52.89% | **54.93%** | 103.9% |
| Low | 44.71% | **48.38%** | **108.2%** |

✅ 结果表明：尽管参数量仅为原模型的 ~73%，`gpt-oss-puzzle-88B` 在多数推理等级上**匹配甚至略微超越**原模型。

### 请求级效率（Relative Request Rate）

从 Figure 1 可见，在整个 **accuracy-speed frontier** 上，`gpt-oss-puzzle-88B` 均优于 `gpt-oss-120B` 和 `HyperNova-60B`：
- 在低推理等级下，达到最高 **1.29× 的请求级效率增益**
- Effort Length Ratio 保持与教师模型相近，确保用户可通过调节 effort 控制成本与质量

### 与其他压缩模型对比
- **HyperNova-60B** 虽然 per-token 吞吐较高，但由于生成 trace 更长，**请求级效率反而被抵消**，尤其在高推理等级下表现不佳。

### 消融实验结果（Appendix C）

| 注意力评分方式 | MMLU-Pro ↑ | GPQA ↑ | AALCR ↑ |
|----------------|------------|--------|---------|
| Activation-MSE（常规） | 79.26 | 73.67 | 17.00 |
| **AALCR-based（本文）** | **79.40** | **74.43** | **37.00** |

➡️ 使用 **AA-LCR 任务性能下降作为评分信号**，显著提升了长上下文推理能力保留，验证了长上下文感知评分的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Puzzle 框架可成功扩展至 MoE 架构和复杂注意力机制**，支持异构剪枝与选择性 window attention 替换。
2. ✅ **FP8 KV Cache + calibrated scales 可在几乎无损精度前提下减半 KV 内存占用**，极大提升长上下文服务能力。
3. ✅ **请求级效率指标比 per-token 吞吐更能反映真实部署效益**，应成为推理模型优化的核心目标。
4. ✅ **通过 RL 双路径训练+融合策略，可在不增加生成长度的前提下恢复并提升推理准确性**。
5. ✅ `gpt-oss-puzzle-88B` 在 **吞吐、效率、准确性三方面全面优于基线**，证明 post-training NAS 是高效部署的强大工具。

### 方法的局限性
- 🔸 当前 Puzzle 仍依赖“单块替换”近似打分，可能忽略模块间非线性交互效应。
- 🔸 对于极端长上下文（>128K），window attention 的窗口大小需进一步调优。
- 🔸 所有优化基于特定硬件（H100），跨设备泛化能力有待验证。
- 🔸 当前未探索 MoE router 的微调，可能存在进一步压缩空间。

### 未来工作方向
- 🔄 将 Puzzle 应用于更多新型架构（如 Mamba、RetNet）
- 🧩 探索联合优化 MoE routing 与 FFN 结构
- 📈 开发更精确的 long-range interaction 评估代理（surrogate metric）
- ☁️ 构建自动化 Puzzle-as-a-Service 流水线，支持用户自定义约束下的模型定制

---

> 📌 **总结一句话**：  
> 本文通过扩展 Puzzle 框架，首次实现了对 MoE 推理模型的系统性后训练架构搜索优化，推出了 `gpt-oss-puzzle-88B`，在保持甚至提升推理准确性的前提下，实现了高达 **2.82× 的单卡吞吐提升** 和 **1.29× 的请求级效率增益**，为高效部署大型推理模型提供了完整范式。

</details>

---

### 3. [T3D: Few-Step Diffusion Language Models via Trajectory Self-Distillation with Direct Discriminative Optimization](https://arxiv.org/abs/2602.12262)

**Authors**: Tunyu Zhang, Xinxi Zhang, Ligong Han, Haizhou Shi, Xiaoxiao He, Zhuowei Li, Hao Wang, Kai Xu, Akash Srivastava, Hao Wang, Vladimir Pavlovic, Dimitris N. Metaxas  
**Category**: cs.CL  
**Published**: 2026-02-13  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.12262v1  

#### Abstract
Diffusion large language models (DLLMs) have the potential to enable fast text generation by decoding multiple tokens in parallel. However, in practice, their inference efficiency is constrained by the need for many refinement steps, while aggressively reducing the number of steps leads to a substan...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《T3D: Few-Step Diffusion Language Models via Trajectory Self-Distillation with Direct Discriminative Optimization》总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
Diffusion Large Language Models (DLLMs) 虽然支持并行生成多个 token，具备潜在的高效推理能力，但在实际应用中仍需大量去噪步骤（denoising steps）才能保证生成质量。当减少步数以提升效率时，生成质量会显著下降。这一**few-step decoding**（少步解码）问题限制了 DLLMs 在低延迟场景（如实时决策、端侧部署）的应用。

此外，现有方法存在两个核心挑战：
- **Mean-field approximation error**：由于采用 token-factorized 参数化，导致在步数压缩下因子化误差增大。
- **Train-inference distribution mismatch**：训练时使用随机掩码，而推理时使用基于置信度的非随机调度策略，导致中间状态分布不一致。

### 提出了什么新方法或新思路
本文提出 **T3D**（Trajectory Self-Distillation via DDO），一种通过**轨迹自蒸馏**（trajectory self-distillation）结合**直接判别优化**（Direct Discriminative Optimization, DDO）来提升少步解码性能的新框架。其三大核心组件为：

1. **Trajectory Self-Distillation（轨迹自蒸馏）**  
   利用预训练教师模型在其目标解码流程下的完整生成轨迹（从全掩码到干净序列的中间状态序列）作为监督信号，训练一个“学生”模型。这使得学生模型在训练阶段就接触到推理时的真实中间状态分布，从而消除 train-inference mismatch。

2. **Direct Discriminative Optimization (DDO)**  
   引入一种 GAN 启发式的反向 KL（reverse-KL）风格目标函数，替代传统的前向 KL（forward-KL）损失。DDO 鼓励“模式寻求”（mode-seeking）行为，使学生更关注高概率的教师生成路径，避免传统 mode-covering 导致的预测平滑化问题。

3. **Path Consistency Regularization（路径一致性正则化）**  
   对早期解码的 token 赋予更高的损失权重（$w_i = \frac{B - \tau_i + 1}{B}$），以缓解在块状少步解码中因早期错误引发的级联误差传播问题。

### 相比现有方法的优势
- **无需额外标注数据**：完全基于教师模型自身生成的轨迹进行自蒸馏，不依赖真实数据。
- **更强的对齐能力**：相比仅匹配最终输出的传统蒸馏方法，T3D 在整个生成轨迹上进行对齐，提升了对复杂推理过程的建模能力。
- **更优的少步性能**：在极紧的解码预算下（如每块仅1–2步），T3D 显著优于 ReDi、dParallel 等强基线。
- **保留全步解码能力**：即使针对少步任务训练，T3D 仍能保持接近原始模型的全步扩散性能，避免“遗忘”现象。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **训练数据集**（用于自蒸馏）：
  - 数学推理：`MATH` 训练集（Hendrycks et al., 2021）
  - 代码生成：`PrimeIntellect` 数据集（Jaghouar et al., 2024）
- **基准测试数据集**：
  - `GSM8K`：小学算术文字题
  - `MATH500`：高中及竞赛级别数学题
  - `MBPP`：Python 编程任务
  - `HumanEval`：代码生成评估

所有数据均被截断至最大长度 600 tokens。

### 实验设置和评估指标
- **模型架构**：基于 **SDAR** 系列模型（Cheng et al., 2025），采用块式扩散 + 块间自回归机制。
  - 使用 `SDAR-1.7B-Chat` 和 `SDAR-4B-Chat` 两种规模。
- **解码配置**：
  - Block Size ∈ {4, 8}
  - Tokens Per Step (TokPS) ∈ {2, 4} → 对应每块解码步数分别为 2/1 或 4/2 步
- **评估指标**：
  - 主要：准确率（Accuracy）在各 benchmark 上的表现
  - 辅助：吞吐量（TPS）、延迟（Latency）、平均解码步数（Avg Steps）等动态解码效率指标

### 基线方法对比
| 方法 | 简介 |
|------|------|
| **Original Model** | 原始预训练 SDAR 模型 |
| **SFT** | 在真实数据上进行监督微调（Supervised Fine-Tuning） |
| **ReDi** | Rectified Discrete Flow，标准自蒸馏方法（Yoo et al., 2025） |
| **dParallel** | 学习并行解码策略的蒸馏方法（Chen et al., 2025） |
| **Naive TD** | 朴素轨迹蒸馏（Eqn. 5 中的 forward-KL 版本） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

#### 在 SDAR-1.7B-Chat 上（TokPS=4, Block=4）：
| 方法 | MATH500 | GSM8K | MBPP | HumanEval | 平均提升 |
|------|--------|-------|------|-----------|----------|
| ReDi | 40.60 | 63.99 | 13.20 | 16.46 | ↓19.11% |
| dParallel | 43.40 | 68.23 | 22.20 | 24.39 | ↑0.29% |
| **T3D (Ours)** | **47.00** | **70.96** | **27.20** | **30.49** | **↑7.59%** |

#### 在 SDAR-4B-Chat 上（TokPS=4, Block=8）：
| 方法 | MATH500 | GSM8K | MBPP | HumanEval | 平均提升 |
|------|--------|-------|------|-----------|----------|
| ReDi | 25.40 | 53.30 | 5.00 | 7.32 | ↑0.65% |
| dParallel | 34.20 | 45.94 | 13.20 | 20.73 | ↑39.05% |
| **T3D (Ours)** | **47.80** | **69.90** | **22.60** | **23.78** | **↑85.02%** |

> ✅ **结论**：T3D 在所有设置下均取得最佳或次佳性能，尤其在大模型和高压缩比下优势明显。

---

### 与基线方法的对比结果
- **全面超越现有 self-distillation 方法**：在四种不同解码配置下，T3D 始终优于 ReDi、dParallel 和 Naive TD。
- **抗压能力强**：随着 TokPS 增加（即解码步数减少），其他方法性能急剧下降，而 T3D 表现稳定。
- **动态解码兼容性好**（见 Table 3）：
  - 在动态解码（dynamic decoding）下，T3D 实现更高准确率的同时，显著降低平均解码步数和延迟。
  - 例如在 GSM8K 上，T3D 准确率达 **72.40%**（原模型 61.56%），同时吞吐量提升至 **843.05 TPS**。

---

### 消融实验结果（Ablation Studies）

#### （1）是否保留全步解码能力？（Table 2）
| 方法 | MATH500 (1.7B) | GSM8K (1.7B) | MATH500 (4B) | GSM8K (4B) |
|------|----------------|--------------|-------------|------------|
| ReDi | 47.00 | 73.77 | 50.40 | 82.03 |
| dParallel | 0.40 | 0.23 | 13.20 | 2.88 |
| **T3D (Ours)** | **56.80** | **78.01** | **70.00** | **89.31** |

> 🔍 **发现**：T3D 成功保留了全步扩散性能，几乎无“遗忘”，而 ReDi 和 dParallel 严重退化。

#### （2）各组件作用分析（Table 5–7）
| 组件组合 | MATH500 ACC（Full-step） | Few-step 性能 |
|---------|----------------------------|---------------|
| Naive TD | 22.00 | 中等 |
| Naive TD + Lpath | 58.00 | 提升稳定性 |
| DDO + Random Tokens | 65.40 | 显著改善 |
| **DDO + Lpath + Random (T3D)** | **69.00** | **最优** |

> 🔬 **关键发现**：
> - DDO 对初始化敏感，需配合 random token 混合训练才能稳定。
> - Path Consistency 正则化有效抑制早期错误传播。
> - 三者协同作用是成功的关键。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **轨迹自蒸馏可有效解决 train-inference 分布偏移问题**，并通过监督完整生成路径提升少步解码鲁棒性。
2. **DDO 的 reverse-KL 特性更适合多模态去噪后验**，能引导学生聚焦于高质量生成路径，避免预测模糊。
3. **T3D 在极端少步条件下仍能维持高生成质量**，大幅缩小与全步解码之间的性能差距。
4. **该方法具有良好的泛化性**，不仅适用于静态解码，在动态解码中也表现出色。

### 方法的局限性
- 当前方法依赖于一个高质量的教师模型生成轨迹，若教师本身存在偏差，则可能被继承甚至放大。
- DDO 的训练过程对超参数（如正则化系数 $\lambda$）较为敏感，需要仔细调参（文中设 $\lambda=0.2$ 效果最佳）。
- 尚未探索如何将 T3D 扩展至更大规模模型或多模态场景。

### 未来工作方向
- 探索 **online distillation** 或 **self-play** 机制，让模型在训练过程中不断迭代优化自身轨迹。
- 结合 **KV Cache** 技术进一步加速推理（如 dKV-Cache、Fast-DLLM）。
- 将 T3D 应用于 **text-to-image** 或 **speech generation** 等离散扩散模型任务。
- 研究如何在 **low-resource setting** 下实现高效的 trajectory distillation。

---

> 🌟 **总体评价**：T3D 是迈向实用化少步扩散语言模型的重要一步。它通过简单而有效的轨迹蒸馏 + 判别优化 + 路径正则化设计，在不牺牲全步性能的前提下显著提升了少步生成质量，为高效文本生成提供了新的范式。  
> **开源地址**：[https://github.com/Tyrion58/T3D](https://github.com/Tyrion58/T3D)

</details>

---

### 4. [RL over Commodity Networks: Overcoming the Bandwidth Barrier with Lossless Sparse Deltas](https://arxiv.org/abs/2602.11456)

**Authors**: Chaoyi Ruan, Geng Luo, Xinyi Wan, Long Zhao, Qinghe Wang, Jiaan Zhu, Duling Xu, Guanbin Xu, Dehui Wei, Xiang Liu, Cheng Li, Haifeng Sun, Congcong Miao, Jialin Li  
**Category**: cs.DC  
**Published**: 2026-02-13  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.11456v1  

#### Abstract
LLM post-training with reinforcement learning (RL) requires frequent synchronization of large model parameters between the trainer and distributed rollout actors. High-throughput RL post-training therefore relies on dedicated RDMA HPC clusters, an infrastructure cost most organizations cannot absorb...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*RL over Commodity Networks: Overcoming the Bandwidth Barrier with Lossless Sparse Deltas*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLM）的强化学习（**Reinforcement Learning, RL**）后训练依赖于在 **Trainer** 和分布式 **Rollout Actors** 之间频繁同步大规模模型参数。当前主流系统依赖高带宽、低延迟的 **RDMA HPC 集群**，这类基础设施成本高昂，大多数学术机构和初创公司难以负担。

然而，使用标准以太网和广域网（**WAN**）连接的“松散耦合”GPU 资源（如跨云、跨区域、跨机构）虽然经济灵活，但其有限的带宽（通常为 1-10 Gbps）导致全权重广播（full-weight broadcast）耗时极长（例如，一个 8B 模型传输超过 100 秒），远超生成时间，造成 GPU 利用率极低。

### 提出的新方法：SparrowRL
本文提出 **SparrowRL**，一个专为商品网络（commodity networks）设计的高性能 RL 训练系统，其核心思想是利用 RL 更新的**高度稀疏性**。

#### 关键创新点：
1. **Lossless Sparse Delta Checkpoints（无损稀疏增量检查点）**  
   - 观察到 RL 微调中每步仅有约 **1% 的参数元素发生变化**。
   - 不再传输完整模型权重，而是将更新表示为**稀疏增量（sparse delta）**，仅包含变化的参数及其精确位置。
   - 采用 **delta-encoded variable-length indexing** 进一步压缩索引元数据，实现无损（lossless）且高效的存储与传输。

2. **Streaming Delta Transfer Protocol（流式增量传输协议）**  
   - 将稀疏增量分段，通过多路并行 TCP 流进行条带化（striping）传输，提高链路利用率。
   - 实现 **cut-through forwarding**：在 Trainer 完成整个增量提取前就开始传输已编码的段，实现提取与传输的流水线重叠。
   - 引入 **Relay-based fanout**：每个区域指定一个 Relay 节点接收增量，并在区域内转发给其他 Actors，减少跨区域通信开销。

3. **Heterogeneity-aware Scheduling & Lease-based Fault Tolerance**  
   - **异构感知调度**：根据每个 Actor 的实时吞吐量和网络带宽动态分配任务批次大小，确保所有 Actor 在一个策略迭代周期内完成。
   - **基于租约的容错**：使用时间限定的租约管理任务，自动检测失败节点并重新分配工作，避免全局阻塞。

### 相比现有方法的优势
- **打破对 RDMA 的依赖**：使在低成本、跨云、地理分布的 GPU 资源上进行高效 RL 训练成为可能。
- **保持精度**：采用无损压缩，不进行量化或丢弃信息，保证模型更新的比特级精确性（bit-exact）。
- **高吞吐与高性价比**：显著降低传输负载，提升端到端吞吐量，并实现更高的 **tokens per dollar**。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Hendrycks MATH**：数学推理基准。
- **GSM8K**：小学数学应用题数据集。
- **DeepScaleR**：用于扩展 RL 推理能力的基准。

### 模型
- **Qwen3 系列模型**：4B、8B、14B 参数规模。

### 实验设置
- **部署环境**：地理分布式 GPU 部署，覆盖北美、欧洲、亚太等地区。
- **网络条件**：使用跨云 WAN 链接，带宽在 500 Mbps 至 1 Gbps 之间波动。
- **硬件配置**：
  - **Trainer**：在美国，使用 H100 GPU。
  - **Rollout Actors**：分布在加拿大等地，使用 A100 或 L40 GPU。
- **RL 算法**：使用 **GRPO** 算法进行训练。

### 评估指标
- **Throughput**：系统每秒处理的 token 数量（tokens/s）。
- **Training Step Time**：单个优化器步骤的墙钟时间（wall-clock time）。
- **Tokens per Dollar (tokens/$)**：单位成本下的吞吐量，衡量成本效益。

### 基线方法对比
1. **Ideal-SingleDC**：理想化的单数据中心 RDMA 基线（800 Gbps RDMA + NVLink），作为性能上限。
2. **PrimeRL-Full**：在地理分布环境下广播完整模型权重。
3. **PrimeRL-MultiStream**：在多路 TCP 流上传输完整模型权重，提升带宽利用率。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 方法 | 吞吐量 (tokens/s) | 步骤时间 (s) | Tokens/$ (×10⁶) |
|------|------|-------------------|--------------|------------------|
| Qwen3-8B | SparrowRL | ~15.9k | ~12 | ~3.60 |
| Qwen3-8B | Ideal-SingleDC | ~16.5k | ~11 | ~2.99 |
| Qwen3-14B | SparrowRL | ~14.0k | ~15 | ~2.12 |
| Qwen3-14B | Ideal-SingleDC | ~14.8k | ~12 | ~1.33 |

### 与基线方法的对比结果
- **吞吐量提升**：
  - 相比 **PrimeRL-Full**，SparrowRL 在 Qwen3-4B 上提升 **2.4–3.7×**，在 Qwen3-14B 上提升 **7.7–9.5×**。
- **步骤时间大幅缩短**：
  - PrimeRL-Full 在 Qwen3-14B 上步骤时间超过 **500 秒**，而 SparrowRL 仅为 **~15 秒**。
- **接近理想性能**：
  - SparrowRL 的吞吐量达到 Ideal-SingleDC 的 **91.09%–98.69%**，即仅落后 **1.31%–8.91%**。
- **成本效率优势显著**：
  - 在相同吞吐水平下，SparrowRL 的 **tokens per dollar** 比保留的 RDMA 集群高出 **1.21–1.59×**。

### 消融实验结果
- **稀疏编码优化**：
  - 使用 `uint8` delta 编码相比 `int32/64`，将每步增量从 **414 MB** 压缩至 **202 MB**，传输时间从 **9.22s** 降至 **4.71s**。
- **多流传输（Multi-Stream）**：
  - 启用多流后，传输时间进一步降至 **2.90s**，提升链路利用率。
- **Relay 机制**：
  - 在加拿大-澳大利亚部署中，使用 Relay 使吞吐量提升 **4.4%–13.9%**，尤其在 DeepScaleR 上增益更大。
- **异构调度**：
  - 在混合 A100 + L40 的异构环境中，异构感知调度相比均匀分配，吞吐量提升 **26.4%–35.5%**。

---

## 4. 关键结论和发现

### 主要发现
1. **RL 参数更新具有高度稀疏性**：每步仅有约 **1% 的参数元素发生变化**，这一特性是 SparrowRL 设计的基础。
2. **稀疏增量可有效克服带宽瓶颈**：通过传输稀疏 delta 而非完整权重，SparrowRL 将每步传输负载减少 **79×**（Qwen3-8B）。
3. **商品网络上的 RL 训练是可行的**：SparrowRL 成功弥合了商品网络与理想 RDMA 集群之间的性能差距，使其接近理想性能的 **91% 以上**。
4. **成本效益显著**：利用按需跨云 GPU，SparrowRL 实现了比专用 RDMA 集群 **更高性价比** 的训练方案。

### 方法的局限性
- **依赖于 RL 更新的稀疏性**：若未来 RL 算法导致更密集的更新，稀疏性假设可能不再成立。
- **CPU 开销**：稀疏增量的提取和编码带来额外的 CPU 开销，虽被掩盖但仍存在。
- **中心化 Trainer 架构**：仍依赖一个高性能 Trainer 节点，未完全去中心化。

### 未来工作方向
- **结合分布式训练技术**：将 SparrowRL 与跨数据中心的梯度聚合方法（如 MAST、Varuna）结合，支持更大规模的训练。
- **支持更多 RL 算法**：验证 SparrowRL 在其他 RLHF 算法（如 PPO、DPO 变体）上的通用性。
- **完全去中心化架构**：探索无需中心 Trainer 的全去中心化 RL 训练框架。
- **动态稀疏性感知**：根据训练阶段动态调整稀疏编码策略，进一步优化性能。

--- 

> **总结**：SparrowRL 通过洞察 RL 更新的稀疏性，提出了一套面向商品网络的高效 RL 训练系统，在不牺牲精度的前提下，显著降低了对高带宽网络的依赖，为学术界和中小企业提供了进行前沿 LLM RL 研究的实用路径。

</details>

---

### 5. [MAPLE: Modality-Aware Post-training and Learning Ecosystem](https://arxiv.org/abs/2602.11596)

**Authors**: Nikhil Verma, Minjung Kim, JooYoung Yoo, Kyung-Min Jin, Manasa Bharadwaj, Kevin Ferreira, Ko Keun Kim, Youngjoon Kim  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.11596v1  

#### Abstract
Multimodal language models now integrate text, audio, and video for unified reasoning. Yet existing RL post-training pipelines treat all input signals as equally relevant, ignoring which modalities each task actually requires. This modality-blind training inflates policy-gradient variance, slows con...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MAPLE: Modality-Aware Post-training and Learning Ecosystem

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前的 **Multimodal Language Models (MLMs)** 在进行强化学习（RL）后训练时，普遍采用“模态盲”（modality-blind）策略，即假设所有输入模态（如视频、音频、字幕）对所有任务都同等重要。这种做法忽略了现实场景中不同任务实际依赖的模态组合是不同的。例如：

- 有些问题仅需视觉（V）即可回答；
- 有些需要音视频结合（VA）；
- 有些则必须依赖全部三种模态（VAS）。

这种统一处理方式导致以下问题：

- **梯度方差增大**：混合异构模态组合会引入不同奖励尺度和噪声特性，增加 policy gradient 的方差。
- **收敛速度慢**：训练不稳定，优化效率低。
- **鲁棒性差**：在测试时遇到缺失模态（missing-at-test）或部分观测的情况时表现急剧下降。

---

### 提出了什么新方法或新思路

作者提出了 **MAPLE** ——一个完整的 **模态感知后训练与学习生态系统**（Modality-Aware Post-training and Learning Ecosystem），包含三大核心组件：

#### （1）MAPLE-bench：首个显式标注最小必要模态的基准

- 数据集覆盖七种模态组合：`{V, A, S, VA, VS, AS, VAS}`。
- 每个样本都带有 **Required Modality Tags (RMTs)**，明确标注解决该任务所需的最小模态子集。
- 支持两种任务：
  - **MAPLE-QA**：多项选择题问答（判别式）
  - **MAPLE-Caption**：开放式多模态描述生成（生成式）
- 引入了动态模态扰动版本 **MAPLE-QA+**，用于评估模型在模态冗余或缺失下的泛化能力。

#### （2）MAPO：模态感知策略优化框架（Modality-Aware Policy Optimization）

- 核心思想：按 RMT 对 batch 进行分层（stratified batching），并在每个子组内独立归一化优势函数（advantage normalization）。
- 数学上证明：  
  $$
  \text{Var}(g_{\text{MA}}) \leq \text{Var}(g_{\text{MU}})
  $$
  即模态感知训练能有效降低梯度方差。
- 优化设计四轴分析：
  - **Loss aggregation**：推荐 sample-level 聚合优于 token-level。
  - **Clipping**：采用非对称裁剪（asymmetric clipping, ε⁺=0.3, ε⁻=0.2）提升探索稳定性。
  - **Sampling**：早期过滤零方差样本显著加速训练。
  - **Curriculum learning**：从单模态到多模态逐步进阶训练。

#### （3）自适应训练策略（Adaptive Training）

- **Adaptive weighting**：基于历史 KL 散度衡量每类 RMT 的难度，并动态调整其损失权重（`adpw`），使难任务获得更多更新幅度。
- **Adaptive curriculum**：根据 KL 动态调度训练顺序，优先训练表现较差的任务（`adpcur`）。

---

### 相比现有方法的优势

| 维度 | MAPLE 优势 |
|------|-----------|
| **准确性** | 缩小了 uni-/multi-modal 准确率差距达 **30.24%** |
| **收敛速度** | 比全信号训练快 **3.18×** |
| **训练效率** | 更低的 step-time 和更高的 wall-clock 效率 |
| **鲁棒性** | 在模态缺失、冗余等真实分布偏移下保持稳定性能 |
| **可扩展性** | 不依赖额外 critic 或辅助正则项，易于部署 |

---

## 2. 核心实验方法和设置

### 使用的数据集

| 数据集 | 类型 | 规模 | 特点 |
|-------|------|-----|------|
| **MAPLE-QA-train** | QA | 47,893 样本 | 来自 Daily-Omni，带 RMT 标签，4 选 1 多项选择 |
| **MAPLE-QA-eval** | QA | 5,001 样本 | 人工审核，跨 68 个视频 |
| **MAPLE-Caption-train** | Captioning | 5,120 样本 | 自动生成 |
| **MAPLE-Caption-eval** | Captioning | 5,348 样本 | 人工校准，每 RMT 均衡为 764 样本 |
| **MAPLE-QA+** | 扰动增强版 | 137,313 总样本 | 包含 exact/superset/deficit 三种模态配置，引入 “None” 选项以测试 abstention 能力 |

---

### 实验设置

- **模型**：Qwen2.5-Omni-3B（原生支持文本、音频、视频输入）
- **序列长度**：10,240 tokens（输入 8,192 + 输出 2,048）
- **优化器**：AdamW，学习率 $2 \times 10^{-6}$，batch size 256（mini-batch 32）
- **RL 设置**：
  - 使用 **veRL** 库实现 GRPO
  - 每个 prompt 生成 G=8 个响应（temperature=1.0, top-p=1.0）
  - 无 KL 正则化，使用对称裁剪（±0.2）作为 baseline
- **硬件**：4× NVIDIA H100-80GB

---

### 评估指标

| 指标 | 定义 | 用途 |
|------|------|------|
| **Pass@1 Accuracy** | 5 次采样中至少一次正确的比例 | QA 任务主指标 |
| **LLM-as-Judge Score** | 使用 GPT-4o 对生成 caption 打分，综合 missing/hallucination/fusion 三个维度 | Captioning 主指标 |
| **Modality Gap (%)** | Uni/Bi/Tri 模态组之间的平均准确率差异 | 衡量模态间不平衡程度 |
| **Training Efficiency** | 每步耗时（秒/step），归一化 batch size | 反映训练成本 |
| **Fusion Gain** | 多模态输出优于最佳单模态的比例 | 衡量真正融合能力 |
| **Cross-tag Disparity Heatmap** | 各 RMT 之间性能差异的热力图 | 可视化鲁棒性 |

---

### 基线方法对比

| 方法 | 描述 |
|------|------|
| **Zero-shot** | 未经后训练的原始模型 |
| **MUPO**（Modality-Unaware PO） | 接收完整 `(V, A, S)` 输入，混合所有模态批次训练（标准 baseline） |
| **MAPO**（本文提出） | 按 RMT 分层训练，保留各模态组合结构 |
| **MAPO + adpw/adpcur** | 加入自适应加权与课程调度 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1–3）

#### ✅ MAPLE-QA 结果（Qwen2.5-Omni-3B）

| 方法 | 平均 Pass@1 | Modality Gap (%) | Step Time (s) |
|------|------------|------------------|---------------|
| Zero-shot | 39.38% | 8.72 | – |
| MUPO（baseline） | 58.58% | 2.27 | 523.28 |
| MAPO（基础） | 58.68% | 2.11 | 389.11 |
| **Full-recipe (最优)** | **58.72%** | **1.74** | **164.72** |

> 💡 **结论**：尽管 MAPO 接收更少模态信号，仍略优于 MUPO，且训练速度快 **3.18×**，模态差距缩小近 24%。

#### ✅ MAPLE-Caption 结果

| 方法 | 平均 LLM-as-Judge | Fusion Gain |
|------|--------------------|-------------|
| MUPO | 67.67% | – |
| MAPO | 73.88% | 18.19% |
| **MAPO + adpw + adpcur** | **74.00%** | **30.24%** |

> 💡 **结论**：自适应策略进一步提升生成质量，多模态融合增益大幅提升。

#### ✅ 自适应策略消融实验（Table 2 & 3）

| 配置 | MAPLE-QA Avg | MAPLE-Caption Avg |
|------|--------------|------------------|
| MAPO | 58.68% | 73.88% |
| +adpw | 58.98% | 72.89% |
| +cur | 59.05% | 72.07% |
| **+adpw + adpcur** | **59.82%** | **74.00%** |

> 📌 **发现**：
> - `adpw` 显著提升硬任务（如 V, VA）表现，缓解 scale imbalance。
> - `adpcur` 提高整体一致性，减少跨标签差异（见 Fig. 4–5）。

#### ✅ 消融实验：数据增强与泛化能力（Table 4）

| 训练数据 | 平均准确率 |
|---------|----------|
| MAPLE-QA（exact only） | 51.90% |
| **MAPLE-QA+**（exact/superset/deficit） | **76.99%** |

> ✅ 模型不仅能泛化到未见过的模态组合，还能正确判断何时应 abstain（选择 “None”），说明具备真正的 **modality-aware reasoning** 能力。

#### ✅ 消融实验：Contrastive Reward Weighting (CRW)

应用于 captioning 任务，防止模式坍缩（mode collapse）：

| 指标 | w/o CRW | w/ CRW |
|------|--------|-------|
| Intra-group dispersion | 0.35 → **0.36** | ↑ 多样性 |
| Inter-group separation | 0.41 → **0.46** | ↑ 区分度 |
| t-SNE 可视化 | 簇重叠严重 → 分布更分散（Fig. 16） | 有效缓解表示坍缩 |

---

## 4. 关键结论和发现

### 主要发现

1. **模态分层训练至关重要**：
   - 忽视模态依赖关系会导致梯度方差大、收敛慢、鲁棒性差。
   - MAPO 通过 stratified batching 和 group-normalized advantages 显著改善这些问题。

2. **最优训练配方已确立**：
   - **Sample-level loss aggregation**
   - **Asymmetric clipping**
   - **Early zero-variance filtering**
   - **Curriculum learning by complexity**

3. **自适应机制进一步突破瓶颈**：
   - `adpw` 解决了“易任务主导”的梯度倾斜问题。
   - `adpcur` 控制更新时机，确保难任务不被淹没。

4. **真正实现了模态感知推理**：
   - 在 MAPLE-QA+ 上表现出色，能识别信息不足并主动 abstain。
   - 不再是记忆模态-查询配对，而是学会根据证据强度做出决策。

5. **显著提升多模态融合能力**：
   - Fusion Gain 从 18.19% 提升至 30.24%，表明模型真正学会了整合互补信号。

---

### 方法的局限性

| 局限性 | 说明 |
|--------|------|
| **依赖高质量 RMT 标注** | 当前 RMT 由人工或强模型标注，自动化打标仍有误差（文中提到约 74.2% 需人工修正）。 |
| **CRW 仅适用于连续奖励任务** | 如 captioning，无法直接用于离散 QA 任务。 |
| **未考虑实时模态丢包建模** | 当前训练假设模态完全可用或完全缺失，尚未模拟部分遮挡、延迟等复杂情况。 |
| **计算开销略有上升** | 尽管总时间下降，但因分组管理增加了调度复杂度。 |

---

### 未来工作方向

1. **自动 RMT 推理机制**：让模型自行推断所需模态，而非依赖外部标注。
2. **在线模态选择策略**：构建轻量级门控模块，在推理时动态决定激活哪些模态。
3. **面向边缘设备的稀疏激活训练**：结合 MBQ 等量化技术，打造高效节能的多模态 agent。
4. **扩展至更多模态**：加入触觉、位置、传感器等新型输入通道。
5. **安全与可控性研究**：防止模型滥用冗余模态生成误导性输出。

---

## 总结

📌 **MAPLE 是首个将“模态感知”贯穿于数据、算法、训练全流程的 RL 后训练生态系统**。它不仅提升了性能与效率，更重要的是增强了模型在真实世界中面对模态缺失、冗余、干扰时的 **鲁棒性和可信度**。该工作揭示了一个深刻洞见：

> **“不是越多模态越好，而是要用对模态。”**

MAPLE 为下一代 **production-ready multimodal agents** 提供了一套完整、实用、可复现的技术蓝图。

</details>

---

### 6. [TSR: Trajectory-Search Rollouts for Multi-Turn RL of LLM Agents](https://arxiv.org/abs/2602.11767)

**Authors**: Aladin Djuhera, Swanand Ravindra Kadhe, Farhan Ahmed, Holger Boche  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.11767v1  

#### Abstract
Advances in large language models (LLMs) are driving a shift toward using reinforcement learning (RL) to train agents from iterative, multi-turn interactions across tasks. However, multi-turn RL remains challenging as rewards are often sparse or delayed, and environments can be stochastic. In this r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：TSR: Trajectory-Search Rollouts for Multi-Turn RL of LLM Agents**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
多轮次强化学习（multi-turn RL）在训练大语言模型（LLM）智能体时面临以下挑战：
- **稀疏或延迟奖励**（Sparse/Delayed Rewards）：许多任务中，只有在任务完成时才获得正向反馈，中间步骤无有效信号，导致策略梯度估计方差高。
- **不可逆陷阱**（Irreversible Traps）：早期错误可能导致状态无法恢复（如Sokoban中将箱子推入死角），后续动作无效。
- **模式崩溃**（Mode Collapse / Echo Trap）：由于低质量rollout导致梯度剧烈波动，模型性能突然下降。
- **探索与利用失衡**：朴素的随机采样难以平衡高质量轨迹生成与多样性。

### **提出的新方法：TSR (Trajectory-Search Rollouts)**
TSR 是一种**训练阶段的rollout生成框架**，其核心思想是：
> 将原本用于推理阶段的**test-time scaling**技术（如best-of-N、beam search等）迁移至**训练阶段的rollout生成过程**，通过轻量级树状搜索构建更高质量的轨迹。

#### **关键技术机制**
- 在每一轮决策中，并非直接从当前策略 $\pi_\theta$ 采样单一动作，而是：
  1. 生成候选动作集合（candidate action set）
  2. 利用任务相关的评分函数 $S(\cdot)$ 对动作或前缀进行打分
  3. 应用搜索策略（如beam search）选择最优路径
- 支持多种搜索策略：
  - **Best-of-N**：独立生成N条完整轨迹，选最高回报的一条
  - **Beam Search**：每步保留B个最佳部分轨迹，逐步扩展
  - **Shallow Lookahead**：向前模拟D步，评估长期收益再做决策

#### **设计优势**
- **Optimizer-Agnostic**：不改变底层优化目标（如PPO、GRPO），仅替换rollout生成方式，可无缝集成到现有RL框架。
- **提升rollout质量**：避免“不幸”的低质量轨迹主导训练信号。
- **稳定学习动态**：减少梯度方差，防止Echo Trap。
- **提高样本效率**：生成的轨迹更具代表性，加速收敛。

### **相比现有方法的优势**
| 方法 | 局限性 | TSR 的改进 |
|------|--------|------------|
| **Naive Rollout Sampling** | 易陷入局部最优，受随机性影响大 | 引入搜索机制主动探索高质量路径 |
| **Instance Filtering** | 只筛选任务实例，不优化单个轨迹内部决策 | 同时优化**任务多样性**与**轨迹质量** |
| **Rejection Sampling (e.g., RAFT, GFPO)** | 事后过滤，浪费计算资源 | 事前引导，高效生成优质轨迹 |
| **Test-Time Search** | 推理开销高，部署延迟大 | 将搜索“蒸馏”进模型权重，**训练时花一次钱，推理时永久受益** |

---

## **2. 核心实验方法和设置**

### **使用的数据集与环境**
在三个具有代表性的多轮交互环境中进行评估：
| 环境 | 类型 | 特点 |
|------|------|------|
| **Sokoban** | 确定性逻辑谜题 | 即时奖励，需避免死锁；测试精确规划能力 |
| **FrozenLake** | 随机导航游戏 | 动作有滑动噪声，奖励极度稀疏（仅终点+1） |
| **WebShop** | 网页购物代理 | 长周期任务，成功信号延迟，需语义理解与页面导航 |

### **实验设置**
- **模型**：Qwen2.5-0.5B 和 Qwen2.5-3B；WebShop仅用3B版本
- **RL算法**：
  - Sokoban 使用 **PPO**
  - FrozenLake & WebShop 使用 **GRPO**（更适合稀疏奖励）
- **训练流程**：
  - 每轮采样 P=16 个任务组，每组生成 L=16 条轨迹
  - 最大回合数 K=5
  - 使用 **instance-level filtering**（保留reward标准差最高的p=25%的任务组）作为基础增强手段
- **TSR 参数配置**：
  - **Best-of-N**: N=28 → 保留16条
  - **Beam Search**: 分支因子 M=4，束宽 B=2
  - **Lookahead**: M=4, B=2, 深度 D=2

### **评估指标**
| 指标 | 描述 |
|------|------|
| **Success Rate** | 在固定验证集（256 prompts）上的任务完成率 |
| **Average Response Length** | 平均生成token数，衡量推理简洁性 |
| **Average Interaction Turns** | 完成任务所需的平均交互轮次 |
| **Rollout Entropy** | 输出熵，反映探索程度 |
| **Gradient Norm** | 梯度范数，用于检测训练稳定性（是否出现尖峰） |
| **Wall-Clock Time** | 总训练耗时，分析实际开销 |

### **基线方法对比**
- **Base Model**：仅监督微调后的初始模型
- **Instance Filtering**：当前主流稳定训练方法，仅做任务筛选
- **TSR variants**：Best-of-N, Beam Search, Shallow Lookahead

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（最终成功率）**

#### **表1：Sokoban 与 FrozenLake 结果**
| 方法 | Sokoban (0.5B) | Sokoban (3B) | FrozenLake (0.5B) | FrozenLake (3B) |
|------|----------------|--------------|-------------------|-----------------|
| Instance Filtering | 29.0% | 43.7% | 19.7% | 48.7% |
| TSR (Best-of-N) | 33.3% | 47.7% | 25.0% | 55.0% |
| TSR (Lookahead) | 36.1% | 49.5% | 27.8% | 57.0% |
| **TSR (Beam Search)** | **38.3%** | **52.3%** | **30.0%** | **60.7%** |

#### **表2：WebShop (Qwen2.5-3B) 结果**
| 方法 | Success Rate | Resp. Len ↓ | Turns ↓ |
|------|-------------|-------------|---------|
| Base Model | 3.0% | 747 | 7.7 |
| Instance Filtering | 70.3% | 519 | 6.8 |
| TSR (Best-of-N) | 77.3% | 504 | 6.5 |
| TSR (Lookahead) | 82.3% | 475 | 6.1 |
| **TSR (Beam Search)** | **85.3%** | **453** | **5.8** |

> ✅ **最大绝对增益达15%**（WebShop上从70.3%→85.3%）

---

### **与基线方法对比结果**
- 在所有任务和模型规模下，**所有TSR变体均显著优于Instance Filtering基线**。
- **Beam Search 表现最强**，尤其在复杂长周期任务（WebShop）上领先明显。
- **Lookahead 次之**，适合确定性环境（如Sokoban）。
- **Best-of-N 提升有限**，因其仍依赖完整轨迹生成后再筛选，无法纠正中途错误。

---

### **消融实验结果**

#### **(1) 搜索预算扩展分析（Scaling Search Budget）**
- 增加束宽 **B 从1到2** 带来最大提升（+1.7% on Sokoban 3B）
- 继续增加采样数 M（从2→6）带来**边际收益递减**
- 表明：**适度的搜索即可获得大部分收益**，无需过度计算

#### **(2) 与更大模型比较（Table 3）**
| 模型 | Sokoban | FrozenLake |
|------|--------|-----------|
| GPT-4o (zero-shot) | 27.7% | 26.6% |
| Qwen2.5-72B (zero-shot) | 19.5% | 23.8% |
| **Qwen2.5-0.5B + TSR (Beam)** | **38.3%** | **30.0%** |

> 🔥 **0.5B小模型 + TSR 超越 GPT-4o 和 72B巨模型！**

#### **(3) 推理效率提升**
- 所有TSR方法均生成**更短响应**和**更少交互轮次**
- Beam Search 最优：平均减少 **~20% token数** 和 **~20% turns**
- 示例显示：TSR-beam能直接命中正确商品并购买，而基线反复循环点击过滤器

#### **(4) 训练稳定性分析**
- **梯度范数平稳无尖峰** → 未发生Echo Trap
- **rollout entropy平滑下降** → 探索-利用平衡良好
- **收敛更快**：多数TSR方法在更少训练步内达到80%性能

#### **(5) 实际训练时间分析（Wall-Clock Time）**
尽管每步计算成本更高，但由于**收敛速度加快**，**总训练时间接近甚至优于基线**（见App. F.3表格）。  
例如在WebShop上，TSR(Beam)虽耗时更长（14.94h vs 6.24h），但**提前35步收敛**，单位时间效率更高。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **训练时引入轻量搜索可大幅提升多轮RL性能**：即使只增加一次性的训练计算，也能换来显著的性能增益（最高+15%）和更强的泛化能力。
2. ✅ **TSR是通用且模块化的插件式方法**：兼容PPO/GRPO等多种optimizer，易于集成进现有框架（如RAGEN）。
3. ✅ **Beam Search是最有效的搜索策略**：因其能在每一步维持多个候选路径，具备容错能力和长期规划潜力。
4. ✅ **小模型+TSR可超越大模型**：证明了**训练数据质量 > 模型参数量**的趋势，在特定任务上实现“以巧胜力”。
5. ✅ **不仅提效还提质**：TSR不仅能提高成功率，还能让模型学会更简洁高效的交互行为，降低推理延迟。

### **方法的局限性**
- **依赖任务特定评分函数**（proxy score）：在WebShop和FrozenLake中需人工设计启发式分数（如距离目标曼哈顿距离、文本相似度），可能不够鲁棒。
- **训练计算成本上升**：虽然总体收敛快，但单步rollout生成更慢，对GPU资源要求更高。
- **搜索深度受限**：shallow lookahead因计算限制只能看几步，难以应对极长周期任务。
- **未完全解决探索问题**：TSR侧重exploitation，需配合instance filtering等机制保障diversity。

### **未来工作方向**
- 设计**可学习的评分函数**（learned scorer）替代手工规则，适应更多任务。
- 探索**自适应搜索预算分配**：根据任务难度动态调整beam width或lookahead depth。
- 结合**分层RL**或**记忆机制**，支持超长周期任务。
- 系统研究TSR与其他前沿方法（如Process Reward Modeling, PRM）的协同效应。
- 将TSR应用于真实世界工具使用（tool-using）、代码生成、机器人控制等场景。

---

> 📌 **一句话总结**：  
> **TSR通过将test-time scaling的思想前移到训练阶段的rollout生成环节，用少量额外训练计算换取显著的性能提升、更强的稳定性以及更优的推理效率，是一种简单、通用且强大的multi-turn RL增强范式。**

</details>

---

### 7. [Learn from Your Mistakes: Self-Correcting Masked Diffusion Models](https://arxiv.org/abs/2602.11590)

**Authors**: Yair Schiff, Omer Belhasin, Roy Uziel, Guanghan Wang, Marianne Arriola, Gilad Turok, Michael Elad, Volodymyr Kuleshov  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.11590v1  

#### Abstract
Masked diffusion models (MDMs) have emerged as a promising alternative to autoregressive models, enabling parallel token generation while achieving competitive performance. Despite these advantages, MDMs face a fundamental limitation: once tokens are unmasked, they remain fixed, leading to error acc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learn from Your Mistakes: Self-Correcting Masked Diffusion Models*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
**Masked Diffusion Models (MDMs)** 虽然在离散数据生成中表现出色，具备并行生成能力，但存在一个根本性缺陷：**一旦某个 token 被解码（unmasked），其值就固定不变**。这导致早期生成错误无法被修正，随着生成过程推进，错误不断累积，最终导致样本质量下降（distributional drift）。

### 提出的新方法：Progressive Self-Correction (ProSeCo)
作者提出了一种名为 **Progressive Self-Correction (ProSeCo)** 的新框架，其核心思想是：
- **将模型自身的输出视为“带噪声”的数据**，其中的错误即为一种“corruption”。
- 在训练阶段，**复用 MDM 去噪网络（denoiser）的输出作为输入，训练同一个模型学会“纠正”这些潜在的错误**。
- 在推理阶段，**在标准的 unmasking 步骤之间插入额外的“corrective refinement”步骤**，允许模型动态修改已生成的 token。

### 相比现有方法的优势
- **统一的训练目标**：通过一个简单的交叉熵（cross-entropy）损失项扩展标准 MDM 目标函数，实现“去噪”和“纠错”能力的联合训练。
- **权重共享（Weight Tying）**：纠错器（corrector）和去噪器（denoiser）共享同一套参数，无需引入额外的模型，节省内存。
- **灵活的推理控制**：用户可通过调整 `corrector frequency` 和 `steps per loop` 来权衡生成速度与质量。
- **显著提升效率-质量权衡**：相比标准 MDM，ProSeCo 可实现 **2-3 倍更快的采样速度**而质量不降，或通过增加计算量进一步提升质量（最高达 **~1.3x 改进**）。

---

## 2. 核心实验方法和设置

### 数据集
- **数学与代码任务**：
  - **rStar-Coder** 和 **OpenMathInstruct-2** 用于对 LLaDA-Base 8B 模型进行监督微调（SFT）。
  - 评测基准：**HumanEval**, **MBPP**（代码生成）；**GSM8K**, **Minerva**（数学推理）。
- **引导分子设计**（Guided Molecule Design）：
  - **QM9** 数据集，使用 SMILES 字符串表示分子。
  - 优化目标：**环数（ring count）** 和 **药物相似性（QED）**。
- **无条件文本生成**（Unconditional Text Generation）：
  - **OpenWebText (OWT)** 数据集。

### 实验设置和评估指标
- **模型架构**：基于 LLaDA-Base 8B 和 DiT-style backbone。
- **训练**：使用 ProSeCo 的扩展目标函数进行训练，包含标准 MDM 损失和自纠错损失。
- **采样算法**：在标准 MDM 采样过程中，按设定频率 `w` 插入 `S` 步的纠错循环（Algorithm 3）。
- **评估指标**：
  - **Pass@1 准确率**（HumanEval, MBPP, GSM8K, Minerva）。
  - **MAUVE**（衡量生成文本与人类文本的分布差距）。
  - **生成困惑度（Gen. PPL）** 和 **序列熵（Entropy）**（衡量质量和多样性）。
  - **有效样本数**（Valid, Unique, Novel）及其平均属性值（分子设计）。

### 基线方法对比
- **Off-the-shelf 模型**：Llama3.1-Instruct, LLaDA1.5。
- **MDM 基线**：LLaDA-Base, LLaDA-Instruct。
- **带纠错机制的 MDM**：ReMDM, PRISM。
- **其他生成范式**：Autoregressive (AR) 模型, Uniform Categorical Noise Diffusion (UDLM)。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比
在 **Table 1** 中，ProSeCo 表现全面领先：
- **仅使用 SFT 训练（不启用采样纠错）**：ProSeCo SFT 已超越所有基线（如 Vanilla SFT 在 HumanEval 上从 48.17 提升至 52.44）。
- **结合 ProSeCo 采样**：性能大幅提升，在 HumanEval 上达到 **62.20**，MBPP 达到 **50.20**，GSM8K 达到 **82.18**，Minerva 达到 **35.10**，**超越同规模 AR 模型（Llama3.1-Instruct）在 3/4 任务上的表现**。

### 效率-质量权衡分析（Figure 3）
- **Fast 模式**：通过增加并行解码，减少 unmasking 步骤，并辅以纠错，可实现 **2-3 倍加速**（NFEs 显著降低）且保持甚至超过基线准确率。
- **Balanced 模式**：在适度增加计算量下，获得最佳权衡。
- **Max 模式**：通过大幅增加推理时计算量（inference-time compute scaling），可将性能推至最高，证明了 ProSeCo 具备良好的**可扩展性**。

### 消融实验结果
- **纠错预算配置**（Appendix C.3, Figure 8）：
  - 在高并行度（快速采样）场景下，需要更频繁的纠错（`w ≥ T/16`）来抵消质量下降。
  - 对于固定的总纠错步数，**更频繁但更短的纠错循环通常更有效**。
- **平行解码的帕累托前沿**（Figure 4）：标准 MDM 随着并行度增加质量急剧下降，而 ProSeCo 能有效恢复错误，显著改善了平行解码与质量之间的帕累托前沿。

---

## 4. 关键结论和发现

### 主要发现
1. **错误纠正是提升 MDM 性能的关键**：允许模型在生成过程中“自我修正”已被证明是解决错误累积问题的有效途径。
2. **ProSeCo 极大地改进了效率-质量权衡**：它不仅能在更少的函数调用（NFEs）下达到更高性能，还能通过增加推理计算量来持续提升质量，这是标准 MDM 无法做到的。
3. **方法通用性强**：在条件生成（代码、数学）、引导生成（分子设计）和无条件生成（文本）等多种任务上均取得一致且显著的提升。

### 局限性
- **训练成本增加**：由于需要额外的前向传播来计算纠错损失，**训练时的计算开销有所增加**，尽管作者认为这一代价在下游任务收益面前是值得的。
- **依赖 argmax 采样**：当前方法依赖于 argmax 采样来构建纠错输入，可能限制了其探索能力。

### 未来工作方向
- **分离纠错与去噪模型**：探索解除权重绑定（weight untying），或为纠错和去噪设计完全独立的网络骨干。
- **更复杂的纠错策略**：开发更 sophisticated 的方案来联合调度纠错和解码步骤，例如基于置信度的动态决策。
- **扩展到其他模态**：将 ProSeCo 框架应用于图像、音频等其他领域的离散扩散模型。

</details>

---

### 8. [Improved state mixing in higher-order and block diagonal linear recurrent networks](https://arxiv.org/abs/2602.12021)

**Authors**: Igor Dubinin, Antonio Orvieto, Felix Effenberger  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.12021v1  

#### Abstract
Linear recurrent networks (LRNNs) and linear state space models (SSMs) promise computational and memory efficiency on long-sequence modeling tasks, yet their diagonal state transitions limit expressivity. Dense and nonlinear architectures (e.g., LSTMs) on the other hand are provably more expressive,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Improved State Mixing in Higher-order and Block Diagonal Linear Recurrent Networks**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
现代 **Linear Recurrent Networks (LRNNs)** 和 **State Space Models (SSMs)** 虽然在长序列建模中具有计算和内存效率优势，但由于其状态转移矩阵通常为**对角形式**，导致表达能力受限。相比之下，密集或非线性架构（如 LSTM）虽然表达力更强，但计算成本高昂。

本文旨在探索如何在保持高效性的前提下，通过**更丰富的状态混合机制**提升 LRNNs 的表达能力。

---

### **提出的新方法与新思路**
作者提出了两种新型结构化 LRNN 架构：

#### **(i) Higher-order Linear Recurrent Units (H-LRU)**
- 将传统的一阶递推推广到 **m 阶递推**，即当前状态由前 `m` 个隐藏状态加权组合而成：
  $$
  h_t = \sum_{i=1}^m a_{i,t} \odot h_{t-i} + a_{0,t} \odot v_t
  $$
- 这种高阶结构允许模型在时间维度上进行更复杂的记忆整合，增强了对长期依赖的建模能力。

#### **(ii) Block-Diagonal LRUs (BD-LRU)**
- 在通道维度上引入**块对角结构**，每个块内实现**全连接的状态转移矩阵**，从而支持密集的通道间信息混合。
- 每个 block 内部是一个 $m \times m$ 的稠密矩阵，不同 blocks 之间无交互。

---

### **相比现有方法的优势**
| 特性 | H-LRU | BD-LRU |
|------|-------|--------|
| **表达能力增强方式** | 时间维度上的多步历史混合 | 通道维度上的块内密集混合 |
| **参数效率** | 更高（尤其在压缩任务中） | 较低但性能更强 |
| **可扩展性** | 支持 moderate window size 扩展 | 支持 moderate block size 扩展 |
| **实现效率** | 平行扫描（parallel-scan）保持高效 | 同样支持 parallel-scan，吞吐量接近对角模型 |

此外，作者提出了一种**联合门控归一化策略（per-row L1-normalization）**，确保训练稳定性，并允许安全地扩大 window/block size。

---

## **2. 核心实验方法和设置**

### **使用的数据集**

#### **合成任务（Synthetic Tasks）**
基于 MAD benchmark (Mechanistic Architecture Design)，用于快速评估架构潜力：

1. **Compression Task**  
   - 输入随机 token 序列 → 编码为单个聚合 token → 解码还原原序列  
   - 测试模型的信息压缩与重建能力  

2. **Selective Copying Task**  
   - 复制特定位置的 token 到输出，忽略噪声  
   - 测试选择性时序集成能力  

3. **In-context Recall Task**  
   - 给定 key-value 对序列，根据 query 检索对应 value  
   - 测试上下文学习与动态检索能力  

4. **Permutation Composition Tasks (S3–S5)**  
   - 学习从输入序列到排列后输出的映射，基于对称群 $S_n$  
   - 测试复杂结构归纳与状态追踪能力  

5. **Chomsky Hierarchy Tasks**  
   - 包括 Parity、Cycle Navigation、Modular Arithmetic（带/不带括号）等  
   - 测试形式语言层级中的计算能力  

#### **真实语言建模任务**
- **FineWeb 数据集**  
  - 用于大规模语言建模实验，评估 perplexity 和 scaling behavior

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|--------|------|
| **模型配置** | 单层结构，最大隐藏维度 6144；H-LRU window size $m \in [1,16]$，BD-LRU block size $m \in [1,16]$ |
| **优化器** | AdamW，cosine learning rate schedule，初始 LR ∈ {0.001, 0.0005, 0.0001} |
| **评估指标** | - 分类任务：平均测试准确率（accuracy）<br>- 语言建模：perplexity<br>- 效率：FLOPs / throughput（tokens/sec） |
| **硬件平台** | NVIDIA A100/H100 GPUs |

---

### **基线方法对比**
- **Diagonal LRNNs**: Mamba2, DeltaNet, DeltaProduct4
- **经典 RNNs**: LSTM
- **其他结构化模型**: Deltanet (low-rank), Mamba (selective SSM)

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表1：合成任务综合表现（Accuracy）**
| Model | Recall | Copy | Compress | **Overall** |
|-------|--------|------|----------|------------|
| LSTM | 1.000 | 1.000 | 0.750 | **0.916** |
| Mamba2 | 1.000 | 0.807 | 0.720 | 0.842 |
| Deltanet | 1.000 | 0.892 | 0.782 | 0.892 |
| **BD-LRU m5 (ours)** | **1.000** | **0.985** | **0.782** | **0.922** ✅ |
| H-LRU m5 | 1.000 | 0.838 | 0.775 | 0.871 |

> ✅ **BD-LRU 在总体性能上超越所有 baseline，包括 LSTM**

---

#### **表2：Permutation Composition 任务表现**
| Model | S3 (10k) | S4 (50k) | **S5 (100k)** |
|-------|-----------|-----------|----------------|
| LSTM | 1.000 | 1.000 | 1.000 |
| Mamba2 | 0.660 | 0.430 | 0.260 |
| Deltanet | 1.000 | 0.470 | 0.140 |
| **BD-LRU m5 (ours)** | **1.000** | **1.000** | **1.000** ✅ |

> ✅ **BD-LRU m5 仅用 ~200K 参数即可完美解决 S5 任务，样本效率极高**

---

#### **语言建模（FineWeb）**
- **BD-LRU (m=4,8)** 在 210M 参数下达到最低 **perplexity**
- **H-LRU 参数效率更高**，但需增大 hidden dimension 来匹配参数量，导致计算开销上升
- **Moderate block size (m=2–8)** 表现最优，过大（m=16）反而下降

---

### **消融实验结果**

#### **归一化策略的影响（Fig. 2）**
- **L1-normalization（softmax/sigmoid）显著提升性能**
- 无归一化或 ReLU 归一化效果差
- 归一化使模型能稳定扩展 window/block size

#### **Selectivity 消融（Appendix F）**
- 移除 selectivity 后，在 selective copying、recall 等任务上性能崩溃
- 但在 compression 任务中，non-selective H-LRU 仍可通过大 window size 实现较好压缩，显示其强归纳偏置

#### **Block Size Scaling 分析**
- 性能随 block/window size 增加先升后降，**最佳值集中在 m=3–5**
- 过大的 block size 导致过拟合或训练不稳定
- BD-LRU 在 autoregressive 任务中 scaling 更优，H-LRU 在 compression 中更高效

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **状态混合结构比单纯增加宽度更能决定表达能力**
   - 结构化的 state mixing（时间或多通道）是提升 LRNN 表达力的关键
   - 宽度扩展无法弥补结构限制

2. ✅ **BD-LRU 是综合性能最强的架构**
   - 在 selective copying、permutation、language modeling 中均达到 SOTA
   - 具备强大的 sample efficiency 和 parameter utilization

3. ✅ **H-LRU 是最参数高效的架构**
   - 在 compression 任务中以更少参数超越 Mamba 和 LSTM
   - 高阶递推提供了优秀的压缩先验

4. ✅ **Moderate block/window sizes 最优**
   - m=2–8 提供最佳 expressivity-efficiency trade-off
   - 过大反而损害性能，尤其在压缩任务中

5. ✅ **Parallel-scan 实现维持高吞吐**
   - 尽管理论复杂度上升，但利用 block-diagonal 结构可将 parallel-scan 复杂度降至 $O(H m^3 \log T)$
   - 实际运行速度与 diagonal LRNNs 接近

---

### **方法的局限性**
1. **计算效率随 batch size 增加下降较快**
   - 相比高度优化的 Mamba，本方法在大 batch 下 GPU 利用率较低
2. **未覆盖所有 gate parameterization 空间**
   - 当前仅测试了 softmax/sigmoid/ReLU 等常见形式
3. **难以处理需要非线性推理的任务**
   - 如带括号的 modular arithmetic 仍具挑战，表明非线性可能仍是必要组件

---

### **未来工作方向**
1. **进一步优化实现以提升吞吐**
   - 针对大 batch 和长序列进行 kernel 级别调优
2. **探索更广泛的 gate 设计空间**
   - 引入动态稀疏、条件路由等机制
3. **结合局部注意力机制**
   - 如 Griffin-style 混合架构，兼顾 long-range 与 local precision
4. **应用于更大规模的语言模型训练**
   - 验证在 billion-scale setting 下的 scaling laws
5. **研究最优 inductive bias 的自动搜索**
   - 自动选择合适的 block/window size 和结构配置

---

> 🔚 **总结**：  
> 该论文系统性地探索了 LRNN 中“**结构化状态混合**”的设计空间，提出了 **H-LRU** 与 **BD-LRU** 两种新架构，并证明：**表达力不仅取决于宽度，更取决于状态混合的方式**。通过合理的归一化与 parallel-scan 实现，这些模型在保持效率的同时实现了媲美甚至超越 LSTM 和 Mamba 的性能，为构建高效且强大的线性序列模型提供了新路径。

</details>

---

### 9. [Predicting LLM Output Length via Entropy-Guided Representations](https://arxiv.org/abs/2602.11812)

**Authors**: Huanyi Xie, Yubin Chen, Liangyu Wang, Lijie Hu, Di Wang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.11812v1  

#### Abstract
The long-tailed distribution of sequence lengths in LLM serving and reinforcement learning (RL) sampling causes significant computational waste due to excessive padding in batched inference. Existing methods rely on auxiliary models for static length prediction, but they incur high overhead, general...

---

### 10. [RELATE: A Reinforcement Learning-Enhanced LLM Framework for Advertising Text Generation](https://arxiv.org/abs/2602.11780)

**Authors**: Jinfang Wang, Jiajie Liu, Jianwei Wu, Ziqin Luo, Zhen Chen, Chunlei Li, Biao Han, Tao Deng, Yi Li, Shuanglong Li, Lin Liu  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.11780v1  

#### Abstract
In online advertising, advertising text plays a critical role in attracting user engagement and driving advertiser value. Existing industrial systems typically follow a two-stage paradigm, where candidate texts are first generated and subsequently aligned with online performance metrics such as clic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《RELATE: A Reinforcement Learning-Enhanced LLM Framework for Advertising Text Generation》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在线广告系统中，**广告文案生成**（Advertising Text Generation）通常采用“先生成后对齐”（generate-and-align）的两阶段范式：
- 第一阶段：通过规则或生成模型产生候选文案；
- 第二阶段：利用点击率（CTR）、转化率（CVR）等在线反馈信号进行排序或优化。

这种分离方式存在以下问题：
- **目标不一致**（Objective Misalignment）：生成目标（如语言流畅性）与业务目标（如CTCVR）脱节；
- **漏斗效率低**（Low Funnel Efficiency）：大量候选需后续过滤，资源浪费；
- **多样性不足**（Diversity Deficiency）：模型容易陷入模式坍缩（mode collapse），导致文本疲劳（text fatigue）；
- **约束处理割裂**：合规性、相关性等质量要求常以事后规则形式加入，难以端到端优化。

---

### 🚀 提出的新方法：RELATE 框架

**RELATE**（REinforcement learning-enhanced LLM framework for Advertising TExt generation）是一个**基于强化学习的端到端大语言模型框架**，其核心思想是将广告文案生成建模为一个**带约束的序列优化问题**，并通过强化学习实现统一优化。

#### 主要创新点：
1. **端到端的目标对齐机制**
   - 将 CTCVR、合规性（compliance）、多样性（diversity）等多维目标统一建模为**多维度奖励函数**（multi-dimensional reward），直接嵌入生成过程。
   - 避免传统多阶段流程中的目标漂移和性能损失。

2. **统一的奖励建模框架**
   - 设计了三个关键奖励组件：
     - **CTCVR Reward**：基于预测的转化率提供商业价值导向；
     - **Quality Reward**：涵盖长度、格式、相关性、正确性和风险控制，确保输出可用且合规；
     - **Diversity Reward**：通过惩罚高频 n-gram 抑制模板化表达，缓解文本疲劳。

3. **任务定制的信用分配机制**（Credit Assignment）
   - 区分 **token-level** 和 **sentence-level** 奖励：
     - Token-level（如黑名单词、高频短语）：即时反馈，精准归因；
     - Sentence-level（如CTCVR、整体相关性）：全局评估，批量相对优势计算。
   - 在 GRPO（Generalized Reward-based Policy Optimization）基础上引入细粒度信用分配，显著提升训练效率和收敛稳定性。

4. **工业级可部署性设计**
   - 支持大规模生产环境下的稳定训练与推理；
   - 已在百度真实广告平台上线，验证了实际有效性。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如SFT + 后处理 / DPO） | RELATE |
|------|-------------------------------|--------|
| 架构 | 多阶段（生成 + 对齐） | 端到端一体化 |
| 目标对齐 | 间接依赖日志信号 | 强化学习直接优化CTCVR |
| 约束整合 | 事后规则过滤 | 奖励中软约束建模 |
| 多样性 | 易出现模式坍缩 | 显式多样性奖励抑制重复 |
| 训练效率 | 受限于延迟反馈 | 信用分配缓解稀疏奖励 |

---

## 2. 核心实验方法和设置

### 📊 数据集
- 使用 **百度搜索广告真实数据集**：
  - 包含 **40万条真实搜索广告样本**；
  - 输入包括用户查询（query）、竞品关键词（bidwords）、落地页内容等；
  - 场景聚焦于信息流广告文案生成，强调意图匹配与说服力。

---

### ⚙️ 实验设置
- **基础模型**：`Qwen3-8B`（cold-start 初始化）；
- **训练硬件**：8 × NVIDIA A800 GPU，使用 DeepSpeed ZeRO Stage 2；
- **采样策略**：每轮 rollout 生成 `G=5` 个候选文案用于组内比较；
- **批量大小**：global batch size = 1440；
- **RL算法**：基于 **GRPO**（Generalized Reward-based Policy Optimization）；
- **信用分配机制**：区分 token/sentence-level 奖励，并分别构建优势函数。

---

### 📏 评估指标
从三个维度综合评估：

| 类别 | 指标 | 定义说明 |
|------|------|----------|
| **商业效能** | `ΔCTCVR`（ACTCVR） | 在线 A/B 测试中相对于基线的 CTCVR 提升百分比 |
| **合规性** | `Compliance Rate` | 通过工业级风控系统审核的比例 |
| **多样性** | `Diversity Score` | 基于训练时相同的多样性奖励函数计算，反映对高频模式的抑制程度 |

此外还进行了：
- **人工评测**（Human Evaluation）：采用 GSB（Good/Same/Bad）配对比较法，评估语义正确性和自然度；
- **案例分析**（Case Study）：展示典型 query 下生成文案的质量差异。

---

### 🆚 基线方法对比
| 基线模型 | 描述 |
|---------|------|
| **NLG** | 基于历史高CTR广告数据训练的传统预训练语言模型 |
| **Qwen-SFT** | 在领域内高质量广告数据上进行监督微调（Supervised Fine-Tuning），并在推理时添加后处理规则保证合规性 |

---

## 3. 主要实验结果和性能指标

### 📈 性能对比（见 Table 1）

| 模型 | Compliance Rate | Diversity | ΔCTCVR (ACTCVR) |
|------|------------------|-----------|------------------|
| NLG | 73.00% | 0.75 | — |
| Qwen-SFT | 82.70% | 0.72 | +5.25% |
| **RELATE** | **93.98%** | **0.82** | **+9.19%** |

✅ **关键发现**：
- RELATE 在所有维度均显著优于两个基线；
- 相比当前线上模型（Qwen-SFT），CTCVR 提升达 **+9.19%**，具有统计显著性；
- 合规率提升超 **11个百分点**，表明更强的风险控制能力；
- 多样性得分提高约 **14%**，有效缓解文本疲劳。

---

### 🔬 消融实验（Ablation Study）

#### 实验设计（Table 2 & Table 3）
逐步增加奖励项并测试不同信用分配机制的影响：

| 模型 | Structural Quality | CTCVR | Diversity | Semantic Quality | Reward Granularity |
|------|--------------------|-------|-----------|------------------|---------------------|
| Model 1 | √ | × | × | × | Sentence-level |
| Model 2 | √ | √ | × | × | Sentence-level |
| Model 3 | √ | √ | √ | × | Sentence-level |
| Model 4 | √ | √ | √ | √ | Sentence-level |
| **RELATE** | √ | √ | √ | √ | **Token-level**（Diversity & Risk Control） |

#### 关键观察（Figure 3 & Table 3）：
1. **仅加 CTCVR（Model 2）**：
   - CTCVR 快速上升，但 **多样性与语义质量明显下降** → 存在目标冲突。
2. **加入多样性奖励（Model 3）**：
   - 多样性回升，但 CTCVR 增长放缓 → 表明多目标协调困难。
3. **加入语义质量奖励（Model 4）**：
   - 正确性、相关性提升，但整体性能提升受限。
4. **引入 token-level 信用分配（RELATE）**：
   - 所有指标同步提升，尤其 **Diversity 达到 0.965**，远高于其他配置；
   - 证明 **细粒度信用分配能缓解多目标竞争**，实现协同优化。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **端到端强化学习框架优于传统两阶段范式**：
   - RELATE 成功实现了生成与业务目标的直接对齐，避免了目标错位问题。
2. **多维度奖励建模有效平衡商业价值与内容质量**：
   - 将 CTCVR、合规性、多样性统一建模为奖励信号，使模型能在复杂约束下仍保持高性能。
3. **信用分配机制是解决稀疏奖励的关键**：
   - 区分 token/sentence-level 奖励并差异化归因，大幅提升训练效率与最终性能。
4. **已在真实工业场景中验证有效性**：
   - 上线后带来 **+9.19% CTCVR 提升**，同时提升合规率与多样性，具备强实用性。

---

### ⚠️ 局限性
1. **依赖高质量的 reward modeling**：
   - CTCVR 预测模型的准确性直接影响训练效果；
   - 若 reward signal 存在噪声或偏差，可能导致策略误导。
2. **多样性度量仍偏表层**：
   - 当前 diversity reward 基于 n-gram 频率，尚未深入语义层面（如主题分布、句式结构）。
3. **训练成本较高**：
   - 需要大量 GPU 资源支持 rollout 与 group computation，不适合小规模团队复现。

---

### 🔮 未来工作方向
1. **更丰富的奖励设计**：
   - 探索 **semantic diversity reward**（如基于 embedding 距离）；
   - 引入用户长期留存、品牌认知等长效指标作为 reward。
2. **更精细的信用分配策略**：
   - 如 segment-level 或 clause-level 归因，进一步细化行为解释。
3. **跨模态广告生成扩展**：
   - 将框架推广至图文、视频广告联合生成场景。
4. **冷启动与个性化适配**：
   - 结合 advertiser profile 实现定制化创意生成。

---

> 💡 **一句话总结**：  
> RELATE 通过**强化学习 + 多维度奖励 + 细粒度信用分配**，首次实现了广告文案生成中**商业目标、合规约束与多样性的端到端协同优化**，在真实工业系统中取得显著性能突破。

</details>

---

### 11. [GAC-KAN: An Ultra-Lightweight GNSS Interference Classifier for GenAI-Powered Consumer Edge Devices](https://arxiv.org/abs/2602.11186)

**Authors**: Zhihan Zeng, Kaihe Wang, Zhongpei Zhang, Yue Xiu  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.11186v1  

#### Abstract
The integration of Generative AI (GenAI) into Consumer Electronics (CE)--from AI-powered assistants in wearables to generative planning in autonomous Uncrewed Aerial Vehicles (UAVs)--has revolutionized user experiences. However, these GenAI applications impose immense computational burdens on edge h...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GAC-KAN: An Ultra-Lightweight GNSS Interference Classifier for GenAI-Powered Consumer Edge Devices

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本论文针对**生成式人工智能（GenAI）驱动的消费类边缘设备**在资源极度受限背景下，面临两大挑战：
- **数据稀缺性**：真实世界中的GNSS干扰信号难以大规模采集，尤其缺乏多样化的标注数据用于训练深度学习模型。
- **计算资源冲突**：GenAI应用（如LLM、扩散模型）本身已占用大量NPU和内存资源，传统重型DL模型（如ResNet、ViT）无法与其共存运行。

因此，亟需一种**超轻量级、高精度且能“常驻后台”运行**的GNSS干扰分类器，以保障PNT（定位、导航与授时）服务的安全性和可靠性。

---

### 🚀 提出的新方法与创新思路

作者提出了一种名为 **GAC-KAN** 的新型框架，其核心由两部分构成：

#### （1）物理引导的生成式仿真策略（Physics-Guided Generative Simulation）
- 利用严格的数学建模合成六类典型干扰信号（STJ, MTJ, LFM, Pulse, PBNJ, SCI）及噪声类。
- 参数随机化确保多样性，模拟真实场景下的动态变化，有效缓解**real-world data scarcity**问题。

#### （2）轻量化网络架构设计：MS-GAC + KAN Head
- **Multi-Scale Ghost-ACB-Coordinate (MS-GAC) Backbone**：
  - 融合 **Asymmetric Convolution Blocks (ACB)** 和 **Ghost Modules**，在训练时增强感受野，在推理时融合为标准卷积核，无额外部署开销。
  - 引入 **Coordinate Attention (CA)** 显式捕捉时间-频率域的能量分布特征，提升对局部干扰模式的敏感度。
  - 多分支结构支持多尺度特征提取，适应不同带宽与时变特性的干扰信号。
- **Kolmogorov-Arnold Network (KAN) 分类头**：
  - 替代传统的MLP分类头，采用可学习的**B-spline激活函数**作为边上的非线性映射。
  - 相比固定激活函数（如ReLU），具备更强的非线性拟合能力，同时参数更稀疏。

---

### 🔍 相比现有方法的优势
| 维度 | GAC-KAN优势 |
|------|-------------|
| **参数量** | 仅 **0.13M parameters**，约为ViT-B/16的 **1/660** |
| **计算效率** | **0.19 G FLOPs**，是所有对比模型中最低 |
| **准确率** | 达到 **98.0% OA（Overall Accuracy）**，优于当前SOTA |
| **适用场景** | 可作为“always-on”安全守护进程，不争抢GenAI主任务资源 |
| **鲁棒性** | 在低JNR（-20 dB）环境下仍保持约80%准确率 |

---

## 2. 核心实验方法和设置

### 📊 数据集构建
- **自建大规模合成数据集**（无公开可用真实数据集）
- 包含 **7个类别**：
  - 单音干扰（STJ）
  - 多音干扰（MTJ）
  - 线性调频（LFM / Chirp）
  - 脉冲干扰（Pulse）
  - 部分带宽噪声干扰（PBNJ）
  - 正弦调频干扰（SCI）
  - 无干扰（None / Thermal Noise）
- **JNR范围**：从 -25 dB 到 +10 dB，每5 dB一个等级
- 每类每JNR水平进行1000次Monte Carlo试验 → 总样本数：**56,000**
- 数据划分：训练集70%，验证集15%，测试集15%

---

### 🔧 特征提取与预处理
- 输入信号经 **Short-Time Fourier Transform (STFT)** 转换为时频谱图：
  - Hann窗长度：128
  - 重叠率：92%
  - FFT点数：4096
- 输出为对数幅度谱，并进行gamma校正（γ=0.9）增强弱信号可见性
- 最终伪彩色化并调整至 **224×224×3 RGB图像格式**，适配CNN输入

---

### ⚙️ 实验设置
- **硬件环境**：Intel i9-12900KF CPU, 64GB RAM, NVIDIA RTX 5060Ti (16GB)
- **软件框架**：PyTorch 2.x, CUDA 11.8, cuDNN
- **优化器**：AdamW，初始学习率 $1\times10^{-3}$，weight decay $1\times10^{-4}$
- **训练策略**：
  - 200 epochs
  - 前5轮线性warmup，后续cosine退火
  - 使用 **Mixup**（α=1.0）和 **Label Smoothing**（ε=0.1）提升泛化
- **损失函数**：
  $$
  \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{CE}} + \lambda \sum |\mathbf{c}_i|,\quad \lambda=10^{-5}
  $$
  其中 $\mathbf{c}_i$ 是KAN层的spline系数，L1正则促进稀疏性

---

### 📈 评估指标
| 指标 | 定义 |
|------|------|
| **OA (Overall Accuracy)** | 所有类别平均识别准确率 |
| **Confusion Matrix** | 展示各类别间误判情况 |
| **Params (M)** | 模型参数数量（百万级） |
| **FLOPs (G)** | 单次前向传播浮点运算量（十亿级） |
| **Inference Time (ms)** | GPU上单样本推理延迟 |
| **JNR Robustness Curve** | 不同JNR条件下的分类准确率趋势 |

---

### 🆚 基线方法对比
- **Vision Transformer (ViT-B/16)**：典型Transformer架构，参数量大
- **FA-CNN**：带频率注意力的传统CNN
- **RCNN**：循环卷积神经网络，适合时序建模
- **ST-MSFF-KAN**：近期提出的轻量KAN-based方法（SOTA之一）

---

## 3. 主要实验结果和性能指标

### 📈 表格：性能综合对比（Test Set）

| Model | OA (%) | Params (M) | FLOPs (G) | Time (ms) |
|-------|--------|------------|-----------|-----------|
| **GAC-KAN (Ours)** | **98.0** | **0.1289** | **0.1901** | 7.0140 |
| ST-MSFF-KAN | 96.8 | 0.2883 | 0.5026 | 3.0140 |
| ViT-B/16 | 95.2 | 85.8040 | 11.2855 | 6.6676 |
| FA-CNN | 93.8 | 57.0530 | 0.7102 | 0.8693 |
| RCNN | 91.8 | 13.1194 | 0.5547 | 0.3097 |

> ✅ **GAC-KAN在准确率第一的同时，参数量最小、计算成本最低**

---

### 🔬 关键实验发现

#### （1）卓越的整体性能
- **最高准确率 98.0%**，显著优于第二名 ST-MSFF-KAN（96.8%），领先近1.2个百分点。
- 尤其在复杂干扰类型（如SCI vs PBNJ）之间具有良好的区分能力。

#### （2）极端轻量化优势
- 参数仅为 **0.13M**，相较ViT减少 **660倍**，远低于其他CNN架构。
- 存储需求极低，适用于OTA更新和低成本Flash存储设备。

#### （3）低JNR下的强鲁棒性
- 如图4所示，在 **JNR = -20 dB** 极端噪声条件下：
  - GAC-KAN 准确率 ≈ **80%**
  - FA-CNN ≈ 55%，RCNN ≈ 50%
- 归功于 **Coordinate Attention机制** 对微弱能量区域的有效聚焦，以及 **KAN的灵活决策边界**

#### （4）混淆矩阵分析（图5）
- 对角线高度集中，表明整体分类精准。
- 主要误判发生在：
  - **PBNJ 被误判为 None（87例）**：因两者在低JNR下频谱相似
  - **SCI 被误判为 PBNJ**：正弦调制特征被噪声掩盖
- 但仍能有效区分形态相近信号（如LChirp vs SCI），证明MS-GAC的有效性

#### （5）消融实验（虽未明确列出表格，文中隐含验证）
- **ABlation by Design Components**：
  - 使用ACB提升中心骨架强度 → 提高特征表达力
  - Ghost模块显著降低冗余 → 控制参数增长
  - CA机制显式建模时空依赖 → 提升低信噪比表现
  - KAN替代MLP → 在极少参数下实现更强非线性映射

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **GAC-KAN实现了精度与效率的最优平衡**：
   - 在仅有 **0.13M参数** 和 **0.19G FLOPs** 的代价下达到 **98.0%准确率**，是目前最高效的GNSS干扰分类器之一。
   
2. **KAN首次成功应用于GNSS领域**：
   - 验证了基于可学习spline activation的KAN在紧凑模型中优于传统MLP，尤其适合边缘部署。

3. **生成式仿真可有效解决data scarcity问题**：
   - 物理建模+参数随机化生成的数据足够多样化，使模型具备良好泛化能力。

4. **“Always-On”安全成为可能**：
   - 极低的资源消耗使其可在GenAI设备上长期后台运行，不影响主AI任务性能。

---

### ⚠️ 局限性
- **完全依赖合成数据**：尚未在真实GNSS接收机实测数据上验证，可能存在仿真-现实差距（sim-to-real gap）。
- **推理延迟相对较高（7ms）**：虽然满足多数实时系统要求（<100ms @10–50Hz），但对于超高频控制环路可能略慢。
- **未考虑spoofing攻击**：仅聚焦于jamming classification，未涵盖欺骗信号检测。

---

### 🔮 未来工作方向
1. **扩展至real-world数据迁移学习**：结合少量真实数据进行fine-tuning，缩小仿真偏差。
2. **集成spoofing detection模块**：构建统一的GNSS威胁感知系统（anti-jamming + anti-spoofing）。
3. **部署到实际SoC平台**：在手机、无人机等GenAI-native芯片上实测功耗与能效。
4. **探索KAN与其他轻量结构组合**：如MobileNet、EfficientNet等，进一步压缩模型。
5. **支持few-shot learning场景**：应对新型未知干扰类型的快速适应能力。

---

## 总结

> **GAC-KAN 是面向 GenAI 时代的下一代 GNSS 安全基石**。它通过“物理引导仿真 + 轻量骨干 + KAN分类头”的协同设计，在几乎不占用宝贵算力的前提下，提供高达98%的干扰识别精度，真正实现了“**永远在线、永不妥协**”的安全愿景。

</details>

---

### 12. [Robust Optimization Approach and Learning Based Hide-and-Seek Game for Resilient Network Design](https://arxiv.org/abs/2602.11854)

**Authors**: Mohammad Khosravi, Setareh Maghsudi  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.11854v1  

#### Abstract
We study the design of resilient and reliable communication networks in which a signal can be transferred only up to a limited distance before its quality falls below an acceptable threshold. When excessive signal degradation occurs, regeneration is required through regenerators installed at selecte...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

**论文标题**：*Robust Optimization Approach and Learning Based Hide-and-Seek Game for Resilient Network Design*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

本文研究的是**Resilient Network Design**（弹性网络设计）中的**Regenerator Location Problem (RLP)**。该问题旨在为通信网络（如光网络、卫星网络）部署信号再生器（regenerators），以克服信号在长距离传输中的质量衰减。其核心挑战在于：

- **物理限制**：信号只能在不超过最大距离 `d_max` 的链路上传输，否则需要通过 regenerator 进行再生。
- **不确定性**：现实世界中，**网络节点**的安装成本和**网络边**（链路）的长度均存在不确定性，这些不确定性会随时间动态变化。

因此，目标是设计一个**鲁棒且可靠**的网络，在最坏情况下也能保证全网连通性，同时最小化总成本。

### **提出了什么新方法或新思路**

1. **新型不确定性建模**：
   - **Static Budgeted Uncertainty Set**：用于建模**节点安装成本**的不确定性，允许对手在预算内将最多 `I_v` 个节点的成本提升至其上限。
   - **Dynamic Budgeted Uncertainty Set**：这是本文提出的一个**新概念**，用于建模**边长度**的时变不确定性。它假设链路的有效长度会随时间周期性波动，对手可以在每个时间段 `t` 内选择最多 `I_e` 条边进行干扰。

2. **多范式求解框架**：
   - **数学规划模型**：构建了针对静态和动态不确定性的混合整数规划（MIP）模型。
   - **可扩展的求解算法**：为解决大规模实例，提出了三种高效算法：
     - **Column-and-Constraint Generation (CCG)**
     - **Benders Decomposition (BDC)**
     - **Iterative Robust Optimization (IRO)**
   - **学习型博弈框架**：提出了一个新颖的 **Learning-based Hide-and-Seek (HSL) Game**，将鲁棒优化问题建模为网络规划者（seeker）与对手（adversary）之间的重复博弈，并采用梯度更新规则进行求解。

### **相比现有方法的优势**

- **更贴近现实**：传统的鲁棒优化通常只考虑静态不确定性。本文提出的 **dynamic budgeted uncertainty** 更好地捕捉了实际网络中（如温度变化、流量波动）导致的时变链路质量。
- **更高的成本效益**：通过显式建模动态不确定性，所提方法（特别是 RDB 和 HSL）能够找到比传统确定性最坏情况（DWC）和静态鲁棒（RSB）方法**成本更低**的解决方案。
- **更强的可扩展性**：CCG、BDC 等分解算法显著提升了求解大规模网络实例的能力，其中 **CCG 在效率上表现最优**。

---

## 2. 核心实验方法和设置

### **使用的数据集**

论文未使用公开的真实数据集，而是**自行生成了合成数据集**，以系统性地评估不同参数下的性能。

- **图结构**：生成具有 `n` 个节点和密度 `dens=0.3` 的随机无向图。
- **参数生成**：
  - **边长度**：名义长度 `c_e` 从 `{350, ..., 600}` 中均匀随机抽取；最大偏差 `d_e` 从 `{1, ..., 250}` 中抽取。
  - **节点成本**：名义成本 `c_v` 从 `{250, ..., 300}` 中抽取；最大偏差 `d_v` 从 `{1, ..., 50}` 中抽取。
- **固定参数**：`d_max = 1000`。

### **实验设置和评估指标**

进行了四组实验（Exp-1 到 Exp-4）：

| 实验 | 网络规模 (`n`) | 不确定性预算 (`I_e`, `I_v`) | 目的 |
| :--- | :--- | :--- | :--- |
| **Exp-1** | 10 到 30 | 固定 (`2, 2`) | 小规模实例，比较 DWC, RSB, RDB |
| **Exp-2** | 固定 (25) | 变化 (`{1,2}`, `{1,2,3}`) | 分析预算对性能的影响 |
| **Exp-3** | 40 到 60 | 固定 (`2, 2`) | 大规模实例，测试可扩展性 |
| **Exp-4** | 固定 (50) | 变化 (`{1,2}`, `{1,2,3}`) | 大规模实例，分析预算影响 |

**评估指标**：
- **平均总成本 (Average Cost)**：衡量解决方案的经济性。
- **计算时间 (Computational Time)**：衡量算法效率。
- **迭代次数 (Iterations)**：衡量收敛速度。
- **性能对比**：使用 `R-DWC` 和 `R-RSB` 表示相对于 DWC 和 RSB 的成本降低百分比。

### **基线方法对比**

- **DWC (Deterministic Worst-Case)**：所有参数取其最坏值（上界），然后求解确定性问题。
- **RSB (Robust Static Budgeted)**：节点成本和边长度均使用**静态**预算不确定性集建模。
- **RDB (Robust Dynamic Budgeted)**：本文提出的基准模型，结合了静态节点成本和动态边长度不确定性。

本文提出的方法（CCG, BDC, IRO, HSL）均与上述基线进行对比。

---

## 3. 主要实验结果和性能指标

### **关键性能数据与对比结果**

#### **小规模实例 (Exp-1 & Exp-2)**

- **成本优势**：
  - RDB 相比 DWC 和 RSB 能实现 **4% 到 11%** 的成本降低（见 Table II）。
  - 当 `I_e=1` 时，RDB 的成本降低高达 **28%**（见 Table III），表明在对手攻击能力受限时，动态建模的优势更为显著。
- **计算时间**：
  - RDB 的求解时间最长，因为其建模更复杂。
  - DWC 和 RSB 最快。

#### **大规模实例 (Exp-3 & Exp-4)**

- **成本优势**：
  - 在更大规模下，CCG、BDC、IRO、HSL 等方法仍能进一步降低最终成本，最高可达 **2.40%**（见 Table IV）。
  - 当 `I_e=1` 时，成本降低高达 **31%**（见 Table V）。
- **计算效率与可扩展性**：
  - **CCG 是最快的方法**，在许多情况下甚至比求解确定性问题（DWC）还快（见 Fig. 3 & 4）。
  - 收敛速度排序：**CCG ≈ BDC > IRO > HSL**。CCG 和 BDC 迭代次数最少。
  - 性能曲线（Performance Profile）显示，CCG 在大多数实例上都能快速找到高质量解。

### **消融实验结果**

虽然没有明确标注为“消融实验”，但通过比较不同设置下的结果，可以得出以下结论：

- **动态不确定性建模的价值**：RDB 相比 RSB 的显著成本降低证明了引入 **dynamic budgeted uncertainty** 的有效性。
- **不同求解算法的权衡**：
  - **CCG** 在**速度和效率**上最优，适合大规模应用。
  - **HSL** 作为一种基于学习的启发式方法，虽然收敛较慢，但提供了一种模拟对抗性交互的新颖视角。

---

## 4. 关键结论和发现

### **主要发现**

1. **动态不确定性至关重要**：显式地对网络链路的**时变特性**进行建模（Dynamic Budgeted Uncertainty）能够显著提升网络设计的鲁棒性和成本效益，尤其是在对手资源有限的情况下。
2. **所提方法高效且可扩展**：提出的 **CCG** 和 **Benders Decomposition** 算法能够高效求解大规模网络实例，其中 **CCG 是计算效率最高的方法**。
3. **学习型博弈框架有效**：**Hide-and-Seek Game (HSL)** 成功地将复杂的鲁棒优化问题转化为一个可通过梯度学习求解的博弈过程，验证了其作为替代求解范式的潜力。
4. **传统方法过于保守**：确定性最坏情况（DWC）和静态鲁棒（RSB）方法由于未能充分利用不确定性的时间动态性，导致了不必要的高成本。

### **方法的局限性**

- **计算复杂性**：尽管有高效的算法，但核心问题被证明是 **NP-hard**，对于超大规模网络，计算负担依然存在。
- **模型假设**：动态预算不确定性集虽然新颖，但仍是一个简化模型，可能无法完全捕捉所有现实世界的复杂动态。
- **HSL 的收敛性**：学习型方法依赖于迭代和梯度更新，其收敛到全局最优的理论保证可能不如精确算法强。

### **未来工作方向**

1. **开发启发式/元启发式算法**：为图变换和再生器放置组件设计高效的启发式方法，以进一步提升可扩展性。
2. **探索其他不确定性表示**：例如，研究 **dynamic interval uncertainty** 或 **distributional uncertainty**。
3. **集成故障与生存性约束**：将边故障、节点失效等生存性（survivability）要求直接纳入模型。
4. **应用于下一代网络**：将该框架推广到 6G、空天地一体化网络等更复杂的场景中。

</details>

---

### 13. [Pushing Forward Pareto Frontiers of Proactive Agents with Behavioral Agentic Optimization](https://arxiv.org/abs/2602.11351)

**Authors**: Yihang Yao, Zhepeng Cen, Haohong Lin, Shiqi Liu, Zuxin Liu, Jiacheng Zhu, Zhang-Wei Hong, Laixi Shi, Ding Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.11351v1  

#### Abstract
Proactive large language model (LLM) agents aim to actively plan, query, and interact over multiple turns, enabling efficient task completion beyond passive instruction following and making them essential for real-world, user-centric applications. Agentic reinforcement learning (RL) has recently eme...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Pushing Forward Pareto Frontiers of Proactive Agents with Behavioral Agentic Optimization

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文针对**Proactive LLM Agents**在多轮交互任务中面临的核心挑战——**任务性能与用户参与度之间的权衡（trade-off）**。具体表现为：
- 被动型Agent无法主动获取信息，难以准确理解用户意图；
- 过度依赖用户反馈的主动型Agent虽然能提升最终表现，但频繁请求会降低用户体验和满意度。

该问题本质上是一个**多目标优化（Multi-Objective Optimization, MOO）问题**：最大化任务完成质量的同时最小化对用户的打扰。

### 提出了什么新方法或新思路
作者提出了一种新的强化学习框架：**Behavioral Agentic Optimization (BAO)**，其核心思想是通过**行为增强（Behavior Enhancement）** 和 **行为正则化（Behavior Regularization）** 来优化Agent的多轮推理能力。

#### 主要创新点：
- **将Proactive Agent训练建模为MOO问题**，并明确识别出Pareto前沿的存在。
- 引入两类关键的**multi-turn behaviors**：
  - **Retrospective Reasoning**（回溯式推理）：基于历史信息进行记忆管理和假设修正（Hypothesis Refinement），避免重复提问。
  - **Prospective Planning**（前瞻性规划）：动态调度查询策略（Dynamic Scheduling）和战略性提问（Strategical Querying），提高探索效率。
- 设计了两阶段训练流程：
  1. **SFT阶段注入行为模式**：利用教师模型（如GPT-4o）生成带有指定行为特征的数据；
  2. **RL阶段施加行为正则化奖励**：通过turn-level reward shaping抑制低效行为（如冗余提问、过度思考）。

### 相比现有方法的优势
- **优于传统RL方法**（如UserRL）：在更少用户交互下达到更高任务成功率。
- **媲美甚至超越商业闭源模型**（如GPT-4o、Gemini系列）：尤其在Function-Gym等复杂推理任务上表现突出。
- **有效缓解reward hacking问题**：相比基线方法，在跨模拟器评估时性能下降更小，说明泛化性更强。
- **推动了Pareto前沿前移**：实现了任务性能与用户负担之间更好的平衡。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
所有实验均基于 **UserRL benchmark suite** 中的三个任务：
| 任务 | 描述 |
|------|------|
| **Function-Gym** | 推断一个未知四变量数学函数形式，通过输入输出采样来推测函数结构。完全规则化反馈，无sim-to-real gap。 |
| **Telepathy-Gym** | 推测隐藏实体，通过向环境提问“Yes/No”问题逐步缩小范围。训练用Qwen3-8B模拟用户，测试用GPT-4o，存在分布偏移。 |
| **Turtle-Gym** | “龟汤游戏”，需从表面故事中推理出隐藏转折（twist）。结合叙事理解和隐喻识别，训练与测试间存在显著sim-to-real gap。 |

每个任务最多允许 `T=15` 轮交互（Turtle-Gym例外为12轮）。

### 实验设置和评估指标
#### 模型架构
- 基础模型：**Qwen3系列**（1.7B 和 4B 参数量）
- 教师模型：**GPT-4o**

#### 训练流程
1. **Behavior-Enhanced SFT**：构建包含Retrospective & Prospective行为的监督微调数据集。
2. **Behavior-Regularized RL**：采用GRPO算法进行强化学习，并引入以下正则项：
   - **Information-Seeking Regularization**：惩罚连续无信息增益的用户交互。
   - **Over-Thinking Regularization**：惩罚因token耗尽而未完成任务的轨迹。

#### 评估指标
| 指标 | 定义 | 越高越好？ |
|------|------|------------|
| **Pass@U-k** | 在最多允许k次用户交互（Answer动作）的情况下成功完成任务的比例 | ✅ |
| **Score (R(T))** | 未经调整的累积奖励 | ✅ |
| **User Involvement Rate (UR)** | 用户相关动作占比 $ \mathbb{E}[U(T)/|T|] $ | ❌ |
| **Exploration Ratio** | 工具/环境交互数 vs 用户提交答案次数 | ✅ |

### 基线方法对比
| 类别 | 方法 |
|-------|--------|
| 商业模型 | GPT-4o, GPT-4o-mini, GPT-5-0807, Gemini系列 |
| 开源原始模型 | Qwen3-32B/14B/8B (Raw) |
| 微调基线 | **UserRL**（Qian et al., 2025c） |
| 消融变体 | BAO w/o BE（无行为增强）、BAO w/o BR（无行为正则化） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 1）

| 方法 | Function-Gym Pass@U-1 | Pass@U-2 | Score | UR ↓ |
|------|------------------------|----------|-------|-------|
| **BAO-4B** | **0.2692** | **0.5354** | **0.6923** | **0.2148** |
| UserRL-4B | 0.2436 | 0.4103 | 0.5256 | 0.3758 |
| GPT-4o | 0.1282 | 0.1282 | 0.1410 | 0.2248 |
| GPT-5-0807 | 0.5000 | 0.6923 | 0.7436 | 0.1365 |

> 注：BAO-4B在保持较低UR的同时，Pass@U-2接近GPT-5水平，且远超UserRL。

| 方法 | Turtle-Gym Pass@U-1 | Pass@U-2 | Score | UR ↓ |
|------|---------------------|----------|-------|-------|
| **BAO-4B** | **0.1063** | **0.1125** | **0.1125** | **0.2917** |
| UserRL-4B | 0.0417 | 0.0563 | 0.0594 | 0.7617 |

> BAO在最难的Turtle-Gym上仍显著优于UserRL，且用户参与率大幅降低。

### 与基线方法的对比结果
- **BAO全面超越UserRL**：
  - 更高的Pass@U-1/2：表明早期即可做出正确判断。
  - 更低的UR：减少对用户验证的依赖。
  - 更高的探索比例（见Figure 4）：说明更善于利用工具和环境交互。
- **媲美甚至超越部分商业模型**：
  - 在Function-Gym上，BAO-4B得分超过GPT-4o和Gemini系列。
  - 在Turtle-Gym上虽不及GPT-5，但优于多数闭源模型。

### 消融实验结果
#### 行为组件消融（Table 1 & Figure 6）
| 变体 | 影响 |
|------|------|
| **BAO w/o BE** | Pass@U-k下降，Score降低 → 证明行为增强对性能提升至关重要 |
| **BAO w/o BR** | UR急剧上升至0.8698（vs BAO的0.2917）→ 显示行为正则化有效抑制冗余交互 |

#### 行为类型分析（Figure 6）
- **Retrospective Reasoning**：提升长期性能上限（Score↑），但不显著改善首次提交准确率。
- **Prospective Planning**：显著提升Pass@U-1，因其支持提前规划。
- **二者结合**：实现早期准确性与长程推理能力的均衡。

#### 探索多样性分析（Figures 7 & 8）
- **Self-BLEU & Embedding Similarity更低** → BAO生成的问题和答案更具多样性。
- 说明BAO能更高效地探索信息空间，避免陷入无效循环。

#### Reward Hacking分析（Figure 9）
- UserRL在训练中产生更长的回答以“欺骗”奖励模型，但在GPT-4o评估时性能骤降。
- BAO训练响应长度适中，**RTR（Reward Translation Rate）高达0.575**（UserRL仅0.154），显示更强鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **Proactive Agent训练本质是MOO问题**，必须同时考虑任务性能与用户参与成本。
2. **显式建模multi-turn behaviors**（Retrospective Reasoning + Prospective Planning）可显著提升Agent的自主推理能力。
3. **BAO成功推动了Pareto Frontier前移**：在同等用户负担下获得更高性能，或在同等性能下显著降低干扰。
4. **行为正则化机制有效防止reward hacking**，增强了模型在真实场景中的可靠性。
5. **开放模型经BAO训练后可媲美甚至超越部分商业模型**，展示了开源Agent的巨大潜力。

### 方法的局限性
- 当前工作聚焦于**纯语言Agent**，尚未扩展到多模态场景。
- 所有任务均在仿真环境中进行，实际部署中的复杂性和安全性风险未充分验证。
- 对教师模型（如GPT-4o）有一定依赖，可能限制完全去中心化的训练路径。

### 未来工作方向
- 将BAO框架拓展至**Multimodal Agents**（视觉+语言+动作）。
- 探索更轻量级的行为注入方式，降低对强Teacher Model的依赖。
- 在真实人机协作场景中测试BAO训练的Agent，研究其社会影响与伦理边界。
- 结合**User Preference Modeling**进一步个性化Agent行为。

---

> 🌐 **项目主页**: [https://proactive-agentic-rl.github.io/](https://proactive-agentic-rl.github.io/)

</details>

---

### 14. [Credit Where It is Due: Cross-Modality Connectivity Drives Precise Reinforcement Learning for MLLM Reasoning](https://arxiv.org/abs/2602.11455)

**Authors**: Zhengbo Jiao, Shaobo Wang, Zifan Zhang, Wei Wang, Bing Zhao, Hu Wei, Linfeng Zhang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.11455v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly advanced the reasoning capabilities of Multimodal Large Language Models (MLLMs), yet how visual evidence is integrated during reasoning remains poorly understood. We explore multimodal RLVR through the lens of cross-modal attent...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Credit Where It's Due: Cross-Modality Connectivity Drives Precise Reinforcement Learning for MLLM Reasoning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Reinforcement Learning with Verifiable Rewards (RLVR)** 在多模态大语言模型（MLLM）推理任务中普遍存在“**均匀信用分配**”（uniform credit assignment）的问题。即在训练过程中，所有生成的文本 token 被同等对待，无论其是否真正依赖视觉输入进行推理。

然而，研究表明，MLLMs 在视觉推理中常出现“**伪推理**”现象——大量 token 仅遵循语言模式而未真实锚定于图像内容。这导致训练信号被稀释，影响模型对视觉证据的有效利用。

---

### 🚀 提出了什么新方法或新思路

作者提出 **Anchor-Token Reinforcement Learning (AT-RL)**，一种轻量级、即插即用的强化学习框架，其核心思想是：

> **将强化学习的信用（credit）精准分配给那些真正连接视觉与语言的“感知锚点”（Perceptual Anchors）**。

#### 创新机制包括：
- **发现感知锚点**：通过分析跨模态注意力（cross-modal attention）拓扑结构，识别出约 **15% 的高连通性 token**，这些 token 构成“感知锚点”，负责将推理过程锚定在图像上。
- **图聚类分组**：构建基于注意力相似性的 token 图，并使用 METIS 算法进行语义聚类，形成具有功能一致性的推理步骤单元。
- **软加权优势调制**：根据每个 cluster 的跨模态连通密度（connectivity density），动态调整其对应的 advantage 信号，实现细粒度的 credit assignment。

---

### 🔍 相比现有方法的优势

| 特性 | 传统 RLVR 方法（如 GRPO, DAPO） | AT-RL |
|------|-------------------------------|-------|
| Credit Assignment | 均匀广播至所有 token | 集中于高连通性 anchor tokens |
| 视觉接地能力 | 弱，易受语言先验干扰 | 显著增强，提升 grounding 精度 |
| 模型效率 | 无额外开销 | 仅引入 **1.2%** 额外计算开销 |
| 可扩展性 | 固定策略 | 支持从 3B 到 32B 模型一致增益 |
| 泛化性 | 多数限于静态图像 | 成功扩展至视频理解任务 |

此外，AT-RL 是 **plug-and-play** 的，可无缝集成到 GRPO、DAPO、GSPO 等主流 RLVR 框架中，无需修改网络架构或奖励设计。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 类别 | 数据集 |
|------|--------|
| 主训练数据 | **ViRL-39K**（用于 RL 微调） |
| 跨数据集泛化 | **Geometry-3K**, **GeoQA-8K** |
| 数学推理评测 | **MathVista**, **MathVerse**, **MathVision**, **GeoQAtest**, **WeMath** |
| 多学科理解 | **MMMU-Pro**, **MMMU_Val**, **EMMA_core** |
| 实际场景 | **MEGA** |
| 图表理解 | **ChartQAPro**, **ChartMimic** |
| 视频理解 | **Video-R1**（含 VSI-Bench, VideoMMMU, MMVU 子集） |

---

### ⚙️ 实验设置和评估指标

- **模型系列**：Qwen2.5-VL-3B / 7B / 32B / 72B-Instruct
- **评估协议**：
  - 使用 `Acc@1` 指标，在温度 `0.1` 下进行零样本测试。
  - 所有方法均采用统一超参数配置，运行于 8×NVIDIA A100 GPU。
- **答案提取策略**：
  - 优先提取 `\boxed{}` 内容；
  - 若失败，则通过全文搜索选项字母或数值。
- **验证方式**：使用 Qwen2.5-72B-Instruct 作为 LLM Judge 进行语义正确性判断。

---

### 🆚 基线方法对比

| 基线类别 | 方法列表 |
|---------|----------|
| 统一信用分配 | GRPO, DAPO, GSPO, SAPO |
| 细粒度信用分配 | StepGRPO, FT-RL, VPPO |
| 其他 | SFT（监督微调）、Zero-shot |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 3）

| 方法 | MathVista | MathVerse | MMMU-Pro | MEGA | ChartQAPro |
|------|-----------|-----------|----------|------|------------|
| Qwen2.5-VL-32B (Zero-shot) | 75.7 | 49.3 | 48.5 | 48.9 | 43.4 |
| Qwen2.5-VL-32B + VPPO | 78.3 | 54.5 | 49.2 | 52.3 | 46.6 |
| **Qwen2.5-VL-32B + AT-RL (ours)** | **80.2** | **56.6** | **51.9** | **53.0** | **48.4** |
| Qwen2.5-VL-72B-Instruct (Zero-shot) | 77.8 | 57.2 | 51.6 | 49.6 | 47.2 |

> ✅ **亮点**：**32B 模型 + AT-RL 超越了更大的 72B-Instruct 模型**，尤其在 MathVista 上达到 **80.2**，显著领先。

---

### 🔁 与基线方法的对比结果（Table 1 & Table 3）

- 在 Qwen2.5-VL-7B 上，SAPO + AT-RL 相比原始 SAPO 提升高达 **+8.24pp**（平均提升）。
- 在多个任务上，AT-RL 均优于所有基线，包括最先进的 VPPO 和 SAPO。
- 在视频理解任务中（Table 4）：
  - 输入 64 帧时，AT-RL 达到 **56.8 平均准确率**，比 T-GRPO 高出 **+5.1pp**，比零样本高出 **+9.4pp**。
  - 在 VSI-Bench 上增益达 **+11.8pp**，表明其能有效捕捉时间维度上的关键 anchor tokens。

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）软加权 vs 硬截断（Table 5）
| 方法 | MathVerse 提升（Δ） |
|------|------------------|
| Hard Truncation (top 15%) | +2.96pp |
| Random Weighting | -1.52pp |
| Reverse Weighting | -2.82pp |
| **AT-RL (Soft Weighting)** | **+5.65pp** |

> 结论：**软加权策略远优于硬掩码或随机选择**，说明保留低权重 token 的梯度有助于维持语言连贯性。

#### （2）偏置校正的重要性（Table 6）
| 方法 | MathVerse |
|------|----------|
| AT-RL w/o Bias Correction | 34.20 |
| **AT-RL w/ Bias Correction** | **37.50** |

> 去除注意力偏差后性能明显提升，证明 debiasing 对识别真实感知锚点至关重要。

#### （3）组件消融（Table 7）
| 配置 | MathVerse ↑ | MathVista ↑ |
|------|-------------|------------|
| 无分组（Raw Attention） | +2.73 | +2.20 |
| 仅有分组（METIS） | +3.56 | +3.20 |
| 完整 AT-RL（含 refine + expand） | **+4.98** | **+4.50** |

> 表明 **语义分组 + 结构优化（denoising & neighborhood expansion）共同贡献最大增益**。

#### （4）计算效率分析
- AT-RL 模块仅增加 **1.2%** 的训练迭代开销。
- 前向传播占 65.4%，反向传播占 33.5%，其余为 AT-RL 处理流程。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **存在感知锚点（Perceptual Anchors）**：
   - MLLM 推理链中仅有约 **15% 的 token 展现出强跨模态连通性**，它们充当“灯塔”角色，将推理锚定于图像。
   - 高连通性 token 多为动作导向词（如 “calculate”, “given”, “assume”），而大多数 token 仅为语言过渡服务。

2. **信用应归于锚点**：
   - RLVR 中的 credit assignment 应聚焦于这些感知锚点，而非平均分配。
   - 正确或错误的答案都会让 anchor tokens 承担主要责任，因此强化信号必须集中于此。

3. **AT-RL 显著提升性能且高效**：
   - 在不改变模型结构的前提下，AT-RL 实现了跨规模、跨任务的一致增益。
   - **32B 模型超越 72B 基线**，验证了“质量胜于数量”的推理优化路径。

4. **SFT 与 RL 互补**：
   - RL（如 AT-RL）擅长优化已有知识的视觉接地；
   - SFT/CPT 更适合注入新知识；
   - 二者结合才能同时解决 **Knowledge Deployment Error (KDE)** 和 **Visual Perception Error (VPE)**。

---

### ⚠️ 方法的局限性

1. **无法弥补知识缺失**：
   - 如 Discussion 4 所述，AT-RL 不提供新的领域知识，若模型本身缺乏相关概念（如几何定理），仍会失败。

2. **依赖高质量注意力信号**：
   - 若基础模型的 cross-modal attention 本身不准（如早期层混乱），则 anchor 定位可能失效。

3. **聚类参数敏感性**：
   - 尽管默认参数表现稳定，但在极端短/长序列中需调整聚类数量 $ K $ 或阈值 $ T_{sim} $。

---

### 🔮 未来工作方向

1. **联合训练 SFT + AT-RL**：
   - 设计端到端框架，同步注入知识并优化 grounding。

2. **动态识别 anchor 类型**：
   - 区分不同类型的 anchor（如空间定位、数值读取、逻辑判断），实施差异化强化。

3. **扩展至更多模态**：
   - 将 AT-RL 推广至音频、3D 点云等多模态场景，探索通用的跨模态 anchoring 机制。

4. **在线 anchor 发现与反馈**：
   - 构建闭环系统，在推理过程中实时检测 anchor 并引导模型修正路径。

---

> 💡 **一句话总结**：  
> **Reasoning quality is governed not by token quantity, but by the fidelity of cross-modal anchoring.**  
> —— 真正决定推理质量的不是 token 数量，而是跨模态锚定的精确性。

</details>

---

### 15. [Few-Shot Design Optimization by Exploiting Auxiliary Information](https://arxiv.org/abs/2602.12112)

**Authors**: Arjun Mani, Carl Vondrick, Richard Zemel  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.12112v1  

#### Abstract
Many real-world design problems involve optimizing an expensive black-box function $f(x)$, such as hardware design or drug discovery. Bayesian Optimization has emerged as a sample-efficient framework for this problem. However, the basic setting considered by these methods is simplified compared to r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Few-Shot Design Optimization by Exploiting Auxiliary Information**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
该论文针对现实世界中**昂贵黑盒函数优化**（expensive black-box optimization）的问题，指出传统 **Bayesian Optimization (BayesOpt)** 存在两个关键局限：
1. **仅依赖标量奖励 $f(x)$**，忽略了实验过程中产生的丰富辅助信息（如传感器时序、学习曲线等）；
2. **单任务设定**，无法利用历史相似任务的知识进行迁移。

现实中许多设计任务（如机器人硬件设计、药物发现、神经网络超参调优）不仅产生高维、多模态的**辅助信息 $h(x)$**，还往往属于同一任务家族，具备可迁移的经验。如何有效利用这些信息以实现更高效的少样本（few-shot）优化，是本文要解决的核心问题。

---

### **提出了什么新方法或新思路**
作者提出了一种**新的优化框架**，其核心思想是：

> **将辅助信息 $h(x)$ 融入上下文（context），训练一个神经模型来预测未见设计的性能 $f(x)$，从而作为 BayesOpt 的代理模型（surrogate model）。**

具体创新点包括：

- **新问题设定**：正式定义了一个包含“辅助信息”和“多任务历史”的设计优化新范式，强调模型需从 $h(x)$ 中学习通用表征以加速新任务优化。
- **基于 Transformer 的神经过程架构**：采用类似 **Transformer Neural Processes** 的结构，模型接受一个包含 $(x, f(x), h(x))$ 的 few-shot 上下文集，对目标设计 $x$ 预测其 $f(x)$ 分布。
- **端到端学习表征**：模型直接学习如何编码 $h(x)$ 并用于预测 $f(x)$，而非强制建模 $h(x)$ 本身，避免了高维输出带来的训练困难。
- **无需微调的部署方式**：训练完成后，模型冻结，在新任务上仅通过更新上下文即可进行推理，无需重新训练，适合快速部署。

---

### **相比现有方法的优势**
| 对比维度 | 传统方法 | 本文方法 |
|---------|--------|--------|
| **是否利用 $h(x)$** | 否（或简单拼接） | 是，且学习其语义表征 |
| **是否支持多任务迁移** | 单任务或弱迁移 | 显式利用任务历史进行 few-shot 迁移 |
| **模型灵活性** | GP 受限于核函数假设 | 神经网络可学习复杂非线性关系 |
| **计算效率** | GP 推理慢，随数据增长 | Transformer 架构支持并行，推理快 |
| **表示能力** | 难以处理高维异构 $h(x)$ | 可灵活编码图像、序列等多种形式 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
论文在两个截然不同的领域验证方法有效性：

#### **(1) 机器人抓手设计任务（Gripper Design Task）**
- **自建大规模基准数据集**：基于 **ShapeNet** 中 997 个物体构建。
- **每个任务**：为特定物体设计最优抓手形状 $x$。
- **$f(x)$**：抓取稳定性（抵抗扰动力的最大牛顿数，最高约 6.0N）。
- **$h(x)$**：每次仿真中的**触觉反馈**，包括：
  - 左右夹爪内表面的 16×16 触觉图（tactile maps）
  - 其他面的接触读数
  - 系统状态（关节位置、速度、施加力等）
- **数据规模**：共 **428万次** 抓手评估，平均每个物体 ~4300 个设计，是当前最大的多任务 BayesOpt 基准之一。

#### **(2) 神经网络超参数调优任务（Hyperparameter Tuning）**
- **使用公开基准 LCBench**：
  - 包含 35 个分类任务
  - 每个任务评估 2000 个超参配置
- **$f(x)$**：最佳验证准确率
- **$h(x)$**：训练全过程的**每 epoch 学习曲线**，包括：
  - 训练/验证准确率
  - 类别平衡准确率
  - 学习率变化（cosine annealing）

---

### **实验设置和评估指标**

#### **训练阶段**
- **模型输入**：上下文集 $C = \{(x_i, f(x_i), h(x_i))\}$ 和目标设计集 $T_x$
- **目标**：预测 $P_\theta(f(T_x) | C, T_x)$
- **损失函数**：负对数似然（NLL）
- **采样策略**：上下文大小随机采样（Gripper: 5–30, HP: 3–30），目标集固定为 100

#### **优化阶段（BayesOpt Loop）**
- 初始上下文：5 个（Gripper）或 3 个（HP）随机设计
- 每轮选择下一个 $x_{t+1} = \arg\max_x \alpha(x, P_\theta(\cdot|C_t, x))$
- 使用 **Probability of Improvement (PI)** 作为 acquisition function
- 总迭代次数：30 次

#### **评估指标**
| 指标 | 描述 |
|------|------|
| **Target MSE** | 在目标集上的 $f(x)$ 预测均方误差 |
| **Avg. Normalized Best $f(x)$** | 归一化后的最优性能随迭代的变化 |
| **Average Regret** | 当前最优与全局最优之间的差距 |
| **Fraction Solved** | 达到近似最优（如 regret ≤ 0.5 或 0.01）的任务比例 |

---

### **基线方法对比**
| 基线 | 是否用 $h(x)$ | 是否多任务 | 说明 |
|------|---------------|------------|------|
| **Ours w/o h** | ❌ | ✅ | 消融版本，仅用 $f(x)$，其余结构相同 |
| **DGP** | ❌ | ✅ | Deep Kernel GP，SoTA 多任务 BayesOpt 方法 |
| **GP-H** | ✅ | ✅ | 多输出 GP，联合建模 $(E_\phi(h(x)), f(x))$ |
| **STGP** | ❌ | ❌ | 单任务 GP，仅在测试时拟合当前上下文 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 抓手设计任务（Gripper Design）**
| 方法 | 最终归一化性能 | 30步解决率（regret ≤ 0.5） |
|------|----------------|----------------------------|
| **Ours (full)** | **~0.89** | **67.2%** |
| Ours w/o h | ~0.82 | 58.0% |
| DGP | ~0.80 | ~55% |
| GP-H | ~0.78 | ~50% |
| STGP | ~0.75 | ~45% |

- **预测 MSE**：在 context size=5 时，**Ours MSE=170.9**，而 Ours w/o h=201.6，表明 $h(x)$ 显著提升预测精度。
- **收敛速度**：Ours 在前 10 步即显著领先，说明能更快识别高质量设计。

#### **(2) 超参调优任务（LCBench）**
| 方法 | 30步解决率（within 0.01 of max） |
|------|----------------------------------|
| **Ours (full)** | **89.7%** |
| Ours w/o h | 82.1% |
| DGP | ~80% |
| GP-H | ~75% |
| STGP | ~70% |

- 在较小 context（如 3–5）下，Ours 的优势尤为明显，体现其强大的 few-shot 学习能力。
- GP-H 表现不佳，说明简单地将 $h(x)$ 拼接进 GP 并不能有效利用其信息。

---

### **消融实验结果**
- **Ours vs. Ours w/o h**：在两个任务上均有显著差距，证明 **$h(x)$ 的价值不可替代**。
- **不同 $h(x)$ 编码方式**：论文尝试了多种编码器（CNN for tactile, MLP for curves），最终结构经过验证有效。
- **上下文大小影响**：随着 context 增大，所有方法性能上升，但 **Ours 在小 context 下优势最大**，凸显其 few-shot 能力。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **辅助信息 $h(x)$ 蕴含深层优化信号**：仅靠 $f(x)$ 很难区分失败原因，而 $h(x)$（如触觉分布、学习曲线趋势）提供了“为何失败”的线索，帮助模型推断改进方向。
2. ✅ **神经模型优于 GP 处理高维 $h(x)$**：GP-H 表现差，说明传统 GP 难以建模复杂异构的辅助信息；而神经网络可通过端到端学习提取有用特征。
3. ✅ **Few-shot 预测能力可迁移**：模型在训练时从未见过测试任务，却能在极少量样本下快速建立准确代理模型，实现高效优化。
4. ✅ **发现创造性设计策略**：模型学会利用物体缝隙、薄结构等进行稳定抓握（见 Fig. 8），甚至通过动态旋转调整姿态（Fig. 7），远超人类直觉。

---

### **方法的局限性**
- **依赖任务同分布假设**：测试任务需来自与训练任务相同的分布，对 OOD 任务泛化能力未知。
- **不支持主动选择 $h(x)$**：当前 $h(x)$ 是被动观测的，未考虑如何设计实验以获取最有信息量的 $h(x)$。
- **静态上下文机制**：未对上下文进行筛选或记忆压缩，可能受噪声数据影响。
- **离散搜索空间为主**：连续空间优化需结合梯度方法，尚未深入探索。

---

### **未来工作方向**
1. **预训练 + 微调 pipeline**：先用自监督任务（如重建 $h(x)$）预训练 $h(x)$ 编码器，再用于下游预测。
2. **OOD 任务检测与冷启动融合**：当模型不确定性高时，切换至在线学习模式（如 STGP）进行探索。
3. **长视界规划（Long-horizon Strategy）**：不再局限于一步最优，而是选择能最大化信息增益的设计，以更好理解任务结构。
4. **扩展更多模态 $h(x)$**：如语言描述、视频、分子图谱等，进一步丰富信息源。

---

> **总结**：本文开创性地将 **auxiliary information** 引入多任务设计优化，提出一种基于 **neural process + transformer** 的 few-shot 预测框架，在两个极具挑战性的领域实现了显著超越 SoTA 的性能，为 AI-driven 科学发现与工程设计提供了更强大、更贴近真实场景的工具。

</details>

---

### 16. [Bi-Level Prompt Optimization for Multimodal LLM-as-a-Judge](https://arxiv.org/abs/2602.11340)

**Authors**: Bo Pan, Xuan Kan, Kaitai Zhang, Yan Yan, Shunwen Tan, Zihao He, Zixin Ding, Junjie Wu, Liang Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11340v1  

#### Abstract
Large language models (LLMs) have become widely adopted as automated judges for evaluating AI-generated content. Despite their success, aligning LLM-based evaluations with human judgments remains challenging. While supervised fine-tuning on human-labeled data can improve alignment, it is costly and ...

---

### 17. [AgentLeak: A Full-Stack Benchmark for Privacy Leakage in Multi-Agent LLM Systems](https://arxiv.org/abs/2602.11510)

**Authors**: Faouzi El Yagoubi, Ranwa Al Mallah, Godwin Badu-Marfo  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11510v1  

#### Abstract
Multi-agent Large Language Model (LLM) systems create privacy risks that current benchmarks cannot measure. When agents coordinate on tasks, sensitive data passes through inter-agent messages, shared memory, and tool arguments; pathways that output-only audits never inspect. We introduce AgentLeak, ...

---

### 18. [AlphaPROBE: Alpha Mining via Principled Retrieval and On-graph biased evolution](https://arxiv.org/abs/2602.11917)

**Authors**: Taian Guo, Haiyang Shen, Junyu Luo, Binqi Chen, Hongjun Ding, Jinsheng Huang, Luchen Liu, Yun Ma, Ming Zhang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11917v1  

#### Abstract
Extracting signals through alpha factor mining is a fundamental challenge in quantitative finance. Existing automated methods primarily follow two paradigms: Decoupled Factor Generation, which treats factor discovery as isolated events, and Iterative Factor Evolution, which focuses on local parent-c...

---

### 19. [Pedagogically-Inspired Data Synthesis for Language Model Knowledge Distillation](https://arxiv.org/abs/2602.12172)

**Authors**: Bowei He, Yankai Chen, Xiaokun Zhang, Linghe Kong, Philip S. Yu, Xue Liu, Chen Ma  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.12172v1  

#### Abstract
Knowledge distillation from Large Language Models (LLMs) to smaller models has emerged as a critical technique for deploying efficient AI systems. However, current methods for distillation via synthetic data lack pedagogical awareness, treating knowledge transfer as a one-off data synthesis and trai...

---

### 20. [Improving HPC Code Generation Capability of LLMs via Online Reinforcement Learning with Real-Machine Benchmark Rewards](https://arxiv.org/abs/2602.12049)

**Authors**: Ryo Mikasa, Shun-ichiro Hayashi, Daichi Mukunoki, Tetsuya Hoshino, Takahiro Katagiri  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.12049v1  

#### Abstract
Large language models (LLMs) have demonstrated strong code generation capabilities, yet the runtime performance of generated code is not guaranteed, and there have been few attempts to train LLMs using runtime performance as a reward in the HPC domain. We propose an online reinforcement learning app...

---

### 21. [AgentNoiseBench: Benchmarking Robustness of Tool-Using LLM Agents Under Noisy Condition](https://arxiv.org/abs/2602.11348)

**Authors**: Ruipeng Wang, Yuxin Chen, Yukai Wang, Chang Wu, Junfeng Fang, Xiaodong Cai, Qi Gu, Hui Su, An Zhang, Xiang Wang, Xunliang Cai, Tat-Seng Chua  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11348v1  

#### Abstract
Recent advances in large language models have enabled LLM-based agents to achieve strong performance on a variety of benchmarks. However, their performance in real-world deployments often that observed on benchmark settings, especially in complex and imperfect environments. This discrepancy largely ...

---

### 22. [Gaia2: Benchmarking LLM Agents on Dynamic and Asynchronous Environments](https://arxiv.org/abs/2602.11964)

**Authors**: Romain Froger, Pierre Andrews, Matteo Bettini, Amar Budhiraja, Ricardo Silveira Cabral, Virginie Do, Emilien Garreau, Jean-Baptiste Gaya, Hugo Lauren\c{c}on, Maxime Lecanu, Kunal Malkan, Dheeraj Mekala, Pierre M\'enard, Gerard Moreno-Torres Bertran, Ulyana Piterbarg, Mikhail Plekhanov, Mathieu Rita, Andrey Rusakov, Vladislav Vorotilov, Mengjue Wang, Ian Yu, Amine Benhalloum, Gr\'egoire Mialon, Thomas Scialom  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11964v1  

#### Abstract
We introduce Gaia2, a benchmark for evaluating large language model agents in realistic, asynchronous environments. Unlike prior static or synchronous evaluations, Gaia2 introduces scenarios where environments evolve independently of agent actions, requiring agents to operate under temporal constrai...

---

### 23. [Advancing AI Trustworthiness Through Patient Simulation: Risk Assessment of Conversational Agents for Antidepressant Selection](https://arxiv.org/abs/2602.11391)

**Authors**: Md Tanvir Rouf Shawon, Mohammad Sabik Irbaz, Hadeel R. A. Elyazori, Keerti Reddy Resapu, Yili Lin, Vladimir Franzuela Cardenas, Farrokh Alemi, Kevin Lybarger  
**Category**: cs.CL  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11391v1  

#### Abstract
Objective: This paper introduces a patient simulator designed to enable scalable, automated evaluation of healthcare conversational agents. The simulator generates realistic, controllable patient interactions that systematically vary across medical, linguistic, and behavioral dimensions, allowing an...

---

### 24. [CADET: Context-Conditioned Ads CTR Prediction With a Decoder-Only Transformer](https://arxiv.org/abs/2602.11410)

**Authors**: David Pardoe, Neil Daftary, Miro Furtado, Aditya Aiyer, Yu Wang, Liuqing Li, Tao Song, Lars Hertel, Young Jin Yun, Senthil Radhakrishnan, Zhiwei Wang, Tommy Li, Khai Tran, Ananth Nagarajan, Ali Naqvi, Yue Zhang, Renpeng Fang, Avi Romascanu, Arjun Kulothungun, Deepak Kumar, Praneeth Boda, Fedor Borisyuk, Ruoyan Wang  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11410v1  

#### Abstract
Click-through rate (CTR) prediction is fundamental to online advertising systems. While Deep Learning Recommendation Models (DLRMs) with explicit feature interactions have long dominated this domain, recent advances in generative recommenders have shown promising results in content recommendation. H...

---

### 25. [TimeSynth: A Framework for Uncovering Systematic Biases in Time Series Forecasting](https://arxiv.org/abs/2602.11413)

**Authors**: Md Rakibul Haque, Vishwa Goudar, Shireen Elhabian, Warren Woodrich Pettine  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11413v1  

#### Abstract
Time series forecasting is a fundamental tool with wide ranging applications, yet recent debates question whether complex nonlinear architectures truly outperform simple linear models. Prior claims of dominance of the linear model often stem from benchmarks that lack diverse temporal dynamics and em...

---

### 26. [TUBO: A Tailored ML Framework for Reliable Network Traffic Forecasting](https://arxiv.org/abs/2602.11759)

**Authors**: Zhihang Yuan, Leyang Xue, Waleed Ahsan, Mahesh K. Marina  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11759v1  

#### Abstract
Traffic forecasting based network operation optimization and management offers enormous promise but also presents significant challenges from traffic forecasting perspective. While deep learning models have proven to be relatively more effective than traditional statistical methods for time series f...

---

### 27. [Temperature as a Meta-Policy: Adaptive Temperature in LLM Reinforcement Learning](https://arxiv.org/abs/2602.11779)

**Authors**: Haoran Dang, Cuiling Lan, Hai Wan, Xibin Zhao, Yan Lu  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11779v1  

#### Abstract
Temperature is a crucial hyperparameter in large language models (LLMs), controlling the trade-off between exploration and exploitation during text generation. High temperatures encourage diverse but noisy outputs, while low temperatures produce focused outputs but may cause premature convergence. Y...

---

### 28. [Latent Generative Solvers for Generalizable Long-Term Physics Simulation](https://arxiv.org/abs/2602.11229)

**Authors**: Zituo Chen, Haixu Wu, Sili Deng  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11229v1  

#### Abstract
We study long-horizon surrogate simulation across heterogeneous PDE systems. We introduce Latent Generative Solvers (LGS), a two-stage framework that (i) maps diverse PDE states into a shared latent physics space with a pretrained VAE, and (ii) learns probabilistic latent dynamics with a Transformer...

---

### 29. [Benchmark Health Index: A Systematic Framework for Benchmarking the Benchmarks of LLMs](https://arxiv.org/abs/2602.11674)

**Authors**: Longyuan Zhu, Hairan Hua, Linlin Miao, Bing Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11674v1  

#### Abstract
Large Language Models (LLMs) are advancing rapidly, yet the benchmarks used to measure this progress are becoming increasingly unreliable. Score inflation and selective reporting have eroded the authority of standard benchmarks, leaving the community uncertain about which evaluation results remain t...

---

### 30. [Text2GQL-Bench: A Text to Graph Query Language Benchmark [Experiment, Analysis & Benchmark]](https://arxiv.org/abs/2602.11745)

**Authors**: Songlin Lyu, Lujie Ban, Zihang Wu, Tianqi Luo, Jirong Liu, Chenhao Ma, Yuyu Luo, Nan Tang, Shipeng Qi, Heng Lin, Yongchao Liu, Chuntao Hong  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11745v1  

#### Abstract
Graph models are fundamental to data analysis in domains rich with complex relationships. Text-to-Graph-Query-Language (Text-to-GQL) systems act as a translator, converting natural language into executable graph queries. This capability allows Large Language Models (LLMs) to directly analyze and man...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- kv cache, offload, State Space, SSM, framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
